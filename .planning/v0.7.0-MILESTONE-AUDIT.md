---
milestone: v0.7.0
title: 方案编排（需求 → 主方案）
audited: 2026-06-16
auditor: gsd-verifier (goal-backward milestone audit)
status: passed
phases_audited: [36, 37, 38, 39, 40, 41, 42]
requirements_total: 19
requirements_complete: 19
requirements_partial: 0
requirements_missing: 0
invariants_checked: [INV-2, INV-5, INV-6]
invariants_held: [INV-2, INV-5, INV-6]
deferrals_acceptable: true
blocking_gaps: 0
method: goal-backward
---

# v0.7.0 方案编排 — 里程碑完成度审计

**里程碑目标：** 把「需求 → 一份高质量多仓主技术方案」做成可复用的 map-reduce 多 agent 编排引擎（拆分 → 路由 → 召回 → 澄清 → 并行调研 → 架构师融合），并立 canonical `TechnicalPlan` 脊柱、编排状态机 `PlanSession`、事件 taxonomy——作为 v0.8 多仓编码 / v0.9 SDD 的方案底座。

**审计方法：** goal-backward——从里程碑 19 项需求 + 3 不变量 + 端到端流水线倒推，逐项核验代码与测试证据（非「任务做完」），并交叉核对 7 个 phase 的 VERIFICATION.md / REVIEW.md。已对关键产物（编排 engine、状态机 `_ALLOWED` 表、薄共享 helper、8 个 migration、核心模型/adapter）做源码 spot-check。

**结论：status = passed。** 19/19 需求均有实质交付（代码 + 测试证据），3 个不变量经 grep 守护持续成立，7 个 phase 的全部 code review BLOCKER 已逐条修复。剩余 deferred 项为：① 真实 LLM/容器/网络端到端验收（mock 全覆盖逻辑，沿用历史里程碑惯例）；② 一处已登记的 chat 入口 deep-research 自动回流接线缺口（主入口「工作流先行」端到端已跑通）；③ v0.7 有意收窄的 HITL 答复→affected 映射。三类均**不阻断里程碑目标**，判为可接受顺延。

---

## 1. 需求覆盖矩阵（19/19 Complete）

| Requirement | Phase | 状态 | 证据（VERIFICATION + 代码） |
|-------------|-------|------|------|
| **PF-01** 工具名漂移修复 + fail-loud | 36 | ✅ Complete | 36-V SC-1：`search_code`→`search_repository_code` 全量改名，`build_langchain_tools` 未知工具 `raise ValueError`；`rg "search_code"` 零残留；`test_plan_generation_tools_guard` / `test_langchain_adapter` 绿。 |
| **PF-02** verify_plan 对齐 execution_plan | 36 | ✅ Complete | 36-V SC-2：`verify_plan.py` 必填 `["title","execution_plan"]` 逐项校验 repository_id/coding_instruction；`test_verify_plan` 9→13 passed（含 CR-01 非 list fail-safe 加固）。 |
| **ORCH-01** 可复用 `ai_plan_research` 编排 engine | 36 | ✅ Complete | 36-V SC-4：`PlanOrchestrationEngine` 仅接 session_service + 4 可注入 stage 协议、不持 IO 对象（入口无关）；`engine.py` 无 `.status=` 直写（守护测试）；`test_plan_orchestration_engine` 7 passed。 |
| **ORCH-02** `PlanSession` 可持久化可恢复状态机 | 36 | ✅ Complete | 36-V SC-3：`plan_session.py` 8 态模型 + `0009` migration；`_ALLOWED` 逐行对齐 §14（spot-check 确认）；`transition` 单一入口 + resume 从 DB 重取；`test_plan_session_*` 20 passed。 |
| **PLAN-01** canonical TechnicalPlan/PlanVersion + INV-2 | 37 | ✅ Complete | 37-V SC-1：`technical_plan.py` 三模型 + `0010` migration；`work_item` nullable SET_NULL；版本链 supersedes + content_hash；`test_technical_plan_models` 6 passed。 |
| **PLAN-02** TechnicalPlanService 唯一写入入口（INV-6） | 37 | ✅ Complete | 37-V SC-2：`create_from`/`add_version`/`archive`/`resolve`/`link`；INV-6 grep 守护除 service 外无旁路写；`test_technical_plan_inv6_guard` 绿。 |
| **PLAN-03** 3 路径软链 + read-time lazy 迁移 | 37 | ✅ Complete | 37-V SC-3/4：chat/mcp `canonical_plan_id` UUID 软链 + workflow `PlanExternalRef`；lazy 三路径 + chat eager 投影示范；冲突以 canonical 为准、归档不级联。mcp/workflow eager 投影顺延（lazy 兜底覆盖读路径）。 |
| **ROUTE-01** RepoRouterV2 路由接入 | 38 | ✅ Complete | 38-V SC-1：`RepoRouterV2Adapter` → `PlanSession.routing`（候选仓+confidence）；§14 routed 转移；`test_repo_router_adapter` 5 例。 |
| **RECALL-01** 历史召回接入 | 38 | ✅ Complete | 38-V SC-2：`DeliveryKnowledgeRecallAdapter` 复用 `search_similar`，created_by 透传 fail-closed；CR-01 async lazy-FK 已修；`test_recall_adapter` 5 例。 |
| **RESEARCH-01** filter_then_container 并行调研 + PartialPlan | 39 | ✅ Complete | 39-V SC-1：high/medium→隔离容器、low→轻量 partial；`parse_partial_plan_content` 产 §7 PartialPlan；`test_research_adapter`/`_aggregation`/`_callback` 绿。**真实容器 E2E deferred（mock 覆盖）。** |
| **RESEARCH-02** 单仓重试隔离 | 39 | ✅ Complete | 39-V SC-2：`retry_task` 条件更新仅命中目标 task、不动其他/不改 session.status；`test_retry_task_isolation`。 |
| **RESEARCH-03** 重索引 → PartialPlan stale 重跑 | 39 | ✅ Complete | 39-V SC-3：`invalidate_for_repo` 置 stale + indexer FINALIZING best-effort 钩子；barrier 终态不含 stale 阻塞融合；`test_research_stale_hook`。 |
| **MERGE-01** 架构师融合 → MergedPlan 落 canonical | 40 | ✅ Complete | 40-V SC-1：`ArchitectMergeAdapter` 收齐 partial → 可注入合成器 → `create_from(origin=orchestration)` 落 canonical；`test_architect_merge_adapter`。**真实 LLM E2E deferred（mock synthesizer 覆盖）。** |
| **MERGE-02** PlanValidator 5 类拦截 + §14 回退 | 40 | ✅ Complete | 40-V SC-2：契约一致/依赖成环（DFS 三色）/迁移顺序/发布顺序/缺回滚 5 项校验；CR-01 schema 闸口已补、WR-01 空 plan 拦截已补；`test_plan_validator` 绿。 |
| **MERGE-03** 跨仓依赖显式建模 | 40 | ✅ Complete | 40-V SC-3：`dependency_dag` + `execution_plan[].dependencies` 落 canonical content；e2e 断言 `dependency_dag={repoB:[repoA]}`。 |
| **CLARIFY-01** HITL 澄清 + affected 重跑 | 41 | ✅ Complete | 41-V SC-1：`Clarification` 模型 + `ClarificationService` + `0016`；CR-01 单轮 guard 已修（答后不无限追问）；`mark_stale` 仅 affected；`test_e2e_clarification_loop_reruns_only_affected`。HITL 答复→affected 映射 v0.7 默认为空（IN-02 有意收窄，见 §3）。 |
| **ENTRY-01** 工作流入口端到端（主入口，工作流先行） | 41 | ✅ Complete | 41-V SC-2：`AIPlanResearchNode` 注入真实 adapters 驱动 engine；CR-02 researching waiting_event resume 通路已修（透传 node_execution）；`test_e2e_requirement_to_merged_plan_with_cross_repo_deps` → done + canonical MergedPlan 带跨仓依赖。**IO 边界 mock。** |
| **EVENT-01** §15 事件 taxonomy 全程持久化 | 41 | ✅ Complete | 41-V SC-3：`PlanSessionEvent` append-only + `0015` + `event_taxonomy` 常量集；全 emit 点引用常量；`test_event_taxonomy_alignment`（无裸字面量 + 覆盖性反查）。 |
| **ENTRY-02** Chat 入口薄封装复用同一 engine | 42 | ✅ Complete | 42-V SC-1/2/3：薄 helper `start_orchestration`/`build_orchestration_engine` 被 workflow + chat **两入口共引**（grep 守护）；`test_chat_and_workflow_entries_yield_equivalent_merged_plan`（同产物 + 同 §15 事件序列）；INV-2 chat work_item=None。**deep-research 自动回流接线缺口 deferred（见 §3 WR-01）。** |

**覆盖率：19/19 Complete。** 0 Partial、0 Missing。每条需求均有实现文件 + 测试 + phase VERIFICATION 三级证据；spot-check 确认产物非桩（engine/状态机/helper/模型/migration 实存且接线）。

---

## 2. 不变量核验（INV-2 / INV-5 / INV-6 全部成立）

| 不变量 | 要求 | 判定 | 证据 |
|--------|------|------|------|
| **INV-2** 方案可追溯到 WorkItem，chat 自然语言允许 null 但显式标记 | ✅ 成立 | `TechnicalPlan.work_item = FK(null=True, SET_NULL)`（37）；chat 入口显式传 `work_item=None` + `entrypoint=chat` 落 `PlanSession`（42）；融合落 canonical 时 work_item_id None 经 by-id 查合法；`test_..._inv2_null_work_item` 守护。null 由「来源标记（entrypoint=chat / origin=orchestration）」显式表达，非裸 null。 |
| **INV-5** 对外事件为 progress/trace 非模型私有 CoT | ✅ 成立 | §15 事件 payload 仅含 `repo_id/confidence`、`hits` 计数、`plan_version_id`、`reasons:[check]` 等稳定词表，**不含 reasoning/CoT**（38-V/40-V/41-V 逐条核验）；`knowledge.recalling` 仅计数不外泄命中明细；降级 report 的 `str(exc)` 仅入库不进事件（40-IN-03）。 |
| **INV-6** 方案解析/创建只经 service，禁旁路写表 | ✅ 成立 | grep 守护覆盖全部脊柱写入：`TechnicalPlan`/`PlanVersion`（37 `test_technical_plan_inv6_guard`）、`PlanSession.status`（36 engine 纯度 + `_apply_transition_sync` 条件更新）、`RepoResearchTask`/`PartialPlan`（39 `test_research_inv6_guard`）、`ArchitectMerge`（40 `test_architect_merge_inv6_guard`）、`Clarification`（41 grep 守护）。engine 全程仅经 `transition`，无 `.status=` 直写（`test_engine_does_not_write_status_directly` 跨 phase 持续绿）。 |

---

## 3. Deferred / Tech-Debt 台账

> 分类原则：**acceptable-deferred** = 不阻断里程碑目标、与历史惯例一致、逻辑已被 mock/单测坐实；**tech-debt** = 已登记、需后续接线但不影响主路径可达。

| # | 项 | 来源 | 分类 | 判据 |
|---|----|------|------|------|
| D-1 | 真实 LLM / 真实调研容器 / 真实网络的端到端验收（路由/召回 LLM、容器 dispatch、容器回调、merge 合成器均 IO 边界 mock） | 39/40/41/42 V + REVIEW | **acceptable-deferred** | 全路径逻辑（pass/fail/降级/限次回退/事件/canonical 落库/resume 往返）以 mock 全覆盖；沿用 v0.2 RTOOL 容器 E2E / v0.6 既有 deferred 惯例（本地无 runner+docker+真实凭证）。不阻断目标。 |
| D-2 | **Chat 入口 deep-research 自动回流接线缺口**：chat fire-and-forget 编排进 researching 且容器在途时，容器完成后无消费者驱动 engine researching→merging→done（chat 无 node_execution，`_schedule_workflow_resume` 跳过；`_schedule_agent_session_resume` 对 plan_research 短路） | 42 REVIEW WR-01 | **acceptable-deferred（tech-debt）** | 主入口「工作流先行」(ENTRY-01) resume 通路完整（41 CR-02 透传 node_execution 已修，端到端跑通到 done）；chat happy-path（全 light 仓 / barrier 直通）亦到 done。仅 chat **deep-research** 路径需后续接线。placeholder 文案已如实化 + 缺口已登记 `deferred-items.md`。ENTRY-02 SC（同 engine、同产物、INV-2）均 PASS。**里程碑「工作流先行、Chat 薄封装」的设计取向使此为可接受顺延，但应作为 v0.8 接线首项 tech-debt 跟踪。** |
| D-3 | HITL 答复 → affected_partials 映射：默认 `affected_task_ids=[]`，纯 HITL 答复走「纯解除挂起」不触发 affected 重跑 | 41 REVIEW IN-02 | **acceptable-deferred** | v0.7 有意收窄（「答复关联候选仓」为后置能力）；§14「仅 affected 重跑」经注入 policy / merge-reclarify / e2e 注入路径仍真实工作（`test_e2e_clarification_loop_reruns_only_affected`）。CLARIFY-01 SC 不依赖默认 affected 非空。 |
| D-4 | canonical 被硬删后悬空软链回退重建（resolve 当前直接 PlanNotFound） | 37 REVIEW IN-02 | **tech-debt（低风险）** | 非平凡（需清空 canonical_plan_id + 重建分支）；正常归档走 status=archived 不删行，硬删为边角。留后续。 |
| D-5 | mcp/workflow 旧入口 eager 投影（chat eager 已示范） | 37-V Deferred | **acceptable-deferred** | read-time lazy 迁移已兜底三路径读路径；eager 仅性能优化，非正确性缺口。40/41 接编排时顺带。 |
| D-6 | 预存在、与本里程碑无关的测试失败：3 个 coding_session E2E（本地缺 Anthropic 凭证）+ `test_search_repository_code_input_schema_snapshot` 快照文本漂移 | 39-V / 42-V Notes | **acceptable（非回归）** | 经 base commit 复跑确认为环境性/预存在，非 v0.7 引入；不影响里程碑产物。 |
| D-7 | WS 实时 trace 推送 / plan-session / trace 富前端可视化 | 41-V / 42-V Deferred | **acceptable-deferred（范围外）** | EVENT-01 仅要求产出+持久化事件（已达）；对外 adapter 透出为 v0.11 主题。 |

**Code review 收口状态：** 7 个 phase 全部 BLOCKER（CR-*）均已修复——36(CR-01)/38(CR-01)/39(CR-01)/40(CR-01)/41(CR-01+CR-02) 逐条原子提交 + 回归测试；WARNING/INFO 均 fixed 或 accepted/deferred 并记录理由。**无开放 BLOCKER。**

---

## 4. 端到端流水线确认

### 主入口 ENTRY-01（工作流先行）— ✅ 端到端跑通
`AIPlanResearchNode` 建 `PlanSession(entrypoint=workflow)` → 注入真实 5 adapters 驱动 `PlanOrchestrationEngine.advance`：拆分(decomposing) → 路由(RepoRouterV2) → 召回(DeliveryKnowledgeRecall) → 澄清(Clarify，单轮 guard) → 并行调研(ResearchDispatch，researching 段 waiting_event + node_execution resume 通路) → 架构师融合(ArchitectMerge + PlanValidator) → done。终态产出 canonical `PlanVersion`，content 为 §7 `MergedPlan`，**跨仓依赖显式存在**（`dependency_dag={repoB:[repoA]}`、`execution_plan[t2].dependencies=["t1"]`、`dependencies_on_other_repos=["ContractX"]`）。证据：`test_e2e_requirement_to_merged_plan_with_cross_repo_deps`（真实 engine + 全部真实 service，仅 IO 边界 mock）。

### 次入口 ENTRY-02（Chat 薄封装）— ✅ 复用同一 engine，产物一致
chat 工具 `start_plan_research` 与 workflow 节点**共引同一** `start_orchestration` / `build_orchestration_engine`（grep 守护成立，证明不造两套编排）。`test_chat_and_workflow_entries_yield_equivalent_merged_plan`：同需求经两入口分别驱动到 done，`PlanVersion.content` dict 相等 + §15 事件 taxonomy 序列相同。INV-2：chat work_item=None 显式可追溯。**唯一缺口**：deep-research 容器在途时的 chat 自动回流（D-2）——happy-path 与主入口已闭环。

---

## 5. 里程碑小结

v0.7.0「方案编排」**达成其定义的完成标准**：

- **可复用 map-reduce 编排引擎已立**：`PlanOrchestrationEngine` 入口无关 + 4 可注入 stage 协议，工作流与 Chat 经同一薄 helper 复用同一 engine（不造两套），拆分→路由→召回→澄清→并行调研→架构师融合六段流水线全程贯通。
- **canonical 方案脊柱已立**：`TechnicalPlan`/`PlanVersion` + `TechnicalPlanService` 唯一写入入口（INV-6）+ 旧 3 路径软链/lazy 迁移；架构师融合产物经 service 落 canonical，可追溯 WorkItem（INV-2）。
- **编排状态机 + 可靠恢复**：`PlanSession` 8 态机按 §14 推进、可持久化可恢复、条件更新防 TOCTOU 双推进；单仓调研失败隔离重试、重索引 stale 重跑。
- **架构师融合非「更贵的总结器」**：`PlanValidator` 5 类跨仓校验（契约一致/依赖成环/迁移顺序/发布顺序/缺回滚）+ 限次回退；跨仓依赖 `dependency_dag` 显式建模，为 v0.8 wave 编码铺底。
- **事件 taxonomy 已沉淀**：§15 统一信封 trace 事件全程持久化，稳定词表为 v0.11 对外 adapter 备料（INV-5 progress/trace 非 CoT）。

**剩余顺延项均不阻断目标**：真实环境 E2E（mock 全覆盖，历史惯例）、chat deep-research 自动回流（主入口已闭环，已登记 tech-debt）、HITL 答复→affected 映射（v0.7 有意收窄）。建议将 **D-2（chat deep-research resume 接线）** 列为 v0.8 开工首项 tech-debt 一并消化（v0.8 多仓 wave 编码本就需要 plan_session → 工作流/会话 resume 的通用回流通路）。

**审计裁定：PASSED — 可归档。** 19/19 需求交付，3 不变量成立，无阻断缺口；顺延项可接受且已登记跟踪。

---

*Audited: 2026-06-16 · Auditor: Claude (gsd-verifier, goal-backward milestone audit)*
*交叉核对：7 phase VERIFICATION.md（全 passed）+ 7 phase REVIEW.md（全 findings resolved/fixed/accepted）+ 源码 spot-check（engine/状态机/helper/8 migration/核心模型）。*
