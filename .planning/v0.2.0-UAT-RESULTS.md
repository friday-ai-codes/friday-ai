---
milestone: v0.2.0
tested: 2026-06-10
method: Chrome DevTools MCP 浏览器自动化（真实前后端 + 真实 LLM 流式）
accounts: admin（临时改密后已恢复原哈希）、uat_user_a、uat_user_b（保留于 dev DB）
status: passed_with_notes
---

# v0.2.0 人工验收结果（浏览器自动化执行）

## 结果总览：11/12 通过，1 项部分通过；Phase 11 容器项 3 条未跑（环境依赖）

| # | Phase | 验收项 | 结果 | 证据 |
|---|-------|--------|------|------|
| 3 | 7 | Web cookie-JWT 登录回归（认证类 PAT-first 重排后） | ✅ | uat_user_a 登录成功，首页/受保护 API 200 |
| 4 | 8 | 多账户隔离：列表只见自己；越权 URL 404 不泄漏 | ✅ | A/B 列表互不可见；B 访问 A 会话（含 fork 副本）API 404、自己的 200；A 访问 B 会话 404、页面零内容泄漏 |
| 5 | 8 | 编辑消息→fork→续聊（CR-01 修复点） | ✅ | A 编辑历史消息 → fork 出「UAT-A 的会话一（编辑）」归 A、即时上列表，编辑消息流式发送成功（deepseek 真实回复） |
| 6 | 9 | 管理员后台跨用户浏览 + 非管理员被挡 | ✅ | admin 后台列出全部 4 个会话（owner 列正确）；uat_user_b 访问 /admin/conversations 被守卫挡到 /403，API 403 |
| 7 | 9 | fork 到我的名下 → 续聊 | ✅ | fork B 的会话 → 「UAT-B 的会话一（管理员副本）」归 admin、消息完整复制 → 编辑重发流式回复成功 |
| 8 | 9 | 只读详情视觉 | ✅ | 详情对话框纯只读回放（无输入框/发送/编辑/删除），markdown 正常渲染 |
| ISO-03 | 8 | 管理员普通 AI 对话默认只见自己 | ✅ | admin（is_superuser）普通会话列表 API 返回 0 条，UI 不见 A/B 会话 |
| 1 | 6 | 一次性明文展示 + 复制 | ⚠️ 部分 | 明文一次性弹窗 ✅、关闭后仅指纹 `friday_pat_t…JK4Q` 不可再获取 ✅；剪贴板复制在自动化隔离上下文读不到（权限限制），**点「复制」粘贴验证留 10 秒人工确认** |
| 2 | 6 | 永不过期 amber 警告非阻塞 | ✅ | amber 风险提示出现、创建按钮可用（截图 /tmp/uat-06-never-expire-warning.png） |
| 9 | 10 | 绑定全流程 | ✅ | 下拉仅列有效令牌（名称+指纹无明文）→ 绑定成功 toast → 刷新持久 → 解绑（二次确认）→ 恢复未绑定；全程页面无完整明文（regex 校验） |
| 10-12 | 11 | 真实容器 SDK 调用 / docker inspect 无明文 / 在途吊销 graceful | ⏸ 未跑 | 需 Docker+runner，且 `_resolve_user_pat` 休眠需先接通 user_pat 注入（已知 follow-up） |
| 13-14 | v0.1.0 | setup 向导 E2E | ⏸ 未跑 | 需空库实例，与现有 dev DB 冲突；保持 v0.1.0 deferred |

## 备注

- 测试数据保留在 dev DB：uat_user_a/uat_user_b（密码 UatUser#2026!）、UAT 测试空间、UAT 会话与 fork 副本，便于后续手测；测试令牌 uat-browser-token 已吊销。
- admin 密码已恢复为原始哈希（测试期间临时重置）。
- 唯一真正剩余的人工动作：①「复制」按钮粘贴一次（10 秒）；② Phase 11 三项容器 E2E（依赖 follow-up：实时明文 PAT 通道）。
