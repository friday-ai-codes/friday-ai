---
milestone: v0.12.0
audited: 2026-06-20
status: tech_debt
scores:
  requirements: 16/16
  phases: 5/5
  integration: integration_ok
  flows: 3/3
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: 60-durable
    items:
      - "human_needed: 真实 Postgres postgres_queue 后端测试实跑 / forged-heartbeat rescue / 真实 kill-worker E2E / GitHub Actions postgres-queue job 绿灯（需真实 Postgres + CI）"
  - phase: 61-migrate
    items:
      - "human_needed: 真实升级迁移（migrate_resumable_to_durable on Postgres）/ queueing_lock 去重 / 多副本 reconcile 不误杀（需真实 Postgres）"
      - "pre-existing 无关失败: test_index_retry_resume / test_index_history_changed_files（pytest-django infra，非本里程碑回归）"
  - phase: 62-crawl
    items:
      - "human_needed: 真实容器/Pod 重启后队列恢复 + durable job 自动续跑 / postgres_queue 并发 at-least-once（需真实 Postgres + 容器重启）"
  - phase: 63-deploy
    items:
      - "human_needed: 真实 k8s 滚动更新 worker SIGTERM drain / KEDA 真实伸缩 / compose up -d 升级既有部署 / 真实 GitLab·GitHub·飞书外部副作用去重（需真实集群/平台）"
      - "accepted limitation: migration-job 明文 secret（既有，范围外；本次已补 FRIDAY_PROCESS_ROLE=migrate）"
  - phase: 64-runner
    items:
      - "human_needed: 真实 k0s/containerd 集群经 k8s Job executor 跑通任务容器 / 日志流式·退出码·Pod 清理 / 失败重试 + activeDeadline 兜底（需真实集群）"
      - "accepted limitation: HITL-answer-in-k8s 与 ReadContainerFile k8s 产物读取需 task 容器改动 / RWX 卷（显式 out-of-scope，留 v2）；Job env 明文 secret 与 docker env 注入行为一致（RBAC 缓解，Secret-based env 留 v2）"
  - phase: pre-existing
    items:
      - "test_plan_session_inv6_guard 持续失败：INV-6 守护正则误命中 chat/conversation_service.py 中文注释（Phase 36 产物 + 工作树既有未提交改动），非 v0.12.0 任何阶段触改，全程确认为 pre-existing 误报"
---

# Milestone v0.12.0 — 弹性任务底座（durable 任务队列与多副本就绪）审计报告

**审计时间:** 2026-06-20
**状态:** tech_debt（无 BLOCKER；16/16 需求代码层满足、integration_ok；遗留为真机/真实平台运行期人工验收 + 显式 v2 限制）

## 1. 需求覆盖（3-source 交叉核对）

16/16 需求满足（REQUIREMENTS.md traceability 全 Complete；各 phase VERIFICATION 代码层 must-haves 全过；各 SUMMARY requirements 列出）。

| Phase | Requirements | 状态 |
|-------|--------------|------|
| 60 | DURABLE-01, DURABLE-02, DURABLE-03, DURABLE-04 | ✅ satisfied（code-verified；runtime human_needed） |
| 61 | MIGRATE-01, MIGRATE-02, IDEMP-01 | ✅ satisfied（code-verified；runtime human_needed） |
| 62 | CRAWL-01, CRAWL-02, PAGEIDX-01 | ✅ satisfied（code-verified；runtime human_needed） |
| 63 | DEPLOY-01, DEPLOY-02, DEPLOY-03, IDEMP-02 | ✅ satisfied（render/static-verified；runtime human_needed） |
| 64 | RUNNER-01, RUNNER-02 | ✅ satisfied（code-verified；real-cluster human_needed） |

无 unsatisfied、无 orphaned 需求。

## 2. 阶段验证汇总

| Phase | Plans | VERIFICATION status | 自动化测试 |
|-------|-------|---------------------|-----------|
| 60-durable | 4/4 | human_needed | tests/durable 31 passed |
| 61-migrate | 4/4 | human_needed | durable+resumable 66 passed |
| 62-crawl | 3/3 | human_needed | 后端 495 passed + 前端 vitest 8 passed |
| 63-deploy | 3/3 | human_needed | 536 passed + helm lint 0 failed + fail-closed render |
| 64-runner | 2/2 | human_needed | runner go build/vet/test green + fake-clientset 单测 |

所有 human_needed 项均为"实现/渲染/静态测试就位、仅真机/真实平台运行期待验"，与本项目既有里程碑（v0.8.0 容器 E2E 延后）惯例一致。

## 3. 跨阶段集成（integration_ok）

[gsd-integration-checker 裁决 integration_ok]：durable 底座（60）被 61/62 端到端正确消费，`DurableTaskService.defer`/显式任务名/队列常量/`has_active_by_key` 全链一致，no-direct-import 适配层隔离成立；FRIDAY_PROCESS_ROLE 门禁与 63 compose/helm workload 拆分对齐，周期 rescue 由 worker periodic deferrer 单例承载（非 scheduler，无双跑）；64 runner executor 经不变 `ws.ExecutorService` 接口接入、docker 默认零回归；配置键跨阶段无矛盾。

集成核查 WARNING-1（helm migration-job 缺 FRIDAY_PROCESS_ROLE=migrate）**已于审计期修复**（commit 见 git log，helm 渲染含 migrate 角色、lint 0 failed）。

### E2E 叙事流（3/3 闭合）
- (a) repo index → durable job → IndexHistory 真相源。
- (b) 粘贴链接 → crawl_ingest durable → BatchIngestPanel 后端恢复。
- (c) 多副本 worker 扩缩（KEDA min≥1）+ 优雅 drain（grace 110<term 120）+ leader rescue（queueing_lock 单例）。

## 4. Nyquist 合规

| Phase | VALIDATION.md | nyquist_compliant |
|-------|---------------|-------------------|
| 60 | 存在 | true |
| 61 | 存在 | true |
| 62 | 存在 | true |
| 63 | 存在 | true |
| 64 | 存在 | true |

## 5. 结论

无 BLOCKER。16/16 需求代码层满足，跨阶段集成连贯，3 条 E2E 流闭合。遗留 tech_debt 为真机/真实平台（Postgres / k8s / k0s+containerd / 真实 Git 平台·飞书）运行期人工验收，以及显式 v2 限制（k8s HITL answer / ReadContainerFile 产物读取 / Job env Secret 化）。按 autonomous lifecycle 接受 tech debt，推进 complete-milestone。
