---
phase: 96-external-deps-search-overview
plan: 04
type: execute
wave: 2
depends_on: [96-02]
files_modified:
  - web/src/api/knowledge.ts
  - web/src/pages/knowledge/index.vue
  - web/src/locales/zh-CN.json
autonomous: true
requirements: [KDEP-02]
must_haves:
  truths:
    - "/knowledge 搜索结果命中工件时展示工件类型徽标（PRD/埋点评审/UI…）+ 所属项目名"
    - "结果项主操作按载体自适应：feishu_doc/feishu_bitable/markdown/repo_file → 查看弹窗（markdown 渲染）；external_link → 新标签打开外链"
    - "非工件结果项视觉与既有一致（不回归）"
  artifacts:
    - path: "web/src/api/knowledge.ts"
      provides: "KnowledgeSearchResultItem 扩展 origin/source_kind/artifact 字段"
      contains: "artifact"
    - path: "web/src/pages/knowledge/index.vue"
      provides: "搜索结果项工件徽标 + 项目名 + 一键查看（弹窗/新标签）"
      contains: "artifactsApi"
  key_links:
    - from: "web/src/pages/knowledge/index.vue"
      to: "web/src/api/artifacts.ts (artifactsApi.view)"
      via: "查看弹窗调用 view(project_id, artifact_id) 复用 ArtifactView"
      pattern: "artifactsApi\\.view"
    - from: "web/src/pages/knowledge/index.vue"
      to: "KnowledgeSearchResultItem.artifact"
      via: "按 artifact.carrier 决定查看弹窗 or 新标签打开"
      pattern: "item\\.artifact"
---

<objective>
交付 KDEP-02 前端：`/knowledge` 搜索结果命中工件时，结果项加**工件类型徽标** + **所属项目名**，并提供**一键查看**——文字载体走 markdown 弹窗（复用 `ArtifactView`），`external_link` 直接新标签打开。视觉复用既有搜索结果卡片，仅在既有结构上增补徽标 slot 与操作按钮。

Purpose: 消费 96-02 后端补的 `origin/source_kind/artifact` 字段，让用户在全局知识搜索里分辨工件类型、归属项目并一键跳查看，闭合「搜到 → 看懂 → 打开」。
Output: `knowledge.ts` 类型扩展 + `index.vue` 搜索结果渲染增强（含查看弹窗）+ i18n 文案。
</objective>

<execution_context>
@.cursor/gsd-core/workflows/execute-plan.md
@.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/96-external-deps-search-overview/96-CONTEXT.md

@web/src/api/knowledge.ts
@web/src/api/artifacts.ts
@web/src/pages/knowledge/index.vue
@web/src/components/project/workbench/DependenciesSection.vue
@web/src/components/knowledge/EntityKindBadge.vue
@web/src/locales/zh-CN.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: knowledge.ts 搜索结果类型扩展</name>
  <files>web/src/api/knowledge.ts</files>
  <read_first>
    - web/src/api/knowledge.ts L47-56（`KnowledgeSearchResultItem` 接口）
    - web/src/api/artifacts.ts L10-57（`ArtifactCarrier` / `ArtifactView` 类型，供查看弹窗复用）
  </read_first>
  <action>
    扩展 `KnowledgeSearchResultItem`（web/src/api/knowledge.ts）：加可选字段
    `origin?: string`、`source_kind?: string`、
    `artifact?: { type_key: string, type_name: string, carrier: string, url: string, artifact_id: string, project_id: string, project_name: string } | null`
    （对齐 96-02 后端序列化输出）。不改 `searchDeliveryKnowledge` 请求签名（后端视图已默认 include_document_kind）。保持既有导出结构。
  </action>
  <verify>
    <automated>cd web &amp;&amp; pnpm exec vue-tsc --noEmit</automated>
  </verify>
  <done>`KnowledgeSearchResultItem` 含 origin/source_kind/artifact 可选字段；类型检查通过。</done>
</task>

<task type="auto">
  <name>Task 2: index.vue 搜索结果工件徽标 + 项目名 + 一键查看</name>
  <files>web/src/pages/knowledge/index.vue, web/src/locales/zh-CN.json</files>
  <read_first>
    - web/src/pages/knowledge/index.vue L194-228（搜索结果卡片渲染：EntityKindBadge / 标题 / score / ProvenanceLinkButton）
    - web/src/pages/knowledge/index.vue L1-93（script setup：imports、searchQuery、results）
    - web/src/components/project/workbench/DependenciesSection.vue L146-176 与 L475-521（`openView`/查看弹窗 + MarkdownRenderer/render_type 分派，复用范式）
    - web/src/locales/zh-CN.json（`knowledge.search.*` 与 `projects.artifacts.*` 键位，复用「查看/打开飞书/打开外链」文案）
  </read_first>
  <action>
    在 `web/src/pages/knowledge/index.vue` 搜索结果卡片（L194-228）内，对 `item.artifact` 存在的结果项：
    - 在 `EntityKindBadge` 旁加一枚**工件类型徽标**（展示 `item.artifact.type_name`，样式复用 reka-ui `Badge variant="outline"` + 既有徽标配色令牌，与 EntityKindBadge 视觉一致；配色映射由执行者按既有 token 自定，遵循「优雅、好用」）。
    - 在 score 行旁标注**所属项目名**（`item.artifact.project_name`，muted 小字 + `lucide--folder` 图标）。
    - 加**一键查看**主操作：按 `item.artifact.carrier` 自适应——`external_link` → `<a target="_blank" rel="noopener noreferrer" :href="item.artifact.url">` 打开外链（文案「打开外链/打开飞书」按 carrier）；其它文字载体（feishu_doc/feishu_bitable/markdown/repo_file）→「查看」按钮，点击调 `artifactsApi.view(item.artifact.project_id, item.artifact.artifact_id)` 打开查看弹窗。
    - 复用 `DependenciesSection` 的查看弹窗范式：新增 `Dialog + DialogScrollContent + DialogHeader`，按 `ArtifactView.render_type`（markdown → `MarkdownRenderer`；text → `<pre>`；records → JSON；link → 链接；error → 错误提示）渲染；loading/error 用 `useErrorHandler` fail-soft。import `artifactsApi`、`MarkdownRenderer`、Dialog 系列组件。
    - 非工件结果项渲染保持不变（不回归）。

    在 `web/src/locales/zh-CN.json` 补必要文案键（如 `knowledge.search.artifactType`、`knowledge.search.owningProject`、查看/打开复用既有 `projects.artifacts.view`/`openExternal`/`openFeishu`/`openLink` 或新增 `knowledge.search.view*`）。文案默认中文；仅新增，不改既有键。
  </action>
  <verify>
    <automated>cd web &amp;&amp; pnpm exec vue-tsc --noEmit</automated>
  </verify>
  <done>工件搜索结果项展示类型徽标 + 项目名；文字载体点「查看」弹出 markdown 渲染、external_link 新标签打开；非工件项不变；类型检查通过。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 后端搜索响应 → 前端渲染 | 未信任 url/title 渲染，外链跳转安全 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-12 | Tampering | 外链新标签打开 | mitigate | `<a target="_blank" rel="noopener noreferrer">` 防 reverse tabnabbing（沿用 DependenciesSection 范式） |
| T-96-13 | Information Disclosure | 查看弹窗正文 | mitigate | 走后端 `artifactsApi.view`（正文已服务端脱敏）；前端仅渲染，不再拉原始凭证 |
| T-96-14 | Tampering | markdown 正文渲染 XSS | mitigate | 复用既有 `MarkdownRenderer`（既有消毒策略），不引入 v-html 裸渲染 |
| T-96-SC | Tampering | npm/pip/cargo installs | mitigate | 本 plan 不新增前端依赖（复用既有组件/API），无供应链面 |
</threat_model>

<verification>
- `cd web && pnpm exec vue-tsc --noEmit` 通过
- 手工：搜索命中工件 → 结果项显示类型徽标 + 项目名；点「查看」弹 markdown、点 external_link 新标签打开
- 非工件结果项渲染无回归
</verification>

<success_criteria>
- 工件搜索结果标类型 + 所属项目名
- 一键查看按载体自适应（弹窗 / 新标签）
- 复用既有卡片/弹窗/徽标样式，视觉一致
- i18n 中文，类型检查通过
</success_criteria>

<output>
Create `.planning/phases/96-external-deps-search-overview/96-04-SUMMARY.md` when done.

## Artifacts this plan produces (96-04)
- `KnowledgeSearchResultItem` 扩展工件字段（`web/src/api/knowledge.ts`）
- 搜索结果工件徽标 + 项目名 + 一键查看弹窗（`web/src/pages/knowledge/index.vue`）
- 新增 i18n 文案（`web/src/locales/zh-CN.json`）
</output>
