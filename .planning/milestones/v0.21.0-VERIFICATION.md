# Verification — v0.21.0 蓝图过程可见与返工闭环

**Verified:** 2026-08-05
**Scope:** Phases 117–120（15 条需求）
**Status:** `tech_debt` —— 14 条完全交付 / 1 条部分交付（LIVE-04 的「推送」）/ 0 条未达

> 验证口径：每条需求都要指到**实现锚点 + 用例锚点**两样；只有实现没有用例背书的一律不记
> 满足。判据不接受「代码里有个函数」——必须有消费方（可达性），这是 v0.20.0 审计上
> CLAR-03 被打回的教训（端点实现了但前端零消费 ⇒ 用户能力不可达）。

---

## 1. 逐条需求

### LINK — 蓝图与项目双向可达

| 需求 | 判定 | 实现锚点 | 用例锚点 |
|------|------|----------|----------|
| LINK-01 | ✅ | `BlueprintViewerHeader.vue` 顶栏面包屑（`data-testid="blueprint-header-project-crumb"`，`RouterLink → /projects/{id}`）；页面向 Header 传 `doc` | `waitVisibility.spec.ts` 5 条（链接目标 / 无归属不渲染 / 空名回落「未命名项目」不回落 UUID / 顶层键缺失回落 `content.meta` / doc 为 null） |
| LINK-02 | ✅ | `blueprint_doc_views._resolve_project` + detail payload 追加 `project_id`/`project_name`（口径对齐 `blueprint_list_views._list_row`） | `test_blueprint_doc_views.py::test_document_exposes_project_id_and_name_at_top_level` / `..._when_project_row_is_gone`（项目行被删时 id 照实回传、name 留空） |

**可达性**：顶栏面包屑在 `[id].vue` 无条件渲染（不受 `readonly`/`isDiffMode` 影响），关联段的既有链接保留为第二入口。

### WAIT — 等待不无限、资源不占用

| 需求 | 判定 | 实现锚点 | 用例锚点 |
|------|------|----------|----------|
| WAIT-01 | ✅ | `BlueprintThread.reminder_count` / `expired_at`（migration `delivery/0034`）；`_write_reminder_anchors` 同批写计数与到期；`_list_pending_threads` 排除已到期；`_aload_reminder_max` 读 `BLUEPRINT_REVIEW_CONFIG.pending_reminder_max` | `test_blueprint_pending_reminder.py` 4 条新增（到上限落到期 / 到期后退出扫描面 / 到期不作答不解阻塞不转状态 / 上限可配且坏值回落） |
| WAIT-02 | ✅ | 既有落库状态机（`ProcessEngine.advance` 状态驱动）+ durable 队列续驱（`QUEUE_BLUEPRINT` + `lock=blueprint-resume-{session_id}`）+ 僵尸恢复扫描（15/60 分钟窗口） | 既有 `test_blueprint_pending_reminder.py::test_reminder_never_answers_transitions_or_fails_anything` + 本相位 `test_expiry_never_answers_unblocks_or_transitions_anything`（回归锁：到期路径不得动 `status`/`blocking`/蓝图状态） |
| WAIT-03 | ✅ | `_thread_row` 追加 `reminder_count`/`last_reminded_at`/`expired_at`；`BlueprintThreadCard.vue` 等待态行 + 「已到期」徽标与说明 | 后端 `test_thread_rows_expose_waiting_state_scalars`；前端 `waitVisibility.spec.ts` 5 条（到期徽标与状态徽标并列、等待天数与提醒次数、未到期无徽标、已作答不显示等待、旧数据缺字段不出到期面） |

**关键不变式（有变异守卫）**：到期**不改** `ThreadStatus`/`blocking`/蓝图状态 —— 那三样是 confirm 守卫与续驱 pause 判据的输入，动它们等于让超时自动放行未决澄清（与 v0.20.0 锁定的 CLAR-04「不自动作答、不判失败」冲突）。前端用例另有一条断言「到期时状态徽标仍是『未决』」，防后人把它并进状态标签表。

### LIVE — 按阶段展示 agent 活动

| 需求 | 判定 | 实现锚点 | 用例锚点 |
|------|------|----------|----------|
| LIVE-01 | ✅ | `BlueprintActivityPanel.vue`（两张视图）挂在 `[id].vue` 阶段时间线下方；六个活动事件的叙事文案（`progress.*`，刻意不配 `Generic` 兜底以免插值被顶掉） | `activityPanel.spec.ts` 6 条 |
| LIVE-02 | ✅ | `blueprint_route._emit_recalled` / `_emit_plan_drafted`；`route.scored` 补 `repository_name`/`confidence`/`role_suggestion`；`blueprint_route_history._aemit_retrieval_event`（RetrievalTrace 标量镜像）；`buildRouteFitness` 折成适配度表 | `blueprintActivity.spec.ts` 6 条（含「重路由只取最新一轮」「缺分数为 null 不显示 0%」「半可信 payload 不抛」）；`activityPanel.spec.ts` 断言 `79.87%` 与三分量 |
| LIVE-03 | ✅ | `blueprint_repo_plan._aemit_repo_activity` / `_aemit_wave_advanced` / `_aemit_repo_plan_completed`；`buildRepoPlanProgress` 三态聚合 | `blueprintActivity.spec.ts` 5 条（三态排序 / 已产出不被拉回 / 被重派解除等待 / 无 id 不产空行 / 空流）；`activityPanel.spec.ts` 断言每仓波次与产出计数 |
| **LIVE-04** | **⚠️ 部分** | 已达成：历史回放（`events` 接口 + 客户端累积去重 `event\|ts`）、增量拉取（`since_ts` 严格大于 + `limit` 夹紧 500）、采样聚合（活动事件按 `category="sampling"`，容器内逐条 tool call 不上流）。**未达成：推送通道** —— 仍是 5s 轮询，只是每轮只搬新增事件 | `test_blueprint_doc_views.py` 4 条（增量 / 默认全量对照 / 坏时间戳回落全量不 400 / limit 夹紧） |
| LIVE-05 | ✅ | 活动事件 payload 只含标量/枚举/计数/id 与引用 id；出网过既有 `sanitize_process_event_payload`；正文归蓝图 content 与线程 | 既有 `test_process_event_wire.py`（46 条）+ `test_blueprint_log_redaction_guard.py`（21 条）覆盖脱敏与正文不外泄 |

### REDO — Review 与带上下文重跑

| 需求 | 判定 | 实现锚点 | 用例锚点 |
|------|------|----------|----------|
| REDO-01 | ✅ | `_REWORK_SCOPE_STAGES` 四值 → 真实 stage；`repos` 范围经既有 `ResearchService.mark_stale` 失效指定仓；端点入参 + 归一回传；`BlueprintRejectDialog` 单选 + 仓库勾选 | 后端 6 条（四范围参数化复位 / 不传保持旧行为 / 非法值回落不 400 / repos 只失效点名仓 / 未点名仓一字不动 / 无 ids 零失效）；前端 5 条（默认 merge / full 不带清单 / repos 未勾不可提交 / 勾选后载荷带 id / 无仓可勾不给该选项） |
| REDO-02 | ✅ | `blueprint_merge._format_human_feedback` + `_acollect_human_feedback`，经**唯一装配出口** `_prompt_parts` 追加到 system（六段起草共用） | 4 条（首轮 prompt 与改动前逐字相同 / 返工带轮次与每条意见且要求不复述 / 条数与长度有界 / 纯净首轮为空串） |
| REDO-03 | ✅ | `SubAgentSession` 三列留痕（migration `subagent/0015`）；两个完成回调在改 task 状态**之前**留痕；`_aresume_env` 按「同仓 + 同蓝图会话」反查注入分片（`blueprint_research_adapter.py:498`，research/plan 两模式共用同一 dispatch） | `test_blueprint_repo_resume.py` 8 条（落库 / 超限两字段一起弃 / 按仓反查上一轮 / 不跨蓝图会话 / 取最新 / 无留痕空 env / 分片契约与容器侧一致 / 空与超限输入） |
| REDO-04 | ✅ | transcript 超 `MAX_SDK_TRANSCRIPT_CHARS` 丢弃；resume env 超 `MAX_RESUME_TRANSCRIPT_BYTES` 不下发；人审反馈条数 10 / 单条 600 字上界；留痕入库前不含凭证（回调 payload 已过脱敏链） | 上表超限两条 + `test_feedback_is_bounded_and_truncated` |
| REDO-05 | ✅ | 重跑一律产新版本（`add_version` + `revision_round`）；人工块保护（B3）挂在审查入口，本相位未改；批注重锚定（114-04）未改 | 既有 `test_blueprint_review_views.py` 版本/轮次用例 + `test_blueprint_anchor.py`(10) 回归绿 |

---

## 2. 回归结果

| 套件 | 结果 |
|------|------|
| 后端全量 `server/tests/` | **9849 passed**, 61 skipped, 26 deselected, 1 xfailed（0 failed） |
| 前端全量 `web` vitest | **2133 passed**, 1 skipped, **1 failed（既存）** |
| 前端类型 `vue-tsc --noEmit` | 干净 |
| 迁移完整性 `makemigrations --check` | `No changes detected` |

**唯一失败项是既存问题、非本里程碑引入**：`src/__tests__/blueprint-source-guard.spec.ts` 的
「扫描面内零颜色字面量」在 `BlueprintThreadSidebar.vue:344-345`（`bg-emerald-500/10` /
`text-emerald-600`）与 `BlueprintViewerHeader.vue` 的 `text-amber-800` 上命中。已用
`git stash` 在改动前的干净树上复现同一失败（本里程碑只让其中一处的行号平移）。
⚠️ 本相位自己引入过一次该守卫的命中（活动面板分数用了 Body 档加粗），已在提交前修正为
Heading 档。

---

## 3. 部分交付项与遗留工作

### 3.1 LIVE-04 的「推送」（部分交付，已登记）

- **现状**：`useBlueprintLive.ts` 仍是 5s 轮询，但已增量化（`since_ts` + 客户端累积），
  单轮只搬新增事件 ⇒ 活动事件让事件量上一个量级后不会每轮重传整条历史。
- **缺口**：真正的推送通道（蓝图 WS consumer 或 artifact 级 SSE）。
- **为什么没做**：需要新建 consumer + 路由 + 前端订阅端，是一个独立工作项；且拆开做不影响
  已交付的三层（事件契约 / 历史回放 / UI）。
- **切换点仍然唯一**：`web/src/composables/useBlueprintLive.ts`（该文件的既有契约就是
  「换推送只改这一个文件」）。⛔ 不要在组件里写 `refetchInterval`（源码守卫已锁）。

### 3.2 未执行项

- 本里程碑**未打 tag**（与 v0.19.0 / v0.20.0 一致，两者同样未打）。
- 人工验收（真实飞书卡片重推、真实容器 resume 续跑、浏览器端视觉走查）未执行 ——
  与前两个里程碑同类遗留。
- 活动流的**容器内 agent 步骤**（tool call 级）仍不上事件流：这是 v0.20.0 `event_taxonomy`
  INV-5 的既有纪律（progress/trace，非模型私有 CoT），本里程碑刻意未破。
