---
phase: 41-hitl-taxonomy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/delivery/models/plan_session_event.py
  - server/delivery/models/__init__.py
  - server/delivery/migrations/0015_plansessionevent.py
  - server/delivery/services/event_taxonomy.py
  - server/delivery/services/__init__.py
  - server/delivery/services/plan_session_service.py
  - server/services/plan_orchestration/engine.py
  - server/services/plan_orchestration/research_adapter.py
  - server/services/plan_orchestration/architect_merge_adapter.py
  - server/subagent/api/callbacks.py
  - server/tests/delivery/test_plan_session_event.py
  - server/tests/services/test_event_taxonomy_alignment.py
autonomous: true
requirements: [EVENT-01]
must_haves:
  truths:
    - "编排全程产出的 §15 trace 事件持久化为 PlanSessionEvent 行（统一信封）"
    - "_emit_event 持久化失败时只 log warning，绝不抛出影响状态转移（best-effort）"
    - "§15 全 taxonomy 名以稳定常量集中定义，各 emit 点引用常量（无字符串漂移）"
    - "Phase 38/39/40 既有 emit 名与 §15 常量逐一对齐（漂移即修）"
  artifacts:
    - path: "server/delivery/models/plan_session_event.py"
      provides: "PlanSessionEvent append-only 模型（§15 信封字段）"
      contains: "class PlanSessionEvent"
    - path: "server/delivery/services/event_taxonomy.py"
      provides: "§15 taxonomy 常量集 + 统一信封 helper"
      contains: "EVENT_"
    - path: "server/delivery/migrations/0015_plansessionevent.py"
      provides: "PlanSessionEvent 建表迁移"
      contains: "PlanSessionEvent"
  key_links:
    - from: "server/delivery/services/plan_session_service.py"
      to: "PlanSessionEvent"
      via: "_emit_event 持久化"
      pattern: "PlanSessionEvent.objects.create"
    - from: "server/services/plan_orchestration/engine.py"
      to: "server/delivery/services/event_taxonomy.py"
      via: "import 常量引用"
      pattern: "event_taxonomy|EVENT_"
---

<objective>
把 Phase 36-40 经 `PlanSessionService._emit_event` 钩子产出的编排事件**沉淀为稳定 §15 词表并持久化**（EVENT-01）。
建 append-only `PlanSessionEvent` 模型（delivery）+ migration 0015；把 `_emit_event` 从 best-effort no-op+log 升级为持久化 `PlanSessionEvent`（§15 统一信封 `{event, session_id, work_item_id?, ts, payload}`），仍 best-effort（DB 写失败只 log、绝不抛影响转移）。定义 §15 全 taxonomy 常量集 + 信封 helper，把现有 38/39/40 各 emit 点的字符串字面量改引用常量（消除漂移），并校验对齐 §15。

Purpose: 为 v0.11 对外 adapter 沉淀稳定底座（INV-5，progress/trace 非 CoT）；taxonomy 词表本 phase 必须落稳。
Output: `PlanSessionEvent` 模型 + 0015 迁移 + `event_taxonomy` 常量/信封 helper + 升级后的 `_emit_event` + 全 emit 点常量引用。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/DOMAIN-MODEL.md
@.planning/phases/41-hitl-taxonomy/41-CONTEXT.md

@server/delivery/services/plan_session_service.py
@server/delivery/models/plan_session.py
@server/delivery/models/__init__.py
@server/delivery/services/__init__.py
@server/delivery/migrations/0014_architectmerge.py
@server/services/plan_orchestration/engine.py
@server/services/plan_orchestration/research_adapter.py
@server/services/plan_orchestration/architect_merge_adapter.py
@server/subagent/api/callbacks.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: PlanSessionEvent 模型 + migration 0015 + curated re-export</name>
  <files>server/delivery/models/plan_session_event.py, server/delivery/models/__init__.py, server/delivery/migrations/0015_plansessionevent.py</files>
  <action>
新建 `server/delivery/models/plan_session_event.py`，定义 append-only `PlanSessionEvent`（DOMAIN §15，CONTEXT 决策字段），zh-CN docstring：
- `id = UUIDField(primary_key=True, default=uuid4, editable=False)`
- `session = ForeignKey("delivery.PlanSession", on_delete=CASCADE, related_name="events")`
- `event = CharField(max_length=64)`（§15 taxonomy 名，引用常量但模型层不强制枚举——开放集，便于 v0.11 扩展）
- `work_item = UUIDField(null=True, blank=True)`（软引用，§15 信封 `work_item_id?`；不建 FK，对齐 PlanSession.current_plan_version 软引用范式，避免与 WorkItem 删除耦合）
- `payload = JSONField(default=dict)`
- `ts = DateTimeField(default=django.utils.timezone.now)`（§15 信封事件时间，可由 emit 端传入）
- `created_at = DateTimeField(auto_now_add=True)`
- `Meta`: `db_table="delivery_plan_session_event"`，`ordering=["created_at"]`（append-only 顺序），`indexes=[Index(fields=["session", "ts"]), Index(fields=["event"])]`。
模型层**不写**任何 create/save 业务方法（写入只经 `PlanSessionService._emit_event`，INV-6 精神）。

在 `server/delivery/models/__init__.py` 增加 `from delivery.models.plan_session_event import PlanSessionEvent` 并加入 `__all__`。

生成迁移：`cd server && uv run python manage.py makemigrations delivery`，确认产出 `0015_plansessionevent.py`（dependencies 指向 `0014_architectmerge`）。**migration 编号协调**：本 plan 占用 0015；Clarification 模型（41-02）占用 0016。
  </action>
  <verify>
    <automated>cd server && uv run python manage.py makemigrations delivery --check --dry-run</automated>
  </verify>
  <done>0015 迁移已生成且 `makemigrations --check` 干净（无未生成变更）；`PlanSessionEvent` 可经 `from delivery.models import PlanSessionEvent` 导入。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: §15 taxonomy 常量集 + 信封 helper + _emit_event 持久化升级</name>
  <files>server/delivery/services/event_taxonomy.py, server/delivery/services/__init__.py, server/delivery/services/plan_session_service.py, server/tests/delivery/test_plan_session_event.py</files>
  <behavior>
    - 信封形状：`_emit_event(event, session, payload)` 持久化一行 `PlanSessionEvent`，字段 `event==传入名`、`session==传入 session`、`work_item==session.work_item_id`（无则 None）、`payload==传入 payload`，`ts` 默认 now。
    - best-effort：当 `PlanSessionEvent.objects.create` 抛异常（mock 注入）时，`_emit_event` 只 log warning、不重新抛出；调用方 transition 不受影响（status 已落库）。
    - 常量覆盖：`event_taxonomy` 暴露 §15 全部事件名常量，且一个 `ALL_EVENTS`/集合包含 §15 表全集（work_item.syncing / knowledge.recalling / repo.routing / repo.research.started|completed|failed / clarification.asked|answered / plan.merge.started|completed / plan.validation.failed / plan.session.failed）。
  </behavior>
  <action>
新建 `server/delivery/services/event_taxonomy.py`（zh-CN docstring，DOMAIN §15）：
- 定义稳定常量（`Final[str]`），命名如 `EVENT_WORK_ITEM_SYNCING="work_item.syncing"`、`EVENT_KNOWLEDGE_RECALLING="knowledge.recalling"`、`EVENT_REPO_ROUTING="repo.routing"`、`EVENT_REPO_RESEARCH_STARTED/COMPLETED/FAILED`、`EVENT_CLARIFICATION_ASKED="clarification.asked"`、`EVENT_CLARIFICATION_ANSWERED="clarification.answered"`、`EVENT_PLAN_MERGE_STARTED/COMPLETED`、`EVENT_PLAN_VALIDATION_FAILED="plan.validation.failed"`、`EVENT_PLAN_SESSION_FAILED="plan.session.failed"`。
- 暴露 `ALL_EVENTS: frozenset[str]`（§15 全集，供守护测试断言覆盖）。
- 提供信封 helper `build_envelope(event, session, payload) -> dict`：返回 `{"event": event, "session_id": str(session.id), "work_item_id": str(session.work_item_id) if session.work_item_id else None, "ts": ..., "payload": payload}`（v0.11 adapter 复用同一 shape；本 phase 持久化用其字段拆解到模型列）。
注意 `coding.wave.*` 属 v0.8（OUT OF SCOPE 本 phase），常量可预留但不计入 §15 v0.7 必覆盖集——以 CONTEXT/§15 v0.7 编排段为准，`ALL_EVENTS` 仅含本 phase 编排实际产出的事件。

在 `server/delivery/services/__init__.py` re-export `event_taxonomy` 常量模块所需名（或整模块），保持 curated re-export 范式。

升级 `PlanSessionService._emit_event`（`plan_session_service.py`）：保留签名 `async def _emit_event(self, event_name, session, payload)`；包一层 `sync_to_async` 持久化 `PlanSessionEvent.objects.create(session=session, event=event_name, work_item=session.work_item_id, payload=payload or {})`；整体 try/except 包裹——失败只 `logger.warning("plan_session_event_persist_failed", ...)`，**绝不抛出**（best-effort，绝不影响 transition）。保留既有 `logger.info("plan_session_event", ...)` 结构化日志。async 上下文禁裸 lazy-FK：用 `session.work_item_id` 标量（不访问 `session.work_item`），规避 Phase 38 CR-01 类。

测试 `server/tests/delivery/test_plan_session_event.py`（pytest `@pytest.mark.django_db @pytest.mark.asyncio`）：
- 真实 PlanSession → `await PlanSessionService()._emit_event("repo.routing", session, {"candidates": []})` → 断言 DB 有一行 `PlanSessionEvent`，`event=="repo.routing"`、`payload=={"candidates": []}`、`work_item` 与 session 一致（含 work_item=None 与有 work_item 两情形）。
- best-effort：monkeypatch `PlanSessionEvent.objects.create` 抛异常 → `_emit_event` 不抛、不影响（断言无异常冒泡）。
- 常量覆盖：断言 `ALL_EVENTS` 等于 §15 v0.7 编排事件名集合（逐一列出对账）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/delivery/test_plan_session_event.py -x -q</automated>
  </verify>
  <done>事件持久化为 §15 信封行；DB 失败被吞不抛；`ALL_EVENTS` 覆盖 §15 v0.7 编排全集；测试全绿。</done>
</task>

<task type="auto">
  <name>Task 3: 各 emit 点引用常量 + 38/39/40 漂移对齐守护</name>
  <files>server/services/plan_orchestration/engine.py, server/services/plan_orchestration/research_adapter.py, server/services/plan_orchestration/architect_merge_adapter.py, server/subagent/api/callbacks.py, server/tests/services/test_event_taxonomy_alignment.py</files>
  <action>
把现有 emit 点的字符串字面量替换为 `event_taxonomy` 常量引用（行为不变，仅去字符串漂移）：
- `engine.py` `_route` → `EVENT_REPO_ROUTING`；`_recall` → `EVENT_KNOWLEDGE_RECALLING`。
- `research_adapter.py` `_emit_started` → `EVENT_REPO_RESEARCH_STARTED`；`_emit_failed` → `EVENT_REPO_RESEARCH_FAILED`。
- `architect_merge_adapter.py` `_emit` 调用点 → `EVENT_PLAN_MERGE_STARTED` / `EVENT_PLAN_MERGE_COMPLETED` / `EVENT_PLAN_VALIDATION_FAILED`。
- `subagent/api/callbacks.py`（39-04 容器回调）`repo.research.completed` / `repo.research.failed` → `EVENT_REPO_RESEARCH_COMPLETED` / `EVENT_REPO_RESEARCH_FAILED`。
- `plan_session_service.py` `_fail` 内 `"plan.session.failed"` → `EVENT_PLAN_SESSION_FAILED`（注意避免 services↔delivery import 环：常量在 delivery 层，plan_session_service 同 app 直接 import）。
不改任何 payload/调用时序——纯常量替换，保持 best-effort 语义。

新建守护测试 `server/tests/services/test_event_taxonomy_alignment.py`：
- 用 `grep`/AST 或源码正则扫描 engine.py / research_adapter.py / architect_merge_adapter.py / callbacks.py / plan_session_service.py 的 `_emit_event(` 调用，断言**第一参数不再是裸字符串字面量**（应为 `EVENT_*` 名称引用）；过滤注释行（`grep -v '^[[:space:]]*#'`）防 header 自我失效。
- 断言所有被引用的常量值 ∈ `ALL_EVENTS`（§15 对齐）。
- 断言 §15 表中每个 v0.7 编排事件名在代码库中至少被一个 emit 点产出（覆盖性反查）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/test_event_taxonomy_alignment.py tests/services/test_plan_orchestration_engine.py tests/services/test_architect_merge_adapter.py tests/services/test_research_adapter.py tests/services/test_research_completion_callback.py -q</automated>
  </verify>
  <done>所有 emit 点引用 §15 常量（无裸字面量）；既有 38/39/40 emit 名与 §15 逐一对齐；既有事件单测仍绿（行为不变）。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine/adapter → _emit_event | 半可信 payload（含 router/recall/merge 结果）跨入持久化 |
| 持久化事件 → 未来 v0.11 对外 adapter | trace 事件外暴露边界（INV-5，progress/trace 非 CoT） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-01-01 | Information Disclosure | PlanSessionEvent.payload | mitigate | payload 仅落 §15 规定的 progress/trace 字段（候选计数、repo_id、plan_version_id），**不落模型私有 CoT/推理明细**（INV-5）；emit 点已按 §15 精简 payload，本 plan 不扩大 payload |
| T-41-01-02 | Denial of Service | _emit_event 持久化 | mitigate | best-effort try/except 包裹：DB 写失败只 log warning，绝不抛出阻断状态转移（编排可靠性优先于事件完整性） |
| T-41-01-03 | Tampering | event 字段开放集 | accept | event 为开放 CharField（v0.11 可扩展），写入只经 `_emit_event` 单一入口 + `ALL_EVENTS` 守护测试约束本 phase 取值；不引入外部可写路径 |
</threat_model>

<verification>
- `cd server && uv run python manage.py makemigrations --check --dry-run` 全仓干净（0015 已落、无遗漏）。
- 全量相关单测绿：`tests/delivery/test_plan_session_event.py`、`tests/services/test_event_taxonomy_alignment.py` + 既有 38/39/40 事件单测无回归。
- `ruff format`/lint（line 100）通过。
</verification>

<success_criteria>
- 编排全程事件持久化为 `PlanSessionEvent`（§15 统一信封），覆盖 §15 v0.7 taxonomy 全集。
- `_emit_event` best-effort（DB 失败不抛、不阻断转移）。
- §15 词表以稳定常量集中定义，所有 emit 点引用常量，38/39/40 emit 名对齐（漂移已修）。
- `makemigrations --check` 干净。
</success_criteria>

<output>
Create `.planning/phases/41-hitl-taxonomy/41-01-SUMMARY.md` when done.
</output>
