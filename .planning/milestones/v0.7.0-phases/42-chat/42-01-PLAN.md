---
phase: 42-chat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/services/plan_orchestration/entrypoint.py
  - server/services/plan_orchestration/__init__.py
  - server/workflows/nodes/ai/plan_research.py
  - server/agents/tools/plan_research_tools.py
  - server/agents/tools/__init__.py
  - server/agents/chat_runner.py
  - server/tests/agents/test_start_plan_research_tool.py
  - server/tests/services/test_orchestration_entry_consistency.py
autonomous: true
requirements: [ENTRY-02]
must_haves:
  truths:
    - "用户在对话中可经 start_plan_research 工具发起方案编排，复用同一 PlanOrchestrationEngine（不并行造两套编排，ENTRY-02 / SC-1）"
    - "chat 入口创建 entrypoint=chat 的 PlanSession 并驱动同一 engine 到 done 产出 canonical MergedPlan（SC-1/SC-2）"
    - "相同需求经 chat 与 workflow 入口产出结构等价的 MergedPlan 与相同 §15 事件 taxonomy 序列（SC-2）"
    - "chat 自然语言需求 PlanSession.work_item 与 canonical TechnicalPlan.work_item 为 None 且经 entrypoint=chat 显式可追溯（INV-2 / SC-3）"
    - "start_plan_research 自动注册进工具 registry 且在 chat 工具白名单可用"
  artifacts:
    - path: "server/services/plan_orchestration/entrypoint.py"
      provides: "start_orchestration + build_orchestration_engine 薄共享 helper（两入口共用）"
    - path: "server/agents/tools/plan_research_tools.py"
      provides: "start_plan_research @tool（chat 入口薄封装）"
    - path: "server/tests/services/test_orchestration_entry_consistency.py"
      provides: "SC-2 chat↔workflow 一致性 + SC-3 INV-2 测试"
  key_links:
    - from: "server/agents/tools/plan_research_tools.py"
      to: "server/services/plan_orchestration/entrypoint.py"
      via: "start_orchestration(entrypoint='chat', work_item=None) + build_orchestration_engine"
      pattern: "start_orchestration\\(|build_orchestration_engine\\("
    - from: "server/workflows/nodes/ai/plan_research.py"
      to: "server/services/plan_orchestration/entrypoint.py"
      via: "复用同一 helper（底层 engine 复用、不造两套）"
      pattern: "start_orchestration\\(|build_orchestration_engine\\("
    - from: "server/agents/chat_runner.py"
      to: "start_plan_research"
      via: "工具白名单注入"
      pattern: "start_plan_research"
---

<objective>
给 Chat 加一层**薄封装**入口，复用 Phase 36-41 已建的同一 `PlanOrchestrationEngine` + 真实
adapters 发起方案编排（ENTRY-02）——不并行造两套编排，验证 engine 入口无关性。落地三步：
① 抽薄共享 helper（建 session + 构建 engine），让 Phase 41 工作流节点与 chat 工具调同一份；
② 加 chat agent 工具 `start_plan_research`（`@tool`，对齐 deep_analysis/create_coding_plan 范式）；
③ 测试覆盖 chat 驱动到 done、chat↔workflow 一致性（SC-2）、INV-2 自然语言 null work_item（SC-3）。

Purpose: 收口 v0.7.0 末 phase——把对话即可发起方案编排做成「同一 engine、同一状态机」的薄入口。
Output: 共享 helper 模块 + chat 工具 + 三类守护测试；无新模型、无 migration（makemigrations --check 干净）。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/42-chat/42-CONTEXT.md

# 对称参考：Phase 41 工作流入口（建 session + 构建 engine + 驱动 + 挂起），抽公共 helper 的取材源
@server/workflows/nodes/ai/plan_research.py
# 入口无关的状态机推进器（advance 按 status 分派 stage handler，经 service transition）
@server/services/plan_orchestration/engine.py
@server/services/plan_orchestration/__init__.py
# PlanSession 单一写入入口（create_session 校验 entrypoint；INV-6 transition）
@server/delivery/services/plan_session_service.py
# 融合 adapter：_handle_pass 用 create_from("orchestration", work_item=session.work_item)，
# work_item 由 session.work_item_id 决定 → chat 入口 None 即 INV-2 自然语言标记
@server/services/plan_orchestration/architect_merge_adapter.py
# @tool 范式 + deep_analysis fire-and-forget/blocking marker（chat 挂起复用既有机制）
@server/agents/tools/chat_tools.py
# create_coding_plan：session 创建型 @tool + canonical eager 投影 + work_item=None(INV-2) 范式
@server/agents/tools/coding_tools.py
# @tool 装饰器 + 模块级 _tool_registry（自动注册）
@server/agents/tools/base.py
# chat 工具白名单（_INDEXED_TOOL_NAMES）+ 工具模块 import 触发注册
@server/agents/chat_runner.py
# MCP 适配层：category=PROJECT 工具自动注入 space_id/conversation_id（LLM 不可见）
@server/agents/sdk/mcp_adapter.py
# canonical 模型：origin 枚举 + work_item nullable（INV-2）
@server/delivery/models/technical_plan.py
# Phase 41 ENTRY-01 测试：节点单测 + SC-2 端到端（IO 边界 mock）——本 plan 测试取材/对称
@server/tests/workflows/test_plan_research_node.py
@server/tests/services/test_plan_research_e2e.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 抽薄共享 helper（start_orchestration + build_orchestration_engine）并重构 Phase 41 节点复用</name>
  <files>server/services/plan_orchestration/entrypoint.py, server/services/plan_orchestration/__init__.py, server/workflows/nodes/ai/plan_research.py</files>
  <action>
新建 server/services/plan_orchestration/entrypoint.py（zh-CN docstring，ruff line 100），落「底层 engine 复用、不造两套」的薄抽象，两入口共用：
(1) async `start_orchestration(entrypoint, requirement_text, *, work_item=None, created_by=None, include_repos=None) -> PlanSession`：薄包 `PlanSessionService().create_session(entrypoint=entrypoint, work_item=work_item, decomposition={"requirement_text": requirement_text, "include_repos": include_repos or []}, created_by=created_by)`。entrypoint 合法性由 create_session 既有校验（workflow|chat 否则 ValueError），helper 不重复校验。仅负责「按统一 decomposition 形态建 session」，不驱动（驱动是入口私有，避免把工作流 waiting_event / chat interrupt 这两种入口运行时混进 helper）。
(2) `build_orchestration_engine(*, session_service=None, node_execution_id="") -> PlanOrchestrationEngine`：注入与 Phase 41 **完全相同**的真实 adapters 构造 engine —— `PlanOrchestrationEngine(session_service=session_service or PlanSessionService(), router=RepoRouterV2Adapter(), recall=DeliveryKnowledgeRecallAdapter(), research=ResearchDispatchAdapter(node_execution_id=node_execution_id), merge=ArchitectMergeAdapter(), clarify=ClarifyAdapter())`。`node_execution_id` 仅工作流入口传（CR-02 调研容器回调 resume 钥匙），chat 入口传默认 ""（chat 用既有 deep_analysis 机制 resume，不依赖 node_execution）。adapters 用 lazy import（函数内 from services.plan_orchestration import ...）规避 import 环。
在 services/plan_orchestration/__init__.py 的 curated re-export 增加 `start_orchestration` 与 `build_orchestration_engine`（加入 import + __all__）。
重构 server/workflows/nodes/ai/plan_research.py（行为不变，仅复用 helper）：`_create_session` 改为调 `start_orchestration(entrypoint="workflow", requirement_text=..., work_item=work_item, created_by=created_by, include_repos=include_repos)`（移除内联 PlanSessionService().create_session 块，保留前置 requirement/work_item/created_by 解析）；`_build_engine` 改为调 `build_orchestration_engine(node_execution_id=node_execution_id)`（保留从 context.node_execution 取 node_execution_id 的逻辑）。保持方法签名/返回与节点测试对 `_build_engine` 的 monkeypatch override 兼容（仍是实例方法，仍返回 engine）。entrypoint 仍为 "workflow"、decomposition 形态不变 → Phase 41 节点/e2e 测试无需改动即绿。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/workflows/test_plan_research_node.py tests/services/test_plan_research_e2e.py -q</automated>
  </verify>
  <done>entrypoint.py 暴露 start_orchestration + build_orchestration_engine 并经 __init__ re-export；plan_research.py 节点改用两 helper；Phase 41 节点单测与 SC-2 e2e 测试全绿（行为零变更）。</done>
</task>

<task type="auto">
  <name>Task 2: 加 chat agent 工具 start_plan_research（薄入口）+ 注册接线</name>
  <files>server/agents/tools/plan_research_tools.py, server/agents/tools/__init__.py, server/agents/chat_runner.py</files>
  <action>
新建 server/agents/tools/plan_research_tools.py，定义 `@tool(name="start_plan_research", category="PROJECT", parameters=...)` async 工具（zh-CN docstring，ruff line 100，对齐 deep_analysis/create_coding_plan 范式）。参数 schema：`requirement_text`(string, required，自然语言需求文本) + `include_repos`(array[string], 可选，限定候选仓库 UUID) + `space_id`(string, auto-inject) + `conversation_id`(string, auto-inject)。category=PROJECT 使 MCP 适配层自动注入 space_id/conversation_id 且 LLM 不可见（见 mcp_adapter._adapt_tool）。description 写明用途：当对话中识别「做多仓技术方案/跨仓方案编排」意图时调用。
工具职责（**薄**，绝不写新编排逻辑）：
(1) 解析 created_by：从 Conversation 取发起用户——`await Conversation.objects.filter(id=conversation_id).values("created_by").afirst()` 取 created_by 标量，再 `await User.objects.filter(id=...).afirst()` 取用户对象（async 安全，不裸 lazy-FK）；解析失败/为空 → None（recall stage 对 None actor 已 fail-closed 返回空召回，文档化降级，不报错）。
(2) include_repos 透传（与工作流节点对称，不在工具内做新路由）；可选 best-effort 过滤到属于 space 的仓库 UUID。
(3) 建 session：`session = await start_orchestration(entrypoint="chat", requirement_text=requirement_text, work_item=None, created_by=created_by, include_repos=include_repos)`。**work_item=None 即 INV-2 自然语言需求显式标记**（entrypoint=chat 可追溯）。
(4) 构建 engine：`engine = build_orchestration_engine()`（无 node_execution_id；chat resume 走既有机制）。
(5) 驱动：advance 循环（mirror 节点：终态集合 {DONE, FAILED}，带 _MAX_ADVANCE_STEPS 步数上限防死循环，每步 advance 后 `session = await PlanSession.objects.aget(id=session.id)` 重读）。
   - 终态 done → 返回 `ToolResult(success=True, output={"session_id", "plan_version_id": str(session.current_plan_version) if set else None, "status": "done", "message": ...})`。
   - 终态 failed → 返回 `ToolResult(success=False, error=...)`（取 session.error message/reason）。
   - 挂起复用 chat 既有 HITL，**不重实现**：CLARIFYING 且有 pending Clarification → 返回 `ToolResult(success=True, output={"clarification_id", "pending": True, "marker": "ask_clarification", "question": <pending.question>, "options": [], "allow_freeform": True, "session_id": ...})`（复用 orchestration.graph 既有 ask_clarification interrupt 识别 marker）；RESEARCHING 且容器在途（`not await aall_research_tasks_terminal(session.id)`）→ 返回 deep_analysis 式 fire-and-forget marker（`output={"__blocking_task__": True, "task_type": "plan_research", "session_id": ..., "placeholder": ...}` + `register_blocking_task(conversation_id, ...)`）。engine 状态全持久化 → 跨轮次/回调可由既有 chat 机制 resume；真实 LLM/容器端到端 resume E2E 沿用既有 deferred（mock）。
所有 delivery/engine import 用函数内 lazy import（规避 chat→delivery 循环，对齐 coding_tools）。
接线注册：在 server/agents/tools/__init__.py import `start_plan_research` 并加入 __all__；在 server/agents/chat_runner.py 顶部 `import agents.tools.plan_research_tools  # noqa: F401`（触发 @tool 注册）并把 `"start_plan_research"` 加入 `_INDEXED_TOOL_NAMES`（有已索引仓库即可在对话中发起编排）。
  </action>
  <verify>
    <automated>cd server && uv run python -c "import os,django; os.environ.setdefault('DJANGO_SETTINGS_MODULE','friday.settings'); django.setup(); import agents.tools.plan_research_tools; from agents.tools.registry import ToolRegistry; t=ToolRegistry.get_tool('start_plan_research'); assert t is not None and t.category.value=='PROJECT'; print('ok')" && cd server && uv run ruff check agents/tools/plan_research_tools.py agents/tools/__init__.py agents/chat_runner.py</automated>
  </verify>
  <done>start_plan_research @tool 落 plan_research_tools.py（category=PROJECT、auto-inject space_id/conversation_id）；建 entrypoint=chat + work_item=None session 并经共享 helper 驱动同一 engine；已 import 注册 + 加入 chat 工具白名单 + ruff 干净。</done>
</task>

<task type="auto">
  <name>Task 3: 守护测试（chat 驱动到 done + SC-2 一致性 + SC-3 INV-2 + 注册）</name>
  <files>server/tests/agents/test_start_plan_research_tool.py, server/tests/services/test_orchestration_entry_consistency.py</files>
  <action>
新建两测试文件（zh-CN docstring，`pytestmark = pytest.mark.django_db(transaction=True)`，IO 边界 mock，取材对称 tests/services/test_plan_research_e2e.py 的 `_make_repo` / `_valid_merged` / `_FakeSynth` / mock router/recall 范式）。真实 PlanSessionService/TechnicalPlanService/ArchitectMergeAdapter + 真实 engine，仅 IO 边界 mock（router/recall AsyncMock、dispatch/runner 计数 patch、merge synthesizer fake）。

server/tests/agents/test_start_plan_research_tool.py：
- `test_start_plan_research_drives_to_done_merged_plan`：mock `build_orchestration_engine`（monkeypatch `agents.tools.plan_research_tools.build_orchestration_engine` 或经 lazy import 源 `services.plan_orchestration.build_orchestration_engine`）返回注入了 mock 直通 adapters 的 engine（router 返回候选、recall 空、research.dispatch 返回 {} → barrier 直通 merging、merge 经 `set_current_plan_version` 后返回 validation_status=passed，对齐 test_plan_research_node.py::test_drive_to_done_emits_merged_plan_ref）。建好 Project/Conversation 后调 `start_plan_research(requirement_text="...", space_id=..., conversation_id=...)` → 断言 ToolResult.success、output["status"]=="done"、output["plan_version_id"] 非空；DB 中该 PlanSession.entrypoint=="chat"、status==DONE。
- `test_start_plan_research_registered`：`ToolRegistry.get_tool("start_plan_research")` 非空、category==PROJECT、schema properties 含 requirement_text/include_repos（space_id/conversation_id 在 schema 中存在，由 MCP 适配层注入移除）；并断言已在 chat_runner `_INDEXED_TOOL_NAMES`（`from agents.chat_runner import _INDEXED_TOOL_NAMES; assert "start_plan_research" in _INDEXED_TOOL_NAMES`）。
- `test_start_plan_research_inv2_null_work_item`（SC-3）：自然语言需求（不传 work_item）驱动到 done → 断言 PlanSession.work_item_id is None 且 entrypoint=="chat"（显式可追溯）；canonical：经 session.current_plan_version 取 PlanVersion → 其 plan(TechnicalPlan).work_item_id is None（INV-2：work_item 可空且经 entrypoint=chat 标记）。

server/tests/services/test_orchestration_entry_consistency.py（SC-2，入口无关性）：
- `test_chat_and_workflow_entries_yield_equivalent_merged_plan`：同一 requirement + 同一 include_repos，分别经 `start_orchestration(entrypoint="workflow", ...)` 与 `start_orchestration(entrypoint="chat", work_item=None, ...)` 建两 session；用**结构相同的**注入 engine（同 _FakeSynth(repo_a, repo_b) 产同一 §7 MergedPlan、同 mock router/recall、真实 ResearchDispatchAdapter + ArchitectMergeAdapter，dispatch/runner patch、容器回调用 e2e 范式的 `_complete_running_tasks`）分别驱动到 done。断言：两 session 各自 current_plan_version 对应 PlanVersion.content **结构等价**（去除 id/时间类字段后 dict 相等，尤其 dependency_dag / execution_plan[].dependencies 跨仓拓扑一致）；两 session 的 §15 事件 taxonomy **序列相同**（按 created 顺序取 PlanSessionEvent.event 列表，断言两入口序列相等，覆盖 repo.routing/knowledge.recalling/repo.research.*/plan.merge.* 同序）。这证明「同一 engine、同一状态机 → 入口无关一致产物」。

复用既有驱动范式：clarify 用 `ClarifyAdapter` 或 AsyncMock(needs_clarification=False) 直通；研究段用 e2e 的 `_drive` + `_complete_running_tasks(exposer_id)` 模拟容器回调，或 research.dispatch 返回 {} 直通（happy path 无需深入仓时）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/agents/test_start_plan_research_tool.py tests/services/test_orchestration_entry_consistency.py -q && cd server && uv run python manage.py makemigrations --check --dry-run</automated>
  </verify>
  <done>chat 工具驱动到 done 产出 MergedPlan、SC-2 chat↔workflow 结构等价 MergedPlan + 同序 §15 事件、SC-3 INV-2 null work_item 可追溯、工具注册 4 类测试全绿；makemigrations --check 干净（无模型变更）。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chat LLM → start_plan_research 工具 | LLM 生成的 requirement_text / include_repos 为半可信输入，跨入工具 |
| 工具 → 编排 engine（recall/research/merge） | 编排经注入用户 actor（created_by）作权限/召回边界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-01 | Information Disclosure | start_plan_research include_repos / recall stage | mitigate | created_by 从 Conversation.created_by 解析为 recall actor；None 时召回 fail-closed 返回空（沿用 38-03 DeliveryKnowledgeRecallAdapter），include_repos best-effort 过滤到属于 space 的仓库，避免越权拉取他人仓库上下文 |
| T-42-02 | Tampering | LLM 编造 space_id/conversation_id | mitigate | space_id/conversation_id 由 MCP 适配层自动注入、不暴露给 LLM（mcp_adapter._adapt_tool 从 schema 移除），LLM 无法伪造 |
| T-42-03 | Denial of Service | advance 驱动循环 | mitigate | 复用节点 _MAX_ADVANCE_STEPS 步数上限防 advance 不前进死循环；超限落 failed 终态 |
| T-42-SC | Tampering | 依赖安装 | accept | 本 phase 无 npm/pip/cargo 新依赖（薄封装复用既有 engine/adapters/工具框架），无 supply-chain 安装面 |
</threat_model>

<verification>
- 三任务 verify 命令全绿：Phase 41 节点/e2e 测试（helper 重构后回归）、工具注册与 ruff、新增 4 类守护测试。
- `makemigrations --check --dry-run` 干净（无新模型 / 无 migration）。
- grep 守护：`start_orchestration` / `build_orchestration_engine` 同时被 plan_research.py（workflow）与 plan_research_tools.py（chat）引用——证明「底层 engine 复用、不造两套」。
</verification>

<success_criteria>
- SC-1：Chat 入口经 start_plan_research 复用同一 PlanOrchestrationEngine 从对话发起编排（共享 build_orchestration_engine，工具与节点同一 engine 构造）。
- SC-2：chat 与 workflow 入口产出结构等价 MergedPlan + 同序 §15 事件 taxonomy（test_orchestration_entry_consistency 断言）。
- SC-3：chat 自然语言需求 PlanSession.work_item 与 canonical TechnicalPlan.work_item 为 None 且经 entrypoint=chat 显式可追溯（INV-2，test_start_plan_research_inv2_null_work_item 断言）。
- 工具自动注册进 registry 且在 chat 白名单可用。
</success_criteria>

<output>
Create `.planning/phases/42-chat/42-01-SUMMARY.md` when done
</output>