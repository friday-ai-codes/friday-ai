---
phase: 37-canonical-technicalplan-technicalplanservice
plan: 03
type: execute
wave: 3
depends_on: [37-02]
files_modified:
  - server/delivery/services/technical_plan_service.py
  - server/agents/tools/chat_tools.py
  - server/tests/delivery/test_technical_plan_lazy_migration.py
  - server/tests/delivery/test_chat_eager_projection.py
autonomous: true
requirements: [PLAN-03]
must_haves:
  truths:
    - "首次 resolve 无 canonical 的旧记录（chat/mcp/workflow）→ service 建 canonical + 回填软链（read-time lazy migration）"
    - "lazy 取材忠实映射旧 payload（chat affected_files/recommended_repository_ids、mcp plan_body/repository_tasks → 合法 content）"
    - "再次 resolve 同一旧记录 → 读已回填的 canonical、不重复 create（幂等）"
    - "至少一条旧路径（chat CodingPlan 创建入口）eager 投影：创建即 create_from + link 回填 canonical_plan_id（best-effort 非阻断）"
    - "冲突以 canonical 为准：旧记录与 canonical 不一致时 resolve 读 canonical（DOMAIN §5.4）"
  artifacts:
    - path: "server/tests/delivery/test_technical_plan_lazy_migration.py"
      provides: "三路径 lazy 迁移 + 幂等 + 冲突测试"
    - path: "server/tests/delivery/test_chat_eager_projection.py"
      provides: "chat eager 投影示范守护测试"
  key_links:
    - from: "server/agents/tools/chat_tools.py"
      to: "server/delivery/services/technical_plan_service.py"
      via: "create_from + link 调用（lazy import 防循环）"
      pattern: "TechnicalPlanService"
---

<objective>
把 PLAN-03 的**迁移行为**闭环：read-time lazy migration 在三路径（chat/mcp/workflow）全链路可用——首次读到无 canonical 的旧记录 → service 建 canonical 并回填软链；并在最活跃的 chat `CodingPlan` 创建入口接一条 **eager 投影示范**（create_from + link）+ 守护。冲突以 canonical 为准、归档不级联删旧表（DOMAIN §5.4）。

Purpose: 兑现「不全量双写、不爆改」的渐进收敛——lazy 兜底保证不断层（DOMAIN §5.3），eager 示范一条路径证明投影 API 可用（mcp/workflow eager 接入留 deferred，由 40/41 接 orchestration 时顺带）。schema（37-01）+ service API（37-02）已就绪，本 plan 做忠实取材 + 入口接线 + 端到端守护。

Output: 完善 `resolve` 的三路径忠实取材 + 幂等；chat 创建入口 eager 投影示范；lazy 迁移 / 幂等 / 冲突 / eager 守护测试。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/DOMAIN-MODEL.md
@.planning/phases/37-canonical-technicalplan-technicalplanservice/37-CONTEXT.md
@.planning/phases/37-canonical-technicalplan-technicalplanservice/37-02-SUMMARY.md

# 本 plan 主体（37-02 已建，本 plan 完善 resolve 取材）
@server/delivery/services/technical_plan_service.py
# eager 投影接线目标：chat CodingPlan 创建入口
@server/agents/tools/chat_tools.py
@server/chat/models.py
# lazy 取材源：mcp 旧表 payload 结构
@server/mcp_tools/models.py
@server/mcp_tools/technical_plan_service.py
# content 必填校验（取材产物须过 validate）
@server/workflows/schemas/technical_plan.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 完善 resolve 三路径忠实取材 + 幂等</name>
  <files>server/delivery/services/technical_plan_service.py</files>
  <action>
把 37-02 resolve 的「最小合法 content」取材升级为**忠实映射**（每路径一个纯 helper，zh-CN docstring，ruff 行宽 100）。产物必须满足 `validate_technical_plan`（title/summary/execution_plan，execution_plan item 含 id/name/repository_id/repository_name/branch_strategy）：

- `_chat_codingplan_to_content(coding_plan) -> dict`：`title = coding_plan.title or coding_plan.tech_plan[:80]`；`summary = coding_plan.tech_plan`（或截断）；`execution_plan` 由 `recommended_repository_ids` 派生——每个 repo_id 生成一个 task（id=f"chat-{i}"、name、repository_id=str(repo_id)、repository_name 占位、branch_strategy="feature"、coding_instruction=tech_plan、files 由 affected_files 映射成 {path, action=change_type}）。无 recommended_repository_ids 时给单个占位 task（保证 execution_plan 非空过 validate）。
- `_mcp_plan_to_content(mcp_plan) -> dict`：优先用 `plan_body`（若其本身已是 §7/execution_plan 形态则直接复用并补全必填）；否则由 `repository_tasks` 映射成 execution_plan；title/summary 取 `mcp_plan.title` / `markdown[:200]`。
- workflow：无旧独立表，resolve 仅在 `PlanExternalRef` 命中时读 canonical；未 link 则 `raise PlanNotFound`（eager 投影由调用方先 create_from+link，DOMAIN §5.3）——保持 37-02 行为，不取材。

**幂等保证**（DOMAIN §5.4 规则 1）：resolve 命中软链分支（canonical_plan_id 非空 / PlanExternalRef 存在）必须直接 `return TechnicalPlan.objects.aget(id=...)`，**绝不**再 create——这是再 resolve 读 canonical 不重建的关键。lazy 建分支仅在软链为空且旧记录存在时进入，建后回填使下次 resolve 走命中分支。chat/mcp 模型函数内 lazy import 防循环。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/delivery/test_technical_plan_lazy_migration.py -x -q -k "chat or mcp"</automated>
  </verify>
  <done>chat/mcp lazy 取材产 content 过 validate_technical_plan；resolve 命中软链直接读 canonical（幂等不重建）；workflow 未 link → PlanNotFound。</done>
</task>

<task type="auto">
  <name>Task 2: chat CodingPlan 创建入口 eager 投影示范</name>
  <files>server/agents/tools/chat_tools.py</files>
  <action>
在 chat `CodingPlan` 创建入口（`agents/tools/chat_tools.py` 中 `create_coding_plan` 工具调用 `CodingPlan.aget_or_create_for_conversation` 处；先 Read 该文件定位最活跃单一创建 chokepoint）接 **eager 投影示范**：CodingPlan 创建成功（created=True）后，best-effort 调用
`canonical = await TechnicalPlanService().create_from(origin="chat", payload={"content": _chat_codingplan_to_content(plan)}, work_item=None)`（INV-2：chat 自然语言 work_item=None + origin=chat 显式标记）→ `await TechnicalPlanService().link(plan, canonical)` 回填 `canonical_plan_id`。

约束：
- **lazy import** `from delivery.services import TechnicalPlanService`（函数内，防 chat→delivery 循环依赖，对齐既有 `from knowledge import ingestion` lazy import 范式）。
- **best-effort 非阻断**：整段 try/except 包裹，失败仅 `logger.warning("chat_eager_plan_projection_failed", ...)`，**绝不**影响 CodingPlan 创建 / 工具返回（对齐 sensitive-detect / ingestion 派发的「失败不致命」范式）。
- 仅 created=True 时投影（已存在 plan 不重复投影；再次走 lazy/命中分支）。
- 复用 Task 1 的 `_chat_codingplan_to_content`（从 delivery.services 暴露或 import），不另写取材。
- **不**直接写 TechnicalPlan/PlanVersion（经 service，INV-6——本接线调 service，不触发 grep 守护）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/delivery/test_chat_eager_projection.py -x -q</automated>
  </verify>
  <done>chat CodingPlan 创建入口创建即 eager 投影 create_from+link 回填 canonical_plan_id；投影失败不阻断创建；已存在 plan 不重复投影。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: lazy 迁移 / 幂等 / 冲突 / eager 守护测试</name>
  <files>server/tests/delivery/test_technical_plan_lazy_migration.py, server/tests/delivery/test_chat_eager_projection.py</files>
  <behavior>
    - lazy chat：造 CodingPlan(canonical_plan_id=None) → resolve(PlanRef.for_chat(id)) → 建 canonical + CodingPlan.canonical_plan_id 回填非空；content 过 validate。
    - lazy mcp：造 McpWorkItemTechnicalPlan(canonical_plan_id=None, plan_body/repository_tasks) → resolve → 建 canonical + 回填。
    - lazy workflow：先 create_from + link(external_ref) → resolve(PlanRef.for_workflow) 命中读 canonical；未 link 的 ref → PlanNotFound。
    - 幂等：对已回填的 chat 旧记录连续 resolve 两次 → 返回同一 canonical.id、TechnicalPlan 计数不增（读不重建）。
    - 冲突以 canonical 为准：lazy 建 canonical 后用 add_version 改 canonical content（与旧记录分叉）→ resolve 仍读 canonical（最新 current_version），不被旧记录覆盖。
    - 归档不级联：archive(canonical) 后旧 CodingPlan 仍在、canonical_plan_id 仍指向（DOMAIN §5.4 规则 5）。
    - eager（test_chat_eager_projection）：经 chat 创建入口建 CodingPlan → canonical_plan_id 自动回填 + 存在对应 TechnicalPlan(origin=chat, work_item=None)；mock 投影抛错 → CodingPlan 仍创建成功（best-effort 守护）。
  </behavior>
  <action>
新建 `test_technical_plan_lazy_migration.py`（async + django_db，经 service API + `sync_to_async` 造旧记录）覆盖 lazy 三路径 / 幂等 / 冲突 / 归档不级联。新建 `test_chat_eager_projection.py` 守护 chat 入口 eager 投影 + best-effort（用 monkeypatch 让 create_from raise，断言 CodingPlan 仍建成且工具不抛）。断言计数用 `await sync_to_async(TechnicalPlan.objects.count)()`。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/delivery/test_technical_plan_lazy_migration.py tests/delivery/test_chat_eager_projection.py -q</automated>
  </verify>
  <done>三路径 lazy 迁移建+回填、幂等再 resolve 读不重建、冲突以 canonical 为准、归档不级联、eager 示范 + best-effort 守护全绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 旧表 payload → lazy 取材 | 存量旧记录字段为半可信输入，取材产物须过 validate |
| chat 工具入口 → delivery service | 跨 app eager 投影调用（lazy import + best-effort 隔离） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-06 | Tampering/Integrity | 旧记录与 canonical 分叉 | mitigate | resolve 软链命中只读 canonical（§5.4 规则 1/3 冲突以 canonical 为准）；幂等测试守护 |
| T-37-07 | Availability | eager 投影失败阻断 chat 创建 | mitigate | best-effort try/except 隔离，失败仅 warning，绝不阻断 CodingPlan 创建 |
| T-37-08 | Integrity | lazy 取材产非法 content | mitigate | 取材产物经 validate_technical_plan，非法 raise PlanContentInvalid（不静默落坏 content） |
| T-37-03-SC | Tampering | npm/pip/cargo installs | mitigate | 无新增依赖；无 install 任务 |
</threat_model>

<verification>
- `cd server && uv run pytest tests/delivery/test_technical_plan_lazy_migration.py tests/delivery/test_chat_eager_projection.py tests/delivery/test_technical_plan_inv6_guard.py -q` 全绿（含 eager 接线后 INV-6 守护仍通过——接线调 service 非旁路写）。
- `cd server && uv run python manage.py makemigrations --check --dry-run` 仍 No changes detected。
- `cd server && uv run ruff format --check delivery/services/technical_plan_service.py agents/tools/chat_tools.py` 通过。
</verification>

<success_criteria>
- 存量 3 路径首次读无 canonical → lazy 建 canonical + 回填软链，不全量双写（SC-3）。
- 至少一条旧路径（chat）eager 投影示范挂软链（SC-3 eager 部分）。
- 迁移期旧表只读历史、冲突以 canonical 为准、归档不级联删旧表（SC-4）。
- 再 resolve 幂等读 canonical 不重建；eager 投影 best-effort 不阻断 chat 创建。
- INV-6 守护（37-02）在接线后仍通过。
</success_criteria>

<output>
Create `.planning/phases/37-canonical-technicalplan-technicalplanservice/37-03-SUMMARY.md` when done
</output>
