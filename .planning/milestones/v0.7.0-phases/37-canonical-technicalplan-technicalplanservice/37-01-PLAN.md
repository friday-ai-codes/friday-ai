---
phase: 37-canonical-technicalplan-technicalplanservice
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/delivery/models/technical_plan.py
  - server/delivery/models/__init__.py
  - server/delivery/migrations/0010_technicalplan_planversion_planexternalref.py
  - server/chat/migrations/0022_codingplan_canonical_plan_id.py
  - server/mcp_tools/migrations/0008_mcpworkitemtechnicalplan_canonical_plan_id.py
  - server/chat/models.py
  - server/mcp_tools/models.py
  - server/tests/delivery/test_technical_plan_models.py
autonomous: true
requirements: [PLAN-01, PLAN-03]
must_haves:
  truths:
    - "canonical TechnicalPlan/PlanVersion 可落库（origin/status/version/supersedes 版本链 + content=MergedPlan schema）"
    - "TechnicalPlan.work_item 可为 null（INV-2，chat 自然语言需求）且删 WorkItem 不删 plan（SET_NULL）"
    - "PlanVersion unique_together(plan, version) 在 DB 层强制版本唯一"
    - "chat.CodingPlan / mcp.McpWorkItemTechnicalPlan 各有 canonical_plan_id UUIDField 软链字段（无跨 app 硬 FK）"
    - "workflow 软链承载表 PlanExternalRef(external_ref unique, canonical FK CASCADE) 落库"
    - "makemigrations --check 干净（无未生成 migration）"
  artifacts:
    - path: "server/delivery/models/technical_plan.py"
      provides: "TechnicalPlan / PlanVersion / PlanExternalRef 模型 + origin/status 枚举"
      contains: "class TechnicalPlan"
    - path: "server/delivery/migrations/0010_technicalplan_planversion_planexternalref.py"
      provides: "单 migration 建 3 表（circular FK 经字符串引用）"
      contains: "CreateModel"
    - path: "server/chat/migrations/0022_codingplan_canonical_plan_id.py"
      provides: "chat.CodingPlan.canonical_plan_id 软链字段"
    - path: "server/mcp_tools/migrations/0008_mcpworkitemtechnicalplan_canonical_plan_id.py"
      provides: "mcp McpWorkItemTechnicalPlan.canonical_plan_id 软链字段"
    - path: "server/tests/delivery/test_technical_plan_models.py"
      provides: "模型层守护测试（INV-2 / unique_together / 版本链 / 软链字段类型）"
  key_links:
    - from: "server/delivery/models/__init__.py"
      to: "server/delivery/models/technical_plan.py"
      via: "curated re-export"
      pattern: "from delivery.models.technical_plan import"
---

<objective>
立 canonical 方案脊柱的**全部 schema**：canonical `TechnicalPlan`/`PlanVersion`（delivery app，DOMAIN §5.1/§12.7）+ workflow 软链承载表 `PlanExternalRef`，并给存量 chat/mcp 两旧表加 `canonical_plan_id` UUID 软链字段（DOMAIN §5.2/§5.3）。

Purpose: 这是方案唯一事实源的落库底座。**schema-first 决策**：把旧路径软链 DDL（chat/mcp 字段 + PlanExternalRef）与 canonical 模型同波次落地，使 Wave 2 的 `TechnicalPlanService.resolve/link` 可针对已存在的列/表直接实现并测试（避免「service 引用尚不存在的列」的执行期断裂）。本 plan 覆盖 PLAN-01 全部 + PLAN-03 的 schema/DDL 部分；PLAN-03 的 lazy 迁移行为与 eager 投影在 37-03。

Output: `server/delivery/models/technical_plan.py`（3 模型 + 枚举）、delivery migration 0010（单 migration 建 3 表）、chat/mcp 各一条加字段 migration、模型层守护测试。**本 plan 不写任何落库/版本/关联业务逻辑**（INV-6——写入逻辑归 37-02 service）。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/DOMAIN-MODEL.md
@.planning/phases/37-canonical-technicalplan-technicalplanservice/37-CONTEXT.md

# 范式来源（版本链 supersedes self FK + unique_together + 循环 current_version 字符串引用 + UUID pk + curated re-export）
@server/delivery/models/document.py
@server/delivery/models/plan_session.py
@server/delivery/models/__init__.py
# 旧表（加 canonical_plan_id 软链字段的目标）
@server/chat/models.py
@server/mcp_tools/models.py
# 单 migration 建两互引用表的既有先例（Document + DocumentVersion 同 migration）
@server/delivery/migrations/0004_document_documentversion.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: canonical 模型 + PlanExternalRef + curated re-export</name>
  <files>server/delivery/models/technical_plan.py, server/delivery/models/__init__.py</files>
  <action>
新建 `server/delivery/models/technical_plan.py`（zh-CN docstring，ruff 行宽 100，UUIDField(default=uuid4) pk，对齐 document.py 范式）：

- 枚举 `TechnicalPlanOrigin(models.TextChoices)`：`CHAT="chat"` / `MCP="mcp"` / `WORKFLOW="workflow"` / `ORCHESTRATION="orchestration"`（DOMAIN §12.7）。
- 枚举 `TechnicalPlanStatus(models.TextChoices)`：`DRAFT="draft"`（默认）/ `UNDER_REVIEW="under_review"` / `APPROVED="approved"` / `SUPERSEDED="superseded"` / `ARCHIVED="archived"`。
- `TechnicalPlan`：`id UUIDField(pk)`；`work_item = FK("delivery.WorkItem", null=True, blank=True, on_delete=SET_NULL, related_name="technical_plans")`（INV-2：chat 自然语言需求 null 允许；不另设 bool，null work_item + origin=chat 即「自然语言需求」）；`origin CharField(max_length=16, choices=TechnicalPlanOrigin.choices)`；`current_version = FK("delivery.PlanVersion", null=True, blank=True, on_delete=SET_NULL, related_name="+")`（**字符串前向引用** + related_name="+" 避免与 PlanVersion.plan 反查名冲突，处理循环 FK，对齐 Document.current_version）；`status CharField(max_length=16, choices=TechnicalPlanStatus.choices, default=TechnicalPlanStatus.DRAFT)`；`created_at auto_now_add` / `updated_at auto_now`。Meta：`db_table="delivery_technical_plan"`，`indexes=[Index(fields=["work_item"]), Index(fields=["origin", "status"])]`。
- `PlanVersion`：`id UUIDField(pk)`；`plan = FK(TechnicalPlan, on_delete=CASCADE, related_name="versions")`；`version = PositiveIntegerField()`；`supersedes = FK("self", null=True, blank=True, on_delete=SET_NULL, related_name="superseded_by")`（对齐 DocumentVersion）；`content = JSONField(default=dict)`（§7 MergedPlan schema：title/summary/api_contracts/dependency_dag/data_migrations/compat_risks/release_order/rollback_plan/execution_plan——本 plan 只存不校验）；`content_hash = CharField(max_length=64, blank=True, default="")`；`created_at auto_now_add`。Meta：`db_table="delivery_plan_version"`，`unique_together=(("plan", "version"),)`，`indexes=[Index(fields=["plan", "-version"])]`。
- `PlanExternalRef`（workflow 软链承载，DOMAIN §5.2，Claude's Discretion 采纳 DOMAIN 措辞：chat/mcp 用各自表 canonical_plan_id 字段、workflow 用此映射表）：`id UUIDField(pk)`；`external_ref = CharField(max_length=255, unique=True, db_index=True)`（形如 `workflow:{execution_id}:{node_id}`）；`canonical = FK(TechnicalPlan, on_delete=CASCADE, related_name="external_refs")`；`created_at auto_now_add`。Meta：`db_table="delivery_plan_external_ref"`。
- 每个模型写 `__str__` + 简短 zh-CN docstring；**绝不**写 create/save/版本管理等业务方法（INV-6，写入只经 37-02 service）。

在 `server/delivery/models/__init__.py` curated re-export 新增 `TechnicalPlan` / `TechnicalPlanOrigin` / `TechnicalPlanStatus` / `PlanVersion` / `PlanExternalRef`（import 块 + `__all__`，对齐既有风格）。
  </action>
  <verify>
    <automated>cd server && uv run python -c "from delivery.models import TechnicalPlan, PlanVersion, PlanExternalRef, TechnicalPlanOrigin, TechnicalPlanStatus; print('ok')"</automated>
  </verify>
  <done>三模型 + 两枚举可从 delivery.models 导入；TechnicalPlan.work_item nullable SET_NULL；PlanVersion 有 unique_together(plan, version) + supersedes self FK；PlanExternalRef.external_ref unique；无任何业务写方法。</done>
</task>

<task type="auto">
  <name>Task 2: 旧表 canonical_plan_id 软链字段 + 三条 migration</name>
  <files>server/chat/models.py, server/mcp_tools/models.py, server/delivery/migrations/0010_technicalplan_planversion_planexternalref.py, server/chat/migrations/0022_codingplan_canonical_plan_id.py, server/mcp_tools/migrations/0008_mcpworkitemtechnicalplan_canonical_plan_id.py</files>
  <action>
软链字段（**不建跨 app 硬 FK**，避免 chat/mcp→delivery 耦合 + 循环依赖；存 TechnicalPlan.id，DOMAIN §5.2 / CONTEXT 决策）：

- `server/chat/models.py` `CodingPlan`：加 `canonical_plan_id = models.UUIDField(null=True, blank=True, db_index=True)`（zh-CN `verbose_name`/`help_text` 注明「canonical delivery.TechnicalPlan.id 软链，非 FK」）。放在 `updated_at` 之前的字段区，**不动** `aget_or_create_for_conversation` 等既有方法。
- `server/mcp_tools/models.py` `McpWorkItemTechnicalPlan`：同样加 `canonical_plan_id = models.UUIDField(null=True, blank=True, db_index=True)`。

生成 migration（**用 makemigrations 生成，不手写**，保证 --check 干净）：
- delivery：`cd server && uv run python manage.py makemigrations delivery` → 期望产出 0010（CreateModel TechnicalPlan + PlanVersion + PlanExternalRef）。circular `current_version` 经字符串模型引用 + nullable，Django 会在单 migration 内用 `AddField`/`CreateModel` 正确编排（对齐 0004 先例）。把文件重命名/确认名为 `0010_technicalplan_planversion_planexternalref.py`（若 Django 生成名不同，保留 Django 生成的真实文件名并更新 frontmatter files_modified；不要手改 migration 内容）。
- chat：`makemigrations chat` → 0022（AddField canonical_plan_id）。
- mcp_tools：`makemigrations mcp_tools` → 0008（AddField canonical_plan_id）。
- 跑 `migrate` 应用全部，再 `makemigrations --check --dry-run` 必须报告 "No changes detected"。
  </action>
  <verify>
    <automated>cd server && uv run python manage.py makemigrations --check --dry-run</automated>
  </verify>
  <done>三条 migration 生成并可 migrate；makemigrations --check 报 No changes detected；CodingPlan / McpWorkItemTechnicalPlan 各有 canonical_plan_id UUIDField(null, db_index)。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 模型层守护测试</name>
  <files>server/tests/delivery/test_technical_plan_models.py</files>
  <behavior>
    - INV-2：`TechnicalPlan.objects.create(origin="chat")` work_item=None 合法；status 默认 draft。
    - SET_NULL：建 WorkItem + plan(work_item=wi)，删 WorkItem → plan.work_item_id 置 None、plan 存活。
    - 版本链：建 plan → v1(version=1)；建 v2(version=2, supersedes=v1)；同 (plan, version=1) 再 create → IntegrityError（unique_together）。
    - PlanExternalRef：external_ref 重复 create → IntegrityError（unique）；canonical 删除级联删 PlanExternalRef（CASCADE）。
    - 软链字段类型：`CodingPlan._meta.get_field("canonical_plan_id")` 与 `McpWorkItemTechnicalPlan._meta.get_field("canonical_plan_id")` 均为 UUIDField 且非关系字段（`is_relation is False`）——守护「软引用非硬 FK」。
  </behavior>
  <action>
新建 `server/tests/delivery/test_technical_plan_models.py`（`@pytest.mark.django_db`，对齐 test_plan_session_models.py 范式 + test_document* 的 IntegrityError 守护）。用 `pytest.raises(IntegrityError)` 包裹 unique 冲突断言（注意 Django 事务——必要时用 `transaction.atomic()` 隔离）。软链字段类型断言读 `_meta.get_field(...)`，不需要 import delivery。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/delivery/test_technical_plan_models.py -x -q</automated>
  </verify>
  <done>5 组行为全绿：INV-2 null work_item / SET_NULL / 版本链 unique_together / PlanExternalRef unique+CASCADE / 软链字段为 UUIDField 非 relation。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 应用代码 → DB schema | 本 plan 仅 DDL/模型，无外部输入；写入逻辑（半可信 payload）在 37-02 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-01 | Tampering | 旧表 canonical_plan_id 软链 | mitigate | UUIDField 软引用而非硬 FK——避免跨 app 耦合/循环依赖；守护测试断言 is_relation=False |
| T-37-02 | Repudiation | PlanVersion 版本链 | accept | append-only 版本 + supersedes 链留痕（不就地改写），归 37-02 service 写入 |
| T-37-01-SC | Tampering | npm/pip/cargo installs | mitigate | 无新增依赖（仅 Django ORM）；无 install 任务 |
</threat_model>

<verification>
- `cd server && uv run python manage.py makemigrations --check --dry-run` → No changes detected。
- `cd server && uv run pytest tests/delivery/test_technical_plan_models.py -q` 全绿。
- `cd server && uv run ruff format --check delivery/models/technical_plan.py` 通过（行宽 100）。
</verification>

<success_criteria>
- canonical `TechnicalPlan`/`PlanVersion`（origin/status/version/supersedes/content/content_hash）+ `PlanExternalRef` 落库（SC-1 schema 底座）。
- `TechnicalPlan.work_item` 可 null 且 SET_NULL（INV-2）。
- chat/mcp 旧表各加 `canonical_plan_id` UUID 软链字段（PLAN-03 schema 部分），无跨 app 硬 FK。
- makemigrations --check 干净；模型守护测试全绿。
- 模型层无任何落库/版本/关联业务逻辑（INV-6 留给 37-02）。
</success_criteria>

<output>
Create `.planning/phases/37-canonical-technicalplan-technicalplanservice/37-01-SUMMARY.md` when done
</output>
