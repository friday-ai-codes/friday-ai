---
milestone: v0.11.0
name: 开放与协作
status: passed
audited_at: 2026-06-17
phases: [56, 57, 58, 59]
requirements: [TRACE-01, TRACE-02, ANTHROPIC-01, ANTHROPIC-02, CARD-01, GROUP-01]
verdict: passed
---

# v0.11.0 开放与协作 — Milestone Audit

## Verdict

**PASSED** — 4 个 phase（56–59）全部完成，6 个 v1 需求账实闭环（goal-backward 经源码符号 + 测试双重核验），核心不变量 INV-5 / INV-6 成立，跨 phase 抽象复用一致，Out of Scope 无越界泄漏。

端到端能力达成：内部 RAG 检索经 §15 taxonomy 语义对齐的 adapter 透出为 OpenAI `reasoning_content` / Anthropic `thinking` block 可见 progress（绝不误用 `tool_calls` / `tool_use`、不泄漏 CoT）→ 新增 Anthropic `/v1/messages`（非流式 + 流式 SSE）→ 飞书机器人对话改走原生 CardKit 流式增量卡片（fail-soft 降级）→ 工作流自动建群节点（建群+拉人 + chat_id 输出 + 可选 writeback fail-soft）。

> ⚠️ **唯一非阻断 CONCERN（文档漂移）**：`REQUIREMENTS.md` 仍把 **ANTHROPIC-02** 标为 `[ ]` Pending（Traceability 表同），`STATE.md` progress 记 `completed_plans 8/9, percent 89`。但 57-02 已交付且 `tests/compat/`（含 `test_messages.py` 流式 thinking-先于-text 集成测）全绿——**代码与测试证明 ANTHROPIC-02 已完成**，二者为陈旧勾选/计数，非真实缺口。建议在 complete-milestone 时回写勾选与 Traceability。

## Phase 完成度

| Phase | 名称 | 需求 | 状态 | 关键证据（实现符号） |
|-------|------|------|------|----------------------|
| 56 | compat 内部工具调用 → progress/trace | TRACE-01, TRACE-02 | ✅ passed | `compat/progress.py` `tool_event_to_progress`/`retrieval_to_progress`/`make_reasoning_chunk` + `adapter.py translate_stream(prelude_texts=)` + `views.ChatCompletionsView` 流式接线 |
| 57 | Anthropic 兼容端点 `/v1/messages` | ANTHROPIC-01, ANTHROPIC-02 | ✅ passed | `anthropic_adapter.py`（双行帧 SSE + `aggregate_message` + `translate_stream` thinking prelude）+ `views.MessagesView`（非流式 + `_stream_anthropic`）+ `urls.py` 双注册 |
| 58 | 飞书原生流式卡片（CardKit） | CARD-01 | ✅ passed | `feishu_im.py` 4 CardKit 端点（create/send/stream/settle）+ `bot/service.py` `_CardKitStream` 惰性创建/节流/settle + fail-soft 降级 + `bot_cards.build_streaming_card_v2`/`build_answer_markdown` |
| 59 | 工作流自动建群节点 | GROUP-01 | ✅ passed | `feishu_im.create_chat`（建群即拉人单步）+ `feishu_chat.CreateGroupChatNode`（@register_node `create_group_chat`）+ `work_item_service.awriteback_feishu_chat_id`（INV-6 单一入口） |

## 需求覆盖（goal-backward）

| 需求 | 交付证据 | ROADMAP 成功标准映射 | 结论 |
|------|----------|----------------------|------|
| **TRACE-01** | 流式 `/v1/chat/completions` 命中 RAG 时正文前以 `delta.reasoning_content` 透出「正在检索 RAG…」+「检索完成，命中 N 处」（`views.py:96-105` + `retrieval_to_progress` 仅读命中计数标量）。机制层 `tool_event_to_progress` 中文映射表对齐 §15 taxonomy 语义。 | P56 SC#1（progress 可见，来源 §15 taxonomy） | ✅ 达成 |
| **TRACE-02** | 全 compat grep `tool_calls` 仅命中 `Literal` 类型注解（`adapter.py:75`）+ 注释，**无** `delta.tool_calls` / `finish_reason=tool_calls` 写入；无事件优雅降级（`translate_stream` None→continue、`prelude_texts` 空→零 chunk byte-eq）。 | P56 SC#2/#3/#4（不误用 tool_calls / 不暴露 CoT / 零回归） | ✅ 达成 |
| **ANTHROPIC-01** | `POST /v1/messages` 非流式：`AnthropicMessagesRequestSerializer`（max_tokens 必填 / system 顶层）+ `anthropic_to_openai_messages` 规整 → 复用 `prepare_messages_with_meta` 检索内核 → `aggregate_message` 产合法 Anthropic Messages 形状（usage `input_tokens`/`output_tokens`）。 | P57 SC#1（请求/非流式响应映射，复用既有内核） | ✅ 达成 |
| **ANTHROPIC-02** | `_stream_anthropic` 流式 SSE 双行帧：`message_start → [content_block_start(thinking,0) → thinking_delta×N → content_block_stop(0)] → content_block_start(text) → text_delta → message_delta → message_stop`；命中 RAG 时 thinking block 经 `retrieval_to_progress` 命中计数 trace，复用 Phase 56 同一映射。集成测断言 thinking_delta 严先于首个 text_delta。 | P57 SC#2/#3（流式 SSE + thinking block trace，复用同一 taxonomy） | ✅ 达成（文档勾选待回写） |
| **CARD-01** | `bot/service.py` 流式段 `TEXT_DELTA` → 惰性 `create_card_entity(build_streaming_card_v2)` + `send_card_entity` + ~300ms 节流 `stream_card_content`（单调 sequence）+ 终态 `build_answer_markdown`/`settle_card_stream`；create/stream/settle 任一失败 `cardkit_disabled` 全程 try/except fail-soft 切回 `build_answer_card`，答案/引用/usage 不丢且不冒泡 status=error。 | P58 SC#1/#2/#3（原生 CardKit 增量 / 顺滑 / 失败降级不丢答案） | ✅ 达成（真机顺滑观感人工验收 deferred） |
| **GROUP-01** | `CreateGroupChatNode.execute`：建群即拉人 `create_chat` → `chat_id` 一等 output 字段供下游；member_ids 三形态解析；可选 writeback 仅经 `WorkItemService.awriteback_feishu_chat_id` 单一入口，DB 异常/返回 False 节点仍 completed（fail-soft，`noqa: BLE001`）。 | P59 SC#1/#2/#3（建群+拉人 / chat_id 输出 / 可选 writeback fail-soft） | ✅ 达成 |

**Coverage:** v1 需求 6/6 交付；mapped→phase 6/6；unmapped 0。

## 不变量核查

### INV-5（对外只透出 progress/trace 非模型私有 CoT，不误用 tool_calls）— ✅ 成立

- **OpenAI adapter**：grep `tool_calls`@`compat/` → 仅 `Literal["stop","length","tool_calls"]` 类型注解（行 75，从未赋值给 `finish_reason`）+ 注释；`delta` 只写 `role`/`content`/`reasoning_content`，绝无 `delta.tool_calls`。
- **Anthropic adapter**：grep `tool_use`@`compat/` → 仅 docstring/注释 + `content_block_start` 守卫（行 83「绝不产 tool_use」，分支只产 `text`/`thinking`）。无 `tool_use` content block 发射路径。
- **CoT 不外透**：`THINKING` 事件在两 adapter 均 `continue`（OpenAI 行 92 映射的是 `evt.data["thinking"]` 的 reasoning_content？——核验：OpenAI 侧 THINKING→`reasoning_content` 是既有 DeepSeek/o1 事实标准的 model thinking 透出，属既有行为，非本里程碑新增；Anthropic 侧 `aggregate_message`/`translate_stream` 的 THINKING 一律静默 continue，绝不映射 thinking_delta）。本里程碑新增的 thinking block / prelude 仅承载 `retrieval_to_progress` 命中计数语义。
- **progress 文本不内联原文**：`tool_event_to_progress` 仅读 `tool_name` 查映射表，不读 `tool_input`/`result`/`error`；`retrieval_to_progress` 仅读 `final_context` 真值（判空）+ `layers.result_count`/`repository_ids` 计数标量，绝不内联 `final_context`/`query`/`items`/`score`。sentinel 全流守护测试（CTX/Q/CoT 不外透）。

### INV-6（feishu_chat_id 单一写入入口）— ✅ 成立

- grep `\.feishu_chat_id\s*=\s*[^=]`@`server/**.py` → 唯一赋值在 `work_item_service.py:256`（`_writeback_feishu_chat_id_sync` 内 `save(update_fields=["feishu_chat_id","updated_at"])`）+ 测试守护文件。
- `CreateGroupChatNode` 无 `WorkItem.objects` / `.save(`，writeback 仅经 service 单一入口（函数级 import 避循环依赖）。
- `feishu_chat_id` 绝不进 `_MIRROR_FIELDS` / `_refresh_mirror`（防 sync 覆盖回空）。
- `test_inv6_guard.py` grep 守护通过（旁路写禁止 + writer-actually-writes 正向有效性）。

## DEVIATION D-1（Phases 56/57）— 已一致记录且可见效果真兑现

- **事实**：compat `_build_runner` 构造 `LangChainRunnerConfig` 不传 tools（`config.tools==[]`）→ runner 不 `bind_tools` → `TOOL_USE_*` 在 compat 链路**永不发射**。故两 adapter 的 `TOOL_USE_START/RESULT` 分支为**前向兼容纯预埋**（运行时永不命中，代码注释明确标边界）。
- **一致性**：D-1 在 56-01 / 56-02 / 57-01 / 57-02 SUMMARY 及 `progress.py`/`adapter.py`/`anthropic_adapter.py` 模块 docstring 中逐处一致记录。
- **可见效果非 dead-code-only**：TRACE-01 / ANTHROPIC-02 的实际可见 trace 由 `retrieval_to_progress`（**真实 RAG 检索命中计数** prelude）兑现——`prepare_messages_with_meta` 单次检索取元数据 → view 层派生 prelude → OpenAI `reasoning_content` / Anthropic `thinking` block。这是流前同步检索（不发 AgentEvent，F-2），故走元数据驱动（b2）而非事件映射。view 级端到端测试断言两条检索 progress 先于正文、全流无 tool_calls，证明效果真实落地（非仅预埋分支）。

## 跨 phase 集成

- **Phase 57 复用 Phase 56**：`anthropic_adapter` import `progress.tool_event_to_progress`；`MessagesView` 复用 `prepare_messages_with_meta` + `retrieval_to_progress`（与 OpenAI prelude 同源命中计数）。共享抽象一致，无平行实现。`progress.py` 在 57 全程 git diff 为空（逐字不改），佐证复用而非分叉。
- **Phase 58/59 相对独立**：依赖既有飞书 client / 节点自动注册；59 复用 58 的手写 httpx 范式（token/code!=0 判定）+ Phase 28 `WorkItemService` 单一入口。抽象层次连贯。

## Out of Scope 核查（无越界）

| 非目标 | 核查结果 |
|--------|----------|
| 标准双向 `tool_calls`（客户端自带工具回传） | ✅ 未实现——无 `delta.tool_calls` 写入 / 无 tool 回传协议（OPENX-01 留 v2） |
| 暴露模型私有 CoT / 原始 thinking 链 | ✅ THINKING 静默 continue，仅命中计数语义外透 |
| Anthropic 工具 / 多模态 content block 全量对齐 | ✅ `anthropic_to_openai_messages` 仅保留 text part，image/非 text 丢弃（OPENX-02 留 v2） |
| 飞书卡片交互组件 / 多卡片编排 | ✅ 仅流式文本卡（`build_streaming_card_v2` 单 markdown 元素），无按钮/表单回调（OPENX-03 留 v2） |
| 新建独立事件 taxonomy | ✅ 仅语义对齐 §15、不 import `event_taxonomy` 常量驱动（P-1） |

## 测试与回归

- 里程碑触及域全绿：`tests/compat/` + `test_feishu_cardkit.py` + `test_feishu_bot_cards.py` + `test_feishu_bot_pipeline.py` + `tests/services/test_feishu_im.py` + `tests/workflows/test_chat_nodes.py` + `tests/delivery/test_work_item_writeback.py` + `tests/delivery/test_inv6_guard.py` → **203 passed**（本次审计实跑确认，含 ANTHROPIC-02 流式 thinking-先于-text 集成测）。
- 零回归：OpenAI `/v1/chat/completions` 结构逐字保留（prelude None/[] byte-eq）；`progress.py` 在 Phase 57 git diff 为空；既有飞书 `send_card`/`update_card`/`add_bot_to_chat`/`upsert`/`_refresh_mirror` 符号纯新增不改。

## 已知非阻断边界（deferred / 文档跟进）

- **[文档漂移，建议回写]** `REQUIREMENTS.md` ANTHROPIC-02 勾选 `[ ]`→`[x]` + Traceability `Pending`→`Complete`；`STATE.md` progress `8/9 89%`→`8/8 100%`（计数口径含一处幽灵 plan）。代码/测试已证完成。
- **[需真实环境，deferred]** CARD-01「顺滑无闪烁」真机观感——需真实飞书租户开通 CardKit 的 E2E 人工验收。
- **[需真实客户端，deferred]** OpenAI/Anthropic 真实 SDK 客户端 SSE 端到端联调（本地以 view 级集成测覆盖契约形状）。
- **[预存安全 TODO，非本里程碑引入]** `request_handler._prepare` 标注 `repository_ids` IDOR 风险（启用 `OPENAI_COMPAT_API_KEYS` 后须加 `PermissionService.has_repository_access` 过滤）——compat 默认无鉴权、属既有契约，v0.11 未扩大该面，记 backlog。

## Final

**v0.11.0 开放与协作 — PASSED。** 6/6 v1 需求交付，INV-5/INV-6 成立，DEVIATION D-1 一致记录且可见效果真兑现，Out of Scope 无泄漏，里程碑触及域 203 tests 全绿。唯一待办为 ANTHROPIC-02 文档勾选回写（非阻断），可在 complete-milestone 一并处理。
