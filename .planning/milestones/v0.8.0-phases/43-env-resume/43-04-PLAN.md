---
phase: 43-env-resume
plan: 04
type: execute
wave: 3
depends_on: [43-02, 43-03]
files_modified:
  - server/workflows/nodes/ai/plan_research.py
  - server/agents/tools/plan_research_tools.py
autonomous: true
requirements: [RESUME-01]
must_haves:
  truths:
    - "工作流节点 plan_research advance 循环复用共享 helper adrive_plan_session_to_pause_or_terminal（不再各造一套循环）"
    - "chat 工具 start_plan_research advance 循环复用同一共享 helper（入口私有挂起 marker 映射保留，行为等价）"
    - "clarifying-未答 与 researching-在途 两种挂起情形行为零回归：helper 短路后节点/工具仍返回 waiting_event/挂起 marker，needs-clarification 会话不被错误 FAILED"
    - "start_plan_research 工具的占位文案/description 接通后如实改为「调研完成后自动融合回流」，不再声称未接入"
  artifacts:
    - path: "server/workflows/nodes/ai/plan_research.py"
      provides: "节点 advance 循环复用共享 helper（行为零变更）"
      contains: "adrive_plan_session_to_pause_or_terminal"
    - path: "server/agents/tools/plan_research_tools.py"
      provides: "工具 advance 循环复用 helper + 文案如实更新"
      contains: "adrive_plan_session_to_pause_or_terminal"
  key_links:
    - from: "server/workflows/nodes/ai/plan_research.py execute 循环"
      to: "services.plan_orchestration.adrive_plan_session_to_pause_or_terminal"
      via: "复用共享续驱循环"
      pattern: "adrive_plan_session_to_pause_or_terminal"
    - from: "server/agents/tools/plan_research_tools.py start_plan_research 循环"
      to: "services.plan_orchestration.adrive_plan_session_to_pause_or_terminal"
      via: "复用共享续驱循环"
      pattern: "adrive_plan_session_to_pause_or_terminal"
---

<objective>
RESUME-01「不造两套」收尾：把工作流节点（`plan_research.py:142-167`）与 chat 工具（`plan_research_tools.py:124-153`）两处逐行同构的 advance 循环，重构为复用 43-02 的共享 helper `adrive_plan_session_to_pause_or_terminal`——使三处（节点 / 工具 / 43-03 回调消费者）真正同源一份续驱逻辑（RESEARCH 步骤 1）。入口私有的挂起 marker 映射（`NodeResult` / `ToolResult` via `_maybe_suspend`）各自保留（驱动是入口私有，对齐 `entrypoint.py` docstring）。同时（43-03 接通后）如实更新 `start_plan_research` 占位文案 / 工具 description（去掉「自动回流尚未接入」）。

Purpose: 落 CONTEXT「底层续驱 engine 的逻辑同源、不造两套」的最终态；接通后文案不再误导用户。
Output: 两处 advance 循环复用共享 helper（行为等价）；工具文案如实更新。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-clean/.codex/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-clean/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/43-env-resume/43-CONTEXT.md
@.planning/phases/43-env-resume/43-RESEARCH.md
@.planning/phases/43-env-resume/43-PATTERNS.md
@.planning/phases/43-env-resume/43-VALIDATION.md

# 共享 helper（43-02 产出）
@server/services/plan_orchestration/resume.py
# 工作流节点 advance 循环（待复用 helper）
@server/workflows/nodes/ai/plan_research.py
# chat 工具 advance 循环 + 占位文案（待复用 + 文案更新）
@server/agents/tools/plan_research_tools.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 工作流节点 plan_research advance 循环复用共享 helper（行为零变更）</name>
  <files>server/workflows/nodes/ai/plan_research.py</files>
  <action>
    在 `AIPlanResearchNode.execute` 的驱动循环（`plan_research.py:141-169`）处，把内联 while 循环改为复用共享 helper：`from services.plan_orchestration import adrive_plan_session_to_pause_or_terminal`，调用 `session = await adrive_plan_session_to_pause_or_terminal(engine, session, max_steps=_MAX_ADVANCE_STEPS)` 续驱到「重挂起短路 / 终态」，然后保留入口私有的 `_maybe_suspend(session)` 判定（节点 researching/clarifying 处仍返回 `NodeResult` waiting_event marker）与 `_map_terminal(session)` 终态映射。**前置条件（BLOCKER 修复依赖）：** 43-02 的 helper 必须**同时**在 researching-在途 与 clarifying-未答（`Clarification.answered_at__isnull=True`）两种情形短路返回——否则 needs-clarification 会话会被 helper 一路 advance 到 max_steps 被错误 FAILED，回归 `test_plan_research_node.py::test_clarifying_suspends_waiting_event`（断言 `status=="waiting_event"`、`kind=="clarification"`、`session.status==CLARIFYING`）。helper 在 CLARIFYING-pending 短路返回后，节点再跑一次 `_maybe_suspend(session)` 即得到正确的 clarification waiting_event marker。researching 在途短路同理（都用 `aall_research_tasks_terminal`）。行为须**等价**于现状（不改 waiting_event / 终态语义，不改分支命名）。step 上限 fail 由 helper 内部处理（helper 已 transition fail 并返回 FAILED session → `_map_terminal` 走 failed 分支）。保留既有日志语义。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/workflows/test_plan_research_node.py -x</automated>
  </verify>
  <done>节点复用共享 helper；`tests/workflows/test_plan_research_node.py` 全绿，**含 `test_clarifying_suspends_waiting_event`**（clarifying-pending 仍返回 waiting_event/kind=clarification、不被 FAILED）+ waiting_event / 终态 / step 上限行为零回归。</done>
</task>

<task type="auto">
  <name>Task 2: chat 工具 start_plan_research 复用 helper + 文案如实更新</name>
  <files>server/agents/tools/plan_research_tools.py</files>
  <action>
    1. 把 `start_plan_research` 的驱动循环（`plan_research_tools.py:124-153`）改为复用共享 helper：`from services.plan_orchestration import adrive_plan_session_to_pause_or_terminal`，`session = await adrive_plan_session_to_pause_or_terminal(engine, session, max_steps=_MAX_ADVANCE_STEPS)`，然后保留 `_maybe_suspend(session, conversation_id)`（clarifying/researching 返回 `ToolResult` 挂起 marker + `register_blocking_task`）与 `_map_terminal(session)`。**前置条件（BLOCKER 修复依赖）：** helper 必须同时在 clarifying-未答（`Clarification.answered_at__isnull=True`，与工具 `_maybe_suspend` 的 clarifying 分支 `plan_research_tools.py:218-225` 同一 query）与 researching-在途短路返回——否则 needs-clarification 会话被 helper advance 到 max_steps 错误 FAILED，回归工具澄清挂起。helper 短路返回后工具再跑 `_maybe_suspend` 得到正确的 clarification/blocking marker。行为须等价于现状。
    2. 文案如实更新（43-03 已接通自动回流）：`_maybe_suspend` 的 researching 占位文案（`plan_research_tools.py:262-270`）去掉「自动回流尚未接入 / 当前不会自动继续」的否定表述，改为肯定表述，如「已发起跨仓方案编排调研（session=...，状态=...）；深入调研容器运行中，调研完成后将自动融合并返回主方案」。同步检查工具 description（约 `:41`）若含「自动回流尚未接入」类措辞，一并改为已接入的如实描述。仅改文案，不改 marker/挂起协议。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/agents/test_start_plan_research_tool.py -x</automated>
  </verify>
  <done>工具复用共享 helper（clarifying-pending 与 researching-在途仍正确返回挂起 marker、不被 FAILED）；占位文案/description 不再声称「自动回流尚未接入」；`tests/agents/test_start_plan_research_tool.py` 全绿（挂起/终态行为零回归）。若既有用例硬断言旧文案，同步更新断言为新文案。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 内部重构 | 仅复用同源 helper + 文案更新，不引入新输入跨界、不改信任面 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-REGRESS | Tampering (state integrity) | advance 循环重构 | mitigate | 行为须等价：helper 同时短路 clarifying-未答 + researching-在途 + 入口私有 `_maybe_suspend` marker 映射保留；status 仍只经 transition；既有节点/工具测试（含 `test_clarifying_suspends_waiting_event`）守护零回归 |
| T-43-MISLEAD | Repudiation/UX | 工具占位文案 | accept | 接通后文案如实更新为「自动融合回流」，消除误导（CONTEXT Discretion，倾向同步更新） |
</threat_model>

<verification>
- `cd server && uv run pytest tests/workflows/test_plan_research_node.py tests/agents/test_start_plan_research_tool.py -x` 全绿。
- Phase gate（在 /gsd-verify-work 前）：`cd server && uv run pytest` 全绿。
</verification>

<success_criteria>
- 三处（节点 / 工具 / 43-03 回调消费者）复用同一共享续驱 helper（不造两套）。
- 节点 / 工具的挂起与终态行为零回归（既有测试全绿）。
- `start_plan_research` 文案如实反映自动回流已接入。
</success_criteria>

<output>
Create `.planning/phases/43-env-resume/43-04-SUMMARY.md` when done.
</output>
