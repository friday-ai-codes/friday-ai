---
phase: 43-env-resume
plan: 03
type: execute
wave: 2
depends_on: [43-02]
files_modified:
  - server/subagent/api/callbacks.py
  - server/tests/services/test_research_completion_callback.py
autonomous: true
requirements: [RESUME-01]
must_haves:
  truths:
    - "chat 入口 plan_research 容器全部终态后，callback 驱动 PlanSession engine 续驱到 done（消化 D-2 缺口 b）"
    - "续驱到终态后调用 BarrierManager.task_completed(str(plan_session.id), result) 回灌 chat 会话（消化 D-2 缺口 a）"
    - "工作流入口（有 node_execution）不走 chat 续驱，仍走 _schedule_workflow_resume（回归守护）"
    - "重复 completed 回调 / 部分调研未终态 → 不重复续驱、不重复 notify（幂等 no-op）"
    - "续驱内部抛异常 → 回调仍返 200（fail-soft swallow）"
    - "plan_research 容器 failed → barrier success=False 回灌不卡死"
    - "chat 续驱仅在 PlanSession.entrypoint==chat 守门下触发（runner 可改字段不放大信任面）"
  artifacts:
    - path: "server/subagent/api/callbacks.py"
      provides: "_schedule_chat_plan_resume + _schedule_agent_session_resume plan_research 分支接线"
      contains: "_schedule_chat_plan_resume"
    - path: "server/tests/services/test_research_completion_callback.py"
      provides: "chat 续驱 + barrier 回灌闭环 / 回归 / 幂等 / fail-soft / 失败路径 集成测试"
      contains: "task_completed"
  key_links:
    - from: "server/subagent/api/callbacks.py _schedule_agent_session_resume (plan_research 分支)"
      to: "_schedule_chat_plan_resume"
      via: "entrypoint==chat 守门后调用"
      pattern: "_schedule_chat_plan_resume"
    - from: "_schedule_chat_plan_resume"
      to: "services.plan_orchestration.adrive_plan_session_to_pause_or_terminal"
      via: "engine 续驱"
      pattern: "adrive_plan_session_to_pause_or_terminal"
    - from: "_schedule_chat_plan_resume"
      to: "orchestration.barrier.get_barrier_manager().task_completed"
      via: "task_id=str(plan_session.id) 回灌"
      pattern: "task_completed"
---

<objective>
RESUME-01 步骤 2/3（接线）：新增 `_schedule_chat_plan_resume`（mirror 同文件 `_schedule_workflow_resume`：fire-and-forget + 幂等 + fail-soft），并把 `_schedule_agent_session_resume` 的 `plan_research` 分支当前的提前 `return`（`callbacks.py:121-134`）改为「chat 入口守门 → 续驱 engine 到终态 → barrier 回灌」。该分支已在 `_handle_completed`（`callbacks.py:654`）与 `_handle_failed`（`callbacks.py:715`）两路被调用，天然覆盖 completed + failed。消化 v0.7 audit D-2 两处缺口：(a) chat barrier 从不被通知；(b) `amaybe_complete_research` 只 researching→merging、chat 入口此后无消费者驱动 `engine.advance` 到 done。

Purpose: 让 chat 入口 deep-research 容器在途完成后自动续驱 + 回灌主方案，与工作流入口共享 43-02 的同源续驱 helper（不造两套）。
Output: `callbacks.py` 新增 `_schedule_chat_plan_resume` + 分支接线；`test_research_completion_callback.py` 新增闭环 / 回归 / 幂等 / fail-soft / 失败路径集成测试。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-clean/.codex/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-clean/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/43-env-resume/43-CONTEXT.md
@.planning/phases/43-env-resume/43-RESEARCH.md
@.planning/phases/43-env-resume/43-PATTERNS.md
@.planning/phases/43-env-resume/43-VALIDATION.md

# 接线点 + 结构蓝本（_schedule_workflow_resume / _notify_barrier_manager / plan_research 分支）
@server/subagent/api/callbacks.py
# 同源续驱 helper（43-02 产出）
@server/services/plan_orchestration/resume.py
# engine 构造单一工厂
@server/services/plan_orchestration/entrypoint.py
# 在途短路 + researching→merging
@server/services/plan_orchestration/research_aggregation.py
# 集成测试范式 + chat-entry fixture
@server/tests/services/test_research_completion_callback.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 新增 _schedule_chat_plan_resume + 接线 plan_research 分支</name>
  <files>server/subagent/api/callbacks.py</files>
  <action>
    1. 新增 `def _schedule_chat_plan_resume(session: SubAgentSession, log: BoundLogger) -> None`，与同文件 `_schedule_workflow_resume`（`callbacks.py:193-282`）对称结构、`_notify_barrier_manager`（`callbacks.py:54-97`）的 fire-and-forget 尾部范式。内部 `async def _resume()`（全程独立 `try/except` swallow + `log.warning("chat_plan_resume_error", session_id=session.session_id)`，绝不让回调主流程 5xx），按时序（Pitfall 4 — 同协程内顺序）：
       a. 从 `session.last_output["plan_session_id"]` 取 plan_session（`PlanSession.objects.filter(id=...).afirst()`）；缺失/取不到 → return。
       b. **守门**（T-43-TAMPER）：`if str(plan_session.entrypoint) != str(PlanSessionEntrypoint.CHAT): return`（用服务端权威字段 `entrypoint`，绝不信 runner 可改字段；`PlanSessionEntrypoint` from `delivery.models`）。
       c. 幂等短路：`if not await aall_research_tasks_terminal(plan_session.id): return`（多仓逐个完成时只有最后一个使全终态者真正续驱）。
       d. `engine = build_orchestration_engine()`（无 `node_execution_id`，chat 入口；`from services.plan_orchestration import build_orchestration_engine`）。**绝不**新建第二个 engine 工厂。
       e. `plan_session = await adrive_plan_session_to_pause_or_terminal(engine, plan_session)`（**先**续驱到终态；`from services.plan_orchestration import adrive_plan_session_to_pause_or_terminal`，43-02 产出）。
       f. **再**构建 `BlockingTaskResult`（`from orchestration.contracts import BlockingTaskResult`）：`task_id=str(plan_session.id)`、`task_type="plan_research"`、`success=(plan_session.status == PlanSessionStatus.DONE)`、`output=<主方案摘要 / plan_version_id 文本>`（复用 deep_analysis 回灌通道，A2：失败时 output="")、`error=("" if success else str(plan_session.error or {}))`。
       g. `await get_barrier_manager().task_completed(str(plan_session.id), result)`（`from orchestration.barrier import get_barrier_manager`；**关键 Pitfall 3：task_id 用 `str(plan_session.id)` 而非 `session.session_id`** —— chat barrier 注册键见 `plan_research_tools.py:249`）。barrier 已去重（`task_id in results` → False no-op，幂等安全）。
       尾部 fire-and-forget：照搬 `callbacks.py:277-282` 的 `loop = asyncio.get_running_loop(); loop.create_task(_resume())` / `except RuntimeError: asyncio.run(_resume())`。
    2. 接线（选项 A，最小耦合）：把 `_schedule_agent_session_resume` 的 `plan_research` 分支（`callbacks.py:121-134`）当前的 `log.debug(...); return` 改为：取 plan_session（从 `session.last_output["plan_session_id"]`）→ 守门 `entrypoint==CHAT` → 调 `_schedule_chat_plan_resume(session, log)` 后 return。保留现状注释中「绝不在此触发 SDKAgentRunner resume（幽灵 agent 风险）」的语义——**不**触发 agent resume，只走 `_schedule_chat_plan_resume`。守门可在 `_schedule_chat_plan_resume` 内做（步骤 b），分支处只需判定 `src == "plan_research"` 即委派（避免重复查询，分支保持薄）。
    日志仅记非敏感字段（plan_session_id / status / barrier_satisfied），对齐既有 `barrier_task_notified` 范式（`callbacks.py:84-89`）。
  </action>
  <verify>
    <automated>cd server && uv run python -c "from subagent.api.callbacks import _schedule_chat_plan_resume; print('ok')"</automated>
  </verify>
  <done>`_schedule_chat_plan_resume` 定义且可导入；`_schedule_agent_session_resume` 的 plan_research 分支委派到新函数（chat 入口守门），不触发 agent resume。</done>
</task>

<task type="auto">
  <name>Task 2: 扩展 test_research_completion_callback.py 闭环 + 回归 + 幂等 + fail-soft + 失败路径</name>
  <files>server/tests/services/test_research_completion_callback.py</files>
  <behavior>
    - chat_resume_done：chat 入口（`_setup` 默认 entrypoint=CHAT）唯一 RepoResearchTask 完成 → `_handle_completed` → 续驱使 PlanSession 到 `done`（mock merge/architect adapter 把 merging→done）。
    - barrier_notified：续驱到终态后 `BarrierManager.task_completed(str(plan_session.id), ...)` 被调用（注册真 barrier 后断言 satisfied，或 spy `task_completed` 调用参数 task_id==str(plan_session.id)）。
    - workflow_not_chat：构造 entrypoint=WORKFLOW + 有 node_execution 的 session → 不走 `_schedule_chat_plan_resume`（断言其未被调用），仍由 `_schedule_workflow_resume` 处理（回归守护）。
    - idempotent：重复 `_handle_completed` 回调 / 部分调研未终态（多仓其一仍 RUNNING）→ 不重复续驱、不重复 notify（no-op）。
    - swallow：`_schedule_chat_plan_resume` 续驱内部抛异常（mock adrive/engine raise）→ `_handle_completed` 仍返 200。
    - failed_path：plan_research 容器 failed（`_handle_failed`）→ barrier `success=False` 回灌不卡死（断言 task_completed 以 success=False 调用 或 session 不卡在 researching）。
  </behavior>
  <action>
    直接扩展同文件（`tests/services/test_research_completion_callback.py`），复用 `_setup` chat-entry fixture（`:32-63`，已建 entrypoint=CHAT 的 PlanSession + RUNNING RepoResearchTask + plan_research SubAgentSession，`last_output` 含 `plan_session_id`）、`_log`（`:66-70`）。关键：新增测试须**放开** `_PATCHES` 里对 `_schedule_agent_session_resume` 的整体 mock（`:76`）——否则分支委派被吞；改为让真实 `_schedule_agent_session_resume` 跑、按需 spy `_schedule_chat_plan_resume`（`patch("subagent.api.callbacks._schedule_chat_plan_resume")`）断言调用与否，或放行真实续驱并 mock IO 边界（mock merge adapter / `build_orchestration_engine` 使 merging→done，pytest-socket 禁网）。fire-and-forget 协程需在断言前 await 完成（参考既有用例对 `create_task` 的处理；可直接 await 内部协程或用 `asyncio` 调度后 flush）。`workflow_not_chat` 用例需建 entrypoint=WORKFLOW 的 PlanSession + node_execution。DB marker 已在文件头 `pytestmark = pytest.mark.django_db(transaction=True)`（`:29`）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/test_research_completion_callback.py -x</automated>
  </verify>
  <done>新增 chat_resume_done / barrier_notified / workflow_not_chat / idempotent / swallow / failed_path 用例全绿；既有用例无回归。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runner → server callback | 容器回调 payload + SubAgentSession.last_output 半可信（runner 可经 progress 篡改 plan_session_id / research_task_id） |
| server → chat 会话 | barrier 回灌驱动 LangGraph resume，影响用户会话 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-TAMPER | Tampering / Elevation | `_schedule_chat_plan_resume` 取 plan_session | mitigate | 以服务端权威字段 `PlanSession.entrypoint == CHAT` 守门（沿用 cross_repo_relevance 用权威字段而非 runner 可改字段范式，`callbacks.py:1121-1126`）；不放大既有信任面。Task 2 `workflow_not_chat` 守护 |
| T-43-DOS | Denial of Service | resume 副作用 | mitigate | fire-and-forget + 独立 try/except swallow + warning，绝不让回调 5xx；barrier/transition 幂等去重防重放。Task 2 `swallow` + `idempotent` 守护 |
| T-43-INFO | Information Disclosure | 续驱日志 | mitigate | 仅记 plan_session_id / status / barrier_satisfied 非敏感字段（对齐 `barrier_task_notified` 范式） |
</threat_model>

<verification>
- `cd server && uv run pytest tests/services/test_research_completion_callback.py -x` 全绿。
- Wave 合并采样：`cd server && uv run pytest tests/test_coding_node.py tests/services/ tests/workflows/test_plan_research_node.py tests/agents/test_start_plan_research_tool.py tests/services/test_orchestration_entry_consistency.py`。
</verification>

<success_criteria>
- chat 入口 plan_research 全终态 → engine 续驱到 done + barrier `task_completed(str(plan_session.id))` 回灌（D-2 a/b 消化）。
- 工作流入口不走 chat 续驱（回归守护）。
- 幂等 + fail-soft + 失败路径均有自动化断言通过。
- entrypoint==chat 守门生效。
</success_criteria>

<output>
Create `.planning/phases/43-env-resume/43-03-SUMMARY.md` when done.
</output>
