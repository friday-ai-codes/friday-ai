---
phase: 46-pr-pr
plan: 02
type: execute
wave: 2
depends_on: ["46-01"]
files_modified:
  - server/workflows/services/pr_cross_reference.py
  - server/workflows/services/__init__.py
  - server/workflows/nodes/ai/coding.py
  - server/tests/workflows/test_pr_cross_reference.py
autonomous: true
requirements: [PR-02]
must_haves:
  truths:
    - "成功创建 MR 的仓 ≥ 2 时，每个 PR 描述被回写追加「## 关联 PR」段，含其它兄弟仓 PR 链接（排除自身）"
    - "PR 描述含「关联方案 / 工作项」追溯段（TechnicalPlan 标识 + WorkItem 飞书工作项），经 plan_version_id → PlanVersion → TechnicalPlan → WorkItem 反查"
    - "单仓 / 成功仓 < 2 时不做 cross-ref 回写（不调 _get_repo/_get_project）"
    - "cross-ref / 追溯任一环失败仅 logger.warning，收尾仍 completed、PR 仍在 output、绝不回灌容器回调 5xx"
    - "追溯链断（plan_version_id 缺 / 链取不到）→ 省略追溯段、不抛、PR 仍创建"
  artifacts:
    - path: "server/workflows/services/pr_cross_reference.py"
      provides: "可复用 cross-ref + 追溯 helper（纯函数段生成 + async 追溯渲染 + async 回写编排）"
      min_lines: 80
    - path: "server/workflows/services/__init__.py"
      provides: "barrel 导出 pr_cross_reference 符号"
      contains: "pr_cross_reference"
    - path: "server/workflows/nodes/ai/coding.py"
      provides: "_finalize_and_notify 内 MR 创建循环后 cross-ref 段（≥2 守门 + fail-soft）"
      contains: "add_cross_references"
    - path: "server/tests/workflows/test_pr_cross_reference.py"
      provides: "PR-02 守护测试（纯函数 + 回写 + 追溯 + fail-soft + 守门）"
      min_lines: 120
  key_links:
    - from: "server/workflows/nodes/ai/coding.py:_finalize_and_notify"
      to: "workflows.services.pr_cross_reference.add_cross_references"
      via: "MR 创建循环后调用（len(successful) >= 2 守门 + try/except fail-soft）"
      pattern: "add_cross_references\\("
    - from: "pr_cross_reference.add_cross_references"
      to: "client._get_repo / client._get_project"
      via: "asyncio.to_thread 回写 PR 描述（edit / save）"
      pattern: "_get_repo|_get_project"
    - from: "pr_cross_reference.render_traceability_section"
      to: "PlanVersion / TechnicalPlan / WorkItem"
      via: "plan_version_id → afirst 逐跳（*_id 标量，async ORM 安全）"
      pattern: "PlanVersion\\.objects\\.filter"
---

<objective>
PR-02：在多仓 wave 编码收尾段 `AICodingNode._finalize_and_notify`（`coding.py:1070`）批量创建 MR 之后，对成功名单（≥2 仓）做一次描述回写——追加「## 关联 PR」cross-ref 段（兄弟仓 PR 链接，排除自身）+「关联方案 / 工作项」可追溯段（`plan_version_id → PlanVersion → TechnicalPlan → WorkItem`）。复用 `CreatePRNode` 已验证的「先建后回写」两段式，提取为**可复用 helper** `workflows/services/pr_cross_reference.py`，全程 fail-soft。

Purpose: 同一方案下多仓产出的 PR 互相引用且可追溯到同一 `TechnicalPlan`/`WorkItem`（PR-02），不另造第二套创建/回写路径（硬约束）。
Output: 新 helper 模块 `pr_cross_reference.py`（纯函数 cross-ref 段 + async 追溯渲染 + async 回写编排）+ barrel 导出 + `_finalize_and_notify` 接线 + `test_pr_cross_reference.py` 守护测试。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-clean/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-clean/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/46-pr-pr/46-CONTEXT.md
@.planning/phases/46-pr-pr/46-RESEARCH.md
@.planning/phases/46-pr-pr/46-VALIDATION.md
@.planning/phases/46-pr-pr/46-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
新模块 `server/workflows/services/pr_cross_reference.py`，导出以下符号（经 `workflows/services/__init__.py` barrel 再导出）：

- `generate_cross_reference_section(current_pr_url: str, all_successful: list[dict]) -> str` — **纯函数**：渲染「## 关联 PR」段，列出 `all_successful` 中 url != current_pr_url 的兄弟仓 `- [repository_name](mr_url)`；无兄弟（单 PR）返回 `""`（同源 `CreatePRNode._generate_cross_reference_section`，`pr.py:285`，文案中文化为「## 关联 PR」per D-06）。
- `render_traceability_section(plan_version_id: str | None) -> str` — **async**：经 `plan_version_id → PlanVersion → TechnicalPlan → WorkItem`（`afirst` + `*_id` 标量逐跳）渲染「## 关联方案 / 工作项」段；任一跳取不到 / 异常 → 返回 `""`（fail-soft，D-07）。
- `add_cross_references(successful_mrs: list[dict], *, plan_version_id: str | None) -> dict[str, bool]` — **async**：编排——先 `render_traceability_section` 取追溯段（一次），再对每个成功 MR 经其 `repository_id` 重取 `Repository` + `aresolve_git_token` + `get_git_platform_client`，按平台 `_get_repo().get_pull(int(mr_id)).edit(body=...)` / `_get_project().mergerequests.get(int(mr_id)).save()`（`asyncio.to_thread`）回写 `原 description + cross-ref 段 + 追溯段`；逐 PR try/except fail-soft，返回 `{mr_url: bool}`（同源 `CreatePRNode._add_cross_references`，`pr.py:312`）。

> **D-09 决策（planner，已文档化）：** 采纳 D-09 备选——仅 wave 路径用新 helper，`CreatePRNode` **保持原样不改**（其 cross-ref 段为英文「## Related PRs」+ 共享单一 body + 并行 gather 回写，重构会改文案/触发 `test_batch_pr.py` 回归，blast radius 不值当）。helper docstring 显式标注「同源 `CreatePRNode._generate_cross_reference_section` / `_add_cross_references`，后续统一留 backlog」。这是最小 diff / 零回归选择，D-09 明确允许。

测试文件 `server/tests/workflows/test_pr_cross_reference.py`（无新生产符号）。
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: pr_cross_reference helper + 纯函数/追溯测试</name>
  <files>server/workflows/services/pr_cross_reference.py, server/workflows/services/__init__.py, server/tests/workflows/test_pr_cross_reference.py</files>
  <read_first>
    - server/workflows/nodes/git/pr.py（行 285-310：`_generate_cross_reference_section` 纯函数蓝本；行 312-418：`_add_cross_references` 的 `_get_repo`/`_get_project` + `asyncio.to_thread` + 逐 PR try/except fail-soft 回写蓝本；行 1-22：imports `aresolve_git_token`/`get_git_platform_client`/`MRCreateRequest`）
    - server/workflows/services/mr_service.py（行 13-41：`build_mr_description` 中文段渲染风格「## 关联飞书工作项」，追溯段文案借鉴）
    - server/workflows/services/__init__.py（现 barrel：导出 mr_service 符号；本任务追加 pr_cross_reference 导出）
    - server/delivery/models/technical_plan.py（`PlanVersion` 字段 plan(FK→TechnicalPlan)/version；`TechnicalPlan` 字段 work_item(FK,nullable)/id；注意循环 FK，用 `pv.plan_id` 标量再查）
    - server/delivery/models/work_item.py（`WorkItem` **无 url 字段**，仅三元组 `feishu_project_key`/`work_item_type`/`work_item_id` + `title` + `prd_url`/`tech_doc_url`；飞书链接从三元组构造或降级纯文本，per Research Open Q1 / A2）
    - server/tests/test_batch_pr.py（mock client 范式：`MagicMock(_get_repo / _get_project)` + `AsyncMock`）
    - server/tests/test_coding_wave.py（行 1-90：`django_db(transaction=True)` + 真实 PlanVersion/TechnicalPlan 行造测）
  </read_first>
  <behavior>
    - generate_cross_reference_section（纯函数）：`all_successful` 多个 → 段含其它兄弟仓 `- [repo_name](mr_url)`、排除 current_pr_url；仅 1 个（无兄弟）→ 返回 `""`；段标题为「## 关联 PR」。
    - render_traceability_section（async）：plan_version_id 为 None / 空 → `""`；链全在（PlanVersion→TechnicalPlan，work_item 存在）→ 段含 TechnicalPlan 标识（`tp.id` / version）+ WorkItem 标识（`work_item_type`/`work_item_id` + `title`，有 `prd_url` 则附）；PlanVersion 取不到 / TechnicalPlan 取不到 / 任意异常 → `""` 且不抛。
    - add_cross_references（async）：对 GitHub mock（client 有 `_get_repo`）断言 `get_pull(int(mr_id))` + `pr.edit(body=...)` 被调，body 含兄弟链接 + 追溯段；对 GitLab mock（client 有 `_get_project`）断言 `mergerequests.get(int(mr_id))` + `mr.save()` 被调；逐 PR 回写异常 / 缺凭证 → 该 PR 标 False、不抛、其它 PR 不受影响。
  </behavior>
  <action>
    新建 `server/workflows/services/pr_cross_reference.py`，按 `<artifacts_this_phase_produces>` 实现三个函数：
    - `generate_cross_reference_section`：直接镜像 `pr.py:299-310` 逻辑（`other = [r for r in all_successful if r.get("mr_url") != current_pr_url]`；空 → `""`；标题中文「## 关联 PR」，行 `- [{r.get("repository_name","unknown")}]({r.get("mr_url","")})`）。用 `mr_url`/`repository_name` 键（wave 路径 mr_results 键）。
    - `render_traceability_section`：lazy import `from delivery.models import PlanVersion, TechnicalPlan, WorkItem`（防循环）；`pv = await PlanVersion.objects.filter(id=plan_version_id).afirst()`，None→`""`；`tp = await TechnicalPlan.objects.filter(id=pv.plan_id).afirst()`，None→`""`；渲染「## 关联方案 / 工作项」段含 `- 技术方案: `{tp.id}` (v{pv.version})`；`if tp.work_item_id:` 取 `wi = await WorkItem.objects.filter(id=tp.work_item_id).afirst()`，非 None 追加 `- 工作项: {wi.work_item_type}/{wi.work_item_id} {wi.title}`（若 `wi.prd_url` 非空则附链接，WorkItem 无通用 url 字段——不构造臆造 URL，per Open Q1 / A2）；整函数 try/except 包裹，异常 `logger.warning("pr_traceability_render_failed", error=str(exc))` + 返回 `""`（`# noqa: BLE001`）。
    - `add_cross_references`：先 `traceability = await render_traceability_section(plan_version_id)`（fail-soft 已内含）；逐 `mr in successful_mrs`：取 `repo_id = mr.get("repository_id")`，`repository = await Repository.objects.filter(id=repo_id).afirst()`（helper 自取仓对象，入口无关，便于复用）；`section = generate_cross_reference_section(mr.get("mr_url",""), successful_mrs)`；`new_body = (mr.get("description") or "") + section + traceability`；`token = await aresolve_git_token(repository)`，None → 标 False continue；`client = get_git_platform_client(repository, token)`；按 `hasattr(client, "_get_repo")` / `hasattr(client, "_get_project")` 经 `asyncio.to_thread` 回写（镜像 `pr.py:373-390`，`int(mr.get("mr_id"))`）；逐 PR `try/except Exception` → `logger.warning("coding_cross_reference_failed", ...)` + 标 False（`# noqa: BLE001`）；返回 `{mr_url: bool}`。
    - 模块 docstring 显式标注「同源 `CreatePRNode._generate_cross_reference_section`/`_add_cross_references`（`workflows/nodes/git/pr.py`），wave 收尾路径专用；CreatePRNode 后续统一复用本 helper 留 backlog（D-09 备选）」。
    - 日志仅记 `mr_url`/`repository_name`/`has_token` 布尔——token/凭证绝不入日志或 PR 描述（安全红线）。
    在 `server/workflows/services/__init__.py` 追加 `from .pr_cross_reference import (...)` 并补 `__all__`。
    新建 `server/tests/workflows/test_pr_cross_reference.py`，按 `<behavior>` 写纯函数 + 追溯（`django_db(transaction=True)` 真实 PlanVersion/TechnicalPlan/WorkItem 行 + 链断用例）+ 回写（mock `_get_repo`/`_get_project` client + `monkeypatch` `aresolve_git_token`/`get_git_platform_client`/`Repository.objects`）。
    禁用 watch 模式；ruff line 100；async ORM 严禁裸访问同步 lazy-FK（用 `*_id` 标量 + `afirst`）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/workflows/test_pr_cross_reference.py -q 2>&1 | tail -25</automated>
  </verify>
  <done>helper 三函数实现 + barrel 导出；`test_pr_cross_reference.py` 纯函数（多 PR/单 PR）、追溯（链全/链断不抛）、回写（GitHub edit / GitLab save / 缺凭证 / 单 PR 异常隔离）用例全绿。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 接线 _finalize_and_notify（≥2 守门 + fail-soft）+ 集成测试</name>
  <files>server/workflows/nodes/ai/coding.py, server/tests/workflows/test_pr_cross_reference.py</files>
  <read_first>
    - server/workflows/nodes/ai/coding.py（行 1070-1162：`_finalize_and_notify` 全段——行 1084-1101 MR 创建循环 + `mr_results` 收集；行 1156-1162 `_build_output`；plan_data 在签名内可取 `plan_version_id`）
    - server/workflows/nodes/ai/coding.py（行 1658-1743：`_create_mr_for_repo` 返回 dict——本任务须在成功分支补返回 `description`（回写需原 body）；行 1699-1704 现 `body`）
    - server/workflows/services/pr_cross_reference.py（Task 1 产物：`add_cross_references` 签名/键约定）
    - server/tests/test_coding_wave.py（`_settle_session` / wave 收尾 harness：构造 succeeded、模拟回调 resume；集成测试范式）
  </read_first>
  <behavior>
    - `_create_mr_for_repo` 成功分支返回 dict 追加 `"description": body`（供回写取原 body）；缺凭证/失败分支不必含（回写仅对成功名单）。
    - `_finalize_and_notify` 在 MR 创建循环后：`successful_mrs = [r for r in mr_results if r.get("mr_url") and not r.get("error")]`；`if len(successful_mrs) >= 2:`（D-05 守门）→ `try: await add_cross_references(successful_mrs, plan_version_id=(plan_data or {}).get("plan_version_id")) except Exception: log.warning(...)`（整段 fail-soft，`# noqa: BLE001`）。
    - 单仓 / 成功仓 < 2 → 不调 `add_cross_references`（不触 `_get_repo`/`_get_project`）。
    - 回写抛错 → 收尾仍返回 `completed`、`mr_results` 仍在 output（D-15）。
  </behavior>
  <action>
    改 `_create_mr_for_repo`（`coding.py`）成功分支返回 dict：追加键 `"description": body`（其它键不动）。
    改 `_finalize_and_notify`（`coding.py:1070`）：在 MR 创建循环（行 1101 之后、INGEST-02 持久化段之前或之后均可——置于 `mr_results` 收集完成后、`_send_result_notification` 之前）新增 cross-ref 段：
    - lazy import `from workflows.services.pr_cross_reference import add_cross_references`（防循环）。
    - 计算 `successful_mrs`（含 `mr_url` 且无 `error`），`if len(successful_mrs) >= 2:` 守门（D-05）。
    - 整段 `try/except Exception as exc:` → `log.warning("coding_cross_reference_failed", error=str(exc))`（`# noqa: BLE001`，绝不上抛，Pitfall 1 / T-46-04）。
    - `plan_version_id` 取自 `(plan_data or {}).get("plan_version_id")`（A1：wave finalize 路径 plan_data 稳定携带）。
    不改 MR 创建本体、不改 `_build_output`/通知逻辑、不新增第二处 MR 创建（D-15）。
    在 `test_pr_cross_reference.py` 追加 `_finalize_and_notify` 集成用例：
    - test_finalize_two_repos_triggers_cross_ref：mock 2 仓成功 → 断言回写被调（mock `add_cross_references` 捕获入参 / 或 mock client `_get_repo` 被调）、收尾 `completed`。
    - test_finalize_single_repo_no_cross_ref：1 仓成功 → 断言 `add_cross_references` 未被调、收尾 `completed`、输出结构与 Phase 45 一致（零回归 D-14）。
    - test_finalize_cross_ref_failure_still_completed：`add_cross_references` 抛错（monkeypatch）→ 收尾仍 `completed`、`mr_results` 仍在 output（D-15 fail-soft）。
    禁用 watch 模式；ruff line 100。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/workflows/test_pr_cross_reference.py tests/test_coding_wave.py tests/test_batch_pr.py -q 2>&1 | tail -25</automated>
  </verify>
  <done>`_finalize_and_notify` 在 ≥2 成功仓时调 `add_cross_references`（整段 fail-soft）；单仓不回写；回写抛错收尾仍 completed；`test_pr_cross_reference.py` 全绿 + `test_coding_wave.py`/`test_batch_pr.py` 零回归全绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workflow 收尾 → 外部 git 平台 API（写） | cross-ref 回写携带 token；PR body 含方案/工作项标题（DB 来源，纯展示） |
| DB 读链 → PR body | `PlanVersion/TechnicalPlan/WorkItem` 标题渲染进 markdown（受 DB 约束） |
| 容器回调 → `_finalize_and_notify` | cross-ref 在容器回调链路执行，异常上抛会回灌 5xx |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-04 | Denial of Service | cross-ref 回写异常回灌回调 5xx → runner 重试风暴 | mitigate | `_finalize_and_notify` 整个 PR-02 段 try/except → `logger.warning` 降级，绝不上抛（对齐 `_resume_wave` `noqa: BLE001`，Pitfall 1） |
| T-46-05 | Information Disclosure | token 入 PR 描述 / 日志 | mitigate | 日志仅记 `mr_url`/`repository_name`/`has_token` 布尔；PR body 只含方案链接 + 兄弟 PR 链接，绝不含凭证（既有约定，V7） |
| T-46-06 | Tampering（重复副作用） | 重复 finalize 重复创建/回写 | accept | 沿用既有 wave gating（`aadvance_coding_waves` 幂等 + RepoCodingTask 状态机），本 phase 不新增第二处创建（D-15）；回写幂等性低风险（重复回写同 body 无害） |
| T-46-07 | Spoofing | 跨仓 host 凭证错配 | accept | 沿用 `aresolve_git_token` host 归一化解析（Phase 26 T-26-03 已防），本 plan 不改 |
| T-46-08 | Tampering（展示） | 飞书工作项标题含恶意 markdown 破坏 PR 描述 | accept | 低风险纯展示；本 plan 不引入额外转义（Discretion，非硬项） |
</threat_model>

<verification>
- `cd server && uv run pytest tests/workflows/test_pr_cross_reference.py tests/test_coding_wave.py tests/test_batch_pr.py -q` 全绿。
- Phase gate（验收前）：`cd server && uv run pytest -q` 全绿（含既有 ruff line 100 / mypy）。
- 关键断言：≥2 成功仓回写各兄弟仓链接 + 追溯段；单仓不回写；回写抛错收尾仍 completed；链断省略追溯段不抛。
</verification>

<success_criteria>
- 成功仓 ≥2 时每个 PR 描述含兄弟仓链接（排除自身，D-06）+ 方案/工作项追溯段（D-07）。
- 单仓 / <2 仓不做 cross-ref 回写（D-05）。
- cross-ref / 追溯 fail-soft：失败仅 warning，收尾仍 completed、PR 仍在 output（D-10/D-15）。
- 追溯链断 → 省略段不抛（D-13）。
- 复用既有 git client + `aresolve_git_token` + `CreatePRNode` cross-ref 模式，不另造第二套（硬约束）；`CreatePRNode` 零回归。
</success_criteria>

<output>
Create `.planning/phases/46-pr-pr/46-02-SUMMARY.md` when done
</output>
