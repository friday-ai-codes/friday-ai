---
phase: 45-wave
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/services/plan_orchestration/artifact_extraction.py
  - server/services/plan_orchestration/__init__.py
  - server/services/plan_orchestration/wave_progression.py
  - server/delivery/services/repo_coding_task_service.py
  - server/tests/services/plan_orchestration/test_artifact_extraction.py
  - server/tests/delivery/test_repo_coding_task_service.py
  - server/tests/delivery/test_repo_coding_task_inv6_guard.py
autonomous: true
requirements: [ARTIFACT-01]
must_haves:
  truths:
    - "上游仓 RepoCodingTask 进入 done 后其 produced_artifacts 被写入结构化产物（branch/commit/mr_url/modified_files/api_contracts/openapi/diff_summary）"
    - "无 TaskResult 时 produced_artifacts 落 {available: false} 占位且不抛错"
    - "produced_artifacts 写库只经 RepoCodingTaskService.record_produced_artifacts（INV-6）"
    - "提取/写库任一环失败仅 logger.warning 降级，wave 推进与回调主流程绝不失败（fail-soft）"
  artifacts:
    - path: "server/services/plan_orchestration/artifact_extraction.py"
      provides: "build_produced_artifacts + classify_modified_files 纯函数（DB-free）"
      exports: ["build_produced_artifacts", "classify_modified_files"]
    - path: "server/delivery/services/repo_coding_task_service.py"
      provides: "record_produced_artifacts 单一写入入口（幂等覆盖写）"
      contains: "def record_produced_artifacts"
    - path: "server/services/plan_orchestration/wave_progression.py"
      provides: "_backfill_running_terminal 在 mark_done 后追加 fail-soft 提取段"
      contains: "build_produced_artifacts"
  key_links:
    - from: "server/services/plan_orchestration/wave_progression.py"
      to: "build_produced_artifacts"
      via: "mark_done 后提取调用"
      pattern: "build_produced_artifacts"
    - from: "server/services/plan_orchestration/wave_progression.py"
      to: "RepoCodingTaskService.record_produced_artifacts"
      via: "service 单一写入"
      pattern: "record_produced_artifacts"
---

<objective>
ARTIFACT-01 — 上游 wave 产物提取落库。在 Phase 44 确立的 running→done 唯一回填收口（`wave_progression._backfill_running_terminal` 的 `mark_done` 之后）追加 fail-soft 提取段：取 `SubAgentSession.task_result`（git 产物字段），经纯函数 `build_produced_artifacts` 做轻量路径启发式归类（API 契约 / OpenAPI / diff 计数摘要），经单一写入入口 `RepoCodingTaskService.record_produced_artifacts` 落 `RepoCodingTask.produced_artifacts`。

Purpose: 兑现跨仓上下文传递的「提取」半环——上游仓编码完成后把可消费的契约/变更产物结构化落库，供 Plan 02 注入下游 wave。
Output: 新模块 `artifact_extraction.py`（纯函数）+ service 方法 `record_produced_artifacts` + `_backfill_running_terminal` 提取钩子 + 三组单测（纯函数归类 / service 幂等覆盖写 / INV-6 字段级守护扩充）。
</objective>

<execution_context>
@.planning/phases/45-wave/45-RESEARCH.md
@.planning/phases/45-wave/45-PATTERNS.md
@.planning/phases/45-wave/45-VALIDATION.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/45-wave/45-CONTEXT.md
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 新建 artifact_extraction.py 纯函数 + 路径启发式归类 + DB-free 单测</name>
  <read_first>
    - server/services/plan_orchestration/wave_layering.py（同包纯函数蓝本：`from __future__`、模块 docstring 声明「纯函数无 IO/无 ORM」、`__all__` 显式导出、半可信输入逐字段 `.get`/`or []` 防御、绝不抛）
    - server/subagent/models.py（TaskResult git 字段权威：branch_name/commit_sha/pr_url/modified_files/raw_output；line 253 起 + OneToOne session.task_result）
    - server/services/plan_orchestration/__init__.py（barrel 导入块 line 52-59 + `__all__` 追加 line 91-94，镜像范式）
    - server/tests/services/plan_orchestration/test_wave_layering.py（纯函数测试蓝本：无 django_db、无 IO、小 factory helper、从 barrel import）
    - 45-RESEARCH.md「提取纯函数 + 路径启发式归类」Code Example（line 350-405）
  </read_first>
  <behavior>
    - classify_modified_files(["api/openapi.yaml","proto/svc.proto","src/x.py"]) → openapi 桶含 openapi.yaml、api_contracts 桶含 .proto，src/x.py 不进任何契约桶
    - build_produced_artifacts(task_result=<git TaskResult 含 openapi/proto 路径>) → dict 含 available=True + branch/commit_sha/mr_url/modified_files + 正确归类的 api_contracts/openapi + diff_summary.files_changed==len(modified_files)
    - build_produced_artifacts(task_result=None) → {"available": False} 占位（+ repository_id/repository_name/extracted_at），不抛
    - build_produced_artifacts(task_result=<空 modified_files>) → 结构合法、各契约桶为空、diff_summary.files_changed==0
  </behavior>
  <action>
    新建 `server/services/plan_orchestration/artifact_extraction.py`，镜像 `wave_layering.py` 纯函数风格（`from __future__ import annotations`、中文模块 docstring 声明「纯函数 无 IO/无 ORM/DB-free 可单测」、`__all__ = ["build_produced_artifacts", "classify_modified_files"]`）。实现 `classify_modified_files(modified_files: list[str]) -> tuple[list[str], list[str]]`：纯字符串小写匹配，路径含 `openapi`/`swagger` 模式或以 `openapi.json|openapi.yaml|openapi.yml|swagger.json` 结尾 → 归入 openapi 桶；否则路径含 `/api/`/`schema`/`.proto`/`.graphql`/`.graphqls`/`contract` → 归入 api_contracts 桶；其余忽略。`modified_files` 为 None 时按空列表处理。实现 `build_produced_artifacts(*, repository_id: str, repository_name: str, task_result) -> dict`：返回含 `repository_id`/`repository_name`/`extracted_at`(datetime.now(UTC).isoformat()) 的 base；`task_result is None` → base 加 `{"available": False}`；否则读 `task_result.modified_files`（已物化标量，绝不接 lazy ORM 对象）调 classify，返回 base + `available`=True/`branch`/`commit_sha`/`mr_url`(取 pr_url)/`modified_files`/`api_contracts`/`openapi`/`diff_summary={"files_changed": n}`。绝不落 token/凭证/`raw_output` 正文（仅 path/url/计数）。在 `__init__.py` barrel 追加 import 与 `__all__` 两个新函数（对齐既有 build_repo_waves 导出范式）。新建 `server/tests/services/plan_orchestration/test_artifact_extraction.py`，从 barrel import，用未保存的内存 `TaskResult(result_type="git", modified_files=[...], pr_url=..., branch_name=...)` 实例（不触 DB）覆盖 behavior 四场景。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/plan_orchestration/test_artifact_extraction.py -x</automated>
  </verify>
  <acceptance_criteria>
    - source：`server/services/plan_orchestration/artifact_extraction.py` 存在，含 `def build_produced_artifacts` + `def classify_modified_files` + `__all__`，无任何 ORM/IO 调用（DB-free）
    - source：`__init__.py` 导出 `build_produced_artifacts`、`classify_modified_files`（`from services.plan_orchestration import build_produced_artifacts` 可用）
    - behavior：git TaskResult 含 openapi/proto/schema 路径 → 正确归类 api_contracts/openapi；task_result=None → `{"available": False}` 不抛；空 modified_files → 各桶空 + files_changed==0
    - test-command：`uv run pytest tests/services/plan_orchestration/test_artifact_extraction.py -x` 全绿（≥3 用例）
    - security：断言产物 dict 不含 raw_output 正文 / 不含 token 字段（仅 path/url/计数）
  </acceptance_criteria>
  <done>artifact_extraction.py 纯函数 + barrel 导出 + DB-free 单测全绿，归类/占位/空文件三场景覆盖且产物无敏感值。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RepoCodingTaskService.record_produced_artifacts 单一写入入口 + INV-6 字段级守护扩充</name>
  <read_first>
    - server/delivery/services/repo_coding_task_service.py（镜像蓝本：`mark_done`/`_mark_done_sync` line 106-115「async public + @sync_to_async private + filter().update()」三段范式；`timezone` 已 import line 25、structlog logger line 29）
    - server/tests/delivery/test_repo_coding_task_service.py（`test_mark_done_idempotent` line 80-97 流程 + `_make_repo`/`_make_plan_version` helper line 28-40）
    - server/tests/delivery/test_repo_coding_task_inv6_guard.py（现守护正则 `_RE_ORM_WRITE`/`_RE_INSTANTIATE`/`_RE_INSTANCE_SAVE` line 31-41 + `_ALLOWED_WRITER` line 29 + `_is_scanned` line 53-62 扫描框架）
    - server/delivery/models/repo_coding_task.py（`produced_artifacts JSONField(default=dict)` line 80；模型层零业务方法）
    - 45-RESEARCH.md Pattern 3（line 243-254）+ Pitfall 4（line 321-326，不加 status guard）+ Pitfall 6（line 334-339，字段级守护盲区）
  </read_first>
  <behavior>
    - record_produced_artifacts(done_task, {"available": True, ...}) → done 状态 task（已非 RUNNING）也能写入；aget 重读 produced_artifacts == 写入值
    - 重复 record_produced_artifacts 同产物 → 覆盖式 no-op 语义，再 aget 断言不变（幂等）
    - INV-6 守护：源码出现 `xxx.produced_artifacts =` 字段赋值时，仅允许在 service writer 文件 + delivery/models/ 内，其它文件出现则守护测试 fail
  </behavior>
  <action>
    在 `repo_coding_task_service.py` 新增 `async def record_produced_artifacts(self, task, artifacts: dict) -> None`（中文 docstring：produced_artifacts 写库唯一入口，INV-6，幂等覆盖写）委派 `@sync_to_async def _record_produced_artifacts_sync(self, task, artifacts) -> int`，实现用 `RepoCodingTask.objects.filter(id=task.id).update(produced_artifacts=artifacts, updated_at=timezone.now())`。**不加 status guard**（per Pitfall 4：提取发生在 mark_done 之后 task 已 done，加 `status=RUNNING` 会影响 0 行写不进）；用 `.objects.filter().update()` 而非 `task.produced_artifacts=...; task.save()`（既被现有 INV-6 grep 守护对允许 writer 文件的 `.objects.update` 正向覆盖，又避开字段级旁路写盲区）。扩充 `test_repo_coding_task_service.py` 追加 `test_record_produced_artifacts`，镜像 `test_mark_done_idempotent` 流程（create→mark_running→mark_done→record→aget 断言→重复 record→aget 断言不变），复用既有 helper。扩充 `test_repo_coding_task_inv6_guard.py`：新增正则 `\.produced_artifacts\s*=`（字段赋值，排除 `==` 比较），断言命中只允许出现在 `_ALLOWED_WRITER`(service) + `delivery/models/`（模型字段定义）中，否则 fail（D-14），沿用 `_iter_py_files`/`_is_scanned` 框架。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/delivery/test_repo_coding_task_service.py tests/delivery/test_repo_coding_task_inv6_guard.py -x</automated>
  </verify>
  <acceptance_criteria>
    - source：`repo_coding_task_service.py` 含 `def record_produced_artifacts` + `_record_produced_artifacts_sync`，用 `RepoCodingTask.objects.filter(id=task.id).update(produced_artifacts=...)`，**无** `status=` guard
    - behavior：done 状态 task 写入成功（aget 重读相等）；重复写同产物幂等（值不变）
    - test-command：`uv run pytest tests/delivery/test_repo_coding_task_service.py::test_record_produced_artifacts -x` 全绿
    - security/INV-6：`test_repo_coding_task_inv6_guard.py` 新增 `\.produced_artifacts\s*=` 字段级守护断言，旁路字段写（service/models 之外）触发 fail；既有守护用例不回归
  </acceptance_criteria>
  <done>record_produced_artifacts 幂等覆盖写（无 status guard）落地 + service 单测 + INV-6 字段级守护扩充全绿。</done>
</task>

<task type="auto">
  <name>Task 3: wave_progression._backfill_running_terminal 提取钩子（mark_done 后 fail-soft）</name>
  <read_first>
    - server/services/plan_orchestration/wave_progression.py（`_backfill_running_terminal` line 126-154：`sess` 已在 line 139 经 `*_id` 标量 + afirst 取出；`mark_done` at line 144；模块 docstring line 20-21 async ORM 安全约束；line 91 注释「本函数不吞异常，由调用方包 try/except swallow」→ 提取段须独立 try/except 不冒泡）
    - server/subagent/models.py（TaskResult OneToOne session.task_result，`TaskResult.objects.filter(session=sess).afirst()`）
    - server/repositories/models.py（Repository，`Repository.objects.filter(id=task.repository_id).afirst()` 取 repo_name）
    - 45-RESEARCH.md Pattern 1（line 194-230）+ Pitfall 1（async lazy-FK）+ Pitfall 3（提取异常不让回调 5xx）
  </read_first>
  <action>
    在 `_backfill_running_terminal` 的 `await service.mark_done(task)`（line 144，纯状态转移保持不变）**之后**追加独立 `try/except` 提取段（仅 done 分支，**不**碰 mark_failed 分支 per A2）。段内：复用循环已取出的 `sess`，用 `tr = await TaskResult.objects.filter(session=sess).afirst()`、`repo = await Repository.objects.filter(id=task.repository_id).afirst()`、`repo_name = repo.name if repo else str(task.repository_id)`（async ORM 安全：`*_id` 标量 + afirst，绝不裸访问 lazy-FK）；调纯函数 `build_produced_artifacts(repository_id=str(task.repository_id), repository_name=repo_name, task_result=tr)`（tr=None 时纯函数自落 `{"available": False}` 占位，非异常路径）；调 `await service.record_produced_artifacts(task, artifacts)`。整段 `except Exception as exc:  # noqa: BLE001` → `logger.warning("coding_artifact_extract_failed", task_id=str(task.id), error=str(exc))`，绝不向 `_backfill_running_terminal` 外冒泡（保 mark_done 成功后即使提取失败 task 仍正确 done、wave 推进不受影响）。日志仅记 task_id/error 字符串，绝不记产物正文/token。提取的 build/record 调用经局部 import（对齐既有局部 import 范式，避免模块级循环依赖）。**绝不**在本文件直接写 `task.produced_artifacts`（必经 service，D-14 字段级守护会拦）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/plan_orchestration tests/delivery -x</automated>
  </verify>
  <acceptance_criteria>
    - source：`wave_progression.py` 在 `mark_done` 后出现独立 try/except 提取段，调用 `build_produced_artifacts` + `service.record_produced_artifacts`，`except` 仅 `logger.warning`（不 raise）
    - source：提取段用 `TaskResult.objects.filter(session=sess).afirst()` + `Repository.objects.filter(id=task.repository_id).afirst()`（无裸 lazy-FK 访问）；本文件无 `task.produced_artifacts =` 直接字段写
    - behavior：done 回填后 produced_artifacts 被写入；提取异常被 swallow，mark_done 与三步顺序（回填→阻断→决策）不变
    - test-command：`uv run pytest tests/services/plan_orchestration tests/delivery -x` 全绿（Phase 44 既有 wave 测试零回归）
  </acceptance_criteria>
  <done>提取钩子挂在唯一 done 收口、fail-soft 不冒泡、async ORM 安全，Phase 44 既有测试零回归。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runner 容器 → Server | `SubAgentSession.task_result`（branch/commit/pr_url/modified_files/raw_output）是半可信 runner 产出，跨此边界进入提取逻辑 |
| 提取产物 → produced_artifacts 落库 | 半可信容器产物结构化后写入 DB，后续将注入下游 prompt（Plan 02） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-01 | Info Disclosure | build_produced_artifacts 提取 | mitigate | 只取白名单字段（branch/commit/pr_url/path/计数），**绝不**整体落 `raw_output`；产物绝不含 token/凭证；日志仅记 task_id/error 字符串，不记产物正文（避免泄漏契约/路径敏感信息） |
| T-45-02 | DoS | 半可信 modified_files 超长/超多路径 | mitigate | classify 纯字符串匹配无递归；`diff_summary` 仅计数；不在提取段对 modified_files 做无界展开（注入端截断由 Plan 02 处理） |
| T-45-03 | Tampering / Elevation | runner 经回调篡改产物归属 | mitigate | 提取经服务端权威链 `task.subagent_session_id` → `SubAgentSession` → `TaskResult` OneToOne，按服务端 `RepoCodingTask` 行经 `record_produced_artifacts` 写库，不信 runner 可改字段（对齐 T-44-TAMPER） |
| T-45-04 | DoS | 提取/写库异常 → 回调 5xx 重试风暴 | mitigate | 提取段独立 try/except `logger.warning` 不冒泡；无 TaskResult → `{"available": false}` 占位非异常路径 |
| T-45-SC | Tampering | npm/pip/cargo installs | accept | 本 plan 不安装任何外部包（纯内部 Django service + 纯函数），无 supply-chain 面 |
</threat_model>

<verification>
- `uv run pytest tests/services/plan_orchestration/test_artifact_extraction.py -x` 全绿（归类/占位/空文件）
- `uv run pytest tests/delivery/test_repo_coding_task_service.py tests/delivery/test_repo_coding_task_inv6_guard.py -x` 全绿（幂等覆盖写 + 字段级 INV-6 守护）
- `uv run pytest tests/services/plan_orchestration tests/delivery -x` 全绿（提取钩子接线 + Phase 44 零回归）
- `cd server && uv run ruff format --check .` 与 `ruff check` 对改动文件无新增违规（line 100）
</verification>

<success_criteria>
- ARTIFACT-01：上游仓 done 回填后 `produced_artifacts` 经唯一写入入口落结构化产物（含 api_contracts/openapi/diff_summary 启发式归类）
- 无 TaskResult fail-soft 落占位、提取异常不阻塞 wave 推进
- INV-6 字段级守护拦截 produced_artifacts 旁路写
- 全部新增/扩充测试绿；Phase 44 既有 plan_orchestration/delivery 测试零回归
</success_criteria>

## Artifacts this phase produces

本 plan 新增符号（ARTIFACT-01）：

- **新文件** `server/services/plan_orchestration/artifact_extraction.py`
- **函数** `build_produced_artifacts(*, repository_id, repository_name, task_result) -> dict`（纯函数，DB-free）
- **函数** `classify_modified_files(modified_files) -> tuple[list[str], list[str]]`（路径启发式归类纯函数）
- **方法** `RepoCodingTaskService.record_produced_artifacts(task, artifacts)`（+ `_record_produced_artifacts_sync`，INV-6 单一写入入口）
- **produced_artifacts dict 键**：`repository_id` / `repository_name` / `extracted_at` / `available` / `branch` / `commit_sha` / `mr_url` / `modified_files` / `api_contracts` / `openapi` / `diff_summary`(`{"files_changed": n}`)
- **接线** `wave_progression._backfill_running_terminal` 提取钩子（mark_done 后 fail-soft）
- **barrel 导出** `services.plan_orchestration` 追加 `build_produced_artifacts` / `classify_modified_files`

<output>
完成后创建 `.planning/phases/45-wave/45-01-SUMMARY.md`
</output>
