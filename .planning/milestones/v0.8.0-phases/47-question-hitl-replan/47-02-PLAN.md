---
phase: 47-question-hitl-replan
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - server/subagent/question_handler.py
  - server/tests/test_coding_question_hitl.py
autonomous: true
requirements: [HITL-01]
must_haves:
  truths:
    - "wave 编码（带 node_execution_id、无 main_session）任务的 question 回调能正确创建 InteractionLog 并把提问卡片路由到 node_execution 的 chat_id（复用 _resolve_notification_chat_id，HITL-01b）"
    - "缺 chat_id（无 main_session 且 node 无 chat_id）时 fail-soft：不发卡、不抛、InteractionLog 仍创建供 orchestrator 轮询（D-06/D-09）"
    - "遇阻续跑只走 Phase 43 _schedule_workflow_resume + Phase 44 aadvance_coding_waves：等待期容器 RUNNING → aadvance 返回 waiting（不阻断下游/不死锁）；回答后 completed → 既有 resume 推进（D-08，无新 resume 通路）"
    - "no-replan 守护：question/answer/resume 链路绝不触发 research/replan 编排（HITL-01c / D-10）"
    - "零回归：既有 chat 编码提问循环（main_session chat_id 路径）行为逐字不变"
  artifacts:
    - path: "server/subagent/question_handler.py"
      provides: "send_question_card_enhanced 增 node_execution chat_id fallback（复用 _resolve_notification_chat_id）"
      contains: "_resolve_notification_chat_id"
    - path: "server/tests/test_coding_question_hitl.py"
      provides: "wave question 路由 + e2e（waiting→answer→resume）+ no-replan guard 测试"
      min_lines: 120
  key_links:
    - from: "server/subagent/question_handler.py:send_question_card_enhanced"
      to: "server/subagent/api/callbacks.py:_resolve_notification_chat_id"
      via: "node_execution chat_id fallback（lazy import 防环）"
      pattern: "_resolve_notification_chat_id"
    - from: "test e2e"
      to: "server/services/plan_orchestration/wave_progression.py:aadvance_coding_waves"
      via: "RUNNING 在途 → waiting；completed → 推进"
      pattern: "aadvance_coding_waves"
---

<objective>
HITL-01b/c：server 侧补齐 wave 编码（node_execution）任务的 question 卡片**路由**（唯一生产改动），并以测试坐实「遇阻 question → 等待期容器 RUNNING → Phase 44 `aadvance_coding_waves` 视为 `waiting`（不阻断下游/不死锁）→ 回答 → 容器完成 → Phase 43 `_schedule_workflow_resume` 续跑」，以及 **no-replan 非目标守护**。

Purpose: question 接收 / 回答回灌 / resume 全部既有可复用；唯一缺口是 `send_question_card_enhanced` 只认 `main_session.metadata.chat_id`，wave 编码任务（`node_execution_id`、常无 main_session）取不到 chat_id → 卡片不送达。复用既有 `_resolve_notification_chat_id`（已支持双路由）补 fallback。其余以测试验证既有 resume 通路正确承接 HITL，并守护「只抛人、不 replan」。
Output: `send_question_card_enhanced` 一处 chat_id fallback（复用 `_resolve_notification_chat_id`）+ `server/tests/test_coding_question_hitl.py`（路由 + e2e waiting→answer→resume + no-replan guard + 零回归）。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-clean/.codex/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-clean/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/47-question-hitl-replan/47-CONTEXT.md
@.planning/phases/47-question-hitl-replan/47-RESEARCH.md
@.planning/phases/47-question-hitl-replan/47-VALIDATION.md
</context>

<artifacts_this_phase_produces>
本 plan 不新增独立模块或导出符号——仅在既有 `send_question_card_enhanced` 内增加 node_execution chat_id fallback（无新 public symbol），并新增测试文件 `server/tests/test_coding_question_hitl.py`。
（task 侧符号由 Plan 01 产出。）
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: send_question_card_enhanced 增 node_execution chat_id fallback</name>
  <files>server/subagent/question_handler.py</files>
  <read_first>
    - server/subagent/question_handler.py（行 36-101：`send_question_card_enhanced` 现仅 `session.main_session.metadata.get("chat_id")`，无 chat_id → warning return None）
    - server/subagent/api/callbacks.py（行 437-461：`_resolve_notification_chat_id`——已支持 main_session.metadata.chat_id + node_execution.node.config.chat_id 双路由，async，缺失返回 ""）
    - server/subagent/api/callbacks.py（行 100-145、198-288：`_schedule_agent_session_resume` / `_schedule_workflow_resume` 确认 wave 编码任务 node_execution 路径——理解卡片路由对象）
  </read_first>
  <behavior>
    - `send_question_card_enhanced` 解析 chat_id 时：优先既有 `main_session.metadata.chat_id`；为空则 fallback 调 `_resolve_notification_chat_id(session)`（复用，覆盖 node_execution.node.config.chat_id）。
    - 仍无 chat_id → 保持既有 fail-soft：warning + return None（不发卡、不抛）。`InteractionLog` 由上游 `_handle_question` 已创建（不受影响）。
    - 零回归：既有 main_session chat_id 路径行为逐字不变（fallback 仅在 main_session 路径取空时触发）。
  </behavior>
  <action>
    在 `send_question_card_enhanced` 内，把现有「从 main_session.metadata 取 chat_id」逻辑保留为第一优先；当 `chat_id` 为空时，lazy import 并 `await` 调用 `from subagent.api.callbacks import _resolve_notification_chat_id` → `chat_id = await _resolve_notification_chat_id(session)`（lazy import 防 callbacks↔question_handler 循环——callbacks 已在 `_handle_question` 内 lazy import question_handler）。
    保留 `if not chat_id: log.warning("question_no_chat_id"); return None` 的 fail-soft 出口。
    中文注释说明：wave 编码任务（node_execution、无 main_session）经 _resolve_notification_chat_id 取 node 级 chat_id（HITL-01b 路由补齐）。
    ruff line 100；仅此一处改动，不动发卡主干 / 不动 `handle_container_answer_enhanced`。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/test_coding_question_hitl.py -k routing -q 2>&1 | tail -20</automated>
  </verify>
  <done>main_session chat_id 路径零回归；node_execution-only 任务经 fallback 取到 node chat_id 并尝试发卡；双路径均空时 fail-soft return None 不抛。</done>
</task>

<task type="auto">
  <name>Task 2: server 侧 HITL 守护测试（路由 + e2e + no-replan + 零回归）</name>
  <files>server/tests/test_coding_question_hitl.py</files>
  <read_first>
    - server/tests/test_question_loop_integration.py（既有 question 回调/回答测试范式：mock_session fixture、InteractionLog、handle_container_answer_enhanced、patch question_handler）
    - server/tests/test_coding_wave.py（行 1-90：wave 测试 fixtures——`_stub_provider_resolution`/`_dispatched`、`pytestmark=[django_db(transaction=True), asyncio]`、RepoCodingTask/SubAgentSession 构造范式；aadvance_coding_waves 调用范式）
    - server/services/plan_orchestration/wave_progression.py（行 35-188：`aadvance_coding_waves` 三态出口 + `_backfill_running_terminal` 仅回填 SubAgentSession 终态、RUNNING 跳过）
    - server/subagent/api/callbacks.py（行 873-944：`_handle_question`；行 437-461：`_resolve_notification_chat_id`）
    - server/subagent/question_handler.py（Task 1 改动点）
    - server/delivery/services/repo_coding_task_service.py（RepoCodingTask 建表/状态 service，构造 wave 任务用）
  </read_first>
  <behavior>
    - **routing**: `test_send_card_routes_via_node_execution`：构造带 `node_execution_id`（node.config 含 chat_id）、无 main_session 的 SubAgentSession → patch `FeishuIMClient`/settings 飞书配置 → 调 `send_question_card_enhanced` → 断言 chat_id 取自 node_execution（经 _resolve_notification_chat_id）、尝试 `send_card`。
    - **routing fail-soft**: `test_send_card_failsoft_no_chat_id`：无 main_session、node 无 chat_id → `send_question_card_enhanced` 返回 None、不抛。
    - **routing 零回归**: `test_send_card_main_session_chat_id_unchanged`：有 main_session.metadata.chat_id → 仍走 main_session 路径（不调 fallback / 行为不变）。
    - **e2e waiting（不 dead-end）**: `test_blocked_wave_task_stays_waiting`：构造一个 plan_version 的两仓 RepoCodingTask（wave0 上游 A、wave1 下游 B `depends_on` A）；A 的 SubAgentSession 置 RUNNING + `last_output.pending_question`（模拟遇阻发问）→ 调 `aadvance_coding_waves(plan_version_id)` → 断言返回 `{"waiting": True}`（A 仍在途，**未**被标 failed、B **未**被阻断/dispatch）。证明遇阻不再 dead-end。
    - **e2e resume**: `test_answer_then_complete_resumes_wave`：承接上一场景——模拟回答后 A 容器完成（A 的 SubAgentSession → COMPLETED，建 TaskResult）→ 再调 `aadvance_coding_waves` → 断言 A 回填 done、返回 `{"dispatch":[B...], "wave":1}`（wave 推进）。证明回答后经既有 wave 推进续跑。
    - **no-replan guard**: `test_hitl_path_does_not_trigger_replan`：spy/patch research/replan 编排入口（如 `services.plan_orchestration` 的 research 聚合 / `amaybe_complete_research` / 任何 `start_*_research`）→ 跑遇阻→等待→回答→推进全流程 → 断言这些 replan/research 入口**零调用**（HITL 只抛人、不重规划）。
    - **零回归**: 既有 `test_question_loop_integration.py` 不改、仍绿（在 verify 跑一遍）。
  </behavior>
  <action>
    新建 `server/tests/test_coding_question_hitl.py`。复用 `test_question_loop_integration.py` 的 mock_session/InteractionLog 范式与 `test_coding_wave.py` 的 RepoCodingTask/SubAgentSession/aadvance fixtures。
    routing 测试：构造无 main_session、带 node_execution 的 SubAgentSession（node.config={"chat_id": "oc_node_chat"}）；patch `subagent.question_handler.FeishuIMClient` + settings `FEISHU_APP_ID/SECRET` 使发卡分支可达，捕获 `send_card` 的 `receive_id` 断言 == node chat_id。
    e2e 测试：用 `RepoCodingTaskService` 或直接 ORM 建 plan_version + A/B 两 RepoCodingTask（B.depends_on=A）；用 SubAgentSession 真实 status 字段驱动 `aadvance_coding_waves`（A RUNNING → waiting；A COMPLETED+TaskResult → dispatch B）。对齐 `test_coding_wave.py` 既有断言风格。
    no-replan guard：用 `unittest.mock.patch` 包住 research/replan 编排函数为 `MagicMock`，跑流程后 `assert mock.call_count == 0`。grep 守护：确认 `wave_progression` / question 链路源码无 `replan`/`start_*_research` 调用（可在测试内 import 源文件文本断言，或纯函数审查注释）。
    `pytestmark = [pytest.mark.django_db(transaction=True), pytest.mark.asyncio]`；禁用 watch；targeted ≤15s。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/test_coding_question_hitl.py tests/test_question_loop_integration.py -q 2>&1 | tail -30</automated>
  </verify>
  <done>新测试全绿：routing（node_execution / fail-soft / main_session 零回归）+ e2e（waiting 不 dead-end / 回答后 dispatch 续跑）+ no-replan guard（research/replan 零调用）；既有 question_loop_integration 零回归。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 容器回调 → server question 接收 | _handle_question 经 CONTAINER_CALLBACK_TOKEN 鉴权（既有）；question payload 来自半可信容器 agent |
| server 发卡 → 飞书 | chat_id 取自服务端权威 main_session.metadata / node.config（非容器可篡改的 last_output） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-01 | Tampering / Elevation | 卡片 chat_id 路由 | mitigate | chat_id 仅取服务端权威字段 `main_session.metadata.chat_id` / `node_execution.node.config.chat_id`（经 _resolve_notification_chat_id），**绝不**取 runner 可经 progress 篡改的 `last_output`——不放大信任面（对齐 cross_repo_relevance / chat_plan_resume 权威字段范式） |
| T-47-05 | DoS / Logic | wave 推进死锁 | mitigate | 等待期容器 RUNNING → aadvance 视为在途返回 waiting（既有 liveness 顺序：回填→阻断→决策不变）；测试坐实不阻断下游、回答后正常推进 |
| T-47-06 | Scope creep | no auto-replan | mitigate | 测试守护 HITL 链路零触发 research/replan 编排（HITL-01c 非目标硬守护） |
</threat_model>

<verification>
- `cd server && uv run pytest tests/test_coding_question_hitl.py tests/test_question_loop_integration.py -q` 全绿。
- 路由：node_execution chat_id fallback 生效；缺 chat_id fail-soft；main_session 路径零回归。
- e2e：遇阻 RUNNING → aadvance waiting（不 dead-end）；回答 completed → aadvance dispatch 续跑。
- no-replan：research/replan 编排零调用。
</verification>

<success_criteria>
- wave 编码 question 卡片经 node_execution chat_id 路由（HITL-01b / D-06）。
- 缺 chat_id fail-soft，InteractionLog 仍在（D-09）。
- 续跑只走 Phase 43/44，无新 resume 通路（D-08）。
- no-replan 守护通过（HITL-01c / D-10）。
- 既有 chat 提问循环零回归。
</success_criteria>

<output>
Create `.planning/phases/47-question-hitl-replan/47-02-SUMMARY.md` when done
</output>
