---
phase: 44-repocodingtask-execution-plan-dag-wave
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/delivery/models/repo_coding_task.py
  - server/delivery/models/__init__.py
  - server/delivery/migrations/0017_repocodingtask.py
  - server/tests/delivery/test_repo_coding_task_models.py
autonomous: true
requirements: [WAVE-01]
must_haves:
  truths:
    - "RepoCodingTask 表落库，含 wave / depends_on M2M self / produced_artifacts / follow_openspec / attempt / error 字段"
    - "状态枚举 4 态 pending→running→done|failed（不含 stale）"
    - "迁移 0017 可干净 makemigrations --check（无未生成迁移）"
  artifacts:
    - path: "server/delivery/models/repo_coding_task.py"
      provides: "RepoCodingTask 模型 + RepoCodingTaskStatus 枚举"
      contains: "class RepoCodingTask"
    - path: "server/delivery/migrations/0017_repocodingtask.py"
      provides: "delivery_repo_coding_task 表 DDL + M2M through + 索引"
      contains: "RepoCodingTask"
    - path: "server/delivery/models/__init__.py"
      provides: "RepoCodingTask / RepoCodingTaskStatus barrel 导出"
      contains: "RepoCodingTask"
  key_links:
    - from: "server/delivery/models/repo_coding_task.py"
      to: "delivery.PlanVersion"
      via: "plan_version ForeignKey"
      pattern: "plan_version.*ForeignKey"
    - from: "server/delivery/models/repo_coding_task.py"
      to: "self (depends_on DAG)"
      via: "ManyToManyField self"
      pattern: "depends_on.*ManyToManyField"
---

<objective>
立 `RepoCodingTask` 操作态模型（WAVE-01 建模半幅）——多仓 wave 编码的操作态脊柱，逐项镜像同 app 姊妹模型 `RepoResearchTask` 的形状，新增 wave 调度 DAG 字段（`wave` / `depends_on` M2M self / `produced_artifacts` / `follow_openspec` 预留位），生成迁移 0017。

Purpose: 为 WAVE-01 拓扑分层（plan 02）、单一写入入口 service（plan 03）、wave 推进（plan 04）、AICodingNode 接线（plan 05）提供持久化底座。wave 进度全持久化于本表行（不依赖内存态），是 callback 驱动多 wave resume 的恢复来源。
Output: `RepoCodingTask` 模型 + `RepoCodingTaskStatus` 枚举 + barrel 导出 + 迁移 0017 + 模型字段/Meta/索引/枚举单测。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-repocodingtask-execution-plan-dag-wave/44-CONTEXT.md
@.planning/phases/44-repocodingtask-execution-plan-dag-wave/44-RESEARCH.md
@.planning/phases/44-repocodingtask-execution-plan-dag-wave/44-PATTERNS.md

@server/delivery/models/research_task.py
@server/delivery/models/__init__.py
@server/delivery/migrations/0013_reporesearchtask_partialplan.py
@server/delivery/migrations/0016_clarification.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 RepoCodingTask 模型 + RepoCodingTaskStatus 枚举 + barrel 导出</name>
  <read_first>
    - server/delivery/models/research_task.py（范式蓝本，行 20-103：枚举 + 模型骨架 + Meta/索引/__str__）
    - server/delivery/models/__init__.py（barrel：行 40-44 research_task 导出块 + `__all__` 行 84-86）
    - server/delivery/models/technical_plan.py（PlanVersion FK 目标，确认 app label `delivery`）
    - .planning/phases/44-repocodingtask-execution-plan-dag-wave/44-PATTERNS.md（Copy-from delta 段，行 105-114）
    - .planning/phases/44-repocodingtask-execution-plan-dag-wave/44-CONTEXT.md（Area 1 模型形状决策，行 24-37）
  </read_first>
  <action>
    新建 `server/delivery/models/repo_coding_task.py`，逐项镜像 `research_task.py`：
    - `RepoCodingTaskStatus(models.TextChoices)` 4 态：`PENDING="pending"`、`RUNNING="running"`、`DONE="done"`、`FAILED="failed"`（中文标签待派发/编码中/已完成/失败）。**不含** `STALE`（编码期无重索引语义）。
    - `RepoCodingTask(models.Model)`：`id` UUIDField(primary_key, default=uuid.uuid4, editable=False)；`plan_version = ForeignKey("delivery.PlanVersion", on_delete=CASCADE, related_name="coding_tasks")`（真实 FK，区别于 PlanSession 软引用）；`repository = ForeignKey("repositories.Repository", on_delete=CASCADE, related_name="+")`；`wave = IntegerField(default=0)`（拓扑层级，service 写入）；`depends_on = ManyToManyField("self", symmetrical=False, blank=True, related_name="dependents")`（有向 DAG 仓级边）；`status = CharField(max_length=16, choices=RepoCodingTaskStatus.choices, default=PENDING)`；`produced_artifacts = JSONField(default=dict, blank=True)`（本 phase 仅立字段，Phase 45 才写内容）；`follow_openspec = BooleanField(default=False)`（SDD 扩展点预留，本 phase 不消费）；`subagent_session = ForeignKey("subagent.SubAgentSession", on_delete=SET_NULL, null=True, blank=True, related_name="+")`；`attempt = IntegerField(default=0)`；`error = JSONField(default=dict, blank=True)`；`created_at = DateTimeField(auto_now_add=True)`；`updated_at = DateTimeField(auto_now=True)`。
    - `Meta`：`db_table="delivery_repo_coding_task"`、中文 `verbose_name="仓库编码子任务"` / `verbose_name_plural` 同、`indexes=[Index(fields=["plan_version","wave","status"]), Index(fields=["repository"])]`。
    - `__str__` 返回 `f"RepoCodingTask({self.id}, w{self.wave}, {self.status})"`。
    - **模型层零业务方法**（仅 `__str__`）——不写任何 create/save/状态变更/校验方法（守 INV-6，plan 03 的 grep 守护会断言）。中文 docstring。
    在 `server/delivery/models/__init__.py` 新增 `from delivery.models.repo_coding_task import (RepoCodingTask, RepoCodingTaskStatus)` 并在 `__all__` 追加这两名（镜像 research_task 导出块格式）。
  </action>
  <verify>
    <automated>cd server && uv run python -c "from delivery.models import RepoCodingTask, RepoCodingTaskStatus; print(RepoCodingTask._meta.db_table, [s.value for s in RepoCodingTaskStatus])"</automated>
  </verify>
  <acceptance_criteria>
    - `cd server && uv run python -c "from delivery.models import RepoCodingTask, RepoCodingTaskStatus"` 无 ImportError。
    - 输出 db_table 为 `delivery_repo_coding_task`，状态值集合恰为 `['pending','running','done','failed']`（无 `stale`）。
    - `cd server && uv run ruff check delivery/models/repo_coding_task.py` 通过（line 100）。
    - `cd server && uv run python -c "from delivery.models import RepoCodingTask; fs={f.name for f in RepoCodingTask._meta.get_fields()}; assert {'plan_version','repository','wave','depends_on','status','produced_artifacts','follow_openspec','subagent_session','attempt','error'} <= fs, fs"` 退出 0。
  </acceptance_criteria>
  <done>RepoCodingTask 模型 + 枚举创建并经 barrel 可导入，字段/db_table/状态枚举符合 DOMAIN §6/§14；模型层零业务方法。</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] makemigrations delivery → 0017_repocodingtask</name>
  <read_first>
    - server/delivery/migrations/0013_reporesearchtask_partialplan.py（多 app 依赖 + CreateModel + M2M + AddIndex 结构蓝本，行 10-14 dependencies）
    - server/delivery/migrations/0016_clarification.py（当前最新 delivery 迁移，确认父依赖）
    - .planning/phases/44-repocodingtask-execution-plan-dag-wave/44-RESEARCH.md（Pitfall 6 迁移依赖项，行 295-298）
  </read_first>
  <action>
    [BLOCKING] 用 `cd server && uv run python manage.py makemigrations delivery` **自动生成**迁移（**不手写**——M2M self through 表 `repo_coding_task_depends_on` 须 Django 自动建）。预期生成 `server/delivery/migrations/0017_repocodingtask.py`。生成后核对：`dependencies` 含 `('delivery','0016_clarification')` + `repositories` 当前最新迁移 + `subagent` 当前最新迁移（FK 目标）；`operations` 含 `CreateModel` RepoCodingTask（plan_version/repository/subagent_session FK + depends_on M2M）+ 两条 `AddIndex`（`[plan_version,wave,status]` / `[repository]`）。若 makemigrations 生成了非 0017 的序号（因 repositories/subagent 端有新迁移），保留 Django 选择的实际序号但确保 delivery 端文件名前缀为 `0017_`。
  </action>
  <verify>
    <automated>cd server && uv run python manage.py makemigrations --check --dry-run delivery</automated>
  </verify>
  <acceptance_criteria>
    - `server/delivery/migrations/0017_repocodingtask.py` 存在。
    - `cd server && uv run python manage.py makemigrations --check --dry-run delivery` 退出 0（无未生成迁移——证明模型与迁移一致）。
    - `cd server && uv run python manage.py migrate delivery` 在测试 DB 应用成功（迁移可正向应用）。
    - 迁移文件 `dependencies` 含 `('delivery','0016_clarification')`。
  </acceptance_criteria>
  <done>迁移 0017 自动生成、依赖项完整、`makemigrations --check` 干净、可正向 migrate。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: RepoCodingTask 模型单测（字段/Meta/索引/枚举）</name>
  <read_first>
    - server/tests/delivery/test_research_inv6_guard.py（delivery 测试目录约定 + 模型断言风格）
    - server/delivery/models/repo_coding_task.py（Task 1 产物）
    - .planning/phases/44-repocodingtask-execution-plan-dag-wave/44-VALIDATION.md（WAVE-01 模型测试行，行 41）
  </read_first>
  <behavior>
    - Test 1: 建一条 RepoCodingTask（关联 PlanVersion + Repository fixture），默认 status=pending、wave=0、attempt=0、produced_artifacts={}、follow_openspec=False、error={}。
    - Test 2: db_table == "delivery_repo_coding_task"；RepoCodingTaskStatus 值集恰为 {pending,running,done,failed}。
    - Test 3: depends_on 是 symmetrical=False 的 self-M2M——A.depends_on.add(B) 后 A.depends_on 含 B 且 B.dependents 含 A，但 B.depends_on 不含 A（有向）。
    - Test 4: Meta.indexes 含 (plan_version,wave,status) 与 (repository) 两组字段。
  </behavior>
  <action>
    新建 `server/tests/delivery/test_repo_coding_task_models.py`（`@pytest.mark.django_db`）。用既有 delivery PlanVersion / repositories Repository 测试构造方式建关联对象（grep `tests/delivery/` 或 `tests/` 现有 PlanVersion/Repository fixture 复用，不自造工厂）。断言上述 4 组行为。M2M 有向性断言用同步测试上下文（非 async）直接 `.add()` / `.all()`。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/delivery/test_repo_coding_task_models.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `cd server && uv run pytest tests/delivery/test_repo_coding_task_models.py -x` 全绿。
    - 测试含 depends_on 有向性断言（B.depends_on 不含 A）。
    - 测试断言状态值集合无 `stale`。
  </acceptance_criteria>
  <done>模型单测全绿，覆盖默认值 / db_table / 枚举 / M2M 有向性 / 索引。</done>
</task>

</tasks>

<artifacts_this_phase_produces>
> 本 plan 贡献的符号（phase 44 全量清单见各 plan 汇总）：
- **类**：`RepoCodingTask`（`server/delivery/models/repo_coding_task.py`）、`RepoCodingTaskStatus`（TextChoices，4 态）
- **模型字段**：`plan_version`(FK PlanVersion)、`repository`(FK Repository)、`wave`(int)、`depends_on`(M2M self)、`status`、`produced_artifacts`(JSON)、`follow_openspec`(bool)、`subagent_session`(FK SET_NULL)、`attempt`(int)、`error`(JSON)、`created_at`、`updated_at`
- **db_table**：`delivery_repo_coding_task`（+ M2M through `repo_coding_task_depends_on`）
- **迁移**：`0017_repocodingtask`
- **新文件**：`server/delivery/models/repo_coding_task.py`、`server/tests/delivery/test_repo_coding_task_models.py`
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 无新外部触面 | 本 plan 纯 ORM 建模 + 迁移，无对外端点、无凭证读取、无用户输入直连执行 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-INV6-MODEL | Tampering | RepoCodingTask 模型层 | mitigate | 模型层零业务方法（仅 `__str__`），不写 create/save/状态变更——为 plan 03 的 INV-6 grep 守护铺底；本 plan 不开任何旁路写入口 |
| T-44-MIG | Tampering | 迁移 0017 | mitigate | 用 `makemigrations` 自动生成（不手写 DDL），`makemigrations --check` 门禁防模型/迁移漂移 |

**Note:** 本 plan 是纯内部操作态建模，无新外部攻击面（对齐 RESEARCH Security Domain：V2/V3/V6 不适用）。
</threat_model>

<verification>
- `cd server && uv run pytest tests/delivery/test_repo_coding_task_models.py -x` 全绿。
- `cd server && uv run python manage.py makemigrations --check --dry-run delivery` 干净。
- `cd server && uv run ruff check delivery/models/repo_coding_task.py` 通过。
</verification>

<success_criteria>
RepoCodingTask 模型 + 枚举 + barrel 导出 + 迁移 0017 落地；模型单测全绿；`makemigrations --check` 干净；模型层零业务方法。
</success_criteria>

<output>
Create `.planning/phases/44-repocodingtask-execution-plan-dag-wave/44-01-SUMMARY.md` when done
</output>
