---
phase: 44-repocodingtask-execution-plan-dag-wave
plan: 03
type: execute
wave: 2
depends_on: ["44-01"]
files_modified:
  - server/delivery/services/repo_coding_task_service.py
  - server/delivery/services/__init__.py
  - server/tests/delivery/test_repo_coding_task_service.py
  - server/tests/delivery/test_repo_coding_task_inv6_guard.py
autonomous: true
requirements: [WAVE-01, WAVE-02]
must_haves:
  truths:
    - "所有 RepoCodingTask 建表/状态推进/wave 写入只经 RepoCodingTaskService（INV-6）"
    - "create_tasks_for_plan 幂等（get_or_create），写 wave + depends_on M2M 边"
    - "mark_done 仅 running→done 幂等（重复调用 no-op，不重复推进）"
    - "mark_blocked 标 failed + error={reason: upstream_failed, upstream:[...]}"
    - "INV-6 grep 守护断言无旁路写 RepoCodingTask"
  artifacts:
    - path: "server/delivery/services/repo_coding_task_service.py"
      provides: "RepoCodingTaskService 单一写入入口"
      contains: "class RepoCodingTaskService"
    - path: "server/tests/delivery/test_repo_coding_task_inv6_guard.py"
      provides: "INV-6 旁路写守护（源码扫描）"
      contains: "_ALLOWED_WRITER"
  key_links:
    - from: "server/delivery/services/repo_coding_task_service.py"
      to: "delivery.models.RepoCodingTask"
      via: "@sync_to_async 同步块内唯一写入"
      pattern: "RepoCodingTask.objects"
---

<objective>
立 `RepoCodingTaskService` 单一写入入口（WAVE-01 service 半幅 + WAVE-02 状态语义）——镜像 `ResearchService` 的 INV-6 范式：所有 `RepoCodingTask` 建表/状态推进/wave 写入只经本 service，模型层零业务方法，配 grep 守护断言无旁路写。提供 `create_tasks_for_plan`（写 wave + depends_on M2M）+ `mark_running/done/failed/blocked`（条件更新幂等 status guard）。

Purpose: 为 plan 04 wave 推进 helper（经 service 回填终态 / 阻断下游）与 plan 05 AICodingNode（首发 dispatch 后 mark_running）提供唯一写入口。`mark_blocked` 承载 WAVE-02 下游阻断语义；条件更新幂等承载重复 callback no-op。
Output: `RepoCodingTaskService` + barrel 导出 + service 状态推进/幂等单测 + INV-6 grep 守护测试。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-repocodingtask-execution-plan-dag-wave/44-CONTEXT.md
@.planning/phases/44-repocodingtask-execution-plan-dag-wave/44-RESEARCH.md
@.planning/phases/44-repocodingtask-execution-plan-dag-wave/44-PATTERNS.md

@server/delivery/services/research_service.py
@server/delivery/services/__init__.py
@server/tests/delivery/test_research_inv6_guard.py
@server/delivery/models/repo_coding_task.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: RepoCodingTaskService 单一写入入口</name>
  <read_first>
    - server/delivery/services/research_service.py（行 19-70 imports + 骨架 + create 幂等 get_or_create；行 72-99 mark_running/done/failed；行 157-170 条件更新幂等 status guard 范式）
    - server/delivery/services/__init__.py（barrel 导出 + `__all__`）
    - server/delivery/models/repo_coding_task.py（plan 01 产物：字段名 + 枚举）
    - .planning/phases/44-repocodingtask-execution-plan-dag-wave/44-PATTERNS.md（Copy-from delta service 段，行 262-267）
    - .planning/phases/44-repocodingtask-execution-plan-dag-wave/44-CONTEXT.md（Area 3 单一写入入口 + 幂等；Area 4 下游阻断 mark_blocked 语义）
  </read_first>
  <action>
    新建 `server/delivery/services/repo_coding_task_service.py`，`__all__ = ["RepoCodingTaskService"]`，`from delivery.models import RepoCodingTask, RepoCodingTaskStatus`，`structlog` logger。`class RepoCodingTaskService`，所有写经 `@sync_to_async def _xxx_sync`：
    - `create_tasks_for_plan(plan_version, repo_waves: dict[str, int], repo_dep_edges: dict[str, list[str]])`：同步块内对每个 `repo_id in repo_waves` `RepoCodingTask.objects.get_or_create(plan_version=plan_version, repository_id=repo_id, defaults={"status": PENDING, "wave": repo_waves[repo_id], "attempt": 0})`（幂等）；已存在则更新 wave（`obj.wave != ...` 时 `.save(update_fields=["wave","updated_at"])`）。**同步块内**用 `task.depends_on.set([依赖仓的 RepoCodingTask...])` 连边（按 repo_dep_edges 解析同 plan_version 下 dep repo 的 task 实例；M2M 写必须在 sync 块内，避免 async lazy 访问）。返回 task 列表（按 repo_id 可索引的 dict 或 list）。
    - `mark_running(task, subagent_session)`：`status→running` + 回填 `subagent_session`，`.save(update_fields=["status","subagent_session","updated_at"])`。
    - `mark_done(task)`：**条件更新幂等**——`RepoCodingTask.objects.filter(id=task.id, status=RUNNING).update(status=DONE, updated_at=now())`；影响行数 0 → no-op（重复 callback / 已 done 不报错）。
    - `mark_failed(task, error)`：`status→failed`，`error` 非 dict 包成 `{"message": str(error)}`，`.save(update_fields=["status","error","updated_at"])`。
    - `mark_blocked(task, upstream_ids: list[str])`：条件更新仅 `status=PENDING` → `status=FAILED` + `error={"reason":"upstream_failed","upstream": upstream_ids}`（下游阻断，不 dispatch；已运行/终态的 task 不强翻）。
    中文 docstring，ruff line 100。在 `server/delivery/services/__init__.py` 新增 `from delivery.services.repo_coding_task_service import RepoCodingTaskService` 并追加进 `__all__`。
  </action>
  <verify>
    <automated>cd server && uv run python -c "from delivery.services import RepoCodingTaskService; s=RepoCodingTaskService(); print([m for m in dir(s) if not m.startswith('_')])"</automated>
  </verify>
  <acceptance_criteria>
    - `cd server && uv run python -c "from delivery.services import RepoCodingTaskService"` 无 ImportError。
    - 输出含 `create_tasks_for_plan`、`mark_running`、`mark_done`、`mark_failed`、`mark_blocked`。
    - `cd server && uv run ruff check delivery/services/repo_coding_task_service.py` 通过。
    - 所有 ORM 写在 `@sync_to_async _xxx_sync` 内（grep 确认无 async 函数体内裸 `.objects.create/.save`）。
  </acceptance_criteria>
  <done>RepoCodingTaskService 创建并经 barrel 导出，5 写入方法齐备、幂等 status guard 到位、INV-6 单一写入口。</done>
</task>

<task type="auto">
  <name>Task 2: INV-6 grep 守护测试（镜像 test_research_inv6_guard.py）</name>
  <read_first>
    - server/tests/delivery/test_research_inv6_guard.py（行 28-50 正则 + `_ALLOWED_WRITER` + `_is_scanned` 排除集；两个测试函数 test_inv6_no_bypass_* + test_inv6_writer_actually_writes）
    - .planning/phases/44-repocodingtask-execution-plan-dag-wave/44-PATTERNS.md（Shared Patterns INV-6 守护段，行 518-537）
  </read_first>
  <action>
    新建 `server/tests/delivery/test_repo_coding_task_inv6_guard.py`，逐字镜像 `test_research_inv6_guard.py`，仅换：模型名 `RepoCodingTask`（单模型，正则 `\bRepoCodingTask\.objects\.(?:create|bulk_create|get_or_create|update_or_create|update)\b` / 实例化 `\bRepoCodingTask\s*\(` / 链式 save）；`_ALLOWED_WRITER = "delivery/services/repo_coding_task_service.py"`。`_is_scanned` 排除集（`tests/` / `migrations/` / `delivery/models/` + writer 自身）逐字保留。镜像两个测试函数：`test_inv6_no_bypass`（扫 `server/` 源码断言无旁路写）+ `test_inv6_writer_actually_writes`（断言 writer 自身确实含写）。**注意正则天然排除 `RepoCodingTaskStatus(`**（枚举非写，前缀 `RepoCodingTask` 后须紧跟 `.objects` 或 `(`，`Status` 后无此模式——核对正则边界，必要时用 `\bRepoCodingTask\b` 词边界防误匹配 Status）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/delivery/test_repo_coding_task_inv6_guard.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `cd server && uv run pytest tests/delivery/test_repo_coding_task_inv6_guard.py -x` 全绿。
    - 守护测试确实扫描 `server/` 源码（非空文件集，断言至少扫到 writer 文件）。
    - `RepoCodingTaskStatus(...)` 枚举用法不触发误报（测试通过即证明）。
  </acceptance_criteria>
  <done>INV-6 grep 守护测试全绿，断言除 service 外无旁路写 RepoCodingTask。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: service 状态推进 + 幂等单测</name>
  <read_first>
    - server/delivery/services/repo_coding_task_service.py（Task 1 产物）
    - server/tests/delivery/test_research_inv6_guard.py（delivery 测试构造 fixture 风格）
    - .planning/phases/44-repocodingtask-execution-plan-dag-wave/44-VALIDATION.md（service 测试行 59）
  </read_first>
  <behavior>
    - test_create_idempotent: create_tasks_for_plan 同 plan_version 调两次 → 同一批 task（get_or_create 幂等），wave 与 depends_on 边正确写入。
    - test_mark_done_idempotent: running→done 第一次成功；对已 done task 再 mark_done → no-op 不报错、status 仍 done。
    - test_mark_done_guard: 对 pending（非 running）task mark_done → no-op（条件更新影响 0 行），status 仍 pending。
    - test_mark_blocked: pending task mark_blocked(["u1","u2"]) → status=failed, error={"reason":"upstream_failed","upstream":["u1","u2"]}。
    - test_mark_failed_wraps: mark_failed(task, "boom") → error={"message":"boom"}（非 dict 包装）。
  </behavior>
  <action>
    新建 `server/tests/delivery/test_repo_coding_task_service.py`（`@pytest.mark.django_db`，async 测试用 `@pytest.mark.asyncio` 经 service 调用）。复用 plan 01 模型测试的 PlanVersion/Repository fixture 构造方式。断言上述 5 组行为。幂等断言：mark_done 两次后用 `RepoCodingTask.objects.aget` 复读 status==done 且无异常。M2M depends_on 边断言在同步上下文读取（`sync_to_async` 或 django_db 同步段）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/delivery/test_repo_coding_task_service.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `cd server && uv run pytest tests/delivery/test_repo_coding_task_service.py -x` 全绿。
    - 含 mark_done 幂等（重复调用 no-op）断言。
    - 含 mark_blocked error 结构断言 `{"reason":"upstream_failed",...}`。
    - 含 create_tasks_for_plan 幂等（两次调用不重复建行）断言。
  </acceptance_criteria>
  <done>service 状态推进 + 幂等单测全绿，覆盖创建幂等 / mark_done 幂等与 guard / mark_blocked / mark_failed 包装。</done>
</task>

</tasks>

<artifacts_this_phase_produces>
- **类**：`RepoCodingTaskService`（`server/delivery/services/repo_coding_task_service.py`）
- **方法**：`create_tasks_for_plan(plan_version, repo_waves, repo_dep_edges)`、`mark_running(task, subagent_session)`、`mark_done(task)`、`mark_failed(task, error)`、`mark_blocked(task, upstream_ids)`
- **barrel 导出**：`delivery.services.RepoCodingTaskService`
- **新文件**：`server/delivery/services/repo_coding_task_service.py`、`server/tests/delivery/test_repo_coding_task_service.py`、`server/tests/delivery/test_repo_coding_task_inv6_guard.py`
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service ↔ RepoCodingTask 表 | 唯一受信写入边界——所有状态变更必经此 service |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-INV6 | Tampering | 旁路写 RepoCodingTask 绕过单一写入入口 | mitigate | INV-6 grep 守护测试扫描 `server/` 源码断言除 `repo_coding_task_service.py` 外无 `.objects.create/get_or_create/update` / 实例化 / `.save(`（守护 INV-6 精神，禁旁路写表） |
| T-44-IDEM-DUP | DoS | 重复 callback → 重复状态推进 | mitigate | `mark_done` / `mark_blocked` 用条件 `.filter(status=...).update(...)` + 影响行数判定，重复调用 no-op |

**Note:** service 不读凭证、不直连用户输入；写入纯内部状态。单一写入口 + 幂等是本 plan 的核心安全控制。
</threat_model>

<verification>
- `cd server && uv run pytest tests/delivery/test_repo_coding_task_service.py tests/delivery/test_repo_coding_task_inv6_guard.py -x` 全绿。
- `cd server && uv run ruff check delivery/services/repo_coding_task_service.py` 通过。
- barrel 可导入 RepoCodingTaskService。
</verification>

<success_criteria>
RepoCodingTaskService 单一写入入口落地，5 写入方法 + 幂等 status guard + mark_blocked 下游阻断语义齐备；INV-6 grep 守护与状态推进/幂等单测全绿。
</success_criteria>

<output>
Create `.planning/phases/44-repocodingtask-execution-plan-dag-wave/44-03-SUMMARY.md` when done
</output>
