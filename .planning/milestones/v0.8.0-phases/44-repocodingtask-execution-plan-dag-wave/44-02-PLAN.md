---
phase: 44-repocodingtask-execution-plan-dag-wave
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - server/services/plan_orchestration/wave_layering.py
  - server/services/plan_orchestration/__init__.py
  - server/tests/services/plan_orchestration/test_wave_layering.py
autonomous: true
requirements: [WAVE-01]
must_haves:
  truths:
    - "execution_plan 全空 dependencies → 所有仓 wave=0（单 wave 全并行，零回归）"
    - "线性链 / 菱形依赖 → 按拓扑层级分仓级 wave；同仓多 task 取依赖层级 max"
    - "依赖环 → fail-fast 返回 {reason: dependency_cycle}，不分层"
    - "dependencies 解析为 task id（非 repository_id），跨仓才成 depends_on 边（同仓自环去除）"
  artifacts:
    - path: "server/services/plan_orchestration/wave_layering.py"
      provides: "build_repo_waves + build_repo_dep_edges 纯函数"
      contains: "def build_repo_waves"
  key_links:
    - from: "server/services/plan_orchestration/wave_layering.py"
      to: "services.plan_orchestration.plan_validator.validate_plan"
      via: "复用 dependency_cycle 环检测（不重写）"
      pattern: "validate_plan"
---

<objective>
立拓扑分层纯函数 `wave_layering.py`（WAVE-01 拓扑分层半幅，消化 PF-07）——把 `MergedPlan.execution_plan[].dependencies`（task id 引用）真正消费：建任务级 DAG → 投影为仓级 `depends_on` 边 → Kahn 分层算出每仓 `wave`。复用 `plan_validator.validate_plan` 做环检测（不重写），空依赖退化单 wave 全并行（零回归命门）。

Purpose: 为 plan 03 的 `RepoCodingTaskService.create_tasks_for_plan` 提供 `{repo_id: wave}` 与 `{repo_id: [dep_repo_id]}`，为 plan 05 的 AICodingNode 首发 wave 0 提供分层结果。纯函数无 IO，独立于模型，可与 plan 01 并行。
Output: `build_repo_waves` + `build_repo_dep_edges` 纯函数 + barrel 导出 + 拓扑分层单测（空/线性/菱形/环/同仓 max）。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-repocodingtask-execution-plan-dag-wave/44-CONTEXT.md
@.planning/phases/44-repocodingtask-execution-plan-dag-wave/44-RESEARCH.md
@.planning/phases/44-repocodingtask-execution-plan-dag-wave/44-PATTERNS.md

@server/services/plan_orchestration/plan_validator.py
@server/workflows/schemas/technical_plan.py
@server/services/plan_orchestration/__init__.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: build_repo_waves + build_repo_dep_edges 纯函数（复用 validate_plan 环检测）</name>
  <read_first>
    - server/services/plan_orchestration/plan_validator.py（行 28-76 CHECK_NAMES + validate_plan 契约；行 216-225 task id 建图范式；半可信防御 `t.get("dependencies") or []`）
    - server/workflows/schemas/technical_plan.py（行 32-35：`dependencies: list[str]  # Task IDs this depends on` —— 权威确认 dependencies=task id 非 repository_id）
    - server/services/plan_orchestration/__init__.py（barrel 导出格式 + `__all__`）
    - .planning/phases/44-repocodingtask-execution-plan-dag-wave/44-RESEARCH.md（Code Examples 拓扑分层 + 仓级边投影，行 305-364）
    - .planning/phases/44-repocodingtask-execution-plan-dag-wave/44-PATTERNS.md（wave_layering Apply 段，行 324-328）
  </read_first>
  <action>
    新建 `server/services/plan_orchestration/wave_layering.py`，`__all__ = ["build_repo_waves", "build_repo_dep_edges"]`：
    - `build_repo_waves(execution_plan: list[dict]) -> tuple[dict[str, int], dict | None]`：
      1. 先 `from services.plan_orchestration.plan_validator import validate_plan`（函数内或模块顶部，避免 import 环——参 plan_validator 已是叶子模块可顶部 import）；调 `validate_plan({"execution_plan": execution_plan})` 取 `report["errors"]` 中 `check == "dependency_cycle"` 项 → 非空则 fail-fast 返回 `({}, {"reason": "dependency_cycle", "detail": cycle_errs})`。
      2. 建 `task_repo = {t["id"]: t.get("repository_id", "") for t in execution_plan if t.get("id")}`；`task_deps = {t["id"]: [d for d in (t.get("dependencies") or []) if d in task_repo] for t in execution_plan if t.get("id")}`（半可信防御：缺 id 跳过、无效引用过滤、绝不 eval/不抛）。
      3. 用 `graphlib.TopologicalSorter(task_deps)` 的 `prepare()` + `while is_active(): ready=list(get_ready()); 同层赋 layer; done(ready)` 分层得 `task_wave`（边方向 = task → 它依赖的 task，依赖先行）。
      4. 仓 wave = 该仓所有 task 层级 **max**：`repo_wave[rid] = max(repo_wave.get(rid,0), w)`（跳过空 rid）。返回 `(repo_wave, None)`。
    - `build_repo_dep_edges(execution_plan: list[dict]) -> dict[str, list[str]]`：`taskA` 依赖 `taskB` → `repo(A)` 依赖 `repo(B)`，**仅跨仓成边**（`ra and rb and ra != rb`，去同仓自环）；返回 `{repo_id: sorted(dep_repo_ids)}`。
    半可信防御逐字对齐 plan_validator（`.get(...) or []`、`.get("repository_id","")`）。中文 docstring，ruff line 100。
    在 `server/services/plan_orchestration/__init__.py` 新增 `from services.plan_orchestration.wave_layering import (build_repo_waves, build_repo_dep_edges)` 并追加进 `__all__`。
  </action>
  <verify>
    <automated>cd server && uv run python -c "from services.plan_orchestration import build_repo_waves, build_repo_dep_edges; print(build_repo_waves([]))"</automated>
  </verify>
  <acceptance_criteria>
    - `cd server && uv run python -c "from services.plan_orchestration import build_repo_waves, build_repo_dep_edges"` 无 ImportError。
    - 空 execution_plan → `build_repo_waves([])` 返回 `({}, None)`，不抛。
    - `cd server && uv run ruff check services/plan_orchestration/wave_layering.py` 通过。
    - 内联烟测：含环的 execution_plan（A→B→A）→ 返回 tuple[1] 为 `{"reason": "dependency_cycle", ...}`。
  </acceptance_criteria>
  <done>两纯函数创建并经 barrel 导出；环检测复用 validate_plan；空依赖与半可信输入 fail-safe 不抛。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 拓扑分层单测（空依赖/线性/菱形/环/同仓 max）</name>
  <read_first>
    - server/services/plan_orchestration/wave_layering.py（Task 1 产物）
    - server/services/plan_orchestration/plan_validator.py（环报告 errors 结构）
    - .planning/phases/44-repocodingtask-execution-plan-dag-wave/44-VALIDATION.md（WAVE-01 分层测试行 42-44；test 名 `test_empty_deps_single_wave` / `test_same_repo_max_wave`）
  </read_first>
  <behavior>
    - test_empty_deps_single_wave: 3 仓各 1 task 无 dependencies → 全仓 wave=0；build_repo_dep_edges 返回 {}（零回归命门）。
    - test_linear_chain: taskA(repoA) ← taskB(repoB depends A) ← taskC(repoC depends B) → repoA=0,repoB=1,repoC=2；edges repoB→[repoA], repoC→[repoB]。
    - test_diamond: A←B,A←C,B←D,C←D（D 依赖 B 和 C，B/C 依赖 A）→ repo(A)=0, repo(B)=repo(C)=1, repo(D)=2。
    - test_cycle_fail_fast: A←B, B←A → build_repo_waves 返回 ({}, {"reason":"dependency_cycle",...})。
    - test_same_repo_max_wave: 同仓 repoX 有 task0(wave0) 与 task1(依赖另一仓 repoY 的 wave0 task → wave1) → repoX wave 取 max=1；同仓内部依赖不产生 repoX 自环边。
  </behavior>
  <action>
    新建 `server/tests/services/plan_orchestration/test_wave_layering.py`（纯函数测试，无 `django_db`）。构造 execution_plan list[dict]（每项含 `id`/`repository_id`/`dependencies`）覆盖上述 5 场景。断言 `build_repo_waves` 返回的 `repo_wave` dict 与 cycle report，及 `build_repo_dep_edges` 的跨仓边集合（同仓自环不出现）。若 `tests/services/plan_orchestration/` 无 `__init__.py` 需按既有测试目录约定（grep 现有 `tests/services/plan_orchestration/` 结构）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/plan_orchestration/test_wave_layering.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `cd server && uv run pytest tests/services/plan_orchestration/test_wave_layering.py -x` 全绿。
    - `cd server && uv run pytest tests/services/plan_orchestration/test_wave_layering.py::test_empty_deps_single_wave -x` 单独可跑通（VALIDATION 引用名）。
    - `cd server && uv run pytest tests/services/plan_orchestration/test_wave_layering.py::test_same_repo_max_wave -x` 单独可跑通。
    - 含 5 个测试覆盖：空/线性/菱形/环/同仓 max。
  </acceptance_criteria>
  <done>拓扑分层单测全绿，覆盖空依赖零回归、线性、菱形、环 fail-fast、同仓 max。</done>
</task>

</tasks>

<artifacts_this_phase_produces>
- **函数**：`build_repo_waves(execution_plan) -> (dict[repo_id,wave], cycle_report|None)`、`build_repo_dep_edges(execution_plan) -> dict[repo_id, list[repo_id]]`（`server/services/plan_orchestration/wave_layering.py`）
- **barrel 导出**：`services.plan_orchestration.build_repo_waves` / `build_repo_dep_edges`
- **新文件**：`server/services/plan_orchestration/wave_layering.py`、`server/tests/services/plan_orchestration/test_wave_layering.py`
- **复用（不新增）**：`plan_validator.validate_plan`（环检测 check `dependency_cycle`）、`graphlib.TopologicalSorter`（stdlib）
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 半可信 execution_plan → 分层 | `execution_plan` 是 LLM 合成产物（半可信），跨边界进入拓扑分层纯函数 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-DAG-DOS | DoS | build_repo_waves 对半可信 execution_plan 分层 | mitigate | 复用 `plan_validator.validate_plan`（三色 DFS + 显式栈防递归 DoS + 去重边）做环检测；检测到环 fail-fast 不分层；缺字段补 `[]` 不 eval/不抛 |
| T-44-DAG-MALFORM | Tampering | dependencies 引用语义 | mitigate | 严格按 task id 解析（schema 权威），无效引用过滤；同仓自环去除避免错误仓级边 |

**Note:** 纯函数无 IO、无凭证、无外部触面；唯一攻击面是半可信 execution_plan，已复用现成防御。
</threat_model>

<verification>
- `cd server && uv run pytest tests/services/plan_orchestration/test_wave_layering.py -x` 全绿。
- `cd server && uv run ruff check services/plan_orchestration/wave_layering.py` 通过。
- barrel 可导入两函数。
</verification>

<success_criteria>
拓扑分层纯函数落地，dependencies 按 task id 消费、投影仓级 wave/边、环 fail-fast、空依赖退化单 wave 零回归；5 场景单测全绿。
</success_criteria>

<output>
Create `.planning/phases/44-repocodingtask-execution-plan-dag-wave/44-02-SUMMARY.md` when done
</output>
