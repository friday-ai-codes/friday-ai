---
phase: 44-repocodingtask-execution-plan-dag-wave
plan: 05
type: execute
wave: 4
depends_on: ["44-02", "44-03", "44-04"]
files_modified:
  - server/workflows/nodes/ai/coding.py
  - server/tests/test_coding_wave.py
autonomous: true
requirements: [WAVE-01, WAVE-02]
must_haves:
  truths:
    - "execution_plan 全空 dependencies → 一次性 dispatch 全部仓（与现 AICodingNode 全并行字节级等价，零回归）"
    - "有跨仓 dependencies → 仅 dispatch 当前 wave（最小 pending wave）且 depends_on 全 done 的仓"
    - "依赖环 → 节点返回 failed（error.reason=dependency_cycle），不进 dispatch"
    - "容器回调 resume 经 aadvance_coding_waves 推进：有下一 wave 则 dispatch + 再 waiting_event；全终态则收尾"
    - "部分成功收尾：done 仓出 MR，failed/blocked 仓如实标注，不自动回滚"
    - "wave N→N+1 由既有 _schedule_workflow_resume 回调驱动节点重入（不另造调度）"
  artifacts:
    - path: "server/workflows/nodes/ai/coding.py"
      provides: "wave 分批 dispatch + resume 推进接线（RepoCodingTask 持久化 wave 状态）"
      contains: "build_repo_waves"
  key_links:
    - from: "server/workflows/nodes/ai/coding.py"
      to: "delivery.services.RepoCodingTaskService.create_tasks_for_plan"
      via: "首发段单一写入建 RepoCodingTask 行（INV-6）"
      pattern: "create_tasks_for_plan"
    - from: "server/workflows/nodes/ai/coding.py"
      to: "services.plan_orchestration.aadvance_coding_waves"
      via: "resume 段判 gate → dispatch 下一 wave / 收尾"
      pattern: "aadvance_coding_waves"
---

<objective>
把 `AICodingNode` 从「一把梭全并行 dispatch + 一次性 resume」改成「按拓扑 wave 分批 dispatch，wave N 全终态才推 wave N+1」（WAVE-01 接线 + WAVE-02 推进收尾，消化 PF-07）——薄接线、回归风险最小化：首发段建 RepoCodingTask 行（经 service，写 wave/depends_on）并只 dispatch 当前 wave；resume 段复用 Phase 43 callback 通路重入，调 `aadvance_coding_waves` 判 gate → dispatch 下一 wave 或走既有 MR 收尾段。空依赖退化为现行为字节级等价（零回归命门）。

Purpose: 兑现 phase 44 终态——`dependencies` 真正被消费、下游不再无条件全并行、wave 失败/部分成功语义落地。**不另造调度**：wave 推进由容器回调 `_schedule_workflow_resume` 触发节点重入自驱（callbacks.py 不改契约）。
Output: 改造后的 `coding.py`（`_execute_with_branch` 首发 + `_resume_after_containers` 推进/收尾）+ wave 集成测试（mock dispatcher + SubAgentSession）。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-repocodingtask-execution-plan-dag-wave/44-CONTEXT.md
@.planning/phases/44-repocodingtask-execution-plan-dag-wave/44-RESEARCH.md
@.planning/phases/44-repocodingtask-execution-plan-dag-wave/44-PATTERNS.md

@server/workflows/nodes/ai/coding.py
@server/workflows/nodes/ai/plan_research.py
@server/services/plan_orchestration/research_adapter.py
@server/subagent/api/callbacks.py
@server/tests/test_coding_node.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: _execute_with_branch 首发段——建 RepoCodingTask + 仅 dispatch 当前 wave</name>
  <read_first>
    - server/workflows/nodes/ai/coding.py（行 296-506 `_execute_with_branch`：现有全并行 dispatch + waiting_event；行 706-718 `_extract_plan_data`；行 720-729 `_group_by_repository`；行 415-432 asyncio.gather 隔离；行 468-490 waiting_event output_data 形状）
    - server/workflows/nodes/ai/plan_research.py（行 85-99 + 300-308：canonical 输出含 `plan_version_id`——确认 plan_data 可携 plan_version_id 的来源）
    - server/services/plan_orchestration/research_adapter.py（行 201-216：dispatch → mark_running 经 service 范式）
    - server/services/plan_orchestration/wave_layering.py（plan 02：build_repo_waves / build_repo_dep_edges 签名）
    - server/delivery/services/repo_coding_task_service.py（plan 03：create_tasks_for_plan / mark_running 签名）
    - .planning/phases/44-repocodingtask-execution-plan-dag-wave/44-PATTERNS.md（coding.py Apply 段，行 507-512）
    - .planning/phases/44-repocodingtask-execution-plan-dag-wave/44-RESEARCH.md（Pitfall 2 零回归命门；Pitfall 7 wave 从 DB；Open Q2 同仓多 task 一容器）
  </read_first>
  <action>
    改造 `_execute_with_branch`（薄接线，保留现有凭证解析/分支解析/`asyncio.gather(..., return_exceptions=True)`/单仓隔离逻辑不动）：
    1. 在 `_group_by_repository` 后，调 `repo_waves, cycle = build_repo_waves(execution_plan)`（plan 02 纯函数）。若 `cycle is not None` → 立即 `return NodeResult(status="failed", error="依赖环：" + str(cycle), output={"error": cycle}, next_handle="error")`（fail-fast，不进 dispatch）。
    2. 解析 `plan_version_id`：从 `plan_data.get("plan_version_id")` 读（canonical orchestration 输出携带，参 plan_research 输出）；async 取 `plan_version = await PlanVersion.objects.filter(id=plan_version_id).afirst()`（`from delivery.models import PlanVersion`，lazy import）。
    3. **wave 接线（plan_version 可解析时）**：调 `repo_edges = build_repo_dep_edges(execution_plan)` → `await RepoCodingTaskService().create_tasks_for_plan(plan_version, repo_waves, repo_edges)`（INV-6 单一写入建 RepoCodingTask 行）。当前 wave = 最小 wave（首发恒为 `min(repo_waves.values())`，空 deps 时全 0）。只对 `repo_waves[repo_id] == current_wave` 的仓构造 `_run_repo_coding` 协程（保留 `asyncio.gather(..., return_exceptions=True)`）；dispatch 成功后经 `service.mark_running(task, subagent_session)` 回填（参 research_adapter 范式，按 repo_id 关联 RepoCodingTask）。
    4. **零回归退化**：空 deps → 全仓 wave=0 → 当前 wave 仓 = 全部仓 → 一次性 dispatch 全部 = 现行为字节级等价（succeeded/failed 分离、单 wave 完成即收尾，**不引入额外 resume 轮次**）。若 `plan_version` **不可解析**（非 canonical / legacy plan_data 无 plan_version_id）→ `logger.warning("repo_coding_task_skipped_no_plan_version")` 并退化为**现有全并行 dispatch 全部仓**（不建 RepoCodingTask），保护既有非编排路径零回归。
    5. waiting_event 的 `output_data` 追加 `"plan_version_id": str(plan_version_id) if plan_version else ""`（resume 段从 DB 重算 wave，wave 状态**不存内存**——只传 plan_version_id 这一无状态锚）。其余 output_data 字段（pending_sessions/failed_repos/plan_data/branch_name/base_branch/plan_title/repositories）保持现形状。
    中文注释说明「wave 状态走 DB / 不另造调度」。async ORM 安全（`*_id` 标量 / `afirst`）。ruff line 100。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/test_coding_node.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `cd server && uv run pytest tests/test_coding_node.py -x` 全绿（现有 12 passed + 既有 xfail 无回归——证明零回归）。
    - `cd server && uv run ruff check workflows/nodes/ai/coding.py` 通过。
    - grep 确认 `_execute_with_branch` 含 `build_repo_waves` + `create_tasks_for_plan` 调用，且环检测 fail-fast 分支存在。
    - grep 确认 waiting_event output_data 含 `plan_version_id` 键。
  </acceptance_criteria>
  <done>首发段建 RepoCodingTask（经 service）+ 仅 dispatch 当前 wave + 环 fail-fast + 空依赖/无 plan_version 零回归退化；现有 coding node 测试无回归。</done>
</task>

<task type="auto">
  <name>Task 2: _resume_after_containers——aadvance_coding_waves 推进下一 wave / 部分成功收尾</name>
  <read_first>
    - server/workflows/nodes/ai/coding.py（行 508-705 `_resume_after_containers`：现有 session 结果查询 + MR 创建收尾段；行 219-235 execute resume 分支检测 `_resume_from_callback`）
    - server/subagent/api/callbacks.py（`_schedule_workflow_resume` fire-and-forget + 「所有 SubAgentSession 终态才续跑」二次 guard + fail-soft swallow 范式——**不改契约**）
    - server/services/plan_orchestration/wave_progression.py（plan 04：aadvance_coding_waves 返回 dict 形状 {"waiting"|"dispatch"+"wave"|"all_terminal"}）
    - .planning/phases/44-repocodingtask-execution-plan-dag-wave/44-RESEARCH.md（Pitfall 4 wave 推进不让回调 5xx；Architecture Diagram resume 分支步骤 1-5）
    - .planning/phases/44-repocodingtask-execution-plan-dag-wave/44-CONTEXT.md（Area 4 部分成功收尾 + 不自动回滚 + rollback_plan 仅人工指引）
  </read_first>
  <action>
    改造 `_resume_after_containers`：
    1. 从 `output_data.get("plan_version_id")` 读 plan_version_id。**若为空**（legacy 单 wave 无 plan_version）→ 走现有一次性收尾逻辑（MR 创建段不动，零回归）。
    2. **若非空**：先回填本批 pending_sessions 终态 + 调 `result = await aadvance_coding_waves(plan_version_id)`（plan 04 helper）——整段独立 `try/except Exception` swallow + `logger.warning("coding_wave_advance_failed", ...)` 降级（**绝不让节点重入异常回灌让回调 5xx**，对齐 Pitfall 4；`_schedule_workflow_resume` 本身 fire-and-forget 不改）。
    3. `result` 含 `"dispatch"`（下一 wave 待 dispatch task 列表 + `"wave"`）→ 对这些仓构造 `_run_repo_coding`（复用首发 dispatch 逻辑，建议抽 `_dispatch_wave(repos, ...)` 私有 helper 供首发/推进共用，**不造两套**）+ `service.mark_running` + 返回 `NodeResult(status="waiting_event", output={...含 plan_version_id...})` 再次挂起（wave N→N+1 循环，由下一轮回调重入驱动）。
    4. `result` 为 `{"all_terminal": True}`（或 `{"waiting": True}` 但实际无更多在途）→ 走**既有 MR 创建 + 飞书卡片收尾段**：为 `done` 仓正常创建 MR（每仓独立 MR，Phase 46 才融合），`failed`/`blocked` 仓在结果 `failed_details` 中如实标注（含 `error.reason=upstream_failed` 的 blocked 仓）。**不自动回滚**——已成功仓分支/容器产物不回退/不删分支（v0.8 显式非目标）；`MergedPlan.rollback_plan` 仅作人工指引呈现（如已在卡片渲染则保留，不新增自动执行）。
    中文注释。async ORM 安全。ruff line 100。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/test_coding_node.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `cd server && uv run pytest tests/test_coding_node.py -x` 全绿（resume 路径无回归）。
    - `cd server && uv run ruff check workflows/nodes/ai/coding.py` 通过。
    - grep 确认 `_resume_after_containers` 调 `aadvance_coding_waves` 且包裹 try/except + logger.warning（fail-soft）。
    - grep 确认下一 wave dispatch 复用与首发同一 `_dispatch_wave` helper（不重复 dispatch 逻辑）。
    - 无新增轮询/定时器/调度循环（grep 确认无 `asyncio.sleep` 循环 / `while True` 调度）。
  </acceptance_criteria>
  <done>resume 段经 aadvance_coding_waves 推进下一 wave（再 waiting_event）或部分成功收尾；fail-soft swallow；复用 callback 通路不另造调度；不自动回滚。</done>
</task>

<task type="auto">
  <name>Task 3: AICodingNode wave 集成测试（mock dispatcher + SubAgentSession）</name>
  <read_first>
    - server/tests/test_coding_node.py（现有 AICodingNode 测试 fixture：mock dispatcher / SubAgentSession / ExecutionContext 构造 / waiting_event 断言——复用 mock 边界，planner 不自造）
    - server/workflows/nodes/ai/coding.py（Task 1/2 改造产物）
    - server/delivery/services/repo_coding_task_service.py（plan 03）+ server/services/plan_orchestration/wave_progression.py（plan 04）
    - .planning/phases/44-repocodingtask-execution-plan-dag-wave/44-VALIDATION.md（WAVE-02 部分成功收尾行 50；Manual-Only：真实 runner E2E deferred）
  </read_first>
  <behavior>
    - test_empty_deps_zero_regression: execution_plan 全空 dependencies（2-3 仓）→ 首发一次性 dispatch 全部仓（dispatch 调用次数 == 仓数），resume 一次即收尾出 MR；断言**无额外 resume 轮次**、succeeded/failed 分离与现行为一致。
    - test_multi_wave_progression: 线性 2 仓依赖（repoB depends repoA）→ 首发仅 dispatch repoA（wave0）；mock repoA SubAgentSession done → 回调 resume → dispatch repoB（wave1）+ 再 waiting_event；mock repoB done → resume → 收尾两仓出 MR。
    - test_partial_success_finalize: wave0 repoA done、repoB failed；repoC（wave1）depends repoB → resume 后 repoC 标 blocked（不 dispatch）；收尾为 repoA 出 MR，repoB failed / repoC blocked 在 failed_details 如实标注，无自动回滚（断言未调删分支/回退）。
  </behavior>
  <action>
    新建 `server/tests/test_coding_wave.py`（`@pytest.mark.django_db` + asyncio），复用 `tests/test_coding_node.py` 的 mock dispatcher + SubAgentSession + ExecutionContext fixture（grep 复用，不自造）。构造带 `plan_version_id` 的 plan_data（建真实 PlanVersion fixture）+ execution_plan（含/不含 dependencies）。mock `get_dispatcher().dispatch` 计数 dispatch 次数；mock SubAgentSession 终态驱动 resume（设 output_data `_resume_from_callback=True` 模拟回调重入）。覆盖上述 3 场景。
    **注**：现有 coding 节点测试在扁平 `tests/`（无 `tests/workflows/coding*`），本测试落 `tests/test_coding_wave.py` 对齐实际仓库布局；VALIDATION.md / RESEARCH.md 测试映射命令已统一为 `pytest tests/test_coding_wave.py -x`。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/test_coding_wave.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `cd server && uv run pytest tests/test_coding_wave.py -x` 全绿。
    - test_empty_deps_zero_regression 断言 dispatch 次数 == 仓数且单 resume 收尾（零回归命门）。
    - test_multi_wave_progression 断言首发仅 dispatch wave0 仓、resume 后 dispatch wave1 仓。
    - test_partial_success_finalize 断言 repoC blocked（error.reason=upstream_failed）+ repoA 出 MR + 无自动回滚。
  </acceptance_criteria>
  <done>wave 集成测试全绿，覆盖空依赖零回归 / 多 wave 推进 / 部分成功收尾（mock IO 边界）。</done>
</task>

</tasks>

<artifacts_this_phase_produces>
- **改造**：`server/workflows/nodes/ai/coding.py` —— `_execute_with_branch`（首发建 RepoCodingTask + 仅 dispatch 当前 wave + 环 fail-fast）、`_resume_after_containers`（aadvance_coding_waves 推进 / 部分成功收尾）、新增私有 helper `_dispatch_wave`（首发/推进共用，不造两套）
- **新文件**：`server/tests/test_coding_wave.py`（wave 集成测试，mock dispatcher + SubAgentSession）
- **复用（不改契约）**：`subagent/api/callbacks.py:_schedule_workflow_resume`（wave N→N+1 回调驱动重入）、`_group_by_repository`、`asyncio.gather(return_exceptions=True)` 单仓隔离、既有 MR 创建收尾段
- **消费**：`build_repo_waves`/`build_repo_dep_edges`（plan 02）、`RepoCodingTaskService`（plan 03）、`aadvance_coding_waves`（plan 04）、`PlanVersion`（plan_version_id 锚）
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 容器回调 → 节点重入 → wave dispatch | runner 回调触发节点重入；节点据服务端权威 RepoCodingTask/SubAgentSession 状态决定 dispatch |
| 半可信 execution_plan → wave 分批 dispatch | LLM 合成 execution_plan 决定 dispatch 拓扑 |
| dispatch payload → 容器（git/anthropic 凭证注入） | 凭证经 dispatch metadata 下传容器 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-TAMPER-NODE | Tampering / Elevation | resume 推进越权他人 plan_version | mitigate | wave 推进限本 `plan_version_id`（从节点 output_data 锚，服务端写入）；终态判定经 `aadvance_coding_waves` 读服务端权威 SubAgentSession.status，不信 runner 可改 last_output（对齐 T-43-TAMPER） |
| T-44-DAG-DOS-NODE | DoS | 半可信 execution_plan 含环/超大 DAG | mitigate | `build_repo_waves` 复用 plan_validator 三色 DFS 显式栈；环 fail-fast 节点直接 failed 不进 dispatch |
| T-44-CRED | Info Disclosure | wave dispatch 复用凭证注入 | accept (已缓解) | 复用既有 `_run_repo_coding` 凭证注入逻辑（Phase 43 PF-06 已对齐 git token + anthropic 凭证）；日志仅记 `has_api_key`/`has_git_token` 布尔，凭证绝不入日志——本 plan 不改凭证路径 |
| T-44-IDEM-NODE | DoS | 重复回调 → 重复 dispatch | mitigate | 推进经 `aadvance_coding_waves` + service 条件更新幂等（已 running/done 仓不重复 dispatch）；`_schedule_workflow_resume` 二次 guard「所有 SubAgentSession 终态才续跑」 |

**Note:** 本 plan 不新增对外端点、不改回调契约、不改凭证解析。新增攻击面仅限「半可信 execution_plan 驱动 dispatch 拓扑」与「回调驱动推进」，均复用现成防御（plan_validator / 服务端权威字段 / 幂等）。
</threat_model>

<verification>
- `cd server && uv run pytest tests/test_coding_node.py tests/test_coding_wave.py -x` 全绿（零回归 + 新 wave 行为）。
- `cd server && uv run pytest tests/delivery tests/services/plan_orchestration -x` 全绿（phase 全量）。
- `cd server && uv run ruff check workflows/nodes/ai/coding.py` 通过。
- grep 确认无新增轮询/定时器/独立调度循环（不另造调度）。
</verification>

<success_criteria>
AICodingNode 按 wave 分批 dispatch + callback 驱动多 wave 推进 + 部分成功收尾；空依赖零回归、环 fail-fast、下游阻断、不自动回滚；wave 集成测试全绿、现有 coding node 测试无回归。
</success_criteria>

<output>
Create `.planning/phases/44-repocodingtask-execution-plan-dag-wave/44-05-SUMMARY.md` when done
</output>
