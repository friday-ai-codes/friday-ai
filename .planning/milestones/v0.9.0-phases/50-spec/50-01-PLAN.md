---
phase: 50-spec
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/delivery/models/sdd_spec_review.py
  - server/delivery/models/__init__.py
  - server/delivery/migrations/0019_sddspecreview.py
  - server/tests/delivery/test_sdd_spec_review_model.py
autonomous: true
requirements: [SPECST-02]
must_haves:
  truths:
    - "SddSpecReview 表存在，可记录 reviewer / decision(approve|reject) / comment / created_at"
    - "评审记录不可篡改：模型层无任何 edit/delete/save 业务方法（append-only）"
    - "删 spec 级联删其评审；删 reviewer 用户保留评审记录（reviewer 置空）"
  artifacts:
    - path: "server/delivery/models/sdd_spec_review.py"
      provides: "SddSpecReview 模型 + ReviewDecision 枚举"
      contains: "class SddSpecReview"
    - path: "server/delivery/migrations/0019_sddspecreview.py"
      provides: "delivery_sdd_spec_review 建表 migration"
    - path: "server/delivery/models/__init__.py"
      provides: "curated re-export SddSpecReview / ReviewDecision"
      contains: "SddSpecReview"
  key_links:
    - from: "server/delivery/models/sdd_spec_review.py"
      to: "delivery.SddSpec"
      via: "spec FK on_delete=CASCADE related_name=reviews"
      pattern: "ForeignKey\\(\\s*[\"']delivery.SddSpec"
---

<objective>
为 SddSpec 评审建立不可篡改的数据底座（SPECST-02）：新建 `SddSpecReview` append-only 模型 + `ReviewDecision` 枚举 + curated re-export + 建表 migration。本 plan 只落数据结构，不含状态机/事务逻辑（归 50-02）。

Purpose: 评审记录即审计留痕；模型层零业务方法守 INV-6（落库只允许经 50-02 的 SddSpecService）+ 不可篡改。
Output: `sdd_spec_review.py` 模型、migration `0019`、`models/__init__.py` re-export、模型守护测试。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/50-spec/50-CONTEXT.md
@.planning/ROADMAP.md
@server/delivery/models/sdd_spec.py
@server/delivery/models/__init__.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 新建 SddSpecReview append-only 模型 + ReviewDecision 枚举 + re-export</name>
  <files>server/delivery/models/sdd_spec_review.py, server/delivery/models/__init__.py, server/tests/delivery/test_sdd_spec_review_model.py</files>
  <behavior>
    - SddSpecReview 含字段：id(UUID pk)、spec(FK→delivery.SddSpec, on_delete=CASCADE, related_name="reviews")、reviewer(FK→settings.AUTH_USER_MODEL, on_delete=SET_NULL, null=True, blank=True, related_name="+")、decision(CharField choices=ReviewDecision)、comment(TextField, blank=True)、created_at(auto_now_add)。
    - ReviewDecision 为 TextChoices：APPROVE="approve","批准"；REJECT="reject","驳回"。
    - Meta：db_table="delivery_sdd_spec_review"；ordering=["-created_at"]；index on (spec, created_at)；verbose_name 中文。
    - append-only 契约：模型类仅 `__str__`，无 create/save 覆盖/状态变更/delete 业务方法（仅 Django 默认 save 继承，不得显式定义业务写方法）。
    - 测试断言：模型类自身命名空间（vars(SddSpecReview)）不含 `update`/`edit`/`apply`/`delete_review` 等业务写方法；字段 on_delete 配置正确（spec=CASCADE、reviewer=SET_NULL）。
  </behavior>
  <action>新建 `server/delivery/models/sdd_spec_review.py`，按 behavior 定义 `ReviewDecision(models.TextChoices)` 与 `SddSpec­Review(models.Model)`（per D-50-2）。reviewer 用 `from django.conf import settings` + `settings.AUTH_USER_MODEL` 字符串前向引用（跨 app，避免 import 环，对齐 `sdd_spec.py` 字符串 FK 范式）；spec 用字符串 `"delivery.SddSpec"`。模块 docstring 写明 append-only / INV-6 设计意图（中文，对齐 `sdd_spec.py` docstring 风格）。在 `server/delivery/models/__init__.py` 按既有 curated re-export 范式 import `SddSpecReview, ReviewDecision`（紧邻 `from delivery.models.sdd_spec import ...` 之后）并追加进 `__all__`。新建 `test_sdd_spec_review_model.py` 写 behavior 断言（纯属性/字段内省，无需 DB；append-only 守护用 `vars(SddSpecReview)` 检查无业务写方法名）。禁止在模型层加任何写入方法（写入归 50-02 SddSpecService，INV-6）。</action>
  <verify>
    <automated>cd server && uv run pytest tests/delivery/test_sdd_spec_review_model.py -x && uv run ruff check delivery/models/sdd_spec_review.py delivery/models/__init__.py</automated>
  </verify>
  <done>SddSpecReview + ReviewDecision 定义完整、append-only 守护测试通过、re-export 生效、ruff 干净。</done>
</task>

<task type="auto">
  <name>Task 2: 生成建表 migration + makemigrations --check 干净 + ORM 级联语义测试</name>
  <files>server/delivery/migrations/0019_sddspecreview.py, server/tests/delivery/test_sdd_spec_review_model.py</files>
  <action>在 `server/` 下运行 `uv run python manage.py makemigrations delivery` 生成 `0019_sddspecreview.py`（依赖 `0018_sddspec`）。检查生成的 migration 含 `delivery_sdd_spec_review` 表、spec FK CASCADE、reviewer FK SET_NULL、(spec, created_at) 索引——与 Task 1 模型一致。在 `test_sdd_spec_review_model.py` 追加一个 `@pytest.mark.django_db` 用例：建一个 SddSpec（复用既有 factory/最小构造）+ 两条 SddSpecReview，断言 (a) `spec.reviews.all()` 倒序返回两条；(b) 删除 spec 后评审被级联删除（CASCADE）；(c) reviewer 用户删除后评审仍存在且 reviewer_id 为 None（SET_NULL）。不得手写 migration 文件内容，必须由 makemigrations 生成（仅人工核对）。</action>
  <verify>
    <automated>cd server && uv run python manage.py makemigrations --check --dry-run && uv run pytest tests/delivery/test_sdd_spec_review_model.py -x</automated>
  </verify>
  <done>migration 0019 生成且 `makemigrations --check` 报告无未生成变更（exit 0）；CASCADE/SET_NULL/倒序 ORM 语义测试通过。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service→DB（评审写入） | 评审记录一旦落库即审计证据，须不可篡改；本 plan 仅建结构，写入入口由 50-02 收口 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-01 | Tampering | SddSpecReview 记录 | mitigate | 模型层零业务写方法（append-only）；唯一写入点由 50-02 INV-6 grep 守护 |
| T-50-02 | Repudiation | reviewer 关联 | mitigate | reviewer FK + created_at 留痕；reviewer SET_NULL 仅在用户删除时置空，记录本体不灭 |
</threat_model>

<verification>
- `uv run pytest tests/delivery/test_sdd_spec_review_model.py` 全绿。
- `uv run python manage.py makemigrations --check --dry-run` exit 0（无漂移）。
- `uv run ruff check delivery/` 干净。
</verification>

<success_criteria>
SddSpecReview 表与模型落地、append-only 契约由测试守护、级联/置空语义验证通过、migration 与模型一致无漂移。映射：SPECST-02（评审不可篡改记录的数据底座）；ROADMAP 成功标准 2。
</success_criteria>

<output>
Create `.planning/phases/50-spec/50-01-SUMMARY.md` when done
</output>
