---
phase: 50-spec
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/api/specs.ts
  - web/src/api/index.ts
  - web/src/components/spec/SddSpecStatusBadge.vue
  - web/src/components/spec/SpecReviewTimeline.vue
  - web/src/locales/zh-CN.json
  - web/src/components/spec/__tests__/SddSpecStatusBadge.test.ts
  - web/src/components/spec/__tests__/SpecReviewTimeline.test.ts
autonomous: true
requirements: [SPECST-03]
must_haves:
  truths:
    - "api/specs.ts 提供 list/detail/transition + 完整 TS 类型，barrel 导出生效"
    - "SddSpecStatusBadge 按 5 态映射色彩 + 经 specs.status.<status> 取词，列表/详情共用"
    - "SpecReviewTimeline 倒序渲染评审历史（reviewer/decision/comment/time），空态有文案"
    - "specs i18n 命名空间按 UI-SPEC 草案落入真实 zh-CN.json"
  artifacts:
    - path: "web/src/api/specs.ts"
      provides: "specsApi(list/detail/transition) + SddSpecStatus/SddSpec/SddSpecReview/SpecTransitionAction 类型"
      contains: "SpecTransitionAction"
    - path: "web/src/components/spec/SddSpecStatusBadge.vue"
      provides: "5 态状态徽标"
      contains: "in_review"
    - path: "web/src/components/spec/SpecReviewTimeline.vue"
      provides: "评审历史时间线"
    - path: "web/src/locales/zh-CN.json"
      provides: "specs i18n 命名空间"
      contains: "spec 治理"
  key_links:
    - from: "web/src/components/spec/SddSpecStatusBadge.vue"
      to: "specs.status.<status> i18n"
      via: "useI18n t(`specs.status.${status}`)"
      pattern: "specs.status"
    - from: "web/src/api/index.ts"
      to: "web/src/api/specs.ts"
      via: "barrel re-export"
      pattern: "specs"
---

<objective>
搭前端 spec 治理基础设施（SPECST-03 第一层，按 50-UI-SPEC）：API client + TS 类型、5 态状态徽标、评审历史时间线、specs i18n 命名空间。本 plan 产出叶子级可复用件，供 50-05 页面装配。

Purpose: 契约先行（interface-first），徽标/时间线/类型先定，页面无需重新探索。
Output: api/specs.ts + barrel、SddSpecStatusBadge.vue、SpecReviewTimeline.vue、zh-CN specs 文案、组件 vitest。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/50-spec/50-UI-SPEC.md
@.planning/phases/50-spec/50-CONTEXT.md
@web/src/api/screenshotRecall.ts
@web/src/api/index.ts
@web/src/components/repository/SddMethodologyBadge.vue
@web/src/components/knowledge/EntityVersionTimeline.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: api/specs.ts（list/detail/transition + TS 类型）+ barrel 导出</name>
  <files>web/src/api/specs.ts, web/src/api/index.ts</files>
  <action>新建 web/src/api/specs.ts（镜像 screenshotRecall.ts 的 client 用法，import get/post from './client'）。TS 类型严格对齐 50-03 REST 契约 + UI-SPEC：SddSpecStatus = 'draft'|'in_review'|'approved'|'implemented'|'archived'；SpecTransitionAction = 'submit_for_review'|'approve'|'reject'|'mark_implemented'|'archive'；SddSpecReview { id; reviewer: string|null; decision: 'approve'|'reject'; comment: string; created_at: string }；SddSpec（list 项）{ id; status; change_kind; repository_id; repository_name; work_item?; updated_at }；SddSpecDetail extends SddSpec { body: string|null; reviews: SddSpecReview[]; relations: { repository?: {id;name;methodology?}; work_item?: {id;title}; plan_version?: {id;version} } }。specsApi：list(params?: { status?; repository_id? }) → GET /specs/（拼 query）；detail(id) → GET /specs/<id>/；transition(id, body: { action: SpecTransitionAction; comment?: string }) → POST /specs/<id>/transition/ 返回 SddSpecDetail。在 web/src/api/index.ts 按既有 barrel 范式加 `export * from './specs'`（字母序就近）。</action>
  <verify>
    <automated>cd web && pnpm vue-tsc --noEmit -p tsconfig.json 2>&1 | head -40</automated>
  </verify>
  <done>specs.ts 类型与 specsApi 完整、与 50-03 契约一致、barrel 导出、vue-tsc 对该文件无类型错误。</done>
</task>

<task type="auto">
  <name>Task 2: SddSpecStatusBadge + SpecReviewTimeline + specs i18n 命名空间</name>
  <files>web/src/components/spec/SddSpecStatusBadge.vue, web/src/components/spec/SpecReviewTimeline.vue, web/src/locales/zh-CN.json</files>
  <action>新建 SddSpecStatusBadge.vue（镜像 SddMethodologyBadge.vue 结构 + class-map 范式）：props { status: SddSpecStatus }，按 UI-SPEC「状态徽标色彩映射」5 态 class（draft 灰 / in_review 琥珀 / approved 绿 / implemented 蓝 / archived 中性，含 dark: 变体），label 经 useI18n t(`specs.status.${status}`)，带 title=label（可达性，color 不单独承载语义）；archived 可附 lucide--archive 图标。新建 SpecReviewTimeline.vue（镜像 EntityVersionTimeline.vue 视觉：ol + space-y + 圆点连线）：props { reviews: SddSpecReview[] }，倒序渲染 reviewer（为 null 用 t('specs.detail.unknownReviewer')）/ decision（approve 绿小徽标 t('specs.detail.decisionApprove') / reject 红小徽标 t('specs.detail.decisionReject')）/ comment / created_at；reviews 为空显示 t('specs.detail.reviewEmpty')。在 web/src/locales/zh-CN.json 顶层加 specs 命名空间，键值严格照搬 50-UI-SPEC「i18n Key 草案」全部条目（title/subtitle/loading/loadError/empty/emptyDescription/emptyFiltered/filter/columns/status/detail/actions/reviewDialog/confirm/toast/error）。JSON 须合法（注意去掉草案中的 jsonc 注释）。</action>
  <verify>
    <automated>cd web && pnpm vue-tsc --noEmit -p tsconfig.json 2>&1 | head -40 && node -e "JSON.parse(require('fs').readFileSync('src/locales/zh-CN.json','utf8'));console.log('json ok')"</automated>
  </verify>
  <done>两组件按 UI-SPEC 范式落地、specs 命名空间合法注入 zh-CN.json、vue-tsc 无新错误。</done>
</task>

<task type="auto">
  <name>Task 3: 组件 vitest（5 态徽标 + 真实 zh-CN 文案 + 时间线倒序/空态）</name>
  <files>web/src/components/spec/__tests__/SddSpecStatusBadge.test.ts, web/src/components/spec/__tests__/SpecReviewTimeline.test.ts</files>
  <action>新建两个 vitest（参考既有 web/src/components/__tests__/status-badge.test.ts 与 RelevanceBadge.test.ts 的挂载 + i18n 范式）。SddSpecStatusBadge.test.ts：对 5 态逐一挂载，断言渲染出真实 zh-CN.json 的 specs.status.<status> 文案（草稿/评审中/已批准/已实现/已归档）且对应色彩 class 存在——i18n 用真实 zh-CN messages（导入真实 locale 文件，对齐 D-50-5 守护要求）。SpecReviewTimeline.test.ts：传 2 条 reviews 断言倒序渲染 + approve/reject 文案；传空数组断言显示 specs.detail.reviewEmpty 真实文案。</action>
  <verify>
    <automated>cd web && pnpm vitest run src/components/spec/__tests__/</automated>
  </verify>
  <done>徽标 5 态真实文案+色彩、时间线倒序+空态测试全绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 后端 detail→前端渲染 | spec 正文/评审 comment 为后端文本，渲染须防注入（本 plan 时间线为纯文本绑定，非 v-html）|

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-12 | Tampering (XSS) | 评审 comment / reviewer 文本 | mitigate | SpecReviewTimeline 用 Vue 文本插值（非 v-html），自动转义 |
| T-50-13 | Information Disclosure | 类型契约漂移 | accept | TS 类型与 50-03 契约对齐由 vue-tsc + API 测试双向约束，低风险 |
</threat_model>

<verification>
- `pnpm vitest run src/components/spec/__tests__/` 全绿。
- `pnpm vue-tsc --noEmit` 无新类型错误。
- `pnpm eslint src/api/specs.ts src/components/spec/` 干净。
- zh-CN.json 合法 JSON（node JSON.parse）。
</verification>

<success_criteria>
spec 前端基础件（API/类型/徽标/时间线/i18n）就绪、5 态徽标真实文案守护、时间线倒序+空态、类型与后端契约对齐。映射：SPECST-03（前端可见基础设施）；ROADMAP 成功标准 3。
</success_criteria>

<output>
Create `.planning/phases/50-spec/50-04-SUMMARY.md` when done
</output>
