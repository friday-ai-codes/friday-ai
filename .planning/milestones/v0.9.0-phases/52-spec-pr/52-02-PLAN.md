---
phase: 52-spec-pr
plan: 02
type: execute
wave: 2
depends_on: ["52-01"]
files_modified:
  - server/delivery/api/serializers.py
  - server/tests/delivery/test_sdd_spec_detail_serializer.py
autonomous: true
requirements: [LINK-01, LINK-02]
must_haves:
  truths:
    - "spec 详情序列化输出 implementation_prs（实现 PR 列表，LINK-01 可见）"
    - "spec 详情 relations.work_item 摘要含 url（取 prd_url）+ title（LINK-02 追溯）"
    - "spec 详情 relations.plan_version 摘要保留（spec→需求→PR 追溯链路完整）"
    - "缺 work_item / 无 implementation_prs 时降级（relations 省缺失键、implementation_prs 回空列表），不报错"
  artifacts:
    - path: "server/delivery/api/serializers.py"
      provides: "SddSpecDetailSerializer 扩 implementation_prs + work_item url + plan_version 追溯摘要"
      contains: "implementation_prs"
    - path: "server/tests/delivery/test_sdd_spec_detail_serializer.py"
      provides: "detail serializer 追溯摘要 + fail-soft 降级守护测试"
  key_links:
    - from: "server/delivery/api/serializers.py"
      to: "SddSpec.implementation_prs / work_item.prd_url / plan_version.version"
      via: "SddSpecDetailSerializer 字段 + get_relations 摘要"
      pattern: "implementation_prs"
---

<objective>
扩 Phase 50 SddSpecDetailSerializer 暴露交付验收追溯数据（D-52-4 后端，LINK-01/LINK-02）：
新增 implementation_prs 字段（实现 PR 列表）+ relations.work_item 补 url（取 WorkItem.prd_url，
对齐 pr_cross_reference 不臆造 URL 范式）+ 保留 plan_version 摘要，形成 spec → 需求 → PR
完整追溯 JSON 供前端验收面板消费。

Purpose: 让前端单 spec 详情页能沿 WorkItem → spec 状态 → 实现 PR 链路渲染交付验收视图；
缺数据 fail-soft 降级（D-52-5）。
Output: 扩展后的 SddSpecDetailSerializer + 追溯摘要/降级守护测试。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/52-spec-pr/52-CONTEXT.md

# Phase 50 序列化器（扩展点：SddSpecDetailSerializer / get_relations）
@server/delivery/api/serializers.py

# detail view 预取（work_item/plan_version 已 select_related，implementation_prs 为 JSON 列无需预取）
@server/delivery/api/spec_views.py

# WorkItem 字段（title + prd_url，url 取 prd_url，不臆造）
@server/delivery/models/work_item.py
@server/delivery/models/sdd_spec.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SddSpecDetailSerializer 扩追溯摘要（D-52-4 后端，LINK-01/LINK-02）</name>
  <files>server/delivery/api/serializers.py, server/tests/delivery/test_sdd_spec_detail_serializer.py</files>
  <behavior>
    - implementation_prs：detail 输出该字段为列表（每项 {pr_url, repository_id, linked_at}）；spec 无回填 → 空列表 []
    - work_item 追溯：relations.work_item 含 {id, title, url}，url 取 work_item.prd_url（无 prd_url → 空串 ""）；无 work_item → relations 不含 work_item 键（降级）
    - plan_version 追溯：relations.plan_version 保留 {id, version}；无 → 不含该键
    - 列表序列化（SddSpecListSerializer）不受影响（implementation_prs/追溯摘要仅 detail 暴露）
  </behavior>
  <action>
    在 SddSpecDetailSerializer（serializers.py:193）的 Meta.fields 追加 `implementation_prs`，
    并以 `implementation_prs = serializers.JSONField(read_only=True)`（直接映射模型 JSON 列，
    缺省 default=list → 空列表，天然 fail-soft）。在 get_relations（line 222）的 work_item
    分支补 `"url": obj.work_item.prd_url or ""`（per D-52-4「补 url/title」；url 取 prd_url，
    对齐 pr_cross_reference.py 不构造臆造 URL 的范式）——work_item_id 为空时维持现有「不输出
    work_item 键」降级。plan_version 摘要保持原样。不改 view（work_item/plan_version 已
    select_related；implementation_prs 为 JSON 列无需 prefetch），不改 List 序列化器字段。
    在 test_sdd_spec_detail_serializer.py 覆盖 behavior：有 implementation_prs 回填 → 字段含
    PR 列表；无回填 → []；有 work_item(prd_url) → relations.work_item 含 url/title；
    work_item 有 title 无 prd_url → url 为 ""；无 work_item → relations 无 work_item 键
    （降级不报错）；有 plan_version → relations.plan_version 含 version。复用既有
    test_spec_api.py / test_sdd_spec_service.py 建模 fixture 范式。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/delivery/test_sdd_spec_detail_serializer.py tests/delivery/test_spec_api.py -x -q</automated>
  </verify>
  <done>detail 序列化含 implementation_prs + work_item(url/title) + plan_version 追溯摘要；缺数据降级（[]/省键）不报错；既有 spec API 测试零回归。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → 前端（spec detail 只读序列化） | implementation_prs / prd_url 作为追溯数据下发渲染 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-04 | Information Disclosure | SddSpecDetailSerializer | accept | 仅暴露已有内部 PR url/工作项 prd_url（无新增敏感面），detail 端点须 IsAuthenticated（Phase 50 已守） |
| T-52-05 | Tampering | implementation_prs 序列化 | mitigate | 字段 read_only，状态/PR 写入只经 SddSpecService（INV-6，序列化器不写表） |
| T-52-SC | Tampering | npm/pip/cargo installs | accept | 本 plan 无新增依赖 |
</threat_model>

<verification>
- 后端：`cd server && uv run ruff check delivery && uv run pytest tests/delivery/test_sdd_spec_detail_serializer.py tests/delivery/test_spec_api.py -q`
- 无新增 migration（仅序列化器变更）；如误改模型须 makemigrations --check 干净
</verification>

<success_criteria>
- SddSpecDetailSerializer 暴露 implementation_prs + work_item(url/title) + plan_version 追溯摘要（LINK-01/LINK-02）
- 缺 work_item / 无 PR 回填时降级（[]/省键）不报错（D-52-5 fail-soft）
- 既有 spec list/detail/transition API 测试零回归
- ruff + pytest 通过
</success_criteria>

<output>
Create `.planning/phases/52-spec-pr/52-02-SUMMARY.md` when done
</output>
