---
phase: 52-spec-pr
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/api/specs.ts
  - web/src/components/spec/SpecDeliveryPanel.vue
  - web/src/pages/specs/[id].vue
  - web/src/locales/zh-CN.json
  - web/src/components/spec/__tests__/SpecDeliveryPanel.spec.ts
autonomous: true
requirements: [LINK-02]
must_haves:
  truths:
    - "spec 详情页展示「交付验收」追溯面板：WorkItem（需求）→ spec（状态徽标）→ 实现 PR 列表"
    - "WorkItem 链接（prd_url）与每个实现 PR（pr_url）可点击跳转"
    - "缺 work_item / 无实现 PR → 降级显「未关联」占位，绝不报错/空白崩溃（fail-soft）"
    - "面板文案接入真实 zh-CN.json（specs.delivery.*），默认中文"
  artifacts:
    - path: "web/src/components/spec/SpecDeliveryPanel.vue"
      provides: "交付验收追溯面板（WorkItem→spec→PR 链路 + fail-soft 降级占位）"
      min_lines: 40
    - path: "web/src/pages/specs/[id].vue"
      provides: "详情页挂接 SpecDeliveryPanel"
      contains: "SpecDeliveryPanel"
    - path: "web/src/api/specs.ts"
      provides: "SddSpecDetail 类型补 implementation_prs + work_item.url（对齐 D-52-4 契约）"
      contains: "implementation_prs"
    - path: "web/src/locales/zh-CN.json"
      provides: "specs.delivery.* zh-CN 文案"
      contains: "delivery"
  key_links:
    - from: "web/src/pages/specs/[id].vue"
      to: "SpecDeliveryPanel"
      via: "传入 spec（status/relations/implementation_prs）"
      pattern: "SpecDeliveryPanel"
    - from: "web/src/components/spec/SpecDeliveryPanel.vue"
      to: "spec.relations.work_item.url / spec.implementation_prs[].pr_url"
      via: "可点击 <a :href>（rel noopener）+ 缺数据占位"
      pattern: "pr_url"
---

<objective>
在 Phase 50 spec 详情页（/specs/[id]）增「交付验收」追溯面板（D-52-4 前端，LINK-02）：沿
WorkItem（需求，可点）→ spec（当前状态徽标）→ 实现 PR 列表（pr_url 可点）链路渲染单次
spec-driven 交付状态。复用 Phase 50 SddSpecStatusBadge / 卡片范式 + MarkdownRenderer /
TanStack Query；缺数据 fail-soft 降级占位（D-52-5）；i18n zh-CN。

Purpose: 让用户在单 spec 详情看到「需求 → spec → 实现 PR」可追溯的交付验收闭环视图。
Output: SpecDeliveryPanel 组件、详情页挂接、specs.ts 类型对齐、zh-CN 文案、vitest 守护。

注：本 plan 按 interface-first 对 D-52-4 锁定契约编码，wave 1 与后端 Plan 01 并行；前端
vitest 用契约 fixture（mock spec detail）验证，不依赖后端 Plan 02 运行时（真实端到端联调
见 deferred E2E）。契约见下文 `<delivery_contract>`。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/52-spec-pr/52-CONTEXT.md

# Phase 50 详情页（挂接点）+ 复用组件
@web/src/pages/specs/[id].vue
@web/src/components/spec/SpecReviewTimeline.vue
@web/src/components/spec/SddSpecStatusBadge.vue

# spec API 类型（扩 implementation_prs + work_item.url）
@web/src/api/specs.ts

# i18n（specs.* 既有结构，新增 specs.delivery.*）
@web/src/locales/zh-CN.json
</context>

<delivery_contract>
D-52-4 锁定的 spec detail 交付验收契约（Plan 02 后端实现，前端按此渲染）：
- `spec.implementation_prs: Array<{ pr_url: string; repository_id: string; linked_at: string }>`（无回填 → `[]`）
- `spec.relations.work_item?: { id: string; title: string; url: string }`（无 work_item → 该键缺失；url 取 prd_url，可能为 `""`）
- `spec.relations.plan_version?: { id: string; version: number }`
- `spec.status: SddSpecStatus`（徽标复用 SddSpecStatusBadge）
</delivery_contract>

<tasks>

<task type="auto">
  <name>Task 1: specs.ts 类型对齐 + specs.delivery zh-CN 文案（D-52-4，LINK-02）</name>
  <files>web/src/api/specs.ts, web/src/locales/zh-CN.json</files>
  <action>
    在 specs.ts 的 `SddSpecDetail` 接口补 `implementation_prs?: Array<{ pr_url: string,
    repository_id: string, linked_at: string }>`（按 delivery_contract），并在 relations
    的 `work_item` 摘要类型补 `url: string`（保持现有 `{ id, title }` 并加 url）。新增导出
    类型 `ImplementationPr` 供组件复用。不改 specsApi 调用（detail 端点字段由后端 Plan 02
    扩展，前端类型先对齐契约）。在 zh-CN.json 的 `specs` 节点新增 `delivery` 子树：标题
    （如 "交付验收"）、链路三段标签（需求/规格/实现 PR）、占位文案（workItemUnlinked
    "未关联需求"、prsEmpty "暂无实现 PR"、planVersionUnlinked 视需要）、PR 列表项辅助文案
    （如「已关联于 {time}」可选）。文案默认中文，键风格对齐既有 specs.detail.* / specs.status.*。
  </action>
  <verify>
    <automated>cd web && pnpm vue-tsc --noEmit && node -e "JSON.parse(require('fs').readFileSync('src/locales/zh-CN.json','utf8'))"</automated>
  </verify>
  <done>SddSpecDetail 类型含 implementation_prs + work_item.url；zh-CN.json 含 specs.delivery.* 且 JSON 合法；vue-tsc 通过。</done>
</task>

<task type="auto">
  <name>Task 2: SpecDeliveryPanel 追溯面板 + 详情页挂接（D-52-4，fail-soft，LINK-02）</name>
  <files>web/src/components/spec/SpecDeliveryPanel.vue, web/src/pages/specs/[id].vue</files>
  <action>
    新建 SpecDeliveryPanel.vue（&lt;script setup lang="ts"&gt;）：props `spec: SddSpecDetail`。
    渲染「交付验收」卡片（复用详情页既有 `card p-5 space-y-3` 范式 + h2 标题用
    t('specs.delivery.title')），内部按链路三段：(1) 需求段——有 relations.work_item 且
    url 非空 → `<a :href="work_item.url" target="_blank" rel="noopener noreferrer">`{title}；
    有 work_item 无 url → 纯文本 title；无 work_item → t('specs.delivery.workItemUnlinked')
    占位。(2) 规格段——`<SddSpecStatusBadge :status="spec.status" />` + spec 标识/标题。
    (3) 实现 PR 段——`spec.implementation_prs?.length` 时逐项渲染可点 `<a :href="pr.pr_url"
    target="_blank" rel="noopener noreferrer">`（展示仓库/截断 url，可附 linked_at）；空/未定义
    → t('specs.delivery.prsEmpty') 占位。全程 fail-soft：所有字段经可选链 + 默认值，绝不因
    缺字段崩溃（不用 v-html，pr_url/url 仅作 href，规避注入）。用 lucide 图标对齐既有关联链接
    区风格。在 [id].vue 的「评审历史」section 之前（或正文之后）插入
    `<SpecDeliveryPanel :spec="spec" />` 并 import 组件。
  </action>
  <verify>
    <automated>cd web && pnpm vue-tsc --noEmit && pnpm eslint src/components/spec/SpecDeliveryPanel.vue src/pages/specs/[id].vue</automated>
  </verify>
  <done>详情页渲染交付验收面板（WorkItem→spec→PR 链路）；缺数据降级占位不崩溃；PR/需求链接可点（rel noopener）；vue-tsc + eslint 通过。</done>
</task>

<task type="auto">
  <name>Task 3: SpecDeliveryPanel vitest 守护（链路渲染 + 降级 + 链接 + zh-CN，LINK-02）</name>
  <files>web/src/components/spec/__tests__/SpecDeliveryPanel.spec.ts</files>
  <action>
    新建 vitest（复用 web/src/components/spec/__tests__/ 既有 mount + 真实 i18n 范式：
    用真实 zh-CN.json 注入 vue-i18n，断言渲染真实中文文案而非裸 key）。用例：(1) 全链路
    fixture（work_item{url,title} + status + implementation_prs 2 项）→ 断言渲染需求标题、
    状态徽标、2 个 PR `<a>` 且 href = pr_url、含 rel="noopener"。(2) 缺 work_item → 断言
    显 specs.delivery.workItemUnlinked 中文占位、不崩溃。(3) implementation_prs 为 []/
    undefined → 断言显 specs.delivery.prsEmpty 占位。(4) work_item 有 title 无 url → 断言
    纯文本 title 不渲染 href。断言用真实 zh-CN 文案文本（验证 i18n 接通，非 mock）。
  </action>
  <verify>
    <automated>cd web && pnpm vitest run src/components/spec/__tests__/SpecDeliveryPanel.spec.ts</automated>
  </verify>
  <done>4 类用例全绿：全链路渲染 / 缺 work_item 降级 / 无 PR 降级 / 链接可点；真实 zh-CN 文案断言通过。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spec detail JSON → 前端渲染（href 注入面） | pr_url / work_item.url 来自后端，作为可点链接 href 渲染 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-06 | Tampering (XSS/注入) | SpecDeliveryPanel 链接渲染 | mitigate | url/pr_url 仅绑定 `:href` 不用 v-html；外链统一 `rel="noopener noreferrer"` + `target="_blank"`；不拼接危险属性 |
| T-52-07 | Denial of Service | 缺数据渲染 | mitigate | 全字段可选链 + 默认值 + 占位降级（fail-soft，Task 3 断言不崩溃） |
| T-52-SC | Tampering | npm/pip/cargo installs | accept | 本 plan 无新增依赖（复用既有组件/i18n/TanStack） |
</threat_model>

<verification>
- 前端：`cd web && pnpm vue-tsc --noEmit && pnpm eslint src/components/spec src/pages/specs && pnpm vitest run src/components/spec/__tests__/SpecDeliveryPanel.spec.ts`
- zh-CN.json JSON 合法且 specs.delivery.* 完整（无裸 key 漏配）
</verification>

<success_criteria>
- spec 详情页展示交付验收追溯面板：WorkItem → spec 状态 → 实现 PR 链路（LINK-02，ROADMAP 成功标准 2）
- WorkItem / PR 链接可点（rel noopener）；缺 work_item / 无 PR 降级占位不崩溃（D-52-5，ROADMAP 成功标准 3）
- 真实 zh-CN.json 文案接通（vitest 断言中文）
- vue-tsc + eslint + vitest 通过
</success_criteria>

<output>
Create `.planning/phases/52-spec-pr/52-03-SUMMARY.md` when done
</output>
