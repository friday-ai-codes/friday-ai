---
phase: 49-produce-spec
plan: 05
type: execute
wave: 4
depends_on: [49-04]
files_modified:
  - server/services/plan_orchestration/architect_merge_adapter.py
  - server/tests/services/test_spec_generation_hookup.py
autonomous: true
requirements: [SPEC-01, SPEC-02]

must_haves:
  truths:
    - "融合通过（_handle_pass）在 EVENT_PLAN_MERGE_COMPLETED emit 之后 best-effort 调 agenerate_specs_for_plan"
    - "spec 生成整段 try/except 吞为 warning sdd_spec_generation_failed，绝不阻断融合返回（merge 仍 passed）"
    - "SDD 仓融合 → 产 SddSpec(draft)+Document(sdd_spec)+DocumentVersion，关联 work_item/plan_version/repository，emit spec.drafted"
    - "非 SDD 仓 / 无 SDD 仓 → 不产 spec，merge 返回 passed 零回归"
    - "spec 合成异常 → fail-soft，merge 仍 passed（warning，不冒泡）"
    - "幂等：同 plan_version+repo 重产不重复建 SddSpec"
  artifacts:
    - path: "server/services/plan_orchestration/architect_merge_adapter.py"
      provides: "_handle_pass 末尾 best-effort spec 生成挂接 + 可注入 spec_generation_hook"
      contains: "sdd_spec_generation_failed"
  key_links:
    - from: "ArchitectMergeAdapter._handle_pass"
      to: "agenerate_specs_for_plan"
      via: "EVENT_PLAN_MERGE_COMPLETED 之后 best-effort 调用"
      pattern: "spec_generation_hook|agenerate_specs_for_plan"
---

<objective>
把 spec 生成挂接到融合通过路径：`ArchitectMergeAdapter._handle_pass` 在 `EVENT_PLAN_MERGE_COMPLETED` emit 之后 best-effort 调 `agenerate_specs_for_plan(version_id)`，整段 try/except 吞为 warning `sdd_spec_generation_failed`，绝不阻断融合/编排返回（D-49-5）。adapter 构造注入可选 `spec_generation_hook`（默认真实实现）便于单测 stub。最后用集成测试覆盖 D-49-7 全部守护场景。

Purpose: SPEC-01/SPEC-02 端到端闭环——融合成功即对 SDD 仓产 spec；同时以零回归 + fail-soft + 幂等守护保证不破坏 v0.8 编排可靠性。
Output: adapter 挂接 + 端到端集成/守护测试。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/49-produce-spec/49-CONTEXT.md

# 挂接点 _handle_pass + 既有 best-effort _emit + 可注入依赖范式
@server/services/plan_orchestration/architect_merge_adapter.py
# adapter 测试范式（mock synthesizer 注入 + 真 PlanSession/PartialPlan 直建 + engine 集成）
@server/tests/services/test_architect_merge_adapter.py
# 49-04 产物（依赖：agenerate_specs_for_plan）
@.planning/phases/49-produce-spec/49-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: _handle_pass 挂接 best-effort spec 生成 + 注入 spec_generation_hook</name>
  <files>server/services/plan_orchestration/architect_merge_adapter.py</files>
  <action>
1. `ArchitectMergeAdapter.__init__` 增可选入参 `spec_generation_hook=None`：默认绑定真实实现——`self.spec_generation_hook = spec_generation_hook or _default_spec_generation_hook`，其中 `_default_spec_generation_hook(version_id)` lazy import `agenerate_specs_for_plan`（防 import 环：spec_generation 已在 plan_orchestration 包内，仍用函数内 import 与既有 clarification_service 延迟构造同理）并 `await` 它。hook 签名：`async def hook(plan_version_id) -> Any`。
2. `_handle_pass`：在现有 `await self._emit(session, EVENT_PLAN_MERGE_COMPLETED, {...})` **之后**、`return {...}` **之前**，加整段 best-effort：
   ```
   try:
       await self.spec_generation_hook(version_id)
   except Exception:  # noqa: BLE001 — spec 生成 best-effort，绝不阻断融合返回
       logger.warning("sdd_spec_generation_failed", session_id=str(session.id), plan_version_id=str(version_id))
   ```
   （以上为说明，执行时写入实际代码；遵循「先记账 ArchitectMerge(passed) + 置 current_plan_version + emit merge.completed，再产 spec」的顺序，spec 生成失败绝不回滚已落 canonical/已 emit 的 merge.completed。）
3. 不改 `merge` / `_handle_fail` 返回契约。`__all__` 无需变更（hook 为内部）。docstring 补一句「融合通过后 best-effort 产 SDD spec（fail-soft，D-49-5）」。
  </action>
  <verify>
    <automated>cd server && uv run python -c "import inspect; from services.plan_orchestration import ArchitectMergeAdapter; assert 'spec_generation_hook' in inspect.signature(ArchitectMergeAdapter.__init__).parameters; print('ok')"</automated>
  </verify>
  <done>_handle_pass 在 merge.completed 之后 best-effort 调 hook，异常吞为 warning sdd_spec_generation_failed；__init__ 可注入 spec_generation_hook；merge/fail 契约不变。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 端到端集成 + 守护测试（全链路 / 零回归 / fail-soft / 幂等）</name>
  <files>server/tests/services/test_spec_generation_hookup.py</files>
  <behavior>
    - **全链路（SDD 仓）**：mock MergedPlanSynthesizer 产含 SDD 仓的 valid MergedPlan + 注入产固定 markdown 的 spec hook（或真 agenerate + mock SddSpecSynthesizer）。跑 adapter.merge → 返回 validation_status=='passed'；DB 有 SddSpec(draft) + Document(sdd_spec, internal_generated) + DocumentVersion；SddSpec 关联 work_item/plan_version/repository 正确；PlanSessionEvent 有 spec.drafted。
    - **零回归（非 SDD / 无 SDD 仓）**：execution_plan 仓均非 SDD → merge 返回 passed、零 SddSpec、零 Document(sdd_spec)；ArchitectMerge(passed) 仍落、current_plan_version 仍置（与 v0.8 完全一致）。
    - **fail-soft（合成异常）**：注入抛异常的 spec_generation_hook → merge 仍返回 passed；ArchitectMerge passed 仍落；无 SddSpec；warning 不冒泡（用 caplog 断言 sdd_spec_generation_failed，且 merge 返回正常）。
    - **幂等（端到端重产）**：对同 session 同批 partial 跑两次 merge（或对同 plan_version 调两次 hook）→ 同 (plan_version, repository) SddSpec 只一条（不重复建）。
  </behavior>
  <action>
新建 `@pytest.mark.django_db` async 测试。复用 test_architect_merge_adapter.py 的 `_make_repo` / valid MergedPlan 构造，但需令 execution_plan 的 repository_id 指向真实建的 Repository（设/不设 facets.methodology="SDD" 区分用例）。注入 mock MergedPlanSynthesizer（产 valid MergedPlan）+ 受控 spec 路径（推荐注入 `spec_generation_hook` stub 直接验证「hook 被调 / 异常被吞」；另对真链路用例注入真 `agenerate_specs_for_plan` 但 mock 内部 `SddSpecSynthesizer` 经 spec_service/synthesizer 入参——按 49-04 的 agenerate_specs_for_plan 入参形态在 hook 内闭包构造）。建 PlanSession + RepoResearchTask + valid PartialPlan 使 merge 走 pass 分支（参考既有 adapter 测试建数据）。caplog 断言 fail-soft warning。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/test_spec_generation_hookup.py tests/services/test_architect_merge_adapter.py -x -q</automated>
  </verify>
  <done>全链路/零回归/fail-soft/幂等四类断言全绿；既有 adapter 测试无回归。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spec 生成 → 编排返回 | spec 生成（半可信 LLM + 多次 DB 写）挂在融合关键路径末尾，须绝对 fail-soft |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-10 | Denial of Service | spec 生成异常阻断融合/编排 | mitigate | _handle_pass 整段 try/except 吞 warning sdd_spec_generation_failed；merge 返回不受影响（fail-soft 守护测试断言） |
| T-49-11 | Integrity | spec 生成失败回滚已落 canonical | mitigate | 顺序：先记账 ArchitectMerge(passed)+置指针+emit merge.completed，再产 spec；spec 失败不触碰 canonical |
| T-49-12 | Tampering | 旁路写 SddSpec/Document | mitigate | 全程经 SddSpecService/DocumentService（49-03/49-02 grep 守护已覆盖） |
</threat_model>

<verification>
- 集成/守护测试 + 既有 adapter 测试全绿。
- 全 phase 后端关卡：`uv run ruff check .` + `uv run pytest tests/delivery tests/services -q` + `uv run python manage.py makemigrations --check --dry-run`。
</verification>

<success_criteria>
- SDD 仓融合 → 全链路产 spec + 关联 + emit（SPEC-01/SPEC-02）。
- 非 SDD/无 SDD/合成异常 → 零回归，merge 仍 passed（fail-soft）。
- 幂等：重产不重复建 SddSpec。
- spec 生成绝不阻断融合/编排返回。
</success_criteria>

<output>
Create `.planning/phases/49-produce-spec/49-05-SUMMARY.md` when done
</output>
