---
phase: 16-expo
plan: 03
type: execute
wave: 2
depends_on: [16-01]
files_modified:
  - server/workflows/nodes/ai/delivery_knowledge_search.py
  - server/workflows/nodes/ai/plan_generation.py
  - server/workflows/nodes/ai/__init__.py
  - web/src/types/workflow/schemas.ts
  - web/src/types/workflow/registry.ts
  - web/src/components/workflow/config/DeliveryKnowledgeSearchConfig.vue
  - web/src/components/workflow/editor/nodes/ActionNode.vue
  - web/src/components/workflow/sidebar/NodePalette.vue
  - server/tests/workflows/test_delivery_knowledge_search_node.py
autonomous: true
requirements: [EXPO-02, ENH-04]
must_haves:
  truths:
    - "workflow 编辑器可拖入 delivery_knowledge_search 节点并配置 top_k/project_ids/as_of"
    - "节点执行调用 search_similar，输出 formatted_context markdown + 结构化 results"
    - "检索失败或空结果降级为空上下文，节点 status=success 不阻塞 DAG（per CONTEXT）"
    - "ai_plan_generation 在 execute 前自动 search_similar 注入「相似历史交付」段落到 user prompt（飞轮）"
    - "方案生成节点 config 可选 auto_inject_similar_history（默认 true）与 similar_history_top_k"
  artifacts:
    - path: "server/workflows/nodes/ai/delivery_knowledge_search.py"
      provides: "DeliveryKnowledgeSearchNode BaseNode"
    - path: "server/workflows/nodes/ai/plan_generation.py"
      provides: "execute() 内自动检索 hook"
  key_links:
    - from: "server/workflows/nodes/ai/delivery_knowledge_search.py"
      to: "server/knowledge/retrieval.py"
      via: "search_similar(user=workflow owner)"
      pattern: "search_similar"
    - from: "server/workflows/nodes/ai/plan_generation.py"
      to: "server/knowledge/exposure.py"
      via: "format_search_results_markdown"
      pattern: "format_search_results_markdown"
---

<objective>
交付 EXPO-02：独立 workflow 检索节点 + ai_plan_generation 自动消费历史飞轮；ENH-04 as_of 在节点 config 透传。

Purpose: 方案生成前自动检索相似历史并注入上下文，形成需求→方案→代码→再需求的闭环。
Output: DeliveryKnowledgeSearchNode、plan_generation hook、前端节点注册、节点测试。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-clean/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-clean/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-expo/16-CONTEXT.md
@.planning/phases/16-expo/16-01-SUMMARY.md
@server/workflows/nodes/ai/context_retrieval.py
@server/workflows/nodes/ai/plan_generation.py
@web/src/types/workflow/registry.ts
</context>

## Multi-Source Coverage Audit

| Source | Item | Plan |
|--------|------|------|
| REQ | EXPO-02 | 16-03 |
| REQ | ENH-04 (workflow) | 16-03 |
| CONTEXT | delivery_knowledge_search 节点 + ai_plan_generation hook | 16-03 |
| CONTEXT | 失败降级空上下文 | 16-03 |
| GOAL | 方案生成自动消费历史飞轮 | 16-03 |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DeliveryKnowledgeSearchNode 实现</name>
  <files>server/workflows/nodes/ai/delivery_knowledge_search.py, server/workflows/nodes/ai/__init__.py, server/tests/workflows/test_delivery_knowledge_search_node.py</files>
  <read_first>
    - server/workflows/nodes/ai/context_retrieval.py（节点结构、config_schema、markdown 输出）
    - server/workflows/nodes/base.py（ExecutionContext、NodeResult）
    - server/workflows/engine/scheduler.py（workflow user 解析 grep _get_workflow_user 或 project owner）
  </read_first>
  <behavior>
    - Test 1：mock search_similar 返回 2 条 → output formatted_context 非空 + results len=2
    - Test 2：search_similar 抛异常 → NodeResult status=success + 空 formatted_context（降级 per D-WF）
    - Test 3：config as_of 合法 ISO → 传给 service.search_similar(as_of=...)
    - Test 4：无 query 模板渲染结果 → failed 或空查询短路（与 context_retrieval 一致）
  </behavior>
  <action>
    新建 DeliveryKnowledgeSearchNode（node_type=delivery_knowledge_search, category=AI, icon=search）：
    - config_schema：query(模板必填), top_k(default 5), project_ids(optional list), repository_ids(optional), entity_kinds(optional), as_of(optional ISO string), include_superseded(bool)
    - execute：解析 workflow 执行用户（照 context_retrieval 或 plan_generation._get_project 同路径取 User）；render query；parse_as_of；调 DeliveryKnowledgeSearchService.search_similar；exposure.format_search_results_markdown + serialize_search_results 写入 output {query, total, formatted_context, results}
    - 异常捕获 log knowledge_workflow_search_failed + 返回空上下文 success
    @register_node + __init__.py import
    测试文件 test_delivery_knowledge_search_node.py，方法前缀 test_delivery_node_*。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/workflows/test_delivery_knowledge_search_node.py -x -q</automated>
  </verify>
  <done>节点可单测执行；失败降级不阻塞。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ai_plan_generation hook + 前端节点注册与配置 UI</name>
  <files>server/workflows/nodes/ai/plan_generation.py, web/src/types/workflow/schemas.ts, web/src/types/workflow/registry.ts, web/src/components/workflow/config/DeliveryKnowledgeSearchConfig.vue, web/src/components/workflow/editor/nodes/ActionNode.vue, web/src/components/workflow/sidebar/NodePalette.vue, server/tests/workflows/test_delivery_knowledge_search_node.py</files>
  <behavior>
    - Test 1：plan_generation execute mock search_similar → get_user_prompt 含「相似历史交付」
    - Test 2：auto_inject_similar_history=false → 不调用 search_similar
    - Test 3：search 失败 → plan_generation 仍 success 启动 agent（不 fail 节点）
    - Test 4（frontend）：registry.ts 含 delivery_knowledge_search 条目（rg 门禁）
  </behavior>
  <action>
    plan_generation.py hook（per EXPO-02 飞轮 / CONTEXT specifics）：
    - node_config 新增 auto_inject_similar_history(bool default True), similar_history_top_k(int default 5), similar_history_as_of(optional)
    - 在 execute() 预渲染 base prompt 之后、super().execute 之前：若 auto_inject 且 user_prompt 非空，以 rendered user_prompt 为 query 调 search_similar（project scope 从 _get_project）；结果 format_search_results_markdown 存入 self._similar_history_markdown
    - 覆盖 get_user_prompt：在 upstream_context 之后追加 self._similar_history_markdown（空则跳过）
    - 异常仅 log，不 fail 节点
    前端（照 context_retrieval 范式，per plan-checker）：
    - schemas.ts 增加 deliveryKnowledgeSearchConfigSchema（query/top_k/project_ids/as_of/include_superseded）
    - registry.ts 注册 delivery_knowledge_search + configComponent → DeliveryKnowledgeSearchConfig.vue
    - 新建 DeliveryKnowledgeSearchConfig.vue（照 ContextRetrievalConfig.vue：query 模板、top_k、project 多选、as_of datetime-local、include_superseded）
    - ActionNode.vue icon 映射；NodePalette.vue AI 分组添加入口
    扩展 test 文件覆盖 plan_generation hook。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/workflows/test_delivery_knowledge_search_node.py -k plan_generation -x -q && test -f web/src/components/workflow/config/DeliveryKnowledgeSearchConfig.vue && rg "DeliveryKnowledgeSearchConfig" web/src/types/workflow/registry.ts | wc -l | awk '$1>=1'</automated>
  </verify>
  <done>方案生成自动注入历史；编辑器可见新节点。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Workflow config templates | 不可信文本；仅作 query 不入 SQL |
| Node → service | user 为 workflow 执行主体 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-07 | Information Disclosure | workflow search | mitigate | service scope filter |
| T-16-08 | Denial of Service | auto inject | mitigate | top_k 上限 + 异常降级 |
| T-16-SC | Tampering | npm/pip | accept | 无新后端依赖 |
</threat_model>

<verification>
```bash
cd server && uv run pytest tests/workflows/test_delivery_knowledge_search_node.py -q
cd web && pnpm exec tsx scripts/validate-node-definitions.ts
```
</verification>

<success_criteria>
- 独立节点 + plan_generation hook 均注入 markdown 历史段
- 失败降级；as_of config 透传
- 前端 registry 含 delivery_knowledge_search
</success_criteria>

<output>
Create `.planning/phases/16-expo/16-03-SUMMARY.md` when done
</output>
