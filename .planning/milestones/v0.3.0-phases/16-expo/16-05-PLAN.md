---
phase: 16-expo
plan: 05
type: execute
wave: 3
depends_on: [16-01]
files_modified:
  - server/knowledge/api/views.py
  - server/knowledge/api/urls.py
  - server/tests/knowledge/test_knowledge_api.py
  - web/src/api/knowledge.ts
  - web/src/api/index.ts
  - web/src/pages/knowledge/entities/[id].vue
  - web/src/components/knowledge/EntityDetailToolbar.vue
  - web/src/components/knowledge/EntityMetadataCard.vue
  - web/src/components/knowledge/EntityVersionTimeline.vue
  - web/src/components/knowledge/EntityRelationTree.vue
  - web/src/components/knowledge/EntityKindBadge.vue
  - web/src/components/knowledge/ProvenanceLinkButton.vue
  - web/src/components/layout/AppSidebar.vue
  - web/src/locales/zh-CN.json
  - web/src/pages/knowledge/__tests__/entity-detail.spec.ts
autonomous: true
requirements: [ENH-03, ENH-04]
must_haves:
  truths:
    - "GET /api/knowledge/entities/:id/ 返回 EntityMetadata（含 as_of query）"
    - "GET timeline/related/search 经 JWT 返回完整 DTO + as_of 透传"
    - "用户可访问 /knowledge/entities/:id 只读详情（AnchorNavLayout 三区块）"
    - "EntityVersionTimeline 树/列表展示 需求→方案→代码变更链"
    - "EntityDetailToolbar 历史时点选择器变更后三 query invalidate"
    - "越权实体 REST 404 或空 timeline（不泄露标题）"
  artifacts:
    - path: "web/src/pages/knowledge/entities/[id].vue"
      provides: "只读实体详情页壳"
    - path: "server/knowledge/api/views.py"
      provides: "KnowledgeEntityDetailView + 完整 DTO REST"
    - path: "web/src/api/knowledge.ts"
      provides: "getEntity/getTimeline/getRelated/search"
  key_links:
    - from: "web/src/pages/knowledge/entities/[id].vue"
      to: "/api/knowledge/entities/:id/"
      via: "knowledgeApi.getEntity"
      pattern: "getEntity"
    - from: "server/knowledge/api/views.py"
      to: "server/knowledge/exposure.py"
      via: "serialize_*"
      pattern: "serialize_timeline"
---

<objective>
交付 ENH-03 前端只读实体详情（对齐 16-UI-SPEC.md）+ ENH-04 REST/前端 as_of；扩展 entity detail REST 端点。

Purpose: 控制台用户浏览交付知识图谱、版本历史与关联链，与 MCP/chat/workflow 共享 service。
Output: 详情页组件族、JWT REST 三 query + entity、vitest、越权测试。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-clean/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-clean/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-expo/16-CONTEXT.md
@.planning/phases/16-expo/16-UI-SPEC.md
@.planning/phases/16-expo/16-01-SUMMARY.md
@server/knowledge/api/views.py
@server/knowledge/metadata_hydrate.py
@web/src/components/layout/AnchorNavLayout.vue
</context>

## Multi-Source Coverage Audit

| Source | Item | Plan |
|--------|------|------|
| REQ | ENH-03 | 16-05 |
| REQ | ENH-04 (REST + UI) | 16-05 |
| UI-SPEC | entities/:id + 三 query + 组件拆分 | 16-05 |
| CONTEXT | TanStack Query + 只读 | 16-05 |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: REST entity detail + 完整 DTO + as_of 全端点</name>
  <files>server/knowledge/api/views.py, server/knowledge/api/urls.py, server/tests/knowledge/test_knowledge_api.py</files>
  <behavior>
    - Test 1：GET /api/knowledge/entities/{id}/ → 200 + entity_id/kind/title/provenance
    - Test 2：GET entities/{id}/?as_of=... mock 断言 service/hydrate 收到 as_of
    - Test 3：GET timeline/{id}/?as_of=... 透传 get_timeline(as_of=...)
    - Test 4：other_user GET owner entity → 404 或 403（钉死一种 fail-closed）
    - Test 5：非法 as_of → 400
  </behavior>
  <action>
    新建 KnowledgeEntityDetailView（per 16-UI-SPEC）：
    - GET entities/&lt;uuid&gt;/：hydrate 当前或 as_of 时点最新可见版本 metadata；越权 → 404（不返回 B 项目标题）
    - 复用 exposure.serialize 单实体 helper（可在 exposure.py 增 serialize_entity_metadata）
    urls.py 增加 path entities/&lt;uuid&gt;/
    升级既有 search/timeline/related views：完整 DTO 输出 + query param as_of（timeline 依赖 16-01 get_timeline as_of）
    新建 test_knowledge_api.py（或扩展 test_delivery_search.py）覆盖 entity + as_of + 越权。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/knowledge/test_knowledge_api.py -x -q</automated>
  </verify>
  <done>JWT REST 符合 UI-SPEC 三 query + entity 端点。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: knowledge.ts + UI-SPEC 组件族</name>
  <files>web/src/api/knowledge.ts, web/src/api/index.ts, web/src/components/knowledge/EntityKindBadge.vue, web/src/components/knowledge/ProvenanceLinkButton.vue, web/src/components/knowledge/EntityMetadataCard.vue, web/src/components/knowledge/EntityVersionTimeline.vue, web/src/components/knowledge/EntityRelationTree.vue, web/src/components/knowledge/EntityDetailToolbar.vue, web/src/locales/zh-CN.json</files>
  <behavior>
    - Test 1：EntityKindBadge 渲染 work_item 中文标签
    - Test 2：EntityVersionTimeline 渲染 2 节点 + data-testid timeline-node
    - Test 3：EntityRelationTree 树形缩进 depth=1 子节点可见
  </behavior>
  <action>
    实现 16-UI-SPEC.md 组件表（per ENH-03）：
    - knowledge.ts：getEntity(id, {asOf}), getTimeline, getRelated, searchDeliveryKnowledge；类型 mirror exposure JSON
    - EntityKindBadge / ProvenanceLinkButton（飞书/MR/session 外链，target=_blank rel=noopener）
    - EntityMetadataCard：kind/version/title/valid_at/provenance
    - EntityVersionTimeline：垂直时间线 + 嵌套 code_change（Collapsible 或 SubStepTimeline 风格）
    - EntityRelationTree：work_item→tech_plan→code_change 层级，无子节点时扁平列表回退
    - EntityDetailToolbar：as-of datetime 输入 + include_superseded checkbox + 重置
    zh-CN.json 添加 knowledge.* 文案键。
    vitest 放 components/knowledge/__tests__/ 或各组件旁 spec。
  </action>
  <verify>
    <automated>cd web && pnpm exec vitest run src/components/knowledge --passWithNoTests</automated>
  </verify>
  <done>可复用组件族与 typed API client 就绪。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 详情页编排 + 侧栏导航 + E2E vitest</name>
  <files>web/src/pages/knowledge/entities/[id].vue, web/src/components/layout/AppSidebar.vue, web/src/pages/knowledge/__tests__/entity-detail.spec.ts</files>
  <behavior>
    - Test 1：mount 详情页 mock 三 API → 见 metadata 标题与 toolbar as-of 控件
    - Test 2：mock 404 entity → CompactEmptyState 友好态
    - Test 3：变更 asOf → useQuery invalidate（spy invalidateQueries）
  </behavior>
  <action>
    pages/knowledge/entities/[id].vue：
    - PageContainer + AnchorNavLayout 三锚点（metadata/timeline/related）per UI-SPEC
    - 并行 useQuery 三接口；staleTime 30_000；as_of/superseded 变更 invalidate 全部
    - useHead 动态标题
    AppSidebar mainNavItems 增加「交付知识」入口（链到说明页或搜索入口；实体页经关联链接进入）
    entity-detail.spec.ts 覆盖加载/空/越权 mock。
  </action>
  <verify>
    <automated>cd web && pnpm exec vitest run src/pages/knowledge/__tests__/entity-detail.spec.ts -x</automated>
  </verify>
  <done>只读详情页可浏览；as-of 驱动三 query 刷新。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → JWT REST | IsAuthenticated + service scope |
| 详情页 | 只读，无 mutating API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-10 | Information Disclosure | entity detail | mitigate | 404 fail-closed + REST 越权测试 |
| T-16-11 | XSS | provenance links | mitigate | href 仅来自 API；rel=noopener |
| T-16-SC | Tampering | npm | accept | 无新依赖 |
</threat_model>

<verification>
```bash
cd server && uv run pytest tests/knowledge/test_knowledge_api.py -q
cd web && pnpm exec vitest run src/pages/knowledge src/components/knowledge --passWithNoTests
```
</verification>

<success_criteria>
- UI-SPEC 三 query + entity 端点可用
- /knowledge/entities/:id 只读详情 + as-of 工具栏
- 越权不泄露实体 metadata
</success_criteria>

<output>
Create `.planning/phases/16-expo/16-05-SUMMARY.md` when done
</output>
