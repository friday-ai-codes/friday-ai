---
phase: 16-expo
plan: 02
type: execute
wave: 2
depends_on: [16-01]
files_modified:
  - server/agents/tools/delivery_knowledge_tools.py
  - server/agents/tools/schemas/delivery_knowledge.py
  - server/agents/tools/__init__.py
  - server/tests/agents/tools/test_delivery_knowledge_tools.py
autonomous: true
requirements: [EXPO-03, ENH-04]
must_haves:
  truths:
    - "chat agent 可调用 search_delivery_knowledge / get_entity_timeline / get_related_entities 三工具"
    - "工具以会话 owner 为 user principal 调 DeliveryKnowledgeSearchService（fail-closed）"
    - "工具描述为中文；返回含 provenance metadata"
    - "可选 as_of 参数透传 service（ENH-04 chat 入口）"
    - "langchain_adapter.build_langchain_tools 可装配三工具名"
  artifacts:
    - path: "server/agents/tools/delivery_knowledge_tools.py"
      provides: "三 @tool 函数注册 ToolRegistry"
      exports: ["search_delivery_knowledge", "get_entity_timeline", "get_related_entities"]
    - path: "server/agents/tools/schemas/delivery_knowledge.py"
      provides: "Pydantic input/output 契约"
  key_links:
    - from: "server/agents/tools/delivery_knowledge_tools.py"
      to: "server/knowledge/retrieval.py"
      via: "DeliveryKnowledgeSearchService"
      pattern: "DeliveryKnowledgeSearchService"
    - from: "server/agents/tools/delivery_knowledge_tools.py"
      to: "server/knowledge/exposure.py"
      via: "serialize_* helpers"
      pattern: "from knowledge.exposure import"
---

<objective>
交付 EXPO-03 chat agent tools：三工具注册 + langchain 可装配，复用 16-01 exposure 序列化；ENH-04 as_of 在 chat 入口透传。

Purpose: 控制台对话可问「以前做过类似需求吗」并得到带出处回答。
Output: delivery_knowledge_tools.py、schemas、__init__ 注册、单元/集成测试。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-clean/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-clean/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-expo/16-CONTEXT.md
@.planning/phases/16-expo/16-01-SUMMARY.md
@server/agents/tools/find_related_code.py
@server/agents/tools/base.py
@server/agents/tools/langchain_adapter.py
@server/knowledge/exposure.py
</context>

## Multi-Source Coverage Audit

| Source | Item | Plan |
|--------|------|------|
| REQ | EXPO-03 | 16-02 |
| REQ | ENH-04 (chat) | 16-02 |
| CONTEXT | agents/tools/ + 中文描述 + provenance | 16-02 |
| CONTEXT | 会话 owner 作 user principal | 16-02 |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pydantic schemas + ToolResult 契约</name>
  <files>server/agents/tools/schemas/delivery_knowledge.py, server/tests/agents/tools/test_delivery_knowledge_tools.py</files>
  <behavior>
    - Test 1：SearchDeliveryKnowledgeInput 缺 query → ValidationError
    - Test 2：SearchDeliveryKnowledgeOutput results 元素含 provenance 嵌套 dict
    - Test 3：as_of 可选字符串字段存在且默认 None
  </behavior>
  <action>
    新建 schemas/delivery_knowledge.py（照 find_related_code schemas 布局）：
    - SearchDeliveryKnowledgeInput/Output
    - GetEntityTimelineInput/Output（含 as_of）
    - GetRelatedEntitiesInput/Output（含 direction/max_hops/as_of）
    Output 嵌套 provenance: {feishu_url, mr_url, session_link}。
    在 test_delivery_knowledge_tools.py 先写 schema 契约测试（RED）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/agents/tools/test_delivery_knowledge_tools.py -k schema -x -q</automated>
  </verify>
  <done>Schema 测试 RED→GREEN 就绪。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 三 @tool 实现 + ToolRegistry 注册</name>
  <files>server/agents/tools/delivery_knowledge_tools.py, server/agents/tools/__init__.py</files>
  <read_first>
    - server/agents/tools/find_related_code.py（@tool + ToolResult 模式）
    - server/chat/conversation_service.py 或 chat_runner（user 注入方式 grep conversation owner）
  </read_first>
  <behavior>
    - Test 1：search_delivery_knowledge mock service → ToolResult success + results 列表
    - Test 2：无 user（injected user_id 缺失）→ ToolResult success=False fail-closed
    - Test 3：as_of 非法 → structured error 不抛异常
    - Test 4：ToolRegistry.get("search_delivery_knowledge") 非 None
    - Test 5：build_langchain_tools(["search_delivery_knowledge"], injected_values={...}) 返回 1 个 BaseTool
    - Test 6：other_user 会话 owner + 不可见 project_ids → search 空 results（fail-closed，per RETR-07）
  </behavior>
  <action>
    实现 delivery_knowledge_tools.py（per EXPO-03 / CONTEXT D-CHAT）：
    - 中文 _TOOL_DESCRIPTION（含 USE WHEN / DO NOT USE 决策树：自然语言相似需求→本工具；纯代码图遍历→find_related_code）
    - 通过 tool 闭包或 injected `user_id`/`conversation_id` 解析 User ORM（照既有 chat tools 的 owner 解析 helper，grep `get_conversation_owner` 或等效）
    - 调 DeliveryKnowledgeSearchService + knowledge.exposure.serialize_*
    - parse_as_of 失败 → ToolResult(success=False, error="as_of 格式无效...")
    在 __init__.py 追加 import 触发注册（与 find_related_code 同模式）。
    越权：other_user 调 search + project_ids 不可见 → 空 results（复用 test_cross_project_denied 思路）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/agents/tools/test_delivery_knowledge_tools.py -x -q</automated>
  </verify>
  <done>三工具注册且测试全绿；langchain 可装配。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM → chat tools | 不可信参数；须 schema 校验 |
| tool → service | user 必须来自会话 owner，禁止信任 LLM 传的 user_id |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-05 | Elevation of Privilege | user principal | mitigate | 只从 conversation owner 解析 user |
| T-16-06 | Information Disclosure | search tool | mitigate | 委托 service fail-closed scope |
| T-16-SC | Tampering | pip installs | accept | 无新依赖 |
</threat_model>

<verification>
```bash
cd server && uv run pytest tests/agents/tools/test_delivery_knowledge_tools.py -q
```
</verification>

<success_criteria>
- ToolRegistry 含三工具名
- 中文 description；返回 provenance
- as_of 透传/校验符合 ENH-04
</success_criteria>

<output>
Create `.planning/phases/16-expo/16-02-SUMMARY.md` when done
</output>
