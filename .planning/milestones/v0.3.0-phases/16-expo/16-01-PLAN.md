---
phase: 16-expo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/knowledge/exposure.py
  - server/knowledge/timeline.py
  - server/knowledge/retrieval.py
  - server/mcp_tools/serializers.py
  - server/mcp_tools/views.py
  - server/mcp_tools/urls.py
  - server/tests/knowledge/test_timeline.py
  - server/tests/mcp_tools/test_delivery_knowledge_tools.py
  - server/tests/mcp_tools/test_schema_snapshot.py
autonomous: true
requirements: [EXPO-01, ENH-04]
must_haves:
  truths:
    - "POST /api/mcp/tools/search_delivery_knowledge/ 经 PAT 认证返回结构化检索结果（含 provenance + 可选 llm_grade）"
    - "POST /api/mcp/tools/get_entity_timeline/ 与 get_related_entities/ 经 PAT 可用且写入 interactions 审计链"
    - "三个工具注册进 TOOL_SCHEMA_SNAPSHOT，与既有 19 工具同体系（总计 22）"
    - "A 用户 PAT 查 B 项目 project_ids → 空 results（越权 fail-closed）"
    - "search / timeline / related 三工具均可选 as_of ISO8601 透传 service 层（ENH-04 MCP 入口）"
  artifacts:
    - path: "server/knowledge/exposure.py"
      provides: "DTO 序列化 + parse_as_of + markdown 格式化共享出口"
      exports: ["parse_as_of", "serialize_search_results", "serialize_timeline", "serialize_related", "format_search_results_markdown"]
    - path: "server/mcp_tools/views.py"
      provides: "SearchDeliveryKnowledgeView / GetEntityTimelineView / GetRelatedEntitiesView"
    - path: "server/tests/mcp_tools/test_delivery_knowledge_tools.py"
      provides: "PAT 成功路径 + 越权空结果 + as_of 400 校验"
  key_links:
    - from: "server/mcp_tools/views.py"
      to: "server/knowledge/retrieval.py"
      via: "DeliveryKnowledgeSearchService + request.user"
      pattern: "DeliveryKnowledgeSearchService"
    - from: "server/mcp_tools/views.py"
      to: "interactions/ledger.py"
      via: "begin_interaction_run + _record tool_call"
      pattern: "begin_interaction_run"
---

<objective>
交付 EXPO-01 MCP HTTP 三工具 + ENH-04 的 MCP as_of 透传；建立 `knowledge/exposure.py` 供 chat/workflow/REST 复用。

Purpose: 外部 agent 经 PAT 查询交付知识，审计与既有 MCP 工具一致；共享序列化避免四入口漂移。
Output: exposure.py、三个 McpToolView、urls、schema snapshot 更新、越权测试。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-clean/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-clean/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-expo/16-CONTEXT.md
@.planning/ROADMAP.md
@server/knowledge/retrieval.py
@server/knowledge/retrieval_types.py
@server/mcp_tools/views.py
@server/mcp_tools/serializers.py
@server/tests/mcp_tools/conftest.py
@server/tests/knowledge/test_delivery_search.py
</context>

## 规划定案（供 verify 对照）

- **工具名（locked）**：`search_delivery_knowledge` / `get_entity_timeline` / `get_related_entities`。
- **认证（locked）**：`McpToolView` + `AccessTokenAuthentication`；无 token → 401 fail-closed。
- **DTO（locked）**：响应字段对齐 `retrieval_types.py`（entity_id/kind/title/version/score/provenance/llm_grade/llm_reason）；禁止裸 dict 手搓字段名。
- **as_of（ENH-04，locked）**：三 MCP 工具请求体均可选 `as_of`（ISO8601）；`parse_as_of` 拒绝 naive/非法 → 400。本 plan 扩展 `build_entity_timeline` / `get_timeline(as_of=...)`：版本与 IMPLEMENTED_BY 边按 bi-temporal 过滤（照 `graph_store` / `test_related.py` as_of 先例）。
- **越权**：service 层 `resolve_allowed_project_ids` 已 fail-closed；MCP 集成测试必须播种 A/B 用户 + PAT。

## Multi-Source Coverage Audit

| Source | Item | Plan |
|--------|------|------|
| GOAL | MCP PAT 入口 + fail-closed | 16-01 |
| REQ | EXPO-01 | 16-01 |
| REQ | ENH-04 (MCP as_of) | 16-01 |
| CONTEXT | 三 endpoint + 审计 + 越权测试 | 16-01 |
| CONTEXT | serializer 对齐 Phase 15 DTO | 16-01 |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: exposure 序列化 + timeline as_of service 扩展</name>
  <files>server/knowledge/exposure.py, server/knowledge/timeline.py, server/knowledge/retrieval.py, server/tests/knowledge/test_exposure.py, server/tests/knowledge/test_timeline.py</files>
  <read_first>
    - server/knowledge/retrieval_types.py（EntityMetadata / SearchResultDTO / TimelineNodeDTO / RelatedEntityDTO）
    - server/mcp_tools/views.py（_jsonable 先例）
  </read_first>
  <behavior>
    - Test 1：parse_as_of("2026-05-01T00:00:00+08:00") → timezone-aware datetime
    - Test 2：parse_as_of("not-a-date") → raises ValueError 或返回可被 view 转 400 的明确错误
    - Test 3：serialize_search_results 单条 DTO → dict 含 entity_id/kind/title/score/provenance/feishu_url|mr_url 键
    - Test 4：format_search_results_markdown 非空列表 → 含标题与 provenance 的 markdown 段落
    - Test 5：as_of 在边 valid_at 之前 → timeline 不含该 code_change（ENH-04）
    - Test 6：get_timeline(..., as_of=dt) 透传 build_entity_timeline
  </behavior>
  <action>
    新建 server/knowledge/exposure.py（per CONTEXT D-MCP serializer 对齐 DTO）：
    - parse_as_of(raw: str | None) -> datetime | None：None 直通；非空须 ISO8601 且 timezone-aware（django.utils.dateparse + timezone 校验，拒绝 naive）。
    - serialize_search_result / serialize_search_results：list[SearchResultDTO] → JSONable list（provenance 展开为嵌套 dict，llm_grade/llm_reason 原样）。
    - serialize_timeline(nodes: list[TimelineNodeDTO])：含 version/kind/title/summary/event_time/provenance/code_changes 摘要。
    - serialize_related(related: list[RelatedEntityDTO])：含 relation/depth/metadata。
    - format_search_results_markdown(results, *, as_of=None)：中文标题「## 相似历史交付」+ 每条 bullet（title/kind/score/llm_grade/出处链接）；供 workflow/chat 注入，不在此 task 接线。
    同 task 扩展 build_entity_timeline / get_timeline(as_of=...)（per ENH-04，照 related.py/graph_store bi-temporal 谓词）。
    新建 test_exposure.py + 扩展 test_timeline.py（test_timeline_as_of_*）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/knowledge/test_exposure.py tests/knowledge/test_timeline.py -k "as_of or exposure" -x -q</automated>
  </verify>
  <done>exposure + timeline as_of 地基就绪。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MCP serializers + 三 View + urls + schema snapshot</name>
  <files>server/mcp_tools/serializers.py, server/mcp_tools/views.py, server/mcp_tools/urls.py, server/tests/mcp_tools/test_schema_snapshot.py</files>
  <behavior>
    - Test 1（schema）：TOOL_SCHEMA_SNAPSHOT 新增三键且 request/response 字段列表与 serializer 一致
    - Test 2（grep gate）：urls.py 含三条 path；views 含三个 class 且 tool_name 精确匹配
  </behavior>
  <action>
    在 serializers.py 新增（per EXPO-01 / ENH-04）：
    - SearchDeliveryKnowledgeRequestSerializer：query(required), top_k(1-20 default 5), project_ids, repository_ids, entity_kinds, as_of(optional ISO8601 string), include_superseded(bool)
    - GetEntityTimelineRequestSerializer：entity_id(UUID), include_superseded, as_of(optional)
    - GetRelatedEntitiesRequestSerializer：entity_id, direction(enum both/out/in), max_hops(1-3), as_of(optional)
    在 views.py 新增三个 McpToolView 子类，模式照 SearchLearningCasesView：
    - 实例化 DeliveryKnowledgeSearchService（模块级单例即可）
    - _begin → _validate → 调 service（user=request.user）→ exposure 序列化 → _record_agent_decision + _record（traces 用 retrieval 类 kind，payload 含 entity_id/score）
    - search 输出：{query, results, total, as_of, run_id}
    - timeline 输出：{entity_id, nodes, total, run_id}
    - related 输出：{entity_id, related, total, as_of, run_id}
    urls.py 注册三条 path；更新 TOOL_SCHEMA_SNAPSHOT 三条目；更新 test_schema_snapshot.py 期望 dict（22 工具）。
    OpenAPI：extend_schema tags 使用既有 mcp 工具 tag 惯例。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/mcp_tools/test_schema_snapshot.py -x -q && rg -c "search_delivery_knowledge|get_entity_timeline|get_related_entities" server/mcp_tools/urls.py | awk -F: '$2>=3'</automated>
  </verify>
  <done>三 MCP endpoint 可导入；schema snapshot 测试通过。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: PAT 集成测试 + 越权空结果 + as_of 校验</name>
  <files>server/tests/mcp_tools/test_delivery_knowledge_tools.py</files>
  <read_first>
    - server/tests/mcp_tools/conftest.py（PAT client 工厂）
    - server/tests/knowledge/test_delivery_search.py::test_cross_project_denied（越权先例）
    - server/tests/mcp_tools/test_search_rag_chunks.py（MCP POST 模式）
  </read_first>
  <behavior>
    - Test 1：合法 PAT + mock recall_similar_chunks 返回空 → search 200 + results=[] + run_id
    - Test 2：user_a PAT + project_ids=user_b_project → results=[]（非 403，fail-closed 空结果 per RETR-07）
    - Test 3：as_of="invalid" → 400
    - Test 4：get_entity_timeline 未知 entity → 200 nodes=[] 或 404（与 service 行为一致，测试钉死一种）
    - Test 5：POST 无 Authorization → 401
  </behavior>
  <action>
    新建 test_delivery_knowledge_tools.py（per CONTEXT「越权用例必须进 CI」）：
    - 复用 conftest 的 mcp_authenticated_client / access_token 工厂
    - monkeypatch knowledge.retrieval.recall_similar_chunks 避免 Qdrant
    - 播种 project + membership：owner vs other_user
    - 断言 InteractionRun/ToolCallRecord 创建（至少 search 一条）
    方法名前缀 test_delivery_* 供 -k delivery 精确选中。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/mcp_tools/test_delivery_knowledge_tools.py -x -q</automated>
  </verify>
  <done>越权与 PAT 路径 CI 可重复；as_of 非法输入 fail-closed。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External agent → MCP HTTP | 不可信 PAT；必须经 AccessTokenAuthentication |
| MCP view → DeliveryKnowledgeSearchService | user principal 来自 token，禁止 AllowAny |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Spoofing | MCP PAT auth | mitigate | AccessTokenAuthentication only; 401 without token |
| T-16-02 | Information Disclosure | search_delivery_knowledge | mitigate | service resolve_allowed_project_ids fail-closed; cross-user integration test |
| T-16-03 | Tampering | as_of parameter | mitigate | parse_as_of strict ISO8601 + 400 on invalid |
| T-16-04 | Repudiation | tool calls | mitigate | begin_interaction_run + _record ledger |
| T-16-SC | Tampering | dependency installs | accept | 无新 pip 依赖 |
</threat_model>

<verification>
```bash
cd server && uv run pytest tests/knowledge/test_exposure.py tests/mcp_tools/test_delivery_knowledge_tools.py tests/mcp_tools/test_schema_snapshot.py -q
```
</verification>

<success_criteria>
- 三 MCP 工具 POST 200（合法 PAT）且 TOOL_SCHEMA_SNAPSHOT 含 22 工具
- A 用户 PAT 查 B 项目 → 空 results
- as_of 非法 → 400；合法透传 service
</success_criteria>

<output>
Create `.planning/phases/16-expo/16-01-SUMMARY.md` when done
</output>
