---
phase: 12-kmod
plan: 03
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - server/knowledge/exceptions.py
  - server/knowledge/collection.py
  - server/knowledge/management/__init__.py
  - server/knowledge/management/commands/__init__.py
  - server/knowledge/management/commands/rebuild_delivery_knowledge.py
  - server/system/models.py
  - server/tests/knowledge/test_collection.py
autonomous: true
requirements: [KMOD-01]
must_haves:
  truths:
    - "delivery_knowledge collection 不存在时被创建（hybrid dense+sparse + 全部 payload index），元信息写入 SystemSetting"
    - "collection 已存在且维度/hybrid 结构不匹配时 raise KnowledgeCollectionMismatchError 且绝不调用 delete_collection（collection 原样保留）"
    - "payload schema 常量（entity_kind/entity_id/version/is_latest/project_id/repository_id/source_kind/event_time）第一天定型，Phase 13/15 import 同一份"
    - "rebuild_delivery_knowledge 无 --yes 时只打印警告并退出不动数据；带 --yes 时执行删除→重建→元信息更新"
  artifacts:
    - path: "server/knowledge/exceptions.py"
      provides: "KnowledgeError 基类 + KnowledgeCollectionMismatchError"
      contains: "class KnowledgeCollectionMismatchError"
    - path: "server/knowledge/collection.py"
      provides: "DELIVERY_KNOWLEDGE_COLLECTION / KNOWLEDGE_PAYLOAD_INDEXED_FIELDS 常量 + ensure_delivery_knowledge_collection"
      exports: ["DELIVERY_KNOWLEDGE_COLLECTION", "KNOWLEDGE_PAYLOAD_INDEXED_FIELDS", "ensure_delivery_knowledge_collection"]
    - path: "server/knowledge/management/commands/rebuild_delivery_knowledge.py"
      provides: "显式重建命令（--yes 确认流程）"
    - path: "server/tests/knowledge/test_collection.py"
      provides: "生命周期/维度校验/重建命令全套测试（Qdrant mock）"
  key_links:
    - from: "server/knowledge/collection.py"
      to: "QdrantService.get_client"
      via: "唯一 client 获取路径（60s timeout 等历史修复）"
      pattern: "QdrantService\\.get_client"
    - from: "server/knowledge/management/commands/rebuild_delivery_knowledge.py"
      to: "server/knowledge/collection.py"
      via: "ensure_delivery_knowledge_collection 调用"
      pattern: "ensure_delivery_knowledge_collection"
---

<objective>
交付 `delivery_knowledge` Qdrant collection 的显式生命周期管理（ROADMAP SC#5）：payload schema（含 project_id/repository_id 权限维度）第一天定型为单一事实源常量；ensure 函数在维度/结构不匹配时响亮拒绝、绝不自动删库重建（与既有 indexer 语义刻意相反）；重建只能经 `--yes` 显式命令。

Purpose: P8 灾难防线（切 embedding 模型不得静默清空知识库）+ P6 权限维度防线（payload 权限字段事后回填成本 HIGH，locked decision 本阶段一次定对）。Phase 13 摄取与 Phase 15 检索都 import 本模块 schema 常量。
Output: `knowledge/exceptions.py` + `knowledge/collection.py` + `rebuild_delivery_knowledge` 命令 + `test_collection.py` 全绿（Qdrant 全 mock）。
</objective>

<execution_context>
@.cursor/gsd-core/workflows/execute-plan.md
@.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-kmod/12-RESEARCH.md
@.planning/phases/12-kmod/12-PATTERNS.md
@server/services/qdrant_service.py
@server/services/indexer.py
@server/accounts/management/commands/init_superuser.py
@server/system/models.py
</context>

## Artifacts this phase produces（Plan 03 范围）

- **模块**: `server/knowledge/exceptions.py`、`server/knowledge/collection.py`
- **异常**: `KnowledgeError`（基类，message + details dict）、`KnowledgeCollectionMismatchError`
- **常量**:
  - `DELIVERY_KNOWLEDGE_COLLECTION = "delivery_knowledge"`
  - `KNOWLEDGE_PAYLOAD_INDEXED_FIELDS`（dict：`entity_kind`=KEYWORD、`entity_id`=KEYWORD、`version`=INTEGER、`is_latest`=BOOL、`project_id`=KEYWORD、`repository_id`=KEYWORD、`source_kind`=KEYWORD、`event_time`=DATETIME）
  - `KNOWLEDGE_PAYLOAD_REQUIRED_FIELDS`（非索引必带字段元组：`source_id` / `chunk_kind` / `file_path` / `text` / `embedding_model` / `version_id`，docstring 契约）
- **函数**: `ensure_delivery_knowledge_collection() -> None`（async）
- **SettingKeys 新键**: `KNOWLEDGE_COLLECTION_META = "knowledge_collection_meta"`（`server/system/models.py`）
- **management command**: `rebuild_delivery_knowledge`（`--yes` 确认参数）
- **测试**: `server/tests/knowledge/test_collection.py`

<tasks>

<task type="auto">
  <name>Task 1: exceptions.py + collection.py（payload schema 定型与 ensure 函数）</name>
  <files>server/knowledge/exceptions.py, server/knowledge/collection.py, server/system/models.py</files>
  <read_first>
    - server/agents/core/exceptions.py（KnowledgeError 基类的形态类比：message + details + __str__）
    - server/services/qdrant_service.py（L124-150 get_client 历史修复；L397-500 create_collection 的 vectors_config 解析可借、**删库分支语义禁止照抄**；L452-491 hybrid 创建 + payload index 循环结构可借）
    - server/services/indexer.py（L1531-1538 `_ensure_collection` 的 SettingKeys.EMBEDDING_DIMENSION 读取模式，default 1024）
    - server/system/models.py（SettingKeys 类，新增键的落点）
    - .planning/phases/12-kmod/12-RESEARCH.md §delivery_knowledge collection 生命周期管理（语义反转三禁项）
  </read_first>
  <action>
    1. `server/knowledge/exceptions.py`：照 agents/core/exceptions.py 形态建 `KnowledgeError(Exception)` 基类（`__init__(message, details=None)` + `__str__` 拼 details）与 `KnowledgeCollectionMismatchError(KnowledgeError)`。中文 docstring。
    2. `server/system/models.py` SettingKeys 类追加 `KNOWLEDGE_COLLECTION_META = "knowledge_collection_meta"`（沿用既有键命名风格，值存 JSON：embedding 模型名 + 维度 + schema 版本）。
    3. `server/knowledge/collection.py`：
       - 常量 `DELIVERY_KNOWLEDGE_COLLECTION` / `KNOWLEDGE_PAYLOAD_INDEXED_FIELDS`（8 字段映射 qdrant `PayloadSchemaType`：KEYWORD×5、INTEGER、BOOL、DATETIME，见 Artifacts 清单）/ `KNOWLEDGE_PAYLOAD_REQUIRED_FIELDS`。模块 docstring 写明：本 schema 为 Phase 13 摄取与 Phase 15 检索的单一事实源；`is_latest` 只服务召回面（P10 边界）；`project_id`/`repository_id` 为权限维度，第一天定型不许回填（P6）。
       - `async def ensure_delivery_knowledge_collection() -> None`：
         a. 读 `SystemSetting.objects.filter(key=SettingKeys.EMBEDDING_DIMENSION).afirst()`，default 1024（indexer 同款）。
         b. `client = QdrantService.get_client()`（唯一 client 路径，禁止自建实例）；所有同步 client 调用经 `sync_to_async` 包装。
         c. collection 不存在 → 创建 hybrid（`vectors_config={"dense": VectorParams(size=dim, distance=COSINE)}` + `sparse_vectors_config={"sparse": SparseVectorParams()}`），随后循环 `KNOWLEDGE_PAYLOAD_INDEXED_FIELDS` 建 payload index；将元信息 JSON（model/dimension/schema_version=1）经 update_or_create 写入 SystemSetting `KNOWLEDGE_COLLECTION_META`；`logger.info("knowledge_collection_created", dimension=..., collection=...)`。
         d. collection 存在 → `get_collection` 比对：借 qdrant_service L418-435 的 vectors_config 解析（dict ⇒ hybrid，`.get("dense").size` 取维度），若 `existing_size != 期望维度` 或非 hybrid 结构 → `logger.error("knowledge_collection_config_mismatch", existing_size=..., expected_size=...)` + raise `KnowledgeCollectionMismatchError`，message 含现有/期望维度并指引运行 `manage.py rebuild_delivery_knowledge --yes`。**禁止调用 delete_collection / recreate**。匹配则补校验 SystemSetting 元信息（缺失则补写）。
         e. 任何 Qdrant 异常一律重抛（**禁止 `except: return False` 静默语义**——RESEARCH 三禁项之二）。
       - 不创建代码索引的 file_path/file_hash/language/branch_name payload index（那是 create_collection_by_name 的代码域 schema；三禁项之三：存在时必须严格比对而非直接 return True）。
  </action>
  <verify>
    <automated>cd server && uv run python -c "import django,os; os.environ.setdefault('DJANGO_SETTINGS_MODULE','friday.settings'); django.setup(); from knowledge.collection import DELIVERY_KNOWLEDGE_COLLECTION, KNOWLEDGE_PAYLOAD_INDEXED_FIELDS, ensure_delivery_knowledge_collection; from knowledge.exceptions import KnowledgeCollectionMismatchError; from system.models import SettingKeys; assert DELIVERY_KNOWLEDGE_COLLECTION=='delivery_knowledge'; assert set(KNOWLEDGE_PAYLOAD_INDEXED_FIELDS) >= {'entity_kind','entity_id','version','is_latest','project_id','repository_id','source_kind','event_time'}; assert SettingKeys.KNOWLEDGE_COLLECTION_META=='knowledge_collection_meta'; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `KNOWLEDGE_PAYLOAD_INDEXED_FIELDS` 恰好含 8 个键：entity_kind/entity_id/version/is_latest/project_id/repository_id/source_kind/event_time（权限维度 project_id/repository_id 在内，P6）
    - `rg -c "delete_collection" server/knowledge/collection.py` 为 0（ensure 路径绝无删库调用）
    - `rg -c "return False" server/knowledge/collection.py` 为 0（无静默失败语义）
    - `rg -c "QdrantService.get_client" server/knowledge/collection.py` ≥ 1 且 `rg -c "QdrantClient\(" server/knowledge/collection.py` 为 0（不自建 client）
    - `SettingKeys.KNOWLEDGE_COLLECTION_META == "knowledge_collection_meta"`
    - mismatch 分支 raise 的异常 message 含字符串 `rebuild_delivery_knowledge --yes`（指引可操作）
  </acceptance_criteria>
  <done>payload schema 常量定型、ensure 函数语义为"缺失创建 / 匹配通过 / 不匹配响亮拒绝"，零删库路径。</done>
</task>

<task type="auto">
  <name>Task 2: rebuild_delivery_knowledge 显式重建命令</name>
  <files>server/knowledge/management/__init__.py, server/knowledge/management/commands/__init__.py, server/knowledge/management/commands/rebuild_delivery_knowledge.py</files>
  <read_first>
    - server/knowledge/collection.py（Task 1 产物——ensure 函数与常量）
    - server/accounts/management/commands/init_superuser.py（WARNING 横幅视觉模式，lines 72-79）
    - server/code_relations/management/commands/rebuild_chunk_edges.py（命令骨架：add_arguments / CommandError / asyncio.run / structlog 始末事件 / 退出码契约）
    - server/services/qdrant_service.py（`delete_collection_by_name` L1002 签名）
  </read_first>
  <action>
    创建 `rebuild_delivery_knowledge` management command（`management/` 与 `commands/` 各带空 `__init__.py`）：

    1. `Command(BaseCommand)`，`help` 中文说明"显式删除并重建 delivery_knowledge collection（危险操作）"。
    2. `add_arguments`：`--yes`（`action="store_true"`，dest="yes"）唯一参数。
    3. `handle` 流程：
       - 无 `--yes`：用 `self.style.WARNING` 横幅（init_superuser 同款 `"=" * 60` 视觉）打印将发生什么——删除 collection `delivery_knowledge` 全部向量、按当前 EMBEDDING_DIMENSION 重建空 collection、更新 SystemSetting 元信息——并提示追加 `--yes` 确认；随后直接 return（退出码 0，不动任何数据）。
       - 有 `--yes`：`asyncio.run(self._rebuild())`（rebuild_chunk_edges 的 asyncio.run 模式）。`_rebuild` 内：`sync_to_async(QdrantService.delete_collection_by_name)(DELIVERY_KNOWLEDGE_COLLECTION)` → `await ensure_delivery_knowledge_collection()`（创建 + payload index + SystemSetting 元信息更新都在 ensure 内完成）→ `self.stdout.write(self.style.SUCCESS(...))` 醒目输出新维度与 collection 名。
       - structlog 始末事件：`rebuild_delivery_knowledge_started` / `rebuild_delivery_knowledge_finished`（键值含 dimension）。
       - 任何异常不吞——让其自然冒泡为非零退出码（CommandError 或原异常）。
    4. 命令模块 docstring 注明 TODO 锚点：Phase 13 接入摄取后本命令需扩展"从 PG（KnowledgeEntityVersion.content）全量重嵌入"步骤，当前阶段 collection 无数据，重建 = 删 + 建。
  </action>
  <verify>
    <automated>cd server && uv run python -c "import django,os; os.environ.setdefault('DJANGO_SETTINGS_MODULE','friday.settings'); django.setup(); from django.core.management import get_commands; assert get_commands().get('rebuild_delivery_knowledge')=='knowledge'; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `manage.py help rebuild_delivery_knowledge` 可发现该命令（get_commands 映射到 knowledge app）
    - `rg -c "store_true" server/knowledge/management/commands/rebuild_delivery_knowledge.py` ≥ 1（--yes 为开关参数）
    - 无 `--yes` 分支不出现 `delete_collection` 调用路径（源码断言：delete 调用仅在 `_rebuild` 内，且 `_rebuild` 仅在 options["yes"] 为真时执行）
    - `rg -c "ensure_delivery_knowledge_collection" server/knowledge/management/commands/rebuild_delivery_knowledge.py` ≥ 1（复用 ensure，不重复创建逻辑）
    - 模块 docstring 含 "Phase 13" TODO 锚点字样
  </acceptance_criteria>
  <done>重建入口唯一且显式：无 --yes 安全退出，有 --yes 删→建→元信息更新，全程留痕。</done>
</task>

<task type="auto">
  <name>Task 3: test_collection.py — 生命周期、维度校验与命令流程测试（Qdrant 全 mock）</name>
  <files>server/tests/knowledge/test_collection.py</files>
  <read_first>
    - server/knowledge/collection.py 与 server/knowledge/management/commands/rebuild_delivery_knowledge.py（被测对象）
    - server/tests/knowledge/conftest.py（mock_qdrant_client fixture——Plan 01 Task 2 产物）
    - server/tests/test_git_diff_index.py（Qdrant seam 测试风格）
  </read_first>
  <action>
    创建 `server/tests/knowledge/test_collection.py`（文件头 docstring 列用例清单；`pytestmark = pytest.mark.django_db`；显式使用 `mock_qdrant_client` fixture，mock 的 `get_collections` / `get_collection` 返回值按用例配置；qdrant_client.models 的 VectorParams 可直接构造作为假返回）：

    ensure 函数：
    - collection 不存在（get_collections 返回空列表）→ 调 `client.create_collection` 一次；payload index 创建调用次数 == len(KNOWLEDGE_PAYLOAD_INDEXED_FIELDS) == 8；SystemSetting `knowledge_collection_meta` 落库且 JSON 含 dimension
    - collection 存在且维度/hybrid 匹配（get_collection 返回 dense size=1024 的 named vectors mock）→ 不调 create_collection、不 raise、正常返回
    - 维度不匹配（existing 768 vs 期望 1024）→ raise `KnowledgeCollectionMismatchError`；**断言 `client.delete_collection` 从未被调用**（P8 核心断言）；异常 message 含 "768" 与 "1024" 与 "rebuild_delivery_knowledge --yes"
    - 非 hybrid 结构（vectors_config 非 dict）→ 同样 raise 且不删库
    - Qdrant client 抛 UnexpectedResponse → 异常向上冒泡（pytest.raises 断言，不被吞为 False/None）
    - EMBEDDING_DIMENSION SystemSetting 存在时（如 768）按设置值创建（create_collection 调用参数断言 size=768）

    rebuild 命令（`django.core.management.call_command`）：
    - 无 `--yes`：stdout 含 WARNING 横幅文案；`delete_collection_by_name` 与 `create_collection` 均未被调用
    - 带 `--yes`：`delete_collection_by_name(DELIVERY_KNOWLEDGE_COLLECTION)` 被调用一次，且随后 ensure 创建路径被执行（create_collection 调用一次）

    schema 常量回归（Phase 13/15 契约锁）：
    - `KNOWLEDGE_PAYLOAD_INDEXED_FIELDS` 键集合恰为 8 字段集合（任何增删触发测试失败，schema 变更必须显式过审）
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/knowledge/test_collection.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/knowledge/test_collection.py -x` 全绿，用例数 ≥ 9
    - 存在断言 `delete_collection` mock 未被调用的用例（`rg -c "delete_collection.*assert_not_called|assert_not_called" server/tests/knowledge/test_collection.py` ≥ 1）
    - 维度不匹配用例同时断言异常类型与 message 内容（768/1024/--yes 指引）
    - 测试不发起真实网络（pytest-socket `--disable-socket` 下全绿即证明）
  </acceptance_criteria>
  <done>SC#5 全部行为（创建/校验/拒绝不删库/显式重建）有自动化测试固化，Qdrant 全程 mock。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 运维操作者 → management command | 重建命令可清空知识向量库，必须显式确认 |
| server → Qdrant | collection 配置读写跨进程边界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04 | Tampering / Repudiation | collection 生命周期 | mitigate | Task 1：mismatch → raise 不删库（与 indexer 自动重建语义刻意相反）+ structlog error 留痕；Task 2：删除仅经 `--yes` 显式命令；Task 3 用 `delete_collection.assert_not_called()` 固化 |
| T-12-02 | Information Disclosure | payload 权限维度 | mitigate | Task 1：project_id/repository_id 进 `KNOWLEDGE_PAYLOAD_INDEXED_FIELDS` 第一天定型 + keyword index（P6 回填成本 HIGH，RETR-07 Phase 15 强制过滤的前提）；Task 3 schema 常量回归测试锁键集合 |
| T-12-06 | Denial of Service | ensure 异常静默吞掉导致写入错库 | mitigate | Task 1：异常一律重抛（禁 return False）；Task 3 UnexpectedResponse 冒泡用例固化 |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | 本计划零新依赖（RESEARCH Package Legitimacy Audit：不适用） |
</threat_model>

<verification>
- `cd server && uv run pytest tests/knowledge/ -x`（knowledge 全包绿）
- `cd server && uv run pytest`（wave 合并时全套回归，确认 SettingKeys 改动无副作用）
</verification>

<success_criteria>
- ROADMAP SC#5 成立：delivery_knowledge 有显式生命周期管理——维度不匹配拒绝并响亮报错（含重建指引）、绝不自动删库；payload schema（含权限维度字段）第一天定型且有回归测试锁定
- 重建唯一入口为 `manage.py rebuild_delivery_knowledge --yes`，无确认参数时零副作用
</success_criteria>

<output>
完成后创建 `.planning/phases/12-kmod/12-03-SUMMARY.md`
</output>
