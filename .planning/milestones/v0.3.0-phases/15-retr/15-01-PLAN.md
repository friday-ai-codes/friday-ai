---
phase: 15-retr
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/knowledge/retrieval_types.py
  - server/knowledge/access_scope.py
  - server/knowledge/recency.py
  - server/knowledge/graph_store.py
  - server/friday/settings.py
  - server/tests/knowledge/test_recency.py
  - server/tests/knowledge/test_access_scope.py
  - server/tests/knowledge/test_graph_store.py
autonomous: true
requirements: [RETR-05, RETR-07]
must_haves:
  truths:
    - "recency_score(90天前)=0.5、当天=1.0，half_life 可经 settings 覆盖"
    - "无 user 或 user 无可见 project → resolve_allowed_project_ids 返回空集合（fail-closed，非 AllowAny）"
    - "caller project_ids 只能收窄 allowed 集合，不能放宽"
    - "resolve_allowed_repository_ids 解析 user 可见 repository_id 集合（P6 双维权限）"
    - "recency 拒绝 naive datetime；reference_time/as_of 必须 timezone-aware（P2）"
    - "graph_store.traverse(direction='both', max_hops=2) 双向多跳可达上下游实体（Phase 12 预留位本阶段落地）"
  artifacts:
    - path: "server/knowledge/retrieval_types.py"
      provides: "SearchResultDTO / TimelineNodeDTO / RelatedEntityDTO / ProvenanceLinks 结构化契约"
      exports: ["SearchResultDTO", "TimelineNodeDTO", "RelatedEntityDTO"]
    - path: "server/knowledge/access_scope.py"
      provides: "resolve_allowed_project_ids(user, project_ids?) fail-closed 权限解析"
      exports: ["resolve_allowed_project_ids"]
    - path: "server/knowledge/recency.py"
      provides: "compute_recency_score + fuse_vector_recency 纯函数"
      exports: ["compute_recency_score", "fuse_vector_recency"]
    - path: "server/knowledge/graph_store.py"
      provides: "direction='both' 多跳 traverse 实现（替换 NotImplementedError）"
  key_links:
    - from: "server/knowledge/access_scope.py"
      to: "server/permissions/services.py"
      via: "PermissionService.get_user_projects → id 列表"
      pattern: "PermissionService.get_user_projects"
    - from: "server/knowledge/recency.py"
      to: "server/friday/settings.py"
      via: "KNOWLEDGE_RETRIEVAL_ALPHA/BETA/HALF_LIFE_DAYS"
      pattern: "KNOWLEDGE_RETRIEVAL_HALF_LIFE_DAYS"
---

<objective>
交付 Phase 15 检索地基：结构化 DTO 契约、fail-closed 权限 scope 解析、时间衰减纯函数 + settings 配置、GraphStore `direction="both"` 多跳遍历（RETR-02 前置）。

Purpose: 后续向量召回/融合/LLM 计划只 import 本 plan 产物，不再散落定义 filter 或 decay 公式。
Output: retrieval_types.py、access_scope.py、recency.py、settings 四项配置、graph_store both 方向扩展、三份单元测试。
</objective>

<execution_context>
@~/.claude/skills/gsd-execute-phase/SKILL.md
</execution_context>

<context>
@.planning/phases/15-retr/15-CONTEXT.md
@.planning/phases/15-retr/15-RESEARCH.md
@.planning/phases/12-kmod/12-02-SUMMARY.md
@server/knowledge/graph_store.py
@server/permissions/services.py
@server/knowledge/collection.py
</context>

## 规划定案（供 verify 对照）

- **时间衰减（RESEARCH 定案）**：`recency = exp(-ln(2) * age_days / half_life)`，默认 half_life=90、alpha=0.7、beta=0.3；`as_of` 作 reference_time。
- **权限（CONTEXT 检索 Service 契约）**：签名层后续强制 `user: User`；本 plan 只交付 scope 解析 helper，无 user → 空集合。
- **GraphStore both（12-VERIFICATION Info 项）**：实现 UNION 出 `out`+`in` 两向递归 CTE，保持 path 防环与 MAX_HOPS clamp。

## Artifacts this phase produces

| Artifact | Plan | 说明 |
|----------|------|------|
| DTO + access_scope + recency + settings | **15-01** | 契约与 P6/P5 时间衰减地基 |
| vector_recall 分路 hybrid + is_latest filter | 15-02 | RETR-01/04/07 |
| get_timeline / get_related PG 查询 | 15-03 | RETR-02/03 |
| search_similar 图扩散 + 融合 rerank | 15-04 | RETR-05 |
| LLM grader + 集成测试 + eval fixture | 15-05 | ENH-02 |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: retrieval_types.py + access_scope.py + settings 配置</name>
  <files>server/knowledge/retrieval_types.py, server/knowledge/access_scope.py, server/friday/settings.py, server/tests/knowledge/test_access_scope.py</files>
  <read_first>
    - .planning/phases/15-retr/15-CONTEXT.md §检索 Service 契约、§权限与 metadata
    - server/permissions/services.py
    - server/services/retrieval/types.py
    - server/friday/settings.py
  </read_first>
  <behavior>
    - Test 1：user A 仅 membership 项目 P1 → resolve 含 P1.id；user B 无 membership → 空集合
    - Test 2：caller project_ids=[P1] 且 user 可见 {P1,P2} → 仅 P1；caller 传不可见 P3 → 空集合
    - Test 3：user=None → 空集合
    - Test 4：superuser → 全项目 id（caller 可收窄）
    - Test 5：resolve_allowed_repository_ids 仅返回 user 可见 project 下仓库
  </behavior>
  <action>
    1. 新建 retrieval_types.py（frozen dataclass）：ProvenanceLinks、EntityMetadata、SearchResultDTO、TimelineNodeDTO、RelatedEntityDTO。中文 docstring：Phase 15 检索 DTO 唯一出口。
    2. 新建 access_scope.py：async resolve_allowed_project_ids(user, project_ids=None) → list[str]；async resolve_allowed_repository_ids(user, repository_ids=None) → list[str]（从 user 可见 project 关联 Repository FK 解析，superuser 全量；caller 传入只能 intersect 收窄；无 user → []）。fail-closed 交集语义。
    3. settings 追加 KNOWLEDGE_RETRIEVAL_ALPHA/BETA/HALF_LIFE_DAYS/GRAPH_MAX_HOPS/LLM_RERANK_ENABLED。
    4. test_access_scope.py 四用例。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/knowledge/test_access_scope.py -x</automated>
  </verify>
  <done>DTO 与 fail-closed scope 就绪。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: recency.py 时间衰减纯函数 + test_recency.py</name>
  <files>server/knowledge/recency.py, server/tests/knowledge/test_recency.py</files>
  <behavior>
    - Test 1：event_time=reference_time → recency=1.0
    - Test 2：age=90d, half_life=90 → recency≈0.5
    - Test 3：fuse(0.8, 1.0, 0.7, 0.3)=0.86
    - Test 4：naive datetime → ValueError
    - Test 5：normalize 单元素 → [1.0]
  </behavior>
  <action>
    实现 compute_recency_score、normalize_vector_scores、fuse_vector_recency；拒绝 naive datetime；无 event_time 返回 0.5。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/knowledge/test_recency.py -x</automated>
  </verify>
  <done>90 天半衰期公式单测通过。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GraphStore traverse direction=both 多跳</name>
  <files>server/knowledge/graph_store.py, server/tests/knowledge/test_graph_store.py</files>
  <behavior>
    - Test 1：链 A→B→C both 2 跳含 B/C；从 C both 含 B/A
    - Test 2：环不 infinite loop
    - Test 3：失效边不可见（P2）
  </behavior>
  <action>
    实现 direction=both 多跳 SQL；移除 NotImplementedError；test_both_* 用例组。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/knowledge/test_graph_store.py -k both -x</automated>
  </verify>
  <done>RETR-02 GraphStore 双向多跳就绪。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → access_scope | 不可信 project_ids 只能收窄 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Elevation of Privilege | access_scope.py | mitigate | intersect + fail-closed |
| T-15-02 | Tampering | recency.py | accept | 纯函数 |
| T-15-03 | Information Disclosure | graph_store both | mitigate | 有效性过滤不变 |
| T-15-SC | Tampering | pip installs | accept | 无新依赖 |
</threat_model>

<verification>
cd server && uv run pytest tests/knowledge/test_access_scope.py tests/knowledge/test_recency.py tests/knowledge/test_graph_store.py -k "both or access or recency" -x
</verification>

<success_criteria>
DTO/access/recency/settings 可被 15-02+ import；GraphStore both 测试通过。
</success_criteria>

<output>
Create `.planning/phases/15-retr/15-01-SUMMARY.md` when done
</output>
