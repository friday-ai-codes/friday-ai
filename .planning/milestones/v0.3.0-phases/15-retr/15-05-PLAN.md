---
phase: 15-retr
plan: 05
type: execute
wave: 5
depends_on: [15-04]
files_modified:
  - server/knowledge/llm_grader.py
  - server/knowledge/retrieval.py
  - server/knowledge/api/views.py
  - server/knowledge/api/urls.py
  - server/friday/urls.py
  - server/tests/knowledge/fixtures/retr_eval_queries.json
  - server/tests/knowledge/test_delivery_search.py
  - server/tests/knowledge/test_llm_grader.py
autonomous: true
requirements: [ENH-02, RETR-06, RETR-07]
must_haves:
  truths:
    - "LLM 对 top-K 候选分级 duplicate/related/unrelated + 中文一句话理由"
    - "LLM 失败降级为 vector+time 排序，不阻塞主链路"
    - "A 用户检索 B 项目 → 0 结果（越权集成测试）"
    - "eval fixture ≥20 条合成 query 可跑通 search_similar"
    - "内部 REST GET /api/knowledge/search/ 经 JWT 认证可调（Phase 16 前测试用）"
  artifacts:
    - path: "server/knowledge/llm_grader.py"
      provides: "grade_search_results LLM 二阶段分级"
      exports: ["grade_search_results"]
    - path: "server/knowledge/retrieval.py"
      provides: "search_similar 接入 llm_grader"
    - path: "server/tests/knowledge/fixtures/retr_eval_queries.json"
      provides: "20+ 合成评测 query"
    - path: "server/tests/knowledge/test_delivery_search.py"
      provides: "端到端 + 越权 + eval 回归"
    - path: "server/knowledge/api/views.py"
      provides: "内部测试 REST"
  key_links:
    - from: "server/knowledge/llm_grader.py"
      to: "server/services/provider_config.py"
      via: "resolve chat model"
      pattern: "provider_config"
    - from: "server/knowledge/api/views.py"
      to: "server/knowledge/retrieval.py"
      via: "DeliveryKnowledgeSearchService"
      pattern: "DeliveryKnowledgeSearchService"
---

<objective>
交付 ENH-02 LLM 二阶段复评、端到端集成测试、合成 eval fixture（20+ query）与内部 REST；完成 Phase 15 检索 service 全量收口。

Purpose: 召回可解释；越权与 eval 覆盖 RETR-06/07；为 Phase 16 四入口提供稳定 service。
Output: llm_grader.py、retrieval.py LLM 接线、api/views、fixtures、集成测试。
</objective>

<execution_context>
@~/.claude/skills/gsd-execute-phase/SKILL.md
</execution_context>

<context>
@.planning/phases/15-retr/15-CONTEXT.md   （§LLM 二阶段复评 ENH-02）
@.planning/phases/15-retr/15-RESEARCH.md   （§LLM Second-Stage、§Eval Fixture）
@.planning/phases/15-retr/15-04-SUMMARY.md
@server/services/provider_config.py
@server/chat/views.py   （PermissionService 用法参考）
</context>

## 规划定案

- **Prompt**：中文 system + user；输入 query + 候选列表 JSON（entity_id/kind/title/snippet≤500 字）；要求输出严格 JSON array。
- **降级**：任何 provider/parse 异常 → log knowledge_llm_grade_failed + 原序返回。
- **开关**：settings.KNOWLEDGE_RETRIEVAL_LLM_RERANK_ENABLED=False 时 skip LLM。
- **REST**：三个 GET endpoint（search/timeline/related），IsAuthenticated + 强制传 user 给 service；非 Phase 16 正式 MCP 面。

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: llm_grader.py + test_llm_grader.py</name>
  <files>server/knowledge/llm_grader.py, server/tests/knowledge/test_llm_grader.py</files>
  <read_first>
    - server/services/provider_config.py（chat model 解析）
    - 仓库内 structured JSON LLM 调用先例（grep langchain invoke json）
  </read_first>
  <behavior>
    - Test 1：mock LLM 返回 duplicate/related → DTO.llm_grade/llm_reason 填充
    - Test 2：LLM raise → 原序不变、grade 字段空
    - Test 3：候选 snippet 超 500 字被截断
    - Test 4：LLM_RERANK_ENABLED=False → 零 provider 调用
  </behavior>
  <action>
    async def grade_search_results(query: str, results: list[SearchResultDTO]) -> list[SearchResultDTO]。构造中文 prompt；调用 provider；解析 JSON；写回 llm_grade/llm_reason；unrelated 可选降权排序（related>duplicate>unrelated 或保持 vector 序仅标注——规划定案：标注为主，unrelated 排到末尾）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/knowledge/test_llm_grader.py -x</automated>
  </verify>
  <done>ENH-02 LLM 分级可单测 mock。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: search_similar 接线 + eval fixture + test_delivery_search.py</name>
  <files>server/knowledge/retrieval.py, server/tests/knowledge/fixtures/retr_eval_queries.json, server/tests/knowledge/test_delivery_search.py</files>
  <behavior>
    - Test 1：端到端 search_similar 非空（entity_factory 预置链 + mock qdrant/embedding）
    - Test 2：user A vs project B → []
    - Test 3：eval fixture 每条 query 不抛异常、返回 list
    - Test 4：get_timeline/get_related 经 service 委托可用
  </behavior>
  <action>
    retrieval.py search_similar 末尾：if settings.LLM_RERANK_ENABLED: results = await grade_search_results(...)。
    创建 retr_eval_queries.json：≥20 条 {query, seed_entity_kind, notes} 合成数据。
    test_delivery_search.py：集成 + 越权 + parametrized eval fixture smoke + TestKnowledgeSearchAPI（GET /api/knowledge/search/ 200，Task 3 复用）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/knowledge/test_delivery_search.py -x</automated>
  </verify>
  <done>全链路集成测试绿；eval fixture 就位。</done>
</task>

<task type="auto">
  <name>Task 3: 内部 REST knowledge/api + urls 注册</name>
  <files>server/knowledge/api/views.py, server/knowledge/api/urls.py, server/friday/urls.py</files>
  <action>
    新建 knowledge/api/views.py：KnowledgeSearchView（GET q/top_k/project_ids）、KnowledgeTimelineView、KnowledgeRelatedView；均 IsAuthenticated；调用 DeliveryKnowledgeSearchService。urls.py include 到 friday/urls.py `api/knowledge/`。OpenAPI tag `knowledge-retrieval-test`。中文 docstring 注明 Phase 16 前测试面、非 MCP 正式契约。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/knowledge/test_delivery_search.py -k api -x && uv run python manage.py check</automated>
  </verify>
  <done>REST 可 manage.py check 通过；有 API 测试则全绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JWT user → REST → service | 不可信 query；service fail-closed |
| LLM provider | 候选文本截断；无密钥回传 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-13 | Information Disclosure | test_delivery_search | mitigate | 越权用例 A→B 项目 |
| T-15-14 | Spoofing | REST views | mitigate | IsAuthenticated |
| T-15-15 | Tampering | LLM prompt injection | mitigate | 截断 + 结构化输出；失败降级 |
| T-15-SC | Tampering | pip installs | accept | 无新依赖 |
</threat_model>

<verification>
cd server && uv run pytest tests/knowledge/test_llm_grader.py tests/knowledge/test_delivery_search.py -x
cd server && uv run python manage.py check
python3 -c "import json; d=json.load(open('tests/knowledge/fixtures/retr_eval_queries.json')); assert len(d)>=20"
</verification>

<success_criteria>
ENH-02 分级可用；越权 0 结果；eval≥20；manage.py check 通过。
</success_criteria>

<output>
Create `.planning/phases/15-retr/15-05-SUMMARY.md` when done
</output>
