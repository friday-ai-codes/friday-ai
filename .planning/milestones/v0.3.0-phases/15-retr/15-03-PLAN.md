---
phase: 15-retr
plan: 03
type: execute
wave: 3
depends_on: [15-01, 15-02]
files_modified:
  - server/knowledge/timeline.py
  - server/knowledge/related.py
  - server/tests/knowledge/test_timeline.py
  - server/tests/knowledge/test_related.py
autonomous: true
requirements: [RETR-02, RETR-03]
must_haves:
  truths:
    - "get_timeline 零 Qdrant 调用（P10 轨迹面纯 PG）"
    - "GraphStore as_of 过滤失效边；timeline/related 透传 aware as_of（P2）"
    - "timeline 节点按 version 升序，挂接 code_change 按 event_time 排序"
    - "get_related 支持 direction=in/out/both，max_hops 默认 2，返回含 relation/depth 的 RelatedEntityDTO"
    - "用户无 entity 所属 project 权限 → timeline/related 返回空（fail-closed）"
  artifacts:
    - path: "server/knowledge/timeline.py"
      provides: "build_entity_timeline(entity_id, user, include_superseded)"
      exports: ["build_entity_timeline"]
    - path: "server/knowledge/related.py"
      provides: "fetch_related_entities(entity_id, user, direction, max_hops, as_of)"
      exports: ["fetch_related_entities"]
    - path: "server/tests/knowledge/test_timeline.py"
      provides: "版本链 + code_change 时序回归"
    - path: "server/tests/knowledge/test_related.py"
      provides: "双向上下游 + 权限拒绝回归"
  key_links:
    - from: "server/knowledge/timeline.py"
      to: "server/knowledge/models.py"
      via: "KnowledgeEntityVersion.objects.filter(entity_id=...).order_by('version')"
      pattern: "order_by\\(\"version\"\\)"
    - from: "server/knowledge/related.py"
      to: "server/knowledge/graph_store.py"
      via: "graph_store.traverse(..., direction=..., as_of=...)"
      pattern: "graph_store\\.traverse"
    - from: "server/knowledge/timeline.py"
      to: "server/knowledge/access_scope.py"
      via: "_assert_entity_access 前置校验"
      pattern: "resolve_allowed_project_ids"
---

<objective>
交付 PG 轨迹查询与图关联查询：`get_timeline` 纯 ORM 版本链 + 编码时间线；`get_related` 基于 GraphStore 1–2 跳双向上下游（RETR-02/03，P10 分面）。

Purpose: 轨迹面与召回面解耦——timeline 不依赖 Qdrant；related 复用 15-01 both 多跳。
Output: timeline.py、related.py、两份测试文件。
</objective>

<execution_context>
@~/.claude/skills/gsd-execute-phase/SKILL.md
</execution_context>

<context>
@.planning/phases/15-retr/15-CONTEXT.md   （§迭代轨迹 RETR-03）
@.planning/phases/15-retr/15-RESEARCH.md   （§PG Trajectory）
@.planning/phases/15-retr/15-01-SUMMARY.md
@server/knowledge/models.py
@server/knowledge/graph_store.py
@server/knowledge/retrieval_types.py
</context>

## 规划定案

- **P10 守门**：timeline.py / related.py 禁止 import QdrantService、vector_ops、collection（CI grep test 守护）。
- **权限**：entity.project_id 须在 user allowed 集合；entity.project_id 为空时 fail-closed（与 ingestion 写入侧"权限维度不许空"一致，测试用 SET_NULL 边界）。
- **include_superseded**：false 时 timeline 仅 is_latest=True 或 invalid_at IS NULL 的版本节点。

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: timeline.py 纯 PG 迭代轨迹</name>
  <files>server/knowledge/timeline.py, server/tests/knowledge/test_timeline.py</files>
  <read_first>
    - server/knowledge/models.py（KnowledgeEntityVersion 字段 valid_at/invalid_at/version）
    - .planning/phases/15-retr/15-CONTEXT.md §迭代轨迹
    - server/tests/knowledge/conftest.py（entity_factory/version_factory/edge_factory）
  </read_first>
  <behavior>
    - Test 1：entity 有 v1/v2/v3 → timeline 长度 3、version 单调递增
    - Test 2：v2 关联 code_change 边 → timeline v2 节点含 code_changes 列表按 event_time 排序
    - Test 3：include_superseded=False 且 v1 invalid → 仅 v2/v3
    - Test 4：mock patch QdrantService → 零调用（P10）
    - Test 5：越权 user → []
  </behavior>
  <action>
    async def build_entity_timeline(entity_id, *, user, include_superseded=False) -> list[TimelineNodeDTO]。内部 _assert_entity_access(entity, user)。版本 queryset order_by version；每节点摘要 title/content 截断；code_change 经 graph_store.neighbors 或 KnowledgeEdge filter relation=IMPLEMENTED_BY 反查，event_time 排序 attach。模块 docstring 强调零 Qdrant。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/knowledge/test_timeline.py -x</automated>
  </verify>
  <done>RETR-03 轨迹查询可走 PG 版本链。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: related.py 双向上下游 + test_related.py</name>
  <files>server/knowledge/related.py, server/tests/knowledge/test_related.py</files>
  <read_first>
    - server/knowledge/graph_store.py（traverse/neighbors、EdgeRelation）
    - .planning/phases/14-triggers/14-06-SUMMARY.md（work_item→plan→code 链范例）
  </read_first>
  <behavior>
    - Test 1：work_item --HAS_PLAN--> tech_plan --IMPLEMENTED_BY--> code_change；from work_item direction=out max_hops=2 含 plan+code
    - Test 2：from code_change direction=in 含 plan 与 work_item
    - Test 3：direction=both max_hops=2 合并 depth 取 min
    - Test 4：as_of 早于边 valid_at → 边不可见
    - Test 5：越权 user → []
  </behavior>
  <action>
    async def fetch_related_entities(entity_id, *, user, direction="both", max_hops=2, relations=None, as_of=None) -> list[RelatedEntityDTO]。默认 relations=[HAS_PLAN, IMPLEMENTED_BY, RELATES_TO]。traverse 后 bulk hydrate：优先 import metadata_hydrate.hydrate_entity_metadata（15-02 产物）；15-03 执行时 15-02 已就绪（depends_on 含 15-02）。过滤 related entity project 不在 allowed → 丢弃。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/knowledge/test_related.py -x</automated>
  </verify>
  <done>RETR-02 双向上下游可查。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → timeline/related | entity 级 project 权限 gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-07 | Information Disclosure | timeline.py | mitigate | _assert_entity_access + 越权测试 |
| T-15-08 | Information Disclosure | related.py | mitigate | traverse 后 project filter |
| T-15-09 | Tampering | P10 bypass | mitigate | grep 测试禁止 Qdrant import |
| T-15-SC | Tampering | pip installs | accept | 无新依赖 |
</threat_model>

<verification>
cd server && uv run pytest tests/knowledge/test_timeline.py tests/knowledge/test_related.py -x
rg "QdrantService|vector_ops|delivery_knowledge" server/knowledge/timeline.py server/knowledge/related.py | wc -l  → 0
</verification>

<success_criteria>
Timeline 版本序正确；related 双向 2 跳可达；P10 零 Qdrant。
</success_criteria>

<output>
Create `.planning/phases/15-retr/15-03-SUMMARY.md` when done
</output>
