---
milestone: v0.13.0
name: 并发治理与索引体验
status: tech_debt
audited: 2026-06-23
requirements_total: 11
requirements_satisfied: 11
integration_ok: true
---

# Milestone v0.13.0 — Audit（并发治理与索引体验）

## 结论

**PASS（tech_debt）** — 11/11 v1 需求代码层满足，跨阶段集成成立（integration_ok）。遗留：少量**既有**前端 chat 组件测试失败（与本里程碑无关）、Phase 70 criterion 4 的「URL 拆段拼接」纯 UI 打磨 deferred、真机/真实平台运行期人工验收 deferred。确认后可归档。

## 需求覆盖（11/11 满足）

| Requirement | Phase | 验收证据 |
|-------------|-------|----------|
| STREAM-01 | 65 | handleSSEEvent owner 守护 + sendMessage isCurrent finally；chat.stream-isolation 守护；vitest stores/composables 170 绿 |
| LSP-01 | 66 | VOLAR/GOPLS_BACKEND_ENABLED 默认 False + BACKEND_REGISTRY 回落 TreeSitter；codegraph/ 608 绿（含预置 acceptance 转绿） |
| CONC-01 | 67 | durable defer lock 透传 + 槽位锁池（稳定 slot）+ 5 入队点 + 设置 N；test_concurrency_locks/governance |
| CONC-02 | 67 | ProviderCredential.max_concurrency + acquire_llm_slot（Redis 租约+进程内 fallback+LLMBusyError）+ 两 chokepoint；test_llm_concurrency |
| CONC-03 | 67 | 复用 runner.concurrent + MCP 不限 + 无全局上限；.env.example 文档；governance 守护 |
| PROG-01 | 68 | _compute_index_progress 单调加权阶段（消除归零跳变）；test_index_progress_ai_summary 单调守护 |
| PROG-02 | 68 | 图谱独立轨（SSE graph 段+RepositoryGraphCard）+ ai_summary_status 暴露/渲染 |
| BATCH-01 | 69 | ReindexAllView(IsSuperUser) 复用 67 槽位锁排队消费 + 前端超管按钮/反馈；test_batch_and_reindex |
| BATCH-02 | 69 | RepositoryBatchCreateView 数组接口（复用 _acreate_repository_core）+ CSV 导入；守护 5 例 |
| TOKEN-01 | 70 | Repository.git_instance_credential FK + resolver 优先级 per-repo→FK→host→none；test_token_provider_fk |
| TOKEN-02 | 70 | 建仓 token 可选 + 密钥提供方 select + TestConnection fallback + admin 说明（URL 拆段拼接 UI deferred） |

## 集成验收（integration_ok）

- **全量后端回归**：`uv run pytest` → **6011 passed / 61 skipped / 8 xfailed**，初次 1 failed（`test_base_branch::test_invalid_branch_returns_400`，由 Phase 70 acreate 重构改抛 ValidationError、adrf 异步渲染形状差异引起）→ 已修复（acreate 显式 catch ValidationError 返 `Response(exc.detail, 400)` 复刻旧响应体），复跑全绿。`makemigrations --check` 干净（migration 0008/0039）。
- **跨阶段连线**：Phase 69 ReindexAllView 复用 Phase 67 `_schedule_index` 槽位锁（批量受并发上限排队）；Phase 68 进度读 Phase 67/仓库状态字段；Phase 70 复用既有 GitInstanceCredential 实例池 + aresolve_git_token。
- **前端**：相关组件/页/store/composable 测试零回归（stores+composables 170、repository 组件 83、repositories 页、git-credentials 页）；eslint 改动文件 clean。

## Tech Debt / Deferred（确认后归档）

| 类型 | 项 | 说明 |
|------|-----|------|
| pre_existing | 前端 chat 组件测试 9 例失败（ClarificationCard 4 / ChatInput 3 / TechPlanCard 2） | **既有失败**——在里程碑起点 commit `c58531d0`（本里程碑任何代码之前）已同样失败，与 v0.13.0 无关；记录待后续单独修复 |
| ui_polish | TOKEN-02 建仓表单「URL 拆段拼接（host 前缀只读 + group/repo split + .git）」 | 后端 + 失焦自动校验（防抖自动测连）已就绪；拆段拼接 split-input 为纯 UI 打磨 deferred |
| uat_gap | 真机/真实平台运行期人工验收 | Redis 租约信号量跨副本、k8s KEDA 多 worker 并发上限、真实数百仓 CSV 导入/全量重索引、真实 Git 平台建仓连通——需真实 Redis/集群/平台 |

## 已知 v2（accepted，非 gap）

CONCX-01/02（容器内 LLM 统一 Redis 限流、自适应收敛）、GRAPHX-01（常驻热 LSP 池 + 图谱异步解耦）见 REQUIREMENTS.md v2；Out of Scope（全局总并发硬上限 / chat 硬 429 / DB 计数准入 / 永久删 LSP）明确不做。
