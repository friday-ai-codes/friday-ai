---
phase: 124-coding-chain
plan: 03
type: execute
wave: 2
depends_on: ["124-01", "124-02"]
files_modified:
  - server/workflows/nodes/ai/coding.py
  - server/mcp_tools/merge_request_service.py
  - server/mcp_tools/views.py
  - server/mcp_tools/work_item_execution_service.py
  - server/workflows/services/mr_service.py
  - server/tests/workflows/test_coding_impact_report.py
  - server/tests/mcp_tools/test_mr_impact_report.py
  - .planning/phases/124-coding-chain/124-VALIDATION.md
autonomous: true
requirements: [DIFF-04]

must_haves:
  truths:
    - "AICodingNode._create_mr_for_repo 在 create_merge_request 前 append 同一 build_impact_report_section（D-06 workflow）"
    - "MCP draft/create_merge_request 路径 append 同一 helper；显式 description 幂等不双段（D-06 MCP）"
    - "mr_service.create_mr_for_task 挂同一 append，消除第三条方言；行为测覆盖 fail-soft/append（D-06）"
    - "work_item_execution_service 调用 create_merge_request 时传 user=initiating_user，避免永久 stub（ACL）"
    - "影响面失败时 MR 仍创建且 description 含 stub 或原描述（成功标准 3 / D-09）"
    - "同一 (repo, compare) 下 workflow 与 MCP 拼装段规范化后一致（D-14）"
    - "未改 mcp/ submodule、未改 repo_router_v2.py、未改 runner 硬门禁（D-16）"
  artifacts:
    - path: "server/workflows/nodes/ai/coding.py"
      provides: "_create_mr_for_repo impact append 挂点"
      contains: "build_impact_report_section"
    - path: "server/mcp_tools/merge_request_service.py"
      provides: "MCP MR impact append"
      contains: "append_impact_report"
    - path: "server/mcp_tools/work_item_execution_service.py"
      provides: "create_merge_request(user=initiating_user) 接线"
      contains: "user=initiating_user"
    - path: "server/workflows/services/mr_service.py"
      provides: "create_mr_for_task 消方言挂点"
      contains: "build_impact_report_section"
    - path: "server/tests/workflows/test_coding_impact_report.py"
      provides: "create_mr_for_task fail-soft/append 行为测"
      contains: "test_create_mr_for_task_failsoft_appends_impact"
    - path: "server/tests/mcp_tools/test_mr_impact_report.py"
      provides: "D-14 对等哨兵绿测"
      contains: "test_workflow_mcp_impact_section_parity"
  key_links:
    - from: "_create_mr_for_repo body"
      to: "build_impact_report_section"
      via: "append before MRCreateRequest"
      pattern: "append_impact_report|build_impact_report_section"
    - from: "create_merge_request (mcp)"
      to: "build_impact_report_section"
      via: "user=request.user ACL"
      pattern: "build_impact_report_section"
    - from: "work_item_execution_service.create_merge_request call"
      to: "create_merge_request(..., user=initiating_user)"
      via: "ACL user plumbing"
      pattern: "user=initiating_user"
    - from: "create_mr_for_task"
      to: "build_impact_report_section"
      via: "append before MRCreateRequest"
      pattern: "build_impact_report_section|append_impact_report"
    - from: "test_workflow_mcp_impact_section_parity"
      to: "same helper output"
      via: "规范化字符串相等"
      pattern: "test_workflow_mcp_impact_section_parity"
---

<objective>
把共享 `build_impact_report_section` 挂进 workflow（AICodingNode）与 MCP（merge_request_service）两条建 MR 链路，并挂 `mr_service.create_mr_for_task` 消方言；用 fail-soft 与 D-14 对等哨兵验收（成功标准 2/3）。

Purpose: 报告以建 MR 时服务端重算为准（D-05）；双路径必须同 helper（D-14）。
Output: 三处挂点 + 双链路测试全绿 + VALIDATION Nyquist 收口。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/124-coding-chain/124-CONTEXT.md
@.planning/phases/124-coding-chain/124-RESEARCH.md
@.planning/phases/124-coding-chain/124-PATTERNS.md
@.planning/phases/124-coding-chain/124-02-SUMMARY.md
@server/workflows/nodes/ai/coding.py
@server/mcp_tools/merge_request_service.py
@server/mcp_tools/views.py
@server/mcp_tools/work_item_execution_service.py
@server/workflows/services/mr_service.py
@server/services/code_graph/impact_report.py
@server/tests/workflows/test_coding_pr_target_branch.py
</context>

## Artifacts this phase produces

| Symbol / change | Module | Notes |
|-----------------|--------|-------|
| `_create_mr_for_repo` optional `user` / impact append | `workflows.nodes.ai.coding` | compare=branch_name；base_ref=resolved_target |
| `_finalize_and_notify` 传入 dispatch user | same | 复用 `_resolve_dispatch_user` |
| `create_merge_request(..., user=...)` | `mcp_tools.merge_request_service` | 签名扩展；view 传 `request.user` |
| `create_merge_request(..., user=initiating_user)` | `mcp_tools.work_item_execution_service` | 既有 initiating_user 参数接线，防永久 stub |
| `_draft_from_summary` async 化或 create 路径统一 append | `merge_request_service` | Discretion：优先 create 路径必挂；draft 若同步可改为在 summarize 后 await append |
| `create_mr_for_task` impact append | `workflows.services.mr_service` | user=task.user 或 owner；缺则 stub |
| 双链路绿测 + parity + create_mr_for_task 行为测 | tests | D-14 / D-06 |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Workflow AICodingNode + mr_service 挂点（D-06）</name>
  <files>server/workflows/nodes/ai/coding.py, server/workflows/services/mr_service.py, server/tests/workflows/test_coding_impact_report.py</files>
  <read_first>
    - `server/workflows/nodes/ai/coding.py` `_create_mr_for_repo`（2163–2282）与 `_finalize_and_notify` 调用点（1324–1331）；`_resolve_dispatch_user`（约 1800–1823）
    - `server/workflows/services/mr_service.py` `create_mr_for_task`（122–168）
    - `server/services/code_graph/impact_report.py`（124-02 产出 API）
    - `server/tests/workflows/test_coding_pr_target_branch.py`（直调 `_create_mr_for_repo` 形态）
    - `server/tests/workflows/test_coding_impact_report.py`（Wave 0 桩）
  </read_first>
  <behavior>
    - 成功 mock section → `create_merge_request` 的 description 含 `## 影响面` 四段或注入段
    - `build_impact_report_section` raise/返回 stub → 仍调用平台 `create_merge_request`；description 含 stub 或至少不阻断
    - 外层最后兜底 try/except 保证 helper 若漏吞也不阻断（对齐 pr_cross_reference）
    - 返回值 `"description": body`（dedup 路径）已含影响面段，供后续 cross-ref 使用
    - `test_create_mr_for_task_failsoft_appends_impact`：`create_mr_for_task` 在 helper 成功时 description 含 `## 影响面`；helper raise/返回 stub 时仍调用平台 `create_merge_request` 且不向上抛（D-06/D-09 行为测，非仅 rg）
  </behavior>
  <action>
1. 扩展 `_create_mr_for_repo`：增加可选 `user=None`（或从调用链透传）。在 `body = (...)` 组装之后、`MRCreateRequest(...)` 之前：`section = await build_impact_report_section(repository=repository, user=user, compare=branch_name, base_ref=resolved_target)` 然后 `body = append_impact_report(body, section)`。外层 `except Exception: pass` 最后兜底（D-09）。compare=source 分支；base_ref=已 resolve 的 target（D-03 声明语义）。user 缺失 → helper 内 stub（仍 fail-soft）。（Open Question 1 RESOLVED：user 来自 `_resolve_dispatch_user`。）

2. `_finalize_and_notify` 调用 `_create_mr_for_repo` 时传入 `await self._resolve_dispatch_user(context)`（或等价）；无用户时传 None。

3. `mr_service.create_mr_for_task`：在 `description = build_mr_description(...)` 之后、`MRCreateRequest` 之前 await 同一 helper（compare=branch_name，base_ref=target_branch or repository.default_branch；user 取 task 关联用户若有）。外层 try/except 兜底与 AICodingNode 同姿态。⛔ 不要把 async 逻辑塞进纯函数 `build_mr_description`（PATTERNS）。

4. 去掉 Wave 0 skip，按 `test_coding_pr_target_branch` 风格补丁 git client + `build_impact_report_section`/`run_detect_changes`，转绿 `test_coding_impact_report.py`——**必须含** `test_create_mr_for_task_failsoft_appends_impact`（直调 `create_mr_for_task`：mock helper 成功→description 含段；mock helper 异常→仍 create_merge_request 且不 raise）。

⛔ 不挂 CreatePRNode（deferred）。⛔ 不改 runner / mcp submodule / repo_router_v2（D-16）。
  </action>
  <acceptance_criteria>
    - `cd server && GALAXY_CACHE_WARM_ON_STARTUP=False GRAPH_BUILD_ORPHAN_RECONCILE_ON_STARTUP=False uv run pytest tests/workflows/test_coding_impact_report.py -q --reuse-db` 全绿（含 `test_create_mr_for_task_failsoft_appends_impact`）
    - `rg -n "build_impact_report_section|append_impact_report" server/workflows/nodes/ai/coding.py server/workflows/services/mr_service.py` 两文件均命中
    - `rg -n "test_create_mr_for_task_failsoft_appends_impact" server/tests/workflows/test_coding_impact_report.py` 命中且无 skip
    - `rg -n "build_impact_report_section" server/workflows/nodes/git/pr.py` 无匹配（CreatePRNode 本相位不挂）
  </acceptance_criteria>
  <verify>
    <automated>cd server && GALAXY_CACHE_WARM_ON_STARTUP=False GRAPH_BUILD_ORPHAN_RECONCILE_ON_STARTUP=False uv run pytest tests/workflows/test_coding_impact_report.py -q --reuse-db</automated>
  </verify>
  <done>Workflow + mr_service 挂点 fail-soft 可测（D-06/D-09）</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MCP MR 路径 + D-14 对等哨兵 + VALIDATION 收口</name>
  <files>server/mcp_tools/merge_request_service.py, server/mcp_tools/views.py, server/mcp_tools/work_item_execution_service.py, server/tests/mcp_tools/test_mr_impact_report.py, .planning/phases/124-coding-chain/124-VALIDATION.md</files>
  <read_first>
    - `server/mcp_tools/merge_request_service.py` `_draft_from_summary` / `create_merge_request`（51–170）
    - `server/mcp_tools/views.py` `CreateMergeRequestView.post`（约 2842–2871）与 summarize 调用点 — 对照 PATTERNS §views.py（user 透传，不写 markdown）
    - `server/mcp_tools/work_item_execution_service.py` `create_merge_request(...)` 调用点（约 405–414；已有 `initiating_user` 形参）
    - `.planning/phases/124-coding-chain/124-PATTERNS.md` §merge_request_service + §views.py
    - `server/tests/mcp_tools/test_mr_tools.py`（FakeGitClient 形态）
    - `server/tests/mcp_tools/test_detect_changes_tools.py` 双面哨兵精神（D-14 改编）
    - `server/tests/mcp_tools/test_mr_impact_report.py`（Wave 0 桩）
  </read_first>
  <behavior>
    - MCP create：缺省/显式 description 无 marker → append 段；已含 marker → 不双段
    - impact 失败 → 仍 create_merge_request；description 含 stub 或原描述
    - `test_workflow_mcp_impact_section_parity`：同一 mock envelope / 同一 (repo, compare) 下，workflow 拼装所用 section 与 MCP 拼装所用 section 规范化后字节级或 strip 后一致；失败 stub 文案一致（D-14）
    - view 将 `request.user` 传入 service（ACL）
    - `work_item_execution_service` 将既有 `initiating_user` 以 `user=initiating_user` 传入 `create_merge_request`（缺则 None→stub，仍 fail-soft）
  </behavior>
  <action>
1. 扩展 `create_merge_request` 签名增加 `user=None`（或必填 User）。在填好 description 默认值之后、构造 `MRCreateRequest` 之前：await `build_impact_report_section(repository=..., user=user, compare=source_branch, base_ref=target_branch)` + `append_impact_report`（D-06）。显式传入已含 `## 影响面` 的 description 时幂等跳过（Open Question 2 RESOLVED：仅标记头幂等，不重排章节）。

2. `_draft_from_summary`：若保持同步，则在 `summarize_branch` 拿到 draft 后 await append（推荐）；或将 draft helper 改为 async。保证 draft 描述也带影响面，避免 create 走 draft 回填时缺段——create 路径仍再 append 一次靠幂等安全。

3. `CreateMergeRequestView.post`（及若 draft/summarize 路径需要 user 的调用）传入 `request.user`（PATTERNS §views.py：view 只透传 user，不写影响面 markdown）。更新所有内部/测试调用点签名。

4. `work_item_execution_service.py` 约 405–414 的 `await create_merge_request(...)` **必须**增加 `user=initiating_user`（该函数已接收 `initiating_user`；漏传会导致 ACL stub 永久化）。同步检查同文件其它 `create_merge_request` 调用点一并传 user。

5. 转绿 `test_mr_impact_report.py`：含 fail-soft、幂等、**`test_workflow_mcp_impact_section_parity`**（两侧都调同一 `build_impact_report_section`，断言 section 字符串一致；可再断言两端最终 description 都含该段）。

6. 更新 `124-VALIDATION.md`：Wave 0 Requirements 全勾；Per-Task Status 标绿；`wave_0_complete: true`；相关套件绿后设 `nyquist_compliant: true`。ROADMAP D-27 mcp 漂移计数**不修 submodule**——若 SUMMARY 提及，保持 8 项既有失败记账，本相位不扩大 npm 包面（D-16）。

⛔ 不改 `mcp/` git submodule 内容。⛔ 不改 `repo_router_v2.py`。提交显式路径 only。
  </action>
  <acceptance_criteria>
    - Task verify（单文件）：`test_mr_impact_report.py` 全绿
    - Wave/phase gate（本 task 完成后另跑，不作为本 task verify）：`tests/services/code_graph/test_impact_report.py` + `tests/workflows/test_coding_impact_report.py` + `tests/mcp_tools/test_mr_impact_report.py` 全绿（VALIDATION Quick run）
    - `rg -n "build_impact_report_section|append_impact_report" server/mcp_tools/merge_request_service.py` 命中
    - `rg -n "user=" server/mcp_tools/views.py` 在 CreateMergeRequestView 调用 create_merge_request 处可见 user 传入
    - `rg -n "user=initiating_user" server/mcp_tools/work_item_execution_service.py` 命中
    - `rg -n "test_workflow_mcp_impact_section_parity" server/tests/mcp_tools/test_mr_impact_report.py` 命中且无 skip
    - `rg -n "wave_0_complete: true|nyquist_compliant: true" .planning/phases/124-coding-chain/124-VALIDATION.md` 命中
    - `git diff --name-only` 执行后确认无 `mcp/` 或 `repo_router_v2.py`（D-16 冻结闸）
  </acceptance_criteria>
  <verify>
    <automated>cd server && GALAXY_CACHE_WARM_ON_STARTUP=False GRAPH_BUILD_ORPHAN_RECONCILE_ON_STARTUP=False uv run pytest tests/mcp_tools/test_mr_impact_report.py -q --reuse-db</automated>
  </verify>
  <done>MCP+workflow 双链路对等；fail-soft；work_item 传 user；VALIDATION Nyquist 收口（DIFF-04 / D-14/D-16）</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workflow/MCP shell → impact_report | 壳层只 append；禁止壳内重写 BFS |
| MCP PAT user → repository ACL | user 必须传入；ACL 失败→stub，不绕过私仓数据进 MR |
| MR description → git platform | stub 无凭证/堆栈 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-124-01 | Tampering | N/A | accept | prompt 已在 124-01 钉死 |
| T-124-02 | Information Disclosure | MR description | mitigate | 仅 helper stub；壳层不拼接异常原文 |
| T-124-03 | Information Disclosure | MR body | mitigate | helper 已禁源码；双链路测断言含段即可 |
| T-124-04 | Denial of Service | create_merge_request latency | mitigate | helper 内 30s timeout；fail-soft 测 |
| T-124-05 | Elevation / Spoofing | missing user / ACL | mitigate | view 传 request.user；work_item 传 initiating_user；无用户→stub unavailable |
| T-124-SC | Tampering | package installs | accept | 零新包；mcp submodule 冻结 |
</threat_model>

<verification>
Task 2 verify：`test_mr_impact_report.py` 单文件绿；Wave/phase gate 再跑 VALIDATION Quick run 三文件全绿；D-14 哨兵绿；冻结面 diff 为空；VALIDATION nyquist_compliant。
</verification>

<success_criteria>
- 成功标准 2：workflow + MCP MR 描述自动附四段影响面
- 成功标准 3：报告失败零阻断建 MR
- D-14 对等；D-16 冻结保持；无产品 kill-switch（D-13）
</success_criteria>

<output>
Create `.planning/phases/124-coding-chain/124-03-SUMMARY.md` when done
</output>
