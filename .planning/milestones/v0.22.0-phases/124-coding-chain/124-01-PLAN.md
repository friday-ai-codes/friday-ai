---
phase: 124-coding-chain
plan: 01
type: execute
wave: 1
depends_on: ["124-00"]
files_modified:
  - task/core/knowledge_tools.py
  - task/core/executor.py
  - task/tests/test_knowledge_tools.py
  - task/tests/test_detect_changes_prompt.py
  - task/tests/test_claude_sdk_integration.py
  - task/tests/test_blueprint_context_tools_schema.py
  - task/tests/test_blueprint_context_wait.py
autonomous: true
requirements: [DIFF-03]

must_haves:
  truths:
    - "容器 knowledge 白名单含 mcp__friday-knowledge__detect_changes，schema required=[repository_id, compare]（D-02 / DIFF-03）"
    - "plan/execute 且 knowledge 已挂载时 system prompt 含自查指引；explore/缺 knowledge 不追加（D-01/D-03）"
    - "指引文案明确非阻断：失败/HIGH/CRITICAL 仍继续交付；不改 runner.py commit/push（D-04）"
    - "工具计数断言全部 10→11，无遗漏回归面（Pitfall 3）"
  artifacts:
    - path: "task/core/knowledge_tools.py"
      provides: "detect_changes KNOWLEDGE_TOOL_SCHEMAS 条目"
      contains: "detect_changes"
    - path: "task/core/executor.py"
      provides: "_detect_changes_guidance + _get_system_prompt 条件追加"
      exports: ["_detect_changes_guidance"]
    - path: "task/tests/test_detect_changes_prompt.py"
      provides: "prompt 条件追加绿测"
  key_links:
    - from: "knowledge_allowed_tools"
      to: "KNOWLEDGE_TOOL_SCHEMAS"
      via: "mcp__friday-knowledge__{name} 推导"
      pattern: "detect_changes"
    - from: "_get_system_prompt"
      to: "_detect_changes_guidance"
      via: "knowledge_endpoint and user_token and task_mode in {plan,execute}"
      pattern: "_detect_changes_guidance"
---

<objective>
落地 DIFF-03：编码容器经既有 MCP PAT 白名单可调 `detect_changes`，并在 system prompt 注入非阻断自查指引（per D-01..D-04）。

Purpose: 把 Phase 123 已交付的 server 面真正挂进容器提交决策链路——v1 仅提示不阻断（成功标准 1）。
Output: 第 11 个 knowledge schema + prompt helper + task 侧相关测试全绿。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/124-coding-chain/124-CONTEXT.md
@.planning/phases/124-coding-chain/124-RESEARCH.md
@.planning/phases/124-coding-chain/124-PATTERNS.md
@task/core/knowledge_tools.py
@task/core/executor.py
@server/mcp_tools/serializers.py
@task/tests/test_openspec_prompt.py
</context>

## Artifacts this phase produces

| Symbol | Module | Notes |
|--------|--------|-------|
| `detect_changes` schema dict | `task.core.knowledge_tools.KNOWLEDGE_TOOL_SCHEMAS` | 第 11 工具；对照 DetectChangesRequestSerializer |
| `knowledge_allowed_tools` 自动含新名 | `task.core.knowledge_tools` | 不手写名单 |
| `_detect_changes_guidance` | `task.core.executor.ClaudeRunner` | 静态中文可信文本（T-124-01） |
| `_get_system_prompt` parts-join | `task.core.executor.ClaudeRunner` | openspec + detect_changes 可共存 |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 白名单 + schema（D-02）并把计数断言 10→11</name>
  <files>task/core/knowledge_tools.py, task/tests/test_knowledge_tools.py, task/tests/test_claude_sdk_integration.py, task/tests/test_blueprint_context_tools_schema.py, task/tests/test_blueprint_context_wait.py</files>
  <read_first>
    - `task/core/knowledge_tools.py`（`KNOWLEDGE_TOOL_SCHEMAS` 末条 `await_blueprint_context` 形态；`knowledge_allowed_tools`）
    - `server/mcp_tools/serializers.py` `DetectChangesRequestSerializer`（字段与 required）
    - `.planning/phases/124-coding-chain/124-PATTERNS.md` §knowledge_tools schema entry
    - `task/tests/test_knowledge_tools.py`（`EXPECTED_TOOL_NAMES` / `_LEGACY_*`）
    - `task/tests/test_blueprint_context_tools_schema.py`（`== 10` 位点）
    - `task/tests/test_claude_sdk_integration.py`（约 `:330`）
    - `task/tests/test_blueprint_context_wait.py`（约 `:417-418`）
  </read_first>
  <behavior>
    - `detect_changes` ∈ schema names；`required == ["repository_id", "compare"]`
    - properties 含 `base_ref` / `max_depth` / `min_confidence` / `include_low_confidence` / `limit`
    - `mcp__friday-knowledge__detect_changes` ∈ `knowledge_allowed_tools()`
    - `len(KNOWLEDGE_TOOL_SCHEMAS) == 11` 且 `len(knowledge_allowed_tools()) == 11`
    - 既有 10 个工具名字面量仍在 EXPECTED 列表中（防误删）
  </behavior>
  <action>
按 PATTERNS 在 `KNOWLEDGE_TOOL_SCHEMAS` 追加 `detect_changes` 条目（description 写清：编码完成后、提交前自查；`compare`=当前功能分支；可选 `base_ref` 仅声明；失败/配额用尽继续交付不刷屏——per D-02/D-03）。⛔ 不改 `_make_knowledge_handler` 工厂签名/超时/配额（113 freeze）。⛔ 不改 `mcp/` submodule（D-16）。

同步更新所有硬编码 10 的断言与列表：
- `test_knowledge_tools.py`：`EXPECTED_TOOL_NAMES` 追加 `detect_changes`（可新建 `_NEW_124_TOOL_NAMES`）；docstring「恰 7」若仍误导可改为反映现状或注明历史；新增 schema 形状断言（required + 关键 properties）
- `test_blueprint_context_tools_schema.py` / `test_blueprint_context_wait.py` / `test_claude_sdk_integration.py`：`10` → `11`，并断言新名在 allowed 列表

若模块头注释仍写「7 工具」且本文件已触碰，更新为 11（仅注释）。
  </action>
  <acceptance_criteria>
    - `cd task && uv run pytest tests/test_knowledge_tools.py -k detect_changes -q` 全绿
    - `cd task && uv run pytest tests/test_knowledge_tools.py tests/test_blueprint_context_tools_schema.py tests/test_blueprint_context_wait.py tests/test_claude_sdk_integration.py -q` 全绿
    - `rg -n "len\\(.*knowledge_allowed_tools\\(\\)\\) == 10|KNOWLEDGE_TOOL_SCHEMAS\\) == 10" task/tests` 无匹配（计数已统一 11）
    - `rg -n "repo_router_v2|mcp/" task/core/knowledge_tools.py` 无生产改动指向冻结面
  </acceptance_criteria>
  <verify>
    <automated>cd task && uv run pytest tests/test_knowledge_tools.py tests/test_blueprint_context_tools_schema.py tests/test_blueprint_context_wait.py tests/test_claude_sdk_integration.py -q</automated>
  </verify>
  <done>第 11 工具进白名单；相关计数断言全绿（DIFF-03 / D-02）</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: system prompt 自查指引（D-01/D-03/D-04）</name>
  <files>task/core/executor.py, task/tests/test_detect_changes_prompt.py</files>
  <read_first>
    - `task/core/executor.py` `_get_system_prompt` / `_openspec_guidance`（约 1003–1052）
    - `.planning/phases/124-coding-chain/124-PATTERNS.md` §executor Target pattern
    - `.planning/phases/124-coding-chain/124-RESEARCH.md` Discretionary Defaults（prompt 追加条件）
    - `task/tests/test_detect_changes_prompt.py`（Wave 0 桩）
    - `task/tests/test_openspec_prompt.py`（零回归参照：缺省 knowledge mock 字段时 openspec 行为）
  </read_first>
  <behavior>
    - knowledge_endpoint+user_token+task_mode∈{plan,execute} → prompt 含 detect_changes 自查段
    - task_mode=explore 或不完整 knowledge 配置 → 不追加
    - `_detect_changes_guidance()` 独立 helper；静态字面量无 f-string/外部输入拼接（T-124-01）
    - 文案含参数约定（repository_id / compare / 可选 base_ref）与非阻断语义（D-03/D-04）
    - follow_openspec=True 时 openspec 段与 detect_changes 段可共存；follow_openspec=False 且无 knowledge → 与历史 base 兼容（openspec 测仍绿）
  </behavior>
  <action>
实现 `_detect_changes_guidance`（静态中文；写明：完成修改后结束 turn 前调用 `detect_changes`；`repository_id`=本任务仓；`compare`=当前功能分支；可选 `base_ref`=MR 目标仅声明、勿传工作树 tip 当 base；按清单决定是否继续修补；失败/未索引/配额用尽记录原因继续交付不刷屏；不要因 HIGH/CRITICAL 停止交付——提交由 Runner 负责）。per D-01/D-03/D-04。

改写 `_get_system_prompt` 为 parts-join（勿 early-return 导致 openspec 与 detect_changes 互斥）：base → 可选 openspec → 可选 detect_changes（条件：`self.config.knowledge_endpoint and self.config.user_token and self.config.task_mode in {"plan", "execute"}` —— RESEARCH Discretionary Defaults）。

移除 Wave 0 skip，补齐 MagicMock 字段（`knowledge_endpoint`/`user_token`/`task_mode`/`follow_openspec`），使 `test_detect_changes_prompt.py` 全绿。同步跑 `test_openspec_prompt.py`：若 MagicMock 缺省导致新条件误触发，在 openspec 测里显式设 `knowledge_endpoint=None`/`user_token=None`/`task_mode` 保证零回归。

⛔ 不改 `task/core/runner.py` 任何 commit/push 路径（D-04/D-16）。
  </action>
  <acceptance_criteria>
    - `cd task && uv run pytest tests/test_detect_changes_prompt.py tests/test_openspec_prompt.py -q` 全绿
    - `rg -n "def _detect_changes_guidance" task/core/executor.py` 命中
    - `rg -n "detect_changes" task/core/runner.py` 无匹配（runner 零耦合）
    - `_detect_changes_guidance` 方法体内无 `{` 格式化外部变量拼进 prompt（静态字面量；可用 AST/人工读确认无 f-string 注入用户输入）
  </acceptance_criteria>
  <verify>
    <automated>cd task && uv run pytest tests/test_detect_changes_prompt.py tests/test_openspec_prompt.py -q</automated>
  </verify>
  <done>DIFF-03 prompt 指引落地；非阻断语义可测；openspec 零回归</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| container agent → friday-knowledge MCP (PAT) | 白名单扩容；既有 PAT fail-closed / 配额不变 |
| system prompt 文本 | 仅静态可信指引，无用户/仓库内容拼接 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-124-01 | Tampering | `_detect_changes_guidance` | mitigate | 静态字面量；acceptance 禁外部拼接；独立 helper 测 |
| T-124-02 | Information Disclosure | knowledge_tools logs | accept | 既有 handler redact/脱敏；本 plan 不放宽配额或日志 |
| T-124-03 | Information Disclosure | N/A (task 侧) | accept | 本 plan 不渲染 MR；无源码进 prompt |
| T-124-04 | Denial of Service | knowledge MCP calls | accept | 沿用既有工具超时/配额；不另加 runner 等待（D-10） |
| T-124-05 | Elevation | detect_changes ACL | accept | ACL 仍在 server `run_detect_changes`；容器只透传 PAT |
| T-124-SC | Tampering | package installs | accept | 零新包 |
</threat_model>

<verification>
Task 全套件绿；`runner.py` 无 detect_changes 引用；冻结面未改。
</verification>

<success_criteria>
- 编码容器 allowed_tools 含 `mcp__friday-knowledge__detect_changes`（DIFF-03 / D-02）
- plan/execute + knowledge 挂载时 prompt 含自查指引与参数约定（D-01/D-03）
- 非阻断语义写死在指引中；runner 未改（D-04）
</success_criteria>

<output>
Create `.planning/phases/124-coding-chain/124-01-SUMMARY.md` when done
</output>
