---
phase: 126-process-rename-skills
plan: 02
type: execute
wave: 1
depends_on: ["126-01"]
files_modified:
  - server/codegraph/models.py
  - server/codegraph/migrations/0013_processtrace.py
  - server/services/code_graph/process_trace.py
  - server/services/process_enqueue.py
  - server/durable/tasks.py
  - server/durable/tasks_impl.py
  - server/durable/handlers.py
  - server/friday/settings.py
  - server/tests/codegraph/test_process_trace_model.py
  - server/tests/services/code_graph/test_process_trace.py
  - server/tests/services/code_graph/test_process_enqueue.py
  - server/tests/services/code_graph/test_frozen_surface_126.py
autonomous: true
requirements: [EXEC-01, EXEC-02]
tags: [process-trace, bfs, durable, queue-graph]

# Scope note: ~12 files acceptable — model+migration+BFS kernel+durable enqueue chain; do not split waves.

must_haves:
  truths:
    - "ORM 类名仅为 ProcessTrace（绝无 Process）；纯加表；entry_endpoint JSON 快照无 Endpoint FK（D-01）"
    - "正向 BFS 硬闸 maxDepth=10 / maxBranching=4 / minSteps=3 / edge conf≥0.5；环标 cycle；async 标 boundary:async_dispatch 且不跨越（D-02）"
    - "社区完成后 QUEUE_GRAPH 链式 enqueue；queueing_lock=process:{repo_id}:{branch}；initiated_by_user_id 透传（D-03）"
    - "落库 steps 为主干摘要软引用；community_class=intra_community|cross_community 或 unknown 降级不编造（D-04/D-05）"
  artifacts:
    - path: "server/codegraph/models.py"
      provides: "ProcessTrace 模型"
      contains: "class ProcessTrace"
    - path: "server/codegraph/migrations/0013_processtrace.py"
      provides: "加表迁移"
      contains: "ProcessTrace"
    - path: "server/services/code_graph/process_trace.py"
      provides: "正向 BFS + rebuild/persist + 社区分类"
      exports: ["rebuild_processes", "MAX_DEPTH", "MAX_BRANCHING", "MIN_STEPS", "MIN_CONF"]
    - path: "server/services/process_enqueue.py"
      provides: "enqueue_process_rebuild"
      exports: ["enqueue_process_rebuild"]
    - path: "server/durable/tasks_impl.py"
      provides: "run_process_rebuild + community 成功链式 enqueue"
      contains: "run_process_rebuild"
  key_links:
    - from: "run_community_rebuild success path"
      to: "enqueue_process_rebuild"
      via: "best-effort after community persist"
      pattern: "enqueue_process_rebuild"
    - from: "process_trace.rebuild_processes"
      to: "get_graph_service().get_graph"
      via: "barrel only — no loader/cache import"
      pattern: "get_graph_service"
    - from: "DurableTaskService.defer"
      to: "QUEUE_GRAPH"
      via: "idempotency_key=process:{repo}:{branch}"
      pattern: "process:\\{.*\\}:|idempotency_key=f\\\"process:"
---

<objective>
Wave 1：落地 `ProcessTrace` 模型+迁移、正向 BFS 重建内核、durable `QUEUE_GRAPH` 重建任务，并在社区重建成功路径链式 enqueue（per D-01..D-05）。满足 EXEC-01 与 EXEC-02 的持久化/分类内核（MCP 查询壳在 126-03）。

Purpose: 执行流可重建落库且遵守 GitNexus 同款 BFS 硬闸；刷新语义与 Phase 125 社区同构。
Output: ProcessTrace 表 + process_trace.py + process_enqueue + durable_process_rebuild + 去 skip 后绿的 model/BFS/enqueue 测试。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/126-process-rename-skills/126-CONTEXT.md
@.planning/phases/126-process-rename-skills/126-RESEARCH.md
@.planning/phases/126-process-rename-skills/126-PATTERNS.md
@.planning/phases/126-process-rename-skills/126-01-SUMMARY.md
@server/codegraph/models.py
@server/services/code_graph/community.py
@server/services/code_graph/impact.py
@server/services/community_enqueue.py
@server/durable/tasks_impl.py
</context>

## Artifacts this phase produces

| Symbol / artifact | Module | Introduced in |
|-------------------|--------|---------------|
| `ProcessTrace` + `CommunityClass` TextChoices | `codegraph/models.py` | **126-02** |
| migration `0013_processtrace` | `codegraph/migrations/` | **126-02** |
| `MAX_DEPTH`/`MAX_BRANCHING`/`MIN_STEPS`/`MIN_CONF` / `rebuild_processes` | `services/code_graph/process_trace.py` | **126-02** |
| `enqueue_process_rebuild` | `services/process_enqueue.py` | **126-02** |
| `durable_process_rebuild` / `run_process_rebuild` | durable tasks/handlers | **126-02** |
| settings `CODE_GRAPH_PROCESS_MIN` / `CODE_GRAPH_PROCESS_MAX_CAP` | `friday/settings.py` | **126-02** |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ProcessTrace 模型 + migration（D-01/D-04）</name>
  <files>server/codegraph/models.py, server/codegraph/migrations/0013_processtrace.py, server/tests/codegraph/test_process_trace_model.py</files>
  <read_first>
    - `.planning/phases/126-process-rename-skills/126-CONTEXT.md` D-01 / D-04
    - `server/codegraph/models.py` `SymbolCommunity` + `Endpoint` + `__all__`
    - `server/codegraph/migrations/0011_symbolcommunity.py`（纯加表迁移形态）
    - `server/tests/codegraph/test_process_trace_model.py`（Wave 0 桩）
  </read_first>
  <behavior>
    - ProcessTrace 存在于 codegraph.models；无名为 Process 的 ORM 类
    - unique_together=(repository, branch_name, process_key)
    - entry_endpoint 为 JSONField；模型上无 Endpoint ForeignKey
    - community_class 封闭枚举 intra_community | cross_community（unknown 可用空串或独立 degradation 字段，按 RESEARCH：落库枚举 + 输出 degradation）
  </behavior>
  <action>
按 D-01 在 `server/codegraph/models.py` 新增 **仅** `ProcessTrace`（⛔ 禁止类名 `Process`）：

字段最小集：`repository` FK、`branch_name`（default ""）、`process_key`、`name`、`entry_endpoint` JSON（键含 http_method/url_path/handler_name/file_path/line_number）、`steps` JSON、`community_class`（TextChoices：intra_community / cross_community；无法对账时用空串或约定 sentinel + 由 rebuild 写 degradation，见 D-05）、`step_count`、`built_at_sha`、时间戳；可选 `flags` JSON（cycle/async_boundary/truncated）。`unique_together=(repository, branch_name, process_key)`；indexes 对齐 SymbolCommunity 的 (repository, branch_name)。导出 `__all__`。

生成迁移 `0013_processtrace.py`（下一序号；若并发已有更高序号则用最新+1，文件名含 processtrace）。纯加表，零改既有表。

去掉 `test_process_trace_model.py` 上对应 skip；断言类名、字段、unique_together、无 Endpoint FK、无 Process 类。

观测：无需 caller 日志于模型文件。⛔ 不碰 repo_router_v2 / mcp/。
  </action>
  <acceptance_criteria>
    - `rg -n "class ProcessTrace" server/codegraph/models.py` 命中；`rg -n "^class Process\\b" server/codegraph/models.py` 为 0
    - `rg -n "unique_together.*process_key|process_key" server/codegraph/models.py` 命中
    - `cd server && GALAXY_CACHE_WARM_ON_STARTUP=False GRAPH_BUILD_ORPHAN_RECONCILE_ON_STARTUP=False uv run pytest tests/codegraph/test_process_trace_model.py -q` 全绿（无 skip）
  </acceptance_criteria>
  <verify>
    <automated>cd server && GALAXY_CACHE_WARM_ON_STARTUP=False GRAPH_BUILD_ORPHAN_RECONCILE_ON_STARTUP=False uv run pytest tests/codegraph/test_process_trace_model.py -q</automated>
  </verify>
  <done>ProcessTrace 加表迁移可 apply；模型测试绿；无 Process 撞名类</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 正向 BFS 内核 + 社区分类 + rebuild（D-02/D-04/D-05）</name>
  <files>server/services/code_graph/process_trace.py, server/friday/settings.py, server/tests/services/code_graph/test_process_trace.py</files>
  <read_first>
    - `.planning/phases/126-process-rename-skills/126-CONTEXT.md` D-02 / D-04 / D-05
    - `.planning/phases/126-process-rename-skills/126-RESEARCH.md` Discretionary Defaults + Code Examples Forward BFS
    - `server/services/code_graph/impact.py`（`_edge_score` / predecessors BFS 纪律；本任务改用 successors）
    - `server/services/code_graph/community.py`（get_graph_service + 全删全建 persist 形态）
    - `server/services/code_graph/loader.py` Endpoint→handler 解析语义（file_path+handler_name）
    - Wave 0 `test_process_trace.py`
  </read_first>
  <behavior>
    - MAX_DEPTH=10, MAX_BRANCHING=4, MIN_STEPS=3, MIN_CONF=0.5 为模块 Final/常量
    - 合成图：超过 depth/branching 被截断；短于 minSteps 不落库；低置信边不走
    - 环：steps/flags 含 cycle=true，不静默丢弃
    - async 词表命中：boundary=async_dispatch 且不继续跨越
    - 同社区 → intra_community；跨 ≥2 → cross_community；无法对账 → 仍落库 + unknown/degradation，不编造
    - process_trace.py 源码不 import loader/cache；取图只经 get_graph_service
  </behavior>
  <action>
新建 `server/services/code_graph/process_trace.py`（per D-02/D-04/D-05 + RESEARCH defaults）：

1. 常量：`MAX_DEPTH=10`、`MAX_BRANCHING=4`、`MIN_STEPS=3`、`MIN_CONF=0.5`；`maxProcesses = max(MIN, min(CAP, symbol_count // 10))`，MIN/CAP 读 settings `CODE_GRAPH_PROCESS_MIN`（默认 20）/`CODE_GRAPH_PROCESS_MAX_CAP`（默认 300）。
2. 入口：该仓该分支 `Endpoint` 行 → 映射图上 handler 符号（复用 loader 的 file+name 语义；未解析计入 unresolved_endpoints degradation，⛔ 不造虚拟节点）。
3. 正向 BFS：`graph.successors` + MultiDiGraph 边 attrs 走既有 `_edge_score`/置信度；visited 去重；同 entry→terminal 留最长 + 子串去重；⛔ 禁止 `nx.bfs_layers` 物化全分量。
4. 环显式 `cycle`；async 词表（sync_to_async / defer / delay / apply_async / group_send / create_task / background_runner 等）末端 `boundary: async_dispatch`，v1 不跨越。
5. `process_key`=`{METHOD}:{normalized_path}`；`name`=`{METHOD} {url_path}`（RESEARCH defaults）。
6. `steps[]` 项含 symbol_id?/name/file_path/line?/depth/community_key?；超限标 truncated；过程级 flags。
7. 社区分类：对账 `SymbolCommunity.members` 软引用 → intra / cross；失败 → community_class_unknown / degradation（D-05）。
8. `rebuild_processes`：经 `get_graph_service` 取图；事务内按 (repository, branch_name) 全删全建；`built_at_sha` 对齐索引水位。
9. 日志：`process_rebuild_started/completed/failed`，`category` caller 或 sampling 按生命周期、`component="code_graph"`、`duration_ms`；BFS 循环内禁止 INFO。

在 `friday/settings.py` 增加 PROCESS MIN/CAP knobs（非密钥）。

去掉 `test_process_trace.py` skip，用合成 MultiDiGraph fixture 钉死行为。⛔ 不碰冻结面。
  </action>
  <acceptance_criteria>
    - `rg -n "MAX_DEPTH\\s*=\\s*10|MAX_BRANCHING\\s*=\\s*4|MIN_STEPS\\s*=\\s*3|MIN_CONF\\s*=\\s*0\\.5" server/services/code_graph/process_trace.py` 均命中
    - `rg -n "get_graph_service" server/services/code_graph/process_trace.py` 命中；`rg -n "from services.code_graph.loader|import loader|repo_router_v2" server/services/code_graph/process_trace.py` 为 0（允许注释提及）
    - `cd server && GALAXY_CACHE_WARM_ON_STARTUP=False GRAPH_BUILD_ORPHAN_RECONCILE_ON_STARTUP=False uv run pytest tests/services/code_graph/test_process_trace.py -q` 全绿
  </acceptance_criteria>
  <verify>
    <automated>cd server && GALAXY_CACHE_WARM_ON_STARTUP=False GRAPH_BUILD_ORPHAN_RECONCILE_ON_STARTUP=False uv run pytest tests/services/code_graph/test_process_trace.py -q</automated>
  </verify>
  <done>BFS 四硬闸与环/async/社区分类测试绿；rebuild 可全删全建 ProcessTrace</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: durable enqueue + 社区成功链式（D-03）</name>
  <files>server/services/process_enqueue.py, server/durable/tasks.py, server/durable/tasks_impl.py, server/durable/handlers.py, server/tests/services/code_graph/test_process_enqueue.py, server/tests/services/code_graph/test_frozen_surface_126.py</files>
  <read_first>
    - `.planning/phases/126-process-rename-skills/126-CONTEXT.md` D-03
    - `server/services/community_enqueue.py` 全文
    - `server/durable/tasks.py` `durable_community_rebuild` 壳
    - `server/durable/tasks_impl.py` `run_community_rebuild`
    - `server/durable/handlers.py` community handler 注册
    - Wave 0 `test_process_enqueue.py`
  </read_first>
  <behavior>
    - enqueue_process_rebuild 使用 QUEUE_GRAPH 与 idempotency_key=process:{repository_id}:{branch}
    - initiated_by_user_id 透传；缺省日志记 system；失败 swallow 返回 None
    - run_community_rebuild 成功返回前 best-effort enqueue_process_rebuild；community raise 则不链式
    - run_process_rebuild 绑定用户上下文后调用 rebuild_processes；started/completed/failed + duration_ms
  </behavior>
  <action>
照抄 community 三件套（per D-03 + RESEARCH discretionary：独立 task）：

1. `server/services/process_enqueue.py`：`enqueue_process_rebuild(repository_id, *, branch_name="", initiated_by_user_id=None)` → `DurableTaskService.defer("durable_process_rebuild", ..., queue=QUEUE_GRAPH, idempotency_key=f"process:{repository_id}:{branch}", initiated_by_user_id=...)`；失败 swallow + `enqueue_process_rebuild_failed`。
2. `durable/tasks.py`：`@app.task(name="durable_process_rebuild", queue=QUEUE_GRAPH)` 壳转 `run_process_rebuild`。
3. `durable/tasks_impl.py`：`run_process_rebuild` bind context → `rebuild_processes` → 结构化日志；异常 redact 后 re-raise。在 `run_community_rebuild` **成功路径末尾** best-effort `await enqueue_process_rebuild(...)`（社区空/降级仍 enqueue；若 community 函数 raise 则不执行链式）。
4. `durable/handlers.py`：`register_handler("durable_process_rebuild", ...)`。

更新 `test_frozen_surface_126.py` 确认新文件列入 `_NO_ROUTER_IMPORT` 且 AST 绿。去掉 enqueue 测试 skip。

⛔ 不在索引钩子内联 BFS；⛔ 不直连 loader/cache；⛔ 不进 repo_router_v2。
  </action>
  <acceptance_criteria>
    - `rg -n "durable_process_rebuild|process:\\{repository_id\\}:\\{branch\\}|idempotency_key=f\\\"process:" server/services/process_enqueue.py server/durable/` 命中 enqueue + task
    - `rg -n "enqueue_process_rebuild" server/durable/tasks_impl.py` 命中（社区成功链式）
    - `cd server && GALAXY_CACHE_WARM_ON_STARTUP=False GRAPH_BUILD_ORPHAN_RECONCILE_ON_STARTUP=False uv run pytest tests/services/code_graph/test_process_enqueue.py tests/services/code_graph/test_frozen_surface_126.py -q` 全绿
  </acceptance_criteria>
  <verify>
    <automated>cd server && GALAXY_CACHE_WARM_ON_STARTUP=False GRAPH_BUILD_ORPHAN_RECONCILE_ON_STARTUP=False uv run pytest tests/services/code_graph/test_process_enqueue.py tests/services/code_graph/test_frozen_surface_126.py tests/codegraph/test_process_trace_model.py tests/services/code_graph/test_process_trace.py -q</automated>
  </verify>
  <done>Process 重建可经 QUEUE_GRAPH 去重投递；社区成功后链式；冻结面绿</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| durable worker → DB/graph | 重建任务读图写 ProcessTrace；必须带 initiated_by_user_id |
| community task → process enqueue | 仅成功路径链式；失败不空转 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-126-04 | Denial of Service | process_trace BFS | mitigate | Final caps depth/branching/minSteps/conf/maxProcesses；JSON truncated |
| T-126-03 | Information Disclosure | rebuild logs | mitigate | structlog kv；异常 redact_secrets_in_text；循环内无 INFO |
| T-126-06 | Tampering | frozen surfaces | mitigate | frozen_surface_126 覆盖 process_trace/process_enqueue |
| T-126-05 | Tampering | write APIs | accept | 本 plan 无公共写仓 API；仅 durable 内部 rebuild |
| T-126-SC | Tampering | package installs | accept | 零新包 |
</threat_model>

<verification>
model + process_trace + process_enqueue + frozen_surface_126 全绿；迁移可 apply；工作树无 repo_router_v2 / mcp 改动。
</verification>

<success_criteria>
- EXEC-01：Endpoint→BFS→ProcessTrace 落库，四硬闸+环/async 测试钉死
- EXEC-02 内核：intra/cross/unknown 分类可测
- D-03：QUEUE_GRAPH + process:{repo}:{branch} + initiated_by_user_id + 社区链式
</success_criteria>

<output>
Create `.planning/phases/126-process-rename-skills/126-02-SUMMARY.md` when done
</output>
