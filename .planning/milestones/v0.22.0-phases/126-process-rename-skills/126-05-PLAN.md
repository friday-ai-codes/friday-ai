---
phase: 126-process-rename-skills
plan: 05
type: execute
wave: 4
depends_on: ["126-04"]
files_modified:
  - skills/skills/friday-impact/SKILL.md
  - skills/skills/friday-refactoring/SKILL.md
  - skills/README.md
  - skills/lib/installer.mjs
  - skills/.claude-plugin/plugin.json
  - skills/skills/friday/SKILL.md
  - task/scripts/sync_skills.py
  - task/tests/test_skills_injection.py
  - task/assets/skills/friday-impact/SKILL.md
  - task/assets/skills/friday-refactoring/SKILL.md
autonomous: true
requirements: [SKILL-01]
tags: [skills, friday-impact, friday-refactoring, hash-sync]

must_haves:
  truths:
    - "skills 子模块存在 friday-impact 与 friday-refactoring（zh-CN checklist，不复制工具实现）（D-13）"
    - "task sync_skills SKILL_NAMES 与 test_skills_injection 均含二者；sync 后 assets 与源 sha256 一致（D-14）"
    - "相位验收不依赖 npm publish；分发契约以源目录+hash+注入测为准（D-15）"
    - "本 plan 不修改 repo_router_v2.py 或 mcp/ submodule（D-16）"
  artifacts:
    - path: "skills/skills/friday-impact/SKILL.md"
      provides: "impact-analysis 工作流 skill 正文"
      contains: "friday-impact"
    - path: "skills/skills/friday-refactoring/SKILL.md"
      provides: "refactoring + rename_preview 工作流 skill 正文"
      contains: "rename_preview"
    - path: "task/scripts/sync_skills.py"
      provides: "SKILL_NAMES 扩展"
      contains: "friday-refactoring"
    - path: "task/assets/skills/friday-impact/SKILL.md"
      provides: "sync 镜像（勿手改）"
      contains: "friday-impact"
  key_links:
    - from: "skills/skills/friday-*"
      to: "task/assets/skills/friday-*"
      via: "python task/scripts/sync_skills.py"
      pattern: "SKILL_NAMES"
    - from: "TestSkillsHashConsistency"
      to: "source vs assets sha256"
      via: "parametrize SKILL_NAMES"
      pattern: "friday-impact|friday-refactoring"
---

<objective>
Wave 4：在 `@friday-ai-codes/skills` 子模块新增 `friday-impact` / `friday-refactoring`，扩展容器 `SKILL_NAMES`，跑 sync 并使 sha256 守卫与注入测全绿（per D-13/D-14/D-15）。闭合 SKILL-01。

Purpose: 把 impact / refactoring 工作流经验固化为可对内外分发的 skill，与编码容器同源。
Output: 两 skill 源 + README/installer 文案 7→9 + sync 产物 + hash 测试绿。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/126-process-rename-skills/126-CONTEXT.md
@.planning/phases/126-process-rename-skills/126-RESEARCH.md
@.planning/phases/126-process-rename-skills/126-PATTERNS.md
@.planning/phases/126-process-rename-skills/126-04-SUMMARY.md
@skills/skills/friday-code/SKILL.md
@skills/README.md
@task/scripts/sync_skills.py
@task/tests/test_skills_injection.py
</context>

## Artifacts this phase produces

| Symbol / artifact | Module | Introduced in |
|-------------------|--------|---------------|
| `friday-impact` skill | `skills/skills/friday-impact/` | **126-05** |
| `friday-refactoring` skill | `skills/skills/friday-refactoring/` | **126-05** |
| extended `SKILL_NAMES` | `task/scripts/sync_skills.py` + `task/tests/test_skills_injection.py` | **126-05** |
| synced assets | `task/assets/skills/friday-{impact,refactoring}/` | **126-05** |
| README/installer 7→9 文案 | `skills/README.md` / `installer.mjs` / plugin / friday hub | **126-05** |

<tasks>

<task type="auto">
  <name>Task 1: 子模块新增两 skill 正文 + 安装面文案（D-13）</name>
  <files>skills/skills/friday-impact/SKILL.md, skills/skills/friday-refactoring/SKILL.md, skills/README.md, skills/lib/installer.mjs, skills/.claude-plugin/plugin.json, skills/skills/friday/SKILL.md</files>
  <read_first>
    - `.planning/phases/126-process-rename-skills/126-CONTEXT.md` D-13 / D-15
    - `skills/skills/friday-code/SKILL.md` frontmatter + checklist 形态
    - `skills/README.md` 技能表与「7」计数文案
    - `skills/lib/installer.mjs` `bundledSkills()` 与引导文案中写死数字
    - `skills/.claude-plugin/plugin.json` 与 `skills/skills/friday/SKILL.md` 路由/枚举面
  </read_first>
  <action>
在 skills 子模块源目录新增（D-13；⛔ 不在主仓手写第二份正文）：

1. `skills/skills/friday-impact/SKILL.md`：frontmatter `name: friday-impact`；description 含 impact-analysis 触发词；正文 zh-CN = 触发条件 + 工具顺序 checklist（先 context/staleness → detect_changes/impact/list_processes → 解读 affected_processes）；不复制工具实现。可选短 `references/tool-order.md`。
2. `skills/skills/friday-refactoring/SKILL.md`：`name: friday-refactoring`；description 含 refactoring + rename_preview；checklist 强调先 rename_preview（applied 只读）再自行编辑；失败不阻断交付。
3. 更新 `skills/README.md` 技能表；`installer.mjs` / `plugin.json` / `friday/SKILL.md` 凡写死「7 个」或未列入新技能的枚举面改为 **9** 并列入两新名（installer 若动态扫目录则至少改引导文案）。

npm 正式 bump **不做**（D-15 Deferred）。子模块内改动按仓库既有 submodule 工作流提交；主仓随后更新 pointer（与 Task 2 同提交窗口或显式路径分提交均可，禁止 `git add -A`）。
  </action>
  <acceptance_criteria>
    - `test -f skills/skills/friday-impact/SKILL.md && test -f skills/skills/friday-refactoring/SKILL.md`
    - `rg -n "friday-impact|friday-refactoring" skills/README.md skills/skills/friday-impact/SKILL.md skills/skills/friday-refactoring/SKILL.md` 命中
    - `rg -n "rename_preview|list_processes|detect_changes" skills/skills/friday-impact/SKILL.md skills/skills/friday-refactoring/SKILL.md` 覆盖工具顺序关键词
    - 引导文案不再声称仅 7 个 skill（`rg -n "\\b7\\b" skills/README.md skills/lib/installer.mjs` 人工确认无「7 个技能」过时断言；若数字用于其他含义可保留）
  </acceptance_criteria>
  <verify>
    <automated>python -c "from pathlib import Path; assert Path('skills/skills/friday-impact/SKILL.md').is_file(); assert Path('skills/skills/friday-refactoring/SKILL.md').is_file(); t=Path('skills/skills/friday-refactoring/SKILL.md').read_text(); assert 'rename_preview' in t; print('ok')"</automated>
  </verify>
  <done>两 skill 源与安装面文案就位；无 npm publish 要求</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SKILL_NAMES 扩展 + sync + sha256 守卫（D-14/D-15）</name>
  <files>task/scripts/sync_skills.py, task/tests/test_skills_injection.py, task/assets/skills/friday-impact/SKILL.md, task/assets/skills/friday-refactoring/SKILL.md</files>
  <read_first>
    - `.planning/phases/126-process-rename-skills/126-CONTEXT.md` D-14 / D-15 / D-16
    - `task/scripts/sync_skills.py` 现有 `SKILL_NAMES = ("friday-code", "friday-memory")`
    - `task/tests/test_skills_injection.py` `TestSkillsHashConsistency` 与 SKILL_NAMES
  </read_first>
  <behavior>
    - SKILL_NAMES 在 sync_skills.py 与 test_skills_injection.py 均为 ("friday-code","friday-memory","friday-impact","friday-refactoring")
    - 运行 sync 后 assets 与源逐文件 sha256 一致；注入相关既有测试仍绿
    - 无手改 assets 正文（仅 sync 产物）
  </behavior>
  <action>
1. 扩展 `task/scripts/sync_skills.py` 与 `task/tests/test_skills_injection.py` 的 `SKILL_NAMES` 纳入 `friday-impact` / `friday-refactoring`（D-14；二者为编码期工作流，与 friday-routing「仅 IDE」分流不同——不要误加 routing）。
2. 运行 `python task/scripts/sync_skills.py`（或仓库文档等价命令）生成 `task/assets/skills/friday-impact/**` 与 `friday-refactoring/**`。
3. 确认 `TestSkillsHashConsistency` 对四名 parametrize 全绿。
4. SUMMARY 记录：主仓需包含 submodule pointer（若有变更）+ assets sync；npm publish Deferred（D-15）。
5. ⛔ 不改 `repo_router_v2.py` / `mcp/`；提交只 stage 显式路径（D-16）。
  </action>
  <acceptance_criteria>
    - `rg -n "friday-impact|friday-refactoring" task/scripts/sync_skills.py task/tests/test_skills_injection.py` 均命中
    - `test -f task/assets/skills/friday-impact/SKILL.md && test -f task/assets/skills/friday-refactoring/SKILL.md`
    - `cd task && uv run pytest tests/test_skills_injection.py -q` 全绿
  </acceptance_criteria>
  <verify>
    <automated>cd task && uv run pytest tests/test_skills_injection.py -q</automated>
  </verify>
  <done>SKILL-01：同源 skill 进容器；hash 守卫绿；无 npm 发版阻塞</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| skills submodule → task assets | sync 是唯一复制路径；手改 assets 会破坏 hash |
| skill 正文 → agents | 指引性文案；不执行写仓；rename apply 仍由代理本地编辑 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-126-05 | Tampering | skill 误导写仓 | mitigate | refactoring skill 明确 preview 只读 + 自行编辑；无服务端 apply |
| T-126-06 | Tampering | frozen surfaces | mitigate | 本 plan files_modified 不含 repo_router_v2 / mcp/ |
| T-126-03 | Information Disclosure | skill 正文 | accept | 不含密钥；仅工具名与流程 |
| T-126-SC | Tampering | npm publish | accept | D-15 不在本相位 publish；无新运行时包安装 |
</threat_model>

<verification>
skills 源文件存在；sync 后 hash 测试绿；工作树无冻结面改动。
</verification>

<success_criteria>
- SKILL-01：friday-impact + friday-refactoring 源+assets+SKILL_NAMES+sha256 全绿
- D-15：验收不依赖 npm bump
- D-16：显式路径提交纪律
</success_criteria>

<output>
Create `.planning/phases/126-process-rename-skills/126-05-SUMMARY.md` when done
</output>
