---
phase: 126-process-rename-skills
plan: 03
type: execute
wave: 2
depends_on: ["126-02"]
files_modified:
  - server/services/code_graph_tools.py
  - server/services/code_graph/impact_report.py
  - server/mcp_tools/views.py
  - server/mcp_tools/urls.py
  - server/mcp_tools/serializers.py
  - server/agents/tools/graph_tools.py
  - server/agents/tools/schemas/graph_tools.py
  - server/agents/tools/__init__.py
  - server/tests/services/code_graph/test_affected_processes.py
  - server/tests/services/code_graph/test_process_query.py
  - server/tests/services/code_graph/test_impact_report.py
  - server/tests/mcp_tools/test_schema_snapshot.py
autonomous: true
requirements: [EXEC-02, EXEC-03]
tags: [affected-processes, impact-report, mcp, agents]

# Scope note: ~12 files acceptable — dual-face MCP/agents shell fan-out + shared orchestration; do not split waves.

must_haves:
  truths:
    - "MCP/对话可通过共享编排查询 ProcessTrace（list + get）；壳内无算法分叉；薄壳 call-through 测试强制绿（D-06 / EXEC-02）"
    - "run_list_processes / run_get_process 具备 started/completed/failed + duration_ms + category/component 可观测生命周期"
    - "run_impact 与 run_detect_changes 经单一 assemble_affected_processes helper 回填 affected_processes；无第三套方言（D-07）"
    - "MR ## 影响面 Affected 小节渲染受影响执行流；有数据替换「待 Phase 126」占位句；空则短声明不编造（D-08）"
  artifacts:
    - path: "server/services/code_graph_tools.py"
      provides: "run_list_processes / run_get_process / assemble_affected_processes + 双编排回填"
      contains: "assemble_affected_processes"
    - path: "server/services/code_graph/impact_report.py"
      provides: "受影响执行流段渲染"
      contains: "受影响执行流"
    - path: "server/mcp_tools/views.py"
      provides: "list_processes / get_process 薄壳"
      contains: "list_processes"
    - path: "server/agents/tools/graph_tools.py"
      provides: "对话薄壳 list/get"
      contains: "list_processes"
    - path: "server/tests/services/code_graph/test_process_query.py"
      provides: "list/get 编排 + MCP/@tool call-through 强制测"
      contains: "test_run_list_processes_and_get_process"
  key_links:
    - from: "run_impact / run_detect_changes"
      to: "assemble_affected_processes"
      via: "single helper fills affected_processes"
      pattern: "assemble_affected_processes"
    - from: "McpToolView / @tool"
      to: "run_list_processes / run_get_process"
      via: "thin shell only — verified by test_process_query call-through"
      pattern: "run_list_processes|run_get_process"
    - from: "build_impact_report_section"
      to: "envelope.affected_processes"
      via: "Affected 执行流清单"
      pattern: "affected_processes|受影响执行流"
---

<objective>
Wave 2：双面查询 Process（D-06）、单一 helper 回填 `affected_processes` 进 impact/detect_changes（D-07）、`build_impact_report_section` 消费执行流叙事（D-08）。闭合 EXEC-02 查询面与 EXEC-03。

Purpose: 影响面信封与 MR 描述从空占位变为真实/明确空态叙事。
Output: run_list/get_process + MCP/agents 薄壳 + assemble helper + impact_report 段 + 去 skip 测试绿。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/126-process-rename-skills/126-CONTEXT.md
@.planning/phases/126-process-rename-skills/126-RESEARCH.md
@.planning/phases/126-process-rename-skills/126-PATTERNS.md
@.planning/phases/126-process-rename-skills/126-02-SUMMARY.md
@server/services/code_graph_tools.py
@server/services/code_graph/impact_report.py
@server/mcp_tools/views.py
@server/agents/tools/graph_tools.py
</context>

## Artifacts this phase produces

| Symbol / artifact | Module | Introduced in |
|-------------------|--------|---------------|
| `assemble_affected_processes` | `code_graph_tools.py`（或旁路纯函数模块再 re-export） | **126-03** |
| `run_list_processes` / `run_get_process` | `code_graph_tools.py` | **126-03** |
| MCP `ListProcessesView` / `GetProcessView` + serializers/urls | `mcp_tools/*` | **126-03** |
| agents `list_processes` / `get_process` `@tool` | `agents/tools/graph_tools.py` | **126-03** |
| process_query call-through tests（去 skip） | `tests/.../test_process_query.py` | **126-03** |
| impact_report「受影响执行流」段 | `impact_report.py` | **126-03** |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: assemble_affected_processes + 双编排回填（D-07）</name>
  <files>server/services/code_graph_tools.py, server/tests/services/code_graph/test_affected_processes.py</files>
  <read_first>
    - `.planning/phases/126-process-rename-skills/126-CONTEXT.md` D-07
    - `.planning/phases/126-process-rename-skills/126-RESEARCH.md` assemble_affected_processes Code Example + Pitfall 4
    - `server/services/code_graph_tools.py` `run_impact` / `run_detect_changes` 中 `affected_processes: []` 两处（约 L903 / L1427）
    - Wave 0 `test_affected_processes.py`
  </read_first>
  <behavior>
    - 单一 helper：命中 symbol_id 或 file_path:name ∩ ProcessTrace.steps → 条目含 name/process_key/affected_steps/total_steps/community_class/可选 step
    - 无行或无交集 → []
    - run_impact 与 run_detect_changes 均调用同一 helper；batch detect_changes 一次加载仓分支 Process 集再内存倒排，禁止 N+1
  </behavior>
  <action>
实现 `assemble_affected_processes(*, hit_symbol_ids, hit_file_name_keys, processes) -> list[dict]`（可放在 `code_graph_tools.py` 或 `services/code_graph/affected_processes.py` 再由 tools 导入——禁止第三套方言）。

在 `run_impact` 与 `run_detect_changes` 成功信封处用 helper 结果替换硬编码 `[]`。detect_changes 批量路径：在 batch impact 汇总后一次 `ProcessTrace.objects.filter(repository=..., branch_name=...)`，构建 symbol_id / file:name 倒排再对账（Pitfall 4）。

日志：回填可用 sampling 级 debug（条数），禁止把全 steps 刷 INFO。去掉 affected 测试 skip。⛔ 不碰冻结面。
  </action>
  <acceptance_criteria>
    - `rg -n "assemble_affected_processes" server/services/code_graph_tools.py` ≥ 3（定义 + impact + detect_changes 调用，或定义在旁路则 tools 内 ≥2 调用 + import）
    - `rg -n "affected_processes.: \\[\\]" server/services/code_graph_tools.py` 在成功路径不应再作为最终返回（允许中间初始化）
    - `cd server && GALAXY_CACHE_WARM_ON_STARTUP=False GRAPH_BUILD_ORPHAN_RECONCILE_ON_STARTUP=False uv run pytest tests/services/code_graph/test_affected_processes.py -q` 全绿
  </acceptance_criteria>
  <verify>
    <automated>cd server && GALAXY_CACHE_WARM_ON_STARTUP=False GRAPH_BUILD_ORPHAN_RECONCILE_ON_STARTUP=False uv run pytest tests/services/code_graph/test_affected_processes.py -q</automated>
  </verify>
  <done>impact/detect_changes 信封经单一 helper 回填 affected_processes</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: impact_report 受影响执行流段（D-08）</name>
  <files>server/services/code_graph/impact_report.py, server/tests/services/code_graph/test_impact_report.py</files>
  <read_first>
    - `.planning/phases/126-process-rename-skills/126-CONTEXT.md` D-08
    - `server/services/code_graph/impact_report.py` `_render_affected` / `_render_recommendations`（含「待 Phase 126」占位句约 L285–286）
    - 现有 `test_impact_report.py` 快照/断言风格
  </read_first>
  <behavior>
    - envelope.affected_processes 非空 → Affected 小节出现「受影响执行流」清单（名称 + 可选 step/totalSteps）；Recommendations 不再含「待 Phase 126」字样
    - 空 → 短声明「暂无匹配执行流」或「未构建 Process」类文案；不编造
    - top-N + truncated 纪律继承 124 D-08；仍走 build_impact_report_section 单一入口
  </behavior>
  <action>
扩展 `_render_affected`（或等价私有 helper）消费 `affected_processes`；删除/替换 Recommendations 中 Phase 126 占位句。体积：top-N + truncated 标记。双链路（workflow coding MR + MCP create_merge_request）继续共用同一 formatter——本 task 只改 formatter，不新造第二份。

扩展 `test_impact_report.py`：有数据 / 空态 / 断言源文件不再含「待 Phase 126」。
  </action>
  <acceptance_criteria>
    - `rg -n "待 Phase 126" server/services/code_graph/impact_report.py` 结果为 0
    - `rg -n "受影响执行流|暂无匹配执行流|未构建 Process" server/services/code_graph/impact_report.py` 至少命中空态或清单文案之一
    - `cd server && GALAXY_CACHE_WARM_ON_STARTUP=False GRAPH_BUILD_ORPHAN_RECONCILE_ON_STARTUP=False uv run pytest tests/services/code_graph/test_impact_report.py -q` 全绿
  </acceptance_criteria>
  <verify>
    <automated>cd server && GALAXY_CACHE_WARM_ON_STARTUP=False GRAPH_BUILD_ORPHAN_RECONCILE_ON_STARTUP=False uv run pytest tests/services/code_graph/test_impact_report.py -q</automated>
  </verify>
  <done>MR 影响面段消费真实/空态执行流叙事；占位句消失</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: list/get Process 共享编排 + MCP/对话薄壳（D-06）</name>
  <files>server/services/code_graph_tools.py, server/mcp_tools/views.py, server/mcp_tools/urls.py, server/mcp_tools/serializers.py, server/agents/tools/graph_tools.py, server/agents/tools/schemas/graph_tools.py, server/agents/tools/__init__.py, server/tests/services/code_graph/test_process_query.py, server/tests/mcp_tools/test_schema_snapshot.py</files>
  <read_first>
    - `.planning/phases/126-process-rename-skills/126-CONTEXT.md` D-06 / D-16
    - `.planning/phases/126-process-rename-skills/126-PATTERNS.md` Dual-face + Shared Observability（rename_preview_started 形态）
    - `server/services/code_graph_tools.py` `run_impact` 信封（ok/error_code/staleness/degradation）与既有 `*_started`/`*_completed`/`*_failed` 生命周期
    - `server/mcp_tools/views.py` `ImpactAnalysisView` / `DetectChangesView` 薄壳
    - `server/agents/tools/graph_tools.py` `impact_analysis` `@tool`
    - Wave 0 `test_process_query.py`（必须去 skip，禁止仅靠 rg 符号存在验收）
    - `server/tests/mcp_tools/test_schema_snapshot.py` 漂移记账方式
  </read_first>
  <behavior>
    - run_list_processes / run_get_process 共享编排：参数 repository_id + 可选 branch_name/community_class/symbol_id/limit；信封含 ok/error/staleness/degradation；as_of=built_at_sha；list 默认 cross_community 优先
    - MCP View + agents @tool 只校验/ACL/RetrievalTrace，算法仅在 run_*；call-through 测试强制验证壳→run_*
    - 编排层日志：list_processes_started/completed/failed 与 get_process_started/completed/failed（或等价 snake_case），含 duration_ms、category、component（镜像 rename_preview_* / mcp_tools caller）
    - 不修改 mcp/ submodule；schema snapshot 允许漂移计数更新并在 SUMMARY 记账
  </behavior>
  <action>
新增 `run_list_processes` / `run_get_process`（D-06；两个工具 per RESEARCH discretion）。查询 ORM ProcessTrace；过滤/排序逻辑在编排层；返回信封与 122 同构。

可观测性（强制，对齐 observability-logging.mdc / PATTERNS Shared Observability）：每个 run_* 记录 `*_started` / `*_completed` / `*_failed` + `duration_ms` + `category`（caller）+ `component`（`code_graph` 或既有 mcp_tools 壳侧 component）；高频路径禁止 INFO 刷 steps 全文；凭证脱敏。

MCP：`ListProcessesView` / `GetProcessView`（McpToolView + PAT fail-closed + serializers + urls `tools/list_processes/` / `tools/get_process/`）+ RetrievalTrace + caller 日志 `component` 对齐既有 mcp_tools。

对话：`agents/tools` 注册同名 `@tool`，调用同一 run_*；owner fail-closed；对话链同样写 RetrievalTrace（或既有 chat-chain 等价留痕），禁止仅 MCP 有 trace。

去掉 `test_process_query.py` 全部 Wave 0 skip（强制，非可选）：
- `test_run_list_processes_and_get_process` 断言编排 ORM 过滤/排序/信封
- `test_mcp_list_get_process_call_through` 与 `test_agents_list_get_process_call_through` 断言薄壳只调 run_*（mock/spy），空/坏实现不得仅靠符号存在通过验收

更新 schema snapshot 测试期望（server 面）；SUMMARY 注明 npm `mcp/` 客户端漂移扩大（D-16 / 122 D-27，不修 submodule）。⛔ 不改 repo_router_v2 / mcp/。
  </action>
  <acceptance_criteria>
    - `rg -n "def run_list_processes|def run_get_process" server/services/code_graph_tools.py` 命中
    - `rg -n "list_processes_started|get_process_started|list_processes_completed|get_process_completed" server/services/code_graph_tools.py`（或等价事件名）命中；同文件含 `duration_ms` 与 `category`/`component` 绑定
    - `rg -n "list_processes|get_process" server/mcp_tools/urls.py server/mcp_tools/views.py server/agents/tools/graph_tools.py` 均有命中
    - `rg -n "repo_router_v2" server/mcp_tools/views.py server/services/code_graph_tools.py` 无新增生产 import（既有除外且本相位不引入）
    - `cd server && GALAXY_CACHE_WARM_ON_STARTUP=False GRAPH_BUILD_ORPHAN_RECONCILE_ON_STARTUP=False uv run pytest tests/services/code_graph/test_process_query.py tests/services/code_graph/test_affected_processes.py tests/services/code_graph/test_impact_report.py tests/mcp_tools/test_schema_snapshot.py -q` 全绿或仅记录既有 mcp 包漂移白名单失败（与 D-16 一致时在 SUMMARY 写明）
  </acceptance_criteria>
  <verify>
    <automated>cd server && GALAXY_CACHE_WARM_ON_STARTUP=False GRAPH_BUILD_ORPHAN_RECONCILE_ON_STARTUP=False uv run pytest tests/services/code_graph/test_process_query.py tests/services/code_graph/test_affected_processes.py tests/services/code_graph/test_impact_report.py tests/mcp_tools/test_schema_snapshot.py -q</automated>
  </verify>
  <done>Process 可经 MCP/对话查询且 call-through 测试绿；observability 生命周期齐全；schema snapshot 已跑；不碰 mcp submodule</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → MCP/agents tools | 未可信输入；须 PAT/session + repo ACL |
| orchestration → ORM | 只读查询 ProcessTrace；无写仓 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-126-01 | Information Disclosure | list/get process | mitigate | `_get_indexed_repo` + 既有 ACL；不返回无关仓 |
| T-126-03 | Information Disclosure | RetrievalTrace / logs | mitigate | caller 日志不灌全文 steps；redact |
| T-126-06 | Tampering | mcp submodule | mitigate | 只改 server/mcp_tools；SUMMARY 记账漂移 |
| T-126-02 | Spoofing | empty affected | accept | [] 合法 fail-soft；报告短声明不伪装「已分析无影响」以外的成功叙事 |
| T-126-SC | Tampering | package installs | accept | 零新包 |
</threat_model>

<verification>
affected_processes + process_query + impact_report + schema snapshot 测试绿；MCP/@tool→run_list/get call-through 由 test_process_query 强制；「待 Phase 126」从 impact_report 消失。
</verification>

<success_criteria>
- EXEC-02：MCP/对话可查询 Process（共享编排 + 强制 call-through 测 + 可观测生命周期）
- EXEC-03：双编排回填 + MR 执行流段
- D-07 单一 helper；D-08 占位句移除
</success_criteria>

<output>
Create `.planning/phases/126-process-rename-skills/126-03-SUMMARY.md` when done
</output>
