---
phase: 125-community-summary
plan: 03
type: execute
wave: 2
depends_on: ["125-01", "125-02"]
files_modified:
  - server/services/code_graph/module_summary.py
  - server/services/code_graph/community.py
  - server/durable/tasks_impl.py
  - server/tests/services/code_graph/test_module_summary.py
  - server/tests/services/code_graph/test_community.py
autonomous: true
requirements: [MOD-02, MOD-03]
tags: [module-summary, jaccard, llm, call-source]

must_haves:
  truths:
    - "指纹全等或 Jaccard≥0.8 且旧 summary 非空 → 跳过 LLM；无变更连续 rebuild×2 → LLM 调用数=0（D-05, D-06, D-07）"
    - "指纹/Jaccard 未变但 summary 空白 → 允许重试生成；单社区 LLM 失败不阻断整仓落库（D-08）"
    - "LLM 经 use_call_source(CallSource.MODULE_SUMMARY)，产出 key_files/entry_points/responsibility，默认只喂元数据（D-09 已登记, D-10, D-11）"
    - "unclustered / size&lt;5 不调用 LLM；批处理默认串行（D-04, D-11）"
  artifacts:
    - path: "server/services/code_graph/module_summary.py"
      provides: "agenerate_module_summary + render_module_summary"
      contains: "MODULE_SUMMARY"
    - path: "server/services/code_graph/community.py"
      provides: "Jaccard reconcile + summary_fn 接线"
      contains: "match_communities_greedy|summaries_skipped"
    - path: "server/tests/services/code_graph/test_community.py"
      provides: "test_rebuild_twice_zero_llm 绿"
      contains: "test_rebuild_twice_zero_llm"
  key_links:
    - from: "rebuild_communities"
      to: "agenerate_module_summary"
      via: "仅未 skip 社区调用 summary_fn"
      pattern: "agenerate_module_summary|summary_fn"
    - from: "use_call_source(MODULE_SUMMARY)"
      to: "ModelUsageRecord / 观测"
      via: "call_source 已在 125-01 登记"
      pattern: "MODULE_SUMMARY"
---

<objective>
落地 MOD-02/MOD-03：`module_summary.py` 单轮 LLM（call_source 已登记）、社区指纹/Jaccard 对账跳过，并钉死「无变更 rebuild×2 → LLM=0」验收（D-06…D-12）。

Purpose: 控制摘要成本；为 adapter 注入提供可复用的模块摘要内容。
Output: module_summary 服务 + community reconcile；相关测试去 skip 全绿。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/125-community-summary/125-CONTEXT.md
@.planning/phases/125-community-summary/125-RESEARCH.md
@.planning/phases/125-community-summary/125-PATTERNS.md
@server/repositories/services/charter_service.py
@server/services/code_graph/community.py
@server/agents/call_source.py
@.planning/phases/125-community-summary/125-01-SUMMARY.md
@.planning/phases/125-community-summary/125-02-SUMMARY.md
</context>

## Artifacts this phase produces

| Symbol / artifact | Module | Introduced in |
|-------------------|--------|---------------|
| `agenerate_module_summary` / `render_module_summary` / prompt builder | `services/code_graph/module_summary.py` | **125-03** |
| fingerprint short-circuit + greedy Jaccard reconcile in rebuild | `services/code_graph/community.py` | **125-03**（接线完善） |
| `test_rebuild_twice_zero_llm` 行为绿 | tests | **125-03** |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: module_summary.py LLM helper（元数据-only + call_source）</name>
  <files>server/services/code_graph/module_summary.py, server/tests/services/code_graph/test_module_summary.py</files>
  <read_first>
    - `.planning/phases/125-community-summary/125-CONTEXT.md` D-09, D-10, D-11
    - `.planning/phases/125-community-summary/125-PATTERNS.md` module_summary analog（charter `_agenerate_draft`）
    - `server/repositories/services/charter_service.py` `_agenerate_draft` / `adraft_charter`（`use_call_source` + `build_chat_model(streaming=False)` + `acquire_llm_slot` + fail-soft）
    - `server/agents/call_source.py` 确认 `MODULE_SUMMARY` 已存在（125-01）
    - `server/tests/services/code_graph/test_module_summary.py` Wave 0 桩
  </read_first>
  <behavior>
    - agenerate_module_summary 在 use_call_source(MODULE_SUMMARY) 内 ainvoke
    - prompt/输入仅含路径/符号名/类型/度数启发式，不含源码正文大段
    - LLM 异常 → 返回 None，不抛；事件 module_summary_failed 含 redact 后 error
    - render_module_summary 能把 JSON summary 渲成稳定 markdown/文本
  </behavior>
  <action>
新建 `server/services/code_graph/module_summary.py`（D-10/D-11；**不进** barrel）：

1. 输入：社区 top 成员元数据（file_path / name / symbol_type / 可选 degree）；**默认不喂源码正文**；必要时仅极短 signature 片段且默认关闭。
2. 调用：`ProviderConfigService` / `aresolve` + `build_chat_model(..., streaming=False)` + `acquire_llm_slot`；`with use_call_source(CallSource.MODULE_SUMMARY)`；temperature=0 或 aux 默认。
3. 产出契约：优先解析 JSON `{key_files, entry_points, responsibility}`；失败则文本兜底抽字段。落库形状 Discretion：**summary 存 JSON 文本**；导出 `render_module_summary(summary: str) -> str` 供消费端单一渲染。
4. 观测：`module_summary_started/completed/failed` + `duration_ms`，`component="code_graph"`，`category=sampling`（单社区循环）；异常文本 `redact_secrets_in_text`（T-125-02/03）。
5. 规模门槛：调用方保证不传 size&lt;5 / unclustered；helper 内再断言 size 门槛，过小直接 None。
6. 去 skip 并实现 `test_module_summary.py` 四用例（mock ainvoke / provider）。

⛔ 本 task 仍不得改 `repo_router_v2.py`。⛔ 不得在未登记 call_source 时手写字符串 `"module_summary"` 绕过枚举（必须用 `CallSource.MODULE_SUMMARY`）。
  </action>
  <acceptance_criteria>
    - `cd server && GALAXY_CACHE_WARM_ON_STARTUP=False GRAPH_BUILD_ORPHAN_RECONCILE_ON_STARTUP=False uv run pytest tests/services/code_graph/test_module_summary.py -x` 全绿
    - `rg -n "use_call_source\\(CallSource\\.MODULE_SUMMARY\\)" server/services/code_graph/module_summary.py` 命中
    - `rg -n "MODULE_SUMMARY|module_summary" server/services/code_graph/module_summary.py | grep -v '^#'` ≥ 1
  </acceptance_criteria>
  <verify>
    <automated>cd server && GALAXY_CACHE_WARM_ON_STARTUP=False GRAPH_BUILD_ORPHAN_RECONCILE_ON_STARTUP=False uv run pytest tests/services/code_graph/test_module_summary.py -x</automated>
  </verify>
  <done>LLM 摘要 helper 可测；call_source 正确；元数据-only</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Jaccard 对账接线 + rebuild×2 LLM=0 验收绿</name>
  <files>server/services/code_graph/community.py, server/tests/services/code_graph/test_community.py, server/durable/tasks_impl.py</files>
  <read_first>
    - `.planning/phases/125-community-summary/125-CONTEXT.md` D-06, D-07, D-08
    - `.planning/phases/125-community-summary/125-RESEARCH.md` Pattern 2
    - `server/services/code_graph/community.py`（125-02 已有指纹/greedy 骨架）
    - `server/durable/tasks_impl.py` `run_community_rebuild`（应传入 summary_fn）
    - `server/tests/services/code_graph/test_community.py` 剩余 skip 用例
  </read_first>
  <behavior>
    - 指纹全等且 summary 非空 → short-circuit，不进 Jaccard、不调 LLM
    - Jaccard≥0.8 一对一贪心且 summary 非空 → 复用旧 summary/summary_model/community_key，跳过 LLM
    - summary 空 → 即使成员未变也调用 LLM（重试）
    - 同图 rebuild 两次（第二次成员不变）→ spy/mock LLM 调用次数 == 0
    - unclustered/size&lt;5 从不进入 summary_fn
  </behavior>
  <action>
完善 `rebuild_communities` 对账（D-06/D-07/D-08）：

1. 读旧 `(repository, branch_name)` 行；新社区算 fingerprint。
2. 指纹全等 + 旧 summary 非空 → 复用摘要字段与 `community_key`，`summaries_skipped++`。
3. 其余：成员集贪心最大 Jaccard；≥ `JACCARD_THRESHOLD`(0.8) 且旧 summary 非空 → 复用；否则进 LLM 队列。
4. LLM 队列默认**串行**调用 `agenerate_module_summary`（D-11）；单社区失败留空 summary + failed 事件，继续其它社区。
5. `run_community_rebuild` 默认接线真实 `agenerate_module_summary`（测试可 monkeypatch）。
6. 去 skip：`test_fingerprint_jaccard_skip`、`test_rebuild_twice_zero_llm`、`test_empty_summary_retries`、`test_unclustered_or_small_skips_llm`。
7. `test_rebuild_twice_zero_llm`：合成冻结图 → rebuild 两次 → mock/spy `agenerate_module_summary` 或底层 ainvoke，**第二次（及指纹命中路径）调用次数为 0**；第一次可生成假 summary 写入以便第二次 skip。

观测字段：`communities_total` / `summaries_skipped` / `summaries_generated`（sampling）。

Discretion：Jaccard 阈值保持模块常量 0.8；校准数据可另记 SUMMARY，不阻塞本 plan。
  </action>
  <acceptance_criteria>
    - `cd server && GALAXY_CACHE_WARM_ON_STARTUP=False GRAPH_BUILD_ORPHAN_RECONCILE_ON_STARTUP=False uv run pytest tests/services/code_graph/test_community.py::test_fingerprint_jaccard_skip tests/services/code_graph/test_community.py::test_rebuild_twice_zero_llm tests/services/code_graph/test_community.py::test_empty_summary_retries tests/services/code_graph/test_community.py::test_unclustered_or_small_skips_llm -x` 全绿
    - `test_rebuild_twice_zero_llm` 无 `@pytest.mark.skip`
    - `rg -n "JACCARD_THRESHOLD\\s*=\\s*0\\.8" server/services/code_graph/community.py` 命中
  </acceptance_criteria>
  <verify>
    <automated>cd server && GALAXY_CACHE_WARM_ON_STARTUP=False GRAPH_BUILD_ORPHAN_RECONCILE_ON_STARTUP=False uv run pytest tests/services/code_graph/test_community.py -x</automated>
  </verify>
  <done>MOD-02 铁律测绿；MOD-03 摘要生成与跳过语义完整</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| community metadata → LLM provider | 半可信路径/符号名；禁止源码正文灌入（T-125-02） |
| LLM response → DB summary | 解析 fail-soft；不信任模型输出执行 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-125-02 | Tampering | module_summary prompt/JSON | mitigate | 元数据-only；JSON 解析 fail-soft；render 消毒（`_safe_inline` 同类） |
| T-125-03 | Information Disclosure | module_summary logs | mitigate | redact_secrets_in_text；不记完整上游 body |
| T-125-04 | Denial of Service | LLM batch / prompt size | mitigate | 串行；top 成员截断；size&lt;5 不调 |
| T-125-01 | Information Disclosure | summary 成员来源 | mitigate | 成员来自 get_graph 社区结果，不直查未过滤 Symbol |
| T-125-SC | Tampering | package installs | accept | 零新包 |
</threat_model>

<verification>
`test_module_summary.py` + 完整 `test_community.py`（含 rebuild×2 LLM=0）绿。
</verification>

<success_criteria>
- D-06…D-12 落地
- MOD-02 验收用例自动化通过
- MOD-03 摘要契约 + call_source 使用正确
</success_criteria>

<output>
Create `.planning/phases/125-community-summary/125-03-SUMMARY.md` when done
</output>
