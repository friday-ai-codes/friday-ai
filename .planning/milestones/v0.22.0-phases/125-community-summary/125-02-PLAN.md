---
phase: 125-community-summary
plan: 02
type: execute
wave: 1
depends_on: ["125-01"]
files_modified:
  - server/codegraph/models.py
  - server/codegraph/migrations/0011_symbolcommunity.py
  - server/services/code_graph/community.py
  - server/services/community_enqueue.py
  - server/durable/tasks.py
  - server/durable/tasks_impl.py
  - server/durable/handlers.py
  - server/services/graph_builder.py
  - server/code_relations/tasks.py
  - server/tests/codegraph/test_symbol_community_model.py
  - server/tests/services/code_graph/test_community.py
  - server/tests/services/code_graph/test_community_enqueue.py
autonomous: true
requirements: [MOD-01]
tags: [community, louvain, durable, symbol-community]

must_haves:
  truths:
    - "SymbolCommunity 纯加表落库，members 为 symbol_id 字符串软引用，Symbol 无 community_id/FK/M2M（D-01, D-02）"
    - "Louvain 使用固定 LOUVAIN_SEED + 排序投影，同图两次划分一致；WCC size&lt;5 → unclustered，不发摘要（D-04）"
    - "边/图构建完成后只 enqueue durable_community_rebuild（QUEUE_GRAPH，idempotency_key=community:{repo}:{branch}），钩子不内联 Louvain（D-03）"
    - "worker 经 get_graph_service().get_graph 取图，bind_task_context + initiated_by_user_id（无则 system）"
  artifacts:
    - path: "server/codegraph/models.py"
      provides: "SymbolCommunity 模型"
      contains: "class SymbolCommunity"
    - path: "server/codegraph/migrations/0011_symbolcommunity.py"
      provides: "ADD TABLE only migration"
      contains: "CreateModel"
    - path: "server/services/code_graph/community.py"
      provides: "Louvain + fingerprint + ORM replace"
      contains: "LOUVAIN_SEED"
    - path: "server/services/community_enqueue.py"
      provides: "enqueue_community_rebuild"
      contains: "durable_community_rebuild"
    - path: "server/durable/tasks.py"
      provides: "durable_community_rebuild task shell"
      contains: "durable_community_rebuild"
  key_links:
    - from: "graph_builder.py / code_relations/tasks.py"
      to: "enqueue_community_rebuild"
      via: "invalidate_repository 旁 best-effort defer"
      pattern: "enqueue_community_rebuild"
    - from: "run_community_rebuild"
      to: "get_graph_service().get_graph"
      via: "barrel 取图后 rebuild_communities"
      pattern: "get_graph_service|rebuild_communities"
---

<objective>
落地 MOD-01：`SymbolCommunity` 模型与 migration、`community.py` Louvain/指纹投影与全删全建落库、durable `QUEUE_GRAPH` 任务与双钩子 enqueue（D-01…D-05 中模型/算法/触发部分）。

Purpose: 每仓图可聚成社区并异步刷新；为 125-03 摘要对账提供稳定成员指纹底座。
Output: 新表 + community/enqueue/durable 链路绿；相关 Wave 0 桩去 skip。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/125-community-summary/125-CONTEXT.md
@.planning/phases/125-community-summary/125-RESEARCH.md
@.planning/phases/125-community-summary/125-PATTERNS.md
@.planning/phases/121-graph-base/121-CONTEXT.md
@server/codegraph/models.py
@server/repositories/charter_enqueue.py
@server/durable/tasks.py
@server/services/graph_builder.py
@server/code_relations/tasks.py
@server/services/code_graph/__init__.py
</context>

## Artifacts this phase produces

| Symbol / artifact | Module | Introduced in |
|-------------------|--------|---------------|
| `SymbolCommunity` | `codegraph/models.py` | **125-02** |
| migration `0011_symbolcommunity` | `codegraph/migrations/` | **125-02** |
| `LOUVAIN_SEED` / `JACCARD_THRESHOLD` / `project_undirected` / `member_fingerprint` / `jaccard` / `match_communities_greedy` / `rebuild_communities` | `services/code_graph/community.py` | **125-02** |
| `enqueue_community_rebuild` | `services/community_enqueue.py` | **125-02** |
| `durable_community_rebuild` / `run_community_rebuild` / handler 注册 | `durable/*` | **125-02** |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SymbolCommunity 模型 + migration + 模型测去 skip</name>
  <files>server/codegraph/models.py, server/codegraph/migrations/0011_symbolcommunity.py, server/tests/codegraph/test_symbol_community_model.py</files>
  <read_first>
    - `.planning/phases/125-community-summary/125-CONTEXT.md` D-01, D-02
    - `server/codegraph/models.py` `Symbol`（`branch_name`、`chunk_id` 软引用先例、`unique_together`）
    - `server/codegraph/migrations/0010_branch_name_constraints.py`（depends_on 链）
    - `server/tests/codegraph/test_symbol_community_model.py`（Wave 0 桩）
  </read_first>
  <behavior>
    - SymbolCommunity 字段含 repository FK、branch_name、community_key、algorithm、member_count、members JSON、top_files JSON、member_fingerprint、summary/summary_model/summary_generated_at 可空、built_at_sha、时间戳；unique_together=(repository, branch_name, community_key)
    - Symbol 模型无 community_id / community M2M / FK 到 SymbolCommunity
    - members 项使用 symbol_id 字符串软引用，无 ForeignKey 定义
  </behavior>
  <action>
1. 在 `server/codegraph/models.py` 新增 `SymbolCommunity`（D-01）：字段最小集严格按 CONTEXT；`algorithm` 默认 `"louvain"`；`branch_name` 默认 `""`；`members`/`top_files` JSONField default=list；`unique_together` 或 `UniqueConstraint` 等价。
2. 生成/手写 migration `0011_symbolcommunity.py`：**仅 CreateModel + indexes**，零改既有表/字段（D-01）。depends_on `0010_branch_name_constraints`（或当前最新 leaf）。
3. 去掉 `test_symbol_community_model.py` 的 Wave 0 skip，写真实断言（含 `test_symbol_has_no_community_fk_or_m2m`：检查 Symbol._meta 字段名集合不含 community）。

Discretion（落定）：`community_key` CharField 存指纹派生 key（`fp[:16]`，与 125-03 对账复用约定一致）。`summary` TextField 可空，后续存 JSON 文本。

⛔ 不给 Symbol 加任何 community 关联字段。⛔ 不改 `repo_router_v2.py`。
  </action>
  <acceptance_criteria>
    - `cd server && GALAXY_CACHE_WARM_ON_STARTUP=False GRAPH_BUILD_ORPHAN_RECONCILE_ON_STARTUP=False uv run pytest tests/codegraph/test_symbol_community_model.py -x` 全绿（无 skip）
    - `rg -n "community_id|SymbolCommunity" server/codegraph/models.py | grep -v '^#'`：SymbolCommunity 类存在；Symbol 类体无 `community_id`
    - migration 文件含 `CreateModel` 且不含对 Symbol 的 `AddField`
  </acceptance_criteria>
  <verify>
    <automated>cd server && GALAXY_CACHE_WARM_ON_STARTUP=False GRAPH_BUILD_ORPHAN_RECONCILE_ON_STARTUP=False uv run pytest tests/codegraph/test_symbol_community_model.py -x</automated>
  </verify>
  <done>SymbolCommunity 可迁移落库；软引用契约测绿</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: community.py Louvain/指纹 + durable enqueue 双钩子</name>
  <files>server/services/code_graph/community.py, server/services/community_enqueue.py, server/durable/tasks.py, server/durable/tasks_impl.py, server/durable/handlers.py, server/services/graph_builder.py, server/code_relations/tasks.py, server/tests/services/code_graph/test_community.py, server/tests/services/code_graph/test_community_enqueue.py</files>
  <read_first>
    - `.planning/phases/125-community-summary/125-CONTEXT.md` D-03, D-04, D-05
    - `.planning/phases/125-community-summary/125-RESEARCH.md` Pattern 1/2/3 + Pitfall 1/2/5
    - `.planning/phases/125-community-summary/125-PATTERNS.md` community.py / community_enqueue / durable 三件套
    - `server/services/code_graph/__init__.py`（community **不进** `__all__`，取图仍走 barrel）
    - `server/repositories/charter_enqueue.py`（enqueue 形态）
    - `server/durable/tasks.py` `durable_graph` / `durable_charter_draft` 形参透传
    - `server/durable/tasks_impl.py` `run_charter_draft`（`bind_task_context` + started/completed）
    - `server/services/graph_builder.py` ~invalidate_repository 块
    - `server/code_relations/tasks.py` ~invalidate_repository 块
    - 先 `rg -n "invalidate_repository\\(" server --glob '*.py'` 确认钩子覆盖（RESEARCH A2）
  </read_first>
  <behavior>
    - project_undirected：sorted nodes + sorted undirected edges；不 mutate 冻结 MultiDiGraph
    - louvain_communities(..., seed=LOUVAIN_SEED)；LOUVAIN_SEED 模块常量（建议 42）可被测试导入
    - member_fingerprint = sha256 截断 32；键序无关
    - enqueue：task=durable_community_rebuild，queue=QUEUE_GRAPH，idempotency_key=community:{repo_id}:{branch}（= queueing_lock），透传 initiated_by_user_id
    - 钩子只调用 enqueue，源文件无 louvain_communities 内联
  </behavior>
  <action>
1. 新建 `server/services/code_graph/community.py`（D-04/D-05/D-12 落点）：
   - 常量：`LOUVAIN_SEED`、`JACCARD_THRESHOLD = 0.8`、`MIN_COMMUNITY_SIZE = 5`
   - `project_undirected(g)` → 新 `nx.Graph`（⛔ 不就地改 g）
   - 对 WCC size≥5 跑 `louvain_communities`；过小分量归 `unclustered:{top_dir}`（Discretion）
   - `member_fingerprint` / `jaccard` / `match_communities_greedy` 纯函数（为 125-03 复用；本 plan 可先落库新社区，summary 留空）
   - `rebuild_communities(repository_id, branch_name, *, graph=None, summary_fn=None)`：经 `get_graph_service().get_graph` 取图（测试可注入 graph）；计算社区 → 指纹 →（若提供 summary_fn 则调用，本 plan 默认 None）→ 按 `(repository, branch_name)` **全删全建** 替换行；写 `built_at_sha` 水位；观测 `community_rebuild_started/completed/failed` + `duration_ms`，`component="code_graph"`，高频计数用 `category=sampling`（`communities_total` / `summaries_skipped` / `summaries_generated`）
   - members 每项：`{symbol_id, name, file_path, symbol_type, chunk_id?}`；chunk_id 可选从 `CodeGraph.chunk_evidence` 反查，勿改节点属性契约（Pitfall 7）
   - 截断：top_files / members 入行前设合理上限防 DoS（T-125-04）
   - **不进** barrel `__all__`；**不进** `test_access._INTERNAL_SUBMODULES`

2. 新建 `server/services/community_enqueue.py`：`enqueue_community_rebuild(repository_id, *, branch_name="", initiated_by_user_id=None)` 镜像 charter_enqueue；`DurableTaskService.defer("durable_community_rebuild", {...}, queue=QUEUE_GRAPH, idempotency_key=f"community:{repository_id}:{branch or ''}", initiated_by_user_id=...)`；失败 swallow + `redact_secrets_in_text`。

3. Durable 三件套（新任务名，⛔ 不给 `durable_graph`/`run_graph` 加分支）：
   - `tasks.py`：`@app.task(name="durable_community_rebuild", queue=QUEUE_GRAPH)` 透传 `repository_id` / `branch_name` / `initiated_by_user_id`
   - `tasks_impl.py`：`run_community_rebuild` → `bind_task_context`（actor=`initiated_by_user_id or "system"`）→ 调 `rebuild_communities`
   - `handlers.py`：注册 handler

4. 钩子（D-03）：在 `graph_builder.py` 与 `code_relations/tasks.py` 的 `invalidate_repository` **旁** best-effort `await enqueue_community_rebuild(...)`（同步上下文用 async 桥接与周边一致）；透传能拿到的 user id，否则省略→worker system。⛔ 钩子内禁止 import/调用 louvain。

5. 去 skip 并实现：`test_louvain_seed_stable`、`test_project_undirected_sorted_nodes_edges`、`test_fingerprint_deterministic_order_independent`、`test_get_graph_only_no_loader_import`、enqueue 三测。`test_rebuild_twice_zero_llm` / Jaccard skip / empty summary **仍 skip**（留给 125-03）。

⛔ files_modified 不得含 `repo_router_v2.py` 或 `mcp/`。并发脏改动忽略。
  </action>
  <acceptance_criteria>
    - `cd server && GALAXY_CACHE_WARM_ON_STARTUP=False GRAPH_BUILD_ORPHAN_RECONCILE_ON_STARTUP=False uv run pytest tests/services/code_graph/test_community.py::test_louvain_seed_stable tests/services/code_graph/test_community.py::test_project_undirected_sorted_nodes_edges tests/services/code_graph/test_community.py::test_fingerprint_deterministic_order_independent tests/services/code_graph/test_community.py::test_get_graph_only_no_loader_import tests/services/code_graph/test_community_enqueue.py -x` 全绿
    - `rg -n "louvain_communities" server/services/graph_builder.py server/code_relations/tasks.py` 无匹配
    - `rg -n "enqueue_community_rebuild" server/services/graph_builder.py server/code_relations/tasks.py` 各 ≥1
    - `rg -n "from services\\.code_graph\\.(loader|cache) import|import services\\.code_graph\\.(loader|cache)" server/services/code_graph/community.py` 无匹配
  </acceptance_criteria>
  <verify>
    <automated>cd server && GALAXY_CACHE_WARM_ON_STARTUP=False GRAPH_BUILD_ORPHAN_RECONCILE_ON_STARTUP=False uv run pytest tests/services/code_graph/test_community.py::test_louvain_seed_stable tests/services/code_graph/test_community.py::test_project_undirected_sorted_nodes_edges tests/services/code_graph/test_community.py::test_fingerprint_deterministic_order_independent tests/services/code_graph/test_community.py::test_get_graph_only_no_loader_import tests/services/code_graph/test_community_enqueue.py -x</automated>
  </verify>
  <done>Louvain 落库 + QUEUE_GRAPH 刷新链路可测；钩子只 enqueue</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| index/edge hooks → durable worker | 钩子不可信耗时；只 enqueue |
| durable worker → get_graph | 必须继承 exclusion/权限收口（T-125-01） |
| ORM members JSON | 软引用；截断防膨胀（T-125-04） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-125-01 | Information Disclosure | community.py graph input | mitigate | 只经 get_graph_service；AST 测禁 loader/cache |
| T-125-04 | Denial of Service | SymbolCommunity.members | mitigate | members/top_files 写入前截断 |
| T-125-05 | Elevation of Privilege | enqueue / durable task | mitigate | 无新公共写 API；仅内部 defer + lock 去重 |
| T-125-03 | Information Disclosure | enqueue/worker logs | mitigate | redact_secrets_in_text；不记凭证 |
| T-125-SC | Tampering | package installs | accept | 零新包；只用已 lock 的 networkx |
</threat_model>

<verification>
模型测 + Louvain/指纹/enqueue 测绿；双钩子 grep 通过；diff 不含冻结面路径。
</verification>

<success_criteria>
- MOD-01 可观测：社区行可重建、钩子自动入队
- D-01…D-05（模型/算法/指纹/触发）落地
- 为 125-03 留下 summary_fn / Jaccard 接线面
</success_criteria>

<output>
Create `.planning/phases/125-community-summary/125-02-SUMMARY.md` when done
</output>
