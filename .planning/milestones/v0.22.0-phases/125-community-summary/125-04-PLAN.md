---
phase: 125-community-summary
plan: 04
type: execute
wave: 3
depends_on: ["125-03"]
files_modified:
  - server/services/module_summary_signal.py
  - server/services/process_runtime/blueprint_route.py
  - server/agents/tools/repository_relevance.py
  - server/mcp_tools/views.py
  - server/services/process_runtime/blueprint_research_adapter.py
  - server/services/process_runtime/artifact_injection.py
  - server/tests/services/test_module_summary_signal.py
  - server/tests/services/process_runtime/test_blueprint_route_breakdown.py
  - server/tests/services/process_runtime/test_module_summary_prompt.py
  - server/tests/services/code_graph/test_frozen_surface_125.py
autonomous: true
requirements: [MOD-04]
tags: [adapter, blueprint-route, module-summary-signal, frozen-surface]

must_haves:
  truths:
    - "模块摘要以 adapter evidence / 调用方后信号 / 调研 prompt 三点注入，全程 ⛔ 零改动 repo_router_v2.py 与 mcp/ submodule（D-13）"
    - "blueprint_route evidence 含 module_summaries（默认 []），不破坏 router_base+charter_match+history_match 三分量恒等式（D-14）"
    - "module_summary_signal fail-soft 追加 evidence/reason，默认不改 router_base 分数（D-15）"
    - "调研 prompt 按相关度排序 + 字符/条数预算截断，空段守卫，不全量灌入（D-16）"
  artifacts:
    - path: "server/services/module_summary_signal.py"
      provides: "aapply_module_summary_signal"
      contains: "aapply_module_summary_signal"
    - path: "server/services/process_runtime/blueprint_route.py"
      provides: "_EVIDENCE_KEYS 含 module_summaries"
      contains: "module_summaries"
    - path: "server/services/process_runtime/blueprint_research_adapter.py"
      provides: "模块摘要 prompt 段"
      contains: "模块摘要|module_summar"
    - path: "server/tests/services/code_graph/test_frozen_surface_125.py"
      provides: "冻结面守卫绿"
      contains: "test_phase_125_does_not_touch_repo_router_v2"
  key_links:
    - from: "repository_relevance / RouteRepositoriesView"
      to: "aapply_module_summary_signal"
      via: "charter signal 旁路 best-effort"
      pattern: "aapply_module_summary_signal|module_summary_signal"
    - from: "blueprint_route evidence"
      to: "SymbolCommunity summaries"
      via: "fail-soft load + relevance truncate"
      pattern: "module_summaries"
---

<objective>
落地 MOD-04：三点 adapter 注入（blueprint evidence / 对话·MCP signal / 调研 prompt）+ 相关度排序与 token 预算截断；用冻结面守卫测钉死 ⛔ `repo_router_v2.py` / `mcp/` 零改动（D-13…D-16）。

Purpose: 把模块摘要喂给路由与技术方案消费链，同时遵守 §13.2 冻结面。
Output: signal 模块 + 三注入点接线；冻结面与注入测绿。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/125-community-summary/125-CONTEXT.md
@.planning/phases/125-community-summary/125-RESEARCH.md
@.planning/phases/125-community-summary/125-PATTERNS.md
@server/services/charter_route_signal.py
@server/services/process_runtime/blueprint_route.py
@server/services/process_runtime/blueprint_research_adapter.py
@server/services/process_runtime/artifact_injection.py
@server/agents/tools/repository_relevance.py
@server/mcp_tools/views.py
@.planning/phases/125-community-summary/125-03-SUMMARY.md
</context>

## Artifacts this phase produces

| Symbol / artifact | Module | Introduced in |
|-------------------|--------|---------------|
| `load_module_summaries_for_repo` / `aapply_module_summary_signal` | `services/module_summary_signal.py` | **125-04** |
| `_EVIDENCE_KEYS` + `module_summaries` evidence fill | `process_runtime/blueprint_route.py` | **125-04** |
| research prompt section / budget truncate helper | `blueprint_research_adapter.py` + optional `artifact_injection.py` | **125-04** |
| frozen-surface guard tests green | `test_frozen_surface_125.py` | **125-04** |

**HARD LOCK — files_modified 禁止出现：**
- `server/codegraph/services/repo_router_v2.py`
- `mcp/**`（submodule）

并发 WIP：若工作树已有 `repo_router_v2.py` 脏改动——忽略、不 stage、不整理。

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: module_summary_signal + 蓝图 evidence + 对话/MCP 接线</name>
  <files>server/services/module_summary_signal.py, server/services/process_runtime/blueprint_route.py, server/agents/tools/repository_relevance.py, server/mcp_tools/views.py, server/tests/services/test_module_summary_signal.py, server/tests/services/process_runtime/test_blueprint_route_breakdown.py</files>
  <read_first>
    - `.planning/phases/125-community-summary/125-CONTEXT.md` D-13, D-14, D-15
    - `server/services/charter_route_signal.py` 全文范式（fail-soft / evidence 追加 / 不改 Stage1）
    - `server/services/process_runtime/blueprint_route.py` `_EVIDENCE_KEYS` / `_normalize_evidence` / evidence 组装处（约 L57–71、L173–191、L615–630）与 `_COMPONENT_KEYS`（**勿改**）
    - `server/agents/tools/repository_relevance.py` charter 应用点 `_apply_charter_signal` 旁
    - `server/mcp_tools/views.py` `RouteRepositoriesView` 章程信号块（约 L448–474）
    - `server/tests/services/test_charter_route_signal.py`
    - `server/tests/services/process_runtime/test_blueprint_route_breakdown.py` `test_evidence_missing_keys_get_neutral_defaults`
  </read_first>
  <behavior>
    - aapply_module_summary_signal 异常时原样返回候选列表
    - v1 不修改 router_base / 不加新权重键；只追加 evidence/reason 文本
    - _EVIDENCE_KEYS 含 module_summaries；缺键 normalize 为 []
    - 三分量恒等式测试仍绿
  </behavior>
  <action>
1. 新建 `server/services/module_summary_signal.py`（镜像 charter_route_signal；D-15）：
   - 纯函数加载仓 `SymbolCommunity` 摘要（branch 对齐；空/异常 → []）
   - `aapply_module_summary_signal(candidates, query, ...)` best-effort：按 query↔责任文本轻量相关度排序后截断 top-N，把渲染后的摘要追加到 evidence/reason；**默认不改分数**
   - Discretion：**v1 不做候选补入**（不算缺口）
   - 失败吞掉，返回原 candidates；观测 sampling、脱敏

2. `blueprint_route.py`（D-14）：
   - `_EVIDENCE_KEYS` 增加 `"module_summaries"`
   - `_normalize_evidence` defaults 同步 `[]`
   - candidate evidence 组装处 fail-soft 填入该仓 top 社区摘要（相关度排序 + 预算截断）
   - **禁止**改 `_COMPONENT_KEYS` / `BLUEPRINT_ROUTE_WEIGHTS` / 打分恒等式

3. 接线：
   - `repository_relevance.py`：在 charter signal 应用后旁路调用 `aapply_module_summary_signal`
   - `mcp_tools/views.py` `RouteRepositoriesView`：章程信号之后同样旁路（只改 server 侧 views，⛔ 不改 `mcp/` submodule）

4. 去 skip 实现 `test_module_summary_signal.py`；扩展 `test_blueprint_route_breakdown.py`：缺键默认 `[]`、恒等式仍成立、evidence 可携带 module_summaries。

提交前自检：`git diff --name-only` / staged 列表不得出现 `repo_router_v2.py` 或 `mcp/`。
  </action>
  <acceptance_criteria>
    - `cd server && GALAXY_CACHE_WARM_ON_STARTUP=False GRAPH_BUILD_ORPHAN_RECONCILE_ON_STARTUP=False uv run pytest tests/services/test_module_summary_signal.py tests/services/process_runtime/test_blueprint_route_breakdown.py -x` 全绿
    - `rg -n "module_summaries" server/services/process_runtime/blueprint_route.py` ≥ 2（keys + defaults）
    - `rg -n "aapply_module_summary_signal" server/agents/tools/repository_relevance.py server/mcp_tools/views.py` 各 ≥1
    - `git diff --name-only` 与本 plan staged 集合均不含 `repo_router_v2.py` 且不含 `mcp/` 前缀路径
  </acceptance_criteria>
  <verify>
    <automated>cd server && GALAXY_CACHE_WARM_ON_STARTUP=False GRAPH_BUILD_ORPHAN_RECONCILE_ON_STARTUP=False uv run pytest tests/services/test_module_summary_signal.py tests/services/process_runtime/test_blueprint_route_breakdown.py -x</automated>
  </verify>
  <done>蓝图 evidence + 对话/MCP signal 注入可用且 fail-soft</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 调研 prompt 预算截断 + 冻结面守卫绿</name>
  <files>server/services/process_runtime/blueprint_research_adapter.py, server/services/process_runtime/artifact_injection.py, server/tests/services/process_runtime/test_module_summary_prompt.py, server/tests/services/code_graph/test_frozen_surface_125.py</files>
  <read_first>
    - `.planning/phases/125-community-summary/125-CONTEXT.md` D-16, D-13
    - `server/services/process_runtime/blueprint_research_adapter.py` `_build_prompt` 章程段旁（约 L769–774）
    - `server/services/process_runtime/artifact_injection.py` `render_upstream_artifacts_section` 空段守卫
    - `server/services/code_graph/module_summary.py` `render_module_summary`
    - Wave 0 `test_module_summary_prompt.py` / `test_frozen_surface_125.py`
  </read_first>
  <behavior>
    - 无摘要 → prompt 不含「模块摘要」空标题段
    - 超预算 → 截断并标注 truncated
    - 注入前按相关度排序
    - 社区/摘要/signal 模块不 import repo_router_v2；本相位不修改 mcp/ 树
  </behavior>
  <action>
1. 在 `blueprint_research_adapter._build_prompt`（或抽出 helper 放 `artifact_injection`）注入 `## 模块摘要` 段（D-16）：
   - 空 → 返回 `""`（空段守卫，照抄 upstream artifacts）
   - 非空：query↔摘要相关度排序 → per-repo **2000 字符或 top 5 社区（先到为准）** 截断 → 超限注明 truncated
   - 使用 `render_module_summary`；半可信字段消毒
   - fail-soft：加载失败不影响调研主流程

2. 去 skip 实现 `test_module_summary_prompt.py` 三用例。

3. 实现 `test_frozen_surface_125.py`（D-13）：
   - AST/静态：`community.py` / `module_summary.py` / `module_summary_signal.py` / 本相位改动的 adapter 文件 **不** import `codegraph.services.repo_router_v2` 或 `repo_router_v2`
   - 断言：`mcp/` 目录下无本相位业务改动依赖（守卫断言生产代码不写入 submodule；允许 `mcp_tools/views.py`）
   - 可选：若 `git` 可用，对「相对于 merge-base 的 diff 文件列表」断言不含 `server/codegraph/services/repo_router_v2.py` 与 `mcp/` 前缀——无 git 时 skip 该分支不 fail 全套

Discretion：RepoSummaryBuilder 顺手并入 top 社区名——推荐但**不做不算缺口**，本 task 不强制。
  </action>
  <acceptance_criteria>
    - `cd server && GALAXY_CACHE_WARM_ON_STARTUP=False GRAPH_BUILD_ORPHAN_RECONCILE_ON_STARTUP=False uv run pytest tests/services/process_runtime/test_module_summary_prompt.py tests/services/code_graph/test_frozen_surface_125.py -x` 全绿
    - `rg -n "模块摘要|module_summar" server/services/process_runtime/blueprint_research_adapter.py server/services/process_runtime/artifact_injection.py` ≥ 1
    - 工作树/提交文件列表仍不含 `repo_router_v2.py` 与 `mcp/` submodule 路径
  </acceptance_criteria>
  <verify>
    <automated>cd server && GALAXY_CACHE_WARM_ON_STARTUP=False GRAPH_BUILD_ORPHAN_RECONCILE_ON_STARTUP=False uv run pytest tests/services/test_module_summary_signal.py tests/services/process_runtime/test_blueprint_route_breakdown.py tests/services/process_runtime/test_module_summary_prompt.py tests/services/code_graph/test_frozen_surface_125.py -x</automated>
  </verify>
  <done>三点注入完整；冻结面守卫绿；MOD-04 闭环</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB summaries → router evidence / research prompt | 半可信 LLM 文本；须排序截断 + 消毒 |
| MCP/对话 API → signal apply | 鉴权沿用既有 RouteRepositories / tool；signal 只读增强 |
| frozen router surface | 禁止本相位写入 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-125-01 | Information Disclosure | evidence 来源 | mitigate | 摘要来自经 get_graph 构建的社区行；exclusion 已在图层 |
| T-125-02 | Tampering | prompt/evidence 注入 | mitigate | render 消毒；预算截断；不进 Stage1 |
| T-125-03 | Information Disclosure | signal/route logs | mitigate | redact；不刷 prompt 全文 INFO |
| T-125-04 | Denial of Service | prompt 体积 | mitigate | 2000 字符 / top-5 截断 + truncated 标记 |
| T-125-05 | Elevation of Privilege | MCP route view | mitigate | 无新写 API；只读 apply；沿用既有鉴权 |
| T-125-SC | Tampering | package installs | accept | 零新包；冻结面守卫防误改 |
</threat_model>

<verification>
signal + blueprint breakdown + prompt + frozen surface 全绿；`git diff --name-only` 无冻结路径。
</verification>

<success_criteria>
- D-13…D-16 全部落地
- MOD-04 三点注入可测
- repo_router_v2.py / mcp/ 全程未改
</success_criteria>

<output>
Create `.planning/phases/125-community-summary/125-04-SUMMARY.md` when done
</output>
