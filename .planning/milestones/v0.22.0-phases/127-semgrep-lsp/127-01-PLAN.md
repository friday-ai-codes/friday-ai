---
phase: 127-semgrep-lsp
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - server/tests/services/code_graph/test_semgrep_scan.py
  - server/tests/services/code_graph/test_security_scan_report.py
  - server/tests/services/code_graph/test_semgrep_enqueue.py
  - server/tests/services/code_graph/test_semgrep_app_token.py
  - server/tests/codegraph/test_security_finding_model.py
  - server/codegraph/lsp/tests/test_orphan_reap.py
  - server/tests/codegraph/test_lsp_defaults_unchanged.py
  - server/tests/services/code_graph/test_frozen_surface_127.py
  - server/tests/services/code_graph/test_dockerfile_semgrep_lsp_layers.py
  - server/tests/codegraph/test_revisit_impact03.py
  - server/tests/workflows/test_coding_security_scan.py
  - server/tests/mcp_tools/test_mr_security_scan.py
  - server/tests/fixtures/semgrep/sample_findings.json
  - .planning/phases/127-semgrep-lsp/127-VALIDATION.md
autonomous: true
requirements: [TAINT-01, TAINT-02, TAINT-03, LSP-01]
tags: [semgrep, lsp, wave0, frozen-surface, nyquist]

must_haves:
  truths:
    - "Wave 0 测试桩可被 pytest 收集，覆盖 Semgrep CLI/fail-open（D-01..D-04）、SecurityFinding（D-05）、MR ## 安全扫描 advisory/CE/nosemgrep/token（D-06..D-10）、Dockerfile 层（D-01/D-11）、LSP 默认 False（D-12）、孤儿收割（D-14）、基准/IMPACT-03（D-15..D-17）、冻结面（D-18）"
    - "127-VALIDATION.md Per-Task Map 恰好对应 127-01..05 五个 plan，无幽灵第六 plan"
    - "本 plan 不改生产业务逻辑；⛔ 不 stage repo_router_v2.py、⛔ 不改 mcp/（D-18）"
  artifacts:
    - path: "server/tests/services/code_graph/test_semgrep_scan.py"
      provides: "TAINT-01 CLI/fail-open 验收桩"
      contains: "test_semgrep_argv_uses_baseline_commit_merge_base"
    - path: "server/tests/services/code_graph/test_security_scan_report.py"
      provides: "TAINT-02/03 段文案验收桩"
      contains: "test_security_section_has_ce_disclaimer"
    - path: "server/tests/services/code_graph/test_frozen_surface_127.py"
      provides: "D-18 冻结面守卫"
      contains: "test_phase_127_does_not_touch_repo_router_v2"
    - path: "server/tests/fixtures/semgrep/sample_findings.json"
      provides: "假 Semgrep JSON fixture"
      contains: "check_id"
  key_links:
    - from: "127-VALIDATION.md Wave 0"
      to: "九个新测试文件 + fixture"
      via: "节点名与 RESEARCH Validation Architecture 一一对应"
      pattern: "test_semgrep_argv|test_security_section|test_orphan|test_lsp_defaults"
    - from: "test_frozen_surface_127.py"
      to: "semgrep_scan / security_scan_report / semgrep_enqueue / orphan_reap"
      via: "AST 禁止 import repo_router_v2；禁止改 mcp/"
      pattern: "repo_router_v2"
---

<objective>
Wave 0（Nyquist）：建齐 Phase 127 全部自动化验收测试桩与 Semgrep JSON fixture，并刷新 VALIDATION 表与最终五 plan ID 对齐。覆盖 TAINT-01/02/03、LSP-01 与 D-01..D-18 验收节点名（实现由 127-02..05 去 skip）。

Purpose: 后续每个 task 的 `<automated>` 必须命中可收集节点；禁止先写生产逻辑再补测试名。
Output: Wave 0 测试文件（skip 桩）+ fixture + VALIDATION 与 127-01..05 对齐。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/127-semgrep-lsp/127-CONTEXT.md
@.planning/phases/127-semgrep-lsp/127-RESEARCH.md
@.planning/phases/127-semgrep-lsp/127-PATTERNS.md
@.planning/phases/127-semgrep-lsp/127-VALIDATION.md
@server/tests/services/code_graph/test_frozen_surface_125.py
@server/tests/services/code_graph/test_impact_report.py
@server/tests/services/code_graph/test_process_enqueue.py
</context>

## Artifacts this phase produces

> Phase-level inventory（本 plan 只落测试桩；实现符号由 127-02..05 落地）

| Symbol / artifact | Module | Introduced in |
|-------------------|--------|---------------|
| Wave 0 test stubs + `sample_findings.json` | `server/tests/**` | **127-01** |
| Dockerfile Semgrep `/opt/semgrep` + Node22 + volar/ts + Go/gopls | `server/Dockerfile` | 127-02 |
| `SEMGREP_*` settings + `SettingKeys.SEMGREP_APP_TOKEN` / `CONCURRENCY_SCAN_MAX` | `friday/settings.py` / `system/models.py` | 127-02 |
| `set_semgrep_app_token` / `get_semgrep_app_token` | `services/code_graph/semgrep_token.py` | 127-02 |
| `SecurityFinding` model + migration `0014_*` | `codegraph/models.py` | 127-02 |
| `ensure_worktree_for_scan` | `services/repo_mirror.py` | 127-03 |
| `run_semgrep_scan` / CLI argv builder / finding persist | `services/code_graph/semgrep_scan.py` | 127-03 |
| `QUEUE_SCAN` + `scan_slot_lock` / `ascan_lock` | `durable/queues.py` / `concurrency.py` | 127-03 |
| `enqueue_semgrep_scan` + `durable_semgrep_scan` + `run_semgrep_scan` (impl) | `semgrep_enqueue.py` + durable | 127-03 |
| `SECURITY_SECTION_MARKER` / `build_security_scan_section` / `append_security_scan` | `security_scan_report.py` | 127-04 |
| Dual hang-points + async MR body patch | coding / mr_service / merge_request_service | 127-04 |
| `reap_orphan_lsp_processes` / `lsp_process_reaped` | `codegraph/lsp/orphan_reap.py` | 127-05 |
| `measure_lsp_baseline` / `revisit_impact03_samples` commands | `codegraph/management/commands/` | 127-05 |
| Baseline JSON + IMPACT-03 revisit/honest-defer notes | `.planning/phases/127-semgrep-lsp/` + SUMMARY | 127-05 |

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 验收测试桩 + Semgrep fixture</name>
  <files>server/tests/services/code_graph/test_semgrep_scan.py, server/tests/services/code_graph/test_security_scan_report.py, server/tests/services/code_graph/test_semgrep_enqueue.py, server/tests/services/code_graph/test_semgrep_app_token.py, server/tests/codegraph/test_security_finding_model.py, server/codegraph/lsp/tests/test_orphan_reap.py, server/tests/codegraph/test_lsp_defaults_unchanged.py, server/tests/services/code_graph/test_frozen_surface_127.py, server/tests/services/code_graph/test_dockerfile_semgrep_lsp_layers.py, server/tests/codegraph/test_revisit_impact03.py, server/tests/workflows/test_coding_security_scan.py, server/tests/mcp_tools/test_mr_security_scan.py, server/tests/fixtures/semgrep/sample_findings.json</files>
  <read_first>
    - `.planning/phases/127-semgrep-lsp/127-VALIDATION.md` §Wave 0 Requirements / Per-Task Verification Map / Threat → Test Cross-Ref
    - `.planning/phases/127-semgrep-lsp/127-RESEARCH.md` §Validation Architecture + Security Domain (T-127-01..05)
    - `.planning/phases/127-semgrep-lsp/127-PATTERNS.md` Hard Locks + Tests analog
    - `server/tests/services/code_graph/test_frozen_surface_125.py`（AST 冻结面完整形态）
    - `server/tests/services/code_graph/test_impact_report.py`（幂等 append + stub 脱敏断言形态）
    - `server/tests/services/code_graph/test_process_enqueue.py`（enqueue queue/lock/idempotency 形态）
  </read_first>
  <action>
新建下列测试文件与 fixture。每个行为用例暂为 `pytest.fail("Wave 0 桩")` 并挂 `@pytest.mark.skip(reason="Wave 0 桩：由 127-02/03/04/05 落地")`。冻结面与 defaults 文件可实现真实静态断言而不 skip。⛔ 不改生产业务模块；⛔ 不 stage `repo_router_v2.py` / `mcp/`（D-18）。提交只 stage 本 task 列出路径。

`tests/fixtures/semgrep/sample_findings.json`：假 `semgrep scan --json` 形态，至少含 `results[]` 条目字段 `check_id` / `path` / `start.line` / `extra.severity` / `extra.message` / fingerprint 等价键；可含一条将被 nosemgrep 语义忽略的注释说明（文档用）。

`tests/services/code_graph/test_semgrep_scan.py`（归属 127-03；TAINT-01；D-01..D-04；T-127-02/03）：
- `test_semgrep_argv_uses_baseline_commit_merge_base`（argv 含 `scan`、`--baseline-commit`=&lt;merge-base&gt;；禁止 `ci` 子命令；bin 来自 settings `SEMGREP_BIN`）
- `test_semgrep_never_imports_semgrep_module`（静态读 `semgrep_scan.py` 无 `import semgrep`）
- `test_semgrep_fail_open_on_timeout_and_unavailable`（timeout/mirror/CLI → 稳定 `error_code`；不 raise 到建 MR）
- `test_semgrep_packs_from_semgrep_configs_setting`（`SEMGREP_CONFIGS` CSV → 多个 `--config`）

`tests/services/code_graph/test_security_scan_report.py`（归属 127-04；TAINT-02/03；D-06..D-09；T-127-01/05）：
- `test_append_security_scan_idempotent`（标记头 `## 安全扫描`；已含则不重复；不覆盖 `## 影响面`）
- `test_security_section_lists_severity_advisory`（ERROR/WARNING/INFO 分级展示；无 blocking raise）
- `test_security_section_has_ce_disclaimer`（CE 函数内 taint 边界文案）
- `test_security_section_nosemgrep_mention`（段内/文档句说明 `nosemgrep`）
- `test_stub_omits_token_stack_and_abs_paths`（token/Traceback/绝对路径不得进 section）
- `test_pro_token_configured_line_without_hype`（有 token →「Pro 能力已启用」类短句且不夸大；空 token → 纯 CE）

`tests/services/code_graph/test_semgrep_enqueue.py`（归属 127-03；D-02/D-04）：
- `test_enqueue_uses_queue_scan_and_slot_lock`（`QUEUE_SCAN`；`idempotency_key=semgrep:{repo}:{mr_key}`；`scan-slot-*`；N=2）
- `test_enqueue_passes_initiated_by_user_id`
- `test_enqueue_failure_returns_none_not_raise`

`tests/codegraph/test_security_finding_model.py`（归属 127-02；D-05）：
- `test_security_finding_required_fields`
- `test_security_finding_has_no_symbol_fk`
- `test_security_finding_message_expected_redacted_at_write_path`（约定：写入路径过 `redact_secrets_in_text`——实现期断言 helper/service）

`tests/services/code_graph/test_semgrep_app_token.py`（归属 127-02；D-09；T-127-01）：
- `test_set_semgrep_app_token_encrypts_round_trip`（set → DB `is_encrypted=True` 且 value≠明文 → get 还原）
- `test_empty_token_means_ce`（空/缺失 → get `""`）

`tests/workflows/test_coding_security_scan.py`（归属 127-04；TAINT-02；D-06）：
- `test_coding_create_mr_appends_security_scan_and_enqueues`（coding 缝调 append + enqueue；不阻断建 MR）
- `test_coding_security_scan_shell_failure_is_fail_open`

`tests/mcp_tools/test_mr_security_scan.py`（归属 127-04；TAINT-02；D-06）：
- `test_mcp_create_mr_appends_security_scan_and_enqueues`
- `test_mr_service_security_scan_shell_failure_is_fail_open`（若 mr_service 测放本文件；否则同构节点名）

`tests/services/code_graph/test_dockerfile_semgrep_lsp_layers.py`（归属 127-02；D-01/D-11；LSP-01）：
- `test_dockerfile_installs_semgrep_outside_server_venv`（含 `/opt/semgrep` 或等价独立 install；无 `pyproject`/`uv.lock` 装 semgrep）
- `test_dockerfile_installs_node_go_volar_gopls`（Node/gopls/`@vue/language-server` 字面量或安装指令）

`tests/codegraph/test_lsp_defaults_unchanged.py`（归属 127-02/05；D-12/D-16）：
- `test_volar_and_gopls_backend_defaults_false`（`settings.VOLAR_BACKEND_ENABLED` / `GOPLS_BACKEND_ENABLED` 默认 False）

`codegraph/lsp/tests/test_orphan_reap.py`（归属 127-05；D-14；T-127-04）：
- `test_reap_orphan_lsp_processes_counts_and_best_effort`
- `test_supervisor_stop_invoked_on_index_path_finally`（或静态/契约：索引路径 `finally` 调 stop + reap）

`tests/codegraph/test_revisit_impact03.py`（归属 127-05；D-17）：
- `test_revisit_impact03_zero_samples_honest_defer_path`
- `test_revisit_impact03_positive_samples_invokes_four_branches`

`tests/services/code_graph/test_frozen_surface_127.py`（全程；D-18）：
- 照抄 125 形态：`_NO_ROUTER_IMPORT` 含 `services/code_graph/semgrep_scan.py`、`services/code_graph/security_scan_report.py`、`services/code_graph/semgrep_enqueue.py`、`codegraph/lsp/orphan_reap.py`（文件尚未存在时 `path.is_file()` continue）
- `test_phase_127_does_not_touch_repo_router_v2`
- `test_phase_127_does_not_modify_mcp_submodule`
- 额外：`test_semgrep_modules_do_not_import_graph_service_hot_path`（禁止从上述模块 import `GraphService` / `get_graph_service` 热路径——零耦合）
  </action>
  <acceptance_criteria>
    - `cd server && GALAXY_CACHE_WARM_ON_STARTUP=False GRAPH_BUILD_ORPHAN_RECONCILE_ON_STARTUP=False uv run pytest tests/services/code_graph/test_semgrep_scan.py tests/services/code_graph/test_security_scan_report.py tests/services/code_graph/test_semgrep_enqueue.py tests/services/code_graph/test_semgrep_app_token.py tests/codegraph/test_security_finding_model.py codegraph/lsp/tests/test_orphan_reap.py tests/codegraph/test_lsp_defaults_unchanged.py tests/services/code_graph/test_frozen_surface_127.py tests/services/code_graph/test_dockerfile_semgrep_lsp_layers.py tests/codegraph/test_revisit_impact03.py tests/workflows/test_coding_security_scan.py tests/mcp_tools/test_mr_security_scan.py --collect-only -q` 收集到上方全部节点名
    - `sample_findings.json` 含 `check_id` 与 `extra.severity`（或等价 severity 字段）
    - 冻结面文件含字面量 `repo_router_v2` 与 `semgrep_scan`
  </acceptance_criteria>
  <verify>
    <automated>cd server && GALAXY_CACHE_WARM_ON_STARTUP=False GRAPH_BUILD_ORPHAN_RECONCILE_ON_STARTUP=False uv run pytest tests/services/code_graph/test_semgrep_scan.py tests/services/code_graph/test_security_scan_report.py tests/services/code_graph/test_semgrep_enqueue.py tests/services/code_graph/test_semgrep_app_token.py tests/codegraph/test_security_finding_model.py codegraph/lsp/tests/test_orphan_reap.py tests/codegraph/test_lsp_defaults_unchanged.py tests/services/code_graph/test_frozen_surface_127.py tests/services/code_graph/test_dockerfile_semgrep_lsp_layers.py tests/codegraph/test_revisit_impact03.py tests/workflows/test_coding_security_scan.py tests/mcp_tools/test_mr_security_scan.py --collect-only -q</automated>
  </verify>
  <done>全部 Wave 0 节点可收集；生产代码未改；冻结面与 fixture 就位</done>
</task>

<task type="auto">
  <name>Task 2: VALIDATION 与五 plan 编号对齐</name>
  <files>.planning/phases/127-semgrep-lsp/127-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/127-semgrep-lsp/127-VALIDATION.md` 全文
    - 本目录即将落地的 `127-01`..`127-05` plan frontmatter（wave / requirements）
  </read_first>
  <action>
更新 `127-VALIDATION.md`（与现行五 plan 对齐，禁止幽灵第六 plan）：

- Per-Task Verification Map **恰好 5 行主 plan**：`127-01` / `127-02` / `127-03` / `127-04` / `127-05`；不得出现 `127-06`
- 每行 Wave / Requirement / Automated Command 与 plan frontmatter 一致：
  - `127-01` → Wave **0**，Requirement **TAINT-01..03 + LSP-01**，Command = Wave 0 collect-only
  - `127-02` → Wave **1**，Requirement **TAINT-01 + TAINT-03(key) + LSP-01(image/defaults)**，Command = dockerfile layers + SecurityFinding + lsp defaults
  - `127-03` → Wave **2**，Requirement **TAINT-01**，Command = test_semgrep_scan + test_semgrep_enqueue
  - `127-04` → Wave **3**，Requirement **TAINT-02 + TAINT-03**，Command = test_security_scan_report + test_coding_security_scan + test_mr_security_scan
  - `127-05` → Wave **4**，Requirement **LSP-01**，Command = orphan_reap + revisit_impact03 + defaults；`depends_on: [127-02, 127-04]`
- Wave 0 Requirements 文件列表与 Task 1 一致（含 `test_semgrep_app_token.py`、`test_coding_security_scan.py`、`test_mr_security_scan.py`）；frontmatter 保持 `wave_0_complete: false` 直至本 plan SUMMARY 后由 executor 勾选；计划落地后可将 `nyquist_compliant` 标 true（若 Wave 0 任务全部就位）
- Sampling Rate 标明 quick（task）vs full（wave/phase）
  </action>
  <acceptance_criteria>
    - `127-VALIDATION.md` 含字面量 `127-01`、`127-02`、`127-03`、`127-04`、`127-05`
    - `grep -E '127-06' .planning/phases/127-semgrep-lsp/127-VALIDATION.md | grep -v '^#' | grep -c .` 结果为 **0**（或仅注释提及则不算；正文表无 127-06 行）
    - Map 中 Wave 列分别为 `0`/`1`/`2`/`3`/`4`；Requirement 列覆盖 TAINT-01、TAINT-02、TAINT-03、LSP-01
  </acceptance_criteria>
  <verify>
    <automated>python -c "from pathlib import Path; t=Path('.planning/phases/127-semgrep-lsp/127-VALIDATION.md').read_text(); assert all(x in t for x in ['127-01','127-02','127-03','127-04','127-05']); assert '| 127-06' not in t; print('ok')"</automated>
  </verify>
  <done>VALIDATION 与五 plan 编号对齐；无 127-06 幽灵行</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixtures → CI | Fixture JSON 不得含真实 token/凭证 |
| Frozen-surface guard → repo | 防止本相位误触 router/mcp |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-127-01 | Information Disclosure | fixture / stub tests | mitigate | fixture 用假数据；`test_stub_omits_token_*` 节点锁定脱敏契约 |
| T-127-02 | Denial of Service | scan fail-open tests | mitigate | Wave 0 含 timeout/unavailable fail-open 节点（实现 127-03） |
| T-127-03 | Tampering | argv contract tests | mitigate | `test_semgrep_argv_*` + never-import-semgrep 节点 |
| T-127-04 | Denial of Service | orphan_reap tests | mitigate | orphan 计数节点（实现 127-05） |
| T-127-05 | Spoofing | CE/advisory copy tests | mitigate | disclaimer + advisory 节点（实现 127-04） |
| T-127-SC | Tampering | npm/pip installs | accept | 本 plan 无包安装；合法性审计见 RESEARCH |
</threat_model>

<verification>
Wave 0 collect-only 全绿；VALIDATION 五 plan 对齐；无生产代码变更。
</verification>

<success_criteria>
- 全部验收节点可收集
- D-18 冻结面守卫文件存在且可跑
- VALIDATION 无幽灵 plan
</success_criteria>

<output>
Create `.planning/phases/127-semgrep-lsp/127-01-SUMMARY.md` when done
</output>
