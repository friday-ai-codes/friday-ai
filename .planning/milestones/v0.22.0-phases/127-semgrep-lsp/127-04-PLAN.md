---
phase: 127-semgrep-lsp
plan: 04
type: execute
wave: 3
depends_on: ["127-03"]
files_modified:
  - server/services/code_graph/security_scan_report.py
  - server/services/code_graph/semgrep_scan.py
  - server/services/code_graph/semgrep_enqueue.py
  - server/durable/tasks_impl.py
  - server/workflows/nodes/ai/coding.py
  - server/workflows/services/mr_service.py
  - server/mcp_tools/merge_request_service.py
  - server/tests/services/code_graph/test_security_scan_report.py
  - server/tests/workflows/test_coding_security_scan.py
  - server/tests/mcp_tools/test_mr_security_scan.py
autonomous: true
requirements: [TAINT-02, TAINT-03]
tags: [security-scan-report, mr-hangpoint, advisory, ce-disclaimer, pro-token]

must_haves:
  truths:
    - "MR 描述可幂等挂载 ## 安全扫描，与 ## 影响面并存不互相覆盖；workflow+MCP 双链路同一 helper（D-06）"
    - "finding 带 severity 分级展示；默认全程 advisory，扫描失败不阻断建 MR（D-07；T-127-05）"
    - "段内固定 CE 函数内 taint disclaimer + nosemgrep 说明；Pro 仅加密 token opt-in 且不夸大（D-08, D-09；TAINT-03）"
    - "创建时 stub/enqueue fire-and-forget，异步回填完整段；超时 stub 短码无堆栈/路径/凭证（D-04；T-127-01）"
  artifacts:
    - path: "server/services/code_graph/security_scan_report.py"
      provides: "SECURITY_SECTION_MARKER + build/append helpers"
      contains: "SECURITY_SECTION_MARKER"
    - path: "server/workflows/nodes/ai/coding.py"
      provides: "workflow hang-point"
      contains: "append_security_scan"
    - path: "server/workflows/services/mr_service.py"
      provides: "mr_service hang-point"
      contains: "append_security_scan"
    - path: "server/mcp_tools/merge_request_service.py"
      provides: "MCP hang-point"
      contains: "append_security_scan"
  key_links:
    - from: "coding.py / mr_service.py / merge_request_service.py"
      to: "security_scan_report.append_security_scan"
      via: "create-MR 缝 try/except 外壳；逻辑不分叉"
      pattern: "append_security_scan|build_security_scan"
    - from: "durable run_semgrep_scan"
      to: "MR body patch"
      via: "stub→完整段替换（pr_cross_reference 范式）"
      pattern: "安全扫描|patch|edit"
    - from: "build_security_scan_section"
      to: "CE disclaimer + severity list"
      via: "固定文案 + redact"
      pattern: "函数内|CE|nosemgrep|ERROR|WARNING"
---

<objective>
Wave 3：实现 `## 安全扫描` MR 段（advisory / CE 诚实文案 / nosemgrep / Pro token）、双链路挂点、创建时 stub + 异步回填（TAINT-02/03；D-06..D-10）。

Purpose: 用户在 MR 上看到分级 finding 与诚实边界，且建 MR 永不被扫描阻断。
Output: `security_scan_report.py` + 三处 hang-point + async patch + 测试绿。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/127-semgrep-lsp/127-CONTEXT.md
@.planning/phases/127-semgrep-lsp/127-PATTERNS.md
@server/services/code_graph/impact_report.py
@server/tests/services/code_graph/test_impact_report.py
@server/workflows/nodes/ai/coding.py
@server/workflows/services/mr_service.py
@server/mcp_tools/merge_request_service.py
@server/workflows/services/pr_cross_reference.py
@server/services/feishu_im.py
</context>

## Artifacts this phase produces

| Symbol / artifact | Module | Introduced in |
|-------------------|--------|---------------|
| `SECURITY_SECTION_MARKER` (`## 安全扫描`) | `security_scan_report.py` | **127-04** |
| `append_security_scan` / `build_security_scan_section` / stub helpers | `security_scan_report.py` | **127-04** |
| Dual hang-points (stub + enqueue) | `coding.py` / `mr_service.py` / `merge_request_service.py` | **127-04** |
| Async MR description patch (stub→results) | `semgrep_scan` / `tasks_impl` 或同模块 helper | **127-04** |
| Dual-link tests | `test_coding_security_scan.py` / `test_mr_security_scan.py` | **127-04** |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: security_scan_report helper（advisory + CE + nosemgrep + Pro）</name>
  <files>server/services/code_graph/security_scan_report.py, server/tests/services/code_graph/test_security_scan_report.py</files>
  <read_first>
    - `.planning/phases/127-semgrep-lsp/127-CONTEXT.md` D-06..D-10
    - `server/services/code_graph/impact_report.py` — `IMPACT_SECTION_MARKER` / `append_impact_report` / `_stub_section` / `_sanitize_error_text` / fail-open build
    - `server/tests/services/code_graph/test_impact_report.py` — idempotent + stub omits secrets
    - `server/tests/services/code_graph/test_security_scan_report.py`
  </read_first>
  <behavior>
    - append 幂等：已含 ## 安全扫描则不重复；不影响 ## 影响面
    - 成功段含 severity 分级 + CE 函数内 taint disclaimer + nosemgrep 句
    - 空 token → CE；有 token → Pro 已启用短句且不夸大跨文件
    - stub 含稳定短码；无 token/Traceback/绝对路径
  </behavior>
  <action>
新建 `services/code_graph/security_scan_report.py`，**照抄** `impact_report` 范式（D-06）：
- `SECURITY_SECTION_MARKER = "## 安全扫描"`
- `append_security_scan(description, section)` 幂等；空 section 原样返回
- `build_security_scan_section(...)` / `_stub_section(error_code)`：永不 raise；`timeout|unavailable|…` 短码；文案「安全扫描未能生成」；`_sanitize_error_text` 用 `redact_secrets_in_text` + 去 Traceback/绝对路径（T-127-01）
- 成功段：severity 映射 ERROR/WARNING/INFO（或 Semgrep 等价）；**全程 advisory**——禁止任何 blocking/merge-gate 逻辑（D-07；T-127-05）
- 固定 CE disclaimer：当前 Semgrep **CE**，taint **仅函数内**，不承诺跨函数/跨文件（D-08；TAINT-03）
- `nosemgrep`：段内一句说明可按 Semgrep 原生抑制误报；不自建 suppress 表（D-08）
- Pro：`pro_enabled=True` 时另行列「Pro 能力已启用」类短句，**不得**夸大未验证跨文件覆盖率（D-08/D-09）
- 观测：`security_scan_report_started/completed/failed` + `duration_ms`；`category=caller`；`component=code_graph`；best-effort

去 skip `test_security_scan_report.py` 全部节点至绿。
  </action>
  <acceptance_criteria>
    - `rg -n 'SECURITY_SECTION_MARKER|## 安全扫描|append_security_scan|build_security_scan_section' server/services/code_graph/security_scan_report.py` 均命中
    - 段文案含 CE/函数内 与 `nosemgrep` 字面量
    - `cd server && uv run pytest tests/services/code_graph/test_security_scan_report.py -q` 绿
  </acceptance_criteria>
  <verify>
    <automated>cd server && GALAXY_CACHE_WARM_ON_STARTUP=False GRAPH_BUILD_ORPHAN_RECONCILE_ON_STARTUP=False uv run pytest tests/services/code_graph/test_security_scan_report.py -q</automated>
  </verify>
  <done>安全扫描段 helper 契约闭合（advisory + CE + 脱敏）</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 双链路挂点 + stub enqueue + 异步 MR 回填</name>
  <files>server/workflows/nodes/ai/coding.py, server/workflows/services/mr_service.py, server/mcp_tools/merge_request_service.py, server/services/code_graph/semgrep_scan.py, server/services/code_graph/semgrep_enqueue.py, server/durable/tasks_impl.py, server/tests/workflows/test_coding_security_scan.py, server/tests/mcp_tools/test_mr_security_scan.py</files>
  <read_first>
    - `.planning/phases/127-semgrep-lsp/127-CONTEXT.md` D-04 / D-06 / D-09
    - `server/workflows/nodes/ai/coding.py` — `_create_mr_for_repo` 中 `append_impact_report` 缝（~2227–2252）
    - `server/workflows/services/mr_service.py` / `server/mcp_tools/merge_request_service.py` 同类缝
    - `server/workflows/services/pr_cross_reference.py` — GitHub `pr.edit` / GitLab `mr.save` 回写
    - `server/tests/workflows/test_coding_impact_report.py` 与 `server/tests/mcp_tools/test_mr_impact_report.py`（若存在）作双链路测模板
  </read_first>
  <behavior>
    - 三处建 MR 缝均调用同一 append_security_scan；外壳 except 不阻断建 MR
    - 创建时写入 stub（或空安全段）并 enqueue；扫描完成后替换 stub 为完整段
    - token 不出现在 MR body / 日志断言
  </behavior>
  <action>
**挂点（D-06）：** 在三处已挂 `append_impact_report` 的同一 try 块旁（或紧随其后）挂载安全扫描：
1. `workflows/nodes/ai/coding.py` `_create_mr_for_repo`
2. `workflows/services/mr_service.py`
3. `mcp_tools/merge_request_service.py`

首版策略（D-04 discretion **stub-then-async**）：
- 创建路径：`section = stub("pending")` 或短 pending stub → `append_security_scan`；然后 `enqueue_semgrep_scan(..., initiated_by_user_id=...)` fire-and-forget；enqueue 失败仅 log，不 raise
- 外壳 `except` 事件名 `security_scan_shell_failed`；helper/enqueue 内应已吞；**禁止**三壳分叉业务逻辑
- 首版 **仅 description 段**，不写 MR comment（RESEARCH discretion）

**异步回填：** 在 `tasks_impl.run_semgrep_scan`（或 `semgrep_scan` 内 helper）完成后：
- `build_security_scan_section` 生成完整段（含 severity / CE / Pro 行）
- 用既有 git token 解析 + platform client 读当前 MR body；若段为 stub/`_未能生成`/`pending` 则整段替换；若已是完整结果可幂等 skip
- 对齐 `pr_cross_reference`：GitHub `pr.edit(body=...)` / GitLab `description`+`save`；逐平台 try/except fail-soft
- patch 失败只观测，不重试风暴

**Token（D-09；T-127-01）：** 扫描子进程仅经 `services/code_graph/semgrep_token.get_semgrep_app_token()`（127-02 已落地 Fernet 写读）取密文解密后的值注入 env；有值才设 `SEMGREP_APP_TOKEN`。单元测断言 section/日志不含假 token 字符串。⛔ 本 plan 不再新增明文写入路径（写路径已在 127-02 `set_semgrep_app_token`）。

新增测试文件（可从 impact 双链路测复制精简）：
- `tests/workflows/test_coding_security_scan.py` — coding 缝调用 append + enqueue；异常不阻断
- `tests/mcp_tools/test_mr_security_scan.py` — MCP 缝同构；`mr_service` 可同文件或另测

⛔ 不引入 blocking 白名单（Deferred）；⛔ 不改 `repo_router_v2.py`/`mcp/` submodule（D-18）。
  </action>
  <acceptance_criteria>
    - `rg -n "append_security_scan" server/workflows/nodes/ai/coding.py server/workflows/services/mr_service.py server/mcp_tools/merge_request_service.py` 三文件均命中
    - `rg -n "enqueue_semgrep_scan" server/workflows/nodes/ai/coding.py server/workflows/services/mr_service.py server/mcp_tools/merge_request_service.py` 至少两处入口命中（或集中于共享薄包装且三缝调用该包装——若抽 `attach_security_scan_to_mr_body` 则三缝调包装，包装内 enqueue）
    - `cd server && uv run pytest tests/services/code_graph/test_security_scan_report.py tests/workflows/test_coding_security_scan.py tests/mcp_tools/test_mr_security_scan.py -q` 绿
  </acceptance_criteria>
  <verify>
    <automated>cd server && GALAXY_CACHE_WARM_ON_STARTUP=False GRAPH_BUILD_ORPHAN_RECONCILE_ON_STARTUP=False uv run pytest tests/services/code_graph/test_security_scan_report.py tests/workflows/test_coding_security_scan.py tests/mcp_tools/test_mr_security_scan.py -q</automated>
  </verify>
  <done>双链路 advisory 安全扫描段可挂可回填；建 MR fail-open</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Scan results → MR description | 用户可见文本；必须脱敏且 advisory |
| Encrypted setting → subprocess → MR | Token 不得越过边界进入 MR/日志 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-127-01 | Information Disclosure | MR section / logs | mitigate | redact；stub 禁堆栈/路径；断言无 token |
| T-127-02 | Denial of Service | create-MR path | mitigate | 异步 enqueue；同步路径仅 stub；超时 fail-open |
| T-127-03 | Tampering | hang-point inputs | mitigate | 不拼 shell；复用既有 repo/MR id |
| T-127-04 | Denial of Service | N/A | accept | LSP 属 127-05 |
| T-127-05 | Spoofing | MR copy | mitigate | 固定 advisory + CE disclaimer；禁 blocking 语义 |
| T-127-SC | Tampering | no new packages | accept | 无新依赖 |
</threat_model>

<verification>
security_scan_report + dual-link 测试绿；三挂点均调用同一 helper。
</verification>

<success_criteria>
- TAINT-02 / TAINT-03 可观测于 MR 段
- D-06..D-09 闭合；D-04 异步回填落地
</success_criteria>

<output>
Create `.planning/phases/127-semgrep-lsp/127-04-SUMMARY.md` when done
</output>
