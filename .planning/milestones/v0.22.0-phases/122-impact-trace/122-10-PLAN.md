---
phase: 122-impact-trace
plan: 10
type: execute
wave: 7
depends_on: ["122-08", "122-09"]
files_modified:
  - server/tests/mcp_tools/test_impact_trace_tools.py
  - .planning/ROADMAP.md
autonomous: true
requirements: [IMPACT-06]

must_haves:
  truths:
    - "同一输入下 MCP 与对话壳产出的 data 段逐字节相同（成功态与 ambiguous 失败态各一）"
    - "两面的失败语义同源：ok=False 在两面都是「查询成功但没定位到符号」，不是工具故障"
    - "ROADMAP 记下 Phase 127 补齐 LSP 后复验 IMPACT-03 的欠债（D-26）"
    - "ROADMAP 记下 mcp npm 包漂移由 5 项扩为 7 项（D-27），⛔ 不碰 submodule"
  artifacts:
    - path: "server/tests/mcp_tools/test_impact_trace_tools.py"
      provides: "test_two_surfaces_same_payload —— 本仓第一条双面同源守护"
      contains: "def test_two_surfaces_same_payload"
    - path: ".planning/ROADMAP.md"
      provides: "D-26 / D-27 两笔跨相位记账"
  key_links:
    - from: "tests/mcp_tools/test_impact_trace_tools.py"
      to: "agents.tools.graph_tools.impact_analysis"
      via: "同一进程内直接 await 对话工具，与 MCP HTTP 响应逐字节比对"
      pattern: "impact_analysis"
---

<objective>
给本相位收口两件事：① 立起本仓**第一条**「双面同源」守护——同一输入下 MCP 与对话壳产出的 `data` 段逐字节相同（D-21）；② 把 D-26 与 D-27 两笔跨相位欠债落到 `.planning/ROADMAP.md` 上，从口头承诺变成可查的一行字。

Purpose: `122-PATTERNS.md` §No Analog Found 记得很清楚——全仓**没有**任何双面一致性断言，而最接近的一对（`search_delivery_knowledge`）恰恰**已经漂移**：同一个检索异常，MCP 面 fail-soft 成空结果、对话面报「工具坏了」。122-05～122-09 用共享编排层与共享原语在**结构上**防了这件事，但结构约束会被下一个改动绕过去，只有一条逐字节断言能让漂移在 CI 上立刻可见。
🚨 两笔记账各有一个源头：`122-06:197` 把 D-26 派给了当时并不存在的 122-10；D-27 的 npm 漂移则会因 122-08 新增两个工具从 5 项变成 7 项，而 `mcp` submodule 正被另一个并发会话修改，⛔ 全程不许碰。
Output: 1 个双面守护用例 + ROADMAP 两条记账。
</objective>

<execution_context>
@.cursor/gsd-core/workflows/execute-plan.md
@.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/122-impact-trace/122-CONTEXT.md
@.planning/phases/122-impact-trace/122-PATTERNS.md
@.planning/ROADMAP.md
@server/mcp_tools/views.py
@server/agents/tools/graph_tools.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: test_two_surfaces_same_payload —— 逐字节双面同源守护（D-21）</name>
  <read_first>
    - `.planning/phases/122-impact-trace/122-PATTERNS.md` §Shared Patterns「双面同源（D-21）—— 有半个先例，本相位要补齐另一半」（四条未共享的部分，尤其第 2 条「失败语义已经漂移」与第 4 条「没有任何测试断言两面一致」——本用例就是第 4 条的答案）
    - `.planning/phases/122-impact-trace/122-PATTERNS.md` §No Analog Found（`test_two_surfaces_same_payload` 全仓无先例，本相位建立该范式）
    - `server/mcp_tools/views.py` 的 `ImpactAnalysisView`（122-08：`output_data = {**result, "run_id": str(run.run_id)}`——`run_id` 是 MCP 面**唯一**多出来的键）
    - `server/agents/tools/graph_tools.py` 的 `impact_analysis`（122-09：`output={"data": result, "metadata": {...}}`——`data` 是同一个 `result` 原样）
    - `server/tests/mcp_tools/test_impact_trace_tools.py`（122-08 交付：module 级 `_reset_code_graph_state` autouse、`_build_impact_fixture` 造数 helper、`IMPACT_URL` / `TRACE_URL` 两个常量）
    - `server/tests/mcp_tools/conftest.py:20-25` 与 `server/tests/conftest.py:1469`（`mcp_client` 由 `make_access_token` 造，后者依赖 `access_user` fixture —— 🚨 两面必须跑在**同一个用户**下，否则比出来的差异是权限差异而不是壳的差异）
    - `server/agents/tools/delivery_knowledge_tools.py:56-65`（`_resolve_conversation_user` 走 `Conversation.objects.select_related("created_by").aget(id=…)`——用例要造一个 `created_by=access_user` 的 `Conversation`）
  </read_first>
  <action>
把 `server/tests/mcp_tools/test_impact_trace_tools.py` 里 122-01 留下的 `test_two_surfaces_same_payload` 桩转成真实断言（去掉 `@pytest.mark.skip`）。

造数与前置：复用 122-08 的 `_build_impact_fixture(indexed_repository)`；造一个 `Conversation(created_by=access_user)`，让对话面解析出的 owner 与 `mcp_client` 的 PAT 所属用户**是同一个人**（fixture 链：`mcp_client` → `make_access_token` → `access_user`）。用例签名同时取 `mcp_client` / `indexed_repository` / `access_user` 三个 fixture。

比对协议（两轮，⛔ 不能只比成功态）：

**第一轮 —— 成功态。** 同一组参数（`repository_id` / `symbol` / `max_depth` / `min_confidence` / `limit`）分别打两面：
- MCP：`client.post(IMPACT_URL, payload, format="json")` ⇒ 200，取 `mcp_body = response.json()`，`mcp_data = {k: v for k, v in mcp_body.items() if k != "run_id"}`。
- 对话：`await impact_analysis(**payload, conversation_id=str(conversation.id))` ⇒ `result.success is True`，取 `tool_data = result.output["data"]`。
- 断言：`json.dumps(mcp_data, sort_keys=True, ensure_ascii=False, default=str) == json.dumps(tool_data, sort_keys=True, ensure_ascii=False, default=str)`。🚨 失败时的报错必须可读——先断言 `set(mcp_data) == set(tool_data)` 给出**键集差**，再断言逐字节相等；只有一条 `assert a == b` 的话，两个几 KB 的 dict 一旦不等，报错基本不可用。
- 同一轮补一条**反向**断言：`"run_id" in mcp_body` 且 `"run_id" not in tool_data`（`run_id` 是 MCP 面的 `InteractionRun` 标识，对话面没有这个概念；它是唯一允许的差异，把它写死才能防止后人往 `data` 里塞更多「只有一面有」的键）。

**第二轮 —— `ambiguous_symbol` 失败态。** 这一轮才是真正防漂移的地方（`search_delivery_knowledge` 的漂移正是发生在失败路径上，成功路径两面一直是一致的）。造 3 个同 `(repository, name)` 不同 `file_path` 的 `Symbol`，用同一个重名 `symbol` 打两面：
- MCP：断言 `status_code == 200`（🚨 **不是 4xx**——`ambiguous_symbol` 的候选列表是 agent 二选一的唯一依据，转成 `{"error_code","detail"}` 的 4xx 会把它整段丢掉），`body["ok"] is False`、`body["error_code"] == "ambiguous_symbol"`、`len(body["candidates"]) == 3`。
- 对话：断言 `result.success is True`（🚨 **不是 False**——工具调用本身成功了，`ok=False` 才是查询结论；把它映射成 `success=False` 就是 PATTERNS 记的第 2 条债「同一个故障，一面报没有结果、一面报工具坏了」的复现），`result.output["data"]["error_code"] == "ambiguous_symbol"`。
- 再做一次同款的 `json.dumps(..., sort_keys=True)` 逐字节比对。

用例 docstring 必须写清这条守护防的是什么、以及**为什么不能靠 review 防**：两个壳各自 40 行、看上去都对，漂移是在后续某次「只改一面」的维护里长出来的；`search_delivery_knowledge` 那对就是这么漂的（两侧各自手写七个关键字参数，连注释都是复制粘贴的两份）。同时写明本用例**不覆盖**什么：它只证明「被测的这组输入两面一致」，不证明所有输入都一致——真正的机械保证是共享编排层（122-07）与共享原语（`resolve_tool_graph_branch` / `tool_trace_payload`，122-08），本用例是它们的哨兵。

⚠️ 用例内 ⛔ 不要 patch / mock `run_impact`——mock 掉编排层就等于两面在比同一个假对象，守护退化为自证。两面都必须真的走完全程。
  </action>
  <acceptance_criteria>
    - `cd server && GALAXY_CACHE_WARM_ON_STARTUP=False GRAPH_BUILD_ORPHAN_RECONCILE_ON_STARTUP=False uv run pytest tests/mcp_tools/test_impact_trace_tools.py -q --reuse-db` 结果为 `6 passed`（零 skip —— 122-08 的 5 条 + 本条）
    - 用例内含两轮比对：成功态与 `ambiguous_symbol` 失败态各一次 `json.dumps(..., sort_keys=True)` 相等断言
    - 用例内含 `set(mcp_data) == set(tool_data)` 的键集先行断言（报错可读）
    - 用例内含 `"run_id" in mcp_body and "run_id" not in tool_data` 的唯一差异断言
    - 失败态两面的**成功语义**被显式钉死：MCP 侧 `status_code == 200`，对话侧 `result.success is True`
    - 反自证守护（AST）：`cd server && uv run python -c "import ast; t=ast.parse(open('tests/mcp_tools/test_impact_trace_tools.py').read()); fn=[n for n in ast.walk(t) if isinstance(n,(ast.FunctionDef,ast.AsyncFunctionDef)) and n.name=='test_two_surfaces_same_payload']; assert fn; src=ast.unparse(fn[0]); assert 'run_impact' not in src or 'patch' not in src, '⛔ 不许 mock 编排层'"` 退出码 0
    - `cd server && GALAXY_CACHE_WARM_ON_STARTUP=False GRAPH_BUILD_ORPHAN_RECONCILE_ON_STARTUP=False uv run pytest tests/mcp_tools tests/agents tests/services/code_graph -q --reuse-db` 相对 122-09 收尾基线零新增失败
  </acceptance_criteria>
  <verify>
    <automated>cd server && GALAXY_CACHE_WARM_ON_STARTUP=False GRAPH_BUILD_ORPHAN_RECONCILE_ON_STARTUP=False uv run pytest tests/mcp_tools/test_impact_trace_tools.py -q --reuse-db</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: D-26 / D-27 两笔跨相位记账写进 ROADMAP</name>
  <read_first>
    - `.planning/ROADMAP.md:92-105`（Phase 122 条目：`**Goal**` / `**Depends on**` / `**Requirements**` / `**Success Criteria**` / `**Plans**` 五段的既有排版）
    - `.planning/ROADMAP.md:176-178`（v0.22.0 的「执行顺序（依赖链）」段与紧随其后的 **跨仓记账** 句 —— 现有那句已经写着「`mcp` npm 包需为本里程碑新增的 MCP 工具补条目并发版（另一仓库改动，v0.20.0 已有同款缺口在案）」，D-27 是在它上面把**数字与工具名**补实）
    - `.planning/ROADMAP.md:162-174`（Phase 127 条目 —— D-26 的复验动作要挂到这个相位上，写在它的 Success Criteria 之后作为一条显式的跨相位回访）
    - `.planning/phases/122-impact-trace/122-CONTEXT.md` D-26 / D-27（两条裁决的原文与理由，⛔ 记账文案不得弱化：合成数据通过**不得**表述成「跨仓能力已验证」）
    - `.planning/phases/122-impact-trace/122-06-PLAN.md` §「必须写进 SUMMARY 的如实声明（D-26）」第 1–4 条（第 4 条把 ROADMAP 动作派给了本 plan）
    - `server/tests/mcp_tools/test_mcp_package_alignment.py`（全文 58 行 —— `_TOOL_NAME_RE` 正则与双向断言；本 task 用同一套逻辑离线数出漂移项，⛔ 不修改这个文件、⛔ 不改它的判据）
  </read_first>
  <action>
只改 `.planning/ROADMAP.md`，两处编辑，⛔ 不碰 `mcp/` submodule、⛔ 不改任何守护测试的判据（D-27 的两条理由：该 submodule 正被另一个并发会话修改；ROADMAP 已明文记账走另批发版）。

① **D-26 —— IMPACT-03 的真实数据复验欠债。** 在 `### Phase 127: Semgrep 门禁 + LSP 基准` 的 `**Success Criteria**` 列表之后、`**Plans**: TBD` 之前，插入一行跨相位回访记账，内容必须覆盖四点：
  - 生产库 `CrossRepoApiCall` / `ApiCallSite` / `ApiWrapper` **均为 0 行**（`Endpoint` 6,014 行），上游产出器依赖 volar LSP 而 server 镜像无 Node；
  - Phase 122 的 IMPACT-03 四条分支**全部由合成数据覆盖**，跨仓路径**未经任何真实数据验证**；
  - Phase 127 补齐 LSP 并重建索引后，**回来用真实样本复验 IMPACT-03** 的四条分支，并测出 `(file_path, name)` 二次解析的真实命中率；
  - 121-10 记的「样本不足」实为**样本为零**，命中率在 Phase 127 之前根本不可测。
  措辞纪律：⛔ 不得出现「跨仓 impact 已上线可用」「跨仓能力已验证」这类表述（D-26 明文）。

② **D-27 —— `mcp` npm 包漂移由 5 项扩为 7 项。** 改 `.planning/ROADMAP.md:178` 那句既有的「**跨仓记账:**」，把它从一句泛泛的欠债升级为带**数字与工具名**的记账：`test_mcp_package_tools_match_server_snapshot` 在 HEAD 上已红着 5 项漂移（`apply_repo_association` / `generate_requirement_spec` / `get_repo_research` / `route_blueprint_repos` / `start_repo_research`，来自阶段沙箱工具，与本相位无关），Phase 122 新增 `impact_analysis` / `trace_call_path` 两个 MCP 工具后变为 **7 项**；本相位按 D-27 **不修** submodule（并发会话占用 + 跨仓改动另批发版），该守护继续红着并列入相位门的「已知既有失败」白名单。

③ Phase 122 条目的 `**Plans**` 清单（10 个 plan 按 wave 分组）**已由 planner 在建计划时写入**，本 task ⛔ 不重写它——只在验收里回归一次，防被中途改回 `TBD`。

④ SUMMARY 里必须复述这两笔记账（D-26 的四点 + D-27 的 5→7 与两个工具名），并如实写明本相位使既有失败从 5 项扩大到 7 项——⛔ 不得表述成「没有影响」。
  </action>
  <acceptance_criteria>
    - D-26 记账落地且挂在 Phase 127 名下：`cd . && python3 -c "import re; s=open('.planning/ROADMAP.md',encoding='utf-8').read(); i=s.index('### Phase 127'); j=s.index('###', i+5) if '###' in s[i+5:] else len(s); seg=s[i:j]; assert 'IMPACT-03' in seg and '复验' in seg and 'CrossRepoApiCall' in seg, seg[-400:]"` 退出码 0
    - D-26 措辞纪律：同一段里**不出现**「已验证」「已上线可用」——`cd . && python3 -c "s=open('.planning/ROADMAP.md',encoding='utf-8').read(); i=s.index('### Phase 127'); j=s.index('### ', i+5); seg=s[i:j]; assert '跨仓能力已验证' not in seg and '跨仓 impact 已上线可用' not in seg"` 退出码 0
    - D-27 记账落地且带数字与两个工具名：`cd . && python3 -c "s=open('.planning/ROADMAP.md',encoding='utf-8').read(); assert '跨仓记账' in s; k=s.index('跨仓记账'); seg=s[k:k+900]; assert 'impact_analysis' in seg and 'trace_call_path' in seg and '7' in seg and '5' in seg, seg"` 退出码 0
    - 漂移数确为 7 且含两个新工具（用与守护测试同一套正则离线数，⛔ 不改守护测试）：`cd server && uv run python -c "import django,os,re; os.environ.setdefault('DJANGO_SETTINGS_MODULE','friday.settings'); django.setup(); from pathlib import Path; from mcp_tools.serializers import TOOL_SCHEMA_SNAPSHOT; ts=Path('..')/'mcp'/'src'/'tools.ts'; assert ts.exists(), 'mcp submodule 未 checkout —— 在 SUMMARY 如实记为本机未验证，⛔ 不改守护测试'; names=set(re.findall(r\"^\s*name: '([a-z0-9_]+)',$\", ts.read_text(encoding='utf-8'), re.M)); missing=sorted(set(TOOL_SCHEMA_SNAPSHOT)-names); assert len(missing)==7, missing; assert {'impact_analysis','trace_call_path'} <= set(missing), missing"` 退出码 0
    - Phase 122 的 `**Plans**` 不再是 `TBD`，列出 10 个 plan 且按 wave 分组：`cd . && python3 -c "s=open('.planning/ROADMAP.md',encoding='utf-8').read(); i=s.index('### Phase 122'); j=s.index('### Phase 123'); seg=s[i:j]; assert 'TBD' not in seg; assert all(f'122-{n:02d}-PLAN.md' in seg for n in range(1,11)), seg"` 退出码 0
    - `git diff --name-only` 不含 `mcp/` 下任何路径，也不含 `server/tests/mcp_tools/test_mcp_package_alignment.py`（D-27：既不碰 submodule，也不改守护判据）
  </acceptance_criteria>
  <verify>
    <automated>cd server && uv run python -c "import django,os,re; os.environ.setdefault('DJANGO_SETTINGS_MODULE','friday.settings'); django.setup(); from pathlib import Path; from mcp_tools.serializers import TOOL_SCHEMA_SNAPSHOT; s=Path('../.planning/ROADMAP.md').read_text(encoding='utf-8'); i=s.index('### Phase 127'); j=s.index('### ', i+5); assert 'IMPACT-03' in s[i:j] and '复验' in s[i:j]; k=s.index('跨仓记账'); assert 'impact_analysis' in s[k:k+900] and 'trace_call_path' in s[k:k+900]; ts=Path('..')/'mcp'/'src'/'tools.ts'; names=set(re.findall(r\"^\s*name: '([a-z0-9_]+)',\$\", ts.read_text(encoding='utf-8'), re.M)) if ts.exists() else None; missing=sorted(set(TOOL_SCHEMA_SNAPSHOT)-names) if names is not None else []; assert names is None or (len(missing)==7 and {'impact_analysis','trace_call_path'} <= set(missing)), missing; print('ok')"</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 两个壳 → 同一份 `data` | 双面一致性本身就是一条安全属性：任何一面多透出一个键，就是一条只在那一面存在、无人审过的泄漏面 |
| 记账文案 → 后续相位的判断 | 把合成数据的通过写成「已验证」，会让 Phase 123–126 在一个未经验证的能力上继续加码 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-122-双面漂移 | **Tampering（结论被污染）** | 两个壳的输出信封 | mitigate | 逐字节 `json.dumps(sort_keys=True)` 断言覆盖成功态与 `ambiguous_symbol` 失败态；`run_id` 是唯一被允许的差异并被写死；⛔ 用例不许 mock 编排层（AST 断言守），否则退化为自证 |
| T-122-exclusion 回流 | Information Disclosure | 双面比对断言 | mitigate | 两面 `data` 相同即意味着 122-08 的 `test_excluded_files_invisible` 结论对对话面同样成立——本用例把那条端到端不泄漏断言的覆盖面从一面扩到两面 |
| T-122-记账失真 | **Repudiation** | D-26 / D-27 的措辞 | mitigate | 措辞纪律写成可执行判据：Phase 127 段落里不得出现「已验证 / 已上线可用」；D-27 段落必须带数字（5 → 7）与两个工具名，⛔ 不许含糊成「有一些漂移」 |
| T-122-守护被改绿 | Tampering | `test_mcp_package_alignment.py` | mitigate | 漂移数用**离线复算**（与守护同一套正则）而非改判据来核对；`git diff --name-only` 显式断言未触碰该测试文件与 `mcp/` submodule（D-27） |
| T-122-SC | Tampering | npm/pip/cargo installs | accept | 零新增依赖（`122-RESEARCH.md` §Package Legitimacy Audit 表为空） |
</threat_model>

<verification>
- `cd server && GALAXY_CACHE_WARM_ON_STARTUP=False GRAPH_BUILD_ORPHAN_RECONCILE_ON_STARTUP=False uv run pytest tests/services/code_graph tests/mcp_tools tests/agents -q --reuse-db` —— `test_impact_trace_tools.py` 6 passed 零 skip；全相位新增用例零 skip；唯一已知失败为 `test_mcp_package_alignment.py::test_mcp_package_tools_match_server_snapshot`（7 项漂移，D-27 已记账）
- `cd server && uv run ruff check tests/mcp_tools/` 通过
- `cd server && uv run python manage.py makemigrations --check --dry-run` 退出码 0（全相位零迁移）
- `git diff --name-only` 不含 `mcp/`、`server/tests/mcp_tools/test_mcp_package_alignment.py`、`server/services/code_graph/loader.py`、`server/codegraph/services/repo_router_v2.py`
</verification>

<success_criteria>
- MCP 与对话两面对同一输入产出逐字节相同的 `data` 段，成功态与 `ambiguous_symbol` 失败态各有一条断言；`run_id` 是唯一被允许且被写死的差异
- 失败态在两面都被表达为「查询成功、结论是没定位到符号」（HTTP 200 / `ToolResult.success=True`），⛔ 没有任何一面把它降级成工具故障
- ROADMAP 里能查到 D-26 的复验欠债（挂 Phase 127）与 D-27 的 5 → 7 漂移记账（带两个工具名）
- `mcp` submodule 与 `test_mcp_package_alignment.py` 全程未被触碰
- Phase 122 的 ROADMAP 条目列出 10 个 plan 与其 wave 分组
</success_criteria>

## Artifacts this phase produces

| 类型 | 名称 | 位置 |
|---|---|---|
| 用例（落地） | `test_two_surfaces_same_payload`（本仓第一条双面同源守护） | `server/tests/mcp_tools/test_impact_trace_tools.py` |
| 记账（D-26） | 「Phase 127 补齐 LSP 后回来用真实样本复验 IMPACT-03」+ 生产零样本四点事实 | `.planning/ROADMAP.md` Phase 127 条目 |
| 记账（D-27） | `mcp` npm 包漂移 5 → 7（新增 `impact_analysis` / `trace_call_path`），不修 submodule 的两条理由 | `.planning/ROADMAP.md` 跨仓记账句 |
| ROADMAP 条目（修改） | Phase 122 的 `**Plans**`：10 个 plan 按 wave 分组 | `.planning/ROADMAP.md` |

<output>
Create `.planning/phases/122-impact-trace/122-10-SUMMARY.md` when done
</output>
