---
phase: 123-detect-changes
plan: 04
type: execute
wave: 4
depends_on: ["123-03"]
files_modified:
  - server/agents/tools/schemas/graph_tools.py
  - server/agents/tools/graph_tools.py
  - server/agents/tools/__init__.py
  - server/agents/chat_runner.py
  - server/tests/agents/tools/test_graph_tools.py
autonomous: true
requirements: [DIFF-01, DIFF-02]

must_haves:
  truths:
    - "对话壳 detect_changes 与 MCP 共用 run_detect_changes，data 段零加工（D-13）"
    - "Pydantic 字段与 MCP serializer 同表：compare 必填、base_ref 可选、impact 覆盖上下界一致（D-02/D-10）"
    - "工具已注册到 agents.tools.__all__ 且挂入 chat_runner._INDEXED_TOOL_NAMES（注册≠暴露纪律）"
    - "会话无用户 / 仓不可读 → fail-closed，不返回空 affected 伪装成功"
  artifacts:
    - path: "server/agents/tools/schemas/graph_tools.py"
      provides: "DetectChangesToolInput"
      contains: "class DetectChangesToolInput"
    - path: "server/agents/tools/graph_tools.py"
      provides: "detect_changes @tool"
      contains: "async def detect_changes"
    - path: "server/agents/chat_runner.py"
      provides: "_INDEXED_TOOL_NAMES 含 detect_changes"
  key_links:
    - from: "detect_changes @tool"
      to: "run_detect_changes"
      via: "_detect_changes_impl 薄壳"
      pattern: "run_detect_changes"
    - from: "chat_runner._INDEXED_TOOL_NAMES"
      to: "detect_changes"
      via: "白名单暴露"
      pattern: "detect_changes"
---

<objective>
交付 detect_changes 的 **对话面**：`DetectChangesToolInput` + `@tool detect_changes` + 导出/白名单，逻辑委托 `run_detect_changes`（D-13）。

Purpose: 与 MCP 对称暴露，供 agent 会话一键取受影响符号 + 批量 impact（DIFF-01/02）。
Output: 对话工具可被 chat_runner 索引模式加载；注册/fail-closed 测绿。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/123-detect-changes/123-CONTEXT.md
@.planning/phases/123-detect-changes/123-PATTERNS.md
@server/agents/tools/graph_tools.py
@server/agents/tools/schemas/graph_tools.py
@server/agents/chat_runner.py
@server/mcp_tools/serializers.py
</context>

## Artifacts this phase produces

| Symbol | Module |
|--------|--------|
| `DetectChangesToolInput` | `agents.tools.schemas.graph_tools` |
| `detect_changes` / `_detect_changes_impl` | `agents.tools.graph_tools` |
| `__all__` 导出 | `agents.tools` |
| `_INDEXED_TOOL_NAMES` 条目 | `agents.chat_runner` |

<tasks>

<task type="auto">
  <name>Task 1: DetectChangesToolInput + @tool 薄壳</name>
  <files>server/agents/tools/schemas/graph_tools.py, server/agents/tools/graph_tools.py</files>
  <read_first>
    - `server/agents/tools/schemas/graph_tools.py`：`ImpactAnalysisToolInput`（strict/extra=forbid/frozen + ge/le）
    - `server/agents/tools/graph_tools.py`：`impact_analysis` / `_impact_analysis_impl`（约 239–438）——用户解析、`_resolve_tool_repo`、`GraphError`→`graph_error_to_tool_error`、`_record_chat_retrieval`、`ToolResult(success=True, output={"data": result})` 对 `ok=False` 信封的纪律
    - `server/mcp_tools/serializers.py`：`DetectChangesRequestSerializer`（123-03 已落地；对话 pydantic **必须**与 MCP 字段/上下界同表）
  </read_first>
  <action>
① 新增 `DetectChangesToolInput(BaseModel)`：`model_config=ConfigDict(strict=True, extra="forbid", frozen=True)`；字段与 MCP 同表——`repository_id`、`compare`、可选 `base_ref`、可选 `max_depth`/`min_confidence`/`include_low_confidence`/`limit`（上下界同 impact）。⛔ 不加 overlay `branch`。

② 在 `graph_tools.py` 增加 `@tool(name="detect_changes", …)` + `_detect_changes_impl`：
- description 写清：compare=diff head；base_ref 仅 MR 语义声明；diff 基线永远是索引水位（D-01/D-02）
- 第一步 `_resolve_conversation_user(conversation_id)` fail-closed
- validate → `_resolve_tool_repo` → `run_detect_changes(..., user=user, graph 坐标由编排固定 base)`
- `GraphError` → `ToolResult(success=False, error=message)`（工具故障）
- 编排 `ok=False` → `ToolResult(success=True, output={"data": result, "metadata": …})`（查询结论在信封里）
- `_record_chat_retrieval(RetrievalTrace.Kind.EDGE, tool_trace_payload(..., tool="detect_changes"), …)` best-effort
- caller 事件 `code_graph_detect_changes_*` 或沿用 impact 壳事件命名模式；`component` 与 122 对话壳一致
- `output["data"]` **零加工**（D-13）

更新模块 docstring / `__all__` 列入 `detect_changes`。
  </action>
  <acceptance_criteria>
    - `from agents.tools.graph_tools import detect_changes` 成功
    - `DetectChangesToolInput` 含 `compare`，model_fields 无 overlay `branch`
    - `_detect_changes_impl` 源码调用 `run_detect_changes`
  </acceptance_criteria>
  <verify>
    <automated>cd server && uv run python -c "from agents.tools.schemas.graph_tools import DetectChangesToolInput; from agents.tools.graph_tools import detect_changes; assert 'compare' in DetectChangesToolInput.model_fields and 'branch' not in DetectChangesToolInput.model_fields"</automated>
  </verify>
  <done>对话薄壳与 pydantic 契约落地</done>
</task>

<task type="auto">
  <name>Task 2: 导出、白名单与注册测</name>
  <files>server/agents/tools/__init__.py, server/agents/chat_runner.py, server/tests/agents/tools/test_graph_tools.py</files>
  <read_first>
    - `server/agents/tools/__init__.py`（impact/trace 导入与 `__all__`）
    - `server/agents/chat_runner.py`：`_INDEXED_TOOL_NAMES`（约 86–105）
    - `server/tests/agents/tools/test_graph_tools.py`（122 注册/fail-closed 范式）
    - `server/tests/mcp_tools/test_detect_changes_tools.py`（123-00 仅作对照：`test_conversational_detect_changes_registered` 桩仍在该文件；本 task **不改**该文件以避免与同波 123-03 文件冲突——把可执行断言落到 `test_graph_tools.py`）
  </read_first>
  <action>
1. `__init__.py` import + `__all__` 增加 `detect_changes`
2. `chat_runner._INDEXED_TOOL_NAMES` 在 `impact_analysis` / `trace_call_path` 旁追加 `"detect_changes"`（注册≠暴露：缺此项则工具对索引会话不可见）
3. 在 `tests/agents/tools/test_graph_tools.py` 新增并转绿：
   - `test_detect_changes_registered_in_indexed_tools`（名称在 `_INDEXED_TOOL_NAMES` 且 `agents.tools` 可 import）
   - `test_detect_changes_fail_closed_without_user`（无 conversation user → success=False；与 impact 同构 redact 路径若适用）
4. 123-00 留在 `test_detect_changes_tools.py` 的 `test_conversational_detect_changes_registered` skip 桩由 **123-05** 删除或改为指向/复用 agents 侧断言，避免本波与 123-03 争用该文件。

⛔ 不在壳内解析 diff / 调用 impact 内核。⛔ 不碰 `mcp/` submodule。⛔ 本 plan 不修改 `tests/mcp_tools/test_detect_changes_tools.py`。
  </action>
  <acceptance_criteria>
    - `"detect_changes" in` `chat_runner._INDEXED_TOOL_NAMES`
    - `cd server && uv run pytest tests/agents/tools/test_graph_tools.py -k detect_changes -q --reuse-db` 通过
  </acceptance_criteria>
  <verify>
    <automated>cd server && GALAXY_CACHE_WARM_ON_STARTUP=False GRAPH_BUILD_ORPHAN_RECONCILE_ON_STARTUP=False uv run pytest tests/agents/tools/test_graph_tools.py -k detect_changes -q --reuse-db</automated>
  </verify>
  <done>对话面注册并暴露给索引模式会话（agents 侧绿测；mcp 测试文件留给 03/05）</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chat session → tool | conversation owner 必须解析成功 |
| tool → orchestrator | 不绕过 ACL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-123-ACL | Information Disclosure | `_resolve_conversation_user` + orchestrator | mitigate | fail-closed；可读性复核 |
| T-123-TRACE | Information Disclosure | `_record_chat_retrieval` | mitigate | tool_trace_payload counts-only |
| T-123-BASE | Tampering | compare/base_ref params | mitigate | 与 MCP 同表；编排锁定基线 |
| T-123-DOS | Denial of Service | pydantic ge/le | mitigate | 与 impact 同上下界 |
| T-123-CRED | Information Disclosure | ToolResult error | mitigate | redact_secrets_in_text（沿用 impact 壳） |
| T-123-EXCL | Information Disclosure | data passthrough | mitigate | 不二次拼装；编排已过滤 |
| T-123-SC | Tampering | packages | accept | 零新包 |
</threat_model>

<verification>
conversational 注册测绿；与 123-03 合并后具备双面哨兵前置条件。
</verification>

<success_criteria>
- D-13 对话半边完成；参数命名满足 D-02/D-15 透出需求
</success_criteria>

<output>
Create `.planning/phases/123-detect-changes/123-04-SUMMARY.md` when done
</output>
