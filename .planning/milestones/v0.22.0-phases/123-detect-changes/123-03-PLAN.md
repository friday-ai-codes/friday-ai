---
phase: 123-detect-changes
plan: 03
type: execute
wave: 3
depends_on: ["123-02"]
files_modified:
  - server/mcp_tools/serializers.py
  - server/mcp_tools/views.py
  - server/mcp_tools/urls.py
  - server/tests/mcp_tools/test_schema_snapshot.py
  - server/tests/mcp_tools/test_detect_changes_tools.py
autonomous: true
requirements: [DIFF-01, DIFF-02]

must_haves:
  truths:
    - "未带 PAT 调用 detect_changes → 401 authentication_failed（fail-closed）"
    - "MCP 壳零算法：只校验 → _get_indexed_repo → run_detect_changes → 原样透出 + run_id"
    - "请求字段 compare 必填、base_ref 可选；不复用 branch 作文叠 overlay（D-02）"
    - "TOOL_SCHEMA_SNAPSHOT 与 urls 同批增加 detect_changes；接受 mcp 包漂移 +1（D-27）"
  artifacts:
    - path: "server/mcp_tools/views.py"
      provides: "DetectChangesView"
      contains: "class DetectChangesView"
    - path: "server/mcp_tools/serializers.py"
      provides: "DetectChangesRequestSerializer + TOOL_SCHEMA_SNAPSHOT['detect_changes']"
      contains: "class DetectChangesRequestSerializer"
    - path: "server/mcp_tools/urls.py"
      provides: "tools/detect_changes/"
      contains: "detect_changes"
  key_links:
    - from: "DetectChangesView"
      to: "run_detect_changes"
      via: "薄壳委托（D-13）"
      pattern: "run_detect_changes"
    - from: "urls.py"
      to: "TOOL_SCHEMA_SNAPSHOT"
      via: "snapshot 双向断言同批落地"
      pattern: "tools/detect_changes/"
---

<objective>
交付 detect_changes 的 **MCP 面**：`DetectChangesRequestSerializer` + `DetectChangesView` + url + schema snapshot，业务逻辑 100% 委托 `run_detect_changes`（D-13）。

Purpose: PAT 工具入口是 Phase 124 编码链自查的前置；本相位保证工具可被 PAT 调用。
Output: MCP 路由可打通；MCP 相关绿测；snapshot 漂移 7→8 被接受（不修 submodule）。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/123-detect-changes/123-CONTEXT.md
@.planning/phases/123-detect-changes/123-PATTERNS.md
@.planning/phases/122-impact-trace/122-08-PLAN.md
@server/mcp_tools/views.py
@server/mcp_tools/serializers.py
@server/services/code_graph_tools.py
</context>

## Artifacts this phase produces

| Symbol | Module |
|--------|--------|
| `DetectChangesRequestSerializer` | `mcp_tools.serializers` |
| `TOOL_SCHEMA_SNAPSHOT["detect_changes"]` / `TOOL_SCHEMAS` 若分表 | `mcp_tools.serializers` |
| `DetectChangesView` | `mcp_tools.views` |
| url `tools/detect_changes/` name=`mcp-tool-detect-changes` | `mcp_tools.urls` |
| schema snapshot 测试字面量条目 | `tests/mcp_tools/test_schema_snapshot.py` |

<tasks>

<task type="auto">
  <name>Task 1: Serializer + TOOL_SCHEMA_SNAPSHOT 条目</name>
  <files>server/mcp_tools/serializers.py, server/tests/mcp_tools/test_schema_snapshot.py</files>
  <read_first>
    - `server/mcp_tools/serializers.py`：`ImpactAnalysisRequestSerializer`（约 191–213）与 `TOOL_SCHEMA_SNAPSHOT["impact_analysis"]`（约 1146+）
    - `.planning/phases/123-detect-changes/123-RESEARCH.md` Open Question 2（compare 必填 + base_ref 可选；不要复用 branch）
    - `server/tests/mcp_tools/test_schema_snapshot.py`（第二份字面量必须同批）
  </read_first>
  <action>
新增 `DetectChangesRequestSerializer`（字段顺序即 snapshot request 顺序）：
- `repository_id`：UUIDField required
- `compare`：CharField required（feature 分支名或 40 位 sha；校验可复用 `_SAFE_REF_RE` 或允许 sha——与编排一致）
- `base_ref`：CharField required=False, allow_blank/null, default=None（D-02 声明用）
- 可选透传 impact 覆盖：`max_depth`（1–3, default 3）、`min_confidence`（0–1, default 1.0）、`include_low_confidence`（default False）、`limit`（1–200, default 200）——上下界与 impact 同表（T-123-DOS）
- ⛔ **不要**加 `branch` 图 overlay 字段（避免与交叠坐标混淆；D-02 / RESEARCH）

`validate()`：`compare` 非空；若同时像非法控制字符/ `..` 则 ValidationError。

在 `TOOL_SCHEMA_SNAPSHOT` 增加 `"detect_changes"` 条目（request/response 键与 Impact 条目同形态）。同步更新 `test_schema_snapshot.py` 手写第二份字面量。

⛔ 本 task 可先不挂 url（若 snapshot 测试不依赖 url）；若 `test_registered_tools_match_snapshot` 要求 url↔snapshot 双向，则与 Task 2 同 commit 落地——**推荐 Task 1+2 连续执行、同一波提交**，避免中间态红。
  </action>
  <acceptance_criteria>
    - `DetectChangesRequestSerializer` 可 import；字段含 `compare`，不含用于 overlay 的 `branch`
    - `TOOL_SCHEMA_SNAPSHOT` 含 `"detect_changes"` 键
    - schema snapshot 测试文件含对应字面量
  </acceptance_criteria>
  <verify>
    <automated>cd server && uv run python -c "from mcp_tools.serializers import DetectChangesRequestSerializer, TOOL_SCHEMA_SNAPSHOT; assert 'detect_changes' in TOOL_SCHEMA_SNAPSHOT; f=DetectChangesRequestSerializer().fields; assert 'compare' in f and 'branch' not in f"</automated>
  </verify>
  <done>MCP 请求契约与 snapshot 条目就位</done>
</task>

<task type="auto">
  <name>Task 2: DetectChangesView + url + MCP 绿测</name>
  <files>server/mcp_tools/views.py, server/mcp_tools/urls.py, server/tests/mcp_tools/test_detect_changes_tools.py, server/tests/mcp_tools/test_schema_snapshot.py</files>
  <read_first>
    - `server/mcp_tools/views.py`：`ImpactAnalysisView`（约 1286–1392）、`_graph_error_response`、`_mirror_error_response`、`McpToolView._begin/_validate/_get_indexed_repo/_record`
    - `server/mcp_tools/urls.py` impact/trace 路由旁
    - `.planning/phases/123-detect-changes/123-PATTERNS.md` §DetectChangesView
    - `.cursor/rules/observability-logging.mdc`
  </read_first>
  <action>
实现 `class DetectChangesView(McpToolView)`，`tool_name = "detect_changes"`，同构 ImpactAnalysisView：
1. `_begin` → `_validate(DetectChangesRequestSerializer)` → `_get_indexed_repo`
2. `try: result = await run_detect_changes(repository_id=…, repo=repo, user=request.user, compare=…, base_ref=…, …)`
3. `except GraphError: return _graph_error_response(exc)`
4. MirrorError：若编排已折信封则不会抛；若抛则 `_mirror_error_response`——与编排选定路径一致，双面同形优先编排折信封
5. `output_data = {**result, "run_id": str(run.run_id)}`；`ok=False` 仍 HTTP 200（与 impact 一致）
6. `RetrievalTrace` 一条：`tool_trace_payload(result, tool="detect_changes", …)`（T-123-TRACE）
7. caller 事件 best-effort：`code_graph_detect_changes_completed` 或壳层既有 mcp completed 模式；`component` 可为 `mcp_tools`（壳）而编排已打 `code_graph`
8. `await self._record(...)` → `Response(output_data, status=200)`

`urls.py`：`path("tools/detect_changes/", DetectChangesView.as_view(), name="mcp-tool-detect-changes")` 紧邻 impact/trace。

转绿 `test_mcp_detect_changes_requires_pat` 与 `test_mcp_detect_changes_success_envelope`（mock mirror + 最小 Symbol fixture；⛔ 不许 mock `run_detect_changes`——可 mock `diff_mirror` / `ensure_mirror_commit`）。

⛔ 不碰 `mcp/` submodule（D-13/D-27）。⛔ 不改 `repo_router_v2.py`。
  </action>
  <acceptance_criteria>
    - `cd server && uv run pytest tests/mcp_tools/test_detect_changes_tools.py -k 'mcp_detect_changes' -q --reuse-db` 通过
    - `urls.py` 含 `tools/detect_changes/`
    - View 源码调用 `run_detect_changes` 且不含交叠/BFS 算法函数定义
  </acceptance_criteria>
  <verify>
    <automated>cd server && GALAXY_CACHE_WARM_ON_STARTUP=False GRAPH_BUILD_ORPHAN_RECONCILE_ON_STARTUP=False uv run pytest tests/mcp_tools/test_detect_changes_tools.py -k 'mcp_detect_changes' tests/mcp_tools/test_schema_snapshot.py -q --reuse-db</automated>
  </verify>
  <done>MCP PAT 入口可调用 detect_changes；snapshot 与 url 同批</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → MCP view | PAT 认证；serializer 校验 |
| View → orchestrator | 薄壳不旁路 ACL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-123-ACL | Information Disclosure | `_get_indexed_repo` + orchestrator ACL | mitigate | 未授权不得返回 symbols |
| T-123-BASE | Tampering | serializer `base_ref` | mitigate | 字段只透传；编排锁定左端 |
| T-123-DOS | Denial of Service | max_depth/limit 上下界 | mitigate | 与 impact 同表 |
| T-123-TRACE | Information Disclosure | RetrievalTrace | mitigate | tool_trace_payload counts-only |
| T-123-CRED | Information Disclosure | error responses | mitigate | 沿用 scrubbed MirrorError |
| T-123-EXCL | Information Disclosure | output | mitigate | 编排已过滤；MCP 不二次拼装 |
| T-123-SC | Tampering | mcp submodule | accept | D-27 不改 submodule；漂移记账归 123-05 |
</threat_model>

<verification>
MCP PAT/成功信封测 + schema snapshot 相关断言绿（mcp 包对齐测允许继续红）。
</verification>

<success_criteria>
- D-13 MCP 半边完成；D-02 请求参数形态正确
</success_criteria>

<output>
Create `.planning/phases/123-detect-changes/123-03-SUMMARY.md` when done
</output>
