---
phase: 71-observability-foundation
plan: "71-03"
type: execute
wave: 2
depends_on: ["71-02"]
files_modified:
  - server/system/models.py
  - server/system/settings_service.py
  - server/system/signals.py
  - server/common/logging.py
  - server/system/log_sink.py
  - .planning/observability/LOGGING-SPEC.md
  - server/tests/test_log_runtime_config.py
autonomous: true
requirements: [LOG-05, LOG-06]
user_setup: []

must_haves:
  truths:
    - "运行时改日志级别（全局/分组件）、堆栈记录阈值、采样初始/后续、保留天数/大小，实时生效无需重启"
    - "固定的 _resolve_structlog_level() 改为可热更新级别 + logging.setLevel() 即时生效"
    - "每条落库日志带 category(caller/sampling) + component；sampling 类按运行时采样配置记录（首 N 全记，之后按比例）"
    - "LOGGING-SPEC.md 事件目录补全本 Phase 已知事件（caller/sampling 分类 + 组件归类）"
  artifacts:
    - path: "server/system/models.py"
      provides: "SettingKeys.LOG_* 运行时配置键"
      contains: "LOG_LEVEL"
    - path: "server/common/logging.py"
      provides: "可热更新过滤级别（动态 wrapper / 运行时读级别）+ category/component helper"
      contains: "get_logger"
    - path: "server/system/signals.py"
      provides: "LOG_* 写时失效 + 即时调整 filtering level（logging.setLevel）"
      contains: "LOG_"
  key_links:
    - from: "server/system/signals.py"
      to: "common.logging 运行时级别"
      via: "post_save(SystemSetting) 命中 LOG_* → 重设过滤级别"
      pattern: "LOG_"
    - from: "server/system/log_sink.py"
      to: "settings_service LOG_SAMPLING_*"
      via: "enqueue 前按采样配置决定是否记录 sampling 类事件"
      pattern: "sampling"
---

<objective>
让日志"控得住"（LOG-06 运行时配置 + LOG-05 分类/目录）：复用 `SystemSetting` + `settings_service`(60s 缓存) + `signals`(写时失效) 新增 `SettingKeys.LOG_*`（全局级别、分组件级别 map、堆栈阈值、采样初始/后续、保留天数/大小）；把固定的 `_resolve_structlog_level()` 改为可热更新；为业务事件提供 `category`(caller/sampling) + `component` 约定 helper，sampling 类按运行时采样配置记录；补全 `LOGGING-SPEC.md` 事件目录。

Purpose: 给日志中心装上"运行时旋钮"与"分类骨架"——级别/采样/保留可热改即时生效；caller/sampling 分类让高频内部步骤可采样、用户可归因调用全记。
Output: SettingKeys.LOG_* + settings_service 读取 helper + signals 热更新；common/logging 动态级别 + category/component helper；log_sink 采样过滤；LOGGING-SPEC 事件目录补全；测试。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-observability-foundation/71-CONTEXT.md
@.planning/observability/LOGGING-SPEC.md
@.cursor/rules/observability-logging.mdc

# 关键现有符号
@server/system/models.py            # SettingKeys 常量类（新增 LOG_* 字符串键）
@server/system/settings_service.py  # get_setting/get_bool_setting/get_int_setting + async 变体；60s cache + __none__ 哨兵
@server/system/signals.py           # post_save/post_delete(SystemSetting) → _invalidate_setting_cache；可加 LOG_* 即时生效
@server/common/logging.py           # configure_structlog；_resolve_structlog_level（当前读 env，固定）；make_filtering_bound_logger
@server/system/log_sink.py          # 71-02 产出：enqueue_system_log + counters（采样过滤挂这里）
</context>

<tasks>

<task type="auto">
  <name>Task 1: SettingKeys.LOG_* + settings_service 读取 + signals 热更新级别（LOG-06）</name>
  <files>server/system/models.py, server/system/settings_service.py, server/system/signals.py, server/common/logging.py, server/tests/test_log_runtime_config.py</files>
  <action>
新增运行时日志配置（复用 SystemSetting + settings_service + signals，per 71-CONTEXT LOG-06）：
- `server/system/models.py` `SettingKeys` 增常量（字符串值用点分命名，与既有 `code_index.exclusion.*` 风格一致）：
  - `LOG_LEVEL = "log.level"`（全局，DEBUG/INFO/WARNING/ERROR）。
  - `LOG_COMPONENT_LEVELS = "log.component_levels"`（JSON map：`{component: level}`，分组件覆盖全局）。
  - `LOG_STACK_THRESHOLD = "log.stack_threshold"`（记录堆栈的最低级别，如 ERROR）。
  - `LOG_SAMPLING_INITIAL = "log.sampling_initial"`（int，首 N 条全记，默认 50）。
  - `LOG_SAMPLING_RATE = "log.sampling_rate"`（float 0..1，之后按比例，默认 0.1）。
  - `LOG_RETENTION_DAYS = "log.retention_days"`（int，默认 30；保留清理在 71-04 消费）。
  - `LOG_RETENTION_SIZE = "log.retention_max_rows"`（int，按行数上限兜底，默认 1_000_000；71-04 消费）。
- `server/system/settings_service.py` 增 helper：`get_json_setting(key, default: dict|None=None) -> dict`（json.loads，失败回默认）+ `get_float_setting(key, default=0.0) -> float`（沿用 `_get_raw` 缓存 + try/except）。导出供 common.logging / log_sink 读取。
- `server/common/logging.py` 把固定级别改为**可热更新**（per 71-CONTEXT LOG-06）：
  - 保留 `_resolve_structlog_level()` 但改为：优先读 `settings_service.get_setting(SettingKeys.LOG_LEVEL)`（局部 import 防加载期循环；DB 不可用/空时回退现有 env 逻辑 → INFO）。
  - 提供 `def apply_log_level(level_name: str | None = None) -> None`：解析级别后 `logging.getLogger().setLevel(...)` + 重设 structlog filtering（重新 `structlog.configure(wrapper_class=make_filtering_bound_logger(level))` 或维护一个可变 filtering level——选 structlog 文档推荐的运行时方式；优先用 `structlog.configure` 重设 wrapper_class，幂等）。即时生效无需重启。
  - `configure_structlog()` 末尾调用 `apply_log_level()` 用 DB 级别覆盖（DB 不可用静默回退 env）。
- `server/system/signals.py` 在 `on_system_setting_saved`/`on_system_setting_deleted` 增：命中 `LOG_*` 键时调用 `common.logging.apply_log_level()`（局部 import，best-effort try/except warning，不阻塞写入路径）；保持既有 cache 失效 + qdrant reset 逻辑不动。
新建 `server/tests/test_log_runtime_config.py`：
  - 写 `SystemSetting(key="log.level", value="DEBUG")` → signal 触发 → `apply_log_level` 后 debug 事件进 stdout（capfd）；改回 WARNING → debug 不出（实时生效，无需重启）。
  - `get_json_setting("log.component_levels")` 解析 map；非法 JSON 回默认。
  - settings cache 失效后下次读到新值（与现有 _invalidate_setting_cache 协同）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/test_log_runtime_config.py -x -q</automated>
  </verify>
  <done>SettingKeys.LOG_* 就位；settings_service 增 json/float helper；改 LOG_LEVEL 经 signal 即时调整过滤级别（无需重启）；_resolve_structlog_level 改为读 DB 优先、env 回退；测试证明热更新生效。</done>
</task>

<task type="auto">
  <name>Task 2: category/component helper + 采样过滤 + LOGGING-SPEC 事件目录补全（LOG-05）</name>
  <files>server/common/logging.py, server/system/log_sink.py, .planning/observability/LOGGING-SPEC.md, server/tests/test_log_runtime_config.py</files>
  <action>
让每条日志带 `category`(caller/sampling) + `component`，sampling 类按运行时采样配置记录（per 71-CONTEXT LOG-05 / LOGGING-SPEC §2）。**不强制重写全仓事件**——提供地基 helper + 默认推断，存量渐进迁移。
- `server/common/logging.py` 增 helper（per 71-CONTEXT「提供 helper」）：
  - `def bound_logger(name: str, *, component: str | None = None)`：薄包 `structlog.get_logger(name)`，默认从 logger name 推 component（取模块路径首段映射到 LOGGING-SPEC §5 组件清单；推不出留空），可显式覆盖。返回已 `bind(component=...)` 的 logger。约定业务用 `category=` 显式传（caller 关键调用），缺省视为 sampling。
  - 新增 processor `def annotate_category_component(logger, method_name, event_dict)`：若 event_dict 无 `component`，从 `logger_name`/`logger` 末段推断；若无 `category`，默认 `"sampling"`（高频内部步骤默认采样类，per 判断口诀）。挂在 `merge_contextvars` 之后、`enqueue_system_log` 之前（保证落库带 category/component）。
- `server/system/log_sink.py` 采样过滤：`enqueue_system_log(entry)` 内，对 `category=="sampling"` 的事件按运行时采样配置决定是否真正入队——读 `LOG_SAMPLING_INITIAL`（首 N 条全记，按 `(component,event)` 维度的进程内计数器）+ `LOG_SAMPLING_RATE`（之后 `random.random() < rate` 才记）；`category=="caller"` 全量记录（绝不采样丢弃，per LOGGING-SPEC §2）。采样配置经 `settings_service.get_int_setting/get_float_setting`（局部 import，60s 缓存命中即可，不每条打库）。被采样丢弃**不**计入 `log_dropped_total`（那是队列满语义，区分开；可加独立 `_sampled_out` 计数）。整体 best-effort try/except。
- 补全 `.planning/observability/LOGGING-SPEC.md`：在 §5 之后或新增「§10 事件目录（本 Phase 已知事件）」小节，列出 Phase 71 已知事件名 + 其 category + component（如 `system_log_flush_failed`(sampling/logging)、`webhook_received`(caller/webhook)、`observability_served`(caller/system)、`background_runner_started`(sampling/system) 等本 Phase 涉及/新增事件）。注明"72+ 增量补全"（per 71-CONTEXT LOG-05），不试图穷举全仓。
扩 `tests/test_log_runtime_config.py`：
  - `annotate_category_component`：无 category 默认 sampling；显式 caller 保留；component 从 logger name 推断。
  - 采样：LOG_SAMPLING_INITIAL=2 + RATE=0 → 同 (component,event) 前 2 条入队、第 3 条不入队；category=caller 不受采样影响全入队。
  - capfd/落库断言 component/category 字段出现。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/test_log_runtime_config.py -x -q</automated>
  </verify>
  <done>每条日志带 category/component（缺省推断/默认 sampling）；sampling 类按运行时初始 N + 比例采样、caller 全记；LOGGING-SPEC 事件目录补全本 Phase 已知事件；测试覆盖分类推断与采样行为。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 超管运行时配置 → 日志级别/采样 | 配置变更即时改变记录行为 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-03-01 | Denial of Service | 误设 LOG_LEVEL=DEBUG 致高频刷屏 | mitigate | sampling 类即使 DEBUG 也按比例采样落库；caller 全量但量级低；级别可随时回调（热更新双向） |
| T-71-03-02 | Tampering | 非法采样配置（负数/越界）破坏过滤 | mitigate | settings_service helper try/except 回默认；rate clamp 0..1，initial 取 max(0,int) |
| T-71-03-03 | Repudiation | 采样把 caller 调用丢弃致无法归因 | mitigate | caller 类强制全量记录，绝不参与采样（LOGGING-SPEC §2 契约） |
| T-71-03-SC | Tampering | npm/pip/cargo 安装 | accept | 不新增依赖 |

## Redaction 注记
本 plan 不改脱敏链路顺序（`redact_credentials` 仍在 renderer 前）；`annotate_category_component` 只加 category/component 元字段、不引入用户输入原文。`test_credential_leak_protection.py` 不受影响（71-02/04 各自复跑）。
</threat_model>

<verification>
- `uv run pytest tests/test_log_runtime_config.py -q` 全绿。
- 改 `SystemSetting log.level` 后无需重启即生效（测试断言 capfd）。
- LOGGING-SPEC.md 事件目录小节存在且列出本 Phase 事件 + category/component。
- `uv run ruff check server/common/logging.py server/system/settings_service.py server/system/signals.py server/system/log_sink.py` 干净。
</verification>

<success_criteria>
- LOG-06：级别(全局/分组件)/堆栈阈值/采样初始·后续/保留天数·大小运行时可改、实时生效；_resolve_structlog_level 改为热更新。
- LOG-05：每条日志带 category(caller/sampling)+component；sampling 采样、caller 全记；LOGGING-SPEC 事件目录补全本 Phase 已知事件。
- 零回归：DB 不可用时级别回退 env→INFO；存量事件无 category 默认 sampling，不强制重写。
</success_criteria>

<output>
Create `.planning/phases/71-observability-foundation/71-03-SUMMARY.md` when done.
</output>
