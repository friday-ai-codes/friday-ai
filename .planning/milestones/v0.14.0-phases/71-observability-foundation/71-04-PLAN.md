---
phase: 71-observability-foundation
plan: "71-04"
type: execute
wave: 2
depends_on: ["71-02"]
files_modified:
  - server/system/log_views.py
  - server/system/serializers.py
  - server/system/urls_system.py
  - server/system/log_retention.py
  - server/agents/management/commands/runapscheduler.py
  - server/tests/test_system_log_api.py
autonomous: true
requirements: [LOG-01, LOG-03, LOG-08]
user_setup: []

must_haves:
  truths:
    - "系统日志可按最新时间倒序查看，并按组件/级别(debug·info·warn·error)/用户/来源/关键词/时间段筛选与全文搜索（SQLite icontains 降级）"
    - "顶部计数返回队列(x/5000)/写入/丢弃/失败四计数"
    - "日志可按条件（时间/级别/组件/用户/关键词）批量清理"
    - "保留策略到期定时自动清理 SystemLogEntry + InboundWebhookEvent（apscheduler，按天数/行数）"
  artifacts:
    - path: "server/system/log_views.py"
      provides: "SystemLogQueryView（筛选+全文+分页+计数）+ SystemLogClearView（按条件批量删）"
      contains: "class SystemLogQueryView"
    - path: "server/system/log_retention.py"
      provides: "purge_system_logs / purge_webhook_events（按 LOG_RETENTION_DAYS/SIZE）"
      contains: "def purge_system_logs"
    - path: "server/agents/management/commands/runapscheduler.py"
      provides: "保留清理周期任务（daily）"
      contains: "purge_observability_logs"
  key_links:
    - from: "server/system/urls_system.py"
      to: "system.log_views"
      via: "/api/system/logs/ 查询 + /api/system/logs/clear/ 清理路由"
      pattern: "log_views"
    - from: "server/agents/management/commands/runapscheduler.py"
      to: "system.log_retention.purge_system_logs"
      via: "apscheduler CronTrigger daily"
      pattern: "log_retention"
---

<objective>
让日志中心"可搜索、可清理、可保留"（LOG-01 查询 + LOG-03 按用户筛选 + LOG-08 清理）：把现有内存缓冲版 `SystemLogsView` 升级为基于 `SystemLogEntry` 的查询 API（时间倒序 + 组件/级别/用户/来源/关键词/时间段筛选与全文搜索，SQLite icontains 降级）+ 顶部四计数；新增按条件批量清理 API + apscheduler 保留策略定时自动清理（SystemLogEntry + InboundWebhookEvent）。

Purpose: 兑现"出事时能检索、能按发起人筛、能按条件清、到期自动清"，为 Phase 75 日志页提供后端 API。
Output: `system/log_views.py`（查询 + 清理）+ serializers + urls_system 接入 + `system/log_retention.py` + apscheduler 周期任务 + 测试。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-observability-foundation/71-CONTEXT.md
@.planning/observability/LOGGING-SPEC.md
@.cursor/rules/observability-logging.mdc

# 关键现有符号
@server/system/observability_views.py  # SystemLogsView（现内存缓冲版，IsSuperUser，adrf APIView）；ObservabilityView 范式
@server/system/urls_system.py          # /api/system/ 路由（logs/ 现挂 SystemLogsView）
@server/system/log_sink.py             # 71-02：snapshot_counters() 四计数
@server/system/serializers.py          # 现有 serializer 风格
@server/agents/management/commands/runapscheduler.py  # apscheduler add_job 范式（CronTrigger daily，*_job wrapper，run_async_task）
@server/system/permissions.py          # IsSuperUser（fail-closed 运维端点）
</context>

<tasks>

<task type="auto">
  <name>Task 1: 日志查询/筛选/全文 + 四计数 API（LOG-01 查询, LOG-03）</name>
  <files>server/system/log_views.py, server/system/serializers.py, server/system/urls_system.py, server/tests/test_system_log_api.py</files>
  <action>
新建 `server/system/log_views.py`（adrf APIView + `IsSuperUser`，沿用 observability_views 范式；async ORM 走 `sync_to_async`，per STATE 异步约束）：
- `class SystemLogQueryView(APIView)`（`permission_classes=[IsSuperUser]`）：`async def get`——基于 `SystemLogEntry` 查询，默认 `order_by("-ts")`（倒序）。查询参数（全部可选，组合 AND）：
  - `component`（精确）、`level`（精确，debug/info/warn/error，归一小写）、`user_id`（精确）、`source`（精确）。
  - `start` / `end`（ISO8601 时间段，`ts__gte`/`ts__lte`，解析失败忽略该界）。
  - `keyword`（全文：`message__icontains`；PG 与 SQLite 均用 icontains——量级低不引专用全文索引，SQLite dev 天然降级，per STATE 关键约束 + 71-CONTEXT）。
  - `limit`（默认 100，最大 500）+ `offset`（分页）。
  - 返回 `{"items": [...], "total": <acount>, "counters": <log_sink.snapshot_counters()>}`——顶部计数即队列(x/5000)/写入/丢弃/失败（per 71-CONTEXT「队列四计数必须可被快照采集」，UI-04 顶部计数前置）。查询经 `sync_to_async` 桥接；序列化用 `SystemLogEntrySerializer`。
- `server/system/serializers.py` 增 `class SystemLogEntrySerializer(serializers.ModelSerializer)`：暴露 ts/level/component/category/event/message/user_id/source/trace_id/request_id/payload/correlation/id（全 read_only；payload/correlation 已脱敏直出）。
- `server/system/urls_system.py`：把 `path("logs/", ...)` 改指向 `SystemLogQueryView`（替换内存缓冲版 `observability_views.SystemLogsView`——保留向后兼容查询参数 `limit`/`level`，新增的筛选参数叠加）。**注意路由顺序**：`logs/clear/`（Task 2）必须排在 `logs/` 之前或用不冲突的明确路径，避免被通配拦截（参考 system/urls.py 中 info/ 排在 `<str:key>/` 之前的惯例）。
新建 `server/tests/test_system_log_api.py`（pytest-django + 超管 client）：
  - 造 N 条 SystemLogEntry（不同 component/level/user_id/source/ts/message）→ GET 无参返回倒序全部 + counters 含 queued/max/enqueued/written/dropped/write_failed 键。
  - 按 component / level / user_id / source / keyword(icontains) / start-end 各筛一次命中正确子集。
  - 非超管 403（IsSuperUser fail-closed）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/test_system_log_api.py -x -q -k "query or filter or counters or permission"</automated>
  </verify>
  <done>GET /api/system/logs/ 基于 SystemLogEntry 倒序 + 组件/级别/用户/来源/关键词/时间段筛选与全文（icontains 降级）+ 顶部四计数；IsSuperUser 保护；测试覆盖各筛选维度与权限。</done>
</task>

<task type="auto">
  <name>Task 2: 按条件批量清理 API + 保留策略定时自动清理（LOG-08）</name>
  <files>server/system/log_views.py, server/system/log_retention.py, server/system/urls_system.py, server/agents/management/commands/runapscheduler.py, server/tests/test_system_log_api.py</files>
  <action>
新增按条件清理 + 保留策略定时清理（per 71-CONTEXT LOG-08）：
- `server/system/log_views.py` 增 `class SystemLogClearView(APIView)`（`IsSuperUser`）：`async def post`——body 同 Task 1 的筛选条件（time/level/component/user/keyword），构造同款 queryset 后 `await queryset.adelete()`，返回 `{"deleted": <count>}`。**必须**要求至少一个筛选条件或显式 `confirm_all=true`，防止误清全表（无条件 + 无 confirm → 400 中文报错）。审计：用 `audit.services.AuditService.aemit`（若可用）记一条 caller 类操作（actor=request.user, source="api"），best-effort。
- 新建 `server/system/log_retention.py`：
  - `async def purge_system_logs() -> dict`：读 `settings_service` 的 `LOG_RETENTION_DAYS`（默认 30）+ `LOG_RETENTION_SIZE`（行数上限，默认 1_000_000）。先按 `ts__lt = now - days` 删旧；再若总行数仍 > size，按 `ts` 升序删最旧的超出部分（`.values_list("id")` 取待删 id 批量 `adelete`）。返回 `{"by_age": n1, "by_size": n2}`。best-effort（异常 warning，不抛）。
  - `async def purge_webhook_events() -> dict`：对 `InboundWebhookEvent` 同款保留清理（按 `received_at`，per 71-CONTEXT「InboundWebhookEvent 同款保留清理」）。
- `server/agents/management/commands/runapscheduler.py`：新增 job wrapper `def purge_observability_logs_job()`（沿用既有 `*_job` + `run_async_task` 范式，bind job 日志），内部 `await purge_system_logs()` + `await purge_webhook_events()`；`scheduler.add_job(purge_observability_logs_job, trigger=CronTrigger(hour=4, minute=30), id="purge_observability_logs", max_instances=1, replace_existing=True)`（选 04:30 错开既有 03:00/03:30/04:00 清理任务避免争 SQLite 写锁，per 既有 Claude's Discretion 惯例）。`logger.info("job_registered", job="purge_observability_logs", ...)`。
- `server/system/urls_system.py`：加 `path("logs/clear/", SystemLogClearView.as_view(), name="system-logs-clear")`，**排在** `path("logs/", ...)` 之前（避免被通配/前缀语义影响；Django path 精确匹配无前缀吞噬，但保持显式顺序惯例）。
扩 `tests/test_system_log_api.py`：
  - 清理：造数据 → POST clear with `level=debug` → 仅删 debug 行、返回 deleted 计数；无条件无 confirm → 400；`confirm_all=true` → 全删。
  - 保留：造 ts 跨越 retention_days 的行 + 设 `SystemSetting log.retention_days=7` → `await purge_system_logs()` 删 7 天前的、保留近 7 天；行数超 size 时按最旧删到 size。InboundWebhookEvent 同款一条断言。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/test_system_log_api.py -x -q -k "clear or retention or purge"</automated>
  </verify>
  <done>POST /api/system/logs/clear/ 按条件批量删（无条件需 confirm_all，防误清）；purge_system_logs/purge_webhook_events 按天数+行数清理 best-effort；apscheduler 注册 daily 04:30 保留清理任务；测试覆盖按条件清理 + 保留按天数/行数 + webhook 同款。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 超管 → 日志查询/清理 API | 运维端点，读/删持久化日志 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-04-01 | Elevation of Privilege | 非超管访问日志/清理 | mitigate | 所有视图 `IsSuperUser` fail-closed（沿用 observability_views 惯例） |
| T-71-04-02 | Denial of Service | 无条件批量删全表误操作 | mitigate | clear 要求至少一筛选条件或显式 confirm_all，否则 400 |
| T-71-04-03 | Information Disclosure | 关键词全文搜索回放敏感内容 | mitigate | 落库内容已在 71-02 脱敏（写入前），查询只读已脱敏行；payload 直出无明文 |
| T-71-04-04 | Denial of Service | keyword icontains 全表扫描慢 | accept | 量级低（自托管），复合索引覆盖常用筛选维度；全文不引专用索引（STATE 决策） |
| T-71-04-SC | Tampering | npm/pip/cargo 安装 | accept | 不新增依赖 |

## Redaction 注记
本 plan 只读/删已脱敏的 SystemLogEntry，不引入新落库内容；清理 API 不回显日志正文明文。审计 emit 经既有 AuditService（其入口已强制脱敏）。
</threat_model>

<verification>
- `uv run pytest tests/test_system_log_api.py -q` 全绿。
- `uv run pytest tests/test_scheduler_registration.py -q`（若存在）确认新 job 不破坏既有注册断言；否则人工核对 runapscheduler 注册日志含 purge_observability_logs。
- `uv run ruff check server/system/log_views.py server/system/log_retention.py` 干净。
</verification>

<success_criteria>
- LOG-01（查询）：倒序 + 组件/级别/用户/来源/关键词/时间段筛选与全文（icontains 降级）+ 四计数。
- LOG-03：可按 user_id 筛选（依赖 71-01 绑定的 user_id）。
- LOG-08：按条件批量清理（防误清）+ apscheduler 保留策略到期自动清理 SystemLogEntry + InboundWebhookEvent。
- 零回归：保留 limit/level 旧参数；IsSuperUser fail-closed。
</success_criteria>

<output>
Create `.planning/phases/71-observability-foundation/71-04-SUMMARY.md` when done.
</output>
