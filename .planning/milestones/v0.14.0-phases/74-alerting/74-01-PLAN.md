---
phase: 74-alerting
plan: "74-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - server/system/models.py
  - server/system/migrations/0012_systemalertrule_alertevent.py
  - server/system/alert_serializers.py
  - server/system/alert_views.py
  - server/system/alert_retention.py
  - server/system/urls_system.py
  - server/tests/test_system_alert_models.py
  - server/tests/test_system_alert_api.py
autonomous: true
requirements: [ALERT-01, ALERT-02]
user_setup: []

must_haves:
  truths:
    - "SystemAlertRule 模型落 system app（独立于 workflow AlertRule）：metric/op/value/window/dimension(jsonb)/severity(P0/P1/P2)/enabled/channels(jsonb email|feishu|webhook 子集)/cooldown/title_template，运行时可经 REST CRUD 增删改查（IsSuperUser）"
    - "AlertEvent 模型落 system app：severity/title_zh/rule_info(jsonb 机器可读)/rule FK/target(jsonb)/started_at/ended_at/duration_s/status(firing/resolved)/email_sent/notified_channels；同规则同对象去重由 (rule,target_key) status=firing 条件唯一约束在 DB 层兜底"
    - "GET /api/system/alerts/rules/ 列表 + POST 创建；GET/PATCH/DELETE /api/system/alerts/rules/<id>/ 单条 CRUD（IsSuperUser fail-closed）"
    - "GET /api/system/alerts/events/ 告警事件查询：按 severity/status/rule_id/时间段筛选 + 分页倒序，列对齐 REFERENCE-UI §1.4"
    - "SettingKeys 补 ALERT_EVAL_INTERVAL_SECONDS / ALERT_RETENTION_DAYS / ALERT_RETENTION_SIZE / ALERT_EMAIL_ENABLED / ALERT_EMAIL_RECIPIENTS / ALERT_FEISHU_CHAT_ID / ALERT_WEBHOOK_URL 常量（供 74-02 评估器与 74-03 通知消费）"
    - "alert_retention.purge_alert_events 镜像 Phase 71 log_retention（按 started_at 天数 + 行数上限清理），best-effort 绝不反噬；apscheduler 注册在 74-02"
  artifacts:
    - path: "server/system/models.py"
      provides: "SystemAlertRule + AlertEvent 模型 + SettingKeys.ALERT_* 常量"
      contains: "class SystemAlertRule"
    - path: "server/system/migrations/0012_systemalertrule_alertevent.py"
      provides: "两模型建表迁移（自动生成，base=0011_gaugesample）"
      contains: "SystemAlertRule"
    - path: "server/system/alert_views.py"
      provides: "SystemAlertRuleListCreateView / SystemAlertRuleDetailView / AlertEventListView（adrf APIView，IsSuperUser）"
      contains: "class SystemAlertRuleListCreateView"
    - path: "server/system/alert_serializers.py"
      provides: "SystemAlertRule 读/写序列化器 + AlertEvent 只读序列化器（受控枚举校验）"
      contains: "class SystemAlertRuleSerializer"
    - path: "server/system/alert_retention.py"
      provides: "purge_alert_events——按 started_at 天数 + 行数上限清理 AlertEvent，best-effort（镜像 log_retention）"
      contains: "async def purge_alert_events"
  key_links:
    - from: "server/system/urls_system.py"
      to: "system.alert_views.SystemAlertRuleListCreateView"
      via: "/api/system/alerts/rules/ 路由"
      pattern: "alerts/rules"
    - from: "server/system/alert_views.py"
      to: "system.models.SystemAlertRule"
      via: "CRUD 视图读写 SystemAlertRule"
      pattern: "SystemAlertRule"
    - from: "server/system/alert_retention.py"
      to: "system.models.AlertEvent"
      via: "purge_alert_events 按 started_at 清理"
      pattern: "AlertEvent.objects"
---

<objective>
建立系统级告警的数据载体与运维入口（ALERT-01 模型/CRUD + ALERT-02 事件模型/查询）：新增 `SystemAlertRule`（系统级阈值规则，**不复用** workflow `AlertRule`）与 `AlertEvent`（P0/P1/P2 告警事件，含去重/持续时长/firing-resolved/email_sent）两个模型 + 一条迁移；补 `SettingKeys.ALERT_*` 运行时配置常量；新增 REST CRUD（规则增删改查）+ AlertEvent 查询 API（IsSuperUser）；新增 `alert_retention.purge_alert_events`（镜像 Phase 71 `log_retention` 保留清理，apscheduler 注册留给 74-02）。

Purpose: 给 74-02 评估器（写 AlertEvent）与 74-03 通知分发（读 AlertEvent + 配置）提供模型契约与运行时配置载体；Phase 75 告警事件页消费查询 API。系统告警语义（CPU>85%、错误率、队列深）与 workflow 强绑模型不同，独立建模避免拧巴（MILESTONE-PROPOSAL §A.3）。去重硬约束（一条 firing，恢复收尾）由 `(rule, target_key)` 条件唯一约束在 DB 层兜底 + 74-02 服务层收口。
Output: 两个模型 + 0012 迁移、`SettingKeys.ALERT_*`、`alert_serializers.py`、`alert_views.py`（3 视图）、`alert_retention.py`、urls 接入、模型与 API 守护测试。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-alerting/74-CONTEXT.md
@.planning/observability/MILESTONE-PROPOSAL.md
@.planning/observability/REFERENCE-UI.md
@.cursor/rules/observability-logging.mdc

# 关键现有符号（执行时按需精读，勿全文重读）
@server/system/models.py               # SystemLogEntry/RequestMetric/GaugeSample 模型范式 + SettingKeys（ALERT_* 追加于末尾，仅常量无迁移）；db_table/中文 verbose_name/BigAutoField/JSONField default 惯例
@server/system/serializers.py          # ModelSerializer 读 + serializers.Serializer 写（create/update）+ ChoiceField 白名单防御范式（镜像 ProviderCredential 三件套）
@server/system/log_views.py            # adrf APIView + IsSuperUser + sync_to_async 查询 + _apply_filters/_extract_filters 筛选范式（AlertEventListView 镜像）
@server/system/urls_system.py          # path() 显式路由惯例（字面段排在通配前；运维端点聚集）
@server/system/log_retention.py        # _retention_config + purge_system_logs 按天数+行数清理范式（alert_retention 逐源镜像，时间列换 started_at）
@server/system/settings_service.py     # aget_int_setting/aget_setting（运行时配置 + 60s 缓存）
@server/workflows/models/execution.py  # AlertRule/AlertRuleExecution——**反面教材**：强绑 workflow（project 非空、condition 全 execution_*），确认不复用
@server/permissions/api_permissions.py # IsSuperUser（运维端点 fail-closed）
</context>

<tasks>

<task type="auto">
  <name>Task 1: SystemAlertRule + AlertEvent 模型 + SettingKeys.ALERT_* + 0012 迁移（ALERT-01/02 模型）</name>
  <files>server/system/models.py, server/system/migrations/0012_systemalertrule_alertevent.py, server/tests/test_system_alert_models.py</files>
  <action>
在 `server/system/models.py` 新增两个模型（中文 docstring/verbose_name，沿用本 app `BigAutoField`/`db_table`/`JSONField(default=...)`/复合 Index 惯例，`from __future__ import annotations` 已在文件顶部），落 `system` app（与 SystemLogEntry/RequestMetric/GaugeSample 同域，复用 settings/清理/查询设施）：

1. **`class SystemAlertRule(models.Model)`**（ALERT-01，独立于 `workflows.AlertRule`）：
   - `id = BigAutoField(primary_key=True)`。
   - `name = CharField(max_length=64)`（规则名，中文/英文）。
   - `metric = CharField(max_length=32, db_index=True)`（**受控枚举**：`qps`/`error_rate`/`ttft`/`cpu`/`memory`/`db_connections`/`redis_clients`/`qdrant`/`queue_depth`——校验在 serializer 侧 ChoiceField，模型层注释列出受控集合；趋势类 gauge:* 不纳入，per 74-CONTEXT「RATE-03 默认不参与」）。
   - `op = CharField(max_length=4)`（`gt`/`gte`/`lt`/`lte`）。
   - `value = FloatField()`（阈值）。
   - `window = PositiveIntegerField(default=300)`（评估窗口秒；时序类指标用，快照类忽略）。
   - `dimension = JSONField(default=dict, blank=True)`（**受控维度**，如 `{"provider": "anthropic"}` / `{"queue": "index"}`；空 dict=overall。注释强调禁用户输入原文当 label 基数失控）。
   - `severity = CharField(max_length=2)`（`P0`/`P1`/`P2`）。
   - `enabled = BooleanField(default=True, db_index=True)`。
   - `channels = JSONField(default=list, blank=True)`（`email`/`feishu`/`webhook` 子集列表；空=不通知仅落事件）。
   - `cooldown = PositiveIntegerField(default=600)`（同事件再次通知的冷却秒数，防抖；0=不额外冷却）。
   - `title_template = CharField(max_length=200, blank=True, default="")`（中文标题模板，支持 `{metric}`/`{current}`/`{value}` 占位，由 74-02 渲染；空则用默认拼接）。
   - `created_at = DateTimeField(auto_now_add=True)` / `updated_at = DateTimeField(auto_now=True)`。
   - `Meta`：`db_table="system_alert_rules"`，`verbose_name="系统告警规则"`，`ordering=["-created_at"]`，`indexes=[Index(fields=["enabled", "metric"])]`。
2. **`class AlertEvent(models.Model)`**（ALERT-02，含去重/持续时长/恢复/邮件状态）：
   - `id = BigAutoField(primary_key=True)`。
   - `rule = ForeignKey(SystemAlertRule, on_delete=models.SET_NULL, null=True, blank=True, related_name="events")`（规则删除不抹历史事件）。
   - `severity = CharField(max_length=2, db_index=True)`（冗余规则 severity，便于按级别查询/排序，规则删了仍可见）。
   - `title_zh = CharField(max_length=200, blank=True, default="")`（中文标题，由 74-02 按 title_template 渲染）。
   - `rule_info = JSONField(default=dict, blank=True)`（**机器可读**：`{"metric","op","threshold","current","window_s","dimension","expr"}`，`expr` 为 REFERENCE-UI §1.4 同款字符串 `cpu_usage_percent > 85.00 (current 95.40) over last 5m (overall)`）。
   - `target = JSONField(default=dict, blank=True)`（对象标识 jsonb，如 `{"provider":"anthropic"}`；overall 为 `{}`）。
   - `target_key = CharField(max_length=128, blank=True, default="")`（target 的规范化 JSON 串，由 74-02 写入；**仅用于 DB 层去重约束**，见 constraints）。
   - `status = CharField(max_length=10, default="firing", db_index=True)`（`firing`/`resolved`）。
   - `started_at = DateTimeField(db_index=True)`（首次触发时刻）。
   - `ended_at = DateTimeField(null=True, blank=True)`（恢复时刻，firing 时 null）。
   - `duration_s = PositiveIntegerField(null=True, blank=True)`（恢复时回写 ended-started 秒数）。
   - `current_value = FloatField(null=True, blank=True)`（最近一次评估的当前值，74-02 重复评估时更新）。
   - `last_seen_at = DateTimeField(null=True, blank=True)`（最近一次仍超阈的评估时刻）。
   - `email_sent = CharField(max_length=10, default="pending")`（`pending`/`sent`/`skipped`/`failed`，74-03 回写）。
   - `notified_channels = JSONField(default=list, blank=True)`（实际成功通知的通道列表，74-03 回写）。
   - `created_at = DateTimeField(auto_now_add=True)`。
   - `Meta`：`db_table="alert_events"`，`verbose_name="告警事件"`，`ordering=["-started_at"]`，
     - `indexes=[Index(fields=["status","-started_at"]), Index(fields=["severity","-started_at"]), Index(fields=["-started_at"]), Index(fields=["rule","-started_at"])]`；
     - `constraints=[UniqueConstraint(fields=["rule","target_key"], condition=models.Q(status="firing"), name="uniq_firing_alert_per_rule_target")]`——**去重硬约束**：同规则同对象最多一条 firing（恢复后 status 变 resolved，约束释放，可再触发新 firing）。镜像 ProviderCredential 条件唯一约束范式。
3. **`SettingKeys` 末尾追加 ALERT_* 常量**（仅常量，**不需迁移**，注释风格对齐既有 `LOG_*`/`METRIC_*`）：
   - `ALERT_EVAL_INTERVAL_SECONDS = "alert.eval_interval_seconds"`（int：评估间隔，默认 60；apscheduler 启动值，74-02 注册）。
   - `ALERT_RETENTION_DAYS = "alert.retention_days"`（int：AlertEvent 保留天数，默认 90——告警事件比日志低频，保留略长）。
   - `ALERT_RETENTION_SIZE = "alert.retention_max_rows"`（int：行数上限兜底，默认 500_000）。
   - `ALERT_EMAIL_ENABLED = "alert.email_enabled"`（bool：邮件通道总开关，默认关）。
   - `ALERT_EMAIL_RECIPIENTS = "alert.email_recipients"`（收件人，逗号分隔或 JSON 列表，74-03 解析）。
   - `ALERT_FEISHU_CHAT_ID = "alert.feishu_chat_id"`（系统告警飞书目标群 chat_id）。
   - `ALERT_WEBHOOK_URL = "alert.webhook_url"`（系统告警 webhook 目标 URL）。
4. 生成迁移：`cd server && uv run python manage.py makemigrations system`（自动命名，预期 `0012_systemalertrule_alertevent`，base=0011_gaugesample；实际编号不同则按生成结果重命名 files_modified 引用，per Claude's Discretion）。仅 CreateModel + AddIndex + AddConstraint，无数据迁移。
5. **`server/tests/test_system_alert_models.py`（新建）** `@pytest.mark.django_db`：(a) 建一条 SystemAlertRule（含 channels=["email","feishu"]）→ 字段持久化、默认 enabled=True/cooldown=600；(b) 建一条 AlertEvent(status=firing, target_key="{}") → 再建同 (rule,target_key) firing **抛 IntegrityError**（去重约束生效）；(c) 把首条改 status=resolved 后，同 (rule,target_key) 可再建一条 firing（约束释放）；(d) `SettingKeys.ALERT_EVAL_INTERVAL_SECONDS` 等 7 常量存在且值符合点分命名。
  </action>
  <verify>
    <automated>cd server && uv run python manage.py makemigrations system --check --dry-run && uv run pytest tests/test_system_alert_models.py -x -q</automated>
  </verify>
  <done>SystemAlertRule/AlertEvent 模型 + 0012 迁移就位；去重条件唯一约束 (rule,target_key) status=firing 生效（重复 firing 抛 IntegrityError、resolved 后释放）；SettingKeys.ALERT_* 7 常量就位（无新键迁移）；测试覆盖字段/去重约束/约束释放/常量。</done>
</task>

<task type="auto">
  <name>Task 2: SystemAlertRule CRUD + AlertEvent 查询 API + urls 接入（ALERT-01/02 运维入口）</name>
  <files>server/system/alert_serializers.py, server/system/alert_views.py, server/system/urls_system.py, server/tests/test_system_alert_api.py</files>
  <action>
新增告警规则 CRUD + 事件查询 API（全部 `IsSuperUser` fail-closed，adrf async + `sync_to_async` 桥接，沿用 `log_views`/`serializers` 范式）：

1. **`server/system/alert_serializers.py`（新建）**：
   - `class SystemAlertRuleSerializer(serializers.ModelSerializer)`：读序列化器，`fields` 含全部规则字段 + id + created/updated_at，`read_only_fields=["id","created_at","updated_at"]`。
   - `class SystemAlertRuleWriteSerializer(serializers.ModelSerializer)`（create + partial_update 共用，写字段做白名单防御）：
     - `metric = serializers.ChoiceField(choices=_METRIC_CHOICES)`（模块级 `_METRIC_CHOICES = ["qps","error_rate","ttft","cpu","memory","db_connections","redis_clients","qdrant","queue_depth"]`——与 74-02 评估器 metric 解析表对齐，单一受控集合）。
     - `op = serializers.ChoiceField(choices=["gt","gte","lt","lte"])`，`severity = serializers.ChoiceField(choices=["P0","P1","P2"])`。
     - `value = FloatField()`，`window = IntegerField(min_value=1, default=300)`，`cooldown = IntegerField(min_value=0, default=600)`。
     - `channels`：`validate_channels` 限定子集 ⊆ `{"email","feishu","webhook"}`（非法值抛中文 ValidationError）。
     - `dimension`：`validate_dimension` 限定为 dict 且键 ⊆ 受控集合（`provider`/`credential`/`model`/`source`/`queue`/`route`），值为 str（禁任意嵌套/用户原文，防 label 基数失控，对齐 STATE 关键约束）。
     - `Meta.fields` = 可写字段集合；`partial=True` 时各字段 `required=False`（DRF ModelSerializer 默认处理）。
   - `class AlertEventSerializer(serializers.ModelSerializer)`：只读，`fields` 含全部事件字段（含 rule_info/target/duration_s/email_sent/notified_channels），`read_only_fields = fields`（查询端点不写）。
2. **`server/system/alert_views.py`（新建，adrf APIView + IsSuperUser）**：
   - `class SystemAlertRuleListCreateView(APIView)`：
     - `async def get`：`sync_to_async` 取全部规则（可选 `?enabled=true/false` 过滤）序列化返回 `{"items":[...], "total":n}`；`logger.info("alert_rules_listed", category="caller", component="alerting", ...)`。
     - `async def post`：`SystemAlertRuleWriteSerializer(data=request.data)` → `is_valid(raise_exception=True)` → `sync_to_async(serializer.save)` → 201 返回 `SystemAlertRuleSerializer(instance).data`；`logger.info("alert_rule_created", category="caller", component="alerting", rule_id=..., metric=...)`。
   - `class SystemAlertRuleDetailView(APIView)`：`<int:rule_id>` —
     - `get`：取单条（404 中文 detail）；`patch`：`SystemAlertRuleWriteSerializer(instance, data=request.data, partial=True)` 局部更新；`delete`：删除返回 204。各写操作 `logger.info("alert_rule_updated"/"alert_rule_deleted", category="caller", component="alerting", rule_id=...)`。
   - `class AlertEventListView(APIView)`（镜像 `SystemLogQueryView` 筛选范式）：
     - `async def get`：可选筛选 `severity`(P0/P1/P2) / `status`(firing/resolved) / `rule_id` / `start`/`end`（ISO8601 时间段映射 `started_at__gte/lte`）+ `limit`(默认 100，最大 500)/`offset`；`sync_to_async` 内 count + 分页 `order_by("-started_at")` + 序列化，返回 `{"items":[...], "total":n}`；`logger.info("alert_events_queried", category="sampling", component="alerting", total=..., returned=...)`（查询高频→sampling，不污染 caller 统计）。
   - 抽 `_extract_event_filters(getter)` + `_apply_event_filters(qs, filters)`（镜像 log_views 的 `_extract_filters`/`_apply_filters`，时间界解析复用 `django.utils.dateparse.parse_datetime` + aware 补全）。
3. **`server/system/urls_system.py`**：在 metrics/ 路由附近新增（字面段，运维端点聚集；detail 通配排在 list 之后保持显式顺序惯例）：
   - `path("alerts/rules/", SystemAlertRuleListCreateView.as_view(), name="system-alert-rules")`
   - `path("alerts/rules/<int:rule_id>/", SystemAlertRuleDetailView.as_view(), name="system-alert-rule-detail")`
   - `path("alerts/events/", AlertEventListView.as_view(), name="system-alert-events")`
   - import 自 `.alert_views`。
4. **`server/tests/test_system_alert_api.py`（新建）** `@pytest.mark.django_db(transaction=True)`（async 视图 ORM 写不跨连接泄漏）：(a) 超管 POST `/api/system/alerts/rules/` 合法 body → 201 + DB 有规则；(b) 非超管 → 403（IsSuperUser fail-closed）；(c) POST 非法 metric（如 `metric="foobar"`）/ channels 含 `"sms"` → 400 中文 detail（白名单防御）；(d) PATCH 改 enabled=false → 200 持久化；DELETE → 204 + DB 无该规则；(e) 造若干 AlertEvent（不同 severity/status）→ GET `/api/system/alerts/events/?severity=P0&status=firing` 命中过滤 + 倒序 + total 正确；非超管 events 403。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/test_system_alert_api.py -x -q && uv run ruff check server/system/alert_serializers.py server/system/alert_views.py</automated>
  </verify>
  <done>规则 CRUD（list/create/retrieve/patch/delete，IsSuperUser）+ 事件查询（severity/status/rule_id/时间段筛选 + 分页倒序）API 就位，列对齐 REFERENCE-UI §1.4；metric/op/severity/channels/dimension 白名单防御；测试覆盖 CRUD + 权限 fail-closed + 非法入参 400 + 事件筛选。</done>
</task>

<task type="auto">
  <name>Task 3: alert_retention.purge_alert_events 保留清理（镜像 Phase 71 log_retention）</name>
  <files>server/system/alert_retention.py, server/tests/test_system_alert_api.py</files>
  <action>
镜像 `server/system/log_retention.py` 的"按天数 + 行数上限"清理，专清 `AlertEvent`（中文 docstring，ruff-100，`from __future__ import annotations`；async ORM 用 `acount`/`adelete`/`async for`）。**apscheduler 注册不在本 plan**（在 74-02 与 evaluate_system_alerts 同处注册，避免本 plan 与 74-02 抢 runapscheduler.py）：

1. **`server/system/alert_retention.py`（新建）**：
   - `_DEFAULT_RETENTION_DAYS = 90` / `_DEFAULT_RETENTION_SIZE = 500_000` / `_SIZE_DELETE_BATCH = 50_000`（与 SettingKeys.ALERT_RETENTION_* 默认一致）。
   - `async def _alert_retention_config() -> tuple[int, int]`：读 `SettingKeys.ALERT_RETENTION_DAYS`/`ALERT_RETENTION_SIZE` 经 `system.settings_service.aget_int_setting`，`max(0, ...)` 收口（逐字镜像 `log_retention._retention_config`，仅换 key + 默认）。
   - `async def purge_alert_events() -> dict[str, int]`：先按天数 `cutoff = now - timedelta(days)` 用 `AlertEvent.objects.filter(started_at__lt=cutoff).adelete()`（**按 started_at**——AlertEvent 时间列是 started_at，注释显式标注口径，绝不用 ts/created_at 删错列）；再按行数上限 `acount() > size` 时取 `order_by("started_at").values_list("id", flat=True)[:excess]`（`excess=min(total-size, _SIZE_DELETE_BATCH)`）`async for` 收 id 后 `filter(id__in=ids).adelete()`。整段 `try/except` best-effort（异常 warning `alert_events_purge_failed`，返回已统计部分），绝不反噬。末尾 `logger.info("alert_events_purged", category="caller", component="alert_retention", source="scheduler", by_age=..., by_size=...)`。返回 `{"by_age","by_size"}`。
   > 注：恢复已久的 resolved 事件与极老 firing 一并按 started_at 清理（量级低；如需保留活跃 firing 可在 v2 加 status 过滤，本 phase 不做，避免过度设计）。
2. 扩 `server/tests/test_system_alert_api.py`（或并入，同文件）`@pytest.mark.django_db(transaction=True)`：(a) 造跨保留期 AlertEvent（旧 started_at < cutoff + 新行）→ `purge_alert_events()` 删旧留新、by_age 正确、**不抛 FieldError**（按 started_at）；(b) monkeypatch settings size 调小造超量 → by_size 删最旧超出部分；(c) monkeypatch adelete 抛错 → 返回部分结果不冒泡（best-effort）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/test_system_alert_api.py -x -q && uv run ruff check server/system/alert_retention.py</automated>
  </verify>
  <done>alert_retention.purge_alert_events 按 started_at 天数 + 行数上限清理 AlertEvent（镜像 log_retention，口径分清不删错列），运行时配置 ALERT_RETENTION_DAYS/SIZE，best-effort 绝不反噬；apscheduler 注册留给 74-02；测试覆盖按龄/按量清理 + started_at 口径 + 失败降级。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 超管 → 告警规则 CRUD / 事件查询 API | 运维端点，写阈值规则 + 读告警事件 |
| serializer 写入 → SystemAlertRule.metric/channels/dimension | 受控枚举入库，禁任意字符串/用户原文污染维度 |
| 清理任务 → AlertEvent | 系统周期删行，须 best-effort 不反噬 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-01-01 | Elevation of Privilege | 非超管增删改告警规则 / 读告警事件 | mitigate | 全部 alert_views `IsSuperUser` fail-closed（沿用 observability_views 惯例）；测试断言非超管 403 |
| T-74-01-02 | Tampering | 任意 metric/op/severity/channels/dimension 注入污染评估与 label 基数 | mitigate | WriteSerializer ChoiceField + validate_channels/validate_dimension 白名单（metric/op/severity 闭集、channels⊆{email,feishu,webhook}、dimension 键⊆受控集合值为 str），非法 400 |
| T-74-01-03 | Denial of Service | 重复 firing 刷屏（告警风暴） | mitigate | (rule,target_key) status=firing 条件唯一约束 DB 兜底 + 74-02 服务层去重收口；一条 firing，恢复收尾 |
| T-74-01-04 | Information Disclosure | rule_info/title_zh/target 泄漏凭证或用户输入原文 | mitigate | 模型仅存元数据（metric/阈值/当前值/受控维度）；写入侧（74-02/03）经 redact_secrets_in_text；dimension 受控键禁用户原文 |
| T-74-01-05 | Denial of Service | 行数清理一次性 IN 列表过大 | mitigate | `_SIZE_DELETE_BATCH=50_000` 单次上限（镜像 log_retention），分批删最旧超出部分 |
| T-74-01-SC | Tampering | npm/pip/cargo 安装 | accept | 不新增第三方依赖（Django ORM/DRF/adrf/标准库 datetime 均已在依赖） |

## Redaction 注记
本 plan 只建模型 + CRUD/查询 + 清理；模型字段仅承载元数据（阈值/当前值/受控维度/通道名），**绝不**落 raw payload/凭证。AlertEvent 内容由 74-02 评估器写入（届时经脱敏），74-03 通知正文经 `redact_secrets_in_text`。`server/tests/test_credential_leak_protection.py` 顶层守护保持绿。
</threat_model>

<verification>
- `uv run pytest tests/test_system_alert_models.py tests/test_system_alert_api.py -q` 全绿。
- `uv run python manage.py makemigrations --check --dry-run` 干净（0012 已生成提交，SettingKeys 仅常量无新键迁移）。
- `uv run ruff check server/system/models.py server/system/alert_serializers.py server/system/alert_views.py server/system/alert_retention.py server/system/urls_system.py` 干净（line-length 100）。
- 人工/集成：超管 POST 建规则 → GET 列表可见；造 AlertEvent → GET events 按 severity/status 过滤；purge_alert_events 按保留策略清理。
</verification>

<success_criteria>
- ALERT-01（模型/CRUD）：SystemAlertRule 落 system app（独立 workflow AlertRule），metric/op/value/window/dimension/severity/enabled/channels/cooldown/title_template 全字段就位，REST CRUD 运行时可改（IsSuperUser）。
- ALERT-02（事件模型/查询）：AlertEvent 含 severity/title_zh/rule_info/target/started_at/ended_at/duration_s/status/email_sent/notified_channels，列对齐 REFERENCE-UI §1.4；查询 API 按 severity/status/rule_id/时间段筛选倒序；去重 (rule,target_key) status=firing 条件唯一约束 DB 兜底。
- SettingKeys.ALERT_* 7 常量就位供 74-02/03 消费；alert_retention 复用 Phase 71 log_retention 按 started_at 清理（apscheduler 注册留 74-02）。
- 零回归：白名单防御 + IsSuperUser fail-closed + best-effort 清理绝不反噬；模型仅元数据不落凭证。
</success_criteria>

<output>
Create `.planning/phases/74-alerting/74-01-SUMMARY.md` when done.
</output>
