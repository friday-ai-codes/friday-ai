---
phase: 73-snapshot-query
plan: "73-03"
type: execute
wave: 2
depends_on: ["73-01"]
files_modified:
  - server/system/models.py
  - server/system/metric_sampling.py
  - server/system/metric_retention.py
  - server/agents/management/commands/runapscheduler.py
  - server/tests/test_metric_sampling.py
  - server/tests/test_metric_retention.py
autonomous: true
requirements: [RATE-03]
user_setup: []

must_haves:
  truths:
    - "apscheduler 周期任务（IntervalTrigger ~45s）调 snapshot_service.collect_concurrency_snapshot() 把并发/队列深/积压拍平成多行 GaugeSample 落库（RATE-03 采样侧）"
    - "采样任务 best-effort：采集/落库任一失败只 warning，绝不抛、绝不打断 scheduler 主循环（沿用观测代码绝不反噬业务）"
    - "保留清理复用 Phase 71 log_retention 同款机制——按天数 + 行数上限清理 GaugeSample(ts)/RequestMetric(ts)/ModelUsageRecord(created_at)，daily apscheduler 触发"
    - "ModelUsageRecord 清理按 created_at（非 ts），与 RequestMetric/GaugeSample 的 ts 口径分清，绝不删错列"
    - "采样间隔 + 指标保留天数/行数上限走 settings_service 运行时可配（SettingKeys.METRIC_*），失败回默认；采样任务 user_id=system/source=scheduler（CTX-02）"
    - "MetricDailyRollup 本 Phase 仅留占位说明（量级低，原始行 + percentile_cont 足够），不建模型/不建迁移"
  artifacts:
    - path: "server/system/metric_sampling.py"
      provides: "sample_gauges()——拍平 collect_concurrency_snapshot（+ host 后台任务积压）为受控 name 的 GaugeSample 行 bulk_create，best-effort"
      contains: "async def sample_gauges"
    - path: "server/system/metric_retention.py"
      provides: "purge_gauge_samples/purge_request_metrics/purge_model_usage_records——镜像 log_retention 的按天数+行数清理，ModelUsageRecord 用 created_at"
      contains: "async def purge_gauge_samples"
    - path: "server/agents/management/commands/runapscheduler.py"
      provides: "sample_gauges_job（~45s IntervalTrigger）+ purge_metrics_job（daily CronTrigger）注册，沿用 _with_scheduler_log_context + run_async_task 范式"
      contains: "def sample_gauges_job"
  key_links:
    - from: "server/system/metric_sampling.py"
      to: "system.snapshot_service.collect_concurrency_snapshot"
      via: "sample_gauges 调 73-01 并发采集器拍平成 GaugeSample"
      pattern: "collect_concurrency_snapshot"
    - from: "server/agents/management/commands/runapscheduler.py"
      to: "system.metric_sampling.sample_gauges"
      via: "sample_gauges_job 经 run_async_task 周期调用（IntervalTrigger ~45s）"
      pattern: "sample_gauges"
    - from: "server/agents/management/commands/runapscheduler.py"
      to: "system.metric_retention"
      via: "purge_metrics_job 经 run_async_task daily 调三表清理（镜像 purge_observability_logs_job）"
      pattern: "purge_gauge_samples|purge_request_metrics|purge_model_usage_records"
---

<objective>
补齐趋势采样侧 + 指标表保留清理（RATE-03 采样侧 + 保留治理）：新增 `system/metric_sampling.py`（apscheduler 周期把 73-01 并发/队列/积压快照拍平成 `GaugeSample` 行）+ `system/metric_retention.py`（镜像 Phase 71 `log_retention` 清理 `GaugeSample`/`RequestMetric`/`ModelUsageRecord`），并在 `runapscheduler.py` 注册采样（~45s）与清理（daily）两个周期任务。

Purpose: 兑现"有没有东西在堆积"的趋势记录（RATE-03 只记不告警）——并发/队列深/积压每 ~45s 采一帧落 `GaugeSample`，由 73-02 `gauge:<name>` 查询出趋势；吞吐/错误趋势继续由 Phase 72 事件表 SQL 聚合（**不重复采样**）。同时把指标原始行的无限增长用 Phase 71 同款保留机制收口（§A.4「原始行无限增长 → 每日定时清理 + 保留策略」）。`ModelUsageRecord` 时间列是 `created_at`（非 `ts`），清理口径单独标注绝不删错列。MetricDailyRollup 本 Phase 仅留占位（量级低，原始行 + `percentile_cont` 已足够）。
Output: `metric_sampling.py`（sample_gauges 受控 name 拍平 + bulk_create）、`metric_retention.py`（三表按天数+行数清理）、`SettingKeys.METRIC_*` 运行时配置、`runapscheduler.py` 两个 job 注册、守护测试。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/73-snapshot-query/73-CONTEXT.md
@.planning/observability/MILESTONE-PROPOSAL.md
@.cursor/rules/observability-logging.mdc

# 关键现有符号（执行时按需精读，勿全文重读）
@server/system/snapshot_service.py     # 73-01 新建：collect_concurrency_snapshot（provider_slots/durable_queues/runner/rag）+ collect_host_snapshot（background_tasks 积压）—— 采样源
@server/system/models.py               # 73-01 新建 GaugeSample(ts/name/value/labels) + SettingKeys（本 plan 加 METRIC_RETENTION_DAYS/SIZE + METRIC_SAMPLE_INTERVAL_SECONDS）
@server/system/log_retention.py        # Phase 71：_retention_config + purge_system_logs/purge_webhook_events 按天数+行数清理范式（本 plan 逐源镜像，ModelUsageRecord 改 created_at）
@server/system/settings_service.py     # aget_int_setting/get_int_setting（运行时配置 + 60s 缓存）
@server/interactions/models.py         # ModelUsageRecord（时间列 created_at！清理按 created_at，绝不用 ts）
@server/agents/management/commands/runapscheduler.py  # _with_scheduler_log_context（system/scheduler 上下文）+ run_async_task + purge_observability_logs_job（daily 04:30）+ add_job 范式
</context>

<tasks>

<task type="auto">
  <name>Task 1: SettingKeys.METRIC_* + metric_sampling.py（sample_gauges 拍平 GaugeSample）</name>
  <files>server/system/models.py, server/system/metric_sampling.py, server/tests/test_metric_sampling.py</files>
  <action>
新增运行时配置键 + 周期采样服务（中文 docstring，ruff line-length 100，`from __future__ import annotations`）：

1. **`server/system/models.py` `SettingKeys` 末尾追加三常量**（仅常量，**不需迁移**，与既有 `LOG_RETENTION_DAYS` 注释风格一致）：
   - `METRIC_SAMPLE_INTERVAL_SECONDS = "metric.sample_interval_seconds"`（int：GaugeSample 采样间隔，默认 45，范围约束在 sampling 服务侧 clamp 30..300）。
   - `METRIC_RETENTION_DAYS = "metric.retention_days"`（int：指标表保留天数，默认 30）。
   - `METRIC_RETENTION_SIZE = "metric.retention_max_rows"`（int：单表行数上限兜底，默认 2_000_000——指标比日志高频，上限略放宽）。
2. **`server/system/metric_sampling.py`（新建）—— `async def sample_gauges() -> dict[str, int]`**：调 73-01 `from system.snapshot_service import collect_concurrency_snapshot, collect_host_snapshot`，把并发/队列/积压**拍平成多行受控 name 的 GaugeSample**，`bulk_create` 一次落库。整函数 `try/except` 兜底（异常 warning `gauge_sample_failed`，返回 `{"written": 0}`，绝不抛、绝不反噬）。
   - **受控 name 常量**（模块级 frozenset/枚举，**禁用户输入原文**，与 73-02 `_validate_gauge_name` 受控前缀 `concurrency.`/`queue.`/`backlog.` 对齐）：
     - provider 槽位：每个凭证一行 `name="concurrency.provider_slots"`，`value=in_use`，`labels={"credential": str(credential_id), "provider": provider}`（仅 UUID/provider 枚举，绝不落密钥）。
     - durable 队列：`name="queue.durable_todo"` / `name="queue.durable_doing"`，`value=条数`，`labels={"queue": queue_name}`（按 procrastinate 队列名分行）。
     - runner：`name="queue.runner_pending"`（待派发）/ `name="queue.runner_local"`（本地在跑汇总），`value=条数`。
     - RAG 并发：`name="concurrency.rag"`，`value=in_use`（采集不到记 0 或跳过，不臆造）。
     - 后台积压：`name="backlog.subagent_active"` / `name="backlog.background_tasks"`，取 `collect_host_snapshot()["background_tasks"]` 口径（subagent active / durable todo+doing / orchestration active 等已聚合值），逐项一行。
   - 统一 `ts = timezone.now()`（**所有行同一时刻**，便于按帧对齐）；`GaugeSample(ts=ts, name=..., value=float(...), labels={...})`；`await GaugeSample.objects.abulk_create(rows)`。
   - 源 envelope `available=false` 的块跳过（不落 0 噪声行；provider_slots 为空列表则不落 provider 行）。返回 `{"written": len(rows)}`。
   - `logger.info("gauge_sampled", category="sampling", component="metric_sampling", source="scheduler", written=..., names=...)`（**category=sampling**——高频内部步骤，per observability 规则；事件名 snake_case）。
3. **`server/tests/test_metric_sampling.py`（新建）** `@pytest.mark.django_db(transaction=True)`（async 写不跨连接泄漏，per 73-CONTEXT）：(a) monkeypatch `collect_concurrency_snapshot` 返回含 provider_slots（1 凭证 in_use=2）+ durable_queues + runner，`collect_host_snapshot` 返回 background_tasks → `await sample_gauges()` 后 DB 有对应受控 name 的 GaugeSample 行、value 正确、labels 仅含受控键（credential/provider/queue，无密钥）；(b) 某源 available=false → 该源不落行（不产 0 噪声）；(c) monkeypatch collect_concurrency_snapshot 抛错 → sample_gauges 返回 written=0 不冒泡（best-effort）。事件名 snake_case。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/test_metric_sampling.py -x -q && uv run python manage.py makemigrations system --check --dry-run</automated>
  </verify>
  <done>SettingKeys.METRIC_* 三常量就位（无新迁移）；sample_gauges 调 73-01 并发/积压快照拍平成受控 name 的 GaugeSample bulk_create，best-effort 不抛、available=false 源跳过、labels 仅受控键；测试覆盖落行/跳空源/失败降级。</done>
</task>

<task type="auto">
  <name>Task 2: metric_retention.py（GaugeSample/RequestMetric/ModelUsageRecord 保留清理）</name>
  <files>server/system/metric_retention.py, server/tests/test_metric_retention.py</files>
  <action>
镜像 Phase 71 `system/log_retention.py` 的"按天数 + 行数上限"清理，扩到三张指标表（中文 docstring，ruff-100，`from __future__ import annotations`；async ORM 用 `acount`/`adelete`/`async for`）：

1. **`async def _metric_retention_config() -> tuple[int, int]`**：读 `SettingKeys.METRIC_RETENTION_DAYS`（默认 30）+ `METRIC_RETENTION_SIZE`（默认 2_000_000）经 `system.settings_service.aget_int_setting`，`max(0, ...)` 收口（逐字镜像 `log_retention._retention_config`，仅换 key + 默认）。
2. **`async def _purge_table(model, time_field: str) -> dict[str, int]`**（抽公共清理，避免三份复制）：先按天数 `cutoff = now - timedelta(days)` 用 `filter(**{f"{time_field}__lt": cutoff}).adelete()`（`time_field` 是**白名单字面量**由各 wrapper 传入，非用户输入）；再按行数上限——`acount()` > size 时取 `order_by(time_field).values_list("id", flat=True)[:excess]`（`excess=min(total-size, _SIZE_DELETE_BATCH=50_000)`）`async for` 收 id 后 `filter(id__in=ids).adelete()`。整段 `try/except` best-effort（异常 warning `<table>_purge_failed`，返回已统计部分），绝不反噬。返回 `{"by_age","by_size"}`。
3. **三个 wrapper（time_field 口径分清——关键，绝不删错列）**：
   - `async def purge_gauge_samples()`：`from system.models import GaugeSample`，`_purge_table(GaugeSample, "ts")`。
   - `async def purge_request_metrics()`：`from system.models import RequestMetric`，`_purge_table(RequestMetric, "ts")`。
   - `async def purge_model_usage_records()`：`from interactions.models import ModelUsageRecord`，`_purge_table(ModelUsageRecord, "created_at")`（**created_at 非 ts**——ModelUsageRecord 时间列是 `created_at`，按 ts 会 FieldError；注释显式标注此口径差异）。
   - 各 wrapper 末尾 `logger.info("<table>_purged", category="caller", component="metric_retention", source="scheduler", by_age=..., by_size=...)`。
   > MetricDailyRollup 占位说明：本 Phase **不建 rollup 模型/迁移**——量级低，原始行 + 73-02 `percentile_cont` 已足够覆盖长区间趋势（per 73-CONTEXT「倾向只落保留清理 + 留 rollup 占位」/ MILESTONE-PROPOSAL §C「可选/按需」）。在模块 docstring 注明 rollup 为后续 v2 可选项，避免引入未消费的空表。
4. **`server/tests/test_metric_retention.py`（新建）** `@pytest.mark.django_db(transaction=True)`：(a) 造跨保留期的 GaugeSample（旧行 ts < cutoff + 新行）→ `purge_gauge_samples()` 删旧留新、by_age 正确；(b) 造超过 size 上限的 RequestMetric（monkeypatch settings size 调小）→ by_size 删最旧超出部分；(c) 造 ModelUsageRecord（含 created_at 旧/新）→ `purge_model_usage_records()` 按 created_at 删旧（断言**不**抛 FieldError、删对行）；(d) monkeypatch adelete 抛错 → wrapper 返回部分结果不冒泡（best-effort）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/test_metric_retention.py -x -q && uv run ruff check server/system/metric_retention.py server/system/metric_sampling.py</automated>
  </verify>
  <done>metric_retention 三表按天数+行数清理（镜像 log_retention）：GaugeSample/RequestMetric 按 ts、ModelUsageRecord 按 created_at（口径分清不删错列）；运行时配置 METRIC_RETENTION_DAYS/SIZE；best-effort 不反噬；MetricDailyRollup 留占位不建表；测试覆盖三表按龄/按量清理 + created_at 口径 + 失败降级。</done>
</task>

<task type="auto">
  <name>Task 3: runapscheduler 注册采样(~45s) + 清理(daily) 两个周期任务（RATE-03）</name>
  <files>server/agents/management/commands/runapscheduler.py, server/tests/test_metric_sampling.py</files>
  <action>
把采样与清理接入既有 apscheduler 长驻进程（沿用 `_with_scheduler_log_context` + `run_async_task` + `add_job(..., max_instances=1, replace_existing=True)` 范式，**不动**既有 job/lock/单实例契约）：

1. **`sample_gauges_job()`**（模块级函数，`@_with_scheduler_log_context` 装饰——system/scheduler 上下文 + CTX-02）：`from system.metric_sampling import sample_gauges`；`log = logger.bind(job="sample_gauges")`；`run_async_task(sample_gauges)`；异常 `log.exception("job_error", ...)`（不抛，与既有 `*_job` 一致）。**采样高频**：job 体 `log.info("job_start")`/`job_complete` 保持但 sample_gauges 内部用 category=sampling，避免 INFO 刷屏（高频循环纪律）。
2. **`purge_metrics_job()`**（`@_with_scheduler_log_context`，镜像 `purge_observability_logs_job`）：`from system.metric_retention import purge_gauge_samples, purge_request_metrics, purge_model_usage_records`；内层 `async def _run()` 顺序 `await` 三表清理并汇总 `{"gauge":..., "request":..., "model_usage":...}`；`run_async_task(_run)`；异常 `log.exception`。
3. **在 `Command.handle()` 注册两个 job**（紧邻既有 `purge_observability_logs` 注册块，保持运维任务聚集）：
   - 采样：`scheduler.add_job(sample_gauges_job, trigger=IntervalTrigger(seconds=getattr(settings, "METRIC_SAMPLE_INTERVAL_SECONDS", 45)), id="sample_gauges", name="Sample concurrency/queue/backlog into GaugeSample", max_instances=1, replace_existing=True)` + `logger.info("job_registered", job="sample_gauges", schedule="every ~45s")`。（间隔默认 45s——settings 兜底；运行时改采样间隔走 SettingKeys.METRIC_SAMPLE_INTERVAL_SECONDS 由 sample_gauges 内部读取/clamp，apscheduler 间隔以 settings 启动值为准，注释标注热改间隔需重启 scheduler——可接受，量级低。）
   - 清理：`scheduler.add_job(purge_metrics_job, trigger=CronTrigger(hour=5, minute=0), id="purge_metrics", name="Purge GaugeSample/RequestMetric/ModelUsageRecord by retention policy", max_instances=1, replace_existing=True)` + `logger.info("job_registered", ...)`。（选 05:00 错开既有 03:00/03:30/04:00/04:30 清理避免争 SQLite 写锁——与既有 `prune_cache_volumes` 05:00 同时段；若担心同时段争锁可选 05:30，Claude's Discretion 在执行时定。）
4. 扩 `tests/test_metric_sampling.py`：(a) 直接调 `sample_gauges_job()`（monkeypatch sample_gauges 为可观测的 stub）→ 不抛、stub 被调用一次（验证 job wrapper 接线 + run_async_task 路径）；(b) monkeypatch sample_gauges 抛错 → `sample_gauges_job()` 吞异常不冒泡（scheduler 主循环不被打断）；(c) `purge_metrics_job()` 同款接线 smoke（monkeypatch 三清理为 stub，断言各调一次、异常不冒泡）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/test_metric_sampling.py tests/test_metric_retention.py -x -q && uv run ruff check server/agents/management/commands/runapscheduler.py</automated>
  </verify>
  <done>runapscheduler 注册 sample_gauges（IntervalTrigger ~45s，category=sampling 不刷屏）+ purge_metrics（daily Cron 错峰）两个 job，沿用 _with_scheduler_log_context（system/scheduler）+ run_async_task 范式，不动既有 job/单实例 lock 契约；job wrapper 异常不打断 scheduler；测试覆盖两 job 接线 + 失败不冒泡。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scheduler 线程 → snapshot_service / 指标表 | 系统周期任务（无触发用户）采集并发快照写 GaugeSample、清理三表，须 best-effort 不反噬 |
| 采样/清理写入 → GaugeSample.labels / 指标行 | 受控 name/labels 入库，禁用户输入原文与凭证 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-03-01 | Denial of Service | 采样任务异常拖垮 scheduler 主循环 | mitigate | sample_gauges 整函数 try/except 返回 written=0；job wrapper `log.exception` 吞异常不上抛（与既有 *_job 一致）；不抢既有清理任务时段（错峰 Cron） |
| T-73-03-02 | Information Disclosure | GaugeSample labels 泄漏凭证/用户输入原文 | mitigate | name/labels 受控枚举常量（concurrency./queue./backlog. 前缀 + credential UUID/provider/queue 枚举），绝不落密钥/用户输入；与 73-02 `_validate_gauge_name` 受控前缀对齐 |
| T-73-03-03 | Tampering | ModelUsageRecord 按错误时间列(ts)清理删错/报错 | mitigate | time_field 白名单字面量（GaugeSample/RequestMetric=ts，ModelUsageRecord=created_at），注释显式标注口径差异；测试断言不抛 FieldError 且删对行 |
| T-73-03-04 | Denial of Service | 行数清理一次性 IN 列表过大 | mitigate | `_SIZE_DELETE_BATCH=50_000` 单次上限（镜像 log_retention），分批删最旧超出部分 |
| T-73-03-05 | Repudiation | 系统周期任务无法归因 | mitigate | `_with_scheduler_log_context` 绑定 user_id=system/source=scheduler（CTX-02）；采样 category=sampling、清理 category=caller，事件可归因系统调度 |
| T-73-03-SC | Tampering | npm/pip/cargo 安装 | accept | 不新增第三方依赖（apscheduler/Django ORM/标准库 datetime 均已在依赖） |

## Redaction 注记
采样只写**状态计数**（槽位占用/队列条数/积压数）到 GaugeSample，受控 name/labels（provider/credential UUID/queue 枚举），不含明文凭证；清理只 `adelete` 行不读 payload。`ModelUsageRecord` 既有写入侧（72-02）已脱敏，本 plan 仅按时间清理不触内容。`server/tests/test_credential_leak_protection.py` 顶层守护保持绿。
</threat_model>

<verification>
- `uv run pytest tests/test_metric_sampling.py tests/test_metric_retention.py -q` 全绿。
- `uv run python manage.py makemigrations --check --dry-run` 干净（本 plan 仅加 SettingKeys 常量 + 新模块，不新增模型/迁移）。
- `uv run ruff check server/system/metric_sampling.py server/system/metric_retention.py server/agents/management/commands/runapscheduler.py server/system/models.py` 干净（line-length 100）。
- 人工/集成：起 `runapscheduler` 后 `sample_gauges` ~45s 写入若干 GaugeSample 行（可经 73-02 `gauge:concurrency.provider_slots` 查到趋势）；`purge_metrics` daily 按保留策略清理三表（ModelUsageRecord 按 created_at）。
</verification>

<success_criteria>
- RATE-03（采样侧）：apscheduler IntervalTrigger ~45s 调 collect_concurrency_snapshot（+ host 积压）把并发/队列深/积压拍平成受控 name 的 GaugeSample 行落库；吞吐/错误趋势仍由 Phase 72 事件表 SQL 聚合（不重复采样）。
- 保留治理：GaugeSample/RequestMetric（按 ts）+ ModelUsageRecord（按 created_at）按天数+行数上限 daily 清理，复用 Phase 71 log_retention 同款机制，运行时可配（SettingKeys.METRIC_*）。
- MetricDailyRollup 本 Phase 留占位（不建模型/迁移，原始行 + percentile_cont 足够）。
- 零回归：采样/清理全 best-effort，job wrapper 吞异常绝不打断 scheduler；不动既有 job/单实例 lock 契约；系统任务归因 system/scheduler（CTX-02）；GaugeSample labels 受控无凭证泄漏。
</success_criteria>

<output>
Create `.planning/phases/73-snapshot-query/73-03-SUMMARY.md` when done.
</output>
