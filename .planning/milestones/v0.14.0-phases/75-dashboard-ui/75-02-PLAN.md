---
phase: 75-dashboard-ui
plan: "75-02"
type: execute
wave: 2
depends_on: ["75-01"]
files_modified:
  - web/src/pages/admin/observability/index.vue
  - web/src/components/observability/HealthScoreGauge.vue
  - web/src/components/observability/RealtimeRateCard.vue
  - web/src/components/observability/MetricInfoCard.vue
  - web/src/components/observability/SnapshotRow.vue
  - web/src/components/observability/TrendCharts.vue
  - web/src/components/observability/__tests__/dashboard.spec.ts
autonomous: true
requirements: [UI-01, UI-02]
user_setup: []

must_haves:
  truths:
    - "大盘总览 index.vue 重构为后端时序驱动 + 保留自动刷新：顶部 ObservabilityTabs + ObservabilityTimeRange（5m/1h/24h/自定义 + 自动刷新），消费 getMetricsSnapshot + queryMetrics（不再仅靠客户端滚动采样）"
    - "复合健康分圆环（HealthScoreGauge）0–100：由 CPU/内存/错误率/上游错误/队列积压加权算分 + 健康(≥80 绿)/警告(60–79 琥珀)/严重(<60 红)徽标；输入取 snapshot + 近窗 error/上游指标"
    - "实时速率卡（RealtimeRateCard）：1min/5min/30min/1h 窗口 tab + 当前/峰值/平均 QPS·TPS 三联 + sparkline；消费 queryMetrics(metric qps|tps, step, window)"
    - "6 信息卡（MetricInfoCard 通用）：请求汇总 / SLA(排除业务限制大字+异常数+副注) / 请求错误(错误率+系统·业务限制分列) / 请求时长(P99 大字+P95/P90/P50/Avg/Max) / TTFT(同) / 上游错误(错误率+错误数排除429/529+429·529 单列)，分位 tabular-nums"
    - "快照行（SnapshotRow）CPU/内存/DB(连接·活跃·空闲/max)/Redis(连接 x/maxclients·命中率)/Qdrant(可用·collection 数)/协程/后台：卡内内联阈值超阈变色(绿→琥珀→红) + 源 available=false 显示 n/a 灰态降级不报错；消费 getMetricsSnapshot"
    - "趋势区（TrendCharts）：吞吐(各 provider QPS+TPS，可切 call_source via AnalyticsGroupingSelector)/错误(系统·上游·业务限制三线)/请求时长分布(DurationDistribution)/并发·排队(gauge:concurrency.*·queue.*)；空态+加载骨架 + degraded 标记提示"
  artifacts:
    - path: "web/src/pages/admin/observability/index.vue"
      provides: "大盘总览页（UI-01+UI-02），definePage requiresAdmin，组合健康分/速率/6卡/快照/趋势 + tabs + 时间范围 + 自动刷新"
      contains: "requiresAdmin"
    - path: "web/src/components/observability/HealthScoreGauge.vue"
      provides: "复合健康分圆环（echarts gauge 或 ui/progress 圆环）+ 加权算分 + 状态徽标"
      contains: "HealthScoreGauge"
    - path: "web/src/components/observability/RealtimeRateCard.vue"
      provides: "实时速率卡（窗口 tab + 当前/峰值/平均 QPS·TPS + sparkline）"
      contains: "RealtimeRateCard"
    - path: "web/src/components/observability/MetricInfoCard.vue"
      provides: "通用信息卡（大字主值 + 分位/分列副行 + 副注），6 卡复用"
      contains: "MetricInfoCard"
    - path: "web/src/components/observability/SnapshotRow.vue"
      provides: "快照行内联阈值卡（超阈变色 + n/a 降级）"
      contains: "SnapshotRow"
    - path: "web/src/components/observability/TrendCharts.vue"
      provides: "4 类趋势图（吞吐/错误/时长分布/并发排队）+ 空态骨架 + call_source 切换"
      contains: "queryMetrics"
  key_links:
    - from: "web/src/pages/admin/observability/index.vue"
      to: "web/src/api/system.ts"
      via: "getMetricsSnapshot + queryMetrics 拉数"
      pattern: "getMetricsSnapshot|queryMetrics"
    - from: "web/src/pages/admin/observability/index.vue"
      to: "web/src/components/observability/ObservabilityTabs.vue"
      via: "顶部三视图导航 + 时间范围（75-01 产出）"
      pattern: "ObservabilityTabs"
    - from: "web/src/components/observability/TrendCharts.vue"
      to: "web/src/components/analytics/echarts-setup"
      via: "VChart 出图 + chart-theme 主题"
      pattern: "echarts-setup|chart-theme"
---

<objective>
实现运维大盘总览页（UI-01 上半区 + UI-02 快照·趋势），重构 `web/src/pages/admin/observability/index.vue` 为"后端时序驱动 + 保留自动刷新"：复合健康分圆环、实时速率卡、6 张信息卡、内联阈值快照行、4 类趋势图，顶部统一三视图 tab 导航 + 5m/1h/24h/自定义时间范围 + 自动刷新开关。全部消费 75-01 封装的 `getMetricsSnapshot` / `queryMetrics`，复用 `analytics/` 与 `ui/` 既有组件，实现到 75-UI-SPEC §2–§3 契约。

Purpose: 这是大盘的首屏与核心价值——一眼看整体健康、实时速率与关键口径（SLA 排除业务限制、请求时长/TTFT 分位、上游 429/529 单列），并把当前快照与时序趋势统一呈现。沿用 Friday 既有设计系统保证全站一致与暗色适配。
Output: 重构后的 index.vue + `components/observability/` 下 5 个新组件（HealthScoreGauge/RealtimeRateCard/MetricInfoCard/SnapshotRow/TrendCharts）+ 组件单测。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/75-dashboard-ui/75-CONTEXT.md
@.planning/phases/75-dashboard-ui/75-UI-SPEC.md
@.planning/observability/REFERENCE-UI.md

# 75-01 产出（本 plan 直接消费，执行时精读其导出签名）
@web/src/api/system.ts                 # getMetricsSnapshot/queryMetrics/querySla + MetricsSnapshot/MetricPoint/SlaPoint/MetricsQueryResult 类型
@web/src/components/observability/ObservabilityTabs.vue       # 顶部三视图导航
@web/src/components/observability/ObservabilityTimeRange.vue  # 5m/1h/24h/自定义 + 自动刷新开关 + slot
@web/src/components/observability/format.ts                   # formatNumber/formatRatio/formatDurationMs/formatThousands/formatClock
@web/src/components/observability/status.ts                   # healthBandClass/healthScoreBand/logLevelClass

# 复用组件 / 范式（执行时按需精读）
@web/src/pages/admin/observability/index.vue   # 既有实时采样大盘——重构基底（保留 4s 自动刷新范式、card/lucide/tabular class、VChart baseOption/lineSeries、health/status 色函数可迁到 status.ts 不重复）
@web/src/components/analytics/ChartCard.vue             # 图表卡外壳（title/icon/actions slot）
@web/src/components/analytics/DurationDistribution.vue  # 请求时长分布图（趋势区复用）
@web/src/components/analytics/AnalyticsGroupingSelector.vue # 分组维度切换器范式（吞吐图切 call_source 借鉴，按需自建 provider/call_source 维度切换）
@web/src/components/analytics/echarts-setup.ts          # VChart 注册
@web/src/components/analytics/chart-theme.ts            # tooltip/axis/legend/grid 主题（亮暗）
@web/src/components/ui/tabs/index.ts                    # 速率卡窗口 tab
@web/src/components/ui/card/index.ts                    # 卡片外壳
@web/src/components/ui/skeleton/index.ts               # 加载骨架
@web/src/components/ui/tooltip/index.ts                # 阈值/副注 tooltip
@web/src/components/ui/progress/index.ts               # 圆环/进度（健康分备选）
</context>

<tasks>

<task type="auto">
  <name>Task 1: UI-01 上半区组件 —— HealthScoreGauge / RealtimeRateCard / MetricInfoCard（6 卡）</name>
  <files>web/src/components/observability/HealthScoreGauge.vue, web/src/components/observability/RealtimeRateCard.vue, web/src/components/observability/MetricInfoCard.vue</files>
  <action>
新建 UI-01 上半区三类组件（Vue 3 `<script setup lang="ts">`，中文文案，lucide 图标无 emoji，分位/计数用 `tabular-nums`，亮暗 token，hover 150–300ms，focus-visible，skeleton 加载，空态友好；实现到 UI-SPEC §2）：

1. **`HealthScoreGauge.vue`**（UI-SPEC §2.1）：
   - props：`snapshot: MetricsSnapshot | null`、`errorRate?: number`、`upstreamErrorRate?: number`（近窗错误/上游率，0..1，由父页从 queryMetrics 派生）、`loading?: boolean`。
   - 加权算分（0–100，**Claude's Discretion 公式**，注释列权重，best-effort 缺项跳过并归一化）：取 `host.cpu_percent`、`host.mem_percent`、`errorRate`、`upstreamErrorRate`、队列积压（`concurrency.durable_queues` todo+doing 相对阈值或 provider 槽位占用率）五因子，各转 0..1 健康度（越低越健康的指标取 `1 - clamp(x)`），加权求和 ×100。源缺失（available=false / undefined）该因子不计并重新归一化分母。
   - 圆环用 echarts gauge（VChart + chart-theme）或 `ui/progress` 圆环（二选一，注释说明；推荐 echarts gauge 以呈现刻度），中心大字分数 + `healthScoreBand(score)` 徽标（健康/警告/严重 + 色）。
   - 副行可选：各因子贡献小条（UI-SPEC §2.1 "可选"，本期做精简文字列表：`CPU 99% · 错误率 2% ...`）。
   - loading→skeleton；snapshot=null→灰态 n/a。reduced-motion 时禁圆环动画（gauge animation:false 或 media 查询）。
2. **`RealtimeRateCard.vue`**（UI-SPEC §2.2）：
   - props 无（自取数）或 `timeRange`（由父传 start/end）——本组件**自管窗口 tab**（1min/5min/30min/1h，`ui/tabs`），与页面时间范围解耦（速率卡是"实时滚动窗口"语义）。
   - 选 tab → 计算 start=now-窗口、step（1min→5s、5min→15s、30min→1m、1h→2m 量级，注释），并 `queryMetrics({metric:'qps', start, end:now, step})` 与 `{metric:'tps', ...}`（用 @tanstack/vue-query，keepPreviousData，按 tab refetch；自动刷新由窗口短 + refetchInterval 实现，注释 4s/对齐窗口）。
   - 从 series（按 bucket 聚合各 dim 之和）派生：当前（最后一桶值/step→速率）、峰值（max）、平均（mean）；QPS 与 TPS 两组三联。QPS=每秒请求（count/step）；TPS=每秒 token（sum/step）。
   - sparkline：echarts line 无轴（复用 index.vue lineSeries 范式或精简 option），亮暗主题。tabular-nums。
3. **`MetricInfoCard.vue`**（UI-SPEC §2.3 通用信息卡，6 卡复用，**纯展示**，数据由父页传入）：
   - props：`title: string`、`icon: string`、`mainValue: string`、`mainLabel?: string`（大字下副标，如 "P99"）、`subItems?: { label: string, value: string, class?: string }[]`（分位/分列副行）、`footnote?: string`（副注，如 SLA "排除业务限制（含系统繁忙限流）"）、`tone?: 'default'|'success'|'warning'|'danger'`（大字色）、`loading?: boolean`。
   - 布局：图标芯片 + 标题；大字 `text-2xl/3xl font-bold tabular-nums`（tone 着色）；subItems 网格（label 小字 muted + value tabular）；footnote 极小 muted；loading→skeleton。a11y：tone 不作唯一信号，配文字。
   - 该组件被 6 卡复用（请求汇总/SLA/请求错误/请求时长/TTFT/上游错误）；具体取数与组装在 Task 2 的 index.vue。
  </action>
  <verify>
    <automated>cd web && pnpm vue-tsc --noEmit && pnpm exec eslint src/components/observability/HealthScoreGauge.vue src/components/observability/RealtimeRateCard.vue src/components/observability/MetricInfoCard.vue</automated>
  </verify>
  <done>HealthScoreGauge（加权 0–100 圆环 + 健康/警告/严重徽标 + 缺源归一化）、RealtimeRateCard（窗口 tab + 当前/峰值/平均 QPS·TPS + sparkline）、MetricInfoCard（大字+分位/分列+副注通用卡）就位；tabular-nums/骨架/空态/reduced-motion/亮暗达标；typecheck + eslint 干净。</done>
</task>

<task type="auto">
  <name>Task 2: UI-02 快照行 + 趋势 + 总览页组装与重构</name>
  <files>web/src/components/observability/SnapshotRow.vue, web/src/components/observability/TrendCharts.vue, web/src/pages/admin/observability/index.vue, web/src/components/observability/__tests__/dashboard.spec.ts</files>
  <action>
实现 UI-02 快照行与趋势区，并把 index.vue 重构成组合页（实现到 UI-SPEC §3 + §1 顶部布局）：

1. **`SnapshotRow.vue`**（UI-SPEC §3.1，内联阈值卡）：
   - props：`snapshot: MetricsSnapshot | null`、`loading?: boolean`。
   - 一排卡（grid 桌面多列 / 平板 2 列 / 移动 1 列）：CPU%、内存%、DB(连接·活跃·空闲 / max_connections)、Redis(connected_clients x/maxclients · hit_rate)、Qdrant(liveness 可用 + collection_count)、协程数(host.asyncio_tasks)、后台任务数(host.background_tasks.total_active 或汇总)。
   - **卡内内联阈值**：每卡显示阈值（内置默认，注释来源；如 CPU 警告 60 严重 95 / 内存 警告 70 严重 90 / 协程 警告 8000 严重 15000，对齐 REFERENCE-UI §1.2 示例）；当前值用 `healthBandClass(value, warn, crit)`（75-01 status.ts）超阈变色（绿→琥珀→红）。Redis maxclients 占比、DB 连接占比同理。
   - **降级**：源 `available===false` 或字段缺失 → 该卡显示 `n/a` + 灰态（muted），不报错（best-effort，对齐 snapshot_service envelope）。SQLite dev 下 DB available=false → "n/a (sqlite dev)" 灰态。
   - loading→每卡 skeleton。
2. **`TrendCharts.vue`**（UI-SPEC §3.2，4 类趋势，`ChartCard` 外壳 + VChart）：
   - props：`timeRange: { start: string, end: string }`、`step?: string`。用 @tanstack/vue-query 按 timeRange 拉数（keepPreviousData，watch timeRange refetch）。
   - 吞吐趋势：`queryMetrics({metric:'qps', dimension:'provider', step})` + `{metric:'tps', dimension:'provider'}`，按 dim(provider) 分系列叠加（QPS 折线 + TPS 千 via formatThousands）；内置维度切换（provider / call_source）用 `AnalyticsGroupingSelector` 范式或自建小 Select（dimension 切 'provider'↔'call_source'），切换重查。
   - 错误趋势：`queryMetrics({metric:'error', dimension:'error_class', step})`，按 error_class 取 system/upstream/business 三线（dim 值映射中文"系统/上游/业务限制"；缺失补 0）。
   - 请求时长分布：复用 `analytics/DurationDistribution`（若其入参不匹配则在卡内放 `queryMetrics({metric:'duration', agg:'p95'})` 折线作为兜底；注释抉择，优先复用，签名不符则自渲染分位折线）。
   - 并发·排队趋势：`queryMetrics({metric:'gauge:concurrency.*' 或 'gauge:queue.*'})`——按 75-01/73 写入的受控 gauge 名（concurrency./queue./backlog.）取若干线（provider 槽位 + 索引/AI描述/异步队列深）；具体 gauge 名执行时按 `metrics_query._GAUGE_NAME_PREFIXES` 与 73 GaugeSample 写入命名核对，取存在的几条。
   - 每图：加载骨架 + 空态友好（series 空→"当前时段无数据"）+ tooltip + 可点图例切系列；`degraded===true`（SQLite 近似分位）时图标题旁加小字提示"分位为近似值(SQLite)"。
3. **重构 `index.vue`**（UI-SPEC §1 + 组合）：
   - `definePage({ meta: { requiresAdmin: true } })` 保留。
   - 顶部：标题栏 + `ObservabilityTabs`（高亮总览）+ `ObservabilityTimeRange`（v-model timeRange，默认 1h；自动刷新开关）。自动刷新：开启时 setInterval 重新触发 snapshot 拉取 + 失效趋势 query（沿用既有 4s 或按 UI-SPEC "保留实时刷新 + 后端时序"，建议快照 4–5s、趋势随时间范围 query staleTime；reduced-motion/页面隐藏时暂停，沿用既有 onUnmounted 清理）。
   - 拉数：`getMetricsSnapshot()`（best-effort，catch 不崩）；近窗 errorRate/upstreamErrorRate 从 `queryMetrics({metric:'error',dimension:'error_class'})` 派生喂 HealthScoreGauge。
   - 组合：第一行 HealthScoreGauge + RealtimeRateCard；第二行 6× MetricInfoCard（按 UI-SPEC §2.3 表组装）：
     - 请求汇总（请求数/Token/平均QPS/平均TPS，从 queryMetrics qps/tps 汇总）；
     - SLA（querySla → availability 排除业务限制大字 % + 异常数 failures + footnote"排除业务限制（含系统繁忙限流）"，tone success/warning 按阈值）；
     - 请求错误（错误率 + 系统错误数 + 业务限制数分列，从 error by error_class）；
     - 请求时长（querySla? no → `queryMetrics({metric:'duration',agg:'p99'})` 大字 + p95/p90/p50/avg/max 多 agg 取数 subItems，tabular）；
     - TTFT（同请求时长，metric:'ttft'）；
     - 上游错误（错误率 + 错误数(排除429/529) + 429·529 单列；上游码区分执行时按 RequestMetric error_class='upstream' + 是否有上游码维度核对，若无 429/529 拆分维度则该单列显示总上游错误数并注释"细分 429/529 待后端维度支持"——不臆造）。
   - 第三行 SnapshotRow；第四行 TrendCharts。
   - 移除/替换旧的纯客户端滚动采样趋势（保留自动刷新机制，时序改后端驱动，per CONTEXT Claude's Discretion"后端时序驱动 + 保留实时刷新"）；旧 health/status 色逻辑迁移到 75-01 status.ts 复用，不在页内重复定义。
4. **测试** `web/src/components/observability/__tests__/dashboard.spec.ts`（vitest + @vue/test-utils + happy-dom，mock `~/api/system`）：(a) SnapshotRow 在 `host.available=false` 时渲染 n/a 灰态不抛；(b) SnapshotRow CPU 超严重阈值时命中 danger class；(c) MetricInfoCard 传 subItems 渲染分位行 + footnote；(d) HealthScoreGauge 缺源（snapshot=null）渲染 n/a 不崩。mock @tanstack/vue-query 或用 QueryClient provide（参考既有 admin spec 范式）。
  </action>
  <verify>
    <automated>cd web && pnpm vue-tsc --noEmit && pnpm exec vitest run src/components/observability/__tests__/dashboard.spec.ts && pnpm exec eslint src/pages/admin/observability/index.vue src/components/observability/SnapshotRow.vue src/components/observability/TrendCharts.vue</automated>
  </verify>
  <done>SnapshotRow（内联阈值变色 + n/a 降级）、TrendCharts（吞吐/错误三线/时长分布/并发排队 + 空态骨架 + call_source 切换 + degraded 提示）、index.vue 重构（tabs+时间范围+自动刷新 + 健康分 + 速率 + 6 卡 + 快照 + 趋势）就位；UI-01/02 数据正确（分位 tabular）；typecheck + 单测 + eslint 干净。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 浏览器 → /api/system/metrics/* | 后端 IsSuperUser fail-closed；页面 definePage requiresAdmin 守卫 |
| 后端 snapshot/趋势 → 图表渲染 | 仅数值/标签，无用户原文正文；degraded 标记如实透出 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-02-01 | Elevation of Privilege | 非 admin 访问大盘 | mitigate | index.vue `definePage({meta:{requiresAdmin:true}})` + 后端 IsSuperUser 双重兜底 |
| T-75-02-02 | Information Disclosure | 图表 tooltip/标签泄漏敏感维度值 | mitigate | 仅渲染受控维度（provider/call_source/error_class）数值与枚举，无凭证/用户原文；不渲染 raw payload |
| T-75-02-03 | Denial of Service | 极小 step×大跨度拉海量桶拖垮前端 | accept | 后端 metrics_query `_cap_step`/`_MAX_BUCKETS` 已上限保护；前端时间范围预设有界（5m/1h/24h），自定义沿用后端封顶 |
| T-75-02-SC | Tampering | npm 安装新依赖 | accept | 不新增依赖（echarts/vue-query/reka-ui/lucide 均已在依赖） |

## XSS / secret-in-bundle 注记
无 `v-html`；图表/卡片仅渲染数值与受控枚举文本；无密钥进 bundle。
</threat_model>

<verification>
- `cd web && pnpm vue-tsc --noEmit` 全绿。
- `cd web && pnpm exec vitest run src/components/observability/__tests__/dashboard.spec.ts` 绿。
- `cd web && pnpm exec eslint src/pages/admin/observability src/components/observability` 干净。
- 人工：登录超管访问 `/admin/observability`，健康分/速率/6 卡数据正确、分位 tabular；快照行超阈变色 + 源不可用 n/a；4 类趋势出图 + 空态；切时间范围与自动刷新生效；亮暗双主题、375/768/1024/1440 无溢出。
</verification>

<success_criteria>
- UI-01：复合健康分圆环（加权 + 健康/警告/严重）+ 实时速率卡（窗口 tab + 当前/峰值/平均 QPS·TPS + sparkline）+ 6 信息卡（请求/SLA 排除业务限制/请求错误系统·业务限制分列/请求时长 P99+分位/TTFT P99+分位/上游错误 429·529 单列）+ 时间范围选择器。
- UI-02：快照行内联阈值超阈变色 + 5 类源降级 n/a；吞吐(provider QPS+TPS 可切 call_source)/错误(三口径)/请求时长分布/并发·排队 4 类趋势出图 + 空态骨架。
- 后端时序驱动 + 保留自动刷新；复用 analytics/ui 组件；tabular-nums/a11y/reduced-motion/亮暗/响应式达标。
- typecheck + 组件单测 + eslint 全绿；无新依赖、无 v-html、无明文凭证渲染。
</success_criteria>

<output>
Create `.planning/phases/75-dashboard-ui/75-02-SUMMARY.md` when done.
</output>
