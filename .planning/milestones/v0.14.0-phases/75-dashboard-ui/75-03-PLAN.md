---
phase: 75-dashboard-ui
plan: "75-03"
type: execute
wave: 2
depends_on: ["75-01"]
files_modified:
  - web/src/pages/admin/observability/alerts.vue
  - web/src/components/observability/AlertEventsTable.vue
  - web/src/components/observability/AlertEventDetailSheet.vue
  - web/src/components/observability/AlertRuleFormDialog.vue
  - web/src/components/observability/AlertRulesPanel.vue
  - web/src/components/observability/__tests__/alerts.spec.ts
autonomous: true
requirements: [UI-03]
user_setup: []

must_haves:
  truths:
    - "告警事件页 alerts.vue（/admin/observability/alerts，requiresAdmin）：顶部 ObservabilityTabs（高亮告警）+ 事件表 + 多维筛选 + 规则配置入口；消费 listAlertEvents / 告警规则 CRUD"
    - "事件表（AlertEventsTable）8 列对齐 REFERENCE-UI §1.4：时间 / 级别(P0-P2 徽标) / 状态(firing 红·resolved 绿) / 维度 / 规则ID / 标题+规则信息(rule_info.expr) / 持续时长 / 邮件状态；分页倒序"
    - "多维筛选：级别(P0/P1/P2) / 状态(firing/resolved) / 规则(rule_id) / 时间段，经 listAlertEvents 参数下发"
    - "行点击 → 详情 sheet（AlertEventDetailSheet）展示完整 rule_info + target + notified_channels + 起止/持续/当前值"
    - "阈值规则配置入口（AlertRulesPanel + AlertRuleFormDialog）：规则列表（启用/禁用 switch、编辑、删除带 confirm）+ 新建/编辑 dialog 表单（metric/op/value/window/severity/channels/cooldown/dimension/title_template/enabled），写入经受控枚举与后端 400 校验对齐"
  artifacts:
    - path: "web/src/pages/admin/observability/alerts.vue"
      provides: "告警事件页（UI-03），definePage requiresAdmin，组合事件表 + 筛选 + 规则面板"
      contains: "requiresAdmin"
    - path: "web/src/components/observability/AlertEventsTable.vue"
      provides: "告警事件表（8 列对齐 REFERENCE-UI §1.4 + 分页 + 行点击）"
      contains: "AlertEventsTable"
    - path: "web/src/components/observability/AlertEventDetailSheet.vue"
      provides: "事件详情抽屉（完整 rule_info/target/notified_channels）"
      contains: "AlertEventDetailSheet"
    - path: "web/src/components/observability/AlertRuleFormDialog.vue"
      provides: "规则新建/编辑表单 dialog（受控枚举字段 + 校验）"
      contains: "AlertRuleFormDialog"
    - path: "web/src/components/observability/AlertRulesPanel.vue"
      provides: "规则列表（启用/禁用 switch + 编辑 + 删除 confirm）"
      contains: "AlertRulesPanel"
  key_links:
    - from: "web/src/pages/admin/observability/alerts.vue"
      to: "web/src/api/system.ts"
      via: "listAlertEvents + listAlertRules/createAlertRule/updateAlertRule/deleteAlertRule"
      pattern: "listAlertEvents|AlertRule"
    - from: "web/src/components/observability/AlertRuleFormDialog.vue"
      to: "web/src/api/system.ts"
      via: "createAlertRule / updateAlertRule 写规则"
      pattern: "createAlertRule|updateAlertRule"
    - from: "web/src/pages/admin/observability/alerts.vue"
      to: "web/src/components/observability/ObservabilityTabs.vue"
      via: "顶部三视图导航（75-01 产出）"
      pattern: "ObservabilityTabs"
---

<objective>
实现告警事件页（UI-03）：`/admin/observability/alerts` 子路由页，含告警事件表（8 列对齐 REFERENCE-UI §1.4）+ 多维筛选 + 行详情抽屉，以及阈值规则配置入口（规则列表 + 新建/编辑 dialog 表单 + 启停/删除）。消费 75-01 封装的 `listAlertEvents` 与告警规则 CRUD，实现到 75-UI-SPEC §4。

Purpose: 让超管能查看/筛选历史告警事件（P0/P1/P2、firing/resolved、邮件状态）并运行时管理阈值规则（CPU/错误率/TTFT/队列深等），闭环 Phase 74 告警引擎的人机界面。
Output: `alerts.vue` + `components/observability/` 下 AlertEventsTable / AlertEventDetailSheet / AlertRuleFormDialog / AlertRulesPanel + 组件单测。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/75-dashboard-ui/75-CONTEXT.md
@.planning/phases/75-dashboard-ui/75-UI-SPEC.md
@.planning/observability/REFERENCE-UI.md

# 75-01 产出（直接消费）
@web/src/api/system.ts                 # listAlertEvents/AlertEventRow + listAlertRules/createAlertRule/getAlertRule/updateAlertRule/deleteAlertRule + AlertRule/AlertRuleWrite 类型
@web/src/components/observability/ObservabilityTabs.vue
@web/src/components/observability/ObservabilityTimeRange.vue   # 事件时间段筛选复用
@web/src/components/observability/format.ts                    # formatDateTime/formatNumber/时长
@web/src/components/observability/status.ts                    # alertSeverityClass/alertStatusClass

# 后端契约（受控枚举对齐——表单选项与校验）
@server/system/alert_serializers.py    # metric 受控集合(qps/error_rate/ttft/cpu/memory/db_connections/redis_clients/qdrant/queue_depth) / op(gt/gte/lt/lte) / severity(P0/P1/P2) / channels⊆{email,feishu,webhook} / dimension 键⊆{provider,credential,model,source,queue,route}；非法 400 中文 detail
@server/system/alert_views.py          # events 查询入参(severity/status/rule_id/start/end/limit/offset) + 规则 CRUD 行为/状态码（201/204/404）

# 前端范式（执行时精读）
@web/src/pages/admin/audit/index.vue   # admin 列表页范式：表格 + 筛选 + 分页 + 详情（AlertEventsTable/筛选/sheet 借鉴）
@web/src/components/ui/table/index.ts  # 表格
@web/src/components/ui/select/index.ts # 筛选下拉
@web/src/components/ui/dialog/index.ts # 规则表单 dialog
@web/src/components/ui/sheet/index.ts  # 事件详情抽屉
@web/src/components/ui/pagination/index.ts
@web/src/components/ui/switch/index.ts # 规则启停
@web/src/components/ui/alert-dialog/index.ts # 删除二次确认
@web/src/components/ui/form/index.ts + @web/src/components/ui/input/index.ts + checkbox/label
@web/src/components/ui/sonner/index.ts # toast 反馈（保存/删除）
</context>

<tasks>

<task type="auto">
  <name>Task 1: 事件表 + 多维筛选 + 详情抽屉（AlertEventsTable / AlertEventDetailSheet）</name>
  <files>web/src/components/observability/AlertEventsTable.vue, web/src/components/observability/AlertEventDetailSheet.vue</files>
  <action>
实现告警事件呈现（Vue 3 `<script setup lang="ts">`，中文，lucide 无 emoji，tabular-nums，亮暗 token，hover/focus，骨架/空态；UI-SPEC §4.1）：

1. **`AlertEventsTable.vue`**：
   - props：`timeRange?: { start: string, end: string }`（来自页面，可空）；自管筛选 state 或接受父筛选 props——本组件**自取数**用 @tanstack/vue-query 调 `listAlertEvents`（keepPreviousData），筛选/分页 state 内部维护并 watch refetch；emit `rowClick`（AlertEventRow）供页面打开详情。
   - 顶部多维筛选条：级别 `Select`（全部/P0/P1/P2）、状态 `Select`（全部/firing/resolved）、规则 `Select`（rule_id，选项由父传入 rules 列表或本组件单独取 listAlertRules；注释抉择，建议 props.rules 传入避免重复请求）、时间段（复用页面 `ObservabilityTimeRange` 或精简 from/to）。筛选值映射 `listAlertEvents` 参数（severity/status/rule_id/start/end/limit/offset）。
   - 表 8 列（`ui/table`，对齐 REFERENCE-UI §1.4）：
     1. 时间（started_at，formatDateTime）；
     2. 级别（alertSeverityClass 徽标 P0/P1/P2）；
     3. 状态（alertStatusClass：firing 红 / resolved 绿）；
     4. 维度（target → 紧凑展示如 `provider=anthropic`，空 target 显示"overall"）；
     5. 规则ID（rule，`#<id>`，null 显示"—（已删）"）；
     6. 标题+规则信息（title_zh + 第二行 `rule_info.expr` 机器可读串如 `cpu_usage_percent > 85.00 (current 95.40) over last 5m`，等宽小字截断 + title 全文）；
     7. 持续时长（duration_s→formatDuration；firing 进行中显示"进行中"）；
     8. 邮件状态（email_sent：sent 已发送绿 / skipped 已忽略 muted / failed 失败红 / pending —）。
   - 分页（`ui/pagination` 或 limit/offset 上一页/下一页 + total）；倒序由后端保证。行 `cursor-pointer` + `@click` emit rowClick；空态友好（lucide bell-off + "暂无告警事件"）；loading→骨架行。移动端表格横向滚动或卡片化不溢出。
2. **`AlertEventDetailSheet.vue`**（`ui/sheet` 抽屉）：
   - props：`event: AlertEventRow | null`、`open: boolean`；emit `update:open`。
   - 展示完整信息：标题 + 级别/状态徽标；rule_info 全字段（metric/op/threshold/current/window_s/dimension/expr，键值表）；target（维度 JSON）；起始/结束/持续/当前值/last_seen；email_sent + notified_channels（通道徽标列表）。原始 JSON 用 `<pre>` 文本渲染（**禁 v-html**）。空态（event=null）不渲染主体。a11y：sheet 标题/关闭按钮 aria-label。
  </action>
  <verify>
    <automated>cd web && pnpm vue-tsc --noEmit && pnpm exec eslint src/components/observability/AlertEventsTable.vue src/components/observability/AlertEventDetailSheet.vue</automated>
  </verify>
  <done>AlertEventsTable（8 列对齐 §1.4 + 多维筛选 + 分页倒序 + 行点击 + 空态/骨架）+ AlertEventDetailSheet（完整 rule_info/target/notified_channels，pre 文本无 v-html）就位；typecheck + eslint 干净。</done>
</task>

<task type="auto">
  <name>Task 2: 规则配置入口 + 页面组装（AlertRulesPanel / AlertRuleFormDialog / alerts.vue）</name>
  <files>web/src/components/observability/AlertRuleFormDialog.vue, web/src/components/observability/AlertRulesPanel.vue, web/src/pages/admin/observability/alerts.vue, web/src/components/observability/__tests__/alerts.spec.ts</files>
  <action>
实现阈值规则 CRUD 入口并组装页面（UI-SPEC §4.2）：

1. **`AlertRuleFormDialog.vue`**（`ui/dialog` + `ui/form`/vee-validate + zod，新建/编辑共用）：
   - props：`open: boolean`、`rule?: AlertRule | null`（传入=编辑，空=新建）；emit `update:open`、`saved`（保存成功后父刷新）。
   - 表单字段（受控枚举与后端 alert_serializers 对齐，禁自由字符串污染）：
     - `name`（input）；`metric`（Select，选项 = 后端受控集合 qps/error_rate/ttft/cpu/memory/db_connections/redis_clients/qdrant/queue_depth，中文标签映射）；`op`（Select gt/gte/lt/lte，符号显示 >/≥/</≤）；`value`（number input）；`window`（number 秒，默认 300）；`severity`（Select P0/P1/P2）；`channels`（checkbox 组 email/feishu/webhook，多选）；`cooldown`（number 秒，默认 600）；`dimension`（受控键值：提供 provider/credential/model/source/queue/route 受控键的可选单条 key+value，或留空=overall；UI 用 key Select + value input，注释 dimension 必须是受控键的 dict）；`title_template`（input，可空，提示支持 {metric}/{current}/{value}）；`enabled`（switch，默认 true）。
   - zod schema 前端先校验（metric/op/severity enum、value 数值、channels 子集），提交 `createAlertRule` 或 `updateAlertRule(id, ...)`；后端 400（中文 detail）经 ApiError 捕获 → 字段/toast 错误展示（沿用 client.ts ApiError.detail）。成功 toast（sonner）+ emit saved + 关闭。异步提交禁用按钮 + spinner。
2. **`AlertRulesPanel.vue`**：
   - 自取数 `listAlertRules`（vue-query）；「新建规则」按钮 → 打开 AlertRuleFormDialog（rule=null）。
   - 规则列表（表或卡）：名称 / metric op value（人读 expr 如 `CPU > 85`）/ severity 徽标 / window/cooldown / channels 徽标 / enabled `switch`（切换即 `updateAlertRule(id,{enabled})` + toast，乐观或 invalidate）/ 编辑按钮（打开 dialog rule=该条）/ 删除按钮（`alert-dialog` 二次确认 → `deleteAlertRule(id)` + toast + invalidate）。
   - 空态（无规则）友好 + 引导新建；loading 骨架。
3. **`alerts.vue`**（页面组装）：
   - `definePage({ meta: { requiresAdmin: true } })`。
   - 顶部：标题栏 + `ObservabilityTabs`（高亮告警）。
   - 布局：上方 `AlertRulesPanel`（规则配置入口，可折叠/独立卡），下方 `AlertEventsTable`（事件表，传 rules 列表供筛选 + 监听 rowClick 打开 `AlertEventDetailSheet`）。规则增删改后 invalidate 事件表的 rule 筛选选项（vue-query queryKey 失效）。
4. **测试** `web/src/components/observability/__tests__/alerts.spec.ts`（vitest + @vue/test-utils，mock `~/api/system`）：(a) AlertEventsTable 渲染 8 列表头 + 一行事件含 expr；(b) 选级别筛选 P0 触发以 severity=P0 调 listAlertEvents；(c) AlertRuleFormDialog 提交合法 body 调 createAlertRule；(d) AlertRuleFormDialog 后端返回 ApiError(400) 时展示错误不崩；(e) AlertRulesPanel 删除走 confirm 后调 deleteAlertRule。mock QueryClient/provide 参考既有 admin spec。
  </action>
  <verify>
    <automated>cd web && pnpm vue-tsc --noEmit && pnpm exec vitest run src/components/observability/__tests__/alerts.spec.ts && pnpm exec eslint src/pages/admin/observability/alerts.vue src/components/observability/AlertRuleFormDialog.vue src/components/observability/AlertRulesPanel.vue</automated>
  </verify>
  <done>AlertRuleFormDialog（受控枚举字段 + zod 前校验 + 后端 400 兜底 + toast）、AlertRulesPanel（列表 + 启停 switch + 编辑 + 删除 confirm）、alerts.vue（tabs + 规则面板 + 事件表 + 详情 sheet 联动）就位；规则 CRUD 闭环；typecheck + 单测 + eslint 干净。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 浏览器 → /api/system/alerts/* | 后端 IsSuperUser fail-closed；alerts.vue definePage requiresAdmin |
| 规则表单写入 → SystemAlertRule | metric/op/severity/channels/dimension 受控枚举，后端 serializer 白名单 400 兜底 |
| 删除规则 | 不可逆，需二次确认 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-03-01 | Elevation of Privilege | 非 admin 访问/写告警规则 | mitigate | alerts.vue requiresAdmin + 后端 IsSuperUser；写操作均经后端 CRUD 鉴权 |
| T-75-03-02 | Tampering | 任意 metric/channel/dimension 注入污染评估 | mitigate | 表单 Select/checkbox 限受控枚举 + zod 前校验 + 后端 400 白名单兜底（双层） |
| T-75-03-03 | Information Disclosure | rule_info/target/pre 渲染泄漏敏感原文 | mitigate | rule_info/target 仅后端写入的元数据（受控维度/阈值/当前值），pre 文本渲染禁 v-html；后端写入侧已脱敏 |
| T-75-03-04 | Denial of Service | 误删规则 | mitigate | 删除走 alert-dialog 二次确认 |
| T-75-03-SC | Tampering | npm 安装新依赖 | accept | 复用既有 reka-ui/vee-validate/zod/vue-query/lucide/sonner，无新依赖 |

## XSS / secret-in-bundle 注记
无 `v-html`（rule_info/target 用 `<pre>`/键值表文本）；无密钥进 bundle。
</threat_model>

<verification>
- `cd web && pnpm vue-tsc --noEmit` 全绿。
- `cd web && pnpm exec vitest run src/components/observability/__tests__/alerts.spec.ts` 绿。
- `cd web && pnpm exec eslint src/pages/admin/observability/alerts.vue src/components/observability` 干净。
- 人工：超管访问 `/admin/observability/alerts`，事件表 8 列对齐 §1.4 + 级别/状态/规则/时间段筛选生效 + 行点击看详情；新建/编辑规则 dialog 受控枚举校验、非法值 400 友好提示；启停 switch + 删除二次确认生效；亮暗/响应式无溢出。
</verification>

<success_criteria>
- UI-03：事件表 8 列对齐 REFERENCE-UI §1.4（时间/级别/状态/维度/规则ID/标题+规则信息/持续时长/邮件状态）+ 多维筛选（级别/状态/规则/时间段）+ 行详情抽屉。
- 阈值规则配置入口：列表 + 启停 switch + 新建/编辑 dialog（受控枚举 metric/op/value/window/severity/channels/cooldown/dimension/title_template/enabled）+ 删除二次确认；与后端 CRUD/400 校验对齐。
- typecheck + 组件单测 + eslint 全绿；无新依赖、无 v-html、无明文凭证渲染；requiresAdmin 守卫。
</success_criteria>

<output>
Create `.planning/phases/75-dashboard-ui/75-03-SUMMARY.md` when done.
</output>