---
phase: 75-dashboard-ui
plan: "75-05"
type: execute
wave: 3
depends_on: ["75-02", "75-03", "75-04"]
files_modified:
  - web/src/components/layout/AppSidebar.vue
  - .planning/observability/LOGGING-SPEC.md
  - .cursor/rules/observability-logging.mdc
  - AGENTS.md
  - CLAUDE.md
autonomous: true
requirements: [SPEC-01]
user_setup: []

must_haves:
  truths:
    - "admin 导航的「运维监控」入口指向可观测大盘，三视图（总览/告警/日志）经各页顶部 ObservabilityTabs 子路由可达；typed-router 自动收录 /admin/observability、/admin/observability/alerts、/admin/observability/logs 三路由（requiresAdmin）"
    - "LOGGING-SPEC.md §10 事件目录补全 Phase 72/73/74 已埋点事件（按 component 分组，标注 category caller/sampling），与代码实际 structlog 事件名一致"
    - "LOGGING-SPEC §4.1 call_source 枚举与 §5 component 清单覆盖 72–74 新增（含 metrics/alerting/call_drilldown/conversation_drilldown/webhook_events 等组件与新 call_source）"
    - ".cursor/rules/observability-logging.mdc 的提交前自检清单与 LOGGING-SPEC §9 清单一致并覆盖 LLM call_source/请求入口/召回/队列/webhook/告警阈值；AGENTS.md 与 CLAUDE.md 的可观测性章节与最终规范挂接无矛盾"
    - "PR/Code Review checklist 落地（确认覆盖 LLM call_source、请求入口 QPS/错误率/时长、召回条数/分层耗时/score、队列积压、webhook 原始留痕、告警阈值评估），三处（mdc 规则 + LOGGING-SPEC §9 + AGENTS/CLAUDE）表述收敛"
  artifacts:
    - path: ".planning/observability/LOGGING-SPEC.md"
      provides: "全量事件目录（71–74）+ call_source/component 清单补全 + PR/Review checklist 终稿"
      contains: "事件目录"
    - path: ".cursor/rules/observability-logging.mdc"
      provides: "强制规则与 LOGGING-SPEC 收敛后的提交前自检清单"
      contains: "新功能提交前自检"
    - path: "web/src/components/layout/AppSidebar.vue"
      provides: "admin 导航可观测性入口（指向 /admin/observability）"
      contains: "/admin/observability"
  key_links:
    - from: "web/src/components/layout/AppSidebar.vue"
      to: "/admin/observability"
      via: "adminNavItems 运维监控入口"
      pattern: "/admin/observability"
    - from: ".cursor/rules/observability-logging.mdc"
      to: ".planning/observability/LOGGING-SPEC.md"
      via: "强制规则引用完整规范 + checklist 收敛"
      pattern: "LOGGING-SPEC"
---

<objective>
里程碑收尾：把可观测性大盘的导航/路由收口，并把日志/埋点规范固化为长期约束（SPEC-01）。确认 admin 导航「运维监控」入口与三视图子路由可达；补全 `LOGGING-SPEC.md` 事件目录（71–74 全量已埋点事件）与 call_source/component 清单；复核并收敛 `.cursor/rules/observability-logging.mdc` 与 AGENTS.md/CLAUDE.md 的可观测性章节；落地统一的 PR/Code Review checklist。

Purpose: 前四个 plan 交付了大盘 UI 与数据消费；本 plan 让"新增/改动任何功能必须补齐日志与埋点"成为可执行、可审查的长期工程约束，并确保大盘三视图在 admin 导航中可达。这是 v0.14.0 里程碑的终点 plan。
Output: AppSidebar 导航确认/补全 + LOGGING-SPEC.md 终稿（事件目录/枚举/checklist）+ observability-logging.mdc 与 AGENTS/CLAUDE 收敛。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/75-dashboard-ui/75-CONTEXT.md
@.planning/phases/75-dashboard-ui/75-UI-SPEC.md

# 规范现状（执行时精读，本 plan 修订）
@.planning/observability/LOGGING-SPEC.md          # §4.1 call_source 枚举 / §5 component 清单 / §9 checklist / §10 事件目录（当前仅 Phase 71）——补全 72/73/74
@.cursor/rules/observability-logging.mdc          # 强制规则 + 提交前自检清单（与 LOGGING-SPEC §9 收敛）
@AGENTS.md                                        # 文末「可观测性与日志规范（强制）」章节（与最终规范挂接）
@CLAUDE.md                                        # 同 AGENTS.md 的可观测性章节（保持一致）

# 导航 / 路由现状
@web/src/components/layout/AppSidebar.vue          # adminNavItems 已含 { to:'/admin/observability', label:'运维监控' }——确认/补全
@web/src/typed-router.d.ts                          # 路由声明（pages 文件由 unplugin-vue-router 自动生成；执行后应含三 observability 路由）

# 事件目录补全的代码来源（执行时用 Grep 枚举 structlog 事件名，勿臆造）
# Phase 72: server/agents/llm_concurrency.py、Runner astream 埋点、interactions/、RetrievalTrace 写入点
# Phase 73: server/system/snapshot_service.py、metrics_views.py、metrics_query.py、gauge 采样(apscheduler)
# Phase 74: server/system/alert_views.py、告警评估器(evaluate_system_alerts)、通知分发
# Phase 71: server/system/log_views.py、drilldown_views.py、webhook_views.py（已部分在 §10）
</context>

<tasks>

<task type="auto">
  <name>Task 1: admin 导航入口确认 + 三视图子路由可达性校验</name>
  <files>web/src/components/layout/AppSidebar.vue</files>
  <action>
确认并完善 admin 可观测性导航（最小改动；75-02/03/04 的页面文件已由 unplugin-vue-router 自动生成 `/admin/observability`、`/admin/observability/alerts`、`/admin/observability/logs` 三路由）：

1. **`AppSidebar.vue`**：`adminNavItems` 已含 `{ to: '/admin/observability', label: '运维监控', icon: 'lucide--activity' }`。保留单一顶级入口指向总览（三视图切换由各页顶部 `ObservabilityTabs` 承担，对齐 UI-SPEC §1"子路由 + 顶部 tabs"）。
   - 若要在侧栏直接暴露三子项（可选，Claude's Discretion）：可在该 NavItem 下加二级链接（alerts/logs），但**保持与既有 NavItem 渲染结构一致**，不破坏其它导航；若侧栏无二级结构则不强加，仅保留总览入口 + 页内 tabs（推荐，最小改动避免改 NavItem 类型）。label/icon 用中文 + lucide。
2. **路由可达性校验**：执行 `cd web && pnpm vue-tsc --noEmit`——unplugin-vue-router 在 dev/build 时重新生成 `typed-router.d.ts`，应出现 `/admin/observability/alerts` 与 `/admin/observability/logs`（确认三页 `definePage({meta:{requiresAdmin:true}})` 均生效）。若 typed-router.d.ts 未刷新，运行一次 `pnpm exec vue-tsc --noEmit` 或 dev 触发即可（不手改自动生成文件）。
3. 不改其它导航项；不引入新依赖。
  </action>
  <verify>
    <automated>cd web && pnpm vue-tsc --noEmit && pnpm exec eslint src/components/layout/AppSidebar.vue</automated>
  </verify>
  <done>admin 导航「运维监控」入口指向 /admin/observability；三视图经页内 ObservabilityTabs 子路由可达；typed-router 收录三路由（requiresAdmin）；typecheck + eslint 干净；无新依赖。</done>
</task>

<task type="auto">
  <name>Task 2: LOGGING-SPEC 事件目录/枚举补全 + 规则与 AGENTS/CLAUDE 收敛 + PR checklist 落地</name>
  <files>.planning/observability/LOGGING-SPEC.md, .cursor/rules/observability-logging.mdc, AGENTS.md, CLAUDE.md</files>
  <action>
固化日志/埋点规范为长期约束（文档正文中文，专有名词/代码标识/事件名保留英文原文，对齐 doc-writing-zh 规则）：

1. **`LOGGING-SPEC.md` §10 事件目录补全 72/73/74**（先用 Grep 枚举代码中实际 `structlog` 事件名 + category/component，**勿臆造**）：
   - 用 `rg` 在 `server/` 搜 `logger.(info|warning|error|debug)\(` 与 `category=`/`component=`，聚焦 Phase 72（llm_concurrency/Runner astream/interactions/RetrievalTrace 写入：如 qps/排队/LLMBusyError、TTFT、TPS、上游错误、召回条数/分层耗时/score 等事件）、Phase 73（snapshot 各源 `snapshot_*_failed`、`metrics_snapshot_served`、`metrics_query_served`、gauge 采样事件、指标保留清理）、Phase 74（`alert_rules_listed`/`alert_rule_created/updated/deleted`、`alert_events_queried`、评估器触发/恢复、通知分发/邮件回写事件）。
   - 在 §10 现有 71 小节后**追加** §10.x 小节（如 §10.7 调用采集、§10.8 快照/趋势/查询、§10.9 告警），每行：`事件名 | category(caller/sampling) | component | 说明`，与代码一致；71 已列事件查漏补缺（drilldown/webhook 视图事件 `call_drilldown_viewed`/`conversation_drilldown_viewed`/`webhook_events_queried`/`system_logs_queried`/`system_logs_cleared` 等确认在册）。
2. **§4.1 call_source 枚举 + §5 component 清单复核补全**：确认 72 引入的 call_source（22 类）齐全；component 清单补 `metrics`/`alerting`/`alert_retention`/`call_drilldown`/`conversation_drilldown`/`webhook_events`/`system_logs` 等 71–74 实际使用值（与代码 `component=` 取值核对）。
3. **§9 提交前自检 / Code Review checklist 终稿**：确认覆盖——关键生命周期 started/completed/failed + duration_ms；category+component；触发用户绑定（system 兜底）；脱敏（redact_credentials/redact_secrets_in_text/redact_for_ledger）；新增 LLM 调用赋 call_source + 上报请求数/token/TTFT/上游码；新增请求入口纳入 QPS/错误率/时长；新增召回上报条数/分层耗时/score + RetrievalTrace（MCP+对话两链）；新增队列/异步任务积压可快照 + 携发起用户；新增 webhook 原始脱敏入库；评估是否需告警阈值。补充"v0.14.0 平台设施已就绪"说明（指标门面/SystemLogEntry 落库/用户上下文贯穿已落地，移除/更新原"设施就绪前"过渡语）。
4. **`.cursor/rules/observability-logging.mdc` 收敛**：使其"必守原则 + 事件分类 + 新功能提交前自检"与 LOGGING-SPEC §1/§2/§9 表述一致（同一份 checklist，避免分叉）；保留引用 `LOGGING-SPEC.md` 与 `MILESTONE-PROPOSAL.md` 路径。
5. **`AGENTS.md` + `CLAUDE.md` 可观测性章节复核**：两文件文末「可观测性与日志规范（强制）」章节内容应一致且与最终 LOGGING-SPEC/mdc 不矛盾（同步更新两份；二者本就镜像）。如有 v0.14.0 落地后的措辞（设施已就绪）需同步更新。
6. 不改任何代码/埋点逻辑（本 plan 仅文档与规范固化 + 导航在 Task 1）；不新增依赖。
  </action>
  <verify>
    <automated>rg -n "system_logs_queried|alert_events_queried|metrics_query_served|metrics_snapshot_served" server | head -20 && rg -c "category=" server/system/alert_views.py server/system/log_views.py server/system/metrics_views.py</automated>
  </verify>
  <done>LOGGING-SPEC §10 事件目录补全 72/73/74（事件名/category/component 与代码一致，经 rg 核对）+ §4.1/§5 枚举清单补全 + §9 checklist 终稿；observability-logging.mdc 与 AGENTS.md/CLAUDE.md 可观测性章节与最终规范收敛无矛盾；PR/Review checklist 三处表述一致；无代码改动。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 文档/规范修订 | 仅 .md/.mdc 与导航单文件，无运行时代码逻辑改动 |
| admin 导航入口 | 指向 requiresAdmin 守卫页面，授权由路由 meta + 后端兜底 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-05-01 | Information Disclosure | 事件目录在文档中误抄含明文凭证的示例 | mitigate | 事件目录只列事件名/category/component/说明，不贴 payload 实例；脱敏约定保留为强制条款 |
| T-75-05-02 | Tampering | 规范分叉（mdc 与 LOGGING-SPEC 不一致导致 review 失效） | mitigate | 本 plan 显式把三处 checklist 收敛为同一份，互相引用单一来源 |
| T-75-05-SC | Tampering | npm 安装新依赖 | accept | 无依赖改动（仅文档 + 导航既有结构） |

## XSS / secret-in-bundle 注记
本 plan 不触前端业务代码（仅 AppSidebar 导航既有结构内文案/链接确认）；无 v-html、无密钥进 bundle。
</threat_model>

<verification>
- `cd web && pnpm vue-tsc --noEmit && pnpm exec eslint src/components/layout/AppSidebar.vue` 干净；typed-router 含三 observability 路由。
- `rg` 核对 LOGGING-SPEC §10 新增事件名均能在 `server/` 命中（无臆造事件）。
- 人工：三处 checklist（mdc / LOGGING-SPEC §9 / AGENTS·CLAUDE）逐条对照一致；admin 侧栏点「运维监控」进总览，页内 tabs 切到告警/日志。
</verification>

<success_criteria>
- SPEC-01：LOGGING-SPEC.md 事件目录全量补全 71–74 已埋点事件，call_source/component 清单覆盖新增；§9 PR/Code Review checklist 终稿落地。
- .cursor/rules/observability-logging.mdc 与 AGENTS.md/CLAUDE.md 可观测性章节与最终规范收敛无矛盾、无分叉。
- admin 导航「运维监控」入口可达，三视图子路由（总览/告警/日志）经页内 tabs 可达且 requiresAdmin。
- 仅文档 + 导航改动，无代码/埋点逻辑变更、无新依赖；typecheck + eslint 干净。
</success_criteria>

<output>
Create `.planning/phases/75-dashboard-ui/75-05-SUMMARY.md` when done.
</output>
