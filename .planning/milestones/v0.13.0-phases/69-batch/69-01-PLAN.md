---
phase: 69-batch
plan: 01
type: execute
wave: 1
depends_on: []
requirements: [BATCH-01, BATCH-02]
files_modified:
  - server/repositories/views.py
  - server/repositories/urls.py
  - server/tests/repositories/test_batch_and_reindex.py
  - web/src/api/repositories.ts
  - web/src/pages/repositories/index.vue
must_haves:
  truths:
    - "ReindexAllView(IsSuperUser) 全量入队未删除仓库、跳过 INDEXING、经 _schedule_index(Phase 67 槽位锁)排队消费，返回 queued/skipped/total"
    - "RepositoryBatchCreateView 批量建仓复用 _acreate_repository_core（从 acreate 抽取），单项失败隔离，返回 created/failed，上限 500"
    - "urls：batch/ + reindex-all/ 在 router include 之前（避免被当 repo id）"
    - "前端：超管全部更新索引按钮(确认+已排队N个toast) + CSV 批量建仓按钮(解析+toast)"
  artifacts:
    - path: "server/repositories/views.py"
      provides: "_acreate_repository_core + RepositoryBatchCreateView + ReindexAllView"
      contains: "ReindexAllView"
---

# Phase 69 Plan 01 — 批量加仓 + 全部更新索引（超管）

后端两端点 + acreate 抽取共享 helper + urls；前端列表页两按钮 + API。
守护：`test_batch_and_reindex.py`（reindex 403/queued-skipped、batch 创建/隔离/空）。
