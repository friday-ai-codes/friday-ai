---
phase: 67-concurrency
plan: 03
type: execute
wave: 3
depends_on: ["67-01", "67-02"]
requirements: [CONC-03]
files_modified:
  - .env.example
  - server/tests/durable/test_concurrency_governance.py
must_haves:
  truths:
    - "容器并发复用 Runner.concurrent（DB + Go scheduler 信号量）；MCP 不限；不设全局总上限"
    - ".env.example 并发治理段（索引/图谱/LLM/容器/MCP + 无全局上限）"
    - "设计契约守护：defer 含 lock / 设置键存在 / 凭证 max_concurrency / runner.concurrent / 无全局上限键"
---

# Phase 67 Plan 03 — CONC-03 容器/MCP/无全局上限 + 文档与契约守护

`.env.example` 并发治理段；`tests/durable/test_concurrency_governance.py` 设计契约守护。
