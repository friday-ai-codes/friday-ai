---
phase: 70-token
plan: 01
type: execute
wave: 1
depends_on: []
requirements: [TOKEN-01, TOKEN-02]
files_modified:
  - server/repositories/models.py
  - server/repositories/migrations/0039_repository_git_instance_credential.py
  - server/services/git_credentials.py
  - server/repositories/serializers.py
  - server/repositories/views.py
  - server/tests/repositories/test_token_provider_fk.py
  - web/src/types/index.ts
  - web/src/api/repositories.ts
  - web/src/components/repository/CreateRepositoryModal.vue
  - web/src/pages/admin/git-credentials/index.vue
must_haves:
  truths:
    - "Repository.git_instance_credential FK(可空,SET_NULL)+migration；resolver 优先级 per-repo→FK→host→none，老仓库零回归"
    - "建仓 access_token 可选 + git_instance_credential_id；无 token 必须 FK/host 可解析否则 fail-loud；仅自有 token 才建 GitCredential"
    - "has_credential 反映 per-repo/FK/host 可解析；TestConnection 无 token 按 FK/host fallback"
    - "前端 token 可选 + 密钥提供方 select + 透传 + 防抖自动校验；admin 页用途说明"
  artifacts:
    - path: "server/services/git_credentials.py"
      provides: "resolver FK 优先级"
      contains: "instance_fk"
---

# Phase 70 Plan 01 — access token / 密钥提供方重构（FK）

FK + migration + resolver 优先级 + 建仓 token 可选 + has_credential + TestConnection fallback + 前端 select/可选 token + admin 说明。
守护：`test_token_provider_fk.py`（resolver 4 优先级 + 建仓 FK/host/fail + has_credential）。
