---
phase: 142-mcp
plan: "03"
type: execute
wave: 1
depends_on: ["142-01"]
files_modified:
  - mcp/src/tools.ts
autonomous: true
requirements: [MCP-01, MCP-03]
must_haves:
  truths:
    - "Cursor / Claude Code 通过 npm MCP ListTools 可发现 report_session_knowledge"
    - "npm inputSchema 暴露与服务端相同的 12 个请求键，且只要求 question/answer"
    - "工具描述明确 accepted=true 仅表示 Capture 已收，不代表挂钩或 RAG 入图"
    - "工具 annotations 表示非只读、非破坏、可幂等且不触达开放外部系统"
  artifacts:
    - path: "mcp/src/tools.ts"
      provides: "第 52 个 FRIDAY_TOOLS 条目和 report_session_knowledge 专用 annotations"
      contains: "name: 'report_session_knowledge'"
  key_links:
    - from: "mcp/src/tools.ts"
      to: "/api/mcp/tools/report_session_knowledge/"
      via: "mcp/src/server.ts 的静态 FRIDAY_TOOLS 白名单与 callFridayTool 通用 POST"
      pattern: "report_session_knowledge"
    - from: "mcp/src/tools.ts"
      to: "server/mcp_tools/serializers.py"
      via: "inputSchema.properties 与 TOOL_SCHEMA_SNAPSHOT request 键级相等"
      pattern: "question.*answer"
---

<objective>
在 npm MCP 客户端面公开 report_session_knowledge，冻结 IDE 可发现的输入 schema 与幂等写操作注解。

Purpose: 避免只实现 Django 导致 Cursor/Claude Code 静态白名单不可达，并完成 MCP-03 的第三面契约。
Output: mcp/src/tools.ts 中的工具定义、描述和专用 annotations。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/142-mcp/142-CONTEXT.md
@.planning/phases/142-mcp/142-RESEARCH.md
@.planning/phases/142-mcp/142-PATTERNS.md
@.planning/phases/142-mcp/142-01-SUMMARY.md
@mcp/src/tools.ts
@mcp/src/server.ts
@mcp/tests/server.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 同步发布 npm 工具条目与幂等 annotations</name>
  <files>mcp/src/tools.ts</files>
  <behavior>
    - D-01/D-02/D-09：工具 properties 精确包含 question、answer、repository_id、git_url、branch_name、project_id、session_id、response_model、provider、input_tokens、output_tokens、client。
    - D-01：required 仅为 question、answer，且 additionalProperties=false。
    - D-03/D-10：描述明确 answer 是客户端可见精华，accepted=true 仅代表 Capture 已持久化，不承诺仓库挂钩、价值评估或 RAG 入图。
    - D-10：readOnlyHint=false、destructiveHint=false、idempotentHint=true、openWorldHint=false；TOOL_ANNOTATIONS 键集与 FRIDAY_TOOLS 名集精确相等。
  </behavior>
  <action>在同一次提交内同时改 FRIDAY_TOOLS 与 TOOL_ANNOTATIONS，使 npm tests/server.test.ts 可独立通过：在 report_project_knowledge 相邻区域新增 report_session_knowledge 条目，文件头真实工具计数 51→52；使用既有 str/uuid helper，token 计数声明为字符串而非整数，client 为开放字符串，session_id 不声明 UUID；additionalProperties=false 且 required=['question','answer']；描述禁止诱导上传 transcript/隐藏思维链，并清楚区分已收 Capture 与已入知识库。同一任务在 TOOL_ANNOTATIONS 的项目上下文/记忆区域新增独立对象，中文 title 使用现有“领域 · 动作”格式；不要调用 generator()（idempotentHint=false 与 first-write-wins 冲突），也不要改 generator() 全局语义。不要修改 report_project_knowledge 条目，不增加外部依赖。不要跑 Python 三面相等测试（属 Plan 04）。</action>
  <verify>
    <automated>cd mcp && npm test -- tests/server.test.ts</automated>
  </verify>
  <done>FRIDAY_TOOLS 恰有 52 个唯一工具且含 report_session_knowledge；对应 annotations 四 hint 精确匹配；整个 tests/server.test.ts 在本任务结束后独立通过。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IDE agent → npm MCP schema | Agent 根据公开 JSON schema 组装可能包含敏感正文的请求 |
| npm MCP → Django MCP API | 通用 callFridayTool 透传参数与 Bearer 认证 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-142-01 | Spoofing | npm callFridayTool | accept | 认证头与 401 处理是既有 server.ts 能力，本计划不改 |
| T-142-02 | Information Disclosure | tool description | mitigate | 描述明确不上传隐藏思维链、全文 transcript 或凭证 |
| T-142-04 | Tampering | npm/server schema drift | mitigate | 12 键 schema 与 Python 三面对齐守卫 |
| T-142-05 | Repudiation | idempotency annotation | mitigate | 独立 annotation 声明 idempotentHint=true，与服务端 first-write-wins 一致 |
| T-142-SC | Tampering | npm install | accept | 不安装、不升级依赖，不改 lockfile |
</threat_model>

<verification>
- `cd mcp && npm test -- tests/server.test.ts`
- `git diff -- mcp/package.json mcp/package-lock.json` 必须为空。
</verification>

<success_criteria>
- npm ListTools 静态白名单包含第 52 个工具。
- properties、required、additionalProperties 和 annotations 全部由自动测试锁定。
- 旧 report_project_knowledge npm 定义与全局 generator helper 不变。
</success_criteria>

<output>
Create `.planning/phases/142-mcp/142-03-SUMMARY.md` when done.
</output>
