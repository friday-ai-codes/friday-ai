---
phase: 143-eval
plan: "04"
type: execute
wave: 2
depends_on: ["143-01", "143-03"]
files_modified:
  - server/agents/call_source.py
  - server/initiatives/services/session_capture_eval.py
  - server/tests/initiatives/test_session_capture_eval.py
  - server/tests/test_model_usage_call_source.py
  - .planning/observability/LOGGING-SPEC.md
autonomous: true
requirements: [EVAL-01, EVAL-02, EVAL-05, OBS-04]
must_haves:
  truths:
    - "Friday LLM 独立输出 high/medium/low 与可脱离会话理解的 distilled_essence"
    - "非法结构、缺默认模型或上游失败保持 eval_failed 可重试，不猜值也不降 low"
    - "每次评估以 session_capture_eval 记录模型用量和上游错误"
    - "评估器不调用任何 ProjectMemory 写入口"
  artifacts:
    - path: "server/initiatives/services/session_capture_eval.py"
      provides: "严格结构化 Session Capture 价值评估器"
      exports: ["SessionCaptureEvaluator"]
    - path: "server/agents/call_source.py"
      provides: "SESSION_CAPTURE_EVAL 受控用量标签"
    - path: ".planning/observability/LOGGING-SPEC.md"
      provides: "47 值枚举与 eval sampling 事件目录"
  key_links:
    - from: "server/initiatives/services/session_capture_eval.py"
      to: "interactions.ledger.arecord_llm_usage"
      via: "成功/失败统一用量记录"
      pattern: "arecord_llm_usage"
    - from: "server/initiatives/services/session_capture_eval.py"
      to: "CaptureService"
      via: "只返回强类型结果/异常，由 worker 经唯一 writer 落状态"
      pattern: "SessionCaptureEvalResult"
---

<objective>
实现独立的 Friday LLM 三档评估器及完整模型用量归因。

Purpose: 把知识价值判断与写回质量门、仓库路由 confidence、ProjectMemory 语义彻底隔离。
Output: `session_capture_eval` CallSource、严格 evaluator、用量测试与观测规范。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/143-eval/143-CONTEXT.md
@.planning/phases/143-eval/143-RESEARCH.md
@.planning/phases/143-eval/143-PATTERNS.md
@.planning/phases/143-eval/143-01-SUMMARY.md
@.planning/phases/143-eval/143-03-SUMMARY.md
@server/agents/call_source.py
@server/initiatives/services/memory_distill.py
@server/services/process_runtime/initiative_profile.py
@server/tests/test_model_usage_call_source.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 注册 session_capture_eval 用量与日志合同</name>
  <files>server/agents/call_source.py, server/tests/test_model_usage_call_source.py, .planning/observability/LOGGING-SPEC.md</files>
  <action>在 CallSource 增加 `SESSION_CAPTURE_EVAL = "session_capture_eval"`，更新模块/类 docstring 计数为 47。测试期望集同时补齐既有生产枚举 `initiative_profile` 与新值，集合相等与长度 47 均成立。LOGGING-SPEC §4.1 同步 47 值与表格条目，入口指向 SessionCaptureEvaluator；§10.10 增加 eval/normalize/ingest/recovery sampling 生命周期，字段仅为 capture_id/tier/status/attempt/duration/user 和汇总计数，不含问答或精华正文。</action>
  <verify>
    <automated>cd server &amp;&amp; uv run pytest tests/test_model_usage_call_source.py::TestCallSourceEnum -x &amp;&amp; uv run ruff check agents/call_source.py tests/test_model_usage_call_source.py</automated>
  </verify>
  <done>CallSource、完整期望集与 LOGGING-SPEC 对齐为 47 值，首次 evaluator 调用前合同已存在。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现严格 Friday LLM 三档 evaluator</name>
  <files>server/initiatives/services/session_capture_eval.py, server/tests/initiatives/test_session_capture_eval.py</files>
  <behavior>
    - "JSON {value_tier, distilled_essence} 的 high/medium/low 返回强类型结果"
    - "非法 JSON/tier/空 essence/缺模型/上游错误抛可重试评估错误"
    - "成功与失败都记录 session_capture_eval ModelUsageRecord"
  </behavior>
  <action>新增 frozen `SessionCaptureEvalResult` 与显式可重试异常，Evaluator 接收 capture_id/question/answer 但不写 ORM。空 question 或 answer 在模型前失败；调用 `ProviderConfigService.aresolve()`，只取 resolved.extra.default_model，缺失即失败，禁止 legacy/hardcoded fallback。用 `build_chat_model(resolved, model_name, streaming=False)`、`content_to_text`、`HumanMessage` 和 `use_call_source(CallSource.SESSION_CAPTURE_EVAL)`。提示词要求严格 JSON object，tier 逐字 high/medium/low，distilled_essence 只包含可复用结论、约束、根因、方案和验证证据且脱离会话可理解；解析后再次 `redact_secrets_in_text`。三档都保存非空 essence 供 Phase 144 回放/评测，只有后续 worker 决定是否摄取。成功从 usage_metadata 提取 token/TTFT/duration；失败用 `parse_upstream_status`，两路都 best-effort `arecord_llm_usage(call_source="session_capture_eval", source="initiatives")`。发 sampling started/completed/failed，异常文本先脱敏且不记录正文。不得 import 或调用质量门、llm_grader、repo confidence、MemoryService/record_hook_writeback。</action>
  <verify>
    <automated>cd server &amp;&amp; uv run pytest tests/initiatives/test_session_capture_eval.py tests/test_model_usage_call_source.py::TestCallSourceEnum -q --tb=short &amp;&amp; uv run ruff check initiatives/services/session_capture_eval.py tests/initiatives/test_session_capture_eval.py</automated>
  </verify>
  <done>三档与 essence 变绿；所有非法/不可用路径可重试且不写 low；用量和 sampling 归因完整。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Provider response → evaluator | 外部 LLM 返回非可信文本/JSON |
| Evaluator → metrics/logs | token、错误和用户上下文进入观测面 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-143-01 | Information Disclosure | prompts/errors/logs | mitigate | 脱敏错误与精华；日志禁止正文 |
| T-143-02 | Tampering | structured output | mitigate | 严格 JSON 与闭集校验，非法输出失败 |
| T-143-04 | Elevation of Privilege | Memory boundary | mitigate | evaluator 静态零 Memory imports |
| T-143-SC | Tampering | package installs | accept | 复用既有 LangChain/Provider，无新包 |
</threat_model>

<verification>
运行 evaluator、CallSource 与 ruff；测试网络隔离下全部 mock provider，禁止真实 LLM。
</verification>

<success_criteria>
- EVAL-01/02 的独立结构化判断与 ModelUsageRecord 完整实现。
- 不猜模型、不默认 low、不写 ProjectMemory。
</success_criteria>

<output>
Create `.planning/phases/143-eval/143-04-SUMMARY.md` when done.
</output>
