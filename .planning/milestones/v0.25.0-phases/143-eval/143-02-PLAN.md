---
phase: 143-eval
plan: "02"
type: execute
wave: 0
depends_on: []
files_modified:
  - server/tests/initiatives/test_session_capture_eval_tasks.py
  - server/tests/mcp_tools/test_report_session_knowledge.py
  - server/tests/initiatives/test_capture_inv6_guard.py
  - server/tests/initiatives/test_capture_observability.py
  - .planning/phases/143-eval/143-VALIDATION.md
autonomous: true
requirements: [EVAL-01, EVAL-03, EVAL-04, EVAL-05, OBS-04]
must_haves:
  truths:
    - "Capture 持久化成功后才投递 durable eval，投递失败不撤销 accepted=true"
    - "eval 与 ingest 是独立任务；ingest 重试不会重跑 LLM"
    - "pending/failed 以及无 active job 的 stale evaluating/ingesting 可在重启后扫描恢复；evaluated_low/ingested/legacy evaluated 跳过"
    - "worker 显式 re-bind 发起用户，缺失时绑定 system"
  artifacts:
    - path: "server/tests/initiatives/test_session_capture_eval_tasks.py"
      provides: "双 durable 任务、重试、恢复与 actor rebind 契约"
    - path: "server/tests/mcp_tools/test_report_session_knowledge.py"
      provides: "persist-first accepted 与 fail-soft enqueue 契约"
    - path: "server/tests/initiatives/test_capture_inv6_guard.py"
      provides: "禁止旁路 Capture/Memory/background_runner 的静态红线"
  key_links:
    - from: "server/mcp_tools/views.py"
      to: "initiatives.services.session_capture_enqueue"
      via: "persist 返回后 durable eval 入队"
      pattern: "enqueue_session_capture_eval"
    - from: "server/durable/tasks_impl.py"
      to: "common.log_context.bind_task_context"
      via: "每个 worker 入口 actor rebind"
      pattern: "bind_task_context"
---

<objective>
建立 Phase 143 第二组 tracer-first Wave 0 RED 契约，覆盖持久化投递、双任务重试、恢复扫描、INV-6 与 sampling 观测。

Purpose: 让仅靠 background_runner、单任务重复收费、actor 丢失或 accepted 回退等耐久性缺陷在实现前可执行地失败。
Output: durable/MCP/守卫/观测测试及 Validation 任务映射。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/143-eval/143-CONTEXT.md
@.planning/phases/143-eval/143-RESEARCH.md
@.planning/phases/143-eval/143-PATTERNS.md
@.planning/phases/143-eval/143-VALIDATION.md
@server/durable/service.py
@server/durable/tasks_impl.py
@server/tests/mcp_tools/test_report_session_knowledge.py
@server/tests/initiatives/test_capture_inv6_guard.py
@server/tests/initiatives/test_capture_observability.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 建立双任务 durable 与恢复 RED 契约</name>
  <files>server/tests/initiatives/test_session_capture_eval_tasks.py</files>
  <action>新增任务测试，锁定 `QUEUE_KNOWLEDGE in ALL_QUEUES`、Procrastinate wrapper 与 in-process adapter 对 eval/ingest 两任务同名且 keyword-only/`**payload` 对齐。payload 仅含 capture_id、attempt 与 DurableTaskService 注入的 initiated_by_user_id，禁止 question/answer/distilled_essence/transcript。测试 eval claim 后调用 evaluator；low 结束且不 defer ingest；medium/high 只写 ingest_pending 并 defer 独立 ingest；ingest worker 只 `await knowledge.ingestion.ingest(IngestionRequest("session_capture", ...))`，不调用 evaluator 或 `aschedule_ingestion`。首次入队与 recovery 使用稳定 idempotency key `capture-eval:{id}` / `capture-ingest:{id}` 加同名 lock。worker 退避必须另开一条可调度 job：`lock` + `run_at`，且不得复用该稳定 idempotency key（省略或用 attempt-specific `capture-eval:{id}:retry:{attempt}`）；断言新 job 语义与双后端（tasks.py wrapper 与 handlers.py adapter）参数一致。覆盖 5 秒指数退避封顶 300 秒、自动尝试上限 6、eval/ingest 各自失败态、终态重放 no-op。恢复扫描：due pending/failed 以及 stale `evaluating`/`ingesting` 且 `has_active_by_key` 为假时重派；`test_recovery_redefers_stale_evaluating`、`test_recovery_redefers_stale_ingesting`、`test_recovery_skips_active_fresh_and_terminal`（terminal 含 evaluated_low、ingested、legacy evaluated）。worker resume：`test_eval_resume_when_evaluating_still_runs_llm` 在已是 evaluating 时仍跑 LLM 且不递增 eval_attempts。active/fresh 跳过，单行异常隔离。显式测试每个 worker 用 `bind_task_context(user_id=initiated_by_user_id or "system", source="durable", component="knowledge")`。</action>
  <verify>
    <automated>cd server &amp;&amp; uv run pytest tests/initiatives/test_session_capture_eval_tasks.py -q --tb=short</automated>
  </verify>
  <done>双任务、稳定入队 key、退避新 job、stale evaluating/ingesting 恢复、resume 不递增 attempt、纯标量 payload 与 actor rebind 均有 RED 契约。</done>
</task>

<task type="auto">
  <name>Task 2: 建立 persist-first MCP 与隔离守卫 RED 契约</name>
  <files>server/tests/mcp_tools/test_report_session_knowledge.py, server/tests/initiatives/test_capture_inv6_guard.py</files>
  <action>扩展 HTTP 测试：CaptureService.persist 返回后才调用 eval enqueue；enqueue 异常仍返回 200/accepted=true 且 Capture 行存在；幂等命中终态不重派，pending/failed 可由稳定 key 去重/恢复。测试不改变七键响应与旧 report_project_knowledge。扩展 INV-6 扫描：Capture 状态写仍只允许 capture_service.py；eval/enqueue/worker/normalizer 禁止 SessionCapture ORM 写、MemoryService、record_hook_writeback、aschedule_ingestion、background_runner、直接 KnowledgeEntity/Qdrant 写。保留 persist writer 自身不得调用 deferred sink：durable enqueue 必须在独立 helper/view 边界。</action>
  <verify>
    <automated>cd server &amp;&amp; uv run pytest tests/mcp_tools/test_report_session_knowledge.py -k "enqueue or reenqueue or accepted" tests/initiatives/test_capture_inv6_guard.py -x</automated>
  </verify>
  <done>accepted 与 durable 投递解耦、旧契约零回归、Capture/Memory/ingestion 旁路均被自动化阻断。</done>
</task>

<task type="auto">
  <name>Task 3: 建立 sampling 观测契约并同步 Validation</name>
  <files>server/tests/initiatives/test_capture_observability.py, .planning/phases/143-eval/143-VALIDATION.md</files>
  <action>将 Phase 141 的 `test_no_eval_sampling_events` 替换为 Phase 143 eval/normalize/ingest started/completed/failed 契约；断言 category=sampling、component=knowledge、capture_id、状态/tier、completed/failed duration_ms 与绑定用户存在，序列化日志不含 question/answer/distilled_essence/token，异常先脱敏，logger 自身失败不改变状态机结果。恢复扫描逐行只允许 debug，周期仅汇总一条 sampling。把 Validation 的 Task ID/Plan/Wave 映射到 143-01/143-02，标记 Wave 0 文件将由本两计划创建；保持 status=draft、wave_0_complete=false、nyquist_compliant=false，直到执行实际完成。</action>
  <verify>
    <automated>cd server &amp;&amp; uv run pytest tests/initiatives/test_capture_observability.py -x &amp;&amp; uv run ruff check tests/initiatives/test_session_capture_eval_tasks.py tests/mcp_tools/test_report_session_knowledge.py tests/initiatives/test_capture_inv6_guard.py tests/initiatives/test_capture_observability.py</automated>
  </verify>
  <done>sampling 生命周期、正文禁入日志、best-effort 与 Validation 映射均明确；目标实现缺失导致预期 RED。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP transaction → durable queue | 已接受 Capture 与异步投递跨持久化边界 |
| Durable payload → worker context | 审计标量跨进程并重新绑定用户 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-143-01 | Information Disclosure | durable payload/logs | mitigate | payload 精确键集与日志 sentinel 测试 |
| T-143-03 | Denial of Service | replay/recovery | mitigate | CAS、稳定 key/lock、有界退避与 active-job 守卫 |
| T-143-04 | Elevation of Privilege | Memory boundary | mitigate | INV-6 静态禁止 Memory 写入口 |
| T-143-SC | Tampering | package installs | accept | 零新增依赖 |
</threat_model>

<verification>
Wave 0 目标测试应因 Phase 143 生产符号/行为缺失而 RED，但可收集且 ruff 通过。
</verification>

<success_criteria>
- persist-first、双任务、恢复扫描、actor rebind 和 sampling 观测都有可执行 tracer。
- `background_runner` 不可成为 Session Capture 的投递路径。
</success_criteria>

<output>
Create `.planning/phases/143-eval/143-02-SUMMARY.md` when done.
</output>
