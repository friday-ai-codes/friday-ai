---
phase: 144-capture
plan: "02"
type: execute
wave: 1
depends_on: ["144-01"]
files_modified:
  - server/knowledge/vector_recall.py
  - server/knowledge/retrieval.py
  - server/knowledge/session_capture_retrieval.py
  - server/services/project_context_packer.py
  - server/tests/knowledge/test_vector_recall.py
  - server/tests/knowledge/test_retrieval.py
  - server/tests/knowledge/test_session_capture_retrieval.py
  - server/tests/services/test_project_context_packer.py
autonomous: true
requirements: [RECALL-01, RECALL-02]
must_haves:
  truths:
    - "search_similar 与 recall_similar_chunks 接受可选 source_kinds；默认 None 对旧调用零回归"
    - "会话检索唯一 helper 固定 DOCUMENT + include_document_kind + source_kinds=['session_capture'] 且 repository 必填、project 可选 AND（D-01）"
    - "pack_project_context RAG 层纳入当前项目 DOCUMENT（含 session_capture）且不排他过滤其它 document 源（RECALL-02 已决议）"
  artifacts:
    - path: "server/knowledge/vector_recall.py"
      provides: "Qdrant must 上的 source_kind MatchAny 与空列表短路"
      contains: "source_kinds"
    - path: "server/knowledge/session_capture_retrieval.py"
      provides: "MCP/Chat 共用 search_session_knowledge"
      exports: ["search_session_knowledge"]
    - path: "server/tests/knowledge/test_session_capture_retrieval.py"
      provides: "共享 helper 到真实 DeliveryKnowledgeSearchService 边界的精确参数契约"
    - path: "server/tests/knowledge/test_retrieval.py"
      provides: "search_similar 到 recall_similar_chunks 的 source_kinds 透传契约"
    - path: "server/services/project_context_packer.py"
      provides: "_layer_rag 收窄 project_ids"
  key_links:
    - from: "server/knowledge/retrieval.py"
      to: "knowledge.vector_recall.recall_similar_chunks"
      via: "source_kinds 原样透传"
      pattern: "source_kinds=source_kinds"
    - from: "server/knowledge/session_capture_retrieval.py"
      to: "DeliveryKnowledgeSearchService.search_similar"
      via: "三件套 filter"
      pattern: "source_kinds=\\[\"session_capture\"\\]"
    - from: "server/services/project_context_packer.py"
      to: "DeliveryKnowledgeSearchService.search_similar"
      via: "project_ids 当前项目"
      pattern: "project_ids="
---

<objective>
落地向量层显式 source_kinds 与会话检索共享 helper，并让 pack_project_context 在不丢掉既有 DOCUMENT 源的前提下纳入 session_capture（D-01，RECALL-01/02）。

Purpose: 专用会话检索能闭集过滤；通用 packer/交付检索保持 inclusion 而非 exclusive。
Output: vector_recall/retrieval 透传、session_capture_retrieval helper、packer _layer_rag 补 project_ids。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/144-capture/144-RESEARCH.md
@.planning/phases/144-capture/144-PATTERNS.md
@server/knowledge/vector_recall.py
@server/knowledge/retrieval.py
@server/services/project_context_packer.py
@server/knowledge/access_scope.py
@server/tests/knowledge/test_vector_recall.py
@server/tests/knowledge/test_retrieval.py
@server/tests/services/test_project_context_packer.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 为 recall 与 search_similar 增加可选 source_kinds</name>
  <files>server/knowledge/vector_recall.py, server/knowledge/retrieval.py, server/tests/knowledge/test_retrieval.py</files>
  <behavior>
    - source_kinds=None：must 无 source_kind 条件（旧调用零回归）
    - 非空列表：demand 与 code 两路 filter 均含 source_kind MatchAny
    - source_kinds=[]：recall_similar_chunks 在 embed_query 之前返回 []
    - 不放宽 if not allowed_projects: return []
  </behavior>
  <action>在 _build_knowledge_must_filter 于 entity_kinds 条件之后追加：若 source_kinds 非空则 FieldCondition(key="source_kind", match=MatchAny(any=list(source_kinds)))。recall_similar_chunks 与 search_similar 增加 source_kinds: list[str] | None = None 并透传到两路 _build_knowledge_must_filter。空列表与「两分路 kinds 皆空」一样在调用 embed_query 前 return []。新增 tests/knowledge/test_retrieval.py，调用真实 DeliveryKnowledgeSearchService.search_similar（只 monkeypatch access_scope、recall_similar_chunks 与后续依赖边界，不替换 search_similar 本体），捕获并精确断言 source_kinds=["session_capture"] 原样进入 recall_similar_chunks；另测默认 None 透传且旧调用零回归。不得默认过滤 session_capture；knowledge/api/views.py 与 SearchDeliveryKnowledgeView 不传该参数。保留 project_id 空串 ∧ repository_id∈allowed 逃生支。禁止 hydrate 后丢弃 source_kind。不新增依赖、EntityKind、collection。</action>
  <verify>
    <automated>cd server && uv run pytest tests/knowledge/test_vector_recall.py tests/knowledge/test_retrieval.py -x --tb=short</automated>
  </verify>
  <done>source_kinds MatchAny / None 零回归 / 空列表短路与 search_similar→recall 精确透传断言全绿。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 共享 search_session_knowledge helper 与 packer inclusion</name>
  <files>server/knowledge/session_capture_retrieval.py, server/services/project_context_packer.py, server/tests/knowledge/test_session_capture_retrieval.py</files>
  <behavior>
    - helper 调用 search_similar(repository_ids=[rid], project_ids=[pid] 或 None, entity_kinds=["document"], include_document_kind=True, source_kinds=["session_capture"])
    - helper 不写 RetrievalTrace
    - _layer_rag 传 project_ids=[str(project_id)]、include_document_kind=True、不传 exclusive source_kinds=["session_capture"]（已决议 inclusion）
  </behavior>
  <action>新增 knowledge/session_capture_retrieval.py，导出 async search_session_knowledge(query, *, user, repository_id, project_id=None, top_k=5) 并只委托 DeliveryKnowledgeSearchService。新增 tests/knowledge/test_session_capture_retrieval.py，直接调用真实 helper，仅 monkeypatch DeliveryKnowledgeSearchService.search_similar 边界，精确断言 query、user、top_k、repository_ids=[rid]、project_ids=[pid] 或 None、entity_kinds=["document"]、include_document_kind=True、source_kinds=["session_capture"] 的完整 kwargs 集，禁止只在 MCP view mock helper。MCP/Chat 后续计划必须只调此函数。_layer_rag 补 project_ids=[str(project_id)]；source_kinds 保持默认 None 以便 session_capture 与 project_doc/memory/artifact/feishu_document 等同路召回。禁止把 packer 改成只搜 session_capture。packer 既有 RetrievalTrace 可继续含 query（那是项目上下文链，不是会话专用工具）。无新 LLM call_source。</action>
  <verify>
    <automated>cd server && uv run pytest tests/knowledge/test_session_capture_retrieval.py tests/knowledge/test_retrieval.py tests/services/test_project_context_packer.py tests/knowledge/test_vector_recall.py -x --tb=short</automated>
  </verify>
  <done>helper 的完整 search_similar kwargs、search_similar→recall source_kinds 与 packer inclusion 断言全绿；未改 MCP urls/snapshot。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| search_similar 调用方 → Qdrant | source_kinds 必须进 must，否则跨 kind 泄漏计分面 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-144-03 | Information disclosure | _build_knowledge_must_filter | mitigate | MatchAny 进 must；空 allowed_projects 仍 return [] |
| T-144-SC | Tampering | pip/npm | accept | 无新包 |
</threat_model>

<verification>
vector_recall 与 packer 测试全绿；ruff 触及的生产文件。
</verification>

<success_criteria>
专用 helper 具备闭集三件套；通用 RAG 未变成 session_capture-only。
</success_criteria>

<output>
Create `.planning/phases/144-capture/144-02-SUMMARY.md` when done
</output>
