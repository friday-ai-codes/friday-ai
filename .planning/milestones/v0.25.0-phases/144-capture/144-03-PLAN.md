---
phase: 144-capture
plan: "03"
type: execute
wave: 1
depends_on: ["144-01"]
files_modified:
  - server/services/branch_parsing.py
  - server/mcp_tools/views.py
  - server/mcp_tools/serializers.py
  - server/tests/mcp_tools/test_lookup_project_by_branch.py
  - server/tests/mcp_tools/test_report_session_knowledge.py
  - server/tests/mcp_tools/test_schema_snapshot.py
autonomous: true
requirements: [RECALL-04]
must_haves:
  truths:
    - "main/master/develop 与仓库 default_branch 在仅有唯一 RepoAssociation 时 lookup matched=false、context 空、不 pack（D-03）"
    - "feat 人工分支第三源与默认分支上的 ProjectBranch/work_item 仍可 matched=true"
    - "未传 project_id 的 report_session_knowledge 在默认分支+唯一 association 下仍 accepted 且不绑项目 FK"
  artifacts:
    - path: "server/services/branch_parsing.py"
      provides: "is_default_branch 纯函数"
      exports: ["parse_work_item_id_from_branch", "is_default_branch"]
    - path: "server/mcp_tools/views.py"
      provides: "LookupProjectByBranchView 第三源默认分支跳过"
  key_links:
    - from: "LookupProjectByBranchView.post"
      to: "services.branch_parsing.is_default_branch"
      via: "第三源块在 pack 之前判定"
      pattern: "is_default_branch"
    - from: "LookupProjectByBranchView.post"
      to: "pack_project_context"
      via: "默认分支第三源 skip 时不得调用"
      pattern: "pack_project_context"
---

<objective>
修复 lookup 第三源在默认分支上误 matched 并打包上下文，同时用写路径回归锁住 Capture 仓库优先、不因仓关联绑项目（D-03，RECALL-04）。已决议：unmatched 响应增加 binding_source=repo_association_skipped_default_branch。

Purpose: 阻止 IDE 把错误 project_id 回传 report_session_knowledge。
Output: is_default_branch helper、Lookup 第三源守卫、lookup snapshot 响应键、写路径回归绿。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/144-capture/144-CONTEXT.md
@.planning/phases/144-capture/144-RESEARCH.md
@server/mcp_tools/views.py
@server/services/branch_parsing.py
@server/repositories/models.py
@server/tests/mcp_tools/test_lookup_project_by_branch.py
@server/tests/mcp_tools/test_report_session_knowledge.py
@server/initiatives/services/capture_service.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 增加 is_default_branch 并守卫 lookup 第三源</name>
  <files>server/services/branch_parsing.py, server/mcp_tools/views.py, server/mcp_tools/serializers.py</files>
  <behavior>
    - 精确匹配 main/master/develop 与非空 default_branch，大小写敏感，空名 False
    - 仅当 work_item 与 ProjectBranch 都未命中且传入 repository_id 时进入第三源
    - 默认分支：candidates 来自 association、matched=false、context=""、included_layers=[]、binding_source=repo_association_skipped_default_branch、不调用 pack_project_context
    - 非默认分支保持唯一 association → matched=true
  </behavior>
  <action>在 branch_parsing.py 增加 _WELL_KNOWN_DEFAULT_BRANCHES frozenset 与 is_default_branch(branch_name, default_branch=None)，更新 __all__。LookupProjectByBranchView 第三源：afirst Repository 取 default_branch；若 is_default_branch(branch_name, repo.default_branch) 则不要把 association 项目写入 merged（避免 len==1 走 pack），只填 candidates 与 binding_source，context 保持 ""。非默认分支维持现有 for p in association_projects: merged.setdefault。output_data 始终包含 binding_source 键（默认 ""，命中时写入既有 work_item/branch_binding/both/repo_association）。TOOL_SCHEMA_SNAPSHOT lookup_project_by_branch.response 加入 binding_source。npm inputSchema 不改。禁止因 skip 拒绝 Capture 或改 ReportSessionKnowledgeView 挂钩。禁止默认分支禁用前两源。</action>
  <verify>
    <automated>cd server && uv run pytest tests/mcp_tools/test_lookup_project_by_branch.py -x --tb=short</automated>
  </verify>
  <done>默认分支 unmatched 与 feat 第三源回归、ProjectBranch/work_item 在 main 上仍命中；skip 不 pack。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 锁定 Capture 写路径不因默认分支仓关联绑项目</name>
  <files>server/tests/mcp_tools/test_report_session_knowledge.py</files>
  <behavior>
    - branch_name=main + 真实 repository_id + 唯一 RepoAssociation、未传 project_id → accepted=true、capture.repository_id 有值、project_id is None、reason 不是 branch_unresolved
    - 写路径不调用 lookup_project_by_branch / pack_project_context
  </behavior>
  <action>实现 144-01 已写的 report 回归（若 Wave 0 已有测试则只确保生产已满足，必要时补 monkeypatch 断言 ReportSessionKnowledgeView 不 await LookupProjectByBranchView 或 pack_project_context）。不要为了让 lookup 跳过而把 persist 改成清空仓库 FK。保留 test_default_branch_does_not_mean_rejected。同步确认 test_schema_snapshot 中 lookup 响应键含 binding_source 与生产 snapshot 一致；完整 snapshot 此时仍严格是现有 52 工具，不得提前加入 search_session_knowledge 或 get_session_capture。</action>
  <verify>
    <automated>cd server && uv run pytest tests/mcp_tools/test_report_session_knowledge.py tests/mcp_tools/test_lookup_project_by_branch.py tests/mcp_tools/test_schema_snapshot.py::test_registered_tools_match_snapshot -x --tb=short</automated>
  </verify>
  <done>写路径默认分支不绑项目；lookup snapshot 含 binding_source；report 仍 accepted。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 分支名 + repository_id → 项目匹配 | 弱证据不得自动注入项目上下文 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-144-05 | Tampering | LookupProjectByBranchView 第三源 | mitigate | 默认分支 skip + 不 pack |
| T-144-SC | Tampering | pip/npm | accept | 无新包 |
</threat_model>

<verification>
lookup 与 report 相关 pytest 绿；registered 守卫在 52 工具时通过，lookup `binding_source` 字面契约保留。完整 snapshot 的 search 增量由 Plan 04 处理，get 增量由 Plan 05 处理。
</verification>

<success_criteria>
RECALL-04 读侧与写侧回归同时成立；feat 第三源零回归。
</success_criteria>

<output>
Create `.planning/phases/144-capture/144-03-SUMMARY.md` when done
</output>
