---
phase: 141-capture
plan: 03
type: execute
wave: 2
depends_on:
  - "141-02"
files_modified:
  - server/initiatives/services/capture_service.py
  - server/services/git_url.py
  - server/tests/initiatives/test_capture_service.py
autonomous: true
requirements:
  - STORE-03
  - STORE-04
must_haves:
  truths:
    - "显式 repository_id 优先于 git_url；SSH/HTTPS 归一化后可挂钩未软删仓库"
    - "解析失败、多命中、未授权时仍创建 Capture 行，FK 为空或仅合法侧，link_reason 为闭集显式值"
    - "同 initiated_by_user_id+session_id+question_hash 重复提交返回既有行且不改 answer"
    - "挂钩授权使用 knowledge.access_scope，不使用 RepositoryPermission 宽权限，也不调用 _resolve_report_project_id"
  artifacts:
    - path: server/initiatives/services/capture_service.py
      provides: "完整挂钩状态机 + IntegrityError 幂等回读"
      contains: "resolve_allowed_repository_ids|repo_unresolved|IntegrityError"
    - path: server/services/git_url.py
      provides: "normalize_git_url 供挂钩变体匹配"
    - path: server/tests/initiatives/test_capture_service.py
      provides: "link / unauthorized / idempotent 行为绿"
  key_links:
    - from: server/initiatives/services/capture_service.py
      to: knowledge.access_scope.resolve_allowed_repository_ids
      via: "决定是否写 repository FK"
      pattern: "resolve_allowed_repository_ids"
    - from: server/initiatives/services/capture_service.py
      to: knowledge.access_scope.resolve_allowed_project_ids
      via: "决定是否写 project FK"
      pattern: "resolve_allowed_project_ids"
    - from: server/initiatives/services/capture_service.py
      to: server/services/git_url.py
      via: "URL 变体 filter 匹配 Repository.git_url"
      pattern: "normalize_git_url"
---

<objective>
完成仓库/项目挂钩状态机与幂等 first-write-wins，使 STORE-04 与 STORE-03 幂等用例变绿：任何解析/授权失败仍落 Capture，并写明确 link_reason（D-05/D-07/D-08）。

Purpose: 消灭 `branch_unresolved` 跳过写库反模式；挂钩失败≠未收。
Output: CaptureService 完整 linking + idempotent 行为测试全绿。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/141-capture/141-CONTEXT.md
@.planning/phases/141-capture/141-RESEARCH.md
@.planning/phases/141-capture/141-PATTERNS.md
@.planning/phases/141-capture/141-02-PLAN.md
@server/knowledge/access_scope.py
@server/repositories/permissions.py
@server/mcp_tools/views.py

## Locked decisions

D-05, D-07, D-08, D-15. Reason 闭集（RESEARCH）：`linked`、`linked_with_project`、`repo_unresolved`、`repo_ambiguous`、`repo_unauthorized`、`project_unauthorized`、`project_unresolved`、`project_repo_mismatch`、`unanchored`、`project_only`。

对外防枚举（RESEARCH 推荐）：不存在 vs 未授权对外可折叠为 `repo_unresolved`，内部日志可分；选定一种并在测试钉死。推荐：scope 外用 `repo_unauthorized`，不存在/软删/非法 UUID 用 `repo_unresolved`（与 RESEARCH 主表一致）；若实现者折叠，测试改断言但不得丢行。

无仓时授权项目可绑，`link_reason=project_only`（RESEARCH A2/Open Q2）。

## Worktree safety

Do not expand `report_project_knowledge`. Do not import `_resolve_report_project_id` or `lookup_project_by_branch`. Do not touch unrelated dirty worktree files. No frontend.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 仓库/项目挂钩状态机（STORE-04）</name>
  <files>server/initiatives/services/capture_service.py, server/services/git_url.py, server/tests/initiatives/test_capture_service.py</files>
  <read_first>
    - server/initiatives/services/capture_service.py
    - server/knowledge/access_scope.py
    - server/repositories/serializers.py
    - server/services/git_url.py
    - server/tests/initiatives/test_capture_service.py
  </read_first>
  <behavior>
    - 解析序：显式 repository_id（合法 UUID + is_deleted=False）→ 否则 normalize_git_url(git_url) 变体 filter；多命中 → repo_ambiguous 不绑 FK；零命中 → repo_unresolved（D-07）。
    - test_repo_ambiguous_does_not_bind_fk: 多命中不绑 repository FK，仍有行，link_reason=repo_ambiguous。
    - test_project_only_without_repo: 无仓有授权项目 → 绑项目，link_reason=project_only。
    - test_project_repo_mismatch_binds_repo_only: 仓与项目无关系 → 只绑仓，link_reason=project_repo_mismatch。
    - 显式 id 优先于同时提供的 git_url。
    - SSH `git@host:group/repo.git` 可挂到已存 HTTPS 等价仓。
    - 仓不在 resolve_allowed_repository_ids → FK null + repo_unauthorized（或折叠策略），仍 create（D-08）。
    - 可选 project_id：存在性 + resolve_allowed_project_ids / ProjectMember；未授权 → project_unauthorized；与仓无 Space/RepoAssociation → project_repo_mismatch 只绑仓；无仓有授权项目 → project_only（D-02/D-07）。
    - branch_name 只存字段，不参与项目定位（禁止默认分支第三源）。
    - 禁止 RepositoryPermission；禁止 validate_https_git_url 抛错阻断落库；禁止全表扫描 for-repo 循环（用 git_url__in 变体）。
  </behavior>
  <action>
在 CaptureService 内实现挂钩 helper（可私有函数）。夹具：Space.repositories.add(repo) 以便 access_scope 可见。使 Wave 0 硬性用例 `test_repo_ambiguous_does_not_bind_fk`、`test_project_only_without_repo`、`test_project_repo_mismatch_binds_repo_only` 以及其余 link/unauthorized 用例变绿。不要改 mcp_tools/views.py。不要新增 Python 依赖。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/initiatives/test_capture_service.py::test_repo_ambiguous_does_not_bind_fk tests/initiatives/test_capture_service.py::test_project_only_without_repo tests/initiatives/test_capture_service.py::test_project_repo_mismatch_binds_repo_only -x -q && uv run pytest tests/initiatives/test_capture_service.py -k "link or unauthorized" -x -q</automated>
  </verify>
  <done>挂钩成功与失败路径均落行；FK 授权正确；link_reason 显式；无 MCP skip 语义。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 幂等 first-write-wins（STORE-03）</name>
  <files>server/initiatives/services/capture_service.py, server/tests/initiatives/test_capture_service.py</files>
  <read_first>
    - server/initiatives/services/mr_service.py
    - server/initiatives/services/capture_service.py
    - server/tests/initiatives/test_capture_service.py
  </read_first>
  <behavior>
    - UniqueConstraint 命中：IntegrityError → get 既有行 → 返回同 id，idempotent_hit=True，created=False，answer 保持第一次（D-05）。
    - 禁止 update_or_create / 对既有行 save 新 answer。
    - 缺 session_id 两次同问题：因 session_id=`unspecified` 参与唯一键而去重（RESEARCH A1 locked）；`test_missing_session_id_uses_unspecified` 须保持绿（Plan 02 已实现 D-06）。
    - 并发双插由 DB 约束兜底，应用层 IntegrityError 回读。
  </behavior>
  <action>
加固 atomic create + IntegrityError 回读路径；补齐/确保 `test_idempotent_returns_existing` 与 `test_missing_session_id_uses_unspecified` 绿。确认 capture_service 仍无 deferred sinks。回归跑核心 STORE 子集，确认 141-02 用例未回退。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/initiatives/test_capture_service.py -x -q</automated>
  </verify>
  <done>test_capture_service.py 全绿；幂等不改答案；挂钩与无锚落库共存。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| actor → repository/project FK | 未授权关系不得写入；行仍归属 initiated_by_user_id |
| git_url string → Repository match | 归一化匹配；多命中不绑 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-141-03 | Elevation of Privilege | repository/project FK | mitigate | resolve_allowed_* + unauthorized reasons；不绑 FK（D-08） |
| T-141-08 | Information Disclosure | repo id enumeration | mitigate | 软删/非法 UUID → repo_unresolved；勿泄漏内部存在性细节到业务异常 |
| T-141-04 | Tampering | duplicate captures | mitigate | UniqueConstraint + IntegrityError first-write-wins（D-05） |
| T-141-09 | Tampering | branch_unresolved skip | mitigate | 禁止 _resolve_report_project_id；失败仍 create（D-15） |
| T-141-SC | Tampering | package installs | accept | 无新依赖 |
</threat_model>

<verification>
`test_capture_service.py` 全绿；源码无 `_resolve_report_project_id` / `lookup_project_by_branch` / `RepositoryPermission` 用于挂钩。
</verification>

<success_criteria>
- STORE-04 挂钩与失败落库成立
- STORE-03 幂等 first-write-wins 成立
- 未扩展 report_project_knowledge；无 UI
</success_criteria>

<output>
Create `.planning/phases/141-capture/141-03-SUMMARY.md` when done
</output>
