---
phase: 141-capture
plan: 04
type: execute
wave: 3
depends_on:
  - "141-03"
files_modified:
  - server/initiatives/services/capture_service.py
  - server/tests/initiatives/test_capture_observability.py
  - server/tests/initiatives/test_capture_service.py
  - .planning/observability/LOGGING-SPEC.md
  - .planning/phases/141-capture/141-VALIDATION.md
autonomous: true
requirements:
  - STORE-01
  - OBS-01
  - OBS-02
must_haves:
  truths:
    - "成功 persist 发出 session_capture_persist_started 然后 completed；completed 含 duration_ms、category=caller、component=knowledge、initiated_by_user_id"
    - "业务失败发出 started+failed（含 duration_ms 与脱敏 error），异常原样抛出"
    - "日志与返回审计字段不含 question/answer/git_url/token 正文；密钥 sentinel 不出现在 capture_logs JSON"
    - "logger 抛错仍返回 Capture；本阶段无 eval/ingest sampling 事件"
    - "persist 不写 ProjectMemory / Interaction Ledger 问答正文；report_project_knowledge 回归零变化"
  artifacts:
    - path: server/initiatives/services/capture_service.py
      provides: "best-effort caller 三事件生命周期"
      contains: "session_capture_persist_completed"
    - path: server/tests/initiatives/test_capture_observability.py
      provides: "OBS-01/02 自动化验收"
    - path: .planning/observability/LOGGING-SPEC.md
      provides: "§10 登记 session_capture_persist_*（不改 CallSource）"
  key_links:
    - from: server/initiatives/services/capture_service.py
      to: structlog logger
      via: "每处 logger 调用 except Exception: pass"
      pattern: "session_capture_persist_|except Exception"
    - from: server/initiatives/services/capture_service.py
      to: common.logging.redact_secrets_in_text
      via: "failed 事件 error= 脱敏"
      pattern: "redact_secrets_in_text"
---

<objective>
收口 Capture 持久化可观测性与账本分离回归：caller 生命周期、日志无正文、观测 best-effort、LOGGING-SPEC 登记，以及 Memory/Ledger/`report_project_knowledge` 零回归（D-13/D-14/D-15，OBS-01/02，STORE-01）。

Purpose: 可归因、可排障且永不因日志反噬丢 Capture；明确不把问答写入 Ledger。
Output: 观测测试全绿 + LOGGING-SPEC 事件目录更新 + VALIDATION nyquist 签字准备。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/141-capture/141-CONTEXT.md
@.planning/phases/141-capture/141-RESEARCH.md
@.planning/phases/141-capture/141-PATTERNS.md
@.planning/observability/LOGGING-SPEC.md
@server/services/code_graph/query_service.py
@server/tests/services/code_graph/test_query_observability.py
@server/tests/mcp_tools/test_report_project_knowledge.py

## Locked decisions

D-13, D-14, D-15, D-01, D-09. Do **not** add CallSource `session_capture_eval`（Phase 143）。Do **not** write InteractionRun with Q/A keys（RESEARCH A5：141 完全不建 InteractionRun）。

completed kv 白名单：capture_id、link_reason、repository_bound、project_bound、session_present（bool）、idempotent_hit、initiated_by_user_id、duration_ms、category、component。禁止 question/answer/git_url/token/question[:n]。

## Worktree safety

Regression 只跑既有 `test_report_project_knowledge.py`，不要「顺手」改工具实现或扩成 Capture 入口。不要 revert 无关脏文件 `test_skills_snapshot_guard.py`。无 frontend。
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: caller 生命周期日志 best-effort（OBS-01/02）</name>
  <files>server/initiatives/services/capture_service.py, server/tests/initiatives/test_capture_observability.py</files>
  <read_first>
    - server/services/code_graph/query_service.py
    - server/tests/services/code_graph/test_query_observability.py
    - server/initiatives/services/capture_service.py
    - server/tests/initiatives/test_capture_observability.py
  </read_first>
  <behavior>
    - 顺序：started（吞）→ 业务 create（不吞）→ completed（吞）；create 失败 → failed（吞，error 经 redact_secrets_in_text）→ re-raise（D-13/D-14）。
    - category=caller；component=knowledge（不是 initiatives）。
    - 成功无 failed；本阶段断言无 session_capture_eval_* / ingest sampling 事件名（D-09）。
    - sentinel 问答与 sk-ant-... 不出现在 json.dumps(capture_logs)；DB 正文已 REDACTED（OBS-02）。
    - monkeypatch logger.info/warning 抛错时 persist 仍返回 Capture（T-141-05）。
  </behavior>
  <action>
按 query_service 模式包住每一处 logger 调用。使 `tests/initiatives/test_capture_observability.py` 全绿。若 141-02 仅有 stub 日志，替换为完整三事件与白名单 kv。不要记录问答截断字段。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/initiatives/test_capture_observability.py -x -q</automated>
  </verify>
  <done>OBS-01/02 观测测试全绿；日志无正文；logger 失败不丢行。</done>
</task>

<task type="auto">
  <name>Task 2: LOGGING-SPEC 登记 + 分离账本回归 + VALIDATION 收口</name>
  <files>.planning/observability/LOGGING-SPEC.md, server/tests/initiatives/test_capture_service.py, .planning/phases/141-capture/141-VALIDATION.md</files>
  <read_first>
    - .planning/observability/LOGGING-SPEC.md
    - server/tests/initiatives/test_capture_service.py
    - .planning/phases/141-capture/141-VALIDATION.md
  </read_first>
  <behavior>
    - §10 事件目录追加 session_capture_persist_started/completed/failed（component=knowledge, category=caller）；§4.1 CallSource **不**新增 session_capture_eval。
    - test_persist_does_not_write_memory_or_ledger 保持绿；跑 report_project_knowledge 回归确认旧 skip 语义不变（D-15/STORE-01）。
    - VALIDATION：wave_0_complete=true；nyquist_compliant=true；Per-Task Verification Map 状态可标 ✅；Approval 仍可由 verify-work 最终确认。
  </behavior>
  <action>
更新 LOGGING-SPEC §10（及如需的 §5 注释交叉引用）。跑 Capture 三文件 + memory INV-6/service 抽样 + report_project_knowledge 回归。更新 141-VALIDATION.md frontmatter 与 Wave 0 勾选。禁止修改 `test_skills_snapshot_guard.py` 及其他无关脏文件。禁止 MCP/npm/前端改动。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/initiatives/test_capture_service.py tests/initiatives/test_capture_inv6_guard.py tests/initiatives/test_capture_observability.py tests/initiatives/test_memory_inv6_guard.py tests/mcp_tools/test_report_project_knowledge.py -q</automated>
  </verify>
  <done>LOGGING-SPEC 已登记三事件；Capture 全套与 report_project_knowledge 回归绿；VALIDATION Wave 0/nyquist 标记更新。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CaptureService → structlog / future Ledger | 仅元数据；禁止问答键 |
| Observability failure → business path | 观测异常不得阻断 create |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-141-01 | Information Disclosure | logs | mitigate | kv 白名单 + sentinel 测试（D-14） |
| T-141-05 | Denial of Service | logger exceptions | mitigate | except Exception: pass around logs（D-14） |
| T-141-10 | Information Disclosure | Interaction Ledger | mitigate | 141 不创建 InteractionRun；测试断言无 arecord_tool_call（D-01） |
| T-141-11 | Tampering | report_project_knowledge | mitigate | 回归测试锁定旧行为；不扩工具（D-15） |
| T-141-SC | Tampering | package installs | accept | 无新依赖 |
</threat_model>

<verification>
Capture 三测试模块 + memory INV-6 + report_project_knowledge 全绿；LOGGING-SPEC 含三事件名；无 CallSource 误加。
</verification>

<success_criteria>
- OBS-01/02 可观测契约成立
- STORE-01 账本分离与 MCP 旧工具零回归
- Phase 141 无前端、无 MCP 新工具、无 eval/ingest
</success_criteria>

<output>
Create `.planning/phases/141-capture/141-04-SUMMARY.md` when done
</output>
