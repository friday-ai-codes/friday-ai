---
phase: 114-ai
plan: 03
type: execute
wave: 4
depends_on: ["114-02", "114-04"]
files_modified:
  - server/services/process_runtime/blueprint_review.py
  - server/services/process_runtime/builtin_processes.py
  - server/services/process_runtime/blueprint_resume.py
  - server/services/process_runtime/entrypoint.py
  - server/delivery/services/event_taxonomy.py
  - server/system/models.py
  - server/tests/services/process_runtime/test_blueprint_review_stage.py
  - server/tests/services/process_runtime/test_blueprint_process_graph.py
  - server/tests/services/process_runtime/test_blueprint_merge_gate.py
autonomous: true
requirements: [FLOW-07]
prohibitions:
  - "禁止修改 §13.2 冻结面（`git diff --name-only` 零命中）：`process_runtime/{decompose_segments,research_adapter,architect_merge_adapter,clarify_adapter,render,resume}.py`、delivery 侧 `merged_plan.py`、`repositories/services/charter_service.py`；🔒 `builtin_processes.py` 的 `_TECHNICAL_PLAN_STAGES`（`:208-301`，含 `:246` 的 `merge.merged: STAGE_DONE`）与 `_ECHO_STAGES` **一字不动**"
  - "禁止修改 `task/core/knowledge_tools.py` 公共 handler 工厂（本相位不碰 `task/`）"
  - "🔒 `builtin_processes.py` **只加注册与新 handler**：允许的改动上界仅三处 —— ① 新增 `async def _h_bp_ai_review` ② 新增 `\"ai_review\": StageDef(...)` 一项 ③ 蓝图链 `merge.transitions[\"merged\"]` 一行值 `STAGE_DONE → \"ai_review\"`（外加新增 `_abp_mark_ai_reviewing` helper）。前九个 `_h_bp_*` 的函数体与 transitions **一字不动**；`rg -c \"^register_process_type\\(\"` 必须保持 **3**；**不新增 `artifact_type`**（仍是 `technical_plan`）"
  - "🔒 `blueprint_resume.py` **仅限 `_STAGE_BLUEPRINT_STATUS` 映射表追加一行**（`\"ai_review\": \"ai_reviewing\",  # == BlueprintStatus.AI_REVIEWING`，值用**字面量**——本模块 Django import 全在函数内，模块级表拿不到枚举）+ 追加一行注释。**删除行上界 = 0**：`git diff server/services/process_runtime/blueprint_resume.py | rg \"^-[^-]\"` 必须为空。不改 `_resolve_stage_status` / `_amap_blueprint_status` / `adrive_blueprint_session_to_pause_or_terminal` / `aresume_after_gate_action` / `aresume_blueprint_session` 的任何行为，不在本文件加新函数，不改 `max_steps=20`。本文件**只跑 `ruff check`，不跑 `ruff format`**"
  - "⛔ **`record_answer` 禁用于 finding 留痕**：findings 的中间留痕（「第 N 轮仍存在」）一律用 114-01 的 `append_note`；`rg -n \"record_answer\" server/services/process_runtime/blueprint_review.py` 必须**零命中**。理由（112 教训后果链）：`record_answer` 在同事务把 `open` 推到 `answered`（`_record_answer_sync:536-540`）→ `ahas_open_blocking_threads`（只认 `open`）判为无门 → confirm 守卫放行（人审能通过带未决 BLOCKER 的蓝图）→ `blueprint_resume` 的 pause 判据失守，续驱一路 advance 到 `max_steps` 后落 FAILED"
  - "禁止 adapter / handler 直写 ORM（INV-6）：线程创建/留痕/收尾一律经 `BlueprintLifecycleService`；`rg -n \"BlueprintThread.objects.(create|acreate)\" server/services/process_runtime/blueprint_review.py` 零命中；事件一律经 `session_service.aemit_event`，禁止 `ConvergenceSessionEvent.objects.acreate(` 与 `._emit_event(`（`test_blueprint_process_graph` 的 MN-05 源码扫描守护）"
  - "禁止修改 `ConvergenceSessionEvent` 既有事件类型与 payload 字段：`event_taxonomy.py` **只允许纯追加** `blueprint.review.*` 三个常量（§13.2 第 3 条）；`git diff server/delivery/services/event_taxonomy.py | rg \"^-[^-]\"` 必须为空"
  - "禁止给任何模型加字段 / 建 migration：`makemigrations --check --dry-run` 退出码必须为 0（`SettingKeys` 是纯常量类，追加一个键零 migration）"
  - "禁止修改 `server/system/settings_service.py` 既有 getter（读配置一律经既有 `aget_json_setting`）"
  - "禁止把审查轮次塞进 `stage_state[\"merge\"]` 桶：`blueprint_merge._build_stage_state`（`:2306`）整桶读改写（`{**state, STAGE_STATE_KEY: bucket}`），塞进去的键会在下次 merge 打回时被覆盖 → 计数归零 → **无限循环**。一律用 114-02 的 `STAGE_STATE_KEY = \"ai_review\"`（实测未被占用）"
  - "禁止修改 `blueprint_schema.py` / `blueprint_quality.py` / `blueprint_anchor.py` / `blueprint_merge.py` / `blueprint_reconcile.py` / `blueprint_confirm_gate.py` / `blueprint_spec_gate.py` / `blueprint_repo_plan.py` / `blueprint_lifecycle_service.py` / `blueprint_charter_match.py` / `blueprint_reflow.py` / `blueprint_block_edit.py`（受限面，本 plan 只读消费；114-01/114-04 已做完 lifecycle service 与回灌/编辑/保护模块的全部改动）"
  - "🔒 **B3 定夺：`blueprint_merge.py` 一行不改**——人工块保护挂在本 plan 的 `ai_review` 入口（调 114-04 的 `arestore_human_blocks`），**不在 merge 里做**；`git diff server/services/process_runtime/blueprint_merge.py` 必须为空"
  - "禁止在本 plan 重写 B1/B3 的被调方：`aapply_thread_answers` / `arestore_human_blocks` / `acollect_human_block_ids` / `areanchor_threads` / `ablock_section_writer` 一律**从 114-04 导入使用**（函数内 lazy import），禁止在 `blueprint_review.py` 里复制一份实现"
  - "禁止给任何模型加字段 / 建 migration（**本 plan 内**）：全相位唯一的字段追加（`BlueprintThread.last_reminded_at`）与唯一一条 migration 由 **114-05** 承载"
  - "禁止新增 `CallSource` 枚举值（`agents/call_source.py` 零改动）；禁止改 `blueprint_review.py` 中 114-02 已交付的六类纯函数与 `agoal_backward_review`（本 plan 只在其后追加 adapter 节）"
  - "禁止 `ai_review` 的 `transitions` 含 `failed` 出边（与 `merge` 同纪律：超界是「待人审」不是「流程失败」）；禁止超界落 `STAGE_FAILED`；禁止 `needs_clarification` self-loop 前不 ensure 阻塞线程"
  - "禁止 findings 正文进 `stage_state` / 进事件 payload / 进日志（`DESIGN.md:472`：`stage_state` 只存 id / 计数 / 小摘要，单字段 < 2KB）"
must_haves:
  truths:
    - "`BlueprintReviewAdapter.review(session) -> dict` 返回**恒定键形状**，`review_status ∈ {passed, retry, exhausted, needs_clarification}`；`_h_bp_ai_review` 只据该值走**白名单出边**，绝不透传 adapter 返回值"
    - "⭐ **`aapply_thread_answers` 在 review 入口有生产调用方（B1）**：`review()` 的 0-a 步无条件调它（`section_writer` 不传 ⇒ 走 114-04 的生产实现 `ablock_section_writer`）；返回 `conflict` ⇒ 直接 `needs_clarification`（不带着待裁决冲突继续审）。⇒ 该函数在本相位有**两个**生产调用方（本入口 + 114-05 的 answer 端点），不再是孤儿"
    - "⭐ **人工块保护在 review 入口接线（B3）**：0-b 步调 114-04 的 `arestore_human_blocks`；`conflict` ⇒ `needs_clarification`（人工块已写回、冲突线程已开）。**`blueprint_merge.py` 一行不改**（`git diff` 为空验收）"
    - "⭐ **新版本落库后批量重锚定（B3，判据为版本推进而非本轮是否产版本）**：入口**无条件**比对「artifact 当前最新版本」与「上次已重锚版本」（`stage_state[\"ai_review\"][\"anchored_version_id\"]`，缺省视为未重锚）——不一致即调 `lifecycle.areanchor_threads(artifact, <最新 content>, old_content=<上次已重锚版本 content>)` 并回写 `anchored_version_id`。**必须覆盖 `repo_rework`/`remerge` 重跑 merge 产版本这一主路径**（此时 0-a/0-b 均不产版本，若仍以「本轮是否落新版本」为判据则重锚永不触发、线程 anchor 指向已消失的 block）；best-effort（失败只 warning 且不回写 anchored_version_id，下一轮重试）"
    - "findings 批量落线程经 `open_thread(kind=ThreadKind.AI_REVIEW_FINDING, severity=<114-02 的 severity>, blocking=(severity == ThreadSeverity.BLOCKER), anchor={section_path, block_id, quoted_text}, created_on_version=<当前版本>, initiated_by_user_id=..., return_stage=\"ai_reviewing\")`——`blocking` 与 `severity` 同源（114-01 的不变式会在错配时 raise）"
    - "⭐ 去重幂等：同一 `finding_dedupe_key` 在第 2 轮**不新开线程**，改用 `append_note(thread, body=\"第 N 轮仍存在…\")`（线程 `status` 保持 `open`，门仍在）；已修复的走 `resolve_thread(resolution=...)`、误报走 `resolve_thread(dismissed=True)`"
    - "⭐ `rg -n \"record_answer\" server/services/process_runtime/blueprint_review.py` 零命中（源码扫描断言写进测试）"
    - "有界回退：轮次存 `stage_state[\"ai_review\"][\"round\"]`，上界合计 **≤2 轮**（走 `SettingKeys.BLUEPRINT_REVIEW_CONFIG` 的 `max_review_rounds`，缺配置回落模块常量 `MAX_REVIEW_ROUNDS = 2`）；仓级 BLOCKER → `repo_rework`（带 `back_repository_id`）、融合级 → `remerge`；**回退不落版本**"
    - "⭐ 有界修订不会无限循环（可证伪）：构造持续 BLOCKER 的样例 → 第 3 次进入 `ai_review` 走 `review_exhausted`、蓝图状态为 `pending_review`、`stage_state[\"ai_review\"][\"unresolved\"]` 非空、会话**不是** FAILED"
    - "⭐ `stage_state[\"ai_review\"]` 与 `stage_state[\"merge\"]` 桶**互不覆盖**（断言 merge 桶内容在审查回写后逐字不变）"
    - "仅 WARNING/INFO（零 BLOCKER）→ `review_passed`，蓝图转 `pending_review`，findings 作人审参考，**不打回**"
    - "超界出口携未决清单：`stage_state[\"ai_review\"][\"unresolved\"]` 只含 `{rule_id, severity, section_path, block_id, repository_id, thread_id}`（**无正文**）且条数有界"
    - "状态全经 lifecycle：进入 stage 转 `ai_reviewing`（新增 `_abp_mark_ai_reviewing`，幂等 + best-effort）、打回前转回 `drafting`、通过/超界前转 `pending_review`；合法边已全部存在，**状态机零改动**"
    - "`transition` 的 `ConcurrentBlueprintTransitionError` 被 handler/adapter 捕获（重试一次或映射成 `needs_clarification`），**绝不让 engine 落 FAILED**"
    - "`merge.transitions[\"merged\"]` 指向 `\"ai_review\"`（蓝图链），旧 `technical_plan` 链的 `merge.merged` 仍是 `STAGE_DONE`（正反并列断言，证明只改了蓝图链）"
    - "`blueprint_resume._STAGE_BLUEPRINT_STATUS` 含 `ai_review → ai_reviewing`，且 `test_blueprint_status_stage_map` 的前七 stage 七条参数化断言与 `test_stage_status_table_matches_enum` 继续绿；该文件删除行 **0**"
    - "`entrypoint.build_blueprint_engine` 的 deps 追加 `review=BlueprintReviewAdapter(...)` **并同步补 docstring 名单**（`test_blueprint_deps_roster_matches_the_factory_docstring` 三方一致性守护）"
    - "`event_taxonomy` 纯追加 `blueprint.review.started/completed/failed` 三常量（进 `__all__` 与 `BLUEPRINT_EVENTS`），既有常量与 payload 字段零改动"
  artifacts:
    - path: "server/services/process_runtime/blueprint_review.py"
      provides: "BlueprintReviewAdapter（findings 落线程 + 去重 + 有界回退归因 + 超界未决快照 + 状态转移 + 三事件）"
      contains: "class BlueprintReviewAdapter"
    - path: "server/services/process_runtime/builtin_processes.py"
      provides: "_h_bp_ai_review + ai_review StageDef + merge.merged→ai_review + _abp_mark_ai_reviewing"
      contains: "async def _h_bp_ai_review"
    - path: "server/services/process_runtime/blueprint_resume.py"
      provides: "_STAGE_BLUEPRINT_STATUS 追加 ai_review → ai_reviewing（删除行 0）"
      contains: "\"ai_review\": \"ai_reviewing\""
    - path: "server/delivery/services/event_taxonomy.py"
      provides: "blueprint.review.started/completed/failed 三个新常量（纯追加）"
      contains: "blueprint.review.started"
    - path: "server/tests/services/process_runtime/test_blueprint_review_stage.py"
      provides: "handler 四类 + 持续 BLOCKER 2 轮转 pending_review 携未决 + merge 桶不被覆盖 + 仅 WARNING 不打回 + 去重不重开线程 + record_answer 源码扫描"
      contains: "review_exhausted"
  key_links:
    - from: "server/services/process_runtime/blueprint_review.py"
      to: "server/delivery/services/blueprint_lifecycle_service.py"
      via: "open_thread(severity=) / append_note / resolve_thread / transition（INV-6：adapter 零 ORM 写）"
      pattern: "open_thread"
    - from: "server/services/process_runtime/builtin_processes.py"
      to: "server/services/process_runtime/blueprint_review.py"
      via: "getattr(getattr(engine, \"deps\", None), \"review\", None) → adapter.review(session)"
      pattern: "\"review\""
    - from: "server/services/process_runtime/entrypoint.py"
      to: "server/services/process_runtime/blueprint_review.py"
      via: "build_blueprint_engine 的 deps 追加 review=BlueprintReviewAdapter()（属性名与 handler getattr 逐字一致）"
      pattern: "review="
---

<objective>
把 114-02 的判定内核接进流程，让 `ai_review` 成为蓝图链的**第 10 个 stage**：

① `blueprint_review.py` 追加 `BlueprintReviewAdapter`：**入口先接线三件（B1/B3 定夺）——答案消费（`aapply_thread_answers`，产新版本）、人工块保护（`arestore_human_blocks`，等价保留/冲突开线程）、新版本落库后批量重锚（`areanchor_threads`）** → 再跑六类 + goal-backward → findings **批量落线程**（`severity` 形参 + `append_note` 留痕 + `(rule_id, block_id)` 去重）→ **有界回退归因**（仓级回 `repo_plan` / 融合级回 `merge`，合计 ≤2 轮，计数存 `stage_state["ai_review"]`）→ **超界转 `pending_review` 携未决清单**（绝不 FAILED）；
② `builtin_processes.py` **只加**：`_h_bp_ai_review` + `ai_review` StageDef + `merge.merged → "ai_review"` 一行 + `_abp_mark_ai_reviewing`；
③ `blueprint_resume.py` **只加一行**映射（`ai_review → ai_reviewing`，删除行上界 0）；`entrypoint.py` deps 追加 `review=` 并同步 docstring；`event_taxonomy.py` 纯追加三个 `blueprint.review.*` 常量；`SettingKeys` 追加 `BLUEPRINT_REVIEW_CONFIG`；
④ stage 端到端测试锁死「持续 BLOCKER 两轮后转 `pending_review` 携未决清单且会话不是 FAILED」与「`merge` 桶不被覆盖」两条**无限循环防线**，并同步更新三条随接续点变化的既有回归断言。

Purpose: FLOW-07 的执行面。没有这一层，六类规则只是一堆纯函数——不打回、不建线程、不改状态、不进人审。两条最贵的失效模式都在这一层：轮次桶用错（塞进 `merge` 桶被整桶覆盖 → 计数归零 → 无限打回循环）、超界出口写错（落 FAILED → 蓝图成果连带报废）。
Output: 一个 adapter 节 + 第 10 个 stage 的注册 + 三处受限追加 + 一个 stage 端到端测试文件。**无 migration、无新模型字段、无新 `CallSource`、无新 `artifact_type`、无第二个 engine 工厂。**
</objective>

<execution_context>
工作目录：worktree 根 `/Users/zaneliu/Projects/open-source/friday-clean/.claude/worktrees/v0.20-blueprint`。**wave 4**，依赖 114-02（`run_mechanical_rules` / `agoal_backward_review(..., constraints=)` / `normalize_review_findings` / `finding_dedupe_key` / `STAGE_STATE_KEY`）、114-01（`open_thread(severity=)` / `append_note` / `aunresolved_blocker_count`）与 **114-04**（`areanchor_threads` / `aapply_thread_answers` / `arestore_human_blocks` / `acollect_human_block_ids`）。

⚠️ **wave 顺序已在有界修订第 1 轮调整（原为 wave 3，在 114-04 之前）**：B1/B3 定夺要求本 plan 的 `ai_review` 入口显式接线「答案消费 → 产新版本」「批量重锚定」「人工块保护」，这三者都由 114-04 交付 ⇒ 114-04 必须先行。本 plan 只做**接线**，不在此重写任何一件（`blueprint_reflow.py` / `blueprint_lifecycle_service.py` 在本 plan 是只读受限面）。

**本 plan 是全相位唯一改 `builtin_processes.py` / `blueprint_resume.py` / `entrypoint.py` / `event_taxonomy.py` / `system/models.py` 的 plan。** 改动形状严格受限，逐条登记：

| 文件 | 允许的改动 | 删除行上界 | formatter |
|---|---|---|---|
| `builtin_processes.py` | 新增 `_abp_mark_ai_reviewing` + 新增 `_h_bp_ai_review` + 新增 `"ai_review": StageDef(...)` + 蓝图链 `merge.transitions["merged"]` **一行值** | **1**（就是 `"merged": STAGE_DONE,` 那一行；其余全为新增） | 只 `ruff check` |
| `blueprint_resume.py` | `_STAGE_BLUEPRINT_STATUS` 追加一行 + 追加一行注释 | **0** | 只 `ruff check` |
| `entrypoint.py` | `build_blueprint_engine` 的 `SimpleNamespace` 追加 `review=` + 函数内 import 一行 + docstring 名单补 ``review`` | **0**（docstring 段若需断行改写，逐行登记，≤2） | 只 `ruff check` |
| `event_taxonomy.py` | `__all__` 追加 3 项 + 3 个 `Final[str]` 常量定义 + `BLUEPRINT_EVENTS` 追加 3 项 | **0** | 只 `ruff check` |
| `system/models.py` | `SettingKeys` 追加 `BLUEPRINT_REVIEW_CONFIG` + 上方注释块 | **0** | 只 `ruff check` |
| `blueprint_review.py` | 文件尾**纯追加** adapter 节 + `__all__ += [...]` | **0** | 可 `ruff format` |

⚠️ **随接续点变化的既有回归断言必须同步更新**（这不是「改测试迁就实现」，而是接续点断言本身就写着「追加 ai_review 时改这一个值即可」）：
1. `tests/services/process_runtime/test_blueprint_process_graph.py:223-230` `test_merge_merged_is_the_114_handoff_point` → `merged == "ai_review"`，并**追加正向对照**：旧链 `_TECHNICAL_PLAN_STAGES["merge"].transitions["merged"] == STAGE_DONE`（证明只改了蓝图链）；
2. 同文件 `:66-79` 追加 `BLUEPRINT_STAGE_KEYS_114 = ("ai_review",)` 并并入 `BLUEPRINT_STAGE_KEYS`；`:259` 的 `pausable == {...}` 集合追加 `"ai_review"`；`:289-293` `test_handler_count_and_registration_count` 的 `== 9` 改 `== 10`（注释同步为「7 + 2 + 1(114-03) = 10」），`register_process_type` 计数**保持 3**；`:556-561` 的 `test_handlers_pass_through_without_deps` 参数化追加 `(bp._h_bp_ai_review, "needs_clarification")`；
3. `tests/services/process_runtime/test_blueprint_merge_gate.py:541` 的 `stages["merge"].transitions["merged"] == STAGE_DONE` → `== "ai_review"`（该文件其余断言一字不动）。

⚠️ 已知行为变更（登记在案，CONTEXT 已接受）：`merge.merged` 改指 `ai_review` 后，已 `current_stage="merge"` 的**在途会话**下次 advance 会多走一个 stage。旧 `technical_plan` process 零感知。执行前确认开发环境无残留在途蓝图会话。

⚠️ 字段名与签名一律按 114-RESEARCH 实测，**不得猜测**。关键实测清单：
- `StageOutcome(event, stage_state_update=None, current_artifact_version=None, error=None)`（`engine.py:34-46`）；未登记的 event → engine `ValueError`
- handler 签名恒为 `async def _h_bp_<stage>(session: Any, engine: Any) -> StageOutcome`；软取依赖 `getattr(getattr(engine, "deps", None), "review", None)`
- `_abp_load_artifact(session)`（`builtin_processes.py:452`）/ `_abp_mark_drafting`（`:465`，幂等 + best-effort + 空状态先补一跳 `researching`）/ `_abp_has_open_blocking_threads`（`:507`，探测失败按 **False**）/ `_abp_ensure_blocking_clarification(session, *, stage, reason)`（`:524`，幂等，`return_stage=stage`，问题文本只含 stage 名与枚举化 reason）
- `BlueprintLifecycleService.transition(artifact, to_status, *, initiated_by_user_id, acting_user=None, session=None, return_status=None)`（**方法名是 `transition` 不是 `atransition`**，`:143`）；CAS 失败 → `ConcurrentBlueprintTransitionError`（`:70`）
- 合法边（`:89-121`）：`DRAFTING → AI_REVIEWING`、`AI_REVIEWING → {NEEDS_CLARIFICATION, DRAFTING, PENDING_REVIEW}`；`_CLARIFICATION_RETURN_TARGETS` 已含 `ai_reviewing`（`return_stage="ai_reviewing"` 合法，12 字符 < `max_length=16`）
- adapter 事件走 `self.session_service.aemit_event(event_name, session, payload)`（照 `blueprint_spec_gate._emit:711`）
- 配置读取照 `blueprint_merge._aload_merge_config`（`:1995-2017`）：`aget_json_setting(SettingKeys.X, {})` + 整段 `try/except` 回落模块常量
- 轮次读写照 `blueprint_merge._attempt_of`（`:2298`）/ `_build_stage_state`（`:2306`）的**单点递增 + 整桶回写**范式
- 结果 dict 照 `blueprint_merge._result`（`:2240`）的恒定键 + `extra` 追加归因键
</execution_context>

<context>
@.planning/phases/114-ai/114-CONTEXT.md（锁定：追加第 10 个 stage 且 `builtin_processes` 只加注册与新 handler；归因打回仓级回 `repo_plan`、融合级回 `merge`，合计 ≤2 轮，计数存 `stage_state`；超界转 `pending_review` 携未决 BLOCKER 清单不落 FAILED；仅 WARNING/INFO 直接进 `pending_review` 不打回；`blueprint_resume` 表追加 `ai_review → ai_reviewing`；观测口径：审查生命周期 `caller`、机械规则逐条 `sampling`、正文不进 payload）
@.planning/phases/114-ai/114-RESEARCH.md §3（注册字典与 113 留好的接续点原文 + `StageOutcome` 四字段 + `ai_review` StageDef 建议形状 + 映射表追加纪律 + 合法边全表）、§2（`open_thread` / `resolve_thread` / `ahas_open_blocking_threads` / `add_reviewer` / `transition` 逐字签名 + findings 去重建议）、Pitfalls P2（`stage_state` 顶层键占用表 + **绝不复用 merge 桶**的整桶覆盖机理）、P4（`transition` CAS 语义与异常处理）、P5（§13.2 冻结清单 + `blueprint_resume` 追加纪律）、P6（`_h_bp_merge` 三条备选教训）
@.planning/phases/114-ai/114-PATTERNS.md 冻结清单 §①（`blueprint_resume` 唯一允许的 diff 形状，逐字）、§②（`builtin_processes` 三处改动上界的 diff 形状 + 验收口径 `_h_bp_` 9→10 / register 保持 3）、§③（`record_answer` 四通道表）、第 3 类（`_h_bp_merge` analog 全文 + `ai_review` StageDef 目标形状 + `transitions` 不含 failed 出边 + self-loop 前 ensure 线程）、第 4 类（findings 落线程四条 + `open_thread` 调用形状逐字）、第 5 类（有界回退骨架 + 113→114 逐项映射表 + 可证伪断言）、第 9 类（映射表追加体例）、第 10 类（stage 端到端测试体例）
@server/services/process_runtime/builtin_processes.py（L445-571 四个 `_abp_*` helper 的确切形态、L573-682 `_h_bp_repo_plan` 的「绝不写半截键」、L685-739 `_h_bp_merge` 全文含 docstring 的三条备选论证、L742-836 `_TECHNICAL_BLUEPRINT_STAGES` 九项含 `merge` 的 114 接续点注释、L839-849 三次注册）
@server/services/process_runtime/blueprint_merge.py（L1541-1564 `merge` 的恒定七键返回契约、L1692-1808 覆盖率门的有界回退 + 超界携未决项骨架、L1945-1983 超界开澄清线程且不上抛、L1995-2017 `_aload_merge_config`、L2240-2291 `_result` / `_log`、L2298-2340 `_attempt_of` / `_build_stage_state`）
@server/services/process_runtime/blueprint_spec_gate.py（L284-341 `_open_clarification` 的开线程 + 双通道留痕形状、L711-722 `_emit`）
@server/services/process_runtime/blueprint_resume.py（L58-82 映射表与 `_resolve_stage_status`、L93-157 续驱短路判据与 `max_steps`）
@.planning/phases/114-ai/114-04-SUMMARY.md（**入口接线的被调方逐字签名**：`aapply_thread_answers` / `arestore_human_blocks` / `acollect_human_block_ids` / `areanchor_threads` / `ablock_section_writer` 的签名与恒定返回键、各 `status` 语义、`produced_by_ref` 三个前缀 —— 本 plan 按它对齐，**不猜**）
@server/services/process_runtime/entrypoint.py（L144-186 `build_blueprint_engine` 的 docstring 名单 + `SimpleNamespace` + `__all__ +=` 纯追加范式）
@server/delivery/services/event_taxonomy.py（L39-57 `__all__`、L125-175 `blueprint.*` 常量定义体例、L176-196 `BLUEPRINT_EVENTS` frozenset）
@server/system/models.py（L175-192 `SettingKeys` 的三个 blueprint 键与其**注释体例**：value JSON 形状 + 缺配置回落 + 消费方标注）
@server/tests/services/process_runtime/test_blueprint_process_graph.py（L1-28 「守十件事」+ L66-79 `BLUEPRINT_STAGE_KEYS_11x` 常量分层 + L223-230 114 接续点断言 + L256-263 pausable 集合 + L289-293 handler 计数 + L337-353 deps 名单三方一致性 + L556-570 `pass_through_without_deps` 参数化）
@server/tests/services/process_runtime/test_blueprint_merge_gate.py（L440-545 覆盖率门端到端断言体例 + L541 接续点断言）
</context>

<tasks>

<task type="auto">
  <name>Task 1: 配置键 + 三个事件常量 + BlueprintReviewAdapter（findings 落线程去重 + 有界回退归因 + 超界未决快照 + 状态转移）</name>
  <files>server/system/models.py, server/delivery/services/event_taxonomy.py, server/services/process_runtime/blueprint_review.py</files>
  <read_first>server/system/models.py L170-193（`SettingKeys` 三个 blueprint 键的**注释四段体例**：value JSON 形状 / 未配置时的回落值 / 消费方文件与相位标注；确认它是**纯常量类**不是 models.Model）；server/delivery/services/event_taxonomy.py L36-60（`__all__` 的排列顺序与分组）、L125-196（`blueprint.*` 常量的 `Final[str]` 写法 + 分组注释 + `BLUEPRINT_EVENTS` frozenset 的成员排列，以及「已定义但不计入 ALL_EVENTS」的理由注释）；server/services/process_runtime/blueprint_merge.py L1692-1808（**有界回退 + 超界携未决项的完整骨架**：门的位置、`attempt + 1 <= max_rounds` 判据、回退不落版本的注释理由、`exhausted` 出口的注释、`unresolved` 有界切片）、L1945-1983（超界开线程不上抛）、L2240-2291（`_result` 恒定键 + `extra` + `_log` 五件套）、L2298-2340（`_attempt_of` / `_build_stage_state` 单点递增整桶回写）；server/services/process_runtime/blueprint_spec_gate.py L284-341（`open_thread` 调用的**全部实参名与顺序** + 双通道留痕 + payload 只放计数）、L711-722（`_emit`）；server/delivery/services/blueprint_lifecycle_service.py L347-363（`ahas_open_blocking_threads` 签名与 `kind` 过滤语义）、L365-423（`open_thread` **含 114-01 新增的 `severity` 形参与不变式校验的确切报错文案**）、L456-483（`resolve_thread` 的 `dismissed` 语义与幂等）、以及 114-01 新增的 `append_note` / `aunresolved_blocker_count` 的**实际签名**（读 114-01-SUMMARY.md 对齐）</read_first>
  <action>
**① `SettingKeys` 追加一个键**（`server/system/models.py`，纯追加，照既有三键的注释四段体例）：
```
    # value 为 JSON：{"max_review_rounds": int}。未配置时回落模块常量
    # （MAX_REVIEW_ROUNDS=2，CONTEXT 锁定的「合计 ≤2 轮」）——轮上界可配是为了
    # 「一上线就反复打回」时能秒调，而非让配置成为必需项。
    # 消费方：services/process_runtime/blueprint_review.py（114-03）。
    BLUEPRINT_REVIEW_CONFIG = "blueprint.review.config"
```
`SettingKeys` 是纯常量类 ⇒ **零 migration**（验收有 `makemigrations --check`）。

**② `event_taxonomy.py` 纯追加三常量**（§13.2 第 3 条：只新增 `blueprint_*`，不改既有类型与 payload 字段）：
`__all__` 追加 `"EVENT_BLUEPRINT_REVIEW_STARTED"` / `"..._COMPLETED"` / `"..._FAILED"`；定义
```
EVENT_BLUEPRINT_REVIEW_STARTED: Final[str] = "blueprint.review.started"
EVENT_BLUEPRINT_REVIEW_COMPLETED: Final[str] = "blueprint.review.completed"
EVENT_BLUEPRINT_REVIEW_FAILED: Final[str] = "blueprint.review.failed"
```
并把三者追加进 `BLUEPRINT_EVENTS` frozenset。分组注释一行：「114-03 追加：AI 对抗审查生命周期（payload 只含 findings **计数与分级分布**，正文绝不进 payload）」。

**③ `blueprint_review.py` 文件尾纯追加 adapter 节**（114-02 的纯函数节一字不动；`__all__ += ["BlueprintReviewAdapter", "MAX_REVIEW_ROUNDS"]`）：

模块常量追加：`MAX_REVIEW_ROUNDS = 2`（注释「CONTEXT 锁定的合计 ≤2 轮；可被 `SettingKeys.BLUEPRINT_REVIEW_CONFIG.max_review_rounds` 覆盖，此处是兜底」）、`_MAX_UNRESOLVED = 30`（对齐 `blueprint_merge._MAX_UNRESOLVED`）。

```
class BlueprintReviewAdapter:
    def __init__(self, *, artifact_service=None, lifecycle_service=None, session_service=None, node_execution_id: str = "") -> None: ...
    async def review(self, session: Any) -> dict: ...
```
docstring 写明**恒定返回键**（下游 handler 无需判空分支）：
```
{"review_status": "passed"|"retry"|"exhausted"|"needs_clarification",
 "artifact_version_id": str, "round": int, "back_target": str,
 "back_repository_id": str, "report": dict, "stage_state": dict}
```
`review()` 的固定**九步**（照 `blueprint_merge.merge` 的分步 + `_log` 五件套）。⭐ **前两步是 B1/B3 定夺追加的入口接线，顺序不可换**（必须在读审查基线**之前**跑完，否则审的是「答案未回灌、人工块已被抹掉」的旧内容）：

⏱ `started = time.monotonic()` 与 `EVENT_BLUEPRINT_REVIEW_STARTED` 在 **0-a 之前**发出（`duration_ms` 含入口接线耗时）；下面第 1 步复用同一个 `started`，**不重置**。

0-a. ⭐ **答案消费（B1 的 review 入口接线）**：`reflow = await aapply_thread_answers(artifact, session=session, initiated_by_user_id=self._initiated_by(session), lifecycle_service=..., artifact_service=...)`（114-04 交付，lazy import；`section_writer` 不传 ⇒ 走生产实现 `ablock_section_writer`）。语义：
   - `reflow["status"] == "conflict"` ⇒ 已开阻塞线程等人裁决 ⇒ **直接 return `needs_clarification`** 形状（不跑规则，避免审一份马上要被改的内容）；
   - `applied` ⇒ 本轮已产新版本（`decision_log` 已物化、线程已 `resolved`）；
   - `unchanged` / `noop` / `invalid` ⇒ 记 warning 后继续（`invalid` 不阻断审查：审查结论本身就是「有缺陷」的判据）。
   docstring 写明：「本调用是 `aapply_thread_answers` 的**两个生产调用方之一**（另一个是 114-05 的 answer 端点）。少了它，人在打回轮之间作答的线程永不被消费 ⇒ 答案不落地、同一问题会被反复问。」

0-b. ⭐ **人工块保护（B3 的接线）**：`guard = await arestore_human_blocks(artifact, session=session, initiated_by_user_id=..., lifecycle_service=..., artifact_service=...)`（114-04 交付，lazy import）。语义：
   - `guard["status"] == "conflict"` ⇒ 人工块已写回、冲突线程已开 ⇒ **return `needs_clarification`**（绝不静默覆盖，也绝不带着冲突继续审）；
   - `restored` ⇒ 已落 `human_block_restore:` 版本，继续（下一步读到的最新版本就是它）；
   - `unchanged` / `noop` ⇒ 继续。
   docstring 写明 P-B3 机理：「打回后 `repo_rework` / `remerge` 会重跑 merge 并 `add_version`，那是本相位**主要的产版本路径**。`blueprint_merge.py` 是只读受限面（不改），所以保护挂在这里 —— 每次进入 `ai_review` 都先把版本链里 `produced_by_ref` 带 `human_edit:` 的 block 逐一比对：等价保留、实质冲突写回人工版本并开线程。」

0-c. ⭐ **批量重锚定（B3 的第二件；判据 = 版本推进，不是「本轮是否产版本」）**：**无条件**读取 artifact 当前最新版本 id 与 `stage_state["ai_review"].get("anchored_version_id")`（缺省 = 未重锚过）：
- 二者一致 ⇒ 跳过（无版本推进，无需重锚）
- 不一致 ⇒ `await lifecycle.areanchor_threads(artifact, <最新版本 content>, old_content=<上次已重锚版本 content，缺省取该最新版本的 supersedes 目标>, initiated_by_user_id=...)`（114-04 交付），成功后把 `anchored_version_id` 回写进 `stage_state["ai_review"]`
**为什么不能用「0-a/0-b 是否落新版本」作判据**：B3 点名的主路径是 `repo_rework`/`remerge` **重跑 merge 产的版本**——它由 merge 落库，此时若无已作答线程、也无人工块，0-a/0-b 均不产版本，以「本轮是否产版本」为判据会让重锚**永不触发**，线程 anchor 仍指向已消失的 block（115 批注全部错位/凭空消失，CLAR-02 明令禁止）。
best-effort：失败只 warning、**不回写 `anchored_version_id`**（下一轮自然重试），不阻断审查。

⚠️ 0-a/0-b/0-c 三步整体各自包 `try/except Exception` → warning（脱敏）+ 继续（**唯一例外**：`conflict` 是显式返回 `needs_clarification` 的正常控制流，不是异常）。

1. 读**最新** `ArtifactVersion` 作基线（`order_by("-version_no")`，**不读 `session.current_artifact_version`**——注释理由照 `blueprint_confirm_gate.py:546-550`：merge 的 `add_version` 已推进 current，读 session 那一版会审到旧内容；⚠️ 0-a/0-b 可能刚落了新版本，**必须在此重读**，不得复用入口处的快照）；取不到版本 → `needs_clarification`（fail-closed，不判通过）。`_log("blueprint_review_started", category="caller", component="process_runtime", reflow_status=..., guard_status=..., ...)`（两个入口接线的结果作为**标量**进日志，正文不进）。
2. `round_no = _round_of(state)`（模块级 helper，照 `_attempt_of`：读 `stage_state["ai_review"]["round"]`，异常回 0）；`max_rounds = await self._aload_review_config()`（照 `_aload_merge_config`，`aget_json_setting(SettingKeys.BLUEPRINT_REVIEW_CONFIG, {})`，异常整段回落 `MAX_REVIEW_ROUNDS`）。
3. **跑判定**：`charters = await aload_charters([assoc.repository_id ...])`（`blueprint_charter_match.aload_charters`，best-effort 返 `{}`；lazy import）；`locked_snapshot = (session.stage_state or {}).get("confirmation")`；`mech = run_mechanical_rules(content, charters=charters, locked_snapshot=locked_snapshot)`；`llm = await agoal_backward_review(...)`；`findings = mech + normalize_review_findings(llm)`（**`llm is None` 时 `normalize_review_findings` 会产 warning meta finding ⇒ fail-closed，不判通过**）。机械规则逐条判定的日志用 `category="sampling"`（高频，禁 INFO 刷屏——只在 debug 或聚合后记一条计数）。
4. **findings → 线程**（每条走 `self._aupsert_finding_thread(...)`）：
   - 幂等键 `finding_dedupe_key(finding)`；先取该 artifact 上 `kind=ThreadKind.AI_REVIEW_FINDING` 且 `status__in=[OPEN, ANSWERED]` 的既有线程（**只读查询允许 adapter 直查**，写一律经 service），按 `anchor.block_id or anchor.section_path` + 首条消息中的 rule_id 标记建索引；
   - 命中 → `await lifecycle.append_note(thread, body=f"第 {round_no + 1} 轮仍存在：{detail}", ...)`（⛔ **绝不 `record_answer`**，理由见 prohibitions）；
   - 未命中 → `await lifecycle.open_thread(artifact, kind=ThreadKind.AI_REVIEW_FINDING, severity=finding["severity"], blocking=(finding["severity"] == ThreadSeverity.BLOCKER), question=<finding 正文，含 rule_id 前缀便于去重索引>, anchor={"section_path":…, "block_id":…, "quoted_text":…}, created_on_version=version, initiated_by_user_id=self._initiated_by(session), return_stage="ai_reviewing")`；
   - **本轮已消失的既有 finding 线程** → `await lifecycle.resolve_thread(thread, resolution="本轮复检已不再命中该规则。", ...)`（幂等，终态重复调用 no-op）。
   开线程/留痕单条失败 **best-effort 吞掉**（`logger.warning` + 继续下一条），绝不让一条 finding 落库失败把整轮审查打成异常。
5. **出口判定**：`blockers = [f for f in findings if f["severity"] == SEVERITY_BLOCKER]`。
   - `blockers` 为空 → 转 `pending_review`（`await lifecycle.transition(artifact, BlueprintStatus.PENDING_REVIEW, ...)`）→ `review_status="passed"`，`stage_state` 回写 `{round: round_no, blocker_count: 0, warning_count, info_count, thread_ids, unresolved: []}`。**不打回**（CONTEXT：仅 WARNING/INFO 作人审参考）。
   - 有 `blockers` 且 `round_no + 1 <= max_rounds` → **归因**：`decision = self._decide_back_target(blockers)`（内部两档纯逻辑，形状照 `blueprint_merge.decide_back_target`：全部 BLOCKER 落在同一 `repository_id` → `("repo_plan", rid)`；跨仓或解析不到 → `("merge", "")`）→ 转回 `drafting` → `review_status="retry"`，`back_target`/`back_repository_id` 带上，`stage_state["ai_review"]["round"] = round_no + 1`。**回退不落版本**（审查未过的中间产物不进版本历史，注释写明）。
   - 有 `blockers` 且轮次用尽 → `unresolved = [ {rule_id, severity, section_path, block_id, repository_id, thread_id} ][:_MAX_UNRESOLVED]`（**无正文**）→ 转 `pending_review` → `review_status="exhausted"`，`stage_state["ai_review"]["unresolved"] = unresolved`。**绝不 FAILED**（注释照 `blueprint_merge:1756-1760`：蓝图已成形，只是审查未清 —— 那是「待人审」不是「流程失败」）。
6. **`stage_state` 回写**：`{STAGE_STATE_KEY: bucket}`（`STAGE_STATE_KEY == "ai_review"`）。⚠️ 只做**增量返回**（engine 浅合并进 `session.stage_state`），**绝不整桶读改写 `merge` 键**——注释写明 P2 的机理：`blueprint_merge._build_stage_state` 会整桶覆盖 `merge`，若把审查轮次塞那里会被覆盖 → 计数归零 → 无限循环。桶内单字段 < 2KB（`DESIGN.md:472`）。
7. emit `EVENT_BLUEPRINT_REVIEW_COMPLETED`（payload 只放 `round` / `blocker_count` / `warning_count` / `info_count` / `thread_count` / `back_target` / `review_status`，**正文绝不进 payload**）+ `_log(..., category="caller", duration_ms=round((time.monotonic()-started)*1000, 2), initiated_by_user_id=...)`。整个 `review()` 外层 `try/except Exception` → emit `EVENT_BLUEPRINT_REVIEW_FAILED` + `logger.warning(..., error=redact_secrets_in_text(str(exc)))` + 返回 `needs_clarification` 形状（**不上抛**，让 handler 停在本 stage 等处置而不是让 engine 落 FAILED）。

⚠️ **`transition` 的 CAS**：所有 `transition` 调用包 `try/except ConcurrentBlueprintTransitionError` → `refresh_from_db` 重试一次，仍失败则降级为 `needs_clarification`（P4：**绝不让 engine 落 FAILED**）；`ValueError`（非法边）同样吞成 `needs_clarification` 并 warning。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/test_blueprint_settings.py tests/delivery/test_blueprint_events.py -q ; cd server && uv run python -c "from services.process_runtime.blueprint_review import BlueprintReviewAdapter, MAX_REVIEW_ROUNDS, STAGE_STATE_KEY; from delivery.services.event_taxonomy import EVENT_BLUEPRINT_REVIEW_STARTED, BLUEPRINT_EVENTS; from system.models import SettingKeys; print(MAX_REVIEW_ROUNDS, STAGE_STATE_KEY, EVENT_BLUEPRINT_REVIEW_STARTED, SettingKeys.BLUEPRINT_REVIEW_CONFIG, EVENT_BLUEPRINT_REVIEW_STARTED in BLUEPRINT_EVENTS)"</automated>
  </verify>
  <acceptance_criteria>
- `cd server && uv run python -c "from delivery.services.event_taxonomy import BLUEPRINT_EVENTS, EVENT_BLUEPRINT_REVIEW_STARTED as S, EVENT_BLUEPRINT_REVIEW_COMPLETED as C, EVENT_BLUEPRINT_REVIEW_FAILED as F
assert (S, C, F) == ('blueprint.review.started', 'blueprint.review.completed', 'blueprint.review.failed')
assert {S, C, F} <= BLUEPRINT_EVENTS
print('events OK')"` 输出 `events OK`。
- `git diff server/delivery/services/event_taxonomy.py | rg "^-[^-]"` **为空**（纯追加）；`git diff server/system/models.py | rg "^-[^-]"` **为空**。
- `cd server && uv run python manage.py makemigrations --check --dry-run` 退出码 0（`SettingKeys` 追加零 migration）。
- `rg -n "class BlueprintReviewAdapter" server/services/process_runtime/blueprint_review.py` 命中；`rg -n "MAX_REVIEW_ROUNDS = 2" server/services/process_runtime/blueprint_review.py` 命中。
- ⭐ `rg -n "record_answer" server/services/process_runtime/blueprint_review.py` **零命中**（finding 留痕只走 `append_note`）。
- `rg -n "append_note" server/services/process_runtime/blueprint_review.py` 命中 ≥1；`rg -n "resolve_thread" server/services/process_runtime/blueprint_review.py` 命中 ≥1。
- `rg -n 'severity=|blocking=' server/services/process_runtime/blueprint_review.py` 命中，且 `rg -n 'blocking=\(' server/services/process_runtime/blueprint_review.py` 命中（`blocking` 由 severity 派生，不是各写各的）。
- `rg -n "BlueprintThread.objects.(create|acreate)|BlueprintThreadMessage.objects" server/services/process_runtime/blueprint_review.py` **零命中**（INV-6）；`rg -n "ConvergenceSessionEvent.objects.acreate\(|\._emit_event\(" server/services/process_runtime/blueprint_review.py` **零命中**（MN-05）。
- `rg -n 'STAGE_STATE_KEY: ' server/services/process_runtime/blueprint_review.py` 命中；`rg -n '"merge"' server/services/process_runtime/blueprint_review.py` **零命中**（绝不写 merge 桶）。
- `rg -n "ConcurrentBlueprintTransitionError" server/services/process_runtime/blueprint_review.py` 命中（CAS 异常被处理）。
- ⭐ B1 接线：`rg -n "aapply_thread_answers" server/services/process_runtime/blueprint_review.py` 命中 ≥1（review 入口有生产调用方；**不得只出现在注释里**——断言其出现在 `await` 语句上：`rg -n "await aapply_thread_answers|await _aapply_thread_answers|= await .*aapply_thread_answers" server/services/process_runtime/blueprint_review.py` 命中）。
- ⭐ B3 接线：`rg -n "arestore_human_blocks" server/services/process_runtime/blueprint_review.py` 命中；`rg -n "areanchor_threads" server/services/process_runtime/blueprint_review.py` 命中；`git diff server/services/process_runtime/blueprint_merge.py` 与 `git diff server/services/process_runtime/blueprint_reflow.py` 与 `git diff server/delivery/services/blueprint_lifecycle_service.py` 输出**均为空**（本 plan 只接线、不改被调方）。
- ⭐ 入口顺序断言（源码行号比较，可执行）：`cd server && uv run python -c "
src = open('services/process_runtime/blueprint_review.py').read()
i = src.index('async def review')
body = src[i:]
a = body.index('aapply_thread_answers'); b = body.index('arestore_human_blocks')
c = body.index('order_by(\"-version_no\")')
assert a < c and b < c, ('入口接线必须在读审查基线之前', a, b, c)
print('entry wiring order OK')"` 输出 `entry wiring order OK`。
- `rg -n "STAGE_FAILED|\"failed\"" server/services/process_runtime/blueprint_review.py` **零命中**（超界绝不落 failed）。
- `cd server && uv run pytest tests/services/process_runtime/ tests/delivery/ -q` 无新增失败。
- `cd server && uv run ruff check services/process_runtime/blueprint_review.py delivery/services/event_taxonomy.py system/models.py` 通过；`uv run ruff format --check services/process_runtime/blueprint_review.py` 通过（**不对 `event_taxonomy.py` / `system/models.py` 跑 format**）。
  </acceptance_criteria>
  <done>配置键与三个事件常量纯追加就位（零 migration、零删除行）；adapter 能把 findings 落成分级线程且同一问题第 2 轮走 `append_note` 不重开；有界回退归因两档 + 超界携未决清单 + 全部状态经 lifecycle 且 CAS 异常不外泄；轮次存 `ai_review` 桶，源码里根本不出现写 `merge` 桶的路径。</done>
</task>

<task type="auto">
  <name>Task 2: _h_bp_ai_review + ai_review StageDef + merge.merged→ai_review + _abp_mark_ai_reviewing + entrypoint deps + blueprint_resume 映射表一行</name>
  <files>server/services/process_runtime/builtin_processes.py, server/services/process_runtime/entrypoint.py, server/services/process_runtime/blueprint_resume.py</files>
  <read_first>server/services/process_runtime/builtin_processes.py L465-505（`_abp_mark_drafting` 的**幂等 + 空状态先补 researching + best-effort warning** 三段结构，`_abp_mark_ai_reviewing` 照抄形态但目标态换成 `AI_REVIEWING`，且**前置态必须是 `drafting`**——`"" → ai_reviewing` 与 `researching → ai_reviewing` 都不是合法边，须先补一跳 `drafting`）、L507-571（`_abp_has_open_blocking_threads` 与 `_abp_ensure_blocking_clarification` 的确切签名与幂等判定）、L685-739（`_h_bp_merge` 全文：**docstring 的三条备选论证要逐条照抄成 `_h_bp_ai_review` 的版本** + 软取依赖 + `result if isinstance(result, dict) else {}` 归一 + 白名单出边 + self-loop 前 ensure 线程）、L742-836（`_TECHNICAL_BLUEPRINT_STAGES` 的 StageDef 字段顺序与注释体例 + `merge` 项的 114 接续点注释原文）；server/services/process_runtime/entrypoint.py L144-186（docstring 的 deps 名单**确切措辞** + `SimpleNamespace` 各行格式 + 函数内 import 的字母序 + `__all__ +=` 纯追加）；server/services/process_runtime/blueprint_resume.py L58-68（映射表与其**三段注释原文**——追加注释要与之体例一致）</read_first>
  <action>
**① `builtin_processes.py` 新增 `_abp_mark_ai_reviewing`**（紧跟 `_abp_mark_drafting` 之后，纯追加）：
照 `_abp_mark_drafting` 的三段结构（幂等 → 补前置跳 → best-effort warning）。差异点写进 docstring：「合法边只有 `DRAFTING → AI_REVIEWING`（`blueprint_lifecycle_service.py:89-121`），故当前态不是 `drafting` 时**先补一跳 `drafting`**（`""` 时再先补 `researching`），复用 `_abp_mark_drafting` 完成前置。已是 `ai_reviewing` 直接返回（幂等）。状态映射是展示面，失败绝不阻断 stage 推进。」

**② 新增 `async def _h_bp_ai_review(session, engine) -> StageOutcome`**（紧跟 `_h_bp_merge` 之后，纯追加）。docstring **逐条照抄 `_h_bp_merge` 的三条备选论证**并换成本 stage 的措辞：
- 返自身 event（`needs_clarification` 指回 `ai_review` 但无线程）→ 引擎自旋 / 被续驱推到 `max_steps` 后落 FAILED；
- 返终态 event（`review_passed`）→ **假装成功**：零 findings 落库、蓝图未过审却被判「待人审通过」，人审看不到任何 finding —— 最坏的静默失败；
- 返 `needs_clarification` **且先 ensure 阻塞线程** → 正确（停在本 stage、`wait_status=waiting_clarification`，人工可见可处置可续驱）。
实现：
```
    adapter = getattr(getattr(engine, "deps", None), "review", None)
    if adapter is None:
        return StageOutcome(event="needs_clarification")
    await _abp_mark_ai_reviewing(session)
    result = await adapter.review(session)
    result = result if isinstance(result, dict) else {}
    status = str(result.get("review_status") or "")
    stage_state_update = result.get("stage_state") or None
```
白名单出边：
- `status == "passed"` → `StageOutcome(event="review_passed", stage_state_update=..., current_artifact_version=result.get("artifact_version_id") or None)`；
- `status == "exhausted"` → `event="review_exhausted"`（同样带 `stage_state_update`；**绝不 failed**）；
- `status == "retry"` → `event = "repo_rework" if str(result.get("back_target") or "") == "repo_plan" else "remerge"`；
- 其余（含未知值 / adapter 异常已被吞成 `needs_clarification`）→ 先 `await _abp_ensure_blocking_clarification(session, stage="ai_review", reason=str((result.get("report") or {}).get("reason") or status or "review_incomplete"))` 再 `return StageOutcome(event="needs_clarification", stage_state_update=stage_state_update)`。
⚠️ **绝不写半截键**：`stage_state` 为空时传 `None`（照 `_h_bp_repo_plan:677-682` 的注释理由）。

**③ 新增 `"ai_review": StageDef(...)`**（`_TECHNICAL_BLUEPRINT_STAGES` 内 `merge` 之后，纯追加），形状逐字按 PATTERNS 第 3 类：
```
    # ── 阶段 4 AI 对抗审查（Phase 114-03 追加；上面九个 stage 除 merge.merged 一行外一字未动）──
    "ai_review": StageDef(
        key="ai_review",
        handler=_h_bp_ai_review,
        transitions={
            "review_passed": STAGE_DONE,        # 全清 / 仅 WARNING+INFO → pending_review
            "review_exhausted": STAGE_DONE,     # 超 max_rounds 轮 → pending_review 携未决 BLOCKER
            "repo_rework": "repo_plan",         # 仓级 BLOCKER 归因打回
            "remerge": "merge",                 # 融合级 BLOCKER 打回
            "needs_clarification": "ai_review", # self-loop 等澄清
        },
        pausable=True,
        wait_status="waiting_clarification",
    ),
```
注释一行说明「**不含 `failed` 出边**——与 `merge` 同纪律：超界是『待人审』不是『流程失败』」。

**④ `merge.transitions["merged"]` 改一行**（唯一允许的删除行）：`"merged": STAGE_DONE,` → `"merged": "ai_review",  # 114-03 已接续：融合完成先过 AI 对抗审查再升人审`。原有的「114 接续点」注释块**保留并改写为「已接续」**（体例照 `repo_confirmation.confirmed` 的 `# 113 接续点**已接续**（113-06）`，`:798-800`）。⚠️ 只改**蓝图链**的这一行；`_TECHNICAL_PLAN_STAGES` 的 `merge.merged`（`:246`）**一字不动**——rg 验收时用文件区段（`_TECHNICAL_BLUEPRINT_STAGES` 之后）区分两处同名键。

**⑤ `entrypoint.py`**：`build_blueprint_engine` 的函数内 import 追加 `from services.process_runtime.blueprint_review import BlueprintReviewAdapter`（保持既有字母序）；`SimpleNamespace(...)` 追加 `review=BlueprintReviewAdapter(node_execution_id=node_execution_id),`；**docstring 的 deps 名单同步补 ``review``**（`test_blueprint_deps_roster_matches_the_factory_docstring` 要求 docstring / `SimpleNamespace` / handler `getattr` **三方逐字一致**，漏改即红），并把「后两个是 113-06 追加的阶段 2/3」改写为「后两个是 113-06 追加的阶段 2/3，``review`` 是 114-03 追加的阶段 4」。

**⑥ `blueprint_resume.py` 追加一行**（**删除行上界 0**，唯一允许的 diff）：
```
 _STAGE_BLUEPRINT_STATUS: dict[str, str] = {
     "repo_plan": "drafting",  # == BlueprintStatus.DRAFTING
     "merge": "drafting",  # == BlueprintStatus.DRAFTING
+    # 114 追加：审查 stage 期间蓝图状态为 `ai_reviewing`（映射表追加，消费方零改动）。
+    "ai_review": "ai_reviewing",  # == BlueprintStatus.AI_REVIEWING
 }
```
值用**字面量**（模块级拿不到枚举，`:62-64` 注释已立此纪律）。`test_stage_status_table_matches_enum` 自动覆盖新行；`test_blueprint_status_stage_map` 的七条参数化断言必须继续绿。本文件**只跑 `ruff check`，不跑 `ruff format`**。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/process_runtime/test_blueprint_process_graph.py -x -q</automated>
  </verify>
  <acceptance_criteria>
- `rg -c "^async def _h_bp_" server/services/process_runtime/builtin_processes.py` == **10**（9 → 10）。
- `rg -c "^register_process_type\(" server/services/process_runtime/builtin_processes.py` == **3**（不新增注册项）。
- `rg -n "artifact_type=\"technical_plan\"" server/services/process_runtime/builtin_processes.py | rg -c ""` 与改动前相同（**不新增 `artifact_type`**，SUMMARY 登记实测值）。
- `cd server && uv run python -c "from services.process_runtime.registry import get_process_definition, STAGE_DONE, STAGE_FAILED
s = get_process_definition('technical_blueprint').stages
assert s['merge'].transitions['merged'] == 'ai_review', s['merge'].transitions
d = s['ai_review']
assert d.key == 'ai_review' and d.pausable is True and d.wait_status == 'waiting_clarification'
assert d.transitions == {'review_passed': STAGE_DONE, 'review_exhausted': STAGE_DONE, 'repo_rework': 'repo_plan', 'remerge': 'merge', 'needs_clarification': 'ai_review'}, d.transitions
assert STAGE_FAILED not in set(d.transitions.values())
from services.process_runtime import builtin_processes as bp
assert bp._TECHNICAL_PLAN_STAGES['merge'].transitions['merged'] == STAGE_DONE, '旧链被误改'
print('stage graph OK')"` 输出 `stage graph OK`。
- ⭐ `git diff server/services/process_runtime/blueprint_resume.py | rg "^-[^-]"` **必须为空**（删除行 0）；`rg -n '"ai_review": "ai_reviewing"' server/services/process_runtime/blueprint_resume.py` 命中。
- ⭐ 受限面删除行核算：`git diff server/services/process_runtime/builtin_processes.py | rg "^-" | rg -v "^---" | wc -l` **≤ 3**（`merge.merged` 一行 + 接续点注释改写 ≤2），且逐行核对全部落在 `_TECHNICAL_BLUEPRINT_STAGES["merge"]` 段内 —— SUMMARY 逐行登记。
- `git diff server/services/process_runtime/entrypoint.py | rg "^-" | rg -v "^---" | wc -l` ≤ 2（仅 docstring 断行改写），逐行登记。
- `cd server && uv run python -c "from services.process_runtime.entrypoint import build_blueprint_engine
e = build_blueprint_engine()
names = {n for n in vars(e.deps) if not n.startswith('_')}
assert 'review' in names and getattr(e.deps, 'review') is not None, names
assert '\`\`review\`\`' in (build_blueprint_engine.__doc__ or ''), 'docstring 名单漏改 → 三方一致性断言会红'
print('deps OK', sorted(names))"` 输出 `deps OK` 且含 `review`。
- `cd server && uv run python -c "from services.process_runtime.blueprint_resume import _STAGE_BLUEPRINT_STATUS as t
from delivery.models import BlueprintStatus
assert t['ai_review'] == BlueprintStatus.AI_REVIEWING == 'ai_reviewing', t
assert set(t) == {'repo_plan', 'merge', 'ai_review'}, t
print('resume map OK')"` 输出 `resume map OK`。
- `rg -n "def _abp_mark_ai_reviewing" server/services/process_runtime/builtin_processes.py` 命中；`rg -n "_abp_ensure_blocking_clarification\(" server/services/process_runtime/builtin_processes.py | rg -c ""` 比改动前 +1（新 handler 的 self-loop 前置防线存在）。
- `rg -n "session_service.transition|\.transition\(session" server/services/process_runtime/builtin_processes.py` 在 `_h_bp_ai_review` 函数体内**零命中**（engine 纯度：handler 不调 `session_service.transition`）。
- `cd server && uv run pytest tests/services/process_runtime/test_blueprint_process_graph.py -q` 全绿（含 Task 3 更新后的计数/集合/接续点断言）。
- `cd server && uv run ruff check services/process_runtime/builtin_processes.py services/process_runtime/entrypoint.py services/process_runtime/blueprint_resume.py` 通过。**这三个文件都不跑 `ruff format`**。
  </acceptance_criteria>
  <done>第 10 个 stage 注册完成且出边白名单与 `merge` 同纪律（无 failed 出边）；`merge.merged` 已接续 `ai_review` 而旧链零感知；deps 三方名单一致；`blueprint_resume` 删除行为 0 且映射与枚举等值；handler 在依赖缺失与未知状态两条路径上都停在本 stage 且先 ensure 阻塞线程。</done>
</task>

<task type="auto">
  <name>Task 3: stage 端到端测试（持续 BLOCKER 两轮转 pending_review 携未决 / merge 桶不被覆盖 / 仅 WARNING 不打回 / 去重不重开线程 / handler 四类）+ 三条既有回归断言同步</name>
  <files>server/tests/services/process_runtime/test_blueprint_review_stage.py, server/tests/services/process_runtime/test_blueprint_process_graph.py, server/tests/services/process_runtime/test_blueprint_merge_gate.py</files>
  <read_first>server/tests/services/process_runtime/test_blueprint_merge_gate.py L1-40（「守 N 件事」编号清单 + `pytestmark = [pytest.mark.django_db(transaction=True), pytest.mark.asyncio]` 的确切写法）、L440-545（stage 端到端：`_engine(**deps)` 注入 `SimpleNamespace` + 从 DB 重读断言 + 接续点断言 L541）；server/tests/services/process_runtime/test_blueprint_process_graph.py L66-79（`BLUEPRINT_STAGE_KEYS_11x` 常量分层写法）、L223-230（114 接续点断言原文）、L256-263（pausable 集合断言）、L289-293（handler/注册计数断言与其注释）、L556-570（`test_handlers_pass_through_without_deps` 参数化条目体例，含 `_h_bp_merge` 那条的注释「D-W4：merge 缺依赖既不自旋也不假装成功」）；server/tests/helpers/blueprint_samples.py（`make_blueprint` 的**确切签名与可注入字段**——构造样例复用它，不自造完整蓝图）</read_first>
  <action>
**① 新建 `server/tests/services/process_runtime/test_blueprint_review_stage.py`**，`pytestmark = [pytest.mark.django_db(transaction=True), pytest.mark.asyncio]`（⚠️ async service 跨线程写库必须 `transaction=True`），模块 docstring「守十三件事」编号清单，每条把可证伪断言写进条目：

1. **handler 四类**：`deps.review` 未注入 → `needs_clarification`；`engine.deps` 整体 `None` → `needs_clarification`；adapter 正常返回 `passed` → `review_passed` 且 `stage_state["ai_review"]` 已落；adapter **抛异常**经 engine 兜底落 `failed` 且 `error["stage"] == "ai_review"`（负向路径不许漏）。
2. ⭐ **持续 BLOCKER 不会无限循环**（CONTEXT specifics 第 2 条，本文件的头号靶子）：桩一个恒返同一条 BLOCKER finding 的判定（monkeypatch `blueprint_review.run_mechanical_rules` 返回固定 BLOCKER，`agoal_backward_review` 返 `None`），从 `current_stage="ai_review"` 连续 advance：
   - 第 1 轮 → `repo_rework` 或 `remerge`（按归因），蓝图状态 `drafting`，`stage_state["ai_review"]["round"] == 1`，**版本数未增**（回退不落版本）；
   - 第 2 轮 → 同上，`round == 2`；
   - 第 3 次进入 → `review_exhausted`，**蓝图状态 `pending_review`**、`stage_state["ai_review"]["unresolved"]` **非空**且元素只含 `{rule_id, severity, section_path, block_id, repository_id, thread_id}` 六键（**无正文**）、会话状态 **不是 `FAILED`**（`assert session.status != ConvergenceSessionStatus.FAILED`）。
   配一条对照：把 finding 改成 WARNING 后第 1 轮即 `review_passed`（**证明断言非恒真**）。
3. ⭐ **`stage_state["ai_review"]` 与 `merge` 桶互不覆盖**（P2）：预置 `stage_state = {"merge": {"count": 1, "status": "passed", "sentinel": "keep-me"}}`，跑一轮审查后从 DB 重读 `session.stage_state`：`stage_state["merge"]` **逐字不变**（含 `sentinel`），且 `stage_state["ai_review"]` 已写入。再断言 `blueprint_review.STAGE_STATE_KEY != blueprint_merge.STAGE_STATE_KEY`。
4. **仅 WARNING/INFO 不打回**：判定只产 WARNING → `review_passed`、蓝图 `pending_review`、findings 线程已建但 `blocking is False`、`ahas_open_blocking_threads(artifact) is False`（人审可通过）。
5. ⭐ **去重幂等：同一问题不重复开线程**：连续两轮同一 `(rule_id, block_id)` 的 BLOCKER → 该 artifact 上 `kind=ai_review_finding` 的线程数 **仍为 1**，且该线程的消息数 **+1**（第 2 条是 `append_note` 写的 `author_type == "ai"`）、`status` 仍为 `open`、`ahas_open_blocking_threads(...) is True`（**门仍在**）。
6. ⭐ **`record_answer` 未被用于 finding 留痕**（行为 + 源码双断言）：读 `blueprint_review.py` 源码断言 `"record_answer" not in source`；并断言第 5 条留痕后线程 `status == "open"`（若走了 `record_answer` 会变 `answered`，门就没了）。
7. **BLOCKER finding 落库形状**：线程 `severity == "blocker"`、`blocking is True`、`kind == "ai_review_finding"`、`return_stage == "ai_reviewing"`、`created_on_version_id == <当前最新版本>`、`anchor` 含 `section_path` / `block_id`。
8. **已修复的 finding 被 resolve**：第 1 轮产 finding、第 2 轮判定不再命中 → 该线程 `status == "resolved"`（`resolve_thread` 幂等，重复调用不覆盖首次结论）。
9. **`needs_clarification` 前已 ensure 阻塞线程**：桩 adapter 返 `needs_clarification` 且预先无任何线程 → handler 返回后该 artifact 上存在 `blocking=True, status=open` 的线程（否则续驱会 advance 到 `max_steps` 落 FAILED）。
10. ⭐ **review 入口消费已作答线程并产新版本（B1）**：预置一条 `status=ANSWERED` 的澄清线程（有 human 消息），桩 `section_writer` 改一段 → 进入 `ai_review` 跑一轮 → 断言：`ArtifactVersion` 行数 **+1**、新版本 `content["decision_log"]` 含该 `thread_id` 的条目（键集 ⊇ `{thread_id, question, answer, decided_at, decided_by, applied_in_version}`）、该线程 DB 重读 `status == "resolved"`、且**审查跑的是新内容**（断言 `run_mechanical_rules` 收到的 content 与新版本 content 相等，用 monkeypatch 捕获入参）。**对照**：无已作答线程时版本行数不变（证明断言非恒真）。另一条：`aapply_thread_answers` 桩成返回 `{"status": "conflict", ...}` → 本轮 `review_status == "needs_clarification"` 且**未跑规则**（`run_mechanical_rules` 的桩 `call_count == 0`）。
11. ⭐ **打回重装不抹掉人工编辑 + 冲突有线程（B3，本相位第二号靶子）**：完整链跑通——先经 114-04 的 `aapply_block_edit` 产 `human_edit:` 版本改 block X（记下其 canonical JSON）→ 制造持续 BLOCKER 使其 `repo_rework`/`remerge` 打回 → 桩 merge 侧重装产一个把 block X 改回 AI 版本的新版本 → 再次进入 `ai_review` → 断言：**当前最新版本的 block X 与人工版本逐字相等**（人工编辑未被抹掉）、新增一条 `kind=ai_clarification, blocking=True, return_stage="ai_reviewing"` 的线程（冲突有线程）、其 question **不含**两侧正文、本轮 `review_status == "needs_clarification"`。**对照**：重装未触碰 block X 时 → 无冲突线程、`arestore_human_blocks` 返 `unchanged`/`noop`、审查照常出结论（证明断言非恒真）。
12. ⭐ **新版本落库后线程被重锚（B3 第二件，两条路径都要测）**：
- **12a（0-a 产版本路径）**：0-a 落新版本且新内容里某 block_id 改名 → 断言 `areanchor_threads` `await_count == 1`、入参 `old_content` 为上次已重锚版本、`stage_state["ai_review"]["anchored_version_id"]` 被回写为新版本 id
- **12b ⭐（merge 重装主路径，B3 点名场景）**：构造「**仅 merge 重跑产新版本、无已作答线程、无人工块**」（0-a/0-b 均不产版本）→ 断言 `areanchor_threads` **仍被调用一次**（这条用例专门证伪「以本轮是否产版本为判据」的错误实现——若判据写错，本用例必红）
- **12c（无版本推进）**：`anchored_version_id` 已等于最新版本 id → 断言 `areanchor_threads` `await_count == 0`（不做无谓重锚）
- `areanchor_threads` 桩成抛异常时**审查仍出正常 `review_status`** 且 `anchored_version_id` **未被回写**（best-effort 不阻断、下轮可重试）。
13. **状态转移全经 lifecycle + CAS 不外泄**：monkeypatch 让首次 `transition` 抛 `ConcurrentBlueprintTransitionError` → 审查仍返回合法 `review_status`（重试或降级 `needs_clarification`），会话**不是 FAILED**；并断言 `blueprint_review.py` 源码无 `Artifact.objects.filter(...).update(` 之类裸改状态。

**② 同步更新既有回归断言**（三处，全部属「接续点断言随接续更新」，逐处在 SUMMARY 登记）：
- `test_blueprint_process_graph.py`：`:66-79` 追加 `BLUEPRINT_STAGE_KEYS_114 = ("ai_review",)` 并并入 `BLUEPRINT_STAGE_KEYS`；`:223-230` `test_merge_merged_is_the_114_handoff_point` 改为断言 `merged == "ai_review"` 并**追加**「旧链 `_TECHNICAL_PLAN_STAGES["merge"].transitions["merged"] == STAGE_DONE`」的正向对照（证明只改蓝图链）+ 断言 `stages["ai_review"].transitions["review_exhausted"] == STAGE_DONE`；`:259` pausable 集合追加 `"ai_review"`；`:289-293` handler 计数 `9 → 10`（注释改为「7（112-05）+ 2（113-06）+ 1（114-03）= 10」），register 计数**保持 3**；`:556-561` 参数化追加 `(bp._h_bp_ai_review, "needs_clarification")` 并配注释「D-W4 同款：审查缺依赖既不自旋也不假装通过」；模块 docstring 的编号清单相应补一条。
- `test_blueprint_merge_gate.py:541`：`stages["merge"].transitions["merged"] == STAGE_DONE` → `== "ai_review"`，其余断言**一字不动**。
⚠️ 这两个测试文件**只允许上述定点改动**：`git diff <file> | rg "^-[^-]"` 的删除行必须逐行落在上述位置并在 SUMMARY 登记。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/process_runtime/test_blueprint_review_stage.py tests/services/process_runtime/test_blueprint_process_graph.py tests/services/process_runtime/test_blueprint_merge_gate.py -x -q</automated>
  </verify>
  <acceptance_criteria>
- `cd server && uv run pytest tests/services/process_runtime/test_blueprint_review_stage.py -q` 全绿。
- `rg -n "review_exhausted" server/tests/services/process_runtime/test_blueprint_review_stage.py` 命中；`rg -n "pending_review" server/tests/services/process_runtime/test_blueprint_review_stage.py` 命中；`rg -n "!= ConvergenceSessionStatus.FAILED|is not ConvergenceSessionStatus.FAILED" server/tests/services/process_runtime/test_blueprint_review_stage.py` 命中（超界不落 FAILED 被断言）。
- `rg -n "sentinel" server/tests/services/process_runtime/test_blueprint_review_stage.py` 命中（merge 桶不被覆盖的哨兵断言存在）。
- `rg -n "record_answer" server/tests/services/process_runtime/test_blueprint_review_stage.py` 命中（源码扫描断言存在）。
- `rg -n "transaction=True" server/tests/services/process_runtime/test_blueprint_review_stage.py` 命中。
- `rg -n "ConcurrentBlueprintTransitionError" server/tests/services/process_runtime/test_blueprint_review_stage.py` 命中（CAS 不外泄用例存在）。
- `rg -c "def test_" server/tests/services/process_runtime/test_blueprint_review_stage.py` ≥16。
- ⭐ `rg -n "aapply_thread_answers" server/tests/services/process_runtime/test_blueprint_review_stage.py` 命中 ≥2（B1 入口消费的正反并列）；`rg -n "decision_log" server/tests/services/process_runtime/test_blueprint_review_stage.py` 命中。
- ⭐ `rg -n "arestore_human_blocks|human_edit:" server/tests/services/process_runtime/test_blueprint_review_stage.py` 命中 ≥2（B3 重装不抹人工块的正反并列）；`rg -n "areanchor_threads" server/tests/services/process_runtime/test_blueprint_review_stage.py` 命中（重锚接线被断言）。
- `cd server && uv run pytest tests/services/process_runtime/test_blueprint_process_graph.py -q` 全绿；`rg -n "== 10" server/tests/services/process_runtime/test_blueprint_process_graph.py` 命中（handler 计数已更新）；`rg -n "BLUEPRINT_STAGE_KEYS_114" server/tests/services/process_runtime/test_blueprint_process_graph.py` 命中。
- `cd server && uv run pytest tests/services/process_runtime/ tests/delivery/ tests/test_blueprint_settings.py -q` 全绿（`test_blueprint_status_stage_map` 七条参数化 + `test_stage_status_table_matches_enum` + 112-05 的 deps 三方一致性 + 113-06 的覆盖率门一并背书）。
- `cd server && uv run pytest tests/ -q` 无新增失败（全量相位门；失败项须与本 plan 前的基线逐条比对并在 SUMMARY 登记）。
- `cd server && uv run python manage.py makemigrations --check --dry-run` 退出码 0。
- 冻结面自检：`git diff --name-only` 只含本 plan 声明的九个文件，且**不含** `codegraph/services/repo_router_v2.py`、`process_runtime/{decompose_segments,research_adapter,architect_merge_adapter,merged_plan,clarify_adapter,render,resume,blueprint_schema,blueprint_quality,blueprint_merge,blueprint_reconcile,blueprint_confirm_gate,blueprint_spec_gate,blueprint_repo_plan,blueprint_charter_match}.py`、`delivery/services/{blueprint_lifecycle_service,blueprint_anchor}.py`、`repositories/services/charter_service.py`、`system/settings_service.py`、`agents/call_source.py`、`delivery/models/`、`task/`。
- 受限面删除行汇总：`for f in server/services/process_runtime/{builtin_processes,entrypoint,blueprint_resume}.py server/delivery/services/event_taxonomy.py server/system/models.py; do echo "$f: $(git diff "$f" | rg '^-' | rg -v '^---' | wc -l)"; done` → `blueprint_resume.py` 必须为 **0**，`event_taxonomy.py` / `system/models.py` 必须为 **0**，`builtin_processes.py` ≤3，`entrypoint.py` ≤2；全部逐行登记进 SUMMARY。
  </acceptance_criteria>
  <done>「持续 BLOCKER 两轮后转 `pending_review` 携未决清单且会话不是 FAILED」与「`merge` 桶不被覆盖」两条无限循环防线被单测锁死；同一问题第 2 轮不重开线程且留痕后门仍在；handler 四类含负向路径全绿；三条接续点回归断言同步更新且旧链有正向对照；受限面删除行核算达标。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `merge` 产出 → `ai_review` stage 输入 | 半可信蓝图跨入审查；`merge` 的 `exhausted` 也走这条边，输入可能是未达标半成品 |
| 审查结论 → stage 出边 | 出边决定「打回 / 升人审 / 停等澄清」，写错等于流程失控 |
| `stage_state` 桶 | 轮次计数是有界回退的唯一依据；桶被覆盖 = 无限循环 |
| adapter → 线程/状态写入 | 唯一合法通道是 `BlueprintLifecycleService`（INV-6） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-114-14 | Denial of Service | 审查轮次塞进 `stage_state["merge"]` 桶被 `_build_stage_state` 整桶覆盖 ⇒ 计数归零 ⇒ **无限打回循环** | mitigate | `STAGE_STATE_KEY = "ai_review"`（实测未占用）+ `rg '"merge"'` 零命中验收 + Task 3 第 3 条哨兵断言 + `STAGE_STATE_KEY` 不等式断言 |
| T-114-15 | Tampering | 超界落 `STAGE_FAILED` / `ai_review` 引入 `failed` 出边 ⇒ 蓝图成果连带报废 | mitigate | `transitions` 无 failed 出边（`test_blueprint_chain_has_no_failed_edge_but_old_chain_still_does` 全图扫描守护）+ `rg "STAGE_FAILED"` 在 adapter 零命中 + Task 3 第 2 条断言会话不是 FAILED |
| T-114-16b | Elevation of Privilege | `aapply_thread_answers` 无生产调用方 ⇒ 人在打回轮之间的作答永不被消费、答案不落地、同一问题被反复问（B1） | mitigate | `review()` 的 0-a 步无条件调用 + `await` 语句级 rg 验收 + 入口顺序断言（在读基线之前）+ Task 3 第 10 条正反并列（有已作答线程 → 版本 +1 且 decision_log 有条目；无则版本不变） |
| T-114-16c | Tampering | 打回重装（`repo_rework`/`remerge`）抹掉人工编辑、且线程不重锚 ⇒ 用户修改静默消失 + 批注全部错位（B3） | mitigate | 0-b 调 `arestore_human_blocks`（保留/写回 + 冲突开阻塞线程）、0-c **以版本推进为判据**调 `areanchor_threads`（`anchored_version_id` 比对，覆盖 merge 重装主路径）；**不改 `blueprint_merge.py`**（`git diff` 为空）；Task 3 第 11 条断言「人工块逐字相等 + 冲突有线程」并配「重装未触碰该块 → 无冲突线程」对照；第 12a/12b/12c 三条覆盖「0-a 产版本 / **仅 merge 重装** / 无版本推进」三种重锚判据 |
| T-114-16 | Elevation of Privilege | finding 留痕误用 `record_answer` ⇒ BLOCKER 被推到 `answered` ⇒ confirm 守卫放行、pause 判据失守 | mitigate | prohibitions 明令 + `append_note` 唯一通道 + Task 3 第 6 条源码扫描 + 行为断言（留痕后仍 `open`、门仍在）+ 114-01 守卫判据②纵深覆盖 `answered` 的 blocker finding |
| T-114-17 | Denial of Service | `needs_clarification` self-loop 无阻塞线程 ⇒ 续驱 advance 到 `max_steps=20` 落 FAILED | mitigate | `_abp_ensure_blocking_clarification(stage="ai_review", ...)` 前置（幂等）+ Task 3 第 9 条断言 |
| T-114-18 | Tampering | deps 名单漂移（`review` 漏注入 / docstring 漏改）⇒ handler 恒 pass-through 静默空转 | mitigate | `entrypoint` 三方一致性（docstring / SimpleNamespace / handler getattr）+ `test_blueprint_deps_roster_matches_the_factory_docstring` + Task 2 的 deps 断言 |
| T-114-19 | Tampering | `blueprint_resume` 顺手「整理」既有两行或改 `_resolve_stage_status` ⇒ 阶段 2/3 会话被拉回「调研中」（T-113-43 复现） | mitigate | 删除行上界 **0** 的 `git diff | rg "^-[^-]"` 硬验收 + 只跑 `ruff check` + 七条参数化等价性断言继续绿 |
| T-114-20 | Info Disclosure | findings 正文进 `stage_state` / 事件 payload / 日志 | mitigate | `unresolved` 六键无正文 + payload 只放计数与分级分布 + `duration_ms` 与标量五件套 + `redact_secrets_in_text` 兜底异常文本 + Task 3 第 2 条断言 unresolved 元素键集 |
| T-114-21 | Denial of Service | `transition` CAS 冲突未捕获 ⇒ engine 把会话落 FAILED | mitigate | `ConcurrentBlueprintTransitionError` / `ValueError` 均吞成 `needs_clarification` 并重试一次 + Task 3 第 10 条用例 |
| T-114-22 | Tampering | 旧 `technical_plan` 链被误改（`_TECHNICAL_PLAN_STAGES["merge"].merged`） | mitigate | 旧链正向对照断言 + `_TECHNICAL_PLAN_SNAPSHOT` 冻结快照测试 + 受限面删除行逐行登记 |
| T-114-SC | Tampering | 依赖供应链 | accept | 零新增外部依赖、零 migration（`SettingKeys` 是纯常量类） |
</threat_model>

<verification>
- `cd server && uv run pytest tests/services/process_runtime/ -q` 全绿（stage 图 / 覆盖率门 / reconcile / resume 映射一并背书）。
- `cd server && uv run pytest tests/delivery/ tests/test_blueprint_settings.py -q` 全绿（114-01 线程底座 + 112-05 确认门八端点 + 设置键快照）。
- `cd server && uv run pytest tests/ -q` 无新增失败（与本 plan 前基线逐条比对，差异登记）。
- `cd server && uv run python manage.py makemigrations --check --dry-run` 退出码 0（**零 migration**）。
- `cd server && uv run ruff check services/process_runtime/ delivery/services/ system/models.py tests/services/process_runtime/` 通过；`ruff format --check` **只对** `services/process_runtime/blueprint_review.py` 与 `tests/services/process_runtime/test_blueprint_review_stage.py` 跑。
- 受限面删除行核算（见 Task 3 验收命令）：`blueprint_resume.py` = 0、`event_taxonomy.py` = 0、`system/models.py` = 0、`builtin_processes.py` ≤3、`entrypoint.py` ≤2，逐行登记归属。
- 冻结面自检：`git diff --name-only` 只含本 plan 的九个文件。
- 在途会话检查（行为变更前置）：执行前跑 `cd server && uv run python manage.py shell -c "from delivery.models import ConvergenceSession; print(list(ConvergenceSession.objects.filter(process_type='technical_blueprint', current_stage='merge').values_list('id', flat=True)))"`，非空则在 SUMMARY 登记受影响会话 id（它们下次 advance 会多走一个 stage）。
</verification>

<success_criteria>
- `ai_review` 成为蓝图链第 10 个 stage：`merge` 完成后先过审查再升人审，出边白名单五条且**不含 failed**，旧 `technical_plan` 链零感知。
- ⭐ **入口三件接线成立**：已作答线程在 review 入口被消费产新版本（B1 —— `aapply_thread_answers` 不再是孤儿）；人工编辑的 block 在重装后被保留、冲突时开阻塞线程（B3）；新版本落库后线程被批量重锚（CLAR-02 不失锚）。`blueprint_merge.py` 与 `blueprint_reflow.py` / `blueprint_lifecycle_service.py` 全程零改动，本 plan 只接线。
- findings 变成可分级、可锚定、可去重的划线线程；同一问题第 2 轮走 `append_note` 留痕而**不重开线程、不改线程状态**（门始终有效）。
- 有界修订被证伪为「不会无限循环」：持续 BLOCKER 两轮后走 `review_exhausted` → 蓝图 `pending_review` + 未决清单可见 + 会话不是 FAILED；`merge` 桶不被覆盖。
- 仅 WARNING/INFO 时直接升人审、不打回，且人审不被无关线程挡住（`blocking is False`）。
- 受限三文件的改动上界达标（`blueprint_resume` 删除行 0、`builtin_processes` 只加注册与新 handler）；deps 三方名单一致，无静默空转。
- 114-04/05 可以直接消费：`stage_state["ai_review"]` 的桶形状、findings 线程的 `severity`/`anchor`/`created_on_version`、三个 `blueprint.review.*` 事件、`BLUEPRINT_REVIEW_CONFIG` 配置键。
</success_criteria>

<output>
完成后创建 `.planning/phases/114-ai/114-03-SUMMARY.md`（provides：`BlueprintReviewAdapter.__init__` / `review()` 的**逐字签名与恒定返回键**、**入口三件接线的调用顺序与各 `status` 的分流表（`reflow` / `guard` 的 `conflict` → `needs_clarification`）**、`stage_state["ai_review"]` 桶的**确切键集与 `unresolved` 元素六键**、`ai_review` StageDef 的 transitions 全表、`_h_bp_ai_review` 的 status→event 映射表、`_abp_mark_ai_reviewing` 的前置补跳逻辑、三个 `blueprint.review.*` 事件常量名与 payload 键集、`SettingKeys.BLUEPRINT_REVIEW_CONFIG` 的 value JSON 形状与 `MAX_REVIEW_ROUNDS` 兜底值、`_decide_back_target` 的两档判据、去重索引的构造方式（rule_id 如何从既有线程反查）、**受限面删除行逐行登记**（文件 + 行号 + 归属 + 理由）、三条既有回归断言的**更新前后对照**、`_h_bp_` 计数 9→10 与 register 保持 3 的实测输出、在途 `current_stage="merge"` 会话清单、全量 `pytest tests/ -q` 与基线的差异比对结果）。
</output>
