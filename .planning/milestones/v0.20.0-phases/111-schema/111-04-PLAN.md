---
phase: 111-schema
plan: 04
type: execute
wave: 2
depends_on: [111-01, 111-02, 111-03]
files_modified:
  - server/services/process_runtime/blueprint_quality.py
  - server/delivery/management/__init__.py
  - server/delivery/management/commands/__init__.py
  - server/delivery/management/commands/evaluate_blueprint_golden.py
  - server/tests/fixtures/blueprint_golden/gaokao_boost.json
  - server/tests/services/test_blueprint_quality.py
  - server/tests/delivery/test_evaluate_blueprint_golden.py
  - server/tests/delivery/test_blueprint_integration.py
autonomous: true
requirements: [GATE-02]
prohibitions:
  - "禁止修改 server/codegraph/services/repo_router_v2.py（STATE.md 并行纪律 1）"
  - "禁止修改 process_runtime 六个冻结文件（纪律 2）"
  - "禁止修改 ConvergenceSessionEvent 既有类型/字段（纪律 3）"
  - "golden fixtures 独立目录 server/tests/fixtures/blueprint_golden/，禁止与 v0.19.0 路由 golden set 共文件/共目录（CONTEXT 锁定，零冲突面）"
  - "command 内禁止内联指标算法（全部走 blueprint_quality 纯函数，command 只做 IO 与编排）；未过门槛禁止静默 exit 0"
  - "migration 零新增（本 plan 不动任何 models）"
must_haves:
  truths:
    - "evaluate_blueprint_golden 离线可跑（无 LLM/无网络，天然过 --disable-socket）：逐 case 输出引用覆盖率/目标仓命中率并按 expected 门槛判定，未过门槛非零退出（GATE-02）"
    - "首条 golden case 为高三提分专项：期望 direct 仓集合含 onion-learning 与 study-course，断言机制级（命中率阈值）而非逐仓全等"
    - "同输入重复运行结果一致（command 内置双跑一致性断言，ROADMAP SC5）"
    - "schema→artifact 落库→11 态转移→execution_plan 派生的全链路冒烟通过（三个 wave-1 plan 的接缝无断裂）"
  artifacts:
    - path: "server/services/process_runtime/blueprint_quality.py"
      provides: "citation_coverage / target_repo_hit_rate 纯函数 + AI 打回率/人审修改量/澄清轮次 DB 统计接口占位"
      exports: ["citation_coverage", "target_repo_hit_rate"]
    - path: "server/delivery/management/commands/evaluate_blueprint_golden.py"
      provides: "golden set 离线评估 command（CommandError 非零退出）"
    - path: "server/tests/fixtures/blueprint_golden/gaokao_boost.json"
      provides: "高三提分专项首条 golden case（合法 blueprint/v1 样例 + expected 块）"
      contains: "onion-learning"
  key_links:
    - from: "server/delivery/management/commands/evaluate_blueprint_golden.py"
      to: "server/services/process_runtime/blueprint_schema.py + blueprint_execution.py + blueprint_quality.py"
      via: "validate_blueprint → derive_technical_plan_document → 指标纯函数（command 零算法）"
      pattern: "from services.process_runtime"
    - from: "server/tests/delivery/test_blueprint_integration.py"
      to: "ArtifactService + BlueprintLifecycleService + blueprint_execution"
      via: "golden fixture 驱动的端到端冒烟"
      pattern: "BlueprintLifecycleService"
---

<objective>
建立蓝图质量基线并验收三个 wave-1 底座的接缝：blueprint_quality 指标纯函数（引用覆盖率/目标仓命中率 + 后续相位的 DB 统计接口占位）、高三提分专项首条 golden case、evaluate_blueprint_golden 离线评估 command（含确定性双跑断言与非零退出门槛）、以及 schema→落库→状态机→派生的全链路集成冒烟。

Purpose: GATE-02——质量标尺先建（对齐 v0.19.0 Phase 105 方法论），112–116 每个相位的产出退化可被回归检出。
Output: 一个纯函数模块 + delivery 首个 management command（含包 __init__ 两枚）+ 一条 golden fixture + 三个测试文件。
</objective>

<execution_context>
工作目录：worktree 根。wave 2：依赖 111-01（validate_blueprint/derive/样例工厂）、111-02（LifecycleService/线程模型）、111-03（无直接代码依赖，按相位完整性排后）。
</execution_context>

<context>
@.planning/phases/111-schema/111-CONTEXT.md（锁定：golden 路径/command 名/首条 case/指标口径/DB 统计留接口）
@.planning/phases/111-schema/111-RESEARCH.md（落点地图 #3/#18/#19，Open Question Q3——期望仓取值口径，测试策略 command 范式）
@.planning/technical-blueprint/DESIGN.md §5.7 动机表（高三提分专项实证：onion-learning/study-course/onion-practice/study-plan 等仓名与误报根因——fixture 取材依据）
@server/codegraph/management/commands/measure_extractor_precision.py（评估型 command 权威样板：参数/advisory 跳过/指标 report/非零退出）
@server/tests/repositories/test_rebuild_repo_summaries.py（call_command + StringIO 测试范式）
@server/tests/helpers/blueprint_samples.py（111-01 产出的样例工厂）
@.planning/phases/111-schema/111-01-SUMMARY.md（若存在：blueprint_schema/execution 实际导出面）
@.planning/phases/111-schema/111-02-SUMMARY.md（若存在：LifecycleService 实际签名）
</context>

<tasks>

<task type="auto">
  <name>Task 1: blueprint_quality.py 指标纯函数 + 单测</name>
  <files>server/services/process_runtime/blueprint_quality.py, server/tests/services/test_blueprint_quality.py</files>
  <read_first>server/services/process_runtime/wave_layering.py（纯函数模块形态）、server/services/process_runtime/blueprint_schema.py（iter_blocks 可复用性——同包顶层 import 允许）</read_first>
  <action>
新建 `server/services/process_runtime/blueprint_quality.py`。docstring：GATE-02 / 指标口径出处 CONTEXT；前半纯函数（无 ORM），后半 DB 统计接口占位（依赖 delivery models 的懒 import，数据由 112–114 填充）。`__all__` 显式导出。

① `citation_coverage(blueprint: dict) -> float`：关键结论 = `current_state_analysis[].findings[]` + `repo_associations[]`（rationale 级，citations 取 `rationale.citations`）+ `impact_analysis.affected_features[]` 三类条目；分子 = 其中 `citations` 为非空 list 的条目数，分母 = 条目总数；分母为 0 返回 1.0（空文档视为无缺口，docstring 注明约定）。逐字段 .get 防御，绝不抛。

② `target_repo_hit_rate(blueprint: dict, expected_direct_repo_names: list[str]) -> float`：实际 direct 集合 = `{ra["repository_name"] for ra in repo_associations if ra.get("role") == "direct"}`；命中率 = `len(expected ∩ actual) / len(expected)`；expected 为空返回 1.0。

③ DB 统计接口占位（独立分节 + 注释「数据由 112–114 填充，本相位仅定义签名」）：`ai_rejection_rate(artifact_id: str) -> float | None`、`human_edit_volume(artifact_id: str) -> int | None`、`clarification_rounds(artifact_id: str) -> int | None`——docstring 写清口径（AI 审查打回轮次占比 / 人工编辑版本数 / 澄清线程轮次），函数体 `return None`（或带 TODO 注释的查询骨架），不做任何 ORM 顶层 import。

新建 `server/tests/services/test_blueprint_quality.py`（纯函数）：`make_blueprint()` 全引用样例 → coverage == 1.0；剥掉一个 finding 的 citations → 分子减一的精确比值；三类条目全空 → 1.0；hit_rate：完全命中 1.0 / 半命中 0.5 / expected 空 1.0 / role 全 indirect → 0.0；占位函数返回 None。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/test_blueprint_quality.py -x -q</automated>
  </verify>
  <acceptance_criteria>
- `cd server && uv run pytest tests/services/test_blueprint_quality.py -x -q` 全绿。
- `rg -n "^from delivery|^from django|^import django" server/services/process_runtime/blueprint_quality.py` 零命中（顶层无 ORM）。
- `rg -c "def citation_coverage|def target_repo_hit_rate|def ai_rejection_rate|def human_edit_volume|def clarification_rounds" server/services/process_runtime/blueprint_quality.py` 等于 5。
  </acceptance_criteria>
  <done>两个可用指标纯函数 + 三个占位接口，口径有 docstring、边界有测试。</done>
</task>

<task type="auto">
  <name>Task 2: 高三提分专项 golden fixture + evaluate_blueprint_golden command + command 测试</name>
  <files>server/tests/fixtures/blueprint_golden/gaokao_boost.json, server/delivery/management/__init__.py, server/delivery/management/commands/__init__.py, server/delivery/management/commands/evaluate_blueprint_golden.py, server/tests/delivery/test_evaluate_blueprint_golden.py</files>
  <read_first>server/codegraph/management/commands/measure_extractor_precision.py（add_arguments/report/exit 分层——照抄结构）、server/tests/repositories/test_rebuild_repo_summaries.py L25-59（call_command 测试范式）、DESIGN.md §5.7 动机表（仓名与功能点取材）</read_first>
  <action>
① 新建 `server/tests/fixtures/blueprint_golden/gaokao_boost.json`——首条 golden case（高三提分专项）。顶层结构：
`{"name": "gaokao_boost", "description": "高三提分专项（DESIGN §5.7 路由试验实证语料）", "blueprint": {…}, "expected": {…}}`。
- `blueprint`：一份**必须通过 validate_blueprint** 的完整 blueprint/v1 content（手写 id，不依赖 ulid）：meta（title=「高三提分专项」、project_id 任意 uuid）；requirement_spec 含 ≥3 个 feature_points（fp_01 培优课入口改造〔brownfield，落 onion-learning〕、fp_02 专项学习页〔greenfield，落 study-course〕、fp_03 专项练习〔落 onion-practice〕，各带 acceptance_criteria）；repo_associations：direct 三仓 onion-learning/study-course/onion-practice（各带 rationale.text + rationale.citations + fitness.verdict=suitable）+ indirect 一仓 study-plan（capabilities_used，对应 §5.7「权益鉴权归 study-course」的边界实证——rationale 注明不选它做 direct 的理由）；current_state_analysis 每仓 ≥1 条 finding（带 citations，其中 onion-learning 的 finding 引用「培优课（即将上线）占位入口 learn-textbook-sync」实证）；implementation_overview ≥3 个 items（feature_point_id 对齐 fp_01–03、change_type 覆盖 modify 与 create、含 files_touched/depends_on/wave）；api_contracts ≥1 条（provided）；impact_analysis（business_impact + ≥1 affected_features 带 citations）；interaction_flows ≥1 条（steps 含 seq/actor/action）；must_haves 三键齐；citations 池 ≥5 条（覆盖上述全部引用 id，source_type 用 repo_file/knowledge_entity/repo_charter 混合）。
- `expected`：`{"direct_repos": ["onion-learning", "study-course", "onion-practice"], "required_feature_point_ids": ["fp_01", "fp_02"], "min_citation_coverage": 0.9, "min_repo_hit_rate": 1.0}`（Q3 口径：onion-learning/study-course 必在；断言机制级阈值，不做逐仓全等）。

② 新建 `server/delivery/management/__init__.py` 与 `server/delivery/management/commands/__init__.py`（空 docstring 文件；delivery 现无 management 目录）。

③ 新建 `server/delivery/management/commands/evaluate_blueprint_golden.py`（镜像 measure_extractor_precision.py 结构；中文 help）：
- `add_arguments`：`--fixtures-dir`（default = `Path(__file__).resolve().parents[3] / "tests" / "fixtures" / "blueprint_golden"`，即 server/tests/fixtures/blueprint_golden）、`--output-json`（可选输出路径）。
- `handle` 逐 `*.json` case：(a) `validate_blueprint(blueprint)` 必须 `(True, None)`；(b) `derive_technical_plan_document(blueprint)` 必须 `(doc, None)`；(c) **确定性双跑**——再派生一次，两次 `json.dumps(doc, sort_keys=True, ensure_ascii=False)` 必须逐字节相等（ROADMAP SC5「同输入重复运行结果一致」内建为门槛）；(d) `citation_coverage(blueprint) >= expected["min_citation_coverage"]`；(e) `target_repo_hit_rate(blueprint, expected["direct_repos"]) >= expected["min_repo_hit_rate"]`；(f) `expected["required_feature_point_ids"] ⊆ 实际 feature_points id 集合`。
- fixture 加载独立函数容错（坏 JSON 记为该 case 失败，不 crash 全局）；fixtures 目录不存在/为空 → `self.stderr.write` + `raise CommandError`（golden 基线缺失是硬错误，不做 advisory 跳过）。
- 输出：逐 case 一行（name + 各指标值 + PASS/FAIL）+ 汇总 JSON（`self.stdout.write(json.dumps(report, ensure_ascii=False, indent=2))`）；`--output-json` 时同时写文件；任一 case FAIL → `raise CommandError(f"{n} 个 golden case 未过门槛")`（非零退出）。structlog `logger.info("blueprint_golden_evaluated", category="caller", component="process_runtime", initiated_by_user_id="system", total=…, failed=…, duration_ms=…)`。指标算法零内联（全部调 blueprint_quality/blueprint_schema/blueprint_execution）。
- 全程无 LLM/无网络/无 DB 写（纯函数 + 文件读），天然过 --disable-socket。

④ 新建 `server/tests/delivery/test_evaluate_blueprint_golden.py`（call_command 范式，无需 django_db）：默认目录跑 `call_command("evaluate_blueprint_golden", stdout=buf)` → 不抛且输出含 "gaokao_boost" 与 "PASS"；连续两次调用输出逐字节一致（离线确定性）；tmp_path 造坏 case（缺 interaction_flows 的 blueprint + 同 expected）→ `pytest.raises(CommandError)`；tmp_path 造 expected.min_repo_hit_rate=1.0 但 blueprint 只含一个 direct 仓且不在期望集合 → CommandError（门槛判定生效）；`--fixtures-dir` 指向空目录 → CommandError。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/delivery/test_evaluate_blueprint_golden.py -x -q && uv run python manage.py evaluate_blueprint_golden</automated>
  </verify>
  <acceptance_criteria>
- `cd server && uv run python manage.py evaluate_blueprint_golden` 退出码 0 且 stdout 含 "gaokao_boost"。
- `cd server && uv run pytest tests/delivery/test_evaluate_blueprint_golden.py -x -q` 全绿。
- fixture 合法性：`cd server && uv run python -c "import json,pathlib; from services.process_runtime.blueprint_schema import validate_blueprint; d=json.loads(pathlib.Path('tests/fixtures/blueprint_golden/gaokao_boost.json').read_text()); ok,err=validate_blueprint(d['blueprint']); assert ok, err"` 通过。
- `rg -c "onion-learning" server/tests/fixtures/blueprint_golden/gaokao_boost.json` ≥ 1 且 `rg -c "study-course" server/tests/fixtures/blueprint_golden/gaokao_boost.json` ≥ 1。
- `ls server/tests/fixtures/blueprint_golden/ | grep -vc json` 等于 0（目录独立、只有 json case，不混 0.19 路由 golden）。
  </acceptance_criteria>
  <done>首条 golden case 离线可跑、指标输出、门槛非零退出、重复运行一致——GATE-02 基线成立（打回率等指标接口已占位待 112–114 填数）。</done>
</task>

<task type="auto">
  <name>Task 3: 全链路集成冒烟（schema→落库→状态机→派生）+ 相位收口验证</name>
  <files>server/tests/delivery/test_blueprint_integration.py</files>
  <read_first>server/tests/delivery/test_blueprint_lifecycle_service.py（111-02 产出的 service 用法）、server/tests/delivery/test_blueprint_artifact_wiring.py（111-01 产出的落库用法）、server/tests/fixtures/blueprint_golden/gaokao_boost.json（Task 2 产出）</read_first>
  <action>
新建 `server/tests/delivery/test_blueprint_integration.py`（`pytestmark = pytest.mark.django_db(transaction=True)`）——三个 wave-1 plan 接缝的端到端冒烟，golden fixture 驱动（单一事实源，不再手造第二份大样例）：

① **schema→落库**：读 gaokao_boost.json 的 blueprint → `ArtifactService.create(artifact_type="technical_plan", content=…)` 成功；同 content 删掉 `must_haves` → `pytest.raises(ArtifactContentInvalid)`。
② **落库→状态机**：对上述 artifact 用 `BlueprintLifecycleService.transition` 走主干 `""→researching→drafting→ai_reviewing→pending_review`；建一条 open+blocking 的 BlueprintThread（kind=repo_confirmation）→ confirm 抛 ValueError；线程置 resolved → confirm 成功且 acting_user 进 BlueprintReviewer（DB 重读断言）。
③ **状态机→派生**：confirmed 后 `derive_technical_plan_document(blueprint)` 返回 `(doc, None)`；doc.execution_plan 的 repository_name 集合 == fixture 三个 direct 仓名；双跑 `json.dumps(sort_keys=True)` 一致。
④ **diff 与版本演进**：`add_version` 传入改过一个 finding text（保 block_id）的 blueprint → `diff_blueprint_blocks` modified 恰命中该 block_id。
⑤ **观测面**：transition 传入真实 ConvergenceSession → ConvergenceSessionEvent 新增行 event=="blueprint.status.transitioned"（既有事件类型零改动的共存证明）。

相位收口（本 task 的 verify 一并执行）：delivery/repositories/services 三目录全量测试 + migration 完备性检查。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/delivery/test_blueprint_integration.py -x -q && uv run pytest tests/delivery/ tests/repositories/ tests/services/ -q && uv run python manage.py makemigrations --check --dry-run</automated>
  </verify>
  <acceptance_criteria>
- `cd server && uv run pytest tests/delivery/test_blueprint_integration.py -x -q` 全绿。
- 相位门（RESEARCH Sampling Rate）：`cd server && uv run pytest tests/delivery/ tests/repositories/ tests/services/ -q` 全绿。
- `cd server && uv run python manage.py makemigrations --check --dry-run` 退出码 0。
- 冻结面终检：`git diff --name-only $(git merge-base HEAD origin/main 2>/dev/null || echo HEAD~20) -- server/ | rg "repo_router_v2|decompose_segments|research_adapter|architect_merge_adapter|merged_plan|clarify_adapter|render\.py|convergence_session_event"` 零命中（六冻结文件 + 路由核心 + 事件模型全程未动；rg 无命中时退出码 1 即为通过）。
  </acceptance_criteria>
  <done>golden fixture 驱动的全链路冒烟通过，三目录全量绿，migration 完备，冻结面零触碰——Phase 111 可交 verifier。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| golden fixtures 文件 | 仓库内 JSON 数据，只读进纯函数 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-111-10 | Tampering | fixtures 注入 | mitigate | fixtures 只作数据读入纯函数，无 eval/无模板渲染（RESEARCH 安全域先例）；坏 JSON 容错为 case 失败 |
| T-111-11 | Repudiation | 基线被静默放水 | mitigate | 未过门槛 CommandError 非零退出；确定性双跑内建门槛 |
| T-111-SC | Tampering | 依赖供应链 | accept | 零新增依赖 |
</threat_model>

<verification>
- Task 3 的 verify 即相位门：三目录全量测试 + `manage.py evaluate_blueprint_golden` 退出码 0 + makemigrations --check 干净 + 冻结面 rg 零命中。
</verification>

<success_criteria>
- GATE-02：golden set 与质量指标基线建立——首条 case 高三提分专项、引用覆盖率/目标仓命中率可算、打回率等接口占位、退化（schema 拒绝/派生漂移/覆盖率跌破/命中率跌破）可被 command 非零退出检出（ROADMAP SC5 全量）。
- Phase 111 五条 Success Criteria 的机器全部就位且集成冒烟串通。
</success_criteria>

<output>
完成后创建 `.planning/phases/111-schema/111-04-SUMMARY.md`（provides：blueprint_quality 导出面 + golden 评估入口，供 112–116 每相位回归复用）。
</output>
