---
phase: 116-entry
plan: 07
type: execute
wave: 6
depends_on: ["116-06"]
files_modified:
  - server/services/repo_file_read.py
  - server/mcp_tools/views.py
  - server/repositories/repo_file_views.py
  - server/repositories/urls.py
  - server/tests/repositories/test_repo_file_read_views.py
  - web/src/api/repositoryChunks.ts
  - web/src/components/blueprint/citation/CitationCodePreview.vue
  - web/src/components/blueprint/__tests__/citationPreview.spec.ts
  - web/src/locales/zh-CN.json
autonomous: true
requirements: [VIEW-02]
prohibitions:
  - "⭐ **`PHASE_BASE` 纪律（所有边界/删除行核算的前提，B5）**：GSD 逐 Task 原子提交 ⇒ 收口自检跑时本 plan 的改动**早已 committed**，裸 `git diff` 恒空 ⇒ 所有冻结面、删除行、`--name-only` 边界断言都会**静默恒真**。⇒ 本 plan **第一条执行步骤**是 `PHASE_BASE=$(git rev-parse HEAD)` 并把该 sha 逐字记进 SUMMARY；下文所有边界检查一律写作 `git diff $PHASE_BASE -- <file>` / `git diff $PHASE_BASE --name-only`。⚠️ 同类坑：`rg -c` / `grep -c` **无命中时退出码为 1**（`rg -c` 还不打印任何东西）⇒ 计数型断言一律 `| grep -c '<pat>' || true` 再比对数字，⛔ 不写 `... | rg -c \"...\" == 0`"
  - "⭐ **本 plan 是本相位最后一个、且必须可独立顺延的 plan**：⛔ **不得有任何前面六个 plan 的产物依赖它**。整体回退本 plan 后，后端与前端四道门必须**逐字回到 116-06 的基线** —— ⭐ **这条要实跑验证**（形状照 115-07 的可顺延性验证：临时分支整体回退 → 跑门 → 比对 → 恢复工作区）"
  - "⭐ **若最终决定顺延，⛔ 不得默默丢掉**：必须同时改 `REQUIREMENTS.md` VIEW-02 里那句顺延目标（planning 阶段已把它细化为「Phase 116 的 116-07-PLAN.md」并写了逃生条款；顺延时改成里程碑收尾的独立工作项，⛔ 不许让「顺延 Phase 116」在 116 结束后仍挂着）。⚠️ **本条的触发条件恰好是「本 plan 不被执行」⇒ 它写在这里永远轮不到自己执行**（B6）⇒ **兜底责任已上移一层**：`116-06-PLAN.md` 的 `<output>` 里有一条**相位出口检查**，要求 116-06 的 SUMMARY 给出本 plan 的去留结论、并在「顺延」这一支里给出改写后的 VIEW-02 与其 commit。本条保留是为了「本 plan 真被执行、但中途决定回退」这种情形"
  - "⛔ ⭐ **`is_excluded` 取 `chunk_at` 的中性口径，⛔ 不取 MCP 的 404 `file_excluded`**：仓内两个既有口径**不一样** —— `chunk_at_views.py:5-9` 的 docstring 明写「被排除文件与『无命中』对外**不可区分**，两者统一返回 200 空」；MCP `get_repository_file`（`mcp_tools/views.py:1005-1009`）是 **404 `file_excluded`** 显式告知。SPA 引用预览面取**前者**（与 115-07 的「非 200 不进错误分档」一致，且引用预览本就有快照兜底）"
  - "⛔ ⭐ **不复制一份排除判定**（违反 fail-closed 单一实现纪律）：把 `GetRepositoryFileView` 的方法体**下沉**成 `services/repo_file_read.py` 的模块函数，MCP View 改为调用它。⭐ **service 返回中性结构而不是 `Response`**，由两个 View 各自映射成自己的错误口径"
  - "⛔ ⭐ **MCP `get_repository_file` 的对外契约逐字不变**：抽服务层会改一个 MCP 面 ⇒ **必须回归 `TOOL_SCHEMA_SNAPSHOT` 守门**；响应键集、错误码（含 `file_excluded` 的 404）、`error_response` 的 `code` 取值**一个都不改**。核算判据是「`tests/mcp_tools/` 相对基线无新增失败且 snapshot 条目零改动」"
  - "⛔ **不改 `services/chunk_lookup.py`**（`chunk_at` 面的既有 fail-closed 实现）、⛔ 不改 `repositories/chunk_at_views.py`（它是 analog，不是改造对象）"
  - "⛔ **新端点必须挂仓库可见性/权限闸**：⭐ 按 `repositories/permissions.py` 与既有仓库读面的口径取（执行期实读，⛔ 不猜）；未认证一律拒；⛔ 不因为「引用预览是只读」就放宽"
  - "⛔ **不新增 migration、不新增运行时依赖**（⛔ **不引 CodeMirror / 高亮库** —— 本 plan 只补正文与行高亮所需的**数据**，呈现沿用 115-03 已建的 `<pre class=\"font-mono\">` + 行号列形态）"
  - "⛔ **正文与路径不进日志**：只记 `path_len` / `line_start` / `line_end` / `content_len` / `truncated` 等标量；异常文本过 `redact_secrets_in_text`"
  - "⚠️ **P-15**：跑完 `pnpm` 后 `git status` 检查并 `git checkout -- web/pnpm-workspace.yaml`；⚠️ `pnpm build` 会重写 `web/src/components.d.ts`（STATE 115-06 环境项），还原后按字典序手工插入自己那几行"
must_haves:
  truths:
    - "⭐ **全仓确无按 `path + 行区间` 读源码正文的 SPA 面**（RESEARCH §E.3 实测坐实）：`chunk_at_views.py:57-60` 只返回 `{path, line, chunks}`，`chunks` 项是 `{chunk_id, file_path, line_start, line_end, chunk_index}` —— **不带正文**；唯一带 `content` 的 `POST /repositories/<id>/search/` 是向量搜索（必须给 query、已重排过滤，无法按 path + 行区间取）；`get_repository_file` 是 **PAT 认证的 MCP 工具**，SPA 的 cookie-JWT 走不通。⇒ 这是一个**真·新增端点**"
    - "⭐ **成本比 CONTEXT 估的高：必须先抽服务层**（No Analog #6）。`GetRepositoryFileView`（`mcp_tools/views.py:978-1090+`）的全部逻辑——`_excluded_response` / `_read_from_mirror` / `_get_indexed_repo` / `_resolve_graph_branch` / Qdrant chunk 拼接回退——**全部是 View 的方法**，没有可 import 的服务层。⇒ 新建 `services/repo_file_read.py` 把方法体下沉成模块函数，MCP View 改为调用它；⛔ 复制一份违反 fail-closed 单一实现纪律。**这正是本 plan 值得独立顺延的理由**"
    - "⭐ **service 的返回是中性结构不是 `Response`**：建议 `{\"status\": \"ok\" | \"excluded\" | \"not_found\" | \"unavailable\", \"content\": str, \"path\": str, \"line_start\": int, \"line_end\": int, \"truncated\": bool, \"detail\": str}`（恒定键闭集）。两个 View 各自映射：MCP → **404 `file_excluded`**（既有口径，逐字不变）；SPA → ⭐ **`excluded` 与 `not_found` 统一 200 空**（中性口径，与 `chunk_at` 同源）"
    - "⭐ **排除判定必须对 requested 与 resolved 两个路径都复判**（防后缀解析绕过，T-22-21 —— `_excluded_response` 的既有 docstring 逐字写着这条）；⭐ **命中排除绝不返回任何 content**；命中时调 `log_exclusion_blocked(surface=..., repository_id=..., rel_path=...)`（既有埋点，`surface` 按调用面区分）"
    - "⭐ **新端点**：`GET /api/repositories/<uuid:repository_id>/file-lines/?path=&line_start=&line_end=&branch_name=`（name 待定，建议 `repository-file-lines`）。参数校验**前置 400 不触 service**（照 `chunk_at_views.py:33-52` 的逐个前置校验）：`path` 缺失 → 400；`line_start` / `line_end` 非正整数或 `line_end < line_start` → 400。⭐ **区间上界必须有硬上限**（模块级常量，建议 ≤ 400 行；超出即截断并置 `truncated: true`，⛔ 不 400 —— 截断是可用降级，报错不是）"
    - "⭐ **fail-closed 中性口径**：被排除文件 / 文件不存在 / 仓库无镜像 —— 三者对外**不可区分**，统一 **200** + `{path, line_start, line_end, lines: [], truncated: false}`（⛔ 不 404、⛔ 不带 `detail` 区分）。理由逐字写进模块 docstring：与 `chunk_at` 的存在性防线同源，且 115-07 的前端按「非 200 不进错误分档」实现"
    - "⭐ **响应形状对齐前端已有的渲染形态**：`lines: [{line_no: int, text: str}]`（115-03 的 `CitationCodePreview` 已经是「`<pre class=\"font-mono\">` + 行号列」）⇒ ⛔ **不返回整块字符串让前端切**（前端切行会与后端的行号口径分叉）"
    - "⭐ **MCP 面零契约漂移**：`get_repository_file` 的响应键集、`file_excluded` 的 404、`error_response` 的 `code` 取值**一个都不改**；`TOOL_SCHEMA_SNAPSHOT` 的该条目**零改动**。核算判据是「`tests/mcp_tools/` 相对基线无新增失败」（⚠️ `test_skills_snapshot_guard::test_skill_files_discovered` 是**既有**环境项，P-16）"
    - "⭐ **前端 `CitationCodePreview.vue` 从降级形态升级为真正的代码预览**：`locator` 有 `repository_id` + `path` + `line_start`/`line_end` ⇒ 调新端点取正文并渲染，⭐ **行高亮**落在 citation 指向的那几行；⛔ **任何失败一律快照兜底且不关弹窗**（115-03 立的既有纪律）；⛔ 不回显后端错误体（会泄露内部路径与参数校验细节，T-115-23）"
    - "⭐ **组件 docstring 的「本相位显式降级形态」整段改写**：保留证据链的**历史结论**（为什么 115 做不到）并注明「116-07 已补上源码正文读面」；⛔ 不留过时话术"
    - "⭐ **可独立顺延性实跑验证**（照 115-07 的先例）：临时分支整体回退本 plan 的全部改动 → 跑后端全量门 + 前端四道门 → 断言**逐字回到 116-06 基线** → 恢复工作区。结果与命令记进 SUMMARY"
    - "全量后端门相对基线 **8609 passed / 1 failed** 无新增失败；前端门 `vitest` 相对基线 **1674 passed / 1 skipped** 无新增失败、`type-check` exit 0、`build` 通过、`lint` 在触及文件上零新增问题；`makemigrations --check --dry-run` 退出码 0"
  artifacts:
    - path: "server/services/repo_file_read.py"
      provides: "从 GetRepositoryFileView 下沉的服务层：排除判定（requested + resolved 双复判、命中绝不返 content）/ 镜像读取 / 索引仓解析 / 分支解析 / Qdrant chunk 拼接回退；返回中性结构（恒定键闭集）而不是 Response"
      contains: "is_excluded"
    - path: "server/mcp_tools/views.py"
      provides: "GetRepositoryFileView 的方法体改为调用 services.repo_file_read（⭐ 对外契约逐字不变：响应键集 / file_excluded 的 404 / error_response 的 code）"
      contains: "repo_file_read"
    - path: "server/repositories/repo_file_views.py"
      provides: "GET file-lines/ 端点：adrf APIView + IsAuthenticated + 仓库权限闸 + 参数前置 400 + 区间硬上限截断 + ⭐ 被排除/不存在/无镜像统一 200 空（中性口径，与 chunk_at 同源）+ lines[{line_no,text}] 响应"
      contains: "line_start"
    - path: "server/tests/repositories/test_repo_file_read_views.py"
      provides: "未认证拒 + 参数前置 400 五例 + ⭐ 被排除与不存在返回逐字相同的 200 空 + 区间上限截断 truncated=true + 行号口径与 line_start 对齐 + MCP 面契约零漂移的回归断言"
      contains: "truncated"
    - path: "web/src/components/blueprint/citation/CitationCodePreview.vue"
      provides: "真正的代码预览：取正文 + 行号列 + ⭐ citation 区间行高亮；任何失败一律快照兜底不关弹窗、不回显错误体；docstring 降级段改写"
      contains: "line_start"
  key_links:
    - from: "server/mcp_tools/views.py"
      to: "server/services/repo_file_read.py"
      via: "GetRepositoryFileView 方法体下沉后改为调用同一份实现（⛔ 不复制第二份排除判定）"
      pattern: "repo_file_read"
    - from: "server/repositories/repo_file_views.py"
      to: "server/services/repo_file_read.py"
      via: "同一份 service，SPA 面把 excluded/not_found 映射成中性 200 空（与 MCP 的 404 file_excluded 分道）"
      pattern: "repo_file_read"
    - from: "web/src/components/blueprint/citation/CitationCodePreview.vue"
      to: "web/src/api/repositoryChunks.ts"
      via: "新增 getRepositoryFileLines(repositoryId, {path, lineStart, lineEnd}) 并复用既有 usable 式判据封装"
      pattern: "file-lines"
---

<objective>
补上 115 从 SC-3 顺延过来的最后一块：**按 `path + 行区间` 读源码正文的 SPA 读面**，让引用预览从「文件路径 + 行号区间 + 引用快照」升级为**真正带正文与行高亮**的代码预览。

⭐ **本 plan 是本相位最后一个、且必须可独立顺延的 plan**（CONTEXT 明令）。理由不是它不重要，而是它的**真实成本比 CONTEXT 估的高**：RESEARCH §E.3 实测，`GetRepositoryFileView`（`mcp_tools/views.py:978-1090+`）的全部逻辑——`_excluded_response` / `_read_from_mirror` / `_get_indexed_repo` / `_resolve_graph_branch` / Qdrant chunk 拼接回退——**全部是 View 的方法，没有可 import 的服务层**。新 SPA 端点要么先把这套下沉成 `services/repo_file_read.py`（会改一个 MCP 面、需回归 `TOOL_SCHEMA_SNAPSHOT` 守门），要么复制一份（⛔ 违反 fail-closed 单一实现纪律）。⇒ 抽服务层，并把「可独立顺延」做成**实跑验证过的事实**而不是声明。

两处必须先拍板的口径：

① ⭐ **`is_excluded` 的两个既有口径不一样，必须选一个**。`chunk_at`（`chunk_at_views.py:5-9`）是「被排除与无命中**统一 200 空**」——docstring 明写这是为了不泄漏存在性；MCP `get_repository_file`（`:1005-1009`）是 **404 `file_excluded`** 显式告知。⇒ SPA 引用预览面取**前者**（与 115-07 的「非 200 不进错误分档」一致，且引用预览本就有快照兜底）；MCP 面**逐字保持后者**。service 因此返回**中性结构而不是 `Response`**，由两个 View 各自映射。

② ⭐ **区间上界超出时截断而不是报错**。引用的行区间来自半可信的 citation `locator`，一个写错的区间不该让预览整个失败。⇒ 硬上限 + `truncated: true`，⛔ 不 400。

Purpose: VIEW-02 的最后一块（源码正文与行高亮）闭环。若本 plan 最终被顺延，⛔ **不得默默丢掉** —— 必须同时改 `REQUIREMENTS.md` VIEW-02 的顺延目标（planning 阶段已写好逃生条款）。
Output: 1 个新建 service + 1 个新建 View + 1 处 MCP 面下沉改造 + 1 处路由追加 + 1 个新建后端测试 + 3 处前端（1 API 纯追加 + 1 组件升级 + 1 spec 扩写）+ i18n 追加。**零 migration、零新依赖（⛔ 不引 CodeMirror / 高亮库）。**
</objective>

<execution_context>
工作目录：worktree 根 `/Users/zaneliu/Projects/open-source/friday-ai/.claude/worktrees/v0.20-blueprint`，分支 `milestone/v0.20.0-blueprint`。⛔ 绝不读写主检出。

⭐ **第一条执行步骤（先于读任何源码）：`PHASE_BASE=$(git rev-parse HEAD)`，把该 sha 逐字记进 SUMMARY。** 本 plan 内所有 `git diff` 形态的边界 / 删除行 / 冻结面断言一律写作 `git diff $PHASE_BASE -- <file>`；裸 `git diff` 在逐 Task 原子提交之后恒空、断言会静默恒真（B5）。计数型断言用 `| grep -c '<pat>' || true`。

**wave 6，依赖 116-06**：功能上完全独立，排在最后是因为**它必须是最后一个可独立顺延的 plan**（CONTEXT 明令），且与 116-06 共同改 `mcp_tools/views.py`（文件重叠 ⇒ 隐式依赖后置）。

**测试基线**：后端 **8609 passed / 1 failed**（唯一失败 `tests/mcp_tools/test_skills_snapshot_guard.py::test_skill_files_discovered` 是 worktree 环境产物 —— ⚠️ **本 plan 改动 `mcp_tools/`，务必逐条核对它是同一条既有的红**，P-16）；前端 **1674 passed / 1 skipped**、`type-check` exit 0、`build` 通过、`eslint .` **111 problems**（判据是**触及文件零新增问题**）。

⚠️ **本 plan 的基线是 116-06 收口后的值** —— 前六个 plan 会往上加用例，执行前先复核 116-06-SUMMARY 里登记的实际数字，以它为准。

**受限面配额（硬约束）**：

| 文件 | 允许的改动 | 删除行上界 |
|---|---|---|
| `server/mcp_tools/views.py` | ⭐ **仅** `GetRepositoryFileView` 的五个方法体下沉（对外契约逐字不变） | **90**（方法体整体下沉） |
| `server/repositories/urls.py` | import 追加 + `urlpatterns` 追加 **1** 项 + 一行注释 | **0** |
| `web/src/api/repositoryChunks.ts` | 追加 1 个函数（+ 类型） | **0** |
| `web/src/components/blueprint/citation/CitationCodePreview.vue` | 取正文 + 行高亮 + docstring 降级段改写 | **20** |
| `web/src/components/blueprint/__tests__/citationPreview.spec.ts` | 扩写代码预览相关用例 | **8** |
| `web/src/locales/zh-CN.json` | 新键纯追加 | **0** |
| `server/services/chunk_lookup.py` / `server/repositories/chunk_at_views.py` | **零改动**（`git diff $PHASE_BASE -- <该文件>` 必须为空） | — |
| `server/mcp_tools/serializers.py` | **零改动**（`get_repository_file` 的 snapshot 条目一字不动） | — |

⚠️ 签名与字段名一律按下列**实测**取，不得猜测：

- ⭐ `GetRepositoryFileView`（`mcp_tools/views.py:978-1090+`）的**五个方法全文**：`_excluded_response`（`:988-1010`，⭐ docstring 逐字「resolved_path 必须复判（防后缀解析绕过，T-22-21）。命中绝不返回任何 content」；用 `_exclusion_matcher(repository_id)` + `matcher.is_excluded(path)` + `log_exclusion_blocked(surface=..., repository_id=..., rel_path=...)` + `error_response("file_excluded", "文件已被排除策略屏蔽", status_code=404)`）、`_read_from_mirror`、`_get_indexed_repo`、`_resolve_graph_branch`、Qdrant chunk 拼接回退段。
- `repositories/chunk_at_views.py` **全文 61 行** —— ⭐ **新 View 的逐字 analog**：`from adrf.views import APIView`、`permission_classes = [IsAuthenticated]`、`aget_object_or_404(Repository, id=..., is_deleted=False)`、参数逐个前置 400（`{"error": ...}` 而不是 `{"detail": ...}` —— ⚠️ **错误体键是 `error`**，115-02 已登记这条：`ApiError.detail` 会回落成无意义的 `'请求失败'` ⇒ ⛔ 前端任何调用点不得回显）、⭐ docstring `:5-9` 的**中性口径说明书**。
- `repositories/urls.py:11` 的 import 与 `:286-292` 的 `chunk-at` 路由与「紧随 chunk-at，UUID 通配安全」的顺序注释。
- `repositories/permissions.py` —— 仓库读面的权限口径（**执行期实读，⛔ 不猜**）。
- `services/chunk_lookup.py:44,70` 的排除判定口径（**只读**）。
- `web/src/api/repositoryChunks.ts` —— 115-02 建的 `getChunkAt` 与 ⭐ **`{chunks, usable}` 的判据封装形态**（新函数照同款封装，⛔ 不让调用点各自判）。
- `web/src/components/blueprint/citation/CitationCodePreview.vue` **全文** —— ⭐ `:1-24` 的降级 docstring（本 plan 改写它）、`locator` 逐键收窄的写法、`CitationFallback` 的兜底用法、`Skeleton` 的加载态、⛔ 「任何分支都不回显后端错误体（T-115-23）」。
- `web/src/components/blueprint/__tests__/citationPreview.spec.ts` —— 既有代码预览用例（本 plan 扩写而不是重写）。
</execution_context>

<context>
@.planning/phases/116-entry/116-RESEARCH.md（⭐ §E.3 全节：全仓确无可用面的三条证据 + 成本比 CONTEXT 估的高 + 两个 `is_excluded` 口径必须选一个）
@.planning/phases/116-entry/116-PATTERNS.md（E3/E4 全节：`chunk_at_views.py` 作「中性口径说明书」的 analog、`_excluded_response` 的代码片与 T-22-21、No Analog #6、§0.4 追加点表）
@.planning/phases/116-entry/116-CONTEXT.md（specifics 第 2 条：「拆成可独立顺延的最后一个 plan，但不得默默丢掉 —— 若顺延，必须同时改 `REQUIREMENTS.md` VIEW-02 里那句『顺延 Phase 116』」）
@server/mcp_tools/views.py（⭐ `:978-1090+` `GetRepositoryFileView` **五个方法全文** —— 下沉的对象；`error_response` 的用法与 `code` 取值）
@server/repositories/chunk_at_views.py（**全文 61 行，逐字 analog**：docstring 的中性口径说明书、参数前置 400、错误体键是 `error`）
@server/repositories/urls.py（`:11` import、`:286-292` `chunk-at` 路由与顺序注释）
@server/repositories/permissions.py（仓库读面权限口径）
@server/services/chunk_lookup.py（`:44,70` 排除判定 —— **只读**）
@web/src/api/repositoryChunks.ts（`getChunkAt` 与 `{chunks, usable}` 的判据封装）
@web/src/components/blueprint/citation/CitationCodePreview.vue（**全文**：降级 docstring、`locator` 收窄、`CitationFallback` 兜底、⛔ 不回显错误体）
@web/src/components/blueprint/__tests__/citationPreview.spec.ts（既有用例）
@.planning/phases/115-ui/115-07-SUMMARY.md（⭐ §9 **可独立顺延性的实跑验证记录** —— 本 plan 照它的形状做一遍）
@.planning/phases/116-entry/116-06-SUMMARY.md（前六个 plan 收口后的**实际基线数字** —— 本 plan 的门以它为准）
@.planning/REQUIREMENTS.md（VIEW-02 的顺延目标措辞 —— 若本 plan 被顺延必须改它）
</context>

<tasks>

<task type="auto">
  <name>Task 1: services/repo_file_read.py 服务层下沉 + MCP View 改调（对外契约逐字不变）</name>
  <files>server/services/repo_file_read.py, server/mcp_tools/views.py</files>
  <read_first>server/mcp_tools/views.py **L960-1120（或到 `GetRepositoryFileView` 结束）全文**（⭐ 下沉的对象：`tool_name` 与 `post` 三段式、⭐ **`_excluded_response:988-1010` 全文**（`_exclusion_matcher` 的取法、`matcher.is_excluded` 的调用、`log_exclusion_blocked` 的**逐字参数**、`error_response("file_excluded", ..., status_code=404)` 的**逐字文案与 code**、docstring 里 T-22-21 的双复判要求）、`_read_from_mirror` 的**镜像路径解析与读文件的完整实现**（含编码、二进制判定、大文件处理）、`_get_indexed_repo` 的**索引仓解析**、`_resolve_graph_branch` 的**分支解析**、**Qdrant chunk 拼接回退段**的完整逻辑与它的降级条件、以及整个 `_handle` 的响应键装配（⭐ **响应键集要逐字记下来**，下沉后必须一字不差））；server/mcp_tools/serializers.py（`get_repository_file` 的 `TOOL_SCHEMA_SNAPSHOT` 条目 —— ⭐ **只读，本 plan 一字不动**，它是「契约零漂移」的核算基准）；server/services/chunk_lookup.py **L40-80**（`_exclusion_matcher` 与排除判定的既有口径 —— **只读**，确认下沉后复用的是同一份）；server/services/ 目录下任一既有 service（模块 docstring / `__all__` / `_COMPONENT` / 日志五件套的本目录惯例）</read_first>
  <action>
**① 新建 `server/services/repo_file_read.py`。**

模块 docstring 四段：(a) 用途——「按 `path`（+ 可选行区间）读仓库源码正文的**唯一实现**；从 `mcp_tools/views.GetRepositoryFileView` 下沉，MCP 面与 SPA 面共享它」；(b) ⭐ **两个调用面的口径分道**——「service 返回**中性结构**；MCP 面把 `excluded` 映射成 **404 `file_excluded`**（既有对外契约，逐字不变），SPA 面把 `excluded` 与 `not_found` **统一映射成 200 空**（中性口径，与 `chunk_at_views.py:5-9` 的存在性防线同源）。⛔ **两个口径不得互相污染**」；(c) ⭐ **fail-closed 纪律**——「排除判定对 **requested 与 resolved 两个路径都复判**（防后缀解析绕过，T-22-21）；**命中排除绝不返回任何 content**」；(d) 观测——「⛔ 路径与正文不进日志，只记 `path_len` / `content_len` / `truncated`；命中排除时调既有 `log_exclusion_blocked`，`surface` 由调用方传入以区分调用面」。

`_COMPONENT = "repo_file_read"`；`__all__` 显式列公开函数。

**公开入口**（签名按实读的 View 方法参数收敛，⛔ 不多不少）：

```
async def aread_repository_file(
    repository_id: str, path: str, *, branch_name: str = "", surface: str = "",
    line_start: int | None = None, line_end: int | None = None, max_lines: int | None = None,
) -> dict
```

⭐ **恒定返回键（闭集）**：`{"status": "ok" | "excluded" | "not_found" | "unavailable", "path": str, "resolved_path": str, "content": str, "lines": list[dict], "line_start": int, "line_end": int, "truncated": bool, "detail": str}`
- `status == "excluded"` / `"not_found"` / `"unavailable"` 时 ⭐ **`content` 与 `lines` 必须为空**（命中排除绝不返回任何 content）。
- `line_start` / `line_end` 为 `None` ⇒ 返回整文件（MCP 面的既有行为）；给了区间 ⇒ 只返回该区间的 `lines`，并按 `max_lines` 截断置 `truncated`。
- ⭐ `lines` 项形状 `{"line_no": int, "text": str}`（**1-based**，与 citation 的 `line_start` 口径对齐）。

**下沉的五块**（逐块从 View 方法体搬过来，⛔ **不改逻辑、只改返回形态**）：`_excluded_response` → `_acheck_excluded(...) -> bool`（命中时调 `log_exclusion_blocked` 并返 True）；`_read_from_mirror`；`_get_indexed_repo`；`_resolve_graph_branch`；Qdrant chunk 拼接回退。⚠️ **搬运时逐行核对**：任何一处 `Response` / `error_response` 的构造都要改成设置 `status` 与 `detail`，⛔ 不得把 DRF 对象留在 service 里。

**② `GetRepositoryFileView` 改为调 service（删除行 ≤90）。**

- `post` / `_handle` 的骨架保留；五个方法体删除，改为一次 `result = await aread_repository_file(..., surface="get_repository_file")`。
- ⭐ **映射回既有对外契约，逐字不变**：`excluded` → `error_response("file_excluded", "文件已被排除策略屏蔽", status_code=404)`（**逐字**）；`not_found` / `unavailable` → 各自既有的 code 与文案（**从实读的 View 里逐字抄，⛔ 不重新发明**）；`ok` → 既有响应键集**一字不差**。
- ⭐ **核算判据**：`server/mcp_tools/serializers.py` 的 `get_repository_file` snapshot 条目 `git diff $PHASE_BASE -- server/mcp_tools/serializers.py` 里**无改动行**，且 `tests/mcp_tools/` 相对基线无新增失败。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/mcp_tools/ -q --deselect tests/mcp_tools/test_skills_snapshot_guard.py::test_skill_files_discovered && uv run pytest tests/mcp_tools/test_schema_snapshot.py -q</automated>
  </verify>
  <acceptance_criteria>
- ⭐ **MCP 契约零漂移**：`git diff $PHASE_BASE -- server/mcp_tools/serializers.py` **为空**；`cd server && uv run pytest tests/mcp_tools/ -q` 相对基线**无新增失败**（⚠️ `test_skills_snapshot_guard::test_skill_files_discovered` 逐条核对是同一条既有环境项并在 SUMMARY 登记）。
- ⭐ **`file_excluded` 的 404 逐字保留**：`rg -n 'file_excluded' server/mcp_tools/views.py` 命中；`rg -n '文件已被排除策略屏蔽' server/mcp_tools/views.py` 命中（文案一字未改）。
- ⭐ **service 不返回 DRF 对象**：`rg -n "Response\(|error_response\(|rest_framework" server/services/repo_file_read.py` **零命中**。
- ⭐ **排除判定只有一份**：`rg -c "is_excluded" server/services/repo_file_read.py` ≥ **1**；`rg -c "is_excluded" server/mcp_tools/views.py` == **0**（下沉后 View 里不再有）；`git diff $PHASE_BASE -- server/services/chunk_lookup.py` **为空**。
- ⭐ **双路径复判仍在**：`cd server && uv run python -c "
src = open('services/repo_file_read.py').read()
assert 'resolved' in src, 'resolved_path 复判必须保留（T-22-21）'
i = src.index('is_excluded')
seg = src[max(0,i-800):i+800]
assert seg.count('is_excluded') >= 1
print('dual path recheck OK')"` 输出 `dual path recheck OK`；且 SUMMARY 里逐条登记「五块方法体的搬运对照（原行号 → 新函数名）」。
- ⭐ **命中排除绝不返 content**：写一条 service 级用例（可放 Task 2 的测试文件）断言 `status == "excluded"` 时 `result["content"] == ""` 且 `result["lines"] == []`。
- **恒定键闭集**：`cd server && uv run python -c "
src = open('services/repo_file_read.py').read()
for k in ('status','path','content','lines','line_start','line_end','truncated','detail'):
    assert '\"%s\"' % k in src, k
print('constant keys OK')"` 输出 `constant keys OK`。
- **正文与路径不进日志**：`rg -n "path_len|content_len" server/services/repo_file_read.py` 命中；`cd server && uv run python -c "
import ast
tree = ast.parse(open('services/repo_file_read.py').read())
bad = []
for n in ast.walk(tree):
    if isinstance(n, ast.Call) and isinstance(n.func, ast.Attribute) and n.func.attr in ('info','warning','error','debug','exception'):
        for kw in n.keywords or []:
            if kw.arg in ('content','path','text','source'):
                bad.append((kw.arg, ast.unparse(kw.value)))
assert not bad, bad
print('no raw path/content in logs')"` 输出 `no raw path/content in logs`（⚠️ `log_exclusion_blocked(rel_path=...)` 是**既有埋点**，属既有行为，逐条人工核对后登记豁免）。
- **删除行核算**：`git diff $PHASE_BASE -- server/mcp_tools/views.py | grep -c '^-[^-]' || true` 输出 ≤ **90**（逐块登记）。
- `cd server && uv run ruff check services/repo_file_read.py mcp_tools/views.py` 通过；`uv run ruff format --check services/repo_file_read.py` 通过。
  </acceptance_criteria>
  <done>`repo_file_read.py` 就位且是排除判定的**唯一实现**（双路径复判保留、命中绝不返 content、返回中性结构不含 DRF 对象）；MCP View 改为调它而**对外契约逐字不变**（`file_excluded` 404 与文案一字未改、snapshot 条目零改动、`tests/mcp_tools/` 无新增失败）。</done>
</task>

<task type="auto">
  <name>Task 2: repositories/repo_file_views.py 新端点（中性口径 + 区间截断）+ 路由 + 后端测试</name>
  <files>server/repositories/repo_file_views.py, server/repositories/urls.py, server/tests/repositories/test_repo_file_read_views.py</files>
  <read_first>server/repositories/chunk_at_views.py **全文 61 行**（⭐ **逐字 analog**：docstring `:1-10` 的中性口径说明书体例、`from adrf.views import APIView` + `permission_classes = [IsAuthenticated]`、`aget_object_or_404(Repository, id=repository_id, is_deleted=False)`、⭐ **参数逐个前置 400 的确切写法与错误体键 `error`**（不是 `detail`）、末尾「被排除文件与无命中均返回空 chunks」的注释与响应装配）；server/repositories/permissions.py **全文**（⭐ 仓库读面的权限口径 —— 新端点挂哪一个，⛔ 不猜；若既有读面只用 `IsAuthenticated`，照它并在 docstring 里写明「与 `chunk-at` 同口径」）；server/repositories/urls.py **L1-30 与 L280-300**（import 块体例、`chunk-at` 路由与 ⭐ 「紧随 chunk-at，UUID 通配安全」的顺序注释纪律）；server/services/repo_file_read.py（Task 1 交付：`aread_repository_file` 的逐字签名与恒定返回键）；server/tests/repositories/（既有仓库端点测试的形状 analog：如何造 `Repository` 与镜像、如何 patch 排除规则）</read_first>
  <action>
**① 新建 `server/repositories/repo_file_views.py`（1 端点）。**

模块 docstring（⭐ 照 `chunk_at_views.py:1-10` 的**说明书体例**）：

- 端点：`GET /api/repositories/<uuid:repository_id>/file-lines/?path=&line_start=&line_end=&branch_name=` —— 「按 `path` + 行区间读源码正文，供引用二级预览渲染带行号与高亮的代码片段（VIEW-02，116-07）」。
- ⭐ **安全语义**（逐条写清）：`permission_classes` 与仓库读面同口径（按 `repositories/permissions.py` 实读取）；未认证 401/403；⭐ **被排除文件 / 文件不存在 / 仓库无镜像三者对外不可区分** —— 统一 **200** + `{path, line_start, line_end, lines: [], truncated: false}`，避免存在性泄漏（与 `chunk_at_views` 的 T-25-05 同源）；参数缺失或非法 → **400**（不触 service，与 T-25-07 同源，⚠️ **错误体键是 `error`**）。
- ⭐ **区间上界**：模块级常量 `_MAX_LINES`（建议 **400**）；`line_end - line_start + 1 > _MAX_LINES` ⇒ **截断到上界并置 `truncated: true`**，⛔ **不 400**（引用区间来自半可信 citation `locator`，一个写错的区间不该让预览整个失败）。

实现：`started = time.monotonic()` → `aget_object_or_404(Repository, id=..., is_deleted=False)` → 参数逐个前置 400（`path` 缺失 / `line_start` 缺失或非正整数 / `line_end` 缺失或非正整数 / `line_end < line_start`）→ `result = await aread_repository_file(str(repository_id), path, branch_name=..., surface="blueprint_citation_preview", line_start=..., line_end=..., max_lines=_MAX_LINES)` → 按 `result["status"]` 映射：`ok` → 200 + `{path, line_start, line_end, lines, truncated}`；`excluded` / `not_found` / `unavailable` → ⭐ **同一个 200 空结构**（⛔ 三者响应体**逐字相同**）→ 一条 caller 事件（`repository_id` / `path_len` / `line_start` / `line_end` / `line_count` / `truncated` / `duration_ms`，⛔ **path 与正文不进日志**）。

**② `repositories/urls.py` 追加一条路由（删除行 0）**：import 追加；紧随 `chunk-at` 之后追加 + 一行注释（照既有「紧随 chunk-at，UUID 通配安全」的顺序纪律），`name="repository-file-lines"`。

**③ 新建 `server/tests/repositories/test_repo_file_read_views.py`**，docstring「守十件事」：

1. **未认证一律拒**（401/403）。
2. **参数前置 400 五例**：缺 `path` / 缺 `line_start` / `line_start` 非正整数 / 缺 `line_end` / `line_end < line_start`。⭐ 断言错误体键是 **`error`**（与 `chunk_at` 同口径）。
3. ⭐ **三种不可用情形返回逐字相同的 200 空**（本文件头号靶子）：(a) 文件被排除规则挡掉；(b) 文件不存在；(c) 仓库无镜像 ⇒ `resp_a.json() == resp_b.json() == resp_c.json()` 且 `status_code == 200`、`lines == []`。⛔ 三者**不可区分**。
4. ⭐ **命中排除绝不返回任何 content**（service 级 + 端点级各一条）。
5. **正路**：真实镜像文件 + 区间 `10..20` ⇒ `lines` 有 11 项、`line_no` 从 **10** 开始（1-based 与 citation 口径对齐）、`truncated is False`。
6. ⭐ **区间上界截断**：请求 `1..10000` ⇒ `len(lines) == _MAX_LINES` 且 `truncated is True`、**状态码仍 200**（⛔ 不 400）。
7. **文件末尾越界**：`line_end` 超过文件行数 ⇒ 只返回到最后一行、`truncated is False`（⛔ 不报错）。
8. ⭐ **双路径复判**（T-22-21）：构造一个 `requested_path` 不命中排除但 `resolved_path` 命中的场景 ⇒ 仍返回空（⛔ 不泄露 content）。
9. ⭐ **MCP 面契约零漂移的回归**：同一个被排除文件，MCP `get_repository_file` 仍返 **404 `file_excluded`**、SPA 面返 **200 空** —— 两个口径并列断言（证明分道生效且互不污染）。
10. **观测**：一条 caller 事件带 `path_len` 而**不带** `path` 原文（AST 或日志捕获断言）。
  </action>
  <verify>
    <automated>cd server && uv run python -c "
import os, django
os.environ.setdefault('DJANGO_SETTINGS_MODULE','friday.settings'); django.setup()
from django.urls import reverse
print(reverse('repository-file-lines', args=['11111111-1111-1111-1111-111111111111']))
" && uv run pytest tests/repositories/test_repo_file_read_views.py -x -q && uv run pytest tests/repositories/ -q
    </automated>
  </verify>
  <acceptance_criteria>
- `reverse('repository-file-lines', ...)` 输出 `/api/repositories/<uuid>/file-lines/`；`cd server && uv run pytest tests/repositories/test_repo_file_read_views.py -q` 全绿；`rg -c "def test_" server/tests/repositories/test_repo_file_read_views.py` ≥ **14**。
- ⭐ **三种不可用情形逐字相同**（头号靶子）：`rg -n "\.json\(\) ==" server/tests/repositories/test_repo_file_read_views.py` 命中 ≥2（三者两两比对）。
- ⭐ **中性口径不是 404**：`cd server && uv run python -c "
src = open('repositories/repo_file_views.py').read()
assert 'HTTP_404' not in src and 'status=404' not in src, '⛔ 本端点不得有 404 分支（被排除/不存在/无镜像统一 200 空）'
assert 'file_excluded' not in src, '⛔ 不得沿用 MCP 的显式告知口径'
print('neutral fail-closed OK')"` 输出 `neutral fail-closed OK`。
- ⭐ **截断不报错**：`rg -n "_MAX_LINES" server/repositories/repo_file_views.py` 命中；用例 6 断言 `status_code == 200` 且 `truncated is True`。
- ⭐ **错误体键是 `error` 不是 `detail`**：`rg -n '"error"' server/repositories/repo_file_views.py` 命中；`rg -n '"detail"' server/repositories/repo_file_views.py` **零命中**（与 `chunk_at` 同口径；115-02 已登记 `ApiError.detail` 会回落成无意义的 `'请求失败'`）。
- ⭐ **adrf + 权限闸**：`rg -n "from adrf.views import APIView" server/repositories/repo_file_views.py` 命中；`rg -n "permission_classes" server/repositories/repo_file_views.py` 命中；`rg -n "aget_object_or_404" server/repositories/repo_file_views.py` 命中。
- ⭐ **两个口径并列断言**：用例 9 里同时出现 `file_excluded` 的 404 断言（MCP 面）与 200 空断言（SPA 面）。
- **行号 1-based 且与 `line_start` 对齐**：用例 5 断言 `lines[0]["line_no"] == 10`。
- **纯追加核算**：`git diff $PHASE_BASE -- server/repositories/urls.py | grep -c '^-[^-]' || true` 输出 **0**；`rg -n 'name="repository-file-lines"' server/repositories/urls.py` 命中。
- ⛔ **既有面零改动**：`git diff $PHASE_BASE -- server/repositories/chunk_at_views.py server/services/chunk_lookup.py` **全部为空**。
- **观测**：`rg -n "path_len" server/repositories/repo_file_views.py` 命中；`rg -n 'path=path|path=str\(path\)' server/repositories/repo_file_views.py` 的命中**不落在日志调用**（逐条人工核对并写进 SUMMARY）。
- `cd server && uv run ruff check repositories/repo_file_views.py repositories/urls.py tests/repositories/test_repo_file_read_views.py` 通过；`uv run ruff format --check repositories/repo_file_views.py tests/repositories/test_repo_file_read_views.py` 通过。
  </acceptance_criteria>
  <done>新端点就位并逐条遵循 `chunk_at` 的中性口径（被排除 / 不存在 / 无镜像三者响应体逐字相同的 200 空、参数前置 400 且错误体键是 `error`）；区间超上界**截断而不是报错**、行号 1-based 与 citation 的 `line_start` 对齐；双路径复判仍生效；MCP 面与 SPA 面的两个 `is_excluded` 口径并列断言证明分道且互不污染。</done>
</task>

<task type="auto">
  <name>Task 3: 前端代码预览升级（正文 + 行高亮 + 快照兜底）+ ⭐ 可独立顺延性实跑验证 + 前后端双门</name>
  <files>web/src/api/repositoryChunks.ts, web/src/components/blueprint/citation/CitationCodePreview.vue, web/src/components/blueprint/__tests__/citationPreview.spec.ts, web/src/locales/zh-CN.json</files>
  <read_first>web/src/components/blueprint/citation/CitationCodePreview.vue **全文**（⭐ `:1-24` 的降级 docstring **逐段**（本 Task 改写它但保留历史结论）、`locator` 逐键收窄的写法、`useQuery` 的 key 与 `enabled` 条件、`{chunks, usable}` 的消费方式、`CitationFallback` 的兜底调用、`Skeleton` 的加载态、⛔ 「任何分支都不回显后端错误体（T-115-23）」、行号列与 `<pre class="font-mono">` 的**既有渲染形态**（本 Task 沿用它，⛔ 不引编辑器内核））；web/src/api/repositoryChunks.ts **全文**（`getChunkAt` 的签名与 ⭐ **`{chunks, usable}` 的判据封装形态** —— 新函数照同款封装「调用点不各自判」；`get` helper 的用法与错误处理）；web/src/components/blueprint/__tests__/citationPreview.spec.ts **全文**（既有代码预览用例：mock 组织、`repositoryChunksApi` 的 mock 形态、兜底断言的写法 —— 本 Task **扩写而不是重写**）；web/src/locales/zh-CN.json（`knowledge.blueprints.citation.*` 或等价子树 —— 新键追加位置）；web/src/__tests__/blueprint-source-guard.spec.ts（⭐ **只读**：六条源码守卫的确切判据 —— 本 Task 的改动不得触发它们，尤其 `refetchInterval` 单文件那条）；.planning/phases/115-ui/115-07-SUMMARY.md **§9**（⭐ **可独立顺延性实跑验证的完整做法** —— 本 Task 照它做一遍并记录）</read_first>
  <action>
**① `web/src/api/repositoryChunks.ts` 追加一个函数（删除行 0）。**

```
export async function getRepositoryFileLines(
  repositoryId: string,
  params: { path: string, lineStart: number, lineEnd: number, branchName?: string },
): Promise<{ lines: Array<{ line_no: number, text: string }>, truncated: boolean, usable: boolean }>
```
⭐ **判据封装进返回类型**（照既有 `{chunks, usable}` 的形态，⛔ 调用点不各自判）：`usable = ok && lines.length > 0`（⚠️ **200-空 也不可用** —— 后端对「无命中」与「被排除文件」刻意不可区分，与 115-02 为 `chunk-at` 立的判据同源）。⛔ 任何调用点不得回显后端错误体（错误体键是 `error`，`ApiError.detail` 会回落成无意义的 `'请求失败'`）。

**② `CitationCodePreview.vue` 升级（删除行 ≤20）。**

- ⭐ **docstring 改写**：把「⭐ **本相位显式降级形态**」整段改成「**116-07 已补上源码正文读面**」，但**保留历史结论**（115 为什么做不到：`chunk_lookup` 不 select 正文 / 唯一带 `content` 的是向量搜索 / MCP 工具是 PAT 认证）作为设计沿革；⛔ 不留过时话术（「⛔ 没有源码正文」「归 Phase 116」等）。
- 数据：`locator` 有 `repository_id` + `path` + `line_start` ⇒ 发 `getRepositoryFileLines`（`line_end` 缺失时取 `line_start`）；⭐ **`line_start` 缺失时直接不发请求**（后端必填，发出去也是注定失败的一次往返 —— 既有纪律）。
- 渲染：沿用既有 `<pre class="font-mono">` + 行号列（⛔ **不引 CodeMirror / 高亮库**）；⭐ **行高亮**落在 citation 指向的区间（`locator.line_start..line_end`），高亮走既有语义类（⛔ 零颜色字面量）；`truncated` 时在末尾给一行「已截断」提示（i18n 新键）。
- ⭐ **兜底不变**：`usable === false` 或请求失败 ⇒ 回落 `CitationFallback`（`title` / `quote` 快照），⛔ **不关弹窗**、⛔ **不回显后端错误体**（T-115-23）。
- ⛔ `refetchInterval` 零命中（源码守卫锁死）。

**③ `web/src/locales/zh-CN.json` 新键纯追加（删除行 0）**：截断提示、加载/兜底文案（若既有键已覆盖则复用，⛔ 不造重复键）。

**④ `citationPreview.spec.ts` 扩写（删除行 ≤8）**：

1. ⭐ **有正文时渲染正文与行号**：mock 返回三行 ⇒ DOM 里出现三行且 `line_no` 与后端一致。
2. ⭐ **行高亮落在 citation 区间**：`locator.line_start=12, line_end=13` + 返回 10..15 ⇒ 只有 12/13 两行带高亮标记（`data-*` 或类名断言）。
3. ⭐ **`usable === false` ⇒ 快照兜底且不关弹窗**（沿用既有断言形态）。
4. ⭐ **请求失败 ⇒ 快照兜底且不回显错误体**：mock 抛含 `internal/path/leak` 的错误 ⇒ DOM 里**零出现**该片段。
5. **`line_start` 缺失 ⇒ 零请求**（`getRepositoryFileLines` mock 零调用）。
6. **`truncated: true` ⇒ 出现截断提示**。

**⑤ ⭐ 可独立顺延性实跑验证**（照 115-07 §9 的做法，本 plan 的硬要求）：
1. `git switch -c tmp/verify-116-07-revert`（临时分支）；
2. 整体回退本 plan 的全部改动（`git revert` 本 plan 的三个 commit，或 `git checkout <116-06 收口 commit> -- <本 plan 的 9 个文件>` 并删除新建文件）；
3. 跑**后端全量门** + **前端四道门**；
4. ⭐ 断言结果**逐字回到 `116-06-SUMMARY` 登记的基线**；
5. `git switch -` 并**确认工作区已完整恢复**（`git status --porcelain` 与验证前一致）；
6. 命令与结果逐条记进 SUMMARY。

**⑥ 前后端双门 + 相位收口**（本 Task 收尾）：
- 后端：`cd server && uv run pytest tests/ -q` 与 116-06 收口基线比对；`makemigrations --check --dry-run` 退出码 0。
- 前端：`cd web && pnpm exec vitest run`、`pnpm type-check`、`pnpm build`、`pnpm exec eslint <触及文件>` 零新增问题。
- ⚠️ **P-15** + `components.d.ts` 环境项：`git status` 检查并按 STATE 115-06 的做法还原/手工插入。
  </action>
  <verify>
    <automated>cd web && pnpm exec vitest run src/components/blueprint/__tests__/citationPreview.spec.ts src/__tests__/blueprint-source-guard.spec.ts && pnpm type-check</automated>
  </verify>
  <acceptance_criteria>
- `cd web && pnpm exec vitest run src/components/blueprint/__tests__/citationPreview.spec.ts` 全绿；新增用例 ≥ **6**。
- ⭐ **源码守卫六条全绿**：`cd web && pnpm exec vitest run src/__tests__/blueprint-source-guard.spec.ts`；`rg -n "refetchInterval" web/src/components/blueprint/citation/CitationCodePreview.vue` **零命中**。
- ⭐ **判据封装在返回类型里**：`cd web && node -e "
const s = require('fs').readFileSync('src/api/repositoryChunks.ts','utf8');
if (!/getRepositoryFileLines/.test(s)) throw new Error('缺新函数');
const i = s.indexOf('getRepositoryFileLines');
const seg = s.slice(i, i + 900);
if (!/usable/.test(seg)) throw new Error('判据必须封装进返回值（调用点不各自判）');
console.log('api封装 OK');
"` 输出 `api封装 OK`。
- ⭐ **⛔ 不回显后端错误体**：用例 4 绿；`rg -n "\.detail|err\.message|error\b" web/src/components/blueprint/citation/CitationCodePreview.vue` 的命中**不落在模板渲染**（逐条人工核对并写进 SUMMARY）。
- ⭐ **⛔ 零新增依赖**：`git diff $PHASE_BASE -- web/package.json web/pnpm-lock.yaml` **为空**；`rg -n "codemirror|highlight.js|shiki|prismjs" web/src/components/blueprint/citation/CitationCodePreview.vue` **零命中**。
- ⭐ **docstring 过时话术已清**：`rg -n "本相位显式降级形态|⛔ 没有源码正文|归 Phase 116|归 \*\*Phase 116\*\*" web/src/components/blueprint/citation/CitationCodePreview.vue` **零命中**。
- ⭐ **可独立顺延性实跑验证已完成**：SUMMARY 里给出临时分支名、回退方式、四道门的**回退后数值**与 116-06 基线的逐条比对、以及「工作区已恢复」的 `git status` 证据。⛔ 只声明不实跑不算。
- ⭐ **若最终顺延**：`REQUIREMENTS.md` 的 VIEW-02 顺延目标已同步改写（⛔ 不许让「顺延 Phase 116」在 116 结束后仍挂着），改动内容贴进 SUMMARY。⚠️ **若本 plan 压根没被执行**，该义务由 `116-06-PLAN.md` `<output>` 的相位出口检查兜底（B6）。
- ⭐ **前端门**：`cd web && pnpm exec vitest run` 相对 116-06 收口基线无新增失败；`pnpm type-check` 退出码 **0**；`pnpm build` 通过；`pnpm exec eslint src/api/repositoryChunks.ts src/components/blueprint/citation/CitationCodePreview.vue src/components/blueprint/__tests__/citationPreview.spec.ts src/locales/zh-CN.json` **零新增问题**。
- ⭐ **后端门**：`cd server && uv run pytest tests/ -q` 相对 116-06 收口基线**无新增失败**；`makemigrations --check --dry-run` 退出码 0。
- ⚠️ **环境项核算**：`git status --porcelain web/pnpm-workspace.yaml web/src/components.d.ts` **为空**。
- **i18n 零降级**：`git diff $PHASE_BASE -- web/src/locales/zh-CN.json | grep -c '^-[^-]' || true` 输出 **0**；组件内零中文硬编码（命中只允许落在注释）。
- 相位边界自检：`git diff $PHASE_BASE --name-only` 只含本 plan 声明的 9 个文件；⛔ 不含 §13.2 冻结面与 0.19 归属的四个前端面。
  </acceptance_criteria>
  <done>引用预览从降级形态升级为真正的代码预览（正文 + 行号 + citation 区间行高亮），任何失败仍**快照兜底、不关弹窗、不回显错误体**；判据封装在 API 返回值里、零新增依赖、组件 docstring 的过时话术清干净；⭐ **可独立顺延性经实跑验证**（回退后四道门逐字回到 116-06 基线，工作区已恢复），若最终顺延则 `REQUIREMENTS.md` VIEW-02 的顺延目标已同步改写。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 未认证 / 无权用户 → 新的源码正文读面 | 这是本相位唯一新增的**源码正文**出口；越权即代码泄露 |
| 「文件存在 / 被排除」这一事实本身 | 排除策略的存在性本身是敏感信息；SPA 面必须与 `chunk_at` 的中性口径一致 |
| citation `locator`（半可信，含 LLM 产出）→ 行区间与路径 | 写错的区间不该让预览失败；构造的路径不该绕过排除判定 |
| 下沉后的 service → 两个调用面 | 一份实现两种错误口径，任何一侧污染另一侧都是安全或体验回归 |
| 源码正文 / 文件路径 → 日志与前端错误回显 | 两者都可能泄露内部结构 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-116-59 | Information Disclosure | 复制一份排除判定 ⇒ 两份实现漂移，SPA 面漏判即源码泄露（fail-closed 单一实现纪律的存在理由） | mitigate | 下沉成 `services/repo_file_read.py` 唯一实现；源码扫描断言 `is_excluded` 在 MCP View 里零命中；MCP 与 SPA 两个口径并列用例 |
| T-116-60 | Information Disclosure | 后缀解析绕过排除（T-22-21）⇒ requested 不命中但 resolved 命中时返回了 content | mitigate | 双路径复判保留 + 用例 8（requested 不命中 / resolved 命中 ⇒ 仍返回空） |
| T-116-61 | Information Disclosure | SPA 面沿用 MCP 的 404 `file_excluded` ⇒ 排除策略的存在性可被枚举，且与 115-07 的「非 200 不进错误分档」冲突 | mitigate | 中性口径：被排除 / 不存在 / 无镜像三者**响应体逐字相同**的 200 空 + `HTTP_404` 在该文件零命中的源码断言 |
| T-116-62 | Information Disclosure | 后端错误体被前端回显 ⇒ 泄露内部路径与参数校验细节（T-115-23） | mitigate | 任何失败一律快照兜底；用例 4 构造含 `internal/path/leak` 的错误并断言它零出现 |
| T-116-63 | Elevation of Privilege | 新端点缺仓库权限闸 ⇒ 任一登录用户可读任意仓库源码 | mitigate | 按 `repositories/permissions.py` 实读口径挂闸 + 未认证拒用例；`aget_object_or_404(is_deleted=False)` |
| T-116-64 | Denial of Service | 半可信 `locator` 给出巨大区间 ⇒ 一次请求读整个大文件 | mitigate | `_MAX_LINES` 硬上限 + **截断而不是报错**（`truncated: true`）+ 用例 6 |
| T-116-65 | Tampering | 下沉时改动了 MCP 面的对外契约（响应键 / 错误码 / 文案）⇒ 外部客户端静默失败 | mitigate | snapshot 条目 `git diff $PHASE_BASE -- <该文件>` 为空 + `tests/mcp_tools/` 相对基线无新增失败 + `file_excluded` 与文案的逐字断言 |
| T-116-66 | Information Disclosure | 源码正文 / 文件路径进日志 | mitigate | 只记 `path_len` / `content_len` / `line_count`；AST 断言日志 kwarg 里无 `content` / `path` 原文（⚠️ 既有 `log_exclusion_blocked(rel_path=...)` 属既有行为，逐条登记豁免） |
| T-116-67 | Tampering | 本 plan 被其它 plan 依赖 ⇒ 「可独立顺延」变成空话，顺延时连带回退别的功能 | mitigate | 排在最后一个 wave、⛔ 无任何前置 plan 依赖它；⭐ **实跑验证**（整体回退后四道门逐字回到 116-06 基线） |
| T-116-68 | Repudiation | 本 plan 被顺延但 `REQUIREMENTS.md` 的「顺延 Phase 116」仍挂着 ⇒ 116 结束后该承诺无人认领 | mitigate | ⭐ **兜底在 116-06 而不是这里**（B6：本条的触发条件就是本 plan 不执行）：`116-06-PLAN.md` `<output>` 的相位出口检查要求 116-06 SUMMARY 给出去留结论并在顺延支给出改写后的 VIEW-02 + commit；本 plan 的 prohibitions 与验收覆盖「执行到一半决定回退」那一支 |
| T-116-SC | Tampering | 依赖供应链 | accept | ⭐ **零新增运行时依赖**（⛔ 不引 CodeMirror / 高亮库，`package.json` 与 lock 的 `git diff $PHASE_BASE -- <该文件>` 为空）；零 migration；pnpm 与 `components.d.ts` 两处环境漂移已核算还原 |
</threat_model>

<verification>
- `cd server && uv run pytest tests/repositories/ -q` 全绿（含既有 `chunk-at` 端点用例 —— 中性口径未被污染的第一道证据）。
- `cd server && uv run pytest tests/mcp_tools/ -q` 相对基线**无新增失败**（⚠️ `test_skills_snapshot_guard::test_skill_files_discovered` 逐条核对是同一条既有环境项，P-16）；`tests/mcp_tools/test_schema_snapshot.py` 单独全绿。
- ⭐ `cd server && uv run pytest tests/ -q` 相对 **116-06 收口基线**无新增失败。
- ⭐ `cd web && pnpm exec vitest run` 相对 116-06 收口基线无新增失败；`pnpm type-check` exit 0；`pnpm build` 通过；`pnpm exec eslint <触及文件>` 零新增问题；⭐ 源码守卫六条全绿。
- `cd server && uv run python manage.py makemigrations --check --dry-run` 退出码 0；相位内**零** migration。
- ⭐ **可独立顺延性实跑验证**：临时分支整体回退 → 四道门逐字回到 116-06 基线 → 工作区已恢复；命令与数值逐条记进 SUMMARY。
- 冻结面核算：`services/chunk_lookup.py` / `repositories/chunk_at_views.py` / `mcp_tools/serializers.py` 的 `git diff $PHASE_BASE -- <该文件>` **全部为空**；§13.2 六个冻结文件与 `repo_router_v2.py` 零命中；0.19 归属的四个前端面 `git diff $PHASE_BASE -- <该文件>` 为空。
- 删除行核算：六个受限文件逐个不超过 execution_context 表的上界。
- 环境项核算：`git status --porcelain web/pnpm-workspace.yaml web/src/components.d.ts` 为空；`git diff $PHASE_BASE -- web/package.json web/pnpm-lock.yaml` 为空。
</verification>

<success_criteria>
- **引用预览第一次带上源码正文与行高亮**：点开代码位置引用能看到 citation 指向的那几行真实代码并被高亮，⛔ 而不是只有「路径 + 行号 + 引用快照」的降级形态。VIEW-02 的最后一块闭环。
- ⭐ **排除判定只有一份实现**：MCP 面与 SPA 面共享 `services/repo_file_read.py`，双路径复判保留、命中绝不返 content；两个 `is_excluded` 口径（MCP 的 404 显式告知 / SPA 的中性 200 空）**分道且互不污染**，有并列用例证明。
- ⭐ **MCP 面零契约漂移**：`get_repository_file` 的响应键集、`file_excluded` 的 404 与文案一字未改，`TOOL_SCHEMA_SNAPSHOT` 条目零改动。
- **半可信 `locator` 不会让预览失败**：区间超上界**截断而不是报错**，文件末尾越界只返回到最后一行；任何失败一律快照兜底、不关弹窗、不回显后端错误体。
- ⭐ **「可独立顺延」是实跑验证过的事实而不是声明**：整体回退本 plan 后，后端全量门与前端四道门**逐字回到 116-06 基线**（照 115-07 的先例）。若最终顺延，`REQUIREMENTS.md` VIEW-02 的顺延目标同步改写 —— ⛔ 不留一句无人认领的「顺延 Phase 116」。
- 零 migration、⭐ **零新增运行时依赖**（不引编辑器内核 / 高亮库）；`chunk_lookup.py` 与 `chunk_at_views.py` 逐字未动。
</success_criteria>

<output>
完成后创建 `.planning/phases/116-entry/116-07-SUMMARY.md`，provides 至少包含：
- ⭐ **`aread_repository_file` 的逐字签名与恒定返回键**，以及**五块方法体的搬运对照表**（原 `GetRepositoryFileView` 方法 → 新函数名 → 原行号）；
- ⭐ **两个调用面的错误口径对照表**（MCP：`excluded` → 404 `file_excluded` + 逐字文案 / SPA：`excluded` + `not_found` + `unavailable` → 同一个 200 空），以及「为什么必须分道」的理由；
- **新端点的契约表**（URL / `name` / query 参数与校验 / 响应键逐字 / 全部状态码），含 ⭐ **`_MAX_LINES` 的取值与截断语义**、⭐ **错误体键是 `error` 不是 `detail`** 的登记；
- ⭐ **MCP 契约零漂移的核算证据**（snapshot 条目 `git diff $PHASE_BASE -- <该文件>` 为空 + `tests/mcp_tools/` 与基线的逐条比对，含 `test_skills_snapshot_guard` 是同一条既有环境项的显式登记）；
- 前端 `getRepositoryFileLines` 的签名与 `usable` 判据，以及 `CitationCodePreview` 的渲染契约（行号列 / 高亮区间 / 截断提示 / 兜底分支）；
- ⭐ **可独立顺延性的实跑验证记录**（临时分支名 / 回退方式 / 四道门回退后的数值 / 与 116-06 基线的逐条比对 / 工作区恢复证据）；
- ⭐ **若最终顺延**：`REQUIREMENTS.md` VIEW-02 顺延目标的改写内容；
- 受限面删除行逐行登记；前后端双门与 116-06 基线的差异比对；pnpm 与 `components.d.ts` 两处环境项还原核算；
- ⭐ **VIEW-02 的验收映射表**（哪几条用例分别兑现「源码正文」与「行高亮」）。
</output>
