---
phase: 116-entry
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/system/models.py
  - server/services/process_runtime/blueprint_entry_switch.py
  - server/services/process_runtime/entrypoint.py
  - server/services/process_runtime/builtin_processes.py
  - server/services/process_runtime/resume.py
  - server/delivery/api/blueprint_gate_views.py
  - server/tests/services/process_runtime/test_blueprint_entry_switch.py
  - server/tests/services/process_runtime/test_engine_dispatch.py
  - server/tests/delivery/test_blueprint_gate_scope.py
autonomous: true
requirements: [GATE-01]
prohibitions:
  - "⭐ **`PHASE_BASE` 纪律（所有边界/删除行核算的前提，B5）**：GSD 逐 Task 原子提交 ⇒ 收口自检跑时改动**早已 committed**，裸 `git diff` 恒空、所有冻结面与删除行断言会**静默恒真**。⇒ 本 plan **第一条执行步骤**是 `PHASE_BASE=$(git rev-parse HEAD)` 并把该 sha 逐字记进 SUMMARY；下文所有边界检查一律写作 `git diff $PHASE_BASE -- <file>` / `git diff $PHASE_BASE --name-only`。⛔ 出现任何裸 `git diff <file>` 形态的断言即判不合格"
  - "🔒 **§13.2 冻结六文件零改动**（`git diff $PHASE_BASE -- <file>` 必须为空）：`server/services/process_runtime/decompose_segments.py` / `research_adapter.py` / `architect_merge_adapter.py` / `merged_plan.py` / `clarify_adapter.py` / `render.py`。「旧 process 退役观察」= 打一条事件 + 在 `builtin_processes.py` 的注册项**上方**加注释，⛔ 绝不往这六个文件里加 deprecation 日志、⛔ 绝不注销 `register_process_type(\"technical_plan\", ...)`（在途会话续驱会崩）"
  - "🔒 **`server/codegraph/services/repo_router_v2.py` 零改动**（`git diff $PHASE_BASE -- server/codegraph/services/repo_router_v2.py` 必须为空，STATE §13.2 第 1 条）"
  - "⛔ **`build_engine_for_session` 绝不透传 `skip_clarification` / `force_confirm` 进蓝图工厂**：`build_blueprint_engine` 只接 `session_service` / `node_execution_id` 两个形参（`entrypoint.py:144-146`），蓝图链**根本没有 `clarify` dep** ⇒ 传了要么 TypeError 要么被静默丢弃。两个参数只在 `process_type == \"technical_plan\"` 分支透传给 `build_orchestration_engine`"
  - "⛔ **`aresolve_entry_process_type` 的实参必须是字面量常量，绝不从 session 反推**（PATTERNS §0.1，本相位新发现的硬伤）：MCP 入口给 `start_orchestration` 传的 `entrypoint` 是 **`\"workflow\"`**（`mcp_tools/orchestration_delegate.py:171-178`，该文件 `:4` / `:131` docstring 逐字写明这是既有约定不是笔误）⇒ 写成 `switch[session.entrypoint]` 会让「打开 workflow 键」把 MCP 一起切走，正是 per-entry 要避免的相反面。本 plan 用 `ast` 源码扫描把这条钉成可证伪断言"
  - "⛔ **不改 `entrypoint` 形参的既有取值**：它进 `ConvergenceSession.entrypoint` 列且有既有消费方。退役观察事件记的是**新增的独立 `entry_key`**，⛔ 不是 `entrypoint`"
  - "⛔ **`start_orchestration` 不加 `process_type` 形参**（RESEARCH §A.1）：那会让旧链四个入口共享一个可传错的开关。蓝图会话由 116-02 的 `start_blueprint_orchestration` 纯追加承担"
  - "⛔ **不改 `_DEFAULT_RELATIONS`**、⛔ 不改 `BLUEPRINT_EVENTS`（`len == 21` 双断言锁死）、⛔ 不新增 `CallSource` 枚举值、⛔ 不新增 migration（两个 SettingKeys 是**常量 + JSON 形状注释**，`system/models.py:173-200` 的既有形态本就无 migration）"
  - "⛔ **`blueprint_entry_switch.py` 不加进 `test_blueprint_log_redaction_guard._SCANNED_MODULES`**（与 analog `blueprint_ambiguity_score.py` 同口径）——代价是该模块内 ⛔ **不得出现任何 `error=` 实参**：兜底分支只记事件名 + `entry` + `reason` 标量，异常文本不进日志。本 plan 用 grep 断言这条"
  - "⛔ **`builtin_processes.py` 已在 `test_blueprint_log_redaction_guard._SCANNED_MODULES` 十二项之内** ⇒ 本 plan 往它新增的两个自检分支与任何日志调用 **⛔ 不得出现 `error=` 实参**（守卫是 `ast` 扫描 `logger.*` 调用的 keyword，命中即红）。自检事件只记 `session_id` / `stage` / `got=type(dep).__name__` 三个标量，异常文本一律不进日志"
  - "⛔ **gate 链不 import `blueprint_review_views._aassert_project_scope`**（RESEARCH §E.1）：它的 400 分支正是 115-MN-03 判为「设计决策、本轮不改」的存在性暴露面。gate 链自建薄 helper，复用**零件** `_ablueprint_project_id`（本文件 `:511`）+ `blueprint_review_views._ais_project_member`（`:244-251`），⭐ **两个失败分支都回同一个中性 404**"
  - "⛔ **补闸不得动前端**：`web/` 在本 plan `git diff $PHASE_BASE --name-only` 里零命中。gate 链 404 本就混合三种语义、115-07 已按「非 200 只决定挂载点是否渲染、不进错误分档」实现 ⇒ 补闸零新增存在性暴露面，前端零改动"
  - "⛔ **deps `isinstance` 自检只加在 `_h_bp_repo_research` / `_h_bp_merge` 两个 handler**（两个工厂唯一同名的 dep 是 `research` 与 `merge`，`entrypoint.py:128-137` vs `:173-181`）：其余八个 `_h_bp_*` 的 dep 名蓝图独有，旧工厂取到 `None` 即 pass-through，已由 `reroute` 的 `AttributeError` 兜住。⛔ 不给十个 handler 挨个加检查（那是 991 行文件里的十处无收益 churn）"
must_haves:
  truths:
    - "⭐ **`build_engine_for_session(session, *, session_service=None, node_execution_id=\"\") -> tuple[ProcessEngine, Driver]`** 落在 `entrypoint.py`（engine 工厂唯一集中点纪律），**同步函数**（两个工厂皆同步、`session.process_type` 是已加载字段，零 ORM）：`technical_blueprint` ⇒ `(build_blueprint_engine(...), blueprint_resume.adrive_blueprint_session_to_pause_or_terminal)`；其余 ⇒ `(build_orchestration_engine(...), resume.adrive_convergence_session_to_pause_or_terminal)`。⭐ **返回二元组而不是单个 engine 是硬要求**：旧续驱器的 `waiting_clarification` 短路判据是 `ClarificationService().ahas_pending`（`resume.py:53-57`），对蓝图会话**恒 False** ⇒ 三个 pausable stage 一个都短路不了、self-loop 被推到 `max_steps` 落 `advance_step_limit` FAILED。**只换 engine 不换 driver 仍然坏**（RESEARCH §A.3 第二把锁）"
    - "两个 driver 的签名**实测逐字相同** `(engine, session, *, max_steps: int = 20)`（`resume.py:24-26` / `blueprint_resume.py:112-114`）⇒ 返回的 driver 可被调用方直接以同一形状调用，⛔ 不做参数适配层"
    - "⭐ **未知 process_type 不抛、回落旧链、落一条 caller 事件**：`process_type ∉ {technical_plan, technical_blueprint, echo}` ⇒ 记 `engine_dispatch_unknown_process_type`（`category=\"caller\"` / `component=\"process_runtime\"` / `session_id` / `process_type`）后按旧链返回。理由：抛异常会让「注册了第五个 process」的将来直接崩，而回落 + 响亮事件既不失联也不误伤"
    - "⭐ **`resume.adrive_convergence_session_to_pause_or_terminal` 顶部补对称守卫**：`session.process_type == BLUEPRINT_PROCESS_TYPE` ⇒ 记 `wrong_driver_for_blueprint_session`（五个 kv 逐字镜像 `blueprint_resume.py:132-143`）并 **no-op 返回该 session**（⛔ 不改状态、⛔ 不落 FAILED）。⛔ **只挡蓝图会话**（白名单外拒绝），`resume.py` 是旧链共享面（`plan_deepen` 等非蓝图调用方也走它），不得顺手加别的 process_type 判断"
    - "⭐ **deps `isinstance` 自检**：`_h_bp_repo_research` 取到的 `deps.research` 若不是 `BlueprintResearchAdapter` 实例、`_h_bp_merge` 取到的 `deps.merge` 若不是 `BlueprintMergeAdapter` 实例 ⇒ 走 `_abp_ensure_blocking_clarification` + `needs_clarification` 出边 + 一条 `blueprint_stage_wrong_adapter` caller 事件，⛔ **绝不把旧链 adapter 跑起来**。这是唯一能挡住 `ArchitectMergeAdapter.merge` 经 `ArtifactService.create` 往蓝图会话落一份 **v0 形状 content**（`architect_merge_adapter.py:252-259`）的手段（RESEARCH §A.3）。⭐ 自检放 **handler 组**不放工厂内：工厂内只能挡「工厂自己构造错」，挡不住「调用方用了旧工厂」——而后者才是真实风险面"
    - "⭐ **三条变异用例，期望值按 RESEARCH §A.3 的真实形态写（⛔ 绝不写「断言未到 DONE」——正确实现下会话停在 spec_gate 等澄清、也不是 DONE ⇒ 恒绿、变异不敏感，P-3）**：(A) 用 `build_orchestration_engine()` 从 `repo_research` 驱一条蓝图会话 ⇒ 落 `needs_clarification` + 有 `blueprint_stage_wrong_adapter` 事件 + **零新增 `ArtifactVersion`**；删掉 `_h_bp_repo_research` 的 isinstance 自检 ⇒ 转红。(B) 从 `merge` 驱 ⇒ **`ArtifactVersion` 计数不变**且不存在任何 `content.get(\"schema_version\") != \"blueprint/v1\"` 的新版本；删掉 `_h_bp_merge` 的自检 ⇒ 转红。(C) 用 `resume.adrive_…` 驱蓝图会话 ⇒ no-op 且 `status`/`current_stage` **一字未变** + 有 `wrong_driver_for_blueprint_session` 事件；删掉对称守卫 ⇒ 会话被推到 `advance_step_limit` FAILED ⇒ 转红"
    - "⭐ **Wave 0 探针先跑，且只许动一个变量**（RESEARCH Assumptions A1 未解除）：写变异断言**之前**先在无自检状态下实跑一次「**错工厂 + 对的 driver**」——`build_orchestration_engine()` 配 `blueprint_resume.adrive_blueprint_session_to_pause_or_terminal`，从 `intake` 驱一条蓝图会话，把终局（stage / status / error）逐字记进 SUMMARY。⛔ **不得同时用旧 driver**：那会把「错工厂」与「错 driver」两个变量搅在一起，观测到的多半是错 driver 的症状 `advance_step_limit` 而不是 RESEARCH 推演的 `reroute` `AttributeError`，据此校准只测一个变量的变异 A 会得到错误期望值。⚠️ 探针结论**只在 wave 1 成立**：116-02 落地后 `intake` / `decompose` 不再是 pass-through，落点会前移——SUMMARY 里注明这一点。⭐ **调整后的断言必须落在下列白名单三选一**：(a) 终局三元组 `(current_stage, status, error[\"reason\"])` 的逐字相等；(b) `ArtifactVersion` 计数与调用前相等；(c) 出边是 `needs_clarification` **且**存在一条 `blueprint_stage_wrong_adapter` 事件。⛔ **任何「不是 X」形态的否定式判据一律不接受**（含「未到 DONE」「未产版本」「不为 None」）"
    - "⭐ **新增 `SettingKeys.BLUEPRINT_ENTRY_SWITCH = \"blueprint.entry.switch\"`**（JSON 四键 `workflow`/`chat`/`mcp`/`feature_list`，值域 `technical_plan | technical_blueprint`，**默认四键全 `technical_plan`**）与 **`SettingKeys.BLUEPRINT_ASSUMPTIONS_TIERS = \"blueprint.assumptions_tiers\"`**（三档 `strict`/`balanced`/`assume_more`，各含 `threshold` 与 `max_rounds`；本 plan **只声明键与形状注释**，消费方由 116-06 落地）。两键逐字照 `system/models.py:173-200` 的「常量 + JSON 形状注释 + 消费方文件名」形态，**零 migration**"
    - "⭐ **`blueprint_entry_switch.aresolve_entry_process_type(entry: str) -> str`**：模块级 `DEFAULT_ENTRY_SWITCH`（四键全 `technical_plan`）+ `copy.deepcopy` 兜底 + 函数内懒 import `SettingKeys` / `aget_json_setting` + 整段 `try/except` 回默认（形状逐字照 `blueprint_ambiguity_score.aload_spec_gate_config:239-268`）。⚠️ **`aget_json_setting` 只保证外层是 dict、内层不校验**（`settings_service.py:139-153`）⇒ 逐键 `str(raw.get(entry) or \"\")`、值不在 `{technical_plan, technical_blueprint}` 内一律回落 `technical_plan` 并记一条 warning；`entry` 本身不在四键内同样回落 + warning"
    - "⭐ **源码扫描断言（两条谓词，同一个扫描器）**，扫描面 = `services/process_runtime/` 全目录 + 四个入口文件（`workflows/nodes/ai/plan_research.py` / `agents/tools/plan_research_tools.py` / `mcp_tools/orchestration_delegate.py` / `initiatives/services/feature_solution_service.py`）：**谓词 ①** `aresolve_entry_process_type` 的第一个实参必须是 `ast.Constant`；⭐ **谓词 ②（C2，同一陷阱的另一半）** `start_orchestration` / `start_blueprint_orchestration` 调用上任何 `entry_key=` keyword 的值也必须是 `ast.Constant` —— 写成 `entry_key=session.entrypoint` 会让 `technical_plan_entry_used` 的分桶从第一天起就错（MCP 记成 `workflow`）、**静默且永不报错**，而这正是本 plan 引入 `entry_key` 想解决的问题本身。⭐ **必须配一条「守护的守护」**（形态照 `test_blueprint_log_redaction_guard.py:94-115`）：合成源码里**两条反面各一行** —— `aresolve_entry_process_type(session.entrypoint)` 与 `start_orchestration(entry_key=session.entrypoint)` —— 断言扫描器对两条**都**报违规，否则本条在 116-03 之前是空扫描、形同虚设"
    - "⭐ **`start_orchestration` 追加 keyword-only `entry_key: str = \"\"` 并在内部落 `technical_plan_entry_used`**（`category=\"caller\"` / `component=\"process_runtime\"` / `entry_key=entry_key or \"unknown\"` / `entrypoint` / `initiated_by_user_id` / `session_id`）。落在 `start_orchestration` 内部是**唯一能覆盖全部四个入口且不碰冻结文件**的位置（四入口全经它建会话）；⛔ 不在四个入口各打一条（会漏 `plan_deepen` 那类非入口调用方，也会四份漂移）。⭐ 事件字段是 `entry_key` **不是** `entrypoint`（PATTERNS §0.1：MCP 传的 `entrypoint` 是 `\"workflow\"`，按它聚合从第一天起就是错的且永不报错）"
    - "`builtin_processes.py` 的 `technical_plan` 注册项（`:963-970`）**上方**追加退役观察注释（形状照 `:981-983` 蓝图注册项那三行），写明「不再是任何入口默认的收口顺延同步点 2；流量经 `technical_plan_entry_used` 事件可 SQL 聚合；⛔ 绝不注销注册——在途会话续驱会崩」"
    - "⭐ **`blueprint-gate/` 八个 View 全部挂范围闸**（现状实测：全部只有 `IsAuthenticated`，`_ablueprint_project_id` 只在 `BlueprintRejectedToBoundaryView` 被调过一次，其余七个没闸、其中 `confirm` / `remove-repo` / `add-repo` 是**破坏性写**）：本文件自建薄 helper `_aassert_gate_scope(request, artifact) -> Response | None` —— superuser 直通 → `_ablueprint_project_id(artifact)` 非 UUID/取不到 ⇒ **中性 404** → `_ais_project_member` 为 False ⇒ **同一个中性 404 常量** → 放行返 `None`。⭐ **两个失败分支的响应体逐字相同**（`assert a.json() == b.json()` 是唯一可证伪形态）"
    - "挂载位置：`_aload_gate_context` 之后、**任何 service 调用之前**（与 `_aapply_action` 的 `(error, result, session)` 三元组形态对齐，View 只 `if error is not None: return error`）；`BlueprintRejectedToBoundaryView` 的既有单点调用改为走同一个 helper，⛔ 不留第二份口径"
    - "⭐ **`confirm/` 两处 409 各补 `blocked_reason` 键**：`:239-240` 补 `\"blocked_reason\": \"pending_clarification\"`（service 返回值里的键现在被视图消费掉了）、`:247-256` 补 `\"blocked_reason\": str(lock.get(\"reason\") or \"\")`（原样，`snapshot_changed` 等）。**前端已实现且已有用例**（`gatePanel.spec.ts:577` / `:591`），后端补键即生效、**前端零改动**"
    - "全量后端门：`cd server && uv run pytest tests/ -q` 相对基线 **8609 passed / 1 failed**（唯一失败是 `tests/mcp_tools/test_skills_snapshot_guard.py::test_skill_files_discovered` 的 worktree 环境产物）**无新增失败**；`uv run python manage.py makemigrations --check --dry-run` 退出码 0 且 `git status --porcelain server/*/migrations/` 为空"
  artifacts:
    - path: "server/services/process_runtime/blueprint_entry_switch.py"
      provides: "per-entry 运行时开关读取：DEFAULT_ENTRY_SWITCH 四键全 technical_plan + 逐键强转与非法回落 + 整段 try 回默认 + ⛔ 零 error= 实参"
      contains: "aresolve_entry_process_type"
    - path: "server/services/process_runtime/entrypoint.py"
      provides: "build_engine_for_session 分派器（返 (engine, driver) 二元组、未知 process_type 回落 + caller 事件、⛔ 不透传 skip_clarification/force_confirm）+ start_orchestration 的 entry_key 形参与 technical_plan_entry_used 事件 + __all__ += 纯追加"
      contains: "build_engine_for_session"
    - path: "server/services/process_runtime/resume.py"
      provides: "对称 process_type 守卫（蓝图会话 ⇒ wrong_driver_for_blueprint_session + no-op），删除行 0"
      contains: "wrong_driver_for_blueprint_session"
    - path: "server/services/process_runtime/builtin_processes.py"
      provides: "_h_bp_repo_research / _h_bp_merge 的 deps isinstance 自检（非蓝图 adapter ⇒ needs_clarification + blueprint_stage_wrong_adapter）+ technical_plan 注册项上方退役观察注释"
      contains: "blueprint_stage_wrong_adapter"
    - path: "server/system/models.py"
      provides: "SettingKeys.BLUEPRINT_ENTRY_SWITCH / BLUEPRINT_ASSUMPTIONS_TIERS 两个常量与 JSON 形状注释，零 migration"
      contains: "blueprint.entry.switch"
    - path: "server/delivery/api/blueprint_gate_views.py"
      provides: "_aassert_gate_scope 薄范围闸（两个失败分支同一中性 404）+ 八个 View 挂闸 + confirm 两处 409 补 blocked_reason"
      contains: "_aassert_gate_scope"
    - path: "server/tests/services/process_runtime/test_engine_dispatch.py"
      provides: "分派正向断言（两 process_type × engine deps 实例类型 × driver 身份）+ 三条变异用例（错工厂 repo_research / 错工厂 merge / 错 driver）+ 未知 process_type 回落与事件"
      contains: "build_engine_for_session"
    - path: "server/tests/services/process_runtime/test_blueprint_entry_switch.py"
      provides: "四键默认 / 畸形 JSON 回默认 / 内层非法值回落 / 未知 entry 回落 + ⭐ ast 字面量扫描守卫**两条谓词**（`aresolve_entry_process_type` 首参 + `start_orchestration`/`start_blueprint_orchestration` 的 `entry_key=`）及其「守护的守护」（两条反面各一行）+ 模块内零 error= 实参断言"
      contains: "ast.Constant"
    - path: "server/tests/delivery/test_blueprint_gate_scope.py"
      provides: "八端点参数化非成员用例 + 读不到 project_id 与非成员回同一中性 404 响应体 + superuser 直通 + confirm 两处 409 的 blocked_reason 键"
      contains: "blocked_reason"
  key_links:
    - from: "server/services/process_runtime/entrypoint.py"
      to: "server/services/process_runtime/blueprint_resume.py"
      via: "build_engine_for_session 在 technical_blueprint 分支返回 (build_blueprint_engine(...), adrive_blueprint_session_to_pause_or_terminal)"
      pattern: "adrive_blueprint_session_to_pause_or_terminal"
    - from: "server/services/process_runtime/resume.py"
      to: "server/services/process_runtime/blueprint_resume.py"
      via: "BLUEPRINT_PROCESS_TYPE 常量 import + 对称守卫（反方向，逐字镜像 :132-143）"
      pattern: "BLUEPRINT_PROCESS_TYPE"
    - from: "server/services/process_runtime/builtin_processes.py"
      to: "server/services/process_runtime/blueprint_research_adapter.py"
      via: "_h_bp_repo_research 的 isinstance(dep, BlueprintResearchAdapter) 自检（挡住 ResearchDispatchAdapter 同名污染）"
      pattern: "isinstance"
    - from: "server/services/process_runtime/blueprint_entry_switch.py"
      to: "server/system/settings_service.py"
      via: "aget_json_setting(SettingKeys.BLUEPRINT_ENTRY_SWITCH, DEFAULT_ENTRY_SWITCH) + 逐键强转（外层 dict 有保证、内层无）"
      pattern: "aget_json_setting"
    - from: "server/delivery/api/blueprint_gate_views.py"
      to: "server/delivery/api/blueprint_review_views.py"
      via: "只 import 零件 _ais_project_member；⛔ 不 import _aassert_project_scope（其 400 分支是 115-MN-03 判为设计决策的暴露面）"
      pattern: "_ais_project_member"
---

<objective>
把本相位**每一条后续工作都要踩在上面**的两道闸先立住，并顺手收掉一个 115 登记的既有安全缺口：

① ⭐ **engine 与 driver 按 `process_type` 分派，且两个方向都 LOUD** —— 这是本相位最高危的静默失败面。两个工厂的 deps 名单有**两个同名属性**（`research` / `merge`，`entrypoint.py:128-137` vs `:173-181`），十个 `_h_bp_*` handler 一律 `getattr(..., name, None)` 软取。用错工厂**不会报错**，而是：从 `intake` 驱 ⇒ 一路穿到 `reroute` 撞 `AttributeError` 落 FAILED（用户看到「方案编排失败」）；从 `merge` 驱 ⇒ `ArchitectMergeAdapter` 经 `ArtifactService.create` 落一份 **v0 形状的 content**，把蓝图会话的产物指针钉到一份非 `blueprint/v1` 的版本上——**这一条会同时废掉 SC-3 的渲染判别与 SC-4 的入图门控**。再加一把：旧续驱器的 `waiting_clarification` 短路判据对蓝图**恒 False**，只换 engine 不换 driver 会让三个 pausable stage 一个都短路不了、被推到 `max_steps` 落 FAILED。⇒ 分派器返回**二元组**，并给两个同名 dep 加 `isinstance` 自检、给旧 driver 加反向守卫。

② **per-entry 运行时开关 + 两个设置键 + 旧链退役观察** —— 开关粒度是 per-entry 而非全局硬切（四入口下游成熟度不同，回滚 = 改一个设置值不是回滚代码），⭐ 且**由调用方按自己的静态身份传字面量查**、绝不从 session 反推：MCP 入口给 `start_orchestration` 传的 `entrypoint` 实测是 **`"workflow"`**（既有约定，`orchestration_delegate.py:4` / `:131` docstring 逐字写明），写成 `switch[session.entrypoint]` 会让「打开 workflow 键」把 MCP 一起切走。这条用 `ast` 源码扫描 + 一条「守护的守护」钉成可证伪断言。退役观察事件同理用**独立的 `entry_key`** 而不是 `entrypoint`——否则「还有谁在走旧链」的 SQL 聚合从第一天起就是错的，且**永远不会报错**。

③ **`blueprint-gate/` 八端点补项目范围闸 + `confirm/` 两处 409 补 `blocked_reason`** —— 115-07 登记的后端缺口：实测八个 View 全部只有 `IsAuthenticated`，范围闸零件 `_ablueprint_project_id` 只被调过一次，其中 `confirm` / `remove-repo` / `add-repo` 是**破坏性写**。本相位正在给蓝图加新的写入口（116-06 的 MCP 作答），把新入口挂闸而让旧的破坏性写敞着说不过去。⭐ 用「读不到 `meta.project_id` 回**中性 404**」的更严变体 ⇒ **零新增存在性暴露面**，也不必去动 114/115 那条已被四条理由判为「设计决策」的 400 语义。`blocked_reason` 是两行改动，让 115-07 已经实现且已有用例的「一键跳未决线程」那一档在生产真正生效。

Purpose: 后面六个 plan 全部消费本 plan——116-02 的 intake 要靠自检确认自己拿到的是蓝图 adapter；116-03 的六个续驱点全改调 `build_engine_for_session`；116-06 的 MCP 作答要靠同一套范围闸口径。
Output: 1 个新建源文件 + 4 处既有源文件改造 + 3 个新建测试文件。**零 migration、零新依赖、零 `CallSource` 新枚举、前端零改动。**
</objective>

<execution_context>
工作目录：worktree 根 `/Users/zaneliu/Projects/open-source/friday-ai/.claude/worktrees/v0.20-blueprint`，分支 `milestone/v0.20.0-blueprint`。开工前 `git rev-parse --abbrev-ref HEAD` 复核。⛔ **绝不读写 `/Users/zaneliu/Projects/open-source/friday-ai/server` 或 `.../web`**（主检出，不同分支）。

⭐ **第一条执行步骤（先于读任何源码）：`PHASE_BASE=$(git rev-parse HEAD)`，把该 sha 逐字记进 SUMMARY。** GSD 逐 Task 原子提交 ⇒ 收口自检跑的时候本 plan 的改动**早已 committed**，裸 `git diff` 恒空 ⇒ 所有冻结面 / 删除行 / `--name-only` 边界断言都会**静默恒真**。本 plan（及本相位七个 plan）里凡出现 `git diff` 的地方一律写作 `git diff $PHASE_BASE -- <file>` 或 `git diff $PHASE_BASE --name-only`。⚠️ 另一个同类坑：`rg -c` / `grep -c` **无命中时输出为空/`0` 且退出码 1** ⇒ 计数型断言一律 `| grep -c '<pat>' || true` 再比对数字，⛔ 不写 `... | rg -c "..." == 0`（该形态永远不会以「等于 0」的方式通过）。

**wave 1，无前置依赖**：111–115 的模型 / service / 守卫 / 事件常量全部已就位。

**测试基线（执行前先复核，SUMMARY 登记前后值）**：`cd server && uv run pytest tests/ -q` 当前 **8609 passed / 1 failed**；唯一失败是 `tests/mcp_tools/test_skills_snapshot_guard.py::test_skill_files_discovered`（本 worktree 的 `skills/` 是空目录——**纯环境现象，与本相位无关，⛔ 不得试图修它**）。收口断言是「无**新增**失败」。

**受限面配额（硬约束）**：

| 文件 | 允许的改动 | 删除行上界 | formatter |
|---|---|---|---|
| `server/services/process_runtime/entrypoint.py` | 追加 `build_engine_for_session` + `start_orchestration` 体内加 `entry_key` 形参与一条事件 + `__all__ +=` | **2**（`start_orchestration` 的签名行与 `return await ...create_session(` 前后各允许一行重排） | `ruff check` + `ruff format --check` |
| `server/services/process_runtime/builtin_processes.py` | 两个 handler 内加自检分支 + 一处注册项上方注释 | **0** | `ruff check` |
| `server/services/process_runtime/resume.py` | 顶部守卫（import + 一个 if 块） | **0** | `ruff check` + `ruff format --check` |
| `server/system/models.py` | `SettingKeys` 追加两个常量 + 形状注释 | **0** | `ruff check` |
| `server/delivery/api/blueprint_gate_views.py` | 新 helper + 八处挂闸 + 两处 409 补键 + `BlueprintRejectedToBoundaryView` 的既有单点调用改走 helper | **6**（两处 409 的 `Response({...})` 重排 + 既有单点调用替换） | `ruff check` |

⚠️ 签名与字段名一律按下列**实测**取，不得猜测：

- `build_orchestration_engine(*, session_service=None, node_execution_id="", skip_clarification=False, force_confirm=False) -> ProcessEngine`（`entrypoint.py:89-93`）。
- `build_blueprint_engine(*, session_service=None, node_execution_id="") -> ProcessEngine`（`entrypoint.py:144-146`，**只有两个形参**）。
- `resume.adrive_convergence_session_to_pause_or_terminal(engine, session, *, max_steps: int = 20)`（`resume.py:24-26`）与 `blueprint_resume.adrive_blueprint_session_to_pause_or_terminal(engine, session, *, max_steps: int = 20)`（`blueprint_resume.py:112-114`）——**签名逐字相同**。
- `blueprint_resume.BLUEPRINT_PROCESS_TYPE`；既有反向守卫全文在 `blueprint_resume.py:132-143`（五个 kv + `return session`）。
- `start_orchestration(...)`（`entrypoint.py:32-86`）：`"technical_plan"` 写死在 `:78`；「非空才写键」纪律在 `:68-75`；`__all__ +=` 的纯追加先例在 `:188-189`（注释逐字写着「纯追加纪律（既有 `__all__` 行一字不动）」）。
- `aget_json_setting(key, default) -> dict`（`system/settings_service.py:139-153`）：非 JSON / 非 dict 一律回默认、**不走 60s 缓存**（改设置立即生效）、**只保证外层是 dict**。
- `aload_spec_gate_config()`（`blueprint_ambiguity_score.py:239-268`）—— 本 plan 新配置模块的**逐字 analog**（模块级默认常量 + `copy.deepcopy` + 函数内懒 import + 逐字段强转 + 整段 try 回默认）。
- `system/models.py:173-200`：四个既有蓝图 SettingKeys 的「常量 + JSON 形状注释 + 消费方文件名」体例，**无 migration**。
- `builtin_processes.py`：`technical_plan` 注册项 `:963-970`、蓝图注册项与其三行注释 `:981-991`、`_h_bp_ai_review` 的 `needs_clarification` 出边写法 `:809-814`、`_abp_ensure_blocking_clarification`、`_abp_load_artifact` `:453-463`。
- `blueprint_gate_views.py`：八个 View 类名与 `permission_classes` 行 —— `BlueprintGateSnapshotView:199/:206`、`BlueprintGateConfirmView:220/:230`、`BlueprintGateRemoveRepoView:270/:277`、`BlueprintGateAddRepoView:291/:299`、`BlueprintGateReclassifyRoleView:313/:320`、`BlueprintGateEditResponsibilityView:334/:341`、`BlueprintRejectedToBoundaryView:358/:373`、`BlueprintGateUpgradeResearchView:441/:450`；`_aload_gate_context:98`、`_aapply_action:163`、`_is_uuid:501`、`_ablueprint_project_id:511`；两处 409 在 `:239-240` 与 `:247-256`。
- `blueprint_review_views._ais_project_member(user, project_id)`（`:244-251`）—— 只 import 这一个零件。
- `tests/delivery/test_blueprint_review_views.py:83-100` 的范围闸工厂 `_SCOPE_PROJECT_ID` / `_OTHER_PROJECT_ID` / `_make_project(project_id, member=...)` —— gate 闸测试**逐字复用，不重造**。
- `tests/delivery/test_blueprint_log_redaction_guard.py:94-115` 的「守护的守护」用例形态 —— 本 plan 的 `ast` 扫描守卫照它写。
</execution_context>

<context>
@.planning/phases/116-entry/116-RESEARCH.md（§0.2 用错工厂的真实形态、§A.3 逐 stage 推演与「两把锁」、§A.4 八个续驱点表、§A.6 per-entry 开关落点、§A.8 退役观察落点、§E.1 gate 补闸的更严变体、§E.2 两处 409、P-3 变异用例恒绿陷阱、P-16 基线）
@.planning/phases/116-entry/116-PATTERNS.md（⭐ §0.1 `session.entrypoint` 分不出 MCP 与 workflow 的硬伤与两条对策、§0.2 冻结面表、§0.4 追加点 1/2/3/6/25、A2 配置读取 analog 与「必须 DIFFER」、A3/A4 两个函数与 No Analog #2 的三条必须写死、A5 对称守卫的逐字 analog、E1/E2、Shared Patterns S1/S2/S3、No Analog #4）
@.planning/phases/116-entry/116-CONTEXT.md（入口收编的切换机制与回滚一节；顺带闭掉的既有缺口三条）
@server/services/process_runtime/entrypoint.py（**全文**：`start_orchestration:32-86` 的签名与「非空才写键」、两个工厂 `:89` / `:144` 的 deps 名单与形参、`__all__ +=` 的纯追加先例 `:188-189`）
@server/services/process_runtime/blueprint_resume.py（`:112-143` driver 签名与既有反向守卫全文、`_HUMAN_OWNED_STATUSES:78-87`、`_safe_log:104-110`）
@server/services/process_runtime/resume.py（**全文**：docstring 的「绝不在此新建第二个 engine 工厂」纪律、`:53-57` 的 `ahas_pending` 短路判据、`max_steps` 与 `advance_step_limit`）
@server/services/process_runtime/blueprint_ambiguity_score.py（`:239-268` `aload_spec_gate_config` —— 配置读取的唯一先例，逐字 analog）
@server/system/models.py（`:173-200` 四个蓝图 SettingKeys 的形态）
@server/system/settings_service.py（`:139-153` `aget_json_setting` 的三重兜底与「只保证外层 dict」）
@server/services/process_runtime/builtin_processes.py（`:320-330` 两个空 handler、`:453-463` `_abp_load_artifact`、`:809-814` `_h_bp_ai_review` 的 needs_clarification 写法、`:850-960` stage graph、`:963-991` 两处 `register_process_type` 与蓝图注册项的三行注释体例）
@server/delivery/api/blueprint_gate_views.py（**全文**：八个 View、`_aload_gate_context:98`、`_aapply_action:163`、两处 409 `:239-256`、`_is_uuid:501` / `_ablueprint_project_id:511`）
@server/delivery/api/blueprint_review_views.py（`:244-281` `_ais_project_member` 与 `_aassert_project_scope` 四语义 —— 只 import 前者，后者的 400 分支**不复制**）
@server/tests/delivery/test_blueprint_review_views.py（`:83-100` 范围闸工厂、非成员 404 逐字相同断言的完整写法）
@server/tests/delivery/test_blueprint_log_redaction_guard.py（`:94-115` 「守护的守护」用例形态）
@.planning/STATE.md（§并行开发边界纪律六条；Pending Todos 顶部第 4、5 条）
</context>

<tasks>

<task type="auto">
  <name>Task 1: 两个 SettingKeys + blueprint_entry_switch.py（per-entry 开关）+ ast 字面量守卫及其「守护的守护」</name>
  <files>server/system/models.py, server/services/process_runtime/blueprint_entry_switch.py, server/tests/services/process_runtime/test_blueprint_entry_switch.py</files>
  <read_first>server/services/process_runtime/blueprint_ambiguity_score.py **L230-275**（`DEFAULT_SPEC_GATE_CONFIG` 的模块级常量形态 + `aload_spec_gate_config` 全文：`copy.deepcopy(DEFAULT_...)` 作 fallback、函数内 `from system.models import SettingKeys` 懒 import、逐字段 `float()` + clamp、**整段 `try/except` 回默认且绝不外抛**、docstring 里逐字标注 T-112-07）；server/system/settings_service.py **L130-160**（`aget_json_setting` 全文：三重兜底、返回类型标注 `-> dict`、⚠️ **不走 60s 缓存**的那一行与其注释）；server/system/models.py **L165-205**（`SettingKeys` 蓝图块四个键的**确切体例** —— 常量行 + 紧随其后的 JSON 形状注释 + 末行「消费方：<文件路径>（<相位>）」）；server/mcp_tools/orchestration_delegate.py **L1-10 与 L120-190**（⭐ 本 plan 头号靶子的证据：`:4` 与 `:131` docstring 逐字「建 `PlanSession`（entrypoint=workflow）」+ `:171-178` 的 `start_orchestration(entrypoint="workflow", ...)` 实际调用）；server/tests/delivery/test_blueprint_log_redaction_guard.py **L52-115**（`_log_calls` 的 `ast.walk` 形态、`_violations` 的逐文件 `read_text` + `ast.parse`、`@pytest.mark.parametrize` 用法、⭐ **L94-115 「守护的守护」的完整写法**——用 `tmp_path` 写一段合成违规源码再断言扫描器确实报出来）</read_first>
  <action>
**① `server/system/models.py` 的 `SettingKeys` 追加两个常量（删除行 0）。**

紧跟既有四个蓝图键之后，逐字照 `:181-187` 的体例（常量 + JSON 形状注释 + 消费方行）：

```
BLUEPRINT_ENTRY_SWITCH = "blueprint.entry.switch"
# value 为 JSON：{"workflow"|"chat"|"mcp"|"feature_list": "technical_plan"|"technical_blueprint"}。
# 四个入口各自独立，未配置 / 畸形 / 值域外一律回落 "technical_plan"（安全默认：
# 默认切换与旧 process 退役收口顺延同步点 2 后的收尾 plan）。
# ⛔ 消费方一律传**字面量** entry（MCP 入口的 session.entrypoint 实测是 "workflow"，
# 从 session 反推会让 workflow 键连带把 MCP 一起切走）。
# 消费方：services/process_runtime/blueprint_entry_switch.py（116-01）。
BLUEPRINT_ASSUMPTIONS_TIERS = "blueprint.assumptions_tiers"
# value 为 JSON：{"strict"|"balanced"|"assume_more": {"threshold": float, "max_rounds": int}}。
# 只管「问不问」不管「问了等不等」——超时语义永远是显式 pending 不自动作答（§12 决策 4）。
# 默认档 balanced；未配置时回落 DEFAULT_SPEC_GATE_CONFIG 的 threshold 与 max_rounds=3。
# 消费方：services/process_runtime/blueprint_ambiguity_score.py（116-06）。
```

⚠️ 常量名后紧跟的是 `=` 与字符串，与 `blueprint_status` 那条 INV-6 正则无关（那条只扫 `blueprint_status`），无需规避。

**② 新建 `server/services/process_runtime/blueprint_entry_switch.py`。**

模块 docstring 三段：(a) 用途——「per-entry 运行时开关：四个入口各自独立选择 `technical_plan` / `technical_blueprint`，回滚是改一个设置值而不是回滚代码」；(b) ⭐ **纪律段**——逐字写明「`entry` 必须由调用方按自己的**静态身份**传字面量常量，⛔ **绝不从 `session.entrypoint` 反推**：MCP 入口给 `start_orchestration` 传的 `entrypoint` 是 `\"workflow\"`（`mcp_tools/orchestration_delegate.py:171-178`，既有约定），反推会让打开 workflow 键把 MCP 一起切走。该纪律由 `tests/services/process_runtime/test_blueprint_entry_switch.py` 的 `ast` 源码扫描强制」；(c) 观测——「⛔ 本模块不出现任何 `error=` 实参（因此刻意**不**进 `_SCANNED_MODULES`，与 analog `blueprint_ambiguity_score.py` 同口径）：兜底分支只记事件名 + `entry` + `reason` 标量」。

模块级常量：

- `ENTRY_WORKFLOW = "workflow"` / `ENTRY_CHAT = "chat"` / `ENTRY_MCP = "mcp"` / `ENTRY_FEATURE_LIST = "feature_list"`；`ENTRY_KEYS: tuple[str, ...] = (ENTRY_WORKFLOW, ENTRY_CHAT, ENTRY_MCP, ENTRY_FEATURE_LIST)`。
  ⚠️ **这四个常量在调用点上是「看得见的死代码」**：116-03 的四个入口按扫描器谓词 ① 必须传**字面量**（`ast.Name` 会被判违规）⇒ 它们只服务于 `ENTRY_KEYS` 与测试。**模块 docstring 里逐字写明这一点**（「四个 `ENTRY_*` 常量只作 `ENTRY_KEYS` 的构成与测试参数化用；调用点一律写字面量，见纪律段」），⛔ 不要在任何调用点 import 它们。若执行期觉得注释成本高于收益，允许删掉四个常量只留 `ENTRY_KEYS = ("workflow", "chat", "mcp", "feature_list")` —— **二选一，但不得留下无说明的死常量**。
- `PROCESS_TECHNICAL_PLAN = "technical_plan"` / `PROCESS_TECHNICAL_BLUEPRINT = "technical_blueprint"`；`_ALLOWED_PROCESS_TYPES = frozenset({...})`。
- `DEFAULT_ENTRY_SWITCH: dict[str, str] = {k: PROCESS_TECHNICAL_PLAN for k in ENTRY_KEYS}`（写成显式四行字面量 dict 更利于 review，两种皆可，**默认必须四键全 `technical_plan`**）。
- `__all__ = ["ENTRY_KEYS", "DEFAULT_ENTRY_SWITCH", "aresolve_entry_process_type", ...]`。

唯一公开入口：

```
async def aresolve_entry_process_type(entry: str) -> str
```

实现（逐条照 analog 的形状）：
1. `entry` 归一 `str(entry or "")`；不在 `ENTRY_KEYS` ⇒ 记 `blueprint_entry_switch_unknown_entry`（`category="caller"` / `component="process_runtime"` / `entry=<归一后的值>`）并 `return PROCESS_TECHNICAL_PLAN`。
2. `fallback = copy.deepcopy(DEFAULT_ENTRY_SWITCH)`；整段 `try:` 内 `from system.models import SettingKeys` + `from system.settings_service import aget_json_setting` 懒 import（避免 `process_runtime → system` 的模块级依赖），`raw = await aget_json_setting(SettingKeys.BLUEPRINT_ENTRY_SWITCH, fallback)`；`except Exception:` ⇒ 记 `blueprint_entry_switch_load_failed`（`category="sampling"` / `component="process_runtime"` / `entry` / `reason="load_failed"`，⛔ **不带 `error=`**）并 `return PROCESS_TECHNICAL_PLAN`。
3. ⚠️ **内层不校验**：`raw` 非 dict ⇒ 回 `PROCESS_TECHNICAL_PLAN`；`value = str(raw.get(entry) or "")`；`value not in _ALLOWED_PROCESS_TYPES` ⇒ 记 `blueprint_entry_switch_invalid_value`（`category="sampling"` + `entry` + `value`）并 `return PROCESS_TECHNICAL_PLAN`。
4. 命中 ⇒ `return value`。

⚠️ 日志一律经一个模块内 `_safe_log(event, **fields)`（`try/except Exception: pass`，形状照 `blueprint_resume._safe_log:104-110`）——观测 best-effort，⛔ 绝不反噬。

**③ 新建 `server/tests/services/process_runtime/test_blueprint_entry_switch.py`**，docstring「守六件事」编号清单：

1. **默认**：无设置 ⇒ 四个 entry 全返 `"technical_plan"`（参数化四条）。
2. **正路**：设置为 `{"mcp": "technical_blueprint"}` ⇒ `mcp` 返 `technical_blueprint`、其余三个仍返 `technical_plan`（**per-entry 独立性的正反并列**，⛔ 不接受只断言 mcp 那一条）。
3. **畸形**：设置值为非 JSON 串 / JSON 数组 / `null` ⇒ 三种都回默认且**不抛**。
4. **内层非法值**：`{"workflow": "blueprint"}`（值域外）/ `{"workflow": 123}` / `{"workflow": null}` ⇒ 均回落 `technical_plan`（⚠️ 这一条专治「`aget_json_setting` 只保证外层是 dict」）。
5. **未知 entry**：`aresolve_entry_process_type("cron")` ⇒ 回 `technical_plan`（不抛）。
6. ⭐ **`ast` 源码扫描守卫 + 「守护的守护」**（本 Task 的头号靶子）：
   - 扫描面常量 `_SCANNED_CALLERS`：`services/process_runtime/` 目录下全部 `*.py` + 四个入口文件 `workflows/nodes/ai/plan_research.py` / `agents/tools/plan_research_tools.py` / `mcp_tools/orchestration_delegate.py` / `initiatives/services/feature_solution_service.py`。⚠️ 逐文件 `read_text()` 前 **必须 `if not path.exists(): continue`**（四个入口文件现在都存在，但目录遍历要容忍 `__pycache__` 之类）。
   - **谓词 ①**：`ast.walk` 找 `ast.Call` 且 `func` 的末段名为 `aresolve_entry_process_type`；对每个命中，取第一个位置实参（无位置实参则取 keyword `entry`），断言它 **`isinstance(arg, ast.Constant) and isinstance(arg.value, str)`**；违规收集成 `f"{rel}:{node.lineno}: {ast.unparse(arg)}"`。
   - ⭐ **谓词 ②（同一陷阱的另一半，C2）**：同一次 `ast.walk` 里，`func` 末段名为 **`start_orchestration` 或 `start_blueprint_orchestration`** 的 `ast.Call`，若带 `entry_key` keyword，其 `value` 同样必须 `isinstance(..., ast.Constant) and isinstance(.value, str)`；否则收进同一个违规清单（前缀 `entry_key`）。**理由逐字写进用例 docstring**：`entry_key=session.entrypoint` 会让 `technical_plan_entry_used` 把 MCP 记进 workflow 桶——静默、永不报错，正是 `entry_key` 这个新形参本来要解决的问题。⚠️ **无 `entry_key` keyword 不算违规**（默认空串是 116-03 之前的合法过渡态，116-03 逐点补齐）。
   - ⭐ **「守护的守护」（两条反面各一行）**：用 `tmp_path` 写一段合成源码，**同时**包含 `aresolve_entry_process_type(session.entrypoint)` 与 `start_orchestration(entry_key=session.entrypoint)`，对它跑同一个判据函数并断言**两条都被报出违规**（`len(violations) == 2` 且两条各自可定位）。⛔ 没有这一条，本守卫在 116-03 之前是空扫描、形同虚设。
   - 另一条：断言 `blueprint_entry_switch.py` 源码里 **`error=` 零命中**（`re.search(r"\berror\s*=", src) is None`），钉住「不进 `_SCANNED_MODULES` 的代价」。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/process_runtime/test_blueprint_entry_switch.py -q</automated>
  </verify>
  <acceptance_criteria>
- `cd server && uv run pytest tests/services/process_runtime/test_blueprint_entry_switch.py -q` 全绿；`rg -c "def test_" server/tests/services/process_runtime/test_blueprint_entry_switch.py` ≥ **8**。
- ⭐ **默认必须是旧链**：`cd server && uv run python -c "
from pathlib import Path
src = Path('services/process_runtime/blueprint_entry_switch.py').read_text()
assert 'technical_blueprint' in src, '缺值域常量'
i = src.index('DEFAULT_ENTRY_SWITCH')
seg = src[i:i+400]
assert 'technical_blueprint' not in seg, '默认值里不得出现 technical_blueprint'
print('default is old chain OK')"` 输出 `default is old chain OK`。
- ⭐ **零 `error=` 实参**：`rg -n "\berror\s*=" server/services/process_runtime/blueprint_entry_switch.py` **零命中**；`rg -n "blueprint_entry_switch" server/tests/delivery/test_blueprint_log_redaction_guard.py` **零命中**（刻意不进扫描面，理由已写进模块 docstring）。
- ⭐ **不收 session**：`rg -n "def aresolve_entry_process_type" server/services/process_runtime/blueprint_entry_switch.py` 的签名里 **`session` 零命中**；`rg -n "session" server/services/process_runtime/blueprint_entry_switch.py` 的命中只能落在 docstring 的纪律说明段（逐条人工核对并写进 SUMMARY）。
- ⭐ **「守护的守护」存在且覆盖两条谓词**：`cd server && uv run python -c "
src = open('tests/services/process_runtime/test_blueprint_entry_switch.py').read()
assert 'tmp_path' in src, '缺守护的守护（合成违规源码）'
assert 'session.entrypoint' in src, '守护的守护必须用 session.entrypoint 这个真实反面形态'
assert 'ast.Constant' in src, '缺字面量判据'
assert 'entry_key=session.entrypoint' in src, '缺谓词②（entry_key）的反面形态'
assert 'start_orchestration' in src, '扫描器必须覆盖 start_orchestration 的 entry_key'
print('guard-of-guard OK')"` 输出 `guard-of-guard OK`。
- `rg -n "aget_json_setting" server/services/process_runtime/blueprint_entry_switch.py` 命中；`rg -n "^from system|^import system" server/services/process_runtime/blueprint_entry_switch.py` **零命中**（懒 import，⛔ 不在模块级依赖 `system`）。
- **纯追加核算（⚠️ 用 `$PHASE_BASE`，见 prohibitions 第 1 条）**：`git diff $PHASE_BASE -- server/system/models.py | grep -c '^-[^-]' || true` 输出 **0**（⚠️ `grep -c` / `rg -c` 无命中时退出码为 1，**必须**带 `|| true` 否则该断言在 `&&` 链里会被当成失败或被静默跳过）；`rg -n "blueprint.entry.switch|blueprint.assumptions_tiers" server/system/models.py` 两条命中。
- `cd server && uv run python manage.py makemigrations --check --dry-run` 退出码 **0**（两个 SettingKeys 是常量不是模型字段）。
- `cd server && uv run ruff check services/process_runtime/blueprint_entry_switch.py system/models.py tests/services/process_runtime/test_blueprint_entry_switch.py` 通过；`uv run ruff format --check services/process_runtime/blueprint_entry_switch.py tests/services/process_runtime/test_blueprint_entry_switch.py` 通过。
  </acceptance_criteria>
  <done>两个 SettingKeys 以「常量 + JSON 形状注释 + 消费方」体例纯追加且零 migration；`aresolve_entry_process_type` 只收字面量 entry、默认四键全旧链、三层畸形输入全部 fail-soft 回默认且零 `error=` 实参；⭐ 「实参必须是 `ast.Constant`」的源码扫描守卫就位并由「守护的守护」证明其非平凡。</done>
</task>

<task type="auto">
  <name>Task 2: build_engine_for_session 分派器（含 driver）+ deps isinstance 自检 + resume 对称守卫 + entry_key 与退役观察 + ⭐ Wave 0 探针与三条变异用例</name>
  <files>server/services/process_runtime/entrypoint.py, server/services/process_runtime/builtin_processes.py, server/services/process_runtime/resume.py, server/tests/services/process_runtime/test_engine_dispatch.py</files>
  <read_first>server/services/process_runtime/entrypoint.py **全文**（`start_orchestration:32-86` 的完整签名含 `mode` / `feature_segments` / `feature_meta` 三个 feature list 专用参数与 `:68-75` 的「非空才写键」五个 `if`、`:77-86` 的 `create_session` 调用与 `"technical_plan"` 硬编码位、`build_orchestration_engine:89-141` 的四形参与 deps 七项名单、`build_blueprint_engine:144-186` 的两形参与 deps 七项名单、`__all__ +=` 的纯追加先例 `:188-189`）；server/services/process_runtime/resume.py **全文 ~90 行**（docstring 的「绝不在此新建第二个 engine 工厂」、`:24-26` 签名、`:39-57` 的终态集合与 `ClarificationService().ahas_pending` 短路、`max_steps` 超限的 `transition(session, "fail", error={"reason": "advance_step_limit", ...})`）；server/services/process_runtime/blueprint_resume.py **L100-200**（`_safe_log:104-110`、`adrive_blueprint_session_to_pause_or_terminal:112-114` 签名、⭐ **`:132-143` 反向守卫全文**——本 Task 的对称 analog，连 docstring 的论证都镜像、`:145-160` 的终态循环）；server/services/process_runtime/builtin_processes.py **L440-500 与 L780-1000**（`_abp_load_artifact:453-463`、`_abp_mark_drafting:466-490`、`_abp_ensure_blocking_clarification` 全文与 `_h_bp_ai_review:800-820` 的 `needs_clarification` 出边写法、`_h_bp_repo_research` / `_h_bp_merge` 两个 handler **全文**（含各自 `getattr(getattr(engine, "deps", None), "<name>", None)` 那一行的确切形状与 `StageOutcome` 返回形态）、`:850-960` stage graph 的出边名、`:963-970` `technical_plan` 注册项、`:981-991` 蓝图注册项与其三行注释体例）；server/services/process_runtime/blueprint_research_adapter.py **L1-40 与 L1040-1060**（`BlueprintResearchAdapter` 的类名与 `aadvance_reroute:1049` 的存在位置）；server/services/process_runtime/blueprint_merge.py（`BlueprintMergeAdapter` 的**确切类名与所在模块** —— 自检要 import 它，⛔ 不猜）；server/services/process_runtime/architect_merge_adapter.py **L190-265**（`_handle_pass` 经 `ArtifactService.create(ARTIFACT_TYPE_TECHNICAL_PLAN, merged, ...)` `:252-259` 落 **v0 形状 content** 的那一段 —— 变异用例 B 的靶子，**只读，⛔ 不改**）；server/tests/services/process_runtime/test_blueprint_process_graph.py（本 Task 的测试形状 analog：如何构造一条蓝图 `ConvergenceSession` 与如何驱 stage）</read_first>
  <action>
**⭐ ⓪ Wave 0 探针（写任何断言之前先跑，5 分钟；⭐ 只许动一个变量）。** RESEARCH 的 Assumptions A1「用错工厂从 `intake` 驱的落点是 `reroute` 的 `AttributeError`」是**逐 stage 推演、未实跑**。写一段一次性 pytest（可临时放在 `test_engine_dispatch.py` 里、跑完删掉或转成正式用例）：在**尚未加自检**的状态下，构造一条 `process_type="technical_blueprint"` 的会话从 `intake` 驱一遍，打印终局的 `status` / `current_stage` / `error`。

⭐ **探针的驱动器必须是对的那个** —— `build_orchestration_engine()`（**错工厂**）配 `blueprint_resume.adrive_blueprint_session_to_pause_or_terminal`（**对 driver**）。⛔ **绝不用 `resume.adrive_…`**：那会同时动「错工厂」与「错 driver」两个变量，观测到的终局多半是**错 driver 的症状** `advance_step_limit`（旧 driver 的 `ahas_pending` 短路对蓝图恒 False ⇒ self-loop 撞 `max_steps`），把它当成「错工厂的形态」去校准只动一个变量的变异用例 A，得到的期望值必然是错的。

⚠️ **探针结论只在 wave 1 成立**：116-02 落地后 `intake` / `decompose` 不再是空 handler pass-through，同样的错工厂会更早停下——SUMMARY 里逐字注明这条时效性，供后续 plan 判断能否复用该落点。

**把实测结果逐字记进 SUMMARY**。若落点与推演不同（例如更早在 `ResearchDispatchAdapter.dispatch` 内炸），按实测形态调整下面变异用例 A 的期望值 —— ⭐ **调整后的判据必须落在白名单三选一**：(a) 终局三元组 `(current_stage, status, error["reason"])` 逐字相等；(b) `ArtifactVersion.objects.count()` 与调用前相等；(c) 出边为 `needs_clarification` **且**存在一条 `blueprint_stage_wrong_adapter` 事件。⛔ **任何否定式判据一律不接受**（「未到 DONE」「未产版本」「不为 None」皆属此列，P-3：正确实现下会话停在 spec_gate 等澄清也不是 DONE ⇒ 恒绿、变异不敏感）。

**① `entrypoint.py` 追加 `build_engine_for_session`（⭐ No Analog #2，三件事在此写死，⛔ 执行期不得改选）。**

```
def build_engine_for_session(
    session: Any,
    *,
    session_service: Any = None,
    node_execution_id: str = "",
    skip_clarification: bool = False,
    force_confirm: bool = False,
) -> tuple[ProcessEngine, Any]:
```

- **返回二元组 `(engine, driver)`**，driver 是 `adrive_*` 异步可调用（两个 driver 的签名实测逐字相同 `(engine, session, *, max_steps=20)` ⇒ 调用方直接 `engine, adrive = build_engine_for_session(...)` / `session = await adrive(engine, session)`，⛔ 不做参数适配层）。docstring 必须逐字写明**为什么返回二元组**：旧续驱器的 `waiting_clarification` 短路判据是 `ClarificationService().ahas_pending`（`resume.py:53-57`），对蓝图会话恒 False ⇒ 三个 pausable stage 一个都短路不了、self-loop 被推到 `max_steps` 落 `advance_step_limit` FAILED；**只换 engine 不换 driver 仍然坏**。
- **同步函数**（两个工厂皆同步；`session.process_type` 是已加载字段，零 ORM 访问 ⇒ ⛔ 不要写成 async，那会逼六个调用点多一次 await 且无收益）。
- 分派：`ptype = str(getattr(session, "process_type", "") or "")`。
  - `ptype == BLUEPRINT_PROCESS_TYPE`（函数内懒 import 自 `blueprint_resume`）⇒ `return build_blueprint_engine(session_service=session_service, node_execution_id=node_execution_id), adrive_blueprint_session_to_pause_or_terminal`。⛔ **绝不透传 `skip_clarification` / `force_confirm`**（`build_blueprint_engine` 只接两个形参、蓝图链没有 `clarify` dep）；若调用方传了非默认值，记一条 `blueprint_engine_ignored_legacy_flag`（`category="caller"` / `component="process_runtime"` / `session_id` / `flag`）—— **响亮而不失败**。
  - 否则 ⇒ 先判 `ptype not in {"technical_plan", "echo", ""}`：记 `engine_dispatch_unknown_process_type`（`category="caller"` / `component="process_runtime"` / `session_id` / `process_type`）；然后统一 `return build_orchestration_engine(session_service=..., node_execution_id=..., skip_clarification=..., force_confirm=...), adrive_convergence_session_to_pause_or_terminal`。
- `__all__ += ["build_engine_for_session"]`（**另起一行**，⛔ 既有 `__all__` 行一字不动，形状照 `:188-189`）。

**② `entrypoint.start_orchestration` 追加 `entry_key` 形参与退役观察事件。**

- 签名末尾追加 keyword-only `entry_key: str = ""`（默认空串 ⇒ 既有四个调用点零回归；字面量由 116-03 逐点补上）。
- 在 `create_session` **之后**、`return` 之前落一条事件（改成先接 `session = await ...create_session(...)` 再 `return session`，这是本文件允许的 2 行删除配额之一）：
  `technical_plan_entry_used`，字段 `category="caller"` / `component="process_runtime"` / `entry_key=str(entry_key or "unknown")` / `entrypoint=str(entrypoint or "")` / `initiated_by_user_id=str(initiated_by_user_id or "system")` / `session_id=str(session.id)`。整段经一个 best-effort `_safe_log`（⛔ 观测绝不反噬建会话）。
- docstring 追加一段：⭐ **`entry_key` 与 `entrypoint` 是两回事** —— `entrypoint` 进 `ConvergenceSession` 列且有既有消费方，MCP 入口传的是 `"workflow"`（`orchestration_delegate.py:171-178` 的既有约定）；「还有谁在走旧链」的聚合**只能按 `entry_key`**。

**③ `builtin_processes.py` 的两处 deps `isinstance` 自检 + 一处退役观察注释（删除行 0）。**

新增模块内共享 helper（放在 `_abp_ensure_blocking_clarification` 附近）：

```
def _abp_dep_is(dep: Any, expected_module: str, expected_name: str) -> bool
```
—— 用 `type(dep).__module__` / `__name__` 判定而**不是** `isinstance` + 顶层 import，避免在 991 行的 handler 模块里新增两个重型 adapter 的模块级 import（本文件的既有纪律是重依赖一律懒 import）。⚠️ 若执行期发现 `BlueprintResearchAdapter` / `BlueprintMergeAdapter` 已可零成本懒 import，用 `isinstance` 亦可 —— **判据本身必须是「类型身份」而不是「有没有某个方法」**（后者会被鸭子类型的测试替身绕过，且 `ResearchDispatchAdapter` 恰好也有 `dispatch`）。

- `_h_bp_repo_research`：取到 `dep` 之后、调用之前，`dep is not None and not _abp_dep_is(dep, "...blueprint_research_adapter", "BlueprintResearchAdapter")` ⇒ 落 `blueprint_stage_wrong_adapter`（`category="caller"` / `component="process_runtime"` / `session_id` / `stage="repo_research"` / `got=type(dep).__name__`）+ `await _abp_ensure_blocking_clarification(...)` + 返回 `needs_clarification` 出边的 `StageOutcome`（形状逐字照 `:809-814`）。
- `_h_bp_merge`：同款，`expected = BlueprintMergeAdapter`，`stage="merge"`。⭐ **这一条是唯一能挡住 `ArchitectMergeAdapter.merge` 往蓝图会话落一份 v0 content 的手段**（`architect_merge_adapter.py:252-259`），注释里逐字写明。
- ⚠️ `dep is None` 时**维持既有 pass-through 行为不变**（那是「注册了但没注入」的既有语义，改它会波及测试替身）。
- `technical_plan` 注册项（`:963`）**上方**追加三行注释（形状照 `:981-983`）：「⚠️ 退役观察期（116-01）：旧 process 仍**保留注册**——在途会话续驱依赖它，注销即崩。『不再是任何入口默认』的收口顺延同步点 2 后的收尾 plan；当前残余流量经 `start_orchestration` 内的 `technical_plan_entry_used` 事件（按 `entry_key` 分桶）可 SQL 聚合。⛔ 六个冻结文件一行不改。」

**④ `resume.py` 顶部对称守卫（删除行 0）。**

在 `adrive_convergence_session_to_pause_or_terminal` 函数体**第一行**（懒 import 之前）插入，逐字镜像 `blueprint_resume.py:132-143`：

```
if str(getattr(session, "process_type", "")) == BLUEPRINT_PROCESS_TYPE:
    # 旧续驱器的 waiting_clarification 短路判据是 ClarificationService().ahas_pending，
    # 对蓝图会话恒 False ⇒ 三个 pausable stage 一个都短路不了，self-loop 会被推到
    # max_steps 落 advance_step_limit FAILED。宁可 no-op：调用方传错 driver 是 bug，
    # 不是「该会话该失败」。（116-01，与 blueprint_resume.py:132-143 对称）
    logger.warning(
        "wrong_driver_for_blueprint_session",
        category="caller",
        component="process_runtime",
        session_id=str(getattr(session, "id", "")),
        process_type=str(getattr(session, "process_type", "")),
    )
    return session
```

⚠️ `resume.py` 现在没有 module-level `logger` ⇒ 一并追加 `import structlog` + `logger = structlog.get_logger(__name__)`（这是本文件唯一允许的额外追加）。`BLUEPRINT_PROCESS_TYPE` **函数内懒 import**（`resume.py` 是旧链共享面，⛔ 不在模块级依赖 `blueprint_resume`）。
⭐ **只挡蓝图会话**：⛔ 不得顺手加别的 process_type 判断（`plan_deepen` 等非蓝图调用方也走这条 driver）。

**⑤ 新建 `server/tests/services/process_runtime/test_engine_dispatch.py`**，`pytestmark = pytest.mark.django_db(transaction=True)`，docstring「守七件事」编号清单：

1. **正向分派（两 process_type × 两件事）**：`technical_blueprint` 会话 ⇒ `engine.deps.research` 的类型名是 `BlueprintResearchAdapter`、`engine.deps.merge` 是 `BlueprintMergeAdapter`、driver **is** `blueprint_resume.adrive_blueprint_session_to_pause_or_terminal`；`technical_plan` 会话 ⇒ `ResearchDispatchAdapter` / `ArchitectMergeAdapter` / driver **is** `resume.adrive_convergence_session_to_pause_or_terminal`。⭐ 用 `is` 断言 driver 身份（⛔ 不断言函数名字符串）。
2. **未知 process_type 回落 + 事件**：`process_type="whatever"` ⇒ 返回旧链二元组，且捕获到一条 `engine_dispatch_unknown_process_type`（用 `structlog` 的 capture 或 `caplog`，形状照仓内既有事件断言的做法）。
3. **⛔ 不透传旧链开关**：`build_engine_for_session(blueprint_session, skip_clarification=True, force_confirm=True)` **不抛** TypeError，且返回的蓝图 engine 的 `deps` 上 `clarify` 属性**不存在**（`getattr(deps, "clarify", None) is None`）；同时落一条 `blueprint_engine_ignored_legacy_flag`。
4. ⭐ **变异 A（错工厂 · `repo_research`）**：构造一条推进到 `repo_research` 的蓝图会话，用 `build_orchestration_engine()` 驱一步 ⇒ 断言 (a) 会话落 `needs_clarification` 语义的出边、(b) 存在一条 `blueprint_stage_wrong_adapter` 事件且 `got == "ResearchDispatchAdapter"`、(c) **`ArtifactVersion.objects.count()` 与调用前相等**。**变异验证**：临时删掉 `_h_bp_repo_research` 的自检分支 ⇒ 本用例转红（实跑一次并把红/绿输出记进 SUMMARY）。
5. ⭐ **变异 B（错工厂 · `merge`，最坏形态）**：构造一条推进到 `merge` 的蓝图会话，用 `build_orchestration_engine()` 驱一步 ⇒ 断言 (a) **`ArtifactVersion.objects.count()` 不变**、(b) 全库不存在任何 `content.get("schema_version") != "blueprint/v1"` 的**新** `ArtifactVersion`、(c) 有 `blueprint_stage_wrong_adapter` 且 `stage == "merge"`。**变异验证**：删掉 `_h_bp_merge` 的自检 ⇒ 转红（落一份 v0 content）。
6. ⭐ **变异 C（错 driver）**：`await resume.adrive_convergence_session_to_pause_or_terminal(build_blueprint_engine(), blueprint_session)` ⇒ 断言 (a) 返回的 session `status` 与 `current_stage` **与调用前逐字相同**、(b) 有一条 `wrong_driver_for_blueprint_session` 事件、(c) `status != FAILED` 且 `error` 里**不含** `advance_step_limit`。**变异验证**：删掉 `resume.py` 的守卫 ⇒ 会话被推到 `advance_step_limit` FAILED ⇒ 转红。
7. **反向对照（守卫不误伤）**：旧 driver 驱一条 `technical_plan` 会话仍正常推进（⛔ 证明第 6 条的断言不是恒真）；蓝图 driver 驱一条 `technical_plan` 会话仍走既有的 `blueprint_resume_wrong_process_type` no-op（既有行为零回归）。

⚠️ 构造会话与推进 stage 的写法逐字参考 `tests/services/process_runtime/test_blueprint_process_graph.py`；async service 用例记得 `transaction=True`。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/process_runtime/test_engine_dispatch.py -x -q && uv run pytest tests/services/process_runtime/ tests/services/test_answer_resume.py -q</automated>
  </verify>
  <acceptance_criteria>
- `cd server && uv run pytest tests/services/process_runtime/test_engine_dispatch.py -q` 全绿；`rg -c "def test_" server/tests/services/process_runtime/test_engine_dispatch.py` ≥ **9**。
- ⭐ **返回二元组且 driver 一起换**：`cd server && uv run python -c "
import os, django, inspect
os.environ.setdefault('DJANGO_SETTINGS_MODULE','friday.settings'); django.setup()
from services.process_runtime.entrypoint import build_engine_for_session as f
sig = inspect.signature(f)
assert 'session' in sig.parameters, '缺 session 形参'
assert not inspect.iscoroutinefunction(f), '必须是同步函数'
src = inspect.getsource(f)
assert 'adrive_blueprint_session_to_pause_or_terminal' in src and 'adrive_convergence_session_to_pause_or_terminal' in src, 'driver 必须一起分派'
print('dispatch contract OK')"` 输出 `dispatch contract OK`。
- ⭐ **⛔ 不透传旧链开关进蓝图工厂**：`cd server && uv run python -c "
import inspect, os, django
os.environ.setdefault('DJANGO_SETTINGS_MODULE','friday.settings'); django.setup()
from services.process_runtime.entrypoint import build_engine_for_session as f
src = inspect.getsource(f)
i = src.index('build_blueprint_engine')
seg = src[i:src.index(')', i)]
assert 'skip_clarification' not in seg and 'force_confirm' not in seg, '蓝图工厂分支不得透传旧链开关'
print('no legacy flag leak OK')"` 输出 `no legacy flag leak OK`。
- ⭐ **自检是类型身份判据不是方法探测**：`cd server && uv run python -c "
src = open('services/process_runtime/builtin_processes.py').read()
assert 'blueprint_stage_wrong_adapter' in src, '缺自检事件'
assert src.count('blueprint_stage_wrong_adapter') >= 2, '两个 handler 都要有'
assert 'hasattr(' not in src.split('blueprint_stage_wrong_adapter')[0][-800:], '自检不得用 hasattr 探测方法'
print('dep self-check OK')"` 输出 `dep self-check OK`；且 `rg -n \"BlueprintResearchAdapter|BlueprintMergeAdapter\" server/services/process_runtime/builtin_processes.py` 两个类名各命中。
- ⭐ **对称守卫**：`rg -n "wrong_driver_for_blueprint_session" server/services/process_runtime/resume.py` 命中；`cd server && uv run python -c "
src = open('services/process_runtime/resume.py').read()
i = src.index('async def adrive_convergence_session_to_pause_or_terminal')
body = src[i:i+1600]
assert 'BLUEPRINT_PROCESS_TYPE' in body, '守卫必须在该函数体内'
assert 'return session' in body.split('wrong_driver_for_blueprint_session')[1][:400], '守卫必须 no-op 返回'
print('symmetric guard OK')"` 输出 `symmetric guard OK`；`rg -c "process_type" server/services/process_runtime/resume.py` 的命中**只落在这一个守卫块内**（人工核对写进 SUMMARY，⛔ 不得顺手加别的 process_type 判断）。
- ⭐ **退役观察事件按 `entry_key` 不按 `entrypoint`**：`cd server && uv run python -c "
src = open('services/process_runtime/entrypoint.py').read()
i = src.index('technical_plan_entry_used')
seg = src[i:i+600]
assert 'entry_key=' in seg, '事件必须带 entry_key'
print('retirement event OK')"` 输出 `retirement event OK`；`rg -n "entry_key" server/services/process_runtime/entrypoint.py` 命中 ≥3（形参 + docstring + 事件）。
- **纯追加核算（⚠️ 全部用 `$PHASE_BASE` + `|| true`）**：`git diff $PHASE_BASE -- server/services/process_runtime/builtin_processes.py | grep -c '^-[^-]' || true` 输出 **0**；`git diff $PHASE_BASE -- server/services/process_runtime/resume.py | grep -c '^-[^-]' || true` 输出 **0**；`git diff $PHASE_BASE -- server/services/process_runtime/entrypoint.py | grep -c '^-[^-]' || true` 输出 ≤ **2**。
- ⭐ **六个冻结文件零改动**：`git diff $PHASE_BASE --name-only | rg "decompose_segments|research_adapter\.py|architect_merge_adapter|merged_plan|clarify_adapter|process_runtime/render\.py"` **零命中**；`git diff $PHASE_BASE -- server/codegraph/services/repo_router_v2.py` 输出**为空**。
- ⭐ **`builtin_processes.py` 的新增分支零 `error=`**（该文件在 `_SCANNED_MODULES` 十二项内）：`cd server && uv run pytest tests/delivery/test_blueprint_log_redaction_guard.py -q` 全绿。
- ⭐ **`technical_plan` 注册未被注销**：`rg -n 'register_process_type\(\s*$|register_process_type\("technical_plan"' server/services/process_runtime/builtin_processes.py` 仍命中；`cd server && uv run pytest tests/services/process_runtime/test_blueprint_process_graph.py -q` 全绿。
- ⭐ **三条变异各跑一次真实变异并登记**：SUMMARY 里逐条写「删掉 X ⇒ 用例 Y 转红（贴错误首行）；恢复即绿」。⛔ 只声明不实跑不算。
- `cd server && uv run pytest tests/services/ -q` 无新增失败；`uv run ruff check services/process_runtime/entrypoint.py services/process_runtime/resume.py services/process_runtime/builtin_processes.py tests/services/process_runtime/test_engine_dispatch.py` 通过。
  </acceptance_criteria>
  <done>`build_engine_for_session` 就位并**同时换 driver**，未知 process_type 回落且响亮；两个同名 dep 有类型身份自检、旧工厂再也无法往蓝图会话落 v0 content；旧 driver 有对称守卫且只挡蓝图会话；`entry_key` 与 `technical_plan_entry_used` 让残余旧链流量可按真实入口聚合；三条变异用例的期望值按 Wave 0 探针实测形态写死并各自完成一次真实变异验证。</done>
</task>

<task type="auto">
  <name>Task 3: blueprint-gate/ 八端点补项目范围闸（更严变体：两个失败分支同一中性 404）+ confirm 两处 409 补 blocked_reason + 全量后端门</name>
  <files>server/delivery/api/blueprint_gate_views.py, server/tests/delivery/test_blueprint_gate_scope.py</files>
  <read_first>server/delivery/api/blueprint_gate_views.py **全文**（模块 docstring 的契约书体例；`_aload_artifact:70` / `_aload_session:76`（**带 `process_type` 过滤**，其 docstring 记为 CRITICAL）/ `_aload_gate_context:98` 的三元组返回；`_aserialize_snapshot:110`；`_aapply_action:163-198` 的 `(error, result, session)` 三元组形态与各 View 的 `if error is not None: return error` 用法；⭐ **八个 View 全文**（`:199` / `:220` / `:270` / `:291` / `:313` / `:334` / `:358` / `:441`）—— 逐个确认闸该插在哪一行；⭐ **两处 409 全文 `:236-256`**；`_GATE_NOT_OPEN_DETAIL` 与 `_LOCK_BLOCKED_MESSAGES` / `_LOCK_DEFAULT` 三个常量的定义位置与逐字文案；`_is_uuid:501`；`_ablueprint_project_id:511-522` 的**确切实现**（取哪一版 content、读哪个键、取不到返什么）；`BlueprintRejectedToBoundaryView:385` 处**既有的唯一一次** `_ablueprint_project_id` 调用及其当前的失败处置——本 Task 要把它改成走同一个 helper，⛔ 不留第二份口径）；server/delivery/api/blueprint_review_views.py **L223-294**（`_is_uuid:223` / `_ablueprint_project_id:233` / `_ais_project_member:244-251` 的**确切查询形态** / `_aassert_project_scope:254-281` 的四语义与 superuser 直通行 `:274` / `_ARTIFACT_MISSING_DETAIL` 常量 / `_log:284-294`）—— ⛔ **只 import `_ais_project_member` 这一个零件**，`_aassert_project_scope` 不 import 不复制；server/tests/delivery/test_blueprint_review_views.py **L1-140**（docstring「守 N 件事」体例、`pytestmark = pytest.mark.django_db(transaction=True)`、`async_to_sync` 装配 `:33-34`、⭐ **范围闸工厂 `_SCOPE_PROJECT_ID` / `_OTHER_PROJECT_ID` / `_make_project(project_id, member=...)` `:83-100` —— 逐字复用**、「非成员 404 与不存在 404 响应体逐字相同」那条用例的完整写法）；web/src/components/blueprint/__tests__/gatePanel.spec.ts **L570-600**（⭐ **只读**：`:577` 的 `blocked_reason: 'pending_clarification'` ⇒ 出现「前往未决线程」并 emit `goto-unresolved`、`:591` 的其余 `blocked_reason` ⇒ 只回显 detail —— 后端补键的**取值必须与这两条用例同族**，⛔ 前端一行不改）</read_first>
  <action>
**① 新建薄范围闸 helper（放在 `_ablueprint_project_id` 附近，与既有零件同段）。**

模块级常量：`_SCOPE_MISSING_DETAIL = {"detail": ...}` —— ⭐ **两个失败分支共用同一个常量对象**，文案取「门未开启 / 不存在」语义的中性表述，与本文件既有 `_GATE_NOT_OPEN_DETAIL` **保持同族**（该链的 404 本就混合三种语义，前端 115-07 按「非 200 只决定挂载点是否渲染」实现 ⇒ 三种 404 行为一致）。⚠️ 若 `_GATE_NOT_OPEN_DETAIL` 的文案已经足够中性，**直接复用它**，⛔ 不造第二个常量（少一份可漂移副本）。

```
async def _aassert_gate_scope(request: Any, artifact: Any) -> Response | None
```

四条语义（⭐ 与 `blueprint_review_views._aassert_project_scope` 的**唯一区别**是第二条）：
1. `getattr(request.user, "is_superuser", False)` ⇒ `return None`（直通）。
2. `project_id = await _ablueprint_project_id(artifact)`；`not _is_uuid(project_id)` ⇒ **`Response(<中性 404 常量>, status=404)`** ⭐ **不是 400**。docstring 逐字写明理由：「gate 链的 404 本就混合三种语义（门未开 / artifact 不存在 / 无蓝图会话），前端（115-07）按『非 200 只决定挂载点是否渲染、不进错误分档』实现 ⇒ 回中性 404 **零新增存在性暴露面**；⛔ 也因此不 import `blueprint_review_views._aassert_project_scope` —— 它的 400 分支正是 115-MN-03 判为『设计决策、本轮不改』的那条暴露面，import 会把它扩到这八个端点」。
3. `not await _ais_project_member(request.user, project_id)` ⇒ **同一个中性 404 常量对象**（⭐ 与第 2 条**逐字相同**，这是「零新增存在性暴露面」的唯一可证伪形态）。
4. 放行 `return None`。

import：`from delivery.api.blueprint_review_views import _ais_project_member`，⭐ **带注释说明为何 import 私有符号且只 import 这一个**（形状照 `blueprint_doc_views.py:47-57` 的说明书体例）。

**② 八个 View 挂闸（删除行 ≤ 6）。**

挂载位置：**`_aload_gate_context` / `_aload_artifact` 之后、任何 service 调用之前**。逐个 View 处置：

| View | 现状 | 改法 |
|---|---|---|
| `BlueprintGateSnapshotView`（`:199`） | 只 `IsAuthenticated` | 取到 artifact 后立刻 `err = await _aassert_gate_scope(request, artifact)`；`if err is not None: return err` |
| `BlueprintGateConfirmView`（`:220`，**破坏性写**） | 同上 | 同上，且**必须在 `_aapply_action` / `alock` 之前** |
| `BlueprintGateRemoveRepoView`（`:270`，**破坏性写**） | 同上 | 同上 |
| `BlueprintGateAddRepoView`（`:291`，**破坏性写**） | 同上 | 同上 |
| `BlueprintGateReclassifyRoleView`（`:313`） | 同上 | 同上 |
| `BlueprintGateEditResponsibilityView`（`:334`） | 同上 | 同上 |
| `BlueprintRejectedToBoundaryView`（`:358`，`:385` 有既有单点调用） | 已调 `_ablueprint_project_id` 但无成员校验 | ⭐ 改为走 `_aassert_gate_scope`，**既有那处零散调用与其失败处置一并收进 helper**（⛔ 不留第二份口径）；若它后续仍需要 `project_id` 值，让 helper 返回 `None` 后再取一次或把 helper 拆成 `(project_id, err)` 二元组——⭐ **二选一在执行期定，但全文件必须只有一份判据** |
| `BlueprintGateUpgradeResearchView`（`:441`） | 只 `IsAuthenticated` | 同上 |

⚠️ 若多个 View 共用 `_aapply_action`，**优先把闸插进 `_aapply_action` 内部**（一处生效多处）——但 Snapshot 与不走该 helper 的 View 仍需各自挂。执行期按实读的调用图选：**判据只有一份**，挂载点可以有多处。

**③ `confirm/` 两处 409 各补 `blocked_reason` 键。**

- `:239-240`：`return Response({"detail": "存在未解决的阻塞澄清线程", "blocked_reason": "pending_clarification"}, status=409)` —— 键值**逐字** `pending_clarification`（与 `gatePanel.spec.ts:577` 同族）。
- `:247-256`：在既有 `{"detail": _LOCK_BLOCKED_MESSAGES.get(...)}` 上追加 `"blocked_reason": str(lock.get("reason") or "")`（**原样透传**，`snapshot_changed` 等；与 `gatePanel.spec.ts:591` 那一档同族）。
- ⛔ **前端零改动**（前端已按机器可读键实现两档分流且已有用例）。

**④ 新建 `server/tests/delivery/test_blueprint_gate_scope.py`**，`pytestmark = pytest.mark.django_db(transaction=True)`，docstring「守六件事」：

1. ⭐ **八端点参数化非成员用例**：用 `_make_project(_SCOPE_PROJECT_ID, member=...)` 工厂造一份蓝图，非成员调用八个端点（`reverse("blueprint-gate-*")` 八个 name）⇒ **全部 404**。⛔ 八条必须参数化而不是只测 confirm。
2. ⭐ **两个失败分支响应体逐字相同**：(a) 蓝图 `meta.project_id` 缺失/非 UUID ⇒ 404；(b) 有合法 project_id 但调用者非成员 ⇒ 404；`assert a.json() == b.json()`。**这是「零新增存在性暴露面」的唯一可证伪形态。**
3. **成员 200 / superuser 200**（⛔ 证明第 1 条断言非恒真）：同一 artifact 同一端点，成员与 superuser 各跑一遍拿到非 404。
4. ⭐ **三条破坏性写在非成员调用后 DB 一字未动**：`confirm` / `remove-repo` / `add-repo` 各跑一遍非成员调用，断言 `Artifact.blueprint_status`（用 `current_status` 之类的读法，⛔ **响应键与字典键不得出现字面 `blueprint_status`**，见 P-1 纪律）与关联仓集合**与调用前逐字相同**。
5. ⭐ **`confirm/` 两处 409 的 `blocked_reason`**：(a) 造一条 open+blocking 的 `ai_clarification` 线程 ⇒ 409 且 `body["blocked_reason"] == "pending_clarification"`；(b) 让 `alock` 返回非 `confirmed` ⇒ 409 且 `body["blocked_reason"]` **非空**且等于 `lock["reason"]`。
6. **未认证一律拒**（401/403），八端点参数化。

⚠️ ⛔ **响应键 / 字典键不得出现字面 `blueprint_status`**（`test_blueprint_inv6_guard.py` 的 `_RE_FIELD_DICT_KEY` 扫整个 `server/` 含 tests？—— 实读确认 tests 在豁免面内，但**生产代码**不得出现；测试内读模型字段用 `artifact.blueprint_status` 的**比较**形态是安全的，因为 `_RE_FIELD_WRITE` 的 `[^=]` 排除了 `==`）。写之前手工验算三条正则。

**⑤ 全量后端门**（本 Task 收尾执行并登记进 SUMMARY）：`cd server && uv run pytest tests/ -q` 与基线 **8609 passed / 1 failed** 逐条比对；`uv run python manage.py makemigrations --check --dry-run` 退出码 0 且 `git status --porcelain server/*/migrations/` 为空。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/delivery/test_blueprint_gate_scope.py -x -q && uv run pytest tests/delivery/ -q</automated>
  </verify>
  <acceptance_criteria>
- `cd server && uv run pytest tests/delivery/test_blueprint_gate_scope.py -q` 全绿；`rg -c "def test_" server/tests/delivery/test_blueprint_gate_scope.py` ≥ **10**。
- ⭐ **八个 View 都挂上了闸**：`cd server && uv run python -c "
import re
src = open('delivery/api/blueprint_gate_views.py').read()
classes = re.findall(r'^class (Blueprint\w+View)\(', src, re.M)
assert len(classes) == 8, classes
# 判据只有一份
assert src.count('async def _aassert_gate_scope') == 1, '范围闸判据必须只有一份'
# 每个 View 的类体内（到下一个 class 为止）必须能到达闸：或直接调用、或经 _aapply_action
bodies = {}
for i, c in enumerate(classes):
    s = src.index('class %s(' % c)
    e = src.index('\nclass ', s + 1) if i + 1 < len(classes) else len(src)
    bodies[c] = src[s:e]
missing = [c for c, b in bodies.items() if '_aassert_gate_scope' not in b and '_aapply_action' not in b]
assert not missing, missing
# ⭐ C7：只经 _aapply_action 的 View，必须证明闸确实在 _aapply_action 体内
indirect = [c for c, b in bodies.items() if '_aassert_gate_scope' not in b]
if indirect:
    j = src.index('async def _aapply_action')
    k = src.index('\nclass ', j)
    assert '_aassert_gate_scope' in src[j:k], (
        '这些 View 只经 _aapply_action 间接挂闸，但 _aapply_action 体内没有闸：%s' % indirect
    )
print('all 8 gate views scoped OK')"` 输出 `all 8 gate views scoped OK`。⚠️ 若 `_aapply_action` 之后没有 `class` 行，把 `k` 取成 `len(src)`（执行期按实读的文件结构调整切片边界，**判据不变**）。
- ⭐ **⛔ 不 import review 的 `_aassert_project_scope`**：`rg -n "_aassert_project_scope" server/delivery/api/blueprint_gate_views.py` **零命中**；`rg -n "from delivery.api.blueprint_review_views import" server/delivery/api/blueprint_gate_views.py` 命中且**只 import `_ais_project_member`**（人工核对该 import 行的符号清单并写进 SUMMARY）。
- ⭐ **两个失败分支同一个常量对象**：`cd server && uv run python -c "
import re
src = open('delivery/api/blueprint_gate_views.py').read()
i = src.index('async def _aassert_gate_scope')
body = src[i: src.index('\n\n\n', i) if '\n\n\n' in src[i:] else len(src)]
assert body.count('status=status.HTTP_404_NOT_FOUND') >= 2 or body.count('status=404') >= 2, '两个失败分支都必须 404'
assert 'HTTP_400_BAD_REQUEST' not in body and 'status=400' not in body, '⛔ gate 闸不得有 400 分支（更严变体）'
print('stricter variant OK')"` 输出 `stricter variant OK`。
- ⭐ **中性 404 逐字相同有断言**：`rg -n "\.json\(\) ==" server/tests/delivery/test_blueprint_gate_scope.py` 命中。
- ⭐ **两处 409 补键**：`rg -c "blocked_reason" server/delivery/api/blueprint_gate_views.py` == **2**；`rg -n "pending_clarification" server/delivery/api/blueprint_gate_views.py` 命中；`rg -n 'lock.get\("reason"\)' server/delivery/api/blueprint_gate_views.py` 命中。
- ⭐ **前端零改动**：`git diff $PHASE_BASE --name-only | rg "^web/"` **零命中**；`cd web && pnpm exec vitest run src/components/blueprint/__tests__/gatePanel.spec.ts` 全绿（⚠️ 跑完 `git status` 检查并 `git checkout -- web/pnpm-workspace.yaml`，P-15）。
- **INV-6 与 TOCTOU 两条守卫仍绿**：`cd server && uv run pytest tests/delivery/test_blueprint_inv6_guard.py tests/delivery/test_blueprint_review_threads.py -q` 全绿。
- **删除行核算**：`git diff $PHASE_BASE -- server/delivery/api/blueprint_gate_views.py | grep -c '^-[^-]' || true` 输出 ≤ **6**（逐行登记进 SUMMARY）。
- ⭐ **全量后端门**：`cd server && uv run pytest tests/ -q` 相对基线（8609 passed / 1 failed，唯一失败为 `test_skills_snapshot_guard::test_skill_files_discovered`）**无新增失败** —— 逐条差异写进 SUMMARY。
- `cd server && uv run python manage.py makemigrations --check --dry-run` 退出码 **0**；`git status --porcelain server/delivery/migrations/ server/system/migrations/` 输出**为空**。
- 相位边界自检：`git diff $PHASE_BASE --name-only -- server web` 只含本 plan 声明的九个文件（⚠️ 裸 `git diff $PHASE_BASE --name-only` 在逐 Task 原子提交后恒空 ⇒ 该断言必须带 `$PHASE_BASE`；⚠️ 用 `-- server web` 限定路径，`.planning/` 下的 SUMMARY 与 STATE 维护改动不计入边界）。
- `cd server && uv run ruff check delivery/api/blueprint_gate_views.py tests/delivery/test_blueprint_gate_scope.py` 通过；`uv run ruff format --check tests/delivery/test_blueprint_gate_scope.py` 通过。
  </acceptance_criteria>
  <done>八个 gate 端点全部挂上项目范围闸且**判据只有一份**；读不到 `meta.project_id` 与非成员回同一个中性 404 响应体（有逐字相同断言），三条破坏性写在非成员调用后 DB 一字未动；`confirm/` 两处 409 补上 `blocked_reason` 让 115-07 已实现的「一键跳未决线程」在生产生效，前端零改动；全量后端门相对基线无新增失败、零 migration。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 未认证 / 非成员请求 → `blueprint-gate/` 八端点 | 唯一 fail-closed 不降级的边界；其中三个是破坏性写（确认锁定仓库集 / 移除仓 / 加仓），越权调用直接改变别人项目的蓝图编排走向 |
| 「artifact 存在」这一事实本身 | 该链的 404 已混合三种语义；补闸必须**不**引入第四种可区分的状态码，否则反而新开存在性预言机 |
| 调用方选的 engine 工厂 / driver → 蓝图会话 | 两个工厂有两个同名 dep（`research` / `merge`），错工厂能往蓝图会话落一份 v0 content（篡改产物指针）且全程无异常 |
| 运行时开关值 → 入口 process 选择 | 开关是「回滚 = 改一个设置值」的唯一支点；从 session 反推会让一个键连带切走两个入口 |
| 上游 / 异常文本 → 日志 | 开关模块与三处事件都可能落异常文本 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-116-01 | Elevation of Privilege | `blueprint-gate/` 七个 View 无项目范围闸，其中 `confirm` / `remove-repo` / `add-repo` 是破坏性写 ⇒ 任一登录用户可改任意项目的蓝图仓库集与职责 | mitigate | `_aassert_gate_scope` 挂满八个 View；Task 3 第 1 条八端点参数化非成员 404；第 4 条「三条破坏性写在非成员调用后 DB 一字未动」 |
| T-116-02 | Information Disclosure | 补闸引入 400/404 可区分 ⇒ 新开存在性预言机（正是 115-MN-03 那条被判为设计决策的暴露面被扩大） | mitigate | ⭐ 更严变体：两个失败分支同一个中性 404 常量对象 + `a.json() == b.json()` 断言 + 源码扫描断言闸内**不存在** 400 分支；⛔ 不 import `_aassert_project_scope` |
| T-116-03 | Tampering | 错工厂驱蓝图会话 ⇒ `ArchitectMergeAdapter` 经 `ArtifactService.create` 落一份 **v0 形状 content**，把产物指针钉到非 `blueprint/v1` 的版本（同时废掉 SC-3 判别与 SC-4 门控），全程无异常 | mitigate | `_h_bp_merge` 的类型身份自检 + 变异用例 B（`ArtifactVersion` 计数不变 + 全库无 `schema_version != blueprint/v1` 的新版本）+ 一次真实变异验证 |
| T-116-04 | Denial of Service | 只换 engine 不换 driver ⇒ 旧 driver 的 `ahas_pending` 短路对蓝图恒 False ⇒ 三个 pausable stage 全部 self-loop 到 `max_steps` 落 `advance_step_limit` FAILED（用户看到「方案编排失败」） | mitigate | 分派器返回二元组 + `resume.py` 对称守卫 no-op + 变异用例 C（状态一字未变 + 无 `advance_step_limit`）+ 反向对照（旧会话仍正常推进） |
| T-116-05 | Spoofing | per-entry 开关从 `session.entrypoint` 反推 ⇒ MCP 流量伪装成 workflow，打开 workflow 键把 MCP 一起切走 | mitigate | `aresolve_entry_process_type(entry: str)` 只收字面量 + `ast.Constant` 源码扫描 + ⭐「守护的守护」证明扫描器非平凡 |
| T-116-06 | Repudiation | 退役观察事件按 `entrypoint` 分桶 ⇒ MCP 计进 workflow 桶，「还有谁在走旧链」的聚合从第一天起就是错的且永不报错 | mitigate | 独立 `entry_key` 形参 + 事件字段断言（`entry_key=` 必须出现在事件段内）；116-03 逐点补字面量并由 T-116-05 的扫描器覆盖 |
| T-116-07 | Information Disclosure | 开关模块兜底分支把异常文本写进日志（该模块刻意不进 `_SCANNED_MODULES`） | mitigate | 模块内 `error=` **零命中**的 grep 断言 + 兜底只记 `reason` 标量 |
| T-116-08 | Denial of Service | 未知 process_type 让分派器抛异常 ⇒ 将来注册第五个 process 时全链崩 | mitigate | 回落旧链 + `engine_dispatch_unknown_process_type` caller 事件；Task 2 第 2 条用例 |
| T-116-09 | Tampering | 「退役观察」被实现成注销 `technical_plan` 注册 ⇒ 在途会话续驱直接崩 | mitigate | prohibitions 明令 + 验收断言 `register_process_type("technical_plan"` 仍命中 + `test_blueprint_process_graph.py` 保持绿 |
| T-116-SC | Tampering | 依赖供应链 | accept | 零新增外部依赖、零 migration、零新 `CallSource` 枚举、零新事件常量进 `BLUEPRINT_EVENTS`；`makemigrations --check` 退出码 0 |
</threat_model>

<verification>
- `cd server && uv run pytest tests/services/process_runtime/ -q` 全绿（含既有 stage graph / spec_gate / resume 映射用例，证明分派与自检零回归）。
- `cd server && uv run pytest tests/delivery/ -q` 全绿（含 112 确认门八端点、114 七端点、115 五端点、三条守卫扫描）。
- ⭐ `cd server && uv run pytest tests/ -q` 相对基线（8609 passed / 1 failed）**无新增失败**。
- `cd server && uv run python manage.py makemigrations --check --dry-run` 退出码 0；相位内**零** migration 文件。
- ⭐ **三条变异各实跑一次**（删 → 转红 → 恢复 → 转绿），红/绿输出首行贴进 SUMMARY。
- ⭐ **Wave 0 探针结果逐字登记**（终局 stage / status / error、所用的 engine 工厂与 driver 组合、「只在 wave 1 成立」的时效性说明），并说明变异用例 A 的期望值是否据此调整、调整后落在白名单三条中的哪一条。
- 冻结面核算：`git diff $PHASE_BASE -- <file>` 对六个 `technical_plan` 冻结文件与 `repo_router_v2.py` **均为空**；`git diff $PHASE_BASE --name-only | rg "^web/"` 零命中。
- 删除行核算：`builtin_processes.py` = 0、`resume.py` = 0、`system/models.py` = 0、`entrypoint.py` ≤ 2、`blueprint_gate_views.py` ≤ 6（逐行登记）。
- `cd server && uv run ruff check services/process_runtime/ delivery/api/blueprint_gate_views.py tests/services/process_runtime/ tests/delivery/test_blueprint_gate_scope.py` 通过。
</verification>

<success_criteria>
- **本相位后续六个 plan 有了安全的地基**：任何调用方从此刻起只要写 `engine, adrive = build_engine_for_session(session)` 就同时拿对 engine 与 driver；用错工厂再也无法静默污染蓝图产物，用错 driver 再也无法把健康会话推成 FAILED——三条都有变异用例背书而不是靠声明。
- **开关就位且回滚是改一个设置值**：四个入口各自独立、默认全部留在旧链、值域外与畸形输入一律 fail-soft 回旧链；「绝不从 session 反推」被 `ast` 扫描 + 守护的守护钉成可证伪事实（PATTERNS §0.1 那条新发现的硬伤在代码落地前就被堵死）。
- **旧链残余流量第一次可被 SQL 聚合且分桶正确**：`technical_plan_entry_used` 按独立的 `entry_key` 记录，⛔ 不受「MCP 的 `entrypoint` 实测是 `workflow`」这条既有约定污染。
- **115-07 登记的两条后端缺口就此闭合**：`blueprint-gate/` 八端点（含三条破坏性写）全部挂上项目范围闸且**零新增存在性暴露面**（两个失败分支同一中性 404，有逐字相同断言）；`confirm/` 的 409 补上 `blocked_reason`，让前端已实现且已有用例的「一键跳未决线程」在生产真正生效，**前端零改动**。
- 全量后端门相对基线无新增失败、零 migration、六个冻结文件与 `repo_router_v2.py` 逐字未动。
</success_criteria>

<output>
完成后创建 `.planning/phases/116-entry/116-01-SUMMARY.md`，provides 至少包含：
- ⭐ **`PHASE_BASE` 的 sha**（本相位后续六个 plan 各自开工时同样取一次自己的 base；本条是「所有边界断言非恒真」的前提证据）；
- ⭐ **`build_engine_for_session` 的逐字签名与返回契约**（二元组 / 同步 / 未知 process_type 的行为 / ⛔ 不透传两个旧链开关），116-03 的六个续驱点直接照它改；
- ⭐ **Wave 0 探针的实测终局**（stage / status / error 逐字）与「变异用例 A 的期望值是否据此调整」的结论；
- ⭐ **三条变异验证的红/绿实跑记录**（删了什么 → 哪条用例转红 → 错误首行）；
- `aresolve_entry_process_type` 的签名、四个 `ENTRY_*` 常量名、默认值表，以及 ⭐「实参必须是字面量」的扫描器覆盖面清单（116-03 新增调用点即自动纳入）；
- 两个 SettingKeys 的键名、JSON 形状与**默认值**（116-06 消费 `BLUEPRINT_ASSUMPTIONS_TIERS`）；
- `technical_plan_entry_used` 的字段清单与 ⭐ `entry_key` ≠ `entrypoint` 的理由（供运维写聚合 SQL）；
- `_aassert_gate_scope` 的四条语义与**中性 404 常量的逐字文案**，以及「为何不 import `_aassert_project_scope`」的登记；
- `confirm/` 两处 409 的 `blocked_reason` **取值表**（`pending_clarification` / `lock["reason"]` 原样）与对应的前端既有用例行号；
- 受限面删除行逐行登记；全量 `pytest tests/ -q` 与基线（8609/1）的差异比对；
- ⭐ **STATE 维护项**（四件事，本 plan 是相位内第一个动 STATE 的，一并做掉）：
  1. 把 Pending Todos 的「115-07 提出 · `blueprint-gate/` 七个端点没有项目范围闸」与「`confirm/` 的 409 未下发 `blocked_reason`」两条划掉并注明结论；
  2. 把 Phase 111 的 **MN-12「权限口径」结案**（115 已定夺「前端不自建权限判断、一律以后端状态码为准；项目成员即全权」，本 plan 补齐 gate 链后蓝图全部读写面 20 + 8 个端点口径统一），并注明 **115-MN-03 的存在性预言机整体改版仍是独立工作项**（本 plan 用更严变体，⛔ 未扩大该暴露面 —— gate 链八个端点的两个失败分支回同一中性 404，`_aassert_project_scope` 的 400 分支一行未动）；
  3. ⭐ **修 STATE 里程碑表的两行陈旧状态**（`.planning/STATE.md:53-54`）：Phase 115 与 116 仍写着 `Not started`，而同文件的 `status` 头与 `progress`（27/27，115 全相位完成）已相反。115 改为 `✅ Complete (7/7)`（与 `:207` 的「Phase 115 已全相位完成（7 / 7）」一致）、116 改为 `🟡 In progress`（本 plan 起）。⛔ 只改这两个单元格，不动表格其余内容；
  4. Phase 116 行的 Requirements 列若与订正后的 ROADMAP 不一致，一并对齐为 `GATE-01, VIEW-05（+ 顺延闭合 VIEW-04、VIEW-02）`。
</output>
