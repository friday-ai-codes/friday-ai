---
phase: 115-ui
plan: 02
type: execute
wave: 2
depends_on: ["115-01"]
files_modified:
  - web/src/types/blueprint.ts
  - web/src/api/blueprints.ts
  - web/src/api/repositoryChunks.ts
  - web/src/api/index.ts
  - web/src/config/blueprintStatus.ts
  - web/src/components/blueprint/BlueprintStatusBadge.vue
  - web/src/components/blueprint/annotationTokens.ts
  - web/src/utils/blueprintBlocks.ts
  - web/src/utils/blueprintAnnotations.ts
  - web/src/stores/useBlueprintViewerStore.ts
  - web/src/composables/useBlueprintLive.ts
  - web/src/composables/useBlueprintAnnotations.ts
  - web/src/composables/useCitationPreview.ts
  - web/src/styles/main.css
  - web/src/locales/zh-CN.json
  - web/src/utils/__tests__/blueprintBlocks.test.ts
  - web/src/utils/__tests__/blueprintAnnotations.test.ts
  - web/src/utils/__tests__/domCapabilities.test.ts
  - web/src/config/__tests__/blueprintStatus.spec.ts
  - web/src/__tests__/blueprint-source-guard.spec.ts
  - web/src/composables/__tests__/useBlueprintLive.spec.ts
autonomous: true
requirements: [VIEW-01, VIEW-02, VIEW-03, VIEW-04, CLAR-01, FLOW-08]
prohibitions:
  - "🔒 **前端 CREATE-ONLY（STATE §13.2 第 4 条）**：本 plan 只允许改**三个**既有文件，且**全部为纯追加**——`web/src/api/index.ts`（barrel 两组导出）、`web/src/styles/main.css`（`@source inline(...)` 追加行）、`web/src/locales/zh-CN.json`（`knowledge.blueprints.*` 子树 + `knowledge.tabs.blueprints`）。三者的 `git diff <file> | rg \"^-[^-]\"` **必须为空**。⛔ 第四处既有文件修改 = 计划不通过"
  - "🔒 **零改动清单（`git diff` 必须为空，⭐ 路径逐一钉死、⛔ 不用通配符 —— plan-checker C5）**：`web/src/components/chat/TechPlanCard.vue`、`web/src/components/chat/RoutingDecisionPanel.vue`、`web/src/components/execution/NodeDataTab.vue`、`web/src/components/delivery/ArtifactTimeline.vue`、`web/src/config/status.ts`、`web/src/components/common/CompactEmptyState.vue`、`web/src/components/layout/AnchorNavLayout.vue`、`web/src/components/prompts/PromptVersionDiff.vue`、`web/src/components/project/warroom/MermaidDiagram.vue`、`web/src/api/client.ts`、`web/src/api/deliveryArtifacts.ts`、`web/src/api/knowledge.ts`"
  - "⛔ **`refetchInterval` 字面量只允许出现在 `web/src/composables/useBlueprintLive.ts`**（UI-SPEC §20 断言 6）：全相位其余任何 `web/src/components/blueprint/**` 与 `web/src/pages/knowledge/blueprints/**` 文件对该 token 零命中。同步点 2 之后换 0.19 的推送契约时，只改这一个文件"
  - "⛔ **禁止非响应式常量间隔**（UI-SPEC §8.3 的 `computed(() => isLive ? 5_000 : false)` 传值写法**由本 plan 订正**）。⭐ **三个查询分两种形态，⛔ 不得统一成一种**（plan-checker BLOCKER-1 定夺）：① **snapshot 查询**用函数式读**它自己的 data**——`refetchInterval: query => (LIVE_BLUEPRINT_STATUSES.has(query.state.data?.current_status ?? '') ? 5_000 : false)`，与仓内全部可用先例同款（`DocsSection.vue:73`、`ReconcilePanel.vue:59`、`FeatureBoard.vue:50,58`、`ProjectHealthCard.vue:79`、`BatchIngestPanel.vue:32`）；② **doc / events 查询**自身响应体里**没有状态字段**，写 `refetchInterval: () => (isLive.value ? 5_000 : false)` **且必须配一条 `watch(isLive, (on) => { if (on) { docQuery.refetch(); eventsQuery.refetch() } })` 作为启动保证**。⚠️ **函数体里读外部 ref 不是被追踪的响应式依赖**（vue-query 的 `cloneDeepUnref` 不下探函数体）：该函数只在**本查询自己的 state 更新**或 options 触发 `setOptions` 时重算。打开一个 `drafting` 蓝图时三查询近乎同时发出，doc/events 先落地而共享 `currentStatus` 还是 `''` ⇒ 算出 `false`、**永不装定时器、也永不再重算** ⇒ 首屏有内容、无报错、快照徽标还在跳，而章节进度冻结在打开那一刻 —— 正是 P-9 静默假通过。有了 `watch` 的首次 `refetch()` 踢动后，此后每次 fetch 结束都会重算间隔，链条自持。⛔ 不得删掉该 `watch`"
  - "⛔ **删掉 UI-SPEC §8.3 的 `useDocumentVisibility()` 那一条**：TanStack Query 内建 `refetchIntervalInBackground: false`（默认即 false），仓内 10 处先例都没写。`rg \"useDocumentVisibility\" web/src/composables/useBlueprintLive.ts` 必须零命中"
  - "⛔ **不用 `composables/usePolling.ts`**：那是 `useIntervalFn` 手动 start/stop 的另一套，与 TanStack Query 无关。CONTEXT 说的「对齐它的惯例」只指**间隔量级**（蓝图阶段级取 5s）"
  - "⛔ **`blockText(block)` 按 `text → code.source → rows` 的字段优先级实现，绝不按 `block.type` 分派**（P-13）：后端 `_block_text`（`server/delivery/services/blueprint_anchor.py:34-64`）完全不看 `block.type`。schema 对 `text` 无任何类型约束 ⇒ 「`type: pseudocode` 且 `text` 非空」的块完全合法；按 type 分派会得到与后端不同的坐标系，而后果是 offset 偏移后**仍在合法范围内** ⇒ 不触发越界降级、不报错、`<mark>` 照渲，**只是圈错了字**——全相位最难逮的一类错"
  - "⛔ **不引入 `@floating-ui/vue`**（本 plan 定夺，A1 假设直接绕开）：选区 popover 一律走 **reka-ui 的 `PopoverAnchor`** + **零尺寸虚拟锚点 div**，导入写 `import { PopoverAnchor } from 'reka-ui'`。⚠️ **⛔ 不要从 `~/components/ui/popover` 导入 `PopoverAnchor`**（plan-checker C1 核实）：该 barrel（`web/src/components/ui/popover/index.ts`）**只导出 `Popover` / `PopoverContent` / `PopoverTrigger` 三个**，给它加一行导出 = 第**七**处既有文件修改，违反 CREATE-ONLY。直接从 `reka-ui` 导入与仓内体例一致——本地 wrapper 自身就是 `import { PopoverRoot, ... } from 'reka-ui'` 的薄壳（`Popover.vue:2`、`PopoverContent.vue:5-9`），`reka-ui: ^2.9.10` 是直接依赖。容器/内容仍用 `~/components/ui/popover` 的 `Popover` / `PopoverContent`。若 `PopoverAnchor` 在该版本不可用，**降级用 `PopoverTrigger` + 零尺寸 div** 并在 SUMMARY 登记。⛔ 「不得临场改选」锁在**方案层面**（不得改用 `@floating-ui/vue`、不得手写 `useFloating`），不绑定到某个导入路径"
  - "⛔ **不封装 `knowledgeApi.getRelated` / `getArtifactAssociations`**（P-5，SC-4 范围收窄）：这两者查的是 `initiatives.Artifact` 投影的 KnowledgeEntity（`server/knowledge/artifact_associations.py:75`），而蓝图在 `delivery.Artifact` ⇒ 拿蓝图 id 去调**必然 404/空**。知识图谱物化明确是 Phase 116。`web/src/api/blueprints.ts` 与本相位任何文件对这两个符号**零调用**"
  - "⛔ **`chunk-at` 的可用判据不是状态码**（P-3）：`repositories/chunk_at_views.py:60` 对「无命中」与「被排除文件」统一返回 **200 `{\"chunks\": []}`**（刻意不可区分）。判据一律 `!ok || chunks.length === 0`，且**必须封装进 `repositoryChunks.ts` 的返回类型**（返 `{ chunks, usable }`），⛔ 不让每个调用点各自判"
  - "⛔ **不据 `ApiError.detail` 是否空串做分档**：`api/client.ts:237,242` 在响应体无 `detail` 键时回落 `'请求失败'`，**不是空串**。UI-SPEC §3.6/§10.1 的「空串」判据是错的，⛔ 不据它写断言"
  - "⛔ **配置里不写中文字面量 label**：`config/blueprintStatus.ts` 存 `labelKey`（i18n key），由组件 `t()`。`config/status.ts` 是内联中文的老面，新页面走 i18n（CONTEXT 明示）"
  - "⛔ **不改 `config/status.ts` 的 `type` 联合**（CREATE-ONLY）：只 `import type { StatusConfig } from '~/config/status'` 复用类型契约"
  - "⛔ **服务端态一律不进 Pinia**：`doc` / `threads` / `snapshot` / `events` / `timeline` / `gate` / `list` 全部走 TanStack Query；store 只放 `sidebarCollapsed` / `showClosedAnnotations` / `kindFilters` 三项客户端偏好"
  - "⛔ **零 `v-html`、零日志正文**：本 plan 的组件（`BlueprintStatusBadge.vue`）全程 mustache；任何 `console.*` 不得打印蓝图正文、批注正文、citation quote"
  - "禁止新增运行时依赖：`mermaid` / `diff` / `codemirror` / `@tanstack/vue-query` / `@vueuse/head` / `@vueuse/core` / `reka-ui` 全部已在 `web/package.json`。`git diff web/package.json web/pnpm-lock.yaml web/pnpm-workspace.yaml` 必须为空"
must_haves:
  truths:
    - "⭐ **环境探针先跑通**：`cd web && pnpm install --frozen-lockfile` 成功（本 worktree 无 `node_modules`），`pnpm exec vitest run` 基线全绿，基线用例数登记进 SUMMARY"
    - "⭐ **happy-dom 能力锁**：新建 `web/src/utils/__tests__/domCapabilities.test.ts` 断言 `document.createTreeWalker` / `document.createRange` / `Range.prototype.getBoundingClientRect` / `window.getSelection` 四项的**存在性**，并把实测结论（支持 / 不支持）写进 SUMMARY——它决定 §B.2 的 offset 函数能否被组件级单测覆盖。⚠️ 无论结论如何，**offset 计算已被设计成两段式**（`collectTextNodes` 触 DOM / `offsetInFlatText` 纯数据）⇒ 纯函数那半**恒可单测**，探针只影响前半段是自动化还是 UAT"
    - "⭐ **`blockText(block)` 与后端 `_block_text` 逐字同源**：四分支优先级 `非空 str text → list text（\"\\n\".join(str(item))）→ 非空 code.source → rows 逐行逐格扁平后 \"\\n\".join → \"\"`，**不看 `block.type`**；落一条「同一 fixture block 前后端取文本一致」断言（后端期望值手抄进 fixture）"
    - "⭐ **前端 `iterBlocks` 与 `blueprint_schema.iter_blocks` 的 13 处 collect 逐段对齐**（走查顺序与 `_item_key` 的「优先取标识字段、缺失回退位置下标字符串化」回退规则同款）；⛔ `must_haves` / `decision_log` / `deferred_ideas` / `execution_plan` **不在走查里**"
    - "⭐ **canonical 序列化自写**：后端指纹是 `json.dumps(block, sort_keys=True, ensure_ascii=False)`，前端 `JSON.stringify` **不保证键序** ⇒ `canonicalBlockFingerprint(block)` 递归排序键；单测断言「同一对象不同键序 → 同一指纹」（否则 diff 会把「内容未变、键序不同」误判 `modified`）"
    - "⭐ **区间切分纯函数可被彻底覆盖**：`sliceBlockText(text, anchors)` 返回 `Array<{ text, threadIds }>`；六类边界各一条用例——越界 / 反序（`start >= end`）/ 重叠（不合并、切成不相交子段、每段携带 threadId 集合）/ 非整数 / 空数组 / 全覆盖"
    - "⭐ **`refetchInterval` 是函数式且只出现在 `useBlueprintLive.ts`**：`LIVE_STATUSES = {researching, drafting, ai_reviewing}`，5s；进入 `pending_review` 与任一终态自动停；源码扫描断言（`blueprint-source-guard.spec.ts`）锁死单文件"
    - "⭐ **非活跃 → 活跃时 doc/events 真的开始轮询**（plan-checker BLOCKER-1）：`useBlueprintLive.ts` 里除函数式 `refetchInterval` 外**必须有 `watch(isLive, ...)` 的 `refetch()` 踢动**；`web/src/composables/__tests__/useBlueprintLive.spec.ts` 用「快照首轮返 `pending_review`、doc/events 落地后快照转 `drafting`」的时序断言 doc/events 的 fetch 次数 **1 → 2**。变异（删掉 `watch`）必须转红"
    - "⭐ **源码扫描守卫 spec 就位**（UI-SPEC §20 断言 4/6/10，前端零先例，用 `node:fs` 遍历）：① `refetchInterval` 在 `web/src/components/blueprint/**` 与 `web/src/pages/knowledge/blueprints/**` 零命中；② `edit-block` / `edit-blocks` 在同两个目录零命中（本相位无 block 编辑面）；③ 404 分支只用 `knowledge.blueprints.error.notFoundOrForbidden` 一个 i18n 键（扫描 `notFound` / `notExist` / `不存在` 等竞品文案）。⚠️ 目录尚不存在时 glob 返回空集合 ⇒ 用例平凡通过，后续 plan 逐个把它压实"
    - "`config/blueprintStatus.ts` 导出 11 态 + `''`（v0 旧数据）共 **12** 个键的配置、`getBlueprintStatusConfig(status)` 带未知态兜底、`EDITABLE_BLUEPRINT_STATUSES`（六值含 `''`）、`isBlueprintEditable(status)`、`PRODUCED_BY_PREFIXES`（四前缀 + 兜底「AI 产出」共五档）；配置存 `labelKey` 不存中文"
    - "`api/blueprints.ts` 覆盖 115-01 的五个端点（响应键与后端**逐字同名**，snake_case 不转驼峰）+ 复用端点的调用（人审快照 / approve / reject / answer / resolve / dismiss / gate 快照与七动作）；`api/repositoryChunks.ts` 覆盖 `chunk-at` 与 `charter` 并返 `{chunks, usable}`"
    - "`api/index.ts` barrel 追加两组导出（形状抄 `:25-27` 的 knowledge 组），追加在**末尾新组**，⛔ 不动既有分组顺序（`@antfu/eslint-config` 的自动重排会误伤）"
    - "`main.css` 追加 **12** 个新 safelist 图标（PATTERNS §B4 交割清单逐字），删除行 0"
    - "`zh-CN.json` 一次性写全 `knowledge.blueprints.*` 子树（§17 的 20 个子命名空间）+ `knowledge.tabs.blueprints` = 「技术方案」；⭐ **该文件全相位只由本 plan 修改**，后续 plan 只消费 key 不再改它"
    - "前端门：`cd web && pnpm exec vitest run` 全绿、`pnpm lint` 通过、`pnpm type-check` 通过"
  artifacts:
    - path: "web/src/types/blueprint.ts"
      provides: "BlueprintV1 / BlueprintBlock / Citation / BlueprintDocumentResponse / BlueprintEventsResponse / BlueprintThreadDetail / BlueprintListItem / BlueprintListResponse / BlueprintStatus 等 TS 类型；字段名与后端逐字同名"
      contains: "current_status"
    - path: "web/src/api/blueprints.ts"
      provides: "五个新端点 + 人审七端点 + 确认门八端点的薄封装；每个响应体一个导出 interface；export default 汇总"
      contains: "blueprint-review/threads"
    - path: "web/src/api/repositoryChunks.ts"
      provides: "chunk-at 与 charter 封装；chunk-at 返 { chunks, usable }，usable = ok && chunks.length > 0（P-3 判据封装在此，调用点不各自判）"
      contains: "usable"
    - path: "web/src/config/blueprintStatus.ts"
      provides: "12 态配置（labelKey/icon 裸名/variant/animate）+ getBlueprintStatusConfig 兜底 + EDITABLE_BLUEPRINT_STATUSES + isBlueprintEditable + PRODUCED_BY_PREFIXES 五档映射"
      contains: "isBlueprintEditable"
    - path: "web/src/components/blueprint/annotationTokens.ts"
      provides: "annotationClass(kind, severity, status, active) + ANNOTATION_PRIORITY；§7.5 四色相 + 状态线型叠加的唯一来源，组件内不得再写颜色字面量"
      contains: "ANNOTATION_PRIORITY"
    - path: "web/src/utils/blueprintBlocks.ts"
      provides: "blockText 四分支（P-13）+ iterBlocks 13 处 collect + _item_key 回退 + canonicalBlockFingerprint 递归排序 + classifyBlockDiff（added/removed/modified）+ producedByReason + sectionKeyForEvent（21 事件映射）"
      contains: "canonicalBlockFingerprint"
    - path: "web/src/utils/blueprintAnnotations.ts"
      provides: "sliceBlockText 区间切分（重叠不合并、切成不相交子段）+ isDegradedAnchor 越界判据 + collectTextNodes(root) 与 offsetInFlatText(nodes, node, offset) 两段式 offset 计算 + groupThreadsByBlock + sidebarGroups 四组判据"
      contains: "sliceBlockText"
    - path: "web/src/composables/useBlueprintLive.ts"
      provides: "全相位唯一轮询消费点：snapshot 走函数式 refetchInterval 读自身 data；doc/events 走 isLive 函数式并配 watch(isLive) 的 refetch 启动踢动（BLOCKER-1）；LIVE_STATUSES 三态；stageTimeline 与 sectionProgress 派生"
      contains: "watch(isLive"
    - path: "web/src/composables/__tests__/useBlueprintLive.spec.ts"
      provides: "轮询启停的时序断言：非活跃 → 活跃时 doc/events 调用次数 1 → 2（删掉 watch 即转红）、终态自动停、恒非活跃不轮询"
      contains: "pending_review"
    - path: "web/src/__tests__/blueprint-source-guard.spec.ts"
      provides: "UI-SPEC §20 断言 4/6/10 的源码扫描（refetchInterval 单文件 / edit-block 零命中 / 404 文案单键），node:fs 遍历，形态平移自后端 test_blueprint_inv6_guard.py"
      contains: "refetchInterval"
  key_links:
    - from: "web/src/utils/blueprintBlocks.ts"
      to: "server/delivery/services/blueprint_anchor.py"
      via: "blockText 四分支优先级与 _block_text 逐字同源（fixture 双向一致性断言，后端期望值手抄）"
      pattern: "blockText"
    - from: "web/src/utils/blueprintBlocks.ts"
      to: "server/services/process_runtime/blueprint_schema.py"
      via: "iterBlocks 的 13 处 collect 与 _item_key 回退，与 iter_blocks:919-1036 逐段对齐"
      pattern: "iterBlocks"
    - from: "web/src/api/blueprints.ts"
      to: "server/delivery/api/blueprint_doc_views.py"
      via: "五个新端点的 URL 与响应键逐字对齐 115-01-SUMMARY 的契约表"
      pattern: "blueprint/events"
    - from: "web/src/api/repositoryChunks.ts"
      to: "server/repositories/chunk_at_views.py"
      via: "usable = ok && chunks.length > 0（200-空 chunks 也算不可用，P-3）"
      pattern: "usable"
    - from: "web/src/composables/useBlueprintLive.ts"
      to: "web/src/api/blueprints.ts"
      via: "三个查询两形态：snapshot 读自身 data，doc/events 读 isLive 并由 watch(isLive) 踢动首次 refetch；同步点 2 后换推送契约只改本文件"
      pattern: "LIVE_STATUSES"
---

<objective>
把前端侧**所有会被多处复用的地基**一次建齐，让后面五个 plan 只写组件、不再发明契约：

① **环境与能力探针**（Wave 0 的实质）：本 worktree 的 `web/` **没有 `node_modules`** —— 先 `pnpm install` 打通，再用一条**能力锁用例**settle RESEARCH 挂着的 A2 假设（happy-dom 对 `TreeWalker` / `Range` 的支持度）。⚠️ 但本 plan 不把结论当赌注：offset 计算被设计成**两段式**，纯数据那半恒可单测。

② **数据契约层**：`types/blueprint.ts` + `api/blueprints.ts` + `api/repositoryChunks.ts`，字段名与 115-01 的后端响应**逐字同名**（含订正后的 `current_status` 与五键分页体）。

③ **纯函数层（本相位唯一能被 vitest 廉价而彻底覆盖的部分）**：`blockText`（P-13 的四分支）/ `iterBlocks`（13 处 collect）/ canonical 指纹 / diff 分类 / 区间切分 / 21 事件 → 段 key / `produced_by_ref` 五档 / `isBlueprintEditable`。组件层受限于 happy-dom 只能做「渲染 / 不渲染」级断言，所以**凡能提成纯函数的一律提出来**。

④ **三处纯追加点一次做完**：i18n 全量子树、safelist 12 个图标、api barrel。⭐ 这三个文件**全相位只由本 plan 改** —— 五个后续 plan 若各改一次，`zh-CN.json` 与 `main.css` 会变成五向冲突面。

⑤ **两条并行纪律的物理落点**：唯一轮询消费点 `useBlueprintLive.ts`（同步点 2 后只改这一个文件）+ 源码扫描守卫 spec（锁死「`refetchInterval` 单文件」「`edit-block` 零命中」「404 文案单键」三条）。

Purpose: 后面五个 plan 是「照契约拼组件」，前提是契约先存在且**已被测试锁死**。P-13 / P-9 / P-3 / P-5 四条会静默假通过的陷阱，全部在本层被固化成可证伪的纯函数与源码扫描。
Output: 13 个新建源文件 + 5 个新建测试文件 + 3 处纯追加。**零新增运行时依赖、零既有组件改动。**
</objective>

<execution_context>
工作目录：worktree 根 `/Users/zaneliu/Projects/open-source/friday-ai/.claude/worktrees/v0.20-blueprint`，分支 `milestone/v0.20.0-blueprint`。前端命令一律 `cd web && ...`。

**wave 2，依赖 115-01**：五个端点的 URL / `name` / 响应键逐字以 `115-01-SUMMARY.md` 的契约表为准（含两处对 UI-SPEC §3.3 的订正：响应键 `current_status`、分页体五键）。⚠️ **不得凭 UI-SPEC 原文写 TS 接口**——它那两处已被 115-01 订正。

**⚠️ 前端环境实测**：`web/node_modules` **不存在**（本 worktree 未 install）。`packageManager` 是 `pnpm@10.34.2`。可用脚本：`pnpm lint` / `pnpm lint:fix` / `pnpm type-check`（`vue-tsc --noEmit`）/ `pnpm test:unit`（即 `vitest`，交互式）。**测试一律用 `pnpm exec vitest run <path>`**（`run` 子命令一次性执行，不进 watch）。

**既有前端设施（实测，不得猜测）**：
- `web/vitest.config.ts`：`environment: 'happy-dom'`（`happy-dom@^20.10.2`），`include: ['src/**/*.{test,spec}.{js,ts}']`，`setupFiles: ['./src/test/setup.ts']`；setup 只有 localStorage/sessionStorage 的 MemoryStorage 垫片（37 行），**无 i18n / router / fetch mock / IntersectionObserver 垫片**。
- `web/src/api/client.ts`：`ApiError` 三字段 `status` / `detail`（**缺省 `'请求失败'`，不是空串**）/ `body`（完整 JSON）；具名导出 `get`（`:` 处）与 `post`（`:267`）。
- `web/src/api/index.ts:25-27` 的 knowledge 组是 barrel 追加的形状范式。
- `web/src/api/deliveryArtifacts.ts`（128 行）是 api 模块的逐字 analog：模块 docstring「对接后端 `<后端文件路径>`」+ 易混淆对象辨析、`import { get } from './client'`、每响应体一个导出 `interface`、查询参数 `const query: Record<string, string> = {}` + 逐个 `if (params.x)`、每函数一行 JSDoc、文件末 `export default { ... }`。
- `web/src/config/status.ts`：`StatusConfig` 接口（`label` / `icon`（**裸名**）/ `variant`（八值联合）/ `animate?`）+ `Record<string, StatusConfig>` + `getStatusConfig` 兜底（`:88-102`）；`config/__tests__/status.spec.ts` 已有先例。
- `web/src/components/common/StatusBadge.vue`（43 行）：`withDefaults(defineProps<...>(), {...})` + 三档 `sizeClass`/`iconSizeClass` 映射对象 + `:class="[\`icon-[${config.icon}]\`, ...]"` + `config.animate → animate-spin`；`components/spec/SddSpecStatusBadge.vue` 补两件：`data-testid` 与 `t(\`specs.status.${props.status}\`)` 的 i18n label 取法。
- `web/src/stores/analyticsFilters.ts`（25 行）：全仓最小 setup store，docstring 三段式（职责 / 设计 / **是否持久化及其理由**）。
- `web/src/components/knowledge/artifactDisplay.ts` 的 `ARTIFACT_BADGE_CLASS`：模块级 `Record` 常量 + 查表函数——`annotationTokens.ts` 的形状 analog。
- `web/src/utils/variableRef.ts`（65 行）+ `web/src/utils/__tests__/variableRef.test.ts`：契约型纯函数模块与其单测的唯一先例。
- `web/src/pages/knowledge/entities/[id].vue:93` 的 `is404`：`err instanceof ApiError && err.status === 404`（仓内唯一先例）。
- TanStack Query 惯例：`queryKey: computed(() => [...])` 取响应式；`staleTime: 30_000` 页面级默认；invalidate 走**前缀匹配** `invalidateQueries({ queryKey: ['blueprint'] })`（UI-SPEC §3.7 的 `predicate` 写法仓内零先例，本页只有一个 artifact ⇒ 全域失效无副作用，**取简单写法**）；`refetchInterval` 10 处先例**全部函数式**（如 `DocsSection.vue:73` `query => (query.state.data?.sync_status === 'syncing' ? 2000 : false)`）。

**后端同源口径（写纯函数前必须先读实现，⛔ 不得凭 UI-SPEC 转述）**：
- `server/delivery/services/blueprint_anchor.py:34-64` `_block_text` 的**四分支优先级**（注意路径是 `delivery/services/` 不是 `process_runtime/`）。
- `server/services/process_runtime/blueprint_schema.py:906-912` `_item_key`、`:919-1036` `iter_blocks` 的 **13 处 `collect`**、`:1039-1060` `_block_fingerprint` 与 `diff_blueprint_blocks`。
- `server/delivery/services/blueprint_lifecycle_service.py:92` `EDITABLE_BLUEPRINT_STATUSES` 的**确切成员**（含 `""`）。
- `server/delivery/services/event_taxonomy.py:185-208` 的 21 个 `BLUEPRINT_EVENTS` 常量名。
</execution_context>

<context>
@.planning/phases/115-ui/115-01-SUMMARY.md（**五端点契约表为权威**：URL / name / query / 响应键逐字 / 状态码映射；两处 UI-SPEC 订正；`quality` 四键取值语义；`_thread_detail_row` 完整键集）
@.planning/phases/115-ui/115-RESEARCH.md（§B.1 切分算法与坐标系同源、§B.2 选区 offset 与 `@floating-ui/vue` 的 `[ASSUMED]` 状态、§B.4 前端 `iter_blocks` 的 13 处 collect 完整清单与 canonical 序列化问题、§B.5 TanStack Query 三惯例与 `is404` 先例、§B.6 `chunk-at` 的六种响应形状表、§B.10 前端测试设施与「测试预算四层分配」；P-3 / P-5 / P-9 / P-13 / P-14）
@.planning/phases/115-ui/115-PATTERNS.md（§B1/B2 api 模块的结构要点与三条 DIFFER、§B4 11 态配置的四条 DIFFER 与 ⭐ **safelist 交割清单（12 个图标逐字）** 与「两种 icon 契约」的区分、§B5/B6 纯函数的三条硬约束、§B7 store 的 DIFFER（持久化理由要写进 docstring）、§B8 唯一轮询消费点的四条 DIFFER、§E 的 `annotationTokens` 形状与 emits 风格二选一、§H 测试三层分配与 No Analog #6/#7）
@.planning/phases/115-ui/115-UI-SPEC.md §3.7（queryKey 契约表 8 行）、§6.2（Block 的 props/emits 契约与五类块分发）、§7.1（切分四步算法）、§7.5（**批注四色相 + 状态线型叠加的完整令牌表，含 WCAG 实算值——`annotationTokens.ts` 逐字实现它**）、§7.7（侧栏四组判据）、§7.9（`EDITABLE_BLUEPRINT_STATUSES` 逐字）、§8.1（**21 事件 → 段 key 映射表逐行**）、§9.1（`produced_by_ref` 四前缀映射表）、§13.8/§13.9（数据层文件清单与 12 态配置表逐字）、§17（i18n 的 20 个子命名空间）、§20（11 条契约断言）
@web/src/api/deliveryArtifacts.ts（api 模块逐字 analog，128 行全文）
@web/src/config/status.ts（`StatusConfig` 接口与 `getStatusConfig` 兜底，103 行全文）
@web/src/components/common/StatusBadge.vue + @web/src/components/spec/SddSpecStatusBadge.vue（徽标组件两个 analog 全文）
@web/src/utils/variableRef.ts + @web/src/utils/__tests__/variableRef.test.ts（契约型纯函数模块与其单测的唯一先例）
@web/src/stores/analyticsFilters.ts（最小 setup store 全文 25 行）
@web/src/pages/knowledge/entities/[id].vue L20-100（类型化 route / `useHead` / 三个 query 的 `computed` queryKey / `is404`）
@web/src/styles/main.css L1-25（`@source inline(...)` 现有 safelist 的**确切行体例**）
@web/src/locales/zh-CN.json L227-233（`knowledge.tabs` 现有五键，追加 `blueprints`）
</context>

<tasks>

<task type="auto">
  <name>Task 1: 环境打通 + happy-dom 能力锁探针 + 三处纯追加点（i18n 全量子树 / safelist 12 图标 / api barrel）+ types/blueprint.ts</name>
  <files>web/src/utils/__tests__/domCapabilities.test.ts, web/src/locales/zh-CN.json, web/src/styles/main.css, web/src/api/index.ts, web/src/types/blueprint.ts</files>
  <read_first>web/package.json（脚本清单与 `packageManager`）；web/vitest.config.ts 与 web/src/test/setup.ts **全文**（确认 setup 只有 storage 垫片、无 IntersectionObserver / fetch mock —— 后续 plan 的组件测试要据此自带 stub）；web/src/styles/main.css L1-25（`@source inline(...)` 的**确切行体例**：一行一组、注释在上方说明该组的动态拼接来源）；web/src/locales/zh-CN.json L1-10 与 L220-260（顶层节结构与 `knowledge` 节的缩进/排序体例；`knowledge.tabs` 现有五键**含一个 `release` 但 `TABS` 数组里没有**——历史残留，⛔ 不动它）；web/src/api/index.ts **全文**（追加组的位置与 `export { default as xxxApi } from './x'` + `export * from './x'` 两行成组的体例；⚠️ `@antfu/eslint-config` 的自动重排会误伤既有导出顺序 ⇒ **新组追加在文件末尾，既有分组一行不动**）；web/src/types/prompts.ts（types 模块的 analog：每个 interface 一段 JSDoc、字段与后端逐字同名）；.planning/phases/115-ui/115-01-SUMMARY.md 的五端点契约表（**TS 接口逐字照它**）</read_first>
  <action>
**① 环境打通（第一步，失败即停并如实报告）**：`cd web && pnpm install --frozen-lockfile` → `pnpm exec vitest run` 取基线（登记 passed/failed 数进 SUMMARY）→ `pnpm lint` 与 `pnpm type-check` 各取一次基线。⚠️ 若 `pnpm install` 因网络/registry 失败，**不得**改 lockfile 或降级安装方式（`--no-frozen-lockfile` 会漂移 lock）；如实记录为执行阻塞。

**② happy-dom 能力锁 `web/src/utils/__tests__/domCapabilities.test.ts`**（settle A2）。它是**永久回归锁**不是一次性探针：断言四项能力的存在性与最小行为——
- `typeof document.createTreeWalker === 'function'`，且对 `<div>abc<span>def</span>ghi</div>` 用 `NodeFilter.SHOW_TEXT` 遍历累加得到 `'abcdefghi'`；
- `typeof document.createRange === 'function'`，`range.selectNodeContents(el)` 后 `range.toString()` 等于 `el.textContent`；
- `typeof Range.prototype.getBoundingClientRect === 'function'`（⚠️ **只断存在性不断数值** —— happy-dom 无真实布局，返回值恒为 0 矩形，那是已知且可接受的）；
- `typeof window.getSelection === 'function'`。
文件顶部 docstring 写明：「本用例的作用是**锁住测试环境能力**，不是测业务。任一项转红 = happy-dom 升级后丢了能力 ⇒ `utils/blueprintAnnotations.ts` 的 `collectTextNodes` 单测要改走注入 fixture 的路径」。
⭐ **若某项实测不支持**（plan-checker C4 定夺，覆盖原「改 `expect.soft`」的说法）：把断言改成**记录实测值**的形态——构造一个能力布尔映射 `{ createTreeWalker: …, createRange: …, rangeGetBoundingClientRect: …, getSelection: … }` 并用 `expect(caps).toMatchInlineSnapshot()` 落盘。这样这把锁**反映现实而非期望**，本 plan 的全绿门不会被环境事实卡住，而 happy-dom 升级后任一值变化仍会转红。同时在文件内与 SUMMARY 双处登记「XX 不支持 ⇒ `collectTextNodes` 归 UAT」。⛔ **不删用例**；⛔ **不用 `expect.soft`**（它只是失败后继续跑，用例照样红，与本 plan 的全绿门直接冲突）。

**③ `web/src/locales/zh-CN.json` 一次写全**（追加点 #5，删除行 0，⭐ **全相位只此一次**）：
- `knowledge.tabs` 追加一键：`"blueprints": "技术方案"`；
- `knowledge` 节内追加 `blueprints` 子树，按 UI-SPEC §17 的 **20 个子命名空间**逐个建齐：`pageTitle` / `pageDescription` / `tabPanel.*` / `status.*`（12 键，与 `config/blueprintStatus.ts` 的 `labelKey` 一一对应）/ `section.*`（**10 段标题**）/ `mustHaves.*` / `progress.*`（**21 事件文案 + 3 条状态级回落文案**）/ `block.*`（⛔ **无「编辑」键**——本相位无该动作）/ `repo.*` / `api.*` / `impact.*` / `flow.*` / `annotation.*` / `thread.*` / `finding.*` / `version.*` / `diff.*` / `review.*` / `gate.*` / `quality.*` / `citation.*` / `error.*` / `readonly.notice`。
- ⭐ **文案逐字取 UI-SPEC §16 的 Copywriting Contract**（含四条破坏性确认全文、404 单一文案、指标无数据文案、跨块选区提示、越界降级说明、失锚条目说明、引用兜底、历史版本提示）。§16 未覆盖的键按同一语气自拟。
- ⭐ **`error.notFoundOrForbidden` 是 404 的唯一键**，值逐字「无权访问或该蓝图不存在」。⛔ **不得**再建 `error.notFound` / `error.forbidden` 之类的第二个键——那正是 §20 断言 4 与本 plan 源码守卫要逮的形状。
- `progress.*` 的 21 条按 §8.1 映射表逐行；⚠️ 插值键（`question_count` / `repository_name` / `fitness_verdict` / `candidate_count` / `round` / `seq` / `to_key` / `satisfied_count` / `attempt` / `decision_log_count`）由各 emit 点自行写入、**schema 层零保证**（P-8）⇒ 每条带插值的文案**必须另配一条无插值的兜底键**（如 `progress.repoResearchStarted` 与 `progress.repoResearchStartedGeneric`），消费方在键缺失时用后者。⛔ 不要指望 vue-i18n 对缺失参数报错（它会渲染成 `正在调研 undefined…`）。
- ⛔ **不新增顶层节**、⛔ 不修改 `knowledge` 下既有任何键。

**④ `web/src/styles/main.css` safelist 追加**（追加点 #4，删除行 0）：在现有 `@source inline(...)` 行**之后**追加两行（上方一行中文注释说明来源「蓝图查看器：11 态徽标 / change_type / citation source_type / produced_by_ref 前缀，全部运行期拼接」），内容为 PATTERNS §B4 交割清单的 **12 个新图标**：`icon-[lucide--pen-line]` `icon-[lucide--hammer]` `icon-[lucide--check-check]` `icon-[lucide--archive]` `icon-[lucide--file-plus]` `icon-[lucide--file-pen-line]` `icon-[lucide--file-cog]` `icon-[lucide--book-open]` `icon-[lucide--scroll-text]` `icon-[lucide--list-checks]` `icon-[lucide--user-pen]` `icon-[lucide--refresh-cw]`。⭐ **safelist 只服务「运行期拼接的裸名」**（plan-checker N3 定夺，覆盖原「已在既有 safelist 中」的说法）：其余用到的图标分两类，**都不需要本 plan 追加**——
- 一类**确已在既有 safelist**（`main.css:10-25` 逐字可查）：`scan-eye` / `shield-check` / `help-circle` / `user-check` / `check-circle` / `x-circle` / `loader-2` / `file-x` / `file-text` / `lock` / `undo-2` / `sparkles` / `shield` / `external-link` / `file-code` / `workflow` / `link` / `layers`；
- 一类是**写在 `.vue` 源码里的字面量完整类名**（如 `AnchorNavLayout` 的 `NavSection.icon` 收 `icon-[lucide--target]`、115-03 的 `icon-[lucide--message-square-dot]`、115-05 的 `icon-[lucide--mouse-pointer-click]`）：⭐ **字面量类名不需要 safelist** —— Tailwind 的 content 扫描直接命中源码。`lucide--target` 确实**不在** `main.css` 的 safelist 里，这**不是遗漏**。
⛔ 因此后续 plan 若发现某个图标不在 safelist，**先判断它是不是字面量**：是字面量就什么都不用做，⛔ 不要据此回报「115-02 漏了 safelist」。

⚠️ **两种 icon 契约不要统一**（抄错就出不来图标）：`CompactEmptyState`（`:17`）与 `StatusBadge`（`:38`）内部做 `` :class="`icon-[${icon}]`" `` ⇒ 收**裸名**（`lucide--lock`）；而 `AnchorNavLayout` 的 `NavSection.icon`（`:91` `:class="[section.icon, …]"`）收**完整类名**（`icon-[lucide--target]`）。UI-SPEC §6.1 的导航图标写全名、§13.9 的状态图标写裸名——**两者都对**。

**⑤ `web/src/api/index.ts` barrel 追加**（追加点 #3，删除行 0）：文件**末尾**新起一组，两行成对：`export { default as blueprintsApi } from './blueprints'` + `export * from './blueprints'`；`export { default as repositoryChunksApi } from './repositoryChunks'` + `export * from './repositoryChunks'`。⛔ 既有分组一行不动（`pnpm lint:fix` 若试图重排既有行，改为手动 `eslint --fix` 仅针对本文件并核对 diff 只有追加）。

**⑥ 新建 `web/src/types/blueprint.ts`**，按 115-01-SUMMARY 的契约表逐字写接口（字段名 snake_case 与后端同名，⛔ 不转驼峰）：
- `BlueprintStatus`（12 值字符串联合，含 `''`）、`BlueprintBlock`（`{ block_id, type: 'paragraph'|'pseudocode'|'table'|'list'|'mermaid', text?: unknown, code?: { language?: string, source?: string }, rows?: unknown[][], citations?: string[] }`——⚠️ `text` 的类型是 `unknown`，因为 schema 对它**无任何约束**（paragraph/mermaid 是 string、list 是 string[]，且「pseudocode 带非空 text」也合法，P-13）；
- `Citation`（`citation_id` / `source_type`（**9 值枚举**）/ `source_id?` / `locator?: Record<string, unknown>` / `quote?` / `title?`）；
- `BlueprintV1`（14 个顶层键，`must_haves` / `citations` 必填，`decision_log` / `deferred_ideas` / `execution_plan` 可选且为**零约束裸 array**（P-14）⇒ 类型写 `unknown[]`）；
- `BlueprintDocumentResponse`（七键，`quality` 四键，后三项 `number | null`）；
- `BlueprintEventsResponse`；`BlueprintThreadDetail`（九键 + `options` / `last_reminded_at` / `messages[]`）；
- `BlueprintListItem`（⭐ 键是 **`current_status`**）与 `BlueprintListResponse`（⭐ 五键 `{ total, items, page, page_size, has_next }`）；
- 人审快照 / gate 快照的响应类型（键逐字照 115-01-SUMMARY 与 114-05-SUMMARY）。
每个 interface 一段 JSDoc；对「半可信」字段（`options` / `payload` / `locator` / `decision_log`）在 JSDoc 里逐条写明「运行期形状无 schema 保证，消费方必须逐项可选链」。
  </action>
  <verify>
    <automated>cd web && pnpm exec vitest run src/utils/__tests__/domCapabilities.test.ts && pnpm type-check && node -e "const j=require('./src/locales/zh-CN.json');const b=j.knowledge.blueprints;const need=['pageTitle','tabPanel','status','section','mustHaves','progress','block','repo','api','impact','flow','annotation','thread','finding','version','diff','review','gate','quality','citation','error','readonly'];const miss=need.filter(k=>!(k in b));if(miss.length)throw new Error('missing i18n groups: '+miss);if(Object.keys(b.status).length!==12)throw new Error('status keys != 12: '+Object.keys(b.status).length);if(Object.keys(b.section).length!==10)throw new Error('section keys != 10');if(!j.knowledge.tabs.blueprints)throw new Error('missing knowledge.tabs.blueprints');console.log('i18n subtree OK')"</automated>
  </verify>
  <acceptance_criteria>
- `cd web && pnpm install --frozen-lockfile` 成功；`pnpm exec vitest run` 基线全绿（基线用例数登记进 SUMMARY）；`pnpm lint` 与 `pnpm type-check` 基线通过。
- ⭐ 能力锁：`pnpm exec vitest run src/utils/__tests__/domCapabilities.test.ts` 通过；**四项能力的实测结论（支持 / 不支持）逐条写进 SUMMARY**（这是 A2 假设的正式 settle 记录）。
- ⭐ i18n：上述 `node -e` 校验输出 `i18n subtree OK`；`node -e "const j=require('./src/locales/zh-CN.json');const e=j.knowledge.blueprints.error;if(e.notFoundOrForbidden!=='无权访问或该蓝图不存在')throw new Error('404 文案不逐字');const bad=Object.keys(e).filter(k=>/notFound$|notExist|forbidden/i.test(k));if(bad.length)throw new Error('404 竞品键: '+bad);console.log('404 single key OK')"` 输出 `404 single key OK`。
- ⭐ i18n progress 兜底：`node -e "const p=require('./src/locales/zh-CN.json').knowledge.blueprints.progress;const withVar=Object.entries(p).filter(([k,v])=>typeof v==='string'&&/\{\w+\}/.test(v));const missing=withVar.filter(([k])=>!(k+'Generic' in p));if(missing.length)throw new Error('带插值文案缺无参兜底: '+missing.map(x=>x[0]));console.log('progress fallback OK')"` 输出 `progress fallback OK`。
- ⭐ 三处追加点纯追加：`git diff web/src/locales/zh-CN.json web/src/styles/main.css web/src/api/index.ts | rg "^-[^-]"` **输出为空**。
- safelist：`rg -c "icon-\[lucide--pen-line\]|icon-\[lucide--hammer\]|icon-\[lucide--check-check\]|icon-\[lucide--archive\]|icon-\[lucide--file-plus\]|icon-\[lucide--file-pen-line\]|icon-\[lucide--file-cog\]|icon-\[lucide--book-open\]|icon-\[lucide--scroll-text\]|icon-\[lucide--list-checks\]|icon-\[lucide--user-pen\]|icon-\[lucide--refresh-cw\]" web/src/styles/main.css` ≥ **1**，且逐个图标 `rg -c` 命中（12 个全在）。
- barrel：`rg -n "blueprintsApi|repositoryChunksApi" web/src/api/index.ts` 两条命中。
- types：`rg -n "current_status" web/src/types/blueprint.ts` 命中；`rg -n "blueprint_status" web/src/types/blueprint.ts` **在 `BlueprintListItem` 内零命中**（列表项的状态键是 `current_status`，115-01 订正）；`rg -n "has_next" web/src/types/blueprint.ts` 命中。
- ⛔ 零依赖变更：`git diff web/package.json web/pnpm-lock.yaml web/pnpm-workspace.yaml` 输出**为空**。
- ⛔ 零既有组件改动：`git diff --name-only web/src | rg -v "^web/src/(types|api|config|utils|stores|composables|components/blueprint|__tests__|locales|styles)/"` 输出**为空**（`api/index.ts`、`locales/zh-CN.json`、`styles/main.css` 是允许的追加点）。
  </acceptance_criteria>
  <done>前端环境可跑；A2 假设被一条永久能力锁用例 settle 并登记；三处纯追加点一次做完且删除行为 0（后续五个 plan 不再碰这三个文件）；`types/blueprint.ts` 与 115-01 的响应键逐字对齐，含两处 UI-SPEC 订正。</done>
</task>

<task type="auto">
  <name>Task 2: api/blueprints.ts + api/repositoryChunks.ts + config/blueprintStatus.ts + BlueprintStatusBadge.vue + annotationTokens.ts + 配置单测</name>
  <files>web/src/api/blueprints.ts, web/src/api/repositoryChunks.ts, web/src/config/blueprintStatus.ts, web/src/components/blueprint/BlueprintStatusBadge.vue, web/src/components/blueprint/annotationTokens.ts, web/src/config/__tests__/blueprintStatus.spec.ts</files>
  <read_first>web/src/api/deliveryArtifacts.ts **全文 128 行**（模块 docstring 的「对接后端 `<路径>`」+ 易混淆对象辨析、`import { get } from './client'`、interface 逐字段 JSDoc、query 拼装的 `if (params.x)` 体例、文件末 `export default`）；web/src/api/client.ts L1-60 与 L220-280（`ApiError` 三字段与 `detail` 的**缺省 `'请求失败'`**、`get` / `post` 的具名导出与签名）；web/src/config/status.ts **全文 103 行**（`StatusConfig` 接口 L1-6、`Record<string, StatusConfig>` 常量体例、`getStatusConfig` 的兜底返回 L88-102）；web/src/config/__tests__/status.spec.ts **全文**（配置单测的形状 analog）；web/src/components/common/StatusBadge.vue **全文 43 行** + web/src/components/spec/SddSpecStatusBadge.vue **全文 36 行**（`withDefaults` / 三档 size 映射 / `icon-[${config.icon}]` / `animate-spin` / `data-testid` / i18n label 取法）；web/src/components/knowledge/artifactDisplay.ts（`ARTIFACT_BADGE_CLASS` 的 `Record` 常量 + 查表函数形状——`annotationTokens.ts` 照它）；web/src/components/ui/badge/index.ts（八个 variant 的**确切名字**：`default/secondary/destructive/outline/success/warning/info/muted`）；server/repositories/chunk_at_views.py **全文 60 行**（六种响应形状：404 / 三种 400（错误体键是 **`error`** 不是 `detail`）/ **200 空 chunks** / 200 命中；`chunks[]` 每项 `{chunk_id, file_path, line_start, line_end, chunk_index}`，**无代码正文**）；.planning/phases/115-ui/115-01-SUMMARY.md 的五端点契约表；.planning/phases/114-ai/114-05-SUMMARY.md 的七端点契约表（人审动作端点的入参与状态码）；server/delivery/api/blueprint_gate_views.py L1-30（确认门八端点的 URL 与入参，`api/blueprints.ts` 一并封装）</read_first>
  <action>
**① 新建 `web/src/api/blueprints.ts`**（照 `deliveryArtifacts.ts` 逐字体例）。

模块 docstring 三段：(a)「对接后端 `server/delivery/api/blueprint_doc_views.py` / `blueprint_list_views.py` / `blueprint_review_views.py` / `blueprint_gate_views.py`」；(b) ⭐ **易混淆对象辨析**（analog 的固定动作）：「`~/api/deliveryArtifacts` 是通用 artifact 面（含旧 `technical_plan`），本模块只覆盖 `blueprint_status != ''` 的蓝图；`~/api/artifacts` 是 initiatives 项目工件，勿混用」；(c) ⛔ **本模块不封装 `knowledgeApi.getRelated` / `getArtifactAssociations`** —— 它们查 `initiatives.Artifact` 投影的 KnowledgeEntity，拿蓝图的 `delivery.Artifact.id` 去调**必然 404/空**（P-5）；SC-4 的「引用了本蓝图 / 关联知识」本相位**范围收窄**，顺延 Phase 116 的知识图谱物化。

导出函数（每个一行 JSDoc，响应类型从 `~/types/blueprint` import）：
- `getBlueprintDocument(artifactId, params?: { version_id?: string })`
- `getBlueprintEvents(artifactId)`
- `getBlueprintThreads(artifactId)`
- `createBlueprintComment(artifactId, payload: { body: string, anchor?: {...} })`（`post`）
- `listBlueprints(params: { project_id?, blueprint_status?, repository_id?, q?, page?, page_size? })` —— ⚠️ **query 参数名保留字面 `blueprint_status`**（那是后端的 query 参数名，前端侧不受 INV-6 守卫约束），但**响应项的状态键是 `current_status`**；
- 人审面：`getBlueprintReviewSnapshot` / `approveBlueprint` / `rejectBlueprint` / `answerThread` / `resolveFinding` / `dismissFinding`（⛔ **不封装 `edit-blocks/`**，本相位零调用，§0.1 硬边界第 3 条）；
- 确认门：`getBlueprintGate` + 七动作（`confirmGate` / `removeRepo` / `addRepo` / `reclassifyRole` / `editResponsibility` / `rejectedToBoundary` / `upgradeResearch`）。
query 拼装照 analog：`const query: Record<string, string> = {}` + 逐个 `if (params.x)`，空值不进 query。文件末 `export default { ...全部函数 }`。
⛔ **不 re-export `getArtifactTimeline`**：版本轨直接用既有 `deliveryArtifacts.getArtifactTimeline`（本 plan 定夺，避免同一端点两个入口）。

**② 新建 `web/src/api/repositoryChunks.ts`**（`chunk-at` 与 `charter` 的前端封装，仓内尚无）。

⭐ **P-3 的判据封装在此，调用点不各自判**：
- `getChunkAt(repositoryId, params: { path: string, line: number }): Promise<{ chunks: RepoChunkRef[], usable: boolean }>` —— `usable = chunks.length > 0`；任何异常（含 400/404/5xx）一律 `catch` 成 `{ chunks: [], usable: false }`，**不上抛**。docstring 逐字写明：「后端对『无命中』与『被排除文件』**统一返回 200 `{\"chunks\": []}`**（`chunk_at_views.py:60`，刻意不可区分的存在性防线）⇒ 判据必须是 `!ok || chunks.length === 0`，**不能是「非 2xx」**；且它的错误体键是 `error` 不是 `detail`，`ApiError.detail` 会回落成无意义的 `'请求失败'`（`client.ts:237,242`）⇒ ⛔ 任何调用点都不得回显它」。
- ⭐ **代码正文来源的定夺（本 plan 拍板，UI-SPEC §3.6/§10.1 订正）**：`chunks[]` 只有 `{chunk_id, file_path, line_start, line_end, chunk_index}`，**没有代码正文**；全仓也**没有**「按 path+行区间取源码」的读面（`repository-code-search` 是需要 query 的向量检索，不可用；`components/execution/JsonViewer.vue:4-7` 自承是「替代 CodeMirror 的只读展示」，仓内根本没有只读 CodeMirror 封装）。⇒ **`CitationCodePreview` 本相位降级为「文件路径 + 行号区间 + citation 的 `quote` 快照」**，用与 `pseudocode` 块同一套 `<pre class="font-mono">` + 行号渲染，⛔ 不引入 CodeMirror、⛔ 不新增后端端点（超出本相位「只加读面」的边界且该读面归属 Phase 116）。这段理由写进本文件 docstring，并在 SUMMARY 登记为对 UI-SPEC 的第三处订正。
- `getRepositoryCharter(repositoryId)`：同款容错（失败返 `null`，调用方走快照兜底）。

**③ 新建 `web/src/config/blueprintStatus.ts`**：
- `import type { StatusConfig } from '~/config/status'`（⛔ 只 import 类型，不改那个文件）；本地 `export interface BlueprintStatusConfig extends Omit<StatusConfig, 'label'> { labelKey: string }`（⭐ 用 `labelKey` 而非 `label`：新页面走 i18n，⛔ 配置里不写中文字面量）。
- `BLUEPRINT_STATUS_CONFIG: Record<string, BlueprintStatusConfig>` —— UI-SPEC §13.9 的 **12 行逐字**（含 `''` → `labelKey: 'knowledge.blueprints.status.legacy'` / `lucide--file-text` / `outline`）；`icon` 存**裸名**（与 `status.ts` 全域一致）。
- `getBlueprintStatusConfig(status: string): BlueprintStatusConfig` —— 兜底返回 `{ labelKey: 'knowledge.blueprints.status.unknown', icon: 'lucide--help-circle', variant: 'muted' }`；⚠️ **`''` 必须命中 `''` 那一档而不是走 unknown 分支**（v0 旧数据是合法输入）。
- `EDITABLE_BLUEPRINT_STATUSES = new Set(['', 'researching', 'drafting', 'ai_reviewing', 'needs_clarification', 'pending_review'])` 与 `isBlueprintEditable(status: string): boolean` —— 成员逐字对齐后端 `blueprint_lifecycle_service.py:92`（⚠️ 后端函数入参是 **artifact**，前端是 **status 字符串**，这是刻意的差异，docstring 写明）。
- `LIVE_BLUEPRINT_STATUSES = new Set(['researching', 'drafting', 'ai_reviewing'])`（供 `useBlueprintLive` 消费，⛔ 不在 composable 里另写一份）。
- `PRODUCED_BY_PREFIXES` 五档映射（UI-SPEC §9.1 逐字）+ `producedByReason(ref: string)` 查表函数：`human_edit:` → 人工编辑 / `ai_review_reflow:` → 澄清回灌 / `human_block_restore:` → 人工块保护 / `blueprint_review_reject:` → 人审驳回 / 其余 → AI 产出。**labelKey 同样走 i18n**。
- 每组配置上方一段中文注释说明「与后端哪个枚举/常量同步」。

**④ 新建 `web/src/components/blueprint/BlueprintStatusBadge.vue`**（照 `StatusBadge.vue` + `SddSpecStatusBadge.vue`）：`props: { status: string, size?: 'sm'|'md'|'lg', showIcon?: boolean, showLabel?: boolean }`（`withDefaults`）；`<Badge :variant="config.variant">` 承载**全部**颜色，⛔ **禁止在 Badge 上用 `:class` 追加颜色类**（UI-SPEC §15）；图标 `` :class="[`icon-[${config.icon}]`, iconSizeClass, config.animate ? 'animate-spin' : '']" ``；label 走 `t(config.labelKey)`；`data-testid="blueprint-status-badge"`。

**⑤ 新建 `web/src/components/blueprint/annotationTokens.ts`**（§7.5 令牌的**唯一来源**，形状照 `artifactDisplay.ts` 的 `Record` 常量 + 查表函数）：
- `ANNOTATION_PRIORITY`：`blocker > warning > human_comment > info/ai_clarification`；同级时 `open > answered > resolved/dismissed`。导出一个 `pickTopThread(threads)` 或等价的比较函数供切分层取「优先级最高的一条」着色。
- `annotationClass(kind, severity, status, active): string` —— 返回 Tailwind/CSS 类名串。⭐ **四色相 + 一档灰逐字照 §7.5 的实算值**：`ai_review_finding·blocker` 描边 `hsl(0 72% 45%)`（5.83:1）/ 底纹 `hsl(0 72% 51% / 0.12)`；`·warning` 描边 `hsl(26 90% 37%)`（5.02:1）/ 底纹 `hsl(38 92% 50% / 0.12)`；`·info` 描边 `hsl(215 16% 40%)`（6.08:1）/ 底纹 `hsl(215 16% 47% / 0.10)`；`ai_clarification` 与 `repo_confirmation` 描边 `hsl(167 76% 32%)`（`--color-primary-600`，3.74:1）/ 底纹 `hsl(168 76% 42% / 0.12)`；`human_comment` 描边 `hsl(263 70% 50%)`（7.24:1）/ 底纹 `hsl(263 70% 50% / 0.10)`。⛔ **不用 `--color-primary` 原值 `hsl(168 76% 42%)` 作描边**（白底 2.49:1，不过 WCAG 1.4.11 的 3:1）；⛔ amber `hsl(38 92% 50%)`（2.14:1）只能作底纹。
- **状态叠加**（正交于色相）：`open` 2px solid + 满档底纹；`answered` 2px dashed + 底纹 ×0.6；`resolved`/`dismissed` 1px dotted + 色改 `hsl(215 16% 47%)` + 无底纹；`active` 描边加粗 3px + `outline: 2px solid var(--color-primary-600); outline-offset: 1px`（**不透明**，⛔ 不用半透明的 `hsl(168 76% 42% / 0.5)` —— 既有焦点环实算只有 1.59:1，本相位新增面不得复制它）+ 底纹 ×1.4 封顶 0.20。
- ⭐ **`<mark>` 的浏览器默认黄底必须被重置**（`background: transparent` 起手）—— 该重置规则也由本模块提供（一个 `MARK_BASE_CLASS` 常量或等价物）。
- ⛔ **组件内不得再写任何颜色字面量**（UI-SPEC §15）。

**⑥ 新建 `web/src/config/__tests__/blueprintStatus.spec.ts`**（照 `status.spec.ts`）：12 态各一断言（`labelKey` / `icon` 裸名 / `variant` / `animate`）；未知态兜底命中 unknown；⭐ **`''` 命中 legacy 档而非 unknown**（正反并列）；`isBlueprintEditable` 白名单**内外各三条**（含 `''` 为 true、`confirmed` / `implementing` / `archived` 为 false）；`producedByReason` 五档各一条（含兜底）；⭐ **`labelKey` 全部能在 `zh-CN.json` 里查到**（import JSON 逐个 `expect(get(messages, cfg.labelKey)).toBeTruthy()` —— 这条同时把 Task 1 的 i18n 子树与本配置对齐锁死）。
  </action>
  <verify>
    <automated>cd web && pnpm exec vitest run src/config/__tests__/blueprintStatus.spec.ts && pnpm type-check</automated>
  </verify>
  <acceptance_criteria>
- `cd web && pnpm exec vitest run src/config/__tests__/blueprintStatus.spec.ts` 全绿；`pnpm type-check` 通过。
- ⭐ **P-5 范围收窄**：`rg -n "getArtifactAssociations|getRelated" web/src/api/blueprints.ts web/src/components/blueprint/` **零命中**。
- ⭐ **P-3 判据封装**：`rg -n "usable" web/src/api/repositoryChunks.ts` 命中；`cd web && node -e "const s=require('fs').readFileSync('src/api/repositoryChunks.ts','utf8'); if(!/chunks\.length === 0|chunks\.length > 0/.test(s)) throw new Error('缺 200-空 chunks 的判据'); if(/status === 404|!res\.ok\s*\?/.test(s) && !/chunks\.length/.test(s)) throw new Error('判据仍是状态码'); console.log('chunk-at predicate OK')"` 输出 `chunk-at predicate OK`。
- ⭐ **代码正文来源已定夺**：`rg -n "CodeMirror|codemirror" web/src/api/repositoryChunks.ts` **零命中**，且该文件 docstring 含「quote 快照」与「Phase 116」两处字样（源码扫描 `rg -n "quote|116" web/src/api/repositoryChunks.ts` 命中）。
- ⭐ **不封装 edit-blocks**：`rg -n "edit-blocks|editBlocks" web/src/api/blueprints.ts` **零命中**。
- ⭐ **配置存 labelKey 不存中文**：`cd web && node -e "const s=require('fs').readFileSync('src/config/blueprintStatus.ts','utf8'); const lines=s.split('\n').filter(l=>/label\s*:/.test(l)); if(lines.length) throw new Error('配置里出现 label 字面量: '+lines[0]); if(!/labelKey/.test(s)) throw new Error('缺 labelKey'); console.log('labelKey OK')"` 输出 `labelKey OK`。
- ⭐ **`''` 命中 legacy 档**：`blueprintStatus.spec.ts` 内含 `getBlueprintStatusConfig('')` 与 `getBlueprintStatusConfig('__unknown__')` 的**两条并列**断言且结论不同。
- ⭐ **不改 `config/status.ts`**：`git diff web/src/config/status.ts` 输出**为空**；`rg -n "import type \{ StatusConfig \}" web/src/config/blueprintStatus.ts` 命中。
- ⭐ **令牌唯一来源**：`rg -n "hsl\(" web/src/components/blueprint/annotationTokens.ts` 命中（颜色在此）；`rg -n "hsl\(" web/src/components/blueprint/BlueprintStatusBadge.vue` **零命中**；`rg -n "hsl\(168 76% 42%\)" web/src/components/blueprint/annotationTokens.ts` **零命中**（⛔ teal-500 原值不得作描边，必须是降档的 `hsl(167 76% 32%)`）。
- ⭐ **Badge 不被 `:class` 覆盖颜色**：`cd web && node -e "const s=require('fs').readFileSync('src/components/blueprint/BlueprintStatusBadge.vue','utf8'); const m=s.match(/<Badge[^>]*>/); if(m && /:class=/.test(m[0]) && /(text-|bg-|border-)(red|green|blue|amber|teal|violet)/.test(m[0])) throw new Error('Badge 上追加了颜色类'); console.log('badge variant-only OK')"` 输出 `badge variant-only OK`。
- ⭐ **i18n key 对齐**：`blueprintStatus.spec.ts` 含「12 个 `labelKey` 全部能在 `zh-CN.json` 里查到」的断言且绿。
- api 模块体例：`rg -n "^import \{ get" web/src/api/blueprints.ts web/src/api/repositoryChunks.ts` 两条命中；`rg -n "^export default" web/src/api/blueprints.ts web/src/api/repositoryChunks.ts` 两条命中。
- `cd web && pnpm lint` 通过。
  </acceptance_criteria>
  <done>五个新端点 + 人审七端点 + 确认门八端点有了统一的前端封装且字段逐字同名；`chunk-at` 的 200-空 chunks 判据被封装进返回类型；代码正文来源的缺口被显式定夺为「路径 + 行号 + quote 快照」并登记订正；12 态配置存 i18n key 且 `''` 有专档；批注令牌集中在唯一模块且颜色全部过 WCAG 1.4.11。</done>
</task>

<task type="auto">
  <name>Task 3: utils 两个纯函数模块（blockText 四分支 / iterBlocks 13 collect / canonical 指纹 / diff 分类 / 区间切分 / 两段式 offset）+ store + 三个 composable + 纯函数单测 + 源码扫描守卫</name>
  <files>web/src/utils/blueprintBlocks.ts, web/src/utils/blueprintAnnotations.ts, web/src/stores/useBlueprintViewerStore.ts, web/src/composables/useBlueprintLive.ts, web/src/composables/useBlueprintAnnotations.ts, web/src/composables/useCitationPreview.ts, web/src/utils/__tests__/blueprintBlocks.test.ts, web/src/utils/__tests__/blueprintAnnotations.test.ts, web/src/composables/__tests__/useBlueprintLive.spec.ts, web/src/__tests__/blueprint-source-guard.spec.ts</files>
  <read_first>server/delivery/services/blueprint_anchor.py **L1-70**（⭐ `_block_text` 的**四分支实现逐行**——这是 `blockText` 的唯一同源依据，注意路径是 `delivery/services/` 不是 `process_runtime/`；`list` 分支的连接符是 `"\n"`，`table` 分支是「逐行逐格扁平后 `\n` 连接」）；server/services/process_runtime/blueprint_schema.py **L900-1060**（`_item_key` `:906-912` 的回退规则、`iter_blocks` `:919-1036` 的 **13 处 `collect` 的确切顺序与 section_path 拼法**、`_block_fingerprint` `:1039-1040` 的 `json.dumps(sort_keys=True, ensure_ascii=False)`、`diff_blueprint_blocks` `:1045-1060` 的三类判据）；web/src/utils/variableRef.ts **全文** 与 web/src/utils/__tests__/variableRef.test.ts **全文**（契约型纯函数模块 + 单测的唯一先例：模块 docstring 写「统一格式契约 + 列出全部调用点 + 杜绝各处手写拼接」，每个导出函数一段 JSDoc 带 `@example`）；web/src/stores/analyticsFilters.ts **全文 25 行**（docstring 三段式与 setup store 体例）；web/src/composables/useConfirmDialog.ts（模块级单例 + 返回受控 API 的形状）；web/src/pages/knowledge/index.vue L165-191（受控 Dialog 的 open/loading/data 三 ref 范式——`useCitationPreview` 照它，但 ⭐ **catch 分支完全相反**：analog 失败时关弹窗 + toast，本相位一律兜底不留白）；web/src/components/project/workbench/DocsSection.vue L60-80 与 web/src/components/repository/ReconcilePanel.vue L50-65（**函数式 `refetchInterval` 的逐字先例**，两处都读 `query.state.data`）；web/src/pages/admin/observability/index.vue **L43-85**（⚠️ 仓内唯一「函数体读外部 ref」的写法，但真正驱动它的是 `:47-76` 自建的 `setInterval` + `invalidateQueries` —— 反证「只读外部 ref 不自启动」，⛔ 不复制其 setInterval 方案）；web/src/components/repository/__tests__/ReconcilePanel.spec.ts **L1-80**（⭐ 轮询查询的测试范式：`new QueryClient({ defaultOptions: { queries: { retry: false } } })` + `global.plugins: [i18n, [VueQueryPlugin, { queryClient }]]` + `vi.mocked(api.x).mockResolvedValueOnce(...)` 分轮次 —— `useBlueprintLive.spec.ts` 照它）；web/src/pages/knowledge/entities/[id].vue L40-70（`queryKey: computed(() => [...])` 与 `staleTime: 30_000`、`invalidateQueries({ queryKey: ['knowledge'] })` 的前缀匹配）；server/tests/delivery/test_blueprint_inv6_guard.py L28-95（源码扫描测试的形态：常量正则 + 目录遍历 + 违规清单聚合 + 断言消息把修法写进去——前端守卫 spec 平移它）；.planning/phases/115-ui/115-UI-SPEC.md §7.1（切分四步）、§7.7（侧栏四组判据）、§8.1（21 事件映射表逐行）</read_first>
  <action>
**① 新建 `web/src/utils/blueprintBlocks.ts`**（契约型纯函数模块，docstring 照 `variableRef.ts`：写明「本模块是前后端坐标系同源的唯一落点，杜绝各组件自行取文本 / 自行判 diff」+ 列出全部调用点）。

导出：
- ⭐ `blockText(block): string` —— **四分支优先级逐字复刻 `_block_text`，⛔ 不看 `block.type`**：① `text` 是非空 string → 直取；② `text` 是数组 → `text.map(String).join('\n')`；③ `code?.source` 是非空 string → 取它；④ `rows` 是数组 → 逐行逐格 `String(cell)` 扁平后 `join('\n')`；⑤ 其余 → `''`。docstring 逐字写明**为什么不能按 type 分派**（P-13：schema 对 `text` 无类型约束，「`type: pseudocode` 且 `text` 非空」完全合法；坐标系不一致的后果是 offset 仍在合法范围内 ⇒ 不触发降级、不报错、`<mark>` 照渲，**只是圈错了字**）。
- `iterBlocks(content): Array<{ sectionPath: string, sectionKey: string, block: BlueprintBlock }>` —— **13 处 collect 逐段对齐**（顺序与 `iter_blocks` 一致）：`meta.summary` → `requirement_spec.goal` / `.background` / `.feature_points[<id>].description` → `repo_associations[<repository_id>].rationale.text` / `.responsibility` / `.fitness.reasons` / `.planned_change_summary` / `.support_needed` → `current_state_analysis[<repository_id>].summary` / `.findings[<id>].text` → `implementation_overview.requirement_narrative` / `.modules[<id>].narrative` / `.items[<id>].how` / `.existing_integration` / `.test_strategy` → `api_contracts[<id>].description` / `.data_source.notes` → `impact_analysis.business_impact` / `.affected_features[<feature>].description` / `.compat_risks` / `.rollback_plan` → `interaction_flows[<id>].steps[<seq>].note`。`section_path` 用「点分 + `[标识]`」；`itemKey(obj, index, idField)` 复刻 `_item_key`（优先取标识字段值，缺失回退**位置下标字符串化**）。⛔ `must_haves` / `decision_log` / `deferred_ideas` / `execution_plan` **不走查**（P-14：后端 `iter_blocks` 对它们零 `collect` ⇒ 后端不会往那里挂线程）。
- ⭐ `canonicalBlockFingerprint(block): string` —— **递归排序键**后再 `JSON.stringify`（后端是 `json.dumps(sort_keys=True, ensure_ascii=False)`；前端 `JSON.stringify` 不保证键序，不做 canonical 会把「内容未变、键序不同」误判 `modified`，No Analog #6）。
- `classifyBlockDiff(contentA, contentB)` —— 按 `block_id` 建索引：仅 B 有 → `added`；仅 A 有 → `removed`；两者都有且指纹不等 → `modified`。返回按段分组的结构（供 §9.2 的「未变化的段整段折叠」）。
- `sectionKeyForEvent(eventName): string[]` —— **21 事件 → 段 key 映射逐行照 UI-SPEC §8.1**；返回数组（`repo_research.*` 映射两段、`spec_gate.locked` 映射两段）；未映射事件返 `[]`（那五个 `null` 段的事件只喂阶段时间线）。
- `stageForEvent(eventName): string` —— 事件 → stage（`spec_gate` / `route` / `repo_research` / `confirmation` / `repo_plan` / `merge` / `ai_review` / `pending_review`），供阶段时间线聚合。
- `summaryText(blocks, maxLen)` —— `meta.summary` 首块纯文本截断（列表卡与预览弹层共用）。

**② 新建 `web/src/utils/blueprintAnnotations.ts`**：
- ⭐ `sliceBlockText(text: string, anchors: Array<{ threadId: string, start: number, end: number }>): Array<{ text: string, threadIds: string[] }>` —— §7.1 第 2–4 步：校验（`Number.isInteger` 且 `0 <= start < end <= text.length`，不合法者**剔除并计入降级集合**）→ 按 start 升序 → **重叠不合并**，切成不相交子段，每个子段携带其覆盖的 threadId 集合 → 生成 [纯文本段 | 标注段] 序列。⛔ 不返回 HTML 串（渲染层用 `v-for` + mustache，XSS 面 = 0）。
- `isValidAnchor(anchor, textLength): boolean` 与 `degradedThreadIds(text, anchors): string[]` —— **越界降级是前端概念**，与后端 `orphaned` **不是一回事**（P-7），docstring 逐字写明三态区分表。
- ⭐ **两段式 offset 计算**（把 A2 的不确定性隔离在薄薄一层）：`collectTextNodes(root: Node): Text[]`（触 DOM 的薄函数，用 `document.createTreeWalker(root, NodeFilter.SHOW_TEXT)`；若 Task 1 的能力锁判定不支持，改用递归 `childNodes` 遍历，**签名不变**）+ `offsetInFlatText(nodes: Text[], container: Node, offset: number): number`（**纯数据、恒可单测**：累加前序文本节点长度）。⭐ 二者组合出 `rangeOffsets(range, root)`。
- `groupThreadsByBlock(threads): Record<string, BlueprintThreadDetail[]>`。
- ⭐ `sidebarGroups(threads, orphanedThreads)` —— §7.7 的**四组判据**：未决 `status === 'open' && anchor_status !== 'orphaned'`；已回答 `status === 'answered' && anchor_status !== 'orphaned'`；已关闭 `status ∈ {resolved, dismissed} && anchor_status !== 'orphaned'`；失锚 `anchor_status === 'orphaned'`（**不看 status**，数据源是快照的 `orphaned_threads`，⛔ **前端不再二次过滤**——114 MJ-02 已保证里面只有真失锚）。⚠️ **前三组的 `&& anchor_status !== 'orphaned'` 不可省**：失锚是锚定维度、`status` 是处置维度，两者正交；漏掉否定项会让一条 `open` 的失锚线程在侧栏出现两次、计数重复。组内排序 severity（blocker → warning → info → 无）→ `created_at` 升序。
- ⚠️ **「无 anchor 的系统线程」第三态**（`anchor === null` 但 `anchor_status === 'anchored'`，规格门/确认门/无划线的驳回评论）：**必须出现在侧栏按 status 分组，但正文无任何标记**（§7.1 第 1 步的 `anchor.block_id === block.block_id` 过滤天然把它们排除出正文）。docstring 与单测都要覆盖它。

**③ 新建 `web/src/stores/useBlueprintViewerStore.ts`**（setup store，docstring 三段式）：只放 `sidebarCollapsed` / `showClosedAnnotations` / `kindFilters` 三项**客户端偏好**。⭐ **与 analog（`analyticsFilters.ts` 明确不持久化）的 DIFFER 要写进 docstring**：这三项用 `useLocalStorage`（`@vueuse/core`）持久化，因为它们是**用户偏好**而不是查询语境。⛔ 服务端态（doc / threads / snapshot / events / timeline / gate / list）一律不进 store。

**④ 新建 `web/src/composables/useBlueprintLive.ts`（⭐ 全相位唯一轮询消费点）**：
- `export function useBlueprintLive(artifactId: Ref<string>)`，内部建三个 query（`['blueprint','events',artifactId]` / `['blueprint','snapshot',artifactId]` / `['blueprint','doc',artifactId,versionId ?? 'current']`，queryKey 一律 `computed(() => [...])`）。
- ⭐ **`refetchInterval` 分两种形态写，⛔ 不得统一**（plan-checker BLOCKER-1 定夺，覆盖原「三查询共用一个 helper」的写法）：
  - **snapshot 查询**（响应体自带 `current_status`）：`refetchInterval: query => (LIVE_BLUEPRINT_STATUSES.has(query.state.data?.current_status ?? '') ? 5_000 : false)` —— 读**它自己的 `query.state.data`**，与仓内全部可用先例同款（`DocsSection.vue:73` / `ReconcilePanel.vue:59` / `FeatureBoard.vue:50,58` / `ProjectHealthCard.vue:79` / `BatchIngestPanel.vue:32`）。
  - **doc / events 查询**（响应体**没有**状态字段）：`refetchInterval: () => (isLive.value ? 5_000 : false)`，**并且必须紧跟一条启动保证** `watch(isLive, (on) => { if (on) { docQuery.refetch(); eventsQuery.refetch() } })`。
- ⭐ **docstring 必须逐字解释「为什么快照与 doc/events 写法不同」**：函数式 `refetchInterval` **只在本查询自己的 state 更新或 options 触发 `setOptions` 时重算**，函数体里读外部 ref **不是被追踪的响应式依赖**（`cloneDeepUnref` 不下探函数体）。打开 `drafting` 蓝图时三查询近乎同时发出，doc/events 先落地而共享 `currentStatus` 仍是 `''` ⇒ 算出 `false`、永不装定时器、也永不再重算 ⇒ **首屏有内容、无报错、快照徽标还在跳，章节进度却冻结在打开那一刻**，正是 P-9 静默假通过。`watch` 的首次 `refetch()` 踢动之后，每次 fetch 结束都会重算间隔，链条自持。⛔ **不得删掉该 `watch`**（`useBlueprintLive.spec.ts` 有对应的 1 → 2 断言）。⛔ 同样不写成 `computed(() => isLive ? 5_000 : false)` 的非响应式传值形态。
- ⚠️ 参考实证：仓内**唯一**在函数体里读外部 ref 的先例是 `pages/admin/observability/index.vue:83`（`() => (autoRefresh.value ? AUTO_REFRESH_MS : false)`），但那一页真正驱动刷新的是**页面自建的 `setInterval` + `invalidateQueries`**（`:47-76`）—— 恰好反证「只读外部 ref 不足以自启动」。本文件用 `watch` + `refetch()` 承担同一职责，⛔ 不复制它的 `setInterval` 方案（会绕开 TanStack Query 的窗口失焦策略）。
- ⛔ **不写 `useDocumentVisibility`**（TanStack Query 的 `refetchIntervalInBackground` 默认 false）；⛔ 不用 `composables/usePolling.ts`。
- 返回 `{ isLive, events, stageTimeline, sectionProgress, currentStatus, doc, snapshot, refetchAll }`；`sectionProgress: Record<sectionKey, string>` 由 `sectionKeyForEvent` + 「一段取其命中事件中 `ts` 最大的一条」派生；插值键缺失时**回落无参兜底文案**（Task 1 的 `*Generic` 键，P-8）。
- 文件顶部 docstring 写明：「同步点 2 之后若 0.19 的推送契约就位，**只改这一个文件**（把 `refetchInterval` 换成订阅）。这是本相位对并行纪律最实际的交代 ⇒ `refetchInterval` 字面量**不得**出现在任何组件里（源码扫描锁死）」。

**⑤ 新建 `web/src/composables/useBlueprintAnnotations.ts`**：单一状态源 `activeThreadId: Ref<string | null>` + 由 `utils/blueprintAnnotations` 派生的 `threadsByBlock` / `groups` / `counts`（未决 BLOCKER / 待澄清 / 失锚三个计数）+ `selectThread(id)` / `clearActive()`。⛔ 组件内不得各自持有第二份 `activeThreadId`。

**⑥ 新建 `web/src/composables/useCitationPreview.ts`**：受控 Dialog 的 open/loading/data/citation 四 ref（形状抄 `pages/knowledge/index.vue:165-191`），⭐ **catch 分支与 analog 完全相反**：analog 失败时 `viewOpen.value = false` + toast；本相位 UI-SPEC §10.1 明令**兜底不留白** ⇒ 任何失败一律把 `fallback` 置为 citation 自带的 `title` / `quote` 并**保持弹窗打开**，⛔ 不关弹窗、⛔ 不回显后端错误体。docstring 写明这条反向差异。

**⑦ 新建 `web/src/utils/__tests__/blueprintBlocks.test.ts`**：
- ⭐ **`blockText` 与后端逐字一致的 fixture 断言**（P-13 的头号靶子）：手抄 5 个 fixture block（paragraph / list（三条目）/ pseudocode（**同时带非空 `text` 与 `code.source`** —— 断言取 `text` 不取 `code.source`）/ table（两行三列）/ 空块），期望值**手抄自后端 `_block_text` 的实现逐行推导**并在注释里标注推导过程；变异提示写进注释：「若把实现改成按 `block.type` 分派，第 3 条 fixture 即转红」。
- `iterBlocks`：一份含全部 13 处 collect 的 content ⇒ 断言产出条数与 `sectionPath` 逐条；⭐ 另一份把 `must_haves` / `decision_log` / `deferred_ideas` / `execution_plan` 填满 ⇒ 断言产出**不含**它们（P-14）；`_item_key` 回退：缺 `id` 的条目 `sectionPath` 用位置下标。
- ⭐ `canonicalBlockFingerprint`：同一对象两种键序 ⇒ **同一指纹**；嵌套 dict 与数组内的 dict 同样生效；值不同 ⇒ 指纹不同。
- `classifyBlockDiff` 三类各一条 + 「键序不同但内容相同 ⇒ 不算 modified」（这条是上一条的下游证伪）。
- `sectionKeyForEvent`：**21 个事件穷举**（用一个数组逐个断言，含 5 个映射 `[]` 的、2 个映射两段的）；未知事件返 `[]`。
- `producedByReason` 五档（在 Task 2 的 config 测试里，此处不重复）。

**⑧ 新建 `web/src/utils/__tests__/blueprintAnnotations.test.ts`**：
- ⭐ `sliceBlockText` **六类边界各一条**：越界（`end > text.length`）/ 反序（`start >= end`）/ 重叠（两条区间交叠 ⇒ 切成三段，中间段 `threadIds.length === 2`，**⛔ 不合并**）/ 非整数（`start: 1.5`）/ 空 anchors（返回单段全文）/ 全覆盖（`0..length`）。另断言**拼接回原文**：`result.map(s => s.text).join('') === text`（这条能逮住任何切点算术错误）。
- `offsetInFlatText`：纯数据用例（构造 `[{length:3},{length:3},{length:3}]` 形态的假节点数组），⛔ 不依赖真实 DOM。
- `collectTextNodes`：**依据 Task 1 的能力锁结论**——支持则用 happy-dom 构造 `<div>abc<span>def</span>ghi</div>` 断言收集到三个文本节点；不支持则本用例 `it.skip` 并在注释里指向 UAT 清单。
- ⭐ `sidebarGroups` **三条硬断言**：① 一条 `status === 'open' && anchor_status === 'orphaned'` 的线程 ⇒ **四组条目总数 == 线程总数**且它**只**出现在失锚组（§20 断言 11；变异：去掉前三组的否定项 ⇒ 转红）；② 一条 `anchor === null && anchor_status === 'anchored'` 的系统线程 ⇒ 出现在 status 对应组（P-7 第三态，⛔ 不被漏掉）；③ 一条 `end_offset > text.length` 的 anchored 线程 ⇒ 走越界降级但**不出现在失锚组**（§20 断言 8）。
- `groupThreadsByBlock`：`anchor === null` 与 `anchor.block_id` 不匹配的线程都不进任何 block 分组。

**⑨ 新建 `web/src/composables/__tests__/useBlueprintLive.spec.ts`**（⭐ plan-checker BLOCKER-1 的可证伪落点，范式抄 `ReconcilePanel.spec.ts:13,60,63`：`QueryClient({ defaultOptions: { queries: { retry: false } } })` + `VueQueryPlugin` + `vi.mock('~/api/blueprints')`；用 `vi.useFakeTimers()` 推进 5s 间隔，composable 挂在一个最小宿主组件里以获得 vue-query 的注入上下文）：
- ⭐ **「非活跃 → 活跃：doc/events 真的开始轮询」**（唯一必须存在的 BLOCKER 用例）：快照 mock **首轮**返 `{ current_status: 'pending_review' }`、**次轮起**返 `{ current_status: 'drafting' }`；doc/events 各返固定体。时序：挂载 → 三查询各 fetch **1** 次（此时 `isLive === false`）→ 推进定时器让快照重取并转 `drafting` → `await flushPromises()` → 断言 **`getBlueprintDocument` 与 `getBlueprintEvents` 的调用次数从 1 变为 2**。⚠️ 注释里写死变异提示：「**删掉 `watch(isLive, ...)` 这一条，本用例必须转红**；若删了仍绿，说明断言没测到启动路径，要修用例而不是删断言」。
- 「活跃 → 终态自动停」：快照先 `drafting`、后 `merged`；推进两个 5s 窗口，断言 doc/events 的调用次数**不再增长**。
- 「一直非活跃则不轮询」：快照恒 `pending_review`；推进 15s，断言三者调用次数都仍是 1（防止 `watch` 被写成无条件 `refetch`）。
- ⛔ 本 spec **不测**业务派生（`sectionProgress` / `stageTimeline` 归 Task 3 ⑦ 的纯函数单测），只锁轮询启停这一件事。

**⑩ 新建 `web/src/__tests__/blueprint-source-guard.spec.ts`**（前端零先例，形态平移自 `test_blueprint_inv6_guard.py`：常量 + `node:fs` 递归遍历 + 违规清单聚合 + 断言消息把修法写进去）：
- 扫描面常量：`SCAN_DIRS = ['src/components/blueprint', 'src/pages/knowledge/blueprints']`（用 `import.meta.dirname` 或 `process.cwd()` 定位 `web/src`；**目录不存在时视为空集合**，本 plan 阶段两个目录几乎为空 ⇒ 用例平凡通过，后续 plan 逐个把它压实）。
- 断言 6：`refetchInterval` 在扫描面**零命中**（例外：无）；断言消息写「轮询只能在 `composables/useBlueprintLive.ts`」。
- 断言 10：`edit-block` / `edit-blocks` / `editBlocks` 在扫描面**零命中**；断言消息写「block 正文编辑顺延 Phase 116，本相位零入口」。
- 断言 4：扫描面内出现的 `knowledge.blueprints.error.*` i18n key，用于 404 分支的**只允许 `notFoundOrForbidden` 一个**；实现方式 = 收集所有 `error.<key>` 命中，断言集合 ⊆ `{notFoundOrForbidden, blocked, conflict, unavailable}` 且**不含**任何 `notFound` / `notExist` / `forbidden` 单独键；另扫描面内不得出现中文字面量「该蓝图不存在」「无权限访问」等竞品文案（正则清单写死）。
- ⚠️ 每条断言的 message 里都要写「这条为什么存在 + 怎么修」（照后端守卫的体例），否则后人只会看到一行冷冰冰的 assert 失败。
  </action>
  <verify>
    <automated>cd web && pnpm exec vitest run src/utils/__tests__/blueprintBlocks.test.ts src/utils/__tests__/blueprintAnnotations.test.ts src/composables/__tests__/useBlueprintLive.spec.ts src/__tests__/blueprint-source-guard.spec.ts && pnpm type-check && pnpm lint</automated>
  </verify>
  <acceptance_criteria>
- `cd web && pnpm exec vitest run` **全绿**（含既有 ~50 个 spec，无新增失败）；`pnpm type-check` 通过；`pnpm lint` 通过。
- ⭐ **P-13 双证**：`rg -n "block.type|block\.type ===" web/src/utils/blueprintBlocks.ts` 在 `blockText` 函数体内**零命中**（`cd web && node -e "const s=require('fs').readFileSync('src/utils/blueprintBlocks.ts','utf8'); const i=s.indexOf('function blockText'); const j=s.indexOf('\nexport', i+1); const body=s.slice(i, j>0?j:s.length); if(/\.type\b/.test(body)) throw new Error('blockText 按 type 分派了（P-13）'); console.log('blockText field-priority OK')"` 输出 `blockText field-priority OK`）；且「pseudocode 同时带非空 text 与 code.source ⇒ 取 text」那条 fixture 用例存在并绿。
- ⭐ **canonical 指纹**：`blueprintBlocks.test.ts` 含「同一对象不同键序 ⇒ 同一指纹」断言且绿；`rg -n "JSON.stringify" web/src/utils/blueprintBlocks.ts` 的命中处**前面必有递归排序**（人工核对，SUMMARY 登记）。
- ⭐ **iterBlocks 排除四段**：测试含「填满 `must_haves` / `decision_log` / `deferred_ideas` / `execution_plan` ⇒ 产出不含它们」断言且绿。
- ⭐ **切分六边界 + 拼接还原**：`rg -c "it\(|test\(" web/src/utils/__tests__/blueprintAnnotations.test.ts` ≥ **12**；含 `join('') === text` 形态的还原断言。
- ⭐ **三态不混（P-7）**：`blueprintAnnotations.test.ts` 同时含 (a) `orphaned` 且 `open` 的线程只出现一次、(b) `anchor === null` 的系统线程仍进 status 组、(c) 越界线程不进失锚组 —— **三条并列存在**且绿。
- ⭐ **21 事件穷举**：`sectionKeyForEvent` 的测试用例覆盖 21 个事件名（`rg -c "blueprint\." web/src/utils/__tests__/blueprintBlocks.test.ts` ≥ 21）。
- ⭐ **唯一轮询消费点**：`rg -c "refetchInterval" web/src/composables/useBlueprintLive.ts` ≥ 1；`rg -n "refetchInterval" web/src --glob '!**/useBlueprintLive.ts' --glob '!**/__tests__/**' | rg "components/blueprint|pages/knowledge/blueprints"` **零命中**；`rg -n "useDocumentVisibility|usePolling" web/src/composables/useBlueprintLive.ts` **零命中**。
- ⭐ **函数式 refetchInterval + 启动保证**（plan-checker BLOCKER-1，放宽为「至少一处函数式 **且** 存在 `watch(isLive`」）：`cd web && node -e "const s=require('fs').readFileSync('src/composables/useBlueprintLive.ts','utf8'); if(!/refetchInterval:\s*(\([^)]*\)|[A-Za-z_$][\w$]*)\s*=>/.test(s)) throw new Error('refetchInterval 必须是函数式（P-9）'); if(!/watch\(\s*isLive/.test(s)) throw new Error('缺少 watch(isLive) 启动踢动：doc/events 永不开始轮询（BLOCKER-1）'); console.log('live polling shape OK')"` 输出 `live polling shape OK`。
- ⭐ **1 → 2 的启动断言真实存在且绿**：`cd web && pnpm exec vitest run src/composables/__tests__/useBlueprintLive.spec.ts` 全绿；该文件含「非活跃 → 活跃后 doc/events 调用次数从 1 变 2」用例（`rg -n "pending_review[\s\S]{0,400}drafting" web/src/composables/__tests__/useBlueprintLive.spec.ts` 命中）。执行期**手动做一次变异验证**（临时注释掉 `watch(isLive, ...)` ⇒ 该用例转红 ⇒ 恢复），结论登记进 SUMMARY。
- ⭐ **源码守卫 spec 可运行且三条断言都在**：`rg -n "refetchInterval|edit-block|notFoundOrForbidden" web/src/__tests__/blueprint-source-guard.spec.ts` 三条全命中；用例在两个扫描目录尚不存在/为空时**不报错**（本 plan 阶段应绿）。
- ⭐ **store 只放三项客户端偏好**：`rg -n "doc|threads|snapshot|events" web/src/stores/useBlueprintViewerStore.ts` **零命中**；`rg -n "useLocalStorage" web/src/stores/useBlueprintViewerStore.ts` 命中且 docstring 含「用户偏好」四字。
- ⭐ **citation 预览兜底不关弹窗**：`cd web && node -e "const s=require('fs').readFileSync('src/composables/useCitationPreview.ts','utf8'); const i=s.indexOf('catch'); const body=s.slice(i, i+400); if(/open\.value\s*=\s*false/.test(body)) throw new Error('catch 分支关掉了弹窗（UI-SPEC §10.1 兜底不留白）'); console.log('citation fallback OK')"` 输出 `citation fallback OK`。
- ⛔ 零既有面改动：`git diff --name-only web/src | rg -v "^web/src/(types|api|config|utils|stores|composables|components/blueprint|__tests__|locales|styles)/"` 输出**为空**；`git diff web/src/config/status.ts web/src/api/client.ts web/src/components/layout/AnchorNavLayout.vue web/src/components/common/CompactEmptyState.vue web/src/components/prompts/PromptVersionDiff.vue` 输出**为空**。
  </acceptance_criteria>
  <done>前后端坐标系同源的三条硬约束（`blockText` 四分支 / `iterBlocks` 13 collect / canonical 指纹）各有可证伪的纯函数与单测；区间切分与侧栏四组判据被完整覆盖，含 P-7 三态不混的三条并列断言；唯一轮询消费点是函数式 `refetchInterval` 且被源码扫描锁死单文件；源码守卫 spec 就位，后续五个 plan 每次都要复跑它。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 后端响应（蓝图正文 / 线程消息 / citation quote）→ DOM | 半可信文本进渲染层，是本相位唯一的 XSS 面 |
| 前后端两套「取块文本」实现 → offset 坐标系 | 不一致会让批注**圈错字**且不报错，是最难被逮的一类错 |
| 「无数据」与「零值」→ 人审判断 | `null → 0` 的改写会让评审据错误指标放行 |
| 生成中状态 → 轮询开关 | 非响应式实现会让页面永久停在旧内容而看起来完全正常 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115-13 | Tampering | 蓝图正文 / 线程消息 / citation quote 经 `v-html` 注入 ⇒ 存储型 XSS | mitigate | 切分函数**返回结构化数组而非 HTML 串**，渲染层只能 `v-for` + mustache；`BlueprintStatusBadge.vue` 零 `v-html`；后续 plan 沿用同一约束并由 §20 断言与 code review 复查 |
| T-115-14 | Tampering | `blockText` 按 `block.type` 分派 ⇒ 与后端坐标系不一致，`<mark>` 圈错字且不触发任何降级 | mitigate | 四分支字段优先级 + 源码扫描断言「`blockText` 函数体内无 `.type`」+ 「pseudocode 同时带 text 与 code.source」的 fixture 用例（P-13） |
| T-115-15 | Tampering | 前端 `JSON.stringify` 键序不定 ⇒ diff 把未变化的块误判 `modified`，评审被噪声淹没 | mitigate | `canonicalBlockFingerprint` 递归排序 + 「同对象不同键序 ⇒ 同指纹」断言（No Analog #6） |
| T-115-16 | Denial of Service | `refetchInterval` 被实现成非响应式值、或 doc/events 只在函数体里读外部 ref ⇒ 生成中的蓝图永不刷新且页面看起来正常 | mitigate | 两形态函数式写法 + `watch(isLive)` 启动踢动 + `useBlueprintLive.spec.ts` 的 1 → 2 时序断言 + 源码正则（含 `watch(isLive`）+ 单文件锁（P-9 / §20 断言 6 / BLOCKER-1） |
| T-115-17 | Information Disclosure | 前端把 404 翻译成「该蓝图不存在」/「无权限」两种文案 ⇒ 破坏后端刻意的存在性防线 | mitigate | i18n 只建 `error.notFoundOrForbidden` 一个键 + 源码守卫扫描竞品键与中文字面量（§20 断言 4） |
| T-115-18 | Information Disclosure | 批注/评论正文进 `console` 或埋点 | mitigate | 本 plan 零 `console` 正文；后续 plan 沿用；UI-SPEC §19 的「日志/埋点不打印蓝图正文与批注正文」写进各模块 docstring |
| T-115-19 | Tampering | 侧栏四组漏掉 `&& anchor_status !== 'orphaned'` ⇒ 同一线程出现两次、计数重复、选中时两处高亮 | mitigate | `sidebarGroups` 的三条并列断言（§20 断言 11 / 8 + P-7 第三态） |
| T-115-20 | Elevation of Privilege | 前端自建权限判断（只读/可编辑二态）⇒ 与后端闸不一致 | mitigate | 一律以后端状态码为准；`isBlueprintEditable` 只驱动**渲染与否**（不是权限），成员逐字对齐后端常量 |
| T-115-SC | Tampering | 依赖供应链 | accept | **零新增运行时依赖**（`git diff web/package.json web/pnpm-lock.yaml` 为空）；`pnpm install --frozen-lockfile` 保证 lock 不漂移；⛔ 不引入 `@floating-ui/vue` 的首个调用点（本 plan 定夺改用 reka-ui `PopoverAnchor`） |
</threat_model>

<verification>
- `cd web && pnpm install --frozen-lockfile` 成功；`pnpm exec vitest run` 全绿（相对基线无新增失败，基线数登记 SUMMARY）。
- `cd web && pnpm type-check` 通过；`pnpm lint` 通过。
- 三处追加点纯追加：`git diff web/src/api/index.ts web/src/styles/main.css web/src/locales/zh-CN.json | rg "^-[^-]"` 输出为空。
- 零依赖变更：`git diff web/package.json web/pnpm-lock.yaml web/pnpm-workspace.yaml` 输出为空。
- 零既有组件改动：`git diff web/src/config/status.ts web/src/api/client.ts web/src/api/deliveryArtifacts.ts web/src/api/knowledge.ts web/src/components/layout/AnchorNavLayout.vue web/src/components/common/CompactEmptyState.vue web/src/components/prompts/PromptVersionDiff.vue web/src/components/project/warroom/MermaidDiagram.vue web/src/components/delivery/ArtifactTimeline.vue` 输出为空。
- 后端零改动：`git diff --name-only server/` 输出为空。
- 源码守卫 spec 单独复跑绿：`pnpm exec vitest run src/__tests__/blueprint-source-guard.spec.ts`。
</verification>

<success_criteria>
- 前端环境可跑，且 RESEARCH 挂着的 **A2 假设（happy-dom 的 `TreeWalker`/`Range` 支持度）被一条永久能力锁用例正式 settle 并登记**；offset 计算的两段式设计让「纯数据那半」无论结论如何都恒可单测。
- **三条前后端同源约束固化成纯函数 + 单测**：`blockText` 四分支（P-13）、`iterBlocks` 13 处 collect 与 `_item_key` 回退、canonical 指纹（No Analog #6）。任一被改回错误形态，对应用例立刻转红。
- **区间切分与侧栏四组判据被彻底覆盖**：六类边界 + 拼接还原 + P-7 三态不混三条并列断言。
- **两条并行纪律有物理落点**：唯一轮询消费点（快照读自身 data 的函数式 `refetchInterval` + doc/events 的 `isLive` 函数式 **加 `watch(isLive)` 启动踢动**，同步点 2 后只改一个文件）+ 前端源码扫描守卫（`refetchInterval` 单文件 / `edit-block` 零命中 / 404 文案单键）。
- **P-9 静默假通过被一条时序用例真正堵死**：`useBlueprintLive.spec.ts` 的「非活跃 → 活跃 ⇒ doc/events 调用 1 → 2」用例，删掉 `watch` 即转红。
- **三处纯追加点一次做完**（i18n 全量子树 / safelist 12 图标 / api barrel），后续五个 plan 不再碰这三个文件 —— 消除五向冲突面。
- **四处 UI-SPEC 订正被显式登记**：`refetchInterval` 改「快照函数式读自身 data + doc/events 函数式读 `isLive` 且配 `watch` 踢动」并删可见性判断（§8.3）、`chunk-at` 判据（§10.1）、代码正文来源降级为「路径 + 行号 + quote 快照」（§3.6/§10.1）、选区 popover 改用 **从 `reka-ui` 直接导入的** `PopoverAnchor`（§7.4）。
- 零新增依赖、零既有组件改动、后端零改动。
</success_criteria>

<output>
完成后创建 `.planning/phases/115-ui/115-02-SUMMARY.md`，provides 至少包含：
- ⭐ **A2 的 settle 结论**：happy-dom 四项能力（`createTreeWalker` / `createRange` / `Range.getBoundingClientRect` / `getSelection`）的**逐项实测支持情况**，以及由此决定的 `collectTextNodes` 测试策略（自动化 or UAT）；
- **纯函数模块的完整导出清单与逐字签名**（`blockText` / `iterBlocks` / `canonicalBlockFingerprint` / `classifyBlockDiff` / `sectionKeyForEvent` / `stageForEvent` / `summaryText` / `sliceBlockText` / `isValidAnchor` / `degradedThreadIds` / `collectTextNodes` / `offsetInFlatText` / `rangeOffsets` / `groupThreadsByBlock` / `sidebarGroups`），后续五个 plan 直接照它调用；
- `api/blueprints.ts` 与 `api/repositoryChunks.ts` 的**函数清单与返回类型**（含 `{chunks, usable}`）；
- `config/blueprintStatus.ts` 的 12 态表、`EDITABLE_BLUEPRINT_STATUSES` / `LIVE_BLUEPRINT_STATUSES` 成员、`PRODUCED_BY_PREFIXES` 五档；
- `annotationTokens.annotationClass` 的**签名与全部分档**（四色相 × 四状态 × active），后续组件只调它、不写颜色；
- 三个 composable 的**返回结构**（尤其 `useBlueprintLive` 的返回键与 `sectionProgress` 形状）；
- **四处 UI-SPEC 订正的逐条登记**（§8.3 轮询写法「两形态 + `watch` 踢动」 / §10.1 chunk-at 判据 / §3.6 代码正文来源降级 / §7.4 选区 popover 改**从 `reka-ui` 直接导入** `PopoverAnchor`，含 `~/components/ui/popover` barrel 不导出它的核实结论）；
- ⭐ **`watch(isLive)` 变异验证结论**（临时注释掉后 `useBlueprintLive.spec.ts` 是否转红），以及 happy-dom 能力锁若走 `toMatchInlineSnapshot` 形态时的实测快照值；
- ⭐ **SC-4 的范围收窄登记**（P-5）：`associations` 段本相位只做「本蓝图引用了」+「关联项目」，「引用了本蓝图 / 关联知识」顺延 Phase 116 的知识图谱物化——**同时写进 STATE 的 Pending Todos**；
- i18n 子树的**顶层键清单**与 `progress.*` 的「带插值 + `*Generic` 兜底」配对规则；
- safelist 追加的 12 个图标与「两种 icon 契约」的区分说明；
- 前端基线（vitest 用例数 / lint / type-check）与本 plan 后的对比。
</output>
