---
phase: 115-ui
plan: 04
type: execute
wave: 3
depends_on: ["115-02"]
files_modified:
  - web/src/components/blueprint/BlueprintThreadSidebar.vue
  - web/src/components/blueprint/BlueprintThreadCard.vue
  - web/src/components/blueprint/BlueprintThreadComposer.vue
  - web/src/components/blueprint/BlueprintFindingActions.vue
  - web/src/components/blueprint/BlueprintSelectionPopover.vue
  - web/src/components/blueprint/BlueprintReviewActions.vue
  - web/src/components/blueprint/BlueprintRejectDialog.vue
  - web/src/components/blueprint/BlueprintBlockedDialog.vue
  - web/src/components/blueprint/BlueprintQualityPanel.vue
  - web/src/components/blueprint/BlueprintVersionSwitcher.vue
  - web/src/components/blueprint/BlueprintBlockDiff.vue
  - web/src/components/blueprint/__tests__/threadSidebar.spec.ts
  - web/src/components/blueprint/__tests__/reviewActions.spec.ts
  - web/src/components/blueprint/__tests__/blockDiff.spec.ts
autonomous: true
requirements: [CLAR-01, FLOW-08, VIEW-01]
prohibitions:
  - "🔒 **前端 CREATE-ONLY**：本 plan **零既有文件修改**（`git diff --name-only web/src | rg -v '^web/src/components/blueprint/'` 必须为空）。⛔ 不碰 115-02 已做完的三处追加点（`zh-CN.json` / `main.css` / `api/index.ts`）；缺 i18n key 或缺 safelist 图标 ⇒ **回报而不是自己补**"
  - "⛔ **文件所有权（与同波次的 115-03 零交集）**：本 plan 只创建上方 `files_modified` 列出的 11 个组件与 3 个测试文件。⛔ 不得创建/修改 `BlueprintBlock.vue` / `BlueprintBlockList.vue` / `BlueprintCitationChip.vue` / `CitationPreviewDialog.vue` / `citation/**`（那是 115-03 的所有权）"
  - "⛔ ⭐ **线程动作按 `kind` 硬分流做在渲染层**（114-REVIEW CR-01，全相位最不能错的一条）：`kind === 'ai_review_finding'` 的线程卡**根本不渲染** `BlueprintThreadComposer`（不是 `disabled`、不是隐藏），只给「已修复」/「误报忽略」两个按钮。⛔ **绝不做「统一输入框 + 提交时按 kind 切端点」** —— 后端对 finding 走 `answer/` 通道**一律 400**，且回灌链 `REFLOW_KINDS` fail-closed 过滤；那种实现会稳定撞 400 或误处置"
  - "⛔ ⭐ **`readonly` 是「不存在于 DOM」不是 `disabled`**（114-REVIEW MJ-04 / §7.9）：`isBlueprintEditable(current_status) === false` 时，`BlueprintThreadComposer` 与选区 popover 的「发起评论」按钮**一律 `v-if` 掉**。⚠️ 与之**刻意不同**：终审「通过 / 驳回」按钮是 `disabled` + `Tooltip` 说明原因（§11.1）—— 两种处理不要统一（终审按钮的存在本身有信息量；可编辑闸拦的是会撞 400 的写路径）"
  - "⛔ ⭐ **finding 处置不受 `readonly` 闸约束**（§7.9 末条）：后端未对 `resolve/` `dismiss/` 加状态闸，且那是超界死锁的**唯一正向出口**。`readonly === true` 时 `BlueprintFindingActions` **仍然渲染**"
  - "⛔ ⭐ **approve 409 必须渲染可点清单**（§8.2 / §20 断言 3）：`unresolved_blocker_thread_ids` 逐条渲染成**可点击跳转**的处置入口（点击 = 关 Dialog → 打开侧栏 → 选中该线程 → 正文滚动定位）。**只显示一句「不可确认」即视为不合格** —— 那份清单是超界死锁的唯一解药入口（114-05 原话）"
  - "⛔ **驳回的 `comment` 必填非空**：空 / 纯空格 ⇒ 提交按钮 `disabled` + 输入框下方 `text-destructive` 内联提示。⛔ 不允许空评论驳回"
  - "⛔ **finding 处置的 `reason` 必填非空**：同上；⛔ 前端**不自行拼装**后端的结论文本（后端会写成 `[已修复|误报忽略] {reason}（处置人：{uid}）`，提交成功后重取线程即可看到）"
  - "⛔ **不做乐观更新**（114-REVIEW MJ-01 第二点）：任何动作端点 2xx 后一律以**响应体的 `current_status`** 为准写回展示态，并 `invalidateQueries({ queryKey: ['blueprint'] })` 重取。⛔ 前端不得自行推断下一状态"
  - "⛔ **`answer` 端点恒 200，`reflow.status` 不得当作失败**（§8.2 末段）：`applied` → 成功 toast + 重取正文；`unchanged`/`noop` → info toast；`conflict` → warning toast + 列出 `conflict_block_ids`；`failed`/`invalid` → warning toast「答案已保存，回灌未成功」。⛔ 任何分支都不得渲染成红色错误态或回滚 UI"
  - "⛔ **失锚组直接渲染快照的 `orphaned_threads`，前端不再二次过滤**（114-REVIEW MJ-02）：`_has_anchor_locator` 已前置，里面只有真失锚。⛔ 加 `.filter(t => t.anchor?.block_id)` 会把真失锚也滤掉（§20 断言 5 专门逮这个）"
  - "⛔ **侧栏前三组的 `&& anchor_status !== 'orphaned'` 不可省**（§7.7）：失锚是**锚定维度**、`status` 是**处置维度**，两者正交。漏掉否定项会让一条 `open` 的失锚线程在侧栏出现两次、计数重复、选中时两处同时高亮（§20 断言 11）。分组一律调 115-02 的 `sidebarGroups`，⛔ 组件内不自写四组判据"
  - "⛔ **质量指标 `null` 渲染「暂无数据」绝不显示 0**（§11.2 / §20 断言 7）：`v ?? 0` 是明令禁止的写法。三态并列用例（`null` / `0` / 正值）必须存在"
  - "⛔ **不引入 `@floating-ui/vue`、不手写 `useFloating`**（115-02 定夺，「不得改选」锁在**方案层面**）：选区 popover 走 **`import { PopoverAnchor } from 'reka-ui'`** + 零尺寸虚拟锚点 div（`position: fixed` 到 `rect`），容器与内容用 `~/components/ui/popover` 的 `Popover` / `PopoverContent`。⚠️ ⛔ **不要从 `~/components/ui/popover` 导入 `PopoverAnchor`**（plan-checker C1 已核实：该 barrel 只导出 `Popover` / `PopoverContent` / `PopoverTrigger`，补一行导出即第七处既有文件修改，违反 CREATE-ONLY）。若 `reka-ui@^2.9.10` 不导出它，降级用 `PopoverTrigger` + 零尺寸 div 并在 SUMMARY 登记"
  - "⛔ **`refetchInterval` 字面量在本 plan 任何文件零命中**（§20 断言 6）；⛔ `edit-block` / `edit-blocks` / `editBlocks` 零命中（§20 断言 10）"
  - "⛔ **禁 `v-html`**、⛔ 组件内零颜色字面量（`rg 'hsl\\('` 零命中，颜色一律经 `annotationTokens` 或 `.diff-*` scoped 类）、⛔ 禁止在 `Badge` 上用 `:class` 追加颜色类"
  - "⛔ **`PromptVersionDiff.vue` 的 `.diff-*` 是 `<style scoped>` 无法跨组件复用**（P-11）：「逐字沿用」的落地是**把三条 CSS 原样复制进新组件的 scoped style**，不存在可 import 的共享令牌文件。⛔ 别去找、别新建全局令牌"
  - "⛔ **不照抄 `PromptVersionDiff.vue` 的 `watch(..., { deep: true })`**（`:30-36`）：那是既有的一处自相矛盾（deep watch 配 `shallowRef`）。新组件监听 `versionId` 这类标量即可"
  - "⛔ 不新增运行时依赖、不改 `web/package.json` / lockfile；后端零改动"
must_haves:
  truths:
    - "⭐ **finding 线程的侧栏渲染不出「回复」输入框（§20 断言 1）**：`kind === 'ai_review_finding'` 的 `BlueprintThreadCard` 内 `[data-testid=blueprint-thread-composer]` **不存在于 DOM**，只有 `[data-testid=blueprint-finding-actions]`；`ai_clarification` / `human_comment` / `repo_confirmation` 三类反向为真。变异（把 finding 也渲成可回复）⇒ 用例转红"
    - "⭐ **`readonly` 时作答框与「发起评论」按钮不存在于 DOM（§20 断言 2）**：`readonly === true` ⇒ `blueprint-thread-composer` 与选区 popover 的 `blueprint-selection-comment` **均不存在**（⛔ 不是 `disabled`）；**同一用例同时断言 finding 处置按钮仍然存在**。两个变异（改成 disabled / 把 finding 处置也关掉）各自转红"
    - "⭐ **approve 409 的可点清单（§20 断言 3）**：`BlueprintBlockedDialog` 把 `unresolved_blocker_thread_ids` 每一项渲染成可点条目（severity 点 + finding 首行摘要 + `@click` emit `goto-thread`），条目数 == 数组长度；变异（只渲染一句「不可确认」）⇒ 转红"
    - "⭐ **`orphaned_threads` 直接渲染不二次过滤（§20 断言 5）**：失锚组条数 == 后端返回的 `orphaned_threads.length`（含一条「无 anchor 的系统线程」与一条真失锚线程的混合数据）；变异（加 `.filter(t => t.anchor?.block_id)`）⇒ 转红"
    - "⭐ **失锚线程只出现一次（§20 断言 11）**：一条 `status === 'open' && anchor_status === 'orphaned'` 的线程 ⇒ 四组条目**总数 == 线程总数**且它**只**在失锚组；分组一律调 115-02 的 `sidebarGroups`"
    - "⭐ **质量指标三态（§20 断言 7）**：`null` → 「暂无数据」/ `0` → 「0 次人工编辑」/ 正值 → 具体值，三条并列用例；`citation_coverage` 恒显示百分比，且当 `current_state_analysis` / `repo_associations` / `impact_analysis` 三处**全空**时追加一枚 `<Badge variant=\"outline\">无关键结论</Badge>`（避免把「没内容」读成「证据齐备」）"
    - "⭐ **终审按钮是 `disabled` + Tooltip 不是不渲染**：`current_status !== 'pending_review'` ⇒ 两个按钮存在于 DOM 且 `disabled`，Tooltip 文案含状态中文名；`pending_review` ⇒ 可用。与 `readonly` 的「不渲染」刻意不同"
    - "⭐ **驳回走受控 Dialog 不走 `useConfirmDialog`**（需要输入框）：`comment` 必填非空、空/纯空格 ⇒ 提交 `disabled` + 内联 `text-destructive` 提示；底部常驻「驳回后蓝图回到『产出中』，修订轮次将变为 {n+1}」；主按钮 `variant=\"destructive\"`。**通过**走 `useConfirmDialog`（无输入框），文案逐字照 §16"
    - "⭐ **finding 处置的 `reason` 弹窗是受控 `Dialog` 不是 `useConfirmDialog`**（需要输入框）；两个动作分别调 `resolve/` 与 `dismiss/`；⛔ 前端不拼装结论文本"
    - "⭐ **版本原因由 `produced_by_ref` 四前缀映射**（唯一判据，调 115-02 的 `producedByReason`）：`human_edit:` 人工编辑 / `ai_review_reflow:` 澄清回灌 / `human_block_restore:` 人工块保护 / `blueprint_review_reject:` 人审驳回 / 其余 AI 产出；数据源是既有 `deliveryArtifacts.getArtifactTimeline`（**零新端点**）"
    - "⭐ **非 current 版本时所有写动作不渲染**：`BlueprintVersionSwitcher` emit 切版本后由页面渲染常驻 `Alert`「正在查看历史版本 v{n}，操作已禁用」+「回到当前版本」；本 plan 的写动作组件一律接受一个 `historicalMode` 语义（经 `readonly` 或独立 prop）并在其为真时不渲染"
    - "⭐ **block 级 diff 在前端算，零新端点**：`BlueprintBlockDiff` 消费 115-02 的 `classifyBlockDiff`（canonical 指纹）+ `diff` 包的 `diffWords`；`shallowRef<Change[]>` 存结果；默认单栏 inline、`mode: 'split'` 切左右并排；摘要行 `aria-live=\"polite\"`；⭐ **diff 模式下批注层与所有写动作关闭**"
    - "⭐ **`.diff-added` / `.diff-removed` / `.diff-unchanged` 三条 CSS 原样复制进本组件的 `<style scoped>`**（值逐字同 `PromptVersionDiff.vue:119-137`），⛔ 不 import（P-11）"
    - "组件内零 `v-html`、零颜色字面量、零 `refetchInterval`、零 `edit-block`；`defineEmits` 在 `components/blueprint/**` 内统一用元组简写"
    - "前端门：`cd web && pnpm exec vitest run` 全绿（含源码守卫 spec 复跑）、`pnpm lint` 通过、`pnpm type-check` 通过"
  artifacts:
    - path: "web/src/components/blueprint/BlueprintThreadSidebar.vue"
      provides: "四组 Collapsible（未决/已回答/已关闭/失锚）+ 组内 severity→created_at 排序 + kind 多选筛选 chips + 「显示已关闭批注」开关 + 两数据源合并（threads/ 为准、快照 orphaned_threads 占位）"
      contains: "sidebarGroups"
    - path: "web/src/components/blueprint/BlueprintThreadCard.vue"
      provides: "⭐ 按 kind 硬分流渲染动作（finding → FindingActions；其余 → Composer）+ 多轮消息列表 + options 候选选项 + 失锚说明与 quoted_text 快照 + 已提醒信息"
      contains: "ai_review_finding"
    - path: "web/src/components/blueprint/BlueprintThreadComposer.vue"
      provides: "作答输入框 + options 点选填入 + 提交态；⭐ 仅非 finding 且 !readonly 时被渲染"
      contains: "blueprint-thread-composer"
    - path: "web/src/components/blueprint/BlueprintFindingActions.vue"
      provides: "「已修复」/「误报忽略」两按钮 + 必填 reason 的受控 Dialog；⭐ 不受 readonly 闸约束"
      contains: "blueprint-finding-actions"
    - path: "web/src/components/blueprint/BlueprintSelectionPopover.vue"
      provides: "从 reka-ui 直接导入的 PopoverAnchor + 零尺寸虚拟锚点；「发起评论」（readonly 时不渲染）+「复制原文」；焦点不自动抢占、Esc 关闭保留选区"
      contains: "PopoverAnchor"
    - path: "web/src/components/blueprint/BlueprintReviewActions.vue"
      provides: "顶栏最右侧独立区块（Separator + ml-auto + pl-4 border-l）+ 通过/驳回两按钮 + disabled&Tooltip 由 current_status 驱动 + 通过走 useConfirmDialog"
      contains: "blueprint-review-approve"
    - path: "web/src/components/blueprint/BlueprintBlockedDialog.vue"
      provides: "approve 409 的解药面板：unresolved_blocker_thread_ids 逐条可点跳转（§20 断言 3）"
      contains: "goto-thread"
    - path: "web/src/components/blueprint/BlueprintQualityPanel.vue"
      provides: "四指标格；null → 「暂无数据」绝不显示 0；citation_coverage 恒百分比 + 「无关键结论」旁注（闭 MN-05）"
      contains: "noData"
    - path: "web/src/components/blueprint/BlueprintBlockDiff.vue"
      provides: "block 级 diff（classifyBlockDiff + diffWords + shallowRef）+ inline/split 两形态 + 按段分组与未变段折叠 + aria-live 摘要；.diff-* 三条 CSS 原样复制进 scoped style"
      contains: "diffWords"
    - path: "web/src/components/blueprint/__tests__/threadSidebar.spec.ts"
      provides: "§20 断言 1/2/5/11 + kind 四类分流正反 + readonly 三条并列 + reflow.status 五档 toast 分档"
      contains: "blueprint-thread-composer"
  key_links:
    - from: "web/src/components/blueprint/BlueprintThreadSidebar.vue"
      to: "web/src/utils/blueprintAnnotations.ts"
      via: "sidebarGroups(threads, orphanedThreads) 四组判据的唯一实现（组件不自写）"
      pattern: "sidebarGroups"
    - from: "web/src/components/blueprint/BlueprintThreadCard.vue"
      to: "web/src/components/blueprint/BlueprintFindingActions.vue"
      via: "kind === 'ai_review_finding' 时只渲染它、不渲染 Composer（CR-01 渲染层硬分流）"
      pattern: "ai_review_finding"
    - from: "web/src/components/blueprint/BlueprintBlockedDialog.vue"
      to: "web/src/components/blueprint/BlueprintThreadSidebar.vue"
      via: "goto-thread emit → 页面设 activeThreadId → 侧栏选中并滚动（超界死锁的唯一解药入口）"
      pattern: "goto-thread"
    - from: "web/src/components/blueprint/BlueprintVersionSwitcher.vue"
      to: "web/src/config/blueprintStatus.ts"
      via: "producedByReason(produced_by_ref) 五档映射（唯一判据）"
      pattern: "producedByReason"
    - from: "web/src/components/blueprint/BlueprintBlockDiff.vue"
      to: "web/src/utils/blueprintBlocks.ts"
      via: "classifyBlockDiff（canonical 指纹，避免键序差异被误判 modified）"
      pattern: "classifyBlockDiff"
---

<objective>
把**全部写路径与决策面**做成独立可测的组件，让 115-06 的页面只负责装配与接线：

① **线程侧栏与三条写路径（CLAR-01）** —— 四组分组、多轮消息、`options` 候选选项，以及本相位**最不能做错的一条**：⛔ **线程动作按 `kind` 硬分流做在渲染层**（114-REVIEW CR-01）。`ai_review_finding` 的卡片**压根不给回复框**，只给「已修复」/「误报忽略」两个各自必填 `reason` 的动作 —— 因为后端对 finding 走 `answer/` 通道**一律 400**，UI 若给统一输入框再按 kind 切端点，必然稳定撞 400 或误处置。

② **人审终审（FLOW-08）** —— 通过 / 驳回的防误触两层（视觉分离 + 二次确认），驳回 `comment` 必填非空；⭐ 以及 approve **409 的解药面板**：`unresolved_blocker_thread_ids` 必须渲染成**可点跳转**的处置入口。只显示一句「不可确认」等于把人锁死在超界出口里（114-05 原话）。

③ **质量面板** —— 闭 STATE 登记的 114 review **MN-05（三项统计零消费方）**。⛔ `null` 渲染「暂无数据」，**绝不显示 0**。

④ **版本切换与 block 级 diff（SC-2 后半句）** —— 版本轨复用既有 `getArtifactTimeline`（零新端点），diff 在前端算（零新端点），块内文本用 `diffWords`，颜色**原样复制** `PromptVersionDiff.vue` 的三条 scoped CSS。

Purpose: 这四组是「用户能对蓝图做什么」的全集（本相位写路径穷举为：澄清/评论作答、选区评论建线程、finding 处置 + 终审两动作 + 确认门七动作，后者归 115-07）。它们全部是独立组件、props/emits 契约清晰，因此可以与 115-03 在同一波次并行推进而零文件交集。
Output: 11 个新建组件 + 3 个新建测试文件。**零既有文件修改、零新增依赖、后端零改动。**
</objective>

<execution_context>
工作目录：worktree 根 `/Users/zaneliu/Projects/open-source/friday-ai/.claude/worktrees/v0.20-blueprint`。前端命令 `cd web && ...`，测试 `pnpm exec vitest run <path>`。

**wave 3，依赖 115-02**（与 115-03 同波次、**零文件交集**；`workflow.use_worktrees=false` ⇒ 实际串行执行，波次分组只表达依赖正确性）。以下**已存在**，⛔ 只 import 不重写：
- `~/utils/blueprintAnnotations` 的 `sidebarGroups(threads, orphanedThreads)`（⭐ **四组判据的唯一实现**）与 `groupThreadsByBlock`；
- `~/utils/blueprintBlocks` 的 `classifyBlockDiff` / `canonicalBlockFingerprint` / `iterBlocks` / `blockText` / `summaryText`；
- `~/components/blueprint/annotationTokens` 的 `annotationClass` / `ANNOTATION_PRIORITY`；
- `~/config/blueprintStatus` 的 `getBlueprintStatusConfig` / `isBlueprintEditable` / `producedByReason`；
- `~/api/blueprints` 的人审六个动作与 gate 相关函数；`~/api/deliveryArtifacts` 的 `getArtifactTimeline`（既有，⛔ 不重新封装）；
- i18n `knowledge.blueprints.thread.* / finding.* / review.* / quality.* / version.* / diff.* / annotation.* / error.*`（115-02 已写全）。

⚠️ **115-03 的产物在本 plan 中不可用**（同波次）：⛔ 本 plan 的组件**不得 import** `BlueprintBlock.vue` / `BlueprintBlockList.vue` / `BlueprintCitationChip.vue`。线程卡里的消息正文用**纯 `<p>` / `<pre>` mustache** 渲染即可（消息是纯文本不是 Block）；diff 组件渲染的是**块的纯文本**（经 `blockText`）而非富块 —— 这既是并行的前提，也符合 diff「纯对照视图」的语义。

**后端契约（逐字，不得猜测；来源 114-05-SUMMARY 与 115-01-SUMMARY）**：
- `POST .../blueprint-review/approve/`：200 `{current_status, ...}` / **409 `blocked`**（响应体含 `unresolved_blocker_thread_ids` 与 `unresolved_blocker_count`）/ 409 `conflict` / 400 `invalid`。
- `POST .../reject/`：入参 `{comment?, anchor?}`；200 返 `{version_no, revision_round, thread_id, current_status}` / 409 conflict（响应体带 `version_no`）/ 400。
- `POST .../threads/<id>/answer/`：入参 `{body}`；**恒 200**，响应体 `reflow: {status, version_id, version_no, conflict_block_ids, thread_id}`；⛔ 对 `kind == ai_review_finding` 一律 **400**（所以 UI 压根不给入口）。
- `POST .../threads/<id>/resolve/` 与 `/dismiss/`：入参 `{reason}`（**必填非空**，空 → 400）；200 `resolved`/`dismissed` / 200 `noop`（幂等，不覆盖首次结论）/ 400 `invalid`（理由空 或 kind 非 finding）/ 中性 404。
- `POST .../blueprint-review/threads/`（115-01 新增）：入参 `{body, anchor?}`；200 `{thread_id, current_status}` / 400（body 空 或蓝图不可编辑）/ 中性 404。
- `GET .../blueprint-review/`（快照）：含 `findings`（三级分组）/ `clarifications[]` / `comments[]` / `unresolved[]` / **`orphaned_threads[]`** / `unresolved_blocker_thread_ids` / `unresolved_blocker_count` / `review_round` / `revision_round` / `current_status`。
- `GET .../blueprint/`（115-01）：`quality` 四键，后三项 `number | null`。
- `deliveryArtifacts.getArtifactTimeline(artifactId)`：`versions[]` 含 `version_no` / `produced_by_ref` / `supersedes_id` / `is_current`。

**既有可复用件契约（实测）**：
- `~/composables/useConfirmDialog.ts`：`confirm({title, description, confirmText, cancelText, variant})` → `Promise<boolean>`，**全局单例状态**（`GlobalConfirmDialog.vue` 已在布局里）。⚠️ **需要输入框的场景不能用它**（驳回 / finding 处置）。
- `~/components/accessTokens/AccessTokenRevealDialog.vue`：受控 `Dialog` + 必填输入 + `update:open` 的形状 analog。
- `~/components/ui/popover`：`Popover` / `PopoverTrigger` / `PopoverContent`（⚠️ 该 barrel **只有这三个**，已核实）；⭐ 承接**虚拟锚点**的 `PopoverAnchor` 直接 `import { PopoverAnchor } from 'reka-ui'`。
- `~/components/ui/collapsible`：分组折叠（`aria-expanded` 由 reka-ui 提供）。
- `~/components/ui/scroll-area` / `sheet` / `tooltip` / `separator` / `select` / `textarea` / `switch`。
- `~/components/common/StatCard.vue`：质量面板四格的形状 analog。
- `~/components/delivery/HumanTaskInbox.vue`：分组收件箱 + `hide-when-empty` prop 的 analog。
- `~/components/prompts/PromptVersionDiff.vue`（137 行）：`import type { Change } from 'diff'` + `import { diffLines } from 'diff'`（`:17,19`）；`shallowRef<Change[]>`（`:20,28`）；`aria-live="polite"` 摘要行（`:74-81`）；`data-diff-column` 测试锚点（`:85`）；左右栏 `filter(c => !c.added)` / `filter(c => !c.removed)`；⭐ **`.diff-added` / `.diff-removed` / `.diff-unchanged` 在 `<style scoped>`（`:119-137`）无法跨组件复用**；⚠️ 其 `watch(..., { deep: true })`（`:30-36`）是既有的自相矛盾，⛔ 不照抄。
- `diff` 包：`diffWords` 与 `diffLines` 同包同族，返回 `Change[]`（`{value, added?, removed?, count}`）—— `[ASSUMED]` A3，**本 plan 的 diff 测试真实 import 不 mock**，跑绿即 settle。
- `~/components/ui/badge`：八 variant；⛔ 禁止 `:class` 追加颜色类。

**测试范式**：`components/prompts/__tests__/PromptVersionDiff.test.ts`（113 行，最规范：覆盖路径编号清单 + 工厂 + 正/负向成对 + `data-*` 定位 + 真实 import 不 mock）；`pages/knowledge/__tests__/entity-detail.spec.ts`（68 行：`vi.mock` + **手写最小 i18n 键树**（⛔ 不 import `zh-CN.json`）+ `stubs`）。setup 只有 storage 垫片。
</execution_context>

<context>
@.planning/phases/115-ui/115-02-SUMMARY.md（`sidebarGroups` / `classifyBlockDiff` / `annotationClass` / `producedByReason` / `isBlueprintEditable` 的**逐字签名**；i18n 键清单；A2 能力锁结论）
@.planning/phases/115-ui/115-UI-SPEC.md §7.7（**侧栏四组判据表 + 两数据源合并规则 + 组内排序 + 顶部工具条**逐字）、§7.8（⛔ **kind 硬分流表逐字** + `reason` 弹窗规格）、§7.9（`readonly` 白名单与「不存在于 DOM」的三条穷举 + finding 处置不受约束）、§8.2（**状态码分档表逐行** + answer 端点的 `reflow.status` 五档 toast）、§9.1（版本原因四前缀表 + 历史版本 Alert）、§9.2（**diff 呈现表逐行**，含颜色逐字与「diff 模式下批注层与写动作关闭」）、§11.1（终审操作区的视觉分离规格 + 两层防误触 + 成功 toast 文案）、§11.2（**质量面板四格表 + 「无关键结论」旁注**）、§16（Copywriting Contract：四条破坏性确认全文、409 文案、只读提示、历史版本提示）、§18.1/§18.2（键盘导航与焦点管理）、§20（断言 1/2/3/5/7/11）
@.planning/phases/115-ui/115-RESEARCH.md（§B.4 `PromptVersionDiff` 契约实读与 `diffWords` 的 A3 假设、§B.5 TanStack Query invalidate 惯例、§B.10 测试可达上界；P-7 / P-11）
@.planning/phases/115-ui/115-PATTERNS.md（§E 批注与线程的三个 analog 与 ⭐ **三条「必须 DIFFER」**、§F1–F8 版本/diff/人审/确认门的 analog 表、§F2 的四条 DIFFER）
@.planning/phases/114-ai/114-05-SUMMARY.md（**七端点契约表为权威**：入参键 / 全部状态码映射 / answer 的 `reflow` 响应键 / approve 409 响应体的未决清单形状 / `add_reviewer` 的 `first_action` 取值全集）
@.planning/STATE.md（114 review 的 CR-01 / MJ-01 / MJ-02 / MJ-04 四条决策原文——本 plan 是它们在 UI 上的对称面）
@web/src/components/prompts/PromptVersionDiff.vue（**全文 137 行**：`shallowRef<Change[]>` / `aria-live` 摘要 / `data-diff-column` / **`.diff-*` scoped CSS 的确切值** / ⚠️ 不照抄的 `deep: true`）
@web/src/composables/useConfirmDialog.ts（`confirm(...)` 的逐字签名与全局单例语义）
@web/src/components/accessTokens/AccessTokenRevealDialog.vue（受控 Dialog + 必填输入 + `update:open` 的 analog）
@web/src/components/common/StatCard.vue（质量四格的形状 analog）
@web/src/components/delivery/HumanTaskInbox.vue（分组收件箱 analog）
@web/src/components/prompts/__tests__/PromptVersionDiff.test.ts（**全文 113 行**：组件测试最规范范式）
</context>

<tasks>

<task type="auto">
  <name>Task 1: 线程侧栏四件 + 选区 popover（⭐ kind 硬分流 / readonly 不渲染 / 失锚不二次过滤三条硬约束）</name>
  <files>web/src/components/blueprint/BlueprintThreadSidebar.vue, web/src/components/blueprint/BlueprintThreadCard.vue, web/src/components/blueprint/BlueprintThreadComposer.vue, web/src/components/blueprint/BlueprintFindingActions.vue, web/src/components/blueprint/BlueprintSelectionPopover.vue</files>
  <read_first>.planning/phases/115-ui/115-UI-SPEC.md §7.7 / §7.8 / §7.9（**三节逐字**：四组判据与两数据源合并、kind 硬分流表、readonly 的三条穷举与 finding 例外）、§16（thread / finding 相关文案）、§18.1（侧栏键盘导航：`role="complementary"` / 线程卡是 `<button>` / `↑`/`↓` 同组移动焦点 / `Esc` 清 `activeThreadId` 不关侧栏）；.planning/phases/115-ui/115-02-SUMMARY.md（`sidebarGroups` 的**返回结构逐字** + `annotationClass` 签名 + `isBlueprintEditable` 入参是 status 字符串）；.planning/phases/114-ai/114-05-SUMMARY.md（`answer` / `resolve` / `dismiss` 三端点的**入参键与全部状态码**，含 `reflow` 五档与 `noop` 幂等语义）；web/src/components/delivery/HumanTaskInbox.vue（分组收件箱 + `hide-when-empty` prop 的 analog）；web/src/components/accessTokens/AccessTokenRevealDialog.vue（**受控 Dialog + 必填输入 + `update:open`** 的逐字 analog——`BlueprintFindingActions` 内嵌的 reason 弹窗照它）；web/src/components/feedback/MarkdownField.vue（`Textarea` + `defineEmits<{}>` 的 analog）；web/src/components/ui/collapsible/index.ts 与 web/src/components/ui/popover/index.ts（**导出的子组件名**；⚠️ 已核实 popover barrel **只导出 `Popover` / `PopoverContent` / `PopoverTrigger`**，⛔ 不要给它补 `PopoverAnchor` 导出——那是第七处既有文件修改。`PopoverAnchor` 直接从 `reka-ui` 导入，本地 wrapper 自身就是这么做的：web/src/components/ui/popover/Popover.vue **L1-12** 与 PopoverContent.vue **L1-15**）；web/src/components/knowledge/EntityRelationTree.vue（行卡 + `defineEmits<{}>` 元组简写的 analog）</read_first>
  <action>
**① `BlueprintThreadComposer.vue`**（先建，被 Card 条件渲染）：
`props: { options?: Array<{label, value, note?}>, submitting?: boolean, placeholder?: string }`；`emits: { submit: [body: string] }`。
`Textarea` + 提交按钮；`options` 非空时渲染为**可点选项组**（点选即把 `value`/`label` 填入输入框，**仍可改写后再提交**）；空/纯空格 ⇒ 提交 `disabled`。根元素 `data-testid="blueprint-thread-composer"`。
⚠️ `options` 是 `JSONField(default=list)` **无 schema 校验** ⇒ 逐项 `?.label ?? ''` 防御。

**② `BlueprintFindingActions.vue`**：
`props: { threadId: string, submitting?: boolean }`；`emits: { resolve: [threadId: string, reason: string], dismiss: [threadId: string, reason: string] }`。
两个按钮「已修复」/「误报忽略」（文案 `t('knowledge.blueprints.finding.resolve'|'.dismiss')`）；点任一 ⇒ 打开**内嵌的受控 `Dialog`**（形状照 `AccessTokenRevealDialog.vue`，⛔ **不用 `useConfirmDialog`** —— 它没有输入框），内含 `Textarea`（`reason`）+ 「确认」按钮。
⭐ **`reason` 必填**：空/纯空格 ⇒ 确认按钮 `disabled` + 输入框下方 `text-destructive` 内联提示 `t('knowledge.blueprints.finding.reasonRequired')`。
⛔ **前端不自行拼装结论文本** —— 后端会写成 `[已修复|误报忽略] {reason}（处置人：{uid}）`，提交成功后重取线程即可看到该条消息。
根元素 `data-testid="blueprint-finding-actions"`；打开时初始焦点落在 `Textarea`（reka-ui 的 `@open-auto-focus`）。

**③ `BlueprintThreadCard.vue`（⭐ CR-01 的落点）**：
`props: { thread: BlueprintThreadDetail, active: boolean, readonly: boolean, submitting?: boolean }`；`emits: { select: [threadId], answer: [threadId, body], resolve: [threadId, reason], dismiss: [threadId, reason] }`。
卡片本体是 `<button>`（`data-testid="blueprint-thread-card"`，点击 emit `select`），内含：
- 头部：`kind` 徽标 + `severity` 徽标 + `status` 徽标（颜色一律经 `annotationClass` 或 `Badge variant`，⛔ 零颜色字面量）+ `created_at` 相对时间；
- ⭐ **失锚条目**（`anchor_status === 'orphaned'`）：展示 `anchor.quoted_text` 快照 + 一行说明 `t('knowledge.blueprints.annotation.orphaned')` =「原文已变更，无法定位」；**且仍可回复 / 处置**（⛔ 不 disable 动作）；
- ⭐ **越界降级条目**：附一行小字 `t('knowledge.blueprints.annotation.degraded')` =「无法精确定位到原文片段，已标注整块」（判据由父层传入或用 115-02 的 `isValidAnchor` 就地判）；
- 多轮消息列表：`messages[]` 逐条渲染 `author_display`（**可能为空串**，`author` 是 `SET_NULL` FK）+ `author_type` 徽标 + `body`（**纯 mustache/`<pre>`，⛔ 禁 `v-html`**）+ 时间；
- 澄清线程附「已提醒 N 次 / 上次提醒时间」（`last_reminded_at` 非空时；⚠️ 后端只给 `last_reminded_at` 不给次数 ⇒ 文案退化为「上次提醒：{时间}」并在 SUMMARY 登记这处与 UI-SPEC §0.2 判定 4 的差异）；
- `repo_confirmation` 追加一条「前往确认门」链接（emit 或 `#gate` 锚点，目标面板归 115-07；⭐ **面板缺席时该链接不渲染** —— 由父层传入 `gateAvailable` prop 控制）。

⭐ **动作区按 `kind` 硬分流（渲染层，本 plan 头号靶子）**：
- `kind === 'ai_review_finding'` ⇒ **只渲染** `<BlueprintFindingActions>`；⛔ **`<BlueprintThreadComposer>` 根本不出现在模板的这条分支里**（不是 `v-show`、不是 `disabled`）。⭐ **且不受 `readonly` 约束**（§7.9 末条：后端未对 resolve/dismiss 加状态闸，那是死锁出口）。
- `kind ∈ {ai_clarification, human_comment, repo_confirmation}` ⇒ 渲染 `<BlueprintThreadComposer v-if="!readonly" :options="thread.options" @submit="...">`（⭐ **`readonly` 时「不存在于 DOM」**）；`ai_clarification` 传 `thread.options`。
- 在 `<script setup>` 里用一个 `const isFinding = computed(() => props.thread.kind === 'ai_review_finding')` 并在模板用 `v-if="isFinding"` / `v-else`，**两条分支物理互斥**。docstring 逐字写明理由：「114 对 finding 走 `answer` 通道**一律 400**，且回灌链 `REFLOW_KINDS` fail-closed 过滤。UI 若给统一输入框再按 kind 切端点，必然稳定撞 400 或误处置 ⇒ 分流做在**渲染层**（压根不给错的入口）而非提交层」。

**④ `BlueprintThreadSidebar.vue`**：
`props: { threads: BlueprintThreadDetail[], orphanedThreads: unknown[], activeThreadId: string | null, readonly: boolean, showClosed: boolean, kindFilters: string[], gateAvailable?: boolean, submitting?: boolean }`；`emits: { select, answer, resolve, dismiss, 'create-comment', 'update:kindFilters', 'update:showClosed' }`。
- ⭐ **四组一律调 115-02 的 `sidebarGroups(threads, orphanedThreads)`**，⛔ 组件内**不自写**四组判据（漏掉 `&& anchor_status !== 'orphaned'` 会让失锚线程出现两次、计数重复）。四组用 `~/components/ui/collapsible`，组头带计数；默认展开/展开/折叠/折叠；失锚组计数 > 0 时组头标 `variant="warning"`。
- ⭐ **两数据源合并规则（§7.7）**：前三组来自 `threads/`（带多轮消息与 `options`），第四组来自快照的 `orphaned_threads`；两处对同一 `thread_id` 的字段**以 `threads/` 为准**（它更全），仅在 `threads/` 尚未就绪时用快照条目占位渲染。
- ⭐ **`orphaned_threads` 直接渲染，⛔ 前端不再二次过滤**（114-REVIEW MJ-02 已保证里面只有真失锚；再过滤只会把真失锚也滤掉）。
- 组内排序：severity（`blocker` → `warning` → `info` → 无）→ `created_at` 升序（由 `sidebarGroups` 提供，组件不重排）。
- 顶部工具条：`kind` 多选筛选 chips（AI 提问 / AI 审查 / 人工评论 / 确认门）+「显示已关闭批注」`Switch`（附一行 `text-xs text-muted-foreground`「已关闭的批注以灰色点线标注」）。
- 空态：`CompactEmptyState`（⭐ **`icon` 传裸名**，按钮放**默认 slot**——该组件无 `action-label` prop 也无 `@action` emit，P-6）；文案「暂无批注 / AI 的划线提问与你的评论都会出现在这里；选中正文任意片段即可发起评论」；失锚组空态「没有失锚批注」。
- a11y（§18.1）：根 `role="complementary"` + `aria-label`；线程卡是 `<button>`；`↑`/`↓` 在同组内移动焦点；`Esc` 清 `activeThreadId`（**不关闭侧栏**）。
- ⭐ **草稿线程卡**：`create-comment` 的入口——父层传入选区草稿时，侧栏顶部插入一张草稿卡（`Textarea` 聚焦 + 引文预览 + 提交/取消）；`readonly` 时该草稿卡**不渲染**。

**⑤ `BlueprintSelectionPopover.vue`**：
`props: { rect: DOMRect | null, canComment: boolean }`；`emits: { comment: [], copy: [], dismiss: [] }`。
⭐ **实现方式锁死（115-02 定夺）**：`~/components/ui/popover` 的 `Popover` + **`import { PopoverAnchor } from 'reka-ui'`** 包一个 `position: fixed` 的**零尺寸 div**（`:style="{ top: rect.top + 'px', left: rect.left + 'px', width: rect.width + 'px', height: rect.height + 'px' }"`，`pointer-events: none`）+ `PopoverContent`（`side="top"`、`:side-offset="8"`）。
⚠️ **导入路径已定夺，⛔ 不要试图从 `~/components/ui/popover` 拿 `PopoverAnchor`**（plan-checker C1：该 barrel 只导出三个，补导出 = 第七处既有文件修改）。从 `reka-ui` 直接导入与仓内 wrapper 的写法同源（`Popover.vue:2` 就是 `import { PopoverRoot, useForwardPropsEmits } from 'reka-ui'`）。
⛔ **不引入 `@floating-ui/vue`、不手写 `useFloating`**（全仓零调用点、API 形状 `[ASSUMED]`）。⚠️ 唯一允许的回退：若 `reka-ui@^2.9.10` **未导出 `PopoverAnchor`**（`pnpm type-check` 会立刻报错），改用 `PopoverTrigger` 包同一个零尺寸 div（视觉等价），并在 SUMMARY 登记该回退。
内容：`canComment === true` 时渲染「发起评论」按钮（`data-testid="blueprint-selection-comment"`）+「复制原文」；⭐ **`canComment === false`（即 `readonly`）时「发起评论」按钮不渲染**，只留「复制原文」（§7.9）。
a11y（§18.1）：出现后焦点**不自动抢占**（会打断选区）；`Tab` 可进入；`Esc` 关闭并**保留选区**；按钮触控目标 ≥44px；焦点环用不透明 `--color-primary-600`。
  </action>
  <verify>
    <automated>cd web && pnpm type-check && pnpm lint && pnpm exec vitest run src/__tests__/blueprint-source-guard.spec.ts</automated>
  </verify>
  <acceptance_criteria>
- `cd web && pnpm type-check` 与 `pnpm lint` 通过。
- ⭐ **kind 硬分流在渲染层（源码级）**：`cd web && node -e "const s=require('fs').readFileSync('src/components/blueprint/BlueprintThreadCard.vue','utf8'); const tpl=s.slice(s.indexOf('<template>'), s.indexOf('</template>')); if(!/v-if=\"isFinding\"|v-if=\"kind === 'ai_review_finding'\"/.test(tpl)) throw new Error('缺 finding 分支的 v-if'); const compIdx=tpl.indexOf('BlueprintThreadComposer'); const findIdx=tpl.indexOf('BlueprintFindingActions'); if(compIdx<0||findIdx<0) throw new Error('两个动作组件必须都出现'); if(/v-show/.test(tpl)) throw new Error('分流必须用 v-if 不是 v-show'); console.log('kind hard-split OK')"` 输出 `kind hard-split OK`。
- ⭐ **readonly 用 v-if 不是 disabled**：`cd web && node -e "const s=require('fs').readFileSync('src/components/blueprint/BlueprintThreadCard.vue','utf8'); if(!/BlueprintThreadComposer[\s\S]{0,200}v-if=\"!readonly\"|v-if=\"!readonly\"[\s\S]{0,200}BlueprintThreadComposer/.test(s)) throw new Error('composer 未按 !readonly 做 v-if'); if(/BlueprintThreadComposer[^>]*:disabled=\"readonly\"/.test(s)) throw new Error('composer 用了 disabled 而非不渲染'); console.log('readonly v-if OK')"` 输出 `readonly v-if OK`。
- ⭐ **finding 处置不受 readonly 约束**（plan-checker C2 重写：原写法取「首次出现 `BlueprintFindingActions` 处 ±300 字符」，显式 import 时首次命中落在 import 块 ⇒ 恒真；自动注册时相邻的 Composer 分支又常落进 300 字符窗 ⇒ 假阳性。**照抄同侧 `kind hard-split OK` 的做法：先切到 `<template>` 段**）：`cd web && node -e "const s=require('fs').readFileSync('src/components/blueprint/BlueprintThreadCard.vue','utf8'); const tpl=s.slice(s.indexOf('<template>'), s.indexOf('</template>')); const i=tpl.indexOf('BlueprintFindingActions'); if(i<0) throw new Error('模板里没有 BlueprintFindingActions'); const tag=tpl.slice(tpl.lastIndexOf('<', i), tpl.indexOf('>', i)+1); if(/v-if=\"!readonly\"|v-if=\"!props.readonly\"/.test(tag)) throw new Error('finding 处置被 readonly 关掉了（§7.9 末条）'); console.log('finding not gated OK')"` 输出 `finding not gated OK`。⚠️ 判据收窄到**该组件自己那一个标签内**（`<` 到 `>`），⛔ 不再扫周边窗口。真正的防线仍是 `threadSidebar.spec` 断言 2c（`readonly: true` 时 `blueprint-finding-actions` 仍存在），这条只是源码级的早期兜底。
- ⭐ **四组不自写判据**：`rg -n "sidebarGroups" web/src/components/blueprint/BlueprintThreadSidebar.vue` 命中；`rg -n "anchor_status !== 'orphaned'|=== 'orphaned'" web/src/components/blueprint/BlueprintThreadSidebar.vue` **零命中**（判据在 115-02 的纯函数里）。
- ⭐ **失锚不二次过滤**：`cd web && node -e "const s=require('fs').readFileSync('src/components/blueprint/BlueprintThreadSidebar.vue','utf8'); if(/orphanedThreads[\s\S]{0,80}\.filter\(/.test(s)) throw new Error('对 orphanedThreads 做了二次过滤（MJ-02 / §20 断言 5）'); console.log('no orphan re-filter OK')"` 输出 `no orphan re-filter OK`。
- ⭐ **不引入 floating-ui**：`rg -n "@floating-ui/vue|useFloating" web/src/components/blueprint/` **零命中**；`rg -n "PopoverAnchor|PopoverTrigger" web/src/components/blueprint/BlueprintSelectionPopover.vue` 命中。
- ⭐ **`reason` / `body` 必填**：`rg -n "reasonRequired|disabled" web/src/components/blueprint/BlueprintFindingActions.vue` 命中；`rg -n "trim\(\)" web/src/components/blueprint/BlueprintFindingActions.vue web/src/components/blueprint/BlueprintThreadComposer.vue` 两文件各命中。
- ⭐ **不拼装结论文本**：`rg -n "已修复\]|误报忽略\]|处置人" web/src/components/blueprint/` **零命中**。
- `data-testid` 齐备：`rg -n "blueprint-thread-card|blueprint-thread-composer|blueprint-finding-actions|blueprint-selection-comment" web/src/components/blueprint/` 四个全命中。
- ⛔ 零 `v-html`、零颜色字面量、零 `refetchInterval`、零 `edit-block`：`rg -n "v-html|hsl\(|refetchInterval|edit-block" web/src/components/blueprint/BlueprintThread*.vue web/src/components/blueprint/BlueprintFindingActions.vue web/src/components/blueprint/BlueprintSelectionPopover.vue` **零命中**。
- `pnpm exec vitest run src/__tests__/blueprint-source-guard.spec.ts` 绿。
- ⛔ 零既有文件改动：`git diff --name-only web/src | rg -v "^web/src/components/blueprint/"` 输出**为空**。
  </acceptance_criteria>
  <done>侧栏四组一律经 115-02 的纯函数、失锚组直接渲染不二次过滤；线程卡按 `kind` 在**渲染层**物理分流（finding 卡里根本没有 Composer 这个节点）；`readonly` 是不渲染而 finding 处置不受其约束；选区 popover 用 reka-ui 虚拟锚点、`readonly` 时只留「复制原文」。</done>
</task>

<task type="auto">
  <name>Task 2: 人审终审四件（ReviewActions 防误触两层 / RejectDialog comment 必填 / ⭐ BlockedDialog 可点清单 / QualityPanel 三态）</name>
  <files>web/src/components/blueprint/BlueprintReviewActions.vue, web/src/components/blueprint/BlueprintRejectDialog.vue, web/src/components/blueprint/BlueprintBlockedDialog.vue, web/src/components/blueprint/BlueprintQualityPanel.vue</files>
  <read_first>.planning/phases/115-ui/115-UI-SPEC.md §11.1（**终审操作区的视觉分离规格、两层防误触、disabled+Tooltip 的理由、成功 toast 文案**逐字）、§11.2（**质量面板四格表 + `null` 三态 + 「无关键结论」旁注**逐字）、§8.2（409 三档与 400 的处理表）、§16（**四条破坏性确认全文逐字**：通过 / 驳回 / 移除仓 / 确认锁定，本 Task 用前两条）；.planning/phases/114-ai/114-05-SUMMARY.md（approve/reject 的**全部状态码与响应体键**，尤其 409 blocked 的 `unresolved_blocker_thread_ids` 形状与 reject 409 的 `version_no`）；web/src/composables/useConfirmDialog.ts **全文**（`confirm(...)` 签名与全局单例；⚠️ **驳回不能用它** —— 需要输入框）；web/src/components/accessTokens/AccessTokenRevealDialog.vue **全文**（受控 Dialog + 必填输入 + `update:open` 的 analog）；web/src/pages/specs/[id].vue 的状态流转动作区（**顶栏动作区 + 状态驱动可用性**的 analog，逐字看它怎么摆按钮与 Tooltip）；web/src/components/common/StatCard.vue **全文**（质量四格 analog）；web/src/components/ui/tooltip/index.ts 与 separator/index.ts（导出的子组件名）</read_first>
  <action>
**① `BlueprintReviewActions.vue`**：
`props: { currentStatus: string, revisionRound: number, submitting?: boolean }`；`emits: { approve: [], reject: [] }`。
⭐ **防误触第一层 = 视觉分离**（§11.1 逐字）：顶栏**最右侧**独立区块，与左侧阅读/编辑动作之间用 `~/components/ui/separator` + `ml-auto` 隔开，容器 `pl-4 border-l border-border`。
两个按钮：通过 `<Button variant="default">` + `icon-[lucide--check-circle]` + 文案「通过方案」（`data-testid="blueprint-review-approve"`）；驳回 `<Button variant="destructive">` + `icon-[lucide--undo-2]` + 文案「驳回修订」（`data-testid="blueprint-review-reject"`）。
⭐ **可用性由 `current_status` 驱动**：仅 `pending_review` 可用；否则**两者 `disabled` 并用 `Tooltip` 给出原因** `t('knowledge.blueprints.review.disabledReason', { status: 状态中文名 })`（状态中文名经 `getBlueprintStatusConfig(status).labelKey` + `t()`）。
⚠️ **这里是 `disabled` + tooltip，与 §7.9 的「不渲染」刻意不同** —— 终审按钮的存在本身有信息量（告诉人「这里将来能做什么」），而可编辑闸拦的是会撞 400 的写路径。docstring 写明这条对照。
⭐ **防误触第二层 · 通过**：走 `useConfirmDialog().confirm({...})`，四个字段**逐字照 §16**（标题「确认通过该技术方案？」/ 正文「通过后蓝图状态变为「已确认」，你将被记入本方案的评审人名单，且蓝图不可再直接改写。」/ 按钮「确认通过」/ `variant: 'default'`）；确认后才 emit `approve`。
**驳回**：⛔ **不走 `useConfirmDialog`**（需要输入框）—— 直接 emit `reject`，由父层打开 `BlueprintRejectDialog`。

**② `BlueprintRejectDialog.vue`**：
`props: { open: boolean, revisionRound: number, presetAnchor?: { blockId, startOffset, endOffset, quotedText } | null, submitting?: boolean }`；`emits: { 'update:open': [boolean], submit: [payload: { comment: string, anchor?: ... }] }`。
受控 `Dialog`（形状照 `AccessTokenRevealDialog.vue`），`DialogTitle` 必填（reka-ui a11y）。
- ⭐ **`comment` 必填非空**：`Textarea`，空/纯空格 ⇒ 提交按钮 `disabled` + 内联 `text-destructive` 提示；
- `presetAnchor` 存在时，弹窗顶部显示**引文预览**与「保留此划线」`Switch`（勾选则带 `anchor` 提交）；
- 底部常驻提示 `t('knowledge.blueprints.review.rejectHint', { round: revisionRound + 1 })` =「驳回后蓝图回到『产出中』，修订轮次将变为 {n}」；
- 主按钮 `variant="destructive"`，文案「确认驳回」；打开时初始焦点落在 `Textarea`。
`data-testid="blueprint-reject-dialog"`。

**③ `BlueprintBlockedDialog.vue`（⭐ §20 断言 3，本 Task 头号靶子）**：
`props: { open: boolean, threadIds: string[], threads: BlueprintThreadDetail[] }`；`emits: { 'update:open': [boolean], 'goto-thread': [threadId: string] }`。
受控 `Dialog`，`data-testid="blueprint-blocked-dialog"`。内容：
- 一句说明 `t('knowledge.blueprints.error.blocked', { n: threadIds.length })` =「还有 {n} 条阻塞级审查发现未处置，处置完成后即可通过」；
- ⭐ **`threadIds` 逐条渲染成可点击条目**（`<button>`，`data-testid="blueprint-blocked-item"`）：severity 点 + finding 首行摘要（从 `threads` 里按 id 找 `messages[0].body` 前 40 字；**找不到时回落显示 `thread_id` 前 8 位**，⛔ **绝不因为找不到就跳过该条** —— 条目数必须等于 `threadIds.length`）；点击 ⇒ emit `goto-thread(id)` 并关闭 Dialog（父层负责：打开侧栏 → 选中该线程 → 正文滚动定位）。
- ⛔ **只显示一句「不可确认」即视为不合格** —— 那份清单是超界死锁的唯一解药入口（114-05 原话）。docstring 逐字写明这条。

**④ `BlueprintQualityPanel.vue`（闭 MN-05）**：
`props: { quality: { citation_coverage: number, ai_rejection_rate: number | null, human_edit_volume: number | null, clarification_rounds: number | null }, hasKeyConclusions: boolean }`。
四个指标格（形状照 `StatCard.vue`），`data-testid="blueprint-quality-panel"`：
- **引用覆盖率**：`{(v * 100).toFixed(1)}%`，**恒有值**（后端分母为 0 时返 `1.0`）；⭐ `hasKeyConclusions === false` 时追加一枚 `<Badge variant="outline">无关键结论</Badge>`（§11.2：`current_state_analysis` / `repo_associations` / `impact_analysis` 三处**全空**时显示 100% 是已知口径陷阱，避免把「没内容」读成「证据齐备」）；
- **AI 打回率** / **人审修改量** / **澄清轮次**：⭐ `null` ⇒ 渲染 `t('knowledge.blueprints.quality.noData')` =「暂无数据」（`text-muted-foreground`）；有值 ⇒ `text-2xl font-semibold text-foreground` 的具体值（`{(v*100).toFixed(1)}%` / `{v} 次人工编辑` / `{v} 轮`）。
- ⛔ **绝不 `v ?? 0`**（§20 断言 7 专门逮这个形状：`v ?? 0` 会让 `null` 用例转红而 `0` 用例仍绿）。在 `<script setup>` 里用 `const fmt = (v: number | null, f: (n: number) => string) => (v === null || v === undefined ? null : f(v))`，模板按 `=== null` 分支。
docstring 写明：「本面板是 `blueprint_quality` 三项 DB 统计的**唯一消费面**（闭 114-REVIEW MN-05：口径已实装但全仓零消费方；`evaluate_blueprint_golden` 接不上——golden case 是静态 JSON fixture 无 `artifact_id`）」。
  </action>
  <verify>
    <automated>cd web && pnpm type-check && pnpm lint</automated>
  </verify>
  <acceptance_criteria>
- `cd web && pnpm type-check` 与 `pnpm lint` 通过。
- ⭐ **终审是 disabled + Tooltip 不是不渲染**：`cd web && node -e "const s=require('fs').readFileSync('src/components/blueprint/BlueprintReviewActions.vue','utf8'); if(!/:disabled=/.test(s)) throw new Error('终审按钮必须用 disabled'); if(!/Tooltip/.test(s)) throw new Error('缺 Tooltip 说明原因'); const tpl=s.slice(s.indexOf('<template>')); if(/v-if=\"[^\"]*pending_review/.test(tpl)) throw new Error('终审按钮被 v-if 掉了（应为 disabled，§11.1）'); console.log('review actions gating OK')"` 输出 `review actions gating OK`。
- ⭐ **通过走 useConfirmDialog、驳回不走**：`rg -n "useConfirmDialog" web/src/components/blueprint/BlueprintReviewActions.vue` 命中；`rg -n "useConfirmDialog" web/src/components/blueprint/BlueprintRejectDialog.vue` **零命中**。
- ⭐ **确认文案逐字**：`rg -n "确认通过该技术方案|你将被记入本方案的评审人名单" web/src/components/blueprint/BlueprintReviewActions.vue web/src/locales/zh-CN.json` 命中（文案在 i18n，组件传 key —— 二者至少一处命中且组件不硬编码中文，人工核对）。
- ⭐ **`comment` 必填**：`rg -n "trim\(\)" web/src/components/blueprint/BlueprintRejectDialog.vue` 命中；`rg -n "text-destructive" web/src/components/blueprint/BlueprintRejectDialog.vue` 命中。
- ⭐ **409 可点清单（断言 3 的源码级前置）**：`cd web && node -e "const s=require('fs').readFileSync('src/components/blueprint/BlueprintBlockedDialog.vue','utf8'); if(!/v-for/.test(s)) throw new Error('未逐条渲染 unresolved_blocker_thread_ids'); if(!/goto-thread/.test(s)) throw new Error('缺 goto-thread emit'); if(!/blueprint-blocked-item/.test(s)) throw new Error('缺条目 data-testid'); console.log('blocked list OK')"` 输出 `blocked list OK`。
- ⭐ **质量面板绝不 `?? 0`**：`cd web && node -e "const s=require('fs').readFileSync('src/components/blueprint/BlueprintQualityPanel.vue','utf8'); if(/\?\?\s*0\b|\|\|\s*0\b/.test(s)) throw new Error('出现 ?? 0 / || 0（§20 断言 7）'); if(!/=== null|== null|isNil|noData/.test(s)) throw new Error('缺 null 分支'); console.log('quality null-safe OK')"` 输出 `quality null-safe OK`。
- ⭐ **「无关键结论」旁注存在**：`rg -n "hasKeyConclusions|noKeyConclusion" web/src/components/blueprint/BlueprintQualityPanel.vue` 命中。
- `data-testid` 齐备：`rg -n "blueprint-review-approve|blueprint-review-reject|blueprint-blocked-dialog|blueprint-blocked-item|blueprint-quality-panel|blueprint-reject-dialog" web/src/components/blueprint/` 六个全命中。
- ⛔ 零 `v-html`、零颜色字面量、零 `refetchInterval`、零 `edit-block`：`rg -n "v-html|hsl\(|refetchInterval|edit-block" web/src/components/blueprint/BlueprintReviewActions.vue web/src/components/blueprint/BlueprintRejectDialog.vue web/src/components/blueprint/BlueprintBlockedDialog.vue web/src/components/blueprint/BlueprintQualityPanel.vue` **零命中**。
- ⛔ 零既有文件改动：`git diff --name-only web/src | rg -v "^web/src/components/blueprint/"` 输出**为空**。
  </acceptance_criteria>
  <done>终审两按钮视觉分离 + `disabled`+Tooltip 由状态驱动 + 通过走二次确认；驳回走受控 Dialog 且 `comment` 必填非空、带修订轮次预告与可选划线保留；approve 409 的未决清单逐条可点跳转（不是一句「不可确认」）；质量面板三态正确、`null` 绝不显示 0 且有「无关键结论」旁注。</done>
</task>

<task type="auto">
  <name>Task 3: 版本切换器 + block 级 diff（.diff-* 复制进 scoped style）+ 三个组件测试（§20 断言 1/2/3/5/7/11）</name>
  <files>web/src/components/blueprint/BlueprintVersionSwitcher.vue, web/src/components/blueprint/BlueprintBlockDiff.vue, web/src/components/blueprint/__tests__/threadSidebar.spec.ts, web/src/components/blueprint/__tests__/reviewActions.spec.ts, web/src/components/blueprint/__tests__/blockDiff.spec.ts</files>
  <read_first>web/src/components/prompts/PromptVersionDiff.vue **全文 137 行**（⭐ 逐字级 analog：L1-20 模块 docstring 的「安全 / 性能」两段、L17-20 的 `import type { Change } from 'diff'` + `shallowRef`、L30-36 的 ⚠️ **不照抄的 `deep: true`**、L74-81 的 `aria-live="polite"` 摘要行、L85 的 `data-diff-column`、L90-97 的 `v-for` + `<pre>` 类串、**L119-137 的 `.diff-added` / `.diff-removed` / `.diff-unchanged` 三条 CSS 的确切值**）；web/src/components/prompts/__tests__/PromptVersionDiff.test.ts **全文 113 行**（⭐ 组件测试最规范范式：覆盖路径编号清单 / `makeVersion` 工厂 / 正负成对断言 / `findAll('[data-diff-column]')` / `setProps` 验 watch / **真实 import `diff` 不 mock**）；.planning/phases/115-ui/115-UI-SPEC.md §9.1（版本原因四前缀表 + 历史版本 Alert + 「所有写动作在此模式下不渲染」）、§9.2（**diff 呈现表逐行**：默认 inline / split 切换 / 颜色 / 摘要 / 段分组 / 性能 / 安全 / **diff 模式下批注层与所有写动作关闭**）、§20（断言 1/2/3/5/7/11 的**变异说明**——用例要按变异写成能转红的形态）；.planning/phases/115-ui/115-02-SUMMARY.md（`classifyBlockDiff` 的**返回结构**与 `producedByReason` 五档）；web/src/components/knowledge/EntityVersionTimeline.vue（版本轨的视觉 analog）；web/src/api/deliveryArtifacts.ts 的 `ArtifactTimeline` 接口（`versions[]` 的确切字段）；web/src/pages/knowledge/__tests__/entity-detail.spec.ts **全文 68 行**（mount + 手写 i18n 键树 + stubs 的范式）</read_first>
  <action>
**① `BlueprintVersionSwitcher.vue`**：
`props: { versions: Array<{ id, version_no, produced_by_ref, is_current, supersedes_id, created_at }>, currentVersionId: string | null }`；`emits: { change: [versionId: string], compare: [baseVersionId: string] }`。
数据源是既有 `deliveryArtifacts.getArtifactTimeline`（**零新端点**，由父层查询后传入）。顶栏一个 `Select`/`Popover` 列表，每行：`v{version_no}` + 当前徽标 + ⭐ **版本原因徽标（唯一判据 = `producedByReason(produced_by_ref)`，115-02 的五档映射）** + 相对时间 + 一个「与此版本对比」按钮（emit `compare`）。
`data-testid="blueprint-version-switcher"`。⛔ 组件内**不自写**前缀匹配逻辑。

**② `BlueprintBlockDiff.vue`**：
`props: { baseDoc: BlueprintDocumentResponse, targetDoc: BlueprintDocumentResponse, mode: 'inline' | 'split' }`；`emits: { 'update:mode': ['inline' | 'split'] }`。
模块 docstring 两段**逐字沿用 `PromptVersionDiff.vue:1-20` 的体例**：「安全：全程 Vue mustache 经 `<pre>` 渲染，未使用 `v-html`，XSS 面 = 0」+「性能：用 `shallowRef<Change[]>` 避免对 diff 输出做深响应式」。
实现：
1. ⭐ **块级增删走 `classifyBlockDiff(baseDoc.content, targetDoc.content)`**（115-02 的 canonical 指纹版本；⛔ 组件内不自写 `JSON.stringify` 比较 —— 键序不定会把「内容未变」误判 `modified`）；
2. `modified` 的块**才**进 `diffWords(blockText(a), blockText(b))`（`import { diffWords } from 'diff'` + `import type { Change } from 'diff'`；⭐ **真实 import 不 mock**，本组件的测试跑绿即 settle A3 假设）；结果存 `shallowRef<Change[]>`；
3. ⚠️ **`watch` 只监听 `versionId` 这类标量**（⛔ 不照抄 analog 的 `{ deep: true }` —— 那是既有的一处自相矛盾：deep watch 两个 prop 对象然后整体替换 `shallowRef`）；
4. **呈现**（§9.2 逐行）：默认**单栏 inline**（`added` 块整块绿底 + 左边框；`removed` 块整块红底 + 左边框 + 内容 `line-through` 弱化；`modified` 块内 `diffWords` 结果逐段着色）；`mode === 'split'` ⇒ 左右并排（左栏 `filter(c => !c.added)`、右栏 `filter(c => !c.removed)`，抄 analog 的视角切分）；
5. 顶部摘要行 `aria-live="polite"`：「v{a} 对比 v{b}：新增 {n} 块、删除 {n} 块、修改 {n} 块」；
6. **按段分组**呈现，未变化的段整段折叠（组头「本段无变化」）；⭐ `must_haves` 段整段折叠并标「验收锚点不参与块级对比」（它无 `block_id`，§6.9）；
7. ⭐ **颜色**：把 `PromptVersionDiff.vue:119-137` 的 `.diff-added` / `.diff-removed` / `.diff-unchanged` **三条 CSS 原样复制**进本组件的 `<style scoped>`（值逐字：`.diff-added { background: hsl(142 71% 45% / 0.12); color: hsl(142 71% 20%); border-left: 3px solid hsl(142 71% 45%); }`、`.diff-removed { background: hsl(0 72% 51% / 0.1); color: hsl(0 72% 30%); border-left: 3px solid hsl(0 72% 51%); }`）。⚠️ **P-11：它们是 `scoped` 的，不存在可 import 的共享令牌文件** —— 「逐字沿用」在实现上就是复制。⛔ 别去找、别新建全局令牌。**这是本 plan 唯一允许出现 `hsl(` 的文件**；
8. ⭐ **diff 模式下批注层与所有写动作关闭**（纯对照视图）—— 本组件不渲染 `<mark>`、不接受线程 props、不 emit 任何写动作；
9. `data-testid="blueprint-diff"`，块级 `data-diff-block`（照 analog 的 `data-diff-column` 习惯，供测试定位）。

**③ 新建 `web/src/components/blueprint/__tests__/threadSidebar.spec.ts`**（顶部「覆盖路径」编号清单 + `makeThread(overrides)` 工厂 + 手写最小 i18n 键树）：
1. ⭐ **§20 断言 1**：`kind: 'ai_review_finding'` 的卡 ⇒ `[data-testid=blueprint-thread-composer]` **不存在**、`[data-testid=blueprint-finding-actions]` **存在**；`kind: 'ai_clarification'` / `'human_comment'` / `'repo_confirmation'` 三类**反向为真**（四类参数化）。
2. ⭐ **§20 断言 2（三条并列）**：`readonly: true` ⇒ (a) `blueprint-thread-composer` **不存在于 DOM**（⛔ 不是 disabled：另断言 `wrapper.find('[data-testid=blueprint-thread-composer]').exists() === false`）；(b) 选区 popover 的 `blueprint-selection-comment` 不存在（`canComment: false`）；(c) ⭐ **finding 的 `blueprint-finding-actions` 仍然存在**。`readonly: false` 时前两条反向为真。
3. ⭐ **§20 断言 5**：`orphanedThreads` 传两条（一条「无 anchor 的系统线程」+ 一条真失锚），断言失锚组条目数 **== `orphanedThreads.length`**（变异：加 `.filter(t => t.anchor?.block_id)` ⇒ 转红）。
4. ⭐ **§20 断言 11**：一条 `status: 'open' && anchor_status: 'orphaned'` 的线程 ⇒ 四组条目**总数 == 线程总数**，且它**只**出现在失锚组（用 `findAll('[data-testid=blueprint-thread-card]')` 计数 + 分组容器内计数）。
5. **组内排序**：`blocker` 在 `warning` 之前、同 severity 按 `created_at` 升序。
6. **`options` 候选**：`ai_clarification` 带两个 `options` ⇒ Composer 内渲染两个可点选项；`options` 为非法形状（`{}`）⇒ 不渲染选项组且**不抛**。
7. **`author_display` 为空** 的消息 ⇒ 正常渲染不抛。
8. **失锚条目仍可处置**：失锚的 `human_comment` 线程在 `readonly: false` 时仍渲染 Composer。

**④ 新建 `web/src/components/blueprint/__tests__/reviewActions.spec.ts`**：
1. **终审可用性**：`currentStatus: 'pending_review'` ⇒ 两按钮**存在且非 disabled**；`'ai_reviewing'` ⇒ **存在且 disabled**（⛔ 不是不存在）+ Tooltip 存在。
2. ⭐ **§20 断言 3**：`BlueprintBlockedDialog` 传 `threadIds` 三项（其中一项在 `threads` 里**找不到**）⇒ `[data-testid=blueprint-blocked-item]` 计数 **== 3**（⛔ 找不到的那条也要渲染，回落显示 id 前 8 位）；点击第一项 ⇒ emit `goto-thread` 一次且载荷是该 id。**负向对照**：断言渲染文本**不只**是一句「不可确认」（`findAll` 计数 > 0）。
3. ⭐ **§20 断言 7（三态并列）**：`BlueprintQualityPanel` 三条用例——`ai_rejection_rate: null` ⇒ 文本含「暂无数据」且**不含** `0`；`human_edit_volume: 0` ⇒ 文本含 `0` 且**不含**「暂无数据」；正值 ⇒ 含具体值。**并列存在才逮得住 `v ?? 0`**。
4. **`citation_coverage` 恒百分比** + `hasKeyConclusions: false` ⇒ 出现「无关键结论」徽标；`true` ⇒ 不出现。
5. **驳回 `comment` 必填**：空 ⇒ 提交按钮 disabled；填入 ⇒ 可提交且 emit 载荷含 `comment`；勾选「保留此划线」⇒ 载荷含 `anchor`。

**⑤ 新建 `web/src/components/blueprint/__tests__/blockDiff.spec.ts`**（真实 import `diff`，⛔ 不 mock）：
1. **三类分类**：仅 B 有 ⇒ added；仅 A 有 ⇒ removed；同 id 内容变 ⇒ modified，各一条。
2. ⭐ **键序不同不算 modified**（canonical 指纹的下游证伪）。
3. **`modified` 块进 `diffWords`**：断言渲染出 `.diff-added` 与 `.diff-removed` 两类片段（⭐ 本用例跑绿即 **settle A3 假设**：`diffWords` 返回同构 `Change[]`）。
4. **inline / split 两形态**：`mode: 'split'` ⇒ 左栏不含 `diff-added`、右栏不含 `diff-removed`（抄 analog 的正负成对断言）。
5. **摘要行**：`aria-live="polite"` 存在且文本含三个计数。
6. ⭐ **diff 模式无批注、无写动作**：渲染后 `[data-testid=blueprint-annotation-mark]` 与任何 `blueprint-review-*` / `blueprint-thread-*` testid **均不存在**。
7. **未变化的段整段折叠** + `must_haves` 段标注「不参与块级对比」。
  </action>
  <verify>
    <automated>cd web && pnpm exec vitest run src/components/blueprint/__tests__ src/__tests__/blueprint-source-guard.spec.ts && pnpm type-check && pnpm lint</automated>
  </verify>
  <acceptance_criteria>
- `cd web && pnpm exec vitest run` **全绿**（本 plan 三个 spec + 115-02/115-03 的全部 spec + 既有 ~50 个，无新增失败）。
- `rg -c "it\(|test\(" web/src/components/blueprint/__tests__/threadSidebar.spec.ts` ≥ **14**；`reviewActions.spec.ts` ≥ **10**；`blockDiff.spec.ts` ≥ **8**。
- ⭐ **断言 1/2 成对**：`cd web && node -e "const s=require('fs').readFileSync('src/components/blueprint/__tests__/threadSidebar.spec.ts','utf8'); if(!/exists\(\)\)\.toBe\(false\)/.test(s)) throw new Error('缺「不存在于 DOM」的断言形态'); if(!/blueprint-finding-actions/.test(s)) throw new Error('缺 finding 处置仍存在的对照'); console.log('assertion 1&2 OK')"` 输出 `assertion 1&2 OK`。
- ⭐ **断言 5/11**：`rg -n "orphanedThreads" web/src/components/blueprint/__tests__/threadSidebar.spec.ts` 命中；测试内含「四组总数 == 线程总数」形态的断言（人工核对 + SUMMARY 登记用例名）。
- ⭐ **断言 3**：`rg -n "blueprint-blocked-item" web/src/components/blueprint/__tests__/reviewActions.spec.ts` 命中，且含 `toHaveLength(3)` 或等价计数断言。
- ⭐ **断言 7 三态并列**：`cd web && node -e "const s=require('fs').readFileSync('src/components/blueprint/__tests__/reviewActions.spec.ts','utf8'); for (const k of ['null','暂无数据']) if(!s.includes(k)) throw new Error('缺三态要素: '+k); if(!/human_edit_volume:\s*0/.test(s)) throw new Error('缺「0 值」那一条'); console.log('quality trio OK')"` 输出 `quality trio OK`。
- ⭐ **A3 settle**：`blockDiff.spec.ts` 中 `diff` 是**真实 import 不 mock**（`rg -n "vi.mock\('diff'\)" web/src/components/blueprint/__tests__/blockDiff.spec.ts` **零命中**），且用例全绿 ⇒ 在 SUMMARY 登记「A3（`diffWords` 返回同构 `Change[]`）已 settle」。
- ⭐ **`.diff-*` 是复制不是 import**：`cd web && node -e "const s=require('fs').readFileSync('src/components/blueprint/BlueprintBlockDiff.vue','utf8'); if(!/<style scoped>/.test(s)) throw new Error('缺 scoped style'); if(!/\.diff-added|\.diff-removed/.test(s)) throw new Error('三条 diff CSS 未复制进来（P-11）'); if(/@import|from '.*PromptVersionDiff/.test(s)) throw new Error('试图 import 不存在的共享令牌'); console.log('diff css copied OK')"` 输出 `diff css copied OK`。
- ⭐ **不照抄 deep watch**：`rg -n "deep: true" web/src/components/blueprint/BlueprintBlockDiff.vue` **零命中**。
- ⭐ **diff 用 canonical 分类**：`rg -n "classifyBlockDiff" web/src/components/blueprint/BlueprintBlockDiff.vue` 命中；`rg -n "JSON.stringify" web/src/components/blueprint/BlueprintBlockDiff.vue` **零命中**。
- ⭐ **版本原因唯一判据**：`rg -n "producedByReason" web/src/components/blueprint/BlueprintVersionSwitcher.vue` 命中；`rg -n "human_edit:|ai_review_reflow:|human_block_restore:|blueprint_review_reject:" web/src/components/blueprint/BlueprintVersionSwitcher.vue` **零命中**（前缀匹配在 115-02 的 config 里）。
- ⛔ **`hsl(` 只出现在 `BlueprintBlockDiff.vue`**：`rg -l "hsl\(" web/src/components/blueprint/ | rg -v "annotationTokens.ts|BlueprintBlockDiff.vue"` 输出**为空**。
- `pnpm exec vitest run src/__tests__/blueprint-source-guard.spec.ts` 绿；`pnpm lint` 与 `pnpm type-check` 通过。
- ⛔ 零既有文件改动：`git diff --name-only web/src | rg -v "^web/src/components/blueprint/"` 输出**为空**；`git diff --name-only server/` 输出**为空**。
  </acceptance_criteria>
  <done>版本切换器复用既有时间线端点、版本原因走唯一判据；block 级 diff 在前端算、用 canonical 指纹分类、`diffWords` 真实跑通（A3 settle）、三条颜色 CSS 原样复制进 scoped style、diff 模式下批注与写动作全关；§20 的六条断言（1/2/3/5/7/11）各有能被变异逼红的用例。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 线程 `kind` → 可用动作 | 分流错误 ⇒ 稳定撞 400 或**误处置**（在 BLOCKER 上回一句任意文本就解开 confirm 门） |
| `current_status` → 写入口是否渲染 | 渲染了会撞 400 的入口 = 把用户送进死路 |
| approve 409 → 用户的下一步 | 只说「不可确认」= 把人锁死在超界出口里，无解药入口 |
| 统计 `null` → 人审判断 | `?? 0` 会把「没数据」显示成「零打回」，评审据错误指标放行 |
| 用户输入（`comment` / `reason`）→ 落库并回显给同项目成员 | 半可信文本；空值放行会产生无理由的审计记录 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115-29 | Elevation of Privilege | finding 线程给出统一回复框 ⇒ 走 `answer/` 通道，绕开 `reason` 必填 / `[已修复]`-`[误报忽略]` 语义 / 「处置人」留痕（114-REVIEW CR-01 的 UI 对称面） | mitigate | ⭐ 渲染层硬分流（`v-if="isFinding"` / `v-else`，两条分支物理互斥）+ 源码扫描断言（禁 `v-show`）+ §20 断言 1 的四类参数化用例 |
| T-115-30 | Tampering | 已 `confirmed` 的蓝图仍渲染作答框/评论入口 ⇒ 用户撞 400，或误以为改动生效 | mitigate | `readonly` 一律 `v-if` 掉（⛔ 不是 `disabled`）+ §20 断言 2 的三条并列（含 finding 处置仍在的对照） |
| T-115-31 | Denial of Service | approve 409 只显示一句「不可确认」⇒ 用户在超界出口里**没有任何正向出路**（只能驳回重跑） | mitigate | `BlueprintBlockedDialog` 逐条可点跳转 + 找不到线程时回落显示 id（⛔ 不跳过）+ §20 断言 3 的计数用例 |
| T-115-32 | Tampering | `null` 统计被 `?? 0` 改写 ⇒ 「无数据」显示成「零打回」 | mitigate | `=== null` 分支 + 源码扫描禁 `?? 0` / `|| 0` + §20 断言 7 的三态并列（`v ?? 0` 会让 `null` 用例红而 `0` 用例仍绿——正是要逮的形状） |
| T-115-33 | Tampering | 侧栏对 `orphaned_threads` 二次过滤 ⇒ 真失锚被滤掉，CLAR-02 的「失锚集中可见」失效 | mitigate | 直接渲染 + 源码扫描禁 `.filter(` + §20 断言 5 |
| T-115-34 | Tampering | 侧栏四组漏 `&& anchor_status !== 'orphaned'` ⇒ 线程重复出现、计数重复、选中时两处高亮 | mitigate | 四组一律调 115-02 的 `sidebarGroups`（组件内零判据）+ §20 断言 11 |
| T-115-35 | Repudiation | 空 `reason` / 空 `comment` 放行 ⇒ 无理由的处置与驳回，审计上等于凭空放行 | mitigate | 两处 `trim()` 必填 + `disabled` + 内联提示；后端同样 400（双保险） |
| T-115-36 | Tampering | 前端自行推断下一状态（乐观更新）⇒ 与续驱后的真值不一致（MJ-01 第二点） | mitigate | 一律以响应体 `current_status` 为准 + `invalidateQueries({ queryKey: ['blueprint'] })`；组件只 emit、由页面统一处理响应（115-06） |
| T-115-37 | Tampering | `answer` 的 `reflow.status` 非 `applied` 被当作失败 ⇒ 用户重复提交、答案重复落库 | mitigate | 五档 toast 分档（`applied`/`unchanged`/`noop`/`conflict`/`failed|invalid`），⛔ 任何分支不渲染成红色错误态；文案与处理写进组件 docstring 与 115-06 的接线契约 |
| T-115-38 | Tampering | diff 用 `JSON.stringify` 直接比较 ⇒ 键序不同被误判 `modified`，评审被噪声淹没 | mitigate | 一律 `classifyBlockDiff`（canonical 指纹）+ 源码扫描禁 `JSON.stringify` + 「键序不同不算 modified」用例 |
| T-115-39 | Information Disclosure | 线程消息 / `quoted_text` 经 `v-html` 注入 | mitigate | 全程 mustache + `<pre>`；`rg -n 'v-html'` 零命中 |
| T-115-SC | Tampering | 依赖供应链 | accept | 零新增依赖；⛔ 不引入 `@floating-ui/vue` 的首个调用点（改用 `reka-ui` 直接导入的 `PopoverAnchor`，⛔ 不改 `~/components/ui/popover` barrel）；`diff` 与 `reka-ui` 均为既有依赖 |
</threat_model>

<verification>
- `cd web && pnpm exec vitest run` 全绿（本 plan 三个 spec + 115-02/03 的全部 spec + 既有）。
- `cd web && pnpm type-check` 与 `pnpm lint` 通过。
- `rg -n "v-html" web/src/components/blueprint/` 零命中；`rg -n "refetchInterval|edit-block|edit-blocks|editBlocks" web/src/components/blueprint/` 零命中；`pnpm exec vitest run src/__tests__/blueprint-source-guard.spec.ts` 绿。
- `rg -l "hsl\(" web/src/components/blueprint/ | rg -v "annotationTokens.ts|BlueprintBlockDiff.vue"` 输出为空（颜色只有两个合法出处）。
- `rg -n "@floating-ui/vue|useFloating|codemirror" web/src/components/blueprint/` 零命中。
- `git diff --name-only web/src | rg -v "^web/src/components/blueprint/"` 输出为空；`git diff --name-only server/` 输出为空；`git diff web/package.json web/pnpm-lock.yaml` 输出为空。
- ⛔ 与同波次 115-03 的文件交集核算：`git diff --name-only web/src/components/blueprint/` 的结果**不含** `BlueprintBlock.vue` / `BlueprintBlockList.vue` / `BlueprintCitationChip.vue` / `CitationPreviewDialog.vue` / `citation/**` / `annotationTokens.ts` / `BlueprintStatusBadge.vue`。
</verification>

<success_criteria>
- **CLAR-01 的写路径全部有 UI**：多轮回复（带 `options` 候选）、选区评论建线程、finding 处置——三条各自有独立组件与用例。
- ⭐ **本相位最不能错的一条被物理锁死**：finding 卡里**根本没有** Composer 这个 DOM 节点（渲染层硬分流，不是 disabled、不是提交层切端点），且有四类参数化用例把变异逼红。
- ⭐ **`readonly` 的两处入口「不存在于 DOM」而 finding 处置仍在**，与终审按钮的「disabled + Tooltip」形成刻意的对照，两者各有用例。
- ⭐ **超界死锁有 UI 出口**：approve 409 的 `unresolved_blocker_thread_ids` 逐条可点跳转到处置入口（不是一句「不可确认」）。
- ⭐ **闭 MN-05**：`blueprint_quality` 三项统计有了唯一消费面，`null` 渲染「暂无数据」，三态并列用例锁死「绝不显示 0」；`citation_coverage` 的「空文档满分」口径陷阱有「无关键结论」旁注兜住。
- **SC-2 后半句就位**：版本切换（复用既有端点）+ block 级 diff（前端算、canonical 指纹、`diffWords` 真实跑通 settle A3、颜色复制自 `PromptVersionDiff`）；diff 模式下批注层与写动作全关。
- 与同波次 115-03 零文件交集；零既有文件改动、零新增依赖、零 `v-html`、颜色只有两个合法出处。
</success_criteria>

<output>
完成后创建 `.planning/phases/115-ui/115-04-SUMMARY.md`，provides 至少包含：
- 11 个组件的**最终 props/emits 逐字表**（115-06 的页面装配按它接线，尤其 `BlueprintThreadSidebar` 的九个 prop 与七个 emit、`BlueprintBlockedDialog` 的 `goto-thread`、`BlueprintRejectDialog` 的 `submit` 载荷）；
- ⭐ **接线契约（给 115-06）**：六个动作端点的**响应处理规则**——以响应体 `current_status` 为准 + `invalidateQueries({ queryKey: ['blueprint'] })`、⛔ 不做乐观更新；`answer` 的 `reflow.status` **五档 toast 分档表**；approve 409 blocked → 开 `BlueprintBlockedDialog`、409 conflict / reject 409 → toast + 刷新；400 → 原样回显 `detail`；
- `data-testid` 完整清单（本 plan 新增的 8 个）；
- `sidebarGroups` 的消费方式与「两数据源合并」的落地（`threads/` 为准、快照占位）；
- ⭐ **A3 假设的 settle 记录**（`diffWords` 返回同构 `Change[]`，由 `blockDiff.spec.ts` 真实 import 跑绿证明）；
- `PopoverAnchor` 从 `reka-ui` 直接导入的**实测结论**（`^2.9.10` 是否导出）与（若不导出的）`PopoverTrigger` + 零尺寸 div 回退方案；⛔ 已核实 `~/components/ui/popover` barrel 不导出它且**不得**为此改动该 barrel；
- 「已提醒 N 次」与后端只给 `last_reminded_at` 的**差异登记**（UI-SPEC §0.2 判定 4 的文案退化）；
- ⭐ **UAT 清单**：选区 popover 的实际定位与焦点行为、Tooltip 的悬停呈现、diff 颜色对比度、侧栏 `↑`/`↓` 键盘导航、`Esc` 语义；
- §20 断言 1/2/3/5/7/11 → **绿色用例名映射表**（每条注明「变异是什么、会不会转红」）。
</output>
