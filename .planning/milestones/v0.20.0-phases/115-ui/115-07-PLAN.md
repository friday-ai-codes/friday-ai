---
phase: 115-ui
plan: 07
type: execute
wave: 6
depends_on: ["115-06"]
files_modified:
  - web/src/components/blueprint/BlueprintGatePanel.vue
  - web/src/components/blueprint/BlueprintGateRepoRow.vue
  - web/src/pages/knowledge/blueprints/[id].vue
  - web/src/components/blueprint/__tests__/gatePanel.spec.ts
autonomous: true
requirements: [VIEW-01]
prohibitions:
  - "🔒 **本 plan 是相位的可独立顺延尾巴**（CONTEXT `<deferred>` / UI-SPEC §11.3 双处登记）：它是**相对 ROADMAP SC 的范围增量**——112 只交付后端、全仓零前端 ⇒ 不做则 FLOW-03 在 UI 上**不可达**，用户永远走不到 113 的阶段 2。⛔ **不得默默丢掉**；若确需顺延，顺延目标是 Phase 116 且必须在 STATE 显式登记。⭐ **前六个 plan 不依赖它**：115-06 已在页面预留 `blueprint-gate-mount` 插槽，本 plan 被砍掉页面仍完整可用"
  - "🔒 **前端 CREATE-ONLY**：本 plan 只允许改**一个**既有文件——`web/src/pages/knowledge/blueprints/[id].vue`（115-06 的产物，**在预留插槽处纯追加**）。`git diff web/src/pages/knowledge/blueprints/[id].vue | rg \"^-[^-]\"` **必须为空**。⛔ 不碰 `zh-CN.json` / `main.css` / `api/index.ts`（115-02 已做完）；缺 i18n key 或缺 safelist 图标 ⇒ **回报而不是自己补**"
  - "⛔ ⭐ **不得据 `blueprint-gate/` 的状态码做任何权限推断**（P-10 / §8.2 例外一）：实测 `blueprint_gate_views._ablueprint_project_id`（`:511`）**只在 `BlueprintRejectedToBoundaryView`（`:385`）里被用了一次** —— 八个 gate 端点里**其余七个只有 `IsAuthenticated`，没有项目范围闸**。它的 404 混合了「确认门未开启」（`:53,213`，**绝大多数蓝图在绝大多数时间的状态**）/「artifact 不存在」/「该 artifact 没有蓝图编排会话」三种语义 ⇒ **状态码不携带任何权限信息**。⇒ 一律「gate 查询非 200 ⇒ 不渲染本面板」，⛔ 不报错、⛔ 不弹 toast、⛔ 不进 §8.2 分档、⛔ **不靠 `detail` 文本分支判定**（那等于把后端文案当协议）"
  - "⛔ **七个动作统一范式：一次 POST + 重取快照**，⛔ 不做乐观更新、⛔ 不自行推断下一状态。重取 = `invalidateQueries({ queryKey: ['blueprint','gate', artifactId] })` **且** `invalidateQueries({ queryKey: ['blueprint','snapshot', artifactId] })`（确认门动作会改蓝图状态与线程）"
  - "⛔ **需要输入框的动作不能用 `useConfirmDialog`**（它没有输入框）：`edit-responsibility` 用受控小 `Dialog` + `Textarea`；`remove-repo` 与 `confirm` 用 `useConfirmDialog`（破坏性二次确认，文案逐字照 §16）"
  - "⛔ **`add-repo` 复用既有 `~/components/workflow/RepositoryPicker.vue`**，⛔ 不新造仓库选择器。⭐ **形状已核实并定夺，⛔ 不再「先读再决定」**（plan-checker N2）：该组件是 `withDefaults(defineProps<Props>(), { placeholder: '选择仓库...', allowManualInput: true })`，`modelValue: string[]`（**多选**，`:31`），`emits` **只有** `update:modelValue: string[]`（`:46-47`）。⇒ 面板内起一个本地 `const picked = ref<string[]>([])` 直接 `v-model` 即可满足 `add-repo`，**⛔ 不包薄适配层、⛔ 不改用 `~/components/chat/RepoMultiSelector.vue`**（该备选就此撤销）。⛔ 不修改 `RepositoryPicker.vue` 本身"
  - "⛔ **`pending_research_repository_ids` 命中的行必须 `disabled`**：行内动作全部禁用 + `icon-[lucide--loader-2] animate-spin` + 「调研中」；确认主按钮在存在 pending 时 `disabled` + Tooltip 回显后端 `_LOCK_BLOCKED_MESSAGES` 的语义（「有仓库正在调研，暂不能确认」）"
  - "⛔ **`confirm/` 的 409 分两档**：`blocked_reason === 'pending_clarification'` ⇒ 提示「存在未解决的阻塞澄清线程」+ **一键跳侧栏未决组**（emit 给页面）；其余 409 ⇒ 回显 `detail` + 「刷新重试」"
  - "⛔ **禁 `v-html`**；⛔ 组件内零颜色字面量；⛔ 禁止在 `Badge` 上用 `:class` 追加颜色类；⛔ `refetchInterval` / `edit-block` / `edit-blocks` / `editBlocks` 在本 plan 任何文件零命中（§20 断言 6 / 10）"
  - "⛔ **不新增运行时依赖**、⛔ 不改 `web/package.json` / lockfile；⛔ **后端零改动**（gate 八端点是 112 的既有面，本 plan 只消费；⚠️ 「gate 链无项目范围闸」是一处**既有后端缺口**，本相位边界是「只加读面」不修它，但必须记进 STATE 的 Pending Todos）"
must_haves:
  truths:
    - "⭐ **FLOW-03 在 UI 上第一次可达**：`BlueprintGatePanel` 消费 112 的 `blueprint-gate/` 快照 + 七个动作端点（`confirm` / `remove-repo` / `add-repo` / `reclassify-role` / `edit-responsibility` / `rejected-to-boundary` / `upgrade-research`）；此前全仓零前端，用户走不到 113 的阶段 2"
    - "⭐ **渲染条件只有一条：`GET blueprint-gate/` 返回 200**。任何非 200（含三种 404）⇒ **不渲染本面板**，且⛔ 不报错、不弹 toast、不进 §8.2 分档、不靠 `detail` 文本分支判定（P-10：该链无项目范围闸，状态码不携带权限信息）"
    - "⭐ **七个动作统一范式**：一次 POST + 重取快照（`['blueprint','gate',id]` 与 `['blueprint','snapshot',id]` 双失效），⛔ 零乐观更新；每个动作的成功/失败 toast 分档明确"
    - "⭐ **破坏性动作二次确认**：`remove-repo` 与 `confirm` 走 `useConfirmDialog`，文案**逐字照 §16**（移除仓：标题「从方案中移除该仓库？」／正文「移除后该仓的调研结论与职责将不再参与本方案，并可沉淀为仓库章程的边界禁区候选。」／按钮「确认移除」；确认锁定：标题「确认仓库集并锁定？」／正文「确认后仓库集与职责被锁定，后续变更必须重开确认门。你将被记入本方案的评审人名单。」／按钮「确认锁定」）；`edit-responsibility` 走受控小 `Dialog` + `Textarea`（⛔ 不用 `useConfirmDialog`）"
    - "⭐ **pending 调研态**：`pending_research_repository_ids` 命中的行 `disabled` + `animate-spin` + 「调研中」；确认主按钮在存在 pending 时 `disabled` + Tooltip"
    - "⭐ **`confirm/` 409 两档**：`blocked_reason === 'pending_clarification'` ⇒ 提示 + **一键跳侧栏未决组**（emit `goto-unresolved`）；其余 ⇒ 回显 `detail` + 刷新重试"
    - "`add-repo` **复用既有 `RepositoryPicker.vue`**（⛔ 不新造选择器）；`upgrade-research` 带「重新调研该仓」勾选（`{rerun: true}`）且**仅 `indirect` 行渲染**；`rejected-to-boundary` 是面板底部**次级动作**"
    - "⭐ **页面挂载是纯追加**：在 115-06 预留的 `blueprint-gate-mount` 插槽处追加 `<BlueprintGatePanel>`，`git diff web/src/pages/knowledge/blueprints/[id].vue | rg \"^-[^-]\"` **为空**"
    - "⭐ **面板缺席不影响任何上游**：把本 plan 的两个组件与页面追加整体回退后，`pnpm exec vitest run` / `pnpm type-check` / `pnpm build` **仍全绿**（本 plan 的 SUMMARY 需登记这一验证结果，证明它确实可独立顺延）"
    - "组件内零 `v-html`、零颜色字面量、零 `refetchInterval`、零 `edit-block`；`data-testid` 为 `blueprint-gate-panel` / `blueprint-gate-repo-row`"
    - "前端门：`cd web && pnpm exec vitest run` 全绿（含源码守卫 spec）、`pnpm lint` / `pnpm type-check` / `pnpm build` 通过"
  artifacts:
    - path: "web/src/components/blueprint/BlueprintGatePanel.vue"
      provides: "确认门面板：顶部说明条 + 仓库行列表 + 底部主操作（确认锁定）与次级动作（rejected 沉淀禁区）+ 添加仓库（复用 RepositoryPicker）+ 七动作的统一 POST/重取范式 + confirm 409 两档"
      contains: "blueprint-gate-panel"
    - path: "web/src/components/blueprint/BlueprintGateRepoRow.vue"
      provides: "单仓行：role segmented control（即时提交）+ 移除（二次确认）+ 编辑职责（小 Dialog）+ 升级深调研（仅 indirect，带 rerun 勾选）+ pending 调研态禁用"
      contains: "blueprint-gate-repo-row"
    - path: "web/src/components/blueprint/__tests__/gatePanel.spec.ts"
      provides: "gate 非 200 不渲染且不报错 / 七动作各触发一次 POST 且双失效 / pending 行禁用 / confirm 409 两档 / 破坏性动作走二次确认 / 面板内零 refetchInterval"
      contains: "blueprint-gate-panel"
  key_links:
    - from: "web/src/pages/knowledge/blueprints/[id].vue"
      to: "web/src/components/blueprint/BlueprintGatePanel.vue"
      via: "115-06 预留的 blueprint-gate-mount 插槽处纯追加；渲染条件 = gate 查询 200"
      pattern: "BlueprintGatePanel"
    - from: "web/src/components/blueprint/BlueprintGatePanel.vue"
      to: "web/src/api/blueprints.ts"
      via: "115-02 已封装的 gate 快照 + 七动作函数；一次 POST + 双 invalidate"
      pattern: "confirmGate"
    - from: "web/src/components/blueprint/BlueprintGatePanel.vue"
      to: "web/src/components/workflow/RepositoryPicker.vue"
      via: "add-repo 复用既有仓库选择器（⛔ 不新造）"
      pattern: "RepositoryPicker"
    - from: "web/src/components/blueprint/BlueprintGatePanel.vue"
      to: "web/src/components/blueprint/BlueprintThreadSidebar.vue"
      via: "confirm 409 pending_clarification ⇒ emit goto-unresolved ⇒ 页面打开侧栏未决组"
      pattern: "goto-unresolved"
---

<objective>
把**阶段 1 确认门**接上界面 —— 这是本相位显式登记的**范围增量**，也是它被安排成最后一个可独立顺延 plan 的原因：

112 交付了 `blueprint-gate/` 八个端点，但**全仓零前端**。不做它，FLOW-03 在 UI 上**不可达**：用户看得到蓝图正文，却无法确认仓库集与职责 ⇒ 永远走不到 113 的阶段 2，整条链在界面上断在第一关。⭐ **不得默默丢掉**（CONTEXT `<deferred>` 与 UI-SPEC §11.3 双处登记）。

同时它有一条**独有的、与其余六个 plan 都不同的纪律**：⛔ **不得据这条链的状态码做任何权限推断**。实测 `blueprint_gate_views` 里的项目范围闸 helper **只在八个端点中的一个里被用过一次** —— 其余七个只有 `IsAuthenticated`。它的 404 混合了「确认门未开启」（绝大多数蓝图在绝大多数时间的正常态）、「artifact 不存在」、「该 artifact 没有蓝图编排会话」三种语义。因此本面板的渲染条件被压缩成**一条**：`GET blueprint-gate/` 返回 200，否则**什么都不做**（不渲染、不报错、不 toast、不进错误分档）。

Purpose: 补齐链条第一关的界面可达性，且**不给页面引入任何新的失败模式**——面板缺席时页面必须完全正常，这也是「可独立顺延」的技术前提。
Output: 2 个新建组件 + 1 处页面纯追加 + 1 个测试文件。**零新增依赖、后端零改动。**
</objective>

<execution_context>
工作目录：worktree 根 `/Users/zaneliu/Projects/open-source/friday-ai/.claude/worktrees/v0.20-blueprint`。前端命令 `cd web && ...`，测试 `pnpm exec vitest run <path>`。

**wave 6，依赖 115-06**：页面已存在且**已预留插槽**（`<div id="gate" data-testid="blueprint-gate-mount">` + 注释锚点 + `gate` query（`['blueprint','gate',artifactId]`，`retry: false`）+ `gateAvailable` 派生值）。本 plan 只在该处追加渲染。

**已存在，⛔ 只 import 不修改**：
- 115-02：`~/api/blueprints` 的 gate 快照与七动作封装、`~/config/blueprintStatus`、`~/types/blueprint`；i18n `knowledge.blueprints.gate.*`（**已写全**，⛔ 缺键回报不自补）；safelist（`lucide--loader-2` 等**已在既有 safelist**）；
- 115-04：`BlueprintThreadSidebar`（`goto-unresolved` 的落点由页面接线）；
- 既有：`~/composables/useConfirmDialog`、`~/components/ui/{dialog,textarea,tooltip,badge,button,collapsible,separator}`、⭐ `~/components/workflow/RepositoryPicker.vue`（`add-repo` 复用，形状已定：`v-model` 为 `string[]` 多选 + `placeholder` / `allowManualInput`，只 emit `update:modelValue`）。⛔ `~/components/chat/RepoMultiSelector.vue` 的备选方案**已撤销**（N2）。

**后端契约（112 的既有面，实读 `server/delivery/api/blueprint_gate_views.py` 与 `server/delivery/urls.py` 的 `blueprint-gate/` 分组；⛔ 不得猜测入参键与状态码）**：
- `GET artifacts/<uuid>/blueprint-gate/`（快照）：⭐ **「门未开启」返回 404 `{"detail": "确认门未开启"}`（`:53,213`）**；另有两种 404（artifact 不存在 / 无蓝图会话，`:174,176,179,211`）。快照里含仓库清单（`role` / 职责 / `fitness` / 现状摘要 / 证据）、⭐ `pending_research_repository_ids`、rejected 候选清单、`_LOCK_BLOCKED_MESSAGES` 对应的阻塞语义等 —— **键名逐字实读，不猜**；
- 七个动作端点：`confirm/` / `remove-repo/` / `add-repo/` / `reclassify-role/` / `edit-responsibility/` / `rejected-to-boundary/` / `upgrade-research/`（`name` 为 `blueprint-gate-*`）。**入参键与状态码逐字实读**（`upgrade-research` 带 `{rerun: bool}`；`confirm/` 的 409 带 `blocked_reason`）；
- ⚠️ **`blueprint_gate_views._ablueprint_project_id`（`:511`）只在 `BlueprintRejectedToBoundaryView`（`:385`）里用过一次** —— 这是本 plan「不得据状态码推断权限」的证据源。

**测试范式**：`components/prompts/__tests__/PromptVersionDiff.test.ts`（113 行，最规范：覆盖路径编号清单 / 工厂 / 正负成对 / `data-*` 定位）；`pages/knowledge/__tests__/entity-detail.spec.ts`（68 行：`vi.mock` + 手写最小 i18n 键树 + `stubs`）。setup 只有 storage 垫片。
</execution_context>

<context>
@.planning/phases/115-ui/115-UI-SPEC.md §11.3（**确认门面板的完整规格**：渲染条件 / 面板结构 / **七动作交互表逐行** / pending 态 / confirm 409 两档 / 范围增量与可顺延的双处登记）、§8.2 例外一（**gate 的 404 是正常态**）、§16（**移除仓与确认锁定两条破坏性确认全文逐字**）、§13.5（两个组件的 props/emits 契约）
@.planning/phases/115-ui/115-RESEARCH.md §A.5（⭐ **「`blueprint-gate/` 至今没有项目范围闸」的实读证据**）、P-10（前端不得据其 404 做权限推断，且**该缺口要记进 STATE 的 Pending Todos**）
@.planning/phases/115-ui/115-PATTERNS.md §F7/§F8（`ContextLinksCard.vue` 的「行列表 + 行内多动作 + 二次确认」analog）
@.planning/phases/115-ui/115-06-SUMMARY.md（⭐ **页面预留插槽的确切位置与 `gateAvailable` 的派生方式**；六个动作端点的接线契约；`invalidateQueries` 的前缀写法；toast 分档惯例）
@.planning/phases/115-ui/115-02-SUMMARY.md（`~/api/blueprints` 的 gate 七动作函数签名；i18n `gate.*` 键清单）
@.planning/phases/115-ui/115-04-SUMMARY.md（`BlueprintThreadSidebar` 的 props/emits —— `goto-unresolved` 的落点；破坏性确认与受控 Dialog 的两种用法分工）
@server/delivery/api/blueprint_gate_views.py（**八端点的入参键、状态码、快照响应键的唯一权威**；⭐ `:53,174,176,179,211,213` 的六处 404 与 `:385,511` 的范围闸单次调用）
@server/delivery/urls.py L145-185（`blueprint-gate/` 八条路由的 `name` 命名）
@web/src/components/project/warroom/ContextLinksCard.vue（行列表 + 行内多动作 + 二次确认的 analog）
@web/src/components/workflow/RepositoryPicker.vue（`add-repo` 复用对象；**props/emits 已核实并定夺**：`modelValue: string[]` 多选 + `placeholder` / `allowManualInput`，只 emit `update:modelValue`）
@web/src/composables/useConfirmDialog.ts（`confirm(...)` 签名与全局单例；⚠️ 需要输入框的动作不能用它）
</context>

<tasks>

<task type="auto">
  <name>Task 1: BlueprintGateRepoRow.vue + BlueprintGatePanel.vue（七动作统一范式 / pending 禁用 / confirm 409 两档）</name>
  <files>web/src/components/blueprint/BlueprintGateRepoRow.vue, web/src/components/blueprint/BlueprintGatePanel.vue</files>
  <read_first>server/delivery/api/blueprint_gate_views.py **全文**（⭐ 八端点的**入参键、状态码、快照响应键**的唯一权威；六处 404 的语义；`_LOCK_BLOCKED_MESSAGES` 的取值；`pending_research_repository_ids` 的确切键名；`upgrade-research` 的 `rerun` 参数；`confirm/` 409 的 `blocked_reason` 取值集合；⭐ `:385,511` 证实范围闸只被用过一次）；server/delivery/urls.py L145-185（八条路由的 `name`）；.planning/phases/115-ui/115-UI-SPEC.md §11.3（**面板结构与七动作交互表逐行**）、§16（两条破坏性确认全文）；.planning/phases/115-ui/115-02-SUMMARY.md（`~/api/blueprints` 的 gate 七动作函数**逐字签名**）；web/src/components/project/warroom/ContextLinksCard.vue **全文**（行列表 + 行内多动作 + 二次确认的 analog）；web/src/composables/useConfirmDialog.ts **全文**（签名与「无输入框」的限制）；⭐ web/src/components/workflow/RepositoryPicker.vue **L28-50 与 L140-160**（`add-repo` 复用对象；形状已核实：`Props` 的 `modelValue: string[]` `:31` / `placeholder` `:33` / `allowManualInput` `:34`、`withDefaults` `:41-44`、**唯一 emit** `update:modelValue: string[]` `:46-47`。⇒ 本地 `ref<string[]>([])` 直接 `v-model`，⛔ 不包适配层、⛔ 不改用 `RepoMultiSelector`、⛔ 不修改该组件）；web/src/components/ui/tooltip/index.ts 与 dialog/index.ts（导出子组件名）</read_first>
  <action>
**① `BlueprintGateRepoRow.vue`**：
`props: { repo: <快照仓库行形状，键名逐字实读>, pending: boolean, submitting?: boolean }`；
`emits: { remove: [repositoryId: string], reclassify: [repositoryId: string, role: 'direct' | 'indirect'], 'edit-responsibility': [repositoryId: string, text: string], 'upgrade-research': [repositoryId: string, rerun: boolean] }`（元组简写，与 `components/blueprint/**` 内统一）。

行内容：仓名 + `role` 徽标（双色，`direct`→`default` / `indirect`→`secondary`）+ `fitness.verdict` 徽标（三档）+ 职责摘要 + 现状摘要（截断）+ 证据 chip 数。

行内动作（照 §11.3 交互表）：
- **改判 role**：`direct` / `indirect` 二选一 segmented control（两个 `Button` 或 `Toggle` 组，**即时提交** ⇒ emit `reclassify`）；
- **修改职责**：「编辑职责」→ ⭐ **受控小 `Dialog` + `Textarea`**（⛔ **不用 `useConfirmDialog`** —— 它没有输入框）；空/纯空格 ⇒ 提交 `disabled`；
- **移除**：→ `useConfirmDialog().confirm({...})`，⭐ 四字段**逐字照 §16**（标题「从方案中移除该仓库？」／正文「移除后该仓的调研结论与职责将不再参与本方案，并可沉淀为仓库章程的边界禁区候选。」／按钮「确认移除」／`variant: 'destructive'`），确认后 emit `remove`；
- **升级为深调研**：⭐ **仅 `role === 'indirect'` 的行渲染**；带「重新调研该仓」勾选（`Checkbox`/`Switch`）⇒ emit `upgrade-research(id, rerun)`。

⭐ **pending 态**：`props.pending === true`（该仓在 `pending_research_repository_ids` 里）⇒ **行内全部动作 `disabled`** + 一个 `icon-[lucide--loader-2] animate-spin` + 文案「调研中」。
`data-testid="blueprint-gate-repo-row"`。⛔ 零颜色字面量、⛔ 禁 `Badge` 上 `:class` 加色。

**② `BlueprintGatePanel.vue`**：
`props: { artifactId: string, snapshot: <gate 快照形状>, submitting?: boolean }`；
`emits: { action: [name: string, payload?: unknown], 'goto-unresolved': [] }`（⭐ 动作统一经一个 `action` emit 上抛由页面执行，**或**由面板内部直接调 `~/api/blueprints` 并自行 invalidate —— **PLAN 取后者**：面板自持 `useMutation` + 双 invalidate，页面只负责渲染与 `goto-unresolved` 的落点。理由：七个动作与页面其余六个动作**语义独立**（它们改的是确认门不是人审），塞进页面的动作分发器会让页面再长一截；且面板本就要在被顺延时整体拿掉。**这条选择要写进组件 docstring**）。

结构（§11.3）：
- **顶部说明条**：`t('knowledge.blueprints.gate.notice')` =「确认仓库集与职责后才进入方案拟定；确认后锁定，后续变更须重开确认门」；
- **仓库行列表**：`v-for` 渲染 `BlueprintGateRepoRow`，`pending` 由 `snapshot.pending_research_repository_ids.includes(repo.repository_id)` 派生；
- **添加仓库**：底部「添加仓库」→ ⭐ **复用既有 `~/components/workflow/RepositoryPicker.vue`**（⛔ 不新造、⛔ 不包适配层）：面板内 `const picked = ref<string[]>([])`，`<RepositoryPicker v-model="picked" :placeholder="t('…')" />`；选中后按选中的 id 逐个调 `add-repo`，成功后 `picked.value = []`；
- **次级动作**：面板底部「把被否决的候选沉淀为章程禁区」→ `rejected-to-boundary`（rejected 候选清单为空时**不渲染**该动作）；
- ⭐ **底部主按钮「确认仓库集并进入方案拟定」**：`useConfirmDialog` 二次确认，四字段**逐字照 §16**（标题「确认仓库集并锁定？」／正文「确认后仓库集与职责被锁定，后续变更必须重开确认门。你将被记入本方案的评审人名单。」／按钮「确认锁定」）；⭐ **存在 pending 调研时 `disabled` + Tooltip**（回显 `_LOCK_BLOCKED_MESSAGES` 的语义「有仓库正在调研，暂不能确认」）。

⭐ **七个动作的统一范式（逐条落实现）**：一次 POST → 成功 ⇒ 成功 toast + ⭐ **双 invalidate**（`['blueprint','gate',artifactId]` **与** `['blueprint','snapshot',artifactId]` —— 确认门动作会改蓝图状态与线程）；⛔ **零乐观更新**、⛔ 不自行推断下一状态。
错误分档：
- ⭐ **`confirm/` 的 409 两档**：`blocked_reason === 'pending_clarification'` ⇒ 提示「存在未解决的阻塞澄清线程」+ 一个「前往未决线程」按钮 ⇒ **emit `goto-unresolved`**（页面打开侧栏并展开未决组）；其余 409 ⇒ `destructive` toast 回显 `detail` + 「刷新重试」（触发 invalidate）；
- 其余动作的 4xx ⇒ 原样回显 `detail`；5xx ⇒ 通用失败 toast + 保持面板可重试。

`data-testid="blueprint-gate-panel"`。⛔ **面板内零 `refetchInterval`**（它不是 live 面，动作后靠 invalidate 重取即可）。

组件 docstring 三段：
- 「**范围增量登记**：112 只交付后端、全仓零前端 ⇒ 不做则 FLOW-03 在 UI 上不可达。本面板是相位的**可独立顺延尾巴**（CONTEXT `<deferred>` / UI-SPEC §11.3），顺延目标 Phase 116，⛔ 不得默默丢掉」；
- ⭐ 「**渲染条件只有一条**：`GET blueprint-gate/` 返回 200。任何非 200（含三种 404：确认门未开启 / artifact 不存在 / 无蓝图编排会话）⇒ 不渲染本面板，⛔ 不报错、不弹 toast、不进 §8.2 分档、**不靠 `detail` 文本分支判定**（那等于把后端文案当协议）」；
- ⭐ 「**⛔ 不得据本链状态码推断权限**：实测 `blueprint_gate_views._ablueprint_project_id`（`:511`）**只在 `BlueprintRejectedToBoundaryView`（`:385`）里用过一次** —— 八个端点里其余七个只有 `IsAuthenticated`，**没有项目范围闸**。页面的权限判定由四个主查询（正文 / 快照 / threads / events，全部有闸）承担。这是一处**既有后端缺口**，本相位边界是『只加读面』不修它，已记进 STATE 的 Pending Todos」。
  </action>
  <verify>
    <automated>cd web && pnpm type-check && pnpm lint</automated>
  </verify>
  <acceptance_criteria>
- `cd web && pnpm type-check` 与 `pnpm lint` 通过。
- ⭐ **不靠 detail 文本分支**：`cd web && node -e "const s=require('fs').readFileSync('src/components/blueprint/BlueprintGatePanel.vue','utf8'); if(/确认门未开启|没有蓝图编排会话/.test(s)) throw new Error('用后端文案做分支判定了（P-10）'); console.log('no detail-text branching OK')"` 输出 `no detail-text branching OK`。
- ⭐ **七动作齐全且双 invalidate**：`cd web && node -e "const s=require('fs').readFileSync('src/components/blueprint/BlueprintGatePanel.vue','utf8')+require('fs').readFileSync('src/components/blueprint/BlueprintGateRepoRow.vue','utf8'); for (const a of ['confirm','remove','add','reclassify','responsibility','boundary','upgrade']) if(!new RegExp(a,'i').test(s)) throw new Error('缺动作: '+a); console.log('seven actions OK')"` 输出 `seven actions OK`；`rg -c "invalidateQueries" web/src/components/blueprint/BlueprintGatePanel.vue` ≥ **2**（gate 与 snapshot 双失效）。
- ⭐ **零乐观更新**：`rg -n "setQueryData" web/src/components/blueprint/BlueprintGatePanel.vue` **零命中**。
- ⭐ **破坏性动作走二次确认、编辑职责不走**：`rg -n "useConfirmDialog" web/src/components/blueprint/BlueprintGatePanel.vue web/src/components/blueprint/BlueprintGateRepoRow.vue` 两文件各命中；`cd web && node -e "const s=require('fs').readFileSync('src/components/blueprint/BlueprintGateRepoRow.vue','utf8'); const i=s.indexOf('responsibility'); const around=s.slice(Math.max(0,i-500), i+800); if(/useConfirmDialog/.test(around) && !/Dialog|Textarea/.test(around)) throw new Error('编辑职责用了没有输入框的 useConfirmDialog'); if(!/Textarea/.test(s)) throw new Error('缺编辑职责的 Textarea'); console.log('edit-responsibility dialog OK')"` 输出 `edit-responsibility dialog OK`。
- ⭐ **pending 行禁用**：`rg -n "pending_research_repository_ids" web/src/components/blueprint/BlueprintGatePanel.vue` 命中；`rg -n "animate-spin|loader-2" web/src/components/blueprint/BlueprintGateRepoRow.vue` 命中；`rg -n "disabled" web/src/components/blueprint/BlueprintGateRepoRow.vue` 命中。
- ⭐ **confirm 409 两档**：`rg -n "pending_clarification|goto-unresolved" web/src/components/blueprint/BlueprintGatePanel.vue` 两个 token 全命中。
- ⭐ **复用既有仓库选择器**：`rg -n "RepositoryPicker" web/src/components/blueprint/BlueprintGatePanel.vue` 命中；`rg -n "RepoMultiSelector" web/src/components/blueprint/` **零命中**（N2 已撤销该备选）；`git diff web/src/components/workflow/RepositoryPicker.vue web/src/components/chat/RepoMultiSelector.vue` 输出**为空**（⛔ 不修改被复用组件）。
- ⭐ **`upgrade-research` 仅 indirect 行**：`cd web && node -e "const s=require('fs').readFileSync('src/components/blueprint/BlueprintGateRepoRow.vue','utf8'); const i=s.indexOf('upgrade'); const around=s.slice(Math.max(0,i-400), i+400); if(!/indirect/.test(around)) throw new Error('升级深调研未限定 indirect 行'); console.log('upgrade indirect-only OK')"` 输出 `upgrade indirect-only OK`。
- ⛔ 零 `v-html`、零颜色字面量、零 `refetchInterval`、零 `edit-block`：`rg -n "v-html|hsl\(|refetchInterval|edit-block" web/src/components/blueprint/BlueprintGatePanel.vue web/src/components/blueprint/BlueprintGateRepoRow.vue` **零命中**。
- ⛔ 零既有文件改动（本 Task 阶段）：`git diff --name-only web/src | rg -v "^web/src/components/blueprint/"` 输出**为空**；`git diff --name-only server/` 输出**为空**。
  </acceptance_criteria>
  <done>确认门两件就位：渲染条件被压缩成「gate 200」一条且不靠后端文案分支；七个动作统一「一次 POST + 双 invalidate」且零乐观更新；破坏性动作走二次确认（文案逐字）、需要输入框的走受控 Dialog；pending 行与确认主按钮的禁用态正确；confirm 409 的 `pending_clarification` 有一键跳未决线程的解药入口；`add-repo` 复用既有选择器不新造。</done>
</task>

<task type="auto">
  <name>Task 2: 页面插槽纯追加 + 组件测试 + ⭐ 可顺延性验证 + 全相位收口门</name>
  <files>web/src/pages/knowledge/blueprints/[id].vue, web/src/components/blueprint/__tests__/gatePanel.spec.ts</files>
  <read_first>.planning/phases/115-ui/115-06-SUMMARY.md（⭐ **预留插槽的确切位置**（`blueprint-gate-mount` 注释锚点与 `<div id="gate">`）、`gateAvailable` 的派生方式、`gate` query 的 `queryKey` 与 `retry: false`、`goto-unresolved` 需要页面做什么（打开侧栏 + 展开未决组 + 可选滚动））；web/src/pages/knowledge/blueprints/[id].vue **全文**（115-06 产物；⭐ 确认插槽位置与现有 `gateAvailable`/`gateQuery` 变量名，**逐字复用不重命名**）；web/src/pages/knowledge/__tests__/blueprintViewer.spec.ts（115-06 产物；本 Task 的 gate 用例要与它的 stub 清单与 i18n 键树保持一致，⛔ 不改它）；web/src/components/prompts/__tests__/PromptVersionDiff.test.ts（组件测试最规范范式）</read_first>
  <action>
**① 页面插槽纯追加**（⭐ 唯一允许改的既有文件，删除行必须为 0）：
在 115-06 预留的 `<!-- gate-panel-mount: ... -->` 注释锚点与 `<div id="gate" data-testid="blueprint-gate-mount" />` 处，把空容器替换为**包含面板的同一个容器**（⚠️ 这是「在容器内追加子节点」，不是删除容器——`id` 与 `data-testid` **必须保留**，否则 `?panel=gate` 的滚动定位会失效）：
- `v-if="gateAvailable"` 内渲染 `<BlueprintGatePanel :artifact-id="artifactId" :snapshot="gateQuery.data.value" @goto-unresolved="…" />`；
- `goto-unresolved` 的落点：打开侧栏（窄屏打开 `Sheet`）+ 展开「未决」分组（+ 可选滚动到侧栏顶部）；
- ⭐ **`gateAvailable` 的定义不改**（115-06 已按「gate 查询成功且 200」派生）；⛔ 不新增任何错误分档、不弹 toast。
- 组件用 `defineAsyncComponent` 懒加载（与页面其它重组件一致，若 115-06 未用异步则保持一致风格，SUMMARY 登记选择）。

**② 新建 `web/src/components/blueprint/__tests__/gatePanel.spec.ts`**（顶部「覆盖路径」编号清单 + `makeSnapshot(overrides)` 工厂 + 手写最小 i18n 键树 + `stubs: { RepositoryPicker: true }`）：
1. ⭐ **gate 非 200 不渲染且不报错**（在页面层测，复用 115-06 的 mount helper 或在本文件重建一份最小 mount）：gate 查询抛 `ApiError(404)` 而四个主查询正常 ⇒ `[data-testid=blueprint-gate-panel]` **不存在**、`[data-testid=blueprint-error-state]` **不存在**、toast mock **零调用**。⭐ **三种 404 的 `detail` 各跑一遍**（「确认门未开启」/「artifact 不存在」/「没有蓝图编排会话」）⇒ **行为完全相同**（证明没有靠 `detail` 文本分支）。
2. **gate 200 ⇒ 面板渲染**，仓库行数 == 快照仓库数。
3. ⭐ **七个动作各触发一次 POST**（参数化，api mock 断言 `toHaveBeenCalledTimes(1)` 与入参键）；每个成功后断言 `invalidateQueries` 被调用**至少两次**（gate + snapshot）。
4. ⭐ **pending 行禁用**：`pending_research_repository_ids` 含某仓 ⇒ 该行动作按钮 `disabled`、出现「调研中」；不含 ⇒ 可用（正反并列）。
5. ⭐ **确认主按钮在存在 pending 时 disabled + Tooltip**；无 pending ⇒ 可用。
6. ⭐ **confirm 409 两档**：`blocked_reason: 'pending_clarification'` ⇒ 出现「前往未决线程」按钮且点击 emit `goto-unresolved`；其它 `blocked_reason` ⇒ 只出 toast 回显 `detail`（断言无该按钮）。
7. **破坏性动作走二次确认**：mock `useConfirmDialog().confirm` 返回 `false` ⇒ **不发 POST**；返回 `true` ⇒ 发一次（正反并列，`remove-repo` 与 `confirm` 各一条）。
8. **编辑职责**：空/纯空格 ⇒ 提交 `disabled`；有内容 ⇒ 发一次 POST 且入参含文本。
9. **`upgrade-research` 仅 indirect**：`role: 'direct'` 的行**不出现**该按钮；`indirect` 行出现且勾选 `rerun` 后入参含 `{ rerun: true }`。
10. **`rejected-to-boundary` 在 rejected 清单为空时不渲染**。
11. ⛔ **面板内零 `refetchInterval`**（源码扫描，与守卫 spec 互为双保险）。

**③ ⭐ 可顺延性验证（本 plan 独有的一条，必须实跑并把结果写进 SUMMARY）**：
在一个**临时 stash / 分支**上把本 plan 的三处改动整体回退（两个新组件 + 页面追加），跑 `pnpm exec vitest run && pnpm type-check && pnpm build` ⇒ ⭐ **必须全绿**（证明前六个 plan 不依赖本 plan、本面板确实可独立顺延到 116）。验证完毕后恢复。
⚠️ 操作纪律：用 `git stash` 或在 detached HEAD 上验证，⛔ **不得** `git reset --hard` / `git checkout --` 丢弃未提交工作；验证结束确认工作区恢复原状（`git status` 与验证前一致）。若无法安全执行该验证，改为**静态论证**（列出「页面对本 plan 的唯一引用点是那一处 `v-if` 追加，移除后 `gateAvailable` 仍被定义但无消费方」）并在 SUMMARY 登记为静态验证。

**④ ⭐ 全相位收口门**（本 Task 收尾执行，结果全部登记进 SUMMARY）：
- `cd web && pnpm exec vitest run`（全量，六个 plan 的全部 spec + 既有）；`pnpm lint`；`pnpm type-check`；`pnpm build`；
- `pnpm exec vitest run src/__tests__/blueprint-source-guard.spec.ts` 并登记扫描面文件数；
- ⭐ **相位级边界自检**：
  - 前端**五个追加点**（`pages/knowledge/index.vue` / `ProjectMaterialsPanel.vue` / `api/index.ts` / `styles/main.css` / `locales/zh-CN.json`）逐个 `git diff <file> | rg "^-[^-]"` **为空**；
  - 加上本 plan 的第六处（`pages/knowledge/blueprints/[id].vue`，**它是本相位自己创建的文件**，不计入 §13.2 的既有面配额，但同样核算删除行为 0）；
  - ⭐ **§13.2 的 0.19 归属面零改动**：`git diff web/src/components/chat/TechPlanCard.vue web/src/components/delivery/ArtifactTimeline.vue`（以及 `RoutingDecisionPanel` / 执行时间线 / `NodeDataTab` 的实际路径）输出**全部为空**；
  - 后端相位级：`git diff --name-only server/` 只含 115-01 声明的七个文件；`makemigrations --check --dry-run` 退出码 0 且相位内**零 migration**；`cd server && uv run pytest tests/ -q` 相对基线（8546 passed / 1 failed）无新增失败；
  - `git diff web/package.json web/pnpm-lock.yaml web/pnpm-workspace.yaml` 输出**为空**。
- ⭐ **写进 STATE 的两条 Pending Todo**（由本 plan 的 SUMMARY 提出，供 `/gsd-verify-work` 或 ship 阶段落库）：① **`blueprint-gate/` 八端点中七个没有项目范围闸**（既有后端缺口，本相位只加读面不修它）；② **SC-4 的范围收窄**（「引用了本蓝图 / 关联知识」顺延 Phase 116 的知识图谱物化）。
  </action>
  <verify>
    <automated>cd web && pnpm exec vitest run && pnpm lint && pnpm type-check && pnpm build</automated>
  </verify>
  <acceptance_criteria>
- `cd web && pnpm exec vitest run` **全绿**（六个 plan 的全部 spec + 既有 ~50 个，无新增失败）；`pnpm lint` / `pnpm type-check` / `pnpm build` 通过。
- ⭐ **页面追加是纯追加且保留锚点**：`git diff web/src/pages/knowledge/blueprints/\[id\].vue | rg "^-[^-]"` **输出为空**；`rg -n "blueprint-gate-mount" web/src/pages/knowledge/blueprints/\[id\].vue` **仍命中**（`?panel=gate` 的滚动定位依赖它）；`rg -n "BlueprintGatePanel" web/src/pages/knowledge/blueprints/\[id\].vue` 命中。
- ⭐ **三种 404 行为一致**：`cd web && node -e "const s=require('fs').readFileSync('src/components/blueprint/__tests__/gatePanel.spec.ts','utf8'); const n=(s.match(/404/g)||[]).length; if(n<3) throw new Error('三种 404 的并列用例不全'); if(!/确认门未开启/.test(s)) throw new Error('缺「门未开启」那一档'); console.log('gate 404 trio OK')"` 输出 `gate 404 trio OK`。
- ⭐ **七动作各一条用例**：`rg -c "it\(|test\(" web/src/components/blueprint/__tests__/gatePanel.spec.ts` ≥ **14**；`rg -n "toHaveBeenCalledTimes\(1\)" web/src/components/blueprint/__tests__/gatePanel.spec.ts` 命中 ≥ 5。
- ⭐ **二次确认正反并列**：`rg -n "mockResolvedValue\(false\)|mockResolvedValueOnce\(false\)" web/src/components/blueprint/__tests__/gatePanel.spec.ts` 命中（确认框返回 false ⇒ 不发 POST）。
- ⭐ **confirm 409 两档**：`rg -n "pending_clarification" web/src/components/blueprint/__tests__/gatePanel.spec.ts` 命中且存在「其它 blocked_reason ⇒ 无跳转按钮」的对照（人工核对 + SUMMARY 登记用例名）。
- ⭐ **可顺延性验证结果登记**：SUMMARY 中含「回退本 plan 三处改动后 `vitest` / `type-check` / `build` 全绿」的实跑记录（或明确标注为静态论证及其理由）；且**验证后工作区已恢复**（`git status --porcelain` 与验证前一致）。
- ⭐ **相位级边界自检全绿**（逐条结果写进 SUMMARY）：五个追加点 + 本页各自 `git diff | rg "^-[^-]"` 为空；§13.2 的 0.19 归属面 `git diff` 全部为空；`git diff --name-only server/` 只含 115-01 的七个文件；`cd server && uv run python manage.py makemigrations --check --dry-run` 退出码 0；`cd server && uv run pytest tests/ -q` 相对基线无新增失败；`git diff web/package.json web/pnpm-lock.yaml web/pnpm-workspace.yaml` 为空。
- ⭐ **源码守卫最终态**：`pnpm exec vitest run src/__tests__/blueprint-source-guard.spec.ts` 绿；SUMMARY 登记扫描面文件数（应 ≥ 37）。
- ⛔ 零 `v-html`、零 `refetchInterval`、零 `edit-block`：`rg -n "v-html|refetchInterval|edit-block" web/src/components/blueprint/ web/src/pages/knowledge/blueprints/` **零命中**（`refetchInterval` 的唯一合法出处 `composables/useBlueprintLive.ts` 不在这两个目录内）。
  </acceptance_criteria>
  <done>确认门面板挂上页面且是纯追加（锚点保留）；三种 404 行为完全一致证明没有靠后端文案分支；七个动作、pending 禁用、二次确认正反、confirm 409 两档各有用例；⭐ 可顺延性经实跑验证（回退后全绿）；相位级收口门（前端五个追加点 + §13.2 零改动 + 后端七文件 + 零 migration + 全量 pytest 无新增失败 + 前端四道门）全部达标，两条 Pending Todo 已在 SUMMARY 提出。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `blueprint-gate/` 的状态码 → 权限/存在性推断 | 该链八端点里七个**没有项目范围闸**，状态码不携带权限信息 |
| 后端错误 `detail` 文本 → 前端分支 | 把文案当协议，后端改一个字前端就错 |
| 确认锁定动作 → 不可逆的仓库集锁定 | 误触后必须重开确认门；且操作者会被记入评审人名单 |
| 快照的 `pending_research_repository_ids` → 动作可用性 | 漏判会让用户在调研中提交动作，后端拒绝且体验断裂 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115-59 | Elevation of Privilege | 据 gate 的 404 推断「无权限」或反之把它当权限来源 | mitigate | 渲染条件压缩成「200 与否」一条；⛔ 不进错误分档、不 toast；组件 docstring 写明该链无闸的实读证据；**三种 404 行为一致**的并列用例（P-10） |
| T-115-60 | Tampering | 用后端 `detail` 文本做分支判定 ⇒ 后端改文案即前端逻辑失效 | mitigate | 源码扫描禁中文文案分支 + 三种 404 并列用例 |
| T-115-61 | Tampering | 确认锁定误触 ⇒ 不可逆锁定仓库集且操作者被记入评审人名单 | mitigate | `useConfirmDialog` 二次确认（文案逐字照 §16，明写「后续变更必须重开确认门」与「你将被记入评审人名单」）+ 「确认框返回 false ⇒ 不发 POST」的用例 |
| T-115-62 | Tampering | 移除仓误触 ⇒ 调研结论与职责被移出方案 | mitigate | 同款二次确认 + 正文明写后果（可沉淀为章程禁区候选）+ 正反用例 |
| T-115-63 | Denial of Service | pending 调研中的仓仍可提交动作 ⇒ 后端拒绝，用户反复重试 | mitigate | 行内动作 `disabled` + spin + 「调研中」；确认主按钮 `disabled` + Tooltip 回显阻塞语义；正反用例 |
| T-115-64 | Denial of Service | `confirm/` 409 `pending_clarification` 只显示一句提示 ⇒ 用户找不到未决线程在哪 | mitigate | 一键「前往未决线程」emit `goto-unresolved` → 页面打开侧栏未决组；用例断言按钮存在且 emit |
| T-115-65 | Tampering | 乐观更新确认门状态 ⇒ 与后端锁定结果不一致 | mitigate | 一次 POST + 双 invalidate；⛔ 零 `setQueryData`；源码扫描 |
| T-115-66 | Tampering | 本 plan 的改动破坏页面（让「可顺延」变成假的） | mitigate | ⭐ 页面改动限于预留插槽处的纯追加（锚点保留）+ **回退后全绿的可顺延性实跑验证** |
| T-115-SC | Tampering | 依赖供应链 | accept | 零新增依赖；`add-repo` 复用既有 `RepositoryPicker`（⛔ 不修改它）；`pnpm build` 最终把关 |
</threat_model>

<verification>
- `cd web && pnpm exec vitest run` 全绿；`pnpm lint` / `pnpm type-check` / `pnpm build` 通过。
- `git diff web/src/pages/knowledge/blueprints/[id].vue | rg "^-[^-]"` 为空；`rg -n "blueprint-gate-mount" web/src/pages/knowledge/blueprints/[id].vue` 仍命中。
- `git diff web/src/components/workflow/RepositoryPicker.vue web/src/components/chat/RepoMultiSelector.vue` 输出为空。
- ⭐ **可顺延性**：回退本 plan 三处改动后 `pnpm exec vitest run && pnpm type-check && pnpm build` 全绿（实跑或静态论证，结果登记 SUMMARY），且验证后工作区恢复原状。
- ⭐ **相位级收口**：前端五个追加点 + 本页各自删除行为 0；§13.2 的 0.19 归属面 `git diff` 全部为空；`git diff --name-only server/` 只含 115-01 的七个文件；`cd server && uv run python manage.py makemigrations --check --dry-run` 退出码 0；`cd server && uv run pytest tests/ -q` 相对基线（8546/1）无新增失败；`git diff web/package.json web/pnpm-lock.yaml web/pnpm-workspace.yaml` 为空。
- `pnpm exec vitest run src/__tests__/blueprint-source-guard.spec.ts` 绿（扫描面 ≥ 37 个文件）。
- `rg -n "v-html|refetchInterval|edit-block|edit-blocks|editBlocks" web/src/components/blueprint/ web/src/pages/knowledge/blueprints/` 零命中。
</verification>

<success_criteria>
- ⭐ **FLOW-03 在 UI 上第一次可达**：确认门的快照与七个动作全部有界面，用户能确认仓库集与职责并进入 113 的阶段 2 —— 此前 112 的八个端点全仓零前端，整条链在界面上断在第一关。
- ⭐ **本面板不给页面引入任何新的失败模式**：渲染条件只有「gate 200」一条，三种 404 行为完全一致（不靠后端文案分支）、不报错、不 toast、不进错误分档；页面的权限判定仍只由四个有闸的主查询承担。
- ⭐ **「可独立顺延」是被验证过的事实而不是声明**：回退本 plan 三处改动后前端四道门仍全绿。
- **破坏性动作有两层防护**（视觉分离由面板结构承担 + 二次确认文案逐字照 §16），且「确认框取消 ⇒ 不发 POST」有正反用例；`confirm` 409 的 `pending_clarification` 有一键解药入口。
- **相位收口**：前端五个追加点全部纯追加、§13.2 的 0.19 归属面零改动、后端只动 115-01 的七个文件且零 migration、全量后端 pytest 相对基线无新增失败、前端 vitest/lint/type-check/build 四道门全绿、源码守卫三条断言在完整代码面下生效。
- **两条 Pending Todo 被显式提出**：`blueprint-gate/` 的项目范围闸缺口（既有后端问题，不在本相位修）与 SC-4 的范围收窄（顺延 116）。
</success_criteria>

<output>
完成后创建 `.planning/phases/115-ui/115-07-SUMMARY.md`，provides 至少包含：
- **两个组件的 props/emits 逐字**与七个动作的**端点 → 入参 → 状态码 → toast** 映射表；
- ⭐ **「gate 非 200 ⇒ 不渲染且不报错」的落地方式**与三种 404 并列用例名（证明没有靠 `detail` 分支）；
- `add-repo` 复用 `RepositoryPicker`（N2 已定夺，`RepoMultiSelector` 备选撤销）的**接线形状**：本地 `ref<string[]>` + `v-model`，以及多选提交时逐个调 `add-repo` 的顺序与失败处理；
- ⭐ **可顺延性验证记录**：回退三处改动后 `vitest` / `type-check` / `build` 的实跑结果（或静态论证及理由），以及工作区恢复确认；
- ⭐ **相位级收口报告**：前端五个追加点 + 本页的删除行核算、§13.2 零改动核对、后端七文件与零 migration、全量 pytest 与基线（8546/1）的差异、前端四道门结果、源码守卫扫描面文件数；
- ⭐ **提请写入 STATE 的两条 Pending Todo**：① `blueprint-gate/` 八端点中七个无项目范围闸（既有后端缺口，附实读位置 `blueprint_gate_views.py:385,511`）；② SC-4 范围收窄（「引用了本蓝图 / 关联知识」顺延 Phase 116 知识图谱物化）；
- ⭐ **UAT 清单**（确认门专属）：segmented control 的即时提交手感、`RepositoryPicker` 的弹出与选择流、pending 行的 spin 动效、二次确认弹窗的文案与焦点、`?panel=gate` 的滚动定位与 2s ring 高亮；
- **FLOW-03 → 界面可达性的说明**（本面板是相对 ROADMAP SC 的范围增量，若未来需顺延，顺延目标与不得丢弃的理由）。
</output>
