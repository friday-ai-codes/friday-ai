---
phase: 112-1
plan: 03
type: execute
wave: 2
depends_on: [112-01]
files_modified:
  - server/services/process_runtime/blueprint_charter_match.py
  - server/services/process_runtime/blueprint_route_history.py
  - server/services/process_runtime/blueprint_route.py
  - server/tests/services/process_runtime/test_blueprint_charter_match.py
  - server/tests/services/process_runtime/test_blueprint_route_breakdown.py
  - server/tests/services/process_runtime/test_blueprint_route_stage.py
autonomous: true
requirements: [CHARTER-02, FLOW-04]
prohibitions:
  - "禁止修改 server/codegraph/services/repo_router_v2.py 任何一行（STATE.md 并行纪律 1 + DESIGN §13.2 + §5.7 第 4 条三重明令）——只以 RepoRouterV2.route 的原样输出为输入"
  - "禁止把 charter/history 证据塞进 RepoRouterV2 的 Stage1 prompt 或改其内部：融合只在 adapter 层"
  - "禁止修改 process_runtime 六冻结文件（decompose_segments.py / research_adapter.py / architect_merge_adapter.py / merged_plan.py / clarify_adapter.py / render.py）；repo_router_adapter.py 与 recall_adapter.py 只读参考，不改"
  - "禁止修改 server/services/process_runtime/resume.py（蓝图续驱走 112-05 的 blueprint_resume.py）"
  - "禁止修改 ConvergenceSessionEvent 既有事件类型/字段；只消费 112-01 注册的 blueprint.route.scored 常量，不再改 event_taxonomy.py"
  - "禁止修改 settings_service 既有 getter；只消费 112-01 的 aget_json_setting"
  - "禁止修改 charter_service.py / repositories 模型（章程只读；回灌草案在 112-05）"
  - "禁止修改 builtin_processes.py（stage 注册在 112-05）与 blueprint_lifecycle_service.py（112-02 独占）"
  - "禁止改动 repo_associations.routing_evidence 的字段形状（只往其 object 内加键，不改 schema 定义）"
  - "禁止伪造 acting user 提权：created_by 为空时 history_match 显式标记不可得，不得静默当 0 分"
must_haves:
  truths:
    - "blueprint_route 输出每个候选带 breakdown = {router_base, charter_match, history_match} 且三项加权之和逐位等于该候选总分（可拆解，CHARTER-02）"
    - "章程 owned_domains（含 status=planned）命中的仓即便 RepoRouterV2 未召回也能作为候选补入（router_base=0.0、charter_match>0），复现高三提分专项：onion-learning 不因能力树无节点被淘汰"
    - "命中 boundaries 的候选 charter_match 为负（降权）；evolution=maintenance_only/deprecated 的候选被额外降权，且降权理由进 breakdown 证据"
    - "命中禁区仍保留在候选列表里的仓，必须携带显式 `evidence.boundary_override_reason`（优先取 RepoRouterV2 的 `reasoning`，缺失时经单次 sanity-check LLM 调用补齐）；两条来源都拿不到时该候选被打 `evidence.unjustified_boundary_hit = True`（ROADMAP SC2 后半：LLM 保留禁区候选必须给显式理由）"
    - "`_h_bp_route` 写入 `stage_state[\"routing\"]` 的摘要字段清单是 112-04/112-05 的唯一读取契约（字段逐条固定，见 objective 的契约表）"
    - "intent 驱动权重：greenfield 权重下章程+历史合计权重高于 router_base；brownfield/fix 权重下 router_base 占主导（权重来自 SystemSetting，缺失回默认）"
    - "history_match 经 delivery knowledge（entity_kinds=[code_change, tech_plan]）单次检索得出，落 RetrievalTrace 埋点；无 acting user 时 history_match=0.0 且 breakdown 标 history_match_unavailable=no_acting_user"
    - "路由结果写 ConvergenceSessionEvent(blueprint.route.scored)，payload 含 router_version 与逐候选三分量，供 115 展开"
  artifacts:
    - path: "server/services/process_runtime/blueprint_charter_match.py"
      provides: "章程分量纯函数（owned/boundaries/evolution 三规则）+ 章程批量读取 + 章程候选补入清单"
      contains: "def score_charter_match"
    - path: "server/services/process_runtime/blueprint_route_history.py"
      provides: "history_match 分量（delivery knowledge 检索 + RetrievalTrace 埋点 + 无 actor 降级标记）"
      contains: "entity_kinds"
    - path: "server/services/process_runtime/blueprint_route.py"
      provides: "BlueprintRouteAdapter：RepoRouterV2 原样调用 + 三分量融合 + intent 加权 + breakdown 组装 + 事件"
      contains: "build_score_breakdown"
    - path: "server/tests/services/process_runtime/test_blueprint_route_breakdown.py"
      provides: "机制级断言：分量之和==总分、章程分量对排序的可拆解影响、onion-learning 靠 owned(planned) 进候选"
      contains: "onion-learning"
    - path: "server/tests/services/process_runtime/test_blueprint_route_stage.py"
      provides: "禁区候选必须有 boundary_override_reason 或被标 unjustified_boundary_hit 的机制级断言（SC2 后半）"
      contains: "boundary_override_reason"
  key_links:
    - from: "server/services/process_runtime/blueprint_route.py"
      to: "server/codegraph/services/repo_router_v2.py"
      via: "只调 RepoRouterV2.route(query, top_k=, repository_ids=, use_llm=True) 取原样输出，零改动"
      pattern: "RepoRouterV2"
    - from: "server/services/process_runtime/blueprint_route.py"
      to: "server/system/settings_service.py"
      via: "aget_json_setting(SettingKeys.BLUEPRINT_ROUTE_WEIGHTS) 按 intent 取权重向量"
      pattern: "BLUEPRINT_ROUTE_WEIGHTS"
    - from: "server/services/process_runtime/blueprint_route_history.py"
      to: "server/knowledge/retrieval.py"
      via: "DeliveryKnowledgeSearchService.search_similar(entity_kinds=[code_change, tech_plan], user=actor)"
      pattern: "search_similar"
---

<objective>
落地双面路由：以 `RepoRouterV2` 的原样输出为 `router_base` 单一不可拆分量，在 adapter 层加两个可解释分量——`charter_match`（章程 owned_domains 含 planned / boundaries 命中判负 / evolution 降权）与 `history_match`（delivery knowledge 召回「同类需求近期实际合进哪个仓」）——按 feature_point `intent` 取权重向量加权，组装成三项之和恒等于总分的 `breakdown`，并写 `ConvergenceSessionEvent` 供 115 展开。
额外关键机制：章程 owned(planned) 命中的仓**可作为候选补入**，这是高三提分专项 case（onion-learning 不因能力树无培优节点被淘汰）的机制解。

第二个关键机制（ROADMAP SC2 后半）：命中章程禁区的候选**只降权不淘汰**，因此凡是留在候选列表里的禁区命中仓，都必须带一条**显式保留理由** `evidence.boundary_override_reason`；拿不到理由即打 `evidence.unjustified_boundary_hit` 标记，不允许「命中禁区却无人解释为何还留着」。

Purpose: CHARTER-02 全量 + FLOW-04 的「direct/indirect 各带结构化选仓理由与证据」的证据面 + SC2 后半的显式理由要求。
Output: 3 个新 process_runtime 文件（章程分量 / 历史分量 / 路由 adapter）+ 3 个测试文件（含机制级断言）。

## `stage_state["routing"]` 契约（B4：112-04 与 112-05 的唯一读取面）

`BlueprintRouteAdapter.route()` 的返回值就是本摘要；112-05 注册的 handler 以
`StageOutcome(event="routed", stage_state_update={"routing": <本摘要>})` 落盘（`engine.py:103-105` 浅合并），
112-04 的 `dispatch` 与 112-05 的确认门快照**只读下列字段，不得读其它键**：

| 键 | 类型 | 读取方 | 说明 |
|---|---|---|---|
| `router_version` | `str` | 04 事件 / 05 快照 | `RepoRouterV2` 原样透传（`v2` / `v2_stage0_only` / `v1_fallback`）；无候选短路时为 `"skipped"` |
| `auto_selected` | `bool` | 05 快照 | 原样透传 |
| `intent` | `str` | 04 分桶 | 主导 intent ∈ `greenfield\|brownfield\|fix` |
| `weights_used` | `dict` | 05 快照 | `{router_base, charter_match, history_match}` 三个 float |
| `charter_supplement_count` | `int` | 事件 | 章程补入候选数 |
| `unjustified_boundary_hit_count` | `int` | 事件 / 05 快照 | 命中禁区但无显式理由的候选数 |
| `candidates[]` | `list[dict]` | 04 分桶 / 05 快照 | 逐候选，字段见下 |

`candidates[]` 每项固定七键（**112-04 按 `role_suggestion` + `confidence` 分 deep/light 桶，按 `repository_id` 去重，`confidence` 直接作为 `create_tasks_for_session` 的 `routed_confidence` 入参**）：

| 键 | 类型 | 说明 |
|---|---|---|
| `repository_id` | `str` | |
| `repository_name` | `str` | |
| `role_suggestion` | `"direct" \| "indirect"` | 路由期初判，**确定规则**：`confidence == "high"` 或 `breakdown["charter_match"] > 0` → `direct`；否则 `indirect`（保守：不确定的仓走轻量合成） |
| `confidence` | `"high" \| "medium" \| "low"` | `RepoRouterV2` 原样透传；章程补入候选为 `"low"` |
| `total` | `float` | 等于 `breakdown` 三项之和 |
| `breakdown` | `dict` | `{router_base, charter_match, history_match}`（均为**加权后**值） |
| `evidence` | `dict` | `{router_version, auto_selected, confidence, reasoning, matched_node_paths, charter_source, charter_version, matched_domains, violated_boundaries, penalty_reasons, history_match_unavailable, boundary_override_reason, unjustified_boundary_hit}` |

顶层另有 `citations`（章程/知识实体引用条目，115 消费），**不属于 04/05 的读取面**。
</objective>

<execution_context>
工作目录：worktree 根。**与 112-02 同 wave 并行**——本 plan 独占 `blueprint_charter_match.py` / `blueprint_route_history.py` / `blueprint_route.py` 三个新文件，绝不碰 `blueprint_spec_gate.py` / `blueprint_ambiguity_score.py` / `blueprint_intent_classify.py` / `blueprint_lifecycle_service.py`（112-02 独占）。`repo_router_v2.py` 是三重明令的冻结面，只读调用。
</execution_context>

<context>
@.planning/phases/112-1/112-CONTEXT.md（锁定：blueprint_route 新文件内嵌调用 RepoRouterV2；charter_match 三规则与 intent 加权；history_match 用 delivery knowledge kinds=code_change/tech_plan；breakdown = router_base + charter_match + history_match 三项之和等于总分且记 router_version；章程被引用产 citation.source_type=repo_charter；断言写机制级）
@.planning/phases/112-1/112-RESEARCH-ROUTING.md §1（RepoRouteResultV2/RepoRouteCandidateV2 逐字段真实名与 score 已 min 到 1.0、router_version 三取值、v1_fallback 时 matched_node_paths 恒空的可观测性要求）、§2（RepoCharter 字段与 normalize 白名单定义的 owned_domains/boundaries/evolution 形状；charter 无读取函数走裸 ORM `RepoCharter.objects.select_related("repository").aget(...)`）、§5（search_similar 参数名是 entity_kinds 不是 kinds；kinds 实际值逐字一致；单次调用覆盖两分路；未知 kind 绝不回退全量；user 必填 fail-closed → 无 actor 必须显式降级标记）
@.planning/phases/112-1/112-PATTERNS.md 第 3 类（repo_router_adapter 骨架：空 query 短路保持 dict 形状恒定 / 显式逐字段映射不 to_dict 整包透传 / 候选范围三级解析 / @sync_to_async 私有方法；recall_adapter 的 RetrievalTrace 埋点 payload 形状）、第 10 类（单查后按 kind 截断、top_k 超采样、用候选仓 repository_ids 收窄）
@.planning/phases/112-1/112-01-SUMMARY.md（本 plan 前置：feature_points[].intent 枚举、BLUEPRINT_ROUTE_WEIGHTS 默认形状、EVENT_BLUEPRINT_ROUTE_SCORED）
@server/codegraph/services/repo_router_v2.py（L61-95 输出契约、L99-107 route 签名——**冻结只读**）
@server/services/process_runtime/repo_router_adapter.py（L8-10 边界 docstring、L36-38 空 query 短路、L44-51 显式映射、L58-82 候选范围与 sync_to_async——只读参考）
@server/services/process_runtime/recall_adapter.py（L66-126 主流程与异常吞掉、L97-107 search_similar 调用形、L180-232 _record_trace 埋点、L234-262 按 kind 截断与 _resolve_actor——只读参考）
@server/repositories/models.py（L1091-1161 RepoCharter 字段）
@server/knowledge/models.py（L34-54 EntityKind 取值）
@server/tests/fixtures/blueprint_golden/gaokao_boost.json（高三提分专项 case 的 feature_point 与目标仓期望，intent 已由 112-01 补齐）
@server/tests/services/process_runtime/test_classify_stage.py（stage 测试范式）
</context>

<tasks>

<task type="auto">
  <name>Task 1: charter_match 分量（三规则纯函数 + 章程读取 + 章程候选补入清单）</name>
  <files>server/services/process_runtime/blueprint_charter_match.py, server/tests/services/process_runtime/test_blueprint_charter_match.py</files>
  <read_first>server/repositories/models.py L1091-1161（RepoCharter 全字段与 Source/Evolution 枚举）；server/repositories/services/charter_service.py L75-157（normalize_charter_draft 定义的 owned_domains/boundaries/placement_preferences 白名单形状——分量读取口径的唯一权威）；server/services/process_runtime/repo_router_adapter.py L58-82（@sync_to_async 私有 ORM 方法范式）；server/services/process_runtime/wave_layering.py（本仓纯函数模块的组织风格，若存在）</read_first>
  <action>
新建 `server/services/process_runtime/blueprint_charter_match.py`。模块 docstring 三段：「本模块只算章程分量，不碰 RepoRouterV2（§13.2）」/「读章程正式字段（ai_draft 也在正式字段上），breakdown 证据里带 source 与 version 让 115 能标注『依据未经人工确认的草案』」/「纯函数与 ORM 读严格分离，前者可单测、可被 golden set 评估」。

`__all__ = ["score_charter_match", "aload_charters", "acollect_charter_candidates", "CharterMatchResult", "DEFAULT_CHARTER_RULES"]`。

- `DEFAULT_CHARTER_RULES`（模块常量，作为规则强度基准）：`{"owned_implemented": 1.0, "owned_planned": 0.7, "boundary_hit": -1.0, "evolution_maintenance_only": -0.4, "evolution_deprecated": -0.8}`。注释写清「planned 低于 implemented 但显著为正——这是 greenfield 场景让规划中领域的仓进候选的机制」。
- `@dataclass(frozen=True) class CharterMatchResult`：`score: float`（clamp `[-1.0, 1.0]`）、`matched_domains: list[dict]`（每项 `{domain, status}`）、`violated_boundaries: list[str]`、`evolution: str`、`penalty_reasons: list[str]`、`charter_source: str`、`charter_version: int`、`citation_ids: list[str]`。
- `def score_charter_match(charter: dict | None, *, query_terms: list[str], rules: dict | None = None) -> CharterMatchResult`（**纯函数**，入参是已取出的 dict 而非 ORM 对象，便于无 DB 单测）：
  1. `charter` 为 None/空 → `score=0.0` 且 `penalty_reasons=["no_charter"]`（无章程不是负分，只是无证据）。
  2. `owned_domains` 逐项：`domain` 文本与 `query_terms` 做**大小写无关的子串/分词命中**（不引入外部依赖，实现为规范化后的 token 交集判定）；命中即按 `status` 取 `owned_implemented` / `owned_planned`（非法 status 按 `implemented` 处理，与 `normalize_charter_draft` 回退一致），累加并记入 `matched_domains`；同项 `citations` 进 `citation_ids`。
  3. `boundaries` 逐项：`rule` 文本命中 query_terms → 加 `boundary_hit`（负值）并记入 `violated_boundaries`。
  4. `evolution == "maintenance_only"` / `"deprecated"` → 加对应负值并记 `penalty_reasons`。
  5. 多命中做上界收敛：正分累加后 clamp 到 `1.0`，总分 clamp 到 `[-1.0, 1.0]`（避免章程写得越长分越高）。
- `async def aload_charters(repository_ids: list[str]) -> dict[str, dict]`：`@sync_to_async` 私有实现里 `RepoCharter.objects.filter(repository_id__in=...).values(...)` 一次取全（避免 N+1 与 async 裸 lazy-FK），返回 `{repository_id: {owned_domains, boundaries, evolution, source, version, positioning}}`。整段 `except Exception` → 返 `{}` 并 warning（best-effort，路由不因章程读失败而中断）。
- `async def acollect_charter_candidates(*, query_terms: list[str], exclude_repository_ids: set[str], repository_ids: list[str] | None = None, limit: int = 5) -> list[dict]`：**高三提分 case 的机制解**——扫章程（可用 `repository_ids` 收窄；`owned_domains` 命中 query_terms 且 `status ∈ {implemented, planned}`）找出 `exclude_repository_ids` 之外的仓，返回 `[{repository_id, repository_name, charter_match_raw, matched_domains, source: "charter_supplement"}]`，按 `charter_match_raw` 降序取前 `limit`。仓名经 `select_related("repository").values("repository__name")` 取，绝不在 async 里裸访问 FK。

新建 `server/tests/services/process_runtime/test_blueprint_charter_match.py`：
- 纯函数（无 DB，`@pytest.mark.asyncio` 不需要）：owned implemented 命中 > owned planned 命中 > 0；planned 命中分数严格大于 0（**这条断言直接锁住「planned 也能把仓推进候选」的机制**）；boundaries 命中使总分为负；同时 owned 命中 + boundary 命中 → 分数被抵消为负或接近 0 且 `violated_boundaries` 非空；`evolution=maintenance_only`/`deprecated` 各自降权且 `penalty_reasons` 记录原因；charter=None → 0.0 且 `penalty_reasons==["no_charter"]`；owned_domains 非 list / item 非 dict / status 非法值 → 不抛且按回退处理；正分多命中被 clamp 到 1.0。
- DB 部分（`@pytest.mark.django_db(transaction=True)` + `@pytest.mark.asyncio`）：`aload_charters` 一次取多仓且缺章程仓不出现在结果里；`acollect_charter_candidates` 在「章程 owned_domains 含 `status=planned` 的培优/学习领域」的仓上返回该仓，且 `exclude_repository_ids` 内的仓被排除；`repository_ids` 收窄生效。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/process_runtime/test_blueprint_charter_match.py -x -q</automated>
  </verify>
  <acceptance_criteria>
- `cd server && uv run pytest tests/services/process_runtime/test_blueprint_charter_match.py -x -q` 全绿。
- `rg -n "def score_charter_match" server/services/process_runtime/blueprint_charter_match.py` 命中；`rg -n "owned_planned" server/services/process_runtime/blueprint_charter_match.py` 命中（planned 规则显式存在）。
- `rg -n "async def acollect_charter_candidates" server/services/process_runtime/blueprint_charter_match.py` 命中（章程候选补入机制存在）。
- `rg -n "repo_router_v2|RepoRouterV2" server/services/process_runtime/blueprint_charter_match.py` 零命中（章程分量与路由器解耦）。
- `rg -n "sync_to_async" server/services/process_runtime/blueprint_charter_match.py` 命中（ORM 读经 sync_to_async）。
  </acceptance_criteria>
  <done>章程三规则成为可单测纯函数（planned 正分被断言锁死）；章程可批量读取并能独立产出补入候选清单——高三提分 case 的机制前提就位。</done>
</task>

<task type="auto">
  <name>Task 2: history_match 分量（delivery knowledge 检索 + RetrievalTrace 埋点 + 无 actor 显式降级）</name>
  <files>server/services/process_runtime/blueprint_route_history.py</files>
  <read_first>server/services/process_runtime/recall_adapter.py 全文（L66-126 主流程、L97-107 search_similar 调用形、L128-178 配置逐项降级 + warning、L180-232 _record_trace payload 形状、L234-252 按 kind 截断、L254-262 _resolve_actor 不伪造 actor——只读参考，本文件独立实现）；server/knowledge/retrieval.py L30-47（search_similar 全部 keyword-only 参数与 user fail-closed）；server/knowledge/models.py L34-54（EntityKind 值）；server/tests/knowledge/test_vector_recall.py L260-316（entity_kinds 分路白名单行为与未知 kind 不回退全量的断言）</read_first>
  <action>
新建 `server/services/process_runtime/blueprint_route_history.py`。模块 docstring 三段：「本模块算 history_match 分量：『同类需求近期实际合进哪个仓』」/「best-effort：检索异常/无 actor 一律降级返回可解释结果，绝不阻断路由」/「LOGGING-SPEC 强制：新增召回必写 RetrievalTrace 并上报召回条数/耗时/score」。

`__all__ = ["ascore_history_match", "HistoryMatchResult", "HISTORY_ENTITY_KINDS"]`；`HISTORY_ENTITY_KINDS = ["code_change", "tech_plan"]`（逐字对齐 `EntityKind` 实际值，CONTEXT 锁定；**单次调用即覆盖 demand/code 两条分路，不拆两次查**）。

- `@dataclass(frozen=True) class HistoryMatchResult`：`scores: dict[str, float]`（repository_id → `[0,1]` 归一分）、`hit_counts: dict[str, int]`、`top_scores: dict[str, float]`、`citation_ids: list[str]`、`unavailable_reason: str`（`""` 表示可得；无 actor 时为 `"no_acting_user"`；检索异常时为 `"retrieval_error"`）。
- `async def ascore_history_match(*, query: str, candidate_repository_ids: list[str], session, top_k_multiplier: int = 2) -> HistoryMatchResult`：
  1. `query` 为空或候选为空 → 返回全 0 + `unavailable_reason="empty_query"`（保持结果形状恒定，下游无需判空分支）。
  2. actor 解析：`@sync_to_async` 私有 `_resolve_actor(session)` 取 `session.created_by`；**为空直接返回 `unavailable_reason="no_acting_user"` + 全 0**（绝不伪造/提权，且不能静默当 0 分——breakdown 会据此打标记）。
  3. `await DeliveryKnowledgeSearchService().search_similar(query, user=actor, top_k=len(candidate_repository_ids) * top_k_multiplier + 10, entity_kinds=HISTORY_ENTITY_KINDS, repository_ids=candidate_repository_ids, include_document_kind=False)`（用候选仓收窄召回，`top_k` 超采样留截断余量）。
  4. 聚合：按结果的 repository 归并，`hit_counts` 计数、`top_scores` 取最高 score；`scores[repo] = clamp(top_score, 0, 1)`（**用 top_score 而非命中数，避免历史噪声多的仓虚高**；实现里写注释说明取舍）；`citation_ids` 收集来源实体 id（供 `citation.source_type=knowledge_entity` 引用）。
  5. 整段 try/except → 异常先 `redact_secrets_in_text` 再 warning，返回全 0 + `unavailable_reason="retrieval_error"`。
  6. 埋点私有方法 `_record_trace`：`arecord_retrieval_trace(None, kind=RetrievalTrace.Kind.CHUNK, payload={"source": "blueprint_route_history", "session_id": str(session.id), "kinds": HISTORY_ENTITY_KINDS, "result_count": ..., "per_repo_counts": ..., "scores": ..., "top_score": ..., "duration_ms": ...})`，整段吞异常。**query 原文与召回正文绝不进 payload/日志，只记指标与关联键。**
  7. structlog 三事件 `blueprint_route_history_started/completed/failed`，带 `category="sampling"`、`component="process_runtime"`、`duration_ms`；completed 带 `candidate_count`/`result_count`/`unavailable_reason`。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/process_runtime/test_blueprint_charter_match.py -x -q && uv run ruff check services/process_runtime/blueprint_route_history.py</automated>
  </verify>
  <acceptance_criteria>
- `rg -n "entity_kinds=HISTORY_ENTITY_KINDS" server/services/process_runtime/blueprint_route_history.py` 命中（参数名用 entity_kinds，非 kinds）。
- `rg -n 'HISTORY_ENTITY_KINDS = \["code_change", "tech_plan"\]' server/services/process_runtime/blueprint_route_history.py` 命中。
- `rg -n "arecord_retrieval_trace" server/services/process_runtime/blueprint_route_history.py` 命中（RetrievalTrace 埋点强制项）。
- `rg -n "no_acting_user" server/services/process_runtime/blueprint_route_history.py` 命中（无 actor 显式降级标记，非静默 0 分）。
- W1 边界：本 task 只跑上面 verify 里的单文件 + ruff，**不跑 `tests/services/process_runtime/` 目录全量**（112-02 同 wave 的新文件未完成会导致收集失败/假红）；`blueprint_route_history.py` 的行为断言由 Task 3 的 `test_blueprint_route_stage.py` 以 `AsyncMock` 注入路径覆盖。
- `rg -n "redact_secrets_in_text" server/services/process_runtime/blueprint_route_history.py` 命中。
- `rg -n "\"query\"|query=query," server/services/process_runtime/blueprint_route_history.py | rg "payload"` 零命中（query 原文不进 trace payload）。
  </acceptance_criteria>
  <done>history_match 单次检索覆盖两条分路、结果形状恒定、无 actor 与检索异常各有显式降级原因、召回埋点齐备且不泄漏正文。</done>
</task>

<task type="auto">
  <name>Task 3: BlueprintRouteAdapter —— 三分量融合、intent 加权、breakdown 组装与机制级验收</name>
  <files>server/services/process_runtime/blueprint_route.py, server/tests/services/process_runtime/test_blueprint_route_breakdown.py, server/tests/services/process_runtime/test_blueprint_route_stage.py</files>
  <read_first>server/services/process_runtime/repo_router_adapter.py 全文（骨架：docstring 边界声明、空 query 短路、显式逐字段映射、候选范围三级解析——只读参考）；server/codegraph/services/repo_router_v2.py L61-107（候选/结果字段与 route 签名，冻结只读）；server/delivery/models/convergence_session.py L80-151（stage_state 只读视图与写入纪律）；server/delivery/services/event_taxonomy.py（112-01 新增的 EVENT_BLUEPRINT_ROUTE_SCORED）；server/tests/fixtures/blueprint_golden/gaokao_boost.json（高三提分 case 的三条 feature_point 与目标仓）</read_first>
  <action>
新建 `server/services/process_runtime/blueprint_route.py`。模块 docstring 四段：「本 adapter **不重写路由逻辑**——RepoRouterV2 原样输出即 `router_base`，融合只在本层」/「§13.2：repo_router_v2.py 零改动；`router_base` 是单一不可分量（RepoRouteCandidateV2 无 breakdown 字段且 score 已 min 到 1.0），Phase 105 分数分解落地后只需把 router_base 展开为内部信号，本层组装契约不变」/「INV-6：本文件零 ORM 写，stage_state 写入由 handler 的 StageOutcome 承担」/「降级可观测：router_version 必须进 breakdown，v1_fallback 时 matched_node_paths 恒空需可解释」。

`__all__ = ["BlueprintRouteAdapter", "build_score_breakdown", "aload_route_weights", "resolve_boundary_override", "DEFAULT_ROUTE_WEIGHTS"]`。

- `DEFAULT_ROUTE_WEIGHTS`（与 112-01 SettingKeys 注释逐字一致）：`{"greenfield": {"router_base": 0.40, "charter_match": 0.35, "history_match": 0.25}, "brownfield": {"router_base": 0.60, "charter_match": 0.20, "history_match": 0.20}, "fix": {"router_base": 0.70, "charter_match": 0.15, "history_match": 0.15}}`。
- `async def aload_route_weights() -> dict`：`aget_json_setting(SettingKeys.BLUEPRINT_ROUTE_WEIGHTS, DEFAULT_ROUTE_WEIGHTS)` + 逐 intent 逐分量 `float()` 强转、负值取 0、缺 intent 键回该 intent 默认、非 dict 回整段默认；整段 `except` 回默认，绝不抛。
- `def build_score_breakdown(*, router_base: float, charter_match: float, history_match: float, weights: dict, evidence: dict) -> dict`（**纯函数，本 plan 最重要的可验证单元**）：返回
  ```
  {"router_base": w_rb * router_base, "charter_match": w_cm * charter_match, "history_match": w_hm * history_match,
   "total": <三项之和>, "weights": {...}, "evidence": {...}}
  ```
  实现纪律：`total` 必须由三项**同一批浮点值求和**得出（`total = sum(components.values())`），不得另算一遍——「各项之和等于总分」是恒等式而非近似；`evidence` 透传 `router_version` / `auto_selected` / `confidence` / `reasoning` / `matched_node_paths` / `charter_source` / `charter_version` / `matched_domains` / `violated_boundaries` / `penalty_reasons` / `history_match_unavailable`（无 actor 或检索失败时写入原因串）/ `boundary_override_reason` / `unjustified_boundary_hit`（后两键由下条的 `resolve_boundary_override` 填，字段清单与 objective 的契约表逐字一致）。
- `def resolve_boundary_override(*, violated_boundaries: list[str], router_reasoning: str, llm_reason: str = "") -> tuple[str, bool]`（**纯函数，SC2 后半的判定单元**）：返回 `(boundary_override_reason, unjustified_boundary_hit)`。
  - `violated_boundaries` 为空 → 返回 `("", False)`（未命中禁区，不涉及本机制）。
  - 命中禁区时按优先级取理由：① `router_reasoning` 非空白 → 取它（截断 300，`RepoRouterV2` 的 `reasoning` 是必填 str，Stage0 形如 `"命中能力节点: ..."`，见 RESEARCH-ROUTING §1）；② 否则取 `llm_reason`（下条 sanity-check 调用的产物）；③ 两者皆空 → 返回 `("", True)`，即**打 `unjustified_boundary_hit` 标记**。
  - 不变量（测试要断言）：命中禁区时 `bool(reason) != unjustified`——恰有其一为真，绝不出现「既无理由也无标记」的静默保留。
- `class BlueprintRouteAdapter`（策略可注入：`__init__(self, *, router=None, charter=None, history=None)`，缺省分别取 `RepoRouterV2`、`blueprint_charter_match` 模块函数、`ascore_history_match`）。主入口 `async def route(self, session) -> dict`：
  1. 从 `session` 取当前蓝图 `requirement_spec`（`goal` + `feature_points`，含 112-01 的 `intent`）。`goal` 与 feature_points 均空 → 返回 `{"candidates": [], "router_version": "skipped", "weights_used": {}}`（形状恒定）。
  2. 候选范围解析独立成 `_resolve_repository_ids(session)`（三级：显式 include → work_item.space 仓 → None 全库；ORM 经 `@sync_to_async`）。
  3. `result = await router.route(query, top_k=..., repository_ids=..., use_llm=True)`（**取原样输出**，不加额外容错，RepoRouterV2 自带降级链）。逐字段显式映射成内部 dict（不 `to_dict()` 整包透传）。
  4. **章程候选补入**：`query_terms` 由 goal + 各 feature_point title/description 规范化得出；`await acollect_charter_candidates(query_terms=..., exclude_repository_ids={已有候选 id}, repository_ids=<同 2>)` → 补入项 `router_base=0.0`、`confidence="low"`、`reasoning="章程 owned_domains 命中（能力树未召回）"`、`matched_node_paths=[]`。记事件字段 `charter_supplement_count`。
  5. `charters = await aload_charters([所有候选 id])`；逐候选 `score_charter_match(charters.get(id), query_terms=query_terms)`。
  6. `history = await ascore_history_match(query=..., candidate_repository_ids=[所有候选 id], session=session)`。
  7. **intent 加权**：主导 intent 取自 feature_points——单功能点直接取其 intent；多功能点按出现次数取多数，平票时取「更偏保守」的 `brownfield`（注释写明取舍，避免混合需求被当纯 greenfield 处理）。逐候选 `build_score_breakdown(...)` 并按 `total` 降序。
  7b. **禁区候选的显式理由（SC2 后半，B3）**：候选**只降权不淘汰**，所以凡 `violated_boundaries` 非空且仍在候选列表里的仓，都要过一遍 `resolve_boundary_override`。
     - 先用该候选的 `reasoning`（RepoRouterV2 原样字段）当理由。
     - 全部「`reasoning` 为空白的禁区命中候选」凑成一批，走**一次** `_aexplain_boundary_overrides(...)`（模块私有 async 函数，**单次 LLM 调用覆盖全部待补候选，绝不逐候选调**）：`ProviderConfigService.aresolve()` → 无 `default_model` 返 `{}` → `build_chat_model(..., streaming=False)` → `with use_call_source(CallSource.BLUEPRINT_REROUTE):`（**不新增 call_source 枚举值**——`server/agents/call_source.py` 不在本相位可改文件内，路由族已注册值即 `BLUEPRINT_REROUTE`；日志/事件 kv 用 `reason_kind="boundary_override"` 区分子用途）→ 解析 `{repository_id: reason}` 白名单归一（只保留入参内的 id，理由截断 300）。整段 `except Exception` → `redact_secrets_in_text` + warning + 返 `{}`（best-effort，绝不阻断路由）。
     - 逐候选写 `evidence["boundary_override_reason"]` 与 `evidence["unjustified_boundary_hit"]`；累计 `unjustified_boundary_hit_count`，并记 `blueprint_route_unjustified_boundary_hit` warning（kv：`repository_id`、`violated_count`，**禁区规则正文不进日志**）。
     - 观测三事件 `blueprint_route_boundary_explain_started/completed/failed`（`category="sampling"`、`component="process_runtime"`、`duration_ms`）。
  8. 章程条目被引用时产出 `citations` 条目 `{citation_id, source_type: "repo_charter", source_id: repository_id, locator: {domain: ...}}`；history 命中产 `source_type: "knowledge_entity"`。**只产引用条目，不写蓝图**（写入在下游 stage）。
  9. emit `EVENT_BLUEPRINT_ROUTE_SCORED`（payload：`candidate_count` / `router_version` / `auto_selected` / `charter_supplement_count` / `unjustified_boundary_hit_count` / `intent` / `weights_used` / 逐候选 `{repository_id, total, router_base, charter_match, history_match}`）——独立私有方法 + `except Exception` 吞掉。
  10. 返回值**逐字按 objective 的 `stage_state["routing"]` 契约表**组装（B4：112-05 的 `_h_bp_route` 直接把它塞进 `StageOutcome(event="routed", stage_state_update={"routing": <本返回值>})`，112-04 的 `dispatch` 与 112-05 的确认门快照只读该表列出的键）：顶层 `router_version` / `auto_selected` / `intent` / `weights_used` / `charter_supplement_count` / `unjustified_boundary_hit_count` / `candidates` / `citations`；`candidates[]` 每项固定七键 `repository_id` / `repository_name` / `role_suggestion` / `confidence` / `total` / `breakdown` / `evidence`。`role_suggestion` 按契约表的确定规则算（`confidence == "high"` 或 `breakdown["charter_match"] > 0` → `direct`，否则 `indirect`），**不得省略该键**（112-04 靠它分 deep/light 桶）。`routing_evidence` 形状保持 schema 既有 object 定义（只往其内加键）。

新建 `server/tests/services/process_runtime/test_blueprint_route_breakdown.py`（**纯函数为主，机制级断言**，无 DB）：
- **恒等式**：随机若干组 `(router_base, charter_match, history_match, weights)` 下 `abs(bd["total"] - (bd["router_base"] + bd["charter_match"] + bd["history_match"])) < 1e-9`。
- **章程分量对排序的可拆解影响**（对齐 CONTEXT「断言机制级而非结果级名次」）：构造两个候选 A/B，`router_base` 相同、A 的 `charter_match` 更高 → A 的 `total` 严格大于 B，且「把 A 的 `charter_match` 置 0 后重算，两者 `total` 相等」——证明排序差异**完全且仅**由章程分量贡献，可被拆解归因。
- **intent 加权方向**：同一组分量下，`greenfield` 权重使 `charter_match + history_match` 的合计贡献严格大于 `router_base` 贡献；`brownfield`/`fix` 权重使 `router_base` 贡献占主导。
- **禁区降权**：`charter_match` 为负时 `total` 严格小于 `charter_match=0` 的同组结果（命中禁区被降权而非被无视）。
- **`resolve_boundary_override` 不变量（SC2 后半）**：`violated_boundaries=[]` → `("", False)`；命中禁区 + `router_reasoning` 非空 → 理由取 `router_reasoning` 且 `unjustified is False`；命中禁区 + `reasoning` 空 + `llm_reason` 非空 → 理由取 `llm_reason` 且 `unjustified is False`；命中禁区 + 两者皆空 → `("", True)`。**参数化断言「命中禁区时 `bool(reason) != unjustified` 恒成立」**（不存在既无理由也无标记的静默保留）。
- `aload_route_weights`：缺键回默认；某 intent 缺 `charter_match` → 该项回默认；权重为 `"abc"` → 回默认；负权重取 0。
- `evidence` 必带 `router_version`；`history_match_unavailable` 有值时该项贡献为 0 但字段在 evidence 里可见（不伪装成「历史无命中」）。

新建 `server/tests/services/process_runtime/test_blueprint_route_stage.py`（`@pytest.mark.django_db(transaction=True)` + `@pytest.mark.asyncio`；`router`/`history` 用 `AsyncMock` 注入，章程用真 DB 行）：
- 正常路径：mock router 返 2 候选 → 结果按 total 降序、每候选 breakdown 三分量齐、事件 emit 一次且 payload 含 `router_version`。
- 空 query 短路：router 的 `AsyncMock.await_count == 0` 且返回 `router_version == "skipped"`。
- **高三提分专项机制断言**：建 `onion-learning` 仓 + 其 `RepoCharter.owned_domains` 含 `{"domain": "培优/学习提分", "status": "planned"}`；mock router **不返回** onion-learning（模拟能力树无培优节点）；对 greenfield feature_point 跑 `route` → onion-learning **出现在候选里**，其 `breakdown["router_base"] == 0.0` 且 `breakdown["charter_match"] > 0`，`evidence["matched_domains"]` 含该 planned 领域。这条断言同时锁住 SC2 的两个机制：章程补入 + planned 计正分。
- **禁区降权机制断言**：建 `study-plan` 仓 charter `boundaries` 含命中规则；router 返回它且 `score` 较高 → 其 `breakdown["charter_match"] < 0` 且 `evidence["violated_boundaries"]` 非空；与「同 router_base 但无禁区」的对照仓相比 `total` 更低。
- **禁区候选必须有显式理由（SC2 后半机制级断言，B3）**——同一个禁区命中候选构造三种上游情形，各断言一次：
  1. router 返回的候选 `reasoning` 非空 → 该候选**仍在 candidates 里**（只降权不淘汰）且 `evidence["boundary_override_reason"]` 等于该 `reasoning`、`evidence["unjustified_boundary_hit"] is False`，且 `_aexplain_boundary_overrides` 未被调用（`AsyncMock.await_count == 0`，有现成理由就不多花一次 LLM）。
  2. `reasoning=""` + sanity-check 调用（patch 模块内 `build_chat_model` 返 `FakeChatModel`）返 `{repository_id: "该仓虽在禁区清单内，但本次需求的写入面落在其 owned 领域"}` → `boundary_override_reason` 非空且 `unjustified_boundary_hit is False`，且该次 LLM 调用**只发生一次**（多个禁区候选合并成一批）。
  3. `reasoning=""` + `build_chat_model` 抛异常（LLM 不可得）→ 不抛异常、候选仍返回，且 `evidence["unjustified_boundary_hit"] is True`、顶层 `unjustified_boundary_hit_count == 1`。
  另断言总不变量：`for c in candidates: if c["evidence"]["violated_boundaries"]: assert c["evidence"]["boundary_override_reason"] or c["evidence"]["unjustified_boundary_hit"]`。
- **`stage_state["routing"]` 契约断言（B4）**：`route()` 返回值顶层键集合 ⊇ `{router_version, auto_selected, intent, weights_used, charter_supplement_count, unjustified_boundary_hit_count, candidates}`；每个候选键集合 ⊇ `{repository_id, repository_name, role_suggestion, confidence, total, breakdown, evidence}`，且 `role_suggestion ∈ {"direct", "indirect"}`（112-04 分桶与 112-05 快照的读取面不被漏字段打破）。
- `evolution=maintenance_only` 候选被额外降权且 `penalty_reasons` 可见。
- history 不可得降级：`ascore_history_match` 返 `unavailable_reason="no_acting_user"` → 各候选 `breakdown["history_match"] == 0.0` 且 `evidence["history_match_unavailable"] == "no_acting_user"`。
- 依赖异常隔离：`aload_charters` 抛异常时 `route` 仍返回候选（章程分量全 0），不上抛。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/process_runtime/test_blueprint_route_breakdown.py tests/services/process_runtime/test_blueprint_route_stage.py -x -q</automated>
  </verify>
  <acceptance_criteria>
- `cd server && uv run pytest tests/services/process_runtime/test_blueprint_route_breakdown.py tests/services/process_runtime/test_blueprint_route_stage.py -x -q` 全绿。
- `rg -n "onion-learning" server/tests/services/process_runtime/test_blueprint_route_stage.py` 命中（高三提分专项机制断言存在）。
- `rg -n "router_base.*0\.0|== 0\.0" server/tests/services/process_runtime/test_blueprint_route_stage.py` 命中（章程补入候选的 router_base 为 0 被断言）。
- `rg -n "def build_score_breakdown" server/services/process_runtime/blueprint_route.py` 命中；`rg -n "sum\(" server/services/process_runtime/blueprint_route.py` 命中（total 由三项同批求和）。
- `rg -n "acollect_charter_candidates" server/services/process_runtime/blueprint_route.py` 命中（章程补入接线存在）。
- `rg -n "EVENT_BLUEPRINT_ROUTE_SCORED" server/services/process_runtime/blueprint_route.py` 命中。
- `rg -n "def resolve_boundary_override" server/services/process_runtime/blueprint_route.py` 命中；`rg -n "unjustified_boundary_hit" server/services/process_runtime/blueprint_route.py` 命中 ≥ 2（判定 + 计数）——SC2 后半机制存在。
- `rg -n "use_call_source\(CallSource.BLUEPRINT_REROUTE\)" server/services/process_runtime/blueprint_route.py` 命中；`git diff --stat server/agents/call_source.py` 输出为空（未新增枚举值）。
- `rg -n "role_suggestion" server/services/process_runtime/blueprint_route.py` 命中（`stage_state["routing"]` 契约里 112-04 分桶所需键未漏）。
- `git diff --name-only | rg "repo_router_v2.py"` 零命中；`git diff --stat server/codegraph/services/repo_router_v2.py` 输出为空。
- `rg -n "\.objects\.(create|acreate|update|aupdate)" server/services/process_runtime/blueprint_route.py` 零命中（adapter 零 ORM 写）。
- 回归面**只跑本 plan 的三个测试文件**（W1：本 plan 与 112-02 同 wave 并行，跑 `tests/services/` 全量会被 112-02 的半成品染红）：`cd server && uv run pytest tests/services/process_runtime/test_blueprint_charter_match.py tests/services/process_runtime/test_blueprint_route_breakdown.py tests/services/process_runtime/test_blueprint_route_stage.py -q` 全绿。既有路由链路零扰动只需跑 `cd server && uv run pytest tests/codegraph/ -q`（该目录不含 112 新文件）。**相位门全量测试留给 wave 4 的 112-05。**
  </acceptance_criteria>
  <done>候选分数可拆解为三项且恒等于总分；章程分量能把 owned(planned) 的仓补进候选并对排序产生可归因影响（高三提分 case 机制级验证通过）；禁区与 maintenance_only 降权可解释；history 不可得有显式标记而非静默 0 分。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RepoCharter 内容（可能是未确认 ai_draft） | 半可信文本进入路由打分，影响候选排序 |
| delivery knowledge 检索 | 跨项目数据面，权限必须 fail-closed |
| SystemSetting 权重 | 运行时可改配置影响排序 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-112-10 | Elevation of Privilege | history_match 检索 | mitigate | `search_similar(user=actor)` 必填且 fail-closed；`session.created_by` 为空时**不伪造 actor**，直接降级返回 0 + `no_acting_user` 标记 |
| T-112-11 | Tampering | 未确认 ai_draft 章程操纵排序 | mitigate | breakdown evidence 带 `charter_source`/`charter_version`，115 可标注「依据未经人工确认的草案」；章程分量 clamp `[-1,1]` 防「章程写越长分越高」 |
| T-112-12 | Tampering / DoS | 路由权重被写成极端值 | mitigate | `aload_route_weights` 逐分量 `float()` + 负值取 0 + 缺项回默认 + 非 dict 回整段默认；绝不 eval |
| T-112-13 | Info Disclosure | 召回正文/需求 query 进 trace payload 或日志 | mitigate | trace payload 只记 kinds/计数/score/duration_ms 与关联键；异常经 `redact_secrets_in_text`；验收有反向 rg 断言 |
| T-112-14 | Tampering | 冻结面被绕改 | mitigate | 验收含 `git diff --stat server/codegraph/services/repo_router_v2.py` 必为空的硬断言 |
| T-112-14b | Tampering / Repudiation | 禁区候选无解释地被保留 | mitigate | `resolve_boundary_override` 保证「命中禁区 → 有显式理由 或 有 `unjustified_boundary_hit` 标记」二者恰一（参数化断言）；sanity-check LLM 理由经白名单归一（只保留入参内 `repository_id`、截断 300），LLM 不可得时不静默通过而是打标记并计数进事件 payload |
| T-112-SC | Tampering | 依赖供应链 | accept | 零新增外部依赖（分词/命中判定用标准库实现，不引入 NLP 包） |
</threat_model>

<verification>
- **并行 wave 的验证边界（W1）**：只跑本 plan 独占的三个测试文件 + 不含 112 新文件的既有目录，**不跑 `tests/services/process_runtime/` 目录全量**（112-02 同 wave 未完成会假红）：`cd server && uv run pytest tests/services/process_runtime/test_blueprint_charter_match.py tests/services/process_runtime/test_blueprint_route_breakdown.py tests/services/process_runtime/test_blueprint_route_stage.py tests/codegraph/ tests/knowledge/ -q` 全绿。
- **相位门全量测试（`cd server && uv run pytest -q`）不在本 plan 执行，由 wave 4 的 112-05 收口。**
- 冻结面自检：`git diff --name-only` 不含 `codegraph/services/repo_router_v2.py`、`process_runtime/{decompose_segments,research_adapter,architect_merge_adapter,merged_plan,clarify_adapter,render,resume,builtin_processes,repo_router_adapter,recall_adapter}.py`、`event_taxonomy.py`、`system/models.py`、`system/settings_service.py`、`charter_service.py`。
- 并行自检：`git diff --name-only` 不含 `blueprint_spec_gate.py` / `blueprint_ambiguity_score.py` / `blueprint_intent_classify.py` / `blueprint_lifecycle_service.py`（112-02 独占）。
- `cd server && uv run python manage.py makemigrations --check --dry-run` 退出码 0。
</verification>

<success_criteria>
- CHARTER-02：路由按 feature_point 意图分流加权（净新增重章程与历史落点、改造重能力树），章程作 sanity check（命中禁区/maintenance_only 降权）；score breakdown 含 `charter_match` 分量且可解释（三项之和等于总分）。
- ROADMAP SC2 机制级达成：greenfield 功能点上章程 owned(planned) 的仓能进入候选（onion-learning 不再因能力树无节点被淘汰）；命中禁区的候选被降权，**且凡保留在候选里的禁区命中仓必须带显式 `boundary_override_reason`，拿不到理由即被打 `unjustified_boundary_hit` 标记**（SC2 后半「LLM 保留它必须给显式理由」）。
- FLOW-04 证据面：每个候选带结构化选仓理由与证据（router_version / matched_node_paths / matched_domains / violated_boundaries / history 命中），供确认门与蓝图 `routing_evidence` 消费。
</success_criteria>

<output>
完成后创建 `.planning/phases/112-1/112-03-SUMMARY.md`（provides：**`stage_state["routing"]` 的逐字字段契约**——把 objective 的契约表原样抄入 SUMMARY（顶层 8 键 + `candidates[]` 七键 + `evidence` 键清单），112-04 的 `dispatch` 与 112-05 的确认门快照按此读取；另含 `BlueprintRouteAdapter.route` 返回契约、`build_score_breakdown` 恒等式、`resolve_boundary_override` 的 `(reason, unjustified)` 语义、`role_suggestion` 的确定规则、章程补入机制、`HistoryMatchResult` 降级语义）。
</output>
