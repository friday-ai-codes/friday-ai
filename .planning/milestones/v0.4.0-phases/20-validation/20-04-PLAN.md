---
phase: 20-validation
plan: 04
type: execute
wave: 2
depends_on: ["20-01", "20-02"]
files_modified:
  - web/src/stores/useWorkflowValidationStore.ts
  - web/src/stores/useWorkflowsStore.ts
  - web/src/components/workflow/validation/IssuesPanel.vue
  - web/src/stores/__tests__/useWorkflowValidationStore.test.ts
autonomous: true
requirements: [VAL-03]
must_haves:
  truths:
    - "useWorkflowValidationStore 支持后端 ValidationIssue 形态（severity error/warning + 多 reason + node 级或 edge 级），addIssues 批量摄入后端 {errors,warnings}"
    - "saveWorkflow 在 bulk-update 返回 400 时解析 e.body.errors/warnings 灌入 validation store（或保存前 dry-run），错误阻断保存"
    - "IssuesPanel 由 store 驱动真实渲染 errors 与 warnings，error/warning 按 severity 视觉区分（不再 v-if 永 false）"
    - "前端 reason 字段与后端 ValidationIssue.reason 枚举对齐（node_not_found/field_not_found/cycle/config_schema_invalid/invalid_source_handle/...）"
  artifacts:
    - path: "web/src/stores/useWorkflowValidationStore.ts"
      provides: "扩展类型 + addIssues 摄入后端结果"
      contains: "addIssues"
    - path: "web/src/stores/useWorkflowsStore.ts"
      provides: "saveWorkflow 接 dry-run / 解析 400 body"
      contains: "useWorkflowValidationStore"
    - path: "web/src/components/workflow/validation/IssuesPanel.vue"
      provides: "按 severity 渲染 error/warning"
      contains: "severity"
    - path: "web/src/stores/__tests__/useWorkflowValidationStore.test.ts"
      provides: "VAL-03 store 摄入/渲染单测"
      contains: "addIssues"
  key_links:
    - from: "web/src/stores/useWorkflowsStore.ts"
      to: "web/src/stores/useWorkflowValidationStore.ts"
      via: "saveWorkflow catch 400 → addIssues(e.body)"
      pattern: "addIssues"
    - from: "web/src/components/workflow/validation/IssuesPanel.vue"
      to: "web/src/stores/useWorkflowValidationStore.ts"
      via: "storeToRefs 驱动 issuesList/hasIssues 渲染"
      pattern: "useWorkflowValidationStore"
---

<objective>
接通前端校验链路（VAL-03）：扩展 `useWorkflowValidationStore` 摄入后端 `WorkflowGraphValidator` 的 `{errors, warnings}`（severity + 多 reason，支持 node 级与 edge 级问题），让 `saveWorkflow` 在 bulk-update 返回 400 时（或保存前 dry-run）把结构化结果灌入 store，`IssuesPanel` 由 store 驱动真实渲染并按 severity 区分 error/warning。消除"`useWorkflowValidationStore` 无调用方、`IssuesPanel` 的 `v-if=hasWarnings` 永 false"的死代码。

Purpose: VAL-03 落地——保存非法工作流时面板真实展示后端校验的错误/警告，修正后可保存。依赖 20-01（reason 枚举）与 20-02（400 body 结构 / dry-run 端点）。
Output: useWorkflowValidationStore.ts（扩展 + addIssues）、useWorkflowsStore.ts（saveWorkflow 接线）、IssuesPanel.vue（severity 渲染）、store 单测。
</objective>

<execution_context>
@.cursor/gsd-core/workflows/execute-plan.md
@.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-validation/20-CONTEXT.md
@.planning/phases/20-validation/20-PATTERNS.md
@.planning/phases/20-validation/20-01-PLAN.md
@.planning/phases/20-validation/20-02-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 扩展 useWorkflowValidationStore + addIssues 摄入后端结果</name>
  <files>web/src/stores/useWorkflowValidationStore.ts, web/src/stores/__tests__/useWorkflowValidationStore.test.ts</files>
  <read_first>
    - web/src/stores/useWorkflowValidationStore.ts 全文（当前 ValidationWarning 仅 type:'schema_mismatch'、edge 级、Map keyed by edgeId；addWarning/clearAllWarnings/warningsList/warningCount/hasWarnings/getWarningForEdge/syncWithEdges API）
    - 20-PATTERNS.md L376-389（store 扩展：保留既有 API 形态，新增 severity + reason + node 级支持，新增 addIssues 批量摄入；reason 与后端枚举对齐）
    - 20-01-PLAN.md（后端 ValidationIssue 字段：reason/severity/field_path/node_id/edge_id/message）
    - 既有前端 store 测试范式（vitest + setActivePinia(createPinia)）
  </read_first>
  <action>
    1. 扩展类型：保留 `ValidationWarning`（向后兼容 edge 视觉），新增 `ValidationIssue` 接口对应后端字段：`reason: string`、`severity: 'error' | 'warning'`、`fieldPath?: string`、`nodeId?: string`、`edgeId?: string`、`message: string`、`id: string`（生成：edgeId/nodeId/index 组合保唯一）。
    2. 新增 state（issues 列表或按 severity 拆分）与 getters：`issuesList`、`errorCount`、`warningCount`、`hasIssues`、`hasErrors`（computed）。保留既有 `getWarningForEdge`（edge 级视觉查找，从 issues 中过滤 edgeId）。
    3. 新增 action `addIssues(payload: { errors?: any[], warnings?: any[] })`：把后端 snake_case（node_id/edge_id/field_path）映射为前端 camelCase，写入 issues；`clearAllIssues()` 清空。保留既有 `addWarning/clearAllWarnings/syncWithEdges` 以免破坏现有调用方（若有）。
    4. reason 字段直接透传后端枚举值（node_not_found/field_not_found/cycle/no_entry/orphan_node/edge_node_missing/invalid_source_handle/invalid_target_handle/config_schema_invalid/unknown_node_type），不在前端重命名。
    新建 `__tests__/useWorkflowValidationStore.test.ts`：断言 addIssues 摄入混合 errors+warnings → issuesList 正确、errorCount/warningCount/hasErrors 正确、snake→camel 映射正确、clearAllIssues 清空。
    遵循 ESLint @antfu 配置；TS 严格。
  </action>
  <behavior>
    - addIssues({errors:[{reason:'cycle',severity:'error',message:'...'}], warnings:[{reason:'orphan_node',severity:'warning',node_id:'n1',message:'...'}]}) → issuesList.length===2、errorCount===1、warningCount===1、hasErrors===true。
    - node_id/field_path 映射为 nodeId/fieldPath。
    - clearAllIssues() 后 issuesList.length===0、hasIssues===false。
  </behavior>
  <verify>
    <automated>cd web && pnpm vitest run src/stores/__tests__/useWorkflowValidationStore.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - 上述 vitest 命令全绿。
    - `grep -n "addIssues" web/src/stores/useWorkflowValidationStore.ts` 命中且导出。
    - `grep -n "severity" web/src/stores/useWorkflowValidationStore.ts` 命中（error/warning 区分）。
    - `cd web && pnpm type-check` 通过（无新 TS 错误）。
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: saveWorkflow 接 400/dry-run + IssuesPanel severity 渲染</name>
  <files>web/src/stores/useWorkflowsStore.ts, web/src/components/workflow/validation/IssuesPanel.vue</files>
  <read_first>
    - web/src/stores/useWorkflowsStore.ts L373-404（saveWorkflow：bulk-update PUT + catch 仅设 error.value；toBackendNodes/toBackendEdges）
    - web/src/api/client.ts（ApiError 带 status + body——400 body 含 {errors, warnings}；如何取 e.body）
    - web/src/components/workflow/validation/IssuesPanel.vue 全文（Collapsible v-if=hasWarnings、warningsList v-for、amber 视觉、handleWarningClick TODO）
    - 20-PATTERNS.md L391-417（saveWorkflow analog + IssuesPanel severity 区分）
    - 20-02-PLAN.md（dry-run 端点路径 /api/workflows/{id}/validate/ 与 /api/workflows/validate/；400 body 形态）
  </read_first>
  <action>
    1. `saveWorkflow`（useWorkflowsStore.ts）：引入 `useWorkflowValidationStore`。保存开始时 `validationStore.clearAllIssues()`。在 bulk-update 的 `catch (e)` 分支中，判断 ApiError 且 `e.status === 400` 且 `e.body` 含 `errors` → `validationStore.addIssues(e.body)` 并保持 throw（错误阻断保存，由调用方/面板呈现）。可选增强（Discretion）：保存前先调 dry-run（`POST /workflows/{id}/validate/` with nodes/edges），有 errors 则不发 bulk-update、直接 addIssues——二选一实现即可，**务必与 20-02 同源端点/字段**。成功保存路径 `validationStore.clearAllIssues()`。
    2. `IssuesPanel.vue`：从 validationStore 改用 `issuesList`/`hasIssues`/`errorCount`/`warningCount`（storeToRefs），`v-if="hasIssues"`。v-for 渲染 issue，按 `issue.severity` 区分视觉：error 用红色系（如 destructive/red），warning 保持 amber；标题/计数 Badge 同理。node 级问题（无 sourceNodeId/targetNodeId）展示 nodeId/fieldPath（用 getNodeName 兜底），edge 级保留 source→target 展示。`handleWarningClick` 保留 TODO（D-06 非阻断）。
    遵循 ESLint @antfu；保持既有组件结构与 i18n（中文文案）习惯。
  </action>
  <verify>
    <automated>cd web && pnpm type-check && pnpm vitest run src/stores/__tests__/useWorkflowValidationStore.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "useWorkflowValidationStore\|addIssues" web/src/stores/useWorkflowsStore.ts` 命中（saveWorkflow 接线）。
    - `grep -n "severity" web/src/components/workflow/validation/IssuesPanel.vue` 命中；`grep -c "hasWarnings" web/src/components/workflow/validation/IssuesPanel.vue` 不再依赖（改为 hasIssues）。
    - `cd web && pnpm type-check` 通过。
    - `cd web && pnpm vitest run` 相关测试全绿。
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 后端 400 body / dry-run 响应 → 前端 store | 后端校验结果（仅键名/reason）进入前端展示 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-11 | Information Disclosure | IssuesPanel 渲染后端 message | mitigate | 后端 ValidationIssue 已只含键名/路径/reason（T-17-01，20-01/20-02 保证）；前端原样展示，不额外拼接 config 值 |
| T-20-12 | Tampering | 前端跳过校验直接保存 | accept | 前端校验为体验增强；服务端 bulk-update 仍强制校验（20-02），前端绕过不影响落库安全 |
</threat_model>

<verification>
- `cd web && pnpm type-check` 通过。
- `cd web && pnpm vitest run src/stores/__tests__/useWorkflowValidationStore.test.ts` 全绿。
- grep 守护：addIssues 接线、severity 区分、IssuesPanel 改用 hasIssues。
</verification>

<success_criteria>
- store 摄入后端 {errors,warnings}（severity + reason，node/edge 级）。
- saveWorkflow 400 → addIssues，错误阻断保存。
- IssuesPanel 真实渲染并按 severity 区分 error/warning（死代码消除）。
</success_criteria>

## Manual verification (end-of-phase, see 20-VALIDATION.md)

| Behavior | Why Manual |
|----------|------------|
| 编辑器造环/坏 handle 保存 → IssuesPanel 弹出结构化 errors 且保存被拒；修正后保存成功 | 浏览器交互观感，vitest 无法覆盖完整画布交互 |

## Artifacts this phase produces

| Artifact | Type | Provides |
|----------|------|----------|
| `web/src/stores/useWorkflowValidationStore.ts` | modified | 扩展类型 + addIssues 摄入后端结果 |
| `web/src/stores/useWorkflowsStore.ts` | modified | saveWorkflow 接 400/dry-run |
| `web/src/components/workflow/validation/IssuesPanel.vue` | modified | severity 区分真实渲染 |
| `web/src/stores/__tests__/useWorkflowValidationStore.test.ts` | new | VAL-03 store 单测 |

<output>
Create `.planning/phases/20-validation/20-04-SUMMARY.md` when done
</output>
