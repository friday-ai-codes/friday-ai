---
phase: 21-observability
plan: 05
type: execute
wave: 1
depends_on: ["21-02"]
files_modified:
  - web/src/stores/useWorkflowsStore.ts
  - web/src/pages/executions/index.vue
  - web/src/components/execution/ExecutionCard.vue
  - web/src/components/execution/ExecutionHistoryCard.vue
  - web/src/components/__tests__/executions-datatable.test.ts
  - web/src/components/__tests__/logs-datatable.test.ts
autonomous: true
requirements: [TRIG-02, OBS-03]
must_haves:
  truths:
    - "前端无 schedule 触发类型选项/标签/类型（TRIG-02）"
    - "executions 列表 statusOptions 含 suspended/timeout 筛选项（OBS-03）"
    - "列表页 stats 的 execution 级等待判 suspended，node 级 waiting_approval 经 some() 旁路保留（OBS-03）"
    - "仓库索引 'scheduled' 触发类型不被误删（与工作流正交）"
  artifacts:
    - path: "web/src/stores/useWorkflowsStore.ts"
      provides: "trigger_type 联合类型移除 'schedule'"
      contains: "trigger_type"
    - path: "web/src/pages/executions/index.vue"
      provides: "移除 schedule 标签/图标 + statusOptions 补 suspended/timeout + stats 纠偏"
      contains: "statusOptions"
  key_links:
    - from: "executions/index.vue statusOptions"
      to: "后端 ExecutionStatus"
      via: "补 suspended/timeout 筛选"
      pattern: "suspended"
---

<objective>
前端清理两项（与其它前端计划文件互斥、可并行）：(TRIG-02) 移除所有工作流 `schedule` 触发类型的前端残留（联合类型、列表标签/图标、卡片标签）并更新引用 schedule 的测试夹具；(OBS-03 列表侧) `executions/index.vue` 的 `statusOptions` 补 `suspended`/`timeout` 筛选项，stats 的 execution 级"等待"按 `suspended` 判定（node 级 `waiting_approval` 经 `node_executions.some()` 旁路保留）。

Purpose: 用户无法再在 UI 配出 schedule 假触发器；列表状态枚举与后端 ExecutionStatus 对齐。
Output: 4 个前端源文件 + 2 个测试夹具更新。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/21-observability/21-RESEARCH.md
@.planning/phases/21-observability/21-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: TRIG-02 — 移除前端 schedule 触发类型残留 + 更新夹具</name>
  <files>web/src/stores/useWorkflowsStore.ts, web/src/components/execution/ExecutionCard.vue, web/src/components/execution/ExecutionHistoryCard.vue, web/src/components/__tests__/executions-datatable.test.ts, web/src/components/__tests__/logs-datatable.test.ts</files>
  <read_first>
    - web/src/stores/useWorkflowsStore.ts L52（`trigger_type` 联合类型含 `'schedule'`）
    - web/src/components/execution/ExecutionCard.vue L29（`schedule: '定时触发'`）
    - web/src/components/execution/ExecutionHistoryCard.vue L54（`schedule: '定时'`）
    - web/src/components/__tests__/executions-datatable.test.ts L34,92 + logs-datatable.test.ts L37（夹具引用 schedule）
    - 21-PATTERNS.md L441-482（前端移除点清单 + 勿动 repositories.ts 'scheduled'）
  </read_first>
  <action>
    按 21-PATTERNS 移除点清单（per D-02）：
    - `useWorkflowsStore.ts:52`：联合类型改为 `trigger_type: 'manual' | 'webhook' | 'event'`（删 `'schedule'`）。
    - `ExecutionCard.vue:29` / `ExecutionHistoryCard.vue:54`：删除 schedule 标签 map 条目。
    - 测试夹具 `executions-datatable.test.ts` / `logs-datatable.test.ts`：把引用 schedule 的夹具行改为 manual/webhook/event（保持测试有效）。
    **勿动**：`src/api/repositories.ts`（`'scheduled'` 为仓库索引触发类型，与工作流正交）、`chat.ts`/`useRunnerMonitor.ts`/`VariablePicker.vue`/`IndexHistoryList.vue`（非工作流 trigger_type 语义——先 grep 确认上下文再决定是否相关，默认不动）。
    中文注释标注 TRIG-02。
  </action>
  <verify>
    <automated>pnpm -C web test:unit -- src/components/__tests__/executions-datatable.test.ts src/components/__tests__/logs-datatable.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg "schedule" web/src/stores/useWorkflowsStore.ts web/src/components/execution/ExecutionCard.vue web/src/components/execution/ExecutionHistoryCard.vue` 无命中（工作流触发类型 schedule 已清）。
    - `rg "'scheduled'" web/src/api/repositories.ts` 仍命中（仓库索引未被误删）。
    - 两个 datatable 测试全绿。
  </acceptance_criteria>
  <done>前端工作流 schedule 触发类型残留清除，夹具更新，仓库 'scheduled' 不动，相关测试绿。</done>
</task>

<task type="auto">
  <name>Task 2: TRIG-02 + OBS-03 — executions/index.vue schedule 标签移除 + statusOptions/stats 对齐</name>
  <files>web/src/pages/executions/index.vue</files>
  <read_first>
    - web/src/pages/executions/index.vue L255-267（`triggerTypeLabels`/`triggerTypeIcons` 含 schedule）、L61-69（`statusOptions` 缺 suspended/timeout）、L129（stats waitingApproval 误用）、L98-105（refetchInterval 范式，理解列表轮询）
    - server/workflows/models/execution.py L17-27（ExecutionStatus 全集对齐源）
    - 21-PATTERNS.md L441-473（index.vue 改点：标签移除 + statusOptions + stats）
  </read_first>
  <action>
    (TRIG-02) 删除 `triggerTypeLabels` 的 `schedule: '定时触发'` 与 `triggerTypeIcons` 的 `schedule:` 条目（L255-267）。
    (OBS-03) `statusOptions`（L61-69）增 `{ value: 'suspended', label: '挂起中' }`、`{ value: 'timeout', label: '超时' }`。
    (OBS-03) stats（L129）的 execution 级"等待"判 `e.status === 'suspended'`；node 级 `waiting_approval` 经 `e.node_executions?.some(n => n.status === 'waiting_approval')` 旁路保留（命名区分 execution/node 语义，不把 `e.status === 'waiting_approval'` 当真——execution 级永不为该值）。
    中文注释标注 TRIG-02 / OBS-03 / Pitfall 7。
  </action>
  <verify>
    <automated>pnpm -C web test:unit -- src/pages/executions/__tests__/index.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg "schedule" web/src/pages/executions/index.vue` 无命中。
    - `rg "suspended|timeout" web/src/pages/executions/index.vue` 命中（statusOptions 已补）。
    - `rg "status === 'waiting_approval'" web/src/pages/executions/index.vue` 不再用于 execution 级判断（仅 node_executions.some 内出现或被移除）。
    - `pnpm -C web type-check` 对该文件无新增错误，index.spec.ts 绿。
  </acceptance_criteria>
  <done>index.vue 移除 schedule 标签，statusOptions 含 suspended/timeout，stats execution 级判 suspended。</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Type | Requirement |
|----------|------|-------------|
| `web/src/stores/useWorkflowsStore.ts` | 修复 | TRIG-02 |
| `web/src/pages/executions/index.vue` | 修复 | TRIG-02, OBS-03 |
| `web/src/components/execution/ExecutionCard.vue` / `ExecutionHistoryCard.vue` | 修复 | TRIG-02 |
| `web/src/components/__tests__/executions-datatable.test.ts` / `logs-datatable.test.ts` | 夹具更新 | TRIG-02 |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 前端枚举 → 用户配置面 | 移除 schedule 选项即移除可配出不生效触发器的入口（fail-safe） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-05-01 | Tampering/Misconfiguration | schedule 假触发入口 | mitigate | 移除前端 schedule 类型/标签/选项，用户无法再配出不生效触发器（TRIG-02 fail-safe） |
| T-21-05-02 | Integrity | 误删仓库 'scheduled' | mitigate | 明确边界：repositories.ts 'scheduled'（仓库索引）不动，仅清工作流 trigger_type schedule |
| T-21-05-SC | Tampering | 无 package 安装 | accept | 零依赖新增 |
</threat_model>

<verification>
- `pnpm -C web test:unit -- src/components/__tests__/executions-datatable.test.ts src/components/__tests__/logs-datatable.test.ts src/pages/executions/__tests__/index.spec.ts` 全绿。
- `pnpm -C web type-check` 无新增错误。
- `rg "schedule" web/src/stores/useWorkflowsStore.ts web/src/pages/executions/index.vue web/src/components/execution/ExecutionCard.vue web/src/components/execution/ExecutionHistoryCard.vue` 无命中。
</verification>

<success_criteria>
- TRIG-02 前端：无 schedule 触发类型残留，仓库 'scheduled' 保留。
- OBS-03 列表：statusOptions 含 suspended/timeout，stats execution/node 等待态区分。
</success_criteria>

<output>
Create `.planning/phases/21-observability/21-05-SUMMARY.md` when done.
</output>
