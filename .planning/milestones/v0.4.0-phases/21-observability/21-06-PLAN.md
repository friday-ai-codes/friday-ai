---
phase: 21-observability
plan: 06
type: execute
wave: 2
depends_on: ["21-02", "21-04"]
files_modified:
  - web/src/config/status.ts
  - web/src/stores/useExecutionsStore.ts
autonomous: true
requirements: [OBS-01, OBS-03]
must_haves:
  truths:
    - "config/status.ts executionStatusConfig 含 suspended（非 fallback badge），timeout 保留（OBS-03）"
    - "node_failed WS 消息写入对应 NodeExecution 的 error_message/error_code（防御读 != null）（OBS-01）"
    - "store stats execution 级等待判 suspended，node 级 waiting_approval 经 some() 旁路保留（OBS-03）"
  artifacts:
    - path: "web/src/config/status.ts"
      provides: "executionStatusConfig 补 suspended badge"
      contains: "suspended"
    - path: "web/src/stores/useExecutionsStore.ts"
      provides: "node_failed 写 error 字段 + stats 语义纠偏"
      contains: "node_failed"
  key_links:
    - from: "useExecutionsStore node_failed"
      to: "WS message error_message/error_code（21-04 广播契约）"
      via: "find NE by node_id 写字段"
      pattern: "error_message"
---

<objective>
前端状态/数据层对齐（OBS-01 store 消费 + OBS-03 枚举/统计）：(OBS-03) `config/status.ts` 的 `executionStatusConfig` 补 `suspended` badge，消除后端转 SUSPENDED 时的 raw 字符串 fallback；(OBS-01) `useExecutionsStore` 的 `node_failed` WS 处理把 21-04 广播的 `error_message`/`error_code` 写入对应 `NodeExecution`（不只 failed_nodes++）；(OBS-03) store `stats` 的 execution 级"等待"按 `suspended` 判定。

依赖 21-04（后端 WS 广播新增 error_message/error_code 可选键契约）+ 21-02（前端测试）。
Output: status.ts + useExecutionsStore.ts；Wave 0 的 status.spec / useExecutionsStore.spec 转 GREEN。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/21-observability/21-RESEARCH.md
@.planning/phases/21-observability/21-PATTERNS.md
@.planning/phases/21-observability/21-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: OBS-03 — config/status.ts 补 suspended badge</name>
  <files>web/src/config/status.ts</files>
  <read_first>
    - web/src/config/status.ts L9-21（`executionStatusConfig` 现状，缺 suspended）、L54-59（triggerLogStatusConfig analog 范式）、L84-99（getStatusConfig fallback，无需改）
    - server/workflows/models/execution.py L17-27（ExecutionStatus 全集对齐源：suspended/timeout 须有 badge）
    - 21-PATTERNS.md L241-265（Pattern 6 改造方向）
  </read_first>
  <action>
    在 `executionStatusConfig`（per D-06）新增 `suspended: { label: '挂起中', icon: 'lucide--pause-circle', variant: 'warning' }`（timeout 已有，保留）。`waiting_approval`/`waiting_input` 条目**保留**（供 `StatusBadge type="execution"` 渲染 node 状态，如 NodeOverviewTab），但注释标注：仅用于 node 状态渲染，不用于 execution 级筛选/统计。**不要**改 getStatusConfig fallback 逻辑。中文注释标注 OBS-03 / Pitfall 7。
  </action>
  <verify>
    <automated>pnpm -C web test:unit -- src/config/__tests__/status.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - status.spec.ts 全绿（每个后端 ExecutionStatus 值有非 fallback badge，含 suspended 专项）。
    - `rg "suspended" web/src/config/status.ts` 命中 executionStatusConfig。
  </acceptance_criteria>
  <done>executionStatusConfig 含 suspended badge，status.spec 全覆盖断言绿。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: OBS-01/03 — useExecutionsStore node_failed 写 error + stats 纠偏</name>
  <files>web/src/stores/useExecutionsStore.ts</files>
  <behavior>
    - node_failed WS → failed_nodes++ 且写对应 NE.error_message/error_code（防御读 != null）
    - 缺 error 字段的 node_failed 不破坏 NE 状态（不覆盖为 undefined）
    - stats execution 级等待判 suspended；node 级 waiting_approval 经 some() 旁路保留
  </behavior>
  <read_first>
    - web/src/stores/useExecutionsStore.ts L357-376（node WS 处理 + node_failed L374-376 现状只 ++）、L420-428（sub_step 找 NE 写字段 analog 范式）、L144-154（stats waitingApproval 误用）、L17/L29（NE error_message/error_code 类型已就绪）、L201-215（fetchExecution 全量覆盖，权威值）
    - .planning/phases/21-observability/21-04-SUMMARY.md（确认后端广播 message 键名 error_message/error_code）
    - 21-PATTERNS.md L269-317（store node_failed + stats 改造方向）
  </read_first>
  <action>
    (OBS-01) 改造 `node_failed` 分支（per D-04，Pitfall 5）：保留 `failed_nodes++`，新增按 `n.node === node_id` 找 NE（仿 L420-428 范式），若找到则 `if (data.error_message != null) ne.error_message = data.error_message`、`if (data.error_code != null) ne.error_code = data.error_code`（防御读，缺字段不覆盖）。
    (OBS-03) 改造 `stats`（L144-154，per D-06）：execution 级"等待"判 `e.status === 'suspended'`；node 级 `waiting_approval` 经 `e.node_executions?.some(n => n.status === 'waiting_approval')` 旁路保留（命名/注释区分 execution vs node 语义，不把 `e.status === 'waiting_approval'` 当真）。
    **不要**在断线/轮询场景混入额外本地推断（权威值由 fetchExecution 覆盖，OBS-02 在 21-07 处理）。中文注释标注 OBS-01/OBS-03。
  </action>
  <verify>
    <automated>pnpm -C web test:unit -- src/stores/__tests__/useExecutionsStore.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - useExecutionsStore.spec.ts 全绿（node_failed 写 error_message/error_code、缺字段安全、stats execution 级判 suspended）。
    - `rg "data.error_message != null|data.error_code != null" web/src/stores/useExecutionsStore.ts` 命中（防御读）。
    - `rg "status === 'suspended'" web/src/stores/useExecutionsStore.ts` 命中（stats 纠偏）。
  </acceptance_criteria>
  <done>node_failed 防御性写入 NE error 字段，stats execution/node 等待态区分，store spec 绿。</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Type | Requirement |
|----------|------|-------------|
| `web/src/config/status.ts`（`executionStatusConfig`） | 修复 | OBS-03 |
| `web/src/stores/useExecutionsStore.ts`（`node_failed` + `stats`） | 修复 | OBS-01, OBS-03 |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WS message → store | 不可信广播 message dict 写入 store NE 字段 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-06-01 | Tampering | node_failed 缺字段/异常消息 | mitigate | 防御读 `data.x != null`，缺字段不覆盖 NE 状态（Pitfall 5），仅展示后端摘要不再加工 |
| T-21-06-02 | Information Disclosure | error_message 前端展示 | accept | 值由后端 ASVS V7 截断/脱敏（21-03/21-04），前端仅渲染，不引入新泄露面 |
| T-21-06-SC | Tampering | 无 package 安装 | accept | 零依赖新增 |
</threat_model>

<verification>
- `pnpm -C web test:unit -- src/config/__tests__/status.spec.ts src/stores/__tests__/useExecutionsStore.spec.ts` 全绿。
- `pnpm -C web type-check` 对两文件无新增错误。
</verification>

<success_criteria>
- OBS-03：suspended badge 存在，每个后端枚举值非 fallback；stats execution/node 等待态区分。
- OBS-01：node_failed 防御性写入 NE error_message/error_code。
</success_criteria>

<output>
Create `.planning/phases/21-observability/21-06-SUMMARY.md` when done.
</output>
