---
phase: 21-observability
plan: 07
type: execute
wave: 2
depends_on: ["21-02", "21-04"]
files_modified:
  - web/src/components/execution/NodeOverviewTab.vue
  - web/src/components/execution/dag/ExecutionNode.vue
  - web/src/pages/executions/composables/useExecutionState.ts
autonomous: true
requirements: [OBS-01, OBS-02, OBS-03]
must_haves:
  truths:
    - "NodeOverviewTab 展示 error_code + 结构化变量错误（末行 JSON）友好解析，非 JSON 回退纯文本（OBS-01）"
    - "DAG ExecutionNode 失败节点 tooltip 含 error_message；statusBorderClass 补 suspended/timeout 防 fallback（OBS-01/03）"
    - "useExecutionState 在 WS CLOSED 且执行活跃时启动 REST 轮询（fetchExecution 权威值），重连/终态停止（OBS-02）"
    - "轮询期间不混合 WS 本地推断（服务端权威值单一真相，Pitfall 6）"
  artifacts:
    - path: "web/src/components/execution/NodeOverviewTab.vue"
      provides: "error_code 行 + parseStructuredError 展示/回退"
      contains: "parseStructuredError"
    - path: "web/src/pages/executions/composables/useExecutionState.ts"
      provides: "WS 断线降级 REST 轮询状态机"
      contains: "usePolling"
  key_links:
    - from: "useExecutionState"
      to: "store.fetchExecution"
      via: "usePolling 回调（断线降级）"
      pattern: "fetchExecution"
    - from: "useExecutionState wsDisconnected watch"
      to: "startPoll/stopPoll"
      via: "断线 start，重连 stop"
      pattern: "wsDisconnected"
---

<objective>
执行详情页可观测三项（与其它前端计划文件互斥、可并行）：(OBS-01) `NodeOverviewTab` 展示 `error_code` 并对 Phase 17/18 结构化错误（中文摘要 + 末行 JSON）做 `JSON.parse` 友好展示（失败回退纯文本）；(OBS-03) DAG `ExecutionNode` 失败节点 error tooltip + `statusBorderClass` 防御性补 suspended/timeout 色；(OBS-02) `useExecutionState` 在 WS 断线且执行活跃时降级 REST 轮询（复用 `usePolling`，5s，调 `store.fetchExecution` 权威值），重连/终态停止。

依赖 21-04（WS 广播字段契约，使 tooltip/error 实时可见）+ 21-02（前端测试）。
Output: NodeOverviewTab.vue + ExecutionNode.vue + useExecutionState.ts；Wave 0 的 NodeOverviewTab.spec / useExecutionState.spec 转 GREEN。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/21-observability/21-RESEARCH.md
@.planning/phases/21-observability/21-PATTERNS.md
@web/src/composables/usePolling.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: OBS-01 — NodeOverviewTab error_code 行 + 结构化变量错误解析展示</name>
  <files>web/src/components/execution/NodeOverviewTab.vue</files>
  <behavior>
    - error_message 末行为 JSON → 摘要 + 结构化 detail 友好展示
    - error_message 非 JSON → 原样纯文本，不抛错
    - error_code 存在 → 渲染错误码行
  </behavior>
  <read_first>
    - web/src/components/execution/NodeOverviewTab.vue L90-121（信息行 grid + 重试行 L115-120 analog）、L133-148（错误块现状，无 error_code、无结构化解析）、L95（StatusBadge type="execution" 渲染 node 状态）
    - .planning/phases/18-engine/18-03-SUMMARY.md（结构化错误约定：中文摘要 \n 末行 json.dumps）
    - 21-PATTERNS.md L370-414（parseStructuredError + 改造方向）
  </read_first>
  <action>
    (per D-04) 在 `<script setup>` 加 `parseStructuredError(msg: string): { summary: string, detail: any | null }`：split('\n')，尝试 `JSON.parse(末行)`，成功返回 `{ summary: 前面行 join, detail }`，失败 `catch` 返回 `{ summary: msg, detail: null }`（回退纯文本）。
    错误块（L133-148）改为展示 `summary`（纯文本 pre）+ 当 `detail` 非 null 时友好展示结构化引用/拓扑（如 reference/node 字段，简单 key-value 列表，不必富交互——Deferred 留 v2）。
    信息行 grid（L90-121）新增 `v-if="nodeExecution.error_code"` 的"错误码"行（仿重试行 L115-120）。
    中文注释标注 OBS-01。
  </action>
  <verify>
    <automated>pnpm -C web test:unit -- src/components/execution/__tests__/NodeOverviewTab.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - NodeOverviewTab.spec.ts 全绿（结构化解析展示、纯文本回退不抛错、error_code 行渲染）。
    - `rg "parseStructuredError" web/src/components/execution/NodeOverviewTab.vue` 命中。
    - `rg "error_code" web/src/components/execution/NodeOverviewTab.vue` 命中（错误码行存在）。
  </acceptance_criteria>
  <done>NodeOverviewTab 含 error_code 行 + 结构化错误解析/回退，spec 绿。</done>
</task>

<task type="auto">
  <name>Task 2: OBS-03/01 — ExecutionNode DAG 色补全 + 失败节点 error tooltip</name>
  <files>web/src/components/execution/dag/ExecutionNode.vue</files>
  <read_first>
    - web/src/components/execution/dag/ExecutionNode.vue L13-18（已 import Tooltip*）、L38-50（`statusBorderClass` map，缺 suspended/timeout）
    - 21-PATTERNS.md L418-437（改造方向：补色 + 失败 tooltip 最小实现）
    - 21-RESEARCH.md L448-450（OQ-3：DAG 渲染 NodeExecution，防御性补 suspended/timeout 色）
  </read_first>
  <action>
    (OBS-03) `statusBorderClass` map（L38-49）防御性增 `suspended: 'border-purple-400/60'`、`timeout: 'border-rose-400/60'`（避免 fallback；DAG 理论无 suspended，补色仅防御）。
    (OBS-01 最小) 失败节点 error tooltip：复用已 import 的 `Tooltip*`，当 `props.data.status === 'failed'` 且存在 `error_message` 时把其注入 `<TooltipContent>`（最小实现；DAG 富交互留 Deferred v2）。若 `props.data` 不含 error_message，先 grep 节点 data 结构确认字段来源（来自 NodeExecution，21-06 store 已写入），缺则降级仅展示状态。
    中文注释标注 OBS-03 / OBS-01。
  </action>
  <verify>
    <automated>pnpm -C web type-check</automated>
  </verify>
  <acceptance_criteria>
    - `rg "suspended|timeout" web/src/components/execution/dag/ExecutionNode.vue` 命中 statusBorderClass map。
    - 失败节点 tooltip 注入 error_message（`rg "error_message" web/src/components/execution/dag/ExecutionNode.vue` 命中或经 TooltipContent 渲染）。
    - `pnpm -C web type-check` 对该文件无新增错误。
  </acceptance_criteria>
  <done>ExecutionNode statusBorderClass 含 suspended/timeout，失败节点 tooltip 含 error_message。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: OBS-02 — useExecutionState WS 断线降级 REST 轮询</name>
  <files>web/src/pages/executions/composables/useExecutionState.ts</files>
  <behavior>
    - wsDisconnected=true（active + CLOSED）→ 启动 usePolling（调 store.fetchExecution）
    - 重连/执行终态 → 停止轮询
    - 轮询用 store.fetchExecution（服务端权威全量覆盖），不混合 WS 本地推断
  </behavior>
  <read_first>
    - web/src/pages/executions/composables/useExecutionState.ts L11（isActiveStatus 含 suspended）、L74-81（`wsDisconnected` 已就绪，watch 它）、L120-143（WS 连接/重连）
    - web/src/composables/usePolling.ts L43-57（start/stop + onUnmounted 自动清理）
    - web/src/stores/useExecutionsStore.ts L201-215（fetchExecution 全量覆盖 = 权威值）
    - 21-PATTERNS.md L321-366（Pattern 5 接法 + 权威值约束 Pitfall 6）
  </read_first>
  <action>
    (per D-05) 在 useExecutionState 新增 `usePolling(() => store.fetchExecution(executionId.value), { interval: 5000, immediate: true })` 取得 `{ start: startPoll, stop: stopPoll }`；`watch(wsDisconnected, d => d ? startPoll() : stopPoll())`。
    权威值约束（Pitfall 6）：轮询调 `fetchExecution`（整体覆盖 currentExecution）为单一真相；断线时 WS 无消息流，**不要**让 WS `completed_nodes++`/`failed_nodes++` 本地推断与轮询并存（确认现有 WS handler 在断线期间不触发；如有竞态隐患在注释标注，不引入并行计数）。onUnmounted 由 usePolling 内部处理，无需手动清理。
    中文注释标注 OBS-02 / D-05 / Pitfall 6。
  </action>
  <verify>
    <automated>pnpm -C web test:unit -- src/pages/executions/composables/__tests__/useExecutionState.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - useExecutionState.spec.ts 全绿（WS CLOSED→start、重连→stop、轮询调 fetchExecution）。
    - `rg "usePolling" web/src/pages/executions/composables/useExecutionState.ts` 命中。
    - `rg "wsDisconnected" web/src/pages/executions/composables/useExecutionState.ts` 出现在 watch 中（断线触发）。
  </acceptance_criteria>
  <done>useExecutionState 断线降级 REST 轮询、重连停止、以 fetchExecution 权威值为单一真相，spec 绿。</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Type | Requirement |
|----------|------|-------------|
| `web/src/components/execution/NodeOverviewTab.vue` | 修复 | OBS-01 |
| `web/src/components/execution/dag/ExecutionNode.vue` | 修复 | OBS-01, OBS-03 |
| `web/src/pages/executions/composables/useExecutionState.ts` | 修复 | OBS-02 |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WS message / error_message → 详情页渲染 | 后端摘要在前端 JSON.parse + 展示 |
| REST `/workflow-executions/{id}/`（降级轮询） | 服务端权威值跨入前端，须复用既有对象级权限 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-07-01 | Information Disclosure | error_message 结构化展示 | accept | 值由后端 ASVS V7 截断/仅拓扑元数据（Phase 18），前端仅 parse 渲染，不引入新泄露 |
| T-21-07-02 | Elevation/IDOR | 降级轮询取他人 execution | mitigate | 复用 store.fetchExecution → REST `/workflow-executions/{id}/` 既有对象级权限，不新增旁路（ASVS V4） |
| T-21-07-03 | Tampering/DoS | parseStructuredError 处理恶意/超大字符串 | mitigate | try/catch 回退纯文本，不 eval；不递归展开未知结构（仅浅层 key-value） |
| T-21-07-04 | Integrity | 轮询/WS 双写竞态进度回跳 | mitigate | fetchExecution 全量覆盖为单一真相，断线期不混合 WS 本地推断（Pitfall 6） |
| T-21-07-SC | Tampering | 无 package 安装 | accept | 零依赖新增 |
</threat_model>

<verification>
- `pnpm -C web test:unit -- src/components/execution/__tests__/NodeOverviewTab.spec.ts src/pages/executions/composables/__tests__/useExecutionState.spec.ts` 全绿。
- `pnpm -C web type-check` 对三文件无新增错误。
</verification>

<success_criteria>
- OBS-01：NodeOverviewTab 展示 error_code + 结构化错误解析/回退；DAG 失败节点 error tooltip。
- OBS-02：WS 断线降级 REST 轮询、重连停止、服务端权威值单一真相。
- OBS-03：DAG statusBorderClass 含 suspended/timeout。
</success_criteria>

<output>
Create `.planning/phases/21-observability/21-07-SUMMARY.md` when done.
</output>
