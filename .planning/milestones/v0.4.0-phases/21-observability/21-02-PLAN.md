---
phase: 21-observability
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - web/src/config/__tests__/status.spec.ts
  - web/src/stores/__tests__/useExecutionsStore.spec.ts
  - web/src/pages/executions/composables/__tests__/useExecutionState.spec.ts
  - web/src/components/execution/__tests__/NodeOverviewTab.spec.ts
autonomous: true
requirements: [OBS-01, OBS-02, OBS-03]
must_haves:
  truths:
    - "每个后端 ExecutionStatus 值都有非 fallback badge（status.spec 遍历断言）"
    - "node_failed WS 消息写入对应 NodeExecution 的 error_message/error_code（store spec 断言）"
    - "WS CLOSED 且执行活跃时启动 REST 轮询，重连后停止（useExecutionState spec 断言）"
    - "结构化变量错误（末行 JSON）被 parse 友好展示，非 JSON 回退纯文本（NodeOverviewTab spec 断言）"
  artifacts:
    - path: "web/src/config/__tests__/status.spec.ts"
      provides: "OBS-03 ExecutionStatus 全覆盖 RED 测试"
      contains: "getStatusConfig"
    - path: "web/src/stores/__tests__/useExecutionsStore.spec.ts"
      provides: "OBS-01/03 node_failed + stats 语义测试"
      contains: "node_failed"
    - path: "web/src/pages/executions/composables/__tests__/useExecutionState.spec.ts"
      provides: "OBS-02 WS 降级状态机测试"
      contains: "wsDisconnected"
  key_links:
    - from: "status.spec.ts"
      to: "config/status.ts getStatusConfig"
      via: "遍历后端枚举断言非 fallback"
      pattern: "getStatusConfig\\('execution'"
---

<objective>
建立 Phase 21 前端验证基建（Wave 0）：为 OBS-01 / OBS-02 / OBS-03（前端侧）创建 vitest 测试，断言**修复后**的目标行为。提交时应为 RED，由 Wave 1/2 前端实现转 GREEN。

Purpose: 为前端实现任务提供 `<automated>` 锚点（Nyquist）；锁死契约（状态枚举全覆盖、node_failed 写 error 字段、WS 断线降级轮询、结构化错误 parse/回退）。
Output: 4 个新 vitest spec 文件。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/21-observability/21-RESEARCH.md
@.planning/phases/21-observability/21-PATTERNS.md
@web/src/pages/executions/__tests__/index.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 新建 status.spec.ts — OBS-03 ExecutionStatus 全覆盖</name>
  <files>web/src/config/__tests__/status.spec.ts</files>
  <read_first>
    - web/src/config/status.ts L9-21（`executionStatusConfig` 现状，缺 suspended）、L84-99（`getStatusConfig` + fallback）
    - server/workflows/models/execution.py L17-27（`ExecutionStatus` 后端 SSOT：pending/running/paused/suspended/completed/failed/cancelled/timeout）
    - web/src/pages/executions/__tests__/index.spec.ts（既有 vitest 范式：import/describe/it 写法）
    - 21-PATTERNS.md L241-265（Pattern 6 + No-Analog 说明）
  </read_first>
  <action>
    创建 spec（per D-06）：在文件顶部定义后端 ExecutionStatus 全集常量数组 `EXECUTION_STATUSES = ['pending','running','paused','suspended','completed','failed','cancelled','timeout']`（镜像后端 SSOT，注释引用 execution.py L17-27）。
    - `test_every_execution_status_has_non_fallback_badge`：遍历该数组，对每个 status 断言 `getStatusConfig('execution', status)` 返回的 label **不等于** 原始 status 字符串且 icon **不等于** `'lucide--help-circle'`（即非 fallback）。
    - `test_suspended_badge_present`：单独断言 `getStatusConfig('execution','suspended')` 的 label 为中文（如 '挂起中'）。
    - `test_unknown_status_falls_back`：断言未知 status（如 `'bogus'`）走 fallback（label==='bogus' 或 icon help-circle），保护 fallback 行为不被破坏。
    中文注释标注 OBS-03 与 Pitfall 7。
  </action>
  <verify>
    <automated>pnpm -C web test:unit -- src/config/__tests__/status.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - spec 可被 vitest 收集运行（无 import error）。
    - `test_every_execution_status_has_non_fallback_badge` 与 `test_suspended_badge_present` 在实现前为 RED（当前 status.ts 缺 suspended）。
    - `test_unknown_status_falls_back` 应为 GREEN（保护既有 fallback）。
  </acceptance_criteria>
  <done>status.spec.ts 遍历后端 ExecutionStatus 全集断言非 fallback badge，含 suspended 专项与 fallback 保护。</done>
</task>

<task type="auto">
  <name>Task 2: 新建 useExecutionsStore.spec.ts — OBS-01 node_failed 写 error + OBS-03 stats 语义</name>
  <files>web/src/stores/__tests__/useExecutionsStore.spec.ts</files>
  <read_first>
    - web/src/stores/useExecutionsStore.ts L144-154（stats，含 waitingApproval 误用）、L357-376（node WS 处理 + node_failed L374-376）、L17/L29（NodeExecution error_message/error_code 类型）、L201-215（fetchExecution 覆盖）
    - web/src/pages/executions/__tests__/index.spec.ts（pinia setActivePinia/createPinia 范式）
    - 21-PATTERNS.md L269-317（store node_failed + stats 改造方向）
  </read_first>
  <action>
    创建 spec（per D-04/D-06）。用 `setActivePinia(createPinia())` 初始化 store，向 `currentExecution` 注入一个含 `node_executions:[{node:'n1', id:'ne1', status:'running', error_message:'', error_code:null}]` 的执行对象：
    - `test_node_failed_writes_error_message`：调用 `handleWebSocketMessage`（或对应 WS 入口）传 `{event:'node_failed', node_id:'n1', error_message:'变量解析失败', error_code:'VAR_RESOLUTION_FAILED'}`，断言 `currentExecution.failed_nodes` 自增**且** `node_executions[0].error_message==='变量解析失败'`、`error_code==='VAR_RESOLUTION_FAILED'`。
    - `test_node_failed_without_error_fields_is_safe`：传 `{event:'node_failed', node_id:'n1'}`（无 error 字段），断言不抛错且不把 error_message 覆盖为 undefined（防御读 `!= null`，Pitfall 5）。
    - `test_stats_execution_waiting_uses_suspended`：构造 executions 列表含一个 `status:'suspended'` 执行 + 一个仅 `node_executions` 含 `waiting_approval` 的执行，断言 stats 把 execution 级"等待"正确归到 suspended（execution 级），不把 execution.status==='waiting_approval' 当真（OBS-03）。
    中文注释标注 OBS-01/OBS-03。若 WS 入口方法名与研究不同，先 grep store 导出的 message handler 名再调用。
  </action>
  <verify>
    <automated>pnpm -C web test:unit -- src/stores/__tests__/useExecutionsStore.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - spec 可被 vitest 收集运行。
    - `test_node_failed_writes_error_message` 在实现前为 RED（当前 node_failed 只 failed_nodes++）。
    - `test_stats_execution_waiting_uses_suspended` 在实现前为 RED（当前 stats 判 waiting_approval）。
  </acceptance_criteria>
  <done>useExecutionsStore.spec.ts 编码 node_failed 写 error 字段 + stats execution/node 等待态区分。</done>
</task>

<task type="auto">
  <name>Task 3: 新建 useExecutionState.spec.ts（OBS-02）+ NodeOverviewTab.spec.ts（OBS-01 结构化错误）</name>
  <files>web/src/pages/executions/composables/__tests__/useExecutionState.spec.ts, web/src/components/execution/__tests__/NodeOverviewTab.spec.ts</files>
  <read_first>
    - web/src/pages/executions/composables/useExecutionState.ts L74-81（`wsDisconnected` 计算）、L11（isActiveStatus 含 suspended）、L120-143（WS 连接）
    - web/src/composables/usePolling.ts L43-57（start/stop 接口）
    - web/src/components/execution/NodeOverviewTab.vue L133-148（错误块）、L90-121（信息行 grid + 重试行 L115-120）
    - 21-PATTERNS.md L321-366（Pattern 5 + 权威值约束）、L370-414（NodeOverviewTab parseStructuredError）
  </read_first>
  <action>
    (A) useExecutionState.spec.ts（per D-05，OBS-02）：mock `usePolling`（vi.mock 返回 `{ start: vi.fn(), stop: vi.fn() }`）+ mock store.fetchExecution。驱动 `wsDisconnected` 为 true（执行 active + wsStatus='CLOSED'）：
    - `test_ws_closed_starts_polling`：断线时断言 `usePolling().start` 被调用。
    - `test_ws_reconnect_stops_polling`：从断线恢复（wsStatus 非 CLOSED 或执行终态）断言 `stop` 被调用。
    - `test_polling_uses_fetch_execution`：断言传给 usePolling 的回调调用 `store.fetchExecution`（服务端权威值，D-05）。
    若 composable 需 mount 宿主组件才能跑 watch，使用 `@vue/test-utils` 最小宿主或 `effectScope`；先 grep `src/**/composables/__tests__` 取最近范式（无则用 index.spec.ts 风格）。
    (B) NodeOverviewTab.spec.ts（OBS-01 结构化变量错误）：用 @vue/test-utils mount，传 `nodeExecution` prop：
    - `test_structured_error_parsed`：`error_message="变量解析失败\n{\"reference\":\"nodes.n1.output\",\"node\":\"n1\"}"`，断言摘要行渲染"变量解析失败"且结构化 detail（reference/node）被友好展示（非整段 JSON 原文堆在一起）。
    - `test_plain_error_fallback`：`error_message="普通错误无 JSON"`，断言原样纯文本展示、不抛错。
    - `test_error_code_rendered`：`error_code="VAR_RESOLUTION_FAILED"` 时断言页面出现错误码行。
    中文注释标注 OBS-01/OBS-02。
  </action>
  <verify>
    <automated>pnpm -C web test:unit -- src/pages/executions/composables/__tests__/useExecutionState.spec.ts src/components/execution/__tests__/NodeOverviewTab.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - 两 spec 可被 vitest 收集运行。
    - OBS-02 轮询启停断言在实现前为 RED（当前 useExecutionState 无降级轮询）。
    - NodeOverviewTab 结构化解析/error_code 断言在实现前为 RED（当前无 parse、无 error_code 行）；plain fallback 不抛错。
  </acceptance_criteria>
  <done>两 spec 编码 OBS-02 WS 降级状态机与 OBS-01 结构化错误 parse/回退/error_code 展示。</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Type | Requirement |
|----------|------|-------------|
| `web/src/config/__tests__/status.spec.ts` | 新建测试 | OBS-03 |
| `web/src/stores/__tests__/useExecutionsStore.spec.ts` | 新建测试 | OBS-01, OBS-03 |
| `web/src/pages/executions/composables/__tests__/useExecutionState.spec.ts` | 新建测试 | OBS-02 |
| `web/src/components/execution/__tests__/NodeOverviewTab.spec.ts` | 新建测试 | OBS-01 |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WS message → 前端 store | 测试构造的 WS message dict 是不可信输入边界（断言防御读 `!= null`） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-02-01 | Tampering | node_failed 缺字段消息 | mitigate | `test_node_failed_without_error_fields_is_safe` 断言缺字段不破坏 store 状态（防御性消费契约） |
| T-21-02-SC | Tampering | 无 package 安装 | accept | 纯前端测试，零依赖新增 |
</threat_model>

<verification>
- `pnpm -C web test:unit -- src/config/__tests__ src/stores/__tests__/useExecutionsStore.spec.ts src/pages/executions/composables/__tests__ src/components/execution/__tests__/NodeOverviewTab.spec.ts` 收集运行成功（无 import error）。
- RED 失败为断言失败（非编译/导入错误）。
</verification>

<success_criteria>
- 4 个 vitest spec 可运行，编码 OBS-01/02/03 前端目标契约。
- fallback/防御性用例为 GREEN，目标行为用例为 RED（实现前）。
</success_criteria>

<output>
Create `.planning/phases/21-observability/21-02-SUMMARY.md` when done.
</output>
