---
phase: 21-observability
plan: 04
type: execute
wave: 1
depends_on: ["21-01"]
files_modified:
  - server/workflows/models/workflow.py
  - server/workflows/migrations/0027_remove_schedule_trigger_type.py
  - server/workflows/hooks/builtin.py
autonomous: true
requirements: [TRIG-02, OBS-01]
must_haves:
  truths:
    - "Workflow.TriggerType.choices 不含 schedule（TRIG-02）"
    - "AlterField migration 应用后存量 trigger_type='schedule' 行不报错（TextChoices 无 DB 约束）"
    - "WebSocketBroadcastHook 在 node 失败/超时时 message 含 error_message/error_code，成功态不含（OBS-01，向后兼容）"
  artifacts:
    - path: "server/workflows/models/workflow.py"
      provides: "TriggerType 移除 SCHEDULE"
      contains: "class TriggerType"
    - path: "server/workflows/migrations/0027_remove_schedule_trigger_type.py"
      provides: "AlterField trigger_type choices 收窄"
      contains: "AlterField"
    - path: "server/workflows/hooks/builtin.py"
      provides: "node 失败广播追加 error_message/error_code 可选键"
      contains: "error_message"
  key_links:
    - from: "0027 migration"
      to: "workflow.trigger_type"
      via: "AlterField choices"
      pattern: "trigger_type"
    - from: "WebSocketBroadcastHook"
      to: "WS message dict"
      via: "失败态追加可选键"
      pattern: "node_execution.status in"
---

<objective>
后端契约清理两项（文件互斥、可并行）：(TRIG-02) 从 `Workflow.TriggerType` 移除僵尸枚举 `schedule` 并生成安全 `AlterField` migration（fail-safe，用户无法再配出不生效触发器）；(OBS-01 后端) `WebSocketBroadcastHook` 在节点失败/超时时向广播 message 追加可选 `error_message`/`error_code`，使前端无需 full fetch 即见失败原因。

Purpose: TRIG-02 移除假功能（非实现原生定时）；OBS-01 让运行中节点失败实时可见。
Output: workflow.py 枚举收窄 + 0027 migration + builtin.py 广播追加可选键；Wave 0 的 test_trigger_type_choices / test_hooks 转 GREEN。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/21-observability/21-RESEARCH.md
@.planning/phases/21-observability/21-PATTERNS.md
@server/workflows/migrations/0022_remove_cancelled_status.py
@server/workflows/templates/daily_summary.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: TRIG-02 — TriggerType 移除 SCHEDULE</name>
  <files>server/workflows/models/workflow.py</files>
  <read_first>
    - server/workflows/models/workflow.py L30-34（`TriggerType` 现状，含 `SCHEDULE = "schedule", "定时触发"`）
    - server/workflows/models/execution.py L17-27（ExecutionStatus TextChoices 增删范式 analog）
    - 21-PATTERNS.md L110-121（Pattern 2 现状）
  </read_first>
  <action>
    从 `Workflow.TriggerType` 删除 `SCHEDULE = "schedule", "定时触发"` 行（per D-02），保留 MANUAL/WEBHOOK/EVENT。在该枚举上方或邻近加中文注释：定时触发改用"外部 cron → 调 webhook"模式（沿用 `templates/daily_summary.json` 既定口径），不实现原生定时调度。**不要**改其他字段/默认值。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/workflows/test_trigger_type_choices.py -k "schedule or keep" -x</automated>
  </verify>
  <acceptance_criteria>
    - `rg "schedule" server/workflows/models/workflow.py` 仅出现在注释（不在 choices 值中）。
    - test_trigger_type_choices 的 `exclude_schedule` 与 `keep_others` 用例全绿。
  </acceptance_criteria>
  <done>TriggerType 不含 schedule，manual/webhook/event 保留，含外部 cron→webhook 注释。</done>
</task>

<task type="auto">
  <name>Task 2: TRIG-02 — 0027 AlterField migration（安全收窄 + 可选数据迁移）</name>
  <files>server/workflows/migrations/0027_remove_schedule_trigger_type.py</files>
  <read_first>
    - server/workflows/migrations/0022_remove_cancelled_status.py L1-18（AlterField choices 收窄范式，逐字模板）
    - server/workflows/migrations/0003_suspended_status.py（多操作可叠加 + RunPython 范式）
    - server/workflows/migrations/0026_rename_fetch_project_info_nodes.py（最新 migration，dependency 取它）
    - 21-PATTERNS.md L125-146（Pattern 2 migration 模板 + 禁止删行/加 CHECK）
  </read_first>
  <action>
    新建 `0027_remove_schedule_trigger_type.py`（per D-02，参 0022）：
    - `dependencies = [("workflows", "0026_rename_fetch_project_info_nodes")]`。
    - `operations`：`migrations.AlterField(model_name="workflow", name="trigger_type", field=models.CharField(choices=[("manual","手动触发"),("webhook","Webhook 触发"),("event","事件触发")], default="manual", max_length=20, verbose_name="触发类型"))`。
    - 追加可选数据迁移（Claude's Discretion，更干净）：`migrations.RunPython(forwards, migrations.RunPython.noop)`，`forwards` 用 `apps.get_model("workflows","Workflow").objects.filter(trigger_type="schedule").update(trigger_type="manual")`（仅 update，**禁止**删行/加 CHECK 约束——Pitfall 3）。
    中文注释标注 TRIG-02。
  </action>
  <verify>
    <automated>cd server && uv run python manage.py makemigrations --check --dry-run workflows && uv run python manage.py migrate workflows --plan</automated>
  </verify>
  <acceptance_criteria>
    - `makemigrations --check` 不再报"missing migration"（模型与迁移一致）。
    - `migrate workflows --plan` 列出 0027 且无报错；migration 仅 AlterField(+可选 RunPython update)，`rg "RunSQL|AddConstraint|CheckConstraint" server/workflows/migrations/0027_remove_schedule_trigger_type.py` 无命中。
    - dependency 指向 0026。
  </acceptance_criteria>
  <done>0027 migration 以 AlterField 收窄 choices（+可选 schedule→manual update），存量行不报错。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: OBS-01 后端 — WebSocketBroadcastHook 失败态追加可选键</name>
  <files>server/workflows/hooks/builtin.py</files>
  <behavior>
    - node 失败/超时 → message 含 error_message + error_code
    - node 成功/其他态 → message 不含 error_message/error_code（向后兼容）
    - 不影响 AlertRuleHook（其读 DB 对象非 message dict）
  </behavior>
  <read_first>
    - server/workflows/hooks/builtin.py L38-78（`WebSocketBroadcastHook`，L55-65 message 组装、L67-70 node_debug_paused 可选键范式 analog）、L207-242 + L313-317（AlertRuleHook node_error_code 读 DB 对象，证明追加可选键不影响）
    - server/workflows/models/execution.py L437-515（NodeExecution.error_message/error_code）
    - 21-PATTERNS.md L198-237（Pattern 4 后端改点 + 契约一致性）、L493-501（WS 只追加可选键 + 截断/零泄露）
  </read_first>
  <action>
    在 `WebSocketBroadcastHook` 的 node_execution 分支（L62-65 之后，per D-04，Pitfall 5）追加：当 `node_execution.status in ("failed", "timeout")` 时 `message["error_message"] = node_execution.error_message or ""`、`message["error_code"] = node_execution.error_code or ""`。仅失败态写入（成功态不追加键，保持现有消费方向后兼容）。沿用同 Hook `node_debug_paused` 条件键写法。**不**对 error_message 二次拼接敏感数据（值由后端 Phase 17/18 已产出"摘要+末行JSON"，本处直接透传）。中文注释标注 OBS-01 / D-04 / Pitfall 5。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/workflows/test_hooks.py -k "broadcast and error" -x</automated>
  </verify>
  <acceptance_criteria>
    - test_hooks 的失败态用例（含 error_message/error_code）+ 成功态用例（不含）+ timeout 用例全绿。
    - `rg "node_execution.status in" server/workflows/hooks/builtin.py` 命中失败态条件。
    - AlertRuleHook 相关既有测试不回退（`uv run pytest tests/workflows/test_alert_rules.py -q` 全绿）。
  </acceptance_criteria>
  <done>WebSocketBroadcastHook 仅在 failed/timeout 追加 error_message/error_code 可选键，成功态不含，AlertRuleHook 不受影响。</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Type | Requirement |
|----------|------|-------------|
| `server/workflows/models/workflow.py`（`TriggerType`） | 修复 | TRIG-02 |
| `server/workflows/migrations/0027_remove_schedule_trigger_type.py` | 新建 migration | TRIG-02 |
| `server/workflows/hooks/builtin.py`（`WebSocketBroadcastHook`） | 修复 | OBS-01 |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 引擎 hook → WS group（execution_{id}） | 节点状态广播跨入前端订阅，含失败 error 字段 |
| migration → DB | trigger_type 存量行处理，走 ORM 非裸 SQL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-04-01 | Information Disclosure | WS error_message 广播 | mitigate | 仅透传后端已产出的"摘要+拓扑 JSON"（Phase 18 零输出值泄露原则），不二次拼接 payload/凭证；仅失败态写入，不扩大订阅面（已按 execution_{id} group 隔离，ASVS V7） |
| T-21-04-02 | Tampering/Integrity | schedule migration 误删存量 | mitigate | 仅 AlterField(+可选 update)，禁止删行/CHECK 约束（Pitfall 3），TextChoices 无 DB 约束存量字符串合法 |
| T-21-04-03 | DoS | WS error_message 超大 | accept | 字段值已由 Phase 18 约定为有限摘要+拓扑；如需可在产出侧截断（本计划仅透传） |
| T-21-04-SC | Tampering | 无 package 安装 | accept | 零依赖新增 |
</threat_model>

<verification>
- `cd server && uv run pytest tests/workflows/test_trigger_type_choices.py tests/workflows/test_hooks.py tests/workflows/test_alert_rules.py -q` 全绿。
- `cd server && uv run python manage.py makemigrations --check --dry-run workflows` 通过。
- `cd server && uv run ruff check server/workflows/models/workflow.py server/workflows/hooks/builtin.py` 无新增告警。
</verification>

<success_criteria>
- TRIG-02：TriggerType 无 schedule + 0027 AlterField 安全收窄（存量不报错）。
- OBS-01 后端：失败/超时节点广播含 error_message/error_code，成功态不含，AlertRuleHook 不回退。
</success_criteria>

<output>
Create `.planning/phases/21-observability/21-04-SUMMARY.md` when done.
</output>
