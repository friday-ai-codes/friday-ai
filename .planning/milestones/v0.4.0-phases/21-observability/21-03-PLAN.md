---
phase: 21-observability
plan: 03
type: execute
wave: 1
depends_on: ["21-01"]
files_modified:
  - server/workflows/api/views.py
  - server/feishu/views.py
autonomous: true
requirements: [TRIG-01, TRIG-03]
must_haves:
  truths:
    - "保存含单数 event_type 的 feishu_event_trigger 节点后 WorkflowTrigger 正确生成且 is_active=True（TRIG-01）"
    - "仅含历史复数 event_types 的节点同步后仍生成 trigger（兜底，不丢存量）"
    - "filter_status(数组)/filter_project_key/filter_work_item_type 正确写入 filter_config；exclude_*/project_ids 不入正向 filter_config（OQ#1）"
    - "飞书 dispatch 异常 → TriggerLog.status=error+error_message；无匹配 → status=ignored+原因（TRIG-03）"
    - "webhook dispatch 失败/无匹配返回区分原因的结构化响应（TRIG-03 webhook，OQ#2）"
  artifacts:
    - path: "server/workflows/api/views.py"
      provides: "async_sync_workflow_triggers 单数读取 + filter_config 映射 + webhook 失败结构化响应"
      contains: "async_sync_workflow_triggers"
    - path: "server/feishu/views.py"
      provides: "_dispatch_to_workflows 失败/无匹配落 TriggerLog"
      contains: "_dispatch_to_workflows"
  key_links:
    - from: "async_sync_workflow_triggers"
      to: "WorkflowTrigger.filter_config"
      via: "node.config.event_type + filter_* 映射"
      pattern: "event_type"
    - from: "_dispatch_to_workflows"
      to: "TriggerLog.status/error_message"
      via: "asave(update_fields=...)"
      pattern: "TriggerLogStatus"
---

<objective>
修复触发链路：(TRIG-01) `async_sync_workflow_triggers` 改读单数 `event_type` + 历史复数兜底，正确把可正向表达的 filter 字段写入 `filter_config`，消除"读复数→恒空→trigger 被 deactivate→表空→飞书事件无法匹配"的根因；(TRIG-03) dispatch 失败不再静默吞掉，飞书路径落 `TriggerLog`（失败/无匹配态 + error_message），webhook 路径返回区分原因的结构化响应。

Purpose: 让"保存 → 生成 WorkflowTrigger → 飞书事件匹配触发"端到端真实可用，且"触发了没跑起来"有可查询原因。
Output: views.py / feishu/views.py 就地修复，Wave 0 后端触发测试转 GREEN。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/21-observability/21-RESEARCH.md
@.planning/phases/21-observability/21-PATTERNS.md
@server/workflows/models/trigger.py
@server/feishu/models.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TRIG-01 — async_sync_workflow_triggers 单数读取 + filter_config 映射</name>
  <files>server/workflows/api/views.py</files>
  <behavior>
    - 单数 event_type 节点 → 生成 1 条 active WorkflowTrigger（filter_config 正确）
    - 仅复数 event_types 历史节点 → 兜底仍生成 trigger
    - filter_status 数组 → filter_config["cur_work_item_status.state_key"] 为数组（走 list 成员匹配）
    - exclude_* / project_ids → 不写入 filter_config
    - 端到端：生成的 trigger.matches_event(event_type, payload) 命中
  </behavior>
  <read_first>
    - server/workflows/api/views.py L96-166（`async_sync_workflow_triggers` 全函数：L106 读复数 bug、L117 for 循环、L155-159 deactivate 副作用）、L722-748（bulk_update 唯一调用方，理解触发时机）
    - server/workflows/models/trigger.py L104-166（`matches_event`/`_matches_filter` list 成员 + dotted-path 契约——filter_config 形态依据）
    - server/workflows/types/schemas.ts 或 web/src/types/workflow/schemas.ts L132-143（feishu_event_trigger config 字段：单数 event_type、filter_status 数组、project_ids/exclude_* 负向）
    - 21-PATTERNS.md L38-106（Pattern 1 改造方向 + 匹配契约 + OQ#1 关键约束）
  </read_first>
  <action>
    改造 `async_sync_workflow_triggers`（per D-01，OQ#1）：
    - 读取改为 `event_type = config.get("event_type")` + `legacy = config.get("event_types") or []`，组 `event_type_list = [et for et in ([event_type] if event_type else legacy) if et]`（单数优先，复数兜底；Pitfall 1 并存不删复数读取）。
    - 构建 `filter_config`：`filter_project_key`→`project_key`、`filter_work_item_type`→`work_item_type_key`；`filter_status`（已是数组）→ `filter_config["cur_work_item_status.state_key"] = statuses`（若存在 `filter_status_custom` 追加进数组）。
    - **不要**把 `project_ids` / `exclude_project_ids` / `exclude_work_item_pattern` / `exclude_work_item_regex` 写入 filter_config（OQ#1：`_matches_filter` 仅正向 include，写入会静默误匹配）。在该处加中文注释：负向/Space UUID 字段留 v2（见 21-CONTEXT Deferred）。
    - 循环 `for et in event_type_list:` 组 `configured_triggers`（event_type/filter_config/node_id/node_name），后续 upsert/deactivate 逻辑（L130-159）保持不变。
    保持 async ORM 走既有 `async for` / `asave` 范式，ruff line length 100，中文注释标注 TRIG-01 / D-01。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/workflows/test_trigger_sync.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `tests/workflows/test_trigger_sync.py` 全绿（含单数生成、复数兜底、filter_config 映射、端到端匹配、exclude/project_ids 不入 filter_config）。
    - `rg "config.get\\(\"event_types\"\\)" server/workflows/api/views.py` 仍出现（兜底保留），但单数 `event_type` 为优先来源（`rg "config.get\\(\"event_type\"\\)"` 命中）。
    - `rg "exclude_|project_ids" server/workflows/api/views.py` 在 async_sync 函数体内不写入 filter_config（仅注释提及）。
  </acceptance_criteria>
  <done>async_sync_workflow_triggers 单数为准 + 复数兜底，filter_config 仅含可正向表达字段，test_trigger_sync 全绿。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TRIG-03 飞书路径 — _dispatch_to_workflows 失败/无匹配落 TriggerLog</name>
  <files>server/feishu/views.py</files>
  <behavior>
    - dispatch 抛异常 → trigger_log.status=ERROR + error_message（截断 ≤2000）
    - dispatch 返回空 → trigger_log.status=IGNORED + error_message 含 event_type
    - dispatch 成功 → 保持既有 workflow_execution 写入，不改成功语义
  </behavior>
  <read_first>
    - server/feishu/views.py L685-731（`_dispatch_to_workflows` 现状：成功分支 L712-724、except 仅 logger.error L726-731、缺无匹配/异常落 status）
    - server/feishu/models.py L14-20（`TriggerLogStatus`: accepted/ignored/error/duplicate）、L71-76（status/error_message 字段）
    - 21-PATTERNS.md L150-194（Pattern 3 改造方向 + 截断/零泄露约束）
    - 21-RESEARCH.md L521-529（ASVS V7：error_message 截断 + 不泄露输出值/凭证）
  </read_first>
  <action>
    改造 `_dispatch_to_workflows`（per D-03）：
    - 在 `if executions:` 之后新增 `else:` 分支：`trigger_log.status = TriggerLogStatus.IGNORED`、`trigger_log.error_message = f"无匹配工作流（event_type={event_type}）"`，`await trigger_log.asave(update_fields=["status", "error_message"])`。
    - `except Exception as e:` 分支保留 `logger.error(...)`，追加 `trigger_log.status = TriggerLogStatus.ERROR`、`trigger_log.error_message = str(e)[:2000]`（截断防膨胀 + 仅人类可读摘要，**不**拼接 payload/凭证/node 输出值，ASVS V7），`await trigger_log.asave(update_fields=["status", "error_message"])`。
    导入 `TriggerLogStatus`（若未导入）。中文注释标注 TRIG-03 / D-03 / ASVS V7。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/test_trigger_dispatcher.py -k "error or ignored or fail" -x</automated>
  </verify>
  <acceptance_criteria>
    - test_trigger_dispatcher 中 TRIG-03 飞书失败/无匹配持久化用例全绿。
    - `rg "TriggerLogStatus.ERROR|TriggerLogStatus.IGNORED" server/feishu/views.py` 命中。
    - `rg "\\[:2000\\]" server/feishu/views.py` 命中（截断存在）。
  </acceptance_criteria>
  <done>飞书 dispatch 异常/无匹配落 TriggerLog 失败态 + 截断 error_message，成功语义不变。</done>
</task>

<task type="auto">
  <name>Task 3: TRIG-03 webhook 路径 — 区分原因的结构化响应（OQ#2 最小）</name>
  <files>server/workflows/api/views.py</files>
  <read_first>
    - server/workflows/api/views.py L1535-1588（`WebhookTriggerView.post`：L1570-1574 no_workflows 200 无区分）
    - server/workflows/models/webhook.py L83-124（WebhookLog 需 webhook_config FK——理解为何 path 触发难落库 → 选结构化响应）
    - 21-PATTERNS.md L194（webhook 最小方案）、21-RESEARCH.md L443-446（OQ-2 裁定）
  </read_first>
  <action>
    按 ORCHESTRATOR OQ#2 裁定（webhook 用 WebhookLog 或结构化响应）实现最小方案：
    - 用 `try/except` 包住 `await dispatcher.dispatch(context)`；捕获异常时返回结构化错误响应（HTTP 500，body `{"status":"error","message":<截断摘要 str(e)[:2000]>}`，并 `log.error("webhook_dispatch_failed", ...)`），**不**泄露 payload/凭证。
    - 无匹配（`not executions`）保持 200 但 body 增 `reason` 键明确区分（如 `{"status":"no_workflows","message":"No matching workflows found","reason":"no_matching_trigger"}`），使前端/调用方可区分"已接受未匹配" vs "分发异常"。
    - 若 path 能解析到唯一 `WebhookConfig`，可选地写 `WebhookLog(success=False, error_message=...)`（Claude's Discretion；解析不到则仅结构化响应，**不**强塞 TriggerLog——Pitfall 4）。
    中文注释标注 TRIG-03 / OQ#2。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/test_trigger_dispatcher.py tests/test_trigger_views.py -q</automated>
  </verify>
  <acceptance_criteria>
    - 既有 webhook 相关测试不回退（全绿）。
    - `rg "webhook_dispatch_failed" server/workflows/api/views.py` 命中（异常路径有结构化日志/响应）。
    - 异常响应 message 经 `[:2000]` 截断（`rg "\\[:2000\\]" server/workflows/api/views.py` 命中）。
  </acceptance_criteria>
  <done>webhook dispatch 异常返回截断的结构化错误响应、无匹配返回带 reason 的区分响应，不强塞 TriggerLog。</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Type | Requirement |
|----------|------|-------------|
| `server/workflows/api/views.py`（`async_sync_workflow_triggers`） | 修复 | TRIG-01 |
| `server/workflows/api/views.py`（`WebhookTriggerView`） | 修复 | TRIG-03 |
| `server/feishu/views.py`（`_dispatch_to_workflows`） | 修复 | TRIG-03 |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 飞书 webhook / 外部 webhook → 后端 dispatch | 不可信外部事件 payload 跨入；error_message 落库/响应 |
| node.config → filter_config | 编辑态配置（已过 schema/WorkflowGraphValidator）转运行态匹配规则 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-03-01 | Information Disclosure | TriggerLog.error_message / webhook 响应 | mitigate | error_message 仅人类可读摘要 + `str(e)[:2000]` 截断；不拼接 payload/凭证/node 输出值（ASVS V7） |
| T-21-03-02 | Tampering | filter_config 负向字段误同步 | mitigate | OQ#1：exclude_*/project_ids 不写入正向 filter_config，避免静默误匹配/绕过预期过滤 |
| T-21-03-03 | DoS | error_message TextField 无限增长 | mitigate | 写入前 `[:2000]` 截断 |
| T-21-03-04 | Elevation/IDOR | dispatch → start_execution 项目归属 | accept | 复用既有 TriggerDispatcher/find_workflows 项目隔离，本计划不新增旁路 |
| T-21-03-SC | Tampering | 无 package 安装 | accept | 零依赖新增（既有代码内部重构） |
</threat_model>

<verification>
- `cd server && uv run pytest tests/workflows/test_trigger_sync.py tests/test_trigger_dispatcher.py tests/test_trigger_views.py -q` 全绿。
- `cd server && uv run ruff check server/workflows/api/views.py server/feishu/views.py` 无新增告警。
</verification>

<success_criteria>
- TRIG-01：保存→生成 WorkflowTrigger→matches_event 端到端绿；复数兜底绿；filter_config 仅正向字段。
- TRIG-03：飞书失败/无匹配落 TriggerLog 失败态；webhook 异常/无匹配区分原因的结构化响应；error_message 截断。
</success_criteria>

<output>
Create `.planning/phases/21-observability/21-03-SUMMARY.md` when done.
</output>
