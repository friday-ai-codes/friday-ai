---
phase: 17-varref
plan: 04
type: execute
wave: 2
depends_on: [17-01, 17-02, 17-03]
files_modified:
  - server/workflows/nodes/（按核查结果按需小修，预期 0-3 个文件）
  - server/tests/workflows/（按需更新依赖旧静默语义的存量断言）
autonomous: true
requirements: [VAR-01, VAR-02, VAR-03, VAR-04]
must_haves:
  truths:
    - "全部 ~20 个 render_template/get_template_value 调用方节点中，模板渲染发生在外部副作用之前（fail-fast 不留半成品副作用，RESEARCH 假设 A1 闭环）"
    - "没有节点用裸 except 把 TemplateResolutionError 吞成空串/默认值继续执行（新失败语义不被静默改写）"
    - "后端 workflows 测试套件与前端单测套件在新语义下全量通过"
  artifacts:
    - path: ".planning/phases/17-varref/17-04-SUMMARY.md"
      provides: "调用面核查清单（文件 × 渲染时机 × 吞错风险 × 处置）与回归结果"
  key_links:
    - from: "server/workflows/nodes/**"
      to: "server/workflows/engine/template_resolver.py"
      via: "节点 execute() 内的渲染调用在新严格语义下行为正确"
      pattern: "render_template|get_template_value"
---

<objective>
解析语义严格化后的调用面核查与全链路回归（wave 2 收尾）：逐一核查 ~20 个调用方节点的渲染时机与异常处理，跑通后端/前端全量测试，关闭 RESEARCH 假设 A1。

Purpose: 17-01 改变了 ~20 个节点文件共享的解析语义但刻意未触碰调用点；本计划验证"语义变化由核心统一生效"的前提成立——渲染先于副作用、异常不被吞——并以全量回归兜底三个 wave-1 计划的交叉影响。
Output: 调用面核查清单（落 SUMMARY）、必要的最小修复、后端 workflows 套件 + 前端单测全绿。
</objective>

<context>
@.planning/phases/17-varref/17-RESEARCH.md（§2 调用面、Assumptions A1、Pitfall 2）
@.planning/phases/17-varref/17-01-SUMMARY.md
@.planning/phases/17-varref/17-02-SUMMARY.md
@.planning/phases/17-varref/17-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 调用面逐节点核查——渲染时机与吞错风险</name>
  <files>server/workflows/nodes/（按核查结果按需小修）</files>
  <read_first>
    - server/workflows/engine/template_resolver.py（TemplateResolutionError 定义——核查吞错时的判定对象）
    - server/workflows/nodes/ai/prompt.py（:332-333 渲染先于业务逻辑的正例范本，A1 抽样已证实）
    - 用 `rg -ln "render_template|get_template_value" server/workflows/ server/prompts/` 列出全部调用文件作为核查清单
  </read_first>
  <action>
    1. 生成调用方清单：`rg -ln "render_template\(|get_template_value\(" server/workflows/nodes/ server/prompts/`（预期 ~20 个节点文件 + prompts/services.py）。
    2. 逐文件核查两点并记录结论（文件 → 渲染时机 OK/违规 → 吞错风险 OK/违规 → 处置）：
       a. 渲染时机：render/get_template_value 调用是否位于外部副作用（HTTP 请求、git 操作、飞书 API、DB 写、容器分发）之前；违规者把渲染调用前移到 execute() 入口段（最小改动，不重构节点）。
       b. 吞错风险：渲染调用是否被 `except ValueError`/`except Exception` 包裹且 except 分支用空串/默认值继续执行（吞错改写语义）；违规者在 except 分支顶部 `raise`（re-raise TemplateResolutionError）或缩小 try 范围，保持 fail-fast 直达 scheduler。注意：`except ValueError` 后转 NodeResult(status="failed", error=...) 的写法**不算吞错**（语义等价于 fail-fast，CONVENTIONS 正路）。
    3. 核查清单（全文件 × 两维度 × 处置）写入本计划 SUMMARY 的专门小节——这是 A1 假设的闭环证据。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/workflows/test_template_resolver.py tests/workflows/test_error_handling.py -q</automated>
  </verify>
  <acceptance_criteria>
    - 核查清单覆盖 `rg -ln "render_template\(|get_template_value\(" server/workflows/nodes/ server/prompts/` 输出的全部文件，零遗漏
    - 每个文件有明确结论（OK 或 已修复+修复点）；修复仅限渲染调用前移或 except 分支 re-raise/收窄，无节点重构
    - 修复涉及的节点既有测试全绿
  </acceptance_criteria>
  <done>A1 假设闭环：所有调用方渲染先于副作用、解析失败不被吞错，证据清单落 SUMMARY。</done>
</task>

<task type="auto">
  <name>Task 2: 全链路回归——后端 workflows 套件 + 前端单测</name>
  <files>server/tests/workflows/（按需更新依赖旧静默语义的存量断言）</files>
  <read_first>
    - .planning/phases/17-varref/17-CONTEXT.md（定界：input./trigger. 等前缀维持现状——若存量测试失败面涉及非 nodes 前缀，优先怀疑实现扩大化而非改测试，Pitfall 2 警示信号）
    - 失败用例对应的被测源文件（按回归结果定向阅读）
  </read_first>
  <action>
    1. 跑后端全量 workflows 套件：`cd server && uv run pytest tests/workflows/ -q`。失败分诊三类：
       a. 测试依赖旧静默语义（断言空串回退/字面量保留的 nodes.* 用例）→ 按新语义更新断言（注明 Phase 17 语义变更）；
       b. 大批与变量无关的节点测试失败 → 警示信号：实现把严格化扩大到了非 nodes 前缀（Pitfall 2），回到 template_resolver.py 修定界，**不许改测试迁就**；
       c. 真实回归 → 修实现。
    2. 跑前端全量单测：`cd web && pnpm vitest run`，同样分诊处理。
    3. 后端整体冒烟：`cd server && uv run pytest -q` 全量（workflows 之外的套件确认无交叉破坏，如 prompts/services.py 调用面）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/workflows/ -q && cd ../web && pnpm vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `cd server && uv run pytest tests/workflows/ -q` 全绿
    - `cd web && pnpm vitest run` 全绿
    - `cd server && uv run pytest -q` 全量通过（或失败项经查证与本阶段改动无关并在 SUMMARY 记录证据）
    - 测试改动仅限"旧静默语义断言"的用例，每处注释标明 Phase 17 语义变更依据
  </acceptance_criteria>
  <done>三个 wave-1 计划的交叉影响经全量回归验证收敛，阶段四项 ROADMAP 成功标准的自动化部分全部为绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| （继承）节点 config → 解析核心 | 本计划为核查/回归性质，无新增信任边界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-30 | Repudiation | 节点失败但已产生外部副作用（半成品） | mitigate | Task 1 渲染时机核查——渲染前移保证 fail-fast 在副作用之前 |
| T-17-SC | Tampering | npm/pip/cargo installs | accept | 本计划零新依赖（RESEARCH Package Legitimacy Audit：无安装） |
</threat_model>

<verification>
- `cd server && uv run pytest tests/workflows/ -q` 与 `cd web && pnpm vitest run` 全绿
- 核查清单（调用文件全集 × 渲染时机 × 吞错风险 × 处置）存在于 17-04-SUMMARY.md
</verification>

<success_criteria>
- RESEARCH 假设 A1（渲染先于副作用）经逐文件核查闭环
- 新失败语义在全部调用方生效且不被吞错改写（ROADMAP 成功标准 2 的调用面保障）
- 阶段全量自动化验证（后端 + 前端）通过，可进入 /gsd-verify-work
</success_criteria>

<output>
完成后创建 `.planning/phases/17-varref/17-04-SUMMARY.md`（含调用面核查清单小节）
</output>
