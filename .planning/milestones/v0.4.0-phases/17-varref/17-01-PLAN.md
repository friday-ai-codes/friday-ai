---
phase: 17-varref
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/workflows/engine/template_resolver.py
  - server/workflows/nodes/base.py
  - server/workflows/engine/scheduler.py
  - server/tests/workflows/test_template_resolver.py
  - server/tests/workflows/test_error_handling.py
autonomous: true
requirements: [VAR-02, VAR-04]
must_haves:
  truths:
    - "变量引用解析失败（节点 ID 不存在、字段不存在、未知前缀）时节点显式失败，错误信息指明引用、原因分类与可用候选，不再静默空串或保留 {{...}} 字面量"
    - "{{nodes.x.data.name}} 嵌套路径能取到 output['data']['name']，list 数字索引（items.0.name）同样可用"
    - "render_template 与 get_template_value 共享同一解析核心，错误语义一致"
    - "input./trigger./global./context./config. 前缀的字段缺失行为维持现状（空串），有现状锁定测试保护"
    - "解析失败的 NodeExecution.error_message 为中文一句话 + 结构化 JSON（reference/reason/available/template），Phase 21 可直接 JSON.parse 最后一行"
  artifacts:
    - path: "server/workflows/engine/template_resolver.py"
      provides: "解析核心：TemplateResolutionError + 嵌套下钻 + 前缀分发 + 严格 nodes/未知前缀语义"
      min_lines: 120
    - path: "server/tests/workflows/test_template_resolver.py"
      provides: "VAR-02/VAR-04 全场景专项单测（零 DB 纯函数）"
      min_lines: 150
  key_links:
    - from: "server/workflows/nodes/base.py"
      to: "server/workflows/engine/template_resolver.py"
      via: "render_template/get_template_value 薄委托"
      pattern: "from workflows.engine.template_resolver import"
    - from: "server/workflows/engine/scheduler.py"
      to: "server/workflows/engine/template_resolver.py"
      via: "_execute_node except 分支识别 TemplateResolutionError 并结构化写 error_message"
      pattern: "TemplateResolutionError"
---

<objective>
重写模板变量解析核心，落地 VAR-02 解析失败显式报错语义与 VAR-04 嵌套路径支持。

Purpose: 这是全里程碑的语义地基——Phase 18 trigger 注入、Phase 20 保存校验、Phase 21 错误展示全部消费本计划定稿的失败语义与错误结构。
Output: 新模块 `server/workflows/engine/template_resolver.py`（纯函数核心 + 类型化异常）、`base.py` 两 API 薄委托、scheduler 结构化 error_message、专项单测全绿。
</objective>

<context>
@.planning/phases/17-varref/17-CONTEXT.md
@.planning/phases/17-varref/17-RESEARCH.md
@server/workflows/nodes/base.py
@server/workflows/engine/scheduler.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 新建 template_resolver.py 解析核心 + 全场景专项单测</name>
  <files>server/workflows/engine/template_resolver.py, server/tests/workflows/test_template_resolver.py</files>
  <read_first>
    - server/workflows/nodes/base.py（:101-156 get_input/get_trigger_data 嵌套下钻范式；:341-429 render_template 现状；:431-462 get_template_value；:558-608 _resolve_simple_path 现状）
    - .planning/phases/17-varref/17-RESEARCH.md（§2 现状对照表、Pattern 1/2、Pitfall 1/2/3、Open Questions #1）
    - server/tests/workflows/conftest.py（确认本测试不需要任何 fixture——纯函数零 DB）
  </read_first>
  <behavior>
    解析核心为纯函数模块（不 import Django ORM、不依赖 ExecutionContext），输入全部为 plain dict，pytest 零 DB 可测。预期行为（每条都是一个测试用例组）：
    - 节点 ID 不存在：`{{nodes.zzz.field}}` → raise TemplateResolutionError(reason="node_not_found")，错误消息中文，含大小写近似提示（previous_outputs 有 "aB1" 时引用 "ab1" 提示"你是否想使用 'aB1'"）与可用候选；available 候选列表过滤掉可被 UUID 正则（`^[0-9a-f]{8}-[0-9a-f]{4}-` 形式）识别的键，只列 short_id 形态，过滤后为空则回退列全部键；available 只含键名绝不含输出值
    - 字段不存在：`{{nodes.aB1.nope}}`（节点存在）→ reason="field_not_found"，消息指明"节点 aB1 输出中不存在字段 nope"，available=该节点输出顶层字段 keys
    - 嵌套路径：`{{nodes.aB1.data.name}}` 取到 output["data"]["name"]；`{{nodes.aB1.items.0.name}}` 取到 output["items"][0]["name"]；中途非 dict/list 或键/索引缺失 → reason="field_not_found" 并指明失败的完整 path
    - 未知前缀：`{{foo.bar}}` → reason="unknown_prefix"，不再原样保留字面量；available=合法前缀列表 [input, context, config, nodes, global, trigger, $]
    - 段数不足：`{{nodes.aB1}}`（只有两段）→ reason="missing_field_path"，消息为"引用缺少字段路径"类文案，不得误报为未知前缀（Pitfall 3）
    - 双键兼容：previous_outputs 同时含 UUID 键与 short_id 键时，`{{nodes.<uuid>.x}}` 与 `{{nodes.<short_id>.x}}` 都解析成功
    - 保类型：get_template_value 模式下单变量 `{{nodes.aB1.count}}`（int 42）返回 int 42 而非 "42"；`[n]`/`[-n]` 数组索引后缀现状保留
    - 多变量渲染：render 模式 "a={{nodes.aB1.x}}, b={{input.k}}" 返回拼接字符串
    - 现状锁定（OQ#1 定界）：`{{input.missing}}` / `{{trigger.payload.missing}}` / `{{global.missing}}` / `{{context.missing}}` / `{{config.missing}}` 字段缺失时 render 返回空串、value 模式返回 ""——与现状一致，不报错
  </behavior>
  <action>
    创建 `server/workflows/engine/template_resolver.py`（中文 docstring，注明"Phase 17 实现契约"）：
    1. `class TemplateResolutionError(ValueError)`：构造参数 template/reference/reason/available/message（参照 RESEARCH Pattern 2 示意），reason 取值枚举字符串 node_not_found | field_not_found | unknown_prefix | missing_field_path。继承 ValueError 保持既有 except ValueError 调用方兼容。
    2. `@dataclass ResolutionSources`：previous_outputs/input_data/workflow_context/node_config/trigger_data 五个 dict + `global_values: dict`（由调用方预先展平传入，对应 base.py `_get_global_values` 的产物）。
    3. `resolve_path(path: str, sources: ResolutionSources) -> Any`：前缀分发（input/context/config/nodes/global/trigger）。nodes 前缀执行严格语义（节点归一化查找 + 嵌套下钻 dict 键与 list 数字索引，参照 get_input 的逐段遍历改造为"断路即抛 TemplateResolutionError"）；非 nodes 前缀字段缺失维持现状返回 None（由上层转空串）——此定界必须写进模块 docstring。未知前缀抛 unknown_prefix。
    4. `render_template(template: str, sources, jsonpath_resolver: Callable[[str], Any]) -> str`：迁移 base.py:355-429 的 re.sub 主循环；`$`+`[` 的 JSONPath 分支调用传入的 jsonpath_resolver 回调（行为一字不改，含"空结果保留字面量"现状）；`$.`/`$` 简写维持现状；nodes/未知前缀走新严格语义。
    5. `get_template_value(template: str, sources, jsonpath_resolver) -> Any`：迁移 base.py:447-462 + _resolve_simple_path 逻辑，单变量保类型、`[n]` 索引后缀保留；nodes/未知前缀严格语义与 render 一致。
    创建 `server/tests/workflows/test_template_resolver.py`：按 behavior 列出的全部用例编写（纯 dict 构造 ResolutionSources，jsonpath_resolver 用简单 lambda stub），按场景分 class 组织。先写测试（RED），再实现使其通过（GREEN）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/workflows/test_template_resolver.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `server/workflows/engine/template_resolver.py` 存在，模块内不出现 `from django` / `import django` / ORM 导入（grep 验证：`rg -c "^from django|^import django" server/workflows/engine/template_resolver.py` 退出码非 0）
    - `uv run pytest tests/workflows/test_template_resolver.py -q` 全绿，用例覆盖 behavior 中全部 10 组场景（节点不存在/大小写提示/available 过滤 UUID、字段不存在、嵌套 dict、list 索引、未知前缀、missing_field_path、双键、保类型、多变量、非 nodes 前缀现状锁定）
    - TemplateResolutionError 实例具有 .template/.reference/.reason/.available 四个属性且 isinstance(err, ValueError) 为 True
    - 全部错误消息为中文
  </acceptance_criteria>
  <done>解析核心模块与专项单测齐备，pytest 零 DB 全绿，错误语义三分类 + 定界（仅 nodes.* 与未知前缀严格化）落地。</done>
</task>

<task type="auto">
  <name>Task 2: base.py 两 API 委托同一核心 + get_previous_output 嵌套下钻</name>
  <files>server/workflows/nodes/base.py, server/tests/workflows/test_template_resolver.py</files>
  <read_first>
    - server/workflows/engine/template_resolver.py（Task 1 产物——委托目标的函数签名）
    - server/workflows/nodes/base.py（:130-135 get_previous_output；:341-429 render_template 待删除的旧 replace 逻辑；:431-462 get_template_value；:464-542 _resolve_jsonpath——保留不动；:544-556 _get_global_values）
  </read_first>
  <action>
    1. `ExecutionContext.render_template` 改为薄委托：构造 ResolutionSources（global_values 用 self._get_global_values()），调用 template_resolver.render_template，jsonpath_resolver 传 self._resolve_jsonpath。删除原 replace 闭包中的简单路径分发逻辑（:355-427），`_resolve_jsonpath` 与其 ValueError 行为原样保留。
    2. `ExecutionContext.get_template_value` 同样委托 template_resolver.get_template_value；删除 `_resolve_simple_path` 方法（逻辑已迁入 resolver）。
    3. `get_previous_output` 增加点分嵌套下钻（dict 键 + list 数字索引，仿 get_input 逐段遍历），但保留 default 语义（路径任一段缺失返回 default，不抛异常）——该方法被节点代码直接调用，不能破坏直接调用方；严格报错只存在于模板解析路径。方法 docstring 注明此差异。
    4. 在 test_template_resolver.py 追加 ExecutionContext 集成层测试类：构造 ExecutionContext（纯 dict 字段，无 DB），断言 (a) context.render_template 对坏引用抛 TemplateResolutionError；(b) context.get_template_value 单变量保类型；(c) JSONPath 现状锁定——`{{$nodes.aB1.items[*].name}}` 正常解析、JSONPath 零匹配时 render 保留 `{{...}}` 字面量（Pitfall 6 characterization test）；(d) get_previous_output("aB1", "data.name") 返回嵌套值、缺失返回 default。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/workflows/test_template_resolver.py tests/workflows/test_nodes.py -q</automated>
  </verify>
  <acceptance_criteria>
    - base.py 中 render_template/get_template_value 方法体不再含前缀分发实现（grep：`rg -c "无法解析则保持原样" server/workflows/nodes/base.py` 退出码非 0，旧注释随旧逻辑删除）
    - `_resolve_jsonpath` 方法保持未改动（git diff 该方法零变更）
    - `get_previous_output(node_id, "data.name")` 在 output={"data":{"name":"x"}} 时返回 "x"；键缺失时返回 default 不抛异常
    - `cd server && uv run pytest tests/workflows/test_template_resolver.py tests/workflows/test_nodes.py -q` 全绿
  </acceptance_criteria>
  <done>两 API 共享同一解析核心、错误语义一致；JSONPath 与非 nodes 前缀现状有锁定测试；既有节点测试不回归。</done>
</task>

<task type="auto">
  <name>Task 3: scheduler 结构化 error_message + 解析失败集成测试</name>
  <files>server/workflows/engine/scheduler.py, server/tests/workflows/test_error_handling.py</files>
  <read_first>
    - server/workflows/engine/scheduler.py（:999-1045 _execute_node 异常分支与 amark_failed 调用；:135-150 _map_error_code）
    - server/workflows/models/execution.py（:647-659 NodeExecution.amark_failed——error_message 为纯文本字段，不开新字段）
    - server/tests/workflows/test_error_handling.py（既有测试组织方式与 fixture 用法）
  </read_first>
  <action>
    1. 在 scheduler `_execute_node` 的 `except Exception as _exc` 分支（:1013 附近）增加 TemplateResolutionError 识别：last_error 改写为"中文一句话\n" + `json.dumps({"reference": ..., "reason": ..., "available": ..., "template": ...}, ensure_ascii=False)`（OQ#3 推荐编码：最后一行为可 JSON.parse 的结构化对象，Phase 21 直接消费）。其余异常路径不变；`_map_error_code` 不新增分类（沿用现有映射）。import 置于模块顶部。
    2. 在 test_error_handling.py 追加集成测试（django_db，仿既有用例构造 Workflow + 节点）：工作流含一个 config 引用不存在节点的节点（如 ai_prompt 的 user_prompt 含 `{{nodes.zzz.output}}`），执行后断言：(a) NodeExecution.status == failed；(b) error_message 含中文描述；(c) error_message 最后一行可被 json.loads 且含 reference/reason/available/template 四键、reason == "node_not_found"；(d) WorkflowExecution 状态为 failed（fail-fast 生效，未静默继续）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/workflows/test_error_handling.py tests/workflows/test_engine.py -q</automated>
  </verify>
  <acceptance_criteria>
    - scheduler.py 含对 TemplateResolutionError 的 isinstance 判定且 json.dumps 使用 ensure_ascii=False
    - 集成测试断言 error_message 最后一行 json.loads 成功且 reason 字段值属于 {node_not_found, field_not_found, unknown_prefix, missing_field_path}
    - `cd server && uv run pytest tests/workflows/test_error_handling.py tests/workflows/test_engine.py -q` 全绿
  </acceptance_criteria>
  <done>解析失败经 scheduler 落入 NodeExecution.error_message（中文 + 结构化 JSON），端到端集成测试锁定该契约。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 节点 config（用户编辑）→ 模板解析核心 | 用户可写任意 `{{...}}` 引用，解析器是不可信输入的第一消费方 |
| 解析错误 → NodeExecution.error_message（前端展示） | 错误内容会原样展示给用户 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Information Disclosure | TemplateResolutionError.available | mitigate | available 候选只列键名（节点 ID/字段 key），绝不包含上游输出值；Task 1 单测显式断言 |
| T-17-02 | Denial of Service | 嵌套下钻递归 | accept | 路径段数由模板字符串长度自然限制（re 捕获组 + split），无递归爆栈面 |
| T-17-SC | Tampering | npm/pip/cargo installs | accept | 本计划零新依赖（RESEARCH Package Legitimacy Audit：无安装） |
</threat_model>

<verification>
- `cd server && uv run pytest tests/workflows/test_template_resolver.py tests/workflows/test_error_handling.py tests/workflows/test_nodes.py tests/workflows/test_engine.py -q` 全绿
- `cd server && uv run ruff check workflows/engine/template_resolver.py workflows/nodes/base.py workflows/engine/scheduler.py` 无报错
</verification>

<success_criteria>
- ROADMAP 成功标准 2：解析失败（节点不存在/字段不存在/未知前缀）显式失败且错误指明引用与原因，无静默空串/字面量保留
- ROADMAP 成功标准 4：嵌套路径可取值，render_template/get_template_value 对错误 ID、未知前缀、UUID vs short_id、嵌套路径有专项单测
- 错误结构（reference/reason/available/template）落 error_message，供 Phase 21 消费
</success_criteria>

<output>
完成后创建 `.planning/phases/17-varref/17-01-SUMMARY.md`
</output>
