---
phase: 18-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/workflows/engine/routing.py
  - server/workflows/engine/dag.py
  - server/workflows/engine/__init__.py
  - server/tests/workflows/test_engine_routing.py
  - server/tests/workflows/test_engine_deadlock.py
  - server/tests/workflows/test_engine_inputs.py
autonomous: true
requirements: [ENG-02, ENG-04, ENG-05]
must_haves:
  truths:
    - "evaluate_node_readiness 对菱形汇合'一支 skip 一支活'判 ready；双支全 skip 判 skip_unselected（CONTEXT 锁定：汇合节点一条活路即执行）"
    - "next_handle 无任何匹配出边时 default 边回退选中（存量工作流兼容，禁删）"
    - "diagnose_deadlock 输出最后一行可 json.loads，含 pending/short_id/waiting_on/handle，不含任何节点输出值"
    - "collect_inputs 按 target_handle 归集且 plan 链不双重嵌套、coding_result 链命中端口键（characterization）"
    - "back-edge（反馈环）不参与就绪/级联判定，回退目标不被误标 skip"
  artifacts:
    - path: "server/workflows/engine/routing.py"
      provides: "路由/级联/死锁/输入收集纯函数——主循环与回调续跑的唯一语义源"
      exports: ["RoutingState", "evaluate_node_readiness", "select_successors", "compute_skippable", "diagnose_deadlock", "collect_inputs"]
      min_lines: 150
    - path: "server/workflows/engine/dag.py"
      provides: "DAGNode.incoming_edges 入边明细 (source_id, source_handle, target_handle)"
      contains: "incoming_edges"
    - path: "server/tests/workflows/test_engine_routing.py"
      provides: "路由/级联零 DB 单测"
    - path: "server/tests/workflows/test_engine_deadlock.py"
      provides: "死锁诊断零 DB 单测"
    - path: "server/tests/workflows/test_engine_inputs.py"
      provides: "输入归集零 DB 单测 + plan/coding_result 两条真实节点链 characterization"
  key_links:
    - from: "server/workflows/engine/routing.py"
      to: "server/workflows/engine/dag.py"
      via: "消费 DAGNode.incoming_edges / outgoing / back_edge_sources"
      pattern: "incoming_edges"
    - from: "server/workflows/engine/__init__.py"
      to: "server/workflows/engine/routing.py"
      via: "barrel 导出"
      pattern: "from workflows.engine.routing import"
---

<objective>
创建 `server/workflows/engine/routing.py` 纯函数路由核心（边感知就绪判定、skipped 级联、死锁诊断、target_handle 输入归集），并为 DAGNode 补充入边明细。这是 Phase 18 全部后续计划的语义地基：主循环（18-02/18-03）与回调续跑（18-04/18-05）共用这一套函数，消除审计定性的"两套路由实现漂移"根因。

Purpose: 落实 CONTEXT 锁定决策"调度主循环与回调续跑两条路径共用同一路由函数"（ENG-02）、"死锁结构化诊断"（ENG-04）、"按 target_handle 归集输入"（ENG-05）。纯函数 + plain dict 入参，零 DB 可测（与 Phase 17 template_resolver 同策略）。
Output: routing.py 模块 + dag.py 入边明细 + 三个零 DB 单测文件（同时是 18-VALIDATION.md Wave 0 缺口中 test_engine_routing/deadlock/inputs 的 unit 部分）。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-engine/18-CONTEXT.md
@.planning/phases/18-engine/18-RESEARCH.md
@.planning/phases/18-engine/18-PATTERNS.md
@server/workflows/engine/dag.py
@server/workflows/engine/template_resolver.py
</context>

## Artifacts this phase produces（本计划部分）

- 新文件 `server/workflows/engine/routing.py`：
  - `RoutingState` dataclass（`statuses: dict[str, str]`、`handles: dict[str, str]`）
  - 模块常量 `STATUS_COMPLETED / STATUS_SKIPPED / STATUS_FAILED / STATUS_TOLERATED / STATUS_WAITING / STATUS_RUNNING / STATUS_PENDING`
  - `evaluate_node_readiness(dag, node_id, state) -> str`（返回 `"ready" | "skip_failed" | "skip_unselected" | "blocked"`）
  - `select_successors(dag, node_id, next_handle) -> list[DAGNode]`（handle 匹配 + default 回退）
  - `compute_skippable(dag, state, pending) -> dict[str, str]`（fixpoint 级联，node_id → skip 原因）
  - `diagnose_deadlock(dag, state, pending) -> dict | None`
  - `collect_inputs(dag, node_id, node_outputs) -> dict`（target_handle 归集）
- 修改 `server/workflows/engine/dag.py`：`DAGNode.incoming_edges: list[tuple[str, str, str]]` 字段及 `from_workflow`/`afrom_workflow` 双构建器收集逻辑
- 修改 `server/workflows/engine/__init__.py`：导出 routing 公开 API
- 新测试 `server/tests/workflows/test_engine_routing.py`、`test_engine_deadlock.py`、`test_engine_inputs.py`（unit 部分）

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DAGNode 增加入边明细 incoming_edges（双构建器对称）</name>
  <files>server/workflows/engine/dag.py, server/tests/workflows/test_engine_routing.py</files>
  <read_first>
    - server/workflows/engine/dag.py（全文 ~210 行：DAGNode dataclass :14-37、from_workflow :58-72、afrom_workflow :76-98、_detect_back_edges :100-126、get_successors :189-196）
    - server/tests/workflows/test_dag.py（条件分支边 fixture 范式 :104-120，参考其 WorkflowEdge.objects.create 写法）
    - .planning/phases/18-engine/18-PATTERNS.md 的 dag.py 小节（收集点位置与 dataclass 风格）
  </read_first>
  <behavior>
    - Test 1: 经 `DAG.from_workflow` 构建后，目标节点 `incoming_edges` 含 `(source_id, source_handle, target_handle)` 三元组，source_id 为 str(UUID)，与 `incoming` 集合的成员一一对应（django_db fixture，参照 test_dag.py 建 workflow）
    - Test 2: 同一对节点间多条不同 handle 的边各自独立成元组（不去重）
    - Test 3: 手工构造 DAG（不经 ORM：直接实例化 DAG()、填 dag.nodes 与 incoming_edges）时字段默认值为空 list，旧调用零回退
  </behavior>
  <action>
    在 `DAGNode` dataclass（dag.py:14-21）追加字段 `incoming_edges: list = field(default_factory=list)`，行尾中文注释说明元素形状为 `(source_id, source_handle, target_handle)`、handle 缺省 "default"。在 `from_workflow`（:58-72）与 `afrom_workflow`（:76-98）两个构建循环内、`dag.nodes[target_id].incoming.add(source_id)` 同位置追加 `dag.nodes[target_id].incoming_edges.append((source_id, edge.source_handle or "default", edge.target_handle or "default"))`——两处必须对称（PATTERNS 已确认 edge 对象在该作用域完整可用）。不改动 back-edge 检测与 outgoing 分桶逻辑。测试写入 `server/tests/workflows/test_engine_routing.py` 的 `TestDagIncomingEdges` 类（django_db 标记仅此类需要）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/workflows/test_engine_routing.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `rg "incoming_edges" server/workflows/engine/dag.py` 至少 3 处命中（字段声明 + 两个构建器）
    - `from_workflow` 与 `afrom_workflow` 的 incoming_edges 追加语句逐字符一致（仅 sync/async 循环形式不同）
    - 新增测试全绿；`cd server && uv run pytest tests/workflows/test_dag.py -x` 既有 DAG 测试零回归
  </acceptance_criteria>
  <done>DAGNode 持有入边明细，ORM 双构建器对称收集，既有 DAG 测试不回退</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: routing.py 就绪判定/后继选择/级联纯函数 + 零 DB 单测</name>
  <files>server/workflows/engine/routing.py, server/workflows/engine/__init__.py, server/tests/workflows/test_engine_routing.py</files>
  <read_first>
    - server/workflows/engine/template_resolver.py:1-90（模块 docstring 契约声明范式、ResolutionSources dataclass 范式——routing.py 逐项照抄此结构）
    - server/workflows/engine/dag.py（forward_incoming :31-37、outgoing 分桶 :20、back_edge_sources、get_successors :189-196）
    - .planning/phases/18-engine/18-RESEARCH.md §2（两路径漂移清单）与 Pattern 3（边感知判定的精确定义——本任务的语义规范）
    - server/workflows/engine/scheduler.py:1411-1420（现状回调路由的 default 回退写法，select_successors 必须保持等价语义）
  </read_first>
  <behavior>
    - Test 1（分支选中）: 条件节点 completed 且 handles 给 "true" → true 边后继 evaluate 为 "ready"，false 边后继为 "skip_unselected"
    - Test 2（default 回退）: 源节点 next_handle="success" 但其出边只有 source_handle="default" → default 边视为选中，后继 "ready"
    - Test 3（菱形汇合一活一死）: A→B(true)/A→C(false)、B/C→D；B completed、C skipped → D 为 "ready"（CONTEXT 锁定：一条活路即执行）
    - Test 4（双支全 skip 级联）: B、C 均 skipped → D 为 "skip_unselected"；compute_skippable 对链式下游 fixpoint 全部标记
    - Test 5（前置失败 ANY 语义保留）: 任一 forward 依赖 status=failed（非 tolerated）→ "skip_failed"
    - Test 6（tolerated 算已解析且选中）: 依赖 status=tolerated → 边已解析、按 default 选中，后继可 ready
    - Test 7（back-edge 不阻塞）: 含反馈环（非 default handle 指向祖先）的节点，仅按 forward 入边判定，不因环边未解析而 "blocked"
    - Test 8（blocked）: 任一 forward 入边源仍 pending/running/waiting → "blocked"
    - Test 9（select_successors）: handle 命中返回该桶后继；未命中且 handle != "default" 时回退 default 桶（与 scheduler.py:1411-1420 现状等价）
    - 全部用例零 DB：手工构造 DAG()/DAGNode（node 属性用轻量 stub，仅需 id/short_id/name），不标 django_db
  </behavior>
  <action>
    新建 `server/workflows/engine/routing.py`。模块 docstring 用中文声明契约（照抄 template_resolver.py:1-23 范式）："本模块是调度主循环与回调续跑共用的纯函数路由核心，不 import Django ORM、不依赖 WorkflowEngine，输入为 DAG 对象 + plain dict；路由/级联/死锁/输入收集四类判定的唯一语义源（Phase 18 实现契约，ENG-02/04/05）"。

    定义模块常量 STATUS_COMPLETED="completed"、STATUS_SKIPPED="skipped"、STATUS_FAILED="failed"、STATUS_TOLERATED="tolerated"、STATUS_WAITING="waiting"、STATUS_RUNNING="running"、STATUS_PENDING="pending"，及终态集合 `_RESOLVED_STATUSES = {completed, skipped, tolerated}`。

    定义 `@dataclass RoutingState`：`statuses: dict[str, str]`（node_id → 上述常量之一）、`handles: dict[str, str]`（node_id → next_handle，缺省 "default"）。docstring 注明两字段由调用方填充：主循环热路径用内存 result["handle"]，重入重建用 DB output_data["_next_handle"]——纯函数不感知来源（封堵 RESEARCH Pitfall 2 的双来源不对称）。

    实现 `evaluate_node_readiness(dag, node_id, state) -> str`，语义严格按 RESEARCH Pattern 3：取节点 forward 入边（incoming_edges 过滤掉 source ∈ back_edge_sources 的边）；任一 forward 入边源 status == STATUS_FAILED → 返回 "skip_failed"（保留现状 ANY 语义，scheduler.py:576-583）；"入边已解析" := 源 status ∈ _RESOLVED_STATUSES；"入边选中" := 源 ∈ {completed, tolerated} 且（state.handles.get(source,"default") == 边 source_handle，或回退规则：源的 next_handle 在源节点 outgoing 的全部 handle 桶中无任何匹配时 source_handle=="default" 的边视为选中）；全部已解析且 ≥1 选中 → "ready"；全部已解析且 0 选中 → "skip_unselected"；否则 "blocked"。无 forward 入边的节点（入口）恒 "ready"。

    实现 `select_successors(dag, node_id, next_handle) -> list`：委托 dag.get_successors(node_id, next_handle)，空且 next_handle != "default" 时回退 dag.get_successors(node_id, "default")——与 scheduler.py:1411-1420 现状逐语义等价（characterization，禁止收紧）。

    实现 `compute_skippable(dag, state, pending) -> dict[str, str]`：对 pending 集合迭代调 evaluate_node_readiness，凡返回 skip_* 的节点记入结果（值为原因字符串）并在本轮内把其 status 临时视为 skipped 继续迭代，直至 fixpoint；不修改入参 state（纯函数，复制内部工作副本）。

    `server/workflows/engine/__init__.py` 追加 routing 公开 API 导出并扩 `__all__`（与 DAG/DAGNode/WorkflowEngine 并列）。

    测试写入 test_engine_routing.py（与 Task 1 的 django_db 类分离，纯函数类不标 django_db）。DAG 手工构造 helper 可做成模块级函数 `_build_dag(edges: list[tuple]) -> DAG` 供 deadlock/inputs 测试文件复用（保持本地 import，不进 conftest——conftest 工厂归 18-02）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/workflows/test_engine_routing.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `rg "from django|import django" server/workflows/engine/routing.py` 零命中（纯函数模块禁 ORM）
    - evaluate_node_readiness 返回值仅 {"ready","skip_failed","skip_unselected","blocked"} 四枚举（测试断言覆盖全部四值）
    - Test 1-9 全部存在且通过；菱形汇合/全 skip/back-edge 三个用例的断言与 CONTEXT 锁定决策原文一致
    - `python -c "from workflows.engine import routing"` 不报错（在 server 目录 uv run 下）；`__init__.py` 的 `__all__` 含新导出
  </acceptance_criteria>
  <done>routing 纯函数核心可用且零 DB 单测全绿，主循环/回调续跑后续计划可直接消费</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: diagnose_deadlock + collect_inputs 纯函数 + characterization 单测</name>
  <files>server/workflows/engine/routing.py, server/tests/workflows/test_engine_deadlock.py, server/tests/workflows/test_engine_inputs.py</files>
  <read_first>
    - .planning/phases/18-engine/18-RESEARCH.md §4（死锁现状与诊断目标形状）、§5（两条真实节点链的端口期望——归集规则兼容性的事实依据）、Pattern 5（非破坏性归集规则原文）、Code Examples 末段（诊断 JSON 目标形状）
    - server/workflows/engine/scheduler.py:1093-1107（现状 _collect_inputs 扁平合并——保底兼容语义的 characterization 对象）
    - server/tests/workflows/test_error_handling.py:709-722（结构化 error_message 末行 json.loads 断言范式，deadlock 测试照抄）
    - server/workflows/nodes/ai/plan_generation.py:329-352 与 server/workflows/nodes/ai/coding.py:706-712、server/workflows/nodes/ai/code_review.py:308-322（plan/coding_result 端口期望，characterization 用真实输出形状）
  </read_first>
  <behavior>
    - Test D1: 人造死锁状态（pending 两节点互为 forward 依赖、无 waiting/running、无 ready）→ diagnose_deadlock 返回 dict，键集 {"reason","pending"}，reason=="deadlock"，pending 每项含 {"node","short_id","waiting_on"}，waiting_on 每项含 {"node","short_id","status","handle"}
    - Test D2: 存在 status==waiting 或 running 的节点 → 返回 None（不误报，CONTEXT 锁定判定条件）
    - Test D3: pending 中存在 ready 节点 → 返回 None
    - Test D4: 诊断 dict 经 json.dumps(ensure_ascii=False) 后不含任何节点 output 值（构造带 output 的场景断言序列化串中无该值——V5 信息泄露防线）
    - Test I1（扁平保底）: 无 target_handle（或 "default"）的边 → 行为与现状 _collect_inputs 完全一致（逐上游 update 扁平合并）
    - Test I2（plan 链不双重嵌套）: 上游输出 {"plan": {...方案对象...}, "summary": ...}、边 target_handle="plan" → inputs["plan"] 仍是方案对象本身（同名键不覆盖规则生效），不得变成 {"plan": {"plan": ...}}
    - Test I3（coding_result 链命中端口键）: 上游输出顶层无 "coding_result" 键、边 target_handle="coding_result" → inputs["coding_result"] == 上游完整输出 dict
    - Test I4（多上游确定性）: 两个上游有同名扁平键 → 按 source_id 字符串排序的处理顺序决定胜者，重复调用结果稳定
    - 全部零 DB（复用 test_engine_routing.py 的 _build_dag helper 或本地构造）
  </behavior>
  <action>
    在 routing.py 追加两个纯函数。

    `diagnose_deadlock(dag, state, pending) -> dict | None`：判定条件严格按 CONTEXT 锁定决策——pending 非空、pending 中无任何节点 evaluate 为 "ready"、state.statuses 中无任何节点处于 STATUS_WAITING/STATUS_RUNNING；不满足返回 None。满足时对每个 pending 节点列出其未解析的 forward 入边：waiting_on 元素为 {"node": 源节点 name, "short_id": 源 short_id, "status": state.statuses.get(source, "unknown"), "handle": 边 source_handle}；返回 {"reason": "deadlock", "pending": [{"node", "short_id", "waiting_on"}]}。docstring 注明：只含拓扑元数据（名称/short_id/状态/handle），绝不读取 node_outputs（V5 信息泄露防线，同 Phase 17 约定）；不抛异常、由 scheduler 写入 amark_failed（引擎"结果不外抛"约定）。

    `collect_inputs(dag, node_id, node_outputs) -> dict`：按 RESEARCH Pattern 5 非破坏性叠加规则——取节点 incoming_edges 按 source_id 字符串排序逐条处理：(1) 若 source_id 在 node_outputs 中，inputs.update(node_outputs[source_id]) 扁平合并（现状语义保底）；(2) 若该边 target_handle 非空且非 "default"：当且仅当 target_handle 不在该上游自身的输出 dict 键中时，设 inputs[target_handle] = node_outputs[source_id]（上游完整输出对象）——同名键不覆盖规则防止 plan 链双重嵌套（RESEARCH §5：两个真实节点的端口期望互相矛盾，此规则是唯一同时兼容两者的交集）。docstring 引用 §5 两条链作为 characterization 锚点。

    测试分别写入 test_engine_deadlock.py（TestDiagnoseDeadlockUnit 类）与 test_engine_inputs.py（TestCollectInputsUnit 类），均不标 django_db。test_engine_inputs.py 的 I2/I3 用例注释中标注"characterization：plan_generation→ai_coding / ai_coding→ai_code_review 真实输出形状"并引用源文件行号。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/workflows/test_engine_deadlock.py tests/workflows/test_engine_inputs.py tests/workflows/test_engine_routing.py -x</automated>
  </verify>
  <acceptance_criteria>
    - diagnose_deadlock 返回 dict 可 json.dumps(ensure_ascii=False) 且键形状与 RESEARCH Code Examples 目标一致（reason/pending/waiting_on/short_id/handle）
    - Test D2/D3 证明"无 waiting/running 且无 ready"判定条件三要素齐备（漏一即误报/漏报）
    - Test I2 断言 `inputs["plan"]` 与方案对象同一性（is 或 ==），绝非双层嵌套
    - `rg "node_outputs" server/workflows/engine/routing.py` 中 diagnose_deadlock 函数体内零命中（诊断不读输出值）
    - 三个测试文件合计全绿，零 django_db 标记（D/I 类）
  </acceptance_criteria>
  <done>死锁诊断与输入归集纯函数全部可用，Phase 21 可直接 JSON.parse 诊断、Phase 19 NodePort 语义有了运行时落点</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 死锁诊断 → execution.error_message → 前端/API | 诊断内容会经 API 暴露给用户，不得携带节点输出值 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Information Disclosure | routing.diagnose_deadlock | mitigate | 诊断 JSON 仅含拓扑元数据（名称/short_id/状态/handle），函数签名不接收 node_outputs；Task 3 Test D4 断言序列化串不含输出值 |
| T-18-SC | Tampering | 包安装 | accept | 本阶段零新依赖（RESEARCH Package Legitimacy Audit：无安装），无供应链面 |
</threat_model>

<verification>
- `cd server && uv run pytest tests/workflows/test_engine_routing.py tests/workflows/test_engine_deadlock.py tests/workflows/test_engine_inputs.py -x` 全绿
- `cd server && uv run pytest tests/workflows/ -x` 既有 368 例零回归（本计划不改 scheduler，理论零影响，跑一遍兜底）
- `cd server && uv run ruff format --check workflows/engine/routing.py && uv run ruff check workflows/engine/`
</verification>

<success_criteria>
- routing.py 四类纯函数齐备且零 DB 单测覆盖：分支选中/default 回退/菱形汇合/全 skip 级联/前置失败/tolerated/back-edge/blocked/死锁三条件/归集四规则
- DAGNode.incoming_edges 双构建器对称收集
- 后续计划（18-02..05）可直接 import routing 而无需任何语义再设计
</success_criteria>

<output>
完成后创建 `.planning/phases/18-engine/18-01-SUMMARY.md`，记录：routing 公开 API 最终签名（18-02/03/04 消费）、_build_dag 测试 helper 位置、与 RESEARCH Pattern 3/5 的任何实现偏差。
</output>
