---
phase: 18-engine
plan: 05
type: execute
wave: 5
depends_on: ["18-02", "18-04"]
files_modified:
  - server/workflows/triggers/dispatcher.py
  - server/workflows/engine/scheduler.py
  - server/tests/workflows/test_engine_trigger_data.py
autonomous: true
requirements: [ENG-03]
must_haves:
  truths:
    - "任意触发方式（manual/api/feishu/webhook）经 dispatcher 创建的执行，trigger_data 含 source 键与 raw_payload 键（CONTEXT 锁定：缺省注入 {source, ...payload}）"
    - "真实调度执行中 {{trigger.source}} 与 {{trigger.raw_payload.*}} 可解析（ExecutionContext 注入 execution.trigger_data，不再永远空 dict）"
    - "resume_from_node 创建的新执行继承原 trigger_data（raw_payload/source 不丢失）并附加 metadata"
    - "trigger_data 形状零破坏：raw_payload 键保留、不新增 payload 别名键（Pitfall 8 characterization——coding 节点分支确认降级行为不变）"
  artifacts:
    - path: "server/workflows/triggers/dispatcher.py"
      provides: "trigger_data 统一写入 source 键"
      contains: "source"
    - path: "server/workflows/engine/scheduler.py"
      provides: "_execute_node 注入 trigger_data；resume_from_node 继承原 trigger_data"
      contains: "trigger_data=execution.trigger_data"
    - path: "server/tests/workflows/test_engine_trigger_data.py"
      provides: "ENG-03 集成测试（注入/解析/继承/形状 characterization）"
  key_links:
    - from: "server/workflows/engine/scheduler.py"
      to: "server/workflows/nodes/base.py"
      via: "ExecutionContext(trigger_data=...) 注入"
      pattern: "trigger_data="
    - from: "server/workflows/engine/template_resolver.py"
      to: "ExecutionContext.trigger_data"
      via: "trigger 前缀宽松下钻（Phase 17 已定稿，本计划零改动 resolver）"
      pattern: "trigger"
---

<objective>
打通 trigger_data 全链路：写入侧 dispatcher 统一补 `source` 键、`resume_from_node` 改为继承原 trigger_data 再附加 metadata；读取侧 `_execute_node` 构造 ExecutionContext 时注入 `execution.trigger_data`（现状唯一读取侧缺口——真实执行中 `{{trigger.*}}` 永远解析空 dict 的根因）。

Purpose: 落实 CONTEXT 锁定决策"trigger_data 注入（ENG-03）"——任意触发方式下 `{{trigger.*}}` 可解析；解析语义复用 Phase 17 定稿的 template_resolver 宽松路径，resolver 零改动（锁定边界：不扩大严格化）。排在 18-04 之后：coding_callback 手工 context（另一处不注入点）已随 18-04 删除，本计划注入点收敛为 `_execute_node` 单点。
Output: dispatcher source 键 + scheduler 两处修改 + test_engine_trigger_data.py（18-VALIDATION Wave 0 最后一个缺口文件）。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-engine/18-CONTEXT.md
@.planning/phases/18-engine/18-RESEARCH.md
@.planning/phases/18-engine/18-PATTERNS.md
@server/workflows/triggers/dispatcher.py
@server/workflows/engine/scheduler.py
@server/workflows/engine/template_resolver.py
</context>

## Artifacts this phase produces（本计划部分）

- 修改 `server/workflows/triggers/dispatcher.py`：`start_execution(trigger_data={"source": ..., "raw_payload": ...})` 统一写入 source 键
- 修改 `server/workflows/engine/scheduler.py`：
  - `_execute_node` 构造 ExecutionContext 处追加 `trigger_data=execution.trigger_data or {}`
  - `resume_from_node`（:1720-1725 对应位置）trigger_data 改为继承原执行 + 附加 metadata
- 新测试 `server/tests/workflows/test_engine_trigger_data.py`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 写入侧——dispatcher source 键 + resume_from_node 继承</name>
  <files>server/workflows/triggers/dispatcher.py, server/workflows/engine/scheduler.py, server/tests/workflows/test_engine_trigger_data.py</files>
  <read_first>
    - server/workflows/triggers/dispatcher.py（全文：TriggerContext 的 trigger_type 取值约定、:102-115 start_execution 调用点、:117-129 structlog 风格）
    - server/workflows/engine/scheduler.py:1644-1788（resume_from_node 全段，重点 :1720-1725 trigger_data 整体替换缺陷点）
    - .planning/phases/18-engine/18-RESEARCH.md §3（写入/读取全链路与形状兼容性约束原文——retry 读 raw_payload、normalizer 双键、coding 节点 payload miss 降级）、Pitfall 8
    - server/workflows/api/views.py:872-894（retry 入口读 trigger_data["raw_payload"]——存量消费方，形状不得破坏）
  </read_first>
  <behavior>
    - Test 1（dispatcher 写入形状）: 经 TriggerDispatcher.dispatch（trigger_type="manual"）创建的 WorkflowExecution，trigger_data == {"source": "manual", "raw_payload": <原 payload>}（允许既有附加键如 event_type 并存）
    - Test 2（形状 characterization）: trigger_data 中无 "payload" 键（Pitfall 8：禁止别名键，coding.py:775 的 miss→分支确认降级行为保持）
    - Test 3（resume_from_node 继承）: 原执行 trigger_data 含 source/raw_payload，resume_from_node 创建的新执行 trigger_data 含原 source 与 raw_payload 原值，且含 metadata（resumed_from/failed_node_id）
  </behavior>
  <action>
    1. `dispatcher.py:102-115`：`trigger_data={"raw_payload": context.raw_payload}` 改为 `trigger_data={"source": context.trigger_type, "raw_payload": context.raw_payload}`——source 直接透传 `context.trigger_type`（其取值即 manual/api/feishu/webhook 注册名，先 `rg "TriggerHandlerRegistry.register|trigger_type=" server/workflows/triggers/` 核实注册名枚举；若存在与锁定决策 manual/api 不一致的取值，按实际枚举透传并在 SUMMARY 记录映射）。禁止顺手加 "payload" 别名键（Pitfall 8）。
    2. `scheduler.py resume_from_node`（:1720-1725 对应位置，18-02/03/04 改造后行号会漂移，按 `rg "resumed_from" server/workflows/engine/scheduler.py` 定位）：`trigger_data={"metadata": {...}}` 改为 `trigger_data={**(execution.trigger_data or {}), "metadata": {"resumed_from": ..., "failed_node_id": ...}}`——继承原值，metadata 键追加（若原 trigger_data 已有 metadata 键则覆盖，属可接受语义）。
    3. 测试写入新文件 `server/tests/workflows/test_engine_trigger_data.py`：类 `TestTriggerDataWriteSide`（@pytest.mark.asyncio + @pytest.mark.django_db(transaction=True)）。dispatch 路径构造 TriggerContext(trigger_type="manual", raw_payload={...}) 走 `TriggerDispatcher.dispatch`（工作流用 conftest 最简工厂；执行不必跑完——断言 WorkflowExecution.trigger_data 形状即可，可 debug_mode 或 run_sync 后查库）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/workflows/test_engine_trigger_data.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `rg "\"source\"" server/workflows/triggers/dispatcher.py` 命中 start_execution 调用点
    - `rg "payload\":" server/workflows/triggers/dispatcher.py` 不含新增 "payload" 别名键
    - resume_from_node 的 trigger_data 构造含 `**` 展开继承（`rg "trigger_data" server/workflows/engine/scheduler.py` 核对）
    - Test 1-3 全绿；`cd server && uv run pytest tests/workflows/api/ tests/workflows/ -x` 含 retry/normalizer 存量消费方零回归
  </acceptance_criteria>
  <done>写入侧统一：所有 dispatcher 触发带 source 键，resume 执行不再丢 trigger_data</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 读取侧注入 + {{trigger.*}} 端到端解析测试</name>
  <files>server/workflows/engine/scheduler.py, server/tests/workflows/test_engine_trigger_data.py</files>
  <read_first>
    - server/workflows/engine/scheduler.py 的 `_execute_node` ExecutionContext 构造处（原 :928-938，按 `rg "ExecutionContext\(" server/workflows/engine/scheduler.py` 定位改造后位置）
    - server/workflows/nodes/base.py:80-95（ExecutionContext.trigger_data 字段定义 :85）
    - server/workflows/engine/template_resolver.py:219-220（trigger 前缀宽松下钻——零改动，仅确认消费路径）
    - server/tests/workflows/conftest.py（18-02 产出的 EchoTriggerDataNode）与 server/tests/workflows/test_error_handling.py 的 TemplateRenderNode 范式（config 模板渲染断言用）
  </read_first>
  <behavior>
    - Test 1（注入端到端）: trigger→EchoTriggerDataNode 工作流，start_execution(trigger_data={"source": "manual", "raw_payload": {"k": "v"}}, run_sync=True) → Echo NE 的 output_data["echoed_trigger"] == 传入 trigger_data（不再空 dict）
    - Test 2（{{trigger.*}} 模板解析）: 节点 config 含 "{{trigger.source}}" 与 "{{trigger.raw_payload.k}}" 的工作流（TemplateRenderNode 范式或 code 节点）run_sync → 渲染结果为 "manual" 与 "v"
    - Test 3（经 dispatcher 全链路）: TriggerDispatcher.dispatch(trigger_type="manual") + run_sync 类路径 → {{trigger.source}} 解析为 "manual"（写入侧+读取侧合龙）
    - Test 4（缺省宽松语义不变）: trigger_data 为空的执行中 "{{trigger.nonexistent}}" 维持 Phase 17 宽松行为（不因本计划改变——characterization，断言以现状 resolver 行为为准）
  </behavior>
  <action>
    1. `_execute_node` 的 ExecutionContext kwargs 追加一行 `trigger_data=execution.trigger_data or {}`（RESEARCH Pattern 4 原文；execution 对象在该作用域必有）。行尾中文注释标注"ENG-03 唯一读取侧缺口"。resolver/`get_trigger_data` 零改动（CONTEXT 锁定：不扩大严格化）。
    2. 测试追加到 test_engine_trigger_data.py：类 `TestTriggerDataReadSide`。Test 2 的模板渲染节点若 conftest 无现成，仿 test_error_handling.py 的 TemplateRenderNode 在测试文件内定义局部节点 + autouse 注册夹具（模式 A/B）。Test 4 先 `rg "trigger" server/workflows/engine/template_resolver.py` 与现有 resolver 测试确认宽松行为的精确表现（空串/原样保留），断言对齐现状。
    3. 跑 18-VALIDATION.md 阶段门禁前置：`cd server && uv run pytest tests/workflows/ -x` 全量确认五个 Wave 0 测试文件（routing/waiting/trigger_data/deadlock/inputs）齐备且全绿，在 SUMMARY 中对照 VALIDATION Per-Task Verification Map 逐行勾销。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/workflows/test_engine_trigger_data.py tests/workflows/ -x</automated>
  </verify>
  <acceptance_criteria>
    - `rg "trigger_data=execution.trigger_data" server/workflows/engine/scheduler.py` 恰好命中 _execute_node 注入点
    - `git diff server/workflows/engine/template_resolver.py` 为空（resolver 零改动）
    - Test 1-4 全绿；`cd server && uv run pytest tests/workflows/ -x` 全量绿（ENG-01..05 五个测试文件齐备）
    - `cd server && uv run ruff format --check workflows/ && uv run ruff check workflows/` 通过
  </acceptance_criteria>
  <done>{{trigger.source}}/{{trigger.raw_payload.*}} 在真实调度中可解析，ENG-03 闭环且 Wave 0 测试缺口清零</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 外部 webhook/feishu payload → trigger_data → 模板解析 | 不可信外部输入进入执行上下文 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-07 | Injection (V5) | trigger_data 透传到 ExecutionContext/模板 | mitigate | 只透传不执行：template_resolver 为纯取值替换（Phase 17 定稿，无 eval/exec 面）；本计划不改 resolver、不新增对 trigger_data 的代码执行路径；Test 4 锁定宽松取值语义 |
| T-18-08 | Spoofing | source 键伪造（外部 payload 自带 source 字段） | mitigate | source 键由 dispatcher 服务端按 trigger_type 写入顶层，raw_payload 内同名字段不会覆盖（形状分层），Test 1 断言顶层 source 为服务端值 |
| T-18-SC | Tampering | 包安装 | accept | 零新依赖 |
</threat_model>

<verification>
- `cd server && uv run pytest tests/workflows/test_engine_trigger_data.py -x` 全绿
- `cd server && uv run pytest tests/workflows/ -x` 全量绿（阶段五个 Wave 0 测试文件齐备）
- `cd server && uv run pytest` 全量绿（阶段门禁，18-VALIDATION.md Phase gate）
- `cd server && uv run ruff format --check workflows/ && uv run ruff check workflows/`
</verification>

<success_criteria>
- manual/api/feishu/webhook 触发的执行 trigger_data 统一含 source + raw_payload，{{trigger.*}} 真实可解析
- resume_from_node 不再丢失 trigger_data
- 形状 characterization 锁定：无 payload 别名、coding 分支确认降级行为不变、retry/normalizer 存量消费零回归
- 全阶段 pytest 门禁绿（ENG-01..05 测试矩阵齐备）
</success_criteria>

<output>
完成后创建 `.planning/phases/18-engine/18-05-SUMMARY.md`，记录：trigger_data 最终形状定稿（Phase 21 触发模型消费）、trigger_type 枚举透传映射、VALIDATION Per-Task Map 勾销情况、Open Question 1（payload 别名）留待 Phase 21 的决策记录。
</output>
