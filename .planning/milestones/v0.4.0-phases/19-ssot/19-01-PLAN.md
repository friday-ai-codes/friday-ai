---
phase: 19-ssot
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/workflows/nodes/base.py
  - server/workflows/api/serializers.py
  - server/tests/workflows/test_api.py
  - server/workflows/management/commands/dump_node_fixture.py
  - web/src/types/workflow/__fixtures__/node-types.fixture.json
  - web/package.json
autonomous: true
requirements: [SSOT-01, SSOT-03]
must_haves:
  truths:
    - "GET /api/node-types/ 每个节点返回 ui_schema 与 default_config 字段"
    - "default_config 的键全部出自 config_schema.properties 且仅含带 default 的字段"
    - "fetch_space_info 出现在 /api/node-types/，fetch_project_info 不出现"
    - "离线 fixture（node-types.fixture.json）由后端 NodeRegistry dump 生成并入库"
  artifacts:
    - path: "server/workflows/nodes/base.py"
      provides: "_get_default_config classmethod + get_schema 增 default_config key"
      contains: "_get_default_config"
    - path: "server/workflows/api/serializers.py"
      provides: "NodeTypeSerializer 暴露 ui_schema + default_config"
      contains: "default_config"
    - path: "server/workflows/management/commands/dump_node_fixture.py"
      provides: "从 NodeRegistry dump 精简节点 fixture 到前端版本库"
      contains: "node-types.fixture.json"
    - path: "web/src/types/workflow/__fixtures__/node-types.fixture.json"
      provides: "离线节点定义快照（node_type/category/inputs[].name/outputs[].name）"
  key_links:
    - from: "BaseNode.get_schema()"
      to: "NodeTypeSerializer"
      via: "新增 default_config/ui_schema key 被序列化器暴露"
      pattern: "default_config"
    - from: "dump_node_fixture command"
      to: "web/src/types/workflow/__fixtures__/node-types.fixture.json"
      via: "NodeRegistry.get_all_schemas() 落盘"
      pattern: "get_all_schemas"
---

<objective>
后端 `NodeRegistry` 是事实源：让 `BaseNode.get_schema()` 派生 `default_config`，`NodeTypeSerializer` 暴露 `ui_schema` 与 `default_config`（纯增量、零回归），并提供一个离线 fixture dump 管理命令把节点定义精简集落入前端版本库，作为 CI 漂移守护（SSOT-03）的对账基准。

Purpose: 闭合前端运行时驱动所需的两个缺失字段（D-01），并建立"不起后端即可对账"的离线快照基础设施。
Output: 扩字段后的 serializer、后端字段级断言、`dump_node_fixture` 命令、入库的 `node-types.fixture.json`。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-ssot/19-CONTEXT.md
@.planning/phases/19-ssot/19-RESEARCH.md
@.planning/phases/19-ssot/19-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 后端派生 default_config + serializer 暴露 ui_schema/default_config</name>
  <files>server/workflows/nodes/base.py, server/workflows/api/serializers.py</files>
  <read_first>
    - server/workflows/nodes/base.py L560-627（`_get_ui_schema()` L581-589 与 `get_schema()` L591-627，增 key 的 analog，见 19-PATTERNS.md L34-78）
    - server/workflows/api/serializers.py L785-809（`NodePortSerializer` + `NodeTypeSerializer`，见 19-PATTERNS.md L82-110）
    - 19-RESEARCH.md L173-190（Pattern 1 零回归扩字段依据）
  </read_first>
  <behavior>
    - get_schema() 返回 dict 新增 "default_config" key，值为 {字段名: default}，仅收集 config_schema.properties 中含 "default" 的顶层字段
    - config_schema 为空/无 properties 时 default_config 返回 {}（不抛错）
    - NodeTypeSerializer 序列化输出含 ui_schema（可为 null）与 default_config（dict）
  </behavior>
  <action>
    在 base.py 仿 `_get_ui_schema()` 新增 classmethod `_get_default_config()`：从 `(cls.config_schema or {}).get("properties", {})` 收集 `{k: v["default"] for k,v in props.items() if isinstance(v, dict) and "default" in v}`；`get_schema()` 返回 dict 增 `"default_config": cls._get_default_config()`（不删改既有 key，中文 docstring 保留）。在 serializers.py 的 `NodeTypeSerializer` 增 `ui_schema = serializers.JSONField(required=False, allow_null=True)` 与 `default_config = serializers.JSONField(required=False)`。不改 `NodeTypeViewSet` 与路由。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/workflows/test_api.py -k NodeType -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "_get_default_config" server/workflows/nodes/base.py` 命中 ≥2 行（定义 + 调用）
    - `rg -n "default_config|ui_schema" server/workflows/api/serializers.py` 命中 ≥2 行
    - `pytest -k NodeType` 既有 `test_node_types_have_metadata` 仍通过（零回归）
  </acceptance_criteria>
  <done>get_schema() 产出 default_config；NodeTypeSerializer 暴露 ui_schema+default_config；既有节点元数据测试不回退。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 扩展 TestNodeTypeAPI 字段级断言</name>
  <files>server/tests/workflows/test_api.py</files>
  <read_first>
    - server/tests/workflows/test_api.py L365-393（`TestNodeTypeAPI`，`test_node_types_have_metadata` L383-392 为 analog，见 19-PATTERNS.md L114-133）
    - 19-RESEARCH.md L319-331（pytest 断言示意）
  </read_first>
  <behavior>
    - 新增测试断言 /api/node-types/ 响应：fetch_space_info 在、fetch_project_info 不在
    - 每个节点含 ui_schema 与 default_config 键
    - 每个节点 default_config 的键 ⊆ config_schema.properties 的键
  </behavior>
  <action>
    在 `test_api.py::TestNodeTypeAPI` 沿用 `authenticated_admin_client` fixture 与 `/api/node-types/` 路径，新增 `test_node_types_expose_ui_schema_and_default_config`：构造 `by_type = {n["node_type"]: n for n in response.data}`；断言 `"fetch_space_info" in by_type`、`"fetch_project_info" not in by_type`；遍历断言每节点含 `ui_schema`/`default_config`，且 `set(n["default_config"]).issubset(set((n["config_schema"] or {}).get("properties", {})))`。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/workflows/test_api.py -k NodeType -x -q</automated>
  </verify>
  <acceptance_criteria>
    - 新增测试函数 `test_node_types_expose_ui_schema_and_default_config` 存在且通过
    - 测试显式断言 `"fetch_project_info" not in by_type`
  </acceptance_criteria>
  <done>后端对账测试覆盖新字段与幽灵节点缺席；`pytest -k NodeType` 全绿。</done>
</task>

<task type="auto">
  <name>Task 3: dump_node_fixture 管理命令 + 入库离线 fixture</name>
  <files>server/workflows/management/commands/dump_node_fixture.py, web/src/types/workflow/__fixtures__/node-types.fixture.json, web/package.json</files>
  <read_first>
    - server/workflows/management/commands/export_node_spec.py（全文：`Command(BaseCommand)`、`NodeRegistry.get_all_schemas()` L40、`--check` 范式——本任务直接 analog）
    - 19-PATTERNS.md L362-389（fixture 脚本范式与精简集字段）
    - 19-RESEARCH.md L378-382 / L413-419（免起 HTTP 直接 dump NodeRegistry；Wave 0 fixture 缺口）
  </read_first>
  <action>
    新建 `server/workflows/management/commands/dump_node_fixture.py`：仿 `export_node_spec.py` 的 `Command(BaseCommand)`，从 `NodeRegistry.get_all_schemas()` 取全集，映射精简集 `{"node_type","category","inputs":[{"name"}],"outputs":[{"name"}]}`（按 node_type 排序保证稳定 diff），写入 `web/src/types/workflow/__fixtures__/node-types.fixture.json`（顶层含 `node_count` + `nodes`，`json.dumps(ensure_ascii=False, indent=2)`，末尾换行）。文件头中文注释注明"改后端节点后须重跑 `uv run python manage.py dump_node_fixture`"（Pitfall 6）。运行命令生成并入库 fixture。`web/package.json` scripts 增说明性 `"gen:node-fixture"`（指向后端命令）。
  </action>
  <verify>
    <automated>cd server && uv run python manage.py dump_node_fixture && node -e "const f=require('../web/src/types/workflow/__fixtures__/node-types.fixture.json'); const t=f.nodes.map(n=>n.node_type); if(t.includes('fetch_project_info'))process.exit(1); if(!t.includes('fetch_space_info'))process.exit(1); console.log('ok',f.node_count)"</automated>
  </verify>
  <acceptance_criteria>
    - `web/src/types/workflow/__fixtures__/node-types.fixture.json` 存在且 `nodes[].node_type` 含 `fetch_space_info`、不含 `fetch_project_info`
    - fixture 每个节点含 `inputs`/`outputs`（数组，元素含 `name`）与 `category`
    - 再次运行 `uv run python manage.py dump_node_fixture` 后 fixture 内容稳定（git diff 为空，排序确定性）
  </acceptance_criteria>
  <done>离线 fixture 由后端命令生成并入库；含真实节点、无幽灵节点；可重复生成且 diff 稳定。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/node-types/ | 已认证客户端读取只读节点元数据；本计划仅增字段，不引入写路径 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Information Disclosure | NodeTypeSerializer.default_config | mitigate | default_config 仅取自 `config_schema.properties.*.default` 的静态默认值，不含运行时凭证；review 确认无敏感默认值落入 schema default |
| T-19-02 | Tampering | /api/node-types/ | accept | 端点 `IsAuthenticated` 只读，节点元数据全局静态、非租户敏感；无新增写入面 |
</threat_model>

<verification>
- `cd server && uv run pytest tests/workflows/test_api.py -k NodeType -x` 全绿（含新断言）
- `uv run python manage.py dump_node_fixture` 成功落盘且幽灵节点缺席
- 既有 `tests/workflows/` 不回退（wave 合并时 full suite）
</verification>

<success_criteria>
- GET /api/node-types/ 暴露 `ui_schema` + `default_config`，`default_config` 键 ⊆ `config_schema.properties`
- `fetch_space_info` 在、`fetch_project_info` 不在
- 离线 fixture 入库，供 19-05 漂移守护对账
</success_criteria>

## Artifacts this phase produces

- `server/workflows/nodes/base.py` — `_get_default_config()` + `get_schema()` 增 `default_config`
- `server/workflows/api/serializers.py` — `NodeTypeSerializer` 暴露 `ui_schema` + `default_config`
- `server/workflows/management/commands/dump_node_fixture.py` — 离线 fixture 生成命令
- `web/src/types/workflow/__fixtures__/node-types.fixture.json` — 入库节点定义快照
- `server/tests/workflows/test_api.py` — 字段级 + 幽灵缺席断言

<output>
Create `.planning/phases/19-ssot/19-01-SUMMARY.md` when done
</output>
