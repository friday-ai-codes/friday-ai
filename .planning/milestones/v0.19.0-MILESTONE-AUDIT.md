---
milestone: v0.19.0
milestone_name: 技术方案可信度
audited: 2026-07-31T18:17:27+08:00
status: gaps_found
phases: [105, 106, 107, 109, 110]
requirements_total: 19
requirements_satisfied: 13
requirements_partial: 4
requirements_not_met: 2
integration: seams_found
human_verification_outstanding: 26
---

# v0.19.0 技术方案可信度 — 里程碑审计报告

**审计时间:** 2026-07-31（UTC+8）
**范围:** Phases 105 / 106 / 107 / 109 / 110（Phase 108「方案深度」已于 2026-07-29 移交 v0.20.0，不在范围内）
**审计仓:** worktree `.claude/worktrees/v0.19-plan-trust`，分支 `milestone/v0.19.0-plan-trust`，HEAD `7750565b`，`main..HEAD` 292 commits
**结论:** **gaps_found** —— 19 条需求中 13 条满足、4 条部分满足、**2 条（ROUTE-01 / ROUTE-02）在用户侧未达成**。里程碑目标四个分句中的第二句「路由基于多维证据分层呈现并可解释」**对真实用户不成立**：承载分组、跨组标注、降级横幅与分数分解的 `RoutingDecisionPanel` 在 SPA 内没有任何挂载点。四道自动化闸门本次实跑全绿，26 项人工验收全部未执行。

---

## 1. 审计方法

- **不采信 VERIFICATION / REVIEW 的自述。** 五份 VERIFICATION 全为 `human_needed`（110 曾判 `gaps_found`，GAP-1 闭合后转 `human_needed`），它们本身是被审对象。所有判定回到源码与实跑。
- **四道闸门本次独立实跑**（见 §5），未引用相位报告的数字。
- **两组反向对照本次独立执行**：把实现改坏 → 跑测 → 还原 → 复跑确认工作区干净（见 §5.2）。针对本里程碑「五次被空转用例咬到」的历史，重点验证「证据是一条测试」的需求，其测试在实现缺席时是否真会变红。
- **接线核查用 grep 全仓扫挂载点与消费者**，不接受「组件存在 / 字段出参」当作「到达用户」。

---

## 2. 需求逐条核验（13 满足 / 4 部分 / 2 未达）

### Phase 105 — 编排解锁与评估标尺

| REQ | 判定 | 代码证据（本次实查） |
|---|---|---|
| **RELY-04** 置信度由分数 margin 确定性推导，LLM 降为输入 | ✅ **SATISFIED** | `repo_router_scoring.py:653-682` `derive_confidence` 严格实现 `S(1)>=θ_abs ∧ margin>=θ_margin → high`，`margin = max(0.0, s1-s2)`；`repo_router_v2.py:403` 降级路径与 `:235` 正常路径同一 `auto_selected` 规则；LLM 调整走 min 语义只降不升。**反向对照（本次执行）**：把 `derive_confidence` 短路为恒 `low`（即复现生产事故形态）→ `test_repo_router_v2_degraded.py` **14 failed**。不是空转用例。 |
| **ROUTE-07** 分数可展开到各信号贡献值，用户与开发者都能看懂 | ⚠️ **PARTIAL** | 后端半边成立：`score = math.fsum(breakdown.values())`（`:179`）按构造精确，`breakdown` 进工具出参 schema（`schemas/repository_relevance.py:42`）与快照。**用户半边不成立**：`rg breakdown web/src`（除测试）只有 `types/routing.ts:40` 的类型声明与 `RoutingDecisionPanel.vue` 的渲染，而该组件无挂载点（§3.1）。开发者可经 API / 快照读到，用户读不到。 |
| **ROUTE-08** golden set 回归门禁，退化能被自动检出 | ✅ **SATISFIED** | `test_repo_router_golden.py:170-211` 三条硬断言（Recall@5 不降 / Top-1 容忍 1 例 / 误自动选中率 ≤10%）+ `weight_set_version` 绑定守护，无特殊 marker 即随默认 suite 跑。**门禁有一处非对称，如实记录**：反向对照里 confidence 被打成恒 `low` 时，degraded 套件 14 红而 golden 门禁**全绿**（误自动选中率反而降到 0）。门禁能抓排序退化，抓不到「置信度整体塌陷」这一类正是本里程碑起因的故障形态。另 gk-001 仍是合成重写版，真实生产样本替换是 105-UAT #2（未执行）。 |
| **ROUTE-09** 同需求同索引重复路由同结果 | ✅ **SATISFIED** | 排序键 `(-round(score,6), repo_id)` 先量化再比；`test_shuffle_invariance_100_seeds` 乱序 100 次；缓存 key 含 model_id/模板版本/canonical 输入/decode/index_version；`test_repo_router_replay.py` 在 `--disable-socket` 下回放逐字段相等。 |

### Phase 106 — 多信号打分函数重构

| REQ | 判定 | 代码证据（本次实查） |
|---|---|---|
| **ROUTE-03** 大单体不再因命中节点多被系统性高估 | ⚠️ **PARTIAL** | 公式已落地且经 106-VERIFICATION 的 6 组反事实复核确认翻转由公式驱动。**但生产链路上尺寸归一化尚未启用**：`_breadth_signal`（`repo_router_scoring.py:345-358`）只有在 `n_r > 0 ∧ n_bar > 0` 时才算 `denom_size = 1-b+b·(N_r/N̄)`，二者均来自 `SystemSetting repo_router.nr_snapshot`；写入方是生产手动命令 `measure_repo_index_stats --write-snapshot`（106-UAT #1，**未执行**），缺失时 `denom_size = 1.0`——即 pivoted normalization 整段失效。106-VERIFICATION 的变体 E 已实测：关掉该归一后 `breadth(study-app)=0.1444 > breadth(onion-learning)=0.0544`，机制断言会失败。残余收益来自 106 把广度压进 `w_text·λ`（上限 0.1375，105 时是独占权重 0.20）+ 对数饱和，方向正确但不是需求承诺的机制。**这是本里程碑最典型的「生产方从未运行」接缝**（§3.2）。 |
| **ROUTE-04** 元数据参与排序打分而非只给 LLM 看 | ✅ **SATISFIED** | `domain` / `stack` / `team` 三键真进 `breakdown` 且 `Σ==score` 严格相等；缺失走键剔除 + 权重重归一化（「未知 ≠ 不匹配」）；多值 `0.8·max + 0.2·second`，全模块无 sum/mean 路径。注：T2 余弦通道按初值 `c_lo=0.25/c_hi=0.55` 运行，O-2 校准是 106-UAT #2（未执行）——影响的是匹配质量，不影响「元数据真正入分」这一机制。 |
| **ROUTE-05** 活跃度以连续量参与打分 | ✅ **SATISFIED** | `_activity_signal_v2` 指数衰减（106-VERIFICATION 独立探针实测 0d→2000d 严格递减）；废弃惩罚封顶在 activity 项内 `min(A, 0.10)`，不再乘性污染总分；无 `last_commit_at` 时退回枚举映射。 |
| **ROUTE-06** 运维不发版调权重 | ✅ **SATISFIED** | 专用端点 + `load_weight_config` 单点二次校验 + `post_save` 缓存失效；**前端设置区确有挂载**：`pages/admin/index.vue:10,121` `<RepoRouterWeightSettings />`（与 §3.1 的死组件相反，这一条接线是活的）。`weight_set_version` 进结果 / 快照 / baseline。残余：该组件 484 行零单测（MN-05 deferred），交互与渲染是 106-UAT #4（未执行）。 |

### Phase 107 — 分层呈现与链路韧性

| REQ | 判定 | 代码证据（本次实查） |
|---|---|---|
| **ROUTE-01** 路由结果分「本项目关联仓」/「全局候选」两组呈现 | ❌ **NOT MET（用户侧）** | 后端齐备：`repo_router_ranking.annotate_groups` / `decide_block_order`、trace 落 `block_order` 列、detail API 与工具出参都带该字段。**但全前端只有 `RoutingDecisionPanel.vue` 消费 `blockOrder`，而它没有挂载点**；`ClarificationCard.vue` 与 `RepoMultiSelector.vue` 各自 grep `group` / `cross_group` 均零命中。用户在对话里实际看到的候选来自 `useToolDisplay.relevanceCandidates`（`:126-153`），产出的是扁平的 `{id, name, score, level, evidence}`——没有分组、没有组标题、没有区内排序概念。详见 §3.1。 |
| **ROUTE-02** 跨组候选带「未关联当前平台」标注 | ❌ **NOT MET（用户侧）** | `cross_group_note` 在 `web/src` 里只有一处出现：`types/routing.ts:56` 的类型声明。没有任何组件渲染它（连死组件里的常驻说明句也一并到不了用户）。 |
| **RELY-02** 澄清不会无人应答地永久停在澄清阶段 | ⚠️ **PARTIAL** | 后半句成立且是真修复：`expire_pending_clarifications` 命令经 `adrive_convergence_session_to_pause_or_terminal` 真实续驱（不是只换状态标签），apscheduler job **无条件注册**、默认 60s 间隔（`agents/management/commands/runapscheduler.py:806-813`），CAS 幂等、两条立即出口、积压进 gauge。**前半句「澄清必达用户、可作答」未验证**——依赖真实飞书环境（107-UAT #1）。更值得注意的是 107-UAT #5：会话内 pending 态是否对用户可见，本 phase 未新增前端面，验收项自己写着「若实际不可见，作为独立缺陷进 backlog」——即这半句目前处于「假定既有实现满足」而非「已确认」。 |
| **RELY-03** 降级时用户能看见「本次未经 LLM 推理，置信度仅供参考」 | ⚠️ **PARTIAL** | 后端完整：6 值闭集 `DEGRADE_REASONS`、`derive_routing_degraded` 单一派生点、detail/override/实时三路一致、不回显原始异常串。**主承载（amber 横幅 + 主句 + 徽标灰化）在死组件里**——`degradeReason` 的唯一消费者是 `RoutingDecisionPanel.vue:246-253,443`。用户实际能看到的降级信号只剩 Phase 110 时间线在「路由」步上的一枚两字徽标「降级」（`useOrchestrationTimeline.ts:114,548`），**不带任何解释句**；且它依赖 `snapshot.stage0.degraded === true`，在 skipped / stub router / v1_fallback 三种情形下该键缺失、徽标不显示。需求原文要求的那句话，用户看不到。 |
| **RELY-05** 单次调用有重试与延迟上界 | ✅ **SATISFIED** | `for attempt in range(2)` 硬上界 1 次重试；`budget_deadline` 在循环外取一次，`remaining<=0` 直接抛不发第二次；per-attempt `timeout=min(timeout_seconds, remaining)`；退避受预算封顶；两次都失败即降级继续。有界重排的后置条件经 107 verifier 穷举 9460 组零违规。O-6 生产分位是测量而非机制，deferred 合规。 |

### Phase 109 — 双脊柱合流

| REQ | 判定 | 代码证据（本次实查） |
|---|---|---|
| **SPINE-01** 编排产出直连选仓→分支→确认编码→导出 | ✅ **SATISFIED（代码层）** | `OrchestratedPlanCard.vue` 挂在 `ChatMessageBubble.vue:1382`（真挂载，非死组件）；「进入编码」触发惰性投影后就地内嵌 `TechPlanCard`（`OrchestratedPlanCard.vue:155`）；`test_spa_coding_chain_e2e.py` 断言四步共用同一 `CodingPlan.id`。真机全链（真实 Git 平台 + Runner + 飞书）是 109-UAT #1（未执行）。 |
| **SPINE-02** 不再存在「对话模型徒手编写方案正文」的路径 | ✅ **SATISFIED** | `test_coding_tools_schema_guard.py` 用**键集合枚举式相等**断言（`set(properties) == 白名单`、`set(required) == 三键`）+ 函数签名断言三层守护，换名重新加回也会红。本次独立扫全仓生产侧 `tech_plan=` 写入点：全部来自投影（`plan_projection_service.py:510,530`）、MCP 执行服务（正文来自 `McpCodingPlanVersion`，Phase 94 起已 delegate 到统一编排）、离线迁移命令与 `coding_session_service.py:819` 的既有值拷贝——无 LLM 自由创作入口。 |
| **RELY-01** 方案一定来自完整编排链路，草稿显式标注 | ✅ **SATISFIED** | 服务端 fail-closed gate 在 `create_sessions_for_plan` 最前面、`acknowledge_unresearched is not True` 即抛（`coding_session_service.py:720-727`），回稳定机器码 `draft_requires_explicit_confirm`；三处判定全为**允许清单**形态（`!== 'orchestrated'`），未知取值落标注侧；界面横幅 + 头部常驻徽标 + 阻断式弹层；飞书导出物告示置于「## 技术方案」之前；`CodingExecutionSpec.unresearched` 进 dispatch payload。 |

### Phase 110 — 过程可观测

| REQ | 判定 | 代码证据（本次实查） |
|---|---|---|
| **OBS-01** 阶段进展与内容实时返回 | ✅ **SATISFIED** | `_emit_event` 内单点 fan-out（`convergence_session_service.py:334-390`），stage handler 零改动即自动获得推送；六阶段标签取 ROADMAP 原文；摘要全部取 payload 已有字段、零新增 LLM 调用。**残余不确定性**：全部 fan-out 用例都 monkeypatch 掉 `get_stream_writer`，端到端 SSE 从未真跑（110-UAT #1）。但即便直播链静默失效，退化也只是「2 秒快照节拍」——需求原文「而不是长时间静默后一次性吐出结果」在两种情形下都成立，故判 satisfied 而非 partial。 |
| **OBS-02** 调研容器日志对用户可见 | ✅ **SATISFIED（代码层）** | `runtime["plan_research_sessions"]` 是独立分支不混入 `deep_sessions`；归属链改用 `RepoResearchTask` 真 FK（去掉容器可写键）；仓库名服务端解析、`redact_secrets_in_text` 读取面脱敏；`PlanResearchLogGroup` 挂在 `ChatMessageBubble.vue:1372`。跨进程 `plan_session_id` 相等性本次 grep 复核：产出侧 `research_adapter.py:180/193` 与 `conversation_service.py:2832` 均为 `str(session.id)`，消费侧 `ChatMessageBubble.vue:917-921` 按同值过滤，形态一致；端到端实测仍为 110-UAT #2。 |
| **OBS-03** 前端展示阶段时间线，失败停在哪一步一目了然 | ✅ **SATISFIED（GAP-1 已闭合，本次复核确认）** | `OrchestrationStageTimeline` 挂在 `ChatMessageBubble.vue:1362`；失败原因走 7 值闭集。GAP-1 修复在 `useOrchestrationTimeline.ts:461`：`isFailed = status === 'failed' \|\| (status === null && folded.sawSessionFailed)`——快照在场仍以快照为准，仅前半程（快照缺席）认失败事件。**反向对照（本次执行）**：删掉事件兜底那一支 → spec **3 failed**（含「快照在场不被残留失败事件翻转」与「无失败事件不得凭空判失败」两条负向锚仍绿，说明修复没有把判据放松）。缺维用例已补齐（`spec:499-572` 覆盖「快照有无 × 会话状态 × runtime 活跃」）。`REQUIREMENTS.md` 把 OBS-03 标 Complete 现在是**准确的**（110-VERIFICATION 提出该质疑时缺口尚未闭合）。 |

**合计：13 SATISFIED / 4 PARTIAL / 2 NOT MET = 19。** 无孤儿需求，Traceability 表 19/19 映射与 PLAN frontmatter 声明一致。

---

## 3. 跨阶段集成检查 —— `integration: seams_found`

主链 105 确定性置信度 → 106 多信号打分 → 107 分组呈现 → 109 编排交棒 → 110 过程可见，**后端一路贯通**：`repo_router_scoring` 是 router / eval harness / replay 三方共用的唯一纯函数实现；106 在其上加信号、107 加 `repo_router_ranking` 的分组与有界重排、109 从 `artifact_version_id` 投影、110 从 `ConvergenceSessionEvent` 单表双链取数。断裂全部发生在**后端产物到用户之间**。

### 3.1 断裂一（BLOCKER）：`RoutingDecisionPanel` 无挂载点 —— 已在 STATE.md 登记，本次核实并扩大

**核实结论：属实，且影响面比 STATE.md 记录的更大。**

- `rg '<RoutingDecisionPanel'` 全 `web/src` **零命中**；非测试引用只剩注释与 `components.d.ts` 的自动导入声明。
- 锁测试确凿存在：`partsApiIntegration.spec.ts:178` 「RoutingDecisionPanel 已下线：即便 routing_trace_id + store 有 trace 也不渲染（与底部澄清卡去重）」。
- 下线发生在 `29247521`（2026-05-29），**早于本里程碑立项**，是一个刻意做过的产品决定。
- 替代面确认无承载：`ClarificationCard.vue` 与 `RepoMultiSelector.vue`（挂在 `TechPlanCard.vue:763,951`）grep `group` / `cross_group` / `trust` / `degraded` 全部零命中；`RelevanceBadge.vue` 只渲染 `score` 百分比。

**STATE.md 记为影响 ROUTE-01 / ROUTE-02 / RELY-03。本次审计增补：ROUTE-07 亦受同一断裂影响**——`breakdown` 在整个前端只被这一个组件渲染，「用户……能看懂它为什么排这个位置」的用户半边同样到不了。这条此前无人提出。

四条需求的用户半边合计压在一个无挂载点的组件上。之所以五个相位全绿仍未被发现：107 与 105 的 VERIFICATION 都以「组件内有渲染分支 + vitest 结构断言通过」为证据，而两相的「浏览器视觉核对」人工项从未执行；110 的 verifier 是唯一注意到该组件已退役的（写在 SC-4 的分析里），但按边界纪律正确地记为里程碑级缺口而非本相缺陷。

### 3.2 断裂二（MAJOR）：`nr_snapshot` 的生产方从未运行

同一类形态的第二个实例，方向相反——**消费方在线、生产方离线**：

| 环节 | 状态 |
|---|---|
| 生产方 | `measure_repo_index_stats --write-snapshot` 写 `SystemSetting repo_router.nr_snapshot`。**手动命令，生产实例从未执行**（106-UAT #1 pending） |
| 消费方 | `aload_nr_snapshot()` → 缺失返回空形状 → `_breadth_signal` 走 `denom_size = 1.0` |
| 后果 | ROUTE-03 承诺的 pivoted 尺寸归一化在生产上**未启用**，且**静默**——只有一条 `repo_router_nr_snapshot_missing` warning，用户与运维在结果里看不出差别 |

同族的还有两项：O-2 余弦校准常数按初值 `0.25/0.55` 运行（106-UAT #2）、`s_top` 在生产走 dense 还是 RRF 口径未观测（106-UAT #3）。三者都是「链路通、参数没喂」。

### 3.3 已核实为**接通**的接缝

| From → To | 载体 | 结论 |
|---|---|---|
| 105 scoring → 106 scoring → 107 ranking | 同一份 `repo_router_scoring` 纯函数，router/eval/replay 三方共用 | ✅ WIRED，无第二份推导实现 |
| 105 RELY-04 → 编排推进 | `clarify_adapter` 读 `session.routing candidates[].confidence` | ✅ WIRED（反向对照 14 红） |
| 107 澄清出口 → 引擎续驱 | `adrive_convergence_session_to_pause_or_terminal` + apscheduler 无条件注册 60s | ✅ WIRED（job 非开关控制，上线即生效） |
| 106 权重端点 → 管理页 | `pages/admin/index.vue:121 <RepoRouterWeightSettings />` | ✅ WIRED |
| 109 编排卡 → 投影端点 → TechPlanCard | `ChatMessageBubble.vue:1382` → `OrchestratedPlanCard.vue:155` | ✅ WIRED |
| 109 草稿 gate → 界面 → 导出 → dispatch | `provenance` 单一载体，三处允许清单判定 | ✅ WIRED |
| 110 事件 → SSE / 快照双链 → 时间线 / 日志组 | `ChatMessageBubble.vue:1362,1372`，同一张事件表同一把筛子 | ✅ WIRED |
| 110 `plan_session_id` 跨进程 | 两侧均 `str(ConvergenceSession.id)` | ✅ 形态一致（端到端实测挂账） |

### 3.4 需求接线映射表

| REQ | 集成路径 | 状态 | 断点 |
|---|---|---|---|
| RELY-04 | scoring → router → clarify_adapter → 引擎推进 | WIRED | — |
| ROUTE-07 | scoring.breakdown → 工具出参/trace → **RoutingDecisionPanel** | **PARTIAL** | 前端渲染者无挂载点；API 半边可用 |
| ROUTE-08 | golden fixture → eval harness → 默认 pytest suite | WIRED | 门禁对「置信度整体塌陷」不敏感 |
| ROUTE-09 | 稳定排序 + 缓存 key + 快照回放 | WIRED | — |
| ROUTE-03 | **nr_snapshot 生产方** → loader → `_breadth_signal` | **UNWIRED（生产）** | 生产命令未执行 ⇒ denom_size=1.0 |
| ROUTE-04 | facet resolver → breakdown → 排序 | WIRED | T2 常数未校准 |
| ROUTE-05 | last_commit 聚合 → activity 信号 | WIRED | — |
| ROUTE-06 | 端点 → SystemSetting → loader → route() → 管理页 | WIRED | 组件零单测 |
| ROUTE-01 | ranking.block_order → trace/出参 → **RoutingDecisionPanel** | **UNWIRED** | 无渲染者 |
| ROUTE-02 | cross_group_note → 出参 → **RoutingDecisionPanel** | **UNWIRED** | 全前端仅剩类型声明 |
| RELY-02 | 发卡 5 条失败路径 + 超时出口 job + 续驱 | WIRED | 送达半边与 pending 态可见性未确认 |
| RELY-03 | degrade_reason → API 三路 → **RoutingDecisionPanel**（主）/ 110 时间线徽标（次） | **PARTIAL** | 主承载缺席；次承载只有两字徽标无解释句 |
| RELY-05 | 重试/预算/有界重排 → ModelUsageRecord | WIRED | — |
| SPINE-01 | 编排 tool result → 投影端点 → TechPlanCard 四步 | WIRED | 真机全链未跑 |
| SPINE-02 | schema 收窄 + 签名 fixture + 全仓无第二写入口 | WIRED | — |
| RELY-01 | provenance → gate/界面/导出/dispatch | WIRED | 容器侧消费策略未定 |
| OBS-01 | `_emit_event` fan-out → SSE → store 分桶 | WIRED | 端到端 writer 解析未实测 |
| OBS-02 | plan_research_sessions 分支 → 日志组 | WIRED | 真实容器未验 |
| OBS-03 | 事件/快照双链 → 时间线 | WIRED | — |

无「单相位自足、零集成触面」的需求——19 条全部至少跨两层。

---

## 4. 里程碑头条主张对真实用户是否成立

里程碑目标四个分句，逐句判定。前提：五个相位的 VERIFICATION 全为 `human_needed`，26 项 UAT 无一执行，因此凡依赖真实环境的结论只能停在「代码层成立」。

| # | 分句 | 对用户成立？ | 依据 |
|---|---|---|---|
| 1 | 编排不再中途卡死被降级工具顶替 | **基本成立（代码层）** | 事故根因链已从三处切断：RELY-04 使 Stage 1 失联时仍出 high/medium 并自动推进（反向对照坐实）；RELY-02 的超时出口 job 无条件注册、真实续驱到终态；SPINE-02 把「绕道徒手编方案」这个出口从 schema 层焊死。残余：「澄清必达用户」这半句从未在真实飞书环境验证，而它正是原事故里会话停住的那一环。 |
| 2 | 路由基于多维证据分层呈现并可解释 | **不成立** | 「多维证据」成立于打分层（但 ROUTE-03 的尺寸归一化在生产未启用）；「分层呈现」与「可解释」对用户**完全不成立**——分组、跨组标注、降级横幅、分数分解四件事的唯一渲染者没有挂载点。用户在对话里看到的仍是一个扁平的 `{名称, 分数, 等级}` 列表，与本里程碑开工前没有可感差别。 |
| 3 | 编排产出直连执行流 | **代码层成立，真机未验** | 编排卡真挂载、投影幂等、四步共用一个 `CodingPlan.id` 有 e2e 护栏。但「容器真实拉起并产出 PR」是 109-UAT #1，未执行。 |
| 4 | 全过程对用户实时可见 | **成立（代码层）** | 时间线与调研日志组均真实挂载在编排气泡内；GAP-1（前半程失败时时间线撒谎）已闭合并经本次反向对照确认。残余是节奏问题（秒级直播 vs 2 秒快照）而非可见性问题。 |

**四分之一的分句在用户侧明确失败。** 这不是「差最后一步人工验收」，而是需求被建在一个已下线的组件里——即使把 26 项 UAT 全部执行，第 2 句也不会因此成立。

---

## 5. 自动化闸门与反向对照（本次独立实跑，非引用）

### 5.1 四道闸门

| 闸门 | 命令 | 结果 | 与相位报告声称 |
|---|---|---|---|
| 后端全量 | `uv run pytest tests/ -q -p no:cacheprovider --ignore=<三个沙箱受限文件>` | **8204 passed, 61 skipped, 26 deselected, 1 xfailed, 0 failed**（445.36s） | 一致 |
| 前端全量 | `cd web && CI=true pnpm vitest run --watch=false` | **1622 passed, 1 skipped / 202 files**（11.87s） | 一致 |
| 类型 | `npx vue-tsc --noEmit` | 退出码 **0**、零输出 | 一致 |
| 迁移 | `manage.py makemigrations --check --dry-run` | **No changes detected**，退出码 0 | 一致 |

排除的三个文件（`tests/services/test_commit_index.py` / `test_commit_index_integration.py` / `tests/mcp_tools/test_grep_repository.py`）为文件系统沙箱阻断临时目录 `git init`，与本里程碑改动无耦合。

### 5.2 反向对照（本次独立执行，两组，跑完即还原）

| # | 改坏点 | 结果 | 还原后 |
|---|---|---|---|
| NC-A | `useOrchestrationTimeline.ts:461` 删掉失败事件兜底（回退 GAP-1 修复） | **3 failed** / 75 passed；两条负向锚（「快照在场不被翻转」「无失败事件不得凭空判失败」）仍绿 | 78 passed，`git status` 干净 |
| NC-B | `derive_confidence` 短路为恒 `low`（复现生产事故形态） | `test_repo_router_v2_degraded.py` **14 failed**；**但 golden 门禁全绿** | 86 passed，`git status` 干净 |

NC-B 的副产物值得记入：golden 门禁的三条规则（Recall@5 / Top-1 / 误自动选中率）对「置信度整体塌陷」是**盲的**——误自动选中率反而变得更好。ROUTE-08 的门禁能防排序退化，防不住 v0.19.0 立项时那个具体故障的复发。补一条「degraded 路径下 auto_selected 必须为真」的门禁断言成本极低。

另抽查两处「证据是一条测试」的高载重项，结构上非空转：`test_coding_tools_schema_guard.py:82,94` 用键集合枚举式相等断言（换名加回也会红）；`test_repo_router_golden.py:170-211` 真对 baseline 逐项比较且绑定 `weight_set_version`。

---

## 6. 累积技术债与人工验收

### 6.1 载重债（会影响里程碑主张，必须处置）

| 项 | 相位 | 影响 |
|---|---|---|
| **`RoutingDecisionPanel` 无挂载点** | 107（跨 105/106） | ROUTE-01 / ROUTE-02 未达，ROUTE-07 / RELY-03 部分未达。**唯一的里程碑级 BLOCKER** |
| **生产 `nr_snapshot` 未写入**（106-UAT #1） | 106 | ROUTE-03 的核心机制在生产未启用；一条命令即可闭合 |
| **澄清必达真机链路**（107-UAT #1）+ **pending 态可见性**（107-UAT #5） | 107 | RELY-02 的前半句；后者甚至尚未确认既有实现是否满足 |
| **SPINE-01 真机全链**（109-UAT #1） | 109 | 里程碑第 3 分句唯一的端到端证据 |
| **SSE 直播链端到端**（110-UAT #1）+ **`plan_session_id` 跨进程相等**（110-UAT #2） | 110 | 前者失效只降级为 2s 节拍；后者失效会让日志组永不出现（OBS-02 全废） |

### 6.2 低后果债（可接受挂账）

| 相位 | 项 |
|---|---|
| 105 | IN-02 缓存命中路径快照 `stage1.response` 为空串（`cache_hit` 标志可回溯）；IN-03 confidence Tooltip 文案按 rank-1 语义写但展示在所有候选上（原定归 107 处理，**未处理**，且因面板下线已无显示面） |
| 106 | MN-05 权重设置组件零单测底座 + 网格外历史值只显示 placeholder；MN-07 golden 负向样本与 fixture 数值来源标注（原定「Phase 107 权重调参前作为独立任务」，**未处理**）；W-1 MEASUREMENTS §1 与 MJ-04 后的代码矛盾（一行订正） |
| 107 | MJ-02 α 参与 `auto_selected` 判定（方向单调安全，只抑制不误开，有参数化护栏）；IN-02 迁移文件名与内容不符（已 applied，不改名） |
| 109 | LO-01 gate 拒绝后重开的弹层是死胡同（触发条件苛刻但确为用户可见缺陷）；LO-02 `orchestratedPlanData` 用 `toolCalls.find`，同消息内两次编排会拿到同一 `artifact_version_id`；LO-03 `create_coding_plan` 的 `conversation_id` 不做一致性校验（跨用户越权已由 `_assert_owner` 挡死）；LO-04 常量位置可读性；LO-05 ROADMAP 「条件唯一约束」措辞已订正 |
| 110 | LO-01 `SubStepTimeline` 新增无条件 `:title` 与失败行 `role="alert"`（a11y 净收益，注释表述不准）；LO-02 两处新增行未过 `ruff format`；LO-03 `_emit_event` per-event `logger.info` 仍在（pre-existing，但与本相位刚立的纪律相反）；UI-MN-02 日志组折叠按钮 `aria-label` 盖掉可见文案（WCAG 2.5.3 Level A） |

**106 的 MN-05 / MN-07 明确写着「随 Phase 107 一并补齐」/「Phase 107 权重调参前作为独立任务」，Phase 107 完成后两项都仍未处置**——递延承诺落空，应改为显式挂账而非继续标注去向。

### 6.3 人工验收清单（26 项，全部 pending）

| 相位 | 数量 | 其中载重 |
|---|---|---|
| 105 | 3（O-1 生产实测 / gk-001 真实样本 / 分数分解目视） | 第 3 项因面板下线**已无从执行** |
| 106 | 4（nr_snapshot 写入 / O-2 校准 / S_top 口径观测 / 权重设置区交互） | 第 1 项载重 |
| 107 | 6（澄清必达 / 三块 UI 观感 / O-6 分位 / 出口 dry-run / pending 可见性 / D-1 跨项目仓名可见性判断） | 第 2 项因面板下线**已无从执行**；第 1、5 项载重；第 6 项是产品判断题，未答 |
| 109 | 6（真机全链 / 飞书告示 / 浏览器视觉 / lark_md 观感 / 迁移 0033 影响面 / 容器消费策略） | 第 1 项载重；第 5 项会让**全部存量方案卡集体显示「未经代码调研」横幅**，上线前必须先向用户交代 |
| 110 | 7（SSE 直播 / session_id 相等 / 容器日志 / GAP-1 复验 / 读屏 / live region 节奏 / 空心点辨识度） | 第 1、2 项载重 |

**其中 2 项已因 `RoutingDecisionPanel` 下线而不可执行**（105-UAT #3、107-UAT #2）——它们本该是第一批发现该缺口的人。

### 6.4 台账漂移（Housekeeping）

- `ROADMAP.md` 进度表 Phase 109 / 110 两行仍写 `0/TBD | Not started`，而两相位已 8/8、7/7 完成（106-VERIFICATION 的 W-2 指出过 106 行，随后 105/106/107 已订正，109/110 未跟进）。
- `REQUIREMENTS.md` Traceability 状态列口径不一：105/106/107 的 13 条仍为 `Pending`，109/110 的 6 条为 `Complete`，且勾选框 `[x]` 只打了 RELY-01 / SPINE-01 / SPINE-02。本次审计判定 13 SATISFIED，与该表两侧都对不上，完成里程碑时须按本报告统一翻转。
- 五个相位中 **110 无 `110-VALIDATION.md`**（105/106/107/109 均有），Nyquist 覆盖不完整。

---

## 7. 结论与放行条件

**status: gaps_found。** 本里程碑在后端是一次扎实的交付：事故根因链被三处切断且经反向对照坐实，打分函数重构由公式而非调参驱动（106-VERIFICATION 的 6 组反事实复核我复核后认可），双脊柱合流把徒手创作路径从 schema 层焊死，过程可观测的时间线与日志组真实挂载并在执行期自查出并闭合了 GAP-1。四道自动化闸门本次实跑全绿，8204 / 1622 / 0 / No changes 与相位报告完全一致。

**但它不能被称为已交付**，理由不是「人工验收没做完」，而是一个结构性问题：**ROUTE-01 / ROUTE-02 / ROUTE-07 / RELY-03 四条需求的用户可见半边，全部建在一个 2026-05-29 就已下线、且有锁测试断言其不渲染的组件里。** Phase 107 从设计到验证到 UI 审计，无一环发现自己在往一个没有挂载点的组件里加功能——因为唯一能发现它的那条人工项（浏览器视觉核对）恰恰是被延后的那条。这不是 107 一相的失误：105-06 的分数分解、106-05 的新信号中文标签同样落在这里。里程碑目标的第二个分句因此对真实用户不成立，且不会因为补做 UAT 而成立。

第二类问题是同一形态的镜像：`nr_snapshot` 的消费方在线、生产方是一条从未在生产执行过的手动命令，ROUTE-03 承诺的尺寸归一化因此在生产上静默失效。它比第一类容易修——跑一条命令而已——但同样说明「代码写完」与「用户拿到」之间还差一层没人负责的接线。

### 放行前必须完成（阻断项）

1. **对 `RoutingDecisionPanel` 做产品决策并落地**（STATE.md 已给三个候选，需人决定）：
   - ① 只恢复「解释」职能（分组 / 跨组标注 / 降级横幅 / 分数分解只读呈现），选仓仍留给澄清卡，重新挂载并撤销 `partsApiIntegration.spec.ts:178` 的锁测试；
   - ② 把分组与跨组标注下沉进 `RepoMultiSelector`，正式废弃该面板；
   - ③ 确认下线是长期决定 ⇒ **如实把 ROUTE-01 / ROUTE-02 退回未达、把 ROUTE-07 / RELY-03 降为部分满足，并从里程碑目标里删掉「分层呈现并可解释」这一分句**。
   选 ③ 也是可接受的诚实收口，但不能既保留目标措辞又保留 Complete 标记。
2. **在生产执行 `measure_repo_index_stats --write-snapshot`**，复跑一次真实路由确认 breakdown 里 breadth 不再是降级值——ROUTE-03 一条命令即可从 PARTIAL 转 SATISFIED。
3. **执行 5 项载重人工验收**：107-UAT #1（澄清必达真机）、107-UAT #5（pending 态可见性）、109-UAT #1（编排→PR 真机全链）、110-UAT #1（SSE 直播节奏）、110-UAT #2（`plan_session_id` 跨进程相等）。这五项分别是第 1、3、4 分句仅存的端到端证据。
4. **109-UAT #5 的上线交代**：迁移 0033 会让全部存量 `CodingPlan` 落 `provenance=draft`，历史方案卡集体出现「未经代码调研」横幅、送编码各弹一次确认。事实层正确，但必须先告知用户，否则会被当成故障。

### 建议同期处理（非阻断）

- 给 golden 门禁补一条「degraded 路径下 auto_selected 必须为真」的断言——NC-B 证明当前三条规则对本里程碑立项时那个具体故障是盲的。
- 106 的 MN-05 / MN-07 递延去向已落空，改为显式挂账。
- 台账三处漂移（ROADMAP 109/110 行、REQUIREMENTS Traceability 口径、110 缺 VALIDATION）在 `/gsd-complete-milestone` 时统一处置。
- 109-LO-01（gate 拒绝后的死胡同弹层）与 110-UI-MN-02（WCAG 2.5.3）各为一处小修，建议合入下一个 quick。

**若第 1 项走 ①/②，本里程碑可在补做人工验收后转 `tech_debt` 关闭；若走 ③，则应在如实下修需求与目标措辞后转 `tech_debt` 关闭。在三选一做出之前，不建议按「已交付」归档。**

---

## 8. ROUTE 缺口闭环（2026-08-02 追加）

**本节只记录 §7 放行条件第 1 项的落地结果。本报告的 `status: gaps_found` 未改动**——第 2、3、4
项（生产 `nr_snapshot` 未写入、5 项载重人工验收、109-UAT #5 的上线交代）以及 26 项 UAT 全未执行
的事实一条都没变，不在本次范围。

### 8.1 走的是 ①，但落点与 §7 的措辞不同

§7 给的候选 ① 是「重新挂载 `RoutingDecisionPanel` 的解释职能 + 撤销锁测试」。实际核到源码后
**改为把解释职能折进活下来的那条面，并删除该组件**。理由是候选 ① 在这条分支上不成立：

- 该面板的数据源是 `useRoutingStore` 按 `trace_id` 反查，而 store 只由**对话工具链路**写入；
  编排链路走 `repo.routing` 事件、手里没有 `trace_id`。挂到编排面上恒渲染空——那会原样复制
  本报告 §3.1 正在批评的形态。
- 它自带 Checkbox 与两个提交按钮，那正是当初与澄清卡重复、被下线的部分；挂到任何位置都会一起
  带回来。

因此最终形态：**只读解释面 `RoutingCandidateList.vue` 挂进 `ToolProcessGroup`「仓库分级路由」
步的 L2 详情区**（即 §2 ROUTE-01 行点名的 `useToolDisplay.relevanceCandidates` 那条活着的面），
选仓入口仍然只有底部澄清卡一个。RELY-03 因跨两条链，在编排时间线上另有一份承载。

### 8.2 四条需求的判定更新

| REQ | 本报告原判定 | 现判定 | 关键证据 |
|---|---|---|---|
| **ROUTE-01** | ❌ NOT MET（用户侧） | ✅ SATISFIED | 组标题与计数 `RoutingCandidateList.vue:202`；分组启用唯一依据仍是后端 `block_order`（`useToolDisplay.ts:278`），区顺序不前端重排；挂载 `ToolProcessGroup.vue:229` → `ChatMessageBubble.vue:1312` |
| **ROUTE-02** | ❌ NOT MET（用户侧） | ✅ SATISFIED | 组级常驻句 `:211` + 候选级徽标 `:236` + 置顶因果句 `:190`；文案取前端常量，`cross_group_note` 仍不进 DOM（T-107-06 保持） |
| **ROUTE-07** | ⚠️ PARTIAL（用户半边缺席） | ✅ SATISFIED | 披露开关 `:254` → 逐信号行 + 合计行 `:271`；六信号中文标签，未知 key 回显原文 |
| **RELY-03** | ⚠️ PARTIAL（主承载缺席、次承载只有两字徽标） | ✅ SATISFIED | 对话链 `RoutingCandidateList.vue:172`（横幅 + 原因 + 徽标灰化）；编排链 `OrchestrationStageTimeline.vue:181`（角标标位置、横幅说人话） |

**§2 合计相应更新为 16 SATISFIED / 2 PARTIAL（ROUTE-03、RELY-02）/ 0 NOT MET。**

### 8.3 §4 头条主张第 2 分句的重判

> 「路由基于多维证据分层呈现并可解释」

原判 **不成立**（唯一渲染者无挂载点）。现判 **代码层成立**：分组、跨组标注、分数分解、降级
解释四件事都在用户点得到的位置，并有挂载宿主级用例 + 变异验证守护。残余保留两条：
① ROUTE-03 的尺寸归一化在生产仍未启用（§3.2 不变，属「多维证据」的质量而非呈现）；
② 浏览器目视核对未做（见 8.6）。

### 8.4 §3.1 断裂一的处置

**已消除，且是结构性消除**：组件与它的 39 条隔离单测一并删除，四处 `vi.mock` 清掉。
§3.4 接线映射表相应更新：

| REQ | 集成路径 | 状态 |
|---|---|---|
| ROUTE-07 | scoring.breakdown → 工具出参 → `useToolDisplay.routingDecisionView` → `RoutingCandidateList` | **WIRED** |
| ROUTE-01 | ranking.block_order → 工具出参 → 同上 | **WIRED** |
| ROUTE-02 | 归属组 → 工具出参 → 同上（说明句取前端常量） | **WIRED** |
| RELY-03 | degrade_reason → 工具出参 → `RoutingCandidateList`（对话链）／ `repo.routing` 事件 → `useOrchestrationTimeline` → `OrchestrationStageTimeline`（编排链） | **WIRED（两条链）** |

### 8.5 附带闭合的一个本报告未记的洞

§2 RELY-03 行写「依赖 `snapshot.stage0.degraded === true`，在 skipped / stub router /
v1_fallback 三种情形下该键缺失、徽标不显示」——核到源码，机制比这更尖锐：`_h_route` 的快照
分支门是 `snapshot["stage0"]` 非空，而 **`v1_fallback` 的 snapshot 只有 stage1**
（`codegraph/services/repo_router_v2.py:1847`），于是它落到精简分支，而精简分支根本不带
`degraded`。换句话说，**降级徽标恰好在 `degraded=True` 的那条路径上永不显示**。

已在 `server/services/process_runtime/builtin_processes.py:177-179` 补齐三键（精简分支）与
`:136`（快照分支补 `degrade_reason`），加性、无迁移，配两条回归锚。这是本次**唯一**的后端改动。

`skipped` 保持 `degraded=False`——它是空 query、零候选、根本没路由，零候选时说「置信度仅供参考」
是在给一个不存在的置信度加注脚。键不再缺席，值如实为假。

### 8.6 §6.3 人工验收的变化

**105-UAT #3（分数分解目视）与 107-UAT #2（三块 UI 观感）从「已无从执行」恢复为「可执行、
未执行」**——本报告 §6.3 记这两项「因面板下线已无从执行」，现在浏览器里展开「分析过程 → 仓库
分级路由」即可目视核对。**26 项 pending 的总数不变。**

### 8.7 闸门（本次实跑，与本分支现测基线对比）

| 闸门 | 闭环前（本分支现测） | 闭环后 |
|---|---|---|
| 后端全量 | 8204 passed（本报告值，未变） | **8206 passed, 61 skipped, 26 deselected, 1 xfailed** |
| 前端全量 | 1630 passed / 1 skipped | **1604 passed / 1 skipped**（1630 − 39 删除的死组件单测 + 13 新增，精确对账，零 failed） |
| 类型 | 退出码 0 | **退出码 0** |
| 前端 lint | 112 problems（107E/5W），28 文件 | **111 problems（106E/5W），27 文件**，新增问题文件 0 |
| 构建 | ✓ 6.39s | **✓ 5.69s** |
| 迁移 | — | **No changes detected** |

变异验证 5 组（分组关闭 → 3 红；两条链的降级横幅各摘 → 2 红 / 2 红；把候选面从宿主摘掉即复现
「组件存在但无挂载点」原形态 → **11 红全灭**；回退后端降级键 → 3 红），跑完即还原、工作区干净。

完整记录见 `.planning/phases/ROUTE-GAP-CLOSURE.md`（本次闭环横跨 105/107/110 三相，不属于任何
单一相位目录，故置于 `phases/` 顶层）。

---
_Audited: 2026-07-31 by gsd-integration-checker（里程碑审计，goal-backward + 独立反向对照）_
_方法：五份 VERIFICATION / REVIEW / UI-REVIEW / UAT 通读后回源码复核 + 全仓挂载点与消费者 grep + 四道闸门实跑 + 2 组反向对照（改坏→跑测→还原→确认工作区干净）_
