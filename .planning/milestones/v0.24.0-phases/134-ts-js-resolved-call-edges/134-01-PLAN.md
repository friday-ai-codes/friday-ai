---
phase: 134-ts-js-resolved-call-edges
plan: 01
wave: 1
autonomous: true
requirements: [EDGE-01, EDGE-02, EDGE-03]
files_modified:
  - server/codegraph/resolver/base.py
  - server/codegraph/resolver/symbol_resolver.py
  - server/codegraph/resolver/tests/test_ts_js_resolution.py
---

# Plan 134-01：可审计 TS/JS 解析契约

## 目标

扩展统一解析结果并实现前端 alias、re-export 与静态 receiver binding；任何非唯一候选均
显式降级，不用全仓同名兜底。

## 任务

1. 为 `ResolveResult` 增加 status/language/call_shape/strategy/candidates/evidence，
   保持旧调用兼容。
2. 重构 `SymbolResolver` 的前端分支：确定性候选裁定、有界 re-export 链、
   namespace receiver 解析。
3. 新增 TS/JS 专项测试覆盖 resolved/ambiguous/unresolved 与证据链。

## 验证

`uv run pytest codegraph/resolver/tests/test_ts_js_resolution.py codegraph/resolver/tests/test_symbol_resolver.py -q --reuse-db`
