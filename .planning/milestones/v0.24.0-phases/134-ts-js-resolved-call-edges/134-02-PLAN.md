---
phase: 134-ts-js-resolved-call-edges
plan: 02
wave: 2
depends_on: [134-01]
autonomous: true
requirements: [EDGE-07]
files_modified:
  - server/codegraph/resolver/symbol_resolver.py
  - server/codegraph/resolver/wiring.py
  - server/codegraph/resolver/tests/test_wiring.py
---

# Plan 134-02：分支 dry-run 回填与派生投影失效

## 目标

把解析回填收紧到 `(repository, branch)`，提供零写入 dry-run 对比；实际写入后使目标
分支 Community、Process 和进程图缓存失效，并输出低基数汇总。

## 任务

1. `SymbolIndex`、ImportEdge、CallEdge 全链路增加 branch filter。
2. backfill 支持 `dry_run`，返回 resolved/ambiguous/unresolved/changed 汇总。
3. 非 dry-run 且有变更时删除目标分支派生投影并驱逐缓存；观测 best-effort。
4. 添加分支隔离、dry-run 和失效测试。

## 验证

`uv run pytest codegraph/resolver/tests -q --reuse-db`
