---
phase: 140-threshold-policy
plan: 02
type: execute
wave: 2
depends_on:
  - "140-01"
files_modified:
  - server/codegraph/benchmark_policies/graph_query_threshold_policy.v1.json
  - server/codegraph/services/graph_bench_compare.py
  - server/codegraph/management/commands/compare_graph_bench.py
  - server/tests/codegraph/test_graph_bench_policy.py
  - server/tests/codegraph/test_graph_bench_compare.py
  - server/tests/codegraph/test_compare_graph_bench_command.py
autonomous: true
requirements:
  - BENCH-06
  - BENCH-07
  - EDGE-06
must_haves:
  truths:
    - "threshold policy 是独立、版本化、只读、内容寻址且可审查的仓库 JSON；测试失败和 candidate/holdout 永不写回 baseline 或 policy。"
    - "comparator 先验证 policy/baseline/candidate 的 hash、identity、case set、evaluator 与 ranking，再计算四态 verdict。"
    - "overall、受保护桶、TS/JS、Python、Process、impact、trace、冷/热延迟与 token 各自判门，局部退化不能被 aggregate 抵消。"
    - "真实 baseline 缺失时正式 policy 不得含占位或伪造数值，阶段保持 human_needed/tech debt。"
  artifacts:
    - path: server/codegraph/benchmark_policies/graph_query_threshold_policy.v1.json
      provides: "由真实冻结 baseline 推导的独立静态 threshold policy；仅在证据齐全时产生"
    - path: server/codegraph/services/graph_bench_compare.py
      provides: "policy loader/hash、strict paired compare、四态 gate 纯函数"
    - path: server/codegraph/management/commands/compare_graph_bench.py
      provides: "只读三输入并输出审计报告的薄 I/O command"
  key_links:
    - from: server/codegraph/services/graph_bench_compare.py
      to: server/codegraph/services/graph_bench_eval.py
      via: "直接消费 evaluator 报告与 marker，不复制评分算法"
      pattern: "per_case|per_bucket|resolver"
    - from: server/codegraph/management/commands/compare_graph_bench.py
      to: server/codegraph/services/graph_bench_compare.py
      via: "读取 bytes/hash 后调用纯 comparator"
      pattern: "compare"
---

<objective>
把冻结 baseline 分布固化为不可自动改写的 threshold policy，并提供严格 paired、direction-aware、四态 fail-closed comparator。

Purpose: 让 BENCH-06/07 与 EDGE-06 从“报告数字”升级为可审查、可回放、不能被测试自刷新或 aggregate 掩盖的门禁。
Output: policy schema/静态产物、纯 comparator、薄 compare command 与完整单测。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.claude/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/140-threshold-policy/140-CONTEXT.md
@.planning/phases/140-threshold-policy/140-RESEARCH.md
@.planning/phases/140-threshold-policy/140-PATTERNS.md
@.planning/phases/140-threshold-policy/140-VALIDATION.md
@.planning/phases/140-threshold-policy/140-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 实现只读 policy schema 与四态 comparator</name>
  <files>server/codegraph/services/graph_bench_compare.py, server/codegraph/benchmark_policies/graph_query_threshold_policy.v1.json, server/tests/codegraph/test_graph_bench_policy.py, server/tests/codegraph/test_graph_bench_compare.py</files>
  <read_first>
    - server/codegraph/services/graph_bench_eval.py
    - server/services/code_graph/query_manifest.py
    - server/contracts/graph-query.v1.json
    - server/tests/codegraph/test_graph_bench_gold_schema.py
    - server/tests/codegraph/test_graph_bench_eval.py
    - .planning/phases/140-threshold-policy/140-RESEARCH.md
  </read_first>
  <behavior>
    - policy schema/version/status、baseline report/manifest hash、comparison identity、baseline system identity、candidate expectation、insufficient-data 规则与每个 gate 的 scope/metric/direction/baseline_value/allowed_abs_regression/required/protected 均显式必填。
    - 未知 schema/direction、非法或占位 hash、duplicate gate、隐式默认、marker 冒充数字、required gate 缺失均 fail-closed。
    - comparator verdict 闭集及优先级为 `INVALID` > `FAIL` > `INSUFFICIENT_DATA` > `PASS`。
    - higher_is_better 与 lower_is_better 分支显式；边界等号可过，marker 不参与算术。
    - required/protected missing 或样本不足为 FAIL；仅 optional sparse 为 `INSUFFICIENT_DATA`。
    - 至少一个 policy 声明的 primary quality metric 严格改善，其余 required 零退化，才能支持“提升”结论。
  </behavior>
  <action>
先用 table-driven 测试固定 policy 与 comparator 契约，再实现零 I/O `server/codegraph/services/graph_bench_compare.py`。使用 stdlib `dataclasses/json/hashlib`；policy 原始 bytes 算 `policy_sha256`，报告/case-set/evaluator 的 hash 口径字段名分开。先完整验证三份 artifact 和严格 case_id/bucket/cell 配对，任何 invalidity 后不再计算“部分通过”。质量与错误率按 RESEARCH 推荐零退化；延迟必须依据真实 paired raw trials 的 median/P95 噪声推导容差并写理由；token 未有稳定归因时保持 blocking `INSUFFICIENT_DATA`/tech debt，绝不能把 0 作为 gate。正式 policy 路径固定为 `server/codegraph/benchmark_policies/graph_query_threshold_policy.v1.json`。若 Plan 01 后仍没有真实 v0.22 baseline，不创建含占位值的该文件（可只提交 schema 测试用的非法样本于 tests/fixtures，不得入库正式 policy），并由 Plan 04 报 `human_needed`；若证据齐全，则从 baseline 精确复制 baseline_value，禁止使用 candidate 或 holdout 推导。loader 拒绝 `REPLACE_WITH_*` 与非 64-hex hash。
  </action>
  <acceptance_criteria>
    - policy loader 拒绝占位、缺键、默认方向/容差、重复 scope+metric 和非法 hash。
    - comparator 覆盖 identity/hash/evaluator/ranking/case set mismatch、missing/extra/duplicate case、两种方向、protected/required/optional sparse 与全部 marker。
    - comparator 输出 policy/baseline/candidate 三 hash、两类 identity、逐 case diff、逐 bucket/cell diff、gate details 和总 verdict。
    - 源码与测试不存在 `--update`、snapshot accept、baseline/policy write-back 或失败时自动刷新路径。
    - 无真实 baseline 时仓库中不存在带伪造数值的正式 `graph_query_threshold_policy.v1.json`。
  </acceptance_criteria>
  <verify>
    <automated>cd server && uv run pytest tests/codegraph/test_graph_bench_policy.py tests/codegraph/test_graph_bench_compare.py -x -q</automated>
  </verify>
  <done>BENCH-06 的不可变 policy 与 BENCH-07/EDGE-06 的四态 compare 契约可在零网络默认套件完整验证；无真实 baseline 时没有伪 policy。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 建立只读 compare command 与审计报告</name>
  <files>server/codegraph/management/commands/compare_graph_bench.py, server/tests/codegraph/test_compare_graph_bench_command.py</files>
  <read_first>
    - server/codegraph/management/commands/evaluate_graph_bench.py
    - server/tests/codegraph/test_evaluate_graph_bench_command.py
    - server/codegraph/services/graph_bench_compare.py
    - server/common/logging.py
  </read_first>
  <behavior>
    - command 只读取 baseline、candidate、policy，计算 raw-byte hashes，调用纯 comparator，并写新的 compare report。
    - `INVALID`、`FAIL`、`INSUFFICIENT_DATA` 均非零退出；只有全部 required gate 通过且 improvement evidence 成立才返回 PASS/零退出。
    - output 保存三 hash、两 run identities、两条 reproducible command、ranking/response/contract version、逐例与逐桶 diff。
    - 命令执行前后 baseline/candidate/policy bytes 完全不变。
  </behavior>
  <action>
按 `evaluate_graph_bench.py` 的 argparse/json/pathlib/`CommandError` 外壳实现 `server/codegraph/management/commands/compare_graph_bench.py`，但不重跑 scorer、不访问 ORM/Qdrant、不修改任何输入。为 command 增加 `graph_bench_compare_started/completed/failed` caller 生命周期，公共 kv 与评测 command 对齐：`category=caller`、`component=codegraph`、`initiated_by_user_id=system`；completed/failed 含 `duration_ms`；异常在埋点处经 `redact_secrets_in_text` 且观测独立 try/except。command integration test 用 `tmp_path` 和合成合法 artifact 测 I/O，不把这些合成数据当正式 baseline；断言输入 bytes 不变与失败退出语义。
  </action>
  <acceptance_criteria>
    - command 可对合法合成输入产生完整 PASS/FAIL 报告，对 hash/identity/case mismatch 明确 INVALID。
    - 三份输入在成功和失败路径均不变；代码无任何 update/accept/write-back 参数。
    - 日志不包含 query、凭证或 artifact 正文；观测失败不改变 compare 业务 verdict。
    - 事件使用 `duration_ms` 与 `initiated_by_user_id`，error 经 `redact_secrets_in_text`。
  </acceptance_criteria>
  <verify>
    <automated>cd server && uv run pytest tests/codegraph/test_compare_graph_bench_command.py tests/codegraph/test_graph_bench_compare.py tests/codegraph/test_graph_bench_policy.py -x -q</automated>
  </verify>
  <done>compare command 可审计、只读、可回放，失败不会刷新基线或阈值。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo-owned policy → comparator | 人审静态规则进入自动 gate，必须 schema/hash/闭集验证 |
| baseline/candidate files → compare command | 可被替换的文件系统输入进入通过判定，需内容寻址和严格配对 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-140-05 | Tampering | threshold policy | mitigate | 独立只读 JSON、raw-byte hash、占位拒绝、无 update/write-back 路径 |
| T-140-06 | Tampering | baseline/candidate pairing | mitigate | 三 hash、comparison identity、case/gold/evaluator 严格配对，失配 INVALID |
| T-140-07 | Repudiation | gate verdict | mitigate | 保存逐 gate 理由、逐例/桶 diff、两命令和两类 system identity |
| T-140-08 | Denial of Service | observability | mitigate | 日志 best-effort，不能改变 comparator/command verdict |
| T-140-SC | Tampering | package supply chain | accept | 仅使用 Python stdlib 与既有 Django/pytest/structlog，无新增依赖 |
</threat_model>

<verification>
运行 policy/comparator/command 测试；静态检查没有 update/accept/write-back。若真实 baseline 未提供，验证正式 policy 不存在或不含任何占位/伪数值，并把外部证据缺口交给 Plan 04 的 human_needed closure。
</verification>

<success_criteria>
- policy 只能由真实冻结 baseline 一次性推导并审查，candidate/holdout/失败测试不能改写。
- comparator 对所有 required/protected/capability/language gate 独立判定。
- 只有身份有效且全部 required gates 通过时才能输出 PASS。
</success_criteria>

<artifacts_this_phase_produces>
- `server/codegraph/services/graph_bench_compare.py` 纯函数 policy/comparison 核心。
- `compare_graph_bench` 只读审计命令及 compare report schema。
- 有真实 baseline 时的 `server/codegraph/benchmark_policies/graph_query_threshold_policy.v1.json`；否则为明确 human_needed/tech debt，绝不提交伪数值。
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/140-threshold-policy/140-02-SUMMARY.md` when done.
</output>
