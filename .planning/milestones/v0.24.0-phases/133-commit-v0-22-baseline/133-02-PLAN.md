---
phase: 133-commit-v0-22-baseline
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - server/tests/fixtures/graph_bench/manifest.json
  - server/tests/fixtures/graph_bench/dev.json
  - server/tests/fixtures/graph_bench/locked_test.json
  - server/tests/fixtures/graph_bench/holdout.json
  - server/tests/fixtures/graph_bench/README.md
autonomous: true
requirements:
  - BENCH-02
must_haves:
  truths:
    - "冻结数据集含 manifest + dev + locked_test + holdout 三切分文件，manifest 记录 gold_version/annotated_at_sha/splits 三键（BENCH-02）"
    - "每条 case 显式标注 language/framework/entry_type 分桶维度；edge_golds 显式 call_shape 与 evidence_file_line 独立标注锚点，不从被测 codegraph 反向导出（BENCH-02，防循环论证）"
    - "dev/locked_test 至少各含覆盖全部指标路径（symbol/process/edge/trace/impact）与多个分桶的 seed case；holdout 为空壳 schema 留 Phase 140（BENCH-02）"
    - "README 声明标注口径、分桶维度、防反导约束与数据集再生成/扩容 runbook（BENCH-02）"
  artifacts:
    - path: server/tests/fixtures/graph_bench/manifest.json
      provides: "数据集级身份 gold_version + annotated_at_sha + splits 清单"
      contains: "gold_version"
    - path: server/tests/fixtures/graph_bench/dev.json
      provides: "baseline 用 dev 切分 seed case（含分桶维度与 edge evidence 锚点）"
    - path: server/tests/fixtures/graph_bench/locked_test.json
      provides: "baseline 用 locked test 切分 seed case"
    - path: server/tests/fixtures/graph_bench/holdout.json
      provides: "holdout 空壳（Phase 140 填充），baseline 阶段不读"
    - path: server/tests/fixtures/graph_bench/README.md
      provides: "标注口径 / 分桶维度 / 防反导声明 / 扩容 runbook"
  key_links:
    - from: server/tests/fixtures/graph_bench/dev.json
      to: server/tests/fixtures/graph_bench/manifest.json
      via: "split 文件名与 manifest.splits 映射一致；case.split 字段与所在文件一致"
      pattern: "\"split\": \"dev\""
    - from: server/tests/fixtures/graph_bench/locked_test.json
      to: server/codegraph/services/graph_bench_eval.py
      via: "case 分桶维度取值落在 validate_gold_case 的闭集枚举内（language/framework/entry_type/call_shape）"
      pattern: "\"entry_type\""
---

<objective>
落地 Phase 133 的可版本化冻结 gold 数据集（独立于被测 codegraph）：manifest + dev/locked_test/holdout 三切分 + README。这是 BENCH-02 的核心交付——独立标注、三切分、resolved edge gold 来自独立 callsite 标注而非被测图反导。

Purpose: baseline 只在冻结数据上才可比。数据集先行（schema 由 Plan 01 定义并将在 Plan 04 被权威校验），让 Plan 04 的 command 有真实输入可跑，也让后续扩容/评审有据可依。
Output: `server/tests/fixtures/graph_bench/` 下 5 个版本化文件。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.claude/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/133-commit-v0-22-baseline/133-CONTEXT.md
@.planning/phases/133-commit-v0-22-baseline/133-RESEARCH.md
@server/tests/fixtures/hybrid_graph_capable_golden/README.md

<interfaces>
<!-- gold schema（与 Plan 01 validate_gold_case/validate_gold_dataset 完全对齐；本 plan 只 authoring JSON，不写校验代码）。分桶四维为必填标注字段，因 Symbol/Endpoint 模型无显式 language/framework 字段，不从被测图派生。 -->

manifest.json（数据集级）：
- gold_version: 字符串，随数据演进递增（初始 "1"）
- annotated_at_sha: 冻结 commit SHA（字符串，与评测目标仓水位对齐）
- repository / branch: 评测目标标识
- splits: { "dev": "dev.json", "locked_test": "locked_test.json", "holdout": "holdout.json" }
- _doc: 防反导声明（gold 独立标注，禁止从被测 codegraph 反向导出；resolved edge gold 来自独立 callsite 抽样）

case（每条，分桶维度必填）：
- case_id, split, query
- language ∈ {python, typescript, javascript, go}
- framework ∈ {django, vue, gin, none}
- entry_type ∈ {http_endpoint, process_entry, plain_symbol}
- expected_symbols: [{uid, file_path, start_line, name}]
- expected_processes: [{process_key, name}]
- edge_golds: [{caller_uid, callee_uid, call_shape ∈ {direct, member, import_alias, receiver, from_import}, evidence_file_line}]   # evidence_file_line 必填（独立 callsite 标注锚点）
- trace_golds: [{source_uid, target_uid}]
- impact_golds: [{seed_uid, expected_affected_uids: [uid,...]}]
- protected: bool（缺省 false；受保护桶标 true）
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 编写 manifest + dev + locked_test 三个数据集文件</name>
  <files>server/tests/fixtures/graph_bench/manifest.json, server/tests/fixtures/graph_bench/dev.json, server/tests/fixtures/graph_bench/locked_test.json</files>
  <read_first>
    - .planning/phases/133-commit-v0-22-baseline/133-RESEARCH.md  （「冻结数据集与 gold schema」节的 JSON 样例；「分桶与 INSUFFICIENT_DATA」节的分桶维度）
    - server/tests/fixtures/repo_route_recall/cases.json  （既有版本化 fixtures 的 JSON 形态参考）
    - .planning/phases/133-commit-v0-22-baseline/133-CONTEXT.md  （BENCH-02 决策：独立标注、三切分、防反导）
  </read_first>
  <action>
    创建 `server/tests/fixtures/graph_bench/` 目录并写三个 JSON 文件（UTF-8、2 空格缩进、确保为合法 JSON）。

    `manifest.json`：按 <interfaces> 结构写 gold_version="1"、annotated_at_sha（填一个明确的占位冻结 SHA 字符串，并在 `_doc` 注明「评测者运行前须以目标仓实际 `last_indexed_commit_sha` 对齐」）、repository/branch 占位标识、splits 三键映射、`_doc` 防反导声明（gold 独立标注、禁止从被测 codegraph 反向导出、resolved edge gold 来自独立 callsite 抽样）。

    `dev.json` 与 `locked_test.json`：各写一个 `{ "cases": [...] }` 结构。每个切分编写覆盖全部五条指标路径与多个分桶的 seed case：
    - 至少 1 条 python/django/http_endpoint、1 条 typescript/vue/plain_symbol、1 条 process_entry；两切分分桶组合不完全相同（让 Plan 03 的分桶与 INSUFFICIENT_DATA 逻辑有真实多桶输入）。
    - 每条 case 填齐 language/framework/entry_type 必填维度；query 为非空白自然语言。
    - 每条 case 按其对应用途填 expected_symbols / expected_processes / edge_golds / trace_golds / impact_golds（不用的维度给空列表）；edge_golds 每条都填 call_shape 与 evidence_file_line（形如 `path/to/file.py:123` 的人工标注锚点字符串）。
    - 至少 1 条 case 标 protected=true（供受保护桶单列逻辑）；至少 1 条 impact_golds 用例、1 条 trace_golds 用例。
    - uid 用稳定可读字符串（如 `py:app/urls.py::router`），在同一切分内自洽；这些 seed case 用于让 harness 端到端可跑，README 会说明完整独立标注（对真实冻结仓）是评测者的后续动作。
  </action>
  <verify>
    <automated>cd server && for f in tests/fixtures/graph_bench/manifest.json tests/fixtures/graph_bench/dev.json tests/fixtures/graph_bench/locked_test.json; do python -c "import json,sys; d=json.load(open('$f')); print('$f OK')"; done</automated>
  </verify>
  <acceptance_criteria>
    - 三个文件均为合法 JSON（上方 verify 退出码 0）
    - `cd server && python -c "import json; m=json.load(open('tests/fixtures/graph_bench/manifest.json')); assert all(k in m for k in ('gold_version','annotated_at_sha','splits')); assert all(k in m['splits'] for k in ('dev','locked_test','holdout')); print('manifest OK')"` 退出码 0
    - `cd server && python -c "import json; [json.load(open(f'tests/fixtures/graph_bench/{s}.json')) for s in ('dev','locked_test')]; cases=[c for s in ('dev','locked_test') for c in json.load(open(f'tests/fixtures/graph_bench/{s}.json'))['cases']]; assert all(all(k in c for k in ('language','framework','entry_type','query')) for c in cases); assert any(c.get('protected') for c in cases); assert any(c.get('impact_golds') for c in cases); assert any(c.get('trace_golds') for c in cases); assert any(all('evidence_file_line' in e and 'call_shape' in e for e in c.get('edge_golds',[])) for c in cases if c.get('edge_golds')); print('cases OK')"` 退出码 0
    - `cd server && python -c "import json; cases=[c for s in ('dev','locked_test') for c in json.load(open(f'tests/fixtures/graph_bench/{s}.json'))['cases']]; langs={c['language'] for c in cases}; ets={c['entry_type'] for c in cases}; assert 'python' in langs and 'typescript' in langs; assert 'process_entry' in ets; print('buckets OK')"` 退出码 0
    - `cd server && python -c "import json; L={'python','typescript','javascript','go'}; F={'django','vue','gin','none'}; E={'http_endpoint','process_entry','plain_symbol'}; CS={'direct','member','import_alias','receiver','from_import'}; cases=[c for s in ('dev','locked_test') for c in json.load(open(f'tests/fixtures/graph_bench/{s}.json'))['cases']]; assert all(c['language'] in L and c['framework'] in F and c['entry_type'] in E for c in cases); assert all(e['call_shape'] in CS for c in cases for e in c.get('edge_golds',[])); print('closed-set OK')"` 退出码 0（闭集取值校验提前到 wave 1，不 import Plan 01 模块）
  </acceptance_criteria>
  <done>manifest 含三键与 splits 映射；dev/locked_test 各含覆盖全部指标路径与多语言/入口分桶的 seed case；edge gold 带 evidence_file_line；至少一条 protected、一条 impact、一条 trace 用例；JSON 全部合法</done>
</task>

<task type="auto">
  <name>Task 2: 编写 holdout 空壳 + README（标注口径 / 防反导 / 扩容 runbook）</name>
  <files>server/tests/fixtures/graph_bench/holdout.json, server/tests/fixtures/graph_bench/README.md</files>
  <read_first>
    - server/tests/fixtures/hybrid_graph_capable_golden/README.md  （既有 fixtures README 的结构与口吻）
    - .planning/phases/133-commit-v0-22-baseline/133-RESEARCH.md  （「Common Pitfalls」Pitfall 5 防反导、「冻结数据集与 gold schema」节；Open Question 1 的扩容建议）
    - .planning/phases/133-commit-v0-22-baseline/133-CONTEXT.md  （BENCH-02 决策：holdout 留最终验收）
  </read_first>
  <action>
    `holdout.json`：写 `{ "cases": [] }` 空壳（合法 JSON），供 Phase 140 最终验收填充；baseline 阶段 command 不读它（README 与 Plan 04 command 都会钉死这一点）。

    `README.md`（中文，沿袭既有 fixtures README 风格）写清：
    - 数据集用途：v0.22 图查询能力的同仓同 commit 原始 baseline 评测；baseline 只用 dev + locked_test，holdout 留 Phase 140 最终验收。
    - 标注口径：四个分桶维度（language/framework/entry_type/call_shape）为必填标注字段及其闭集取值；expected_symbols/expected_processes/edge_golds/trace_golds/impact_golds 各字段含义。
    - 防循环论证声明：resolved edge gold 来自独立 callsite 抽样人工/规则标注并以 evidence_file_line 记录核验锚点；禁止用 `CallEdge` 现值反向回填 gold（README 用文字声明此约束，不引用具体私有变量名）。
    - 水位对齐：annotated_at_sha 必须与评测目标仓的 `last_indexed_commit_sha`（按 `(repository, branch)`）一致，否则 run 标 INVALID。
    - 扩容 runbook：如何选定并冻结目标仓（选定已索引仓 + 其 per-branch `last_indexed_commit_sha`）、如何按 split 增加独立标注 case、如何递增 gold_version；说明当前 dev/locked_test 为最小 seed 集，完整独立标注是评测者运行真实 baseline 前的后续动作。
  </action>
  <verify>
    <automated>cd server && python -c "import json; d=json.load(open('tests/fixtures/graph_bench/holdout.json')); assert d=={'cases':[]}; print('holdout OK')" && test -s tests/fixtures/graph_bench/README.md && echo README OK</automated>
  </verify>
  <acceptance_criteria>
    - `cd server && python -c "import json; assert json.load(open('tests/fixtures/graph_bench/holdout.json'))=={'cases':[]}"` 退出码 0
    - `cd server && grep -cE 'annotated_at_sha|last_indexed_commit_sha' tests/fixtures/graph_bench/README.md` ≥ 1（水位对齐说明存在）
    - `cd server && grep -cE 'holdout|locked_test|dev' tests/fixtures/graph_bench/README.md` ≥ 1（切分口径说明存在）
    - `cd server && grep -ciE 'callsite|反向|反导|独立标注' tests/fixtures/graph_bench/README.md` ≥ 1（防反导声明存在）
  </acceptance_criteria>
  <done>holdout 为空壳合法 JSON；README 写明标注口径、四维分桶、防反导声明、水位对齐与扩容 runbook</done>
</task>

</tasks>

## Artifacts this phase produces (Plan 02)

- `server/tests/fixtures/graph_bench/manifest.json` — 数据集身份（gold_version / annotated_at_sha / splits / 防反导 _doc）
- `server/tests/fixtures/graph_bench/dev.json` — baseline 用 dev 切分 seed case（含分桶维度 + edge evidence 锚点）
- `server/tests/fixtures/graph_bench/locked_test.json` — baseline 用 locked test 切分 seed case
- `server/tests/fixtures/graph_bench/holdout.json` — holdout 空壳（Phase 140 填充）
- `server/tests/fixtures/graph_bench/README.md` — 标注口径 / 分桶维度 / 防反导声明 / 扩容 runbook

字段（case 级，供 Plan 03/04 消费）：`language` / `framework` / `entry_type` / `expected_symbols[].uid` / `expected_processes[].process_key` / `edge_golds[].call_shape` / `edge_golds[].evidence_file_line` / `trace_golds[]` / `impact_golds[]` / `protected`。

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 人工/规则标注 → gold fixtures | gold 是 ground truth，若被被测 codegraph 现值污染则整个 baseline 失去意义（循环论证） |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-133-04 | Tampering | dev.json / locked_test.json (edge_golds) | critical | mitigate | resolved edge gold 独立 callsite 标注 + evidence_file_line 锚点必填 + README 防反导声明；禁止从被测 CallEdge 反向回填；fixtures 版本化入 git 可审查 diff |
| T-133-05 | Tampering | manifest.json (annotated_at_sha / gold_version) | high | mitigate | annotated_at_sha 与目标仓水位对齐是 INVALID 校验前提；gold_version 随标注演进递增，便于 Phase 140 追溯 |
| T-133-SC | Tampering | pip/uv installs | high | mitigate | 本 phase 零新增生产依赖，无安装面，supply-chain 门不触发 |
</threat_model>

<verification>
- 5 个 JSON/MD 文件存在；4 个 JSON 全部合法
- manifest 三键 + splits 三切分映射齐全；case 四维分桶必填 + edge evidence 锚点 + protected/impact/trace 用例存在
- README 含标注口径、防反导声明、水位对齐与扩容 runbook
</verification>

<success_criteria>
- 冻结数据集结构完整（manifest + 三切分），可版本化、独立于被测图（BENCH-02）
- 每条 case 显式分桶维度；edge gold 带独立 callsite 锚点，未从被测 codegraph 反导（BENCH-02）
- dev/locked_test 可直接作为 Plan 04 command 的真实输入；holdout 空壳留给 Phase 140
</success_criteria>

<output>
Create `.planning/phases/133-commit-v0-22-baseline/133-02-SUMMARY.md` when done
</output>
