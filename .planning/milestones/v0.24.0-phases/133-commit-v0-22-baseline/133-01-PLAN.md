---
phase: 133-commit-v0-22-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/codegraph/services/graph_bench_eval.py
  - server/tests/codegraph/test_graph_bench_watermark.py
  - server/tests/codegraph/test_graph_bench_gold_schema.py
autonomous: true
requirements:
  - BENCH-01
  - BENCH-02
must_haves:
  truths:
    - "三方水位（index_built_at_sha / gold_annotated_at_sha / source_checkout_sha）任一为空或不全相等时，validate_watermark 返回 INVALID；仅当三者非空且相等才返回 OK（fail-closed，BENCH-01）"
    - "run identity 以 (repository, branch, commit_sha, index_key, gold_version) 五元组构造，可序列化为含 index_key_source 字段的 dict 供 run manifest 复用（BENCH-01）"
    - "gold case 缺少必填分桶维度 language/framework/entry_type 或 manifest 缺 gold_version/annotated_at_sha/splits 时，schema 校验拒绝并指明缺失字段；call_shape 仅 edge_golds 需要且取闭集值（BENCH-02）"
    - "language/framework/entry_type/call_shape 四个维度取闭集枚举值，越界值被校验拒绝（BENCH-02）"
  artifacts:
    - path: server/codegraph/services/graph_bench_eval.py
      provides: "纯函数评测模块（零 I/O）：run identity + 三方水位校验 + gold schema 校验"
      exports: ["RunIdentity", "build_run_identity", "validate_watermark", "GoldDataset", "GoldCase", "validate_gold_dataset", "validate_gold_case"]
      contains: "def validate_watermark"
    - path: server/tests/codegraph/test_graph_bench_watermark.py
      provides: "BENCH-01 水位与五元组单测（默认套件，--disable-socket 下可跑）"
    - path: server/tests/codegraph/test_graph_bench_gold_schema.py
      provides: "BENCH-02 gold schema 校验单测（默认套件）"
  key_links:
    - from: server/codegraph/services/graph_bench_eval.py
      to: "(none)"
      via: "纯函数零 I/O —— 不 import django ORM / qdrant / network；水位 sha 由调用方注入"
      pattern: "^(?!.*(qdrant|models import|objects\\.)).*$"
    - from: server/tests/codegraph/test_graph_bench_watermark.py
      to: server/codegraph/services/graph_bench_eval.py
      via: "import build_run_identity / validate_watermark 直接断言"
      pattern: "from codegraph.services.graph_bench_eval import"
---

<objective>
建立 Phase 133 的纯函数评测地基（零 I/O、可在默认 `--disable-socket` 套件单测）：run identity 五元组构造、三方水位一致性校验（fail-closed → INVALID）、以及 gold 冻结数据集的 schema 定义与校验。

Purpose: BENCH-01 要求「水位不一致即标 INVALID 且不出部分结论」，BENCH-02 要求 gold 数据集有可校验的分桶维度与版本化身份。把这两块做成纯函数，让口径在默认套件可测、与生产实现零漂移，并供 Plan 03 的指标模块与 Plan 04 的 command 直接复用。
Output: `graph_bench_eval.py`（本 plan 只产 identity/watermark/gold-schema 三段；指标与分桶由 Plan 03 追加到同一文件）+ 两个纯函数测试文件。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.claude/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/133-commit-v0-22-baseline/133-CONTEXT.md
@.planning/phases/133-commit-v0-22-baseline/133-RESEARCH.md
@server/codegraph/services/repo_route_recall_eval.py

<interfaces>
<!-- 模式来源：repo_route_recall_eval.py 的纯函数分工（零 I/O、dataclass + to_dict、__all__、macro 聚合理由注释）。本模块复刻其形态，但【不】引入其 baseline 比对/容差语义（属 Phase 140）。 -->

关键约束（已核对于本仓源码，仅供理解水位来源；本模块保持纯函数，不直接读这些模型——sha 由 Plan 04 command 注入）：
- 索引水位（per-branch）：`repositories/models.py` `RepositoryBranchIndex.last_indexed_commit_sha`(L747) / `RepositoryBranchIndex.head_sha`(L745) / `is_base_branch`(L742)
- 仓级兜底：`Repository.last_indexed_commit_sha`(L233)
- 派生水位：`codegraph/models.py` `ProcessTrace.built_at_sha`(L427) / `SymbolCommunity.built_at_sha`(L370)
- 已知缺口：`_resolve_built_at_sha` 只按 repository_id 取值不带 branch —— 故水位必须按 `(repository_id, branch)` 由 command 解析后注入，本模块只比对注入的三个 sha。

gold 闭集枚举（schema 校验必须强制）：
- language ∈ {python, typescript, javascript, go}
- framework ∈ {django, vue, gin, none}        # 无显式 framework 字段，由标注者显式填写（防反导）
- entry_type ∈ {http_endpoint, process_entry, plain_symbol}
- call_shape ∈ {direct, member, import_alias, receiver, from_import}   # 仅 edge_golds 需要
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: run identity 五元组 + 三方水位校验（纯函数）</name>
  <files>server/codegraph/services/graph_bench_eval.py, server/tests/codegraph/test_graph_bench_watermark.py</files>
  <read_first>
    - server/codegraph/services/repo_route_recall_eval.py  （纯函数形态：__all__、@dataclass + to_dict、模块 docstring 说明存在理由；复刻其风格但剔除比对/容差语义）
    - .planning/phases/133-commit-v0-22-baseline/133-RESEARCH.md  （「水位与评测身份」节的三方水位表与校验规则；「Pattern 2」的 index_key 映射）
  </read_first>
  <behavior>
    - validate_watermark(index_built_at_sha="", gold_annotated_at_sha="x", source_checkout_sha="x") → "INVALID"（任一为空即 INVALID）
    - validate_watermark(index_built_at_sha="a", gold_annotated_at_sha="a", source_checkout_sha="b") → "INVALID"（不全相等）
    - validate_watermark(index_built_at_sha="a", gold_annotated_at_sha="a", source_checkout_sha="a") → "OK"
    - validate_watermark 支持可选第四参 process_built_at_sha：传入且与三者不一致 → "INVALID"；不传则不参与判定
    - build_run_identity(repository="r", branch="b", commit_sha="a", index_key="a", gold_version="1") → RunIdentity，其 to_dict() 含五个键 + index_key_source（默认 "last_indexed_commit_sha"）
    - 空 repository / 空 gold_version 构造时抛 ValueError（身份必填）
  </behavior>
  <action>
    在 `server/codegraph/services/graph_bench_eval.py` 新建模块（本 plan 的第一个 task，文件不存在则创建；模块 docstring 用中文说明「只做算术、零 I/O，水位 sha 由调用方注入」）。声明 `__all__`。

    实现两个纯函数与一个 dataclass（均不 import django ORM / qdrant / 网络，不读文件）：
    - `@dataclass RunIdentity`：字段 repository、branch、commit_sha、index_key、gold_version、index_key_source（默认 "last_indexed_commit_sha"，供 Phase 140 演进复合键）；`to_dict()` 返回含全部字段的 dict（沿袭 repo_route_recall_eval 的 to_dict 形态）。构造时 repository 或 gold_version 为空串 → raise ValueError。
    - `build_run_identity(*, repository, branch, commit_sha, index_key, gold_version, index_key_source="last_indexed_commit_sha") -> RunIdentity`：直接装配；`branch` 允许为空串（"" = base 分支，与 `Symbol.branch_name` 同口径）。
    - `validate_watermark(*, index_built_at_sha, gold_annotated_at_sha, source_checkout_sha, process_built_at_sha=None) -> str`：返回 "OK" 或 "INVALID"。规则（fail-closed）：三个必填 sha 任一为空/None → "INVALID"；三者集合大小 != 1 → "INVALID"；process_built_at_sha 非 None 且与公共 sha 不等 → "INVALID"；否则 "OK"。

    先写 RED 测试 `server/tests/codegraph/test_graph_bench_watermark.py`（覆盖 <behavior> 全部用例 + 空值/不等/相等/第四参四条路径 + RunIdentity.to_dict 含 index_key_source + 空身份 ValueError），跑出失败后再写实现转 GREEN。遵循项目 ruff（line-length 100）与 mypy（ Django/DRF stubs）约束。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/codegraph/test_graph_bench_watermark.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd server && uv run pytest tests/codegraph/test_graph_bench_watermark.py -x -q` 退出码 0
    - `cd server && uv run pytest tests/codegraph/test_graph_bench_watermark.py --collect-only -q | grep -cE '::' ` ≥ 6（覆盖空值/不等/相等/第四参/to_dict/空身份）
    - `cd server && grep -cE 'qdrant|\.objects\.|import django' codegraph/services/graph_bench_eval.py` == 0（保持零 I/O 纯函数）
    - `cd server && uv run ruff check codegraph/services/graph_bench_eval.py` 退出码 0
  </acceptance_criteria>
  <done>validate_watermark 对空值/不一致/一致/第四参与否四条路径返回正确；RunIdentity.to_dict 含 index_key_source；模块零 I/O；测试与 lint 全绿</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: gold 冻结数据集 schema 与校验（纯函数）</name>
  <files>server/codegraph/services/graph_bench_eval.py, server/tests/codegraph/test_graph_bench_gold_schema.py</files>
  <read_first>
    - server/codegraph/services/graph_bench_eval.py  （Task 1 已建的模块头/__all__/风格，向其追加）
    - .planning/phases/133-commit-v0-22-baseline/133-RESEARCH.md  （「冻结数据集与 gold schema」节的 manifest 与 case schema JSON；「分桶与 INSUFFICIENT_DATA」节的四个分桶维度必填结论）
    - server/tests/fixtures/hybrid_graph_capable_golden/README.md  （fixtures README 约定，供理解版本化 fixtures 形态）
  </read_first>
  <behavior>
    - manifest 缺 gold_version / annotated_at_sha / splits → validate_gold_dataset 抛错并指明缺失键
    - splits 缺 dev / locked_test / holdout 任一 → 抛错
    - case 缺 language / framework / entry_type（必填分桶维度）→ 抛错并指明 case_id + 缺失字段
    - case 的 language/framework/entry_type 越出闭集 → 抛错；edge_golds 的 call_shape 越出闭集 → 抛错
    - case 缺 query 或 query 为空白 → 抛错（与 QUERY-01 空白拒绝同口径）
    - 合法最小数据集（含全部必填维度 + 空 expected 列表）→ 校验通过并返回 GoldDataset，其 cases 为 GoldCase 列表
    - edge_golds[].callee_uid 存在时必填 evidence_file_line（独立 callsite 标注锚点），缺 → 抛错（防反导）
  </behavior>
  <action>
    在 `server/codegraph/services/graph_bench_eval.py` 追加 gold schema 段（保持纯函数，输入为已 `json.load` 的 dict，不读文件——文件 I/O 归 Plan 04 command）。

    定义 dataclass（均带 to_dict，沿袭同文件已有风格）：
    - `GoldCase`：case_id、split、query、language、framework、entry_type、expected_symbols(list[dict])、expected_processes(list[dict])、edge_golds(list[dict])、trace_golds(list[dict])、impact_golds(list[dict])、protected(bool=False)。
    - `GoldDataset`：gold_version、annotated_at_sha、repository、branch、splits(dict)、cases(list[GoldCase])。

    定义模块级闭集常量 `LANGUAGES`/`FRAMEWORKS`/`ENTRY_TYPES`/`CALL_SHAPES`（取值见 <interfaces>）。

    实现校验函数（失败时 raise ValueError，消息含 case_id 与缺失/越界字段名，便于定位）：
    - `validate_gold_case(case: dict) -> GoldCase`：必填 query（非空白）、language/framework/entry_type（存在且 ∈ 闭集）；edge_golds 每条 call_shape ∈ 闭集、且 callee_uid 非空时 evidence_file_line 必填；expected_* 缺省给空列表；protected 缺省 False。
    - `validate_gold_dataset(manifest: dict, cases: list[dict]) -> GoldDataset`：manifest 必填 gold_version/annotated_at_sha/splits，且 splits 含 dev/locked_test/holdout 三键；逐条 validate_gold_case。

    先写 RED 测试 `server/tests/codegraph/test_graph_bench_gold_schema.py`（用内存 dict 构造合法/非法样本，覆盖 <behavior> 全部用例；不读 fixture 文件——fixture 文件的权威校验在 Plan 04），跑失败再实现转 GREEN。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/codegraph/test_graph_bench_gold_schema.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd server && uv run pytest tests/codegraph/test_graph_bench_gold_schema.py -x -q` 退出码 0
    - `cd server && uv run pytest tests/codegraph/test_graph_bench_gold_schema.py --collect-only -q | grep -cE '::'` ≥ 7
    - `cd server && grep -cE 'LANGUAGES|FRAMEWORKS|ENTRY_TYPES|CALL_SHAPES' codegraph/services/graph_bench_eval.py` ≥ 4（四个闭集常量已定义）
    - `cd server && grep -cE 'evidence_file_line' codegraph/services/graph_bench_eval.py` ≥ 1（防反导锚点必填校验存在）
    - `cd server && uv run ruff check codegraph/services/graph_bench_eval.py && uv run mypy codegraph/services/graph_bench_eval.py` 退出码 0
  </acceptance_criteria>
  <done>gold manifest/case 必填维度与闭集校验生效；edge gold 的 evidence_file_line 必填；内存 dict 即可测；测试、lint、mypy 全绿</done>
</task>

</tasks>

## Artifacts this phase produces (Plan 01)

- `server/codegraph/services/graph_bench_eval.py` — 纯函数评测模块（零 I/O）
  - `RunIdentity`（dataclass）/ `build_run_identity()` / `validate_watermark()`（BENCH-01）
  - `GoldCase` / `GoldDataset`（dataclass）/ `validate_gold_case()` / `validate_gold_dataset()`（BENCH-02）
  - 闭集常量 `LANGUAGES` / `FRAMEWORKS` / `ENTRY_TYPES` / `CALL_SHAPES`
  - 字段：`RunIdentity.index_key_source`（默认 `"last_indexed_commit_sha"`）
- `server/tests/codegraph/test_graph_bench_watermark.py` — BENCH-01 单测
- `server/tests/codegraph/test_graph_bench_gold_schema.py` — BENCH-02 单测

（指标 / 分桶 / 报告 / command / fixtures 由 Plan 02/03/04 产出；本 plan 不产 CLI 标志。）

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gold fixtures → validator | gold JSON 来自仓内文件（可被 PR 修改），schema 校验是信任入口，必须 fail-closed 拒绝畸形/越界数据 |
| 调用方注入的水位 sha → validate_watermark | sha 由 command 从 ORM 解析注入，纯函数只比对；校验失败必须 INVALID 而非放行 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-133-01 | Tampering | validate_gold_dataset / gold schema | high | mitigate | 闭集枚举强制 + evidence_file_line 必填 + 缺必填维度即 raise（防 gold 被被测图污染/反导）；fixture 版本化入 git 可审查 |
| T-133-02 | Tampering | validate_watermark | high | mitigate | 三方 sha 任一为空或不全相等即 INVALID（fail-closed），不出部分结论；可选第四参 process built_at_sha 防投影漂移 |
| T-133-03 | Information Disclosure | graph_bench_eval.py | low | mitigate | 纯函数零 I/O、不触碰凭证/query 正文；只处理 sha/计数/闭集枚举 |
| T-133-SC | Tampering | pip/uv installs | high | mitigate | 本 phase 零新增生产依赖（研究 Package Legitimacy Audit 确认无新包），无安装面，supply-chain 门不触发 |
</threat_model>

<verification>
- `cd server && uv run pytest tests/codegraph/test_graph_bench_watermark.py tests/codegraph/test_graph_bench_gold_schema.py -q` 全绿
- `cd server && uv run ruff check codegraph/services/graph_bench_eval.py && uv run mypy codegraph/services/graph_bench_eval.py` 通过
- 模块保持零 I/O：`grep -cE 'qdrant|\.objects\.|import django' codegraph/services/graph_bench_eval.py` == 0
</verification>

<success_criteria>
- validate_watermark 三方/第四参四条路径行为正确，空值/不一致一律 INVALID（BENCH-01）
- build_run_identity 产含 index_key_source 的五元组 dict（BENCH-01）
- gold schema 校验强制四个分桶维度必填 + 闭集 + evidence_file_line 防反导锚点（BENCH-02）
- 全部逻辑零 I/O，可在默认 `--disable-socket` 套件运行；lint/mypy 通过
</success_criteria>

<output>
Create `.planning/phases/133-commit-v0-22-baseline/133-01-SUMMARY.md` when done
</output>
