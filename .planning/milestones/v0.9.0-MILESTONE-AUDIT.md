---
milestone: v0.9.0
milestone_name: SDD / OpenSpec 支持（重型）
status: passed
audited_at: "2026-06-17"
requirements_total: 11
requirements_delivered: 11
integration_ok: true
phases: "48-52"
---

# v0.9.0 里程碑审计 — SDD / OpenSpec 支持（重型）

**结论：PASSED** — 11/11 需求交付、integration_ok=true、跨 phase 无契约漂移、INV-6/INV-2 成立；仅余真实容器/索引 E2E 与浏览器视觉验收（human_needed，本地无法闭环，记 deferred，与既有里程碑边界一致）。

## 需求覆盖（11/11）

| REQ-ID | 描述 | Phase | 状态 |
|--------|------|-------|------|
| SDD-01 | 索引后检测 `openspec/` → `facets["methodology"]="SDD"`（best-effort 不阻断索引） | 48 | ✅ |
| SDD-02 | 仓库列表/详情可见 SDD 方法论标签 | 48 | ✅ |
| SPEC-01 | SDD 仓融合产 openspec spec draft，落 `Document(sdd_spec)` 经 DocumentService 单一入口 | 49 | ✅ |
| SPEC-02 | spec 关联来源 WorkItem + PlanVersion，可追溯 | 49 | ✅ |
| SPECST-01 | spec 状态机 draft→in_review→approved→implemented→archived，单一入口、非法流转被拒 | 50 | ✅ |
| SPECST-02 | spec 评审不可篡改记录（reviewer/decision/comment/time）驱动状态 | 50 | ✅ |
| SPECST-03 | 前端 spec 列表/详情/状态/评审可见可操作 | 50 | ✅ |
| GATE-01 | SDD 仓编码前校验 spec 已 approved，未批准拦截（fail-closed）不静默放行 | 51 | ✅ |
| GATE-02 | SDD 仓编码容器注入 openspec 指引 + 原生加载 `.claude/skills` | 51 | ✅ |
| LINK-01 | spec 挂 WorkItem + 关联实现 PR（编码产出回填） | 52 | ✅ |
| LINK-02 | 交付验收视图沿 spec→WorkItem→PR 链路追溯 | 52 | ✅ |

## 跨 phase 集成（integration_ok=true）

见 `.planning/v0.9.0-INTEGRATION-CHECK.md`。5 条端到端链路全 WIRED，无契约漂移：
1. 检测→产 spec：`facets["methodology"]="SDD"` 同键同值（大写）贯通 Phase 48→49。
2. 产 spec→状态机：`SddSpec`/`SddSpecStatus` 单点定义跨 49→50 复用。
3. 状态机→gate：`(plan_version_id,repository_id,status=APPROVED)` 查询与 Phase 49 `unique_together` 建表键一致（50→51）。
4. gate→关联：`link_implementation_pr` 同锚回填 + `approved→implemented` 复用 `_LEGAL_TRANSITIONS` 常量（51→52）。
5. follow_openspec 流：facets → create_tasks_for_plan → dispatch env → TaskConfig → system_prompt 一致（48→51）。
6. `spec.drafted` 已纳入 `ALL_EVENTS`，Phase 49 遗留事件集测试债已结清。

## 不变式

- **INV-6（单一写入入口）**：`SddSpec`/`SddSpecReview` 写入只经 `SddSpecService`；`Document(sdd_spec)` 只经 `DocumentService.create_internal_spec`；grep 守护断言通过。
- **INV-2（方案可追溯）**：spec `work_item`/`plan_version` 可空对齐 chat 自然语言需求。
- **fail-closed / fail-soft 边界**：gate 未批准/异常一律拦截不放行（fail-closed）；spec 生成 / PR 回填 / 检测打标全 best-effort fail-soft，绝不阻断索引/融合/PR 主流程。
- **零回归**：非 SDD 仓不经 gate/注入/产 spec，与 v0.8 编码逐字一致（守护测试断言）。

## 复用 v0.7/v0.8 预留扩展点（核查结论）

- `Document.SDD_SPEC` 枚举（v0.6 字段位）→ Phase 49 首次落库。
- `RepoCodingTask.follow_openspec`（v0.8 字段位）→ Phase 51 首次消费（建任务置位 + gate + 注入）。
- `Repository.facets` JSON / task `setting_sources=["project"]` → Phase 48/51 复用。
- 新增建模（非复用）：`SddSpec` 脊柱（Phase 49）+ `SddSpecReview`（Phase 50）+ spec 状态机命名 `in_review`/`implemented`（刻意区别于 `TechnicalPlan.status`）。

## 代码审查累计（5 phases）

| Phase | Review status | BLOCKER | 处置 |
|-------|---------------|---------|------|
| 48 | issues_found | 0 | IN-01 修；WR-01（facets 并发写）记既有范式 tech-debt |
| 49 | issues_found | 0 | WR-01（孤儿 Document）+ IN-01（代码围栏剥离）已修 |
| 50 | issues_found | 0 | WR-01（非字符串 comment 500→400）已修 |
| 51 | issues_found | 0 | WR-01（gate-block 写入隔离）已修 |
| 52 | issues_found | 0 | 3 INFO（死 i18n 键/时间戳格式/scheme 白名单）记 polish |

## Deferred（human_needed，真实环境/浏览器验收）

- 真实含 `openspec/` 仓索引打标端到端（Phase 48）。
- 真实容器 E2E：gate 拦截真实编码 / openspec skill 真实加载 + 真模型遵循 / spec.drafted 真模型产 openspec 格式 spec / PR 回填（Phase 49/51/52）。
- 浏览器视觉验收：spec 列表/详情/状态流转/评审 + 交付验收追溯面板（Phase 50/52）。
- 已知 polish（非阻断）：facets 并发写原子化（全代码库既有范式）、spec 详情 INFO 项（死键/时间戳/scheme 白名单）。

## 测试基线

- 跨 phase 回归：delivery + 编排 + 编码 + spec 套件 **308 passed / 1 skipped**；`makemigrations --check` 干净（migrations 0018/0019/0020）。
- 集成：55 + 4（事件集）+ 11（task）passed。
- 前端：spec 治理 UI + 交付验收面板 vitest 全绿；vue-tsc + eslint 通过。
