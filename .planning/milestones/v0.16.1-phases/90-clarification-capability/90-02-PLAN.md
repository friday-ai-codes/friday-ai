---
phase: 90-clarification-capability
plan: 02
type: execute
wave: 2
depends_on: ["90-01"]
files_modified:
  - server/delivery/services/clarification_service.py
  - server/tests/delivery/test_clarification_service.py
autonomous: true
requirements: [CLARIFY-01]
must_haves:
  truths:
    - "ClarificationService.create_round 建轮次容器 + 多 ClarificationQuestion 子题（INV-6 单一写入）"
    - "answer_round 按题写 selected/freeform/answered_at 并一次性算 recommendation_adopted（single==rec[0] / multi set 全等 / 无推荐→None）"
    - "ahas_pending(session_id) 统一 pending 谓词：轮内有未答子题 OR 旧单题行（无子题且容器 answered_at IS NULL）"
    - "采纳率可经 SQL 聚合（recommendation_adopted__isnull=False 的 adopted/total）"
    - "INV-6 grep 守护扩展覆盖 ClarificationQuestion.objects.create/.save"
  artifacts:
    - path: "server/delivery/services/clarification_service.py"
      provides: "create_round / answer_round / ahas_pending（澄清/问题/答案唯一写入入口）"
      contains: "async def create_round"
    - path: "server/tests/delivery/test_clarification_service.py"
      provides: "采纳信号/采纳率聚合/向后兼容/按题幂等/INV-6 子模型守护用例"
      contains: "recommendation_adopted"
  key_links:
    - from: "server/delivery/services/clarification_service.py"
      to: "delivery.models.ClarificationQuestion"
      via: "bulk_create 子题（sync_to_async 同步块）"
      pattern: "ClarificationQuestion"
    - from: "server/tests/delivery/test_clarification_service.py"
      to: "ClarificationQuestion.objects.create"
      via: "INV-6 grep 守护扩展"
      pattern: "ClarificationQuestion.objects"
---

<objective>
把 `ClarificationService` 扩展为结构化澄清的**唯一写入入口**（CLARIFY-01 service 半，INV-6）：新增 `create_round`（建轮次容器 + bulk_create 多子题）、`answer_round`（按题作答 + **作答时一次性定格 `recommendation_adopted`**）、`ahas_pending`（统一 pending 谓词，兼容旧单题行）。配套 Wave 0 测试：采纳信号 single/multi/无推荐、采纳率聚合、向后兼容旧行、按题幂等、INV-6 子模型守护。

Purpose: 采纳率是 CONTEXT 强诉求——必须作答时定格、可 SQL 聚合（不靠日志事后拼）。`ahas_pending` 收口三处 pending 判定（90-03 消费），避免判定逻辑漂移。
Output: 扩展后的 service + 扩充的 `test_clarification_service.py`。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/90-clarification-capability/90-RESEARCH.md
@.planning/phases/90-clarification-capability/90-PATTERNS.md
@.planning/phases/90-clarification-capability/90-01-SUMMARY.md
</context>

<artifacts_produced>
本 plan 新增的 `ClarificationService` 方法（下游 90-03 adapter / 90-04 helper / Phase 91 回流消费）：
- `async create_round(session, questions, *, origin_repo=None, round_no=None, plan_version_id=None) -> Clarification`：建容器 + `ClarificationQuestion.bulk_create`（容器 `question=""` 占位保旧 NOT NULL 列）。
- `async answer_round(round_or_id, answers) -> Clarification`（`answers`=[{question_id, selected, freeform_text}]）+ 内部 `@sync_to_async _answer_question_sync`：按题幂等条件更新 + 定格 `recommendation_adopted`。
- `async ahas_pending(session_id) -> bool`：统一 pending 谓词（轮内未答子题 OR 旧单题 pending 行）。
- 采纳率聚合不另写方法，由 SQL `ClarificationQuestion.objects.filter(recommendation_adopted__isnull=False).aaggregate(...)` 直接消费（测试演示形态）。
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 扩展 ClarificationService（create_round / answer_round / recommendation_adopted / ahas_pending）</name>
  <files>server/delivery/services/clarification_service.py</files>
  <read_first>
    - server/delivery/services/clarification_service.py（既有 create/answer/emit 范式，63-70 sync 写入块、89-105 幂等条件更新、107-133 best-effort emit）
    - server/delivery/models/clarification.py（90-01 扩展后的容器 + ClarificationQuestion 字段）
    - server/services/plan_orchestration/clarification_questions.py:62-98（normalize 形态：question/type/options/recommended）
  </read_first>
  <behavior>
    - create_round(session, [{question,type,options,recommended}, ...]) → 1 容器 + N 子题；容器 question="" 占位、子题按 order 递增；可携 origin_repo/round_no/plan_version_id；返回容器。
    - answer_round 单选命中：selected==recommended[0]（rec 存 [str] 或 str）→ recommendation_adopted=True；不命中→False。
    - answer_round 多选命中：set(selected)==set(recommended) → True；否则 False。
    - answer_round 无 recommended 或纯 freeform（无 selected）→ recommendation_adopted=None（不计入采纳率分母）。
    - answer_round 重复作答同一题（answered_at 已非空）→ 幂等 no-op，不二次覆盖首答。
    - ahas_pending(session_id)：轮内存在 answered_at IS NULL 的子题 → True；旧单题行（容器有 answered_at IS NULL 且无任何子题）→ True；全部已答 → False。
  </behavior>
  <action>
    复用既有 `@sync_to_async _create_sync` 范式新增 `create_round`：`Clarification.objects.create(session=session, question="", origin_repo=origin_repo, round_no=round_no, plan_version_id=plan_version_id)`（`question=""` 占位保旧 NOT NULL 列，真身在子题）+ `ClarificationQuestion.objects.bulk_create([...])` 按 enumerate order 写入 `question/qtype(取 q.get("type","single"))/options/recommended/origin_repo`。

    新增 `answer_round` + `@sync_to_async _answer_question_sync(question_id, selected, freeform_text) -> bool`：先 `ClarificationQuestion.objects.filter(id=question_id, answered_at__isnull=True).first()`，None → 返回 False（幂等 no-op）。算 `adopted`：取 `rec = q.recommended or []`；`if rec`：`qtype=="multi"` → `adopted = set(selected or []) == set(rec)`，否则 `want = rec[0] if isinstance(rec, list) else rec; adopted = (selected == want)`；无 rec 或 selected 空（纯 freeform）→ `adopted=None`。再 `filter(id=question_id, answered_at__isnull=True).update(selected=..., freeform_text=freeform_text or "", answered_at=timezone.now(), recommendation_adopted=adopted)`。`answer_round` 遍历 answers 调子方法。命中语义写进 docstring（multi 全等，CONTEXT 未指定子集，全等最无歧义）。

    新增 `async ahas_pending(self, session_id) -> bool` + `@sync_to_async` 同步实现：`(ClarificationQuestion.objects.filter(clarification__session_id=session_id, answered_at__isnull=True).exists())` OR `(Clarification.objects.filter(session_id=session_id, answered_at__isnull=True).exclude(questions__isnull=False).exists())`（旧单题 pending：容器未答且无任何子题——用 `~Q(questions__isnull=False)` 或 `.filter(questions__isnull=True)` 表达「无子题」，去重用 distinct）。注释引 Pitfall 2：旧单题行无子题须仍判 pending，否则历史挂起被误放行。

    全程 async 禁裸 lazy-FK：用 `session_id`/`*_id` 标量、`.values_list`、写入包 `sync_to_async` 同步块（既有范式）。所有写入只经本 service（INV-6）；docstring 内若出现字面子模型构造用全角括号避 grep 误判（STATE.md:275 先例）。`recommendation_adopted` 只 server 端作答时计算，**绝不**接受调用方/客户端传入（T-90-02-02）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/delivery/test_clarification_service.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `create_round` 建 1 容器 + N 子题（order 递增、字段齐备），只经 service。
    - `answer_round` 按题写答 + 定格 `recommendation_adopted`（single/multi/None 三态正确）。
    - 重复作答幂等 no-op。
    - `ahas_pending` 兼容旧单题行与新子题两形态。
    - async 无 `SynchronousOnlyOperation`（用例跑通即证）。
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Wave 0 测试——采纳信号/采纳率聚合/向后兼容/INV-6 子模型守护</name>
  <files>server/tests/delivery/test_clarification_service.py</files>
  <read_first>
    - server/tests/delivery/test_clarification_service.py（既有 5 测 + INV-6 grep 守护 146-163）
    - server/delivery/services/clarification_service.py（Task 1 新增方法）
  </read_first>
  <action>
    在 `test_clarification_service.py` 新增 async 用例（沿用文件顶部 `pytestmark = django_db(transaction=True)` 与 `_make_task` 范式）：
    1. `test_create_round_builds_container_and_questions`：`create_round` 后容器 `question==""`、`questions.count()==N`、子题 order/qtype/options/recommended 落库正确。
    2. `test_recommendation_adopted_single`（命中=True、不命中=False）、`test_recommendation_adopted_multi`（set 全等=True、部分=False）、`test_recommendation_adopted_none`（无 recommended 或纯 freeform → None）。命名含 `adopted` 便于 `-k adopted` 选跑。
    3. `test_adoption_rate_aggregation`：作答多题后 `ClarificationQuestion.objects.filter(recommendation_adopted__isnull=False).aaggregate(total=Count("id"), adopted=Count("id", filter=Q(recommendation_adopted=True)))` 得正确 adopted/total（命名含 `adoption_rate`）。
    4. `test_ahas_pending_legacy_single_row`：用 `create_clarification`（旧 API）建无子题旧行，`ahas_pending` 仍返回 True；作答后 False（命名含 `legacy`）。
    5. `test_answer_round_idempotent`：重复作答同题 no-op，首答不被覆盖。
    扩展 `test_inv6_clarification_single_write_entry`（或新增 `test_inv6_clarification_question_single_write_entry`）：grep 断言扩展覆盖 `ClarificationQuestion.objects.create` / `ClarificationQuestion.objects.bulk_create` / `ClarificationQuestion(...).save` 仅出现在 `clarification_service.py`（命名含 `inv6`）。docstring 内字面子模型构造用全角括号避自我误判。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/delivery/test_clarification_service.py -q && cd server && uv run pytest tests/delivery/test_clarification_service.py -k "adopted or adoption_rate or legacy or inv6" -q</automated>
  </verify>
  <acceptance_criteria>
    - 新增用例全绿，既有 5 测零回归。
    - 采纳信号 single/multi/None 三态、采纳率聚合、向后兼容旧行、按题幂等均有断言。
    - INV-6 grep 守护扩展覆盖 `ClarificationQuestion` 旁路写。
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 调用方 → ClarificationService（写入） | 澄清/问题/答案唯一写入入口；采纳信号必须 server 端计算 |
| async 上下文 → ORM（lazy-FK） | 裸访问 `clarification.session`/`question.clarification` 会崩 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90-02-01 | Tampering | INV-6 写入收口（子模型） | mitigate | 所有澄清/问题/答案写入只经 service；grep 守护扩展覆盖 `ClarificationQuestion.objects.create/.save/bulk_create`（ASVS V5） |
| T-90-02-02 | Tampering | `recommendation_adopted` 采纳率维度 | mitigate | server 端作答时算（不接受调用方传入 adopted），与答案写入同事务定格 |
| T-90-02-03 | Denial of Service | async 裸 lazy-FK | mitigate | 标量 `*_id` + `sync_to_async` 同步块 + `.values_list`（既有范式，规避 SynchronousOnlyOperation） |
| T-90-02-04 | Tampering | 旧单题行向后兼容 pending | mitigate | `ahas_pending` 收口「无子题且容器未答」也判 pending（Pitfall 2），防历史挂起误放行 |
| T-90-02-SC | Tampering | 依赖安装 | accept | 无 `pip/npm install`，无供应链面 |
</threat_model>

<verification>
- `cd server && uv run pytest tests/delivery/test_clarification_service.py -q` 全绿（既有 5 + 新增）。
- INV-6 grep 守护覆盖子模型，全仓无旁路写。
- 采纳率可经 `aaggregate` SQL 聚合（用例证）。
</verification>

<success_criteria>
- `create_round`/`answer_round`/`ahas_pending` 落地，INV-6 单一写入。
- `recommendation_adopted` 作答时定格、可 SQL 聚合采纳率。
- 向后兼容旧单题行（读 + pending 判定）。
</success_criteria>

<output>
Create `.planning/phases/90-clarification-capability/90-02-SUMMARY.md` when done
</output>
