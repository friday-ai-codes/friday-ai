---
phase: 94-entrypoint-unify
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/services/plan_orchestration/render.py
  - server/services/plan_orchestration/__init__.py
  - server/workflows/nodes/ai/plan_research.py
  - server/workflows/templates/technical_plan_generation.json
  - server/tests/workflows/test_template_loader.py
  - server/tests/workflows/test_plan_research_node.py
autonomous: true
requirements: [UNIFY-01, UNIFY-06]
must_haves:
  truths:
    - "ai_plan_research 的 done 出口产出干净结构化 plan_markdown（不 dump LLM 原文）"
    - "technical_plan_generation 模板的方案节点是 ai_plan_research（不再是 ai_plan_generation）"
    - "切换后的 technical_plan_generation 模板经 WorkflowGraphValidator 校验零 error（plan_markdown field_not_found 消除）"
    - "既有已实例化 ai_plan_generation 工作流不受模板改动影响（模板只改定义）"
  artifacts:
    - path: "server/services/plan_orchestration/render.py"
      provides: "render_merged_plan_markdown 共享渲染 helper（MergedPlan §7 → lark_md 卡片正文）"
      contains: "def render_merged_plan_markdown"
    - path: "server/workflows/nodes/ai/plan_research.py"
      provides: "default 输出端口 schema 声明 plan_markdown + _map_terminal DONE 分支填充渲染结果"
      contains: "plan_markdown"
    - path: "server/workflows/templates/technical_plan_generation.json"
      provides: "generate_plan 节点切到 ai_plan_research + requirement_text config"
      contains: "ai_plan_research"
  key_links:
    - from: "server/workflows/templates/technical_plan_generation.json"
      to: "ai_plan_research.outputs.default.schema.plan_markdown"
      via: "{{nodes.generate_plan.plan_markdown}} 字段引用"
      pattern: "plan_markdown"
    - from: "server/workflows/nodes/ai/plan_research.py"
      to: "server/services/plan_orchestration/render.py"
      via: "_map_terminal 调 render_merged_plan_markdown"
      pattern: "render_merged_plan_markdown"
---

<objective>
UNIFY-06 + UNIFY-01：给统一编排入口节点 `ai_plan_research` 的 `done` 出口补「干净结构化
markdown」渲染（`plan_markdown` 字段，复用本轮渲染修复范式：`•` 项目符号 + markdown 组件，
不 dump LLM 原文），并在 `default` 输出端口 schema 显式声明该字段；然后把内置模板
`technical_plan_generation.json` 的方案节点从废弃的 `ai_plan_generation` 切到
`ai_plan_research`（参照 `code_generation.json` 已切样板），同步 `test_template_loader` 断言。

Purpose: 三入口归一的工作流侧落地——既消除「模板引用 `{{nodes.generate_plan.plan_markdown}}`
而 ai_plan_research 不声明该字段」的 `field_not_found` 既有失败，又让 done 出口用结构化
markdown 推群（UNIFY-06）。两者强耦合（模板字段引用依赖节点 schema 声明），故同 plan 落地。
Output: 共享渲染 helper + ai_plan_research 输出字段 + 切换后的模板 + 同步测试。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/94-entrypoint-unify/94-CONTEXT.md
@.planning/phases/94-entrypoint-unify/94-RESEARCH.md
@server/workflows/nodes/ai/plan_research.py
@server/workflows/nodes/ai/plan_generation.py
@server/workflows/templates/technical_plan_generation.json
@server/workflows/templates/code_generation.json
@server/services/plan_orchestration/merged_plan.py
@server/services/plan_orchestration/__init__.py
@server/tests/workflows/test_template_loader.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 共享 MergedPlan markdown 渲染 helper（UNIFY-06 基础）</name>
  <files>server/services/plan_orchestration/render.py, server/services/plan_orchestration/__init__.py, server/tests/workflows/test_plan_research_node.py</files>
  <read_first>
    - server/workflows/nodes/ai/plan_generation.py（`_render_plan_markdown` 范式，第 464-514 行：title/summary/execution_plan 逐任务 name+repository_name+coding_instruction 截断 + risks/assumptions 用 `•` 字面项目符号；lark_md 不支持列表语法）
    - server/services/plan_orchestration/merged_plan.py（§7 MergedPlan 字段：title/summary/execution_plan[]/api_contracts/compat_risks/release_order/rollback_plan）
    - server/services/plan_orchestration/__init__.py（barrel re-export 形态，新增导出对齐既有 `__all__`）
  </read_first>
  <behavior>
    - Test 1: render_merged_plan_markdown 含 title → 输出含 `**{title}**`；含 summary → 含 summary 文本。
    - Test 2: execution_plan 非空 → 输出含「执行计划（共 N 项）」+ 逐任务 name + repository_name；coding_instruction 超 300 字符截断带省略号。
    - Test 3: compat_risks 非空 → 用 `•` 字面项目符号（不用 Markdown `- ` 列表，跨客户端稳定）。
    - Test 4: 半可信输入防御——顶层非 dict / 空 dict → 返回空串，绝不抛异常。
    - Test 5: 渲染结果**不含** LLM 原始文本字段（仅消费 MergedPlan 结构化字段，UNIFY-06「不 dump 原文」）。
  </behavior>
  <action>
    新建 `server/services/plan_orchestration/render.py`，定义纯函数
    `render_merged_plan_markdown(plan: dict[str, Any]) -> str`：移植 `plan_generation._render_plan_markdown`
    的渲染范式但适配 §7 MergedPlan schema——`title`（粗体）/`summary`/`execution_plan[]`（逐项
    name + repository_name + description + coding_instruction 截断 300）/`compat_risks`（用 `•` 字面
    项目符号，**禁用** Markdown `- ` 列表，对齐 lark_md 限制）。顶层非 dict / 空 → 返回 `""`（防御性，
    对齐 merged_plan.validate fail-safe 范式）。**只读 MergedPlan 结构化字段，绝不内联任何 LLM 原文/
    raw_* 字段**（UNIFY-06 / 脱敏纵深，Info Disclosure T-94-01-INFO）。在
    `services/plan_orchestration/__init__.py` 的 barrel 加 import + `__all__` 导出 `render_merged_plan_markdown`
    （供 UNIFY-03 MCP delegate 与本节点共用，落「不造两套」）。docstring 标注「Phase 94 UNIFY-06，
    MCP delegate 与 ai_plan_research 共享渲染」。本 helper 纯函数无 IO/ORM/LLM，故无观测埋点需求。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/workflows/test_plan_research_node.py -k "render_merged_plan or plan_markdown" -x</automated>
  </verify>
  <acceptance_criteria>
    - render_merged_plan_markdown 渲染 title/summary/execution_plan/compat_risks 正确，risks 用 `•`；
      非 dict 输入返回空串不抛；渲染不含 raw/原文字段。
    - `from services.plan_orchestration import render_merged_plan_markdown` 可导入（barrel 导出生效）。
    - ruff format/check + mypy 干净。
  </acceptance_criteria>
  <done>render_merged_plan_markdown 共享 helper 就位并经 barrel 导出，单测覆盖 5 行为全绿。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ai_plan_research done 出口产 plan_markdown + schema 声明（UNIFY-06）</name>
  <files>server/workflows/nodes/ai/plan_research.py, server/tests/workflows/test_plan_research_node.py</files>
  <read_first>
    - server/workflows/nodes/ai/plan_research.py（`outputs` 第 122-162 行 default 端口 schema.properties；`_map_terminal` 第 501-540 行 DONE 分支填充 output dict）
    - server/services/plan_orchestration/render.py（Task 1 新建的 render_merged_plan_markdown）
  </read_first>
  <behavior>
    - Test 1: DONE session 且 PlanVersion.content 为合法 MergedPlan → NodeResult.output 含
      `plan_markdown` 非空字符串（render_merged_plan_markdown(content) 结果），且含 plan_version_id/session_id/status/plan（既有字段零回归）。
    - Test 2: DONE 但 current_plan_version 为空 / content 非 dict → `plan_markdown` 为空串（不抛），plan 为 `{}`。
    - Test 3: get_schema()（经 NodeRegistry）的 default 输出端口 schema.properties 含 `plan_markdown`（type string），既有 plan_version_id/session_id/status/plan 键保留。
    - Test 4: failed 分支零回归——next_handle="error"，无 plan_markdown 依赖。
  </behavior>
  <action>
    在 `AIPlanResearchNode.outputs` 的 `default` 端口 `schema.properties` 追加
    `"plan_markdown": {"type": "string"}`（与既有 plan_version_id/session_id/status/plan 并列；满足
    模板 `{{nodes.generate_plan.plan_markdown}}` 字段引用，规避 Pitfall 1 field_not_found）。在
    `_map_terminal` 的 DONE 分支：加载 `plan_content` 后调
    `render_merged_plan_markdown(plan_content)`（模块顶 import 或函数内 lazy import，对齐既有
    import 风格）→ 写入 output `"plan_markdown": markdown`（content 缺失/非 dict 时 markdown 为空串，
    不改既有 plan 为 `{}` 的零回归行为）。**不改** `execute`/`_maybe_suspend`/clarify 端口路由
    （92-02 契约：clarify 仅声明端口，next_handle 仍只走 default/error）。脱敏：plan_markdown 来自
    canonical content（结构化，非 LLM 原文）；若需进一步纵深可在 render 内 best-effort，但本 plan
    不引入新脱敏点（content 已是 schema 化产物）。观测豁免：本节点已纳入既有工作流执行埋点，
    新增字段非新调用入口/LLM 调用，无需新埋点。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/workflows/test_plan_research_node.py -x</automated>
  </verify>
  <acceptance_criteria>
    - default 输出端口 schema 含 plan_markdown；DONE 分支 output 含渲染后的 plan_markdown。
    - 既有 test_plan_research_node.py 全绿（execute/_maybe_suspend/clarify 端口行为零回归）。
    - ruff format/check + mypy 干净；`makemigrations --check` 无变化（仅改节点 schema 声明非 DB 迁移）。
  </acceptance_criteria>
  <done>ai_plan_research done 出口产 plan_markdown 且 schema 声明，下游模板字段引用可校验通过。</done>
</task>

<task type="auto">
  <name>Task 3: technical_plan_generation 模板切到 ai_plan_research + 同步 loader 断言（UNIFY-01）</name>
  <files>server/workflows/templates/technical_plan_generation.json, server/tests/workflows/test_template_loader.py</files>
  <read_first>
    - server/workflows/templates/technical_plan_generation.json（现 generate_plan=ai_plan_generation + notify_clarify 经 need_clarification 出边）
    - server/workflows/templates/code_generation.json（已切样板：generate_plan=ai_plan_research + requirement_text config + 无 need_clarification 出边）
    - server/tests/workflows/test_template_loader.py（第 175-181 行 code_generation 的 `ai_plan_research in node_types` / `ai_plan_generation not in node_types` 断言范式；TestTemplateGraphValidation 零 error 守护）
    - server/workflows/nodes/ai/plan_research.py（outputs：default/clarify/error——**无 need_clarification 端口**；澄清经 _maybe_suspend 内部发飞书卡，不走出边）
  </read_first>
  <action>
    改 `technical_plan_generation.json`：① `generate_plan` 节点 `type` 由 `ai_plan_generation` 改
    `ai_plan_research`；config 由 `user_prompt` 改 `requirement_text`（保留同样的需求文本模板
    `根据以下需求生成技术方案。\n\n标题：{{nodes.fetch_work_item.name}}\n\n描述：{{nodes.fetch_work_item.description}}`），
    并加 `work_item_id: "{{nodes.trigger.work_item_id}}"`（锚 WorkItem，参照 code_generation 形态）+
    `include_repos: []`。② **删除 `notify_clarify` 节点及 `generate_plan --need_clarification--> notify_clarify`
    边**——ai_plan_research 无 `need_clarification` 出口端口（澄清经节点内部 `_send_clarify_card` 发飞书卡 +
    WorkflowEventSubscription，91-02 已落），保留该边会触发 `invalid_source_handle`；这与 code_generation
    样板一致（方案节点不再有 need_clarification 出边）。③ `notify_plan` 节点 content 保持
    `{{nodes.generate_plan.plan_markdown}}`（Task 2 已声明该字段）；`generate_plan --default--> notify_plan`
    边保留。**绝不写数据迁移改既有 DB 工作流实例**（UNIFY-01：模板只改定义，既有实例 node_type/config 已落
    DB 不受影响）。在 `test_template_loader.py` 为 technical_plan_generation 补 mirror code_generation 的断言：
    新增/扩展用例断言切换后该模板 node_types 含 `ai_plan_research` 且不含 `ai_plan_generation`（参照第
    179-181 行范式）。既有 TestTemplateGraphValidation `test_template_validates_with_zero_errors[technical_plan_generation]`
    与 `test_acreate_accepts_valid_templates` 修复后应转绿（消除 plan_markdown field_not_found 既有失败）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/workflows/test_template_loader.py -k "technical_plan or validates_with_zero_errors or accepts_valid" -x</automated>
  </verify>
  <acceptance_criteria>
    - technical_plan_generation 模板 generate_plan=ai_plan_research、requirement_text config、无 notify_clarify/need_clarification 边。
    - `test_template_validates_with_zero_errors[technical_plan_generation]` + `test_acreate_accepts_valid_templates` 全绿（field_not_found 既有失败消除）。
    - 新增断言：technical_plan_generation node_types 含 ai_plan_research、不含 ai_plan_generation。
    - 全套 `cd server && uv run pytest tests/workflows/test_template_loader.py` 绿。
  </acceptance_criteria>
  <done>technical_plan_generation 模板切到 ai_plan_research 编排路径，loader 校验零 error 且断言同步。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 编排 canonical content → 飞书群推送 | MergedPlan content 经 render → notify_feishu_im 卡片正文（推送到项目群） |
| 模板定义 → 新建工作流实例 | 模板是新建工作流的种子；既有 DB 实例不受影响 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94-01-INFO | Information Disclosure | render_merged_plan_markdown | mitigate | 仅渲染 MergedPlan 结构化字段，绝不内联 LLM 原始文本 / raw_* 字段（UNIFY-06「不 dump 原文」）；content 已是 schema 化产物 |
| T-94-01-COMPAT | Tampering（回归破坏） | technical_plan_generation 模板切换 | mitigate | 模板只改定义不写数据迁移；既有 ai_plan_generation DB 实例保留可运行（Plan 02 deprecated 不删注册）；切换后经同一 WorkflowGraphValidator 零 error 守护 |
| T-94-01-SC | Tampering | 依赖安装 | accept | 本 plan 无 npm/pip 安装（纯内部重构，RESEARCH Package Audit 不适用） |
</threat_model>

<verification>
- `cd server && uv run pytest tests/workflows/test_template_loader.py tests/workflows/test_plan_research_node.py -x` 全绿。
- `cd server && uv run ruff format --check . && uv run ruff check . && uv run mypy workflows/nodes/ai/plan_research.py services/plan_orchestration/render.py` 干净。
- `cd server && uv run python manage.py makemigrations --check` 无新迁移。
</verification>

<success_criteria>
- ai_plan_research done 出口产干净结构化 plan_markdown 且 default 端口 schema 声明该字段（UNIFY-06）。
- technical_plan_generation 模板切到 ai_plan_research、校验零 error、既有实例不破坏（UNIFY-01）。
- 既有 template_loader field_not_found 失败消除；test_plan_research_node 零回归。
</success_criteria>

<artifacts_this_phase_produces>
- 新建符号：`services/plan_orchestration/render.py::render_merged_plan_markdown`（共享渲染 helper，barrel 导出）。
- 改动符号：`AIPlanResearchNode.outputs.default.schema.properties.plan_markdown`（新字段）；`_map_terminal` DONE 分支填充 plan_markdown。
- 模板切换：`technical_plan_generation.json` generate_plan→ai_plan_research、删 notify_clarify。
- 测试：test_template_loader（technical_plan ai_plan_research 断言 + 零 error 修复）、test_plan_research_node（plan_markdown 渲染 + schema 断言）。
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/94-entrypoint-unify/94-01-SUMMARY.md` when done.
</output>
