---
phase: 93-capability-slot-editor
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/components/workflow/editor/composables/useWorkflowTransform.ts
  - web/src/stores/useWorkflowsStore.ts
  - web/src/components/workflow/editor/composables/useAutoLayout.ts
  - web/src/components/workflow/editor/composables/__tests__/useWorkflowTransform.parent.test.ts
  - web/src/stores/__tests__/useWorkflowsStore.attach.test.ts
autonomous: true
requirements: [SLOT-04]

must_haves:
  truths:
    - "澄清节点可作为方案节点的子节点持久化（经 metadata.parentNodeId，无后端 schema 变更）"
    - "toVueFlowNodes 把带 parentNodeId 的节点映射为 Vue Flow parentNode + extent:'parent'，且父节点在数组中先于子节点"
    - "数据契约固化：toVueFlowNodes 输出的附着子节点同时持有 top-level node.parentNode 与 node.data.metadata.parentNodeId（同源不丢），供 93-05 经 props.data.metadata.parentNodeId 判附着徽标"
    - "删除父方案节点时其附着澄清子节点级联一并删除（兑现生命周期绑定）"
    - "attach/detach 经 store 单一入口写入 metadata.parentNodeId，往返 save/reload 不丢"
  artifacts:
    - path: "web/src/stores/useWorkflowsStore.ts"
      provides: "attachChild/detachChild + removeNode 级联删除子节点"
      contains: "parentNodeId"
    - path: "web/src/components/workflow/editor/composables/useWorkflowTransform.ts"
      provides: "parentNode/extent 映射 + 父先于子排序"
      contains: "extent"
  key_links:
    - from: "useWorkflowTransform.ts"
      to: "Vue Flow node.parentNode"
      via: "metadata.parentNodeId → parentNode + extent:'parent'"
      pattern: "parentNode"
    - from: "useWorkflowsStore.removeNode"
      to: "子节点级联删除"
      via: "metadata.parentNodeId 反查"
      pattern: "parentNodeId"
---

<objective>
SLOT-04 的数据模型与生命周期绑定（无 UI 渲染）：用 `metadata.parentNodeId` 持久化"附着子节点"父子关系（零后端 schema 变更，metadata 是既有可持久 JSON 列），在 transform 映射为 Vue Flow `parentNode + extent:'parent'`，并在 store 兑现「删父级联删子」+ attach/detach 单一入口；useAutoLayout 把父子视为整体不打乱编组。

Purpose: 「删方案节点 → 澄清节点一并移除」是 CONTEXT 锁定的生命周期语义（决策 B / UI-SPEC 方案 A）；parent/child + extent:'parent' + 级联删除天然兑现。
Output: store attach/detach + 级联删除、transform parent 映射 + 父先子排序、useAutoLayout 父子整体处理。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/93-capability-slot-editor/93-CONTEXT.md
@.planning/phases/93-capability-slot-editor/93-UI-SPEC.md
@web/src/types/workflow/store.ts
</context>

<artifacts_this_phase_produces>
- store：`attachChild(childId, parentId, relativePosition)` / `detachChild(childId)`（写/清 `metadata.parentNodeId`，单一入口，入历史）+ `removeNode` 级联删除 `metadata.parentNodeId===被删节点` 的子节点（连同其边）。
- `useWorkflowTransform.toVueFlowNodes`：读 `metadata.parentNodeId` → 输出 `parentNode` + `extent: 'parent'`，并保证父节点排在子节点之前（Vue Flow 硬约束）；`fromVueFlowNodes` 往返保 parentNodeId 不丢。
- `useAutoLayout`：附着子节点不参与 dagre 自由布局（随父定位），父子视为整体。
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: store parentNodeId 持久化 + attach/detach + 级联删除</name>
  <read_first>
    - web/src/stores/useWorkflowsStore.ts（addNode/removeNode/saveToHistory；toStoreNodes/toBackendNodes 已透传 metadata；无 parent 字段）
    - web/src/types/workflow/store.ts（WorkflowNodeStore，无 parent 字段——故用 metadata.parentNodeId）
  </read_first>
  <files>web/src/stores/useWorkflowsStore.ts</files>
  <action>
    用 `metadata.parentNodeId`（既有 `metadata: Record<string,unknown>` 透传到后端 bulk-update 持久化，**无新后端字段/迁移**，作 Claude's Discretion 决策记 SUMMARY）。新增：
    - `attachChild(childId: string, parentId: string, relativePosition: { x: number, y: number })`：把子节点 `metadata.parentNodeId=parentId`、`position=relativePosition`（已是相对父的坐标，由调用方 93-06 换算），`saveToHistory()`。
    - `detachChild(childId: string, absolutePosition: { x: number, y: number })`：清 `metadata.parentNodeId`（delete 键）、`position=absolutePosition`（恢复绝对坐标），`saveToHistory()`。
    - 改 `removeNode(nodeId)`：删除目标后，**级联**删除所有 `metadata.parentNodeId===nodeId` 的子节点（连同其相关边），再 saveToHistory；删子节点本身（无子）时只删自身边（既有行为）。注意先收集子 id 集合再统一过滤，避免遍历中改数组。
    导出 attachChild/detachChild。`getChildNodes(parentId)` 辅助（filter metadata.parentNodeId===parentId）便于 UI 取附着数。
  </action>
  <verify>
    <automated>cd web && pnpm vitest run useWorkflowsStore.attach</automated>
  </verify>
  <acceptance_criteria>
    - attachChild 写入 metadata.parentNodeId；detachChild 清除并恢复位置。
    - removeNode(父) 同时移除其全部附着子节点 + 两者相关边；removeNode(普通节点) 行为零回归（只删自身 + 连边）。
    - metadata.parentNodeId 经 toBackendNodes → toStoreNodes 往返保留（metadata 透传，单测断言）。
  </acceptance_criteria>
  <done>store 父子持久化 + attach/detach + 级联删除就绪（无后端 schema 变更），单测覆盖 attach/detach/级联/往返/普通节点零回归。</done>
</task>

<task type="auto">
  <name>Task 2: transform parentNode/extent 映射 + 父先子排序 + autoLayout 整体处理</name>
  <read_first>
    - web/src/components/workflow/editor/composables/useWorkflowTransform.ts（toVueFlowNodes/fromVueFlowNodes，当前不含 parentNode）
    - web/src/components/workflow/editor/composables/useAutoLayout.ts（applyAutoLayout 经 dagre 重排全部节点）
    - Vue Flow 文档约束：parent 节点必须在 nodes 数组中先于 child 出现，子坐标为相对父
  </read_first>
  <files>web/src/components/workflow/editor/composables/useWorkflowTransform.ts, web/src/components/workflow/editor/composables/useAutoLayout.ts</files>
  <action>
    `toVueFlowNodes`：对每个 store node，若 `metadata.parentNodeId` 非空 → 输出节点附 `parentNode: <parentId>` + `extent: 'parent'`（Vue Flow 原生包含/级联拖拽/相对定位）。**数据契约命门（WARNING 1 跨 plan 固化）**：top-level `parentNode` 与 `data.metadata`（含 `parentNodeId`）必须**同源并存**——既有 `data: { ...metadata: storeNode.metadata }` 透传逐字保留，不得为了提 parentNode 到顶层而从 data.metadata 删除/改写 parentNodeId。下游 93-05 经 `props.data.metadata.parentNodeId` 判附着徽标，故 `node.data.metadata.parentNodeId` 是该判定的唯一权威来源，必须随节点透传。**排序命门**：返回前对结果排序使带 parent 的子节点排在其父之后（简单两趟：先无 parent 的，再有 parent 的；或 stable 排序 parent 优先）——否则 Vue Flow 报 "parent node not found"。
    `fromVueFlowNodes`：保持往返不丢 metadata.parentNodeId（metadata 已整体透传，确认不被覆盖）。
    `useAutoLayout.applyAutoLayout`：dagre 布局时**排除**附着子节点（`metadata.parentNodeId` 非空者不进 dagre 输入、不被重排坐标），父子作为整体随父移动——避免编组被拆散（UI-SPEC Layout 第 1 条「parent/child 视为整体」）。最小改动：在收集 dagre 输入节点处过滤掉子节点，写回坐标时跳过子节点。
  </action>
  <verify>
    <automated>cd web && pnpm vitest run useWorkflowTransform.parent</automated>
  </verify>
  <acceptance_criteria>
    - 带 metadata.parentNodeId 的 store 节点 → vf 节点含 parentNode + extent:'parent'。
    - **数据契约断言（WARNING 1）**：同一附着子节点输出同时满足 `node.parentNode===parentId` 且 `node.data.metadata.parentNodeId===parentId`（两者同源不丢，锁死 93-05 读取来源）。
    - 输出数组中父节点索引恒小于其子节点索引（排序断言）。
    - applyAutoLayout 不改动附着子节点坐标（单测：子节点 position 在布局前后不变 / 不进 dagre）。
    - 既有 toVueFlowNodes/edges、applyAutoLayout 对无父子图零回归。
  </acceptance_criteria>
  <done>transform parent/extent 映射 + 父先子排序 + autoLayout 编组整体处理就绪，无父子图零回归。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 前端 metadata → 后端持久化 | metadata 为既有可持久 JSON，无新权限面 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-03-DATA | Tampering | metadata.parentNodeId | accept | 父子关系仅是编辑器视觉/生命周期元数据，落 metadata（既有 JSON 列），后端 bulk-update 既有校验不变；无敏感数据 |
| T-93-03-SC | Tampering | 依赖安装 | mitigate | 无 npm 安装，无供应链面 |
</threat_model>

<verification>
- `pnpm -C web vitest run useWorkflowsStore.attach useWorkflowTransform.parent` 全绿。
- `pnpm -C web vue-tsc --noEmit` 通过；既有 store / transform / autoLayout 测试零回归。
</verification>

<success_criteria>
- 父子附着可持久化（无后端变更）、映射为 Vue Flow parent/child、删父级联删子、编组在自动布局中作整体；无父子图零回归。
</success_criteria>

<output>
Create `.planning/phases/93-capability-slot-editor/93-03-SUMMARY.md` when done
</output>
