---
phase: 93-capability-slot-editor
plan: 01
type: execute
wave: 2
depends_on: [93-00]
files_modified:
  - web/src/stores/useNodeTypesStore.ts
  - web/src/components/workflow/editor/composables/portShapes.ts
  - web/src/components/workflow/editor/composables/useConnectionValidator.ts
  - web/src/locales/zh-CN.json
  - web/src/components/workflow/editor/composables/__tests__/portShapes.test.ts
  - web/src/components/workflow/editor/composables/__tests__/useConnectionValidator.test.ts
autonomous: true
requirements: [SLOT-03]

must_haves:
  truths:
    - "前端从 /node-types/ 读到每个端口的 shape（NodePort.shape?:string，依赖 93-00 后端已声明 shape 字段、API 真实回传），无 shape 字段时为空字符串"
    - "拖拽两个 typed shape 不兼容（双端非空且不等）的端口时连接被判定非法"
    - "任一端 shape 为空（含 default/error 通用端口）时连接放行（零回归，与后端 _validate_port_shapes 同口径）"
    - "不兼容连接的提示文案用中文 shape 友好名（不暴露 clarification_request 等英文标识符）"
  artifacts:
    - path: "web/src/components/workflow/editor/composables/portShapes.ts"
      provides: "shape 兼容纯函数 + shape 索引 Map 构建 + shape 中文显示名解析"
      contains: "arePortShapesCompatible"
    - path: "web/src/stores/useNodeTypesStore.ts"
      provides: "NodePort.shape?:string 字段"
      contains: "shape?"
  key_links:
    - from: "useConnectionValidator.ts"
      to: "portShapes.ts"
      via: "契约兼容判定调用"
      pattern: "arePortShapesCompatible"
    - from: "portShapes.ts"
      to: "useNodeTypesStore"
      via: "按 node_type + handle 解析端口 shape"
      pattern: "getNodeType"
---

<objective>
为前端插槽磁吸打地基：让前端从 `/api/node-types/`（端口 `shape` 由 **93-00** 在 NodePortSerializer 声明后真实回传——Phase 92 已写入 get_schema 但 DRF 曾剥离，故本 plan 依赖 93-00）拿到端口契约，并在既有连接校验链中追加「形状契约兼容」判定（SLOT-03 的判定核心）。本 plan 只产纯逻辑 + 类型 + i18n，不碰任何 UI 组件。

Purpose: 前端权威即时判定连接合法性（不必等后端保存），后端 `WorkflowGraphValidator._validate_port_shapes` 仍是最终防线（兜底）。
Output: `NodePort.shape?` 类型字段、`portShapes.ts`（shape 兼容纯函数 + shape→中文名解析）、`useConnectionValidator` 第 4 条契约规则、`workflow.editor.{slot,shape,palette}` 全量 i18n 键。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/93-capability-slot-editor/93-CONTEXT.md
@.planning/phases/93-capability-slot-editor/93-UI-SPEC.md
@.planning/phases/93-capability-slot-editor/93-00-SUMMARY.md
@.planning/phases/92-capability-slot-backend/92-03-SUMMARY.md
</context>

<artifacts_this_phase_produces>
- `NodePort.shape?: string`（store 类型扩展，消费 93-00 经 `/node-types/` 真实回传的 shape）
- `portShapes.ts`：`arePortShapesCompatible(srcShape, tgtShape)` 纯函数（空通配 + 双非空相等放行）、`resolvePortShape(nodeType, handleId, group)`（从 store 取端口 shape）、`SHAPE_DISPLAY_KEY`（shape→i18n key 映射）
- `useConnectionValidator` 第 4 条「契约兼容」规则（前端权威判定，后端兜底）
- i18n 键空间 `workflow.editor.slot.*` / `workflow.editor.shape.*` / `workflow.editor.palette.empty`（默认中文）
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: NodePort.shape 字段 + portShapes 纯逻辑模块</name>
  <read_first>
    - web/src/stores/useNodeTypesStore.ts（NodePort 接口、getNodeType）
    - web/src/components/workflow/editor/composables/useConnectionValidator.ts（既有 Connection 用法）
    - .planning/phases/92-capability-slot-backend/92-03-SUMMARY.md（shape 取值集：clarification_request/clarification_answer/feishu_message/technical_plan/coding_assignment/feishu_document/approval_result）
    - .planning/phases/93-capability-slot-editor/93-UI-SPEC.md（"能力 shape 显示名" + 着色优先级）
  </read_first>
  <files>web/src/stores/useNodeTypesStore.ts, web/src/components/workflow/editor/composables/portShapes.ts</files>
  <action>
    在 `useNodeTypesStore.ts` 的 `NodePort` 接口末位追加可选 `shape?: string`（与 `schema?` 同级，默认 undefined；消费 93-00 经 `/node-types/` 真实回传的 shape 字段——93-00 已在 NodePortSerializer 声明 shape，API 不再剥离；不改 fetchNodeTypes 逻辑）。
    新建 `portShapes.ts`（纯逻辑，不引入 Vue 组件/响应式副作用）导出：
    (1) `arePortShapesCompatible(srcShape: string | undefined, tgtShape: string | undefined): boolean` —— 任一端为空/undefined（含未声明、default、error 通用端口 shape 恒空）→ `true`（空通配，零回归命门，与后端 `_validate_port_shapes` 同口径）；双端非空且相等 → `true`；双端非空且不等 → `false`。
    (2) `resolvePortShape(nodeType: string, handleId: string, group: 'input' | 'output'): string | undefined` —— 经 `useNodeTypesStore().getNodeType(nodeType)` 取 inputs/outputs，按 `name===handleId` 找端口返回其 `shape`（找不到返回 undefined）。
    (3) `SHAPE_DISPLAY_KEY: Record<string, string>` —— 7 个 typed shape → i18n key（如 `clarification_request: 'workflow.editor.shape.clarificationRequest'` …），用于校验提示展示中文友好名。
    (4) `shapeDisplayName(shape: string | undefined, t: (key: string) => string): string` —— 有映射用 `t(key)`，无映射回退 `shape ?? ''`（不暴露未知英文标识符时退化为空串）。
    注释用中文说明"空契约=通配"为零回归命门。高频拖拽路径：本模块为纯函数，不打日志、不建每帧新对象。
  </action>
  <verify>
    <automated>cd web && pnpm vitest run portShapes</automated>
  </verify>
  <acceptance_criteria>
    - `NodePort` 含可选 `shape?: string`，`pnpm -C web vue-tsc --noEmit` 通过。
    - `arePortShapesCompatible('','x')`/`('x','')`/`(undefined,'x')`/`('x','x')` 均 true；`('a','b')` false。
    - `resolvePortShape` 对 store 未就绪/未知端口返回 undefined 不抛。
  </acceptance_criteria>
  <done>portShapes.ts 三函数 + SHAPE_DISPLAY_KEY 落地，NodePort.shape? 就位，纯函数单测覆盖空通配/相等/不等/缺失四态全绿。</done>
</task>

<task type="auto">
  <name>Task 2: useConnectionValidator 第 4 条契约兼容规则 + i18n 全量键</name>
  <read_first>
    - web/src/components/workflow/editor/composables/useConnectionValidator.ts（getValidationError 现有三条：防自连 / 四元组重复 / BFS 防环；返回 string|null，onConnect 用其文案 toast）
    - web/src/components/workflow/editor/WorkflowCanvas.vue（onConnect 调 getValidationError + showError('连线失败', err)）
    - web/src/locales/zh-CN.json（顶层 namespace 结构；chat.clarification 范式；当前无 workflow 顶层键）
    - .planning/phases/93-capability-slot-editor/93-UI-SPEC.md（Copywriting Contract 表 + 能力 shape 显示名）
  </read_first>
  <files>web/src/components/workflow/editor/composables/useConnectionValidator.ts, web/src/locales/zh-CN.json</files>
  <action>
    i18n（zh-CN.json）：新增顶层 `workflow` namespace（若不存在），写入 `editor.slot.*` / `editor.shape.*` / `editor.palette.empty` 全量键，**覆盖 UI-SPEC Copywriting Contract 全表**（本 phase 一次性补齐，后续 W3 plan 只消费不再改本文件）：
      - `workflow.editor.slot.compatible`="可连接"
      - `workflow.editor.slot.incompatibleTitle`="连线失败"
      - `workflow.editor.slot.incompatibleBody`="形状不兼容：「{source}」无法接入「{target}」"
      - `workflow.editor.slot.attachedBadge`="附着"
      - `workflow.editor.slot.attachedHint`="澄清随方案节点存在，删除方案节点将一并移除"
      - `workflow.editor.slot.imGatedHint`="需先添加「创建群聊」节点以提供 chat_id"
      - `workflow.editor.slot.detachTitle`="解除附着"
      - `workflow.editor.slot.detachBody`="解除后澄清节点将成为独立节点，不再随方案节点联动。确认解除？"
      - `workflow.editor.slot.deleteWithChildBody`="删除「{name}」将一并移除其 {count} 个附着澄清节点。确认删除？"
      - `workflow.editor.palette.empty`="未找到匹配节点"
      - `workflow.editor.shape.*`：clarificationRequest="澄清请求"、clarificationAnswer="澄清答复"、feishuMessage="飞书消息"、feishuDocument="飞书文档"、technicalPlan="技术方案"、codingAssignment="编码指派"、approvalResult="审批结果"
    `useConnectionValidator.ts`：在 `getValidationError` 既有三条之后追加第 4 条「契约兼容」——解析 `connection.source`/`connection.target` 的 nodeType（经 useVueFlow `findNode` 或 store 取 `data.nodeType`）+ `sourceHandle`/`targetHandle`（`?? 'default'`）→ `resolvePortShape(srcType, srcHandle, 'output')` / `resolvePortShape(tgtType, tgtHandle, 'input')` → `arePortShapesCompatible` 为 false 时返回经全局 i18n（`i18n.global.t`，见既有 main.ts i18n 实例导出）渲染的 `slot.incompatibleBody`（具名插值 `{source}`/`{target}` 用 `shapeDisplayName` 中文名）。兼容/缺类型/缺 shape → 不拦截（返回 null 继续既有流程）。
    若全局 i18n 实例不便在 composable 直接 import，则改为：`getValidationError` 新增可选 `t` 注入参数（默认从 `useI18n()` 取或全局实例），WorkflowCanvas onConnect 调用处传入——保持 `validateConnection`（boolean）签名不变（isValidConnection 仍只判 boolean，文案仅 toast 路径用）。
    高频 isValidConnection 路径：shape 解析走 store getNodeType（已是 O(1) find），避免每次重建大对象。
  </action>
  <verify>
    <automated>cd web && pnpm vitest run useConnectionValidator</automated>
  </verify>
  <acceptance_criteria>
    - 双端 typed shape 不等 → getValidationError 返回含中文 shape 名的 incompatibleBody（用真实 zh-CN.json 作 createI18n messages 断言，沿用 Phase 24/91 i18n 守护范式）。
    - 任一端空 shape（default/error/未声明）→ 返回 null（零回归：既有 default→default 合法连线不被拦）。
    - 既有三条规则（自连/重复/环）行为逐字不变（保留原测/补回归断言）。
    - `pnpm -C web vue-tsc --noEmit` 通过；受改文件 `pnpm -C web eslint` 干净。
  </acceptance_criteria>
  <done>契约兼容第 4 条接入 getValidationError（前端权威 + 后端兜底），i18n 全量键落 zh-CN.json，i18n 守护 spec 锁「形状不兼容」中文不被改空，既有三规则零回归。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 前端校验 → 后端保存 | 前端契约判定仅为即时 UX，非权威 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-01-BYPASS | Tampering | 前端 isValidConnection | accept | 前端契约判定为体验优化；后端 `WorkflowGraphValidator._validate_port_shapes` 保存即校验仍是权威防线（92-01），前端绕过不影响落库合法性 |
| T-93-01-INFO | Information Disclosure | 校验提示文案 | mitigate | 提示只展示 shape 中文友好名（i18n），不回显节点 config/凭证；shape 名为非敏感能力语义 |
| T-93-01-SC | Tampering | 依赖安装 | mitigate | 本 plan 无 npm 安装（仅改既有源 + JSON），无供应链面 |
</threat_model>

<verification>
- `pnpm -C web vitest run portShapes useConnectionValidator` 全绿。
- `pnpm -C web vue-tsc --noEmit` 通过。
- 既有 `node-sync`、workflow 相关既有 vitest 零回归。
</verification>

<success_criteria>
- 前端可从 store 读端口 shape；不兼容 typed 连接被前端判非法且提示中文友好名；空契约通配零回归；i18n 全量键就位。
</success_criteria>

<output>
Create `.planning/phases/93-capability-slot-editor/93-01-SUMMARY.md` when done
</output>
