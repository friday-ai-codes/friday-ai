---
phase: 93-capability-slot-editor
plan: 05
type: execute
wave: 4
depends_on: [93-01, 93-02, 93-03, 93-04]
files_modified:
  - web/src/components/workflow/editor/composables/useImCapability.ts
  - web/src/components/workflow/editor/nodes/BaseWorkflowNode.vue
  - web/src/components/workflow/editor/composables/__tests__/useImCapability.test.ts
  - web/src/components/workflow/editor/nodes/__tests__/BaseWorkflowNode.slot.test.ts
autonomous: true
requirements: [SLOT-03, SLOT-04]

must_haves:
  truths:
    - "typed shape 端口渲染为圆角方形（input 凹槽描边 / output 凸点实心），通用 default/error 端口保持圆形（零回归）"
    - "typed shape 端口按 shape 色板着色，shape 为空时回退既有 handle-id 语义色"
    - "拖拽连线中，兼容目标 input handle 高亮放大、不兼容 handle 降透明（compatible-highlight/forbidden 态）"
    - "图中无 create_group_chat/create_work_item_chat 时，依赖 chat_id 的通知 handle/节点降级（opacity + 锁徽标 + tooltip 引导）"
    - "附着澄清子节点显示『附着』徽标 + hover tooltip 说明生命周期绑定"
  artifacts:
    - path: "web/src/components/workflow/editor/composables/useImCapability.ts"
      provides: "图级 IM 能力判定（存在建群节点 → 具备 chat_id 源）"
      contains: "create_group_chat"
    - path: "web/src/components/workflow/editor/nodes/BaseWorkflowNode.vue"
      provides: "shape 方形/圆形 handle + shape 着色 + 拖拽态类 + IM 门控 + 附着徽标"
      contains: "isCompatibleTarget"
  key_links:
    - from: "BaseWorkflowNode.vue"
      to: "useConnectionDragState"
      via: "拖拽态驱动 handle compatible/forbidden 类"
      pattern: "isCompatibleTarget"
    - from: "BaseWorkflowNode.vue"
      to: "useImCapability"
      via: "无 chat_id 源 → IM handle 降级"
      pattern: "useImCapability"
    - from: "BaseWorkflowNode.vue"
      to: "portShapes shape 色板"
      via: "typed shape 端口着色"
      pattern: "shape"
---

<objective>
节点卡层面的全部插槽视觉（SLOT-03 端口视觉 + 拖拽态 + SLOT-04 附着徽标 + 能力分类 + IM 门控）一次性落在 `BaseWorkflowNode.vue`（该文件单一 owner，避免跨 plan 冲突），并新建图级 IM 能力判定 composable。消费 Wave 1/2 产物（portShapes / dragState / store metadata / nodeVisuals）。

Purpose: 让用户一眼区分「契约插槽（方形）vs 通用流（圆形）」，拖拽时聚焦可落点，缺 IM 能力时给引导，附着子节点显式生命周期绑定。
Output: useImCapability + BaseWorkflowNode 端口形状/着色/拖拽态/IM 门控/附着徽标。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/93-capability-slot-editor/93-CONTEXT.md
@.planning/phases/93-capability-slot-editor/93-UI-SPEC.md
@web/src/components/workflow/editor/nodes/BaseWorkflowNode.vue
@web/src/components/workflow/editor/composables/portShapes.ts
@web/src/components/workflow/editor/composables/useConnectionDragState.ts
</context>

<artifacts_this_phase_produces>
- `useImCapability.ts`：`useImCapability()` → `hasImCapability` computed（store.nodes 含 `create_group_chat` 或 `create_work_item_chat` → true）+ `IM_DEPENDENT_TYPES` 集（依赖 chat_id 的节点，如 notify_feishu/notify_feishu_im）+ `isImGated(nodeType)`（节点级门控判定）。
- BaseWorkflowNode：typed shape handle 方形（input 描边凹槽 / output 实心凸点）+ shape 色板着色（shape 优先、空回退 PORT_DOT_COLOR）；clarify(凹槽)/resume(凸点) AI 端口由后端 shape 自然渲染（无需特判）；拖拽态 handle 类（compatible-highlight 放大+绿光环 / forbidden 降透明）；IM 门控（opacity 0.4 + 锁徽标 + tooltip imGatedHint）；附着子节点『附着』徽标 + tooltip attachedHint。
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: useImCapability 图级 IM 能力判定</name>
  <read_first>
    - web/src/stores/useWorkflowsStore.ts（nodes ref 结构，nodeType 字段）
    - .planning/phases/93-capability-slot-editor/93-CONTEXT.md（决策 D：含 create_group_chat/create_work_item_chat → 具备 IM 能力；缺失则 IM 相关降级）
    - .planning/phases/93-capability-slot-editor/93-UI-SPEC.md（"IM 能力门控" 段：默认禁用 = opacity 0.4 + 锁徽标 + tooltip imGatedHint）
  </read_first>
  <files>web/src/components/workflow/editor/composables/useImCapability.ts</files>
  <action>
    新建 `useImCapability.ts`：导出 `IM_SOURCE_TYPES = new Set(['create_group_chat','create_work_item_chat'])`（提供 chat_id 源的节点）+ `IM_DEPENDENT_TYPES = new Set(['notify_feishu','notify_feishu_im'])`（依赖 chat_id 的发群/通知节点；按 CONTEXT D，缺源时降级）。`useImCapability()` 返回 `hasImCapability`（computed：`useWorkflowsStore().nodes.some(n => IM_SOURCE_TYPES.has(n.nodeType))`）+ `isImGated(nodeType: string): boolean`（`IM_DEPENDENT_TYPES.has(nodeType) && !hasImCapability.value`）。纯派生，无副作用/日志。
    注：呈现选型采「禁用」（默认，UI-SPEC 推荐）——可发现性 + 引导补全；本 composable 只给判定，视觉在 Task 2。
  </action>
  <verify>
    <automated>cd web && pnpm vitest run useImCapability</automated>
  </verify>
  <acceptance_criteria>
    - 图含 create_group_chat → hasImCapability=true → isImGated('notify_feishu_im')=false。
    - 图无任何 IM 源 → isImGated('notify_feishu_im')=true、isImGated('notify_feishu')=true；非 IM 节点（如 ai_coding）恒 false。
  </acceptance_criteria>
  <done>图级 IM 能力判定就绪（源/依赖集 + hasImCapability + isImGated），单测覆盖有源/无源/非 IM 节点。</done>
</task>

<task type="auto">
  <name>Task 2: BaseWorkflowNode 端口形状/着色 + 拖拽态 + IM 门控 + 附着徽标</name>
  <read_first>
    - web/src/components/workflow/editor/nodes/BaseWorkflowNode.vue（ports computed、portKind/PORT_DOT_COLOR、Handle 渲染三处：单出口/多出口分支列表/input；style 绑定）
    - web/src/components/workflow/editor/composables/portShapes.ts（resolvePortShape；新增 shape 色板见下）
    - web/src/components/workflow/editor/composables/useConnectionDragState.ts（dragging / isCompatibleTarget）
    - web/src/components/workflow/editor/composables/useImCapability.ts（Task 1）
    - web/src/components/workflow/editor/nodes/nodeVisuals.ts（clarification_card 视觉，93-04）
    - web/src/components/workflow/editor/composables/useWorkflowTransform.ts（93-03 数据契约：附着子节点 node.data.metadata.parentNodeId 与 top-level parentNode 同源——本 plan 附着徽标读 data.metadata.parentNodeId 这一路）
    - .planning/phases/93-capability-slot-editor/93-UI-SPEC.md（"端口形状语义可视化"凹槽/凸点 + 能力 shape 色板 + 拖拽态机表 + IM 门控 + 附着徽标规格）
  </read_first>
  <files>web/src/components/workflow/editor/nodes/BaseWorkflowNode.vue</files>
  <action>
    端口集扩展：`ports` computed 把每端口的 `shape`（从 nt.inputs/outputs 的 `shape`）并入 PortItem（`{ id, group, label, shape }`）。
    着色（prescriptive，UI-SPEC「着色优先级」）：新增 shape→hex 色板（`SHAPE_DOT_COLOR`：clarification_request/answer=#f59e0b、feishu_message=#06b6d4、feishu_document=#6366f1、technical_plan=#8b5cf6、coding_assignment=#a78bfa、approval_result=#10b981）；handle 背景色 = shape 非空用 `SHAPE_DOT_COLOR[shape]`，否则回退既有 `PORT_DOT_COLOR[portKind(id)]`（保 default 绿/error 红零回归）。
    形状（UI-SPEC「凹槽/凸点」）：typed shape 端口（shape 非空）→ handle 加 `4px 圆角方形` 类（input 描边凹槽 `border` + 透明底；output 实心凸点 `bg`）；空契约 default/error → 既有圆形 handle 不变（零回归）。用 class 绑定区分（不引入新 CSS 库；Tailwind `rounded-[4px]` + border/bg）。
    拖拽态（消费 useConnectionDragState）：在 input handle 上，当 `dragging` 为 true：兼容（`isCompatibleTarget(props.data.nodeType, port.id)`）→ 加 compatible-highlight 类（emerald 4px 光环 `box-shadow` + 放大至 14px，命中吸附的脉冲由 93-06 控制）；不兼容 → 加 forbidden 类（`opacity:0.3`）。非拖拽态保持 idle 既有外观。
    IM 门控（消费 useImCapability）：节点 `isImGated(props.data.nodeType)` 为 true → 卡片整体 `opacity-40` + 右上角锁徽标（`icon-[lucide--lock]`）+ tooltip 文案 `t('workflow.editor.slot.imGatedHint')`（用既有 ~/components/ui Tooltip 或 title）；不阻断既有交互逻辑（仅视觉 + cursor-not-allowed 在 IM handle 上）。
    附着徽标（SLOT-04，WARNING 1 跨 plan 数据契约）：读取来源**固定**为 `props.data.metadata?.parentNodeId`——此即 93-03 transform 契约固化的同源字段（93-03 保证 top-level `node.parentNode` 与 `node.data.metadata.parentNodeId` 同源并存，本 plan 只读 data.metadata 这一路，**不**改读 top-level parentNode）。若该值非空（本节点是附着子）→ 左上角 `附着` Badge（`bg-amber-500/12 text-amber-600 text-[10px]`）+ hover tooltip `t('workflow.editor.slot.attachedHint')`。
    i18n：用 vue-i18n `useI18n().t`（组件内），键已由 93-01 落 zh-CN.json，本 plan 只读不改 locale 文件。
    高频渲染：shape 色板/类计算为 O(1) 查表，避免每帧建新对象；保留既有 portTop/PORT_TEXT_CLASS 等不动。
  </action>
  <verify>
    <automated>cd web && pnpm vitest run BaseWorkflowNode</automated>
  </verify>
  <acceptance_criteria>
    - typed shape 端口渲染方形类（input 凹/output 凸）+ shape 色；default/error 端口仍圆形 + 语义色（既有 BaseWorkflowNode.test.ts 零回归）。
    - 模拟 dragging + 兼容源 → 对应兼容 input handle 带 highlight 类、不兼容 handle 带 forbidden（降透明）类。
    - 无 IM 源图中 notify_feishu_im 节点渲染锁徽标 + imGatedHint（真实 zh-CN.json 断言文案）；有 IM 源时不门控。
    - 挂载 props.data.metadata.parentNodeId 非空的节点 → 渲染『附着』徽标 + attachedHint（断言读取来源为 data.metadata.parentNodeId，与 93-03 契约一致）。
    - `pnpm -C web vue-tsc --noEmit` 通过；受改文件 eslint 干净。
  </acceptance_criteria>
  <done>BaseWorkflowNode 端口方形/圆形 + shape 着色 + 拖拽兼容/禁止态 + IM 门控锁徽标 + 附着徽标全落地，既有节点渲染零回归，i18n 守护文案不被改空。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 前端门控视觉 → 后端 | IM 门控/附着徽标仅前端引导，非权威约束 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-05-BYPASS | Tampering | IM 门控视觉 | accept | 门控为前端引导（缺 chat_id 源时降级提示），后端节点执行期仍校验 chat_id 必填；视觉绕过不影响后端权威 |
| T-93-05-A11Y | (可达性) | 禁止/门控仅靠色 | mitigate | 不兼容除降透明外附 cursor-not-allowed + 形状语言 + Toast 文字（93-06）；IM 门控用锁图标 + tooltip 文字（非仅颜色），满足 WCAG 1.4.1 |
| T-93-05-SC | Tampering | 依赖安装 | mitigate | 无 npm 安装（仅既有 reka-ui/lucide），无供应链面 |
</threat_model>

<verification>
- `pnpm -C web vitest run useImCapability BaseWorkflowNode` 全绿。
- `pnpm -C web vue-tsc --noEmit` 通过；既有 BaseWorkflowNode.test.ts 零回归。
</verification>

<success_criteria>
- 节点卡端口区分契约槽（方形+shape 色）/通用流（圆形）、拖拽态高亮/禁止、IM 缺源门控引导、附着子节点徽标；既有渲染零回归。
</success_criteria>

<output>
Create `.planning/phases/93-capability-slot-editor/93-05-SUMMARY.md` when done
</output>
