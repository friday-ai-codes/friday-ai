---
phase: 91-clarification-outlets-resume
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - server/feishu/cards/chat_question_card.py
  - server/workflows/nodes/ai/plan_research.py
  - server/agents/tools/plan_research_tools.py
  - server/workflows/nodes/integrations/plan_deepen.py
  - server/tests/workflows/test_plan_research_node.py
autonomous: true
requirements: [CLARIFY-05, WR-03]
must_haves:
  truths:
    - "工作流 ai_plan_research 节点在 CLARIFYING 挂起时把澄清问题发成飞书交互卡到项目群"
    - "挂起时建 WorkflowEventSubscription(event_type=PlanClarifyCallback) 等待回调/超时兜底"
    - "澄清卡 form_submit 携带 clarification_id + action=plan_clarify_answer（不撞 chat_question_answer）"
    - "三处 pending 读法经 ahas_pending 谓词判存在性（结构化子题轮不误判）"
  artifacts:
    - path: server/feishu/cards/chat_question_card.py
      provides: "build_clarification_card 扩 clarification_id 入参 + 新 action"
      contains: "clarification_id"
    - path: server/workflows/nodes/ai/plan_research.py
      provides: "_maybe_suspend CLARIFYING 发卡 + 订阅"
      contains: "WorkflowEventSubscription"
  key_links:
    - from: server/workflows/nodes/ai/plan_research.py
      to: server/feishu/cards/chat_question_card.py
      via: "build_clarification_card(clarification_id=...)"
      pattern: "build_clarification_card"
    - from: server/workflows/nodes/ai/plan_research.py
      to: server/workflows/models/execution.py
      via: "WorkflowEventSubscription.objects.acreate(event_type=PlanClarifyCallback)"
      pattern: "PlanClarifyCallback"
---

<objective>
建设工作流/群出口面发卡侧（CLARIFY-05 工作流半）+ WR-03 收账：
1. 扩 `build_clarification_card` 加 `clarification_id` 入参（写进 form_submit `value`）+ 把 `value.action` 改为新动作 `plan_clarify_answer`（Pitfall 1：现 `chat_question_answer` 路由到工作流 GroupChatQuestion 回调，错位）。
2. 照 `plan_deepen.py` 范式补 `ai_plan_research._maybe_suspend` 的 CLARIFYING 分支：取 pending 轮 + 其 `ClarificationQuestion` 列表 → 发 `build_clarification_card` 到项目群 → 建 `WorkflowEventSubscription(event_type="PlanClarifyCallback")` → `waiting_event`。
3. WR-03：三处裸 `filter(answered_at__isnull=True)` pending 读法收口到 `ClarificationService.ahas_pending`（判存在性），取问题内容仍用显式查询（分工：判存在用谓词、取内容用查询）。

Purpose: 工作流澄清当前只挂 waiting_event、既不发卡也不订阅，群里收不到澄清；本 plan 补发卡 + 订阅，回调续推由 91-03 接。
Output: 扩展后的卡片构造器 + 节点发卡/订阅 + WR-03 三处收口 + 扩展测试。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/91-clarification-outlets-resume/91-CONTEXT.md
@.planning/phases/91-clarification-outlets-resume/91-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: WR-03 三处 pending 读法收口 ahas_pending</name>
  <files>server/workflows/nodes/ai/plan_research.py, server/agents/tools/plan_research_tools.py, server/workflows/nodes/integrations/plan_deepen.py, server/tests/workflows/test_plan_research_node.py</files>
  <read_first>
    - server/delivery/services/clarification_service.py:330-352（ahas_pending 统一谓词：子题未答 OR 旧单题行，兼容两形态）
    - server/workflows/nodes/ai/plan_research.py:307-345（_maybe_suspend CLARIFYING 现用 `Clarification.objects.filter(session_id, answered_at__isnull=True).values("id","question").afirst()`）
    - server/agents/tools/plan_research_tools.py:212 区域（chat 工具挂起判定，裸 filter answered_at__isnull=True）
    - server/workflows/nodes/integrations/plan_deepen.py:209-221（_apending_clarification_question 取最新未答问题）
  </read_first>
  <action>
    三处 pending 判定**存在性**统一收口到 `ClarificationService().ahas_pending(session.id)`（per WR-03，防结构化子题轮误判：容器 answered_at 仍空但子题已答 / 反之）。① `plan_research.py:_maybe_suspend` CLARIFYING 分支：gate 改用 `await ClarificationService().ahas_pending(session.id)` 判是否仍需挂起（替换裸 filter 的存在性语义）；**取问题内容**用于发卡（Task 3）仍保留显式查询取 pending 轮 + 子题（见 Task 3）。② `plan_research_tools.py:212`：同样把存在性判定换 `ahas_pending`（chat 工具挂起判定）。③ `plan_deepen.py:215 _apending_clarification_question`：判存在性用 `ahas_pending`，取最新未答问题文本仍用既有 `.order_by("-created_at").afirst()` 查询（分工：判存在用谓词、取内容用查询，零回归 plan_deepen 现有发卡）。async 防裸 lazy-FK（用 `session.id` 标量）。注意 `chat/views.py:3047` 是 `ConversationIntentTrace`（chat 路径）**不在** WR-03 范围，不动。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/workflows/test_plan_research_node.py tests/services -k pending -x -q</automated>
  </verify>
  <acceptance_criteria>
    - 三处（plan_research.py / plan_research_tools.py / plan_deepen.py）pending 存在性判定均经 `ahas_pending`；取问题内容查询保留。
    - 结构化子题轮场景：子题全已答 → ahas_pending=False 不误挂起；子题未答 → True 正确挂起（测试覆盖）。
    - plan_deepen 既有发卡用例零回归；ruff/mypy 干净。
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: 扩 build_clarification_card 携 clarification_id + 新 action plan_clarify_answer（CLARIFY-05 / Pitfall 1）</name>
  <files>server/feishu/cards/chat_question_card.py, server/tests/workflows/test_plan_research_node.py</files>
  <read_first>
    - server/feishu/cards/chat_question_card.py:122-274（build_clarification_card 现签名 + form_submit value action="chat_question_answer" + question_count，字段命名 q{i}/qt{i}）
    - server/feishu/cards/chat_question_card.py:277+（build_clarification_answered_card 置灰卡）
    - server/feishu/callbacks/plan_revision_callback.py:1-20（新前缀刻意区别既有前缀的范式：plan_revision_ vs plan_revise）
  </read_first>
  <action>
    扩 `build_clarification_card` 签名新增 keyword 入参 `clarification_id: str = ""`（per Pitfall 1，回调据此定位 delivery.Clarification 轮 + 按 order 映射子题）。在 form_submit 的 `value` dict 中：把 `"action": "chat_question_answer"` 改为 `"action": "plan_clarify_answer"`（新前缀 `plan_clarify_` 隔离，绝不撞 GroupChatQuestion 既有 `chat_question_answer` 路由——CardCallbackView 前缀 startswith 匹配），并新增 `"clarification_id": clarification_id`（与既有 execution_id/node_id/question_count 并列）。**零回归确认**：`server/feishu/cards/chat_question_card.py::build_clarification_card` 当前无生产调用方（grep 确认；`feishu/bot/service.py` 用的是 `bot_cards.py` 的同名单题版本，签名不同），故改 action 安全；不动 `bot_cards.py::build_clarification_card`。字段命名 `q{i}`/`qt{i}` 保持不变（回调据 order=i 映射子题）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/workflows/test_plan_research_node.py -k clarif -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `build_clarification_card(questions, execution_id, node_id, *, clarification_id="")` 返回卡 form_submit `value` 含 `action="plan_clarify_answer"` + `clarification_id`。
    - 测试断言卡片 value.action / value.clarification_id；`bot_cards.py` 同名函数与 `feishu/bot/service.py` 调用零回归。
    - ruff/mypy 干净。
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: ai_plan_research 节点 CLARIFYING 发卡 + WorkflowEventSubscription（CLARIFY-05 / Pattern 2）</name>
  <files>server/workflows/nodes/ai/plan_research.py, server/tests/workflows/test_plan_research_node.py</files>
  <read_first>
    - server/workflows/nodes/integrations/plan_deepen.py:186-292（发卡 + WorkflowEventSubscription.acreate + _send_clarify_card + _asend_card 经 ProjectService().resolve_or_create_group + FeishuIMService.send_card + _resolve_initiator 取触发用户）
    - server/workflows/nodes/integrations/board_split_review.py（_resolve_space / _aresolve_project 群解析 helper — 复用，对齐 Open Q2 推荐解）
    - server/workflows/nodes/ai/plan_research.py:289-345（_build_engine 注入 node_execution_id；_maybe_suspend CLARIFYING/RESEARCHING 分支结构）
    - server/feishu/cards/chat_question_card.py:122（扩展后的 build_clarification_card，Task 2）
    - server/workflows/models/execution.py（WorkflowEventSubscription 字段：workflow_execution/node_execution/event_type/project_key/timeout_at/timeout_action）
  </read_first>
  <action>
    在 `plan_research.py:_maybe_suspend` 的 CLARIFYING 分支补发卡 + 订阅（per CLARIFY-05，mirror plan_deepen.py:186-207）。仅当 `context.workflow_execution and context.node_execution`（工作流入口，chat 入口不发卡——chat 走 91-04 会话出口面）时执行。步骤：① 取 pending 轮 + 其子题：`round = await Clarification.objects.filter(session_id=session.id, answered_at__isnull=True).order_by("round_no","created_at").afirst()`；子题 `questions_qs = ClarificationQuestion.objects.filter(clarification_id=round.id, answered_at__isnull=True).order_by("order")` → 组装 `questions=[{"question":..., "type": qtype, "options":..., "recommended":...}]`（按 order）。② 解析群：复用 `board_split_review._resolve_space` / `_aresolve_project`（Open Q2 推荐解），取 chat_id 经 `ProjectService().resolve_or_create_group(project=..., member_ids=[], initiated_by_user_id=...)`（mirror plan_deepen._asend_card）；`initiated_by_user_id` 取触发用户（mirror plan_deepen `_resolve_initiator`：`context.workflow_execution.triggered_by_id` 缺则 "system"，**观测约束：后台/外部触发带 initiated_by_user_id**）。③ 发卡：`build_clarification_card(questions, execution_id=context.execution_id, node_id=context.node_id, clarification_id=str(round.id), round_no=round.round_no or 1)`（携 clarification_id，Task 2）→ `im_service = await FeishuIMService.create(space)` → `send_card(receive_id=chat_id, receive_id_type="chat_id", card=card)`；发卡全程 best-effort try/except + log（`plan_research_clarify_card_failed`，绝不反噬挂起）；卡片正文经 `redact_secrets_in_text` 脱敏（mirror plan_deepen）。④ 建订阅：`await WorkflowEventSubscription.objects.acreate(workflow_execution=context.workflow_execution, node_execution=context.node_execution, event_type="PlanClarifyCallback", project_key=context.workflow_context.get("project_key",""), timeout_at=timezone.now()+timedelta(minutes=60), timeout_action="fail")`。⑤ 保留既有 `NodeResult(status="waiting_event", output={session_id, kind:"clarification", suspension:{...clarification_id...}})`（output 仍带 clarification_id 供回调/节点重入定位）。WR-03 gate（Task 1）已把存在性判定换 ahas_pending；本 task 取问题内容用显式查询（分工）。async 防裸 lazy-FK（用 `*_id` 标量 / `.afirst` / `sync_to_async` 块）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/workflows/test_plan_research_node.py -k "clarif or subscription" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - 工作流入口 CLARIFYING 挂起：发 build_clarification_card（携 clarification_id）到项目群 + 建 WorkflowEventSubscription(event_type="PlanClarifyCallback")（测试 mock FeishuIMService.send_card 断言被调 + 断言订阅行被建）。
    - chat 入口（无 workflow_execution/node_execution）不发卡、不订阅（零回归 test_clarifying_suspends_waiting_event）。
    - 发卡 best-effort：send_card 抛错节点仍返回 waiting_event（不反噬挂起）；卡片正文脱敏；触发用户带 initiated_by_user_id。
    - ruff/mypy 干净。
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 节点→飞书群 | 澄清问题正文（含需求/召回）发到群卡，可能含敏感信息 |
| 卡片 value | clarification_id/action 作为回调路由锚，回调侧不可信 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-02-01 | Tampering | form_submit value.action 路由 | mitigate | 新前缀 `plan_clarify_` 隔离 GroupChatQuestion 的 `chat_question_answer`（前缀 startswith 不交叉，Pitfall 1）；卡片携服务端权威 `clarification_id` 供回调按 round 取轮 |
| T-91-02-02 | Info Disclosure | 澄清卡正文 / 日志泄密 | mitigate | 卡片正文经 `redact_secrets_in_text`（mirror plan_deepen._send_clarify_card）；发卡日志仅记 session_id/clarification_id/chat_id 标量 |
| T-91-02-03 | Repudiation | 后台/外部触发无触发用户归因 | mitigate | 发卡带 `initiated_by_user_id`（_resolve_initiator，缺记 system），观测 rule 强制 |
| T-91-02-04 | DoS | 挂起无超时兜底 | mitigate | `WorkflowEventSubscription.timeout_at`(60min)+`timeout_action="fail"` |
| T-91-02-05 | Availability | 发卡失败反噬节点挂起 | mitigate | 发卡 best-effort try/except，失败仍返回 waiting_event（观测 best-effort 不反噬业务） |
</threat_model>

<verification>
- `cd server && uv run pytest tests/workflows/test_plan_research_node.py tests/services -k "clarif or subscription or pending" -q` 全绿。
- `cd server && uv run pytest tests/workflows tests/delivery -q` 无回归（含 plan_deepen 发卡 + INV-6 守护）。
- `cd server && uv run ruff format --check feishu/cards/chat_question_card.py workflows/nodes/ai/plan_research.py && uv run ruff check feishu/cards/chat_question_card.py workflows/nodes/ai/plan_research.py agents/tools/plan_research_tools.py workflows/nodes/integrations/plan_deepen.py && uv run mypy workflows/nodes/ai/plan_research.py`。
- `cd server && uv run python manage.py makemigrations --check`（无新模型字段，应干净）。
</verification>

<success_criteria>
- 工作流场景澄清请求经飞书交互卡（单/多选 + ⭐推荐 + 其他）由机器人发到群（复用 build_clarification_card），挂起建 PlanClarifyCallback 订阅。
- 澄清卡携 clarification_id + 新 action plan_clarify_answer（不撞既有 chat_question_answer）。
- 三处 pending 读法收口 ahas_pending，结构化子题轮不误判；脱敏/归因/best-effort/INV-6 满足。
</success_criteria>

<artifacts>
## Artifacts this plan produces（供下游 plan / phase 引用）
- 扩展符号：`build_clarification_card(..., *, clarification_id="")`，form_submit `value.action="plan_clarify_answer"` + `value.clarification_id`。
- 接线：`ai_plan_research._maybe_suspend` CLARIFYING → 发卡 + `WorkflowEventSubscription(event_type="PlanClarifyCallback")`。
- WR-03 收口：plan_research.py / plan_research_tools.py / plan_deepen.py 三处 pending 存在性经 `ahas_pending`。
- 新订阅事件键：`PlanClarifyCallback`（91-03 回调消费）。
</artifacts>

<output>
Create `.planning/phases/91-clarification-outlets-resume/91-02-SUMMARY.md` when done.
</output>
