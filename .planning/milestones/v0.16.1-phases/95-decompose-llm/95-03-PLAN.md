---
phase: 95-decompose-llm
plan: 03
type: execute
wave: 3
depends_on: [95-02]
files_modified:
  - server/services/plan_orchestration/engine.py
  - server/tests/services/test_plan_orchestration_engine.py
autonomous: true
requirements: [DECOMP-01]
must_haves:
  truths:
    - "engine._decompose 调 agenerate_decomposition_segments；LLM 成功 → segments 为结构化 list[dict]"
    - "LLM 返回 None（失败/缺 model/解析空）→ 回退按非空行切分 list[str]（现状行为）"
    - "_decompose 永远走到 transition('decomposed')，异常被 helper 吞、session 推进 ROUTING（绝不落 FAILED）"
    - "decomposition dict 始终保留 requirement_text / include_repos 两键（routing 契约不变）"
    - "既有 test_advance_from_decomposing_real_decompose（回退路径，list[str]）零改动通过"
  artifacts:
    - path: "server/services/plan_orchestration/engine.py"
      provides: "_decompose 接线 helper + splitlines 回退 + 契约保持"
      contains: "agenerate_decomposition_segments"
    - path: "server/tests/services/test_plan_orchestration_engine.py"
      provides: "decompose LLM 成功 / fail-soft / no-model 用例"
      contains: "decompose"
  key_links:
    - from: "server/services/plan_orchestration/engine.py"
      to: "services.plan_orchestration.decompose_segments.agenerate_decomposition_segments"
      via: "_decompose 内 await 调用，None → splitlines 回退"
      pattern: "agenerate_decomposition_segments\\("
    - from: "server/services/plan_orchestration/engine.py"
      to: "PlanSessionService.transition"
      via: "transition(session, 'decomposed', decomposition=...)"
      pattern: "transition\\(session, \"decomposed\""
---

<objective>
把 `PlanOrchestrationEngine._decompose` 从「`requirement_text.splitlines()` 按行切分 stub」接线到 95-02 的 LLM helper：调 `agenerate_decomposition_segments` 取结构化 `segments`，helper 返回 `None`（失败/缺 model/解析空）时**回退现状按非空行切分**（D-C），始终保留 `requirement_text`/`include_repos` 契约键并照常 `transition("decomposed")`。

Purpose: 落地 DECOMP-01 的核心行为升级（D-A），同时严守 RESEARCH 的三条 Anti-Pattern：①decompose 异常绝不落 FAILED（helper 自包 fail-soft + handler 永远 transition decomposed）；②不改契约键；③engine 不直接 mutate status（只经 transition，T-36-03-01 守护）。回退路径保持产 list[str]，使既有断言零改通过。

Output: 接线后的 `_decompose` + 扩展的 engine 测试（LLM 成功 / fail-soft / no-model）。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/95-decompose-llm/95-CONTEXT.md
@.planning/phases/95-decompose-llm/95-RESEARCH.md

@server/services/plan_orchestration/engine.py
@server/services/plan_orchestration/decompose_segments.py
@server/tests/services/test_plan_orchestration_engine.py
@server/tests/services/test_engine_clarify.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: engine._decompose 接线 LLM helper + splitlines 回退</name>
  <files>server/services/plan_orchestration/engine.py</files>
  <read_first>
    - server/services/plan_orchestration/engine.py L106-124（现 `_decompose` stub + transition("decomposed")）
    - server/services/plan_orchestration/engine.py L92-104（`advance` 通用 except → transition("fail")，说明为何 helper 必须自包 fail-soft）
    - server/services/plan_orchestration/clarify_adapter.py（参考 engine/adapter 如何 import + 调用同级 helper、记回退事件的范式；如无则参照 engine 内其它 stage 的 lazy import 风格）
  </read_first>
  <behavior>
    - 既有输入解析不变：`requirement_text = existing.get("requirement_text", "")`，空且有 work_item → `_work_item_title`；`include_repos = existing.get("include_repos", [])`。
    - 调 `result = await agenerate_decomposition_segments(requirement_text=requirement_text, include_repos=include_repos)`。
    - `result` 非空（list[dict]）→ `segments = result`。
    - `result` 为 None/空 → 回退 `segments = [line.strip() for line in requirement_text.splitlines() if line.strip()]`（严格保持现状 list[str]），并记 `plan_decompose_fallback_splitlines`（category=sampling, component=plan_orchestration, segment_count=…）便于测试断言。
    - `decomposition = {"requirement_text", "include_repos", "segments"}`，**前两键不变**。
    - 始终 `await self.session_service.transition(session, "decomposed", decomposition=decomposition)`（绝不直接写 status）。
  </behavior>
  <action>
    改写 `_decompose`（per D-A/C，DECOMP-01）：在函数内 lazy import `from services.plan_orchestration.decompose_segments import agenerate_decomposition_segments`（与其它 stage 内 import 风格一致，避免顶层循环依赖），按 `<behavior>` 接线。
    关键守护：
    - helper 已自包 `except Exception` 返回 None，`_decompose` **不要**再 try 包裹 helper 调用（让其正常返回 None 即可）；即便保留防御，最终也必须走到 `transition("decomposed")`，绝不让 decompose 落 FAILED（RESEARCH Anti-Pattern 1 / Pitfall 2）。
    - 不改 `requirement_text`/`include_repos` 键名（RESEARCH Anti-Pattern 2 / routing 契约）。
    - 不写 `session.status = ...`（源码守护测试 `test_engine_does_not_write_status_directly` grep `\.status\s*=`，RESEARCH Anti-Pattern 3）。
    - 更新 `_decompose` docstring：删除「TODO(Phase 38+) 真实拆分」，改述为 LLM 拆分 + fail-soft 回退按行切分。
  </action>
  <verify>
    <automated>cd server &amp;&amp; uv run pytest tests/services/test_plan_orchestration_engine.py -x</automated>
  </verify>
  <done>`_decompose` 调 helper；None → splitlines list[str] 回退 + 记回退事件；契约键保留；恒走 transition("decomposed")；既有 engine 测试（含源码纯度守护）全绿。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 扩展 engine 测试（LLM 成功 / fail-soft / no-model）</name>
  <files>server/tests/services/test_plan_orchestration_engine.py</files>
  <read_first>
    - server/tests/services/test_plan_orchestration_engine.py L27-60（既有 `test_advance_from_decomposing_real_decompose` 回退路径断言 + AsyncMock 注入范式）
    - server/tests/services/test_engine_clarify.py L186-276（patch 模块级 helper 常量 + AsyncMock 注入 LLM 产出 / 返回空触发 fail-soft + patch logger 断言事件名 范式）
  </read_first>
  <behavior>
    - decompose_llm（成功）：patch helper 返回 `[{"title":"登录页改造","module":"用户中心","layer":"frontend","repo_hint":"r1"}]` → advance → `decomposition["segments"]` 为该 dict 列表 + status==ROUTING + 保留 requirement_text/include_repos。
    - decompose_fail_soft（None）：patch helper 返回 None → advance → `segments == ["做A","做B"]`（splitlines list[str]）+ status==ROUTING（**非 FAILED**）+ patch logger 断言含 `plan_decompose_fallback_splitlines`。
    - decompose_no_model：等价 fail-soft（helper 返回 None）→ 回退 + ROUTING（用同一回退断言覆盖；no_default_model 行为已在 95-02 helper 单测覆盖）。
    - 契约保持：LLM 成功路径也断言 `decomposition["requirement_text"]` / `["include_repos"]` 仍在。
    - 既有 `test_advance_from_decomposing_real_decompose` 保持零改动（测试环境无 Provider 凭证 → helper 内 aresolve 抛 → 返回 None → 回退 list[str]）。
  </behavior>
  <action>
    在 `test_plan_orchestration_engine.py` 顶部加模块级常量 `_DECOMPOSE_GEN = "services.plan_orchestration.engine.agenerate_decomposition_segments"`（因 95-03 Task 1 在 `_decompose` 内 lazy import，patch 目标为 engine 模块命名空间中被引入的名字——若 lazy import 导致 patch 点不可达，则改 patch `services.plan_orchestration.decompose_segments.agenerate_decomposition_segments` 源定义点，执行期二选一以实际可 patch 为准）。
    新增三个 `@pytest.mark.django_db @pytest.mark.asyncio` 用例实现 `<behavior>`（成功 / fail-soft None / 回退事件断言），用真实 `PlanSession` + `PlanOrchestrationEngine()`，`with patch(_DECOMPOSE_GEN, new=AsyncMock(return_value=...))`；fail-soft 用例额外 `patch("services.plan_orchestration.engine.logger")` 断言 `plan_decompose_fallback_splitlines` 在 info 事件名中（对齐 test_engine_clarify L243-256 范式）。
    **不要**修改既有 `test_advance_from_decomposing_real_decompose` 的断言（回退路径仍产 list[str]）。
  </action>
  <verify>
    <automated>cd server &amp;&amp; uv run pytest tests/services/test_plan_orchestration_engine.py -x</automated>
  </verify>
  <done>新增 LLM 成功 / fail-soft / 回退事件三类断言全绿；既有 decompose 用例零改通过；fail-soft 用例证明 session 推进 ROUTING 而非 FAILED。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| helper 返回值 → engine | helper 返回 None / list[dict] 跨入 engine；engine 不得因 None 崩溃或落 FAILED |
| engine → PlanSession.status | 状态只能经 `transition` 改（INV-6 / T-36-03-01 纯度守护） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-95-07 | Denial of Service | `_decompose` / 编排链 | mitigate | helper 自包 fail-soft 返回 None；`_decompose` 恒走 transition("decomposed") → decompose 永不落 FAILED（D-C / Anti-Pattern 1）；fail-soft 测试断言 session 推进 ROUTING |
| T-95-08 | Tampering | routing 契约（requirement_text/include_repos） | mitigate | `_decompose` 始终保留两契约键；测试断言 LLM 与回退两路径均含两键（RESEARCH Downstream Contract） |
| T-95-09 | Tampering | PlanSession 状态机纯度 | mitigate | engine 只经 `transition` 改 status；既有源码守护测试 `test_engine_does_not_write_status_directly`（grep `\.status\s*=`）保持全绿 |
| T-95-SC | Tampering | pip installs | mitigate | 不安装任何包（纯接线 + 测试） |
</threat_model>

<verification>
- `cd server && uv run pytest tests/services/test_plan_orchestration_engine.py -x` 全绿（含既有源码纯度守护 + 既有 decompose 用例零改）
- `cd server && uv run pytest tests/services/ -x`（wave 合并门）全绿
- 人工抽查：fail-soft 用例 status==ROUTING（非 FAILED）；LLM 成功路径 segments 为 list[dict]
</verification>

<success_criteria>
- `_decompose` 接线 helper；None → splitlines list[str] 回退 + 回退事件
- decomposition 恒含 requirement_text/include_repos；恒 transition("decomposed")
- 新增 LLM 成功 / fail-soft / 回退事件用例全绿
- 既有 `test_advance_from_decomposing_real_decompose` + 源码纯度守护零改通过
- decompose 任何路径不落 FAILED
</success_criteria>

<output>
Create `.planning/phases/95-decompose-llm/95-03-SUMMARY.md` when done.

## Artifacts this phase produces (95-03)
- 改动符号：`PlanOrchestrationEngine._decompose`（接线 `agenerate_decomposition_segments` + splitlines 回退 + `plan_decompose_fallback_splitlines` 事件）
- 新增测试：`test_plan_orchestration_engine.py` decompose LLM 成功 / fail-soft / 回退事件用例
- 完成 DECOMP-01：decompose 由按行切分升级为 LLM 跨仓拆分 + fail-soft 回退
</output>
