---
phase: 88
plan: 05
type: execute
wave: 5
depends_on: ["88-04", "88-03"]
files_modified:
  - server/feishu/callbacks/repo_association_callback.py
  - server/initiatives/services/repo_association_service.py
  - server/tests/feishu/test_repo_association_callback.py
  - server/tests/initiatives/test_repo_association_output.py
autonomous: true
requirements: [REPO-02]
must_haves:
  truths:
    - "卡片回调状态机（D-03 交互回路）：@register_card_callback('repo_assoc_') 前缀唯一不撞 board_split_；同步即时返回轻量确认卡，重活 _run_in_thread + bind_task_context（re-bind 触发用户），逐字镜像 board_split_callback"
    - "确认动作 repo_assoc_confirm → confirm_repos + dispatch_verify（派逐仓深验容器，保持 waiting，绝不 approve）；澄清 repo_assoc_refine → service.refine 重 route 重发候选卡（保持 waiting）"
    - "回退动作（D-03 发现不符可回退重确认）：repo_assoc_reconfirm → 重开候选选择（回 clarify，重发候选卡，保持 waiting）；repo_assoc_accept_mismatch → 置 RepoAssociation status=verified + approve_node 恢复（用户接受 mismatch）"
    - "最终确认：全 fit 由节点续驱直接 approve_node（88-04），mismatch 接受经回调 approve_node；恢复携 verified 仓 + 各仓 verdict"
    - "Phase 89 输出契约（D-06）：RepoAssociationService.get_verified_associations(project) → verified RepoAssociation 列表 + 各仓 verdict，供 Phase 89 PlanSession.decomposition.include_repos 直接消费（对齐 RepoRouterV2Adapter include 优先级）"
    - "全程 fail-soft（D-03）：回调重活异常记 failed 不反噬飞书主响应；单仓校验失败不阻断其余（聚合时缺失记 unknown）；正文/异常经 redact_secrets_in_text；归因 callback.user_open_id"
  artifacts:
    - path: "server/feishu/callbacks/repo_association_callback.py"
      provides: "handle_repo_assoc_action（confirm/refine/reconfirm/accept_mismatch 状态机，@register_card_callback('repo_assoc_')）"
      contains: "register_card_callback"
    - path: "server/initiatives/services/repo_association_service.py"
      provides: "get_verified_associations / accept_mismatch / reopen_candidates（Phase 89 输出契约 + 回退状态迁移，INV-6）"
      contains: "def get_verified_associations"
    - path: "server/tests/feishu/test_repo_association_callback.py"
      provides: "confirm→dispatch_verify 保持 waiting / refine 保持 waiting / reconfirm 回 clarify / accept_mismatch→approve_node"
      contains: "test_mismatch_rollback"
    - path: "server/tests/initiatives/test_repo_association_output.py"
      provides: "get_verified_associations 输出契约（verified 仓 + verdict）供 Phase 89 消费"
      contains: "test_verified_output"
  key_links:
    - from: "server/feishu/callbacks/repo_association_callback.py"
      to: "initiatives.services.repo_association_service.RepoAssociationService.confirm_repos / dispatch_verify"
      via: "repo_assoc_confirm → 派逐仓深验（携 node_execution_id），保持 waiting"
      pattern: "dispatch_verify"
    - from: "server/feishu/callbacks/repo_association_callback.py"
      to: "workflows.engine.scheduler.WorkflowEngine.approve_node"
      via: "accept_mismatch → status=verified + approve_node 恢复（mirror board_split_callback approve_node）"
      pattern: "approve_node"
---

<objective>
交付 REPO-02 的交互闭环尾段：`repo_association_callback` 卡片回调状态机（确认→派逐仓深验 / 澄清→重 route / 回退→重确认 / 接受 mismatch→恢复），以及给 Phase 89 的「确认仓库 + verdict」输出契约。逐字镜像 Phase 87 `board_split_callback` 的同步即时返回 + `_run_in_thread`+`bind_task_context` 后台处理范式。

锁定决策：D-03 用户确认仓库 → 逐仓深验 → 发现不符可回退重确认 → 最终卡片确认；D-06 输出确认仓库关联 → Phase 89 技术方案深化输入（建议 Phase 88 产「该仓是否适配 + 适配哪些 feature（粗）」，精确分配留 Phase 89，RESEARCH Open Q1/Q2）。

Purpose: 串起「候选卡 → 确认 → 容器深验 → mismatch 回退 / 最终确认」的人机协同状态机，并把结果以稳定契约交给 Phase 89。
Output: `repo_association_callback`（4 动作状态机）+ `get_verified_associations`/`accept_mismatch`/`reopen_candidates` + 测试。
</objective>

<execution_context>
@.planning/phases/88-repo-association/88-CONTEXT.md
@.planning/phases/88-repo-association/88-RESEARCH.md
@.planning/observability/LOGGING-SPEC.md
</execution_context>

<context>
@.planning/PROJECT.md
@server/feishu/callbacks/board_split_callback.py
@server/workflows/nodes/integrations/repo_association.py
@server/feishu/cards/repo_association_card.py
@server/initiatives/services/repo_association_service.py
@server/services/plan_orchestration/repo_router_adapter.py
@.cursor/rules/observability-logging.mdc
</context>

<artifacts_produced>
- NEW `server/feishu/callbacks/repo_association_callback.py`（逐字镜像 `board_split_callback.py`）：
  - `@register_card_callback("repo_assoc_")` `handle_repo_assoc_action(callback)`：同步入口，提取 action/execution_id/node_id/round（`_extract_callback_data`），缺 ID → None；按 action 分支 `_run_in_thread(...)` + 即时返回 `_ack_card`。
  - `repo_assoc_confirm` → `_do_confirm_and_verify_async`：取 waiting NodeExecution（`_aget_waiting_node` mirror）→ 从 output_data 取选中 repo_ids + node_execution.id → `confirm_repos` + `dispatch_verify(node_execution_id=str(node_execution.id), ...)`（派逐仓深验容器，**保持 waiting**，更新 output_data stage="verifying"，发 `build_repo_assoc_verifying_card`，绝不 approve——等容器回调经 _schedule_workflow_resume 重入节点续驱）。
  - `repo_assoc_refine` → `_do_refine_async`：`RepoAssociationService.refine(extra_instruction=refine_input)` 重 route → 更新 output_data(round+1, proposal) → 重发候选流式卡（新建实体，mirror board_split _resend_streaming_card）→ 保持 waiting。
  - `repo_assoc_reconfirm` → `_do_reconfirm_async`：回 clarify 阶段（output_data stage="clarify"）→ 重发候选卡 → 保持 waiting（重新确认仓库）。
  - `repo_assoc_accept_mismatch` → `_do_accept_async`：`accept_mismatch`（mismatch 仓也置 verified 或剔除——选置 verified 接受）→ 发 `build_repo_assoc_done_card` → `approve_node`（next_handle verified 恢复，mirror board_split _do_board_split_start_async）。
  - `_ack_card`/`_extract_callback_data`/`_resolve_space`/`_aget_waiting_node`/`_FeishuResponder`/`_resend_streaming_card`（逐字镜像 board_split_callback）。
- EXTEND `server/initiatives/services/repo_association_service.py`（INV-6 写入收口）：
  - `get_verified_associations(project) -> list[dict]`（Phase 89 契约：verified RepoAssociation + 各仓 verdict → `[{repository_id, repo_name, verdict, matched_node_paths}]`，供 `PlanSession.decomposition.include_repos` 消费，对齐 `RepoRouterV2Adapter` include 优先级）。
  - `accept_mismatch(association)`（mismatch 仓置 verified——用户接受；status=verified）。
  - `reopen_candidates(association)`（回 proposed/clarify，承载回退重确认）。
- NEW `server/tests/feishu/test_repo_association_callback.py`：confirm→dispatch_verify 保持 waiting（断言不 approve）；refine→重 route 保持 waiting；reconfirm→回 clarify 重发卡；accept_mismatch→approve_node 恢复 + status=verified；回调重活异常 fail-soft（不反噬 ack）。
- NEW `server/tests/initiatives/test_repo_association_output.py`：`get_verified_associations` 仅返 verified 关联 + verdict；空（无 verified）返 []；契约字段稳定供 Phase 89。
- structlog 事件：`repo_association_card_action`(caller, component=repo_association, action, execution_id, round)、`repo_association_confirm`/`_refine`/`_reconfirm`/`_accept_mismatch`(caller, +duration_ms)，失败 status=failed（error 经 redact_secrets_in_text）；归因 user_id=callback.user_open_id（bind_task_context re-bind）。
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: repo_association_callback 状态机（confirm/refine，保持 waiting 派深验/重 route）</name>
  <read_first>
    - server/feishu/callbacks/board_split_callback.py（全文——@register_card_callback / _extract_callback_data / _ack_card / _run_in_thread+bind_task_context / _aget_waiting_node / _resolve_space / _resend_streaming_card / _FeishuResponder，逐字镜像）
    - server/initiatives/services/repo_association_service.py（confirm_repos / dispatch_verify / refine，88-02/88-03）
    - server/feishu/cards/repo_association_card.py（build_repo_assoc_card / verifying / _resend，88-04）
  </read_first>
  <action>
    新建 `repo_association_callback.py`，`@register_card_callback("repo_assoc_")`（前缀唯一，不撞 board_split_/chat_question）。同步入口即时返回 `_ack_card`，重活 `_run_in_thread` + `bind_task_context(user_id=callback.user_open_id, source="feishu", component="repo_association")`。`repo_assoc_confirm`→`_do_confirm_and_verify_async`：`_aget_waiting_node`（非 waiting → no-op 幂等）→ 从 output_data 取 proposal/选中 repo_ids + chat_id → `confirm_repos` + `dispatch_verify(project=..., confirmed=..., node_execution_id=str(node_execution.id), initiated_by_user_id=responder_id or "system")` → 更新 output_data(stage="verifying") asave → 发 `build_repo_assoc_verifying_card`（best-effort）→ **不 approve**（等容器回调 _schedule_workflow_resume 重入节点续驱聚合）。`repo_assoc_refine`→`_do_refine_async`：`refine(extra_instruction=refine_input)` → 更新 output_data(round+1, proposal) → `_resend_streaming_card`（候选卡，新建实体 mirror board_split）→ 保持 waiting。全程 fail-soft（异常 redact + status=failed，不反噬 ack）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/feishu/test_repo_association_callback.py -x -q -k "confirm or refine"</automated>
  </verify>
  <acceptance_criteria>
    - `@register_card_callback("repo_assoc_")` 注册；前缀不撞既有 board_split_/chat_question（grep 确认唯一）。
    - confirm → 调 `confirm_repos` + `dispatch_verify(node_execution_id=...)`，节点保持 waiting（断言 approve_node 未被调）。
    - refine → 调 `refine(extra_instruction=...)` + 重发候选卡，保持 waiting。
    - 回调重活异常 → fail-soft（断言 ack 仍返回、异常不冒泡）。
  </acceptance_criteria>
  <done>confirm/refine 状态机：确认派逐仓深验保持 waiting、澄清重 route 重发卡（D-03 多轮，复用 board_split 范式）。</done>
</task>

<task type="auto">
  <name>Task 2: 回退/接受状态机（reconfirm 回 clarify / accept_mismatch → approve_node）+ 服务态迁移</name>
  <read_first>
    - server/feishu/callbacks/board_split_callback.py（_do_board_split_start_async 行 174-270——approve_node 恢复 + SUSPENDED→RUNNING + approval_data 写法，逐字镜像 accept 流程）
    - server/workflows/nodes/integrations/repo_association.py（续驱发 mismatch 卡 + verified handle，88-04，确认 accept/reconfirm 与续驱不冲突）
    - server/initiatives/services/repo_association_service.py（collect_verdicts / 置 verified 入口，88-03）
  </read_first>
  <action>
    在 `repo_association_callback.py` 加：`repo_assoc_reconfirm`→`_do_reconfirm_async`：取 waiting node → output_data stage="clarify"（回候选选择）→ 重发候选卡 → 保持 waiting（重新确认仓库，D-03 回退）。`repo_assoc_accept_mismatch`→`_do_accept_async`：`accept_mismatch(association)`（mismatch 仓置 verified——用户接受）→ 发 `build_repo_assoc_done_card`（best-effort）→ 写 node_execution.approval_data(verified_repos/verdicts) + SUSPENDED→RUNNING → `WorkflowEngine().approve_node(node_execution, _FeishuResponder(responder_id), "repo_assoc_accept_mismatch")` 恢复（next_handle verified，mirror board_split_start）。在 service 加 `accept_mismatch`/`reopen_candidates` 状态迁移（INV-6 写入收口）。全程 fail-soft。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/feishu/test_repo_association_callback.py -x -q -k "reconfirm or mismatch or accept"</automated>
  </verify>
  <acceptance_criteria>
    - reconfirm → output_data stage="clarify" + 重发候选卡，保持 waiting（断言不 approve）。
    - accept_mismatch → `accept_mismatch` 置 status=verified + `approve_node` 恢复（断言 approve_node 被调、next_handle verified）。
    - 状态迁移经 service（INV-6 guard 仍绿）。
  </acceptance_criteria>
  <done>回退重确认 + 接受 mismatch 状态机闭环（D-03 发现不符可回退 + 最终确认）。</done>
</task>

<task type="auto">
  <name>Task 3: Phase 89 输出契约 get_verified_associations + 测试</name>
  <read_first>
    - server/services/plan_orchestration/repo_router_adapter.py（_resolve_repository_ids 的 include 优先级——Phase 89 PlanSession.decomposition.include_repos 消费形态对齐）
    - server/initiatives/services/repo_association_service.py（collect_verdicts / RepoAssociation verified 查询）
    - .planning/phases/88-repo-association/88-RESEARCH.md（Open Q1/Q2——Phase 88 产粗 feature→repo + verdict，精确分配留 Phase 89）
  </read_first>
  <action>
    在 service 加 `get_verified_associations(project) -> list[dict]`：查 `RepoAssociation(status=verified, project=...)` + 关联各仓最新 RepoVerifyTask.verdict → 返回 `[{repository_id, repo_name, verdict:{fit,confidence,summary,...}, matched_node_paths, routed_reason}]`（粗 feature→repo + 适配性 verdict，对齐 Phase 89 include_repos 优先级；精确分配留 Phase 89）。新建 `test_repo_association_output.py`：verified 关联 + verdict 输出契约（字段稳定）、无 verified 返 []、mismatch/proposed 不计入。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/initiatives/test_repo_association_output.py -x -q && uv run pytest tests/initiatives/test_repo_association_inv6_guard.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `get_verified_associations(project)` 仅返 status=verified 关联 + 各仓 verdict（断言字段含 repository_id/verdict/matched_node_paths）。
    - 无 verified → 返 []；proposed/confirmed/rejected 不计入。
    - INV-6 guard 仍绿（输出查询为只读，不旁路写）。
  </acceptance_criteria>
  <done>Phase 89 输入契约固化（确认仓库 + verdict），对齐 include_repos 优先级（D-06）。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 飞书卡片回调 → 工作流恢复/派容器 | 半可信，归因经 user_open_id，状态机服务端权威 |
| 回调重活 → 飞书主响应 | 重活异常不可反噬同步 ack |
| 确认 repo_ids → dispatch_verify | 仅服务端 output_data 权威，action_value 仅携 ID |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88-05-FAILSOFT | Denial of Service | 回调重活异常反噬主响应 | mitigate | _run_in_thread 后台 + 整段 try/except fail-soft，status=failed 不抛（mirror board_split_callback） |
| T-88-05-INFO | Information Disclosure | 回调日志/异常泄漏 | mitigate | 正文/异常经 redact_secrets_in_text；日志仅记 action/execution_id/round |
| T-88-05-ATTR | Repudiation | 确认/接受归因 | mitigate | bind_task_context(user_id=callback.user_open_id) re-bind；approve_node 携 _FeishuResponder 审计 |
| T-88-05-IDEMP | Tampering | 重复回调重复派容器 | mitigate | _aget_waiting_node 非 waiting → no-op 幂等；dispatch 仅 confirmed 子集 |
| T-88-05-SC | Tampering | npm/pip/cargo installs | accept | 本 plan 无包安装（纯内部复刻 callback + service 查询） |
</threat_model>

<verification>
- `cd server && uv run pytest tests/feishu/test_repo_association_callback.py tests/initiatives/test_repo_association_output.py tests/initiatives/test_repo_association_inv6_guard.py -q` 全绿。
- grep 确认 `@register_card_callback("repo_assoc_")` 前缀唯一；回调重活 fail-soft；confirm 不 approve、accept_mismatch approve。
- `get_verified_associations` 只读契约稳定供 Phase 89。
</verification>

<success_criteria>
- 卡片回调状态机闭环：confirm→派逐仓深验(保持 waiting) / refine→重 route / reconfirm→回退重确认 / accept_mismatch→approve_node（REPO-02，D-03 全交互回路）。
- Phase 89 输出契约 get_verified_associations（确认仓库 + verdict）固化（D-06）。
- 全程 fail-soft + 归因 + 脱敏（观测/安全约束满足）。
</success_criteria>

<output>
完成后创建 `.planning/phases/88-repo-association/88-05-SUMMARY.md`
</output>
