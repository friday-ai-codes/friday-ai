---
phase: 89
plan: 02
type: execute
wave: 2
depends_on: [89-01]
files_modified:
  - server/initiatives/services/plan_deepen_service.py
  - server/feishu/cards/plan_revision_card.py
  - server/feishu/callbacks/plan_revision_callback.py
  - server/feishu/urls.py
  - server/tests/feishu/test_plan_revision_callback.py
  - server/tests/initiatives/test_plan_revision_service.py
autonomous: true
requirements: [PLAN-02]
must_haves:
  truths:
    - "方案修订回路：执行中发现要改/增/删仓库 → 「调研问题发现」卡片 → 更新方案/创建补充修订 + 同步改仓库关联（多轮，优雅处理）——复用 v0.7 PlanVersion.supersedes self FK 加补充修订版本（绝不新建 PlanRevision 模型），改仓库关联经 Phase 88 RepoAssociationService 写收口（INV-6）"
    - "补充修订 = TechnicalPlanService 加新 PlanVersion(supersedes=当前版本)；content_hash 相等不翻版本（v0.3/v0.6 铁律，service 已处理），修订回路复用既有版本链不旁路"
    - "「调研问题发现」检测经 plan_revision LLM（89-01 已注册 call_source=plan_revision）：判定要改/增/删哪些仓 → 同步 RepoAssociationService（增 → propose/confirm 新仓关联、删 → reopen/移除、改 → 重深验），多轮优雅（保持 waiting）"
    - "卡片回调状态机逐字镜像 Phase 87/88：@register_card_callback 前缀唯一 + 同步轻 ack + _run_in_thread + bind_task_context(callback.user_open_id) + 澄清/重算保持 waiting、确认补充修订 approve_node；action_value 仅携路由 ID 不携方案/仓库正文"
    - "全程 fail-soft：检测/发卡/修订/关联同步任一失败不反噬飞书主响应；非 waiting 态幂等 no-op；状态写入一律经 service（INV-6），回调零旁路写表"
  artifacts:
    - path: "server/feishu/callbacks/plan_revision_callback.py"
      provides: "@register_card_callback('plan_revise_') 修订回路 FSM（调研问题发现/确认补充修订/重算）"
      contains: "register_card_callback"
    - path: "server/feishu/cards/plan_revision_card.py"
      provides: "「调研问题发现」卡片 + 补充修订确认卡构建"
      contains: "build_plan_revision_card"
    - path: "server/initiatives/services/plan_deepen_service.py"
      provides: "PlanDeepenService.{detect_revision, apply_supplement_revision} 修订回路方法（EXTEND）"
      contains: "apply_supplement_revision"
  key_links:
    - from: "server/initiatives/services/plan_deepen_service.py::PlanDeepenService.apply_supplement_revision"
      to: "server/delivery/services/technical_plan_service.py::TechnicalPlanService"
      via: "加新 PlanVersion(supersedes) 补充修订（写收口 INV-6）"
      pattern: "TechnicalPlanService"
    - from: "server/initiatives/services/plan_deepen_service.py::PlanDeepenService.detect_revision"
      to: "server/initiatives/services/repo_association_service.py::RepoAssociationService"
      via: "同步改/增/删仓库关联（写收口 INV-6）"
      pattern: "RepoAssociationService"
    - from: "server/feishu/callbacks/plan_revision_callback.py::handle_plan_revision_action"
      to: "server/initiatives/services/plan_deepen_service.py::PlanDeepenService.apply_supplement_revision"
      via: "卡片确认 → 补充修订 + 关联同步 → approve_node"
      pattern: "apply_supplement_revision"
---

<objective>
交付 Phase 89 PLAN-02 方案修订回路：执行中发现要改/增/删仓库 → 发「调研问题发现」卡片 → 用户确认后**更新方案/创建补充修订**（复用 `PlanVersion.supersedes` 加版本，绝不新建模型）+ **同步改仓库关联**（经 88 `RepoAssociationService` 写收口），多轮优雅处理。卡片状态机逐字镜像 Phase 87/88（`@register_card_callback` + `_run_in_thread` + `bind_task_context`），`plan_revision` 检测 LLM 复用 89-01 已注册 `call_source`。

锁定决策（CONTEXT/RESEARCH）：补充修订 = 新 `PlanVersion`（supersedes）；改仓库关联 = 88 service；多轮卡片 fail-soft；写入收口 INV-6；脱敏 + `initiated_by_user_id`。

Output: `PlanDeepenService.{detect_revision, apply_supplement_revision}` + 修订卡片 + 回调 FSM + 单测。
</objective>

<execution_context>
@.planning/phases/89-tech-plan-branch/89-CONTEXT.md
@.planning/phases/89-tech-plan-branch/89-RESEARCH.md
@.planning/observability/LOGGING-SPEC.md
</execution_context>

<context>
@.planning/ROADMAP.md
@server/delivery/models/technical_plan.py
@server/delivery/services/technical_plan_service.py
@server/initiatives/services/repo_association_service.py
@server/feishu/callbacks/repo_association_callback.py
@server/feishu/cards/repo_association_card.py
@server/agents/call_source.py
@.cursor/rules/observability-logging.mdc
</context>

<artifacts_produced>
- EXTEND `server/initiatives/services/plan_deepen_service.py`：
  - `async detect_revision(*, session_or_plan, observed_change_text, initiated_by_user_id) -> dict`：经 `use_call_source(CallSource.PLAN_REVISION)` LLM 判定执行中观测（observed_change_text，来自编码容器问答/人工）要改/增/删哪些仓 + 修订要点；返回结构化 `{add_repos[], remove_repos[], change_repos[], plan_delta_summary}`（脱敏，`arecord_llm_usage`）。best-effort。
  - `async apply_supplement_revision(*, plan, revision, actor, initiated_by_user_id) -> PlanVersion`：① 经 `TechnicalPlanService` 加新 `PlanVersion(supersedes=current)`，content = 当前 content + plan_delta（content_hash 相等则 service 不翻版本，幂等）；② 同步仓库关联——`add_repos` → `RepoAssociationService` 新增 propose/confirm（按需重深验）、`remove_repos` → `reopen_candidates`/移除、`change_repos` → 标记重深验；③ 结构化事件 `plan_revision_proposed/_applied`（caller, component=plan_deepen, duration_ms）。写入全经 service（INV-6），不旁路。
- NEW `server/feishu/cards/plan_revision_card.py`：`build_plan_revision_card`（「调研问题发现」问询卡：列出检测到的改/增/删仓 + 修订摘要 + 确认/调整/取消按钮）+ `build_plan_revision_done_card`（补充修订完成卡）。action_value 仅 execution_id/node_id/round/action，不携正文。
- NEW `server/feishu/callbacks/plan_revision_callback.py`：`@register_card_callback("plan_revise_")`（前缀唯一，grep 确认不撞 board_split_/repo_assoc_/chat_question_/branch_confirm_）。`handle_plan_revision_action`：同步入口即时轻 ack（3s 内）；重活 `_run_in_thread` + `bind_task_context(user_id=callback.user_open_id)`。动作 FSM：
  - `plan_revise_confirm` → `apply_supplement_revision`（加版本 + 关联同步）→ 终态卡 → `approve_node`（恢复）；
  - `plan_revise_adjust` → 带用户调整重 `detect_revision` → round+1 重发卡（保持 waiting）；
  - `plan_revise_cancel` → 不修订，保持原方案 → approve_node / no-op。
  - 全段 try/except fail-soft（失败记 `plan_revision_failed` 脱敏，不反噬飞书响应）；非 waiting 幂等 no-op。
- EXTEND `server/feishu/urls.py`：import `plan_revision_callback`（触发 `@register_card_callback` 注册）。
- NEW `server/tests/feishu/test_plan_revision_callback.py`：同步入口（confirm/adjust/cancel + 未知动作）、confirm → apply_supplement_revision 被调 + approve_node、adjust 保持 waiting（不 approve + round+1）、fail-soft（service 抛 → ack 仍返回 + 记 failed）、前缀唯一。
- NEW `server/tests/initiatives/test_plan_revision_service.py`：detect_revision 用 `use_call_source(plan_revision)`（seam 断言）、apply_supplement_revision 加 PlanVersion(supersedes)（mock TechnicalPlanService）、content 相等不翻版本（幂等）、add/remove/change → RepoAssociationService 对应方法被调。
- structlog：`plan_revision_card_action`(caller)、`plan_revision_proposed`/`_applied`/`_failed`（caller, component=plan_deepen, duration_ms，失败脱敏）；归因 `callback.user_open_id`（bind_task_context re-bind），系统 `system`。
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: PlanDeepenService.detect_revision + apply_supplement_revision（PlanVersion.supersedes + 88 关联同步，INV-6）</name>
  <read_first>
    - server/delivery/models/technical_plan.py（PlanVersion.supersedes self FK + content/content_hash + unique(plan,version)）
    - server/delivery/services/technical_plan_service.py（加版本 / content_hash 相等不翻版本契约）
    - server/initiatives/services/repo_association_service.py（confirm_repos/reopen_candidates/get_verified_associations 等写收口方法签名）
    - server/initiatives/services/plan_deepen_service.py（89-01 产物：deepen + 服务风格）
    - server/agents/call_source.py（PLAN_REVISION 已 89-01 注册）
  </read_first>
  <action>
    在 `plan_deepen_service.py` 扩 `detect_revision`（`use_call_source(CallSource.PLAN_REVISION)` LLM 判 add/remove/change 仓 + plan_delta，脱敏 + `arecord_llm_usage` + best-effort）与 `apply_supplement_revision`（经 `TechnicalPlanService` 加 `PlanVersion(supersedes=current)`，content_hash 相等不翻版本；按 add/remove/change 调 `RepoAssociationService` 对应写收口方法同步关联）。结构化事件 + duration_ms + 归因。所有写经 service（INV-6），async ORM 经 sync_to_async。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/initiatives/test_plan_revision_service.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `apply_supplement_revision` 经 `TechnicalPlanService` 加 `PlanVersion(supersedes=...)`（grep 无旁路写 PlanVersion）；content 相等不翻版本（测试断言幂等）。
    - add/remove/change 分别触发 `RepoAssociationService` 对应方法（测试 mock 断言）。
    - `detect_revision` 包 `use_call_source(CallSource.PLAN_REVISION)` + `arecord_llm_usage`。
  </acceptance_criteria>
  <done>修订回路核心：补充修订复用版本链 + 关联同步经 88 service，全 INV-6 收口。</done>
</task>

<task type="auto">
  <name>Task 2: 「调研问题发现」卡片 + 回调 FSM（镜像 87/88，前缀唯一 + fail-soft）</name>
  <read_first>
    - server/feishu/callbacks/repo_association_callback.py（@register_card_callback + _run_in_thread + bind_task_context + 同步 ack + approve_node/保持 waiting 逐字模板）
    - server/feishu/cards/repo_association_card.py（CardKit 卡 + action_value 仅路由 ID 范式）
    - server/feishu/urls.py（callback import 注册位置）
    - server/initiatives/services/plan_deepen_service.py（Task 1 产物：detect_revision/apply_supplement_revision）
  </read_first>
  <action>
    新建 `plan_revision_card.py`（`build_plan_revision_card` 列改/增/删仓 + 修订摘要 + confirm/adjust/cancel；`build_plan_revision_done_card`）+ `plan_revision_callback.py`（`@register_card_callback("plan_revise_")`，三动作 FSM：confirm→apply_supplement_revision→approve_node；adjust→detect_revision 重算 round+1 保持 waiting；cancel→保持原方案）。同步轻 ack + `_run_in_thread` + `bind_task_context`；全段 try/except fail-soft（失败记 `plan_revision_failed` 脱敏不反噬）；非 waiting 幂等 no-op；action_value 仅路由 ID。`feishu/urls.py` import 触发注册。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/feishu/test_plan_revision_callback.py -x -q && cd server && grep -rn "plan_revise_" feishu/ | head</automated>
  </verify>
  <acceptance_criteria>
    - `@register_card_callback("plan_revise_")` 前缀唯一（grep 不撞既有前缀）；urls.py 已 import。
    - confirm → `apply_supplement_revision` 被调 + `approve_node`；adjust → 保持 waiting（不 approve，round+1）；cancel → 不修订。
    - service 抛异常 → 同步 ack 仍返回 + 记 `plan_revision_failed`（fail-soft，测试断言）；非 waiting 幂等。
  </acceptance_criteria>
  <done>修订回路卡片 HITL 就位，多轮优雅，回调零旁路写表（写经 service）。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 用户卡片输入 → 修订/关联 | action_value 仅路由 ID；修订内容经 server 权威态 + service 写收口 |
| observed_change_text → LLM | 执行中观测文本，脱敏后入 detect_revision prompt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-02-TAMPER | Tampering | 旁路写 PlanVersion/RepoAssociation | mitigate | 写经 TechnicalPlanService/RepoAssociationService（INV-6）；回调零旁路 |
| T-89-02-DOS | DoS | 回调重入风暴 | mitigate | 非 waiting 幂等 no-op + fail-soft 不回灌 5xx |
| T-89-02-INFO | Info Disclosure | 修订正文/token 落日志 | mitigate | redact_secrets_in_text；action_value 不携正文 |
| T-89-02-SC | Tampering | npm/pip installs | accept | 本 plan 无包安装 |
</threat_model>

<verification>
- `cd server && uv run pytest tests/initiatives/test_plan_revision_service.py tests/feishu/test_plan_revision_callback.py -q` 全绿。
- grep `plan_revise_` 前缀唯一；urls.py 已 import callback。
- grep 确认 apply_supplement_revision 经 TechnicalPlanService（无旁路写 PlanVersion）。
- ruff + mypy（新文件）通过。
</verification>

<success_criteria>
- 执行中发现改/增/删仓 → 「调研问题发现」卡片 → 补充修订（PlanVersion.supersedes）+ 关联同步（88 service），多轮优雅 fail-soft。
- 修订检测 LLM 用 plan_revision call_source（89-01 注册）+ 留痕脱敏。
- 写入收口 INV-6，回调零旁路写表。
</success_criteria>

<output>
完成后创建 `.planning/phases/89-tech-plan-branch/89-02-SUMMARY.md`
</output>
