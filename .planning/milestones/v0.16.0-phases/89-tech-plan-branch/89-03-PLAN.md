---
phase: 89
plan: 03
type: execute
wave: 2
depends_on: [89-01]
files_modified:
  - server/chat/models.py
  - server/chat/migrations/00XX_coding_session_suspended.py
  - server/chat/container_suspend_service.py
  - server/subagent/api/callbacks.py
  - server/feishu/callbacks/coding_question_callback.py
  - server/tests/chat/test_container_suspend.py
autonomous: true
requirements: [PLAN-03]
must_haves:
  truths:
    - "容器资源优化：单仓任务遇阻等待用户时，5 分钟无回复挂起/暂存容器（finish turn → 停容器 dispatcher.cancel + CodingSession.status=SUSPENDED + parked_at）；用户卡片回复后 resume 续到终态——复用 Phase 86 SessionStore→Redis + v0.8 callback resume + v0.12 durable，绝不重造 session 持久化"
    - "resume 经 chat/sdk_resume.build_resume_dispatch_env（SessionStore.load: Redis 镜像 → DB fallback + assert_cwd_consistent cwd 一致校验 + 分片 transcript 下发）re-dispatch DispatchTask 续跑容器到终态（ClaudeAgentOptions resume）"
    - "session 找不到兜底（官方推荐）：SessionStore.load 返回 None（Redis 失效 + DB 无 transcript / cwd 漂移）→ build_resume_dispatch_env 返回 {} → 用应用态（canonical 方案 + 用户回复）重灌新 session 全新执行（绝不静默错配他容器 transcript）"
    - "5min 计时器经 apscheduler date trigger 一次性 job（已在栈，repo sync 轮询用；多副本 django-apscheduler jobstore 去重）；容器 question 回调发卡时注册，到点未答触发挂起；用户回复先取消计时器"
    - "挂起/resume 竞态 fail-soft：CAS 状态机（RUNNING→SUSPENDED 条件更新；resume 仅 SUSPENDED/AWAITING 时执行），计时到点先读态幂等短路；停容器/计时/resume 任一失败不反噬容器回调主流程（绝不回灌 5xx 致 runner 重试风暴）"
    - "新增持久化经 service（INV-6）：CodingSession.Status.SUSPENDED + parked_at 经 ContainerSuspendService 写收口；迁移纯 AddField/AlterField 无回填"
  artifacts:
    - path: "server/chat/models.py"
      provides: "CodingSession.Status.SUSPENDED 枚举值 + parked_at 字段"
      contains: "SUSPENDED"
    - path: "server/chat/container_suspend_service.py"
      provides: "ContainerSuspendService（5min 计时注册/取消 + 挂起停容器 + resume re-dispatch + miss 重灌，INV-6 写收口）"
      contains: "class ContainerSuspendService"
    - path: "server/chat/migrations/00XX_coding_session_suspended.py"
      provides: "纯 AddField parked_at + AlterField status choices（无回填）"
      contains: "AddField"
  key_links:
    - from: "server/chat/container_suspend_service.py::ContainerSuspendService.resume"
      to: "server/chat/sdk_resume.py::build_resume_dispatch_env"
      via: "SessionStore 取 transcript 构建 resume env re-dispatch"
      pattern: "build_resume_dispatch_env"
    - from: "server/chat/container_suspend_service.py::ContainerSuspendService.suspend"
      to: "server/runners/dispatcher.py::get_dispatcher().cancel"
      via: "5min 无回复 → 停容器"
      pattern: "cancel"
    - from: "server/subagent/api/callbacks.py::_handle_question"
      to: "server/chat/container_suspend_service.py::ContainerSuspendService.arm_timeout"
      via: "容器问答发卡时注册 5min 计时"
      pattern: "arm_timeout"
---

<objective>
交付 Phase 89 PLAN-03 容器 5min 挂起/resume：单仓编码容器遇阻等待用户（`question` 回调发飞书卡）时注册 5min 计时；到点无回复 → finish turn → 停容器（`dispatcher.cancel`）+ `CodingSession.status=SUSPENDED`/`parked_at`；用户卡片回复 → 经 `build_resume_dispatch_env`（`SessionStore` Redis→DB + cwd 一致 + 分片 transcript）re-dispatch 续跑到终态；**session 找不到 → 用应用态重灌新 session**（官方推荐兜底）。复用 Phase 86 `SessionStore` + v0.8 callback resume + v0.12 durable，绝不重造。

锁定决策（CONTEXT/RESEARCH/MILESTONE §8/§9）：挂起=finish turn→停容器→回复后 SessionStore resume；miss→应用态重灌；写入收口 INV-6；fail-soft 不反噬；脱敏 + `initiated_by_user_id`。

Output: `CodingSession.SUSPENDED`+`parked_at` 迁移 + `ContainerSuspendService` + question 回调接线计时 + 回复回调 resume + 单测。
</objective>

<execution_context>
@.planning/phases/89-tech-plan-branch/89-CONTEXT.md
@.planning/phases/89-tech-plan-branch/89-RESEARCH.md
@.planning/observability/LOGGING-SPEC.md
</execution_context>

<context>
@.planning/ROADMAP.md
@server/chat/session_store.py
@server/chat/sdk_resume.py
@server/chat/coding_session_service.py
@server/chat/models.py
@server/subagent/api/callbacks.py
@server/runners/dispatcher.py
@.cursor/rules/observability-logging.mdc
</context>

<artifacts_produced>
- EXTEND `server/chat/models.py`：`CodingSession.Status` 新增 `SUSPENDED = "suspended", "已挂起"`；新增字段 `parked_at = models.DateTimeField(null=True, blank=True, verbose_name="容器挂起时间")`。模型层无业务写方法（INV-6，写经 service）。
  - **read_first 先确认承载模型（A1）**：若「单仓任务」SDK session 实际承载于 `RepoCodingTask` 而非 `CodingSession`，挂起字段落实际持有 `sdk_session_id`/`sdk_transcript` 的模型上（仍同形态 SUSPENDED + parked_at）。
- NEW `server/chat/migrations/00XX_coding_session_suspended.py`：`makemigrations chat` 生成的纯 `AddField`(parked_at) + `AlterField`(status choices)，无 `RunPython`。编号为 chat app 下一可用号（makemigrations 自动）。
- NEW `server/chat/container_suspend_service.py`：`class ContainerSuspendService`（INV-6 挂起/恢复写收口）：
  - `async arm_timeout(*, coding_session_id, task_id, initiated_by_user_id, minutes=5)`：注册 apscheduler date trigger 一次性 job（job_id=`suspend-{coding_session_id}`，run_date=now+5min）→ 到点回调 `self.suspend(...)`；幂等（同 id replace_existing）。
  - `async cancel_timeout(*, coding_session_id)`：用户回复时取消未触发的 job（不存在 no-op）。
  - `async suspend(*, coding_session_id, task_id)`：CAS RUNNING/AWAITING→SUSPENDED（条件 update，已非该态幂等短路）→ `get_dispatcher().cancel(task_id)` 停容器 → 写 `parked_at` → 结构化事件 `container_suspended`（caller, component=chat, duration_ms）；全段 fail-soft。
  - `async resume(*, coding_session, user_reply, dispatch_cwd=WORKSPACE_CWD, initiated_by_user_id)`：仅 SUSPENDED/AWAITING 时执行（CAS）→ `env = build_resume_dispatch_env(coding_session, dispatch_cwd=dispatch_cwd)`；env 非空 → re-dispatch `DispatchTask`（携 resume env + user_reply 并进 prompt）续跑（事件 `container_resumed`）；env 空（session miss / cwd 漂移）→ **应用态重灌**：用 canonical 方案 + user_reply 构建全新 `DispatchTask`（无 resume 标记，事件 `container_resume_reloaded`）。状态回 RUNNING（CAS）。绝不反噬容器回调主流程。
- EXTEND `server/subagent/api/callbacks.py`：`_handle_question`（容器问答发卡）后接线 `ContainerSuspendService().arm_timeout(...)`（best-effort，try/except 不反噬回调）。
- NEW（或 EXTEND 既有 coding 问答回调）`server/feishu/callbacks/coding_question_callback.py`：用户回复卡片 → `cancel_timeout` + `ContainerSuspendService().resume(...)`（`_run_in_thread` + `bind_task_context(callback.user_open_id)`，fail-soft）。若既有 question 回复回调已存在则 EXTEND 接线 resume，不另起前缀。
- NEW `server/tests/chat/test_container_suspend.py`：
  - `test_timeout_suspends`：arm_timeout 到点 → CAS RUNNING→SUSPENDED + dispatcher.cancel 被调 + parked_at 写入。
  - `test_reply_resumes`：用户回复 → cancel_timeout + build_resume_dispatch_env 非空 → re-dispatch 携 resume env（seam 断言）。
  - `test_session_miss_reload`：SessionStore.load→None（mock）→ build_resume_dispatch_env 返回 {} → 应用态重灌新 session（无 resume 标记，事件 reloaded）。
  - `test_suspend_resume_race`：已 SUSPENDED 再 suspend 幂等 no-op；resume 后再到点 timeout 幂等短路（CAS）。
  - `test_fail_soft`：dispatcher.cancel 抛 → suspend 吞异常不反噬。
- structlog：`container_suspended`/`container_resumed`/`container_resume_reloaded`/`container_suspend_failed`（category=caller, component=chat, duration_ms）；归因 `initiated_by_user_id`（回复取 `callback.user_open_id`，bind_task_context re-bind），系统 `system`。
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: CodingSession SUSPENDED + parked_at 迁移（确认承载模型，纯 schema 无回填）</name>
  <read_first>
    - server/chat/models.py（CodingSession.Status 332-369 + sdk_session_id/sdk_transcript 字段；确认 SDK session 承载模型 A1——若 RepoCodingTask 承载则落该模型）
    - server/chat/migrations/（最近迁移编号，确认下一可用号）
    - .planning/phases/85-context-read-branch-binding/85-03-PLAN.md（迁移纯 CreateModel/AddField 无回填范式）
  </read_first>
  <action>
    在实际承载 SDK session 的模型（默认 `CodingSession`，A1 read_first 确认）的 `Status` 末尾加 `SUSPENDED = "suspended", "已挂起"`，新增 `parked_at = DateTimeField(null=True, blank=True)`。`cd server && uv run python manage.py makemigrations chat` 生成纯 `AddField`(parked_at) + `AlterField`(status choices) 迁移，无 RunPython。`makemigrations --check --dry-run` 干净。模型层不加业务写方法（写经 ContainerSuspendService）。
  </action>
  <verify>
    <automated>cd server && uv run python manage.py makemigrations --check --dry-run && uv run python -c "import django; django.setup(); from chat.models import CodingSession; assert CodingSession.Status.SUSPENDED=='suspended'"</automated>
  </verify>
  <acceptance_criteria>
    - `CodingSession.Status.SUSPENDED == "suspended"`（或承载模型对应枚举）；`parked_at` 字段存在。
    - `makemigrations --check --dry-run` 退出 0；迁移含 AddField/AlterField，无 RunPython。
  </acceptance_criteria>
  <done>挂起态 schema 落地（纯新增无回填），写收口待 ContainerSuspendService。</done>
</task>

<task type="auto">
  <name>Task 2: ContainerSuspendService —— 5min 计时 + 挂起停容器 + resume + miss 重灌（复用 86/v0.8/v0.12，INV-6）</name>
  <read_first>
    - server/chat/session_store.py（SessionStore.load/mirror/assert_cwd_consistent + WORKSPACE_CWD）
    - server/chat/sdk_resume.py（build_resume_dispatch_env：env 空语义 = miss/漂移/超限回退新 session）
    - server/chat/coding_session_service.py（dispatch metadata 构建 + build_resume_dispatch_env 接线 393-399 范式）
    - server/runners/dispatcher.py（get_dispatcher().dispatch(DispatchTask) + cancel(task_id) 179）
    - server/friday/settings.py（apscheduler 配置 / 既有 repo sync date/interval trigger 用法）
  </read_first>
  <action>
    新建 `container_suspend_service.py::ContainerSuspendService`：`arm_timeout`（apscheduler date trigger 一次性 job，job_id 幂等 replace_existing）、`cancel_timeout`、`suspend`（CAS RUNNING/AWAITING→SUSPENDED + `dispatcher.cancel` + parked_at + 事件 + fail-soft）、`resume`（CAS → `build_resume_dispatch_env`；非空 re-dispatch 携 resume + user_reply、空走应用态重灌新 session）。全程 `structlog` + duration_ms + 归因 + 脱敏（user_reply/token 不明文）；写态经本 service（INV-6）；async ORM sync_to_async；绝不反噬主流程。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/chat/test_container_suspend.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `suspend` CAS RUNNING/AWAITING→SUSPENDED + `dispatcher.cancel` 被调 + parked_at 写（测试断言）；已 SUSPENDED 幂等。
    - `resume` env 非空 → re-dispatch 携 resume env；env 空 → 应用态重灌新 session（无 resume 标记）。
    - 竞态：resume 后 timeout 到点幂等短路；任一外部调用抛 → fail-soft 吞异常（测试断言）。
  </acceptance_criteria>
  <done>容器挂起/resume 编排收口，复用 SessionStore/sdk_resume/dispatcher，miss 应用态重灌兜底。</done>
</task>

<task type="auto">
  <name>Task 3: 接线 question 回调注册计时 + 回复回调 resume（best-effort fail-soft）</name>
  <read_first>
    - server/subagent/api/callbacks.py（_handle_question 发卡逻辑 + heartbeat + node_execution 续驱位置）
    - server/feishu/callbacks/（既有 coding 问答回复回调 / chat_question 前缀；优先 EXTEND 不另起前缀）
    - server/chat/container_suspend_service.py（Task 2 产物）
    - server/common/log_context.py（bind_task_context 归因）
  </read_first>
  <action>
    `subagent/api/callbacks.py` 的 `_handle_question` 发卡后接线 `ContainerSuspendService().arm_timeout(coding_session_id, task_id, initiated_by_user_id, minutes=5)`（try/except best-effort 不反噬回调）。用户回复卡片回调（EXTEND 既有 coding question 回复回调，或新建 `coding_question_callback.py`）：`cancel_timeout` + `_run_in_thread(ContainerSuspendService().resume(...))` + `bind_task_context(callback.user_open_id)`，fail-soft。绝不在回调路径上抛 5xx（防 runner 重试风暴）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/chat/test_container_suspend.py tests/subagent -k "question or suspend or resume" -q 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `_handle_question` 后调 `arm_timeout`（grep + 测试断言），异常不反噬回调（fail-soft）。
    - 用户回复 → `cancel_timeout` + `resume` 被调（_run_in_thread + bind_task_context）。
    - 回调路径异常吞掉不回灌 5xx（测试断言）。
  </acceptance_criteria>
  <done>容器问答 → 5min 计时 → 挂起/回复 resume 全链接通，复用既有回调入口，fail-soft。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 容器回调 → 挂起/resume | node_execution_id/session_id 服务端权威；resume 经 SessionStore cwd 一致校验 |
| 用户回复 → 容器 prompt | user_reply 脱敏后并进 prompt，不裸拼执行指令 |
| git token / transcript | dispatch env 注入，绝不入日志 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-03-TAMPER | Tampering | resume 错配他容器 transcript | mitigate | SessionStore.assert_cwd_consistent，漂移回退新 session（绝不静默错配） |
| T-89-03-DOS | DoS | 回调重入/竞态触发风暴 | mitigate | CAS 状态机幂等短路 + fail-soft 不回灌 5xx |
| T-89-03-INFO | Info Disclosure | transcript/token/user_reply 落日志 | mitigate | redact_secrets_in_text；token 仅 has_git_token 布尔 |
| T-89-03-SC | Tampering | npm/pip installs | accept | 本 plan 无包安装（apscheduler 已在栈） |
</threat_model>

<verification>
- `cd server && uv run pytest tests/chat/test_container_suspend.py -q` 全绿（含 timeout 挂起 / reply resume / session miss 重灌 / 竞态幂等 / fail-soft）。
- `makemigrations --check --dry-run` 干净；迁移纯 AddField/AlterField 无回填。
- grep 确认 resume 经 build_resume_dispatch_env、suspend 经 dispatcher.cancel；写态经 ContainerSuspendService（无旁路写 status）。
- ruff + mypy（新文件）通过。
</verification>

<success_criteria>
- 单仓容器遇阻 5min 无回复 → 停容器 + SUSPENDED + parked_at（finish turn 释放资源）。
- 用户回复 → SessionStore resume re-dispatch 续到终态；session miss → 应用态重灌新 session。
- 复用 86 SessionStore + v0.8 callback resume + v0.12 durable/apscheduler，全 fail-soft 不反噬。
- 写入收口 INV-6，脱敏 + 归因。
</success_criteria>

<output>
完成后创建 `.planning/phases/89-tech-plan-branch/89-03-SUMMARY.md`
</output>
