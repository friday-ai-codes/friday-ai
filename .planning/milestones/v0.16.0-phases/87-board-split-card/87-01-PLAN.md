---
phase: 87
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/services/feishu.py
  - server/tests/services/test_feishu_create_work_item.py
  - .planning/phases/87-board-split-card/87-UAT.md
autonomous: true
requirements: [BOARD-01]
deferred_uat:
  - service: feishu
    why: "work_item/create 写 API + 父子/关联(relation_type=1) 写端点形态、鉴权(plugin_token+user_key)、relation 类型是否需配置中心预配，Phase 78 仅验证读、写从未真机跑通；自动链路以 respx + [ASSUMED] seam 覆盖，真机写验证 deferred 记 87-UAT.md（autonomous 模式不打断）"
    checks:
      - "真实空间用 plugin_token+user_key 调 work_item/create 建一个 story，确认端点路径/请求体/返回 id 字段名（A-CREATE）"
      - "对新建 work_item 写 relation_type=1 关联项目跟踪，确认 relation 写端点 + 是否需预配关系类型（A-REL）"
      - "确认父子关系类型未配置时的真实错误码，回填降级判定阈值（A-DEGRADE）"
must_haves:
  truths:
    - "FeishuClient.create_work_item 经 plugin_token + user_key 调飞书 work_item/create，建一个 work_item 并返回新 work_item_id；非 0 err_code fail-loud 抛 + 脱敏"
    - "FeishuClient.add_work_item_relation 写 relation_type=1（关联项目跟踪）与父子关系；写失败 fail-loud 抛带脱敏 body 片段"
    - "FeishuClient.detect_relation_capability 探测父子/关联关系类型是否在空间配置；缺失返回结构化能力位供上层降级（绝不抛、绝不阻断建看板）"
    - "所有写 API 的真实端点/请求体标 [ASSUMED]，autonomous 路径以 respx 覆盖，真机写验证 deferred 记 87-UAT.md"
  artifacts:
    - path: "server/services/feishu.py"
      provides: "FeishuClient.create_work_item / add_work_item_relation / detect_relation_capability 三个写/探测方法"
      contains: "create_work_item"
    - path: "server/tests/services/test_feishu_create_work_item.py"
      provides: "respx mock 覆盖建项/写关系/能力探测的成功+失败+降级路径"
      contains: "create_work_item"
    - path: ".planning/phases/87-board-split-card/87-UAT.md"
      provides: "写 API live 验证 deferred 清单（A-CREATE/A-REL/A-DEGRADE）"
      contains: "A-CREATE"
  key_links:
    - from: "server/services/feishu.py"
      to: "https://project.feishu.cn/open_api/{project_key}/work_item/{work_item_type}/create"
      via: "create_work_item POST（X-PLUGIN-TOKEN + X-USER-KEY，镜像 update_work_item_fields 鉴权骨架）"
      pattern: "work_item/.*?/create"
---

<objective>
交付 BOARD-01 的飞书写 API 地基：在 `FeishuClient`（`server/services/feishu.py`）新增「建工作项 / 写关系 / 探测关系能力」三个方法，为后续看板拆分（每 feature 一个子看板 work_item + `relation_type=1` 关联项目跟踪 + 可选父子）提供底层调用。

Phase 78 只验证了飞书项目 API 的**读**（get_work_item / query / relation 读），**写 API 从未真机跑通**。本计划遵循「实现 + respx + [ASSUMED] seam」范式（镜像 83-02）：autonomous 路径全部以 respx mock 覆盖端点契约，真机写验证（建项/写关系/关系类型预配）**deferred 记 `87-UAT.md`**，autonomous 模式下不打断、不询问。

Purpose: 让 87-03 看板拆分服务可直接调用「建子看板 + 关联项目跟踪 + 父子（缺则降级）」，写 API 的真实形态先 [ASSUMED]、经 UAT 固化。
Output: `create_work_item` / `add_work_item_relation` / `detect_relation_capability` + respx 测试 + 87-UAT.md deferred 清单。
</objective>

<execution_context>
@.planning/phases/87-board-split-card/87-CONTEXT.md
@.planning/project-workspace/MILESTONE-PROPOSAL.md
</execution_context>

<context>
@.planning/PROJECT.md
@server/services/feishu.py
@server/services/feishu_parsing.py
@.cursor/rules/observability-logging.mdc
</context>

<artifacts_produced>
- EXTEND `server/services/feishu.py`：
  - `FeishuClient.create_work_item(self, project_key, work_item_type, name, *, description="", template_id=None, extra_fields=None) -> int`（POST `work_item/{type}/create`，返回新 work_item_id；非 0 err_code 经 `strict_response_json` fail-loud）。
  - `FeishuClient.add_work_item_relation(self, project_key, work_item_type, work_item_id, *, relation_type, target_id, target_type=None) -> bool`（写 `relation_type=1` 关联项目跟踪 / 父子关系）。
  - `FeishuClient.detect_relation_capability(self, project_key, work_item_type) -> dict`（探测空间是否配置父子/关联关系类型；返回 `{"parent_child": bool, "project_track": bool, "raw": ...}`，fail-soft 不抛）。
- NEW `server/tests/services/test_feishu_create_work_item.py`（respx：建项成功/err_code 非 0 抛/脱敏；写关系成功/失败；探测命中/缺失/解析失败 fail-soft）。
- NEW `.planning/phases/87-board-split-card/87-UAT.md`（A-CREATE/A-REL/A-DEGRADE deferred 清单）。
- structlog 事件：`feishu_work_item_create_started/_completed/_failed`(caller, component=feishu, +duration_ms)、`feishu_relation_capability_probed`(sampling, debug)。
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: create_work_item + add_work_item_relation（写 API，[ASSUMED] 端点 + 脱敏 + fail-loud）</name>
  <read_first>
    - server/services/feishu.py（get_plugin_token 行 74-116；update_work_item_fields 写骨架 行 206-266；get_work_item_relations 读关系 行 268-333；strict/safe_response_json 用法）
    - server/services/feishu_parsing.py（strict_response_json / safe_response_json 签名与脱敏行为）
    - .cursor/rules/observability-logging.mdc（started/completed/failed + duration_ms + category/component；上游响应体脱敏）
  </read_first>
  <action>
    在 `FeishuClient` 新增 `create_work_item`：复用 `get_plugin_token()` + `X-PLUGIN-TOKEN`/`X-USER-KEY`/`Content-Type` 头骨架（与 `update_work_item_fields` 一致）；POST `f"{self.PROJECT_API_BASE}/open_api/{project_key}/work_item/{work_item_type}/create"`，请求体 `{"name": name, "field_value_pairs": [...]}`（description 经 `_markdown_to_rich_text` 落 description 字段；extra_fields 合并）；端点路径/请求体字段名加 `# [ASSUMED] A-CREATE` 注释。响应经 `strict_response_json`（非 JSON fail-loud），`err_code != 0` 抛 `Exception`（消息走脱敏 helper，绝不拼明文 token）；成功取新 id（`data.id` 或 `data.work_item_id`，[ASSUMED]）返回 int。
    新增 `add_work_item_relation`：POST `work_item/{type}/{id}/relation`（[ASSUMED] A-REL），请求体含 `relation_type`/`target_id`/`target_type`；成功 `err_code==0` 返回 True，失败 fail-loud 抛带脱敏 body。
    两方法包 `structlog` 生命周期事件 `feishu_work_item_create_started/_completed/_failed`（category=caller, component=feishu, +duration_ms），字段只记 project_key/work_item_type/name 长度，绝不记 token / 完整请求体。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/test_feishu_create_work_item.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `server/services/feishu.py` 含 `def create_work_item(` 与 `def add_work_item_relation(`。
    - respx：create_work_item 返回 `err_code==0` + data.id → 返回该 int；`err_code!=0` → 抛 Exception 且异常文本不含明文 token（断言）。
    - respx：add_work_item_relation `err_code==0`→True；非 0→抛。
    - grep 确认两方法无明文 token 入日志（日志字段仅 project_key/类型/长度）。
  </acceptance_criteria>
  <done>create_work_item / add_work_item_relation 写 API 落地，[ASSUMED] 端点 + 脱敏 + fail-loud，respx 绿。</done>
</task>

<task type="auto">
  <name>Task 2: detect_relation_capability（父子/关联关系类型探测 + fail-soft 降级位）</name>
  <read_first>
    - server/services/feishu.py（get_work_item_relations safe_response_json fail-soft 降级 行 268-333）
    - server/services/feishu_parsing.py（safe_response_json expect=dict 用法）
  </read_first>
  <action>
    新增 `detect_relation_capability(project_key, work_item_type) -> dict`：GET 空间关系类型配置端点（[ASSUMED] A-DEGRADE，候选 `work_item/{type}/meta` 或 `/relation/type`，无法确定时退化为「调一次 get_work_item_relations 风格的探测 + 解析 err_code」），经 `safe_response_json(expect=dict)` fail-soft；解析失败/非 dict/err_code 非 0 → 返回保守 `{"parent_child": False, "project_track": True, "raw": None}`（关联项目跟踪默认可用、父子默认不可用，宁可降级不可误挂）。命中配置则置位对应布尔。记 `feishu_relation_capability_probed`(sampling, debug, component=feishu)。绝不抛。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/test_feishu_create_work_item.py -x -q -k capability</automated>
  </verify>
  <acceptance_criteria>
    - `detect_relation_capability` 返回 dict 含 `parent_child`/`project_track`/`raw` 键。
    - respx：配置含父子关系类型→`parent_child=True`；解析失败/err_code 非 0→`parent_child=False` 且不抛（断言无异常冒泡）。
  </acceptance_criteria>
  <done>关系能力探测 fail-soft，返回结构化降级位供 87-03 决定是否挂父子。</done>
</task>

<task type="auto">
  <name>Task 3: 写 87-UAT.md（deferred live 验证清单，autonomous 不打断）</name>
  <read_first>
    - .planning/phases/83-feishu-doc-bi-sync-engine/83-02-PLAN.md（live-Feishu UAT 检查点写法参考）
  </read_first>
  <action>
    新建 `.planning/phases/87-board-split-card/87-UAT.md`，记录三项 deferred live 验证：A-CREATE（work_item/create 真实端点/请求体/返回 id 字段名）、A-REL（relation_type=1 关联项目跟踪 + 父子写端点、是否需配置中心预配关系类型）、A-DEGRADE（父子关系未配的真实错误码 → 回填降级阈值）。每项写「如何验证（真实凭证 + 真实空间）」「待回填 [ASSUMED]→[VERIFIED] 项」。标注 autonomous 模式下本期不阻断、执行后人工补跑。
  </action>
  <verify>
    <automated>test -f .planning/phases/87-board-split-card/87-UAT.md && grep -q "A-CREATE" .planning/phases/87-board-split-card/87-UAT.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - 87-UAT.md 存在且含 A-CREATE / A-REL / A-DEGRADE 三项及验证步骤。
  </acceptance_criteria>
  <done>写 API live 验证 deferred 固化为 87-UAT.md，不阻断 autonomous 链路。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Friday → 飞书项目写 API | 建工作项/写关系会真实改飞书项目数据 |
| 飞书写响应 → 日志/异常 | 半可信，可能回显含敏感片段 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-01-INFO | Information Disclosure | 写 API 日志/异常 | mitigate | 上游响应体/异常经脱敏 helper；plugin_token 绝不入日志，日志仅记 project_key/类型/长度 |
| T-87-01-TAMPER | Tampering | 误建/误挂关系 | mitigate | create/relation fail-loud 抛由上层（87-03）幂等 + ProjectWorkItemLink 去重控制；能力探测缺失即降级不强挂父子 |
| T-87-01-DOS | Denial of Service | 批量建项触发飞书频控 | mitigate | 87-03 串行 + 退避（本计划方法单次调用、不并发） |
</threat_model>

<verification>
- `cd server && uv run pytest tests/services/test_feishu_create_work_item.py -q` 全绿。
- grep `server/services/feishu.py` 确认三方法存在、无明文 token 入日志；[ASSUMED] 注释齐备。
- 87-UAT.md 存在含三项 deferred 清单。
</verification>

<success_criteria>
- FeishuClient 具备建子看板（create_work_item）+ 写 relation_type=1 关联项目跟踪/父子（add_work_item_relation）+ 关系能力探测（detect_relation_capability）三能力。
- autonomous 路径以 respx 全覆盖；写 API 真机验证 deferred 记 87-UAT.md，不打断、不询问。
- 脱敏不可绕过、fail-loud（写）/ fail-soft（探测）边界清晰。
</success_criteria>

<output>
完成后创建 `.planning/phases/87-board-split-card/87-01-SUMMARY.md`
</output>
