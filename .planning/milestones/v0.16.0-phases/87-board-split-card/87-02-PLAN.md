---
phase: 87
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - server/agents/call_source.py
  - server/initiatives/services/feature_list_extractor.py
  - server/tests/initiatives/test_feature_list_extractor.py
  - .planning/observability/LOGGING-SPEC.md
autonomous: true
requirements: [BOARD-01]
must_haves:
  truths:
    - "多源输入归一化为统一原文：文件上传(md 文本) + 飞书文档链接(经 fetch_feishu_document 回拉正文) + 粘贴文本，三源任一缺失/失败 fail-soft 各自降级，至少一源即可继续"
    - "结构化抽取：feature list 原文 → {modules:[{name, features:[{name, description(原文片段), acceptance:[...]}]}]}，每个 feature 一条（看板拆分粒度=每 feature 一子看板）"
    - "feature list 过大(82KB demo)→ 按模块/标题分块 + token 预算降级（超预算时降级为粗粒度抽取/分批，绝不整篇塞爆 LLM 上下文）"
    - "抽取走 LLM，赋新 call_source `board_split`（call_source.py 枚举 + LOGGING-SPEC §4.1 登记）并上报 LLM 请求数/token/TTFT/上游错误码"
  artifacts:
    - path: "server/agents/call_source.py"
      provides: "CallSource.BOARD_SPLIT = 'board_split' 新枚举值"
      contains: "BOARD_SPLIT"
    - path: "server/initiatives/services/feature_list_extractor.py"
      provides: "FeatureListExtractor：normalize_sources + extract_structure（分块/token 预算/LLM 抽取/call_source）"
      exports: ["FeatureListExtractor"]
    - path: "server/tests/initiatives/test_feature_list_extractor.py"
      provides: "多源归一化 + 抽取(FakeChatModel seam) + 分块降级 + 82KB demo 冒烟"
      contains: "feature-list-demo"
    - path: ".planning/observability/LOGGING-SPEC.md"
      provides: "§4.1 call_source 表新增 board_split 行（枚举数 24→25）"
      contains: "board_split"
  key_links:
    - from: "server/initiatives/services/feature_list_extractor.py"
      to: "agents.tools.feishu_doc_tools.fetch_feishu_document / services.feishu_doc.FeishuDocClient.get_document_content"
      via: "飞书链接源经 Phase 83 回拉正文（document_id 从 URL 提取）"
      pattern: "fetch_feishu_document|get_document_content"
    - from: "server/initiatives/services/feature_list_extractor.py"
      to: "agents.call_source.use_call_source(CallSource.BOARD_SPLIT)"
      via: "LLM 抽取 ainvoke 外层声明 call_source，chokepoint 上报指标"
      pattern: "use_call_source"
---

<objective>
交付 BOARD-01 的「输入 + 拆分智能」一半：把 feature list 多源输入归一化为统一原文，并用 LLM 做结构化抽取（模块→功能点→验收项），输出每个 feature 一条的拆分结构，供 87-03 落子看板。

锁定决策（CONTEXT）：feature list 输入为**多源**——文件上传(md) + 飞书文档链接(回拉正文，复用 Phase 83) + 粘贴文本；feature list 过大（82KB demo）→ 结构化抽取 + 分块 + token 预算降级。新增 LLM（看板拆分）赋 `call_source`（LOGGING-SPEC §4.1 登记新值）+ 上报请求/token/TTFT/上游错误码。

Purpose: 把"杂乱多源 feature list"变成"可逐 feature 建看板的结构化拆分"，并把这次 LLM 调用纳入可观测体系。
Output: `CallSource.BOARD_SPLIT` + `FeatureListExtractor`（归一化 + 分块 + token 预算 + LLM 抽取）+ LOGGING-SPEC §4.1 登记 + 测试。
</objective>

<execution_context>
@.planning/phases/87-board-split-card/87-CONTEXT.md
@.planning/feature-list-demo.md
@.planning/observability/LOGGING-SPEC.md
</execution_context>

<context>
@.planning/PROJECT.md
@server/agents/call_source.py
@server/agents/tools/feishu_doc_tools.py
@server/services/feishu_doc.py
@server/workflows/nodes/ai/prompt.py
@server/agents/llm_factory.py
@.cursor/rules/observability-logging.mdc
</context>

<artifacts_produced>
- EXTEND `server/agents/call_source.py`：`CallSource.BOARD_SPLIT = "board_split"`（更新模块 docstring 枚举计数 + normalize 自动覆盖）。
- EXTEND `.planning/observability/LOGGING-SPEC.md`：§4.1 表新增 `| board_split | FeatureListExtractor / BoardSplitService（v0.16.0 Phase 87） | feature list 结构化抽取/重拆，单轮，分块可多次 |`，并更新枚举计数注释。
- NEW `server/initiatives/services/feature_list_extractor.py`：
  - `FeatureListExtractor.normalize_sources(*, uploaded_text=None, feishu_url=None, pasted_text=None, space_id=None) -> str`（三源合并为统一原文；飞书链接经 `fetch_feishu_document`/`get_document_content` 回拉；各源 fail-soft 降级）。
  - `FeatureListExtractor.extract_structure(raw_text, *, space, initiated_by_user_id=None) -> dict`（分块 + token 预算降级 + `use_call_source(CallSource.BOARD_SPLIT)` 包裹 `build_chat_model().ainvoke`；返回 `{modules:[...], features_flat:[...], degraded:bool}`）。
- NEW `server/tests/initiatives/test_feature_list_extractor.py`（FakeChatModel seam；82KB demo 冒烟分块；飞书源 respx；token 预算降级）。
- structlog 事件：`feature_list_normalize_completed`(caller, component=board_split, +duration_ms, source_count)、`feature_list_extract_started/_completed/_failed`(caller, +duration_ms, chunk_count, degraded)、`feature_list_chunk_extracted`(sampling, debug)。正文/异常经 `redact_secrets_in_text`。
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: 登记 call_source board_split（枚举 + LOGGING-SPEC §4.1）</name>
  <read_first>
    - server/agents/call_source.py（CallSource 枚举 + docstring 计数 + normalize 行 32-81）
    - .planning/observability/LOGGING-SPEC.md（§4.1 表 行 62-92 + 4.2 指标名）
    - server/tests/test_model_usage_call_source.py（如存在，确认枚举消费方）
  </read_first>
  <action>
    `call_source.py`：在枚举末尾新增 `BOARD_SPLIT = "board_split"`（带 Phase 87 注释），更新模块/类 docstring 的「24 值」计数为「25 值」。`LOGGING-SPEC.md` §4.1 表追加 board_split 行（入口=FeatureListExtractor / BoardSplitService），并把 §4.1 标题处「枚举数」与 call_source.py 计数同步。
  </action>
  <verify>
    <automated>cd server && uv run python -c "import django; django.setup(); from agents.call_source import CallSource; assert CallSource.normalize('board_split')=='board_split'"</automated>
  </verify>
  <acceptance_criteria>
    - `CallSource.BOARD_SPLIT.value == "board_split"`；`normalize('board_split')` 命中（不回退 unknown）。
    - LOGGING-SPEC §4.1 表含 `board_split` 行；计数注释一致。
  </acceptance_criteria>
  <done>board_split 受控枚举登记，可作为 ModelUsageRecord/指标标签。</done>
</task>

<task type="auto">
  <name>Task 2: 多源输入归一化 normalize_sources（文件 + 飞书链接回拉 + 文本，fail-soft）</name>
  <read_first>
    - server/agents/tools/feishu_doc_tools.py（fetch_feishu_document + _extract_document_id + create_feishu_doc_client_for_project 行 47-189/317-333）
    - server/services/feishu_doc.py（FeishuDocClient.get_document_content 签名与错误码）
    - server/common/logging.py（redact_secrets_in_text 用法）
  </read_first>
  <action>
    新建 `feature_list_extractor.py`，`FeatureListExtractor.normalize_sources`：接收三源（uploaded_text 直接用；feishu_url → `_extract_document_id` + `create_feishu_doc_client_for_project(space)` + `get_document_content` 回拉 markdown；pasted_text 直接用）。各源独立 try/except fail-soft（一源失败记 warning 降级，不影响其余源）；合并为统一原文（带 `## [来源:文件]`/`## [来源:飞书文档]`/`## [来源:粘贴]` 分隔标注便于抽取定位）。三源全空 → 抛 `ValueError("无可用 feature list 输入源")`。记 `feature_list_normalize_completed`(caller, source_count, total_len)。正文不整篇入日志（仅长度）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/initiatives/test_feature_list_extractor.py -x -q -k normalize</automated>
  </verify>
  <acceptance_criteria>
    - 三源任一提供即返回合并原文；飞书源经 respx mock get_document_content 回拉成功并入合并文本。
    - 飞书源回拉失败（mock err）→ 仅 warning + 跳过该源，其余源仍合并（断言不抛）。
    - 三源全空 → 抛 ValueError。
  </acceptance_criteria>
  <done>多源输入归一化 fail-soft，飞书链接复用 Phase 83 回拉。</done>
</task>

<task type="auto">
  <name>Task 3: 结构化抽取 extract_structure（分块 + token 预算降级 + LLM + call_source）</name>
  <read_first>
    - server/workflows/nodes/ai/prompt.py（build_chat_model().ainvoke 单 turn 范式 行 597-637；FakeChatModel seam 注释 行 530-533）
    - server/agents/llm_factory.py（build_chat_model 签名 + resolved provider）
    - server/agents/call_source.py（use_call_source 行 100-107）
    - .planning/feature-list-demo.md（真实富文档形态，确认标题/模块层级用于分块）
    - server/common/logging.py（redact_secrets_in_text）
  </read_first>
  <action>
    `extract_structure(raw_text, *, space, initiated_by_user_id=None)`：① 按 markdown 标题（模块级 `#`/`##`）切块，估算 token（字符数/系数粗估）；② token 预算检查——单块超预算则二次细切，整体块数超阈值则降级为「逐块抽取后合并」（degraded=True）；③ 每块构造抽取 prompt（system: 你是看板拆分助手，把 feature list 拆为 模块→功能点(每点=一个子看板)→验收项；user: 块原文），`with use_call_source(CallSource.BOARD_SPLIT):` 包 `build_chat_model(resolved=..., streaming=False).ainvoke(messages)`（函数体内 import build_chat_model 保留 FakeChatModel seam）；④ 解析 LLM JSON 输出为 `{modules:[{name, features:[{name, description, acceptance:[...]}]}]}`，跨块合并去重 + 拍平 `features_flat`（每条含 module 分组 + name + description 原文片段）。生命周期事件带 duration_ms + chunk_count + degraded；per-chunk sampling+debug。异常 fail-loud 抛由调用方（87-03/87-04），但异常文本经 `redact_secrets_in_text`。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/initiatives/test_feature_list_extractor.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - FakeChatModel seam 下，给定多模块原文 → 返回 dict 含 `modules` 且 `features_flat` 每条有 name/description/module。
    - 喂 `.planning/feature-list-demo.md`（82KB）→ chunk_count>1 且不抛（断言分块生效 + degraded 标志可被置位）。
    - 断言抽取调用处于 `board_split` call_source 作用域（monkeypatch get_call_source 捕获 / 断言 use_call_source 被调）。
  </acceptance_criteria>
  <done>结构化抽取分块 + token 预算降级 + board_split call_source；82KB demo 冒烟通过。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 多源输入（上传/飞书/粘贴）→ LLM | 半可信文本，可能含密钥/超大体积 |
| 飞书文档回拉正文 → 日志 | 半可信内容 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-02-INFO | Information Disclosure | 抽取日志/异常 | mitigate | 正文/异常经 redact_secrets_in_text；日志仅记长度/块数 |
| T-87-02-DOS | Denial of Service | 82KB 超大输入塞爆 LLM | mitigate | 分块 + token 预算降级 + degraded 标志；超阈值粗粒度/分批 |
| T-87-02-ATTR | Repudiation | 后台抽取归因 | mitigate | initiated_by_user_id 透传，缺省 system；LLM 调用带 board_split call_source |
</threat_model>

<verification>
- `cd server && uv run pytest tests/initiatives/test_feature_list_extractor.py -q` 全绿。
- `CallSource.normalize('board_split')` 命中；LOGGING-SPEC §4.1 含 board_split 行。
- grep extractor 正文/异常经 redact_secrets_in_text；LLM ainvoke 在 use_call_source(BOARD_SPLIT) 作用域内。
</verification>

<success_criteria>
- feature list 三源输入归一化 fail-soft（飞书链接复用 Phase 83 回拉）。
- 结构化抽取按 模块→功能点→验收项，每 feature 一条；82KB demo 经分块 + token 预算降级不爆。
- 抽取 LLM 赋 board_split call_source 并纳入指标上报。
</success_criteria>

<output>
完成后创建 `.planning/phases/87-board-split-card/87-02-SUMMARY.md`
</output>
