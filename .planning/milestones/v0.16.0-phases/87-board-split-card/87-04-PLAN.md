---
phase: 87
plan: 04
type: execute
wave: 3
depends_on: [87-03]
files_modified:
  - server/initiatives/models/project.py
  - server/initiatives/migrations/0009_project_feishu_chat_id.py
  - server/initiatives/services/project_service.py
  - server/feishu/cards/board_split_card.py
  - server/workflows/nodes/integrations/board_split_review.py
  - server/feishu/callbacks/board_split_callback.py
  - server/tests/initiatives/test_project_group_resolve.py
  - server/tests/workflows/test_board_split_review_node.py
  - server/tests/feishu/test_board_split_callback.py
autonomous: true
requirements: [BOARD-02]
must_haves:
  truths:
    - "群聊策略：复用项目群（Project.feishu_chat_id 命中即复用），项目无群则 create_chat 建新群 + ensure_bot_in_chat 入群 + writeback 群 id 到项目（INV-6 经 ProjectService）"
    - "拆分结果以 CardKit 流式卡片下发（create_card_entity→send→stream_card_content→settle，sequence 严格递增），卡片含「开始创建」按钮 + 输入框 + 发送"
    - "用户点「开始创建」→ 回调调 BoardSplitService.create_boards 直接建看板并恢复工作流；用户输入信息 → 多轮重拆（带用户指令重新 propose_split）后重新发群卡片，保持等待"
    - "节点 is_blocking waiting_event，提案/sources/轮次持久化 NodeExecution.output_data；回调后台线程 re-bind initiated_by_user_id，fail-soft 不反噬"
  artifacts:
    - path: "server/initiatives/models/project.py"
      provides: "Project.feishu_chat_id 字段（复用项目群）"
      contains: "feishu_chat_id"
    - path: "server/initiatives/services/project_service.py"
      provides: "ProjectService.resolve_or_create_group（复用/建新群+bot入群+writeback，INV-6）"
      contains: "resolve_or_create_group"
    - path: "server/feishu/cards/board_split_card.py"
      provides: "build_board_split_card（流式结果 + 开始创建 + 输入框）+ build_board_split_done_card"
      contains: "build_board_split_card"
    - path: "server/workflows/nodes/integrations/board_split_review.py"
      provides: "BoardSplitReviewNode(node_type='board_split_review') 拉群+发流式卡片+waiting_event"
      contains: "board_split_review"
    - path: "server/feishu/callbacks/board_split_callback.py"
      provides: "board_split_action 回调：开始创建建看板 / 输入重拆重发"
      contains: "board_split_"
  key_links:
    - from: "server/workflows/nodes/integrations/board_split_review.py"
      to: "initiatives.services.project_service.ProjectService.resolve_or_create_group"
      via: "节点 execute 先复用/建项目群 + bot 入群"
      pattern: "resolve_or_create_group"
    - from: "server/feishu/callbacks/board_split_callback.py"
      to: "initiatives.services.board_split_service.BoardSplitService.create_boards"
      via: "开始创建回调后台线程建看板 + engine.approve_node 恢复"
      pattern: "create_boards"
    - from: "server/feishu/callbacks/board_split_callback.py"
      to: "feishu.views.register_card_callback / workflows.engine.scheduler._run_in_thread"
      via: "镜像 chat_question_callback：同步回调 + 后台线程恢复"
      pattern: "register_card_callback|_run_in_thread"
---

<objective>
交付 BOARD-02：把 87-03 的拆分能力包成「拉群 + bot 入群 + 流式结果卡片 + 人机协同多轮重拆」的交互回路。复用 v0.11 CardKit 流式卡片 + v0.5x 建群 + 群聊提问/回调范式（chat_question）。

锁定决策（CONTEXT）：复用项目群，无则建新群 + 飞书 bot 入群；拉群 + 拆分结果流式卡片（「开始创建」/ 输入框+发送）；用户点「开始创建」→ 直接建看板；用户输入信息 → 多轮重拆后重新发群。

Purpose: 让拆分结果在群里被人确认/修订——一键建看板或多轮调优后再建，构成 feature list 流水线第 1-2 步的人机协同闭环。
Output: `Project.feishu_chat_id` + `resolve_or_create_group` + 流式拆分卡片 + `BoardSplitReviewNode`（waiting_event）+ `board_split_callback`（开始创建/重拆）。
</objective>

<execution_context>
@.planning/phases/87-board-split-card/87-CONTEXT.md
@.planning/phases/87-board-split-card/87-03-PLAN.md
</execution_context>

<context>
@.planning/PROJECT.md
@server/initiatives/models/project.py
@server/initiatives/services/project_service.py
@server/services/feishu_im.py
@server/workflows/nodes/integrations/feishu_chat.py
@server/workflows/nodes/integrations/chat_question.py
@server/feishu/callbacks/chat_question_callback.py
@server/feishu/cards/chat_question_card.py
@server/feishu/views.py
@server/workflows/engine/scheduler.py
@server/initiatives/services/board_split_service.py
@.cursor/rules/observability-logging.mdc
</context>

<artifacts_produced>
- EXTEND `server/initiatives/models/project.py`：`feishu_chat_id = models.CharField(max_length=128, blank=True, default="")`（复用项目群）。
- NEW `server/initiatives/migrations/0009_project_feishu_chat_id.py`（纯 AddField，无回填）。
- EXTEND `server/initiatives/services/project_service.py`：`resolve_or_create_group(*, project, member_ids, actor=None, initiated_by_user_id=None) -> str`（命中 feishu_chat_id 复用；否则 `FeishuIMService.create_chat` 建群 + `ensure_bot_in_chat` + writeback feishu_chat_id（INV-6 + 审计），fail-soft）。
- NEW `server/feishu/cards/board_split_card.py`：`build_board_split_card(proposal, *, execution_id, node_id, round, streamable_element_id)`（schema 2.0，config.streaming_mode；含 拆分结果可流式文本元素 + 「开始创建」button(action="board_split_start") + 输入框 + 「发送」button(action="board_split_refine")）+ `build_board_split_done_card(result)`。
- NEW `server/workflows/nodes/integrations/board_split_review.py`：`BoardSplitReviewNode`（`node_type="board_split_review"`, is_blocking, execution_mode server_local）。
- NEW `server/feishu/callbacks/board_split_callback.py`：`@register_card_callback("board_split_")` → `board_split_start`/`board_split_refine` 分支。
- NEW tests：`test_project_group_resolve.py`、`test_board_split_review_node.py`、`test_board_split_callback.py`。
- structlog 事件：`board_group_resolved`(caller, +duration_ms, reused:bool)、`board_split_card_sent`(caller)、`board_split_card_action`(caller, action, round)、`board_split_refine`(caller, round)、`board_split_card_create_done`(caller, created_count)。脱敏不可绕过；回调后台 re-bind initiated_by_user_id。
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Project.feishu_chat_id + 迁移 + resolve_or_create_group（复用/建新群+bot入群+writeback INV-6）</name>
  <read_first>
    - server/initiatives/models/project.py（Project 字段 + feishu_folder_token 范式 行 44-120）
    - server/initiatives/services/project_service.py（async 写收口 + AuditService.aemit + _COMPONENT 范式 行 661-728）
    - server/services/feishu_im.py（FeishuIMService.create / create_chat 行 870-960 / ensure_bot_in_chat）
    - server/workflows/nodes/integrations/feishu_chat.py（CreateGroupChatNode 建群+writeback fail-soft 范式 行 287-487）
    - server/initiatives/migrations/（最新迁移号确认 0009 命名）
  </read_first>
  <action>
    `project.py` 加 `feishu_chat_id` CharField（blank/default ""）。生成迁移 `0009_project_feishu_chat_id`（纯 AddField）。
    `project_service.py` 新增 `resolve_or_create_group`：① 取 project，命中非空 `feishu_chat_id` → 记 `board_group_resolved`(reused=True) 直接返回；② 否则 `FeishuIMService.create(space)` → `create_chat(name=f"{project.name} 项目群", user_id_list=member_ids, ...)` 拿 chat_id → `ensure_bot_in_chat(chat_id)` → writeback `project.feishu_chat_id`（@sync_to_async + transaction，select_for_update 防并发双建）+ `AuditService.aemit`(component=_COMPONENT, initiated_by_user_id)；建群失败 fail-soft 返回 ""（不抛，由上层节点降级提示）。记 `board_group_resolved`(reused=False, +duration_ms)。
  </action>
  <verify>
    <automated>cd server && uv run python manage.py makemigrations initiatives --check --dry-run; uv run pytest tests/initiatives/test_project_group_resolve.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - migration 0009 仅 AddField Project.feishu_chat_id；`makemigrations --check` 无未决变更。
    - project 已有 feishu_chat_id → resolve_or_create_group 复用（不调 create_chat，断言 mock 未被调）+ reused=True。
    - 无群 → create_chat + ensure_bot_in_chat + writeback 持久化 chat_id；create_chat 抛 → 返回 "" 不冒泡。
    - INV-6：grep 不旁路写 Project.feishu_chat_id（经 ProjectService 收口 + 审计）。
  </acceptance_criteria>
  <done>项目群复用/新建+bot入群+writeback，INV-6 + fail-soft。</done>
</task>

<task type="auto">
  <name>Task 2: 流式拆分结果卡片 build_board_split_card（开始创建 + 输入框 + 发送）</name>
  <read_first>
    - server/feishu/cards/chat_question_card.py（按钮 + 表单 + action value(execution_id/node_id) + 历史渲染范式）
    - server/services/feishu_im.py（CardKit create_card_entity/stream_card_content/settle schema 2.0 + streaming_mode 行 367-520）
    - server/feishu/views.py（CardCallbackView：action.value + form_value 合并、action 字段 行 250-269）
  </read_first>
  <action>
    新建 `board_split_card.py`，`build_board_split_card(proposal, *, execution_id, node_id, round, streamable_element_id="split_md")`：返回 schema 2.0 card_json（`config.streaming_mode=True`）：标题"看板拆分结果（第 {round} 轮）"，可流式 markdown 元素（element_id=streamable_element_id，初值空，用于 stream_card_content 灌入按模块分组的 feature 列表）；交互区——「开始创建」button（`value={"action":"board_split_start","execution_id":...,"node_id":...,"round":round}`）+ input 元素 + 「发送」button（`value={"action":"board_split_refine",...}`，输入经 form_value 合并取 `refine_input`）；degraded 时附 hint 文本。另 `build_board_split_done_card(result)` 渲染建看板结果（created 数 + degraded_parent_child hint + 链接）。action_value 仅携 ids/round，不含敏感正文。
  </action>
  <verify>
    <automated>cd server && uv run python -c "import django; django.setup(); from feishu.cards.board_split_card import build_board_split_card; c=build_board_split_card({'modules':[],'features_flat':[],'degraded':False}, execution_id='e', node_id='n', round=1); assert c.get('config',{}).get('streaming_mode') is True"</automated>
  </verify>
  <acceptance_criteria>
    - `build_board_split_card` 返回 schema 2.0 dict，`config.streaming_mode is True`，含可流式 element_id、开始创建 button(action=board_split_start)、输入框 + 发送 button(action=board_split_refine)。
    - degraded=True → 卡片含"配置中心"降级 hint 文本。
    - action_value 仅含 execution_id/node_id/round/action，不含 feature 原文。
  </acceptance_criteria>
  <done>流式拆分卡片：可流式结果 + 开始创建 + 输入框+发送 + 降级提示。</done>
</task>

<task type="auto">
  <name>Task 3: BoardSplitReviewNode（拉群 + 流式发卡 + waiting_event + 持久化提案/轮次）</name>
  <read_first>
    - server/workflows/nodes/integrations/chat_question.py（is_blocking + waiting_event + output_data 存轮次 + WorkflowEventSubscription 行 54-256）
    - server/workflows/nodes/integrations/feishu_chat.py（_resolve_project + member_ids 解析 行 30-91）
    - server/services/feishu_im.py（create_card_entity→send_card_entity→stream_card_content→settle 流式序列 + FeishuIMService.create）
    - server/initiatives/services/board_split_service.py（propose_split 返回结构）
    - server/initiatives/services/project_service.py（resolve_or_create_group Task 1）
  </read_first>
  <action>
    新建 `board_split_review.py`，`@register_node class BoardSplitReviewNode(BaseNode)`：`node_type="board_split_review"`, display_name="看板拆分评审", category=INTEGRATION, execution_mode="server_local", is_blocking=True。config：feature_list_url/feature_list_text/uploaded_text/work_item_type/member_ids。execute：① 解析 space + member_ids；② `ProjectService().resolve_or_create_group(...)` 拿 chat_id（空 → failed/error 提示无法拉群）；③ `BoardSplitService().propose_split(...)` 拿 proposal；④ CardKit 流式下发：`create_card_entity(build_board_split_card(proposal, execution_id, node_id, round=1))` → `send_card_entity(chat_id)` → `stream_card_content`（灌结果文本，sequence 单调递增）→ `settle_card_stream`；⑤ 建 WorkflowEventSubscription（event_type="BoardSplitCallback", timeout）；⑥ 返回 `waiting_event`，output_data 持久化 `{proposal, sources, work_item_type, chat_id, card_id, round:1, member_ids}`（供回调重拆/建看板复用）。流式失败 fail-soft 降级普通发卡。outputs：created(default) + refining + timeout + error。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/workflows/test_board_split_review_node.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `board_split_review` 注册到 NodeRegistry；is_blocking True。
    - mock（resolve_or_create_group→chat_id、propose_split→proposal、CardKit 方法）→ 节点返回 status waiting_event，output_data 含 proposal/sources/card_id/round=1。
    - resolve_or_create_group 返回 "" → status failed + error handle（断言不发卡）。
  </acceptance_criteria>
  <done>评审节点：拉群+流式发卡+waiting_event+持久化提案/轮次。</done>
</task>

<task type="auto">
  <name>Task 4: board_split_callback（开始创建→建看板恢复 / 输入→多轮重拆重发）</name>
  <read_first>
    - server/feishu/callbacks/chat_question_callback.py（register_card_callback + _extract_callback_data + _run_in_thread + 多轮 output_data 更新 + engine.approve_node 恢复 全文）
    - server/feishu/views.py（CardCallback dataclass + register_card_callback + form_value 合并 行 165-269）
    - server/workflows/engine/scheduler.py（_run_in_thread + WorkflowEngine.approve_node + SUSPENDED→RUNNING）
    - server/initiatives/services/board_split_service.py（create_boards + propose_split）
    - server/feishu/cards/board_split_card.py（build_board_split_card 重发 + build_board_split_done_card）
    - server/common/log_context.py（bind_task_context / re-bind initiated_by_user_id 范式）
  </read_first>
  <action>
    新建 `board_split_callback.py`，`@register_card_callback("board_split_")`（同步处理器，3s 内响应，后台线程做重活，镜像 chat_question_callback）。解析 action_value 取 action/execution_id/node_id/round（+ refine_input 来自 form_value）。
    - `board_split_start`：`_run_in_thread` 后台 → 查 waiting NodeExecution → `BoardSplitService().create_boards(space, proposal=output_data.proposal, ...)`（worker 入口 bind_task_context re-bind initiated_by_user_id=触发用户 / system）→ 发 `build_board_split_done_card(result)` 到 chat_id → `engine.approve_node` 恢复工作流（携 created 结果，节点走 default/created handle）。记 `board_split_card_create_done`(created_count)。
    - `board_split_refine`：后台 → `BoardSplitService().propose_split(..., extra_instruction=refine_input)`（重拆，原 sources + 用户指令）→ 更新 output_data（round+1, proposal=新）→ 复用 card_id 流式重灌结果（stream_card_content 续 sequence）或新发卡 → 保持 waiting_event（不 approve）。记 `board_split_refine`(round)。
    回调即时返回轻量确认卡（"已收到，正在处理"）。全程 fail-soft：异常记 `board_split_card_action`(failed) 不反噬主流程；正文/异常脱敏。
    注：87-02 `extract_structure` / 87-03 `propose_split` 需支持可选 `extra_instruction`（重拆指令）；若未支持，本任务补 propose_split 透传形参（read_first 确认）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/feishu/test_board_split_callback.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `board_split_start`：mock 下调 create_boards 一次 + 发 done 卡 + engine.approve_node 恢复（断言被调）；非 waiting 状态 → 幂等忽略。
    - `board_split_refine`：调 propose_split(extra_instruction=输入) + output_data.round 递增 + 重发卡 + 保持 waiting（不 approve，断言 approve_node 未被调）。
    - 回调同步路径不阻塞（重活在 _run_in_thread）；异常 fail-soft 不抛回飞书。
  </acceptance_criteria>
  <done>回调：开始创建建看板+恢复、输入多轮重拆重发，fail-soft + re-bind 归因。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 飞书卡片回调 → 建看板 / 重拆 | 未签名/伪造回调可触发建项 |
| 回调 operator → 归因 | 群成员/非成员触发 |
| 拆分提案/用户输入 → 日志 | 半可信文本 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-04-SPOOF | Spoofing | 卡片回调入口 | mitigate | 复用现有飞书回调签名校验（CardCallbackView）+ waiting 状态校验幂等 |
| T-87-04-INFO | Information Disclosure | 卡片/回调日志 | mitigate | 正文/异常 redact_secrets_in_text；action_value 仅携 ids/round 不含原文 |
| T-87-04-DOS | Denial of Service | 反复点重拆刷 LLM | mitigate | round 上限 + 单调 sequence；重拆走 board_split call_source 受控；超时订阅兜底 |
| T-87-04-ATTR | Repudiation | 建看板归因 | mitigate | 后台 bind_task_context re-bind initiated_by_user_id（触发用户/ system）；create_boards 经 attach_work_item 审计 |
| T-87-04-FAILSOFT | Tampering | 回调失败反噬 | mitigate | 重活 _run_in_thread best-effort，异常不抛回飞书 3s 主响应 |
</threat_model>

<verification>
- `cd server && uv run pytest tests/initiatives/test_project_group_resolve.py tests/workflows/test_board_split_review_node.py tests/feishu/test_board_split_callback.py -q` 全绿。
- `board_split_review` 在 NodeRegistry；`board_split_` 回调注册。
- `makemigrations initiatives --check` 无未决；grep Project.feishu_chat_id 写经 ProjectService 收口。
</verification>

<success_criteria>
- 复用项目群 / 无则建新群 + bot 入群 + writeback。
- 拆分结果 CardKit 流式卡片（开始创建 / 输入框+发送）。
- 开始创建 → 直接建看板并恢复；输入 → 多轮重拆后重新发群；fail-soft + 归因 + 脱敏。
</success_criteria>

<output>
完成后创建 `.planning/phases/87-board-split-card/87-04-SUMMARY.md`
</output>
