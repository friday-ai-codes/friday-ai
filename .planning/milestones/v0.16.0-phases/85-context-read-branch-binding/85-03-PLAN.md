---
phase: 85-context-read-branch-binding
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - server/initiatives/models/project_branch.py
  - server/initiatives/models/__init__.py
  - server/initiatives/migrations/0008_project_branch.py
  - server/initiatives/services/project_branch_service.py
  - server/initiatives/services/__init__.py
  - server/initiatives/views.py
  - server/initiatives/urls.py
  - server/initiatives/serializers.py
  - server/audit/services/taxonomy.py
  - server/tests/initiatives/test_project_branch_model.py
  - server/tests/initiatives/test_project_branch_service.py
  - server/tests/initiatives/test_project_branch_inv6_guard.py
  - server/tests/initiatives/test_project_branch_api.py
autonomous: true
requirements: [BIND-01]
must_haves:
  truths:
    - "一个项目可绑定多个 (repository, branch_name)；同一 (project, repository, branch_name) 唯一"
    - "分支绑定的所有写入收口于 ProjectBranchService（INV-6），模型层无业务 create/save"
    - "前端可经 REST 在项目下增删查分支绑定（写仅项目成员）"
    - "ProjectBranch 携带 source(manual/plan/coding) 标记绑定来源，并可经看板字段关联（branch↔board）"
  artifacts:
    - path: "server/initiatives/models/project_branch.py"
      provides: "ProjectBranch 模型 + BranchSource 枚举"
      contains: "class ProjectBranch"
    - path: "server/initiatives/migrations/0008_project_branch.py"
      provides: "CreateModel 迁移（紧跟 0007）"
      contains: "CreateModel"
    - path: "server/initiatives/services/project_branch_service.py"
      provides: "ProjectBranchService 写收口（bind/unbind/list）"
      contains: "class ProjectBranchService"
  key_links:
    - from: "server/initiatives/views.py"
      to: "initiatives.services.project_branch_service.ProjectBranchService"
      via: "REST bind/unbind/list 经 service 写收口"
      pattern: "ProjectBranchService"
    - from: "server/initiatives/models/project_branch.py"
      to: "initiatives.Project / repositories.Repository"
      via: "project FK + repository FK + unique_together"
      pattern: "unique"
---

<objective>
新增 `ProjectBranch` 多绑定模型（项目 FK + 仓库 FK + branch_name + source + 时间戳，唯一 `(project, repository, branch_name)`），把分支↔项目绑定的所有写入收口于 `ProjectBranchService`（INV-6），并提供前端绑定 REST（项目下增/删/查分支绑定，写仅成员）。这是 BIND-01 的模型与写半，也是 Plan 04（`lookup_project_by_branch` 显式多绑定反查）的依赖。

Purpose: BIND-01「分支↔项目多绑定模型 + 分支↔看板结合」。
Output: 模型 + 迁移 0008 + 写收口 service + REST + 审计 action + Wave 0 测试（模型/service/INV-6 grep/API）。
</objective>

<execution_context>
@.planning/phases/85-context-read-branch-binding/85-RESEARCH.md
@.planning/phases/85-context-read-branch-binding/85-CONTEXT.md
</execution_context>

<context>
@.planning/PROJECT.md
@.cursor/rules/observability-logging.mdc
@server/initiatives/models/project.py
@server/initiatives/services/knowledge_graph.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: ProjectBranch 模型 + BranchSource 枚举 + 迁移 0008 + 导出</name>
  <read_first>
    - server/initiatives/models/project.py（Project UUID PK + Meta/constraints 范式）
    - server/initiatives/models/work_item_link.py（through 模型 + provenance 枚举范式，INV-6「模型层无业务方法」纪律）
    - server/initiatives/models/__init__.py（导出清单 + `__all__`）
    - server/initiatives/migrations/0007_doc_sync_engine.py（最新迁移号 + dependencies 写法）
    - server/repositories/models.py（Repository UUID PK，确认 FK target app 标签 `repositories.Repository`）
  </read_first>
  <action>
    新建 `server/initiatives/models/project_branch.py`：`class BranchSource(models.TextChoices)`（`MANUAL="manual"`、`PLAN="plan"`、`CODING="coding"`）；`class ProjectBranch(models.Model)`——`id=UUIDField(default=uuid4)`、`project=FK("initiatives.Project", on_delete=CASCADE, related_name="branch_bindings")`、`repository=FK("repositories.Repository", on_delete=CASCADE, related_name="project_branch_bindings")`、`branch_name=CharField(max_length=255)`、`source=CharField(choices=BranchSource.choices, default=MANUAL)`、可选 `feishu_board_id=CharField(blank=True, default="")`（branch↔board 结合，绑定时可携项目看板 id 冗余便于查）、`created_by=FK(AUTH_USER_MODEL, SET_NULL, null=True)`、`created_at`/`updated_at`。Meta：`db_table="initiative_project_branches"`、`UniqueConstraint(fields=["project","repository","branch_name"], name="uniq_project_repo_branch")`、`indexes=[Index(fields=["branch_name"]), Index(fields=["repository","branch_name"])]`（分支名反查走索引）。**模型层不提供业务 create/save 方法**（INV-6，镜像 Project 模型 docstring）。

    在 `server/initiatives/models/__init__.py` 导出 `ProjectBranch` + `BranchSource` 并加入 `__all__`。

    生成迁移：`cd server && uv run python manage.py makemigrations initiatives`，确认产出 `0008_project_branch.py`（纯 `CreateModel` 无回填，dependencies 含 `0007_doc_sync_engine` + `repositories` 最新迁移）。文件名须为 `0008_project_branch.py`（避免迁移号撞车，Pitfall 4）。

    新建 `server/tests/initiatives/test_project_branch_model.py`：断言 `(project, repository, branch_name)` 重复插入 `pytest.raises(IntegrityError)`；同项目不同仓 / 不同分支可并存；`source` 默认 `manual`。
  </action>
  <verify>
    <automated>cd server && uv run python manage.py makemigrations initiatives --check --dry-run && uv run pytest tests/initiatives/test_project_branch_model.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `ProjectBranch` 模型存在，字段含 project/repository FK、branch_name、source(BranchSource)、feishu_board_id、created_by、时间戳。
    - 唯一约束 `(project, repository, branch_name)` 在 DB 层生效（重复插入 IntegrityError）。
    - 迁移文件名为 `0008_project_branch.py`，`makemigrations --check` 干净（无未生成迁移）。
    - 模型层无 create/save 业务方法（grep 确认）。
  </acceptance_criteria>
  <done>ProjectBranch 模型 + 迁移 0008 + 导出 + 模型测试全绿。</done>
</task>

<task type="auto">
  <name>Task 2: ProjectBranchService 写收口（INV-6）+ 审计 action + INV-6 grep 守护</name>
  <read_first>
    - server/initiatives/services/project_service.py（INV-6 写收口范式：成员校验 + 审计 emit + 归因 initiated_by_user_id）
    - server/initiatives/services/knowledge_graph.py（`_emit_linked` → `AuditService.aemit` 范式 + `taxonomy.ACTION_*`）
    - server/audit/services/taxonomy.py（action 常量登记位置 + `ALL_ACTIONS`）
    - server/initiatives/services/__init__.py（service 导出 + `MemoryPermissionError` 等权限异常范式）
    - server/initiatives/services/memory_service.py（`_assert_member` 成员校验范式，写仅成员）
  </read_first>
  <action>
    在 `server/audit/services/taxonomy.py` 新增 `ACTION_PROJECT_BRANCH_BOUND` / `ACTION_PROJECT_BRANCH_UNBOUND` 常量并并入 `ALL_ACTIONS`。

    新建 `server/initiatives/services/project_branch_service.py`：`class ProjectBranchService`——分支绑定唯一写入入口（INV-6）。方法：
    - `async def bind(self, *, project_id, repository_id, branch_name, source=BranchSource.MANUAL, actor=None, initiated_by_user_id=None, feishu_board_id="") -> ProjectBranch`：成员校验（写仅成员，复用 memory_service 同款 `_assert_member` 语义；非成员抛权限异常）→ `get_or_create((project, repository, branch_name), defaults={source, created_by, feishu_board_id})` 幂等（已存在仅按需回填 source 漂移）→ `AuditService.aemit(ACTION_PROJECT_BRANCH_BOUND, actor=, target_type="project_branch", target_id=, metadata={component:"initiatives", category:"caller", initiated_by_user_id})`。结构化日志 `project_branch_bound`（项目/仓库/分支/source/`initiated_by_user_id`，`category="caller"`，`component="initiatives"`）。
    - `async def unbind(self, *, project_id, repository_id, branch_name, actor=None, initiated_by_user_id=None) -> bool`：成员校验 → 删除该绑定（不存在返回 False，不抛）→ 审计 `ACTION_PROJECT_BRANCH_UNBOUND`。
    - `async def list_for_project(self, *, project_id) -> list[ProjectBranch]`：只读列出项目全部绑定（select_related repository）。
    所有 ORM 经 `sync_to_async`；async 安全。

    在 `server/initiatives/services/__init__.py` 导出 `ProjectBranchService`。

    新建 `server/tests/initiatives/test_project_branch_service.py`：bind 幂等（重复 bind 不报错、不产重复行）、unbind 不存在返回 False 不抛、非成员 bind/unbind 抛权限异常、审计事件 emit、`initiated_by_user_id` 归因落审计 metadata。

    新建 `server/tests/initiatives/test_project_branch_inv6_guard.py`（镜像既有 `test_project_inv6_guard`）：grep 全 `server/` 断言除 `ProjectBranchService` 外无旁路 `ProjectBranch.objects.create(` / `.save(` 写入（正则排除迁移文件与 service 自身 + 测试），并正向断言 service 内确实写入（writer-actually-writes）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/initiatives/test_project_branch_service.py tests/initiatives/test_project_branch_inv6_guard.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `ProjectBranchService.bind/unbind/list_for_project` 存在；bind 幂等（重复 bind 无重复行、无异常）。
    - 写仅成员：非成员 bind/unbind 抛权限异常。
    - bind/unbind 各 emit 审计事件（`ACTION_PROJECT_BRANCH_BOUND`/`UNBOUND`），metadata 含 `initiated_by_user_id`（无则 system）。
    - INV-6 grep 守护：除 service 外无旁路写 ProjectBranch；正向断言 service 写入有效。
  </acceptance_criteria>
  <done>ProjectBranchService 写收口 + 审计 + INV-6 守护测试全绿。</done>
</task>

<task type="auto">
  <name>Task 3: 前端绑定 REST（项目下分支绑定 增/删/查）</name>
  <read_first>
    - server/initiatives/views.py（`ProjectMemoryListCreateView` 699-740 范式：adrf APIView + 成员校验 + serializer + service 调用；`ProjectDetailView` 取 project + 权限）
    - server/initiatives/urls.py（`<uuid:project_id>/...` 子路由注册范式 37-137）
    - server/initiatives/serializers.py（请求/响应序列化器范式）
    - server/initiatives/services/project_branch_service.py（Task 2 service）
    - server/initiatives/permissions.py（成员/读权限校验范式）
  </read_first>
  <action>
    在 `server/initiatives/serializers.py` 新增 `ProjectBranchSerializer`（响应：id/repository_id/repository_name/branch_name/source/feishu_board_id/created_at）+ `ProjectBranchBindRequestSerializer`（请求：必填 `repository_id`、必填 `branch_name`、可选 `source`(默认 manual)、可选 `feishu_board_id`）。

    在 `server/initiatives/views.py` 新增 `ProjectBranchListCreateView(APIView)`：`get`（读权限：成员或 public_org 可读，复用既有读权限范式）列出 `ProjectBranchService().list_for_project(project_id=)` 序列化；`post`（写仅成员）`_validate` → `ProjectBranchService().bind(project_id, repository_id, branch_name, source, actor=request.user, initiated_by_user_id=request.user.id, feishu_board_id=)` → 201 返回序列化绑定。新增 `ProjectBranchDetailView(APIView)`：`delete`（写仅成员）`ProjectBranchService().unbind(...)` → 204/200。

    在 `server/initiatives/urls.py` 注册 `path("<uuid:project_id>/branches/", ProjectBranchListCreateView.as_view(), name="project-branch-list")` 与 `path("<uuid:project_id>/branches/<uuid:branch_id>/", ProjectBranchDetailView.as_view(), name="project-branch-detail")`（字面段在 router 通配前，UUID 通配安全）。

    新建 `server/tests/initiatives/test_project_branch_api.py`：成员 POST 绑定 201、列出 GET 200、DELETE 解绑；非成员 POST/DELETE 403；缺参 400；重复绑定幂等（不 500）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/initiatives/test_project_branch_api.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `GET /api/initiatives/projects/<project_id>/branches/` 列出绑定（读权限 = 成员或 public_org）。
    - `POST .../branches/` 成员可绑定返回 201；非成员 403；缺 `repository_id`/`branch_name` 400。
    - `DELETE .../branches/<branch_id>/` 成员可解绑；非成员 403。
    - 写入全部经 `ProjectBranchService`（无 view 内直接 ORM 写）。
  </acceptance_criteria>
  <done>分支绑定 REST 落地，权限 + 序列化 + API 测试全绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 前端/流水线 → ProjectBranch 写 | 绑定写入须仅成员、单一 service 收口 |
| REST 入参 → DB | branch_name/repository_id 须 serializer 校验 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-85-03-01 | Elevation of Privilege | ProjectBranch 写 | mitigate | bind/unbind 成员校验 fail-closed（非成员抛权限异常）；REST 写端点成员闸 |
| T-85-03-02 | Tampering | 旁路写表 | mitigate | INV-6 单一写收口 + grep 守护测试（除 service 无旁路 create/save） |
| T-85-03-03 | Repudiation | 绑定操作 | mitigate | bind/unbind 审计 emit + `initiated_by_user_id` 归因 |
| T-85-03-04 | Input Validation | bind REST | mitigate | serializer 校验 repository_id(UUID)/branch_name/source 枚举 |
</threat_model>

## Artifacts this phase produces (Plan 03)

- **新模型（project_branch.py）**：`ProjectBranch`（字段：id、project FK、repository FK、branch_name、source、feishu_board_id、created_by、created_at、updated_at；唯一 `(project,repository,branch_name)`；索引 branch_name / (repository,branch_name)）、`BranchSource` 枚举（manual/plan/coding）
- **新迁移**：`server/initiatives/migrations/0008_project_branch.py`（CreateModel）
- **新 service（project_branch_service.py）**：`ProjectBranchService.bind / unbind / list_for_project`
- **新审计 action（taxonomy.py）**：`ACTION_PROJECT_BRANCH_BOUND`、`ACTION_PROJECT_BRANCH_UNBOUND`
- **新 REST（views.py + urls.py）**：`ProjectBranchListCreateView`、`ProjectBranchDetailView`；路由 `projects/<id>/branches/`、`projects/<id>/branches/<branch_id>/`
- **新序列化器（serializers.py）**：`ProjectBranchSerializer`、`ProjectBranchBindRequestSerializer`
- **新测试**：`test_project_branch_model.py`、`test_project_branch_service.py`、`test_project_branch_inv6_guard.py`、`test_project_branch_api.py`
- **call_source**：无 LLM 调用，不涉及 §4.1。
- **out-of-scope（记 Phase 89 handoff）**：source=coding 的 git push 自动绑定（现 git webhook 仅 MR、无 push 事件，A4）——本 plan 仅提供 `ProjectBranchService.bind(source=coding)` seam，push 触发的自动绑定留 Phase 89 调用该 service；source=plan 由方案流水线（Phase 89）写入。

<verification>
- `cd server && uv run pytest tests/initiatives/ -q` 全绿。
- `cd server && uv run python manage.py makemigrations --check --dry-run` 干净。
- `cd server && uv run ruff check server/initiatives/` 无新增 lint。
</verification>

<success_criteria>
- ProjectBranch 多绑定模型 + 唯一约束 + branch↔board 字段就位（BIND-01）。
- 分支绑定写入收口单一 service（INV-6）+ 审计 + 归因。
- 前端可经 REST 增删查项目分支绑定，写仅成员。
</success_criteria>

<output>
Create `.planning/phases/85-context-read-branch-binding/85-03-SUMMARY.md` when done.
</output>
