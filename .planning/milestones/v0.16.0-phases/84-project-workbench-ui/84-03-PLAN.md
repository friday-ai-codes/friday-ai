---
phase: 84-project-workbench-ui
plan: 03
type: execute
wave: 2
depends_on: [84-01, 84-02]
files_modified:
  - web/pnpm-workspace.yaml
  - web/package.json
  - web/src/components/project/workbench/DocsSection.vue
  - web/src/components/project/workbench/MarkdownSourceEditor.vue
  - web/src/components/project/workbench/MemorySection.vue
  - web/src/components/project/workbench/__tests__/DocsSection.spec.ts
autonomous: true
requirements: [WB-03]
must_haves:
  truths:
    - "5 个工作区文件可在线查看：markdown 实时渲染"
    - "可切换到编辑态用 CodeMirror 编辑 markdown 源码，仅人工区可编辑、系统区只读"
    - "保存编辑经后端人工区写回端点提交，触发 Phase 83 同步引擎 block 级回灌（前端不直写飞书）"
    - "MEMORY 复用条目式渲染 + 编辑 + LLM 提议(draft) 确认 UI（采纳/拒绝带二次确认）"
  artifacts:
    - path: web/src/components/project/workbench/MarkdownSourceEditor.vue
      provides: "CodeMirror6 markdown 源码编辑器（lang-markdown + fridayLightTheme + :readonly）"
    - path: web/src/components/project/workbench/DocsSection.vue
      provides: "5 文件列 + 查看(渲染)/编辑(源码) 切换 + 人工区写回 + 系统区只读"
    - path: web/src/components/project/workbench/MemorySection.vue
      provides: "记忆条目渲染/编辑 + LLM 草稿确认 UI"
  key_links:
    - from: web/src/components/project/workbench/DocsSection.vue
      to: web/src/api/projectWorkspace.ts
      via: "getDocContent / updateHumanBlocks"
      pattern: "getDocContent|updateHumanBlocks"
    - from: web/src/components/project/workbench/MemorySection.vue
      to: web/src/api/projectMemory.ts
      via: "list/create/edit/confirmDraft/rejectDraft + useConfirmDialog"
      pattern: "projectMemoryApi|confirmDraft"
---

<objective>
落地 WB-03：5 文件在线查看（markdown 实时渲染）+ 编辑（CodeMirror 源码，仅人工区可编辑、系统区只读，保存经 Phase 83 同步引擎 block 级回灌）+ MEMORY 条目式渲染/编辑/LLM 提议确认。补全 84-02 占位的 `DocsSection.vue`，新增 markdown 源码编辑器与记忆区块。本期新增 `@codemirror/lang-markdown`（CM6 官方语言包）。

Purpose: 让用户在 Friday 内直接看/改项目工作区文件，人工区改动安全回灌飞书、不冲掉系统区；记忆/草稿走人工确认防共享记忆污染。
Output: MarkdownSourceEditor + DocsSection（含查看/编辑/分区只读/写回轮询）+ MemorySection + 守护测试。
</objective>

<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/84-project-workbench-ui/84-CONTEXT.md
@.planning/phases/84-project-workbench-ui/84-UI-SPEC.md
@web/src/components/execution/MarkdownRenderer.vue
@web/src/composables/useMarkdownRenderer.ts
@web/src/components/prompts/PromptBodyEditor.vue
@web/src/components/execution/JsonEditor.vue
@web/src/components/codemirror/fridayLightTheme.ts
@web/src/components/project/workbench/MemoryTab.vue
@web/src/composables/useConfirmDialog.ts
@web/src/api/projectMemory.ts
@web/pnpm-workspace.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: MarkdownSourceEditor（CM6 markdown 源码编辑器）+ 安装 lang-markdown</name>
  <files>web/pnpm-workspace.yaml, web/package.json, web/src/components/project/workbench/MarkdownSourceEditor.vue</files>
  <read_first>
  - web/src/components/prompts/PromptBodyEditor.vue（Codemirror 封装 + extensions + :disabled readonly + update 绑定）
  - web/src/components/execution/JsonEditor.vue（lang() + fridayLightTheme + search() + lineWrapping 模式）
  - web/src/components/codemirror/fridayLightTheme.ts（主题）
  - web/pnpm-workspace.yaml（catalog 版本钉法；codemirror 6.x 生态版本）
  </read_first>
  <action>
  在 pnpm catalog 增 `@codemirror/lang-markdown`（与已装 `@codemirror/*` 6.x 对齐的最新兼容版，沿用 catalog 钉法），并在 `web/package.json` 引用 catalog。新增 `MarkdownSourceEditor.vue`：包 `vue-codemirror` 的 `<Codemirror>`，extensions = `[markdown(), fridayLightTheme, search(), EditorView.lineWrapping]`，props `modelValue` + `readonly`（`:disabled="readonly"`），`@update:model-value` emit；仿 `PromptBodyEditor` 结构与样式（固定/自适应高度、tab-size 2）。不引入暗色 one-dark 主题。
  </action>
  <verify>
  <automated>pnpm -C web install --frozen-lockfile=false && pnpm -C web run type-check</automated>
  </verify>
  <done>`@codemirror/lang-markdown` 经 catalog 安装；MarkdownSourceEditor 双向绑定 + readonly 生效；vue-tsc 绿。</done>
</task>

<task type="auto">
  <name>Task 2: DocsSection — 5 文件查看/编辑 + 系统区只读 + 人工区写回</name>
  <files>web/src/components/project/workbench/DocsSection.vue</files>
  <read_first>
  - web/src/components/execution/MarkdownRenderer.vue（查看态渲染，html:false）
  - web/src/api/projectWorkspace.ts（getDocContent/updateHumanBlocks/listDocs，DocBlock.section/editable）
  - web/src/components/repository/ReconcilePanel.vue（派发→轮询：useMutation + enabled + refetchInterval）
  - .planning/phases/84-project-workbench-ui/84-UI-SPEC.md（Interaction & State Contracts、文件保存冲突文案、只读区提示）
  </read_first>
  <action>
  补全 `DocsSection.vue`：左侧 5 文件切换（memory/state/milestones/research/preflight，沿用 listDocs 元数据 + sync_status badge），右侧默认**查看态**用 `MarkdownRenderer` 渲染 `getDocContent` 的 rendered_markdown；点「编辑源码」进编辑态——按 block `section` 渲染：system block 用 `MarkdownSourceEditor :readonly` + 顶部「系统区由 Friday 自动维护，只读」提示，human block 用可编辑 `MarkdownSourceEditor`；「保存到飞书」调 `updateHumanBlocks`（`useMutation`），成功 toast「已提交同步，飞书将很快更新」并 `invalidateQueries(['project-doc', id, docType])` + 轮询 sync_status（仿 ReconcilePanel：`refetchInterval: q => q.state.data?.sync_status==='syncing' ? 2000 : false`）。保存冲突（后端 409/422）按 UI-SPEC 文案提示并保留改动。queryKey `['project-doc', idRef, docType]`。memory 文档 doc_type 路由到 MemorySection（见 Task 3），其余 4 文件走本通用查看/编辑。
  </action>
  <verify>
  <automated>pnpm -C web test -- DocsSection</automated>
  </verify>
  <done>5 文件可切换查看（渲染）；进编辑态 system block 只读、human block 可编辑；保存调 updateHumanBlocks 并轮询 sync_status；冲突有提示；vue-tsc 绿。</done>
</task>

<task type="auto">
  <name>Task 3: MemorySection — 记忆条目 + LLM 草稿确认</name>
  <files>web/src/components/project/workbench/MemorySection.vue, web/src/components/project/workbench/__tests__/DocsSection.spec.ts</files>
  <read_first>
  - web/src/components/project/workbench/MemoryTab.vue（草稿区 data-testid="draft-section"、accept/reject + useConfirmDialog、新增/编辑记忆、queryKeys）
  - web/src/api/projectMemory.ts（list/create/edit/supersede/listDrafts/confirmDraft/rejectDraft/distill）
  - web/src/components/project/workbench/__tests__/MemoryTab.spec.ts（真实 zh-CN.json + mock 范式）
  </read_first>
  <action>
  新增 `MemorySection.vue`：在工作台「文件 → MEMORY」内复用 `MemoryTab` 的条目式渲染 + 编辑 + LLM 草稿确认交互（采纳走 `useConfirmDialog` 二次确认 → `confirmDraft`；拒绝 → `rejectDraft`，文案沿用 `projects.memory.draft.*` 与 UI-SPEC 拒绝草稿确认）。条目正文可选用 `MarkdownRenderer` 渲染（查看）+ 行内编辑保存。DocsSection 在 doc_type===memory 时挂载 MemorySection。新增守护测试 `DocsSection.spec.ts`：mock `projectWorkspaceApi`/`projectMemoryApi`/`useConfirmDialog`，断言：5 文件切换、查看渲染、编辑态 system 只读/human 可编辑、保存调用 updateHumanBlocks、草稿采纳触发 confirm + confirmDraft，关键 zh-CN 文案。
  </action>
  <verify>
  <automated>pnpm -C web test -- DocsSection</automated>
  </verify>
  <done>MEMORY 区块呈现条目 + 草稿确认；采纳/拒绝带二次确认并调对应 API；测试断言交互与 zh-CN 文案通过。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 文档/记忆正文 → v-html | markdown 渲染输出注入 DOM |
| 用户编辑 → 人工区写回 | 不可信文本提交后端 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-08 | Tampering (XSS) | MarkdownRenderer 输出 | mitigate | 复用 `getMarkdownRenderer`（`html:false`）；不渲染原始 HTML；CM 源码不执行 |
| T-84-09 | Tampering | 系统区编辑 | mitigate | 前端 system block 强制 `:readonly`；真正闸门在后端（84-01 拒绝 system 写）|
| T-84-10 | Repudiation | 草稿采纳 | mitigate | 采纳/拒绝经二次确认；归因由后端记录 |
| T-84-SC | Tampering | npm 安装 `@codemirror/lang-markdown` | mitigate | CM6 官方包，经 pnpm catalog 钉版本 + lockfile；[ASSUMED] 审查见 verify |
</threat_model>

<verification>
- 查看=markdown 渲染、编辑=CM 源码（非 tiptap 富文本，避免 md 转换损耗，per CONTEXT 特定意见）
- 系统区只读、人工区可写并经同步引擎回灌（不前端直写飞书）
- vue-tsc 绿；既有 MemoryTab.spec 不破
- `@codemirror/lang-markdown` 来源核验（npmjs 官方 @codemirror scope）
</verification>

<success_criteria>
WB-03 完整：5 文件查看/编辑回灌 + 记忆/草稿确认 UI；区段只读保护到位；基线不破。
</success_criteria>

<output>
Create `.planning/phases/84-project-workbench-ui/84-03-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

- `MarkdownSourceEditor.vue`（CM6 markdown 源码编辑器）
- `DocsSection.vue`（完整：5 文件查看/编辑/分区只读/写回轮询）
- `MemorySection.vue`（记忆条目 + 草稿确认）
- 新依赖：`@codemirror/lang-markdown`（pnpm catalog）
- queryKeys：`['project-doc', id, docType]`
- 测试：`DocsSection.spec.ts`
