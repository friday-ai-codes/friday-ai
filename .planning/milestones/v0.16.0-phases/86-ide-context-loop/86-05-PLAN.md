---
phase: 86-ide-context-loop
plan: 05
type: execute
wave: 3
depends_on: [86-03, 86-04]
files_modified:
  - server/initiatives/services/ide_hook_assets.py
  - server/initiatives/services/cursor_rules.py
  - server/initiatives/views.py
  - server/tests/initiatives/test_ide_hook_assets.py
autonomous: true
requirements: [HOOK-02, HOOK-03]
must_haves:
  truths:
    - "三家（Cursor / Claude Code / Codex）各产出 stop hook 资产：会话结束默认开启 + 静默回写——调 report_project_knowledge(writeback_mode=active) 写 MEMORY/RESEARCH + report_project_state 写结构化 API 清单"
    - "stop hook 默认开启 + 静默：不弹窗、不阻断 IDE 编码；无 PAT / 未绑项目 / 接口失败 → 静默 exit 0，绝不 block（accepted deviation 配套客户端）"
    - "stop hook 资产正文显式写明三道兜底由服务端保证（质量门槛/脱敏/审计回滚）+ 绝不上报凭证/密钥/token"
    - "写路径资产经同一 ide-hook-assets 端点按 runtime + kind=write 下发；cursor_rules 措辞同步到 active 直写（不再说「人工确认」），与 86-01 服务端行为一致"
  artifacts:
    - path: "server/initiatives/services/ide_hook_assets.py"
      provides: "build_write_path_assets(project, runtime) —— 三家 stop hook 脚本/settings（active 写回 + STATE 回写）"
      contains: "build_write_path_assets"
    - path: "server/initiatives/views.py"
      provides: "ProjectIdeHookAssetsView 支持 kind=write 下发写路径资产"
      contains: "kind"
  key_links:
    - from: "server/initiatives/services/ide_hook_assets.py"
      to: "mcp_tools report_project_knowledge / report_project_state"
      via: "stop hook 脚本调 active 写回 + STATE 结构化回写工具"
      pattern: "report_project_knowledge|report_project_state"
    - from: "server/initiatives/views.py"
      to: "initiatives.services.ide_hook_assets.build_write_path_assets"
      via: "kind=write 下发写路径资产 bundle"
      pattern: "build_write_path_assets"
---

<objective>
落地 HOOK-02/03 写路径的**客户端 stop hook 资产**（三家：Cursor `hooks.json` 的 `stop`、Claude Code `Stop` hook、Codex 最弱按可行降级）：会话结束**默认开启 + 静默回写**——调 86-01 的 `report_project_knowledge(writeback_mode=active)` 直写 MEMORY/RESEARCH（active 生效，accepted deviation）+ 调 86-04 的 `report_project_state` 写结构化 API 清单（STATE）。stop hook **不弹窗、不阻断 IDE 编码**；无 PAT / 未绑项目 / 接口失败 → 静默 exit 0，绝不 block。

同步把 `cursor_rules` 措辞从「落 draft 人工确认」更新为「active 直写生效（经服务端质量门槛/脱敏/审计回滚兜底）」，与 86-01 服务端行为一致（accepted deviation）。资产经 86-03 的 `ide-hook-assets` 端点扩 `kind=write` 下发。

Purpose: 兑现「stop hook 默认开启 + 静默直写生效」的客户端半，闭合写路径闭环。
Output: `build_write_path_assets`（三家 stop hook）+ 端点 kind=write + cursor_rules 措辞同步 + 测试。
</objective>

<execution_context>
@.planning/phases/86-ide-context-loop/86-CONTEXT.md
@.planning/project-workspace/MILESTONE-PROPOSAL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.cursor/rules/observability-logging.mdc
@server/initiatives/services/ide_hook_assets.py
@server/initiatives/services/cursor_rules.py
@server/initiatives/views.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: build_write_path_assets —— 三家 stop hook 资产（active 写回 + STATE 回写，默认开启 + 静默不阻断）</name>
  <read_first>
    - server/initiatives/services/ide_hook_assets.py（86-03 产 `build_read_path_assets` + `RUNTIME` 常量 + 共用强制流程 helper；本 task 加 `build_write_path_assets` 对称结构）
    - server/mcp_tools/serializers.py（`ReportProjectKnowledgeRequestSerializer` 86-01 扩展的 `writeback_mode=active`/`target`/`distill`；`ReportProjectStateRequestSerializer` 86-04 的 apis 结构——stop hook 脚本据此构请求体）
    - server/mcp_tools/urls.py（`tools/report_project_knowledge/` + `tools/report_project_state/` 路由名——脚本调用端点）
    - .planning/phases/86-ide-context-loop/86-CONTEXT.md（写路径 stop hook 行为 + 三道兜底 + 无 PAT/未绑项目静默跳过 + accepted deviation 措辞）
    - .planning/project-workspace/MILESTONE-PROPOSAL.md（§7 三家 hook 差异：Cursor hooks.json stop / CC Stop / Codex 弱）
    - .cursor/rules/observability-logging.mdc（资产正文脱敏告诫）
  </read_first>
  <action>
    在 `ide_hook_assets.py` 新增 `def build_write_path_assets(project, runtime: str) -> dict`（与 `build_read_path_assets` 对称返回 `{"runtime","kind":"write","files":[...],"notes"}`）：
    - **cursor**：`.cursor/hooks.json` 的 `stop` 钩子片段 + stop 脚本（`.cursor/hooks/friday-stop-writeback.sh`/`.py`）：会话结束收集本次「上下文 + 用户改动摘要」+「新增/改动 API 结构化清单」，调 `report_project_knowledge(project_id, content, writeback_mode="active", target="memory"|"research")` + `report_project_state(project_id, apis=[…])`（用户 PAT）。**默认开启 + 静默**：不弹窗、stdout 不打断编码；无 PAT/未绑项目/接口非 2xx → 静默 exit 0。
    - **claude_code**：`.claude/settings.json` 的 `Stop` hook 注册 + 同款 stop 脚本（`.claude/hooks/friday-stop-writeback.*`）。
    - **codex**：按 Codex hook 能力弱——产一段尽力而为的 stop 脚本或在 `notes` 标「Codex 原生 hook 注入/回写能力弱，写路径以手动运行脚本 / CI 兜底为主」（deferred 对齐 CONTEXT「Codex 原生 hook 按仅 MCP+rules 对待」），但仍提供可手动执行的回写脚本资产。
    - 所有脚本资产正文显式写：① 三道兜底由 Friday 服务端保证（质量门槛过滤低质/空/重复、脱敏不可绕过、审计可回滚）；② 绝不上报凭证/密钥/token/个人敏感信息；③ 静默不阻断编码的失败处理。
    - 复用 86-03 的脱敏告诫 helper + project 信息渲染，避免重复措辞。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/initiatives/test_ide_hook_assets.py -k write_path -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `build_write_path_assets(project, "cursor")` 返回含 `.cursor/hooks.json` stop 片段 + stop 脚本，脚本含 `report_project_knowledge`（writeback_mode=active）+ `report_project_state` 调用 + 无 PAT/失败静默 exit 0。
    - `build_write_path_assets(project, "claude_code")` 返回含 `.claude/settings.json` Stop hook + 同款脚本。
    - `build_write_path_assets(project, "codex")` 返回可手动执行的回写脚本 + notes 标 Codex 弱。
    - 三家写路径资产正文均含「三道兜底由服务端保证」+ 脱敏告诫 + 静默不阻断说明。
  </acceptance_criteria>
  <done>三家 stop hook 写路径资产生成（active 写回 + STATE 回写，默认开启 + 静默不阻断 + 兜底/脱敏告诫），测试全绿。</done>
</task>

<task type="auto">
  <name>Task 2: 端点 kind=write 下发 + cursor_rules 措辞同步 active（accepted deviation）+ 测试</name>
  <read_first>
    - server/initiatives/views.py（86-03 `ProjectIdeHookAssetsView`：`kind` 参数当前仅 read；本 task 扩 kind=write 分流到 `build_write_path_assets`）
    - server/initiatives/services/cursor_rules.py（`build_project_cursor_rules` 72-74 现「写入项目记忆**草稿**，由项目成员人工确认后才正式入库」——需同步为 active 直写生效 + 服务端兜底措辞，与 86-01 行为一致）
    - server/tests/initiatives/test_cursor_rules.py（cursor_rules 现有断言——措辞改动需同步更新断言）
    - server/tests/initiatives/test_ide_hook_assets.py（86-03 端点测试范式，扩 kind=write 用例）
  </read_first>
  <action>
    在 `ProjectIdeHookAssetsView` 扩 `kind` 分流：`kind=read` → `build_read_path_assets`（不变）；`kind=write` → `build_write_path_assets`；非法 kind → 400。读权限口径不变（按项目读口径；写路径资产仍是「告诉用户怎么装 hook」的文本，不执行写，按项目读口径下发即可）。

    **cursor_rules 措辞同步（accepted deviation）**：把 `build_project_cursor_rules` 第 3 步「上报内容会经脱敏 + 质量门槛过滤后，写入项目记忆**草稿**，由项目成员人工确认后才正式入库」更新为反映 86-01 行为——「经脱敏 + 质量门槛过滤 + 审计可回滚后**直接写入生效（active）**，无需人工确认；非成员/无 PAT/未绑项目静默跳过」。同步更新 `test_cursor_rules.py` 相关断言。保持文件名/frontmatter/其余结构不变（前端 cursor-rules 端点不破）。

    在 `test_ide_hook_assets.py` 加：端点 `kind=write&runtime=cursor|claude_code|codex` 各取到写路径 bundle（200，含 stop hook 资产）；非法 kind → 400；members_only 非成员遵守读口径。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/initiatives/test_ide_hook_assets.py tests/initiatives/test_cursor_rules.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `GET /api/projects/<id>/ide-hook-assets/?kind=write&runtime=claude_code` 返回 200，含 Stop hook 写路径资产。
    - `kind=read` 行为不变；非法 kind → 400。
    - `build_project_cursor_rules` 措辞反映 active 直写生效（不再说「人工确认」），且保留三道兜底 + 静默跳过说明；`test_cursor_rules.py` 全绿。
    - members_only 项目对非成员遵守项目读口径。
  </acceptance_criteria>
  <done>写路径资产经端点 kind=write 下发 + cursor_rules 措辞同步 active（accepted deviation）+ 测试全绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 写路径资产下发端点 → 用户安装 stop hook | 资产是安装说明文本，按项目读口径下发 |
| 用户本地 stop hook → Friday 写回工具 | 实际写入安全由服务端 86-01/86-04 三道兜底兜住，客户端只是触发器 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-05-01 | Tampering | stop hook 静默直写污染共享记忆/STATE | mitigate | 实际防污染由服务端三道兜底保证（质量门槛/脱敏/审计回滚，86-01/86-04）；客户端脚本仅触发，资产正文显式声明兜底由服务端兜住 |
| T-86-05-02 | Information Disclosure | hook 脚本误上报密钥 | mitigate | 资产正文脱敏告诫「绝不上报凭证/密钥/token」+ 服务端 redact_secrets_in_text 入库前强制兜底 |
| T-86-05-03 | Denial of Service | stop hook 失败阻断编码 | mitigate | 资产模板内置：无 PAT/未绑项目/接口失败 → 静默 exit 0，绝不 block（默认开启但绝不反噬编码） |
| T-86-05-04 | Repudiation | 自动写入无归因 | mitigate | 写回工具服务端绑定 initiated_by_user_id + 审计（86-01/86-04），客户端用用户 PAT 鉴权 |
</threat_model>

## Artifacts this phase produces (Plan 05)

- **新方法（ide_hook_assets.py）**：`build_write_path_assets`（三家 stop hook 脚本/settings：active 写回 + STATE 回写 + 静默不阻断）
- **改动符号（views.py）**：`ProjectIdeHookAssetsView` 扩 `kind=write` 分流
- **改动符号（cursor_rules.py）**：`build_project_cursor_rules` 措辞同步 active 直写生效（accepted deviation，保留三道兜底 + 静默跳过说明）
- **复用（不重造）**：86-03 `build_read_path_assets`/脱敏告诫 helper、86-01 `report_project_knowledge` active、86-04 `report_project_state`、86-03 `ProjectIdeHookAssetsView` 端点
- **call_source**：无新增 LLM 调用（客户端资产为文本/脚本模板）；不涉及 §4.1 新值
- **改动测试**：扩充 `tests/initiatives/test_ide_hook_assets.py`、更新 `tests/initiatives/test_cursor_rules.py`（措辞断言）

<verification>
- `cd server && uv run pytest tests/initiatives/test_ide_hook_assets.py tests/initiatives/test_cursor_rules.py -q` 全绿。
- `cd server && uv run ruff check server/initiatives/services/ide_hook_assets.py server/initiatives/services/cursor_rules.py server/initiatives/views.py` 无新增 lint。
- 端点 `kind=write` 三家 runtime 可下发。
</verification>

<success_criteria>
- 三家 stop hook 资产：默认开启 + 静默回写（active MEMORY/RESEARCH + 结构化 STATE），绝不弹窗/阻断编码（HOOK-02/03 客户端半）。
- 无 PAT / 未绑项目 / 失败 → 静默 exit 0；三道兜底由服务端保证（资产正文声明）。
- cursor_rules 措辞与服务端 active 行为一致（accepted deviation），前端 cursor-rules 端点不破。
</success_criteria>

<output>
Create `.planning/phases/86-ide-context-loop/86-05-SUMMARY.md` when done.
</output>
