---
phase: 83
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - server/initiatives/services/doc_sync_cache.py
  - server/friday/settings.py
  - server/tests/initiatives/test_doc_sync_cache.py
autonomous: true
requirements: [SYNC-05]
must_haves:
  truths:
    - "命中缓存时不读 DB；未命中时回填缓存并返回 DB 渲染结果"
    - "写时 / 收事件时缓存按 doc_id 失效（delete 而非 set 空）"
    - "redis 不可用时静默降级直读 DB（绝不反噬主流程）"
  artifacts:
    - path: "server/initiatives/services/doc_sync_cache.py"
      provides: "read-through 渲染缓存 get/set/invalidate（IGNORE_EXCEPTIONS 降级）"
      exports: ["get_doc_render", "set_doc_render", "invalidate_doc_render", "doc_render_cache_key"]
    - path: "server/tests/initiatives/test_doc_sync_cache.py"
      provides: "命中/未命中/失效/redis 故障降级单测"
      contains: "invalidate_doc_render"
  key_links:
    - from: "server/initiatives/services/doc_sync_cache.py"
      to: "django.core.cache"
      via: "cache.get/set/delete + IGNORE_EXCEPTIONS 降级"
      pattern: "from django.core.cache import cache"
---

<objective>
交付 SYNC-05：项目文件渲染的 read-through 缓存层，写时/收事件失效、TTL 兜底、redis 不可用降级直读 DB。

落点为独立模块 `doc_sync_cache.py`（不绑定 DocSyncService），让 Wave 2+ 的 pull/push 写完后只需调一行 `invalidate_doc_render(doc_id)` 失效，渲染读路径调 `get_doc_render` 命中即返回。模块自包含、可独立单测，与 Wave 1 的 83-01 零文件重叠，可并行。

Purpose: 把缓存做成基础设施薄层，统一缓存键（`projdoc:render:{doc_id}`）与降级语义，复用 Django CACHES 框架（不重造 redis wrapper）。
Output: doc_sync_cache 模块 + settings IGNORE_EXCEPTIONS（A7）+ TTL 常量 + 单测。
</objective>

<execution_context>
@.planning/phases/83-feishu-doc-bi-sync-engine/83-PATTERNS.md
@.planning/phases/83-feishu-doc-bi-sync-engine/83-RESEARCH.md
@.planning/phases/83-feishu-doc-bi-sync-engine/83-CONTEXT.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@server/friday/settings.py
@.cursor/rules/observability-logging.mdc
</context>

<artifacts_produced>
- NEW `server/initiatives/services/doc_sync_cache.py`：`doc_render_cache_key`/`get_doc_render`/`set_doc_render`/`invalidate_doc_render`
- settings.py CACHES `OPTIONS.IGNORE_EXCEPTIONS=True` + `DOC_RENDER_CACHE_TTL` 常量
- NEW `server/tests/initiatives/test_doc_sync_cache.py`
- structlog 事件：`doc_render_cache_degraded`(sampling, component=doc_sync, debug)
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: settings CACHES IGNORE_EXCEPTIONS + TTL 常量（A7）</name>
  <read_first>
    - server/friday/settings.py（CACHES 配置 + REDIS_* + django_redis/LocMem 回退）
    - .planning/phases/83-feishu-doc-bi-sync-engine/83-RESEARCH.md（Code Examples read-through；A7）
  </read_first>
  <action>
    在 redis CACHES backend（`django_redis.cache.RedisCache`）OPTIONS 加 `"IGNORE_EXCEPTIONS": True`（redis 故障静默返回 None/no-op → 降级直读 DB）。LocMem 回退分支不变。新增 `DOC_RENDER_CACHE_TTL = env.int("DOC_RENDER_CACHE_TTL", default=300)`。若已有 OPTIONS，最小 diff 仅追加 IGNORE_EXCEPTIONS，绝不改 backend/LOCATION/channel（零回归）。
  </action>
  <verify>
    <automated>cd server && uv run python -c "import django; django.setup(); from django.conf import settings; print('IGNORE_EXCEPTIONS' in str(settings.CACHES), settings.DOC_RENDER_CACHE_TTL)"</automated>
  </verify>
  <acceptance_criteria>
    - CACHES redis OPTIONS 含 `IGNORE_EXCEPTIONS: True`；`DOC_RENDER_CACHE_TTL` 默认 300。
    - LocMem 回退分支未改动。
  </acceptance_criteria>
  <done>redis CACHES 开 IGNORE_EXCEPTIONS 降级；TTL 常量就位；无 backend 回归。</done>
</task>

<task type="auto">
  <name>Task 2: doc_sync_cache.py read-through 模块 + 降级 + 单测</name>
  <read_first>
    - server/friday/settings.py（Task 1 后的 CACHES + DOC_RENDER_CACHE_TTL）
    - .planning/phases/83-feishu-doc-bi-sync-engine/83-RESEARCH.md（Code Examples render_doc_cached）
    - server/common/logging.py（structlog；sampling 级日志）
    - server/tests/initiatives/test_project_doc_service.py（pytestmark / mock 范式）
  </read_first>
  <action>
    新建 `doc_sync_cache.py`：`from django.core.cache import cache`、`structlog.get_logger(__name__)`。`doc_render_cache_key(doc_id)`→`f"projdoc:render:{doc_id}"`；`get_doc_render(doc_id)` 调 `cache.get(key)` 整段 try/except，异常→debug 记 `doc_render_cache_degraded`(category="sampling", component="doc_sync")+返回 None（降级直读 DB，绝不抛）；`set_doc_render(doc_id, value, *, timeout=None)` 用 `settings.DOC_RENDER_CACHE_TTL` 默认 timeout，try/except 吞异常；`invalidate_doc_render(doc_id)` 调 `cache.delete(key)`（**delete 而非 set 空**，Pitfall 7），try/except 吞异常。不记正文（只记 doc_id/key 计数）。
    `test_doc_sync_cache.py`：`override_settings` + LocMemCache 验证命中/未命中/失效；monkeypatch 让 `cache.get` 抛异常验证降级返回 None 不抛 + 记一次 degraded。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/initiatives/test_doc_sync_cache.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/initiatives/test_doc_sync_cache.py -x` 全绿。
    - 覆盖：命中返回缓存值（不查 DB）、未命中 None、invalidate 后 get None、cache.get 抛异常→get_doc_render 返回 None 不抛。
    - grep 确认 invalidate 用 `cache.delete`（非 set 空）；日志无正文。
  </acceptance_criteria>
  <done>read-through get/set/invalidate、redis 故障降级直读 DB、失效用 delete；单测覆盖四路径。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 缓存层 → 渲染内容 | 渲染 value 可能含飞书正文片段；不入日志、TTL 过期 |
| redis 外部服务 → 应用 | redis 故障不得反噬主流程 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83-05-DOS | Denial of Service | redis 故障 | mitigate | IGNORE_EXCEPTIONS + try/except 降级直读 DB，缓存故障绝不阻断渲染 |
| T-83-05-INFO | Information Disclosure | 缓存日志 | mitigate | 日志只记 doc_id/key/计数，不记渲染正文；degraded 事件 sampling+debug |
| T-83-05-STALE | Tampering | 失效语义 | accept | delete 失效，下次读回填；秒级最终一致（亚秒 OT 不在本期） |
</threat_model>

<verification>
- `cd server && uv run pytest tests/initiatives/test_doc_sync_cache.py -x` 全绿。
- settings 自检：IGNORE_EXCEPTIONS 已开、TTL 常量可读。
</verification>

<success_criteria>
- read-through 命中不查 DB、未命中回填、写/事件失效用 delete。
- redis 不可用静默降级直读 DB，绝不反噬主流程；日志无正文泄漏。
</success_criteria>

<output>
完成后创建 `.planning/phases/83-feishu-doc-bi-sync-engine/83-05-SUMMARY.md`
</output>
