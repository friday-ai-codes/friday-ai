---
phase: 82-project-workspace-entity
plan: 02
type: execute
wave: 2
depends_on: ["82-01"]
files_modified:
  - server/services/feishu_doc.py
  - server/initiatives/services/project_doc_service.py
  - server/initiatives/services/project_service.py
  - server/initiatives/services/__init__.py
  - server/audit/services/taxonomy.py
  - server/tests/services/test_feishu_doc_create_folder.py
  - server/tests/initiatives/test_project_doc_service.py
  - server/tests/initiatives/test_project_doc_inv6_guard.py
autonomous: true
requirements: [WS-04, DOC-01, DOC-02, DOC-03, DOC-04, DOC-05, DOC-06]
user_setup:
  - service: feishu
    why: "真实建文件夹 + 5 文件 + 看板描述写入需真实飞书 app 凭证（Drive/Docx scope）"
    env_vars: []
    dashboard_config:
      - task: "确认 Drive v1 create_folder 端点形态（body {name, folder_token} → data.token）与工作项 description field_key"
        location: "飞书开放平台 Drive v1 / 项目 OpenAPI（A1/A2 MEDIUM，需 live 验证）"
must_haves:
  truths:
    - "FeishuDocClient.create_folder(name, folder_token) 返回新文件夹 token，限流走 RateLimitError 退避"
    - "ProjectDocService 是 ProjectDoc/ProjectDocBlockMap/ProjectStateApi 的唯一写入入口（INV-6）"
    - "建项目后台串行创建飞书文件夹 + 5 文件并落 token，任一步失败置对应 ProjectDoc.sync_status=broken（持久化 DB，非内存）"
    - "后台 provision 经 run_in_background(initiated_by_user_id=) 携带触发用户并 worker 入口 re-bind；未知归因记 system"
    - "飞书外呼任何失败 fail-soft，绝不阻断项目创建主流程"
    - "5 文件头部导航 block 互链（其余 4 文件 + 看板 + Friday 项目页）；看板描述 read-then-append「📁 项目工作区」段，幂等不覆盖"
    - "INV-6 guard 覆盖三个新模型且不被前缀重叠误伤/漏守"
  artifacts:
    - path: "server/services/feishu_doc.py"
      provides: "FeishuDocClient.create_folder"
      contains: "async def create_folder"
    - path: "server/initiatives/services/project_doc_service.py"
      provides: "ProjectDocService 单一写入入口 + provision_workspace/rebuild_workspace 后台编排"
      contains: "class ProjectDocService"
    - path: "server/tests/initiatives/test_project_doc_inv6_guard.py"
      provides: "三模型旁路写守护 + writer-actually-writes 有效性断言"
      contains: "_MODELS"
  key_links:
    - from: "server/initiatives/services/project_service.py"
      to: "ProjectDocService.provision_dispatch"
      via: "create() 成功后 best-effort 后台派发"
      pattern: "provision_dispatch|provision_workspace"
    - from: "server/initiatives/services/project_doc_service.py"
      to: "services.background_runner.run_in_background"
      via: "传 factory + initiated_by_user_id"
      pattern: "run_in_background"
    - from: "server/initiatives/services/project_doc_service.py"
      to: "FeishuDocClient.create_folder / create_document"
      via: "串行 await（绝不 asyncio.gather）"
      pattern: "create_folder|create_document"
---

<objective>
落地"项目工作区"的写入与外呼层：① 给 `FeishuDocClient` 补 `create_folder`（库内缺失，镜像 `create_document` 的 token/headers/retry/错误码）；② 建 `ProjectDocService`——`ProjectDoc`/`ProjectDocBlockMap`/`ProjectStateApi` 的唯一写入入口（INV-6）+ `set_folder_token`（落到 `Project`，经 ProjectService）+ `provision_workspace` 后台编排（串行建飞书文件夹 → 5 文件 → 落 token → 互链 → 看板描述追加，失败置 broken，fail-soft）；③ 建 INV-6 多模型 grep 守护；④ 单测覆盖 create_folder respx 形状、service 写入、provision happy/broken 路径。

Purpose: 兑现 WS-04（每项目飞书文件夹 + 5 文件落 token）、DOC-01~05（5 文件容器首建）、DOC-06（互链 + 看板描述可打开）。本 plan 不做飞书↔Friday 双向同步循环（Phase 83）——只做单向首建落地。
Output: create_folder 方法 + ProjectDocService（含全部 3 模型写入方法）+ provision/rebuild 编排 + 新审计 action + INV-6 guard + 3 个测试文件。
</objective>

<execution_context>
@.cursor/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/82-project-workspace-entity/82-CONTEXT.md
@.planning/phases/82-project-workspace-entity/82-RESEARCH.md
@.planning/phases/82-project-workspace-entity/82-01-PLAN.md
@.cursor/rules/observability-logging.mdc

@server/initiatives/services/project_service.py
@server/services/feishu_doc.py
@server/services/background_runner.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FeishuDocClient.create_folder + respx 形状/限流单测</name>
  <files>
    server/services/feishu_doc.py,
    server/tests/services/test_feishu_doc_create_folder.py
  </files>
  <read_first>
    - server/services/feishu_doc.py（被改文件：`create_document` line 177-250 的 @retry 装饰、get_tenant_access_token、错误码 99991400/RateLimitError 处理、OPEN_API_BASE）
    - 82-RESEARCH.md「Pitfall 1」「Code Examples / 飞书 create_folder」（端点 `POST /drive/v1/files/create_folder`，body `{name, folder_token}`，返回 `data["data"]["token"]`，A1 MEDIUM 需 live 验证；5QPS/不可并发）
    - 既有 httpx respx mock 测试范式（参考任一 tests/services 内 respx 用例 + token 缓存预置避免真实鉴权）
  </read_first>
  <behavior>
    - code==0 → 返回 `data["data"]["token"]`
    - code==99991400 或 msg 含 "rate limit" → 抛 RateLimitError（@retry 退避，stop_after_attempt(3)）
    - code!=0 其它 → 抛 FeishuDocAPIError
    - 返回体缺 token → 抛 FeishuDocAPIError
  </behavior>
  <action>
    在 `FeishuDocClient` 增 `async def create_folder(self, name: str, folder_token: str) -> str`，镜像 `create_document` 的 `@retry(stop=stop_after_attempt(3), wait=wait_exponential(min=4,max=60), retry=retry_if_exception_type(RateLimitError), reraise=True)`；内部 `get_tenant_access_token()` → `httpx.AsyncClient` POST `f"{self.OPEN_API_BASE}/drive/v1/files/create_folder"`，headers Bearer + Content-Type json，body `{"name": name, "folder_token": folder_token}`；按 `create_document` 同款分支处理 code（99991400/含 rate limit → RateLimitError；非 0 → FeishuDocAPIError），成功取 `data["data"]["token"]`，缺失抛 FeishuDocAPIError。`logger.info("feishu_folder_created", folder_token=<父>, new_token=<返回>)`——只记 token 末段/计数语义，**不记** name 全文外的敏感正文。标注 `# A1: 端点形态 MEDIUM，需 live 验证`。
    新建 tests/services/test_feishu_doc_create_folder.py：respx mock 上述四种响应形状（成功/99991400 重试后成功/非 0 错误/缺 token），预置 `_tenant_token` 跳过真实鉴权；断言返回 token、断言 RateLimitError 重试、断言 FeishuDocAPIError。
  </action>
  <acceptance_criteria>
    - <automated>cd server && uv run pytest tests/services/test_feishu_doc_create_folder.py -x</automated> 全绿。
    - grep 断言 feishu_doc.py 含 `async def create_folder` 且端点字符串含 `/drive/v1/files/create_folder`。
    - 测试覆盖：成功返回 token、99991400→RateLimitError、非 0→FeishuDocAPIError、缺 token→FeishuDocAPIError 四例。
  </acceptance_criteria>
  <done>create_folder 落地并镜像 create_document 的鉴权/重试/错误码；respx 形状测试全绿。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ProjectDocService 写入入口 + set_folder_token + 审计 action + INV-6 guard</name>
  <files>
    server/initiatives/services/project_doc_service.py,
    server/initiatives/services/project_service.py,
    server/initiatives/services/__init__.py,
    server/audit/services/taxonomy.py,
    server/tests/initiatives/test_project_doc_service.py,
    server/tests/initiatives/test_project_doc_inv6_guard.py
  </files>
  <read_first>
    - server/initiatives/services/project_service.py（镜像：async + `@sync_to_async _xxx_locked` + transaction.atomic + AuditService.aemit metadata{component,category,initiated_by_user_id} + apush_project_event；line 64-216 create/update 范式）
    - server/initiatives/services/__init__.py（被改文件：re-export 清单 line 7-54）
    - server/audit/services/taxonomy.py（被改文件：常量 + __all__ + ALL_ACTIONS 三处同步登记范式 line 23-177；object.verb 命名）
    - server/tests/initiatives/test_artifact_inv6_guard.py（多模型 guard 范式：_MODELS、前缀重叠跳过逻辑 line 73-79、writer-actually-writes 有效性断言 line 87-94）
    - 82-RESEARCH.md「Pattern 2」「Pitfall 3」（set_folder_token 专用方法，不进 update 白名单）「Pitfall 4」（ProjectDoc/ProjectDocBlockMap 前缀重叠需跳过）
  </read_first>
  <behavior>
    - upsert_doc(project_id, doc_type, **fields) → get_or_create，按 (project, doc_type) 幂等
    - upsert_state_api(project_id, method, path, params, status, source) → get_or_create 按 (project, method, path) 幂等 + 审计 state_api_added
    - remove_state_api(project_id, api_id) → 不存在返回 False（幂等）
    - upsert_block_map(doc_id, feishu_block_id, db_ref, section, content_hash) → get_or_create/update 按 (doc, feishu_block_id) 幂等
    - set_sync_status(doc_id, status) → 持久化 sync_status（broken 落 DB）
    - ProjectService.set_folder_token(project_id, token) → 写 Project.feishu_folder_token（select_for_update + save update_fields）
  </behavior>
  <action>
    新建 initiatives/services/project_doc_service.py：`class ProjectDocService`，逐字镜像 ProjectService 的 async + `@sync_to_async _xxx_locked`(transaction.atomic + select_for_update) 范式。实现写入方法：`upsert_doc`、`set_doc_feishu`(doc_id, document_id, doc_token, sync_status=READY)、`set_sync_status`、`upsert_block_map`、`upsert_state_api`、`remove_state_api`。写操作（state_api 增删、doc provision 完成）经 `AuditService.aemit(action=<常量>, target_type="project_state_api"/"project_doc", metadata={"component":"initiatives","category":"caller","initiated_by_user_id": str(actor_id) if actor_id else "system"}, source="api")`。三模型的 `.objects.create/get_or_create/update_or_create` **只允许出现在本文件**。

    在 ProjectService 增 `async def set_folder_token(self, *, project_id, token, actor=None, initiated_by_user_id=None)`（专用方法，不塞进 update 白名单，per Pitfall 3）——`@sync_to_async` select_for_update + save(update_fields=["feishu_folder_token","updated_at"]) + 审计 `ACTION_PROJECT_WORKSPACE_PROVISIONED`。

    在 taxonomy.py 三处（__all__ / 具名常量 / ALL_ACTIONS）同步登记：`ACTION_PROJECT_WORKSPACE_PROVISIONED="project.workspace_provisioned"`、`ACTION_PROJECT_STATE_API_ADDED="project.state_api_added"`、`ACTION_PROJECT_STATE_API_REMOVED="project.state_api_removed"`。在 initiatives/services/__init__.py re-export `ProjectDocService`（+ 任何新异常类）。

    新建 tests/initiatives/test_project_doc_inv6_guard.py：镜像 test_artifact_inv6_guard.py 多模型版，`_MODELS=("ProjectDoc","ProjectDocBlockMap","ProjectStateApi")`、`_ALLOWED_WRITER="initiatives/services/project_doc_service.py"`，处理 `ProjectDoc(` 误伤 `ProjectDocBlockMap(` 的前缀重叠（参照 artifact guard 对 Artifact/ArtifactType 的跳过逻辑），并加 writer-actually-writes 有效性断言（writer 文件确含三模型的 `.objects.create`/`get_or_create`）。

    新建 tests/initiatives/test_project_doc_service.py：覆盖 upsert_doc/upsert_state_api/upsert_block_map/set_sync_status/remove_state_api 的写入与幂等（唯一约束命中返回既有），以及 ProjectService.set_folder_token 落字段（pytest-django + asyncio）。
  </action>
  <acceptance_criteria>
    - <automated>cd server && uv run pytest tests/initiatives/test_project_doc_service.py tests/initiatives/test_project_doc_inv6_guard.py -x</automated> 全绿。
    - <automated>cd server && uv run pytest tests/initiatives/test_project_inv6_guard.py tests/initiatives/test_artifact_inv6_guard.py -x</automated> 既有 guard 零回归（新 service 不触发误报）。
    - INV-6 guard：故意在 service 外加一行 `ProjectDoc.objects.create(` 临时验证 guard 变红、删除后变绿（执行期自检，最终代码不留该行）。
    - grep 断言新审计 3 常量同时出现在 taxonomy.py 的 `__all__`、具名定义、`ALL_ACTIONS` 三处。
    - grep 断言 `ProjectDoc.objects`/`ProjectDocBlockMap.objects`/`ProjectStateApi.objects` 的写方法仅出现在 project_doc_service.py。
  </acceptance_criteria>
  <done>三模型唯一写入入口 + set_folder_token + 审计常量就位；INV-6 多模型 guard 全绿且既有 guard 零回归。</done>
</task>

<task type="auto">
  <name>Task 3: provision_workspace 后台编排（文件夹+5 文件+互链+看板）+ DOC-06 + 观测 + provision/broken 测试</name>
  <files>
    server/initiatives/services/project_doc_service.py,
    server/initiatives/services/project_service.py,
    server/tests/initiatives/test_project_doc_service.py
  </files>
  <read_first>
    - server/services/background_runner.py（run_in_background 传 factory 不传 coroutine、initiated_by_user_id worker re-bind、line 111-210；进程内重启即丢，故 broken 状态须落 DB）
    - server/agents/tools/feishu_doc_tools.py（create_feishu_doc_client_for_project(project: Space) 工厂入参是 Space，line 47-80；Pitfall 5）
    - server/services/feishu.py（update_work_item_fields(project_key, work_item_id:int, work_item_type, fields) line 206-246；Pitfall 6 read-then-append 须先 get_work_item，work_item_id 是 int、type 必填 fail-loud）
    - server/services/feishu_doc.py（create_document(title, folder_token, content)、append_markdown line 252-271 用于互链 nav block 追加）
    - server/common/logging.py（redact_secrets_in_text；飞书上游响应体/异常文本脱敏）
    - 82-RESEARCH.md「Pattern 3/4」「Pitfall 5/6/7」「观测与日志强制项」（provision started/completed/failed + duration_ms + initiated_by_user_id；串行不并发；select_related("space") 防 async lazy FK）
  </read_first>
  <action>
    在 ProjectDocService 增 `provision_dispatch(project_id, initiated_by_user_id)`（同步调度入口，内部 `run_in_background(lambda: self._provision_workspace_coro(project_id), name=f"project-workspace:{project_id}", initiated_by_user_id=str(initiated_by_user_id) if initiated_by_user_id else None)`）与 `async def _provision_workspace_coro(project_id)` + `async def rebuild_workspace(project_id, initiated_by_user_id=None)`（rebuild = 重新派发 provision，供 82-05 端点调用）。

    _provision_workspace_coro 严格**串行**（绝不 asyncio.gather，create_folder 5QPS/不可并发）：① 取 `Project.objects.select_related("space").aget(pk=project_id)`（Pitfall 7 预取 space）；space 无 `feishu_doc_folder_token` 或无飞书凭证 → 为 5 个 doc upsert_doc(sync_status=BROKEN) + warning 返回（fail-soft，不抛）。② `client = await create_feishu_doc_client_for_project(project.space)`（入参 Space，Pitfall 5）。③ `folder_token = await client.create_folder(name=f"{project.name} 工作区", folder_token=project.space.feishu_doc_folder_token)` → `await ProjectService().set_folder_token(project_id=, token=folder_token, initiated_by_user_id=)`。④ 顺序遍历 `DocType` 五值，每个 `create_document(title, folder_token, content=<zh-CN 初始模板>)`（per-doc 串行）→ `upsert_doc(... feishu_document_id, feishu_doc_token, sync_status=READY)`；单文件失败 → 该 doc set_sync_status(BROKEN) + warning，**继续**其余（fail-soft）。⑤ 互链（DOC-06）：5 文件都就绪后，为每个文档 `append_markdown` 一个头部导航段（链到其余 4 文件 + 看板 url + Friday 项目页路径 `/projects/{id}`）——用 block 级 append（绝不整篇 replace，为 Phase 83 留接口）。⑥ 看板描述追加（DOC-06）：若 `project.feishu_project_key` + 看板工作项 id/type 齐备，先 `get_work_item` 读现描述，检测无「📁 项目工作区」marker 才 `update_work_item_fields` 追加该段（幂等不覆盖，Pitfall 6）；缺 key/缺 work_item → 跳过（fail-soft）。整个 coro 包 try/except，飞书上游响应体/异常文本经 `redact_secrets_in_text` 后才入日志，日志只记 doc_id/计数/sync_status，**绝不**记正文/token 明文。

    观测：coro 入口 `logger.info("project_workspace_provision_started", project_id=, initiated_by_user_id=, component="initiatives", category="caller")`；终点 `..._completed` 带 `duration_ms` + ready/broken 计数；任一飞书外呼失败 `..._failed` warning（best-effort，绝不 re-throw）。

    在 ProjectService.create 成功（created=True）分支末尾 best-effort 调用 `ProjectDocService().provision_dispatch(project.id, initiated_by_user_id=actor_id)`（函数级 import 避免循环依赖；整段 try/except 吞异常 + warning，绝不阻断 create 返回）——覆盖所有建项目路径（WS-04「每项目」）。

    扩 tests/initiatives/test_project_doc_service.py：用 respx mock 飞书 create_folder/create_document/get_work_item/update_work_item_fields，断言 provision happy（5 doc READY + folder_token 落 Project + 看板追加调用一次 + 互链 append 调用）；broken 路径（create_document 抛 FeishuDocAPIError → 对应 doc sync_status=BROKEN，其余继续，主流程不抛）；归因（provision 经 run_in_background 携带 initiated_by_user_id）。可用 `services.background_runner.wait_for_pending` 或直接 await `_provision_workspace_coro` 同步验证编排（避免依赖后台线程时序）。
  </action>
  <acceptance_criteria>
    - <automated>cd server && uv run pytest tests/initiatives/test_project_doc_service.py -x</automated> 全绿（含 provision happy + broken + 看板幂等追加）。
    - 测试断言：飞书失败时对应 `ProjectDoc.sync_status == "broken"` 且持久化到 DB（重新 aget 仍为 broken）。
    - 测试断言：provision 编排为串行（不使用 asyncio.gather）——grep 断言 _provision_workspace_coro 体内无 `asyncio.gather`。
    - 测试断言：看板描述追加为 read-then-append——已含 marker 时不再调用 update_work_item_fields（幂等）。
    - grep 断言日志/审计无飞书 token/正文明文落点（provision 路径只 log doc_id/计数）。
  </acceptance_criteria>
  <done>建项目后台串行 provision 飞书文件夹+5 文件+互链+看板描述落地，失败置 broken 持久化、fail-soft 不阻断；观测/脱敏/归因合规；provision 测试全绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Friday 后台 worker → 飞书 OpenAPI | 外呼携带 tenant_access_token；上游响应体/异常文本不可信，可能含敏感 |
| 服务写入面 → DB | 三个新模型写入必须收口 ProjectDocService |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-04 | Tampering | 旁路写 ProjectDoc/BlockMap/StateApi 绕过审计 | mitigate | INV-6 多模型 grep guard（test_project_doc_inv6_guard）+ writer-actually-writes 有效性断言 |
| T-82-05 | Information Disclosure | 飞书正文/doc-token 泄漏进日志 | mitigate | redact_secrets_in_text 处理上游响应体/异常；日志只记 doc_id/计数/sync_status，绝不记 token/正文 |
| T-82-06 | Repudiation | 后台 provision 归因丢失 | mitigate | run_in_background(initiated_by_user_id=) + worker re-bind；未知记 "system" |
| T-82-07 | Denial of Service | create_folder 5QPS/不可并发被并发撞限流 | mitigate | per-task 内文件夹+5 文件全部串行 await（禁止 asyncio.gather）+ @retry 退避 |
| T-82-08 | Tampering | 看板描述被整篇覆盖 | mitigate | read-then-append + marker 幂等（Pitfall 6），绝不整篇 replace |
| T-82-09-SC | Tampering | 包安装 | accept | 本 phase 不安装任何外部包（82-RESEARCH Package Legitimacy Audit: SKIPPED） |
</threat_model>

<verification>
- `cd server && uv run pytest tests/services/test_feishu_doc_create_folder.py tests/initiatives/test_project_doc_service.py tests/initiatives/test_project_doc_inv6_guard.py -x` 全绿。
- `cd server && uv run pytest tests/initiatives/ -x` 既有用例零回归。
- 手动（需真实飞书凭证）：建项目后飞书出现专属文件夹 + 5 文件 + 看板描述「📁 项目工作区」段可打开；断网/无凭证则 5 doc 置 broken 且项目创建成功（fail-soft）。
</verification>

<success_criteria>
- WS-04：每项目经 create_folder 建飞书专属文件夹（父=Space folder），5 文件用 create_document(folder_token=…) 建于其下，DB 存 folder/doc token（Success Criteria 3）。
- DOC-01~05：5 文件容器首建落地。
- DOC-06：5 文件头部互链 + 看板描述追加可打开（Success Criteria 4）。
- 失败 fail-soft 置 broken（持久化），为 82-05 一键重建端点留状态。
</success_criteria>

<output>
完成后创建 `.planning/phases/82-project-workspace-entity/82-02-SUMMARY.md`。
</output>
