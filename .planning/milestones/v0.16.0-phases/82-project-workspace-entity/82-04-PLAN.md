---
phase: 82-project-workspace-entity
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/components/layout/AppSidebar.vue
  - web/src/pages/projects/index.vue
  - web/src/pages/projects/__tests__/projects-list.spec.ts
  - web/src/pages/projects/__tests__/app-sidebar-nav.spec.ts
autonomous: true
requirements: [WS-01]
must_haves:
  truths:
    - "侧边栏 mainNavItems 含「项目」入口，位置在「首页」之下、「空间」之上"
    - "/projects 页默认展示当前所选空间的项目（不是全局跨空间）"
    - "所选空间用 localStorage 键 projects-selected-space 本地记忆，刷新后沿用"
    - "列表仍支持状态/成员/搜索筛选（既有能力不回退）"
    - "全量 zh-CN 文案，vue-tsc 绿"
  artifacts:
    - path: "web/src/components/layout/AppSidebar.vue"
      provides: "mainNavItems 新增 /projects 入口（index 1，首页与空间之间）"
      contains: "/projects"
    - path: "web/src/pages/projects/index.vue"
      provides: "spaceFilter 用 useLocalStorage('projects-selected-space') 记忆"
      contains: "projects-selected-space"
  key_links:
    - from: "web/src/pages/projects/index.vue"
      to: "@vueuse/core useLocalStorage"
      via: "spaceFilter 持久化所选空间"
      pattern: "useLocalStorage\\('projects-selected-space'"
    - from: "web/src/components/layout/AppSidebar.vue"
      to: "/projects 路由"
      via: "mainNavItems 注册导航项"
      pattern: "label: '项目'"
---

<objective>
前端落地工作区一等入口（WS-01）：侧边栏 `mainNavItems` 在「首页」与「空间」之间插入「项目」tab；复用既有 `/projects` 页（Phase 81），把空间筛选默认改为"当前所选空间"并用 `@vueuse/core useLocalStorage('projects-selected-space')` 本地记忆（前端本地，无后端偏好）。zh-CN i18n、vue-tsc 绿、不破前端基线。

Purpose: 兑现 Success Criteria 5 的"侧边栏「项目」tab（首页↓空间↑）"与 D-08 的"默认按所选空间 + localStorage 记忆"。本 plan 纯前端导航 + 本地偏好，独立于后端改动，可与 Wave 1 的 82-01 并行。
Output: 侧边栏导航项 + 项目页默认空间记忆 + vitest 守护（localStorage 默认 + 侧边栏顺序）。
</objective>

<execution_context>
@.cursor/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/82-project-workspace-entity/82-CONTEXT.md
@.planning/phases/82-project-workspace-entity/82-RESEARCH.md

@web/src/components/layout/AppSidebar.vue
@web/src/pages/projects/index.vue
@web/src/pages/projects/__tests__/projects-list.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 侧边栏「项目」tab（首页↓空间↑）+ 顺序守护测试</name>
  <files>
    web/src/components/layout/AppSidebar.vue,
    web/src/pages/projects/__tests__/app-sidebar-nav.spec.ts
  </files>
  <read_first>
    - web/src/components/layout/AppSidebar.vue（被改文件：`mainNavItems` line 89-100，index 0 首页 / index 1 空间；NavItem 形态 line 19-26；图标用 lucide）
    - 82-RESEARCH.md「Code Examples / 前端侧边栏插 tab」（在首页 index0 与空间 index1 之间插入 `{ to: '/projects', label: '项目', icon: 'lucide--folder-kanban' }`）
  </read_first>
  <action>
    在 AppSidebar.vue `mainNavItems` 数组的首项（`{ to: '/', label: '首页', ... }`）之后、空间项（`{ to: '/spaces', ... }`）之前，插入 `{ to: '/projects', label: '项目', icon: 'lucide--folder-kanban' }`（label 沿用侧边栏既有硬编码中文风格，非 i18n key——与同数组其它项一致）。不改其它导航项。

    新建 web/src/pages/projects/__tests__/app-sidebar-nav.spec.ts：以 Node fs 读取 AppSidebar.vue 源文本（不挂载组件，避免 auto-import/store 依赖），用正则断言 `'/projects'` 在 `mainNavItems` 中出现且位于 `'/'`（首页）之后、`'/spaces'`（空间）之前（按字符串首次出现下标比较）。这是确定性静态守护，验证插入顺序不被回归。
  </action>
  <acceptance_criteria>
    - <automated>cd web && pnpm vitest run src/pages/projects/__tests__/app-sidebar-nav.spec.ts</automated> 全绿。
    - 测试断言：源文本中 `'/'` 入口下标 < `'/projects'` 下标 < `'/spaces'` 下标。
    - <automated>cd web && pnpm vue-tsc --noEmit</automated> 无新增类型错误。
  </acceptance_criteria>
  <done>侧边栏在首页与空间之间出现「项目」入口；顺序守护测试 + vue-tsc 绿。</done>
</task>

<task type="auto">
  <name>Task 2: /projects 默认按所选空间 + localStorage 记忆 + 列表守护扩充</name>
  <files>
    web/src/pages/projects/index.vue,
    web/src/pages/projects/__tests__/projects-list.spec.ts
  </files>
  <read_first>
    - web/src/pages/projects/index.vue（被改文件：`spaceFilter = ref<string>(ALL)` line 36、filters computed line 49-60、空间下拉 line 128-141）
    - web/src/pages/projects/__tests__/projects-list.spec.ts（被改文件：现有 mount + i18n + listMock 范式 line 1-106；vi.mock 范式）
    - 82-RESEARCH.md「Code Examples / projects/index.vue 所选空间记忆」（`useLocalStorage('projects-selected-space', '__all__')`）；AppSidebar 已有 `useLocalStorage('sidebar-collapsed')` 范式
    - 82-CONTEXT.md `<decisions>`「侧边栏「项目」tab 范围」（默认展示当前所选空间、localStorage 本地记忆、列表仍支持状态/成员筛选）
  </read_first>
  <action>
    在 index.vue 把 `const spaceFilter = ref<string>(ALL)` 改为 `const spaceFilter = useLocalStorage<string>('projects-selected-space', ALL)`（从 `@vueuse/core` 引入 useLocalStorage；ALL 常量 `__all__` 既有）。其余 filters computed 逻辑、状态/成员/搜索筛选、空间下拉绑定均不变——这样用户选空间后写入 localStorage，刷新/重进沿用所选空间（默认仍 `__all__` 全部空间，首次无记忆时不破坏现状）。不引入后端偏好接口。保持全量 zh-CN（既有 t() 文案不动）。

    扩 projects-list.spec.ts：新增一例——预置 `localStorage.setItem('projects-selected-space', 's1')`，mount 页后断言 `projectsApi.list` 被以包含 `space_id: 's1'` 的 filters 调用（验证 localStorage 默认空间生效驱动查询）；再清空 localStorage 验证默认 `__all__` 不带 space_id。复用既有 listMock/spaces mock 范式。
  </action>
  <acceptance_criteria>
    - <automated>cd web && pnpm vitest run src/pages/projects/__tests__/projects-list.spec.ts</automated> 全绿（含既有 4 例 + 新增 localStorage 默认例）。
    - 测试断言：localStorage 有 `projects-selected-space=s1` 时 list 调用 filters 含 `space_id: 's1'`。
    - 测试断言：无记忆时（ALL）list 调用 filters 不含 space_id。
    - <automated>cd web && pnpm vue-tsc --noEmit</automated> 无新增类型错误。
  </acceptance_criteria>
  <done>/projects 默认按 localStorage 所选空间过滤、刷新沿用；既有筛选不回退；vitest + vue-tsc 绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 浏览器 localStorage → 前端查询参数 | 仅存所选 space_id（非敏感），驱动既有后端 list（后端权限按 Space 成员可见过滤，不受前端记忆影响） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-14 | Information Disclosure | localStorage 存所选空间 | accept | 仅存非敏感 space_id；后端 list 仍按 Space 成员可见 fail-closed 过滤，前端记忆不放宽任何权限 |
| T-82-15-SC | Tampering | 前端依赖安装 | accept | 本 plan 不新增任何 npm 依赖（复用既有 @vueuse/core useLocalStorage） |
</threat_model>

<verification>
- `cd web && pnpm vitest run src/pages/projects/__tests__/` 全绿。
- `cd web && pnpm vue-tsc --noEmit` 绿。
- 手动观感：侧边栏「项目」在首页与空间之间；选空间后刷新沿用所选空间。
</verification>

<success_criteria>
- WS-01：侧边栏「项目」tab（首页↓空间↑），点进为项目工作台；/projects 默认按所选空间 + localStorage 记忆（Success Criteria 5 前端半）。
</success_criteria>

<output>
完成后创建 `.planning/phases/82-project-workspace-entity/82-04-SUMMARY.md`。
</output>
