---
phase: 128-initiative-profile-team-gate
# 专项画像模块（PROF）
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/services/process_runtime/initiative_profile.py
  - server/agents/call_source.py
  - server/tests/services/process_runtime/test_initiative_profile.py
  - .planning/observability/LOGGING-SPEC.md
autonomous: true
requirements:
  - PROF-01
  - PROF-02
  - PROF-03
user_setup: []

must_haves:
  truths:
    - "给定含模块总览/简述的 feature list，产出可 JSON 序列化的专项画像（产品形态、域、brownfield|greenfield|fix、主能力簇、显式非目标、复用声明摘要）"
    - "主路径语料排除 acceptance/测试 case；仅有操作细节时返回 clarify(insufficient_profile_corpus)，不静默噪声画像"
    - "画像失败 fail-soft：带 degrade_reason，不抛垮调用方；观测含 request_id/run_id 绑定字段且不写需求原文全文"
  artifacts:
    - path: server/services/process_runtime/initiative_profile.py
      provides: "InitiativeProfile 形状 + corpus 选择 + build_profile 入口"
      exports: ["InitiativeProfile", "build_profile", "select_profile_corpus"]
    - path: server/tests/services/process_runtime/test_initiative_profile.py
      provides: "成功 / 语料不足 clarify / fail-soft degrade 测试"
  key_links:
    - from: build_profile
      to: CallSource / structlog
      via: "LLM 调用带 call_source；日志 category + component，fail-soft"
      pattern: "initiative_profile|build_profile|insufficient_profile_corpus"
---

<objective>
交付可单测的专项画像模块：从 feature list 产出机读画像，语料剔除测试/验收正文，失败 fail-soft 并写入可观测结构（PROF-01/02/03）。

Purpose: 漏斗后续阶段（门禁/shortlist）依赖稳定画像输入；本 plan 不接线三入口。
Output: `initiative_profile.py` + CallSource/LOGGING 登记 + 单元测试。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/128-initiative-profile-team-gate/128-CONTEXT.md
@.planning/phases/128-initiative-profile-team-gate/128-RESEARCH.md
@.planning/milestones/v0.23.0-DECISIONS.md
@.planning/REQUIREMENTS.md
@server/initiatives/services/feature_list_extractor.py
@server/initiatives/services/repo_association_service.py
@server/agents/call_source.py
@.planning/observability/LOGGING-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: InitiativeProfile 契约 + corpus 选择器</name>
  <files>server/services/process_runtime/initiative_profile.py, server/tests/services/process_runtime/test_initiative_profile.py</files>
  <behavior>
    - 合成含 modules[].summary / features 描述的 feature list → select_profile_corpus 包含简述与功能描述，不包含 acceptance 正文
    - 仅有 acceptance / 操作步骤语料 → select_profile_corpus 判定不足 → build_profile 返回 status=clarify, clarify_reason=insufficient_profile_corpus
    - InitiativeProfile 字段可 asdict/JSON 序列化：product_form, domains, change_kind(brownfield|greenfield|fix), capability_clusters, non_goals, reuse_summary
  </behavior>
  <action>
    新建 `initiative_profile.py`（per CONTEXT）：用 dataclass 或 TypedDict 定义 `InitiativeProfile` 与统一结果包装（含 `status` ∈ ok|clarify|degraded、`clarify_reason`、`degrade_reason`、`profile`）。实现 `select_profile_corpus(feature_list|features_flat|modules)`：主路径输入优先模块总览/简述、全局流转摘要、功能 name+description；**默认剔除** `acceptance` / 测试 case 正文（对齐锁定决策「画像语料排除测试 case」与 PROF-02）。语料不足（无简述/模块总览且仅有操作细节）时不调用 LLM，直接 clarify。禁止在 action 中内联大段实现代码；形状以可 JSON 进 stage 观测为准。本 task 可先用规则/stub 抽取填字段，Task 2 再接 LLM。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/process_runtime/test_initiative_profile.py -q --tb=short -k "corpus or clarify or shape"</automated>
  </verify>
  <done>画像形状可序列化；语料剔除验收项；不足语料走 clarify(insufficient_profile_corpus)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: LLM 画像抽取 + fail-soft 观测</name>
  <files>server/services/process_runtime/initiative_profile.py, server/agents/call_source.py, .planning/observability/LOGGING-SPEC.md, server/tests/services/process_runtime/test_initiative_profile.py</files>
  <behavior>
    - mock LLM 返回合法 JSON → status=ok，字段齐全（PROF-01）
    - LLM 抛错 / 非法 JSON → status=degraded 或 clarify，带 degrade_reason，不向上抛（PROF-03）
    - 日志事件含 started/completed/failed + duration_ms + category + component；不出现需求原文全文
  </behavior>
  <action>
    实现 `build_profile(..., initiated_by_user_id=, request_id=, run_id=)`：在 corpus 充足时用既有 LLM 调用栈抽取画像（Claude's Discretion：新增 `CallSource` 值如 `initiative_profile`，并在 LOGGING-SPEC §4.1 补一行；temperature/idempotency 对齐 blueprint 路径）。包 `use_call_source`。structlog：`initiative_profile_started/completed/failed`，`category` 用 `sampling`（内部步骤）或入口 caller 由接线层决定——模块内默认 `sampling`，`component=process_runtime`；字段 kv 含 degrade、corpus_char_len、duration_ms；**禁止**把需求/语料全文写入日志。异常文本走 `redact_secrets_in_text`。观测 best-effort，失败吞掉不反噬。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/process_runtime/test_initiative_profile.py -q --tb=short</automated>
  </verify>
  <done>成功画像 + fail-soft degrade + CallSource/LOGGING 已登记；测试覆盖 PROF-01~03 核心行为</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| feature_list / LLM → profile | 不可信需求文本与上游模型输出进入机读结构 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-128-01 | Information Disclosure | initiative_profile logs | mitigate | 日志仅长度/计数/reason；禁止需求原文；redact_secrets_in_text |
| T-128-02 | Tampering | LLM JSON parse | mitigate | 严格解析 + schema 校验；失败 degrade/clarify，不静默编造字段 |
| T-128-SC | Tampering | pip installs | accept | 本 plan 无新包安装 |
</threat_model>

<verification>
- pytest `test_initiative_profile.py` 全绿
- grep CallSource 含新 initiative_profile 值（若新增）
- 无对 RepoRouterV2 的修改
</verification>

<success_criteria>
可独立调用 `build_profile` 得到 ok/clarify/degraded 三态；语料规则符合 PROF-02；观测符合 PROF-03 与可观测规范。
</success_criteria>

<output>
Create `.planning/phases/128-initiative-profile-team-gate/128-01-SUMMARY.md` when done
</output>
