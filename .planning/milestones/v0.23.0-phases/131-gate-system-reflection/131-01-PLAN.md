---
phase: 131-gate-system-reflection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/services/process_runtime/funnel_gates.py
  - server/tests/services/process_runtime/test_funnel_gates.py
autonomous: true
requirements:
  - GATE-01
  - GATE-02
  - GATE-03
user_setup: []

must_haves:
  truths:
    - "五门统一输出 pass|clarify|block + reason_codes[] + evidence"
    - "全局一致性门拦截出界 primary、双状态域写方、壳散落、复用不改造"
    - "发布门默认 confirmation；auto_selected 仅当 D-02 三条件全满足"
  artifacts:
    - path: server/services/process_runtime/funnel_gates.py
      provides: "GateResult + evaluate_funnel_gates + 五门"
      exports: ["evaluate_funnel_gates", "GateResult", "FunnelGateReport"]
    - path: server/tests/services/process_runtime/test_funnel_gates.py
      provides: "GATE-01/02/03 单测"
  key_links:
    - from: evaluate_funnel_gates
      to: placements/role_map/team/shortlist
      via: "五门顺序求值 + 最严重 status 聚合"
      pattern: "evaluate_funnel_gates|reason_codes|global_consistency|publish"
---

<objective>
交付可单测的统一门禁模块：`pass|clarify|block` + `reason_codes[]` + evidence，落地团队/短名单覆盖/单元落点/全局一致性/发布五门（GATE-01、GATE-02、GATE-03；发布纪律 per D-02←D4）。

Purpose: 漏斗收口的可观测决策面；本 plan 不接线 Adapter、不做反思环。
Output: `funnel_gates.py` + 单元测试。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/131-gate-system-reflection/131-CONTEXT.md
@.planning/phases/131-gate-system-reflection/131-RESEARCH.md
@.planning/milestones/v0.23.0-DECISIONS.md
@.planning/REQUIREMENTS.md
@server/services/process_runtime/team_gate.py
@server/services/process_runtime/place_units.py
@server/services/process_runtime/role_map.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — 五门与统一契约单测</name>
  <files>server/tests/services/process_runtime/test_funnel_gates.py</files>
  <behavior>
    - evaluate_funnel_gates 返回 FunnelGateReport：per-gate GateResult 均含 status∈{pass,clarify,block}、reason_codes、evidence
    - team：缺团队 → clarify(missing_team)；out_of_team primary → block(out_of_team_primary)
    - shortlist_coverage：空 shortlist + 有 unit → clarify(empty_shortlist)；unit hard_scope 空 → clarify(coverage_hole)
    - unit_placement：无 primary → clarify(missing_primary)；primary∉hard_scope → block(primary_out_of_scope)
    - global_consistency：双 learning_state primary → block(dual_state_domain_writer)；多 app_shell 壳 primary → block/clarify(app_shell_scattered)；reuse 却改造 host → block(reuse_modify_forbidden)；出界 primary → block
    - publish：默认 confirmation → clarify(needs_confirmation) 且建议 auto_selected=False；满足 role_map 完整 + 全 unit high + 双证据 → pass 且 allow_auto_selected=True；confirmation_acked=True 时可 pass 即使未 auto
    - 聚合：任一 block → 报告 status=block；否则任一 clarify → clarify；全 pass → pass
    - 观测事件名 funnel_gates_* 可断言；kwargs 无超长需求全文
  </behavior>
  <action>
    用合成 team/shortlist/role_map/placements 构造失败用例；不连 Qdrant；引用 D-04~D-11、GATE-01~03、D-02（D4）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/process_runtime/test_funnel_gates.py -q --tb=line 2>&1 | head -50</automated>
  </verify>
  <done>测试文件存在且对缺失实现失败（RED）或明确 import 失败。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — 实现 funnel_gates</name>
  <files>server/services/process_runtime/funnel_gates.py, server/tests/services/process_runtime/test_funnel_gates.py</files>
  <action>
    实现 GateResult / FunnelGateReport dataclass 与 `evaluate_funnel_gates(...)`（sync 纯函数优先）：输入至少 team 摘要、shortlist_ids、role_map、placements、可选 confirmation_acked / membership 标注。按 D-07~D-11 顺序求值五门；GATE-03 四类拦截必须有独立 reason_code。publish 按 D-02 计算 `allow_auto_selected`。导出稳定 reason_code 常量或文档化集合。structlog：funnel_gates_started/completed/failed，category=sampling，component=process_runtime；异常 redact_secrets_in_text；禁止需求全文。不修改 repo_router_v2.py；不改 Adapter。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/process_runtime/test_funnel_gates.py -q --tb=short --reuse-db</automated>
  </verify>
  <done>GATE-01/02/03 单测全绿；模块可被 Plan 02/03 import。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 路由输入（placements/role_map） | 半可信内部产物，仍可能含脏 primary |
| 日志/对外摘要 | 不得泄漏需求全文 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-131-01 | I | funnel_gates 日志 | mitigate | 只记 codes/ids/counts；redact_secrets_in_text |
| T-131-02 | T | primary 出界仍 pass | mitigate | global_consistency + unit_placement block |
| T-131-03 | E | auto_selected 误放行 | mitigate | publish 门 D-02 三条件；单测锁定 |
| T-131-SC | T | 无新包 | accept | 无 pip/npm 安装 |
</threat_model>

<verification>
pytest test_funnel_gates.py 全绿；grep 确认无 repo_router_v2 改动。
</verification>

<success_criteria>
五门可独立求值；统一契约可观测；D4 发布纪律有自动化断言。
</success_criteria>

<output>
Create `.planning/phases/131-gate-system-reflection/131-01-SUMMARY.md` when done
</output>
