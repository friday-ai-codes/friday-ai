---
phase: 129-shortlist-history-role-map
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/services/process_runtime/shortlist.py
  - server/tests/services/process_runtime/test_shortlist.py
autonomous: true
requirements:
  - LIST-01
  - LIST-02
  - LIST-04
user_setup: []

must_haves:
  truths:
    - "在 team_core（∪ 证据合法 adjacent）内用 activity + capability_coarse + charter_domain 生成可排序 shortlist"
    - "章程 planned domain 在能力粗分为 0 时仍可 force-include（force_include_reasons 含 charter_planned）"
    - "逐仓 signals breakdown 可解释；观测上报 shortlist_count/duration_ms，不写需求原文"
  artifacts:
    - path: server/services/process_runtime/shortlist.py
      provides: "build_shortlist + ShortlistResult + 信号合成"
      exports: ["build_shortlist", "ShortlistResult"]
    - path: server/tests/services/process_runtime/test_shortlist.py
      provides: "LIST-01/02/04 单测"
  key_links:
    - from: build_shortlist
      to: team_core universe
      via: "universe ⊆ team_core ∪ adjacent；out_of_team 剔除"
      pattern: "build_shortlist|force_include|charter_planned|shortlist_count"
---

<objective>
交付可单测的 shortlist 生成器：在团队宇宙内合成活跃度、能力粗相关、章程域命中，支持 planned 章程强制拉入，并产出可解释 breakdown 与采样观测（LIST-01/02/04；D-01、D-04、D-06~D-10）。

Purpose: 漏斗中段候选收窄地基；本 plan 不接历史分桶、不接角色图、不改 Adapter。
Output: `shortlist.py` + 单元测试。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/129-shortlist-history-role-map/129-CONTEXT.md
@.planning/phases/129-shortlist-history-role-map/129-RESEARCH.md
@.planning/milestones/v0.23.0-DECISIONS.md
@.planning/REQUIREMENTS.md
@server/services/process_runtime/team_gate.py
@server/services/process_runtime/initiative_profile.py
@server/services/process_runtime/blueprint_charter_match.py
@server/codegraph/services/repo_router_v2.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — shortlist 行为单测</name>
  <files>server/tests/services/process_runtime/test_shortlist.py</files>
  <behavior>
    - 给定 team_core=[A,B,C] 与 mock 信号，build_shortlist 返回仅含宇宙内仓，按综合分排序，每仓含 signals.activity/capability_coarse/charter_domain
    - planned 章程命中仓 D 能力粗分为 0 时仍在 shortlist，且 force_include_reasons 含 charter_planned（LIST-02）
    - out_of_team 仓即使分数高也不进入 shortlist（D-01）
    - 观测字段：可断言 logger 事件名含 shortlist_started/completed 且 kwargs 无超长需求正文；meta 含 shortlist_count
    - 接受 force_include_ids 参数钩子（为空时行为不变，供 Plan 02/04 注入）
  </behavior>
  <action>
    编写失败用例覆盖上述行为；用 monkeypatch/假注入 activity、capability、charter 源，不连真实 Qdrant。引用 D-06~D-10、LIST-01/02/04。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/process_runtime/test_shortlist.py -q --tb=line 2>&1 | head -40</automated>
  </verify>
  <done>测试文件存在且对缺失实现失败（RED）或明确 import 失败。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — 实现 build_shortlist</name>
  <files>server/services/process_runtime/shortlist.py, server/tests/services/process_runtime/test_shortlist.py</files>
  <action>
    实现 `build_shortlist`（async 或 sync+async 包装与仓库惯例一致）：宇宙 = team_core ∪ adjacent_ids；剔除 out_of_team。信号：activity（FileIndex last_commit 或 facets 回退，口径对齐 ROUTING-RANKING/V2，可注入）、capability_coarse（可选调用 RepoRouterV2.route(repository_ids=universe) 只取分数，或可注入 stub——生产路径可注入，单测用 stub，**禁止修改 repo_router_v2.py**）、charter_domain（score_charter_match + acollect planned 补入）。合并 force_include_ids。默认 top N=10，force-include 可突破上界。structlog：shortlist_started/completed/failed，category=sampling，component=process_runtime，duration_ms，shortlist_count；异常 redact_secrets_in_text。不写需求原文。导出 ShortlistResult dataclass/dict。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/process_runtime/test_shortlist.py -q --tb=short --reuse-db</automated>
  </verify>
  <done>LIST-01/02/04 单测全绿；shortlist.py 可被后续 plan import。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shortlist 输入（profile/query/team ids） | 调用方传入的查询与仓 ID |
| 外部检索（V2/ORM） | 索引与章程读 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-129-01 | Information Disclosure | shortlist logs | mitigate | 禁止需求原文；只记 count/reason；redact_secrets_in_text |
| T-129-02 | Elevation of Privilege | force_include | mitigate | force-include ∩ team_core∪合法 adjacent；拒绝 out_of_team |
| T-129-SC | Tampering | pip installs | accept | 本 plan 无新包 |
</threat_model>

<verification>
`uv run pytest tests/services/process_runtime/test_shortlist.py -q` 全绿。
</verification>

<success_criteria>
可单测证明：团队内 shortlist、planned force-include、breakdown + 观测无原文。
</success_criteria>

<output>
Create `.planning/phases/129-shortlist-history-role-map/129-01-SUMMARY.md` when done
</output>
