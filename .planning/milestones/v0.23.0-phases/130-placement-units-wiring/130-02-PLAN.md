---
phase: 130-placement-units-wiring
plan: 02
type: execute
wave: 2
depends_on:
  - 130-01
files_modified:
  - server/services/process_runtime/place_units.py
  - server/tests/services/process_runtime/test_place_units.py
autonomous: true
requirements:
  - UNIT-02
  - UNIT-03
user_setup: []

must_haves:
  truths:
    - "每个放置单元产出 primary_repo + supporting_repos + confidence + evidence + open_questions"
    - "细排调用 RepoRouterV2 时 repository_ids 硬限制在 shortlist ∪ reuse hosts"
    - "primary 不得落在 hard_scope 外；违反 placement_defaults/role forbidden 时降级或 open_questions"
  artifacts:
    - path: server/services/process_runtime/place_units.py
      provides: "place_units + hard_scope + V2 限定调用"
      exports: ["place_units", "PlacementResult"]
    - path: server/tests/services/process_runtime/test_place_units.py
      provides: "UNIT-02/03 单测"
  key_links:
    - from: place_units
      to: RepoRouterV2.route
      via: "repository_ids=hard_scope only"
      pattern: "hard_scope|repository_ids|primary_repo|placement_defaults"
---

<objective>
在 shortlist（∪ 复用宿主）内为每个 Placement Unit 细落点，产出可解释 primary/supporting/confidence/evidence/open_questions；可调用 RepoRouterV2 但禁止全库开放 primary（UNIT-02、UNIT-03；D-01、D-03、D-07~D-11）。

Purpose: 把 129 的 shortlist/role_map 变成单元级落点结果；本 plan 不接 Adapter 主路径。
Output: `place_units.py` + 单元测试。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/130-placement-units-wiring/130-CONTEXT.md
@.planning/phases/130-placement-units-wiring/130-RESEARCH.md
@.planning/phases/130-placement-units-wiring/130-01-PLAN.md
@server/services/process_runtime/placement_units.py
@server/services/process_runtime/role_map.py
@server/services/process_runtime/shortlist.py
@server/codegraph/services/repo_router_v2.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — place_units 行为单测</name>
  <files>server/tests/services/process_runtime/test_place_units.py</files>
  <behavior>
    - 给定 shortlist=[A,B,C] 与 mock V2，place_units 对每个 unit 返回 primary∈{A,B,C}（或 ∪ mock reuse host D∈team），含 supporting/confidence/evidence/open_questions 字段
    - mock V2.route 被调用时 repository_ids 参数 ⊆ hard_scope；从未传入 None/全库
    - 若 stub 返回 scope 外仓 E，结果 primary 不得为 E（丢弃 + degrade/open_question）
    - placement_defaults.learning_state_writer_not_app_shell：当 unit hint 为 learning_state 且 app_shell=A 时，primary ≠ A（除非无其他候选则 open_questions）
    - role_map forbidden 仓不得为 primary
    - 观测 place_units_started/completed；无需求原文
  </behavior>
  <action>
    编写 RED 用例；注入 fake router（记录 repository_ids）、合成 units/shortlist/role_map。引用 D-07~D-11、UNIT-02/03。**禁止**修改 repo_router_v2.py。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/process_runtime/test_place_units.py -q --tb=line 2>&1 | head -40</automated>
  </verify>
  <done>RED 就位。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — 实现 place_units</name>
  <files>server/services/process_runtime/place_units.py, server/tests/services/process_runtime/test_place_units.py</files>
  <action>
    实现 async `place_units(units, *, shortlist_ids, role_map=None, placement_defaults=None, team_core=None, router=None, ...)`：解析 reuse_host_repo_ids（role_map.practice_reuse_host + hints，∩ team）；hard_scope = shortlist ∪ hosts；对每个 unit 可选调用 router.route(query=unit.query_text, repository_ids=list(hard_scope), ...) 取分；结合 shortlist/role/defaults 选 primary/supporting；写 evidence；冲突填 open_questions。三分量/charter/history 可作为可注入信号（非唯一决策）。structlog：place_units_started/completed/failed，category=sampling，component=process_runtime，duration_ms，unit_count；redact。不改 V2 内核。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/process_runtime/test_place_units.py tests/services/process_runtime/test_placement_units.py -q --tb=short --reuse-db</automated>
  </verify>
  <done>UNIT-02/03 单测全绿；可被 Plan 03 接线。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hard_scope 构造 | shortlist/host ids 来自上游；必须 ∩ team |
| V2 调用 | 外部检索工具；只收分数 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-130-03 | Elevation of Privilege | place_units primary | mitigate | primary∉hard_scope 强制丢弃；out_of_team 不可入 scope |
| T-130-04 | Information Disclosure | place_units logs | mitigate | 无 query 全文；redact |
| T-130-05 | Tampering | injected router | accept | 测试注入；生产默认 V2 只读调用 |
| T-130-SC | Tampering | package installs | accept | 无新依赖 |
</threat_model>

<verification>
test_place_units + test_placement_units 全绿；repo_router_v2.py 未改。
</verification>

<success_criteria>
- 单元级落点契约完整
- V2 调用硬限制可测
- defaults/forbidden 生效
</success_criteria>

<output>
Create `.planning/phases/130-placement-units-wiring/130-02-SUMMARY.md` when done
</output>
