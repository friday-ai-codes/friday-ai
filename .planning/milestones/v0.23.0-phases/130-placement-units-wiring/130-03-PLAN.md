---
phase: 130-placement-units-wiring
plan: 03
type: execute
wave: 3
depends_on:
  - 130-01
  - 130-02
files_modified:
  - server/services/process_runtime/blueprint_route.py
  - server/initiatives/services/repo_association_service.py
  - server/services/process_runtime/stage_sandbox.py
  - server/tests/services/process_runtime/test_funnel_placement.py
  - server/tests/services/process_runtime/test_funnel_shortlist.py
  - server/tests/initiatives/test_repo_association_service.py
autonomous: true
requirements:
  - INT-01
  - UNIT-02
  - UNIT-03
user_setup: []

must_haves:
  truths:
    - "Blueprint 漏斗在 role_map 之后执行 build_placement_units → place_units，stage 携带 placements"
    - "项目选仓（RepoAssociation）feature-list 主路径走同一漏斗编排；三分量不再作为唯一决策"
    - "主路径 primary/候选 ⊆ shortlist ∪ reuse hosts；未改 RepoRouterV2 内核；观测无需求原文"
  artifacts:
    - path: server/services/process_runtime/blueprint_route.py
      provides: "funnel wiring placements after role_map"
    - path: server/initiatives/services/repo_association_service.py
      provides: "association 主路径走漏斗 + placements"
    - path: server/tests/services/process_runtime/test_funnel_placement.py
      provides: "INT-01 接线守卫"
  key_links:
    - from: BlueprintRouteAdapter.route
      to: build_placement_units / place_units
      via: "role_map → placement → place；三分量为信号"
      pattern: "place_units|placement_units|placements|hard_scope"
---

<objective>
把放置单元与细落点接到蓝图路由与项目选仓主路径，使决策漏斗成为 feature-list 选仓的唯一主路径；三分量加权仅作漏斗内信号（INT-01；D-02、D-12~D-15）。

Purpose: 关闭「整篇 V2+三分量唯一决策」主路径；为 Phase 131 门禁提供 placements 载荷。
Output: Adapter/Association/sandbox 接线 + `test_funnel_placement.py`；129 守卫不回归。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/130-placement-units-wiring/130-CONTEXT.md
@.planning/phases/130-placement-units-wiring/130-RESEARCH.md
@.planning/phases/129-shortlist-history-role-map/129-04-SUMMARY.md
@server/services/process_runtime/placement_units.py
@server/services/process_runtime/place_units.py
@server/services/process_runtime/blueprint_route.py
@server/initiatives/services/repo_association_service.py
@server/services/process_runtime/stage_sandbox.py
@server/tests/services/process_runtime/test_funnel_shortlist.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — 漏斗放置接线守卫测</name>
  <files>server/tests/services/process_runtime/test_funnel_placement.py</files>
  <behavior>
    - mock team_gate+shortlist+role_map 通过后，Adapter.route 结果含 placement_units 摘要与 placements（每项含 primary_repo/supporting_repos/confidence）
    - 任一 placement.primary_repo（非空）∈ shortlist_ids ∪ 声明的 reuse hosts
    - 断言 RepoRouterV2（或注入 router）时，所有 route(..., repository_ids=) 均为非空收窄集合，且 ⊆ hard_scope
    - 合成「多 feature」时 V2 调用次数按 unit 计而非按 feature 计（unit_count &lt; feature_count 且调用次数 ≈ unit_count 或更少）
    - RepoAssociation propose/route 路径（可抽测内部编排）返回 placements 或等价字段，且不再仅依赖「三分量 total 最高单一仓」作为唯一决策依据（断言存在 placements 或 multi-primary 聚合逻辑）
  </behavior>
  <action>
    写 RED 接线测；复用 test_funnel_shortlist 风格（stub router/charter/history/shortlist/role_map）。覆盖 D-12~D-14、INT-01。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/process_runtime/test_funnel_placement.py -q --tb=line 2>&1 | head -40</automated>
  </verify>
  <done>RED 就位。</done>
</task>

<task type="auto">
  <name>Task 2: GREEN — Adapter / Association / sandbox 接线</name>
  <files>server/services/process_runtime/blueprint_route.py, server/initiatives/services/repo_association_service.py, server/services/process_runtime/stage_sandbox.py, server/tests/services/process_runtime/test_funnel_placement.py, server/tests/services/process_runtime/test_funnel_shortlist.py, server/tests/initiatives/test_repo_association_service.py</files>
  <action>
    在 BlueprintRouteAdapter.route：role_map 成功后从 session/requirement 取 feature_list（或 _requirement_spec_to_feature_list），调用 build_placement_units → place_units(shortlist_ids, role_map, placement_defaults, router=限定调用)；将 placement_units/placements 写入 routing 摘要与观测。后续候选集合由 placements 的 primary/supporting 推导，且 ⊆ hard_scope；既有三分量 breakdown 可保留在 evidence/信号侧，但不得覆盖 hard_scope 外 primary。RepoAssociationService：feature-list 主路径改为 team→shortlist(±history)→（可选 role_map）→placement→place；三分量融合降为 place 内信号或附加 breakdown，不再作为唯一决策。stage_sandbox：至少 hard_scope 守卫。fail-soft：placement 失败不回退全库（保留 shortlist 收窄）。不修改 repo_router_v2.py。日志遵守 D-15。更新/保持 129 funnel shortlist 测与 association 测不回归。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/process_runtime/test_funnel_placement.py tests/services/process_runtime/test_funnel_shortlist.py tests/services/process_runtime/test_funnel_team_gate.py tests/services/process_runtime/test_placement_units.py tests/services/process_runtime/test_place_units.py tests/initiatives/test_repo_association_service.py -q --tb=short --reuse-db</automated>
  </verify>
  <done>接线测与 Wave1–2 模块测全绿；129 漏斗不回归；V2 文件未改。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Blueprint/Association 入口 | 用户/项目触发的 feature-list 选仓 |
| 下游编码/调研 | 消费 routing.placements |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-130-06 | Elevation of Privilege | Adapter primary | mitigate | placements primary ⊆ hard_scope；out_of_team 禁止 |
| T-130-07 | Information Disclosure | blueprint_route / association logs | mitigate | 只记 count/ids 长度；redact；无需求原文 |
| T-130-08 | Denial of Service | per-unit V2 | mitigate | unit 数远小于 feature；fail-soft 超时/异常不阻断全路径回全库 |
| T-130-SC | Tampering | package installs | accept | 无新依赖 |
</threat_model>

<verification>
funnel_placement + shortlist + team_gate + placement/place + association 子集全绿；git diff 不含 repo_router_v2.py。
</verification>

<success_criteria>
- 主路径走漏斗 placements
- 三分量非唯一决策
- hard_scope 守卫成立
</success_criteria>

<output>
Create `.planning/phases/130-placement-units-wiring/130-03-SUMMARY.md` when done
</output>
