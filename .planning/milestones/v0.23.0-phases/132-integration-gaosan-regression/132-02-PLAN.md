---
phase: 132-integration-gaosan-regression
plan: 02
type: execute
wave: 2
depends_on:
  - 132-01
files_modified:
  - server/tests/services/process_runtime/test_gaosan_funnel_regression.py
  - server/tests/services/process_runtime/fixtures/gaosan_learning_tools.py
  - server/services/process_runtime/gaosan_eval.py
  - server/services/process_runtime/role_map.py
  - server/services/process_runtime/place_units.py
  - server/services/process_runtime/shortlist.py
autonomous: true
requirements:
  - INT-02
user_setup: []

must_haves:
  truths:
    - "合成 Learning-tools 宇宙下漏斗路径产出 placements 后 score_placement_bar.passed==True"
    - "同用例 out_of_team_primary_count==0"
    - "验收路径经 funnel（shortlist/role/place/gates），非裸 RepoRouterV2.route 作为唯一决策"
  artifacts:
    - path: server/tests/services/process_runtime/test_gaosan_funnel_regression.py
      provides: "INT-02 漏斗回归（D2 bar）"
  key_links:
    - from: gaosan_learning_tools fixture
      to: place_units / Adapter funnel
      via: "stub V2 scores within hard_scope → placements → score_placement_bar"
      pattern: "score_placement_bar|gaosan|out_of_team_primary|place_units"
---

<objective>
用合成 Learning-tools Space 驱动决策漏斗至 placements，断言 D2 bar（四基线各≥1 primary、out_of_team=0），评测路径非裸 V2（per D-02、D-03、D-04、D-07、D-10）。

Purpose: 关闭 INT-02 回归锚点；必要时小修漏斗信号/fixture 对齐以达门槛，不得降低 D2。
Output: `test_gaosan_funnel_regression.py`；仅当合成路径无法达 bar 时最小改动 process_runtime 漏斗模块。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/132-integration-gaosan-regression/132-CONTEXT.md
@.planning/phases/132-integration-gaosan-regression/132-RESEARCH.md
@.planning/phases/132-integration-gaosan-regression/132-01-PLAN.md
@.planning/milestones/v0.23.0-DECISIONS.md
@server/services/process_runtime/gaosan_eval.py
@server/tests/services/process_runtime/fixtures/gaosan_learning_tools.py
@server/services/process_runtime/blueprint_route.py
@server/services/process_runtime/place_units.py
@server/tests/services/process_runtime/test_funnel_placement.py
@.planning/quick/260811-gaosan-route-5rounds/route-5rounds-results.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — 漏斗路径 D2 回归测</name>
  <files>server/tests/services/process_runtime/test_gaosan_funnel_regression.py</files>
  <behavior>
    - 使用 `gaosan_learning_tools` fixture 构建 team/membership/shortlist 宇宙（含四基线 + out_of_team 诱饵）
    - 经漏斗组件链（至少：role_map 或等价 + `place_units` / Adapter placement funnel；允许 stub `RepoRouterV2.route` **仅作 hard_scope 内打分**）得到 `placements`
    - 调用 `score_placement_bar` → 期望 `passed is True`、`missing_baselines == []`、`out_of_team_primary_count == 0`
    - 显式断言测试未把「无 repository_ids 的全库 V2」当作决策（例如 spy：凡 V2 调用必带非空 `repository_ids` ⊆ team/shortlist 宇宙）— per D-04
    - 附加（per Claude discretion / ROADMAP 角色覆盖）：四角色 `app_shell` / `practice_reuse_host` / `course_config` / `learning_state` 各有 primary，或四基线与角色映射一致
    - 可选：`@pytest.mark.live_space` 空壳/skip 测试，注释引用 260809 Space id 与 D-07；默认不跑
  </behavior>
  <action>
    编写 RED 测试文件。优先复用 130/131 funnel 测试的 stub 模式（`test_funnel_placement.py` / `test_funnel_gates_wiring.py`）。不要降低 D2 断言。不要导入 quick JSON 作为 expected primary 集合。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/process_runtime/test_gaosan_funnel_regression.py -q --tb=line 2>&1 | head -50; test ${PIPESTATUS[0]} -ne 0</automated>
  </verify>
  <done>RED：漏斗回归断言已存在且当前失败或依赖未满足。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — 对齐漏斗/fixture 使 D2 bar 通过</name>
  <files>server/tests/services/process_runtime/test_gaosan_funnel_regression.py, server/tests/services/process_runtime/fixtures/gaosan_learning_tools.py, server/services/process_runtime/gaosan_eval.py, server/services/process_runtime/role_map.py, server/services/process_runtime/place_units.py, server/services/process_runtime/shortlist.py</files>
  <action>
    使 Task 1 全绿（per D-10）：优先调整合成 fixture 的 charter/history/role/shortlist 信号与 stub 分数，使真实漏斗入口自然把四基线各落为至少一 unit primary，且诱饵仓不得 primary。若产品 bug 阻止达 bar，允许**最小**修补 `role_map` / `place_units` / `shortlist`（例如 placement_defaults、reuse host 解析、history force-include）。**不要**改 `blueprint_route.py`（Adapter 接线归 plan 03 / INT-03）；禁止改 `repo_router_v2.py`，禁止把验收改成 feature-point top1，禁止删除 out_of_team=0 断言。

    文档：在测试模块 docstring 写明「Eval path = Learning-tools 合成宇宙 + funnel；参照 quick 260809/260811 仅作语料/失败模式，非 pass 标准」（per D-07）。

    跑通后确认 `test_gaosan_eval.py` 仍绿。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/process_runtime/test_gaosan_eval.py tests/services/process_runtime/test_gaosan_funnel_regression.py -q --tb=short --reuse-db</automated>
  </verify>
  <done>INT-02：合成漏斗路径上 D2 bar 自动化通过；V2 调用均受限 hard_scope。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stub V2 → place_units | 测试注入分数；生产仍走真实 router |
| membership → primary gate | out_of_team 不得成为 primary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-132-03 | Elevation of Privilege | out_of_team primary | mitigate | bar + funnel 断言 count==0；诱饵仓在 fixture 中 |
| T-132-04 | Tampering | bare V2 eval bypass | mitigate | spy repository_ids 非空且 ⊆ scope |
| T-132-05 | Information Disclosure | regression logs | mitigate | 无 feature list 全文；sampling counts only |
| T-132-SC | Tampering | package installs | accept | 无新包 |
</threat_model>

<verification>
gaosan_eval + gaosan_funnel_regression 全绿；git diff 不含 `repo_router_v2.py` 业务改动。
</verification>

<success_criteria>
- 合成 Learning-tools + 漏斗 → D2 passed
- out_of_team primary = 0
- 非裸 V2 验收主路径
</success_criteria>

<output>
Create `.planning/phases/132-integration-gaosan-regression/132-02-SUMMARY.md` when done
</output>
