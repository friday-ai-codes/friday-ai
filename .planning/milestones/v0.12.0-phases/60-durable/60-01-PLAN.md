---
phase: 60-durable
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/durable/__init__.py
  - server/durable/apps.py
  - server/durable/service.py
  - server/durable/backends.py
  - server/durable/queues.py
  - server/durable/roles.py
  - server/friday/settings.py
  - server/pyproject.toml
  - server/tests/durable/__init__.py
  - server/tests/durable/conftest.py
  - server/tests/durable/test_service_fallback.py
  - server/tests/durable/test_no_direct_import.py
autonomous: true
requirements: [DURABLE-01]
must_haves:
  truths:
    - "业务代码经 DurableTaskService.defer/get/cancel/retry_stalled 入队/查询/取消，无 Postgres 时退化为 in-process fallback 不报错"
    - "DurableTaskService 与队列常量经 durable.__init__ 暴露，业务侧无需 import procrastinate"
    - "make dev / pytest 默认 SQLite 路径不引入任何 Procrastinate 依赖即可跑通 fallback 路径"
    - "存在 grep 守护断言除 durable.backends/durable.tasks 外业务代码无直接 import procrastinate"
  artifacts:
    - path: "server/durable/service.py"
      provides: "DurableTaskService 适配层统一入口 + _use_procrastinate(engine, backend) 唯一权威后端判定（service 与 settings 共用，amended auto+postgres→durable 语义）"
      contains: "class DurableTaskService"
    - path: "server/durable/backends.py"
      provides: "DurableBackend 协议 + InProcessBackend(复用 background_runner)"
      contains: "class InProcessBackend"
    - path: "server/durable/queues.py"
      provides: "队列命名常量（index/graph/crawl_ingest/page_index/maintenance）"
      contains: "QUEUE_INDEX"
    - path: "server/durable/roles.py"
      provides: "current_role() + should_run_startup_side_effects() 进程角色判定（Plan 02 消费）"
      contains: "def should_run_startup_side_effects"
    - path: "server/durable/__init__.py"
      provides: "curated re-export：DurableTaskService + 队列常量"
      contains: "DurableTaskService"
  key_links:
    - from: "server/durable/service.py"
      to: "server/durable/backends.py"
      via: "_use_procrastinate() 选择 in_process_backend / procrastinate_backend（懒局部 import）"
      pattern: "in_process_backend|procrastinate_backend"
    - from: "server/durable/backends.py"
      to: "services.background_runner.run_in_background"
      via: "InProcessBackend.defer 调度 coro factory 到常驻 worker loop"
      pattern: "run_in_background"
---

<objective>
立起 `DurableTaskService` 适配层的进程内地基：统一接口（`defer/get/cancel/retry_stalled` + `idempotency_key` + `queue` + `priority` + `run_at`）、SQLite/无 `DATABASE_URL` 的 in-process 非 durable fallback（复用 `services.background_runner`）、队列命名常量、进程角色判定 helper（供 Plan 02 消费），以及"业务代码不直接 import procrastinate"的 grep 守护。

Purpose: 这是 v0.12.0 全部后续阶段的地基——所有入队点未来只见 `DurableTaskService`，看不见队列实现；本 plan 让 dev/pytest 开箱即用（无需 Postgres）。
Output: 新建 `server/durable/` app 骨架 + settings 注册 + `procrastinate[django]` 依赖落入 `pyproject.toml`（安装但本 plan 仅 SQLite 路径消费）+ SQLite fallback / no-import 守护测试。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-durable/60-CONTEXT.md
@.planning/phases/60-durable/60-RESEARCH.md
@.planning/phases/60-durable/60-VALIDATION.md
@server/resumable/service.py
@server/services/background_runner.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 建 durable app 骨架 + 队列常量 + 进程角色 helper + settings/pyproject 注册</name>
  <read_first>
    - server/resumable/__init__.py（curated docstring re-export 范式）
    - server/resumable/service.py（`INSTANCE_ID`、`lease_ttl_seconds`、sync+`sync_to_async` 双轨范式，DurableTaskService 镜像其心智模型）
    - server/resumable/apps.py（AppConfig 结构 + ready() 形态）
    - server/services/background_runner.py（`run_in_background(coro_factory, *, name)`/`cancel_background_task`/`wait_for_pending`/`_reset_for_tests` 完整 API）
    - server/friday/settings.py:89-131（INSTALLED_APPS 顺序）+ :243-244（DATABASES 解析）
    - server/pyproject.toml:6-80（dependencies 块）+ :105-118（pytest ini_options）
    - .planning/phases/60-durable/60-RESEARCH.md「Code Examples」「Recommended Project Structure」「Don't Hand-Roll」
  </read_first>
  <action>
    新建 `server/durable/` Django app（镜像 `server/resumable/` 结构）：
    `server/durable/__init__.py` 写中文模块 docstring（说明 durable 适配层职责）+ curated re-export `DurableTaskService` 与全部队列常量（`from durable.service import DurableTaskService`、`from durable.queues import *`），供业务侧单点导入。
    `server/durable/queues.py` 定义模块级队列常量 `QUEUE_INDEX="index"`、`QUEUE_GRAPH="graph"`、`QUEUE_CRAWL_INGEST="crawl_ingest"`、`QUEUE_PAGE_INDEX="page_index"`、`QUEUE_MAINTENANCE="maintenance"`，并 `ALL_QUEUES: tuple[str, ...]` 汇总（本阶段只定义命名，实际接入由 Phase 61/62 消费）。
    `server/durable/roles.py` 提供 `current_role() -> str`（读 `os.environ["FRIDAY_PROCESS_ROLE"]`，缺省 `"web"`，`.strip().lower()` 归一）与 `should_run_startup_side_effects(*, job: str, allowed: frozenset[str] = frozenset({"web"})) -> bool`（role∈allowed 返 True；否则经 structlog 记一条 info `startup_side_effect_skipped_by_role`（字段 role/job）后返 False，不静默）。该模块独立读 env、不依赖 Django settings，供 Plan 02 的三处 `apps.py` 消费。
    `server/durable/apps.py` 定义 `DurableConfig(AppConfig)`：`default_auto_field="django.db.models.BigAutoField"`、`name="durable"`、`verbose_name="durable 任务底座"`；`ready()` 本 plan 为空（仅 pass + 中文注释说明 periodic/task 注册由 Plan 03 叠加），不产生任何启动副作用。
    `server/friday/settings.py`：INSTALLED_APPS 在业务 apps 区追加 `"durable"`（紧邻 `"resumable"` 之后即可；本 plan 不加 `procrastinate.contrib.django`——其条件注册留 Plan 03，避免 SQLite migrate 命中 Postgres-only 迁移）；新增 `DURABLE_TASK_BACKEND = env.str("DURABLE_TASK_BACKEND", default="auto")` 与 `FRIDAY_PROCESS_ROLE = env.str("FRIDAY_PROCESS_ROLE", default="web")`（紧邻 DATABASES 定义附近，附中文注释：auto=按引擎自动 / procrastinate=强制需 Postgres / in_process=强制 fallback）。
    `server/pyproject.toml`：用 `uv add 'procrastinate[django]>=3.8.1,<3.9'`（在 server/ 下执行）把依赖写入 `[project].dependencies` 并刷新 `uv.lock`（本 plan 不直接 import procrastinate，仅落地依赖供 Plan 03 消费；安装后 `uv run python -c "import procrastinate; print(procrastinate.__version__)"` 落地确认 3.8.x）。
  </action>
  <acceptance_criteria>
    - `server/durable/{__init__,apps,queues,roles}.py` 均存在；`durable` 出现在 settings INSTALLED_APPS。
    - `cd server && uv run python -c "import durable; from durable import DurableTaskService; from durable.queues import QUEUE_INDEX, ALL_QUEUES; print(QUEUE_INDEX, len(ALL_QUEUES))"` 输出 `index 5`，零异常。
    - `durable.roles.current_role()` 在未设 env 时返回 `"web"`；`FRIDAY_PROCESS_ROLE=worker` 时 `should_run_startup_side_effects(job="x")` 返回 False。
    - `cd server && uv run python -c "import procrastinate; print(procrastinate.__version__)"` 打印 3.8.x（依赖已安装）。
    - `grep -n procrastinate server/friday/settings.py` 不含 `procrastinate.contrib.django`（本 plan 不注册，SQLite migrate 安全）。
    - `cd server && uv run python manage.py check` 通过（无 app 加载/迁移错误）。
  </acceptance_criteria>
  <verify>
    <automated>cd server && uv run python manage.py check && uv run python -c "from durable.queues import ALL_QUEUES; assert len(ALL_QUEUES)==5"</automated>
  </verify>
  <done>durable app 注册成功、队列常量与 roles helper 可导入可用、procrastinate 依赖落地、settings check 通过且未引入 Postgres-only 迁移。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DurableTaskService 适配层 + InProcessBackend 实现（SQLite fallback）</name>
  <read_first>
    - server/durable/queues.py（Task 1 产出，队列常量）
    - server/services/background_runner.py（`run_in_background` 必须传 factory；`cancel_background_task(name)`；返回 `concurrent.futures.Future`）
    - server/resumable/service.py:195-277（`wrap_resumable`/`submit_resumable`：coro_factory 包装 + 终态收尾范式）
    - .planning/phases/60-durable/60-RESEARCH.md「DurableTaskService 适配层骨架」「`background_runner` fallback API」
  </read_first>
  <behavior>
    - defer 在 SQLite/auto 下走 InProcessBackend：返回一个稳定 job id 字符串（不为空），并把任务调度到 background_runner（不触达 Postgres）。
    - get(job_id) 对 in-process 返回当前已知状态（至少 unknown/pending/running/succeeded 之一的结构化 dict），从不抛 ImportError（procrastinate 未启用时）。
    - cancel(job_id) 对 in-process 经 cancel_background_task 取消按名注册的任务，返回布尔。
    - retry_stalled() 在 in-process 后端为安全 no-op（返回 0），不报错。
    - idempotency_key 在 in-process 后端用作 background_runner 的 name（同 key 二次 defer 行为可预期、可观测），不静默吞。
  </behavior>
  <action>
    `server/durable/backends.py`：定义 `DurableBackend`（typing.Protocol，声明 `defer/get/cancel/retry_stalled` 异步签名）；实现 `InProcessBackend`——`defer(task, payload, *, queue, priority=0, idempotency_key=None, run_at=None)` 用 `services.background_runner.run_in_background` 以 `idempotency_key`（或派生 `f"durable:{queue}:{uuid4().hex}"`）为 `name` 调度一个由 `payload` 重建 coroutine 的 factory，返回该 name 作为 job id；`get` 读模块级 in-flight 注册表返回结构化状态 dict；`cancel` 调 `cancel_background_task(name)`；`retry_stalled` 直接返回 0（非 durable 无 stalled 概念，记中文注释）。导出模块级单例 `in_process_backend = InProcessBackend()`。`ProcrastinateBackend` 仅放一个 stub：方法体 `raise NotImplementedError("procrastinate backend 由 Plan 60-03 实现")`，并导出占位 `procrastinate_backend = ProcrastinateBackend()`（保证 service.py 懒 import 的符号存在；SQLite 路径永不触达）。
    `server/durable/service.py`：实现**唯一权威后端判定** `_use_procrastinate(engine: str, backend: str) -> bool`——纯函数、模块级、零 Django 依赖（这是后端选择的 single source of truth，Plan 03 的 settings.py procrastinate.contrib.django 条件注册必须 `from durable.service import _use_procrastinate` 复用同一函数，禁止另写等价判据）。语义（amended，production 默认 Postgres 即 durable 开箱即用）：`return ("postgresql" in (engine or "").lower()) and ((backend or "auto").strip().lower() in {"auto", "procrastinate"})`，即 **当且仅当 默认 DB 引擎含 `postgresql` 且 `DURABLE_TASK_BACKEND ∈ {auto, procrastinate}` 时返回 True**。真值表：`auto`+postgres→True（durable）；`auto`+sqlite/无 DATABASE_URL→False（in-process fallback）；`procrastinate`+postgres→True；`procrastinate`+非 postgres→False（fail-soft 回退）；`inprocess`/`fallback`+任意→False（强制 fallback，即便 Postgres）。为保证 settings.py 在 `django.setup()` 期 `from durable.service import _use_procrastinate` 不触发循环 import，`service.py` 模块顶层**只放 stdlib/typing 导入，绝不在顶层 `from django.conf import settings` 或访问 settings 属性**（settings 读取一律放函数体内局部）。再提供薄封装 `use_procrastinate_backend() -> bool`：局部读 `settings.DATABASES["default"]["ENGINE"]` 与 `getattr(settings, "DURABLE_TASK_BACKEND", "auto")` 后委托 `_use_procrastinate(...)`；当 `backend=="procrastinate"` 而引擎非 postgresql 时经 structlog 记一条 warning `durable_backend_fallback_non_postgres`（字段 engine/backend）再返回 False（fail-soft 回退 + 告警，不在启动期 raise，dev 安全）。`class DurableTaskService` 暴露 `@staticmethod async def defer/get/cancel/retry_stalled`，内部按 `use_procrastinate_backend()` 在方法体内**局部 import** `durable.backends.procrastinate_backend` 或 `durable.backends.in_process_backend` 后委托（局部 import 是隔离命门，模块顶层绝不 import procrastinate）。所有方法签名逐字对齐 CONTEXT 锁定：`defer(task: str, payload: dict, *, queue: str, priority: int = 0, idempotency_key: str | None = None, run_at: datetime | None = None) -> str`。
  </action>
  <acceptance_criteria>
    - `DurableTaskService.defer(...)` 在 SQLite 测试 DB 下返回非空字符串 job id，且不发起任何 Postgres 连接（pytest-socket 不报 SocketBlockedError）。
    - `_use_procrastinate(engine, backend)` 纯函数真值表全覆盖：`("sqlite", "auto")`→False、`("postgresql", "auto")`→**True**（auto+postgres durable 分支）、`("postgresql", "procrastinate")`→True、`("postgresql", "inprocess")`→**False**（强制 fallback 分支，即便 Postgres）、`("sqlite", "procrastinate")`→False。
    - `use_procrastinate_backend()` 在 SQLite 测试 DB 默认（`DURABLE_TASK_BACKEND=auto`）下返回 False；`backend=procrastinate` 而引擎非 postgresql 时记 warning 并返回 False（fail-soft，不 raise）。
    - settings.py 的 procrastinate 条件注册（Plan 03 落地）与 `durable.service._use_procrastinate` 为同一函数（无重复等价判据）——本 plan 仅确保该函数已是可被 settings 安全 import 的 single source of truth（顶层无 settings 访问）。
    - `DurableTaskService.retry_stalled()` 在 in-process 返回 0、不抛异常。
    - `server/durable/service.py` 与 `server/durable/__init__.py`、`server/durable/queues.py` 顶层无 `import procrastinate`（仅 backends.py 内允许，且 service 经局部 import 隔离）。
    - 新建 `server/tests/durable/test_service_fallback.py`：覆盖 defer 返回 id、get 返回结构化状态、cancel 返回布尔、retry_stalled==0、`_use_procrastinate` 双分支。
  </acceptance_criteria>
  <verify>
    <automated>cd server && uv run pytest tests/durable/test_service_fallback.py -q</automated>
  </verify>
  <done>SQLite fallback 下 defer/get/cancel/retry_stalled 全部可用且不触达 Postgres，_use_procrastinate 双分支正确，守护测试绿。</done>
</task>

<task type="auto">
  <name>Task 3: test 基础设施 + "业务代码不直接 import procrastinate" grep 守护</name>
  <read_first>
    - server/tests/conftest.py:1-9（adrf patch 顶层）+ :36-72（`_reset_background_runner`/`_disable_scheduler_in_tests` autouse）
    - server/pyproject.toml:105-118（addopts + markers）
    - 既有 grep 守护范式：server/tests 内 INV-6 守护测试（如 test_research_inv6_guard.py / test_audit 类）——以 `rg` 子进程 + 排除自身实现文件断言无旁路
    - .planning/phases/60-durable/60-VALIDATION.md「Wave 0 Requirements」「Per-Task Verification Map」
  </read_first>
  <action>
    `server/tests/durable/__init__.py`（空包标记）。
    `server/tests/durable/conftest.py`：本 plan 只放 SQLite/fallback 共享 fixture——`durable_service` factory（返回 `DurableTaskService`）+ 每测试后 `background_runner.wait_for_pending` / `_reset_for_tests`（若顶层 conftest 的 autouse 已覆盖则仅补 durable 专用项，不重复）。Postgres 专用 fixture（socket 放行 / procrastinate app）留 Plan 03 叠加，本 plan 不放。
    `server/tests/durable/test_no_direct_import.py`：用 `subprocess` 跑 `rg -l "^\s*(import|from)\s+procrastinate"`（或等价）扫描 `server/`，过滤掉允许清单 `durable/backends.py`、`durable/tasks.py`（Plan 03 产）、`durable/management/`（Plan 03 产）、测试目录与 `.venv`/`migrations`；断言剩余命中集合为空（即业务代码零直接 import），失败时打印命中文件路径。允许清单以常量列出并加中文注释（适配层隔离 = DURABLE-01 核心约束）。
    `server/pyproject.toml`：在 `[tool.pytest.ini_options].markers` 追加 `"postgres_queue: 需真实 Postgres 的 durable 队列测试（GH Actions postgres:17-alpine service 跑 -m postgres_queue）"`，并把 `addopts` 的 `-m` 表达式改为 `-m 'not perf and not integration and not slow and not postgres_queue'`（默认/本地/SQLite CI 不命中 postgres_queue，Pitfall 1）。
  </action>
  <acceptance_criteria>
    - `server/tests/durable/__init__.py` 与 `conftest.py` 存在；`test_no_direct_import.py` 当前仓库状态下通过（业务代码无直接 import）。
    - `grep -n "postgres_queue" server/pyproject.toml` 同时命中 markers 注册行与 addopts 排除表达式。
    - `cd server && uv run pytest -m postgres_queue -q` 在无 Postgres 本地环境下收集 0 项执行（被 addopts 默认排除，不报连接错误）。
    - `cd server && uv run pytest tests/durable -q` 全绿（fallback + no-import 两文件）。
  </acceptance_criteria>
  <verify>
    <automated>cd server && uv run pytest tests/durable -q</automated>
  </verify>
  <done>postgres_queue marker 已注册且默认排除、no-direct-import grep 守护绿、durable 测试目录与 fixture 就位，本地无 Postgres 也能跑通默认套件。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| web 进程 → durable 队列入口 | 业务 view（已认证）调用 DurableTaskService.defer；payload 跨入持久层 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-01 | Information Disclosure | DurableTaskService.defer payload | mitigate | payload 仅放 id/最小参数；本 plan 不接业务任务，CLAUDE.md 凭证约束（不入 payload/不进 env）在 acceptance 中以"payload JSON-serializable、无明文 token"约束 |
| T-60-02 | Tampering | 适配层被业务代码绕过直接 import procrastinate | mitigate | test_no_direct_import.py grep 守护，CI 默认套件即跑 |
| T-60-SC | Tampering | `uv add procrastinate[django]` 安装 | accept | RESEARCH「Package Legitimacy Audit」已核：PyPI Production/Stable、6 年历史、官方文档双验证，Approved（非 slopsquat）；安装后 import 版本确认 |
</threat_model>

<verification>
- `cd server && uv run python manage.py check` 通过。
- `cd server && uv run pytest tests/durable -q` 全绿。
- `cd server && uv run pytest -q` 默认 SQLite 全量零回归（确认新增 app/marker 不破坏既有套件）。
- `cd server && uv run ruff check durable && uv run ruff format --check durable`。
</verification>

<success_criteria>
- 业务侧可经 `from durable import DurableTaskService` 单点导入，`defer/get/cancel/retry_stalled` 在 SQLite 下可用且不触达 Postgres。
- 队列常量、roles helper 就位供 Plan 02/03 消费。
- `procrastinate[django]>=3.8.1,<3.9` 落入依赖且可 import；settings 未引入 Postgres-only 迁移。
- no-direct-import grep 守护 + postgres_queue marker 默认排除生效。
</success_criteria>

<artifacts>
## Artifacts this phase produces (Plan 01)

**New files / modules**
- `server/durable/__init__.py` — curated re-export（`DurableTaskService` + 队列常量）
- `server/durable/apps.py` — `DurableConfig`（ready() 本 plan 空）
- `server/durable/service.py` — `DurableTaskService`、`_use_procrastinate()`
- `server/durable/backends.py` — `DurableBackend`(Protocol)、`InProcessBackend`、`in_process_backend`、`ProcrastinateBackend`(stub)、`procrastinate_backend`(占位)
- `server/durable/queues.py` — `QUEUE_INDEX`/`QUEUE_GRAPH`/`QUEUE_CRAWL_INGEST`/`QUEUE_PAGE_INDEX`/`QUEUE_MAINTENANCE`/`ALL_QUEUES`
- `server/durable/roles.py` — `current_role()`、`should_run_startup_side_effects()`
- `server/tests/durable/__init__.py`、`conftest.py`、`test_service_fallback.py`、`test_no_direct_import.py`

**Settings keys**
- `DURABLE_TASK_BACKEND`（默认 `"auto"`）、`FRIDAY_PROCESS_ROLE`（默认 `"web"`）

**Env vars**
- `DURABLE_TASK_BACKEND`、`FRIDAY_PROCESS_ROLE`（roles.py 直读）

**INSTALLED_APPS additions**
- `"durable"`

**Dependency**
- `procrastinate[django]>=3.8.1,<3.9`（`server/pyproject.toml` + `uv.lock`）

**pytest marker**
- `postgres_queue`（注册 + addopts 默认排除）
</artifacts>

<output>
Create `.planning/phases/60-durable/60-01-SUMMARY.md` when done.
</output>
