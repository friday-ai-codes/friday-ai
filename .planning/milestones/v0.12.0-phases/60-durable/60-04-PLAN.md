---
phase: 60-durable
plan: 04
type: execute
wave: 3
depends_on: [60-03]
files_modified:
  - .github/workflows/ci.yaml
autonomous: true
requirements: [DURABLE-04]
must_haves:
  truths:
    - "从零创建一个聚焦 server 的 GitHub Actions workflow（仓库当前无任何 workflow，commit 5579e45f2 已全删）"
    - "默认 server-ci job 跑 SQLite 路径（uv run pytest，addopts 默认排除 postgres_queue），与既有本地默认路径一致"
    - "独立 postgres-queue job 用 postgres:17-alpine service container + DATABASE_URL=postgres + DURABLE_TASK_BACKEND=procrastinate + migrate + pytest -m postgres_queue --allow-hosts=127.0.0.1,localhost 跑绿"
    - "postgres-queue job 覆盖 defer/priority/retry-backoff/stalled rescue/并发 worker 竞争/SQLite fallback 退化路径（经 Plan 03 测试）"
  artifacts:
    - path: ".github/workflows/ci.yaml"
      provides: "server-ci（SQLite 默认）+ postgres-queue（Postgres service）两 job 的 CI workflow"
      contains: "postgres:17-alpine"
  key_links:
    - from: ".github/workflows/ci.yaml (postgres-queue job)"
      to: "server/tests/durable/test_procrastinate_backend.py + test_stalled_rescue.py"
      via: "pytest -m postgres_queue 跑 Plan 03 的 postgres 测试"
      pattern: "postgres_queue"
    - from: ".github/workflows/ci.yaml (postgres-queue job)"
      to: "procrastinate.contrib.django 迁移"
      via: "manage.py migrate 对 Postgres 建 procrastinate 表后再跑测试"
      pattern: "migrate"
---

<objective>
从零创建一个聚焦 server 的 GitHub Actions workflow（`.github/workflows/ci.yaml`）：一个默认 `server-ci` job 跑 SQLite 路径（零回归基线），一个独立 `postgres-queue` job 用 `postgres:17-alpine` service container 跑 `-m postgres_queue`，验证 Plan 03 的 durable Postgres 行为，与 SQLite 默认路径共存（marker 分层）。

Purpose: 兑现 DURABLE-04。关键坐标：仓库当前**没有任何 workflow**（`5579e45f2` 已整体删除 ci/docs/release），这是创建而非修改；本阶段只建聚焦 server+Postgres 的最小 CI，不顺带恢复无关的 web/runner/docs/security-scan job（范围对齐 RESEARCH Open Q1 推荐）。
Output: `.github/workflows/ci.yaml`（server-ci SQLite job + postgres-queue service job）。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-durable/60-CONTEXT.md
@.planning/phases/60-durable/60-RESEARCH.md
@.planning/phases/60-durable/60-VALIDATION.md
@server/pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 ci.yaml 骨架 + server-ci（SQLite 默认）job</name>
  <read_first>
    - server/pyproject.toml:89-118（dev 依赖组 + addopts `-m 'not perf and not integration and not slow and not postgres_queue'` + markers）
    - server/.python-version（pin 的 Python 版本，CI setup 对齐）+ 仓库根 Makefile（uv 安装/运行约定）
    - .planning/phases/60-durable/60-RESEARCH.md「Environment Availability（workflow 已删，须从零建）」「Pitfall 4」「Open Question 1（最小聚焦 vs 复原 413 行）」
    - 历史参考（只读，不复原全部）：`git show 5579e45f2^:.github/workflows/ci.yaml`（changes / server-ci 结构骨架）
  </read_first>
  <action>
    新建 `.github/workflows/ci.yaml`：`name`、`on`（push 到主分支 + pull_request；可加 paths 过滤 `server/**` 聚焦），`concurrency`（group 按 ref + cancel-in-progress）。`jobs.server-ci`：`runs-on: ubuntu-latest`，步骤 checkout → 安装 uv（`astral-sh/setup-uv`，启用 cache）→ 按 `server/.python-version` 装 Python → `working-directory: server` 下 `uv sync`（含 dev 组）→ `uv run python manage.py check` → `uv run ruff check .` → `uv run pytest`（默认 addopts 即 SQLite 且排除 postgres_queue，无需显式 `-m`）。该 job 即"SQLite 默认路径不受影响"的零回归门禁。不引入 web/runner/docs/task/security-scan job（聚焦范围，RESEARCH Open Q1）。
    用真实 action 版本（`actions/checkout@v4`、`astral-sh/setup-uv@v…` 取当前稳定大版本，不臆造），不写占位伪版本。
  </action>
  <acceptance_criteria>
    - `.github/workflows/ci.yaml` 存在且是合法 YAML（`python -c "import yaml,sys; yaml.safe_load(open('.github/workflows/ci.yaml'))"` 解析无异常）。
    - 含 `jobs.server-ci`，其测试步骤为 `uv run pytest`（依赖 addopts 默认排除 postgres_queue，不显式覆盖 `-m`）。
    - workflow 不含 web/runner/docs/task 构建 job（聚焦 server）。
    - action 版本均为真实存在的大版本标签（无 `@vX.Y.Z-placeholder` 类伪值）。
  </acceptance_criteria>
  <verify>
    <automated>python -c "import yaml; d=yaml.safe_load(open('.github/workflows/ci.yaml')); assert 'server-ci' in d['jobs']"</automated>
  </verify>
  <done>聚焦 server 的 CI workflow 骨架 + server-ci SQLite 零回归 job 就位，YAML 合法、action 版本真实。</done>
</task>

<task type="auto">
  <name>Task 2: postgres-queue job（postgres:17-alpine service + -m postgres_queue）</name>
  <read_first>
    - server/pyproject.toml:105-118（postgres_queue marker 已由 Plan 01 注册 + addopts 排除）
    - server/tests/durable/test_procrastinate_backend.py / test_stalled_rescue.py（Plan 03：被 CI 跑的 postgres 测试 + conftest 的 socket/skip fixture）
    - server/durable/backends.py / settings.py（Plan 03：条件注册 procrastinate.contrib.django——migrate 建 procrastinate 表的前提）
    - .planning/phases/60-durable/60-RESEARCH.md「Pitfall 1/2/3（addopts / pytest-socket / 迁移）」「Validation Architecture（Postgres 专项命令）」
    - .planning/phases/60-durable/60-VALIDATION.md「Manual-Only Verifications（real kill-worker human_needed，forged-heartbeat 自动）」
  </read_first>
  <action>
    在 `ci.yaml` 增 `jobs.postgres-queue`：`runs-on: ubuntu-latest`，`services.postgres` 用 `postgres:17-alpine`（env `POSTGRES_USER/PASSWORD/DB`，`ports: 5432:5432`，`options` 配 `pg_isready` health check）。步骤：checkout → 装 uv + Python → `working-directory: server` `uv sync` → 设 job 级 env `DATABASE_URL=postgres://…@127.0.0.1:5432/…`、`DURABLE_TASK_BACKEND=procrastinate`、`FRIDAY_PROCESS_ROLE=test`（或 migrate 步骤用 `migrate` 角色）→ `uv run python manage.py migrate`（对 Postgres 建含 procrastinate 的全部表，Pitfall 3）→ `uv run pytest -m postgres_queue --allow-hosts=127.0.0.1,localhost`（覆盖 addopts 默认 `-m`，放行 pytest-socket TCP，Pitfall 1/2）。该 job 即 DURABLE-04 覆盖 defer/priority/retry-backoff/stalled rescue/并发竞争/SQLite fallback 退化的绿灯门禁。job 注释标注：real kill-worker E2E 为 human_needed（不在 CI 自动化），forged-heartbeat rescue 已自动覆盖（VALIDATION）。
  </action>
  <acceptance_criteria>
    - `ci.yaml` 含 `jobs.postgres-queue`，`services` 用镜像 `postgres:17-alpine` 且配 `pg_isready` health check。
    - 测试步骤为 `uv run pytest -m postgres_queue --allow-hosts=127.0.0.1,localhost`，前置有 `manage.py migrate`，job env 含 `DATABASE_URL`(postgres) + `DURABLE_TASK_BACKEND=procrastinate`。
    - `grep -n "postgres:17-alpine" .github/workflows/ci.yaml` 命中；`grep -n "postgres_queue" .github/workflows/ci.yaml` 命中。
    - YAML 整体合法；server-ci 与 postgres-queue 两 job 并存。
  </acceptance_criteria>
  <verify>
    <automated>python -c "import yaml; d=yaml.safe_load(open('.github/workflows/ci.yaml')); j=d['jobs']; assert 'postgres-queue' in j and 'server-ci' in j; assert 'postgres:17-alpine' in open('.github/workflows/ci.yaml').read()"</automated>
  </verify>
  <done>postgres-queue job 用 postgres:17-alpine service + migrate + -m postgres_queue 跑 Plan 03 测试，与 SQLite server-ci 共存；YAML 合法、marker/镜像断言命中。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → Postgres service container | 测试连接 127.0.0.1:5432（pytest-socket 默认禁 socket，需显式放行） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-09 | DoS / 正确性 | 默认 CI 误跑 postgres_queue（无 Postgres）全红 | mitigate | server-ci 用默认 addopts 排除；仅 postgres-queue job 显式 `-m postgres_queue` + service container |
| T-60-10 | 正确性 | pytest-socket 拦截 Postgres 连接致 job 假红 | mitigate | postgres-queue 步骤 `--allow-hosts=127.0.0.1,localhost`（Pitfall 2） |
| T-60-11 | 正确性 | 缺 migrate 致 `relation "procrastinate_jobs" does not exist` | mitigate | 测试前 `manage.py migrate`（条件注册 procrastinate.contrib.django 后建表，Pitfall 3） |
| T-60-SC | Supply Chain | GH Actions 第三方 action | accept | 仅用 actions/checkout、astral-sh/setup-uv 等知名官方 action，钉大版本标签 |
</threat_model>

<verification>
- `python -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yaml'))"` 解析无异常。
- `.github/workflows/ci.yaml` 含 server-ci（SQLite）+ postgres-queue（postgres:17-alpine）两 job。
- 推送后 GitHub Actions：server-ci 绿（SQLite 默认路径不受影响）、postgres-queue 绿（durable Postgres 行为覆盖）。
- （人工，VALIDATION human_needed）双 worker 真实 kill-worker rescue E2E。
</verification>

<success_criteria>
- Postgres 专项 CI job（GH Actions service container + `postgres_queue` marker）绿，覆盖 defer/priority/retry-backoff/stalled rescue/并发 worker 竞争/SQLite fallback 退化路径。
- 与默认 SQLite 测试路径共存（marker 分层，默认 job 仍走 SQLite）。
</success_criteria>

<artifacts>
## Artifacts this phase produces (Plan 04)

**New files**
- `.github/workflows/ci.yaml` — `jobs.server-ci`（SQLite 默认零回归）+ `jobs.postgres-queue`（postgres:17-alpine service，`-m postgres_queue`）

**CI 约定**
- 默认套件：`uv run pytest`（addopts 默认排除 postgres_queue → SQLite）
- Postgres 专项：`uv run pytest -m postgres_queue --allow-hosts=127.0.0.1,localhost`（前置 `manage.py migrate`，env `DATABASE_URL`=postgres + `DURABLE_TASK_BACKEND=procrastinate`）

**Consumed（来自 Plan 01/03）**
- `postgres_queue` marker（Plan 01）、`test_procrastinate_backend.py` / `test_stalled_rescue.py`（Plan 03）、条件 `procrastinate.contrib.django` 迁移（Plan 03）
</artifacts>

<output>
Create `.planning/phases/60-durable/60-04-SUMMARY.md` when done.
</output>
