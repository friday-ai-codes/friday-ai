---
phase: 61-migrate
plan: 03
type: execute
wave: 2
depends_on: ["61-01"]
files_modified:
  - server/durable/reconcile.py
  - server/repositories/apps.py
  - server/codegraph/apps.py
  - server/tests/repositories/test_reconcile_durable.py
autonomous: true
requirements: [MIGRATE-02]
must_haves:
  truths:
    - "启动 reconcile 标 RUNNING→FAILED 前查 durable job 接管：有在途 durable job（todo/doing/scheduled）则保留 RUNNING，不误杀（SC3）"
    - "判定 helper 提供同步入口（`async_to_sync` 包），可在 AppConfig.ready 的同步 daemon 线程安全调用，不裸 await 异步门面（SC3）"
    - "非 durable 后端（SQLite/in-process）维持旧'标 FAILED'行为（in-process 重启即丢、不承诺续跑），不留僵尸 RUNNING（SC3）"
    - "repositories/codegraph 两处 reconcile 排除集叠加/替换为'有在途 durable job'判定，既有 recoverable_target_ids 过渡保留不破坏（SC3）"
  artifacts:
    - path: "server/durable/reconcile.py"
      provides: "has_active_durable_job_sync(idempotency_key) 同步判定 helper（durable 后端经 DurableTaskService.has_active_by_key 按 queueing_lock 查在途 / 非 durable 返 False）"
    - path: "server/repositories/apps.py"
      provides: "_reset_stuck_indexing 改判定：排除有在途 durable index job 的仓库"
    - path: "server/codegraph/apps.py"
      provides: "reconcile_orphaned_graph_builds 改判定：排除有在途 durable graph job 的仓库"
    - path: "server/tests/repositories/test_reconcile_durable.py"
      provides: "有在途 durable job → 保留 RUNNING；无 → 标 FAILED；非 durable → 旧行为 守护"
  key_links:
    - from: "server/repositories/apps.py"
      to: "server/durable/reconcile.py"
      via: "has_active_durable_job_sync(f'index:{repo_id}') 排除在途仓库"
      pattern: "has_active_durable_job"
    - from: "server/codegraph/apps.py"
      to: "server/durable/reconcile.py"
      via: "has_active_durable_job_sync(f'graph:{repo_id}') 排除在途仓库"
      pattern: "has_active_durable_job"
    - from: "server/durable/reconcile.py"
      to: "durable.DurableTaskService"
      via: "async_to_sync(DurableTaskService.has_active_by_key) 按 queueing_lock 查在途"
      pattern: "DurableTaskService.has_active_by_key"
---

<objective>
把 `repositories.apps._reset_stuck_indexing` 与 `codegraph.apps.reconcile_orphaned_graph_builds` 的启动 reconcile 改判定语义：标 RUNNING→FAILED 之前先查该 repo 是否有在途 durable job，有则保留 RUNNING，**绝不误杀在途任务**。新增同步判定 helper `durable/reconcile.py` 解决"同步 daemon 线程调异步门面"陷阱。

Purpose: MIGRATE-02 的"启动 reconcile 安全语义"——迁移后生产 index/graph 走 durable，多副本下某 worker 在跑的任务，另一进程启动 reconcile 不能无脑标 FAILED。Phase 60 已加 role 门禁，本 plan 改判定逻辑（查 durable 接管）。
Output: 同步判定 helper + 两处 reconcile 改判定 + 守护测试。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-migrate/61-RESEARCH.md
@.planning/phases/61-migrate/61-CONTEXT.md

@server/durable/service.py
@server/durable/backends.py
@server/repositories/apps.py
@server/codegraph/apps.py
@server/resumable/recovery.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: durable/reconcile.py —— has_active_durable_job 同步/异步判定 helper</name>
  <files>server/durable/reconcile.py, server/tests/repositories/test_reconcile_durable.py</files>
  <read_first>
    - server/durable/service.py（`DurableTaskService.has_active_by_key(idempotency_key)` async 门面，由 Plan 61-01 Task 3 交付——按 queueing_lock/in-process `_jobs` 查在途，活跃集 procrastinate={todo,doing,scheduled}、in-process={pending,running}）+ `use_procrastinate_backend()`
    - server/durable/backends.py:289 `ProcrastinateBackend.get`——`int(job_id)` 按**数字** job id 查，传 deterministic key 必返 `unknown`（**故本 helper 绝不用 `get`+deterministic key**，改用 `has_active_by_key`）
    - server/resumable/recovery.py:94 `recoverable_target_ids`（现同步排除集范式，对齐其同步签名）
  </read_first>
  <behavior>
    - durable 后端（postgres_queue 标记）：某 idempotency_key 有 todo/doing/scheduled 的真实 durable job → `has_active_durable_job_sync(key)` 返回 True；无活跃 job（仅 succeeded/failed/不存在）→ False。
    - 非 durable 后端（SQLite，默认）：`has_active_durable_job_sync(key)` 恒返回 False（in-process 重启即丢、不承诺续跑 → reconcile 维持旧"标 FAILED"，避免僵尸 RUNNING）。
    - 不抛：未知 key / 查询异常一律返回 False（fail-safe 朝"标 FAILED"侧，绝不因查询失败而保留僵尸）。
  </behavior>
  <action>
    新建 `server/durable/reconcile.py`（**不 import procrastinate**，仅经 `DurableTaskService` 门面）。提供异步 + 同步双版本（对齐 resumable/service 同步+异步范式）：
    - `async def has_active_durable_job(idempotency_key: str) -> bool`：先 `from durable.service import use_procrastinate_backend`，非 durable 直接返回 False（维持旧"标 FAILED"）；durable 路径 `return await DurableTaskService.has_active_by_key(idempotency_key)`（Plan 01 Task 3 门面，按 queueing_lock 查 todo/doing/scheduled——**不得**调 `DurableTaskService.get(deterministic_key)`，后者按数字 job id 查恒返 unknown 会误判 False 致误杀）。整段 try/except 吞异常返回 False（fail-safe）。
    - `def has_active_durable_job_sync(idempotency_key: str) -> bool`：`from asgiref.sync import async_to_sync` 包 `has_active_durable_job`（供 AppConfig.ready 同步 daemon 线程调用）。
    中文 docstring 说明"经 has_active_by_key 按 queueing_lock 查 / 非 durable 维持旧标 FAILED / fail-safe 朝标 FAILED 侧 / 同步入口避免裸 await"。
    同时新建守护测试 `server/tests/repositories/test_reconcile_durable.py` 的 helper 级用例：
    - **真实 durable 路径（`@pytest.mark.postgres_queue` + `enable_socket`，复用 conftest `durable_service`/`procrastinate_app`）**：经 `DurableTaskService.defer(..., idempotency_key=key)` 落一个 queueing_lock=key 的真实 todo job（或 forge/seed `procrastinate_jobs` 行）→ `has_active_durable_job_sync(key)` 为 True；不存在的 key → False。**不再 monkeypatch `DurableTaskService.get`**。
    - **in-process facade 路径（默认 SQLite）**：经 `durable.backends._set_job_state(key, status="running")` 注入在跑态后，monkeypatch `use_procrastinate_backend` → True（强制走 durable 分支验证门面委托），断言 `has_active_durable_job_sync(key)` 为 True；succeeded/未知 key → False。
    - **非 durable 维持旧行为**：默认 `use_procrastinate_backend()` False（SQLite）→ `has_active_durable_job_sync(key)` 恒 False；门面/查询抛异常 → False。
  </action>
  <acceptance_criteria>
    - `server/durable/reconcile.py` 存在且 `rg -n "import procrastinate|from procrastinate" server/durable/reconcile.py` 零命中；`rg -n "DurableTaskService.get" server/durable/reconcile.py` **零命中**（只用 `has_active_by_key`）。
    - `has_active_durable_job` 经 `DurableTaskService.has_active_by_key` 判定；`has_active_durable_job_sync` 经 `async_to_sync` 提供同步入口。
    - 非 durable 后端下恒返回 False；查询异常返回 False。
    - helper 级守护测试通过（postgres_queue 用例 seed 真实 job、SQLite 用例验证 in-process facade + 非 durable 旧行为）。
  </acceptance_criteria>
  <verify>
    <automated>cd server && rg -c "import procrastinate|from procrastinate" durable/reconcile.py; test $? -ne 0 && echo OK && uv run pytest tests/repositories/test_reconcile_durable.py -q</automated>
  </verify>
  <done>同步/异步判定 helper 就位，非 durable/异常侧 fail-safe，零 procrastinate 直接 import。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: repositories/codegraph 两处 reconcile 接入 has_active_durable_job_sync</name>
  <files>server/repositories/apps.py, server/codegraph/apps.py</files>
  <read_first>
    - server/repositories/apps.py:34 `_reset_stuck_indexing`（现 `recoverable_target_ids(INDEX)` 排除集 + `Repository.objects.filter(index_status=INDEXING).exclude(id__in=...)` + IndexHistory RUNNING 收尾）
    - server/codegraph/apps.py:10 `reconcile_orphaned_graph_builds`（现 `recoverable_target_ids(GRAPH)` 排除集 + GraphBuildHistory RUNNING + 仓库聚合态归位）
    - server/durable/reconcile.py（Task 1 产出 `has_active_durable_job_sync`）
  </read_first>
  <behavior>
    - 有在途 durable index job 的仓库（`has_active_durable_job_sync(f"index:{repo_id}")` 为 True）不被 `_reset_stuck_indexing` 标 FAILED（保留 INDEXING + 不收尾对应 IndexHistory RUNNING）。
    - 无在途 durable job 的 INDEXING 仓库照旧标 FAILED（不留僵尸）。
    - codegraph 同形（key=`graph:{repo_id}`），orphan GraphBuildHistory 仅当无在途 durable graph job 才回收。
    - 非 durable 后端（SQLite）：helper 恒 False → 维持现有标 FAILED 行为零回归。
  </behavior>
  <action>
    两处 reconcile 在现有 `recoverable_target_ids` 排除集基础上**叠加** durable 在途判定（保留 recoverable_target_ids 过渡兼容，per RESEARCH「函数可保留供过渡」）：
    - `repositories/apps.py._reset_stuck_indexing`：取候选 INDEXING 仓库 id 后，对每个 id 调 `has_active_durable_job_sync(f"index:{repo_id}")`，把为 True 的并入排除集（与既有 recoverable_ids 取并集），再 `.exclude(id__in=排除集)`。IndexHistory RUNNING 收尾同样排除有在途 durable job 的仓库（按 repository_id 过滤），避免误杀在途任务的进度行。`from durable.reconcile import has_active_durable_job_sync`（函数体内 import，避免启动期循环）。整段沿用既有 try/except 包裹（表未就绪退化）。
    - `codegraph/apps.py.reconcile_orphaned_graph_builds`：orphan_qs 取 repo_ids 后，对每个调 `has_active_durable_job_sync(f"graph:{repo_id}")`，为 True 的并入排除集，从回收集剔除（保留 RUNNING + 不归位仓库聚合态）。
    **N+1 处置（已评估，按"小 N 可接受"收口）**：逐 repo 调 `has_active_durable_job_sync` 是 per-key 查询，但 (a) SQLite/非 durable 路径下 `has_active_durable_job` 首句 `use_procrastinate_backend()` 即短路返 False，**整段 O(1)、零 per-repo 查询**（dev/pytest 无放大）；(b) Postgres 路径下 N = 单次启动 reconcile 命中的"卡住 INDEXING 仓库"数，业务上有界且小（通常个位数）。故**不引入批量 `queueing_lock IN (...)` 查询**（会要求 Plan 01 门面再加 `active_keys(keys)` API、扩散改动面），保持 per-repo helper 调用。若后续实测 N 偏大，再在 `durable/reconcile.py` 增批量版本（不影响本判定语义）。中文注释说明"durable 接管才保留 RUNNING / 非 durable helper 恒 False 维持旧行为 / N 小可接受"。注意 helper 调用在同步上下文（已是 daemon 线程 / 同步函数），用 `_sync` 版本。
    扩展守护测试 `test_reconcile_durable.py`：seed INDEXING 仓库 + RUNNING IndexHistory，monkeypatch `has_active_durable_job_sync` 为 True → 断言仓库仍 INDEXING、IndexHistory 仍 RUNNING；为 False → 断言标 FAILED。codegraph 同形一用例。
  </action>
  <acceptance_criteria>
    - 两处 reconcile 均出现 `has_active_durable_job_sync` 调用，key 分别为 `index:{repo_id}` / `graph:{repo_id}`。
    - 守护测试：has_active=True → 仓库/History 保留 RUNNING（不误杀）；has_active=False → 标 FAILED（不留僵尸）。
    - 非 durable 路径（helper 恒 False）下 reconcile 行为与改造前一致（既有 reconcile 测试零回归）。
    - `cd server && uv run pytest tests/repositories tests/codegraph -q` 绿。
  </acceptance_criteria>
  <verify>
    <automated>cd server && uv run pytest tests/repositories/test_reconcile_durable.py tests/repositories tests/codegraph -q</automated>
  </verify>
  <done>两处 reconcile 接入 durable 在途判定，有接管保留 RUNNING、无接管标 FAILED、非 durable 零回归。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 启动 daemon 线程（同步）→ durable 异步门面 | 经 `async_to_sync` 包装，绝不在同步上下文裸 await |
| reconcile 判定 → durable job 状态 | 仅经 `DurableTaskService` 门面查询，不直接 import procrastinate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-08 | Denial of Service | reconcile 误杀在途 durable 任务 | mitigate | 标 FAILED 前查 durable job 接管，有 todo/doing/scheduled 则保留 RUNNING（Task 2 守护测试） |
| T-61-09 | Denial of Service | 同步 daemon 线程裸 await 异步门面崩溃 | mitigate | helper 提供 `async_to_sync` 同步入口（Pitfall 2） |
| T-61-10 | Repudiation | 非 durable 误"保留 RUNNING"留僵尸 | mitigate | 非 durable 后端 helper 恒 False → 维持旧标 FAILED；查询异常 fail-safe 朝标 FAILED 侧 |
</threat_model>

<verification>
- `cd server && uv run pytest tests/repositories tests/codegraph -q` 全绿（含新 test_reconcile_durable + 既有 reconcile 零回归）。
- `rg -n "has_active_durable_job" server/repositories/apps.py server/codegraph/apps.py` 命中两处。
- `rg -n "import procrastinate|from procrastinate" server/durable/reconcile.py` 零命中。
</verification>

<success_criteria>
- reconcile 仅在确认无 durable job 接管时才标 RUNNING→FAILED，不误杀在途（SC3）。
- 同步入口安全（async_to_sync），非 durable 维持旧行为不留僵尸（SC3）。
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 03)
- `server/durable/reconcile.py`（新）—— has_active_durable_job 同步/异步判定 helper
- `server/repositories/apps.py`（改）—— _reset_stuck_indexing 改判定
- `server/codegraph/apps.py`（改）—— reconcile_orphaned_graph_builds 改判定
- `server/tests/repositories/test_reconcile_durable.py`（新）—— reconcile 安全语义守护
</artifacts_produced>

<output>
Create `.planning/phases/61-migrate/61-03-SUMMARY.md` when done
</output>
