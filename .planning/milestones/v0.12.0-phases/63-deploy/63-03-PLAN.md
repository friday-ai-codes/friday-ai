---
phase: 63-deploy
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - server/services/git_platform/base.py
  - server/services/git_platform/gitlab_client.py
  - server/services/git_platform/github_client.py
  - server/workflows/nodes/ai/coding.py
  - server/workflows/nodes/integrations/feishu_chat.py
  - server/delivery/services/work_item_service.py
  - server/tests/workflows/test_coding_mr_dedup.py
  - server/tests/workflows/test_chat_nodes.py
autonomous: true
requirements: [IDEMP-02]
must_haves:
  truths:
    - "_create_mr_for_repo 创建 MR 前先查同 source→target 分支是否已有 open MR/PR，命中则复用其 URL/ID 不重复创建（IDEMP-02）"
    - "CreateGroupChatNode 建群前先查 WorkItem.feishu_chat_id（仅 project_key+work_item_id 均非空时），命中则跳过 create_chat 复用既有 chat_id（IDEMP-02）"
    - "git 平台 client（GitLab/GitHub）新增 find_open_merge_request(source_branch, target_branch) 能力（OQ4：原无此方法）"
    - "无 WorkItem 锚 / 无既有 MR 时 fence 退化 no-op，行为与现状逐字等价（零回归 + fail-soft）"
  artifacts:
    - path: "server/services/git_platform/base.py"
      provides: "find_open_merge_request 抽象方法"
      contains: "find_open_merge_request"
    - path: "server/services/git_platform/gitlab_client.py"
      provides: "GitLab mergerequests.list(source/target, state=opened) 实现"
      contains: "find_open_merge_request"
    - path: "server/services/git_platform/github_client.py"
      provides: "GitHub get_pulls(head/base, state=open) 实现"
      contains: "find_open_merge_request"
    - path: "server/delivery/services/work_item_service.py"
      provides: "aget_feishu_chat_id 读访问器（建群前 fence 查询）"
      contains: "aget_feishu_chat_id"
    - path: "server/tests/workflows/test_coding_mr_dedup.py"
      provides: "MR fence 守护测试"
      contains: "find_open_merge_request"
  key_links:
    - from: "server/workflows/nodes/ai/coding.py"
      to: "server/services/git_platform/base.py"
      via: "_create_mr_for_repo 调 client.find_open_merge_request 前置 fence"
      pattern: "find_open_merge_request"
    - from: "server/workflows/nodes/integrations/feishu_chat.py"
      to: "server/delivery/services/work_item_service.py"
      via: "CreateGroupChatNode 建群前调 aget_feishu_chat_id"
      pattern: "aget_feishu_chat_id"
---

<objective>
IDEMP-02：给 at-least-once 重投 / 节点重入下的外部副作用上 reuse-first fencing——MR/PR 创建前查既有 open MR、飞书建群前查 `WorkItem.feishu_chat_id`，命中即复用不重复执行。不引入 outbox 表（v2 DURABLEX-02），复用既有写回字段 + 平台天然幂等。

Purpose: 重复发的 MR 创建 / 建群不再产生重复外部动作（不重复开 PR / 不重复建群）；爬取入库（`run_crawl_ingest`）的外部副作用已由 `aarchive_exists` fence（无缺口）。
Output: git 平台 client 新增 `find_open_merge_request` + coding MR 前置 fence + `WorkItemService.aget_feishu_chat_id` 读访问器 + CreateGroupChatNode 前置 fence + 守护测试。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-deploy/63-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: git 平台 client 新增 find_open_merge_request（contract-first，IDEMP-02）</name>
  <files>server/services/git_platform/base.py, server/services/git_platform/gitlab_client.py, server/services/git_platform/github_client.py</files>
  <read_first>
    - server/services/git_platform/base.py（GitPlatformClient 抽象方法形态：create_merge_request / branch_exists 范式）
    - server/services/git_platform/gitlab_client.py（`_get_project()` + `project.mergerequests.create` + `asyncio.to_thread` + `branch_exists` 降级范式 + `gitlab.exceptions`）
    - server/services/git_platform/github_client.py（`_get_repo()` + `repo.create_pull(head=, base=)` + `GithubException` 降级范式 + self.owner）
    - server/services/git_platform/models.py（MRCreateResult: success/mr_url/mr_id/has_conflicts/error）
  </read_first>
  <behavior>
    - find_open_merge_request(source_branch, target_branch) 命中既有 open MR → 返回 MRCreateResult(success=True, mr_url=..., mr_id=...)（复用既有）
    - 无既有 open MR → 返回 None
    - 平台异常 → 返回 None（fail-soft，不上抛；让调用方照常尝试创建，绝不因 fence 查询失败阻断）
  </behavior>
  <action>
    在 `base.py` 的 `GitPlatformClient` 新增抽象方法（**非 @abstractmethod 强制双实现以免破坏既有实例化**——改为带默认 `return None` 的普通 async 方法，子类覆盖，避免抽象方法新增导致实例化 TypeError 回归）：`async def find_open_merge_request(self, source_branch: str, target_branch: str) -> MRCreateResult | None`，docstring 中文说明"查同 source→target 的 open MR/PR，命中返回复用结果，无则 None，异常 fail-soft 返回 None"。import `MRCreateResult`（已 import）。
    GitLab 实现（`gitlab_client.py`）：`project = self._get_project()`；`mrs = await asyncio.to_thread(project.mergerequests.list, source_branch=source_branch, target_branch=target_branch, state="opened", get_all=False)`；命中首个 → `MRCreateResult(success=True, mr_url=mr.web_url, mr_id=str(mr.iid))`；空 → None；`except Exception: logger.warning("gitlab_find_open_mr_failed", ...); return None`（token 绝不入日志）。
    GitHub 实现（`github_client.py`）：`repo = self._get_repo()`；`pulls = await asyncio.to_thread(lambda: list(repo.get_pulls(state="open", head=f"{self.owner}:{source_branch}", base=target_branch)))`；命中首个 → `MRCreateResult(success=True, mr_url=pr.html_url, mr_id=str(pr.number))`；空 → None；`except GithubException: logger.warning(...); return None`。
    不改 create_merge_request / 既有方法（纯新增，零回归）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/workflows/test_coding_mr_dedup.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "find_open_merge_request" server/services/git_platform/base.py server/services/git_platform/gitlab_client.py server/services/git_platform/github_client.py` 三文件均命中；
    - 既有 git_platform 实例化零回归：`cd server && uv run pytest tests/ -k "git_platform or coding_pr" -q` 绿（新增非抽象方法不破坏既有子类实例化）；
    - GitLab 用 `state="opened"`、GitHub 用 `state="open"` + `head=f"{owner}:{branch}"`（grep 确认平台差异）。
  </acceptance_criteria>
  <done>双平台 client 提供 find_open_merge_request（fail-soft 返回 None），契约就位供 Task 2 消费，既有实例化零回归。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: coding _create_mr_for_repo 创建前 existing-MR fence（IDEMP-02）</name>
  <files>server/workflows/nodes/ai/coding.py, server/tests/workflows/test_coding_mr_dedup.py</files>
  <read_first>
    - server/workflows/nodes/ai/coding.py（`_create_mr_for_repo` 1805-1896：token 解析 / get_git_platform_client / resolved_target / MRCreateRequest / create_merge_request / 返回 dict 形态含 mr_url/mr_id/has_conflicts/description/error）
    - server/tests/workflows/test_coding_pr_target_branch.py（直测私有方法 `_create_mr_for_repo` 的 MagicMock 仓 + AsyncMock client 范式 — 镜像它建新测试文件）
    - server/services/git_platform/base.py（Task 1 新增的 find_open_merge_request 契约）
  </read_first>
  <behavior>
    - 已有 open MR（find_open_merge_request 返回 success 结果）→ 跳过 create_merge_request，返回复用的 mr_url/mr_id（不重复创建）
    - 无既有 MR（返回 None）→ 照常 create_merge_request（现状路径逐字等价）
    - find 抛错（fail-soft 返回 None）→ 照常创建（绝不因 fence 查询失败阻断创建）
  </behavior>
  <action>
    在 `_create_mr_for_repo` 内、`request = MRCreateRequest(...)` 之后、`client.create_merge_request(request)` 之前，插入前置 fence：`existing = await client.find_open_merge_request(branch_name, resolved_target)`；若 `existing and existing.success`：`log.info("mr_dedup_reuse_existing", mr_url=existing.mr_url, mr_id=existing.mr_id)` 并 `return {"mr_url": existing.mr_url, "mr_id": existing.mr_id, "has_conflicts": False, "description": body, "deduplicated": True}`（保留 description 供 PR-02 cross-ref 拼接，新增 `deduplicated` 标记位）。其余创建/失败分支逐字不变（零回归命门）。fence 查询本身已 fail-soft（Task 1 返回 None），无需额外 try。
    新建 `server/tests/workflows/test_coding_mr_dedup.py`（镜像 test_coding_pr_target_branch.py 结构）：构造 MagicMock repository（default_branch 等 _create_mr_for_repo 实访问字段）+ AsyncMock client；
    - test_mr_dedup_reuse：`client.find_open_merge_request` 返回 `MRCreateResult(success=True, mr_url="http://x/mr/1", mr_id="1")` → 断言返回 mr_url 复用 且 `client.create_merge_request` **未被调用**（assert_not_awaited）；
    - test_mr_create_when_none：`find_open_merge_request` 返回 None → 断言 `create_merge_request` 被调用一次（现状路径不回退）；
    - test_mr_fence_failsoft：`find_open_merge_request` 返回 None（模拟内部已 swallow）→ 仍走创建（验证不阻断）。
    patch token 解析（`aresolve_git_token`）与 `get_git_platform_client` 返回 AsyncMock client（沿用 target_branch 测试的打桩方式）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/workflows/test_coding_mr_dedup.py tests/workflows/test_coding_pr_target_branch.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd server && uv run pytest tests/workflows/test_coding_mr_dedup.py -q` 全绿（含 reuse / create / fail-soft 三例）；
    - `grep -n "find_open_merge_request" server/workflows/nodes/ai/coding.py` 命中前置 fence 调用；
    - 既有 `tests/workflows/test_coding_pr_target_branch.py` 零回归（dedup 注入不改既有 4 测）；
    - `tests/test_coding_wave.py` 零回归：`cd server && uv run pytest tests/test_coding_wave.py -q` 绿。
  </acceptance_criteria>
  <done>MR 创建前查既有 open MR，命中复用不重复创建；无既有 MR / fence 失败照常创建（零回归 + fail-soft）。</done>
</task>

<task type="auto">
  <name>Task 3: 建群前置 feishu_chat_id fence + WorkItemService 读访问器（IDEMP-02）</name>
  <files>server/delivery/services/work_item_service.py, server/workflows/nodes/integrations/feishu_chat.py, server/tests/workflows/test_chat_nodes.py</files>
  <read_first>
    - server/delivery/services/work_item_service.py（`awriteback_feishu_chat_id` / `_writeback_feishu_chat_id_sync` 199-261：三元组定位 + @sync_to_async 私有同步块范式 — 镜像它写读访问器）
    - server/workflows/nodes/integrations/feishu_chat.py（CreateGroupChatNode.execute 354-437：缺参判定 / create_chat 调用 / writeback 块 / 输出 dict 形态）
    - server/tests/workflows/test_chat_nodes.py（CreateGroupChatNode 既有 14 例：happy/缺参/建群失败/writeback；patch `delivery.services.work_item_service.WorkItemService.awriteback_feishu_chat_id` 范式 — 同源 patch 新读方法）
  </read_first>
  <action>
    `work_item_service.py` 新增读访问器 `async def aget_feishu_chat_id(self, feishu_project_key, work_item_type, work_item_id) -> str | None`（镜像 writeback 的 @sync_to_async 私有同步块）：三元组 `WorkItem.objects.filter(feishu_project_key=, work_item_type=, work_item_id=).values_list("feishu_chat_id", flat=True).first()`；返回非空 chat_id 或 None；WorkItem 不存在 / 字段空 → None；DB 异常不吞（与 writeback 风格一致，由调用方 fail-soft）。仅读不写，绝不触 `_refresh_mirror` / `_MIRROR_FIELDS`。
    `feishu_chat.py` CreateGroupChatNode.execute：在缺参校验通过后、`im_service.create_chat(...)` 之前，插入前置 fence——**仅当 `project_key` 与 `work_item_id_str` 均非空且可 int() 时**（与既有 writeback 的锚一致；先把 project_key/work_item_id/work_item_type 的解析上移或复用）：`try: existing_chat_id = await WorkItemService().aget_feishu_chat_id(project_key, work_item_type, work_item_id) except Exception: existing_chat_id = None`（fail-soft）；若 `existing_chat_id`：`log.info("create_chat_dedup_reuse", chat_id=existing_chat_id)` 并 `return NodeResult(status="completed", output={"chat_id": existing_chat_id, "chat_name": name, "owner_id": "", "source": "create_group_chat", "deduplicated": True, "writeback": {"attempted": False, "success": False}}, next_handle="default")`（复用既有 chat_id，跳过 create_chat）。无 WorkItem 锚（project_key/work_item_id 缺）→ 不查、照常建群（fence 退化 no-op，per RESEARCH provenance：节点入口无锚时建群本就 fail-soft）。其余建群/writeback 逻辑逐字不变（零回归）。
    `test_chat_nodes.py` 扩展（不破坏既有 14 例）：
    - test_create_group_fenced：patch `WorkItemService.aget_feishu_chat_id` 返回既有 chat_id → 断言 `FeishuIMService.create_chat` **未被调用**（assert_not_awaited / mock 计数 0）且返回 deduplicated chat_id；
    - test_create_group_no_anchor_creates：无 project_key/work_item_id → aget 不被调用、create_chat 正常调用（现状零回归）；
    - test_create_group_fence_failsoft：`aget_feishu_chat_id` 抛异常 → 节点仍照常建群（不阻断）。
    patch 源同 Phase 59 NOTE-3：节点内函数级 import，patch `delivery.services.work_item_service.WorkItemService.aget_feishu_chat_id`（类属性）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/workflows/test_chat_nodes.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd server && uv run pytest tests/workflows/test_chat_nodes.py -q` 全绿（既有 14 例 + 新增 3 例零回归）；
    - `grep -n "aget_feishu_chat_id" server/delivery/services/work_item_service.py server/workflows/nodes/integrations/feishu_chat.py` 两文件均命中（定义 + 调用）；
    - 建群 fence：已有 feishu_chat_id 时 create_chat 未被调（test_create_group_fenced 通过）；
    - 无锚 / fence 失败时建群路径零回归（两测通过）。
  </acceptance_criteria>
  <done>建群前查 WorkItem.feishu_chat_id 命中即复用跳过 create_chat；无锚/异常 fail-soft 退化为现状（零回归）。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| callback 重投 / 节点重入 → MR 创建 | 同 source→target 分支重复创建会产生重复 PR |
| 节点重入 → 飞书建群 | 重复执行会建多个群 |
| fence 查询 → git 平台 / DB | 查询失败不得阻断主动作（fail-soft） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-08 | Tampering（重复开 PR） | _create_mr_for_repo | mitigate | 创建前 find_open_merge_request 查既有 open MR，命中复用（平台天然幂等 + 显式检查），无新表 |
| T-63-09 | Tampering（重复建群） | CreateGroupChatNode | mitigate | 建群前查 WorkItem.feishu_chat_id（Phase 59 writeback 字段），命中复用，无新表 |
| T-63-10 | Denial of Service（fence 失败阻断主动作） | fence 查询 | mitigate | 全部 fail-soft：find 返回 None / aget 异常被吞 → 照常创建，绝不阻断 |
| T-63-11 | Information Disclosure | git 平台查询 | mitigate | client 日志仅记 source/target/error，token 绝不入日志（沿用既有范式） |
| T-63-SC | Tampering | npm/pip/cargo installs | accept | 本 plan 不新增任何包（RESEARCH Package Legitimacy Audit：无新增；复用 python-gitlab / PyGithub 既有依赖） |
</threat_model>

<verification>
- `cd server && uv run pytest tests/workflows/test_coding_mr_dedup.py tests/workflows/test_chat_nodes.py -q` 绿。
- 既有零回归：`cd server && uv run pytest tests/workflows/test_coding_pr_target_branch.py tests/test_coding_wave.py -q` 绿。
- 真实 GitLab/GitHub 重复 MR 抑制 / 真实建群去重 → human_needed（需真实平台 + 飞书应用）。
</verification>

<success_criteria>
- IDEMP-02：飞书自动建群 + MR/PR 创建上 fencing，at-least-once 重复执行不产生重复外部动作（不重复建群 / 不重复开 PR）；reuse-first（复用 feishu_chat_id 写回字段 + 平台 existing-MR 查询），无 outbox 表，无锚/异常时 fail-soft 退化为现状。
</success_criteria>

## Artifacts this phase produces (Plan 03)

- `server/services/git_platform/base.py`（新增 `find_open_merge_request` 默认方法）
- `server/services/git_platform/gitlab_client.py` / `github_client.py`（find_open_merge_request 双平台实现）
- `server/workflows/nodes/ai/coding.py`（`_create_mr_for_repo` 前置 existing-MR fence）
- `server/delivery/services/work_item_service.py`（新增 `aget_feishu_chat_id` 读访问器）
- `server/workflows/nodes/integrations/feishu_chat.py`（CreateGroupChatNode 前置 feishu_chat_id fence）
- `server/tests/workflows/test_coding_mr_dedup.py`（新建，MR fence 守护）
- `server/tests/workflows/test_chat_nodes.py`（扩展，建群 fence 守护）

<output>
Create `.planning/phases/63-deploy/63-03-SUMMARY.md` when done.
</output>
