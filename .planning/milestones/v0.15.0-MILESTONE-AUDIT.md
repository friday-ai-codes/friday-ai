---
milestone: v0.15.0 项目（交付上下文聚合根）
phases: [76, 77, 78, 79, 80, 81]
audited: 2026-06-26T02:55:00Z
status: passed
requirements_total: 38
requirements_satisfied: 38
integration: integration_ok
auditor: Claude (gsd-verifier, milestone-level goal-backward)
tests:
  makemigrations_check: "No changes detected (exit 0)"
  targeted_domain_subset: "120 passed (tests/initiatives + mcp lookup + context packer + chat recall + feishu identity), 28.4s"
  documented_baseline: "38 known pre-existing failures + 1 flaky cross-suite ordering test (test_webhook_dedup_same_sha)；逐 phase VERIFICATION 全量零新增回归（76:6266 → 81:6421 passed）"
accepted_deferred:
  - "真实飞书「项目跟踪」看板端到端枚举/事件触发——子项关联字段 key / 人员字段 key / 角色标签 / 事件 work_item_type_key 的真实 payload 校验（需真实飞书应用；代码层以集中映射表 + 保守默认 + fail-soft 降级覆盖）"
  - "真实飞书 doc/表格在线查看渲染 + RAG 摄取端到端（需真实飞书凭证 + 文档/多维表格；后端 fail-soft 兜底，前端已兜底渲染）"
  - "真实 Cursor 客户端经本地 MCP 分支反查 + 上报写回端到端（需真实 Cursor + Friday PAT）"
  - "真实 GitHub/GitLab MR webhook 投递驱动状态同步 + 真实 LLM 记忆蒸馏质量（需真实平台/真实 provider）"
  - "飞书 bitable 列结构解析（当前返回原始记录，列解析留 v2 REL-03）"
---

# 里程碑审计：v0.15.0 项目（交付上下文聚合根）（Phases 76–81）

**审计方式:** 里程碑级 goal-backward（从里程碑原始意图反推，逐条核验 38 需求在代码层落地 + 跨 Phase 集成自洽 + 第一性原理/约束未被破坏，不轻信 SUMMARY/per-phase 声明）。
**审计时间:** 2026-06-26T02:55:00Z
**结论:** ✅ **passed** — 38/38 需求在代码层满足、跨 Phase 数据链自洽、约束（零数据丢失重构 / INV-6 单一写入 / 脱敏不可绕过 / 观测埋点 / 不做迭代实体 / UI 稿仅元数据）全部遵守；剩余项均为"真实外部系统才能终态确认"的 accepted-deferred，不构成 gap。

---

## 1. 里程碑意图回顾

把"需求 → 代码"全链路上下文统一收口到一个**在线协作的「项目」聚合根**——每个飞书"项目跟踪"看板对应一个项目，聚合需求/工件依赖/工作项(story·缺陷)/记忆/关联知识/仓库/分支/PR；项目对成员共享可参与，飞书人员经身份映射关联到 Friday 用户带身份；任何对话、Cursor 编码、Agent 调用都能从项目加载完整上下文并把沉淀写回。前置硬约束：先把现有 `projects.Project`（前端"空间"）重命名为 `Space`，腾出 `Project` 名给新聚合根，数据零丢失、行为零回归。

线性 6 Phase：76 命名腾挪 → 77 聚合根+身份+成员 → 78 飞书触发+组合 → 79 工件+知识关联 → 80 记忆+MR+召回 → 81 Cursor 回流+前端工作台。

各 Phase VERIFICATION 判定均为 `passed`（76: 4/4 SC；77: 5/5 SC；78: 4/4 SC；79: 4/4 SC；80: 5/5 SC；81: 5/5 SC）。

---

## 2. 本次实跑结果

| 检查 | 命令 | 结果 |
|------|------|------|
| 迁移完整性 | `uv run python manage.py makemigrations --check --dry-run` | **No changes detected**（exit 0） |
| 新域测试子集 | `uv run pytest tests/initiatives tests/mcp_tools/test_lookup_project_by_branch.py tests/services/test_project_context_packer.py tests/test_chat_project_recall.py tests/feishu/test_feishu_identity.py` | **120 passed**, 44 warnings, 28.4s, exit 0 |

> 按 prompt 约定未跑全量 6000+ 套件（过慢），全量基线信任各 Phase VERIFICATION：76=6266 / 77=6294 / 78=6315 / 79=6352 / 80=6390 / 81=6421 passed，逐 Phase `diff` 确认 **38 failed == baseline，零新增回归**；第 39 个 `test_webhook_dedup_same_sha` 为 prompt 明示的 flaky cross-suite ordering（单跑通过）。warnings 为第三方弃用（pkg_resources/lark_oapi/utcfromtimestamp），与本里程碑无关。

---

## 3. 需求覆盖表（38 行 → satisfied / 交付物）

| # | Requirement | Phase | Evidence file / symbol | 状态 |
|---|-------------|-------|------------------------|------|
| 1 | RENAME-01 `Project`→`Space` 数据零丢失 | 76 | `projects/models.py::Space`(`db_table="projects"` 保留) + 迁移 `projects/0010`（纯 RenameModel/RenameField/AlterField，**零** DeleteModel/CreateModel） | ✅ |
| 2 | RENAME-02 全栈 `project→space` 引用一致更新 | 76 | `agents/tools/space_tools.py` / workflow `fetch_space_info` / `SpaceMembership`·`SpaceRepository`·`Repository.spaces` M2M；对外字段经 `source="space"` 显式保留 | ✅ |
| 3 | PROJ-01 `Project` 聚合根 + `ProjectService`(INV-6) | 77 | `initiatives/models/project.py::Project`（space FK/feishu_project_key/status/created_by）+ `services/project_service.py::ProjectService` + `test_project_inv6_guard.py` | ✅ |
| 4 | PROJ-02 状态机 fail-loud + AuditEvent | 77 | `ProjectService` 状态合法表 + `ProjectTransitionError` + `AuditService.aemit`(action=project.status_changed) | ✅ |
| 5 | PROJ-03 CRUD REST fail-closed（Space 权限） | 77 | `initiatives/views.py` + `permissions.py`（复用 `PermissionService`）；`test_project_api.py` 403/201/200 | ✅ |
| 6 | PROJ-04 项目↔项目关联 | 77/79 | `initiatives/models/relation.py::ProjectRelation` + KnowledgeGraph `link_project`（KnowledgeEdge） | ✅ |
| 7 | PROJ-05 前端手动建项目 + (space,feishu_project_key) 幂等 | 77 | `web/src/components/project/CreateProjectModal.vue` + `web/src/api/projects.ts`；`test_create_is_idempotent_on_space_feishu_key` | ✅ |
| 8 | IDENT-01 `resolve_feishu_user`(manual/jit/fail-soft) | 77 | `feishu/services/identity.py::resolve_feishu_user` + `feishu.FeishuUserBinding`(迁移 0007)；`test_feishu_identity.py`(6 用例) | ✅ |
| 9 | MEMBER-01 成员多对多 + 身份角色 | 77 | `initiatives/models/member.py::ProjectMember`/`ProjectRole`（unique(project,user)） | ✅ |
| 10 | MEMBER-02 主R 唯一可转移 + 增删改审计 | 77 | partial unique(project, role=owner) + `ProjectService` 成员写入 + `test_project_members.py`(7 用例) | ✅ |
| 11 | MEMBER-03 成员/状态变更 WS 实时推送 | 77 | `initiatives/consumers.py::ProjectConsumer` + `services/realtime.py::apush_project_event` | ✅ |
| 12 | FSPROJ-01 飞书看板枚举（子项+人员带角色，fail-soft 降级） | 78 | `services/feishu_project_board.py::enumerate_board`（无整板 API 经字段派生 + `degraded=True`） | ✅ |
| 13 | FSPROJ-02 飞书事件幂等建项目 + 拉人带身份 | 78 | `feishu/views.py::is_project_tracking_event`+`_maybe_schedule_project_board_sync` → `services/project_board_sync.py::sync_from_board`（`initiated_by_user_id`） | ✅ |
| 14 | FSPROJ-03 工作流 `create_project` 节点 | 78 | `workflows/nodes/integrations/create_project.py::CreateProjectNode`（`@register_node`，经同源 sync_from_board，不直写表） | ✅ |
| 15 | COMPOSE-01 story 复用 `delivery.WorkItem` 经关系边挂入/移除 | 78 | `initiatives/models/work_item_link.py::ProjectWorkItemLink` + `ProjectService.attach/detach_work_item` | ✅ |
| 16 | COMPOSE-02 缺陷同经关系边挂入不重复建模 | 78 | `test_story_and_defect_both_link_without_remodeling`（同表 through + provenance） | ✅ |
| 17 | ARTIFACT-01 `ArtifactType` 注册表（内置 8 类 seed）+ 后台增删禁用 | 79 | `initiatives/models/artifact.py::ArtifactType` + seed 迁移 `0004_seed_artifact_types.py`(8 类) + REST `/api/artifact-types/` | ✅ |
| 18 | ARTIFACT-02 `Artifact` 实例（类型/载体/链接/标题/版本/贡献者） | 79 | `Artifact` 模型 + `services/artifact_service.py::ArtifactService`(INV-6) | ✅ |
| 19 | ARTIFACT-03 在线查看（飞书 doc/表格渲染、外链跳转、md 可编辑） | 79 | `services/artifact_view.py::aget_artifact_view` + `GET .../artifacts/<id>/view/`；`test_artifact_view.py` | ✅ |
| 20 | ARTIFACT-04 文字载体全文进 `delivery_knowledge`；图形仅元数据 | 79 | `knowledge/sources/artifact.py`(注册 "artifact"，ragable 全文入；external_link 仅元数据 normalize 返回 []) | ✅ |
| 21 | ARTIFACT-05 禁用不可新建/既有只读 + 删除受实例保护 | 79 | `ArtifactDisabledError` + builtin 禁删 + DB `PROTECT`；`test_artifact_type_service.py` | ✅ |
| 22 | KLINK-01 项目↔知识 M2M | 79 | `services/knowledge_graph.py::ProjectKnowledgeGraphService.link_knowledge`（幂等，经 KnowledgeEdge） | ✅ |
| 23 | KLINK-02 项目↔仓库/空间/知识/项目经 KnowledgeEdge 可查询 | 79 | `link_project/link_repository/link_space` + `query_graph`（graph_store 多跳）；`EntityKind` +project/repository/space（迁移 `knowledge/0007`） | ✅ |
| 24 | MEM-01 项目记忆自由文本 append/edit + 时间戳/贡献者，成员共享 | 80 | `initiatives/models/memory.py::ProjectMemory`+`ProjectMemoryRevision` + `services/memory_service.py::MemoryService`(INV-6) | ✅ |
| 25 | MEM-02 贡献仅限成员；私聊/非成员不纳入 | 80 | `MemoryService._assert_member` → `MemoryPermissionError`(fail-closed)；`test_non_member_cannot_contribute_fail_closed` | ✅ |
| 26 | MEM-03 人工编辑/覆盖可追溯 | 80 | `edit`/`supersede` 每次 append `ProjectMemoryRevision`；`test_edit_preserves_history_via_revisions` | ✅ |
| 27 | MEM-04 LLM 草稿→人工确认入库 + 脱敏不可绕过 | 80 | `services/memory_distill.py::MemoryDistiller.distill_to_draft`→`create_draft`(pending) + `confirm_draft`；`redact_secrets_in_text` 双重脱敏；`call_source=memory_distill` | ✅ |
| 28 | RECALL-01 context packer（聚合 + grep + RAG + 排序压缩 + token 预算降级） | 80 | `services/project_context_packer.py::pack_project_context`；`test_token_budget_degradation_trims_low_priority` | ✅ |
| 29 | RECALL-02 Web 对话绑定项目自动加载 + `search_delivery_knowledge` 入白名单 | 80 | `chat/models.py::Conversation.bound_project`(迁移 0028) + `chat/config.py::_maybe_pack_project_context` + `chat_runner.py:108` 工具白名单 | ✅ |
| 30 | RECALL-03 召回 fail-closed + 写 `RetrievalTrace` | 80 | packer 非成员零召回 + `arecord_retrieval_trace`(source=`chat_project_context`，counts/layer_timing_ms/scores) | ✅ |
| 31 | MR-01 `MergeRequest` 实体经 `MergeRequestService`(INV-6) | 80 | `initiatives/models/merge_request.py::MergeRequest` + `services/mr_service.py::MergeRequestService` + `test_merge_request_inv6_guard.py` | ✅ |
| 32 | MR-02 入站 webhook 同步状态（脱敏 payload）项目内可见 | 80 | `initiatives/webhook_views.py::GitMergeRequestWebhookView`（GitHub HMAC / GitLab token fail-closed + `redact_for_ledger` + 幂等 dedup_key）+ `GET .../merge-requests/` | ✅ |
| 33 | CURSOR-01 MCP 分支→项目反查 + 召回（MCP 链 RetrievalTrace） | 81 | `mcp_tools/views.py::lookup_project_by_branch` + `services/branch_parsing` + `ProjectWorkItemLink` 反查 + `pack_project_context`(source=`mcp_lookup_project_by_branch`) | ✅ |
| 34 | CURSOR-02 Cursor rules 模板（强制先召回再编码） | 81 | `initiatives/services/cursor_rules.py::build_project_cursor_rules`(.mdc, alwaysApply) + `GET .../cursor-rules/` | ✅ |
| 35 | CURSOR-03 沉淀上报写回（认证+归因+脱敏+质量门槛→draft） | 81 | `mcp_tools/views.py::report_project_knowledge` + `services/cursor_writeback.py`(质量门槛) → `MemoryService.create_draft`(pending)；`initiated_by_user_id` | ✅ |
| 36 | UI-01 项目列表页（Space/状态/成员筛选）+ 创建 | 81 | `web/src/pages/projects/index.vue` + `CreateProjectModal.vue`；`projects-list.spec.ts` | ✅ |
| 37 | UI-02 项目详情工作台（6 Tab：概览/成员/工作项/工件/记忆/关联） | 81 | `web/src/pages/projects/[id]/index.vue` + `components/project/workbench/{Overview,Members,WorkItems,Artifacts,Memory,Links}Tab.vue` | ✅ |
| 38 | UI-03 记忆 LLM 提议确认 UI + 工件类型后台管理页 | 81 | `workbench/MemoryTab.vue`(草稿确认) + `web/src/pages/admin/artifact-types/index.vue`(增删禁用) | ✅ |

**覆盖率：38/38 satisfied，0 unsatisfied，0 orphaned。**

---

## 4. 跨 Phase 集成核验（独立确认，非引述 SUMMARY）

| 集成链 | 核验点 | 结论 |
|--------|--------|------|
| 命名腾挪贯穿全栈 | `Space` 重命名后 `WorkItem.space`/`Conversation.space`/`Workflow.space`/M2M `Repository.spaces` 一致；`Project` 名腾给 `initiatives.Project`，两者无冲突 | ✅ 自洽 |
| ProjectWorkItemLink → MCP 反查 | `mcp_tools/views.py::lookup_project_by_branch` 经 `ProjectWorkItemLink` 由 `m{work_item_id}` 反查 `Project`（Phase 78 建表 → Phase 81 消费） | ✅ 闭合 |
| context packer 复用于双链 | `services/project_context_packer.py::pack_project_context` 同时被 `chat/config.py`(Web 对话) 与 `mcp_tools/views.py`(Cursor MCP) 调用，单一打包器 | ✅ 端到端贯通 |
| ArtifactService → 知识摄取 | `knowledge/sources/artifact.py` 注册 "artifact" source，文字载体入 `delivery_knowledge`，经 `pack_project_context` 召回面覆盖 | ✅ 闭合 |
| MemoryService draft ← Cursor 写回 | `report_project_knowledge` → `cursor_writeback`(质量门槛) → `MemoryService.create_draft`(pending，非 active)，与 MEM-04 同入口 | ✅ 复用一致 |
| RetrievalTrace 双链补齐 | packer 写 `chat_project_context`（Phase 80）；MCP 反查写 `mcp_lookup_project_by_branch`（Phase 81 补齐 Phase-80 deferred 的 MCP 链） | ✅ 双链覆盖 |
| KnowledgeEdge 统一关联 | 项目↔知识/项目/仓库/空间均经 `KnowledgeGraphService` + `KnowledgeEdge`，`sync_relations_from_operational` 单向派生不双写 | ✅ 复用脊柱 |
| 迁移齐备 | `initiatives/0001~0005` + `feishu/0007`(FeishuUserBinding) + `knowledge/0007`(EntityKind) + `chat/0028`(bound_project)；`makemigrations --check` 干净 | ✅ |

**集成结论：integration_ok。** 聚合根 → 身份/成员 → 飞书组合/工件 → 记忆/MR/召回 → Cursor 双向回流逐段实接；context packer/MemoryService/KnowledgeEdge 为单点复用，无重复建模。

---

## 5. 约束 / 第一性原理 / 跨切面核验

| 约束（MILESTONE-PROPOSAL §1/§2 + rules） | 落地证据 | 结论 |
|------------------------------------------|----------|------|
| 零数据丢失重命名（76 元数据级迁移） | `projects/0010` 仅 RenameModel/RenameField/AlterField，`db_table` 保留；全新 SQLite 全量 migrate OK；零 DeleteModel/CreateModel | ✅ 遵守 |
| INV-6 单一写入入口 | `ProjectService`/`ArtifactService`/`MemoryService`/`MergeRequestService` 均有 `test_*_inv6_guard.py` 旁路写表零命中守护 | ✅ 遵守 |
| 脱敏不可绕过 | memory（蒸馏+create_draft 双重 `redact_secrets_in_text`）、artifact 摄取（`redact_secrets_in_text`）、git webhook 原始 payload（`redact_for_ledger`）、Cursor 写回（不可绕过） | ✅ 遵守 |
| 观测埋点 | `call_source=memory_distill`（恰入枚举）+ `arecord_llm_usage`；RetrievalTrace 双链；后台任务（board_sync/webhook/写回）带 `initiated_by_user_id` | ✅ 遵守 |
| 不做迭代实体（deferred） | 无 Iteration 模型；"历史迭代"经 `ProjectRelation`/KnowledgeEdge 项目↔项目回看 | ✅ 遵守 |
| UI 稿仅存元数据（不强行 RAG 正文） | `knowledge/sources/artifact.py` 非 ragable 载体 normalize 返回 []，无实体 | ✅ 遵守 |
| 记忆人工为主 + LLM 仅产草稿 | `create_draft`(pending) 绝不自动写 active，`confirm_draft` 人工确认才入库 | ✅ 遵守 |
| 召回 fail-closed（项目 scope + 用户权限） | packer 非成员零召回；chat/MCP 非成员零注入 | ✅ 遵守 |

---

## 6. Accepted / Deferred（真实外部系统才能终态确认，非 gap）

1. **真实飞书「项目跟踪」看板端到端**：子项关联字段 key / 人员字段 key / 角色标签 / 事件 `work_item_type_key` 的真实 payload 校验 + 枚举/事件触发人工验收。代码层以集中映射表 + 保守默认 + fail-soft 降级覆盖，真实 key 经 live UAT 后补登记即可，逻辑不变（Phase 78 deferred）。
2. **真实飞书 doc/表格在线查看渲染 + RAG 摄取**：需真实飞书应用凭证 + 文档/多维表格；后端 fail-soft 返回 error 字段、前端已兜底渲染（Phase 79/81 deferred）。飞书 bitable 列结构解析留 v2（REL-03）。
3. **真实 Cursor 客户端经本地 MCP 反查 + 上报写回端到端**：需真实 Cursor 客户端 + Friday PAT（Phase 81 deferred）。
4. **真实 GitHub/GitLab MR webhook 投递 + 真实 LLM 记忆蒸馏质量**：需真实平台 webhook / 真实 provider（Phase 80 deferred）。

以上代码路径均被 mock/transaction 测试覆盖（分支齐全），仅需真机/真实外部服务终态确认——按里程碑约定属 accepted-deferred，不计 gap。

---

## 7. 结论

**status: passed** — v0.15.0 项目（交付上下文聚合根）6 个 Phase（76–81）完整交付里程碑原始意图：38/38 需求在代码层满足，新 app `initiatives` 聚合根 + 身份映射 + 飞书组合 + 工件/知识关联 + 记忆/MR/召回 + Cursor 双向回流 + 前端工作台逐层实接；命名腾挪零数据丢失、INV-6 单一写入、脱敏不可绕过、观测埋点、不做迭代实体、UI 稿仅元数据等约束全部遵守；context packer/MemoryService/KnowledgeEdge 单点复用、跨 Phase 集成自洽（integration_ok）。`makemigrations --check` 干净，新域测试子集 120 passed，全量基线零新增回归（信任逐 Phase VERIFICATION，76→81: 6266→6421 passed）。剩余项均为真实飞书/Cursor/git-webhook/LLM 等运行期才能终态确认的 accepted-deferred，不构成 gap。可收口里程碑、归档 ROADMAP。

---

_Audited: 2026-06-26T02:55:00Z · Auditor: Claude (gsd-verifier, milestone-level)_
