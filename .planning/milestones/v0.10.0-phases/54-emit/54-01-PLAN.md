---
phase: 54-emit
plan: 01
type: execute
wave: 1
depends_on: []
requirements: [AUDITCOV-01]
files_modified:
  - server/accounts/views.py
  - server/projects/members_views.py
  - server/projects/views.py
  - server/tests/audit/test_emit_identity.py
  - server/tests/audit/test_emit_no_noise_identity.py
autonomous: true
must_haves:
  truths:
    - "成员/用户增删改、用户启停、角色变更、空间配置变更、仓库权限变更均经 AuditService.aemit 产出 AuditEvent（actor=request.user + 目标 + 前后值），覆盖 SC-1（per RESEARCH §1 SC-1 #1–#10）"
    - "emit 一律引用 taxonomy.py 既有 ACTION_* 常量（member.created/updated/deleted、user.activated/deactivated、role.changed、project.config_changed、repository.permission_changed），无字符串字面量（per CONTEXT decisions / INV-2）"
    - "autocommit async 路径主操作 await 成功后直接 await AuditService.aemit(...)；唯一显式 transaction.atomic 块（accounts/views.py _atomic_create_superuser）用 transaction.on_commit(lambda: AuditService.emit(...))（per RESEARCH §4）"
    - "用户启停仅在 is_active 值真正变更时 emit（true→user.activated / false→user.deactivated），值未变不 emit（避免噪音，SC-4）（per RESEARCH §1 #3）"
    - "update/role/permission 变更在 save 前读旧值，before/after 为变更字段前后快照（非整对象 dump）；delete 在 delete 前读快照填 before（per RESEARCH §6 风险 6 / CONTEXT before/after 粒度）"
    - "空间配置变更（飞书 plugin/IM secret / webhook token / doc folder）before/after 只记变更字段名集合 + has_secret 布尔，绝不记 feishu_plugin_secret_encrypted / feishu_app_secret_encrypted / webhook token 明文（per RESEARCH §1 #8 / §6 风险 8）"
    - "读操作（用户列表/成员列表/空间详情）与自助操作（改密/改自己资料）不产生 AuditEvent（SC-4，per RESEARCH §5 SC-4）"
    - "新增 emit 仅经 AuditService，不旁路写 AuditEvent——既有 test_audit_inv6_guard.py 自动覆盖且须保持全绿（per CONTEXT specifics / INV-6）"
  artifacts:
    - path: "server/accounts/views.py"
      provides: "建用户(#1)/首启建superuser(#2 on_commit)/用户启停(#3)/管理员改资料(#4) emit 接线"
      contains: "AuditService"
    - path: "server/projects/members_views.py"
      provides: "添加成员(#5)/变更角色(#6)/移除成员(#7) emit 接线"
      contains: "AuditService"
    - path: "server/projects/views.py"
      provides: "空间配置变更(#8)/仓库权限变更(#9)/仓库关联解绑(#10) emit 接线"
      contains: "AuditService"
    - path: "server/tests/audit/test_emit_identity.py"
      provides: "SC-1 行落库 + actor + before/after + 启停分支 + 删前快照断言"
      contains: "AuditEvent.objects"
    - path: "server/tests/audit/test_emit_no_noise_identity.py"
      provides: "SC-4 身份侧读/自助操作无审计行断言"
      contains: "count"
  key_links:
    - from: "server/accounts/views.py"
      to: "audit.services.audit_service.AuditService"
      via: "用户增删改/启停成功后 await AuditService.aemit(action=ACTION_*, actor=request.user, ...)"
      pattern: "AuditService\\.aemit"
    - from: "server/projects/members_views.py"
      to: "audit.services.audit_service.AuditService"
      via: "成员增删改/角色变更成功后 await AuditService.aemit(actor=request.user, ...)"
      pattern: "AuditService\\.aemit"
    - from: "server/projects/views.py"
      to: "audit.services.taxonomy"
      via: "空间配置/仓库权限 emit 引用 ACTION_PROJECT_CONFIG_CHANGED / ACTION_REPOSITORY_PERMISSION_CHANGED 常量"
      pattern: "ACTION_(PROJECT_CONFIG_CHANGED|REPOSITORY_PERMISSION_CHANGED)"
---

<objective>
把 Phase 53 立起的 `AuditService.aemit`/`emit` 单一写入入口接线到**身份与权限类**敏感/管理操作（AUDITCOV-01 / SC-1）：accounts（邀请建用户、首启建 superuser、用户启停、管理员改资料）+ projects/members（成员增删改 + 角色变更）+ projects（空间配置变更、仓库权限变更/关联解绑），产出全量审计记录（actor=`request.user` + 目标 + 变更前后值）。

Purpose: 让"谁在何时对哪个用户/成员/空间/仓库做了什么管理变更"全程可追溯，且不产生读/自助操作噪音。
Output: 三处 view 文件的 emit 接线（全部引用 taxonomy `ACTION_*` 常量；autocommit 直接 `aemit`，唯一 atomic 块用 `on_commit`）+ `test_emit_identity.py`（SC-1 行落库/actor/前后值/启停分支/删前快照）+ `test_emit_no_noise_identity.py`（SC-4 身份侧无噪音）。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.codex/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/54-emit/54-CONTEXT.md
@.planning/phases/54-emit/54-RESEARCH.md
@server/audit/services/audit_service.py
@server/audit/services/taxonomy.py
@server/accounts/views.py
@server/projects/members_views.py
@server/projects/views.py
@server/tests/audit/test_audit_service.py
@server/tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: accounts 身份操作 emit（建用户 / 首启 superuser / 启停 / 改资料）</name>
  <read_first>
    - server/accounts/views.py:290（InvitationAcceptView.post，`User.objects.create_user`，autocommit async）
    - server/accounts/views.py:319-333（UserDetailView.patch，`is_active` → `asave`）
    - server/accounts/views.py:387-409（AdminProfileView.put，`user.asave()`）
    - server/accounts/views.py:480-525（`_atomic_create_superuser` 含 `transaction.atomic()`；首启 view 调用点 ~:520）
    - server/audit/services/audit_service.py（aemit/emit 关键字签名：action/actor/target_type/target_id/target_repr/before/after/source）
    - server/audit/services/taxonomy.py（ACTION_MEMBER_CREATED / ACTION_MEMBER_UPDATED / ACTION_USER_ACTIVATED / ACTION_USER_DEACTIVATED）
  </read_first>
  <action>
    在 server/accounts/views.py 顶部 import：`from audit.services.audit_service import AuditService`、`from audit.services import taxonomy`（或具名导入用到的 ACTION_* 常量）。所有 emit 在主操作 `await` 成功后执行，引用 taxonomy 常量，不写字符串字面量。

    #1 邀请注册建用户（`InvitationAcceptView.post`，`User.objects.create_user` 成功后，约 :296 之后、return 之前）：
      `await AuditService.aemit(action=taxonomy.ACTION_MEMBER_CREATED, actor=request.user, target_type="user", target_id=user.id, target_repr=user.username, after={"username": user.username, "source": user.source, "email": user.email}, source="api")`。
      注：邀请接受场景 `request.user` 可能为匿名/未认证（公开邀请接受）——若该 view 为 AllowAny 且 `request.user` 是 AnonymousUser，传 `actor=request.user if request.user.is_authenticated else None`，after 标 `source="invitation"`。按 view 实际权限定夺（read_first 确认）。

    #2 首启建 superuser（`_atomic_create_superuser` 的 `transaction.atomic()` 块内，create_superuser 成功后）：
      因处于显式 atomic 块且为 sync（`sync_to_async` 包裹），用 `transaction.on_commit(lambda: AuditService.emit(action=taxonomy.ACTION_MEMBER_CREATED, actor=None, target_type="user", target_id=user.id, target_repr=user.username, after={"username": user.username, "source": "system", "is_superuser": True}, source="system"))`（sync emit + on_commit；首启无 human actor → actor=None）。

    #3 用户启停（`UserDetailView.patch`，:329-331）：在 `target_user.is_active = bool(...)` **赋值前**先存 `old_active = target_user.is_active`；仅当 `new_active != old_active` 时在 `asave` 成功后 emit：
      `action = taxonomy.ACTION_USER_ACTIVATED if new_active else taxonomy.ACTION_USER_DEACTIVATED`；
      `await AuditService.aemit(action=action, actor=request.user, target_type="user", target_id=target_user.id, target_repr=target_user.username, before={"is_active": old_active}, after={"is_active": new_active}, source="api")`。
      值未变更则跳过 emit（SC-4 噪音控制）。

    #4 管理员改资料（`AdminProfileView.put`，:400-405）：在赋值前读旧值 `old = {"username": user.username, "display_name": user.display_name}`；`user.asave()` 成功后，构造仅含**实际变更字段**的 before/after diff（如仅改 username 则 before/after 只含 username），emit：
      `await AuditService.aemit(action=taxonomy.ACTION_MEMBER_UPDATED, actor=request.user, target_type="user", target_id=user.id, target_repr=user.username, before=<changed-old>, after=<changed-new>, source="api")`。无字段实际变更则不 emit。

    刻意不接线（SC-4）：`ChangePasswordView` / `ForceChangePasswordView` / `AdminChangePasswordView`（自助改密非管理审计点）、`UserListView.get`（读）。
  </action>
  <acceptance_criteria>
    - server/accounts/views.py contains `AuditService` 且 import `taxonomy`（或 ACTION_* 具名常量）。
    - `_atomic_create_superuser` 路径 emit 经 `transaction.on_commit`（grep `on_commit` 在该函数附近命中）；其余三处为 `await AuditService.aemit`。
    - 启停 emit 仅在值变更分支内（emit 调用在 `if new_active != old_active:` 块内）。
    - 无字符串 action 字面量（grep `action="member` / `action="user` 在本文件无命中——全走 taxonomy 常量）。
    - `cd server && uv run python manage.py check` 退出码 0。
  </acceptance_criteria>
  <verify>
    <automated>cd server && uv run python manage.py check</automated>
  </verify>
  <done>accounts 四类身份操作接线完成：建用户/启停/改资料直接 aemit，首启 superuser 用 on_commit；启停仅值变更时 emit。</done>
  <commit>feat(54-01): accounts 身份操作接线审计 emit（建用户/启停/改资料 + 首启 superuser on_commit）</commit>
</task>

<task type="auto">
  <name>Task 2: projects/members 成员增删改 + 角色变更 emit</name>
  <read_first>
    - server/projects/members_views.py:82-91（SpaceMemberListView.post，`ProjectMembership.objects.create`）
    - server/projects/members_views.py:111-133（SpaceMemberDetailView.patch，`membership.save(update_fields=["role"])`）
    - server/projects/members_views.py:135-152（SpaceMemberDetailView.delete，`membership.delete()`）
    - server/audit/services/taxonomy.py（ACTION_MEMBER_CREATED / ACTION_ROLE_CHANGED / ACTION_MEMBER_DELETED）
  </read_first>
  <action>
    在 server/projects/members_views.py 顶部 import `AuditService` 与 `taxonomy`。全部 autocommit async 路径 → 主操作 `await sync_to_async(...)` 成功后直接 `await AuditService.aemit(...)`，actor=`request.user`。

    #5 添加成员（:82 create 成功、:89 重取 select_related 后，return 前）：
      `await AuditService.aemit(action=taxonomy.ACTION_MEMBER_CREATED, actor=request.user, target_type="project_membership", target_id=membership.id, target_repr=f"{target_user.username} @ {project.name}", after={"user_id": str(target_user.id), "project_id": str(project.id), "role": role}, source="api")`。

    #6 变更成员角色（:130 改 role **前**先存 `old_role = membership.role`；:131 `save(update_fields=["role"])` 成功后 emit）：
      `await AuditService.aemit(action=taxonomy.ACTION_ROLE_CHANGED, actor=request.user, target_type="project_membership", target_id=membership.id, target_repr=f"{membership.user.username} @ {project.name}", before={"role": old_role}, after={"role": membership.role}, source="api")`。仅当 `old_role != membership.role` 时 emit（值未变不记，SC-4）。

    #7 移除成员（:151 `membership.delete()` **前**先快照 `snapshot = {"user_id": str(membership.user_id), "project_id": str(membership.project_id), "role": membership.role}` 与 `member_id = membership.id`、`repr = f"{membership.user.username} @ {project.name}"`；delete 成功后 emit）：
      `await AuditService.aemit(action=taxonomy.ACTION_MEMBER_DELETED, actor=request.user, target_type="project_membership", target_id=member_id, target_repr=repr, before=snapshot, source="api")`。
  </action>
  <acceptance_criteria>
    - server/projects/members_views.py contains `AuditService.aemit` 三处（create/patch/delete 各一）。
    - 角色变更在 `save` 前读 `old_role`（grep `old_role` 命中）；删除在 `delete()` 前快照（before 含删前 role/user_id/project_id）。
    - emit 全部引用 `taxonomy.ACTION_*`（无 `action="member` / `action="role` 字面量）。
    - `cd server && uv run python manage.py check` 退出码 0。
  </acceptance_criteria>
  <verify>
    <automated>cd server && uv run python manage.py check</automated>
  </verify>
  <done>成员增删改 + 角色变更三处 emit 接线完成，角色/删除均在写前读旧值/快照。</done>
  <commit>feat(54-01): projects 成员增删改与角色变更接线审计 emit（写前快照前后值）</commit>
</task>

<task type="auto">
  <name>Task 3: projects 空间配置变更 + 仓库权限变更/关联解绑 emit</name>
  <read_first>
    - server/projects/views.py:170,178（feishu_config PUT/DELETE）、:268（refresh_webhook_token）、:301（update_webhook_token）、:338,345（feishu_im PUT/DELETE）、:436（feishu_doc PUT）
    - server/projects/views.py:120（link）/:138（unlink）/:618（批量 link）/:675（SpaceRepositoryDetailView.patch permission_level）/:688（SpaceRepositoryDetailView.delete）
    - server/audit/services/taxonomy.py（ACTION_PROJECT_CONFIG_CHANGED / ACTION_REPOSITORY_PERMISSION_CHANGED）
    - server/audit/services/redaction.py（确认 feishu_*_secret / token 键名命中入口脱敏——接线侧仍只传字段名集合 + has_secret 布尔）
  </read_first>
  <action>
    在 server/projects/views.py 顶部 import `AuditService` 与 `taxonomy`。全部 autocommit async → 主操作成功后 `await AuditService.aemit(...)`，actor=`request.user`。

    #8 空间配置变更（feishu plugin/IM/doc config + webhook token）——统一用 `ACTION_PROJECT_CONFIG_CHANGED`，**before/after 只记变更字段名集合 + has_secret 布尔，绝不记任何 secret/token 值**：
      每个落库点（PUT 写入 / DELETE 清空 / refresh/update webhook token 成功后）：
      `await AuditService.aemit(action=taxonomy.ACTION_PROJECT_CONFIG_CHANGED, actor=request.user, target_type="project", target_id=project.id, target_repr=project.name, after={"changed": ["feishu_plugin_id", ...], "has_secret": True/False}, metadata={"config_subtype": "feishu_plugin"|"feishu_im"|"feishu_doc"|"webhook_token"}, source="api")`。
      DELETE/清空场景用 before 记被清字段名集合。**接线侧禁止把 `feishu_plugin_secret_encrypted` / `feishu_app_secret_encrypted` / webhook token 值放入 before/after/metadata**（双重保险，入口脱敏为兜底）。

    #9 仓库权限级别变更（`SpaceRepositoryDetailView.patch`，:675 改 permission_level **前**存 `old_level`；`link.asave(update_fields=["permission_level"])` 成功后 emit）：
      `await AuditService.aemit(action=taxonomy.ACTION_REPOSITORY_PERMISSION_CHANGED, actor=request.user, target_type="project_repository", target_id=link.id, target_repr=f"{repo} @ {project}", before={"permission_level": old_level}, after={"permission_level": link.permission_level}, source="api")`。值未变不 emit。

    #10 仓库关联/解绑空间（复用 `ACTION_REPOSITORY_PERMISSION_CHANGED`）：link（:120/批量 :618）成功后 emit after=`{"repo_id", "project_id"}`；unlink（:138）与 `SpaceRepositoryDetailView.delete`（:688）在 delete 前快照、成功后 emit before=`{"repo_id", "project_id"}`。target_type=`project_repository`，target_id=link.id，target_repr=repo@project repr。

    刻意不接线（SC-4）：空间/仓库的 list/detail GET 读、`feishu_config` GET 等读路径不 emit。
  </action>
  <acceptance_criteria>
    - server/projects/views.py 的空间配置 emit 全部用 `taxonomy.ACTION_PROJECT_CONFIG_CHANGED`；仓库权限/关联 emit 用 `taxonomy.ACTION_REPOSITORY_PERMISSION_CHANGED`。
    - 配置 emit 的 after/before/metadata **不含** `secret` / `token` 值字面量（grep `feishu_plugin_secret_encrypted` / `feishu_app_secret_encrypted` 在 emit 调用参数中无命中；仅传字段名集合 + has_secret）。
    - permission_level 变更在 `asave` 前读 `old_level`；unlink/delete 在删前快照 before。
    - `cd server && uv run python manage.py check` 退出码 0。
  </acceptance_criteria>
  <verify>
    <automated>cd server && uv run python manage.py check</automated>
  </verify>
  <done>空间配置变更（飞书/webhook 仅记字段名+has_secret）与仓库权限/关联解绑 emit 接线完成。</done>
  <commit>feat(54-01): projects 空间配置与仓库权限变更接线审计 emit（凭证型字段仅记字段名/has_secret）</commit>
</task>

<task type="auto">
  <name>Task 4: SC-1 行落库测试 + SC-4 身份侧无噪音测试</name>
  <read_first>
    - server/tests/audit/test_audit_service.py（emit 落库断言风格 + @pytest.mark.django_db）
    - server/tests/conftest.py（db / admin_user / 认证 request fixtures；async 测试 @pytest.mark.asyncio 用法）
    - server/tests/audit/test_audit_inv6_guard.py（确认新接线不触发旁路写守护）
    - 既有 accounts/projects 视图测试（如 server/tests/accounts/、server/tests/projects/）作 async view 调用范式参考
  </read_first>
  <action>
    新建 server/tests/audit/test_emit_identity.py（中文 docstring；async 路径用 pytest-asyncio；调用真实 async view method 并注入带 `request.user` 的 mock/DRF async request，或经 adrf 测试客户端）：
    - test_invite_create_emits_member_created：执行邀请建用户路径，断言 `AuditEvent.objects.filter(action="member.created", target_type="user", target_id=str(user.id)).exists()`，且 after 含 username/source。
    - test_superuser_create_emits_on_commit：首启建 superuser 后断言产 1 条 `member.created`，actor_id is None、after["is_superuser"] is True、source=="system"。
    - test_user_activate_emits_activated / test_user_deactivate_emits_deactivated：切 is_active 真/假分别断言 `user.activated` / `user.deactivated`，before/after 的 is_active 前后值正确，actor_id==request.user.id。
    - test_user_status_unchanged_no_emit：patch 时 is_active 与原值相同 → 断言**无** AuditEvent（SC-4 噪音控制）。
    - test_admin_profile_update_emits_member_updated：改 username → 断言 `member.updated`，before/after 仅含 username diff。
    - test_member_add_emits / test_role_change_emits / test_member_delete_emits：成员增/角色改/删分别断言对应 action + actor + target_repr；角色改断言 `before["role"] != after["role"]`；删除断言 before 含删前 role/user_id（删后对象不存在仍可追溯）。
    - test_space_config_change_emits_no_secret：触发飞书 plugin secret 配置写入 → 断言产 `project.config_changed`，且 `json.dumps(event.before)+json.dumps(event.after)+json.dumps(event.metadata)` **不含** 任何 secret/token 明文（只含字段名集合 + has_secret 布尔）。
    - test_repo_permission_change_emits：改 permission_level → 断言 `repository.permission_changed`，before/after 级别前后值正确。

    新建 server/tests/audit/test_emit_no_noise_identity.py（SC-4）：
    - test_user_list_get_no_audit / test_member_list_get_no_audit / test_space_detail_get_no_audit：调对应 GET 读路径后断言 `AuditEvent.objects.count() == 0`。
    - test_self_change_password_no_audit：调 ChangePasswordView 自助改密后断言无 AuditEvent（刻意不审计自助操作）。
    - 每个测试用例前置 `AuditEvent.objects.all().delete()` 不可行（append-only）→ 用独立 django_db 事务隔离 + 计数断言「该操作未新增审计行」（记录调用前后 count 差为 0）。
  </action>
  <acceptance_criteria>
    - server/tests/audit/test_emit_identity.py 与 test_emit_no_noise_identity.py 存在且含上述用例函数。
    - `cd server && uv run pytest tests/audit/test_emit_identity.py tests/audit/test_emit_no_noise_identity.py -q` 全部通过。
    - `cd server && uv run pytest tests/audit/test_audit_inv6_guard.py -q` 仍全绿（新接线无旁路写）。
  </acceptance_criteria>
  <verify>
    <automated>cd server && uv run pytest tests/audit/test_emit_identity.py tests/audit/test_emit_no_noise_identity.py tests/audit/test_audit_inv6_guard.py -q</automated>
  </verify>
  <done>SC-1 身份/权限 emit 行落库（actor/前后值/启停分支/删前快照）全绿；SC-4 身份侧读/自助无噪音断言全绿；INV-6 守护未破。</done>
  <commit>test(54-01): 补身份/权限 emit 行落库与 SC-4 无噪音单测</commit>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 01)

- **修改文件（emit 接线）**：
  - `server/accounts/views.py` — `InvitationAcceptView.post`(#1)、`_atomic_create_superuser`(#2, on_commit)、`UserDetailView.patch`(#3)、`AdminProfileView.put`(#4) 接入 `AuditService.aemit/emit`
  - `server/projects/members_views.py` — `SpaceMemberListView.post`(#5)、`SpaceMemberDetailView.patch`(#6)、`SpaceMemberDetailView.delete`(#7)
  - `server/projects/views.py` — 空间配置变更(#8)、仓库权限变更(#9)、仓库关联/解绑(#10)
- **新文件（测试）**：
  - `server/tests/audit/test_emit_identity.py`
  - `server/tests/audit/test_emit_no_noise_identity.py`
- **复用既有符号（不新建）**：`AuditService.emit/aemit`、taxonomy `ACTION_MEMBER_CREATED/UPDATED/DELETED`、`ACTION_USER_ACTIVATED/DEACTIVATED`、`ACTION_ROLE_CHANGED`、`ACTION_PROJECT_CONFIG_CHANGED`、`ACTION_REPOSITORY_PERMISSION_CHANGED`（均 Phase 53 既成）
- **决策落地**：决策点 B（#8 空间配置 vs 凭证）→ 本 plan 统一用 `ACTION_PROJECT_CONFIG_CHANGED` + metadata `config_subtype` 标注子类型，凭证值绝不入载荷
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 审计只记已提交事实 | autocommit 路径主操作 await 成功后才 emit；唯一 atomic 块（_atomic_create_superuser）用 on_commit，事务回滚不留审计行 |
| 凭证型配置字段不落明文 | 空间配置 emit 仅传字段名集合 + has_secret 布尔；feishu secret / webhook token 值绝不入 before/after/metadata（入口脱敏为第二道防线） |
| 审计写入唯一收口 | 所有新接线仅经 AuditService.aemit/emit；INV-6 grep 守护断言无旁路写 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-01-01 | Repudiation | 身份/权限管理操作 | mitigate | 成员/用户增删改、启停、角色、空间配置、仓库权限全量 emit（actor + target + 前后值），不可抵赖 |
| T-54-01-02 | Information Disclosure | 空间飞书凭证字段 | mitigate | 配置 emit 仅记字段名 + has_secret，禁传 secret/token 值；入口 `_redact_audit_payload` 兜底 |
| T-54-01-03 | Tampering | atomic 块审计 | mitigate | `_atomic_create_superuser` 用 transaction.on_commit，回滚不留审计行 |
| T-54-01-04 | DoS / 噪音 | 审计可用性 | mitigate | 启停/角色/权限仅值真正变更时 emit；读/自助操作不 emit（SC-4） |
</threat_model>

<verification>
- `cd server && uv run ruff format --check accounts projects tests/audit && uv run ruff check accounts projects tests/audit`
- `cd server && uv run python manage.py check`
- `cd server && uv run python manage.py makemigrations --check --dry-run`（应无新 migration——本 plan 仅 view/test，无模型变更）
- `cd server && uv run pytest tests/audit/ -q`（含 Phase 53 既有套件，全绿）
</verification>

<success_criteria>
- 成员/用户增删改、用户启停、角色变更、空间配置变更、仓库权限变更均经 `AuditService.aemit/emit` 产出 AuditEvent（actor + 目标 + 前后值）——SC-1 覆盖。
- emit 全部引用 taxonomy `ACTION_*` 常量，无字符串字面量；autocommit 直接 aemit，唯一 atomic 块用 on_commit。
- 启停/角色/权限仅值变更时 emit；读操作/自助改密不产生审计行——SC-4 身份侧覆盖。
- 凭证型空间配置字段仅记字段名 + has_secret，DB 无 secret/token 明文。
- INV-6 grep 守护保持全绿（新接线无旁路写）。
- 映射：AUDITCOV-01（身份/权限类全量 emit）；SC-1（行落库 + actor + 前后值）、SC-4（身份侧无噪音）。SC-2/SC-3 归 54-02。
</success_criteria>

<output>
Create `.planning/phases/54-emit/54-01-SUMMARY.md` when done
</output>
