---
phase: 54-emit
plan: 02
type: execute
wave: 1
depends_on: []
requirements: [AUDITCOV-02]
files_modified:
  - server/audit/services/taxonomy.py
  - server/tests/audit/test_audit_taxonomy.py
  - server/system/views.py
  - server/repositories/views.py
  - server/projects/views.py
  - server/access_tokens/views.py
  - server/feishu/views.py
  - server/services/purge_reconcile.py
  - server/tests/audit/test_emit_credentials.py
  - server/tests/audit/test_emit_purge_consolidation.py
  - server/tests/audit/test_emit_no_noise_credentials.py
autonomous: true
must_haves:
  truths:
    - "Provider/Git 实例/per-repo Git/飞书凭证增删改、Agent API key(PAT) 增吊、飞书同步触发均经 AuditService.aemit 产出 AuditEvent，覆盖 SC-2（per RESEARCH §1 SC-2 #11–#25）"
    - "排除规则增删（v0.5 邻接埋点）与清理任务 purge 埋点（#26）收口统一 AuditEvent，保留既有 logger.info 不破坏既有 purge 结构化日志/流程，覆盖 SC-3（per RESEARCH §1 #24/#25/#26 / CONTEXT v0.5 收口）"
    - "purge 收口在 services/purge_reconcile.py log_purge_event 单点追加 AuditService.emit（sync），三处调用点（:239/:253/:304）零改动；RESERVED_ACTIONS 的 purge.started/completed 提升为具名 ACTION_PURGE_STARTED/ACTION_PURGE_COMPLETED 并纳入 ALL_ACTIONS（per RESEARCH §2 / §4）"
    - "凭证字段绝不进 before/after 明文：接线侧只传非敏感标识（provider_type/scope/host/name/has_token 布尔/token_prefix），encrypted_config/api_key/access_token/app_secret/PAT 明文/token_hash 绝不入载荷（per RESEARCH §1 #11/#16/#20 / §6 风险 8 / CONTEXT 脱敏）"
    - "PAT 吊销幂等：仅 token.revoked_at is None 首吊分支 emit pat.revoked，重复 revoke 不重复 emit（per RESEARCH §1 #21 / §6 风险 1）"
    - "飞书 webhook 自动同步（#23，决策 A）actor=None + source=feishu_webhook，仅在 _dispatch_to_workflows 真正派发成功点 emit 一次，不对 url_verification/duplicate/ignored 事件 emit；手动重试（#22）actor=request.user + source=api（per RESEARCH §2 决策 A / §6 风险 7）"
    - "refresh-models 等运维读取不 emit；list/detail 读路径不 emit（SC-4 噪音控制，per RESEARCH §5 / §6 风险 1）"
    - "新增 emit 仅经 AuditService，不旁路写 AuditEvent——既有 test_audit_inv6_guard.py 自动覆盖且保持全绿（INV-6）"
  artifacts:
    - path: "server/audit/services/taxonomy.py"
      provides: "ACTION_PURGE_STARTED/ACTION_PURGE_COMPLETED 具名常量 + 纳入 ALL_ACTIONS（purge 从 RESERVED 提升）"
      contains: "ACTION_PURGE_STARTED"
    - path: "server/system/views.py"
      provides: "Provider 凭证 CRUD/toggle/set_default emit 接线"
      contains: "AuditService"
    - path: "server/repositories/views.py"
      provides: "Git 实例凭证 + 排除规则增删 emit 接线"
      contains: "AuditService"
    - path: "server/access_tokens/views.py"
      provides: "PAT 创建/吊销 emit 接线（吊销幂等）"
      contains: "AuditService"
    - path: "server/feishu/views.py"
      provides: "飞书同步触发 emit（手动 actor=user / webhook actor=None）"
      contains: "AuditService"
    - path: "server/services/purge_reconcile.py"
      provides: "log_purge_event 追加 AuditService.emit（purge 收口单点）"
      contains: "AuditService.emit"
    - path: "server/tests/audit/test_emit_credentials.py"
      provides: "SC-2 凭证行落库 + 脱敏 DB 断言 + PAT 幂等 + 飞书同步 actor 分支"
      contains: "AuditEvent.objects"
    - path: "server/tests/audit/test_emit_purge_consolidation.py"
      provides: "SC-3 purge 收口（保留 logger.info + 产 2 条 AuditEvent）"
      contains: "log_purge_event"
  key_links:
    - from: "server/services/purge_reconcile.py"
      to: "audit.services.audit_service.AuditService"
      via: "log_purge_event 体内 logger.info 后追加 AuditService.emit(action=ACTION_PURGE_*, source='scheduler')"
      pattern: "AuditService\\.emit"
    - from: "server/audit/services/taxonomy.py"
      to: "ALL_ACTIONS"
      via: "ACTION_PURGE_STARTED/COMPLETED 纳入 ALL_ACTIONS（从 RESERVED 提升）"
      pattern: "ACTION_PURGE_(STARTED|COMPLETED)"
    - from: "server/system/views.py"
      to: "audit.services.taxonomy"
      via: "Provider 凭证 emit 引用 ACTION_CREDENTIAL_CREATED/UPDATED/DELETED"
      pattern: "ACTION_CREDENTIAL_"
---

<objective>
把 `AuditService.aemit`/`emit` 接线到**凭证与数据治理类**敏感操作并完成 **v0.5 purge 埋点收口**（AUDITCOV-02 / SC-2 / SC-3）：Provider 凭证 CRUD + toggle/set_default（system）、Git 实例凭证 + per-repo Git 凭证（repositories/projects）、排除规则增删（repositories）、Agent API key/PAT 增吊（access_tokens）、飞书同步触发（feishu，手动 + webhook 自动）；并把 `services/purge_reconcile.py:log_purge_event` 收口为「保留结构化日志 + 追加 AuditEvent emit」，同时把 `RESERVED_ACTIONS` 的 purge 字符串提升为具名 `ACTION_PURGE_*` 常量纳入 `ALL_ACTIONS`。

Purpose: 凭证治理与清理操作全程可追溯，且凭证密文/明文绝不落审计；统一 v0.5 分散埋点到单一 AuditEvent 表。
Output: taxonomy purge 常量提升 + 五处 view 文件 emit 接线 + purge 单点收口 + `test_emit_credentials.py` / `test_emit_purge_consolidation.py` / `test_emit_no_noise_credentials.py`（含脱敏 DB 断言、PAT 幂等、webhook actor=None、读无噪音）。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.codex/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/54-emit/54-CONTEXT.md
@.planning/phases/54-emit/54-RESEARCH.md
@server/audit/services/audit_service.py
@server/audit/services/taxonomy.py
@server/audit/services/redaction.py
@server/tests/audit/test_audit_taxonomy.py
@server/system/views.py
@server/repositories/views.py
@server/access_tokens/views.py
@server/feishu/views.py
@server/services/purge_reconcile.py
@server/tests/audit/test_audit_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: taxonomy purge 常量提升（RESERVED → ACTION_PURGE_* 纳入 ALL_ACTIONS）</name>
  <read_first>
    - server/audit/services/taxonomy.py（现状：purge.started/completed 在 RESERVED_ACTIONS，注释「Phase 54 接线」）
    - server/tests/audit/test_audit_taxonomy.py（现 test_reserved_disjoint_from_all 断言 purge.* ⊆ RESERVED——提升后须更新）
    - server/delivery/services/event_taxonomy.py（常量 + frozenset 范式参考）
  </read_first>
  <action>
    在 server/audit/services/taxonomy.py：
    - 新增具名常量：`ACTION_PURGE_STARTED: Final[str] = "purge.started"`、`ACTION_PURGE_COMPLETED: Final[str] = "purge.completed"`（放「凭证/数据治理类」段或新增「清理/治理类」段，中文注释说明 Phase 54 接线自 v0.5 purge 埋点收口）。
    - 把这两个常量加入 `ALL_ACTIONS` frozenset。
    - 从 `RESERVED_ACTIONS` 移除 purge.started/completed——使其变为空 frozenset（`RESERVED_ACTIONS: Final[frozenset[str]] = frozenset()`），并更新模块 docstring（purge 已于 Phase 54 接线提升，RESERVED 现为空预留位）。
    - 把 `ACTION_PURGE_STARTED` / `ACTION_PURGE_COMPLETED` 加入 `__all__`。
    更新 server/tests/audit/test_audit_taxonomy.py：
    - test_reserved_disjoint_from_all：保留 `RESERVED_ACTIONS.isdisjoint(ALL_ACTIONS)`（空集天然不相交），删除/改写 `{"purge.started","purge.completed"} <= RESERVED_ACTIONS` 断言。
    - 新增 test_purge_actions_promoted_into_all：断言 `{"purge.started", "purge.completed"} <= ALL_ACTIONS` 且 `RESERVED_ACTIONS == frozenset()`（或不含 purge.*）。
    - test_actions_naming_convention 自动覆盖新常量（purge.started/completed 符合 object.verb 正则）。
  </action>
  <acceptance_criteria>
    - server/audit/services/taxonomy.py contains `ACTION_PURGE_STARTED` 与 `ACTION_PURGE_COMPLETED`，且二者在 `ALL_ACTIONS` 内、不在 `RESERVED_ACTIONS` 内。
    - `__all__` 含两个新常量名。
    - `cd server && uv run pytest tests/audit/test_audit_taxonomy.py -q` 全部通过（命名规范 + purge 已提升 + reserved 不再含 purge）。
  </acceptance_criteria>
  <verify>
    <automated>cd server && uv run pytest tests/audit/test_audit_taxonomy.py -q</automated>
  </verify>
  <done>purge.started/completed 提升为具名 ACTION_PURGE_* 常量并纳入 ALL_ACTIONS；taxonomy 守护测试同步更新且全绿。</done>
  <commit>feat(54-02): taxonomy 提升 purge 埋点为具名 ACTION_PURGE_* 常量并纳入 ALL_ACTIONS</commit>
</task>

<task type="auto">
  <name>Task 2: system Provider 凭证 CRUD + toggle/set_default emit</name>
  <read_first>
    - server/system/views.py:346（perform_acreate，sync_to_async(serializer.save) 后，已有 provider_credential_created 日志）
    - server/system/views.py:427（perform_aupdate）、:437（perform_adestroy）、:460（toggle_active @action）、:487-491（_set_default_atomic，含 transaction.atomic()）
    - server/audit/services/taxonomy.py（ACTION_CREDENTIAL_CREATED/UPDATED/DELETED）
    - server/audit/services/redaction.py（确认 encrypted_config/api_key 键名命中脱敏——接线侧仍不传值）
  </read_first>
  <action>
    在 server/system/views.py import `AuditService` 与 `taxonomy`。**凭证字段绝不入载荷**——只传 provider_type/scope/scope_id/name/is_active 等非敏感标识。

    #11 创建（`perform_acreate` :346，serializer.save 成功后）：
      `await AuditService.aemit(action=taxonomy.ACTION_CREDENTIAL_CREATED, actor=request.user, target_type="provider_credential", target_id=serializer.instance.id, target_repr=f"{instance.provider_type}:{instance.name}", after={"provider_type": instance.provider_type, "scope": instance.scope, "scope_id": str(instance.scope_id) if ... else None, "name": instance.name}, source="api")`。
    #12 更新（`perform_aupdate` :427）：emit `ACTION_CREDENTIAL_UPDATED`，after 记**变更字段名集合**（不含密文，如 `{"changed": ["name", "is_active"]}`）。
    #13 删除（`perform_adestroy` :437，硬删；delete **前**快照非敏感字段 + id）：emit `ACTION_CREDENTIAL_DELETED`，before=删前 `{provider_type, scope, name}`。
    #14 软禁用 toggle（`toggle_active` :460，asave 成功后）：emit `ACTION_CREDENTIAL_UPDATED`，before/after `{"is_active": 旧/新}`，metadata `{"op": "toggle_active"}`。仅值变更时 emit。
    #15 设默认（`_set_default_atomic` :487-491 的 `transaction.atomic()` 块内，set 默认成功后）：用 `transaction.on_commit(lambda: AuditService.emit(action=taxonomy.ACTION_CREDENTIAL_UPDATED, actor=<下传或None>, target_type="provider_credential", target_id=instance.id, after={"is_default": True, "scope": ..., "provider_type": ...}, metadata={"op": "set_default"}, source="api"))`（sync emit + on_commit，因处于显式 atomic）。actor 若在 sync 块内不可得 `request.user`，由外层下传或记 None（read_first 确认上下文）。

    刻意不接线（SC-4 / 风险 1）：`refresh-models`（拉模型列表，普通运维非凭证内容变更）、list/detail GET 读路径不 emit。避免 perform_a* 与 @action 对同一变更 double-emit。
  </action>
  <acceptance_criteria>
    - server/system/views.py 的 Provider 凭证 emit 全部引用 `taxonomy.ACTION_CREDENTIAL_*`。
    - `_set_default_atomic` 的 emit 经 `transaction.on_commit`（grep `on_commit` 命中该函数附近）。
    - emit 参数 **不含** `encrypted_config` / `api_key` 字面量（grep 确认 emit 调用 after/before 无密文字段）。
    - `cd server && uv run python manage.py check` 退出码 0。
  </acceptance_criteria>
  <verify>
    <automated>cd server && uv run python manage.py check</automated>
  </verify>
  <done>Provider 凭证 CRUD/toggle/set_default 接线完成；set_default 用 on_commit；密文绝不入载荷；refresh-models 不 emit。</done>
  <commit>feat(54-02): system Provider 凭证增删改/启停/设默认接线审计 emit（密文不落审计）</commit>
</task>

<task type="auto">
  <name>Task 3: Git 实例凭证 + per-repo Git 凭证 + 排除规则增删 emit</name>
  <read_first>
    - server/repositories/views.py:1175（GitInstanceCredentialsView.post，GitInstanceCredential.objects.acreate）、:1254（_update credential.asave）、:1275（DetailView.delete credential.adelete）
    - server/repositories/views.py:1082（RepositoryExclusionRulesView.post，RepoExclusionRule.objects.acreate）、:1118（RepositoryExclusionRuleDetailView.delete rule.adelete）
    - server/projects/views.py:478（建仓时建凭证）、:505（RepositoryViewSet.credential DELETE）、:539/:543（SetAccessTokenView.post create/update）
    - server/audit/services/taxonomy.py（ACTION_CREDENTIAL_*、ACTION_EXCLUSION_RULE_CHANGED）
  </read_first>
  <action>
    在 repositories/views.py 与 projects/views.py（per-repo 凭证段）import `AuditService` 与 `taxonomy`。凭证侧只传 host/provider/has_token/git_user_name，**绝不**传 `encrypted_token`。

    #16 Git 实例凭证创建（:1175 acreate 后）：emit `ACTION_CREDENTIAL_CREATED`，target_type="git_instance_credential"，target_id=credential.id，target_repr=host，after={"host", "provider", "has_token": True}。
    #17 Git 实例凭证更新（:1254 asave 后）：emit `ACTION_CREDENTIAL_UPDATED`，after={"host", "provider", "token_changed": bool}。
    #18 Git 实例凭证删除（:1275 adelete **前**快照）：emit `ACTION_CREDENTIAL_DELETED`，before={"host", "provider"}。
    #19 per-repo Git 凭证（projects/views.py :478 建仓建凭证 / :539 create / :543 update / :505 DELETE）：emit `ACTION_CREDENTIAL_*`，target_type="git_credential"，target_id=repo.id，target_repr=repo repr，after={"has_token": True, "git_user_name": ...}；删除 before 快照非敏感字段。**绝不**传 encrypted_token。
    #24 排除规则新增（repositories/views.py :1082 acreate 后）：emit `ACTION_EXCLUSION_RULE_CHANGED`，target_type="repo_exclusion_rule"，target_id=rule.id，target_repr=f"{repo}:{pattern}"，after={"pattern", "rule_type", "enabled", "source"}，metadata={"op": "created"}。
    #25 排除规则删除（:1118 adelete **前**快照）：emit `ACTION_EXCLUSION_RULE_CHANGED`，before={"pattern", "rule_type"}，metadata={"op": "deleted"}。

    刻意不接线（SC-4）：Git 实例/排除规则的 list/detail GET 读路径不 emit；仓库状态轮询不 emit。
  </action>
  <acceptance_criteria>
    - repositories/views.py 与 projects/views.py（per-repo 段）凭证 emit 引用 `taxonomy.ACTION_CREDENTIAL_*`；排除规则 emit 引用 `taxonomy.ACTION_EXCLUSION_RULE_CHANGED`。
    - emit 参数 **不含** `encrypted_token` 字面量（grep 确认）。
    - Git 实例/排除规则删除均在 adelete 前快照 before。
    - `cd server && uv run python manage.py check` 退出码 0。
  </acceptance_criteria>
  <verify>
    <automated>cd server && uv run python manage.py check</automated>
  </verify>
  <done>Git 实例凭证 + per-repo Git 凭证 + 排除规则增删 emit 接线完成；token 密文不入载荷；删前快照。</done>
  <commit>feat(54-02): repositories/projects Git 凭证与排除规则增删接线审计 emit（token 不落审计）</commit>
</task>

<task type="auto">
  <name>Task 4: PAT 创建/吊销 + 飞书同步触发 emit</name>
  <read_first>
    - server/access_tokens/views.py:46-74（acreate，AccessToken.objects.acreate；token_prefix/token_suffix 可记，token_hash/明文不可）、:76-85（revoke，幂等：仅 revoked_at is None 写入）
    - server/feishu/views.py:1422（TriggerLogRetryView.post，手动重试，有 request.user）
    - server/feishu/views.py:540（FeishuWebhookView.post，AllowAny）、:691（_dispatch_to_workflows 派发成功点）
    - server/audit/services/taxonomy.py（ACTION_PAT_CREATED/REVOKED、ACTION_FEISHU_SYNC_TRIGGERED）
  </read_first>
  <action>
    在 access_tokens/views.py 与 feishu/views.py import `AuditService` 与 `taxonomy`。

    #20 PAT 创建（access_tokens/views.py :60 acreate 后，return 前）：
      `await AuditService.aemit(action=taxonomy.ACTION_PAT_CREATED, actor=request.user, target_type="pat", target_id=token.id, target_repr=token.name, after={"name": token.name, "token_prefix": token.token_prefix, "token_suffix": token.token_suffix, "expires_at": token.expires_at.isoformat() if token.expires_at else None}, source="api")`。**绝不**记明文/token_hash。
    #21 PAT 吊销（:82-84，**仅** `if token.revoked_at is None:` 首吊分支内，asave 成功后 emit；重复 revoke 不 emit）：
      `await AuditService.aemit(action=taxonomy.ACTION_PAT_REVOKED, actor=request.user, target_type="pat", target_id=token.id, target_repr=token.name, after={"revoked_at": token.revoked_at.isoformat()}, source="api")`。
    #22 飞书手动重试（feishu/views.py :1422，重新派发成功后）：
      `await AuditService.aemit(action=taxonomy.ACTION_FEISHU_SYNC_TRIGGERED, actor=request.user, target_type="trigger_log", target_id=original_log_id, target_repr=event_type, metadata={"event_type": event_type, "source": "api"}, source="api")`。
    #23 飞书 webhook 自动同步（决策 A：纳入，actor=None）：**仅在 `_dispatch_to_workflows`（:691）真正派发工作流成功点** emit 一次：
      `await AuditService.aemit(action=taxonomy.ACTION_FEISHU_SYNC_TRIGGERED, actor=None, target_type="trigger_log", target_id=log.id, target_repr=event_type, metadata={"event_type": event_type}, source="feishu_webhook")`。
      **不对** url_verification / duplicate / ignored / 校验失败事件 emit（避免噪音，风险 7）。
  </action>
  <acceptance_criteria>
    - access_tokens/views.py 的吊销 emit **在** `if token.revoked_at is None:` 块内（幂等，grep 确认 emit 缩进位于该分支）。
    - PAT emit 参数 **不含** 明文 / `token_hash` 字面量（grep 确认 after 仅 token_prefix/token_suffix）。
    - feishu webhook emit 的 `actor=None` 且 `source="feishu_webhook"`；手动重试 `actor=request.user` 且 `source="api"`。
    - emit 全部引用 `taxonomy.ACTION_PAT_*` / `taxonomy.ACTION_FEISHU_SYNC_TRIGGERED`。
    - `cd server && uv run python manage.py check` 退出码 0。
  </acceptance_criteria>
  <verify>
    <automated>cd server && uv run python manage.py check</automated>
  </verify>
  <done>PAT 增吊（吊销幂等）+ 飞书手动/webhook 同步 emit 接线完成；明文/hash 不落审计；webhook actor=None 仅派发成功 emit。</done>
  <commit>feat(54-02): access_tokens PAT 增吊与 feishu 同步触发接线审计 emit（吊销幂等/webhook actor=None）</commit>
</task>

<task type="auto">
  <name>Task 5: purge 埋点收口（log_purge_event 追加 AuditService.emit）</name>
  <read_first>
    - server/services/purge_reconcile.py:349-365（log_purge_event，sync 函数，保留 logger.info）
    - server/services/purge_reconcile.py:239/253/304（三处调用点，零改动）
    - server/audit/services/taxonomy.py（ACTION_PURGE_STARTED/COMPLETED，Task 1 产物）
    - .planning/phases/54-emit/54-RESEARCH.md（§1 #26 收口注意：sync emit，不用 aemit）
  </read_first>
  <action>
    在 server/services/purge_reconcile.py import `AuditService` 与 `taxonomy`（顶部，避免循环导入则函数内懒导入——read_first 确认 audit 为叶子包无环）。
    在 `log_purge_event`（:349）函数体内，**保留** 既有 `logger.info(event, ...)`（下游可能依赖 structlog 事件名），**追加**：
      ```
      action = taxonomy.ACTION_PURGE_STARTED if event == "purge.started" else taxonomy.ACTION_PURGE_COMPLETED
      AuditService.emit(
          action=action,
          actor=None,
          target_type="repository",
          target_id=repository_id,
          target_repr=str(repository_id),
          metadata={"mode": mode, "match_count": match_count, "failures": failures or []},
          source="scheduler",
      )
      ```
    用 sync `emit`（**不用 aemit**）——`log_purge_event` 是 sync 函数，从 async `run_cleanup` 调用时各步 ORM 已 await 完成（autocommit），emit 内部 atomic 自建 savepoint/独立事务，fail-soft 不阻断清理。三处调用点（:239/:253/:304）零改动自动收口。
    若 event 既非 purge.started 也非 purge.completed（防御），fallback 直接传 event 字符串（保持向后兼容，但本 phase 调用点只这两值）。
  </action>
  <acceptance_criteria>
    - server/services/purge_reconcile.py 的 `log_purge_event` 同时保留 `logger.info(event, ...)` 与新增 `AuditService.emit(...)`。
    - emit 用 sync 面（**非** `aemit`），actor=None，source="scheduler"，target_type="repository"，metadata 含 mode/match_count/failures。
    - 三处调用点（:239/:253/:304）未改动（grep `log_purge_event(` 仍 3 处调用，签名不变）。
    - `cd server && uv run python manage.py check` 退出码 0，且既有 `cd server && uv run pytest tests/ -k purge_reconcile -q` 仍全绿（收口不破坏既有流程）。
  </acceptance_criteria>
  <verify>
    <automated>cd server && uv run python manage.py check && uv run pytest tests/ -k purge_reconcile -q</automated>
  </verify>
  <done>purge 收口完成：log_purge_event 保留结构化日志 + 追加 sync emit（actor=None/scheduler），既有 purge 测试不破。</done>
  <commit>feat(54-02): purge_reconcile 清理埋点收口统一 AuditEvent（保留日志 + 追加 emit）</commit>
</task>

<task type="auto">
  <name>Task 6: SC-2 凭证行落库+脱敏 / SC-3 purge 收口 / SC-4 凭证侧无噪音测试</name>
  <read_first>
    - server/tests/audit/test_audit_service.py / test_audit_redaction.py（落库 + 脱敏断言风格）
    - server/tests/audit/test_audit_inv6_guard.py（确认新接线不触发旁路写守护）
    - 既有 server/tests/ 下 system/repositories/access_tokens/feishu 视图测试范式（async view 调用 + request.user 注入）
    - 既有 purge_reconcile 测试（structlog/caplog 捕获范式）
  </read_first>
  <action>
    新建 server/tests/audit/test_emit_credentials.py（@pytest.mark.django_db，async 用 pytest-asyncio）：
    - test_provider_credential_create_emits / update / delete / toggle / set_default：各断言对应 `credential.*` action + actor + target_type="provider_credential"；删除断言 before 含删前非敏感快照。
    - test_credential_payload_redacted_in_db（**SC-2 核心**）：创建含 api_key/encrypted_config 的 Provider 凭证后 `AuditEvent.objects.aget(...)`，断言 `json.dumps(event.before)+json.dumps(event.after)+json.dumps(event.metadata)` **不含** 明文 api_key / access_token / app_secret / encrypted_config 密文；断言只含 provider_type/scope/name 等非敏感标识。
    - test_git_instance_credential_emits / test_per_repo_credential_emits：断言 `credential.*`，载荷不含 encrypted_token（断言 has_token 布尔在、token 值不在）。
    - test_exclusion_rule_create_emits / delete_emits：断言 `exclusion_rule.changed` + pattern/rule_type 在 after/before。
    - test_pat_create_emits：断言 `pat.created`，after 含 token_prefix/token_suffix，**不含** 明文/token_hash。
    - test_pat_revoke_idempotent（**SC-2**）：revoke 同一 token 两次，断言只产 1 条 `pat.revoked`（`AuditEvent.objects.filter(action="pat.revoked", target_id=str(token.id)).count() == 1`）。
    - test_feishu_manual_retry_emits：断言 `feishu_sync.triggered`，actor_id==request.user.id，source=="api"。
    - test_feishu_webhook_emits_actor_none（决策 A）：构造派发成功 webhook payload，断言 emit 时 actor_id is None、source=="feishu_webhook"；构造 url_verification/duplicate 事件断言**无** AuditEvent。

    新建 server/tests/audit/test_emit_purge_consolidation.py（**SC-3**）：
    - test_log_purge_event_emits_audit：调 `log_purge_event("purge.started", mode="normal", repository_id=<id>, match_count=3)` 后断言产 1 条 `purge.started` AuditEvent，target_type=="repository"，metadata 含 mode/match_count；调 "purge.completed" 同理。
    - test_log_purge_event_preserves_logger：用 structlog capture / caplog 断言 `logger.info` 仍发出原事件名（purge.started/completed 结构化日志未丢）。
    - test_run_cleanup_emits_started_and_completed：mock purge_file，调 `run_cleanup(repo_id)` 断言产 2 条 AuditEvent（started + completed），actor_id is None、source=="scheduler"。

    新建 server/tests/audit/test_emit_no_noise_credentials.py（**SC-4**）：
    - test_provider_credential_list_no_audit / test_refresh_models_no_audit / test_git_instance_list_no_audit / test_exclusion_rule_list_no_audit / test_pat_list_no_audit：调对应读/运维路径后断言该操作未新增 AuditEvent（调用前后 count 差为 0）。
  </action>
  <acceptance_criteria>
    - 三个测试文件存在且含上述用例。
    - `cd server && uv run pytest tests/audit/test_emit_credentials.py tests/audit/test_emit_purge_consolidation.py tests/audit/test_emit_no_noise_credentials.py -q` 全部通过。
    - `cd server && uv run pytest tests/audit/test_audit_inv6_guard.py -q` 仍全绿（新接线无旁路写）。
  </acceptance_criteria>
  <verify>
    <automated>cd server && uv run pytest tests/audit/test_emit_credentials.py tests/audit/test_emit_purge_consolidation.py tests/audit/test_emit_no_noise_credentials.py tests/audit/test_audit_inv6_guard.py -q</automated>
  </verify>
  <done>SC-2 凭证行落库 + 脱敏 DB 断言 + PAT 幂等 + webhook actor=None 全绿；SC-3 purge 收口（保留日志 + 2 条 AuditEvent）全绿；SC-4 凭证侧读无噪音全绿；INV-6 未破。</done>
  <commit>test(54-02): 补凭证 emit/脱敏、purge 收口、SC-4 无噪音单测</commit>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 02)

- **修改文件（taxonomy 提升）**：`server/audit/services/taxonomy.py`（新增 `ACTION_PURGE_STARTED`/`ACTION_PURGE_COMPLETED`，纳入 `ALL_ACTIONS`，`RESERVED_ACTIONS` 清空）、`server/tests/audit/test_audit_taxonomy.py`（同步守护）
- **修改文件（emit 接线）**：
  - `server/system/views.py` — Provider 凭证 CRUD/toggle/set_default(#11–#15，set_default on_commit)
  - `server/repositories/views.py` — Git 实例凭证(#16–#18) + 排除规则增删(#24/#25)
  - `server/projects/views.py` — per-repo Git 凭证(#19)
  - `server/access_tokens/views.py` — PAT 创建/吊销(#20/#21，吊销幂等)
  - `server/feishu/views.py` — 飞书手动重试(#22) + webhook 自动同步(#23, actor=None)
  - `server/services/purge_reconcile.py` — `log_purge_event` 追加 sync emit(#26)
- **新文件（测试）**：`server/tests/audit/test_emit_credentials.py`、`test_emit_purge_consolidation.py`、`test_emit_no_noise_credentials.py`
- **新常量**：`ACTION_PURGE_STARTED` / `ACTION_PURGE_COMPLETED`（taxonomy.py）
- **复用既有符号**：`AuditService.emit/aemit`、taxonomy `ACTION_CREDENTIAL_*`、`ACTION_PAT_*`、`ACTION_FEISHU_SYNC_TRIGGERED`、`ACTION_EXCLUSION_RULE_CHANGED`
- **决策落地**：决策点 A（#23 webhook 自动同步）→ 纳入，actor=None + source=feishu_webhook，仅派发成功点 emit
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 凭证密文/明文绝不落审计 | provider/git/feishu/PAT 凭证 emit 仅传非敏感标识（type/host/name/has_token/token_prefix）；encrypted_*/api_key/token_hash/明文绝不入载荷；入口 `_redact_audit_payload` 为第二道防线 |
| 审计只记已提交事实 | autocommit 路径主操作 await 成功后 emit；set_default atomic 块用 on_commit；purge emit 在各步 ORM await 完成后 |
| 无 actor 操作仍可追溯 | webhook 自动同步 / purge 调度 actor=None，但 source（feishu_webhook/scheduler）+ target + metadata 足以追溯 |
| 审计写入唯一收口 | 所有新接线仅经 AuditService；INV-6 grep 守护断言无旁路写 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-02-01 | Information Disclosure | 凭证字段 | mitigate | 接线侧只传非敏感标识 + 入口强制脱敏；DB 无明文测试断言（SC-2 核心） |
| T-54-02-02 | Repudiation | 凭证治理/同步/清理操作 | mitigate | provider/git/feishu/PAT/排除规则/purge 全量 emit（actor 或 source + target + 前后值） |
| T-54-02-03 | DoS / 噪音 | 审计可用性 | mitigate | PAT 吊销幂等仅首吊 emit；webhook 仅派发成功 emit（不对 url_verification/duplicate）；refresh-models/读路径不 emit（SC-4） |
| T-54-02-04 | Tampering | set_default atomic / purge fail-soft | mitigate | set_default 用 on_commit；purge emit fail-soft（入口 savepoint）不阻断清理流程，保留既有结构化日志 |
</threat_model>

<verification>
- `cd server && uv run ruff format --check audit system repositories projects access_tokens feishu services tests/audit && uv run ruff check audit system repositories projects access_tokens feishu services tests/audit`
- `cd server && uv run python manage.py check`
- `cd server && uv run python manage.py makemigrations --check --dry-run`（应无新 migration——本 plan 仅 view/service/taxonomy/test，无模型变更）
- `cd server && uv run pytest tests/audit/ -q`（含 Phase 53 既有套件 + 54 新增，全绿）
- `cd server && uv run pytest tests/ -k purge_reconcile -q`（既有 purge 测试不破）
</verification>

<success_criteria>
- Provider/Git 实例/per-repo/飞书凭证增删改、PAT 增吊、飞书同步触发均经 `AuditService.aemit` 产出 AuditEvent——SC-2 覆盖。
- 凭证字段脱敏：DB 无 api_key/access_token/app_secret/encrypted_config/PAT 明文/token_hash；只含非敏感标识——SC-2 核心断言。
- PAT 吊销幂等（仅首吊 emit）；webhook 自动同步 actor=None 仅派发成功 emit；refresh-models/读路径不 emit——SC-4 凭证侧覆盖。
- purge 埋点收口：`log_purge_event` 保留结构化日志 + 追加 sync emit（actor=None/scheduler），run_cleanup 产 started+completed 2 条 AuditEvent，既有 purge 流程/测试不破——SC-3 覆盖。
- taxonomy purge 常量提升（ACTION_PURGE_* 纳入 ALL_ACTIONS），消除字符串字面量。
- INV-6 grep 守护保持全绿（新接线无旁路写）。
- 映射：AUDITCOV-02（凭证治理 + purge 收口全量 emit）；SC-2（凭证行落库 + 脱敏 + PAT 幂等 + 同步 actor 分支）、SC-3（purge 收口统一 AuditEvent）、SC-4（凭证侧无噪音）。
</success_criteria>

<output>
Create `.planning/phases/54-emit/54-02-SUMMARY.md` when done
</output>
