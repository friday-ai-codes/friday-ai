---
phase: 53-auditevent-emit
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - server/audit/services/__init__.py
  - server/audit/services/taxonomy.py
  - server/audit/services/redaction.py
  - server/audit/services/audit_service.py
  - server/tests/audit/test_audit_taxonomy.py
  - server/tests/audit/test_audit_redaction.py
  - server/tests/audit/test_audit_service.py
  - server/tests/audit/test_audit_failsoft.py
  - server/tests/audit/test_audit_inv6_guard.py
autonomous: true
requirements: [AUDIT-01, AUDIT-02]
must_haves:
  truths:
    - "AuditService 是 AuditEvent 唯一写入入口（INV-6）：emit(sync) + aemit(async via sync_to_async)，所有落库经 AuditEvent.objects.create 收口于此（per RESEARCH §3.1）"
    - "脱敏在 emit 入口内强制执行——before/after/metadata 经 _redact_audit_payload 兜底，调用方传明文也绝不落明文（per CONTEXT 脱敏统一构造入口，AUDIT-02）"
    - "fail-soft：emit 整段 try/except 吞异常 + logger.warning('audit.emit_failed')，绝不冒泡/阻断主操作（per RESEARCH §3.1 / §5，AUDIT-02）"
    - "INV-6 grep 守护：除 audit/services/audit_service.py 外，server 源码无旁路 AuditEvent.objects.<write> / 实例化 / .save()（镜像 test_sdd_spec_inv6_guard.py，AUDIT-01）"
    - "action taxonomy：模块级 Final[str] 稳定常量 + ALL_ACTIONS frozenset 容器（object.verb 命名），本 phase 仅定义稳定容器 + 种子/预留常量，具体值由 Phase 54 补充（per RESEARCH §4，AUDIT-02）"
    - "脱敏覆盖 key-name 命中（token/secret/password/api_key/access_token/private_key/credential/encrypted_config/token_hash 等）+ 值级高熵/密钥模式兜底，递归 dict/list 只抹命中叶子（per RESEARCH §5，AUDIT-02）"
    - "sync emit + async aemit 双面均落一行且字段一致；actor 字段访问全在 sync_to_async(emit) 同步块内（async 安全，per RESEARCH §3.1/§6）"
  artifacts:
    - path: "server/audit/services/audit_service.py"
      provides: "AuditService 单一写入入口（emit/aemit）+ actor 解析 + 强制脱敏 + fail-soft"
      contains: "class AuditService"
    - path: "server/audit/services/redaction.py"
      provides: "_redact_audit_payload 递归脱敏（key-name + 值级密钥/高熵）+ REDACTION_PLACEHOLDER"
      contains: "def _redact_audit_payload"
    - path: "server/audit/services/taxonomy.py"
      provides: "action taxonomy Final[str] 常量 + ALL_ACTIONS / RESERVED_ACTIONS frozenset"
      contains: "ALL_ACTIONS"
    - path: "server/tests/audit/test_audit_inv6_guard.py"
      provides: "INV-6 grep 守护（无旁路写）+ writer-actually-writes 反向断言"
      contains: "AuditEvent.objects"
    - path: "server/tests/audit/test_audit_failsoft.py"
      provides: "emit 失败吞异常不冒泡 + warning 日志 + 主操作不受影响测试"
      contains: "audit.emit_failed"
    - path: "server/tests/audit/test_audit_redaction.py"
      provides: "key-name/值级/嵌套脱敏 + DB 无明文 + 入口强制脱敏测试"
      contains: "_redact_audit_payload"
  key_links:
    - from: "server/audit/services/audit_service.py"
      to: "audit.models.AuditEvent"
      via: "emit 经 AuditEvent.objects.create 落库（唯一 writer）"
      pattern: "AuditEvent\\.objects\\.create"
    - from: "server/audit/services/audit_service.py"
      to: "audit.services.redaction._redact_audit_payload"
      via: "emit 对 before/after/metadata 强制调用脱敏"
      pattern: "_redact_audit_payload"
    - from: "server/audit/services/audit_service.py"
      to: "fail-soft 不阻断主操作"
      via: "create 包裹 try/except 吞异常 + logger.warning"
      pattern: "audit\\.emit_failed"
---

<objective>
落地审计 emit 地基的「单一写入入口 + 强制脱敏 + fail-soft + 稳定 taxonomy」（AUDIT-01 / AUDIT-02）：实现 `AuditService.emit`/`aemit`（AuditEvent 唯一写入点，INV-6），入口内强制对 before/after/metadata 脱敏（`redaction.py`），整段 fail-soft 吞异常不阻断主操作，并定义 `action` taxonomy 稳定常量容器（`taxonomy.py`）。配套 INV-6 grep 守护 + 脱敏 + fail-soft + sync/async 双面测试。

Purpose: 给 Phase 54 任意敏感操作一个「可被任意调用方安全调用、绝不落明文、绝不阻断主流程、写入唯一收口」的 emit 地基。
Output: `audit/services/` 三模块（taxonomy / redaction / audit_service）+ INV-6 grep 守护测试 + 脱敏/fail-soft/双面 service 测试，全绿。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.codex/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/53-auditevent-emit/53-CONTEXT.md
@.planning/phases/53-auditevent-emit/53-RESEARCH.md
@.planning/phases/53-auditevent-emit/53-01-PLAN.md
@server/audit/models/audit_event.py
@server/delivery/services/sdd_spec_service.py
@server/delivery/services/event_taxonomy.py
@server/tests/delivery/test_sdd_spec_inv6_guard.py
@server/services/sensitive_detect.py
@server/delivery/services/work_item_service.py
@server/services/sensitive_purge.py
@server/common/logging.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: action taxonomy 稳定常量容器（taxonomy.py）</name>
  <read_first>
    - server/delivery/services/event_taxonomy.py（Final[str] 常量 + frozenset ALL_EVENTS/RESERVED_EVENTS + 守护基准范式）
    - .planning/phases/53-auditevent-emit/53-RESEARCH.md（§4 taxonomy：object.verb 命名 + 种子/预留常量）
  </read_first>
  <action>
    新建 server/audit/services/__init__.py（模块 docstring：audit service 层；curated 不强制 re-export 全部）。
    新建 server/audit/services/taxonomy.py（中文 docstring，镜像 event_taxonomy.py）：
    - 模块级 Final[str] 种子常量，object.verb 命名（对齐既有 purge.started 风格），覆盖 Phase 54 已知维度的预留常量，至少含：
      身份/权限类：ACTION_MEMBER_CREATED="member.created"、ACTION_MEMBER_UPDATED="member.updated"、ACTION_MEMBER_DELETED="member.deleted"、ACTION_USER_ACTIVATED="user.activated"、ACTION_USER_DEACTIVATED="user.deactivated"、ACTION_ROLE_CHANGED="role.changed"、ACTION_PROJECT_CONFIG_CHANGED="project.config_changed"、ACTION_REPOSITORY_PERMISSION_CHANGED="repository.permission_changed"；
      凭证/数据治理类：ACTION_CREDENTIAL_CREATED="credential.created"、ACTION_CREDENTIAL_UPDATED="credential.updated"、ACTION_CREDENTIAL_DELETED="credential.deleted"、ACTION_PAT_CREATED="pat.created"、ACTION_PAT_REVOKED="pat.revoked"、ACTION_FEISHU_SYNC_TRIGGERED="feishu_sync.triggered"、ACTION_EXCLUSION_RULE_CHANGED="exclusion_rule.changed"。
    - RESERVED_ACTIONS: Final[frozenset[str]]：v0.5 既有埋点收口预留（Phase 54 接线，非本 phase）——"purge.started"、"purge.completed"（对齐 event_taxonomy RESERVED 范式，预留常量但不接线）。
    - ALL_ACTIONS: Final[frozenset[str]]：上述种子常量全集（守护测试基准）。
    - __all__ 导出所有常量名 + ALL_ACTIONS + RESERVED_ACTIONS。
    说明：本 phase 仅定义稳定容器 + 种子/预留常量，具体 action 值由 Phase 54 各埋点补充消费（CONTEXT decisions）。
    新建 server/tests/audit/test_audit_taxonomy.py：
    - test_actions_naming_convention：遍历 ALL_ACTIONS，断言每个值全小写、含且仅含一个或多个 "." 分隔的 object.verb 形（正则 ^[a-z0-9_]+(\\.[a-z0-9_]+)+$）。
    - test_all_actions_contains_seeds：断言 ALL_ACTIONS ⊇ {member.created, credential.updated, pat.revoked} 等代表常量值。
    - test_reserved_disjoint_from_all：断言 RESERVED_ACTIONS 与 ALL_ACTIONS 不相交（purge.* 预留不计入本 phase 词表，对齐 event_taxonomy）。
  </action>
  <acceptance_criteria>
    - server/audit/services/taxonomy.py contains `ALL_ACTIONS` 与 `RESERVED_ACTIONS`（均为 frozenset）。
    - taxonomy.py contains `member.created`、`credential.updated`、`pat.revoked` 等种子常量值；RESERVED_ACTIONS contains `purge.started` 与 `purge.completed`。
    - `cd server && uv run pytest tests/audit/test_audit_taxonomy.py -q` 全部通过（命名规范 + 种子覆盖 + reserved 不相交）。
  </acceptance_criteria>
  <verify>
    <automated>cd server && uv run pytest tests/audit/test_audit_taxonomy.py -q</automated>
  </verify>
  <done>taxonomy.py 稳定常量容器齐备（种子 + reserved + ALL_ACTIONS），命名规范与不相交守护通过。</done>
</task>

<task type="auto">
  <name>Task 2: 脱敏入口 _redact_audit_payload（redaction.py）</name>
  <read_first>
    - server/services/sensitive_detect.py（_SECRET_PATTERNS 私钥/AKIA/gh[pousr]_/xox/通用赋值正则 + _HIGH_ENTROPY_TOKEN_RE + _shannon_entropy ≥4.0 范式）
    - server/delivery/services/work_item_service.py（_SECRET_KV_RE 敏感键名集 + _redact_secrets 范式）
    - server/services/sensitive_purge.py（_redact_value 递归只抹命中叶子、保留同载荷其余字段范式）
    - .planning/phases/53-auditevent-emit/53-RESEARCH.md（§5 脱敏：key-name + 值级 + 递归 + 绝不回填明文 + INV-3 不 import）
  </read_first>
  <action>
    新建 server/audit/services/redaction.py（中文 docstring；语义对齐既有范式但 INV-3 不 import sensitive_detect/work_item_service，复刻常量到本模块）：
    - REDACTION_PLACEHOLDER: Final[str] = "[已脱敏]"。
    - 敏感键名集（小写匹配，子串/全词命中）：_SENSITIVE_KEY_TOKENS 含 token、secret、password、passwd、api_key、apikey、access_token、refresh_token、access_key、secret_key、private_key、credential、authorization、encrypted_config、token_hash、app_secret。判定 helper _is_sensitive_key(key: str) -> bool：key.lower() 命中任一 token（用归一化去分隔符比较，命中 api_key/apikey 等变体）。
    - 值级密钥/高熵兜底（复刻，不 import）：_SECRET_VALUE_PATTERNS 含私钥块 (-----BEGIN ... PRIVATE KEY-----)、AKIA[0-9A-Z]{16}、\\bgh[pousr]_[A-Za-z0-9]{20,}\\b、\\bxox[baprs]-[A-Za-z0-9-]{10,}\\b、通用赋值 (?i)\\b(api[_-]?key|secret|password|token)\\b\\s*[:=]\\s*\\S{8,}；_HIGH_ENTROPY_TOKEN_RE = [A-Za-z0-9+/=_\\-]{40,} + _shannon_entropy(s) ≥ 4.0 阈值（复刻 sensitive_detect 的 Shannon 实现）。
    - _redact_str(value: str) -> tuple[str, bool]：str 叶子若命中任一 _SECRET_VALUE_PATTERNS 或（长度≥40 高熵 token 且 Shannon≥4.0）→ 整串替换 REDACTION_PLACEHOLDER，返回 (新值, True)；否则原样返回 (value, False)。
    - _redact_audit_payload(payload: Any) -> Any：递归遍历——
      dict：逐 (k, v)；若 _is_sensitive_key(k) → 值整体替换 REDACTION_PLACEHOLDER（无论类型）；否则对 v 递归 _redact_audit_payload；
      list：逐元素递归；
      str 叶子：走 _redact_str 取新值；
      其余标量（int/bool/None/float）：原样返回。
      入参非 dict/list 时也安全（兜底返回处理后的标量）。保留结构与非敏感字段（参考 sensitive_purge._redact_value 只抹命中叶子）。
    - __all__ = ["REDACTION_PLACEHOLDER", "_redact_audit_payload"]（_redact_str/_is_sensitive_key 模块私有）。
    新建 server/tests/audit/test_audit_redaction.py：
    - test_key_name_hit：_redact_audit_payload({"access_token": "abc", "name": "x"}) → access_token=="[已脱敏]"、name=="x"（覆盖 token/secret/password/api_key/access_token/private_key/credential/encrypted_config/token_hash 各一断言）。
    - test_value_level_secret：{"note": "ghp_AAAABBBBCCCCDDDDEEEE1234"} 与私钥块字符串 → 值被替换 "[已脱敏]"（键名未命中但值命中）。
    - test_high_entropy_value：含 ≥40 字符高熵 base64 串的值被替换。
    - test_nested_structure：dict/list 嵌套含敏感叶子 → 只命中叶子被抹、同载荷非敏感字段（如 {"user":"alice","creds":{"password":"p"}}）的 user 保留。
    - test_non_string_scalars_preserved：int/bool/None 非敏感键的值原样保留。
  </action>
  <acceptance_criteria>
    - server/audit/services/redaction.py contains `def _redact_audit_payload` 与 `REDACTION_PLACEHOLDER = "[已脱敏]"`。
    - redaction.py 不 import sensitive_detect / work_item_service（grep `import sensitive_detect` 与 `work_item_service` 在该文件无命中，守 INV-3）。
    - `cd server && uv run pytest tests/audit/test_audit_redaction.py -q` 全部通过（key-name + 值级密钥 + 高熵 + 嵌套只抹叶子 + 标量保留）。
  </acceptance_criteria>
  <verify>
    <automated>cd server && uv run pytest tests/audit/test_audit_redaction.py -q</automated>
  </verify>
  <done>_redact_audit_payload 覆盖键名/值级/高熵/递归脱敏，绝不回填明文，INV-3 无跨层 import；测试全绿。</done>
</task>

<task type="auto">
  <name>Task 3: AuditService 单一写入入口 emit/aemit + fail-soft + 强制脱敏</name>
  <read_first>
    - server/delivery/services/sdd_spec_service.py（单一写入入口 + sync_to_async 桥接 ORM + async 安全标量访问范式）
    - server/audit/models/audit_event.py（AuditEvent.objects.create 字段契约，Plan 01 产物）
    - server/audit/services/redaction.py（_redact_audit_payload，Task 2 产物）
    - server/common/logging.py（structlog 结构化日志 + 凭证泄漏保护，warning 走此通道）
    - .planning/phases/53-auditevent-emit/53-RESEARCH.md（§3.1 emit/aemit 设计 / §6 async 约束）
  </read_first>
  <action>
    新建 server/audit/services/audit_service.py（中文 docstring，structlog logger）：
    - import：from asgiref.sync import sync_to_async；from django.utils import timezone；from audit.models import AuditEvent；from audit.services.redaction import _redact_audit_payload；structlog。
    - actor 解析 helper（模块私有，async 安全——仅在 sync 块内调用）：
      _actor_id(actor) -> UUID|None：actor 为 None → None；否则取 getattr(actor, "id", None)。
      _actor_repr(actor) -> str：actor 为 None → ""；否则构造形如 f"{actor.username}" + 若 is_superuser 追加 " (superuser)"（容错 getattr，缺字段返回 str(actor)）。
    - class AuditService：
      @staticmethod def emit(*, action, actor=None, target_type="", target_id="", target_repr="", before=None, after=None, source="", occurred_at=None, metadata=None) -> None：
        整段 try/except 包裹 AuditEvent.objects.create(actor_id=_actor_id(actor), actor_repr=_actor_repr(actor), action=action, target_type=target_type, target_id=str(target_id) if target_id != "" else "", target_repr=target_repr, before=_redact_audit_payload(before or {}), after=_redact_audit_payload(after or {}), source=source, occurred_at=occurred_at or timezone.now(), metadata=_redact_audit_payload(metadata or {}))；
        except Exception: logger.warning("audit.emit_failed", action=action, target_type=target_type)（# noqa: BLE001，fail-soft 绝不冒泡，不记敏感载荷）。docstring 注明：脱敏在入口内强制（调用方无法绕过）；建议调用方在主操作成功后调用（如 transaction.on_commit），事务边界由 Phase 54 各调用方按场景处理。
      @staticmethod async def aemit(**kwargs) -> None：await sync_to_async(AuditService.emit)(**kwargs)（actor 字段访问全在 sync_to_async(emit) 同步块内，async 安全）。
    新建 server/tests/audit/test_audit_service.py（@pytest.mark.django_db）：
    - test_emit_persists_all_fields：emit(action="member.created", actor=<user>, target_type="user", target_id=<uuid>, target_repr="用户 alice", before={"a":1}, after={"a":2}, source="web", metadata={"ip":"127.0.0.1"}) → AuditEvent.objects.get(...) 断言全字段落库正确（actor_id==user.id、actor_repr 含 username、action、target_*、before/after/source/occurred_at/recorded_at/metadata）。
    - test_emit_actor_none：emit(action="purge.started", actor=None) → actor_id is None、actor_repr=="".
    - test_aemit_persists（async, pytest-asyncio）：await AuditService.aemit(action="credential.updated", source="api") → 落一行且字段一致。
    - test_emit_default_occurred_at：不传 occurred_at → occurred_at 自动填 now（非 None）。
    新建 server/tests/audit/test_audit_failsoft.py（@pytest.mark.django_db）：
    - test_emit_swallows_exception：monkeypatch AuditEvent.objects.create 抛 RuntimeError → AuditService.emit(...) 返回 None 且不冒泡（无异常逃逸）；用 caplog/structlog capture 断言记 "audit.emit_failed" warning。
    - test_main_op_not_blocked：模拟「主操作返回值 + emit 失败」——一个函数先算结果再 emit（create 被 patch 抛错），断言函数仍返回正确结果（emit 失败不影响主操作）。
    - test_aemit_failsoft（async）：patch create 抛错 → await aemit(...) 不冒泡。
    - test_redaction_enforced_at_entry：emit(action="credential.updated", before={"access_token":"PLAINTEXT_SECRET_VALUE"}) → 落库后 AuditEvent.before["access_token"]=="[已脱敏]"；序列化整行 JSON 断言 "PLAINTEXT_SECRET_VALUE" not in json.dumps(...)（DB 无明文终极防线 + 入口强制脱敏，证明禁绕过）。
  </action>
  <acceptance_criteria>
    - server/audit/services/audit_service.py contains `class AuditService`、`def emit(`、`async def aemit(`、`AuditEvent.objects.create`、`_redact_audit_payload`、`audit.emit_failed`。
    - emit 体内 create 被 try/except 包裹且 except 分支不 re-raise（grep 确认 except 块内无 `raise`）。
    - `cd server && uv run pytest tests/audit/test_audit_service.py tests/audit/test_audit_failsoft.py -q` 全部通过（字段持久化 + sync/async 双面 + fail-soft 不冒泡 + warning + 主操作不受影响 + 入口强制脱敏 + DB 无明文）。
  </acceptance_criteria>
  <verify>
    <automated>cd server && uv run pytest tests/audit/test_audit_service.py tests/audit/test_audit_failsoft.py -q</automated>
  </verify>
  <done>AuditService.emit/aemit 双面落库一致、入口强制脱敏、fail-soft 吞异常不阻断主操作，测试全绿。</done>
</task>

<task type="auto">
  <name>Task 4: INV-6 grep 守护测试（无旁路写表）</name>
  <read_first>
    - server/tests/delivery/test_sdd_spec_inv6_guard.py（逐行镜像源：扫描 + 正则锚定 + 负向前瞻 + writer-actually-writes + _is_scanned 排除）
    - server/audit/services/audit_service.py（唯一允许 writer，Task 3 产物）
    - .planning/phases/53-auditevent-emit/53-RESEARCH.md（§3.2 INV-6 grep 守护规格）
  </read_first>
  <action>
    新建 server/tests/audit/test_audit_inv6_guard.py（中文 docstring，逐行镜像 test_sdd_spec_inv6_guard.py，纯本地源码扫描无 DB/网络）：
    - SERVER_DIR = Path(__file__).resolve().parents[2]；_PRUNE_DIRS 同源（.venv/node_modules/staticfiles/__pycache__/.git/htmlcov/.pytest_cache/.ruff_cache/.mypy_cache）。
    - _ALLOWED_WRITER = "audit/services/audit_service.py"（唯一允许写 AuditEvent 的模块）。
    - 正则：
      _RE_ORM_WRITE = \\bAuditEvent\\.objects\\.(?:create|bulk_create|get_or_create|update_or_create|update)\\b；
      _RE_INSTANTIATE = \\bAuditEvent(?!Immutable)\\s*\\(（负向前瞻排除 AuditEventImmutableError(）；
      _RE_INSTANCE_SAVE = \\bAuditEvent(?!Immutable)\\([^)]*\\)\\.save\\(。
    - _is_scanned(rel)：排除 _ALLOWED_WRITER 自身、tests/ 与 /tests/、/migrations/、audit/models/（模型定义处）。
    - test_inv6_no_bypass_audit_event_write：扫所有 .py（剪 prune），跳过 stripped.startswith("class AuditEvent") 模型定义行，命中 _RE_ORM_WRITE/_RE_INSTANCE_SAVE/_RE_INSTANTIATE 即记 violation 列 文件:行；assert not violations（除 AuditService 外无旁路写）。
    - test_inv6_audit_writer_actually_writes：读 _ALLOWED_WRITER 文件文本，assert _RE_ORM_WRITE.search(text)（断言 writer 确含 AuditEvent.objects.create，防守护形同虚设）。
    说明：AuditEventImmutableError 由负向前瞻排除；任何 Plan 02 之外的源码若直写 AuditEvent 即 fail——这是本 phase append-only「无旁路写表」第二道防线（模型层守护为第一道）。
  </action>
  <acceptance_criteria>
    - server/tests/audit/test_audit_inv6_guard.py contains `_ALLOWED_WRITER = "audit/services/audit_service.py"` 与 `def test_inv6_no_bypass_audit_event_write` 与 `def test_inv6_audit_writer_actually_writes`。
    - 守护正则含负向前瞻排除 `AuditEventImmutable`（避免误判异常类）。
    - `cd server && uv run pytest tests/audit/test_audit_inv6_guard.py -q` 全部通过（无旁路写 + writer 确实写）。
  </acceptance_criteria>
  <verify>
    <automated>cd server && uv run pytest tests/audit/test_audit_inv6_guard.py -q</automated>
  </verify>
  <done>INV-6 grep 守护通过：server 源码除 AuditService 外无旁路 AuditEvent 写入；writer-actually-writes 反向断言生效。</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 02)

新建符号 / 文件（供 plan-review 源码 grounding 排除「新建符号被当漂移」）：

- **新文件**：
  - `server/audit/services/__init__.py`
  - `server/audit/services/taxonomy.py`
  - `server/audit/services/redaction.py`
  - `server/audit/services/audit_service.py`
  - `server/tests/audit/test_audit_taxonomy.py`
  - `server/tests/audit/test_audit_redaction.py`
  - `server/tests/audit/test_audit_service.py`
  - `server/tests/audit/test_audit_failsoft.py`
  - `server/tests/audit/test_audit_inv6_guard.py`
- **新类**：`AuditService`（`audit/services/audit_service.py`）
- **新方法**：`AuditService.emit`（@staticmethod，sync 单一写入入口）、`AuditService.aemit`（@staticmethod，async via sync_to_async）
- **新函数**：`_redact_audit_payload`、模块私有 `_redact_str` / `_is_sensitive_key` / `_shannon_entropy`（redaction.py）；模块私有 `_actor_id` / `_actor_repr`（audit_service.py）
- **新常量**：`REDACTION_PLACEHOLDER`（redaction.py）；taxonomy 种子常量 `ACTION_MEMBER_CREATED / ACTION_MEMBER_UPDATED / ACTION_MEMBER_DELETED / ACTION_USER_ACTIVATED / ACTION_USER_DEACTIVATED / ACTION_ROLE_CHANGED / ACTION_PROJECT_CONFIG_CHANGED / ACTION_REPOSITORY_PERMISSION_CHANGED / ACTION_CREDENTIAL_CREATED / ACTION_CREDENTIAL_UPDATED / ACTION_CREDENTIAL_DELETED / ACTION_PAT_CREATED / ACTION_PAT_REVOKED / ACTION_FEISHU_SYNC_TRIGGERED / ACTION_EXCLUSION_RULE_CHANGED` + `ALL_ACTIONS` + `RESERVED_ACTIONS`（taxonomy.py）
- **新结构化日志事件**：`audit.emit_failed`（warning，fail-soft 通道）
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 审计写入唯一收口 | 所有 AuditEvent 落库只允许经 AuditService.emit/aemit；INV-6 grep 守护断言无旁路写表入口 |
| before/after/metadata 载荷 | 调用方传入的快照可能含明文凭证/token；入口强制脱敏是纵深防御兜底，绝不信任调用方已脱敏 |
| emit 失败 | emit 失败绝不冒泡阻断主操作（fail-soft）；但失败也绝不把敏感载荷写进 warning 日志 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-04 | Information Disclosure | before/after/metadata | mitigate | _redact_audit_payload 在 emit 入口强制对三字段脱敏（key-name + 值级密钥/高熵），绝不回填明文；DB-无明文测试断言（AUDIT-02） |
| T-53-05 | Information Disclosure | emit 失败日志 | mitigate | warning 仅记 action/target_type，不记 before/after/metadata 载荷（避免失败路径泄漏明文） |
| T-53-06 | Tampering / Elevation | AuditEvent 写入 | mitigate | AuditService 单一写入入口 + INV-6 grep 守护断言无旁路 AuditEvent.objects.<write> / 实例化 / .save()（AUDIT-01） |
| T-53-07 | Denial of Service | 主操作可用性 | mitigate | emit 整段 try/except 吞异常 + warning，绝不阻断/回滚主操作（fail-soft，AUDIT-02） |
| T-53-08 | Tampering | async actor 访问 | mitigate | actor 字段访问全在 sync_to_async(emit) 同步块内，规避 async 裸访问 lazy-FK（per RESEARCH §6） |
</threat_model>

<verification>
- `cd server && uv run ruff format --check audit tests/audit && uv run ruff check audit tests/audit`
- `cd server && uv run pytest tests/audit/ -q`（含 Plan 01 的 model/append-only 测试，全 phase 套件绿）
- `cd server && uv run python manage.py makemigrations --check --dry-run`（无新 migration——本 plan 仅 service/helper，无模型字段变更）
</verification>

<success_criteria>
- AuditService.emit/aemit 是 AuditEvent 唯一写入入口；sync/async 双面落库字段一致。
- emit 入口强制脱敏 before/after/metadata（key-name + 值级密钥/高熵），DB 无明文；调用方传明文也兜底脱敏。
- emit fail-soft：异常吞掉 + audit.emit_failed warning，绝不冒泡阻断主操作。
- action taxonomy 稳定常量容器（种子 + RESERVED + ALL_ACTIONS）齐备，命名规范守护通过。
- INV-6 grep 守护通过：除 AuditService 外无旁路 AuditEvent 写表；writer-actually-writes 反向断言生效。
- 映射：AUDIT-01（单一写入入口 INV-6 + 无旁路写表第二道防线）、AUDIT-02（统一 emit 机制 + taxonomy + fail-soft + 凭证脱敏不落明文）；SC-1（单一 service 入口 + INV-6 grep）、SC-3（sync/async 双面 + fail-soft）、SC-4（脱敏不落明文）全覆盖。
</success_criteria>

<output>
Create `.planning/phases/53-auditevent-emit/53-02-SUMMARY.md` when done
</output>
