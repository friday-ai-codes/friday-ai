---
phase: 55
plan: 01
slug: query-api
requirements: [AUDITUI-01]
status: ready
---

# 55-01 — 审计查询 REST API（列表+详情+过滤+分页+fail-closed+只读）

## Tasks
1. `server/audit/api/__init__.py` + `serializers.py`：`AuditEventSerializer`（全字段只读 ModelSerializer）。
2. `server/audit/api/views.py`：
   - `AuditEventListView(APIView)`：`permission_classes=[IsSuperUser]`，async get，过滤 actor_id/action/target_type/target_id/source/occurred_from/occurred_to/q，offset/limit（默认 50/上限 200），返回 `{items, total}`。
   - `AuditEventDetailView(APIView)`：`IsSuperUser`，async get，404 容错。
3. `server/audit/urls.py`：注册 `events/`、`events/<uuid:event_id>/`（仅 GET）。
4. `friday/urls.py`：`path("audit/", include("audit.urls"))`。
5. `server/tests/audit/test_query_api.py`：过滤/分页/详情/fail-closed(403 非 superuser)/只读(无写路由)。

## Acceptance
- superuser 可列表/详情/过滤/分页；非 superuser 403；POST/DELETE 不可用。
- `uv run pytest tests/audit/test_query_api.py -q` 全绿；`manage.py check` 0 issues。
