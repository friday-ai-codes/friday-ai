---
phase: 55
plan: 03
slug: frontend
requirements: [AUDITUI-02]
status: ready
---

# 55-03 — 前端审计视图（列表+过滤+详情+导出+导航）

## Tasks
1. `web/src/api/audit.ts`：类型 `AuditEvent`/`AuditListResp` + `auditApi.list(params)`/`detail(id)`/`exportUrl(params, format)`。
2. `web/src/pages/admin/audit/index.vue`：`definePage({ meta: { requiresAdmin: true } })`；过滤栏 + 表格 + 分页 + 详情弹窗(before/after 并排) + 导出按钮（CSV/JSON）。用 vue-query/useToast/useErrorHandler/PageContainer/ui 组件。
3. `web/src/locales/zh-CN.json`：`audit.*` 文案命名空间。
4. `web/src/components/layout/AppSidebar.vue`：adminNavItems 增「操作审计」入口。
5. `web/src/pages/admin/__tests__/audit.spec.ts`：渲染/过滤/导出按钮基本断言。

## Acceptance
- `/admin/audit` 渲染列表+过滤+分页+详情+导出；非 superuser 不可达（守卫）。
- `pnpm test audit` 全绿；`vue-tsc` 无类型错误（涉及文件）；eslint 通过。
