---
phase: 29-comment-events
plan: 03
type: execute
wave: 3
depends_on: ["29-02"]
files_modified:
  - server/feishu/views.py
  - server/delivery/api/serializers.py
  - server/delivery/api/views.py
  - server/delivery/urls.py
  - server/tests/delivery/test_comment_api.py
  - server/tests/delivery/test_comment_entry_wiring.py
  - server/tests/delivery/test_inv6_guard.py
autonomous: true
requirements: [CMT-01, CMT-02]
must_haves:
  truths:
    - "飞书 webhook 评论事件在保留既有 approval 处理的同时，后台 append CommentEvent（INV-3：不改 knowledge）"
    - "缺三元组 / 缺 canonical work item → 跳过 append + warning，不抛"
    - "最小只读 REST 端点按三元组返回当前评论树投影（IsAuthenticated）"
    - "CommentEvent 落库只经 CommentEventService（INV-6 grep 守护，无旁路写表）"
  artifacts:
    - path: "server/feishu/views.py"
      provides: "_handle_workitem_comment 接线后台 append CommentEvent（approval 单一来源复用 classify_approval_semantic）"
      contains: "append_webhook_comment"
    - path: "server/delivery/api/views.py"
      provides: "WorkItemCommentTreeView（按三元组返回投影，IsAuthenticated）"
      contains: "CommentTree"
    - path: "server/delivery/urls.py"
      provides: "评论树只读路由"
      contains: "comments"
  key_links:
    - from: "server/feishu/views.py"
      to: "delivery.services.CommentEventService.append_webhook_comment"
      via: "run_in_background lazy import（同 _schedule_delivery_upsert 范式）"
      pattern: "append_webhook_comment"
    - from: "server/delivery/api/views.py"
      to: "delivery.services.project_comment_tree"
      via: "REST 读端点投影"
      pattern: "comment_tree|project_comment_tree"
---

<objective>
把 29-02 的评论事件流接到两条真实入口：① 飞书 webhook 评论事件 `_handle_workitem_comment` 在保留既有 approval 处理的同时后台 append CommentEvent（INV-3 不动 knowledge）；② 最小只读 REST 端点按三元组返回当前评论树投影（IsAuthenticated）。补 INV-6 评论旁路写表 grep 守护 + 接线/REST 测试。

Purpose: 兑现 CMT-01/CMT-02 的可观察闭环——评论从真实 webhook 入库、从事件流投影评论树、且落库单一入口受守护。
Output: webhook 接线、评论树 REST（serializer+view+url）、INV-6 评论守护测试、接线/REST 守护测试。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-comment-events/29-CONTEXT.md
@server/feishu/views.py
@server/delivery/api/views.py
@server/delivery/api/serializers.py
@server/delivery/urls.py
@server/tests/delivery/test_api.py
@server/tests/delivery/test_entry_wiring.py
@server/tests/delivery/test_inv6_guard.py
@.planning/phases/29-comment-events/29-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 飞书 webhook 评论事件后台 append CommentEvent</name>
  <files>server/feishu/views.py</files>
  <read_first>
    - server/feishu/views.py _handle_workitem_comment（既有 approval 关键词 + FeishuApprovalHandler 处理，保留不动）+ _schedule_delivery_upsert（webhook 后台投递范式：lazy import delivery + background_runner、缺三元组跳过 + warning、run_in_background best-effort）
    - server/delivery/services/comment_event_service.py（29-02：append_webhook_comment / classify_approval_semantic 签名）
    - server/delivery/services/__init__.py（re-export）
  </read_first>
  <action>
    修改 server/feishu/views.py 的 `_handle_workitem_comment`：在保留既有 approval 关键词识别 + FeishuApprovalHandler 处理逻辑的前提下，**追加**后台 append CommentEvent（与 _schedule_delivery_upsert 同范式）。
    - 既有 approval 判定改为复用 29-02 `classify_approval_semantic`（lazy import delivery）作单一来源——webhook 自身的 is_approved/is_rejected 分支保持行为不变（可由 classify 结果推导：semantic=="approve" → approved 分支，"reject" → rejected 分支），避免关键词在两处漂移（per CONTEXT Grey Area 3）。若改造范围有风险，最小改动：保留既有 is_approved/is_rejected 关键词分支不动，append 路径单独调 classify_approval_semantic——二选一，按既有约定决定（保证关键词单一来源优先）。
    - 新增后台 append：从 payload 取 work_item_id、work_item_type_key（缺任一 → 跳过 + warning，不构造身份，沿用 INV-1 占位类型分裂防护）、comment 文本、可得的 comment_id/author/created_at/thread_parent_id（payload 不提供则空/None，不臆造 edited/deleted）。经 lazy import `from delivery.services import CommentEventService` + `from services.background_runner import run_in_background`，构造 `WorkItemIdentity`，`run_in_background(lambda: CommentEventService().append_webhook_comment(identity, comment_id=..., body=comment, author=..., thread_parent_id=..., created_at=..., source="feishu_webhook"), name=f"comment-append:{...}")`。
    - INV-3：既有 knowledge ingestion（若该 handler 有）与 approval 处理完全保留，评论 append 仅 ADD 在其后并存；webhook 主响应不被后台 append 阻塞（best-effort 脱离请求生命周期）。
    - 缺 work_item_id/comment → 维持既有早返回；append 路径在三元组不全时跳过 + warning。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/test_webhooks.py tests/delivery/test_comment_entry_wiring.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - _handle_workitem_comment 既有 approval 行为零回归（test_webhooks 全绿）。
    - 评论事件携完整三元组 → 后台经 CommentEventService.append_webhook_comment 投递（接线测试断言被调）。
    - 缺 work_item_type_key / 缺 work_item_id → 跳过 append + warning，不抛。
    - 既有 knowledge ingestion / approval handler 调用保留（INV-3）。
  </acceptance_criteria>
  <done>webhook 评论事件在保留 approval/knowledge 的同时后台 append CommentEvent，接线测试 + webhook 回归全绿。</done>
</task>

<task type="auto">
  <name>Task 2: 评论树只读 REST 端点</name>
  <files>server/delivery/api/serializers.py, server/delivery/api/views.py, server/delivery/urls.py</files>
  <read_first>
    - server/delivery/api/views.py（adrf APIView async get + IsAuthenticated + 三元组 query params 校验 + sync_to_async 序列化范式，见 WorkItemDetailView）
    - server/delivery/api/serializers.py（只读序列化器范式）
    - server/delivery/urls.py（字面段路由；friday/urls.py 已挂 delivery/，本端点新增字面段即可）
    - server/delivery/services/comment_projection.py（29-02：project_comment_tree / async 包装）
  </read_first>
  <action>
    在 server/delivery/api/serializers.py 新增 `CommentTreeNodeSerializer`（递归只读：feishu_comment_id / author / body / event_type / approval_semantic / is_deleted / event_time / children）——children 用 `serializers.ListField(child=...)` 或自引用 `CommentTreeNodeSerializer(many=True)` 递归（按既有约定，投影已是 dict 树，可直接 Serializer 校验/透传或简单透传 dict）。若投影直接产 dict 树，可省 serializer 直接返回 dict（按既有约定决定；优先轻量）。
    在 server/delivery/api/views.py 新增 `WorkItemCommentTreeView(APIView)`，permission_classes=[IsAuthenticated]，`async def get`：
      - 校验三元组 query params（feishu_project_key / work_item_type / work_item_id），缺失 → 400、work_item_id 非整数 → 400（复用 WorkItemDetailView 校验范式）；
      - 按三元组 afirst 取已落库 WorkItem，不存在 → 404（不旁路 fetch/upsert，只读）；
      - 经 sync_to_async 调 `project_comment_tree(work_item)`（或 async 包装），返回 `{"work_item_id": ..., "comments": <tree>}` 200。
    在 server/delivery/urls.py 新增字面段路由，如 `path("work-items/comments/", WorkItemCommentTreeView.as_view(), name="work-item-comment-tree")`（与既有 work-items/ 字面段不冲突）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/delivery/test_comment_api.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - 认证用户 GET 携三元组 → 200 + 评论树投影（含线程层级）。
    - 未认证 → 401/403。
    - 缺三元组参数 → 400；work_item 不存在 → 404；非法 work_item_id → 400。
    - 端点只读，不触发回源/落库（事件行数前后不变）。
  </acceptance_criteria>
  <done>评论树只读 REST 端点就位（IsAuthenticated + 三元组 → 投影），REST 测试全绿。</done>
</task>

<task type="auto">
  <name>Task 3: INV-6 评论旁路写表 grep 守护</name>
  <files>server/tests/delivery/test_inv6_guard.py</files>
  <read_first>
    - server/tests/delivery/test_inv6_guard.py（既有 INV-6 WorkItem 守护：精确锚定 `\bWorkItem\.objects\.(create|bulk_create|get_or_create|update_or_create)` + 实例化 + .save，排除 tests/ migrations/ models/ 与 service 自身；INV-3 投影保留守护范式）
  </read_first>
  <action>
    在 server/tests/delivery/test_inv6_guard.py 追加 CommentEvent 的 INV-6 守护测试（复用既有精确锚定范式，零误伤）：
      - 扫描 server/delivery/ + server/feishu/ 源码，断言除 `server/delivery/services/comment_event_service.py`（唯一 writer）外，无旁路 `WorkItemCommentEvent.objects.(create|bulk_create|get_or_create|update_or_create)` / `WorkItemCommentEvent(` 实例化 / `WorkItemCommentEvent(...).save(`；排除 tests/ / migrations/ / delivery/models/（模型定义自身）；命中报 文件:行；
      - 断言唯一 writer（comment_event_service.py）确含 WorkItemCommentEvent 写形态（守护有效性自证，避免守护因重命名而空转）；
      - 复用/扩展既有 INV-3 守护：delivery comment 接线不 import/写 knowledge 模型，feishu/views.py 评论 handler 仍保留既有 knowledge/approval 调用。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/delivery/test_inv6_guard.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - INV-6 评论守护：唯一 writer 外无旁路 WorkItemCommentEvent 写表（含 webhook 接线、REST、projection 均不写）。
    - writer 自证：comment_event_service.py 确含写表（守护非空转）。
    - INV-3：评论接线不动 knowledge；既有 INV-6/INV-3 WorkItem 守护无回归。
  </acceptance_criteria>
  <done>INV-6 评论旁路写表 grep 守护就位（精确锚定 + writer 自证），全套守护测试全绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 飞书 webhook → 后台 append | webhook payload 不可信，只取三元组/评论文本后台经 service append |
| client → 评论树 REST | 认证用户读评论树投影（IsAuthenticated） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-07 | Elevation of Privilege | 评论树 REST 端点 | mitigate | permission_classes=[IsAuthenticated]；只读端点不旁路 fetch/落库（测试守护未认证 401/403） |
| T-29-08 | Tampering | CommentEvent 旁路写表 | mitigate | INV-6 grep 守护：除 CommentEventService 外无旁路 WorkItemCommentEvent 写表（精确锚定测试守护） |
| T-29-09 | Spoofing | 伪造 webhook 评论 | mitigate | 复用既有 FeishuWebhookView token 校验；append 只取三元组 + 文本，canonical work_item 须已落库否则跳过 |
| T-29-10 | DoS | webhook 后台 append 阻塞主响应 | mitigate | run_in_background 脱离请求生命周期 best-effort；service 内幂等去重 + fail-soft（29-02） |
| T-29-11 | Repudiation/Integrity | knowledge 投影被改 | mitigate | INV-3 守护：评论接线仅 ADD，不动既有 knowledge ingestion / approval handler |
</threat_model>

<verification>
- `uv run pytest tests/delivery/ -q` 全绿（28 + 29-01/02/03 全套件）。
- `uv run pytest tests/test_webhooks.py -q` 无回归（webhook 接线未破坏既有触发/ingestion/approval）。
- `uv run ruff format --check delivery/ feishu/views.py tests/delivery/` + `ruff check delivery/ feishu/views.py tests/delivery/` 干净。
- 全程 respx mock + pytest-socket 零真实网络；未改 knowledge app（INV-3）；未新增第三方依赖。
</verification>

<success_criteria>
- webhook 评论事件在保留 approval/knowledge 的同时后台 append CommentEvent（INV-3）。
- 最小只读 REST 端点按三元组返回当前评论树投影（IsAuthenticated）。
- INV-6 评论旁路写表 grep 守护就位（落库仅经 CommentEventService）。
</success_criteria>

<artifacts_produced>
- `server/feishu/views.py` — _handle_workitem_comment 后台 append 接线（approval 复用单一来源）
- `server/delivery/api/serializers.py` — `CommentTreeNodeSerializer`（或轻量 dict 透传）
- `server/delivery/api/views.py` — `WorkItemCommentTreeView`（IsAuthenticated）
- `server/delivery/urls.py` — 评论树只读路由
- `server/tests/delivery/test_comment_api.py` — REST 守护测试
- `server/tests/delivery/test_comment_entry_wiring.py` — webhook 接线守护测试
- `server/tests/delivery/test_inv6_guard.py` — 追加 INV-6 评论守护
</artifacts_produced>

<output>
Create `.planning/phases/29-comment-events/29-03-SUMMARY.md` when done
</output>
