---
phase: 28-workitem-spine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/delivery/__init__.py
  - server/delivery/apps.py
  - server/delivery/models/__init__.py
  - server/delivery/models/work_item.py
  - server/delivery/models/sync_state.py
  - server/delivery/models/relation.py
  - server/delivery/models/status_event.py
  - server/delivery/migrations/__init__.py
  - server/delivery/migrations/0001_initial.py
  - server/friday/settings.py
  - server/tests/delivery/__init__.py
  - server/tests/delivery/test_models.py
autonomous: true
requirements: [WIT-01, WIT-02, WIT-03, WIT-04, WIT-05]
must_haves:
  truths:
    - "delivery app 注册进 INSTALLED_APPS，Django 可加载且 makemigrations 无报错"
    - "同一三元组 (feishu_project_key, work_item_type, work_item_id) 只能存在一行 WorkItem（DB unique_together 强制 INV-1）"
    - "WorkItem 字段按 mirror / friday_enhanced / writeback / 元数据 四组齐备，逐字段对齐 DOMAIN §12.1"
    - "WorkItemSyncState / WorkItemRelation / WorkItemStatusEvent 三表就位，含 §12.2/§12.3/§12.4 字段、choices 与 unique_together 约束"
    - "执行 migrate 后数据库实际建出 delivery 的四张表"
  artifacts:
    - path: "server/delivery/models/work_item.py"
      provides: "canonical WorkItem 模型 + Origin choices（含 bitable_import/mr_reverse 枚举占位）"
      contains: "class WorkItem"
    - path: "server/delivery/models/sync_state.py"
      provides: "WorkItemSyncState（按 facet 完整度）"
      contains: "class WorkItemSyncState"
    - path: "server/delivery/models/relation.py"
      provides: "WorkItemRelation（派生关系 + target_external_id 占位）"
      contains: "target_external_id"
    - path: "server/delivery/models/status_event.py"
      provides: "WorkItemStatusEvent（append-only 状态事件）"
      contains: "class WorkItemStatusEvent"
    - path: "server/delivery/migrations/0001_initial.py"
      provides: "四表初始迁移"
      contains: "WorkItem"
  key_links:
    - from: "server/friday/settings.py"
      to: "delivery app"
      via: "INSTALLED_APPS 追加 \"delivery\""
      pattern: "\"delivery\""
    - from: "server/delivery/models/work_item.py"
      to: "INV-1 自然键唯一"
      via: "Meta.unique_together / UniqueConstraint"
      pattern: "feishu_project_key.*work_item_type.*work_item_id"
---

<objective>
新建 Django `delivery` app 并立起 v0.6.0 操作态脊柱的**数据层**：canonical `WorkItem`（飞书三元组身份）+ `WorkItemSyncState`（按 facet 来源完整度）+ `WorkItemRelation`（派生关系 + 占位）+ `WorkItemStatusEvent`（append-only 状态事件），生成并应用初始 migration（本 phase 创建 DB 表）。

本 plan 只产出 **app 脚手架 + 四个模型 + migration + 模型层单测**；不写 service、不接线 webhook/REST（Plan 28-02 / 28-03）。模型字段严格对齐 DOMAIN §12.1–§12.4，INV-1 由 DB `unique_together` 强制。

Purpose: 为 Plan 28-02 `WorkItemService.upsert`（DOMAIN §13.1）与后续 Phase 29/30/31/32/34 的 FK 关联提供操作态事实源的表结构。
Output: 新增 `server/delivery/` app（models 包 + apps.py + migrations）+ settings 注册 + 模型单测。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/28-workitem-spine/28-CONTEXT.md
@.planning/DOMAIN-MODEL.md
@server/friday/settings.py
@server/feishu/models.py
@server/workflows/models/__init__.py
</context>

<artifacts_produced>
## Artifacts this phase produces（本 plan 部分）

新增 app `server/delivery/`（snake_case 模块、PascalCase 模型 + `str/Enum`(TextChoices) choices、中文 docstring、ruff line 100）：

- `server/delivery/__init__.py`、`server/delivery/apps.py`（`class DeliveryConfig(AppConfig)`，`name = "delivery"`，`default_auto_field = "django.db.models.BigAutoField"`，`verbose_name = "交付脊柱"`）。
- `server/delivery/models/__init__.py` — curated re-export：`WorkItem`、`WorkItemOrigin`、`WorkItemSyncState`、`SyncFacet`、`SyncStatus`、`WorkItemRelation`、`RelationType`、`RelationOrigin`、`WorkItemStatusEvent`（+ `__all__`）。
- `server/delivery/models/work_item.py`：
  - `class WorkItemOrigin(models.TextChoices)` — `FEISHU_WEBHOOK="feishu_webhook"`、`MANUAL="manual"`、`BITABLE_IMPORT="bitable_import"`、`MR_REVERSE="mr_reverse"`（后两者本 phase 仅枚举占位，真实调用方 Phase 31/32）。
  - `class WorkItem(models.Model)` — 字段逐项对齐 DOMAIN §12.1：`id=UUIDField(primary_key, default=uuid4)`；自然键 `feishu_project_key=CharField(64)` / `work_item_type=CharField(32)` / `work_item_id=BigIntegerField`；`feishu_project_simple_name=CharField(128, blank)`；`project=FK("projects.Project", null=True, on_delete=SET_NULL, related_name="work_items")`；`origin=CharField(choices=WorkItemOrigin.choices)`；mirror：`title=CharField(512, blank)`、`status_state_key=CharField(64, blank)`、`status_sub_stage=CharField(64, blank)`、`status_display_name=CharField(128, blank)`、`is_archived_state=BooleanField(default=False)`、`is_init_state=BooleanField(default=False)`、`feishu_fields=JSONField(default=list)`、`prd_url=URLField(max_length=1000, blank)`、`tech_doc_url=URLField(max_length=1000, blank)`；friday_enhanced：`business_line_normalized=CharField(128, blank)`、`module_normalized=CharField(128, blank)`、`internal_note=TextField(blank)`；writeback：`feishu_chat_id=CharField(128, blank)`；元数据：`field_provenance=JSONField(default=dict)`、`last_synced_at=DateTimeField(null=True, blank=True)`、`created_at=DateTimeField(auto_now_add=True)`、`updated_at=DateTimeField(auto_now=True)`、`event_time=DateTimeField(null=True, blank=True)`。`Meta`：`db_table="delivery_work_item"`、`unique_together=(("feishu_project_key","work_item_type","work_item_id"),)`、`indexes=[Index(fields=["project","work_item_type"]), Index(fields=["status_state_key"])]`。
- `server/delivery/models/sync_state.py`（DOMAIN §12.2）：`class SyncFacet(TextChoices)` = `BASIC_FIELDS="basic_fields"`、`PRD_BODY="prd_body"`、`TECH_DOC="tech_doc"`、`COMMENTS="comments"`、`RELATIONS="relations"`；`class SyncStatus(TextChoices)` = `COMPLETE`、`PARTIAL`、`MISSING`、`STALE`；`class WorkItemSyncState(models.Model)` — `id=UUID`、`work_item=FK(WorkItem, CASCADE, related_name="sync_states")`、`facet=CharField(choices=SyncFacet.choices)`、`status=CharField(choices=SyncStatus.choices)`、`source=CharField(choices=WorkItemOrigin.choices)`、`last_synced_at=DateTimeField(null=True, blank=True)`、`error=TextField(blank=True)`、`updated_at=DateTimeField(auto_now=True)`。`Meta`：`db_table="delivery_work_item_sync_state"`、`unique_together=(("work_item","facet"),)`。
- `server/delivery/models/relation.py`（DOMAIN §12.3）：`class RelationType(TextChoices)` = `BELONGS_TO_PROJECT="belongs_to_project"`、`SPRINT="sprint"`、`VERSION="version"`、`RELATED="related"`；`class RelationOrigin(TextChoices)` = `FEISHU_FIELD="feishu_field"`、`FEISHU_RELATION_API="feishu_relation_api"`、`FRIDAY="friday"`；`class WorkItemRelation(models.Model)` — `id=UUID`、`source_work_item=FK(WorkItem, CASCADE, related_name="out_relations")`、`target_work_item=FK(WorkItem, CASCADE, null=True, blank=True, related_name="in_relations")`、`target_external_id=BigIntegerField(null=True, blank=True)`、`relation_type=CharField(choices=RelationType.choices)`、`source_field_key=CharField(64)`、`origin=CharField(choices=RelationOrigin.choices, default=RelationOrigin.FEISHU_FIELD)`、`created_at=DateTimeField(auto_now_add=True)`。`Meta`：`db_table="delivery_work_item_relation"`、`unique_together=(("source_work_item","relation_type","target_external_id","source_field_key"),)`。
- `server/delivery/models/status_event.py`（DOMAIN §12.4）：`class WorkItemStatusEvent(models.Model)` — `id=UUID`、`work_item=FK(WorkItem, CASCADE, related_name="status_events")`、`pre_state_key=CharField(64, blank)`、`cur_state_key=CharField(64, blank)`、`pre_sub_stage=CharField(64, blank)`、`cur_sub_stage=CharField(64, blank)`、`operator=CharField(128, blank)`、`event_time=DateTimeField(null=True, blank=True)`、`ingested_at=DateTimeField(auto_now_add=True)`。`Meta`：`db_table="delivery_work_item_status_event"`、`indexes=[Index(fields=["work_item","event_time"])]`。
- `server/delivery/migrations/__init__.py` + `server/delivery/migrations/0001_initial.py`（由 makemigrations 生成，纳入版本控制）。
- `server/tests/delivery/__init__.py` + `server/tests/delivery/test_models.py`。
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: 创建 delivery app 脚手架并注册 INSTALLED_APPS</name>
  <read_first>
    - server/repositories/apps.py（AppConfig 范式：default_auto_field/name/verbose_name）
    - server/friday/settings.py L89-126（INSTALLED_APPS，bare app 名，注册顺序）
    - server/workflows/models/__init__.py（models 包 curated re-export + __all__ 范式）
  </read_first>
  <action>
    新建目录 `server/delivery/` 与 `server/delivery/migrations/`。创建：`server/delivery/__init__.py`（空）；`server/delivery/apps.py`（`class DeliveryConfig(AppConfig)`，`default_auto_field = "django.db.models.BigAutoField"`，`name = "delivery"`，`verbose_name = "交付脊柱"`，无 `ready()` 副作用）；`server/delivery/migrations/__init__.py`（空）；`server/delivery/models/__init__.py`（先建空占位，Task 2 填 re-export）。在 `server/friday/settings.py` 的 `INSTALLED_APPS` 列表中、`"feishu"` 之后追加一行 `"delivery",`（bare 名即可，Django 自动发现 `DeliveryConfig`）。本 task 不定义任何模型字段。
  </action>
  <verify>
    <automated>cd server && uv run python -c "import django,os; os.environ.setdefault('DJANGO_SETTINGS_MODULE','friday.settings'); django.setup(); from django.apps import apps; print(apps.get_app_config('delivery').name)"</automated>
  </verify>
  <acceptance_criteria>
    - `server/delivery/{__init__.py,apps.py,models/__init__.py,migrations/__init__.py}` 均存在。
    - `"delivery"` 出现在 settings INSTALLED_APPS。
    - Django setup 后 `apps.get_app_config("delivery")` 解析成功，无 import 报错。
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: 定义四个模型（WorkItem / SyncState / Relation / StatusEvent）+ choices + 包 re-export</name>
  <read_first>
    - .planning/DOMAIN-MODEL.md §12.1（WorkItem 字段表 L413-438）、§12.2（SyncState L440-451）、§12.3（Relation L453-464）、§12.4（StatusEvent L466-470）
    - .planning/phases/28-workitem-spine/28-CONTEXT.md（Grey Area 2/4/5/6 字段决策 L27-62）
    - server/feishu/models.py（TextChoices + Meta + UUIDField + db_table 既有范式）
    - server/projects/models.py L21-33（projects.Project：FK 目标、feishu_project_key）
  </read_first>
  <action>
    按 `<artifacts_produced>` 清单逐文件创建 `server/delivery/models/work_item.py`、`sync_state.py`、`relation.py`、`status_event.py`，字段/类型/choices/`Meta`（db_table / unique_together / indexes）逐项对齐 DOMAIN §12.1–§12.4 与本 plan artifacts 描述。`id` 一律 `UUIDField(primary_key=True, default=uuid.uuid4, editable=False)`。FK 用字符串引用 `"projects.Project"` 避免循环 import；同 app 内 FK（SyncState/Relation/StatusEvent → WorkItem）可直接类引用。所有 `WorkItem.origin` / `WorkItemSyncState.source` 共用 `WorkItemOrigin.choices`（含 `bitable_import`/`mr_reverse` 枚举占位）。每个模型加中文 docstring 标注 source-of-truth 三分类（mirror/friday_enhanced/writeback）。`server/delivery/models/__init__.py` re-export 全部模型与 choices 类并定义 `__all__`。**不**在模型层写任何 create/save 业务逻辑（落库逻辑归 Plan 28-02 service，INV-6）。
  </action>
  <verify>
    <automated>cd server && uv run python manage.py makemigrations delivery --check --dry-run</automated>
  </verify>
  <acceptance_criteria>
    - 四模型字段逐项匹配 DOMAIN §12.1–§12.4（含 feishu_fields=JSONField(default=list)、field_provenance=JSONField(default=dict)、target_external_id 可空）。
    - `WorkItem.Meta.unique_together` 含三元组 (feishu_project_key, work_item_type, work_item_id)；`WorkItemSyncState` 含 (work_item, facet)；`WorkItemRelation` 含 (source_work_item, relation_type, target_external_id, source_field_key)。
    - `from delivery.models import WorkItem, WorkItemSyncState, WorkItemRelation, WorkItemStatusEvent` 全部可导入。
    - `makemigrations --check --dry-run` 报告"有待生成迁移"（即模型已就绪、尚未生成），无字段定义错误。
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3 [BLOCKING]: 生成并应用 migration + 模型层单测</name>
  <read_first>
    - server/tests/（pytest-django + factory-boy + pytest-socket 既有约定；conftest）
    - server/feishu/migrations/0001_initial.py（迁移文件风格参考）
    - .planning/DOMAIN-MODEL.md §16（story 1000000002 / issue 1000000006 作 fixture 取值）
  </read_first>
  <action>
    运行 `python manage.py makemigrations delivery` 生成 `0001_initial.py`（纳入版本控制），再 `python manage.py migrate delivery` 应用到 DB。新建 `server/tests/delivery/__init__.py` 与 `server/tests/delivery/test_models.py`（`@pytest.mark.django_db`）：① 创建一个 WorkItem（origin=manual），断言可读回字段；② 同三元组再 `WorkItem.objects.create(...)` → 断言抛 `IntegrityError`（验证 unique_together / INV-1 的 DB 强制，用 `pytest.raises(IntegrityError)`）；③ 建 WorkItemSyncState 同 (work_item, facet) 二次创建抛 IntegrityError；④ 建 WorkItemRelation 带 target_external_id 占位（target_work_item=None）成功；⑤ 建 WorkItemStatusEvent(pre/cur state_key) 成功。测试纯 ORM、不发网络（pytest-socket 隔离）。
  </action>
  <verify>
    <automated>cd server && uv run python manage.py migrate delivery && uv run pytest tests/delivery/test_models.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `0001_initial.py` 已生成并提交；`migrate delivery` 成功建出四张表（delivery_work_item / _sync_state / _relation / _status_event）。
    - 三元组重复创建 WorkItem 抛 IntegrityError（INV-1 DB 强制）。
    - (work_item, facet) 重复 SyncState 抛 IntegrityError。
    - 全部模型单测通过，无真实网络调用。
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 飞书工作项身份 → DB 持久层 | 三元组自然键作为唯一身份写入 DB，重复/伪造身份不得分裂成多行 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01 | Tampering | WorkItem 自然键 | mitigate | DB `unique_together(feishu_project_key, work_item_type, work_item_id)` 强制 INV-1，同一工作项无法分裂成多行 |
| T-28-02 | Tampering | append-only 事件表 | accept | WorkItemStatusEvent 设计为追加流（本 plan 仅建表，就地改写防护由 28-02 service 落地）；表层无删改约束，依赖 service 不就地改写 |
| T-28-03 | Information Disclosure | feishu_fields / field_provenance JSON 列 | accept | 镜像飞书业务字段（非凭证），凭证仍仅在 services 层加密存储；本 plan 不引入新凭证面 |
</threat_model>

<verification>
- `cd server && uv run python manage.py makemigrations delivery --check --dry-run`（Task 2 后应提示需生成；Task 3 生成后 `--check` 应报告 no changes）。
- `cd server && uv run python manage.py migrate delivery` 成功。
- `cd server && uv run pytest tests/delivery/test_models.py -q` 全绿。
- `cd server && uv run ruff format --check delivery/ tests/delivery/`（line 100）。
- 未改 knowledge app（INV-3）：本 plan files_modified 不含 knowledge/*。
</verification>

<success_criteria>
- `delivery` app 注册并可加载；四模型与 0001 迁移就位并已 migrate。
- INV-1 由 DB unique_together 强制（三元组重复创建抛 IntegrityError，测试覆盖）。
- 字段逐项对齐 DOMAIN §12.1–§12.4；无新增第三方依赖。
- 模型单测全绿，无真实网络。
</success_criteria>

<output>
Create `.planning/phases/28-workitem-spine/28-01-SUMMARY.md` when done.
</output>
