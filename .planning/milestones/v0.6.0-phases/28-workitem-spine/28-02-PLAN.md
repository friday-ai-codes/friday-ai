---
phase: 28-workitem-spine
plan: 02
type: execute
wave: 2
depends_on: ["28-01"]
files_modified:
  - server/delivery/services/__init__.py
  - server/delivery/services/derivation.py
  - server/delivery/services/work_item_service.py
  - server/delivery/signals.py
  - server/tests/delivery/test_work_item_service.py
autonomous: true
requirements: [WIT-01, WIT-02, WIT-03, WIT-04, WIT-05]
must_haves:
  truths:
    - "WorkItemService.upsert 是 WorkItem 唯一写入入口（INV-6）；同三元组多次 upsert（含不同 origin）收敛同一行（WIT-01）"
    - "upsert 只刷 mirror 字段，friday_enhanced（business_line_normalized/module_normalized/internal_note）被保护不被覆盖（WIT-02）"
    - "按 facet 记 WorkItemSyncState（basic_fields/relations）；某 facet 回源失败 → 该 facet status=missing/error，WorkItem 不整体回滚（WIT-03）"
    - "从 work_item_related_multi_select 字段派生 WorkItemRelation，目标未落库时以 target_external_id 占位（WIT-04）"
    - "incoming 状态与库内不同 → append 一条 WorkItemStatusEvent(pre/cur)，非就地改写（WIT-05）"
  artifacts:
    - path: "server/delivery/services/work_item_service.py"
      provides: "WorkItemIdentity + WorkItemService.upsert 单一写入入口"
      contains: "async def upsert"
    - path: "server/delivery/services/derivation.py"
      provides: "纯函数：从飞书工作项响应派生 mirror 字段（status_display_name/sub_stage/archived/init）"
      contains: "def derive_status_fields"
    - path: "server/tests/delivery/test_work_item_service.py"
      provides: "upsert 行为守护测试（WIT-01..05，respx mock 回源）"
      contains: "respx"
  key_links:
    - from: "server/delivery/services/work_item_service.py"
      to: "server/services/feishu.py get_work_item"
      via: "回源拉取（fetch=True）"
      pattern: "get_work_item"
    - from: "server/delivery/services/work_item_service.py"
      to: "server/services/feishu_parsing.py"
      via: "复用 derive_relations_from_fields / extract_prd_url / extract_tech_doc_url"
      pattern: "from services.feishu_parsing import"
---

<objective>
实现操作态脊柱的**唯一写入入口** `WorkItemService.upsert`（DOMAIN §13.1，INV-6），覆盖：三元组幂等收敛（WIT-01）、mirror-only 刷新且保护 friday_enhanced（WIT-02）、按 facet 记 `WorkItemSyncState` 且部分 facet 失败不回滚整体（WIT-03）、从关联字段派生 `WorkItemRelation` + `target_external_id` 占位（WIT-04）、状态变更 append `WorkItemStatusEvent`（WIT-05）。

派生逻辑**复用 Phase 27** `server/services/feishu_parsing.py`（`build_feishu_fields` 已在 `get_work_item` 内产出于 `WorkItemInfo.feishu_fields`；`derive_relations_from_fields` / `extract_prd_url` / `extract_tech_doc_url` 直接消费），仅新增 delivery 侧 `derive_status_fields`（status_display_name / sub_stage / archived / init，从工作项响应 dict 取，非 Phase 27 覆盖范围）。

本 plan 只产出 **service + 派生纯函数 + 信号 + 单测**；REST/webhook 接线与 INV-6 grep 守护归 Plan 28-03。
Purpose: 给 webhook / manual / (后续 bitable_import / mr_reverse) 各路径提供收敛唯一入口。
Output: `delivery/services/` 模块 + `delivery/signals.py` + service 守护测试。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/28-workitem-spine/28-CONTEXT.md
@.planning/DOMAIN-MODEL.md
@server/delivery/models/__init__.py
@server/services/feishu.py
@server/services/feishu_parsing.py
@server/knowledge/sources/feishu_work_item.py
@server/services/background_runner.py
</context>

<artifacts_produced>
## Artifacts this phase produces（本 plan 部分）

- `server/delivery/services/__init__.py` — re-export `WorkItemService`、`WorkItemIdentity`。
- `server/delivery/services/derivation.py`（纯函数、无 DB、无网络）：
  - `def derive_status_fields(raw_item: dict) -> dict` — 从单条工作项响应 dict 取 `status_state_key`(work_item_status.state_key)、`status_sub_stage`(work_item_status.sub_stage)、`status_display_name`（优先 `current_nodes[].name`，回退 `state_times[].name`，取能稳定拿到的人类名）、`is_archived_state`、`is_init_state`（容错缺失，缺省空串/False）。
  - `def derive_status_events(raw_item: dict) -> list[dict]` — 从工作项响应 `work_item_status.history[]` 归一历史状态事件（state_key + updated_at/updated_by），供回填去重（best-effort，可空）。
- `server/delivery/signals.py` — `work_item_synced = django.dispatch.Signal()`（发 `work_item.synced` 事件，best-effort）。
- `server/delivery/services/work_item_service.py`：
  - `@dataclass(frozen=True) class WorkItemIdentity` — `feishu_project_key: str`、`work_item_type: str`、`work_item_id: int`。
  - `class WorkItemService`：
    - `async def upsert(self, identity: WorkItemIdentity, source: str, *, fetch: bool = True) -> WorkItem` — DOMAIN §13.1 步骤：
      1. `select_for_update` 取/建 WorkItem（三元组幂等，INV-1）——封装为同步函数 `_get_or_create_locked`，内部 `transaction.atomic()`，经 `sync_to_async` 调用；`origin` 仅在首次创建时落，已存在不覆盖 origin。
      2. `fetch=True` → 经 `create_feishu_client_for_project(project)` + `get_work_item(project_key, work_item_id, work_item_type)` 回源（按真实 type，Phase 27 修好）；拿到 `WorkItemInfo`。
      3. 刷新 **mirror** 字段（title/status_*/feishu_fields/prd_url/tech_doc_url/is_archived/is_init）——`feishu_fields=info.feishu_fields`；prd_url/tech_doc_url 经 `extract_prd_url`/`extract_tech_doc_url`；status_* 经 `derive_status_fields(json.loads(info.raw_response)["data"][0])`。**绝不写** friday_enhanced（business_line_normalized/module_normalized/internal_note）与 writeback（feishu_chat_id）。
      4. 派生关系 → `WorkItemRelation`：`derive_relations_from_fields(info.feishu_fields)` 产 `RelationSpec`，逐条 `update_or_create`（按 unique_together 键），target 已落库则连 `target_work_item`、否则 `target_external_id` 占位；并 best-effort 反向回填：本次 upsert 的 WorkItem 作为他人 `target_external_id` 的占位目标时补连 `target_work_item`。
      5. 状态事件 → `WorkItemStatusEvent`：incoming `status_state_key` 与库内当前不同 → append 一条（pre=旧/cur=新），再更新 WorkItem.status_* mirror（先比对后更新）；history 回填去重（按 (work_item, cur_state_key, event_time)）。
      6. 写 `field_provenance`（{字段: source}）+ `last_synced_at`；按 facet 落 `WorkItemSyncState`（basic_fields、relations）：成功 complete、回源失败该 facet status=missing/error + error 文本；prd_body/tech_doc/comments 本 phase 不摄取 → 记 missing（不假装 complete）。
      7. best-effort 发 `work_item_synced` 信号（异常吞掉 + warning，不影响落库）。
      - **失败策略**：fetch 回源失败 → 不整体回滚 WorkItem（已 get_or_create 的行保留）；basic_fields facet 记 missing/error；缺料降配继续（对齐 knowledge normalizer 范式）。
- `server/tests/delivery/test_work_item_service.py`。

## 异步 / 事务约定
- service `async def`；所有 ORM 访问经 `sync_to_async` 桥接；`select_for_update` + 写操作放在同步函数内 `transaction.atomic()`，再 `sync_to_async` 调用（项目既有 async ORM 约定，参见 CLAUDE.md / background_runner）。
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 派生纯函数 derivation.py + work_item.synced 信号</name>
  <read_first>
    - .planning/DOMAIN-MODEL.md §1.5（状态人类名免映射：current_nodes/state_times L112）、§16（status：state_key + history[] + current_nodes + state_times L565）
    - server/services/feishu.py（WorkItemInfo.raw_response = json.dumps(data)，data={"data":[item],...}；get_work_item L118-204）
    - server/knowledge/sources/feishu_work_item.py（降级范式：部分缺料 warning 不 raise）
  </read_first>
  <behavior>
    - Test: `derive_status_fields` 输入 story 响应 dict（含 work_item_status.state_key="fi46o4r6m" + current_nodes=[{name:"Sprint计划"}]）→ status_state_key="fi46o4r6m"、status_display_name="Sprint计划"。
    - Test: 输入 issue 响应（work_item_status.state_key="OPEN"，无 current_nodes 但 state_times[].name）→ display_name 回退 state_times 名；缺两者 → 空串。
    - Test: `is_archived_state`/`is_init_state` 缺失 → False；存在 → 透传。
    - Test: `derive_status_events` 从 history[] 归一出事件列表；无 history → []。
  </behavior>
  <action>
    新建 `server/delivery/services/__init__.py`（占位，Task 2 补 re-export）、`server/delivery/services/derivation.py` 实现 `derive_status_fields` / `derive_status_events`（纯函数，容错 dict.get 取值，缺失降级空串/False/[]，不抛）。新建 `server/delivery/signals.py` 定义 `work_item_synced = Signal()`，docstring 标注 payload `{work_item_id, facets}`、best-effort（订阅者异常不回传）。不在本 task 触 ORM。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/delivery/test_work_item_service.py -x -q -k "derive or status_fields or status_events"</automated>
  </verify>
  <acceptance_criteria>
    - `derive_status_fields` 对 story/issue 两种响应正确取 state_key 与 display_name（current_nodes 优先、state_times 回退）。
    - 缺字段一律降级（空串/False/[]），不抛异常。
    - `work_item_synced` 信号对象可 import。
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WorkItemService.upsert 核心 —— 幂等收敛 + mirror-only 刷新 + facet SyncState</name>
  <read_first>
    - .planning/DOMAIN-MODEL.md §13.1（upsert 步骤 1-6 L499-505）、§1.2（三分类冲突规则 L56-60）、§1.4（幂等键/来源完整度/失败策略 L120-135）
    - .planning/phases/28-workitem-spine/28-CONTEXT.md（Grey Area 3 事务语义 L37-47、Grey Area 4 facet L49-52）
    - server/services/feishu.py（create_feishu_client_for_project L559-585、get_work_item、WorkItemInfo.feishu_fields/raw_response）
    - server/services/feishu_parsing.py（extract_prd_url/extract_tech_doc_url/derive_relations_from_fields 签名）
    - server/delivery/models/__init__.py（WorkItem/WorkItemSyncState/SyncFacet/SyncStatus/WorkItemOrigin）
  </read_first>
  <behavior>
    - Test (WIT-01): 同三元组连续 upsert 两次（origin=manual 再 origin=feishu_webhook，respx mock 同一工作项）→ `WorkItem.objects.count()==1`，origin 保持首次值。
    - Test (WIT-02): 预先在库内某 WorkItem 写 internal_note="x"/business_line_normalized="L"，再 upsert（mock 回源 title 变化）→ title 被刷新、internal_note 与 business_line_normalized 原样保留（mirror-only）。
    - Test (WIT-03): mock get_work_item 抛异常（回源失败）→ upsert 不抛、WorkItem 行仍存在（已 get_or_create），basic_fields 的 WorkItemSyncState.status 为 missing/error 且 error 非空。
    - Test: 成功回源 → basic_fields SyncState=complete、last_synced_at 写入、field_provenance 含 mirror 字段来源。
  </behavior>
  <action>
    在 `server/delivery/services/work_item_service.py` 实现 `WorkItemIdentity`(frozen dataclass) 与 `WorkItemService.upsert`。落 §13.1 步骤 1/2/3/6 与失败策略：① 同步 `_get_or_create_locked(identity, source, project)` 内 `transaction.atomic()` + `WorkItem.objects.select_for_update().get_or_create(...defaults={origin,project,...})`，经 `sync_to_async` 调用，返回 (work_item, created)；② project 经 `projects.Project.objects.filter(feishu_project_key=...)`（async）解析，缺失 → 仍建/取 WorkItem（project=None），basic_fields=missing + error="project_unconfigured"，不抛；③ fetch=True 调 `create_feishu_client_for_project` + `get_work_item`，try/except 包裹：成功刷 mirror（title/feishu_fields/prd_url/tech_doc_url 经 helper），失败记 basic_fields SyncState=missing/error；④ 刷新只写 mirror 字段集合（显式列出 update_fields），**不**触 friday_enhanced/writeback；⑤ 写 field_provenance + last_synced_at；按 facet `WorkItemSyncState.objects.update_or_create((work_item,facet), defaults)` 落 basic_fields；prd_body/tech_doc/comments 记 missing（本 phase 不摄取）；relations facet 由 Task 3 补；⑥ 末尾 best-effort `work_item_synced.send(...)`（try/except 吞）。`derive_status_fields` 调用：`json.loads(info.raw_response)["data"][0]` 容错取 item dict（缺失降级）。`server/delivery/services/__init__.py` re-export `WorkItemService`/`WorkItemIdentity`。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/delivery/test_work_item_service.py -x -q -k "idempotent or mirror or enhanced or facet or sync_state or fetch_fail"</automated>
  </verify>
  <acceptance_criteria>
    - 同三元组多次 upsert 收敛唯一行，origin 不被后续 upsert 覆盖（WIT-01）。
    - friday_enhanced 字段在 mirror 刷新中被保护（WIT-02）。
    - 回源失败 basic_fields facet 记 missing/error，WorkItem 不整体回滚（WIT-03）。
    - 成功路径写 last_synced_at + field_provenance + basic_fields=complete。
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 关系派生持久化（占位/回填）+ 状态事件 append + relations facet</name>
  <read_first>
    - .planning/DOMAIN-MODEL.md §12.3（Relation unique_together L464）、§12.4（StatusEvent L466-470）、§13.1 步骤 4/5
    - .planning/phases/28-workitem-spine/28-CONTEXT.md（Grey Area 5 关系占位/回填 L54-57、Grey Area 6 状态 append L59-62）
    - server/services/feishu_parsing.py（RelationSpec / derive_relations_from_fields L441-487）
    - server/delivery/models/__init__.py（WorkItemRelation/RelationType/RelationOrigin、WorkItemStatusEvent）
  </read_first>
  <behavior>
    - Test (WIT-04): mock 工作项含 field_caadeb=[7010938167]（target 未落库）→ upsert 后存在一条 WorkItemRelation(relation_type=belongs_to_project, target_external_id=7010938167, target_work_item=None)。
    - Test (WIT-04 回填): 先 upsert source（产占位 target_external_id=N），再 upsert id=N 的 WorkItem → 占位关系的 target_work_item 被回填为新行。
    - Test: 重复 upsert 不产生重复 Relation（unique_together 幂等，走 update_or_create）。
    - Test (WIT-05): 库内 status_state_key="A"，upsert 回源 cur="B" → 新增一条 WorkItemStatusEvent(pre_state_key="A", cur_state_key="B")，WorkItem.status_state_key 更新为 "B"；再次 upsert 同 "B" → 不再新增事件（无变更不 append）。
    - Test: relations facet SyncState 在派生成功后记 complete。
  </behavior>
  <action>
    续写 `upsert`（步骤 4/5 与 relations facet）：① 调 `derive_relations_from_fields(info.feishu_fields)`，逐 `RelationSpec` 经 `WorkItemRelation.objects.update_or_create(source_work_item, relation_type, target_external_id, source_field_key, defaults={origin})`；若已存在三元组为 (project_key, work_item_type, target_external_id) 的 WorkItem 则连 `target_work_item`；② 反向回填：本次 upsert 的 WorkItem.work_item_id 作为他行占位 target_external_id（同 project_key）时，补连这些 `WorkItemRelation.target_work_item`（best-effort，async queryset 过滤更新）；③ 状态事件：比对库内 `status_state_key` 与 incoming（derive_status_fields 结果），不同 → `WorkItemStatusEvent.objects.create(pre_*, cur_*, operator, event_time)` 再更新 WorkItem.status_* mirror（**先 append 后改 mirror**，非就地覆盖历史）；history 回填用 `derive_status_events` 去重创建（(work_item, cur_state_key, event_time) 已存在则跳过）；④ relations facet `WorkItemSyncState` 记 complete（派生成功）/ 回源失败时随 basic_fields 记 missing。全部 ORM 经 `sync_to_async`；关系/事件写入与 mirror 更新可在同一原子块或分步（部分 facet 失败不回滚整体——关系派生异常仅记 relations facet error，不掀翻 WorkItem）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/delivery/test_work_item_service.py -x -q -k "relation or placeholder or backfill or status_event"</automated>
  </verify>
  <acceptance_criteria>
    - field_caadeb=[id] 派生 belongs_to_project，target 未落库走 target_external_id 占位（WIT-04）。
    - 目标后续 upsert 落库后占位关系回填 target_work_item。
    - 状态变更 append StatusEvent(pre/cur)、非就地改写；无变更不重复 append（WIT-05）。
    - relations facet SyncState 正确记录；重复 upsert 关系幂等不重复。
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 飞书回源响应 → upsert mirror 刷新 | 外部不可信数据经 service 写入操作态事实源（仅刷 mirror，不污染 enhanced） |
| 调用方 source 入参 → 落库 origin/SyncState.source | 各路径声明的 source 须落账、不串味 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-04 | Tampering | upsert mirror 刷新 | mitigate | 仅显式 update_fields 写 mirror 字段集合，friday_enhanced/writeback 不在集合内，回源数据无法覆盖本地拥有字段（WIT-02） |
| T-28-05 | Denial of Service | fetch 回源失败 | mitigate | get_work_item try/except，部分 facet 失败落 SyncState.error 不整体回滚，缺料降配继续（WIT-03） |
| T-28-06 | Repudiation | 状态变更历史 | mitigate | 状态变更 append-only WorkItemStatusEvent（pre/cur），先 append 后改 mirror，不就地覆盖历史（WIT-05） |
| T-28-07 | Information Disclosure | 回源异常日志 | mitigate | 复用 services.feishu 既有脱敏（body 截断、凭证不入日志）；SyncState.error 仅存人类可读摘要，不拼凭证 |
</threat_model>

<verification>
- `cd server && uv run pytest tests/delivery/test_work_item_service.py -q` 全绿（respx mock 回源，pytest-socket 不发真实网络）。
- `cd server && uv run ruff format --check delivery/services/ delivery/signals.py tests/delivery/test_work_item_service.py`。
- 未改 knowledge app（INV-3）；未新增第三方依赖。
- 快速回归：`cd server && uv run pytest tests/delivery/ -q`。
</verification>

<success_criteria>
- `WorkItemService.upsert` 落地 DOMAIN §13.1 全部步骤；同三元组/跨 origin 收敛唯一行（WIT-01）。
- mirror-only 刷新保护 enhanced（WIT-02）；per-facet SyncState 且部分失败不回滚（WIT-03）。
- 关系派生 + 占位 + 回填（WIT-04）；状态 append-only（WIT-05）。
- service 守护测试全绿，无真实网络。
</success_criteria>

<output>
Create `.planning/phases/28-workitem-spine/28-02-SUMMARY.md` when done.
</output>
