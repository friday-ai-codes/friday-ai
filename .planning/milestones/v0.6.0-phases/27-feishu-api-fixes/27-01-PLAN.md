---
phase: 27-feishu-api-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/services/feishu_parsing.py
  - server/tests/services/test_feishu_parsing.py
autonomous: true
requirements: [FIX-02, FIX-03, FIX-04]
must_haves:
  truths:
    - "非 JSON 飞书响应（HTML/空/Extra data）经防御解析不抛崩，返回 None 并记 warning（fail-soft 路径）"
    - "完整 fields[] 对象（field_key/field_name/field_value/field_type_key/field_alias）被保留，不丢元数据"
    - "从 work_item_related_multi_select 字段能派生出 belongs_to_project/sprint/version/related 关系规格"
    - "能按 alias/key 从 feishu_fields 取出 prd_url 与 select 字段 label"
  artifacts:
    - path: "server/services/feishu_parsing.py"
      provides: "飞书响应防御解析 + 字段保留/提取 + 关系派生 + 评论解析 共享 helper"
      contains: "def derive_relations_from_fields"
    - path: "server/tests/services/test_feishu_parsing.py"
      provides: "helper 全函数单测（DOMAIN §16 实测 fixture）"
      contains: "field_caadeb"
  key_links:
    - from: "server/services/feishu_parsing.py"
      to: "DOMAIN-MODEL.md §16 字段对象形状"
      via: "build_feishu_fields / derive_relations_from_fields"
      pattern: "work_item_related_multi_select"
---

<objective>
建立飞书取数/解析的**共享 helper 模块** `server/services/feishu_parsing.py`，把本 phase 4 个修复点中"可纯函数化"的解析逻辑一次性收敛于此，供 Plan 27-02（`services/feishu.py`）与 Plan 27-03（`feishu/client.py`）两份 client 共同调用，消除解析漂移（CONTEXT 决策："consolidate fixed parsing into a shared helper used by BOTH client copies"）。

本 plan 只产出**纯解析模块 + 单测**，不改任何 client、不动调用方、不落库。

Purpose: 为 PF-10/11/12 提供单一可信的解析实现，并为 FIX-02 的关系派生提供纯函数（Plan 28 `WorkItemService.upsert` 步骤 4 将从此消费）。
Output: 1 个新模块文件 + 1 个新测试文件。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/27-feishu-api-fixes/27-CONTEXT.md
@.planning/DOMAIN-MODEL.md
@server/services/feishu.py
@server/feishu/models.py
</context>

<artifacts_produced>
## Artifacts this phase produces（本 plan 部分）

新文件 `server/services/feishu_parsing.py` 导出以下符号（snake_case、中文 docstring、ruff line 100）：

- `class FeishuResponseError(Exception)` — 防御式解析硬失败时抛出（带 body 截断片段，禁含凭证）。
- `def safe_response_json(response, *, log_event: str, **log_ctx) -> Any | None` — content-type 校验 + try/except 包裹 `response.json()`；非 JSON → `logger.warning(log_event, ...)` 返回 `None`（fail-soft）。
- `def strict_response_json(response, *, log_event: str, **log_ctx) -> Any` — 同上但非 JSON → 抛 `FeishuResponseError`（硬取数路径用）。
- `def rich_text_to_markdown(rich_text: Any) -> str` — 由两份 client 的 `_parse_rich_text`/`_parse_paragraph` 上移而来的纯函数（行为等价：paragraph/heading/bullet_list/ordered_list/code_block/image + bold/italic/code/link marks）。
- `def build_feishu_fields(raw_fields: list[dict]) -> list[dict]` — 保留完整字段对象 `{field_key, field_name, field_value, field_type_key, field_alias}`（FIX-04 核心）。
- `def flatten_fields(raw_fields: list[dict]) -> dict[str, Any]` — 向后兼容拍平 `{field_key: field_value}`。
- `def find_field(feishu_fields: list[dict], *, key: str | None = None, alias: str | None = None) -> dict | None` — 按 field_key 或 field_alias 查字段对象。
- `def extract_select_label(field_value: Any) -> str | None` — select 类 `{label, value}` 取 label。
- `def extract_related_ids(field_value: Any) -> list[int]` — 关联类 `[id...]` 归一为 int 列表。
- `def extract_prd_url(feishu_fields: list[dict]) -> str` — alias `prd_url` 或 key `field_bcff9b`（`feishu.models.KeyFields.PRD_URL`）。
- `def extract_tech_doc_url(feishu_fields: list[dict]) -> str` — key `field_3f6667`（`KeyFields.TECH_DOC_URL`）。
- `RELATION_TYPE_BY_FIELD: dict[str, str]` — `{"field_caadeb": "belongs_to_project", "planning_sprint": "sprint", "planning_version": "version", "actual_online_version": "version"}`。
- `RELATION_FIELD_TYPE_KEY = "work_item_related_multi_select"`。
- `@dataclass class RelationSpec` — `relation_type: str`、`source_field_key: str`、`target_external_id: int`、`origin: str = "feishu_field"`（对齐 DOMAIN §12.3）。
- `def derive_relations_from_fields(feishu_fields: list[dict]) -> list[RelationSpec]` — 遍历 `field_type_key == work_item_related_multi_select` 的字段，对每个 `target_external_id` 产出一条 `RelationSpec`，relation_type 经 `RELATION_TYPE_BY_FIELD` 映射、未命中归 `related`（FIX-02 主路径，纯函数、无 DB、无网络）。
- `def parse_comments(data: Any) -> list[dict]` — 对齐飞书 `comment/list` 形状，逐条取 `{id, content(经 rich_text_to_markdown), created_at, author, thread_parent_id}`；data 为 None/形状不符 → 返回 `[]`（FIX-03 解析部分）。
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 防御式 JSON 解析 + 富文本上移 + 字段保留/提取 helper</name>
  <read_first>
    - server/services/feishu.py（`_parse_rich_text` L444-523、`_parse_paragraph` L499-523、get_work_item 字段拍平 L159-164 现状）
    - server/feishu/models.py（`KeyFields.PRD_URL=field_bcff9b` / `TECH_DOC_URL=field_3f6667` L184-189）
    - .planning/DOMAIN-MODEL.md §16（字段对象形状 L582、别名表 L569-578）
    - server/common/logging（structlog 既有配置约定，logger = structlog.get_logger(__name__)）
  </read_first>
  <behavior>
    - Test: `safe_response_json` 对一个 `content-type: text/html` 或 body=`Extra data: line 1 column 5` 的伪造 httpx.Response 返回 None，且不抛异常。
    - Test: `safe_response_json` 对合法 JSON 响应返回解析后的 dict。
    - Test: `strict_response_json` 对非 JSON 响应抛 `FeishuResponseError`；对合法 JSON 返回 dict。
    - Test: `build_feishu_fields([{field_key,field_name,field_value,field_type_key,field_alias}, ...])` 完整保留全部 5 个键。
    - Test: `flatten_fields(...)` 仍产出 `{field_key: field_value}`（向后兼容）。
    - Test: `extract_prd_url` 从含 alias `prd_url`（值为 docx 链接）的 feishu_fields 取出链接；`extract_select_label` 从 `{"label":"学习A","value":...}` 取 "学习A"。
    - Test: `rich_text_to_markdown` 对 paragraph + bold mark 行为与原 `_parse_rich_text` 等价。
  </behavior>
  <action>
    新建 `server/services/feishu_parsing.py`。先实现：`FeishuResponseError`；`safe_response_json` / `strict_response_json`（用 `httpx.Response`；content-type 不含 `json` 或 `response.json()` 抛 `json.JSONDecodeError`/`ValueError` 时按 fail-soft / 硬失败二分处理；warning/异常消息只放 `response.text[:200]` 截断片段，绝不放 token/header）；把 `services/feishu.py` 的 `_parse_rich_text`+`_parse_paragraph` **逐行等价**上移为模块函数 `rich_text_to_markdown` + 内部 `_paragraph_to_text`；`build_feishu_fields` / `flatten_fields` / `find_field` / `extract_select_label` / `extract_related_ids` / `extract_prd_url` / `extract_tech_doc_url`（prd/tech key 从 `feishu.models.KeyFields` 引用，禁硬编码重复）。用 `structlog.get_logger(__name__)`。不在本 task 写 RelationSpec/derive/parse_comments（Task 2）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/test_feishu_parsing.py -x -q -k "json or fields or rich or prd or select"</automated>
  </verify>
  <done>防御解析三函数 + 富文本上移 + 字段保留/提取 helper 实现并通过对应单测；非 JSON 输入永不抛未捕获异常（fail-soft 路径）。</done>
  <acceptance_criteria>
    - 非 JSON 响应经 `safe_response_json` 返回 None + warning，绝不冒泡异常。
    - `build_feishu_fields` 保留 field_name/field_type_key/field_alias（断言 5 键齐全）。
    - prd_url/select label 提取断言通过，key 常量取自 `KeyFields`。
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RelationSpec + derive_relations_from_fields + parse_comments</name>
  <read_first>
    - server/services/feishu.py（`get_comments` 评论解析 L314-323、`get_work_item_relations` L227-238 现状形状）
    - .planning/DOMAIN-MODEL.md §12.3（WorkItemRelation 字段 L453-464）、§16（field_caadeb=[7010938167]、planning_sprint=[6290075691] L573-575）
    - 本 plan Task 1 已产出的 `rich_text_to_markdown`（parse_comments 复用）
  </read_first>
  <behavior>
    - Test: `derive_relations_from_fields` 输入含 `field_caadeb`(type=work_item_related_multi_select, value=[7010938167]) → 产出 1 条 `RelationSpec(relation_type="belongs_to_project", source_field_key="field_caadeb", target_external_id=7010938167, origin="feishu_field")`。
    - Test: `planning_sprint=[6290075691]` → `relation_type="sprint"`；未知关联字段 → `relation_type="related"`；空 `[]` → 不产出关系。
    - Test: 非关联类型字段（如 select/multi_text）被忽略，不产出关系。
    - Test: `parse_comments` 对齐 comment/list 形状，逐条取 id/content/created_at/author/thread_parent_id；`data=None` 或缺 `comments` 键 → `[]`。
  </behavior>
  <action>
    在 `feishu_parsing.py` 续写：`RELATION_TYPE_BY_FIELD` 映射、`RELATION_FIELD_TYPE_KEY` 常量、`@dataclass RelationSpec`、`derive_relations_from_fields`（仅遍历 `field.get("field_type_key") == RELATION_FIELD_TYPE_KEY` 的项，对 `extract_related_ids(field_value)` 的每个 id 产一条 spec；relation_type 走映射、缺省 `related`；origin 固定 `"feishu_field"`，对齐 DOMAIN §12.3）；`parse_comments(data)` 容错读取 `data["data"]["comments"]`（兼容现状 `data.get("data",{}).get("comments",[])`），content 经 `rich_text_to_markdown`，author 取 `author.name` 缺省 "Unknown"，thread_parent_id 取评论父 id 字段（无则空串）。纯函数：无网络、无 DB、无 Django 依赖。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/test_feishu_parsing.py -x -q -k "relation or comment"</automated>
  </verify>
  <done>关系派生与评论解析纯函数实现，覆盖 belongs_to_project/sprint/version/related 与 fail-soft 评论形状。</done>
  <acceptance_criteria>
    - story `field_caadeb=[7010938167]` 正确派生 `belongs_to_project`（DOMAIN §16 实测）。
    - 每条 RelationSpec 带 relation_type/source_field_key/target_external_id/origin="feishu_field"。
    - `parse_comments(None)` 与缺键输入返回 `[]`，不抛异常。
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: helper 单测补全（DOMAIN §16 实测 fixture）</name>
  <read_first>
    - server/tests/services/（既有测试目录与 conftest 约定）
    - server/tests/test_provider_health.py（respx mock 范式参考）
    - .planning/DOMAIN-MODEL.md §16（story 7010225564 / issue 5580252273 字段样例）
  </read_first>
  <action>
    在 `server/tests/services/test_feishu_parsing.py` 补全/整理 Task 1+2 行为对应的单测，构造贴近 DOMAIN §16 的 fixture：含 `field_bcff9b`(alias prd_url)、`field_528f19`(select `{label:"学习A"}`)、`field_caadeb`([7010938167])、`planning_sprint`([6290075691])、`planning_version`([]) 的 fields[] 数组；伪造非 JSON httpx.Response（用 `httpx.Response(200, text="Extra data...")` 或 mock）。纯函数测试无需网络；如需 Response 对象用 `httpx.Response` 直接构造，避免发真实请求（pytest-socket 隔离）。覆盖 CONTEXT 测试策略 ②③④⑤ 的 helper 层部分。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/test_feishu_parsing.py -x -q</automated>
  </verify>
  <done>整文件测试全绿，覆盖全部导出函数；无真实网络调用。</done>
  <acceptance_criteria>
    - `uv run pytest tests/services/test_feishu_parsing.py` 全通过。
    - fixture 取自 DOMAIN §16 实测值（field_caadeb=[7010938167] 等）。
    - 测试不触发 pytest-socket 网络拦截（纯函数 + 本地构造 Response）。
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 飞书 API 响应 → 解析层 | 不可信外部输入（可能非 JSON / 形状漂移 / 超大 body）跨界进入解析 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01 | Denial of Service | `safe_response_json`/`strict_response_json` | mitigate | try/except 包裹 `.json()` + content-type 校验，非 JSON fail-soft 返回 None 或抛带上下文异常，绝不让解析崩进调用栈 |
| T-27-02 | Information Disclosure | 异常/日志消息 | mitigate | 错误信息只放 `response.text[:200]` 截断片段，禁含 X-PLUGIN-TOKEN/X-USER-KEY/plugin_secret |
| T-27-03 | Tampering | `derive_relations_from_fields`/`parse_comments` | accept | 纯函数对畸形字段值做归一（非 list/dict 跳过），无副作用；输入污染最坏只产出空结果 |
</threat_model>

<verification>
- `cd server && uv run pytest tests/services/test_feishu_parsing.py -q` 全绿。
- `cd server && uv run ruff format --check services/feishu_parsing.py tests/services/test_feishu_parsing.py`（line 100）。
- 无新增第三方依赖（httpx/respx/pytest-asyncio 均已在栈内）。
</verification>

<success_criteria>
- `server/services/feishu_parsing.py` 存在并导出 artifacts 清单全部符号。
- 防御解析对非 JSON 输入永不抛未捕获异常（fail-soft）或抛明确 `FeishuResponseError`（strict）。
- `derive_relations_from_fields` 从实测字段正确派生关系；`build_feishu_fields` 保元数据；`parse_comments` 容错。
- 全部单测通过，未发真实网络。
</success_criteria>

<output>
Create `.planning/phases/27-feishu-api-fixes/27-01-SUMMARY.md` when done.
</output>
