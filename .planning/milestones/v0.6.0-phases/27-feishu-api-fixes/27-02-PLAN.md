---
phase: 27-feishu-api-fixes
plan: 02
type: execute
wave: 2
depends_on: ["27-01"]
files_modified:
  - server/services/feishu.py
  - server/tests/services/test_feishu_service.py
autonomous: true
requirements: [FIX-01, FIX-02, FIX-03, FIX-04]
must_haves:
  truths:
    - "调用 get_work_item / get_comments 不传 work_item_type 时 fail-loud（TypeError），不再静默落 story"
    - "WorkItemInfo 新增 feishu_fields 完整对象数组，旧 fields 拍平 dict 仍保留（向后兼容）"
    - "get_comments 遇非 JSON 响应 fail-soft 返回 [] 并 warning，正常响应正确解析"
    - "get_work_item_relations 遇非 JSON（Extra data）降级返回 [] 并 warning，绝不抛断"
  artifacts:
    - path: "server/services/feishu.py"
      provides: "canonical FeishuClient 修复（FIX-01/02/03/04），调用共享 helper"
      contains: "feishu_fields"
    - path: "server/tests/services/test_feishu_service.py"
      provides: "canonical client respx 单测"
      contains: "respx"
  key_links:
    - from: "server/services/feishu.py"
      to: "server/services/feishu_parsing.py"
      via: "import 共享 helper"
      pattern: "from services.feishu_parsing import"
    - from: "server/services/feishu.py get_work_item"
      to: "WorkItemInfo.feishu_fields"
      via: "build_feishu_fields(item.fields)"
      pattern: "build_feishu_fields"
---

<objective>
把 Plan 27-01 的共享 helper 接入 **canonical** client `server/services/feishu.py`，落地 4 个修复：FIX-01（work_item_type 必填、fail-loud）、FIX-04（WorkItemInfo.feishu_fields 完整对象 + 保留旧 fields）、FIX-03（get_comments 防御解析 fail-soft）、FIX-02（get_work_item_relations 端点降级为可选 fail-soft）。

公开方法签名**严格向后兼容**：仅移除 `work_item_type` 的 `= "story"` 默认（变必填关键字参数，不重排参数）、仅**新增** `WorkItemInfo.feishu_fields` 属性（带默认值不破坏既有构造），不动任何调用方。

Purpose: 为 Phase 28 `WorkItemService.upsert`（DOMAIN §13.1 步骤 2/4）与 `knowledge/sources/feishu_work_item.py` normalizer 提供修对的回源。
Output: 改 1 个 client 文件 + 1 个新测试文件。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/27-feishu-api-fixes/27-CONTEXT.md
@.planning/DOMAIN-MODEL.md
@server/services/feishu.py
@server/services/feishu_parsing.py
@server/knowledge/sources/feishu_work_item.py
</context>

<artifacts_produced>
## Artifacts this phase produces（本 plan 部分）

- `WorkItemInfo` 新增字段：`feishu_fields: list[dict] = field(default_factory=list)`（保留完整 `fields[]` 对象；旧 `fields: dict` 不变，向后兼容）。
- `FeishuClient.get_work_item(project_key, work_item_id, work_item_type, fields=None)` — `work_item_type` 变**必填**（移除 `= "story"`），`.json()` 走 `strict_response_json`，字段经 `build_feishu_fields` + `flatten_fields` 双写，status 仍取 `work_item_status.state_key`。
- `FeishuClient.get_comments(project_key, work_item_id, work_item_type, limit=50)` — `work_item_type` 变**必填**；`.json()` 走 `safe_response_json` fail-soft；解析走 `parse_comments`；非 JSON / err_code≠0 → `[]` + warning。
- `FeishuClient.get_work_item_relations(...)` — 降级为可选：`.json()` 走 `safe_response_json`，非 JSON / err_code≠0 → `[]` + warning（保留方法签名，返回项可标注 `origin="feishu_relation_api"`，主路径不依赖它）。
- `FeishuClient._parse_rich_text` — 改为薄封装委托 `rich_text_to_markdown`（保留私有方法名，避免触达其它内部引用）。
- 新测试文件 `server/tests/services/test_feishu_service.py`。
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FIX-01 必填 type + FIX-04 feishu_fields + 硬路径防御解析</name>
  <read_first>
    - server/services/feishu.py（`WorkItemInfo` L13-24、`get_work_item` L104-186、`_parse_rich_text` L444-523）
    - server/services/feishu_parsing.py（`strict_response_json`/`build_feishu_fields`/`flatten_fields`/`rich_text_to_markdown` 签名）
    - 调用方（确认已显式传 type，签名变更安全）：server/knowledge/sources/feishu_work_item.py L149、server/mcp_tools/work_item_context_service.py L196、server/agents/tools/work_item_tools.py L84、server/workflows/nodes/integrations/feishu_workitem.py（注意此处 import 的是 feishu.client，本 plan 不动它）
  </read_first>
  <behavior>
    - Test: `get_work_item(project_key, id)` 不传 `work_item_type` → 抛 `TypeError`（必填，fail-loud，PF-09）。
    - Test: respx mock 返回含完整 fields[] 的工作项 → `info.feishu_fields` 为完整对象数组（含 field_name/type/alias），`info.fields` 仍是拍平 dict。
    - Test: `info.work_item_type` 等于传入的 `issue`（按真实 type 取数，不默认 story）。
    - Test: get_work_item 收到非 JSON 响应 → 抛 `FeishuResponseError`（硬取数路径 fail-loud）。
  </behavior>
  <action>
    给 `WorkItemInfo` 增 `feishu_fields: list[dict] = field(default_factory=list)`（import `field`）。`get_work_item` 签名把 `work_item_type: str = "story"` 改为 `work_item_type: str`（必填、保持关键字位置不变）；`data = response.json()` 改为 `data = strict_response_json(response, log_event="feishu_get_work_item_parse_failed", project_key=project_key, work_item_id=work_item_id)`；err_code≠0 / 空 items 仍抛带上下文异常（保留现有语义）；字段解析改为 `feishu_fields = build_feishu_fields(item.get("fields", []))` 与 `fields_dict = flatten_fields(item.get("fields", []))` 双写，description 仍从 `fields_dict["description"]` 经 `rich_text_to_markdown` 解析；构造 WorkItemInfo 时带上 `feishu_fields=feishu_fields`。`_parse_rich_text` 改为 `return rich_text_to_markdown(rich_text)`（删除重复的 `_parse_paragraph` 或保留委托均可，二选一不破坏行为）。`get_plugin_token` 的 `.json()` 也换 `strict_response_json`（token 路径硬失败）。导入：`from services.feishu_parsing import (...)`。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/test_feishu_service.py -x -q -k "type or fields or feishu_fields or parse"</automated>
  </verify>
  <done>work_item_type 必填且 fail-loud；feishu_fields 完整保留且 fields 拍平向后兼容；硬路径非 JSON 抛 FeishuResponseError。</done>
  <acceptance_criteria>
    - 不传 type 调用 get_work_item 抛 TypeError（无静默 story）。
    - `info.feishu_fields` 含 field_name/field_type_key/field_alias；`info.fields` 仍为 `{field_key: field_value}`。
    - get_work_item 非 JSON 响应抛 FeishuResponseError，异常消息不含凭证。
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FIX-03 get_comments + FIX-02 relations 端点降级（fail-soft）</name>
  <read_first>
    - server/services/feishu.py（`get_comments` L278-325、`get_work_item_relations` L188-238）
    - server/services/feishu_parsing.py（`safe_response_json`/`parse_comments`）
    - .planning/phases/27-feishu-api-fixes/27-CONTEXT.md（FIX-03/FIX-02 决策、fail-soft 范式 L46-54）
  </read_first>
  <behavior>
    - Test: get_comments 收到 `text="Extra data: line 1 column 5"`（非 JSON）→ 返回 `[]` 且记 warning，不抛。
    - Test: get_comments 收到合法 comment/list 响应 → 正确解析每条 id/content/created_at/author/thread_parent_id。
    - Test: get_comments 不传 work_item_type → `TypeError`（与 get_work_item 一致，FIX-01）。
    - Test: get_work_item_relations 收到非 JSON（Extra data）→ 返回 `[]` + warning，绝不抛（PF-10 降级为可选）。
  </behavior>
  <action>
    `get_comments` 签名同样移除 type 默认（必填）；`data = response.json()` 改 `data = safe_response_json(response, log_event="feishu_get_comments_parse_failed", project_key=project_key, work_item_id=work_item_id)`；`data is None` → 返回 `[]`；err_code≠0 → `[]`（保留）；解析改用 `parse_comments(data)`。`get_work_item_relations` 同样把 `.json()` 换 `safe_response_json(...log_event="feishu_get_relations_parse_failed"...)`，`data is None` → `[]` + 已记 warning；保留方法签名与 err_code≠0 → `[]` 的降级；解析出的关系项标注 `origin="feishu_relation_api"`（主路径不依赖此端点，关系主路径走 Plan 27-01 `derive_relations_from_fields`）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/test_feishu_service.py -x -q -k "comment or relation"</automated>
  </verify>
  <done>get_comments 防御解析 fail-soft + 正常解析正确；relations 端点非 JSON 降级返回 [] 不崩；两方法 type 必填。</done>
  <acceptance_criteria>
    - get_comments 非 JSON → `[]` + warning，永不抛。
    - get_comments 正常响应逐条字段解析正确。
    - get_work_item_relations 遇 `Extra data` 非 JSON 返回 `[]`，不抛（PF-10 实测场景）。
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: canonical client respx 单测 + 回归</name>
  <read_first>
    - server/tests/test_provider_health.py（@respx.mock + httpx mock 范式）
    - server/tests/services/（conftest / 既有 services 测试约定）
    - .planning/DOMAIN-MODEL.md §16（issue 5580252273 / story 7010225564 字段样例做 fixture）
  </read_first>
  <action>
    新建 `server/tests/services/test_feishu_service.py`，用 `@respx.mock` + `pytest.mark.asyncio` mock `https://project.feishu.cn` 的 plugin_token / work_item query / comment list / relation 端点。覆盖 Task 1+2 全部 behavior，外加：构造 client 时直接传 plugin_id/secret/project_key/user_key（绕过 DB 工厂），先 mock token 端点再 mock 业务端点。fixture 字段取 DOMAIN §16 形状（含 field_bcff9b/field_528f19/field_caadeb）。确保 pytest-socket 下不发真实网络（respx 拦截）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/test_feishu_service.py -q && uv run ruff format --check services/feishu.py</automated>
  </verify>
  <done>canonical client 测试全绿，4 修复均有断言覆盖；ruff 格式通过。</done>
  <acceptance_criteria>
    - `uv run pytest tests/services/test_feishu_service.py` 全通过。
    - 所有 HTTP 经 respx mock，无真实网络。
    - FIX-01/02/03/04 各至少一条断言。
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 飞书 API 响应 → canonical FeishuClient | 外部不可信响应（非 JSON / 漂移 / err_code）进入取数与解析 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-04 | Denial of Service | get_comments / get_work_item_relations | mitigate | 经 `safe_response_json` fail-soft 返回 [] + warning，单端点失效不拖垮上层摄取 |
| T-27-05 | Tampering | get_work_item 硬路径 | mitigate | `strict_response_json` + err_code 校验 + 空 items 校验，畸形/取空数据 fail-loud 抛带上下文异常，不静默落错值 |
| T-27-06 | Information Disclosure | 异常/日志 | mitigate | 复用 helper 的 body 截断，X-PLUGIN-TOKEN/X-USER-KEY 不入日志 |
</threat_model>

<verification>
- `cd server && uv run pytest tests/services/test_feishu_service.py -q` 全绿。
- 快速回归相关下游导入未破坏：`cd server && uv run pytest tests/knowledge/test_triggers.py tests/mcp_tools/test_feishu_work_item_context.py -q`（确认签名变更未破坏既有 mock 调用）。
- `cd server && uv run ruff format --check services/feishu.py tests/services/test_feishu_service.py`。
</verification>

<success_criteria>
- canonical `FeishuClient` 四项修复落地且向后兼容（仅去默认 + 加可选属性）。
- 不传 type fail-loud；feishu_fields 完整；get_comments / relations 端点 fail-soft。
- 单测 + 下游回归全绿。
</success_criteria>

<output>
Create `.planning/phases/27-feishu-api-fixes/27-02-SUMMARY.md` when done.
</output>
