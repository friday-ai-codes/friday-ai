---
phase: 27-feishu-api-fixes
plan: 03
type: execute
wave: 2
depends_on: ["27-01"]
files_modified:
  - server/feishu/client.py
  - server/tests/test_feishu_api_client.py
autonomous: true
requirements: [FIX-01, FIX-03, FIX-04]
must_haves:
  truths:
    - "feishu/client.py 的 get_work_item / get_comments 不传 work_item_type 时 fail-loud（TypeError）"
    - "feishu/client.py 的 WorkItemInfo 同样新增 feishu_fields 完整对象，旧 fields 拍平保留"
    - "feishu/client.py 的 get_comments 遇非 JSON 响应 fail-soft 返回 [] 并 warning"
    - "两份 client 的解析行为一致（同源共享 helper，无漂移）"
  artifacts:
    - path: "server/feishu/client.py"
      provides: "near-dup FeishuClient 修复（FIX-01/03/04），调用同一共享 helper"
      contains: "feishu_fields"
    - path: "server/tests/test_feishu_api_client.py"
      provides: "feishu.client respx 单测"
      contains: "respx"
  key_links:
    - from: "server/feishu/client.py"
      to: "server/services/feishu_parsing.py"
      via: "import 共享 helper（与 services/feishu.py 同源）"
      pattern: "from services.feishu_parsing import"
---

<objective>
把 Plan 27-01 的共享 helper 接入**近重复** client `server/feishu/client.py`，与 Plan 27-02 对 canonical client 的修复**保持完全一致的解析行为**（同源 helper 消除漂移）。本 client 无独立 relation 端点（无 `get_work_item_relations`），故落地 FIX-01（type 必填 fail-loud）、FIX-04（feishu_fields 完整对象）、FIX-03（get_comments 防御解析 fail-soft）三项。

公开方法签名严格向后兼容：仅移除 `work_item_type` 默认、仅新增 `WorkItemInfo.feishu_fields`，不动调用方（`feishu/views.py`、`workflows/services/mr_service.py`、`workflows/nodes/integrations/feishu_workitem.py`、`workflows/nodes/control/wait_feishu.py` 等均已显式传 type）。

Purpose: 两份 client 同步修复，避免 webhook/workflow 路径（走 feishu.client）与 normalizer/mcp 路径（走 services.feishu）解析不一致。
Output: 改 1 个 client 文件 + 1 个新测试文件。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/27-feishu-api-fixes/27-CONTEXT.md
@server/feishu/client.py
@server/services/feishu_parsing.py
</context>

<artifacts_produced>
## Artifacts this phase produces（本 plan 部分）

- `feishu.client.WorkItemInfo` 新增 `feishu_fields: list[dict] = field(default_factory=list)`（旧 `fields` 拍平不变）。
- `feishu.client.FeishuClient.get_work_item(...)` — `work_item_type` 变必填；`.json()` 走 `strict_response_json`；字段经 `build_feishu_fields` + `flatten_fields` 双写。
- `feishu.client.FeishuClient.get_comments(...)` — `work_item_type` 变必填；`.json()` 走 `safe_response_json` fail-soft；解析走 `parse_comments`。
- `feishu.client.FeishuClient._parse_rich_text` — 薄封装委托 `rich_text_to_markdown`。
- `feishu.client.FeishuClient.get_plugin_token` — `.json()` 走 `strict_response_json`。
- 新测试文件 `server/tests/test_feishu_api_client.py`。
- （可选，非破坏）`feishu/client.py` 顶部加注释，指明取数/解析以 `services/feishu.py` + `services/feishu_parsing.py` 为 canonical，本文件为兼容副本。
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: feishu/client.py 接入 helper（FIX-01 / FIX-04 / FIX-03 + 防御解析）</name>
  <read_first>
    - server/feishu/client.py（`WorkItemInfo` L19-30、`get_work_item` L110-192、`get_comments` L232-279、`_parse_rich_text` L450-529、`get_plugin_token` L70-108）
    - server/services/feishu_parsing.py（全部导出函数签名）
    - server/services/feishu.py（Plan 27-02 已落地的等价写法，作为一致性参照——保持两份行为对齐）
    - 调用方确认已显式传 type：server/feishu/views.py L747/L1002、server/workflows/nodes/integrations/feishu_workitem.py L183、server/workflows/nodes/control/wait_feishu.py L186、server/services/feishu_im.py L802、server/workflows/services/mr_service.py
  </read_first>
  <behavior>
    - Test: `get_work_item(project_key, id)` 不传 work_item_type → `TypeError`（FIX-01 fail-loud）。
    - Test: respx 返回含完整 fields[] → `info.feishu_fields` 完整对象数组，`info.fields` 拍平 dict（FIX-04 + 向后兼容）。
    - Test: `info.work_item_type` == 传入的真实 type（不默认 story）。
    - Test: get_work_item 非 JSON → 抛 `FeishuResponseError`（硬路径）。
    - Test: get_comments 非 JSON → 返回 `[]` + warning，不抛（FIX-03 fail-soft）；正常响应正确解析 id/content/created_at/author/thread_parent_id。
    - Test: get_comments 不传 type → `TypeError`。
  </behavior>
  <action>
    在 `feishu/client.py` 顶部 `from services.feishu_parsing import (FeishuResponseError, build_feishu_fields, flatten_fields, parse_comments, rich_text_to_markdown, safe_response_json, strict_response_json)`。给 `WorkItemInfo` 增 `feishu_fields: list[dict] = field(default_factory=list)`（import `field`）。`get_work_item`：移除 `work_item_type` 默认、`.json()` → `strict_response_json(log_event="feishu_get_work_item_parse_failed", ...)`、字段双写 `build_feishu_fields`+`flatten_fields`、构造带 `feishu_fields=...`、description 经 `rich_text_to_markdown`、保留 err_code≠0 / 空 items 抛异常语义。`get_comments`：移除 type 默认、`.json()` → `safe_response_json(...)`、`data is None` → `[]`、解析用 `parse_comments(data)`。`get_plugin_token` `.json()` → `strict_response_json`。`_parse_rich_text` → `return rich_text_to_markdown(rich_text)`。`add_comment` / `update_field` / `transition_status` / `test_connection` 这些写/探查方法**本 plan 不改行为**（可顺手把其 `.json()` 换 `safe_response_json` 但非必须，避免越界；若改需保持原返回语义）。`FeishuAPIError` 保留不动。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/test_feishu_api_client.py -x -q -k "type or fields or comment or parse"</automated>
  </verify>
  <done>feishu/client.py 三项修复落地，行为与 canonical client 一致；签名向后兼容。</done>
  <acceptance_criteria>
    - 不传 type 调用 → TypeError（无静默 story）。
    - feishu_fields 完整 + fields 拍平保留。
    - get_work_item 非 JSON 抛 FeishuResponseError；get_comments 非 JSON 返回 []。
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: feishu.client respx 单测 + 一致性回归</name>
  <read_first>
    - server/tests/test_provider_health.py（@respx.mock 范式）
    - server/tests/e2e/fixtures/mock_services.py（既有 feishu.client mock 约定 L87-120）
    - server/tests/test_feishu.py（feishu app 既有测试，避免命名/fixture 冲突——本测试文件独立命名 test_feishu_api_client.py）
  </read_first>
  <action>
    新建 `server/tests/test_feishu_api_client.py`，`from feishu.client import FeishuClient, WorkItemInfo` 起测，用 `@respx.mock` + `pytest.mark.asyncio` mock token + work_item query + comment list 端点，覆盖 Task 1 全部 behavior。fixture 字段形状取 DOMAIN §16（含 field_000001/field_000002/field_000008）。断言与 Plan 27-02 的 services client 测试**结果一致**（同输入同输出），佐证两份 client 无漂移。respx 拦截确保 pytest-socket 下无真实网络。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/test_feishu_api_client.py -q && uv run ruff format --check feishu/client.py</automated>
  </verify>
  <done>feishu.client 测试全绿，与 canonical client 行为一致；ruff 通过。</done>
  <acceptance_criteria>
    - `uv run pytest tests/test_feishu_api_client.py` 全通过。
    - 所有 HTTP 经 respx mock，无真实网络。
    - FIX-01/03/04 各至少一条断言；同输入下输出与 services client 一致。
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 飞书 API 响应 → feishu.client FeishuClient | webhook/workflow 路径使用的副本 client 同样面对非 JSON / 漂移响应 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-07 | Denial of Service | feishu.client.get_comments | mitigate | 经 `safe_response_json` fail-soft 返回 [] + warning，webhook 摄取不因评论端点失效崩断 |
| T-27-08 | Tampering | feishu.client.get_work_item | mitigate | `strict_response_json` + err_code/空 items 校验，fail-loud 不静默落错 type 数据 |
| T-27-09 | Repudiation | 两份 client 解析漂移 | mitigate | 强制共享同一 helper，单测断言同输入同输出，消除路径间行为不一致 |
</threat_model>

<verification>
- `cd server && uv run pytest tests/test_feishu_api_client.py -q` 全绿。
- 回归 webhook/workflow 路径既有测试未破坏：`cd server && uv run pytest tests/test_feishu.py tests/workflows/test_nodes.py -q`（确认签名变更未破坏既有调用/mock）。
- `cd server && uv run ruff format --check feishu/client.py tests/test_feishu_api_client.py`。
</verification>

<success_criteria>
- near-dup `FeishuClient`（feishu/client.py）三项修复落地且与 canonical client 行为一致。
- 不传 type fail-loud；feishu_fields 完整；get_comments fail-soft。
- 单测 + webhook/workflow 回归全绿。
</success_criteria>

<output>
Create `.planning/phases/27-feishu-api-fixes/27-03-SUMMARY.md` when done.
</output>
