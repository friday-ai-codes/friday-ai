---
phase: 30-document-references
plan: 02
type: execute
wave: 2
depends_on: ["30-01"]
files_modified:
  - server/delivery/services/document_service.py
  - server/delivery/services/__init__.py
  - server/tests/delivery/test_document_service.py
  - server/tests/delivery/test_document_inv6_guard.py
autonomous: true
requirements: [DOC-01]
must_haves:
  truths:
    - "Document/DocumentVersion 落库经 DocumentService 单一写入入口（INV-6），禁旁路写表（grep 守护）"
    - "external_feishu 文档按 (feishu_tenant, external_ref) 唯一定位/去重（同 token 重摄收敛同一 Document）"
    - "内容 content_hash 相等不产生新 DocumentVersion（复用 knowledge hash 范式）；不等则建新版本 + supersedes 链 + 推进 current_version"
    - "外部飞书文档落 content_storage=both（快照 + canonical_url 引用），feishu_tenant 由 doc URL host 派生"
    - "文档摄取成功时记 WorkItemSyncState(prd_body|tech_doc facet=complete)，缺正文则 missing（对齐 §1.4 facet）"
  artifacts:
    - path: "server/delivery/services/document_service.py"
      provides: "DocumentService.upsert_from_feishu 单一写入入口 + feishu_tenant 派生 helper + content_hash"
      contains: "class DocumentService"
    - path: "server/delivery/services/__init__.py"
      provides: "re-export DocumentService"
      contains: "DocumentService"
    - path: "server/tests/delivery/test_document_inv6_guard.py"
      provides: "Document 旁路写表 grep 守护（唯一 writer = document_service.py）"
      contains: "document_service.py"
  key_links:
    - from: "server/delivery/services/document_service.py"
      to: "delivery.Document / delivery.DocumentVersion"
      via: "唯一写入收口（get_or_create + 版本翻转 + current_version 推进）"
      pattern: "Document\\.objects|DocumentVersion\\.objects"
    - from: "server/delivery/services/document_service.py"
      to: "delivery.WorkItemSyncState(facet=prd_body|tech_doc)"
      via: "摄取成功记 facet 完整度（复用 28-02 update_or_create 范式）"
      pattern: "SyncFacet.PRD_BODY|SyncFacet.TECH_DOC"
---

<objective>
实现 Document/DocumentVersion 的服务层唯一写入入口 `DocumentService.upsert_from_feishu`（INV-6）：external_feishu 文档按 `(feishu_tenant, external_ref=doc_token)` 去重定位，内容 `content_hash` 相等不翻版本、不等则建新 `DocumentVersion` + supersedes 链并推进 `current_version`，落 `content_storage=both`，`feishu_tenant` 由 doc URL host 派生；文档摄取成功时按 `document_type` 映射记 `WorkItemSyncState(prd_body|tech_doc)` facet 完整度。配 Document 旁路写表 INV-6 grep 守护。

Purpose: DOC-01 的写入收口——把"区分外部/内部 + PRD/技术方案落独立实体"的落库统一收敛到单一 service（守 INV-6），供 30-03 normalizer 调用；内容去重/版本范式复用 knowledge"hash 相等不翻版本"。
Output: `DocumentService`、`feishu_tenant` 派生 helper、re-export、service 守护测试、Document INV-6 grep 守护测试。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-document-references/30-CONTEXT.md
@.planning/DOMAIN-MODEL.md
@server/delivery/services/work_item_service.py
@server/delivery/services/comment_event_service.py
@server/delivery/services/__init__.py
@server/delivery/models/document.py
@server/delivery/models/sync_state.py
@server/tests/delivery/test_inv6_guard.py
@.planning/phases/30-document-references/30-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DocumentService.upsert_from_feishu 单一写入入口 + 去重 + 版本范式 + facet</name>
  <files>server/delivery/services/document_service.py, server/delivery/services/__init__.py</files>
  <behavior>
    - 首次摄取某 (feishu_tenant, external_ref) → 建 Document(source_kind=external_feishu, content_storage=both) + DocumentVersion(version=1) + current_version 指向 v1。
    - 同 (feishu_tenant, external_ref) 再摄、content_hash 相等 → 复用 Document，不建新 DocumentVersion（版本数不变）。
    - 同 Document content 变化（hash 不等）→ 建 version=2、supersedes=v1、推进 current_version=v2。
    - document_type=prd 摄取成功 → WorkItemSyncState(work_item, facet=prd_body, status=complete)；tech_plan → facet=tech_doc complete。
    - content 为空（doc 拉取失败降级）→ 仍 upsert Document/Version（缺正文），facet 记 missing，不抛。
    - work_item=None（脊柱未落库）→ Document.work_item 置空，跳过 facet 记录，不抛。
  </behavior>
  <read_first>
    - server/delivery/services/work_item_service.py（async service 范式：sync_to_async 桥接 ORM、transaction.atomic + select_for_update、_record_sync_state(facet) update_or_create、_safe_error/_redact_secrets 脱敏、_MIRROR_FIELDS update_fields 白名单写法、中文 docstring/structlog）
    - server/delivery/services/comment_event_service.py（独立 service 复用 work_item_service 脱敏思路、get_or_create 幂等收口范式）
    - server/knowledge/ingestion.py _persist_sync（hash 相等三态：相等不翻版本、不等翻版本 supersedes 链；content_hash=hashlib.sha256(content.encode("utf-8")).hexdigest() 单一算法——DocumentService 复用此 hash 写法，但不 import knowledge.models，守 INV-3）
    - server/delivery/models/document.py（Document/DocumentVersion 字段 + DocumentType/DocumentSourceKind/ContentStorage 枚举）
    - server/delivery/models/sync_state.py（SyncFacet.PRD_BODY/TECH_DOC/SyncStatus、WorkItemSyncState.update_or_create unique(work_item, facet)）
    - .planning/phases/30-document-references/30-CONTEXT.md Grey Area 1/4（去重键 (feishu_tenant, external_ref)、内容 hash 不翻版本、单一写入入口、facet 对齐 §1.4）
  </read_first>
  <action>
    新建 server/delivery/services/document_service.py，async-first，ORM 经 sync_to_async（沿用 work_item_service.py 范式）。**这是 Document/DocumentVersion 落库的唯一写入收口（INV-6）**——禁任何其他模块直接写 Document/DocumentVersion 表。

    纯函数 helper：
      - `_content_hash(text: str) -> str`：`hashlib.sha256(text.encode("utf-8")).hexdigest()`（与 knowledge 同算法，单一来源；不 import knowledge）。
      - `derive_feishu_tenant(canonical_url: str) -> str`：从 doc URL host 派生租户 slug（`<tenant>.feishu.cn` / `<tenant>.larksuite.com` 取第一段子域，如 `acme.feishu.cn` → "acme"）；非飞书域或解析不出 → ""（用 urlparse(url).hostname，按 "." 切首段；对 `feishu.cn`/`www` 等无意义首段返回 ""）。per CONTEXT Claude's Discretion（取 doc URL host 派生为稳定区分租户来源）。

    `DocumentService`：
      - `async def upsert_from_feishu(self, *, work_item, document_type: str, doc_token: str, content: str, canonical_url: str, feishu_tenant: str = "", source: str = "manual") -> Document`：
          · external_feishu 文档去重定位键 = `(feishu_tenant or derive_feishu_tenant(canonical_url), external_ref=doc_token)`；
          · 经 sync_to_async 包裹的同步函数内 `transaction.atomic()` + `Document.objects.select_for_update().get_or_create(feishu_tenant=tenant, external_ref=doc_token, defaults={document_type, source_kind=external_feishu, content_storage=both, canonical_url, work_item})`（首建落 both + canonical_url + work_item）；
          · 已存在时刷新 mirror 类字段（canonical_url、work_item 若先前为 None 则补连、document_type 保持首建——doc_token 不变 type 不应漂移）、last_synced_at=now()；
          · 版本判定（复用 knowledge"hash 相等不翻版本"铁律）：取 current_version；新 content_hash = _content_hash(content)。
              - current_version 存在且 content_hash 相等 → **不建新版本**（return Document，仅刷 last_synced_at）；
              - 否则建 `DocumentVersion(document, version=(cur.version+1 if cur else 1), supersedes=cur, content=content, content_hash=hash)`，并把 Document.current_version 指向新版本、save（update_fields 含 current_version/last_synced_at/canonical_url/work_item）。
          · 缺正文降级：content == "" 时仍走上面流程（建空 content 版本，缺段不缺实体，CONTEXT 降级范式）——但 facet 记 missing（见下）。
        返回 Document 实例。
      - facet 记录（在 upsert_from_feishu 末尾，work_item 非空时）：把 document_type 映射 facet（prd→SyncFacet.PRD_BODY、tech_plan→SyncFacet.TECH_DOC；其余类型不记 facet）；content 非空 → status=complete，content 空（拉取失败降级）→ status=missing。经 `WorkItemSyncState.objects.update_or_create(work_item, facet, defaults={status, source, last_synced_at, error})`（sync_to_async，复用 28-02 _record_sync_state 范式；work_item=None 时跳过）。

    错误/日志脱敏：复用 work_item_service 的 `_redact_secrets`/`_safe_error` 思路（import 单一来源或本地小副本，按既有约定）；文档正文 content 属业务内容不脱敏。
    在 server/delivery/services/__init__.py re-export `DocumentService`（及 derive_feishu_tenant 若对外有用），更新 __all__。
    **不 import knowledge.models / 不写 KnowledgeEntity**（INV-3：knowledge 投影由 30-03 normalizer 经既有 ingestion 管线产出，DocumentService 只管操作态）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/delivery/test_document_service.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - 首摄建 Document(content_storage=both, source_kind=external_feishu) + DocumentVersion(version=1) + current_version=v1；feishu_tenant 由 URL 派生（acme.feishu.cn → acme）。
    - 去重：同 (feishu_tenant, doc_token) 再摄收敛同一 Document（不新增 Document 行）。
    - hash 相等不翻版本：同 content 再摄，DocumentVersion 计数不变；hash 不等 → version=2 + supersedes=v1 + current_version 推进。
    - facet：document_type=prd 且 content 非空 → SyncState(prd_body)=complete；content 空 → missing；tech_plan → tech_doc facet。
    - work_item=None 不抛，Document.work_item 为空，不记 facet。
  </acceptance_criteria>
  <done>DocumentService.upsert_from_feishu 单一写入入口就位：去重定位 + hash 不翻版本 + supersedes 链 + current_version 推进 + facet 记录，service 守护测试全绿。</done>
</task>

<task type="auto">
  <name>Task 2: Document INV-6 旁路写表 grep 守护测试</name>
  <files>server/tests/delivery/test_document_inv6_guard.py</files>
  <read_first>
    - server/tests/delivery/test_inv6_guard.py（既有 WorkItem/Comment INV-6 grep 守护范式：_iter_py_files 遍历 server/ 剪枝、精确锚定正则避免误伤更长符号、跳过 class 定义行、排除 tests//migrations//models/ 与 allowed writer、writer 有效性断言）
    - server/delivery/services/document_service.py（Task 1：唯一允许的 Document writer）
  </read_first>
  <action>
    新建 server/tests/delivery/test_document_inv6_guard.py，纯本地源码扫描（无 DB/网络），沿用 test_inv6_guard.py 的精确锚定范式，新增 Document 专用守护（不修改既有 test_inv6_guard.py，避免与 Phase 28 文件所有权冲突）：

    - 常量 `_ALLOWED_DOCUMENT_WRITER = "delivery/services/document_service.py"`。
    - 精确锚定正则（避免误伤 DocumentVersion/DocumentType/DocumentSerializer 等更长符号与读路径 .filter/.objects.get）：
        · `_RE_DOC_ORM_WRITE = re.compile(r"\bDocument\.objects\.(?:create|bulk_create|get_or_create|update_or_create)\b")`（紧跟 ".objects" 锚定 Document 类本体，非 DocumentVersion）；
        · `_RE_DOC_INSTANTIATE = re.compile(r"\bDocument\s*\(")`（"\s*\(" 紧跟天然排除 DocumentVersion(/DocumentType(/DocumentService(/DocumentSerializer( 等）；
        · `_RE_DOC_INSTANCE_SAVE = re.compile(r"\bDocument\([^)]*\)\.save\(")`；
        · 同款再为 `DocumentVersion` 加一组正则（_RE_DOCVER_ORM_WRITE / _RE_DOCVER_INSTANTIATE / _RE_DOCVER_INSTANCE_SAVE）——DocumentVersion 写入同样只允许在 document_service.py。
    - 扫描范围 helper `_is_scanned_for_document_inv6(rel)`：排除 `_ALLOWED_DOCUMENT_WRITER`、tests/、migrations/、delivery/models/（与既有范式一致）。
    - `test_inv6_no_bypass_document_write()`：遍历 server/ .py，跳过 `class Document`/`class DocumentVersion` 定义行，命中 Document 或 DocumentVersion 写表正则即记 violation（rel:lineno: line），断言无 violation，报错信息列出文件:行 + 指明落库只允许经 document_service.py。
    - `test_inv6_document_writer_module_actually_writes()`：断言 document_service.py 确实含 Document/DocumentVersion 写表（get_or_create + DocumentVersion(... 或 .objects.create），否则守护形同虚设。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/delivery/test_document_inv6_guard.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - 全 server/ 源码（除 document_service.py / tests/ / migrations/ / models/）无旁路 Document/DocumentVersion 写表，测试通过。
    - writer 有效性断言通过（document_service.py 确含 Document 写表）。
    - 正则不误伤 DocumentVersion(...)（在 Document 正则中）、DocumentService(、DocumentType.、Document.objects.filter/get 读路径。
  </acceptance_criteria>
  <done>Document/DocumentVersion 旁路写表 INV-6 grep 守护就位，唯一 writer 锚定 document_service.py，守护与有效性断言全绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 飞书文档正文 → service | content 为外部不可信内容（仅落快照，不执行） |
| service → delivery DB | Document/DocumentVersion 落库（单一写入入口 INV-6） |
| 错误信息 → SyncState.error / 日志 | 回源失败摘要可能夹带凭证（脱敏面） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-03 | Tampering | 旁路写 Document 表 | mitigate | DocumentService 单一写入收口 + test_document_inv6_guard.py grep 守护（精确锚定无误伤） |
| T-30-04 | DoS | 重复摄取放大版本表 | mitigate | (feishu_tenant, external_ref) 去重 + content_hash 相等不翻版本（service 测试守护） |
| T-30-05 | Information Disclosure | SyncState.error / 日志凭证泄漏 | mitigate | 回源失败摘要经 _safe_error/_redact_secrets 脱敏（复用 28-02 单一来源）；文档正文属业务内容不入 error 面 |
| T-30-SC | Tampering | 依赖供应链 | accept | 无新增 npm/pip 包（纯 Django service + stdlib hashlib/urllib），不触发包合法性门 |
</threat_model>

<verification>
- `uv run pytest tests/delivery/test_document_service.py tests/delivery/test_document_inv6_guard.py -q` 全绿；service 测试全程无真实网络（pytest-socket；DocumentService 不直接回源，content 由调用方传入）。
- `uv run pytest tests/delivery/ -q` 无回归（含既有 test_inv6_guard.py / 28 / 29 套件）。
- `uv run ruff format --check delivery/services/document_service.py tests/delivery/test_document_service.py tests/delivery/test_document_inv6_guard.py` + `ruff check delivery/ tests/delivery/` 干净。
- delivery app 未 import knowledge.models（INV-3，既有 test_inv6_guard.py test_inv3_delivery_does_not_write_knowledge_models 覆盖）；未新增第三方依赖。
</verification>

<success_criteria>
- Document/DocumentVersion 落库只经 DocumentService.upsert_from_feishu 单一入口（INV-6，grep 守护）。
- external_feishu 按 (feishu_tenant, external_ref) 去重；content_hash 相等不翻版本，不等翻版本 + supersedes + current_version 推进。
- 外部飞书文档落 content_storage=both，feishu_tenant 由 doc URL host 派生。
- 摄取成功记 prd_body/tech_doc facet=complete，缺正文 missing（对齐 §1.4）。
</success_criteria>

<artifacts_produced>
- `server/delivery/services/document_service.py` — `DocumentService.upsert_from_feishu` + `derive_feishu_tenant` + `_content_hash`
- `server/delivery/services/__init__.py` — 追加 re-export
- `server/tests/delivery/test_document_service.py` — 去重/版本/facet/降级守护测试
- `server/tests/delivery/test_document_inv6_guard.py` — Document/DocumentVersion 旁路写表 grep 守护
</artifacts_produced>

<output>
Create `.planning/phases/30-document-references/30-02-SUMMARY.md` when done
</output>
