---
phase: 30-document-references
plan: 04
type: execute
wave: 3
depends_on: ["30-02"]
files_modified:
  - server/delivery/api/views.py
  - server/delivery/api/serializers.py
  - server/delivery/urls.py
  - server/tests/delivery/test_document_api.py
autonomous: true
requirements: [DOC-02]
must_haves:
  truths:
    - "给定带 prd_url 的 WorkItem（三元组），可经 Document 实体检索到其 PRD 正文快照（DOC-02 成功标准 3）"
    - "查询路径：Document.objects.filter(work_item=..., document_type=prd) → current_version.content"
    - "最小只读 REST（IsAuthenticated），不旁路写表 / 不旁路 fetch，纯读已落库 Document"
    - "未命中 Document / 缺 current_version → 明确返回（404 / 空 content），不臆造、不泄漏存在性歧义"
  artifacts:
    - path: "server/delivery/api/views.py"
      provides: "WorkItemPrdDocumentView（按三元组只读检索 PRD 正文快照）"
      contains: "class WorkItemPrdDocumentView"
    - path: "server/delivery/api/serializers.py"
      provides: "DocumentSnapshotSerializer（只读：文档元数据 + current_version content）"
      contains: "DocumentSnapshotSerializer"
    - path: "server/delivery/urls.py"
      provides: "work-items/prd-document/ 路由"
      contains: "prd-document"
  key_links:
    - from: "server/delivery/api/views.py WorkItemPrdDocumentView"
      to: "delivery.Document.objects.filter(work_item, document_type=prd).current_version.content"
      via: "只读查询路径（不旁路 fetch / 不写表）"
      pattern: "document_type=DocumentType.PRD|document_type=\"prd\""
---

<objective>
提供最小只读 REST 端点：给定带 `prd_url` 的 WorkItem（三元组），经独立操作态 `Document` 实体检索到其 PRD 正文快照（`Document.objects.filter(work_item=..., document_type=prd)` → `current_version.content`）。IsAuthenticated，纯读已落库 Document（不旁路 fetch、不写表），未命中返回明确 404。

Purpose: DOC-02 成功标准 3——"给定一个带 prd_url 的 WorkItem，可经文档实体检索到其 PRD 正文快照"，兑现 Document 实体相对 work_item 内联正文的独立可检索价值。
Output: `WorkItemPrdDocumentView`、`DocumentSnapshotSerializer`、路由、检索守护测试。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-document-references/30-CONTEXT.md
@.planning/DOMAIN-MODEL.md
@server/delivery/api/views.py
@server/delivery/api/serializers.py
@server/delivery/urls.py
@server/delivery/models/document.py
@server/delivery/services/document_service.py
@server/tests/delivery/test_api.py
@.planning/phases/30-document-references/30-02-PLAN.md
---
本端点与 30-03 normalizer 解耦：仅依赖 30-01 模型 + 30-02 DocumentService（测试用 DocumentService 建 Document 夹具）。
</context>

<tasks>

<task type="auto">
  <name>Task 1: PRD 正文快照只读 REST 端点 + serializer + 路由</name>
  <files>server/delivery/api/views.py, server/delivery/api/serializers.py, server/delivery/urls.py</files>
  <read_first>
    - server/delivery/api/views.py（adrf APIView 范式：IsAuthenticated、async get、三元组 query params 校验（缺参 400 / work_item_id 非整数 400）、afirst 读已落库行（不旁路 fetch）、不存在 404、序列化经 sync_to_async 包裹的 WorkItemDetailView/WorkItemCommentTreeView 既有写法）
    - server/delivery/api/serializers.py（只读 serializer 范式：ModelSerializer 全字段 read_only / SerializerMethodField；CommentTreeNodeSerializer 透传 dict 节点写法）
    - server/delivery/urls.py（字面段路由顺序：更具体的字面前缀须排在通配 work-items/ 之前，沿用既有 path 顺序）
    - server/delivery/models/document.py（Document.work_item / document_type / current_version / DocumentType.PRD）
  </read_first>
  <action>
    在 server/delivery/api/serializers.py 新增 `DocumentSnapshotSerializer`（只读）：暴露 Document 元数据 + 当前版本正文快照——字段 `id / document_type / source_kind / content_storage / external_ref / canonical_url / feishu_tenant / last_synced_at` + `content`（SerializerMethodField，取 `obj.current_version.content`，current_version 为 None → ""）+ `version`（SerializerMethodField，取 `obj.current_version.version`，None → null）。全 read_only（落库只经 DocumentService，INV-6）。

    在 server/delivery/api/views.py 新增 `WorkItemPrdDocumentView(APIView)`（permission_classes=[IsAuthenticated]，async get）：
      - 入参三元组 query params（feishu_project_key / work_item_type / work_item_id），校验沿用既有 view 范式：缺参 → 400 中文 detail；work_item_id 非整数 → 400。
      - 查询路径（只读，不旁路 fetch / 不写表）：先按三元组 afirst 命中已落库 WorkItem；不存在 → 404 "WorkItem 不存在"。
      - `document = await Document.objects.filter(work_item=work_item, document_type=DocumentType.PRD).select_related("current_version").order_by("-updated_at").afirst()`（同 work_item 多 PRD 取最近更新一条；select_related 防 async 隐式同步访问 current_version）。
      - document 为 None → 404 "该 WorkItem 暂无 PRD 文档快照"（明确语义，不臆造空文档）。
      - 命中 → `payload = await sync_to_async(lambda: DocumentSnapshotSerializer(document).data)()`，返回 200。
      - 可选 query param `?document_type=tech_plan` 支持复用同端点取技术方案快照（默认 prd）——按既有约定决定是否加；若加则校验 document_type ∈ DocumentType.values，非法 400。

    在 server/delivery/urls.py 注册路由 `path("work-items/prd-document/", WorkItemPrdDocumentView.as_view(), name="work-item-prd-document")`，置于通配 `work-items/` detail 路由之前（字面段优先，沿用既有顺序）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/delivery/test_document_api.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `GET /api/.../work-items/prd-document/?feishu_project_key=&work_item_type=&work_item_id=` 命中已落库 WorkItem 的 PRD Document → 200 + content 为 current_version 正文。
    - 缺三元组参数 → 400；work_item_id 非整数 → 400。
    - WorkItem 不存在 → 404；WorkItem 存在但无 PRD Document → 404（明确语义）。
    - serializer 全字段 read_only；content 取 current_version.content（None → ""）。
  </acceptance_criteria>
  <done>PRD 正文快照只读 REST 端点 + serializer + 路由就位，IsAuthenticated，纯读已落库 Document 不旁路写/不旁路 fetch。</done>
</task>

<task type="auto">
  <name>Task 2: PRD 快照检索守护测试</name>
  <files>server/tests/delivery/test_document_api.py</files>
  <read_first>
    - server/tests/delivery/test_api.py（adrf APIView 测试范式：async client / force_authenticate、三元组 query、401 未认证、404 不存在、200 payload 断言；async ORM 经 sync_to_async 建夹具）
    - server/delivery/services/document_service.py（建 Document 夹具经 DocumentService.upsert_from_feishu，守 INV-6——测试不旁路 ORM 建 Document）
    - server/delivery/api/views.py（Task 1 实现）
  </read_first>
  <action>
    新建 server/tests/delivery/test_document_api.py（pytest.mark.django_db(transaction=True)，pytest-socket 零真实网络，端点不回源故无需 respx）：

    - 夹具：建 Project + delivery WorkItem（三元组，复用 test_api.py/test_models.py 既有建法）；经 `DocumentService().upsert_from_feishu(work_item=wi, document_type="prd", doc_token="tok", content="PRD 正文快照", canonical_url="https://acme.feishu.cn/docx/tok")` 建 Document 夹具（守 INV-6，不旁路 ORM 写 Document）。
    - test 命中：force_authenticate 用户 → GET prd-document 三元组 → 200 + response.data["content"] == "PRD 正文快照" + document_type=="prd" + content_storage=="both"（DOC-02 成功标准 3 端到端守护）。
    - test 未认证：匿名 GET → 401（IsAuthenticated，T-30-09）。
    - test 缺参 / 非整数 work_item_id → 400。
    - test WorkItem 不存在 → 404；WorkItem 存在但未建 PRD Document → 404（明确语义，不返回空文档）。
    - test 只读不写：请求前后 Document/DocumentVersion 行数不变（端点纯读）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/delivery/test_document_api.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - 命中：200 + content == PRD 正文快照（经 Document 实体检索到 PRD 正文，DOC-02 成功标准 3）。
    - 未认证 401；缺参/非整数 400；WorkItem 不存在 404；无 PRD Document 404。
    - 端点只读：请求前后 Document/DocumentVersion 行数不变。
  </acceptance_criteria>
  <done>PRD 快照检索守护测试覆盖命中/认证/参数/未命中/只读，端到端兑现 DOC-02 成功标准 3，全绿无真实网络。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → REST | 未认证/越权读取需拦截（IsAuthenticated） |
| REST → delivery DB | 只读查询已落库 Document（不旁路 fetch / 不写表） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-09 | Elevation of Privilege | PRD 快照只读端点 | mitigate | permission_classes=[IsAuthenticated]，匿名 401（测试守护） |
| T-30-10 | Tampering | 经读端点旁路写表 | mitigate | 端点纯读（afirst/filter），serializer 全 read_only；只读不写测试守护（行数不变）；Document 写入 INV-6 grep 守护在 30-02 |
| T-30-SC | Tampering | 依赖供应链 | accept | 无新增 npm/pip 包（adrf APIView + DRF serializer），不触发包合法性门 |
</threat_model>

<verification>
- `uv run pytest tests/delivery/test_document_api.py -q` 全绿；pytest-socket 零真实网络（端点不回源）。
- `uv run pytest tests/delivery/ -q` 无回归（28/29/30-01/30-02 套件）。
- `uv run ruff format --check delivery/api/views.py delivery/api/serializers.py delivery/urls.py tests/delivery/test_document_api.py` + `ruff check delivery/ tests/delivery/` 干净。
- 未改 knowledge app（INV-3）；未新增第三方依赖；Document 夹具经 DocumentService 建（不旁路 ORM 写）。
</verification>

<success_criteria>
- 给定带 prd_url 的 WorkItem（三元组），经 Document 实体检索到 PRD 正文快照（DOC-02 成功标准 3）。
- 查询路径 Document.objects.filter(work_item, document_type=prd) → current_version.content。
- 最小只读 REST（IsAuthenticated），未命中明确 404，端点纯读不写。
</success_criteria>

<artifacts_produced>
- `server/delivery/api/views.py` — `WorkItemPrdDocumentView`
- `server/delivery/api/serializers.py` — `DocumentSnapshotSerializer`
- `server/delivery/urls.py` — `work-items/prd-document/` 路由
- `server/tests/delivery/test_document_api.py` — PRD 快照检索守护测试
</artifacts_produced>

<output>
Create `.planning/phases/30-document-references/30-04-SUMMARY.md` when done
</output>
