---
phase: 30-document-references
plan: 03
type: execute
wave: 3
depends_on: ["30-02"]
files_modified:
  - server/knowledge/sources/feishu_document.py
  - server/knowledge/sources/__init__.py
  - server/tests/knowledge/test_feishu_document_normalizer.py
autonomous: true
requirements: [DOC-02]
must_haves:
  truths:
    - "feishu_document normalizer 注册进 get_normalizer 注册表，摄取飞书 docx（PRD/技术方案）"
    - "doc 取材复用既有路径：_extract_doc_token + create_feishu_doc_client_for_project + get_document_content（不重写）"
    - "产出操作态 Document/DocumentVersion 经 DocumentService 单一入口（INV-6）+ work_item FK 关联"
    - "产出 knowledge 投影 KnowledgeEntity(kind=document) + KnowledgeEdge(relation=REFERENCES) 连 work_item 实体 → document 实体"
    - "doc 拉取失败降级：快照缺正文段 + warning，Document/实体仍建（缺段不缺实体），不抛不回滚"
    - "既有 feishu_work_item normalizer 内联正文保留不动（INV-3），work_item 锚事件复用其产出避免 clobber 既有快照"
  artifacts:
    - path: "server/knowledge/sources/feishu_document.py"
      provides: "feishu_document normalizer（async normalize → 操作态 Document + knowledge document 实体 + REFERENCES 边）"
      contains: "async def normalize"
    - path: "server/knowledge/sources/__init__.py"
      provides: "注册 feishu_document → knowledge.sources.feishu_document"
      contains: "feishu_document"
  key_links:
    - from: "server/knowledge/sources/feishu_document.py"
      to: "delivery.services.DocumentService.upsert_from_feishu"
      via: "操作态 Document 落库经单一入口（INV-6）"
      pattern: "DocumentService"
    - from: "server/knowledge/sources/feishu_document.py work_item 锚事件"
      to: "document 实体（generate_entity_id('document','feishu_document',doc_token)）"
      via: "EdgeSpec(relation=REFERENCES, target_entity_id=...) 出边（work_item→document）"
      pattern: "EdgeRelation.REFERENCES"
    - from: "server/knowledge/sources/feishu_document.py"
      to: "knowledge.sources.feishu_work_item.normalize"
      via: "复用既有 work_item 锚事件产出（同 content 不 clobber 既有快照）"
      pattern: "feishu_work_item"
---

<objective>
新增 `server/knowledge/sources/feishu_document.py` normalizer 并注册进既有 `get_normalizer` 注册表：从 work item 三元组 + `prd_url`/`tech_doc_url` 提取 doc token（复用 `_extract_doc_token`），经既有 `create_feishu_doc_client_for_project` + `get_document_content` 拉正文（不重写取材路径），产出 ① 操作态 `Document`/`DocumentVersion`（经 30-02 `DocumentService` 单一入口 + `work_item` FK）；② knowledge 投影 `KnowledgeEntity(kind=document)` + `KnowledgeEdge(relation=REFERENCES)` 连 work_item 实体 → document 实体。doc 拉取失败降级（缺正文段 + warning，缺段不缺实体，不抛不回滚）。既有 `feishu_work_item` normalizer 保留不动（INV-3）。

Purpose: DOC-02——feishu_document normalizer 把飞书 docx 摄取入库并建 REFERENCES 边关联 WorkItem；REFERENCES 方向 work_item→document（与 mcp_plan HAS_PLAN 双事件出边范式一致）。
Output: `feishu_document` normalizer、注册表登记、normalizer 守护测试。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-document-references/30-CONTEXT.md
@.planning/DOMAIN-MODEL.md
@server/knowledge/sources/feishu_work_item.py
@server/knowledge/sources/mcp_plan.py
@server/knowledge/sources/__init__.py
@server/knowledge/ingestion.py
@server/knowledge/models.py
@server/agents/tools/feishu_doc_tools.py
@server/delivery/services/document_service.py
@server/tests/knowledge/test_ingestion.py
@.planning/phases/30-document-references/30-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: feishu_document normalizer（操作态 Document + knowledge document 实体 + REFERENCES 边）+ 注册</name>
  <files>server/knowledge/sources/feishu_document.py, server/knowledge/sources/__init__.py</files>
  <read_first>
    - server/knowledge/sources/feishu_work_item.py（**复用对象**：`_extract_doc_token`（剥 query 取 token）、`_fetch_doc_body`（拉正文降级返回空串不抛）、`_parse_event_time`、doc client 取材路径、normalize 产出 work_item 全量快照事件的契约——本 normalizer 复用其 work_item 锚事件产出，不重写取材）
    - server/knowledge/sources/mcp_plan.py（**边方向范式**：产出两事件——work_item 锚事件携 EdgeSpec 出边（relation=HAS_PLAN, target=plan 实体 id 经 generate_entity_id）+ plan 事件；本 normalizer 同款：work_item 锚事件携 REFERENCES 出边 + document 事件，确保边方向 work_item→document）
    - server/knowledge/ingestion.py（IngestionRequest/IngestionEvent/EdgeSpec 形态；ingest_events 两阶段编排：先持久化全部实体再统一 apply_edge_specs（两端实体须已存在）；hash 相等不翻版本铁律）
    - server/knowledge/models.py（EntityKind.DOCUMENT / EntityOrigin.FEISHU / EdgeRelation.REFERENCES 已定义；generate_entity_id natural key 规则：feishu_document → source_id = 飞书文档 token）
    - server/agents/tools/feishu_doc_tools.py create_feishu_doc_client_for_project（飞书 doc client，get_document_content(token) → (markdown, blocks)）
    - server/delivery/services/document_service.py（DocumentService.upsert_from_feishu 签名 + derive_feishu_tenant）
    - .planning/phases/30-document-references/30-CONTEXT.md Grey Area 3/6（normalizer 输入签名 Claude's Discretion、REFERENCES 边、INV-3 既有 feishu_work_item 不动）
  </read_first>
  <action>
    新建 server/knowledge/sources/feishu_document.py，async `normalize(request: IngestionRequest) -> list[IngestionEvent]`，source_kind 字面值 "feishu_document"，source_id = work item 三元组 `{project_key}:{work_item_type}:{work_item_id}`（与 feishu_work_item 同锚——本 normalizer 经三元组定位 work item 及其 prd_url/tech_doc_url，per CONTEXT Grey Area 3 选三元组输入签名）。

    流程（复用既有取材，不重写；缺料降级不抛）：
      1. 解析 source_id 三元组（非法/缺段 → warning "knowledge_normalize_source_missing" + return []，沿用 feishu_work_item 范式）；按 feishu_project_key 取 Project（缺 → warning + return []）。
      2. **work_item 锚事件复用既有产出（关键，避免 clobber 既有 work_item 快照 + 不重写）**：`from knowledge.sources import feishu_work_item; wi_events = await feishu_work_item.normalize(request)`。wi_events 为空（源缺失降级）→ warning + return []。取 wi_events[0] 作 work_item 锚事件——其 content 与既有 feishu_work_item 投影逐字一致，hash 相等不翻版本（INV-3：既有快照不被本 normalizer clobber）。
      3. 取 doc token：经 create_feishu_client_for_project 调 get_work_item 取 fields（或复用 wi 锚事件 payload 内的 prd_url/tech_doc_url——payload 已含 prd_url/tech_doc_url 字面值），用 `_extract_doc_token` 从 prd_url / tech_doc_url 各提取 token。两者皆空 → 仍返回 [wi 锚事件]（无文档，事件照常，缺段不缺实体）。
      4. doc client：`doc_client = await create_feishu_doc_client_for_project(project)`（try/except → warning "knowledge_normalize_doc_client_unavailable"，doc_client=None 降级）。
      5. 对每个 (token, document_type∈{prd, tech_plan}, canonical_url) 非空项：
          a. `body = await _fetch_doc_body(doc_client, token, request=request, label=...)`（复用既有降级 helper：失败返回空串 + warning，不抛）。
          b. **操作态落库（INV-6）**：resolve 已落库 delivery WorkItem（`from delivery.models import WorkItem; wi = await WorkItem.objects.filter(三元组).afirst()`，缺则 None 占位）；`await DocumentService().upsert_from_feishu(work_item=wi, document_type=document_type, doc_token=token, content=body, canonical_url=canonical_url, feishu_tenant=derive_feishu_tenant(canonical_url), source="manual")`（body 为空亦调——缺段不缺实体；DocumentService 内部按 content 空记 facet=missing）。整段 try/except 包裹：操作态写入异常仅 warning，不阻断 knowledge 投影产出（降级不回滚，对齐 §1.4）。
          c. **knowledge 投影事件**：构造 document IngestionEvent（kind=EntityKind.DOCUMENT, origin=EntityOrigin.FEISHU, source_kind="feishu_document", source_id=token, title=取文档首个标题或 work_item 名+文档类型, content=body, payload={document_type, work_item triplet, canonical_url, feishu_tenant}, project_id=str(project.id), repository_id=None, event_time=wi 锚事件 event_time 或 timezone.now()）。
          d. 在 work_item 锚事件上累加一条 REFERENCES 出边 EdgeSpec：`EdgeSpec(relation=EdgeRelation.REFERENCES, target_entity_id=generate_entity_id("document", "feishu_document", token))`（方向 work_item→document）。
      6. 因 IngestionEvent 为 frozen dataclass，用 `dataclasses.replace(wi_event, edges=(*wi_event.edges, *reference_edges))` 把 REFERENCES 边挂到 work_item 锚事件，再 return [wi_event_with_edges, *document_events]（顺序不限——ingest_events 先持久化全部实体再统一处理边，两端实体保证已存在）。

    在 server/knowledge/sources/__init__.py 的 `_NORMALIZERS` dict 注册 `"feishu_document": "knowledge.sources.feishu_document"`（沿用既有惰性 import 登记风格）。

    复用纪律：`_extract_doc_token` / `_fetch_doc_body` 从 feishu_work_item import 复用（不复刻）；create_feishu_doc_client_for_project / get_document_content 经既有 service 取材（不重写 doc 拉取）；feishu_work_item.py **不修改**（INV-3，既有内联正文保留不动）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/knowledge/test_feishu_document_normalizer.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `get_normalizer("feishu_document")` 返回该 normalize（注册生效）。
    - normalize 复用 feishu_work_item.normalize 产出 work_item 锚事件（content 一致，不 clobber 既有快照）。
    - 取材复用 _extract_doc_token + create_feishu_doc_client_for_project + get_document_content（grep 确认未重写 doc 拉取逻辑）。
    - work_item 锚事件携 REFERENCES 出边，target_entity_id=generate_entity_id("document","feishu_document",token)；document 事件 kind=document/source_kind=feishu_document/source_id=token。
    - feishu_work_item.py 未被修改（INV-3）。
  </acceptance_criteria>
  <done>feishu_document normalizer 就位并注册：复用既有取材产出操作态 Document（经 DocumentService）+ knowledge document 实体 + work_item→REFERENCES→document 出边，feishu_work_item 不动。</done>
</task>

<task type="auto">
  <name>Task 2: normalizer 端到端守护测试（Document + REFERENCES 边 + 降级 + INV-3）</name>
  <files>server/tests/knowledge/test_feishu_document_normalizer.py</files>
  <read_first>
    - server/tests/knowledge/test_ingestion.py（摄取测试纪律：django_db(transaction=True)、直接 await ingest_events 绕调度层、Qdrant/embedding mock、--disable-socket 第二保险、make_event 工厂、KnowledgeEntity/KnowledgeEdge/generate_entity_id 断言）
    - server/tests/knowledge/conftest.py（embedding / collection / sparse mock fixtures，复用避免真跑向量）
    - server/delivery/services/document_service.py（DocumentService 行为：去重/hash 不翻版本/facet）
    - server/knowledge/sources/feishu_document.py（Task 1 实现）
  </read_first>
  <action>
    新建 server/tests/knowledge/test_feishu_document_normalizer.py（pytest.mark.django_db(transaction=True)，复用 conftest 的 embedding/collection/sparse mock，respx mock get_work_item / get_document_content / doc client，pytest-socket 零真实网络）：

    - 测 normalize 产出形状：mock feishu client get_work_item 返回带 prd_url/tech_doc_url 的 fields；mock doc client get_document_content 返回 markdown 正文。建 delivery WorkItem（三元组）+ Project。调 normalize(request) → 断言返回含 work_item 锚事件（携 REFERENCES 边）+ document 事件；REFERENCES EdgeSpec.target_entity_id == generate_entity_id("document","feishu_document",token)。
    - 测端到端入图（await ingest_events(events)）：断言建出 KnowledgeEntity(kind=document, source_kind=feishu_document, source_id=token) + KnowledgeEdge(relation=REFERENCES, source=work_item 实体, target=document 实体)，边方向 work_item→document。
    - 测操作态 Document：normalize 后断言 delivery Document(work_item=wi, document_type=prd, external_ref=token, content_storage=both) + DocumentVersion(content=正文) 落库（经 DocumentService）；prd_body facet=complete。
    - 测降级（DOC-02 关键）：mock get_document_content 抛异常 → _fetch_doc_body 返回空 → 断言 normalize 不抛、仍产出 document 事件与 REFERENCES 边、Document 仍建（content 空）、prd_body facet=missing（缺段不缺实体）。
    - 测 hash 相等不翻版本：同 work item 二次 normalize（同正文）→ DocumentVersion 计数不变（复用 DocumentService 范式，端到端验证）。
    - 测 INV-3：normalize 复用 feishu_work_item.normalize 产出 work_item 锚事件，content 与单独跑 feishu_work_item.normalize 一致（hash 相等不 clobber 既有 work_item 快照版本）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/knowledge/test_feishu_document_normalizer.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - 端到端：KnowledgeEntity(kind=document) + KnowledgeEdge(REFERENCES, work_item→document) 建出。
    - 操作态 Document/DocumentVersion 经 DocumentService 落库 + work_item FK + prd_body facet=complete。
    - doc 拉取失败降级：不抛、document 实体 + REFERENCES 边仍在、Document content 空、facet=missing。
    - hash 相等二次摄取不翻 DocumentVersion 版本。
    - work_item 锚事件 content 与既有 feishu_work_item 投影一致（INV-3 不 clobber）。
  </acceptance_criteria>
  <done>normalizer 端到端守护测试覆盖 Document + REFERENCES 边 + 降级缺段不缺实体 + hash 不翻版本 + INV-3 不 clobber，全绿无真实网络。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 飞书 docx 回源 → normalizer | get_document_content 返回外部不可信正文（仅落快照/向量，不执行） |
| normalizer → delivery DB | 操作态 Document 经 DocumentService 单一入口（INV-6） |
| normalizer → knowledge 投影 | document 实体 + REFERENCES 边经既有 ingest_events 管线（INV-3 投影） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-06 | DoS | doc 拉取失败掀翻摄取 | mitigate | _fetch_doc_body 失败返回空串 + warning（复用既有降级）；操作态写入 try/except 不阻断投影；缺段不缺实体不抛不回滚（测试守护） |
| T-30-07 | Tampering | clobber 既有 feishu_work_item work_item 快照 | mitigate | work_item 锚事件复用 feishu_work_item.normalize 产出（content 一致 hash 相等不翻版本），feishu_work_item.py 不修改（INV-3 测试守护） |
| T-30-08 | Information Disclosure | doc 拉取失败日志/错误泄漏凭证 | mitigate | 沿用 feishu_work_item warning 仅记 doc_token/error 类型，凭证经既有 service 层（DB 加密，零 env）；DocumentService.error 面已脱敏 |
| T-30-SC | Tampering | 依赖供应链 | accept | 无新增 npm/pip 包（复用既有 feishu doc client + ingestion 管线），不触发包合法性门 |
</threat_model>

<verification>
- `uv run pytest tests/knowledge/test_feishu_document_normalizer.py -q` 全绿；全程 respx mock get_work_item/get_document_content + conftest embedding/sparse/collection mock + pytest-socket 零真实网络。
- `uv run pytest tests/knowledge/ tests/delivery/ -q` 无回归（既有 feishu_work_item / ingestion / DocumentService 套件）。
- `uv run ruff format --check knowledge/sources/feishu_document.py tests/knowledge/test_feishu_document_normalizer.py` + `ruff check knowledge/sources/ tests/knowledge/` 干净。
- feishu_work_item.py 未被修改（INV-3，git diff 确认）；未新增第三方依赖；feishu_document 取材全程复用既有 helper（未重写 doc 拉取）。
</verification>

<success_criteria>
- feishu_document normalizer 注册并把飞书 docx（PRD/技术方案）摄取入库（DOC-02）。
- 产出操作态 Document/DocumentVersion（经 DocumentService）+ knowledge document 实体 + work_item→REFERENCES→document 边。
- doc 拉取失败降级缺段不缺实体，不抛不回滚；hash 相等不翻版本。
- 既有 feishu_work_item normalizer 内联正文保留不动（INV-3）。
</success_criteria>

<artifacts_produced>
- `server/knowledge/sources/feishu_document.py` — feishu_document normalizer（操作态 Document + knowledge document 实体 + REFERENCES 边）
- `server/knowledge/sources/__init__.py` — 注册 feishu_document
- `server/tests/knowledge/test_feishu_document_normalizer.py` — 端到端 Document/REFERENCES/降级/hash/INV-3 守护测试
</artifacts_produced>

<output>
Create `.planning/phases/30-document-references/30-03-SUMMARY.md` when done
</output>
