---
phase: 31-release-ledger
plan: 02
type: execute
wave: 2
depends_on: ["31-01"]
files_modified:
  - server/delivery/services/release_service.py
  - server/delivery/services/__init__.py
  - server/tests/delivery/test_release_service.py
  - server/tests/delivery/test_release_inv6_guard.py
autonomous: true
requirements: [REL-01]
must_haves:
  truths:
    - "Release 账本落库只经 ReleaseService 单一入口（旁路写表 grep 守护 fail 即报）"
    - "ingest_batch 用 raw_rows 建 ReleaseBatch + 多条 ReleaseRecord，raw_row 原样保留"
    - "ReleaseRecord 按 bitable_record_key 幂等 upsert（同 key 重摄收敛同行不新增）"
    - "work_item 经 work_item_external_id 反查：命中连 FK，未命中留占位（不抛）"
  artifacts:
    - path: "server/delivery/services/release_service.py"
      provides: "ReleaseService — Release 账本唯一写入入口（INV-6）"
      exports: ["ReleaseService"]
    - path: "server/tests/delivery/test_release_inv6_guard.py"
      provides: "Release 三模型旁路写表 grep 守护 + writer 有效性"
    - path: "server/tests/delivery/test_release_service.py"
      provides: "ingest 幂等 + raw_row 无损 + work_item 反查 守护"
  key_links:
    - from: "server/delivery/services/release_service.py"
      to: "delivery.models.ReleaseBatch / ReleaseRecord"
      via: "get_or_create / select_for_update upsert"
      pattern: "ReleaseRecord.objects.(get_or_create|update_or_create|select_for_update)"
---

<objective>
落地 `ReleaseService` —— Release 账本（ReleaseBatch/ReleaseRecord/ReleaseArtifact）**唯一写入收口**（INV-6 精神，对齐 28-02 WorkItemService / 30-02 DocumentService 单一写入范式）。提供 `ingest_batch(raw_rows, source, batch_meta)` 把一批 Bitable 原始行 → 一个 ReleaseBatch + 多条 ReleaseRecord（**保留 raw_row**），按 `bitable_record_key` 幂等 upsert；`work_item` 经 `work_item_external_id` 反查已落库 WorkItem（命中连 FK，未命中留占位——对齐 WorkItemRelation/Document.work_item 占位范式）。配套 INV-6 grep 守护测试 + 服务行为测试。

Purpose: 收口账本写入（INV-6），31-03 Bitable adapter 经此入口落库而非旁路写表；raw_row 保留是 REL-01 "adapter 演进不丢数据" 的运行时兑现。
Output: `release_service.py`、services re-export、INV-6 守护测试、服务行为测试。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-release-ledger/31-CONTEXT.md
@.planning/phases/31-release-ledger/31-01-PLAN.md

# 直接镜像的既有单一写入 + sync_to_async + INV-6 守护范式
@server/delivery/services/document_service.py
@server/delivery/services/__init__.py
@server/tests/delivery/test_inv6_guard.py
@server/tests/delivery/test_document_inv6_guard.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: ReleaseService 单一写入入口 + work_item 反查 + re-export</name>
  <files>server/delivery/services/release_service.py, server/delivery/services/__init__.py</files>
  <read_first>
    - server/delivery/services/document_service.py（@sync_to_async + transaction.atomic + select_for_update().get_or_create + 显式 update_fields + 中文 docstring + 降级"缺段不缺实体"范式）
    - server/delivery/services/work_item_service.py（lines 1–50：service 类结构 + sync_to_async 跨线程写库 + structlog logger 范式）
    - server/delivery/models/release.py（31-01 产出：字段 + build_bitable_record_key + 条件唯一约束）
    - server/delivery/models/work_item.py（三元组 + work_item_id BigIntegerField，反查锚）
    - server/delivery/services/__init__.py（curated re-export + __all__）
  </read_first>
  <action>
    新建 `server/delivery/services/release_service.py`，`ReleaseService` 为 Release 账本唯一写入入口（INV-6 精神，REL-01）。结构镜像 DocumentService：async 公共方法 + `@sync_to_async` 包同步 `transaction.atomic` 写库；`structlog` logger；中文 docstring 标注 "唯一写入收口/禁旁路写表"。

    `ingest_batch(*, raw_rows: list[dict], source: str, batch_meta: dict | None = None) -> ReleaseBatch`：
    - 建一个 ReleaseBatch：`source`（ReleaseSource 值）、`name`/`released_at`/`external_ref` 取自 `batch_meta`（缺省空/None）、`raw_row` 落 batch_meta 原始内容（保留，缺省 {}）。
    - 对 `raw_rows` 每行调内部 `_upsert_record(batch, raw_row, source)`：经 `build_bitable_record_key(...)` 从 raw_row 提取 natural key 三段（app_token/table_id/record_id 的取值来源在本 phase 为**占位映射**：从 raw_row 顶层同名键取，取不到则空 key——业务列→字段映射 TODO 标 REL-03）。
    - 返回 ReleaseBatch（含 records 已落库）。
    - 失败策略沿用 §1.4 降级：单行映射异常不回滚整批（best-effort 逐行 try/except + warning），已建 batch 与成功行保留（对齐 WorkItemService facet 失败不回滚精神）。

    `_upsert_record(batch, raw_row, source)`（`@sync_to_async` + transaction.atomic）：
    - natural key 非空 → `ReleaseRecord.objects.select_for_update().get_or_create(bitable_record_key=key, defaults={...})`（幂等收敛同行，复用 document_service get_or_create 范式 + 31-01 条件唯一约束防并发重复）；natural key 空 → 直接 `get_or_create` 不可行（无去重键）则按 batch 内新建（空 key 行豁免唯一，允许多行）。
    - 已存在：刷新 `raw_row`（保留最新原始行）、`status`/`note`（若 raw_row 占位映射可得）、`updated_at`；**raw_row 始终覆盖为传入原始行**（不丢数据）。
    - `work_item` 反查：从 raw_row 占位映射取 `work_item_external_id`（BigInt，取不到则 None）；非 None 时按 `WorkItem.objects.filter(work_item_id=external_id).afirst()`/sync 等价查询（注意：WorkItem 自然键是三元组，本 phase 占位只按 work_item_id 反查，命中多条取首条 + warning；命中连 FK，未命中留 `work_item_external_id` 占位 + work_item=None，**不抛**——对齐 WorkItemRelation 占位范式，真实粒度定型留 REL-03）。

    `upsert_record(...)`：暴露单行 upsert 的 async 公共方法（供 31-03 adapter / 未来手动录入复用），内部委托 `_upsert_record`。

    natural key / 列映射的"业务列→字段"占位处显式 `# TODO(REL-03): 真实 Bitable 列映射待开放平台凭证 + 列样例` 注释，避免被误当成完成态。

    更新 `server/delivery/services/__init__.py`：`from delivery.services.release_service import ReleaseService` + 加入 `__all__`（保持 curated re-export 风格）。

    ruff line 100；中文 docstring；structlog 结构化日志（`release_batch_ingested` batch_id/record_count、`release_record_workitem_linked`/`_placeholder`）。
  </action>
  <acceptance_criteria>
    - `ReleaseService` 经 `delivery.services` 顶层可 import。
    - `ingest_batch` 写库经 `ReleaseBatch`/`ReleaseRecord` 的 get_or_create/create，且全程 `@sync_to_async` 包同步事务（无裸 async ORM）。
    - raw_row 始终以传入原始行覆盖落库（无损保留）。
    - work_item 反查命中连 FK、未命中留 work_item_external_id 占位且 work_item=None、不抛。
    - 含 `TODO(REL-03)` 注释标记占位列映射。
    - ReleaseArtifact 在本 phase 无需 adapter 写入（骨架），但 service 可留 `add_artifact` 占位方法或暂不实现——若实现须同样经 service（不旁路）。
  </acceptance_criteria>
  <verify>
    <automated>cd server && uv run python -c "from delivery.services import ReleaseService; print('ok')" && uv run ruff check delivery/services/release_service.py</automated>
  </verify>
  <done>ReleaseService 落库收口就位（ingest_batch + upsert_record + work_item 反查 + raw_row 保留），re-export 完成。</done>
</task>

<task type="auto">
  <name>Task 2: Release INV-6 旁路写表 grep 守护测试</name>
  <files>server/tests/delivery/test_release_inv6_guard.py</files>
  <read_first>
    - server/tests/delivery/test_inv6_guard.py（精确锚定 grep 范式：_RE_*_ORM_WRITE / _RE_*_INSTANTIATE / _RE_*_INSTANCE_SAVE、_PRUNE_DIRS、_is_scanned_for_inv6 排除 tests/migrations/models、writer 有效性断言）
    - server/tests/delivery/test_document_inv6_guard.py（多模型同款守护 + class 定义行跳过技巧）
  </read_first>
  <action>
    新建 `server/tests/delivery/test_release_inv6_guard.py`，**纯本地源码扫描**（无 DB/网络），镜像 `test_inv6_guard.py` 精确锚定范式，守护 Release 三模型落库只经 `delivery/services/release_service.py`：
    - 唯一 writer 常量 `_ALLOWED_RELEASE_WRITER = "delivery/services/release_service.py"`。
    - 对 `ReleaseBatch` / `ReleaseRecord` / `ReleaseArtifact` 各建三类精确正则：`\b<Model>\.objects\.(?:create|bulk_create|get_or_create|update_or_create)\b`、`\b<Model>\s*\(`、`\b<Model>\([^)]*\)\.save\(`。注意 case-sensitive + `\s*\(` 紧跟天然排除更长符号（如 `ReleaseBatch` 不误伤、`ReleaseRecord` 与 `ReleaseRecordSerializer` 区分）。
    - 扫描范围排除：writer 自身、`tests/`、`/migrations/`、`delivery/models/`（沿用 `_is_scanned_for_inv6` 剪枝 + `_PRUNE_DIRS`）。
    - 跳过 `class Release...` 定义行。
    - 命中即 assert fail 并列出 `文件:行`。
    - writer 有效性测试：断言 `release_service.py` 确实含 Release 写表（`ReleaseRecord.objects.<write>` 或实例化），否则守护形同虚设。

    中文 docstring；ruff line 100。
  </action>
  <acceptance_criteria>
    - 测试当前全绿（除 ReleaseService 外无旁路写表）。
    - writer 有效性测试断言 release_service.py 含 Release 写表入口。
    - 扫描正确排除 tests/ / migrations/ / models/ 与 writer 自身。
  </acceptance_criteria>
  <verify>
    <automated>cd server && uv run pytest tests/delivery/test_release_inv6_guard.py -x -q</automated>
  </verify>
  <done>Release INV-6 grep 守护全绿，账本写入收口经测试锁定。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ReleaseService 行为守护测试（幂等 + raw_row 无损 + work_item 反查）</name>
  <files>server/tests/delivery/test_release_service.py</files>
  <read_first>
    - server/tests/delivery/test_document_service.py（lines 1–60：django_db(transaction=True) + _make_work_item helper + async 写库测试范式）
    - server/tests/delivery/test_work_item_service.py（service 行为测试 + work_item 反查/占位断言参考）
    - server/delivery/services/release_service.py（Task 1 产出）
  </read_first>
  <behavior>
    - ingest 建批 + 多记录：ingest_batch 传 N 行 raw_rows + source=bitable → 建 1 个 ReleaseBatch + N 条 ReleaseRecord，每条 raw_row 与传入原始行完全相等（REL-01 无损）。
    - 幂等 upsert：同一批含相同非空 bitable_record_key 的行二次 ingest → 不新增 ReleaseRecord（收敛同行），raw_row 刷新为最新。
    - work_item 反查命中：先落一个 WorkItem(work_item_id=X)，raw_row 占位映射含 work_item_external_id=X → 该 ReleaseRecord.work_item 连 FK 命中。
    - work_item 反查未命中：work_item_external_id=Y（无对应 WorkItem）→ work_item=None 且 work_item_external_id=Y 占位，不抛。
    - 降级：某行映射触发异常（构造一个会让占位映射抛错的畸形行）不回滚整批，其余行与 batch 仍落库（best-effort）。
  </behavior>
  <action>
    新建 `server/tests/delivery/test_release_service.py`，`pytestmark = pytest.mark.django_db(transaction=True)`（sync_to_async 跨线程写库需 transaction=True，与 30-02 同理）。覆盖上述 behavior。复用 `_make_work_item` 风格 helper 建反查目标。无真实网络（service 不回源，raw_rows 由测试直接传入；pytest-socket 隔离）。中文 docstring；ruff line 100。
  </action>
  <acceptance_criteria>
    - ingest 建批 + N 记录 + raw_row 无损用例通过。
    - 同 bitable_record_key 二次 ingest 不新增行（幂等）用例通过。
    - work_item 反查命中连 FK / 未命中占位不抛 两用例均通过。
    - 降级（畸形行不回滚整批）用例通过。
  </acceptance_criteria>
  <verify>
    <automated>cd server && uv run pytest tests/delivery/test_release_service.py -x -q</automated>
  </verify>
  <done>ReleaseService 行为守护全绿：幂等 ingest + raw_row 无损 + work_item 反查命中/占位 + 降级。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 调用方 → ReleaseService | raw_rows（未来来自 Bitable / 手动）作为不可信外部数据跨入账本写入入口 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-03 | Tampering | ReleaseService 旁路写表 | mitigate | INV-6 grep 守护测试（test_release_inv6_guard.py）断言账本落库只经 ReleaseService；命中即 fail |
| T-31-04 | Denial of Service | ingest_batch 单行异常 | mitigate | best-effort 逐行 try/except + warning，单行畸形不回滚整批、不冒泡致整批失败 |
| T-31-05 | Information Disclosure | work_item 反查多条命中 | accept | 占位反查按 work_item_id（非三元组）取首条 + warning，本 phase 骨架可接受；真实粒度 REL-03 |

无 package 安装变更。
</threat_model>

<verification>
- `pytest tests/delivery/test_release_service.py tests/delivery/test_release_inv6_guard.py` 全绿。
- INV-6 grep 守护：除 ReleaseService 外无旁路 Release 写表。
- `ruff check delivery/services/release_service.py tests/delivery/test_release_service.py tests/delivery/test_release_inv6_guard.py` 干净。
</verification>

<success_criteria>
- Release 账本落库经 ReleaseService 单一入口（INV-6，grep 守护锁定）。
- raw_row 经 ingest 原样保留（REL-01 运行时兑现，已测）。
- bitable_record_key 幂等 upsert（重摄收敛同行）。
- work_item 经 work_item_external_id 反查命中连 FK / 未命中占位（不抛），对齐既有占位范式。
</success_criteria>

<artifacts_this_phase_produces>
- `server/delivery/services/release_service.py` — ReleaseService（ingest_batch / upsert_record / work_item 反查，Release 账本唯一写入入口）。
- `server/tests/delivery/test_release_inv6_guard.py` — Release INV-6 旁路写表 grep 守护。
- `server/tests/delivery/test_release_service.py` — ingest 幂等 + raw_row 无损 + work_item 反查 行为守护。
- `server/delivery/services/__init__.py`（修改）— ReleaseService re-export。
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/31-release-ledger/31-02-SUMMARY.md` when done
</output>
