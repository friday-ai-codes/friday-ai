---
phase: 31-release-ledger
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/delivery/models/release.py
  - server/delivery/models/__init__.py
  - server/delivery/migrations/0006_releasebatch_releaserecord_releaseartifact.py
  - server/tests/delivery/test_release_models.py
autonomous: true
requirements: [REL-01]
must_haves:
  truths:
    - "ReleaseBatch/ReleaseRecord/ReleaseArtifact 三张表存在于 DB（migrate 成功）"
    - "raw_row 原样存取无损（写入任意 JSON 行，读回完全相等）"
    - "ReleaseRecord 可在 work_item 未落库时用 work_item_external_id 占位（work_item=None）"
    - "Bitable 记录 natural key {app_token}:{table_id}:{record_id} 标识就位（字段存在且非空唯一）"
    - "ReleaseArtifact.artifact_type 限定 mr|branch|commit|diff|release_note|doc 枚举"
  artifacts:
    - path: "server/delivery/models/release.py"
      provides: "ReleaseBatch / ReleaseRecord / ReleaseArtifact 宽容模型 + 枚举 + natural-key helper"
      contains: "class ReleaseBatch"
    - path: "server/delivery/migrations/0006_releasebatch_releaserecord_releaseartifact.py"
      provides: "三表初始迁移（依赖 0005）"
      contains: "0005_document"
    - path: "server/tests/delivery/test_release_models.py"
      provides: "raw_row 无损 + natural key + 占位 + 枚举 守护单测"
  key_links:
    - from: "server/delivery/models/__init__.py"
      to: "server/delivery/models/release.py"
      via: "curated re-export"
      pattern: "from delivery.models.release import"
---

<objective>
落地 Release 账本**宽容模型**三表（`ReleaseBatch` / `ReleaseRecord` / `ReleaseArtifact`），逐字段对齐 DOMAIN §4 / §12.6，每个 row-bearing 模型带 `raw_row` JSONField 保留 Bitable 原始行（adapter 演进/列映射变化**不丢数据**，REL-01 核心）；`ReleaseRecord` 用 `work_item FK(null, SET_NULL)` + `work_item_external_id` 占位（反查未落库范式，对齐 WorkItemRelation / Document.work_item）；Bitable 记录 natural key `{app_token}:{table_id}:{record_id}` 标识就位。本 plan **只建表 + 单测**，无落库业务逻辑（落库归 31-02 `ReleaseService`，守 INV-6）。

Purpose: 为 31-02 ReleaseService 与 31-03 Bitable adapter 提供宽容数据底座；raw_row 是 REL-01 "adapter 演进不丢数据" 的物理保证。
Output: `release.py` 三模型 + 枚举 + natural-key helper、re-export、migration 0006、模型守护单测。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-release-ledger/31-CONTEXT.md
@.planning/DOMAIN-MODEL.md

# 直接镜像的既有范式（建表 + 占位 external_id + JSON 列 + 包 re-export）
@server/delivery/models/work_item.py
@server/delivery/models/relation.py
@server/delivery/models/document.py
@server/delivery/models/__init__.py
@server/delivery/migrations/0005_document_uniq_document_feishu_tenant_external_ref.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 三宽容模型 + 枚举 + natural-key helper（release.py + re-export）</name>
  <files>server/delivery/models/release.py, server/delivery/models/__init__.py</files>
  <read_first>
    - server/delivery/models/work_item.py（UUID PK + JSONField(default=...) + TextChoices + Meta db_table/verbose_name/indexes 范式）
    - server/delivery/models/relation.py（占位 external_id 范式：target_work_item FK(null, CASCADE) + target_external_id BigIntegerField(null)）
    - server/delivery/models/document.py（条件 UniqueConstraint condition=~Q(field="") 范式、work_item FK(null, SET_NULL, related_name)、URLField max_length=1000）
    - server/delivery/models/__init__.py（curated re-export + __all__）
    - .planning/DOMAIN-MODEL.md §4（lines 184–205）/ §12.6（line 478–480）字段权威
  </read_first>
  <action>
    新建 `server/delivery/models/release.py`，逐字段对齐 DOMAIN §4/§12.6（REL-01 宽容模型）：

    枚举（TextChoices，沿用 work_item.py 风格 + 中文 label）：
    - `ReleaseSource`：`BITABLE = "bitable"`、`MANUAL = "manual"`。
    - `ReleaseArtifactType`：`MR = "mr"`、`BRANCH = "branch"`、`COMMIT = "commit"`、`DIFF = "diff"`、`RELEASE_NOTE = "release_note"`、`DOC = "doc"`。

    `ReleaseBatch`（一次上线/发布窗口）：`id` UUIDField(primary_key, default=uuid.uuid4, editable=False)；`name` CharField(max_length=512, blank, default="")；`released_at` DateTimeField(null, blank)；`source` CharField(max_length=16, choices=ReleaseSource.choices)；`external_ref` CharField(max_length=255, blank, default="")（批次级外部引用）；`raw_row` JSONField(default=dict)（保留 Bitable 原始行，REL-01）；`created_at` auto_now_add；`updated_at` auto_now。`Meta.db_table="delivery_release_batch"` + 中文 verbose_name。

    `ReleaseRecord`（某需求/缺陷在某次上线中的记录 = 一个 Bitable 行）：`id` UUIDField(...)；`batch` FK(ReleaseBatch, on_delete=CASCADE, related_name="records")；`work_item` FK("delivery.WorkItem" 直接类引用或字符串, on_delete=SET_NULL, null, blank, related_name="release_records")（反查目标未落库时为 None）；`work_item_external_id` BigIntegerField(null, blank)（占位，对齐 WorkItemRelation.target_external_id）；`status` CharField(max_length=64, blank, default="")；`note` TextField(blank, default="")；`bitable_record_key` CharField(max_length=255, blank, default="")（natural key `{app_token}:{table_id}:{record_id}` 落地点，per Claude's Discretion 选独立字段而非复用 external_ref，便于幂等 upsert）；`raw_row` JSONField(default=dict)（REL-01）；`created_at`/`updated_at`。`Meta.db_table="delivery_release_record"` + 中文 verbose_name + `indexes`（work_item / work_item_external_id 反查）+ 条件唯一约束 `UniqueConstraint(fields=["bitable_record_key"], condition=~Q(bitable_record_key=""), name="uniq_release_record_bitable_key")`（镜像 document.py uniq 范式——非空 natural key DB 级唯一，支撑 31-03 adapter 幂等 upsert；空键行豁免不互撞）。

    `ReleaseArtifact`（证据：MR/分支/commit/diff/上线说明/文档）：`id` UUIDField(...)；`release_record` FK(ReleaseRecord, on_delete=CASCADE, related_name="artifacts")；`artifact_type` CharField(max_length=16, choices=ReleaseArtifactType.choices)；`ref` CharField(max_length=1000, blank, default="")（MR URL / sha / doc token，URL 可超长沿用 1000 宽度）；`payload` JSONField(default=dict)；`created_at` auto_now_add。`Meta.db_table="delivery_release_artifact"` + 中文 verbose_name。

    模块级 helper `build_bitable_record_key(app_token, table_id, record_id) -> str` 返回 `f"{app_token}:{table_id}:{record_id}"`（natural key 唯一构造入口，31-03 adapter 复用，避免拼接漂移）。任一段为空时返回 ""（无法定位即不立 key）。

    **不写** 任何 create/save/业务方法（落库归 31-02 ReleaseService，守 INV-6 精神，对齐 28-01/30-02 "只建表" 范式）。`__all__` 导出全部公共符号。

    更新 `server/delivery/models/__init__.py`：`from delivery.models.release import (...)` 追加 `ReleaseBatch, ReleaseRecord, ReleaseArtifact, ReleaseSource, ReleaseArtifactType, build_bitable_record_key` 并加入 `__all__`（保持既有 curated re-export 风格）。

    中文 docstring（模块 + 类）；ruff line 100。
  </action>
  <acceptance_criteria>
    - `release.py` 含 `class ReleaseBatch` / `class ReleaseRecord` / `class ReleaseArtifact`，三者均 UUID PK。
    - `ReleaseBatch.raw_row` 与 `ReleaseRecord.raw_row` 均为 `JSONField(default=dict)`。
    - `ReleaseRecord.work_item` 为 FK(null=True, on_delete=SET_NULL)，且 `work_item_external_id` 为 BigIntegerField(null=True)。
    - `ReleaseArtifact.artifact_type` choices 恰为 mr|branch|commit|diff|release_note|doc。
    - `build_bitable_record_key("appX", "tblY", "recZ") == "appX:tblY:recZ"`。
    - `delivery.models` 顶层可 import 出 `ReleaseBatch/ReleaseRecord/ReleaseArtifact/ReleaseSource/ReleaseArtifactType/build_bitable_record_key`。
    - 无 create/save 业务逻辑（模型层纯结构）。
  </acceptance_criteria>
  <verify>
    <automated>cd server && uv run python -c "from delivery.models import ReleaseBatch, ReleaseRecord, ReleaseArtifact, ReleaseSource, ReleaseArtifactType, build_bitable_record_key; assert build_bitable_record_key('a','t','r')=='a:t:r'; print('ok')"</automated>
  </verify>
  <done>三模型 + 枚举 + helper 落 release.py 并经 models/__init__.py re-export，Django 可 import，无落库业务逻辑。</done>
</task>

<task type="auto">
  <name>Task 2 [BLOCKING]: 生成并应用 migration 0006 + makemigrations --check 干净</name>
  <files>server/delivery/migrations/0006_releasebatch_releaserecord_releaseartifact.py</files>
  <read_first>
    - server/delivery/migrations/0005_document_uniq_document_feishu_tenant_external_ref.py（dependencies 写法 + Django 6.0.1 生成格式）
    - server/delivery/migrations/0001_initial.py（UniqueConstraint/Index 在 migration 中的序列化形态，仅参考勿改）
  </read_first>
  <action>
    [BLOCKING] 经 Django 生成迁移（**不手写**）：`cd server && uv run python manage.py makemigrations delivery`，应产出 `0006_*`（dependencies 指向 `('delivery', '0005_document_uniq_document_feishu_tenant_external_ref')`，含三张新表 + ReleaseRecord 的条件 UniqueConstraint + indexes）。若文件名非 `0006_releasebatch_releaserecord_releaseartifact.py` 则重命名为该名（保持 dependencies 不变）。
    然后 `cd server && uv run python manage.py migrate delivery` 应用。
    生成产物若触发 ruff I001（import 未排序），用 `cd server && uv run ruff check --fix delivery/migrations/0006_*.py` 规范化（属生成产物格式收尾，非逻辑改动）。
    最后 `makemigrations delivery --check --dry-run` 必须报 "No changes detected"（模型与迁移一致，无漏字段）。
  </action>
  <acceptance_criteria>
    - `0006` migration 文件存在，dependencies 含 `0005_document...`。
    - `migrate delivery` 输出 `Applying delivery.0006... OK`。
    - DB 实际建出 `delivery_release_batch` / `delivery_release_record` / `delivery_release_artifact` 三张表。
    - `makemigrations delivery --check --dry-run` → No changes detected。
  </acceptance_criteria>
  <verify>
    <automated>cd server && uv run python manage.py migrate delivery && uv run python manage.py makemigrations delivery --check --dry-run</automated>
  </verify>
  <done>migration 0006 生成并应用成功，三表建出，--check 干净。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 模型守护单测（raw_row 无损 + natural key + 占位 + 枚举）</name>
  <files>server/tests/delivery/test_release_models.py</files>
  <read_first>
    - server/tests/delivery/test_models.py（pytest.mark.django_db / acreate / IntegrityError 守护 / 字段读回 范式）
    - server/tests/delivery/test_document_models.py（条件 UniqueConstraint 守护写法参考）
  </read_first>
  <behavior>
    - raw_row 无损：以含嵌套 dict/list/中文/数字的复杂 JSON 行 acreate ReleaseBatch + ReleaseRecord，afresh 读回 `raw_row` 与原 dict 完全相等（REL-01 核心：adapter 演进不丢数据的物理保证）。
    - natural key 占位：ReleaseRecord 在 work_item=None + work_item_external_id=<某 BigInt> + bitable_record_key=build_bitable_record_key(...) 时可创建并读回（占位范式）。
    - natural key 唯一：相同非空 bitable_record_key 二次 create 抛 IntegrityError；两个空 bitable_record_key 行可共存（条件唯一豁免空键）。
    - 关联：ReleaseRecord.batch FK 命中 ReleaseBatch；ReleaseArtifact.release_record FK 命中，artifact_type 取 ReleaseArtifactType 枚举值落库读回。
    - work_item 连 FK：建一个真实 WorkItem，ReleaseRecord.work_item=该 wi 可关联并经 related_name="release_records" 反查命中。
  </behavior>
  <action>
    新建 `server/tests/delivery/test_release_models.py`，`pytestmark = pytest.mark.django_db(transaction=True)`（与既有 delivery service 测试同理，跨线程安全）。覆盖上述 behavior 全部用例。IntegrityError 用例用 `with pytest.raises(IntegrityError)` + 包在独立 `transaction.atomic()` 或独立测试避免污染事务。无真实网络（pytest-socket 默认隔离，模型层不触网）。中文 docstring；ruff line 100。
  </action>
  <acceptance_criteria>
    - raw_row 复杂 JSON 写入→读回完全相等用例通过。
    - bitable_record_key 非空重复抛 IntegrityError、空键可共存用例通过。
    - work_item=None + work_item_external_id 占位 与 work_item 连 FK 两路径用例均通过。
    - artifact_type 枚举落库读回用例通过。
  </acceptance_criteria>
  <verify>
    <automated>cd server && uv run pytest tests/delivery/test_release_models.py -x -q</automated>
  </verify>
  <done>模型守护单测全绿，REL-01 raw_row 无损 + natural key 标识就位经测试锁定。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | 本 plan 仅建表 + 单测，无外部输入跨界（无网络、无用户输入路径） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01 | Tampering | raw_row JSONField | accept | 操作态非事实源（INV-3 精神）；raw_row 仅保留原始行供 adapter 演进，无信任决策依赖其内容 |
| T-31-02 | Information Disclosure | ReleaseRecord 关联 work_item | accept | 仅 FK/占位整数，无敏感明文；与既有 WorkItemRelation 占位范式同风险面 |

无 package 安装，无 npm/pip/cargo 变更（仅 Django 内置 ORM/migration）。
</threat_model>

<verification>
- `migrate delivery` 成功，三表建出，`makemigrations --check` 干净。
- `pytest tests/delivery/test_release_models.py` 全绿（raw_row 无损 + natural key + 占位 + 枚举）。
- `ruff format --check delivery/ tests/delivery/` + `ruff check delivery/models/release.py tests/delivery/test_release_models.py` 干净。
</verification>

<success_criteria>
- REL-01 宽容模型三表落地，`raw_row` 原样存取无损（adapter 演进不丢数据，已测）。
- natural key `{app_token}:{table_id}:{record_id}` 标识就位（`bitable_record_key` + helper + 条件唯一）。
- work_item FK(null) + work_item_external_id 占位范式就位，供 31-02 反查回填。
- 模型层无落库业务逻辑（守 INV-6 精神，落库归 31-02）。
</success_criteria>

<artifacts_this_phase_produces>
- `server/delivery/models/release.py` — ReleaseBatch / ReleaseRecord / ReleaseArtifact + ReleaseSource / ReleaseArtifactType + `build_bitable_record_key`。
- `server/delivery/migrations/0006_releasebatch_releaserecord_releaseartifact.py` — 三表迁移（已 migrate）。
- `server/tests/delivery/test_release_models.py` — REL-01 守护单测。
- `server/delivery/models/__init__.py`（修改）— Release 符号 re-export。
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/31-release-ledger/31-01-SUMMARY.md` when done
</output>
