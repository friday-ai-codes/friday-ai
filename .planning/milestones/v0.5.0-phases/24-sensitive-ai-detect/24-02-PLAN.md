---
phase: 24-sensitive-ai-detect
plan: 02
type: execute
wave: 2
depends_on: [24-01]
files_modified:
  - server/services/indexer.py
  - server/services/sensitive_detect.py
  - server/tests/services/test_sensitive_detect_llm.py
  - server/tests/repositories/test_sensitive_index_trigger.py
autonomous: true
requirements: [EXCL-03]
must_haves:
  truths:
    - "全量索引完成后，敏感检测在后台 best-effort 触发，不阻断索引终态"
    - "检测抛异常时索引仍返回 success（检测失败 fail-safe，不污染 INDEXED）"
    - "未配置 LLM provider 时，确定性检测仍正常工作（graceful 退化，不阻断）"
    - "LLM 分类只针对启发式未覆盖的可疑配置/文档文件；real_secret 强命中绝不送 LLM"
  artifacts:
    - path: "server/services/indexer.py"
      provides: "run_full_index FINALIZING 末尾经 run_in_background 触发 detect_sensitive_files"
      contains: "detect_sensitive_files"
    - path: "server/services/sensitive_detect.py"
      provides: "可选 LLM 二分类段（classify_ambiguous_files），provider 缺失/失败 graceful 退化"
      contains: "ProviderConfigService"
  key_links:
    - from: "server/services/indexer.py"
      to: "services.background_runner.run_in_background"
      via: "后台派发检测，不阻断索引返回"
      pattern: "run_in_background"
    - from: "server/services/sensitive_detect.py"
      to: "services.provider_config.ProviderConfigService"
      via: "aresolve_or_error → ProviderMissingError graceful 退化"
      pattern: "aresolve_or_error"
---

<objective>
把确定性检测接入索引流程并补上可选 LLM 增强段（EXCL-03 触发 + 增强）：在 `run_full_index` 的 FINALIZING 末尾经 `run_in_background` best-effort 触发 `detect_sensitive_files`（不阻断索引终态）；在 `sensitive_detect.py` 增加可选 LLM 二分类（复用 `ProviderConfigService` + `build_chat_model`，provider 缺失/调用失败时 graceful 退化为纯确定性结果）。

Purpose: 让检测在「索引/描述生成阶段」自动产出建议（D-03），同时对启发式未覆盖的模糊配置文件做「是否可能含敏感信息」分类（D-01），且严守 fail-safe（不阻断索引）与隐私（不把强密钥送 LLM、最小化特征）。
Output: indexer 触发钩子 + LLM 分类段 + 触发/退化 guard 测试。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-sensitive-ai-detect/24-CONTEXT.md
@.planning/phases/24-sensitive-ai-detect/24-01-PLAN.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: run_full_index 后台触发 detect_sensitive_files（best-effort，不阻断）</name>
  <files>server/services/indexer.py, server/tests/repositories/test_sensitive_index_trigger.py</files>
  <read_first>
    - server/services/indexer.py（line 1217-1239：FINALIZING 阶段 RepoSummaryBuilder.build + _refresh_tree_facts 后、`return {"status": "success", ...}` 前的位置——触发点；注意 repo_path 形参可用）
    - server/services/background_runner.py（line 98-185 `run_in_background(coro_factory, name=...)`：须传无参 factory，不能传 coroutine 本体）
    - server/services/sensitive_detect.py（24-01 产出 `detect_sensitive_files(repository_id, repo_path)`）
    - server/tests/repositories/test_indexer_auto_after_index_history.py（索引器测试装配范式：tmp repo + run_full_index 调用）
  </read_first>
  <action>
    在 `run_full_index` 的 FINALIZING 段（`_refresh_tree_facts()` 之后、`return {"status": "success", ...}` 之前）插入敏感检测后台触发：
    - 用 `from services.background_runner import run_in_background` + `from services.sensitive_detect import detect_sensitive_files`（函数体内局部 import，避免顶层循环依赖，与既有 RepoSummaryBuilder 局部 import 风格一致）。
    - 经 `run_in_background(lambda: detect_sensitive_files(self.repository_id, repo_path), name=f"sensitive-detect:{self.repository_id}")` 派发；**不** await 其结果（脱离请求/索引生命周期，best-effort）。
    - 整段包 try/except，捕获派发自身异常并 `logger.warning("sensitive_detect_dispatch_failed", repository_id=..., exc_info=True)`，绝不冒泡——派发失败也不得影响索引 success 终态（D-04 fail-safe）。
    - 不改 `return` 结构与既有 graph/summary 行为（零回归）。
    新增 `server/tests/repositories/test_sensitive_index_trigger.py`：
    - monkeypatch `services.sensitive_detect.detect_sensitive_files`（或在其被 import 的位置）为可断言被调用的 stub，断言 run_full_index 完成后该 stub 收到 (repository_id, repo_path)。
    - 另一例：把 stub 设为 `raise RuntimeError("boom")`（并在后台 runner 路径上确保异常被吞），断言 run_full_index 仍返回 status=="success"（检测失败不阻断索引）。测试中用 background_runner 的 `wait_for_pending` / `_reset_for_tests` 控制后台任务收敛。
  </action>
  <verify>
    <automated>cd server && .venv/bin/python -m pytest tests/repositories/test_sensitive_index_trigger.py -x -q 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - 检测经 `run_in_background` 派发（传 factory，非 coroutine），index 不 await 其结果。
    - 检测 stub 抛异常时 run_full_index 仍返回 success（fail-safe 断言通过）。
    - 触发点位于 FINALIZING 之后、return 之前；既有 graph/summary 路径未改动。
  </acceptance_criteria>
  <done>索引完成自动后台触发检测，检测失败不阻断索引，guard 测试全绿。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 可选 LLM 二分类段（graceful 退化 + 最小化特征 + 不送强密钥）</name>
  <files>server/services/sensitive_detect.py, server/tests/services/test_sensitive_detect_llm.py</files>
  <read_first>
    - server/knowledge/llm_grader.py（line 39-103：一次性 LLM 调用权威范式——`ProviderConfigService.aresolve_or_error()` → `ProviderMissingError` 退化、`build_chat_model(resolved, model_name, streaming=False)`、`model.ainvoke([system, human])`、`content_to_text`、JSON 解析、try/except 整体退化）
    - server/agents/llm_factory.py（line 64-175 `build_chat_model` 签名；`content_to_text`）
    - server/services/provider_config.py（line 740-787 `aresolve_or_error` 返回 `ResolvedProviderConfig | ProviderMissingError`；`extra["default_model"]` 取模型名）
    - server/services/sensitive_detect.py（24-01：复用 SuggestionCandidate / _upsert_suggestion / severity 常量）
  </read_first>
  <behavior>
    - Test: provider 未配置（monkeypatch `aresolve_or_error` 返回 `ProviderMissingError`）→ `classify_ambiguous_files` 返回空增量，确定性检测结果不受影响（graceful 退化，不抛）。
    - Test: provider 调用抛异常（monkeypatch build_chat_model/ainvoke raise）→ 同样退化为空增量，不冒泡。
    - Test: provider 可用 + fake model 返回 JSON 判定某 ambiguous 文件「sensitive=true」→ 产出 severity=likely_sensitive, detector=llm 的建议（经 _upsert_suggestion 入库）。
    - Test: 已被确定性段判为 real_secret 的文件**不**进入 LLM 候选集（断言传给 model 的输入不含该文件名/内容）。
    - Test: 传给 LLM 的 human 内容只含文件名 + 截断/最小化特征，**不含**任何命中的密钥值（断言密钥值 not in 调用入参）。
  </behavior>
  <action>
    在 `server/services/sensitive_detect.py` 增加可选 LLM 段：
    - `async def classify_ambiguous_files(repository_id, candidates_or_paths, ...)`：仅接收启发式未覆盖但「可疑」的配置/文档类文件（real_secret 强命中显式排除，绝不入候选）。
    - 解析 provider：`from services.provider_config import ProviderConfigService, ProviderMissingError`；`resolved = await ProviderConfigService.aresolve_or_error()`；`isinstance(resolved, ProviderMissingError)` → 直接返回空（graceful，不抛，logger.info 记一次「llm_skipped_no_provider」无敏感内容）。
    - 取 `model_name = (resolved.extra or {}).get("default_model", "")`，缺失 → 退化返回空。
    - `from agents.llm_factory import build_chat_model, content_to_text`；`model = build_chat_model(resolved, model_name, streaming=False)`。
    - SystemMessage 指示「判断文件是否可能包含敏感信息，严格输出 JSON 数组，每项含 path、sensitive(bool)、reason(中文一句，不要回显任何密钥值)」；HumanMessage 只携带 **文件名 + 最小化特征**（如扩展名、是否含 `key/secret/token` 关键字的布尔、首行非敏感摘要或截断 N 字符且经 _redact_reason 脱敏）——构造特征时复用确定性脱敏，确保不把密钥明文送外部 provider（D-04）。
    - 整段 try/except 包裹：任何异常（解析、网络、JSON）→ logger.warning("sensitive_llm_classify_failed", error=str(exc)) 并返回空增量（绝不阻断、绝不冒泡）。
    - 对判定 sensitive=true 的文件调 `_upsert_suggestion`（severity=likely_sensitive, detector=llm, reason 取 LLM 中文理由并经 `_redact_reason` 兜底过滤）。
    - 不在本 plan 把 LLM 段默认接入 `detect_sensitive_files` 的强制路径：以「确定性段始终启用、LLM 段对 ambiguous 子集可选调用」编排，确定性结果不依赖 LLM 成功（Claude 自行决定是否在 detect_sensitive_files 内对 ambiguous 子集顺带调用 classify_ambiguous_files，但必须保证 LLM 失败时确定性结果已落库）。
  </action>
  <verify>
    <automated>cd server && .venv/bin/python -m pytest tests/services/test_sensitive_detect_llm.py -x -q 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - provider 缺失/失败两路径均 graceful 退化为空增量且确定性结果不受损（测试锁定）。
    - real_secret 文件不入 LLM 候选；传给 model 的入参不含密钥值（脱敏断言通过）。
    - LLM 命中产出 severity=likely_sensitive, detector=llm，经统一 `_upsert_suggestion` 入库。
  </acceptance_criteria>
  <done>可选 LLM 分类段落地，graceful 退化 + 最小化特征 + 强密钥不外送均被测试锁定。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 检测器 → 索引主流程 | 后台检测的任何失败不得阻断索引 INDEXED 终态 |
| 文件内容 → 外部 LLM provider | 密钥明文绝不能随分类请求外送 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-05 | Denial of Service | run_full_index 触发段 | mitigate | run_in_background 后台派发 + try/except 吞派发异常；检测失败索引仍 success（测试锁定 fail-safe） |
| T-24-06 | Information Disclosure | LLM human 输入 | mitigate | 只送文件名 + 最小化脱敏特征；real_secret 强命中排除出候选；断言密钥值 not in 入参 |
| T-24-07 | Availability | LLM provider 缺失/超时 | mitigate | aresolve_or_error → ProviderMissingError graceful 退化；整段 try/except 返回空增量，确定性结果不受影响 |
| T-24-08 | Tampering | 误标 / 静默删 | mitigate | LLM 段仅产 likely_sensitive 的 pending 建议，绝不建规则/删数据 |
| T-24-SC | Tampering | npm/pip 安装 | accept | 复用既有 langchain/provider_config 栈，无新增依赖 |
</threat_model>

<verification>
- `cd server && .venv/bin/python -m pytest tests/repositories/test_sensitive_index_trigger.py tests/services/test_sensitive_detect_llm.py -q` 全绿。
- `grep -n "run_in_background" server/services/indexer.py` 命中敏感检测派发；触发位于 return success 前。
- `grep -n "aresolve_or_error" server/services/sensitive_detect.py` 命中（LLM 退化路径）。
- `ruff check server/services/sensitive_detect.py server/services/indexer.py` 0 错。
</verification>

<success_criteria>
- 索引完成自动后台触发敏感检测，检测失败不阻断索引。
- LLM 分类可选、graceful 退化、最小化特征、强密钥不外送，确定性段始终可用。
</success_criteria>

<output>
Create `.planning/phases/24-sensitive-ai-detect/24-02-SUMMARY.md` when done
</output>
