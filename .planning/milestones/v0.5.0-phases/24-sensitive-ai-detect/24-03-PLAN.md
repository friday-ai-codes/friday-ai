---
phase: 24-sensitive-ai-detect
plan: 03
type: execute
wave: 2
depends_on: [24-01]
files_modified:
  - server/repositories/serializers.py
  - server/repositories/views.py
  - server/repositories/urls.py
  - server/tests/repositories/test_sensitive_suggestions_api.py
autonomous: true
requirements: [EXCL-03]
must_haves:
  truths:
    - "用户可列出某仓库的 AI 敏感文件建议（按 severity 排序，real_secret 优先）"
    - "接受建议 → 创建 RepoExclusionRule(source=ai_suggested) 并把 suggestion 标 accepted"
    - "忽略建议 → 标 dismissed，不建规则、不删任何数据"
    - "accept/dismiss 绝不静默删除已索引数据；删除仍由既有 reconcile/cleanup 显式触发"
  artifacts:
    - path: "server/repositories/views.py"
      provides: "RepositorySensitiveSuggestionsView(list) + RepositorySensitiveSuggestionActionView(accept/dismiss)"
      contains: "class RepositorySensitiveSuggestion"
    - path: "server/repositories/urls.py"
      provides: "sensitive-suggestions 路由（list + action）"
      contains: "sensitive-suggestions"
    - path: "server/repositories/serializers.py"
      provides: "SensitiveFileSuggestionSerializer"
      contains: "SensitiveFileSuggestionSerializer"
  key_links:
    - from: "server/repositories/views.py"
      to: "repositories.models.RepoExclusionRule"
      via: "accept 创建 source=ai_suggested 规则 + invalidate_matcher_cache"
      pattern: "ai_suggested"
    - from: "server/repositories/views.py"
      to: "services.exclusion.invalidate_matcher_cache"
      via: "accept 建规则后失效匹配器缓存"
      pattern: "invalidate_matcher_cache"
---

<objective>
暴露敏感文件建议的 REST 工作流（EXCL-03 的「建议 + 确认」面）：列出建议、接受（→ 建 `RepoExclusionRule(source=ai_suggested)` 并标 accepted）、忽略（标 dismissed）。复用 Phase 22 的规则创建路径（serializer 校验 + `invalidate_matcher_cache`），绝不自动建规则/删数据——删除仍走既有 Phase 23 reconcile/cleanup 由用户显式触发。

Purpose: 兑现 D-03 工作流契约并接通 Phase 22/23 既有面；为 24-04 前端面板提供稳定 API 契约。
Output: serializer + 两个 APIView + 路由 + API guard 测试。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-sensitive-ai-detect/24-CONTEXT.md
@.planning/phases/24-sensitive-ai-detect/24-01-PLAN.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: SensitiveFileSuggestionSerializer</name>
  <files>server/repositories/serializers.py</files>
  <read_first>
    - server/repositories/serializers.py（line 172-218 `RepoExclusionRuleSerializer`：ModelSerializer 风格、fields/read_only_fields 写法、zh-CN docstring）
    - server/repositories/models.py（24-01 新增 `SensitiveFileSuggestion` 字段集）
  </read_first>
  <action>
    在 `server/repositories/serializers.py` 新增 `SensitiveFileSuggestionSerializer(serializers.ModelSerializer)`：
    - Meta.model = SensitiveFileSuggestion；fields = ["id", "path", "severity", "detector", "reason", "status", "detected_at", "updated_at"]；全部 read_only（建议由检测器产出，API 只读 + 通过专用 action 改 status，不允许直接 PATCH 任意字段）。
    - zh-CN docstring 说明 reason 已脱敏、severity 取值与排序含义。
  </action>
  <verify>
    <automated>cd server && .venv/bin/python -m pytest tests/repositories/test_sensitive_suggestions_api.py -x -q 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - 序列化器字段集与模型一致，全部 read_only。
    - 不暴露除既定字段外的任何字段（reason 仍为脱敏文本）。
  </acceptance_criteria>
  <done>序列化器落地，供 list/action 视图复用。</done>
</task>

<task type="auto">
  <name>Task 2: list / accept / dismiss 视图 + 路由</name>
  <files>server/repositories/views.py, server/repositories/urls.py, server/tests/repositories/test_sensitive_suggestions_api.py</files>
  <read_first>
    - server/repositories/views.py（line 1006-1113 `RepositoryExclusionRulesView` / `RepositoryExclusionRuleDetailView`：APIView async get/post/delete 范式、Repository.objects.aget 404、`RepoExclusionRule.objects.acreate(...)` + `invalidate_matcher_cache(str(repository_id))`、IntegrityError → 400；line 25 import `invalidate_matcher_cache`）
    - server/repositories/urls.py（line 41-44, 241-259：既有 exclusions / reconcile 路由注册范式）
    - server/repositories/models.py（`RepoExclusionRule.Source.AI_SUGGESTED`、`SensitiveFileSuggestion.Status`）
    - server/tests/repositories/test_exclusion_api.py（API 测试装配范式：async test、认证 client、断言 DB 行）
  </read_first>
  <action>
    在 `server/repositories/views.py` 新增两个 APIView（permission_classes=[IsAuthenticated]，async 方法，Repository.aget 不存在→404，与既有面一致）：
    1. `RepositorySensitiveSuggestionsView.get(request, repository_id)`：列出该仓库建议。默认仅 status=pending（支持 `?status=` 过滤 pending/accepted/dismissed/all），排序 real_secret > likely_sensitive > config_review 再按 detected_at desc（可用 DB 注解或 Python 侧排序映射）。经 `sync_to_async(lambda: SensitiveFileSuggestionSerializer(rows, many=True).data)` 返回 `{"suggestions": [...]}`。
    2. `RepositorySensitiveSuggestionActionView.post(request, repository_id, suggestion_id)`：body `{action: "accept"|"dismiss"}`：
       - 取本仓 suggestion（越仓/不存在→404）。
       - action=="dismiss"：仅置 status=dismissed 并 asave；**不**建规则、**不**删数据。返回更新后的序列化。
       - action=="accept"：在事务/幂等下创建 `RepoExclusionRule(repository_id=..., pattern=suggestion.path, rule_type=RuleType.GLOB, source=Source.AI_SUGGESTED, enabled=True)`（用 `aget_or_create` 或捕获 IntegrityError 实现幂等，避免重复接受报错）；置 suggestion.status=accepted 并 asave；调 `invalidate_matcher_cache(str(repository_id))` 使各读取面即时生效。返回 `{"suggestion": <序列化>, "rule": <RepoExclusionRuleSerializer 序列化>}`。
       - 非法 action → 400。
       - **绝不**在 accept 路径触发任何删除/清理（删除留给既有 reconcile/cleanup 由用户显式发起，response 可附 `cleanup_available: true` 提示前端引导，但不自动派发）。
    在 `server/repositories/urls.py` 注册：
    - `<uuid:repository_id>/sensitive-suggestions/` → list（name="repository-sensitive-suggestions"）。
    - `<uuid:repository_id>/sensitive-suggestions/<uuid:suggestion_id>/action/` → action（name="repository-sensitive-suggestion-action"）。
    新增 `server/tests/repositories/test_sensitive_suggestions_api.py`，覆盖：
    - list 返回 pending 建议且 real_secret 排在前。
    - accept → DB 出现 `RepoExclusionRule(source="ai_suggested", pattern=<path>, rule_type="glob")` 且 suggestion.status=="accepted"；断言**没有**任何索引/派生数据被删（无 CleanupRun 创建、无 purge 调用）。
    - accept 幂等：对同一 path 二次 accept 不抛 500（规则不重复创建）。
    - dismiss → status=="dismissed" 且无 RepoExclusionRule 创建。
    - 越仓 suggestion_id → 404；非法 action → 400。
  </action>
  <verify>
    <automated>cd server && .venv/bin/python -m pytest tests/repositories/test_sensitive_suggestions_api.py -x -q 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - accept 创建 `RepoExclusionRule(source=ai_suggested, rule_type=glob)` + 标 accepted + invalidate 缓存；幂等不报错。
    - accept/dismiss 路径均无删除/清理副作用（测试断言无 CleanupRun/purge）。
    - dismiss 仅改 status；越仓 404、非法 action 400。
    - list 按 severity 排序（real_secret 优先）。
  </acceptance_criteria>
  <done>list/accept/dismiss 三端点 + 路由落地，accept 接通 Phase 22 规则面，绝不静默删，guard 测试全绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 客户端 → 建议 action API | 越仓访问、未授权改 status、误触发删除 |
| accept → 排除规则/清理面 | 接受动作绝不能隐式删除已索引/派生数据 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-09 | Elevation of Privilege | action/list 视图 | mitigate | IsAuthenticated + 以 repository_id 限定查询；越仓 suggestion_id → 404（不泄漏存在性） |
| T-24-10 | Tampering | accept 路径 | mitigate | accept 只建 ai_suggested 规则 + 标 accepted，绝不删数据；测试断言无 CleanupRun/purge 副作用（NEVER silent-delete） |
| T-24-11 | Information Disclosure | 序列化输出 | mitigate | 序列化器仅暴露脱敏 reason 与既定字段，不回显文件内容/密钥 |
| T-24-12 | Denial of Service | 重复 accept | mitigate | aget_or_create / IntegrityError 幂等，避免唯一约束冲突 500 |
| T-24-SC | Tampering | npm/pip 安装 | accept | 无新增依赖（复用 DRF + 既有 serializer/exclusion 面） |
</threat_model>

<verification>
- `cd server && .venv/bin/python -m pytest tests/repositories/test_sensitive_suggestions_api.py -q` 全绿。
- `grep -n "ai_suggested\|AI_SUGGESTED" server/repositories/views.py` 命中 accept 路径。
- `grep -n "invalidate_matcher_cache" server/repositories/views.py` 在 accept 后调用。
- `ruff check server/repositories/views.py server/repositories/serializers.py` 0 错。
</verification>

<success_criteria>
- 三端点可列出/接受/忽略建议；accept 接通 Phase 22 `RepoExclusionRule(source=ai_suggested)`。
- 全程不静默删除已索引数据，删除仍由既有 reconcile/cleanup 显式触发。
</success_criteria>

<output>
Create `.planning/phases/24-sensitive-ai-detect/24-03-SUMMARY.md` when done
</output>
