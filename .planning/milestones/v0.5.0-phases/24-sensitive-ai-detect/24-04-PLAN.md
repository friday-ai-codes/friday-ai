---
phase: 24-sensitive-ai-detect
plan: 04
type: execute
wave: 3
depends_on: [24-03]
files_modified:
  - web/src/api/sensitiveSuggestions.ts
  - web/src/components/repository/SensitiveSuggestionsPanel.vue
  - web/src/components/repository/__tests__/SensitiveSuggestionsPanel.spec.ts
  - web/src/locales/zh-CN.json
  - web/src/pages/repositories/[id]/index.vue
autonomous: true
requirements: [EXCL-03]
must_haves:
  truths:
    - "用户能在仓库详情页看到 AI 敏感文件建议列表（按 severity 排序）"
    - "real_secret 建议以高优先级告警样式呈现（醒目危险色 + 图标）"
    - "用户能接受建议（建排除规则）或忽略建议；接受/忽略后列表即时更新"
    - "接受不触发静默删除；UI 引导用户用既有清理面板显式清理"
  artifacts:
    - path: "web/src/api/sensitiveSuggestions.ts"
      provides: "类型化 client：list + action(accept/dismiss)"
      contains: "sensitiveSuggestionsApi"
    - path: "web/src/components/repository/SensitiveSuggestionsPanel.vue"
      provides: "建议列表面板 + real_secret 告警 + accept/dismiss 操作"
      contains: "SensitiveSuggestionsPanel"
  key_links:
    - from: "web/src/components/repository/SensitiveSuggestionsPanel.vue"
      to: "web/src/api/sensitiveSuggestions.ts"
      via: "useQuery list + useMutation action"
      pattern: "sensitiveSuggestionsApi"
    - from: "web/src/pages/repositories/[id]/index.vue"
      to: "SensitiveSuggestionsPanel"
      via: "在 #exclusions section 内挂载（ExclusionRulesPanel/ReconcilePanel 旁）"
      pattern: "SensitiveSuggestionsPanel"
---

<objective>
兑现 EXCL-03 的用户可见闭环：在仓库详情页排除区新增「AI 建议敏感文件」面板——按 severity 排序展示建议、real_secret 高优先级告警样式、接受（建规则）/忽略（dismiss）操作，并引导用户用既有清理面板做删除（绝不静默删）。

Purpose: 让「建议 + 提醒 + 用户确认」工作流对用户真正可达（D-03），接通 24-03 REST 契约与 Phase 22/23 既有面板。
Output: 类型化 API client + 面板组件 + 页面挂载 + zh-CN 文案 + 守护测试。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-sensitive-ai-detect/24-CONTEXT.md
@.planning/phases/24-sensitive-ai-detect/24-03-PLAN.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: sensitiveSuggestions API client + zh-CN 文案</name>
  <files>web/src/api/sensitiveSuggestions.ts, web/src/locales/zh-CN.json</files>
  <read_first>
    - web/src/api/exclusions.ts（全文：client import { del, get, post }、interface 与 api 对象导出范式、zh-CN 注释风格——对齐其结构）
    - web/src/api/reconcile.ts（getReconcile/cleanup/getCleanupStatus 类型与端点拼接范式）
    - web/src/locales/zh-CN.json（reconcile.* / exclusion 相关命名空间，新增 sensitive.* 命名空间需与既有缩进/风格一致）
    - 24-03-PLAN.md（REST 契约：GET /repositories/{id}/sensitive-suggestions/?status= → {suggestions:[]}；POST .../{suggestion_id}/action/ {action} → {suggestion, rule?}）
  </read_first>
  <action>
    新建 `web/src/api/sensitiveSuggestions.ts`：
    - `export type SensitiveSeverity = 'real_secret' | 'likely_sensitive' | 'config_review'`；`export type SensitiveDetector = 'heuristic' | 'content' | 'llm'`；`export type SensitiveStatus = 'pending' | 'accepted' | 'dismissed'`。
    - `interface SensitiveSuggestion { id; path; severity; detector; reason; status; detected_at; updated_at }`（字段与 24-03 序列化器对齐）。
    - `interface SensitiveSuggestionListResponse { suggestions: SensitiveSuggestion[] }`。
    - `interface SensitiveActionResponse { suggestion: SensitiveSuggestion; rule?: { id; pattern; rule_type; source } }`。
    - `sensitiveSuggestionsApi`：`list(repoId, status?)` → `get(...sensitive-suggestions/?status=...)`；`accept(repoId, id)` / `dismiss(repoId, id)` → `post(...{id}/action/, { action })`。
    - zh-CN 注释说明 reason 已脱敏。
    在 `web/src/locales/zh-CN.json` 新增 `sensitive.*` 命名空间（全中文）：面板标题/副标题、severity 标签（真实密钥/疑似敏感/建议复核）、detector 标签、空态、real_secret 告警标题与说明（如「检测到真实密钥，高优先级，请尽快处理」）、接受/忽略按钮、接受确认（建排除规则）、接受成功提示（含「如需清除已索引内容，请在下方清理面板执行」引导，不自动删）、忽略提示、加载/错误文案。
  </action>
  <verify>
    <automated>cd web && pnpm exec vue-tsc --noEmit 2>&1 | grep -i "sensitiveSuggestions\|SensitiveSuggestions" | head -5; echo "tsc-checked"</automated>
  </verify>
  <acceptance_criteria>
    - client 类型与 24-03 契约一致；list 支持 status 过滤参数。
    - `sensitive.*` 文案全中文，含 real_secret 告警与「不自动删、引导显式清理」措辞。
    - vue-tsc 对该文件无类型错误。
  </acceptance_criteria>
  <done>API client + zh-CN 文案落地，契约与 24-03 对齐。</done>
</task>

<task type="auto">
  <name>Task 2: SensitiveSuggestionsPanel.vue + 页面挂载 + 守护测试</name>
  <files>web/src/components/repository/SensitiveSuggestionsPanel.vue, web/src/components/repository/__tests__/SensitiveSuggestionsPanel.spec.ts, web/src/pages/repositories/[id]/index.vue</files>
  <read_first>
    - web/src/components/repository/ReconcilePanel.vue（全文：useQuery/useMutation + useConfirmDialog/useToast/useErrorHandler 模式、reka-ui 组件用法、destructive 告警样式、refetch/invalidate 范式、zh-CN i18n key 使用）
    - web/src/components/repository/ExclusionRulesPanel.vue（列表渲染 + 行操作按钮 + severity/badge 样式参照）
    - web/src/components/repository/__tests__/ReconcilePanel.spec.ts（守护测试范式：真实 zh-CN.json 作 i18n messages、vue-test-utils 装配、mutation 断言、createI18n messages 用 zhCN as any）
    - web/src/pages/repositories/[id]/index.vue（`#exclusions` section：ExclusionRulesPanel + ReconcilePanel 挂载点，本面板挂在其旁）
  </read_first>
  <action>
    新建 `web/src/components/repository/SensitiveSuggestionsPanel.vue`（`<script setup lang="ts">`，props: `repoId: string`）：
    - `useQuery(['repository-sensitive-suggestions', repoId], () => sensitiveSuggestionsApi.list(repoId))` 拉 pending 建议。
    - 渲染：标题 + 副标题；建议列表按 severity 排序（后端已排序，前端保序渲染）；每行展示 path、severity badge（real_secret 危险色、likely_sensitive 警示色、config_review 中性色）、detector 标签、脱敏 reason。
    - real_secret：在列表顶部或行内以高优先级告警样式（destructive 色 + shield-alert/triangle-alert 图标 + `sensitive.realSecretAlert*` 文案）突出，确保用户第一眼可见。
    - 行操作：`接受`（useMutation accept + `useConfirmDialog`：确认将建排除规则，提示「不会自动删除已索引内容，需在下方清理面板显式执行」）→ 成功 toast + `invalidateQueries(['repository-sensitive-suggestions', repoId])` 并 invalidate exclusions 列表（`['repository-exclusions', repoId]` 若存在键）使新规则即时显现；`忽略`（useMutation dismiss）→ 成功后 invalidate 列表。
    - 空态：无 pending 建议时渲染 `sensitive.empty` 文案（不报错、不渲染告警）。
    - 错误：useErrorHandler 兜底，加载态 skeleton/loading 文案。
    在 `web/src/pages/repositories/[id]/index.vue` import 并在 `#exclusions` section 内挂 `<SensitiveSuggestionsPanel :repo-id="..." />`（ExclusionRulesPanel / ReconcilePanel 旁，顺序合理即可）。
    新建守护测试 `__tests__/SensitiveSuggestionsPanel.spec.ts`（真实 zh-CN.json 作 i18n messages，`zhCN as any`）：
    - (a) list 返回含 real_secret + likely_sensitive → 渲染两行，real_secret 行带告警样式/文案断言（断真实 zh-CN 文案，防被改空）。
    - (b) 点击接受 → 触发 `sensitiveSuggestionsApi.accept(repoId, id)`（mock 断言入参），确认弹窗含「不会自动删除/需显式清理」措辞断言。
    - (c) 点击忽略 → 触发 `dismiss`，列表 invalidate 后该建议消失（mock 二次 list 返回去掉它）。
    - (d) 空 suggestions → 渲染空态、无告警。
  </action>
  <verify>
    <automated>cd web && pnpm vitest run src/components/repository/__tests__/SensitiveSuggestionsPanel.spec.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - real_secret 建议有高优先级告警样式且断言真实 zh-CN 文案。
    - 接受触发 accept（建规则）且确认弹窗明示「不自动删、需显式清理」；忽略触发 dismiss。
    - accept/dismiss 后列表即时更新（invalidate）。
    - 空态正确渲染；4 例守护测试全绿。
  </acceptance_criteria>
  <done>建议面板落地、挂载到仓库详情页、守护测试全绿，EXCL-03 用户可见闭环达成。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 前端展示 → 用户 | 不得在 UI 回显密钥本体（仅展示后端脱敏 reason） |
| 接受动作 → 删除预期 | UI 不得让用户误以为接受即清除已索引内容 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-13 | Information Disclosure | 面板渲染 | mitigate | 仅渲染后端脱敏 reason；不构造/展示原始文件内容或密钥 |
| T-24-14 | Tampering | 接受按钮语义 | mitigate | 确认弹窗明示「建规则、不自动删除、需在清理面板显式清理」；测试断言该措辞（防过度删除承诺/误导） |
| T-24-15 | Repudiation | real_secret 可见性 | mitigate | real_secret 高优先级告警样式，确保用户必然注意到真实密钥（不被淹没在普通建议中） |
| T-24-SC | Tampering | npm 安装 | accept | 复用既有 client / TanStack Query / reka-ui / vue-i18n 栈，无新增 npm 依赖 |
</threat_model>

<verification>
- `cd web && pnpm vitest run src/components/repository/__tests__/SensitiveSuggestionsPanel.spec.ts` 全绿（4 例）。
- `cd web && pnpm exec vue-tsc --noEmit 2>&1 | grep -ci "sensitive"` → 0（无 sensitive 相关类型错误）。
- `cd web && pnpm exec eslint src/api/sensitiveSuggestions.ts src/components/repository/SensitiveSuggestionsPanel.vue` 0 错。
- `grep -n "SensitiveSuggestionsPanel" web/src/pages/repositories/\[id\]/index.vue` 命中挂载。
</verification>

<success_criteria>
- 仓库详情页可见 AI 敏感文件建议面板，real_secret 高优先级告警。
- 接受（建规则）/忽略可达且列表即时更新；接受不静默删，UI 引导显式清理。
</success_criteria>

<output>
Create `.planning/phases/24-sensitive-ai-detect/24-04-SUMMARY.md` when done
</output>
