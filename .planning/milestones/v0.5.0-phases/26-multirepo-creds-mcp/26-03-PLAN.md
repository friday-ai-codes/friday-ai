---
phase: 26-multirepo-creds-mcp
plan: 03
type: execute
wave: 2
depends_on: ["26-01"]
files_modified:
  - server/mcp_tools/merge_request_service.py
  - server/workflows/services/mr_service.py
  - server/workflows/nodes/ai/coding.py
  - server/chat/coding_session_service.py
  - server/knowledge/diff_archive.py
  - server/tests/test_git_credential_platform_wiring.py
autonomous: true
requirements: [REPO-01]
must_haves:
  truths:
    - "git 平台客户端（MR/PR 创建）在仓库无 per-repo token 时按 host 用实例凭证创建 client"
    - "编码容器 dispatch 注入的 git access_token 经解析器取得（无 per-repo token 时用实例凭证）"
    - "diff archive 拉取经解析器取 token"
    - "仓库有 per-repo token 时所有平台/dispatch 路径仍优先用 per-repo token"
    - "token 绝不进日志，也不回显到 dispatch 元数据以外的明文出口"
  artifacts:
    - path: "server/mcp_tools/merge_request_service.py"
      provides: "_get_client 经解析器取 token"
      contains: "aresolve_git_token"
    - path: "server/workflows/services/mr_service.py"
      provides: "MR 创建经解析器取 token"
      contains: "aresolve_git_token"
    - path: "server/workflows/nodes/ai/coding.py"
      provides: "dispatch git_credentials + MR 创建经解析器取 token"
      contains: "aresolve_git_token"
  key_links:
    - from: "server/mcp_tools/merge_request_service.py"
      to: "services.git_credentials.aresolve_git_token"
      via: "替换内联 GitCredential 取 token 再 get_git_platform_client"
      pattern: "aresolve_git_token"
    - from: "server/chat/coding_session_service.py"
      to: "services.git_credentials"
      via: "dispatch token 注入经解析器"
      pattern: "resolve_git_token"
---

<objective>
把 26-01 的凭证解析器接入「git 平台 API 客户端（MR/PR 创建）+ 编码容器 dispatch 的 git token 注入 + diff archive 拉取」路径，使无 per-repo token 的仓库能按 host 用实例凭证调用 git 平台与派发编码任务；per-repo token 存在时仍优先（向后兼容）。

Purpose: 兑现 REPO-01「同一 GitLab 实例多仓复用凭证」在 git 平台 / 编码 dispatch 路径的可观察行为（D-02 接线点）；与 26-02 共同消除所有散落取 token 逻辑。
Output: 五处取 token 路径改为调用解析器 + git 平台守护测试。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/26-multirepo-creds-mcp/26-CONTEXT.md
@.planning/phases/26-multirepo-creds-mcp/26-01-PLAN.md
</context>

<artifacts_this_phase_produces>
- git 平台客户端（mcp/workflows MR）、编码 dispatch token 注入、diff archive 取 token 统一经解析器
- git 平台守护测试：同 host 多仓共享实例凭证 + per-repo 优先 + token 不泄漏
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: git 平台客户端（MR 创建）接入解析器</name>
  <files>server/mcp_tools/merge_request_service.py, server/workflows/services/mr_service.py, server/workflows/nodes/ai/coding.py</files>
  <read_first>
    - server/mcp_tools/merge_request_service.py L22-L27（`_get_client`：GitCredential.afirst() → decrypt_value → get_git_platform_client(repo, token)；无凭证 raise MergeRequestToolError("仓库缺少 Git 平台访问凭据")）
    - server/workflows/services/mr_service.py L132-L144（getattr credential → decrypt_value → get_git_platform_client；无凭证 return MRCreateResult(success=False, error="No API token configured for repository")）
    - server/workflows/nodes/ai/coding.py L1161-L1177（select_related credential → decrypt_value → get_git_platform_client，MR 创建路径；无凭证 log warning + 早返回）
    - server/services/git_platform/__init__.py L113-L130（get_git_platform_client(repository, token) 签名——只吃 token，解析器只需供 token）
    - server/services/git_credentials.py（aresolve_git_token 异步入口）
  </read_first>
  <action>
    三处 git 平台 client 构造均为机械替换「内联取 per-repo token」→「解析器取 token」，保留各自既有缺凭证报错文案（解析器返回 None 时走原 raise/返回分支，行为不回退）：
    - merge_request_service._get_client：`token = await aresolve_git_token(repository)`；token 为 None 时仍 raise MergeRequestToolError("仓库缺少 Git 平台访问凭据")；非 None 走 get_git_platform_client(repository, token)。
    - workflows/services/mr_service.py：`token = await aresolve_git_token(repository)`；None → 保留 MRCreateResult(success=False, error="No API token configured for repository")。
    - workflows/nodes/ai/coding.py L1161-1177（MR 创建段）：`token = await aresolve_git_token(repository)`；None → 保留既有 log warning + 空 mr_url 早返回。
    绝不新增 token 日志。get_git_platform_client 调用契约不变。
  </action>
  <verify>
    <automated>cd server && uv run ruff check mcp_tools/merge_request_service.py workflows/services/mr_service.py workflows/nodes/ai/coding.py</automated>
  </verify>
  <acceptance_criteria>
    - 三处 git 平台 client 构造经 `aresolve_git_token(repository)` 取 token，无内联解密残留。
    - 缺凭证报错文案/早返回行为零回退；per-repo token 存在时优先。
  </acceptance_criteria>
  <done>MR/PR git 平台客户端取 token 统一经解析器。</done>
</task>

<task type="auto">
  <name>Task 2: 编码 dispatch token 注入 + diff archive 接入解析器</name>
  <files>server/workflows/nodes/ai/coding.py, server/chat/coding_session_service.py, server/knowledge/diff_archive.py</files>
  <read_first>
    - server/workflows/nodes/ai/coding.py L920-L988（git_credentials = {"access_token": token, "ssl_verify": ...}，注入容器 dispatch payload。token 经 repository.credential.encrypted_token 解密；ssl_verify 取自 per-repo credential）
    - server/chat/coding_session_service.py L170-L180 与 L310-L318（两处 `if cred.encrypted_token: token = decrypt_value(...)` 编码会话 token 注入）
    - server/knowledge/diff_archive.py L560-L570（`token = decrypt_value(cred.encrypted_token or "")` diff 拉取）
    - server/services/git_credentials.py（aresolve_git_token / resolve_git_token_sync）
  </read_first>
  <action>
    - coding.py dispatch 段（L920-988）：`token = await aresolve_git_token(repository)`；`git_credentials` 仅在 token 非空时注入 access_token；`ssl_verify` 仍取自 per-repo credential（存在时），无 per-repo credential（纯靠实例池）时 ssl_verify 取既有默认（保持 str 小写布尔）。token 为空时 git_credentials 维持空 dict（与既有「无凭证」行为一致）。绝不 log token。
    - coding_session_service.py 两处（L170-180 / L310-318）：把 `decrypt_value(cred.encrypted_token)` 取 token 替换为经解析器取 token（async 上下文用 `await aresolve_git_token(repo)`，sync 上下文用 `resolve_git_token_sync(repo)`——按各自函数是否 async 判定）；保留 token 为空时的既有降级路径。
    - diff_archive.py L560-570：替换为经解析器取 token（按上下文 async/sync 选入口），空 token 行为不变。
    所有改动机械且零新增 token 日志。
  </action>
  <verify>
    <automated>cd server && uv run ruff check workflows/nodes/ai/coding.py chat/coding_session_service.py knowledge/diff_archive.py</automated>
  </verify>
  <acceptance_criteria>
    - dispatch git_credentials、编码会话 token、diff archive 取 token 均经解析器；无 per-repo token 时按 host 用实例凭证。
    - ssl_verify 取值与空 token 降级行为零回退；token 不进日志/不进 dispatch 之外的明文出口。
  </acceptance_criteria>
  <done>编码 dispatch 与 diff archive 取 token 统一经解析器。</done>
</task>

<task type="auto">
  <name>Task 3: git 平台守护测试（同 host 共享 + per-repo 优先 + 不泄漏）</name>
  <files>server/tests/test_git_credential_platform_wiring.py</files>
  <read_first>
    - server/tests/mcp_tools/test_mr_tools.py（MR 工具既有测试 fixture / mock get_git_platform_client 的范式）
    - server/tests/test_coding_node.py / server/tests/test_coding_session_service.py（mock_cred.encrypted_token 的既有 mock 风格）
    - server/services/git_credentials.py（解析器入口）
  </read_first>
  <action>
    新建 test_git_credential_platform_wiring.py，patch `get_git_platform_client` 捕获传入 token（不真正打 git 平台 API）：
    - Test 1（同 host 共享）：两个同 host、无 per-repo token 的 repo + 一个 GitInstanceCredential，断言 merge_request_service._get_client 对两 repo 都用实例 token 构造 client。
    - Test 2（per-repo 优先）：repo 同时有 per-repo token + 同 host 实例凭证，断言平台 client 用 per-repo token。
    - Test 3（缺凭证报错不回退）：repo 无 per-repo token 且 host 无实例凭证 → _get_client 仍 raise MergeRequestToolError("仓库缺少 Git 平台访问凭据")。
    - Test 4（不泄漏）：捕获日志断言平台/ dispatch 取 token 路径不含 token 明文子串。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/test_git_credential_platform_wiring.py -x</automated>
  </verify>
  <acceptance_criteria>
    - 同 host 两仓共享实例凭证调平台 API；per-repo 优先；缺凭证报错文案不回退；日志无 token——四测全绿。
    - 测试驱动真实接线入口（_get_client 等），非仅解析器。
  </acceptance_criteria>
  <done>git 平台守护测试全绿，REPO-01 平台/dispatch 行为兑现。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 解析器 ↔ git 平台 client | token 仅传 get_git_platform_client，不落库不记日志 |
| 解析器 ↔ 容器 dispatch | token 仅进 dispatch payload（既有明文通道），不进日志 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-09 | Information Disclosure | 平台 client token | mitigate | token 仅传 get_git_platform_client；接线不新增 token 日志；Test 4 守护 |
| T-26-10 | Information Disclosure | dispatch git_credentials | mitigate | 仅 token 非空时注入 access_token；既有 dispatch 通道不扩大；不 log |
| T-26-11 | Spoofing | host fallback | mitigate | 复用 26-01 host 唯一匹配，避免错配他 host 凭证 |
| T-26-12 | Repudiation | 缺凭证行为 | accept | 解析器 None 时各调用方保留既有明确报错/降级，不静默 |
</threat_model>

<verification>
- `cd server && uv run pytest tests/test_git_credential_platform_wiring.py -x` 全绿。
- `cd server && uv run ruff check mcp_tools/merge_request_service.py workflows/services/mr_service.py workflows/nodes/ai/coding.py chat/coding_session_service.py knowledge/diff_archive.py` 干净。
- 回归：`cd server && uv run pytest tests/mcp_tools/test_mr_tools.py tests/test_coding_session_service.py -q`。
</verification>

<success_criteria>
- git 平台客户端、编码 dispatch、diff archive 取 token 统一经解析器。
- 同 host 多仓共享实例凭证；per-repo 优先不回退；token 不泄漏——守护测试全绿。
</success_criteria>

<output>
Create `.planning/phases/26-multirepo-creds-mcp/26-03-SUMMARY.md` when done.
</output>
