---
phase: 26-multirepo-creds-mcp
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - server/mcp_tools/serializers.py
  - server/mcp_tools/views.py
  - server/tests/mcp_tools/test_search_rag_multi_repo.py
autonomous: true
requirements: [REPO-02]
must_haves:
  truths:
    - "search_rag_chunks 传 repository_ids=[a,b] 时跨多仓召回，结果标注来源仓库"
    - "省略多仓参数（仅 repository_id）时维持既有单仓行为与响应形状"
    - "all_repositories=true 跨全部已索引仓库检索（受 max_repos 限制）"
    - "任一仓库被排除的文件跨仓也不出现在结果中（每仓 fail-closed 复用 Phase 22 排除）"
    - "只检索存在且已索引的仓库（不存在/未索引仓库被跳过或报错，不越权检索）"
  artifacts:
    - path: "server/mcp_tools/serializers.py"
      provides: "SearchRagChunksRequestSerializer 多仓参数 + TOOL_FIELD_SPECS 更新"
      contains: "all_repositories"
    - path: "server/mcp_tools/views.py"
      provides: "SearchRagChunksView 多仓解析 + 合并检索 + 来源仓库标注"
      contains: "target_repository_ids"
  key_links:
    - from: "server/mcp_tools/views.py"
      to: "services.retrieval.HybridSearchService.search"
      via: "以多仓 repository_ids 列表调用（search_rag 已内建每仓 fail-closed 排除）"
      pattern: "repository_ids="
    - from: "server/mcp_tools/views.py"
      to: "services.exclusion.build_matcher_for_repo"
      via: "每仓 fail-closed 排除经 search_rag chokepoint 复用（Phase 22）"
      pattern: "search"
---

<objective>
为 MCP RAG 检索工具 `search_rag_chunks` 增加多仓 / 全仓检索参数（`repository_ids` 列表 / `all_repositories` 标志 / `max_repos` 上限），跨多仓召回并标注结果来源仓库；省略多仓参数时维持既有单仓行为。复用 `grep_repository` 已落地的多仓 MCP 范式与 `HybridSearchService.search(repository_ids=[...])` / `search_rag` 既有多仓路径——每仓仍经 Phase 22 `build_matcher_for_repo` fail-closed 排除（被排除文件跨仓不可见）。

Purpose: 兑现 REPO-02「MCP RAG 工具暴露多仓 / 全仓检索参数」，且每仓排除与访问范围受控（D-03）。
Output: search_rag_chunks 多仓 serializer + view + 守护测试。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/26-multirepo-creds-mcp/26-CONTEXT.md
@.planning/phases/22-fail-closed/22-03-SUMMARY.md
</context>

<artifacts_this_phase_produces>
- search_rag_chunks 多仓 / 全仓检索参数（serializer + view），结果标注来源仓库
- TOOL_FIELD_SPECS search_rag_chunks 请求/响应字段更新
- 多仓召回 / 跨仓 fail-closed 排除 / 单仓向后兼容 / 访问范围守护测试
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: SearchRagChunksRequestSerializer 多仓参数（mirror grep_repository）</name>
  <files>server/mcp_tools/serializers.py</files>
  <read_first>
    - server/mcp_tools/serializers.py L15-L20（现有 SearchRagChunksRequestSerializer：repository_id 必填、query、branch、top_k、max_tokens）
    - server/mcp_tools/serializers.py L56-L117（GrepRepositoryRequestSerializer 多仓范式：repository_id 可选 + repository_ids ListField + all_repositories + max_repos + validate 构建 target_repository_ids、branch 仅单仓校验。这是直接模板）
    - server/mcp_tools/serializers.py L446-L451（TOOL_FIELD_SPECS["search_rag_chunks"] request/response 字段清单——多仓参数须同步登记）
  </read_first>
  <action>
    把 SearchRagChunksRequestSerializer 改造为多仓（严格对齐 GrepRepositoryRequestSerializer 语义，保单仓向后兼容）：
    - `repository_id` 由必填改为 `required=False, allow_null=True, default=None`（单仓便捷参数）。
    - 新增 `repository_ids = ListField(child=UUIDField(), required=False, allow_empty=True, default=list, max_length=...)`（显式多仓；max_length 与 max_repos 上限一致，如 20）。
    - 新增 `all_repositories = BooleanField(required=False, default=False)`、`max_repos = IntegerField(required=False, default=10, min_value=1, max_value=20)`。
    - 保留 query / branch / top_k / max_tokens 原样。
    - `validate`：复用 grep 写法构建 `target_repository_ids`（repository_id 去重插入 repository_ids 头部）；既无 targets 又无 all_repositories → ValidationError「必须提供 repository_id / repository_ids，或显式设置 all_repositories=true」；`branch` 非空时强制单仓（len(targets)==1，否则 ValidationError「branch 仅支持单仓检索时指定」）；写回 `attrs["target_repository_ids"]`。
    - 更新 TOOL_FIELD_SPECS["search_rag_chunks"]：request 增 repository_ids/all_repositories/max_repos；response 增 repository_ids（多仓回显）。
  </action>
  <verify>
    <automated>cd server && uv run ruff check mcp_tools/serializers.py && uv run python -c "import ast; ast.parse(open('mcp_tools/serializers.py').read())"</automated>
  </verify>
  <acceptance_criteria>
    - 序列化器接受 repository_id（单）/ repository_ids（多）/ all_repositories（全），三者缺失报中文错误；branch 仅单仓允许。
    - validate 产出 target_repository_ids；TOOL_FIELD_SPECS 同步含新参数。
    - 单仓老用法（仅 repository_id）仍合法。
  </acceptance_criteria>
  <done>多仓参数与校验落地，单仓向后兼容，字段规格同步。</done>
</task>

<task type="auto">
  <name>Task 2: SearchRagChunksView 多仓解析 + 合并检索 + 来源标注</name>
  <files>server/mcp_tools/views.py</files>
  <read_first>
    - server/mcp_tools/views.py L456-L537（现有 SearchRagChunksView.post：单仓 _get_indexed_repo + _resolve_graph_branch + HybridSearchService.search(repository_ids=[repository_id]) + 组 results，repo_id 硬编码单仓。改造点）
    - server/mcp_tools/views.py L594-L743（GrepRepositoryView.post 多仓解析范式：target_ids 来自 input_data["target_repository_ids"]；all_repositories 时列 indexed 非删除仓库 order_by name [:max_repos]；single_target 检测；单仓失败 return err、多仓失败记 per-repo error 条目继续）
    - server/services/retrieval/rag_search.py L78-L123（search_rag 已对 repo_ids 逐仓 build_matcher_for_repo fail-closed 过滤 + 跨仓去重 + 每项打 repository_id；多仓行为已内建）
    - server/services/retrieval/hybrid_search.py L256-L320, L487-L555（HybridSearchService.search(repository_ids=[...]) 多仓 + 图谱邻居 fail-closed 过滤已就绪）
  </read_first>
  <action>
    改造 SearchRagChunksView.post 为多仓（复用 grep 解析范式，单仓走既有行为）：
    - 取 `target_ids = input_data["target_repository_ids"]`；为空且 all_repositories → 列 `Repository.objects.filter(is_deleted=False, index_status=INDEXED).order_by("name").values_list("id")[:max_repos]`（无则 repository_not_found 404，与 grep 一致——访问范围=已索引非删除仓，复用既有权限模型，不新增 ACL）。`target_ids = target_ids[:max_repos]`。`single_target = len(target_ids)==1`。
    - 逐 target 经 `_get_indexed_repo` 校验：单仓失败 → return err（保旧 404/400 行为）；多仓某仓不存在/未索引 → 跳过（收集到 skipped 列表，不越权、不报致命）。保留通过校验的 repo 对象到 dict {id: repo}。
    - branch：单仓时 `graph_branch = await _resolve_graph_branch(...)`（保旧）；多仓时 branch_name=None（各仓 base 分支，serializer 已禁多仓传 branch）。
    - **一次** `HybridSearchService(get_provider()).search(query, repository_ids=valid_ids, branch_name=branch_name, max_tokens=..., top_k=...)`——search_rag 内部已逐仓 fail-closed 排除 + 跨仓合并去重，无需在 view 再循环各仓。
    - 组 results：遍历 L3 items，**每项来源仓库取 `item.get("repository_id")`**（search_rag 已逐项打），`repo_id` 用该值；branch 标签取 `repos[item_repo_id]` 的 base_branch/default_branch（多仓）或 graph_branch（单仓）。其余字段（chunk_id/file_path/line/content/score/language）保持既有映射。
    - output_data：单仓保留既有 `repository_id` 字段（向后兼容）；新增 `repository_ids`（实际检索的 valid_ids 列表，回显来源范围）。related_edges 沿用既有（hybrid 已 fail-closed 过滤跨仓邻居）。
    - 绝不绕过 search_rag chokepoint 自行直读向量库（被排除文件跨仓不可见依赖该 chokepoint）。
  </action>
  <verify>
    <automated>cd server && uv run ruff check mcp_tools/views.py && uv run python -c "import ast; ast.parse(open('mcp_tools/views.py').read())"</automated>
  </verify>
  <acceptance_criteria>
    - 多仓/全仓解析复用 grep 范式；single_target 保旧单仓行为与响应；不存在/未索引仓被跳过（多仓）或报错（单仓）。
    - 一次 HybridSearchService.search 传完整 valid_ids 列表；结果每项来源仓库取 item 自带 repository_id（非硬编码单仓）。
    - 不绕过 search_rag chokepoint；每仓 fail-closed 排除依赖既有路径保留。
  </acceptance_criteria>
  <done>search_rag_chunks 多仓检索 + 来源标注落地，单仓向后兼容。</done>
</task>

<task type="auto">
  <name>Task 3: 多仓召回 / 跨仓 fail-closed / 单仓兼容 / 范围守护测试</name>
  <files>server/tests/mcp_tools/test_search_rag_multi_repo.py</files>
  <read_first>
    - server/tests/services/test_retrieval_exclusion.py（22-03 守护：mock BranchAwareSearchService.search 按 query 子串命中、build_matcher_for_repo 用真实实现走 search_rag chokepoint 验证排除——多仓召回 + 跨仓排除测试范式）
    - server/tests/mcp_tools/conftest.py（MCP 工具测试 fixture：AccessToken / 认证 / repo 索引态构造）
    - server/mcp_tools/views.py SearchRagChunksView（被测入口 path tools/search_rag_chunks/）
    - server/tests/mcp_tools/test_mr_tools.py（MCP view POST 调用 + 响应断言范式）
  </read_first>
  <action>
    新建 test_search_rag_multi_repo.py，mock BranchAwareSearchService.search 按 query 子串对各仓返回带 file_path/payload 的命中（模拟语义召回），build_matcher_for_repo 用真实实现（仅 builtin 默认）真正经 search_rag chokepoint：
    - Test 1（多仓召回 + 来源标注）：建仓 a、b（INDEXED），`search_rag_chunks(repository_ids=[a,b])` 返回含两仓命中，每条结果 repo_id 正确标注来源仓；response 含 repository_ids=[a,b]。
    - Test 2（跨仓 fail-closed）：仓 b 有命中被排除文件（如 config/secret.json，命中 builtin 默认或 per-repo 规则），断言该文件**不**出现在多仓结果中（每仓 fail-closed 复用 Phase 22）。
    - Test 3（单仓向后兼容）：仅传 repository_id（不传多仓参数）→ 行为/响应形状与既有单仓一致（repository_id 字段在、results 只来自该仓）。
    - Test 4（all_repositories 范围）：建 INDEXED 仓 + 一个 NOT_INDEXED / is_deleted 仓，all_repositories=true 只检索已索引非删除仓（未索引/已删不在范围，不越权）。
    - Test 5（不存在仓跳过）：repository_ids 含不存在 UUID + 一个有效仓 → 有效仓正常召回，不存在仓被跳过不致命。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/mcp_tools/test_search_rag_multi_repo.py -x</automated>
  </verify>
  <acceptance_criteria>
    - 多仓召回 + 来源标注、跨仓被排除文件不可见、单仓向后兼容、all_repositories 仅已索引非删除仓、不存在仓跳过——五测全绿。
    - 测试经真实 search_rag chokepoint（build_matcher_for_repo 真实实现），非绕过过滤层。
  </acceptance_criteria>
  <done>REPO-02 多仓守护测试全绿，跨仓 fail-closed 与访问范围成立。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP 调用方 ↔ 多仓检索 | 跨仓召回不得绕过每仓 fail-closed 排除 |
| 多仓范围 ↔ 仓库访问 | 只检索存在且已索引的仓（复用既有访问模型，不新增越权面） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-18 | Information Disclosure | 跨仓被排除文件泄漏 | mitigate | 经 search_rag chokepoint 每仓 build_matcher_for_repo fail-closed（Phase 22）；Test 2 守护，view 不绕过直读向量库 |
| T-26-19 | Elevation of Privilege | 检索越权仓库 | mitigate | target 仅取存在+INDEXED+非删除仓（复用 grep 访问模型）；不存在仓跳过；Test 4/5 守护 |
| T-26-20 | Tampering | 多仓邻居泄漏 | mitigate | hybrid_search 图谱邻居 any-repo-hit fail-closed 过滤已就绪，复用不削弱 |
| T-26-21 | Denial of Service | 全仓检索成本 | mitigate | max_repos（默认 10，上限 20）+ repository_ids max_length 限制，控跨仓上限 |
</threat_model>

<verification>
- `cd server && uv run pytest tests/mcp_tools/test_search_rag_multi_repo.py -x` 全绿。
- 回归：`cd server && uv run pytest tests/services/test_retrieval_exclusion.py tests/mcp_tools/ -q`（确保单仓 RAG 与既有排除守护不破）。
- `cd server && uv run ruff check mcp_tools/serializers.py mcp_tools/views.py` 干净。
</verification>

<success_criteria>
- search_rag_chunks 支持 repository_ids（多）/ all_repositories（全）/ max_repos，结果标注来源仓库。
- 省略多仓参数 = 单仓行为；跨仓每仓 fail-closed 排除；只检索已索引非删除仓——守护测试全绿。
</success_criteria>

<output>
Create `.planning/phases/26-multirepo-creds-mcp/26-05-SUMMARY.md` when done.
</output>
