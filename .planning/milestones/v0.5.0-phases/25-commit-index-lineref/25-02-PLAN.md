---
phase: 25-commit-index-lineref
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - server/services/chunk_lookup.py
  - server/repositories/chunk_at_views.py
  - server/repositories/urls.py
  - server/tests/services/test_find_chunk_at.py
  - server/tests/repositories/test_chunk_at_view.py
autonomous: true
requirements: [IDX-02]
must_haves:
  truths:
    - "给定 repo+file+line，find_chunk_at 返回覆盖该行的 chunk_id（line_start<=line<=line_end）"
    - "被排除文件的 chunk-at 查询 fail-closed：不返回任何 chunk_id"
    - "GET /api/repositories/<id>/chunk-at/?path=&line= 返回 chunk_id 及行范围，认证保护"
  artifacts:
    - path: "server/services/chunk_lookup.py"
      provides: "find_chunk_at(repository_id, file_path, line, branch_name) service"
      exports: ["find_chunk_at"]
    - path: "server/repositories/chunk_at_views.py"
      provides: "ChunkAtView APIView"
      contains: "class ChunkAtView"
    - path: "server/repositories/urls.py"
      provides: "chunk-at 路由注册"
      contains: "chunk-at"
  key_links:
    - from: "server/repositories/chunk_at_views.py::ChunkAtView"
      to: "server/services/chunk_lookup.py::find_chunk_at"
      via: "await find_chunk_at(...)"
      pattern: "find_chunk_at"
    - from: "server/services/chunk_lookup.py::find_chunk_at"
      to: "server/services/exclusion.py::build_matcher_for_repo"
      via: "fail-closed 排除判定"
      pattern: "build_matcher_for_repo"
---

<objective>
`file:line → chunk_id` 反查（IDX-02 后半）：service + REST API，按 `repo + file_path + line_start<=line<=line_end` 命中覆盖该行的 chunk(s)，被排除文件 fail-closed 不返回。这是"片段↔位置"反查地基（下游 v0.6 片段→需求反查复用）。

Purpose: 给定仓库 + 文件 + 行号，定位覆盖该行的 chunk_id。
Output: `find_chunk_at` service（复用 Phase 22 排除 matcher）+ `GET /api/repositories/<id>/chunk-at/` REST 端点。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-commit-index-lineref/25-CONTEXT.md
@.planning/phases/22-fail-closed/22-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
本 plan 产出：
- `server/services/chunk_lookup.py`：`async def find_chunk_at(repository_id, file_path, line, *, branch_name="") -> list[dict]` —— 查询 ChunkRegistry，命中覆盖该行的 chunk，按覆盖区间从小到大（最具体优先）排序，fail-closed 排除被排除文件。
- `server/repositories/chunk_at_views.py`：`ChunkAtView` APIView（`GET ?path=&line=`）。
- `server/repositories/urls.py`：`<uuid:repository_id>/chunk-at/` 路由。

本 plan **不产出**：line 字段回填逻辑（25-01，本 plan 测试直接造带 line_start/end 的 ChunkRegistry row）、commit 索引（25-03/25-04）、前端 UI（deferred）。
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: find_chunk_at service（区间命中 + fail-closed 排除）</name>
  <files>server/services/chunk_lookup.py, server/tests/services/test_find_chunk_at.py</files>
  <read_first>
    - server/code_relations/models.py 行 39-110（ChunkRegistry：repository FK、file_path、chunk_index、branch_name、line_start、line_end、idx_chunk_reg_repo_file 索引）
    - server/services/exclusion.py 行 117-130（`normalize_rel_path`）、行 343-380（`async build_matcher_for_repo` / `is_excluded`、`log_exclusion_blocked`），行 209-230（`ExclusionMatcher.is_excluded`，运行期异常 fail-closed True）
    - server/services/retrieval/rag_search.py 行 81-116（fail-closed 排除范式：build_matcher_for_repo → matcher.is_excluded → log_exclusion_blocked，构造/判定异常一律视为排除）
  </read_first>
  <behavior>
    - Test: 造 ChunkRegistry row(file=src/a.py, line_start=10, line_end=30)，find_chunk_at(repo, "src/a.py", 15) 返回该 chunk_id。
    - Test: line=5（区间外）→ 返回空列表。
    - Test: 两 chunk 覆盖同一行（[1,50] 与 [10,20]）→ 返回两者，最小区间 [10,20] 排在前（最具体优先）。
    - Test: file_path 命中排除规则（如 .env / *.pem）→ 返回空列表（fail-closed），并打 exclusion.blocked 埋点。
    - Test: build_matcher_for_repo 抛异常 → 返回空列表（构造失败 fail-closed，绝不放行）。
    - Test: 路径归一化——传入 "./src/a.py" 与 "src/a.py" 命中同一 chunk（normalize_rel_path 统一口径）。
  </behavior>
  <action>
    新建 `server/services/chunk_lookup.py`，实现 `async def find_chunk_at(repository_id: str, file_path: str, line: int, *, branch_name: str = "") -> list[dict]`。流程：(1) 先经 `build_matcher_for_repo(repository_id)` 取 matcher，构造异常 → `log_exclusion_blocked(surface="chunk_at", ...)` 后返回 `[]`（fail-closed，对齐 rag_search.py 范式，per D-03/D-04）；(2) 用 `normalize_rel_path(file_path)` 归一，None（越界/非法）→ 返回 `[]`；(3) `matcher.is_excluded(norm_path)` 为 True（含判定异常视为 True）→ log + 返回 `[]`；(4) 经 `sync_to_async` 查询 `ChunkRegistry.objects.filter(repository_id=..., file_path=norm_path, branch_name=branch_name, line_start__isnull=False, line_end__isnull=False, line_start__lte=line, line_end__gte=line)`，对每行产出 `{"chunk_id": str, "file_path", "line_start", "line_end", "chunk_index"}`；(5) 按覆盖区间宽度 `(line_end - line_start)` 升序排序（最具体优先，per Claude's Discretion 返回全部命中），返回列表。模块级 `structlog` logger，命名 `chunk_lookup_*`。仅读 ChunkRegistry，不触 Qdrant。
  </action>
  <verify>
    <automated>cd server && .venv/bin/python -m pytest tests/services/test_find_chunk_at.py -x</automated>
  </verify>
  <done>区间命中正确（含 1-based 闭区间边界 line==line_start/line==line_end）；多 chunk 命中按最具体优先排序；被排除文件 + 构造失败 + 判定异常均 fail-closed 返回空；路径归一化统一口径。</done>
</task>

<task type="auto">
  <name>Task 2: ChunkAtView REST 端点 + 路由注册</name>
  <files>server/repositories/chunk_at_views.py, server/repositories/urls.py, server/tests/repositories/test_chunk_at_view.py</files>
  <read_first>
    - server/repositories/views.py 行 1008-1090（`RepositoryExclusionRulesView`：APIView + `permission_classes=[IsAuthenticated]` + async get + `Repository.objects.aget(id=..., is_deleted=False)` + 错误响应 idiom）
    - server/repositories/urls.py 行 241-262（exclusions / sensitive-suggestions 路由注册范式：`<uuid:repository_id>/.../`，置于 router include 之后）
    - server/repositories/index_views.py（任一 async APIView 的 import 头与 Response/状态码风格，供新 views 文件对齐）
  </read_first>
  <action>
    新建 `server/repositories/chunk_at_views.py`，实现 `class ChunkAtView(APIView)`，`permission_classes = [IsAuthenticated]`，`async def get(self, request, repository_id)`。流程：(1) `await aget_object_or_404(Repository, id=repository_id, is_deleted=False)`；(2) 取 query param `path`（必填）与 `line`（必填，int），缺失/`line` 非正整数 → `Response({"error": ...}, status=400)`；(3) 调 `await find_chunk_at(str(repository_id), path, line)`（可选透传 `branch_name` query param，默认 ""）；(4) 返回 `Response({"path": path, "line": line, "chunks": result})`——空命中返回 `{"chunks": []}` 200（被排除文件与无命中对外不可区分，避免存在性泄漏，fail-closed 语义一致，per D-03）。在 `server/repositories/urls.py` import `ChunkAtView` 并在 sensitive-suggestions 路由附近新增 `path("<uuid:repository_id>/chunk-at/", ChunkAtView.as_view(), name="repository-chunk-at")`（router include 之后，UUID 通配安全）。
  </action>
  <verify>
    <automated>cd server && .venv/bin/python -m pytest tests/repositories/test_chunk_at_view.py -x</automated>
  </verify>
  <done>GET chunk-at 命中返回 chunk_id+行范围 200；缺 path/line 或非法 line 返回 400；未认证 401/403；被排除文件返回空 chunks（与无命中同形，不泄漏存在性）；路由解析正确。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → chunk-at REST | 未可信的 path/line 查询参数跨入；被排除文件不得经此面泄漏存在性/位置 |
| service → ChunkRegistry | 只读 DB，不回写 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-04 | Information Disclosure | chunk-at 暴露被排除文件的 chunk_id/行位置 | mitigate | find_chunk_at 入口 build_matcher_for_repo + is_excluded fail-closed；构造/判定异常一律视为排除（复用 22 范式）；守护测试断言被排除文件返回空 |
| T-25-05 | Information Disclosure | 被排除文件「无命中」vs「被排除」可区分 → 存在性泄漏 | mitigate | 两种情形统一返回 `{"chunks": []}` 200，对外不可区分 |
| T-25-06 | Spoofing/Elevation | 未认证访问 chunk-at | mitigate | `permission_classes=[IsAuthenticated]` + 守护测试断言 401/403 |
| T-25-07 | Denial of Service | 恶意 path 触发越界/异常 | mitigate | normalize_rel_path None → 空返回；service 不抛 past view；line 校验为正整数 |
</threat_model>

<verification>
- `cd server && .venv/bin/python -m pytest tests/services/test_find_chunk_at.py tests/repositories/test_chunk_at_view.py`
- `cd server && .venv/bin/python -c "from repositories.urls import urlpatterns"`（路由 import 无误）
- `cd server && .venv/bin/python -m mypy services/chunk_lookup.py repositories/chunk_at_views.py`
</verification>

<success_criteria>
`find_chunk_at` 与 `GET /api/repositories/<id>/chunk-at/` 可用：覆盖行命中正确、最具体优先、被排除文件 fail-closed、认证保护、存在性不泄漏。
</success_criteria>

<output>
Create `.planning/phases/25-commit-index-lineref/25-02-SUMMARY.md` when done.
</output>
