---
phase: 22-fail-closed
plan: 04
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - server/chat/coding_session_service.py
  - server/workflows/nodes/ai/coding.py
  - server/services/exclusion.py
  - task/core/exclusion.py
  - task/core/config.py
  - task/git_ops/operations.py
  - task/tests/test_exclusion_prune.py
  - server/tests/chat/test_coding_exclusion_env.py
autonomous: true
requirements: [EXCL-02]
must_haves:
  truths:
    - "编码容器在 clone + checkout 后按 exclude 列表删除被排除文件，容器内 agent 不可见"
    - "server 两条编码派发路径（chat build_dispatch_metadata + workflow _run_repo_coding）均下传 exclude 列表"
    - "exclude 列表序列化经 env_FRIDAY_TASK_EXCLUDE_PATTERNS 注入（复用 runner env_ 前缀约定）"
    - "容器侧 prune 失败模式 fail-closed（删除异常时不让被排除文件残留可用）"
  artifacts:
    - path: "server/services/exclusion.py"
      provides: "serialize_rules_for_repo(repository_id) -> list[dict]（容器下传规则导出）"
      contains: "serialize_rules_for_repo"
    - path: "task/core/exclusion.py"
      provides: "容器侧轻量匹配器 + prune_excluded(workspace, rules)"
      contains: "def prune_excluded"
    - path: "task/git_ops/operations.py"
      provides: "setup() clone+checkout 后调用 prune_excluded"
      contains: "prune_excluded"
  key_links:
    - from: "server/chat/coding_session_service.py:build_dispatch_metadata"
      to: "services.exclusion.serialize_rules_for_repo"
      via: "env_metadata['env_FRIDAY_TASK_EXCLUDE_PATTERNS'] = json.dumps(rules)"
      pattern: "EXCLUDE_PATTERNS"
    - from: "task/git_ops/operations.py:setup"
      to: "task.core.exclusion.prune_excluded"
      via: "checkout 后按 config.exclude_patterns 删除工作树文件"
      pattern: "prune_excluded"
---

<objective>
把排除过滤延伸到**编码容器面**：server 两条编码派发路径（chat `coding_session_service.build_dispatch_metadata` 与 workflow `AICodingNode._run_repo_coding`）下传有效排除规则，task 容器在 clone + checkout 后从工作树删除被排除文件，使容器内 Claude Code agent 不可见（per D-03.4 + DOMAIN §9.3 "task 容器工作树：clone 后过滤"）。

Purpose: EXCL-02 第四个读取面。容器内 agent 直接读真实工作树文件，必须在 agent 启动前物理移除被排除文件。
Output: server 规则导出 + 两路径 env 注入 + task 侧匹配器 + 容器 prune + 测试。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-fail-closed/22-CONTEXT.md
@.planning/DOMAIN-MODEL.md
@.planning/phases/22-fail-closed/22-01-PLAN.md
@server/services/exclusion.py
</context>

<artifacts_this_phase_produces>
- `server/services/exclusion.py`：`async def serialize_rules_for_repo(repository_id) -> list[dict]`（导出有效规则的可 JSON 序列化形：[{pattern, rule_type}]，含 builtin∪全局∪per-repo 合并 + override 处理，复用 build_matcher_for_repo 的合并逻辑）。
- `server/chat/coding_session_service.py`：模块级 `build_dispatch_metadata`（:125，env metadata 构建 ~156-198）注入 `env_FRIDAY_TASK_EXCLUDE_PATTERNS`。
- `server/workflows/nodes/ai/coding.py`：`AICodingNode._run_repo_coding`（~894，inline DispatchTask.metadata ~968）注入同键。
- `task/core/exclusion.py`：容器侧轻量匹配器（dir/glob/regex 与 server 语义对齐）+ `prune_excluded(workspace: Path, rules: list[dict]) -> int`。
- `task/core/config.py`：`exclude_patterns: list[dict]` 字段（env FRIDAY_TASK_EXCLUDE_PATTERNS，JSON）。
- 测试：`task/tests/test_exclusion_prune.py`、`server/tests/chat/test_coding_exclusion_env.py`。
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: server 端导出有效规则 + 两条派发路径注入 env_FRIDAY_TASK_EXCLUDE_PATTERNS</name>
  <files>server/services/exclusion.py, server/chat/coding_session_service.py, server/workflows/nodes/ai/coding.py, server/tests/chat/test_coding_exclusion_env.py</files>
  <read_first>
    - server/chat/coding_session_service.py（模块级 build_dispatch_metadata ~125；env_metadata dict 构建 ~156-198，含 env_FRIDAY_TASK_AFFECTED_FILES = json.dumps(...) idiom；返回 (env_metadata, repo_url)）
    - server/workflows/nodes/ai/coding.py（AICodingNode._run_repo_coding ~894；anthropic_env/tools_env 构造 + inline DispatchTask.metadata ~968；env_ 前缀经 runner/internal/docker/executor.go TrimPrefix 注入容器）
    - server/services/exclusion.py（build_matcher_for_repo 合并逻辑 —— serialize_rules_for_repo 复用同一合并，避免双份真相）
  </read_first>
  <behavior>
    - serialize_rules_for_repo(repository_id)：返回合并后有效规则列表（builtin ∪ 全局设置 ∪ per-repo，去掉 override 关闭项），每项 {pattern, rule_type}，可 json.dumps。空时返回内置默认（绝不返回空列表导致容器不过滤）。
    - coding_session_service.build_dispatch_metadata：await serialize_rules_for_repo(repository.id)，写入 env_metadata["env_FRIDAY_TASK_EXCLUDE_PATTERNS"] = json.dumps(rules, ensure_ascii=False)。
    - coding.py _run_repo_coding：同样注入到 inline DispatchTask.metadata（与 anthropic_env/tools_env 并列）。
    - 两路径均无条件注入（即便仅 builtin），保证容器面默认安全。
  </behavior>
  <action>
    在 services/exclusion.py 实现 serialize_rules_for_repo，复用 build_matcher_for_repo 的合并实现（抽一个内部 _resolve_effective_specs(repository_id)->list[ExclusionRuleSpec] 供匹配器与序列化共用，单一真相）。在 coding_session_service.build_dispatch_metadata（~156-198 env_metadata 构建处）和 coding.py _run_repo_coding（~968 inline DispatchTask.metadata 处）两处注入 env_FRIDAY_TASK_EXCLUDE_PATTERNS（json）。两函数均 async，可 await。不下传任何凭证类信息，仅规则模式。
  </action>
  <verify>
    <automated>cd server && python -m pytest tests/chat/test_coding_exclusion_env.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - serialize_rules_for_repo 返回合并规则（含 builtin），与匹配器使用的有效规则集一致（同 _resolve_effective_specs 来源）。
    - build_dispatch_metadata 输出的 env_metadata 含 env_FRIDAY_TASK_EXCLUDE_PATTERNS，json 可解析为规则列表。
    - coding.py _run_repo_coding 的 DispatchTask.metadata 含同键。
    - 无 per-repo 规则时仍注入内置默认（断言非空）。
  </acceptance_criteria>
  <done>两条派发路径均下传有效排除规则，单一真相合并。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: task 容器侧轻量匹配器 + config 字段 + clone 后 prune（fail-closed）</name>
  <files>task/core/config.py, task/core/exclusion.py, task/git_ops/operations.py, task/tests/test_exclusion_prune.py</files>
  <read_first>
    - task/core/config.py（TaskConfig BaseSettings，env_prefix=FRIDAY_TASK_；list[dict] 字段 idiom 见 remote_tools ~94）
    - task/git_ops/operations.py（setup ~56：mkdtemp → auth → _clone_repo → _checkout_branch；get_workspace_path ~366）
    - server/services/exclusion.py（dir/glob/regex 语义 —— 容器侧需对齐相同语义，但 task 包不可 import server）
  </read_first>
  <behavior>
    - TaskConfig 新增 exclude_patterns: list[dict] = Field(default_factory=list)，从 FRIDAY_TASK_EXCLUDE_PATTERNS JSON 解析（与 remote_tools 同 idiom）。
    - task/core/exclusion.py：轻量匹配器，语义与 server ExclusionMatcher 对齐（dir 前缀树/glob fnmatch 相对仓库根/regex fullmatch）；prune_excluded(workspace, rules) 遍历工作树相对路径，命中规则即删除文件；删除后清理空目录可选。返回删除计数。归一化/匹配异常 → 保守删除（fail-closed：宁可多删被排除候选，也不残留）。
    - git_ops.setup：在 _checkout_branch 之后、返回前调用 prune_excluded(self.workspace, self.config.exclude_patterns)，记录删除计数日志。explore/repo_summary 模式也应 prune（容器内 agent 仍会读文件）。
    - 不删除 .git 目录本身的逻辑边界：prune 只针对工作树受版本控制的文件；.git/ 命中 dir 规则时删除工作树内 .git 会破坏 git 操作 —— prune 必须**跳过 `.git/` 目录**（防止破坏 commit/push），其余命中照删。
    - **fail-closed 失败模式（关键，WARNING 3 修正）**：单文件删除失败时**重试**（短重试，如 os.remove 失败后 chmod +w 再删，最多 N 次）；若某个被排除文件**仍无法删除**（持久失败），prune_excluded 视为致命——抛出 ExclusionPruneError（新建于 task/core/exclusions），由 git_ops.setup 向上传播使容器 setup 失败 / 任务标记 failed。**绝不**在被排除文件仍可读的情况下让 setup 成功返回（不允许"log and continue"残留可读文件）。归一化/匹配异常按"保守删除"处理，删除本身的持久失败按致命处理。
  </behavior>
  <action>
    在 task/core/config.py 增 exclude_patterns 字段 + JSON 解析（参考 remote_tools 的 list[dict] 处理；若 BaseSettings 不自动 parse，加 validator）。新建 task/core/exclusion.py 实现轻量匹配器 + prune_excluded（os.walk 工作树，跳过 .git 目录，命中删除）+ ExclusionPruneError。删除失败先重试（chmod 后重删，最多 N 次）；持久失败 → 收集失败项并在遍历结束后 raise ExclusionPruneError(failed_paths)（fail-closed：被排除文件绝不残留可读）。在 git_ops/operations.py setup() checkout 后调用 prune_excluded，bind 日志 task_id + pruned_count；不捕获吞掉 ExclusionPruneError —— 让其传播使 setup 失败（runner 据此标记任务 failed），宁可任务失败也不让容器内 agent 看到被排除文件。
  </action>
  <verify>
    <automated>cd task && python -m pytest tests/test_exclusion_prune.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - prune_excluded 删除命中 dir/glob/regex 的工作树文件（如 `.env`、`secrets/x.json`、自定义 regex），保留正常文件。
    - `.git/` 目录不被删除（断言 git 元数据完整）。
    - TaskConfig 能从 FRIDAY_TASK_EXCLUDE_PATTERNS（JSON）解析出 exclude_patterns。
    - 空规则/未设置 env → prune 不删任何文件且不报错。
    - 持久删除失败注入（mock os.remove 始终抛错）→ prune_excluded 重试后抛 ExclusionPruneError，setup **失败传播**（不静默成功）；断言被排除文件绝不在 setup 成功路径上残留可读（fail-closed）。
    - 可恢复失败注入（首次失败、chmod 后第二次成功）→ 文件最终被删除，setup 成功。
  </acceptance_criteria>
  <done>容器 clone 后按规则删净被排除文件，agent 不可见，且不破坏 git。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server → 容器 env | 经 runner env_ 前缀注入；下传的是规则模式（非敏感），但若不下传则容器面裸奔 |
| 容器工作树 → Claude Code agent | agent 直接读真实文件，被排除文件若残留即对 agent 完全可见 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-13 | Information Disclosure | 容器内 agent 读到未删除的被排除文件 | mitigate | clone+checkout 后 prune_excluded 删净；setup 必经路径调用 |
| T-22-14 | Information Disclosure | 仅一条派发路径下传规则，另一路径漏 | mitigate | chat + workflow 两路径均注入；测试各自断言 |
| T-22-15 | Tampering | prune 误删 .git 破坏 commit/push（可用性破坏） | mitigate | prune 跳过 .git/ 目录；测试断言 git 元数据完整 |
| T-22-16 | Information Disclosure | 删除异常 fail-open 残留可读文件 | mitigate | 删除失败先重试；持久失败 → raise ExclusionPruneError 使 setup 失败（fail-closed），绝不在被排除文件仍可读时让 setup 成功 |
| T-22-SC | Tampering | task 侧新增依赖（如 pathspec）供应链风险 | accept | 优先用标准库 fnmatch/re，不新增第三方包；若需新包须走 Package Legitimacy 检查 |
</threat_model>

<verification>
- `cd task && python -m pytest tests/test_exclusion_prune.py -q` 全绿。
- `cd server && python -m pytest tests/chat/test_coding_exclusion_env.py -q` 全绿。
- grep 确认两派发路径均含 env_FRIDAY_TASK_EXCLUDE_PATTERNS（rg "EXCLUDE_PATTERNS" server/chat server/workflows/nodes/ai/coding.py）。
</verification>

<success_criteria>
- 被排除文件在编码容器 clone 后不可见，两条编码派发路径均下传规则，prune 不破坏 git。
</success_criteria>

<output>
Create `.planning/phases/22-fail-closed/22-04-SUMMARY.md` when done.
</output>
