---
phase: 22-fail-closed
plan: 06
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - server/mcp_tools/views.py
  - server/tests/mcp_tools/test_mcp_exclusion.py
autonomous: true
requirements: [EXCL-02]
must_haves:
  truths:
    - "外部暴露的 MCP HTTP 工具读取 bare 镜像/索引时，被排除文件 fail-closed 不可见"
    - "get_repository_file 命中排除路径 → 返回明确「已排除」信号，绝不返回任何 plaintext 文件内容（镜像或索引回退两条路径都拦）"
    - "grep_repository 结果过滤掉被排除文件的命中行与 file_counts，不泄漏 plaintext 命中行"
    - "list_repository_files 不列出被排除文件/纯由被排除文件构成的目录"
    - "命中排除产生 exclusion.blocked 审计埋点（surface 标记 MCP 工具名）"
  artifacts:
    - path: "server/mcp_tools/views.py"
      provides: "GrepRepositoryView / GetRepositoryFileView / ListRepositoryFilesView fail-closed 过滤"
      contains: "is_excluded"
    - path: "server/tests/mcp_tools/test_mcp_exclusion.py"
      provides: "MCP HTTP 读取面排除守护测试（bare 镜像 + 索引回退）"
      min_lines: 60
  key_links:
    - from: "server/mcp_tools/views.py:GetRepositoryFileView.post"
      to: "services.exclusion.is_excluded"
      via: "镜像/索引读出 resolved_path 后、返回 content 前 fail-closed 拦截"
      pattern: "is_excluded"
    - from: "server/mcp_tools/views.py:GrepRepositoryView.post"
      to: "services.exclusion.build_matcher_for_repo"
      via: "grep_mirror 返回后按 file_path 过滤 matches + file_counts"
      pattern: "is_excluded|build_matcher_for_repo"
    - from: "server/mcp_tools/views.py:ListRepositoryFilesView.post"
      to: "services.exclusion.build_matcher_for_repo"
      via: "FileIndex paths 过滤被排除路径后再分页"
      pattern: "is_excluded|build_matcher_for_repo"
---

<objective>
堵住 **externally-exposed MCP HTTP 读取面**（`server/mcp_tools/views.py`）这条残留泄漏通道：`grep_repository` / `get_repository_file` / `list_repository_files` 直接读 **bare 镜像 git objects**（`grep_mirror` / `read_mirror_file` / `list_mirror_paths`，来自 `services/repo_mirror.py`），"不索引"对它们零保护——这正是 DOMAIN §9.1/§9.3 标注的"工具层 denylist 兜底"残留泄漏面。对这三个 view（及其镜像/索引回退路径）挂接 Plan 01 的 `is_excluded` fail-closed 拦截 + `log_exclusion_blocked`。

Purpose: 补齐 EXCL-02 的 MCP 工具面——这是 plan-checker 标出的 BLOCKER。`search_rag_chunks → HybridSearchService.search_rag` 已由 22-03 覆盖，**本 plan 不重复**；只处理三个 bare-mirror/索引直读 view（外加同文件的 find_related_chunks 防御性过滤）。
Output: 三个 MCP view fail-closed 过滤 + 守护测试。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-fail-closed/22-CONTEXT.md
@.planning/DOMAIN-MODEL.md
@.planning/phases/22-fail-closed/22-01-PLAN.md
@server/services/exclusion.py
</context>

<artifacts_this_phase_produces>
- `server/mcp_tools/views.py`：
  - `GetRepositoryFileView.post`（~823）+ `_read_from_mirror`（~950）：返回 content 前对 resolved_path 与 requested file_path 双判定 is_excluded，命中 → 返回「已排除」错误（error_response 或等价 4xx/标记），镜像路径与索引回退路径都拦。
  - `GrepRepositoryView.post`（~543）：`grep_mirror` 返回后按 (repository_id, file_path) 过滤 `result["matches"]` 与 `result["file_counts"]`（每 repo 预取 matcher）。
  - `ListRepositoryFilesView.post`（~739）：`FileIndex` paths 过滤被排除路径后再构建 items/分页；纯由被排除文件构成的目录不出现。
  - `FindRelatedChunksView.post`（~986）：返回邻居 chunk 前按 file_path 过滤被排除项（同文件防御性兜底）。
- `server/tests/mcp_tools/test_mcp_exclusion.py`：守护测试（mock repo_mirror 助手 + FileIndex/Qdrant）。
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: get_repository_file / grep_repository / list_repository_files bare-mirror 读取面 fail-closed</name>
  <files>server/mcp_tools/views.py, server/tests/mcp_tools/test_mcp_exclusion.py</files>
  <read_first>
    - server/mcp_tools/views.py（GrepRepositoryView.post ~543 + grep_mirror 结果结构 result["matches"]/["file_counts"]（每项含 file_path）；GetRepositoryFileView.post ~823 + _read_from_mirror ~950（mirror resolved_path / 索引回退 resolved_path，content 出口 ~860/~923）；ListRepositoryFilesView.post ~739 FileIndex paths）
    - server/mcp_tools/urls.py（确认三个工具的注册名 grep_repository / get_repository_file / list_repository_files，surface 标记用工具名）
    - server/services/exclusion.py（is_excluded / build_matcher_for_repo / log_exclusion_blocked；按 repo 预取 matcher 复用）
    - server/services/repo_mirror.py（grep_mirror / read_mirror_file / list_mirror_paths 签名 + 返回结构，确认 file_path 字段口径为相对仓库根 POSIX）
  </read_first>
  <behavior>
    - GetRepositoryFileView：镜像命中（_read_from_mirror 返回 resolved_path/full_text）与索引回退（_scroll_file_from_collection / 后缀候选 resolved_path）两条路径，在拼装/返回 output_data 前，对 resolved_path（及 requested file_path）调用 is_excluded(repository_id, path)；命中 → 返回 error_response("file_excluded","文件已被排除策略屏蔽", 404/403) 且 log_exclusion_blocked(surface="get_repository_file")，**绝不返回任何 content/lines**。后缀解析出的 resolved_path 也必须判定（防绕过）。
    - GrepRepositoryView：每个 repository_id 预取 matcher（build_matcher_for_repo）；grep_mirror 返回后过滤 result["matches"]（按 match["file_path"]）与 result["file_counts"]（按 f["file_path"]），被排除文件的命中行/计数全部剔除，再做 token 预算与 entry 组装；命中过滤时 log_exclusion_blocked(surface="grep_repository")。total_matches/files_with_matches 用过滤后口径或明确标注（避免泄漏被排除文件存在性时优先用过滤后计数）。
    - ListRepositoryFilesView：FileIndex paths 过滤被排除项后再构建 items + 分页；过滤发生在 requested_path 前缀筛选之后、items 组装之前。
    - 失败模式 fail-closed：判定异常 → 视为命中排除（拒读 / 丢弃该项），不降级返回明文。
    - 不改 repo_mirror.py 助手本身（只在 view 层过滤），不重复 22-03 已覆盖的 search_rag_chunks。
  </behavior>
  <action>
    在 mcp_tools/views.py 三个 view 的读取出口挂接 is_excluded（GetRepositoryFile 用单文件 is_excluded 入口；Grep/List 用 per-repo build_matcher_for_repo 预取后批量过滤，避免逐项重复加载）。GetRepositoryFile 必须在 mirror 路径（~860 前）与 index 回退路径（~923 前）都拦，且对后缀解析后的 resolved_path 复判。命中统一走 log_exclusion_blocked(surface=<tool_name>)。保持 view 既有返回契约（error_response idiom / output_data 结构），仅减少/拒绝被排除内容。判定异常 fail-closed。
  </action>
  <verify>
    <automated>cd server && python -m pytest tests/mcp_tools/test_mcp_exclusion.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - get_repository_file 对被排除 file_path：镜像路径 → 返回「已排除」错误、无 content；索引回退路径（mock 镜像不可用）→ 同样「已排除」、无 content；后缀解析到被排除真实路径同样拒读（断言不能绕过）。
    - grep_repository：grep_mirror mock 返回含被排除文件命中行 → 响应的 matches/file_counts 不含被排除文件的任何 plaintext 命中行。
    - list_repository_files：被排除文件不出现在 items；纯由被排除文件构成的子目录不出现。
    - 命中排除产生 exclusion.blocked 日志，surface 为对应工具名（可断言）。
    - 内置全局默认（无 per-repo 规则）即可使 `.env` 在三个工具中均不可见（开箱即用断言）。
    - 判定异常注入 → fail-closed（拒读 / 丢弃），不返回明文。
  </acceptance_criteria>
  <done>三个 bare-mirror/索引直读 MCP HTTP 工具均 fail-closed，被排除文件无 plaintext 泄漏。</done>
</task>

<task type="auto">
  <name>Task 2: find_related_chunks 防御性过滤 + MCP 面跨工具守护</name>
  <files>server/mcp_tools/views.py, server/tests/mcp_tools/test_mcp_exclusion.py</files>
  <read_first>
    - server/mcp_tools/views.py（FindRelatedChunksView.post ~986：find_related 返回邻居 → output_data；邻居含 file_path ~130 _serialize helper）
    - server/services/exclusion.py（is_excluded / build_matcher_for_repo）
    - server/tests/mcp_tools/test_mcp_read_flow.py（既有 MCP 读取流测试 idiom：mock 助手 + InteractionRun begin/auth fixture）
  </read_first>
  <behavior>
    - FindRelatedChunksView：返回邻居前按 (repository_id, file_path) 过滤被排除邻居（防御性，与 22-03 的 hybrid_search 邻居过滤同口径，但此 view 自行组装 output，需独立过滤）；命中 log_exclusion_blocked(surface="find_related_chunks")。
    - 跨工具守护：同一被排除文件在 grep_repository / get_repository_file / list_repository_files / find_related_chunks 四个 MCP 工具中均不可见（一个测试用例覆盖四面）。
  </behavior>
  <action>
    在 FindRelatedChunksView.post 邻居 output 组装处加 is_excluded 过滤（per-repo matcher 预取）。在 test_mcp_exclusion.py 增跨工具守护用例：构造命中规则的文件，mock repo_mirror + FileIndex + find_related，断言四个工具响应均不含该文件的路径/内容/命中行。复用 test_mcp_read_flow.py 的 auth/run fixture idiom。
  </action>
  <verify>
    <automated>cd server && python -m pytest tests/mcp_tools/test_mcp_exclusion.py -q</automated>
  </verify>
  <acceptance_criteria>
    - find_related_chunks 不返回被排除文件的邻居。
    - 跨工具守护：被排除文件在 grep / get_file / list / find_related 四面均不可见（单测断言）。
  </acceptance_criteria>
  <done>MCP 工具面排除一致不可见，find_related_chunks 防御性兜底到位。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 外部 MCP 客户端 → MCP HTTP 工具 | 经 /api/.../tools/* 暴露给外部 agent，入参 file_path/pattern 不可信 |
| bare 镜像 git objects → MCP 工具 | grep_mirror/read_mirror_file 直读 bare 对象，绕过索引层；"不索引"对其零保护 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-21 | Information Disclosure | get_repository_file 经 bare 镜像返回被排除文件 plaintext | mitigate | mirror + index 两路径返回前 is_excluded 拒读；resolved_path 复判防后缀绕过 |
| T-22-22 | Information Disclosure | grep_repository 命中行泄漏被排除文件内容 | mitigate | grep_mirror 结果按 file_path 过滤 matches/file_counts |
| T-22-23 | Information Disclosure | list_repository_files 暴露被排除文件存在性/路径 | mitigate | FileIndex paths 过滤后再分页 |
| T-22-24 | Information Disclosure | find_related_chunks 邻居泄漏被排除文件 | mitigate | 返回前 file_path 过滤（防御性兜底） |
| T-22-25 | Information Disclosure | view 判定异常 fail-open 返回内容 | mitigate | except → 拒读/丢弃（fail-closed）+ log_exclusion_blocked |
</threat_model>

<verification>
- `cd server && python -m pytest tests/mcp_tools/test_mcp_exclusion.py -q` 全绿。
- grep 确认三个 bare-mirror view（grep_mirror/read_mirror_file/list_mirror_paths 调用点）均经 is_excluded（rg "is_excluded|build_matcher_for_repo" server/mcp_tools/views.py 命中 ≥3）。
- 既有 MCP 读取流测试不回归（pytest tests/mcp_tools/test_mcp_read_flow.py -q）。
</verification>

<success_criteria>
- 外部 MCP HTTP 工具（grep_repository / get_repository_file / list_repository_files / find_related_chunks）对被排除文件 fail-closed 不可见，关闭 bare-mirror 残留泄漏面（EXCL-02 MCP 工具面补齐）。
</success_criteria>

<output>
Create `.planning/phases/22-fail-closed/22-06-SUMMARY.md` when done.
</output>
