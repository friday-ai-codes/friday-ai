---
phase: 57-anthropic-messages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/compat/schemas.py
  - server/compat/request_handler.py
  - server/compat/anthropic_adapter.py
  - server/compat/error_handlers.py
  - server/compat/views.py
  - server/compat/urls.py
  - server/tests/compat/test_anthropic_schemas.py
  - server/tests/compat/test_anthropic_adapter.py
  - server/tests/compat/test_messages.py
autonomous: true
requirements: [ANTHROPIC-01]
must_haves:
  truths:
    - "POST /v1/messages（+ /v1/messages/ 双注册）经新增 MessagesView 生效；既有 urls.py 4 行（chat/completions×2、models×2）逐字不变，仅追加 messages 两行 path"
    - "AnthropicMessagesRequestSerializer：max_tokens 必填（IntegerField required min 1，缺失/<1 → 400）；system 顶层可选（string 或 content blocks）；messages role∈{user,assistant}；content 支持 string 与 text/image_url parts（镜像 _MessageSerializer.validate_content）；stream 默认 False；temperature 可选 0–1；复用 repository_ids/project_id 扩展字段"
    - "anthropic_to_openai_messages(system, messages) 纯函数把 Anthropic 形状摊平为 prepare_messages_with_meta 期望的 [{role, content}]：system 非空 → 列表首位插入 {role:'system', content:<text 摊平>}（复用 _content_text 取 text parts 拼接）；messages 逐条透传 {role, content}，content 为 blocks 时仅规整 text part 为 OpenAI {type:'text',text} 形状"
    - "anthropic_sse_encode(event_type, data) 产 Anthropic 双行帧 b'event: <type>\\n' + b'data: ' + json + b'\\n\\n'；data JSON 内 type 字段与 event_type 一致；绝不复用 OpenAI sse_encode（它只产 data: 行、无 event: 行）"
    - "8 个事件骨架纯函数产最小合法 payload：message_start（message 骨架 id/type/role/model/content:[]/stop_reason:null/stop_sequence:null/usage:{input_tokens,output_tokens}）、content_block_start(text/thinking)、content_block_delta_text、content_block_delta_thinking、content_block_stop、message_delta(stop_reason+usage.output_tokens)、message_stop、anthropic_error_event"
    - "AnthropicCompatAdapter.translate_stream 文本/收尾路径（无 prelude）：message_start → content_block_start(text,index 0) → 每个 TEXT_DELTA 一个 text_delta(index 0) → content_block_stop(0) → message_delta(stop_reason + 累计 output_tokens) → message_stop；THINKING 及其余事件静默 continue（INV-5：THINKING 不外透）；ERROR → 发 anthropic_error_event 后 return（不发 message_stop、不泄漏 traceback）"
    - "stop_reason 映射（_status_to_stop_reason）：completed/interrupted/max_iterations → end_turn；usage 改名（_rename_usage）：input→input_tokens、output→output_tokens（P-2，绝不透传 input/output 原 key）"
    - "MessagesView 非流式 post(stream=False)：serializer 失败 → Anthropic error envelope 400；_build_runner None → 503；经 anthropic_to_openai_messages + prepare_messages_with_meta + aggregate_message 聚合为 Anthropic Messages 形状 {id:msg_*, type:'message', role:'assistant', content:[{type:'text',text}], model:'friday-default', stop_reason, stop_sequence:null, usage:{input_tokens,output_tokens}}；content[].text 仅 TEXT_DELTA 正文（零 trace 污染，P-8）"
    - "绝不输出 tool_use content block（TRACE-02/P-5）；任一 content_block.type ∈ {text,thinking}"
    - "零回归：既有 tests/compat/（OpenAI adapter/view/progress/auth）逐字全绿；OpenAI 符号（OpenAICompatAdapter/ChatCompletionsView/ModelsView/sse_encode/openai_error_response/ChatCompletionsRequestSerializer）不改；progress.py 不改"
  artifacts:
    - path: "server/compat/anthropic_adapter.py"
      provides: "Anthropic 专用 SSE 双行帧编码 helper + 8 个事件骨架纯函数 + AnthropicCompatAdapter（text/收尾路径）+ aggregate_message 非流式聚合 + stop_reason/usage 映射纯函数"
      contains: "class AnthropicCompatAdapter"
    - path: "server/compat/anthropic_adapter.py"
      provides: "Anthropic SSE 双行帧编码 helper（绝不复用 OpenAI sse_encode）"
      contains: "def anthropic_sse_encode"
    - path: "server/compat/anthropic_adapter.py"
      provides: "非流式聚合 helper：消费 runner 事件聚合 Anthropic Messages 形状（usage 改名 + stop_reason 映射）"
      contains: "def aggregate_message"
    - path: "server/compat/schemas.py"
      provides: "AnthropicMessagesRequestSerializer（新增，不改既有 ChatCompletionsRequestSerializer）"
      contains: "class AnthropicMessagesRequestSerializer"
    - path: "server/compat/request_handler.py"
      provides: "Anthropic 形状规整纯函数（system 提顶 + block→text part），委托既有 _content_text"
      contains: "def anthropic_to_openai_messages"
    - path: "server/compat/error_handlers.py"
      provides: "Anthropic error envelope helper（{type:'error', error:{type,message}}，不复用 openai_error_response）"
      contains: "def anthropic_error_response"
    - path: "server/compat/views.py"
      provides: "MessagesView（adrf APIView，非流式路径；复用 _build_runner/OptionalBearerTokenAuth）"
      contains: "class MessagesView"
    - path: "server/compat/urls.py"
      provides: "POST /v1/messages 双注册（追加两行，既有 4 行不动）"
      contains: "messages"
    - path: "server/tests/compat/test_anthropic_schemas.py"
      provides: "serializer 校验 + anthropic_to_openai_messages 规整纯函数单测（新文件）"
      contains: "def test_"
    - path: "server/tests/compat/test_anthropic_adapter.py"
      provides: "anthropic_sse_encode + 事件骨架纯函数 + adapter text 路径集成测（新文件）"
      contains: "def test_"
    - path: "server/tests/compat/test_messages.py"
      provides: "MessagesView 非流式 view 级集成测 + 零回归（新文件）"
      contains: "def test_"
  key_links:
    - from: "server/compat/urls.py"
      to: "server/compat/views.py"
      via: "path('messages', MessagesView.as_view()) 注册"
      pattern: "MessagesView"
    - from: "server/compat/views.py"
      to: "server/compat/request_handler.py"
      via: "MessagesView 经 anthropic_to_openai_messages 规整后调 prepare_messages_with_meta"
      pattern: "anthropic_to_openai_messages"
    - from: "server/compat/views.py"
      to: "server/compat/anthropic_adapter.py"
      via: "非流式经 aggregate_message 聚合 Anthropic Messages 形状"
      pattern: "aggregate_message"
    - from: "server/compat/anthropic_adapter.py"
      to: "server/agents/core/events.py"
      via: "translate_stream/aggregate_message 按 AgentEvent type 分派"
      pattern: "MESSAGE_COMPLETE"
---

<objective>
建立 ANTHROPIC-01 地基：纯增量新增 Anthropic Messages 兼容端点 `POST /v1/messages` 的**请求映射 + 非流式响应**。复用 Phase 56 内核（`_build_runner` / `prepare_messages_with_meta` / `LangChainAgentRunner.stream`）零改动，只新增四件产物——`AnthropicMessagesRequestSerializer`（schemas.py）、`anthropic_to_openai_messages` 形状规整纯函数（request_handler.py）、`server/compat/anthropic_adapter.py`（Anthropic 专用 SSE 双行帧编码 helper + 8 个事件骨架纯函数 + `AnthropicCompatAdapter.translate_stream` 文本/收尾路径 + `aggregate_message` 非流式聚合 + stop_reason/usage 映射纯函数）、`MessagesView`（非流式）+ urls 两行。

Purpose: 兑现 ANTHROPIC-01——`/v1/messages` 按 Anthropic Messages 形状映射请求（system 顶层 / max_tokens 必填 / messages）+ 返回合法 Anthropic 非流式响应（`id/type:"message"/role/content:[{type:"text",text}]/model/stop_reason/stop_sequence/usage:{input_tokens,output_tokens}`），同时严守零回归（OpenAI 符号逐字不变、progress.py 不改）与 TRACE-02/INV-5（绝不发 tool_use block、绝不外透 THINKING/CoT/工具入参出参原文）。
Output: `schemas.py` 增 serializer；`request_handler.py` 增规整纯函数；新建 `anthropic_adapter.py`（编码 helper + 事件骨架 + adapter text 路径 + aggregate_message + 映射纯函数）；`error_handlers.py` 增 Anthropic error helper；`views.py` 增 `MessagesView`（非流式）；`urls.py` 增 2 行；新建 `test_anthropic_schemas.py` / `test_anthropic_adapter.py` / `test_messages.py`。流式 trace（thinking block prelude + TOOL_USE 预埋）留 Plan 02。

> **DEVIATION D-1（显式记录，同 Phase 56，来自 57-RESEARCH §3 D-1）**：CONTEXT/里程碑默认假设 `TOOL_USE_*` 会流入 compat adapter。**实证不符**——`_build_runner()`（views.py:33-48）构造 `LangChainRunnerConfig` 不传 tools（`config.tools==[]`）→ runner 不 `bind_tools` → `TOOL_USE_*` 在 57 链路也**永不发射**。故 ANTHROPIC-02 的可见 trace 不能靠 `tool_event_to_progress`（前向兼容预埋，Plan 02 接），必须由 `retrieval_to_progress(retr)`（真实 RAG 命中计数）经 thinking block 兑现（Plan 02）。本 Plan 01 只做非流式（不涉 trace），但 adapter `translate_stream` 设计须为 Plan 02 的 prelude/TOOL_USE 扩展留好结构（index 单线性计数器、事件骨架纯函数齐备）。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.codex/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/57-anthropic-messages/57-CONTEXT.md
@.planning/phases/57-anthropic-messages/57-RESEARCH.md
@.planning/phases/57-anthropic-messages/57-VALIDATION.md

# 镜像结构参考 + 复用 _build_runner + 非流式聚合范式（换 Anthropic 形状）
@server/compat/views.py
# OpenAI adapter 平行参考（AgentEvent if/elif 分派、prelude 注入范式）——不改此文件
@server/compat/adapter.py
# 复用 _content_text/_content_blocks 语义 + prepare_messages_with_meta（规整后委托）
@server/compat/request_handler.py
# 复用纯函数（retrieval_to_progress/tool_event_to_progress/make_reasoning_chunk）——不改，仅 import
@server/compat/progress.py
# OpenAI sse_encode（绝不复用，对比新建 anthropic_sse_encode）
@server/compat/streaming.py
# 既有 serializer 镜像（_MessageSerializer.validate_content 复用语义）
@server/compat/schemas.py
# 既有 4 行 path（逐字不动，仅追加 2 行）
@server/compat/urls.py
# OpenAI error helper（对比，新增 anthropic_error_response 平行）
@server/compat/error_handlers.py
# AgentEvent 形状 + TEXT_DELTA/THINKING/MESSAGE_COMPLETE/ERROR 常量
@server/agents/core/events.py
# MESSAGE_COMPLETE.data 形状（status/usage:{input,output}）—— adapter 翻译源
@server/agents/langchain_runner.py
# 既有守护测试范式（_make_runner/_collect/_collect_raw 直接复用）
@server/tests/compat/test_adapter.py
# 既有 view 级范式（AsyncClient + patch _build_runner / prepare_messages_with_meta）
@server/tests/compat/test_chat_completions.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: AnthropicMessagesRequestSerializer（schemas.py）+ anthropic_to_openai_messages 规整纯函数（request_handler.py）+ 纯函数单测</name>
  <read_first>
    - server/compat/schemas.py（_MessageSerializer.validate_content 校验逻辑——text/image_url part，须镜像；ChatCompletionsRequestSerializer 字段范式 repository_ids/project_id）
    - server/compat/request_handler.py:35-45（_content_text：取 type=="text" part text 拼接——system 摊平复用）+ :48-80（_content_blocks：OpenAI text/image_url part → LangChain content blocks）
    - 57-RESEARCH.md §2 F-7（请求字段 + 规整目标）、§3 D-6（serializer 字段表）、§4 第 2 点、§5 P-11（ruff I001 import 排序）
    - 57-CONTEXT.md <decisions> 端点与请求映射（max_tokens 必填、system 顶层、role user/assistant、复用 repository_ids/project_id）
  </read_first>
  <behavior>
    - `AnthropicMessagesRequestSerializer`（schemas.py 新增，**不改** ChatCompletionsRequestSerializer/_MessageSerializer）：`model`=CharField()（固定忽略实际值，view 复用 friday-default）；`max_tokens`=IntegerField(required=True, min_value=1)（Anthropic 必填，缺失/`<1`→400）；`messages`=新增 `_AnthropicMessageSerializer(many=True)`（role=ChoiceField(["user","assistant"])，content=JSONField + validate_content **镜像** _MessageSerializer.validate_content 的 text/image_url part 校验）；`system`=JSONField(required=False)（string 或 content blocks 数组，可选）；`stream`=BooleanField(default=False)；`temperature`=FloatField(required=False, min_value=0.0, max_value=1.0)；`repository_ids`=ListField(UUIDField, required=False, default=list)；`project_id`=UUIDField(required=False, allow_null=True)。
    - `anthropic_to_openai_messages(system, messages) -> list[dict]`（request_handler.py 新增纯函数，无 IO、无 DB）：返回 `prepare_messages_with_meta` 期望的 `[{role, content}]`。规整规则：①`system` 非空（string 或 blocks）→ 用 `_content_text(system)` 摊平为纯文本，在结果列表**首位**插入 `{"role":"system", "content":<摊平文本>}`（摊平为空串则不插入）；②`messages` 逐条 → `{"role": m["role"], "content": <规整 content>}`：content 为 str 直接透传；content 为 blocks 列表时仅保留 text part，规整为 OpenAI 形状 `{"type":"text","text":...}`（Anthropic text block 本就是该形状，原样保留；非 text part 本 phase 丢弃——image 全量对齐 Out of Scope）。
    - 规整后**完全委托** `prepare_messages_with_meta`（不重写 RAG 注入/检索内核）——RAG query 取最后一条 user message 由内核处理。
  </behavior>
  <action>
  1. `server/compat/schemas.py`：在文件末尾新增 `_AnthropicMessageSerializer`（role=ChoiceField(["user","assistant"])，content=JSONField()，`validate_content` 复制 _MessageSerializer.validate_content 的 text/image_url 校验逻辑——保持 zh-CN 错误文案）与 `AnthropicMessagesRequestSerializer`（字段如上）。**绝不**改既有 `_MessageSerializer` / `_StreamOptionsSerializer` / `ChatCompletionsRequestSerializer`。
  2. `server/compat/request_handler.py`：新增 `def anthropic_to_openai_messages(system: object, messages: list[dict]) -> list[dict]`（zh-CN docstring：说明 system 提顶 + block→text part + 委托内核；复用 `_content_text`；image part 本 phase 丢弃属 Out of Scope）。**绝不**改 `_prepare` / `prepare_messages` / `prepare_messages_with_meta` / `_content_text` / `_content_blocks` 既有实现。
  3. 新建 `server/tests/compat/test_anthropic_schemas.py`（纯函数 / serializer 直调，无需 DB / async）：
     - serializer：`max_tokens` 缺失 → `is_valid()` False；`max_tokens=0` → False；合法 payload（含 max_tokens≥1）→ True；`system` 省略合法、给 string 合法、给 blocks 合法；`messages` role="system" → False（仅 user/assistant）、role="user" 合法；content 为 string 合法、为 `[{type:text,text}]` 合法、为 `[{type:bogus}]` → False；temperature=1.5 → False、temperature=0.5 合法。
     - `anthropic_to_openai_messages`：system="你是助手" + messages=[{role:user,content:"hi"}] → 结果首位 `{"role":"system","content":"你是助手"}`、其后 `{"role":"user","content":"hi"}`；system 为 `[{type:text,text:"A"},{type:text,text:"B"}]` → 摊平 "AB"；system 为 None/"" → 不插入 system；messages content 为 `[{type:text,text:"x"},{type:image,source:{...}}]` → 规整 content 仅含 `{"type":"text","text":"x"}`。
  ruff 行宽 100，import 排序（I001），注释/docstring 中文。
  </action>
  <acceptance_criteria>
    - 源断言：`schemas.py` 含 `class AnthropicMessagesRequestSerializer` 与 `_AnthropicMessageSerializer`；`request_handler.py` 含 `def anthropic_to_openai_messages`；grep `schemas.py` 既有 `class ChatCompletionsRequestSerializer` 行与字段逐字不变。
    - 行为断言：见 Task action 中 serializer 与规整纯函数用例（max_tokens 必填、system 提顶、block→text）。
    - 测试命令：`cd server && uv run pytest tests/compat/test_anthropic_schemas.py -q` 全绿。
  </acceptance_criteria>
  <verify>
    <automated>cd server && uv run ruff check compat/schemas.py compat/request_handler.py && uv run pytest tests/compat/test_anthropic_schemas.py -q</automated>
  </verify>
  <done>AnthropicMessagesRequestSerializer（max_tokens 必填、system 可选、role user/assistant、content parts 校验）+ anthropic_to_openai_messages（system 提顶、block→text part、委托内核）落地；既有 OpenAI serializer/_content_* 逐字不变；纯函数单测全绿。</done>
</task>

<task type="auto">
  <name>Task 2: 新建 anthropic_adapter.py（SSE 双行帧编码 helper + 8 事件骨架纯函数 + stop_reason/usage 映射 + AnthropicCompatAdapter.translate_stream 文本/收尾路径 + aggregate_message）+ 纯函数与 adapter 集成测</name>
  <read_first>
    - server/compat/streaming.py（sse_encode 是 b"data: {...}\n\n" 单行帧——**绝不复用**；anthropic_sse_encode 须产 event:+data: 双行帧）
    - server/compat/adapter.py:34-168（OpenAICompatAdapter.translate_stream 结构：common/chat_id 构造、AgentEvent if/elif 分派、TEXT_DELTA/MESSAGE_COMPLETE/ERROR/THINKING 处理、include_usage 收尾——平行参考，**不改此文件**）
    - server/agents/core/events.py（TEXT_DELTA/THINKING/MESSAGE_COMPLETE/ERROR/TOOL_USE_START/TOOL_USE_RESULT 常量；AgentEvent(type,data)）
    - server/agents/langchain_runner.py:396-404/414-419/526-540（TEXT_DELTA.data={text,...}；THINKING.data={thinking,...}（INV-5 不外透）；MESSAGE_COMPLETE.data={final_answer,status,usage:{input,output,...},...}；status∈{completed,interrupted,max_iterations,error}）
    - 57-RESEARCH.md §2 F-4（data 形状 + usage key 改名）、F-5（SSE 双行帧 + 8 事件最小 payload 表）、F-6（非流式响应形状）、§3 D-2（index 单线性计数）/D-3（stop_reason 映射表）/D-4（SSE 编码 helper 独立）/D-5（非流式聚合）/D-6（文件放置 + 函数清单）、§5 P-1/P-2/P-3/P-5/P-6/P-7/P-10
    - server/tests/compat/test_adapter.py:35-67（_make_runner/_collect/_collect_raw 范式，本 task 测试复用）
  </read_first>
  <behavior>
    - `anthropic_sse_encode(event_type: str, data: dict) -> bytes`：产 `b"event: " + event_type + b"\n" + b"data: " + json.dumps(data, ensure_ascii=False) + b"\n\n"`；约束 `data["type"] == event_type`（调用方保证）。**绝不** import/调用 `sse_encode`。
    - 8 个事件骨架纯函数（返回 `dict`，含 `type` 字段与 §15 F-5 最小 payload 表一致；含 `index`）：
      - `message_start_event(msg_id, model, input_tokens=0)` → `{"type":"message_start","message":{"id":msg_id,"type":"message","role":"assistant","content":[],"model":model,"stop_reason":None,"stop_sequence":None,"usage":{"input_tokens":input_tokens,"output_tokens":0}}}`
      - `content_block_start(index, block_type)` → text: `{"type":"content_block_start","index":index,"content_block":{"type":"text","text":""}}`；thinking: `{...,"content_block":{"type":"thinking","thinking":""}}`
      - `content_block_delta_text(index, text)` → `{"type":"content_block_delta","index":index,"delta":{"type":"text_delta","text":text}}`
      - `content_block_delta_thinking(index, text)` → `{...,"delta":{"type":"thinking_delta","thinking":text}}`（Plan 02 用，本 plan 一并实现纯函数 + 测形状）
      - `content_block_stop(index)` → `{"type":"content_block_stop","index":index}`
      - `message_delta_event(stop_reason, output_tokens)` → `{"type":"message_delta","delta":{"stop_reason":stop_reason,"stop_sequence":None},"usage":{"output_tokens":output_tokens}}`
      - `message_stop_event()` → `{"type":"message_stop"}`
      - `anthropic_error_event(message)` → `{"type":"error","error":{"type":"api_error","message":message}}`（不含 traceback，P-10）
    - 映射纯函数：`_status_to_stop_reason(status) -> str`：`completed/interrupted/max_iterations`（及其余非 error）→ `"end_turn"`（D-3，预留 max_tokens 映射位）；`_rename_usage(usage: dict|None) -> tuple[int,int]`：返回 `(input_tokens, output_tokens)`，取 `usage.get("input",0)`/`usage.get("output",0)`（int 化、非负，P-2，绝不透传原 key）。
    - `AnthropicCompatAdapter.translate_stream(runner, prompt, *, model) -> AsyncGenerator[bytes]`（**本 plan 仅 text/收尾路径，无 prelude/TOOL_USE**——Plan 02 扩展）：
      1. `msg_id = f"msg_{uuid.uuid4().hex[:24]}"`；yield `anthropic_sse_encode("message_start", message_start_event(msg_id, model))`。
      2. `text_index = 0`；yield `content_block_start(text_index, "text")`（D-2：text block 总在正文前发 start，保证非空 content 形状稳定）。
      3. `output_tokens=0`、`stop_reason="end_turn"`；`async for evt in runner.stream(prompt)`：
         - `TEXT_DELTA` → yield `content_block_delta_text(text_index, evt.data.get("text",""))`。
         - `MESSAGE_COMPLETE` → `stop_reason=_status_to_stop_reason(evt.data.get("status","completed"))`；`_, output_tokens=_rename_usage(evt.data.get("usage"))`；break。
         - `ERROR` → yield `anthropic_error_event(evt.data.get("message","internal_error"))` 后 **return**（不发 content_block_stop/message_delta/message_stop；不泄漏 traceback）。
         - `THINKING` 及其余类型 → `continue`（INV-5：THINKING 不外透；TOOL_USE_* 留 Plan 02 分支）。
      4. 收尾：yield `content_block_stop(text_index)` → `message_delta_event(stop_reason, output_tokens)` → `message_stop_event()`。
    - `aggregate_message(runner, prompt, *, model, msg_id=None) -> dict`（非流式聚合，async；D-5(a) 同源翻译核——复用 `_status_to_stop_reason`/`_rename_usage`）：`async for evt in runner.stream(prompt)`：TEXT_DELTA→拼 text；MESSAGE_COMPLETE→记 status/usage，break；ERROR→raise RuntimeError(message)（view 捕获转 Anthropic error）；THINKING/其余→continue。返回 `{"id":msg_id or f"msg_{uuid4...}", "type":"message", "role":"assistant", "content":[{"type":"text","text":<拼接>}], "model":model, "stop_reason":<映射>, "stop_sequence":None, "usage":{"input_tokens":<input>, "output_tokens":<output>}}`（content 仅 text 正文，零 trace 污染）。
  </behavior>
  <action>
  新建 `server/compat/anthropic_adapter.py`（zh-CN 模块 docstring：说明本模块是 Anthropic Messages 兼容专属 adapter，与 OpenAI `adapter.py` 平行；SSE 为 event:+data: 双行帧、不发 `[DONE]`、绝不复用 sse_encode（P-1）；usage 改名 input_tokens/output_tokens（P-2）；THINKING 不外透、绝不发 tool_use block（INV-5/P-5）；DEVIATION D-1 见 objective）。
  - import：`json`、`time`(若需)、`uuid`；`from collections.abc import AsyncGenerator`；`from typing import Any`；`from agents.core.events import ERROR, MESSAGE_COMPLETE, TEXT_DELTA, THINKING, AgentEvent`（TOOL_USE_* 留 Plan 02 加）；`from agents.langchain_runner import LangChainAgentRunner`。**不** import `sse_encode`。
  - 依 behavior 实现 `anthropic_sse_encode`、8 个事件骨架纯函数、`_status_to_stop_reason`、`_rename_usage`、`class AnthropicCompatAdapter`（`translate_stream` staticmethod）、`aggregate_message`。
  - 顶部新增 `error_handlers.py` 无关——但本 task **同时**在 `server/compat/error_handlers.py` 新增 `def anthropic_error_response(message, type_="api_error", http_status=400) -> Response`（产 `{"type":"error","error":{"type":type_,"message":message}}`，不复用 openai_error_response，zh-CN docstring 标注 ASVS V8.3 不泄漏 traceback）。
  - 新建 `server/tests/compat/test_anthropic_adapter.py`（复用 test_adapter.py 的 `_make_runner` / 自写 `_collect_events` 解析双行帧）：
    - **编码 helper（6.1）**：`anthropic_sse_encode("message_stop", {"type":"message_stop"})` 解码含 `"event: message_stop\n"` 与 `"data: {...}\n\n"` 双行帧；data JSON 的 `type`=="message_stop"。
    - **8 事件骨架形状（6.1）**：逐个断言最小 payload——message_start.message 含 id/type/role/model/content==[]/stop_reason None/stop_sequence None/usage{input_tokens,output_tokens}；content_block_start text/thinking 的 content_block.type 区分；content_block_delta_text.delta.type=="text_delta"、_thinking.delta.type=="thinking_delta"；content_block_stop.index 透传；message_delta.delta.stop_reason + usage.output_tokens；message_stop type；anthropic_error_event.error.type=="api_error"。
    - **映射纯函数**：`_status_to_stop_reason("completed"/"interrupted"/"max_iterations")=="end_turn"`；`_rename_usage({"input":5,"output":2})==(5,2)`、`_rename_usage(None)==(0,0)`。
    - **adapter text 路径集成（6.2）**：`_make_runner(TEXT_DELTA{text:"你好"}, TEXT_DELTA{text:"世界"}, MESSAGE_COMPLETE{status:completed, usage:{input:5,output:2}})` → translate_stream 收集事件序列断言**顺序**：`message_start → content_block_start(text,0) → text_delta×2 → content_block_stop(0) → message_delta(end_turn, output_tokens=2) → message_stop`；任一 content_block.type ∈ {text}（无 tool_use/thinking）；message_delta.usage.output_tokens==2。
    - **ERROR 路径**：`_make_runner(ERROR{message:"X"}, TEXT_DELTA{text:"不应出现"})` → 含 `event: error` 帧、其后无 message_stop、全流不含 "不应出现"。
    - **interrupted/max_iterations → end_turn**：注入 MESSAGE_COMPLETE status=interrupted / max_iterations → message_delta.stop_reason=="end_turn"。
    - **不发 tool_use block（TRACE-02/P-5）**：全流任一 content_block_start 的 content_block.type ∈ {text,thinking}，绝无 "tool_use"。
    - **aggregate_message（6.3 非流式核）**：同上 runner → 返回 dict 形状 `{id:msg_*, type:message, role:assistant, content:[{type:text,text:"你好世界"}], model, stop_reason:end_turn, stop_sequence:None, usage:{input_tokens:5,output_tokens:2}}`；ERROR runner → raise RuntimeError。
  ruff 行宽 100，import I001，注释中文。
  </action>
  <acceptance_criteria>
    - 源断言：`anthropic_adapter.py` 存在，含 `def anthropic_sse_encode`、`class AnthropicCompatAdapter`、`def aggregate_message`、`def message_start_event`、`def _status_to_stop_reason`；grep 该文件**不含** `from .streaming import` / `sse_encode(` / `[DONE]` / `tool_use`；`error_handlers.py` 含 `def anthropic_error_response`，既有 `def openai_error_response` 逐字不变。
    - 行为断言：见 action 各测点（双行帧、8 事件形状、text 路径顺序、ERROR 不发 message_stop、stop_reason 映射、usage 改名、aggregate_message 形状）。
    - 测试命令：`cd server && uv run pytest tests/compat/test_anthropic_adapter.py -q` 全绿。
  </acceptance_criteria>
  <verify>
    <automated>cd server && uv run ruff check compat/anthropic_adapter.py compat/error_handlers.py && uv run pytest tests/compat/test_anthropic_adapter.py -q</automated>
  </verify>
  <done>anthropic_adapter.py 落地：双行帧编码 helper（不复用 sse_encode）、8 事件骨架纯函数、stop_reason/usage 映射纯函数、translate_stream text/收尾路径（THINKING/TOOL_USE 静默、ERROR 不发 message_stop）、aggregate_message 非流式聚合（content 仅 text 正文、usage 改名）；anthropic_error_response helper；纯函数+adapter 集成测全绿；绝不发 tool_use block。</done>
</task>

<task type="auto">
  <name>Task 3: MessagesView 非流式（views.py）+ urls 两行注册 + view 级非流式集成测 + 零回归</name>
  <read_first>
    - server/compat/views.py:51-143（ChatCompletionsView 结构：authentication_classes=[]/permission_classes=[OptionalBearerTokenAuth]、serializer 校验失败、_build_runner None→503、非流式聚合范式——MessagesView 镜像换 Anthropic 形状）+ :33-48（_build_runner 复用）
    - server/compat/urls.py（既有 4 行 path 逐字不动，仅追加 messages 两行）
    - server/compat/anthropic_adapter.py（Task 2 的 aggregate_message）、server/compat/request_handler.py（anthropic_to_openai_messages + prepare_messages_with_meta）、server/compat/error_handlers.py（anthropic_error_response）、server/compat/schemas.py（AnthropicMessagesRequestSerializer）
    - 57-RESEARCH.md §4 第 3 点（MessagesView 结构）、§3 D-5（非流式聚合默认丢 trace）、§5 P-8/P-9/P-10、F-6（响应形状）
    - server/tests/compat/test_chat_completions.py:35-137（AsyncClient + patch _build_runner / patch prepare_messages_with_meta view 级范式，直接镜像）
  </read_first>
  <behavior>
    - `MessagesView(APIView)`（views.py 新增，**不改** ChatCompletionsView/ModelsView/_build_runner）：`authentication_classes=[]`、`permission_classes=[OptionalBearerTokenAuth]`（P-9 async 禁 JWT lazy-load user）。`async def post(request)`：
      1. `AnthropicMessagesRequestSerializer(data=request.data)`；`is_valid()` 失败 → `anthropic_error_response(str(serializer.errors), type_="invalid_request_error", http_status=400)`。
      2. 规整：`oai_messages = anthropic_to_openai_messages(params.get("system"), params["messages"])`；`lc_messages, retr = await prepare_messages_with_meta(messages=oai_messages, repository_ids=[str(r) for r in params.get("repository_ids") or []], project_id=str(params["project_id"]) if params.get("project_id") else None)`。
      3. `runner = await _build_runner()`；None → `anthropic_error_response("LLM 提供商凭证未配置...", type_="api_error", http_status=503)`。
      4. `model_name = "friday-default"`（固定，忽略客户端 model）。
      5. **本 plan 仅非流式**：`stream=True` 暂走非流式同路径或返回 501/留 Plan 02 接线——**采纳**：`if params.get("stream"):` 暂时**也走非流式聚合**并标注 TODO(Plan 02 接流式)，或更稳妥地保持仅非流式实现、流式分支留待 Plan 02（避免半成品流式）。本 plan 决策：流式分支**留空待 Plan 02**——若 `stream=True` 暂返回非流式聚合结果（功能可用、Plan 02 替换为真正 SSE）。在代码处加显式中文 TODO 注释引用 Plan 02。
      6. 非流式：`message = await aggregate_message(runner, lc_messages, model=model_name)`（try/except RuntimeError → `anthropic_error_response(str(e), type_="api_error", http_status=500)`）；`return Response(message)`（已是 Anthropic Messages 形状 dict）。retr 非流式忽略（不合成 trace、不污染 content，P-8）。
    - urls.py：追加 `path("messages", MessagesView.as_view())` + `path("messages/", MessagesView.as_view())`；import 增 `MessagesView`。既有 4 行 path 逐字不变。
  </behavior>
  <action>
  1. `server/compat/views.py`：顶部 import 增 `from .anthropic_adapter import aggregate_message`、`from .request_handler import anthropic_to_openai_messages`（既有 `prepare_messages_with_meta` import 保留）、`from .error_handlers import anthropic_error_response`（与 openai_error_response 并列）、`from .schemas import AnthropicMessagesRequestSerializer`。新增 `class MessagesView(APIView)`（依 behavior）。**绝不**改 ChatCompletionsView/ModelsView/_build_runner 任何符号。流式分支按 behavior 步骤 5 留 Plan 02 TODO（暂走非流式聚合，加中文注释 `# TODO(Plan 02 57-02)：流式 SSE 接线 + thinking prelude`）。
  2. `server/compat/urls.py`：import `MessagesView`；urlpatterns 追加两行 messages path。
  3. 新建 `server/tests/compat/test_messages.py`（AsyncClient + patch，镜像 test_chat_completions.py）：
     - **非流式响应形状（6.3 / ANTHROPIC-01）**：patch `compat.views._build_runner`(AsyncMock→runner 仅 TEXT_DELTA "Hello, "+"world!"+MESSAGE_COMPLETE{usage:{input:5,output:3},status:completed}) + patch `compat.views.prepare_messages_with_meta`(AsyncMock→([HumanMessage], None)) → `POST /v1/messages` stream=False, max_tokens=1024 → 200；data 形状 `{id startswith "msg_", type=="message", role=="assistant", content==[{"type":"text","text":"Hello, world!"}], model=="friday-default", stop_reason=="end_turn", stop_sequence is None, usage=={"input_tokens":5,"output_tokens":3}}`。
     - **content 零污染（P-8）**：上例断言 `content[0]["text"]=="Hello, world!"`，无额外 thinking/progress 文本；usage 仅 input_tokens/output_tokens（无 input/output 原 key）。
     - **max_tokens 缺失 → 400 Anthropic envelope**：`POST` 不带 max_tokens → 400，body `{"type":"error","error":{"type":...,"message":...}}`（断言含 `data["type"]=="error"` 与 `data["error"]["type"]`/`["message"]`）。
     - **_build_runner None → 503**：patch `_build_runner` 返回 None + patch prepare_messages_with_meta → 503 + Anthropic error envelope。
     - **双注册 + 末尾斜杠**：`POST /v1/messages/`（带斜杠）同样 200（patch 同上）。
     - **零回归**：本文件不动既有测试；最终 `uv run pytest tests/compat/ -q` 全套（OpenAI adapter/view/progress/auth + 新增）全绿。
  ruff 行宽 100，import I001，注释中文。
  </action>
  <acceptance_criteria>
    - 源断言：`views.py` 含 `class MessagesView`，含 `authentication_classes` `= []` 与 `permission_classes = [OptionalBearerTokenAuth]`、调用 `anthropic_to_openai_messages`/`prepare_messages_with_meta`/`aggregate_message`/`anthropic_error_response`；`urls.py` 含两行 `messages` path 且既有 `chat/completions`×2、`models`×2 行逐字不变；grep `views.py` 既有 `class ChatCompletionsView` 主体未改（_build_runner / 非流式聚合）。
    - 行为断言：`POST /v1/messages` 非流式返回 Anthropic Messages 形状（id msg_*、type message、content[].text 正文、usage input_tokens/output_tokens）；max_tokens 缺失 400 Anthropic envelope；runner None 503。
    - 测试命令：`cd server && uv run pytest tests/compat/ -q` 全绿（既有 OpenAI 全套 + 新增 anthropic 三文件不回退）。
  </acceptance_criteria>
  <verify>
    <automated>cd server && uv run ruff check compat/views.py compat/urls.py && uv run pytest tests/compat/ -q</automated>
  </verify>
  <done>MessagesView 非流式落地（serializer 校验→Anthropic 400、_build_runner None→503、规整+内核+aggregate_message→Anthropic Messages 形状、content 零污染、usage 改名）；urls 双注册（既有 4 行不动）；view 级非流式集成测 + 零回归全绿（OpenAI 端点逐字不变、progress.py 未改）。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Anthropic 请求 → 内核 | 客户端 system/messages/content 经 serializer 校验 + anthropic_to_openai_messages 规整后委托 prepare_messages_with_meta；max_tokens 必填挡非法请求 |
| AgentEvent.data → Anthropic 响应 | MESSAGE_COMPLETE.usage(input/output)/status、TEXT_DELTA.text 翻译为 Anthropic 形状；THINKING/tool 入参出参绝不外透（INV-5） |
| 非流式 content[].text | 仅承载 TEXT_DELTA 正文，绝不混入 trace/thinking/progress（P-8） |
| 错误响应 | Anthropic error envelope，不泄漏 traceback（ASVS V8.3 / P-10） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-01-01 | Information Disclosure | 非流式 content 或响应泄漏 THINKING/tool_input/result/CoT 原文 | mitigate | aggregate_message 只拼 TEXT_DELTA.text；THINKING/其余 continue；content 零污染断言（INV-5/P-8） |
| T-57-01-02 | 协议误用 | 误发 tool_use content block 致规范客户端挂起 | mitigate | content_block.type 仅 text/thinking；adapter 测断言全流无 tool_use（TRACE-02/P-5） |
| T-57-01-03 | Information Disclosure | usage 透传 input/output 原 key 或 stop_reason 非法 | mitigate | _rename_usage 改名 input_tokens/output_tokens；_status_to_stop_reason 归 end_turn（P-2/D-3） |
| T-57-01-04 | Tampering / DoS | max_tokens 缺失/非法请求穿透到内核 | mitigate | serializer max_tokens required min 1 → 400 Anthropic envelope |
| T-57-01-05 | Information Disclosure | 错误响应泄漏 traceback | mitigate | anthropic_error_response 仅 {type,error:{type,message}}；ERROR 事件 anthropic_error_event 不含 stack（P-10） |
| T-57-01-06 | 回归 | OpenAI 端点 / progress.py 漂移 | mitigate | 纯增量，OpenAI 符号逐字不变、progress.py 不改；tests/compat/ 全套全绿 |
</threat_model>

<verification>
- `cd server && uv run pytest tests/compat/ -q` 全绿（既有 OpenAI 全套 + 新增 test_anthropic_schemas/test_anthropic_adapter/test_messages）。
- `cd server && uv run ruff check compat/` 无 lint。
- grep `compat/anthropic_adapter.py`：无 `from .streaming import` / `sse_encode(` / `[DONE]` / `tool_use`。
- grep `compat/urls.py`：既有 `chat/completions`、`models` 4 行逐字不变；progress.py git diff 为空（未改）。
</verification>

<success_criteria>
- ANTHROPIC-01（请求映射）：serializer max_tokens 必填 + anthropic_to_openai_messages 把 system 提顶、messages/content 规整，委托既有内核（不重写 RAG/检索）。
- ANTHROPIC-01（非流式响应）：`POST /v1/messages` 返回合法 Anthropic Messages 形状（id msg_*、type message、role assistant、content:[{type:text,text}]、model、stop_reason、stop_sequence null、usage:{input_tokens,output_tokens}）。
- TRACE-02 / INV-5：绝不发 tool_use content block；非流式 content 仅 text 正文（零 trace 污染）；THINKING/tool 入参出参不外透；错误不泄漏 traceback。
- 复用 + 零回归：复用 _build_runner/prepare_messages_with_meta/OptionalBearerTokenAuth/_content_text；OpenAI 符号逐字不变、progress.py 不改；既有 tests/compat/ 全绿。
- 为 Plan 02 预埋：translate_stream（index 单线性计数）+ 事件骨架纯函数（含 thinking 系列）齐备，Plan 02 仅追加 prelude/TOOL_USE 分支。
</success_criteria>

<artifacts_this_phase_produces>
本 Plan（Wave 1）创建的符号：
- 新模块 `server/compat/anthropic_adapter.py`
  - 函数 `anthropic_sse_encode(event_type: str, data: dict) -> bytes`
  - 事件骨架纯函数 `message_start_event` / `content_block_start` / `content_block_delta_text` / `content_block_delta_thinking` / `content_block_stop` / `message_delta_event` / `message_stop_event` / `anthropic_error_event`
  - 映射纯函数 `_status_to_stop_reason(status) -> str` / `_rename_usage(usage) -> tuple[int,int]`
  - 类 `AnthropicCompatAdapter`（`translate_stream` staticmethod，text/收尾路径；Plan 02 扩展 prelude/TOOL_USE）
  - 函数 `aggregate_message(runner, prompt, *, model, msg_id=None) -> dict`（非流式聚合）
- 修改 `server/compat/schemas.py`：新增 `_AnthropicMessageSerializer`、`AnthropicMessagesRequestSerializer`
- 修改 `server/compat/request_handler.py`：新增纯函数 `anthropic_to_openai_messages(system, messages) -> list[dict]`
- 修改 `server/compat/error_handlers.py`：新增 `anthropic_error_response(message, type_, http_status) -> Response`
- 修改 `server/compat/views.py`：新增 `class MessagesView(APIView)`（非流式；流式分支 Plan 02 接）
- 修改 `server/compat/urls.py`：新增 `path("messages", ...)` + `path("messages/", ...)`（import MessagesView）
- 新测试文件 `server/tests/compat/test_anthropic_schemas.py` / `test_anthropic_adapter.py` / `test_messages.py`
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/57-anthropic-messages/57-01-SUMMARY.md` when done
</output>
