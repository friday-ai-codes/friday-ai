---
phase: 57-anthropic-messages
plan: 02
type: execute
wave: 2
depends_on: [57-01]
files_modified:
  - server/compat/anthropic_adapter.py
  - server/compat/views.py
  - server/tests/compat/test_anthropic_adapter.py
  - server/tests/compat/test_messages.py
autonomous: true
requirements: [ANTHROPIC-02]
must_haves:
  truths:
    - "AnthropicCompatAdapter.translate_stream 新增可选 prelude_texts: list[str] | None = None：非空（命中 RAG）→ 在正文 text block 之前发 thinking block（content_block_start(thinking, index 0) → 每条 content_block_delta_thinking(0, 文本) → content_block_stop(0)），正文 text block index 顺延为 1；prelude 为 None/空 → 不发 thinking block，text block 占 index 0（序列仍合法，与 Plan 01 逐字等价）"
    - "content block index 单线性计数器（D-2）：thinking block（有 prelude 时）占 0、text block 紧随；同一时刻只一个 block open，开 text block 前已发 thinking block 的 content_block_stop"
    - "translate_stream 新增 TOOL_USE_START/TOOL_USE_RESULT 前向兼容分支：调 tool_event_to_progress(evt)（progress.py 既有纯函数，不改），命中非空 → 在当前 thinking block 内发 content_block_delta_thinking；DEVIATION D-1——compat _build_runner 不绑定 tools，此分支当前永不触发（纯预埋）"
    - "INV-5 硬约束：THINKING 事件仍静默 continue（绝不映射 thinking_delta）；thinking block 仅承载 retrieval_to_progress/tool_event_to_progress 的高层语义文本（命中计数/工具名）；绝不内联 final_context/query/tool_input/result/error/CoT 原文"
    - "MessagesView.post(stream=True)：经 prepare_messages_with_meta 单次检索取 (lc_messages, retr)，prelude_texts = retrieval_to_progress(retr)，StreamingHttpResponse(content_type='text/event-stream') 经 _stream_anthropic 包 AnthropicCompatAdapter.translate_stream(..., prelude_texts=prelude_texts)；流式异常 → anthropic_error_event（不泄漏 traceback），不发 message_stop"
    - "MessagesView 非流式分支保持 Plan 01 行为：复用同一次 prepare_messages_with_meta 的 lc_messages（不二次检索），retr 忽略（不合成 thinking、content 零污染）"
    - "命中 RAG 时流式 SSE 序列：message_start → content_block_start(thinking,0) → thinking_delta×N → content_block_stop(0) → content_block_start(text,1) → text_delta×M → content_block_stop(1) → message_delta(end_turn) → message_stop；thinking progress 严格先于 text 正文"
    - "绝不发 tool_use content block（TRACE-02/P-5）；任一 content_block.type ∈ {text,thinking}；绝不复用 OpenAI sse_encode；流式不发 [DONE]，以 message_stop 收尾"
    - "零回归：prelude_texts=None/[] 时 translate_stream 与 Plan 01 逐字等价（既有 Plan 01 adapter 测试不回退）；MessagesView 非流式 content 逐字不变；既有 tests/compat/ OpenAI 全套全绿；progress.py 逐字不改"
  artifacts:
    - path: "server/compat/anthropic_adapter.py"
      provides: "translate_stream 新增 prelude_texts（thinking block trace）+ TOOL_USE_* 前向兼容分支（复用 progress.py 纯函数）"
      contains: "prelude_texts"
    - path: "server/compat/views.py"
      provides: "MessagesView 流式接线：prepare_messages_with_meta 派生 prelude + _stream_anthropic 包 translate_stream"
      contains: "_stream_anthropic"
    - path: "server/tests/compat/test_anthropic_adapter.py"
      provides: "thinking block prelude 集成测 + 无 prelude 降级 byte-eq + TOOL_USE 前向兼容 + sentinel（扩充）"
      contains: "prelude_texts"
    - path: "server/tests/compat/test_messages.py"
      provides: "MessagesView 流式 view 级集成测（thinking 先于 text）+ 安全 sentinel + 非流式 content 零回归（扩充）"
      contains: "stream"
  key_links:
    - from: "server/compat/views.py"
      to: "server/compat/request_handler.py"
      via: "流式分支调 prepare_messages_with_meta 取检索结果"
      pattern: "prepare_messages_with_meta"
    - from: "server/compat/views.py"
      to: "server/compat/progress.py"
      via: "由检索结果派生 prelude_texts（retrieval_to_progress，复用 Phase 56 纯函数，不改）"
      pattern: "retrieval_to_progress"
    - from: "server/compat/views.py"
      to: "server/compat/anthropic_adapter.py"
      via: "_stream_anthropic 经 translate_stream(prelude_texts=...) 透出 thinking block"
      pattern: "translate_stream"
    - from: "server/compat/anthropic_adapter.py"
      to: "server/compat/progress.py"
      via: "TOOL_USE_* 分支调 tool_event_to_progress（前向兼容预埋）"
      pattern: "tool_event_to_progress"
---

<objective>
兑现 ANTHROPIC-02 的**用户可见 trace**：让流式 `POST /v1/messages` 在命中 RAG 时，于正文 text block 之前以 Anthropic `thinking` content block（`content_block_start{type:thinking}` + `thinking_delta` + `content_block_stop`）透出"正在检索 RAG…/检索完成，命中 N 处"。数据源复用 Phase 56 已交付的 `retrieval_to_progress(retr)`（真实 RAG 命中计数，兑现可见效果），并预埋 `tool_event_to_progress` 经 `TOOL_USE_*` 的前向兼容分支（DEVIATION D-1：当前 compat runner 无 tools，永不触发）。

Purpose: Plan 01 已就位非流式 + adapter text 路径；本 Plan 接通流式 SSE（`MessagesView.post(stream=True)`）+ translate_stream 的 thinking block prelude，真正交付 ANTHROPIC-02 可见验收（"看得到 thinking block trace 先于正文"），同时严守 INV-5（thinking block 只承载命中计数/工具名高层语义、THINKING 事件不外透）、TRACE-02（绝不发 tool_use block）与零回归（无命中逐字等价、非流式 content 不变、OpenAI 端点 + progress.py 不改）。
Output: `anthropic_adapter.py` `translate_stream` 加 `prelude_texts` + thinking block + TOOL_USE_* 前向兼容分支；`views.py` `MessagesView` 流式接线（`_stream_anthropic` + prepare_messages_with_meta 单次检索派生 prelude）；扩充 `test_anthropic_adapter.py` / `test_messages.py`。

> **DEVIATION D-1（承接 Plan 01 / 57-RESEARCH §3 D-1，须显式记录）**：CONTEXT/里程碑默认假设可见 trace 经 `TOOL_USE_*` 事件流入 adapter。**实证不符**——compat `_build_runner` 不绑定 tools（`config.tools==[]`）→ `TOOL_USE_*` 永不发射。故 ANTHROPIC-02 可见 trace **由 `retrieval_to_progress`（检索命中计数）经 view 层 prelude 注入兑现**（与 Phase 56 OpenAI prelude 同源），`tool_event_to_progress` 经 `TOOL_USE_*` 分支为前向兼容**纯预埋**（未来 compat 绑定工具 / 真实工具调用时自动生效）。thinking block 仅在**命中**时合成（未命中 → 不发 thinking block，序列仍合法 = 优雅降级 P-7）。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.codex/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/57-anthropic-messages/57-CONTEXT.md
@.planning/phases/57-anthropic-messages/57-RESEARCH.md
@.planning/phases/57-anthropic-messages/57-VALIDATION.md
@.planning/phases/57-anthropic-messages/57-01-PLAN.md

# Plan 01 产出（本 plan 扩展）
@server/compat/anthropic_adapter.py
@server/compat/views.py

# 复用 Phase 56 纯函数（不改，仅 import）：retrieval_to_progress / tool_event_to_progress
@server/compat/progress.py
# 流式单次检索范式参考（ChatCompletionsView.post stream 分支 + _stream_chunks + prepare_messages_with_meta）
@server/compat/request_handler.py
# OpenAI prelude 注入范式参考（translate_stream prelude_texts，role chunk 后、正文前）——平行不改
@server/compat/adapter.py
# AgentEvent 形状 + TOOL_USE_START/RESULT 常量
@server/agents/core/events.py
# 既有守护测试范式（_make_runner/_collect/_collect_raw）+ Plan 01 anthropic adapter 测试
@server/tests/compat/test_adapter.py
@server/tests/compat/test_anthropic_adapter.py
# 既有 view 级范式（AsyncClient + patch _build_runner / LayeredSearchService.search）+ Plan 01 messages 测试
@server/tests/compat/test_chat_completions.py
@server/tests/compat/test_messages.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: translate_stream 接入 prelude thinking block（index 0）+ TOOL_USE_* 前向兼容分支 + adapter 流式集成测</name>
  <read_first>
    - server/compat/anthropic_adapter.py（Plan 01 的 translate_stream text/收尾路径 + content_block_*_thinking 事件骨架 + _status_to_stop_reason/_rename_usage）
    - server/compat/progress.py:50-66（tool_event_to_progress：仅 TOOL_USE_START 命中映射表返中文，否则 None；不改，仅 import）+ :69-101（retrieval_to_progress，view 层用）
    - server/compat/adapter.py:64-152（OpenAI prelude_texts 注入范式：role chunk 后逐条 make_reasoning_chunk；TOOL_USE_* 分支 tool_event_to_progress→make_reasoning_chunk None 静默——平行参考）
    - 57-RESEARCH.md §3 D-1（trace 数据源）/D-2（index 单线性计数：thinking 0、text 紧随）、§4 第 1 点（translate_stream 完整分派）、§5 P-3（THINKING 不外透）/P-5（不发 tool_use）/P-6（index 配对）/P-7（无 prelude 降级合法）
    - server/tests/compat/test_anthropic_adapter.py（Plan 01 测试，本 task 扩充；复用 _make_runner / 双行帧解析）
  </read_first>
  <behavior>
    - `translate_stream` 新增可选参 `prelude_texts: list[str] | None = None`（签名与 Plan 01 兼容——默认 None 即 Plan 01 行为逐字等价）。
    - index 单线性计数（D-2）：`next_index = 0`。message_start 之后：
      - 若 `prelude_texts`（非空）：发 thinking block —— `content_block_start(next_index, "thinking")` → 逐条 `content_block_delta_thinking(next_index, text)` → `content_block_stop(next_index)`；`thinking_index = next_index`；`next_index += 1`。
      - `text_index = next_index`（无 prelude→0，有 prelude→1）；发 `content_block_start(text_index, "text")`（D-2：text block 总在正文前发 start）。
    - runner 循环：
      - `TEXT_DELTA` → `content_block_delta_text(text_index, text)`。
      - `TOOL_USE_START`/`TOOL_USE_RESULT`（**前向兼容预埋**）→ `progress = tool_event_to_progress(evt)`；命中非空 → 若已开 thinking block（有 prelude）则在 thinking block 内 `content_block_delta_thinking(thinking_index, progress)`；**注**：当前 compat 无 tools，永不触发——加中文 DEVIATION 注释。返 None → continue（不产空 block）。
      - `MESSAGE_COMPLETE` → 记 stop_reason/output_tokens，break。
      - `ERROR` → `anthropic_error_event` 后 return（同 Plan 01）。
      - `THINKING` 及其余 → continue（INV-5：THINKING 绝不映射 thinking_delta）。
    - 收尾：`content_block_stop(text_index)` → `message_delta_event(stop_reason, output_tokens)` → `message_stop_event()`。
    - `prelude_texts` 为 None/空 → 不发 thinking block，text_index=0——与 Plan 01 路径逐字等价（零回归）。
  </behavior>
  <action>
  改 `server/compat/anthropic_adapter.py`：
  - import 增 `TOOL_USE_START, TOOL_USE_RESULT`（from agents.core.events）+ `from .progress import tool_event_to_progress`（复用 Phase 56 纯函数，**不改 progress.py**）。
  - `translate_stream` 签名加 `prelude_texts: list[str] | None = None`；按 behavior 重构：message_start 后插 thinking block（有 prelude）+ index 单线性计数；runner 循环加 `elif evt.type in (TOOL_USE_START, TOOL_USE_RESULT):` 前向兼容分支（调 tool_event_to_progress，命中且 thinking block 已开则发 thinking_delta，否则 continue）+ 中文 DEVIATION 注释（引用 57-RESEARCH D-1：compat 无 tools，预埋）。THINKING 仍 continue。
  - docstring 更新：注明 prelude_texts 用途（命中 RAG 时由 view 经 retrieval_to_progress 派生）、index 单线性计数、INV-5（thinking 仅高层语义、THINKING 不外透）。
  扩充 `server/tests/compat/test_anthropic_adapter.py`：
  - **thinking prelude 集成（6.2 / ANTHROPIC-02）**：`prelude_texts=["正在检索 RAG…","检索完成，命中 1 处"]` + runner(TEXT_DELTA"你好"+TEXT_DELTA"世界"+MESSAGE_COMPLETE{completed, usage:{input:5,output:2}}) → 断言事件**顺序**：`message_start → content_block_start(thinking,0) → thinking_delta×2 → content_block_stop(0) → content_block_start(text,1) → text_delta×2 → content_block_stop(1) → message_delta(end_turn,output_tokens=2) → message_stop`；trace 经 thinking_delta、正文经 text_delta、index 正确（thinking 0 / text 1）。
  - **无 prelude 降级（6.2 / P-7）**：`prelude_texts=[]`（或省略）+ TEXT_DELTA+MESSAGE_COMPLETE → 无 thinking block、text block 占 index 0、序列合法。
  - **零回归 byte-eq（6.3）**：`prelude_texts=None` / 省略 / `[]` 三者 translate_stream 输出结构逐字等价（去 msg_id 后），与 Plan 01 一致。
  - **不发 tool_use（TRACE-02/P-5）**：含 prelude 全流任一 content_block.type ∈ {text,thinking}，绝无 tool_use。
  - **TOOL_USE 前向兼容（预埋验证）**：注入 `prelude_texts=["x"]` + `TOOL_USE_START{tool_name:search_rag}` + TEXT_DELTA + MESSAGE_COMPLETE → tool_event_to_progress 命中 → 在 thinking block 内多一条 thinking_delta（"正在检索 RAG"）；注入未知工具名 → 无新增 thinking_delta（验证预埋分支逻辑，即便 runtime 不触发）。
  - **THINKING 不外透（INV-5/P-3）**：注入 `THINKING{thinking:"SENTINEL_COT"}` + TEXT_DELTA + MESSAGE_COMPLETE → 全流不含 "SENTINEL_COT"、无 thinking_delta 承载 CoT。
  - **stop_reason 映射**：interrupted/max_iterations → message_delta.stop_reason=="end_turn"（含 prelude 路径再验一次）。
  ruff 行宽 100，import I001，注释中文。
  </action>
  <acceptance_criteria>
    - 源断言：`anthropic_adapter.py` `translate_stream` 签名含 `prelude_texts`；含 `elif evt.type in (TOOL_USE_START, TOOL_USE_RESULT)` 分支调 `tool_event_to_progress`；import `from .progress import tool_event_to_progress`；grep 全文件仍**不含** `sse_encode(` / `[DONE]` / tool_use content block 写入；`progress.py` git diff 为空（未改）。
    - 行为断言：命中 prelude → thinking block(index 0) 先于 text block(index 1)，顺序如 action；无 prelude → 无 thinking block、text index 0、与 Plan 01 byte-eq；THINKING 不外透、无 tool_use。
    - 测试命令：`cd server && uv run pytest tests/compat/test_anthropic_adapter.py -q` 全绿（含 Plan 01 用例不回退）。
  </acceptance_criteria>
  <verify>
    <automated>cd server && uv run ruff check compat/anthropic_adapter.py && uv run pytest tests/compat/test_anthropic_adapter.py -q</automated>
  </verify>
  <done>translate_stream prelude thinking block（index 单线性计数：thinking 0、text 紧随）+ TOOL_USE_* 前向兼容分支（复用 tool_event_to_progress、当前不触发）落地；无 prelude 与 Plan 01 byte-eq；THINKING 不外透、绝不发 tool_use；progress.py 未改；adapter 集成测全绿。</done>
</task>

<task type="auto">
  <name>Task 2: MessagesView 流式接线（_stream_anthropic + prepare_messages_with_meta 单次检索派生 prelude）+ view 级流式集成测 + 安全 sentinel + 非流式 content 零回归</name>
  <read_first>
    - server/compat/views.py（Plan 01 MessagesView 非流式 + 流式 TODO 占位；ChatCompletionsView.post stream 分支 + _stream_chunks 范式镜像；_build_runner 复用）
    - server/compat/anthropic_adapter.py（Task 1 的 translate_stream(prelude_texts=...) + anthropic_error_event）
    - server/compat/progress.py:69-101（retrieval_to_progress：命中→["正在检索 RAG…","检索完成，命中 N 处"]，未命中→[]；不改，仅 import）
    - server/compat/request_handler.py:163-173（prepare_messages_with_meta 返回 (lc_messages, retr)）
    - 57-RESEARCH.md §4 第 3 点（MessagesView 流式接线）、§3 D-1（prelude 由 retrieval_to_progress 兑现）、§5 P-8（非流式 content 零污染）/P-9（async adrf）/P-10（流式 error 不泄漏）
    - server/tests/compat/test_adapter.py:403-491（view 级流式/非流式 patch 范式 test_view_stream_emits_rag_progress_before_body / test_view_non_stream_content_zero_regression：AsyncClient + patch _build_runner + patch compat.request_handler.LayeredSearchService.search 命中 final_context，prelude 先于正文）+ test_chat_completions.py:35-137（view 级基础范式，仅未命中 final_context="" 一例）+ test_messages.py（Plan 01 非流式用例，不回退）
  </read_first>
  <behavior>
    - `MessagesView.post`：替换 Plan 01 的流式 TODO 占位为真正 SSE 接线。统一在分支前 `lc_messages, retr = await prepare_messages_with_meta(...)`（单次检索，两路径共用 lc_messages——非流式 content 零回归命门）。
      - `stream=True`：`prelude_texts = retrieval_to_progress(retr)`（命中→2 条、未命中→[]）；`return StreamingHttpResponse(streaming_content=self._stream_anthropic(runner, lc_messages, model_name, prelude_texts), content_type="text/event-stream", headers={"Cache-Control":"no-cache","X-Accel-Buffering":"no"})`。
      - `stream=False`：保持 Plan 01——`aggregate_message(runner, lc_messages, model=model_name)`，retr 忽略（不传 prelude、content 零污染）。
    - `async def _stream_anthropic(self, runner, lc_messages, model_name, prelude_texts)`：try `async for chunk in AnthropicCompatAdapter.translate_stream(runner, lc_messages, model=model_name, prelude_texts=prelude_texts): yield chunk`；except → `logger.exception(...)` + `yield anthropic_sse_encode("error", anthropic_error_event("内部错误"))`（不泄漏 traceback，不发 message_stop）。Anthropic 流**不发 [DONE]**，以 translate_stream 内的 message_stop 收尾。
    - async + adrf（P-9）：MessagesView 仍 authentication_classes=[]/permission_classes=[OptionalBearerTokenAuth]；检索/runner 经既有 sync_to_async（内核已处理）。
  </behavior>
  <action>
  1. `server/compat/views.py`：import 增 `from .anthropic_adapter import AnthropicCompatAdapter, anthropic_error_event, anthropic_sse_encode`（aggregate_message 已 Plan 01 import）+ `from .progress import retrieval_to_progress`（既有 OpenAI 路径已 import retrieval_to_progress，复用同一 import 行即可）。`MessagesView.post`：把 Plan 01 流式 TODO 占位改为真正 `StreamingHttpResponse` + `_stream_anthropic`；非流式分支不变。新增 `async def _stream_anthropic(...)`（依 behavior，try/except 包 translate_stream）。**绝不**改 ChatCompletionsView/ModelsView/_build_runner。
  2. 扩充 `server/tests/compat/test_messages.py`（AsyncClient + patch，镜像 test_chat_completions.py 流式范式）：
     - **流式 thinking 先于 text（6.3 / ANTHROPIC-02 可见验收）**：patch `compat.views._build_runner`→runner(TEXT_DELTA"正文"+MESSAGE_COMPLETE{usage:{input:1,output:1},completed}) + patch `compat.request_handler.LayeredSearchService.search`→`SimpleNamespace(final_context="相关上下文", layers=[SimpleNamespace(result_count=2)], repository_ids=[], query="解释代码")` → `POST /v1/messages` stream=True, max_tokens=1024 → 200 + `content_type` 含 `text/event-stream`；收集 SSE 双行帧解析：断言出现 `event: content_block_start`(thinking) + `thinking_delta`("正在检索 RAG…"/"检索完成，命中 2 处") **先于** 首个 `text_delta`("正文")；含 `event: message_stop`收尾；全流任一 content_block.type ∈ {text,thinking}（无 tool_use）。
     - **未命中降级（P-7）**：search→`SimpleNamespace(final_context="")`（未命中）→ 流式无 thinking block、仅 text block + message_stop、序列合法。
     - **安全 sentinel（6.4 / INV-5）**：search 命中结果 `final_context="SENTINEL_CTX_secret 敏感片段"`、`query="SENTINEL_Q"`，runner 注入 `THINKING{thinking:"SENTINEL_COT"}` + TEXT_DELTA + MESSAGE_COMPLETE → 全量 SSE 字节流**不含** "SENTINEL_CTX_secret" / "SENTINEL_Q" / "SENTINEL_COT"（只透出"命中 N 处"计数语义）。
     - **非流式 content 零回归（P-8）**：命中检索（search final_context 非空）下 `stream=False` → message.content 仅 TEXT_DELTA 正文（逐字不变，不引入 thinking/检索 progress）；usage 仅 input_tokens/output_tokens。
     - **流式异常 → error 事件**：patch runner.stream 抛异常（或 translate_stream 内 ERROR）→ SSE 含 `event: error` + `{type:error,error:{type:api_error}}`，不含 traceback、不发 message_stop。
  3. 末轮跑 `uv run pytest tests/compat/ -q` 全套（OpenAI 全套 + Plan 01/02 anthropic）确认零回归。
  ruff 行宽 100，import I001，注释中文。
  </action>
  <acceptance_criteria>
    - 源断言：`views.py` `MessagesView` 含 `async def _stream_anthropic`、流式分支 `StreamingHttpResponse(content_type="text/event-stream")` 调 `translate_stream(..., prelude_texts=...)`、`prelude_texts = retrieval_to_progress(retr)`；非流式分支仍调 `aggregate_message`；grep 全文件流式路径**不含** `[DONE]`、不含 tool_use；progress.py git diff 为空。
    - 行为断言：流式命中 RAG → thinking_delta（命中 N 处）严格先于 text_delta 正文、含 message_stop；未命中 → 无 thinking block；非流式 content 逐字不变；sentinel 全不出现；流式异常 → event: error 不泄漏 traceback。
    - 测试命令：`cd server && uv run pytest tests/compat/ -q` 全绿（OpenAI 全套 + anthropic 全套不回退）。
  </acceptance_criteria>
  <verify>
    <automated>cd server && uv run ruff check compat/views.py && uv run pytest tests/compat/ -q</automated>
  </verify>
  <done>MessagesView 流式接线（单次检索派生 prelude → _stream_anthropic 包 translate_stream(prelude_texts)、流式异常 anthropic_error_event 不泄漏 traceback、不发 [DONE]）；view 级流式集成测（thinking 先于 text、未命中降级、message_stop 收尾）+ 安全 sentinel + 非流式 content 零回归全绿；OpenAI 端点 + progress.py 逐字不变。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RagSearchResult → thinking block 文本 | 检索结果含 final_context（代码上下文，敏感），prelude 只允许取 retrieval_to_progress 的命中计数语义 |
| view 检索元数据 → SSE thinking block | 对外仅透出"命中 N 处"计数，绝不回显 final_context/query |
| AgentEvent.data → SSE | THINKING/tool_input/result/error 绝不外透；thinking block 仅高层语义（INV-5） |
| 流式 vs 非流式分支 | thinking prelude 仅注入流式；非流式 content 必须逐字不变（P-8） |
| 流式错误 | event: error envelope，不泄漏 traceback（ASVS V8.3 / P-10） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-02-01 | Information Disclosure | thinking block 泄漏 final_context/query/tool_input/CoT 原文 | mitigate | prelude 仅取 retrieval_to_progress 命中计数；THINKING continue 不外透；tool_event_to_progress 仅读 tool_name；sentinel 全流断言（INV-5/P-3） |
| T-57-02-02 | 协议误用 | 误发 tool_use content block 致规范客户端挂起 | mitigate | content_block.type 仅 text/thinking；TOOL_USE 分支只发 thinking_delta；测断言全流无 tool_use（TRACE-02/P-5） |
| T-57-02-03 | 协议误用 | thinking/text block index 错乱或漏发 content_block_stop | mitigate | index 单线性计数（thinking 0、text 紧随）；开 text 前已 stop thinking；顺序集成测断言（P-6） |
| T-57-02-04 | 回归 | thinking prelude 污染非流式 content / 无命中产空 thinking block | mitigate | 非流式不传 prelude（content 逐字不变）；未命中 retrieval_to_progress→[]→无 thinking block；byte-eq + 非流式 content 断言（P-7/P-8） |
| T-57-02-05 | Information Disclosure | 流式异常泄漏 traceback | mitigate | _stream_anthropic except → anthropic_error_event("内部错误")，logger.exception 仅服务端（P-10） |
| T-57-02-06 | 回归 | OpenAI 端点 / progress.py 漂移 | mitigate | 纯增量复用 progress.py（不改）；OpenAI 符号逐字不变；tests/compat/ 全套全绿 |
</threat_model>

<verification>
- `cd server && uv run pytest tests/compat/ -q` 全绿（OpenAI 全套 + Plan 01/02 anthropic）。
- `cd server && uv run ruff check compat/` 无 lint。
- grep `compat/anthropic_adapter.py` / `compat/views.py` 流式路径：无 `sse_encode(`、无 `[DONE]`、无 tool_use content block 写入。
- `git diff server/compat/progress.py` 为空（Phase 56 纯函数逐字复用未改）。
</verification>

<success_criteria>
- ANTHROPIC-02（流式 SSE 序列）：`POST /v1/messages` stream=True 产合法 Anthropic SSE（message_start → content_block_* → message_delta → message_stop），双行帧 event:+data:、不发 [DONE]。
- ANTHROPIC-02（trace 经 thinking block）：命中 RAG 时 thinking block（thinking_delta 承载"正在检索 RAG…/命中 N 处"）严格先于正文 text block；数据源复用 retrieval_to_progress（D-1 兑现），tool_event_to_progress 为前向兼容预埋。
- INV-5 / TRACE-02：THINKING 事件不外透、thinking block 仅高层语义；绝不发 tool_use content block；sentinel 全不泄漏；流式错误不泄漏 traceback。
- 优雅降级 + 零回归：未命中 → 无 thinking block 序列仍合法；prelude None/[] 与 Plan 01 byte-eq；非流式 content 逐字不变；OpenAI 端点 + progress.py 逐字不改；tests/compat/ 全绿。
</success_criteria>

<artifacts_this_phase_produces>
本 Plan（Wave 2）创建/修改的符号：
- 修改 `server/compat/anthropic_adapter.py`：`AnthropicCompatAdapter.translate_stream` 新增 `prelude_texts: list[str] | None = None` 参数（thinking block trace）+ `TOOL_USE_START/TOOL_USE_RESULT` 前向兼容分支（import `tool_event_to_progress` / `TOOL_USE_START` / `TOOL_USE_RESULT`）
- 修改 `server/compat/views.py`：`MessagesView` 流式分支接线（`prepare_messages_with_meta` + `retrieval_to_progress` → `prelude_texts`）；新增方法 `async def _stream_anthropic(self, runner, lc_messages, model_name, prelude_texts)`
- 扩充 `server/tests/compat/test_anthropic_adapter.py`（thinking prelude 集成 + 无 prelude 降级 byte-eq + TOOL_USE 前向兼容 + THINKING 不外透 + stop_reason）
- 扩充 `server/tests/compat/test_messages.py`（流式 view 级 thinking 先于 text + 未命中降级 + 安全 sentinel + 非流式 content 零回归 + 流式 error）
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/57-anthropic-messages/57-02-SUMMARY.md` when done
</output>
