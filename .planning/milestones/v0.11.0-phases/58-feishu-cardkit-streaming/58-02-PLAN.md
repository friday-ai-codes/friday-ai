---
phase: 58-feishu-cardkit-streaming
plan: 02
type: execute
wave: 2
depends_on: [58-01]
files_modified:
  - server/feishu/bot/service.py
  - server/tests/test_feishu_bot_pipeline.py
autonomous: true
requirements: [CARD-01]
must_haves:
  truths:
    - "D-2 降级架构：既有 thinking 卡（line 129 send_card build_thinking_card）+ 所有非流式分支（澄清/图片错误/waiting/空答案/异常）+ _replace_card/update_card/send_card/build_answer_card 完整保留；CardKit 仅增强 line 270-376 happy 正文流"
    - "P-1：bot 流式段只消费 TEXT_DELTA（event.data['text']）累积 body，绝不消费 PART_DELTA（双轨共存否则正文翻倍）；TOOL_USE_START/PHASE_TRANSITION/MESSAGE_COMPLETE 既有处理保留"
    - "惰性创建：首个 TEXT_DELTA 到达才 try create_card_entity(build_streaming_card_v2) + send_card_entity；失败 → warning + cardkit=None（本轮不再尝试）；纯工具/waiting/空答案轮次不产 TEXT_DELTA 故不创建 CardKit 实体（F-1）"
    - "P-4：每个 TEXT_DELTA 累积 body += text，节流后推 stream_card_content(content=body 全量, sequence=next())；P-2：sequence 单调计数器（content+settle 共享，每次写前 +1，起始 1）；P-3：节流 ~300ms 合并 delta（首个 delta 与终态强制 flush）"
    - "D-4：TOOL_USE_START 仍走既有 update_card(thinking_card_id, build_streaming_card(tool_names))（PATCH，工具进度留 thinking 卡，不并入 CardKit）"
    - "D-3 终态：CardKit 成功且有正文 → 最后一次 stream_card_content(content=build_answer_markdown(answer,refs,usage,label)) + settle_card_stream(streaming_mode=false)；thinking 卡 _replace_card 收口避免「思考中」悬挂"
    - "P-9 fail-soft：CardKit 任一步（create/send/stream/settle）抛错 → try/except + 结构化 warning（含 card_id/code）+ cardkit=None，切回既有 build_answer_card + _replace_card(thinking_card_id) 终态（答案/引用/usage 不丢）；CardKit 失败绝不冒泡成 status=error"
    - "不变式：无论 CardKit 是否启用，结束恒有一条卡片承载最终答案（CardKit 流式卡 或 thinking→answer 卡）；result['status']=='answered'（有正文时）"
    - "P-9 边界：真实处理错误（image error / empty answer / waiting / 澄清 / 异常分支）仍走既有 error/waiting/clarification 卡，不被误并入 CardKit 降级而吞掉"
    - "零回归：既有 11 个 test_feishu_bot_pipeline 测试 + test_feishu_bot_integration + test_feishu_bot_cards 全绿；TOOL_USE_START/PHASE_TRANSITION/MESSAGE_COMPLETE/waiting poll/_replace_card/非流式分支逐字行为不变"
  artifacts:
    - path: "server/feishu/bot/service.py"
      provides: "_CardKitStream 轻量 dataclass（card_id/element_id/message_id/sequence 单调计数器 + next_sequence()）"
      contains: "class _CardKitStream"
    - path: "server/feishu/bot/service.py"
      provides: "process_message 流式段接入 TEXT_DELTA → CardKit 惰性创建 + 节流增量推送 + settle 收尾 + fail-soft 降级"
      contains: "TEXT_DELTA"
  key_links:
    - from: "server/feishu/bot/service.py"
      to: "server/services/feishu_im.py"
      via: "流式段调 im_service.create_card_entity/send_card_entity/stream_card_content/settle_card_stream（Wave 1 产物）"
      pattern: "stream_card_content"
    - from: "server/feishu/bot/service.py"
      to: "server/feishu/cards/bot_cards.py"
      via: "build_streaming_card_v2 建实体、build_answer_markdown 拼终态（Wave 1 产物）；降级仍用 build_answer_card"
      pattern: "build_answer_markdown"
    - from: "server/feishu/bot/service.py"
      to: "server/agents/core/events.py"
      via: "流式段按 AgentEvent.type 分派，新增消费 TEXT_DELTA"
      pattern: "TEXT_DELTA"
---

<objective>
完成 Phase 58 的 **bot 接线 + 优雅降级（Wave 2）**：把 `FeishuBotService.process_message` 流式段（line 259-376）从「仅消费 TOOL_USE_START + 终态 build_answer_card」升级为**新接 TEXT_DELTA → CardKit 流式增量**：首个 TEXT_DELTA 惰性创建 CardKit 实体（`build_streaming_card_v2` + `create_card_entity` + `send_card_entity`），每个 TEXT_DELTA 累积全量 body 经节流推 `stream_card_content`（sequence 单调递增），`MESSAGE_COMPLETE` 终态推 `build_answer_markdown` + `settle_card_stream` 收尾；全程 try/except fail-soft，CardKit 任一步失败 → 切回既有 thinking→`build_answer_card`→`_replace_card` 路径，答案/引用/usage 不丢（CARD-01 硬要求）。

Purpose: 兑现 CARD-01 + Success #1/#2/#3——正文随 AI 生成逐字增量推进同一张 CardKit 卡片（替代 PATCH 全量替换、无整段跳变），流式失败/租户未开通时优雅降级回既有路径、回复绝不丢失。复用 Wave 1 的 4 个 CardKit 方法与 2 个卡片 helper，**只改 bot/service.py 流式段**，非流式分支（澄清/图片错误/waiting/空答案/异常）零改动。
Output: `bot/service.py` 新增 `_CardKitStream` dataclass + 流式段 TEXT_DELTA 接线 + fail-soft 降级 + 终态 settle；扩展 `test_feishu_bot_pipeline.py`（流式集成测 + parts 双轨防翻倍 + 降级测 + 零回归）。

> **依赖 Wave 1（58-01）**：`im_service.create_card_entity/send_card_entity/stream_card_content/settle_card_stream` 与 `build_streaming_card_v2/build_answer_markdown` 必须已落地。
> **关键坑**：P-1 只消费 TEXT_DELTA（绝不同时累积 PART_DELTA）；P-2 sequence content+settle 共享单调计数器；P-3 节流 ~300ms；P-4 content 全量；F-1 waiting 路径不发 TEXT_DELTA 天然走既有分支。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.codex/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/58-feishu-cardkit-streaming/58-CONTEXT.md
@.planning/phases/58-feishu-cardkit-streaming/58-RESEARCH.md
@.planning/phases/58-feishu-cardkit-streaming/58-VALIDATION.md
@.planning/phases/58-feishu-cardkit-streaming/58-01-PLAN.md

# 主改文件：流式段 line 259-376 接 TEXT_DELTA + 降级；非流式分支/_replace_card 不改
@server/feishu/bot/service.py
# Wave 1 产物：CardKit 4 方法 + service 委托（本 plan 调用）
@server/services/feishu_im.py
# Wave 1 产物：build_streaming_card_v2 + build_answer_markdown；降级仍用 build_answer_card
@server/feishu/cards/bot_cards.py
# 事件常量：新增 import TEXT_DELTA；TEXT_DELTA.data={"text","model","session_id"}；PART_DELTA 双轨（绝不消费）
@server/agents/core/events.py
# 既有 pipeline 测试范式（SimpleNamespace im_service + patch send_message_stream + AgentEvent 序列）——扩展，零回归
@server/tests/test_feishu_bot_pipeline.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: _CardKitStream dataclass + 流式段接 TEXT_DELTA（惰性创建 + 节流增量推送 + 终态 settle）+ 流式集成测</name>
  <read_first>
    - server/feishu/bot/service.py:259-300（流式循环：TOOL_USE_START→update_card build_streaming_card / PHASE_TRANSITION / MESSAGE_COMPLETE 既有处理——TEXT_DELTA 在此新增分支，工具/phase/complete 处理保留）、:301-384（终态：waiting poll / 空答案 / build_answer_card + _replace_card——CardKit 成功时终态改走 stream+settle、失败降级回此）、:1-42（import 块——新增 from agents.core.events import TEXT_DELTA）
    - server/services/feishu_im.py（Wave 1 新增 create_card_entity/send_card_entity/stream_card_content/settle_card_stream 委托签名）
    - server/feishu/cards/bot_cards.py（Wave 1 build_streaming_card_v2/build_answer_markdown）
    - 58-RESEARCH.md §3 D-2（降级架构流式段改造逐条）/D-3（终态 content PUT + settle）/D-4（工具进度留 thinking 卡）/D-5（sequence 计数器 + ~300ms 节流，首个 delta+终态强制 flush）/D-6（uuid 幂等）、§2 F-1（TEXT_DELTA 形状 event.data["text"]、waiting 不发）、§5 P-1/P-2/P-3/P-4/P-6（实体一次性，每轮新建）
    - 58-VALIDATION.md §6.2（流式集成测断言点）
  </read_first>
  <behavior>
    - 新增模块级轻量 dataclass `_CardKitStream`（`@dataclass(slots=True)`）：字段 `card_id: str`、`element_id: str`、`message_id: str`、`sequence: int = 0`；方法 `next_sequence(self) -> int`：`self.sequence += 1; return self.sequence`（P-2：content PUT 与 settle 共享，每次写前 +1，起始得 1）。
    - import 顶部新增 `TEXT_DELTA`（`from agents.core.events import MESSAGE_COMPLETE, PHASE_TRANSITION, TEXT_DELTA, TOOL_USE_START`）；新增 `import time`（节流计时）；`build_streaming_card_v2`/`build_answer_markdown` 加入 `feishu.cards.bot_cards` import。
    - 流式段（line 270-300 循环内）状态：循环前初始化 `cardkit: _CardKitStream | None = None`、`body = ""`、`last_push_at = 0.0`、`element_id = "md_body"`、`stream_throttle_s = 0.3`（节流阈值，the agent's Discretion 默认 0.3）。
    - 新增 `elif event.type == TEXT_DELTA:` 分支（**与 TOOL_USE_START/PHASE_TRANSITION/MESSAGE_COMPLETE 并列**）：
      1. `delta = str(event.data.get("text") or "")`；`if not delta: continue`。
      2. `body += delta`（P-4 累积全量；**仅 TEXT_DELTA，P-1 绝不读 PART_DELTA**）。
      3. 惰性创建：`if cardkit is None:` → `try`: `cid = await im_service.create_card_entity(build_streaming_card_v2(element_id=element_id))`；`mid = await im_service.send_card_entity(receive_id=message.chat_id, receive_id_type="chat_id", card_id=cid)`；`cardkit = _CardKitStream(card_id=cid, element_id=element_id, message_id=mid)`。`except Exception:` → `logger.warning("feishu_cardkit_create_failed", chat_id=..., exc_info=True)`；`cardkit = None`（保持失效，本轮后续 TEXT_DELTA 不再重试创建——可用单独 `cardkit_disabled = True` 标记避免每次重试）。
      4. 节流推送：`if cardkit is not None:` → `now = time.monotonic()`；`if now - last_push_at >= stream_throttle_s:` → `try`: `await im_service.stream_card_content(card_id=cardkit.card_id, element_id=cardkit.element_id, content=body, sequence=cardkit.next_sequence(), uuid=uuid.uuid4().hex)`；`last_push_at = now`。`except Exception:` → warning + 设 `cardkit_disabled=True` + `cardkit=None`（降级，本轮后续不再推）。
    - TOOL_USE_START 分支：**保持既有** `update_card(thinking_card_id, build_streaming_card(tool_names))`（D-4，不改）。
    - MESSAGE_COMPLETE 后终态（在既有 final_answer/references/usage_info 计算之后、build_answer_card 之前）：
      - **CardKit 成功且 final_answer 非空**（`cardkit is not None`）→ `try`: 终态串 `final_md = build_answer_markdown(final_answer, references, usage_info, matched_space_label)`；`await im_service.stream_card_content(card_id=cardkit.card_id, element_id=cardkit.element_id, content=final_md, sequence=cardkit.next_sequence(), uuid=uuid.uuid4().hex)`（强制 flush，无视节流）；`await im_service.settle_card_stream(card_id=cardkit.card_id, sequence=cardkit.next_sequence(), uuid=uuid.uuid4().hex)`；并把 thinking 卡 `_replace_card(thinking_card_id → build_streaming_card(tool_names))` 收口（避免「思考中」悬挂；tool_names 为空则 build_streaming_card([]) 即「思考中...」→ 可改为简短收口卡，the agent's Discretion，保持非空）；`thread.last_bot_message_id = cardkit.message_id`；置 `cardkit_done = True`。`except Exception:` → warning + `cardkit_done=False`（落入下方既有 build_answer_card 降级）。
      - **CardKit 不可用 / 终态失败 / 无正文**（`cardkit is None` 或终态 except 或既有空答案/waiting 分支）→ **完全走既有**：`build_answer_card(...)` + `_replace_card(thinking_card_id → answer_card)`（逐字保留 line 362-376）。
    - waiting / 空答案 / 图片错误 / 澄清 / 异常分支：**逐字不动**（P-9 边界：CardKit 失败 ≠ 处理失败）。waiting 路径无 TEXT_DELTA（F-1）故 cardkit 恒为 None，天然走既有。
  </behavior>
  <action>
  1. `server/feishu/bot/service.py`：
     - 顶部 import：`from agents.core.events import ...` 增 `TEXT_DELTA`；新增 `import time`；`from feishu.cards.bot_cards import (...)` 增 `build_streaming_card_v2, build_answer_markdown`。
     - 模块级新增 `@dataclass(slots=True) class _CardKitStream`（依 behavior）。
     - 流式循环前初始化 `cardkit`/`body`/`last_push_at`/`cardkit_disabled` 等状态变量。
     - 循环内新增 `elif event.type == TEXT_DELTA:` 分支（惰性创建 + 节流推送，全程 try/except fail-soft）。**不改** TOOL_USE_START/PHASE_TRANSITION/MESSAGE_COMPLETE 分支既有逻辑。
     - MESSAGE_COMPLETE 终态：在 references/usage_info 计算后插入「CardKit 成功 → stream final_md + settle + 收口 thinking 卡 + 设 cardkit_done」；用 `if not cardkit_done:` 包住既有 build_answer_card + _replace_card 降级路径（既有逻辑逐字保留，仅加守卫）。
     - 全程结构化 warning（`feishu_cardkit_create_failed`/`feishu_cardkit_stream_failed`/`feishu_cardkit_settle_failed`，带 chat_id/card_id/exc_info），CardKit 失败不改 result（仍 `{"status":"answered", ...}`）。
  2. `server/tests/test_feishu_bot_pipeline.py`：新增 fake stream helper `_text_delta_stream`（产 `TOOL_USE_START` + `TEXT_DELTA("你")` + `TEXT_DELTA("好")` + 可选 `PART_DELTA("不应消费")` + `MESSAGE_COMPLETE(final_answer="你好", usage)`）；im_service `SimpleNamespace` 增 `create_card_entity=AsyncMock(return_value="c_1")`、`send_card_entity=AsyncMock(return_value="om_1")`、`stream_card_content=AsyncMock(return_value=True)`、`settle_card_stream=AsyncMock(return_value=True)`；新增测试 `test_text_delta_stream_drives_cardkit_incremental`：
     - 断言首个 TEXT_DELTA 触发 `create_card_entity` 1 次（arg 为 schema 2.0 卡）+ `send_card_entity` 1 次；
     - `stream_card_content` 被调多次，**content 参数累积全量**（"你"→"你好"→终态含 "你好"），**sequence 参数严格递增**（收集 await_args_list 的 sequence kwarg 断言单调 +1）；
     - 终态 `stream_card_content`（含 build_answer_markdown 输出：`**回答**`/`你好`/引用区）+ `settle_card_stream` 各 ≥1 次；
     - TOOL_USE_START 仍触发 `update_card(thinking_card_id, ...)`（D-4，断言 update_card 被调且含「浏览文件」之类工具名）；
     - **P-1 防翻倍**：混入 PART_DELTA → 断言累积 body 不含重复（终态 content 不出现 "你好你好"，且 PART_DELTA 文本未被累积）；
     - `result["status"]=="answered"`，`thread.last_bot_message_id=="om_1"`（CardKit message_id）。
     - 节流：测试用 `patch feishu.bot.service` 的节流阈值为 0（如把 `stream_throttle_s` 设为模块常量 `_CARDKIT_STREAM_THROTTLE_S` 并 patch 为 0）确保每个 delta 都推，便于断言累积/sequence。
  ruff 行宽 100，import I001，注释/docstring 中文。
  </action>
  <acceptance_criteria>
    - 源断言：`bot/service.py` 含 `class _CardKitStream` 与 `elif event.type == TEXT_DELTA:`；import 含 `TEXT_DELTA`、`build_streaming_card_v2`、`build_answer_markdown`；既有 `elif event.type == TOOL_USE_START` / `MESSAGE_COMPLETE` 分支处理保留；`build_streaming_card(tool_names)` 工具进度 update_card 调用保留（D-4）。
    - 行为断言：见 action 测点（惰性创建 1 次、content 累积全量、sequence 严格递增、终态 stream+settle、工具进度仍 thinking 卡、PART_DELTA 不翻倍、status=answered、last_bot_message_id=CardKit message_id）。
    - 测试命令：`cd server && uv run pytest tests/test_feishu_bot_pipeline.py -q` 全绿（既有 11 测 + 新增）。
  </acceptance_criteria>
  <verify>
    <automated>cd server && uv run ruff check feishu/bot/service.py && uv run pytest tests/test_feishu_bot_pipeline.py -q</automated>
  </verify>
  <done>_CardKitStream + 流式段 TEXT_DELTA 接线（惰性创建 + 节流全量增量推送 + sequence 单调 + 终态 stream+settle）落地；工具进度保留 thinking 卡（D-4）；只消费 TEXT_DELTA 不翻倍（P-1）；流式集成测全绿。</done>
</task>

<task type="auto">
  <name>Task 2: fail-soft 降级路径 + 终态兜底 + 零回归测试</name>
  <read_first>
    - server/feishu/bot/service.py:354-419（终态 build_answer_card + _replace_card / 空答案 / 异常分支——降级须复用 build_answer_card 路径，逐字保留作兜底）+ :583-604（_replace_card fail-soft 范式）+ Task 1 新增的 CardKit 终态守卫 `if not cardkit_done:`
    - 58-RESEARCH.md §3 D-2（降级：create/stream/settle 失败切既有路径，答案不丢）、§2 F-5（错误码——统一 code!=0 抛 FeishuIMError）、§5 P-5（租户未开通→create 报权限错→降级）/P-9（fail-soft 边界：CardKit 失败 ≠ 处理失败）/P-8（旧客户端兜底由既有 answer 卡覆盖）
    - 58-VALIDATION.md §6.3（降级路径测）+ §6.4（零回归）
    - server/tests/test_feishu_bot_pipeline.py（既有 11 测——零回归必须全绿；test_update_failure_falls_back_to_new_answer_card / test_waiting_* / test_tool_use_updates_streaming_card / p2p 等范式）
  </read_first>
  <behavior>
    - 降级判定（Task 1 已埋 try/except）：CardKit `create_card_entity` / `send_card_entity` / `stream_card_content` / `settle_card_stream` 抛任何异常（含 Wave 1 的 `FeishuIMError`，如租户未开通 99991672 / 限频 / 300317）→ 捕获 + 结构化 warning + 设 `cardkit=None` & `cardkit_disabled=True`，**绝不冒泡**。
    - 终态兜底：`if not cardkit_done:`（CardKit 从未创建 / 中途失效 / 终态 stream/settle 失败）→ 走既有 `build_answer_card(question, answer=final_answer, references, usage=usage_info, compact=is_p2p, matched_space_label)` + `_replace_card(thinking_card_id → answer_card)`（line 362-376 逐字），`result` 仍 `{"status":"answered","session_id":...}`（答案/引用/usage 不丢）。
    - 边界保持（P-9）：image error（line 237-257）/ empty answer（line 338-352）/ waiting（line 311-336）/ clarification / 顶层 except（line 385-419）分支**逐字不变**——这些是真实处理错误/非正文场景，不走 CardKit、不被降级逻辑吞掉。
  </behavior>
  <action>
  1. `server/feishu/bot/service.py`：复核 Task 1 的 try/except 覆盖 create/send/stream/settle 四类调用（确保每个 `await im_service.*card*` 都在 try 内，except 落 warning + cardkit 失效）；确认 `if not cardkit_done:` 守卫包住既有 build_answer_card 降级且既有分支主体逐字未改。**本 task 主要补测试 + 收敛 fail-soft 守卫，不重写 Task 1 happy path。**
  2. `server/tests/test_feishu_bot_pipeline.py` 新增降级测试：
     - `test_cardkit_create_failure_falls_back_to_answer_card`：im_service `create_card_entity=AsyncMock(side_effect=FeishuIMError("租户未开通", code=99991672))`（+ 既有 send_card/update_card/_text_delta_stream）→ 断言：`stream_card_content` / `settle_card_stream` **从未被调**；终态走 `update_card`（_replace_card）出 build_answer_card（content 含「已参考上下文」/answer）；`result["status"]=="answered"`（答案不丢）。
     - `test_cardkit_stream_failure_midway_degrades`：`stream_card_content=AsyncMock(side_effect=[True, FeishuIMError("rate limit", code=99991400)])` → 断言中途失效后不再推、终态降级出 answer 卡、`result["status"]=="answered"`。
     - `test_cardkit_settle_failure_still_answers`：stream 全成功但 `settle_card_stream=AsyncMock(side_effect=FeishuIMError("settle fail"))` → 断言终态以 build_answer_card 兜底（不抛、status=answered）。
     - `test_waiting_stream_does_not_create_cardkit`：复用既有 `_waiting_stream`（无 TEXT_DELTA）→ 断言 `create_card_entity` 从未被调（零新 CardKit 调用）、走既有 background_analysis 卡、`result["status"]=="waiting"`（F-1 + P-9 边界）。
     - 零回归断言：本文件既有 11 测不改；新增测试不污染既有。
  3. 零回归门禁：运行既有 `test_feishu_bot_integration` + `test_feishu_bot_cards` 全绿（PATCH 卡片路径 / 工具进度 / p2p / 群聊 / welcome 不破）。
  ruff 行宽 100，import I001，注释中文。
  </action>
  <acceptance_criteria>
    - 源断言：`bot/service.py` 含 `if not cardkit_done` 守卫；CardKit 四类调用均在 try/except 内（grep `feishu_cardkit_` warning 事件 ≥3 处）；既有非流式分支（image/empty/waiting/clarification/异常）主体逐字未改（git diff 仅流式段 + import + dataclass）。
    - 行为断言：create/stream/settle 抛错 → answer 卡兜底、status=answered（答案不丢）；waiting 无 TEXT_DELTA → 零 CardKit 调用、status=waiting；CardKit 失败不冒泡成 status=error（P-9）。
    - 测试命令：`cd server && uv run pytest tests/test_feishu_bot_pipeline.py tests/test_feishu_bot_integration.py tests/test_feishu_bot_cards.py -q` 全绿。
  </acceptance_criteria>
  <verify>
    <automated>cd server && uv run ruff check feishu/bot/service.py && uv run pytest tests/test_feishu_bot_pipeline.py tests/test_feishu_bot_integration.py tests/test_feishu_bot_cards.py -q</automated>
  </verify>
  <done>fail-soft 降级落地：create/send/stream/settle 抛错 → 结构化 warning + 切既有 build_answer_card + _replace_card（答案/引用/usage 不丢、status=answered 不冒泡 error）；waiting/空答案/图片错误/澄清/异常分支零新 CardKit 调用逐字保留（P-9 边界）；降级测 + 零回归全绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| send_message_stream 事件 → bot 流式段 | 只消费 TEXT_DELTA（event.data["text"]）累积正文；PART_DELTA 双轨绝不消费（P-1 防翻倍）；TOOL_USE_START/PHASE_TRANSITION/MESSAGE_COMPLETE 既有处理 |
| bot → CardKit OpenAPI（经 Wave 1） | create/send/stream/settle 任一异常 try/except fail-soft，绝不冒泡成无回复/报错卡 |
| CardKit 失败 vs 真实处理错误 | CardKit 异常 → 降级出 answer 卡（status=answered）；真实错误（image/empty/异常）→ 既有 error 卡（status=error），两类严格分开（P-9） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-02 | 协议误用 / 数据正确性 | 同时消费 TEXT_DELTA + PART_DELTA 致正文翻倍 | mitigate | 只新增 TEXT_DELTA 分支，绝不读 PART_DELTA；测试混入 PART_DELTA 断言不翻倍（P-1） |
| T-58-03 | 数据正确性 | sequence 非单调（content+settle 共享计数器乱序）触发 300317 | mitigate | _CardKitStream.next_sequence 单调 +1，content/settle 共享；测试断言 sequence 严格递增（P-2） |
| T-58-04 | 可用性 / 数据丢失 | CardKit create/stream/settle 失败致回复丢失（违反 CARD-01） | mitigate | 全程 try/except + cardkit_done 守卫 → 降级 build_answer_card + _replace_card；降级测断言答案不丢（D-2/P-5/P-9） |
| T-58-05 | 错误处理 | CardKit 失败冒泡成 status=error / 无回复卡 | mitigate | CardKit except 仅 warning 不改 result；status 恒 answered（有正文）；测试断言不冒泡（P-9） |
| T-58-06 | 回归 | 既有 PATCH 卡片路径 / 工具进度 / 非流式分支漂移 | mitigate | 仅改流式段 + import + dataclass；非流式分支逐字不变；test_feishu_bot_pipeline/integration/cards 全套全绿 |
| T-58-07 | Information Disclosure | warning 日志泄漏 token/secret | mitigate | warning 仅记 chat_id/card_id/code/exc_info（对齐 common.logging 凭证保护），不记凭证 |
</threat_model>

<verification>
- `cd server && uv run pytest tests/test_feishu_bot_pipeline.py tests/test_feishu_bot_integration.py tests/test_feishu_bot_cards.py -q` 全绿（既有全套 + 新增流式/降级测）。
- `cd server && uv run ruff check feishu/bot/service.py` 无 lint。
- grep `feishu/bot/service.py`：含 `class _CardKitStream`、`elif event.type == TEXT_DELTA`、`build_answer_markdown`、`settle_card_stream`、`if not cardkit_done`；既有 `build_answer_card`/`_replace_card`/`build_streaming_card`/waiting/clarification 分支主体未改（git diff 限流式段）。
- grep `feishu/bot/service.py`：**不含** `PART_DELTA`（P-1 绝不消费）。
</verification>

<success_criteria>
- CARD-01 / Success #1：正文经 CardKit `stream_card_content` 增量更新（替代 PATCH 全量替换），content 累积全量前缀递增（打字机）。
- Success #2：节流 ~300ms 合并 delta（P-3 单卡 10QPS 内）+ content 全量前缀递增（P-4）→ 无明显闪烁/全量重绘（真实顺滑观感为人工验收 deferred）。
- Success #3 / CARD-01 硬要求：CardKit create/stream/settle 失败 → fail-soft 降级回 build_answer_card + _replace_card，答案/引用/usage 不丢、status=answered 不冒泡 error。
- D-1~D-7 落地：惰性创建（D-2）、终态 content PUT + settle（D-3）、工具进度留 thinking 卡（D-4）、sequence 单调 + 节流（D-5）、uuid 幂等（D-6）、无 SystemSetting 开关纯 fail-soft（D-7）。
- P-1 只消费 TEXT_DELTA 不翻倍；waiting/空答案/图片错误/澄清/异常分支零新 CardKit 调用逐字保留（P-9 边界）。
- 零回归：既有 test_feishu_bot_pipeline/integration/cards 全绿；PATCH 卡片路径与所有 builder 不破。
</success_criteria>

<artifacts_this_phase_produces>
本 Plan（Wave 2）创建/修改的符号：
- 修改 `server/feishu/bot/service.py`：
  - 模块级 `@dataclass(slots=True) class _CardKitStream`（字段 card_id/element_id/message_id/sequence；方法 `next_sequence(self) -> int`）
  - 模块级节流常量（如 `_CARDKIT_STREAM_THROTTLE_S = 0.3`，可被测试 patch）
  - `process_message` 流式段新增 `elif event.type == TEXT_DELTA:` 分支（惰性创建 + 节流增量推送）+ MESSAGE_COMPLETE 终态 CardKit stream+settle + `if not cardkit_done` 降级守卫
  - import 新增 `TEXT_DELTA`、`time`、`build_streaming_card_v2`、`build_answer_markdown`
- 扩展 `server/tests/test_feishu_bot_pipeline.py`：新增 `_text_delta_stream` helper + 流式集成测 + parts 双轨防翻倍测 + 4 个降级/边界测（create/stream/settle 失败 + waiting 零调用）
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/58-feishu-cardkit-streaming/58-02-SUMMARY.md` when done
</output>
