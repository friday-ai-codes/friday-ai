---
phase: 56-compat-progress-trace
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/compat/progress.py
  - server/compat/adapter.py
  - server/tests/compat/test_progress.py
  - server/tests/compat/test_adapter.py
autonomous: true
requirements: [TRACE-01, TRACE-02]
must_haves:
  truths:
    - "纯函数 helper 模块 server/compat/progress.py 存在：tool_event_to_progress(evt) 仅读 tool_name 查中文映射表，未知/无映射 → None（保守静默），绝不读 tool_input/result/error"
    - "工具名→中文语义映射表覆盖：search_rag→正在检索 RAG、grep→正在 grep 搜索、get_file→正在读取文件、仓库分析/路由→正在分析仓库；未知工具→None"
    - "make_reasoning_chunk(common, text, include_usage) 复用 sse_encode 产出合法 reasoning_content chunk（object=chat.completion.chunk、finish_reason=None、include_usage=True 时 usage=None）"
    - "adapter.translate_stream 把原 else:continue 丢弃的 TOOL_USE_START 经 helper 映射为 delta.reasoning_content progress chunk；TOOL_USE_RESULT 一律静默（helper 返 None，完成态计数留 Plan 02 retrieval_to_progress）；helper 返 None 时仍 continue（保持降级逐字等价）"
    - "TRACE-02/INV-5 硬约束：任何路径绝不写 delta.tool_calls、绝不把 finish_reason 赋为 tool_calls；progress 文本只含工具名高层中文语义，不内联 tool_input/result/error/THINKING/query 原文"
    - "零回归：无工具事件的既有序列（THINKING/TEXT_DELTA/MESSAGE_COMPLETE/ERROR）SSE 输出与现状逐字等价，不新增任何空 progress chunk；既有 5 个 adapter 测试保持全绿"
  artifacts:
    - path: "server/compat/progress.py"
      provides: "纯函数 progress 映射 helper（工具名映射表 + tool_event_to_progress + make_reasoning_chunk），与 adapter/view 解耦，便于独测 + Phase 57 复用"
      contains: "def tool_event_to_progress"
    - path: "server/compat/progress.py"
      provides: "工具名→中文语义集中映射表（纯数据常量）"
      contains: "_TOOL_PROGRESS_LABELS"
    - path: "server/compat/progress.py"
      provides: "复用 sse_encode 的 reasoning_content chunk 构造小工具"
      contains: "def make_reasoning_chunk"
    - path: "server/tests/compat/test_progress.py"
      provides: "纯函数映射单测 + 安全 sentinel 不泄漏测（新文件）"
      contains: "def test_"
  key_links:
    - from: "server/compat/adapter.py"
      to: "server/compat/progress.py"
      via: "translate_stream 在原 else 分支调 tool_event_to_progress / make_reasoning_chunk"
      pattern: "tool_event_to_progress"
    - from: "server/compat/progress.py"
      to: "server/compat/streaming.py"
      via: "make_reasoning_chunk 复用 sse_encode 序列化"
      pattern: "sse_encode"
---

<objective>
建立 compat progress 透出的**纯函数机制层**：新建 `server/compat/progress.py`（工具名→中文语义映射表 + `tool_event_to_progress(evt)->str|None` + `make_reasoning_chunk(...)`），并在 `adapter.py` `translate_stream` 把当前被 `else: continue`（adapter.py:124-127）丢弃的 `TOOL_USE_START`（及有完成摘要的 `TOOL_USE_RESULT`）映射为 `delta.reasoning_content` progress chunk。helper 做成与 adapter/view 解耦的可独测纯函数，为 Phase 57（Anthropic thinking block 复用同一映射）预埋抽象。

Purpose: 兑现 TRACE-01 的**机制就位**（progress 经事件 taxonomy 语义映射为 reasoning_content）+ TRACE-02/INV-5 硬约束（绝不 tool_calls、绝不泄漏 CoT/入参/出参原文），同时严守零回归（无工具事件序列逐字等价）。
Output: `server/compat/progress.py`（新模块）；`adapter.py translate_stream` 接入映射；`test_progress.py`（纯函数 + sentinel 安全测）；扩充 `test_adapter.py`（TOOL_USE_* 集成 + 零回归 byte-eq）。

> **DEVIATION（显式记录，来自 56-RESEARCH §3 D-1）**：CONTEXT 默认假设 `TOOL_USE_START/RESULT` 会流入 compat `translate_stream`。**实证与此不符**——`_build_runner()`（views.py:43-47）构造 `LangChainRunnerConfig` 时不传 tools（`config.tools==[]`），runner 主循环 `bind_tools` 仅在有 tools 时进入，故 compat 链路下 `TOOL_USE_*` **永不发射**。因此本 Plan 01 的 adapter 映射在**当前 compat 链路产 0 progress chunk**（恒走降级），属"机制预埋 + 前向兼容（未来 compat 绑定工具 / Phase 57 复用）"。TRACE-01 的**可见效果**由 Plan 02（真实 RAG 检索 progress 合成）兑现。两 Plan 合起来覆盖研究推荐的 Option C（A 机制 + B 效果）。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/56-compat-progress-trace/56-CONTEXT.md
@.planning/phases/56-compat-progress-trace/56-RESEARCH.md
@.planning/phases/56-compat-progress-trace/56-VALIDATION.md

# 主改点 + 既有 THINKING→reasoning_content 范式 + else:continue 丢弃点
@server/compat/adapter.py
# sse_encode / _omit（progress chunk 复用）
@server/compat/streaming.py
# AgentEvent 形状 + TOOL_USE_START/RESULT/THINKING 常量
@server/agents/core/events.py
# 既有守护测试范式（_make_runner / _collect 可直接复用）
@server/tests/compat/test_adapter.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 新建 server/compat/progress.py 纯函数 helper + 映射表（TRACE-01 机制 / INV-5）</name>
  <read_first>
    - server/agents/core/events.py（TOOL_USE_START="tool_use_start" / TOOL_USE_RESULT="tool_use_result" 常量；AgentEvent(type,data) dataclass）
    - server/compat/streaming.py（sse_encode 签名与 b"data: {...}\n\n" 帧格式）
    - server/compat/adapter.py:78-90（既有 THINKING→reasoning_content chunk 结构，make_reasoning_chunk 须与之逐字一致）
    - 56-RESEARCH.md §2 F-5（TOOL_USE_START.data={tool_call_id,tool_name,tool_input,model,session_id}；RESULT 成功含 result，失败含 error）、§3 D-4（映射表条目）、§5 P-1/P-2（不 import event_taxonomy 常量、不泄漏 tool_input/result/error）
    - 56-CONTEXT.md <decisions>（透出字段 reasoning_content、未知工具保守静默、progress 仅高层语义）
  </read_first>
  <behavior>
    - `tool_event_to_progress(evt: AgentEvent) -> str | None`：仅对 `TOOL_USE_START` 从 `evt.data.get("tool_name")` 取工具名查映射表（命中返中文文本，未命中/缺 tool_name → None）；`TOOL_USE_RESULT` **一律返回 None**（保守静默，OQ-3；完成态计数留 Plan 02 的 retrieval_to_progress 处理）；其余事件类型 → None。**绝不**读取 `evt.data["tool_input"]` / `["result"]` / `["error"]`。
    - 映射表（纯数据常量 `_TOOL_PROGRESS_LABELS`）：`search_rag`→"正在检索 RAG"、`grep`→"正在 grep 搜索"、`get_file`→"正在读取文件"、仓库分析/路由相关工具名→"正在分析仓库"。条目精确措辞对齐 §15 taxonomy 人类可读释义（knowledge.recalling / repo.routing），但**不** import event_taxonomy 常量（P-1）。
    - 非工具事件（TEXT_DELTA/THINKING/MESSAGE_COMPLETE）即使被误传入也返 None（防误命中）——实现上仅看 tool_name 即天然满足。
    - `make_reasoning_chunk(common: dict, text: str, include_usage: bool) -> bytes`：产出 reasoning_content progress chunk 字节，结构与 adapter.py:80-90 THINKING chunk **逐字一致**（`object` 来自 common、`choices=[{index:0, delta:{reasoning_content:text}, finish_reason:None}]`，`include_usage=True` 时 `usage=None`），复用 `sse_encode`。
  </behavior>
  <action>
  新建 `server/compat/progress.py`（zh-CN docstring，说明：本模块是 compat progress 透出的纯函数机制层；只读 tool_name 翻高层中文语义，绝不内联 tool_input/result/error/CoT（INV-5）；不 import §15 event_taxonomy 常量——那套事件不在 AgentEvent 流，仅语义对齐（P-1）；为 Phase 57 Anthropic thinking block 复用预留）。
  - `from agents.core.events import TOOL_USE_START, TOOL_USE_RESULT, AgentEvent`；`from .streaming import sse_encode`。
  - `_TOOL_PROGRESS_LABELS: dict[str, str]`：纯数据映射表（含上述 4 类语义；仓库分析/路由可含多个别名 key 指向"正在分析仓库"）。
  - `def tool_event_to_progress(evt: AgentEvent) -> str | None`：仅当 `evt.type == TOOL_USE_START` 时取 `evt.data.get("tool_name")` 查表返回 str 或 None；`evt.type == TOOL_USE_RESULT` **一律返回 None**（保守静默，OQ-3——本 plan 不透出 RESULT 完成态；F-6 计数派生留 Plan 02 检索 progress 处理）；其余 type 直接 None。docstring 标注安全约束。
  - `def make_reasoning_chunk(common: dict[str, Any], text: str, include_usage: bool) -> bytes`：构造并 `sse_encode` 返回 bytes。
  - 暴露 `__all__ = ["tool_event_to_progress", "make_reasoning_chunk"]`。ruff 行宽 100，注释中文。
  </action>
  <acceptance_criteria>
    - 源断言：`server/compat/progress.py` 存在；含 `def tool_event_to_progress`、`def make_reasoning_chunk`、`_TOOL_PROGRESS_LABELS`；grep 该文件**不含** `tool_input` / `["result"]` / `["error"]` 读取；**不含** `from delivery` / `event_taxonomy` import。
    - 行为断言：`tool_event_to_progress(AgentEvent(TOOL_USE_START, {"tool_name":"search_rag"}))=="正在检索 RAG"`；`grep`→"正在 grep 搜索"；`get_file`→"正在读取文件"；未知工具名 → None；缺 tool_name → None；`AgentEvent(THINKING, {...})`→None；**`tool_event_to_progress(AgentEvent(TOOL_USE_RESULT, {"tool_name":"search_rag", "success":True, "result":"..."})) is None`（RESULT 一律静默）**。
    - 测试命令：`cd server && uv run pytest tests/compat/test_progress.py -q`（Task 3 落地后全绿）。
  </acceptance_criteria>
  <verify>
    <automated>cd server && uv run ruff check compat/progress.py && uv run python -c "from compat.progress import tool_event_to_progress, make_reasoning_chunk"</automated>
  </verify>
  <done>progress.py 纯函数 helper + 映射表落地；只读 tool_name、不泄漏入参/出参/CoT；不 import event_taxonomy；make_reasoning_chunk 与既有 THINKING chunk 结构一致；import 无误、ruff 干净。</done>
</task>

<task type="auto">
  <name>Task 2: adapter.translate_stream 接入 TOOL_USE_* → reasoning_content 映射（TRACE-01 / TRACE-02 / 零回归）</name>
  <read_first>
    - server/compat/adapter.py:64-127（事件分派主循环；当前 else:continue 丢弃 TOOL_USE_*；THINKING 分支范式；finish_reason 字面量含 "tool_calls" 但不得赋值）
    - server/compat/progress.py（Task 1 产出的 tool_event_to_progress / make_reasoning_chunk）
    - 56-RESEARCH.md §4 第 2 点（else 分支改造方案）、§5 P-3/P-4/P-5（不写 tool_calls / include_usage 一致 / progress chunk finish_reason=None）
  </read_first>
  <behavior>
    - 在 `translate_stream` 主循环新增对 `TOOL_USE_START` / `TOOL_USE_RESULT` 的处理：调 `tool_event_to_progress(evt)`；返 None → `continue`（与现状降级逐字等价，不产空 chunk）；返非空 str → `yield make_reasoning_chunk(common, text, include_usage)`。
    - 其余未知事件类型（BUDGET_WARNING 等）仍走 `else: continue`，不变。
    - 绝不新增 `delta.tool_calls`；`finish_reason` 仍只由 MESSAGE_COMPLETE 赋 "stop"/"length"，progress chunk `finish_reason=None`。include_usage 语义与 TEXT_DELTA/THINKING 一致。
  </behavior>
  <action>
  改 `server/compat/adapter.py`：
  - 顶部 import 增 `TOOL_USE_START, TOOL_USE_RESULT`（from agents.core.events）与 `from .progress import make_reasoning_chunk, tool_event_to_progress`。
  - 在主循环 `elif evt.type == ERROR:` 之前（或 THINKING 之后）新增分支：
    ```
    elif evt.type in (TOOL_USE_START, TOOL_USE_RESULT):
        progress = tool_event_to_progress(evt)
        if progress is not None:
            yield make_reasoning_chunk(common, progress, include_usage)
        # progress 为 None → 不 emit（降级逐字等价）
    ```
  - 保留末尾 `else: continue`（覆盖 BUDGET_WARNING / 其余类型）。更新该 else 注释，去掉"TOOL_USE_* 暂不 emit"措辞，改注明 TOOL_USE_* 已经独立分支映射、未知类型静默降级。
  - **DEVIATION 注释**：在新分支处加简短中文注释，注明"compat 当前 _build_runner 不绑定 tools，此分支为前向兼容/Phase 57 复用预埋（见 56-RESEARCH D-1）；真实 RAG 检索 progress 由 Plan 02 在 view 层合成"。
  - ruff 行宽 100。
  </action>
  <acceptance_criteria>
    - 源断言：`adapter.py` import 含 `tool_event_to_progress`；新增 `elif evt.type in (TOOL_USE_START, TOOL_USE_RESULT)` 分支；全文件**不含** `delta` 写 `tool_calls`、**不含** `finish_reason = "tool_calls"` / `finish_reason="tool_calls"` 赋值。
    - 行为断言：注入 `[TOOL_USE_START{tool_name:search_rag}, MESSAGE_COMPLETE]` → SSE 含一个 `delta.reasoning_content=="正在检索 RAG"` 的 chunk，该 chunk `finish_reason is None` 且 `object=="chat.completion.chunk"`；注入未知工具名 → 无新增 chunk。
    - 测试命令：`cd server && uv run pytest tests/compat/test_adapter.py -q`（Task 3 集成测落地后全绿）。
  </acceptance_criteria>
  <verify>
    <automated>cd server && uv run ruff check compat/adapter.py && uv run pytest tests/compat/test_adapter.py -q</automated>
  </verify>
  <done>translate_stream 新增 TOOL_USE_* 映射分支（None 静默、非空 yield reasoning_content）；绝不写 tool_calls / finish_reason=tool_calls；未知类型仍降级；既有 5 个 adapter 测试不回退。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 纯函数映射单测 + 安全 sentinel 不泄漏测 + adapter 集成测 + 零回归 byte-eq（VALIDATION 6.1/6.2/6.3/6.4）</name>
  <read_first>
    - server/tests/compat/test_adapter.py:32-56（_make_runner / _collect 范式，直接复用）+ :64-155（既有 5 个 adapter 测试，须保持绿）
    - server/compat/progress.py（被测纯函数）、server/compat/adapter.py（被测集成）
    - 56-VALIDATION.md §Per-Task Verification Map + §6.1/6.2/6.3/6.4（四层断言要点）、56-RESEARCH §6 Nyquist 覆盖矩阵
  </read_first>
  <action>
  **新建 `server/tests/compat/test_progress.py`**（纯函数层 6.1 + 安全层 6.4 单测，无需 DB / async runner，纯函数直调）：
  - 各内部工具名 → 预期中文文本：search_rag/grep/get_file/仓库分析路由（参数化）。
  - 未知工具名 `"some_unknown_tool"` → None；缺 tool_name → None。
  - **TOOL_USE_RESULT 一律静默**：`tool_event_to_progress(AgentEvent(TOOL_USE_RESULT, {"tool_name":"search_rag", "success":True, "result":"..."})) is None`（即使工具名命中映射表也返 None）。
  - 非工具事件 `AgentEvent(THINKING/TEXT_DELTA/MESSAGE_COMPLETE)` → None（不误命中）。
  - **安全 sentinel（INV-5）**：构造 `AgentEvent(TOOL_USE_START, {"tool_name":"search_rag", "tool_input":"SENTINEL_SECRET_KEY_abc", ...})` 与 `TOOL_USE_RESULT` 含 `result="SENTINEL_PRIVATE_CODE_xyz"` / `error="SENTINEL_TRACEBACK"` → 断言 `tool_event_to_progress(evt)` 返回值中**不含**任一 sentinel 子串。
  - `make_reasoning_chunk` 单测：产出 bytes 解析后 `choices[0].delta.reasoning_content==text`、`finish_reason is None`、`object=="chat.completion.chunk"`；include_usage=True 时含 `usage` 键且为 None，False 时无 `usage` 键。

  **扩充 `server/tests/compat/test_adapter.py`**（集成层 6.2 + 零回归 6.3，复用 _make_runner/_collect）：
  - 集成 TOOL_USE_START：注入 `[TOOL_USE_START{tool_name:search_rag}, MESSAGE_COMPLETE]` → 断言含 reasoning_content=="正在检索 RAG" 的 chunk；该 chunk finish_reason is None、object==chat.completion.chunk。
  - 不误用 tool_calls（TRACE-02）：全量 chunk 中无任一 delta 含 `tool_calls` 键；无任一 choice 的 finish_reason=="tool_calls"。
  - content 不被污染：progress chunk 不混入 `delta.content`（断言含 reasoning_content 的 chunk 不同时含非空 content）。
  - include_usage=True 时 progress chunk 带 `usage=None`，末尾仍恰好一个 `choices==[]` 的 usage chunk（复用既有 test_include_usage_true 模式 + TOOL_USE_START 序列）。
  - **安全 sentinel 全流**：注入含敏感 tool_input/result + THINKING(CoT sentinel) 的完整序列 → 断言全量 SSE 字节流（`b"".join(...)`）中不出现任一 sentinel。
  - **零回归 byte-eq（6.3）**：对 `[THINKING, MESSAGE_COMPLETE]`、`[TEXT_DELTA, MESSAGE_COMPLETE]` 两序列，断言新代码 SSE 字节序列**不新增任何 chunk**（chunk 数 / 内容与无 TOOL_USE 事件时一致）；既有 5 个测试（reasoning_content / text_delta / include_usage true/false / error_closes_stream）保持全绿。
  </action>
  <acceptance_criteria>
    - 源断言：`server/tests/compat/test_progress.py` 存在且含纯函数映射用例 + sentinel 安全用例；`test_adapter.py` 新增 TOOL_USE_START 集成 + tool_calls 禁线 + sentinel 全流 + 零回归用例。
    - 行为断言：见各 §6 层断言；sentinel 子串全不出现于 helper 返回值与全量 SSE 字节流。
    - 测试命令：`cd server && uv run pytest tests/compat/ -q` 全绿（含既有 9 个测试不回退）。
  </acceptance_criteria>
  <verify>
    <automated>cd server && uv run pytest tests/compat/ -q</automated>
  </verify>
  <done>四层测试落地全绿：纯函数映射（各工具→文本 / 未知→None / 非工具事件不误命中）、安全 sentinel（helper + 全流不泄漏）、adapter 集成（含 reasoning_content progress、无 tool_calls、content 不污染、usage 一致）、零回归 byte-eq（无工具序列不新增 chunk + 既有测试全绿）。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AgentEvent.data → progress 文本 | event payload 含 tool_input/result/error（半敏感），progress 只允许取 tool_name 翻高层语义 |
| translate_stream → OpenAI SSE | 对外 SSE 绝不含 tool_calls / 私有 CoT；progress 经 reasoning_content 通道 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-01-01 | Information Disclosure | progress 文本泄漏 tool_input/result/error/CoT 原文 | mitigate | helper 仅读 tool_name；sentinel 测试断言全流不出现敏感子串（INV-5 / P-2） |
| T-56-01-02 | Tampering / 协议误用 | 误写 delta.tool_calls 或 finish_reason=tool_calls 致规范客户端卡死 | mitigate | 新分支只 yield reasoning_content；测试断言无 tool_calls 字段 / finish_reason≠tool_calls（TRACE-02 / P-3） |
| T-56-01-03 | 回归 | 无工具事件序列输出漂移 / 产空 progress chunk | mitigate | None 即 continue；零回归 byte-eq 测试 + 既有 5 测试全绿（P-5） |
</threat_model>

<verification>
- `cd server && uv run pytest tests/compat/ -q` 全绿（既有 9 + 新增）。
- `cd server && uv run ruff check compat/progress.py compat/adapter.py` 无 lint。
- grep `compat/progress.py` / `compat/adapter.py`：无 `tool_calls` 写入、无 `event_taxonomy` import。
</verification>

<success_criteria>
- TRACE-01（机制）：TOOL_USE_START 经 tool_event_to_progress → make_reasoning_chunk → delta.reasoning_content progress chunk（前向兼容；当前 compat 链路因无 tools 走降级，可见效果由 Plan 02 兑现 — DEVIATION D-1）。
- TRACE-02 / INV-5：绝不写 delta.tool_calls、finish_reason≠tool_calls；progress 文本仅工具名高层中文语义，sentinel 测试证不泄漏 tool_input/result/error/CoT。
- 零回归：无工具事件序列 SSE 逐字等价、零空 chunk；既有 adapter 测试全绿。
- helper 为纯函数、与 adapter/view 解耦（progress.py），为 Phase 57 复用预埋。
</success_criteria>

<artifacts_this_phase_produces>
本 Plan（Wave 1）创建的符号：
- 新模块 `server/compat/progress.py`
  - 常量 `_TOOL_PROGRESS_LABELS: dict[str, str]`（工具名→中文语义映射表）
  - 函数 `tool_event_to_progress(evt: AgentEvent) -> str | None`
  - 函数 `make_reasoning_chunk(common: dict, text: str, include_usage: bool) -> bytes`
- 修改 `server/compat/adapter.py`：`translate_stream` 新增 `TOOL_USE_START/TOOL_USE_RESULT` 映射分支（import `tool_event_to_progress` / `make_reasoning_chunk` / `TOOL_USE_START` / `TOOL_USE_RESULT`）
- 新测试文件 `server/tests/compat/test_progress.py`（纯函数映射单测 + sentinel 安全测）
- 扩充 `server/tests/compat/test_adapter.py`（TOOL_USE_* 集成测 + tool_calls 禁线 + sentinel 全流 + 零回归 byte-eq）
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/56-compat-progress-trace/56-01-SUMMARY.md` when done
</output>
