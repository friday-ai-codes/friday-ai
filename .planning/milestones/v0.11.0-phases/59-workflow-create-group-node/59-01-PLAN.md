---
phase: 59-workflow-create-group-node
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/services/feishu_im.py
  - server/delivery/services/work_item_service.py
  - server/tests/services/test_feishu_im.py
  - server/tests/delivery/test_work_item_writeback.py
  - server/tests/delivery/test_inv6_guard.py
autonomous: true
requirements: [GROUP-01]
must_haves:
  truths:
    - "D-1：建群即拉人单步——`FeishuIMClient.create_chat` 一次 `POST /open-apis/im/v1/chats` 带 body `user_id_list`(≤50)+`bot_id_list`(≤5)，不做建群后第二次 add_chat_members；query `user_id_type` 默认 open_id（+ `set_bot_manager` 仅 owner_id 非空且需设管理员时）"
    - "D-2：`create_chat` 手写 httpx 复用 `get_tenant_access_token` + `httpx.AsyncClient` + `data.get('code')==0` 判定 + structlog + tenacity rate-limit（99991400 → RateLimitError），对齐 `add_bot_to_chat`；不引 lark-oapi SDK；落点 feishu_im.py（群聊成员管理区块下）"
    - "create_chat body 仅放非空字段：`name` 恒放；`user_id_list`/`bot_id_list`/`owner_id`/`description` 非空才放；返回 `data.get('data', {})`（含 chat_id）；`code!=0` → raise FeishuIMError（含 code 诊断）"
    - "D-3：`FeishuIMService.create_chat(...)` 透传委托 `self.client.create_chat(...)`，与既有 `send_card`/`ensure_bot_in_chat` 委托同构"
    - "D-6：writeback 单一入口——`WorkItemService.awriteback_feishu_chat_id(feishu_project_key, work_item_type, work_item_id, chat_id) -> bool`，三元组定位 WorkItem，`save(update_fields=['feishu_chat_id','updated_at'])`，WorkItem 不存在返回 False 不抛；用 `@sync_to_async` 包同步块"
    - "P-5 INV-6 命门：`feishu_chat_id` 写入**绝不**进 `_MIRROR_FIELDS`、**绝不**在 `_refresh_mirror` 内写它（sync 会覆盖回空）；writeback 是独立写入路径"
    - "F-6/INV-6：writeback 经 `WorkItemService` 方法 → 天然落在 `test_inv6_guard.py` 的 `_ALLOWED_WRITER`（work_item_service.py）；新增 `feishu_chat_id` 正向 grep 守护：赋值/save 仅出现在 work_item_service.py"
    - "零回归：`add_bot_to_chat`/`ensure_bot_in_chat`/`get_chat_members`/`is_bot_in_chat`/`send_card`/`get_tenant_access_token` 签名与行为逐字不变；`_refresh_mirror`/`_MIRROR_FIELDS`/`upsert` 逐字不变"
  artifacts:
    - path: "server/services/feishu_im.py"
      provides: "FeishuIMClient.create_chat（POST /im/v1/chats 建群即拉人，返回 data 含 chat_id）"
      contains: "async def create_chat"
    - path: "server/services/feishu_im.py"
      provides: "FeishuIMService.create_chat（委托方法）"
      contains: "async def create_chat"
    - path: "server/delivery/services/work_item_service.py"
      provides: "WorkItemService.awriteback_feishu_chat_id（feishu_chat_id writeback 单一入口，INV-6）"
      contains: "async def awriteback_feishu_chat_id"
    - path: "server/tests/services/test_feishu_im.py"
      provides: "create_chat httpx 形状单测（扩展既有文件）"
      contains: "create_chat"
    - path: "server/tests/delivery/test_work_item_writeback.py"
      provides: "awriteback_feishu_chat_id DB 单测（新文件）"
      contains: "awriteback_feishu_chat_id"
    - path: "server/tests/delivery/test_inv6_guard.py"
      provides: "feishu_chat_id 写入正向 grep 守护（扩展既有文件）"
      contains: "feishu_chat_id"
  key_links:
    - from: "server/workflows/nodes/integrations/feishu_chat.py"
      to: "server/services/feishu_im.py"
      via: "Wave 2 CreateGroupChatNode 经 FeishuIMService.create(project) → create_chat 建群"
      pattern: "create_chat"
    - from: "server/workflows/nodes/integrations/feishu_chat.py"
      to: "server/delivery/services/work_item_service.py"
      via: "Wave 2 CreateGroupChatNode 可选 writeback 调 WorkItemService.awriteback_feishu_chat_id"
      pattern: "awriteback_feishu_chat_id"
---

<objective>
建立 Phase 59 的 **封装层 + writeback 入口（Wave 1）**：在 `FeishuIMClient`/`FeishuIMService` 手写 httpx 新增 **`create_chat`（建群即拉人单步 `POST /im/v1/chats`，body 带 `user_id_list`/`bot_id_list`/`owner_id`/`description`，query `user_id_type` 默认 open_id，复用 token/httpx/structlog/tenacity rate-limit 基建，`code!=0 → FeishuIMError`）**，并在 `WorkItemService` 新增 **`awriteback_feishu_chat_id`（writeback 单一入口，三元组定位 + `save(update_fields=["feishu_chat_id","updated_at"])`，不存在返回 False，INV-6）**。**绝不改** `add_bot_to_chat`/`ensure_bot_in_chat`/`_refresh_mirror`/`_MIRROR_FIELDS`。

Purpose: 兑现 GROUP-01 的协议地基与 writeback 收口——`create_chat` 端点/payload/`user_id_type`/`code!=0` 形状 100% 正确（供 Wave 2 节点建群消费），`awriteback_feishu_chat_id` 提供唯一合规写入路径（INV-6 grep 守护，绝不污染 mirror）。本 plan **不改节点**（不接 `CreateGroupChatNode`），纯封装 + writeback 入口 + 形状/DB/守护测试。
Output: `feishu_im.py` 增 `FeishuIMClient.create_chat` + `FeishuIMService.create_chat` 委托；`work_item_service.py` 增 `awriteback_feishu_chat_id`；扩展 `test_feishu_im.py`（create_chat httpx 形状单测）；新建 `test_work_item_writeback.py`（writeback DB 测）；扩展 `test_inv6_guard.py`（feishu_chat_id 正向 grep 守护）。

> **关键约束**：`feishu_chat_id` 写入是 writeback（独立来源），**绝不**混进 `_MIRROR_FIELDS` / `_refresh_mirror`（P-5）。writeback 失败语义（fail-soft）由 Wave 2 节点处理——本 plan 的 `awriteback_feishu_chat_id` 只「WorkItem 不存在返回 False（不抛）」，DB 异常由调用方 fail-soft 捕获（service 方法本身不吞 DB 异常）。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.codex/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/59-workflow-create-group-node/59-CONTEXT.md
@.planning/phases/59-workflow-create-group-node/59-RESEARCH.md
@.planning/phases/59-workflow-create-group-node/59-VALIDATION.md

# 主改文件：复用 get_tenant_access_token/add_bot_to_chat 的 httpx/tenacity/code!=0 范式，新增 create_chat + 委托；绝不改 add_bot_to_chat/ensure_bot_in_chat
@server/services/feishu_im.py
# 主改文件：新增 awriteback_feishu_chat_id；绝不改 _refresh_mirror/_MIRROR_FIELDS/upsert
@server/delivery/services/work_item_service.py
# 既有 IM 群方法测试范式（_make_client 预置 token + _mock_response + patch httpx.AsyncClient）——扩展，不破既有
@server/tests/services/test_feishu_im.py
# 既有 INV-6 grep 守护范式（_ALLOWED_WRITER + 正向有效性断言）——扩展 feishu_chat_id 守护
@server/tests/delivery/test_inv6_guard.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: FeishuIMClient.create_chat（建群即拉人单步）+ FeishuIMService.create_chat 委托 + httpx 形状单测</name>
  <read_first>
    - server/services/feishu_im.py:761-804（add_bot_to_chat：`token = await self.get_tenant_access_token()` + `httpx.AsyncClient` + `params` + `headers` Bearer + `json=` body + `code==99991400 → RateLimitError` + `code!=0 → FeishuIMError` + 返回 `data.get("data", {})`——create_chat 直接同构）、:113-185（FeishuIMClient 类 + OPEN_API_BASE + get_tenant_access_token）、:922-985（FeishuIMService 委托范式 send_card/ensure_bot_in_chat）
    - 59-RESEARCH.md §2 F-1（端点 `POST /open-apis/im/v1/chats` + query user_id_type/set_bot_manager + 限频 50/s,1000/min）、F-2（body 字段表：name/description/owner_id/user_id_list≤50/bot_id_list≤5/chat_mode）、F-3（bot 自动入群、owner 省略 bot 为群主、owner 为 bot 时响应缺 owner_id）、§3 D-1（单步建群+拉人）/D-2（手写 httpx + 方法签名）、§5.1（httpx 形状单测断言点）、§6 P-1/P-2/P-4/P-8/P-10
    - server/tests/services/test_feishu_im.py:18-39（_make_client 预置 _tenant_token + _mock_response）、:47-84（add_bot_to_chat 风格的 patch httpx.AsyncClient + code!=0 抛错 测试范式）
  </read_first>
  <behavior>
    - `FeishuIMClient.create_chat(self, name: str, *, user_id_list: list[str] | None = None, bot_id_list: list[str] | None = None, owner_id: str = "", description: str = "", user_id_type: Literal["open_id", "union_id", "user_id"] = "open_id", set_bot_manager: bool = False) -> dict[str, Any]`（async）：
      - `token = await self.get_tenant_access_token()`；
      - `POST {self.OPEN_API_BASE}/im/v1/chats`，`params={"user_id_type": user_id_type}`（`set_bot_manager` 仅当 `owner_id` 非空且 `set_bot_manager` True 时加 `"set_bot_manager": "true"`）；headers Bearer + Content-Type json；
      - body 仅放非空字段：`{"name": name}` 恒放；`user_id_list` 非空 → 加 `"user_id_list": user_id_list`；`bot_id_list` 非空 → 加；`owner_id` 非空 → 加 `"owner_id": owner_id`；`description` 非空 → 加；
      - `data = response.json()`；`code = data.get("code", -1)`；`code == 99991400 or "rate limit" in str(data).lower()` → `raise RateLimitError(...)`（对齐 add_bot_to_chat）；`code != 0` → `raise FeishuIMError(f"创建群聊失败: {data.get('msg', data)}", code=code)`；
      - 成功 → structlog `chat_created`（带 chat_id）→ 返回 `data.get("data", {})`（含 chat_id + 群元信息）。
    - `FeishuIMService.create_chat(self, name, *, user_id_list=None, bot_id_list=None, owner_id="", description="", user_id_type="open_id", set_bot_manager=False) -> dict[str, Any]`（async）：透传委托 `self.client.create_chat(...)`，与既有 `ensure_bot_in_chat` 委托同构。
  </behavior>
  <action>
  1. `server/services/feishu_im.py`：在 `FeishuIMClient` 内（建议紧邻 `ensure_bot_in_chat` 之后、`create_feishu_im_client_for_project` 之前的群聊管理区块）新增 `async def create_chat`，依 behavior 实现。统一用 `self.OPEN_API_BASE`（`https://open.feishu.cn/open-apis`）；body 用「先建 `payload = {"name": name}` 再按非空条件 `payload[...] = ...`」方式构造（确保空 `owner_id`/`description` 不出现在 body）。**绝不**改 `add_bot_to_chat`/`ensure_bot_in_chat`/`get_chat_members`/`is_bot_in_chat`/`get_tenant_access_token` 任何行。
  2. 同文件 `FeishuIMService` 类内（紧邻 `ensure_bot_in_chat` 委托后）新增 `async def create_chat` 委托方法，async 透传。
  3. `server/tests/services/test_feishu_im.py`：import 复用既有 `FeishuIMClient`/`FeishuIMError`/`FeishuIMService`（按需加 `RateLimitError`）；复用 `_make_client()`（预置 `_tenant_token`）+ `_mock_response()` + `patch("httpx.AsyncClient")`（无 DB），新增测试：
     - happy：mock `mock_http.post.return_value = _mock_response({"code":0,"data":{"chat_id":"oc_new","name":"需求群"}})` → 调 `create_chat("需求群", user_id_list=["ou_a","ou_b"])` → 断言 `result["chat_id"]=="oc_new"`；断言 `mock_http.post.await_args.args[0]` endswith `/im/v1/chats`；`kwargs["params"]=={"user_id_type":"open_id"}`；`kwargs["json"]["name"]=="需求群"`；`kwargs["json"]["user_id_list"]==["ou_a","ou_b"]`。
     - 空字段不放：`create_chat("群", user_id_list=["ou_a"])`（不传 owner_id/description）→ 断言 `"owner_id" not in kwargs["json"]` 且 `"description" not in kwargs["json"]`。
     - user_id_type 透传：`create_chat("群", user_id_list=["u1"], user_id_type="user_id")` → 断言 `kwargs["params"]["user_id_type"]=="user_id"`。
     - owner_id + set_bot_manager：`create_chat("群", user_id_list=["ou_a"], owner_id="ou_owner", set_bot_manager=True)` → 断言 body 含 `"owner_id":"ou_owner"`、`kwargs["params"]` 含 `"set_bot_manager"`。
     - code!=0：`_mock_response({"code":230002,"msg":"no permission"})` → `pytest.raises(FeishuIMError, match="创建群聊失败")`（断言 `.code==230002`）。
     - rate-limit：`_mock_response({"code":99991400,"msg":"rate limit"})` → `pytest.raises(RateLimitError)`。
  ruff 行宽 100，import 排序（I001），注释/docstring 中文。
  </action>
  <acceptance_criteria>
    - 源断言：`feishu_im.py` 含 `async def create_chat`（FeishuIMClient 与 FeishuIMService 各一）；grep 该文件 `create_chat` 出现 ≥2 次；既有 `async def add_bot_to_chat` / `async def ensure_bot_in_chat` / `async def get_chat_members` 行逐字不变（git diff 仅为新增方法）。
    - 行为断言：见 action 各测点（端点 `/im/v1/chats`、query `user_id_type`、body 仅非空字段、user_id_list 传入、code!=0 → FeishuIMError、99991400 → RateLimitError）。
    - 测试命令：`cd server && uv run pytest tests/services/test_feishu_im.py -q` 全绿（既有群方法测试 + 新增不回退）。
  </acceptance_criteria>
  <verify>
    <automated>cd server && uv run ruff check services/feishu_im.py && uv run pytest tests/services/test_feishu_im.py -q</automated>
  </verify>
  <done>FeishuIMClient.create_chat（建群即拉人单步 POST /im/v1/chats）+ FeishuIMService.create_chat 委托落地，端点/query user_id_type/body 非空字段/code!=0→FeishuIMError/99991400→RateLimitError 形状正确；既有 add_bot_to_chat/ensure_bot_in_chat 逐字不变；httpx 形状单测全绿。</done>
</task>

<task type="auto">
  <name>Task 2: WorkItemService.awriteback_feishu_chat_id（writeback 单一入口）+ DB 单测 + INV-6 grep 守护</name>
  <read_first>
    - server/delivery/services/work_item_service.py:50-63（_MIRROR_FIELDS 白名单——确认 feishu_chat_id 不在内、绝不加入）、:95-102（WorkItemIdentity 三元组）、:104-127（WorkItemService 类 + upsert 入口范式）、:207-218（_get_or_create_locked 的 `@sync_to_async` + `WorkItem.objects` 写表范式——awriteback 同款 sync_to_async 包同步块）、:229-296（_refresh_mirror：确认 writeback/feishu_chat_id 绝不写在此、line 246-248 注释）
    - server/delivery/models/work_item.py:37-40（三元组字段）、:69（feishu_chat_id CharField）、:75（updated_at auto_now）、:82（unique_together）
    - 59-RESEARCH.md §3 D-6（awriteback 签名 + 实现契约）、§2 F-5（feishu_chat_id 当前零写入入口）、F-6（INV-6 grep 守护范式）、§5.3（writeback service DB 测断言点：写入 + mirror 未动 + 不存在返回 False）、§5.4（INV-6 守护）、§6 P-5/P-6/P-10/P-11
    - server/tests/delivery/test_inv6_guard.py:33-48（_ALLOWED_WRITER + 正则锚定范式）、:101-133（test_inv6_no_bypass_work_item_write + test_inv6_writer_module_actually_writes 正向有效性范式——feishu_chat_id 守护镜像此结构）
  </read_first>
  <behavior>
    - `WorkItemService.awriteback_feishu_chat_id(self, feishu_project_key: str, work_item_type: str, work_item_id: int, chat_id: str) -> bool`（async）：把建群 chat_id 写回 `WorkItem.feishu_chat_id`（writeback 单一入口，INV-6）。
      - 用 `@sync_to_async` 包内部同步函数（或 service 方法用 `await sync_to_async(self._writeback_chat_id_sync)(...)` 范式，对齐既有 `_get_or_create_locked`）：`WorkItem.objects.filter(feishu_project_key=..., work_item_type=..., work_item_id=...).first()`；
      - 命中 None → 返回 `False`（不抛，调用方 fail-soft）；
      - 命中 → `wi.feishu_chat_id = chat_id`；`wi.save(update_fields=["feishu_chat_id", "updated_at"])`；返回 `True`；structlog `feishu_chat_id_writeback`（带 work_item_id/chat_id）。
      - **绝不**触碰 `_MIRROR_FIELDS` / `_refresh_mirror` / `field_provenance` mirror 语义；不读改其他 mirror 字段（只动 feishu_chat_id + updated_at）。
    - **不改** `upsert` / `_refresh_mirror` / `_MIRROR_FIELDS` / 任何既有方法。
  </behavior>
  <action>
  1. `server/delivery/services/work_item_service.py`：在 `WorkItemService` 类内（建议紧邻 `upsert` 之后、`_resolve_project` 之前，或类尾部 helper 区前）新增 `async def awriteback_feishu_chat_id` + 一个 `@sync_to_async` 同步实现（命名如 `_writeback_feishu_chat_id_sync`，与既有 `_get_or_create_locked`/`_refresh_mirror` 的 `@sync_to_async` 私有同步块风格一致）。同步块用 `WorkItem.objects.filter(三元组).first()`；命中则 `wi.feishu_chat_id = chat_id; wi.save(update_fields=["feishu_chat_id", "updated_at"])` 返回 True，否则 False。**绝不**把 `feishu_chat_id` 加进 `_MIRROR_FIELDS`、绝不在 `_refresh_mirror` 写它。
  2. 新建 `server/tests/delivery/test_work_item_writeback.py`（`@pytest.mark.django_db`）：
     - import `WorkItem`（`delivery.models`）+ `WorkItemService`；用 `sync_to_async`/`async def` + `@pytest.mark.asyncio` 或 `django_db` + `async_to_sync` 任一既有 delivery 测试范式（对齐 server/tests/delivery 既有写法）。
     - happy：`WorkItem.objects.create(feishu_project_key="P", work_item_type="story", work_item_id=123, title="原标题")` → `await WorkItemService().awriteback_feishu_chat_id("P","story",123,"oc_new")` 返回 True → reload（`WorkItem.objects.get(pk=...)`）断言 `feishu_chat_id=="oc_new"`；**断言 mirror 未动**：`title=="原标题"`（writeback 不污染 mirror，P-5）。
     - 不存在：`awriteback_feishu_chat_id("P","story",999,"oc_x")` → 返回 False（不抛）、库中无该行。
     - mirror 隔离断言：`assert "feishu_chat_id" not in WorkItemService 的 _MIRROR_FIELDS`（import `_MIRROR_FIELDS` 断言 feishu_chat_id 不在内）。
  3. `server/tests/delivery/test_inv6_guard.py`：扩展（既有 INV-6/INV-3 测试逐字保留），新增 2 个测试镜像既有范式：
     - `test_inv6_no_bypass_feishu_chat_id_write`：grep 扫描 server/ 源码（复用既有 `_iter_py_files` + `_is_scanned_for_inv6` 剪枝），断言「`.feishu_chat_id = ` 赋值 / `feishu_chat_id` 入 `update_fields` 的 save」只出现在 `delivery/services/work_item_service.py`（防旁路写 chat_id；可用正则锚 `\.feishu_chat_id\s*=` 排除 models/ 与 service 自身）。
     - `test_inv6_feishu_chat_id_writer_actually_writes`：断言 `work_item_service.py` 含 `awriteback_feishu_chat_id` 且含 `feishu_chat_id` 赋值 + `update_fields=["feishu_chat_id"` 片段（守护有效性，否则断言形同虚设，镜像 `test_inv6_writer_module_actually_writes`）。
  ruff 行宽 100，import I001，注释中文。
  </action>
  <acceptance_criteria>
    - 源断言：`work_item_service.py` 含 `async def awriteback_feishu_chat_id`；grep `_MIRROR_FIELDS` 仍**不含** `feishu_chat_id`（白名单逐字不变）；`_refresh_mirror` 方法体逐字不变（git diff 无改动）。
    - 行为断言：DB 测——写入 feishu_chat_id 成功且 title（mirror）未动；WorkItem 不存在返回 False 不抛；`_MIRROR_FIELDS` 不含 feishu_chat_id。
    - 守护断言：`test_inv6_no_bypass_feishu_chat_id_write` 绿（无旁路写 chat_id）；`test_inv6_feishu_chat_id_writer_actually_writes` 绿（writer 确实写）；既有 `test_inv6_no_bypass_work_item_write` 仍绿。
    - 测试命令：`cd server && uv run pytest tests/delivery/test_work_item_writeback.py tests/delivery/test_inv6_guard.py -q` 全绿。
  </acceptance_criteria>
  <verify>
    <automated>cd server && uv run ruff check delivery/services/work_item_service.py && uv run pytest tests/delivery/test_work_item_writeback.py tests/delivery/test_inv6_guard.py -q</automated>
  </verify>
  <done>WorkItemService.awriteback_feishu_chat_id（三元组定位 + save update_fields=["feishu_chat_id","updated_at"]，不存在返回 False）落地，绝不污染 mirror；INV-6 grep 守护扩 feishu_chat_id（写入仅在 work_item_service.py + 正向有效性）；DB 测 + 守护测 + 既有 INV-6 全绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 节点/服务 → 飞书 IM OpenAPI | `create_chat` 经 tenant_access_token 鉴权调用 open.feishu.cn；payload（name/成员/owner）由本层构造，成员 ID 与 user_id_type 一致性由调用方保证 |
| 飞书 API 响应 → 调用方 | `code!=0`（权限/超限/参数）统一抛 `FeishuIMError`/`RateLimitError`，供 Wave 2 节点 error handle 判定（绝不静默成功） |
| 工作流 → WorkItem 持久层 | `feishu_chat_id` 写入单一收口 `awriteback_feishu_chat_id`（INV-6），grep 守护防旁路；绝不进 mirror sync 路径 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-01 | Tampering / 协议误用 | create_chat payload 形状错误（成员未传 / user_id_type 不一致 / 空字段污染 body）致建群/拉人失败 | mitigate | body 仅放非空字段；query user_id_type 默认 open_id；httpx 形状单测断言端点/params/body（F-1/F-2/P-1） |
| T-59-01b | Information Disclosure | 错误响应/日志泄漏 token 或 app_secret | mitigate | structlog 仅记 chat_id/code，不记 token/secret；复用既有 get_tenant_access_token（凭证不入日志，对齐 common.logging 凭证保护） |
| T-59-01c | 可用性 | code!=0 被吞成成功致 Wave 2 误判建群成功 | mitigate | create_chat code!=0 必抛 FeishuIMError（含 code），单测覆盖 230002/99991400 |
| T-59-02 | Tampering / INV-6 旁路 | feishu_chat_id 被旁路写入（绕过单一入口）或污染 mirror 致 sync 覆盖回空 | mitigate | awriteback 单一入口 + `update_fields` 只含 feishu_chat_id/updated_at；feishu_chat_id 绝不进 _MIRROR_FIELDS；INV-6 grep 守护防旁路（P-5/P-6） |
</threat_model>

<verification>
- `cd server && uv run pytest tests/services/test_feishu_im.py tests/delivery/test_work_item_writeback.py tests/delivery/test_inv6_guard.py -q` 全绿。
- `cd server && uv run ruff check services/feishu_im.py delivery/services/work_item_service.py` 无 lint。
- grep `services/feishu_im.py`：含 `async def create_chat`（client + service）+ `/im/v1/chats`；既有 `async def add_bot_to_chat`/`async def ensure_bot_in_chat` git diff 仅新增方法、既有方法体不变。
- grep `delivery/services/work_item_service.py`：含 `async def awriteback_feishu_chat_id`；`_MIRROR_FIELDS` 不含 `feishu_chat_id`；`_refresh_mirror` 主体未改。
</verification>

<success_criteria>
- GROUP-01 地基：`create_chat` 单步建群即拉人（D-1），端点 `/im/v1/chats` + query user_id_type(open_id) + body user_id_list/bot_id_list/owner_id/description 仅非空、`code!=0 → FeishuIMError`、99991400 → RateLimitError 形状 100% 正确。
- D-2/D-3：手写 httpx 复用 get_tenant_access_token + httpx + tenacity，不引 lark-oapi SDK；FeishuIMService.create_chat 委托同构。
- D-6/INV-6：feishu_chat_id 写入单一收口 awriteback_feishu_chat_id（三元组 + save update_fields），不存在返回 False；grep 守护防旁路、绝不污染 mirror（P-5）。
- 零回归：add_bot_to_chat/ensure_bot_in_chat/get_chat_members/_refresh_mirror/_MIRROR_FIELDS/upsert 符号逐字不变；既有 test_feishu_im 群方法测试 + INV-6 守护全绿。
- 为 Wave 2 预埋：FeishuIMService.create_chat + WorkItemService.awriteback_feishu_chat_id 齐备，Wave 2 仅在 feishu_chat.py 新增 CreateGroupChatNode 接线消费。
</success_criteria>

<artifacts_this_phase_produces>
本 Plan（Wave 1）创建的符号：
- 修改 `server/services/feishu_im.py`：
  - `FeishuIMClient.create_chat(self, name: str, *, user_id_list=None, bot_id_list=None, owner_id="", description="", user_id_type="open_id", set_bot_manager=False) -> dict[str, Any]`
  - `FeishuIMService.create_chat(...)`（委托方法，同签名透传）
- 修改 `server/delivery/services/work_item_service.py`：
  - `WorkItemService.awriteback_feishu_chat_id(self, feishu_project_key: str, work_item_type: str, work_item_id: int, chat_id: str) -> bool`
  - `WorkItemService._writeback_feishu_chat_id_sync`（`@sync_to_async` 同步实现，命名 plan 自决）
- 新测试文件 `server/tests/delivery/test_work_item_writeback.py`
- 扩展 `server/tests/services/test_feishu_im.py`（新增 create_chat httpx 形状单测）
- 扩展 `server/tests/delivery/test_inv6_guard.py`（新增 `test_inv6_no_bypass_feishu_chat_id_write` + `test_inv6_feishu_chat_id_writer_actually_writes`）
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/59-workflow-create-group-node/59-01-SUMMARY.md` when done
</output>
