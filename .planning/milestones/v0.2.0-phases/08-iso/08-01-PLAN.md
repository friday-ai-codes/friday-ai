---
phase: 08-iso
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/tests/test_conversation_isolation.py
  - server/tests/conftest.py
autonomous: true
requirements: [ISO-01, ISO-02, ISO-03, ISO-04]
must_haves:
  truths:
    - "存在一个对 RESEARCH 全部 25 个会话访问路径的 cross-user-denied（404）测试"
    - "测试覆盖 ISO-01 created_by 落库 + 历史回填、ISO-02 owner 过滤、ISO-03 管理员无 bypass、ISO-04 404 不泄漏存在性"
    - "测试在 Wave 1 全部 RED（功能未实现），Wave 2-4 落地后转 GREEN"
    - "开放/匿名模式回归测试存在（鉴权关闭时仍可访问，行为不变）"
  artifacts:
    - path: "server/tests/test_conversation_isolation.py"
      provides: "隔离 RED 测试全集（25 路径 cross-user-denied + owner-allowed + admin-no-bypass + 回填 + open-mode）"
      min_lines: 200
    - path: "server/tests/conftest.py"
      provides: "second_user / 二号用户 JWT / owner 可注入的会话工厂 fixtures"
      contains: "created_by"
  key_links:
    - from: "server/tests/test_conversation_isolation.py"
      to: "/api/chat/conversations/"
      via: "AsyncClient + Bearer JWT 跨用户取他人会话 id"
      pattern: "status_code == 404"
---

<objective>
为 Phase 8（对话/会话用户隔离）建立 Wave 0 RED 验证脚手架：新增 `server/tests/test_conversation_isolation.py`，对 RESEARCH「Conversation Access Point Inventory」列出的全部 25 个访问路径，逐一写显式的「用户 B 直取用户 A 会话 id → 404」测试；并补 ISO-01 created_by 落库/历史回填、ISO-03 管理员无特权 bypass、open-mode 回归。同时更新 `conftest.py` fixtures，使既有套件在 created_by 收紧后不大面积 break。

Purpose: 隔离阶段是 all-or-nothing（ISO-04），任一路径遗漏即安全漏洞。先用一套 1:1 映射 25 路径的测试把"完整性"钉死，后续 Wave 2-4 的实现以"让这些测试转 GREEN"为唯一验收。
Output: 一个 RED 测试文件 + conftest fixtures 增量。本 plan 完成时测试全部 RED（因功能尚未实现），这是预期状态。
</objective>

<execution_context>
@.cursor/gsd-core/workflows/execute-plan.md
@.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-iso/08-RESEARCH.md
@.planning/phases/08-iso/08-VALIDATION.md
@.planning/phases/08-iso/08-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: conftest fixtures — owner 可注入会话工厂 + 二号用户/JWT</name>
  <files>server/tests/conftest.py</files>
  <read_first>
    - server/tests/conftest.py L131-148（`user` / `admin_user` fixtures）、L185-196（`project`）、L811-852（`frozen_conversation_factory`：注意它用 `**overrides` 透传字段、`apps.get_model("chat","Conversation")`）
    - server/tests/test_conversation_integration.py L26-41（`user_and_token` / `auth_headers`：`User.objects.acreate_user` + `RefreshToken.for_user` → `{"authorization": f"Bearer {access_token}"}`）
    - 08-RESEARCH.md §Validation Architecture「Wave 0 Gaps」「Regression Surface」
  </read_first>
  <action>
    在 conftest.py 新增隔离测试所需 fixtures（沿用现有风格：函数类型注解、中文 docstring、`apps.get_model` 防 app-registry race）：
    (1) 扩展 `frozen_conversation_factory`：`_factory` 已是 `**overrides` 透传，确认 `created_by` 能经 overrides 写入（Conversation 加字段后即生效）；若担心字段未迁移，沿用其既有「字段存在性探测」写法对 `created_by` 同样处理。
    (2) 新增 `second_user` fixture（同步，`User.objects.create_user(username="iso_second_user", password=...)`，非 superuser），用于跨用户越权样本。
    (3) 新增 `second_user_and_token`（async，`User.objects.acreate_user` + `sync_to_async(RefreshToken.for_user)`）+ `second_auth_headers`（`{"authorization": f"Bearer <access>"}`），与 test_conversation_integration 的 `user_and_token`/`auth_headers` 同形态，供二号用户发认证请求。
    (4) 新增 `superuser_and_token` / `superuser_auth_headers`（is_superuser=True 用户 + JWT），用于 ISO-03 管理员无 bypass 测试。
    不改动既有 fixtures 的语义；仅新增 + 对 frozen_conversation_factory 做向后兼容的 created_by 透传确认。
  </action>
  <acceptance_criteria>
    - `conftest.py` 含 `second_user`、`second_user_and_token`、`second_auth_headers`、`superuser_and_token`、`superuser_auth_headers` fixtures
    - `frozen_conversation_factory(created_by=<user>)` 可设置会话 owner（字段落地后）
    - `cd server && uv run pytest tests/test_conversation_integration.py -q` 仍可收集运行（fixture 增量不破坏既有套件）
  </acceptance_criteria>
  <verify>
    <automated>cd server && uv run pytest tests/test_conversation_integration.py -q</automated>
  </verify>
  <done>新 fixtures 可用，既有集成测试仍能运行（不被 fixture 增量破坏）。</done>
</task>

<task type="auto">
  <name>Task 2: RED — ISO-01/02/03 + open-mode（created_by、owner 过滤、管理员无 bypass、回填、开放模式）</name>
  <files>server/tests/test_conversation_isolation.py</files>
  <read_first>
    - 08-RESEARCH.md §Phase Requirements、§Architecture Patterns（Pattern 1-5）、§Runtime State Inventory「Backfill specifics」、§Assumptions A2（earliest superuser 排序字段）
    - server/accounts/models.py L53（`accounts.User.created_at` 存在；**用 `order_by("created_at","id")`，不是 date_joined**）
    - server/chat/conversation_service.py L696-723（`create_conversation`）、L1309-1316（`list_conversations`）
    - server/chat/views.py L321-375（list/create）、L393-519（detail）
  </read_first>
  <action>
    新建 `server/tests/test_conversation_isolation.py`，`@pytest.mark.django_db(transaction=True)` async 类，使用 `django.test.AsyncClient` + Bearer JWT。写以下用例（此刻应全部 RED）：
    - `test_create_sets_owner`：用户经认证 POST `/api/chat/conversations/` 后，DB 中该会话 `created_by_id == 请求用户.id`（per ISO-01；A1：仅 JWT/web 用户）。
    - `test_list_only_owner`：用户 A、B 各建会话，A 的 list 仅含自己的（ISO-02 #1）。
    - `test_admin_no_bypass`：superuser 作为认证用户 list/detail 普通用户会话 → 看不到 / 404（ISO-03，断言 owner gate 无 `is_superuser` 分支）。
    - `test_open_mode_unaffected`：未认证（无 Authorization，开放模式）路径维持既有行为（可访问，不强加 owner 过滤）—— 回归保护 compat/chat-key。
    - 回填用例 `test_backfill_assigns_earliest_superuser` / `test_backfill_no_superuser_leaves_null` / `test_backfill_reversible`：直接 import 0019 迁移模块的 `forwards`/`backwards`，用 `apps.get_model` 形态在测试内构造场景（最早 superuser 按 `created_at,id`；无 superuser 不阻塞留 null；backwards 置 None）。
    断言式 `created_by`、`status_code`、`==404` 必须显式；不要 try/except 吞断言。
  </action>
  <acceptance_criteria>
    - 文件存在且含上述命名用例
    - `cd server && uv run pytest tests/test_conversation_isolation.py -q` 能收集到测试（import 无语法错误）
    - 因功能未实现，相关用例 FAIL（RED）——这是本 plan 的预期结果
  </acceptance_criteria>
  <verify>
    <automated>cd server && uv run pytest tests/test_conversation_isolation.py -q --co</automated>
  </verify>
  <done>ISO-01/02/03 + open-mode + 回填用例写就，可被收集，当前 RED。</done>
</task>

<task type="auto">
  <name>Task 3: RED — ISO-04 全 25 路径 cross-user-denied（404）+ SSE 流前 404</name>
  <files>server/tests/test_conversation_isolation.py</files>
  <read_first>
    - 08-RESEARCH.md §Conversation Access Point Inventory（A 直接端点 #1-12、B SSE #10′、C coding-session/plan #13-23、D trace/clarification #24-25、E 排除项）
    - server/chat/urls.py（全部 route name 与路径：conversation-detail / conversation-stream / conversation-runtime / conversation-preflight / conversation-messages-delete / conversation-message-fork / conversation-interrupt / conversation-export-to-feishu / coding-session-* / coding-plan-* / routing-trace-manual-override / chat-clarification-answer）
    - server/chat/views.py（各端点取对象方式：直接 `aget` vs `session.conversation` vs `plan.conversation` vs `trace.conversation`）
    - 08-RESEARCH.md §Pitfall 5 / Pattern 5（SSE 必须在 `ChatStreamView.post` 流打开前 404，而非流内 error event）
  </read_first>
  <action>
    在同文件追加 ISO-04 全覆盖用例：
    - `test_cross_user_denied`：参数化（pytest.mark.parametrize）覆盖 RESEARCH 表中需对象级访问的全部路径 #3–#25。每参数：用户 A 建会话 / coding-session / coding-plan / routing-trace / clarification（用 `created_by=user_A` 或经 A 认证创建），用户 B 携带 B 的 JWT 访问该 id，断言 `status_code == 404`（不可为 403/200）。coding-session/plan 类需先在 A 的会话下建 `CodingSession`/`CodingPlan` 关联行。
    - `test_404_indistinguishable`：B 访问「不存在的随机 uuid」与「A 的真实会话 id」返回体/状态码一致（不泄漏存在性）。
    - `test_stream_cross_user_404`：B POST `/api/chat/conversations/{A 会话 id}/stream/` 在 SSE 流打开前返回 HTTP 404（断言 `resp.status_code == 404`，而非 200 的 text/event-stream 里夹 error 事件）。
    - `test_list_scoping_coding`：#13 coding-sessions?conversation_id= 与 #20 coding-plans?conversation_id= 用 B 传 A 的 conversation_id → 返回 `[]`（不列他人）。
    在测试文件顶部用注释把每个 parametrize id 标注到 RESEARCH 端点编号（#3..#25），形成 1:1 可审计映射。
  </action>
  <acceptance_criteria>
    - 参数化集合显式覆盖 #3 至 #25（每个 RESEARCH 编号至少 1 个断言），加 #10′ SSE 与 #13/#20 list-scoping
    - 每条断言为 `== 404`（对象级）或 `== []`（list 级），无 403
    - `cd server && uv run pytest tests/test_conversation_isolation.py -q --co` 可收集全部参数
    - 当前 RED（功能未实现）
  </acceptance_criteria>
  <verify>
    <automated>cd server && uv run pytest tests/test_conversation_isolation.py -q --co</automated>
  </verify>
  <done>25 路径 + SSE-流前-404 + list-scoping 的 cross-user-denied 用例写就，可被收集，当前 RED。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 已认证用户 B → 会话 API | B 持自己 JWT 直取 A 的会话 id（IDOR 攻击面，测试要复现并断言被拒） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Information Disclosure | 测试覆盖完整性 | mitigate | 参数化 1:1 映射 RESEARCH 25 路径，任一遗漏 = 测试缺口；注释标注端点编号供审计 |
| T-08-02 | Information Disclosure | 403-vs-404 区分 | mitigate | 断言越权一律 404 且与「不存在」不可区分（test_404_indistinguishable） |
| T-08-SC | Tampering | 依赖安装 | accept | 本 plan 零新增依赖（pytest 栈已在），无 npm/pip 安装 |
</threat_model>

<verification>
- `cd server && uv run pytest tests/test_conversation_isolation.py -q --co` 能收集全部用例（含参数化）
- 既有套件 `cd server && uv run pytest tests/test_conversation_integration.py -q` 不被 fixture 增量破坏
- 当前阶段隔离用例 RED 属预期（功能在 Wave 2-4 落地）
</verification>

<success_criteria>
- `test_conversation_isolation.py` 存在，覆盖 ISO-01..04 + open-mode + 回填，含全 25 路径参数化 cross-user-denied（404）
- conftest 新增 second_user / 二号 JWT / superuser JWT / owner 可注入会话工厂
- 测试可被收集；越权断言全为 404/`[]`，无 403
</success_criteria>

<output>
Create `.planning/phases/08-iso/08-01-SUMMARY.md` when done
</output>
