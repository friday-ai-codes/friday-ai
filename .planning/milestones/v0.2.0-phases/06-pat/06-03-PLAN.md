---
phase: 06-pat
plan: 03
type: execute
wave: 1
depends_on: ["06-01"]
files_modified:
  - web/src/types/accessToken.ts
  - web/src/components/accessTokens/AccessTokenForm.vue
  - web/src/components/accessTokens/AccessTokenListTable.vue
autonomous: true
requirements: [PAT-01, PAT-03, PAT-04, PAT-05]
must_haves:
  truths:
    - "Create form accepts an optional note (≤500) and forwards it in the create payload"
    - "Selecting never-expire shows a non-blocking amber warning that does not block submit"
    - "Token list shows a note column and a prefix…suffix fingerprint, degrading to prefix-only when suffix is empty"
    - "List exposes no token renewal/extension control (revoke only)"
  artifacts:
    - path: "web/src/types/accessToken.ts"
      provides: "note + token_suffix on DTO; optional note on create payload"
      contains: "token_suffix"
    - path: "web/src/components/accessTokens/AccessTokenForm.vue"
      provides: "note input + zod max(500) + name max(200) + never-expire amber warning"
      contains: "never"
    - path: "web/src/components/accessTokens/AccessTokenListTable.vue"
      provides: "note column + prefix…suffix fingerprint"
      contains: "token_suffix"
  key_links:
    - from: "web/src/components/accessTokens/AccessTokenForm.vue"
      to: "AccessTokenCreatePayload.note"
      via: "onSubmit sets payload.note = values.note?.trim() || undefined"
      pattern: "note"
    - from: "web/src/components/accessTokens/AccessTokenListTable.vue"
      to: "AccessTokenDto.token_suffix"
      via: "t.token_suffix ? `${t.token_prefix}…${t.token_suffix}` : t.token_prefix"
      pattern: "token_suffix"
---

<objective>
Extend the existing `accessTokens` Vue components to surface the new `note` and `token_suffix` fields and the never-expire safety warning, turning the Wave-0 frontend specs (06-01) GREEN. Purely additive UI/DTO work — no store/api/persistence changes (the store already strips plaintext and never persists it).

Purpose: Deliver the UI half of PAT-01 (optional note input), PAT-03 (note column + prefix…suffix fingerprint), PAT-05 (non-blocking never-expire warning), and preserve PAT-04 (revoke only, no renewal entry).
Output: Extended DTO types, form (note input + zod alignment + amber warning), and list table (note column + fingerprint).
</objective>

<execution_context>
@.cursor/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-pat/06-CONTEXT.md
@.planning/phases/06-pat/06-RESEARCH.md
@web/src/types/accessToken.ts
@web/src/components/accessTokens/AccessTokenForm.vue
@web/src/components/accessTokens/AccessTokenListTable.vue
@web/src/components/accessTokens/AccessTokenRevealDialog.vue
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend DTO + create payload with note / token_suffix</name>
  <files>web/src/types/accessToken.ts</files>
  <read_first>
    - web/src/types/accessToken.ts (current `AccessTokenDto`, `AccessTokenCreatePayload`, `AccessTokenCreateResult`)
    - server/access_tokens/serializers.py (the backend output contract these DTOs must mirror — added `note` + `token_suffix`)
  </read_first>
  <behavior>
    - `AccessTokenDto` carries `note: string` and `token_suffix: string` (both may be empty for historical tokens).
    - `AccessTokenCreatePayload` carries optional `note?: string`.
  </behavior>
  <action>
    Add to `AccessTokenDto`: `note: string` (备注，可能为空串) and `token_suffix: string` (明文后 4 字符，空串=历史 token). Add `note?: string` to `AccessTokenCreatePayload`. Keep the security doc-comment that list/detail DTOs never contain plaintext. `AccessTokenCreateResult extends AccessTokenDto` so it inherits the new fields automatically — no change needed there.
  </action>
  <verify>
    <automated>cd web && pnpm vue-tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `accessToken.ts` `AccessTokenDto` includes `note: string` and `token_suffix: string`.
    - `AccessTokenCreatePayload` includes `note?: string`.
    - Type-check passes (no TS errors introduced).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Form note input + zod alignment + never-expire amber warning</name>
  <files>web/src/components/accessTokens/AccessTokenForm.vue</files>
  <read_first>
    - web/src/components/accessTokens/AccessTokenForm.vue (current zod schema `name: min(1)`, `expiryStrategy` ref, `onSubmit` payload assembly, FormField/Input usage)
    - web/src/components/accessTokens/AccessTokenRevealDialog.vue (amber class pattern: `border-amber-200 bg-amber-50 ... dark:border-amber-800 dark:bg-amber-950/30 dark:text-amber-400` + `icon-[lucide--alert-triangle]`)
    - web/src/components/accessTokens/__tests__/AccessTokenSettings.spec.ts (the 06-01 specs this task must satisfy: never-warning + note-in-payload)
  </read_first>
  <behavior>
    - Entering a note (≤500) forwards `note` (trimmed; empty → undefined) in the submit payload.
    - `name` >200 chars is rejected client-side (aligns with backend max_length=200).
    - Selecting `never` renders a non-blocking amber warning; submit still fires.
  </behavior>
  <action>
    Extend the zod schema: `name: z.string().min(1, '请填写 Token 名称').max(200, '名称不超过 200 字符')` and add `note: z.string().max(500, '备注不超过 500 字符').optional()`. Add a `note` `FormField` + `Input` (multiline optional — a plain Input is acceptable; label 备注，可选) below the name field. In `onSubmit`, set `payload.note = values.note?.trim() || undefined` (do not send empty strings). Add an inline amber warning block rendered with `v-if="expiryStrategy === 'never'"` directly under the expiry Select, reusing the RevealDialog amber class范式 and `icon-[lucide--alert-triangle]`; the warning is presentation-only — it must NOT participate in zod validation and must NOT block `onSubmit`. Warning copy is Claude's discretion (per CONTEXT) — convey that a never-expiring token stays valid indefinitely if leaked.
  </action>
  <verify>
    <automated>cd web && pnpm vitest run src/components/accessTokens</automated>
  </verify>
  <acceptance_criteria>
    - zod schema has `name.max(200)` and optional `note.max(500)`.
    - A note input renders in the form; `onSubmit` adds `payload.note` only when non-empty after trim.
    - An amber warning (`bg-amber-50`/`text-amber-700` + alert-triangle icon) renders only when `expiryStrategy === 'never'` and does not block submit.
    - `pnpm vitest run src/components/accessTokens` exits 0 for the never-warning + note-payload specs.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: List table note column + prefix…suffix fingerprint</name>
  <files>web/src/components/accessTokens/AccessTokenListTable.vue</files>
  <read_first>
    - web/src/components/accessTokens/AccessTokenListTable.vue (current 指纹 cell renders `t.token_prefix`; column headers; `colspan="7"` empty row; revoke-only action column — confirm no renewal control exists)
    - web/src/components/accessTokens/__tests__/AccessTokenListTable.spec.ts (the 06-01 spec this task must satisfy)
  </read_first>
  <behavior>
    - Fingerprint renders `prefix…suffix` when `token_suffix` is non-empty, else just `prefix` (no dangling `…`).
    - A note column renders the note text (escaped via Vue interpolation — never `v-html`).
    - No renewal/extension control is added (revoke remains the only mutating action).
  </behavior>
  <action>
    Change the 指纹 cell to render `{{ t.token_suffix ? `${t.token_prefix}…${t.token_suffix}` : t.token_prefix }}` (U+2026 `…` separator). Add a 备注 column: a new `<th>` header (uppercase style matching siblings) and a `<td>` rendering `{{ t.note }}` via text interpolation (auto-escaped — do NOT use `v-html`; note is user-controlled, V5/XSS). Apply truncation/width per existing column style (Claude's discretion). Bump the empty-state row `colspan` to match the new total column count. Do NOT add any 续期/延期/renew control — revoke stays the only action (PAT-04).
  </action>
  <verify>
    <automated>cd web && pnpm vitest run src/components/accessTokens</automated>
  </verify>
  <acceptance_criteria>
    - Fingerprint cell renders `friday_pat_ab…WXYZ` for a suffixed token and `friday_pat_ab` (no trailing `…`) when suffix is empty.
    - A 备注 column header + cell render `t.note` via interpolation (no `v-html`).
    - Empty-state `colspan` updated to the new column count.
    - No renew/extend control present in the table.
    - `pnpm vitest run src/components/accessTokens` exits 0 (06-01 ListTable spec GREEN).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → browser render | `note` is user-controlled text rendered in the list |
| form → store/api | Untrusted note string sent on create |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07 | Tampering | stored XSS via note in list | mitigate | Render `note` with Vue text interpolation (auto HTML-escape); never `v-html` |
| T-06-08 | Information Disclosure | plaintext leaking into store/localStorage via new fields | accept (unchanged) | Store still strips `token` and persists nothing; new fields are metadata only; existing `AccessTokenSettings.spec.ts` guards clearing of plaintext |
| T-06-09 | Input Validation | oversized name/note client-side | mitigate | zod `name.max(200)` + `note.max(500)` mirror backend authoritative limits |
| T-06-SC | Tampering | pnpm installs | accept | No package installs (uses existing vee-validate/zod/reka-ui); RESEARCH Package Legitimacy Audit SKIPPED — zero new deps |
</threat_model>

<verification>
- `cd web && pnpm vitest run src/components/accessTokens` exits 0 (all 06-01 frontend specs GREEN).
- `cd web && pnpm vue-tsc --noEmit -p tsconfig.json` passes (DTO additions type-check).
- Manual (per 06-VALIDATION Manual-Only): browser create → never-expire shows amber warning, submit succeeds; list shows note + prefix…suffix.
</verification>

<success_criteria>
- DTO + create payload carry note/token_suffix; form has optional note + aligned zod + non-blocking never warning; list shows note column + prefix…suffix fingerprint (prefix-only fallback).
- No renewal UI; no plaintext persisted; note rendered escaped.
</success_criteria>

<output>
Create `.planning/phases/06-pat/06-03-SUMMARY.md` when done.
</output>
