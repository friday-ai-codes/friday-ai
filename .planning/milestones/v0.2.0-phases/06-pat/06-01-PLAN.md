---
phase: 06-pat
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - server/tests/conftest.py
  - server/tests/test_access_tokens.py
  - web/src/components/accessTokens/__tests__/AccessTokenListTable.spec.ts
  - web/src/components/accessTokens/__tests__/AccessTokenSettings.spec.ts
autonomous: true
requirements: [PAT-01, PAT-02, PAT-03, PAT-05]
must_haves:
  truths:
    - "Backend test suite asserts token_suffix == plaintext[-4:] and note persists"
    - "Backend test suite asserts AccessTokenSerializer output exposes note + token_suffix read-only and never plaintext/token_hash"
    - "Frontend spec asserts fingerprint renders prefix…suffix, and prefix-only when suffix empty"
    - "Frontend spec asserts never-expire selection shows a non-blocking warning and note flows into createToken payload"
  artifacts:
    - path: "server/tests/test_access_tokens.py"
      provides: "token_suffix / note / serializer assertions"
      contains: "token_suffix"
    - path: "web/src/components/accessTokens/__tests__/AccessTokenListTable.spec.ts"
      provides: "fingerprint + note column spec"
      contains: "token_suffix"
  key_links:
    - from: "server/tests/conftest.py"
      to: "access_tokens.models.AccessToken"
      via: "make_access_token sets token_suffix=plaintext[-4:]"
      pattern: "token_suffix"
---

<objective>
Write the validation contract FIRST (Nyquist Wave 0) for the PAT enhancement. These tests encode PAT-01/02/03/05 as executable assertions and are expected to be RED until the backend (06-02) and frontend (06-03) implementation plans land, at which point they turn GREEN.

This plan touches ONLY test files and the shared test fixture — no production code. It establishes the failing assertions so the implementation plans have an objective target.

Purpose: Lock the contract (suffix fingerprint, note persistence, read-only serializer output, never-expire non-blocking warning) before code so implementation cannot silently drift.
Output: Extended `test_access_tokens.py` + `conftest.py` fixture; new `AccessTokenListTable.spec.ts`; extended `AccessTokenSettings.spec.ts`.
</objective>

<execution_context>
@.cursor/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-pat/06-CONTEXT.md
@.planning/phases/06-pat/06-RESEARCH.md
@.planning/phases/06-pat/06-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend backend fixture + token_suffix/note assertions</name>
  <files>server/tests/conftest.py, server/tests/test_access_tokens.py</files>
  <read_first>
    - server/tests/conftest.py (the `make_access_token` factory fixture, ~lines 1229-1267 — currently sets `token_prefix=plaintext[:12]`, does NOT set `token_suffix`/`note`)
    - server/tests/test_access_tokens.py (existing `test_create_returns_plaintext_once`, `test_list_never_returns_plaintext` — mirror their style and the `plaintext not in str(...)` assertion semantics)
    - server/access_tokens/models.py (current field set, to know what attributes the fixture/asserts reference)
  </read_first>
  <action>
    In `conftest.py` `make_access_token`: extend the inner `_make` signature to accept `note: str = ""`, and set BOTH `token_suffix=plaintext[-4:]` and `note=note` on the `AccessToken.objects.create(...)` call (mirroring the existing `token_prefix=plaintext[:12]`). Keep `pytest.importorskip("access_tokens.models")` at the top so the fixture still skips gracefully if the model is absent.
    In `test_access_tokens.py`: (a) extend `test_create_returns_plaintext_once` to also assert `token.token_suffix == plaintext[-4:]` and `len(token.token_suffix) <= 4` (PAT-03 fingerprint; PAT-02 suffix is non-sensitive, ≤4 chars to bound entropy loss). (b) Add `test_create_persists_note` using `make_access_token(name="noted", note="ci pipeline")` asserting `token.note == "ci pipeline"` (PAT-01). (c) Confirm `test_list_never_returns_plaintext` still iterates `obj._meta.concrete_fields` — do NOT weaken its `plaintext not in str(...)` assertion (the 4-char suffix is a substring, never the full plaintext, so it stays GREEN).
    Do NOT add `default`-less columns or touch production code here.
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/test_access_tokens.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `conftest.py` `_make` accepts a `note` kwarg and sets `token_suffix=plaintext[-4:]` and `note=note`.
    - `test_access_tokens.py` contains an assertion `token.token_suffix == plaintext[-4:]` and a `test_create_persists_note` function asserting `token.note`.
    - `test_list_never_returns_plaintext` assertion text (`plaintext not in str(...)`) is unchanged.
    - Running the command exits non-zero ONLY because `token_suffix`/`note` attributes do not yet exist on the model (expected RED); collection succeeds with no import/collection error.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add serializer read-only assertion (note + token_suffix, no plaintext)</name>
  <files>server/tests/test_access_tokens.py</files>
  <read_first>
    - server/access_tokens/serializers.py (current `AccessTokenSerializer.Meta.fields` + `read_only_fields = fields`)
    - server/tests/test_access_tokens.py (target file from Task 1)
  </read_first>
  <action>
    Add `test_serializer_exposes_note_and_suffix_readonly`: build a token via `make_access_token(name="ser", note="hello")`, serialize with `from access_tokens.serializers import AccessTokenSerializer; data = AccessTokenSerializer(token).data`, then assert `"note" in data`, `"token_suffix" in data`, `data["token_suffix"] == token.token_suffix`, and the negative security contract `"token_hash" not in data` and `"token" not in data` (PAT-02 — plaintext/hash never serialized). Also assert `AccessTokenSerializer.Meta.read_only_fields == AccessTokenSerializer.Meta.fields` so the read-only safety contract is locked.
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/test_access_tokens.py -k serializer -q</automated>
  </verify>
  <acceptance_criteria>
    - A `test_serializer_exposes_note_and_suffix_readonly` test exists asserting `note` and `token_suffix` present in serialized output.
    - Test asserts `token_hash` and `token` are absent from serialized output.
    - Test asserts `read_only_fields == fields`.
    - Command is RED until serializer exposes the new fields (expected), with no collection error.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Frontend specs — fingerprint/note column + never-warning + note payload</name>
  <files>web/src/components/accessTokens/__tests__/AccessTokenListTable.spec.ts, web/src/components/accessTokens/__tests__/AccessTokenSettings.spec.ts</files>
  <read_first>
    - web/src/components/accessTokens/__tests__/AccessTokenSettings.spec.ts (existing mount/stub patterns, `makeToken` helper, store mocks)
    - web/src/components/accessTokens/AccessTokenListTable.vue (current fingerprint cell renders `t.token_prefix`)
    - web/src/components/accessTokens/AccessTokenForm.vue (current submit emits payload; expiryStrategy ref)
    - web/src/types/accessToken.ts (DTO shape the `makeToken` helper must satisfy)
  </read_first>
  <action>
    Create `AccessTokenListTable.spec.ts`: mount `AccessTokenListTable` with tokens including `token_suffix`. Assert (a) a token with `token_prefix: 'friday_pat_ab'` and `token_suffix: 'WXYZ'` renders text matching `friday_pat_ab…WXYZ` (the `…` U+2026 separator); (b) a token with `token_suffix: ''` (historical) renders only the prefix with NO trailing `…`; (c) a `note` value renders as escaped text in the row (use Vue text interpolation expectation — assert the note string appears in `wrapper.text()`). Add `token_suffix` and `note` to the local `makeToken` helper.
    Extend `AccessTokenSettings.spec.ts` (or co-locate a Form-focused test): (a) assert selecting `never` expiry surfaces a non-blocking amber warning element (e.g. text contains 永不过期 risk copy and submit is still possible — emitting submit still calls `createToken`); (b) assert that when the form emits submit with `{ name, note }`, the `createToken` mock receives a payload whose `note` equals the provided value. Update the local `makeToken` helper to include `token_suffix` and `note` so it matches the extended DTO.
  </action>
  <verify>
    <automated>cd web && pnpm vitest run src/components/accessTokens</automated>
  </verify>
  <acceptance_criteria>
    - `AccessTokenListTable.spec.ts` exists and asserts `friday_pat_ab…WXYZ` for a suffixed token and prefix-only (no dangling `…`) for an empty-suffix token.
    - A spec asserts the never-expire selection shows a warning AND does not block submit (createToken still invoked).
    - A spec asserts `createToken` receives `note` in its payload.
    - `makeToken` helpers include `token_suffix` and `note`.
    - Specs are RED until 06-03 ships the markup/payload (expected), with no module-resolution error.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → model/serializer | Tests assert the production security contract; weak tests would let real leaks pass |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure | `test_list_never_returns_plaintext` weakened by new fields | mitigate | Keep `plaintext not in str(...)` over all concrete fields unchanged; suffix is a 4-char substring, never the full token |
| T-06-02 | Tampering | serializer gains a writable field | mitigate | `test_serializer_exposes_note_and_suffix_readonly` asserts `read_only_fields == fields` |
| T-06-SC | Tampering | npm/pip installs | accept | No package installs in this plan (test edits only); RESEARCH Package Legitimacy Audit SKIPPED — no new deps |
</threat_model>

<verification>
- `cd server && uv run pytest tests/test_access_tokens.py -q` collects with no errors (assertions RED until 06-02).
- `cd web && pnpm vitest run src/components/accessTokens` collects with no errors (specs RED until 06-03).
- No production source file modified by this plan.
</verification>

<success_criteria>
- Backend: token_suffix==plaintext[-4:], note persistence, and serializer read-only assertions exist and reference the locked field names.
- Frontend: fingerprint prefix…suffix (and prefix-only fallback), never-warning non-blocking, and note-in-payload specs exist.
- All new tests fail only due to not-yet-implemented production behavior, never due to collection/import errors.
</success_criteria>

<output>
Create `.planning/phases/06-pat/06-01-SUMMARY.md` when done.
</output>
