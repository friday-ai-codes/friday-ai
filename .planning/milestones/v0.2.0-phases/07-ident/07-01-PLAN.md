---
phase: 07-ident
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - server/tests/test_access_tokens.py
  - server/tests/test_pat_identity.py
  - server/tests/mcp_tools/test_mcp_auth_errors.py
autonomous: true
requirements: [IDENT-01, IDENT-02, IDENT-03, IDENT-04, IDENT-05]
must_haves:
  truths:
    - "test_valid_token_passes asserts request user == token.created_by (not None)"
    - "test_pat_identity asserts non-friday_pat_ Bearer makes AccessTokenAuthentication return None (passthrough to JWT)"
    - "test_pat_identity asserts valid friday_pat_ token authenticates the /me endpoint as the owner via the full DRF auth chain"
    - "test_pat_identity asserts a valid JWT Bearer still authenticates with PAT class ordered first"
    - "test_mcp_auth_errors asserts anonymous MCP request returns 401 with error_code authentication_failed"
  artifacts:
    - path: "server/tests/test_pat_identity.py"
      provides: "PAT identity + prefix-gate + PAT/JWT coexistence assertions"
      contains: "friday_pat_"
      min_lines: 60
    - path: "server/tests/test_access_tokens.py"
      provides: "updated test_valid_token_passes owner assertion"
      contains: "created_by"
    - path: "server/tests/mcp_tools/test_mcp_auth_errors.py"
      provides: "MCP fail-closed error_code assertion"
      contains: "authentication_failed"
  key_links:
    - from: "server/tests/test_pat_identity.py"
      to: "access_tokens.authentication.AccessTokenAuthentication"
      via: "authenticate() returns (token.created_by, token)"
      pattern: "created_by"
    - from: "server/tests/test_pat_identity.py"
      to: "/api/auth/me"
      via: "APIClient Bearer friday_pat_ token hits IsAuthenticated endpoint"
      pattern: "Bearer"
---

<objective>
Write the validation contract FIRST (Nyquist Wave 0) for the "令牌即用户身份" auth-base refactor. These tests encode IDENT-01..05 as executable assertions and are expected to be RED until the implementation plans (07-02 auth+settings, 07-03 MCP) land, at which point they turn GREEN.

This plan touches ONLY test files — no production code. It locks four behaviors before code so implementation cannot drift:
1. Successful PAT auth returns the owner as `request.user` (IDENT-01).
2. The `friday_pat_` prefix gate lets non-PAT Bearer tokens fall through to JWT (IDENT-02).
3. PAT and JWT coexist through the global auth chain with PAT class ordered first (IDENT-02).
4. MCP entry is fail-closed: anonymous request → 401 with the converged `authentication_failed` error code (IDENT-03).

It also documents the BLOCKING 401 regression guard: the existing `test_auth.py` / `test_auth_e2e.py` unauthenticated-401 assertions MUST stay GREEN after 07-02 adds `authenticate_header` (Pitfall 2). No new test needed there — they are protective.

Purpose: Encode the auth contract (owner identity, prefix gate, fail-closed, audit unbroken) so 07-02/07-03 have an objective target.
Output: Updated `test_access_tokens.py`; new `test_pat_identity.py`; updated `test_mcp_auth_errors.py`.
</objective>

<execution_context>
@.cursor/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ident/07-CONTEXT.md
@.planning/phases/07-ident/07-RESEARCH.md
@.planning/phases/07-ident/07-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip test_valid_token_passes to assert owner identity</name>
  <files>server/tests/test_access_tokens.py</files>
  <read_first>
    - server/tests/test_access_tokens.py (existing `test_valid_token_passes`, ~lines 93-113 — currently asserts `user is None` and comments "返回 (None, token)")
    - server/access_tokens/authentication.py (current success branch `return (None, token)` at line 74 — the value this test now contracts against)
    - server/tests/conftest.py (`make_access_token` factory ~lines 1229-1274, returns `(token, plaintext)`; `access_user` aliases the `user` fixture so `token.created_by == user`)
  </read_first>
  <action>
    In `test_valid_token_passes`: change `assert user is None` to `assert user == token.created_by` (IDENT-01 — successful auth returns the owner User as request.user). Update the inline comment "仿 RunnerTokenAuthentication：返回 (None, token)" to reflect the new contract "返回 (token.created_by, token)：request.user=owner，request.auth=token". Keep the existing `assert auth_token.token_hash == token.token_hash` and `assert not hasattr(auth_token, "scope")` assertions unchanged (request.auth stays the AccessToken instance; no scope split — milestone decision). Do NOT touch `test_revoked_expired_denied_and_logged` (IDENT-05 stays GREEN unchanged — only the success branch changes in 07-02).
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/test_access_tokens.py::test_valid_token_passes -q</automated>
  </verify>
  <acceptance_criteria>
    - `test_valid_token_passes` contains `assert user == token.created_by` and no longer contains `assert user is None`.
    - `auth_token.token_hash == token.token_hash` assertion is unchanged.
    - The test is RED (fails on the owner assertion) until 07-02 ships, with no collection/import error.
    - `test_revoked_expired_denied_and_logged` is unmodified.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: New test_pat_identity.py — prefix gate + owner identity + PAT/JWT coexistence</name>
  <files>server/tests/test_pat_identity.py</files>
  <read_first>
    - server/access_tokens/authentication.py (full file — `authenticate` signature; the prefix-gate insertion point is after `plaintext = auth_header[7:]`; success branch returns the tuple this test asserts)
    - server/access_tokens/models.py (`PAT_PREFIX = "friday_pat_"`, `generate_pat()`, `created_by` FK, `is_valid`)
    - server/tests/test_auth.py (lines 85-100 — `TestMe`, the `urls` fixture exposing `urls.me`, `authenticated_client`/`api_client` fixtures; `/me` is IsAuthenticated-protected and ideal for chain-level assertions)
    - server/tests/conftest.py (`make_access_token`, `access_user`, `user`, `api_client`, `urls` fixtures)
    - RESEARCH §Pitfall 1 + §Architecture Patterns: SimpleJWT raises InvalidToken (not return None) for a malformed Bearer, so PAT class MUST be first and MUST `return None` (never raise) for non-`friday_pat_` Bearer; minting a test JWT uses `from rest_framework_simplejwt.tokens import AccessToken as JWTAccessToken; str(JWTAccessToken.for_user(user))`.
  </read_first>
  <action>
    Create `server/tests/test_pat_identity.py` (mirror the `pytest.importorskip` + `APIRequestFactory` style of `test_access_tokens.py`; mark module `pytestmark = pytest.mark.django_db`). Add these tests:
    1. `test_valid_pat_authenticates_as_owner` (IDENT-01, unit): drive `AccessTokenAuthentication().authenticate(request)` with `Bearer <plaintext friday_pat_>` via `APIRequestFactory`; assert the returned `(user, auth)` has `user == token.created_by` and `auth.token_hash == token.token_hash`.
    2. `test_non_pat_bearer_falls_through` (IDENT-02, unit): build a request with `Authorization: Bearer not_a_friday_pat_value` (no `friday_pat_` prefix); assert `AccessTokenAuthentication().authenticate(request) is None` (passthrough/let-row, NEVER raises) — this is the gate that prevents swallowing JWT.
    3. `test_unknown_pat_is_rejected_not_passed_through` (IDENT-02/IDENT-05, unit): build a request with `Bearer friday_pat_<random-unknown>`; assert `authenticate` raises `AuthenticationFailed` (a known-prefix-but-absent token is denied, NOT silently passed to JWT).
    4. `test_pat_authenticates_protected_endpoint_as_owner` (IDENT-01/IDENT-02, integration): use `APIClient().credentials(HTTP_AUTHORIZATION=f"Bearer {plaintext}")` and GET `urls.me`; assert `response.status_code == 200` and the returned username equals `token.created_by.username` (proves owner RBAC flows through `IsAuthenticated`).
    5. `test_jwt_bearer_still_authenticates_with_pat_class_first` (IDENT-02, integration): mint `token_str = str(JWTAccessToken.for_user(user))`, call `APIClient().credentials(HTTP_AUTHORIZATION=f"Bearer {token_str}")` GET `urls.me`; assert `200` (PAT class returns None on the non-`friday_pat_` JWT and CookieJWT authenticates it — PAT and JWT互不吞).
    6. `test_revoked_pat_rejected_through_chain` (IDENT-05, integration): create a revoked token via `make_access_token(revoked=True)`, call `urls.me` with its Bearer; assert `401`.
    Do NOT mock the auth classes — exercise the real chain so the settings ordering in 07-02 is what is validated.
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/test_pat_identity.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_pat_identity.py` exists, collects with no import error, and defines all six tests named above.
    - `test_non_pat_bearer_falls_through` asserts `authenticate(...) is None` (passthrough), not an exception.
    - `test_unknown_pat_is_rejected_not_passed_through` asserts `AuthenticationFailed` is raised.
    - The integration tests target `urls.me` through `APIClient` (real DRF auth chain), not a mocked authenticator.
    - Suite is RED on the owner/coexistence assertions until 07-02 ships; the JWT-only and 401 assertions may already pass — no collection error in any state.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Converge MCP unauth error code + assert fail-closed 401</name>
  <files>server/tests/mcp_tools/test_mcp_auth_errors.py</files>
  <read_first>
    - server/tests/mcp_tools/test_mcp_auth_errors.py (existing `test_missing_token_returns_error_code` asserts `error_code == "authentication_required"` and status 401; the two valid-PAT tests use the `mcp_client` fixture and must stay GREEN once 07-02/07-03 land)
    - server/mcp_tools/views.py (lines 141-164 — `McpToolView.handle_exception` already returns `error_response("authentication_failed", ..., 401)` for `NotAuthenticated`; `_begin` currently returns `"authentication_required"` while base is `AllowAny`)
    - RESEARCH §Pitfall 3 + §Open Questions 1 (resolved): once base is `IsAuthenticated`, the permission layer rejects before `_begin`, so the response goes through `handle_exception` → `authentication_failed`. Decision: converge to `authentication_failed`.
    - server/tests/mcp_tools/conftest.py (`mcp_client` fixture builds a valid-PAT APIClient)
  </read_first>
  <action>
    In `test_missing_token_returns_error_code`: change the expected `response.json()["error_code"]` from `"authentication_required"` to `"authentication_failed"` (per resolved Open Question 1 — IsAuthenticated rejects anonymous at the permission layer, routing through `McpToolView.handle_exception`). Keep `assert response.status_code == 401` (fail-closed; not 403 — MCP handle_exception hard-codes 401, immune to the global 403 downgrade). Optionally rename/clarify the docstring to "anonymous MCP request is fail-closed 401 (authentication_failed)". Do NOT modify `test_repository_not_found_error_code` or `test_repository_not_indexed_error_code` — they use a valid PAT (`mcp_client`) and must stay GREEN once owner identity lands (their request.user becomes the owner, satisfying IsAuthenticated).
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/mcp_tools/test_mcp_auth_errors.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `test_missing_token_returns_error_code` asserts `error_code == "authentication_failed"` and `status_code == 401`.
    - The two valid-PAT MCP tests are unmodified.
    - The missing-token test is RED until 07-03 tightens the base to `IsAuthenticated` (anonymous currently returns `authentication_required` via `_begin`), with no collection error.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → auth pipeline | Tests assert the production access-control contract; weak tests would let an auth regression (401→403 downgrade, JWT swallow, fail-open MCP) pass silently |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Elevation of Privilege | MCP fail-open if test allows anonymous | mitigate | `test_missing_token_returns_error_code` asserts 401 (fail-closed), not a 2xx |
| T-07-02 | Denial of Service | JWT swallowed by PAT class | mitigate | `test_non_pat_bearer_falls_through` + `test_jwt_bearer_still_authenticates_with_pat_class_first` lock passthrough |
| T-07-03 | Spoofing | revoked token still usable | mitigate | `test_revoked_pat_rejected_through_chain` asserts 401 end-to-end |
| T-07-SC | Tampering | npm/pip installs | accept | No package installs in this plan (test edits only); RESEARCH Package Legitimacy Audit: N/A — zero new deps |
</threat_model>

<verification>
- `cd server && uv run pytest tests/test_pat_identity.py tests/test_access_tokens.py tests/mcp_tools/test_mcp_auth_errors.py -q` collects with no import/collection error (assertions RED until 07-02/07-03).
- No production source file modified by this plan.
- `tests/test_auth.py` / `tests/test_auth_e2e.py` unauthenticated-401 assertions are left untouched as the BLOCKING regression guard for Pitfall 2.
</verification>

<success_criteria>
- IDENT-01 owner identity, IDENT-02 prefix-gate passthrough + PAT/JWT coexistence, IDENT-03 MCP fail-closed (authentication_failed/401), and IDENT-05 revoked denial are all encoded as executable assertions referencing the locked identifiers (`friday_pat_`, `created_by`, `authentication_failed`).
- All new/changed tests fail only due to not-yet-implemented production behavior, never due to collection/import errors.
</success_criteria>

<output>
Create `.planning/phases/07-ident/07-01-SUMMARY.md` when done.
</output>
