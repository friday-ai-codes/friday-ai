---
phase: 10-mcpb
plan: 04
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - web/src/types/toolBinding.ts
  - web/src/api/toolBindings.ts
  - web/src/stores/toolBindings.ts
  - web/src/components/toolBindings/ToolBindingSettings.vue
  - web/src/components/toolBindings/ToolBindingTable.vue
  - web/src/components/toolBindings/ToolBindDialog.vue
  - web/src/pages/profile.vue
autonomous: true
requirements: [MCPB-01, MCPB-03]
must_haves:
  truths:
    - "profile.vue 在 Access Tokens 卡片下方新增「工具令牌绑定」卡片，渲染 ToolBindingSettings"
    - "列出可绑定 mcp/skill 工具 + 各自当前绑定令牌（仅名称/前后缀，绝不明文）"
    - "绑定操作：从「我的有效令牌」下拉选一把（仅 is_valid===true）绑给某工具 → 调 toolBindingsApi.upsert {remote_tool, access_token}"
    - "解绑操作 → 调 toolBindingsApi.unbind(id)"
    - "明文绝不进入任何 store state / 组件渲染（store 只缓存绑定与可绑定工具元数据）"
  artifacts:
    - path: "web/src/api/toolBindings.ts"
      provides: "list/bindable/upsert/unbind API 客户端"
      contains: "tools/execute" 
    - path: "web/src/stores/toolBindings.ts"
      provides: "useToolBindingStore（bindings/bindableTools + fetch/upsert/unbind）"
      contains: "useToolBindingStore"
    - path: "web/src/components/toolBindings/ToolBindingSettings.vue"
      provides: "绑定管理区容器（mirror AccessTokenSettings）"
      contains: "ToolBindingSettings"
    - path: "web/src/types/toolBinding.ts"
      provides: "ToolBindingDto / BindableToolDto / ToolBindingUpsertPayload"
      contains: "BindableToolDto"
  key_links:
    - from: "web/src/components/toolBindings/ToolBindDialog.vue"
      to: "useAccessTokenStore"
      via: "下拉仅列 is_valid 令牌（name/前后缀）"
      pattern: "is_valid"
    - from: "web/src/api/toolBindings.ts"
      to: "/api/tools/bindings/"
      via: "client.ts get/post/del 自动 /api 前缀"
      pattern: "/tools/bindings/"
    - from: "web/src/pages/profile.vue"
      to: "web/src/components/toolBindings/ToolBindingSettings.vue"
      via: "新增卡片 import + 渲染"
      pattern: "ToolBindingSettings"
---

<objective>
实现「工具令牌绑定」管理 UI（mirror access tokens 设置区/组件范式）：新增 types/api/store + 三个组件（Settings 容器 / Table 列表 / BindDialog 绑定对话框），并在 profile.vue 的 Access Tokens 卡片下方挂一张新卡片。用户能看到可绑定的 mcp/skill 工具及其当前绑定令牌，从「我的有效令牌」下拉选一把绑定/换绑、或解绑。使 10-01 前端 RED spec 转 GREEN。

Purpose: 满足 MCPB-01（把自己的令牌绑给 skill/mcp）、MCPB-03（查看并解除绑定）的 Browser 层职责（per RESEARCH §Architectural Responsibility Map）。消费 10-03 的 /api/tools/ 端点。
Output: 4 个新文件 + 1 组件目录（3 vue）+ profile.vue 增量。安全：明文绝不进 store/渲染（沿用 accessTokens store 契约）。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-mcpb/10-RESEARCH.md
@web/src/api/accessTokens.ts
@web/src/stores/accessTokens.ts
@web/src/types/accessToken.ts
@web/src/components/accessTokens/AccessTokenSettings.vue
@web/src/pages/profile.vue
@web/src/components/toolBindings/__tests__/ToolBindingSettings.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: types + api + store（toolBinding.ts / toolBindings.ts / stores/toolBindings.ts）</name>
  <files>web/src/types/toolBinding.ts, web/src/api/toolBindings.ts, web/src/stores/toolBindings.ts</files>
  <read_first>
    - web/src/types/accessToken.ts:11-29（AccessTokenDto 字段范式：DTO 与后端 serializer 一一对应、明文契约注释）
    - web/src/api/accessTokens.ts:11-55（client get/post 用法、extractList 兼容 DRF 分页/裸 list、相对路径 + 末尾 /）
    - web/src/stores/accessTokens.ts:20-113（defineStore setup 风格：state ref + fetch/action + lastError + 明文剥离安全注释）
    - .planning/phases/10-mcpb/10-RESEARCH.md §Code Examples「前端 API（toolBindings.ts）」+ §Recommended Project Structure
  </read_first>
  <action>
    - `web/src/types/toolBinding.ts`：
      - `BoundTokenDto`：{ id: string, name: string, token_prefix: string, token_suffix: string, is_valid: boolean }（与后端 BoundTokenSerializer 一一对应，绝不含明文/hash）。
      - `ToolBindingDto`：{ id: number, remote_tool: number, remote_tool_name: string, remote_tool_source: 'mcp'|'skill', access_token: BoundTokenDto, created_at: string, updated_at: string }。
      - `BindableToolDto`：{ id: number, name: string, description: string, source: 'mcp'|'skill' }。
      - `ToolBindingUpsertPayload`：{ remote_tool: number, access_token: string }（access_token 为 AccessToken UUID）。
    - `web/src/api/toolBindings.ts`（复用 `./client` get/post/del，相对路径 `/tools/...`，末尾带 `/`，复用 extractList 兼容分页）：
      - `list()` → GET `/tools/bindings/` → ToolBindingDto[]；`bindable()` → GET `/tools/bindable/` → BindableToolDto[]；`upsert(p)` → POST `/tools/bindings/` → ToolBindingDto；`unbind(id)` → DELETE `/tools/bindings/${id}/`。
    - `web/src/stores/toolBindings.ts`：`useToolBindingStore`（setup 风格，mirror accessTokens store）：state `bindings: ref<ToolBindingDto[]>([])`、`bindableTools: ref<BindableToolDto[]>([])`、`loading`、`lastError`；actions `fetchBindings()`、`fetchBindable()`、`upsertBinding(payload)`（成功后刷新或就地更新 bindings）、`unbindBinding(id)`（成功后从 bindings 移除）。错误统一 re-throw 供 UI useErrorHandler 承接。**绝不**缓存任何令牌明文（绑定只含元数据）。
  </action>
  <verify>
    <automated>cd web && pnpm vitest run src/components/toolBindings 2>&1 | tail -15 || true</automated>
  </verify>
  <acceptance_criteria>
    - 三文件存在；api 路径用 `/tools/bindings/`、`/tools/bindable/`、`/tools/bindings/${id}/`（末尾 /）。
    - useToolBindingStore 暴露 bindings/bindableTools/fetchBindings/fetchBindable/upsertBinding/unbindBinding（与 10-01 spec mock 形状一致）。
    - 类型/ store 中无任何明文字段（仅 prefix/suffix/is_valid 元数据）。
    - `pnpm -C web typecheck` 对这三文件无新增类型错误。
  </acceptance_criteria>
  <done>types/api/store 落地，形状与 spec mock + 后端 DTO 对齐。</done>
</task>

<task type="auto">
  <name>Task 2: 组件 ToolBindingTable + ToolBindDialog + ToolBindingSettings</name>
  <files>web/src/components/toolBindings/ToolBindingSettings.vue, web/src/components/toolBindings/ToolBindingTable.vue, web/src/components/toolBindings/ToolBindDialog.vue</files>
  <read_first>
    - web/src/components/accessTokens/AccessTokenSettings.vue（整文件：onMounted 拉取、Dialog/AlertDialog 编排、useErrorHandler/useToast、二次确认范式——ToolBindingSettings 的直接蓝本）
    - web/src/components/accessTokens/AccessTokenListTable.vue（列表渲染 + emit 事件范式，供 ToolBindingTable 仿写）
    - web/src/components/toolBindings/__tests__/ToolBindingSettings.spec.ts（锁名断言：lists_bindable_mcp_skill_tools / bind_dropdown_only_lists_valid_tokens / bind_calls_store_upsert_with_tool_and_token / unbind_calls_store_unbind / never_renders_token_plaintext——组件结构必须满足这些选择器/行为）
    - .planning/phases/10-mcpb/10-RESEARCH.md §Pattern 1 + §Common Pitfalls 5（下拉只列 is_valid 令牌）
  </read_first>
  <action>
    - `ToolBindingTable.vue`：props `{ tools: BindableToolDto[], bindings: ToolBindingDto[] }`；按工具渲染行（工具名 + source 标签 + 当前绑定令牌的 name/前后缀指纹或「未绑定」）；每行 emit `bind`(tool) 与（已绑定时）`unbind`(binding)。绝不渲染明文，仅 token_prefix…token_suffix。
    - `ToolBindDialog.vue`：props `{ open, tool }`，从 `useAccessTokenStore` 读 tokens 并**仅列 is_valid===true** 的令牌（Select 选项展示 name + 前后缀，per Pitfall 5）；确认时 emit `confirm`({ remote_tool: tool.id, access_token: <选中 token id> })。
    - `ToolBindingSettings.vue`（mirror AccessTokenSettings）：onMounted 并行 `fetchBindable()` + `fetchBindings()`（同时确保 access token store 有数据供下拉，调 useAccessTokenStore().fetchTokens()）；渲染 ToolBindingTable；`bind` 事件打开 ToolBindDialog；dialog `confirm` → `store.upsertBinding(payload)` + toast；`unbind` 事件走 AlertDialog 二次确认 → `store.unbindBinding(id)`。错误经 useErrorHandler。

    所有交互 emit/handler 命名与 10-01 spec 的 findComponent/选择器一致（spec 是契约）。组件文本绝不出现完整 `friday_pat_` 明文。
  </action>
  <verify>
    <automated>cd web && pnpm vitest run src/components/toolBindings 2>&1 | tail -20 || true</automated>
  </verify>
  <acceptance_criteria>
    - `lists_bindable_mcp_skill_tools` GREEN：表格渲染传入的 mcp/skill 工具行。
    - `bind_dropdown_only_lists_valid_tokens` GREEN：下拉仅出现 is_valid===true 令牌。
    - `bind_calls_store_upsert_with_tool_and_token` GREEN：确认调 upsertBinding({remote_tool, access_token})。
    - `unbind_calls_store_unbind` GREEN：解绑确认调 unbindBinding(id)。
    - `never_renders_token_plaintext` GREEN：渲染文本无完整明文。
  </acceptance_criteria>
  <done>三组件实现，10-01 前端 spec 全 GREEN。</done>
</task>

<task type="auto">
  <name>Task 3: profile.vue 挂载「工具令牌绑定」卡片</name>
  <files>web/src/pages/profile.vue</files>
  <read_first>
    - web/src/pages/profile.vue:1-16, 238-262（script import AccessTokenSettings + Access Tokens 卡片 DOM 结构；新卡片紧随其后，复用 card/header 样式）
    - web/src/components/toolBindings/ToolBindingSettings.vue（Task 2 产物，import 目标）
  </read_first>
  <action>
    在 web/src/pages/profile.vue：
    - script 区 import `ToolBindingSettings from '~/components/toolBindings/ToolBindingSettings.vue'`。
    - template 在「Access Tokens」卡片（约 238-258 行）**之后**新增一张同款 card：header 用 lucide 图标（如 `icon-[lucide--link]`）+ 标题「工具令牌绑定」+ 副标题「把访问令牌绑定给 skill / mcp 工具，调用时以令牌所有者身份执行」，body 渲染 `<ToolBindingSettings />`。
    - 不改动既有 Access Tokens 卡片与其它区块。
  </action>
  <verify>
    <automated>cd web && pnpm vitest run src/components/toolBindings 2>&1 | tail -8 && pnpm -C web typecheck 2>&1 | tail -8 || true</automated>
  </verify>
  <acceptance_criteria>
    - profile.vue 渲染 ToolBindingSettings（import + 模板卡片就位），位于 Access Tokens 卡片下方。
    - `pnpm -C web typecheck` 清白（无新增类型错误）。
    - 既有 Access Tokens 卡片 DOM/逻辑未改动（git diff 仅新增 import + 一张卡片）。
  </acceptance_criteria>
  <done>profile 页新增绑定卡片，typecheck 清白，前端整套 GREEN。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 浏览器组件 ↔ store/api | 绑定管理纯展示；明文绝不进浏览器（后端 serializer 已白名单，前端不再引入明文来源） |
| 令牌下拉 ↔ AccessToken store | 仅渲染 name/前后缀指纹 + is_valid；不渲染/不持久化明文 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05 | Information Disclosure | UI 渲染/缓存令牌明文 | mitigate | store 仅缓存绑定/工具元数据；下拉用 BoundTokenDto(name/prefix/suffix)；spec never_renders_token_plaintext 守卫 |
| T-10-07 | Tampering | 绑到已吊销/过期令牌 | mitigate | 下拉仅列 is_valid===true 令牌（per Pitfall 5）；后端 owner 校验兜底 |
| T-10-02 | Elevation | 前端越权管理他人绑定 | accept | 后端 owner 隔离（10-03 get_queryset）为唯一关卡；前端仅展示自己的数据 |
| T-10-SC | Tampering | 供应链（包安装） | accept | 零新增依赖，无 install 任务（复用既有 vue/pinia/reka-ui） |
</threat_model>

<verification>
- `cd web && pnpm vitest run src/components/toolBindings` → exit 0（5 条 spec GREEN）。
- `cd web && pnpm -C web typecheck` → 清白。
- `rg "friday_pat_" web/src/stores/toolBindings.ts web/src/components/toolBindings` → 无明文驻留（仅 spec mock 内允许）。
</verification>

<success_criteria>
- MCPB-01/03 前端行为 GREEN：列出可绑定 mcp/skill 工具 + 当前绑定令牌、从有效令牌下拉绑定/换绑、解绑；profile 页新增卡片。
- 明文零进入 store/渲染；下拉仅列 is_valid 令牌。
- typecheck 清白，既有 Access Tokens 区不回退。
</success_criteria>

<output>
Create `.planning/phases/10-mcpb/10-04-SUMMARY.md` when done
</output>
