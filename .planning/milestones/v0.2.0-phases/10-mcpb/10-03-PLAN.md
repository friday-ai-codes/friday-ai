---
phase: 10-mcpb
plan: 03
type: execute
wave: 3
depends_on: ["10-02"]
files_modified:
  - server/tools/serializers.py
  - server/tools/views.py
  - server/tools/urls.py
  - server/friday/urls.py
autonomous: true
requirements: [MCPB-01, MCPB-02, MCPB-03, RTOOL-01]
must_haves:
  truths:
    - "GET /api/tools/bindings/ 仅返回 request.user 自己的绑定（owner 隔离），响应不含明文/token_hash"
    - "POST /api/tools/bindings/ upsert：重复绑同一工具更新所绑令牌（update_or_create），不撞 unique 抛 500"
    - "POST 绑定校验 access_token.created_by==request.user（引用他人令牌 id → 4xx）且 remote_tool.source∈{mcp,skill} 且 is_active（builtin/失活 → 4xx）"
    - "DELETE /api/tools/bindings/{id}/ 仅能删自己的绑定（他人 → 404）"
    - "GET /api/tools/bindable/ 只列 source∈{mcp,skill} 且 is_active 的工具"
    - "POST /api/tools/execute/ authentication=[AccessTokenAuthentication] + IsAuthenticated fail-closed：匿名/无效 PAT → 401；有效 PAT → begin_interaction_run 审计 + execute_tool(name,args) → 200 {ok,...}"
    - "tools.urls 挂载进 friday/urls.py，前缀 /api/tools/"
  artifacts:
    - path: "server/tools/serializers.py"
      provides: "BoundTokenSerializer / ToolTokenBindingSerializer / ToolTokenBindingCreateSerializer / BindableToolSerializer / RemoteToolExecuteSerializer"
      contains: "class ToolTokenBindingCreateSerializer"
    - path: "server/tools/views.py"
      provides: "ToolTokenBindingViewSet / BindableToolsView / RemoteToolExecuteView"
      exports: ["ToolTokenBindingViewSet", "BindableToolsView", "RemoteToolExecuteView"]
    - path: "server/tools/urls.py"
      provides: "router bindings/ + bindable/ + execute/ 路由"
      contains: "tool-execute"
    - path: "server/friday/urls.py"
      provides: "/api/tools/ 挂载"
      contains: "include(\"tools.urls\")"
  key_links:
    - from: "server/tools/views.py RemoteToolExecuteView"
      to: "tools.executor.execute_tool"
      via: "await execute_tool(name, arguments) after begin_interaction_run"
      pattern: "execute_tool\\("
    - from: "server/tools/views.py ToolTokenBindingViewSet.get_queryset"
      to: "ToolTokenBinding"
      via: "filter(user=request.user)"
      pattern: "filter\\(user=self.request.user\\)"
    - from: "server/friday/urls.py"
      to: "tools.urls"
      via: "api_patterns path tools/ include"
      pattern: "include\\(\"tools.urls\"\\)"
---

<objective>
装配绑定 CRUD 与 RemoteTool 执行端点：在 tools app 新建 serializers.py / views.py / urls.py，挂进 friday/urls.py（/api/tools/）。两条主线——(1) 绑定 CRUD：owner 隔离 ModelViewSet（mirror AccessTokenViewSet）+ upsert + access_token 归属校验 + bindable 只读端点；(2) 执行端点：PAT-only fail-closed APIView（mirror McpToolView）→ begin_interaction_run 审计 → execute_tool 透传。使 10-01 后端 RED 用例转 GREEN。

Purpose: 满足 MCPB-01（绑定入库 + upsert）、MCPB-03（owner 查看/解绑）、MCPB-02（执行端点以 owner 身份运行 + 审计指纹=token_hash）、RTOOL-01（按 name 执行的认证端点）。
Output: 3 个新文件（serializers/views/urls）+ 1 处 friday/urls.py 增量。**本期不改 execute_tool 签名**（无 user 上下文；Phase 11 gap 已在 RESEARCH §Open Questions Q1 记录）。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-mcpb/10-RESEARCH.md
@server/access_tokens/views.py
@server/access_tokens/serializers.py
@server/access_tokens/urls.py
@server/mcp_tools/views.py
@server/interactions/entry.py
@server/tools/executor.py
@server/friday/urls.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: serializers.py — 绑定读/写/可绑定/执行四类序列化器</name>
  <files>server/tools/serializers.py</files>
  <read_first>
    - server/access_tokens/serializers.py:13-46（AccessTokenSerializer 白名单 read_only_fields 范式——绝不含 token_hash/明文；BoundTokenSerializer 直接复用该字段集子集）
    - server/tools/models.py（ToolTokenBinding 字段 + RemoteTool.Source 枚举 MCP/SKILL/BUILTIN + is_active）
    - server/access_tokens/models.py:29-78（AccessToken.id 为 UUID 主键、created_by FK、is_valid property）
    - server/mcp_tools/views.py:171-186（_validate：sync_to_async(serializer.is_valid) async-safe 校验范式）
    - .planning/phases/10-mcpb/10-RESEARCH.md §Code Examples「绑定序列化器」「执行端点 serializer」+ §Common Pitfalls 1（access_token 归属校验）
  </read_first>
  <behavior>
    - BoundTokenSerializer(AccessToken)：fields=["id","name","token_prefix","token_suffix","is_valid"]，全 read_only —— 绝不含 token_hash/明文。
    - ToolTokenBindingSerializer(ToolTokenBinding, read)：嵌套 access_token=BoundTokenSerializer(read_only)；扁平 remote_tool / remote_tool_name(source="remote_tool.name") / remote_tool_source(source="remote_tool.source") / created_at / updated_at；read_only_fields=fields。
    - ToolTokenBindingCreateSerializer：remote_tool=PrimaryKeyRelatedField(queryset=RemoteTool.objects.all())、access_token=PrimaryKeyRelatedField(queryset=AccessToken.objects.all())。validate_access_token：断言 `value.created_by_id == self.context["request"].user.id`，否则 raise ValidationError（per Pitfall 1，不泄漏存在性）。validate_remote_tool：断言 `value.source in {MCP, SKILL}` 且 `value.is_active`，否则 raise ValidationError。
    - BindableToolSerializer：id/name/description/source（只读展示）。
    - RemoteToolExecuteSerializer：name=CharField(必填)、arguments=DictField(required=False, default=dict)（RTOOL-01 按 name 执行）。
  </behavior>
  <action>
    新建 server/tools/serializers.py，定义上述 5 个 serializer（中文 docstring）。BoundTokenSerializer / ToolTokenBindingSerializer 严格走字段白名单，**严禁** token_hash 或明文进入任何 fields（沿用 AccessTokenSerializer 契约）。Create serializer 的两个 validate_* 是 owner 隔离在 access_token 维度的唯一关卡（per Pitfall 1）+ source/active 白名单（per MCPB-01 可绑定范围）。校验失败一律 ValidationError（由 view 收成 400）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/test_tool_bindings.py -k "builtin or others_token or no_plaintext" -q 2>&1 | tail -15 || true</automated>
  </verify>
  <acceptance_criteria>
    - `binding serializer output contains no token plaintext and no token_hash`：ToolTokenBindingSerializer/BoundTokenSerializer fields 集合不含 `token_hash`、不含任何明文字段。
    - `binding create with another user's access_token id is rejected`：validate_access_token 对 created_by != request.user 抛 ValidationError（test_bind_others_token_rejected 转 GREEN）。
    - `binding create with builtin/inactive tool is rejected`：validate_remote_tool 对 source=builtin 或 is_active=False 抛 ValidationError（test_bind_builtin_rejected 转 GREEN）。
    - RemoteToolExecuteSerializer 接受 {name, arguments?} 且 arguments 缺省为 {}。
  </acceptance_criteria>
  <done>5 个序列化器落地，白名单杜绝明文/hash，归属+source 校验生效。</done>
</task>

<task type="auto">
  <name>Task 2: views.py — 绑定 ViewSet(owner 隔离+upsert) + 可绑定列表 + 执行端点(PAT fail-closed)</name>
  <files>server/tools/views.py</files>
  <read_first>
    - server/access_tokens/views.py:33-85（AccessTokenViewSet：get_queryset(created_by=request.user) owner 隔离 + acreate 覆写 + http_method_names 白名单——绑定 ViewSet 的镜像蓝本）
    - server/mcp_tools/views.py:142-186（McpToolView：authentication_classes/permission_classes、handle_exception 把 AuthenticationFailed/NotAuthenticated→401、_begin/_validate async-safe 范式）
    - server/interactions/entry.py:39-112（begin_interaction_run(request, source=...)：request.auth.token_hash 作指纹；AccessTokenAuthentication re-export）
    - server/tools/executor.py:17-43（execute_tool async 协程，直接 await，无需 sync_to_async；返回 {ok,...}）
    - .planning/phases/10-mcpb/10-RESEARCH.md §Code Examples「绑定 ViewSet」「可绑定工具列表端点」「执行端点」+ §Common Pitfalls 2/3/4（401 不降级 / upsert update_or_create / async ORM sync_to_async）
  </read_first>
  <action>
    新建 server/tools/views.py（adrf APIView/ModelViewSet），三个类：

    - `ToolTokenBindingViewSet(ModelViewSet)`：`serializer_class=ToolTokenBindingSerializer`、`permission_classes=[IsAuthenticated]`、`http_method_names=["get","post","delete","head","options"]`。`get_queryset` → `ToolTokenBinding.objects.filter(user=self.request.user).select_related("access_token","remote_tool").order_by("-created_at")`（owner 隔离，越权天然空集 / 删他人 → aget_object 404，per MCPB-03）。覆写 `async def acreate`：用 `ToolTokenBindingCreateSerializer(data=request.data, context={"request": request})` + `await sync_to_async(ser.is_valid)(raise_exception=True)`（async-safe，per Pitfall 4），再 `await ToolTokenBinding.objects.aupdate_or_create(user=request.user, remote_tool=ser.validated_data["remote_tool"], defaults={"access_token": ser.validated_data["access_token"]})`（upsert，per Pitfall 3），返回 `Response(ToolTokenBindingSerializer(binding).data, status=201)`。

    - `BindableToolsView(APIView)`：`permission_classes=[IsAuthenticated]`，`async def get` → 异步迭代 `RemoteTool.objects.filter(source__in=[RemoteTool.Source.MCP, RemoteTool.Source.SKILL], is_active=True).order_by("name")` 经 BindableToolSerializer 序列化 → Response(list)（per MCPB-01 可绑定范围）。

    - `RemoteToolExecuteView(APIView)`：`authentication_classes=[AccessTokenAuthentication]`（PAT only，locked）、`permission_classes=[IsAuthenticated]`（fail-closed）。覆写 `handle_exception`：`AuthenticationFailed/NotAuthenticated → Response({"ok": False, "error": {"code":"authentication_failed","message":...}}, status=401)`（mirror McpToolView，per Pitfall 2，401 不降级 403）。`async def post`：`RemoteToolExecuteSerializer(data=request.data)` + `await sync_to_async(ser.is_valid)(raise_exception=True)` → `await begin_interaction_run(request, source="tool")`（审计，指纹=token_hash，per MCPB-02/IDENT-04）→ `result = await execute_tool(ser.validated_data["name"], ser.validated_data.get("arguments") or {})` → `Response(result, status=200)`（透传 executor {ok,...} 契约，per RTOOL-01）。

    `execute_tool` 从模块顶层 `from tools.executor import execute_tool` 引入（使 10-01 的 `monkeypatch.setattr("tools.views.execute_tool", ...)` 生效）。**不改 execute_tool 签名**，不向其传 user（Phase 11 gap）。执行端点**不做**绑定强校验（discretion：可执行任意 active 工具，per RESEARCH Open Q2）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/test_remote_tool_execute.py -q 2>&1 | tail -15 || true</automated>
  </verify>
  <acceptance_criteria>
    - `POST /api/tools/execute/ without PAT returns 401`（test_anonymous_401 GREEN，非 403）。
    - `POST /api/tools/execute/ with valid PAT executes tool by name and returns ok`（test_pat_execute_ok GREEN，200 + ok:True）。
    - `POST /api/tools/execute/ with unknown tool returns ok:false code=not_found`（test_unknown_tool_ok_false GREEN，透传 executor）。
    - `execute endpoint records InteractionRun with fingerprint=token_hash`（test_execute_records_run GREEN）。
    - `GET bindings only returns request.user's bindings` + `upsert rebind updates token`（test_list_owner_isolation / test_upsert_rebind_updates_token GREEN）。
    - `DELETE another user's binding returns 404`（test_unbind_and_cross_user_404 GREEN）。
  </acceptance_criteria>
  <done>三个 view 实现，绑定 owner 隔离+upsert、执行端点 fail-closed+审计+透传，对应后端用例 GREEN。</done>
</task>

<task type="auto">
  <name>Task 3: urls.py（router + bindable/ + execute/）+ 挂载 /api/tools/</name>
  <files>server/tools/urls.py, server/friday/urls.py</files>
  <read_first>
    - server/access_tokens/urls.py:1-14（adrf DefaultRouter().register("", ViewSet, basename=...) + include 范式）
    - server/friday/urls.py:22-63（api_patterns 列表；在 access-tokens/ 与 mcp/ 并列处新增 tools/ 行）
    - .planning/phases/10-mcpb/10-RESEARCH.md §Code Examples「urls（server/tools/urls.py 新建）+ 挂载」
  </read_first>
  <action>
    新建 server/tools/urls.py：
    - `from adrf.routers import DefaultRouter`；`router = DefaultRouter()`；`router.register("bindings", ToolTokenBindingViewSet, basename="tool-binding")`。
    - urlpatterns = `[ path("", include(router.urls)), path("bindable/", BindableToolsView.as_view(), name="bindable-tools"), path("execute/", RemoteToolExecuteView.as_view(), name="tool-execute") ]`。
    - 从 tools.views import 三个类。

    在 server/friday/urls.py 的 `api_patterns` 内、与 `path("access-tokens/", include("access_tokens.urls"))` / `path("mcp/", include("mcp_tools.urls"))` 并列处新增一行 `path("tools/", include("tools.urls"))` → 端点 /api/tools/bindings/ /api/tools/bindable/ /api/tools/execute/。**不得**改动其它既有行。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/test_tool_bindings.py tests/test_remote_tool_execute.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_tool_bindings.py tests/test_remote_tool_execute.py -q exits 0`（MCPB-01/02/03 + RTOOL-01 后端全 GREEN）。
    - /api/tools/bindings/ 、/api/tools/bindable/ 、/api/tools/execute/ 三路由可解析（reverse("tool-execute") 成功）。
    - friday/urls.py 仅新增 tools/ 一行，其它 include 行未改动（git diff 范围最小）。
  </acceptance_criteria>
  <done>路由挂载完成，本阶段后端两套测试全绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→/api/tools/bindings/ | 绑定 CRUD（CookieJWT）；owner 隔离 + access_token 归属校验是唯一授权点 |
| container/agent→/api/tools/execute/ | PAT 认证执行端点；fail-closed 是站点基线 |
| serializer 输出 ↔ 客户端 | 令牌元数据出口；白名单杜绝明文/hash |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Elevation / Tampering | 绑定引用他人令牌 id（IDOR） | mitigate | ToolTokenBindingCreateSerializer.validate_access_token 断言 created_by==request.user → 400 |
| T-10-02 | Elevation | list/delete 他人绑定 | mitigate | get_queryset(user=request.user)；越权 list 空集、delete aget_object → 404 |
| T-10-03 | Spoofing | 匿名/无效 PAT 调执行端点 | mitigate | authentication_classes=[AccessTokenAuthentication]+IsAuthenticated；handle_exception 401 不降级 |
| T-10-04 | Tampering | 重复绑定撞 unique 抛 500 | mitigate | acreate 用 aupdate_or_create 收敛为 upsert |
| T-10-05 | Information Disclosure | 响应/日志泄漏明文/hash | mitigate | serializer 白名单 + begin_interaction_run 指纹=token_hash + ledger redact（沿用既有契约） |
| T-10-06 | Tampering | 绑 builtin/失活工具 | mitigate | validate_remote_tool 校验 source∈{mcp,skill} 且 is_active |
| T-10-SC | Tampering | 供应链（包安装） | accept | 零新增依赖，无 install 任务（RESEARCH §Standard Stack） |
</threat_model>

<verification>
- `cd server && uv run pytest tests/test_tool_bindings.py tests/test_remote_tool_execute.py -q` → exit 0。
- `cd server && uv run pytest -q` 关键回归（access_tokens / mcp_tools / interactions 套件）不回退。
- `rg "token_hash" server/tools/serializers.py` → 无（白名单不含 hash）。
</verification>

<success_criteria>
- MCPB-01/02/03 + RTOOL-01 后端行为全部 GREEN：owner 隔离绑定 CRUD、upsert 换令牌、跨用户令牌引用/builtin 拒绝、bindable 过滤、PAT fail-closed 执行端点（匿名 401）、审计指纹=token_hash、executor 契约透传。
- 响应与日志零明文/hash 泄漏；execute_tool 签名未改（Phase 11 gap 保留）。
- /api/tools/ 三路由挂载，friday/urls.py 改动最小。
</success_criteria>

<output>
Create `.planning/phases/10-mcpb/10-03-SUMMARY.md` when done
</output>
