---
phase: 10-mcpb
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/tests/conftest.py
  - server/tests/test_tool_bindings.py
  - server/tests/test_remote_tool_execute.py
  - web/src/components/toolBindings/__tests__/ToolBindingSettings.spec.ts
autonomous: true
requirements: [MCPB-01, MCPB-02, MCPB-03, RTOOL-01]
must_haves:
  truths:
    - "test_tool_bindings.py / test_remote_tool_execute.py / ToolBindingSettings.spec.ts 可被收集且初始 RED（实现前红，实现后绿），无 import/collection error"
    - "make_remote_tool / make_tool_binding fixtures 可用（make_tool_binding 在 ToolTokenBinding 未落地时优雅 skip，不报错）"
    - "绑定测试覆盖 owner 隔离、跨用户令牌引用拒绝、upsert 换令牌、builtin 拒绝、bindable 过滤、无明文/hash 泄漏"
    - "执行端点测试覆盖 PAT happy(200+ok)、匿名 401、吊销 PAT 401、未知工具 ok:false、审计 InteractionRun 创建(fingerprint=token_hash)"
    - "前端 spec 覆盖：列出可绑定 mcp/skill 工具、下拉只列 is_valid 令牌、bind 调 store.upsert、unbind 调 store.unbind、不渲染明文"
  artifacts:
    - path: "server/tests/test_tool_bindings.py"
      provides: "MCPB-01/03 绑定 CRUD owner 隔离 + 不泄漏锁名测试"
      contains: "def test_bind_others_token_rejected"
    - path: "server/tests/test_remote_tool_execute.py"
      provides: "RTOOL-01/MCPB-02 PAT 执行端点 fail-closed + 审计锁名测试"
      contains: "def test_anonymous_401"
    - path: "web/src/components/toolBindings/__tests__/ToolBindingSettings.spec.ts"
      provides: "绑定管理 UI 行为锁名 spec（mock store）"
      contains: "ToolBindingSettings"
    - path: "server/tests/conftest.py"
      provides: "make_remote_tool / make_tool_binding fixtures"
      contains: "def make_remote_tool"
  key_links:
    - from: "server/tests/test_remote_tool_execute.py"
      to: "/api/tools/execute/"
      via: "APIClient.post with Bearer friday_pat_ 凭证"
      pattern: "/api/tools/execute/"
    - from: "server/tests/test_tool_bindings.py"
      to: "/api/tools/bindings/"
      via: "force_authenticate + APIClient CRUD"
      pattern: "/api/tools/bindings/"
---

<objective>
Wave 0 Nyquist 验证脚手架（RED-first）：在实现任何绑定模型/端点/前端之前，先把 MCPB-01/02/03 + RTOOL-01 的可验证行为用锁名测试钉死。新增 conftest 两个 fixtures（make_remote_tool / make_tool_binding），后端两份测试（test_tool_bindings.py / test_remote_tool_execute.py），前端一份 spec（ToolBindingSettings.spec.ts）。

Purpose: 满足 10-VALIDATION.md「Wave 0 Requirements」，让后续 10-02/10-03/10-04 的实现有红→绿的可度量目标；安全契约（owner 隔离、跨用户令牌引用拒绝、fail-closed、无明文泄漏）在测试层先行固定。
Output: 1 处 conftest 增量 + 2 个后端测试文件 + 1 个前端 spec 文件。预期：后端测试 RED（端点 404 / 模型缺失 skip），前端 spec RED（组件/ store 未实现）。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-mcpb/10-RESEARCH.md
@.planning/phases/10-mcpb/10-VALIDATION.md
@server/tests/conftest.py
@server/tests/test_pat_identity.py
@server/tools/models.py
@server/tools/executor.py
@web/src/components/accessTokens/__tests__/AccessTokenSettings.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: conftest fixtures — make_remote_tool + make_tool_binding</name>
  <files>server/tests/conftest.py</files>
  <read_first>
    - server/tests/conftest.py:1293-1348（make_access_token 工厂范式：返回 (实例, 明文)；access_user/user 复用；importorskip 守卫未落地 app 的写法）
    - server/tests/conftest.py:130-215（user / second_user / second_user_and_token / superuser fixtures 命名与 async 铸 JWT 范式）
    - server/tools/models.py:12-34（RemoteTool 字段：name unique / source builtin·mcp·skill / description / input_schema(JSON, 必填) / timeout / is_active / config(default dict)）
    - .planning/phases/10-mcpb/10-RESEARCH.md §Code Examples「模型」+ §Validation Architecture「Wave 0 Gaps」（make_remote_tool 参数化 source/is_active；make_tool_binding(user/token/tool)）
  </read_first>
  <action>
    在 server/tests/conftest.py 末尾新增两个 fixtures（中文 docstring + 完整类型注解，遵循 CLAUDE.md 约定）：

    - `make_remote_tool`：工厂 fixture（依赖 db），签名 `(name="mcp_tool", source="mcp", is_active=True, description="t", input_schema=None, timeout=30, config=None) -> RemoteTool`。直接 `from tools.models import RemoteTool`（该模型已存在，无需 importorskip）。input_schema 缺省填 `{"type": "object", "properties": {}}`，config 缺省 `{}`。每次调用用唯一 name（拼随机后缀，因 name unique），但允许显式传 name 覆盖。返回 RemoteTool 实例。

    - `make_tool_binding`：工厂 fixture（依赖 db），签名 `(user, access_token, remote_tool) -> ToolTokenBinding`。**前向兼容守卫**：函数体内 `models = pytest.importorskip("tools.models")`，再 `binding_cls = getattr(models, "ToolTokenBinding", None)`；若为 None 则 `pytest.skip("ToolTokenBinding 尚未实现（10-02）")`。用 `binding_cls.objects.create(user=user, access_token=access_token, remote_tool=remote_tool)` 创建并返回（供跨用户隔离用例直接播种他人绑定）。

    NEVER 改动既有 fixtures 语义；仅新增。绝不在 fixture 中打印/返回任何令牌明文以外的敏感字段（make_tool_binding 不接触明文）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/conftest.py -q 2>&1 | head -5 || true</automated>
  </verify>
  <acceptance_criteria>
    - `make_remote_tool` 可创建 source ∈ {mcp, skill, builtin} 且 is_active 可参数化的 RemoteTool（被后端测试消费时不报错）。
    - `make_tool_binding` 在 ToolTokenBinding 未落地时触发 `pytest.skip`（非 ImportError / 非 collection error）。
    - `cd server && uv run pytest tests/test_pat_identity.py -q` 仍全绿（既有 fixtures 未被破坏）。
  </acceptance_criteria>
  <done>两个 fixtures 落地，既有套件不回退，make_tool_binding 在模型缺失时优雅 skip。</done>
</task>

<task type="auto">
  <name>Task 2: 后端 RED 测试 — test_tool_bindings.py + test_remote_tool_execute.py</name>
  <files>server/tests/test_tool_bindings.py, server/tests/test_remote_tool_execute.py</files>
  <read_first>
    - server/tests/test_pat_identity.py:38-122（真实 PAT 经完整 DRF 链路：`APIClient().credentials(HTTP_AUTHORIZATION=f"Bearer {plaintext}")`；匿名/吊销 401 断言范式；make_access_token(revoked=True) 用法）
    - server/access_tokens/views.py:33-44（AccessTokenViewSet owner 隔离 get_queryset 范式——绑定 CRUD 的镜像目标）
    - server/tools/executor.py:17-33（execute_tool 返回契约：{"ok": True, "result"} / {"ok": False, "error": {"code": "not_found"...}}；未知工具 not_found）
    - server/interactions/entry.py:53-112（begin_interaction_run：token_fingerprint=request.auth.token_hash；InteractionRun 落库）
    - .planning/phases/10-mcpb/10-RESEARCH.md §Validation Architecture「Phase Requirements → Test Map」（11 条用例的精确名）+ §Common Pitfalls 1/2/3（跨用户令牌引用、401 不降级、upsert 不撞 500）
  </read_first>
  <action>
    新建两份 pytest 文件（`pytestmark = pytest.mark.django_db`，文件头中文 docstring 说明 RED 意图与覆盖的需求 ID）。所有 URL 用硬编码字符串（`/api/tools/bindings/`、`/api/tools/bindable/`、`/api/tools/execute/`，末尾带 `/`），**不 import 未落地的 views/serializers**，从而实现「端点 404 → RED」而非 collection error。

    **server/tests/test_tool_bindings.py（绑定 CRUD，CookieJWT 经 `APIClient.force_authenticate(user=...)`）**，至少含：
    - `test_upsert_rebind_updates_token`（MCPB-01）：force_authenticate(user) → make_remote_tool(source="mcp") + 两把 make_access_token；POST `/api/tools/bindings/` 绑 token A → 201；再 POST 同 remote_tool 绑 token B → 仍 201/200 且 GET 列表只 1 条、其 access_token 指向 B（upsert 换令牌，per Pitfall 3）。
    - `test_bind_builtin_rejected`（MCPB-01）：绑 source="builtin" 工具 → 400（source 白名单，per Pitfall 1）。
    - `test_bind_others_token_rejected`（MCPB-01）：用 `make_access_token` 给 access_user 造 token，再 force_authenticate(second_user) 用该 token id 绑自己可见的工具 → 4xx（400/403/404，断言 `>= 400`），且不创建绑定（per Pitfall 1：access_token.created_by==request.user 校验）。
    - `test_list_owner_isolation`（MCPB-03）：second_user 经 API 绑一条；force_authenticate(user) GET 列表 → 不含 second_user 的绑定（owner 隔离）。
    - `test_unbind_and_cross_user_404`（MCPB-03）：自己绑后 DELETE `/api/tools/bindings/{id}/` → 204；另一用户 DELETE 他人绑定 id → 404（不泄漏存在性）。
    - `test_serializer_no_plaintext_no_hash`（MCPB-01/03）：绑定后 GET 列表，断言响应 JSON 文本里不含明文（`make_access_token` 返回的 plaintext 子串）且不含 `token_hash` 键（白名单 only：name/token_prefix/token_suffix/is_valid）。
    - `test_bindable_filters_mcp_skill_active`（MCPB-01）：造 mcp(active) + skill(active) + builtin(active) + mcp(inactive)；GET `/api/tools/bindable/` → 只含前两者（source∈{mcp,skill} 且 is_active）。

    **server/tests/test_remote_tool_execute.py（执行端点，真实 PAT 经 `client.credentials(Bearer)`）**，至少含：
    - `test_pat_execute_ok`（RTOOL-01）：`monkeypatch.setattr("tools.views.execute_tool", _stub, raising=False)`，`_stub` 为 async 返回 `{"ok": True, "result": {...}}`；make_access_token → client.credentials → POST `/api/tools/execute/` body `{"name": "x", "arguments": {}}` → 200 且 `resp.data["ok"] is True`。
    - `test_anonymous_401`（RTOOL-01）：无凭证 POST → 401（fail-closed，per Pitfall 2，断言 401 而非 403）。
    - `test_revoked_pat_401`（RTOOL-01/IDENT-05）：make_access_token(revoked=True) → POST → 401。
    - `test_unknown_tool_ok_false`（RTOOL-01）：**不 mock** execute_tool，make_access_token，POST name="does_not_exist" → 200 且 `resp.data["ok"] is False` 且 `resp.data["error"]["code"] == "not_found"`（透传 executor 契约）。
    - `test_execute_records_run`（MCPB-02/IDENT-04）：mock execute_tool stub；POST 后断言 `InteractionRun.objects.filter(token_fingerprint=token.token_hash).exists()`（审计建 run，指纹=hash，无明文）。

    所有 async stub 写成 `async def _stub(name, arguments): return {...}`。断言 status_code 用具体数值（401/200/204/4xx）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/test_tool_bindings.py tests/test_remote_tool_execute.py -q 2>&1 | tail -20 || true</automated>
  </verify>
  <acceptance_criteria>
    - 两文件可被 pytest 收集，无 import/collection error（端点未实现时为 RED 失败，不是 error）。
    - 含 `test_bind_others_token_rejected`、`test_anonymous_401`、`test_unknown_tool_ok_false`、`test_execute_records_run`、`test_serializer_no_plaintext_no_hash` 等锁名用例。
    - 当前运行整体 RED（至少绑定/执行 happy 路径因端点缺失而失败）；`test_anonymous_401` 可能因全站默认认证已是 RED 或偶然 PASS，均不算 collection error。
    - 无明文断言用 make_access_token 返回的 plaintext 子串做 `assert plaintext not in resp.content.decode()`。
  </acceptance_criteria>
  <done>两份后端测试落地、可收集、初始 RED，覆盖 MCPB-01/02/03 + RTOOL-01 全部 Wave 0 用例。</done>
</task>

<task type="auto">
  <name>Task 3: 前端 RED spec — ToolBindingSettings.spec.ts</name>
  <files>web/src/components/toolBindings/__tests__/ToolBindingSettings.spec.ts</files>
  <read_first>
    - web/src/components/accessTokens/__tests__/AccessTokenSettings.spec.ts（整文件：vi.mock('~/stores/...') 注入 store、stubs 透传 Dialog/AlertDialog、makeToken 工厂、findComponent + emit 驱动断言范式）
    - web/src/types/accessToken.ts:12-29（AccessTokenDto 字段：is_valid / token_prefix / token_suffix；下拉只列 is_valid 的依据）
    - .planning/phases/10-mcpb/10-RESEARCH.md §Recommended Project Structure（前端文件：components/toolBindings/{ToolBindingSettings,ToolBindingTable,ToolBindDialog}.vue + stores/toolBindings.ts + api/toolBindings.ts + types/toolBinding.ts）+ §前端 API 片段
  </read_first>
  <action>
    新建 `web/src/components/toolBindings/__tests__/ToolBindingSettings.spec.ts`（mirror AccessTokenSettings.spec 结构）。用 `vi.mock` 注入两个 store 与 toast/errorHandler，**不触网**：
    - mock `~/stores/toolBindings`：`useToolBindingStore` 返回 `{ bindings: ref([])/数组, bindableTools: 数组, loading: false, fetchBindings, fetchBindable, upsertBinding, unbindBinding }`（fetch* 默认 resolve []）。
    - mock `~/stores/accessTokens`：`useAccessTokenStore` 返回含 `tokens` 的对象，tokens 同时含一个 `is_valid:true` 与一个 `is_valid:false` 令牌（验证下拉过滤）。
    - mock `~/composables/useToast` 与 `~/composables/useErrorHandler`（同 access token spec）。
    - 透传 stub Dialog/AlertDialog/Select 原语（复用 access token spec 的 stubs/selectStubs 写法）。

    断言（锁名 it）：
    - `lists_bindable_mcp_skill_tools`：渲染传入的 bindable mcp/skill 工具行（每行展示工具名）。
    - `bind_dropdown_only_lists_valid_tokens`：打开绑定对话框/下拉，仅出现 is_valid===true 的令牌（按 name/前后缀展示），不出现 is_valid:false 的令牌。
    - `bind_calls_store_upsert_with_tool_and_token`：选工具 + 选令牌 + 确认 → `upsertBinding` 被调用且参数含 `{ remote_tool, access_token }`。
    - `unbind_calls_store_unbind`：对已有绑定点解绑/确认 → `unbindBinding` 被调用且传该绑定 id。
    - `never_renders_token_plaintext`：组件文本不含任何 `friday_pat_` 完整明文（仅 prefix/suffix 指纹），断言不渲染 mock 注入的 `token` 明文字段（store 不应暴露明文，故组件无来源）。

    组件/ store 尚未实现 → spec 初始 RED（import 组件失败即 RED；为可收集，import 路径指向待建 `~/components/toolBindings/ToolBindingSettings.vue`，与 10-04 产物一致）。
  </action>
  <verify>
    <automated>cd web && pnpm vitest run src/components/toolBindings 2>&1 | tail -20 || true</automated>
  </verify>
  <acceptance_criteria>
    - spec 文件存在且 import 目标路径与 10-04 将创建的组件/ store 完全一致（ToolBindingSettings.vue / useToolBindingStore / useAccessTokenStore）。
    - 含锁名用例 `bind_dropdown_only_lists_valid_tokens`、`bind_calls_store_upsert_with_tool_and_token`、`unbind_calls_store_unbind`、`never_renders_token_plaintext`。
    - 当前 `pnpm vitest run src/components/toolBindings` 为 RED（组件未实现），无语法/解析错误。
  </acceptance_criteria>
  <done>前端绑定 UI 行为 spec 落地、RED，钉死下拉只列 valid 令牌 + 无明文渲染契约。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 测试断言 ↔ 实现 | Wave 0 测试是安全契约的可执行规格；owner 隔离 / fail-closed / 无明文 在此先行固定 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Information Disclosure | 测试可能误用明文断言不严 | mitigate | test_serializer_no_plaintext_no_hash 用 make_access_token 真实 plaintext 子串做 `not in` 断言 + 断言无 token_hash 键 |
| T-10-02 | Elevation of Privilege | 跨用户令牌引用 / 越权 list-delete 未被钉死 | mitigate | test_bind_others_token_rejected + test_list_owner_isolation + test_unbind_and_cross_user_404 锁名用例 |
| T-10-03 | Spoofing | 执行端点 fail-closed 未被验证 | mitigate | test_anonymous_401 + test_revoked_pat_401 断言精确 401（非 403 降级） |
| T-10-SC | Tampering | 供应链（包安装） | accept | 本阶段零新增依赖（RESEARCH §Standard Stack「无需安装任何包」），无 install 任务 → 无 slopcheck 需求 |
</threat_model>

<verification>
- `cd server && uv run pytest tests/test_tool_bindings.py tests/test_remote_tool_execute.py -q` → 可收集、初始 RED（实现后由 10-02/10-03 转 GREEN）。
- `cd server && uv run pytest tests/test_pat_identity.py -q` → exit 0（既有套件不回退）。
- `cd web && pnpm vitest run src/components/toolBindings` → 可解析、初始 RED（实现后由 10-04 转 GREEN）。
</verification>

<success_criteria>
- 全部 Wave 0 文件落地且可收集（无 import/collection error）。
- 后端 11 条 + 前端 5 条锁名用例就位，分别覆盖 MCPB-01/02/03 + RTOOL-01 的 owner 隔离、fail-closed、upsert、bindable 过滤、审计、无明文泄漏。
- make_remote_tool / make_tool_binding fixtures 可被后续 plan 消费，且模型缺失时优雅 skip。
</success_criteria>

<output>
Create `.planning/phases/10-mcpb/10-01-SUMMARY.md` when done
</output>
