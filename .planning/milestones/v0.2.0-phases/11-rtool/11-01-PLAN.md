---
phase: 11-rtool
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - task/tests/test_remote_tools.py
  - task/tests/conftest.py
  - server/tests/test_remote_tool_dispatch.py
  - runner/internal/docker/executor_test.go
autonomous: true
requirements: [RTOOL-02, RTOOL-03, RTOOL-04]
must_haves:
  truths:
    - "task 测试覆盖：schema 列表→N 个 SdkMcpTool、无 tools/无 token→不挂 server、handler 200→文本结果、handler 401→结构化错误不抛、PAT 不进日志/返回文本"
    - "server 测试覆盖：dispatch metadata 含 env_FRIDAY_TASK_TOOLS_ENDPOINT（由 FRIDAY_BASE_URL 推导，非 callback_url）；机会性 PAT 存在时含 env_FRIDAY_TASK_USER_TOKEN；无明文来源时不注入（绝不读 AccessToken/DB）"
    - "runner 测试覆盖：buildContainerEnv 产出 FRIDAY_TASK_REMOTE_TOOLS（来自 payload remote_tools）+ 经 metadata env_ 透传 FRIDAY_TASK_USER_TOKEN/FRIDAY_TASK_TOOLS_ENDPOINT"
    - "三组件 Wave 0 用例在 impl 落地前不产生 collection error（task 用 importorskip 守卫），impl 落地后即为可执行断言"
  artifacts:
    - path: "task/tests/test_remote_tools.py"
      provides: "RTOOL-02/03/04 task 侧单测（SDK MCP 构建 + handler 回调 mocked httpx + 401 graceful + 脱敏）"
      contains: "def test_"
    - path: "server/tests/test_remote_tool_dispatch.py"
      provides: "RTOOL-03 server dispatch 契约测试（tools endpoint 推导 + 机会性 PAT 路径 + 不读 DB）"
      contains: "env_FRIDAY_TASK_TOOLS_ENDPOINT"
    - path: "runner/internal/docker/executor_test.go"
      provides: "RTOOL-03 runner env 装配测试（新增 TestBuildContainerEnv_RemoteTools）"
      contains: "FRIDAY_TASK_REMOTE_TOOLS"
  key_links:
    - from: "task/tests/test_remote_tools.py"
      to: "task/core/remote_tools.py"
      via: "importorskip('core.remote_tools') 调 build_remote_tools_mcp_server / _make_handler"
      pattern: "importorskip"
    - from: "server/tests/test_remote_tool_dispatch.py"
      to: "workflows.nodes.ai.coding.AICodingNode._execute_with_branch"
      via: "mock_dispatcher 捕获 DispatchTask.metadata 断言新键"
      pattern: "env_FRIDAY_TASK_TOOLS_ENDPOINT"
---

<objective>
Wave 0 RED 脚手架：在 task / server / runner 三组件各落一套自动化测试，钉死 RemoteTool 闭环的可验证行为契约（RTOOL-02 SDK MCP 构建与调用、RTOOL-03 三跳注入与脱敏、RTOOL-04 吊销 graceful），供 Wave 1 实现转 GREEN。

Purpose: 满足 Nyquist——每条 RTOOL 行为先有 `<automated>` 测试再有实现；锁定跨进程契约字段名（`FRIDAY_TASK_USER_TOKEN` / `FRIDAY_TASK_REMOTE_TOOLS` / `FRIDAY_TASK_TOOLS_ENDPOINT`）三侧一致。
Output: 3 个测试文件（task 新建 + conftest 扩展、server 新建、runner 扩展既有）；本 plan 不写任何生产代码。impl 落地前 task 用例经 importorskip 优雅跳过（无 collection error），落地后即为硬断言。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-rtool/11-RESEARCH.md
@.planning/phases/11-rtool/11-VALIDATION.md
@server/agents/sdk/mcp_adapter.py
@server/tests/test_coding_anthropic_base_url_passthrough.py
@task/tests/conftest.py
@runner/internal/docker/executor_test.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: task/tests/test_remote_tools.py（新建）+ conftest mock_config 扩展 — RTOOL-02/03/04 RED</name>
  <files>task/tests/test_remote_tools.py, task/tests/conftest.py</files>
  <read_first>
    - server/agents/sdk/mcp_adapter.py:24-127（SdkMcpTool 直接构造 + create_sdk_mcp_server(name, tools) 返回 McpSdkServerConfig；handler 返回 {"content":[{"type":"text","text":...}], "is_error"?} 结构——task handler 同款返回契约的断言依据）
    - task/tests/conftest.py:32-49（既有 mock_config fixture：MagicMock TaskConfig，需补 user_token/remote_tools/tools_endpoint 三字段）
    - task/tests/test_callback.py（httpx.AsyncClient 单测范式：monkeypatch client.post，参考 mock 响应构造）
    - .planning/phases/11-rtool/11-RESEARCH.md §Code Examples「task/core/remote_tools.py」（build_remote_tools_mcp_server / _make_handler 蓝本——签名与返回结构）+ §Wave 0 Gaps「无 live Claude 的测试法」（直接调 handler(args)，monkeypatch httpx，无需 mock query()）
    - .planning/phases/11-rtool/11-RESEARCH.md §Common Pitfalls 5（401 不 raise_for_status，转结构化错误返回）
  </read_first>
  <action>
    新建 task/tests/test_remote_tools.py（pytest + pytest-asyncio，中文 docstring）。**所有用例顶部** `remote_tools = pytest.importorskip("core.remote_tools")`（impl 未落地→优雅 skip，无 collection error；mirror Phase 10 10-01 守卫范式），再经 `getattr` 取 `build_remote_tools_mcp_server` / `_make_handler` / `remote_allowed_tools` / `REMOTE_MCP_SERVER_NAME`。

    覆盖用例（RTOOL-02/04 直接调 handler，无需 live Claude/query）：
    - `test_builds_n_tools`：给 2 条 schema（`[{"name":"a","description":"da","input_schema":{...}}, {"name":"b",...}]`）+ 非空 token + endpoint → `build_remote_tools_mcp_server(...)` 返回非 None；断言其内含 2 个工具且名称为 a/b（按 mcp_adapter 返回结构取 tools 列表/名称）。
    - `test_no_remote_tools_returns_none` 与 `test_no_token_returns_none` 与 `test_no_endpoint_returns_none`：任一缺失 → 返回 None（向后兼容，不挂 server）。
    - `test_remote_allowed_tools_naming`：`remote_allowed_tools([...])` 产出 `mcp__{REMOTE_MCP_SERVER_NAME}__{name}` 列表。
    - `test_handler_success_returns_content`：monkeypatch `httpx.AsyncClient.post` 返回伪 200，body `{"ok": True, "result": "DONE"}` → `await handler({...})` 返回 `{"content":[{"type":"text","text": 含 "DONE"}]}` 且 **无** is_error。
    - `test_handler_401_returns_tool_error_not_raise`（RTOOL-04）：post 返回 401 → handler **返回** `{"is_error": True, ...}`，**绝不抛异常**（用例不 expect raise；断言 is_error 为 True）。
    - `test_handler_transport_error_graceful`：post 抛 `httpx.HTTPError` → handler 捕获并返回 is_error，不冒泡。
    - `test_handler_non200_returns_tool_error`：post 返回 500 → is_error，不抛。
    - `test_token_not_in_logs_or_result`（RTOOL-03 脱敏）：用 `structlog` testing capture（或 caplog）+ token `friday_pat_SECRET123`，跑成功/401 两路 → 断言捕获日志文本与 handler 返回文本均 **不含** `friday_pat_SECRET123`。

    扩展 task/tests/conftest.py 的 `mock_config` fixture：补 `config.user_token = ""`、`config.remote_tools = []`、`config.tools_endpoint = ""` 三字段（供 Wave 1 executor 装配测试复用，默认空=向后兼容）。
  </action>
  <verify>
    <automated>cd task && uv run pytest tests/test_remote_tools.py -q 2>&1 | tail -15 || true</automated>
  </verify>
  <acceptance_criteria>
    - `cd task && uv run pytest tests/test_remote_tools.py -q` 在 impl 未落地时 **全部 skip 或 fail，且 0 errors（无 import/collection error）**。
    - 用例集合包含：N 工具构建、无 tools/token/endpoint→None、handler 200→content、handler 401→is_error 不抛、transport/500→is_error、token 不进日志/返回文本。
    - conftest `mock_config` 暴露 `user_token`/`remote_tools`/`tools_endpoint` 三属性。
    - 测试**不** import `claude_agent_sdk.query`、不依赖真实 Claude/网络（httpx 全 monkeypatch）。
  </acceptance_criteria>
  <done>task 侧 RTOOL-02/03/04 RED 用例与 mock_config 扩展就位，collection 干净。</done>
</task>

<task type="auto">
  <name>Task 2: server/tests/test_remote_tool_dispatch.py（新建）— RTOOL-03 dispatch 契约 RED</name>
  <files>server/tests/test_remote_tool_dispatch.py</files>
  <read_first>
    - server/tests/test_coding_anthropic_base_url_passthrough.py:72-407（dispatch 捕获范式全套：workflow/node/execution/repository/execution_context/mock_dispatcher/mock_subagent_session_create/mock_fetch_repositories_with_credential fixtures + `_execute_with_branch` 调用 + `mock_dispatcher[0].metadata` 断言——本测试直接复刻这套 fixture）
    - server/workflows/nodes/ai/coding.py:807-871（_run_repo_coding metadata 装配点：anthropic_env 模式即新 tools_env 注入点）
    - server/workflows/nodes/ai/coding.py:331-396（_execute_with_branch provider 解析 + _run_repo_coding 调用签名——机会性 PAT 注入的上游）
    - server/friday/settings.py:402（FRIDAY_BASE_URL 默认值——tools endpoint 推导来源）
    - .planning/phases/11-rtool/11-RESEARCH.md §Common Pitfalls 1（callback_url≠Friday Server，端点必须由 FRIDAY_BASE_URL 推导）+ §Open Q1 resolution（机会性 PAT，绝不读 AccessToken/DB）
  </read_first>
  <action>
    新建 server/tests/test_remote_tool_dispatch.py，复刻 test_coding_anthropic_base_url_passthrough.py 的 fixture 套（workflow/workflow_node/workflow_execution/node_execution/conv02_repository/execution_context/mock_dispatcher/mock_subagent_session_create/mock_fetch_repositories_with_credential/log）——可 import 复用或就地复制（保持独立文件可单跑）。monkeypatch `ProviderConfigService.aresolve_or_error` 返回合法 `ResolvedProviderConfig`（mirror 既有）。

    RED 断言（实现落地后转 GREEN）：
    - `test_dispatch_metadata_includes_tools_endpoint`：用 `settings.FRIDAY_BASE_URL` override（`@pytest.mark.django_db` + `settings` fixture 设 `FRIDAY_BASE_URL="https://friday.example.com"`）跑 `_execute_with_branch` → 断言 `mock_dispatcher[0].metadata["env_FRIDAY_TASK_TOOLS_ENDPOINT"] == "https://friday.example.com/api/tools/execute/"`（由 base 推导，**非** callback_url）。
    - `test_dispatch_opportunistic_pat_injected_when_present`：模拟「实时请求线程提供明文 PAT」路径（按 Wave 1 实现选定的传参/上下文机制 monkeypatch 注入一个明文 `friday_pat_REALTIME`）→ 断言 metadata `env_FRIDAY_TASK_USER_TOKEN == "friday_pat_REALTIME"`。
    - `test_dispatch_omits_pat_when_no_realtime_source`：无明文来源（默认背景触发）→ 断言 metadata **不含** `env_FRIDAY_TASK_USER_TOKEN` 键（PAT-02：绝不从 AccessToken/DB 取明文）。
    - `test_dispatch_never_reads_access_token_plaintext`：monkeypatch/spy 确认 dispatch 路径**未**调用任何 AccessToken 查询取明文（断言不存在 `AccessToken.objects` 取明文调用；或断言无 `friday_pat_` 来自 DB 的注入）。

    为避免 impl 未落地时断言 KeyError 误判，对「应存在键」用例可在取键前 `pytest.xfail`/条件 skip 守卫，或直接断言（RED 失败即预期）。优先：直接断言（RED 红，落地转绿），保持 Nyquist 语义。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/test_remote_tool_dispatch.py -q 2>&1 | tail -20 || true</automated>
  </verify>
  <acceptance_criteria>
    - 文件可被 pytest 收集（fixtures 完整，0 collection error）。
    - 含四条断言：tools_endpoint 由 FRIDAY_BASE_URL 推导、机会性 PAT 存在→注入、无来源→不注入、dispatch 不读 AccessToken 明文。
    - impl 未落地时这些用例 RED（fail），不污染既有 test_coding_anthropic_base_url_passthrough.py。
  </acceptance_criteria>
  <done>server dispatch 契约 RED 用例就位，钉死 endpoint 推导 + 机会性 PAT + 不读 DB。</done>
</task>

<task type="auto">
  <name>Task 3: runner/internal/docker/executor_test.go 扩展 — RTOOL-03 env 装配 RED</name>
  <files>runner/internal/docker/executor_test.go</files>
  <read_first>
    - runner/internal/docker/executor_test.go:9-51（envMap helper + TestBuildContainerEnvSeparatesTaskModeAndTaskType：TaskPayload 构造 + metadata env_ 透传断言范式——直接复用）
    - runner/internal/docker/executor.go:90-133（buildContainerEnv：现有 FRIDAY_REMOTE_TOOLS 顶层注入 + metadata env_ TrimPrefix 循环；本测试钉死新增 FRIDAY_TASK_REMOTE_TOOLS 与透传键）
    - .planning/phases/11-rtool/11-RESEARCH.md §Common Pitfalls 2（FRIDAY_REMOTE_TOOLS vs FRIDAY_TASK_REMOTE_TOOLS 前缀错位）+ §Dispatch Contract Map step 5
  </read_first>
  <action>
    在 executor_test.go 新增 `TestBuildContainerEnv_RemoteTools`（复用 envMap helper）：
    - 构造 `ws.TaskPayload`，`Payload` 含 `"remote_tools": []any{map[string]any{"name":"a","description":"da","input_schema":map[string]any{}}}` + `"metadata": map[string]any{"env_FRIDAY_TASK_USER_TOKEN":"friday_pat_TESTTOKEN","env_FRIDAY_TASK_TOOLS_ENDPOINT":"https://friday.example.com/api/tools/execute/"}`。
    - 调 `envMap(buildContainerEnv(task, "http://callback", "token"))`。
    - 断言：`env["FRIDAY_TASK_REMOTE_TOOLS"]` 非空且为 remote_tools 的 JSON（含 `"a"`）——钉死 Pitfall 2 的 FRIDAY_TASK_ 前缀修复；`env["FRIDAY_TASK_USER_TOKEN"] == "friday_pat_TESTTOKEN"`、`env["FRIDAY_TASK_TOOLS_ENDPOINT"] == "https://.../api/tools/execute/"`（metadata env_ 透传）。
    - 追加 `test`：当 metadata 无 PAT 键时，env 不含 `FRIDAY_TASK_USER_TOKEN`（向后兼容，无空键注入——既有循环已 `s != ""` 守卫）。
    本 task 仅写测试，不改 executor.go（FRIDAY_TASK_REMOTE_TOOLS 的实现在 11-03 落地，此处 RED）。
  </action>
  <verify>
    <automated>cd runner && go test ./internal/docker/ -run TestBuildContainerEnv_RemoteTools 2>&1 | tail -15 || true</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/docker/ -run TestBuildContainerEnv_RemoteTools` 在 11-03 未落地时 **FAIL**（FRIDAY_TASK_REMOTE_TOOLS 缺失），落地后转 PASS。
    - 既有 `TestBuildContainerEnvSeparatesTaskModeAndTaskType` 不受影响（`go test ./internal/docker/` 仅新测试红）。
    - 断言覆盖 FRIDAY_TASK_REMOTE_TOOLS + FRIDAY_TASK_USER_TOKEN + FRIDAY_TASK_TOOLS_ENDPOINT 三键 + 无 PAT 时不注入空键。
  </acceptance_criteria>
  <done>runner env 装配 RED 用例就位，钉死前缀修复与透传三键。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 测试断言 ↔ 生产契约 | 测试是 PAT 脱敏/不读 DB 等安全不变量的可执行守卫 |
| 测试夹具中的伪 PAT | 测试用 `friday_pat_*` 字面量仅在内存，不落任何持久文件 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Information Disclosure | 测试断言反向锁定「PAT 不进日志/返回文本」 | mitigate | task `test_token_not_in_logs_or_result` 断言捕获日志与返回均无 `friday_pat_` |
| T-11-02 | Tampering | 误从 AccessToken/DB 取明文（违反 PAT-02） | mitigate | server `test_dispatch_never_reads_access_token_plaintext` 守卫 |
| T-11-03 | Spoofing | 吊销/401 被误判致命 | mitigate | task `test_handler_401_returns_tool_error_not_raise` 钉死 graceful |
| T-11-SC | Tampering | 供应链（包安装） | accept | 零新增依赖，纯测试文件（RESEARCH §Package Legitimacy Audit 不适用） |
</threat_model>

<verification>
- `cd task && uv run pytest tests/test_remote_tools.py -q` → 0 errors（impl 前 skip/fail，无 collection error）。
- `cd server && uv run pytest tests/test_remote_tool_dispatch.py -q` → 用例可收集（impl 前 RED）。
- `cd runner && go test ./internal/docker/ -run TestBuildContainerEnv_RemoteTools` → impl 前 FAIL（预期 RED）。
- 既有套件不退：`cd server && uv run pytest tests/test_coding_anthropic_base_url_passthrough.py -q`、`cd task && uv run pytest tests/test_callback.py tests/test_claude_sdk_integration.py -q`、`cd runner && go test ./internal/docker/` 其余用例绿。
</verification>

<success_criteria>
- 三组件各有覆盖 RTOOL-02/03/04 的自动化用例，跨进程契约字段名（USER_TOKEN/REMOTE_TOOLS/TOOLS_ENDPOINT）三侧一致。
- task 用例无 collection error（importorskip 守卫），server/runner 用例呈预期 RED。
- 安全不变量（PAT 不进日志、不读 DB、401 graceful）以测试形式钉死。
</success_criteria>

<output>
Create `.planning/phases/11-rtool/11-01-SUMMARY.md` when done
</output>
