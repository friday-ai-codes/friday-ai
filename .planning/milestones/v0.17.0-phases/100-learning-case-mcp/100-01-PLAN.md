---
phase: 100-learning-case-mcp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/knowledge/models.py
  - server/knowledge/migrations/0008_extend_entity_kind_learning_case.py
  - server/knowledge/sources/__init__.py
  - server/knowledge/vector_recall.py
  - server/tests/knowledge/test_models.py
  - server/tests/knowledge/test_vector_recall.py
autonomous: true
requirements: [KNOW-01, KNOW-02, KNOW-03]

must_haves:
  truths:
    - "generate_entity_id docstring 规则表含 learning_case / mcp_coding_plan / mcp_repository_analysis / mcp_execution_trace 四行，后续所有入图工作按此构造 source_id"
    - "EntityKind.LEARNING_CASE 字面值存在且 DB CHECK 约束（kentity_kind_valid）放行 learning_case"
    - "search_similar(entity_kinds=[\"learning_case\"]) 只返回 learning_case 实体——显式传入 kinds 与白名单交集为空时返回空结果，不回退全量"
    - "不传 entity_kinds 时既有 demand/code 分路行为零回归，且 demand 白名单已纳入 learning_case"
  artifacts:
    - path: "server/knowledge/models.py"
      provides: "EntityKind.LEARNING_CASE + natural key 规则表 4 新行"
      contains: "LEARNING_CASE"
    - path: "server/knowledge/migrations/0008_extend_entity_kind_learning_case.py"
      provides: "RemoveConstraint → AlterField → AddConstraint 枚举扩展 migration（照抄 0007 先例）"
      contains: "kentity_kind_valid"
    - path: "server/knowledge/vector_recall.py"
      provides: "_DEMAND_KINDS 纳入 LEARNING_CASE + 显式 kinds 严格过滤（吞参 bug 修复）"
  key_links:
    - from: "server/knowledge/vector_recall.py"
      to: "EntityKind.LEARNING_CASE"
      via: "_DEMAND_KINDS 白名单"
      pattern: "LEARNING_CASE"
    - from: "server/knowledge/retrieval.py search_similar"
      to: "recall_similar_chunks entity_kinds 参数"
      via: "透传（本 plan 不改 retrieval.py，修复全部在 vector_recall.py 内）"
      pattern: "entity_kinds"
---

<objective>
Phase 100 地基：natural key 规则表扩表（P7 前置，锁定后续全部入图工作的 source_id 构造）、
`EntityKind.LEARNING_CASE` 枚举扩展 + CHECK 约束 migration、`vector_recall.py` kind 过滤
吞参 bug 修复（KNOW-02 的隐藏前置——不修则 `entity_kinds=["learning_case"]` 静默回退全量）。

Purpose: 本 plan 是全里程碑枢纽（STATE 关键约束：「natural key 规则表决策先于一切入图工作」）。
100-02/03/04 全部依赖本 plan 的枚举与检索过滤语义。
Output: 规则表 4 新行 + 枚举字面值 + migration 0008 + 修复后的 kind 过滤 + 专项回归测试。
</objective>

<context>
@.planning/phases/100-learning-case-mcp/100-CONTEXT.md （locked decisions：Natural key 规则表 / EntityKind 扩展 / 检索分路修复 三节）
@.planning/ROADMAP.md （Phase 100 五条 Success Criteria）
@server/knowledge/models.py （L34-50 EntityKind、L92-124 generate_entity_id docstring 规则表、L168-190 Meta 约束）
@server/knowledge/migrations/0007_remove_knowledgeentity_kentity_kind_valid_and_more.py （枚举扩展 migration 逐字先例）
@server/knowledge/vector_recall.py （L27-33 白名单常量、L181-260 recall_similar_chunks、L213-223 吞参 bug 现场）
@server/tests/knowledge/test_vector_recall.py （既有 mock 范式：_hybrid_query / EmbeddingService / SparseEncoderService monkeypatch）
</context>

<tasks>

<task type="auto">
  <name>Task 1: natural key 规则表扩表 + EntityKind.LEARNING_CASE + migration 0008</name>
  <files>server/knowledge/models.py, server/knowledge/migrations/0008_extend_entity_kind_learning_case.py, server/tests/knowledge/test_models.py</files>
  <action>
按 CONTEXT locked decision「Natural key 规则表」与「EntityKind 扩展」两节实现：

1. `server/knowledge/models.py`：
   - `EntityKind` 追加 `LEARNING_CASE = "learning_case", "经验案例"`（字面值 13 字符，CharField max_length=20 足够，不改字段长度）。加注释说明 v0.17.0 Phase 100（KNOW-01）来源，参照 PROJECT/REPOSITORY/SPACE 的 Phase 79 注释风格（中文，说明「新增字面值不改既有 kind，仅扩派生空间与 CHECK 约束，既有 uuid5 PK 零漂移」）。
   - `generate_entity_id` docstring 规则表追加 4 行（列宽对齐既有表）：
     | `learning_case` | McpLearningCase UUID str（kind=learning_case） | 经验案例（Phase 100）|
     | `mcp_coding_plan` | McpCodingPlan UUID str（kind=tech_plan） | MCP 编码方案（Phase 100）|
     | `mcp_repository_analysis` | McpRepositoryAnalysis UUID str（kind=document） | MCP 仓库分析（Phase 100）|
     | `mcp_execution_trace` | McpCodingExecutionTrace UUID str（kind=code_change） | MCP 执行 trace（Phase 100）|
   - docstring 规则表下方追加一行说明（locked decision 存档）：Chat `coding_plan` 与 MCP `mcp_coding_plan` 保持**不同实体 + 边显式关联（RELATES_TO / 共享 work_item 锚）**，不做硬去重；work_item 锚一律沿用 `feishu_work_item` + `{project_key}:{work_item_type_key}:{work_item_id}` 既有行，禁止自造锚格式。
2. 生成 migration：`uv run python manage.py makemigrations knowledge` 后将生成文件重命名/确认为 `0008_extend_entity_kind_learning_case.py`（Django 自动命名可能不同，用 `makemigrations knowledge -n extend_entity_kind_learning_case` 指定）。结构必须与 0007 先例一致：`RemoveConstraint(kentity_kind_valid)` → `AlterField(kind, choices 含 learning_case)` → `AddConstraint(kentity_kind_valid, kind__in 含 learning_case)`。origin 枚举本次不动（MCP origin 已存在），若 makemigrations 未生成 origin 相关操作即为正确。
3. `server/knowledge/sources/__init__.py` 的 `_NORMALIZERS` 一次性登记 4 个新条目（沿用 13-02 先例——「先注册，模块落地前 `get_normalizer` 触发 ImportError 响亮失败，不静默」，见该文件 L22-26 既有注释；此举同时避免 100-02/03 并行修改同一注册表文件）：
   - `"learning_case": "knowledge.sources.learning_case"`（100-02 落地）
   - `"mcp_coding_plan": "knowledge.sources.mcp_coding_plan"`（100-03 落地）
   - `"mcp_repository_analysis": "knowledge.sources.mcp_repository_analysis"`（100-03 落地）
   - `"mcp_execution_trace": "knowledge.sources.mcp_execution_trace"`（100-03 落地）
   附中文注释标注 Phase 100 KNOW-01/03。
4. `server/tests/knowledge/test_models.py` 追加用例：`generate_entity_id("learning_case", "learning_case", uuid_str)` 确定性（同参两次调用同 id）；`KnowledgeEntity.objects.create(kind="learning_case", ...)` 过 CHECK 约束不抛 IntegrityError；非法 kind（如 "bogus"）仍被 `kentity_kind_valid` 拒绝（回归）。沿用该文件既有 fixture/写法。
  </action>
  <verify>
    <automated>cd server && uv run python manage.py makemigrations --check --dry-run && uv run pytest tests/knowledge/test_models.py -x -q</automated>
  </verify>
  <done>EntityKind 含 LEARNING_CASE；规则表 4 新行 + 关联决策存档；migration 0008 三段式结构与 0007 一致；makemigrations --check 无 pending；test_models 全绿含新用例。</done>
</task>

<task type="auto">
  <name>Task 2: vector_recall kind 过滤修复（吞参 bug）+ 专项回归测试</name>
  <files>server/knowledge/vector_recall.py, server/tests/knowledge/test_vector_recall.py</files>
  <action>
按 CONTEXT locked decision「检索分路修复」实现，只改 `vector_recall.py`（`retrieval.py` 的 `search_similar` 是纯透传，不动）：

1. `_DEMAND_KINDS` 追加 `EntityKind.LEARNING_CASE`（元组变为 WORK_ITEM/TECH_PLAN/LEARNING_CASE），注释标注 Phase 100 KNOW-02。
2. 修复吞参行为（L213-223 现场）：当调用方**显式传入** `entity_kinds` 时，各分路取「传入 kinds ∩ 分路白名单」严格过滤——交集为空则**该分路不发 Qdrant 查询**（demand 分路交集空 → 跳过 demand 查询；code 分路交集空 → 跳过 code 查询）；两分路交集皆空 → 直接返回 `[]`（可短路在 embedding 之前，省一次远程调用）。删除 `or demand_allowed` / `or list(_CODE_KINDS)` 的回退写法。`entity_kinds=None`（未显式传入）时保持现状：demand 分路用完整白名单、code 分路用 `_CODE_KINDS`，零回归。
3. 结构化日志：`knowledge_vector_recall_completed` 事件追加 `entity_kinds` 字段（显式传入值或 None），便于排障归因；该事件已存在，勿新增 INFO 刷屏点（本函数属检索内部步骤，维持现有单条 completed 事件，category 按现状不加——该文件尚无 category 字段，保持一致性不在本 plan 引入）。
4. `server/tests/knowledge/test_vector_recall.py` 追加用例（复用既有 `recall_deps` fixture 的 monkeypatch 范式）：
   - `entity_kinds=["learning_case"]` → 仅 1 次 hybrid 调用（demand 分路），filter 中 entity_kind MatchAny 值集合 == {"learning_case"}（用既有 `_demand_entity_kinds` 辅助函数）；
   - `entity_kinds=["nonexistent_kind"]` → 0 次 hybrid 调用、返回 `[]`（不回退全量——这是本次修复的核心断言，修复前该用例必然失败）；
   - `entity_kinds=["code_change"]` → 仅 code 分路 1 次调用（demand 交集空被跳过）；
   - `entity_kinds=None` → demand filter 值集合含 work_item/tech_plan/learning_case、code 分路正常（既有 route quota 用例回归 + 白名单扩容断言）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/knowledge/test_vector_recall.py tests/knowledge/test_delivery_search.py -x -q</automated>
  </verify>
  <done>显式 kinds 严格过滤（交集空 = 空结果零查询）；learning_case 进 demand 白名单；不传 kinds 零回归；test_vector_recall + test_delivery_search 全绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP/API 调用方 → entity_kinds 参数 | 外部可控字符串列表进入 Qdrant filter 构造 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-100-01 | Information Disclosure | recall_similar_chunks kind 过滤 | mitigate | 修复吞参回退：显式 kinds 交集为空必须返回空，杜绝「传未知 kind 反而放大召回面」；project_id/repository_id 权限闸（allowed_* fail-closed）保持不动 |
| T-100-02 | Tampering | migration 0008 | mitigate | 三段式约束重建期间窗口极短且 kind 写入仅经 ingest（应用层枚举校验双保险）；migration 结构逐字照抄 0007 已验证先例 |
| T-100-SC | Tampering | 依赖安装 | accept | 本 plan 零新依赖，无供应链面 |
</threat_model>

<verification>
- `cd server && uv run pytest tests/knowledge/ -x -q` 全绿（knowledge 域整体回归）。
- `uv run python manage.py makemigrations --check` 退出码 0（无未生成 migration）。
- 人工抽查：`generate_entity_id` docstring 规则表含 4 新行且格式与既有行对齐。
</verification>

<success_criteria>
- ROADMAP 成功标准 1 的前置成立：`EntityKind.LEARNING_CASE` 过滤可用且真过滤（criterion 1 的「统一向量排序 + kind 过滤」地基）。
- P7 前置兑现：natural key 规则表先于一切入图工作定版，100-02/03 的 normalizer 按表构造 source_id。
- 修复前必失败、修复后通过的吞参断言用例在（证伪型测试，防假通过）。
</success_criteria>

<output>
完成后创建 `.planning/phases/100-learning-case-mcp/100-01-SUMMARY.md`
</output>
