---
phase: 107-layered-presentation
plan: 05
type: execute
wave: 3
depends_on: [107-01, 107-03]
files_modified:
  - server/codegraph/services/repo_router_v2.py
  - server/tests/codegraph/test_repo_router_v2_degraded.py
autonomous: true
requirements: [RELY-05]
assumptions:
  - "**不改 Stage 1 system prompt**：K 裁剪在**消费侧**做（比让 LLM 自觉遵守可靠），因此 `PROMPT_TEMPLATE_VERSION` 保持不变 → Stage 1 输入哈希缓存 key 不变 → 历史缓存继续命中，无需清理（107-RESEARCH §Runtime State Inventory「缓存」行的硬约束：改 prompt 必须递增该常量）。"
  - "**不重新打开 langchain `max_retries`**：保持 `max_retries=0`，重试写在我们自己的循环里。langchain 内部重试不受我们的总预算约束，且 105-05 刻意关掉它（注释记录了 30s×3≈90s 的旧病）。"
  - "**离线 golden harness 不引入凸组合**：`repo_router_eval.score_case` / `evaluate_cases` 一行不改（它们结构上不跑 Stage 1，α 恒 0）。把凸组合塞进离线 harness 会污染 `phase106-v2` baseline（Pitfall 5 同源）。"
  - "`arecord_llm_usage(run=None, ...)` 是 run 可选的单一 LLM 写入入口（`interactions/ledger.py:458`），Stage 1 无 `InteractionRun` 上下文 → 传 `run=None`；触发用户从 structlog contextvars 取（无则 `system`）。"
must_haves:
  truths:
    - "Stage 1 单次调用有 1 次重试（指数退避）且首调与重试**共享同一个总延迟上界**：总预算耗尽即刻降级，不发第二次调用（可断言调用次数）"
    - "per-attempt 超时取 `min(REPO_ROUTER_STAGE1_TIMEOUT_SECONDS, 剩余预算)`——不存在「首调吃满 per-call 后重试无余量」的结构"
    - "两次都失败 → 降级继续（`degraded=True`、`router_version='v2_stage0_only'`、`degrade_reason` ∈ 6 值闭集），用户不会无限等待"
    - "有界重排：LLM 排列经 K=3 裁剪，**`clamped_order`（裁剪产物）**中每个候选满足 `|rank_clamped - rank_stage0_relative| <= K`（`rank_stage0_relative` = 该 rid 在**被 LLM 返回子集**内的 Stage 0 相对位次——LLM 只返回窗口子集是常态，与全量 8 元窗口下标相减会让位移恒 >K 从而丢弃全部重排）；违规数进快照与日志留痕；LLM 编造的 repo_id 被丢弃（不在 Stage 0 窗口内）；`clamped_order` 的元素集合恒 == LLM 返回的合法子集（不膨胀回全量）"
    - "**K 预算的作用对象是 `clamped_order`，不是最终扁平列表**：最终列表按凸组合后的 `score_ranked` 排序（`_apply_presentation` 承载），凸组合会相对 `clamped_order` 再次重排（相对 Stage 0 的位移上界因此是 2K 而非 K）。最终列表上唯一恒成立的性质是**元素集合 == LLM 返回的合法子集**；顺序性质只在 `clamped_order` / `stage1_meta` 上断言，或在把 fixture 的 Stage 0 分差钉死后再断言最终顺序"
    - "「丢弃式提升」可观测：`stage1_meta` 同时带 `llm_returned_count` 与 `stage0_window_count`——只返回少量候选时 K 预算的字面损害上界会被弱化（少返回 = 子集内位次天然靠前），`rank_budget_violations` 结构上看不到这种提升，靠这两个计数可事后识别"
    - "凸组合结果只写 `score_ranked`：`score` 与 `breakdown` 逐字节不变，`Σbreakdown == score` 两条既有断言继续绿（D-3 硬约束）"
    - "Stage 1 降级时 α 按 0 处理（`score_ranked` 保持 `None`，消费方回退 `score`）；N==1 不除零"
    - "`confidence` 推导仍吃 Stage 0 分数列表，绝不改吃 `score_ranked`——LLM 不得重新变成置信度决策者（RELY-04 不回退）"
    - "Stage 1 每次调用落一行 `ModelUsageRecord`：`call_source=aux_repo_router`、`duration_ms`、`upstream_status_code`（可解析时）、`failure_type`（失败时）、`user_id`（无触发用户记 `system`）——补齐 LOGGING-SPEC §9「新增 LLM 调用点」检查项"
    - "golden 门禁不因本 plan 改动而变：`phase106-v2` baseline 与三规则继续绿（离线 harness 不含 Stage 1）"
  artifacts:
    - path: server/codegraph/services/repo_router_v2.py
      provides: "Stage 1 重试与共享总预算、K 裁剪与凸组合接线、ModelUsageRecord 埋点"
      contains: "budget_deadline"
  key_links:
    - from: server/codegraph/services/repo_router_v2.py
      to: server/codegraph/services/repo_router_ranking.py
      via: "clamp_llm_permutation / blend_ranked_scores 在 parsed 消费循环之后调用（此时白名单过滤与去重已完成）"
      pattern: "clamp_llm_permutation"
    - from: server/codegraph/services/repo_router_v2.py
      to: server/interactions/ledger.py
      via: "arecord_llm_usage(run=None, call_source=CallSource.AUX_REPO_ROUTER, ...) 每次 Stage 1 调用收尾写一行"
      pattern: "arecord_llm_usage"
---

<objective>
Stage 1 从「单层 `asyncio.wait_for` + `max_retries=0` + 无预算」改造为**有界调用**：首调 + 1 次指数退避重试共享一个总延迟 deadline，per-attempt 超时取 `min(per_call, 剩余预算)`，超出即降级继续；LLM 排列经 K=3 rank-swap 预算裁剪后做凸组合，结果**只写旁路字段 `score_ranked`**；顺带补齐 Stage 1 的 `ModelUsageRecord` 埋点（Phase 105 遗留的埋点缺口，LOGGING-SPEC §9 检查项）。

**三条不可越界**：`score`/`breakdown` 不动（D-3）；`confidence` 推导不改吃 `score_ranked`（否则回退 RELY-04）；Stage 1 prompt 不改（否则必须 bump `PROMPT_TEMPLATE_VERSION` 并丢弃全部历史缓存）。

Purpose: SC-5 前半句「Stage 1 单次调用有重试与延迟上界，超出即降级继续，用户不会无限等待」+ RELY-05 的有界重排损害上限。
Output: router 单文件改造 + 降级测试文件补用例。
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/107-layered-presentation/107-CONTEXT.md
@.planning/phases/107-layered-presentation/107-RESEARCH.md
@.planning/research/ROUTING-RANKING.md
@server/codegraph/services/repo_router_ranking.py
@server/codegraph/services/repo_router_v2.py
</context>

## Artifacts this phase produces（本 plan 新建符号）

| Symbol | Kind | Location |
|--------|------|----------|
| `budget_deadline`（Stage 1 总预算截止时刻，`time.monotonic()` 基准） | 内部变量/语义 | `server/codegraph/services/repo_router_v2.py` |
| `stage1_meta["rank_budget_violations"]` | 快照键（K 裁剪违规数留痕） | 同上 |
| `stage1_meta["clamped_order"]` | 快照键（裁剪产物的 repo_id 序列；K 预算后置条件的**唯一可断言对象**——最终扁平列表已被凸组合重排） | 同上 |
| `stage1_meta["llm_returned_count"]` / `stage1_meta["stage0_window_count"]` | 快照键（「丢弃式提升」可观测：LLM 少返回候选即可绕过 K 的字面损害上界，`rank_budget_violations` 看不到这一点） | 同上 |
| `stage1_meta["alpha"]` / `stage1_meta["rank_budget_k"]` | 快照键（本次生效参数，回放自包含） | 同上 |
| `stage1_meta["attempts"]` | 快照键（实际调用次数：1 或 2） | 同上 |
| `repo_router_v2_stage1_retry`（structlog 事件名，`category="sampling"`, `component="repo_router_v2"`） | 事件名 | 同上 |
| `repo_router_v2_stage1_budget_exhausted`（structlog 事件名，同上分类） | 事件名 | 同上 |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 1 次重试 + 共享总预算 deadline</name>
  <files>server/codegraph/services/repo_router_v2.py, server/tests/codegraph/test_repo_router_v2_degraded.py</files>
  <read_first>
    - server/codegraph/services/repo_router_v2.py:991-1080（`_stage1_llm_reasoning` 开头：provider 解析 / model 名 / `max_retries=0` 的构造与其注释 / `timeout_seconds` 取值）
    - server/codegraph/services/repo_router_v2.py:1150-1200（`if not cache_hit:` 块与 `asyncio.wait_for(model.ainvoke(...), timeout=timeout_seconds)` 的确切位置、`use_call_source` 上下文、`repo_router_v2_stage1_completed` 日志与其 `duration_ms`）
    - server/codegraph/services/repo_router_v2.py:100-120（`_stage1_conf` 读取 helper——总预算与退避基数同款读取）
    - .planning/phases/107-layered-presentation/107-RESEARCH.md §5(a)（推荐结构：`budget_deadline` + `for attempt in range(2)` + `min(per_call, remaining)` + 退避睡眠不超剩余预算；以及「参数拆分是硬约束」的理由）
    - .planning/phases/107-layered-presentation/107-RESEARCH.md Pitfall 说明与 §Don't Hand-Roll「LLM 重试」行（为何不重新打开 langchain `max_retries`）
  </read_first>
  <behavior>
    - 首调抛 `asyncio.TimeoutError`、第二次成功 → 结果为正常 v2（`degraded is False`），替身被调用 2 次，`stage1_meta["attempts"] == 2`
    - 两次都抛 → 降级（`degraded is True`、`router_version == "v2_stage0_only"`、`degrade_reason == "timeout"`），替身被调用 2 次
    - 总预算已耗尽（`REPO_ROUTER_STAGE1_TOTAL_BUDGET_SECONDS` override 为极小值如 0.01，首调即超）→ **不发第二次调用**（替身调用次数 == 1），日志出现 `repo_router_v2_stage1_budget_exhausted`
    - per-attempt 超时 = `min(per_call, remaining)`：override per_call=1000、总预算=2 → 传给 `wait_for` 的 timeout <= 2（用替身捕获 timeout 实参断言）
    - 退避睡眠不超过剩余预算：override 退避基数=1000、总预算=1 → 重试前的 sleep 实参 <= 1（替身捕获）
    - 缓存命中路径不受影响：命中时零调用、零重试、`attempts` 不出现或为 0
    - `max_retries=0` 仍在（langchain 内部重试未被打开）
  </behavior>
  <action>
    1. `_stage1_conf` 侧新增读取 `REPO_ROUTER_STAGE1_TOTAL_BUDGET_SECONDS`（默认 120.0）与 `REPO_ROUTER_STAGE1_RETRY_BACKOFF_SECONDS`（默认 2.0）；非法/非正值回退默认（不抛）。
    2. 在 `if not cache_hit:` 块内把单次 `asyncio.wait_for(...)` 替换为有界循环（结构照 107-RESEARCH §5(a)）：
       - `budget_deadline = time.monotonic() + total_budget_seconds`（**在循环外取一次**，首调与重试共享）；
       - `for attempt in range(2)`：`remaining = budget_deadline - time.monotonic()`；`remaining <= 0` → 记 `repo_router_v2_stage1_budget_exhausted`（`category="sampling"`, `component="repo_router_v2"`, kv 含 `attempt` / `total_budget_seconds`）后 `raise TimeoutError("stage1_budget_exhausted")`；
       - 调用用 `timeout=min(per_call_timeout, remaining)`，保留既有 `use_call_source(CallSource.AUX_REPO_ROUTER)` 上下文；
       - 捕获 `asyncio.TimeoutError` / `TimeoutError` 与上游连接/HTTP 类异常：`attempt == 1` 时 `raise`；否则记 `repo_router_v2_stage1_retry`（kv 含 `attempt`、`error_type`、`error=redact_secrets_in_text(str(exc))`、`remaining_seconds`）并 `await asyncio.sleep(min(backoff_base, max(0.0, budget_deadline - time.monotonic())))` 后继续。
       - **异常捕获范围保持宽口径**（`except Exception`）以维持既有「Stage 1 任意失败都降级」语义，但重试只对可重试类（超时/连接/上游 5xx-ish）生效；不可重试类（如解析错误、provider 缺失）直接抛出不浪费预算——判定用异常类型名子串（与 `classify_degrade_reason` 同口径），实现处加注释说明分类依据。
    3. `stage1_meta` 追加 `attempts`（实际调用次数）与 `total_budget_seconds`，进快照供回放/排障。
    4. `max_retries=0` 的构造与其注释保持不动，并在新循环旁加注释：重试写在我们的循环里以受总预算约束，langchain 内部重试**不受**该约束（105-05 刻意关闭）。
    5. `test_repo_router_v2_degraded.py` 补用例覆盖 `<behavior>` 全部 7 条；替身需能捕获 `wait_for` 的 timeout 实参与 `asyncio.sleep` 实参（monkeypatch 记录调用参数）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/codegraph/test_repo_router_v2_degraded.py -x -q -k "retry or budget"</automated>
  </verify>
  <acceptance_criteria>
    - `<behavior>` 7 条全绿；`-k retry` 与 `-k budget` 两个筛选各自能命中用例（与 107-VALIDATION 命令口径一致）
    - 重试次数硬上界为 1：`test "$(rg -c 'range\(2\)' server/codegraph/services/repo_router_v2.py)" != "0"`，且测试断言最大调用次数 == 2
    - langchain 重试未被打开：`test "$(rg -c 'max_retries=0' server/codegraph/services/repo_router_v2.py)" != "0"` 且 `test "$(rg -v '^[[:space:]]*#' server/codegraph/services/repo_router_v2.py | rg -c 'max_retries=[1-9]' || echo 0)" = "0"`（**必须滤注释行**：`repo_router_v2.py:1054` 的既有注释含 `max_retries=2`，且 action 步骤 4 要求该注释保持不动 → 不滤必红）
    - prompt 未改 → 缓存 key 不变：`cd server && git diff -- codegraph/services/repo_router_v2.py | rg -c '^\+.*PROMPT_TEMPLATE_VERSION' || true` 输出 0（该常量未出现在新增行）
    - 定向套全绿（**收窄到本 plan `files_modified` 覆盖面**，不跑整个 `tests/codegraph`）：`cd server && uv run pytest tests/codegraph/test_repo_router_v2_degraded.py tests/codegraph/test_repo_router_v2_meta.py tests/codegraph/test_repo_router_golden.py tests/codegraph/test_repo_router_replay.py tests/codegraph/test_repo_router_stage1_idempotency.py -q`
  </acceptance_criteria>
  <done>Stage 1 延迟有硬上界，重试有硬次数上界，超出即降级继续——用户不会无限等待（T-107-07 自伤 DoS 同时被挡）。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: K=3 rank-swap 裁剪 + 凸组合写 score_ranked</name>
  <files>server/codegraph/services/repo_router_v2.py, server/tests/codegraph/test_repo_router_v2_degraded.py</files>
  <read_first>
    - server/codegraph/services/repo_router_v2.py:1200-1270（`parsed` 消费循环：`by_id` / `rank_by_id` / `seen` 白名单过滤与去重、`score=float(base["score"])`（Stage 0 分原样）、`_deterministic_confidence(sorted_scores, rank)` 与 `apply_llm_adjustment` 的调用点——裁剪与凸组合必须落在这段之后）
    - server/codegraph/services/repo_router_ranking.py（`clamp_llm_permutation` / `blend_ranked_scores` 的签名、后置条件与 violations 语义）
    - server/codegraph/services/repo_router_v2.py:138-170（`RepoRouteCandidateV2.score_ranked` 字段与其 D-3 注释，107-03 已落）
    - .planning/research/ROUTING-RANKING.md §1.3(b)(c)（rank-swap budget 与凸组合定义）与 §6.2 第 4 条（稳定 tie-breaking）
    - .planning/phases/107-layered-presentation/107-RESEARCH.md §5(c)(d)(e)(f)（`S_ranked` 不得覆盖 `score` 的三重硬证据 / confidence 不得改吃 `S_ranked` / N==1 除零 / golden 门禁不受影响的条件）
  </read_first>
  <behavior>
    - LLM 返回**全部 8 个**且把 Stage 0 第 8 位提到第 1 位（K=3）→ **`stage1_meta["clamped_order"]`** 中该候选下标 >= `7-3`（0-based 子集相对位次 7，裁剪后不早于 4）；`stage1_meta["rank_budget_violations"] >= 1`。🔴 **断言落在 `clamped_order` 上，不落在最终扁平列表上**：最终列表按凸组合后的 `score_ranked` 排序（`_apply_presentation`），凸组合会相对 `clamped_order` 再次重排，最终列表相对 Stage 0 的位移上界是 2K 而非 K
    - LLM 排列 == Stage 0 排列 → `violations == 0` 且 `clamped_order == stage0_order`（同上：断言 `clamped_order`，不断言最终顺序）
    - **子集常态（BLOCKER 修复的回归点）**：Stage 0 窗口 8 个、LLM 只返回其中第 6/7/8 位共 3 个 → (a) `stage1_meta["clamped_order"] == ["r6","r7","r8"] 的 LLM 给定顺序`（长度恰 3，**不膨胀回 8、不出现无对应候选的 id**，LLM 重排未被整体丢弃）；(b) `rank_budget_violations == 0`（不因「子集下标 vs 全量下标」相减而虚高）；(c) `stage1_meta["llm_returned_count"] == 3`、`stage1_meta["stage0_window_count"] == 8`；(d) **最终扁平列表的元素集合恒 == 这 3 个 id**（集合恒等式，不断言顺序——顺序由凸组合决定）
    - **最终顺序只在钉死分差后断言**：单独一条用例把 fixture 的 Stage 0 分数钉死（如 `{r1:0.90, r2:0.50, r3:0.40}`、`llm_order=[r3,r2,r1]`、α=0.35 → `score_ranked` 为 r3 0.610 / r1 0.585 / r2 0.500 → 最终 `[r3, r1, r2]`），断言这个**算出来的**顺序，而**不是**断言「最终顺序 == LLM 顺序」（凸组合会重排，该性质不成立）
    - LLM 返回 Stage 0 窗口外的 repo_id（编造）→ 该 id 被丢弃、不进 `clamped_order`、不出现在结果候选中
    - 凸组合生效：α=0.35 下某候选 `score_ranked == 0.65*score + 0.35*(1 - idx/(n-1))`（浮点 1e-9 容差）
    - `score` 与 `breakdown` 不变：同一 Stage 0 输入下，开启/关闭 Stage 1 两次调用中同一候选的 `score` 与 `breakdown` 逐键相等；`fsum(breakdown.values()) == score`（1e-9）
    - 单候选（N==1）→ 不抛 `ZeroDivisionError`，`score_ranked` 为 `None` 或等于 `score`
    - Stage 1 降级（`degraded is True`）→ 全部候选 `score_ranked is None`（α 按 0 处理）
    - `confidence` 不受凸组合影响：构造「凸组合把第 2 位提到第 1 位」的输入，断言各候选 `confidence` 与关闭凸组合（α=0）时相同
    - 结果扁平列表按 `_rank_value`（107-03 的唯一所有者：`score_ranked ?? score`）降序，tie 时按 `repo_id` 升序（稳定 tie-break）——**该排序由 107-03 的 `_apply_presentation` 唯一承载**，本 plan 只断言写上 `score_ranked` 后该顺序确实生效，不新增排序点也不新增等价取值表达式
  </behavior>
  <action>
    1. 在 `parsed` 消费循环**之后**（`by_id` / `rank_by_id` / `seen` 已就绪、白名单过滤与去重已完成）插入两步：
       - `clamped_order, violations = clamp_llm_permutation(llm_order, stage0_order, k=k)`，其中 `stage0_order` 取送进 prompt 的候选顺序（`stage0_candidates[:REPO_ROUTER_STAGE1_MAX_CANDIDATES]` 的 repo_id 序列，默认 8 元），`llm_order` 取解析后经白名单过滤的顺序。
         🔴 **`llm_order` 常态是 `stage0_order` 的真子集**（`:1206-1256` 的最终候选只由 `parsed` 构造，未被 LLM 返回的 Stage 0 仓整体丢弃）。107-01 的 `clamp_llm_permutation` 已把 base rank 定义为「该 rid 在被返回子集内的 Stage 0 相对位次」，两侧同集合同长度 —— 本 plan 只需**原样传全量 `stage0_order`**（函数内部自行取子集相对位次），**不得**在调用侧自己拿 `llm_order` 下标去减全量下标，也不得在拿到 `clamped_order` 后按全量 `stage0_order` 补齐元素（补齐会引入没有对应候选的 repo_id）。测试必须覆盖「LLM 只返回 Stage 0 窗口末几位」这一常态，断言 `clamped_order` 长度 == LLM 返回数且 `rank_budget_violations` 不因子集而虚高。
       - `ranked = blend_ranked_scores({rid: stage0_score}, clamped_order, alpha=alpha)`，把结果写进各候选的 **`score_ranked`**。
       `k` / `alpha` 取自 107-03 的 `_ranking_conf()`（已 clamp）。
    2. 🔴 **绝不**把 `ranked` 写进 `score` 或 `breakdown`——在写入处加注释引用三重硬证据（`test_repo_router_v2_meta.py:248`/`:462` 的 fsum 断言 + `RoutingDecisionPanel.vue` 的 1e-6 容差校验 + ROUTE-07 的 INV-R3 承诺）。
    3. `_deterministic_confidence` 的入参**保持** Stage 0 分数列表（`sorted_scores = [float(c["score"]) for c in stage0_candidates]`）不变，并在旁加注释：若改吃 `score_ranked`，LLM 就重新变成置信度决策者，直接违背 RELY-04（Phase 105 刚修完的死锁根因）。
    4. **排序点与比较值表达式都不新增**：107-03 已落模块级 `_rank_value(candidate)`（`score_ranked` 非 `None` 则取它，否则回退 `score`）作为**唯一所有者**，`_apply_presentation` 的比较键是 `(-round(_rank_value(c), 6), c.repository_id)`（先量化再比较；第二键用不可变 id，不用 name/path——§6.2 第 4 条）。本 plan 只负责把 `score_ranked` **写上**，**不在 `repo_router_v2.py` 里新增任何排序调用，也不新写一遍 `score_ranked if ... is not None else score`**——同一语义在两处声明必然「改一处漏一处」（WARNING 3）。若确认 107-03 落的 helper 与此不一致，按「修 `_rank_value` / `_apply_presentation`」处理并在 SUMMARY 记录，不得在本 plan 另起一个取值或排序点。
    5. 降级路径（`_stage0_only_result` / `_fallback_v1`）不设 `score_ranked`（保持 `None` = α 按 0），并在 107-03 落的 `_apply_presentation` 里天然回退 `score` 排序（`_rank_value` 承载该回退）。
    6. `stage1_meta` 追加六键（进快照，回放自包含 + 约束违反必须留痕）：
       - `rank_budget_violations` / `alpha` / `rank_budget_k`；
       - `clamped_order`（裁剪产物的 repo_id 序列）—— **K 预算后置条件的唯一可断言对象**：最终扁平列表已被凸组合按 `score_ranked` 重排，在最终列表上断言「位移 <= K」不严谨（凸组合后相对 Stage 0 的位移上界是 2K）；
       - `llm_returned_count`（= `len(clamped_order)`）与 `stage0_window_count`（= `len(stage0_order)`）—— **让「丢弃式提升」可观测**（WARNING 1）：base rank 改为「子集内相对位次」后，LLM 靠**少返回候选**实现的提升不再受 K 约束（极端情形：只返回 Stage 0 第 8 位这一个仓，它以 `violations == 0` 被提到首位），`rank_budget_violations` 结构上看不到这条路径；两个计数落进快照后，「返回数远小于窗口数且违规为 0」这种模式可事后识别与告警。
       violations > 0 时记一条 `logger.info("repo_router_v2_stage1_rank_budget_clamped", ...)`（`category="sampling"`, `component="repo_router_v2"`，kv 含 `violations` / `k` / `llm_returned_count` / `stage0_window_count`）。
    7. **离线 harness 一行不改**：不得把凸组合塞进 `repo_router_eval.py`（会污染 `phase106-v2` baseline）。
    8. `test_repo_router_v2_degraded.py` 补用例覆盖 `<behavior>` 全部 11 条；「`score`/`breakdown` 不变」「confidence 不受影响」「子集常态」「钉死分差后的最终顺序」四条必须独立命名（机制断言，门禁可定位）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/codegraph/test_repo_router_v2_degraded.py tests/codegraph/test_repo_router_v2_meta.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `<behavior>` 11 条全绿；「子集常态」用例独立命名（`-k subset` 可定位）
    - K 预算的断言对象正确（WARNING 2）：测试对「位移 <= K」的断言全部读 `stage1_meta["clamped_order"]`，**不读**最终扁平列表顺序 —— `test "$(rg -c 'clamped_order' server/tests/codegraph/test_repo_router_v2_degraded.py)" != "0"`；最终列表上只断言元素集合恒等（另有一条钉死 Stage 0 分差后断言最终顺序的独立用例）
    - 「丢弃式提升」可观测（WARNING 1）：`test "$(rg -c 'llm_returned_count' server/codegraph/services/repo_router_v2.py)" != "0"` 且 `test "$(rg -c 'stage0_window_count' server/codegraph/services/repo_router_v2.py)" != "0"`；测试断言「LLM 只返回 1 个候选」时 `llm_returned_count == 1` / `stage0_window_count == 8` / `rank_budget_violations == 0`（即该提升路径确实绕过了 K 且被两个计数记录下来）
    - `score_ranked` 未覆盖 `score`：`test "$(rg -c 'score = .*blend_ranked|\.score = ranked' server/codegraph/services/repo_router_v2.py || echo 0)" = "0"`，且两条既有 fsum 断言绿
    - `breakdown` 未被写入 α 项：`test "$(rg -n 'breakdown\[' server/codegraph/services/repo_router_v2.py | rg -c 'llm|alpha|ranked' || echo 0)" = "0"`
    - 离线 harness 未被改：`cd server && git diff --name-only | rg -q 'repo_router_eval\.py' && exit 1 || true`
    - golden 门禁绿且 baseline 未变：`cd server && uv run pytest tests/codegraph/test_repo_router_golden.py -q` 全绿 + `git diff --name-only | rg -q 'golden_baseline\.json' && exit 1 || true`
    - 比较值所有者唯一（WARNING 3）：107-03 落的 `_rank_value` **定义**恰一处，本 plan 未新增第二个 —— `test "$(rg -c '^def _rank_value' server/codegraph/services/repo_router_v2.py)" = "1"`，且全文件没有绕过它的内联等价回退表达式 —— `test "$(rg -v '^[[:space:]]*#' server/codegraph/services/repo_router_v2.py | rg -c 'score_ranked is not None else .*score')" = "1"`（唯一一次出现即 `_rank_value` 函数体本身；`to_dict()` 的 else 分支回退 `None` 不会误命中）。**注意不要断言 `_rank_value` 的调用次数为 1**：`_apply_presentation` 至少三处要用它（组内排序截断 / 并集后全局排序 / 取两组首位喂 `decide_block_order`），唯一性约束落在**定义**上而不是调用上
    - 本 plan 未新增排序调用：`cd server && git diff -- codegraph/services/repo_router_v2.py | rg '^\+' | rg -c 'sorted\(|\.sort\(' || true` 输出 0；测试含 tie 场景断言按 `repo_id` 升序（由 `_apply_presentation` 承载）
  </acceptance_criteria>
  <done>LLM 重排损害有硬上界且可测；凸组合走旁路字段，可拆解不变量与前端渲染约定均未破。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Stage 1 ModelUsageRecord 埋点（LOGGING-SPEC §9 检查项）</name>
  <files>server/codegraph/services/repo_router_v2.py, server/tests/codegraph/test_repo_router_v2_degraded.py</files>
  <read_first>
    - server/interactions/ledger.py:458-500（`arecord_llm_usage`：`run` 可选的单一 LLM 写入入口，签名含 `call_source` / `duration_ms` / `ttft_ms` / `upstream_status_code` / `failure_type` / `user_id` / `source`；best-effort 返回 None 不反噬）
    - server/interactions/models.py:240-305（`ModelUsageRecord` 字段与索引：`call_source` db_index、`Index(call_source, -created_at)`——分位/TPS 聚合依赖它）
    - server/agents/call_source.py（`CallSource.AUX_REPO_ROUTER` 枚举值字面量；contextvar 传播说明）
    - server/agents/langchain_runner.py:540-560（既有埋点调用范式：`ttft_ms` / `upstream_status_code` 的取值方式与 `try/except` 包裹）
    - .planning/observability/LOGGING-SPEC.md §4.1 与 §9（call_source 枚举 + 「新增 LLM 调用点：赋 call_source，上报请求数/token/TTFT/上游错误码」检查项）
    - .planning/phases/107-layered-presentation/107-RESEARCH.md §9（Stage 1 当前不落 ModelUsageRecord 的确切原因：直调 `build_chat_model(...).ainvoke(...)` 不经 `record_model_usage` chokepoint）
  </read_first>
  <behavior>
    - Stage 1 成功一次 → 落 1 行 `ModelUsageRecord`，`call_source == CallSource.AUX_REPO_ROUTER`（字面值）、`duration_ms` 为正、`failure_type == ""`、`model` 非空
    - Stage 1 失败（超时）→ 仍落 1 行，`failure_type` 非空（如 `timeout`），`duration_ms` 为正
    - 重试 2 次（首调失败 + 重试成功）→ 落 **2 行**（每次上游调用各一行，符合「请求数」口径）
    - 缓存命中 → **零行**（未发上游调用；口径与 `repo_router_v2_stage1_completed` 一致）
    - 触发用户：contextvars 有 `user_id` 时取该值；无则 `"system"`
    - token 未知时 → `prompt_tokens/completion_tokens/total_tokens` 为 0（不猜）
    - 埋点写库抛异常 → Stage 1 主流程不受影响（结果仍正常返回；观测 best-effort）
  </behavior>
  <action>
    1. 在有界循环的每次调用收尾（成功与失败两条路径）调 `await arecord_llm_usage(run=None, call_source=CallSource.AUX_REPO_ROUTER, provider=<已解析 provider 名>, model=<已解析 model 名>, duration_ms=int(...), upstream_status_code=<可从异常解析则给，否则 None>, failure_type=<成功为 ""，失败取 classify 口径的短标签>, user_id=<contextvars 的 user_id 或 "system">, source="scheduler" 或既有 source 语义留空)`。
       - 触发用户取法：`structlog.contextvars.get_contextvars().get("user_id") or "system"`（无 getter helper，按 contextvars 直读；取值失败一律 `"system"`）；
       - token 字段能从 response 的 usage metadata 取到就填，取不到填 0（**不猜**）；
       - `ttft_ms` 非流式调用 → 传 `None`（不伪造）。
    2. 整个埋点块 `try/except Exception: pass` 包裹（观测 best-effort，绝不反噬路由主流程——与 `_load_repo_meta` 的既成纪律一致）。
    3. 在埋点处加注释写明口径：**每次上游调用一行**（重试算两次请求），缓存命中零行；并说明 `SystemLogEntry.payload.duration_ms`（107-02 的 O-6 数据源）与本表是两个口径，前者是事件日志、后者可用 `system/metrics_query.py` 既有分位聚合。
    4. `test_repo_router_v2_degraded.py` 补用例覆盖 `<behavior>` 全部 7 条（`pytest.mark.django_db` 断言 `ModelUsageRecord.objects.count()` 与字段值）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/codegraph/test_repo_router_v2_degraded.py -x -q -k "usage or ledger"</automated>
  </verify>
  <acceptance_criteria>
    - `<behavior>` 7 条全绿；`-k usage` 能命中用例
    - `call_source` 用枚举而非字面串：`test "$(rg -c 'CallSource.AUX_REPO_ROUTER' server/codegraph/services/repo_router_v2.py)" != "0"`
    - 埋点 best-effort：测试注入 `arecord_llm_usage` 抛异常，断言 `route()` 仍返回正常结果
    - 不伪造指标：`test "$(rg -n 'arecord_llm_usage' -A 14 server/codegraph/services/repo_router_v2.py | rg -v '^[0-9]+[-:][[:space:]]*#' | rg -c 'ttft_ms=[0-9]' || echo 0)" = "0"`（非流式不得填假 TTFT；**窗口输出必须滤注释行**——action 步骤 1/3 要求写「不伪造 TTFT」口径注释，注释里出现 `ttft_ms=0` 会让断言恒假。`-A` 输出的行前缀是 `NNN:` / `NNN-`，故过滤模式为 `^[0-9]+[-:][[:space:]]*#`）
    - 定向套全绿：`cd server && uv run pytest tests/codegraph/test_repo_router_v2_degraded.py tests/test_model_usage_call_source.py tests/test_interactions_ledger.py -q` + `uv run ruff check codegraph/services/repo_router_v2.py tests/codegraph/test_repo_router_v2_degraded.py`
  </acceptance_criteria>
  <done>Stage 1 的请求数/延迟/上游错误码进入 `ModelUsageRecord`，可用既有分位聚合持续观测（LOGGING-SPEC §9 检查项补齐）。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 上游 LLM 网关 → Stage 1 重试循环 | 上游慢/抖动会被重试放大成对上游与自身的压力 |
| LLM 输出排列 → 最终排序 | LLM 可幻觉出窗口外仓库或做大幅位移 |
| 上游异常 → 埋点 / 日志 | 异常文本可能含凭证 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-107-07 | Denial of Service | Stage 1 重试循环 | mitigate | 重试次数硬上界 1（`range(2)`）+ 首调与重试共享 `budget_deadline` + 指数退避且睡眠不超剩余预算；langchain 内部重试保持关闭（不受我们预算约束） |
| T-107-05 | Tampering | K / α 极端值 | mitigate | 取值经 107-01 的 `clamp_ranking_params`（`α ∈ [0,1]`、`k >= 0`）；α=0 时凸组合恒等返回 S_final（排序退化为 Stage 0，安全侧） |
| T-107-02 | Information Disclosure | 重试日志 / 埋点 failure_type | mitigate | 日志 `error=redact_secrets_in_text(str(exc))`；`failure_type` 用短标签枚举而非异常消息；`upstream_status_code` 只取整数码，不取 body |
| T-107-10 | Tampering | LLM 幻觉 repo_id / 大幅位移 | mitigate | 白名单过滤（Stage 0 窗口外一律丢弃）+ K=3 位移预算裁剪（base 为子集内相对位次）+ violations 留痕 + 后置条件单测（107-01 已做「子集 × 排列」穷举） |
| T-107-10b | Tampering | LLM 用「少返回候选」绕过 K 的字面损害上界（丢弃式提升） | accept（可观测缓解） | base rank 改为子集内相对位次是**必须的**（否则常态下重排被整体丢弃），代价是该路径不受 K 约束。缓解：`stage1_meta` 落 `llm_returned_count` / `stage0_window_count`，「返回数远小于窗口数且 violations==0」事后可识别可告警；边界与代价如实记入 107-VALIDATION 的 Explicit Scope Boundaries |
| T-107-SC | Tampering | npm/pip/cargo installs | accept | 零新增依赖 |
</threat_model>

<verification>
定向口径（本 plan 只改 `repo_router_v2.py` 与其降级测试；**刻意不跑 `tests/services` / `tests/agents` 宽口径套**——那些是 107-07 的 `files_modified` 覆盖面，交叉跑只会引入无关噪声与假红）：

- `cd server && uv run pytest tests/codegraph/test_repo_router_v2_degraded.py tests/codegraph/test_repo_router_v2_meta.py tests/codegraph/test_repo_router_golden.py tests/codegraph/test_repo_router_replay.py tests/codegraph/test_repo_router_stage1_idempotency.py -q` 全绿
- `cd server && uv run pytest tests/test_model_usage_call_source.py tests/test_interactions_ledger.py -q` 全绿（Stage 1 埋点新增写入点）
- `cd server && uv run ruff check codegraph/services/repo_router_v2.py tests/codegraph/test_repo_router_v2_degraded.py`
- baseline 与离线 harness 未变：`cd server && git diff --name-only | rg -q 'golden_baseline\.json|repo_router_eval\.py' && exit 1 || true`

## 归零断言纪律（本 phase 统一口径，写给执行者）

1. 任何 `rg -c ... = "0"` 归零断言，若其模式串**同时出现在本 task 要求写的注释里**（或文件里既有注释中）→ **必须**先滤注释行：`rg -v '^[[:space:]]*#' <file> | rg -c 'PATTERN' || echo 0`。`.vue` 用 `rg -v '^[[:space:]]*(//|<!--)'`。`rg -c` 计的是行、不区分代码与注释，不滤即自引入必红。
2. 任何 `-A N` 窗口归零断言，若窗口**可能越过被断言的代码构造**（函数/分支结束后的相邻代码或 docstring）→ 改为对**构造本身**的精确断言，或在窗口输出上滤 `^[0-9]+[-:][[:space:]]*#`（`-A` 输出的行前缀是 `NNN:` / `NNN-`）。
</verification>

<success_criteria>
- Stage 1 有 1 次重试且首调与重试共享总延迟上界；预算耗尽不发第二次调用（可断言）
- K=3 裁剪后置条件在 `stage1_meta["clamped_order"]` 上成立、违规数留痕；`llm_returned_count` / `stage0_window_count` 让「丢弃式提升」可观测；LLM 编造 id 被丢弃
- 凸组合只写 `score_ranked`；`score`/`breakdown`/`confidence`/golden baseline 四者零变化
- Stage 1 每次上游调用落一行 `ModelUsageRecord`（缓存命中零行），埋点 best-effort
</success_criteria>

<output>
完成后创建 `.planning/phases/107-layered-presentation/107-05-SUMMARY.md`。
</output>
