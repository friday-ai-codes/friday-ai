---
phase: 107-layered-presentation
plan: 04
type: execute
wave: 2
depends_on: [107-01]
files_modified:
  - server/delivery/services/event_taxonomy.py
  - server/tests/services/test_event_taxonomy_alignment.py
  - server/workflows/nodes/ai/plan_research.py
  - server/tests/workflows/test_plan_research_node.py
autonomous: true
requirements: [RELY-02]
assumptions:
  - "A3：**不新增 `ConvergenceSessionStatus` 枚举值**。CONTEXT 写的 `resumed_with_assumptions` / `failed_no_answer` 是语义名而非 DB 值；新增枚举值要迁移且会撞 `lifecycle_projection` / `chat` / `orchestration` 多处 status 分支（30 个文件引用 `waiting_clarification`）。送达失败标记复用 `Clarification.container_status` 的**新取值** `delivery_failed`（零迁移列变更），出口状态转移走既有 `transition` 事件（107-06）。"
  - "A4：新增两个事件名占用 `clarification.*` 命名空间（`clarification.timed_out` / `clarification.delivery_failed`）。若 v0.20.0 蓝图侧已规划同名，需二次改名——落地前用 grep 确认 main 分支蓝图 DESIGN.md 未占用该命名空间。"
  - "D-4 的实现取径为 OQ-2 推荐的 (a)：`WorkflowEventSubscription.timeout_at` 从代码写死的 `now+60min` 改为读同一个 `CLARIFICATION_TIMEOUT_HOURS`，让工作流侧与会话侧**同时**到期，出口动作由澄清扫描 job（107-06）统一驱动。生产中已存在的活跃订阅行携带旧 `timeout_at`（60min），**不会自动更新**——只影响新建订阅，可接受（107-06 的扫描器另有「workflow 已 TIMEOUT + 会话仍 waiting_clarification 立即出口」的纵深条件兜住存量行）。"
  - "`delivery_failed` 轮**仍算 pending**（判定统一走 `ClarificationService.ahas_pending`，按 `answered_at__isnull`，不按 `container_status` 过滤——旧行的 `container_status` 可能为 NULL，按它过滤会漏掉全部历史行）；扫描侧对该标记用「立即出口」而非等满 24h（107-06 实现）。"
must_haves:
  truths:
    - "澄清送达的 5 条失败路径（`no_questions` / `no_space` / `no_project` / `no_chat_id` / `send_failed`）全部留痕：各记一条 warning 级结构化日志 + emit `clarification.delivery_failed` 事件 + 把该轮 `container_status` 标为 `delivery_failed`——不存在任何静默 `return`（这是「无声卡死」的确切成因之一）"
    - "`clarification.timed_out` 与 `clarification.delivery_failed` 两个事件常量已加入 `ALL_EVENTS`，且**两个 producer 已登记进守护测试的硬编码 `_EVENT_PRODUCERS` 与 `_EMIT_FILES`**（`plan_research.py` / `expire_pending_clarifications.py`）；taxonomy 双向守护测试（引用值 ∈ ALL_EVENTS + 每个成员至少被一个 emit 点产出）继续绿——emit 点未落地的事件走带 plan 标注的显式待落地豁免（`_PENDING_PRODUCER_EVENTS`），而非削弱 `assert producer is not None`"
    - "工作流侧订阅超时与澄清超时读同一配置 `CLARIFICATION_TIMEOUT_HOURS`（D-4）：不再存在「工作流已判 TIMEOUT 而会话仍 waiting_clarification」的 23 小时矛盾态窗口"
    - "发卡仍是 best-effort：留痕/事件失败不反噬挂起，节点行为（返回 `waiting_event` + suspension）逐字不变"
    - "送达失败事件的 `reason` 是受控枚举 5 值，payload 不含飞书原始响应体或凭证（正文经既有 `redact_secrets_in_text`，事件 payload 入库另经 `redact_for_ledger`）"
  artifacts:
    - path: server/delivery/services/event_taxonomy.py
      provides: "EVENT_CLARIFICATION_TIMED_OUT / EVENT_CLARIFICATION_DELIVERY_FAILED 两个常量 + 入 ALL_EVENTS"
      contains: "clarification.timed_out"
    - path: server/workflows/nodes/ai/plan_research.py
      provides: "_send_clarify_card 五条失败路径留痕 + emit + container_status 标记；订阅 timeout_at 读统一配置"
      contains: "delivery_failed"
  key_links:
    - from: server/workflows/nodes/ai/plan_research.py
      to: server/delivery/services/convergence_session_service.py
      via: "送达失败经 ConvergenceSessionService._emit_event 落 ConvergenceSessionEvent（事件唯一入口，Phase 110 复用同一事件源）"
      pattern: "EVENT_CLARIFICATION_DELIVERY_FAILED"
    - from: server/workflows/nodes/ai/plan_research.py
      to: server/friday/settings.py
      via: "WorkflowEventSubscription.timeout_at = now + CLARIFICATION_TIMEOUT_HOURS（D-4 单一超时口径）"
      pattern: "CLARIFICATION_TIMEOUT_HOURS"
---

<objective>
修掉「无声卡死」的发卡侧成因并把两个新事件名落进 taxonomy：`plan_research._send_clarify_card` 现有 **4 个连 warning 都不记的静默 `return`**（`no_questions` / `space is None` / `project is None` / `not chat_id`）加上 `except` 分支共 5 条失败路径，逐条改为「记结构化日志 + emit `clarification.delivery_failed` + 把该澄清轮 `container_status` 标 `delivery_failed`」；同时按 D-4 把 `WorkflowEventSubscription.timeout_at` 从代码写死的 60 分钟改为读 `CLARIFICATION_TIMEOUT_HOURS`，让工作流侧与会话侧同一口径到期。

**DEPTH 冻结遵守**：只改发卡侧与订阅超时，不碰 `clarification_questions.py`（prompt）、不碰 `agenerate_clarification_questions` 的 schema、不碰 `spec_generation` / `merged_plan` / `architect_merge_adapter` / `render.py` 的产出结构。

Purpose: RELY-02 前半句「澄清必达」——送达失败必须可观测且走得出去，绝不静默挂起；并为 107-06 的超时扫描提供「已知送达失败」的立即出口信号。
Output: 2 个事件常量 + 发卡侧 5 条路径留痕 + 订阅超时统一口径 + 两个测试文件补用例。
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/107-layered-presentation/107-CONTEXT.md
@.planning/phases/107-layered-presentation/107-RESEARCH.md
@server/delivery/services/event_taxonomy.py
@server/workflows/nodes/ai/plan_research.py
@server/delivery/models/clarification.py
</context>

## Artifacts this phase produces（本 plan 新建符号）

| Symbol | Kind | Location |
|--------|------|----------|
| `EVENT_CLARIFICATION_TIMED_OUT = "clarification.timed_out"` | 事件常量（入 `ALL_EVENTS`；emit 点在 107-06） | `server/delivery/services/event_taxonomy.py` |
| `EVENT_CLARIFICATION_DELIVERY_FAILED = "clarification.delivery_failed"` | 事件常量（入 `ALL_EVENTS`；emit 点在本 plan） | 同上 |
| `Clarification.container_status == "delivery_failed"` | 既有列的**新取值**（零迁移列变更） | `server/delivery/models/clarification.py`（docstring 补该取值说明） |
| `_DELIVERY_FAILURE_REASONS`（受控 5 值：`no_questions` / `no_space` / `no_project` / `no_chat_id` / `send_failed`） | 模块常量 | `server/workflows/nodes/ai/plan_research.py` |
| `clarification_delivery_failed`（structlog 事件名，`category="caller"`, `component="plan_research"`） | 事件名 | 同上 |

<tasks>

<task type="auto">
  <name>Task 1: 两个事件常量入 taxonomy + 守护测试对齐</name>
  <files>server/delivery/services/event_taxonomy.py, server/tests/services/test_event_taxonomy_alignment.py</files>
  <read_first>
    - server/delivery/services/event_taxonomy.py（`__all__` / 常量段 / `ALL_EVENTS` / `RESERVED_EVENTS` 三处必须同步；`EVENT_SPEC_DRAFTED` 是 v0.9 新增常量的既有扩展先例，逐字沿用同款注释格式：写明 producer 与 payload 形状）
    - server/tests/services/test_event_taxonomy_alignment.py:19-44（`_EMIT_FILES` 硬编码列表：只有这几个文件被扫 emit 点）与 :47-61（`_EVENT_PRODUCERS` **硬编码 map**）与 :121-141（`test_all_events_each_emitted_by_producer`：对 `ALL_EVENTS` 每个成员先 `assert producer is not None`，未登记即失败；`if not producer.exists(): continue` 只在 producer 文件**不存在**时才是安全分支）
    - server/tests/services/test_event_taxonomy_alignment.py:104-118（`test_referenced_constants_in_all_events`：`_EMIT_FILES` 里被引用的 `EVENT_*` 必须 ∈ `ALL_EVENTS`——把 `plan_research.py` 加进 `_EMIT_FILES` 后其 emit 点会被纳入扫描）
    - .planning/phases/107-layered-presentation/107-RESEARCH.md §7（两个事件的 payload 设计 + 为何不塞进 `clarification.answered`：Phase 110 时间线必须能区分「答了」与「超时放行」）与 Pitfall 8（常量 + ALL_EVENTS + emit 点 + 测试四者同提交）
  </read_first>
  <action>
    1. 追加两个常量（含中文注释写明 producer 与 payload 形状）：
       - `EVENT_CLARIFICATION_TIMED_OUT: Final[str] = "clarification.timed_out"` —— producer = `delivery/management/commands/expire_pending_clarifications.py`（107-06）；payload `{clarification_id, round_no, exit_action: "resumed_with_assumptions"|"failed_no_answer", waited_seconds, unclarified_points: [...]}`。注释写明**为何不复用 `clarification.answered`**：Phase 110 时间线要区分「用户答了」与「超时放行」，混用会让时间线撒谎。
       - `EVENT_CLARIFICATION_DELIVERY_FAILED: Final[str] = "clarification.delivery_failed"` —— producer = `workflows/nodes/ai/plan_research.py::_send_clarify_card`；payload `{clarification_id, round_no, channel: "feishu", reason: <5 值枚举>}`。
    2. 两个常量同步进 `__all__` 与 `ALL_EVENTS`（**不进** `RESERVED_EVENTS`——两者本 phase 都有真实 emit 点）。
    3. 🔴 **同一提交内必须改 `tests/services/test_event_taxonomy_alignment.py`，否则本 task 自己的 automated verify 必红**。该守护测试是**静态扫描 + 硬编码注册表**（已确认，见 `<read_first>`），常量一进 `ALL_EVENTS` 就会触发 `:132-134` 的 `assert producer is not None`。四件事一次做齐：
       - (a) `_EVENT_PRODUCERS` 登记两个 producer Path：`"clarification.delivery_failed": _PLAN_RESEARCH`（新增模块级 `_PLAN_RESEARCH = _SERVER_ROOT / "workflows" / "nodes" / "ai" / "plan_research.py"`）、`"clarification.timed_out": _EXPIRE_CLARIFICATIONS`（新增 `_EXPIRE_CLARIFICATIONS = _SERVER_ROOT / "delivery" / "management" / "commands" / "expire_pending_clarifications.py"`，该文件由 107-06 创建）。
       - (b) 两个 producer 文件加进 `_EMIT_FILES`（保持 `_EMIT_FILES` 与 `_EVENT_PRODUCERS` 口径一致；`expire_pending_clarifications.py` 此刻不存在 → 三个测试的 `if not path.exists(): continue` 分支自动跳过，顺序安全）。
       - (c) **为两个事件各设带 plan 标注的待落地豁免**（机制明确写死）：新增模块级
         ```
         # 待落地豁免：emit 点尚未提交，落地后删除对应行（覆盖性反查跳过这些事件）
         _PENDING_PRODUCER_EVENTS: dict[str, str] = {
             "clarification.delivery_failed": "107-04 Task 2",
             "clarification.timed_out": "107-06",
         }
         ```
         并在 `test_all_events_each_emitted_by_producer` 的循环体内、`assert producer is not None` **之后**插入 `if event in _PENDING_PRODUCER_EVENTS: continue`。
         为什么 `delivery_failed` 也必须豁免：其 producer `plan_research.py` **已经存在**，走不到 `if not producer.exists(): continue` 的安全分支，而 emit 点在本 plan 的 Task 2 才写入 → Task 1 的提交边界上 `assert const_name in tokens` 会红。
       - (d) plan 标注（`107-04` / `107-06` 字样）**只允许出现在 `_PENDING_PRODUCER_EVENTS` 的 dict value 里**（即上面 (c) 的 `"107-04 Task 2"` / `"107-06"` 两个字面值），**块注释与任何其它位置一律不得写这两个编号**。因果：删豁免行即等于删掉编号，下游两条归零断言才成立——107-04 Task 2 的 `rg -c '107-04' = 0` 与 107-06 Task 3 的 `rg -c '107-06|107-04' = 0`；若另写一段含编号的块注释，删行后编号仍在，两条断言双双变红。(c) 的示例块注释文案（`# 待落地豁免：emit 点尚未提交，落地后删除对应行……`）已刻意不含编号，逐字沿用即可。
       **禁止**为了绕过守护测试把常量放进 `RESERVED_EVENTS`（语义错误：它不是「非本 phase 产出」），也**禁止**放宽/删除 `assert producer is not None`（那会永久失去登记强制力）。
  </action>
  <verify>
    <automated>test "$(git show main:.planning/technical-blueprint/DESIGN.md 2>/dev/null | rg -c 'clarification\.(timed_out|delivery_failed)' || echo 0)" = "0" && cd server && uv run pytest tests/services/test_event_taxonomy_alignment.py -x -q && uv run python -c "
import os, django
os.environ.setdefault('DJANGO_SETTINGS_MODULE','friday.settings'); django.setup()
from delivery.services.event_taxonomy import ALL_EVENTS, EVENT_CLARIFICATION_TIMED_OUT, EVENT_CLARIFICATION_DELIVERY_FAILED
assert EVENT_CLARIFICATION_TIMED_OUT in ALL_EVENTS and EVENT_CLARIFICATION_DELIVERY_FAILED in ALL_EVENTS
assert EVENT_CLARIFICATION_TIMED_OUT == 'clarification.timed_out'
assert EVENT_CLARIFICATION_DELIVERY_FAILED == 'clarification.delivery_failed'
print('taxonomy ok')
"</automated>
  </verify>
  <acceptance_criteria>
    - automated 断言全过（含 A4 命名空间前置确认，`|| true` 兜住 main 不可达）
    - 两个常量同时出现在 `__all__` 与 `ALL_EVENTS`：`test "$(rg -c 'EVENT_CLARIFICATION_TIMED_OUT' server/delivery/services/event_taxonomy.py)" = "3"`（`__all__` / 常量定义 / `ALL_EVENTS` 各一次）
    - 未误入 RESERVED：`test "$(rg -n 'RESERVED_EVENTS' -A 8 server/delivery/services/event_taxonomy.py | rg -c 'CLARIFICATION' || echo 0)" = "0"`
    - 两个 producer 已登记（BLOCKER 修复点）：`test "$(rg -c 'clarification.delivery_failed' server/tests/services/test_event_taxonomy_alignment.py)" != "0"` 且 `test "$(rg -c 'clarification.timed_out' server/tests/services/test_event_taxonomy_alignment.py)" != "0"` 且 `test "$(rg -c 'plan_research' server/tests/services/test_event_taxonomy_alignment.py)" != "0"` 且 `test "$(rg -c 'expire_pending_clarifications' server/tests/services/test_event_taxonomy_alignment.py)" != "0"`
    - 两条豁免都带 plan 标注（供后续删除定位）：`test "$(rg -c '107-04' server/tests/services/test_event_taxonomy_alignment.py)" != "0"` 且 `test "$(rg -c '107-06' server/tests/services/test_event_taxonomy_alignment.py)" != "0"`
    - 登记强制力未被削弱：`test "$(rg -c 'assert producer is not None' server/tests/services/test_event_taxonomy_alignment.py)" = "1"`
    - `cd server && uv run pytest tests/services/test_event_taxonomy_alignment.py tests/delivery/test_clarification_service.py -q` 全绿（定向套；不跑整个 `tests/delivery`）
  </acceptance_criteria>
  <done>两个事件名进入稳定 taxonomy，Phase 110 时间线可按 `clarification.*` 前缀分组消费。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: _send_clarify_card 五条失败路径留痕 + emit + delivery_failed 标记</name>
  <files>server/workflows/nodes/ai/plan_research.py, server/tests/workflows/test_plan_research_node.py, server/tests/services/test_event_taxonomy_alignment.py</files>
  <read_first>
    - server/workflows/nodes/ai/plan_research.py:432-492（`_send_clarify_card` 全文：4 个静默 `return` 的确切位置 + `except Exception` 分支 + `log` 已 bind `component="plan_research"` / `category="caller"`）
    - server/workflows/nodes/ai/plan_research.py:493-530（`_acollect_round_questions`：正文脱敏已在用 `redact_secrets_in_text`；`_resolve_initiator` 的取值方式）
    - server/delivery/models/clarification.py:21-60（`container_status CharField(max_length=16, null=True)` 现有取值 `pending/answered/skipped` 与「严禁命名 status」的 docstring 纪律；`answered_at` 是 pending 判定的权威字段）
    - server/delivery/services/convergence_session_service.py:304-330（`_emit_event`：事件唯一入口、best-effort、绝不抛）
    - .planning/phases/107-layered-presentation/107-RESEARCH.md Pitfall 11（只补 except 分支不够——生产里最常见的失败是 `space is None` / `project is None` / `not chat_id` 这三条不抛异常的配置缺失路径）
  </read_first>
  <behavior>
    - `_acollect_round_questions` 返回空 → 记 `clarification_delivery_failed(reason="no_questions")`、emit `clarification.delivery_failed`、该轮 `container_status == "delivery_failed"`，函数正常返回（不抛）
    - `_resolve_space` 返回 None → reason == `"no_space"`，同上三件事
    - `_aresolve_project` 返回 None → reason == `"no_project"`
    - `resolve_or_create_group` 返回空 → reason == `"no_chat_id"`
    - `send_card` 抛异常 → reason == `"send_failed"`，且异常文本不出现在事件 payload 中
    - 成功路径 → 不记 warning、不 emit `delivery_failed`、`container_status` 不被改成 `delivery_failed`
    - 留痕本身失败（如 emit 抛、DB 写抛）→ `_send_clarify_card` 仍正常返回（best-effort，绝不反噬），节点 `waiting_event` 行为不变
    - `delivery_failed` 轮仍被 `ClarificationService.ahas_pending` 判为 pending（不因标记而被当成已答）
  </behavior>
  <action>
    1. 模块级新增 `_DELIVERY_FAILURE_REASONS: Final[frozenset[str]] = frozenset({"no_questions", "no_space", "no_project", "no_chat_id", "send_failed"})`。
    2. 新增私有 helper `async def _amark_delivery_failed(self, session, clarification_id: str, reason: str, *, round_no: int | None = None) -> None`：
       - `assert reason in _DELIVERY_FAILURE_REASONS` 的等价校验（非法 reason 记 `unknown` 而非抛）；
       - 记 `log.warning("clarification_delivery_failed", clarification_id=..., reason=reason, channel="feishu", session_id=str(session.id), initiated_by_user_id=self._resolve_initiator(context) or "system", category="caller", component="plan_research")`；
       - `Clarification.objects.filter(id=clarification_id).aupdate(container_status="delivery_failed")`（**只写 `container_status`，绝不写 `answered_at`**——否则该轮会被误判为已答，永久失去出口）；
       - `await ConvergenceSessionService()._emit_event(EVENT_CLARIFICATION_DELIVERY_FAILED, session, {"clarification_id": clarification_id, "round_no": round_no, "channel": "feishu", "reason": reason})`（事件唯一入口，Phase 110 复用）；
       - **整体 `try/except Exception: pass`** 包裹（观测代码 best-effort，绝不反噬发卡与节点挂起语义）。
    3. `_send_clarify_card` 的 4 个静默 `return` 逐条改为 `await self._amark_delivery_failed(...); return`（reason 依次 `no_questions` / `no_space` / `no_project` / `no_chat_id`）；`except Exception` 分支在既有 warning 之后追加 `await self._amark_delivery_failed(..., reason="send_failed")`。**保留** `except` 的 best-effort 语义与既有 warning 事件名（排障口径不破）。
    4. `Clarification` 模型 docstring 补一行：`container_status` 新取值 `delivery_failed` 的语义（「卡片未送达，该轮仍算 pending，由澄清超时扫描做立即出口」）——**只改 docstring，不改字段定义**（零迁移）。
    5. `test_plan_research_node.py` 补用例覆盖 `<behavior>` 全部 8 条（用 monkeypatch 替身分别制造 5 条失败；断言 emit 被调用的事件名与 payload reason；断言成功路径零 emit；断言留痕失败时函数不抛）。
    6. **删除 Task 1 加的 `clarification.delivery_failed` 待落地豁免**（emit 点本 task 已落地）：从 `test_event_taxonomy_alignment.py` 的 `_PENDING_PRODUCER_EVENTS` 里删掉 `"clarification.delivery_failed": "107-04 Task 2"` 那一行（`clarification.timed_out` 那行**保留**，由 107-06 Task 3 删除），并确认覆盖性反查在 `plan_research.py` 里能扫到 `EVENT_CLARIFICATION_DELIVERY_FAILED` token 后仍绿。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/workflows/test_plan_research_node.py -x -q -k "delivery or clarify" && uv run pytest tests/services/test_event_taxonomy_alignment.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `<behavior>` 8 条全绿；`-k delivery` 能命中 5 条失败路径用例（与 107-VALIDATION 命令口径一致）
    - 静默 return 归零：`_send_clarify_card` 函数体内不存在「裸 `return` 且其上方 3 行内无 `_amark_delivery_failed` 调用」的路径（人工核对 + 一条测试：5 条路径各断言 emit 被调用一次）
    - `answered_at` 未被误写：`test "$(rg -c 'aupdate\([^)]*answered_at' server/workflows/nodes/ai/plan_research.py || echo 0)" = "0"`（**改为对「写 `answered_at`」这一构造本身的精确断言**：原先的 `rg -n '_amark_delivery_failed' -A 20` 恒假——`_send_clarify_card` 止于 `:491`，`-A 20` 窗口必然越进紧随的 `_acollect_round_questions` docstring（`:497` 含「不按 `answered_at` 过滤」），且 `:388` 的既有 `answered_at__isnull=True` 查询也是合法读。断言的真实意图是「`_amark_delivery_failed` 只写 `container_status`、绝不写 `answered_at`」，故直接钉死 `aupdate(...answered_at` 形态）
    - `delivery_failed` 仍算 pending：测试断言 `await ClarificationService().ahas_pending(session.id) is True`
    - 事件 payload 无异常原文：测试注入含 `sk-ant-` 的异常，断言 emit payload 的 JSON 序列化中不含 `sk-ant-`
    - `delivery_failed` 豁免已删除、`timed_out` 豁免仍在：`test "$(rg -c '107-04' server/tests/services/test_event_taxonomy_alignment.py || echo 0)" = "0"` 且 `test "$(rg -c '107-06' server/tests/services/test_event_taxonomy_alignment.py)" != "0"`
    - `cd server && uv run pytest tests/workflows/test_plan_research_node.py tests/services/test_event_taxonomy_alignment.py -q` 全绿 + `uv run ruff check workflows/nodes/ai/plan_research.py tests/workflows/test_plan_research_node.py tests/services/test_event_taxonomy_alignment.py`
  </acceptance_criteria>
  <done>发卡失败从「完全不可见」变成「必留痕 + 有事件 + 有出口信号」，无声卡死的发卡侧成因消除。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: D-4 单一超时口径——订阅 timeout_at 读 CLARIFICATION_TIMEOUT_HOURS</name>
  <files>server/workflows/nodes/ai/plan_research.py, server/tests/workflows/test_plan_research_node.py</files>
  <read_first>
    - server/workflows/nodes/ai/plan_research.py:395-406（`WorkflowEventSubscription.objects.acreate(..., timeout_at=timezone.now() + timedelta(minutes=60), timeout_action="fail")` 的确切位置）
    - server/workflows/management/commands/check_timeouts.py（到期后只把 NodeExecution / WorkflowExecution 标 TIMEOUT，**不动 `ConvergenceSession`**——D-4 矛盾态的另一半，本 plan 不改该命令，纵深条件归 107-06）
    - .planning/phases/107-layered-presentation/107-CONTEXT.md D-4（结果要求：任何时刻都不得存在「工作流已判超时而会话仍在等」的窗口）
    - .planning/phases/107-layered-presentation/107-RESEARCH.md OQ-2 推荐 (a)（对齐订阅超时与澄清超时，改动更小且语义一致；副作用是飞书群卡有效期变长——体验上是好事）
    - .planning/phases/107-layered-presentation/107-RESEARCH.md §Runtime State Inventory「活跃服务配置」行（生产已有活跃订阅行携带旧 60min `timeout_at`，不会自动更新）
  </read_first>
  <behavior>
    - `CLARIFICATION_TIMEOUT_HOURS = 24`（默认）→ 新建订阅的 `timeout_at` 距 `now` 约 24 小时（容差 60s）
    - `CLARIFICATION_TIMEOUT_HOURS` 被 override 为 1 → `timeout_at` 距 `now` 约 1 小时
    - 配置缺失（`getattr` 兜底）→ 回退 24 小时且不抛
    - `timeout_action` 保持 `"fail"`（本 plan 不改工作流侧动作语义——会话侧出口由 107-06 的扫描 job 驱动）
  </behavior>
  <action>
    1. 把写死的 `timedelta(minutes=60)` 改为 `timedelta(hours=getattr(settings, "CLARIFICATION_TIMEOUT_HOURS", 24))`（`from django.conf import settings` 按文件既有 import 风格局部或顶层导入）。
    2. 在该调用旁加注释写明 D-4 的因果：工作流侧订阅与会话侧澄清读**同一个**配置键，两侧同时到期；否则第 60 分钟工作流已标 TIMEOUT 而会话仍 `waiting_clarification`，中间是 23 小时矛盾态。并注明：生产已有的活跃订阅行携带旧 `timeout_at`，本改动只影响新建订阅，存量行由 107-06 扫描器的「workflow 已 TIMEOUT + 会话仍 waiting_clarification → 立即出口」纵深条件兜住。
    3. `timeout_action` 保持 `"fail"` 不动（改它属工作流引擎语义面，超出本 phase 的韧性边界）。
    4. `test_plan_research_node.py` 补用例覆盖 `<behavior>` 四条（用 `django.test.override_settings` 注入不同小时数，断言 `timeout_at` 差值）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/workflows/test_plan_research_node.py -x -q -k "timeout or subscription"</automated>
  </verify>
  <acceptance_criteria>
    - `<behavior>` 四条全绿
    - 60 分钟字面量消失：`test "$(rg -c 'timedelta\(minutes=60\)' server/workflows/nodes/ai/plan_research.py || echo 0)" = "0"`
    - 配置键被引用：`test "$(rg -c 'CLARIFICATION_TIMEOUT_HOURS' server/workflows/nodes/ai/plan_research.py)" != "0"`
    - `timeout_action` 未改：`test "$(rg -c 'timeout_action="fail"' server/workflows/nodes/ai/plan_research.py)" != "0"`
    - `cd server && uv run pytest tests/workflows -q` 全绿
  </acceptance_criteria>
  <done>工作流侧与会话侧超时口径统一，23 小时矛盾态窗口在新建订阅上被消除（存量行由 107-06 兜底）。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 飞书 IM 上游响应 / 异常 → 事件 payload | 上游响应体可能含 token 或请求回显 |
| 澄清正文（需求原文）→ 卡片 / 日志 / 事件 | 需求原文可能含用户粘贴的凭证 |
| 澄清超时出口 → HITL 绕过 | 攻击者刻意不答以拿到「带假设」的方案 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-107-02 | Information Disclosure | `clarification.delivery_failed` payload | mitigate | payload 只含 `clarification_id` / `round_no` / `channel` / 5 值 `reason` 枚举——**结构上不含异常文本**；测试注入 `sk-ant-` 断言 payload 序列化无明文；入库另经既有 `redact_for_ledger` |
| T-107-04 | Tampering / Repudiation | 送达失败与超时出口 | mitigate | 每条失败路径必留痕（日志 + 事件 + `container_status`）且带 `initiated_by_user_id`；出口的 `unclarified_points` 显式标注归 107-06 |
| V8 数据保护 | Information Disclosure | 澄清正文进卡片 | mitigate | 复用发卡侧既有 `redact_secrets_in_text`（`_acollect_round_questions` 已在用），本 plan 不绕过 |
| T-107-SC | Tampering | npm/pip/cargo installs | accept | 零新增依赖 |
</threat_model>

<verification>
- `cd server && uv run pytest tests/workflows tests/delivery tests/services/test_event_taxonomy_alignment.py -q` 全绿
- `cd server && uv run ruff check delivery/services/event_taxonomy.py workflows/nodes/ai/plan_research.py tests/workflows/test_plan_research_node.py tests/services/test_event_taxonomy_alignment.py`
- DEPTH 冻结自检：`cd server && git diff --name-only | rg -q 'clarification_questions\.py|spec_generation\.py|merged_plan\.py|architect_merge_adapter\.py|render\.py|decompose_segments\.py|research_adapter\.py' && exit 1 || true`
- A4 命名空间确认已**自动化**（不留人工步骤）：Task 1 的 automated verify 首段即 `test "$(git show main:.planning/technical-blueprint/DESIGN.md 2>/dev/null | rg -c 'clarification\.(timed_out|delivery_failed)' || echo 0)" = "0"`（`|| echo 0` 兜住 main 不可达/文件缺失；非 0 即命名空间撞车 → 改名并记入 SUMMARY）

## 归零断言纪律（本 phase 统一口径，写给执行者）

1. 任何 `rg -c ... = "0"` 归零断言，若其模式串**同时出现在本 task 要求写的注释里**（或文件里既有注释中）→ **必须**先滤注释行：`rg -v '^[[:space:]]*#' <file> | rg -c 'PATTERN' || echo 0`。`.vue` 用 `rg -v '^[[:space:]]*(//|<!--)'`。`rg -c` 计的是行、不区分代码与注释，不滤即自引入必红。
2. 任何 `-A N` 窗口归零断言，若窗口**可能越过被断言的代码构造**（函数/分支结束后的相邻代码或 docstring）→ 改为对**构造本身**的精确断言（如 `rg -c 'aupdate\([^)]*answered_at'`），或在窗口输出上滤 `^[0-9]+[-:][[:space:]]*#`（`-A` 输出的行前缀是 `NNN:` / `NNN-`）。
3. **plan 编号（`107-XX`）不得出现在块注释里**，只能出现在会被整行删除的数据结构 value 中（见 Task 1 action 3(d)）——否则「删豁免」类归零断言删不干净。
</verification>

<success_criteria>
- 5 条送达失败路径零静默：各有日志 + 事件 + `container_status` 标记，且 `delivery_failed` 轮仍算 pending
- 两个事件常量入 `ALL_EVENTS` 且 taxonomy 双向守护绿
- 订阅超时与澄清超时同一配置键（D-4），60 分钟字面量消失
- 发卡与节点挂起语义零变化（best-effort 不反噬）
</success_criteria>

<output>
完成后创建 `.planning/phases/107-layered-presentation/107-04-SUMMARY.md`。
</output>
