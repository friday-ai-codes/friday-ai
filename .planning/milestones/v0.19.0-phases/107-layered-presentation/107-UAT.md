---
status: partial
phase: 107-layered-presentation
source: [107-VERIFICATION.md]
started: 2026-07-30T01:20:00Z
updated: 2026-08-02T00:00:00Z
audit_acknowledged:
  milestone: v0.25.0
  at: 2026-08-31
  gap_snapshot: "partial::scenarios=4"
---

## Current Test

number: 1
name: 澄清必达真机链路（IM/飞书送达 + 可作答）
expected: |
  真实会话触发澄清后：卡片确实送达用户、用户可作答、答复能回流并推进编排；
  送达失败时留痕（`clarification.delivery_failed` 事件 + 该轮 `container_status=delivery_failed`）
  且走超时出口而非静默挂起。SC-4 的前半句依赖真实飞书环境，无法程序化断言。
awaiting: user response

## Tests

### 1. 澄清必达真机链路（IM/飞书送达 + 可作答）

expected: 澄清卡送达可答；送达失败有事件留痕并进入出口；会话不停在 `waiting_clarification`。
result: [pending]
not_automatable: 需要真实飞书环境（送达与回流是跨系统链路，mock 掉就等于把被测物换成 mock 自己）。

### 2. 分组/跨组/降级三块 UI 视觉观感

expected: 对话路由面板与 107-UI-SPEC 一致——两组分区标题、各组 Top-3 + 已选 pin-in + 溢出披露、全局组常驻跨组说明句 + 候选级「跨组」徽标、迟滞置顶提示条、降级 amber 横幅 + 置信徽标灰化（折叠后仍留「降级」徽标）；零新色板。历史 trace（无 group/degraded 字段）渲染与改动前一致。
result: [pass]
evidence: `web/tests/e2e/routing-panel.spec.ts` 中六条 107-2 用例：组标题与计数、跨组说明句与候选级徽标、置顶提示两种措辞、降级横幅 + 徽标灰化（与未降级同页对照计算色）、闭集外原因不回显、历史 trace 平铺渲染。
residual_human_judgement: 「与 UI-SPEC 观感一致 / 零新色板」这类整体观感属人判；「各组 Top-3 + 已选 pin-in + 溢出披露」在当前 `RoutingCandidateList` 实现里没有对应渲染分支（组内全量列出），因此未覆盖——这条契约与实现的差异值得单独确认。

### 3. O-6 生产延迟分位回填

expected: 生产执行 `measure_stage1_latency` 取 Stage 1 `duration_ms` 的 p50/p90/p99 回填 107-MEASUREMENTS.md（现为 deferred 占位）；据此判定 per-call 超时是否可下调，或确认「输入哈希缓存 + 快照回放为主要收益来源」。
result: [pending]
not_automatable: 需要生产实例（真实流量下的延迟分位）。

### 4. 澄清超时出口的生产 dry-run 影响面

expected: 生产先只读评估 `expire_pending_clarifications` 的命中集合（存量 `waiting_clarification` 会话数、其中 workflow 已 TIMEOUT 的数量），确认批量出口不会一次性推进过多历史会话；再开启 job。
result: [pending]
not_automatable: 需要生产实例（存量数据的命中集合）。

### 5. 澄清 pending 态可见性确认

expected: 会话内 `waiting_clarification` 状态对用户可见（由既有 `ChatStatusBar` / `ClarificationCard` / `HumanTaskInbox` 满足，本 phase 未新增前端面）。若实际不可见，作为独立缺陷进 backlog。
result: [pass]
evidence: `web/tests/e2e/clarification-visibility.spec.ts` —— runtime 回 `active + phase=waiting_clarification + pending_clarification` 时，状态条显示「等待你在上方卡片中确认...」、澄清卡带问题正文与「待回复」徽标、选项可见，并给得出「跳过，直接回答」的兜底出口；配无待回复的负向对照。结论：**可见，不构成缺陷**。
residual_human_judgement: `HumanTaskInbox` 那一路未覆盖（本次只验证了会话内两处承载）。

### 6. D-1 候选范围放开后的结果可见性判断

expected: 放开硬过滤后路由结果会含项目外仓名（沿用两个已上线全库入口的既有判断，未新增可见性面）。请确认团队对「跨项目仓名可见」的判断仍成立；若不成立需加可见性过滤。
result: [pending]
not_automatable: 这是团队策略决定，不是可断言的行为——需要人确认「跨项目仓名对成员可见」这条判断是否仍然接受。

## Summary

total: 6
passed: 2
issues: 0
pending: 4
skipped: 0
blocked: 0

## Gaps

浏览器可验证子集已于 2026-08-02 由 Playwright e2e 执行，详见 `.planning/UAT-AUTOMATION-REPORT.md`。
余下 4 项：1 项需真实飞书、2 项需生产实例、1 项为团队策略决定。

新增待确认：107-UI-SPEC §「Top-3 与溢出披露」要求每组默认只渲染 Top-3、其余进组内
Collapsible（trigger 文案「显示其余 {n} 个候选」）。`RoutingCandidateList.vue` 组内
候选**全量列出**，前端全库检索不到该 trigger 文案，无 Top-3 截断。
两点缓解：① pin-in 那一半是为带 Checkbox 的 `RoutingDecisionPanel` 写的，而实际挂载
的是只读清单，pin-in 天然不适用；② 工具侧 `top_k` 默认 5（按组配额截断），组内最多
5 行，溢出披露的实际收益有限。需要确认这是契约被降级采纳还是漏实现。
