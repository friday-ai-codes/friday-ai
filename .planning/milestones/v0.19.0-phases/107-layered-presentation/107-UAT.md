---
status: testing
phase: 107-layered-presentation
source: [107-VERIFICATION.md]
started: 2026-07-30T01:20:00Z
updated: 2026-07-30T01:20:00Z
---

## Current Test

number: 1
name: 澄清必达真机链路（IM/飞书送达 + 可作答）
expected: |
  真实会话触发澄清后：卡片确实送达用户、用户可作答、答复能回流并推进编排；
  送达失败时留痕（`clarification.delivery_failed` 事件 + 该轮 `container_status=delivery_failed`）
  且走超时出口而非静默挂起。SC-4 的前半句依赖真实飞书环境，无法程序化断言。
awaiting: user response

## Tests

### 1. 澄清必达真机链路（IM/飞书送达 + 可作答）
expected: 澄清卡送达可答；送达失败有事件留痕并进入出口；会话不停在 `waiting_clarification`。
result: [pending]

### 2. 分组/跨组/降级三块 UI 视觉观感
expected: 对话路由面板与 107-UI-SPEC 一致——两组分区标题、各组 Top-3 + 已选 pin-in + 溢出披露、全局组常驻跨组说明句 + 候选级「跨组」徽标、迟滞置顶提示条、降级 amber 横幅 + 置信徽标灰化（折叠后仍留「降级」徽标）；零新色板。历史 trace（无 group/degraded 字段）渲染与改动前一致。
result: [pending]

### 3. O-6 生产延迟分位回填
expected: 生产执行 `measure_stage1_latency` 取 Stage 1 `duration_ms` 的 p50/p90/p99 回填 107-MEASUREMENTS.md（现为 deferred 占位）；据此判定 per-call 超时是否可下调，或确认「输入哈希缓存 + 快照回放为主要收益来源」。
result: [pending]

### 4. 澄清超时出口的生产 dry-run 影响面
expected: 生产先只读评估 `expire_pending_clarifications` 的命中集合（存量 `waiting_clarification` 会话数、其中 workflow 已 TIMEOUT 的数量），确认批量出口不会一次性推进过多历史会话；再开启 job。
result: [pending]

### 5. 澄清 pending 态可见性确认
expected: 会话内 `waiting_clarification` 状态对用户可见（由既有 `ChatStatusBar` / `ClarificationCard` / `HumanTaskInbox` 满足，本 phase 未新增前端面）。若实际不可见，作为独立缺陷进 backlog。
result: [pending]

### 6. D-1 候选范围放开后的结果可见性判断
expected: 放开硬过滤后路由结果会含项目外仓名（沿用两个已上线全库入口的既有判断，未新增可见性面）。请确认团队对「跨项目仓名可见」的判断仍成立；若不成立需加可见性过滤。
result: [pending]

## Summary

total: 6
passed: 0
issues: 0
pending: 6
skipped: 0
blocked: 0

## Gaps
