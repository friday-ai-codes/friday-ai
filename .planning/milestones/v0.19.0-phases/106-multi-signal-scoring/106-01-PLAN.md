---
phase: 106-multi-signal-scoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/codegraph/services/repo_router_scoring.py
  - server/tests/codegraph/test_repo_router_scoring.py
autonomous: true
requirements: [ROUTE-03, ROUTE-04, ROUTE-05]
must_haves:
  truths:
    - "传入 repo_meta（含 n_r/n_bar）时，命中数多但 N_r 大的仓其 breadth 贡献不高于命中数少但 N_r 小的仓（pivoted-size-normalized 对数饱和，ROUTE-03 机制在纯函数层成立）"
    - "repo_meta.facet_scores 中 domain/stack/team 有值时对应 breakdown 键出现且 Σbreakdown == score；facet 分数为 None（缺失）时该键不出现，其余信号经权重重归一化，分数不被系统性压低（ROUTE-04）"
    - "同一仓 last_commit_at 距 now 分别为 30/180/365/730 天时活跃度贡献严格递减（指数衰减 H=180d、offset=14d、floor=0.05）；疑似废弃封顶 min(A, 0.10) 完全落在 activity 一项内（ROUTE-05）"
    - "repo_meta=None 时行为与 Phase 105 逐字段一致（legacy 路径零破坏，golden 门禁在本 plan 后保持全绿）"
    - "关键程度不进加性和：Σbreakdown == score 恒成立且 breakdown 无 criticality 键；仅同分带（|ΔS|<0.03 量化桶）内按锚点 {核心1.0/重要0.7/一般0.4/边缘0.15} 决序，值经 ScoredCandidate.criticality 旁路字段透出"
  artifacts:
    - path: server/codegraph/services/repo_router_scoring.py
      provides: "六信号纯函数打分核心（新路径 repo_meta/constants/now 参数注入；legacy 路径不变）"
      exports: ["DEFAULT_WEIGHT_CONFIG", "SIGNAL_DOMAIN", "SIGNAL_STACK", "SIGNAL_TEAM", "aggregate_and_score"]
      min_lines: 380
    - path: server/tests/codegraph/test_repo_router_scoring.py
      provides: "INV-R1~R4 对六信号新路径的性质测试 + legacy 路径回归守护"
      min_lines: 450
  key_links:
    - from: server/codegraph/services/repo_router_scoring.py
      to: server/codegraph/services/repo_router_v2.py
      via: "aggregate_and_score 新 kwargs（repo_meta/constants/now/weights）由 router 层（106-06）组装注入；本模块保持零 I/O/零 Django/不读配置"
      pattern: "def aggregate_and_score\\("
---

<objective>
把 `repo_router_scoring.py` 纯函数核心从三信号扩展为六信号：MaxP 主干（dense 余弦 affine clip 校准，缺失回退 RRF s_hat）+ pivoted-size-normalized 对数饱和 breadth（消除尺寸偏置，ROUTE-03）、元数据三信号 domain/stack/team 消费 resolver 注入的匹配分（ROUTE-04）、活跃度指数衰减连续化 + 枚举回退 + 废弃封顶（ROUTE-05）、关键程度作同分带 tie-break 旁路（CONTEXT Open Question 1 裁决：不进加性和）。全部权重与常数经参数注入，`DEFAULT_WEIGHT_CONFIG` 作为唯一默认值来源（harness/replay/config-loader 三方 import）。

关键约束：**repo_meta=None 时走 Phase 105 legacy 路径，行为逐字段不变**——`WEIGHT_SET_VERSION` 常量保持 `"phase105-v1"`（bump 与 baseline 重建归 106-08），本 plan 落地后 golden 门禁必须全绿。公式与常数以 `.planning/research/ROUTING-RANKING.md` §2.3/§3/§4 为权威，冲突以 §0 结论速览裁决。

Purpose: 全 phase 的接口契约层——106-02 config loader 的默认值、106-03 resolver 的输出契约、106-06 router 注入、106-07 replay、106-08 golden harness 全部构建在本 plan 的签名之上。
Output: 扩展后的 `repo_router_scoring.py` + 性质测试。
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/106-multi-signal-scoring/106-CONTEXT.md
@.planning/phases/106-multi-signal-scoring/106-RESEARCH.md
@.planning/research/ROUTING-RANKING.md
@server/codegraph/services/repo_router_scoring.py
@server/tests/codegraph/test_repo_router_scoring.py
</context>

## Artifacts this phase produces（本 plan 新建符号）

| Symbol | Kind | Location |
|--------|------|----------|
| `SIGNAL_DOMAIN = "domain"` / `SIGNAL_STACK = "stack"` / `SIGNAL_TEAM = "team"` | breakdown key 常量 | `server/codegraph/services/repo_router_scoring.py` |
| `DEFAULT_WEIGHT_CONFIG: dict` | 默认权重/常数配置（weight_set_version="phase106-v1"、weights 5 键、constants、criticality_anchors、t2_disabled_facets） | 同上 |
| `aggregate_and_score(node_hits, *, weights=None, repo_meta=None, constants=None, now=None)` | 纯函数（签名扩展） | 同上 |
| `ScoredCandidate.criticality: float \| None` | dataclass 新字段（关键程度锚点值，旁路展示，不计入 Σ） | 同上 |
| repo_meta 契约：`{rid: {n_r, last_commit_at, dense_cos_max, facet_scores: {domain/stack/team: {score, layer}}, criticality_value}}` | dict 形状（docstring 权威定义） | 同上 |

<tasks>

<task type="auto">
  <name>Task 1: 六信号打分核心扩展（新路径参数注入，legacy 路径零破坏）</name>
  <files>server/codegraph/services/repo_router_scoring.py</files>
  <read_first>
    - server/codegraph/services/repo_router_scoring.py（现行 250 行全文——分桶/归一/重归一化/排序骨架全部复用）
    - .planning/research/ROUTING-RANKING.md §2.3（聚合三步公式）/§3.4（重归一化）/§3.5（活跃度衰减）/§3.6（关键程度先验）/§4（权重初值表与 INV-R1~R4）
    - .planning/phases/106-multi-signal-scoring/106-CONTEXT.md（锁定决策：加性合成、多值 max、缺失重归一化、C_crit tie-break 裁决、五信号权重表）
    - .planning/phases/106-multi-signal-scoring/106-RESEARCH.md §1（扩展面三处签名变化 + breakdown text/breadth 拆两键的表示方案）
  </read_first>
  <action>
    在 `repo_router_scoring.py` 内扩展（保持零 I/O、零 Django、仅 stdlib——新增 `datetime` import 属 stdlib 允许）：

    **新常量：**
    - `SIGNAL_DOMAIN = "domain"`、`SIGNAL_STACK = "stack"`、`SIGNAL_TEAM = "team"`（breakdown key，前端映射表 106-05 与之对齐，禁止改名；`SIGNAL_TEXT/SIGNAL_BREADTH/SIGNAL_ACTIVITY` 不动）。
    - `DEFAULT_WEIGHT_CONFIG: dict[str, Any]`——唯一默认配置来源（106-02 loader 回退值、106-08 harness 默认值、106-07 replay 兜底值都 import 它）：
      `weight_set_version: "phase106-v1"`；
      `weights: {SIGNAL_TEXT: 0.55, SIGNAL_DOMAIN: 0.15, SIGNAL_ACTIVITY: 0.12, SIGNAL_STACK: 0.08, SIGNAL_TEAM: 0.05}`（per CONTEXT 五信号加性表——C_crit 已裁决为 tie-break 不进和，相对权重经重归一化生效，绝对和 0.95 无须为 1）；
      `constants: {"p": 2.0, "b": 0.6, "n_cap": 6.0, "lam": 0.25, "n_bar": None, "half_life_days": 180.0, "offset_days": 14.0, "activity_floor": 0.05, "deprecated_cap": 0.10, "s_top_c_lo": 0.25, "s_top_c_hi": 0.55, "t2_c_lo": 0.25, "t2_c_hi": 0.55, "crit_band": 0.03}`；
      `criticality_anchors: {"核心": 1.0, "重要": 0.7, "一般": 0.4, "边缘": 0.15}`（四档全保留——facet 自动值只有三档但人工 pin 可出现「一般」，Pitfall 1）；
      `crit_weight_reserved: 0.05`（未来切换加性方案的开关位，当前不参与计算）；
      `t2_disabled_facets: []`、`embedding_model_id: None`、`calibrated_at: None`。
      注释标注 s_top_c_lo/c_hi 与 t2_c_lo/c_hi 均为 O-2 校准输出的初值（生产回填 deferred）。
    - `PHASE105_WEIGHTS` / `WEIGHT_SET_VERSION = "phase105-v1"` / `ACTIVITY_ENUM_MAP` / `DEPRECATED_ACTIVITY_CAP` **全部保持不变**；WEIGHT_SET_VERSION 旁注释「bump 到 phase106-v1 与 golden baseline 重建同步进行（106-08），提前 bump 会使门禁版本守护失败」。

    **ScoredCandidate 扩展：** 新字段 `criticality: float | None = None`（关键程度锚点值，informational 旁路——绝不计入 breakdown/score）。

    **`aggregate_and_score` 签名扩展：**
    `aggregate_and_score(node_hits, *, weights=None, repo_meta=None, constants=None, now=None) -> list[ScoredCandidate]`
    - `repo_meta: dict[str, dict[str, Any]] | None`——per-repo 元数据注入，docstring 定义键契约：`n_r: int|None`、`last_commit_at: ISO str|None`、`dense_cos_max: float|None`、`facet_scores: {"domain"|"stack"|"team": {"score": float|None, "layer": "t1"|"t2"|None}}`、`criticality_value: str|None`。
    - `constants: dict | None`——新路径下 None 回退 `DEFAULT_WEIGHT_CONFIG["constants"]`；传入时与默认 merge（缺键补默认）。
    - `now: str | None`——ISO 8601 时间锚点（活跃度衰减用）。**禁止函数内 datetime.now()**（回放/golden 确定性）；解析用 `datetime.fromisoformat`，naive 时间戳按 UTC 处理（`tzinfo=None → replace(tzinfo=timezone.utc)`），last_commit_at 同规则。
    - **模式判定：`repo_meta is None` → legacy 路径**（现行三信号逻辑与 PHASE105_WEIGHTS 默认，一行不改）；`repo_meta is not None` → 新路径（weights None 回退 `DEFAULT_WEIGHT_CONFIG["weights"]`）。

    **新路径逐信号（分桶/桶内排序/query-local s_hat 归一复用现有实现）：**
    1. `S_top`：`meta.get("dense_cos_max")` 非 None → `clip((cos - s_top_c_lo)/(s_top_c_hi - s_top_c_lo), 0, 1)`；None → 回退桶内 max s_hat（RRF fallback，per CONTEXT O-3 口径，Pitfall 6 缺失不打死）。
    2. `breadth`（ROUTING-RANKING §2.3 三步）：`n_eff = Σ (s_hat_i / s_hat_bucket_top) ** p`（桶内软计数）；`denom_size = 1 - b + b * (n_r / n_bar)` 当 n_r 与 n_bar 均可用，否则 `denom_size = 1.0`（b=0 等价降级路径——n_bar 缺失时 breadth 仍有定义）；`breadth = min(log1p(n_eff / denom_size) / log1p(n_cap), 1.0)`。
    3. 文本合成 `S_text = (1 - lam) * S_top + lam * breadth`，但 **breakdown 拆两个扁平键**（RESEARCH §1 表示方案）：`text` 贡献 = `w_text * (1-lam) * S_top / D`、`breadth` 贡献 = `w_text * lam * breadth / D`（D 为重归一化分母）——INV-R3 与 106-08 机制断言 `breakdown[repo]["breadth"]` 同时成立，前端零结构改动。
    4. `activity`（真值表，Pitfall 4）：`last_commit_at` 与 `now` 均可用 → `delta = max(0, (now - last_commit).days - offset_days)`，`A = max(0.5 ** (delta / half_life_days), activity_floor)`；`last_commit_at` 不可用 → facets 活跃度枚举映射（ACTIVITY_ENUM_MAP）；两者皆无 → 信号不可用（None）。**无论哪个来源**，facets["活跃度"] == "疑似废弃" 时 `A = min(A, deprecated_cap)`（封顶语义接管，惩罚完全落在 activity 项内）。
    5. `domain`/`stack`/`team`：直接消费 `facet_scores[key]["score"]`——float 取值、None/键缺失 → 信号不可用。scorer 不做任何匹配/max 聚合（那是 resolver 106-03 的职责，多值 max、"未分类"→缺失、团队条件信号均在上游落定）。
    6. 重归一化 + 合成（现有模式扩展）：`available` 信号集合上 `D = fsum(w[j])`；`breakdown[j] = w[j] * M[j] / D`（text 拆两键见第 3 条，两键之和 = w_text*S_text/D）；`score = fsum(breakdown.values())`（INV-R3 按构造成立）；denom<=0 防御分支保留。
    7. `criticality`：`meta.get("criticality_value")` 经 `criticality_anchors` 映射 → `ScoredCandidate.criticality`；缺失/枚举外 → None。**不进 breakdown**。
    8. 新路径排序（同分带 tie-break，CONTEXT 裁决）：`band_bucket = int(math.floor(round(score, 6) / crit_band + 1e-9))`；排序键 `(-band_bucket, -crit_rank, -round(score, 6), repo_id)`，其中 `crit_rank = candidate.criticality if criticality is not None else 0.4`（未知居中不奖不罚，与「一般」同档）。代码注释记录取舍：量化桶实现下跨桶边界的邻近对不触发 tie-break（|ΔS|<0.03 的字面语义近似），换显式带内比较会破坏排序键全序性。legacy 路径排序键不变。

    `__all__` 补 `DEFAULT_WEIGHT_CONFIG`、`SIGNAL_DOMAIN/SIGNAL_STACK/SIGNAL_TEAM`。模块 docstring 更新：repo_meta 键契约 + 「resolver（I/O，router 层 106-06）→ scorer（纯函数）」分层说明。本模块仍不加日志。
  </action>
  <verify>
    <automated>cd server && uv run python -c "
from codegraph.services.repo_router_scoring import DEFAULT_WEIGHT_CONFIG, SIGNAL_DOMAIN, aggregate_and_score, WEIGHT_SET_VERSION
assert WEIGHT_SET_VERSION == 'phase105-v1'
assert DEFAULT_WEIGHT_CONFIG['weight_set_version'] == 'phase106-v1'
w = DEFAULT_WEIGHT_CONFIG['weights']; import math
assert math.fsum([w['domain'], w['stack'], w['team']]) <= 0.5 * math.fsum(w.values())
hits = [{'id': 'n1', 'score': 0.016, 'payload': {'repository_id': 'r1', 'node_id': 'n1', 'facets': {}}}]
legacy = aggregate_and_score(hits)
assert set(legacy[0].breakdown) == {'text', 'breadth'} and legacy[0].criticality is None
meta = {'r1': {'n_r': 30, 'last_commit_at': '2026-07-01T00:00:00+00:00', 'dense_cos_max': 0.6, 'facet_scores': {'domain': {'score': 1.0, 'layer': 't1'}}, 'criticality_value': '核心'}}
new = aggregate_and_score(hits, repo_meta=meta, constants={'n_bar': 60.0}, now='2026-07-29T00:00:00+00:00')
assert 'domain' in new[0].breakdown and 'activity' in new[0].breakdown and new[0].criticality == 1.0
assert abs(math.fsum(new[0].breakdown.values()) - new[0].score) < 1e-9
" && uv run pytest tests/codegraph/test_repo_router_scoring.py tests/codegraph/test_repo_router_golden.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `rg -c "datetime\.now\(\)" server/codegraph/services/repo_router_scoring.py` 返回 0（now 必须参数注入）
    - `rg -c "^import django|^from django" server/codegraph/services/repo_router_scoring.py` 返回 0（零 Django 依赖保持）
    - legacy 路径回归：既有 30 条性质测试零改动通过；`uv run pytest tests/codegraph/test_repo_router_golden.py -q` 全绿（WEIGHT_SET_VERSION 未 bump、默认行为未变）
    - 新路径 INV-R3：任意 repo_meta 组合下 `abs(fsum(breakdown.values()) - score) < 1e-9`，breakdown 无 "criticality" 键
    - breadth 反向倾斜可复现：ROUTING-RANKING §2.4 数值（study-app n_r=620 命中 6 vs onion-learning n_r=30 命中 1，n_bar=60）代入后 breadth(大仓) < breadth(小仓)
    - facet_scores 全缺失 + 无活跃度信息时退化为 text+breadth 两键（纯文本分数，正确降级语义）
  </acceptance_criteria>
  <done>六信号新路径与 legacy 路径共存于同一纯函数；DEFAULT_WEIGHT_CONFIG 可被 config/harness/replay import；golden 门禁保持全绿。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 六信号不变量与机制性质测试（INV-R1~R4 + 真值表）</name>
  <files>server/tests/codegraph/test_repo_router_scoring.py</files>
  <read_first>
    - server/codegraph/services/repo_router_scoring.py（Task 1 产物——repo_meta 契约与常数键名）
    - server/tests/codegraph/test_repo_router_scoring.py（既有 30 条测试的 helper/风格——`_make_hit` 模式沿用，新增用例不改旧用例）
    - .planning/research/ROUTING-RANKING.md §4（INV-R1~R4 原文）
  </read_first>
  <behavior>
    - INV-R1：构造 8+ 仓、含 dense_cos_max 超界（>c_hi/<c_lo）、facet 全缺/全有、废弃仓的输入，所有候选 0 <= score <= 1 且源码无 min(score, 1.0) 截断
    - INV-R2（可证明推论落测试）：DEFAULT weights 下构造 S_text 落后首位 0.5 以上的仓，任意元数据组合（全给 1.0）都无法使其升至第一
    - INV-R3：新路径每候选 abs(fsum(breakdown)-score) < 1e-9；任一信号缺失（重归一化）后仍成立；text+breadth 两键之和 == w_text*S_text/D
    - INV-R4：关掉任一信号（该键权重置 0）后，其余信号贡献的两两比值不变（容差 1e-9）
    - 尺寸偏置机制：§2.4 数值代入，breakdown 的 breadth 键满足 大仓 <= 小仓（106-08 golden 机制断言的纯函数前置）
    - 活跃度衰减单调：同仓 last_commit_at 距 now 30/180/365/730 天 → activity 贡献严格递减；180 天时 A≈0.5^((180-14)/180)（公式字面复算）；730 天时 floor=0.05 生效；疑似废弃 + 近期 commit → A 仍 <= 0.10（封顶跨来源，Pitfall 4 真值表四行全覆盖：连续/枚举/皆无/废弃封顶）
    - 关键程度 tie-break：两仓分数同带（|ΔS| < 0.03）且 criticality_value 分别为 核心/边缘 → 核心排前；分差跨带（>= 一个 crit_band 桶）→ 分数决序不受 criticality 影响；criticality 缺失按 0.4 居中
    - 乱序确定性：新路径（含完整 repo_meta）输入打乱 100 seed，输出逐字段（含 criticality）相等
    - legacy 等价：repo_meta=None 时输出与 Phase 105 基准逐字段相等（守护「旧调用零破坏」）
    - now 缺失：新路径 now=None 且无枚举 facet → activity 不可用走重归一化，不抛异常
  </behavior>
  <action>
    在 `test_repo_router_scoring.py` 追加测试类（建议 `TestMultiSignalInvariants` / `TestActivityDecay` / `TestCriticalityTieBreak` / `TestSizeBiasMechanism`），覆盖 behavior 全部条目。新增 helper `_make_meta(rid, **kwargs)` 构造 repo_meta 条目（默认全缺，kwargs 按需覆盖）。机制级断言优先（锁性质不锁名次，per ROUTING-RANKING §7.4）。既有 30 条用例一行不改（legacy 守护即它们本身）。纯函数测试不依赖 Django DB。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/codegraph/test_repo_router_scoring.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/codegraph/test_repo_router_scoring.py -q` 全绿，用例总数 >= 55（既有 30 + 新增 >= 25）
    - 测试文件含字面 `INV-R2` 与 `INV-R4`（新增不变量可追溯）
    - Pitfall 4 真值表四行（连续衰减/枚举回退/皆无→不可用/废弃封顶跨来源）各有独立断言
    - `uv run pytest tests/codegraph -q` 全绿（含 golden 门禁——版本未 bump、legacy 默认行为未变）
  </acceptance_criteria>
  <done>六信号新路径的全部锁定语义被性质测试守护；106-03/06/07/08 依赖的契约不可回归。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo_meta（router 层组装）→ 打分核心 | last_commit_at/dense_cos_max/facet_scores 为上游 I/O 产物，类型与取值需容错 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-106-01 | DoS | aggregate_and_score 新路径 | mitigate | ISO 时间戳解析失败/类型错误 → 该信号标不可用（不抛）；denom<=0 防御分支保留；cos 超界经 clip 吸收 |
| T-106-02 | Tampering | constants 注入 | mitigate | constants 与默认 merge，缺键补 DEFAULT_WEIGHT_CONFIG；非法值（如 crit_band<=0）按默认处理——严格校验在 106-02 loader/view 层 |
| T-106-SC | Tampering | 包安装 | accept | 零新增依赖（RESEARCH Package Legitimacy Audit：无安装项，禁 numpy/scipy） |
</threat_model>

<verification>
- `cd server && uv run pytest tests/codegraph/test_repo_router_scoring.py tests/codegraph/test_repo_router_golden.py tests/codegraph/test_repo_router_replay.py -q` 全绿
- `cd server && uv run ruff check codegraph/services/repo_router_scoring.py tests/codegraph/test_repo_router_scoring.py && uv run ruff format --check codegraph/services/repo_router_scoring.py tests/codegraph/test_repo_router_scoring.py`
</verification>

<success_criteria>
- 六信号纯函数就位：尺寸偏置消除机制、元数据消费、活跃度连续化、关键程度 tie-break 全部被性质测试锁定
- repo_meta=None legacy 路径逐字段不变，golden 门禁与 replay 测试零改动全绿
- DEFAULT_WEIGHT_CONFIG 成为全 phase 唯一默认配置来源
</success_criteria>

<output>
完成后创建 `.planning/phases/106-multi-signal-scoring/106-01-SUMMARY.md`。
</output>
