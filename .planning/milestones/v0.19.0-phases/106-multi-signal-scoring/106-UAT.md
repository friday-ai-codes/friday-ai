---
status: testing
phase: 106-multi-signal-scoring
source: [106-VERIFICATION.md]
started: 2026-07-29T16:40:00Z
updated: 2026-07-29T16:40:00Z
---

## Current Test

number: 1
name: 生产 N_r/N̄ 快照写入（对 ROUTE-03 影响最大）
expected: |
  在生产实例执行 `uv run python manage.py measure_repo_index_stats --write-snapshot`，
  写入 `repo_router.nr_snapshot`（N_r 全表 + N̄ 中位数）。未写入前生产 breadth 一直走
  `denom_size=1.0` 降级路径，pivoted 尺寸归一化在生产上尚未真正启用。
  索引重建后需重跑该命令刷新快照。
awaiting: user response

## Tests

### 1. 生产 N_r/N̄ 快照写入
expected: `repo_router.nr_snapshot` 已写入且非空；复跑一次真实路由，候选 breakdown 中 breadth 项不再是降级值（`denom_size` 反映真实 N_r/N̄）。
result: [pending]

### 2. O-2 余弦校准数字回填
expected: 生产执行 `calibrate_repo_router_metadata`（负样本 p95→c_lo、正样本 p50→c_hi），把区间与逐 facet 区分度回填 106-MEASUREMENTS.md；区分度 <0.10 的 facet 写入 `t2_disabled_facets`。管线已就绪并按初值运行，本项为常数定版。
result: [pending]

### 3. 生产 dense 覆盖率与 S_top 实际口径
expected: 观测生产路由的 `stage0.s_top_source`——确认实际走「校准余弦」还是「RRF 回退」口径（BL-01 修复后为 per-query 二选一）。决定 `s_top_c_lo/c_hi` 是否值得校准。
result: [pending]

### 4. 前端权重设置区交互与渲染
expected: 管理页 RAG tab 的「仓库路由权重」区：五权重网格下拉可选、常数可编辑、INV-R2/区间预校验拦截非法输入、后端 400 的 errors 逐条显示、保存后回读并提示「下一次路由立即生效」；T2 弃用维度多选可用。组件目前零单测（MN-05 deferred），行为需目视确认。
result: [pending]

## Summary

total: 4
passed: 0
issues: 0
pending: 4
skipped: 0
blocked: 0

## Gaps
