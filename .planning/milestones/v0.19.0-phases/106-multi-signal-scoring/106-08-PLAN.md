---
phase: 106-multi-signal-scoring
plan: 08
type: execute
wave: 4
depends_on: [106-01, 106-06]
files_modified:
  - server/codegraph/services/repo_router_scoring.py
  - server/codegraph/services/repo_router_eval.py
  - server/tests/codegraph/test_repo_router_golden.py
  - server/tests/codegraph/fixtures/repo_router_golden/golden_main.json
  - server/tests/codegraph/fixtures/repo_router_golden/golden_holdout.json
  - server/tests/codegraph/fixtures/repo_router_golden/golden_baseline.json
autonomous: true
requirements: [ROUTE-03, ROUTE-04, ROUTE-05]
must_haves:
  truths:
    - "gk-001「高三提分专项」在新公式下 Top-1 == onion-learning（翻转完成）；断言锁定机制而非名次：breakdown 中 study-app 的 breadth 贡献 <= onion-learning 的 breadth 贡献、rank(onion-learning) < rank(study-app)、study-course 与 study-user-status 进入 Top-5（SC-1 全三条）"
    - "GENERATE_GOLDEN=1 重建后的 baseline：weight_set_version == 'phase106-v1'、Recall@5 >= 0.9643（不低于 105 基线）、Top-1 正确数 == 14/14（gk-001 翻转后唯一错例消失）、误自动选中率 <= 10%——门禁三规则在新基线下继续生效"
    - "golden 门禁在默认 pytest suite 全绿且全量评估 < 10s 零网络（fixture 内联 repo_meta/facet 匹配分，T2 不参与离线评估——机制断言不依赖 embedding）"
    - "hold-out 6 条仅做同形字段扩展（结构性编辑），opened_count 保持 0、门禁测试文件对其零引用（封存纪律不破）"
  artifacts:
    - path: server/tests/codegraph/fixtures/repo_router_golden/golden_main.json
      provides: "14 条 case 扩展：hit facets 五维 + case 级 repo_meta（n_r/last_commit_at/facet_scores 内联）+ scored_at + constants(n_bar)"
    - path: server/tests/codegraph/fixtures/repo_router_golden/golden_baseline.json
      provides: "phase106-v1 新基线（GENERATE_GOLDEN=1 生成，非手写）"
    - path: server/tests/codegraph/test_repo_router_golden.py
      provides: "gk-001 机制断言三件套 + 新基线门禁"
  key_links:
    - from: server/codegraph/services/repo_router_eval.py
      to: server/codegraph/services/repo_router_scoring.py
      via: "evaluate_cases 按 case 携带的 repo_meta/constants/scored_at 走新路径（与 route()/replay 同一纯函数）"
      pattern: "repo_meta"
    - from: server/tests/codegraph/test_repo_router_golden.py
      to: server/codegraph/services/repo_router_scoring.py
      via: "版本守护断言绑定 WEIGHT_SET_VERSION == 'phase106-v1'（bump 与 baseline 重建同一提交生效）"
      pattern: "WEIGHT_SET_VERSION"
---

<objective>
本 phase 的验收锚点（SC-1）：golden fixture 扩展出离线评估新公式所需字段（hit facets 五维补全、case 级 repo_meta——n_r/last_commit_at/facet 匹配分内联、scored_at 固定时间戳、constants.n_bar）；`evaluate_cases` 透传新参数走六信号路径；`WEIGHT_SET_VERSION` bump 至 `"phase106-v1"`（与 105-01 注释约定一致）；`GENERATE_GOLDEN=1` 重建 baseline 并人工 review 逐例 diff；机制级断言三件套落 `test_repo_router_golden.py`（gk-001 `_notice` 注明的「由 Phase 106 翻转」在此兑现）。

离线评估口径（RESEARCH §8 裁决）：harness 走 T1-only + fixture 内联 facet 匹配分（repo_meta.facet_scores 直接给数值）——机制断言不依赖 T2/embedding，`--disable-socket` 下零网络。gk-001 的 repo_meta 数值按 ROUTING-RANKING §2.4 示意：N_r(study-app)=620、N_r(onion-learning)=30、n_bar=60。

Purpose: SC-1 兑现 + 此后所有权重/公式改动的回归标尺切换到六信号口径。
Output: fixture 扩展 + evaluate_cases 扩展 + 版本 bump + 新 baseline + 机制断言。
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/106-multi-signal-scoring/106-CONTEXT.md
@.planning/phases/106-multi-signal-scoring/106-RESEARCH.md
@.planning/research/ROUTING-RANKING.md
@server/codegraph/services/repo_router_eval.py
@server/tests/codegraph/test_repo_router_golden.py
@server/tests/codegraph/fixtures/repo_router_golden/golden_main.json
</context>

## Artifacts this phase produces（本 plan 新建符号）

| Symbol | Kind | Location |
|--------|------|----------|
| golden case 新字段：`repo_meta`（`{rid: {n_r, last_commit_at, dense_cos_max?, facet_scores?, criticality_value?}}`）、`scored_at`、`constants`（`{"n_bar": ...}`）、可选 `weight_overrides` | fixture 契约 | `golden_main.json` / `golden_holdout.json` |
| `evaluate_cases(..., )` 消费 case 新字段（repo_meta 存在 → 新路径：DEFAULT_WEIGHT_CONFIG weights + case constants merge + now=scored_at） | 函数行为扩展 | `server/codegraph/services/repo_router_eval.py` |
| `WEIGHT_SET_VERSION = "phase106-v1"` | 常量 bump | `server/codegraph/services/repo_router_scoring.py` |
| `test_gk001_mechanism_*` 机制断言（breadth 反向倾斜 / rank 翻转 / 跨组进 Top-5） | 测试 | `server/tests/codegraph/test_repo_router_golden.py` |
| phase106-v1 baseline（Recall@5 / MRR@10 / Top-1 / 误自动选中率 + bootstrap CI） | 生成物 | `golden_baseline.json` |

<tasks>

<task type="auto">
  <name>Task 1: fixture 字段扩展 + evaluate_cases 新路径透传</name>
  <files>server/tests/codegraph/fixtures/repo_router_golden/golden_main.json, server/tests/codegraph/fixtures/repo_router_golden/golden_holdout.json, server/codegraph/services/repo_router_eval.py</files>
  <read_first>
    - server/tests/codegraph/fixtures/repo_router_golden/golden_main.json（14 条 case 现行形状：{id, _notice, query, label_source, cross_group, expected_repos, node_hits}——gk-001 的 _notice 场景设定与命中结构）
    - server/codegraph/services/repo_router_eval.py:232-330（evaluate_cases/CaseResult——θ 注入模式，新参数同款）
    - server/codegraph/services/repo_router_scoring.py（repo_meta 键契约与 DEFAULT_WEIGHT_CONFIG——fixture 字段必须严格同形）
    - .planning/research/ROUTING-RANKING.md §2.4（gk-001 数值示意表——N_r/n_bar/breadth 手算值，fixture 取值依据）
  </read_first>
  <action>
    1. `golden_main.json` 全部 14 条 case 扩展（合成数据受审原则：**元数据取值必须与 expected_repos 语义一致**，不得让新信号与标签打架造成无关退化）：
       - hit `payload.facets` 五维补全（业务线/产品线、技术栈、团队归属、关键程度、活跃度——合理值；原有活跃度值不变）；
       - case 级 `repo_meta`：每个出现在 node_hits 的仓给 `n_r`（与仓的体量设定一致）与 `last_commit_at`（相对 scored_at 的合理天数——原「疑似废弃仓参与」case 保持废弃语义）；`facet_scores` 内联匹配分（T1-only 口径：与 query 是否提到该 facet 一致，未提 → 省略该键 = 信号缺失）；`criticality_value` 按 facets；`dense_cos_max` 仅 gk-001 等需要 S_top 区分度的 case 给值，其余省略（省略 → S_top 回退 RRF s_hat，行为与 105 文本信号连续）；
       - case 级 `scored_at`（固定 `"2026-07-29T00:00:00+00:00"`）与 `constants: {"n_bar": 60}`（全集统一，gk-001 数值依据 §2.4）；
       - gk-001 专项：`repo_meta["study-app"].n_r = 620`、`repo_meta["onion-learning"].n_r = 30`；dense_cos_max 给 onion-learning 单个强命中高余弦（如 0.62）、study-app 中等（如 0.52）——与 `_notice`「6 个中等命中 vs 1 个 top 命中」场景一致；study-course/study-user-status 的 meta 保证其进 Top-5（勿被新信号压出）。更新 gk-001 `_notice`：「Phase 106 已按 pivoted normalization 翻转（106-08）」。
    2. `golden_holdout.json` 同形字段扩展（**结构性编辑**：顶层 `opened_count` 保持 0、`opened_log` 不动、追加 `_notice` 说明「106-08 仅做字段同形扩展，未参与评估」）。
    3. `evaluate_cases` 扩展：case 含 `repo_meta` → `aggregate_and_score(hits, weights={**DEFAULT_WEIGHT_CONFIG["weights"], **case.get("weight_overrides", {})}, repo_meta=case["repo_meta"], constants={**DEFAULT_WEIGHT_CONFIG["constants"], **case.get("constants", {})}, now=case.get("scored_at"))`；不含 → legacy 路径（向后兼容）。模块保持零 Django/numpy import（既有纪律）。CaseResult 附 per-case breakdown 已有则透传新键。
  </action>
  <verify>
    <automated>cd server && uv run python -c "
import json, pathlib
main = json.loads(pathlib.Path('tests/codegraph/fixtures/repo_router_golden/golden_main.json').read_text())
cases = main if isinstance(main, list) else main.get('cases', main)
assert all('repo_meta' in c and 'scored_at' in c for c in cases), 'main cases missing new fields'
gk = next(c for c in cases if c['id'].startswith('gk-001'))
assert gk['repo_meta']['study-app']['n_r'] == 620 and gk['repo_meta']['onion-learning']['n_r'] == 30
hold = json.loads(pathlib.Path('tests/codegraph/fixtures/repo_router_golden/golden_holdout.json').read_text())
assert hold.get('opened_count') == 0
" && uv run pytest tests/codegraph/test_repo_router_scoring.py -q</automated>
  </verify>
  <acceptance_criteria>
    - 14 条 main case 全部含 repo_meta/scored_at/constants；gk-001 数值与 §2.4 一致（620/30/60）
    - `rg -c "golden_holdout" server/tests/codegraph/test_repo_router_golden.py` == 0（封存零引用保持）且 holdout opened_count == 0
    - `rg -c "import numpy|from numpy|import django|from django" server/codegraph/services/repo_router_eval.py` == 0
    - evaluate_cases 对不含 repo_meta 的 case 仍走 legacy（构造一条最小 case 断言，防旧口径破坏）
  </acceptance_criteria>
  <done>fixture 与 harness 具备六信号离线评估能力，gk-001 事故机制以数据形式完整编码。</done>
</task>

<task type="auto">
  <name>Task 2: WEIGHT_SET_VERSION bump + 机制断言 + GENERATE_GOLDEN=1 重建 baseline + 门禁核对</name>
  <files>server/codegraph/services/repo_router_scoring.py, server/tests/codegraph/test_repo_router_golden.py, server/tests/codegraph/fixtures/repo_router_golden/golden_baseline.json</files>
  <read_first>
    - server/tests/codegraph/test_repo_router_golden.py（版本守护断言 test_golden_gate_vs_baseline 首个 assert / GENERATE_GOLDEN 重生成路径 / 既有 gk-001 召回断言扩展点）
    - server/tests/codegraph/fixtures/repo_router_golden/golden_baseline.json（105 基线值：recall@5=0.9643、top1=13/14、误自动选中率=0.0——新基线的比较锚）
    - .planning/research/ROUTING-RANKING.md §7.4（机制级断言 > 结果级断言——三条断言原文）
  </read_first>
  <action>
    1. `repo_router_scoring.py`：`WEIGHT_SET_VERSION` 改为 `"phase106-v1"`（与 DEFAULT_WEIGHT_CONFIG["weight_set_version"] 一致——加断言注释或直接 `WEIGHT_SET_VERSION = DEFAULT_WEIGHT_CONFIG["weight_set_version"]` 单一来源）；删除 105 的「bump 归 106-08」过渡注释。
    2. `test_repo_router_golden.py` 新增机制断言（§7.4 三件套，基于 evaluate_cases 对 gk-001 的输出）：
       - `test_gk001_mechanism_breadth_not_favor_monolith`：gk-001 候选 breakdown 中 `study-app 的 "breadth" 贡献 <= onion-learning 的 "breadth" 贡献`（尺寸偏置已消除的因果性质）；
       - `test_gk001_mechanism_rank_flipped`：`rank(onion-learning) < rank(study-app)`；
       - `test_gk001_cross_group_repos_in_top5`：`study-course` 与 `study-user-status` 均在 gk-001 Top-5（SC-1 后半句；既有「进候选集合」断言升级为进 Top-5）。
       注释标注：不断言权重敏感的绝对名次之外的内容——三条锁机制，抗权重微调（ROUTING-RANKING §7.4）。
    3. 重建 baseline：`GENERATE_GOLDEN=1 uv run pytest tests/codegraph/test_repo_router_golden.py -q` → `git diff` 逐例 review（diff_reports 输出记入 SUMMARY：哪些 case 变好/变坏及其 breakdown 对照——预期唯一显著变化为 gk-001 翻转）；若出现 gk-001 之外的 regressed case，回 Task 1 修 fixture 元数据取值与标签的一致性（合成数据受审），**不得**通过调低新信号权重来凑基线（权重是 CONTEXT 锁定初值）。
    4. 门禁核对（正常模式重跑）：baseline `weight_set_version == "phase106-v1"`、`recall_at_5 >= 0.9643`、`top1_correct == 14`、误自动选中率 <= 0.10、全量评估 < 10s。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/codegraph/test_repo_router_golden.py -q && uv run python -c "
import json, pathlib
b = json.loads(pathlib.Path('tests/codegraph/fixtures/repo_router_golden/golden_baseline.json').read_text())
assert b['weight_set_version'] == 'phase106-v1', b['weight_set_version']
assert b['recall_at_5'] >= 0.9643 - 1e-9
assert b['top1_correct'] == 14
assert b['false_auto_select_rate'] <= 0.10
"</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/codegraph/test_repo_router_golden.py -q` 全绿且总耗时 < 10s（既有耗时断言覆盖）
    - 机制断言三条独立存在且引用 breakdown["breadth"]（`rg -c 'breadth' server/tests/codegraph/test_repo_router_golden.py` >= 1）
    - baseline 四字段核对（automated verify 的 python 断言）：版本/Recall@5/Top-1=14/护栏
    - `rg -c '"phase105-v1"' server/codegraph/services/repo_router_scoring.py` == 0（旧版本字符串仅允许存在于 replay 回退检测，scoring 模块清零）
    - 收官全量：`uv run pytest tests/codegraph tests/delivery tests/services/test_repo_router_adapter.py -q` 全绿
  </acceptance_criteria>
  <done>gk-001 翻转兑现、机制断言进门禁、phase106-v1 基线成为此后一切排序改动的回归标尺（SC-1 完整成立）。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture 合成数据 → 门禁判定 | fixture 元数据取值若与标签打架会制造假红/假绿 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-106-19 | Repudiation | baseline 重建 | mitigate | baseline 仅经 GENERATE_GOLDEN=1 生成（非手写）；逐例 diff review 记入 SUMMARY；版本守护断言防「改公式不 bump 版本」（Pitfall 8） |
| T-106-20 | Tampering | fixture 元数据取值 | mitigate | 合成数据受审原则写入 Task 1 action；机制断言锁因果性质（不锁名次）抗过拟合；hold-out 零引用静态可验证 |
| T-106-SC | Tampering | 包安装 | accept | 零新增依赖 |
</threat_model>

<verification>
- `cd server && uv run pytest tests/codegraph tests/delivery tests/services/test_repo_router_adapter.py -q` 全绿（phase 收官口径）
- `cd server && uv run ruff check codegraph/services/repo_router_eval.py codegraph/services/repo_router_scoring.py tests/codegraph/test_repo_router_golden.py`
- `cd web && pnpm vitest run src/components/chat/__tests__/RoutingDecisionPanel.test.ts`（前端标签回归，105-06/106-05 口径）
</verification>

<success_criteria>
- SC-1 三断言全部成立：breadth 反向倾斜（机制）、onion-learning 排前（翻转）、跨组两仓进 Top-5
- phase106-v1 baseline：Recall@5 >= 0.9643、Top-1 = 14/14、误自动选中率 <= 10%、评估 < 10s 零网络
- hold-out 封存纪律不破（opened_count=0、门禁零引用）
</success_criteria>

<output>
完成后创建 `.planning/phases/106-multi-signal-scoring/106-08-SUMMARY.md`。
</output>
