---
phase: 106-multi-signal-scoring
plan: 07
type: execute
wave: 4
depends_on: [106-06]
files_modified:
  - server/codegraph/services/repo_router_replay.py
  - server/tests/codegraph/test_repo_router_replay.py
autonomous: true
requirements: [ROUTE-06]
must_haves:
  truths:
    - "106-06 产出的新格式快照（weight_config + repo_meta + scored_at）经 replay_route_from_snapshot 零网络重算，候选顺序/分数/breakdown（含 domain/stack/team 键）/confidence 逐字段与记录相等——权重与元数据从快照读取，不依赖当时的 SystemSetting"
    - "105 旧快照（versions.weight_set_version=='phase105-v1' 或缺 weight_config 节）回放不抛异常：回退 PHASE105_WEIGHTS + legacy 三信号路径重算同结果，diff 输出标注「旧版本快照（phase105-v1），按当时版本比对」——版本不同即不可比，不做跨版本换算（research §6.2-9）"
    - "50 hits 满配 + 12 候选 repo_meta + weight_config 全值的新格式快照 < 64KB（体积护栏在新字段下复核通过，Pitfall 5）"
    - "replay 模块保持零 I/O import 纯度（仅 stdlib + repo_router_scoring，import 纯度测试对新代码继续成立）"
  artifacts:
    - path: server/codegraph/services/repo_router_replay.py
      provides: "双版本回放：新快照读 weight_config/repo_meta/scored_at；旧快照 legacy 回退 + 标注"
    - path: server/tests/codegraph/test_repo_router_replay.py
      provides: "新格式回放等值 / 旧快照回退 / 篡改拦截 / 64KB 复核守护"
      min_lines: 380
  key_links:
    - from: server/codegraph/services/repo_router_replay.py
      to: server/codegraph/services/repo_router_scoring.py
      via: "新路径 aggregate_and_score(weights=快照值, repo_meta=快照值, constants=快照值, now=scored_at)；旧路径 aggregate_and_score(hits)（legacy 默认）——与 route()/golden 共用同一纯函数"
      pattern: "aggregate_and_score"
---

<objective>
恢复「快照可离线回放零网络同结果」在六信号时代的成立（ROUTE-09 既有性质 × ROUTE-06 版本绑定）：`replay_route_from_snapshot` 按快照内 `weight_config` 节分流——新格式快照从 payload 读权重/常数/repo_meta/scored_at 注入纯函数（外部 I/O 产物记录为数据、回放消费数据——105-07 既定模式的延续）；旧快照（phase105-v1 / 缺 weight_config）回退 `PHASE105_WEIGHTS` 与 legacy 路径并在输出标注版本，不做跨版本比较。`verify_snapshot_replay` 比对键（repo_id/score/breakdown/confidence）不变——breakdown 键集合比对天然覆盖新信号。

采用 TDD：先落守护用例（RED），再改 replay 实现（GREEN）——回放正确性是本 phase 的确定性底线。

Purpose: SC-4「跨版本结果不被混作同一口径比较」的回放侧闭环；Phase 107+ 的权重调参回放底座。
Output: 双版本 replay + 守护测试。
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/106-multi-signal-scoring/106-CONTEXT.md
@.planning/phases/106-multi-signal-scoring/106-RESEARCH.md
@server/codegraph/services/repo_router_replay.py
@server/tests/codegraph/test_repo_router_replay.py
@server/codegraph/services/repo_router_v2.py
</context>

## Artifacts this phase produces（本 plan 新建符号）

| Symbol | Kind | Location |
|--------|------|----------|
| `replay_route_from_snapshot` 双版本分流（新格式读 weight_config/repo_meta/scored_at；旧格式 legacy 回退） | 函数行为扩展 | `server/codegraph/services/repo_router_replay.py` |
| `LEGACY_SNAPSHOT_NOTE = "旧版本快照（phase105-v1），按当时版本比对"` | 常量（diff/结果标注字符串） | 同上 |
| replay 结果新字段：`weight_set_version`（本次回放采用的版本）、`legacy_snapshot: bool` | 返回值契约 | 同上 |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): 双版本回放守护用例</name>
  <files>server/tests/codegraph/test_repo_router_replay.py</files>
  <read_first>
    - server/tests/codegraph/test_repo_router_replay.py（既有 8 用例——快照构造走真实 _h_route 组装路径的模式，新用例沿用）
    - server/codegraph/services/repo_router_v2.py（106-06 后 _build_snapshot 的新形状：weight_config/repo_meta/stage0.scored_at 节）
    - server/codegraph/services/repo_router_replay.py（现行 _rebuild_hits/replay/verify 签名——RED 用例基于将扩展的行为写断言）
  </read_first>
  <behavior>
    - 新格式回放等值：构造含完整 repo_meta（n_r/last_commit_at/dense_cos_max/facet_scores 全有）+ weight_config（非默认权重，如 domain 0.20）+ scored_at 的快照 → replay 输出候选顺序/score/breakdown（含 domain 键）/confidence 与记录逐字段相等（score 容差 1e-9，round 口径对齐 to_dict）
    - 权重来自快照而非环境：把 DEFAULT_WEIGHT_CONFIG mock/monkeypatch 改成另一组值 → 同一快照 replay 结果不变（快照自包含）
    - scored_at 确定性：同一快照相隔调用两次（不同真实时间）→ 活跃度贡献相同（衰减锚点取快照 scored_at 而非 now）
    - 旧快照回退：既有测试里 phase105 形状快照（无 weight_config 节）→ 回放不抛、结果与 legacy 重算相等、返回值 legacy_snapshot=True 且标注字符串出现在 verify diff 文本（构造不等值场景时）
    - 篡改拦截保持：新格式快照篡改某候选 breakdown["domain"] → verify_snapshot_replay 报 diff
    - 64KB 复核：50 node_hits + 12 候选满配 repo_meta + weight_config 全值 → 序列化 payload < 64KB
    - import 纯度：模块仍禁 django/qdrant/langchain import（既有源码扫描测试对新代码生效）
  </behavior>
  <action>
    在 `test_repo_router_replay.py` 追加测试类（建议 `TestMultiSignalReplay` / `TestLegacySnapshotFallback`），覆盖 behavior 全条目。新格式快照构造沿用「真实组装路径」纪律：优先经 106-06 的 `_build_snapshot`（mock I/O 供数）生成，不手写 payload 字面量。既有 8 用例一行不改（legacy 路径回归即其本身）。提交 RED：新用例对现行实现失败（新格式回放读不到权重 → 数值不等）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/codegraph/test_repo_router_replay.py -q; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - 新增用例 >= 7 且在未改 replay 实现前至少 3 条失败（RED 证据，提交信息注明）
    - 既有 8 用例保持全绿（RED 提交不破坏 legacy 守护）
  </acceptance_criteria>
  <done>六信号回放契约以失败测试形式固化。</done>
</task>

<task type="auto">
  <name>Task 2 (GREEN): replay 双版本实现</name>
  <files>server/codegraph/services/repo_router_replay.py</files>
  <read_first>
    - server/codegraph/services/repo_router_replay.py:48-214（_rebuild_hits/replay_route_from_snapshot/verify_snapshot_replay 全文——node_id 回填 payload 的既有契约保持）
    - server/codegraph/services/repo_router_scoring.py（aggregate_and_score 新签名 + DEFAULT_WEIGHT_CONFIG + PHASE105_WEIGHTS）
    - server/tests/codegraph/test_repo_router_replay.py（Task 1 的 RED 用例——实现目标）
  </read_first>
  <action>
    改造 `replay_route_from_snapshot`：
    1. 版本分流：`payload.get("weight_config")` 存在且含 weights → 新路径；否则（含 `versions.weight_set_version == "phase105-v1"`）→ legacy 路径。
    2. 新路径：`aggregate_and_score(_rebuild_hits(...), weights=wc["weights"], repo_meta=payload.get("repo_meta") or {}, constants=wc["constants"], now=payload["stage0"].get("scored_at"))`——常数含快照当时生效的 n_bar，全部自包含；`repo_meta` 只覆盖候选仓（≤12），非候选分桶仓在重算中自然拿不到 meta——与录制时不同？**注意**：录制时全部分桶仓有 meta、快照只存候选仓——重算时非候选仓分数可能有差，但 verify 比对键只含候选（既有语义），且候选相对序由候选自身信号决定；在 docstring 记录该边界（非候选仓不参与比对）。
    3. legacy 路径：现行实现不动（`aggregate_and_score(hits)` 默认 legacy），返回值补 `legacy_snapshot=True`；新路径 `legacy_snapshot=False`。返回值统一补 `weight_set_version`（新：wc 值；旧：`"phase105-v1"`）。
    4. `verify_snapshot_replay`：比对键与容差不变；`legacy_snapshot=True` 时 diff 文本头部加 `LEGACY_SNAPSHOT_NOTE` 常量行。
    5. `_deterministic_confidence` 阈值注入路径不变（θ 与权重无关）。模块 docstring 更新双版本契约 + 「版本不同即不可比」原则引用（research §6.2-9）。零 I/O 纯度保持（新增 import 仅来自 repo_router_scoring / stdlib）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/codegraph/test_repo_router_replay.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/codegraph/test_repo_router_replay.py -q` 全绿（既有 8 + 新增 >= 7）
    - `rg -c "phase105-v1" server/codegraph/services/repo_router_replay.py` >= 1（显式回退检测）
    - `rg -c "^import |^from " server/codegraph/services/repo_router_replay.py` 的 import 仅含 stdlib 与 codegraph.services.repo_router_scoring（import 纯度测试自动守护）
    - `uv run pytest tests/codegraph tests/delivery tests/services/test_repo_router_adapter.py -q` 全绿
  </acceptance_criteria>
  <done>新旧两代快照都可零网络回放：新格式逐字段等值，旧格式按当时版本比对并显式标注。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ConvergenceSessionEvent 快照 payload → replay | 历史快照内容可能被篡改或字段残缺 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-106-17 | Tampering | verify_snapshot_replay | mitigate | 篡改 breakdown/confidence 的快照与重算不一致被 diff 拦截（既有性质延续到新信号键） |
| T-106-18 | DoS | replay 双版本分流 | mitigate | weight_config 节残缺/类型错误 → 视为 legacy 回退路径，不抛；repo_meta 缺仓按信号不可用处理 |
| T-106-SC | Tampering | 包安装 | accept | 零新增依赖 |
</threat_model>

<verification>
- `cd server && uv run pytest tests/codegraph/test_repo_router_replay.py tests/codegraph tests/delivery -q` 全绿
- `cd server && uv run ruff check codegraph/services/repo_router_replay.py tests/codegraph/test_repo_router_replay.py`
</verification>

<success_criteria>
- 新格式快照零网络逐字段回放；105 旧快照回退默认值并标注，不混口径比较（SC-4 回放侧）
- 64KB 体积护栏在新字段下复核通过；replay import 纯度保持
</success_criteria>

<output>
完成后创建 `.planning/phases/106-multi-signal-scoring/106-07-SUMMARY.md`。
</output>
