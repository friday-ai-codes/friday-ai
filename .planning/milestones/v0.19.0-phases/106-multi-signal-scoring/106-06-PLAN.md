---
phase: 106-multi-signal-scoring
plan: 06
type: execute
wave: 3
depends_on: [106-01, 106-02, 106-03]
files_modified:
  - server/services/qdrant_service.py
  - server/codegraph/services/repo_router_v2.py
  - server/codegraph/services/repo_router_config.py
  - server/tests/codegraph/test_repo_router_v2_meta.py
autonomous: true
requirements: [ROUTE-03, ROUTE-04, ROUTE-05, ROUTE-06]
must_haves:
  truths:
    - "一次路由 = 既有 hybrid 检索 + 恰一次额外 dense-only Qdrant 查询（复用已算好的 query_dense，零额外 embedding）+ 恰一次 FileIndex 聚合 DB 查询——无 N+1；dense 查询失败/某仓不在 dense top-50 时该仓 S_top 回退 RRF s_hat，路由不失败（Pitfall 6）"
    - "需求文本提到业务域/技术栈/团队时，路由候选 breakdown 出现 domain/stack/team 贡献键且 Σ==score；仓库元数据缺失时该键不出现、其余重归一化（SC-2 端到端）"
    - "保存新权重配置后（无重启）下一次 route() 按新值打分；快照 versions.weight_set_version 与 weight_config 节记录本次生效的完整权重/常数/alias_dict_hash——跨版本结果不被混作同一口径（SC-4）"
    - "快照携带 per-候选 repo_meta（n_r/last_commit_at/dense_cos_max/facet_scores/criticality_value）与 scored_at——T2 余弦与 DB 聚合以数据形式记录，106-07 回放可零网络消费"
    - "权重配置缺失/非法、nr_snapshot 缺失、embedding 未配置、dense 查询异常四种降级下路由全部可用（观测 warning，不反噬业务）"
  artifacts:
    - path: server/codegraph/services/repo_router_v2.py
      provides: "Stage 0 repo_meta 组装（resolver 编排）+ 新签名打分注入 + 快照扩展"
    - path: server/services/qdrant_service.py
      provides: "dense_search_by_name（using=\"dense\" 单独查询封装）"
      exports: ["dense_search_by_name"]
    - path: server/tests/codegraph/test_repo_router_v2_meta.py
      provides: "repo_meta 组装/降级矩阵/快照携带/保存即生效 集成测试"
      min_lines: 200
  key_links:
    - from: server/codegraph/services/repo_router_v2.py
      to: server/codegraph/services/repo_router_scoring.py
      via: "aggregate_and_score(node_hits, weights=…, repo_meta=…, constants=…, now=…) 新路径注入"
      pattern: "repo_meta="
    - from: server/codegraph/services/repo_router_v2.py
      to: server/codegraph/services/repo_router_config.py
      via: "aload_weight_config / aload_nr_snapshot / load_alias_dict（保存即生效链路）"
      pattern: "aload_weight_config"
    - from: server/codegraph/services/repo_router_v2.py
      to: server/repositories/models.py
      via: "FileIndex.objects.values(\"repository_id\").annotate(Max(\"last_commit_authored_at\")) 一次聚合（sync_to_async）"
      pattern: "last_commit_authored_at"
---

<objective>
把六信号打分接入生产路由链路：`QdrantService` 新增 `dense_search_by_name`（O-3 定论——fusion 不回传 dense 余弦，必须 `using="dense"` 单独查询，照抄 `measure_repo_index_stats._verify_cosine` 探针写法）；`RepoRouterV2` Stage 0 组装 repo_meta（dense 余弦归仓 max、FileIndex last_commit 一次聚合、N_r/N̄ 快照读取、106-03 resolver 的 facet_scores、criticality_value）并以新签名调用打分核心；权重经 106-02 loader 调用时读取（保存即生效）；快照扩展——weight_config 全值 + per-候选 repo_meta + scored_at，versions.weight_set_version 从 loader 取真实版本（替换模块常量 WEIGHT_SET_VERSION 的引用，105 版本四元组占位换真）。

延迟取舍纪律（CONTEXT 锁定）：dense 余弦优先；实现后若结构性测试/本地实测显示额外往返代价不可接受，回退 RRF query-local max 并把取舍记录进 SUMMARY 与代码注释——回退路径本 plan 本就作为 dense 缺失 fallback 存在，切换只是默认值问题，不是新工程。

注意：本 plan 落地后新格式快照的 replay 兼容由 106-07（下一 wave）恢复；本 plan 必须保证既有 `test_repo_router_replay.py` / `test_repo_router_adapter.py`（legacy 构造快照）零改动全绿。

Purpose: ROUTE-03/04/05/06 的生产接线点——SC-2/SC-3/SC-4 的行为主体。
Output: dense 查询封装 + repo_meta 组装 + 打分注入 + 快照扩展 + 集成测试。
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/106-multi-signal-scoring/106-CONTEXT.md
@.planning/phases/106-multi-signal-scoring/106-RESEARCH.md
@server/codegraph/services/repo_router_v2.py
@server/codegraph/services/repo_router_scoring.py
@server/codegraph/services/repo_router_metadata.py
@server/codegraph/services/repo_router_config.py
@server/services/qdrant_service.py
@server/codegraph/management/commands/measure_repo_index_stats.py
</context>

## Artifacts this phase produces（本 plan 新建符号）

| Symbol | Kind | Location |
|--------|------|----------|
| `QdrantService.dense_search_by_name(collection_name, query_dense, *, top_k, filters=None)` | 方法（与 hybrid_search_by_name 同形，仅 `using="dense"`） | `server/services/qdrant_service.py` |
| `load_alias_dict() -> tuple[dict, str]` | loader（DEFAULT_ALIAS_DICT + SystemSetting 覆盖合并，返回 (生效词典, hash)） | `server/codegraph/services/repo_router_config.py` |
| `RepoRouterV2._load_repo_meta(...)` | 私有方法（repo_meta 组装编排） | `server/codegraph/services/repo_router_v2.py` |
| 快照新节：`weight_config`（weights/constants/weight_set_version/alias_dict_hash/embedding_model_id）、`repo_meta`（per-候选 ≤12）、`stage0.scored_at` | 快照契约（106-07 replay 的输入） | 同上 |
| `RepoRouteCandidateV2.criticality: float \| None` + `to_dict()["criticality"]` | 字段（旁路展示，不入 breakdown） | 同上 |
| 观测事件：`repo_router_meta_resolved`（debug/sampling/duration_ms）、`repo_router_dense_search_failed`（warning/sampling）、`repo_router_nr_snapshot_missing`（warning/sampling） | structlog 事件 | 同上 |

<tasks>

<task type="auto">
  <name>Task 1: dense 查询封装 + alias loader + repo_meta 组装编排</name>
  <files>server/services/qdrant_service.py, server/codegraph/services/repo_router_config.py, server/codegraph/services/repo_router_v2.py</files>
  <read_first>
    - server/services/qdrant_service.py:1317-1425（hybrid_search_by_name 的 client/filter/异常/日志形态——dense 版对称照抄）
    - server/codegraph/management/commands/measure_repo_index_stats.py:247-307（_verify_cosine 的 query_points(using="dense") 探针写法——唯一已验证口径）
    - server/codegraph/services/repo_router_v2.py:250-323（_stage0_node_search 的 query_dense 复用点 / _stage0_candidates 薄封装 / sync_to_async 模式）
    - server/codegraph/services/repo_router_metadata.py（resolve_facet_scores/FacetT2Matcher/merge_alias_dict/alias_dict_hash 签名）
    - server/codegraph/services/repo_router_config.py（aload_weight_config/aload_nr_snapshot——本 task 在此追加 load_alias_dict）
    - server/repositories/models.py:568-608（FileIndex 字段名 last_commit_authored_at）
  </read_first>
  <action>
    1. `QdrantService.dense_search_by_name`：与 `hybrid_search_by_name` 同层对称（classmethod、同 filter 构造、同返回形状 `[{"id", "score", "payload"}]`、同异常处理——失败返回空列表不抛）；核心为 `client.query_points(collection_name, query=query_dense, using="dense", query_filter=..., limit=top_k, with_payload=True)`。
    2. `repo_router_config.py` 追加 `load_alias_dict() -> tuple[dict, str]`：`get_json_setting(SettingKeys.REPO_ROUTER_ALIAS_DICT, {})` → `merge_alias_dict(DEFAULT_ALIAS_DICT, override)` → 返回 `(生效词典, alias_dict_hash(生效词典))`；坏结构容错（merge 已容错）。附 `aload_alias_dict` 包装。
    3. `RepoRouterV2._load_repo_meta(node_hits, query, query_dense, config) -> tuple[dict, dict]`（返回 `(repo_meta, meta_stats)`，meta_stats 供观测）：
       - 候选仓集合 = node_hits 分桶后的全部 repository_id（打分发生在 top_k 截断之前，须为全部分桶仓组 meta）。
       - dense 余弦：`sync_to_async(QdrantService.dense_search_by_name)(COLLECTION_NAME, query_dense, top_k=STAGE0_NODE_K, filters=同 hybrid)` → 按 payload.repository_id 归仓取 max → `dense_cos_max`；查询异常/空 → 全仓 None + `repo_router_dense_search_failed` warning（sampling）。
       - last_commit：一次聚合 `FileIndex.objects.filter(repository_id__in=ids).values("repository_id").annotate(latest=Max("last_commit_authored_at"))`（`sync_to_async(..., thread_sensitive=False)` 包装，RESEARCH Code Examples 照抄），isoformat 或 None。
       - N_r/N̄：`aload_nr_snapshot()` → per-repo `n_r = n_r_by_repo.get(rid)`、`n_bar` 进 constants；快照缺失（n_bar None）→ `repo_router_nr_snapshot_missing` warning（sampling，每次路由至多一条）——breadth 走 denom=1.0 降级（106-01 已定义）。
       - facet_scores：alias loader + `FacetT2Matcher(model_id=EmbeddingService.get_config()["model"], t2_c_lo/t2_c_hi=config.constants)`（embedding 未配置 → matcher 传 None，全走 T1）；对每仓桶首 hit 的 facets 调 `resolve_facet_scores(query, facets, alias_dict=..., constants=config, query_embedding=query_dense, t2_matcher=...)`；`criticality_value = facets.get("关键程度")`。
       - 观测：组装完成记 `repo_router_meta_resolved`（debug、sampling、component="repo_router_v2"、kv：repo_count/dense_hit_ratio/t2_used/duration_ms）；全部观测 try/except 吞异常。
    4. 本 task 结束时 `_load_repo_meta` 尚未接入 route()（Task 2 接线）——保证中间态可提交、既有测试全绿。
  </action>
  <verify>
    <automated>cd server && uv run python -c "
from services.qdrant_service import QdrantService
assert hasattr(QdrantService, 'dense_search_by_name')
from codegraph.services.repo_router_config import load_alias_dict
" && uv run pytest tests/codegraph -q</automated>
  </verify>
  <acceptance_criteria>
    - `rg -c 'using="dense"' server/services/qdrant_service.py` >= 1；dense_search_by_name 异常路径返回空列表（不抛）
    - `rg -c "annotate\(.*Max\(" server/codegraph/services/repo_router_v2.py` == 1（一次聚合，无循环内查询）
    - `rg -c "generate_embedding" server/codegraph/services/repo_router_v2.py` 不因本 task 增加（query_dense 复用，零额外 embedding）
    - `uv run pytest tests/codegraph tests/services/test_repo_router_adapter.py -q` 全绿（中间态零回归）
  </acceptance_criteria>
  <done>repo_meta 的全部 I/O 供数路径就位：+1 次 Qdrant 往返、+1 次 DB 聚合、快照/词典缓存读取，全部有降级分支。</done>
</task>

<task type="auto">
  <name>Task 2: route() 打分注入 + 快照扩展 + criticality 透传</name>
  <files>server/codegraph/services/repo_router_v2.py</files>
  <read_first>
    - server/codegraph/services/repo_router_v2.py:161-244（route() 主流程）与 423-480（_build_snapshot 现行形状 + versions 四元组）
    - server/codegraph/services/repo_router_scoring.py（aggregate_and_score 新签名与 ScoredCandidate.criticality）
    - .planning/phases/106-multi-signal-scoring/106-RESEARCH.md §7（快照必须新增的字段清单与 64KB 护栏 Pitfall 5）
  </read_first>
  <action>
    1. `route()` 流程注入（`v2_stage0_only` 降级路径同样生效——两条出口共用 Stage 0）：
       - Stage 0 检索后：`config = await aload_weight_config()`；`repo_meta, meta_stats = await cls._load_repo_meta(...)`；`scored_at = datetime.now(timezone.utc).isoformat()`（**唯一取 now 的位置**，进快照）。
       - `_stage0_candidates` 改签名透传：`aggregate_and_score(node_hits, weights=config["weights"], repo_meta=repo_meta, constants={**config["constants"], "n_bar": nr_snapshot["n_bar"]}, now=scored_at)`；dict 形状补 `criticality` 键。
       - `_finalize_stage0` → `RepoRouteCandidateV2` 新字段 `criticality: float | None = None`，`to_dict()` 增 `"criticality"`（旁路展示字段，**不进 breakdown**——前端 Σ 校验不受影响）。
    2. `_build_snapshot` 扩展（RESEARCH §7 清单）：
       - `stage0` 增 `"scored_at": scored_at`。
       - 新顶层节 `"weight_config": {"weights", "constants"(含生效 n_bar), "weight_set_version", "alias_dict_hash", "embedding_model_id"}`（本次生效全值——回放不依赖当时的 SystemSetting）。
       - 新顶层节 `"repo_meta"`：**只记最终候选仓（≤ STAGE0_REPO_K=12）**，不记全部分桶仓（Pitfall 5 体积护栏）；每仓记 n_r/last_commit_at/dense_cos_max/facet_scores（数值+层级，不记 facet 原文）/criticality_value。
       - `versions["weight_set_version"]` 改取 `config["weight_set_version"]`（**删除对模块常量 WEIGHT_SET_VERSION 的引用**——105 占位换真实版本；import 行同步清理）。
       - `_build_snapshot`/`_stage0_only_result` 签名按需传递 config/repo_meta/scored_at。
    3. 降级语义保持：`repo_meta` 组装整体失败（意外异常）→ try/except 回退 `repo_meta=None`（legacy 三信号路径）+ warning——观测代码与新信号永不反噬路由主流程。
    4. `_log_scored` kv 增 `weight_set_version`（debug 采样，现有事件扩字段不新增事件）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/codegraph tests/services/test_repo_router_adapter.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `rg -c "WEIGHT_SET_VERSION" server/codegraph/services/repo_router_v2.py` == 0（版本一律来自 config loader）
    - `rg -c "datetime.now" server/codegraph/services/repo_router_v2.py` == 1（唯一 now 来源，值进快照 scored_at）
    - 快照断言（Task 3 测试锁定）：payload 含 weight_config.weight_set_version=="phase106-v1"（默认配置）、repo_meta 仓数 <= 12、stage0.scored_at 为 ISO 字符串
    - `uv run pytest tests/codegraph/test_repo_router_replay.py tests/services/test_repo_router_adapter.py -q` 全绿（legacy 构造快照的既有用例零改动；新格式快照回放兼容归 106-07）
  </acceptance_criteria>
  <done>生产路由按六信号打分，快照携带完整回放材料与真实权重版本。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 集成测试——降级矩阵 / 快照契约 / 保存即生效</name>
  <files>server/tests/codegraph/test_repo_router_v2_meta.py</files>
  <read_first>
    - server/tests/codegraph/ 中既有 router v2 测试文件（如 test_repo_router_v2_degraded.py——mock QdrantService/EmbeddingService 的模式）
    - server/codegraph/services/repo_router_v2.py（Task 1/2 产物）
    - server/tests/codegraph/test_repo_router_config.py（SystemSetting 写入 fixture 模式）
  </read_first>
  <behavior>
    - 全量 meta 可用（mock dense 返回余弦、DB 有 FileIndex、nr_snapshot 已写、facets 五维齐）→ 候选 breakdown 含 domain（或 stack/team 视 fixture）键、criticality 字段有值、快照 weight_config/repo_meta/scored_at 齐备且 repo_meta 仓数 <= 候选数
    - 降级矩阵四行（各自独立用例）：dense_search 抛异常 → 路由成功且全仓 S_top 走 RRF fallback；nr_snapshot 缺失 → 路由成功 + breadth 降级（n_bar None）；embedding 未配置 → facet_scores 全走 T1；weight_config 行为非法 JSON → loader 回退默认且路由成功
    - 保存即生效：先 route 一次（默认权重）→ 写入合法新配置（domain 权重改 0.20，version 改 "test-v2"）→ 再 route → 第二次快照 weight_config.weight_set_version == "test-v2" 且 breakdown 数值随权重变化
    - 一次路由的 Qdrant 查询次数 == 2（hybrid + dense，mock 计数）、FileIndex 聚合查询次数 == 1（django_assert_num_queries 或 mock 计数——免 N+1 守护）
    - repo_meta 组装整体异常（mock _load_repo_meta 抛）→ 路由回退 legacy 三信号成功（永不反噬）
  </behavior>
  <action>
    新建 `server/tests/codegraph/test_repo_router_v2_meta.py`：沿用既有 router 测试的 mock 模式（QdrantService/EmbeddingService/SparseEncoderService patch；`pytest.mark.django_db`），Stage 1 一律 use_llm=False（Stage 0 行为聚焦）。fixture 构造 3 仓（大仓多命中/小仓单命中/无 facets 仓）复现尺寸偏置场景，断言 breakdown["breadth"] 反向倾斜在生产链路成立（SC-1 机制的路由侧印证，golden 侧归 106-08）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/codegraph/test_repo_router_v2_meta.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/codegraph/test_repo_router_v2_meta.py -q` 全绿，用例数 >= 8（降级矩阵 4 + 快照契约 + 保存即生效 + 查询次数 + 整体回退）
    - `uv run pytest tests/codegraph tests/delivery tests/services/test_repo_router_adapter.py -q` 全绿（105-07 收官口径回归）
  </acceptance_criteria>
  <done>六信号路由的可用性边界（四降级 + 整体回退）、快照契约、生效语义、免 N+1 全部被集成测试锁定。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Qdrant payload / FileIndex 数据 → repo_meta | 上游索引数据类型不可信，逐字段容错 |
| SystemSetting（权重/词典/N_r 快照）→ 路由打分 | 运维可写面直接影响排序 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-106-14 | DoS | _load_repo_meta | mitigate | 四条降级分支 + 整体 try/except 回退 legacy；观测 best-effort 吞异常——元数据链路任何故障不阻塞路由 |
| T-106-15 | Information Disclosure | 快照 repo_meta/weight_config | mitigate | 快照整体仍经 _h_route 的 redact_for_ledger（105-07 既有，新增字段自动覆盖）；facet 匹配只记数值+层级不记需求原文 |
| T-106-16 | DoS | dense 额外往返 | mitigate | 失败返回空 + fallback；若实测延迟不可接受按 CONTEXT 锁定回退 RRF 口径（取舍记 SUMMARY） |
| T-106-SC | Tampering | 包安装 | accept | 零新增依赖 |
</threat_model>

<verification>
- `cd server && uv run pytest tests/codegraph tests/delivery tests/services/test_repo_router_adapter.py -q` 全绿
- `cd server && uv run ruff check services/qdrant_service.py codegraph/services/repo_router_v2.py codegraph/services/repo_router_config.py tests/codegraph/test_repo_router_v2_meta.py`
</verification>

<success_criteria>
- 生产路由按六信号打分：dense 余弦主干 + pivoted breadth + 元数据 + 连续活跃度，全链路降级可用
- 快照携带 weight_config 全值 + repo_meta + scored_at，版本占位换真实（SC-4 后半）
- 免 N+1（+1 Qdrant / +1 DB 聚合）被测试锁定；观测事件符合 LOGGING-SPEC
</success_criteria>

<output>
完成后创建 `.planning/phases/106-multi-signal-scoring/106-06-SUMMARY.md`。
</output>
