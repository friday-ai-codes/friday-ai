---
phase: 106-multi-signal-scoring
plan: 03
type: execute
wave: 2
depends_on: [106-01]
files_modified:
  - server/codegraph/services/repo_router_metadata.py
  - server/tests/codegraph/test_repo_router_metadata.py
autonomous: true
requirements: [ROUTE-04]
must_haves:
  truths:
    - "需求文本精确命中或命中别名的 facet 值得 1.0、命中上位类目得 0.6（T1 确定性，零网络零 DB，golden harness 可离线 import）"
    - "`技术栈` 单字符串多值（如 \"Python/Vue/Go\"）被 split(\"/\") 后按 0.8·max + 0.2·second_max 聚合——标签多的仓不因此得分更高（尺寸偏置不在元数据侧重演）"
    - "facet 值为 \"未分类\"/空串/缺失 → 该信号返回 None（不可用，进重归一化）；需求文本未提及任何团队 → 团队信号返回 None 而非 0.5（条件信号语义）"
    - "T2 embedding 不可用（未配置/调用失败/facet 被 t2_disabled_facets 禁用）时静默降级 T1-only，绝不阻塞路由；每个 facet 分数携带来源层标注 t1/t2/None"
  artifacts:
    - path: server/codegraph/services/repo_router_metadata.py
      provides: "元数据 resolver：T1 别名词典匹配（纯函数）+ T2 校准余弦（async，facet 向量缓存）+ facet_scores 组装"
      exports: ["DEFAULT_ALIAS_DICT", "FACET_DOMAIN", "FACET_STACK", "FACET_TEAM", "FACET_CRITICALITY", "LAYER_T1", "LAYER_T2", "match_t1", "merge_alias_dict", "alias_dict_hash", "resolve_facet_scores"]
      min_lines: 180
    - path: server/tests/codegraph/test_repo_router_metadata.py
      provides: "T1/T2/多值/条件信号/未分类/降级路径测试"
      min_lines: 150
  key_links:
    - from: server/codegraph/services/repo_router_metadata.py
      to: server/codegraph/services/repo_router_scoring.py
      via: "resolve_facet_scores 输出即 repo_meta.facet_scores 契约（{score, layer}），scorer 只消费数值"
      pattern: "facet_scores"
    - from: server/codegraph/services/repo_router_metadata.py
      to: server/services/embedding.py
      via: "T2 用已算好的 query dense 向量 + facet 值向量（EmbeddingService 批量预热 + Django cache），零额外 query embedding"
      pattern: "EmbeddingService"
---

<objective>
建立元数据 resolver 模块 `repo_router_metadata.py`：把 `Repository.facets` 的五维原始值解析为 scorer 可消费的 `facet_scores`（ROUTE-04 三层匹配的 T1/T2；T3 LLM 绝不进分数）。T1 别名词典为纯函数部分（golden harness 可确定性 import）；T2 为 async 部分（校准余弦 + facet 值向量 Django cache 缓存），失败静默降级 T1-only。别名词典按 planner 裁决采用「代码常量起步 + SystemSetting 覆盖合并」双轨（合并与 hash 的纯函数在本 plan，运行时读取覆盖在 106-06 router 层）。

数据实况约束（RESEARCH §2，全部写成测试）：`业务线/产品线` 是复合键名且 "未分类" 视为缺失；`技术栈` 是斜杠拼接单字符串；`团队归属` 是开放集条件信号（只走 T1，需求未提团队 → 不可用）；`关键程度` 值透传给 scorer 做锚点映射（resolver 只取原值字符串）。

Purpose: ROUTE-04 的信号生产层——106-06 router 组装 repo_meta 时调用；分数来源层（t1/t2/缺失）随 facet_scores 进快照满足可解释性最低要求。
Output: resolver 模块 + 测试。
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/106-multi-signal-scoring/106-CONTEXT.md
@.planning/phases/106-multi-signal-scoring/106-RESEARCH.md
@server/codegraph/services/repo_router_scoring.py
@server/repositories/facet_service.py
@server/services/embedding.py
</context>

## Artifacts this phase produces（本 plan 新建符号）

| Symbol | Kind | Location |
|--------|------|----------|
| `FACET_DOMAIN = "业务线/产品线"` / `FACET_STACK = "技术栈"` / `FACET_TEAM = "团队归属"` / `FACET_CRITICALITY = "关键程度"` / `FACET_ACTIVITY = "活跃度"` | facet 键名常量（全中文实际键名） | `server/codegraph/services/repo_router_metadata.py` |
| `UNCLASSIFIED_VALUE = "未分类"` | 常量（视为缺失） | 同上 |
| `LAYER_T1 = "t1"` / `LAYER_T2 = "t2"` | 来源层标注常量 | 同上 |
| `DEFAULT_ALIAS_DICT: dict[str, dict[str, dict]]` | 别名词典骨架（`{facet_dim: {canonical: {"aliases": [...], "parent": str\|None}}}`） | 同上 |
| `match_t1(query_text, dim, value, alias_dict) -> float \| None` | 纯函数（1.0/0.6/None） | 同上 |
| `merge_alias_dict(default, override) -> dict` / `alias_dict_hash(alias_dict) -> str` | 纯函数（覆盖合并 / canonical JSON sha256，进快照保回放确定性） | 同上 |
| `resolve_facet_scores(query_text, facets, *, alias_dict, constants, query_embedding=None, t2_matcher=None) -> dict` | async resolver 主入口（输出 repo_meta.facet_scores 契约） | 同上 |
| `FacetT2Matcher` | class（facet 值向量缓存 + 校准余弦；cache key `repo_router:facet_vec:{model_id}:{sha256(value)}`，TTL 7d） | 同上 |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: T1 别名词典 + facet 解析纯函数（含多值/条件/未分类语义）</name>
  <files>server/codegraph/services/repo_router_metadata.py, server/tests/codegraph/test_repo_router_metadata.py</files>
  <read_first>
    - server/repositories/facet_service.py（facet 事实闭集来源：`_EXT_LANGUAGE_MAP` 语言名、criticality 三档、activity 四档、team namespace 链格式——DEFAULT_ALIAS_DICT 骨架照抄这些枚举）
    - server/repositories/summary_service.py（SEMANTIC_FACET_DIMENSIONS——语义分面维度名与 "未分类" 填充语义）
    - server/codegraph/services/repo_router_scoring.py（Task 输出要对齐的 repo_meta.facet_scores 键契约 {score, layer}）
    - .planning/phases/106-multi-signal-scoring/106-RESEARCH.md §2/§9（facet 键名实况表 + 别名词典最小形态建议）
  </read_first>
  <behavior>
    - match_t1("给高三提分专项加功能", FACET_DOMAIN, "高三提分", 词典含 alias "提分专项") → 1.0（别名子串命中）；canonical 值本身子串命中 → 1.0；仅 parent 命中 → 0.6；均未命中 → None
    - resolve_facet_scores 对 facets={"技术栈": "Python/Vue/Go"}、query 提到 "python 后端"：stack 分数 = 0.8*1.0 + 0.2*(second_max)——split("/") 后逐值匹配（大小写不敏感比较）
    - facets={"业务线/产品线": "未分类"} → domain: {score: None, layer: None}；facets 缺该键 / 值为空串 → 同样 None
    - query 未含任何团队 token（词典 team 维度所有 canonical/alias 都不在 query 中）→ team: {score: None, layer: None}（不给 0.5）；query 含 "group/sub" 命名空间链或其别名 → 按 T1 打分
    - merge_alias_dict：override 新增 canonical / 追加 aliases 生效，default 不被原地修改；alias_dict_hash 对键序不同但内容相同的 dict 输出相同 sha256
    - 超长 facet 值（> 200 字符）→ 直接视为不可匹配（None，DoS 护栏）
  </behavior>
  <action>
    创建 `repo_router_metadata.py`（本 task 部分**零 Django import**——T1/合并/hash 为纯函数，golden harness 可离线 import；T2 类归 Task 2 再引入 Django cache 依赖，用局部 import 隔离）。

    - facet 键名/层级/未分类常量见 Artifacts 表。
    - `DEFAULT_ALIAS_DICT` 初始骨架（Claude's Discretion 裁决：代码常量起步）：技术栈维度含 `_EXT_LANGUAGE_MAP` 全部语言名 + 常见别名（Python:["py","python3"], Go:["golang"], Vue:["vue3","vuejs"], TypeScript:["ts"], JavaScript:["js"] 等）；活跃度/关键程度维度枚举骨架（无别名）；业务线/产品线、服务对象、技术形态、团队归属维度留空骨架 + 注释「生产词表条目 deferred（同 O-2 纪律），运维经 SystemSetting repo_router.alias_dict 覆盖补充」。
    - `match_t1`：query/值统一 casefold 后子串包含匹配（中文短语直接 in，不引分词库——零依赖约束）；canonical 或任一 alias 命中 → 1.0；parent 命中 → 0.6；否则 None。
    - `resolve_facet_scores(query_text, facets, *, alias_dict, constants, query_embedding=None, t2_matcher=None) -> dict[str, dict]`：输出 `{"domain": {"score": ..., "layer": ...}, "stack": {...}, "team": {...}}`：
      - domain：facets[FACET_DOMAIN]，"未分类"/空/缺失 → 不可用；T1 命中 → 分数+LAYER_T1；未命中且 t2_matcher 可用且 "domain" 不在 constants 相关禁用清单（t2_disabled_facets）→ T2（Task 2）；T2 亦不可用 → None。
      - stack：split("/") 逐值 T1（T2 同理逐值），聚合 `0.8*max + 0.2*second_max`（单值时 second_max=0 → 0.8*max，注释说明）；layer 取贡献 max 的那个值的层。
      - team：**只走 T1**（开放集，RESEARCH A3——T2 对团队名区分度存疑，禁用）；query 未提团队 → None。
      - criticality/activity 原值**不在本函数处理**（由 router 直接放进 repo_meta.criticality_value / facets——注释说明分工）。
    - `merge_alias_dict` / `alias_dict_hash`（`hashlib.sha256(json.dumps(..., sort_keys=True, ensure_ascii=False))`）。
    - 测试文件覆盖 behavior 全条目（本 task 用例均为纯函数测试，无 DB/网络）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/codegraph/test_repo_router_metadata.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/codegraph/test_repo_router_metadata.py -q` 全绿，用例数 >= 12
    - `rg -c "sum\(|mean\(" server/codegraph/services/repo_router_metadata.py` 中不存在对多值 facet 匹配分的 sum/mean 聚合（人工检查 stack 聚合行只有 max/second_max）
    - "未分类"、空串、超长值三种输入均返回 {score: None, layer: None} 且不抛异常
    - `match_t1` 与 `merge_alias_dict`/`alias_dict_hash` 可在无 Django 环境 import（`uv run python -c "import django_free_check"` 形式：直接 import 模块顶层不触发 django.setup——用局部 import 隔离 T2 依赖后成立）
  </acceptance_criteria>
  <done>T1 确定性匹配与 facet 解析语义全部落地并被测试锁定；golden harness（106-08）可离线复用。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FacetT2Matcher——校准余弦 + facet 值向量缓存 + 静默降级</name>
  <files>server/codegraph/services/repo_router_metadata.py, server/tests/codegraph/test_repo_router_metadata.py</files>
  <read_first>
    - server/services/embedding.py（generate_embedding/generate_embeddings_batch 签名与失败返回 None 语义；get_config()["model"] 取模型 id）
    - server/agents/call_source.py（CallSource.EMBEDDING 枚举——T2 调用需在 use_call_source 作用域内）
    - server/codegraph/services/repo_router_v2.py（Django cache import 与使用模式——`from django.core.cache import cache`）
  </read_first>
  <behavior>
    - T2 校准：cos=0.55 且 t2_c_lo=0.25/t2_c_hi=0.55 → 1.0；cos=0.25 → 0.0；cos=0.40 → 0.5（线性 clip）
    - facet 值向量缓存命中时零 embedding 调用（mock EmbeddingService 断言调用次数 0）；miss 时调用一次并回写 cache（第二次调用同值 → 调用次数不增）
    - EmbeddingService 返回 None（未配置/失败）→ match 返回 None + 记一次 warning（repo_router_t2_degraded，采样），不抛异常
    - query_embedding=None 时 T2 整体不可用（resolver 全走 T1），零 embedding 调用
  </behavior>
  <action>
    在 `repo_router_metadata.py` 追加：
    - `FacetT2Matcher`：构造入参 `model_id: str`、`t2_c_lo: float`、`t2_c_hi: float`。`async match(query_embedding: list[float], value: str) -> float | None`：facet 值向量取 Django cache（key `repo_router:facet_vec:{model_id}:{sha256(value)}`，TTL 7 天 = 604800；进程内模块级 dict 兜底二级缓存）；miss → `EmbeddingService.generate_embedding(value)`（外层调用需处于 `use_call_source(CallSource.EMBEDDING)` 作用域——matcher 内部包裹，注释引用 LOGGING-SPEC call_source 要求）；余弦（stdlib 实现：点积/范数，禁 numpy）→ `clip((cos - t2_c_lo)/(t2_c_hi - t2_c_lo), 0, 1)`。任何失败 → None + `repo_router_t2_degraded` warning（structlog kv：facet_value 截断 32 字符、reason、category="sampling"、component="codegraph"；try/except 吞异常，观测 best-effort）。
    - `warm_facet_vectors(values: list[str], matcher) -> int`：批量预热（`generate_embeddings_batch`），供 106-04/106-06 冷启动调用；返回成功条数。
    - Django 相关 import（cache、EmbeddingService、CallSource）**放在类方法/函数内部局部 import**，保证模块顶层仍可被无 Django 环境 import（Task 1 acceptance 依赖）。
    - `resolve_facet_scores` 接线 t2_matcher（Task 1 已留参数位）：T1 未命中且 matcher 与 query_embedding 均可用且 facet 不在 t2_disabled_facets → await matcher.match(...)。
    - 测试：mock `EmbeddingService.generate_embedding`（AsyncMock）+ Django `cache`（locmem 即可），覆盖 behavior 全条目；校准 clip 三点断言用 pytest.approx。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/codegraph/test_repo_router_metadata.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/codegraph/test_repo_router_metadata.py -q` 全绿，用例总数 >= 18（Task 1 >= 12 + 本 task >= 6）
    - `rg -c "import numpy|from numpy" server/codegraph/services/repo_router_metadata.py` == 0（余弦 stdlib 实现）
    - `rg -c "use_call_source" server/codegraph/services/repo_router_metadata.py` >= 1（EMBEDDING call_source 作用域）
    - embedding 失败路径测试断言：返回 None、无异常传播、warning 事件名 repo_router_t2_degraded 被记录（capsys/structlog capture 任一方式）
  </acceptance_criteria>
  <done>T2 通道可用且失败静默降级；facet 向量闭集缓存避免重复 embedding；来源层标注完整进 facet_scores。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repository.facets（索引管线写入）→ resolver | facet 值为外部生成内容（LLM 语义分面/git namespace），长度与类型需设防 |
| SystemSetting alias_dict override → merge_alias_dict | 运维可注入词典条目（106-06 读取），结构需容错 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-106-06 | DoS | FacetT2Matcher / match_t1 | mitigate | facet 值长度上限 200 字符（超长 → 不可匹配），闭集来源约束；embedding 失败降级 T1-only 不阻塞路由 |
| T-106-07 | Information Disclosure | repo_router_t2_degraded 日志 | mitigate | facet_value 截断 32 字符入日志；异常文本经 structlog redact_credentials 自动 processor |
| T-106-08 | Tampering | alias_dict override 合并 | mitigate | merge 容错（非 dict 结构条目跳过）；生效词典 hash 进快照（106-06），回放可审计 |
| T-106-SC | Tampering | 包安装 | accept | 零新增依赖（不引分词库/numpy） |
</threat_model>

<verification>
- `cd server && uv run pytest tests/codegraph/test_repo_router_metadata.py -q` 全绿
- `cd server && uv run ruff check codegraph/services/repo_router_metadata.py tests/codegraph/test_repo_router_metadata.py`
</verification>

<success_criteria>
- T1/T2 两层匹配就位：确定性别名匹配可离线复用，T2 校准余弦带缓存且失败静默降级
- 多值 max（0.8/0.2）、"未分类"→缺失、团队条件信号三条锁定语义全部有测试
- facet_scores 输出契约与 106-01 scorer 的 repo_meta 键契约严格一致
</success_criteria>

<output>
完成后创建 `.planning/phases/106-multi-signal-scoring/106-03-SUMMARY.md`。
</output>
