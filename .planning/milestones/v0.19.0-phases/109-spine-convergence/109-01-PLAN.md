---
phase: 109-spine-convergence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/tests/test_spa_coding_chain_e2e.py
  - server/tests/mcp_tools/test_bridge_session.py
  - server/tests/agents/test_tool_contracts.py
  - server/tests/agents/_generate_contract_fixtures.py
  - server/tests/agents/fixtures/create_coding_plan_signature.json
  - server/tests/agents/fixtures/update_coding_plan_signature.json
autonomous: true
requirements: [SPINE-01, SPINE-02]
must_haves:
  truths:
    - "SPA 编码链路的『四步都挂在同一个 CodingPlan.id 上』这条不变量有一条端到端自动化用例锁住，且该用例在任何 schema 改动之前已通过"
    - "MCP 桥接的『三对象（Conversation / CodingPlan / CodingSession）全部建成』有直接断言，不依赖间接路径"
    - "chat @tool create_coding_plan 与 update_coding_plan 的函数签名有 fixture 字节级漂移守护，任何入参增删都会变红"
  artifacts:
    - path: "server/tests/test_spa_coding_chain_e2e.py"
      provides: "SPA ①选仓 ②配分支 ③确认编码 ④飞书导出 同一 plan_id 端到端护栏"
      contains: "def test_spa_coding_chain"
    - path: "server/tests/mcp_tools/test_bridge_session.py"
      provides: "_create_bridge_session 三对象直接断言（MCP 零回归锁）"
      contains: "_create_bridge_session"
    - path: "server/tests/agents/fixtures/create_coding_plan_signature.json"
      provides: "chat @tool create_coding_plan 签名 baseline"
    - path: "server/tests/agents/fixtures/update_coding_plan_signature.json"
      provides: "chat @tool update_coding_plan 签名 baseline"
  key_links:
    - from: "server/tests/test_spa_coding_chain_e2e.py"
      to: "POST /api/chat/coding-plans/{plan_id}/sessions/ 与 POST /api/chat/coding-plans/{plan_id}/export-to-feishu/"
      via: "同一个 plan_id 打两个端点"
      pattern: "coding-plans/.*sessions|coding-plans/.*export-to-feishu"
    - from: "server/tests/agents/test_tool_contracts.py"
      to: "server/tests/agents/fixtures/create_coding_plan_signature.json"
      via: "_normalize_signature 字节级 diff"
      pattern: "create_coding_plan_signature"
---

<objective>
建立 SPINE-02 动刀前必须先绿的回归护栏，并给 chat `@tool` 的创作入参加上漂移守护基线。

Purpose: CONTEXT §移除徒手创作路径 明文「回归护栏先行 —— SPA 与 MCP 两条编码链路的端到端守护测试先绿再动刀」，且 109-RESEARCH §11.1 实证「SPA 四步各有独立测试，但没有任何一条把『四步都还挂在同一个 plan_id 上』锁住的端到端用例」。ROADMAP 的 SPINE-01 → SPINE-02 顺序硬约束在计划层的具体落法就是本 plan 处于 wave 1、早于任何 schema/模型改动。
Output: 两个新测试文件 + 两份工具签名 fixture + `_generate_contract_fixtures.py` 的生成分支。本 plan **不改任何生产代码**。
</objective>

<execution_context>
@.cursor/gsd-core/workflows/execute-plan.md
@.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/109-spine-convergence/109-CONTEXT.md
@.planning/phases/109-spine-convergence/109-RESEARCH.md
@.planning/phases/109-spine-convergence/109-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: SPA 四步同一 plan_id 端到端护栏（Wave 0 缺口，最高优先）</name>
  <files>server/tests/test_spa_coding_chain_e2e.py</files>
  <read_first>
    - `.planning/phases/109-spine-convergence/109-RESEARCH.md` §4「执行流四步现状」（四步端点与 owner gate 模式）与 §11.1「SPA 编码链路」（缺口结论）
    - `.planning/phases/109-spine-convergence/109-CONTEXT.md` §移除徒手创作路径 —— 「护栏缺口（研究结论，直接决定 wave 划分）」
    - `server/tests/test_coding_plans_sessions_api.py`（fan-out 端点 6 类场景的既有 fixture 与断言风格，作为本用例的基线范式）
    - `server/tests/test_coding_plan_export_api.py`（导出端点测试如何 mock `FeishuDocClient`；`--disable-socket` 下禁止真实网络）
    - `server/chat/urls.py:154-176`（三个 CodingPlan 端点的 url name）
    - `server/chat/views.py:2573-2668`（`CodingPlanSessionsBatchCreateView`）与 `server/chat/views.py:1820-1911`（`ExportCodingPlanToFeishuView`）
  </read_first>
  <action>
    新建 `server/tests/test_spa_coding_chain_e2e.py`，写一条把 SPA 执行流四步串起来、并显式断言「四步共用同一个 `CodingPlan.id`」的端到端用例（函数名以 `test_spa_coding_chain` 开头，便于 `-k` 定向）。

    用例骨架（沿用 `test_coding_plans_sessions_api.py` 的 fixture 与 `pytest.mark.django_db(transaction=True)` 约定）：
    1. 造 `Space` + 两个 indexed `Repository` + `Conversation`（`created_by` = 测试用户）+ 一条 `CodingPlan`（本 plan 阶段仍走 `CodingPlan.aget_or_create_for_conversation`，因为投影 service 尚未存在），把 `plan.id` 记为 `plan_id`。
    2. ①②：断言 `GET /api/chat/coding-plans/{plan_id}/` 返回 200 且 `recommended_repository_ids` 原样透出（这是 `RepoMultiSelector` 推荐高亮的数据来源；分支配置是纯前端态，后端侧以 `branch_template` / `target_branch` 入参形式在第 3 步一起提交）。
    3. ③：`POST /api/chat/coding-plans/{plan_id}/sessions/`，body 带 `repository_ids`（两个仓）+ `branch_template` + `target_branch`；断言 200、`created` 长度为 2、每条 `session_id` 对应的 `CodingSession.coding_plan_id == plan_id`、`branch_name` 已按模板生成、`target_branch` 已落库。
    4. ④：`POST /api/chat/coding-plans/{plan_id}/export-to-feishu/`（mock `feishu.coding_plan_exporter` 内部使用的 `FeishuDocClient`，不触网），断言 200 且回填后的 `CodingPlan.feishu_doc_url` 非空。
    5. **不变量断言（本用例存在的唯一理由）**：把三个 HTTP 调用用过的 plan 标识收进一个集合，断言集合长度为 1 且等于 `{str(plan_id)}`；并断言经由 `CodingSession.objects.filter(coding_plan_id=plan_id)` 反查得到的 session 数量与第 3 步 `created` 数量一致。用注释写明「这条断言锁的是『一次投影/一份方案即点亮全部四步』，任何把某一步改成依赖别的锚点的改动都会在此变红」。
    6. 追加一条 owner gate 对照用例：换一个非 owner 用户打第 3、4 步，断言两者均为 404（不是 403，不泄漏存在性；照抄 `views.py:2617-2625` 的门）。

    禁止事项：不得 mock `create_sessions_for_plan` 或导出 service 本体（那样护栏就空了），只允许 mock 飞书 HTTP 客户端这一层 IO 边界。
  </action>
  <acceptance_criteria>
    - `server/tests/test_spa_coding_chain_e2e.py` 存在，且至少含一个名字以 `test_spa_coding_chain` 开头的用例。
    - 该用例内出现对 `coding-plans/{plan_id}/sessions/` 与 `coding-plans/{plan_id}/export-to-feishu/` 两个端点的调用，且两处使用的 plan 标识是同一个变量。
    - 存在一条断言把「四步使用的 plan 标识集合」长度断言为 1。
    - 存在一条非 owner 打这两个端点得到 404 的对照断言。
    - `cd server && uv run pytest tests/test_spa_coding_chain_e2e.py -x` 全绿，且运行时间 < 60s。
  </acceptance_criteria>
  <verify>
    <automated>cd server && uv run pytest tests/test_spa_coding_chain_e2e.py -x</automated>
  </verify>
  <done>SPA 四步端到端护栏存在且通过；后续任何 schema 收窄若打断四步共用同一 plan_id 的不变量，本用例会立刻变红。</done>
</task>

<task type="auto">
  <name>Task 2: MCP 桥接三对象直接断言（零回归锁）</name>
  <files>server/tests/mcp_tools/test_bridge_session.py</files>
  <read_first>
    - `.planning/phases/109-spine-convergence/109-RESEARCH.md` §2.3「与 mcp_tools.McpCodingPlan 的关系与桥接点」（`_create_bridge_session` 全文引用 + 「零回归要保的 5 项」）与 §11.2（`test_execution_tools.py` 断言了 `CodingSession` 但未显式断言 `CodingPlan`）
    - `.planning/phases/109-spine-convergence/109-CONTEXT.md` §移除徒手创作路径 —— 研究纠正 C-1 续（MCP 执行链从不调用 chat `@tool`，桥接是裸 ORM，新增字段必须带 default）
    - `.planning/phases/109-spine-convergence/109-VALIDATION.md` §Explicit Scope Boundaries 第 5 条
    - `server/mcp_tools/execution_service.py:86-127`（`_create_bridge_session` 本体）
    - `server/tests/mcp_tools/conftest.py` 与 `server/tests/mcp_tools/test_execution_tools.py:1-40`（既有 fixture 与 mark 约定）
  </read_first>
  <action>
    新建 `server/tests/mcp_tools/test_bridge_session.py`，直接调用 `mcp_tools.execution_service._create_bridge_session`（不经 HTTP、不经 `execute_coding_plan`），断言它一次建成三个对象且字段形状不变。

    用例内容：
    1. 造 `Space`（作为 `project` 参数）+ `Repository` + `McpCodingPlan` + `McpCodingPlanVersion`（`plan_body` 用一份最小 §7 形状 dict），`created_by` 传一个真实 user。
    2. 调 `_create_bridge_session(project=..., plan=..., version=..., branch_name="feat/2026-07-30.bridge", created_by=user)`，拿回 `(conversation, chat_plan, coding_session)` 三元组。
    3. 逐对象断言：
       - `Conversation`：`space_id == project.id`、`status == Conversation.Status.RUNNING`、`created_by_id == user.id`、`title` 以 `"MCP execution: "` 开头。
       - chat `CodingPlan`：`conversation_id == conversation.id`、`tech_plan` 非空、`affected_files` 是 list、`recommended_repository_ids == [str(plan.repository_id)]`。
       - `CodingSession`：`coding_plan_id == chat_plan.id`、`repository_id == plan.repository_id`、`branch_name` 等于传入值、`status == CodingSession.Status.DRAFT`。
    4. 再补一条计数断言：调用前后 `Conversation` / `CodingPlan` / `CodingSession` 三张表各自恰好 +1（防止未来某次改动把桥接拆成多写）。
    5. 文件 docstring 写明：本文件是「MCP 零回归」的直接锁 —— 保护对象是**这三个模型的字段形状**（裸 `objects.create()` 可构造），不是 chat `@tool` 的行为；后续给 `CodingPlan` 加列时若忘了 `default`，本文件必红。

    ⚠️ 本 task 只断言**当前**字段形状（尚无 `provenance` / `source_artifact_version_id`）。新字段的默认值断言由 plan 109-02 在同一文件内追加，不要在此提前写（跨 task 的完成态不得写进本 task 的验收）。
  </action>
  <acceptance_criteria>
    - `server/tests/mcp_tools/test_bridge_session.py` 存在且直接 import/调用 `_create_bridge_session`。
    - 三个对象各自至少有 3 条字段断言；存在三张表各 +1 的计数断言。
    - 断言中出现 `recommended_repository_ids`、`CodingSession.Status.DRAFT`、`Conversation.Status.RUNNING` 三个标识。
    - `cd server && uv run pytest tests/mcp_tools/test_bridge_session.py -x` 全绿。
    - `cd server && uv run pytest tests/mcp_tools/test_execution_tools.py tests/mcp_tools/test_create_coding_plan_delegate.py -x` 仍全绿（既有 MCP 护栏零回归）。
  </acceptance_criteria>
  <verify>
    <automated>cd server && uv run pytest tests/mcp_tools/test_bridge_session.py tests/mcp_tools/test_execution_tools.py tests/mcp_tools/test_create_coding_plan_delegate.py -x</automated>
  </verify>
  <done>`_create_bridge_session` 建成三对象且字段形状被直接断言；MCP 侧既有护栏未回归。</done>
</task>

<task type="auto">
  <name>Task 3: chat @tool 创作入参的签名漂移守护基线</name>
  <files>server/tests/agents/_generate_contract_fixtures.py, server/tests/agents/test_tool_contracts.py, server/tests/agents/fixtures/create_coding_plan_signature.json, server/tests/agents/fixtures/update_coding_plan_signature.json</files>
  <read_first>
    - `.planning/phases/109-spine-convergence/109-RESEARCH.md` §8.3「schema 漂移守护：现状与缺口」（实体 A 零守护的实证结论 + 建议范式）
    - `.planning/phases/109-spine-convergence/109-CONTEXT.md` 裁决 D-2（补 schema 漂移守护）与 D-1（两个门一起收窄 ⇒ 两个工具都要基线）
    - `server/tests/agents/test_tool_contracts.py`（`_normalize_signature` + `_REGENERATE_HINT` + 既有 6 个工具的 snapshot 范式）
    - `server/tests/agents/_generate_contract_fixtures.py`（`main()` 里逐工具 `_dump` 的写法与「必须从 server/ 执行 + django.setup()」的约束）
    - `server/agents/tools/coding_tools.py:112-119`（`create_coding_plan` 当前签名）与 `:339-344`（`update_coding_plan` 当前签名）
  </read_first>
  <action>
    按 `test_tool_contracts.py` 的既有范式给两个 chat `@tool` 补签名 snapshot 守护：

    1. 在 `_generate_contract_fixtures.py` 的 `main()` 里追加两行 import（`from agents.tools.coding_tools import create_coding_plan, update_coding_plan`）与两次 `_dump(FIXTURE_DIR / "create_coding_plan_signature.json", _normalize_signature(create_coding_plan))` / 同形 `update_coding_plan`。
    2. 从 `server/` 目录运行一次 `DJANGO_SETTINGS_MODULE=friday.settings uv run python -m tests.agents._generate_contract_fixtures` 生成两份 fixture（记录**收窄前**的 baseline：`create_coding_plan` 含 `tech_plan` / `affected_files`，`update_coding_plan` 含 `tech_plan` / `affected_files`）。
    3. 在 `test_tool_contracts.py` 顶部按既有命名风格加两个模块常量 `_CREATE_CODING_PLAN_SIGNATURE_FIXTURE` / `_UPDATE_CODING_PLAN_SIGNATURE_FIXTURE`，并加两个测试函数 `test_create_coding_plan_signature_snapshot` / `test_update_coding_plan_signature_snapshot`，断言 `_normalize_signature(...) == json.loads(fixture)`，失败消息带 `_REGENERATE_HINT`。
    4. 两个新测试的 docstring 必须写明：本 baseline 是 **SPINE-02 收窄前的现状快照**；收窄发生时它们**应当变红**，届时按 `_REGENERATE_HINT` 显式再生成 + review diff 即视作契约升级（这正是「契约升级 = 一次显式提交」的既有工作流）。

    ⚠️ 本 task **不写** SPINE-02 的正向不变量断言（`"tech_plan" not in parameters["properties"]`）—— 它在收窄前必然失败，属于 plan 109-05 的交付物。本 task 交付的是「任何入参增删都能被看见」的漂移守护，这已满足 109-VALIDATION.md Wave 0 第 2 条的目的（守护存在、漂移可见）。
  </action>
  <acceptance_criteria>
    - `server/tests/agents/fixtures/create_coding_plan_signature.json` 与 `update_coding_plan_signature.json` 两文件存在，且各自的 JSON 顶层键含 `tech_plan` 与 `affected_files`（证明记录的是收窄前 baseline）。
    - `server/tests/agents/test_tool_contracts.py` 中存在 `test_create_coding_plan_signature_snapshot` 与 `test_update_coding_plan_signature_snapshot` 两个函数。
    - `_generate_contract_fixtures.py` 的 `main()` 内出现 `create_coding_plan_signature.json` 与 `update_coding_plan_signature.json` 两个写出路径。
    - `cd server && uv run pytest tests/agents/test_tool_contracts.py -x` 全绿（12+ 个 snapshot 用例）。
  </acceptance_criteria>
  <verify>
    <automated>cd server && uv run pytest tests/agents/test_tool_contracts.py -x</automated>
  </verify>
  <done>两个 chat `@tool` 的签名有 fixture 字节级守护并已 baseline；后续任何入参增删（含 SPINE-02 的收窄）都会显式变红。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 测试进程 ↔ 外部网络 | `pytest` 配置了 `--disable-socket`；护栏用例若尝试真实飞书 HTTP 会直接失败 |
| 测试用户 ↔ CodingPlan owner | 端到端护栏必须覆盖非 owner 打端点的路径，否则护栏无法保护 owner gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-109-01-01 | Information Disclosure | `tests/test_spa_coding_chain_e2e.py` | mitigate | 护栏内含非 owner 对照用例，断言 fan-out 与导出端点均返回 404（不是 403），锁住「不泄漏存在性」 |
| T-109-01-02 | Tampering | 护栏本身被 mock 掉 | mitigate | Task 1 action 显式禁止 mock `create_sessions_for_plan` / 导出 service 本体，只允许 mock 飞书 HTTP 客户端 |
| T-109-01-03 | Repudiation | 工具签名静默回退 | mitigate | fixture 字节 diff + `_REGENERATE_HINT` 的显式再生成流程，让契约变更必须留下一次可 review 的提交 |
| T-109-01-SC | Tampering | npm/pip/cargo installs | accept | 本 plan 零新增依赖（109-RESEARCH §Package Legitimacy Audit：本 phase 不安装任何外部包），无 slopcheck 面 |
</threat_model>

<verification>
- `cd server && uv run pytest tests/test_spa_coding_chain_e2e.py tests/mcp_tools/test_bridge_session.py tests/agents/test_tool_contracts.py -x` 全绿。
- `cd server && uv run pytest tests/mcp_tools/ -x` 全绿（MCP 侧既有 6 组 delegate 守护 + 执行链 e2e 零回归）。
- `git diff --stat` 显示改动只落在 `server/tests/` 之下（本 plan 不碰生产代码）。
</verification>

<success_criteria>
- SPA 四步同一 `plan_id` 不变量的端到端用例存在且通过，且它先于本 phase 任何 schema / 模型改动落地（wave 1）。
- `_create_bridge_session` 三对象直接断言存在且通过。
- 两个 chat `@tool` 的签名 fixture baseline 已提交，漂移可见。
- 无生产代码改动，无新增依赖。
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces（本 plan 部分）

> 约定：每个 plan 的本章节穷举该 plan 新建的符号；8 个 plan 的并集即本 phase 的完整新建清单。

**新建文件**
- `server/tests/test_spa_coding_chain_e2e.py`
- `server/tests/mcp_tools/test_bridge_session.py`
- `server/tests/agents/fixtures/create_coding_plan_signature.json`
- `server/tests/agents/fixtures/update_coding_plan_signature.json`

**新建符号**
- `test_spa_coding_chain_*`（pytest 用例，SPA 四步同一 plan_id 护栏）
- `test_create_coding_plan_signature_snapshot` / `test_update_coding_plan_signature_snapshot`（`server/tests/agents/test_tool_contracts.py`）
- `_CREATE_CODING_PLAN_SIGNATURE_FIXTURE` / `_UPDATE_CODING_PLAN_SIGNATURE_FIXTURE`（同文件模块常量）

**新建事件名 / 端点 / 迁移 / 前端符号**：无
</artifacts_produced>

<output>
Create `.planning/phases/109-spine-convergence/109-01-SUMMARY.md` when done
</output>
