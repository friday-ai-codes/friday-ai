---
status: partial
phase: 109-spine-convergence
source: [109-VERIFICATION.md]
started: 2026-07-31T03:45:00Z
updated: 2026-08-02T00:00:00Z
audit_acknowledged:
  milestone: v0.25.0
  at: 2026-08-31
  gap_snapshot: "partial::scenarios=4"
---

## Current Test

number: 1
name: 编排产出直连执行流的真机全链（SPINE-01 头条路径）
expected: |
  真实会话触发一次编排，从「进入编码」卡片走完选目标仓 → 配置分支 → 确认编码 → 飞书导出全链。
  四步全部挂在同一个 `CodingPlan.id` 上，无需重新生成方案；容器真实拉起并产出 PR。
  需真实 Git 平台、Runner 与飞书环境；本地只验证到 dispatch 契约组装（e2e 护栏覆盖四步 HTTP 面）。
awaiting: user response

## Tests

### 1. 编排产出直连执行流的真机全链（SPINE-01 头条路径）

expected: 四步全部挂在同一个 `CodingPlan.id` 上，无需重新生成方案；容器真实拉起并产出 PR
requirement: SPINE-01
result: [pending]
not_automatable: 需要真实 Git 平台 + Runner + 飞书（要的是「容器真实拉起并产出 PR」，把这三者 mock 掉就等于不验它）。

### 2. 草稿方案飞书导出物的「未经代码调研」告示

expected: 导出物含告示块且置于「## 技术方案」之前；主句「本方案未经代码调研」与界面横幅逐字一致
requirement: RELY-01
result: [pending]
not_automatable: 需要真实飞书（被验的是导出到飞书文档后的产出物本身）。

### 3. 浏览器视觉与交互核对（草稿横幅 / 常驻徽标 / 阻断式弹层）

expected: 与 109-UI-SPEC 一致；无新色板 / 新字号 / 新组件；弹层焦点陷阱与 label 点击生效；折叠后徽标仍可见
requirement: RELY-01
result: [pass]
evidence: `web/tests/e2e/draft-plan-gate.spec.ts` 四条 109-3 用例，路径是「编排产出卡 →进入编码→ 惰性投影 → 内嵌 TechPlanCard → 选仓 → 确认编码 → 弹层」。覆盖：横幅在正文之前（按文档顺序断言）、折叠后常驻徽标仍可见、弹层必勾才能确认、Tab 一圈焦点不出弹层、label 点击生效、取消不发请求、确认不跨次记忆；并断言 `acknowledge_unresearched` 只在用户勾选确认后才出现在请求体里。配 `provenance=orchestrated` 负向对照。
residual_human_judgement: 「无新色板 / 新字号 / 新组件」属人判（需对照 DESIGN.md 逐条看），未覆盖。

### 4. 投影出的 tech_plan 在界面 markdown 下的观感

expected: `render_merged_plan_markdown` 产 lark_md 方言，项目符号显示为纯文本 `•` 而非 `- `，可读、语义不丢
note: 109-VALIDATION 第 10 条已裁定「接受现状」，仅需人判观感是否可接受；若不可接受，处置是给该函数加 flavor 参数而非 fork 渲染器
requirement: SPINE-01
result: [pass]
evidence: `web/tests/e2e/draft-plan-gate.spec.ts` —「109-4 lark_md 方言在界面 markdown 下的呈现」。正文 fixture 是对 `render_merged_plan_markdown` 实跑得到的真实输出。断言：渲染结果含字面 `•`、不产生 `<ul>/<li>`、不漏出裸 `- `、两条风险原文都在（语义不丢）、`**加粗**` 与 `> 引用` 仍走真 markdown。
residual_human_judgement: 「观感是否可接受」本身是人判（VALIDATION 第 10 条也只要求人判），未覆盖；用例只锁住「渲染出来不是 `- `、语义不丢」这一半。

### 5. 迁移 0033 在生产存量数据上的影响面

expected: 存量 `CodingPlan` 全部落 `provenance=draft`，历史方案卡集体出现「未经代码调研」横幅与徽标，送编码时各弹一次确认；影响面可接受
note: 这是 RELY-01 的**预期行为而非回归**——存量确实是 SPINE-02 之前的徒手产物，保守标注在事实层正确。唯一值得观察的是观感冲击会不会让用户误以为系统故障。
result: [pending]
not_automatable: 需要生产实例（存量数据规模与用户观感冲击）。

### 6. 下游容器对 dispatch payload `unresearched` 标志的消费策略

expected: 本 phase 只保证标志出现在 payload；容器侧是否据此调整行为由后续决定
note: 跨进程契约的下游半边不在本 phase 边界内（109-07 已显式声明「容器侧消费与否留后续」）
result: [pending]
not_automatable: 这是团队策略决定（要不要让容器消费该标志），不是可断言的行为。

## Summary

total: 6
passed: 2
issues: 0
pending: 4
skipped: 0
blocked: 0

## Gaps

浏览器可验证子集已于 2026-08-02 由 Playwright e2e 执行，详见 `.planning/UAT-AUTOMATION-REPORT.md`。
余下 4 项：2 项需真实飞书 / Git 平台 / Runner、1 项需生产实例、1 项为团队策略决定。
