---
phase: 109-spine-convergence
plan: 07
type: execute
wave: 6
depends_on: ["109-05"]
files_modified:
  - server/chat/serializers.py
  - server/chat/views.py
  - server/chat/coding_session_service.py
  - server/feishu/coding_plan_exporter.py
  - server/tests/test_coding_plans_sessions_api.py
  - server/tests/test_coding_session_service.py
  - server/tests/test_coding_plan_exporter.py
  - server/tests/test_spa_coding_chain_e2e.py
  - server/tests/knowledge/test_triggers.py
autonomous: true
requirements: [RELY-01]
must_haves:
  truths:
    - "草稿方案默认不可直接送编码：未带显式确认标志的 fan-out 请求被服务端拒绝，绕过前端也拒绝（fail-closed）"
    - "拒绝响应带稳定机器码 draft_requires_explicit_confirm，前端可按 code 分支而不必匹配文案"
    - "编排产出的方案不受影响：provenance != draft 时确认标志被忽略、行为与今日一致"
    - "草稿经显式确认送出时，下发给 Runner/容器的执行契约里携带「未经调研」标志，下游可据此判断"
    - "草稿方案的飞书导出物正文顶部带「未经代码调研」告示，判定读 provenance 字段而非匹配正文文案；未知取值同样标注"
    - "草稿送编码的确认与拒绝两条路径都有留痕"
    - "SPA 四步端到端护栏在 gate 落地后仍覆盖草稿形态：既有一条『draft 未确认被 gate 拦』用例，也有一条置为 orchestrated 的四步连通性用例；acknowledge_unresearched 在任何一层都不存在 True 默认值"
  artifacts:
    - path: "server/chat/coding_session_service.py"
      provides: "草稿送编码 gate + ERROR_CODE_DRAFT_REQUIRES_CONFIRM 常量 + CodingExecutionSpec.unresearched"
      contains: "draft_requires_explicit_confirm"
    - path: "server/feishu/coding_plan_exporter.py"
      provides: "_DRAFT_NOTICE 告示块 + _compose_plan_markdown 读 provenance 插入"
      contains: "_DRAFT_NOTICE"
  key_links:
    - from: "server/chat/views.py CodingPlanSessionsBatchCreateView"
      to: "server/chat/coding_session_service.py create_sessions_for_plan"
      via: "acknowledge_unresearched 透传 + provenance==draft 时 fail-closed"
      pattern: "acknowledge_unresearched"
    - from: "server/feishu/coding_plan_exporter.py _compose_plan_markdown"
      to: "chat.models.CodingPlanProvenance"
      via: "允许清单判定（仅 orchestrated 免标注）"
      pattern: "CodingPlanProvenance"
    - from: "server/chat/coding_session_service.py CodingExecutionSpec"
      to: "dispatch payload"
      via: "as_dict() 增补 unresearched 键"
      pattern: "unresearched"
---

<objective>
落地 RELY-01 的服务端半边：草稿方案默认不可送编码（需显式确认，fail-closed 在服务端）、送出时下游携带「未经调研」标志、导出物带「未经代码调研」告示。

Purpose: CONTEXT §草稿标注 要求标注同时出现在界面与飞书导出物两侧、送编码防护默认拒绝且确认后编码上下文携带标志；裁决 D-5 把显式确认的载体定为**请求体布尔字段**（不新开端点）。109-RESEARCH §Common Pitfalls Pitfall 6 明确：只在前端做防护等于没做 —— 用户/脚本直接打 fan-out 端点就能绕过。
Output: fan-out 请求体的 `acknowledge_unresearched` 字段与服务端 gate、稳定机器码、两个留痕事件、`CodingExecutionSpec.unresearched`、飞书导出告示块。
</objective>

<execution_context>
@.cursor/gsd-core/workflows/execute-plan.md
@.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/109-spine-convergence/109-CONTEXT.md
@.planning/phases/109-spine-convergence/109-RESEARCH.md
@.planning/phases/109-spine-convergence/109-UI-SPEC.md
@.planning/phases/109-spine-convergence/109-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: fan-out 端点草稿 gate（fail-closed + 稳定机器码 + 留痕）</name>
  <files>server/chat/serializers.py, server/chat/views.py, server/chat/coding_session_service.py, server/tests/test_coding_plans_sessions_api.py, server/tests/test_spa_coding_chain_e2e.py, server/tests/knowledge/test_triggers.py, server/tests/test_coding_session_service.py</files>
  <read_first>
    - `.planning/phases/109-spine-convergence/109-CONTEXT.md` §草稿标注（送编码防护：默认不可直接送编码，确需送出必须显式确认）与裁决 D-5（载体取请求体布尔字段，不新开端点）
    - `.planning/phases/109-spine-convergence/109-UI-SPEC.md` §前端数据契约变更 —— 后端契约要求第 3/4/5 条（请求体新增布尔字段；🔴 拒绝响应必须带稳定 `code`；`provenance != draft` 时该字段被忽略，这是前端保守默认能安全落地的前提）与 §交互契约 C.3
    - `.planning/phases/109-spine-convergence/109-RESEARCH.md` §Common Pitfalls Pitfall 6（只在前端做防护 = 没做；警示信号是「验收只有前端测试，没有直接打端点的后端测试」）、§9.4 消费点第 3 行、§Observability（`draft_plan_coding_confirmed` / `draft_plan_coding_rejected` 两个事件的字段定义）、§Security Domain（Tampering / Elevation：绕过前端确认）
    - `server/chat/serializers.py:613-638`（`CodingSessionsBatchCreateRequestSerializer` 现有三字段）
    - `server/chat/views.py:2573-2668`（`CodingPlanSessionsBatchCreateView`：owner gate → 项目级 ownership → `create_sessions_for_plan` 的既有顺序）
    - `server/chat/coding_session_service.py:37-47`（`ACTIVE_STATUSES` 与「常量与约束字面一致」纪律）、`:599-604`（`create_sessions_for_plan` 签名）
    - **gate 打红的三个测试文件（逐个都要读，处置见 action 第 6 条）**：`server/tests/test_spa_coding_chain_e2e.py`（109-01 建，ROADMAP 顺序硬约束的唯一物化护栏）、`server/tests/knowledge/test_triggers.py:220`（`_make_fanout_plan()` 造数默认 draft）与 `:829,848`（两处调用）、`server/tests/test_coding_session_service.py:534,552,569`（`TestCreateSessionsForPlan` 三处 service 层直调）
  </read_first>
  <action>
    1. `server/chat/coding_session_service.py` 顶部新增共享常量（与既有 `ERROR_REPO_ACTIVE_BUSY` 同处，避免 service / view 多处硬编码漂移）：
       ```
       ERROR_CODE_DRAFT_REQUIRES_CONFIRM = "draft_requires_explicit_confirm"
       ERROR_DRAFT_REQUIRES_CONFIRM = "草稿方案需显式确认后才能送编码"
       ```
    2. `CodingSessionsBatchCreateRequestSerializer` 新增
       `acknowledge_unresearched = serializers.BooleanField(required=False, default=False, help_text="草稿方案（provenance=draft）送编码的显式确认；provenance != draft 时被忽略")`。
    3. `create_sessions_for_plan` 签名新增关键字参数 `acknowledge_unresearched: bool = False`，并在**函数最前面**（任何 session 创建之前）落 gate：
       - 若 `plan.provenance == CodingPlanProvenance.DRAFT` 且 `acknowledge_unresearched is False` → **不创建任何 session**，抛一个可被 view 识别的领域异常（新增 `class DraftPlanRequiresConfirmError(Exception)`，带 `code = ERROR_CODE_DRAFT_REQUIRES_CONFIRM`）。
       - 判定采**允许清单**：只有 `provenance == ORCHESTRATED` 才完全免 gate；`draft` / 未知取值 / 空值一律按需确认（与前端保守默认同一口径，见 UI-SPEC §B.1）。
       - `provenance != draft` 时 `acknowledge_unresearched` 被**忽略**、不影响结果（后端契约第 5 条）—— 这让前端的保守默认对正常流程无副作用。
       - 留痕（best-effort、不反噬）：拒绝时打 `draft_plan_coding_rejected`（`category="caller"`、`component="chat"`、`coding_plan_id`、`reason="draft_requires_explicit_confirm"`）；通过且 `provenance` 非 orchestrated 时打 `draft_plan_coding_confirmed`（`category="caller"`、`component="chat"`、`coding_plan_id`、`user_id`、`repo_count`）。`user_id` 由中间件注入的上下文提供，无触发用户记 `system`。
    4. `CodingPlanSessionsBatchCreateView.post`：把 `req_ser.validated_data.get("acknowledge_unresearched", False)` 透传给 `create_sessions_for_plan`；捕获 `DraftPlanRequiresConfirmError` → 返回 **400** 且响应体为 `{"code": ERROR_CODE_DRAFT_REQUIRES_CONFIRM, "detail": ERROR_DRAFT_REQUIRES_CONFIRM}`。gate 位置在既有 owner gate / 项目级 ownership **之后**（权限先判，避免用 gate 泄漏方案存在性）。在 `@extend_schema` 的 `responses` 里补 400 描述。
    5. `server/tests/test_coding_plans_sessions_api.py` 新增用例（`-k draft_gate` 可选中），全部**直接打端点**（不经前端）：
       - `provenance=draft` + 请求体**不带** `acknowledge_unresearched` → 400，响应体 `code == "draft_requires_explicit_confirm"`，且 `CodingSession` 计数**未增加**（fail-closed 的实质断言）。
       - `provenance=draft` + `acknowledge_unresearched: false` 显式传 → 同样 400（`false` 不等于确认）。
       - `provenance=draft` + `acknowledge_unresearched: true` → 200 且 session 正常创建。
       - `provenance=orchestrated` + **不带**该字段 → 200（编排方案零摩擦，行为与今日一致）。
       - `provenance=orchestrated` + `acknowledge_unresearched: true` → 200 且结果与不带时一致（字段被忽略）。
       - 未知 `provenance` 取值（直接用 `queryset.update(provenance="weird_value")` 绕过 choices 写入）+ 不带确认 → 400（允许清单的保守分支）。
       - 既有 6 类场景（3 仓全成功 / 部分成功 / 全失败 / 403 / 404 / 400）**全部保持绿**；若既有用例造的 plan 走 DB default `draft`，需给它们补 `acknowledge_unresearched: true` 或把 plan 置为 `orchestrated`，并在注释里写明原因（这是 gate 生效的预期连带影响，不是回归）。
    6. 🔴 **穷举 gate 打红的三个测试文件（每个都必须有明确处置，不得留给执行者临场发挥）**。
       `create_sessions_for_plan` 的全仓调用方只有 `chat/views.py:2645` 与下列三个测试文件；gate 落在函数最前面 ⇒ 这三个文件**必然变红**：

       | 文件 | 破坏方式 | 本 task 的处置 |
       |---|---|---|
       | `server/tests/test_spa_coding_chain_e2e.py` | 109-01 造的 plan 走 DB default `provenance=draft` ⇒ 第③步 fan-out 变 400 | ① **新增**一条用例：`provenance=draft` 且不带确认 → fan-out 被 gate 拦（400 + `code == "draft_requires_explicit_confirm"`），护栏因此**覆盖存量真实形态**；② 原四步用例把 plan 显式置为 `orchestrated`，并在造数处注释「四步连通性护栏测的是编排方案的正常路径；草稿路径由本文件新增的 gate 用例覆盖」 |
       | `server/tests/knowledge/test_triggers.py:829,848` | `_make_fanout_plan()`（`:220`）造的 plan 默认 `draft`，gate 先抛 `DraftPlanRequiresConfirmError`，摄取触发断言到不了 | 两处 `create_sessions_for_plan(...)` 调用补 `acknowledge_unresearched=True`，并在调用处注释「这是 109-07 草稿 gate 生效的预期连带影响，不是回归；本用例测的是摄取触发，不是 gate」 |
       | `server/tests/test_coding_session_service.py::TestCreateSessionsForPlan`（`:534,552,569`） | service 层直调，同样被 gate 拦 | 三处调用补 `acknowledge_unresearched=True` + 同款注释 |

       🔴 **明确禁止的两条"省力修法"**（它们会静默取消 RELY-01 或让护栏失去覆盖）：
       - **不得**给 `acknowledge_unresearched` 设 `True` 默认值（无论在 serializer、`create_sessions_for_plan` 签名还是 view 透传处）—— 那等于让 gate 恒不触发，直接取消 RELY-01。
       - **不得**把 `test_spa_coding_chain_e2e.py` 的 plan 一律置为 `orchestrated` 而不补草稿用例 —— 那会让这条「ROADMAP 顺序硬约束的唯一物化护栏」不再覆盖存量方案的真实形态（迁移 default 是 `draft`，存量全是草稿）。上表第一行的 ①②**两条都要做**，缺一即视为未完成。
  </action>
  <acceptance_criteria>
    - `server/chat/coding_session_service.py` 含常量 `ERROR_CODE_DRAFT_REQUIRES_CONFIRM = "draft_requires_explicit_confirm"` 与 `class DraftPlanRequiresConfirmError`。
    - `create_sessions_for_plan` 签名含 `acknowledge_unresearched`，且 gate 位于任何 session 创建之前（拒绝时 DB 零写入，有测试断言计数不变）。
    - `CodingSessionsBatchCreateRequestSerializer` 含 `acknowledge_unresearched` 布尔字段，`default=False`。
    - 拒绝响应体同时含 `code` 与 `detail` 两键，`code` 取值为 `draft_requires_explicit_confirm`。
    - 事件名 `draft_plan_coding_rejected` 与 `draft_plan_coding_confirmed` 各出现至少一次，均带 `category="caller"` 与 `component="chat"`。
    - `cd server && uv run pytest tests/test_coding_plans_sessions_api.py -x` 全绿，且 `-k draft_gate` 能选中 ≥6 个用例。
    - `rg -n 'acknowledge_unresearched' server/chat/serializers.py server/chat/views.py server/chat/coding_session_service.py` 的结果中**没有任何**把该字段默认为 `True` 的写法（`default=False` / `= False` 才是允许形态）。
    - `server/tests/test_spa_coding_chain_e2e.py` 中同时存在：① 一条 `provenance=draft` 未确认 → fan-out 400 且 `code == "draft_requires_explicit_confirm"` 的用例；② 原四步连通性用例把 plan 显式置为 `orchestrated` 且造数处有注释说明原因。
    - `server/tests/knowledge/test_triggers.py` 的两处 `create_sessions_for_plan` 调用与 `server/tests/test_coding_session_service.py` 的三处调用均带 `acknowledge_unresearched=True`，且各有注释标明「gate 生效的预期连带影响」。
    - `cd server && uv run pytest tests/test_spa_coding_chain_e2e.py tests/knowledge/test_triggers.py tests/test_coding_session_service.py -x` 全绿。
  </acceptance_criteria>
  <verify>
    <automated>cd server && uv run pytest tests/test_coding_plans_sessions_api.py tests/test_spa_coding_chain_e2e.py tests/knowledge/test_triggers.py tests/test_coding_session_service.py -x</automated>
  </verify>
  <done>草稿送编码在服务端 fail-closed，绕过前端也拒绝；机器码稳定；编排方案零摩擦；两条留痕在位。</done>
</task>

<task type="auto">
  <name>Task 2: 编码执行契约携带「未经调研」标志</name>
  <files>server/chat/coding_session_service.py, server/tests/test_coding_session_service.py</files>
  <read_first>
    - `.planning/phases/109-spine-convergence/109-CONTEXT.md` §草稿标注（「编码上下文携带『未经调研』标志（下游可据此判断）」）
    - `.planning/phases/109-spine-convergence/109-RESEARCH.md` §9.4「下游携带」（落点是 `CodingExecutionSpec`，本 phase 至少要让它出现在 dispatch payload 里）
    - `server/chat/coding_session_service.py:63-84`（`CodingExecutionSpec` frozen dataclass 与 `as_dict()`）、`:113-140`（`build_coding_execution_spec` 如何从 session/plan 取值）
    - `server/chat/coding_session_service.py:49-60`（`CREDENTIAL_ENV_KEYS` 落库脱敏名单，确认新增字段不涉凭证）
  </read_first>
  <action>
    1. `CodingExecutionSpec` 新增字段 `unresearched: bool = False`（**带默认值** —— frozen dataclass 新增无默认字段会破坏既有构造点），并在 `as_dict()` 返回体追加 `"unresearched": self.unresearched`。字段 docstring 说明：该方案未经完整编排调研（`CodingPlan.provenance != orchestrated`），下游容器可据此调整策略（本 phase 只保证标志出现在 dispatch payload 里，容器侧消费与否留后续）。
    2. `build_coding_execution_spec` 取 `coding_session.coding_plan` 的 `provenance` 计算该值，判定同样用**允许清单**：`provenance != CodingPlanProvenance.ORCHESTRATED` ⇒ `unresearched=True`。`coding_plan_id` 为空（历史 session 未迁移）时按保守分支 `True`。async 上下文取值走 `*_id` 标量 / `values()` / `afirst`，**不裸访问 lazy FK**。
    3. `server/tests/test_coding_session_service.py` 新增用例（`-k unresearched` 可选中）：
       - `provenance=draft` 的 plan → `build_coding_execution_spec(...).unresearched is True`，且 `as_dict()["unresearched"] is True`。
       - `provenance=orchestrated` → `False`。
       - 未知 `provenance` 取值 → `True`（保守分支）。
       - `coding_plan_id` 为 None 的历史 session → `True`（保守分支，且不抛）。
       - 一条 dispatch payload 断言：`unresearched` 键确实出现在下发结构里（沿用该文件既有对 dispatch metadata 的断言方式；不要求容器侧消费）。
  </action>
  <acceptance_criteria>
    - `CodingExecutionSpec` 含 `unresearched: bool = False`，且 `as_dict()` 返回体含 `"unresearched"` 键。
    - `build_coding_execution_spec` 的判定表达式与 `CodingPlanProvenance.ORCHESTRATED` 做**不等于**比较（允许清单，非拒绝清单）。
    - `cd server && uv run pytest tests/test_coding_session_service.py -x` 全绿，且 `-k unresearched` 能选中 ≥4 个用例。
    - `cd server && uv run pytest tests/test_coding_session_graph.py tests/test_coding_session_graph_e2e.py -x` 全绿（dispatch 链零回归）。
  </acceptance_criteria>
  <verify>
    <automated>cd server && uv run pytest tests/test_coding_session_service.py tests/test_coding_session_graph.py tests/test_coding_session_graph_e2e.py -x</automated>
  </verify>
  <done>执行契约携带 `unresearched` 标志，判定为允许清单且历史数据走保守分支；dispatch 链零回归。</done>
</task>

<task type="auto">
  <name>Task 3: 飞书导出侧「未经代码调研」告示（RELY-01 第二出口）</name>
  <files>server/feishu/coding_plan_exporter.py, server/tests/test_coding_plan_exporter.py</files>
  <read_first>
    - `.planning/phases/109-spine-convergence/109-UI-SPEC.md` §交互契约 D（渲染位置、三条与界面一致的纪律、不加二次弹层的理由）与 §Copywriting Contract 导出侧代码块（**逐字文案**：主句与次行前半段与界面侧逐字一致，导出侧仅追加「正式方案请经技术方案编排产出。」）与 §UI Considerations 第 15 条
    - `.planning/phases/109-spine-convergence/109-RESEARCH.md` §5「飞书导出路径」（`_compose_plan_markdown` 是唯一插入点；不覆盖第三条 ArchitectMerge → 项目 RESEARCH 文档出口及其理由）、§Code Examples「草稿标注（飞书导出侧）」、§Security Domain（存储型注入：既有 `_md_escape` 与 `render_merged_plan_markdown` 的脱敏纵深）
    - `.planning/phases/109-spine-convergence/109-CONTEXT.md` §草稿标注（标注载体是数据层来源标志，不靠文案硬编码 —— 避免新增产出路径时漏标）
    - `server/feishu/coding_plan_exporter.py:184-203`（`_compose_plan_markdown` 与 `parts.append(coding_plan.tech_plan)` 的位置）
    - `server/tests/test_coding_plan_exporter.py`（既有 mock `FeishuDocClient` 的模式；`--disable-socket` 下禁止真实网络）
  </read_first>
  <action>
    1. `server/feishu/coding_plan_exporter.py` 新增模块级常量 `_DRAFT_NOTICE`，内容为 markdown blockquote，**逐字**按 UI-SPEC §Copywriting Contract 导出侧代码块：
       ```
       > ⚠️ **本方案未经代码调研**
       >
       > 由对话直接生成，未经仓库路由、代码召回与并行调研，文件清单与实现步骤可能不准确。正式方案请经技术方案编排产出。
       ```
       在常量旁注释：主句 `本方案未经代码调研` 与次行前半段与界面侧（`TechPlanCard` 草稿横幅）**逐字一致**，导出侧仅追加一句行动指引；双侧口径一致才能让用户在界面与文档间建立同一心智。
    2. `_compose_plan_markdown` 在 `parts.append(f"# {title}")` 之后、`parts.append(coding_plan.tech_plan)`（`:191-193`）**之前**插入告示块。判定三条纪律：
       - 读 `coding_plan.provenance` 字段，**不匹配正文文案**（新增产出路径时正文格式不可控，文案判定必然漏标）；
       - **允许清单**：仅 `provenance == CodingPlanProvenance.ORCHESTRATED` 免标注，其余（含未知取值、空值）一律标注；
       - **不把 `provenance` 原始取值写进文档**（上游非受控值上屏即泄漏面）。
       `_compose_plan_markdown` 是纯同步函数，取 `provenance` 时不触发额外查询（字段已在实例上）。
    3. `server/tests/test_coding_plan_exporter.py` 新增用例（`-k draft` 可选中）：
       - `provenance=draft` → 拼出的 markdown 含 `本方案未经代码调研`，且该告示出现在 `coding_plan.tech_plan` 正文**之前**（用 `str.index` 比较位置）。
       - `provenance=orchestrated` → markdown **不含**该主句。
       - 未知 `provenance` 取值（`"weird_value"`）→ **含**告示（允许清单的保守分支），且 markdown 中**不含** `weird_value` 字符串（不回显原始取值）。
       - 空 `tech_plan` + `provenance=draft` → 告示仍在，且既有 `（暂无技术方案文本）` 兜底不受影响。
       - 一条**双侧口径一致**断言：把界面侧主句与次行前半段作为字面量常量写在测试里，断言导出文案包含这两段（若前端常量变更未同步，此处会红）。
    4. **显式不覆盖的出口**（写进 SUMMARY，避免后续审计误判为遗漏）：`ArchitectMergeAdapter._maybe_bind_plan_to_project` → `ProjectDocService.append_research_note` 这条镜像出口只走**编排产物**（永远是 `orchestrated`，不可能是草稿），因此 RELY-01 的双侧标注不需要覆盖它。
  </action>
  <acceptance_criteria>
    - `server/feishu/coding_plan_exporter.py` 含 `_DRAFT_NOTICE` 常量，其文本含 `本方案未经代码调研` 与 `正式方案请经技术方案编排产出。`。
    - `_compose_plan_markdown` 中判定表达式与 `CodingPlanProvenance.ORCHESTRATED` 做不等于比较（允许清单），且插入位置在 `tech_plan` 之前。
    - 文件中**不存在**把 `coding_plan.provenance` 原始取值拼进 markdown 的代码。
    - `cd server && uv run pytest tests/test_coding_plan_exporter.py -x` 全绿，且 `-k draft` 能选中 ≥4 个用例，其中含未知取值仍标注、且文档不含原始取值的断言。
    - `cd server && uv run pytest tests/test_coding_plan_export_api.py -x` 全绿（导出端点零回归）。
  </acceptance_criteria>
  <verify>
    <automated>cd server && uv run pytest tests/test_coding_plan_exporter.py tests/test_coding_plan_export_api.py -x</automated>
  </verify>
  <done>导出物在正文之前带「未经代码调研」告示，判定为数据驱动的允许清单，未知取值保守标注且不回显原始取值。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 任意 HTTP 客户端 ↔ fan-out 端点 | 用户/脚本可绕过前端弹层直接打端点，防护必须在服务端 |
| `CodingPlan.provenance` ↔ 导出物与执行契约 | 单一数据真源，界面与导出各读一处；文案判定必然漏标 |
| chat 编码域 ↔ Runner/容器 | dispatch payload 跨进程边界，标志需随契约下发 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-109-07-01 | Elevation of Privilege | `create_sessions_for_plan` | mitigate | gate 落在 service 内、位于任何 session 创建之前；直接打端点的后端用例断言 DB 零写入（Pitfall 6 的警示信号被显式消除） |
| T-109-07-02 | Tampering | `acknowledge_unresearched` | mitigate | 只有布尔 `true` 视为确认；`false` 与缺字段一律拒绝；`provenance != draft` 时忽略该字段，防止用它影响正常流程 |
| T-109-07-03 | Repudiation | 草稿送编码 | mitigate | `draft_plan_coding_confirmed`（含 `user_id`、`repo_count`）与 `draft_plan_coding_rejected`（含 `reason`）两条留痕 |
| T-109-07-04 | Information Disclosure | 导出文档 | mitigate | 不把 `provenance` 原始取值写进文档；沿用既有 `_md_escape` 与 `render_merged_plan_markdown` 的脱敏纵深（不 dump LLM 原文） |
| T-109-07-05 | Information Disclosure | gate 与权限门顺序 | mitigate | gate 置于 owner gate / 项目级 ownership **之后**，避免用 400 泄漏方案存在性 |
| T-109-07-06 | Spoofing | 未知 `provenance` 取值 | mitigate | 三处判定（gate / 执行契约 / 导出）统一用允许清单：仅 `orchestrated` 免标注/免确认 |
| T-109-07-SC | Tampering | npm/pip/cargo installs | accept | 本 plan 零新增依赖 |
</threat_model>

<verification>
- `cd server && uv run pytest tests/test_coding_plans_sessions_api.py tests/test_coding_session_service.py tests/test_coding_plan_exporter.py tests/test_coding_plan_export_api.py -x` 全绿。
- `cd server && uv run pytest tests/test_spa_coding_chain_e2e.py tests/knowledge/test_triggers.py tests/test_plan_projection_api.py tests/mcp_tools/ -x` 全绿（执行流护栏与 MCP 侧零回归）。三个被 gate 打红的测试文件的处置由 **Task 1 第 6 条**逐文件穷举指定并纳入其验收 —— 不是「若有需要则处理」的可选项。
- 观测自检：两个新事件带 `category` / `component` / 可归因触发用户；无凭证入日志；未新增 `call_source`、未新增召回。
</verification>

<success_criteria>
- 草稿方案未经显式确认无法送编码，且拒绝时 DB 零写入（服务端 fail-closed）。
- 拒绝响应带稳定 `code`，前端无需匹配文案。
- 编排方案零摩擦，行为与今日一致。
- 送出时 dispatch payload 携带 `unresearched` 标志。
- 导出物带「未经代码调研」告示，与界面主句逐字一致，未知取值保守标注。
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces（本 plan 部分）

**新建符号（后端）**
- `chat.coding_session_service.ERROR_CODE_DRAFT_REQUIRES_CONFIRM = "draft_requires_explicit_confirm"`
- `chat.coding_session_service.ERROR_DRAFT_REQUIRES_CONFIRM`（中文文案常量）
- `chat.coding_session_service.DraftPlanRequiresConfirmError`
- `chat.coding_session_service.CodingExecutionSpec.unresearched`（bool，default False；`as_dict()` 新增同名键）
- `create_sessions_for_plan` 新增关键字参数 `acknowledge_unresearched`
- `chat.serializers.CodingSessionsBatchCreateRequestSerializer.acknowledge_unresearched`
- `feishu.coding_plan_exporter._DRAFT_NOTICE`（导出侧告示 markdown 常量）

**新建错误机器码**
- `draft_requires_explicit_confirm`（fan-out 端点 400）

**新建观测事件名**
- `draft_plan_coding_confirmed`（`category="caller"`、`component="chat"`）
- `draft_plan_coding_rejected`（同上）

**新建文件 / 迁移 / 端点 / 前端符号**：无（沿用既有 fan-out 端点，裁决 D-5）
</artifacts_produced>

<output>
Create `.planning/phases/109-spine-convergence/109-07-SUMMARY.md` when done
</output>
