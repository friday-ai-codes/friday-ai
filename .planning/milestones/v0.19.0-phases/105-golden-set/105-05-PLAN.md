---
phase: 105-golden-set
plan: 05
type: execute
wave: 3
depends_on: [105-03]
files_modified:
  - server/agents/llm_factory.py
  - server/codegraph/services/repo_router_v2.py
  - server/friday/settings.py
  - .env.example
  - server/tests/test_llm_factory.py
  - server/tests/codegraph/test_repo_router_stage1_idempotency.py
autonomous: true
requirements: [ROUTE-09, RELY-04]
must_haves:
  truths:
    - "同一需求 + 同一索引状态重复路由两次，候选顺序与分数完全相同（Stage 1 可用路径经输入哈希缓存保证；缓存命中零 LLM 调用）"
    - "Stage 1 LLM 只输出排列（有序 repo_id 数组 + 文本字段），不输出浮点分数；decode 参数全固定（temperature=0, top_p=1, 固定 seed）"
    - "缓存 key 由 sha256(model_id ‖ prompt_template_version ‖ canonical_json(stage0_input) ‖ decode_params ‖ index_version) 构成——重索引后 key 变化，旧缓存自然失效"
    - "Stage 1 调用统一包 use_call_source(CallSource.AUX_REPO_ROUTER) 作用域（消灭调用方遗漏）"
    - "result.snapshot 补齐 stage1 材料：redact 后的 prompt/response、model_id、prompt_hash（版本绑定四元组齐备）"
  artifacts:
    - path: server/codegraph/services/repo_router_v2.py
      provides: "Stage 1 幂等三件套：输入哈希缓存 + 排列输出 + decode 固定"
      contains: "PROMPT_TEMPLATE_VERSION"
    - path: server/agents/llm_factory.py
      provides: "build_chat_model 可选 temperature/top_p/seed 透传"
    - path: server/tests/codegraph/test_repo_router_stage1_idempotency.py
      provides: "双跑幂等 + 缓存命中零调用 + 禁缓存纯函数同结果 守护"
      min_lines: 80
  key_links:
    - from: server/codegraph/services/repo_router_v2.py
      to: django.core.cache
      via: "django cache（Redis/LocMem 双轨已配，IGNORE_EXCEPTIONS=True）存 parsed 排列"
      pattern: "from django.core.cache import cache"
    - from: server/codegraph/services/repo_router_v2.py
      to: server/agents/call_source.py
      via: "use_call_source(CallSource.AUX_REPO_ROUTER) 包裹 ainvoke"
      pattern: "AUX_REPO_ROUTER"
---

<objective>
落实 CONTEXT「LLM 幂等三件套」（ROUTE-09）：(1) Stage 1 输入哈希缓存（key 绑定模型/模板版本/规范化输入/decode 参数/索引版本，Django cache 存储）；(2) LLM 只输出排列不输出浮点分数（离散低熵输出对 logits 微扰鲁棒——temperature=0 不足以保证幂等，ROUTING-RANKING §6.1 batch invariance 结论）；(3) decode 参数全固定（temperature=0/top_p=1/固定 seed，候选按 Stage 0 分数降序喂入已由 105-03 保证）。同时补齐 Stage 1 观测（call_source 统一作用域）与 snapshot 的 stage1 材料（脱敏 prompt/response + prompt_hash + model_id）。

Purpose: success criterion 3 的「Stage 1 可用情形下重复路由完全相同」由系统层（缓存+排列+固定 decode）保证，不依赖模型层。
Output: llm_factory 扩展 + stage1 重写 + 幂等测试。
</objective>

<context>
@.planning/phases/105-golden-set/105-CONTEXT.md
@.planning/phases/105-golden-set/105-RESEARCH.md（§9 缓存/观测基建 + Open Question 1/A4 + Pitfall 7）
@server/codegraph/services/repo_router_v2.py
@server/agents/llm_factory.py
@server/agents/call_source.py
</context>

## Artifacts this phase produces（本 plan 新建/变更符号）

| Symbol | Kind | Location |
|--------|------|----------|
| `build_chat_model(..., temperature=None, top_p=None, seed=None)` | 函数形参扩展 | `server/agents/llm_factory.py` |
| `PROMPT_TEMPLATE_VERSION = "stage1-permutation-v1"` | 常量 | `server/codegraph/services/repo_router_v2.py` |
| `_stage1_cache_key(model_id, stage0_input, decode_params, index_version) -> str` | 私有函数 | 同上 |
| `REPO_ROUTER_STAGE1_CACHE_TTL_SECONDS`（默认 86400） | settings 键 | `server/friday/settings.py` |
| 事件 `repo_router_v2_stage1_cache_hit`（debug，sampling） | 日志事件 | `server/codegraph/services/repo_router_v2.py` |

<tasks>

<task type="auto">
  <name>Task 1: build_chat_model 扩展 decode 参数透传</name>
  <files>server/agents/llm_factory.py, server/tests/test_llm_factory.py</files>
  <read_first>
    - server/agents/llm_factory.py（`build_chat_model` 现签名 line 64-75 与各 provider 分支的 kwargs 分派方式——A4 已验证现签名无 temperature/top_p/seed 形参）
    - server/tests/test_llm_factory.py（既有 llm_factory 测试的 mock/断言风格——注意测试在 tests/ 顶层，不在 tests/agents/）
    - .planning/phases/105-golden-set/105-RESEARCH.md Open Question 1（recommendation：可选形参透传；seed 不被支持的 provider 记 debug log 静默忽略）
  </read_first>
  <action>
    给 `build_chat_model` 加三个可选形参 `temperature: float | None = None, top_p: float | None = None, seed: int | None = None`（默认 None = 行为与现状逐字一致，零回归面）。按既有 capabilities/kwargs 分派模式把非 None 值传给各 provider 构造（anthropic/openai/gemini/ollama 分支）；某 provider 构造不支持某参数（如 seed）时不传并 `logger.debug("llm_decode_param_ignored", param=..., provider_type=..., category="sampling", component="llm_factory")` 静默忽略——幂等主防线是缓存+排列输出，decode 固定是第三道防线（per RESEARCH）。docstring 补充参数说明。不改任何既有调用方。

    在 `server/tests/test_llm_factory.py` 按既有风格补透传用例：传 temperature/top_p/seed 时对应值到达 provider 构造 kwargs；默认 None 时构造 kwargs 与现状一致（无新键）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/test_llm_factory.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `build_chat_model` 签名含 temperature/top_p/seed 三个默认 None 形参
    - 全部既有 llm_factory 测试（tests/test_llm_factory.py）零改动通过（默认 None 路径行为不变）
    - 新增透传用例 >= 2（非 None 透传到构造 kwargs + 默认 None 无新键）
  </acceptance_criteria>
  <done>decode 参数可注入且向后兼容。</done>
</task>

<task type="auto">
  <name>Task 2: Stage 1 重写——排列输出 + 输入哈希缓存 + call_source + snapshot 材料</name>
  <files>server/codegraph/services/repo_router_v2.py, server/friday/settings.py, .env.example</files>
  <read_first>
    - server/codegraph/services/repo_router_v2.py（105-03 后现状：`_stage1_llm_reasoning` 的 prompt 组装 / `_parse_llm_json_array` / 白名单校验 / apply_llm_adjustment 接入点）
    - .planning/phases/105-golden-set/105-RESEARCH.md「Code Examples: 输入哈希缓存 key」与「index_version 的现实口径」（参与候选各仓 built_at 拼接哈希——105-03 已在 snapshot.versions 算过，复用同一 helper）
    - server/interactions/redaction.py（`redact_for_ledger` 签名——注意 import 路径是 interactions.redaction，不是 common/logging）
    - server/agents/call_source.py（`CallSource.AUX_REPO_ROUTER` 与 `use_call_source` 上下文管理器；先例 server/services/process_runtime/feature_classify.py）
  </read_first>
  <action>
    改造 `_stage1_llm_reasoning`（CONTEXT §确定性与快照回放）：

    1. **排列输出**：system prompt 改为要求输出「按相关度降序的 JSON 数组」，每项 `{"repo_id": str, "sub_project": str, "reasoning": str, "matched_node_paths": [str], "confidence": "high"|"medium"|"low"}`——**明确禁止输出任何数值分数字段**；数组顺序即排列（当前行为本就以 LLM 顺序为候选顺序，保持）。`confidence` 仅作降级提示信号（105-03 已实现 apply_llm_adjustment 只降不升，本 task 不改该逻辑）。解析侧：`_parse_llm_json_array` 后过滤含数值 "score" 键的项中的该键（防模型不遵指令引入浮点）。模块常量 `PROMPT_TEMPLATE_VERSION = "stage1-permutation-v1"`（prompt 文案变更时必须递增，注释说明其参与缓存 key 与 prompt_hash）。
    2. **decode 固定**：`build_chat_model(..., temperature=0.0, top_p=1.0, seed=42)`（Task 1 形参）。
    3. **输入哈希缓存**：新增 `_stage1_cache_key(model_id, stage0_input, decode_params, index_version)`——`canonical_json = json.dumps(obj, sort_keys=True, ensure_ascii=False, separators=(",", ":"))`；`key_material = "\x1f".join([model_id, PROMPT_TEMPLATE_VERSION, canonical_json(stage0_input), canonical_json(decode_params), index_version])`；返回 `f"repo_router_v2:stage1:{sha256(key_material).hexdigest()}"`。`stage0_input` = 喂给 LLM 的候选材料（repo_id + 有序 hits 的 node_path/summary，即 context_blocks 的结构化源）。ainvoke 前 `cache.get(key)` 命中则跳过 LLM 直接用缓存的 parsed 列表，记 `logger.debug("repo_router_v2_stage1_cache_hit", cache_key_prefix=key[:24], category="sampling", component="repo_router_v2")`；未命中则调用后 `cache.set(key, parsed, timeout=REPO_ROUTER_STAGE1_CACHE_TTL_SECONDS)`。缓存读写 best-effort（异常吞掉走直调，绝不反噬路由）。settings 加 `REPO_ROUTER_STAGE1_CACHE_TTL_SECONDS = env.int(..., default=86400)`（紧邻 STAGE1 段落，注释：key 已含 index_version，TTL 仅兜底防无界增长）+ `.env.example` 补一行。
    4. **call_source 统一作用域**：`ainvoke` 调用包进 `use_call_source(CallSource.AUX_REPO_ROUTER)`（在 router 内部统一包，消灭调用方遗漏——RESEARCH §9 现状缺口）。
    5. **snapshot stage1 材料**：Stage 1 成功路径把 `{"prompt": redact_for_ledger(prompt 文本), "response": redact_for_ledger(response 文本), "model_id": model_name, "prompt_hash": sha256(system+human 文本).hexdigest(), "cache_hit": bool}` 并入 `result.snapshot["stage1"]`；`snapshot["versions"]` 补 `prompt_hash` 与 `model_id`（与 105-03 的 weight_set_version/index_version 合成完整四元组）。失联/降级路径写 `snapshot["stage1"] = {"skipped_reason": ...}`。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/codegraph/test_repo_router_v2_degraded.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `rg -c "PROMPT_TEMPLATE_VERSION" server/codegraph/services/repo_router_v2.py` >= 2（定义 + 入 key）
    - `rg -c "use_call_source" server/codegraph/services/repo_router_v2.py` >= 1
    - `rg -c "redact_for_ledger" server/codegraph/services/repo_router_v2.py` >= 2（prompt 与 response 均脱敏）
    - system prompt 文本含「不要输出」数值分数类指令；settings 含 `REPO_ROUTER_STAGE1_CACHE_TTL_SECONDS`
    - 105-03 的 degraded 测试零改动通过（降级链行为不变）
  </acceptance_criteria>
  <done>幂等三件套 + call_source + snapshot 四元组齐备。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 幂等行为测试</name>
  <files>server/tests/codegraph/test_repo_router_stage1_idempotency.py</files>
  <read_first>
    - server/codegraph/services/repo_router_v2.py（Task 2 产物）
    - server/tests/conftest.py（`fake_chat_model_factory` seam + `_clear_throttle_cache` autouse 每测试 cache.clear()——Pitfall 7：确定性测试须分别断言缓存命中路径与禁缓存路径）
  </read_first>
  <behavior>
    - 双跑幂等（缓存路径）：fake model 带调用计数器；同 query 连续两次 route() → 第二次 LLM 调用数为 0（缓存命中），两次结果 `[c.to_dict() for c in candidates]` 逐字段相等，auto_selected/degraded/router_version 相等
    - 禁缓存纯函数同结果：monkeypatch cache 为 no-op（get 恒 None/set 吞掉）→ fake model 返回固定响应 → 两次 route() 结果仍逐字段相等（确定性不靠缓存，Pitfall 7 假绿防护）
    - key 敏感性：改变 stage0_input（不同 query 的 node_hits）→ cache key 不同；改变 index_version（built_at 变化）→ key 不同（直接断言 _stage1_cache_key 输出）
    - LLM 输出含数值 score 字段 → 被过滤，候选分数仍为 Stage 0 归一化分
    - 缓存后端异常（monkeypatch cache.get raise）→ 路由正常完成（best-effort 不反噬）
  </behavior>
  <action>
    写 `server/tests/codegraph/test_repo_router_stage1_idempotency.py`，Stage 0 用 monkeypatch `_stage0_node_search` 固定输入，Stage 1 用 conftest fake seam；覆盖 behavior 全部条目。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/codegraph/test_repo_router_stage1_idempotency.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - 「第二次调用 LLM 次数 == 0」有显式断言（调用计数器）
    - 缓存命中与禁缓存两条路径都有「结果逐字段相等」断言
    - `uv run pytest tests/codegraph/test_repo_router_stage1_idempotency.py -q` 全绿，用例数 >= 5
  </acceptance_criteria>
  <done>success criterion 3 的「Stage 1 可用情形重复路由完全相同」被自动化守护。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM 响应 → 缓存/快照 | 不可信内容进入持久层 |
| Stage 1 prompt（含仓库 summary） → snapshot | 可能含敏感串 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-105-10 | Information Disclosure | snapshot.stage1 | mitigate | prompt/response 入 snapshot 前必经 `interactions.redaction.redact_for_ledger`；105-07 补「快照无 sk- 模式」断言 |
| T-105-11 | Tampering | 缓存投毒 | mitigate | cache key 含 sha256 全输入绑定；value 仅为 parsed 排列，消费前仍过 repo_id 白名单校验（105-03 保留的防线） |
| T-105-12 | DoS | 缓存无界增长 | mitigate | TTL 86400 兜底；key 前缀 repo_router_v2:stage1: 可观测 |
| T-105-SC | Tampering | 包安装 | accept | 零新增依赖（hashlib/json/django cache 均既有） |
</threat_model>

<verification>
- `cd server && uv run pytest tests/codegraph/ tests/test_llm_factory.py -q` 全绿
</verification>

<success_criteria>
- Stage 1 可用路径双跑结果完全相同（缓存命中零 LLM 调用；禁缓存纯函数仍同结果）
- decode 参数固定、排列输出、call_source 作用域、snapshot 版本四元组齐备
</success_criteria>

<output>
完成后创建 `.planning/phases/105-golden-set/105-05-SUMMARY.md`。
</output>
