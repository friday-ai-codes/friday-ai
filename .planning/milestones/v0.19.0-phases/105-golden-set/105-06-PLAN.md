---
phase: 105-golden-set
plan: 06
type: execute
wave: 3
depends_on: [105-03]
files_modified:
  - server/agents/tools/schemas/repository_relevance.py
  - server/agents/tools/repository_relevance.py
  - server/tests/agents/test_repository_relevance_tool.py
  - web/src/types/routing.ts
  - web/src/components/chat/RoutingDecisionPanel.vue
  - web/src/components/chat/__tests__/RoutingDecisionPanel.test.ts
autonomous: true
requirements: [ROUTE-07]
must_haves:
  truths:
    - "路由结果中每个带 breakdown 的候选可展开，逐信号显示中文标签 + 贡献值（3 位小数、font-mono），含分隔线与「合计」行，合计等于候选总分（success criterion 2 前端部分）"
    - "既有 高/中/低 Badge 语义升级为确定性分级，外包 Tooltip 显示分级依据一句话；RelevanceBadge 复用点自动受益，无结构改动"
    - "数据契约：RoutingCandidate 新增可选 breakdown?: Record<string, number>；后端从 RepoRouterV2 候选经 RepositoryRelevanceCandidate / RepositoryRoutingTrace.candidates 透传到前端"
    - "信号名映射表未知 key 回退英文原名——Phase 106 新信号零前端改动即可展示"
    - "视觉零漂移：不新增颜色/字号/间距值/第三方依赖；复用 ui/collapsible；遵守 DESIGN.md Badge 禁令与彩虹卡片禁令"
    - statement: "breakdown 缺失（legacy 路径/历史 trace/v1_fallback）时不渲染 trigger，候选行与现状逐像素一致；无空态占位、无报错"
      verification: backstop
    - statement: "前端 |Σbreakdown − score| > 1e-6 时仅 console.warn，正常渲染不阻断（不变量由后端测试守护）"
      verification: backstop
    - statement: "展开态为组件本地 ref，trace 更新（manual_override 写新 trace）后重置为收起"
      verification: backstop
    - statement: "RoutingDecisionPanel 测试补充：有 breakdown 可展开且合计行等于 score、无 breakdown 不渲染 trigger 两条用例"
      verification: backstop
  artifacts:
    - path: web/src/components/chat/RoutingDecisionPanel.vue
      provides: "候选行下的「分数分解」Collapsible 展开区 + confidence Tooltip"
      contains: "Collapsible"
    - path: server/agents/tools/schemas/repository_relevance.py
      provides: "RepositoryRelevanceCandidate.breakdown 可选字段"
      contains: "breakdown"
  key_links:
    - from: server/agents/tools/repository_relevance.py
      to: web/src/components/chat/RoutingDecisionPanel.vue
      via: "v2 候选 breakdown → RepositoryRelevanceCandidate → RepositoryRoutingTrace.candidates JSON → useRoutingStore → panel"
      pattern: "breakdown"
---

<objective>
落实 success criterion 2 的用户可见部分（ROUTE-07 最小前端展开，UI 契约以 `105-UI-SPEC.md` 为准）：用户在路由结果里展开任一候选仓，能看到每个信号的贡献值且各项之和恰等于总分。数据链：`RepoRouterV2` 候选 `breakdown`（105-03 已产出）→ `RepositoryRelevanceCandidate`（pydantic 加可选字段）→ `RepositoryRoutingTrace.candidates` JSON → `useRoutingStore` → `RoutingDecisionPanel.vue` Collapsible 展开区。完整分组呈现/跨组标注/降级标注 UI 归 Phase 107（不做）。

Purpose: 分数可解释性对用户可感知；确定性 confidence 语义经 Tooltip 透出。
Output: 后端透传 + 前端展开 UI + 双端测试。
</objective>

<context>
@.planning/phases/105-golden-set/105-UI-SPEC.md
@.planning/phases/105-golden-set/105-CONTEXT.md
@server/agents/tools/repository_relevance.py
@server/agents/tools/schemas/repository_relevance.py
@web/src/components/chat/RoutingDecisionPanel.vue
@web/src/types/routing.ts
</context>

## Artifacts this phase produces（本 plan 新建/变更符号）

| Symbol | Kind | Location |
|--------|------|----------|
| `RepositoryRelevanceCandidate.breakdown: dict[str, float]`（默认空 dict） | pydantic 字段 | `server/agents/tools/schemas/repository_relevance.py` |
| `RoutingCandidate.breakdown?: Record<string, number>` | TS 接口字段 | `web/src/types/routing.ts` |
| `SIGNAL_LABELS`（text→文本相关 / breadth→命中广度 / activity→活跃度，未知 key 回退原名） | 常量 | `web/src/components/chat/RoutingDecisionPanel.vue` |
| `CONFIDENCE_TOOLTIPS`（high/medium/low 分级依据文案） | 常量 | 同上 |

## Assumptions（UI-SPEC Unresolved 项的显式处理）

- breakdown 信号 key 最终清单以 105-01 落地的 `SIGNAL_TEXT/SIGNAL_BREADTH/SIGNAL_ACTIVITY`（"text"/"breadth"/"activity"）为准；前端映射表与之对齐，未知 key 有回退，不阻塞（UI-SPEC Unresolved 1）。
- `degraded` 降级标注 UI、分组呈现、编排链路由结果前端呈现、组件家族 i18n 迁移——均明确不做，归 Phase 107/110（UI-SPEC Unresolved 2–5）；本 plan 文案沿用组件家族硬编码中文惯例。

<tasks>

<task type="auto">
  <name>Task 1: 后端 breakdown 透传（schema + v2 路径）</name>
  <files>server/agents/tools/schemas/repository_relevance.py, server/agents/tools/repository_relevance.py, server/tests/agents/test_repository_relevance_tool.py</files>
  <read_first>
    - server/agents/tools/schemas/repository_relevance.py（`RepositoryRelevanceCandidate` 现字段；注意 `score` 已有 ge=0/le=1 约束——归一化分天然满足；schema snapshot 守护机制见模块 docstring）
    - server/agents/tools/repository_relevance.py:204-260（v2 路径候选构造——`RepositoryRelevanceCandidate(...)` 调用点、`level=c.confidence` 映射、`selected = high or (medium and score>=threshold)` 行为）
    - server/tests/agents/test_repository_relevance_tool.py（schema snapshot fixture 的更新方式）
  </read_first>
  <action>
    1. `RepositoryRelevanceCandidate` 加 `breakdown: dict[str, float] = Field(default_factory=dict)`（可选带默认——legacy 路径为空 dict，历史 trace 反序列化不破坏）。
    2. `repository_relevance.py` v2 路径候选构造处透传 `breakdown=c.breakdown`（v2 candidates 来自 `RepoRouterV2`，105-03 后必有该字段）；legacy 聚合路径不赋值（默认空 dict）。
    3. 补/更新测试：v2 路径产出的 trace candidates JSON 含 breakdown 且 Σ值≈score（1e-6 容差）；legacy 路径 candidates 的 breakdown 为空 dict；schema snapshot fixture 若覆盖 candidate 结构则按守护流程更新并在 commit 中体现 diff。
    4. `selected = high or (medium and score>=threshold)` 逻辑不改——confidence 语义升级后该行为自动随确定性分级变化，为其补一条行为断言（margin 达标输入 → selected=True）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/agents/test_repository_relevance_tool.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `RepositoryRelevanceCandidate(repository_id="r", repository_name="n", score=0.5, level="low", evidence="", selected_by_ai=False, selected_by_user_final=False).breakdown == {}`（默认值兼容）
    - v2 路径测试断言 trace JSON 中候选含非空 breakdown
    - 既有测试零回归
  </acceptance_criteria>
  <done>breakdown 从 router 一路可达 RepositoryRoutingTrace.candidates JSON。</done>
</task>

<task type="auto">
  <name>Task 2: 前端「分数分解」展开区 + confidence Tooltip</name>
  <files>web/src/types/routing.ts, web/src/components/chat/RoutingDecisionPanel.vue</files>
  <read_first>
    - .planning/phases/105-golden-set/105-UI-SPEC.md（交互契约/文案契约/间距/字号——逐条为准：CollapsibleTrigger 独立一行、非手风琴、3 位小数 font-mono、合计行 font-semibold、合计直接显示 candidate.score、和校验仅 console.warn、缺失不渲染 trigger）
    - web/src/components/chat/RoutingDecisionPanel.vue（现结构：candidate 行 Checkbox + 名称 + Badge + evidence Tooltip；`variantOf`/`labelOf` 既有映射不改）
    - web/src/components/ui/collapsible/（既有 Collapsible/CollapsibleTrigger/CollapsibleContent 封装的导出名与用法）
  </read_first>
  <action>
    1. `web/src/types/routing.ts`：`RoutingCandidate` 加 `breakdown?: Record<string, number>`（注释：Σ值 == score，后端 INV-R1/R3 保证）。
    2. `RoutingDecisionPanel.vue` 按 UI-SPEC 交互契约实现：
       - 每候选 `li` 内、候选行下方渲染独立 `Collapsible`（`v-if="c.breakdown && Object.keys(c.breakdown).length"`，缺失不渲染 trigger——静默降级）；`CollapsibleTrigger` 一行：`icon-[lucide--chevron-right]`（展开旋转 90°，`transition-transform`）+ 文案「分数分解」（`text-xs text-muted-foreground`）。默认收起、多候选可同时展开（每候选本地 ref，非手风琴）；trace 更新后展开态随组件状态自然重置。
       - `CollapsibleContent`：明细行两列（左信号中文标签 `text-xs text-muted-foreground`，右贡献值 `text-xs font-mono text-foreground` 右对齐 `toFixed(3)`）→ `border-t border-border/50` 分隔线 → 合计行（左「合计」右 `c.score.toFixed(3)` `font-semibold font-mono`）。**合计直接显示 candidate.score，不前端求和**；挂一次容差校验 `Math.abs(sum - score) > 1e-6 → console.warn`（不阻断渲染）。展开区左缩进 `pl-3` 对齐内容列，间距仅用 gap-1/gap-2/px-2/py-1（UI-SPEC Spacing）。
       - `SIGNAL_LABELS: Record<string, string> = { text: '文本相关', breadth: '命中广度', activity: '活跃度' }`；未知 key 回退显示原始英文 key。
       - confidence Badge 外包既有 Tooltip：`CONFIDENCE_TOOLTIPS = { high: '高置信：首位分数与领先幅度均超过阈值，由分数确定性推导', medium: '中置信：首位分数达标但领先幅度不足，建议人工确认', low: '低置信：候选分数整体偏低，请人工选择' }`（UI-SPEC Copywriting 原文）。Badge variant 映射不改、不 :class 覆色（DESIGN.md 禁令）；不给信号行分配颜色（彩虹卡片禁令）。
       - `degraded` 不显示（Phase 107 范围）。
  </action>
  <verify>
    <automated>cd web && pnpm exec vue-tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `rg -c "Collapsible" web/src/components/chat/RoutingDecisionPanel.vue` >= 3（Collapsible/Trigger/Content）
    - `rg -c "toFixed\(3\)" web/src/components/chat/RoutingDecisionPanel.vue` >= 2（贡献值 + 合计）
    - `rg -c "console\.warn" web/src/components/chat/RoutingDecisionPanel.vue` >= 1 且和校验不影响渲染分支
    - 组件不新增任何第三方依赖（package.json 无 diff）；无新增颜色 token / 内联样式
  </acceptance_criteria>
  <done>展开可见分解、合计==总分、confidence Tooltip 透出，全部对齐 UI-SPEC。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 组件测试补用例</name>
  <files>web/src/components/chat/__tests__/RoutingDecisionPanel.test.ts</files>
  <read_first>
    - web/src/components/chat/__tests__/RoutingDecisionPanel.test.ts（既有 mount/store 注入方式）
  </read_first>
  <behavior>
    - 有 breakdown 的候选：trigger 可见 → 点击展开 → 明细行数 == breakdown 键数、信号中文标签正确、未知 key（构造 "novel_signal"）显示原名、合计行文本 == score.toFixed(3)
    - 无 breakdown 的候选（字段缺失与空 dict 两种）：不渲染「分数分解」trigger，候选行既有元素（Checkbox/名称/Badge/Tooltip）齐备
    - Σbreakdown 与 score 偏差 > 1e-6 的构造数据：仍正常渲染合计行，console.warn 被调用（spy）
  </behavior>
  <action>
    按既有测试风格补三组用例（fixture candidate 带/不带 breakdown）；vitest + @vue/test-utils。
  </action>
  <verify>
    <automated>cd web && pnpm exec vitest run src/components/chat/__tests__/RoutingDecisionPanel.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - 新用例 >= 3 条且全绿；既有用例零改动通过
  </acceptance_criteria>
  <done>UI-SPEC Backstop 4 的两条守护用例 + 容差告警用例入库。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| trace JSON → 前端渲染 | breakdown 键名为后端产出，前端做纯文本插值展示 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-105-13 | Spoofing/XSS | RoutingDecisionPanel breakdown 渲染 | mitigate | Vue 模板插值自动转义；不用 v-html；未知 key 直接文本展示 |
| T-105-14 | Information Disclosure | breakdown 值 | accept | 贡献值为归一化分数，无敏感信息 |
| T-105-SC | Tampering | 包安装 | accept | 零新增依赖（复用 ui/collapsible） |
</threat_model>

<verification>
- `cd server && uv run pytest tests/agents/test_repository_relevance_tool.py -q` 全绿
- `cd web && pnpm exec vue-tsc --noEmit && pnpm exec vitest run src/components/chat/__tests__/RoutingDecisionPanel.test.ts` 全绿
</verification>

<success_criteria>
- Phase success criterion 2 用户侧成立：展开任一候选可见各信号贡献值且和恰等于总分
- UI-SPEC Covered 5 条全落地；Backstop 4 条行为实现且有测试；Unresolved 项均显式记为 assumption 未静默丢弃
</success_criteria>

<output>
完成后创建 `.planning/phases/105-golden-set/105-06-SUMMARY.md`。
</output>
