---
quick_id: 260817-xb9
phase: 260817-xb9-readable-repo-research-detail
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/services/process_runtime/blueprint_research_adapter.py
  - server/subagent/api/callbacks.py
  - server/delivery/services/event_taxonomy.py
  - server/tests/services/process_runtime/test_blueprint_research_stage.py
  - server/tests/subagent/test_blueprint_research_callback.py
  - web/src/locales/zh-CN.json
  - web/src/utils/blueprintActivity.ts
  - web/src/components/blueprint/BlueprintStageStepper.vue
  - web/src/utils/__tests__/blueprintBlocks.test.ts
  - web/src/utils/__tests__/blueprintActivity.spec.ts
  - web/src/components/blueprint/__tests__/stageStepper.spec.ts
autonomous: true
requirements:
  - UX-RESEARCH-DETAIL-01
  - UX-RESEARCH-DETAIL-02
  - UX-RESEARCH-DETAIL-03
user_setup: []
must_haves:
  truths:
    - "仓库调研 started 事件标题显示仓库名（有 name 时不再回退到「正在调研相关仓库…」）"
    - "每条 started 明细含一句人话 research_reason（路由理由 / 主落点或支撑仓 / 命中说明），且 ≤120 字符"
    - "completed 标题用 repository_name + fitness_verdict；failed 标题用 repository_name + attempt"
    - "字段标签无裸英文键（routed_confidence 等有中文标签）；high/medium/low 与 suitable/partial/unsuitable 显示中文"
    - "UUID 类字段（repository_id / task_id）仍可查但排在人话字段之后；缺新字段的老事件优雅回退不白屏"
  artifacts:
    - path: "server/services/process_runtime/blueprint_research_adapter.py"
      provides: "started/failed payload 带 repository_name + research_reason；last_output 写入 repository_name"
      contains: "research_reason"
    - path: "server/subagent/api/callbacks.py"
      provides: "completed/failed emit 对齐 repository_name + fitness_verdict（并存 verdict）+ attempt"
      contains: "fitness_verdict"
    - path: "web/src/locales/zh-CN.json"
      provides: "payload.routed_confidence / research_reason + 枚举中文"
      contains: "routed_confidence"
    - path: "web/src/utils/blueprintActivity.ts"
      provides: "事件字段排序：人话优先、*_id 殿后"
    - path: "web/src/components/blueprint/BlueprintStageStepper.vue"
      provides: "fieldValue / eventLabel 枚举人话化"
  key_links:
    - from: "blueprint_research_adapter._emit_started"
      to: "zh-CN progress.repoResearchStarted"
      via: "payload.repository_name 满足 PROGRESS_PARAMS"
      pattern: "repository_name"
    - from: "candidate.evidence.reasoning"
      to: "event payload.research_reason"
      via: "placement_* 枚举翻人话 + ≤120 截断"
      pattern: "research_reason"
    - from: "callbacks._handle_blueprint_research_completion"
      to: "progress.repoResearchCompleted"
      via: "repository_name + fitness_verdict（保留 verdict 兼容）"
      pattern: "fitness_verdict"
    - from: "describeEventPayload / fieldValue"
      to: "BlueprintStageStepper 过程明细"
      via: "标签 i18n + 枚举映射 + UUID 殿后"
      pattern: "routed_confidence|fieldValue"
---

<objective>
让蓝图「第 2 步 仓库调研」过程明细的 started / completed / failed 事件人类可读：标题带仓库名，字段含选仓理由，枚举中文化，UUID 降为次要信息；存量缺字段事件保持通用文案回退。

Purpose: 用户原话要求「在调研哪个仓库、因为啥调研这个仓库」，当前因后端漏传 `repository_name` / `fitness_verdict` 且缺理由字段，前端已写好的 i18n 全部回退成通用文案 + 裸 UUID/英文键。
Output: 后端三事件 payload 对齐 + `research_reason` 截断标量；前端标签/枚举/字段排序；后端与前端既有测试补齐断言。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.cursor/rules/observability-logging.mdc
@server/services/process_runtime/blueprint_research_adapter.py
@server/subagent/api/callbacks.py
@server/services/process_runtime/blueprint_route.py
@server/delivery/services/event_taxonomy.py
@web/src/locales/zh-CN.json
@web/src/utils/blueprintBlocks.ts
@web/src/utils/blueprintActivity.ts
@web/src/components/blueprint/BlueprintStageStepper.vue
@web/src/utils/__tests__/blueprintBlocks.test.ts
@web/src/utils/__tests__/blueprintActivity.spec.ts
@web/src/components/blueprint/__tests__/stageStepper.spec.ts
@server/tests/subagent/test_blueprint_research_callback.py
@server/tests/services/process_runtime/test_blueprint_research_stage.py
@server/tests/delivery/test_blueprint_event_taxonomy_112.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 对齐调研三事件 payload（name / reason / verdict / attempt）</name>
  <files>server/services/process_runtime/blueprint_research_adapter.py, server/subagent/api/callbacks.py, server/delivery/services/event_taxonomy.py, server/tests/services/process_runtime/test_blueprint_research_stage.py, server/tests/subagent/test_blueprint_research_callback.py</files>
  <behavior>
    - started research 分支 payload 含非空 repository_name（调用方已传入时）与 research_reason（由 candidate.evidence 派生）
    - research_reason：placement_primary→「主落点仓」、placement_supporting→「支撑仓」；其余沿用 evidence.reasoning 原文；总长 ≤120；空则省略键或空串（前端不展示空）
    - SubAgentSession.last_output 在派发时写入 repository_name（供 completed/failed 回调读取，无需新查库）
    - completed emit：repository_name + fitness_verdict（= fitness.verdict）+ 保留既有 verdict 键不破坏消费方；仍含 repository_id/task_id/role_suggestion/findings_count
    - failed emit（adapter `_emit_failed` 与 callback `_handle_blueprint_research_failure` / 不可解析失败分支）：repository_name + attempt（来自 task.attempt）+ 既有 error 字段；缺 name 时允许空串（前端走 Generic）
    - emit 仍 best-effort：失败只打 `blueprint_repo_research_emit_failed`，不抛穿主流程；payload 不含 token/prompt/需求正文
  </behavior>
  <action>
    1. 在 `blueprint_research_adapter.py` 增加纯函数/静态方法（如 `_format_research_reason(candidate)`）：读 `candidate.evidence`（经路由已 `_normalize_evidence`），把 `placement_primary` / `placement_supporting` 翻成「主落点仓」/「支撑仓」；其他 reasoning 字符串原样；`strip` 后截断至 ≤120。不得把整段 matched_node_paths 列表塞进 payload（可选用一句已存在的 reasoning；列表只作排障留在 stage_state）。
    2. 修复 `_emit_started` 的 research 分支：payload 加入已有入参 `repository_name`，并加入 `research_reason`；键序建议人话在前、`repository_id`/`task_id` 在后（前端亦会再排序）。plan 分支已有 name，保持行为，可按同样规则附带 research_reason（若无 candidate 则空）。
    3. 创建 SubAgentSession 的 `last_output` 时写入 `repository_name`（与已有 `repository_id` / `research_task_id` 并列）。
    4. 扩展 `_emit_failed(..., *, repository_name="", attempt=None)`：payload 加 `repository_name`、`attempt`（默认 `getattr(task,"attempt",None)`）；各调用点在能拿到 candidate/name 时传入，拿不到则空串。
    5. 在 `callbacks.py` 的 `_handle_blueprint_research_completion` / `_handle_blueprint_research_failure`（及不可解析 failed 分支）：从 `session.last_output` 取 `repository_name`；completed 同时写 `fitness_verdict` 与 `verdict`（同值）；failed 写 `attempt=task.attempt`。对照 `event_taxonomy.py` 注释：COMPLETED 早已写明 `fitness_verdict`，STARTED/FAILED 注释补上 `repository_name` / `research_reason` / `attempt`，与实现一致。
    6. 测试：在 `test_blueprint_research_stage.py`（或最贴近 `_emit_started` 的既有用例）断言 started payload 含 `repository_name`+`research_reason`；在 `test_blueprint_research_callback.py` 断言 completed 含 `repository_name`+`fitness_verdict` 且仍有 `verdict`，failed 含 `repository_name`+`attempt`。既有 taxonomy 常量测试保持绿。遵守 observability-logging：不新增明文敏感字段。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/subagent/test_blueprint_research_callback.py tests/services/process_runtime/test_blueprint_research_stage.py tests/delivery/test_blueprint_event_taxonomy_112.py -q --tb=short</automated>
  </verify>
  <done>三事件 payload 人类可读键齐全；verdict 兼容保留；last_output 可回填 name；测试绿。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 前端标签、枚举人话化与 UUID 殿后 + 存量回退</name>
  <files>web/src/locales/zh-CN.json, web/src/utils/blueprintActivity.ts, web/src/components/blueprint/BlueprintStageStepper.vue, web/src/utils/__tests__/blueprintBlocks.test.ts, web/src/utils/__tests__/blueprintActivity.spec.ts, web/src/components/blueprint/__tests__/stageStepper.spec.ts</files>
  <behavior>
    - resolveProgressKeys：payload 有 repository_name 时选 repoResearchStarted/Completed/Failed；缺失时仍走 *Generic（存量事件）
    - fieldLabel：routed_confidence→「路由置信度」、research_reason→「调研理由」（或等价中文）；不再原样显示英文键
    - fieldValue / eventLabel 插值：high/medium/low→高/中/低；suitable/partial/unsuitable→沿用 knowledge.blueprints.repo.fitness* 文案；true/false 保持现有是/否
    - describeEventPayload（或 stepper 渲染前）：标量字段排序为人话键优先（repository_name、research_reason、routed_confidence、fitness_verdict、attempt…），`repository_id`/`task_id` 及一般 `*_id` 殿后；缺键不渲染、不出现 undefined
    - 不在 .vue 内硬编码中文句子（走 zh-CN.json）
  </behavior>
  <action>
    1. `zh-CN.json` 的 `knowledge.blueprints.activity.payload` 增加 `routed_confidence`、`research_reason`（及如需的 `verdict` 别名标签）；在 `activity` 下增加枚举映射键（如 `confidenceHigh/Medium/Low`），复用或引用已有 `repo.fitnessSuitable|Partial|Unsuitable`。
    2. `BlueprintStageStepper.vue`：扩展 `fieldValue`（或抽小函数到 `blueprintActivity.ts` 供单测）映射置信度与适配结论枚举；`eventLabel` 在调用 `t(key, payload)` 前对 payload 做浅拷贝人话化，避免标题里出现 suitable/high 英文。
    3. `blueprintActivity.ts` 的 `describeEventPayload`：对 fields 排序——优先键列表（repository_name、research_reason、routed_confidence、fitness_verdict、attempt、role_suggestion、findings_count、error、error_kind…）在前，`task_id`/`repository_id`/其他 `*_id` 在后；其余保持相对顺序。不删除 UUID（「保留可查」）。
    4. 测试：`blueprintBlocks.test.ts` 覆盖有/无 `repository_name` 时 progress key 选择；`blueprintActivity.spec.ts` 覆盖字段排序与缺字段；`stageStepper.spec.ts` 覆盖标签非英文键、枚举中文、缺 payload 不崩。仓库仅有 zh-CN.json（无 en），不要新建 locale 文件。
  </action>
  <verify>
    <automated>cd web && pnpm exec vitest run src/utils/__tests__/blueprintBlocks.test.ts src/utils/__tests__/blueprintActivity.spec.ts src/components/blueprint/__tests__/stageStepper.spec.ts</automated>
  </verify>
  <done>过程明细标题可读、理由可见、枚举中文、UUID 殿后；老事件 Generic 回退稳定。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| process_runtime / subagent callback → ConvergenceSession 事件 payload | 容器回调与路由 candidate 数据进入用户可见事件流 |
| 事件 API → 前端过程明细 | 已落库 payload 原样展示；缺字段必须安全回退 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-xb9-01 | Information Disclosure | research_reason / error_detail | mitigate | 理由仅来自路由 evidence 标量，截断 ≤120；error 继续走 redact_secrets_in_text + 既有长度上限；禁止 prompt/token/需求正文入 payload |
| T-xb9-02 | Tampering | last_output.repository_name | accept | name 为展示用标量，权威关联仍靠 repository_id；回调不采信容器上报的 repo id（既有注释） |
| T-xb9-03 | Elevation of Privilege | emit 路径 | accept | 无新权限面；emit best-effort 失败不改变任务终态 |
| T-xb9-SC | Tampering | npm/pip installs | accept | 本计划无新包安装 |
</threat_model>

<verification>
- 后端：pytest 上述三文件全绿；手动抽查 started payload 键序/截断。
- 前端：vitest 三文件全绿；缺 `repository_name` 的假事件仍显示 Generic 文案且无 undefined。
- 契约：completed 同时含 `verdict` 与 `fitness_verdict`；taxonomy 常量测试不回归。
</verification>

<success_criteria>
- 新会话「仓库调研」明细：标题含仓库名；可见调研理由；置信度/适配结论中文；UUID 不抢第一视觉。
- 老会话缺新字段：回退通用文案，页面不白屏。
- 观测规范：payload 无敏感文本；emit 失败不反噬。
</success_criteria>

<output>
Create `.planning/quick/260817-xb9-readable-repo-research-detail/260817-xb9-SUMMARY.md` when done
</output>
