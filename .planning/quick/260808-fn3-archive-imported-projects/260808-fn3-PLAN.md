# Quick Task 260808-fn3: 归档批量导入的历史项目并清理默认分支绑定

**Date:** 2026-08-08
**Mode:** quick（inline 执行，数据运维）

## 背景

260807 两次批量导入（ricelove feature 页 + 能力簇方案 + 上线记录表）把 532 个历史
项目全部落成默认状态「开发中」，且给每个关联仓库绑了 `repository.default_branch`
（302×master / 76×feat/coding-agent-base / 4×main），污染 `ProjectBranch` 的
「分支→项目反查」语义（master 对应 ~300 个项目）。

## 处理决策（用户已确认）

1. **归档**：`feishu_project_key` 前缀为 `ricelove:`（247）/ `ricelove-scheme:`（284）/
   `release-bitable:`（1）的项目批量 `ProjectService.archive`（状态机合法：
   developing → archived，且 archived → developing 可逆，误归档可恢复）。
   空 key 的 3 个手工项目（高三提分专项 / 小学思维培优-刷题入口 / test1）不动。
2. **清绑定**：删除导入项目名下全部 377 条分支绑定（核对过：全部等于所在仓库
   default_branch，无真实 feature 分支误伤）。非导入项目的 6 条绑定保留。
3. **⛔ 不改仓库 default_branch**：4 个 `default_branch=feat/coding-agent-base` 的
   仓库按用户确认保持现状（"它就是这个分支"）。

## 约束

- 全走 service 层（INV-6）：`ProjectService.archive` / `ProjectBranchService.unbind`，
  自带审计（`initiated_by_user_id` 归因到 admin）+ 结构化日志 + WS 推送（best-effort）。
- 脚本幂等：archive 对已 archived 项目是合法自环；unbind 不存在返回 False 不抛。
- 落 JSON 报告（处理数/跳过数/错误清单）供核对。

## Tasks

### Task 1: 编写并执行清理脚本

- **files**: `.planning/quick/260808-fn3-archive-imported-projects/cleanup_imported.py`
- **action**: 按上述范围归档 + 解绑；打印并落盘报告。
- **verify**: 复查 DB —— developing 项目应只剩 3 个手工项目；`ProjectBranch` 总数 383→6。
- **done**: 状态分布与绑定分布符合预期，报告无错误。
