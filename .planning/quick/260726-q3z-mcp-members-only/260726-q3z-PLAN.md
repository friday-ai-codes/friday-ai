---
phase: quick-260726-q3z-mcp-members-only
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/knowledge/vector_recall.py
  - server/knowledge/access_scope.py
  - server/knowledge/sources/mcp_coding_plan.py
  - server/knowledge/sources/mcp_execution_trace.py
  - server/knowledge/sources/learning_case.py
  - server/tests/knowledge/test_vector_recall.py
  - server/tests/knowledge/test_access_scope.py
  - server/tests/knowledge/test_mcp_artifact_sources.py
  - server/tests/knowledge/test_learning_case_source.py
autonomous: true
requirements: [QUICK-Q3Z]
must_haves:
  truths:
    - "MCP 三类产物（mcp_coding_plan/mcp_repository_analysis/mcp_execution_trace 的向量点，payload project_id 为空串但 repository_id 非空）对可见该仓库的用户可被向量召回（D-01）"
    - "members_only 项目成员（ProjectMember）与 superuser 的 resolve_allowed_project_ids 含该 initiatives.Project id，项目文档 RAG 不再全黑（D-02）"
    - "非成员对 members_only 项目仍零可见；allowed_repository_ids 为空时召回 filter 与修复前逐字段一致（fail-closed 零回归，D-01/D-02）"
    - "新摄取的 mcp_coding_plan 主事件在 work_item 可解析且 technical_plan 带 space 时携带 space_id；content 逐字节不变（D-03）"
    - "双 None 且恰好单一可解析仓库的 learning_case 事件回填 repository_id；解析不了打 warning 不再静默（D-04）"
  artifacts:
    - "server/knowledge/vector_recall.py（project 闸嵌套 OR 逃生支）"
    - "server/knowledge/access_scope.py（Project 维度并集）"
    - "server/knowledge/sources/mcp_coding_plan.py（build_plan_event 可选 space_id）"
    - "server/knowledge/sources/mcp_execution_trace.py（锚调用处同步传 space_id）"
    - "server/knowledge/sources/learning_case.py（单仓回填 + unanchored warning）"
  key_links:
    - "_build_knowledge_must_filter 的 should 逃生支仅在 allowed_repository_ids 非空时构造，且第二支的 repository_id MatchAny 绝不含空串（防双空串孤儿点泄漏）"
    - "build_plan_event 的 space_id 形参不进 content/hash——mcp_execution_trace 锚同源拼法 byte-equal 守护测试必须恒绿"
    - "learning_case 仓库回填仅在 space_id is None 时进行——space 已有时回填反而会把「可见项目但不可见仓库」的用户挡在 demand 分路之外（回归陷阱）"
---

<objective>
修复知识库检索面两处「只进不出」盲区（quick 任务 260726-q3z）：

1. **盲区 1（D-01/D-03）**：MCP 三类产物（编码方案/仓库分析/执行痕迹）向量点 payload `project_id=""`，而 `_build_knowledge_must_filter` 的 project 闸是平铺 must 条件 → 任何人（含 superuser）召不回。修检索面（逃生口）+ 摄取面（mcp_coding_plan 回填 space_id）。
2. **盲区 2（D-02）**：members_only 项目文档向量点写 initiatives.Project id，而 `resolve_allowed_project_ids` 只给 Space id（成员）/ Space id + public_org Project id（superuser）→ 成员乃至 superuser RAG 全黑。补权限面 Project 维度。
3. **连带（D-04）**：learning_case 双 None（无 space 无 repo）永不可召回且查重失效。单仓可解析时回填 repository_id 吃修复 1 的逃生口，否则 warning 不再静默。

Purpose: 知识入图却永不可达 = 存储浪费 + 查重失效 + 用户可感知的 RAG 缺料；且 members_only 全黑连 superuser 都无法诊断。
Output: 5 个源文件修复 + 4 个测试文件的证伪型测试（先红后绿，D-05），3 个原子提交。

**明确不做（D-06，防画蛇添足）：**
- 不改 `_vector_project_id` payload 写入口径（双写 Space id 方案已否决）。
- 不动 `knowledge/ingestion.py` 的 `_persist_sync`（entity 已存在时不刷新 space_id/repository_id——通用行为变更，超出本次范围）。
- 不做存量数据 rebuild/迁移：MCP 三类产物存量向量点带非空 repository_id，修复 1 的检索逃生口已让它们可召回；learning_case 双 None 存量点（repository_id 也是空串）确实吃不到逃生口，按 D-04 接受现状，只保证增量正确 + warning 可见。
- PG 面（related/timeline/detail/_entity_visible_metadata）按 entity.space_id 判权的口径保持不变（FK 组织维度是文档化设计）。
- 不加 DUPLICATE_OF 等无关清理。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-clean/.claude/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-clean/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@server/knowledge/vector_recall.py
@server/knowledge/access_scope.py
@server/knowledge/vector_ops.py
@server/knowledge/sources/mcp_coding_plan.py
@server/knowledge/sources/mcp_execution_trace.py
@server/knowledge/sources/learning_case.py
@server/tests/knowledge/test_vector_recall.py
@server/tests/knowledge/test_access_scope.py
@server/tests/knowledge/test_mcp_artifact_sources.py
@server/tests/knowledge/test_learning_case_source.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: vector_recall project 闸增加可见仓库逃生口（D-01，检索面）</name>
  <files>server/knowledge/vector_recall.py, server/tests/knowledge/test_vector_recall.py</files>
  <behavior>
    先写测试（红）再修（绿），全部落在 test_vector_recall.py 既有直调 `_build_knowledge_must_filter` 的模式：
    - Test 1（证伪，修前红）：allowed_project_ids=["p1"] 且 allowed_repository_ids=["r1"]、require_repository=False（demand 分路）→ must 列表中的 project 闸是嵌套 `models.Filter(should=[...])`：第一支为 FieldCondition(project_id ∈ ["p1"])；第二支为内层 `models.Filter(must=[...])`，含 FieldCondition(project_id 精确匹配空串 via MatchValue) 与 FieldCondition(repository_id ∈ ["r1"]，MatchAny **不含空串**)。用 isinstance(cond, models.Filter) 定位嵌套支后断言两支结构。
    - Test 2（证伪，修前红）：require_repository=True（code 分路）同样携带该嵌套 project 闸（两分路同享 `_build_knowledge_must_filter`，天然覆盖，仍需独立断言防未来分叉）。
    - Test 3（守护，恒绿）：allowed_repository_ids=[] → must 中的 project 闸退回平铺 FieldCondition(project_id ∈ allowed)，**无任何嵌套 should 支**——fail-closed：无可见仓库时行为与修复前逐字段一致。
    - Test 4（守护）：嵌套第二支的 repository_id MatchAny.any 列表中不含空串——防止 project_id 与 repository_id 双空串的孤儿点（如双 None learning_case 存量）对任意有仓库权限的用户泄漏。
  </behavior>
  <action>
    修改 `_build_knowledge_must_filter`（server/knowledge/vector_recall.py L63-69 的 project 闸，per D-01）：当 allowed_project_ids 非空且 allowed_repository_ids **非空**时，把平铺的 FieldCondition(project_id MatchAny) 替换为追加到 must 的嵌套 `models.Filter(should=[分支A, 分支B])`——分支 A 为原 FieldCondition(key="project_id", MatchAny(any=allowed_project_ids))；分支 B 为 `models.Filter(must=[FieldCondition(key="project_id", MatchValue(value="")), FieldCondition(key="repository_id", MatchAny(any=allowed_repository_ids))])`。语义：「实体锚定在可见仓库上 ⇒ 可召回」，与既有 demand 分路 repository 空串逃生口（L78-85）对称。allowed_repository_ids 为空时保持原平铺条件不动（fail-closed 零回归）。qdrant-client 的 models.Filter 本身是合法 Condition，可直接放进 must 列表。demand 与 code 两分路都经此函数，无需改 recall_similar_chunks。

    同步修复既有测试的结构假设：`test_vector_is_latest_filter_in_must`（L66-75）与 `test_vector_project_and_repository_filter`（L78-88）用 `{c.key for c in flt.must}` 抽 key——嵌套 models.Filter 元素没有 .key 属性会 AttributeError。改为 `{getattr(c, "key", None) for c in flt.must}` 并按新结构调整断言（repository_id 仍在顶层 must；project_id 移入嵌套支后顶层集合断言相应更新）。`_demand_entity_kinds` 助手已用 getattr，无需动。

    注意：不改 `recall_similar_chunks` 的 `if not allowed_project_ids: return []` 短路（P1 防线）；不改 entity_kinds 分路白名单逻辑；不加 INFO 日志（召回完成事件已有，逃生支命中与否无需高频埋点）。docstring 补一行中文说明逃生支语义与 D-01 引用（沿用文件既有「Phase 15-02」注释风格）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/knowledge/test_vector_recall.py -x -q</automated>
  </verify>
  <done>新增 4 个断言用例通过；既有 filter/召回用例（route quotas、entity_kinds 严格过滤、document kind、fail-closed 空 allowed）全绿；allowed_repository_ids 为空时 filter 与修复前逐字段一致。原子提交：`fix(knowledge): 向量召回 project 闸增加可见仓库逃生口`</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: access_scope 并入 initiatives.Project 维度（D-02，权限面）</name>
  <files>server/knowledge/access_scope.py, server/tests/knowledge/test_access_scope.py</files>
  <behavior>
    先写测试（红）再修（绿），复用 test_access_scope.py 既有 `_make_initiative_project` 夹具（Phase 85-02 段落）：
    - Test 1（证伪，修前红）：members_only initiatives.Project + 为 member 用户建 `ProjectMember(project=project, user=member)` 行（不建 SpaceMembership）→ `resolve_allowed_project_ids(member)` 含 str(project.id)。
    - Test 2（证伪，修前红）：superuser（admin_user 夹具或 create_superuser）→ allowed 含 members_only project 的 str(project.id)（修复前 superuser 只有全量 Space id ∪ public_org Project id）。
    - Test 3（守护，恒绿）：非成员 stranger → allowed 不含 members_only project id，caller 显式传该 id 收窄为 []（既有 test_members_only_non_member_scope_zero_leak 已覆盖，保持通过即可，如需补一条针对 ProjectMember 语义的对称断言可加）。
  </behavior>
  <action>
    修改 `resolve_allowed_project_ids`（server/knowledge/access_scope.py，per D-02）：在既有 `allowed_set |= await _public_org_project_ids()` 之后并入 Project 维度——(a) 普通用户：仿照 `_public_org_project_ids` 增加 sync_to_async 私有助手（如 `_member_project_ids(user)`），查 `initiatives.models.Project.objects.filter(members__user=user)` 的 id 集合（ProjectMember 的 related_name 是 members，见 server/initiatives/models/member.py）；(b) superuser：增加全量 `Project.objects.values_list("id", flat=True)` 集合助手，仅 `user.is_superuser` 分支并入。均为只读放行，lazy import initiatives.models（文件既有惯例）。

    不动 caller intersect 收窄语义（issubset 判定不变）；不动 `resolve_allowed_repository_ids`——它拿 allowed_projects 后走 `Space.objects.filter(id__in=...)`，新混入的 initiatives.Project UUID 匹配不到 Space 行、自然被忽略，无需分流（在 docstring 注明这一点，避免后人误判为 bug）。members_only 非成员仍零可见：非成员既无 SpaceMembership 也无 ProjectMember 行，fail-closed 不破坏。

    docstring 更新集合定义描述（普通用户 = SpaceMembership 空间 ∪ ProjectMember 项目 ∪ public_org 项目；superuser = 全量空间 ∪ 全量项目），引用 D-02。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/knowledge/test_access_scope.py -x -q</automated>
  </verify>
  <done>新增 member/superuser 两条证伪用例通过；既有 fail-closed 全套（none user、非成员零泄漏、caller intersect、search_similar members_only 零命中）恒绿。原子提交：`fix(knowledge): 权限集合并入 initiatives.Project 维度修复 members_only 全黑`</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 摄取面回填——mcp_coding_plan space_id 与 learning_case 单仓锚（D-03/D-04）</name>
  <files>server/knowledge/sources/mcp_coding_plan.py, server/knowledge/sources/mcp_execution_trace.py, server/knowledge/sources/learning_case.py, server/tests/knowledge/test_mcp_artifact_sources.py, server/tests/knowledge/test_learning_case_source.py</files>
  <behavior>
    先写测试（红）再修（绿）：
    - Test 1（test_mcp_artifact_sources.py，证伪修前红）：给 `_make_work_item_closure` 增加可选 space 形参（默认 None 不动既有用例）——注意既有工厂建 McpWorkItemTechnicalPlan 时**未传 space**，需要显式传入 Space 实例；断言 work_item 闭包齐备且 technical_plan 带 space 时，normalize_mcp_coding_plan 产出的 tech_plan 主事件 `event.space_id == str(space.id)`（修前恒 None）。
    - Test 2（test_mcp_artifact_sources.py，证伪修前红）：mcp_execution_trace 的 plan 锚事件在同样闭包下 `space_id == str(space.id)`。
    - Test 3（守护，恒绿）：既有 `test_execution_trace_dual_events_anchor_content_byte_equal`（锚同源拼法 content 逐字节相等）与 `test_coding_plan_single_event_with_references_edge` 中 `event.space_id is None`（无闭包场景）保持通过——space_id 绝不进 content/hash。
    - Test 4（test_learning_case_source.py，证伪修前红）：case 的 context=None 且 technical_plan=None（双 None），repositories 恰含 1 个名称且该名称在 Repository 表恰有 1 行 → learning_case 事件 `repository_id == str(repo.id)`。
    - Test 5（test_learning_case_source.py，证伪修前红）：双 None 且 repositories 为空/多元素/名称无法唯一解析 → repository_id 保持 None，且 capture_logs 捕获到 `knowledge_normalize_unanchored` warning（含 component=knowledge、category=sampling）。
    - Test 6（守护）：technical_plan 带 space 的既有工厂路径 → repository_id 保持 None（space 已有时**不回填**——见 action 中回归陷阱说明），既有 space_id 断言恒绿。
  </behavior>
  <action>
    **mcp_coding_plan.py（D-03）**：`build_plan_event` 增加 keyword-only 形参 `space_id: str | None = None`，透传给 IngestionEvent 的 space_id 字段（替换硬编码 None，L92）。**content 拼法一个字符都不动**——同源拼法纪律（模块 docstring L25-27）：mcp_execution_trace 锚复用本函数，content 逐字节一致才不翻版本 ping-pong；space_id 不参与 content/hash，安全。`normalize` 主路径已在调用 build_plan_event 之前执行 `_resolve_work_item(plan.id)`（L203-207），当 resolved 非 None 时取 `tp = resolved[1]`，传 `space_id=str(tp.space_id) if tp.space_id else None`（与既有锚事件 L227 同款来源）；resolved 为 None 时不传（默认 None，零回归）。

    **mcp_execution_trace.py（D-03）**：锚事件调用处（L148 build_plan_event）同步传 space_id——从 mcp_coding_plan 导入 `_resolve_work_item`（同包私有复用，该文件已 import build_plan_event，同一 import 语句扩展即可），以 `trace.plan_id` 解析；resolved 非 None 时同款取 tp.space_id 传入，None 时保持不传。code_change 主事件的 space_id=None 不动（它有非空 repository_id，Task 1 逃生口已覆盖）。mcp_repository_analysis.py 同理不动（无 work_item 挂载语义，仓库锚已够）。

    **learning_case.py（D-04）**：normalize 中在既有 space_id 双级 fallback 之后：仅当 `space_id is None` 时尝试仓库回填——repositories 字段是**仓库名称列表**（learning_case_service.py L113 取 task.repository.name，Repository.name 无唯一约束），故回填条件为「len(case.repositories) == 1 且该名称在 Repository 表恰好匹配 1 行」（async ORM 查两行判唯一即可，lazy import repositories.models.Repository）；满足则 learning_case 主事件 repository_id 用 str(repo.id)。**space_id 非 None 时绝不回填**——回归陷阱：demand 分路 filter 是 repository_id ∈ [allowed, 空串]，space 已有的案例现在靠空串支对「可见项目但不可见该仓库」的用户可见，若回填真实 repo id 会反把这类用户挡掉；只在 space_id is None（现状为零可达、只增不减）时回填。work_item 锚事件不动（锚与 mcp_plan.py 共享 natural key，跨 normalizer 口径必须一致）。

    回填失败（双 None 且未能唯一解析）时打 `logger.warning("knowledge_normalize_unanchored", source_kind=..., source_id=..., trigger=request.trigger, repositories_count=len(case.repositories), component="knowledge", category="sampling")`——沿用文件内既有 warning 事件的 kv 风格（CLAUDE.md 可观测性规范），不再静默。mcp_tools/learning_case_service.py 的相似查重（L301 search_similar entity_kinds=["learning_case"]）无需改动，自动受益。

    **存量数据说明（写进代码注释与 SUMMARY，防执行者与后人画蛇添足）**：既有实体的 entity.space_id/repository_id 不会自动更新（_persist_sync get_or_create 仅创建时写 defaults，更新分支只动 title/event_time/current_version——**不要**去改 _persist_sync，这是通用行为变更超出本次范围）；向量点靠 content hash 短路也不重建。MCP 三类产物存量点带真实 repository_id，Task 1 逃生口已让它们可召回，**无需任何 rebuild/迁移**；learning_case 双 None 存量点吃不到逃生口，按 D-04 接受，增量正确 + warning 可见即可。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/knowledge/test_mcp_artifact_sources.py tests/knowledge/test_learning_case_source.py -x -q</automated>
  </verify>
  <done>6 组新断言通过；锚 content byte-equal 守护、无闭包 space_id=None、既有幂等重摄/触发投递用例全绿。原子提交：`fix(knowledge): MCP 方案事件回填 space,learning_case 单仓案例回填仓库锚`</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 检索 API → Qdrant filter | 用户身份经 access_scope 折算为 allowed 集合后注入 must filter，是知识可见性唯一闸口 |
| MCP 摄取 → 向量点 payload | normalizer 写入的 space_id/repository_id 决定该知识对谁可见 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-q3z-01 | Information Disclosure | _build_knowledge_must_filter 逃生支 | high | mitigate | 第二支强制要求 repository_id ∈ allowed_repos 且 MatchAny 不含空串；allowed_repos 为空时不构造该支（Task 1 守护测试锁定） |
| T-q3z-02 | Elevation of Privilege | resolve_allowed_project_ids 并集扩容 | high | mitigate | 仅并入 ProjectMember 关系行与 superuser 全量；非成员零可见由既有 test_members_only_non_member_scope_zero_leak + Task 2 守护断言双重锁定 |
| T-q3z-03 | Information Disclosure | learning_case 仓库回填 | medium | mitigate | 仅 space_id is None 且名称唯一解析时回填（只增不减）；space 已有时不回填防「可见项目不可见仓库」用户被反向挡出 |
| T-q3z-SC | Tampering | npm/pip/cargo installs | low | accept | 本任务零新增依赖，无包安装 |
</threat_model>

<verification>
全量回归（constraints 指定命令）：

1. `cd server && uv run pytest tests/knowledge/test_vector_recall.py tests/knowledge/test_access_scope.py tests/knowledge/test_mcp_artifact_sources.py tests/knowledge/test_learning_case_source.py -x -q`
2. `cd server && uv run ruff check knowledge/ && uv run ruff format --check knowledge/`

TDD 纪律核对（D-05）：每个 task 先提交/确认新测试在修复前为红（证伪型），修复后转绿；守护类断言（byte-equal、fail-closed、零泄漏）修前修后恒绿。
</verification>

<success_criteria>
- [ ] 4 个测试文件全绿，ruff check/format 通过
- [ ] filter 层：project 闸嵌套 OR 逃生支存在且 fail-closed（无可见仓库时与修复前一致）
- [ ] 权限层：members_only 成员与 superuser 的 allowed 含 initiatives.Project id；非成员零可见不变
- [ ] 摄取层：mcp_coding_plan/mcp_execution_trace 锚在 work_item 可解析时带 space_id，content 逐字节不变
- [ ] learning_case：双 None 单仓回填 repository_id，解析不了打 knowledge_normalize_unanchored warning
- [ ] 未触碰 _persist_sync、payload 写入口径、PG 面判权口径；无 rebuild/迁移
- [ ] 3 个原子提交，commit message 遵循 `fix(knowledge): 中文摘要` 风格
</success_criteria>

<output>
完成后创建 `.planning/quick/260726-q3z-mcp-members-only/260726-q3z-SUMMARY.md`，记录：三处修复的最终 filter/集合/事件结构、存量数据无需迁移的结论（及 learning_case 双 None 存量接受现状的边界）、新增 warning 事件名。
</output>
