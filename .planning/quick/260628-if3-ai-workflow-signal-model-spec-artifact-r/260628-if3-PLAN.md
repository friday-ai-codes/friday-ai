# Quick Task 260628-if3: 完善 AI workflow signal model spec

**Date:** 2026-06-28
**Status:** In progress

## Goal

完善 `.planning/AI-WORKFLOW-SIGNAL-MODEL-SPEC.md`，把它从“信号/插槽/UI 心智模型”补成可执行的完整 Friday AI 工作流系统方案。

## Tasks

1. 补齐第一性原理下的目标工作流定义：交付 DAG、AI 收敛状态机、artifact spine、human task、reaction/runtime。
2. 明确 signal 不新造第三套事件体系，而是复用/投影 `Workflow hook` 与 `PlanSessionEvent`。
3. 调整实施路线：先补 runtime contract，再做生命周期可视、reaction、双层视图和 artifact timeline。

## Verification

- 目标 spec 保持自包含。
- 新增内容与现有 `PlanSession`、`PlanSessionEvent`、`PlanVersion`、`NodeExecution` 模型一致。
- 不改业务代码，不碰无关 WIP。
