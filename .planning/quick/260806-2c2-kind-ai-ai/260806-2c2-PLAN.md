---
phase: quick-260806-2c2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/utils/blueprintAnnotations.ts
  - web/src/utils/__tests__/blueprintAnnotations.test.ts
  - web/src/components/blueprint/BlueprintThreadSidebar.vue
  - web/src/pages/knowledge/blueprints/[id].vue
  - web/src/stores/useBlueprintViewerStore.ts
  - web/src/components/blueprint/__tests__/threadSidebar.spec.ts
  - web/src/pages/knowledge/__tests__/blueprintViewer.spec.ts
autonomous: true
requirements: [QUICK-260806-2C2]

must_haves:
  truths:
    - "侧栏按 kind 分四组手风琴（AI 提问 → AI 审查 → 人工评论 → 确认门），默认展开"
    - "没有内容的 kind 组整块不渲染（含标题行），不再有恒在的失锚组与其专属空态"
    - "未回答（open）的线程排在各组最前，其次 answered，再 closed"
    - "顶部四个 kind 筛选 chips 不复存在；「显示已关闭批注」开关行为不变"
    - "失锚线程按自己的 kind 进组渲染，卡片上的失锚标记仍由 BlueprintThreadCard 承载"
    - "四组全空时仍呈现原有两种空态（emptyClosed 提示态 / CompactEmptyState）"
  artifacts:
    - path: "web/src/utils/blueprintAnnotations.ts"
      provides: "新纯函数 sidebarKindGroups（按 kind 分组 + status→severity→created_at 排序）；原 sidebarGroups 保留"
      exports: ["sidebarKindGroups", "sidebarGroups"]
    - path: "web/src/components/blueprint/BlueprintThreadSidebar.vue"
      provides: "按 kind 分组的手风琴侧栏，无 kind chips，无 kindFilters prop"
    - path: "web/src/stores/useBlueprintViewerStore.ts"
      provides: "清理后的 viewer store（无 kindFilters 及其两个方法）"
  key_links:
    - from: "web/src/components/blueprint/BlueprintThreadSidebar.vue"
      to: "web/src/utils/blueprintAnnotations.ts"
      via: "import sidebarKindGroups"
      pattern: "sidebarKindGroups"
    - from: "web/src/pages/knowledge/blueprints/[id].vue"
      to: "web/src/components/blueprint/BlueprintThreadSidebar.vue"
      via: "两处使用点（xl 常驻 aside + 窄屏 Sheet）不再传 kind-filters"
      pattern: "BlueprintThreadSidebar"
---

<objective>
蓝图查看页右侧线程侧栏从「按处置状态分组（待处理/已答复/已关闭/失锚）+ 顶部 kind 筛选 chips」
改为「按 kind 手风琴分组（AI 提问 / AI 审查 / 人工评论 / 确认门）」：有内容才渲染该组，
空组整块（含标题行）不出现；组内未回答的排最前。chips 与 `kindFilters` 全链路删除。

Purpose: 用户要「没回答的问题统一到 AI 提问栏里」，且不要一排空分组噪音 —— 分组本身取代筛选。
Output: 改造后的 `BlueprintThreadSidebar.vue` + 新纯函数 `sidebarKindGroups` + 同步更新的 vitest。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@web/src/components/blueprint/BlueprintThreadSidebar.vue
@web/src/utils/blueprintAnnotations.ts
@web/src/composables/useBlueprintAnnotations.ts
@web/src/stores/useBlueprintViewerStore.ts
@web/src/components/blueprint/__tests__/threadSidebar.spec.ts
@web/src/utils/__tests__/blueprintAnnotations.test.ts

关键现状（已核实）：
- `sidebarGroups(threads, orphanedThreads)` 是四组（open/answered/closed/orphaned）判据唯一实现；
  `useBlueprintAnnotations.ts` 与 `annotationCounts` 依赖它 —— **保留不删**。
- 组内排序 `sortInGroup`：severity（blocker→warning→info→无）→ `created_at` 升序（模块私有函数）。
- 侧栏组件里 `mergedOrphaned` 负责两数据源合并（同 `thread_id` 以 `threads/` 为准，快照占位）。
- `kindFilters` 链路触点：组件 prop/emit、`[id].vue` 两处绑定 + `onKindFiltersChange`（L1002）+
  L41 的 `BlueprintThreadKindFilter` 类型 import、store 的 `kindFilters`/`toggleKindFilter`/
  `resetKindFilters`、`blueprintViewer.spec.ts` 的 SidebarStub props 列表。除此之外无其他使用点（已 grep 核实）。
- i18n 分组标题四个键已存在：`knowledge.blueprints.thread.kindAiClarification` /
  `kindAiReviewFinding` / `kindHumanComment` / `kindRepoConfirmation`（zh-CN.json L558 起），不新增键。
</context>

<tasks>

<task type="auto">
  <name>Task 1: 在 blueprintAnnotations.ts 新增 sidebarKindGroups 纯函数并补单测</name>
  <files>web/src/utils/blueprintAnnotations.ts, web/src/utils/__tests__/blueprintAnnotations.test.ts</files>
  <action>
在 `web/src/utils/blueprintAnnotations.ts` 新增导出（决策 3，原 `sidebarGroups` 保留不动，
`annotationCounts` / `useBlueprintAnnotations` 仍依赖它）：

1. `export interface SidebarKindGroups`：四个 key 恰为 `BlueprintThreadKind` 的四个取值
   （`ai_clarification` / `ai_review_finding` / `human_comment` / `repo_confirmation`），
   值为 `BlueprintThreadDetail[]`。
2. `export function sidebarKindGroups(threads, orphanedThreads?): SidebarKindGroups`，
   签名与 `sidebarGroups` 一致（第二参可选）：
   - 数据合并口径沿用 `sidebarGroups` 的现有约定（决策 5）：`anchored` = threads 中
     `anchor_status !== 'orphaned'` 的；`orphaned` = `orphanedThreads`（`undefined` 时从
     threads 按 `anchor_status === 'orphaned'` 派生，与 `sidebarGroups` 同款兜底）。
     合并成一份列表时按 `thread_id` 去重 —— `orphaned` 列表里出现过的 id 从 `anchored`
     里剔除，保证任一线程只进一次（与 §20 断言 11「不重复」同精神；调用方传入的是组件
     `mergedOrphaned` 的产物，快照占位/threads 优先由调用方口径承载）。
   - 按 `thread.kind` 落入四组；kind 不在四个枚举内的线程丢弃（防御性，现状不会出现）。
   - 组内排序（决策 3）：先按 status rank（`open`=0 → `answered`=1 → `resolved`/`dismissed`=2，
     其余=3），同 status 内复用现有 `sortInGroup` 口径（severity → created_at）。实现上新增
     模块私有 `sortInKindGroup`，内部调用或内联 `sortInGroup` 的比较逻辑，⛔ 不复制两份
     SEVERITY_ORDER。
   - 与既有函数同款输入防御：`threads` 非数组按 `[]` 处理。
   - JSDoc 注释按本文件既有风格写中文、解释 why（点明「kind 是呈现维度、失锚线程并入
     各自 kind 组、closed 的显隐由调用方开关控制而非本函数职责」）。

在 `web/src/utils/__tests__/blueprintAnnotations.test.ts` 追加 `describe('sidebarKindGroups …')`
（沿用文件内既有 `thread()` 工厂风格），至少覆盖：
   - 四 kind 各一条 ⇒ 各组恰一条、其余组为空数组；
   - 排序：同 kind 内 `answered` 早于 `open` 创建，仍是 open 在前；同为 open 的
     blocker 排 warning 前、同 severity 按 created_at 升序；closed（resolved/dismissed）排最后；
   - 失锚并组：`orphanedThreads` 传一条 `kind: 'ai_clarification'` 的失锚线程 ⇒ 出现在
     `ai_clarification` 组且全量只出现一次（threads 里同 id 的 open 线程不再另占一坑）；
   - `orphanedThreads` 为 `undefined` 时从 threads 按 `anchor_status` 派生；
   - `threads` 传 `null as unknown as …` 不抛、四组皆空数组。
  </action>
  <verify>
    <automated>cd web && pnpm exec vitest run src/utils/__tests__/blueprintAnnotations.test.ts</automated>
  </verify>
  <done>`sidebarKindGroups` 导出可用，新增用例全绿，原 `sidebarGroups` 相关既有用例不受影响。</done>
</task>

<task type="auto">
  <name>Task 2: 改造侧栏组件为按 kind 手风琴分组，删除 chips 与 kindFilters 全链路</name>
  <files>web/src/components/blueprint/BlueprintThreadSidebar.vue, web/src/pages/knowledge/blueprints/[id].vue, web/src/stores/useBlueprintViewerStore.ts</files>
  <action>
**BlueprintThreadSidebar.vue**（改造主体）：

1. 删除（决策 6）：`KIND_CHIPS` 常量、`kindFilters` prop 与默认值、`update:kindFilters` emit、
   `matchesKindFilter` / `kindCounts` / `applyKindFilter` / `toggleKind`、模板顶部工具条里整个
   chips `<div class="flex flex-wrap gap-1.5">` 块。顶部工具条只剩「显示已关闭批注」开关
   （`<label>` 包裹写法与 showClosedHint 渐进披露保持原样）。
2. 分组切换（决策 1/2/4/5）：
   - `mergedOrphaned` computed 保留原样（threads/ 优先、快照占位）。
   - `groups` 改为 `sidebarKindGroups(props.threads, mergedOrphaned.value)`（不再有
     visibleThreads/visibleOrphaned 这层 kind 过滤）。
   - `SidebarSection` 精简：`key` 为四个 kind 字面量，`labelKey` 对应
     `kindAiClarification` / `kindAiReviewFinding` / `kindHumanComment` / `kindRepoConfirmation`
     （i18n 前缀仍是 `knowledge.blueprints.thread.`，决策 10），`defaultOpen` 恒 true；
     `isOrphanGroup` / `isClosedGroup` 字段删除。
   - 每组 `items`：从对应 kind 组取出后，`showClosed === false` 时过滤掉
     `status === 'resolved' || status === 'dismissed'` 的条目（决策 4：closed 的显隐是组件的
     开关职责，写注释说明为何不进纯函数）。
   - `sections` 过滤条件只剩 `items.length > 0`（决策 2：空组整块不渲染，⛔ 失锚组恒在的
     例外废弃）；模板里 `groupOrphanedEmpty` 那段 `<p>` 删除。
   - 顺序固定：ai_clarification → ai_review_finding → human_comment → repo_confirmation。
3. 空态与计数：
   - `totalCount` / `isEmpty` 口径改为按过滤后 sections 求和（语义不变）。
   - `hiddenClosedCount`：开关关闭时统计被隐藏的 closed 条数 —— 从合并后的全量线程
     （anchored + mergedOrphaned 去重，与 Task 1 的合并口径一致）里数
     `resolved`/`dismissed`；最简实现是取 `sidebarKindGroups` 的未过滤产物聚合。
     emptyClosed 提示态与 CompactEmptyState 两种空态的模板保持不动（决策 2）。
4. 保持不变（决策 7/8）：Collapsible/Trigger/Badge 结构、`min-h-11` 触控目标、
   `data-group-key` / `data-testid="blueprint-thread-group-${section.key}"` 命名模式、
   Badge `>0` 才渲染（空组本就不渲染故恒显示，variant 统一 `muted`，warning 变体随失锚组
   废弃）、`onKeydown` 的同组 ↑/↓ 与 Esc 逻辑一字不动、草稿卡逻辑不动。
5. 更新文件头注释：四组判据来源改指 `sidebarKindGroups`；「失锚组直接渲染」与「kind 筛选
   chips」两段按新现实改写（失锚并入 kind 组、chips 已删），保留「两个数据源 threads/ 为准」
   与 a11y 段。

**[id].vue**（两处使用点，约 L1571-1587 与 L1607-1623）：
- 两处 `<BlueprintThreadSidebar>` 上删除 `:kind-filters="viewerStore.kindFilters"` 与
  `@update:kind-filters="onKindFiltersChange"`；其余绑定不动。
- 删除 `onKindFiltersChange` 函数（约 L1002-1004）与 L41 的
  `import type { BlueprintThreadKindFilter }`。

**useBlueprintViewerStore.ts**（决策 6，已 grep 确认无其他使用点）：
- 删除 `kindFilters`（含 `blueprint-kind-filters` localStorage 键）、`toggleKindFilter`、
  `resetKindFilters`、`BlueprintThreadKindFilter` 类型导出及 return 里对应三项。
- 文件头注释「三项纯客户端偏好」改为两项并删去 kind 筛选相关描述。
- ⚠️ `auto-imports.d.ts` 里有一行 `export type { BlueprintThreadKindFilter }` 的自动生成残留，
  运行 vitest/vite 会自动再生；若 lint/typecheck 先行报错，手工删除该行即可（⛔ 不要为了保
  d.ts 而保留死代码）。
  </action>
  <verify>
    <automated>cd web && pnpm exec vue-tsc --noEmit -p tsconfig.json 2>&1 | grep -E 'BlueprintThreadSidebar|useBlueprintViewerStore|blueprints/\[id\]' || echo OK</automated>
  </verify>
  <done>侧栏按 kind 四组手风琴渲染、空组整块不出现、chips 与 kindFilters 在组件/页面/store 三层均无残留（`rg kindFilters web/src --glob '!auto-imports.d.ts'` 零命中）。</done>
</task>

<task type="auto">
  <name>Task 3: 同步更新组件测试并全量验证</name>
  <files>web/src/components/blueprint/__tests__/threadSidebar.spec.ts, web/src/pages/knowledge/__tests__/blueprintViewer.spec.ts</files>
  <action>
**threadSidebar.spec.ts**（决策 9，只动侧栏分组相关断言，卡片/finding/焦点环等用例不动）：

1. 「§20 断言 5 / 11」describe 改写为 kind 分组口径：
   - 原 5（orphanedThreads 直接渲染）：断言改为两条失锚线程按各自 kind 落组 ——
     `kind: 'human_comment'` 的失锚线程出现在 `[data-testid="blueprint-thread-group-human_comment"]`；
     全侧栏卡片总数 == 传入总数（不二次过滤的精神保留）。
   - 原 11（open 且 orphaned 只出现一次）：改为该线程只在其 kind 组出现一次、全量卡片数 ==
     线程总数（去重口径转由 `sidebarKindGroups` 承载）。
   - 原 11b（失锚组专属空态）**删除**（决策 5：该规则废弃），替换为新断言：
     「无任何线程的 kind 组整块不渲染 —— 只传一条 `ai_clarification` 时，
     `blueprint-thread-group-ai_review_finding` / `-human_comment` / `-repo_confirmation`
     与 `blueprint-thread-group-trigger` 标题行均不存在于 DOM」。
2. 「侧栏分组、排序与筛选」describe：
   - 5b 排序：同一 kind 组内构造 open/answered/closed 混合 + severity 混合，断言顺序为
     open(blocker) → open(warning) → answered → closed（`showClosed: true` 时）。
   - 5c showClosed：closed 线程在开关关闭时不出现在其 kind 组（该组若因此为空则整块不渲染），
     打开后出现。
   - 5d 空态用例保留原样。
3. 新增分组顺序断言：四 kind 各一条时，`[data-group-key]` 的 DOM 顺序为
   ai_clarification → ai_review_finding → human_comment → repo_confirmation。
4. 手写 i18n 键树：`groupOpen`/`groupAnswered`/`groupClosed`/`groupOrphaned`/`groupOrphanedEmpty`
   可删（组件不再消费），四个 kind* 键保留。若文件内有 chips 相关断言（当前无）一并清理。
5. 13a（min-h-11 触控目标）：mount 时保证至少一个非空组即可，断言逻辑不变。

**blueprintViewer.spec.ts**：SidebarStub 的 props 数组删掉 `'kindFilters'`（避免 stub 声明
已不存在的 prop 误导后人）。

**全量验证**：跑受影响的四个测试文件 + eslint。
  </action>
  <verify>
    <automated>cd web && pnpm exec vitest run src/components/blueprint/__tests__/threadSidebar.spec.ts src/utils/__tests__/blueprintAnnotations.test.ts src/pages/knowledge/__tests__/blueprintViewer.spec.ts && pnpm exec eslint src/components/blueprint/BlueprintThreadSidebar.vue src/utils/blueprintAnnotations.ts src/stores/useBlueprintViewerStore.ts 'src/pages/knowledge/blueprints/[id].vue'</automated>
  </verify>
  <done>三个测试文件全绿，eslint 对改动文件零报错；旧的按状态分组断言与失锚组恒在断言已全部替换为 kind 分组口径。</done>
</task>

</tasks>

<verification>
- `rg 'kindFilters|toggleKindFilter|resetKindFilters|KIND_CHIPS|blueprint-kind-chip' web/src --glob '!auto-imports.d.ts'` 零命中。
- `rg 'sidebarKindGroups' web/src` 命中 utils、组件与两个测试文件。
- `cd web && pnpm exec vitest run src/components/blueprint/__tests__/threadSidebar.spec.ts src/utils/__tests__/blueprintAnnotations.test.ts src/pages/knowledge/__tests__/blueprintViewer.spec.ts` 全绿。
- 原 `sidebarGroups` / `annotationCounts` / `useBlueprintAnnotations` 未被改动（`git diff` 中 composable 无变更）。
</verification>

<success_criteria>
- 侧栏渲染四个 kind 手风琴组，顺序固定，默认展开，空组整块（含标题行）不存在于 DOM。
- 组内排序 open → answered → closed，同 status 内 severity → created_at。
- closed 线程受「显示已关闭批注」开关控制；失锚线程并入各自 kind 组且只出现一次。
- kind 筛选 chips 及 `kindFilters` prop/emit/store 字段全链路删除，页面两处使用点同步。
- 四组全空时两种既有空态行为不变；键盘导航（↑/↓ 同组、Esc 清选中）不变。
- 相关 vitest 与 eslint 全绿。
</success_criteria>

<output>
完成后创建 `.planning/quick/260806-2c2-kind-ai-ai/260806-2c2-SUMMARY.md`。
</output>
