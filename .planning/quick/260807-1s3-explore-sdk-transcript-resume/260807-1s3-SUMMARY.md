---
phase: quick-260807-1s3
plan: 01
status: complete
completed: 2026-08-07
commits:
  - c8b0b9d2 fix(task): explore 容器 completed 帧上报产物并携带 SDK 会话 transcript，打通分仓方案 resume 续跑
---

# Quick Task 260807-1s3 Summary — explore 容器上传 SDK 会话，打通分仓方案 resume

## 根因（实测会话 7d7dc501，2026-08-06 15:47 分仓方案）

Phase 120（REDO-03）已落齐 resume 全链的 server 侧与容器还原侧：
`SubAgentSession.sdk_session_id/sdk_transcript` 留痕位、callbacks 蓝图调研/拟方案两个
completed 处理器的 `_apersist_subagent_sdk_session`（先留痕再落结论）、
`blueprint_research_adapter._aresume_env` 的同仓 resume env 注入、容器侧 transcript 还原 +
`ClaudeAgentOptions(resume=...)`（所有模式生效）。

断点只有一处：**容器上传侧只接了编码 execute 模式**（`_run_execute_mode`），而蓝图调研/
拟方案容器全部跑 **explore 模式**，其 completed 帧不带 `sdk_session_id/sdk_transcript`
⇒ 留痕恒空（DB 实测该会话 10 条 SubAgentSession 全部 sdk_session=False）⇒ `_aresume_env`
永远查不到可续会话 ⇒ 每轮重派都是全新会话。

## 修复

- `task/core/runner.py::_run_explore_mode`：照 execute 模式同款，从 result 取 `session_id`、
  `core.sdk_sessions.read_transcript` 读 jsonl，随 completed 帧上传。零 server 改动。
- `task/tests/test_workspace_check.py`：fixture 给 `report_completed` 配 AsyncMock——修复另一
  会话在途 hunk（explore 补 completed 帧上报）导致的既有红测。
- 新增 `task/tests/test_explore_sdk_transcript.py` 正反两用例。
- 提交与在途 hunk 合并说明：`task/core/runner.py` 上另一会话未提交的「explore completed 帧
  上报产物」hunk 与本修复是同一调用、同一 hunk 无法拆分，且互为前置 ⇒ 合入本提交，
  提交信息同时覆盖两件事。
- `make build-task` 已重建 `friday-task:latest`（2026-08-07 01:2x），runner 新容器即生效。

## 验证

- `cd task && uv run pytest tests/ -q` → 270 passed, 3 skipped（含修复前红着的
  `test_explore_clean_workspace_returns_0` 转绿）。
- 端到端判据（待下次真实跑分仓方案时人工确认）：SubAgentSession 出现 `sdk_session_saved_at`
  非空行；同仓重派日志出现 `blueprint_research_resume_env_injected`（server）与
  `claude_sdk_resume_enabled`（容器）。

## 附带结论（诊断中确认，未改动）

- 用户看到的「repo_completed 0 项实现 / 0 条契约」是事件计数键误读蓝图顶层
  `implementation_items/api_contracts`（RepoPlan 段里不存在）所致，**数据本身没丢**
  （PartialPlan 里 impl 4/6/12/5、契约齐全）；工作区里另一会话已修（改读
  `impl_items` + `apis_provided/consumed`），随其工作流提交。
- 2026-08-06 15:47 那次会话的容器 transcript 已随容器销毁，无法追溯 resume；修复自下次
  运行起生效。
