---
phase: quick-260807-1s3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - task/core/runner.py
  - task/tests/test_workspace_check.py
  - task/tests/test_explore_sdk_transcript.py
autonomous: true
requirements: [QUICK-260807-1S3]
must_haves:
  truths:
    - "explore 模式容器完成时，completed 帧携带 sdk_session_id 与 sdk_transcript（有会话 id 时）"
    - "服务端既有 _apersist_subagent_sdk_session（callbacks.py，蓝图调研/拟方案两个 completed 处理器已接）因此能落 SubAgentSession 留痕；同仓重派时 _aresume_env 注入 resume——本任务零服务端改动"
    - "无 session_id 时行为与现状一致（两个字段为空串）"
  artifacts:
    - path: "task/core/runner.py"
      provides: "_run_explore_mode 上传 SDK 会话（复用 core.sdk_sessions.read_transcript）"
    - path: "task/tests/test_explore_sdk_transcript.py"
      provides: "explore 上传 SDK 会话的正反用例"
  key_links:
    - from: "task/core/runner.py"
      to: "task/core/sdk_sessions.py"
      via: "read_transcript(session_id, workspace_path)"
      pattern: "read_transcript"
---

<objective>
打通分仓方案（蓝图调研/拟方案）容器的 claude 会话 resume 续跑闭环（quick 260807-1s3）。

背景：Phase 120（REDO-03）已落 server 侧全链——SubAgentSession 的 sdk_session_id/sdk_transcript 留痕位、
callbacks 两个蓝图 completed 处理器的 `_apersist_subagent_sdk_session`、`blueprint_research_adapter._aresume_env`
的 resume env 注入、容器侧 transcript 还原与 `ClaudeAgentOptions(resume=...)`。但**容器上传侧只接了编码
execute 模式**（runner.py:507-519）：蓝图调研/拟方案容器跑的 explore 模式在 completed 帧不带
sdk_session_id/sdk_transcript ⇒ 留痕恒空 ⇒ `_aresume_env` 永远查不到可续会话 ⇒ 每轮重派都是全新会话。
实测（会话 7d7dc501，2026-08-06 15:47）：SubAgentSession 全部 sdk_session=False。

修复：_run_explore_mode 照 execute 模式同款逻辑，从 result 取 session_id、read_transcript 读 jsonl，
随 completed 帧上传。`_execute_claude` 所有模式都返回 session_id 且已支持 resume 选项，零其它改动。
</objective>

<execution_context>
⚠️ git 纪律：工作树有大量无关在途改动。只 add 本任务三个文件。
⚠️ `task/core/runner.py` 现有一处**未提交的在途 hunk**（另一会话给 explore 模式补 completed 帧上报，
修「四仓调研/拟方案全灭」）——本任务的改动扩展的正是同一个调用、同一 hunk 无法拆分暂存，
且两改动语义上是同一件事的两半（先有 completed 帧才有地方挂 transcript）⇒ 合入同一提交，
提交信息注明同时覆盖两件事。该在途 hunk 还让 `test_explore_clean_workspace_returns_0` 处于红态
（fixture 未给 report_completed 配 AsyncMock），一并修复。
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: explore 模式 completed 帧携带 SDK 会话 + 测试</name>
  <files>task/core/runner.py, task/tests/test_workspace_check.py, task/tests/test_explore_sdk_transcript.py</files>
  <action>
1. `_run_explore_mode`：在 `report_completed` 前照 execute 模式（runner.py:492-501）取
   `sdk_session_id = str(result.get("session_id") or "")`；非空则 `from core.sdk_sessions import read_transcript`
   读 `read_transcript(sdk_session_id, str(self.git_ops.get_workspace_path()))`；两个值随
   `report_completed(..., sdk_session_id=..., sdk_transcript=...)` 上传。
2. `test_workspace_check.py` 的 `mock_task_runner` fixture：`mock_cb.report_completed = AsyncMock()`（修当前红测）。
3. 新建 `task/tests/test_explore_sdk_transcript.py`：
   - 正例：run_explore_mode 返回 {"success": True, "output": "分析", "session_id": "sess-1"}，patch
     `core.runner` 里 lazy import 的 `core.sdk_sessions.read_transcript` 返回 '{"jsonl":1}'，断言
     report_completed 被 await 且 kwargs 带 sdk_session_id="sess-1"、sdk_transcript='{"jsonl":1}'。
   - 反例：result 无 session_id → sdk_session_id=="" 且 sdk_transcript==""、read_transcript 不被调。
  </action>
  <verify>
    <automated>cd task && uv run pytest tests/test_workspace_check.py tests/test_explore_sdk_transcript.py tests/test_218_callback_migration.py -q</automated>
  </verify>
  <done>
探索模式上传用例全绿 + 既有红测转绿。只 `git add task/core/runner.py task/tests/test_workspace_check.py task/tests/test_explore_sdk_transcript.py`，
提交信息：`fix(task): explore 容器 completed 帧上报产物并携带 SDK 会话 transcript，打通分仓方案 resume 续跑`。
之后 `make build-task` 重建镜像使修复对 runner 派发的容器生效。
  </done>
</task>

</tasks>

<verification>
- `cd task && uv run pytest tests/ -q` 相关文件全绿。
- 端到端判据（部署后人工验证）：跑一次蓝图分仓方案 → SubAgentSession 出现 sdk_session_saved_at 非空行 →
  触发同仓重派（长等待/澄清）时日志出现 `blueprint_research_resume_env_injected` 与容器内 `claude_sdk_resume_enabled`。
</verification>

<output>
完成后创建 `.planning/quick/260807-1s3-explore-sdk-transcript-resume/260807-1s3-SUMMARY.md`。
</output>
