---
phase: quick-260806-fy2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/services/process_runtime/blueprint_ambiguity_score.py
  - server/tests/services/process_runtime/test_blueprint_ambiguity_score.py
  - web/src/types/blueprint.ts
  - web/src/utils/clarificationQuestions.ts
  - web/src/utils/__tests__/clarificationQuestions.test.ts
  - web/src/components/blueprint/BlueprintClarificationWizard.vue
  - web/src/components/blueprint/BlueprintThreadComposer.vue
  - web/src/components/blueprint/BlueprintThreadCard.vue
  - web/src/components/blueprint/BlueprintThreadSidebar.vue
  - web/src/pages/knowledge/blueprints/[id].vue
  - web/src/locales/zh-CN.json
  - web/src/components/blueprint/__tests__/threadSidebar.spec.ts
autonomous: true
requirements: [QUICK-260806-FY2]

must_haves:
  truths:
    - "结构化澄清题在侧栏以逐步向导展示：一题一题、每题有选项+其他、最后整包提交"
    - "题面引用功能点时以标题 chip 展示，点击跳转左侧 fp-<id>"
    - "打分 prompt 要求人话提问并输出 related_feature_points / recommended；归一保留这些键"
    - "非结构化 options（label/value）与确认门/人工评论仍走旧 composer"
  artifacts:
    - path: "web/src/components/blueprint/BlueprintClarificationWizard.vue"
      provides: "逐步问答向导（选项/推荐/其他/整包提交）"
    - path: "web/src/utils/clarificationQuestions.ts"
      provides: "结构化题检测、归一、答案打包、fp 抽取"
    - path: "server/services/process_runtime/blueprint_ambiguity_score.py"
      provides: "prompt + normalize 含 related_feature_points / recommended"
  key_links:
    - from: "BlueprintThreadCard"
      to: "BlueprintClarificationWizard"
      via: "isStructuredClarificationQuestions(thread.options)"
      pattern: "BlueprintClarificationWizard"
    - from: "wizard chip"
      to: "onGotoAnchor"
      via: "emit goto-anchor fp-<id>"
      pattern: "goto-anchor"
---

<objective>
把蓝图侧栏 AI 澄清从「一坨编号题 + 自由文本」改成对话式逐步问答：选项、推荐、其他、
整包提交；题面用人话，功能点可点跳左侧。

Purpose: 人类能高效回答规格门阻塞问题，不必回翻 fp_id。
Output: 后端 prompt/归一增强；前端向导组件 + 接线 + vitest/pytest。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/quick/260806-fy2-ai-fp-id/CONTEXT.md
@server/services/process_runtime/blueprint_ambiguity_score.py
@server/services/process_runtime/blueprint_spec_gate.py
@web/src/components/blueprint/BlueprintThreadComposer.vue
@web/src/components/blueprint/BlueprintThreadCard.vue
@server/feishu/cards/chat_question_card.py
@server/delivery/services/blueprint_review_action.py

## 已核实现状

- `_open_clarification`：`question_text = "\n".join(f"{i}. {q['text']}" …)`，
  `options=questions`（`{text, options[], citations[]}` 原样入库）。
- `_reminder_questions` / 飞书卡已按结构化题消费 `options`。
- Web composer 只认 `{label,value}` ⇒ 结构化题的 `text` 键被当成空 label 丢掉。
- 功能点 DOM 锚点：`id="fp-${point.id}"`；跨段跳转 `emit('goto-anchor', \`fp-${fp}\`)`。
- 飞书卡参考：每题 select + 「其他」input + 底部统一提交。

## 锁定口径（D-01..D-05）

见 CONTEXT.md。⛔ 不拆多线程、不改 answer API。
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 打分 prompt + 问题归一（人话 / related_feature_points / recommended）</name>
  <files>server/services/process_runtime/blueprint_ambiguity_score.py, server/tests/services/process_runtime/test_blueprint_ambiguity_score.py</files>
  <behavior>
    - normalize 保留 related_feature_points（最多 8 个 id 串）与 recommended（须属于 options，否则丢）
    - 未知键仍丢弃；空 text 整项丢
    - system prompt 含：人话提问、禁止裸写 fp_id、须填 related_feature_points、每题给 recommended
  </behavior>
  <action>
1. `_normalize_questions`：白名单扩为 `{text, options, citations, related_feature_points, recommended}`。
   - `related_feature_points`：list→去空白字符串，截断 8。
   - `recommended`：str strip；不在 options 内则置 `""` 不写入或写空（测试钉死：非法 recommended 被丢）。
2. `_system_prompt` 追加硬约束（中文）：
   - 题面必须是业务人员能直接理解的口语化中文；
   - **禁止**只写 `fp_27` 这类 id；引用功能点时写其**标题**，并把 id 放进 `related_feature_points`；
   - 每题 `options` 2–4 个，并给 `recommended`（必须是 options 之一）。
3. JSON 形例更新含 `related_feature_points` / `recommended`。
4. 扩展既有 normalize 测试；加 prompt 子串断言（人话 / related_feature_points / 禁止裸写）。
5. 可观测性：本任务只改 LLM prompt 与归一，无新调用入口；既有 `blueprint_ambiguity_*` 事件字段无需新增问题正文（继续脱敏纪律）。
  </action>
  <verify>
    cd server && uv run pytest tests/services/process_runtime/test_blueprint_ambiguity_score.py -q
  </verify>
  <done>normalize 保留新键；prompt 含人话与 related_feature_points 约束；测试绿。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 前端结构化向导 + 接线 + i18n + 测试</name>
  <files>web/src/utils/clarificationQuestions.ts, web/src/utils/__tests__/clarificationQuestions.test.ts, web/src/components/blueprint/BlueprintClarificationWizard.vue, web/src/components/blueprint/BlueprintThreadCard.vue, web/src/components/blueprint/BlueprintThreadSidebar.vue, web/src/pages/knowledge/blueprints/[id].vue, web/src/types/blueprint.ts, web/src/locales/zh-CN.json, web/src/components/blueprint/__tests__/threadSidebar.spec.ts</files>
  <behavior>
    - isStructuredClarificationQuestions([{text, options}]) === true；[{label,value}] === false
    - 向导：一题可见；选选项或填其他；末题提交打包正文；emit goto-anchor `fp-x`
    - ThreadCard：结构化 ⇒ Wizard；否则旧 Composer；结构化时隐藏首条 AI 编号题面重复
    - 页面传入 featurePointTitles；chip 显示标题（缺则回退 id）
  </behavior>
  <action>
per D-01..D-05：

1. `clarificationQuestions.ts`：
   - `ClarificationQuestion` 类型：`{ text, options: string[], recommended?: string, related_feature_points?: string[], citations?: string[] }`
   - `isStructuredClarificationQuestions(raw)`：至少一项有非空 text/question
   - `normalizeClarificationQuestions(raw)`：兼容 text|question、options 字符串数组、recommended、related_feature_points
   - `extractFeaturePointIds(text, related?)`：合并 related + 题面 `/fp_[A-Za-z0-9_]+/g`
   - `formatClarificationAnswers(pairs)`：打包提交正文

2. `BlueprintClarificationWizard.vue`：
   - props: questions, featurePointTitles?: Record&lt;string,string&gt;, submitting
   - emits: submit(body), goto-anchor(domId)
   - UI：进度「第 i / n 题」、题文、FP chips（标题优先）、单选 options（推荐标 ⭐ / Badge）、「其他」展开 Textarea、上一步/下一题或提交
   - 每题必须有答案（选项或非空其他）才能下一步/提交
   - 安全：全文 mustache，无 v-html

3. `BlueprintThreadCard`：
   - 新增 props：`featurePointTitles`；emit `goto-anchor`
   - 结构化且 open 可作答 ⇒ 渲染 Wizard；首条 AI 消息若与编号题面重复则不渲染 body（可用 `isStructured` + author_type===ai 的首条跳过；后续 human/ai 仍显示）
   - 非结构化 ⇒ 旧 Composer

4. Sidebar + `[id].vue`：向下传 `featurePointTitles`（从 `requirement_spec.feature_points` 建 id→title map），冒泡 `goto-anchor` 到 `onGotoAnchor`。

5. types：`options` 联合为结构化题 | 扁平候选（文档注释说明双形态）。

6. i18n 键（`knowledge.blueprints.thread.*`）：wizardProgress / wizardOther / wizardOtherPlaceholder / wizardNext / wizardPrev / submitAll / recommended / relatedFeaturePoints 等。

7. 测试：utils 单测 + threadSidebar 扩展（结构化 options 出向导、点 chip emit、选选项+其他后提交正文含题与答）；旧 6a/6b 扁平 options 仍绿。
  </action>
  <verify>
    cd web && pnpm exec vitest run src/utils/__tests__/clarificationQuestions.test.ts src/components/blueprint/__tests__/threadSidebar.spec.ts
  </verify>
  <done>逐步向导可用；FP 可跳转；扁平路径未回归；测试绿。</done>
</task>

</tasks>

<verification>
- pytest ambiguity_score
- vitest clarificationQuestions + threadSidebar
- 手动：侧栏 AI 提问卡应逐步出题、有选项与其他、提交一条；chip 跳左侧功能点
</verification>

<success_criteria>
- 人类无需理解裸 fp_id 即可作答
- 交互对齐飞书澄清卡精神（选项 + 其他 + 整包提交），Web 额外「一题一题」
- 旧线程/非结构化路径不坏
</success_criteria>

<output>
After completion, create `.planning/quick/260806-fy2-ai-fp-id/260806-fy2-SUMMARY.md`
</output>
