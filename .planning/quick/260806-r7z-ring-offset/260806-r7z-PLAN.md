---
quick_id: 260806-r7z
slug: ring-offset
description: 蓝图查看页段落跳转高亮环紧贴内容，加 ring-offset 呼吸边距
date: 2026-08-06
---

# Quick Task 260806-r7z: 段落跳转高亮环加呼吸边距

## 问题

蓝图查看页（`web/src/pages/knowledge/blueprints/[id].vue`）的段落跳转高亮
`HIGHLIGHT_CLASS = 'rounded-xl ring-2 ring-primary/60'` 直接画在段容器的内容边界上，
而段容器（`sectionClass` 仅 `space-y-4 scroll-mt-24`）没有任何 padding ——
高亮出现的 2 秒内，段标题与卡片紧贴着高亮框，视觉上像内容顶死在边框上。

## 任务

### Task 1: HIGHLIGHT_CLASS 加 ring-offset

- **files**: `web/src/pages/knowledge/blueprints/[id].vue`
- **action**: `HIGHLIGHT_CLASS` 追加 `ring-offset-8 ring-offset-background`，
  让高亮环外扩 8px、offset 填充用页面背景色，与内容之间留出呼吸空间。
  该常量的四处消费点（十个段容器 / `#gate` 锚点 / gate 面板包裹层 / 质量报告卡）
  自动同步，⛔ 不逐处改。
- **verify**: `pnpm vitest run src/pages/knowledge/__tests__/blueprintViewer.spec.ts`
  通过；grep 确认无测试断言旧类名串。
- **done**: 高亮环与内容之间有 8px 背景色间隔，四处消费点行为一致。
