---
quick_id: 260818-pt8
phase: 260818-pt8-agent-friday-mcp-repo-summary-json
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - task/core/agent_submit_mcp.py
  - task/core/executor.py
  - task/core/runner.py
  - task/core/config.py
  - task/tests/test_agent_submit_mcp.py
  - task/tests/test_216_02_repo_summary.py
  - task/tests/test_explore_structured_submit.py
  - task/tests/test_claude_sdk_integration.py
  - server/services/process_runtime/blueprint_research_adapter.py
  - server/services/process_runtime/blueprint_route.py
  - server/subagent/api/callbacks.py
  - server/tests/test_repo_summary_callback.py
  - server/tests/subagent/test_summary_callback_charter_enqueue.py
  - server/tests/subagent/test_blueprint_research_callback.py
  - server/tests/subagent/test_blueprint_repo_plan_callback.py
  - server/tests/services/process_runtime/test_blueprint_research_stage.py
  - server/tests/delivery/test_blueprint_repo_resume.py
autonomous: true
requirements:
  - MCP-SUBMIT-01
  - MCP-SUBMIT-02
  - MCP-SUBMIT-03
  - MCP-SUBMIT-04
  - MCP-SUBMIT-05
  - MCP-SUBMIT-06
user_setup: []
must_haves:
  truths:
    - "三个场景（repo_summary / blueprint research fitness / blueprint repo plan）均经共享 Agent→Friday MCP 工厂提交结构化结果，场景 schema 隔离"
    - "未调用对应 MCP 提交工具时任务明确失败，不得静默用自由文本 JSON 兜底"
    - "结构化 MCP 捕获成功后，即使 SDK 普通文本为空也判定成功并上报（经共享 apply_capture_to_result 收口，模式内不复制）"
    - "服务端只接受结构化 MCP output；仅含 text/围栏 JSON 的旧渠道明确拒绝"
    - "失败重试不得 resume 上次未按协议提交结果的污染会话（mcp_submit_ok 缺失/False 不可续）"
    - "blueprint 派发 last_output / started 事件的 repository_name 在仓库有 name 时非空"
  artifacts:
    - path: "task/core/agent_submit_mcp.py"
      provides: "场景注册 + schema + server/tool/prompt 工厂 + apply_capture_to_result 统一收口"
      contains: "register_scenario|build_submit_mcp|apply_capture_to_result|prompt_contract"
    - path: "task/core/executor.py"
      provides: "repo_summary 与 explore(submit_scenario) 调用工厂+apply_capture_to_result；无本地复制空文本/未调用分支"
      contains: "apply_capture_to_result|submit_scenario"
    - path: "task/core/runner.py"
      provides: "completed 帧上报 mcp_result/submit_scenario，不再依赖 _extract_summary_json 兜底"
      contains: "mcp_result|submit_scenario"
    - path: "server/subagent/api/callbacks.py"
      provides: "三场景只认结构化 MCP output；拒绝 text JSON；协议失败不留污染 resume"
      contains: "mcp_result|mcp_tool_not_called|mcp_submit_ok"
    - path: "server/services/process_runtime/blueprint_research_adapter.py"
      provides: "派发注入 SUBMIT_SCENARIO；prompt MCP 契约；name 回填；resume 过滤 mcp_submit_ok"
      contains: "SUBMIT_SCENARIO|mcp_submit_ok|repository_name"
    - path: "server/tests/test_repo_summary_callback.py"
      provides: "repo_summary 服务端硬切：mcp_result 成功 + 纯 text 拒绝"
      contains: "mcp_result"
    - path: "server/tests/delivery/test_blueprint_repo_resume.py"
      provides: "resume 夹具 mcp_submit_ok=True 才可续；False/缺失不得 resume"
      contains: "mcp_submit_ok"
  key_links:
    - from: "agent_submit_mcp.build_submit_mcp + apply_capture_to_result"
      to: "ClaudeRunner.run_repo_summary_mode / run_explore_mode"
      via: "挂载 MCP 后统一收口空文本成功/未调用失败"
      pattern: "apply_capture_to_result"
    - from: "runner.report_completed(output.mcp_result)"
      to: "callbacks._parse_blueprint_fitness / _parse_blueprint_repo_plan / _update_repository_on_summary_complete"
      via: "只读 mcp_result；拒 text JSON"
      pattern: "mcp_result"
    - from: "callbacks 协议成功"
      to: "SubAgentSession.last_output.mcp_submit_ok + sdk_transcript"
      via: "仅协议成功才可被 _aresume_env 选中"
      pattern: "mcp_submit_ok"
    - from: "BlueprintResearchAdapter._dispatch_deep_task"
      to: "last_output.repository_name / started payload"
      via: "candidate.name 空则回退 repo.name"
      pattern: "repository_name"
---

<objective>
抽离共享 Agent→Friday MCP 结构化提交工厂（含 `apply_capture_to_result` 统一收口），并严格切换 repo_summary / blueprint research fitness / blueprint repo plan 到该渠道：删除自由文本 JSON 解析与 fallback；未调用 MCP 明确失败；SDK 空文本但已捕获仍成功；失败重试不 resume 污染会话；修复 blueprint `repository_name` 派发为空。

Purpose: 统一所有 agent 结构化提交路径，消灭「围栏 JSON / 前言文本」不可靠解析，避免污染 resume 与空仓库名 UI 退化。
Output: 共享 MCP 工厂 + 三场景接线 + 服务端只认结构化 output + 测试矩阵；**禁止 git commit / stage**（父代理或用户另行提交）。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md

⚠️ **git 纪律（NON-NEGOTIABLE）**
- **禁止** `git commit` / `git add` / stage。只改本计划 `files_modified` 内文件；勿触碰无关在途改动。
- 无 DB schema migration；结构化结果走既有 callback `output` dict 键（`result_type=text` 仍可，但正文键改为 `mcp_result`）。
- 不清理库内既有历史有效产物（D-05）。
- **不修改** `task/integrations/callback.py`：既有 `report_completed(output=dict, …)` 已足够承载 `mcp_result`/`submit_scenario`；runner 只改调用方 payload 形状。
</execution_context>

<context>
@.planning/STATE.md
@.cursor/rules/observability-logging.mdc
@task/core/executor.py
@task/core/runner.py
@task/core/remote_tools.py
@server/services/process_runtime/blueprint_research_adapter.py
@server/services/process_runtime/blueprint_route.py
@server/services/process_runtime/blueprint_repo_plan_schema.py
@server/subagent/api/callbacks.py
@task/tests/test_216_02_repo_summary.py
@server/tests/test_repo_summary_callback.py
@server/tests/subagent/test_summary_callback_charter_enqueue.py
@server/tests/subagent/test_blueprint_research_callback.py
@server/tests/subagent/test_blueprint_repo_plan_callback.py
@server/tests/delivery/test_blueprint_repo_resume.py

## Locked decisions (cite in actions)

- **D-01** 严格切换到统一 MCP 结构化通信，**不保留**旧结果渠道。
- **D-02** 删除自由文本 JSON 解析 / fallback；新执行路径**只接受** MCP 工具捕获结果。
- **D-03** 未调用 MCP 必须明确失败。
- **D-04** 不保留 repo_summary 私有 `submit_summary` 实现、blueprint research 文本 JSON、blueprint repo plan 文本 JSON 等旧渠道（场景可仍用语义化 tool 名，但实现必须走共享工厂）。
- **D-05** 数据库中已有历史有效产物**不是**本次清库范围。
- **D-06** 共享模块：按场景注册 schema、捕获经 MCP schema 校验的 dict、生成 server/tool 名与提示词契约、场景隔离；handler 直接捕获结构化参数；并用 `apply_capture_to_result`（或等价）统一「空文本覆盖 / 未调用失败」收口，禁止各 mode 复制。
- **D-07** 服务端继续做业务枚举 / 安全校验；结构化提交后 SDK 无普通文本也必须成功。
- **D-08** 失败重试不得 resume 上次未按协议提交结果的污染会话。
- **D-09** 修复 blueprint `repository_name` 派发元数据为空。
- **D-10** 日志遵守 structlog：事件 snake_case + kv + `category`/`component`/`duration_ms`；**不记录** payload 敏感正文。

## Claude's Discretion

- 新模块路径：`task/core/agent_submit_mcp.py`（镜像 `remote_tools.py` / `knowledge_tools.py` 工厂风格）。
- 场景 ID 固定三值：`repo_summary` / `blueprint_research_fitness` / `blueprint_repo_plan`。
- completed 帧契约：`output.submit_scenario` + `output.mcp_result`（dict）；可保留 `task_type` 标量；**禁止**再把整份 JSON 塞进 `output.text` 当权威结果。`text` 可空串或省略。
- explore 链通过 `FRIDAY_TASK_SUBMIT_SCENARIO`（dispatch `env_FRIDAY_TASK_SUBMIT_SCENARIO`）选择 fitness vs plan；未设置则保持普通 explore（无提交工具，行为不变）。
- MCP server/tool 名：`friday-submit` + 场景专用 tool（如 `submit_repo_summary` / `submit_blueprint_fitness` / `submit_blueprint_repo_plan`），全名 `mcp__friday-submit__{tool}`；**删除**旧 `mcp__repo-summary__submit_summary` 私有 server。
- `apply_capture_to_result(result, capture, *, scenario) -> dict`：若 capture 有值 → 写入 `mcp_result`/`submit_scenario`、强制 `success=True`、清 `error`；若无值 → `success=False`、稳定 reason `mcp_tool_not_called`（可附短 preview 脱敏）；**唯一**实现点，repo_summary 与 explore 只调用它。
- resume 门闩：仅 `last_output.mcp_submit_ok=True` 可进 `_aresume_env`；协议失败不落 / 清空 `sdk_transcript` 或显式 `mcp_submit_ok=False`。
- `repository_name`：dispatch 时空名回退 `repo.name`；路由 placement 种子批量补名。
- **不改** `task/integrations/callback.py`（见 execution_context）。
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 共享 Agent→Friday MCP 工厂 + 严格迁移 repo_summary</name>
  <files>task/core/agent_submit_mcp.py, task/core/executor.py, task/core/runner.py, task/core/config.py, task/tests/test_agent_submit_mcp.py, task/tests/test_216_02_repo_summary.py, task/tests/test_claude_sdk_integration.py, server/subagent/api/callbacks.py, server/tests/test_repo_summary_callback.py, server/tests/subagent/test_summary_callback_charter_enqueue.py</files>
  <behavior>
    - 工厂可按场景注册 schema；`build_submit_mcp(scenario)` 返回 server 配置、allowed tool 全名、prompt 契约段、CaptureStore（handler 写入经 schema 形状的 dict）(D-06)。
    - 导出 `apply_capture_to_result`：统一空文本成功 / 未调用失败；`run_repo_summary_mode` **不得**内联复制该分支 (D-06/D-07)。
    - 场景隔离：注册 A 的 schema/tool 不得被 B 的 capture 污染；未知场景 raise 明确错误。
    - `run_repo_summary_mode` 只走工厂；删除私有 `create_sdk_mcp_server(name=repo-summary…)` / `_handle_submit_summary` / `_REPO_SUMMARY_INPUT_SCHEMA` 内联旧渠道 (D-04)。
    - 未调用提交工具 → `success=False`，error reason=`mcp_tool_not_called`，**不得**把普通文本当成功 (D-03)。
    - 已捕获 + SDK empty response / 非零退出 → 仍 `success=True`，runner 上报 `mcp_result` (D-07)。
    - runner `_run_repo_summary_mode`：**删除** `_extract_summary_json` 作为权威路径；completed `output` 含 `submit_scenario=repo_summary` + `mcp_result` (D-01/D-02)。
    - 服务端 `_update_repository_on_summary_complete` 只读 `mcp_result`；仅 text/围栏 JSON → 明确拒绝写树/走失败语义，**不再** `_parse_summary_json` 成功落库 (D-02)。
    - repo_summary 生命周期日志：`repo_summary_submit_started/completed/failed`（或等价）带 `category`/`component`/`duration_ms`；**不**记录 mcp_result/overview 正文 (D-10)。
  </behavior>
  <action>
  1. 新建 `task/core/agent_submit_mcp.py`：定义三场景常量；`ScenarioSpec`；`register_scenario` / `get_scenario`；`build_submit_mcp(scenario) -> BuiltSubmitMcp`（`mcp_servers`、`allowed_tools`、`prompt_contract`、`capture`）。handler 只写 `capture.value = dict(args)`；日志 `agent_submit_mcp_captured`（scenario/tool，禁 dump args）(D-06/D-10)。实现 `apply_capture_to_result(result, capture, *, scenario) -> dict` 作为唯一收口：有 capture → `mcp_result` + `submit_scenario` + `success=True` + pop error；无 capture → `success=False` + reason `mcp_tool_not_called`。把现有 `_REPO_SUMMARY_INPUT_SCHEMA` / tree node schema **原样迁入** `repo_summary` 注册（含 charter 可选字段）。
  2. `TaskConfig` 增加可选 `submit_scenario: str = ""`（env `FRIDAY_TASK_SUBMIT_SCENARIO`）；repo_summary 模式硬绑定 `repo_summary`。
  3. 改写 `ClaudeRunner.run_repo_summary_mode`：挂载工厂 MCP；prompt 追加 `prompt_contract`；`_execute_claude` 返回后**只**调用 `apply_capture_to_result`（含 CLI 非零退出后仍有 capture 的路径）；补生命周期日志 started/completed/failed + duration_ms + category=caller + component=task（或既有 task 组件名）；禁止日志 payload (D-03/D-07/D-10)。删除旧私有 server/handler/schema 常量。
  4. 改写 `runner._run_repo_summary_mode`：成功 `report_completed(output={"task_type":"repo_summary","submit_scenario":"repo_summary","mcp_result":...}, result_type="text")`；失败 `report_failed`。删除对本路径权威的 `_extract_summary_json` / `_sanitize_summary` 依赖。**不改** `task/integrations/callback.py`。
  5. 改写 `callbacks._update_repository_on_summary_complete`：权威输入=`p["output"].get("mcp_result")`（必须 dict）；否则 `repo_summary_mcp_result_missing` + 失败/拒绝写树（与「明确拒绝旧文本」一致）。本路径**删除**对 `_parse_summary_json` 的调用 (D-02)。业务校验（tree/facets/charter）保持 (D-07)。同步更新 `test_summary_callback_charter_enqueue.py`：夹具改为 `mcp_result`（含 charter），确保 enqueue 路径不回退 text JSON。
  6. 测试（task + **server 硬切必须同跑**）：
     - `test_agent_submit_mcp.py`：注册隔离、tool 全名、prompt 契约、`apply_capture_to_result` 空捕获失败 / 有捕获覆盖 empty error。
     - 更新 `test_216_02_repo_summary.py` / `test_claude_sdk_integration.py`：新 tool 全名；未调用失败；空文本成功；上报 `mcp_result`。
     - **`server/tests/test_repo_summary_callback.py`**：`mcp_result` 成功写库；纯 `output.text` 围栏/裸 JSON **明确拒绝**（不写有效 tree / 走失败语义）。
     - **`server/tests/subagent/test_summary_callback_charter_enqueue.py`**：改为 mcp_result 夹具，断言 charter enqueue 仍通。
  </action>
  <verify>
    <automated>cd /Users/zaneliu/Projects/open-source/friday-ai/task && uv run pytest tests/test_agent_submit_mcp.py tests/test_216_02_repo_summary.py tests/test_claude_sdk_integration.py -q --tb=short -k "repo_summary or submit_mcp or agent_submit or apply_capture" && cd /Users/zaneliu/Projects/open-source/friday-ai/server && uv run pytest tests/test_repo_summary_callback.py tests/subagent/test_summary_callback_charter_enqueue.py -q --tb=short -k "mcp_result or summary or charter or text or reject or parse"</automated>
  </verify>
  <done>
  共享工厂含 apply_capture_to_result；repo_summary 仅 MCP；空文本可成功；服务端 callback 硬切测试绿（含 charter enqueue）；生命周期日志合规；无 git commit。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 迁移 blueprint fitness / repo_plan + resume 污染过滤 + repository_name</name>
  <files>task/core/agent_submit_mcp.py, task/core/executor.py, task/core/runner.py, task/tests/test_explore_structured_submit.py, task/tests/test_agent_submit_mcp.py, server/services/process_runtime/blueprint_research_adapter.py, server/services/process_runtime/blueprint_route.py, server/subagent/api/callbacks.py, server/tests/subagent/test_blueprint_research_callback.py, server/tests/subagent/test_blueprint_repo_plan_callback.py, server/tests/services/process_runtime/test_blueprint_research_stage.py, server/tests/delivery/test_blueprint_repo_resume.py</files>
  <behavior>
    - 工厂注册 `blueprint_research_fitness` 与 `blueprint_repo_plan` schema；task 侧 required/enum 与 server 契约对照有自动化测试防漂移 (D-06)。
    - dispatch `mode=research|plan` 注入 `env_FRIDAY_TASK_SUBMIT_SCENARIO`；prompt **删除**「只输出一个 JSON 对象」权威要求，改为工厂 MCP 契约 (D-01/D-04)。
    - `run_explore_mode`：有 `submit_scenario` 则挂载 MCP，收口**只**调 `apply_capture_to_result`（不复制 repo_summary 分支）(D-03/D-06/D-07)。
    - `_run_explore_mode`：有 submit_scenario 时 completed 上报 `mcp_result`；无 mcp_result → `report_failed`；失败路径不上传可 resume transcript (D-02/D-08)。
    - `_parse_blueprint_fitness` / `_parse_blueprint_repo_plan`：**删除** text/围栏/`_parse_summary_json` 分支；只接受 `output.mcp_result`；纯 text → None/err (D-02)。
    - 协议成功：`last_output.mcp_submit_ok=True`；失败：`False` 且不留污染 transcript；`_aresume_env` 仅选 `mcp_submit_ok=True` (D-08)。
    - `server/tests/delivery/test_blueprint_repo_resume.py`：可 resume 夹具必须写 `mcp_submit_ok=True`；False/缺失断言不得注入 resume env (D-08)。
    - `repository_name`：dispatch 回退 `repo.name`；placement 种子批量补名 (D-09)。
  </behavior>
  <action>
  1. 在 `agent_submit_mcp.py` 注册两场景 schema：fitness 含 `fitness.verdict` enum（suitable|partial|unsuitable）+ `role_suggestion`（direct|indirect）+ `responsibility` + `findings`；repo_plan 以 `repo_plan` 为 required 对象（从 `BLUEPRINT_REPO_PLAN_SCHEMA` **复制**必要 required/enum 到 task 侧——禁止 import server）。在 `test_agent_submit_mcp.py`（或新建对照测）增加 **schema 契约对照**：断言 task 侧 fitness/repo_plan 的 `required` 与关键 `enum` 与 `server/subagent/api/callbacks.py` / `blueprint_repo_plan_schema.py` 导出常量一致（可读 server 文件做静态对照，或共享一份冻结快照常量；不得在运行时跨包 import Django）。
  2. `blueprint_research_adapter._build_dispatch_metadata`：按 mode 注入 `env_FRIDAY_TASK_SUBMIT_SCENARIO`。业务 prompt 去掉「只输出 JSON」权威段；容器侧统一追加工厂 `prompt_contract`。
  3. `run_explore_mode`：非空 scenario → `build_submit_mcp` + 挂载 + **仅** `apply_capture_to_result`；无 scenario 零回归。
  4. `_run_explore_mode`：有 `mcp_result` 则结构化 `report_completed`；配置了 scenario 却无结果 → `report_failed`；仅成功捕获时上传 sdk transcript。
  5. 回调：两 parse 函数只读 `mcp_result`；成功 merge `mcp_submit_ok=True`；失败 `False` 且跳过/清空 `_apersist_subagent_sdk_session`。`_aresume_env`：`.filter(last_output__mcp_submit_ok=True)` (D-08)。
  6. `repository_name` (D-09)：dispatch 空名回退 `repo.name`；route 对 placement 空名批量补 Repository.name。
  7. 观测：structured submit 事件带 scenario/duration_ms/category/component；禁 payload 正文 (D-10)。
  8. 测试：`test_explore_structured_submit.py`（两场景、未调用失败、空文本成功、无 scenario 零回归）；research/plan callback：mcp_result 成功 + 纯 text **拒绝**；**更新 `test_blueprint_repo_resume.py`**：既有可 resume 夹具加 `last_output.mcp_submit_ok=True`；新增 False/缺失不得 resume；research_stage：SUBMIT_SCENARIO + repository_name 非空；schema 对照测见上。
  </action>
  <verify>
    <automated>cd /Users/zaneliu/Projects/open-source/friday-ai/task && uv run pytest tests/test_explore_structured_submit.py tests/test_agent_submit_mcp.py -q --tb=short && cd /Users/zaneliu/Projects/open-source/friday-ai/server && uv run pytest tests/subagent/test_blueprint_research_callback.py tests/subagent/test_blueprint_repo_plan_callback.py tests/services/process_runtime/test_blueprint_research_stage.py tests/delivery/test_blueprint_repo_resume.py -q --tb=short -k "fitness or repo_plan or mcp_result or resume or repository_name or submit or mcp_submit"</automated>
  </verify>
  <done>
  蓝图两场景仅 MCP；schema 不漂移；旧文本拒绝；resume 夹具与过滤齐备；repository_name 非空；测试绿；无 git commit。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 收口旧渠道残留 + 跨场景验收矩阵</name>
  <files>task/core/executor.py, task/core/runner.py, server/subagent/api/callbacks.py, task/tests/test_216_02_repo_summary.py, task/tests/test_explore_structured_submit.py, server/tests/test_repo_summary_callback.py, server/tests/subagent/test_blueprint_research_callback.py, server/tests/subagent/test_blueprint_repo_plan_callback.py, server/tests/delivery/test_blueprint_repo_resume.py</files>
  <behavior>
    - 三场景新执行路径不再引用 `mcp__repo-summary__`；`_parse_blueprint_fitness` / `_parse_blueprint_repo_plan` / `_update_repository_on_summary_complete` 成功路径无 text JSON 提取 (D-01/D-04)。
    - grep 门禁**收窄到本次三场景**：不得误伤 `parse_verify_verdict` / repo_verify 文本解析（该链不在本 quick 范围）。
    - 验收矩阵：共享工厂、`apply_capture_to_result`、三场景、未调用错误、空文本成功、resume 过滤、服务端只认 MCP、旧文本拒绝。
    - 不清理 DB 历史有效产物 (D-05)。
  </behavior>
  <action>
  1. grep 门禁（写入测试断言或文档化命令）：
     - 禁止：`mcp__repo-summary__` 出现在 `task/core/` 生产代码。
     - 禁止：`_parse_blueprint_fitness` / `_parse_blueprint_repo_plan` / `_update_repository_on_summary_complete` 函数体内对 `output["text"]` / `_parse_summary_json` 的成功路径调用。
     - **允许保留**：`parse_verify_verdict` 及 repo_verify 相关 `_parse_summary_json` 用法（本 quick 不改 verify 链）。门禁命令必须 scoped（函数名/文件片段），禁止对整个 `callbacks.py` 做「凡 `_parse_summary_json` 即失败」的裸扫。
  2. 表驱动验收矩阵覆盖用户列出断言点；error reason 稳定。
  3. 回归：普通 explore / execute 未误挂 friday-submit；与 knowledge/remote 同挂时 allowed_tools 仍含提交工具。
  4. 工厂与三场景 parse docstring 标明「唯一结构化渠道 / 拒 text JSON」(D-01)。
  </action>
  <verify>
    <automated>cd /Users/zaneliu/Projects/open-source/friday-ai && uv run --directory task pytest tests/test_agent_submit_mcp.py tests/test_216_02_repo_summary.py tests/test_explore_structured_submit.py -q --tb=short && uv run --directory server pytest tests/test_repo_summary_callback.py tests/subagent/test_blueprint_research_callback.py tests/subagent/test_blueprint_repo_plan_callback.py tests/delivery/test_blueprint_repo_resume.py -q --tb=short && ! rg -n "mcp__repo-summary__" task/core && python - <<'PY'
# scoped gate from repo root: three-scenario parsers must not call _parse_summary_json
# (does not scan parse_verify_verdict / whole-file _parse_summary_json)
from pathlib import Path
import re
p = Path("server/subagent/api/callbacks.py").read_text()
for name in ("_parse_blueprint_fitness", "_parse_blueprint_repo_plan", "_update_repository_on_summary_complete"):
    m = re.search(rf"def {name}\(.*?(?=\ndef |\Z)", p, re.S)
    assert m, name
    body = m.group(0)
    assert "_parse_summary_json" not in body, f"{name} still uses _parse_summary_json"
print("three_scenario_parse_gate_ok")
PY</automated>
  </verify>
  <done>
  三场景旧渠道清零；grep 不误伤 repo_verify；验收矩阵全绿；无 git commit。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 容器 agent → completed callback `output.mcp_result` | LLM 经 MCP schema 形状的 dict 进入服务端写库 |
| 服务端业务校验 → Repository / PartialPlan | 枚举/白名单/树校验仍为权威；MCP 只保证 JSON 形状 |
| resume transcript → 下一轮容器 | 污染会话若被 resume 会复读失败协议上下文 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-pt8-01 | Tampering | `output.mcp_result` | mitigate | 服务端继续 enum/白名单/jsonschema（fitness verdict、repo_plan validate）；不采信容器上报 repository_id |
| T-pt8-02 | Information Disclosure | structlog / events | mitigate | 禁止日志与活动事件打印 mcp_result/prompt/token；error 经 redact_secrets_in_text；生命周期日志带 category/component/duration_ms |
| T-pt8-03 | Denial of Service | 超大 mcp_result | mitigate | 沿用既有长度/条数上界（summary 8192、findings cap、repo_plan validate）；超限失败可重试 |
| T-pt8-04 | Tampering | resume 污染会话 | mitigate | 仅 `mcp_submit_ok=True` 可注入 resume env；协议失败不落 transcript；resume 测试夹具显式覆盖 |
| T-pt8-SC | Tampering | npm/pip installs | accept | 本计划无新包；复用 claude-agent-sdk 既有 create_sdk_mcp_server |
</threat_model>

<verification>
- Task：工厂 + `apply_capture_to_result` + repo_summary + explore structured submit 全绿。
- Server：`test_repo_summary_callback.py` + charter enqueue + research/plan callback + `test_blueprint_repo_resume.py` 全绿。
- 三场景 scoped grep 门禁通过；`parse_verify_verdict` 不受影响。
</verification>

<success_criteria>
- 共享 MCP 工厂（含 apply_capture_to_result）为唯一结构化提交收口；三场景均迁移完成。
- 未调用 MCP → 明确失败；已捕获 + 空文本 → 成功（无模式内复制逻辑）。
- 服务端拒绝旧文本 JSON；不 resume 协议失败会话（resume 测试夹具已更新）。
- blueprint 派发 `repository_name` 在有仓库名时非空；fitness/repo_plan schema 有对照测。
- repo_summary 生命周期日志含 category/component/duration_ms；无 payload 正文；无 git commit。
</success_criteria>

## Source Audit

| SOURCE | ID | Item | Plan | Status |
|--------|-----|------|------|--------|
| GOAL | — | 统一 Agent→Friday MCP 结构化提交并迁移三场景 | 01 | COVERED |
| REQ | MCP-SUBMIT-01 | 共享工厂：注册 schema / 捕获 / 名与契约 / 隔离 / apply_capture_to_result | Task 1 | COVERED |
| REQ | MCP-SUBMIT-02 | 迁移 repo_summary（删私有 submit 渠道） | Task 1 | COVERED |
| REQ | MCP-SUBMIT-03 | 迁移 blueprint research fitness | Task 2 | COVERED |
| REQ | MCP-SUBMIT-04 | 迁移 blueprint repo plan | Task 2 | COVERED |
| REQ | MCP-SUBMIT-05 | 拒文本 JSON；未调用失败；空文本成功；resume 过滤 | Task 1–3 | COVERED |
| REQ | MCP-SUBMIT-06 | 修复 repository_name 派发为空 | Task 2 | COVERED |
| CONTEXT | D-01..D-10 | 用户锁定决策全文（MCP-only 硬切） | Task 1–3 | COVERED |
| RESEARCH | — | （本 quick 无独立 RESEARCH.md；以代码根因为准） | — | N/A |

<output>
Create `.planning/quick/260818-pt8-agent-friday-mcp-repo-summary-json/260818-pt8-SUMMARY.md` when done
</output>
