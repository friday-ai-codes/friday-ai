---
quick_id: 260826-m6h
phase: quick-260826-m6h-mcp-dev
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/tests/delivery/test_blueprint_gate_api.py
  - mcp/src/tools.ts
  - mcp/src/server.ts
  - mcp/package.json
  - mcp/README.md
  - mcp/tests/server.test.ts
  - mcp/dist/cli.js
autonomous: true
requirements:
  - MCP-SYNC-01
  - MCP-BUILD-01
  - CHARTER-GATE-TEST-01
  - DEV-VALIDATE-01
user_setup: []
must_haves:
  truths:
    - "MCP 客户端 `FRIDAY_TOOLS` 与服务端 `server/mcp_tools/urls.py` 公开工具名集合逐字一致（当前缺口 12 个：graph_query / impact_analysis / detect_changes / list_processes / get_process / rename_preview / trace_call_path / route_blueprint_repos / generate_requirement_spec / start_repo_research / get_repo_research / apply_repo_association）"
    - "每个新增工具有非空 description、object 型 inputSchema（字段约束镜像对应 DRF serializer），且在 `TOOL_ANNOTATIONS` 有中文 title + 行为提示"
    - "`@friday-ai-codes/mcp` 版本已 bump（0.5.0→0.6.0），`SERVER_VERSION` 与 package.json 对齐，`pnpm build` 后 dist 含全部新工具名"
    - "`test_rejected_to_boundary_never_overwrites_human_confirmed` 断言正式字段冻结 + 侧信道增长（appendices / change_proposals），不再读 draft_content.boundaries；模块 docstring 同步改正"
    - "后端确认门相关测试、MCP vitest、前端 typecheck 通过；本地 `make dev`（server:10241 / web:10240）热加载可用"
  artifacts:
    - path: "mcp/src/tools.ts"
      provides: "49 工具定义 + TOOL_ANNOTATIONS，与 urls.py 对齐"
      contains: "graph_query"
    - path: "mcp/package.json"
      provides: "版本 0.6.0"
      contains: '"version": "0.6.0"'
    - path: "mcp/dist/cli.js"
      provides: "重建后的可发布 CLI，含 12 个新工具名"
      contains: "graph_query"
    - path: "mcp/tests/server.test.ts"
      provides: "工具数量与关键名断言更新为 49"
      contains: "toHaveLength(49)"
    - path: "server/tests/delivery/test_blueprint_gate_api.py"
      provides: "确认门章程回灌侧信道契约断言"
      contains: "appendices|change_proposals"
  key_links:
    - from: "server/mcp_tools/urls.py"
      to: "mcp/src/tools.ts"
      via: "工具名集合双向 diff（python/rg）后补齐缺失定义"
      pattern: "tools/[a-z_]+/"
    - from: "server/mcp_tools/serializers.py"
      to: "mcp/src/tools.ts inputSchema"
      via: "每个新工具字段/required/enum/default 镜像对应 *RequestSerializer"
      pattern: "GraphQueryRequestSerializer|ImpactAnalysisRequestSerializer|RouteBlueprintReposRequestSerializer"
    - from: "server/contracts/graph-query.v1.json"
      to: "mcp/src/tools.ts graph_query"
      via: "description + inputSchema + annotations 以 canonical manifest 为准"
      pattern: "graph_query"
    - from: "server/repositories/services/charter_draft_writeback.py"
      to: "server/tests/delivery/test_blueprint_gate_api.py"
      via: "asubmit_charter_draft → appendices/change_proposals；测试改断言侧信道而非 draft_content"
      pattern: "appendices|change_proposals|draft_content"
    - from: "mcp/src/tools.ts"
      to: "mcp/dist/cli.js"
      via: "pnpm build (tsdown)"
      pattern: "graph_query|route_blueprint_repos"
---

<objective>
补齐 MCP 客户端与服务端工具契约（12 个缺口全量同步，不只 graph_query），bump 并重建 `@friday-ai-codes/mcp` dist；修正确认门章程回灌旧测试契约（侧信道 appendices/change_proposals，而非 draft_content）；验证后端 / MCP / 前端与本地 `make dev` 热加载可用。

Purpose: 本地 IDE 经 MCP 能调用 v0.24 图查询与蓝图 stage 单跑工具；确认门回归不再因 append-only 章程契约漂移而红；dev 链路可继续迭代。

Output: 更新后的 mcp 源码+dist、修好的 gate 测试、通过的验证命令。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@AGENTS.md
@server/mcp_tools/urls.py
@server/mcp_tools/serializers.py
@server/contracts/graph-query.v1.json
@server/repositories/services/charter_draft_writeback.py
@server/tests/delivery/test_blueprint_gate_api.py
@server/tests/repositories/test_charter_draft_writeback.py
@mcp/src/tools.ts
@mcp/src/server.ts
@mcp/package.json
@mcp/README.md
@mcp/tests/server.test.ts
@mcp/tsdown.config.ts
@Makefile
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IDE MCP client → Friday `/api/mcp/tools/*` | 未鉴权/错误工具名/超大输入可能被滥用；客户端仅透传 PAT + JSON args |
| Confirm-gate REST → RepoCharter writeback | 章程回灌须 append-only；测试若仍断言 draft_content 会掩盖正式字段被覆盖的回归 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-m6h-01 | Spoofing | mcp callFridayTool Authorization | accept | 既有 Bearer PAT；本计划不改鉴权面 |
| T-m6h-02 | Tampering | FRIDAY_TOOLS 与服务端漂移 | mitigate | 用 urls.py 名集合双向 diff 作验收闸；测试 `toHaveLength(49)` + 12 名 `toContain` |
| T-m6h-03 | Information Disclosure | MCP 错误返回体 | accept | 既有 body 截断 + PAT 不入返回文本；本计划不改 callFridayTool |
| T-m6h-04 | Elevation | apply_repo_association / report_* 写工具 | mitigate | 客户端只暴露已有服务端端点；写工具 annotations 用 generator/executor，不伪造 readOnly |
| T-m6h-05 | Tampering | charter formal fields via gate | mitigate | 测试断言正式字段字节不变 + draft_content 仍为空对象 + 侧信道增长 |
| T-m6h-SC | Tampering | npm/pip installs | accept | 本计划不新增包依赖；仅 bump 自身 version 与 rebuild |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 修正确认门章程回灌测试为侧信道契约</name>
  <files>server/tests/delivery/test_blueprint_gate_api.py</files>
  <behavior>
    - `test_rejected_to_boundary_never_overwrites_human_confirmed`：正式字段（positioning / owned_domains / boundaries / evolution / version / source）在 rejected→boundary 后不变
    - `fresh.draft_content` 为空或无 `boundaries` 键（自动化永不写 draft_content）
    - 侧信道有实质写入：`len(appendices) + len(change_proposals) > 0`，且能在侧信道中找到与 rejected reason（「该类需求不落此仓」）相关的内容（appendix text / proposal after/before/rule 任一字段）
    - 模块顶 docstring 第 7/14 行：删除「只写 draft_content」表述，改为 append-only 侧信道（appendices / change_proposals）
  </behavior>
  <action>
    以 `charter_draft_writeback.asubmit_charter_draft` 与 `test_charter_draft_writeback.test_human_confirmed_formal_unchanged_no_draft_content` 为权威契约：已有 human_confirmed 行时自动化只写 `appendices` / `change_proposals`，永不写正式字段或 `draft_content`。

    修改 `server/tests/delivery/test_blueprint_gate_api.py`：
    1. 更新文件头 docstring（约 L13–14）：把「只写 draft_content」改为「正式字段冻结，差异进 appendices/change_proposals」。
    2. 在 `test_rejected_to_boundary_never_overwrites_human_confirmed`（约 L705–737）保留正式字段 before/after 对比；删除 `assert fresh.draft_content["boundaries"]`（当前 KeyError: boundaries）。改为：
       - `assert fresh.draft_content == {}`（或 `not (fresh.draft_content or {}).get("boundaries")`）
       - `assert (fresh.appendices or []) or (fresh.change_proposals or [])`
       - 用侧信道内容包含 rejected reason 子串做正向断言（对齐 writeback 测试风格）
    3. ⛔ 不改生产代码（writeback / charter_service / gate views）；只修测试与 docstring。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/delivery/test_blueprint_gate_api.py -k "rejected_to_boundary" -q --tb=short</automated>
  </verify>
  <done>四个 rejected_to_boundary 用例全绿；不再出现 draft_content["boundaries"] KeyError；docstring 表述与 append-only 契约一致。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 同步 MCP 客户端 12 个缺口工具并 bump/重建 dist</name>
  <files>mcp/src/tools.ts, mcp/src/server.ts, mcp/package.json, mcp/README.md, mcp/tests/server.test.ts, mcp/dist/cli.js</files>
  <behavior>
    - `FRIDAY_TOOLS.length === 49`，名集合与 `urls.py` 的 `tools/<name>/` 逐字相等
    - 12 个缺口工具均出现在 `FRIDAY_TOOLS` 与 `TOOL_ANNOTATIONS`：`graph_query`, `impact_analysis`, `detect_changes`, `list_processes`, `get_process`, `rename_preview`, `trace_call_path`, `route_blueprint_repos`, `generate_requirement_spec`, `start_repo_research`, `get_repo_research`, `apply_repo_association`
    - `graph_query` 的 description/inputSchema/annotations 对齐 `server/contracts/graph-query.v1.json`
    - 其余 11 个 inputSchema 镜像对应 `*RequestSerializer`（required / enum / default / min/max）
    - `package.json` version = `0.6.0`；`SERVER_VERSION` = `0.6.0`
    - `pnpm test` 与 `pnpm build` 通过；`dist/cli.js` 含全部 12 个新工具名字符串
  </behavior>
  <action>
    权威源：`server/mcp_tools/urls.py`（49 路由）+ `server/mcp_tools/serializers.py`（字段契约）+ `server/contracts/graph-query.v1.json`（graph_query canonical）。当前客户端 37 工具，缺口恰好 12，无多余。

    1. **先改测试（RED）**：`mcp/tests/server.test.ts` 把 `37` → `49`；增加对上述 12 名的 `toContain`；保留「每个工具有 annotations 且无多余条目」断言。
    2. **`mcp/src/tools.ts`**：
       - 更新文件头注释工具数 37→49。
       - 在合适分组位置追加 12 个 `FridayToolDefinition`：
         - 图查询族：`graph_query`（优先拷贝 manifest 的 description + inputSchema；annotations title 用 manifest `annotations.title`：「代码图谱 · 单仓统一查询」）
         - 影响/变更族：`impact_analysis`, `detect_changes`, `trace_call_path`, `rename_preview`
         - Process 族：`list_processes`, `get_process`
         - 蓝图 stage sandbox 族：`route_blueprint_repos`, `generate_requirement_spec`, `start_repo_research`, `get_repo_research`, `apply_repo_association`（后者为唯一写回路径，annotations 用 generator/executor，openWorld 按是否触达外部系统判断——本工具写 Friday 项目绑定，用 generator）
       - 每个工具的 `required` / ChoiceField enum / 默认值必须镜像 serializer；复杂 JSONField（`requirement_spec`, `repositories`, `bindings`, `feature_points`）用 `type: object` / `dictList` / `array of object`，description 写清形状。
       - 同步补全 `TOOL_ANNOTATIONS`（中文 `阶段 · 动作` title + query/generator/executor 三档）。
    3. **版本**：`mcp/package.json` `0.5.0`→`0.6.0`；`mcp/src/server.ts` `SERVER_VERSION` `0.2.0`→`0.6.0`（与 package 对齐，避免 banner/IDE 显示漂移）。
    4. **README**：`mcp/README.md`「23 个工具」改为「49 个工具」，并补上图查询 / Process / 影响面 / 蓝图 stage 单跑家族的一句话清单（勿再漏列）。
    5. **构建**：在 `mcp/` 执行 `pnpm test` → `pnpm build`；确认 `dist/cli.js` 含 12 个新名。
    6. ⛔ 不新增 npm 依赖；⛔ 不改服务端 urls/serializers（契约已存在）；⛔ 不在本任务提交 git（含 mcp submodule）。
  </action>
  <verify>
    <automated>cd mcp && pnpm test && pnpm build && node -e "const fs=require('fs');const s=fs.readFileSync('dist/cli.js','utf8');const need=['graph_query','impact_analysis','detect_changes','list_processes','get_process','rename_preview','trace_call_path','route_blueprint_repos','generate_requirement_spec','start_repo_research','get_repo_research','apply_repo_association'];const miss=need.filter(n=>!s.includes(n));if(miss.length){console.error('missing',miss);process.exit(1)};console.log('dist ok',need.length)"</automated>
  </verify>
  <done>客户端 49 工具与 urls.py 集合一致；测试与 dist 含 12 新工具；版本 0.6.0。</done>
</task>

<task type="auto">
  <name>Task 3: 验证后端、MCP、前端与 local dev 热加载</name>
  <files></files>
  <action>
    全链路验收（quick-full/validate），只跑验证命令、不改业务源码：

    1. **后端**：`cd server && uv run pytest tests/delivery/test_blueprint_gate_api.py -q --tb=line`（确认门全文件绿，含 Task 1 修复）。可选补充：`uv run pytest tests/repositories/test_charter_draft_writeback.py -q` 确认侧信道权威测试仍绿。
    2. **MCP**：`cd mcp && pnpm test && pnpm typecheck`（build 已在 Task 2 完成则可跳过重复 build）。
    3. **前端**：`cd web && pnpm type-check`（或仓库既有等价脚本 `vue-tsc`）；若耗时过长至少跑与蓝图/章程相关的既有 vitest 冒烟：`pnpm vitest run src/components/blueprint/__tests__/gatePanel.spec.ts`（存在则跑，不存在则跳过并在 SUMMARY 注明）。
    4. **Dev 热加载**：确认 `make dev` 会话已在跑（tmux `friday-ai`，server `10241` / web `10240`）。对 `mcp/src/tools.ts` 或无关紧要的注释级触碰后：
       - server：`uvicorn --reload` 应在 `.logs/server.log` 出现 reload 而无 traceback；
       - web：Vite 保持 listening，无 crash；
       - MCP 为独立包，dev 不自动热更 dist——在 SUMMARY 写明：改 tools 后须 `cd mcp && pnpm build`，IDE 侧按需重启 MCP 进程才能看到新工具。
    5. **契约 diff 闸**（必须）：用 python/rg 复验 `urls.py` 工具名集合 == `FRIDAY_TOOLS` name 集合，差集为空。
    6. 将命令输出要点写入 `.planning/quick/260826-m6h-mcp-dev/260826-m6h-SUMMARY.md`（执行阶段产出，本计划文件阶段可不预写）。
  </action>
  <verify>
    <automated>python3 -c "
import re, pathlib
root=pathlib.Path('.')
server=set(re.findall(r'tools/([a-z_]+)/', (root/'server/mcp_tools/urls.py').read_text()))
client=set(re.findall(r\"name: '([a-z_]+)'\", (root/'mcp/src/tools.ts').read_text()))
assert server==client, (sorted(server-client), sorted(client-server))
print('tool parity', len(server))
" && cd server && uv run pytest tests/delivery/test_blueprint_gate_api.py -q --tb=line && cd ../mcp && pnpm test && cd ../web && pnpm type-check</automated>
  </verify>
  <done>工具名差集为空；gate API 测试绿；MCP test 绿；前端 type-check 绿；dev 会话仍健康且已知 MCP rebuild 步骤。</done>
</task>

</tasks>

<verification>
- 工具名双向 diff：`server 49 == client 49`，missing/extra 均为空
- `pytest …test_blueprint_gate_api.py -k rejected_to_boundary` 与整文件通过
- `cd mcp && pnpm test && pnpm build`；dist 含 12 新工具名
- `cd web && pnpm type-check` 通过
- `make dev` 仍服务 10240/10241，无持续崩溃日志
</verification>

<success_criteria>
1. MCP 客户端公开工具与服务端 urls 完全同步（含 graph_query 与全部 stage/impact/process 工具）。
2. mcp 包版本 0.6.0 且 dist 已重建。
3. 确认门 human_confirmed 回灌测试按 appendices/change_proposals 侧信道断言并通过。
4. 后端 / MCP / 前端验证命令通过，本地 dev 可用。
</success_criteria>

<output>
Create `.planning/quick/260826-m6h-mcp-dev/260826-m6h-SUMMARY.md` when execution completes（本规划阶段不创建 SUMMARY，且不 git commit）。
</output>
