---
quick_id: 260817-h88
status: complete
completed: 2026-08-17
---

# 分仓 OpenSpec Proposal 上下文注入

## 完成内容

- 将分仓 proposal 完备化：保留完整实现说明、文件、依赖、既有集成、测试策略、API
  描述与 schema、影响说明、需求说明、验收标准和 Given/When/Then 场景。
- 新增 `render_single_repo_proposal_markdown`，从同一份 `blueprint/v1` 确定性提取单仓
  proposal；无 ORM、无 IO、无新增 LLM 调用。
- 主技术方案继续合并展示 direct 与 indirect 仓的全部 proposal。
- coding 首发与后续 wave 均从 `ArtifactVersion.content` 读取蓝图，只把当前 direct 仓的
  proposal 注入其 prompt，并明确标记为只读权威上下文。
- 未新增目标仓文件写入、环境变量分片或 `openspec/changes` 路径。
- 观测事件只记录仓库、正文长度、SHA-256 与耗时，不记录 proposal 正文。

## 决策覆盖

- D-01：`coding.py` 仅拼 prompt，无目标仓写盘代码；静态搜索无写盘运输标识。
- D-02/D-06：`render_repo_proposals_markdown` 复用完备四段投影。
- D-03/D-04：单仓 renderer 按 `repository_id` 隔离；coding fan-out 只覆盖实际执行仓。
- D-05：纯函数确定性投影，无 LLM。
- D-07：两处 `_dispatch_wave` 调用均传入 `plan_data`。
- D-08：`openspec_proposal_prompt_started/completed/failed/finished` 使用结构化字段与脱敏错误。

## 验证

- `uv run pytest tests/services/process_runtime/test_blueprint_proposal_render.py tests/test_coding_node.py tests/test_coding_wave.py -q`
  - 40 passed
- `uv run pytest tests/services/process_runtime/test_blueprint_render.py -q`
  - 39 passed
- `uv run ruff check ...`
  - All checks passed
- 静态守门：
  - `openspec/changes`
  - `_write_openspec_proposal`
  - `OPENSPEC_PROPOSAL_CHUNK`
  - `openspec_proposal_transport`
  - 均无命中

未创建 commit。
