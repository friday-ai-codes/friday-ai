---
quick_id: 260817-h88
phase: 260817-h88-openspec-proposal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/services/process_runtime/blueprint_proposal_render.py
  - server/services/process_runtime/blueprint_render.py
  - server/workflows/nodes/ai/coding.py
  - server/tests/services/process_runtime/test_blueprint_proposal_render.py
  - server/tests/test_coding_node.py
  - server/tests/test_coding_wave.py
autonomous: true
requirements:
  - OPENSPEC-DOC-01
  - OPENSPEC-PROMPT-01
  - OPENSPEC-PROMPT-02
  - OPENSPEC-DET-01
  - OPENSPEC-WAVE-01
  - OPENSPEC-OBS-01
user_setup: []
must_haves:
  truths:
    - "Friday 技术方案 markdown 含完备分仓 OpenSpec Proposal（Why / What Changes / Impact / Spec Deltas），间接仓也在主文档中"
    - "每个 direct 仓编码派发的 prompt 含且仅含该仓 proposal，标注为只读权威实现上下文"
    - "目标仓库工作树不落盘 proposal.md / openspec/changes/**；无新 LLM 调用"
    - "首发 wave 与后续 wave 共用同一注入路径；无蓝图 / 非 blueprint 路径零回归"
    - "结构化日志含 category/component/duration_ms/repository_id/bytes（或 hash），从不打印 proposal 全文"
  artifacts:
    - path: "server/services/process_runtime/blueprint_proposal_render.py"
      provides: "多仓章节 + 单仓完备 proposal 纯函数（无 LLM）"
      exports: ["render_repo_proposals_markdown", "render_single_repo_proposal_markdown"]
    - path: "server/workflows/nodes/ai/coding.py"
      provides: "_dispatch_wave / _run_repo_coding / _build_coding_prompt 注入本仓只读 proposal"
      contains: "repo_proposal|render_single_repo_proposal_markdown|openspec_proposal_prompt"
    - path: "server/tests/services/process_runtime/test_blueprint_proposal_render.py"
      provides: "完备字段 + 单仓提取 + 中央多仓回归"
    - path: "server/tests/test_coding_node.py"
      provides: "prompt 注入段 + 无蓝图零回归"
  key_links:
    - from: "blueprint_proposal_render.render_repo_proposals_markdown"
      to: "blueprint_render.render_blueprint_markdown"
      via: "主技术方案嵌入「分仓方案（OpenSpec Proposal）」章节"
      pattern: "render_repo_proposals_markdown"
    - from: "blueprint_proposal_render.render_single_repo_proposal_markdown"
      to: "AICodingNode._build_coding_prompt"
      via: "本仓只读权威段（非文件运输）"
      pattern: "OpenSpec Proposal|repo_proposal"
    - from: "plan_data.artifact_version_id"
      to: "ArtifactVersion.content"
      via: "_dispatch_wave 一次加载 blueprint/v1，逐仓投影"
      pattern: "artifact_version_id"
    - from: "derive_execution_plan"
      to: "coding fan-out"
      via: "仅有实现项的仓进 execution_plan；纯 indirect 无 dispatch（仍在主文档）"
      pattern: "derive_execution_plan"
---

<objective>
在保留已有未提交工作（中央分仓 OpenSpec 章节渲染 + 前端展示 + 测试）的基础上，把分仓 proposal 做到 blueprint/v1 完备投影，并在蓝图编码 fan-out 时把**本仓** proposal 作为只读权威实现上下文注入 coding prompt；**绝不**把 `proposal.md` 写入目标仓。

Purpose: 编码代理按仓拿到完整 Why/What/Impact/Spec Deltas，与 Friday 技术方案同源；间接仓只出现在主技术方案，不派发编码。
Output: 完备单仓/多仓纯函数 + coding 全 wave 注入 + 观测守门测试；**不 git commit**（父代理实现与验证，用户未要求 commit）。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md

⚠️ **git 纪律（NON-NEGOTIABLE）**
- 工作树已有大量无关在途改动（章程 append-only、frontend、其它测试、以及本功能已有的 `blueprint_proposal_render.py` 初版等）。**只改本计划 `files_modified` 列表内文件**；不得 revert/覆盖无关 hunk。
- **禁止 `git commit` / `git add` 整树提交**。实现与跑测即可；父代理收尾。
- 已有未提交文件 `blueprint_proposal_render.py` / `blueprint_render.py` 嵌入点 / `test_blueprint_proposal_render.py` / 前端展示是**起点**：扩展它们，不要重写中央多仓章节对外契约（仍以 `## 分仓方案（OpenSpec Proposal）` + `### 仓名（角色）` 呈现）。

⚠️ **与 260817-h0v 的边界（ANTI-GOAL）**
- `.planning/quick/260817-h0v-openspec-proposal-md/` 规划的是「写入目标仓 `openspec/changes/.../proposal.md` + chunked env 运输」。
- **本任务 h88 明确否定写盘**：禁止新建 `openspec_proposal_transport.py`、禁止 `task/core/runner.py` 写 `proposal.md`、禁止 `env_FRIDAY_TASK_OPENSPEC_PROPOSAL_*` 文件运输键。运输面 = coding **prompt 文本段** only。
</execution_context>

<context>
@.planning/STATE.md
@.cursor/rules/observability-logging.mdc
@.planning/observability/LOGGING-SPEC.md
@server/services/process_runtime/blueprint_proposal_render.py
@server/services/process_runtime/blueprint_render.py
@server/services/process_runtime/blueprint_execution.py
@server/services/process_runtime/artifact_injection.py
@server/workflows/nodes/ai/coding.py
@server/workflows/nodes/ai/plan_research.py
@server/tests/services/process_runtime/test_blueprint_proposal_render.py
@server/tests/helpers/blueprint_samples.py
@server/tests/test_coding_node.py
@server/tests/test_coding_wave.py

## Locked decisions (NON-NEGOTIABLE) — cite D-01…D-08 in actions

- **D-01** 禁止把 `proposal.md`（或等价 openspec change 文件）写入目标仓库工作树 / PR。
- **D-02** proposal 存在于 Friday 技术方案文档内，且按仓完备（中央 `render_repo_proposals_markdown` 路径保留并增强）。
- **D-03** 每个 **direct 编码 fan-out** 只把**该仓** proposal 注入其 coding prompt，作为**只读权威实现上下文**。
- **D-04** indirect 仓继续出现在主技术方案分仓章节；不因本任务新增编码派发（依赖既有 `derive_execution_plan`：无实现项则无 task）。
- **D-05** 从 `blueprint/v1` content **确定性**投影；**禁止**新 LLM 调用。
- **D-06** 保留 OpenSpec 四段：Why / What Changes / Impact / Spec Deltas；在 blueprint/v1 已有字段范围内补齐 scenario / acceptance / test / API / file / dependency 细节（不得编造蓝图没有的字段）。
- **D-07** 首发 wave 与后续 wave（`_dispatch_wave` / `_dispatch_next_wave`）必须都能注入。
- **D-08** 日志禁止 proposal 全文；生命周期事件带 `category` / `component` / `duration_ms` / 用户归因（能绑则绑 `initiated_by_user_id`，否则 `system`）与 `repository_id`。

## Claude's Discretion

- 单仓 prompt 段标题建议：`# 本仓 OpenSpec Proposal（只读权威实现上下文）`，正文复用四段加粗标签；并附一行固定说明「实现须与此一致；此上下文仅存在于 prompt，勿在仓内新建 openspec proposal 文件」。
- 蓝图加载优先：`plan_data["artifact_version_id"]` → `ArtifactVersion.content`；缺 id / 非 dict / 非 blueprint 形状 → skip 注入（零回归）。可选读取 `plan_data.get("blueprint_content")` 作内存旁路，但不得要求改 workflow 边线映射才能工作。
- What Changes 完备化：实现项除标题/变更类型/文件外，纳入完整 `how` 文本（可用 `_blocks_text`，勿再只取首行丢掉伪代码）、`depends_on`（列依赖 item id）、`test_strategy`、`existing_integration`（有则）。API 契约补 `description` / `data_source.fields_needed`（有则）。Spec Deltas 补功能点 `description` 摘要（有则）；acceptance / Scenario 路径已有则保留并测回归。
- 单仓 API 对 unknown / 缺 assoc 返回 `""`；indirect 仓**可以**渲染（供主文档），但 coding 注入只对当前 fan-out 的 `repo_ids`（天然多为有实现项的仓）调用。
- prompt 膨胀：单仓段建议硬上限（例如 50_000 字符）；超限截断尾部并打 `openspec_proposal_prompt_truncated` warning（kv 只含 bytes/limit），仍注入截断结果而非整段丢弃——编码不能完全失去方案。
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 完备分仓投影 + 单仓纯函数 API</name>
  <files>server/services/process_runtime/blueprint_proposal_render.py, server/services/process_runtime/blueprint_render.py, server/tests/services/process_runtime/test_blueprint_proposal_render.py</files>
  <behavior>
    - `render_repo_proposals_markdown(content)`：中央章节零回归（heading、四标签、indirect 占位、主渲染器嵌入）；What Changes / Spec Deltas 在样例蓝图上可见完整 how / files / depends_on / test_strategy / API description|fields / acceptance / Scenario（D-02/D-05/D-06）。
    - `render_single_repo_proposal_markdown(content, repository_id) -> str`：只含该仓四段；无 assoc / 空 id → `""`；不调用 LLM；不带多仓包装标题 `## 分仓方案（OpenSpec Proposal）`（D-03/D-05）。
    - 单仓正文与多仓中该仓块语义同源（同一 `_render_why` / `_render_what_changes` / `_render_impact` / `_render_spec_deltas` 路径），仅外层 heading/包装不同。
    - `__all__` 导出两个公共函数；模块仍无 IO / 无 ORM / 无 LLM。
  </behavior>
  <action>
  1. **保留并扩展**既有 `blueprint_proposal_render.py`（per D-05/D-06）：把 `_render_what_changes` 从「how 仅首行」升级为可用的完整 `_blocks_text(how)`（伪代码块一并进入文本）；对实现项追加 `depends_on`、`test_strategy`、`existing_integration`（有值才输出，缺料不留空标题刷屏）。API 行在现有 provided/consumed 基础上附加 description / fields_needed（有则）。`_render_spec_deltas` 在 ADDED/MODIFIED 行下增加功能点 description 摘要（有则），保留 SHALL / Scenario / GWT。
  2. 抽出并导出 `render_single_repo_proposal_markdown(content, repository_id)`：查找匹配 `repo_associations` 项 → `_render_repo_proposal` 主体；外层用适合 prompt 的单仓标题（如 `# Proposal: {repo_name}`），**不要**套 `## 分仓方案` 多仓包装。更新 `__all__`。
  3. 确认 `blueprint_render.py` 仍调用 `render_repo_proposals_markdown`（已有嵌入点则勿破坏顺序：仓库关联之后、现状分析之前）。
  4. 扩展 `test_blueprint_proposal_render.py`：完备字段断言（how 全文关键词、depends_on、test_strategy、API fields、Scenario）；单仓提取等于该仓内容且不含其它仓名块；缺 id 空串；中央多仓 + `render_blueprint_markdown` 嵌入回归保持绿。
  </action>
  <verify>
    <automated>cd /Users/zaneliu/Projects/open-source/friday-ai/server && uv run pytest tests/services/process_runtime/test_blueprint_proposal_render.py -q</automated>
  </verify>
  <done>
  多仓文档章节与单仓 API 完备可测且绿；无新 LLM；无目标仓写盘代码；无 git commit。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 全 wave coding prompt 注入本仓只读 proposal</name>
  <files>server/workflows/nodes/ai/coding.py, server/tests/test_coding_node.py, server/tests/test_coding_wave.py</files>
  <behavior>
    - `_dispatch_wave`（首发与后续 wave 共用）：若 `plan_data`/`调用方`可解析到 blueprint/v1 content，则对每个待派发 `repo_id` 调用 `render_single_repo_proposal_markdown`，经 `_run_repo_coding` → `_build_coding_prompt` 注入（D-03/D-07）。
    - 无 `artifact_version_id` / 加载失败 / 空正文 → 不注入段，prompt 与现行为一致（零回归）。
    - 注入段含「只读权威」语义；**不**写 metadata 文件运输键；**不**触碰 task runner 写盘（D-01）。
    - 日志：`openspec_proposal_prompt_prepared`（或等价）含 `repository_id`、`bytes`/`char_len`、可选 content hash、`duration_ms`、`category=caller`、`component=workflows`、`initiated_by_user_id`；失败 fail-soft 记 `…_failed`（`redact_secrets_in_text`）且不阻断 dispatch（D-08）。
    - 与 `follow_openspec` / GATE-01/02 正交：proposal prompt 注入不依赖 SDD approved。
  </behavior>
  <action>
  1. **加载蓝图（per D-05/D-07）**：在 `_execute_with_branch` / `_dispatch_next_wave` 把 `plan_data` 传入 `_dispatch_wave`（若尚未传入则扩展签名，默认 `None` 保兼容）。于 `_dispatch_wave` 内 best-effort 加载一次 content：优先 `ArtifactVersion.objects.filter(id=plan_data["artifact_version_id"]).afirst()` 的 `content`；若 `plan_data` 已带 `blueprint_content` dict 可作旁路。计时用 `time.perf_counter()`。
  2. **注入（per D-01/D-03）**：扩展 `_run_repo_coding` / `_build_coding_prompt` 增加可选 `repo_proposal: str = ""`。非空时在「项目背景」之后、「上游产物」之前或紧随项目背景插入本仓 OpenSpec 段（顺序写进测试，避免与 ARTIFACT-02 零回归冲突：无 proposal 时既有 upstream 测试仍绿）。段内不得要求 agent 落盘 openspec 文件。
  3. **观测（per D-08）**：成功/跳过/失败结构化事件；字段只用长度/hash/repo/change 元数据；**禁止** `proposal=` / `markdown=` 全文 kv。用户归因：能从 `dispatch_user` 取 id 则写入，否则 `system`。
  4. **测试**：
     - `test_coding_node.py`：有 blueprint content + repository_id → prompt 含本仓四标签与仓专属实现项，**不含**他仓实现项标题；默认参数路径与 Phase 44/45 baseline 逐字一致；断言 coding.py / 相关模块**无** `proposal.md` 写路径字符串（可用简单 grep 式断言或禁止调用不存在的 write helper）。
     - `test_coding_wave.py`（或同文件新用例）：模拟 `_dispatch_next_wave` / `_dispatch_wave` 在带 `artifact_version_id` 的 `plan_data` 下会把 proposal 传入（可用 mock `render_single_repo_proposal_markdown` 或 stub ArtifactVersion）——证明后续 wave 不丢注入。
  </action>
  <verify>
    <automated>cd /Users/zaneliu/Projects/open-source/friday-ai/server && uv run pytest tests/services/process_runtime/test_blueprint_proposal_render.py tests/test_coding_node.py tests/test_coding_wave.py -q</automated>
  </verify>
  <done>
  首发与后续 wave 的 direct 编码 prompt 含本仓只读 proposal；无目标仓写盘；观测无全文；相关测试绿；无 git commit。
  </done>
</task>

<task type="auto">
  <name>Task 3: 反写盘守门 + 决策覆盖自检（不提交）</name>
  <files>server/tests/services/process_runtime/test_blueprint_proposal_render.py, server/tests/test_coding_node.py</files>
  <action>
  1. 跑相交回归：proposal 渲染、coding prompt 基线、coding wave（若 Task 2 已扩）。
  2. 静态守门：`rg` 确认本任务触及文件中**不存在** `openspec/changes`、`_write_openspec_proposal`、`OPENSPEC_PROPOSAL_CHUNK`、`openspec_proposal_transport`（per D-01；与 h0v 划界）。
  3. 对照 D-01…D-08 写进执行 SUMMARY 的证据清单（由父代理在实现后写 `260817-h88-SUMMARY.md`）：每条一句（测试名或函数名）。
  4. `git status --short`：仅本计划文件 + 原已存在无关改动；**禁止 commit**。
  </action>
  <verify>
    <automated>cd /Users/zaneliu/Projects/open-source/friday-ai/server && uv run pytest tests/services/process_runtime/test_blueprint_proposal_render.py tests/test_coding_node.py tests/test_coding_wave.py -q && cd /Users/zaneliu/Projects/open-source/friday-ai && rg -n "openspec/changes|_write_openspec_proposal|OPENSPEC_PROPOSAL_CHUNK|openspec_proposal_transport" server/services/process_runtime/blueprint_proposal_render.py server/workflows/nodes/ai/coding.py || true && git status --short | head -80</automated>
  </verify>
  <done>
  测试绿；无目标仓写盘痕迹；决策可核对；工作树未意外整树暂存；无 commit。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Blueprint/v1 → proposal markdown | 半可信结构化字段投影为 prompt/文档文本 |
| Server → coding agent prompt | 本仓 proposal 进入容器 prompt；膨胀与注入风险 |
| Server logs | 观测面不得泄漏完整方案正文 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-h88-01 | Information Disclosure | coding/proposal logs | mitigate | 只记 bytes/hash/duration/repo；禁止 markdown 全文；错误走 `redact_secrets_in_text`（D-08） |
| T-h88-02 | Tampering / Prompt injection | blueprint → prompt | mitigate | 只投影结构化白名单字段；块文本经既有 `_blocks_text`；超限截断；标注「只读数据/权威方案」而非让 agent 执行蓝图内任意指令块 |
| T-h88-03 | Elevation / Scope creep | target repo filesystem | mitigate | 本任务不写 `proposal.md`；测试 + rg 守门禁止写盘路径（D-01） |
| T-h88-04 | Denial of Service | 超大 proposal prompt | mitigate | 单仓字符硬上限 + truncate 事件；fail-soft 不阻断 dispatch |
| T-h88-05 | Information Disclosure | cross-repo leakage | mitigate | 单仓 API + 测试断言他仓实现项标题不出现在本仓 prompt（D-03） |
| T-h88-SC | Tampering | npm/pip installs | accept | 本任务无新包依赖 |
</threat_model>

<verification>
- `test_blueprint_proposal_render`：中央章节 + 完备字段 + 单仓 API 全绿。
- `test_coding_node`：有 proposal 注入；无蓝图零回归；无他仓泄漏。
- `test_coding_wave`：后续 wave 仍注入。
- rg 无目标仓写盘 / chunked transport 痕迹。
- `git status` 证明未 commit；无关在途改动保留。
</verification>

<success_criteria>
1. D-01…D-08 均有实现与自动化证据。
2. Friday 技术方案可见完备分仓 OpenSpec；direct 编码 prompt 含本仓只读 proposal。
3. 目标仓无 proposal 落盘；无新 LLM；无 git commit（本任务）。
</success_criteria>

## Source Audit

| SOURCE | ID | Item | Plan | Status |
|--------|-----|------|------|--------|
| GOAL | — | 完备分仓 OpenSpec proposal + 只读注入编码 prompt，不落目标仓 | 01 | COVERED |
| REQ | OPENSPEC-DOC-01 | 技术方案内完备分仓 proposal | 01 T1 | COVERED |
| REQ | OPENSPEC-PROMPT-01 | 每仓 fan-out 只注入本仓 proposal | 01 T2 | COVERED |
| REQ | OPENSPEC-PROMPT-02 | 不写目标仓 proposal.md | 01 T2+T3 | COVERED |
| REQ | OPENSPEC-DET-01 | 确定性投影无新 LLM | 01 T1 | COVERED |
| REQ | OPENSPEC-WAVE-01 | 首发与后续 wave | 01 T2 | COVERED |
| REQ | OPENSPEC-OBS-01 | 结构化日志无全文 | 01 T2+T3 | COVERED |
| CONTEXT | D-01…D-08 | Locked decisions | 01 | COVERED |
| RESEARCH | — | 无独立 RESEARCH；沿用既有 render + coding wave 挂点 | 01 | COVERED |
| ANTI | h0v write-path | 写入 openspec/changes + env 分片 | — | EXCLUDED（明确否定） |

<output>
执行完成后由父代理创建 `.planning/quick/260817-h88-openspec-proposal/260817-h88-SUMMARY.md`（本规划任务不写 SUMMARY、不 commit）。
</output>
