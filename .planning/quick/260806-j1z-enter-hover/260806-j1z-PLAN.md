---
phase: quick-260806-j1z
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/components/blueprint/BlueprintCommentPopover.vue
  - web/src/components/blueprint/__tests__/commentPopover.spec.ts
  - web/src/components/blueprint/annotationTokens.ts
  - web/src/pages/knowledge/blueprints/[id].vue
  - web/src/locales/zh-CN.json
autonomous: true
requirements: [QUICK-260806-J1Z]

must_haves:
  truths:
    - "点击正文划线 ⇒ 划线旁就地浮出线程卡（消息流 + 作答/处置），Esc/点外关闭"
    - "选中文字点「发起评论」⇒ 选区下方就地浮出评论输入卡：引用条 + 输入框 + 发送；Enter 发送、Shift+Enter 换行"
    - "划线 hover 背景加深（transition），对齐飞书文档高亮手感"
    - "右侧侧栏与深链路径行为不回退（selectThread 同步、readonly 闸不变）"
  artifacts:
    - path: "web/src/components/blueprint/BlueprintCommentPopover.vue"
      provides: "文档坐标绝对定位浮层（Teleport body）：draft 模式评论输入卡 / thread 模式线程卡"
  key_links:
    - from: "[id].vue onThreadClick"
      to: "BlueprintCommentPopover"
      via: "mark[data-thread-id] 矩形 → thread 模式"
      pattern: "BlueprintCommentPopover"
---

<objective>
划线评论交互对齐飞书文档：划线点击就地看线程、选中就地写评论，不再强制跳右侧栏。
样式：白卡 + shadow-lg 浮层、引用条、Enter 发送；划线 hover 加深。
坐标经文档坐标绝对定位（Teleport body），随文档滚动。
</objective>

<context>
已核实：
- resolve/dismiss 后端仅对 ai_review_finding 开放 ⇒ 浮层内复用 BlueprintThreadCard 的 kind 硬分流，⛔ 不给评论加「解决」按钮。
- 页面已有 selection → BlueprintSelectionPopover → startDraft()（现草稿进侧栏）；onThreadClick → selectThread（现仅侧栏高亮）。
- annotationTokens 是划线视觉唯一来源；类名须为完整字面量（Tailwind 扫描）。
- blueprintViewer.spec 对新组件需 stub。
</context>

<tasks>

<task type="auto">
  <name>Task 1: BlueprintCommentPopover 组件 + hover 加深 + i18n</name>
  <action>
- 新建 BlueprintCommentPopover.vue：props rect（viewport DOMRect，打开时转文档坐标）、draft 模式（quotedText）或 thread 模式（thread + readonly/submitting/gateAvailable/featurePointTitles）。
  Teleport body + position:absolute；宽 360、左右夹取视口；点外(onClickOutside)与 Esc emit close；
  draft：引用条（border-l-2 warning 底纹）+ Textarea 自动聚焦 + Enter 发送/Shift+Enter 换行 + 取消/发送；
  thread：max-h-[65vh] overflow-auto 内嵌 BlueprintThreadCard（active），透传 answer/resolve/dismiss/goto-gate/goto-anchor。
- annotationTokens：MARK_SHAPE_CLASS 加 transition；HUE_CLASS open/answered 的 normal 档补 hover:bg 字面量（取 active 底纹值）。
- zh-CN.json：annotation.inlineComposer { placeholder/send/cancel } 键。
  </action>
</task>

<task type="auto">
  <name>Task 2: 页面接线 + 组件测试</name>
  <action>
- [id].vue：startDraft → inlineDraft（浮层输入卡，含键盘路径）；onThreadClick → selectThread + 定位 mark[data-thread-id]（找不到回退 revealAnnotations）→ inlineThread 浮层；提交复用 onCreateComment；close 清 activeThreadId。侧栏 draft 绑定摘除（组件与测试保留）。
- commentPopover.spec.ts：draft 渲染/空禁用/Enter 发送/Shift+Enter 不发/Esc cancel；thread 模式渲染线程卡且 answer 透传。
- blueprintViewer.spec：stub BlueprintCommentPopover。
  </action>
</task>

</tasks>

<verification>
- vitest（commentPopover + blueprintViewer + threadSidebar + BlueprintBlock）
- vue-tsc；Playwright 真机截图（划线点击浮卡、选区浮出输入卡）
</verification>
