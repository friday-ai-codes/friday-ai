---
milestone: v0.4.0
audited: 2026-06-13
status: tech_debt
auditor: orchestrator-inline
note: gsd-integration-checker 子代理受间歇性账单错误阻断，由 orchestrator 按 inline fallback 基于全量测试门禁 + SUMMARY 交付物做集成评估
scores:
  requirements: 24/24
  phases: 5/5
  integration: pass
  flows: pass
gaps: {}
tech_debt:
  - phase: 17-varref / 18-engine / 19-ssot / 20-validation / 21-observability
    items:
      - "人工验收项（human_needed）按既有惯例 deferred 至里程碑收尾：Phase 17 变量引用浏览器观感、Phase 18 真实容器回调 E2E、Phase 19 画布编辑观感、Phase 20 IssuesPanel 交互/模板端到端执行、Phase 21 真实飞书事件触发/WS 断线降级观感/suspended 显示"
  - phase: 18/19/20/21
    items:
      - "VALIDATION.md frontmatter nyquist_compliant 仍为 false（Wave 0 测试实际全绿，属文档收尾遗漏，非功能缺口）"
  - phase: 21-observability
    items:
      - "project_ids / exclude_* 触发过滤（负向 + 跨 ID 空间）按裁定 deferred v2（matches_event 需扩负向分支 + Space→project_key 映射）"
  - phase: 20-validation
    items:
      - "input.*/trigger.* 变量引用严格静态校验留 v2；IssuesPanel 点击告警画布居中留 TODO"
---

# Milestone v0.4.0 工作流系统契约重构 — Audit

## Definition of Done

收敛工作流系统的编辑态与运行态契约——保存即合法、模板开箱能跑、变量所选即所得、执行状态真实可见。

## Requirements Coverage (24/24 satisfied)

3-source 交叉核对（REQUIREMENTS.md 追踪表 = Complete · 各阶段 VERIFICATION.md · SUMMARY requirements-completed）：

| Phase | Requirements | VERIFICATION | 结论 |
|-------|--------------|--------------|------|
| 17 变量引用链路修复 | VAR-01..04 | human_needed（自动化通过，人工 deferred） | satisfied |
| 18 执行引擎状态机修复 | ENG-01..05 | passed | satisfied |
| 19 节点定义单一事实源 | SSOT-01..03 | passed | satisfied |
| 20 保存即合法与模板修复 | VAL-01..03, TPL-01..03 | passed | satisfied |
| 21 触发模型与执行可观测 | TRIG-01..03, OBS-01..03 | passed | satisfied |

无 unsatisfied、无 orphaned 需求。24/24 在追踪表标记 Complete。

## Phases (5/5 verified)

全部 5 个阶段有 VERIFICATION.md。Phase 18-21 status: passed；Phase 17 status: human_needed（自动化全过，人工浏览器观感项 deferred，里程碑收尾人工验收）。

## Cross-Phase Integration (pass)

集成由序贯依赖设计 + 共享契约 + 全量测试门禁佐证（非凭声明）：

- **routing 纯函数（18）** 被主循环与回调续跑共用（消除两套路由漂移）；trigger_data 注入（18）端到端可解析。
- **node-types API + NodePort（19）** 驱动前端 palette/表单/画布 Handle（删除硬编码 NODE_REGISTRY）。
- **WorkflowGraphValidator（20）** 被 bulk-update / 单节点边 CRUD / import / template loader / dry-run 共用（单一校验事实源）；复用 17 的 template_resolver + 19 的 registry/NodePort。
- **触发与可观测（21）** 复用 18 的 suspended/死锁语义 + 17 的结构化错误，前端状态枚举对齐后端 ExecutionStatus。
- **测试门禁**：后端 `tests/workflows/ + tests/test_trigger*.py` **497 passed**；前端 `pnpm -C web test:unit` **1017 passed**；`type-check` exit 0 —— 跨切面集成（共享模块、API 契约、状态枚举）由这些套件覆盖。

## E2E Flows (pass)

- 保存非法工作流 → 结构化拒绝（VAL）；合法保存不受影响。
- 模板创建 → loader 校验 → 执行（TPL + VAL + ENG）；daily_summary 自足、code_review_pipeline 契约对齐（webhook payload 前提文档化）。
- 飞书事件 → WorkflowTrigger 匹配 → 执行 → 前端如实展示状态/失败（TRIG + ENG + OBS）。

## Nyquist Coverage (PARTIAL — 文档收尾)

| Phase | VALIDATION.md | nyquist_compliant 标记 | Wave 0 测试 | 备注 |
|-------|---------------|------------------------|-------------|------|
| 18/19/20/21 | exists | false（未翻转） | 全绿 | 测试实际通过，frontmatter 未收尾，属文档 hygiene |

非功能缺口——各阶段 Wave 0 测试在执行中已全绿，仅 VALIDATION.md frontmatter 未回写 true。

## Status: tech_debt

所有 24 需求满足、无关键阻断；存在累积的 deferred 项（人工验收、nyquist frontmatter 收尾、v2 范围延迟）。按自主模式裁定：**接受 tech debt，继续完成里程碑**（deferred 项纳入 STATE.md Deferred Items 跟踪）。

## Decision

Audit ⏭ Tech debt acknowledged — proceeding to complete milestone（CTRL-01 自主路径，用户指令"不中断、用 smart 结果"）。
