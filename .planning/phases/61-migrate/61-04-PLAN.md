---
phase: 61-migrate
plan: 04
type: execute
wave: 3
depends_on: ["61-01", "61-02"]
files_modified:
  - server/resumable/models.py
  - server/resumable/migrations/0002_resumable_migrated.py
  - server/durable/management/commands/migrate_resumable_to_durable.py
  - server/services/background_runner.py
  - server/tests/durable/test_migrate_command.py
autonomous: true
requirements: [MIGRATE-02]
must_haves:
  truths:
    - "一次性 management command `migrate_resumable_to_durable` 扫 PENDING/RUNNING 的 index/graph resumable_tasks，按 deterministic key defer durable job，旧行标 MIGRATED 记 legacy_durable_job_id（不双跑）（SC2）"
    - "命令幂等可重入：重跑不产生重复 durable job（deterministic key 去重）、不重处理已 MIGRATED 行（状态过滤）（SC2）"
    - "命令 SQLite/非 durable 后端下安全降级：显式中文提示不静默'迁移'成进程内任务，不报错崩溃（SC2）"
    - "ResumableTask 新增 MIGRATED 状态枚举 + legacy_durable_job_id 列（migration 0002），MIGRATED 行被 recoverable_target_ids 天然排除（不再被 recovery/reconcile 重驱）（SC2）"
    - "background_runner docstring/注释明确降级为仅 SQLite dev fallback / 轻任务，生产 index/graph 不再经它（SC3）"
  artifacts:
    - path: "server/resumable/models.py"
      provides: "ResumableTaskStatus.MIGRATED 枚举 + ResumableTask.legacy_durable_job_id 列"
    - path: "server/resumable/migrations/0002_resumable_migrated.py"
      provides: "AddField legacy_durable_job_id + status choices 扩展"
    - path: "server/durable/management/commands/migrate_resumable_to_durable.py"
      provides: "一次性迁移命令（--dry-run、幂等、SQLite 降级提示）"
    - path: "server/services/background_runner.py"
      provides: "降级注释（dev fallback / 轻任务定位）"
    - path: "server/tests/durable/test_migrate_command.py"
      provides: "迁移/幂等可重入/SQLite 降级/不双跑 守护"
  key_links:
    - from: "server/durable/management/commands/migrate_resumable_to_durable.py"
      to: "durable.DurableTaskService"
      via: "async_to_sync(DurableTaskService.defer) deterministic key"
      pattern: "DurableTaskService.defer"
    - from: "server/durable/management/commands/migrate_resumable_to_durable.py"
      to: "server/resumable/models.py"
      via: "旧行 .update(status=MIGRATED, legacy_durable_job_id=job_id)"
      pattern: "ResumableTaskStatus.MIGRATED"
---

<objective>
新增 `ResumableTask` 的 `MIGRATED` 状态枚举与 `legacy_durable_job_id` 列（migration 0002），实现一次性迁移命令 `migrate_resumable_to_durable`，把存量 PENDING/RUNNING 的 index/graph `resumable_tasks` 按 deterministic key defer 成 durable job、旧行标 MIGRATED 记 legacy id（**不双跑**、幂等可重入、SQLite 安全降级），并把 `background_runner` 注释降级为 dev fallback / 轻任务定位。

Purpose: MIGRATE-02 的"一次性迁移 + 三套收口"。升级时存量在途行必须平滑转入 durable，绝不双跑（旧行标 MIGRATED 后被 recoverable_target_ids 天然排除），绝不在 SQLite dev 误"迁移"成进程内任务（重启即丢）。
Output: resumable 模型/迁移 + 一次性命令 + background_runner 降级注释 + 守护测试。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-migrate/61-RESEARCH.md
@.planning/phases/61-migrate/61-CONTEXT.md
@.planning/phases/61-migrate/61-01-SUMMARY.md
@.planning/phases/61-migrate/61-02-SUMMARY.md

@server/durable/service.py
@server/durable/queues.py
@server/resumable/models.py
@server/resumable/recovery.py
@server/resumable/management/commands/recover_tasks.py
@server/services/background_runner.py

<tasks>

<task type="auto">
  <name>Task 1: ResumableTask MIGRATED 状态 + legacy_durable_job_id 列 + migration 0002</name>
  <files>server/resumable/models.py, server/resumable/migrations/0002_resumable_migrated.py</files>
  <read_first>
    - server/resumable/models.py（`ResumableTaskStatus` TextChoices、`ResumableTask` 字段 + `db_table="resumable_tasks"`）
    - server/resumable/migrations/0001_initial.py（迁移依赖链格式 + 字段定义范式）
    - server/resumable/recovery.py:94 `recoverable_target_ids`（仅返回 status=RUNNING → MIGRATED 行天然排除）
  </read_first>
  <action>
    在 `server/resumable/models.py`：
    - `ResumableTaskStatus` 新增 `MIGRATED = "migrated", "已迁移至 durable"`（区分"用户取消 CANCELLED" vs "系统迁移 MIGRATED"，per RESEARCH A3）。
    - `ResumableTask` 新增字段 `legacy_durable_job_id = models.CharField(max_length=200, blank=True, default="")`（记迁移产生的 durable job 标识，可观测/排障）。中文 docstring 行内注释。
    生成 migration：`cd server && uv run python manage.py makemigrations resumable`（自动产 `0002_*.py`；若文件名非 `0002_resumable_migrated.py` 由执行者重命名 + 修正 migration 内 `name=` 或直接采用自动名并更新本 plan frontmatter 记录）。migration 仅 AddField（status 是 CharField 无 DB 约束，choices 改不强制 migration，但 makemigrations 会生成 AlterField，接受）。
    `cd server && uv run python manage.py makemigrations --check --dry-run` 确认无遗漏。
  </action>
  <acceptance_criteria>
    - `ResumableTaskStatus.MIGRATED` 存在；`ResumableTask.legacy_durable_job_id` 字段存在（CharField，blank，default=""）。
    - `server/resumable/migrations/0002_*.py` 存在，依赖 0001。
    - `cd server && uv run python manage.py makemigrations --check --dry-run` 干净（无未生成迁移）。
    - `cd server && uv run python manage.py migrate resumable` 成功（SQLite）。
  </acceptance_criteria>
  <verify>
    <automated>cd server && uv run python manage.py makemigrations --check --dry-run && uv run python manage.py migrate resumable</automated>
  </verify>
  <done>MIGRATED 枚举 + legacy_durable_job_id 列 + migration 0002 就位，makemigrations --check 干净。</done>
</task>

<task type="auto">
  <name>Task 2: 一次性迁移命令 migrate_resumable_to_durable</name>
  <files>server/durable/management/commands/migrate_resumable_to_durable.py</files>
  <read_first>
    - server/resumable/management/commands/recover_tasks.py（management command + async_to_sync 范式）
    - server/durable/service.py（`DurableTaskService.defer` async + `use_procrastinate_backend()`）+ server/durable/queues.py（QUEUE_INDEX/QUEUE_GRAPH）
    - server/resumable/models.py（ResumableTask kind/status/payload/legacy_durable_job_id）
    - .planning/phases/61-migrate/61-01-SUMMARY.md（任务名 durable_index/durable_graph + payload 契约）
  </read_first>
  <action>
    新建 `server/durable/management/commands/migrate_resumable_to_durable.py`（`durable/management/` 已在 no-direct-import allowlist，但本命令经 `DurableTaskService` 门面即可，无需直接 import procrastinate）。`Command(BaseCommand)`：
    - `add_arguments`: `--dry-run`（action=store_true，仅打印不写）。
    - `handle`:
      1. **判后端**：`from durable.service import use_procrastinate_backend`；若 False（SQLite/非 durable）→ `self.stdout.write` 清晰中文提示（"当前为非 durable 后端（SQLite/in-process），存量行迁移意义有限：迁移出的任务为进程内、重启即丢。已跳过 defer。如需真实迁移请在 Postgres + DURABLE_TASK_BACKEND=procrastinate 下重跑"），不报错崩溃，按 dry-run 语义只统计不 defer（或直接返回 skipped 统计）。
      2. **扫描**：`ResumableTask.objects.filter(kind__in=[INDEX, GRAPH], status__in=[PENDING, RUNNING])`（只处理 index/graph + 在途态；MIGRATED/CANCELLED/COMPLETED 天然排除 → 幂等可重入）。
      3. **逐行迁移**：按 kind 选 task 名（index→"durable_index"+QUEUE_INDEX、graph→"durable_graph"+QUEUE_GRAPH）+ deterministic key（`index:{target_id}` / `graph:{target_id}`）；payload 从 `task.payload` 重建并补齐 history_id（无则 None）。`job_id = async_to_sync(DurableTaskService.defer)(task_name, payload, queue=queue, idempotency_key=key)`。非 dry-run 时 `ResumableTask.objects.filter(id=task.id, status__in=[PENDING,RUNNING]).update(status=MIGRATED, legacy_durable_job_id=str(job_id))`（条件 update 防并发重复处理）。
      4. **打印汇总** `{scanned, migrated, skipped, dry_run, backend}`。
    中文 docstring + help 文案。ruff line 100。
  </action>
  <acceptance_criteria>
    - 命令存在，`cd server && uv run python manage.py migrate_resumable_to_durable --dry-run` 在 SQLite 下不崩溃且打印非 durable 提示。
    - 命令只处理 `kind∈{index,graph}` 且 `status∈{pending,running}` 行；迁移后旧行 `status=migrated` 且 `legacy_durable_job_id` 非空。
    - deterministic key 为 `index:{target_id}` / `graph:{target_id}`。
    - 重复执行不重处理已 MIGRATED 行（状态过滤）。
  </acceptance_criteria>
  <verify>
    <automated>cd server && uv run python manage.py migrate_resumable_to_durable --dry-run && uv run pytest tests/durable/test_migrate_command.py -q</automated>
  </verify>
  <done>一次性迁移命令就位：deterministic key defer + 标 MIGRATED 记 legacy id + SQLite 安全降级。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: background_runner 降级注释 + 迁移命令守护测试</name>
  <files>server/services/background_runner.py, server/tests/durable/test_migrate_command.py</files>
  <read_first>
    - server/services/background_runner.py（模块 docstring + run_in_background 注释现状）
    - server/tests/durable/conftest.py（autouse reset、postgres_queue/procrastinate_app、durable_service fixtures）
    - server/resumable/models.py（ResumableTask seed 范式）
  </read_first>
  <behavior>
    - migrates：seed PENDING index + RUNNING graph ResumableTask → 跑命令（postgres_queue 标记，真实 durable）→ 旧行 status=MIGRATED + legacy_durable_job_id 非空 + durable job 已 defer（按 key）。
    - idempotent：连续跑两次命令 → 第二次 migrated=0、不产生重复 durable job（deterministic key 命中既有）、已 MIGRATED 行不再进扫描集。
    - sqlite_safe：SQLite 默认路径跑命令 → 不抛、打印非 durable 提示、不静默把行标 MIGRATED（或按降级策略 skipped 统计），断言无崩溃。
    - no_double_run：迁移标 MIGRATED 后 `recoverable_target_ids(INDEX/GRAPH)` 不含该 target_id（天然排除，recovery/reconcile 不再驱动）。
  </behavior>
  <action>
    在 `server/services/background_runner.py` 模块 docstring / `run_in_background` 注释补一段中文：明确 background_runner 自本阶段起**降级为仅 SQLite dev fallback / 少量非持久轻任务**——生产 index/graph 改走 durable（`DurableTaskService` → Postgres worker），不再经 background_runner/ResumableTask，避免三套并存。**仅注释/docstring 变更，不动运行逻辑**（in-process 后端仍复用它，零回归）。
    新建 `server/tests/durable/test_migrate_command.py`：用 `call_command("migrate_resumable_to_durable")` 驱动。`migrates`/`idempotent` 用例加 `@pytest.mark.postgres_queue` + `enable_socket`（需真实 durable 才能断言 defer + key 去重）；`sqlite_safe` 用例默认 SQLite 路径断言不崩溃 + 提示文案；`no_double_run` 默认 SQLite 路径 seed + 直接把行标 MIGRATED 后断言 `recoverable_target_ids` 排除（不依赖真实 durable）。中文注释标注每用例对应 Pitfall（双跑/SQLite 误迁/幂等）。
  </action>
  <acceptance_criteria>
    - `background_runner.py` docstring/注释含"降级为 dev fallback / 轻任务、生产 index/graph 不再经它"表述；运行逻辑无改动（既有 background_runner 测试零回归）。
    - `cd server && uv run pytest tests/durable/test_migrate_command.py -q` 默认 SQLite 路径绿（sqlite_safe + no_double_run，postgres 专项 skip）。
    - no_double_run 断言 MIGRATED 行被 `recoverable_target_ids` 排除。
  </acceptance_criteria>
  <verify>
    <automated>cd server && uv run pytest tests/durable/test_migrate_command.py -q && rg -c "dev fallback|轻任务" services/background_runner.py</automated>
  </verify>
  <done>background_runner 降级注释 + 迁移命令幂等/不双跑/SQLite 降级守护测试全绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 运维/CLI（migrate role）→ 一次性迁移命令 | 命令仅经 CLI 执行、不暴露 REST；defer 经 `DurableTaskService` 门面 |
| 存量 resumable_tasks 在途行 → durable 队列 | deterministic key 去重 + 旧行标 MIGRATED 排除出 recovery 集（不双跑） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-11 | Tampering | 双跑（迁移 + recovery 同驱同 repo） | mitigate | deterministic key 去重 + 旧行标 MIGRATED → recoverable_target_ids 天然排除（no_double_run 守护） |
| T-61-12 | Repudiation | SQLite 误"迁移"成进程内任务（重启即丢） | mitigate | 命令判 `use_procrastinate_backend()`，非 Postgres 清晰中文提示不静默（sqlite_safe 守护） |
| T-61-13 | Tampering | 命令重跑产生重复 durable job | mitigate | 状态过滤（只扫 PENDING/RUNNING）+ deterministic key 命中既有 job（idempotent 守护） |
| T-61-14 | Information Disclosure | 迁移 payload/legacy id 入日志 | accept | 仅内部 UUID 串/job 标识，无凭证；低风险 |
</threat_model>

<verification>
- `cd server && uv run pytest tests/durable tests/resumable -q` 全绿（含新 test_migrate_command + 既有 resumable 零回归）。
- `cd server && uv run python manage.py makemigrations --check --dry-run` 干净。
- `cd server && uv run python manage.py migrate_resumable_to_durable --dry-run` SQLite 下不崩溃。
- `rg -n "dev fallback|轻任务" server/services/background_runner.py` 命中（降级注释存在）。
</verification>

<success_criteria>
- 一次性迁移命令把存量在途行 defer durable + 标 MIGRATED 记 legacy id，不双跑、幂等可重入、SQLite 安全降级（SC2）。
- background_runner 降级为 dev fallback / 轻任务，生产 index/graph 不再经它（SC3）。
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 04)
- `server/resumable/models.py`（改）—— MIGRATED 枚举 + legacy_durable_job_id 列
- `server/resumable/migrations/0002_resumable_migrated.py`（新）—— AddField + status choices
- `server/durable/management/commands/migrate_resumable_to_durable.py`（新）—— 一次性迁移命令
- `server/services/background_runner.py`（改）—— 降级注释
- `server/tests/durable/test_migrate_command.py`（新）—— 迁移/幂等/不双跑/SQLite 降级守护
</artifacts_produced>

<output>
Create `.planning/phases/61-migrate/61-04-SUMMARY.md` when done
</output>
