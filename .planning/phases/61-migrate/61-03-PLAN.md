---
phase: 61-migrate
plan: 03
type: execute
wave: 2
depends_on: ["61-01"]
files_modified:
  - server/resumable/models.py
  - server/resumable/migrations/0002_migrated_status_legacy_job_id.py
  - server/durable/reconcile.py
  - server/durable/management/commands/migrate_resumable_to_durable.py
  - server/repositories/apps.py
  - server/codegraph/apps.py
  - server/services/background_runner.py
  - server/tests/durable/test_migrate_command.py
  - server/tests/repositories/test_reconcile_durable.py
autonomous: true
requirements: [MIGRATE-02]
must_haves:
  truths:
    - "一次性 management command migrate_resumable_to_durable 把存量 PENDING/RUNNING 的 index/graph resumable_tasks 按 deterministic key defer 成 durable job，旧行标 MIGRATED 并记 legacy_durable_job_id —— 不双跑"
    - "命令幂等可重入：重跑不产生重复 durable job（deterministic key + 旧行已 MIGRATED 排除出扫描集），SQLite/无 durable 后端安全降级给提示不崩"
    - "启动 reconcile（repositories.apps / codegraph.apps）改为：仅确认无在途 durable job 接管时才把 RUNNING 标 FAILED——经 durable.reconcile.has_active_durable_job_sync(index:/graph:{repo_id})，非 durable 后端(SQLite)维持旧标 FAILED 行为"
    - "ResumableTask 新增 MIGRATED 状态枚举 + legacy_durable_job_id 列（migration 0002）；MIGRATED 行天然排除出 recoverable_target_ids（仅返回 RUNNING）"
    - "background_runner 降级为 SQLite dev fallback / 轻任务定位（docstring 收口），生产 index/graph 不再三套并存"
  artifacts:
    - path: "server/durable/management/commands/migrate_resumable_to_durable.py"
      provides: "一次性迁移命令（--dry-run，幂等可重入，SQLite 安全降级）"
      contains: "class Command"
    - path: "server/durable/reconcile.py"
      provides: "has_active_durable_job_sync / async 判定 helper（非 procrastinate 返回 False）"
      contains: "def has_active_durable_job_sync"
    - path: "server/resumable/models.py"
      provides: "ResumableTaskStatus.MIGRATED + ResumableTask.legacy_durable_job_id"
      contains: "MIGRATED"
    - path: "server/resumable/migrations/0002_migrated_status_legacy_job_id.py"
      provides: "legacy_durable_job_id 列 + MIGRATED choices 迁移"
      contains: "legacy_durable_job_id"
  key_links:
    - from: "server/repositories/apps.py"
      to: "server/durable/reconcile.py"
      via: "_reset_stuck_indexing 标 FAILED 前查 has_active_durable_job_sync(index:{repo_id})"
      pattern: "has_active_durable_job_sync"
    - from: "server/codegraph/apps.py"
      to: "server/durable/reconcile.py"
      via: "reconcile_orphaned_graph_builds 标 FAILED 前查 has_active_durable_job_sync(graph:{repo_id})"
      pattern: "has_active_durable_job_sync"
    - from: "server/durable/management/commands/migrate_resumable_to_durable.py"
      to: "durable.DurableTaskService.defer"
      via: "async_to_sync defer + 旧行标 MIGRATED 记 legacy id"
      pattern: "ResumableTaskStatus.MIGRATED"
---

<objective>
完成"收口 ResumableTask"的运维/安全语义半（MIGRATE-02）：一次性迁移命令把存量在途行平滑转入 durable 队列（不双跑），启动 reconcile 改为"仅无 durable 接管才标 FAILED"（不误杀在途），ResumableTask 加 MIGRATED 状态 + legacy id 列（可观测），background_runner 降级定位收口。

Purpose: 入队迁移（61-02）后生产不再产生新 index/graph ResumableTask 行，但升级窗口内仍有存量在途行；必须一次性迁移 + reconcile 改判定双保险，确保升级"不双跑、不误杀"（61-RESEARCH §Pattern 3/4、Pitfall 2/3、Runtime State Inventory）。
Output: resumable 模型迁移 + reconcile helper + 迁移命令 + 两处 reconcile 改判定 + background_runner docstring 收口 + 迁移/reconcile 守护测试。

实现 MIGRATE-02 全部三件：一次性迁移命令、reconcile 安全语义、background_runner 降级。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-migrate/61-RESEARCH.md
@.planning/phases/61-migrate/61-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ResumableTask 加 MIGRATED 状态 + legacy_durable_job_id 列（migration 0002）</name>
  <files>server/resumable/models.py, server/resumable/migrations/0002_migrated_status_legacy_job_id.py</files>
  <read_first>
    - server/resumable/models.py 全文（ResumableTaskStatus TextChoices:28 / ResumableTask 字段 + Meta db_table="resumable_tasks":68）
    - server/resumable/migrations/（既有 migration 命名/依赖范式；查最新一条作为 0002 的 dependencies）
    - 61-RESEARCH §Standard Stack Alternatives（建议加列 + 加 MIGRATED 枚举，A3）
  </read_first>
  <action>
    在 `server/resumable/models.py`：
    - `ResumableTaskStatus` 新增 `MIGRATED = "migrated", "已迁移到 durable"`（区分"用户取消 CANCELLED" vs "系统迁移"，审计/排障可分辨）。
    - `ResumableTask` 新增 `legacy_durable_job_id = models.CharField(max_length=200, blank=True, default="")`（迁移后记对应 durable job 标识，可观测、可查）。
    用 `python manage.py makemigrations resumable` 生成 `0002_migrated_status_legacy_job_id.py`（AddField legacy_durable_job_id + AlterField status choices）；dependencies 指向 resumable 最新一条 migration。choices 改不强制 DB 约束变更（CharField），AddField 需 default 无回填。
  </action>
  <verify>
    <automated>cd server && uv run python manage.py makemigrations resumable --check --dry-run && uv run pytest tests/resumable -q</automated>
  </verify>
  <done>ResumableTaskStatus.MIGRATED + legacy_durable_job_id 列就位，migration 0002 干净。</done>
  <acceptance_criteria>
    - `ResumableTaskStatus.MIGRATED` 存在；`ResumableTask.legacy_durable_job_id` 字段存在（CharField, blank, default="")。
    - migration 0002 存在且 `makemigrations --check` 干净（无未生成迁移）。
    - 既有 resumable 测试不回归。
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: 一次性迁移命令 migrate_resumable_to_durable（不双跑 + 幂等可重入 + SQLite 安全降级）</name>
  <files>server/durable/management/commands/migrate_resumable_to_durable.py, server/tests/durable/test_migrate_command.py</files>
  <read_first>
    - server/resumable/management/commands/recover_tasks.py（management command 范式：handle + stdout 汇总）
    - server/resumable/models.py（ResumableTask / ResumableTaskKind index,graph / ResumableTaskStatus PENDING,RUNNING,MIGRATED）
    - server/durable/service.py（DurableTaskService.defer 签名 + use_procrastinate_backend()）
    - server/durable/queues.py（QUEUE_INDEX/QUEUE_GRAPH）
    - 61-RESEARCH §Pattern 4（命令骨架 + 幂等可重入设计 + 不双跑保障）+ §Pitfall 3（SQLite 误迁）
  </read_first>
  <action>
    新建 `server/durable/management/commands/migrate_resumable_to_durable.py`（建议以 `migrate` 角色运行）：
    - `add_arguments`：`--dry-run`（只扫描打印不写）。
    - `handle`：
      1. 判后端 `use_procrastinate_backend()`——非 Postgres(SQLite/无 DATABASE_URL)：打印清晰中文提示（"当前为非 durable 后端，存量行不转 durable（in-process 重启即丢）；跳过"），**不报错崩溃**直接返回（CONTEXT 锁定 SQLite 安全降级）。
      2. 扫 `ResumableTask.objects.filter(kind__in=[INDEX,GRAPH], status__in=[PENDING,RUNNING])`。
      3. 逐行：按 kind 取 `task="durable_index"/"durable_graph"`、`queue=QUEUE_INDEX/QUEUE_GRAPH`、`idempotency_key=f"{kind}:{target_id}"`，payload 从 `task.payload` 重建（{repository_id, branch, trigger}，缺失用 target_id 兜底 repository_id）；`async_to_sync(DurableTaskService.defer)(...)` 取 job_id。
      4. 旧行 `.update(status=MIGRATED, legacy_durable_job_id=job_id)`（dry-run 则只计数不写）。
      5. 打印 `{scanned, migrated, skipped, dry_run, backend}` 汇总。
    幂等可重入：deterministic key 命中 AlreadyEnqueued 自动去重（不产生重复 durable job）；重跑时已 MIGRATED 行不在扫描集（status filter）→ 不重处理。不双跑：MIGRATED 行排除出 `recoverable_target_ids`（其只返回 RUNNING）。
    新建 `server/tests/durable/test_migrate_command.py`（SQLite + monkeypatch use_procrastinate_backend / DurableTaskService.defer）：
    - `test_migrates_pending_running_marks_migrated`：建 PENDING/RUNNING index/graph ResumableTask，mock defer 返 job_id，跑命令断言旧行 status==MIGRATED 且 legacy_durable_job_id 写入、defer 以正确 task/queue/key 调。
    - `test_idempotent_rerun`：连跑两次，断言第二次扫描集为空（已 MIGRATED）、defer 不被二次调用同一行。
    - `test_sqlite_safe_degrade`：mock use_procrastinate_backend→False，断言命令打印提示、不抛、不改任何行。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/durable/test_migrate_command.py -q</automated>
  </verify>
  <done>migrate_resumable_to_durable 命令：迁移+标 MIGRATED+记 legacy id、幂等可重入、SQLite 安全降级，三测绿。</done>
  <acceptance_criteria>
    - 命令存在，`--dry-run` 只计数不写。
    - 存量 PENDING/RUNNING index/graph 行被 defer + 标 MIGRATED + 记 legacy_durable_job_id。
    - 重跑幂等（已 MIGRATED 行不再处理，无重复 defer）。
    - 非 Postgres 后端打印提示、不崩、不改行。
    - 三个测试通过。
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: reconcile "无 durable 接管才标 FAILED" + background_runner 降级收口</name>
  <files>server/durable/reconcile.py, server/repositories/apps.py, server/codegraph/apps.py, server/services/background_runner.py, server/tests/repositories/test_reconcile_durable.py</files>
  <read_first>
    - server/repositories/apps.py:33-91（_reset_stuck_indexing：现排除 recoverable_target_ids(INDEX) 的 RUNNING 行后标 FAILED）
    - server/codegraph/apps.py:10-95（reconcile_orphaned_graph_builds：现排除 recoverable_target_ids(GRAPH)）
    - server/durable/service.py（has_active_durable_job 由 61-01 提供 + use_procrastinate_backend）
    - server/services/background_runner.py（模块 docstring / run_in_background：现"生产 index/graph 驱动器"定位需收口为 dev fallback/轻任务）
    - 61-RESEARCH §Pattern 3（reconcile 改判定 + 同步入口 async_to_sync + 非 durable 维持旧行为 A2）+ Pitfall 2
  </read_first>
  <action>
    新建 `server/durable/reconcile.py`：`has_active_durable_job_sync(idempotency_key: str) -> bool`——`if not use_procrastinate_backend(): return False`（非 durable/SQLite 维持旧"标 FAILED"行为，A2，避免僵尸 RUNNING 永不收尾）；否则 `async_to_sync(DurableTaskService.has_active_durable_job)(idempotency_key)`。同步入口对齐 reconcile 跑在 AppConfig.ready daemon 线程的同步上下文（Pitfall 2）。可附 async 版 `has_active_durable_job(key)` 直委托。
    `server/repositories/apps.py._reset_stuck_indexing`：在标 RUNNING→FAILED 前，对每个候选 repo 调 `has_active_durable_job_sync(f"index:{repo_id}")`，为真则排除（保留 RUNNING，不误杀）。实现上：保留既有 `recoverable_target_ids(INDEX)` 排除（过渡兼容），叠加 durable 在途排除——把 Repository.INDEXING 候选与 IndexHistory RUNNING 候选先取 repo_id 集，逐一 `has_active_durable_job_sync` 过滤后再 update FAILED；durable 查询整段 try/except fail-soft（异常退化为旧行为，不阻断启动）。
    `server/codegraph/apps.py.reconcile_orphaned_graph_builds`：同样在 `orphan_qs.update(FAILED)` 前，对 repo_ids 逐一 `has_active_durable_job_sync(f"graph:{repo_id}")` 过滤掉在途 durable job 的 repo（保留 RUNNING）；保留既有 recoverable_target_ids(GRAPH) 排除与 timeout cutoff。
    `server/services/background_runner.py`：更新模块 docstring 明确"降级为 SQLite dev fallback / 少量非持久轻任务；生产 index/graph 已迁 durable（Phase 61），不再经此三套并存"（仅文档收口，run_in_background 仍是 InProcessBackend 的执行器，行为不变）。
    新建 `server/tests/repositories/test_reconcile_durable.py`：
    - `test_reconcile_preserves_running_with_active_durable_job`：mock `use_procrastinate_backend`→True + `DurableTaskService.has_active_durable_job`→True（某 repo），建 INDEXING Repository + RUNNING IndexHistory，跑 `_reset_stuck_indexing`，断言该 repo 仍 RUNNING（未标 FAILED）。
    - `test_reconcile_marks_failed_without_durable_job`：has_active_durable_job→False，断言标 FAILED（旧行为保留）。
    - `test_reconcile_sqlite_maintains_failed`：use_procrastinate_backend→False，断言走旧标 FAILED 路径（has_active_durable_job_sync 短路 False）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/repositories/test_reconcile_durable.py tests/codegraph -q -k "reconcile or orphan"</automated>
  </verify>
  <done>reconcile 改"无 durable 接管才标 FAILED"（不误杀/SQLite 旧行为）、background_runner 降级 docstring 收口，reconcile 守护测试绿。</done>
  <acceptance_criteria>
    - `durable/reconcile.py` 的 `has_active_durable_job_sync` 非 procrastinate 后端返回 False；procrastinate 后端经 async_to_sync 委托 has_active_durable_job。
    - repositories.apps / codegraph.apps 在标 FAILED 前查 durable 在途、为真则保留 RUNNING（不误杀），durable 查询 fail-soft。
    - background_runner 模块 docstring 反映"dev fallback / 轻任务"降级定位。
    - 三个 reconcile 测试通过（保留 RUNNING / 标 FAILED / SQLite 旧行为）；既有 codegraph orphan reconcile 测试不回归。
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 升级运维(migrate role) → 存量数据迁移 | 一次性命令改写 resumable_tasks 状态，须不双跑/不误删 |
| 启动 reconcile(web role, daemon 线程) → durable job 查询 | 同步上下文调异步查询；误判会误杀在途任务 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-09 | Tampering | 双跑（迁移 + recovery 同时驱动同 repo 产重复数据） | mitigate | deterministic key 去重 + 旧行标 MIGRATED 排除出 recoverable_target_ids；test_idempotent_rerun |
| T-61-10 | DoS | reconcile 误杀在途活任务（标 FAILED 活 durable job） | mitigate | reconcile 标 FAILED 前查 has_active_durable_job_sync；test_reconcile_preserves_running |
| T-61-11 | Repudiation | SQLite 误"迁移"成进程内任务（运维误判已转 durable） | mitigate | 命令判后端 + 清晰中文提示不静默；test_sqlite_safe_degrade |
| T-61-12 | DoS | reconcile 同步线程裸 await 异步查询（SynchronousOnlyOperation） | mitigate | has_active_durable_job_sync 用 async_to_sync 包；durable 查询整段 fail-soft |
| T-61-13 | DoS | 非 durable(SQLite) 误"保留 RUNNING"致僵尸 RUNNING 永不收尾 | mitigate | has_active_durable_job_sync 非 procrastinate 后端返回 False，维持旧标 FAILED |
</threat_model>

<verification>
- `cd server && uv run pytest tests/durable tests/repositories tests/codegraph tests/resumable -q` 全绿。
- `cd server && uv run python manage.py makemigrations --check --dry-run` 干净。
- `python manage.py check` 无新增错误（reconcile 改判定不破坏 app 加载）。
- 手动（需真实 Postgres + 存量在途行，见 VALIDATION.md Manual-Only）：对带 PENDING/RUNNING index/graph resumable_tasks 的 Postgres 部署跑 `migrate_resumable_to_durable`，确认 durable job 建立、旧行 MIGRATED、无双跑。
</verification>

<success_criteria>
- 一次性迁移命令把存量在途 index/graph resumable_tasks 转 durable + 标 MIGRATED 记 legacy id，不双跑、幂等可重入、SQLite 安全降级。
- 启动 reconcile 改"仅无 durable 接管才标 FAILED"，非 durable 后端维持旧行为，不误杀在途。
- ResumableTask 加 MIGRATED 状态 + legacy_durable_job_id 列（migration 0002）。
- background_runner 降级为 dev fallback / 轻任务定位（docstring 收口），生产不三套并存。
</success_criteria>

<artifacts_this_phase_produces>
- `server/resumable/models.py`（改）+ `server/resumable/migrations/0002_*.py`（新）：MIGRATED 状态 + legacy_durable_job_id 列。
- `server/durable/reconcile.py`（新）：has_active_durable_job_sync 同步/异步判定 helper。
- `server/durable/management/commands/migrate_resumable_to_durable.py`（新）：一次性迁移命令。
- `server/repositories/apps.py` + `server/codegraph/apps.py`（改）：reconcile "无 durable 接管才标 FAILED"。
- `server/services/background_runner.py`（改）：降级定位 docstring 收口。
- `server/tests/durable/test_migrate_command.py` + `server/tests/repositories/test_reconcile_durable.py`（新守护测试）。
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/61-migrate/61-03-SUMMARY.md` when done
</output>
