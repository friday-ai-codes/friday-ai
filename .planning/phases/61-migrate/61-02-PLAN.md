---
phase: 61-migrate
plan: 02
type: execute
wave: 2
depends_on: ["61-01"]
files_modified:
  - server/repositories/index_views.py
  - server/repositories/views.py
  - server/tasks/index_trigger_tasks.py
  - server/codegraph/views.py
  - server/resumable/handlers.py
  - server/tests/durable/test_index_graph_migration.py
  - server/tests/durable/test_idempotency.py
autonomous: true
requirements: [MIGRATE-01, IDEMP-01]
must_haves:
  truths:
    - "全部 5 处生产 index/graph 入队点改 `DurableTaskService.defer`（queue=QUEUE_INDEX/QUEUE_GRAPH，deterministic idempotency_key `index:{repo_id}`/`graph:{repo_id}`），不再 `run_in_background(wrap_resumable(...))`（SC1）"
    - "grep 守护断言生产入队路径零 `wrap_resumable`/`submit_resumable` 残留（三套并存真正收口）（SC1）"
    - "同 repo 重复投递经 idempotency_key 去重：procrastinate `queueing_lock` todo 唯一 / in-process 同名覆盖，不产生重复 durable job（SC4）"
    - "重复执行经 FileIndex/GraphFileIndex checkpoint + 入队点 History RUNNING 复用，不产生重复数据/重复 IndexHistory RUNNING 行（SC4）"
    - "IndexHistory/GraphBuildHistory 仍在入队点创建并作进度真相源；chat/RAG 流式问答不进队列边界保留（SC1）"
  artifacts:
    - path: "server/repositories/index_views.py"
      provides: "入队点#1 `_schedule_index` → async_to_sync(defer)"
    - path: "server/repositories/views.py"
      provides: "入队点#2 `_schedule_default_branch_rolling_index` → await defer"
    - path: "server/tasks/index_trigger_tasks.py"
      provides: "入队点#3 `trigger_auto_index` → await defer"
    - path: "server/codegraph/views.py"
      provides: "入队点#4 `CodegraphRebuildView` → await defer (graph)"
    - path: "server/resumable/handlers.py"
      provides: "入队点#5 resume_index/resume_graph → async_to_sync(defer)（单一驱动入口）"
    - path: "server/tests/durable/test_index_graph_migration.py"
      provides: "5 点改 defer + queue/key 正确 + grep 无残留守护"
    - path: "server/tests/durable/test_idempotency.py"
      provides: "重复投递/重复执行 + page_index 幂等守护"
  key_links:
    - from: "server/repositories/index_views.py"
      to: "durable.DurableTaskService"
      via: "async_to_sync(DurableTaskService.defer) with idempotency_key=index:{repo_id}"
      pattern: "DurableTaskService.defer"
    - from: "server/codegraph/views.py"
      to: "durable.DurableTaskService"
      via: "await DurableTaskService.defer with idempotency_key=graph:{repo_id}"
      pattern: "DurableTaskService.defer"
    - from: "server/resumable/handlers.py"
      to: "durable.DurableTaskService"
      via: "resume handler 改走 defer（不再 submit_resumable）"
      pattern: "DurableTaskService.defer"
---

<objective>
把研究"接入点全清单"中的全部 **5 处**生产 index/graph 入队点从 `run_in_background(wrap_resumable(...))` / `submit_resumable(...)` 改为 `DurableTaskService.defer(...)`，统一 queue 常量 + deterministic idempotency_key，并补齐"重复投递/重复执行不产生重复数据"的幂等守护测试。

Purpose: 这是 MIGRATE-01 的核心交付——只有迁全 5 处（含 CONTEXT 漏列的 `index_views.py`、`codegraph/views.py`），生产 index/graph 才真正只走 durable，三套并存才算收口。idempotency_key 是 at-least-once 去重的命门。
Output: 5 处入队点改 defer + grep 守护无残留 + 幂等守护测试。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-migrate/61-RESEARCH.md
@.planning/phases/61-migrate/61-CONTEXT.md
@.planning/phases/61-migrate/61-01-SUMMARY.md

@server/durable/service.py
@server/durable/queues.py
@server/repositories/index_views.py
@server/repositories/views.py
@server/tasks/index_trigger_tasks.py
@server/codegraph/views.py
@server/resumable/handlers.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 迁移 3 处 index 入队点 → DurableTaskService.defer（#1 #2 #3）</name>
  <files>server/repositories/index_views.py, server/repositories/views.py, server/tasks/index_trigger_tasks.py</files>
  <read_first>
    - server/repositories/index_views.py:141 `_schedule_index`（**同步**函数 + `_acquire_index_lock` select_for_update 业务防抖）
    - server/repositories/views.py:357 `_schedule_default_branch_rolling_index`（**async**，已 acreate IndexHistory）
    - server/tasks/index_trigger_tasks.py:123 `trigger_auto_index`（**async**，已有 `already_indexing` 防抖 + acreate IndexHistory）
    - server/durable/service.py（`DurableTaskService.defer` async 签名）+ server/durable/queues.py（QUEUE_INDEX）
    - .planning/phases/61-migrate/61-01-SUMMARY.md（任务名 `durable_index` + payload 键契约 repository_id/history_id/branch/trigger）
  </read_first>
  <action>
    三处 index 入队点改为 `DurableTaskService.defer("durable_index", payload, queue=QUEUE_INDEX, idempotency_key=f"index:{repository_id}")`，payload 键严格为 `{"repository_id": str(repo_id), "history_id": <已创建 history.id 字符串或 None>, "branch": <branch 或 None>, "trigger": <trigger>}`（键名必须与 Plan 01 任务体 keyword-only 形参逐字对齐）：
    - **#1 `index_views._schedule_index`（同步上下文）**：删除 `wrap_resumable` + `run_in_background` 调用，改 `from asgiref.sync import async_to_sync` + `from durable import DurableTaskService, QUEUE_INDEX`，`async_to_sync(DurableTaskService.defer)("durable_index", {...history_id 来自入参...}, queue=QUEUE_INDEX, idempotency_key=f"index:{repository_id}")`。保留 `_acquire_index_lock` 业务防抖（与 key 去重互补）。返回值不再是 Future，可返回 job_id 或 None（调用方仅作可观测，逐点核对调用方不依赖 Future 语义）。
    - **#2 `views._schedule_default_branch_rolling_index`（async）**：删除 `wrap_resumable`+`run_in_background`，`await DurableTaskService.defer("durable_index", {"repository_id": repo_id, "history_id": str(history.id), "branch": None, "trigger": "manual"}, queue=QUEUE_INDEX, idempotency_key=f"index:{repo_id}")`。保留前面的 Repository/RepositoryBranchIndex/IndexHistory 写入（真相源不变）。
    - **#3 `index_trigger_tasks.trigger_auto_index`（async）**：删除 `wrap_resumable`+`run_in_background`，`await DurableTaskService.defer("durable_index", {"repository_id": repo_id, "history_id": history_id_str, "branch": None, "trigger": tt}, queue=QUEUE_INDEX, idempotency_key=f"index:{repo_id}")`。保留 `already_indexing` 防抖与 `_is_duplicate` 去重（业务层防抖，与队列层 key 去重互补）。
    清理各文件中因移除而不再使用的 `wrap_resumable`/`ResumableTaskKind` import（若该文件仅此一处使用）。中文注释说明"durable 入队 + key 去重 + 保留业务防抖"。ruff line 100。
  </action>
  <acceptance_criteria>
    - 三文件均出现 `DurableTaskService.defer("durable_index"` 且 `idempotency_key=f"index:{...}"`、`queue=QUEUE_INDEX`。
    - `rg -n "wrap_resumable|submit_resumable" server/repositories/index_views.py server/repositories/views.py server/tasks/index_trigger_tasks.py` 零命中。
    - #1 同步点使用 `async_to_sync`；#2/#3 async 点直接 `await`（无裸 await 在 sync / 无 async_to_sync 套 async）。
    - payload 键集合 == {repository_id, history_id, branch, trigger}。
    - `cd server && uv run pytest tests/repositories tests/tasks -q` 绿（既有索引触发测试零回归）。
  </acceptance_criteria>
  <verify>
    <automated>cd server && rg -c "wrap_resumable|submit_resumable" repositories/index_views.py repositories/views.py tasks/index_trigger_tasks.py; test $? -ne 0 && echo OK-no-residue && uv run pytest tests/repositories tests/tasks -q</automated>
  </verify>
  <done>3 处 index 入队点改 defer、key=index:{repo_id}、无 wrap_resumable 残留、既有测试零回归。</done>
</task>

<task type="auto">
  <name>Task 2: 迁移 graph 入队点 #4 + recovery resume 入队点 #5 → defer（单一驱动入口）</name>
  <files>server/codegraph/views.py, server/resumable/handlers.py</files>
  <read_first>
    - server/codegraph/views.py:712 `CodegraphRebuildView`（**async view**，已 acquire lock + acreate GraphBuildHistory，trigger=`GraphBuildHistoryTrigger.MANUAL.value`）
    - server/resumable/handlers.py（`resume_index`/`resume_graph` **同步** handler，从 `ResumableTask.payload` 重建，经 `submit_resumable` 续跑；`register_default_handlers` 注册到 recovery）
    - server/durable/queues.py（QUEUE_GRAPH）
    - .planning/phases/61-migrate/61-01-SUMMARY.md（任务名 `durable_graph` payload 契约）
  </read_first>
  <action>
    - **#4 `codegraph/views.CodegraphRebuildView`（async）**：删除 `wrap_resumable`+`run_in_background`（graph 段），改 `await DurableTaskService.defer("durable_graph", {"repository_id": repo_id_str, "history_id": history_id_str, "branch": branch_arg, "trigger": GraphBuildHistoryTrigger.MANUAL.value}, queue=QUEUE_GRAPH, idempotency_key=f"graph:{repo_id_str}")`。保留前面的 lock + GraphBuildHistory 创建（真相源不变）、保留 202 响应契约（返回 history_id）。清理无用 `wrap_resumable`/`ResumableTaskKind` import。
    - **#5 `resumable/handlers.py` resume_index / resume_graph**：改为 recovery 续驱也经 durable 单一入口（per RESEARCH A4 推荐"resume handler 也改 defer"）。两 handler 改为 `from asgiref.sync import async_to_sync` + `async_to_sync(DurableTaskService.defer)(...)`：resume_index → `defer("durable_index", payload, queue=QUEUE_INDEX, idempotency_key=f"index:{repository_id}")`；resume_graph → `defer("durable_graph", payload, queue=QUEUE_GRAPH, idempotency_key=f"graph:{repository_id}")`。payload 从既有 `task.payload` 重建并补齐 `history_id`（resume 路径无既有 history → 传 `None`，任务体内 service 自创建 RUNNING 行）。删除 `submit_resumable` import 与 `_run_index_resume`/`_run_graph_resume` 内联 History/续跑逻辑（durable 任务体已覆盖）；`register_default_handlers` 是否保留由执行者判断——**迁移后生产不再产生新 index/graph ResumableTask 行**（入队点 1-4 已改 durable），recovery 续驱自然枯竭；保留注册但 handler 改 defer 即可（不双跑：deterministic key 命中在途 durable job 去重）。中文注释说明语义变化。
  </action>
  <acceptance_criteria>
    - `codegraph/views.py` 出现 `DurableTaskService.defer("durable_graph"`、`idempotency_key=f"graph:{...}"`、`queue=QUEUE_GRAPH`；202 响应（history_id）不变。
    - `resumable/handlers.py` 的 resume_index/resume_graph 改走 `DurableTaskService.defer`（index/graph 各自 key），不再 `submit_resumable`。
    - `rg -n "wrap_resumable|submit_resumable" server/codegraph/views.py server/resumable/handlers.py` 零命中。
    - `cd server && uv run pytest tests/codegraph tests/resumable -q` 绿（图谱重建 / recovery 既有测试零回归）。
  </acceptance_criteria>
  <verify>
    <automated>cd server && rg -c "wrap_resumable|submit_resumable" codegraph/views.py resumable/handlers.py; test $? -ne 0 && echo OK-no-residue && uv run pytest tests/codegraph tests/resumable -q</automated>
  </verify>
  <done>graph 入队点 + recovery resume 改 defer（单一驱动入口），无 wrap_resumable/submit_resumable 残留，既有测试零回归。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 入队迁移守护 + 重复投递/重复执行/page_index 幂等守护测试</name>
  <files>server/tests/durable/test_index_graph_migration.py, server/tests/durable/test_idempotency.py</files>
  <read_first>
    - server/tests/durable/conftest.py（autouse reset、durable_service、procrastinate_app fixtures）
    - server/tests/durable/test_service_fallback.py（in-process defer/get 断言范式）
    - server/repositories/models.py:557 `FileIndex`（`uq_repo_file_path`）/ :600 `GraphFileIndex`（`uq_graph_repo_branch_file`）唯一约束
    - server/tests/durable/test_business_tasks.py（Plan 01 产出，handler 注册范式）
  </read_first>
  <behavior>
    - test_index_graph_migration（grep/接线守护）：
      - `enqueue` 用例：`rg "wrap_resumable|submit_resumable"` 在 5 个生产入队文件零命中（subprocess + rg，复用 test_no_direct_import.py 风格）。
      - `idempotency_key` 用例：断言 5 处 defer 调用使用 `index:{repo_id}`/`graph:{repo_id}` 派生 key（可经 monkeypatch `DurableTaskService.defer` 捕获调用参数，或对 helper 直接调用断言传入的 queue/key）。
    - test_idempotency（行为守护）：
      - `duplicate_dispatch`：同一 `idempotency_key="index:R"` 两次 `DurableTaskService.defer` → 第二次不产生新的 in-flight 标识（in-process：同名覆盖单条；postgres_queue 标记下断言 procrastinate `queueing_lock` todo 唯一返回既有 job id）。
      - `duplicate_execution`：预置 `FileIndex` 行后两次执行 `run_index`（mock 真实 clone/embedding 的重活，仅验证 checkpoint 跳过语义），断言不产生重复 `FileIndex`（uq 约束成立）/ 不新增第二条 IndexHistory RUNNING（history_id 复用入参）。
      - `page_index`：`run_page_index` 重复执行恒等、零副作用（与 Plan 01 Test 3 互补，确认 IDEMP-01 page_index 基线）。
  </behavior>
  <action>
    新建两个测试模块。`test_index_graph_migration.py` 的 grep 守护用 `shutil.which("rg")` + `subprocess.run`（rg 不可用 `pytest.skip`），扫描 5 个入队文件断言无残留；key 守护 monkeypatch `durable.service.DurableTaskService.defer` 为 AsyncMock 捕获 `(task, payload, queue, idempotency_key)` 并断言。`test_idempotency.py` 的 duplicate_dispatch 默认 SQLite 路径断言 in-process 行为 + 加 `postgres_queue` 标记的 procrastinate 分支；duplicate_execution 用 monkeypatch 把 `services.indexer.clone_and_index_repository` 的重活替换为只读 FileIndex 的轻量 stub（或直接在 FileIndex/GraphFileIndex 层断言 unique_together 去重），避免触发真实克隆/网络（pytest-socket 默认 disable）。所有 async 测试 `pytest.mark.asyncio`，postgres 专项测试 `@pytest.mark.postgres_queue` + `enable_socket`。中文注释标注每个断言对应的 Pitfall（双跑/重复 History/checkpoint）。
  </action>
  <acceptance_criteria>
    - `cd server && uv run pytest tests/durable/test_index_graph_migration.py tests/durable/test_idempotency.py -q` 默认 SQLite 路径绿（postgres 专项用例 skip）。
    - grep 守护断言 5 个入队文件零 `wrap_resumable`/`submit_resumable`。
    - duplicate_dispatch 断言同 key 二次 defer 不产生重复 in-flight job。
    - duplicate_execution 断言重复执行不产生重复 FileIndex/GraphFileIndex 行、不新增重复 IndexHistory RUNNING。
    - page_index 幂等基线断言通过。
  </acceptance_criteria>
  <verify>
    <automated>cd server && uv run pytest tests/durable/test_index_graph_migration.py tests/durable/test_idempotency.py -q</automated>
  </verify>
  <done>5 点迁移 grep 守护 + 重复投递/执行 + page_index 幂等守护测试全绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| REST/webhook/scheduler 触发 → durable 入队 | 入队点沿用既有 IsAuthenticated/IsSuperUser 鉴权，本 plan 不改权限 |
| 同 repo 并发触发 → 队列去重 | 业务防抖（already_indexing / select_for_update）+ 队列层 deterministic key 双层去重 |
| recovery 续驱 → durable 单一入口 | resume handler 改 defer，避免与 durable stalled rescue 双跑 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-04 | Tampering | 双跑（recovery + durable 同驱同 repo） | mitigate | 入队点 1-4 改 durable 后不再产新 ResumableTask 行；#5 resume 也改 defer + deterministic key 命中在途 job 去重 |
| T-61-05 | Tampering | 重复投递产生重复 IndexHistory RUNNING | mitigate | 保留 already_indexing / select_for_update 业务防抖 + key 去重；test_idempotency duplicate_dispatch 守护 |
| T-61-06 | Tampering | 重复执行产生重复索引产物 | mitigate | FileIndex/GraphFileIndex hash checkpoint 零改动；test_idempotency duplicate_execution 守护 unique_together |
| T-61-07 | Information Disclosure | defer payload 入日志 | accept | payload 仅含内部 repository_id（UUID 串）/history_id，无凭证；沿用既有结构化日志，低风险 |
</threat_model>

<verification>
- `cd server && uv run pytest tests/durable tests/repositories tests/codegraph tests/tasks tests/resumable -q` 全绿。
- `cd server && rg "wrap_resumable|submit_resumable" repositories/index_views.py repositories/views.py tasks/index_trigger_tasks.py codegraph/views.py resumable/handlers.py` 零命中。
- 5 处入队点均出现 `DurableTaskService.defer` + 正确 queue/key。
</verification>

<success_criteria>
- 全部 5 处生产 index/graph 入队点改 `DurableTaskService.defer`（queue + deterministic key），生产路径零 wrap_resumable/submit_resumable 残留（SC1）。
- IndexHistory/GraphBuildHistory 仍为进度真相源、FileIndex/GraphFileIndex checkpoint 保留（SC1）。
- 重复投递/重复执行经守护测试证明不产生重复数据/重复副作用（SC4）。
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 02)
- `server/repositories/index_views.py`（改）—— 入队点#1 → async_to_sync(defer)
- `server/repositories/views.py`（改）—— 入队点#2 → await defer
- `server/tasks/index_trigger_tasks.py`（改）—— 入队点#3 → await defer
- `server/codegraph/views.py`（改）—— 入队点#4 graph → await defer
- `server/resumable/handlers.py`（改）—— 入队点#5 resume → async_to_sync(defer)
- `server/tests/durable/test_index_graph_migration.py`（新）—— 5 点迁移 + key 守护
- `server/tests/durable/test_idempotency.py`（新）—— 重复投递/执行 + page_index 幂等守护
</artifacts_produced>

<output>
Create `.planning/phases/61-migrate/61-02-SUMMARY.md` when done
</output>
