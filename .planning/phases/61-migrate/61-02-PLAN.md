---
phase: 61-migrate
plan: 02
type: execute
wave: 2
depends_on: ["61-01"]
files_modified:
  - server/repositories/index_views.py
  - server/repositories/views.py
  - server/tasks/index_trigger_tasks.py
  - server/codegraph/views.py
  - server/resumable/handlers.py
  - server/tests/durable/test_index_graph_migration.py
autonomous: true
requirements: [MIGRATE-01, IDEMP-01]
must_haves:
  truths:
    - "全部 5 处 index/graph 生产入队点改走 DurableTaskService.defer（queue=QUEUE_INDEX/QUEUE_GRAPH，idempotency_key=index:{repo_id}/graph:{repo_id}），不再 run_in_background(wrap_resumable(...)) / submit_resumable(...)"
    - "同步上下文入队点(#1 _schedule_index、#5 resume_index/resume_graph)用 async_to_sync 包 defer；async 上下文(#2/#3/#4)直接 await"
    - "IndexHistory/GraphBuildHistory 仍为进度/结果真相源（入队点仍创建/复用 History 并把 history_id 放进 payload），FileIndex/GraphFileIndex checkpoint 跳过逻辑原样保留"
    - "同一 repo 重复投递经 deterministic idempotency_key 在队列层去重（procrastinate queueing_lock），既有业务层防抖(already_indexing / _acquire_index_lock)保留叠加"
    - "grep 守护：5 个迁移文件中 index/graph 路径零 wrap_resumable/submit_resumable 残留"
  artifacts:
    - path: "server/repositories/index_views.py"
      provides: "_schedule_index 改 async_to_sync(DurableTaskService.defer) (入队点#1)"
      contains: "DurableTaskService"
    - path: "server/tasks/index_trigger_tasks.py"
      provides: "trigger_auto_index 改 await DurableTaskService.defer (入队点#3)"
      contains: "QUEUE_INDEX"
    - path: "server/codegraph/views.py"
      provides: "CodegraphRebuildView.post 改 await DurableTaskService.defer (入队点#4)"
      contains: "QUEUE_GRAPH"
    - path: "server/resumable/handlers.py"
      provides: "resume_index/resume_graph 改 async_to_sync(DurableTaskService.defer) (入队点#5，单一驱动入口)"
      contains: "DurableTaskService"
  key_links:
    - from: "server/repositories/index_views.py"
      to: "durable.DurableTaskService.defer"
      via: "async_to_sync defer with idempotency_key=index:{repo_id}"
      pattern: "idempotency_key=f\"index:"
    - from: "server/codegraph/views.py"
      to: "durable.DurableTaskService.defer"
      via: "await defer with idempotency_key=graph:{repo_id}"
      pattern: "idempotency_key=f\"graph:"
---

<objective>
把 5 处 index/graph 生产入队点从 `run_in_background(wrap_resumable(...))` / `submit_resumable(...)` 全部收口到 `DurableTaskService.defer`（消费 61-01 注册的 durable 任务名 + 队列常量 + deterministic idempotency_key），并补"重复投递去重 + grep 零残留"守护测试。这是 MIGRATE-01 的入队迁移半，也是"不三套并存"的核心一步。

Purpose: 61-RESEARCH 实读源码修正 CONTEXT 的"3 处"为 **5 处**入队点——漏 `index_views.py`/`codegraph/views.py` 任一即三套并存未真正收口（Pitfall 4）。本 plan 必须迁全 5 处，并用 grep 守护断言生产路径零残留。
Output: 5 个入队点改造 + `test_index_graph_migration.py`（defer 入参/queue/key 正确 + 重复投递去重 + grep 守护）。

实现 MIGRATE-01 的入队点迁移 + IDEMP-01 的"重复投递→单次有效执行（deterministic key 去重）"半。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-migrate/61-RESEARCH.md
@.planning/phases/61-migrate/61-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 迁移 3 处 index 入队点（#1 手动 / #2 默认分支滚动 / #3 webhook·scheduled）</name>
  <files>server/repositories/index_views.py, server/repositories/views.py, server/tasks/index_trigger_tasks.py</files>
  <read_first>
    - server/repositories/index_views.py:141-175（_schedule_index：**同步函数**，现 run_in_background(wrap_resumable(...))，payload {repository_id, branch, trigger}，history_id 入参）
    - server/repositories/views.py:357-404（_schedule_default_branch_rolling_index：**async**，先 acreate IndexHistory 再 run_in_background(wrap_resumable(...))）
    - server/tasks/index_trigger_tasks.py:123-198（trigger_auto_index：**async**，already_indexing 防抖 + acreate IndexHistory + run_in_background(wrap_resumable(...))）
    - server/durable/__init__.py（curated re-export：`from durable import DurableTaskService, QUEUE_INDEX`）
    - server/durable/service.py:82-113（defer 签名：defer(task, payload, *, queue, priority=0, idempotency_key=None, run_at=None)）
    - 61-RESEARCH §"接入点全清单"#1/#2/#3 + §"改造后入队（推荐范式）" + Code Examples
  </read_first>
  <action>
    把这 3 处 INDEX 入队改为调用 `DurableTaskService.defer("durable_index", {"repository_id": str(rid), "history_id": <hid or None>, "branch": <branch>, "trigger": <trigger>}, queue=QUEUE_INDEX, idempotency_key=f"index:{rid}")`，删除对应的 `wrap_resumable` + `run_in_background` 调用与其 import：
    - 入队点 #1 `index_views._schedule_index`（同步函数）：用 `from asgiref.sync import async_to_sync` 包 `async_to_sync(DurableTaskService.defer)(...)`；保留 history_id 透传、保留 `_acquire_index_lock` 业务防抖（互补，不删）。返回值语义（Future→job_id 字符串）调用方仅作可观测，按需调整类型注解。
    - 入队点 #2 `views._schedule_default_branch_rolling_index`（async）：`await DurableTaskService.defer(...)`，payload 带 `history_id=str(history.id)`、`branch=None`、`trigger="manual"`；其余 Repository/RepositoryBranchIndex aupdate 状态写入逻辑不动（History 仍真相源）。
    - 入队点 #3 `trigger_auto_index`（async）：`await DurableTaskService.defer(...)`，payload 带 `history_id=str(history.id)`、`branch=None`、`trigger=tt`；保留 `already_indexing`/`_is_duplicate` 防抖（互补叠加 deterministic key）。
    全部用 `QUEUE_INDEX` 常量（禁裸字符串），idempotency_key 一律 `f"index:{repo_id}"`。不改 History 创建时机/字段（CONTEXT 锁定 History 真相源）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/repositories tests/tasks -q -k "index or trigger"</automated>
  </verify>
  <done>3 处 index 入队点改 DurableTaskService.defer（#1 async_to_sync、#2/#3 await），History 真相源保留，index 路径零 wrap_resumable 残留。</done>
  <acceptance_criteria>
    - 3 处均调用 `DurableTaskService.defer("durable_index", ..., queue=QUEUE_INDEX, idempotency_key=f"index:{...}")`。
    - #1 用 `async_to_sync` 包裹（同步上下文）；#2/#3 直接 `await`（async 上下文）。
    - `rg -n "wrap_resumable|submit_resumable" server/repositories/index_views.py server/repositories/views.py server/tasks/index_trigger_tasks.py` 在 index 路径零命中。
    - History 创建逻辑保持（payload 含 history_id），既有 already_indexing / _acquire_index_lock 防抖保留。
    - 既有 repositories/tasks 相关测试不回归（绿）。
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: 迁移 graph 入队点(#4 手动重建) + recovery 续驱入队点(#5 resume_index/resume_graph)</name>
  <files>server/codegraph/views.py, server/resumable/handlers.py</files>
  <read_first>
    - server/codegraph/views.py:670-743（CodegraphRebuildView.post：**async view**，_acquire_lock_and_create_history_async 后 run_in_background(wrap_resumable(GRAPH,...))，payload {repository_id, branch, trigger=MANUAL}，history_id 入参）
    - server/resumable/handlers.py 全文（resume_index:58 / resume_graph:108 **同步函数**，现 submit_resumable(...)；_run_index_resume / _run_graph_resume 任务体）
    - server/durable/__init__.py（QUEUE_GRAPH re-export）
    - 61-RESEARCH §"接入点全清单"#4/#5 + §A4（#5 策略=resume handler 也改 defer，单一驱动入口）
  </read_first>
  <action>
    入队点 #4 `codegraph/views.CodegraphRebuildView.post`（async view）：把 `run_in_background(wrap_resumable(kind=GRAPH,...))` 改为 `await DurableTaskService.defer("durable_graph", {"repository_id": repo_id_str, "history_id": history_id_str, "branch": branch_arg, "trigger": GraphBuildHistoryTrigger.MANUAL.value}, queue=QUEUE_GRAPH, idempotency_key=f"graph:{repo_id_str}")`；删 wrap_resumable/run_in_background import；保留 _acquire_lock_and_create_history_async 互斥 + 409 冲突逻辑（History 真相源不变）。
    入队点 #5 `resumable/handlers.py`（recovery 续驱，**同步**）：把 `resume_index`/`resume_graph` 的 `submit_resumable(...)` 改为 `async_to_sync(DurableTaskService.defer)(...)`——`resume_index` → `("durable_index", {repository_id, branch, trigger}, queue=QUEUE_INDEX, idempotency_key=f"index:{repository_id}")`；`resume_graph` → `("durable_graph", {...}, queue=QUEUE_GRAPH, idempotency_key=f"graph:{repository_id}")`，payload 从既有 `task.payload` 重建（与 #1-#4 同 payload 形状）。使 recovery 与生产入队同源单一驱动入口（不再 submit_resumable 进 background_runner）。`register_default_handlers()` 仍可保留注册（迁移后生产不再产生 index/graph ResumableTask 行，该路径自然枯竭；存量行由 61-03 迁移命令处理）。`_run_index_resume`/`_run_graph_resume` 任务体可删（已由 61-01 run_index/run_graph 承接）或保留为 dead——倾向删除避免双实现，但 graph supersede RUNNING 语义若需保留则并入说明。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/codegraph tests/resumable -q -k "graph or resume or rebuild"</automated>
  </verify>
  <done>graph 入队点(#4) + recovery 续驱(#5) 改 defer，graph/index 路径零 submit_resumable 残留，409 互斥/History 保留。</done>
  <acceptance_criteria>
    - #4 调用 `DurableTaskService.defer("durable_graph", ..., queue=QUEUE_GRAPH, idempotency_key=f"graph:{...}")`，`await` 直调（async view）。
    - #5 resume_index/resume_graph 改 `async_to_sync(DurableTaskService.defer)`，payload 从 task.payload 重建，queue/key 正确。
    - `rg -n "wrap_resumable|submit_resumable" server/codegraph/views.py server/resumable/handlers.py` 在 graph/index 路径零命中。
    - codegraph 重建 409 互斥 + History 创建逻辑保留；既有 codegraph/resumable 测试不回归。
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: 入队迁移守护测试（defer 入参/queue/key + 重复投递去重 + grep 零残留）</name>
  <files>server/tests/durable/test_index_graph_migration.py</files>
  <read_first>
    - server/durable/backends.py:97-124（InProcessBackend.defer：idempotency_key 作 job_id，同 key 覆盖同名注册）、:107-109 注释（in-process 去重语义）
    - server/tests/durable/conftest.py（durable_service / _reset_durable_inprocess_jobs autouse）
    - 61-RESEARCH §Validation Architecture（Req→Test Map：enqueue / idempotency_key / duplicate_dispatch）、§Pitfall 6（in-process 同 key 覆盖 vs Postgres queueing_lock todo 唯一——测试区分两后端）
  </read_first>
  <action>
    新建 `server/tests/durable/test_index_graph_migration.py`（SQLite 默认路径）：
    - `test_enqueue_points_call_defer`：monkeypatch `DurableTaskService.defer`（AsyncMock 捕获 args/kwargs），分别触发 5 个入队点的最小可达路径（或直接单测各入队 helper），断言以 `task in {"durable_index","durable_graph"}`、正确 `queue`（QUEUE_INDEX/QUEUE_GRAPH）、`idempotency_key in {"index:{rid}","graph:{rid}"}` 调用。对同步入队点（#1/#5）验证 async_to_sync 包装路径不抛。
    - `test_no_wrap_resumable_residual`：grep 守护——`rg -l "wrap_resumable|submit_resumable"` 扫 5 个迁移文件，断言 index/graph 生产路径零残留（参照 test_no_direct_import.py 的 subprocess+rg 范式，rg 不可用则 skip）。
    - `test_duplicate_dispatch_dedup`：连续两次以同一 `idempotency_key=f"index:{rid}"` 调 `DurableTaskService.defer("durable_index", ...)`（in-process 后端），断言返回同一 job_id（同 key 派生）；附注 Postgres queueing_lock todo 唯一语义留 postgres_queue 用例（61-03 / CI）。
    mock 所有真实 IO（不真索引/建图）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/durable/test_index_graph_migration.py -q</automated>
  </verify>
  <done>入队迁移守护测试绿：defer 入参/queue/key 正确、重复投递去重、5 文件 grep 零残留。</done>
  <acceptance_criteria>
    - test_enqueue_points_call_defer 断言各入队点以正确 task/queue/idempotency_key 调 defer。
    - test_no_wrap_resumable_residual 对 5 个迁移文件 grep index/graph 路径零残留（rg 缺失则 skip）。
    - test_duplicate_dispatch_dedup 断言同 key 二次 defer 返回同一 job_id（in-process）。
    - 全部测试通过。
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| REST/webhook/scheduler 触发 → durable 入队 | 请求级薄入队；权限沿用既有 IsAuthenticated/IsSuperUser，不回退 |
| 同步 helper → 异步 defer | _schedule_index/resume_* 在同步上下文调异步 defer，async/sync 误用是炸点 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-05 | Tampering | 漏迁入队点致三套并存（生产仍产 ResumableTask 行） | mitigate | 迁全 5 处 + grep 守护 test_no_wrap_resumable_residual |
| T-61-06 | Tampering | 重复投递产生重复 durable job / 重复 History | mitigate | deterministic idempotency_key=index:/graph:{repo_id} 队列去重 + 既有业务防抖叠加；test_duplicate_dispatch_dedup |
| T-61-07 | DoS | 同步上下文裸 await defer（SynchronousOnlyOperation / 协程未 await） | mitigate | #1/#5 用 async_to_sync 包；测试覆盖同步入队路径不抛 |
| T-61-08 | Repudiation | 权限面回退（迁移误删鉴权） | accept | 入队点权限装饰器/视图不动，仅替换入队实现；既有视图测试守护 |
</threat_model>

<verification>
- `cd server && uv run pytest tests/durable tests/repositories tests/codegraph tests/resumable tests/tasks -q` 全绿。
- `rg -n "wrap_resumable|submit_resumable" server/repositories/index_views.py server/repositories/views.py server/tasks/index_trigger_tasks.py server/codegraph/views.py server/resumable/handlers.py` 在 index/graph 生产路径零命中。
- `python manage.py check` 无新增错误。
</verification>

<success_criteria>
- 全部 5 处 index/graph 入队点经 DurableTaskService.defer（queue 常量 + deterministic idempotency_key）入队。
- 同步入队点用 async_to_sync 包装，async 入队点直接 await。
- IndexHistory/GraphBuildHistory 仍为真相源；FileIndex/GraphFileIndex checkpoint 保留。
- 重复投递经 idempotency_key 去重；grep 守护生产路径零 wrap_resumable/submit_resumable 残留。
</success_criteria>

<artifacts_this_phase_produces>
- `server/repositories/index_views.py`（改）：_schedule_index → async_to_sync defer（入队点#1）。
- `server/repositories/views.py`（改）：_schedule_default_branch_rolling_index → await defer（入队点#2）。
- `server/tasks/index_trigger_tasks.py`（改）：trigger_auto_index → await defer（入队点#3）。
- `server/codegraph/views.py`（改）：CodegraphRebuildView.post → await defer（入队点#4）。
- `server/resumable/handlers.py`（改）：resume_index/resume_graph → async_to_sync defer（入队点#5，单一驱动入口）。
- `server/tests/durable/test_index_graph_migration.py`（新）：入参/去重/grep 守护测试。
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/61-migrate/61-02-SUMMARY.md` when done
</output>
