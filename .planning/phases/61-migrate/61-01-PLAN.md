---
phase: 61-migrate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/durable/tasks_impl.py
  - server/durable/tasks.py
  - server/durable/handlers.py
  - server/durable/apps.py
  - server/durable/service.py
  - server/durable/backends.py
  - server/tests/durable/test_business_tasks.py
  - server/tests/durable/test_idempotency.py
autonomous: true
requirements: [MIGRATE-01, IDEMP-01]
must_haves:
  truths:
    - "durable_index / durable_graph / durable_page_index 三个 durable 任务在 Postgres(procrastinate) 与 SQLite(in-process) 两后端用同一 payload(显式 kwargs) 都能正确驱动执行（双后端入参一致）"
    - "DurableConfig.ready() 在 SQLite/in-process 路径也注册业务 handler（不再 not use_procrastinate_backend() 即 return），dev 下 defer 能真正跑 handler 而非 no-op"
    - "index/graph handler 在 at-least-once 重复执行下经 FileIndex/GraphFileIndex checkpoint + RUNNING History 复用结果一致（不产生重复 History RUNNING 行 / 不重复写产物）"
    - "page_index 占位 handler 幂等可重复执行（Phase 62 实接前的基线）"
  artifacts:
    - path: "server/durable/tasks_impl.py"
      provides: "run_index / run_graph / run_page_index 任务体（不 import procrastinate），@app.task 与 in-process adapter 共用单一实现"
      contains: "async def run_index"
    - path: "server/durable/tasks.py"
      provides: "@app.task(name=durable_index/durable_graph/durable_page_index) procrastinate 路径任务壳"
      contains: "name=\"durable_index\""
    - path: "server/durable/handlers.py"
      provides: "register_business_handlers() 注册 in-process **payload 展开 adapter"
      contains: "def register_business_handlers"
    - path: "server/durable/service.py"
      provides: "DurableTaskService.has_active_durable_job(idempotency_key) 双后端在途查询（reconcile 用）"
      contains: "has_active_durable_job"
  key_links:
    - from: "server/durable/apps.py"
      to: "server/durable/handlers.py"
      via: "DurableConfig.ready() 无条件调用 register_business_handlers()"
      pattern: "register_business_handlers"
    - from: "server/durable/tasks.py"
      to: "server/durable/tasks_impl.py"
      via: "@app.task 任务壳委托任务体"
      pattern: "from durable.tasks_impl import"
---

<objective>
立起 Phase 61 的 durable 任务层（消费 Phase 60 的 `DurableTaskService`/`durable/` 适配层），为 index/graph/page_index 提供"同一 handler 在 procrastinate 与 in-process 双后端都能跑"的任务定义与注册，并补齐 at-least-once 幂等基线测试。本 plan 不改任何入队点（入队迁移在 61-02），只产出"任务壳 + 任务体 + 双后端注册 + 在途查询 API + 幂等守护"。

Purpose: 入队点（61-02）与迁移命令（61-03）都依赖一个**已注册**的 durable 任务名（`app.tasks.get(task)` / `_handlers[task]` 必须命中），且两后端入参约定（procrastinate `defer_async(**payload)` 展开 vs in-process `handler(payload)` 整传）必须先用 adapter 抹平——这是实读源码发现、CONTEXT 未提及的关键陷阱（61-RESEARCH §Pattern 1 / Pitfall 1）。
Output: `durable/tasks_impl.py`（新）、`durable/tasks.py`（增 3 任务）、`durable/handlers.py`（新）、`durable/apps.py`（ready 重构）、`durable/service.py`+`durable/backends.py`（增在途查询）、双后端契约与幂等守护测试。

实现 MIGRATE-01 的"任务注册 + 双后端契约"半（入队点改造在 61-02）与 IDEMP-01 的"handler 幂等基线 + page_index 占位"。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-migrate/61-RESEARCH.md
@.planning/phases/61-migrate/61-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: durable_index/graph/page_index 任务体 + procrastinate 任务壳（双后端入参契约）</name>
  <files>server/durable/tasks_impl.py, server/durable/tasks.py, server/tests/durable/test_business_tasks.py</files>
  <read_first>
    - server/durable/tasks.py（既有 durable_ping / retry_stalled_durable_jobs 范式；@app.task 必须显式 name=，见 60-REVIEW CR-01）
    - server/durable/queues.py（QUEUE_INDEX/QUEUE_GRAPH/QUEUE_PAGE_INDEX 常量）
    - server/durable/backends.py:215-262（ProcrastinateBackend.defer：defer_async(**payload) 展开）/ :97-163（InProcessBackend.defer/_run_job：handler(payload) 整传）
    - server/services/indexer.py:3622（clone_and_index_repository(repository_id, *, history_id=None, branch=None)）
    - server/services/graph_builder.py:323（build_graph_for_repository(repository_id, *, trigger, history_id=None, branch=None, skip_unchanged=False)）
    - server/resumable/handlers.py:26-55（_run_index_resume 的 History 复用/创建范式）、:78-105（_run_graph_resume 先 supersede RUNNING 再 build skip_unchanged=True）
    - server/repositories/models.py（IndexHistory:378 / GraphBuildHistory:494 / IndexHistoryStatus / GraphBuildHistoryStatus）
  </read_first>
  <action>
    新建 `server/durable/tasks_impl.py`（**禁止 import procrastinate**——保持在 no-direct-import 允许清单之外，让 tasks_impl 可被 in-process adapter 安全 import）。定义 3 个 async 任务体，全部接受**显式 kwargs**（与 `defer_async(**payload)` 展开对齐）：
    - `run_index(*, repository_id: str, history_id: str | None = None, branch: str | None = None, trigger: str = "manual") -> dict`：函数内局部 import `services.indexer.clone_and_index_repository`。当 `history_id` 为 None 时，先复用该 repo 最近一条 RUNNING `IndexHistory`（`IndexHistory.objects.filter(repository_id=, status=RUNNING).order_by("-started_at").afirst()`），命中则复用其 id、否则 `acreate(RUNNING)`——避免 at-least-once 重投每次新建 RUNNING History（61-RESEARCH Pitfall 5）；`history_id` 非 None 时直接透传。返回 `clone_and_index_repository(...)` 结果。
    - `run_graph(*, repository_id: str, history_id: str | None = None, branch: str | None = None, trigger: str = "manual") -> dict`：局部 import `services.graph_builder.build_graph_for_repository`，以 `skip_unchanged=True` 调用（GraphFileIndex checkpoint 幂等基础），`history_id` 透传；镜像 `_run_graph_resume` 不在任务体内 supersede（supersede 是 resume 专属语义，durable 重投靠 History 复用/skip_unchanged）。
    - `run_page_index(*, target_id: str, target_hash: str | None = None, **_: object) -> dict`：**占位 handler**（Phase 62 PAGEIDX-01 实接）——按 `target_hash` 幂等：记 structlog `durable_page_index_placeholder`，返回 `{"placeholder": True, "target_id": target_id, "target_hash": target_hash}`，绝不写库、可安全重复执行。
    在 `server/durable/tasks.py` 新增 3 个 `@app.task(name="durable_index", queue=QUEUE_INDEX)` / `@app.task(name="durable_graph", queue=QUEUE_GRAPH)` / `@app.task(name="durable_page_index", queue=QUEUE_PAGE_INDEX)`，函数体内局部 `from durable.tasks_impl import run_index/run_graph/run_page_index` 并 `return await run_*(**payload)`（payload 由 procrastinate 展开为 kwargs，签名 `async def durable_index(**payload)`）。**必须显式 name=**（否则 `backends.defer` 按裸名 `app.tasks.get(task)` 查不到，60-REVIEW CR-01）。
    新建 `server/tests/durable/test_business_tasks.py`：断言 3 个任务名经 in-process `register_handler` 后用单一 dict payload `defer(...)` 能驱动 `run_*(**payload)` 不抛 `TypeError`（双后端入参一致）；mock `clone_and_index_repository`/`build_graph_for_repository` 捕获被调用的 kwargs。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/durable/test_business_tasks.py -q</automated>
  </verify>
  <done>三个 durable 任务体 + procrastinate 任务壳就位，双后端入参契约测试绿。</done>
  <acceptance_criteria>
    - `durable/tasks_impl.py` 存在且 `rg -n "import procrastinate|from procrastinate" server/durable/tasks_impl.py` 零命中（保持 no-direct-import 守护绿）。
    - `tasks.py` 含 `@app.task(name="durable_index")` / `"durable_graph"` / `"durable_page_index"` 三个显式 name。
    - `run_index`/`run_graph`/`run_page_index` 全部使用 keyword-only 参数（`*,`），`run_*(**payload)` 与 `defer_async(**payload)` 调用方式一致。
    - test_business_tasks.py 断言"单 dict payload → kwargs 展开"在 in-process 路径不抛 TypeError；测试通过。
    - `run_page_index` 不触任何 ORM 写入（占位），可重复调用结果一致。
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: DurableConfig.ready 双后端注册业务 handler + has_active_durable_job 在途查询 API</name>
  <files>server/durable/handlers.py, server/durable/apps.py, server/durable/service.py, server/durable/backends.py, server/tests/durable/test_business_tasks.py</files>
  <read_first>
    - server/durable/apps.py:15-37（现 ready()：`if not use_procrastinate_backend(): return` 早退——SQLite 路径不注册任何业务 handler，必须重构）
    - server/durable/backends.py:75-80（register_handler）、:68（_handlers 注册表）、:264-287（_find_job_by_queueing_lock 范式）、:289-313（get 按 id 查询）
    - server/durable/service.py:82-146（defer/get/cancel/retry_stalled 双后端委托范式）
    - server/durable/roles.py:30-49（should_run_startup_side_effects(job=, allowed=)）
    - 61-RESEARCH §"in-process adapter 注册" + §Open Question 1（ready 重构）+ §"reconcile 改判定 helper"
  </read_first>
  <action>
    新建 `server/durable/handlers.py`，定义 `register_business_handlers() -> None`：用 `from durable.backends import register_handler` 注册 3 个 in-process adapter，每个 adapter 形如 `async def _index(payload: dict): from durable.tasks_impl import run_index; return await run_index(**payload)`（**`**payload` 展开**——与 procrastinate `defer_async(**payload)` 对齐，消除 Pitfall 1）；注册键用 `"durable_index"/"durable_graph"/"durable_page_index"`（与 Task 1 的 `@app.task(name=...)` 同名，single source of truth）。
    重构 `server/durable/apps.py` 的 `DurableConfig.ready()`：**先**做 role 门禁（`should_run_startup_side_effects(job="durable_business_handlers", allowed=frozenset({"web","worker","scheduler"}))`），通过后**无条件**调用 `register_business_handlers()`（in-process handler 在 SQLite dev 也必须注册——这是本 plan 的核心修复，对应 SC：dev 下 defer 不再 no-op）；**之后**保留既有"仅 `use_procrastinate_backend()` 为真才 `import durable.tasks` 触发 @app.task 注册"分支（procrastinate-only）。即把"业务 handler 注册"从 procrastinate-only 分支提到通用段，`import durable.tasks` 仍只在 procrastinate 分支。
    在 `server/durable/service.py` 新增 `@staticmethod async def has_active_durable_job(idempotency_key: str) -> bool`（双后端委托，供 61-03 reconcile 消费）；在 `server/durable/backends.py` 给 `ProcrastinateBackend` 与 `InProcessBackend` 各加 `async def has_active_durable_job(self, idempotency_key)`：
    - Procrastinate：`app.job_manager.list_jobs_async(queueing_lock=idempotency_key, status="todo")` 或 `status="doing"` 任一非空即 True（复用 `_find_job_by_queueing_lock` 的 todo→doing 顺序范式；只判在途，不含 succeeded/failed）。
    - In-process：查 `_jobs.get(idempotency_key)` 的 `status in {"pending","running"}`。
    在 test_business_tasks.py 增断言：`DurableConfig.ready()` 等价调用后 `_handlers` 含三个键（或直接断言 `register_business_handlers()` 后 `from durable.backends import _handlers` 三键就位）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/durable/test_business_tasks.py tests/durable/test_no_direct_import.py -q</automated>
  </verify>
  <done>ready() 双后端注册业务 handler、has_active_durable_job 查询就位，no-direct-import 守护绿。</done>
  <acceptance_criteria>
    - `durable/apps.py` 的 `ready()` 不再以 `if not use_procrastinate_backend(): return` 早退于业务 handler 注册之前；`register_business_handlers()` 在 in-process(SQLite) 路径也被调用（测试断言 `_handlers` 三键就位）。
    - `import durable.tasks`（@app.task 触发）仍只在 `use_procrastinate_backend()` 为真的分支。
    - `DurableTaskService.has_active_durable_job` 存在且双后端各有实现；in-process 命中 `_jobs` pending/running 返回 True，未知 key 返回 False。
    - `test_no_direct_import.py` 仍绿（handlers.py 不直接 import procrastinate；tasks_impl.py 不在允许清单且零命中）。
    - 全部测试通过。
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: at-least-once 幂等基线守护测试（重复执行 / page_index 占位）</name>
  <files>server/tests/durable/test_idempotency.py</files>
  <read_first>
    - server/durable/tasks_impl.py（Task 1 产出：run_index History 复用、run_graph skip_unchanged、run_page_index 占位）
    - server/durable/backends.py:186-189（_reset_for_tests）、tests/durable/conftest.py（_reset_durable_inprocess_jobs autouse / durable_service fixture）
    - server/repositories/models.py（IndexHistory/GraphBuildHistory/FileIndex uq_repo_file_path / GraphFileIndex uq_graph_repo_branch_file）
    - 61-RESEARCH §Validation Architecture（Req→Test Map：duplicate_dispatch/duplicate_execution/page_index）
  </read_first>
  <action>
    新建 `server/tests/durable/test_idempotency.py`（SQLite 默认路径，无需 Postgres）覆盖 IDEMP-01 的"重复执行"半（"重复投递"的 deterministic-key 去重在 61-02）：
    - `test_duplicate_execution_index_reuses_running_history`：建一条 RUNNING IndexHistory；mock `clone_and_index_repository` 为 no-op；以 `history_id=None` 连续两次 `await run_index(repository_id=..., history_id=None)`，断言 `IndexHistory.objects.filter(repository_id=, status=RUNNING).count() == 1`（复用而非新建，Pitfall 5）。
    - `test_duplicate_execution_graph_skip_unchanged`：mock `build_graph_for_repository` 捕获 kwargs，断言两次 `run_graph(...)` 都以 `skip_unchanged=True` 调用（GraphFileIndex hash checkpoint 幂等基础保留）。
    - `test_page_index_placeholder_idempotent`：连续两次 `run_page_index(target_id="t1", target_hash="h1")` 返回结构一致、无 DB 写入（断言无新建任何模型行——可用 `IndexHistory.objects.count()` 前后不变兜底）。
    全部 mock IO 边界（不真克隆仓库 / 不真建图），只验"重复执行不产生重复 History / 重复产物 / 重复副作用"。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/durable/test_idempotency.py -q</automated>
  </verify>
  <done>重复执行/占位幂等守护测试绿（RUNNING History 复用、skip_unchanged、page_index 占位）。</done>
  <acceptance_criteria>
    - 三个测试存在且通过。
    - duplicate_execution_index 断言重复执行后 RUNNING IndexHistory 行数 == 1。
    - duplicate_execution_graph 断言 `skip_unchanged=True` 被传入。
    - page_index 占位测试断言重复调用结果一致且零 DB 写入。
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 入队侧(web) → 任务体(worker/in-process) | payload dict 跨后端传递；双后端入参约定不一致是 Tampering 面 |
| durable 适配层 → procrastinate | 仅 backends.py/tasks.py 允许直接 import（no-direct-import 守护） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-01 | Tampering | 双后端 payload 入参（defer_async(**payload) vs handler(payload)） | mitigate | in-process adapter 用 `**payload` 展开统一约定；test_business_tasks.py 断言双后端一致 |
| T-61-02 | Tampering | at-least-once 重复执行产生重复 History/产物 | mitigate | run_index 复用 RUNNING History + FileIndex/GraphFileIndex checkpoint + run_graph skip_unchanged；test_idempotency.py 守护 |
| T-61-03 | Tampering | 业务代码绕过适配层直接 import procrastinate | mitigate | tasks_impl.py/handlers.py 不 import procrastinate；test_no_direct_import.py 守护 |
| T-61-04 | DoS | SQLite dev 下 ready() 不注册 handler 致 defer 永远 no-op（任务静默不执行） | mitigate | ready() 重构：in-process 路径无条件 register_business_handlers() |
</threat_model>

<verification>
- `cd server && uv run pytest tests/durable -q` 全绿（含 test_business_tasks / test_idempotency / test_no_direct_import）。
- `rg -n "import procrastinate|from procrastinate" server/durable/tasks_impl.py server/durable/handlers.py` 零命中。
- `python manage.py check` 无新增错误（ready() 重构不破坏 app 加载）。
</verification>

<success_criteria>
- durable_index/durable_graph/durable_page_index 三任务在两后端用同一显式-kwargs payload 驱动一致（双后端契约测试绿）。
- DurableConfig.ready() 在 SQLite/in-process 路径注册业务 handler（dev 下 defer 不再 no-op）。
- index/graph handler 重复执行不产生重复 RUNNING History / 重复产物（幂等守护测试绿）。
- page_index 占位 handler 幂等可重复执行。
- DurableTaskService.has_active_durable_job 就位供 61-03 reconcile 消费。
</success_criteria>

<artifacts_this_phase_produces>
- `server/durable/tasks_impl.py`（新）：run_index/run_graph/run_page_index 任务体（单一实现，不 import procrastinate）。
- `server/durable/tasks.py`（增）：durable_index/durable_graph/durable_page_index 三个 `@app.task(name=...)`。
- `server/durable/handlers.py`（新）：register_business_handlers() in-process adapter 注册。
- `server/durable/apps.py`（改）：ready() 双后端注册业务 handler。
- `server/durable/service.py` + `server/durable/backends.py`（增）：has_active_durable_job 在途查询 API。
- `server/tests/durable/test_business_tasks.py`、`server/tests/durable/test_idempotency.py`（新守护测试）。
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/61-migrate/61-01-SUMMARY.md` when done
</output>
