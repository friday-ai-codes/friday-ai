---
phase: 95-decompose-llm
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/agents/call_source.py
  - .planning/observability/LOGGING-SPEC.md
autonomous: true
requirements: [DECOMP-01]
must_haves:
  truths:
    - "CallSource 枚举包含 PLAN_DECOMPOSE = 'plan_decompose'，可被 helper import 标注调用来源"
    - "LOGGING-SPEC §4.1 表格登记 plan_decompose（并补齐缺失的 plan_clarification）"
  artifacts:
    - path: "server/agents/call_source.py"
      provides: "PLAN_DECOMPOSE 受控枚举值"
      contains: "PLAN_DECOMPOSE"
    - path: ".planning/observability/LOGGING-SPEC.md"
      provides: "plan_decompose / plan_clarification call_source 登记行"
      contains: "plan_decompose"
  key_links:
    - from: "server/agents/call_source.py"
      to: "CallSource.normalize"
      via: "Enum 成员遍历（新增值自动受控）"
      pattern: "PLAN_DECOMPOSE\\s*=\\s*\"plan_decompose\""
---

<objective>
为 DECOMP-01 的 LLM 拆分调用铺设可观测性底座：在 `CallSource` 受控枚举新增 `PLAN_DECOMPOSE`，并在 `LOGGING-SPEC §4.1` call_source 表格登记 `plan_decompose`（顺手补齐代码已有但 spec 漏登记的 `plan_clarification`）。

Purpose: helper（95-02）需要 `CallSource.PLAN_DECOMPOSE` 才能经 `use_call_source` 标注 LLM 调用来源，让 chokepoint 按维度上报请求/token/TTFT/上游错误码（per D-B / AGENTS.md 观测规范）。本 plan 是纯枚举 + 文档登记，零行为风险，先于 helper 落地。

Output: 更新后的 `call_source.py`（含 PLAN_DECOMPOSE）+ `LOGGING-SPEC.md`（含两条登记行）。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/95-decompose-llm/95-CONTEXT.md
@.planning/phases/95-decompose-llm/95-RESEARCH.md

@server/agents/call_source.py
@.planning/observability/LOGGING-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: CallSource 新增 PLAN_DECOMPOSE 枚举值</name>
  <files>server/agents/call_source.py</files>
  <read_first>
    - server/agents/call_source.py L86-89（`BRANCH_NAMING` / `PLAN_CLARIFICATION` 末尾成员，作为追加锚点）
    - server/agents/call_source.py L7-14 + L36（模块顶部 docstring 与类 docstring 的「30 值」计数，需更新为实际成员数）
  </read_first>
  <action>
    在 `CallSource` 枚举末尾（`PLAN_CLARIFICATION` 之后）新增成员 `PLAN_DECOMPOSE = "plan_decompose"`（per D-A / D-B / DECOMP-01），并加中文注释说明：方案编排拆分阶段——LLM 跨仓业务线/模块/前后端拆需求产结构化 segments（单轮，best-effort，失败回退按非空行切分），提升路由/调研精度。
    同步把模块顶部 docstring（L7 附近）与类 docstring（L36 附近）的「30 值」计数订正为**实际成员数 32**（当前实测 31 个成员 + 新增 PLAN_DECOMPOSE = 32；`PLAN_CLARIFICATION` 此前加入时漏更计数，本次一并修正）。`CallSource.normalize` 遍历成员、无需改动。不要改动其它枚举值的拼写或顺序。
  </action>
  <verify>
    <automated>cd server &amp;&amp; uv run python -c "from agents.call_source import CallSource; assert CallSource.PLAN_DECOMPOSE.value == 'plan_decompose'; assert CallSource.normalize('plan_decompose') == 'plan_decompose'; assert len(list(CallSource)) == 32; print('ok')"</automated>
  </verify>
  <done>`CallSource.PLAN_DECOMPOSE` 存在且值为 `plan_decompose`；`normalize('plan_decompose')` 回显自身；枚举成员数为 32；docstring 计数与实际一致。</done>
</task>

<task type="auto">
  <name>Task 2: LOGGING-SPEC §4.1 登记 plan_decompose + 补 plan_clarification</name>
  <files>.planning/observability/LOGGING-SPEC.md</files>
  <read_first>
    - .planning/observability/LOGGING-SPEC.md L62-99（§4.1 call_source 表格，当前末行为 `branch_naming`，缺 `plan_clarification` 与 `plan_decompose`）
  </read_first>
  <action>
    在 §4.1 表格 `branch_naming` 行（L97 附近）之后追加两行（per D-B）：
    `| \`plan_clarification\` | \`clarification_questions\` / ClarifyAdapter（v0.16.1 Phase 90） | 基于需求+路由+召回产结构化澄清问题（多题/单多选/推荐），单轮 |`
    `| \`plan_decompose\` | \`decompose_segments\` / \`PlanOrchestrationEngine._decompose\`（v0.16.1 Phase 95） | LLM 跨仓业务线/模块/前后端拆需求 → 结构化 segments，单轮，best-effort 失败回退按行切分 |`
    `plan_clarification` 为补登（代码枚举已有、spec 此前漏登记）。保持表格列对齐与既有中文表述风格（文档正文中文、专有名词/代码标识保留英文，per doc-writing-zh 规范）。不改动表格其它行。
  </action>
  <verify>
    <automated>cd /Users/zaneliu/Projects/open-source/friday-ai &amp;&amp; grep -F '| `plan_decompose` |' .planning/observability/LOGGING-SPEC.md &amp;&amp; grep -F '| `plan_clarification` |' .planning/observability/LOGGING-SPEC.md</automated>
  </verify>
  <done>§4.1 表格同时存在 `plan_decompose` 与 `plan_clarification` 两行登记，列对齐、备注准确。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM 调用维度标签 | `call_source` 作为指标/筛选维度，基数必须受控（不可被外部输入污染） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-95-01 | Tampering | CallSource 维度基数 | mitigate | 新值为硬编码受控枚举成员；`CallSource.normalize` 对任意非法字符串回退 `unknown`（既有机制，自动覆盖新值，无需改动） |
| T-95-02 | Information Disclosure | LOGGING-SPEC 文档 | accept | 文档仅登记枚举名与入口说明，不含任何凭证/需求原文 |
| T-95-SC | Tampering | npm/pip/cargo installs | mitigate | 本 plan 不安装任何包（纯仓内枚举 + 文档），无供应链面 |
</threat_model>

<verification>
- `cd server && uv run python -c "from agents.call_source import CallSource; print(CallSource.PLAN_DECOMPOSE.value)"` → `plan_decompose`
- `cd server && uv run pytest tests/agents -k call_source -x`（若存在 call_source 相关用例则保持全绿；无则跳过）
- LOGGING-SPEC §4.1 含 `plan_decompose` 与 `plan_clarification` 两行
</verification>

<success_criteria>
- `CallSource.PLAN_DECOMPOSE = "plan_decompose"` 已落地且 `normalize` 受控
- 枚举成员计数 docstring 与实际（32）一致
- LOGGING-SPEC §4.1 完成 `plan_decompose` 登记 + `plan_clarification` 补登
- 无既有 import / 枚举测试回归
</success_criteria>

<output>
Create `.planning/phases/95-decompose-llm/95-01-SUMMARY.md` when done.

## Artifacts this phase produces (95-01)
- 新建符号：`CallSource.PLAN_DECOMPOSE = "plan_decompose"`（`server/agents/call_source.py`）
- 文档登记：LOGGING-SPEC §4.1 新增 `plan_decompose` 行 + 补 `plan_clarification` 行
</output>
