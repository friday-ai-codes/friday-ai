---
phase: 95-decompose-llm
plan: 02
type: execute
wave: 2
depends_on: [95-01]
files_modified:
  - server/services/plan_orchestration/decompose_segments.py
  - server/tests/services/test_decompose_segments.py
autonomous: true
requirements: [DECOMP-01]
must_haves:
  truths:
    - "decompose_segments.py 导出 agenerate_decomposition_segments，LLM 成功时返回结构化 list[dict]（带 title/module/layer/repo_hint）"
    - "缺 default_model / aresolve 抛 / 解析空 / 任意异常时返回 None（best-effort，绝不抛）"
    - "LLM 调用经 use_call_source(CallSource.PLAN_DECOMPOSE) 标注；记 started/completed/failed + duration_ms（category=sampling, component=plan_orchestration）"
    - "normalize_decomposition_segments 防御 LLM 畸形输出（缺 title 跳过、非法 layer 回退空、字段强转 str/strip、上限截断）"
  artifacts:
    - path: "server/services/plan_orchestration/decompose_segments.py"
      provides: "入口无关 LLM 拆分 helper（镜像 clarification_questions.py）"
      exports: ["agenerate_decomposition_segments", "normalize_decomposition_segments"]
      min_lines: 120
    - path: "server/tests/services/test_decompose_segments.py"
      provides: "helper 解析/normalize/fail-soft/call_source 单测"
      contains: "agenerate_decomposition_segments"
  key_links:
    - from: "server/services/plan_orchestration/decompose_segments.py"
      to: "agents.call_source.CallSource.PLAN_DECOMPOSE"
      via: "use_call_source 上下文标注"
      pattern: "use_call_source\\(CallSource\\.PLAN_DECOMPOSE\\)"
    - from: "server/services/plan_orchestration/decompose_segments.py"
      to: "agents.llm_factory.build_chat_model"
      via: "ProviderConfigService.aresolve → build_chat_model(streaming=False)"
      pattern: "build_chat_model\\("
---

<objective>
新建 `server/services/plan_orchestration/decompose_segments.py`，**逐段镜像** `clarification_questions.py`（CLARIFY-02 权威样板）：入口无关纯 helper，单轮 LLM 产结构化 `segments`，健壮 JSON 解析 + normalize 防御 + `use_call_source(CallSource.PLAN_DECOMPOSE)` 标注 + structlog 生命周期事件 + best-effort fail-soft（失败返回 `None`，绝不抛）。

Purpose: 把 D-A 的「LLM 跨仓业务线/模块/前后端拆分」做成可独立测试、与 clarification_questions 对称的 service helper，避免污染 engine 纯度。失败信号（`None`）交由 95-03 的 `_decompose` 触发 splitlines 回退（D-C）。

Output: helper 模块 + 专用单测文件，覆盖 LLM 成功 / 缺 model / 异常 / 解析空 / normalize 边角。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/95-decompose-llm/95-CONTEXT.md
@.planning/phases/95-decompose-llm/95-RESEARCH.md

@server/services/plan_orchestration/clarification_questions.py
@server/agents/call_source.py
@server/agents/llm_factory.py
@server/services/provider_config.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: decompose_segments.py 纯函数（prompt + 健壮 JSON 解析 + normalize）</name>
  <files>server/services/plan_orchestration/decompose_segments.py, server/tests/services/test_decompose_segments.py</files>
  <read_first>
    - server/services/plan_orchestration/clarification_questions.py L31-129（`_content_to_text` / `_parse_questions_json` / `normalize_clarification_questions` / `_system_prompt` / `_build_prompt` —— 逐段镜像范式，per RESEARCH Pattern 1/2/3）
    - .planning/phases/95-decompose-llm/95-RESEARCH.md L170-187（Segment 结构建议：union schema，LLM 路径 list[dict]）
  </read_first>
  <behavior>
    - `_parse_segments_json`：从 ```json 代码块 / 裸 JSON 中提取 → 接受 `{"segments": [...]}` 或顶层 list；非 JSON / 非法 → 返回 `[]`（不抛）。
    - `normalize_decomposition_segments`：
      - 缺/空 `title` 的项跳过（title 为必填，字段强转 str/strip）。
      - `layer` 仅接受 `frontend|backend|fullstack|infra`，非法/缺失回退空字符串。
      - `module` / `repo_hint` 强转 str/strip，缺失为空字符串。
      - 截断到 `_MAX_SEGMENTS`（如 20）防 LLM 失控。
      - 返回每项形如 `{"title","module","layer","repo_hint"}` 的 list[dict]。
    - `_content_to_text`：兼容 reasoning 模型 content_blocks list（只拼 type=="text"/含 text 的 block）。
  </behavior>
  <action>
    创建 `decompose_segments.py`，镜像 `clarification_questions.py` 结构实现纯函数部分（per D-A，Claude's Discretion 采用 RESEARCH 推荐的 union schema）：
    - 模块 docstring（中文）说明定位：plan_orchestration decompose stage 的 LLM 拆分 helper，入口无关、best-effort、失败回退；`__all__ = ["agenerate_decomposition_segments", "normalize_decomposition_segments"]`。
    - `_MAX_SEGMENTS = 20`、`_VALID_LAYERS = ("frontend", "backend", "fullstack", "infra")`。
    - `_content_to_text(content)`：复用 clarification 同款实现（或从 `llm_factory.content_to_text` 复用，二选一）。
    - `_parse_segments_json(text)`：复用 `_parse_questions_json` 正则范式（``` 代码块 + 裸文本兜底 + `json.loads` try/continue），但解析键为 `segments`。
    - `normalize_decomposition_segments(raw, *, max_segments=_MAX_SEGMENTS)`：实现 `<behavior>` 防御逻辑。
    - `_system_prompt()` + `_build_prompt(requirement, include_repos)`：拆分 prompt 设计（Claude's Discretion）——要求 LLM 只输出 `{"segments":[{"title","module","layer","repo_hint"}]}` JSON，跨业务线/模块/前后端拆分，`repo_hint` 取自 include_repos，无解释性文字。
    同时创建 `test_decompose_segments.py`，覆盖纯函数：`_parse_segments_json` 解析 ```json 代码块 / 裸 JSON / 非法文本（→[]）；`normalize_decomposition_segments` 缺 title 跳过、非法 layer 回退空、超 `_MAX_SEGMENTS` 截断、字段 strip。**不**在本 task 触网（纯函数单测）。
  </action>
  <verify>
    <automated>cd server &amp;&amp; uv run pytest tests/services/test_decompose_segments.py -x</automated>
  </verify>
  <done>纯函数 + 其单测全绿；normalize 防御所有 `<behavior>` 边角；模块可被 import。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: agenerate_decomposition_segments 异步 LLM 接线 + 观测 + fail-soft</name>
  <files>server/services/plan_orchestration/decompose_segments.py, server/tests/services/test_decompose_segments.py</files>
  <read_first>
    - server/services/plan_orchestration/clarification_questions.py L132-177（`agenerate_clarification_questions`：aresolve → default_model 守卫 → build_chat_model → use_call_source → ainvoke → parse → normalize → try/except 返回降级值）
    - .planning/phases/95-decompose-llm/95-RESEARCH.md L201-230（观测埋点：category=sampling / component=plan_orchestration / started/completed/failed/no_default_model + duration_ms / 脱敏只记长度计数）
    - server/tests/services/test_engine_clarify.py L1-26 + L186-276（patch helper 范式 + AsyncMock 注入 / 返回空触发 fail-soft 的测试范式）
  </read_first>
  <behavior>
    - 成功路径：`aresolve()` 得 `default_model` → `build_chat_model(streaming=False)` → `with use_call_source(CallSource.PLAN_DECOMPOSE): ainvoke([System, Human])` → `normalize(_parse_segments_json(_content_to_text(resp.content)))` → 非空 → 返回 list[dict] + 记 `plan_decompose_completed`(segment_count, duration_ms)。
    - 缺 default_model：记 `plan_decompose_no_default_model` → 返回 `None`。
    - aresolve/build/ainvoke/解析 任意异常：记 `plan_decompose_failed`(error=str(exc), duration_ms) → 返回 `None`（`except Exception` best-effort，绝不抛）。
    - 解析后 normalize 为空：返回 `None`（触发上游回退）。
    - 入参 `requirement_text` 为空/空白：直接返回 `None`（不触网）。
  </behavior>
  <action>
    在 `decompose_segments.py` 追加 `async def agenerate_decomposition_segments(*, requirement_text: str, include_repos: list | None = None, max_segments: int = _MAX_SEGMENTS) -> list[dict] | None`（per D-A/B/C）：
    - 用 `time.monotonic()` 起止算 `duration_ms`。
    - 进 try 前空白校验返回 None。
    - try 内 lazy import `langchain_core.messages`、`agents.call_source.{CallSource, use_call_source}`、`agents.llm_factory.build_chat_model`、`services.provider_config.ProviderConfigService`（镜像 clarification L143-147 的函数内导入，避免顶层循环依赖）。
    - 记 `plan_decompose_started`（category=sampling, component=plan_orchestration, requirement_len=len(requirement_text), include_repos_count=len(include_repos or [])）。
    - 缺 model → `plan_decompose_no_default_model`（含 duration_ms）→ return None。
    - `with use_call_source(CallSource.PLAN_DECOMPOSE): response = await model.ainvoke(messages)`（指标/TTFT/上游错误由 chokepoint 自动按 plan_decompose 维度上报，helper 不手写数值，per RESEARCH §观测）。
    - normalize 后为空 → return None（可记 `plan_decompose_completed` segment_count=0 或 fallback 提示，由实现定）；非空 → 记 `plan_decompose_completed` 返回。
    - `except Exception as exc:` 记 `plan_decompose_failed`（error 经脱敏：日志只记 `str(exc)`，**不**记 requirement 原文；如需入异常文本用 `common.logging.redact_secrets_in_text`，best-effort）→ return None。
    扩展 `test_decompose_segments.py`：patch `ProviderConfigService.aresolve` + `build_chat_model`（或 patch `model.ainvoke`）断言——① 成功返回 list[dict]；② 缺 default_model 返回 None；③ aresolve 抛 → None（不抛）；④ ainvoke 返回畸形 content → None；⑤（可选）断言 LLM 调用期 `get_call_source()` == `"plan_decompose"`（在 patched ainvoke 内读 contextvar）。空白 requirement → None。
  </action>
  <verify>
    <automated>cd server &amp;&amp; uv run pytest tests/services/test_decompose_segments.py -x</automated>
  </verify>
  <done>`agenerate_decomposition_segments` 实现全部 `<behavior>` 分支；异步单测全绿；任何失败路径返回 None 且不抛；call_source 在调用期为 plan_decompose。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM 上游 → helper 解析 | LLM 返回不可信文本（可能畸形/超大/含 reasoning 前缀），跨入 helper 解析层 |
| Provider 凭证 → build_chat_model | 系统默认 Provider api_key（敏感）经 SecretStr 注入，不得落日志 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-95-03 | Tampering / DoS | `_parse_segments_json` + `normalize_decomposition_segments` | mitigate | 健壮 JSON 解析（容错代码块/裸文本/解析失败返回[]）+ normalize 字段强转/枚举校验 + `_MAX_SEGMENTS` 上限；畸形输入降级为 None 触发回退（V5 Input Validation, ASVS L1） |
| T-95-04 | Information Disclosure | structlog 事件 / 异常文本 | mitigate | 日志只记 `requirement_len`/计数，**不落 requirement 原文**；异常文本如入日志走 `redact_secrets_in_text`；api_key 经 `build_chat_model` SecretStr 包装不手碰（per AGENTS.md 脱敏） |
| T-95-05 | Denial of Service | 编排主流程 | mitigate | helper 自包 `except Exception` 返回 None，绝不抛 → 不阻断编排（D-C / RESEARCH Anti-Pattern） |
| T-95-06 | Tampering | call_source 维度基数 | mitigate | 经 `use_call_source(CallSource.PLAN_DECOMPOSE)` 受控枚举标注（normalize 兜底） |
| T-95-SC | Tampering | pip installs | mitigate | 不安装任何包（纯仓内复用 llm_factory/provider_config/call_source/langchain/structlog） |
</threat_model>

<verification>
- `cd server && uv run pytest tests/services/test_decompose_segments.py -x` 全绿
- `cd server && uv run python -c "from services.plan_orchestration.decompose_segments import agenerate_decomposition_segments, normalize_decomposition_segments; print('ok')"`
- 人工抽查：失败/无 model 路径返回 None，无异常上抛；日志事件不含 requirement 原文
</verification>

<success_criteria>
- helper 模块导出 `agenerate_decomposition_segments` / `normalize_decomposition_segments`
- LLM 成功 → 结构化 list[dict]；所有失败/降级路径 → None（绝不抛）
- `use_call_source(CallSource.PLAN_DECOMPOSE)` 标注 + started/completed/failed + duration_ms（category=sampling, component=plan_orchestration）
- normalize 防御覆盖缺字段/非法 layer/超限
- 专用单测全绿，纯函数测试不触网
</success_criteria>

<output>
Create `.planning/phases/95-decompose-llm/95-02-SUMMARY.md` when done.

## Artifacts this phase produces (95-02)
- 新建文件：`server/services/plan_orchestration/decompose_segments.py`
- 新建符号：`agenerate_decomposition_segments(...)`、`normalize_decomposition_segments(...)`、`_parse_segments_json`、`_system_prompt`、`_build_prompt`
- 新建测试：`server/tests/services/test_decompose_segments.py`
- 被 95-03 `engine._decompose` 接线消费
</output>
