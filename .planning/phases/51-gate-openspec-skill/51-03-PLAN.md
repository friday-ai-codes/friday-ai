---
phase: 51-gate-openspec-skill
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - task/core/config.py
  - task/core/executor.py
  - task/tests/test_config.py
  - task/tests/test_openspec_prompt.py
  - task/tests/test_callback.py
autonomous: true
requirements: [GATE-02]
must_haves:
  truths:
    - "task config 含 follow_openspec: bool=False 字段，读 FRIDAY_TASK_FOLLOW_OPENSPEC env（pydantic-settings 前缀映射，per D-51-4）"
    - "follow_openspec=True → _get_system_prompt 在既有 prompt 后追加 openspec 指引段（独立 helper，per D-51-4）"
    - "follow_openspec=False/缺省 → _get_system_prompt 与现状逐字一致（零回归，per D-51-5）"
    - "openspec 指引段指示 agent：本仓为 SDD/openspec 仓，编码遵循 openspec/ 下已批准 spec，优先查仓库内 openspec skill 与 spec 文档按 delta 实现"
  artifacts:
    - path: "task/core/config.py"
      provides: "TaskConfig.follow_openspec 字段"
      contains: "follow_openspec"
    - path: "task/core/executor.py"
      provides: "_get_system_prompt 条件追加 openspec 段 + 独立 _openspec_guidance helper"
      contains: "openspec"
    - path: "task/tests/test_openspec_prompt.py"
      provides: "follow_openspec=true 含 openspec 段 / false 逐字等现状守护"
      contains: "follow_openspec"
  key_links:
    - from: "task/core/executor.py"
      to: "TaskConfig.follow_openspec"
      via: "_get_system_prompt 读 self.config.follow_openspec 决定是否追加 openspec 段"
      pattern: "self\\.config\\.follow_openspec"
    - from: "task/core/executor.py setting_sources"
      to: "仓库内 .claude/skills"
      via: "setting_sources=[project] 既有原生加载（本 plan 不改，仅文档化复用）"
      pattern: "setting_sources"
---

<objective>
task 容器侧消费 openspec 信号（GATE-02 task 半，D-51-4）：① `TaskConfig` 加 `follow_openspec: bool=False`（读 `FRIDAY_TASK_FOLLOW_OPENSPEC` env，默认 False 零回归）；② `_get_system_prompt` 当 follow_openspec 为真时追加 openspec 指引段（独立 helper 便于测试），指示 agent 遵循 `openspec/` 下已批准 spec、优先查仓库内 openspec skill。仓库内 `.claude/skills` 由既有 `setting_sources=["project"]` 原生加载 —— 本 plan 不改加载逻辑，仅加 system_prompt 注入点（极小改动）。

Purpose: 让 server gate 放行的 approved SDD 仓在容器内真正「按 openspec 流程编码」，而非仅拦截。
Output: TaskConfig 字段 + _get_system_prompt 条件追加 + openspec 段 helper + true/false 守护测试 + 既有 MagicMock 测试零回归修正。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/51-gate-openspec-skill/51-CONTEXT.md
@task/core/config.py
@task/core/executor.py
@task/tests/test_config.py
@task/tests/test_callback.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TaskConfig.follow_openspec 字段（D-51-4）</name>
  <files>task/core/config.py, task/tests/test_config.py</files>
  <behavior>
    - TaskConfig 含 `follow_openspec: bool = Field(default=False, ...)`，经 env_prefix="FRIDAY_TASK_" 自动映射 FRIDAY_TASK_FOLLOW_OPENSPEC。
    - 缺省（未设 env / 未传参）→ False（零回归）。
    - 设 env FRIDAY_TASK_FOLLOW_OPENSPEC=true 或参数 follow_openspec=True → True；pydantic 布尔解析（"true"/"1" → True）。
  </behavior>
  <action>
    在 `TaskConfig` 的 Claude/SDD 相关字段区（紧邻 remote_tools / exclude_patterns 等 v0.9 扩展位）加 `follow_openspec: bool = Field(default=False, description="SDD/openspec 仓标记：为真时 system_prompt 追加 openspec 指引段（server gate 放行的 approved SDD 仓注入 FRIDAY_TASK_FOLLOW_OPENSPEC=true）")`。复用既有 model_config env_prefix 自动映射，无需 json_schema_extra。
  </action>
  <verify>
    <automated>cd task && uv run pytest tests/test_config.py -x -q</automated>
  </verify>
  <done>follow_openspec 默认 False；参数/env 置 True 生效；既有 config 测试零回归。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: _get_system_prompt 条件追加 openspec 指引段（独立 helper，D-51-4/D-51-5）</name>
  <files>task/core/executor.py, task/tests/test_openspec_prompt.py, task/tests/test_callback.py</files>
  <behavior>
    - 抽独立 helper（如 `_openspec_guidance() -> str`）返回 openspec 指引段文本：指示 agent 本仓为 SDD/openspec 仓，编码须遵循 `openspec/` 下已批准的 spec/change proposal，优先查阅仓库内 openspec skill 与 spec 文档，按 spec delta 实现。
    - `_get_system_prompt`：base prompt 不变；当 `bool(self.config.follow_openspec)` 为真时返回 base + "\n\n" + openspec 段；为假/缺省时返回 base（与现状逐字一致）。
    - 零回归：follow_openspec=False/缺省 → 返回值与改动前逐字相等（守护测试断言）。
  </behavior>
  <action>
    在 executor.py 加 `_openspec_guidance` helper（纯函数/方法返回固定中文指引段）。改 `_get_system_prompt`：保留现有 base prompt 字面量不动，末尾按 `if bool(self.config.follow_openspec): return base + "\n\n" + self._openspec_guidance()` 条件拼接，否则返回 base。
    ⚠ 既有测试坑（必修）：`test_callback.py::test_system_prompt_explains_runner_handles_git` 与同文件 run_execute_mode 用例以 `MagicMock()` 作 config —— MagicMock 的 `.follow_openspec` 属性默认是「真值 Mock」，会误触 openspec 追加破坏零回归断言。本 task 必须在这些 MagicMock-config 用例显式设 `config.follow_openspec = False`，使「不含 openspec 段 == 现状」断言成立。新建 test_openspec_prompt.py 用真实/Mock config 显式控两态。
    指引段文本不内联到 _get_system_prompt（保 helper 独立可测）；不改 setting_sources（.claude/skills 原生加载不动）。
  </action>
  <verify>
    <automated>cd task && uv run pytest tests/test_openspec_prompt.py tests/test_callback.py -x -q</automated>
  </verify>
  <done>follow_openspec=True → prompt 含 openspec 指引段（含 "openspec" / spec / 已批准 关键词）；False/缺省 → 与现状逐字等（base 不变）；既有 test_callback 系统提示词测试经显式 follow_openspec=False 修正后零回归通过。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FRIDAY_TASK_FOLLOW_OPENSPEC env → system_prompt | 仅布尔信号；误置真值会对非 SDD 仓追加 openspec 段（行为漂移），误置假值会让 SDD 仓不遵循 openspec |
| system_prompt 注入 | 追加段为静态可信文本，无外部输入拼接（无 prompt 注入面） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-PROMPT | Tampering | _get_system_prompt 条件追加 | mitigate | follow_openspec 默认 False；openspec 段为静态文本（无外部输入拼接）；零回归守护断言 false 路径逐字等现状 |
| T-51-MOCK | Tampering | 既有 MagicMock 测试 | mitigate | 显式设 config.follow_openspec=False，防 MagicMock 真值属性误触 openspec 追加致零回归断言失真 |
</threat_model>

<verification>
- `cd task && uv run ruff format --check . && uv run ruff check .`
- `cd task && uv run pytest tests/test_config.py tests/test_openspec_prompt.py tests/test_callback.py -q`
</verification>

<success_criteria>
- TaskConfig.follow_openspec 默认 False，env/参数可置 True。
- follow_openspec=True → system_prompt 含 openspec 指引段；False/缺省 → 与现状逐字一致。
- 既有 test_callback MagicMock 用例经显式 follow_openspec=False 修正零回归。
- `.claude/skills` 由既有 setting_sources=["project"] 原生加载（本 plan 不改，已文档化复用）。
- 映射：GATE-02（task system_prompt 注入点 + 复用 setting_sources 原生加载 skill）；ROADMAP 成功标准 3（容器注入 openspec 指引，编码遵循 openspec 流程）。
</success_criteria>

<output>
Create `.planning/phases/51-gate-openspec-skill/51-03-SUMMARY.md` when done
</output>
