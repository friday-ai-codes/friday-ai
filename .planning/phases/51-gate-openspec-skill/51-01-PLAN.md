---
phase: 51-gate-openspec-skill
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/delivery/services/repo_coding_task_service.py
  - server/tests/delivery/test_repo_coding_task_service.py
  - server/tests/delivery/test_repo_coding_task_inv6_guard.py
autonomous: true
requirements: [GATE-01]
must_haves:
  truths:
    - "建任务时 SDD 仓（Repository.facets.methodology==SDD）的 RepoCodingTask.follow_openspec=True，非 SDD 仓=False（首次消费 v0.8 预留字段，per D-51-1）"
    - "已存在 task 的 follow_openspec 漂移（facets 后打标）被回填，幂等不重复写（per D-51-1）"
    - "mark_gate_blocked 条件 pending→failed + error={reason:spec_not_approved, spec_status:<status|missing>}，仅 pending 生效，非 pending no-op（per D-51-3）"
    - "gate 阻断写入只经 RepoCodingTaskService，INV-6 grep 守护无旁路（per D-51-6）"
  artifacts:
    - path: "server/delivery/services/repo_coding_task_service.py"
      provides: "create_tasks_for_plan follow_openspec 置位 + 新增 mark_gate_blocked"
      contains: "def _mark_gate_blocked_sync"
    - path: "server/tests/delivery/test_repo_coding_task_service.py"
      provides: "follow_openspec 置位（SDD/非SDD/漂移回填）+ mark_gate_blocked 条件更新/幂等守护"
      contains: "follow_openspec"
  key_links:
    - from: "server/delivery/services/repo_coding_task_service.py"
      to: "repositories.Repository.facets"
      via: "create_tasks_for_plan 同步块内按 repo_id 查 facets.methodology==SDD"
      pattern: "methodology"
    - from: "server/delivery/services/repo_coding_task_service.py"
      to: "RepoCodingTask status 条件更新"
      via: "mark_gate_blocked .filter(status=pending).update(status=failed, error=...)"
      pattern: "filter\\([^)]*PENDING[^)]*\\)\\.update"
---

<objective>
为编码前置 gate 准备 service 层底座（GATE-01）：① `RepoCodingTaskService.create_tasks_for_plan` 建任务时按仓库 SDD 标记置位 `follow_openspec`（v0.8 预留字段首次消费，D-51-1）；② 新增 `mark_gate_blocked(task, reason, spec_status)` —— gate 拦截的唯一写入入口（条件 pending→failed + 结构化 error，D-51-3/INV-6）。

Purpose: gate 的「判定结果落库」与「follow_openspec 标记来源」都收口到唯一 service，杜绝旁路写、保幂等与下游阻断可达性。
Output: service 两处改动 + follow_openspec 置位/漂移回填测试 + mark_gate_blocked 条件更新/幂等测试 + INV-6 grep 守护通过。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/51-gate-openspec-skill/51-CONTEXT.md
@server/delivery/services/repo_coding_task_service.py
@server/delivery/models/repo_coding_task.py
@server/tests/delivery/test_repo_coding_task_service.py
@server/tests/delivery/test_repo_coding_task_inv6_guard.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: create_tasks_for_plan 按仓库 SDD 标记置位 follow_openspec（D-51-1）</name>
  <files>server/delivery/services/repo_coding_task_service.py, server/tests/delivery/test_repo_coding_task_service.py</files>
  <behavior>
    - SDD 仓置位：repo_id 对应 Repository.facets.get("methodology")=="SDD" → 新建 RepoCodingTask 时 defaults 写 follow_openspec=True；非 SDD（facets 缺失/methodology!=SDD）→ follow_openspec=False。
    - 漂移回填幂等：已存在 task（get_or_create 命中 not created）且其 follow_openspec 与当前 facets 推导值不一致 → 回填该字段（与既有 wave 漂移回填同范式：相等不写）；重复调用不重复写。
    - async ORM 安全：facets 查询在 `_create_tasks_sync` 同步块内做（`Repository.objects.filter(id=repo_id).values_list("facets", flat=True).first()` 或等价），用 *_id 标量，绝不裸 lazy-FK（D-51-6）。
    - 零回归：非 SDD 仓行为与现状一致（follow_openspec 默认 False，wave/depends_on 边逻辑不变）。
  </behavior>
  <action>
    在 `_create_tasks_sync` 同步块内为每个 repo_id 推导 SDD 标记：查 Repository.facets（标量字段，同步上下文安全），`is_sdd = (facets or {}).get("methodology") == "SDD"`。get_or_create 的 defaults 增加 `"follow_openspec": is_sdd`。新建分支无需额外写。已存在分支（not created）在既有 wave 漂移回填逻辑旁，增加 follow_openspec 漂移回填：当 `task.follow_openspec != is_sdd` 时一并 save（合并到同一 save 的 update_fields，避免两次写）。复用既有 `Repository` import 或在同步块顶部 lazy import `from repositories.models import Repository`。保持「相等不写」幂等。不改 depends_on 同步块逻辑。
    注意：facets 字段为 JSON（dict），缺省可能为 {} 或 None —— 用 `(facets or {})` 兜底。methodology 值大小写以 Phase 48 检测器写入为准（"SDD" 大写，见 SDD-01）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/delivery/test_repo_coding_task_service.py -x -q</automated>
  </verify>
  <done>SDD 仓 follow_openspec=True、非 SDD=False、漂移回填幂等三组断言通过；既有 create_tasks_for_plan 幂等/wave/边测试零回归。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 新增 mark_gate_blocked 单一写入入口（D-51-3 / INV-6）</name>
  <files>server/delivery/services/repo_coding_task_service.py, server/tests/delivery/test_repo_coding_task_service.py, server/tests/delivery/test_repo_coding_task_inv6_guard.py</files>
  <behavior>
    - mark_gate_blocked(task, reason, spec_status) → 条件 `.filter(id=task.id, status=PENDING).update(status=FAILED, error={"reason": reason, "spec_status": spec_status}, updated_at=now)`，返回影响行数（对齐 mark_running/mark_blocked 范式）。
    - 仅 pending→failed 生效：task 已 running/done/failed → 影响 0 行 no-op（不强翻在途/终态，防覆盖在途结果）。
    - error 结构：reason 形如 "spec_not_approved"（或 gate 异常路径的 "gate_error"，由调用方 51-02 传入），spec_status 为关联 spec 当前状态字符串或 "missing"。
    - 幂等：重复调用同一已 blocked task → 影响 0 行（status 已 failed 不再匹配 pending）。
  </behavior>
  <action>
    在 `RepoCodingTaskService` 加 `async def mark_gate_blocked(self, task, reason, spec_status)` + `@sync_to_async def _mark_gate_blocked_sync(...)`，逐字镜像 `_mark_blocked_sync` 的条件更新形（仅 error payload 不同：携带 reason + spec_status，而非 upstream_failed）。返回影响行数。docstring 说明：gate 拦截唯一写入入口，对齐既有 mark_* 幂等条件更新范式。不新增任何模型层方法。
    INV-6 守护：新增写入仍在唯一 allowed writer（repo_coding_task_service.py）内，既有 `test_repo_coding_task_inv6_guard.py` 应自动覆盖通过；在 `test_inv6_writer_actually_writes` 不变前提下，仅需确认 grep 守护无新违反（无需改守护逻辑）。如守护需补断言 mark_gate_blocked 经 service，可在该测试加一条「service 含 mark_gate_blocked 定义」正向断言。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/delivery/test_repo_coding_task_service.py tests/delivery/test_repo_coding_task_inv6_guard.py -x -q</automated>
  </verify>
  <done>mark_gate_blocked：pending→failed+error 结构正确、非 pending no-op、重复 no-op 三组断言通过；INV-6 grep 守护全绿（无旁路写）。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 仓库 facets → follow_openspec 标记 | facets 由 Phase 48 索引检测写入，是 gate 判定的「是否受治理」来源；标记缺失=漏判（SDD 仓被当普通仓放行） |
| 任务状态写入 | 仅 RepoCodingTaskService 可写；旁路写会绕过 gate 终态语义 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-01 | Tampering | follow_openspec 置位 | mitigate | facets 读取在同步块、`(facets or {})` 兜底，避免 None 致异常误判；置位逻辑幂等回填防漂移漏标 |
| T-51-02 | Elevation | RepoCodingTask 状态写入 | mitigate | mark_gate_blocked 经唯一 service + 条件更新；INV-6 grep 守护断言无旁路写 |
| T-51-03 | Repudiation | gate 阻断诊断 | mitigate | error 落 {reason, spec_status} 结构化诊断，操作者可追溯阻断原因（不静默） |
</threat_model>

<verification>
- `cd server && uv run ruff format --check . && uv run ruff check .`
- `cd server && uv run pytest tests/delivery/test_repo_coding_task_service.py tests/delivery/test_repo_coding_task_inv6_guard.py -q`
- `cd server && uv run python manage.py makemigrations --check --dry-run`（应无新 migration —— follow_openspec 字段 v0.8 已存在，本 plan 仅消费）
</verification>

<success_criteria>
- create_tasks_for_plan 对 SDD 仓置 follow_openspec=True、非 SDD=False、漂移回填幂等。
- mark_gate_blocked 仅 pending→failed + 结构化 error，非 pending/重复 no-op。
- INV-6 grep 守护通过；ruff + 目标 pytest 全绿；makemigrations --check 无新增 migration。
- 映射：GATE-01（gate 判定结果落库 + follow_openspec 标记来源）；ROADMAP 成功标准 1（未批准拦截需「如实标注阻断原因」—— 本 plan 提供 error 诊断载体）。
</success_criteria>

<output>
Create `.planning/phases/51-gate-openspec-skill/51-01-SUMMARY.md` when done
</output>
