---
phase: 41-hitl-taxonomy
plan: 02
type: execute
wave: 2
depends_on: ["41-01"]
files_modified:
  - server/delivery/models/clarification.py
  - server/delivery/models/__init__.py
  - server/delivery/migrations/0016_clarification.py
  - server/delivery/services/clarification_service.py
  - server/delivery/services/__init__.py
  - server/services/plan_orchestration/clarify_adapter.py
  - server/services/plan_orchestration/protocols.py
  - server/services/plan_orchestration/__init__.py
  - server/services/plan_orchestration/engine.py
  - server/tests/delivery/test_clarification_service.py
  - server/tests/services/test_engine_clarify.py
autonomous: true
requirements: [CLARIFY-01]
must_haves:
  truths:
    - "不清晰时编排建 Clarification(pending) + emit clarification.asked + 保持 clarifying 挂起（不 transition）"
    - "回答后 engine 重入 _clarify：pending 清空 → clarified → researching"
    - "回答后仅 affected_partials 内的 RepoResearchTask 经 stale 机制重跑，其余 partial 复用"
    - "无待澄清/全部已答路径直通 researching（pass-through 行为保留）"
    - "Clarification 落库/状态变更只经 ClarificationService（INV-6）"
  artifacts:
    - path: "server/delivery/models/clarification.py"
      provides: "Clarification 模型（§6 字段 + affected_partials M2M）"
      contains: "class Clarification"
    - path: "server/delivery/services/clarification_service.py"
      provides: "Clarification 唯一写入入口（create/answer + stale 重跑联动）"
      contains: "class ClarificationService"
    - path: "server/services/plan_orchestration/clarify_adapter.py"
      provides: "ClarifyProtocol 真实实现（needs-clarification policy 可注入）"
      contains: "class ClarifyAdapter"
  key_links:
    - from: "server/delivery/services/clarification_service.py"
      to: "ResearchService.retry_task"
      via: "answer 后置 affected_partials 的 task stale 重跑"
      pattern: "ResearchService|retry_task|invalidate"
    - from: "server/services/plan_orchestration/engine.py"
      to: "server/services/plan_orchestration/clarify_adapter.py"
      via: "engine._clarify 调注入 ClarifyProtocol"
      pattern: "self.clarify"
---

<objective>
实现 HITL 澄清回路（CLARIFY-01，DOMAIN §6/§14）：编排在不清晰时发 `Clarification` 挂起等用户，回答后仅 `affected_partials` 内的 `RepoResearchTask` 重跑、其余 partial 复用。
建 `Clarification` 模型（delivery）+ migration 0016 + `ClarificationService`（INV-6 单一写入入口）；把骨架 `engine._clarify`（pass-through）替换为接真实可注入 `ClarifyProtocol`——按 needs-clarification policy 判定：需澄清则建 pending Clarification + emit `clarification.asked` + 保持 clarifying 挂起（不转移）；否则（无待澄清/全部已答）转移 `clarified`（clarifying→researching）。回答经 `ClarificationService.answer_clarification` → 把 affected_partials 对应 RepoResearchTask 经 Phase 39 stale 机制置失效重跑 + emit `clarification.answered`。

Purpose: 补齐编排的人在回路灰区收敛，避免对模糊需求盲跑，且回答后增量重跑（不重跑整 session）。
Output: `Clarification` 模型 + 0016 迁移 + `ClarificationService` + `ClarifyProtocol`/`ClarifyAdapter` + 接真实的 `engine._clarify`。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/DOMAIN-MODEL.md
@.planning/phases/41-hitl-taxonomy/41-CONTEXT.md

@server/delivery/models/plan_session.py
@server/delivery/models/research_task.py
@server/delivery/services/research_service.py
@server/delivery/services/plan_session_service.py
@server/services/plan_orchestration/engine.py
@server/services/plan_orchestration/protocols.py
@server/services/plan_orchestration/research_aggregation.py

# 41-01 产出 §15 事件常量（clarification.asked/answered）
@.planning/phases/41-hitl-taxonomy/41-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clarification 模型 + migration 0016 + curated re-export</name>
  <files>server/delivery/models/clarification.py, server/delivery/models/__init__.py, server/delivery/migrations/0016_clarification.py</files>
  <action>
新建 `server/delivery/models/clarification.py`（zh-CN docstring，DOMAIN §6/§12，CONTEXT 决策字段）：
- `id = UUIDField(primary_key=True, default=uuid4, editable=False)`
- `session = ForeignKey("delivery.PlanSession", on_delete=CASCADE, related_name="clarifications")`
- `question = TextField()`
- `answer = TextField(blank=True, default="")`
- `answered_at = DateTimeField(null=True, blank=True)`
- `affected_partials = ManyToManyField("delivery.RepoResearchTask", blank=True, related_name="+")`（回答后哪些 task 要重跑；related_name="+" 不污染 RepoResearchTask 反查）
- `created_at = DateTimeField(auto_now_add=True)`
- `Meta`: `db_table="delivery_clarification"`，`ordering=["created_at"]`，`indexes=[Index(fields=["session"])]`。
模型层**不写**任何 create/save/answer 业务方法（写入只经 `ClarificationService`，INV-6）。状态语义：clarification pending = 存在 `answered_at IS NULL` 的 Clarification（由 service/engine 判定，不在模型上加方法）。

`server/delivery/models/__init__.py`：`from delivery.models.clarification import Clarification` + 加入 `__all__`。

生成迁移：`cd server && uv run python manage.py makemigrations delivery` → 产出 `0016_clarification.py`（dependencies 指向 `0015_plansessionevent`，**编号协调**：0015=PlanSessionEvent（41-01），0016=Clarification（本 plan））。M2M 中间表随 makemigrations 自动生成。
  </action>
  <verify>
    <automated>cd server && uv run python manage.py makemigrations delivery --check --dry-run</automated>
  </verify>
  <done>0016 迁移生成且 `makemigrations --check` 干净；`from delivery.models import Clarification` 可导入；M2M `affected_partials` 中间表已建。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ClarificationService（INV-6 单一写入入口 + affected_partials stale 重跑联动）</name>
  <files>server/delivery/services/clarification_service.py, server/delivery/services/__init__.py, server/tests/delivery/test_clarification_service.py</files>
  <behavior>
    - `create_clarification(session, question, affected_task_ids)` → 建 Clarification(answered_at=None) + 设 affected_partials M2M（按 task_ids），返回实例；pending 判定为真。
    - `answer_clarification(clarification, answer)` → 写 `answer`/`answered_at=now`；对 affected_partials 对应的 RepoResearchTask 经 Phase 39 stale 机制重跑（其余 task 不动）；无 affected_partials 时纯解除挂起（不触任何 task）。
    - INV-6：Clarification 落库/状态变更仅经本 service（grep 守护断言无旁路 `Clarification.objects.create`/`.save(` 出现在 service 外）。
  </behavior>
  <action>
新建 `server/delivery/services/clarification_service.py`（zh-CN docstring，对齐 `PlanSessionService`/`ResearchService` 单一写入范式，async ORM 经 `sync_to_async`，禁裸 lazy-FK——规避 Phase 38 CR-01 类，用 `*_id`/`.values`/`afirst`）：
- `class ClarificationService`：
  - `async def create_clarification(self, session, question, affected_task_ids) -> Clarification`：`sync_to_async` 内 `Clarification.objects.create(session=session, question=question)`，再 `clar.affected_partials.set(affected_task_ids)`（task_ids 为 RepoResearchTask id 列表）。返回 clar。
  - `async def answer_clarification(self, clarification, answer) -> Clarification`：条件更新 `answer`/`answered_at=timezone.now()`（镜像 PlanSessionService 条件更新断言风格：仅 `answered_at IS NULL` 可答，重复答幂等 no-op 或 raise——选幂等 no-op 更安全）；取 affected_partials 的 task（`.values_list("id", flat=True)`），对每个经 **`ResearchService`** 置 stale/重跑：复用 39-02 既有机制——优先 `ResearchService.invalidate_for_repo` 不适用（那是按 repo 重索引）；澄清场景应直接把指定 task 置 stale 再由 researching 重派。**实现选择（Claude's Discretion）**：在 `ResearchService` 复用既有 `retry_task`（failed/stale→pending）不直接适用 done task；故 answer 流程对 affected 的 done task 先标 stale（新增窄方法 `ResearchService.mark_stale_for_clarification(task_ids)` 或复用 invalidate 语义），使其满足 §14「stale 须重跑后才满足 barrier」。**若需在 ResearchService 增窄方法**：加 `async def mark_stale(self, task_ids: list)` —— 把指定 RepoResearchTask 关联 valid PartialPlan 置 `valid=False, invalidated_reason="clarification"` + task→stale（幂等，仅 done/其余 → stale），只触指定 task，绝不动其他。该窄方法落在 `research_service.py`（视为本 plan files_modified 之外的既有文件最小扩展——若执行时发现 39-02 已有等价入口则直接复用，不重复造）。
  - 无 affected_partials → 跳过 stale 联动（纯解除挂起）。
说明：affected_partials 为空或对应 task 已非 done/有效时安全跳过；所有写经 service，engine/节点不旁路。

`server/delivery/services/__init__.py`：re-export `ClarificationService` + 加入 `__all__`。

测试 `server/tests/delivery/test_clarification_service.py`（`@pytest.mark.django_db @pytest.mark.asyncio`，真实 PlanSession/RepoResearchTask/PartialPlan）：
- `create_clarification` → DB 有 pending Clarification（answered_at None）+ affected_partials 正确关联。
- `answer_clarification` → answer/answered_at 落库；affected 的 task 置 stale + 其 valid PartialPlan invalidated；**非 affected 的 task/partial 不变**（复用断言）。
- 无 affected_partials → answer 仅写字段、不触任何 task。
- INV-6 grep 守护：扫描 server/ 断言 `Clarification.objects.create`/`Clarification(...).save` 仅出现在 `clarification_service.py`（过滤注释 + 测试目录）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/delivery/test_clarification_service.py -x -q</automated>
  </verify>
  <done>create/answer 经 service 单一入口；回答后仅 affected_partials 重跑（stale）、其余复用；INV-6 grep 守护绿；测试全绿。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ClarifyProtocol + ClarifyAdapter + engine._clarify 接真实</name>
  <files>server/services/plan_orchestration/clarify_adapter.py, server/services/plan_orchestration/protocols.py, server/services/plan_orchestration/__init__.py, server/services/plan_orchestration/engine.py, server/tests/services/test_engine_clarify.py</files>
  <behavior>
    - needs-clarification = True（默认 policy：routing 无 high/medium 候选 **或** decomposition 标 ambiguous）→ ClarifyAdapter 建 pending Clarification + emit clarification.asked + 返回 `{"needs_clarification": True, "clarification_id": ...}`；engine 经 transition `needs_clarification`（clarifying→clarifying 自挂起），**不进 researching**。
    - needs-clarification = False（无待澄清 pending 且无 ambiguous）→ 返回 `{"needs_clarification": False}`；engine transition `clarified`（clarifying→researching）。
    - resume 重入：存在已 answered 的 Clarification 且无 pending → needs_clarification False → 直通 researching。
    - emit clarification.answered 由 answer 流程（节点/service 侧）触发，payload `{clarification_id, answer, affected_partials}`。
  </behavior>
  <action>
在 `protocols.py` 增 `ClarifyProtocol`（`async def clarify(self, session) -> dict`）+ `SkeletonClarify`（沿用其他 Skeleton 的 NotImplementedError 范式，附 Phase 41 TODO）。`engine.py` 构造器加 `clarify: ClarifyProtocol | None = None` 注入位，缺省 `SkeletonClarify()`。

新建 `server/services/plan_orchestration/clarify_adapter.py`（zh-CN docstring，CLARIFY-01）：
- `NeedsClarificationPolicy` 可注入纯函数/协议（CONTEXT Claude's Discretion）：默认 `default_needs_clarification(session) -> tuple[bool, str, list]`——判定规则：
  - routing 候选中无任一 confidence ∈ {high, medium}（`(session.routing or {}).get("candidates", [])`）→ 需澄清，question="未能高/中置信路由到候选仓，请补充涉及的仓库/模块"。
  - decomposition 标 ambiguous（`(session.decomposition or {}).get("ambiguous")` 真）→ 需澄清，question 取 decomposition 提示。
  - 否则不需澄清。affected_task_ids：默认空（澄清答复后由 policy/answer 端决定影响面；本 phase 默认全 affected 或空——空表示纯解除挂起后全量按现状继续）。
- `class ClarifyAdapter`（满足 ClarifyProtocol，注入 `policy`、`clarification_service=ClarificationService()`、`session_service=PlanSessionService()`）：
  - `async def clarify(self, session) -> dict`：
    1. 先查是否已有 pending Clarification（`Clarification.objects.filter(session_id=session.id, answered_at__isnull=True).aexists()`）——**有 pending 仍未答** → 返回 `{"needs_clarification": True, "pending": True}`（保持挂起，不重复建）。
    2. 无 pending → 跑 policy；需澄清 → `create_clarification` + emit `EVENT_CLARIFICATION_ASKED`（41-01 常量，payload `{clarification_id, question}`，best-effort）+ 返回 `{"needs_clarification": True, "clarification_id": str(clar.id)}`。
    3. 不需澄清 → 返回 `{"needs_clarification": False}`。
  async ORM 禁裸 lazy-FK（用 `session_id`/`.aexists`/`.values`）。

替换 `engine._clarify`（移除 pass-through TODO）：
- `result = await self.clarify.clarify(session)`；`if result.get("needs_clarification"): await self.session_service.transition(session, "needs_clarification")`（clarifying→clarifying 自挂起，§14「有待澄清」行）；`else: await self.session_service.transition(session, "clarified")`（→researching）。
- `ConcurrentTransitionError` 沿 `_research`/`_merge` 范式视为良性 no-op（防御）。
- `clarify` 真实 adapter 抛异常时由 advance 通用 except 落 failed（与其他 stage 一致）；不再主动抛 NotImplementedError。

`__init__.py` re-export `ClarifyProtocol`/`SkeletonClarify`/`ClarifyAdapter`/`default_needs_clarification`。

测试 `server/tests/services/test_engine_clarify.py`（`@pytest.mark.django_db @pytest.mark.asyncio`，真实 PlanSession/PlanSessionService，clarify 用真实 ClarifyAdapter + mock policy 或注入 AsyncMock ClarifyProtocol）：
- pending 路径：注入 policy 判需澄清 → advance(clarifying) → status 仍 clarifying（未转 researching）+ DB 有 pending Clarification + emit clarification.asked（spy `_emit_event`）。
- 已答路径：先建已 answered Clarification（无 pending）→ advance(clarifying) → status=researching（clarified 转移）。
- 无澄清路径：policy 判不需 + 无 pending → advance(clarifying) → status=researching（pass-through 行为保留）。
- engine 纯度：断言 status 变更只经 transition（不直接 mutate；可断言 `_ALLOWED` 路径生效）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/test_engine_clarify.py tests/services/test_plan_orchestration_engine.py -q</automated>
  </verify>
  <done>pending clarification 挂起（不转移）；已答/无澄清直通 researching；emit clarification.asked；engine 经 transition 推进（纯度守护绿）；测试全绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 用户答复 → answer_clarification | 用户自由文本答复跨入编排（半可信） |
| ClarifyAdapter policy → Clarification 落库 | 自动判定触发挂起的决策点 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-02-01 | Tampering | Clarification 写入 | mitigate | 落库/状态变更只经 `ClarificationService`（INV-6），grep 守护断言无旁路 create/save；engine 不旁路改 session.status（只经 transition） |
| T-41-02-02 | Elevation of Privilege | answer 重答覆盖 | mitigate | `answer_clarification` 以 `answered_at IS NULL` 为前置条件（镜像 PlanSessionService 条件更新断言），重复答幂等 no-op，不二次覆盖首答 |
| T-41-02-03 | Denial of Service | affected_partials 重跑面 | mitigate | 仅对 affected_partials 指定 task 置 stale 重跑，**绝不触其他 task**（§14 增量重跑）；非 researching session 下 ResearchService 既有 IN-01 前置校验拒绝越状态复位 |
| T-41-02-04 | Information Disclosure | clarification.asked payload | mitigate | payload 仅 `{clarification_id, question}`（§15），不含模型 CoT/内部推理（INV-5） |
</threat_model>

<verification>
- `cd server && uv run python manage.py makemigrations --check --dry-run` 干净（0015+0016 均已落）。
- 单测绿：`test_clarification_service.py`、`test_engine_clarify.py` + 既有 `test_plan_orchestration_engine.py` 无回归。
- INV-6 grep 守护绿（Clarification 写入唯一入口）。
- `ruff` line 100 + zh-CN docstring 通过。
</verification>

<success_criteria>
- 不清晰时建 Clarification 挂起（emit clarification.asked，不转移到 researching）。
- 回答后仅 affected_partials 重跑、其余 partial 复用（§14）。
- 无澄清/全部已答路径直通 researching。
- Clarification 写入只经 ClarificationService（INV-6）；engine 经 transition 推进（纯度）。
</success_criteria>

<output>
Create `.planning/phases/41-hitl-taxonomy/41-02-SUMMARY.md` when done.
</output>
