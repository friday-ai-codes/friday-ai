---
phase: 08-iso
plan: 04
type: execute
wave: 4
depends_on: ["08-02", "08-03"]
files_modified:
  - server/chat/views.py
autonomous: true
requirements: [ISO-02, ISO-03, ISO-04]
must_haves:
  truths:
    - "关联模型端点 #13-25（coding-session/plan + trace/clarification）全部经 owner gate via .conversation"
    - "当前无任何 owner 校验的 coding-session 端点 #13-23 越权他人会话 → 404"
    - "trace/clarification #24-25 的 owner gate 无 superuser bypass（替换旧 has_project_access 语义）"
    - "list 型 #13/#20 用他人 conversation_id → 返回 []（不列他人）"
  artifacts:
    - path: "server/chat/views.py"
      provides: "#13-25 端点 owner gate（resolve .conversation → created_by_id 比对 → 404）"
      contains: "created_by_id"
  key_links:
    - from: "server/chat/views.py CodingSessionDetailView.get"
      to: "session.conversation.created_by_id"
      via: "select_related('conversation') then owner compare → 404"
      pattern: "created_by_id"
---

<objective>
把 RESEARCH 端点清单 C/D 组（关联模型端点 #13-25）补齐 owner gate：coding-session #13-19、coding-plan #20-23、routing-trace override #24、clarification answer #25。这些端点经 FK 链 `session.conversation` / `plan.conversation` / `trace.conversation` 触达 Conversation，其中 #13-23 当前**完全没有**会话归属校验（任何已认证用户可读/操作他人 coding session），是本阶段最大的横向越权面。

Purpose: ISO-04 是 all-or-nothing；这些是清单里最易被忽略的一组（无既有 owner 检查）。全部用 `select_related("conversation")` 取关联行后比对 `conversation.created_by_id != request.user.id` → 404；#24/#25 的旧 `has_project_access`(superuser bypass) 必须替换为 owner gate（ISO-03 无 bypass、ISO-04 404）。
Output: views.py 中 13 个端点接线 owner gate。落地后 08-01 中 #13-25 + list-scoping 用例转 GREEN，全阶段隔离套件全绿。
</objective>

<execution_context>
@.cursor/gsd-core/workflows/execute-plan.md
@.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-iso/08-RESEARCH.md
@.planning/phases/08-iso/08-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: coding-session 端点 #13-19 owner gate via session.conversation</name>
  <files>server/chat/views.py</files>
  <read_first>
    - server/chat/views.py L1976-2027（`CodingSessionListView` #13、`CodingSessionDetailView` #14）、L1560-1683（`CodingSessionConfirmView` #15）、L1686-1773（`CommitConfirmView` #16）、L1776-1926（`PRConfirmView` #17）、L1929-1949（`ConflictCheckView` #18）、L1952-1973（`DiffSummaryView` #19）
    - server/chat/models.py L278-300（`CodingSession.conversation` FK）
    - 08-RESEARCH.md §Access Inventory C #13-19、§Pattern 4、§Pitfall 1/4（这些端点今无 owner 检查；用 select_related + created_by_id）
  </read_first>
  <action>
    为 #14-19 在 `CodingSession.objects.aget(id=session_id)` 处加 `.select_related("conversation")`（#15 已 select_related `conversation__project`，沿用），取到后立即比对：`if getattr(request.user,"is_authenticated",False) and coding_session.conversation.created_by_id != request.user.id: return Response(..., status=404)`（用 `created_by_id` 避免 async 惰性 FK；在读取任何其它字段/状态机校验之前）。404 body 与既有「CodingSession not found」一致以不泄漏存在性。
    #13 list（`CodingSessionListView.get`）：取 `conversation_id` 后，把存在性 `Conversation.objects.aget(...)` 换成 owner-scoped（`ConversationService.aget_for_user(conversation_id, request.user)`），越权/不存在 → 返回 `[]`（保持既有「missing 返回 []」语义，不列他人）。
    全部 404/[]，不加 is_superuser 分支。
  </action>
  <acceptance_criteria>
    - #14-19：B JWT 访问 A 的 coding-session id → 404；#13：B 传 A 的 conversation_id → `[]`
    - owner 比对用 `created_by_id`，置于状态机/字段读取之前
    - 这些段不含 `is_superuser`
    - 08-01 用例 #13-19 cross-user-denied + list-scoping(coding-sessions) GREEN
  </acceptance_criteria>
  <verify>
    <automated>cd server && uv run pytest tests/test_conversation_isolation.py -q -k "coding_session or session"</automated>
  </verify>
  <done>#13-19 经 owner gate，越权 404/[]，无 bypass。</done>
</task>

<task type="auto">
  <name>Task 2: coding-plan 端点 #20-23 owner gate via plan.conversation</name>
  <files>server/chat/views.py</files>
  <read_first>
    - server/chat/views.py L2030-2059（`CodingPlanListView` #20）、L2062-2082（`CodingPlanDetailView` #21）、L2085-2167（`CodingPlanSessionsBatchCreateView` #22：含 `has_project_access(MEMBER)` + superuser bypass → 403）、L1472-1552（`ExportCodingPlanToFeishuView` #23）
    - server/chat/models.py L159-211（`CodingPlan.conversation` FK）
    - 08-RESEARCH.md §Access Inventory C #20-23、§Anti-Patterns（#22 旧 403+bypass 须被 owner gate 前置覆盖为 404）
  </read_first>
  <action>
    - #21 detail（`CodingPlanDetailView.get`）：`CodingPlan.objects.select_related("conversation").aget(id=plan_id)`，比对 `plan.conversation.created_by_id != request.user.id` → 404。
    - #23 export（`ExportCodingPlanToFeishuView.post`）：已 select_related `conversation__project`，在读取 project 之前加同样 owner gate → 404。
    - #20 list（`CodingPlanListView.get`）：把存在性 `Conversation.objects.aget(...)` 换成 `aget_for_user(conversation_id, request.user)`，越权/不存在 → `[]`。
    - #22 batch-create（`CodingPlanSessionsBatchCreateView.post`）：在既有 `has_project_access(MEMBER)`(superuser bypass→403) **之前**插入 owner gate（`plan.conversation.created_by_id != request.user.id → 404`，已 select_related `conversation__project`）。owner-miss 必须先 404；保留 has_project_access 为 null-owner 次层。
    全部 404/[]，owner gate 段不加 is_superuser。
  </action>
  <acceptance_criteria>
    - #21/#22/#23：B JWT 操作 A 的 coding-plan id → 404；#20：B 传 A 的 conversation_id → `[]`
    - #22 owner gate 先于 has_project_access 执行，owner-miss → 404（不是 403）
    - owner gate 段不含 `is_superuser`
    - 08-01 用例 #20-23 cross-user-denied + list-scoping(coding-plans) GREEN
  </acceptance_criteria>
  <verify>
    <automated>cd server && uv run pytest tests/test_conversation_isolation.py -q -k "coding_plan or plan"</automated>
  </verify>
  <done>#20-23 经 owner gate，越权 404/[]，#22 主层覆盖旧 403。</done>
</task>

<task type="auto">
  <name>Task 3: trace/clarification #24-25 owner gate（替换 superuser-bypass 语义）</name>
  <files>server/chat/views.py</files>
  <read_first>
    - server/chat/views.py L2175-2261（`RoutingTraceManualOverrideView` #24：今 `has_project_access("member")` + `if not is_superuser` → 404）、L2269-2435（`ClarificationAnswerView` #25：同模式）
    - server/chat/models.py L507-572（`RepositoryRoutingTrace.conversation`）、L601-672（`ConversationIntentTrace.conversation`）
    - 08-RESEARCH.md §Access Inventory D #24-25、§Pitfall 2（#24/#25 已是 404 但带 superuser bypass，须去 bypass 改为 created_by owner gate）
  </read_first>
  <action>
    - #24 routing-trace override：`original` 已 select_related `conversation__project`。把现有 `if not getattr(user,"is_superuser",False): has_project_access(...)` 越权块替换/前置为 owner gate：`if getattr(user,"is_authenticated",False) and original.conversation.created_by_id != user.id: return Response(status=404)`（保留越权审计 log）。去掉 owner 判定中的 superuser bypass（ISO-03）。既有 has_project_access 可保留为 null-owner 次层但不得 bypass owner gate。
    - #25 clarification answer：`trace` 已 select_related `conversation__project`。同样把 superuser-bypass 的 has_project_access 块替换/前置为 owner gate：`trace.conversation.created_by_id != user.id → 404`（404 body 与「clarification 不存在或已过期」一致）。owner-miss 必须在落库/resume 之前 404。
    全部 404，owner gate 无 is_superuser 分支。
  </action>
  <acceptance_criteria>
    - #24/#25：B JWT 操作 A 会话下的 trace/clarification → 404
    - owner gate 用 `created_by_id`，无 `is_superuser` bypass（superuser 作为认证用户操作他人 → 404）
    - 越权审计 log 仍记录
    - 08-01 用例 #24/#25 cross-user-denied + admin-no-bypass GREEN；全阶段隔离套件全绿
  </acceptance_criteria>
  <verify>
    <automated>cd server && uv run pytest tests/test_conversation_isolation.py -q</automated>
  </verify>
  <done>#24-25 owner gate 去 superuser bypass，越权 404；隔离套件全绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 已认证用户 → 关联模型端点 | B 持自己 JWT 经 coding-session/plan/trace id 触达 A 的会话（#13-25 横向越权面） |
| 旧 has_project_access | superuser bypass + 403/404 语义与 ISO 相反，须被 owner gate 前置覆盖 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-12 | Elevation of Privilege | coding-session #13-23（今无 owner 检查） | mitigate | select_related("conversation") + created_by_id 比对 → 404；list → [] |
| T-08-13 | Information Disclosure | 403-vs-404（#22 旧 403） | mitigate | owner gate 先于 has_project_access，owner-miss 统一 404 |
| T-08-14 | Elevation of Privilege | 管理员 over-reach（#24/#25 旧 bypass） | mitigate | 去除 owner 判定中的 is_superuser bypass（ISO-03） |
| T-08-15 | Denial of Service | async FK 惰性访问 | mitigate | 用 select_related + created_by_id，避免 SynchronousOnlyOperation |
| T-08-SC | Tampering | 依赖安装 | accept | 零新增依赖（纯 view 接线） |
</threat_model>

<verification>
- `cd server && uv run pytest tests/test_conversation_isolation.py -q` 全绿（全部 25 路径 cross-user-denied GREEN）
- 全套回归：`cd server && uv run pytest tests/test_chat_views.py tests/test_conversation_integration.py tests/test_conversation_facade.py tests/test_coding_session_service.py -q`
- `cd server && uv run python manage.py makemigrations --check --dry-run` 退出 0
- views.py 中 #13-25 owner gate 段无 `is_superuser` bypass；越权一律 404/[]
</verification>

<success_criteria>
- 关联模型端点 #13-25 全部经 owner gate via .conversation，越权 404、list 型 []
- #24/#25 去除 superuser bypass，管理员越权 404（ISO-03）
- 全阶段隔离套件 + 既有 chat/coding 回归套件全绿
</success_criteria>

<output>
Create `.planning/phases/08-iso/08-04-SUMMARY.md` when done
</output>
