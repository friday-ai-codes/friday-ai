---
phase: 08-iso
plan: 03
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - server/chat/views.py
autonomous: true
requirements: [ISO-01, ISO-02, ISO-03, ISO-04]
must_haves:
  truths:
    - "直接会话端点 #1-12 全部经 owner gate：越权他人会话 id → 404，不泄漏存在性"
    - "list 仅返回自己的会话；create 写入 created_by=request.user（已认证时）"
    - "SSE stream 在流打开前 404（非流内 error 事件）"
    - "owner gate 为主/外层，无 superuser bypass；既有 has_project_access 仅作 null-owner 次层"
  artifacts:
    - path: "server/chat/views.py"
      provides: "#1-12 端点 owner gate（aget_for_user / created_by 注入 / SSE 流前 404）"
      contains: "aget_for_user"
  key_links:
    - from: "server/chat/views.py ChatStreamView.post"
      to: "ConversationService.aget_for_user"
      via: "owner-scoped existence check before StreamingHttpResponse"
      pattern: "aget_for_user"
---

<objective>
把 RESEARCH 端点清单 A 组（直接会话端点 #1-12，含 B 组 SSE #10′）全部路由到 Wave 2 的 owner gate：list/create/detail/delete/patch/preflight/runtime/messages-delete/fork/stream/interrupt/export-to-feishu。越权访问他人会话 id 统一 404，list 仅自己的，create 写 created_by，SSE 在流打开前 404。

Purpose: 这是 ISO-02/03/04 在直接会话表面的全部落点。owner gate 必须是**主/外层**门禁（先跑、404），既有 `has_project_access`（superuser bypass + 403）保留为 null-owner/shared 行的次层，绝不复用它做 owner gate（Anti-Pattern）。
Output: views.py 中 12 个端点接线 owner gate。落地后 08-01 中 #1-12 + SSE + admin-no-bypass 用例转 GREEN。
</objective>

<execution_context>
@.cursor/gsd-core/workflows/execute-plan.md
@.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-iso/08-RESEARCH.md
@.planning/phases/08-iso/08-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: CRUD 主路径 #1-5（list/create/detail/delete/patch）owner gate</name>
  <files>server/chat/views.py</files>
  <read_first>
    - server/chat/views.py L321-375（`ConversationListView` get/post：#1 list、#2 create）、L378-539（`ConversationDetailView` get/delete #3/#4）、L556-650（`ConversationDetailView.patch` #5：直接 `Conversation.objects.aget(id=..., is_deleted=False)`）
    - server/chat/conversation_service.py（Wave 2 已加：`aget_for_user`、`list_conversations(user)`、`create_conversation(...,user)`、`delete_conversation(id,user)`）
    - 08-RESEARCH.md §Access Inventory A 行 #1-5、§Anti-Patterns（勿复用 has_project_access；勿 fetch-then-check；404 不 403）
  </read_first>
  <action>
    - #1 list：`ConversationListView.get` 改 `await ConversationService.list_conversations(request.user)`。
    - #2 create：`ConversationListView.post` 调 `create_conversation(space_id=..., title=..., model=..., user=request.user)`（已认证写 created_by）。
    - #3 detail：`ConversationDetailView.get` 把 `get_conversation_with_messages` 前的取数改为先 `await ConversationService.aget_for_user(str(conversation_id), request.user)`；`except Conversation.DoesNotExist: return 404`。后续 select_related 预取仍按既有 id 进行（已确认归属后）。
    - #4 delete：`ConversationDetailView.delete` 调 `delete_conversation(str(conversation_id), request.user)`，0 行 → DoesNotExist → 404（既有 except 已映射 404）。
    - #5 patch：把开头的 `Conversation.objects.aget(id=..., is_deleted=False)` 换成 `await ConversationService.aget_for_user(str(conversation_id), request.user)`，`except Conversation.DoesNotExist → 404`；其余 frozen 校验/序列化逻辑不变。
    全部用 404，不引入 403；不加 is_superuser 分支。
  </action>
  <acceptance_criteria>
    - `GET /api/chat/conversations/`（用户 B JWT）不含 A 的会话；create 后 DB `created_by_id==请求用户`
    - `GET/DELETE/PATCH /api/chat/conversations/{A 会话 id}/`（B JWT）→ 404
    - 这些代码路径不出现 `HTTP_403_FORBIDDEN` 与 `is_superuser`
    - 08-01 用例 `test_list_only_owner` / `test_create_sets_owner` / #3/#4/#5 cross-user-denied GREEN
  </acceptance_criteria>
  <verify>
    <automated>cd server && uv run pytest tests/test_conversation_isolation.py -q -k "list or create or detail or delete or patch or admin"</automated>
  </verify>
  <done>#1-5 经 owner gate；越权 404、list/create owner 正确，无 403/bypass。</done>
</task>

<task type="auto">
  <name>Task 2: 只读/清理路径 #6-9（preflight/runtime/messages-delete/fork）owner gate 作主层</name>
  <files>server/chat/views.py</files>
  <read_first>
    - server/chat/views.py L653-786（`ConversationPreflightView` #6：含 `has_project_access("viewer")` + superuser bypass → 403）、L789-814（`ConversationRuntimeView` #7）、L817-936（`ConversationMessagesDeleteView` #8：`has_project_access("member")` + bypass → 403）、L939-1033（`ConversationMessageForkView` #9：`has_project_access("member")` + bypass → 403）
    - 08-RESEARCH.md §Anti-Patterns、§Pitfall 2/3（owner gate 在 has_project_access 之前、404 not 403、no superuser bypass）、§Pattern 4
  </read_first>
  <action>
    在每个端点取到 conversation 之后、**任何 provider/payload 处理与既有 has_project_access 之前**插入 owner gate：用 `aget_for_user` 取数（或对已 select_related 取得的 conversation 调 `if getattr(request.user,"is_authenticated",False) and conversation.created_by_id != request.user.id: return 404`，用 `created_by_id` 避免 async 惰性 FK）。
    - #6 preflight：owner gate 置于 select_related 预取之后、aresolve_or_error 之前；越权 → 404（注意：现有 403 跨项目分支保留为次层，但 owner-miss 必须先 404，避免 provider payload 信息泄漏）。
    - #7 runtime：把开头存在性 `Conversation.objects.aget(...)` 换为 `aget_for_user(...,request.user)` → 404，再调 `get_conversation_runtime`。
    - #8 messages-delete：owner gate 置于对话存在性之后、has_project_access 之前；owner-miss → 404。
    - #9 fork：owner gate 置于取 conversation 之后、has_project_access 之前；owner-miss → 404（本期 fork 仅限自己，管理员 fork 他人留 Phase 9）。
    既有 has_project_access/403 分支保留（针对 null-owner/共享行的次层防御），但 owner gate 永远先执行且返回 404。
  </action>
  <acceptance_criteria>
    - #6/#7/#8/#9：用户 B 访问 A 的会话 → 404（不是 403、不是 200）
    - owner gate 在 has_project_access 之前执行（preflight 不泄漏 provider payload）
    - owner gate 代码段不含 `is_superuser`；owner-miss 返回 404
    - 08-01 用例 #6/#7/#8/#9 cross-user-denied + admin-no-bypass GREEN
  </acceptance_criteria>
  <verify>
    <automated>cd server && uv run pytest tests/test_conversation_isolation.py -q -k "preflight or runtime or messages or fork"</automated>
  </verify>
  <done>#6-9 owner gate 作主层先于 has_project_access，越权 404 无信息泄漏。</done>
</task>

<task type="auto">
  <name>Task 3: 流式/操作路径 #10-12（stream SSE / interrupt / export-to-feishu）owner gate</name>
  <files>server/chat/views.py</files>
  <read_first>
    - server/chat/views.py L1116-1179（`ChatStreamView.post` #10/#10′：在构造 `StreamingHttpResponse` 前有存在性 `aget`）、L1256-1342（`ChatInterruptView.post` #11）、L1362-1464（`ExportToFeishuView.post` #12）
    - 08-RESEARCH.md §Pattern 5 / §Pitfall 5（SSE 必须在流打开前 404，勿仅在 send_message_stream 内部判）、§Access Inventory #10-12
  </read_first>
  <action>
    - #10 stream：把 `ChatStreamView.post` 中构造 `StreamingHttpResponse` 之前的存在性 `Conversation.objects.aget(id=..., is_deleted=False)` 换成 `await ConversationService.aget_for_user(str(conversation_id), request.user)`，`except Conversation.DoesNotExist → 404`。**必须在流打开前**，确保返回干净 HTTP 404 而非 text/event-stream 内 error 事件。`_stream_events` 内部不需重复 gate。
    - #11 interrupt：在执行 runner.interrupt()/barrier 取消之前，加 owner-scoped 存在性校验 `await ConversationService.aget_for_user(conv_id_str, request.user)`，`except DoesNotExist → 404`（防任意用户中断他人 run）。保留「无活跃对话」原有 404 分支。
    - #12 export-to-feishu：取 conversation（select_related project）后，加 owner gate `if request.user 已认证 and conversation.created_by_id != request.user.id: return 404`（在读取 project/messages 之前）。
    全部 404，不加 is_superuser 分支。
  </action>
  <acceptance_criteria>
    - `POST /api/chat/conversations/{A 会话 id}/stream/`（B JWT）→ HTTP 404（非 200 SSE）
    - `POST .../interrupt/` 与 `.../export-to-feishu/`（B JWT）→ 404
    - stream 的 owner gate 位于 `StreamingHttpResponse` 构造之前
    - 08-01 用例 `test_stream_cross_user_404` + #11/#12 cross-user-denied GREEN
  </acceptance_criteria>
  <verify>
    <automated>cd server && uv run pytest tests/test_conversation_isolation.py -q -k "stream or interrupt or export"</automated>
  </verify>
  <done>#10-12 经 owner gate；SSE 流前 404，interrupt/export 越权 404。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 已认证用户 → 直接会话端点 | B 持自己 JWT 直取 A 的会话 id（#1-12 IDOR 面） |
| SSE 流入口 | 流一旦打开无法返干净 404；必须在 view 层流前门禁 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07 | Elevation of Privilege / Information Disclosure | #1-12 IDOR | mitigate | owner gate（aget_for_user / created_by_id 比对）→ 404，对象不为非 owner 物化 |
| T-08-08 | Information Disclosure | preflight provider payload | mitigate | owner gate 先于 aresolve_or_error，越权不泄漏 provider 信息 |
| T-08-09 | Information Disclosure | SSE 流内泄漏 | mitigate | gate 置于 StreamingHttpResponse 构造前（Pitfall 5） |
| T-08-10 | Elevation of Privilege | 管理员 over-reach | mitigate | owner gate 无 is_superuser 分支（ISO-03） |
| T-08-11 | Tampering | 任意中断他人 run | mitigate | interrupt 加 owner-scoped 存在性校验 → 404 |
| T-08-SC | Tampering | 依赖安装 | accept | 零新增依赖（纯 view 接线） |
</threat_model>

<verification>
- `cd server && uv run pytest tests/test_conversation_isolation.py -q -k "list or create or detail or delete or patch or preflight or runtime or messages or fork or stream or interrupt or export or admin"` GREEN
- views.py #1-12 owner gate 段无 `HTTP_403_FORBIDDEN` 新增、无 `is_superuser`
- `cd server && uv run pytest tests/test_conversation_integration.py tests/test_conversation_facade.py -q` 不回归（owner 已在 fixture 设置）
</verification>

<success_criteria>
- 直接会话端点 #1-12（含 SSE #10′）全部经 owner gate，越权 404、不泄漏存在性
- list 仅自己的、create 写 created_by、SSE 流前 404
- owner gate 主层、无 superuser bypass；既有 has_project_access 保留为次层
</success_criteria>

<output>
Create `.planning/phases/08-iso/08-03-SUMMARY.md` when done
</output>
