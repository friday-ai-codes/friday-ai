---
phase: 08-iso
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - server/chat/models.py
  - server/chat/migrations/0018_conversation_created_by.py
  - server/chat/migrations/0019_backfill_conversation_created_by.py
  - server/chat/conversation_service.py
autonomous: true
requirements: [ISO-01, ISO-02, ISO-03, ISO-04]
must_haves:
  truths:
    - "Conversation 有 created_by 可空 FK 指向 accounts.User"
    - "升级迁移后历史会话 created_by 归属最早 superuser（按 created_at,id），无 superuser 时留 null 不阻塞"
    - "ConversationService 暴露 owner-scoped 取数能力（aget_for_user / list / create / delete 带 user）"
    - "owner 过滤无 superuser bypass；越权/不存在统一抛 Conversation.DoesNotExist"
  artifacts:
    - path: "server/chat/models.py"
      provides: "Conversation.created_by FK"
      contains: "created_by"
    - path: "server/chat/migrations/0018_conversation_created_by.py"
      provides: "AddField schema 迁移"
      contains: "AddField"
    - path: "server/chat/migrations/0019_backfill_conversation_created_by.py"
      provides: "RunPython 历史回填（可逆）"
      contains: "RunPython"
    - path: "server/chat/conversation_service.py"
      provides: "aget_for_user / list_conversations(user) / create_conversation(user) / delete_conversation(user)"
      contains: "aget_for_user"
  key_links:
    - from: "server/chat/conversation_service.py"
      to: "Conversation.created_by"
      via: "queryset.filter(created_by=user) when user.is_authenticated"
      pattern: "created_by=user"
---

<objective>
落地 Phase 8 的数据地基与服务层 owner-scoped 取数：给 `Conversation` 增加可空 FK `created_by`（两步迁移：AddField + RunPython 回填给最早 superuser），并在 `ConversationService` 引入唯一的 owner 过滤入口 `aget_for_user`，同时给 `list_conversations` / `create_conversation` / `delete_conversation` 加 `user` 参数实现 owner 过滤与 owner 写入。

Purpose: 把"隔离规则"收口到 service 单一真源（CONTEXT 推荐落点），让 Wave 3/4 的 25 个端点只需路由到这里，最小化漏判风险。本 plan 不改 views.py（端点接线在 Wave 3/4）。
Output: created_by 字段 + 两个迁移 + service 4 个方法的 owner-aware 版本。本 plan 落地后，08-01 中「回填」「list-only-owner（service 级）」相关用例可转 GREEN，端点级用例待 Wave 3/4。
</objective>

<execution_context>
@.cursor/gsd-core/workflows/execute-plan.md
@.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-iso/08-RESEARCH.md
@.planning/phases/08-iso/08-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Conversation.created_by 字段 + AddField 迁移 0018</name>
  <files>server/chat/models.py, server/chat/migrations/0018_conversation_created_by.py</files>
  <behavior>
    - Conversation 实例可设置/读取 created_by（FK 到 accounts.User），未设置时为 None
    - on_delete=SET_NULL：删除 owner 用户后会话保留、created_by 变 null（不级联删会话）
    - makemigrations --check 干净（迁移已生成）
  </behavior>
  <read_first>
    - server/chat/models.py L21-84（`Conversation`：已有 `project` FK、`provider_credential_id`（用 `models.SET_NULL` + related_name 的范例）、`status`、`is_deleted`、`created_at`、`Meta`）
    - server/chat/migrations/0017_message_parts.py（最新迁移；下一个编号 0018）
    - server/accounts/models.py（`AUTH_USER_MODEL = accounts.User`）
    - 08-CONTEXT.md §模型与迁移（字段定义锁定）
  </read_first>
  <action>
    在 `Conversation` 增加字段：`created_by = models.ForeignKey("accounts.User", null=True, blank=True, on_delete=models.SET_NULL, related_name="conversations", help_text="会话创建者；历史/匿名/开放模式可为 null（ISO-01）")`。
    用 `cd server && uv run python manage.py makemigrations chat` 生成 `0018_conversation_created_by.py`（AddField，dependencies 指向 0017_message_parts 与 accounts 最新迁移）。保持字段顺序与既有 FK 风格一致。**不要**此阶段收紧 null=False。
  </action>
  <acceptance_criteria>
    - `Conversation` model 含 `created_by` FK（null=True, blank=True, on_delete=SET_NULL, related_name="conversations"）
    - 存在 `server/chat/migrations/0018_conversation_created_by.py` 含 `AddField`
    - `cd server && uv run python manage.py makemigrations --check --dry-run` 退出 0（无未生成迁移）
  </acceptance_criteria>
  <verify>
    <automated>cd server && uv run python manage.py makemigrations --check --dry-run</automated>
  </verify>
  <done>created_by 字段与 AddField 迁移落地，makemigrations --check 干净。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RunPython 历史回填迁移 0019（最早 superuser，可逆）</name>
  <files>server/chat/migrations/0019_backfill_conversation_created_by.py</files>
  <behavior>
    - forwards：所有 created_by 为 null 的会话（含 is_deleted=True）回填给最早 superuser（order_by created_at,id 的第一个）
    - 无 superuser：不回填、不抛错（留 null）
    - backwards：把全部会话 created_by 置回 None（可逆）
  </behavior>
  <read_first>
    - server/accounts/migrations/0005_backfill_user_source.py、server/workflows/migrations/0018_backfill_execution_project.py（RunPython forwards/backwards + `apps.get_model` 范式）
    - server/accounts/models.py L53（确认 `created_at` 字段存在；**排序用 `order_by("created_at","id")`，A2 已确认不是 date_joined**）
    - 08-RESEARCH.md §Code Examples「RunPython backfill」、§Runtime State Inventory「Backfill specifics」
  </read_first>
  <action>
    手写 `0019_backfill_conversation_created_by.py`（dependencies=[("chat","0018_conversation_created_by")]，且依赖 accounts 已迁移以保证 User 表存在）。
    `forwards(apps, schema_editor)`：`Conversation = apps.get_model("chat","Conversation")`；`User = apps.get_model("accounts","User")`；`earliest = User.objects.filter(is_superuser=True).order_by("created_at","id").first()`；`if earliest is None: return`；`Conversation.objects.filter(created_by__isnull=True).update(created_by=earliest)`（覆盖软删行）。
    `backwards(apps, schema_editor)`：`Conversation.objects.update(created_by=None)`。
    `operations=[migrations.RunPython(forwards, backwards)]`。中文注释说明「无 superuser 留 null 不阻塞」「回填含软删行」。
  </action>
  <acceptance_criteria>
    - 文件存在，含 `RunPython(forwards, backwards)`，排序键为 `created_at,id`
    - forwards 在无 superuser 时早返回不抛错；backwards 置 None
    - `cd server && uv run python manage.py migrate chat` 成功；`migrate chat 0018` 可回滚 0019
    - 08-01 的 `test_backfill_*` 用例转 GREEN
  </acceptance_criteria>
  <verify>
    <automated>cd server && uv run pytest tests/test_conversation_isolation.py -q -k backfill</automated>
  </verify>
  <done>回填迁移落地且可逆，回填用例 GREEN。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ConversationService owner-scoped 取数（aget_for_user + list/create/delete 带 user）</name>
  <files>server/chat/conversation_service.py</files>
  <behavior>
    - aget_for_user(id, user)：user 已认证 → 仅返回 created_by=user 的会话，越权/不存在抛 Conversation.DoesNotExist；user 未认证 → 维持开放（不加 owner 过滤）
    - 无 is_superuser 分支（管理员不 bypass）
    - list_conversations(user)：已认证仅列自己的；未认证列全部（开放行为）
    - create_conversation(..., user=None)：已认证写 created_by=user；未认证写 null
    - delete_conversation(id, user)：owner-scoped aupdate，0 行 → 抛 DoesNotExist
  </behavior>
  <read_first>
    - server/chat/conversation_service.py L696-723（`create_conversation`）、L1309-1316（`list_conversations`）、L1318-1345（`get_conversation_with_messages`）、L1684-1708（`delete_conversation`）
    - 08-RESEARCH.md §Pattern 1/2/3、§Code Examples「Owner-scoped delete」、§Pitfall 4（async FK：用 `created_by_id` / queryset filter，勿惰性访问 `.created_by`）、§Pitfall 2（owner gate 无 is_superuser 分支）
  </read_first>
  <action>
    在 `ConversationService` 新增 `@staticmethod async def aget_for_user(conversation_id, user) -> Conversation`：`qs = Conversation.objects.filter(id=conversation_id, is_deleted=False)`；`if getattr(user, "is_authenticated", False): qs = qs.filter(created_by=user)`；`return await qs.aget()`（DoesNotExist 上抛由 view 映射 404）。**不要**加任何 `is_superuser` 分支。
    改 `list_conversations(user=None)`：已认证追加 `.filter(created_by=user)`，未认证保持现状。
    改 `create_conversation(space_id, title="新对话", model="", user=None)`：向 `acreate` 传 `created_by=user if getattr(user,"is_authenticated",False) else None`（用 `created_by` 关系或 `created_by_id`）。
    改 `delete_conversation(conversation_id, user=None)`：owner-scoped `qs.aupdate(is_deleted=True)`，`if updated == 0: raise Conversation.DoesNotExist(...)`。
    保持 async ORM 原生方法；中文注释解释「隔离仅对已认证用户生效、管理员不 bypass」。
  </action>
  <acceptance_criteria>
    - `conversation_service.py` 含 `aget_for_user`，且代码中**不出现** `is_superuser`
    - `list_conversations` / `create_conversation` / `delete_conversation` 均接受 `user` 参数并按 owner 过滤/写入
    - `cd server && grep -n "is_superuser" server/chat/conversation_service.py` 无新增匹配（owner gate 无 bypass）
    - 08-01 的 service 级用例（list-only-owner via service / create-owner 经 service）转 GREEN
  </acceptance_criteria>
  <verify>
    <automated>cd server && uv run pytest tests/test_conversation_isolation.py -q -k "owner or list or create or backfill"</automated>
  </verify>
  <done>service 层 owner-scoped 取数落地，无 superuser bypass；相关用例 GREEN。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service 取数入口 | 所有按 id 取会话最终经 aget_for_user；owner 过滤的唯一真源 |
| 迁移回填 | 一次性 RunPython 改写历史归属，须可逆、不阻塞无 superuser 部署 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03 | Elevation of Privilege | ConversationService owner gate | mitigate | `created_by=user` 过滤无 is_superuser 分支（ISO-03）；grep 守卫 |
| T-08-04 | Information Disclosure | aget_for_user | mitigate | 越权/不存在统一抛 DoesNotExist（view 映射统一 404，ISO-04） |
| T-08-05 | Tampering | RunPython 回填 | mitigate | apps.get_model + 可逆 backwards；无 superuser 早返回不破坏数据 |
| T-08-06 | Denial of Service | async FK 惰性访问 | mitigate | 用 queryset filter / created_by_id，避免 SynchronousOnlyOperation（Pitfall 4） |
| T-08-SC | Tampering | 依赖安装 | accept | 零新增依赖（纯 Django model/migration/service） |
</threat_model>

<verification>
- `cd server && uv run python manage.py makemigrations --check --dry-run` 退出 0
- `cd server && uv run python manage.py migrate chat` 成功，`migrate chat 0018` 可回滚回填
- `cd server && uv run pytest tests/test_conversation_isolation.py -q -k "owner or list or create or backfill"` GREEN
- conversation_service.py 中 owner gate 无 `is_superuser`
</verification>

<success_criteria>
- Conversation.created_by 可空 FK + AddField(0018) + RunPython 回填(0019，可逆)
- ConversationService 暴露 aget_for_user 及 user-aware list/create/delete，无 superuser bypass
- 回填与 service 级隔离用例 GREEN；makemigrations --check 干净
</success_criteria>

<output>
Create `.planning/phases/08-iso/08-02-SUMMARY.md` when done
</output>
