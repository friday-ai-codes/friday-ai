---
phase: 49-produce-spec
plan: 04
type: execute
wave: 4
depends_on: ["49-03"]
files_modified:
  - server/services/plan_orchestration/architect_merge_adapter.py
  - server/tests/services/test_spec_generation_merge_hook.py
autonomous: true
requirements: [SPEC-01, SPEC-02]
must_haves:
  truths:
    - "ArchitectMergeAdapter._handle_pass 在 EVENT_PLAN_MERGE_COMPLETED emit 之后 best-effort 调 spec 生成 hook"
    - "整段 try/except 吞为 warning sdd_spec_generation_failed，绝不阻断融合返回（merge 仍 passed）"
    - "SDD 仓融合通过 → 全链路产 SddSpec(draft) + Document(sdd_spec) + emit spec.drafted，关联 work_item/plan_version/repository"
    - "非 SDD / 无 SDD 仓 / spec 生成异常 → merge 返回 passed 零回归"
  artifacts:
    - path: "server/services/plan_orchestration/architect_merge_adapter.py"
      provides: "_handle_pass 末尾 best-effort spec 生成挂接 + 构造注入可选 spec_generation_hook"
      contains: "sdd_spec_generation_failed"
  key_links:
    - from: "server/services/plan_orchestration/architect_merge_adapter.py"
      to: "agenerate_specs_for_plan"
      via: "_handle_pass best-effort hook（默认真实实现，可注入 stub）"
      pattern: "spec_generation_hook"
---

<objective>
把 Plan 03 的 spec 生成挂接到融合通过路径（D-49-5），并以全链路守护测试封板 Phase 49（D-49-7）：

1. `ArchitectMergeAdapter._handle_pass` 在 `EVENT_PLAN_MERGE_COMPLETED` emit 之后 best-effort 调 `agenerate_specs_for_plan(plan_version_id)`；**整段 try/except 吞为 warning `sdd_spec_generation_failed`，绝不阻断融合/编排返回**。
2. adapter 构造注入可选 `spec_generation_hook`（默认真实实现 `agenerate_specs_for_plan`），便于单测 stub。
3. 全链路守护测试：SDD 产 spec / 非 SDD / 无 SDD 零回归 / 合成异常 fail-soft / 幂等。

Purpose: 闭合 SPEC-01/SPEC-02 全链路；保 v0.8 融合路径零回归。
Output: adapter 挂接 + 可注入 hook、全链路 merge hook 守护测试。

映射：SPEC-01（融合阶段额外产 spec）、SPEC-02（产出关联来源）。ROADMAP 成功标准 #1/#2/#3/#4 全覆盖。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/49-produce-spec/49-CONTEXT.md
@.planning/phases/49-produce-spec/49-03-SUMMARY.md

# 挂接点 + 可注入依赖范式 + best-effort emit 范式
@server/services/plan_orchestration/architect_merge_adapter.py
@server/services/plan_orchestration/entrypoint.py

# 全链路测试范式（mock synthesizer + 真实 session/partial 直建）
@server/tests/services/test_architect_merge_adapter.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: _handle_pass best-effort 挂接 spec 生成 hook + 构造注入</name>
  <files>server/services/plan_orchestration/architect_merge_adapter.py</files>
  <action>
按 D-49-5 改 `ArchitectMergeAdapter`（**只新增挂接，不改既有 pass/fail/降级语义**）：

- 构造函数 `__init__` 新增可选参数 `spec_generation_hook: Any = None`。默认绑定真实实现：`self.spec_generation_hook = spec_generation_hook or <真实 hook>`。为规避 import 环，采用与 clarification_service 相同的延迟默认绑定范式 —— `__init__` 内 `if spec_generation_hook is None: from services.plan_orchestration.spec_generation import agenerate_specs_for_plan; spec_generation_hook = agenerate_specs_for_plan`，再 `self.spec_generation_hook = spec_generation_hook`。
- 在 `_handle_pass` 中，**`EVENT_PLAN_MERGE_COMPLETED` 的 `await self._emit(...)` 之后、`return {...}` 之前**，新增 best-effort 挂接：
  ```
  try:
      await self.spec_generation_hook(version_id)
  except Exception:  # noqa: BLE001 — spec 生成 best-effort，绝不阻断融合返回
      logger.warning("sdd_spec_generation_failed", session_id=str(session.id), plan_version_id=str(version_id))
  ```
  - hook 自身（agenerate_specs_for_plan）已逐仓 try/except 隔离 + 内部吞错；此处的外层 try/except 是**双保险**，确保即便 hook 整体抛错（如 import/解析异常）也绝不冒泡到 `_handle_pass` 的返回路径。
  - 不改 `version_id` / `_record_merge` / `set_current_plan_version` / 既有 emit 的顺序与返回值。
- 更新模块/方法 docstring：注明 `_handle_pass` 末尾 best-effort 产 SDD spec（fail-soft），融合返回不受影响。
- **不修改** `entrypoint.build_orchestration_engine`（adapter 默认构造已绑定真实 hook，两入口自动获得 spec 生成能力，无需额外接线）。
  </action>
  <verify>
    <automated>cd server && uv run python -c "from services.plan_orchestration import ArchitectMergeAdapter;a=ArchitectMergeAdapter();assert callable(a.spec_generation_hook);print('ok')"</automated>
  </verify>
  <done>ArchitectMergeAdapter 默认绑定真实 spec 生成 hook 且可注入 stub；_handle_pass 在 merge.completed emit 后 best-effort 调 hook 并以 warning sdd_spec_generation_failed 吞错，返回值/既有顺序不变。</done>
</task>

<task type="auto">
  <name>Task 2: 全链路 merge hook 守护测试（SDD 产 / 零回归 / fail-soft / 幂等）</name>
  <files>server/tests/services/test_spec_generation_merge_hook.py</files>
  <action>
新建 `server/tests/services/test_spec_generation_merge_hook.py`，复用 test_architect_merge_adapter.py 的 `_valid_merged_plan()` / `_make_repo()` / session+partial 直建范式 + mock MergedPlanSynthesizer（返回 valid MergedPlan）；`pytestmark = pytest.mark.django_db(transaction=True)`。

执行 `adapter.merge(session)` 走真实 pass 分支落 canonical PlanVersion，验证 spec 挂接：

- **SDD 仓全链路产 spec**：MergedPlan.execution_plan 含某 repo_id 且该 Repository facets={"methodology":"SDD"}；注入 mock spec_generation_hook（或注入真实 agenerate_specs_for_plan + mock SddSpecSynthesizer 返回固定 markdown）→ merge 返回 `validation_status=="passed"` 且产 1 个 SddSpec(draft) + Document(sdd_spec, internal_generated) + DocumentVersion，关联 work_item/plan_version/repository 正确；spec.drafted 事件落 PlanSessionEvent。
- **非 SDD 仓零回归**：Repository 无 methodology facets → merge 仍 `passed`，SddSpec 计数 0，与 v0.8 路径一致（ArchitectMerge passed + current_plan_version 已置）。
- **无涉及仓 / execution_plan 无匹配仓**：merge passed，spec 计数 0。
- **spec 合成异常 fail-soft**：注入抛异常的 spec_generation_hook（或 mock SddSpecSynthesizer 抛错）→ merge 仍返回 `passed`（warning sdd_spec_generation_failed，不冒泡）；ArchitectMerge(passed) 与 current_plan_version 不受影响。
- **幂等**：同 session 连跑两次 pass 路径（或对同一 plan_version_id 连调 hook 两次）→ SddSpec 总数为 SDD 仓数（不翻倍），Document/DocumentVersion 不新增。
- （可选断言）注入 stub hook 记录被调用的 plan_version_id == canonical PlanVersion id，证明挂接点传参正确。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/test_spec_generation_merge_hook.py tests/services/test_architect_merge_adapter.py -x -q</automated>
  </verify>
  <done>全链路守护全绿：SDD 产 spec、非 SDD/无仓零回归、合成异常 fail-soft（merge 仍 passed）、幂等不翻倍；既有 test_architect_merge_adapter 全部通过（融合语义零回归）。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spec 生成 hook → 融合编排返回 | spec 生成绝不阻断融合主链路 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-08 | Denial of Service | spec 生成 hook 异常阻断融合返回 | mitigate | _handle_pass 外层 try/except 吞 warning sdd_spec_generation_failed + hook 内逐仓隔离（双保险），merge 始终返回 passed |
| T-49-09 | Tampering | 挂接点改动回归 v0.8 融合语义 | mitigate | 仅在 merge.completed emit 之后追加挂接，不改 _record_merge/set_current_plan_version/返回值；既有 test_architect_merge_adapter 全量回归断言 |
</threat_model>

<verification>
- `cd server && uv run ruff check services/plan_orchestration/architect_merge_adapter.py`
- `cd server && uv run pytest tests/services/test_spec_generation_merge_hook.py tests/services/test_architect_merge_adapter.py tests/services/test_plan_orchestration_engine_merge.py -q`
- 全 phase 收口：`cd server && uv run pytest tests/delivery/test_sdd_spec_service.py tests/delivery/test_sdd_spec_inv6_guard.py tests/delivery/test_document_inv6_guard.py tests/delivery/test_create_internal_spec.py tests/services/test_spec_generation.py tests/services/test_event_taxonomy_alignment.py tests/services/test_spec_generation_merge_hook.py -q`
- `cd server && uv run python manage.py makemigrations --check --dry-run`
</verification>

<success_criteria>
- SDD 仓融合通过 → 全链路产 SddSpec(draft) + Document(sdd_spec) + emit spec.drafted，关联来源齐全（SPEC-01/SPEC-02 闭合）。
- 非 SDD / 无 SDD 仓 / spec 生成异常 → merge 返回 passed 零回归。
- 幂等不翻倍；既有融合测试与 INV-6 守护全绿；ruff + makemigrations --check 干净。
</success_criteria>

<output>
Create `.planning/phases/49-produce-spec/49-04-SUMMARY.md` when done
</output>
