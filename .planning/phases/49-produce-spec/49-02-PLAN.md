---
phase: 49-produce-spec
plan: 02
type: execute
wave: 2
depends_on: ["49-01"]
files_modified:
  - server/delivery/services/sdd_spec_service.py
  - server/delivery/services/__init__.py
  - server/tests/delivery/test_sdd_spec_service.py
  - server/tests/delivery/test_sdd_spec_inv6_guard.py
autonomous: true
requirements: [SPEC-01, SPEC-02]
must_haves:
  truths:
    - "SddSpecService.create_draft 经 DocumentService.create_internal_spec 落 Document(sdd_spec) 后建 SddSpec(status=draft) 连 document/work_item/plan_version/repository"
    - "同 (plan_version, repository) 重产幂等：不重复建 SddSpec、不新增 Document/DocumentVersion"
    - "SddSpec 落库只经 SddSpecService（INV-6 grep 守护通过）"
  artifacts:
    - path: "server/delivery/services/sdd_spec_service.py"
      provides: "SddSpec 单一写入入口，本 phase 仅 create_draft"
      contains: "async def create_draft"
    - path: "server/tests/delivery/test_sdd_spec_inv6_guard.py"
      provides: "SddSpec 旁路写表 INV-6 grep 守护"
      contains: "SddSpec"
  key_links:
    - from: "server/delivery/services/sdd_spec_service.py"
      to: "DocumentService.create_internal_spec"
      via: "落 Document(sdd_spec) 再建 SddSpec"
      pattern: "create_internal_spec"
    - from: "server/delivery/services/__init__.py"
      to: "SddSpecService"
      via: "curated re-export"
      pattern: "SddSpecService"
---

<objective>
新建 `SddSpecService` —— SddSpec 的单一写入入口（D-49-3，INV-6）。本 phase 仅实现 `create_draft`：经 `DocumentService.create_internal_spec` 落 spec 正文文档，再幂等建/取 `SddSpec(status=draft)` 并连 document/work_item/plan_version/repository。状态流转 / 评审写入方法**留 Phase 50，本 phase 不实现**。

Purpose: 为 Plan 03 的 spec 生成提供唯一、幂等的 SddSpec 写入入口；收口 INV-6。
Output: `sdd_spec_service.py`（create_draft）、services curated re-export、create_draft 单测、SddSpec INV-6 grep 守护。

映射：SPEC-01（spec draft 经单一 service 入口落库）、SPEC-02（SddSpec 连 WorkItem + PlanVersion + Repository）。ROADMAP 成功标准 #2/#4。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/49-produce-spec/49-CONTEXT.md
@.planning/phases/49-produce-spec/49-01-SUMMARY.md

# service 写入范式 + curated re-export
@server/delivery/services/document_service.py
@server/delivery/services/technical_plan_service.py
@server/delivery/services/__init__.py

# INV-6 grep 守护范式（精确锚定）
@server/tests/delivery/test_architect_merge_inv6_guard.py
@server/tests/delivery/test_document_inv6_guard.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SddSpecService.create_draft + curated re-export</name>
  <files>server/delivery/services/sdd_spec_service.py, server/delivery/services/__init__.py</files>
  <behavior>
    - 首次 create_draft(plan_version_id, repository, work_item, content, change_kind) → 经 DocumentService.create_internal_spec 落 Document(sdd_spec) + 建 SddSpec(status=draft, change_kind) 连 document/work_item/plan_version_id/repository；返回 SddSpec。
    - 重产同 (plan_version_id, repository)：返回既有 SddSpec，不新增 SddSpec 行，且不新增 Document/DocumentVersion（短路：先探测既有 SddSpec 命中即返回，不再调 create_internal_spec）。
    - work_item=None 合法不抛（INV-2）。
  </behavior>
  <action>
新建 `server/delivery/services/sdd_spec_service.py`，沿用 technical_plan_service.py 的 async + sync_to_async + transaction.atomic 范式，中文 docstring：

`class SddSpecService`，本 phase 仅一个写方法（守 INV-6 单一写入入口；状态流转/评审写入归 Phase 50，**不实现**，docstring 注明）：

```
async def create_draft(self, *, plan_version_id, repository, work_item, content, change_kind="proposal") -> SddSpec
```

实现（per D-49-3，兼顾幂等且不留孤儿 Document）：
1. **幂等短路**：先 `await SddSpec.objects.filter(plan_version_id=plan_version_id, repository=repository).afirst()`；命中即直接返回（不调 create_internal_spec → 不新增 Document/DocumentVersion，满足「重产不重复建、同内容不翻版本」）。
2. 未命中：`document = await DocumentService().create_internal_spec(work_item=work_item, repository_label=repository.name, content=content)`（落 Document(sdd_spec, internal_generated)）。
3. `@sync_to_async` 私有 `_create_locked(...)`，`transaction.atomic` 内 `SddSpec.objects.get_or_create(plan_version_id=plan_version_id, repository=repository, defaults={"document": document, "work_item": work_item, "change_kind": change_kind, "status": SddSpecStatus.DRAFT})` —— unique_together DB 约束 + get_or_create 兜底并发竞态（极小概率孤儿 Document 视为 best-effort 可接受，docstring 注明）。返回 SddSpec。
- DocumentService 用函数内/方法内 lazy import 或顶部 import（同 delivery app，无环）；SddSpec/SddSpecStatus 从 `delivery.models` import。
- 注意 async 安全：用 `plan_version_id` 标量 + `repository`/`work_item` 实例（调用方 Plan 03 会传实例/标量），不裸访问 lazy-FK。

改 `server/delivery/services/__init__.py`：import 并 re-export `SddSpecService`，加入 `__all__`（紧随 DocumentService 附近）。

新建 `server/tests/delivery/test_sdd_spec_service.py`（先写失败测试再实现）：覆盖 <behavior> 三条 + 断言 SddSpec.document 指向 internal_generated Document、SddSpec.status=='draft'、SddSpec.plan_version_id/work_item/repository 关联正确。`pytestmark = pytest.mark.django_db(transaction=True)`。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/delivery/test_sdd_spec_service.py -x -q</automated>
  </verify>
  <done>create_draft 测试全绿；首产建 Document+SddSpec(draft) 关联齐全，重产幂等不新增行；`from delivery.services import SddSpecService` 可用。</done>
</task>

<task type="auto">
  <name>Task 2: SddSpec 旁路写表 INV-6 grep 守护</name>
  <files>server/tests/delivery/test_sdd_spec_inv6_guard.py</files>
  <action>
新建 `server/tests/delivery/test_sdd_spec_inv6_guard.py`，**逐行镜像** `test_architect_merge_inv6_guard.py` 的精确锚定范式（纯本地源码扫描，无 DB/网络）：

- `_ALLOWED_WRITER = "delivery/services/sdd_spec_service.py"`（唯一允许写 SddSpec 的模块）。
- 写表模式（精确锚定，负向前瞻排除更长符号）：
  - `_RE_ORM_WRITE = re.compile(r"\bSddSpec\.objects\.(?:create|bulk_create|get_or_create|update_or_create|update)\b")`
  - `_RE_INSTANTIATE = re.compile(r"\bSddSpec(?!Status|ChangeKind|Service|Synthesizer)\s*\(")` —— 排除 SddSpecStatus(/SddSpecChangeKind(/SddSpecService(/SddSpecSynthesizer(；`LLMSddSpecSynthesizer(` 因前接 `LLM` 无词边界天然不命中。
  - `_RE_INSTANCE_SAVE = re.compile(r"\bSddSpec(?!Status|ChangeKind|Service|Synthesizer)\([^)]*\)\.save\(")`
- 扫描范围 `_is_scanned`：排除 writer 自身 / tests/ / migrations/ / delivery/models/（沿用范式；跳过 `class SddSpec` 模型定义行）。
- `test_inv6_no_bypass_sdd_spec_write()`：断言除 sdd_spec_service.py 外 server 源码无旁路 SddSpec 写表入口，命中列 `文件:行`。
- `test_inv6_sdd_spec_writer_actually_writes()`：守护有效性 —— 断言 sdd_spec_service.py 确实含 `SddSpec.objects.get_or_create`（或 create），防形同虚设。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/delivery/test_sdd_spec_inv6_guard.py -x -q</automated>
  </verify>
  <done>守护测试通过：除 SddSpecService 外无旁路 SddSpec 写表；writer 有效性断言成立（sdd_spec_service.py 确实写 SddSpec）。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 调用方 → SddSpecService | 唯一 SddSpec 写入收口；防旁路写表破坏脊柱一致性 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-03 | Tampering | 旁路写 SddSpec 绕过单一入口 | mitigate | test_sdd_spec_inv6_guard grep 守护断言仅 SddSpecService 可写 |
| T-49-04 | Repudiation | 并发重产产生重复 SddSpec | mitigate | unique_together(plan_version, repository) DB 约束 + 幂等短路 + get_or_create 兜底 |
</threat_model>

<verification>
- `cd server && uv run ruff check delivery/services/sdd_spec_service.py delivery/services/__init__.py`
- `cd server && uv run pytest tests/delivery/test_sdd_spec_service.py tests/delivery/test_sdd_spec_inv6_guard.py -q`
</verification>

<success_criteria>
- SddSpecService.create_draft 落库幂等、关联齐全、status=draft。
- SddSpec 写入收口于 SddSpecService（INV-6 grep 守护通过）。
- ruff + 单测通过。
</success_criteria>

<output>
Create `.planning/phases/49-produce-spec/49-02-SUMMARY.md` when done
</output>
