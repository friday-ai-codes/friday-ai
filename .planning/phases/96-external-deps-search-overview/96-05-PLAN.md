---
phase: 96-external-deps-search-overview
plan: 05
type: execute
wave: 3
depends_on: [96-03, 96-04]
files_modified:
  - web/src/api/knowledge.ts
  - web/src/components/knowledge/KnowledgeDashboard.vue
  - web/src/locales/zh-CN.json
autonomous: true
requirements: [KDEP-03]
must_haves:
  truths:
    - "KnowledgeDashboard 新增「交付文档 / 外部依赖」区块，与现有仓库/域指标并列、风格一致"
    - "区块展示按类型的计数 + 每类入口（点击进搜索预筛该类型）+ 区块内即时搜索框（客户端过滤已加载条目）"
    - "无交付文档时展示优雅空态引导（不显示空网格）"
    - "数据走 96-03 聚合接口（access_scope 过滤，单次请求），不逐项目拉全量"
  artifacts:
    - path: "web/src/api/knowledge.ts"
      provides: "getArtifactOverview() 方法 + 类型"
      contains: "getArtifactOverview"
    - path: "web/src/components/knowledge/KnowledgeDashboard.vue"
      provides: "交付文档/外部依赖 区块（计数 + 入口 + 即时搜索 + 空态）"
      contains: "getArtifactOverview"
  key_links:
    - from: "web/src/components/knowledge/KnowledgeDashboard.vue"
      to: "web/src/api/knowledge.ts (getArtifactOverview)"
      via: "useQuery 拉聚合数据渲染区块"
      pattern: "getArtifactOverview"
    - from: "web/src/components/knowledge/KnowledgeDashboard.vue"
      to: "/knowledge?tab=search"
      via: "点类型入口跳搜索预筛"
      pattern: "tab.*search|emit\\('navigate'"
---

<objective>
交付 KDEP-03 前端：在 `KnowledgeDashboard.vue` 新增独立区块「交付文档 / 外部依赖」，与现有「核心指标 / 业务能力全景 / 运行状态」并列、风格一致（同卡片/网格/图标语言）。内容：按 `ArtifactType` 分组计数磁贴 + 每类入口 + 区块内即时搜索框（沿用 Dashboard 客户端过滤已加载条目模式）+ 优雅空态。

Purpose: 消费 96-03 聚合接口，把外部依赖纳入知识总览大盘，让用户一屏感知交付文档全景并快速下钻。单次请求、access_scope 过滤，避免前端逐项目拉全量。
Output: `knowledge.ts` 聚合方法 + Dashboard 新区块 + i18n 文案。
</objective>

<execution_context>
@.cursor/gsd-core/workflows/execute-plan.md
@.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/96-external-deps-search-overview/96-CONTEXT.md

@web/src/api/knowledge.ts
@web/src/components/knowledge/KnowledgeDashboard.vue
@web/src/components/common/CompactEmptyState.vue
@web/src/locales/zh-CN.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: knowledge.ts 聚合接口方法</name>
  <files>web/src/api/knowledge.ts</files>
  <read_first>
    - web/src/api/knowledge.ts L89-113（`searchDeliveryKnowledge` + `knowledgeApi` 导出结构，追加方法锚点）
    - .planning/phases/96-external-deps-search-overview/96-03-PLAN.md（`<output>` 中对外契约字段）
  </read_first>
  <action>
    在 `web/src/api/knowledge.ts` 新增类型与方法（对齐 96-03 契约）：
    - `interface ArtifactTypeCount { type_key: string, type_name: string, carrier: string, ragable: boolean, count: number }`
    - `interface ArtifactOverviewItem { artifact_id: string, title: string, type_key: string, type_name: string, carrier: string, url: string, project_id: string, project_name: string, updated_at: string }`
    - `interface ArtifactOverview { total: number, types: ArtifactTypeCount[], items: ArtifactOverviewItem[], truncated: boolean }`
    - `export async function getArtifactOverview(params?: { typeKey?: string }) { return get<ArtifactOverview>('/knowledge/artifacts/overview/', { type_key: params?.typeKey }) }`
    - 把 `getArtifactOverview` 加入默认导出 `knowledgeApi` 对象。仅新增，不改既有方法。
  </action>
  <verify>
    <automated>cd web &amp;&amp; pnpm exec vue-tsc --noEmit</automated>
  </verify>
  <done>`getArtifactOverview` 与 `ArtifactOverview` 类型存在并挂到 `knowledgeApi`；类型检查通过。</done>
</task>

<task type="auto">
  <name>Task 2: KnowledgeDashboard 交付文档区块（计数 + 入口 + 即时搜索 + 空态）</name>
  <files>web/src/components/knowledge/KnowledgeDashboard.vue, web/src/locales/zh-CN.json</files>
  <read_first>
    - web/src/components/knowledge/KnowledgeDashboard.vue L39-47（`useQuery` 范式）、L266-332（核心指标磁贴样式令牌）、L334-440（区块 header + 网格 section 结构）
    - web/src/components/knowledge/KnowledgeDashboard.vue L215-236（`goToKnowledge`/`emit('navigate')` 跳转范式）
    - web/src/components/common/CompactEmptyState.vue（空态组件 props）
    - web/src/locales/zh-CN.json（`knowledge.overview.*` 键位命名风格）
  </read_first>
  <action>
    在 `KnowledgeDashboard.vue` 新增区块「交付文档 / 外部依赖」，插在「运行状态」区之后（或核心指标之下，执行者按视觉平衡自定，遵循「优雅、好用」）：
    - script：新增 `const overviewQuery = useQuery({ queryKey: ['knowledge','artifact-overview'], queryFn: () => knowledgeApi.getArtifactOverview(), staleTime: 60_000 })`；派生 `overview`、`types`、`items`、`isEmpty`（total===0）。import `knowledgeApi`。
    - 即时搜索：区块内 `Input` 搜索框，用客户端过滤（简单 `computed` 对 `items` 按 title/type_name/project_name 包含匹配即可；沿用 Dashboard「客户端过滤已加载条目」模式，无需额外请求）。展示过滤后的条目列表（title + 类型徽标 + 项目名 + 载体图标），点击条目按载体跳转/打开（external_link 新标签、其余可复用 `emit('navigate','search')` 或跳 `/knowledge?tab=search` 预填）。
    - 计数磁贴：`types` 渲染为网格磁贴（每格：类型名 + count + 图标），复用核心指标磁贴的圆角/渐变/ring 样式令牌；点某类型 → 跳搜索 Tab 预筛该类型（`router.push({ query: { tab: 'search', dep_type: t.type_key } })` 或 `emit('navigate','search')`，具体预筛参数由执行者定，注明）。
    - 空态：`isEmpty` 时用 `CompactEmptyState`（icon `lucide--package`）+ 引导文案（指向作战室「外部依赖」维护入口），不渲染空网格。loading 用既有 `Skeleton` 骨架范式。
    - `truncated` 为真时区块底部提示「仅展示最近 N 条，更多见搜索」。
    - 区块 header 复用既有 `<span class="h-4 w-1 rounded-full bg-primary" /> + <h3>` 标题范式，图标语言与既有一致。

    `web/src/locales/zh-CN.json`：补 `knowledge.overview.deps.*` 文案（title/hint/searchPlaceholder/empty.title/empty.body/truncated 等）。仅新增，默认中文，不改既有键。
  </action>
  <verify>
    <automated>cd web &amp;&amp; pnpm exec vue-tsc --noEmit</automated>
  </verify>
  <done>Dashboard 出现「交付文档/外部依赖」区块：类型计数磁贴 + 入口 + 即时搜索 + 优雅空态 + 加载骨架；数据来自单次聚合请求；风格与既有区块一致；类型检查通过。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 聚合响应 → Dashboard 渲染 | 未信任 title/url 渲染，外链跳转安全 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-15 | Information Disclosure | 区块跨项目可见性 | mitigate | 数据源 96-03 已服务端 access_scope 过滤；前端不额外聚合他项目数据 |
| T-96-16 | Tampering | 条目外链打开 | mitigate | external_link 用 `rel="noopener noreferrer"` 新标签打开 |
| T-96-17 | Denial of Service | 大量条目渲染 | mitigate | 后端已截断 LIMIT + truncated 提示；前端即时搜索为客户端过滤已加载子集 |
| T-96-SC | Tampering | npm/pip/cargo installs | mitigate | 本 plan 不新增前端依赖（复用既有组件/API），无供应链面 |
</threat_model>

<verification>
- `cd web && pnpm exec vue-tsc --noEmit` 通过
- 手工：`/knowledge?tab=overview` 出现「交付文档/外部依赖」区块，计数正确、即时搜索可用、点类型进搜索预筛
- 无交付文档账户：区块显示优雅空态而非空网格
</verification>

<success_criteria>
- Dashboard 新增交付文档区块（计数 + 入口 + 即时搜索），与既有区块风格一致
- 数据走单次 access_scope 过滤聚合接口
- 优雅空态 + 加载骨架
- i18n 中文，类型检查通过
</success_criteria>

<output>
Create `.planning/phases/96-external-deps-search-overview/96-05-SUMMARY.md` when done.

## Artifacts this plan produces (96-05)
- `getArtifactOverview()` + 聚合类型（`web/src/api/knowledge.ts`）
- 交付文档/外部依赖 Dashboard 区块（`web/src/components/knowledge/KnowledgeDashboard.vue`）
- 新增 i18n 文案（`web/src/locales/zh-CN.json`）
</output>
