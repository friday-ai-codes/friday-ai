---
phase: 89
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/agents/call_source.py
  - .planning/observability/LOGGING-SPEC.md
  - server/initiatives/services/plan_deepen_service.py
  - server/services/plan_orchestration/architect_merge_adapter.py
  - server/services/plan_orchestration/research_adapter.py
  - server/workflows/nodes/integrations/plan_deepen.py
  - server/workflows/nodes/__init__.py
  - server/feishu/cards/plan_deepen_card.py
  - server/tests/initiatives/test_plan_deepen_service.py
  - server/tests/workflows/test_plan_deepen_node.py
autonomous: true
requirements: [PLAN-01]
must_haves:
  truths:
    - "方案载体复用 v0.7 canonical TechnicalPlan/PlanVersion（绝不新建方案模型）——PlanDeepenService 经 build_orchestration_engine + adrive_plan_session_to_pause_or_terminal 同一引擎工厂续驱，route→recall→per-repo research(explore 容器)→merge→canonical 落库（CONTEXT 锁定『复用 v0.7 + 不造两套』）"
    - "include_repos 锚定 Phase 88 RepoAssociationService.get_verified_associations(project) 输出（仅 status=verified，repository_id 对齐 RepoRouterV2Adapter include 优先级）——消费 88 确认仓而非重选仓"
    - "per-repo 方案七要素（负责事项/代码预改动/影响业务模块/预计 e2e·单测+覆盖项/风险/feature list 不清处/与现功能冲突）+ overall 整体方案 + 跨仓上下文经扩展 research prompt + ArchitectMergeAdapter merge schema 承载（PartialPlan/MergedPlan content）"
    - "终态 canonical 方案镜像进项目 RESEARCH：经 ProjectDocService.append_research_note（INV-6 写收口 → 触发 Phase 83 block 级增量双向镜像飞书，never-clobber，绝不整篇覆盖）"
    - "卡片多轮校验/澄清复用既有 ClarifyAdapter clarification HITL + CardKit 流式卡（节点 waiting_event + clarifying 未答短路，镜像 plan_research/87/88 范式）"
    - "观测自检：新增 LLM 调用来源登记 plan_deepen（per-repo/overall 七要素深化）+ plan_revision（89-02 用）+ branch_naming（89-04 用）三枚举并登记 LOGGING-SPEC §4.1（27→30，集中本 plan 注册，下游 wave 仅消费不改 call_source.py 防并行冲突）；既有 plan_merge/deep_analysis_container 沿用"
  artifacts:
    - path: "server/initiatives/services/plan_deepen_service.py"
      provides: "PlanDeepenService（INV-6 编排收口：消费 88 verified → 引擎深化 → 七要素 → canonical → RESEARCH 镜像）"
      contains: "class PlanDeepenService"
    - path: "server/agents/call_source.py"
      provides: "CallSource.{PLAN_DEEPEN, PLAN_REVISION, BRANCH_NAMING} 新枚举值（27→30）"
      contains: "PLAN_DEEPEN"
    - path: ".planning/observability/LOGGING-SPEC.md"
      provides: "§4.1 新增 plan_deepen/plan_revision/branch_naming 行 + 枚举计数 30"
      contains: "plan_deepen"
    - path: "server/workflows/nodes/integrations/plan_deepen.py"
      provides: "PlanDeepenNode（消费 88 + waiting_event 多轮校验卡 + node_execution_id 续驱）"
      contains: "class PlanDeepenNode"
    - path: "server/feishu/cards/plan_deepen_card.py"
      provides: "方案深化候选/澄清/终态卡片构建"
      contains: "build_plan_deepen_card"
  key_links:
    - from: "server/initiatives/services/plan_deepen_service.py::PlanDeepenService.deepen"
      to: "server/initiatives/services/repo_association_service.py::RepoAssociationService.get_verified_associations"
      via: "消费 88 verified 关联 → include_repos"
      pattern: "get_verified_associations"
    - from: "server/initiatives/services/plan_deepen_service.py::PlanDeepenService.deepen"
      to: "server/services/plan_orchestration/entrypoint.py::build_orchestration_engine"
      via: "v0.7 同一引擎工厂 + adrive_plan_session_to_pause_or_terminal 续驱"
      pattern: "build_orchestration_engine"
    - from: "server/initiatives/services/plan_deepen_service.py::PlanDeepenService"
      to: "server/initiatives/services/project_doc_service.py::ProjectDocService.append_research_note"
      via: "终态方案镜像进 RESEARCH → 触发 83 同步飞书"
      pattern: "append_research_note"
---

<objective>
交付 Phase 89 PLAN-01 主链路与观测地基：消费 Phase 88 确认仓（`get_verified_associations`），经 **v0.7 同一编排引擎**（`build_orchestration_engine` + `adrive_plan_session_to_pause_or_terminal`，不造两套）深化出 **per-repo 七要素 + overall 整体方案 + 跨仓上下文**，落 canonical `TechnicalPlan`/`PlanVersion`（复用不新建），并把终态方案**镜像进项目 RESEARCH**（经 `ProjectDocService.append_research_note` → Phase 83 双向镜像飞书）；卡片多轮校验澄清复用既有 `ClarifyAdapter` HITL。本 plan 同时**集中注册** Phase 89 全部新 `call_source`（`plan_deepen`/`plan_revision`/`branch_naming`）+ LOGGING-SPEC §4.1（防 Wave 2 并行改同文件冲突）。

锁定决策（CONTEXT/RESEARCH）：方案载体复用 v0.7 `TechnicalPlan`/`PlanVersion` + 镜像进 RESEARCH（经 83 同步引擎）；消费 88 `get_verified_associations`；观测强制——新 LLM 赋 `call_source` 并登记 §4.1，写入收口单一 service（INV-6），脱敏 + `initiated_by_user_id`。

Output: `PlanDeepenService` + `PlanDeepenNode` + 七要素 merge schema 扩展 + RESEARCH 镜像接线 + 三新 call_source 登记 + 卡片构建 + 单测。
</objective>

<execution_context>
@.planning/phases/89-tech-plan-branch/89-CONTEXT.md
@.planning/phases/89-tech-plan-branch/89-RESEARCH.md
@.planning/observability/LOGGING-SPEC.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@server/services/plan_orchestration/entrypoint.py
@server/services/plan_orchestration/resume.py
@server/services/plan_orchestration/architect_merge_adapter.py
@server/services/plan_orchestration/research_adapter.py
@server/delivery/models/technical_plan.py
@server/initiatives/services/repo_association_service.py
@server/initiatives/services/project_doc_service.py
@server/agents/call_source.py
@server/workflows/nodes/integrations/board_split_review.py
@.cursor/rules/observability-logging.mdc
</context>

<artifacts_produced>
- EXTEND `server/agents/call_source.py`：枚举末尾新增 `PLAN_DEEPEN = "plan_deepen"`（Phase 89 per-repo/overall 七要素方案深化 LLM）、`PLAN_REVISION = "plan_revision"`（Phase 89 「调研问题发现」修订检测 LLM，89-02 消费）、`BRANCH_NAMING = "branch_naming"`（Phase 89 分支名生成 LLM，89-04 消费）；模块/类 docstring「27 值」→「30 值」。
- EXTEND `.planning/observability/LOGGING-SPEC.md` §4.1：追加三行（`plan_deepen` 入口=PlanDeepenService/ArchitectMergeAdapter 深化；`plan_revision` 入口=89-02 修订回路；`branch_naming` 入口=89-04 分支名生成）；标题枚举计数同步 30。
- NEW `server/initiatives/services/plan_deepen_service.py`：`class PlanDeepenService`（INV-6 编排收口）。方法：
  - `async deepen(*, project, work_item=None, requirement_text, node_execution_id="", initiated_by_user_id="system") -> PlanSession`：① `verified = await RepoAssociationService().get_verified_associations(project=project, work_item=work_item)` → `include_repos=[v["repository_id"] for v in verified]`；② `session = await start_orchestration("workflow", requirement_text, work_item=work_item, include_repos=include_repos)`；③ `engine = build_orchestration_engine(node_execution_id=node_execution_id)`；④ `session = await adrive_plan_session_to_pause_or_terminal(engine, session)`；⑤ 终态 DONE → `await self._mirror_to_research(project, session, initiated_by_user_id)`。全程结构化事件 `plan_deepen_started/_completed/_failed`（category=caller, component=plan_deepen, duration_ms）+ best-effort 观测不反噬。
  - `async _mirror_to_research(project, session, initiated_by_user_id)`：取 canonical `current_plan_version` 文本（per-repo 七要素 + overall）→ `await ProjectDocService().append_research_note(project_id=project.id, note=<脱敏后方案 markdown>, initiated_by_user_id=initiated_by_user_id)`（best-effort try/except，失败记 `plan_deepen_mirror_failed` 不反噬主链）。
- EXTEND `server/services/plan_orchestration/research_adapter.py`：per-repo explore 容器深化 prompt 扩为产**七要素**（负责事项 / 代码预改动 / 影响业务模块 / 预计 e2e·单测+覆盖项 / 风险 / feature list 不清处 / 与现功能冲突）的结构化 JSON（注入对应仓 88 verdict 的 `matched_node_paths`/`routed_reason` 作上下文）；`use_call_source(CallSource.PLAN_DEEPEN)` 包裹该深化调用（若与既有 DEEP_ANALYSIS_CONTAINER 同一容器，新增方案深化语义 prompt 段，call_source 维度细分到 plan_deepen）。**保持既有签名/续驱契约零回归**。
- EXTEND `server/services/plan_orchestration/architect_merge_adapter.py`：merge schema 扩 overall 整体方案 + 跨仓上下文段（聚合各仓七要素 + 跨仓依赖/冲突提要）；`use_call_source(CallSource.PLAN_DEEPEN)` 维度（既有 PLAN_MERGE 可保留，深化语义并入）。零回归既有 MergedPlan 字段。
- NEW `server/workflows/nodes/integrations/plan_deepen.py`：`class PlanDeepenNode(BaseNode)`（`node_type="plan_deepen"`，`is_blocking`/`waiting_event`）。execute：解析 project（mirror `_aresolve_project`）+ initiated_by（`triggered_by_id` 或 `system`）→ `PlanDeepenService().deepen(node_execution_id=str(context.node_execution.id), ...)` → clarifying 未答短路返回 `waiting_event`（发澄清卡 + `WorkflowEventSubscription` 超时兜底）→ 终态映射。registry 自动发现（放 `integrations/`）。
- NEW `server/feishu/cards/plan_deepen_card.py`：`build_plan_deepen_card`（深化进行中/澄清问询/终态方案概览，CardKit schema 2.0，action_value 仅携 execution_id/node_id/round，不携方案正文）。
- NEW `server/tests/initiatives/test_plan_deepen_service.py`：`get_verified_associations` 被调且 include_repos 透传（seam mock）、引擎工厂复用（assert `build_orchestration_engine` 调用、无第二工厂）、终态 DONE → `append_research_note` 被调（mirror）、非 DONE 不镜像、`call_source.normalize('plan_deepen'/'plan_revision'/'branch_naming')` 命中。
- NEW `server/tests/workflows/test_plan_deepen_node.py`：clarifying 未答 → `status=="waiting_event"` + 订阅创建；终态 → 映射 completed；initiated_by 透传。
- structlog：`plan_deepen_started/_completed/_failed/_mirror_failed`（category=caller, component=plan_deepen, duration_ms）；per-repo/merge 步骤 category=sampling。
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: 注册 Phase 89 三新 call_source + LOGGING-SPEC §4.1（观测地基，集中注册防并行冲突）</name>
  <read_first>
    - server/agents/call_source.py（CallSource 枚举全文 + docstring「27 值」+ normalize；末尾 repo_verify_container/repo_association 为 88 新增样板）
    - .planning/observability/LOGGING-SPEC.md（§4.1 表 board_split/repo_verify_container 行格式 + 枚举计数注释）
    - .planning/phases/88-repo-association/88-01-PLAN.md（call_source 注册 + LOGGING-SPEC 范式，逐字镜像）
  </read_first>
  <action>
    `call_source.py`：枚举末尾按 88 样式追加 `PLAN_DEEPEN = "plan_deepen"`、`PLAN_REVISION = "plan_revision"`、`BRANCH_NAMING = "branch_naming"`，各带 Phase 89 用途中文注释；模块 docstring 与类 docstring 的「27 值」改「30 值」（含开头 `UNKNOWN_CALL_SOURCE` 注释处「22 类」表述同步审视，按既有计数风格更新）。`LOGGING-SPEC.md` §4.1 表在末尾追加三行：`plan_deepen`（入口=PlanDeepenService / ArchitectMergeAdapter，per-repo 七要素 + overall 深化，v0.16.0 Phase 89）、`plan_revision`（入口=修订回路「调研问题发现」检测，Phase 89）、`branch_naming`（入口=分支名生成，Phase 89）；把 §4.1 标题处枚举计数与 call_source.py 同步为 30。
  </action>
  <verify>
    <automated>cd server && uv run python -c "import django; django.setup(); from agents.call_source import CallSource; assert CallSource.normalize('plan_deepen')=='plan_deepen' and CallSource.normalize('plan_revision')=='plan_revision' and CallSource.normalize('branch_naming')=='branch_naming'"</automated>
  </verify>
  <acceptance_criteria>
    - `CallSource.PLAN_DEEPEN.value=='plan_deepen'`、`PLAN_REVISION.value=='plan_revision'`、`BRANCH_NAMING.value=='branch_naming'`；normalize 命中不回退 unknown。
    - LOGGING-SPEC §4.1 含 `plan_deepen`/`plan_revision`/`branch_naming` 三行；枚举计数注释同步 30。
    - call_source.py docstring「27 值」全部更新为「30 值」（grep 无残留 27）。
  </acceptance_criteria>
  <done>Phase 89 三 LLM 来源受控登记，可作指标/留痕标签；下游 Wave 2 plan 仅消费不再改 call_source.py。</done>
</task>

<task type="auto">
  <name>Task 2: PlanDeepenService —— 消费 88 verified + v0.7 引擎深化 + RESEARCH 镜像（INV-6 收口）</name>
  <read_first>
    - server/services/plan_orchestration/entrypoint.py（start_orchestration / build_orchestration_engine 签名 + include_repos 形态 + node_execution_id 语义）
    - server/services/plan_orchestration/resume.py（adrive_plan_session_to_pause_or_terminal 短路/终态契约）
    - server/initiatives/services/repo_association_service.py（get_verified_associations 输出形态 753-793）
    - server/initiatives/services/project_doc_service.py（append_research_note 签名 104-160 + INV-6/83 触发）
    - server/delivery/models/technical_plan.py（PlanVersion.content / TechnicalPlan.current_version 取终态方案文本）
    - server/initiatives/services/project_branch_service.py（INV-6 service 风格：sync_to_async + structlog duration_ms + 归因范式）
  </read_first>
  <action>
    新建 `server/initiatives/services/plan_deepen_service.py`，`class PlanDeepenService`（编排收口）。`async deepen(...)`：取 `get_verified_associations` → `include_repos` → `start_orchestration("workflow", requirement_text, work_item=work_item, include_repos=include_repos)` → `build_orchestration_engine(node_execution_id=node_execution_id)`（**绝不新建第二个 engine 工厂**）→ `adrive_plan_session_to_pause_or_terminal(engine, session)` → 终态 DONE 调 `_mirror_to_research`。`_mirror_to_research`：从 session 取 canonical `PlanVersion.content` 渲染 per-repo 七要素 + overall markdown（经 `redact_secrets_in_text`）→ `ProjectDocService().append_research_note(project_id=..., ...)`（best-effort）。全程 `structlog` 结构化事件 + `duration_ms` + `category="caller"` + `component="plan_deepen"` + `initiated_by_user_id`；async ORM 经既有 service 异步方法，无裸 lazy-FK。无 verified 仓 → 记 `plan_deepen_no_verified_repos` 并仍可走自然语言需求（include_repos=[]，引擎自路由），不抛。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/initiatives/test_plan_deepen_service.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `plan_deepen_service.py` 含 `class PlanDeepenService`，`deepen` 调 `get_verified_associations` 取 include_repos、调 `build_orchestration_engine`（grep 确认无第二 engine 工厂）、调 `adrive_plan_session_to_pause_or_terminal`。
    - 终态 DONE → `append_research_note` 被调（mirror）；非 DONE 不镜像（测试断言）。
    - 服务无旁路写 TechnicalPlan/PlanVersion（写经 engine/TechnicalPlanService），无旁路写 ProjectDoc（经 append_research_note）。
  </acceptance_criteria>
  <done>PLAN-01 主链路收口：88 确认仓 → v0.7 引擎深化 → canonical 落库 → RESEARCH 镜像，全经既有写收口（INV-6）。</done>
</task>

<task type="auto">
  <name>Task 3: per-repo 七要素 + overall 跨仓 schema 扩展（research/merge adapter，零回归）</name>
  <read_first>
    - server/services/plan_orchestration/research_adapter.py（ResearchDispatchAdapter per-repo explore prompt + verdict/PartialPlan 解析 + use_call_source 现状）
    - server/services/plan_orchestration/architect_merge_adapter.py（merge prompt + MergedPlan schema + call_source=plan_merge）
    - server/delivery/models/research_task.py（PartialPlan content schema）
    - server/agents/call_source.py（PLAN_DEEPEN 已在 Task 1 注册）
  </read_first>
  <action>
    `research_adapter.py`：per-repo explore 深化 prompt 追加产七要素结构化 JSON（负责事项/代码预改动/影响业务模块/预计 e2e·单测+覆盖项/风险/feature list 不清处/与现功能冲突），注入对应仓 88 verdict 的 `matched_node_paths`/`routed_reason` 作上下文；调用处 `with use_call_source(CallSource.PLAN_DEEPEN):`（细分方案深化维度）；`arecord_llm_usage(call_source=...)` 已有则沿用。`architect_merge_adapter.py`：merge schema 扩 overall 整体方案 + 跨仓上下文（聚合各仓七要素 + 跨仓依赖/冲突）。**保持既有 PartialPlan/MergedPlan 字段与续驱/回调契约零回归**（仅扩内容字段，不改状态机/签名）。所有 LLM 调用脱敏 + best-effort 观测。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services -k "plan_orchestration or merge or research_adapter" -q 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - research_adapter 深化调用包 `use_call_source(CallSource.PLAN_DEEPEN)`；prompt 含七要素字段名。
    - merge 产物含 overall 整体方案 + 跨仓上下文段；既有 plan_orchestration 测试零回归（全绿）。
    - per-repo content 可承载七要素（schema 字段存在，测试覆盖渲染）。
  </acceptance_criteria>
  <done>方案产物从基础 PartialPlan/MergedPlan 升级为七要素 + overall 跨仓，承载 PLAN-01 内容契约。</done>
</task>

<task type="auto">
  <name>Task 4: PlanDeepenNode + 卡片多轮校验澄清（waiting_event HITL，复用 ClarifyAdapter）</name>
  <read_first>
    - server/workflows/nodes/integrations/board_split_review.py（waiting_event + WorkflowEventSubscription + 流式卡 + initiated_by 解析逐字模板）
    - server/services/plan_orchestration/clarify_adapter.py（ClarifyAdapter clarification HITL）
    - server/feishu/cards/board_split_card.py（CardKit 流式卡 + action_value 仅路由 ID 范式）
    - server/workflows/nodes/base.py（BaseNode/NodeResult/NodePort/ExecutionContext）+ registry.py（register_node 自动发现）
    - server/initiatives/services/plan_deepen_service.py（Task 2 产物）
  </read_first>
  <action>
    新建 `server/workflows/nodes/integrations/plan_deepen.py`：`PlanDeepenNode`（`node_type="plan_deepen"`，inputs/outputs default+error）。execute：解析 project + initiated_by → `PlanDeepenService().deepen(node_execution_id=str(context.node_execution.id), ...)` → session 处于 CLARIFYING 未答 → 发澄清卡（`build_plan_deepen_card` stage=clarify）+ `WorkflowEventSubscription`（timeout_at + timeout_action="fail")→ 返回 `NodeResult(status="waiting_event", ...)`；终态 DONE → 发终态方案概览卡 → `NodeResult(status="completed", output={plan_version_id,...})`；FAILED → error 分支。新建 `server/feishu/cards/plan_deepen_card.py::build_plan_deepen_card`（进行中/澄清/终态三态，action_value 仅 execution_id/node_id/round）。registry 自动发现（确认 `workflows/nodes/__init__.py` 导入路径覆盖 integrations 包）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/workflows/test_plan_deepen_node.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `PlanDeepenNode` 注册（`node_type=="plan_deepen"`，registry 可发现）；clarifying 未答 → `status=="waiting_event"` + 订阅创建（测试断言）。
    - 终态 → completed，output 含 plan_version 锚；FAILED → error 分支。
    - 卡片 action_value 不含方案正文（仅路由 ID）；多轮澄清保持 waiting（不误 approve）。
  </acceptance_criteria>
  <done>PLAN-01 卡片多轮校验澄清 HITL 就位，复用 ClarifyAdapter + CardKit，节点续驱经 node_execution_id。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 88 verified 关联 → include_repos | 跨 phase 只读契约，喂引擎深化范围 |
| 方案正文 → RESEARCH/飞书 | 经 append_research_note/83 block 级增量（never-clobber） |
| call_source 维度 | 受控枚举，防基数失控 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-01-TAMPER | Tampering | 旁路写 TechnicalPlan/ProjectDoc | mitigate | 写经 engine/TechnicalPlanService/append_research_note（INV-6） |
| T-89-01-INFO | Info Disclosure | 方案正文/token 落日志 | mitigate | redact_secrets_in_text / redact_for_ledger；append_research_note 前脱敏 |
| T-89-01-CARD | Tampering | call_source 基数失控 | mitigate | CallSource.normalize 受控回退 |
| T-89-01-SC | Tampering | npm/pip installs | accept | 本 plan 无包安装（纯内部复用） |
</threat_model>

<verification>
- `cd server && uv run pytest tests/initiatives/test_plan_deepen_service.py tests/workflows/test_plan_deepen_node.py -q` 全绿。
- `cd server && uv run pytest tests/services -k plan_orchestration -q` 零回归。
- grep 确认 PlanDeepenService 用 `build_orchestration_engine`（无第二 engine 工厂）+ `append_research_note`（RESEARCH 镜像）。
- `CallSource.normalize` 命中三新值；LOGGING-SPEC §4.1 含三行 + 计数 30。
- ruff + mypy（新文件）通过。
</verification>

<success_criteria>
- 消费 88 verified 仓 → v0.7 同一引擎深化 → per-repo 七要素 + overall 跨仓 → canonical TechnicalPlan/PlanVersion（复用不新建）。
- 终态方案镜像进项目 RESEARCH（→ 83 双向同步飞书，never-clobber）。
- 卡片多轮校验澄清 HITL 就位（复用 ClarifyAdapter）。
- 三新 call_source 登记 §4.1（观测强制），写入收口 INV-6，脱敏 + 归因。
</success_criteria>

<output>
完成后创建 `.planning/phases/89-tech-plan-branch/89-01-SUMMARY.md`
</output>
