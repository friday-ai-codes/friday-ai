---
phase: 89
plan: 04
type: execute
wave: 2
depends_on: [89-01]
files_modified:
  - server/initiatives/services/branch_provision_service.py
  - server/initiatives/services/branch_naming.py
  - server/feishu/cards/branch_confirm_card.py
  - server/feishu/callbacks/branch_confirm_callback.py
  - server/feishu/urls.py
  - server/tests/initiatives/test_branch_naming.py
  - server/tests/initiatives/test_branch_provision.py
  - server/tests/feishu/test_branch_confirm_callback.py
autonomous: true
requirements: [PLAN-04]
must_haves:
  truths:
    - "方案确认后按方案（含分支名）对每个仓库建分支并推送 + 绑定 仓库↔分支↔项目（写 ProjectBranch 经 ProjectBranchService.bind(source=BranchSource.PLAN)，Phase 85 seam 已就绪），回接 IDE 闭环——绝不旁路写 ProjectBranch"
    - "分支名固定格式（用户精确指定）：{type}/{yymmdd}.m-{项目跟踪id}.{项目名}[-{版本号}]——type=conventional commits(feat/fix/chore…)、yymmdd 日期、m-{飞书项目跟踪 work_item id}、{项目名}=项目跟踪看板名一致、{版本号} 有则填(如 v1.0)否则省略；示例 feat/260610.m-123456770019.高三提分专项-v1.0"
    - "AI 生成分支名 + 用户卡片确认：LLM(call_source=branch_naming，89-01 注册) 仅定 type + 是否带版本号；id/项目名/日期由 server 权威字段拼装（不信 LLM 自由拼）；正则 ^(feat|fix|chore|refactor|docs|style|test|perf|build|ci)/\\d{6}\\.m-\\w+\\.[^/]+(-v[\\d.]+)?$ 校验，非法 server 兜底默认 type=feat 标准名"
    - "逐仓建分支推送复用 CreateBranchNode server-local git 逻辑（DATA_DIR/repos/{repo_id} fetch/checkout/create/push）+ aresolve_git_token 注入 push URL（per-repo→host fallback，SSH→HTTPS 改写）；单仓 try/except 隔离 fail-soft（succeeded/failed 收集），未克隆仓标失败不阻断其余"
    - "分支已存在幂等：建前 branch_exists 判定，已存在跳过 create 仅绑定；ProjectBranchService.bind 本身幂等(get_or_create)；卡片确认 HITL 多轮"
    - "观测/安全：建推绑结构化事件(caller, component=initiatives, duration_ms) + initiated_by_user_id；git token 绝不入日志(仅 has_git_token 布尔)；分支名/异常脱敏；写经 service(INV-6)"
  artifacts:
    - path: "server/initiatives/services/branch_provision_service.py"
      provides: "BranchProvisionService（逐仓建分支推送 + ProjectBranchService.bind 绑定，INV-6 编排收口，单仓 fail-soft）"
      contains: "class BranchProvisionService"
    - path: "server/initiatives/services/branch_naming.py"
      provides: "分支名 AI 生成 + 固定格式权威拼装/正则校验/兜底"
      contains: "build_branch_name"
    - path: "server/feishu/callbacks/branch_confirm_callback.py"
      provides: "@register_card_callback('branch_confirm_') 分支确认 FSM"
      contains: "register_card_callback"
  key_links:
    - from: "server/initiatives/services/branch_provision_service.py::BranchProvisionService.provision_and_bind"
      to: "server/initiatives/services/project_branch_service.py::ProjectBranchService.bind"
      via: "建推后绑 仓库↔分支↔项目（source=BranchSource.PLAN，85 seam）"
      pattern: "ProjectBranchService"
    - from: "server/initiatives/services/branch_provision_service.py::BranchProvisionService"
      to: "server/services/git_credentials.py::aresolve_git_token"
      via: "push URL token 注入（per-repo→host fallback）"
      pattern: "aresolve_git_token"
    - from: "server/initiatives/services/branch_naming.py::build_branch_name"
      to: "server/agents/call_source.py::CallSource.BRANCH_NAMING"
      via: "AI 生成 type/版本号（call_source=branch_naming，89-01 注册）"
      pattern: "BRANCH_NAMING"
---

<objective>
交付 Phase 89 PLAN-04 建分支绑项目：方案确认后逐仓**按固定格式分支名建分支并推送** + **绑定 仓库↔分支↔项目**（写 `ProjectBranch` 经 `ProjectBranchService.bind(source=plan)`，Phase 85 seam 已就绪），回接 IDE 闭环。分支名固定格式 `{type}/{yymmdd}.m-{项目跟踪id}.{项目名}[-{版本号}]`，AI 生成（`call_source=branch_naming`，89-01 注册）+ server 权威拼装/正则校验/兜底 + 用户卡片确认。建分支推送复用 `CreateBranchNode` server-local git 逻辑 + `aresolve_git_token`，单仓 fail-soft 隔离。

锁定决策（CONTEXT/RESEARCH）：固定格式（用户精确指定）；AI 生成 + 卡片确认；绑定经 `ProjectBranchService.bind(source=plan)`；写收口 INV-6；token 不入日志；脱敏 + `initiated_by_user_id`；fail-soft。

Output: `BranchProvisionService` + `branch_naming` 生成/校验 + 确认卡片/回调 + 单测。
</objective>

<execution_context>
@.planning/phases/89-tech-plan-branch/89-CONTEXT.md
@.planning/phases/89-tech-plan-branch/89-RESEARCH.md
@.planning/observability/LOGGING-SPEC.md
</execution_context>

<context>
@.planning/ROADMAP.md
@server/initiatives/services/project_branch_service.py
@server/initiatives/models/project_branch.py
@server/workflows/nodes/git/branch.py
@server/services/git_credentials.py
@server/initiatives/services/repo_association_service.py
@server/agents/call_source.py
@server/feishu/callbacks/repo_association_callback.py
@.cursor/rules/observability-logging.mdc
</context>

<artifacts_produced>
- NEW `server/initiatives/services/branch_naming.py`：
  - `build_branch_name(*, change_type, yymmdd, tracking_id, project_name, version="") -> str`：server 权威拼装固定格式 `{type}/{yymmdd}.m-{tracking_id}.{project_name}[-{version}]`；`project_name` 规整（去非法路径字符/空白）；version 有则 `-{version}`。
  - `async generate_branch_name(*, repo, project, work_item, requirement_text, initiated_by_user_id) -> dict`：`use_call_source(CallSource.BRANCH_NAMING)` LLM 仅定 `change_type`（conventional commits 取值）+ 是否带版本号 + 抽版本号；`tracking_id`=飞书 work_item id（项目跟踪 id，server 权威）、`project_name`=项目跟踪看板名（缺则 project.name 兜底，A6）、`yymmdd`=当天日期，由 server 拼装 → `build_branch_name`；`arecord_llm_usage` + 脱敏 + best-effort（LLM 失败 → 默认 type=feat 兜底）。
  - `validate_branch_name(name) -> bool`：正则 `^(feat|fix|chore|refactor|docs|style|test|perf|build|ci)/\d{6}\.m-\w+\.[^/\s]+(-v[\d.]+)?$`。
- NEW `server/initiatives/services/branch_provision_service.py`：`class BranchProvisionService`（INV-6 编排收口）：
  - `async provision_and_bind(*, project, repositories, branch_names, actor, initiated_by_user_id, feishu_board_id="") -> dict`：逐仓（复用 `CreateBranchNode._create_branch_for_repository` 等价逻辑：`DATA_DIR/repos/{repo_id}` fetch/checkout base/create/push，**push URL 注入 `aresolve_git_token`**，SSH→HTTPS 改写）→ 建/推成功 → `ProjectBranchService().bind(project_id=project.id, repository_id=repo.id, branch_name=name, source=BranchSource.PLAN, actor=actor, initiated_by_user_id=initiated_by_user_id, feishu_board_id=feishu_board_id, _skip_member_check=True)`。单仓 try/except 隔离（succeeded/failed 收集，未克隆/push 失败标失败不阻断其余）；分支已存在 → branch_exists 判定跳过 create 仅 bind（幂等）。结构化事件 `branch_provision_started`/`branch_pushed`/`branch_bound`/`branch_provision_failed`（caller, component=initiatives, duration_ms）；token 仅记 `has_git_token`。
- NEW `server/feishu/cards/branch_confirm_card.py`：`build_branch_confirm_card`（逐仓建议分支名列表 + 确认/改 type/取消按钮）+ `build_branch_done_card`（建推绑结果：succeeded/failed 逐仓）。action_value 仅 execution_id/node_id/round/action（分支名经 server 权威态，不靠 action_value 携正文）。
- NEW `server/feishu/callbacks/branch_confirm_callback.py`：`@register_card_callback("branch_confirm_")`（前缀唯一，grep 不撞既有）。FSM：`branch_confirm_apply` → `BranchProvisionService.provision_and_bind` → done 卡 → `approve_node`；`branch_confirm_edit` → 调整 type 重 `generate_branch_name` round+1 保持 waiting；`branch_confirm_cancel` → 不建分支。同步轻 ack + `_run_in_thread` + `bind_task_context(callback.user_open_id)`；全段 fail-soft；非 waiting 幂等 no-op。
- EXTEND `server/feishu/urls.py`：import `branch_confirm_callback` 触发注册。
- NEW tests：
  - `test_branch_naming.py`：`build_branch_name` 产固定格式（含/不含版本号）、示例 `feat/260610.m-123456770019.高三提分专项-v1.0` 通过 `validate_branch_name`；非法名 validate False；LLM 失败兜底 type=feat；project_name 缺 → project.name 兜底；id/日期 server 权威（LLM 不改）。
  - `test_branch_provision.py`：逐仓建+推（mock git/subprocess seam）+ `ProjectBranchService.bind(source=plan)` 被调；单仓失败隔离不阻断其余（fail-soft）；分支已存在跳过 create 仅 bind（幂等）；token 注入（mock aresolve_git_token，断言不入日志）。
  - `test_branch_confirm_callback.py`：apply→provision_and_bind+approve；edit→保持 waiting round+1；cancel→不建；前缀唯一；fail-soft。
- structlog：`branch_naming`(sampling)、`branch_provision_started`/`branch_pushed`/`branch_bound`/`branch_provision_failed`/`branch_confirm_card_action`（caller, component=initiatives, duration_ms）；归因 `initiated_by_user_id`（回调 `callback.user_open_id`），系统 `system`；token 绝不入日志。
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: 分支名 AI 生成 + 固定格式权威拼装/正则校验/兜底（call_source=branch_naming）</name>
  <read_first>
    - .planning/phases/89-tech-plan-branch/89-CONTEXT.md（分支命名固定格式规则 + 示例，逐字遵循）
    - server/agents/call_source.py（BRANCH_NAMING 已 89-01 注册 + use_call_source）
    - server/initiatives/services/board_split_service.py（LLM 调用 + arecord_llm_usage + 脱敏范式，如可读）
    - server/interactions/ledger.py（arecord_llm_usage 签名）
  </read_first>
  <action>
    新建 `branch_naming.py`：`build_branch_name`（server 权威拼装固定格式 + project_name 路径规整 + version 可选）、`validate_branch_name`（正则）、`generate_branch_name`（`use_call_source(CallSource.BRANCH_NAMING)` LLM 仅定 type + 版本号开关/值；tracking_id/project_name/yymmdd server 权威拼；LLM 失败/非法 → 兜底 type=feat 标准名；`arecord_llm_usage` + 脱敏 + best-effort）。绝不让 LLM 自由拼整名（防注入/格式漂移）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/initiatives/test_branch_naming.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `build_branch_name(change_type="feat", yymmdd="260610", tracking_id="123456770019", project_name="高三提分专项", version="v1.0")=="feat/260610.m-123456770019.高三提分专项-v1.0"`；无版本号则省略 `-vX`。
    - `validate_branch_name` 对示例 True、对非法名 False；id/日期 server 权威（LLM mock 改 id 不生效）。
    - `generate_branch_name` 包 `use_call_source(CallSource.BRANCH_NAMING)` + LLM 失败兜底 type=feat。
  </acceptance_criteria>
  <done>分支名固定格式生成就位，AI 仅定 type/版本号，server 权威拼装 + 正则兜底（防漂移/注入）。</done>
</task>

<task type="auto">
  <name>Task 2: BranchProvisionService —— 逐仓建分支推送 + ProjectBranchService.bind(source=plan)（INV-6 + fail-soft）</name>
  <read_first>
    - server/workflows/nodes/git/branch.py（_create_branch_for_repository fetch/checkout/create/push + 并行 + 单仓 fail-soft 逐字范式 319-466）
    - server/services/git_credentials.py（aresolve_git_token per-repo→host fallback + SSH→HTTPS 改写范式）
    - server/initiatives/services/project_branch_service.py（bind(source=BranchSource.PLAN, _skip_member_check) 幂等 + 审计 75-172）
    - server/initiatives/models/project_branch.py（BranchSource.PLAN + unique 约束）
    - server/services/git_platform/base.py（branch_exists 81 幂等判定）
  </read_first>
  <action>
    新建 `branch_provision_service.py::BranchProvisionService.provision_and_bind`：逐仓复用 CreateBranchNode 建分支推送逻辑（`DATA_DIR/repos/{repo_id}` fetch/checkout base/create/push），**push URL 注入 `aresolve_git_token`**（SSH→HTTPS），建前 `branch_exists` 判定（已存在跳过 create 仅 bind）；成功 → `ProjectBranchService().bind(..., source=BranchSource.PLAN, _skip_member_check=True, feishu_board_id=...)`。单仓 try/except 隔离收集 succeeded/failed（未克隆/push 失败标失败不阻断其余）。结构化事件 + duration_ms + 归因 + token 仅 `has_git_token`；写经 ProjectBranchService（INV-6）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/initiatives/test_branch_provision.py tests/initiatives/test_project_branch_inv6_guard.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - 逐仓建+推（seam mock）+ `ProjectBranchService.bind(source=BranchSource.PLAN)` 被调（grep 无旁路写 ProjectBranch）。
    - 单仓失败隔离不阻断其余（succeeded/failed 收集，测试断言）；分支已存在跳过 create 仅 bind（幂等）。
    - push 注入 `aresolve_git_token`；日志仅 `has_git_token` 布尔（token 不明文，测试断言）。
  </acceptance_criteria>
  <done>逐仓建分支推送 + 绑 仓库↔分支↔项目（source=plan），fail-soft，写收口 INV-6。</done>
</task>

<task type="auto">
  <name>Task 3: 分支确认卡片 + 回调 FSM（镜像 87/88，前缀唯一 + fail-soft，回接 IDE 闭环）</name>
  <read_first>
    - server/feishu/callbacks/repo_association_callback.py（@register_card_callback + _run_in_thread + bind_task_context + approve_node/保持 waiting 逐字模板）
    - server/feishu/cards/repo_association_card.py（CardKit 卡 + action_value 仅路由 ID 范式）
    - server/feishu/urls.py（callback import 注册位置）
    - server/initiatives/services/branch_provision_service.py（Task 2）+ branch_naming.py（Task 1）
  </read_first>
  <action>
    新建 `branch_confirm_card.py`（`build_branch_confirm_card` 逐仓建议分支名 + confirm/edit/cancel；`build_branch_done_card` succeeded/failed）+ `branch_confirm_callback.py`（`@register_card_callback("branch_confirm_")`，FSM：apply→provision_and_bind→done 卡→approve_node；edit→调整 type 重 generate_branch_name round+1 保持 waiting；cancel→不建）。同步轻 ack + `_run_in_thread` + `bind_task_context`；全段 fail-soft（失败记 `branch_provision_failed` 脱敏不反噬）；非 waiting 幂等。`feishu/urls.py` import 触发注册。done 卡含回接 IDE 闭环提示（绑定后分支可被 IDE rule/MCP 反查项目）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/feishu/test_branch_confirm_callback.py -x -q && cd server && grep -rn "branch_confirm_" feishu/ | head</automated>
  </verify>
  <acceptance_criteria>
    - `@register_card_callback("branch_confirm_")` 前缀唯一（grep 不撞 board_split_/repo_assoc_/plan_revise_/chat_question_）；urls.py 已 import。
    - apply → `provision_and_bind` 被调 + `approve_node`；edit → 保持 waiting round+1；cancel → 不建分支。
    - service 抛 → ack 仍返回 + 记 failed（fail-soft）；非 waiting 幂等。
  </acceptance_criteria>
  <done>分支确认 HITL 就位，建推绑回接 IDE 闭环，回调零旁路写表（写经 service）。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 用户/LLM 输入 → 分支名 | id/项目名/日期 server 权威拼；LLM 仅定 type/版本号；正则校验兜底 |
| git token → push URL | aresolve_git_token 注入，绝不入日志 |
| 卡片输入 → 建分支 | action_value 仅路由 ID；分支名经 server 权威态 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-04-TAMPER | Tampering | 旁路写 ProjectBranch / 分支名注入 | mitigate | bind 经 ProjectBranchService（INV-6）；分支名 server 权威拼 + 正则；LLM 不自由拼 |
| T-89-04-INFO | Info Disclosure | git token 落日志 | mitigate | 仅 has_git_token 布尔；分支名/异常脱敏 |
| T-89-04-DOS | DoS | 单仓失败拖垮全部 / 回调风暴 | mitigate | 单仓 try/except 隔离 fail-soft；回调非 waiting 幂等不回灌 5xx |
| T-89-04-SC | Tampering | npm/pip installs | accept | 本 plan 无包安装（git/subprocess 系统命令已在栈） |
</threat_model>

<verification>
- `cd server && uv run pytest tests/initiatives/test_branch_naming.py tests/initiatives/test_branch_provision.py tests/feishu/test_branch_confirm_callback.py -q` 全绿。
- 示例分支名 `feat/260610.m-123456770019.高三提分专项-v1.0` 通过 validate；非法名 False。
- grep 确认 bind 经 ProjectBranchService(source=plan)（无旁路写 ProjectBranch）；push 注入 aresolve_git_token；日志无明文 token。
- `branch_confirm_` 前缀唯一；urls.py 已 import。
- ruff + mypy（新文件）通过。
</verification>

<success_criteria>
- 方案确认后逐仓按固定格式分支名建分支推送 + 绑 仓库↔分支↔项目（source=plan，85 seam），回接 IDE 闭环。
- 分支名固定格式（AI 定 type/版本号 + server 权威拼 + 正则兜底 + 卡片确认）。
- 单仓 fail-soft 隔离；token 不入日志；写收口 INV-6，脱敏 + 归因。
</success_criteria>

<output>
完成后创建 `.planning/phases/89-tech-plan-branch/89-04-SUMMARY.md`
</output>
