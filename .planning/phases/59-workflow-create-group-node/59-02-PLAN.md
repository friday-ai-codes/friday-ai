---
phase: 59-workflow-create-group-node
plan: 02
type: execute
wave: 2
depends_on: [59-01]
files_modified:
  - server/workflows/nodes/integrations/feishu_chat.py
  - server/tests/workflows/test_chat_nodes.py
autonomous: true
requirements: [GROUP-01]
must_haves:
  truths:
    - "D-4：`CreateGroupChatNode(BaseNode)` `@register_node` 自动注册，`node_type=\"create_group_chat\"`（全局唯一，不撞 fetch_group_chat/join_group_chat）、`display_name=\"创建群聊\"`、`category=NodeCategory.INTEGRATION`、`execution_mode=\"server_local\"`，镜像 FetchGroupChatNode/JoinGroupChatNode 结构"
    - "config_schema 全中文 title/description：name(群名,业务必填,模板)、description(可选)、owner_id(可选,留空 bot 为群主)、member_ids(成员,模板,逗号|JSON|列表)、user_id_type(默认 open_id 枚举 open_id/union_id/user_id)、writeback 三件套 project_key/work_item_id/work_item_type(默认 story,命名复用 FetchGroupChatNode)"
    - "inputs=[NodePort('default','输入',OBJECT)]；outputs=[NodePort('default','成功',OBJECT), NodePort('error','失败',OBJECT)]"
    - "D-5：member_ids 三形态解析（render_template + 逗号分隔 / JSON 列表 / 模板 list）→ list[str] 去空；复用 normalize_repositories 思路（可内联轻量 _parse_id_list）；上游可注入 list"
    - "D-4 缺参语义：群名为空 **或** 成员列表为空 → NodeResult(status='failed', error=..., next_handle='error')（建群+拉人是 SC-1 核心，空成员无意义）"
    - "D-7 fail-soft 两类分开：建群失败（create_chat 抛 FeishuIMError / 缺参）→ failed + error handle；writeback 失败（WorkItem 不存在 / DB 异常）→ 节点**仍 completed** 返回 chat_id（try/except + log.warning，绝不冒泡）"
    - "D-7 writeback 触发条件：仅当 project_key + work_item_id 均非空才执行；未配 → 跳过（正常 completed）；P-11 work_item_id int() 转换失败 → warning 跳过 writeback（不掀翻建群成功）"
    - "D-8 输出形状：output={'chat_id': chat_id(一等字段), 'chat_name', 'owner_id'(容错空串,P-3), 'source':'create_group_chat', 'writeback':{'attempted':bool,'success':bool}}；下游 JoinGroupChatNode 可直接消费"
    - "P-6 INV-6：节点**禁**直接 WorkItem.objects/wi.save() 写 chat_id，只调 WorkItemService.awriteback_feishu_chat_id（Wave 1 单一入口）"
    - "零回归：FetchGroupChatNode/JoinGroupChatNode 符号逐字不变；既有 test_chat_nodes.py 全绿"
  artifacts:
    - path: "server/workflows/nodes/integrations/feishu_chat.py"
      provides: "CreateGroupChatNode（@register_node 自动注册，建群→输出 chat_id→可选 writeback fail-soft）"
      contains: "class CreateGroupChatNode"
    - path: "server/tests/workflows/test_chat_nodes.py"
      provides: "CreateGroupChatNode 集成测（扩展既有文件）"
      contains: "CreateGroupChatNode"
  key_links:
    - from: "server/workflows/nodes/integrations/feishu_chat.py"
      to: "server/services/feishu_im.py"
      via: "CreateGroupChatNode.execute → FeishuIMService.create(project).create_chat(...)"
      pattern: "create_chat"
    - from: "server/workflows/nodes/integrations/feishu_chat.py"
      to: "server/delivery/services/work_item_service.py"
      via: "CreateGroupChatNode 可选 writeback → WorkItemService().awriteback_feishu_chat_id(...)"
      pattern: "awriteback_feishu_chat_id"
---

<objective>
建立 Phase 59 的 **节点接线层（Wave 2）**：在 `server/workflows/nodes/integrations/feishu_chat.py` 新增 **`CreateGroupChatNode`**（`@register_node` 自动注册，镜像 `FetchGroupChatNode`/`JoinGroupChatNode` 结构，`execution_mode="server_local"`）——解析群名/成员（member_ids 三形态）→ 取 project + `FeishuIMService.create` → `create_chat`（Wave 1）建群即拉人 → 输出 `chat_id` 一等字段供下游 → **可选 writeback** 经 `WorkItemService.awriteback_feishu_chat_id`（Wave 1，fail-soft）。**绝不改** `FetchGroupChatNode`/`JoinGroupChatNode`。

Purpose: 兑现 GROUP-01 全部 Success Criteria——SC-1（建群+拉人）、SC-2（chat_id 节点输出供下游）、SC-3（可选 writeback + fail-soft）。fail-soft 两类分开（D-7）：建群失败走 `failed`+error handle；writeback 失败节点仍 `completed` 返回 chat_id（warning 不冒泡）。
Output: `feishu_chat.py` 增 `CreateGroupChatNode`（含 member_ids 解析 helper）；扩展 `test_chat_nodes.py`（happy / 缺群名 / 缺成员 / 建群失败 / member_ids 三形态 / writeback happy / writeback fail-soft / 未配 writeback / 自动注册断言）。

> **关键约束**：本 plan 依赖 Wave 1 的 `FeishuIMService.create_chat` 与 `WorkItemService.awriteback_feishu_chat_id`（`depends_on: [59-01]`）。节点 writeback **只调 service 方法**（P-6 INV-6），绝不在节点内直接写 WorkItem 表。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.codex/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/59-workflow-create-group-node/59-CONTEXT.md
@.planning/phases/59-workflow-create-group-node/59-RESEARCH.md
@.planning/phases/59-workflow-create-group-node/59-VALIDATION.md
@.planning/phases/59-workflow-create-group-node/59-01-PLAN.md

# 主改文件：新增 CreateGroupChatNode，镜像 FetchGroupChatNode/JoinGroupChatNode；绝不改既有两节点
@server/workflows/nodes/integrations/feishu_chat.py
# member_ids 解析思路参考（JSON 数组 / 逗号分隔 / 模板）
@server/workflows/nodes/base.py
# Wave 1 产出：FeishuIMService.create_chat + 委托（节点经 FeishuIMService.create(project) 调）
@server/services/feishu_im.py
# Wave 1 产出：WorkItemService.awriteback_feishu_chat_id（节点 writeback 单一入口）
@server/delivery/services/work_item_service.py
# 既有节点测试范式（_make_context + _mock_im_service + patch FeishuIMService.create）——扩展，不破既有
@server/tests/workflows/test_chat_nodes.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: CreateGroupChatNode（建群→输出 chat_id→可选 writeback fail-soft）+ member_ids 三形态解析</name>
  <read_first>
    - server/workflows/nodes/integrations/feishu_chat.py:23-136（FetchGroupChatNode：@register_node + node_type/display_name/category/execution_mode + config_schema 全中文 + inputs/outputs default+error + execute：render_template 取配置 → 缺参 failed+error → 取 project（context.workflow_execution.workflow.project）→ FeishuIMService.create(project) → NodeResult completed/failed）、:139-216（JoinGroupChatNode：config 优先 + input_data 兜底取 chat_id 范式）
    - server/workflows/nodes/base.py:667-787（normalize_repositories：JSON 数组 / 逗号分隔 / 模板解析思路——member_ids _parse_id_list 镜像）、:390-409（get_template_value 保留 list 类型）
    - 59-RESEARCH.md §3 D-4（节点设计 + config_schema + 缺参语义）/D-5（member_ids 三形态 + user_id_type 默认 open_id）/D-7（fail-soft 两类表）/D-8（输出形状）、§2 F-3（owner 为 bot 时响应缺 owner_id → 容错空串）、F-7（FeishuIMService.create 凭证解析）、§5.2/§5.3（节点集成测 + writeback 测断言点）、§6 P-2/P-3/P-6/P-7/P-11/P-12
    - server/services/feishu_im.py:922-985（FeishuIMService.create_chat 委托——节点调用契约：返回 dict 含 chat_id/name/owner_id）
    - server/delivery/services/work_item_service.py（Wave 1 新增 awriteback_feishu_chat_id 签名：feishu_project_key, work_item_type, work_item_id:int, chat_id → bool）
  </read_first>
  <behavior>
    - `CreateGroupChatNode(BaseNode)` `@register_node`：`node_type="create_group_chat"`、`display_name="创建群聊"`、`description="创建飞书群并拉入指定成员，输出 chat_id 供下游使用"`、`icon="users"`（或 message-circle-plus）、`category=NodeCategory.INTEGRATION`、`execution_mode="server_local"`。
    - `config_schema`（全中文 title/description，properties）：`name`(群名,string)、`description`(群描述,string,default "")、`owner_id`(群主 open_id,string,default "",描述「留空则 bot 为群主」)、`member_ids`(成员 ID,string,描述「逗号分隔 或 JSON 列表，支持 {{nodes.x.member_ids}} 模板；与 owner 用同一 user_id_type」)、`user_id_type`(string,enum [open_id,union_id,user_id],default open_id)、`project_key`(string,default "")、`work_item_id`(string,default "")、`work_item_type`(string,default story)——后三为可选 writeback 三件套，命名复用 FetchGroupChatNode。
    - `inputs=[NodePort("default","输入",OBJECT)]`；`outputs=[NodePort("default","成功",OBJECT), NodePort("error","失败",OBJECT)]`。
    - member_ids 解析 helper（节点内方法或模块级 `_parse_id_list(value, context) -> list[str]`，镜像 normalize_repositories）：模板 `{{...}}` → `context.get_template_value`（保留 list）；str → 优先 JSON `["a","b"]` 解析，否则逗号分隔；list → 逐项 str 化 strip 去空。
    - `execute(context)`：
      1. `name = context.render_template(config.get("name","")).strip()`；`member_ids = _parse_id_list(config.get("member_ids",""), context)`；
      2. **缺参**：`name` 空 **或** `member_ids` 空 → `NodeResult(status="failed", error="缺少群名或成员", next_handle="error")`（D-4）；
      3. 取 `project = context.workflow_execution.workflow.project`（若 workflow_execution 非空）→ `im_service = await FeishuIMService.create(project)`；
      4. `user_id_type = config.get("user_id_type","open_id")`；`owner_id = context.render_template(config.get("owner_id","")).strip()`；`description = context.render_template(config.get("description","")).strip()`；
      5. **建群**：`try: data = await im_service.create_chat(name, user_id_list=member_ids, owner_id=owner_id, description=description, user_id_type=user_id_type) except FeishuIMError as e: return NodeResult(status="failed", error=str(e), next_handle="error")`（D-7 建群失败走 error handle）；
      6. `chat_id = data.get("chat_id","")`；
      7. **可选 writeback**（D-7）：`project_key = render(config project_key).strip()`、`work_item_id_str = render(config work_item_id).strip()`；仅当二者均非空才执行——`int(work_item_id_str)` 转换失败 → `log.warning` 跳过（writeback attempted=False）；调 `WorkItemService().awriteback_feishu_chat_id(project_key, work_item_type, work_item_id, chat_id)` 用 `try/except Exception` 包裹（DB 异常 → `log.warning` + success=False，**绝不冒泡**）；
      8. 返回 `NodeResult(status="completed", output={"chat_id":chat_id, "chat_name":data.get("name",""), "owner_id":data.get("owner_id",""), "source":"create_group_chat", "writeback":{"attempted":bool, "success":bool}}, next_handle="default")`（D-8）。
    - import：在文件顶部加 `from services.feishu_im import FeishuIMError`（现仅 import FeishuIMService）；writeback 处 `from delivery.services.work_item_service import WorkItemService`（函数内 import 避免循环依赖，对齐既有风格）。
    - **绝不**改 `FetchGroupChatNode`/`JoinGroupChatNode` 任何行；节点内**禁**直接 `WorkItem.objects`/`wi.save()`（P-6）。
  </behavior>
  <action>
  1. `server/workflows/nodes/integrations/feishu_chat.py`：文件末尾新增 `CreateGroupChatNode`（依 behavior）+ member_ids 解析 helper（模块级 `_parse_id_list` 或节点私有方法）。顶部 import 增 `FeishuIMError`（`from services.feishu_im import FeishuIMService, FeishuIMError`）。建群 `try/except FeishuIMError`；writeback `try/except Exception` + `log.warning`。owner_id 容错：`data.get("owner_id","")`（P-3 owner 为 bot 时飞书不返回）。
  2. `server/tests/workflows/test_chat_nodes.py`：import 增 `CreateGroupChatNode` + `FeishuIMError`（from services.feishu_im）；扩展 `_mock_im_service`（或新建 helper）使其 `create_chat=AsyncMock(...)`；新增测试（复用既有 `_make_context` + `patch(...FeishuIMService.create)` + mock workflow_execution.workflow.project）：
     - happy：mock create_chat 返回 `{"chat_id":"oc_x","name":"需求群","owner_id":""}`，config `{"name":"需求群","member_ids":"ou_a, ou_b"}` → 断言 `status=="completed"`、`next_handle=="default"`、`output["chat_id"]=="oc_x"`、`output["source"]=="create_group_chat"`；断言 `create_chat` 被以 `user_id_list==["ou_a","ou_b"]` 调用。
     - member_ids 三形态：`"ou_a, ou_b"`（逗号）/ `'["ou_a","ou_b"]'`（JSON）/ 模板（mock context.get_template_value 返回 `["ou_a","ou_b"]`）→ 三者均断言传入 create_chat 的 `user_id_list==["ou_a","ou_b"]`。
     - 缺群名：config `{"name":"","member_ids":"ou_a"}` → `failed`+`next_handle=="error"`（不调 create_chat）。
     - 缺成员：config `{"name":"群","member_ids":""}` → `failed`+`error`（不调 create_chat）。
     - 建群失败：`create_chat=AsyncMock(side_effect=FeishuIMError("no permission", code=230002))` → `failed`+`error`（D-7）。
     - writeback happy：config 含 `project_key="P"/work_item_id="123"/work_item_type="story"` + `patch(...WorkItemService.awriteback_feishu_chat_id, AsyncMock(return_value=True))` → 断言被以 `("P","story",123,"oc_x")` 调用、节点 `completed`、`output["writeback"]["success"] is True`。
     - writeback fail-soft：`awriteback_feishu_chat_id=AsyncMock(side_effect=Exception("db down"))` → 断言节点**仍 completed**、`output["chat_id"]=="oc_x"`、异常**不冒泡**、`output["writeback"]["success"] is False`。
     - writeback 不存在：`awriteback_feishu_chat_id` 返回 False → `completed`、`output["writeback"]["success"] is False`。
     - 未配 writeback：config 无 project_key/work_item_id → 不调 awriteback（`assert_not_called`）、`completed`、`output["writeback"]["attempted"] is False`。
     - 自动注册：`from workflows.nodes.registry import NodeRegistry`（或既有 registry 取法）断言 `create_group_chat` 已注册且映射 `CreateGroupChatNode`（参考既有 registry 测试取法；或断言 `CreateGroupChatNode.node_type=="create_group_chat"` + 装饰器存在）。
  ruff 行宽 100，import I001，注释/docstring 中文。
  </action>
  <acceptance_criteria>
    - 源断言：`feishu_chat.py` 含 `class CreateGroupChatNode` + `node_type = "create_group_chat"` + `@register_node`；含 `from services.feishu_im import` 带 `FeishuIMError`；含 `awriteback_feishu_chat_id` 调用；**无** `WorkItem.objects` / `.save(` 直接写表（P-6）；既有 `class FetchGroupChatNode`/`class JoinGroupChatNode` 行逐字不变。
    - 行为断言：见 action 各测点（happy/三形态/缺参/建群失败/writeback happy/fail-soft/不存在/未配/自动注册）。
    - 测试命令：`cd server && uv run pytest tests/workflows/test_chat_nodes.py -q` 全绿（既有 Fetch/Join 节点测试 + 新增不回退）。
  </acceptance_criteria>
  <verify>
    <automated>cd server && uv run ruff check workflows/nodes/integrations/feishu_chat.py && uv run pytest tests/workflows/test_chat_nodes.py -q</automated>
  </verify>
  <done>CreateGroupChatNode（@register_node 自动注册 create_group_chat）落地：member_ids 三形态解析、缺参/建群失败 error handle、建群输出 chat_id 一等字段、可选 writeback fail-soft（仅调 WorkItemService 单一入口，异常不冒泡）；FetchGroupChatNode/JoinGroupChatNode 逐字不变；节点集成测全绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 工作流配置 → CreateGroupChatNode | name/member_ids/owner_id/work_item 标识来自用户配置或上游节点（模板）；member_ids 三形态解析、user_id_type 一致性由节点/用户保证 |
| 节点 → FeishuIMService / WorkItemService | 建群经 create_chat（失败 FeishuIMError → error handle）；writeback 经 awriteback_feishu_chat_id 单一入口（INV-6，节点禁直接写表） |
| 节点 output → 下游节点 | chat_id 一等字段供 JoinGroupChatNode/发卡节点消费；writeback 状态透出但失败不阻断 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-03 | 协议误用 / 拒绝服务 | 缺群名/成员或建群失败被静默成功 → 下游拿不到 chat_id 却以为成功 | mitigate | 缺参 + FeishuIMError → failed + next_handle="error"（D-4/D-7）；集成测覆盖缺群名/缺成员/建群失败 |
| T-59-04 | 可用性 / 容错 | writeback 失败（WorkItem 不存在 / DB 异常）掀翻整节点致建群主产物丢失 | mitigate | writeback try/except Exception + log.warning + success=False，节点仍 completed 返回 chat_id（D-7 fail-soft）；测试断言异常不冒泡 |
| T-59-04b | INV-6 旁路 | 节点内直接 WorkItem.objects/wi.save() 写 chat_id 绕过单一入口 | mitigate | 节点只调 WorkItemService.awriteback_feishu_chat_id；源断言无 WorkItem.objects/.save；Wave 1 INV-6 grep 守护兜底（P-6） |
| T-59-04c | Information Disclosure | owner 为 bot 时响应缺 owner_id 致 KeyError 暴露 | mitigate | output 用 data.get("owner_id","") 容错空串（P-3） |
</threat_model>

<verification>
- `cd server && uv run pytest tests/workflows/test_chat_nodes.py -q` 全绿。
- `cd server && uv run pytest tests/services/test_feishu_im.py tests/workflows/test_chat_nodes.py tests/delivery/test_work_item_writeback.py tests/delivery/test_inv6_guard.py -q` 全绿（Wave 1+2 full suite，零回归）。
- `cd server && uv run ruff check workflows/nodes/integrations/feishu_chat.py` 无 lint。
- grep `feishu_chat.py`：含 `class CreateGroupChatNode` + `node_type = "create_group_chat"`；含 `awriteback_feishu_chat_id`；**无** `WorkItem.objects`/`.save(`；既有 `class FetchGroupChatNode`/`class JoinGroupChatNode` 主体未改。
</verification>

<success_criteria>
- SC-1（GROUP-01）：CreateGroupChatNode 可建飞书群并拉入指定成员（member_ids 三形态 → user_id_list），建群即拉人单步经 create_chat（D-1）。
- SC-2：chat_id 作 output 一等字段（D-8）供下游 JoinGroupChatNode/发卡节点引用。
- SC-3：可选 writeback 经 WorkItemService.awriteback_feishu_chat_id（仅配 work_item 标识时），失败 fail-soft 节点仍 completed 返回 chat_id（D-7，warning 不冒泡）。
- 缺参/建群失败 → failed + error handle；writeback 失败 ≠ 建群失败（两类边界 D-7/P-7）。
- i18n：display_name/config title 全中文（STATE）。
- 零回归：FetchGroupChatNode/JoinGroupChatNode 符号逐字不变；既有 test_chat_nodes 全绿；节点经 @register_node 自动注册 create_group_chat（唯一 node_type，P-12）。
</success_criteria>

<artifacts_this_phase_produces>
本 Plan（Wave 2）创建的符号：
- 修改 `server/workflows/nodes/integrations/feishu_chat.py`：
  - `class CreateGroupChatNode(BaseNode)`（`@register_node`，`node_type="create_group_chat"`，含 `config_schema`/`inputs`/`outputs`/`async def execute`）
  - `_parse_id_list(value, context) -> list[str]`（member_ids 三形态解析 helper，模块级或节点私有，命名 plan 自决）
  - 顶部 import 增 `FeishuIMError`（`from services.feishu_im import FeishuIMService, FeishuIMError`）
- 扩展 `server/tests/workflows/test_chat_nodes.py`（新增 CreateGroupChatNode happy/三形态/缺参/建群失败/writeback happy/fail-soft/不存在/未配/自动注册测试）
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/59-workflow-create-group-node/59-02-SUMMARY.md` when done
</output>
