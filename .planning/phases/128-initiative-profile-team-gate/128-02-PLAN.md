---
phase: 128-initiative-profile-team-gate
# 团队门禁模块（TEAM）
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - server/services/process_runtime/team_gate.py
  - server/tests/services/process_runtime/test_team_gate.py
autonomous: true
requirements:
  - TEAM-01
  - TEAM-02
  - TEAM-03
user_setup: []

must_haves:
  truths:
    - "能从 Project 挂载 Space（优先）或显式 space_id/team_id/上下文 Space 解析出 team_core 仓库 id 集（TEAM-01）"
    - "候选标注 team_core | team_adjacent | out_of_team；primary 仅允许 team_core；adjacent 例外只留接口不校验证据（TEAM-02，D1）"
    - "team_core 空或期望团队缺失时返回 clarify（missing_team|empty_team_core），candidates 不得为全库 top-k 主结果（TEAM-03，D3）"
  artifacts:
    - path: server/services/process_runtime/team_gate.py
      provides: "resolve_team_core + annotate + apply_hard_gate"
      exports: ["resolve_team_core", "annotate_team_membership", "apply_team_gate", "TeamMembership"]
    - path: server/tests/services/process_runtime/test_team_gate.py
      provides: "成功门禁 / 空团队 clarify / out_of_team 非 primary"
  key_links:
    - from: apply_team_gate
      to: Space.repositories
      via: "resolve_team_core 读挂载仓；空集 → clarify 短路"
      pattern: "team_core|empty_team_core|missing_team|out_of_team"
---

<objective>
交付可单测的团队硬门禁：解析 `team_core`、标注三类隶属、空团队 clarify、primary 禁止 out_of_team（TEAM-01/02/03；锁定 D1/D3）。

Purpose: 与画像模块并行，供 Wave 2 三入口接线；本 plan **不**改 Blueprint/MCP/RepoAssociation。
Output: `team_gate.py` + 单元测试。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/128-initiative-profile-team-gate/128-CONTEXT.md
@.planning/phases/128-initiative-profile-team-gate/128-RESEARCH.md
@.planning/milestones/v0.23.0-DECISIONS.md
@server/projects/models.py
@server/initiatives/services/repo_association_service.py
@server/codegraph/services/repo_router_ranking.py
@server/services/process_runtime/blueprint_route.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: resolve_team_core 解析链</name>
  <files>server/services/process_runtime/team_gate.py, server/tests/services/process_runtime/test_team_gate.py</files>
  <behavior>
    - Project 绑定 Space 有挂载仓 → team_core = 该 Space.repositories id 集（TEAM-01）
    - 无 Project 但传入 space_id → 同口径解析
    - 无任何团队上下文 → resolve 结果标记 missing_team（供 clarify）
    - Space 存在但 repositories 空 → empty_team_core
  </behavior>
  <action>
    新建 `team_gate.py`（per CONTEXT）。实现 `resolve_team_core(...)` 优先级（CONTEXT 团队门禁模型）：① Project 挂载 Space 仓库集；② 调用方显式 `space_id`/`team_id`；③ 上下文绑定 Space（若有）。ORM 经 `sync_to_async`。返回结构含 `team_core: list[str]`、`space_id`、`resolution`、以及是否应 clarify 的 reason。**不要**调用 RepoRouterV2。索引可用性：若调用方可注入「已索引 repo 子集」过滤器，team_core 全无索引时等同 empty（TEAM-03）；无注入时默认以挂载 id 为准并在文档/注释标明接线层可补索引过滤。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/process_runtime/test_team_gate.py -q --tb=short -k "resolve or empty or missing"</automated>
  </verify>
  <done>三级解析可测；缺失/空 core 有明确 reason，不静默当全库</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 隶属标注 + hard gate（D1/D3）</name>
  <files>server/services/process_runtime/team_gate.py, server/tests/services/process_runtime/test_team_gate.py</files>
  <behavior>
    - 候选 repo 在 team_core 内 → membership=team_core，可作 primary
    - 候选不在 team_core → out_of_team，apply_team_gate 后不得出现在 primary 位（TEAM-02，D1）
    - team_adjacent 枚举/字段预留；无证据时不得自动升 primary（证据校验留给 129）
    - missing_team / empty_team_core → status=clarify，candidates=[] 或非主结果列表，clarify_reason 对应（TEAM-03，D3）
  </behavior>
  <action>
    实现 `annotate_team_membership(candidates, team_core)` 与 `apply_team_gate(...)`：统一输出与 RESEARCH 中 clarify 载荷兼容的 dict（`status`、`clarify_reason`、`team_core`、`candidates` 带 `team_membership`、`offer` 可选）。Hard gate：当期望团队存在时，过滤/降级使 **primary 仅 team_core**；`out_of_team` 默认可保留在旁路列表或剔除出主候选——产品要求是「不可作 primary」。空/缺团队走 clarify，**禁止**返回全库排序第一当主结果。观测：`team_gate_completed` kv 含 `team_core_count`、`gate_outcome`、`clarify_reason`；`category=sampling`，`component=process_runtime`；不写密钥与需求原文。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/process_runtime/test_team_gate.py -q --tb=short</automated>
  </verify>
  <done>三类标注 + hard primary 规则 + 空团队 clarify 测试全绿；引用 D1/D3</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 调用方 space_id/team_id → team_core | 未认证调用方可能尝试扩大/缩小范围（接线层仍走既有 auth） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-128-03 | Information Disclosure | empty-team fallback | mitigate | D1/D3：禁止静默全库 primary，避免跨团队仓/能力树元数据泄漏为「主结果」 |
| T-128-04 | Elevation of Privilege | fake space_id | accept | 入口层既有 IsAuthenticated；本模块不做 ACL，由 MCP/Blueprint 既有权限边界负责 |
| T-128-SC | Tampering | pip installs | accept | 无新包 |
</threat_model>

<verification>
- `test_team_gate.py` 全绿
- 模块内无 import 修改 RepoRouterV2 行为
</verification>

<success_criteria>
独立调用 team_gate 可解析 team_core、拒绝 out_of_team primary、空团队 clarify（D1/D3）。
</success_criteria>

<output>
Create `.planning/phases/128-initiative-profile-team-gate/128-02-SUMMARY.md` when done
</output>
