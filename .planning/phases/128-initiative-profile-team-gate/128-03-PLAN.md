---
phase: 128-initiative-profile-team-gate
# 漏斗三入口接线 + MCP 门禁
plan: 03
type: execute
wave: 2
depends_on:
  - "01"
  - "02"
files_modified:
  - server/services/process_runtime/blueprint_route.py
  - server/initiatives/services/repo_association_service.py
  - server/services/process_runtime/stage_sandbox.py
  - server/mcp_tools/views.py
  - server/tests/services/process_runtime/test_funnel_team_gate.py
  - server/tests/services/process_runtime/test_stage_sandbox.py
  - server/tests/initiatives/test_repo_association_service.py
autonomous: true
requirements:
  - PROF-01
  - PROF-02
  - PROF-03
  - TEAM-01
  - TEAM-02
  - TEAM-03
user_setup: []

must_haves:
  truths:
    - "BlueprintRoute / RepoAssociation / MCP arun_route_stage 在漏斗路径调用画像+团队门禁；out_of_team 不作 primary（D1）"
    - "无团队或 empty team_core 时三入口返回 clarify，不静默全库 RepoRouterV2.route 作 primary（D3）"
    - "MCP 无团队上下文时 clarify（可带候选 Spaces），不返回全库 top-k 当主结果（D1）"
    - "裸 RepoRouterV2.route（无漏斗包装）行为保持 annotate-only 兼容，测试注明"
  artifacts:
    - path: server/services/process_runtime/blueprint_route.py
      provides: "route() 前画像+gate；clarify 短路"
    - path: server/initiatives/services/repo_association_service.py
      provides: "propose/refine 走画像 corpus + gate clarify"
    - path: server/services/process_runtime/stage_sandbox.py
      provides: "arun_route_stage 团队门禁 + clarify 载荷"
    - path: server/tests/services/process_runtime/test_funnel_team_gate.py
      provides: "三入口 + MCP 无静默全库 primary 集成测"
  key_links:
    - from: BlueprintRouteAdapter.route
      to: build_profile + apply_team_gate
      via: "gate fail → return clarify summary；ok → repository_ids=team_core 再调 V2"
      pattern: "apply_team_gate|build_profile|clarify_reason"
    - from: arun_route_stage
      to: team_gate
      via: "MCP dry-run 同源门禁"
      pattern: "missing_team|empty_team_core|status.: .clarify"
---

<objective>
把画像与团队门禁接到 BlueprintRouteAdapter、RepoAssociationService、MCP `arun_route_stage`（及必要时 `RouteBlueprintReposView`），实现漏斗 hard gate；裸 V2 直调保持兼容（D1/D3；不重写 RepoRouterV2）。

Purpose: 完成 Phase 128 可观测端到端行为与验收成功标准 1–5。
Output: 三入口接线 + 集成/回归测试。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/128-initiative-profile-team-gate/128-CONTEXT.md
@.planning/phases/128-initiative-profile-team-gate/128-RESEARCH.md
@.planning/phases/128-initiative-profile-team-gate/128-01-PLAN.md
@.planning/phases/128-initiative-profile-team-gate/128-02-PLAN.md
@.planning/milestones/v0.23.0-DECISIONS.md
@server/services/process_runtime/blueprint_route.py
@server/initiatives/services/repo_association_service.py
@server/services/process_runtime/stage_sandbox.py
@server/mcp_tools/views.py
@server/services/process_runtime/initiative_profile.py
@server/services/process_runtime/team_gate.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: BlueprintRoute + RepoAssociation 漏斗接线</name>
  <files>server/services/process_runtime/blueprint_route.py, server/initiatives/services/repo_association_service.py, server/tests/services/process_runtime/test_funnel_team_gate.py, server/tests/initiatives/test_repo_association_service.py</files>
  <behavior>
    - Blueprint：有 Space/team_core 时 route 仅在 team_core 内召回；mock 出 out_of_team 高分仓不得排 primary（D1）
    - Blueprint：无团队上下文 → 返回 status=clarify（missing_team），不调用全库 V2 作 primary（D3）
    - RepoAssociation：空 Space 仓库 → clarify，而非仅空 candidates+skipped 静默成功态（D3）
    - RepoAssociation propose query 经画像 corpus 路径，不把 acceptance 当主语料（PROF-02）
    - 画像 degrade 时仍可走门禁；门禁 clarify 优先于噪声路由
  </behavior>
  <action>
    **禁止重写 RepoRouterV2**（锁定决策）。在 `BlueprintRouteAdapter.route` 中于 pin 短路之后、调用 `router.route` 之前： assembles feature/requirement 输入 → `build_profile` → `resolve_team_core`/`apply_team_gate`。gate=clarify 则返回与 stage routing 兼容的摘要（additive 键：`status`、`clarify_reason`、`profile`、`team_core`、`team_core_count`），**不得**再以 `repository_ids=None` 全库检索作为 primary。gate=ok 时将 `repository_ids` 收窄为 `team_core` 再调既有 V2+融合。pin 路径：人工绑定仍可短路，但须标注 team 隶属（若可解析）。`RepoAssociationService._route_and_persist` / `propose`：用画像 corpus 构建检索 query（替换或包装 `_build_query` 对 acceptance 的默认拼入）；空/缺 team_core → clarify 载荷（保留旧键 additive）。观测：入口级 `category=caller`（repo_association 已有）/ blueprint `sampling` 或 caller 按既有惯例；字段含 gate outcome、profile degrade；fail-soft。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/process_runtime/test_funnel_team_gate.py tests/initiatives/test_repo_association_service.py -q --tb=short</automated>
  </verify>
  <done>Blueprint + RepoAssociation 漏斗 hard gate 与 clarify 可测；V2 文件无行为性重写</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MCP arun_route_stage 门禁 + 无静默全库 primary</name>
  <files>server/services/process_runtime/stage_sandbox.py, server/mcp_tools/views.py, server/tests/services/process_runtime/test_stage_sandbox.py, server/tests/services/process_runtime/test_funnel_team_gate.py</files>
  <behavior>
    - arun_route_stage 无 project_id/space_id/team 且无法自动识别 → status=clarify，candidates 不是全库 top-k 主结果（D1/D3）
    - 提供 project_id 且 space 有仓 → 正常路由且 primary ∈ team_core
    - MCP 响应可含 offer.spaces（可枚举时）；不可枚举则仍 clarify 要求选择
    - 文档/测试注释：裸 RepoRouterV2.route 仍允许无 grouping 的全局路径（兼容），漏斗 MCP 工具必须门禁
  </behavior>
  <action>
    演进 `arun_route_stage`（及 `RouteBlueprintReposView` 入参若需 `space_id`/`team_id`）：自动识别优先（project_id → space 仓；显式 space_id/team_id；会话上下文若 sandbox 可取）。无法识别 → clarify，**删除/阻断**「scope 空则全库」作为 primary 的路径（当前 stage_sandbox 注释写明三级含全库——漏斗 MCP 必须改掉该 primary 语义，per D1）。可枚举 Spaces 时填 `offer.spaces`（id+name，无密钥）。输出仍尽量与 routing 摘要同形并 additive。更新/扩展 `test_stage_sandbox.py` 与 `test_funnel_team_gate.py`：断言 MCP 路径「无静默全局 primary」。不要修改 `RepoRouterV2.grouping_repository_ids` annotate-only 语义。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/process_runtime/test_funnel_team_gate.py tests/services/process_runtime/test_stage_sandbox.py -q --tb=short</automated>
  </verify>
  <done>MCP/sandbox 门禁符合 D1/D3；测试证明无团队时非全库 primary</done>
</task>

<task type="auto">
  <name>Task 3: 回归守卫与观测自检</name>
  <files>server/tests/services/process_runtime/test_funnel_team_gate.py, server/tests/codegraph/test_repo_router_v2_degraded.py</files>
  <action>
    补充/标注测试：① 漏斗路径 hard gate；② 裸 `RepoRouterV2.route` 无 grouping 时仍为全局 annotate 兼容（选跑既有用例或加一行注释型断言说明本相位不改变 V2）。快速自检可观测：新事件含 category/component/duration_ms；无凭证字段。若 CallSource 在 01 已加，本 task 确认接线层 `use_call_source` 覆盖画像 LLM。不扩大到 shortlist/角色图（129 deferred）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/process_runtime/test_funnel_team_gate.py tests/services/process_runtime/test_initiative_profile.py tests/services/process_runtime/test_team_gate.py -q --tb=short</automated>
  </verify>
  <done>Phase 128 核心 pytest 集全绿；V2 兼容说明在测或注释中可见</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client → arun_route_stage | 工具调用方可能省略 team；必须 fail 到 clarify 而非扩大可见仓集 |
| Blueprint session → router | stage_state 可被上游写入；gate 以解析到的 Space 为准 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-128-05 | Information Disclosure | MCP full-library primary | mitigate | D1：无团队 clarify；禁止全库 top-k 作主结果 |
| T-128-06 | Denial of Service | profile+gate on every route | accept | 单次路由成本；fail-soft；无重试风暴 |
| T-128-01 | Information Disclosure | stage logs/traces | mitigate | RetrievalTrace/日志不写需求全文；redact |
| T-128-SC | Tampering | pip installs | accept | 无新包 |
</threat_model>

<verification>
- funnel + profile + team_gate + stage_sandbox 相关 pytest 通过
- `git diff` 确认无对 RepoRouterV2 核心算法的重写（至多类型/注释/无关）；grouping 仍为 annotate-only
- ROADMAP 成功标准 1–5 可被测试映射覆盖
</verification>

<success_criteria>
三入口漏斗具备画像+团队硬门禁；MCP 无静默全库 primary；空团队 clarify（D1/D3）；裸 V2 兼容保留。
</success_criteria>

<output>
Create `.planning/phases/128-initiative-profile-team-gate/128-03-SUMMARY.md` when done
</output>
