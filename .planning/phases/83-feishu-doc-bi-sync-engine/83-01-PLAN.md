---
phase: 83
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/initiatives/models/project_doc.py
  - server/initiatives/models/__init__.py
  - server/initiatives/migrations/0007_doc_sync_engine.py
  - server/initiatives/services/doc_sync_diff.py
  - server/tests/initiatives/test_doc_sync_diff.py
  - server/tests/initiatives/conftest.py
autonomous: true
requirements: [SYNC-03, SYNC-04]
must_haves:
  truths:
    - "block_id 结构化 diff 能区分用户在飞书的新增 / 编辑 / 删除三类变更"
    - "同块两侧都改时三方合并产出 merged 或冲突判定（capture 决策），绝不静默丢用户内容"
    - "STATE/MILESTONES/RESEARCH/PREFLIGHT 的 capture-never-clobber 有持久落点（ProjectDocBlockRevision）"
    - "ProjectDoc 能记录飞书订阅态（subscribed）与最近飞书编辑时间（last_feishu_edit_at）"
  artifacts:
    - path: "server/initiatives/services/doc_sync_diff.py"
      provides: "纯函数 block_id 结构化 diff + 三方合并 + content_hash（无 IO/ORM）"
      exports: ["diff_blocks", "three_way_merge", "block_content_hash", "BlockDiff", "MergeResult"]
    - path: "server/initiatives/migrations/0007_doc_sync_engine.py"
      provides: "ProjectDoc 新字段 + ProjectDocBlockRevision 表"
      contains: "ProjectDocBlockRevision"
    - path: "server/tests/initiatives/test_doc_sync_diff.py"
      provides: "纯函数 diff/三方合并单测（无 IO）"
      contains: "three_way_merge"
  key_links:
    - from: "server/initiatives/services/doc_sync_diff.py"
      to: "ProjectDocBlockMap.content_hash"
      via: "block_content_hash 产出落 content_hash 字段（max_length=128）"
      pattern: "hashlib\\.sha256"
---

<objective>
为飞书↔Friday 同步引擎打地基：一次性把"纯算法 + 持久落点"备齐，让 Wave 2+ 的 pull/push/冲突处理无需再碰 schema、可直接 import 纯函数。

本计划交付两件无 IO 的硬骨头与一处 schema 扩展：
1. `doc_sync_diff.py` —— block_id 结构化逐块匹配（新增/编辑/删除）+ 三方合并（base/theirs/ours）+ capture 决策，全部纯函数、零 ORM/httpx 依赖（SYNC-03/04 核心算法）。
2. migration 0007 —— `ProjectDoc` 扩 `subscribed`(bool) + `last_feishu_edit_at`(datetime, null)（OQ-4，承载订阅态与编辑感知探测）；新增 `ProjectDocBlockRevision`（doc + block_id + content + source + captured_at），作为 STATE/MILESTONES/RESEARCH/PREFLIGHT 的 capture-never-clobber 落败方落点（OQ-2 裁决：选 net-new 轻量 revision 表）。
3. Wave 0 测试脚手架（`test_doc_sync_diff.py` + `conftest.py` fixtures），供后续 wave 复用。

Purpose: 把"难在算法"的部分隔离成可在无 DB/无网络下快速验证的纯函数，并把所有 capture 落点的 schema 一次到位（schema-first，避免下游 wave 反复加迁移）。
Output: doc_sync_diff 模块 + 1 个 migration + 2 个模型字段 + 1 张新表 + 纯函数测试套 + conftest fixtures。
</objective>

<execution_context>
@.planning/phases/83-feishu-doc-bi-sync-engine/83-PATTERNS.md
@.planning/phases/83-feishu-doc-bi-sync-engine/83-RESEARCH.md
@.planning/phases/83-feishu-doc-bi-sync-engine/83-CONTEXT.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@server/initiatives/models/project_doc.py
@server/initiatives/migrations/0006_project_workspace_entities.py
@.cursor/rules/observability-logging.mdc
</context>

<artifacts_produced>
- NEW 纯函数模块 `server/initiatives/services/doc_sync_diff.py`：`block_content_hash` / `diff_blocks` / `three_way_merge` + dataclass `BlockDiff` / `MergeResult`
- NEW 模型 `ProjectDocBlockRevision`（capture-never-clobber 落点，写入仍经 ProjectDocService — INV-6）
- ProjectDoc 新字段：`subscribed`(BooleanField default=False)、`last_feishu_edit_at`(DateTimeField null=True)
- NEW migration `0007_doc_sync_engine`
- NEW 测试 `server/tests/initiatives/test_doc_sync_diff.py`、NEW/扩展 `server/tests/initiatives/conftest.py`
- 无新增 structlog 事件（纯函数模块不打日志，IO 收口在下游 service）
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: migration 0007 + 模型扩展（ProjectDoc 字段 + ProjectDocBlockRevision）</name>
  <read_first>
    - server/initiatives/models/project_doc.py（待修改文件；ProjectDocBlockMap 形态模板，行 98-133）
    - server/initiatives/models/__init__.py（curated re-export，须加 ProjectDocBlockRevision）
    - server/initiatives/migrations/0006_project_workspace_entities.py（AddField + CreateModel + AddConstraint/AddIndex 范式）
    - server/tests/initiatives/test_project_doc_inv6_guard.py（_MODELS 需扩 ProjectDocBlockRevision）
  </read_first>
  <action>
    `ProjectDoc` 增两字段（OQ-4）：`subscribed`(BooleanField default=False)、`last_feishu_edit_at`(DateTimeField null=True, blank=True)。
    新增 `ProjectDocBlockRevision`（OQ-2）：`id`(UUID PK)、`doc`(FK CASCADE related_name="block_revisions")、`feishu_block_id`(CharField 200)、`db_ref`(CharField 200 blank default="")、`content`(TextField)、`source`(CharField 20 default="system")、`reason`(CharField 40 blank default="")、`captured_at`(DateTimeField auto_now_add)。db_table="initiative_project_doc_block_revisions" + Index(["doc","feishu_block_id"])。镜像 ProjectDocBlockMap，不加 create/save 业务方法（INV-6，本计划只建表）。
    `models/__init__.py` re-export 加 `ProjectDocBlockRevision`。
    `makemigrations initiatives` 生成 `0007_doc_sync_engine.py`（dependencies → 0006），纯 AddField + CreateModel 无回填。
    扩 `test_project_doc_inv6_guard.py` 的 `_MODELS` 加 `"ProjectDocBlockRevision"`。
  </action>
  <verify>
    <automated>cd server && uv run python manage.py makemigrations --check --dry-run initiatives && uv run pytest tests/initiatives/test_project_doc_inv6_guard.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `makemigrations --check` 干净；`from initiatives.models import ProjectDocBlockRevision` 可导入。
    - INV-6 guard 绿且 `_MODELS` 含 ProjectDocBlockRevision；若 writer 有效性断言报红，则在 ProjectDocService 加最小占位 `capture_block_revision`（供 83-04 填充）使 `ProjectDocBlockRevision.objects` 现于 writer。
    - `ProjectDoc.subscribed` / `last_feishu_edit_at` 字段存在。
  </acceptance_criteria>
  <done>migration 0007 + ProjectDocBlockRevision + ProjectDoc 两新字段就位；INV-6 guard 覆盖新模型且全绿。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: doc_sync_diff.py block_id 结构化 diff + content_hash（纯函数）</name>
  <read_first>
    - server/services/feishu_doc.py（blocks_to_markdown 纯转换 + block 结构）
    - server/tasks/index_trigger_tasks.py（parse_push_event 纯解析 + 防御性 .get，行 88-111；import hashlib）
    - server/initiatives/models/project_doc.py（content_hash max_length=128、section 取值）
    - .planning/phases/83-feishu-doc-bi-sync-engine/83-RESEARCH.md（Pattern 3）
  </read_first>
  <behavior>
    - block_content_hash 归一化（strip+折叠空白）后 sha256 hexdigest，幂等。
    - diff_blocks：不在 block_map→op="added"；在 map 且 hash 变→"edited"；在 map 但飞书已无→"deleted"；hash 未变→no-op。
    - 纯函数无副作用；缺 block_id 跳过不抛；block_map 空→全 added。
  </behavior>
  <action>
    新建 `server/initiatives/services/doc_sync_diff.py`（`from __future__ import annotations`、`import hashlib`、`from dataclasses import dataclass`，不 import structlog/ORM/httpx）。导出 `block_content_hash(text)`、`@dataclass(frozen=True) BlockDiff(op, feishu_block_id, db_ref, section, content, content_hash)`、`diff_blocks(*, base_snapshot, theirs_blocks, block_map)`。防御性 `.get`、缺 block_id 跳过、绝不抛。zh-CN docstring 说明"纯算法、IO 收口在 DocSyncService"。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/initiatives/test_doc_sync_diff.py -k diff -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest ... -k diff -x` 全绿；grep doc_sync_diff.py 无 httpx/.objects./structlog。
    - added/edited/deleted/no-op 四类有断言；缺 block_id 不抛。
  </acceptance_criteria>
  <done>diff_blocks + block_content_hash 三分支正确、纯函数无 IO、单测覆盖四类。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: three_way_merge + capture 决策 + conftest 脚手架</name>
  <read_first>
    - server/initiatives/services/doc_sync_diff.py（Task 2 产出，续写）
    - server/tests/initiatives/test_project_doc_service.py（pytestmark django_db、sync_to_async 工厂、respx，行 14-56）
    - .planning/phases/83-feishu-doc-bi-sync-engine/83-RESEARCH.md（Pattern 4）
  </read_first>
  <behavior>
    - (base=X,theirs=X,ours=Y)→merged=Y,no conflict；(X,Z,X)→merged=Z,no conflict；(X,Z,Y) Z≠Y→has_conflict=True,merged=Z（飞书优先）,loser=Y；(X,X,X)→merged=X。
  </behavior>
  <action>
    续写 `doc_sync_diff.py`：`@dataclass(frozen=True) MergeResult(merged, has_conflict, loser="")`；`three_way_merge(*, base, theirs, ours)` 实现真值表，飞书侧（theirs）优先保留为 merged，落败 ours 放 `loser`（绝不静默丢，SYNC-04 + Pitfall 5）。纯函数按块整体比对。
    新建/扩 `server/tests/initiatives/conftest.py`：`project_doc_factory`/`block_map_factory`/`project_memory_factory`（sync_to_async 工厂）+ `respx_feishu` fixture，供 Wave 2+ 复用。
    `test_doc_sync_diff.py` 补 merge 真值表四用例。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/initiatives/test_doc_sync_diff.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/initiatives/test_doc_sync_diff.py -x` 全绿（diff + merge）。
    - 相交冲突断言 has_conflict=True 且 loser 非空。
    - conftest fixtures 可被 tests/initiatives 其他模块 import。
  </acceptance_criteria>
  <done>three_way_merge 真值表正确、飞书优先 + 落败进 loser；conftest 就位。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 飞书回拉正文 → 纯函数 diff 入参 | 半可信外部内容进入算法层（本层不打日志/不入库，IO 下游收口） |
| 算法层 → DB（capture 落点） | 本计划只建表；写入由下游经 ProjectDocService（INV-6） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83-01-CLOBBER | Tampering | three_way_merge | mitigate | 相交冲突 has_conflict=True 且 loser 非空，飞书侧优先保留；落败方交 service capture，纯函数绝不丢弃（SYNC-04） |
| T-83-01-INJ | Tampering | diff_blocks 半可信输入 | mitigate | 缺 block_id 跳过、防御性 `.get`、绝不抛；不 eval/不拼接执行 |
| T-83-01-BYPASS | Elevation | ProjectDocBlockRevision 写入 | mitigate | 新模型纳入 test_doc_sync_inv6_guard 的 _MODELS，落库只经 ProjectDocService |
</threat_model>

<verification>
- `cd server && uv run pytest tests/initiatives/test_doc_sync_diff.py tests/initiatives/test_project_doc_inv6_guard.py -x` 全绿。
- `makemigrations --check --dry-run` 干净；grep doc_sync_diff.py 无 httpx/ORM/structlog。
</verification>

<success_criteria>
- block_id 结构化 diff 三分支 + 三方合并真值表纯函数实现并测试通过。
- migration 0007 + ProjectDocBlockRevision + ProjectDoc 两新字段就位、--check 干净。
- capture 有持久落点且纳入 INV-6 守护；conftest 脚手架就位。
</success_criteria>

<output>
完成后创建 `.planning/phases/83-feishu-doc-bi-sync-engine/83-01-SUMMARY.md`
</output>
