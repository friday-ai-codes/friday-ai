---
phase: 32-one-click-ingest
plan: 03
type: execute
wave: 3
depends_on: [32-02]
files_modified:
  - web/src/api/ingest.ts
  - web/src/api/index.ts
  - web/src/locales/zh-CN.json
  - web/src/components/knowledge/IngestPanel.vue
  - web/src/pages/knowledge/ingest.vue
  - web/src/components/layout/AppSidebar.vue
  - web/src/components/knowledge/__tests__/IngestPanel.spec.ts
autonomous: true
requirements: [ING-01]
must_haves:
  truths:
    - "/knowledge/ingest 可访问，侧边栏含「一键摄取」入口"
    - "表单两个 URL（看板/MR）+「开始摄取」CTA；空/非 http(s) 内联校验不发请求"
    - "合法提交调 ingestApi.dispatch → run_id → 2s 轮询 getRun（completed/failed 停轮）"
    - "结果区固定渲染三步（工作项/文档/MR diff），按 ok/failed/skipped/pending 语义色 + identifier/link/error"
    - "completed 含非 ok → 部分成功提示；三步全 ok → 完成可检索提示；getRun 失败显示错误行不清空"
  artifacts:
    - path: "web/src/api/ingest.ts"
      provides: "ingestApi.dispatch / getRun + 类型（对齐后端字段）"
      contains: "export const ingestApi"
    - path: "web/src/components/knowledge/IngestPanel.vue"
      provides: "表单 + 派发→轮询 + 三步结果渲染"
      contains: "IngestPanel"
    - path: "web/src/pages/knowledge/ingest.vue"
      provides: "/knowledge/ingest 薄壳页"
      contains: "IngestPanel"
  key_links:
    - from: "web/src/components/knowledge/IngestPanel.vue"
      to: "web/src/api/ingest.ts"
      via: "ingestApi.dispatch + useQuery(getRun) refetchInterval"
      pattern: "ingestApi\\.(dispatch|getRun)"
    - from: "web/src/api/index.ts"
      to: "web/src/api/ingest.ts"
      via: "barrel export"
      pattern: "ingest"
    - from: "web/src/components/layout/AppSidebar.vue"
      to: "/knowledge/ingest"
      via: "mainNavItems 追加导航项"
      pattern: "/knowledge/ingest"
---

<objective>
实装最小一键摄取前端面板（per 32-UI-SPEC）：`/knowledge/ingest` 页 + `IngestPanel` 组件——输入看板 URL + MR URL，提交触发后台编排（`POST /delivery/ingest/`），经 TanStack Query 轮询 run 状态（`GET /delivery/ingest/{run_id}/`），固定渲染三步（工作项 / PRD·技术方案文档 / MR diff）的 ok/failed/skipped/pending 结果（语义色 + identifier + 「查看」外链 + error）。沿用既有「派发→轮询」范式（参照 `ReconcilePanel`），i18n 默认中文，守护测试以真实 `zh-CN.json` 断言关键文案。

Purpose: ING-01 的用户可见闭环——一处输入两个链接即可把需求/文档/diff 一次性入库可检索。
Output: `api/ingest.ts` + `IngestPanel.vue` + `ingest.vue` 页 + 侧边栏入口 + i18n + vitest 守护测试。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/32-one-click-ingest/32-UI-SPEC.md
@.planning/phases/32-one-click-ingest/32-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ingest API client + barrel 导出 + i18n zh-CN ingest 命名空间</name>
  <files>web/src/api/ingest.ts, web/src/api/index.ts, web/src/locales/zh-CN.json</files>
  <read_first>
    - web/src/api/reconcile.ts（`get`/`post` from './client' + 接口/方法对象风格 —— 严格沿用）
    - web/src/api/index.ts（barrel `export * from './xxx'` 追加风格）
    - 32-UI-SPEC.md「API Contract」节（`StepStatus`/`RunStatus`/`IngestStep`/`IngestRun`/`IngestDispatch` 类型 + `dispatch(boardUrl, mrUrl)` POST `/delivery/ingest/`、`getRun(runId)` GET `/delivery/ingest/{run_id}/`）
    - 32-UI-SPEC.md「i18n Keys」节（`ingest` 命名空间完整 key 树 —— 逐字落 zh-CN.json）
    - web/src/locales/zh-CN.json（顶层命名空间结构 + 既有 `reconcile`/`knowledge` 命名空间作风格基准；JSON 严格无尾逗号）
  </read_first>
  <action>
    新建 `web/src/api/ingest.ts`：从 `./client` import `get, post`，按 UI-SPEC「API Contract」定义并导出 `StepStatus`/`RunStatus`/`IngestStep`/`IngestRun`/`IngestDispatch` 类型与 `ingestApi`（`dispatch(boardUrl, mrUrl)` → `post<IngestDispatch>('/delivery/ingest/', { board_url, mr_url })`；`getRun(runId)` → `get<IngestRun>(\`/delivery/ingest/${runId}/\`)`）。字段名对齐 32-02 后端序列化器（`run_id`/`dispatched`/`status`/`steps.{work_item,document,mr_diff}`/`started_at`/`completed_at`）。
    在 `web/src/api/index.ts` 追加 `export * from './ingest'`（按现有字母序就近插入）。
    在 `web/src/locales/zh-CN.json` 顶层新增 `ingest` 命名空间，逐字落 UI-SPEC「i18n Keys」全部 key（title/subtitle/form.*/empty.*/dispatch.*/run.*/steps.*/status.*）。保持 JSON 合法（无尾逗号、缩进与既有一致）。
  </action>
  <verify>
    <automated>cd web && pnpm vue-tsc --noEmit -p tsconfig.json 2>&1 | head -40 && node -e "JSON.parse(require('fs').readFileSync('src/locales/zh-CN.json','utf8'));console.log('zh-CN.json valid')"</automated>
  </verify>
  <acceptance_criteria>
    - `web/src/api/ingest.ts` 导出 `ingestApi.dispatch` / `ingestApi.getRun` 与五个类型，端点路径与 UI-SPEC 一致。
    - `web/src/api/index.ts` 含 `export * from './ingest'`。
    - `zh-CN.json` 顶层 `ingest` 命名空间存在且含 UI-SPEC 列出的全部 key；`node -e JSON.parse(...)` 通过。
    - `pnpm vue-tsc --noEmit` 不因本文件新增类型报错。
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: IngestPanel 组件 + ingest 页薄壳 + 侧边栏入口</name>
  <files>web/src/components/knowledge/IngestPanel.vue, web/src/pages/knowledge/ingest.vue, web/src/components/layout/AppSidebar.vue</files>
  <read_first>
    - web/src/components/repository/ReconcilePanel.vue（`.card` 结构 + header 图标+标题 + `p-5 space-y-5` + mutation 派发 + `useQuery` 条件 `refetchInterval` 轮询 + useToast/useErrorHandler —— 主参照范式）
    - web/src/pages/knowledge/index.vue（页薄壳 `PageContainer` + `space-y-4 max-w-3xl` + 标题；`CompactEmptyState` 用法 icon/title/description；Input/Button from `~/components/ui/*`）
    - web/src/components/common/CompactEmptyState.vue（props：icon/title/description）
    - web/src/components/layout/AppSidebar.vue（`mainNavItems` 结构，追加 `{ to: '/knowledge/ingest', label: '一键摄取', icon: 'lucide--download' }` 紧邻「交付知识」）
    - 32-UI-SPEC.md「Component Breakdown」「States」「Color」「Copywriting」「Accessibility」节（逐项实现：固定三步顺序、状态语义色映射、aria-live、Label:for、外链 rel=noopener、data-testid 锚点）
    - web/src/components/ui/{input,button,label}（既有封装，复用不新增）
  </read_first>
  <action>
    新建 `web/src/components/knowledge/IngestPanel.vue`（`<script setup lang="ts">` + TS + Tailwind + vue-i18n）：
    - 表单态 `boardUrl`/`mrUrl` ref + 内联校验（空 → `ingest.form.errorRequired`；非 http(s) → `ingest.form.errorInvalidUrl`），校验失败不派发并聚焦首个错误字段。字段含 `<Label :for>` 关联 + `data-testid="ingest-board-url"`/`ingest-mr-url`，CTA `data-testid="ingest-submit"`。
    - 派发用 `useMutation`(`ingestApi.dispatch`)；成功 → 取 `run_id` 存 `runId` ref、`useToast` 成功提示 `ingest.dispatch.success`、开启轮询；失败 → `useErrorHandler` + `ingest.dispatch.failed`，不进入轮询。CTA `:disabled="isDispatching"` + loading spinner + 文案切 `ingest.form.submitting`。
    - 轮询用 `useQuery`(`getRun(runId)`)，`enabled = !!runId`，`refetchInterval: q => q.state.data?.status === 'running' ? 2000 : false`（完成/失败停轮，沿用 reconcile 范式）。
    - 结果区（`aria-live="polite"`）：`isError`（getRun 失败）→ `ingest.run.loadError` 错误行且不清空已有结果；无 run → `CompactEmptyState`（icon `lucide--inbox`，`ingest.empty.title`/`body`）；有 run → 顶部 run 状态行（running spinner / completed 绿 / failed 红；completed 且存在非 ok 步 → `ingest.run.partial` amber 提示；三步全 ok → `ingest.run.completed` 绿提示）+ **固定三行** StepRow（工作项 → 文档 → MR diff，内联实现）。
    - StepRow（内联）：状态图标按语义色映射（ok→emerald check-circle-2 / failed→destructive alert-circle / skipped→amber minus-circle / pending→muted；running 用 primary spinner）；步骤名 `text-sm font-medium`（i18n `ingest.steps.*`）；状态徽标 `text-xs`（`ingest.status.*`）；`identifier` 存在 → `code text-xs font-mono break-all`；`link` 存在 → `<a target="_blank" rel="noopener">` + `ingest.run.viewLink` + external-link 图标；error（failed/skipped 且有 error）→ `text-xs text-destructive`。状态语义不仅靠颜色（有文字，WCAG 1.4.1）。
    - 仅复用 `~/components/ui/*` + `.card` + 既有令牌/语义色，不新增字体/颜色变量/registry。
    新建 `web/src/pages/knowledge/ingest.vue`（薄壳）：`<PageContainer>` → `space-y-4 max-w-3xl` → `<h1 class="text-xl font-semibold">{{ t('ingest.title') }}</h1>` → `<IngestPanel />`。
    在 `web/src/components/layout/AppSidebar.vue` `mainNavItems` 追加 `/knowledge/ingest`「一键摄取」入口（icon `lucide--download`，紧邻「交付知识」）。
  </action>
  <verify>
    <automated>cd web && pnpm vue-tsc --noEmit -p tsconfig.json 2>&1 | head -40</automated>
  </verify>
  <acceptance_criteria>
    - `/knowledge/ingest`（file-based route）渲染薄壳页 + `IngestPanel`；侧边栏含「一键摄取」入口指向 `/knowledge/ingest`。
    - 表单两字段带 `<Label :for>` + data-testid，「开始摄取」CTA 有 data-testid；空/非 http(s) 提交触发内联校验错误且不调 dispatch。
    - 合法提交调 `ingestApi.dispatch`，拿 run_id 后开启 2s 轮询 `getRun`，弹派发成功 toast；status running 持续轮询、completed/failed 停轮。
    - 结果区始终渲染三步，按 status 显示语义色图标 + i18n 文案 + identifier/link/error；completed+非 ok → partial 提示；全 ok → 完成提示；getRun 失败 → 错误行不清空。
    - `pnpm vue-tsc --noEmit` 通过（含 i18n messages 类型）。
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: IngestPanel vitest 守护测试（真实 zh-CN.json）</name>
  <files>web/src/components/knowledge/__tests__/IngestPanel.spec.ts</files>
  <read_first>
    - web/src/components/knowledge/IngestPanel.vue（Task 2 产物，测试目标）
    - 既有面板组件的 spec（搜既有 `__tests__/*.spec.ts`，如 SensitiveSuggestionsPanel / ReconcilePanel 测试）—— mount + createI18n(真实 zh-CN.json) + mock api + @tanstack/vue-query 测试装配范式
    - 32-UI-SPEC.md「Acceptance Criteria」第 9 条（守护测试以真实 zh-CN.json 断言：标题 / CTA / 三步骤名 / 三状态）
    - web/src/locales/zh-CN.json（作为真实 i18n messages 注入，断言文案防被改空）
  </read_first>
  <action>
    新建 `web/src/components/knowledge/__tests__/IngestPanel.spec.ts`（vitest + @vue/test-utils + happy-dom）：以**真实** `zh-CN.json` 创建 i18n 注入（防文案被改空），mock `ingestApi`（`dispatch`/`getRun`）+ 装配 vue-query。覆盖：
    - 渲染含标题/CTA/三步骤名/三状态文案（取自真实 zh-CN.json `ingest.*`）。
    - 空提交 → 内联 `errorRequired`，未调 `dispatch`。
    - 合法提交 → 调 `dispatch(boardUrl, mrUrl)`，mock 返回 run_id 后渲染结果区（mock `getRun` 返回三步 ok）并显示完成提示。
    - mock `getRun` 返回 completed + 含 failed 步 → 显示 partial 提示 + 该步 error；getRun 报错 → 显示 loadError 行。
  </action>
  <verify>
    <automated>cd web && pnpm vitest run src/components/knowledge/__tests__/IngestPanel.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - 测试以真实 `zh-CN.json` 作 i18n messages，断言标题 / CTA / 三步骤名 / 三状态关键文案存在（被改空即失败）。
    - 覆盖：空提交校验不派发、合法提交调 dispatch + 轮询渲染三步、partial 提示、loadError 行。
    - `cd web && pnpm vitest run src/components/knowledge/__tests__/IngestPanel.spec.ts` 全绿。
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 用户输入 URL → 前端 | 仅作字符串校验（http(s)）后交后端解析；前端不直连飞书/git |
| 后端响应 → 渲染 | identifier/error/link 为后端已脱敏文本，按文本渲染 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-05 | Tampering (XSS) | StepRow `identifier`/`error`/`link` 渲染 | mitigate | Vue 模板插值默认转义；外链 `<a>` 仅用 `link` 做 href 且加 `rel="noopener"`（`target="_blank"`），不使用 v-html |
| T-32-06 | Information Disclosure | 错误/标识展示 | accept | 后端已脱敏（32-02 T-32-02），前端如实回显，不额外存储 |

无新增第三方 registry / 字体 / 颜色变量；仅复用既有 `~/components/ui/*` 与设计令牌（UI-SPEC Registry Safety：安全门不适用）。
</threat_model>

<verification>
- `cd web && pnpm vue-tsc --noEmit -p tsconfig.json` 通过（含 i18n messages 类型门禁）。
- `cd web && pnpm vitest run src/components/knowledge/__tests__/IngestPanel.spec.ts` 全绿。
- `cd web && pnpm eslint src/api/ingest.ts src/components/knowledge/IngestPanel.vue src/pages/knowledge/ingest.vue` 无新增错误。
- 人工/测试确认：仅复用既有组件与令牌，无新增字体/颜色变量/第三方 registry（UI-SPEC AC 第 10 条）。
</verification>

<success_criteria>
- `/knowledge/ingest` 可访问且侧边栏有入口；表单两 URL + 校验；派发→轮询→三步结果（语义色 + identifier/link/error）；partial/全完成/加载失败状态齐备。
- 全部可见文案来自 `zh-CN.json` `ingest.*`，守护测试以真实 zh-CN.json 锁关键文案。
- 仅复用既有 UI 组件 / 令牌 / 状态语义色，无新增设计资产。
</success_criteria>

<artifacts_produced>
本 plan 产出：
- `web/src/api/ingest.ts`（`ingestApi` + 类型）；barrel 导出。
- `web/src/components/knowledge/IngestPanel.vue`（表单 + 派发→轮询 + 三步结果）。
- `web/src/pages/knowledge/ingest.vue`（`/knowledge/ingest` 页）+ AppSidebar 入口。
- `zh-CN.json` `ingest` 命名空间 + `IngestPanel.spec.ts` 守护测试。
</artifacts_produced>

<output>
Create `.planning/phases/32-one-click-ingest/32-03-SUMMARY.md` when done.
</output>
