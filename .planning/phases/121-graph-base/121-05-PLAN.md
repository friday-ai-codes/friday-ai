---
phase: 121-graph-base
plan: 05
type: execute
wave: 3
depends_on: ["121-02", "121-03"]
files_modified:
  - server/services/code_graph/loader.py
  - server/tests/services/code_graph/test_loader.py
  - server/tests/services/code_graph/test_access.py
autonomous: true
requirements: [GRAPH-01, GRAPH-04]

must_haves:
  truths:
    - "给定 (repository, branch) 能装配出一张 MultiDiGraph，节点 = Symbol、边 = CallEdge"
    - "feature 分支取到的是 base ∪ feature 的 overlay，同文件 feature 覆盖 base"
    - "命中 exclusion 的文件其符号不在节点集，且其邻接边一并消失"
    - "解析边与裸名边分档正确，裸名边默认不装载"
    - "图元数据带 resolution_rate，低于 0.6 时 low_resolution 为真"
  artifacts:
    - path: "server/services/code_graph/loader.py"
      provides: "符号节点装配 + exclusion 过滤 + CallEdge 双档装配 + resolution_rate"
      exports: ["load_graph"]
      min_lines: 200
  key_links:
    - from: "server/services/code_graph/loader.py"
      to: "server/services/code_graph/access.py"
      via: "build_matcher_and_fingerprint + make_path_exclusion_memo"
      pattern: "make_path_exclusion_memo"
    - from: "server/services/code_graph/loader.py"
      to: "networkx"
      via: "nx.MultiDiGraph()"
      pattern: "MultiDiGraph"
---

<objective>
交付 `server/services/code_graph/loader.py` 的主干：ORM 独占的批量取数 + `MultiDiGraph` 装配 —— 符号节点（overlay 去重 + exclusion 过滤）与 `CallEdge` 双档边（解析边 / 裸名边三道过滤）+ `resolution_rate` 统计。

Purpose: 这是 GRAPH-01 的本体，也是 GRAPH-04「排除文件在所有图分析工具输出中不可见」的真正落点——过滤必须发生在**装配阶段**，被排除节点连同其所有邻接边一并丢弃，而不是在输出阶段裁剪。同时本仓应用代码零 networkx 使用，装配形态没有先例，10 万级行数下取数写法（`values_list` + `iterator` + FK attname）是数量级差异。
Output: 一个全同步的 `load_graph(...)`（由 `cache.py` 一次性 `sync_to_async` 包裹）+ 三条 loader 用例 + 一条 exclusion 端到端用例。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/121-graph-base/121-CONTEXT.md
@.planning/phases/121-graph-base/121-RESEARCH.md
@.planning/phases/121-graph-base/121-PATTERNS.md
@.planning/phases/121-graph-base/121-02-SUMMARY.md
@.planning/phases/121-graph-base/121-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 符号节点装配 —— overlay 分支合并与整文件去重（D-06-6）</name>
  <files>server/services/code_graph/loader.py, server/tests/services/code_graph/test_loader.py</files>
  <read_first>
    - server/services/code_intel/local_provider.py（L10–11 lazy import ORM 的理由；L54–66 `branch_filter = ["", branch_name] if branch_name else [""]` 的**权威写法**。⚠️ 它用 `sync_to_async(list)(qs.select_related(...))` 返回模型实例——这一半**不要照抄**，10 万级行数下必须换 `values_list + iterator`）
    - server/services/chunk_lookup.py（L84–99：`@sync_to_async` 私有 ORM 函数的边界写法。⚠️ 本相位差异：锁 + ORM + networkx 装配整段包进**一次** `sync_to_async`，由 cache.py 负责）
    - server/codegraph/models.py（`Symbol` L12–63：字段、`Index(fields=["repository","branch_name","file_path"])`、`unique_together = (repository, branch_name, file_path, name, start_line)`、`signature` 是 TextField）
    - server/services/code_graph/model.py（`CodeGraph` / `GraphMeta` 的节点属性约定）
    - .planning/phases/121-graph-base/121-RESEARCH.md §Code Examples 1、§Pattern 2、§Pitfall 3、§Byte Estimation 的属性数量敏感性表
    - .planning/phases/121-graph-base/121-CONTEXT.md D-06-1 / D-06-6
  </read_first>
  <action>
新建 `server/services/code_graph/loader.py`（全同步，模块 docstring 三段式，写明「ORM 独占层：本模块是本相位唯一碰 ORM 的地方；纯算法层不得碰 ORM，否则 Phase 122 的算法无法单测」）。ORM 模型一律函数内 lazy import。

实现私有 `_load_symbol_nodes(...)`，供 `load_graph` 调用：
① `branch_filter = ["", branch] if branch else [""]` —— 🚨 Pitfall 3：本仓分支语义是 **overlay**，`""` 是 base 全量、feature 分支只写增量行。照 `filter(branch_name="feature/x")` 只取本分支会让绝大多数调用边的另一端缺失，图碎成一地。注释里把这条写清。
② 取数：`Symbol.objects.filter(repository_id=rid, branch_name__in=branch_filter).values_list("id", "name", "symbol_type", "file_path", "start_line", "end_line", "branch_name").iterator(chunk_size=5000)`。
   ⛔ **不取 `signature`**（TextField 可达数 KB，放进节点属性会让内存估算彻底失准）。⛔ 不取 `chunk_id` 进节点属性（节点属性锁死 5 个；chunk 关联由 Plan 121-06 用独立的 `symbol_id → chunk_id` 旁挂映射解决）。
③ **overlay 去重（D-06-6）**：去重键 = **整文件 `file_path`**，不含行号。先扫一遍收集 `feature_files: set[str]` = 所有 `branch_name == branch` 且 `branch != ""` 的行的 `file_path`；base 行（`branch_name == ""`）若其 `file_path ∈ feature_files` 则整行丢弃。理由注释：与索引侧「per-file delete+rebuild」的增量语义一致（`CallEdge` docstring 明写 per-file 幂等删除按 `caller_file` 走）；行号在分支间会漂移，按 `start_line` 去重会漏。
   实现上做两趟 `iterator`（第一趟只取 `file_path` + `branch_name` 建 `feature_files`，第二趟建节点），或一趟收进内存后再筛——由执行方按内存权衡自选，但**必须**在注释里说明选了哪种及理由。
④ 建图：`graph = nx.MultiDiGraph()`（🚨 D-06-1：**不是 `DiGraph`**。实测 `DiGraph.add_edge(u, v, ...)` 对同一对节点的第二次调用是静默覆盖，三条不同 `kind` 的 A→B 边最终只剩最后一条，会让四档边契约直接失效。代价 +224 字节/边 / +44% 内存，已在 CONTEXT 接受）。节点键 = `str(symbol.id)`；节点属性**恰好 5 个**：`name` / `symbol_type` / `file_path` / `start_line` / `end_line`。
⑤ 同时产出供后续 task 复用的两个索引：`node_ids: set[str]`、`by_file_and_name: dict[tuple[str, str], str]`（`(normalize_rel_path(file_path), name) → symbol_id`，Plan 121-06 的跨仓边二次解析要用）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/code_graph/test_loader.py -x -q -k "overlay or assembles"</automated>
  </verify>
  <acceptance_criteria>
    - `test_branch_overlay_feature_over_base`：base（`branch_name=""`）有 `a.py::f` 与 `b.py::g`，feature（`branch_name="feat/x"`）有 `a.py::f2`；以 `branch="feat/x"` 装配后 —— 图中含 `b.py::g`（base 保留）、含 `a.py::f2`（feature 生效）、**不含** `a.py::f`（同文件被整文件覆盖）；以 `branch=""` 装配后只含 base 两个符号。
    - `test_assembles_multidigraph`：`isinstance(graph, nx.MultiDiGraph)` 为真且 `graph.is_multigraph()` 为真；对同一对节点连加两条不同 `kind` 的边后 `graph.number_of_edges(u, v) == 2`（证明未静默覆盖）。
    - 每个节点的属性字典键集合恰为 `{"name","symbol_type","file_path","start_line","end_line"}`（用例遍历断言）。
    - `loader.py` 不出现 `"signature"` 字符串于 `values_list` 调用中（`grep -n 'values_list' loader.py` 人工核对 + 用例断言节点属性无 `signature` 键）。
    - `loader.py` 使用 `.iterator(chunk_size=` 而非 `list(qs)`（`grep -c 'iterator(chunk_size=' loader.py` ≥ 1）。
  </acceptance_criteria>
  <done>符号节点按 overlay 语义装配进 MultiDiGraph，整文件去重规则被用例锁死。</done>
</task>

<task type="auto">
  <name>Task 2: 装配阶段 exclusion 过滤（GRAPH-04 落地）</name>
  <files>server/services/code_graph/loader.py, server/tests/services/code_graph/test_access.py</files>
  <read_first>
    - server/services/code_graph/access.py（`build_matcher_and_fingerprint` / `make_path_exclusion_memo` 及其 `excluded_files` 集合）
    - server/services/exclusion.py（`normalize_rel_path` L117 —— 越界/绝对路径/空 → `None`；`ExclusionMatcher.is_excluded` L209–236 fail-closed 语义）
    - .cursor/rules/observability-logging.mdc（级别纪律：高频循环禁止 INFO 刷屏；规范正文点名过「4000+ 文件的 graph_bundle_written 刷爆 stdout」）
    - .planning/phases/121-graph-base/121-RESEARCH.md §Code Examples 5 过滤时机段落、§Security Domain
  </read_first>
  <action>
在 `_load_symbol_nodes` 的节点循环里接入 exclusion：调用方（`load_graph`）先拿 `matcher, fingerprint = build_matcher_and_fingerprint(repository_id)`，再 `is_excluded = make_path_exclusion_memo(matcher)`。

过滤规则：
① `normalize_rel_path(file_path)` 返回 `None` 的路径**视为排除**（越界 / 绝对路径 / 空）。
② `is_excluded(norm_path)` 为真 → 该 `Symbol` 的 id **不进节点集**（不是「进了再删」，也不是输出阶段裁剪）。
③ 建边阶段（Task 3 与 Plan 121-06）：任一端点不在 `node_ids` 内 → **整条边丢弃**。这条保证「被排除节点连同其所有邻接边一并消失」。
④ 统计 `excluded_file_count = len(memo.excluded_files)` 写进 `GraphMeta`。
🚨 埋点纪律：装配循环是 10 万级迭代，**循环内绝对不能有 per-item 日志**。`log_exclusion_blocked`（INFO）由 `make_path_exclusion_memo` 的闭包按「每个新的被排除 file_path 至多一次」控制；loader 只在装配结束后补一条 `logger.debug("code_graph_exclusion_applied", component="code_graph", category="sampling", repository_id=..., excluded_file_count=..., dropped_symbol_count=...)` 的汇总事件。

⛔ 绝不自己写 glob/regex 匹配 `file_path`——`services/exclusion.py` 的模块 docstring 明写它是排除判定的**唯一事实源**，内含 ReDoS 静态拒绝、大小写不敏感的 global 规则、basename 兜底匹配、运行期异常 fail-closed 四层语义，重写必漏。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/code_graph/test_access.py -x -q -k exclusion</automated>
  </verify>
  <acceptance_criteria>
    - `test_exclusion_hides_symbols_and_edges`：造 `src/ok.py::caller` → `secret/.env.py::callee`（或用 `exclusion_rule_factory` 加一条命中 `secret/*` 的规则）的一条解析 `CallEdge`；装配后 —— 被排除文件的 symbol_id **不在** `graph.nodes`，且该条边**不在** `graph.edges`（`graph.number_of_edges() == 0`），而未被排除的 `caller` 节点仍在。
    - 路径归一失败即排除：`file_path = "../outside/x.py"` 的符号不进节点集。
    - `GraphMeta.excluded_file_count` 等于实际命中排除的**去重文件数**（不是符号数）。
    - 装配 200 个同属一个被排除文件的符号时，`log_exclusion_blocked` 的 spy `call_count == 1`（不刷屏回归）。
    - `loader.py` 中不含自写的 `fnmatch` / `re.compile` 用于 file_path 匹配（`grep -c 'fnmatch\|re.compile' loader.py` == 0）。
  </acceptance_criteria>
  <done>exclusion 在装配阶段生效，被排除文件的符号与其邻接边在图里彻底不存在，且不产生日志刷屏。</done>
</task>

<task type="auto">
  <name>Task 3: CallEdge 双档装配、裸名三道过滤与 resolution_rate</name>
  <files>server/services/code_graph/loader.py, server/tests/services/code_graph/test_loader.py</files>
  <read_first>
    - server/codegraph/models.py（`CallEdge` L97–173：`caller_symbol`(FK nullable SET_NULL) / `callee_symbol`(FK nullable) / `callee_name` / `callee_file` / `callee_qualifier` / `call_type` / `line_number`；`Index(fields=["repository","branch_name","caller_file"])`。🚨 `CallEdge` 上**没有** `match_confidence` 字段——全仓仅 `CrossRepoApiCall` 有）
    - server/services/code_graph/model.py（`EdgeKind` / `EdgeConfidence` / `BARE_NAME_BLACKLIST` / `LOW_RESOLUTION_THRESHOLD`）
    - .planning/phases/121-graph-base/121-RESEARCH.md §Code Examples 1 的 `call_rows` 查询形状（FK 一律用 attname `caller_symbol_id` / `callee_symbol_id`，避免隐式 JOIN）
    - .planning/phases/121-graph-base/121-CONTEXT.md Area 3 裸名边三道过滤
  </read_first>
  <action>
实现私有 `_load_call_edges(...)`：
取数 `CallEdge.objects.filter(repository_id=rid, branch_name__in=branch_filter).values_list("caller_symbol_id", "callee_symbol_id", "caller_file", "callee_name", "callee_file", "callee_qualifier", "call_type", "line_number").iterator(chunk_size=5000)`。FK 用 attname 取列（不产生 JOIN）。

**分档判据**：`callee_symbol_id is not None` ⇒ `EdgeConfidence.RESOLVED`；为 `None` ⇒ `EdgeConfidence.BARE_NAME`。（⛔ 不要去找 `CallEdge.match_confidence`，该字段不存在。）
**`caller_symbol_id is None`**（模块级调用）：无法挂到节点上 → 丢弃该边并计入 `dropped_no_caller_count`（写进日志汇总，不进 `GraphMeta` 契约）。docstring 说明为什么不用 `caller_file` 造虚拟节点：与 D-06-5 同理，虚拟节点污染上层的深度分组与计数。

**裸名边（`bare_name`）默认不装载**——仅当 `include_low_confidence=True` 时装载，且装载时**仍须**过三道过滤（CONTEXT Area 3）：
① 同目录/同文件优先：`callee_file` 与 `caller_file` 的目录不同则丢弃（跨目录同名默认丢弃）；
② `callee_qualifier` 非空时，候选符号必须匹配该 qualifier，否则丢弃；
③ `callee_name` 命中 `BARE_NAME_BLACKLIST` 则丢弃。
三道过滤各自可单测（抽成三个私有谓词函数）。

**边属性恒为 3 个**：`kind=EdgeKind.CALL.value`、`confidence=<档位 value>`、`line_number`。⛔ **不存 `reason`**（D-06-8：现推不存，第 4 个属性会跳一级、300k 边多花约 6.9MB，且 `reason` 只是给人看的展示文案，不参与计算）。

**`resolution_rate`**：统计**全部** `CallEdge` 行（不受 `include_low_confidence` 影响，否则关掉裸名时解析率恒为 1.0 变成假信号）：`resolved_count / (resolved_count + bare_name_count)`；分母为 0 时定义为 `1.0` 并在注释说明。写进 `GraphMeta.resolution_rate`；`< LOW_RESOLUTION_THRESHOLD`（0.6）时 `low_resolution=True`。

最后实现公开 `def load_graph(repository_id: str, branch: str = "", *, include_low_confidence: bool = False) -> tuple[CodeGraph, str]`（返回图与 exclusion 指纹，供 `cache.py` 算签名复用），串起 matcher → 节点 → 调用边，并发一条 `logger.debug("code_graph_assembled", component="code_graph", category="sampling", ...)` 汇总。Plan 121-06 会在此基础上补跨仓边、chunk 证据面与子图路径。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/code_graph/test_loader.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - 解析边：`callee_symbol` 非空的 `CallEdge` → 图中存在对应边且 `data["confidence"] == "resolved"`、`data["kind"] == "call"`。
    - 裸名边默认不装载：`include_low_confidence=False` 时，`callee_symbol IS NULL` 的边不出现在图里；`include_low_confidence=True` 且三道过滤全过时出现，`data["confidence"] == "bare_name"`。
    - 三道过滤各有一条用例：跨目录同名被丢弃、`callee_qualifier` 不匹配被丢弃、`callee_name="handle"`（黑名单）被丢弃。
    - 边属性字典键集合恰为 `{"kind","confidence","line_number"}`；`"reason" not in data`（`test_reason_not_stored_on_edge_attrs` 亦覆盖）。
    - `resolution_rate`：2 条解析边 + 3 条裸名边 → `pytest.approx(0.4)` 且 `low_resolution is True`；4 条解析边 + 1 条裸名边 → `0.8` 且 `low_resolution is False`。**该值与 `include_low_confidence` 取值无关**（两种入参下 `resolution_rate` 相同，用例断言）。
    - `caller_symbol_id IS NULL` 的边被丢弃且不抛异常。
    - `loader.py` 中 FK 取列使用 attname：`grep -c 'caller_symbol_id\|callee_symbol_id' loader.py` ≥ 2，且 `values_list` 参数中不含裸 `"caller_symbol"` / `"callee_symbol"`。
  </acceptance_criteria>
  <done>四档中的前两档（resolved / bare_name）装配完成，裸名假阳性由三道过滤挡住，解析率成为可信信号。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB 行 → 内存图节点 | `Symbol.file_path` 是 exclusion 判定的输入；漏判即泄漏 |
| 装配循环 → 日志 stdout | 10 万级迭代，误用 INFO 会打爆 stdout 与日志落库队列 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-121-泄漏 | Information Disclosure | `_load_symbol_nodes` exclusion 过滤 | mitigate | 装配阶段按 `file_path` 丢弃节点；建边时任一端不在 `node_ids` 即整条丢弃；`normalize_rel_path` 返回 `None` 视为排除；`test_exclusion_hides_symbols_and_edges` 回归 |
| T-121-裸名假阳性 | Tampering（决策污染） | `_load_call_edges` 裸名三道过滤 | mitigate | 默认不装载 + 显式开启时仍过三道过滤（同目录、qualifier 匹配、常见名黑名单）；`resolution_rate` 独立于开关计算，避免关掉裸名后解析率假性 100% |
| T-121-日志刷屏 | Denial of Service | 装配循环埋点 | mitigate | 循环内零 per-item 日志；`log_exclusion_blocked` 每个被排除文件至多一次；汇总事件用 DEBUG |
| T-121-内存失准 | Denial of Service | 节点/边属性个数 | mitigate | 节点恒 5 属性、边恒 3 属性、不取 `Symbol.signature`（TextField 数 KB）；线性估算模型的形态假设由此成立 |
| T-121-覆盖丢边 | Tampering（决策污染） | `MultiDiGraph` 选型 | mitigate | D-06-1 强制 `MultiDiGraph`；用例断言同一对节点可并存多档边（`DiGraph` 会静默覆盖） |
</threat_model>

<verification>
- `cd server && uv run pytest tests/services/code_graph -x -q`。
- `cd server && uv run pytest tests/services/test_exclusion_matcher.py tests/services/test_retrieval_exclusion.py tests/codegraph -q` 零回归。
- `cd server && uv run ruff check services/code_graph/ && uv run mypy services/code_graph/`。
- 观测契约用例仍绿（新埋点带 `component` / `category`）。
</verification>

<success_criteria>
`load_graph()` 能对任一已索引仓库产出 overlay 正确、exclusion 已生效、双档边分档正确、带 `resolution_rate` 的 `MultiDiGraph`；四条自动化用例锁死 overlay 去重、MultiDiGraph 语义、exclusion 端到端与裸名过滤。
</success_criteria>

## Artifacts this phase produces（本 plan 部分）

**新文件**
- `server/services/code_graph/loader.py`

**函数**
- `load_graph(repository_id, branch="", *, include_low_confidence=False) -> tuple[CodeGraph, str]`
- `_load_symbol_nodes(...)` / `_load_call_edges(...)` / 三个裸名过滤私有谓词

**图形态约定**
- 图对象：`networkx.MultiDiGraph`；节点键 = `str(Symbol.id)`
- 节点属性（恒 5）：`name` / `symbol_type` / `file_path` / `start_line` / `end_line`
- 边属性（恒 3）：`kind` / `confidence` / `line_number`

**structlog 事件名**
- `code_graph_exclusion_applied`（DEBUG）
- `code_graph_assembled`（DEBUG）

**测试**
- `test_loader.py::test_assembles_multidigraph` / `test_branch_overlay_feature_over_base`（+ 裸名三道过滤与解析率用例）
- `test_access.py::test_exclusion_hides_symbols_and_edges`

<output>
Create `.planning/phases/121-graph-base/121-05-SUMMARY.md` when done
</output>
