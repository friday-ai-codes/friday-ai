---
phase: 121-graph-base
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - server/pyproject.toml
  - server/uv.lock
  - server/friday/settings.py
  - .planning/observability/LOGGING-SPEC.md
  - server/tests/services/code_graph/__init__.py
  - server/tests/services/code_graph/conftest.py
  - server/tests/services/code_graph/test_model.py
  - server/tests/services/code_graph/test_loader.py
  - server/tests/services/code_graph/test_signature.py
  - server/tests/services/code_graph/test_cache.py
  - server/tests/services/code_graph/test_access.py
autonomous: true
requirements: [GRAPH-01, GRAPH-02, GRAPH-03, GRAPH-04]

must_haves:
  truths:
    - "networkx 是 server/pyproject.toml 的直接依赖，不再依赖 llama-index 传递"
    - "CODE_GRAPH_CACHE_MAX_BYTES / CODE_GRAPH_MAX_GRAPH_BYTES / CODE_GRAPH_BUILD_WAIT_TIMEOUT_SECONDS 可从 settings 读取且可被环境变量覆盖"
    - "component 值 code_graph 已登记进 LOGGING-SPEC §5 组件清单"
    - "server/tests/services/code_graph/ 可被 pytest 收集，indexed_repo fixture 的 index_status 是 INDEXED"
  artifacts:
    - path: "server/tests/services/code_graph/conftest.py"
      provides: "本目录自建 fixture（跨目录不可见问题的解法）"
      contains: "IndexStatus.INDEXED"
    - path: "server/friday/settings.py"
      provides: "CODE_GRAPH_* 预算配置"
      contains: "CODE_GRAPH_CACHE_MAX_BYTES"
    - path: ".planning/observability/LOGGING-SPEC.md"
      provides: "code_graph component 注册"
      contains: "code_graph"
  key_links:
    - from: "server/tests/services/code_graph/conftest.py"
      to: "server/repositories/models.py"
      via: "IndexStatus / RepositoryBranchIndex 直接建模型行"
      pattern: "IndexStatus\\.INDEXED"
---

<objective>
为 Phase 121 打好「地基之下的地基」：把 networkx 从传递依赖提升为直接依赖、落三个 `CODE_GRAPH_*` 配置项、把 `code_graph` 登记进强制的 LOGGING-SPEC 组件清单、并建好 `server/tests/services/code_graph/` 测试包与 fixture。

Purpose: 后续 9 个 plan 的每一个 task 都要跑 `uv run pytest tests/services/code_graph`；本 plan 不落地这套脚手架，Nyquist 验证链条从第一个 task 起就断。同时 `code_graph` 未登记进 LOGGING-SPEC §5 会直接违反 `.cursor/rules/observability-logging.mdc`（强制、code review 必查）。
Output: 依赖声明 + 三个 settings + 一条规范登记 + 一个可收集的测试包（7 个文件）。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/121-graph-base/121-CONTEXT.md
@.planning/phases/121-graph-base/121-RESEARCH.md
@.planning/phases/121-graph-base/121-PATTERNS.md
@.planning/phases/121-graph-base/121-VALIDATION.md
@.cursor/rules/observability-logging.mdc
</context>

<tasks>

<task type="auto">
  <name>Task 1: 依赖提升、CODE_GRAPH_* 配置项与 LOGGING-SPEC 组件登记</name>
  <files>server/pyproject.toml, server/uv.lock, server/friday/settings.py, .planning/observability/LOGGING-SPEC.md</files>
  <read_first>
    - server/pyproject.toml（L1–75 `[project].dependencies`，注意 L45–48 `mcp>=1.25.0,<2` 是「上界约束带解释性注释」的既有范式）
    - server/friday/settings.py 的 codegraph 区块 L809–895（尤其 L858–879：`GALAXY_CACHE_*` 与 `GRAPH_BUILD_ORPHAN_TIMEOUT_MINUTES` 的注释密度与 `env.<type>(...)` 写法）
    - .planning/observability/LOGGING-SPEC.md §5（L144–152，现有 component 清单只有 `codegraph`）
    - .planning/phases/121-graph-base/121-RESEARCH.md §Standard Stack 与 §Byte Estimation（预算算术）
  </read_first>
  <action>
执行 `cd server && uv add "networkx>=3.6,<4"`（不要手改 toml；uv 会同步 uv.lock）。在 `pyproject.toml` 新增行上方补一行中文注释，说明「networkx 原为 llama-index 传递依赖（uv.lock:2371），提升为直接依赖防上游停止传递；不锁死 `==3.6.1` 是因为 3.x 内 `MultiDiGraph` / BFS API 稳定，锁死会与 llama-index 的解析冲突」。

在 `server/friday/settings.py` 的 `GRAPH_BUILD_ORPHAN_TIMEOUT_MINUTES` 之后紧邻追加三项，全部带显式类型注解与 `env.int(...)`：
`CODE_GRAPH_CACHE_MAX_BYTES: int = env.int("CODE_GRAPH_CACHE_MAX_BYTES", default=512 * 1024 * 1024)`；
`CODE_GRAPH_MAX_GRAPH_BYTES: int = env.int("CODE_GRAPH_MAX_GRAPH_BYTES", default=256 * 1024 * 1024)`；
`CODE_GRAPH_BUILD_WAIT_TIMEOUT_SECONDS: int = env.int("CODE_GRAPH_BUILD_WAIT_TIMEOUT_SECONDS", default=120)`。
上方多行中文注释必须写进 RESEARCH §Byte Estimation 的预算算术（供后人调参）：按 `NODE_COST=640` / `EDGE_COST=560`、边:节点 = 3:1，单图约 `n × 2320` 字节；256MB 单图上限 ≈ 11 万符号触顶走降级；512MB 总预算 ≈ 只装得下 2 张接近上限的大图；且这是 per worker，4 worker 最坏 2GB 常驻。并注明「默认值保守，待 Plan 121-10 的最大仓实测交付物出数后复校」。
⛔ 不新增 in-flight 超时配置项——复用既有 `GRAPH_BUILD_ORPHAN_TIMEOUT_MINUTES`（RESEARCH Pitfall 5），在注释里显式写明这条复用决策。

在 `.planning/observability/LOGGING-SPEC.md` §5 的 component 取值行尾追加 `code_graph`，并在该行下方补一句说明：`codegraph` 指索引/抽取侧 Django app，`code_graph` 指 `server/services/code_graph/` 查询/服务侧内存图服务，两者刻意并存以便筛日志分开（D-07）。
  </action>
  <verify>
    <automated>cd server && grep -q 'networkx>=3.6,<4' pyproject.toml && uv run python -c "import networkx; assert networkx.__version__.startswith('3.'), networkx.__version__" && grep -q 'CODE_GRAPH_CACHE_MAX_BYTES' friday/settings.py && grep -q 'CODE_GRAPH_MAX_GRAPH_BYTES' friday/settings.py && grep -q 'CODE_GRAPH_BUILD_WAIT_TIMEOUT_SECONDS' friday/settings.py && grep -q '`code_graph`' ../.planning/observability/LOGGING-SPEC.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `server/pyproject.toml` `[project].dependencies` 含 `"networkx>=3.6,<4"`，且 `server/uv.lock` 被同步更新（`git diff --stat` 显示两个文件都变）。
    - `server/friday/settings.py` 恰好新增 3 个 `CODE_GRAPH_*` 常量，全部带 `: int` 注解与 `env.int` 默认值；`grep -c 'CODE_GRAPH_' friday/settings.py` ≥ 3。
    - settings 注释中出现字符串 `2320` 与 `per worker`（预算算术留痕）。
    - 未新增任何 `*_TIMEOUT_MINUTES` 配置项：`git diff server/friday/settings.py | grep -c '^+.*TIMEOUT_MINUTES'` 为 0。
    - `.planning/observability/LOGGING-SPEC.md` §5 同时含 `codegraph` 与 `code_graph` 两个值。
  </acceptance_criteria>
  <done>三处配置面（依赖 / settings / 规范注册表）齐备，`import networkx` 在 server venv 内可用。</done>
</task>

<task type="auto">
  <name>Task 2: 测试包与本目录自建 fixture</name>
  <files>server/tests/services/code_graph/__init__.py, server/tests/services/code_graph/conftest.py</files>
  <read_first>
    - server/tests/codegraph/conftest.py（L1–90：`graph_repo` / `seed_symbol` / `caller_symbol` / `callee_symbol` / `outgoing_call_edge` 的建模式——**只借写法，不借 fixture**）
    - server/tests/services/retrieval/__init__.py（空 `__init__.py` 的既有约定）
    - server/tests/conftest.py（L1–60：adrf patch、SQLite 文件库 `django_db_modify_db_settings`）
    - server/repositories/models.py（`IndexStatus` L33–41、`Repository` L142–330、`RepositoryBranchIndex` L734–772、`RepoExclusionRule` L843–912）
    - server/codegraph/models.py（`Symbol` L12–63、`CallEdge` L97–173）
    - server/services/exclusion.py（`invalidate_matcher_cache` L356）
    - .planning/phases/121-graph-base/121-RESEARCH.md §Pitfall 11
  </read_first>
  <action>
建 `server/tests/services/code_graph/__init__.py`（空文件，与 `tests/services/retrieval/__init__.py` 一致，与 `tests/codegraph/` 的目录约定对齐）。

建 `conftest.py`，**全部 fixture 自建，绝不从 `tests/codegraph/conftest.py` import**（pytest conftest 作用域是所在目录及子目录，兄弟分支不可见 —— RESEARCH Pitfall 11）。需要的 fixture：

`indexed_repo(db)` — `Repository.objects.create(...)`，**必须显式 `index_status=IndexStatus.INDEXED`**（`tests/codegraph/conftest.py::graph_repo` 没设，默认 `NOT_INDEXED`，会被 `ensure_repository_readable` 在第一道闸拒掉），并设 `is_deleted=False`、`default_branch="main"`、`last_indexed_commit_sha="a" * 40`。
`branch_index(indexed_repo)` — `RepositoryBranchIndex.objects.create(repository=..., branch_name="main", is_base_branch=True, last_indexed_commit_sha=..., last_indexed_at=timezone.now())`。注意 `RepositoryBranchIndex.branch_name` 存**真实分支名**，base 由 `is_base_branch=True` 标识，**从不为空串**（Pitfall 6）。
`symbols_factory(indexed_repo)` — 返回一个可调用体，签名 `(name, file_path, *, branch_name="", start_line=1, end_line=10, symbol_type="FUNCTION")`，创建并返回 `Symbol`。
`call_edges_factory(indexed_repo)` — 返回可调用体，签名 `(caller, callee=None, *, branch_name="", callee_name=None, callee_file=None, callee_qualifier=None, line_number=1, call_type="direct")`，`callee=None` 时造裸名边（`callee_symbol` 留空 + 填 `callee_name`）。
`exclusion_rule_factory(indexed_repo)` — 返回可调用体，签名 `(pattern, *, rule_type=RepoExclusionRule.RuleType.GLOB, enabled=True, source="user")`，创建 `RepoExclusionRule` 行。
`_reset_code_graph_state`（`@pytest.fixture(autouse=True)`）— 每个用例前后调 `services.exclusion.invalidate_matcher_cache()` 清 60s TTL matcher 缓存。docstring 里写明：`GraphService` 的模块级单例重置会在 Plan 121-07 交付 `_reset_for_tests()` 后补进本 fixture。

⚠️ 全部 fixture 走 lazy import（函数体内 import ORM 模型），与 `services/code_intel/local_provider.py` L10–11 的理由一致。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/code_graph -q --collect-only 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `server/tests/services/code_graph/__init__.py` 存在。
    - `grep -c 'IndexStatus.INDEXED' server/tests/services/code_graph/conftest.py` ≥ 1。
    - `conftest.py` 定义了 5 个具名 fixture（`indexed_repo` / `branch_index` / `symbols_factory` / `call_edges_factory` / `exclusion_rule_factory`）与 1 个 autouse fixture；`grep -v '^ *#' conftest.py | grep -c '@pytest.fixture'` == 6。
    - `conftest.py` 中不含 `from tests.codegraph` 或 `import tests.codegraph`（`grep -c 'tests.codegraph' conftest.py` == 0）。
    - `branch_index` 使用 `is_base_branch=True` 而非 `branch_name=""`。
  </acceptance_criteria>
  <done>pytest 能收集本目录且 fixture 可实例化；`indexed_repo` 天然通过后续 `ensure_repository_readable` 的索引态闸门。</done>
</task>

<task type="auto">
  <name>Task 3: 五个用例桩文件（与 121-VALIDATION.md 的 -k 选择器逐一对齐）</name>
  <files>server/tests/services/code_graph/test_model.py, server/tests/services/code_graph/test_loader.py, server/tests/services/code_graph/test_signature.py, server/tests/services/code_graph/test_cache.py, server/tests/services/code_graph/test_access.py</files>
  <read_first>
    - .planning/phases/121-graph-base/121-VALIDATION.md（Per-Task Verification Map 的 21 行 —— 每个 `-k` 选择器必须能选中桩里的一个用例名）
    - server/tests/codegraph/test_galaxy_cache.py（L55–127：签名测试四件套 + `mock.patch.object(..., wraps=...)` + `spy.call_count == 0` 的命中断言范式）
    - server/codegraph/lsp/tests/test_volar_pool.py（L28–34 autouse 单例重置、L99–141 LRU 顺序与逐出断言、L233–260 `threading.Barrier` 并发范式）
  </read_first>
  <action>
每个文件写模块 docstring（中文，说明本文件覆盖哪些 REQ 与由哪个 Plan 填充），然后写 `@pytest.mark.skip(reason="stub：由 Plan 121-0X 实现")` 的空壳用例。用例名必须让 `121-VALIDATION.md` 的 `-k` 选择器命中：

`test_model.py`：`test_edge_confidence_values`、`test_reason_not_stored_on_edge_attrs`。
`test_loader.py`：`test_assembles_multidigraph`、`test_cross_repo_edge_resolution`、`test_branch_overlay_feature_over_base`、`test_chunk_evidence_side_channel`、`test_on_demand_subgraph_depth_bounded`。
`test_signature.py`：`test_signature_watermark_sensitive`、`test_signature_stable_without_changes`、`test_signature_generation_two_tracks`、`test_signature_exclusion_fingerprint_changes`。
`test_cache.py`：`test_cache_hit_no_rebuild`、`test_partial_edges_when_edge_build_running`、`test_pending_not_inflight`、`test_orphan_running_not_inflight`、`test_estimate_bytes_is_pure`、`test_evict_lru_until_within_budget`、`test_single_flight_builds_once`、`test_build_failure_not_cached`、`test_degraded_on_demand_subgraph`、`test_invalidate_evicts_repo_entries`。
`test_access.py`：`test_exclusion_hides_symbols_and_edges`、`test_fail_closed_on_matcher_build_error`、`test_not_indexed_raises`、`test_deleted_repo_denied`、`test_observability_contract`。

⛔ 桩用例体只放 `pass`，**不要** import 尚不存在的 `services.code_graph.*`（会在收集期 ImportError）。每个桩上方留一行注释写明它对应 121-VALIDATION.md 的哪一行行为描述。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/code_graph -q && for k in hit partial pending_not_inflight orphan estimate evict single_flight build_failure degraded cross_repo overlay watermark generation stable exclusion fail_closed not_indexed deleted; do uv run pytest tests/services/code_graph -q -k "$k" --collect-only >/dev/null || { echo "MISS:$k"; exit 1; }; done; echo ALL_SELECTORS_OK</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/services/code_graph -q` 退出码 0，收集数 ≥ 25，全部 skipped，0 failed、0 error。
    - 121-VALIDATION.md 中出现的每一个 `-k <selector>` 都能选中至少 1 个用例（上方 verify 命令逐个校验，任一 miss 即失败）。
    - 五个文件均不含 `from services.code_graph`（`grep -rc 'from services.code_graph' server/tests/services/code_graph/test_*.py` 全为 0）。
  </acceptance_criteria>
  <done>Wave 0 的 Nyquist 缺口全部补齐：后续每个 task 的 `<automated>` 命令都能解析到真实用例。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI → server 运行时 | `uv add` 引入的分发物进入生产镜像 |
| 环境变量 → settings | `CODE_GRAPH_*` 预算值由部署方通过 env 注入 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-121-SC | Tampering | `uv add networkx` | mitigate | 121-RESEARCH.md §Package Legitimacy Audit 已判 **Approved**：已在 `uv.lock:2371-2376` 带 sha256 pin、`BSD-3-Clause`、`Project-URL` 指向 `github.com/networkx/networkx`、无强制运行期子依赖、已随 llama-index 在生产镜像跑过多个里程碑。无 `[ASSUMED]` / `[SUS]` / `[SLOP]` 包，故不设 blocking-human 校验闸 |
| T-121-预算失守 | Denial of Service | `settings.CODE_GRAPH_*` | mitigate | 默认值取保守侧（512MB 总 / 256MB 单图）；注释写死预算算术并标注「per worker」，防运维把它当全进程预算 |
| T-121-规范漏筛 | Repudiation | LOGGING-SPEC §5 | mitigate | 登记 `code_graph`，使后续所有事件可按 component 精确筛出；不复用 `codegraph`（索引侧）以免两条链路混淆 |
</threat_model>

<verification>
- `cd server && uv run pytest tests/services/code_graph -q` 全绿（全 skip）。
- `cd server && uv run ruff check . && uv run mypy .` 无新增告警。
- `cd server && uv run pytest tests/codegraph tests/code_relations -q` 零回归（确认 settings 改动未破坏既有 codegraph 套件）。
</verification>

<success_criteria>
networkx 是直接依赖；三个 `CODE_GRAPH_*` settings 可读；`code_graph` 已在 LOGGING-SPEC §5；`server/tests/services/code_graph/` 可收集且 121-VALIDATION.md 的每个 `-k` 选择器都有落点。
</success_criteria>

## Artifacts this phase produces（本 plan 部分）

**Settings（`server/friday/settings.py`）**
- `CODE_GRAPH_CACHE_MAX_BYTES: int`（默认 536870912）
- `CODE_GRAPH_MAX_GRAPH_BYTES: int`（默认 268435456）
- `CODE_GRAPH_BUILD_WAIT_TIMEOUT_SECONDS: int`（默认 120）

**依赖**
- `server/pyproject.toml`：`networkx>=3.6,<4`

**文档**
- `.planning/observability/LOGGING-SPEC.md` §5 新增 component 取值 `code_graph`

**新文件**
- `server/tests/services/code_graph/__init__.py`
- `server/tests/services/code_graph/conftest.py` — fixture：`indexed_repo` / `branch_index` / `symbols_factory` / `call_edges_factory` / `exclusion_rule_factory` / `_reset_code_graph_state`(autouse)
- `server/tests/services/code_graph/test_{model,loader,signature,cache,access}.py`

<output>
Create `.planning/phases/121-graph-base/121-01-SUMMARY.md` when done
</output>
