---
phase: 121-graph-base
plan: 09
type: execute
wave: 7
depends_on: ["121-08"]
files_modified:
  - server/services/code_graph/__init__.py
  - server/code_relations/tasks.py
  - server/services/graph_builder.py
  - server/tests/services/code_graph/test_cache.py
  - server/tests/services/code_graph/test_access.py
autonomous: true
requirements: [GRAPH-01, GRAPH-02, GRAPH-04]

must_haves:
  truths:
    - "上层只能从 services.code_graph 拿到 GraphService 与数据契约，拿不到 loader/cache 的内部实现"
    - "边构建完成与图谱构建完成后本 worker 的旧图被主动驱逐"
    - "主动失效失败不影响主流程，且不替代取图时的签名校验"
  artifacts:
    - path: "server/services/code_graph/__init__.py"
      provides: "curated 导出面（架构红线的机械防线）"
      exports: ["GraphService", "get_graph_service", "CodeGraph", "GraphMeta", "ChunkEvidence", "EdgeKind", "EdgeConfidence", "GraphError", "GraphAccessDenied", "GraphNotIndexed", "GraphBuildTimeout", "GraphBuildFailed"]
  key_links:
    - from: "server/code_relations/tasks.py"
      to: "server/services/code_graph/cache.py::GraphService.invalidate"
      via: "边构建完成钩子，紧邻既有 GalaxyGraphCache.refresh_repo"
      pattern: "invalidate"
    - from: "server/services/graph_builder.py"
      to: "server/services/code_graph/cache.py::GraphService.invalidate"
      via: "图谱构建完成钩子，紧邻既有 GalaxyGraphCache.refresh_repo"
      pattern: "invalidate"
---

<objective>
把 `code_graph` 包封口并接上失效钩子：`__init__.py` 只 re-export `GraphService` 与数据契约（loader/cache 不导出 = 架构红线的机械防线），并在两处既有的「构建完成」钩子点紧邻加一行主动驱逐。

Purpose: 「上层工具直连 loader」在 CONTEXT 里被明确列为架构红线。红线靠自律守不住，靠 `__init__.py` 不导出才守得住——把「绕过校验」从「需要自律」降级为「需要刻意违规」（ASVS V1）。主动失效则是把「重索引后首查还要等签名比对发现陈旧」这一步省掉，成本两行代码。
Output: 一个 curated barrel + 两处钩子 + 导出面守护测试。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/121-graph-base/121-CONTEXT.md
@.planning/phases/121-graph-base/121-RESEARCH.md
@.planning/phases/121-graph-base/121-PATTERNS.md
@.planning/phases/121-graph-base/121-08-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: curated barrel __init__.py（架构红线的机械防线）</name>
  <files>server/services/code_graph/__init__.py, server/tests/services/code_graph/test_access.py</files>
  <read_first>
    - server/services/code_intel/__init__.py（全文 28 行：绝对导入 `from services.code_intel.protocols import ...`、`__all__` 字母序、docstring 里显式写明使用边界。**照抄形态**）
    - server/services/retrieval/types.py（L120–125 `__all__` 字母序）
    - server/services/code_graph/model.py（Plan 121-02 交付的契约类型与 `__all__`）
    - server/services/code_graph/cache.py（`GraphService` / `get_graph_service`）
    - .planning/phases/121-graph-base/121-CONTEXT.md Area 4「单一入口」
    - .planning/phases/121-graph-base/121-RESEARCH.md §Security Domain V1 行
  </read_first>
  <action>
把 Plan 121-02 留的占位 `__init__.py` 替换为正式 barrel。

导出**且仅导出**：`GraphService`、`get_graph_service`（来自 `services.code_graph.cache`）；`CodeGraph`、`GraphMeta`、`ChunkEvidence`、`EdgeKind`、`EdgeConfidence`、`GraphError`、`GraphAccessDenied`、`GraphNotIndexed`、`GraphBuildTimeout`、`GraphBuildFailed`、`confidence_score`、`derive_reason`、`LOW_RESOLUTION_THRESHOLD`、`REDACTED_REPOSITORY`（来自 `services.code_graph.model`）。
⛔ **不导出** `loader`、`signature`、`access`、`estimate_graph_bytes`、`NODE_COST_BYTES` / `EDGE_COST_BYTES`、任何私有 helper。

绝对导入（本仓 first-party 用绝对路径，不用相对 import）。`__all__` **字母序**。

模块 docstring 三段式，显式写明架构红线（与 `code_intel/__init__.py` 写明 `register_provider` 使用边界同款做法）：所有图访问必须经 `GraphService.get_graph()`；`loader` / `cache` / `signature` / `access` 均不对外导出；上层工具直连 `loader` 视为**架构违规**，在 plan-checker 与 code-review 中列为红线。理由：`get_graph` 是权限校验（`ensure_repository_readable`）、exclusion 过滤与水位一致性校验的唯一收口点，绕过它等于同时绕过这三道闸。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/code_graph/test_access.py -x -q -k "export or barrel" && uv run python -c "import django,os;os.environ.setdefault('DJANGO_SETTINGS_MODULE','friday.settings');django.setup();import services.code_graph as m;assert sorted(m.__all__)==list(m.__all__);assert 'loader' not in m.__all__ and 'estimate_graph_bytes' not in m.__all__;print('BARREL_OK')"</automated>
  </verify>
  <acceptance_criteria>
    - 新增用例 `test_barrel_exports_only_public_surface`：断言 `services.code_graph.__all__` 是字母序；断言集合恰等于上文列出的 14 个名字；断言 `"loader"` / `"cache"` / `"signature"` / `"access"` / `"estimate_graph_bytes"` / `"NODE_COST_BYTES"` 均**不在** `__all__` 中。
    - `from services.code_graph import GraphService, CodeGraph, EdgeConfidence` 可用。
    - `__init__.py` 的 docstring 含子串 `架构` 与 `loader`（用例对 `__doc__` 断言）。
    - `__init__.py` 使用绝对导入（`grep -c 'from \.' server/services/code_graph/__init__.py` == 0）。
  </acceptance_criteria>
  <done>包的公开面收敛到 14 个符号；绕过 `GraphService` 成为需要刻意书写内部模块路径的行为。</done>
</task>

<task type="auto">
  <name>Task 2: GraphService.invalidate 与两处构建完成钩子</name>
  <files>server/services/code_graph/cache.py, server/code_relations/tasks.py, server/services/graph_builder.py, server/tests/services/code_graph/test_cache.py</files>
  <read_first>
    - server/code_relations/tasks.py（L216–232，尤其 L224–229 既有的 `await sync_to_async(GalaxyGraphCache.refresh_repo)(repository_id)` 与其注释「主动刷新只是优化……签名对比仍会自动重建，不影响主流程」）
    - server/services/graph_builder.py（L517–521 同款调用点）
    - server/codegraph/galaxy/cache.py（L263–294 `refresh_repo`：「所有异常内部吞掉」的实现形态；⚠️ 它**驱逐 + 重建**，本相位只驱逐）
    - server/services/code_graph/cache.py（Plan 121-07/08 的 `_cache` / `_total_bytes` / `_lock`）
    - .planning/phases/121-graph-base/121-CONTEXT.md Claude's Discretion 第 3 条
    - .planning/phases/121-graph-base/121-RESEARCH.md §Open Questions 第 3 条、§State of the Art 的 `background_runner` 降级声明
  </read_first>
  <action>
在 `cache.py` 加 `def invalidate(self, repository_id: str) -> None`（同步）与模块级便捷函数 `def invalidate_repository(repository_id: str) -> None`（内部 `get_graph_service().invalidate(...)`）：
- 持 `self._lock`，弹出所有 `key[0] == repository_id` 的条目（**该仓的所有分支**），扣减 `_total_bytes`，发一条汇总 `logger.info("code_graph_cache_invalidated", component="code_graph", category="sampling", repository_id=..., evicted_entries=..., evicted_bytes=..., initiated_by_user_id="system")`。
- 🚨 **只驱逐、不重建**——重建会在钩子线程上跑 2–4 秒纯 CPU（`GalaxyGraphCache.refresh_repo` 会重建，本相位刻意不同）。注释写明这条差异。
- **所有异常内部吞掉**（照 `galaxy/cache.py` L289–294 的 `except Exception → logger.warning` 形态），观测/缓存代码 best-effort，绝不反噬主流程。

在两处钩子点紧邻既有 `GalaxyGraphCache.refresh_repo` 调用加一行（函数内 lazy import 避免循环依赖 + `await sync_to_async(...)`）：
- `server/code_relations/tasks.py` L224–229 的 `if inserted > 0:` 块内（ChunkEdge 边构建完成，轨 A）。
- `server/services/graph_builder.py` L517–521（Symbol/CallEdge 抽取完成，轨 B）。
⛔ **不要**新起 background 任务——`background_runner` 自 Phase 61 起已降级为「仅 SQLite dev fallback / 少量非持久轻任务」。

🚨 两处都必须带这段注释（防后人误删签名校验）：主动失效**只是优化，不是正确性保证**——多 worker 部署下钩子只对本 worker 生效，其余 worker 的旧图仍要靠取图时的签名复校发现陈旧；因此 `GraphService._get_graph_sync` 里的签名比对**不可删除**。

⛔ 不得修改 `server/codegraph/repo_router_v2.py`（本里程碑冻结面）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/code_graph/test_cache.py -x -q -k invalidate && uv run pytest tests/code_relations tests/services/test_update_graph_progress.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `test_invalidate_evicts_repo_entries`：写入 repo A 的两个分支条目 + repo B 一个条目 → `invalidate(A)` 后 repo A 的两个条目消失、repo B 条目保留、`total_bytes` 等于 repo B 条目字节数；发出 `code_graph_cache_invalidated` 且 `evicted_entries == 2`。
    - 异常吞掉：monkeypatch 内部使 `invalidate` 抛异常 → 调用方不感知（不向上抛），发出 warning 事件。
    - 两处钩子：`grep -c 'code_graph' server/code_relations/tasks.py` ≥ 1 且 `grep -c 'code_graph' server/services/graph_builder.py` ≥ 1；两处都使用函数内 lazy import 与 `await sync_to_async(`。
    - 两处钩子的注释均含「签名」与「多 worker」字样（防误删留痕）。
    - `git diff --name-only` 不含 `server/codegraph/repo_router_v2.py`。
    - `uv run pytest tests/code_relations -q` 与 `tests/services/test_update_graph_progress.py` 零回归。
    - 该用例的 `@pytest.mark.skip` 已移除。
  </acceptance_criteria>
  <done>重索引/重建边后本 worker 的旧图被立即驱逐，且「钩子非充分条件」的理由在两处钩子与 cache.py 里都有留痕。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 包导出面 → 上层工具 | `__init__.py` 决定了上层能否绕过权限与 exclusion 校验 |
| 构建完成钩子 → 缓存状态 | 钩子在后台任务上下文执行，无 request 对象与触发用户 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-121-绕闸 | Broken Access Control (ASVS V1) | `__init__.py` 导出面 | mitigate | 只 re-export `GraphService` + 数据契约；`loader` / `cache` / `signature` / `access` 不导出，把「绕过校验」从「需要自律」降级为「需要刻意违规」；`test_barrel_exports_only_public_surface` 回归 |
| T-121-陈旧图 | Tampering（决策污染） | 主动失效钩子 | mitigate | 边构建/图谱构建完成即驱逐本 worker 旧图；**但不替代**取图时签名复校（多 worker 下钩子只对本 worker 生效），该理由在两处钩子与 cache.py 均有注释留痕 |
| T-121-钩子反噬 | Denial of Service | `invalidate` | mitigate | 全部异常内部吞掉（照 `GalaxyGraphCache.refresh_repo`）；只驱逐不重建，避免在钩子线程上跑 2–4 秒 CPU |
| T-121-归因缺失 | Repudiation | 后台钩子埋点 | mitigate | 钩子路径无触发用户，事件显式带 `initiated_by_user_id="system"`（规范强制） |
| T-121-冻结面误改 | (变更风险) | `codegraph/repo_router_v2.py` | mitigate | 本里程碑冻结；acceptance 用 `git diff --name-only` 断言未触碰 |
</threat_model>

<verification>
- `cd server && uv run pytest tests/services/code_graph -x -q` 全绿且**无 skip 残留**。
- `cd server && uv run pytest tests/services/code_graph tests/codegraph tests/code_relations -q` 零回归（wave merge 采样）。
- `cd server && uv run pytest` 全量绿（phase gate 前置）。
- `cd server && uv run ruff check . && uv run mypy .`。
</verification>

<success_criteria>
`services.code_graph` 的公开面收敛为 14 个符号且 loader/cache 不可从包顶层取得；两处构建完成钩子接上主动驱逐且不反噬主流程；「主动失效不替代签名校验」在三处代码注释里留痕。
</success_criteria>

## Artifacts this phase produces（本 plan 部分）

**新文件（正式版）**
- `server/services/code_graph/__init__.py`（curated barrel，`__all__` 14 项字母序）

**方法与函数**
- `GraphService.invalidate(repository_id) -> None`
- `invalidate_repository(repository_id) -> None`（模块级便捷函数）

**修改点**
- `server/code_relations/tasks.py`（~L224–229，ChunkEdge 边构建完成钩子）
- `server/services/graph_builder.py`（~L517–521，图谱构建完成钩子）

**structlog 事件名**
- `code_graph_cache_invalidated`（INFO，`initiated_by_user_id="system"`）

**测试**
- `test_cache.py::test_invalidate_evicts_repo_entries`
- `test_access.py::test_barrel_exports_only_public_surface`

<output>
Create `.planning/phases/121-graph-base/121-09-SUMMARY.md` when done
</output>
