---
phase: 131-gate-system-reflection
plan: 02
type: execute
wave: 2
depends_on:
  - 131-01
files_modified:
  - server/services/process_runtime/reflection.py
  - server/tests/services/process_runtime/test_reflection.py
autonomous: true
requirements:
  - REFL-01
  - REFL-02
  - REFL-03
user_setup: []

must_haves:
  truths:
    - "证据冲突/角色坍塌/复用矛盾/覆盖空洞触发反思，最多 2 轮"
    - "补丁只重算受影响短名单/单元，不触发全库 V2"
    - "超限进入 needs_human_review；每轮 ledger/事件脱敏可回放"
  artifacts:
    - path: server/services/process_runtime/reflection.py
      provides: "run_reflection_loop + ReflectionPatch"
      exports: ["run_reflection_loop", "ReflectionPatch", "detect_reflection_triggers"]
    - path: server/tests/services/process_runtime/test_reflection.py
      provides: "REFL-01/02/03 单测"
  key_links:
    - from: run_reflection_loop
      to: place_units/shortlist subset
      via: "repair_actions.affected_unit_ids"
      pattern: "run_reflection_loop|affected_unit_ids|needs_human_review|reflection_round"
---

<objective>
交付有界反思环：触发检测 → 结构化补丁 → 最多 N=2 轮只重算受影响单元 → 超限 needs_human_review；每轮 structlog + ledger（脱敏）（REFL-01、REFL-02、REFL-03；D-12~D-15）。

Purpose: 冲突可修复时局部回跳，避免无界全库重跑。
Output: `reflection.py` + 单元测试（含角色坍塌→修复合成路径）。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/131-gate-system-reflection/131-CONTEXT.md
@.planning/phases/131-gate-system-reflection/131-RESEARCH.md
@.planning/phases/131-gate-system-reflection/131-01-PLAN.md
@server/services/process_runtime/funnel_gates.py
@server/services/process_runtime/place_units.py
@server/interactions/ledger.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — 反思预算与补丁范围单测</name>
  <files>server/tests/services/process_runtime/test_reflection.py</files>
  <behavior>
    - detect_reflection_triggers：证据冲突、角色坍塌（forbidden/primary 冲突或 role_map clarify）、复用矛盾、覆盖空洞 → 非空 trigger_codes
    - run_reflection_loop max_rounds=2：第 1 轮可调用注入的 re_place / repair hook，仅传入 affected_unit_ids
    - 断言 V2/place mock：调用 repository_ids 或 unit 子集 ⊆ 原 hard_scope，绝不出现「未传 repository_ids」的全库调用
    - 两轮后仍触发 → final review_status/reason 含 needs_human_review
    - 一轮内可 resolve → outcome resolved，不再进入第 2 轮
    - 每轮尝试写 ledger（可 mock arecord_event）：payload 经 redact 路径；无超长需求字段；缺 run 时不抛、仅日志
    - 至少一条合成「角色坍塌 → 反思修复 → gate 可再评估」路径（为 132/INT-03 留钩）
  </behavior>
  <action>
    编写失败用例；注入可观测的 repair_hook / ledger_hook；引用 D-12~D-15、REFL-01~03。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/process_runtime/test_reflection.py -q --tb=line 2>&1 | head -50</automated>
  </verify>
  <done>测试文件存在且 RED（import/行为失败）。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — 实现 reflection 环</name>
  <files>server/services/process_runtime/reflection.py, server/tests/services/process_runtime/test_reflection.py</files>
  <action>
    实现 `detect_reflection_triggers(gate_report, placements, role_map, ...)` 与 `run_reflection_loop(..., max_rounds=2, repair_hook=..., ledger_hook=..., interaction_run=None)`。每轮产出 ReflectionPatch（contradictions、root_cause_hypotheses、jump_back_to、repair_actions、round、outcome）。默认 repair_hook 可调用 `place_units` 仅对 affected units（或由 hook 证明子集）；禁止无界全库。超限设置 needs_human_review。structlog：reflection_round_started/completed/failed 与 reflection_loop_completed；category=sampling；component=process_runtime。Ledger：best-effort `arecord_event`/`record_event` event_type=agent_decision（或 retry），payload 经 `redact_for_ledger`。不修改 repo_router_v2.py；不改 Adapter（接线归 Plan 03）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/process_runtime/test_reflection.py tests/services/process_runtime/test_funnel_gates.py -q --tb=short --reuse-db</automated>
  </verify>
  <done>REFL 单测全绿；Plan 01 门禁测不回归。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ledger payload | 可能含 unit/repo 元数据，禁止需求正文 |
| repair_hook → V2 | 必须带 hard_scope，防信息过度召回 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-131-04 | I | reflection ledger | mitigate | redact_for_ledger；无需求全文 |
| T-131-05 | E | 无限反思 | mitigate | max_rounds=2 硬顶 + needs_human_review |
| T-131-06 | T | 反思偷偷全库 route | mitigate | 单测断言 repository_ids ⊆ scope |
| T-131-SC | T | 无新包 | accept | 无 pip/npm 安装 |
</threat_model>

<verification>
pytest test_reflection.py + test_funnel_gates.py 绿；无 V2 文件改动。
</verification>

<success_criteria>
有界反思可测；补丁范围强制；ledger 脱敏可回放。
</success_criteria>

<output>
Create `.planning/phases/131-gate-system-reflection/131-02-SUMMARY.md` when done
</output>
