---
phase: 131-gate-system-reflection
plan: 03
type: execute
wave: 3
depends_on:
  - 131-01
  - 131-02
files_modified:
  - server/services/process_runtime/blueprint_route.py
  - server/initiatives/services/repo_association_service.py
  - server/services/process_runtime/stage_sandbox.py
  - server/tests/services/process_runtime/test_funnel_gates_wiring.py
autonomous: true
requirements:
  - GATE-01
  - GATE-02
  - REFL-01
  - REFL-03
user_setup: []

must_haves:
  truths:
    - "Blueprint/Association 在 placements 后跑门禁与有界反思；auto_selected 仅由发布门/D-02 决定"
    - "block / needs_human_review 时不得静默全库 primary 或下游开工"
    - "funnel_gates / reflection 摘要写入路由观测载荷；无需求原文"
  artifacts:
    - path: server/services/process_runtime/blueprint_route.py
      provides: "placements 后 gates+reflection 接线"
    - path: server/initiatives/services/repo_association_service.py
      provides: "feature-list 门禁纪律 + auto_selected"
    - path: server/tests/services/process_runtime/test_funnel_gates_wiring.py
      provides: "接线守卫测"
  key_links:
    - from: BlueprintRouteAdapter.route
      to: evaluate_funnel_gates / run_reflection_loop
      via: "_aapply_placement_funnel 之后"
      pattern: "evaluate_funnel_gates|run_reflection_loop|allow_auto_selected|funnel_gates"
---

<objective>
把统一门禁与反思环接到 Blueprint / RepoAssociation / stage_sandbox 主路径：placements 之后求值五门，可修复则 ≤2 轮反思，发布门收口 auto_selected；阻断静默全库开工（D-16、D-17）。

Purpose: 让 GATE/REFL 在真实漏斗出口可观测、可拦截。
Output: Adapter/Association/sandbox 接线 + `test_funnel_gates_wiring.py`；128–130 守卫不回归。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/131-gate-system-reflection/131-CONTEXT.md
@.planning/phases/131-gate-system-reflection/131-RESEARCH.md
@.planning/phases/130-placement-units-wiring/130-VERIFICATION.md
@server/services/process_runtime/funnel_gates.py
@server/services/process_runtime/reflection.py
@server/services/process_runtime/blueprint_route.py
@server/initiatives/services/repo_association_service.py
@server/services/process_runtime/stage_sandbox.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — 漏斗接线守卫测</name>
  <files>server/tests/services/process_runtime/test_funnel_gates_wiring.py</files>
  <behavior>
    - mock placements 后：route/propose 载荷含 funnel_gates（或等价）且 status/reason_codes 可见
    - 不满足 D-02 时 auto_selected 为 False（即使内部 V2 mock 返回 True）
    - 满足 D-02（合成 high+双证据+role_map 完整）时可 auto_selected True
    - gate block 或 needs_human_review：candidates 不得含 out_of_scope / 全库填充；status 非静默 ok
    - 可修复 trigger：reflection 被调用且 rounds≤2；超限后 needs_human_review 可观测
    - 日志/摘要无需求全文
  </behavior>
  <action>
    参考 test_funnel_placement.py 风格 stub router；断言门禁覆盖旧 auto_selected。引用 D-16、D-17、D-02。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/process_runtime/test_funnel_gates_wiring.py -q --tb=line 2>&1 | head -50</automated>
  </verify>
  <done>接线测 RED。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — Adapter/Association/sandbox 接线</name>
  <files>server/services/process_runtime/blueprint_route.py, server/initiatives/services/repo_association_service.py, server/services/process_runtime/stage_sandbox.py, server/tests/services/process_runtime/test_funnel_gates_wiring.py</files>
  <action>
    在 `_aapply_placement_funnel`（及 association 等价 placements 路径）之后调用 `evaluate_funnel_gates`；若 detect_reflection_triggers 非空则 `run_reflection_loop`（注入子集 repair），再二次求值门禁。最终 `auto_selected` 仅取发布门 `allow_auto_selected`（或 confirmation_acked 路径），覆盖 V2 旧值。clarify/block/needs_human_review 走既有 clarify 载荷扩展（additive：funnel_gates、reflection、publish_mode）。stage_sandbox：block/review 时禁止全库 primary。观测：入口 completed 事件可附 gate_status / reflection_rounds（无原文）。禁止修改 repo_router_v2.py。保持 test_funnel_team_gate / shortlist / placement 绿。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/process_runtime/test_funnel_gates_wiring.py tests/services/process_runtime/test_funnel_gates.py tests/services/process_runtime/test_reflection.py tests/services/process_runtime/test_funnel_placement.py tests/services/process_runtime/test_funnel_shortlist.py tests/services/process_runtime/test_funnel_team_gate.py -q --tb=short --reuse-db</automated>
  </verify>
  <done>接线测绿；128–130 漏斗守卫不回归；V2 文件未改。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 路由 API / MCP 出口 | 未确认不得暗示可开工 |
| sandbox 候选集 | block 时不得回填全库 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-131-07 | E | auto_selected 绕过确认 | mitigate | 发布门收口；wiring 测覆盖 V2 True 仍 False |
| T-131-08 | I | route 摘要 | mitigate | additive 摘要无需求全文 |
| T-131-09 | T | sandbox 全库回退 | mitigate | block/review 守卫 + 单测 |
| T-131-SC | T | 无新包 | accept | 无 pip/npm 安装 |
</threat_model>

<verification>
接线套件 + 既有 funnel_* 绿；`git diff --name-only` 执行 commits 不含 repo_router_v2.py。
</verification>

<success_criteria>
主路径可观测门禁与反思；D4 发布纪律生效；静默全库开工被阻断。
</success_criteria>

<output>
Create `.planning/phases/131-gate-system-reflection/131-03-SUMMARY.md` when done
</output>
