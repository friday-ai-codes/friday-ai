---
phase: 85-context-read-branch-binding
plan: 04
type: execute
wave: 3
depends_on: [85-03, 85-02]
files_modified:
  - server/mcp_tools/views.py
  - server/mcp_tools/serializers.py
  - server/tests/mcp_tools/test_lookup_project_by_branch.py
autonomous: true
requirements: [BIND-02]
must_haves:
  truths:
    - "lookup_project_by_branch 在 work_item_id 反查之外叠加 ProjectBranch 显式绑定反查，两源合并去重"
    - "可选 repository_id 收窄候选（跨仓同名分支时定位到具体仓的绑定）"
    - "单命中召回 pack_project_context + 写 RetrievalTrace；多/无命中返回候选列表（fail-soft，绝不抛、绝不阻断编码）"
  artifacts:
    - path: "server/mcp_tools/views.py"
      provides: "LookupProjectByBranchView 叠加 ProjectBranch 显式绑定查询"
      contains: "_lookup_by_branch_binding"
  key_links:
    - from: "server/mcp_tools/views.py"
      to: "initiatives.models.ProjectBranch"
      via: "_lookup_by_branch_binding 按 branch_name[+repository_id] 查显式绑定项目"
      pattern: "ProjectBranch"
    - from: "server/mcp_tools/views.py"
      to: "services.project_context_packer.pack_project_context"
      via: "单命中召回（既有路径不变）"
      pattern: "pack_project_context"
---

<objective>
扩展 MCP `lookup_project_by_branch`：在既有「分支名解析 work_item_id → ProjectWorkItemLink → Project」反查之外，叠加 Plan 03 的 `ProjectBranch` 显式多绑定反查，两源合并去重；序列化器加可选 `repository_id` 收窄跨仓同名分支；保持现有 fail-soft / candidates 契约（单命中召回 + 写 RetrievalTrace，多/无命中返回候选，绝不抛、绝不阻断编码）。

Purpose: BIND-02「扩展 `lookup_project_by_branch` 支持显式多绑定，多/无命中 fail-soft 返回候选」。
Output: `LookupProjectByBranchView` 叠加查询 + 可选 repository_id + 扩充测试。
</objective>

<execution_context>
@.planning/phases/85-context-read-branch-binding/85-RESEARCH.md
@.planning/phases/85-context-read-branch-binding/85-CONTEXT.md
</execution_context>

<context>
@.planning/PROJECT.md
@.cursor/rules/observability-logging.mdc
@server/mcp_tools/views.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: LookupProjectByBranchView 叠加 ProjectBranch 显式绑定反查 + 可选 repository_id</name>
  <read_first>
    - server/mcp_tools/views.py（`LookupProjectByBranchView` 2571-2676：现有 `_lookup_projects(work_item_id)` + 单命中 `pack_project_context` + RetrievalTrace + 多/无命中候选契约；`_project_summary` helper）
    - server/mcp_tools/serializers.py（`LookupProjectByBranchRequestSerializer` 当前仅 `branch_name`）
    - server/initiatives/models/project_branch.py（Plan 03 产 `ProjectBranch`：project FK + repository FK + branch_name + 索引）
    - server/services/branch_parsing.py（`parse_work_item_id_from_branch` 纯函数）
  </read_first>
  <action>
    在 `LookupProjectByBranchRequestSerializer` 新增**可选** `repository_id`（UUID，allow_null/required=False）——Phase 86 IDE hook 通常知当前 repo，可用于收窄；不传则跨仓返回候选（fail-soft 不变）。

    在 `LookupProjectByBranchView` 新增 `@sync_to_async def _lookup_by_branch_binding(self, branch_name, repository_id=None) -> list[Project]`：查 `ProjectBranch.objects.filter(branch_name=branch_name)`（传 `repository_id` 则追加 `repository_id=` 收窄），`select_related("project__space")`，返回去重 `Project` 列表。

    改 `post` 反查段：保留既有 `work_item_id = parse_work_item_id_from_branch(branch_name)` 与 `_lookup_projects(work_item_id)`（work_item_id 为 None 时该源为空，不再直接 early-return），**合并**两源——`projects = {p.id: p for p in work_item_projects}; for p in branch_binding_projects: projects.setdefault(p.id, p)`，取 `list(projects.values())` 去重。`output_data["candidates"] = [_project_summary(p) for p in merged]`。`len(merged)==1` → 既有 `pack_project_context` 召回 + matched=True + RetrievalTrace（source 字符串保留 `mcp_lookup_project_by_branch`，payload 增 `binding_source` 标记命中来自 work_item / branch_binding / both）。多/无命中 → matched=False 仅回候选（fail-soft，绝不抛）。整个反查路径绝不抛异常、绝不阻断编码（保持现契约）。

    `output_data` 增字段 `repository_id`（回显入参，便于排障）。保持 `run_id` / `included_layers` / `context` 既有键不变（不破坏 Phase 86 消费契约）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/mcp_tools/test_lookup_project_by_branch.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - 仅 ProjectBranch 显式绑定（分支名无 work_item_id）单命中 → `matched=True` + `context` 非空 + 写 RetrievalTrace。
    - work_item 反查 + ProjectBranch 绑定指向同一项目 → 合并去重后仍单命中（不因双源变多命中）。
    - 跨仓同名分支绑定多项目 + 不传 repository_id → `matched=False` + `candidates` 含多项；传 `repository_id` 收窄到单命中。
    - 无任何命中 → `matched=False` + `candidates=[]`，HTTP 200，不抛异常。
    - 既有输出键（`branch_name`/`work_item_id`/`matched`/`project`/`candidates`/`context`/`included_layers`/`run_id`）保持不变。
  </acceptance_criteria>
  <done>lookup 叠加显式绑定反查 + 可选 repository_id 收窄，fail-soft 候选契约保持，测试全绿。</done>
</task>

<task type="auto">
  <name>Task 2: 扩充 lookup fail-soft / 多绑定 / RetrievalTrace 守护测试</name>
  <read_first>
    - server/tests/mcp_tools/test_lookup_project_by_branch.py（既有测试结构 + fixtures：项目/work_item_link/分支名）
    - server/mcp_tools/views.py（Task 1 改后的合并逻辑 + RetrievalTrace 写入）
    - server/initiatives/services/project_branch_service.py（用 service.bind 构造测试绑定数据，不旁路写）
  </read_first>
  <action>
    扩充 `test_lookup_project_by_branch.py`：
    - 经 `ProjectBranchService.bind` 建显式绑定 → lookup 命中并召回（断言 matched + RetrievalTrace 写入条数/score）。
    - 双源指向同项目 → 合并去重单命中（不重复召回）。
    - 跨仓多绑定 → 多候选 fail-soft（matched=False、candidates 多项、不抛）。
    - repository_id 收窄 → 单命中。
    - work_item 反查与 ProjectBranch 均无命中 → 空候选 200（不抛、不阻断）。
    - members_only 项目 + 非成员触发用户单命中 → packer fail-closed 返回空 context（matched=True 但 context 空，不泄漏；记录此为既有 packer 设计行为，Pitfall 5）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/mcp_tools/test_lookup_project_by_branch.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - 上述 6 类场景测试存在且全绿。
    - 测试构造绑定数据经 `ProjectBranchService.bind`（不旁路写 ProjectBranch.objects.create）。
    - 多/无命中场景断言无异常抛出（fail-soft）。
  </acceptance_criteria>
  <done>lookup 多绑定 / fail-soft / RetrievalTrace 守护测试全绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IDE hook → 分支反查召回 | 反查触发用户经 packer fail-closed 控权限；反查本身不得抛断编码 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-85-04-01 | Denial of Service | lookup 反查 | mitigate | 全路径 fail-soft（多/无命中返回候选，绝不抛），不阻断编码（保持既有契约） |
| T-85-04-02 | Information Disclosure | 单命中召回 | mitigate | 召回经 `pack_project_context` 内置 fail-closed（非成员零召回），不绕过；RetrievalTrace 经 redact_for_ledger |
| T-85-04-03 | Tampering | branch_name 解析 | accept | `parse_work_item_id_from_branch` 纯函数无 SQL 拼接；ProjectBranch 查询走 ORM 参数化 |
</threat_model>

## Artifacts this phase produces (Plan 04)

- **改动符号（views.py）**：`LookupProjectByBranchView._lookup_by_branch_binding`（新私有方法）+ `post` 合并去重逻辑 + `output_data["repository_id"]` 回显
- **改动符号（serializers.py）**：`LookupProjectByBranchRequestSerializer` 新增可选 `repository_id`
- **RetrievalTrace source**：复用既有 `mcp_lookup_project_by_branch`（payload 增 `binding_source` 标记）
- **改动测试**：`server/tests/mcp_tools/test_lookup_project_by_branch.py`（扩充多绑定 / fail-soft / 收窄 / 两链 trace）
- **call_source**：无 LLM 调用，不涉及 §4.1。

<verification>
- `cd server && uv run pytest tests/mcp_tools/test_lookup_project_by_branch.py -q` 全绿。
- `cd server && uv run ruff check server/mcp_tools/` 无新增 lint。
</verification>

<success_criteria>
- `lookup_project_by_branch` 支持显式多绑定反查（叠加 ProjectBranch）+ 可选 repository_id 收窄（BIND-02）。
- 单命中召回 + RetrievalTrace；多/无命中 fail-soft 返回候选，绝不抛、绝不阻断编码。
- 既有输出契约不破坏（Phase 86 可消费）。
</success_criteria>

<output>
Create `.planning/phases/85-context-read-branch-binding/85-04-SUMMARY.md` when done.
</output>
