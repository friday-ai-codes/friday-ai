---
phase: 109-spine-convergence
plan: 06
type: execute
wave: 5
depends_on: ["109-04"]
files_modified:
  - web/src/components/chat/TechPlanCard.vue
  - web/src/components/chat/ChatMessageBubble.vue
  - web/src/components/chat/__tests__/TechPlanCard.spec.ts
  - web/src/components/chat/__tests__/chatMessageBubble.parts.spec.ts
autonomous: true
requirements: [SPINE-02]
must_haves:
  truths:
    - "SPINE-02 收窄 schema 后，新消息的方案卡仍能显示方案正文（正文改从投影响应与 runtime 取，不再依赖工具 input）"
    - "历史会话（SPINE-02 之前的消息，tech_plan 仍在工具 input 里）的方案卡不变空 —— tool input 保留为兜底级"
    - "多方案多轮会话里不会把新方案的正文渲染到旧方案卡上：runtime 数据只在 runtime.plan_id === props.codingPlanId 时才被采用"
    - "方案正文为空时渲染一行占位文案，而不是一个空的 prose 块"
    - { statement: "无 tech_plan / affected_files 字段的历史 runtime 与历史消息渲染零报错、零 warn", verification: backstop }
    - { statement: "runtime.coding_plan 的所有消费（techPlan / affectedFiles / feishuDocUrl）都过 plan_id 守卫", verification: backstop }
  artifacts:
    - path: "web/src/components/chat/TechPlanCard.vue"
      provides: "techPlan / affectedFiles 的三级优先解析 + plan_id 守卫 + 空正文占位"
      contains: "runtime.plan_id === props.codingPlanId"
  key_links:
    - from: "web/src/components/chat/TechPlanCard.vue"
      to: "chatStore.activeCodingPlan (runtime.coding_plan)"
      via: "第 2 级数据源，必须过 runtime.plan_id === props.codingPlanId 守卫"
      pattern: "plan_id === props.codingPlanId"
    - from: "web/src/components/chat/ChatMessageBubble.vue codingPlanData"
      to: "web/src/components/chat/TechPlanCard.vue"
      via: "tool input 降级为历史消息兜底（第 3 级）"
      pattern: "codingPlanData"
---

<objective>
处置 SPINE-02 对前端的连带影响：把 `TechPlanCard` 的方案正文数据源从「LLM 写进工具 input 的 `tech_plan`」迁到「投影响应 / runtime」，同时保住历史消息不变空、多方案会话不串态。

Purpose: 109-RESEARCH §6.2 与 UI-SPEC §交互契约 E 实证 —— `codingPlanData.techPlan` 目前取自 `create_coding_plan` 的 **tool input**，schema 收窄后该入参不存在 ⇒ 新消息的 `techPlan` 恒为空串，既有 `v-html="renderedPlan"` 会渲染成一个空 `prose` 块（不报错、不崩，只是「方案卡里没有方案」）。UI-SPEC 明确这一改动**必须与 SPINE-02 同 wave 落地**。
Output: 三级优先解析（含不可省的 `plan_id` 守卫）、空正文占位、`codingPlanData` 的兜底降级，以及对应的前端用例。
</objective>

<execution_context>
@.cursor/gsd-core/workflows/execute-plan.md
@.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/109-spine-convergence/109-UI-SPEC.md
@.planning/phases/109-spine-convergence/109-RESEARCH.md
@.planning/phases/109-spine-convergence/109-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: techPlan / affectedFiles 三级优先解析 + plan_id 守卫 + 空正文占位</name>
  <files>web/src/components/chat/TechPlanCard.vue</files>
  <read_first>
    - `.planning/phases/109-spine-convergence/109-UI-SPEC.md` §交互契约 E（三级优先表 + 🔴 第 2 级 `plan_id` 匹配守卫不可省的理由 + 空正文占位）、§关键现状事实 D（`feishuDocUrl` 已建立的三级优先形状与注释纪律）、§UI Considerations 第 16 条、§Backstop 第 1/6 条
    - `.planning/phases/109-spine-convergence/109-RESEARCH.md` §6.2「⚠️ SPINE-02 对前端的连带影响」
    - `web/src/components/chat/TechPlanCard.vue:39-72`（props 契约）、`:88-92`（`codingPlanRuntime`）、`:173-199`（`localFeishuDocUrl` + `feishuDocUrl` 三级优先与注释，逐字沿用其形状）、`:255-268`（`renderedPlan` / `mdReady` / `watchEffect`）、`:340-407`（展开内容区与 `v-html` 位置）、`:648-653`（折叠态摘要与 `（无方案文本）` 兜底）
  </read_first>
  <action>
    在 `TechPlanCard.vue` 内新增两个 computed，替代直接使用 `props.techPlan` / `props.affectedFiles` 的渲染路径。**形状逐字沿用 `feishuDocUrl`（`:185-192`）已建立的三级优先与注释纪律**：

    ```
    resolvedTechPlan = computed<string>(() => {
      1) props.techPlan 非空 → 用它（投影响应本地态：OrchestratedPlanCard 直接传入的 props）
      2) runtime = codingPlanRuntime.value；
         🔴 仅当 runtime && props.codingPlanId && runtime.plan_id === props.codingPlanId
         时才采用 runtime.tech_plan
      3) codingPlanData 传下来的 tool input 正文（历史消息兜底，见下）
      4) '' → 走空正文占位
    })
    ```
    `resolvedAffectedFiles` 同形（第 2 级取 `runtime.affected_files`，同样过 `plan_id` 守卫；第 3 级取 props）。

    🔴 **第 2 级的 `plan_id` 匹配守卫不可省**，注释必须写清理由：`activeCodingPlan` 只指向「对话内最近 CodingPlan」，多轮多方案会话里若不匹配就采用，会把**新方案的正文渲染到旧方案卡上** —— 不报错、不崩，只是内容串了，是最难查的一类缺陷。既有 `feishuDocUrl` 的注释已踩过并记录了这个坑（「不串其它 plan 的已导出态」），此处逐字沿用同一守卫与同一注释口径。

    第 3 级的落法：不新增 prop 也能实现 —— `props.techPlan` 在历史消息路径上本来就来自 `codingPlanData.techPlan`（`ChatMessageBubble` 传入），因此第 1 级与第 3 级共用 `props.techPlan`，两者的差别只在数据来源而非取值方式。**关键是不要删掉 `props.techPlan` 这一级**（砍掉它会让历史会话的方案卡集体变空）。在注释里明确标注：`props.techPlan` 同时承载「投影响应本地态」与「历史消息 tool input 兜底」两个来源。

    渲染路径改动：
    - `watchEffect` 与 `renderedPlan` 改读 `resolvedTechPlan`。
    - 影响文件区块改读 `resolvedAffectedFiles`（`affectedFiles.length > 0` 的判断同改）。
    - 折叠态摘要（`:651`）改读 `resolvedTechPlan`，既有 `（无方案文本）` 兜底**保持不变**。
    - **空正文占位**：`resolvedTechPlan` 为空且 `mdReady` 为真时，**不渲染空 `prose` 块**，改渲染 `<p class="text-xs text-muted-foreground">（暂无方案正文）</p>`。占位文案走插值，**不得用 `v-html`**。
    - `change_type` **不做前端兼容映射**（UI-SPEC §B.4 / §UI Considerations 第 19 条）：保持 `:404` 的原样显示，让 `create → add` 漂移可见。前端偷偷把 `create` 显示成 `add` 会掩盖后端映射缺陷，使该漂移变成永远查不出的问题；正确性由 109-03 的后端映射纯函数 + 显式断言保证。
  </action>
  <acceptance_criteria>
    - `TechPlanCard.vue` 中存在 `resolvedTechPlan` 与 `resolvedAffectedFiles` 两个 computed，且两者的 runtime 分支表达式均包含 `runtime.plan_id === props.codingPlanId`。
    - `renderedPlan` 的 `watchEffect`、影响文件区块、折叠态摘要三处均已改读 resolved computed（不再直接读 `props.techPlan` / `props.affectedFiles` 渲染）。
    - 模板中存在文案 `（暂无方案正文）`，且该占位与既有 `v-html="renderedPlan"` 互斥（`v-if` / `v-else` 分支）。
    - 除既有 markdown 渲染外无新增 `v-html`：`rg -n 'v-html' web/src/components/chat/TechPlanCard.vue | rg -v 'renderedPlan' | rg -v '^[0-9]+:[[:space:]]*(//|\*|<!--)'` 无匹配（同时过滤 `renderedPlan` 与注释行，避免注释提及让断言自我失效）。
    - 影响文件行仍是 `file.change_type` 的原样渲染（无前端 `create → add` 兼容映射）：模板中 `change_type` 的渲染表达式是裸字段引用，组件内**不存在**以 `'create'` 为键或为比较值、并产出 `'add'` 的映射结构（既无 `Record`/对象字面映射表，也无 `=== 'create'` 的三元/条件改写）。
    - `cd web && pnpm vitest run src/components/chat/__tests__/TechPlanCard.spec.ts` 全绿（既有断言零回归）。
  </acceptance_criteria>
  <verify>
    <automated>cd web && pnpm vitest run src/components/chat/__tests__/TechPlanCard.spec.ts</automated>
  </verify>
  <done>方案正文与影响文件走三级优先解析，`plan_id` 守卫在位，空正文渲染占位而非空 prose 块。</done>
</task>

<task type="auto">
  <name>Task 2: codingPlanData 降级为历史兜底 + 三级优先与串态防护的用例</name>
  <files>web/src/components/chat/ChatMessageBubble.vue, web/src/components/chat/__tests__/TechPlanCard.spec.ts, web/src/components/chat/__tests__/chatMessageBubble.parts.spec.ts</files>
  <read_first>
    - `.planning/phases/109-spine-convergence/109-UI-SPEC.md` §交互契约 E、§落点一览第 3 行（`codingPlanData` 的 `techPlan` 降级为历史兜底）、§Backstop 第 1/6 条、§UI Considerations 第 16 条
    - `web/src/components/chat/ChatMessageBubble.vue:758-828`（`codingPlanData` 的 input 取值与防御性双轨 result 解析）、`:1139-1153`（`TechPlanCard` 的 props 传递）
    - `web/src/components/chat/__tests__/TechPlanCard.spec.ts`（既有挂载方式、store mock 方式）
    - `web/src/components/chat/__tests__/chatMessageBubble.parts.spec.ts`（既有 parts 构造 helper）
  </read_first>
  <action>
    **A. `ChatMessageBubble.vue`**
    - 在 `codingPlanData` 的 `techPlan` / `affectedFiles` 取值处（`:764-767`）加注释，把它降级定位为**历史消息兜底**：SPINE-02 后新消息的工具 input 已无这两个字段，此处取值恒为空/空数组；新消息的正文由 `runtime.coding_plan` 或投影响应承载（`TechPlanCard` 的三级优先解析）。**不要删除这段取值**（历史消息仍靠它）。
    - 保留 `:1144-1145` 把 `codingPlanData.techPlan` / `.affectedFiles` 传给 `TechPlanCard` 的 props（它们就是三级优先的第 1/3 级载体）。
    - 不改 result 解析的防御性双轨（string / dict 两吃、解析失败返回 `null` 不抛）。

    **B. 用例（`TechPlanCard.spec.ts`）**——三级优先逐分支 + 串态防护
    - 第 1 级：`techPlan` prop 非空 + runtime 有另一份正文 → 渲染的是 prop 的正文。
    - 第 2 级：`techPlan` prop 为空 + runtime `plan_id` **等于** `codingPlanId` → 渲染 runtime 的 `tech_plan`；`affected_files` 同理生效。
    - 🔴 **串态防护（不可省的守卫）**：`techPlan` prop 为空 + runtime `plan_id` **不等于** `codingPlanId` → **不采用** runtime 正文，落到占位 `（暂无方案正文）`。用例名与注释写明「多方案会话不串态」。
    - 第 3 级：模拟历史消息（runtime 无 `tech_plan` 字段，prop 由 tool input 提供）→ 正文正常渲染，且**无控制台报错/warn**（可用 `vi.spyOn(console, 'warn')` / `'error'` 断言零调用）。
    - 第 4 级：三者皆空 → 渲染占位文案，且**不出现**空的 `.prose` 元素。
    - 历史数据零报错：runtime 对象缺 `provenance` / `tech_plan` / `affected_files` 三个字段（`undefined`）→ 挂载与渲染不抛（判定与取值必须是纯字面比较，不对 `undefined` 做属性访问）。

    **C. 用例（`chatMessageBubble.parts.spec.ts`）**
    - 新消息形态（`create_coding_plan` 的 tool input **不含** `tech_plan` / `affected_files`，result 含 `coding_plan_id`）→ `TechPlanCard` 仍被渲染，`codingPlanId` 正确传下；卡片不因正文为空而崩。
    - 历史消息形态（tool input 含 `tech_plan`）→ 正文经 props 传下且可渲染。
  </action>
  <acceptance_criteria>
    - `ChatMessageBubble.vue` 中 `codingPlanData` 的 `techPlan` / `affectedFiles` 取值仍在（未删除），且旁边有「历史消息兜底」的注释。
    - `TechPlanCard.spec.ts` 中存在一条 `plan_id` 不匹配时**不采用** runtime 正文的用例（串态防护），且该用例断言渲染结果为占位文案。
    - `TechPlanCard.spec.ts` 中存在一条 runtime 缺字段（`undefined`）时挂载不抛、且 `console.warn` / `console.error` 零调用的用例。
    - `TechPlanCard.spec.ts` 覆盖三级优先的四个分支各至少一条用例。
    - `cd web && pnpm vitest run src/components/chat/__tests__/` 全绿。
    - `cd web && pnpm vue-tsc --noEmit -p tsconfig.json` 无新增类型错误。
  </acceptance_criteria>
  <verify>
    <automated>cd web && pnpm vitest run src/components/chat/__tests__/ && pnpm vue-tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>tool input 明确降级为历史兜底且未被删除；三级优先四分支与串态防护均有用例；历史数据渲染零报错。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `runtime.coding_plan`（对话内最近 plan）↔ 单张方案卡 | 全局 runtime 与卡片实例不是一对一，无守卫即跨方案串数据 |
| 后端 markdown 正文 ↔ `v-html` 渲染路径 | 既有 markdown 渲染面不扩大；新增文案一律插值 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-109-06-01 | Tampering | `TechPlanCard.vue` 的 runtime 消费 | mitigate | 所有 runtime 消费过 `runtime.plan_id === props.codingPlanId` 守卫；Task 2 有串态防护用例 |
| T-109-06-02 | Tampering | 新增文案渲染 | mitigate | 占位文案与全部新增文案走 `{{ }}` 插值；`rg` 断言除既有 `renderedPlan` 外无新增 `v-html` |
| T-109-06-03 | Information Disclosure | 空正文时的降级 | accept | 空正文只显示中性占位文案，不回显任何后端 `detail` / 原始取值 |
| T-109-06-SC | Tampering | npm/pnpm installs | mitigate | 零新增依赖；验收含 `git diff --exit-code web/pnpm-lock.yaml` |
</threat_model>

<verification>
- `cd web && pnpm vitest run src/components/chat/__tests__/` 全绿。
- `cd web && pnpm vue-tsc --noEmit -p tsconfig.json` 无新增类型错误。
- `cd web && pnpm eslint src/components/chat/TechPlanCard.vue src/components/chat/ChatMessageBubble.vue` 无 error。
- `git diff --exit-code web/pnpm-lock.yaml` 退出码 0。
</verification>

<success_criteria>
- SPINE-02 后新消息的方案卡仍显示正文；历史会话方案卡不变空。
- `plan_id` 守卫在位且有串态防护用例。
- 空正文渲染占位而非空 `prose` 块。
- `change_type` 无前端兼容映射（后端映射缺陷保持可见）。
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces（本 plan 部分）

**新建符号（前端）**
- `resolvedTechPlan` / `resolvedAffectedFiles`（`web/src/components/chat/TechPlanCard.vue` computed，三级优先解析）
- 文案常量 `（暂无方案正文）`（空正文占位）

**新建文件 / 端点 / 迁移 / 事件名**：无
</artifacts_produced>

<output>
Create `.planning/phases/109-spine-convergence/109-06-SUMMARY.md` when done
</output>
