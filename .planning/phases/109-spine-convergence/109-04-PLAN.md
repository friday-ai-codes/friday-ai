---
phase: 109-spine-convergence
plan: 04
type: execute
wave: 4
depends_on: ["109-03"]
files_modified:
  - web/src/types/chat.ts
  - web/src/api/chat.ts
  - web/src/stores/chat.ts
  - web/src/components/chat/OrchestratedPlanCard.vue
  - web/src/components/chat/ChatMessageBubble.vue
  - web/src/composables/useToolDisplay.ts
  - web/src/components/chat/__tests__/OrchestratedPlanCard.spec.ts
  - web/src/components/chat/__tests__/chatMessageBubble.parts.spec.ts
  - web/src/composables/__tests__/useToolDisplay.spec.ts
autonomous: true
requirements: [SPINE-01]
must_haves:
  truths:
    - "用户在 chat 里看到编排产出后，能点一个「进入编码」按钮直接进入选目标仓 → 配置分支 → 确认编码 → 飞书导出四步，无需重新走一遍方案生成"
    - "start_plan_research 与 start_feature_solution 两个编排入口走同一判定、同一张卡片，任一入口都能进入编码"
    - "编排在途（__blocking_task__ 形态，无 artifact_version_id）与编排失败一律不渲染卡片，界面上零进度条、零阶段文案"
    - "两个编排工具已登记进 UNGROUPABLE_TOOLS，不会被归入「分析过程」折叠面板而导致卡片静默不渲染"
    - "同一方案版本重复点击「进入编码」得到中性的「已复用既有编码方案」提示，不报错、不提示异常"
    - "编排工具在 tool pill 与「分析过程」上有中文标签、图标与摘要，不再是未翻译的裸入参串"
    - { statement: "无 provenance / tech_plan / affected_files 字段的历史 runtime 与历史消息渲染零报错", verification: backstop }
    - { statement: "编排工具 result 为 JSON string 与 dict 两种形态都能解析，解析失败返回 null 不抛", verification: backstop }
    - { statement: "artifact_version_id 为 null 时视为不满足渲染条件，不渲染卡片、不抛错", verification: backstop }
    - { statement: "投影期间按钮 disabled 不重复发请求；即便重复到达，服务端唯一约束保证只产一行，前端按 created=false 走幂等 toast", verification: backstop }
    - { statement: "不回显后端自由文本：编排工具的 message / placeholder 一律不进渲染路径，说明句取前端常量", verification: backstop }
    - { statement: "新增的横幅、徽标、按钮、占位文案一律不使用 v-html，全走插值", verification: backstop }
  artifacts:
    - path: "web/src/components/chat/OrchestratedPlanCard.vue"
      provides: "编排产出卡片 + 「进入编码」按钮 + 投影后就地内嵌 TechPlanCard"
      contains: "OrchestratedPlanCard"
      min_lines: 60
    - path: "web/src/api/chat.ts"
      provides: "投影端点客户端 projectArtifactVersionToCodingPlan"
      contains: "from-artifact-version"
    - path: "web/src/stores/chat.ts"
      provides: "projectPlanToCodingPlan action"
      contains: "projectPlanToCodingPlan"
    - path: "web/src/types/chat.ts"
      provides: "CodingPlanProvenance 类型 + CodingPlanRuntime 扩字段 + ProjectPlanToCodingResponse"
      contains: "ProjectPlanToCodingResponse"
  key_links:
    - from: "web/src/components/chat/ChatMessageBubble.vue"
      to: "web/src/components/chat/OrchestratedPlanCard.vue"
      via: "isOrchestrationTool(name) && item.status === 'done' && 解析得 artifact_version_id 时渲染"
      pattern: "OrchestratedPlanCard"
    - from: "web/src/components/chat/ChatMessageBubble.vue UNGROUPABLE_TOOLS"
      to: "start_plan_research / start_feature_solution"
      via: "登记进不可分组工具集合（漏改则卡片静默不渲染）"
      pattern: "UNGROUPABLE_TOOLS"
    - from: "web/src/components/chat/OrchestratedPlanCard.vue"
      to: "POST /api/chat/coding-plans/from-artifact-version/"
      via: "chatStore.projectPlanToCodingPlan → api/chat.ts"
      pattern: "projectPlanToCodingPlan"
---

<objective>
落地 SPINE-01 的前端半边：在 chat 内新建编排产出的最小可操作面，让用户点「进入编码」触发惰性投影，并就地交棒给既有 `TechPlanCard` 走完执行流四步。

Purpose: 109-RESEARCH §6.1 实证「编排产出在 SPA 里目前没有任何可操作呈现面」（研究纠正 C-3）—— `start_plan_research` 返回的 `artifact_version_id` 前端零消费，工具连中文标签都未登记。UI-SPEC 定稿落点为方案 A（chat 内新卡片），并把「最小可操作面」的边界机械化为「在途完全不呈现」（裁决 D-4）。
Output: `OrchestratedPlanCard.vue`、投影 API 客户端与 store action、前端类型契约扩字段、`ChatMessageBubble` 的判定与渲染分支、`useToolDisplay` 三处登记，以及三个测试文件的用例。
</objective>

<execution_context>
@.cursor/gsd-core/workflows/execute-plan.md
@.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/109-spine-convergence/109-UI-SPEC.md
@.planning/phases/109-spine-convergence/109-CONTEXT.md
@.planning/phases/109-spine-convergence/109-RESEARCH.md
@.planning/phases/109-spine-convergence/109-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 前端数据契约扩字段 + 投影 API 客户端 + store action</name>
  <files>web/src/types/chat.ts, web/src/api/chat.ts, web/src/stores/chat.ts</files>
  <read_first>
    - `.planning/phases/109-spine-convergence/109-UI-SPEC.md` §前端数据契约变更（`CodingPlanProvenance` / `CodingPlanRuntime` 扩字段 / `ProjectPlanToCodingResponse` 的逐字定义与「类型故意保留 `| string`」的理由）与 §UI Considerations 第 17 条
    - `.planning/phases/109-spine-convergence/109-RESEARCH.md` §Runtime State Inventory「特别提示 —— 前端类型契约不是运行时状态但同样会静默漂移」（后端 `ConversationRuntimeCodingPlanSerializer` 与前端 `CodingPlanRuntime` 手工对齐，漏一侧 TS 不报错）
    - `.planning/phases/109-spine-convergence/109-03-SUMMARY.md`（投影端点最终路径与响应字段）
    - `web/src/types/chat.ts:637-643`（`CodingPlanRuntime` 现状）
    - `web/src/api/chat.ts:462-475`（`createSessionsForPlan` 的写法与默认导出清单）
    - `web/src/stores/chat.ts:143,1156,2466-2500,2555-2626`（`activeCodingPlan` 声明、runtime 写入、既有 patch 与 fan-out action 风格）
  </read_first>
  <action>
    1. `web/src/types/chat.ts`：
       - 新增 `export type CodingPlanProvenance = 'orchestrated' | 'draft'`，带 docstring 说明与后端 `CodingPlanProvenance` TextChoices 字面对齐。
       - 扩 `CodingPlanRuntime`：`provenance?: CodingPlanProvenance | string | null`、`tech_plan?: string`、`affected_files?: Array<{ file_path?: string, path?: string, change_type: string }>`、`recommended_repository_ids?: string[]`、`source_artifact_version_id?: string | null`。`provenance` 的类型**故意含 `| string`** —— 后端新增取值时前端不该编译失败，而应走保守分支（未知取值视为未经调研）；把这条理由写进字段注释。
       - 新增 `export interface ProjectPlanToCodingResponse`，七字段与 UI-SPEC 定义逐字一致（`coding_plan_id` / `created` / `title` / `tech_plan` / `affected_files` / `recommended_repository_ids` / `provenance`）；`created` 的注释写明「false = 幂等命中既有投影」。
    2. `web/src/api/chat.ts`：新增 `export async function projectArtifactVersionToCodingPlan(artifactVersionId: string): Promise<ProjectPlanToCodingResponse>`，`post` 到 `/chat/coding-plans/from-artifact-version/`，body `{ artifact_version_id: artifactVersionId }`。加进文件末尾的默认导出清单（与 `createSessionsForPlan` 同列）。
    3. `web/src/stores/chat.ts`：新增 action `projectPlanToCodingPlan(artifactVersionId: string): Promise<ProjectPlanToCodingResponse>`：
       - 调 `projectArtifactVersionToCodingPlan`；
       - 成功后若 `currentConversationId` 非空，调既有的 `scheduleRuntimePoll(currentConversationId.value, 3000)` 让 sessions 状态尽快跟上（**不**手工拼 `activeCodingPlan`，避免与 runtime 刷新竞态；卡片的即时数据由投影响应直接喂 props 承担，见 Task 2）；
       - 原样把响应 `return` 给调用方（错误不吞，交给组件 toast）；
       - 在 `return` 的 store 公开清单里导出该 action。
       - 🔴 **禁止**在该 action 内构造或修改 `CodingPlan` 的业务字段（前端不得自行拼 `CodingPlan`）。
  </action>
  <acceptance_criteria>
    - `web/src/types/chat.ts` 含 `export type CodingPlanProvenance` 与 `export interface ProjectPlanToCodingResponse`，后者字段数为 7。
    - `CodingPlanRuntime` 的 `provenance` 字段类型字面包含 `| string`（保守分支的类型层保障）。
    - `web/src/api/chat.ts` 中出现字符串 `/chat/coding-plans/from-artifact-version/`，且 `projectArtifactVersionToCodingPlan` 出现在文件末尾的默认导出清单里。
    - `web/src/stores/chat.ts` 中 `projectPlanToCodingPlan` 既有定义也出现在 store 的 `return` 公开清单中。
    - `cd web && pnpm vitest run src/stores/__tests__/chat.runtime.spec.ts src/api/__tests__/` 全绿（既有 runtime 与 API 客户端契约零回归）。
    - 全量类型检查（`pnpm vue-tsc --noEmit -p tsconfig.json`）在 **plan 级 `<verification>`** 执行，不作为本 task 的 per-task 验证 —— 全仓 `vue-tsc` 通常远超 109-VALIDATION 声明的 60s 反馈上限，放在 per-task 会让每次 task 提交都撞上限。
  </acceptance_criteria>
  <verify>
    <automated>cd web && pnpm vitest run src/stores/__tests__/chat.runtime.spec.ts src/api/__tests__/</automated>
  </verify>
  <done>前端类型契约与后端两个序列化面对齐；投影端点客户端与 store action 就位。</done>
</task>

<task type="auto">
  <name>Task 2: OrchestratedPlanCard.vue —— 「进入编码」入口 + 投影后就地交棒</name>
  <files>web/src/components/chat/OrchestratedPlanCard.vue, web/src/components/chat/__tests__/OrchestratedPlanCard.spec.ts</files>
  <read_first>
    - `.planning/phases/109-spine-convergence/109-UI-SPEC.md` §交互契约 A.3（卡片结构逐层 DOM）、A.4（点击 → 投影 → 就地交棒的流程与「按钮不消失」「幂等中性呈现」两条裁定）、§Copywriting Contract 界面侧全表、§Design System / Spacing / Typography / Color（零新增 token）、§UI Considerations 第 1/5/6/20 条与 §Backstop 第 4/5/7/8 条
    - `.planning/phases/109-spine-convergence/109-CONTEXT.md` 裁决 D-4（chat 呈现只做最小可操作面）
    - `web/src/components/chat/TechPlanCard.vue:39-72`（props 契约，决定交棒时要传哪些 props）与 `:340-365`（`.card` 头部形态可照抄）
    - `web/src/components/chat/CommitConfirmCard.vue:95-115`（`.card` + 头部 + 操作区的同族形态）
    - `web/src/components/ui/badge/index.ts`（`variant` 取值清单，确认 `success` 存在）
    - `web/src/composables/useToast.ts`（`useToast()` 的 `success` / `error` 用法）
  </read_first>
  <action>
    新建 `web/src/components/chat/OrchestratedPlanCard.vue`（`<script setup lang="ts">`，Composition API，沿用仓内组件风格）。

    **Props**：`artifactVersionId: string`（必填）。不接受任何后端自由文本作为渲染入参。

    **DOM 结构**（逐字按 UI-SPEC §A.3，不新增颜色/字号/间距 token）：
    - 根 `div.card.mt-2.animate-fade-in`
    - 头部 `px-4 py-3 border-b border-border/50 flex items-center gap-2`：`span.icon-[lucide--workflow].text-primary` + `span.text-sm.font-semibold` 文案 `技术方案已产出` + `<Badge variant="success" class="ml-auto">已编排</Badge>`
    - 正文 `px-4 py-3`：`p.text-xs.text-muted-foreground`，文案 `已完成仓库路由、代码召回与并行调研，可直接进入编码执行。`
    - 操作 `px-4 pb-4 pt-1`：`<Button>`（primary 默认），idle 态 `span.icon-[lucide--arrow-right].mr-1` + `进入编码`；loading 态 `span.icon-[lucide--loader-2].animate-spin.mr-2` + `正在准备编码方案…` 且 `:disabled`
    - 投影完成后：操作区的按钮**替换为**一行 `text-xs text-muted-foreground`，文案 `已进入编码，请在下方选择目标仓库`（UI-SPEC §A.4「按钮不消失」裁定：不留一个点了没反应的按钮）
    - 卡片下方就地内嵌 `<TechPlanCard>`（`v-if="localCodingPlanId"`）

    **不渲染方案正文、不折叠、无展开区、无骨架屏、无进度 UI**（D-4）。🔴 **不渲染后端 `message` 字段** —— 说明句取前端常量（沿用 107-UI-SPEC 的 T-107-06 纪律：后端自由文本只用于留痕与排障，不进渲染路径）。

    **文案常量**：在 `<script setup>` 顶部定义一个 `const COPY = { ... } as const` 承载上述全部中文串（硬编码中文常量惯例，沿用 `TOOL_LABELS` / `SIGNAL_LABELS` 先例，**不接 vue-i18n**）。

    **点击流程**（UI-SPEC §A.4）：
    ```
    projecting = true；按钮 disabled
    resp = await chatStore.projectPlanToCodingPlan(props.artifactVersionId)
    成功 → localCodingPlanId / localTechPlan / localAffectedFiles / localProvenance /
           localRecommendedRepositoryIds / localTitle ← resp
           toast：resp.created ? '编码方案已就绪，请选择目标仓库' : '已复用既有编码方案'
    失败 → toastError('未能进入编码，请稍后重试')；按钮回到 idle 可重试
    finally → projecting = false
    ```
    `created === false` 走**中性** toast（`toastSuccess`，非 error），卡片表现与首次一致 —— 幂等是系统正确性，不是用户需要理解的异常状态。

    **交棒**：把投影响应的字段**直接**作为 props 传给内嵌 `<TechPlanCard>`：`:plan-id` / `:coding-plan-id` 均为 `localCodingPlanId`，`:tech-plan="localTechPlan"`、`:affected-files="localAffectedFiles"`、`:title="localTitle"`、`:recommended-repository-ids="localRecommendedRepositoryIds"`、`:status="'draft'"`、`:is-confirming="false"`。理由：不依赖一次 runtime 刷新 ⇒ 点击到卡片出现之间没有空窗；`activeCodingPlan` 仍作为 sessions 状态的实时来源（`TechPlanCard` 内部既有行为，不改）。

    **可访问性与安全**：新增文案全走 `{{ }}` 插值，**不得使用 `v-html`**；按钮 `disabled` 时同时带 `aria-disabled`。

    新建 `web/src/components/chat/__tests__/OrchestratedPlanCard.spec.ts`（`vitest` + `@vue/test-utils`，mock `useChatStore` 与 `useToast`，照抄 `TechPlanCard.spec.ts` 的挂载与 stub 风格）：
    - 点击按钮 → `projectPlanToCodingPlan` 被以 `artifactVersionId` 调用一次；
    - `created=true` → 成功 toast 文案匹配；`created=false` → 「已复用既有编码方案」中性 toast（断言用的是 success 通道而非 error 通道）；
    - 投影成功后内嵌 `TechPlanCard` 被渲染，且收到的 `codingPlanId` / `techPlan` 等 props 来自投影响应；
    - 投影失败 → error toast 出现且按钮回到可点击（`disabled` 为 false）；
    - 投影期间按钮 `disabled` 为 true（不重复发请求）；
    - 组件内**不出现** `v-html`（可用快照/字符串断言组件渲染结果不含未转义 HTML，或直接断言组件源码不含 `v-html`：`rg -n 'v-html' web/src/components/chat/OrchestratedPlanCard.vue` 无匹配）。
  </action>
  <acceptance_criteria>
    - `web/src/components/chat/OrchestratedPlanCard.vue` 存在，含 `icon-[lucide--workflow]`、`Badge variant="success"`、`icon-[lucide--arrow-right]`、`icon-[lucide--loader-2]` 四个标识。
    - 组件内出现 UI-SPEC 界面侧文案表中的这五串：`技术方案已产出`、`已编排`、`进入编码`、`正在准备编码方案…`、`已进入编码，请在下方选择目标仓库`。
    - 新增面零 `v-html`：`rg -n 'v-html' web/src/components/chat/OrchestratedPlanCard.vue | rg -v '^[0-9]+:[[:space:]]*(//|\*|<!--)'` 无匹配（过滤注释行，避免注释里提到 `v-html` 让断言自我失效）。
    - 组件 `<template>` 段内**不出现**对 result/response 的 `message` 或 `placeholder` 字段的插值或属性绑定（说明句取 `COPY` 常量）。
    - 组件内不含任何进度条 / 阶段名 / 百分比相关渲染（D-4：在途完全不呈现）。
    - `cd web && pnpm vitest run src/components/chat/__tests__/OrchestratedPlanCard.spec.ts` 全绿，且用例覆盖：调用一次 / created=true toast / created=false 中性 toast / 交棒 props / 失败可重试 / 期间 disabled。
  </acceptance_criteria>
  <verify>
    <automated>cd web && pnpm vitest run src/components/chat/__tests__/OrchestratedPlanCard.spec.ts</automated>
  </verify>
  <done>「进入编码」入口卡片可用，点击后就地交棒给 `TechPlanCard`，幂等中性呈现，失败可重试。</done>
</task>

<task type="auto">
  <name>Task 3: 渲染分支与 UNGROUPABLE_TOOLS 登记 + useToolDisplay 三处登记（两个编排工具同时覆盖）</name>
  <files>web/src/components/chat/ChatMessageBubble.vue, web/src/composables/useToolDisplay.ts, web/src/components/chat/__tests__/chatMessageBubble.parts.spec.ts, web/src/composables/__tests__/useToolDisplay.spec.ts</files>
  <read_first>
    - `.planning/phases/109-spine-convergence/109-UI-SPEC.md` §关键现状事实 B（编排工具**有两个**，返回体同形）与 C（在途返回形态无 `artifact_version_id`）、§交互契约 A.1（渲染条件三条）/A.2（`UNGROUPABLE_TOOLS` 必须同步，🔴 漏改则卡片静默不渲染）/A.5（三处登记表）、§UI Considerations 第 2/3/4/7 条、§Backstop 第 3/4/7 条
    - `.planning/phases/109-spine-convergence/109-RESEARCH.md` §6.1（`TOOL_LABELS` / `TOOL_ICONS` / `toolAction` 三处均无编排工具条目）与 §6.2（`UNGROUPABLE_TOOLS` → 单例 tool 分支 → 卡片的调用链）
    - `server/agents/tools/plan_research_tools.py:245-298`（在途 `__blocking_task__` 形态与终态 `_map_terminal` 的字段）
    - `server/agents/tools/feature_solution_tools.py:183-195`（`start_feature_solution` 终态同形返回，含 `artifact_version_id`）
    - `web/src/components/chat/ChatMessageBubble.vue:496-502`（`UNGROUPABLE_TOOLS` 与 `isProcessTool`）、`:752-755`（`isCodingPlanTool` 的 `mcp__` 前缀剥离写法）、`:758-828`（`codingPlanData` 的防御性双轨 result 解析）、`:1126-1164`（单例 tool 分支与卡片渲染位置）
    - `web/src/composables/useToolDisplay.ts:23-62`（`TOOL_LABELS`）、`:65-82`（`TOOL_ICONS`）、`:291-386`（`toolAction` 的 `switch` 结构与 `default` 分支）
  </read_first>
  <action>
    **A. `ChatMessageBubble.vue`**
    1. 新增与 `isCodingPlanTool` 平级的判定函数：
       ```
       function isOrchestrationTool(name: string): boolean {
         const bare = name.replace(/^mcp__[^_]+__/, '')
         return bare === 'start_plan_research' || bare === 'start_feature_solution'
       }
       ```
       🔴 **必须同时覆盖两个工具**（UI-SPEC §关键现状事实 B）：只登记 `start_plan_research` 会让 `start_feature_solution` 这条编排入口继续没有编码入口，SC-1 在该入口下不成立。
    2. 🔴 **`UNGROUPABLE_TOOLS` 加入 `'start_plan_research'` 与 `'start_feature_solution'`**（`:499`）。漏这一步则两个工具被 `isProcessTool` 归入「分析过程」折叠面板，而卡片渲染分支只存在于 `item.kind === 'tool'` 的单例分支里 ⇒ **不报错、不崩、只是入口不见**。在该集合旁加注释点明这条静默失守点。
    3. 新增 `orchestratedPlanData` computed：从编排工具的 tool result 里解析 `{ status, artifact_version_id }`。解析沿用 `codingPlanData` 既有的**防御性双轨**（`:778-799`）：result 可能是 JSON string 也可能是 dict，两种都吃，解析失败返回 `null` 不抛。仅当 `status === 'done'` **且** `artifact_version_id` 为非空字符串时返回数据，否则返回 `null`（`artifact_version_id` 为 `null` 时同样返回 `null`）。
    4. 在单例 tool 分支（`:1139` 的 `TechPlanCard` 旁）加 `<OrchestratedPlanCard v-if="isOrchestrationTool(item.name) && item.status === 'done' && orchestratedPlanData" :artifact-version-id="orchestratedPlanData.artifactVersionId" />`。三条渲染条件同时成立才渲染 ⇒ 在途（`__blocking_task__`，无 `artifact_version_id`）与失败一律只剩既有 tool pill。
    5. 把编排工具加入既有「展开详情不显示原始 input/output」的排除条件（`:1154` 的 `!isCodingPlanTool(item.name)` 一并加 `&& !isOrchestrationTool(item.name)`），避免 `placeholder` / `message` 原文经 `StructuredJsonView` 上屏（§Backstop 第 7 条）。

    **B. `useToolDisplay.ts`**（三处登记，缺一则用户看不出这一步做了什么）
    - `TOOL_LABELS`：`start_plan_research: '方案编排调研'`、`start_feature_solution: '功能方案编排'`
    - `TOOL_ICONS`：两者均 `'icon-[lucide--workflow]'`
    - `toolAction`：新增两个 `case`（共用同一段逻辑）——终态返回 `跨仓方案编排已完成`；在途（result 含 `__blocking_task__`）返回 `方案编排调研进行中`；否则回退需求文本 `编排「{requirement 截断 32 字}」`。🔴 **不回显后端 `placeholder` / `message` 原文**。

    **C. 测试**
    - `web/src/composables/__tests__/useToolDisplay.spec.ts` 加用例：两个编排工具的 label / icon 各自正确；`toolAction` 三分支（终态 / 在途 / 兜底）各一条；并断言在途分支的返回值**不包含**后端 `placeholder` 串的任何片段。
    - `web/src/components/chat/__tests__/chatMessageBubble.parts.spec.ts` 加用例：
      - 🔴 **静默失守点断言**：`start_plan_research` 与 `start_feature_solution` 均在 `UNGROUPABLE_TOOLS` 内 ⇒ 断言它们**不被**归入「分析过程」折叠面板、而是走单例 tool 分支（用组件渲染结果断言 `OrchestratedPlanCard` 存在；并对「若从集合中移除则卡片不渲染」的语义写清注释）。
      - 三条渲染条件各自不成立时不渲染：`status !== 'done'` / result 解析得 `status !== 'done'` / `artifact_version_id` 为 `null` 或缺失。
      - `__blocking_task__` 形态（无 `artifact_version_id`）→ 不渲染卡片、不抛错。
      - result 为 JSON string 与 dict 两形态都能解析出 `artifact_version_id`。
  </action>
  <acceptance_criteria>
    - `web/src/components/chat/ChatMessageBubble.vue` 中 `UNGROUPABLE_TOOLS` 集合字面量同时含 `'start_plan_research'` 与 `'start_feature_solution'`。
    - 存在 `function isOrchestrationTool`，其判定同时匹配上述两个工具名（且剥离 `mcp__` 前缀）。
    - `<OrchestratedPlanCard>` 的渲染条件表达式同时包含 `isOrchestrationTool(item.name)`、`item.status === 'done'` 与 `orchestratedPlanData` 三项。
    - `web/src/composables/useToolDisplay.ts` 中两个工具名各自在 `TOOL_LABELS` 对象内有一条 label 条目、在 `TOOL_ICONS` 对象内有一条 icon 条目、在 `toolAction` 的 `switch` 内有一个 `case` —— 三处**按结构逐处核对**（不用 `rg -c` 计数，注释行会让计数断言失真）。对应的行为断言由本 task 的 `useToolDisplay.spec.ts` 用例覆盖（label / icon / action 三分支各一条）。
    - `cd web && pnpm vitest run src/composables/__tests__/useToolDisplay.spec.ts src/components/chat/__tests__/chatMessageBubble.parts.spec.ts` 全绿，且其中存在显式断言两个编排工具属于 `UNGROUPABLE_TOOLS`（不被归入「分析过程」）的用例。
    - `cd web && pnpm vitest run src/components/chat/__tests__/` 全绿（既有 chat 组件测试零回归）。
  </acceptance_criteria>
  <verify>
    <automated>cd web && pnpm vitest run src/composables/__tests__/useToolDisplay.spec.ts src/components/chat/__tests__/</automated>
  </verify>
  <done>两个编排工具都能渲染出「进入编码」卡片；`UNGROUPABLE_TOOLS` 静默失守点有显式断言；工具标签/图标/摘要三处登记齐备；在途与失败零卡片。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 后端 tool result（含 LLM 侧产物）↔ 前端渲染路径 | `message` / `placeholder` 是后端自由文本，上屏即成为惯例并可能带 LLM 原文 |
| 前端 ↔ 投影端点 | 前端只能传 `artifact_version_id`，不得自行构造 `CodingPlan` 字段 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-109-04-01 | Tampering | `OrchestratedPlanCard.vue` / `ChatMessageBubble.vue` | mitigate | 新增文案全走 `{{ }}` 插值，零 `v-html`（Task 2 有 `rg` 断言）；编排工具的原始 input/output 不经 `StructuredJsonView` 展开 |
| T-109-04-02 | Information Disclosure | 说明句与工具摘要 | mitigate | 说明句与摘要一律取前端常量；不回显 `message` / `placeholder`（Task 2/3 有源码级断言） |
| T-109-04-03 | Elevation of Privilege | `projectPlanToCodingPlan` | mitigate | 前端只传 `artifact_version_id`；conversation 与 owner 判定完全在服务端（109-03），前端不得拼 `CodingPlan` |
| T-109-04-04 | Denial of Service | 重复点击「进入编码」 | mitigate | 按钮 `disabled` 期间不重发；服务端 DB 唯一约束 + `IntegrityError` 分支兜底，前端按 `created=false` 走幂等 toast |
| T-109-04-SC | Tampering | npm/pnpm installs | mitigate | 本 plan **零新增依赖**、不跑 `shadcn init` / `shadcn add`、不从任何 registry 拉块（UI-SPEC §Registry Safety）。验收含 `git diff --exit-code web/pnpm-lock.yaml` |
</threat_model>

<verification>
- `cd web && pnpm vitest run src/components/chat/__tests__/ src/composables/__tests__/useToolDisplay.spec.ts` 全绿。
- `cd web && pnpm vue-tsc --noEmit -p tsconfig.json` 无新增类型错误。
- `cd web && pnpm eslint src/components/chat/OrchestratedPlanCard.vue src/components/chat/ChatMessageBubble.vue src/composables/useToolDisplay.ts src/stores/chat.ts src/api/chat.ts src/types/chat.ts` 无 error。
- `git diff --exit-code web/pnpm-lock.yaml` 退出码 0（零新增依赖，Registry Safety）。
</verification>

<success_criteria>
- 用户在 chat 内看到编排产出卡片，点「进入编码」即可进入执行流四步（SPINE-01 用户故事在 chat 入口下完整成立）。
- 两个编排工具同判定同卡片；在途与失败零卡片、零进度 UI（D-4 边界机械成立）。
- `UNGROUPABLE_TOOLS` 静默失守点有显式断言。
- 零新增依赖、零新增设计 token、零 `v-html`。
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces（本 plan 部分）

**新建文件**
- `web/src/components/chat/OrchestratedPlanCard.vue`
- `web/src/components/chat/__tests__/OrchestratedPlanCard.spec.ts`

**新建符号（前端）**
- `CodingPlanProvenance`（`web/src/types/chat.ts`，类型别名）
- `ProjectPlanToCodingResponse`（`web/src/types/chat.ts`，接口）
- `CodingPlanRuntime` 扩字段：`provenance` / `tech_plan` / `affected_files` / `recommended_repository_ids` / `source_artifact_version_id`
- `projectArtifactVersionToCodingPlan`（`web/src/api/chat.ts`）
- `projectPlanToCodingPlan`（`web/src/stores/chat.ts` action）
- `isOrchestrationTool`（`web/src/components/chat/ChatMessageBubble.vue`）
- `orchestratedPlanData`（同文件 computed）
- `COPY` 常量组（`OrchestratedPlanCard.vue`）承载：`技术方案已产出` / `已编排` / `已完成仓库路由、代码召回与并行调研，可直接进入编码执行。` / `进入编码` / `正在准备编码方案…` / `已进入编码，请在下方选择目标仓库` / `编码方案已就绪，请选择目标仓库` / `已复用既有编码方案` / `未能进入编码，请稍后重试`
- `TOOL_LABELS` 新条目：`start_plan_research: '方案编排调研'`、`start_feature_solution: '功能方案编排'`
- `TOOL_ICONS` 新条目：两者均 `icon-[lucide--workflow]`
- `toolAction` 新文案：`跨仓方案编排已完成` / `方案编排调研进行中` / `编排「{需求文本截断 32 字}」`
- `UNGROUPABLE_TOOLS` 新成员：`start_plan_research` / `start_feature_solution`

**新建端点 / 迁移 / 事件名**：无（复用 109-03 的投影端点）
</artifacts_produced>

<output>
Create `.planning/phases/109-spine-convergence/109-04-SUMMARY.md` when done
</output>
