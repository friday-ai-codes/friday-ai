---
phase: 109-spine-convergence
plan: 05
type: execute
wave: 5
depends_on: ["109-03", "109-04"]
files_modified:
  - server/agents/tools/coding_tools.py
  - server/chat/plan_projection_service.py
  - server/chat/views.py
  - server/chat/conversation_service.py
  - server/chat/config.py
  - server/agents/intent_router.py
  - server/agents/tools/repository_relevance.py
  - server/agents/tools/chat_tools.py
  - server/feishu/cards/bot_cards.py
  - server/tests/agents/test_coding_tools_schema_guard.py
  - server/tests/agents/_generate_contract_fixtures.py
  - server/tests/agents/fixtures/create_coding_plan_signature.json
  - server/tests/agents/fixtures/update_coding_plan_signature.json
  - server/tests/test_coding_tools.py
  - server/tests/test_plan_projection_service.py
  - server/tests/test_plan_projection_api.py
  - server/tests/test_conversation_service_fragment_extraction.py
  - server/tests/test_conversation_service_prompt_fragments.py
  - server/tests/test_project_context_line.py
  - server/tests/test_chat_tools.py
autonomous: true
requirements: [SPINE-02]
must_haves:
  truths:
    - "chat @tool create_coding_plan 的 schema 里不存在 tech_plan 与 affected_files 两个入参，模型在结构上无法只凭对话提交方案正文"
    - "chat @tool update_coding_plan 的 schema 里同样不存在这两个入参，模型无法改走 update 把任意正文写进既有方案"
    - "两个工具都必须携带来源方案版本（artifact_version_id 必填），无来源的创建/更新尝试被拒绝并留痕"
    - "create_coding_plan 的执行半边（推荐仓库解析、CodingPlan 落库、返回 payload 键集合）保持可用，SPA 执行流四步零回归"
    - "MCP 两条链（delegate 与执行链）一行未改、护栏全绿"
    - "凡挂载 create_coding_plan 的工具白名单也挂载编排工具，模型不会陷入『被要求带来源但拿不到来源』的死路"
    - "归属判定在投影 service 内部（aproject / arebind 均必填 actor_user_id），工具路径与 HTTP 端点共享同一道门：拿他人 artifact_version_id 投影或 re-bind 一律以 artifact_version_forbidden 拒绝，且不写入、不回显他人方案正文"
  artifacts:
    - path: "server/tests/agents/test_coding_tools_schema_guard.py"
      provides: "SPINE-02 正向不变量断言（创作入参不存在于 schema）"
      contains: "tech_plan"
    - path: "server/agents/tools/coding_tools.py"
      provides: "收窄后的两个 @tool schema + 经投影 service 落库 + 无来源拒绝留痕"
      contains: "artifact_version_id"
  key_links:
    - from: "server/agents/tools/coding_tools.py"
      to: "server/chat/plan_projection_service.py"
      via: "工具与 HTTP 端点共用同一投影 service（唯一写入口）"
      pattern: "PlanProjectionService"
    - from: "server/chat/plan_projection_service.py"
      to: "chat.models.Conversation.created_by_id"
      via: "aproject / arebind 内的归属判定（actor_user_id 不匹配即 artifact_version_forbidden），工具与端点共享同一道门"
      pattern: "artifact_version_forbidden"
    - from: "server/tests/agents/test_coding_tools_schema_guard.py"
      to: "agents.tools.registry.ToolRegistry.get_tool"
      via: "读 tool.parameters['properties'] 做正向不变量断言"
      pattern: "ToolRegistry"
---

<objective>
在 schema 层砍掉「由对话模型徒手编写方案正文」的创作半边（两个门一起收窄），保留执行半边，并补上让这条性质不可静默回退的正向不变量守护。

Purpose: SPINE-02 的字面要求是「系统不再存在由对话模型徒手编写方案正文的产出路径」。CONTEXT 明确「移除方式在 schema 层而非 prompt 层 —— 仅靠 prompt 约束不算达成」，并以裁决 D-1 把 `update_coding_plan` 一并纳入（否则模型可改走 update 写正文）、以裁决 D-2 要求补 schema 漂移守护（该工具此前零守护）。本 plan 处于 wave 5 —— 在 109-03 的投影 service 与 109-04 的前端入口都已成立之后，SPA 才不会短暂失去唯一编码入口（ROADMAP 顺序硬约束）。
Output: 两个收窄后的 `@tool`、`PlanProjectionService.arebind`、service 内归属判定（`actor_user_id` + `artifact_version_forbidden`）、无来源拒绝留痕事件、正向不变量守护测试、fixture 契约升级、11 处影响面的文案与断言同步。
</objective>

<execution_hint>
本 plan 的 `files_modified` 有 19 项（超出 15 的常规阈值），其中 Task 3 单 task 占 10 项。这是影响面清单的真实形状（8 处文案 + 4 处断言分散在 10 个文件），**不拆 plan**（拆开会让「两份白名单一致性」这条断言横跨两个 plan，反而更难验证）。

执行提示：若在 Task 3 中途出现质量衰减信号（对 prompt 文案的改动开始互相覆盖、或断言更新开始遗漏某个文件），把 Task 3 拆成两次提交而非两个 plan：
1. 第一次提交：8 处 prompt / 文案改动（`conversation_service` / `config.py` / `intent_router` / `repository_relevance` / `chat_tools` / `bot_cards`）；
2. 第二次提交：4 处断言升级（三个 prompt 片段测试 + `test_chat_tools.py` 的一致性断言）。

两次提交的验收合并为 Task 3 原有的 `<automated>`，不改变 wave 划分与 plan 边界。
</execution_hint>

<execution_context>
@.cursor/gsd-core/workflows/execute-plan.md
@.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/109-spine-convergence/109-CONTEXT.md
@.planning/phases/109-spine-convergence/109-RESEARCH.md
@.planning/phases/109-spine-convergence/109-VALIDATION.md
@.planning/phases/109-spine-convergence/109-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 两个门一起收窄 —— create/update_coding_plan schema 与实现改为「只接受编排来源」</name>
  <files>server/agents/tools/coding_tools.py, server/chat/plan_projection_service.py, server/chat/views.py</files>
  <read_first>
    - `.planning/phases/109-spine-convergence/109-CONTEXT.md` 裁决 D-1（两个门一起收窄，含理由）、§移除徒手创作路径全节（拆分而非删除；研究纠正 C-1 明确靶子是 chat `@tool`）
    - `.planning/phases/109-spine-convergence/109-RESEARCH.md` §1.2「实体 A 逐项解剖」（5 个入参的创作/执行归属表、内部 6 件事、返回 10 键）、§1.1（两个同名实体的区分表）、§8.1「改动本体（极小）」、§Code Examples「SPINE-02 收窄后的工具 schema（示意）」、§Common Pitfalls Pitfall 1（不要碰 `mcp_tools/`）与 Pitfall 5（顺序）
    - `.planning/phases/109-spine-convergence/109-RESEARCH.md` §Observability（`coding_plan_authoring_attempt_rejected` 事件定义：`category="caller"`、`component="agents"`、字段 `conversation_id` / `reason`）
    - `.planning/phases/109-spine-convergence/109-RESEARCH.md` §State of the Art「Deprecated / 不要碰」（`session_id` / `coding_session_id` 恒 None 的兼容 alias；`update_coding_plan` 的 `session_id` 入参已 deprecated）
    - `server/agents/tools/coding_tools.py`（全文 445 行：两个 `@tool` 的 `parameters` dict、函数签名、`_normalize_affected_files`、返回 payload）
    - `server/chat/plan_projection_service.py`（109-03 产出：`map_merged_plan_to_coding_plan` / `PlanProjectionService.aproject` / `PlanProjectionError`）
    - `.planning/phases/109-spine-convergence/109-VALIDATION.md` §Explicit Scope Boundaries 第 6 条（不整体删除，执行半边保持可用）
  </read_first>
  <action>
    **A. `create_coding_plan` 收窄（`coding_tools.py:35-292`）**
    - `parameters.properties`：**删除** `tech_plan` 与 `affected_files` 两个 property；**新增** `artifact_version_id`（`type: string`，description 写明「编排产出的方案版本 UUID（`start_plan_research` / `start_feature_solution` 返回值）」）。
    - `parameters.required`：改为 `["space_id", "conversation_id", "artifact_version_id"]`。
    - **保留执行半边入参**：`repository_id`（可选）与 `recommended_repository_ids`（可选）原样保留 —— 它们属执行半边（109-RESEARCH §1.2 归属表），SPINE-02 只砍创作半边。
    - 函数签名：删 `tech_plan` / `affected_files`，加 `artifact_version_id: str`（位置紧随 `conversation_id`）。
    - `description` 改写为：「把**已由编排链路产出**的技术方案版本投影为可执行的编码方案。本工具不接受方案正文 —— 正文只能来自完整编排链路（`start_plan_research` / `start_feature_solution` / 工作流方案节点）产出的方案版本。若尚无方案版本，先调 `start_plan_research` 发起编排。」
    - 函数体：
      1. 保留既有的 `Space` / `repository_id` / `Conversation` 三段校验（执行半边）。
      2. **落库改走投影 service**：调 `PlanProjectionService().aproject(artifact_version_id=artifact_version_id, initiated_by_user_id=...)` 拿 `(plan, created)`。**不再**调 `CodingPlan.aget_or_create_for_conversation`、**不再**调 `_normalize_affected_files`（工具已无 files 入参）。工具与 HTTP 端点共用同一 service = 唯一写入口。
      3. 保留 `recommended_repository_ids` 的四种来源解析（explicit / trace_inferred / empty / primary_repo）与「覆盖写入 plan」的既有行为 —— 若解析结果非空则覆盖投影写入的值，为空则保留投影从 `execution_plan[].repository_id` 聚合的值（不要用空列表清空投影结果；在代码里写清这条取舍）。
      4. `artifact_version_id` 缺失/非法/来源不存在（`PlanProjectionError`）→ 返回 `ToolResult(success=False, error=...)`，并打**留痕**事件 `coding_plan_authoring_attempt_rejected`（`category="caller"`、`component="agents"`、字段 `conversation_id` + `reason`），满足 CONTEXT「schema 层移除后的调用尝试需留痕」。异常文本入日志前过 `redact_secrets_in_text`。
      5. **返回 payload 的 10 个键一个不少**（`coding_plan_id` / `coding_session_id`(None) / `session_id`(None) / `repository_id` / `repository_name` / `status`("plan_only") / `branch_name`("") / `recommended_repository_ids` / `recommended_repositories` / `recommended_source` / `message`）—— 前端 `codingPlanData` 与历史消息解析依赖这套键形，删键会静默降级。
    - `_normalize_affected_files` 是否保留：`update_coding_plan` 收窄后也不再需要它；若全仓无其它调用方则**删除该函数**，否则保留并在 docstring 标注唯一剩余调用方。执行前用 `rg -n "_normalize_affected_files" server` 确认。

    **B. `update_coding_plan` 收窄（`coding_tools.py:295-444`，裁决 D-1）**
    - `parameters.properties`：**删除** `tech_plan` 与 `affected_files`；**新增** `artifact_version_id`。`coding_plan_id` / `session_id` 二选一的既有语义与 handler 内校验保留（`session_id` 仍标 deprecated）。
    - `parameters.required`：改为 `["artifact_version_id"]`（plan 定位仍在 handler 内二选一校验）。
    - 语义改为「把既有编码方案重新指向一个**新的**编排方案版本」，即 re-bind 而非任意改写。
    - 在 `server/chat/plan_projection_service.py` 新增方法：
      ```
      async def arebind(self, *, plan: CodingPlan, artifact_version_id: str, actor_user_id: str) -> CodingPlan
      ```
      实现：解析 `ArtifactVersion` → **归属判定（见下方 C 节，必做且在渲染正文之前）** → `map_merged_plan_to_coding_plan(content)` → 写 `tech_plan` / `affected_files` / `recommended_repository_ids` / `provenance=ORCHESTRATED` / `source_artifact_version_id`。
      - **唯一约束冲突处理**：先做一次显式前置查询 —— 若 `CodingPlan.objects.filter(source_artifact_version_id=str(av.id)).exclude(pk=plan.pk)` 存在，直接抛 `PlanProjectionError(code="artifact_version_already_projected")`；写入时再用 `except IntegrityError` 兜住并发窗口、抛同一机器码（fail-closed，不静默改写他人投影）。两层都要，理由是 `IntegrityError` 兜底的前提是 DB 约束真的存在 —— 109-02 已把该约束改为**无条件**唯一（带 `condition` 会在 MySQL 上被 `_unique_supported()` 静默跳过，届时 `IntegrityError` 永不抛出、`arebind` 会静默改写他人投影），前置查询是不依赖后端能力的那一层。
      - 复用既有 `plan.aupdate_plan(...)` 完成 `tech_plan` / `affected_files` 的原子更新与知识库重摄取；其余三个字段用一次 `asave(update_fields=[...])`。
      - 观测：沿用 `plan_projection_*` 事件族（`arebind` 打 `plan_projection_started` / `completed`，`completed` 带 `duration_ms` 与 `rebound=True` 以区分新建）。

    - handler 保留既有的「同步 draft session 的 deprecated 兼容字段」逻辑与 `synced_sessions_count` 返回键（执行半边行为不变）。

    **C. 归属判定下移进 service（工具与端点共享同一道门）**
    实证问题：109-03 把 owner gate 放在**视图**里（`plan.conversation.created_by_id != user.id → 404`），`PlanProjectionService.aproject` 自身**不含**归属判定；本 task 让工具成为同一 service 的第二个调用方，而 `coding_tools.py:145-185` 现有的三段校验只查存在性。若不补判定，`create_coding_plan` 可把 plan 建到他人会话下并回吐他人仓库元数据；`arebind` 更严重 —— 把任意 `artifact_version_id` 的 `content` 渲染成 `tech_plan` 写进调用方自己的 plan，等于**跨会话读取他人完整技术方案正文**。
    - 给 `PlanProjectionService.aproject` 增加**必填**关键字参数 `actor_user_id: str`，替代 109-03 落地的 `initiated_by_user_id: str = "system"`。必填是关键：带默认值的参数会让任何漏传的调用方静默获得 `"system"` 身份从而绕过判定 —— 这正是本 blocker 的成因形状。`arebind` 同样必填。
      观测侧不变：`plan_projection_*` 事件的 kv 键名仍是 `initiated_by_user_id`（取值来自 `actor_user_id`），以符合 `.cursor/rules/observability-logging.mdc` 的「后台任务必须显式携带 `initiated_by_user_id`」；无触发用户的系统调用方显式传 `actor_user_id="system"`，而**不得**靠默认值获得该身份。
    - 两个方法在解析出 `conversation` 之后、在渲染/写入任何正文之前，比较 `conversation.created_by_id`（用 `*_id` 标量，不裸访问 lazy FK）与 `actor_user_id`；不匹配 → 抛 `PlanProjectionError(code="artifact_version_forbidden")`。措辞与「不存在」一致，不泄漏存在性。
    - `arebind` 额外判定被改写的 `plan` 自身归属：`plan.conversation.created_by_id != actor_user_id` 同样抛 `artifact_version_forbidden`。
    - **同步改 109-03 落地的调用方**：`CodingPlanProjectFromArtifactVersionView` 改传 `actor_user_id=str(request.user.id)`；视图侧既有的 owner gate **保留**（纵深，不是替代），并在视图注释里写明「真正的门在 service，这里是第二道」。`artifact_version_forbidden` 在视图侧映射为 **404**（与 `artifact_version_not_found` 同形同措辞）。
    - 工具侧两个 handler 传 `actor_user_id`，取当前会话的发起用户（由 chat_runner 注入的用户上下文）；取不到时**不得**退化为哨兵值放行，而是直接返回 `ToolResult(success=False)` 并打 `coding_plan_authoring_attempt_rejected`。
    - 该判定必须写在 service 内而非各调用方 —— 判定留在调用方就意味着每个新调用方都要重新实现一遍，而工具路径已经漏了一次。

    **D. 绝对不碰的边界**（Pitfall 1）
    - 不改 `server/mcp_tools/` 下任何文件。任务的 files 清单里出现 `server/mcp_tools/views.py` 或 `serializers.py` 即为越界（那是实体 B，早在 Phase 94 已 delegate 到统一编排，动它会撞 `test_schema_snapshot.py` 与 `test_mcp_package_alignment.py` 两道守护）。
  </action>
  <acceptance_criteria>
    - `agents.tools.registry.ToolRegistry.get_tool("create_coding_plan").parameters["properties"]` 中**不含** `tech_plan` 与 `affected_files`，**含** `artifact_version_id`；`required` 恰为 `["space_id", "conversation_id", "artifact_version_id"]`。
    - `ToolRegistry.get_tool("update_coding_plan").parameters["properties"]` 中同样不含这两个键、含 `artifact_version_id`，且 `required` 含 `artifact_version_id`。
    - `inspect.signature` 下两个函数的参数表均不含 `tech_plan` / `affected_files`。
    - `coding_tools.py` 中出现 `PlanProjectionService`，且**不再**出现 `aget_or_create_for_conversation`。
    - `coding_tools.py` 中出现事件名 `coding_plan_authoring_attempt_rejected`，其 logger 调用带 `category="caller"` 与 `component="agents"`。
    - `create_coding_plan` 返回 payload 的键集合与收窄前完全一致（10 键，可用一条对键集合的 snapshot 断言锁住）。
    - `plan_projection_service.py` 含 `async def arebind`，且含机器码 `artifact_version_already_projected`。
    - `plan_projection_service.py` 中 `aproject` 与 `arebind` 的签名**均含 `actor_user_id` 且该参数无默认值**（`inspect.signature` 下 `default is inspect.Parameter.empty`）；文件中含机器码 `artifact_version_forbidden`，且该判定出现在渲染 / 写入正文之前（归属判定在 service 内，不只在视图内）。
    - `server/chat/views.py` 的 `CodingPlanProjectFromArtifactVersionView` 传 `actor_user_id=`，且 `artifact_version_forbidden` 被映射为 404（与 `artifact_version_not_found` 同形）。
    - `git diff --name-only` 的结果中**不含**任何 `server/mcp_tools/` 路径。
    - `cd server && uv run pytest tests/mcp_tools/ -x` 全绿（MCP 两条链零回归）。
    - **预期红（不是回归）**：本 task 收窄 schema 后，109-01 Task 3 建立的两份签名 fixture（`create_coding_plan_signature.json` / `update_coding_plan_signature.json`）与 `tests/test_coding_tools.py` 的 8 个旧签名用例**必然变红** —— 这正是「契约升级 = 一次显式提交」的既有工作流所要求的可见性，由 **Task 2** 完成 fixture 再生成与用例重写。🔴 **不得为让它们变绿而回退 schema**（把 `tech_plan` / `affected_files` 加回入参），也不得删除这两份 fixture 或跳过这些用例。本 task 的 `<automated>` 刻意不含 `tests/agents/test_tool_contracts.py` 与 `tests/test_coding_tools.py`，红窗在 Task 2 收口。
  </acceptance_criteria>
  <verify>
    <automated>cd server && uv run pytest tests/mcp_tools/ tests/test_plan_projection_service.py tests/test_plan_projection_api.py -x</automated>
  </verify>
  <done>两个门都在 schema 层收窄，落库统一走投影 service，无来源尝试被拒绝并留痕；执行半边返回键形与 MCP 两条链零回归。</done>
</task>

<task type="auto">
  <name>Task 2: SPINE-02 正向不变量守护 + 签名 fixture 契约升级 + 工具单测重写</name>
  <files>server/tests/agents/test_coding_tools_schema_guard.py, server/tests/agents/_generate_contract_fixtures.py, server/tests/agents/fixtures/create_coding_plan_signature.json, server/tests/agents/fixtures/update_coding_plan_signature.json, server/tests/test_coding_tools.py, server/tests/test_plan_projection_service.py, server/tests/test_plan_projection_api.py</files>
  <read_first>
    - `.planning/phases/109-spine-convergence/109-CONTEXT.md` 裁决 D-2（补 schema 漂移守护 —— 正向不变量断言：创作入参不存在于 schema）
    - `.planning/phases/109-spine-convergence/109-RESEARCH.md` §8.3「schema 漂移守护：现状与缺口」与 §Code Examples「SPINE-02 的正向守护断言」、§8.2 影响面清单第 7 行（`test_coding_tools.py` 8 个用例全传旧入参 ⇒ 全红需重写）
    - `.planning/phases/109-spine-convergence/109-VALIDATION.md` §Wave 0 Requirements 第 2 条
    - `.planning/phases/109-spine-convergence/109-01-SUMMARY.md`（wave 1 已 baseline 的两份签名 fixture 与 `_REGENERATE_HINT` 流程）
    - `server/tests/agents/test_start_plan_research_tool.py:209-224`（`ToolRegistry.get_tool(...).parameters["properties"]` 的既有读法先例）
    - `server/tests/test_coding_tools.py`（`TestCreateCodingPlan` 8 个用例与 `test_chat_runner_get_tool_names_gates_deep_analysis` 等白名单断言）
  </read_first>
  <action>
    **A. 正向不变量守护（新文件）**
    新建 `server/tests/agents/test_coding_tools_schema_guard.py`，用 `ToolRegistry.get_tool(...)` 读 schema，断言「结构上不可能徒手编方案」：
    - `create_coding_plan`：`"tech_plan" not in props`、`"affected_files" not in props`、`"artifact_version_id" in required`。
    - `update_coding_plan`：同两条否定断言 + `"artifact_version_id" in required`。
    - 失败消息写明「SPINE-02 回退：方案正文入参被加回」，便于后人一眼看懂这条测试在守什么。
    - 文件 docstring 写明：这条断言锁的是**结构上不可能**，而非 prompt 里不建议；未来任何把 `tech_plan` / `affected_files` 加回入参的改动都会在此变红。
    - 追加一条**枚举式**断言：遍历两个工具的 `properties` 键集合，断言其与一个显式白名单集合完全相等（`create`: `{space_id, conversation_id, artifact_version_id, repository_id, recommended_repository_ids}`；`update`: `{coding_plan_id, session_id, artifact_version_id}`）。这让「悄悄加一个新的正文别名入参」也会变红，而不是只防住这两个具体名字。

    **B. 签名 fixture 契约升级**
    - 从 `server/` 运行 `DJANGO_SETTINGS_MODULE=friday.settings uv run python -m tests.agents._generate_contract_fixtures` 重新生成两份 fixture（`_generate_contract_fixtures.py` 在 109-01 已加好写出分支，本 task 通常无需再改它；若签名新增参数需要调整则一并改）。
    - `git diff server/tests/agents/fixtures/` 必须显示 `tech_plan` / `affected_files` 从两份 fixture 中消失、`artifact_version_id` 出现 —— 这就是「契约升级 = 一次显式提交」的 review 证据，请在 SUMMARY 里粘贴该 diff 摘要。

    **C. 工具单测重写**
    - 重写 `server/tests/test_coding_tools.py` 中 `TestCreateCodingPlan` 的用例，全部改用新签名（造一个 `ArtifactVersion` 作为来源，mock 或真实走 `PlanProjectionService`）。逐条保留原有覆盖意图：
      - 产 `CodingPlan` 但**不产** `CodingSession`（调用前后 `CodingSession` 计数不变）；
      - `space_id` / `repository_id` 不存在或不属该 space → 失败；
      - `conversation_id` 不存在 → 失败；
      - `recommended_repository_ids` 四种来源（explicit / trace_inferred / empty / primary_repo）；
      - 返回 payload 的 dual-id 兼容键（`coding_session_id` / `session_id` 恒 `None`）。
    - **新增**用例（`-k reject` 可选中）：`artifact_version_id` 为空串 / 指向不存在的 UUID / 来源 `content` 非 dict → `ToolResult.success is False`，且 `coding_plan_authoring_attempt_rejected` 被记录（用 `structlog` 的 capture 或 caplog 断言事件名出现）。
    - 为 `update_coding_plan` 补用例：re-bind 成功后 `plan.tech_plan` 变成新版本渲染结果、`source_artifact_version_id` 指向新版本、`provenance == "orchestrated"`；以及「目标版本已被另一条 plan 占用」→ 失败且不改写任何一方（机器码 `artifact_version_already_projected`）。
    - 保留该文件内既有的白名单断言用例（`create_coding_plan` / `update_coding_plan` 仍在 `_INDEXED_TOOL_NAMES`，工具**不删除**，只收窄）。

    **D. 跨会话拒绝用例（Task 1 C 节归属判定的验收）**
    归属判定下移进 service 后，必须有用例证明「工具路径与端点路径共享同一道门」，否则 Task 1 C 节会退化为无人验证的声明。
    - `server/tests/test_plan_projection_service.py` 追加（`-k forbidden` 可选中）：
      - 造用户 A 的 `Conversation` + `ArtifactVersion`，以 `actor_user_id=<用户 B>` 调 `aproject` → 抛 `PlanProjectionError` 且 `code == "artifact_version_forbidden"`，且 `CodingPlan.objects.count()` 未增加（拒绝前零写入）。
      - 同形的 `arebind` 用例：用户 B 拿自己的 plan 去 re-bind 用户 A 的 `artifact_version_id` → 同一机器码，且**断言用户 B 的 `plan.tech_plan` 未被写入用户 A 的方案正文**（这条断言是「跨会话读取他人技术方案正文」这个漏洞的直接锁）。
      - `arebind` 的第二条：用户 B 用自己的 `artifact_version_id` 去改写**用户 A 的 plan** → 同一机器码。
    - `server/tests/test_plan_projection_api.py` 追加一条：非 owner 打投影端点 → **404**（与 `artifact_version_not_found` 同形同措辞，不泄漏存在性），并断言响应体不含方案正文。
    - `server/tests/test_coding_tools.py` 追加一条（并入 `-k reject` 组）：工具以 A 会话的 `artifact_version_id` 但 B 的用户上下文调用 → `ToolResult.success is False`，返回的 `error` 不回显他人方案正文的任何片段。
  </action>
  <acceptance_criteria>
    - `server/tests/agents/test_coding_tools_schema_guard.py` 存在，含针对两个工具的 `tech_plan` / `affected_files` 否定断言各一对，以及两组 `properties` 键集合的枚举式相等断言。
    - `server/tests/agents/fixtures/create_coding_plan_signature.json` 与 `update_coding_plan_signature.json` 中均**不含** `tech_plan` / `affected_files` 顶层键，且 `create_coding_plan_signature.json` 含 `artifact_version_id`。
    - `cd server && uv run pytest tests/agents/test_coding_tools_schema_guard.py tests/agents/test_tool_contracts.py -x` 全绿。
    - `cd server && uv run pytest tests/test_coding_tools.py -x` 全绿，且 `-k reject` 能选中至少 3 个无来源拒绝用例。
    - `cd server && uv run pytest tests/test_coding_tools.py -k "update" -x` 覆盖 re-bind 成功与版本已占用两种情形。
    - `cd server && uv run pytest tests/test_plan_projection_service.py -k forbidden -x` 能选中 ≥3 个用例（`aproject` 跨会话 / `arebind` 跨会话读正文 / `arebind` 改写他人 plan），其中含「用户 B 的 `plan.tech_plan` 未被写入用户 A 的正文」这条断言。
    - `cd server && uv run pytest tests/test_plan_projection_api.py -x` 全绿，且含一条非 owner → 404 且响应体不含方案正文的用例。
    - Task 1 的预期红窗在本 task 收口：`cd server && uv run pytest tests/agents/test_tool_contracts.py tests/test_coding_tools.py -x` 在本 task 完成后全绿，且 fixture 的变更是**删除** `tech_plan` / `affected_files`、**新增** `artifact_version_id`（不是把入参加回）。
  </acceptance_criteria>
  <verify>
    <automated>cd server && uv run pytest tests/agents/test_coding_tools_schema_guard.py tests/agents/test_tool_contracts.py tests/test_coding_tools.py tests/test_plan_projection_service.py tests/test_plan_projection_api.py -x</automated>
  </verify>
  <done>SPINE-02 的「结构上不可能」有正向不变量 + 键集合枚举双重守护；签名 fixture 完成契约升级并留下 review diff；工具单测按新签名重写；归属判定有跨会话拒绝用例。</done>
</task>

<task type="auto">
  <name>Task 3: 11 处影响面同步 —— prompt / 两份工具白名单 / 文案 / 断言测试</name>
  <files>server/chat/conversation_service.py, server/chat/config.py, server/agents/intent_router.py, server/agents/tools/repository_relevance.py, server/agents/tools/chat_tools.py, server/feishu/cards/bot_cards.py, server/tests/test_conversation_service_fragment_extraction.py, server/tests/test_conversation_service_prompt_fragments.py, server/tests/test_project_context_line.py, server/tests/test_chat_tools.py</files>
  <read_first>
    - `.planning/phases/109-spine-convergence/109-RESEARCH.md` §1.3「实体 A 的调用方全量清单」（11 行，逐行给出位置与处置）、§8.2「影响面清单（11 类）」（标出哪些会红）、§Common Pitfalls Pitfall 7（忘了第二份工具白名单）、§Assumptions Log A6（两份白名单是否都活，建议执行时先确认）
    - `.planning/phases/109-spine-convergence/109-CONTEXT.md` §移除徒手创作路径（移除方式在 schema 层而非 prompt 层 —— prompt 只是配套文案，不是达成手段）
    - `server/chat/conversation_service.py:216-247`（`_CODING_GUIDANCE`，9 处提名）、`:405-410`（编码请求硬 gate hint）、`:414-442`（`_get_tool_names` 的 `full_tools`）
    - `server/agents/chat_runner.py:80-126`（`_INDEXED_TOOL_NAMES`，含 `start_plan_research` / `start_feature_solution`）
    - `server/chat/config.py:73`（项目上下文行）、`server/agents/intent_router.py:188`、`server/agents/tools/repository_relevance.py:50`、`server/agents/tools/chat_tools.py:930`、`server/feishu/cards/bot_cards.py:15`
    - `server/tests/test_conversation_service_fragment_extraction.py:72-73`、`server/tests/test_conversation_service_prompt_fragments.py:102,173`、`server/tests/test_project_context_line.py:23`、`server/tests/test_chat_tools.py:383-420`
  </read_first>
  <action>
    **A. 两份工具白名单的一致性（Pitfall 7 + A6）**
    先 `rg -n "_get_tool_names" server --glob '!server/tests/**'` 确认哪条路径在跑（研究结论：`agents/chat_runner._get_tool_names` 是活路径，`chat/conversation_service._get_tool_names` 在生产代码中无调用方）。无论哪条活：
    - 🔴 **给 `chat/conversation_service._get_tool_names` 的 `full_tools` 补上 `"start_plan_research"` 与 `"start_feature_solution"`**。理由是一条真实的死路风险：收窄后 `create_coding_plan` 必须携带 `artifact_version_id`，而该清单目前**只挂 create/update、不挂任何编排工具** ⇒ 若这条路径在任何场景下生效，模型会被要求带来源却没有任何工具能产出来源。
    - 新增一条一致性断言测试（放 `server/tests/test_chat_tools.py`）：**凡挂载 `create_coding_plan` 的工具清单，必须同时挂载 `start_plan_research` 与 `start_feature_solution`**。对 `chat_runner._INDEXED_TOOL_NAMES` 与 `conversation_service._get_tool_names` 的返回值各断言一次。这条断言把「两份清单不同步」从靠人记变成靠测试守。
    - 同步更新 `test_chat_tools.py::TestGetToolNames` 中「返回 N 个工具」这类硬编码计数断言（清单变长会红），改为「包含关系」断言或更新计数并在注释里写明变更原因。

    **B. prompt 与文案（8 处，均为文案，不构成 SPINE-02 的达成手段）**
    - `conversation_service._CODING_GUIDANCE`（9 处提名）：把「调用 `create_coding_plan` 工具生成结构化技术方案」改写为「先经编排链路产出方案版本（`start_plan_research` / `start_feature_solution`），再调 `create_coding_plan` 把该版本投影为编码方案」；把「技术方案包含：① 影响文件列表 ② 分步实现步骤」这类**教模型写正文**的表述整段删除或改为「方案正文由编排链路产出，模型不撰写正文」；`update_coding_plan` 的表述改为「重新指向新的方案版本」。
    - `conversation_service:405-410` 的 per-turn hint：保留「必须先 `analyze_repository_relevance`」的前置约束，追加「必须先有编排产出的方案版本才可调 `create_coding_plan`」。
    - `chat/config.py:73`、`agents/intent_router.py:188`、`agents/tools/repository_relevance.py:50`、`agents/tools/chat_tools.py:930`：四处描述文案按同一口径更新（`create_coding_plan` 是「投影」而非「生成方案」）。
    - `feishu/cards/bot_cards.py:15`：工具中文名 `"📝 生成编码方案"` 改为 `"📝 进入编码方案"`（与前端「进入编码」口径一致）。

    **C. 会红的断言测试（4 处，属预期的契约升级）**
    - `test_conversation_service_fragment_extraction.py:72-73`：更新对 `_CODING_GUIDANCE` 内容的断言。
    - `test_conversation_service_prompt_fragments.py:102,173`：更新逐字文案断言；新增一条断言 `_CODING_GUIDANCE` 中**不再**出现「教模型撰写方案正文」的表述关键词（如 `分步实现步骤`），把「prompt 不再诱导徒手撰写」也纳入回归守护。
    - `test_project_context_line.py:23`：随 `config.py:73` 文案更新断言。
    - 上述断言更新一律**改为与新文案一致**，不得为了让测试变绿而回退文案。

    **D. 明确不动的面**
    - `mcp/src/tools.ts`、`mcp/README.md`、`docs/integrations/mcp.md` 指的是**实体 B**（MCP HTTP 端点），一行不改（改了会撞 `test_mcp_package_alignment.py` 并需重新发 npm 包）。
    - `skills/skills/friday-code/SKILL.md` 与 `task/assets/skills/friday-code/SKILL.md` 是两份需保持同步的副本：先 `rg -n "create_coding_plan" skills task docs mcp` 逐处核对指 A 还是 B，**只改指 A 的**，且两份副本必须同改。若两份均只提实体 B，则本 plan 不改文档并在 SUMMARY 中如实记录该核对结论。
  </action>
  <acceptance_criteria>
    - `server/chat/conversation_service.py` 的 `_get_tool_names.full_tools` 列表字面量中出现 `"start_plan_research"` 与 `"start_feature_solution"`。
    - `server/tests/test_chat_tools.py` 中存在一条一致性断言：对 `chat_runner._INDEXED_TOOL_NAMES` 与 `conversation_service._get_tool_names` 的结果各断言「含 `create_coding_plan` ⇒ 同时含两个编排工具」。
    - `_CODING_GUIDANCE` 中出现 `start_plan_research` 字样，且**不再**出现 `分步实现步骤`（用 `rg` 校验时对 `.py` 过滤注释行：`rg -n '分步实现步骤' server/chat/conversation_service.py | rg -v '^[0-9]+:[[:space:]]*#'` 无匹配）。
    - `server/feishu/cards/bot_cards.py` 中 `create_coding_plan` 的中文标签不再是 `"📝 生成编码方案"`。
    - `git diff --name-only` 中不含 `mcp/` 与 `server/mcp_tools/` 下任何路径。
    - `cd server && uv run pytest tests/test_conversation_service_fragment_extraction.py tests/test_conversation_service_prompt_fragments.py tests/test_project_context_line.py tests/test_chat_tools.py -x` 全绿。
  </acceptance_criteria>
  <verify>
    <automated>cd server && uv run pytest tests/test_conversation_service_fragment_extraction.py tests/test_conversation_service_prompt_fragments.py tests/test_project_context_line.py tests/test_chat_tools.py -x</automated>
  </verify>
  <done>两份白名单一致且有断言守护；prompt 与 8 处文案按「投影而非撰写」口径更新；4 处会红的断言完成契约升级；MCP 侧与 npm 包一行未动。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 对话 LLM ↔ 工具 schema | schema 是 LLM 可见能力的唯一定义处；能力边界必须在此收紧，prompt 只是软约束 |
| chat `@tool`（实体 A）↔ MCP HTTP 端点（实体 B） | 同名不同物；越界改动会撞两道既有守护并需重新发 npm 包 |
| 未受信 `artifact_version_id` ↔ 投影 service | 工具入参来自 LLM，可能为空/伪造/指向他人版本 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-109-05-01 | Spoofing | `create_coding_plan` / `update_coding_plan` schema | mitigate | 删除 `tech_plan` / `affected_files` 两个入参，模型结构上无法冒充方案作者；正向不变量 + 键集合枚举断言防回退 |
| T-109-05-02 | Repudiation | 无来源的创建尝试 | mitigate | `coding_plan_authoring_attempt_rejected` 事件留痕（`category="caller"`、`component="agents"`，带 `conversation_id` / `reason`） |
| T-109-05-03 | Tampering | `update_coding_plan` re-bind | mitigate | 目标版本已被另一条 plan 占用时 fail-closed（`artifact_version_already_projected`），不静默改写他人投影 |
| T-109-05-04 | Elevation of Privilege | 工具经 LLM 投影 / re-bind 他人方案版本 | mitigate | 归属判定**下移进 service**（Task 1 C 节）：`aproject` / `arebind` 均必填 `actor_user_id`，解析出的 `conversation.created_by_id` 不匹配即抛 `artifact_version_forbidden`，判定在渲染正文之前 ⇒ 工具与端点共享同一道门。视图侧 owner gate 保留为纵深。<br>⚠️ 不可依赖的旧缓解（已订正）：投影 service **刻意忽略**工具入参的 `conversation_id`（109-03 的端点甚至故意不收该字段以防 IDOR），因此「`conversation_id` 由 chat_runner 注入」提供不了任何归属保护 |
| T-109-05-07 | Information Disclosure | `arebind` 跨会话读取他人方案正文 | mitigate | `arebind` 在渲染 `tech_plan` **之前**做归属判定；Task 2 D 节有「用户 B 的 `plan.tech_plan` 未被写入用户 A 正文」的直接断言；错误 `error` 文本不回显他人正文片段 |
| T-109-05-05 | Denial of Service | 模型陷入「要带来源但拿不到来源」 | mitigate | 两份工具白名单补齐编排工具 + 一致性断言测试（Task 3 A） |
| T-109-05-06 | Information Disclosure | 拒绝路径日志 | mitigate | `reason` 来自异常文本时过 `redact_secrets_in_text` |
| T-109-05-SC | Tampering | npm/pip/cargo installs | accept | 本 plan 零新增依赖；显式不改 `mcp/` npm 包 ⇒ 无需重新发包 |
</threat_model>

<verification>
- `cd server && uv run pytest tests/agents/ tests/test_coding_tools.py tests/test_conversation_service_fragment_extraction.py tests/test_conversation_service_prompt_fragments.py tests/test_project_context_line.py tests/test_chat_tools.py -x` 全绿。
- `cd server && uv run pytest tests/mcp_tools/ -x` 全绿（含 6 组 delegate 守护、执行链 e2e、schema snapshot、npm 包对齐）。
- `cd server && uv run pytest tests/test_spa_coding_chain_e2e.py tests/test_plan_projection_api.py tests/test_coding_plans_sessions_api.py tests/test_coding_plan_export_api.py -x` 全绿（执行半边四步零回归）。
- `git diff --name-only | rg 'server/mcp_tools|^mcp/'` 无输出。
- 观测自检：`coding_plan_authoring_attempt_rejected` 带 `category` / `component` / 触发用户可归因；异常文本已脱敏；未新增 `call_source`（工具本身不调 LLM）。
</verification>

<success_criteria>
- 两个工具的 schema 与函数签名均不存在创作入参，模型结构上无法徒手编写方案正文（SPINE-02 达成）。
- 执行半边完整保留：返回键形不变、推荐仓库解析不变、SPA 四步端到端护栏全绿。
- MCP 两条链一行未改、护栏全绿（零回归）。
- 无来源尝试被拒绝并留痕；正向不变量与键集合枚举双重守护防未来回退。
- 归属判定在 service 内，工具与端点共享同一道门：跨会话投影 / re-bind 一律 `artifact_version_forbidden`，且他人方案正文既不被写入也不被回显（有跨会话拒绝用例）。
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces（本 plan 部分）

**新建文件**
- `server/tests/agents/test_coding_tools_schema_guard.py`

**新建符号（后端）**
- `chat.plan_projection_service.PlanProjectionService.arebind`
- 机器码 `artifact_version_already_projected`（`PlanProjectionError.code` 新取值）
- 机器码 `artifact_version_forbidden`（`PlanProjectionError.code` 新取值；视图侧映射为 404）
- 工具入参 `artifact_version_id`（`create_coding_plan` 与 `update_coding_plan` 各一个，均必填）

**签名变更（109-03 产出的收紧）**
- `PlanProjectionService.aproject`：`initiated_by_user_id: str = "system"` → **必填** `actor_user_id: str`（观测 kv 键名仍为 `initiated_by_user_id`）

**新建观测事件名**
- `coding_plan_authoring_attempt_rejected`（`category="caller"`、`component="agents"`）

**移除的符号（SPINE-02 的实质交付）**
- `create_coding_plan` 入参 `tech_plan` / `affected_files`
- `update_coding_plan` 入参 `tech_plan` / `affected_files`
- `agents.tools.coding_tools._normalize_affected_files`（若全仓无其它调用方）

**文案常量变更**
- `feishu.cards.bot_cards` 中 `create_coding_plan` 的中文标签 → `📝 进入编码方案`
- `chat.conversation_service._CODING_GUIDANCE` / per-turn hint、`chat.config` 项目上下文行、`agents.intent_router`、`agents.tools.repository_relevance`、`agents.tools.chat_tools` 五处描述文案

**新建迁移 / 端点 / 前端符号**：无
</artifacts_produced>

<output>
Create `.planning/phases/109-spine-convergence/109-05-SUMMARY.md` when done
</output>
