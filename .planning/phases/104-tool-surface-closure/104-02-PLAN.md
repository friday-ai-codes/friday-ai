---
phase: 104-tool-surface-closure
plan: 02
type: execute
wave: 2
depends_on: [104-01]
files_modified:
  - server/mcp_tools/repository_analysis_service.py
  - server/mcp_tools/planning_service.py
  - server/mcp_tools/views.py
  - server/mcp_tools/orchestration_delegate.py
  - server/services/process_runtime/entrypoint.py
  - server/services/process_runtime/architect_merge_adapter.py
  - server/tests/mcp_tools/test_planning_tools.py
  - server/tests/mcp_tools/test_create_coding_plan_delegate.py
  - server/tests/services/test_process_runtime_extra_evidence.py
  - server/tests/mcp_tools/test_patch_target_guard.py
  - docs/workflows/ai-plan-generation-deprecation.md
autonomous: true
requirements: [UNIFY-02, UNIFY-03]

must_haves:
  truths:
    - "analyze_repository 的分析产物作为编排输入证据实际被消费：Create/Improve 带 analysis 时 summary 注入 delegate → stage_state → merge prompt，trace 事件可见 evidence 条数（不留无人消费的空转工具）"
    - "mcp_tools/planning_service.py 已删除；build_repository_analysis 随迁 mcp_tools/repository_analysis_service.py，analyze 响应契约不变，相关测试迁移不失覆盖"
    - "rg planning_service 全仓零残留引用（排除 .planning 与 .claude/worktrees）；services/plan_orchestration/ 空目录删除；docs 文案残留清理"
    - "受本次迁移影响的 mock.patch/monkeypatch target 字符串全部可 import（stale target 守卫测试，无假通过）"
  artifacts:
    - path: "server/mcp_tools/repository_analysis_service.py"
      provides: "确定性证据采集器随迁落位（build_repository_analysis + PlanningResult + normalize_context_chunks + 私有 helpers）"
      contains: "def build_repository_analysis"
      min_lines: 120
    - path: "server/services/process_runtime/entrypoint.py"
      provides: "start_orchestration 可选 extra_evidence 注入 stage_state decomposition"
      contains: "extra_evidence"
    - path: "server/tests/mcp_tools/test_patch_target_guard.py"
      provides: "importlib 逐段解析 patch target 字符串的防线测试"
      contains: "importlib"
  key_links:
    - from: "server/mcp_tools/views.py"
      to: "server/mcp_tools/repository_analysis_service.py"
      via: "AnalyzeRepositoryView import 随迁模块"
      pattern: "from \\.repository_analysis_service import"
    - from: "server/mcp_tools/orchestration_delegate.py"
      to: "server/services/process_runtime/entrypoint.py"
      via: "delegate extra_evidence 透传 start_orchestration"
      pattern: "extra_evidence"
    - from: "server/services/process_runtime/architect_merge_adapter.py"
      to: "stage_state decomposition.extra_evidence"
      via: "_build_prompt 纳入补充证据 + EVENT_PLAN_MERGE_STARTED 计数"
      pattern: "extra_evidence"
---

<objective>
完成 UNIFY-02 与 UNIFY-03：`analyze_repository` 的确定性证据采集器（`build_repository_analysis`）随迁独立模块并接通「编排消费」——`delegate_process_runtime` 增可选 `extra_evidence` 参数写入 `start_orchestration` 的 stage_state，merge 阶段 prompt 实际纳入、trace 事件可见条数；Create/Improve 带 analysis 时注入 `McpRepositoryAnalysis.summary`。随后删除 `planning_service.py`（build_coding_plan/improve_coding_plan 一并消亡）、删除 `services/plan_orchestration/` 空目录、清理 docs 残留，并新增 stale patch target 守卫测试。

Purpose: 消除"无人消费的空转工具"（ROADMAP 成功标准 2）+ 确定性缝与空壳残留清零（成功标准 3），P6 收口断裂的最后一段防线。
Output: repository_analysis_service.py 新模块、extra_evidence 全链接线、planning_service.py 与 plan_orchestration/ 删除、docs 更新、test_patch_target_guard.py 防线。
</objective>

<execution_context>
@/Users/zaneliu/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/104-tool-surface-closure/104-CONTEXT.md
@.planning/phases/104-tool-surface-closure/104-01-SUMMARY.md
@server/mcp_tools/planning_service.py
@server/mcp_tools/views.py
@server/mcp_tools/orchestration_delegate.py
@server/services/process_runtime/entrypoint.py
@server/services/process_runtime/architect_merge_adapter.py
@docs/workflows/ai-plan-generation-deprecation.md
</context>

<interfaces>
执行前先读，避免探路：

- `start_orchestration`（entrypoint.py L32–64）：stage_state 注入点是 `create_session(..., stage_state={"decomposition": {"requirement_text": ..., "include_repos": ...}})`——`extra_evidence` 作为 decomposition 下第三个键注入。
- `ArchitectMergeAdapter._build_prompt(session, partials)`（architect_merge_adapter.py L90–112）：从 `session.decomposition` 读 requirement_text 拼 prompt；`merge()` 内 `EVENT_PLAN_MERGE_STARTED` emit 在 L149（payload 现为 `{"partials": repo_ids}`）。
- `build_repository_analysis`（planning_service.py L143–218）依赖同文件符号：`PROMPT_VERSION`/`SYSTEM_PROMPT_VERSION`/`LOCAL_PROVIDER`/`LOCAL_MODEL`（L19–22）、`PlanningResult`（L25–29）、`_token_estimate`/`_usage`（L32–49）、`normalize_context_chunks`（L52–71）、`_module_groups`/`_entry_points`/`_test_paths`（L74–124）。`_files_from_requirement`（L127–140）仅被 build_coding_plan 使用——随删。
- `AnalyzeRepositoryView`（views.py L1745–1826）：L1772 调 build_repository_analysis；响应契约（analysis_id/repository_id/branch/analysis/evidence/run_id）不变。
- 104-01 已交付：`map_canonical_to_coding_plan` 在 orchestration_delegate.py；views 的 planning_service import 仅剩 `build_repository_analysis`；improve/create 测试均走 fake delegate。
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: build_repository_analysis 随迁新模块 + planning_service.py 删除</name>
  <files>server/mcp_tools/repository_analysis_service.py, server/mcp_tools/planning_service.py, server/mcp_tools/views.py</files>
  <action>
    per D（UNIFY-02 随迁 + 直删清单）：

    1. 新建 `server/mcp_tools/repository_analysis_service.py`：从 planning_service.py 整体搬移 `build_repository_analysis` 及其全部依赖符号——模块常量 L19–22、`PlanningResult` L25–29、`_token_estimate`/`_usage` L32–49、`normalize_context_chunks` L52–71（analyze 内部使用，随迁；per D「若仅存量内部使用则随 view 需要迁移」）、`_module_groups`/`_entry_points`/`_test_paths` L74–124、`build_repository_analysis` L143–218。模块 docstring 写明定位：**确定性证据采集器**（模块分组/入口/测试路径，非 LLM 生成），供 analyze_repository 采集编排输入证据。保留 `from repositories.models import Repository` import。
    2. `views.py`：`from .planning_service import build_repository_analysis` 改为 `from .repository_analysis_service import build_repository_analysis`（AnalyzeRepositoryView L1772 调用点不动，响应契约不变）。
    3. **删除 `server/mcp_tools/planning_service.py`**：此时文件内仅剩 `build_coding_plan`（DEPRECATED、无生产调用方，per D 直删）、`improve_coding_plan`（104-01 后无调用方，退役）、`_files_from_requirement`（仅 build_coding_plan 使用）——全部随文件消亡。删除前用 `rg -l "planning_service" --glob '!.planning/**' --glob '!.claude/**' .`（仓库根）比对 104-01 引用清单，确认除 docs（Task 3 处置）外无代码引用。
    4. `tests/mcp_tools/test_planning_tools.py` 的 analyze 用例（test_analyze_repository_persists_artifact_and_replayable_trace）不 import planning_service、走 HTTP 调确定性实现——随迁后应原样通过，不改断言（覆盖不丢失的证明）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/mcp_tools/test_planning_tools.py -x -q && test ! -f mcp_tools/planning_service.py && ! rg -q "planning_service" mcp_tools/ tests/</automated>
  </verify>
  <done>planning_service.py 不存在；analyze 测试原断言全绿（响应契约不变）；server 代码与测试目录零 planning_service 引用。</done>
</task>

<task type="auto">
  <name>Task 2: extra_evidence 编排消费接线（delegate → stage_state → merge prompt/trace）+ 视图注入</name>
  <files>server/mcp_tools/orchestration_delegate.py, server/services/process_runtime/entrypoint.py, server/services/process_runtime/architect_merge_adapter.py, server/mcp_tools/views.py, server/tests/services/test_process_runtime_extra_evidence.py, server/tests/mcp_tools/test_create_coding_plan_delegate.py</files>
  <action>
    per D（UNIFY-02 消除空转，stage_state 键名与 merge 拼装为 Claude's discretion，取以下定案）：

    1. `entrypoint.start_orchestration`（L32–64）：签名增 `extra_evidence: list[dict] | None = None`；仅当 truthy 时在 `stage_state["decomposition"]` 增键 `"extra_evidence": extra_evidence`（不提供则不写键——既有会话形态与其他入口零扰动）。docstring 补一句语义（调用方补充的编排输入证据，merge 阶段消费）。
    2. `delegate_process_runtime`（orchestration_delegate.py L119–173）：签名增 `extra_evidence: list[dict] | None = None`，原样透传 `start_orchestration(..., extra_evidence=extra_evidence)`；`mcp_plan_delegate_started` 事件 kv 增 `extra_evidence_count=len(extra_evidence or [])`（caller 类事件，观测规范达标）。
    3. `ArchitectMergeAdapter`（architect_merge_adapter.py）：`_build_prompt`（L90–112）读 `decomposition.get("extra_evidence") or []`，非空时在「各仓调研产物」段之后插入一段 `f"调用方补充证据（repository analysis 等）：\n{json.dumps(evidence, ensure_ascii=False)}\n\n"`；`merge()` 的 `EVENT_PLAN_MERGE_STARTED` emit（L149）payload 增 `"extra_evidence_count": len(...)`（trace 事件可见条数，per D）。
    4. `views.py` 注入：
       - `CreateCodingPlanView`：analysis 已加载（L1853–1865），delegate 调用（L1879–1884）增 `extra_evidence=[{"kind": "repository_analysis", "analysis_id": str(analysis.id), "summary": analysis.summary}] if analysis else None`——analysis_id 从"仅挂 FK"升级为真实证据输入。
       - `ImproveCodingPlanView`（104-01 收敛后的 delegate 调用）：`plan.analysis_id` 非空时 `analysis = await McpRepositoryAnalysis.objects.filter(id=plan.analysis_id).afirst()`，同型注入。
    5. 新测试 `server/tests/services/test_process_runtime_extra_evidence.py`（组织为 Claude's discretion，覆盖三层）：
       - `start_orchestration(extra_evidence=[...])` → session.decomposition（或 stage_state）含 extra_evidence；不传 → 无该键。
       - `ArchitectMergeAdapter._build_prompt`：构造带 extra_evidence 的 session stub → prompt 字符串含证据内容；无证据 → prompt 不含「补充证据」段（静态方法可直接单测，无需 DB）。
       - `delegate_process_runtime` kwargs 透传：monkeypatch `services.process_runtime.start_orchestration`（delegate 内函数级 import，patch 目标为 `services.process_runtime.start_orchestration`——写测试时先 importlib 验证 patch 路径与运行时解析一致，勿造新 stale target）捕获 extra_evidence 入参。
    6. `tests/mcp_tools/test_create_coding_plan_delegate.py` 增一例：POST create 带 analysis_id → fake delegate 捕获 kwargs，断言 `extra_evidence` 列表含该 analysis 的 summary（视图注入的端到端证明）；improve 侧同型断言并入 test_planning_tools 既有 fake delegate 用例（捕获 kwargs 即可）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/test_process_runtime_extra_evidence.py tests/mcp_tools/test_create_coding_plan_delegate.py tests/mcp_tools/test_planning_tools.py -x -q</automated>
  </verify>
  <done>analysis summary 从视图注入 → decomposition.extra_evidence → merge prompt 实际消费，EVENT_PLAN_MERGE_STARTED trace 可见条数；三层测试 + 视图端到端注入断言全绿；不带 analysis 的调用零行为变化。</done>
</task>

<task type="auto">
  <name>Task 3: 残留清零（空目录/docs）+ stale patch target 守卫测试</name>
  <files>server/tests/mcp_tools/test_patch_target_guard.py, docs/workflows/ai-plan-generation-deprecation.md</files>
  <action>
    per D（UNIFY-03）：

    1. 删除 `server/services/plan_orchestration/` 空目录（已核实为完全空目录，`rmdir` 即可；若 Phase 101–103 并发合入后目录非空则停下核对内容再处置，不盲删）。
    2. `docs/workflows/ai-plan-generation-deprecation.md:10`：「（`plan_orchestration` / `ai_plan_research`）」改为「（`process_runtime` / `ai_plan_research`）」；顺手核查该文件是否有其他 planning_service/plan_orchestration 字样一并更新。
    3. 新增 `server/tests/mcp_tools/test_patch_target_guard.py`（stale patch target 防线，per D importlib+getattr 逐段解析）：
       - 覆盖文件清单（本次迁移影响面 + Phase 26 前科面）：`tests/mcp_tools/test_planning_tools.py`、`tests/mcp_tools/test_create_coding_plan_delegate.py`、`tests/knowledge/test_mcp_artifact_sources.py`、`tests/services/test_process_runtime_extra_evidence.py`、`tests/test_batch_pr.py`（位于 tests/ 根目录；前科面——当前 target 已对齐，仅断言不改动）。
       - 实现：对每个文件源码用正则提取 `monkeypatch.setattr("X.Y.Z", ...)` 与 `mock.patch("X.Y.Z")` / `patch("X.Y.Z")` 的首个字符串字面量参数（形如 dotted path 的），对每个 target 执行「`importlib.import_module` 最长可导入前缀 + 剩余段逐级 `getattr`」，任一解析失败即 fail 并报出 target 与所在文件。测试需 django.setup 后运行（pytest-django 环境天然满足）。
       - 断言至少提取到 N>0 个 target（防正则失配导致空集假通过——grep gate 卫生同款要求）。
    4. 终验（仓库根）：`rg -l "planning_service" --glob '!.planning/**' --glob '!.claude/**' .` 与 `rg -l "plan_orchestration" --glob '!.planning/**' --glob '!.claude/**' .` 均零命中（docs 更新后 plan_orchestration 唯一残留消失）。
  </action>
  <verify>
    <automated>cd /Users/zaneliu/Projects/open-source/friday-clean && test ! -d server/services/plan_orchestration && ! rg -l -q "planning_service" --glob '!.planning/**' --glob '!.claude/**' . && ! rg -l -q "plan_orchestration" --glob '!.planning/**' --glob '!.claude/**' . && cd server && uv run pytest tests/mcp_tools/test_patch_target_guard.py -x -q</automated>
  </verify>
  <done>plan_orchestration 空目录不存在；全仓（排除 .planning/.claude）planning_service 与 plan_orchestration 零残留；patch target 守卫测试提取到非空 target 集且全部可 import。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| extra_evidence → 编排 LLM prompt | McpRepositoryAnalysis.summary（本实例 DB 内确定性采集产物）注入 merge prompt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-104-04 | Tampering | extra_evidence 注入 merge prompt | accept | 证据源为本实例 DB 的确定性采集 summary（repo 元数据/文件路径），非外部自由文本；create 侧 analysis_id 已有 repository 归属校验（L1856–1865），improve 侧走 plan.analysis FK 同库同权 |
| T-104-05 | Info Disclosure | analysis.summary 进编排 trace/prompt | accept | summary 本就经 analyze_repository 返回给同一调用方；不含凭证（源数据为路径/分支/索引状态） |
| T-104-06 | Repudiation | 删缝后测试假通过（stale patch target） | mitigate | test_patch_target_guard.py：importlib+getattr 逐段解析全部 patch target，空集防御断言 |
</threat_model>

<verification>
- `cd server && uv run pytest tests/mcp_tools/ tests/services/test_process_runtime_extra_evidence.py -q` 全绿。
- `cd server && uv run pytest tests/services/ tests/delivery/ -q -k "process_runtime or orchestration or convergence"` 编排面回归绿（entrypoint/merge adapter 改动零破坏）。
- 观测自检：新增 kv 仅 `extra_evidence_count`（既有 caller 事件扩字段），无新增 LLM 调用点/召回面（per CONTEXT 观测节）。
</verification>

<success_criteria>
- ROADMAP 成功标准 2 达成：analyze 收敛且产物被编排实际消费；planning_service.py 删除、helper 随迁、测试迁移不失覆盖。
- ROADMAP 成功标准 3 达成：rg 零残留、空壳目录删除、docs 清理、patch target 全部可 import。
</success_criteria>

<output>
完成后创建 `.planning/phases/104-tool-surface-closure/104-02-SUMMARY.md`（含终验 rg 零残留证据）。
</output>
