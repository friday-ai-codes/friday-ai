---
phase: 104-tool-surface-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/mcp_tools/serializers.py
  - server/mcp_tools/views.py
  - server/mcp_tools/orchestration_delegate.py
  - server/mcp_tools/planning_service.py
  - server/tests/mcp_tools/test_schema_snapshot.py
  - server/tests/mcp_tools/test_planning_tools.py
  - server/tests/mcp_tools/test_create_coding_plan_delegate.py
  - server/tests/knowledge/test_mcp_artifact_sources.py
autonomous: true
requirements: [UNIFY-01]

must_haves:
  truths:
    - "用户经 improve_coding_plan 改版方案时走统一编排（携带 feedback 的编排重跑产新 version），响应含 session_id/status，trace 中可见编排 session"
    - "improve 对外契约（同步 await 至 pause/terminal；research/clarify 在途短路返回 partial+session_id）写进 serializer schema 描述，Cursor 侧调用不挂起不超时"
    - "TOOL_SCHEMA_SNAPSHOT 的 improve 与 create 响应键集补齐 session_id/status（create 既有漂移一并修复），Phase 102 的 registered==snapshot 集合守卫保持绿"
    - "improve/create 相关既有测试全部迁移到 fake delegate（patch mcp_tools.views.delegate_process_runtime），不打真实编排"
  artifacts:
    - path: "server/mcp_tools/views.py"
      provides: "ImproveCodingPlanView 收敛 delegate_process_runtime；import 不再含 improve_coding_plan"
      contains: "coding_plan_improved"
    - path: "server/mcp_tools/orchestration_delegate.py"
      provides: "map_canonical_to_coding_plan 随迁落位（canonical→旧单仓 payload 映射）"
      contains: "def map_canonical_to_coding_plan"
    - path: "server/mcp_tools/serializers.py"
      provides: "improve 契约描述 + snapshot improve/create 响应键含 session_id/status"
      contains: "session_id"
  key_links:
    - from: "server/mcp_tools/views.py"
      to: "server/mcp_tools/orchestration_delegate.py"
      via: "ImproveCodingPlanView 调 delegate_process_runtime + map_canonical_to_coding_plan"
      pattern: "delegate_process_runtime\\("
    - from: "server/tests/mcp_tools/test_planning_tools.py"
      to: "mcp_tools.views.delegate_process_runtime"
      via: "monkeypatch fake delegate（防真实编排时长爆炸）"
      pattern: "mcp_tools\\.views\\.delegate_process_runtime"
---

<objective>
把 `improve_coding_plan` 从确定性假改版（planning_service.improve_coding_plan「往 steps 追加一行」）收敛到 `delegate_process_runtime` 统一编排：改版语义 = 携带 feedback 的编排重跑产新 `McpCodingPlanVersion`。同时完成本 phase 首个 task 的两件定版动作——全仓引用清单（UNIFY-03 前置，per D「首个 task」锁定）与 improve 对外契约定版写进 schema 描述（per ROADMAP 成功标准 1），并修复 create_coding_plan 的 snapshot 既有漂移。

Purpose: UNIFY-01 —— MCP 工具面收口到统一编排的第一步；契约先定、测试同步迁移，杜绝 P6 收口断裂。
Output: 收敛后的 ImproveCodingPlanView + 随迁的 map_canonical_to_coding_plan + 更新的 TOOL_SCHEMA_SNAPSHOT（improve/create 双修）+ 全部迁移到 fake delegate 的测试。
</objective>

<execution_context>
@/Users/zaneliu/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/104-tool-surface-closure/104-CONTEXT.md
@.planning/ROADMAP.md
@server/mcp_tools/planning_service.py
@server/mcp_tools/orchestration_delegate.py
@server/mcp_tools/serializers.py
@server/mcp_tools/views.py
@server/tests/mcp_tools/test_planning_tools.py
@server/tests/mcp_tools/test_create_coding_plan_delegate.py
</context>

<interfaces>
执行前先读，避免探路：

- `delegate_process_runtime(*, requirement_text, work_item=None, include_repos=None, created_by=None) -> DelegateResult`（orchestration_delegate.py L119–173）：同步 await 至 pause/terminal；`DONE→"completed"`、`FAILED→"failed"`、research/clarify 在途→`"partial"`+session；`DelegateResult(session, status, content, plan_version_id, markdown, model_usage)`。
- `map_canonical_to_coding_plan(*, content, repository, branch, requirement) -> dict`（planning_service.py L311–385）：canonical §7 MergedPlan → 旧单仓 payload（title/affected_files/steps/test_plan/risks + repository_id/repository_name/branch/requirement），缺字段填空不抛。
- CreateCodingPlanView（views.py L1829–1970）是收敛先例：actor 解析 L1869–1874、delegate 调用 L1879–1884、model_usage 落 run L1889–1890、映射 L1892–1897、evidence 从 affected_files 推导 L1898–1901、响应含 session_id/status L1946–1947。
- ImproveCodingPlanView 现状（views.py L1973–2075）：plan/latest 加载 L1987–2003 保留；L2007–2014 调确定性 improve_coding_plan 是要替换的部分；current_version+1 递增 L2015–2031 语义不变。
- `McpCodingPlanVersion`（models.py，uniq (plan, version)）。
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 全仓引用清单 + improve 对外契约定版 + snapshot 双修（improve/create）</name>
  <files>server/mcp_tools/serializers.py, server/tests/mcp_tools/test_schema_snapshot.py</files>
  <action>
    per D（UNIFY-03 首个 task + UNIFY-01 契约定版，两个"首个 task"要求合并为一个原子 task）：

    1. **引用清单（只读，产出记录进 SUMMARY）**：在仓库根执行
       `rg -l "planning_service" --glob '!.planning/**' --glob '!.claude/**' .` 与
       `rg -l "plan_orchestration" --glob '!.planning/**' --glob '!.claude/**' .`，
       将命中文件与处置去向逐项记录（已核实现状：planning_service → docs/workflows/ai-plan-generation-deprecation.md、server/mcp_tools/views.py、server/tests/mcp_tools/test_create_coding_plan_delegate.py，外加 planning_service.py 自身与本 plan 将迁移的测试；plan_orchestration → 同一 docs 文件 + 空目录本体）。若发现清单外新增引用（Phase 101–103 并发合入可能引入），一并记录并归入 104-01/104-02 对应任务处置，不得静默跳过。
    2. **契约定版写进 schema 描述**（serializers.py）：给 `ImproveCodingPlanRequestSerializer`（L223–233）添加 docstring 说明对外契约——改版语义 = 携带 feedback 的编排重跑产新 version（current_version+1）；HTTP 请求内同步 await 至 pause/terminal：`DONE→completed`、`FAILED→failed`、research/clarify 在途立即短路返回 `partial` + `session_id`（不阻塞等容器，Cursor 不挂起不超时；partial 后可经 `get_coding_execution`/后续调用跟进）；request 键集不变（`context_chunks` 折入 feedback 块、`max_steps` 收敛后不再截断步骤，均文档化为 accepted-but-advisory）。同型契约在 `CreateCodingPlanRequestSerializer` docstring 补一句对照（同步语义与 partial 短路一致）。
    3. **snapshot 双修**：`TOOL_SCHEMA_SNAPSHOT`（serializers.py L608 起）中 `create_coding_plan.response`（L647）与 `improve_coding_plan.response`（L651）末尾追加 `"session_id", "status"` 两键——create 修复既有漂移（运行时 L1946–1947 已返回但 snapshot 未收录），improve 为本次收敛新增。`server/tests/mcp_tools/test_schema_snapshot.py` 的整表断言同步镜像这两行。只改 response 键列表、不增删工具名键，Phase 102 的 registered==snapshot 集合守卫与 skills 文档守卫（test_skills_snapshot_guard.py）天然保持绿。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/mcp_tools/test_schema_snapshot.py tests/mcp_tools/test_skills_snapshot_guard.py -x -q</automated>
  </verify>
  <done>引用清单已产出并记录处置去向；improve/create 契约描述落 serializer docstring；snapshot 两工具 response 均含 session_id/status，快照测试与集合守卫全绿。</done>
</task>

<task type="auto">
  <name>Task 2: map_canonical_to_coding_plan 随迁 orchestration_delegate.py</name>
  <files>server/mcp_tools/orchestration_delegate.py, server/mcp_tools/planning_service.py, server/mcp_tools/views.py, server/tests/mcp_tools/test_create_coding_plan_delegate.py</files>
  <action>
    per D（UNIFY-02 随迁项，提前到本 plan：improve 收敛复用它，且 104-02 删 planning_service.py 前它必须先落位）：

    1. 将 `map_canonical_to_coding_plan`（planning_service.py L311–385，连同完整 docstring）整体剪切到 `orchestration_delegate.py`（追加到 `delegate_process_runtime` 之后），补 `from repositories.models import Repository` 的类型引用需要（该函数签名用 `repository: Repository`——orchestration_delegate 现无此 import，按现文件惰性 import 风格处理为 TYPE_CHECKING 或直接顶层 import 均可，函数体本身只用 `repository.id/.name`）。`__all__` 追加 `"map_canonical_to_coding_plan"`。
    2. `views.py` L76–80 的 planning_service import 三符号减为两符号（`build_repository_analysis`, `improve_coding_plan`——Task 3 再减 improve），`map_canonical_to_coding_plan` 改从 `.orchestration_delegate` import（与 L75 既有 delegate import 合并为一行）。
    3. `test_create_coding_plan_delegate.py:283` 的 `from mcp_tools.planning_service import map_canonical_to_coding_plan` 改为 `from mcp_tools.orchestration_delegate import map_canonical_to_coding_plan`。
    4. planning_service.py 中原函数删除（不留转发别名——per D 全仓引用清零，转发别名即残留）。
  </action>
  <verify>
    <automated>cd server && DJANGO_SETTINGS_MODULE=friday.settings uv run python -c "import django; django.setup(); from mcp_tools.orchestration_delegate import map_canonical_to_coding_plan" && uv run pytest tests/mcp_tools/test_create_coding_plan_delegate.py -x -q && ! rg -q "map_canonical_to_coding_plan" mcp_tools/planning_service.py</automated>
  </verify>
  <done>函数在 orchestration_delegate.py 可 import；planning_service.py 零残留该符号；delegate 测试全绿。</done>
</task>

<task type="auto">
  <name>Task 3: ImproveCodingPlanView 收敛 delegate + 测试同 task 迁移</name>
  <files>server/mcp_tools/views.py, server/tests/mcp_tools/test_planning_tools.py, server/tests/knowledge/test_mcp_artifact_sources.py</files>
  <action>
    重写 `ImproveCodingPlanView.post`（views.py L1973–2075），镜像 CreateCodingPlanView 收敛先例（L1867–1948）：

    1. plan/latest 加载与 404 分支（L1987–2003）不变；`repo = plan.repository`、branch 解析不变。
    2. actor 解析照抄 create L1869–1874（request.user 认证判定 → None fail-closed）。
    3. **feedback 块构造**（Claude's discretion 措辞，结构 per D 锁定三段）：`requirement_text` = 原需求 + 最新版本方案摘要 + 用户 feedback，例如——
       原文 `plan.requirement`；摘要段用 `json.dumps({"title": latest.plan_body.get("title") if isinstance(latest.plan_body, dict) else "", "affected_files": latest.affected_files, "steps": latest.steps}, ensure_ascii=False)` 截断 2000 字符（用 version 表列而非深挖 plan_body，形态稳定）；feedback 段 = `str(input_data["feedback"])`；`context_chunks` 若提供则以 JSON 行折入「补充上下文」第四段（request 键集不变的兑现方式）。三段用 markdown 小节标题分隔（如 `## 原始需求` / `## 最新方案摘要（v{latest.version}）` / `## 用户改版反馈`）。
    4. delegate 调用：`await delegate_process_runtime(requirement_text=<feedback块>, work_item=None, include_repos=[str(plan.repository_id)], created_by=actor)`；**不加 feedback 专用参数**（per D 签名中性；104-02 再加 extra_evidence）。
    5. `content = delegate.content if isinstance(delegate.content, dict) else {}`；`delegate.model_usage` 非空则 `await self._record_model_usage(run, delegate.model_usage)`（替代原 result.model_usage）。
    6. `plan_payload = map_canonical_to_coding_plan(content=content, repository=repo, branch=branch, requirement=plan.requirement)`（注意 requirement 传原需求、非 feedback 块——响应外形兼容）。
    7. 版本递增语义不变（L2015–2031）：`next_version = plan.current_version + 1`；`McpCodingPlanVersion.acreate` 改为 `plan_body=content or plan_payload`（镜像 create L1918 的回退语义）、affected_files/steps/test_plan/risks 取自 plan_payload、`evidence` 从 `plan_payload["affected_files"]` 推导（镜像 create L1898–1901 的 file evidence 形态）、`change_summary=f"编排改版 v{next_version}（status={delegate.status}）：{feedback[:200]}"`、`risk_delta={"added": [], "reduced": []}`（响应键保留、语义中性化）。
    8. ingestion 调度（L2037–2040）与 `_record_agent_decision` / `self._record`（McpToolView._record 保持不变，per D 观测决策）保留；`output_data` 在既有键末尾追加 `"session_id": str(delegate.session.id)` 与 `"status": delegate.status`（与 Task 1 snapshot 对齐）。
    9. views.py 顶部 import 移除 `improve_coding_plan`（planning_service import 仅剩 `build_repository_analysis`）。**函数本体留在 planning_service.py，104-02 随文件整体删除**——本 task 后它已无生产调用方。

    **测试迁移（与收敛同 task 闭环，per CONTEXT 关键风险）**：
    - `tests/mcp_tools/test_planning_tools.py::test_improve_coding_plan_appends_new_version`：create 与 improve 两次调用前均 `monkeypatch.setattr("mcp_tools.views.delegate_process_runtime", _fake_delegate)`（fake 构造参照同文件 L120–140 既有范式，返回 canonical DONE content）；断言更新为——current_version==2、版本数==2、`body["version"]==2`、`body["session_id"]` 非空、`body["status"]=="completed"`、`body["change_summary"]` 含 feedback 前缀、ToolCallRecord/ModelUsageRecord 存在（fake 带 model_usage）。删除对确定性实现的断言（"回滚步骤" in change_summary 的旧措辞按新 change_summary 调整、risk_delta["added"] 非空改为键存在断言）。补一个 **partial 短路契约用例**：fake 返回 `status="partial"` + content={} → 断言 200、`body["status"]=="partial"`、session_id 非空、新 version 仍落库（plan_body 回退映射 payload）——固化「不挂起不超时」契约。
    - `tests/knowledge/test_mcp_artifact_sources.py::test_trigger_improve_coding_plan`（L576 起）：加同样的 fake delegate monkeypatch（该类中 create 触发用例 L562 已有现成范式），断言 ingestion 触发三元组不变。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/mcp_tools/test_planning_tools.py tests/knowledge/test_mcp_artifact_sources.py -x -q && ! rg -U "from \.planning_service import[\s\S]{0,200}?improve_coding_plan" mcp_tools/views.py</automated>
  </verify>
  <done>improve 走统一编排产新 version（current_version+1 语义不变）；响应含 session_id/status；partial 短路契约有测试固化；improve/analyze 相关测试零真实编排调用；views.py 不再 import improve_coding_plan。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP HTTP API（PAT 认证） | 外部 Cursor/agent 的 feedback / context_chunks 文本经此进入编排 LLM prompt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-104-01 | Tampering | ImproveCodingPlanView feedback→requirement_text | accept | feedback 本就是用户提供的 prompt 素材（与 create 的 requirement 同险级）；serializer max_length=8000 限幅，编排产物经 map_canonical 显式白名单映射，不透传内部键 |
| T-104-02 | Elevation | delegate created_by actor | mitigate | 照抄 create 先例：非认证用户 → None，召回 stage fail-closed 空召回（不绕权限） |
| T-104-03 | Info Disclosure | 响应新增 session_id | accept | session_id 为本人发起的编排会话钥匙，与 create 既有行为一致 |
</threat_model>

<verification>
- `cd server && uv run pytest tests/mcp_tools/ tests/knowledge/test_mcp_artifact_sources.py -q` 全绿。
- `rg -n "improve_coding_plan|map_canonical_to_coding_plan" server/mcp_tools/views.py` 显示两符号均不再来自 planning_service。
- 观测自检：improve 收敛后经编排链路，`mcp_plan_delegate_started/completed` 事件（caller/mcp_tools）+ McpToolView._record 保持；无新增 LLM 调用点（per CONTEXT 观测节）。
</verification>

<success_criteria>
- ROADMAP 成功标准 1 达成：improve 走统一编排、trace 可见 session、契约定版进 schema 描述、partial 短路保证 Cursor 不挂起。
- create snapshot 漂移修复；snapshot 相关守卫全绿。
- 引用清单产出，为 104-02 删缝提供逐项处置依据。
</success_criteria>

<output>
完成后创建 `.planning/phases/104-tool-surface-closure/104-01-SUMMARY.md`（含 Task 1 引用清单与处置表）。
</output>
