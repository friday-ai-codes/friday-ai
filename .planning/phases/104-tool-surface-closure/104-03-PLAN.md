---
phase: 104-tool-surface-closure
plan: 03
type: execute
wave: 3
depends_on: [104-02]
files_modified:
  - server/tests/test_milestone_e2e_learning_case.py
autonomous: true
requirements: [UNIFY-01, UNIFY-02, UNIFY-03]

must_haves:
  truths:
    - "同一条 learning case 在四处均可检索到：Chat 工具 search_learning_cases、编排召回 DeliveryKnowledgeRecallAdapter.recall（learning_case kind）、MCP search_learning_cases view、容器知识 MCP 链（同 URL 契约 + 组合覆盖）"
    - "统一排序成立：MCP 与 Chat 两面返回的 top-1 entity_id 一致（同一 DeliveryKnowledgeSearchService 排序）"
    - "验收测试在收口完成后的最终代码上运行（引用清单→契约→收敛→删缝→E2E 顺序，一步不跳）"
  artifacts:
    - path: "server/tests/test_milestone_e2e_learning_case.py"
      provides: "里程碑四面检索端到端验收测试（内存 Qdrant + 确定性 embedding，自包含）"
      min_lines: 150
      contains: "DeliveryKnowledgeRecallAdapter"
  key_links:
    - from: "server/tests/test_milestone_e2e_learning_case.py"
      to: "server/agents/tools/knowledge_read_tools.py"
      via: "Chat 工具面直调（Phase 102 产物）"
      pattern: "knowledge_read_tools"
    - from: "server/tests/test_milestone_e2e_learning_case.py"
      to: "/api/mcp/tools/search_learning_cases/"
      via: "MCP view HTTP 调用 + 容器链同 URL reverse 断言"
      pattern: "search_learning_cases"
---

<objective>
落地里程碑端到端验收（ROADMAP Phase 104 成功标准 4）：新建 `server/tests/test_milestone_e2e_learning_case.py`，种一条 learning case（create → ingestion → 向量入库），断言同一条 case 在 Chat 工具 / 编排召回 / MCP view / 容器知识 MCP 链四处均可检索到，且 MCP 与 Chat 的 top-1 entity_id 一致（统一排序来自同一 `DeliveryKnowledgeSearchService`）。

Purpose: v0.17.0 里程碑"统一知识库与全链路联动"的最终验收门——证明工具面收口没有降级任何一条检索链。
Output: 一个自包含（内存 Qdrant + 确定性 embedding）的服务端集成测试文件。
</objective>

<execution_context>
@/Users/zaneliu/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/104-tool-surface-closure/104-CONTEXT.md
@server/tests/mcp_tools/test_learning_cases.py
@server/tests/mcp_tools/conftest.py
@server/services/process_runtime/recall_adapter.py
@server/agents/tools/knowledge_read_tools.py
@server/tests/agents/tools/test_knowledge_read_tools.py
@server/tests/mcp_tools/test_container_knowledge_chain.py
</context>

<interfaces>
执行前先读（Phase 100/102/103 产物，执行时均已在 main）：

- 测试基建范式：`tests/mcp_tools/test_learning_cases.py` 的 `golden_vector_stack` fixture（L109–137：`QdrantClient(":memory:")` monkeypatch `QdrantService.get_client` + 确定性 `_bow_dense`/`_bow_sparse` monkeypatch EmbeddingService/SparseEncoderService + `knowledge.ingestion.run_in_background` no-op）与其 golden 用例（L372 起）的种子写入方式（create → 显式 `await ingest(...)` 同步补摄）。
- MCP 客户端范式：`tests/mcp_tools/conftest.py` 的 `mcp_client` fixture（PAT 铸造 + APIClient 认证头）——新文件在 `tests/` 根，不可见该 fixture，需本地复刻。
- MCP view：POST `/api/mcp/tools/search_learning_cases/`（urls.py L59，name=`mcp-tool-search-learning-cases`）。
- Chat 工具：`server/agents/tools/knowledge_read_tools.py` 的 `search_learning_cases`（Phase 102 产物，fail-closed 权限——调用范式与权限前置见 `tests/agents/tools/test_knowledge_read_tools.py`）。
- 编排召回：`server/services/process_runtime/recall_adapter.py` `DeliveryKnowledgeRecallAdapter.recall(session)`（Phase 102 扩容后默认 kinds 含 learning_case；session 需 decomposition.requirement_text 作 query，构造方式见 `tests/services/test_recall_adapter.py`）。
- 容器链：task 侧经进程内 SDK MCP server 对服务端 `/api/mcp/tools/<name>/` HTTP 转调（Phase 103 AGENT-02）；task 侧 handler 契约测试在 `task/tests/test_knowledge_tools.py`，服务端链路测试在 `server/tests/mcp_tools/test_container_knowledge_chain.py`。
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 测试基建 + 种子 + Chat/MCP 两面断言 + 统一排序</name>
  <files>server/tests/test_milestone_e2e_learning_case.py</files>
  <action>
    新建 `server/tests/test_milestone_e2e_learning_case.py`（文件名 per CONTEXT 锁定建议）：

    1. **自包含基建**：本地复刻 `_bow_dense`/`_bow_sparse` 与 `golden_vector_stack` 等价 fixture（参照 tests/mcp_tools/test_learning_cases.py L60–137；不跨测试模块 import，里程碑验收自包含）；本地复刻 `mcp_client` 等价 fixture（参照 tests/mcp_tools/conftest.py 的 PAT + APIClient 范式）。
    2. **种子**：种两条 learning case——一条与查询强相关（如标题/正文含「登录超时」token），一条弱相关（无共享 token），均走 create → 显式 `await ingest(...)` 向量入库（参照 golden 用例范式）。两条用例使排序断言有区分度（top-1 必须是强相关条）。记录强相关条的 knowledge entity_id（从入库产物或检索结果获取，以 MCP 面返回的 entity 标识为准）。
    3. **面 3（MCP view）**：POST `/api/mcp/tools/search_learning_cases/`（query=「登录超时」类查询）→ 断言强相关 case 命中且为 top-1，记录其 entity_id。
    4. **面 1（Chat 工具）**：按 tests/agents/tools/test_knowledge_read_tools.py 的调用范式（含权限前置：会话 owner / 项目成员构造）直调 `search_learning_cases` 工具函数 → 断言同一条 case 命中。
    5. **统一排序断言（locked）**：MCP 面 top-1 entity_id == Chat 面 top-1 entity_id（两面同经 `DeliveryKnowledgeSearchService` 排序的外部可观察证明）。测试注释写明该断言的意图。
    6. pytestmark 与 DB 事务模式参照 test_learning_cases.py golden 用例（含 `run_in_background` no-op 后显式补摄避免竞态的注释）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/test_milestone_e2e_learning_case.py -x -q</automated>
  </verify>
  <done>种子 case 在 Chat 工具与 MCP view 两面均可检索；两面 top-1 entity_id 一致且为强相关条。</done>
</task>

<task type="auto">
  <name>Task 2: 编排召回面 + 容器链同 URL 契约 + 组合覆盖文档化</name>
  <files>server/tests/test_milestone_e2e_learning_case.py</files>
  <action>
    同文件追加两个用例（复用 Task 1 的 fixture 与种子 helper）：

    1. **面 2（编排召回）**：构造 `ConvergenceSession`（或按 tests/services/test_recall_adapter.py 的 session 构造范式），decomposition.requirement_text 含「登录超时」查询语义 → `await DeliveryKnowledgeRecallAdapter(...).recall(session)` → 断言 hits 中存在 kind/entity 指向种子 learning case 的命中（learning_case kind 在 Phase 102 默认集合内；若 adapter 构造需依赖注入按其 __init__ 签名提供）。
    2. **面 4（容器知识 MCP 链，组合覆盖 per D）**：
       - 断言 `django.urls.reverse("mcp-tool-search-learning-cases") == "/api/mcp/tools/search_learning_cases/"`——服务端 view 挂载路径与容器侧转调的字面 URL 模板一致（同 URL 契约）。
       - 测试 docstring 写明组合逻辑（locked）：容器链 = task 侧 handler 契约测试（`task/tests/test_knowledge_tools.py`，mock 端点模式，验证 task 进程内 MCP server 对 `/api/mcp/tools/<name>/` 的请求构造）+ 服务端同 URL view 行为（本文件面 3 已断言真实检索）→ 两端组合即容器内代理可检索到同一条 case；server 侧链路测试另见 `server/tests/mcp_tools/test_container_knowledge_chain.py`。
    3. 收尾回归：全量跑本 phase 触碰面（见 verify），确认 E2E 在删缝后的最终代码上成立（P6 收口顺序最后一步）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/test_milestone_e2e_learning_case.py tests/mcp_tools/ tests/services/test_recall_adapter.py tests/agents/tools/test_knowledge_read_tools.py -q</automated>
  </verify>
  <done>四面检索断言齐备（两面直测 + 编排召回直测 + 容器链同 URL 契约与组合覆盖文档化）；本 phase 触碰面定向回归全绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| （无新增） | 纯测试交付，不改生产代码 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-104-07 | Repudiation | 验收测试假通过 | mitigate | 双种子区分度设计（top-1 必须命中强相关条，弱相关条防「返回任意结果即过」）；容器链 URL 用 reverse 反查而非硬编码复读 |
</threat_model>

<verification>
- `cd server && uv run pytest tests/test_milestone_e2e_learning_case.py -q` 全绿。
- 测试不依赖外部 Qdrant/真实 embedding/网络（pytest-socket 隔离下可跑）。
</verification>

<success_criteria>
- ROADMAP 成功标准 4 达成：同一条 learning case 四处均可检索、统一排序（MCP 与 Chat top-1 entity_id 一致）。
- v0.17.0 里程碑验收门就位，后续任何检索面回归都会在此测试上显形。
</success_criteria>

<output>
完成后创建 `.planning/phases/104-tool-surface-closure/104-03-SUMMARY.md`。
</output>
