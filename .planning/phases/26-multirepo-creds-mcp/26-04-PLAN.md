---
phase: 26-multirepo-creds-mcp
plan: 04
type: execute
wave: 2
depends_on: ["26-01"]
files_modified:
  - server/repositories/serializers.py
  - server/repositories/views.py
  - server/repositories/urls.py
  - server/tests/repositories/test_git_instance_credentials.py
  - web/src/api/gitInstanceCredentials.ts
  - web/src/api/index.ts
  - web/src/pages/admin/git-credentials/index.vue
  - web/src/pages/admin/__tests__/git-credentials.spec.ts
autonomous: true
requirements: [REPO-01]
must_haves:
  truths:
    - "管理员可在前端创建/编辑/删除实例级 Git 凭证（按 host）"
    - "实例凭证 API 列表/详情响应只含 host/provider/label/has_token/时间戳，绝不回显明文 token"
    - "创建/更新时 token 经 Fernet 加密入库（access_token 为 write-only）"
    - "前端表单仅显示「是否已配置」与 host/label，不回显明文 token"
    - "非管理员无权访问实例凭证 CRUD"
  artifacts:
    - path: "server/repositories/serializers.py"
      provides: "GitInstanceCredential 读/写序列化器（token write-only）"
      contains: "GitInstanceCredential"
    - path: "server/repositories/views.py"
      provides: "GitInstanceCredential CRUD 视图（IsSuperUser，token 加密）"
      contains: "GitInstanceCredential"
    - path: "server/repositories/urls.py"
      provides: "git-instance-credentials 路由"
      contains: "git-instance-credentials"
    - path: "web/src/api/gitInstanceCredentials.ts"
      provides: "前端实例凭证 API 客户端"
      exports: ["gitInstanceCredentialsApi"]
    - path: "web/src/pages/admin/git-credentials/index.vue"
      provides: "实例凭证管理页（token 不回显）"
  key_links:
    - from: "server/repositories/views.py"
      to: "common.encryption.encrypt_value"
      via: "写入时加密 access_token"
      pattern: "encrypt_value"
    - from: "web/src/pages/admin/git-credentials/index.vue"
      to: "gitInstanceCredentialsApi"
      via: "CRUD 调用"
      pattern: "gitInstanceCredentialsApi"
---

<objective>
为 REPO-01 实例凭证提供最小管理闭环：后端 REST CRUD（按 host，token Fernet 加密、绝不返回明文，IsSuperUser）+ 前端管理入口（仅显示 host/label/是否已配置，token 不回显）。让管理员能集中维护「同一 GitLab 实例一份凭证」。

Purpose: 兑现 D-01「集中管理实例凭证」与 D-04「token 绝不明文返回前端」的用户可见闭环。
Output: 实例凭证读/写序列化器、CRUD 视图、路由、前端 API 模块 + 管理页 + 守护测试。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/26-multirepo-creds-mcp/26-CONTEXT.md
@.planning/phases/26-multirepo-creds-mcp/26-01-PLAN.md
</context>

<artifacts_this_phase_produces>
- 实例凭证 REST CRUD（token write-only、加密、IsSuperUser）
- 前端实例凭证管理页 + API 模块（token 不回显）
- 守护测试：API 响应/序列化器无明文 token + 前端不回显
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: 实例凭证序列化器 + CRUD 视图 + 路由（token write-only 加密）</name>
  <files>server/repositories/serializers.py, server/repositories/views.py, server/repositories/urls.py</files>
  <read_first>
    - server/repositories/serializers.py L320-L330（既有 `return bool(obj.encrypted_token)` has_token computed 字段写法）
    - server/repositories/views.py L21-L23（IsSuperUser import）、L745-L770（SetAccessTokenView 用 encrypt_value(token) 写入 GitCredential 的范式）
    - server/repositories/views.py L40-L52, RepositoryExclusionRulesView / RepositoryExclusionRuleDetailView（adrf APIView CRUD idiom：list/post/delete + 中文错误）
    - server/repositories/urls.py L54-L119（router 注册 + 字面路径须在 `path("", include(router.urls))` 之前；literal 段不被 UUID 通配匹配）
    - server/common/encryption.py（encrypt_value 契约）
    - server/repositories/models.py（26-01 产出的 GitInstanceCredential 字段）
  </read_first>
  <action>
    serializers.py：新增 `GitInstanceCredentialSerializer`（read）字段 id/host/provider/label/created_at/updated_at + `has_token = SerializerMethodField`（`return bool(obj.encrypted_token)`）；**绝不**包含 encrypted_token / token 任何明文字段。
    新增写入序列化器（或同一序列化器加 `access_token = CharField(write_only=True, required=...)`）：host 必填且做小写归一 + 唯一校验（DB unique 已兜底，序列化器给中文友好报错）、provider 默认 gitlab、label 可选、access_token write_only（创建必填，更新可选「留空=不改 token」）。
    views.py：新增 `GitInstanceCredentialsView`（list GET + create POST）与 `GitInstanceCredentialDetailView`（GET 单条 + PATCH/PUT 更新 + DELETE）——adrf APIView，permission_classes 含 IsSuperUser（仅管理员）。create/update 时 `encrypt_value(access_token)` 存 encrypted_token；access_token 为空的 update 不动 token。所有响应只经 read 序列化器（无明文 token）。日志绝不含 token。
    另：把 views.py L552-L558 base-branch 校验处的 `decrypt_value(credential.encrypted_token)` 改为经 26-01 解析器 `await aresolve_git_token(instance)` 取 token（使无 per-repo token、仅靠实例池的仓库也能做 base-branch 校验）；TestConnectionView 中「校验用户当场输入的 token」流程保持不变（那是验证入参 token，非解析存储凭证，不接解析器）。
    urls.py：在 `path("", include(router.urls))` **之前**注册 `git-instance-credentials/`（→ GitInstanceCredentialsView）与 `git-instance-credentials/<uuid:credential_id>/`（→ GitInstanceCredentialDetailView），name 形如 git-instance-credentials / git-instance-credential-detail。
  </action>
  <verify>
    <automated>cd server && uv run ruff check repositories/serializers.py repositories/views.py repositories/urls.py</automated>
  </verify>
  <acceptance_criteria>
    - CRUD 五动作可用；read 响应只含 host/provider/label/has_token/时间戳，无明文 token。
    - access_token write_only + 写入即 encrypt_value 加密；空 token 的 update 不动既有 token。
    - 仅 IsSuperUser 可访问；host 唯一校验给中文报错。
    - base-branch 校验取 token 经解析器；TestConnection 验证入参 token 流程不变。
  </acceptance_criteria>
  <done>实例凭证 REST CRUD 落地，token 加密 write-only 不回显，路由注册安全。</done>
</task>

<task type="auto">
  <name>Task 2: 前端实例凭证 API 模块 + 管理页（token 不回显）</name>
  <files>web/src/api/gitInstanceCredentials.ts, web/src/api/index.ts, web/src/pages/admin/git-credentials/index.vue</files>
  <read_first>
    - web/src/api/exclusions.ts（API 模块范式：import { del, get, post } from './client'；相对路径；TS interface 与后端序列化器对齐）
    - web/src/api/providerCredentials.ts L1-L50（凭证 API 客户端范式 + index.ts barrel 导出方式）
    - web/src/api/index.ts L21-L33（barrel 导出 providerCredentialsApi / settingsApi 写法）
    - web/src/pages/admin/providers/index.vue（管理页布局/reka-ui 表单/vue-i18n 中文文案/列表+新建+编辑+删除交互范式，token 不回显的输入控件参照）
  </read_first>
  <action>
    gitInstanceCredentials.ts：定义 `GitInstanceCredential`（host/provider/label/has_token/created_at/updated_at，**无 token 字段**）与 create/update payload（含 access_token，仅提交不回显）；`gitInstanceCredentialsApi` 暴露 list/create/update/remove，相对路径 `/repositories/git-instance-credentials/...`（client.ts 自动补 /api 前缀 + 末尾 /）。
    index.ts：barrel 导出 gitInstanceCredentialsApi（沿用既有 export 风格）。
    admin/git-credentials/index.vue：列表展示 host/provider/label + 「是否已配置 token」徽标（用 has_token，绝不渲染明文）；新建/编辑表单 token 输入框为 password 型、占位提示「留空表示不修改」、提交后清空，绝不回显既有 token；删除走二次确认；文案接 vue-i18n（默认中文）。仅管理员可见（沿用 admin 页守卫）。
  </action>
  <verify>
    <automated>cd web && pnpm vue-tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - 前端可 list/create/update/remove 实例凭证；类型与后端 read 序列化器对齐，前端类型无 token 明文字段。
    - token 输入框 password 型、不回显既有 token、留空=不改；列表仅显示 has_token 徽标。
    - vue-tsc 通过；文案中文。
  </acceptance_criteria>
  <done>前端实例凭证管理页 + API 模块落地，token 不回显。</done>
</task>

<task type="auto">
  <name>Task 3: 守护测试（后端无明文 token + 前端不回显）</name>
  <files>server/tests/repositories/test_git_instance_credentials.py, web/src/pages/admin/__tests__/git-credentials.spec.ts</files>
  <read_first>
    - server/tests/test_no_plaintext_token_in_db.py（既有「DB/响应无明文 token」守护断言范式）
    - server/tests/repositories/（既有 repositories REST 测试 fixture / adrf APIView 测试范式）
    - web/src/components/__tests__/ProviderCredentialForm.spec.ts（凭证表单守护测试范式：token 不回显断言）
    - web/src/pages/admin/__tests__/providers.spec.ts（admin 页 vitest + 真实 zh-CN messages 断言范式）
  </read_first>
  <action>
    后端 test_git_instance_credentials.py：
    - Test 1：POST 创建（access_token=明文）后，DB encrypted_token 为密文（≠明文、decrypt_value 还原一致），且 GET list/detail 响应 JSON **不含**该明文 token 子串。
    - Test 2：has_token 反映是否配置；空 access_token 的 PATCH 不清空既有 token。
    - Test 3：非管理员（普通认证用户）访问 → 403/拒绝。
    - Test 4：捕获结构化日志断言 CRUD 路径不含 token 明文。
    前端 git-credentials.spec.ts：
    - 渲染列表只显示 has_token 徽标、不渲染明文 token；编辑表单不回填既有 token（password 框初值为空）；用真实 zh-CN.json messages 断言关键文案（如「留空表示不修改」）不被改空。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/repositories/test_git_instance_credentials.py -x && cd ../web && pnpm vitest run src/pages/admin/__tests__/git-credentials.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - 后端：DB token 密文、响应/日志无明文 token、has_token 正确、空 token PATCH 不清空、非管理员拒绝——全绿。
    - 前端：列表/表单不回显明文 token，中文文案守护——全绿。
  </acceptance_criteria>
  <done>安全守护测试全绿，token 全程不明文出口。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 前端 ↔ 实例凭证 API | 写入 token（明文入站，加密落库）；读取绝不含明文 token |
| API ↔ DB | token 仅以 Fernet 密文存储 |
| 非管理员 ↔ CRUD | 仅 IsSuperUser 可操作实例凭证 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-13 | Information Disclosure | API 响应 token | mitigate | read 序列化器无 token 字段，仅 has_token 布尔；Test 1 守护 |
| T-26-14 | Information Disclosure | DB token | mitigate | 写入即 encrypt_value 加密；无明文列；Test 1 还原校验 |
| T-26-15 | Information Disclosure | 前端回显 | mitigate | token password 框不回填、提交后清空；前端类型无明文字段；spec 守护 |
| T-26-16 | Elevation of Privilege | CRUD 权限 | mitigate | IsSuperUser 限制；Test 3 守护非管理员拒绝 |
| T-26-17 | Information Disclosure | 日志 | mitigate | CRUD 路径禁记 token；Test 4 守护 |
</threat_model>

<verification>
- `cd server && uv run pytest tests/repositories/test_git_instance_credentials.py -x` 全绿。
- `cd web && pnpm vitest run src/pages/admin/__tests__/git-credentials.spec.ts` 全绿；`pnpm vue-tsc --noEmit` 通过。
- `cd server && uv run ruff check repositories/serializers.py repositories/views.py repositories/urls.py` 干净。
</verification>

<success_criteria>
- 管理员可 CRUD 实例凭证；token 加密 write-only、API/DB/日志/前端均无明文回显；非管理员被拒。
</success_criteria>

<output>
Create `.planning/phases/26-multirepo-creds-mcp/26-04-SUMMARY.md` when done.
</output>
