---
phase: 20-validation
plan: 03
type: execute
wave: 1
depends_on: ["20-01"]
files_modified:
  - server/workflows/templates/daily_summary.json
  - server/workflows/templates/code_review_pipeline.json
  - server/workflows/templates/loader.py
  - server/tests/workflows/test_template_loader.py
autonomous: true
requirements: [TPL-01, TPL-02, TPL-03]
must_haves:
  truths:
    - "daily_summary 修复后 {{nodes.fetch_data.body}} / {{nodes.summarize.text}} 引用真实节点输出字段，validator 零 error 且运行语义正确"
    - "code_review_pipeline 按方案 A 结构性对齐：边 target_handle=coding_result、notify 引用 review_report，去除产不出 merge_requests 的 http 节点；validator 零 error；description 文档化 webhook payload 前提"
    - "4 个内置模板（code_generation/feishu_full_pipeline/daily_summary/code_review_pipeline）经 WorkflowGraphValidator 均零 error"
    - "人为注入 schema 可判定的断裂（坏 node_type / 缺必填 config / {{nodes.x.nonexistent}} / 坏 handle）→ 模板校验测试失败"
    - "acreate_workflow_from_template 在建库前调用同一 validator，非法模板拒绝创建并返回结构化错误（不产生半残 workflow）"
  artifacts:
    - path: "server/workflows/templates/daily_summary.json"
      provides: "字段对齐真实输出（body/text）"
      contains: "nodes.fetch_data.body"
    - path: "server/workflows/templates/code_review_pipeline.json"
      provides: "方案 A 契约对齐 + payload 文档化 description"
      contains: "coding_result"
    - path: "server/workflows/templates/loader.py"
      provides: "acreate_workflow_from_template 建库前校验"
      contains: "WorkflowGraphValidator"
    - path: "server/tests/workflows/test_template_loader.py"
      provides: "每模板零 error + 注入断裂失败 + loader 拒绝（TPL-01/02/03）"
      contains: "WorkflowGraphValidator"
  key_links:
    - from: "server/workflows/templates/loader.py"
      to: "server/workflows/validation/graph_validator.py"
      via: "load_template 后、acreate 前调 validate，error→raise ValueError"
      pattern: "WorkflowGraphValidator"
    - from: "server/workflows/templates/code_review_pipeline.json"
      to: "server/workflows/nodes/ai/code_review.py"
      via: "trigger payload coding_result.merge_requests + target_handle=coding_result"
      pattern: "coding_result"
---

<objective>
修复两个断裂的内置模板使其开箱可执行（TPL-01），为模板加自动化校验守护（TPL-02），并让模板 loader 在实例化前执行与保存相同的图校验（TPL-03）。`daily_summary` 为低风险字段重命名；`code_review_pipeline` 按 **orchestrator 已定的方案 A**（结构性契约对齐 + webhook payload 文档化前提）重构。本计划与 20-02（views/serializers）、20-04（前端）文件无重叠，可并行（同属 Wave 1，依赖 20-01 validator）。

Purpose: 让"任一内置模板创建后零改配置执行到业务预期"（TPL-01）成立；用 validator 在模板夹具上的应用守护契约一致性（TPL-02）；loader 走与保存同源校验（TPL-03，D-09）。
Output: daily_summary.json / code_review_pipeline.json（修复）、loader.py（建库前校验）、test_template_loader.py（扩展 TPL-01/02/03）。
</objective>

<execution_context>
@.cursor/gsd-core/workflows/execute-plan.md
@.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-validation/20-CONTEXT.md
@.planning/phases/20-validation/20-RESEARCH.md
@.planning/phases/20-validation/20-PATTERNS.md
@.planning/phases/20-validation/20-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 修复 daily_summary（字段重命名）+ code_review_pipeline（方案 A 契约对齐）</name>
  <files>server/workflows/templates/daily_summary.json, server/workflows/templates/code_review_pipeline.json</files>
  <read_first>
    - server/workflows/templates/daily_summary.json L37 `{{nodes.fetch_data.output}}` / L50 `{{nodes.summarize.output}}`
    - server/workflows/templates/code_review_pipeline.json 全文（trigger=webhook_trigger / fetch_pr=http_request / review=ai_code_review / notify；edges L56-60；notify content L49 `{{nodes.review.output}}`）
    - server/workflows/templates/code_generation.json（健康范式：ai_coding→code_review，无 http 中转）
    - server/workflows/nodes/integrations/http.py L83-138（http 输出 {status_code,headers,body,ok}，无 schema）
    - server/workflows/nodes/ai/prompt.py L262-310, L385-401（ai_prompt 输出 schema 含 text；output 也在 schema 但运行时为数组——Pitfall 3）
    - server/workflows/nodes/ai/code_review.py L215-230（输入端口 coding_result/plan）、L307-328（coding_result 或 input_data.merge_requests 兜底）、L238-258（输出 schema review_report/issues_count/approved 等，无 output）
    - 20-PATTERNS.md L278-336（daily_summary 低风险 diff + code_review_pipeline 方案 A 重构 + payload 文档化前提）
    - **ORCHESTRATOR DECISION（方案 A，已定，勿设为开放）**：见 20-RESEARCH.md Pitfall 4 / Open Question #1 终态语义
  </read_first>
  <action>
    **daily_summary.json（低风险）：**
    - L37：`{{nodes.fetch_data.output}}` → `{{nodes.fetch_data.body}}`（http 实际输出 body；http 无 schema → validator 字段层跳过，但运行语义正确）。
    - L50：`{{nodes.summarize.output}}` → `{{nodes.summarize.text}}`（ai_prompt 主文本字段，text 在输出 schema 内）。

    **code_review_pipeline.json（方案 A — 结构性契约对齐，orchestrator 已定）：**
    - 去除 `fetch_pr`（http_request）节点——它永远产不出 `coding_result.merge_requests`（Pitfall 4），是结构性契约不符，非字段重命名。
    - 边重构为 `trigger → review`（target_handle 设为 `"coding_result"`，让触发器 payload 的编码结果数据进入 ai_code_review 的 coding_result 输入端口；若依 code_review.py L312-314 的 `input_data.merge_requests` 兜底更稳，executor 可依实现选 default 扁平合并——但**终态须 validator 零 error 且给定文档化 payload 可跑到 ai_code_review 业务产出**）、`review → notify`。
    - notify content：`{{nodes.review.output}}` → `{{nodes.review.review_report}}`（真实输出字段；output 不在 ai_code_review 输出 schema → 不修则 validator 报 field_not_found）。
    - 更新 `description`：文档化 webhook payload 前提——触发器需携带 `coding_result: {merge_requests: [{mr_id, repository_id, repository_name, mr_url}]}` 形态数据，且 `repository_id` 必须是 Friday 已注册仓库 UUID + 已配置凭证（外部 PR 审查无法纯 config 自足，这是符合"开箱能跑"意图的务实终态）。
    - 保留 JSON 合法（version/name/template_id 不动），节点 position 可保留或微调避免重叠。
  </action>
  <verify>
    <automated>cd server && uv run python -c "import json; d=json.load(open('workflows/templates/daily_summary.json')); c=json.load(open('workflows/templates/code_review_pipeline.json')); assert 'fetch_data.body' in json.dumps(d,ensure_ascii=False); assert 'review_report' in json.dumps(c,ensure_ascii=False); assert all(n['type']!='http_request' for n in c['nodes']); print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - daily_summary.json 含 `nodes.fetch_data.body` 与 `nodes.summarize.text`，不再含 `fetch_data.output`/`summarize.output`。
    - code_review_pipeline.json 无 `http_request` 节点；notify 引用 `review_report`；存在 `coding_result` target_handle；description 包含 webhook payload + repository_id 注册仓库前提说明。
    - 两文件均为合法 JSON（上述命令打印 ok）。
    - 经 20-01 validator 校验零 error（由 Task 3 测试断言）。
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: loader 建库前同源校验（TPL-03）</name>
  <files>server/workflows/templates/loader.py</files>
  <read_first>
    - server/workflows/templates/loader.py L63-99（rewrite_template_refs：template_to_short 映射；变量扫描口径）
    - server/workflows/templates/loader.py L198-283（acreate_workflow_from_template：load_template L211 → Workflow.objects.acreate L214；template nodes type/config、edges source/target）
    - server/workflows/api/views.py L645-682（from_template @action：ValueError→400 已捕获 L675-676）
    - 20-PATTERNS.md L260-275（loader analog：在 load_template 与 acreate 之间插入 validator）
    - server/workflows/validation/graph_validator.py（20-01 产物：validate 入参形态）
  </read_first>
  <action>
    在 `acreate_workflow_from_template` 中，于 `template = load_template(template_id)`（L211）之后、`Workflow.objects.acreate`（L214）**之前**插入校验：
    - 用模板 `nodes`（`type`→`node_type`、`config`、id 用模板节点 id 兼作 short_id——与 `rewrite_template_refs(template_to_short)` 同源 id 空间）与 `edges`（`source`/`target` → source_node_id/target_node_id）构造 validator 输入；变量引用的 short_id 空间用模板节点 id。
    - 调 `WorkflowGraphValidator().validate(nodes, edges)`；若 `errors` 非空 → `raise ValueError(...)`（消息含结构化 errors，与保存同源；view 层已 `ValueError→400`）。建库前拒绝 → 不产生半残 workflow。
    - validator 纯 CPU 无 DB，可同步调用（在 async 函数中如需严格可 `await sync_to_async(...)`，但纯 CPU 直接调用可接受）。
    保持中文注释、ruff line-length 100、async 既有范式。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/workflows/test_template_loader.py -k "from_template or validator" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "WorkflowGraphValidator" server/workflows/templates/loader.py` 命中，且位于 `load_template` 与 `acreate` 之间。
    - 非法模板（注入断裂）经 `acreate_workflow_from_template` 抛 ValueError / 经 from_template API 返回 400，且无 workflow 落库（Task 3 断言）。
    - 4 个合法内置模板 from_template 成功创建（回归不破）。
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: test_template_loader.py 扩展（TPL-01/02/03）</name>
  <files>server/tests/workflows/test_template_loader.py</files>
  <read_first>
    - server/tests/workflows/test_template_loader.py L23-60（TestListTemplates/TestLoadTemplate：参数化遍历 4 模板范式）
    - 20-RESEARCH.md Pitfall 3（注入断裂必须 schema 可判定：{{nodes.summarize.nonexistent_field}} / {{nodes.ghost.x}} / 坏 node_type / 坏 handle；不可用 http 节点字段→假绿）
    - 20-RESEARCH.md Validation Architecture Test Map（TPL-01/02/03 命令）
    - 20-PATTERNS.md L359-368（test_template_loader analog + 注入清单）
  </read_first>
  <action>
    扩展 `tests/workflows/test_template_loader.py`：
    - **TPL-02 每模板零 error**：参数化遍历 4 模板（list_templates），对每个 load_template → 转 validator 入参（type→node_type、edges source/target，节点 id 兼作 short_id）→ `WorkflowGraphValidator().validate(...)` 断言 `errors == []`。
    - **TPL-02 注入断裂失败**（schema 可判定）：对某模板副本注入 ①坏 node_type、②删除必填 config 字段、③`{{nodes.summarize.nonexistent_field}}`（ai_prompt 有 schema → field_not_found）或 `{{nodes.ghost.x}}`（node_not_found）、④坏 source_handle —— 每种断言 validator errors 非空且 reason 命中。**禁止用 http 节点字段做坏变量注入**（无 schema 跳过→假绿，Pitfall 3）。
    - **TPL-03 loader 拒绝**：注入断裂模板经 `acreate_workflow_from_template`（django_db async）→ 抛 ValueError / 或 from_template API 400，且 DB 无新 workflow。
    - **TPL-01 字段对齐回归**：断言 daily_summary 修复后变量引用解析到真实字段（如 validator 对 `summarize.text` 零 error；对修复前的 `summarize.output` 不作为正确性依据——Pitfall 3 注明 validator 抓不到语义错，TPL-01 正确性靠字段对齐 + 本断言守护"不回退到坏字段"）。
    用既有参数化范式 + django_db（仅 loader 用例需要），中文 docstring，ruff line-length 100。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/workflows/test_template_loader.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/workflows/test_template_loader.py -x -q` 全绿。
    - 含"每模板 validator 零 error"参数化用例（4 模板）与至少 4 类 schema 可判定断裂注入用例。
    - 含 loader 建库前拒绝用例（断言无 workflow 落库）。
    - 坏变量注入用例不使用 http 节点字段（grep 检查注入路径含 ai_prompt/ghost 节点）。
    - `cd server && uv run pytest tests/workflows -q` 零回归。
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 内置模板 JSON → loader → DB | 模板被实例化为用户 workflow |
| webhook payload → code_review_pipeline | 外部 PR 事件携带 coding_result/merge_requests + repository_id |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-08 | Tampering | 非法模板建库产生半残 workflow | mitigate | loader 在 acreate 前调同一 validator，error→ValueError→400，建库前拒绝 |
| T-20-09 | Information Disclosure | 模板 description 暴露真实仓库 UUID/凭证 | mitigate | description 仅说明 payload 形态与"需预注册仓库 UUID + 凭证"前提，不写入任何真实 UUID/凭证值 |
| T-20-10 | Tampering | code_review_pipeline 接收伪造 repository_id 越权拉 diff | accept | repository_id 经 Friday 既有凭证/权限校验（运行态，Phase 14 防线）；本阶段仅对齐编辑态契约，不放宽运行态鉴权 |
</threat_model>

<verification>
- `cd server && uv run pytest tests/workflows/test_template_loader.py -x -q` 全绿。
- `cd server && uv run pytest tests/workflows -q` 零回归。
- 4 模板经 validator 零 error；注入断裂失败；loader 建库前拒绝。
</verification>

<success_criteria>
- daily_summary / code_review_pipeline 修复，4 模板 validator 零 error。
- code_review_pipeline 按方案 A 契约对齐 + payload 文档化（务实终态）。
- 注入 schema 可判定断裂 → 测试失败（TPL-02 真守护）。
- loader 建库前同源校验拒绝非法模板（TPL-03）。
</success_criteria>

## Artifacts this phase produces

| Artifact | Type | Provides |
|----------|------|----------|
| `server/workflows/templates/daily_summary.json` | modified | 字段对齐真实输出（body/text） |
| `server/workflows/templates/code_review_pipeline.json` | modified | 方案 A 契约对齐 + payload 文档化 |
| `server/workflows/templates/loader.py` | modified | acreate 前同源图校验 |
| `server/tests/workflows/test_template_loader.py` | modified | TPL-01/02/03 守护测试 |

<output>
Create `.planning/phases/20-validation/20-03-SUMMARY.md` when done
</output>
