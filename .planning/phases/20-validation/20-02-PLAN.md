---
phase: 20-validation
plan: 02
type: execute
wave: 1
depends_on: ["20-01"]
files_modified:
  - server/workflows/api/serializers.py
  - server/workflows/api/views.py
  - server/tests/workflows/test_api.py
autonomous: true
requirements: [VAL-01, VAL-02]
must_haves:
  truths:
    - "bulk-update 保存含坏 config / 坏 handle / 不可解析 nodes.* 变量引用的图返回 HTTP 400 + {errors:[{node_id|edge_id, field_path, reason, severity}]} 并整体回滚（不落库）"
    - "合法工作流 bulk-update 保存行为零变化（不新增误拒）"
    - "单节点 create（WorkflowNodeCreateSerializer）对不合 schema 的 config 返回 400（闭合既有缺口）"
    - "POST /api/workflows/{id}/validate/ 与 POST /api/workflows/validate/ 调同一 WorkflowGraphValidator，返回 {errors,warnings} 且不写库"
    - "未知 node_type 触发 serializer 错误分支返回 400 而非 500（修复 NodeRegistry.list_types 缺失）"
  artifacts:
    - path: "server/workflows/api/views.py"
      provides: "bulk-update/import 接入 validator + dry-run @action（detail True/False）"
      contains: "WorkflowGraphValidator"
    - path: "server/workflows/api/serializers.py"
      provides: "WorkflowNodeCreateSerializer config 校验 + list_types 修复"
      contains: "validate_config"
    - path: "server/tests/workflows/test_api.py"
      provides: "VAL-02 写入路径 400 + 合法不误拒 + dry-run 集成测试"
      contains: "validate"
  key_links:
    - from: "server/workflows/api/views.py"
      to: "server/workflows/validation/graph_validator.py"
      via: "bulk-update / dry-run / import 调 WorkflowGraphValidator.validate"
      pattern: "WorkflowGraphValidator"
    - from: "server/workflows/api/views.py"
      to: "transaction.atomic 回滚"
      via: "validator error → raise ValidationError → 400 + 回滚"
      pattern: "raise.*ValidationError"
---

<objective>
把 20-01 的 `WorkflowGraphValidator` 接入全部后端写入路径（bulk-update、单节点/边 CRUD、import），并新增 dry-run 校验端点（D-04），使非法工作流在写库前被结构化拒绝（VAL-02），合法工作流保存零变化。dry-run 与真实保存调用同一 validator 实例方法，保证同源（Pitfall 5）。顺手闭合 `WorkflowNodeCreateSerializer` 的 config jsonschema 缺口与 `NodeRegistry.list_types()` 误用（Pitfall 7）。

Purpose: VAL-02 的"保存即合法、结构化错误、合法不误拒"在此落地，VAL-01 的"多写入路径共用"在此兑现。模板 loader 接入（TPL-03）由 20-03 单独处理（不同文件，可并行）。
Output: serializers.py（Create 路径补 config 校验 + list_types 修复）、views.py（写入路径接入 + 两个 validate action）、test_api.py（VAL-02 集成测试）。
</objective>

<execution_context>
@.cursor/gsd-core/workflows/execute-plan.md
@.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-validation/20-CONTEXT.md
@.planning/phases/20-validation/20-RESEARCH.md
@.planning/phases/20-validation/20-PATTERNS.md
@.planning/phases/20-validation/20-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 闭合 WorkflowNodeCreateSerializer config 缺口 + 修复 list_types 误用</name>
  <files>server/workflows/api/serializers.py</files>
  <read_first>
    - server/workflows/api/serializers.py L226-238（WorkflowNodeSerializer.validate：node_class.validate_config → ValidationError({"config": errors}) 范式）
    - server/workflows/api/serializers.py L241-265（WorkflowNodeCreateSerializer：当前仅 validate_node_type，缺 config 校验）
    - server/workflows/api/serializers.py L213 / L467（NodeRegistry.list_types() 误用——registry 无此方法，Pitfall 7）
    - server/workflows/nodes/registry.py L92-113（get/get_all/get_all_schemas；列举类型用 list(get_all().keys())）
    - 20-PATTERNS.md L165-214（serializers analog + Pitfall 7 + validate_config 复用）
  </read_first>
  <action>
    1. 给 `WorkflowNodeCreateSerializer` 增 `validate(self, attrs)`，复制 `WorkflowNodeSerializer.validate`（L226-238）的 `validate_config` 调用：取 `node_type`（create 路径恒在 attrs 中）+ `config = attrs.get("config", {})`，`NodeRegistry.get(node_type)` 命中则 `errors = node_class.validate_config(config)`，非空 `raise serializers.ValidationError({"config": errors})`。保留既有 `validate_node_type`。
    2. 修复 Pitfall 7：将 serializers.py L213、L467 的 `NodeRegistry.list_types()` 改为 `list(NodeRegistry.get_all().keys())`（两处）。保持错误消息文案不变（仅替换列举来源）。
    保持 ruff line-length 100、中文注释风格。
  </action>
  <verify>
    <automated>cd server && uv run python -c "import ast,sys; src=open('workflows/api/serializers.py').read(); assert 'list_types' not in src, 'list_types still present'; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "list_types" server/workflows/api/serializers.py` 输出 0。
    - WorkflowNodeCreateSerializer 含 `validate(` 方法且调用 `validate_config`：`grep -n "validate_config" server/workflows/api/serializers.py` 至少两处（既有 + Create 路径）。
    - `cd server && uv run pytest tests/workflows/test_api.py -k node -x -q` 通过（含新增 create 坏 config → 400 用例，Task 3 编写后联动）。
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: 写入路径接入 validator + dry-run 双端点</name>
  <files>server/workflows/api/views.py</files>
  <read_first>
    - server/workflows/api/views.py L230-316（_bulk_update_nodes_and_edges：transaction.atomic、_resolve_short_ids L252、rewrite_template_refs L287-303、edges 重建 L304-316）
    - server/workflows/api/views.py L460-494（import_workflow @action detail=False 范式 + afrom_json）
    - server/workflows/api/views.py L606-632（bulk_update view：sync_to_async(_bulk_update_nodes_and_edges) L620）
    - server/workflows/api/views.py L645-682（from_template @action：ValueError→400 已捕获 L675-676——loader 接入在 20-03）
    - server/workflows/api/views.py L329（WorkflowPermission 已先于业务执行；L258/L291 workflow 作用域过滤）
    - 20-RESEARCH.md Pattern 4（接入点表）、Pattern 5（dry-run 双形态 + 路由不冲突）、Pitfall 6（short_id 收敛后/commit 前校验；变量用 short_id 空间、edge/handle 用 UUID 空间，节点 dict 同带 id 与 short_id）
    - 20-PATTERNS.md L218-256（views analog A/B + async/sync 边界）
  </read_first>
  <action>
    1. **bulk-update 接入（Pitfall 6 时机）**：在 `_bulk_update_nodes_and_edges` 的 `transaction.atomic()` 内、`_resolve_short_ids` + 引用重写**之后**、提交（edges 重建/返回）**之前**，用**最终状态**构造 validator 输入：节点 dict 同时带 `id`（UUID）与 `short_id`（最终收敛值）+ `node_type` + `config`；边 dict 带 `id`?/`source_node_id`/`target_node_id`(UUID 空间)/`source_handle`/`target_handle`。调 `WorkflowGraphValidator().validate(nodes, edges)`，若 `result["errors"]` 非空 → `raise serializers.ValidationError({"errors": result["errors"], "warnings": result["warnings"]})`（atomic 自动回滚 → DRF 400）。warnings 不阻断。validator 纯 CPU 无 DB，在该同步函数体内同步调用（已整体 sync_to_async）。
    2. **import 接入**：在 `import_workflow`（或 `Workflow.afrom_json`）入库前用解析出的 nodes/edges 调同一 validator，error 非空 → 400 结构化（不入库）。
    3. **单边 CRUD handle 校验**：单边 POST/PUT/PATCH（views.py L548-604，现仅验节点归属）补 handle 子集校验——构造该边 + 两端节点 dict 调 validator 的 handle 规则（或复用 validator 全量、仅取 edge 相关 issue）。简单实现：调 `WorkflowGraphValidator().validate(nodes_of_workflow, [edge])` 并过滤 reason ∈ {edge_node_missing, invalid_source_handle, invalid_target_handle}。
    4. **dry-run 双端点（D-04 / Pattern 5）**：
       - `@action(detail=True, methods=["post"], url_path="validate")` → `/api/workflows/{id}/validate/`：入参可选 `{nodes, edges}`（校验草图），缺省取该 workflow DB 现状构造输入；走 `WorkflowPermission`。
       - `@action(detail=False, methods=["post"], url_path="validate")` → `/api/workflows/validate/`：必填 `{nodes, edges}`，仅校验请求体（不读库，无越权面）。
       - 两者均直接调同一 `WorkflowGraphValidator().validate(...)`，返回 200 + `{"errors":[], "warnings":[]}`，**不写库**。
    保持 async view 既有范式；中文注释；ruff line-length 100。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/workflows/test_api.py -k "bulk or validate or node" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "WorkflowGraphValidator" server/workflows/api/views.py` 命中 bulk-update / import / dry-run 接入。
    - 两个 validate action 存在：`grep -c "url_path=\"validate\"" server/workflows/api/views.py` 输出 2。
    - bulk-update 校验位于 short_id 收敛/重写之后（接入代码在 `_resolve_short_ids` 调用行之后、`raise serializers.ValidationError` 在 atomic 块内）。
    - `cd server && uv run pytest tests/workflows/test_api.py -k "bulk or validate or node" -x -q` 全绿。
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: test_api.py VAL-02 集成测试（400 路径 + 合法不误拒 + dry-run）</name>
  <files>server/tests/workflows/test_api.py</files>
  <read_first>
    - server/tests/workflows/test_api.py（既有 bulk-update / node create 用例与 client/fixture 范式；APIClient + django_db）
    - 20-RESEARCH.md Validation Architecture → Test Map（VAL-02 行：bulk 400 / node create / dry-run 命令）
    - 20-VALIDATION.md Per-Task Verification Map（VAL-02 wave 1）
    - 20-PATTERNS.md L370（test_api analog：bulk-update 非法 → 400 + 合法不误拒回归）
  </read_first>
  <action>
    扩展 `tests/workflows/test_api.py`（django_db）新增用例：
    - bulk-update 保存坏 config（节点 config 不合 schema）→ 400，响应体 `errors` 含 `reason="config_schema_invalid"` 与 node_id；DB 未写入（节点数不变/事务回滚）。
    - bulk-update 保存坏 handle（source_handle 不在端口）→ 400，errors 含 `invalid_source_handle` + edge_id/field_path。
    - bulk-update 保存含 `{{nodes.ghost.x}}` → 400，errors 含 `node_not_found`。
    - **合法工作流 bulk-update 保存零变化（不误拒）**：构造健康图（含 default 边）保存 → 200，落库成功。
    - 单节点 create 坏 config（WorkflowNodeCreateSerializer）→ 400（含 config 错误）。
    - dry-run：`POST /api/workflows/validate/`（detail=False，请求体含坏图）→ 200 + `{errors:[...非空], warnings}`，不写库；`POST /api/workflows/{id}/validate/`（detail=True，合法草图）→ 200 + errors==[]。
    - dry-run 与 bulk-update 同源回归：同一坏图，dry-run 的 errors reason 集合 == bulk-update 400 体的 reason 集合（Pitfall 5）。
    用既有 fixture/client 范式，中文 docstring，ruff line-length 100。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/workflows/test_api.py -k "bulk or validate or node" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - 上述命令全绿；至少含一条"合法保存 200 不误拒"回归用例与一条"dry-run 与 bulk-update 同源"用例。
    - 400 用例断言响应体含 `errors` 列表且每条有 `reason`/`field_path`/(`node_id` 或 `edge_id`)。
    - `cd server && uv run pytest tests/workflows -q` 零回归。
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → DRF 写入端点 | 未受信 nodes/edges/config 经 bulk-update/CRUD/import/dry-run 进入 |
| client → dry-run detail=True | 针对已存 workflow 的校验，跨用户读取风险 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-04 | Tampering | 非法图绕过校验直接落库 | mitigate | 五写入路径（本计划 4 条 + 20-03 loader）全部在写库前/事务内调同一 validator，error→400+回滚 |
| T-20-05 | Elevation of Privilege | dry-run detail=True 越权读他人 workflow | mitigate | detail=True 复用 WorkflowPermission + workflow 作用域过滤（views.py L329/L258/L291）；detail=False 仅校验请求体不读库 |
| T-20-06 | Information Disclosure | 400 错误体回显上游输出/config 值 | mitigate | 400 体仅透传 ValidationIssue（node_id/edge_id/field_path/reason/message 键名），不含值（T-17-01；Task 3 断言） |
| T-20-07 | Information Disclosure | 未知 node_type 触发 500 暴露栈 | mitigate | 修复 list_types 缺失（Pitfall 7），未知类型走 400 结构化分支 |
</threat_model>

<verification>
- `cd server && uv run pytest tests/workflows/test_api.py -k "bulk or validate or node" -x -q` 全绿。
- `cd server && uv run pytest tests/workflows -q` 零回归。
- grep 守护：views 接入 WorkflowGraphValidator、两个 validate action、serializers 无 list_types。
</verification>

<success_criteria>
- 非法 bulk-update/单节点 create/import 返回结构化 400 并回滚；合法保存零变化。
- dry-run 双端点与真实保存同源（同一 validator，同口径输入）。
- list_types 误用修复，未知 node_type 走 400。
</success_criteria>

## Artifacts this phase produces

| Artifact | Type | Provides |
|----------|------|----------|
| `server/workflows/api/serializers.py` | modified | Create 路径 config 校验 + list_types 修复 |
| `server/workflows/api/views.py` | modified | 写入路径接入 validator + dry-run 双端点 |
| `server/tests/workflows/test_api.py` | modified | VAL-02 400 路径 + 合法不误拒 + dry-run 同源集成测试 |

<output>
Create `.planning/phases/20-validation/20-02-SUMMARY.md` when done
</output>
