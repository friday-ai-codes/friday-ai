---
phase: 100-learning-case-mcp
plan: 03
type: execute
wave: 2
depends_on: [100-01]
files_modified:
  - server/knowledge/sources/mcp_coding_plan.py
  - server/knowledge/sources/mcp_repository_analysis.py
  - server/knowledge/sources/mcp_execution_trace.py
  - server/mcp_tools/views.py
  - server/mcp_tools/work_item_execution_service.py
  - server/tests/knowledge/test_mcp_artifact_sources.py
autonomous: true
requirements: [KNOW-03]

must_haves:
  truths:
    - "用户走 MCP 建的 coding plan / 仓库分析 / 执行 trace 各自产生 KnowledgeEntity（kind 复用既有值：plan→tech_plan、analysis→document、trace→code_change）并可被 search_delivery_knowledge 召回"
    - "从 plan 实体沿 IMPLEMENTED_BY 边可达 execution 实体，execution payload 含 PR 信息（mr_url/commit_sha）；plan 若有 work_item 三元组则从 work_item 锚沿边可达 plan（端到端自动化断言）"
    - "MCP plan 实体与 chat coding_plan 实体是不同实体（不同 source_kind → 不同 uuid5，不重复入图），可反查时经 RELATES_TO 边显式关联"
    - "重复摄取同一 MCP 产物幂等：实体数不变、内容未变不翻版本"
  artifacts:
    - path: "server/knowledge/sources/mcp_coding_plan.py"
      provides: "McpCodingPlan → tech_plan 事件（+work_item 锚双事件 + REFERENCES analysis + RELATES_TO chat plan）"
      exports: ["normalize", "build_plan_event"]
    - path: "server/knowledge/sources/mcp_repository_analysis.py"
      provides: "McpRepositoryAnalysis → document 事件"
      exports: ["normalize"]
    - path: "server/knowledge/sources/mcp_execution_trace.py"
      provides: "McpCodingExecutionTrace → [plan 锚, code_change] 双事件 + IMPLEMENTED_BY 边"
      exports: ["normalize"]
    - path: "server/tests/knowledge/test_mcp_artifact_sources.py"
      provides: "三 normalizer 单测 + 写入点触发断言 + plan→execution→work_item 边可达性 E2E + 幂等重摄"
  key_links:
    - from: "server/knowledge/sources/mcp_execution_trace.py"
      to: "server/knowledge/sources/mcp_coding_plan.py build_plan_event"
      via: "锚事件复用同一构造函数（content 逐字节一致，否则重摄版本 ping-pong）"
      pattern: "build_plan_event"
    - from: "server/mcp_tools/views.py"
      to: "knowledge.ingestion.aschedule_ingestion"
      via: "AnalyzeRepositoryView / CreateCodingPlanView / ImproveCodingPlanView / ExecuteCodingPlanView acreate 后投递"
      pattern: "IngestionRequest\\(.*mcp_(coding_plan|repository_analysis|execution_trace)"
    - from: "server/mcp_tools/work_item_execution_service.py"
      to: "knowledge.ingestion.aschedule_ingestion"
      via: "_ensure_coding_plan / _execute_one_task acreate 后投递"
      pattern: "mcp_coding_plan|mcp_execution_trace"
---

<objective>
KNOW-03 落地：MCP 链路三类产物（`McpCodingPlan` / `McpRepositoryAnalysis` / `McpCodingExecutionTrace`）
入统一知识库。三个 normalizer + 全部 acreate 写入点投递钩子，plan→execution（IMPLEMENTED_BY）、
plan↔work_item（双事件锚）、plan↔chat coding_plan（RELATES_TO，可反查时）边关系落地，
端到端边可达性自动化断言。

Purpose: MCP 产物可被 `search_delivery_knowledge` 召回，与 chat 链产物在同一图谱内显式关联不重复入图。
Output: 3 个 normalizer 模块 + 5 处写入点钩子 + 单测/触发/E2E/幂等四组测试。
</objective>

<context>
@.planning/phases/100-learning-case-mcp/100-CONTEXT.md （locked：「MCP 三类产物入图」节——kind 复用既有值 plan→tech_plan / analysis→document / trace→code_change，不扩新 kind；写入点清单；边语义）
@server/knowledge/sources/mcp_plan.py （双事件 work_item 锚范本）
@server/knowledge/sources/task_result.py （[锚, code_change] 双事件 + IMPLEMENTED_BY 边挂锚事件的先例——L219-247「锚事件短路重摄同源拼法」是本 plan 的关键纪律）
@server/knowledge/ingestion.py （IngestionEvent/EdgeSpec 契约）
@server/mcp_tools/models.py （McpRepositoryAnalysis L10-56 / McpCodingPlan L59-110 / McpCodingPlanVersion L113-160 / McpCodingExecutionTrace L163-259 字段全集）
@server/mcp_tools/views.py （写入点：AnalyzeRepositoryView L1778、CreateCodingPlanView L1894/1903、ExecuteCodingPlanView L2083；lazy import 投递范式见 technical_plan_service.py L497）
@server/mcp_tools/work_item_execution_service.py （写入点：_ensure_coding_plan L239/247、_execute_one_task L301）
@server/tests/knowledge/test_triggers.py + test_ingestion.py （测试范式）
</context>

<tasks>

<task type="auto">
  <name>Task 1: mcp_repository_analysis + mcp_coding_plan normalizer</name>
  <files>server/knowledge/sources/mcp_repository_analysis.py, server/knowledge/sources/mcp_coding_plan.py</files>
  <action>
两个新模块（中文 docstring 标注 Phase 100 / KNOW-03，行文照 mcp_plan.py）：

**mcp_repository_analysis.py**（单事件）：
- `select_related("repository")` 按 id 查询，缺失 → warning + `[]`。
- 事件：`kind=EntityKind.DOCUMENT`（locked：analysis→document，kind 按内容归类、来源经 source_kind 区分）、`origin=EntityOrigin.MCP`、`source_kind="mcp_repository_analysis"`、`source_id=str(analysis.id)`、`title=f"{repository.name} 仓库分析" + (f"（{focus}）" if focus)`、`content`＝summary JSON 的 markdown 组装（Claude's Discretion：遍历 summary dict 的 str/list 值拼段落，evidence 取 file_path+reason 列表）、`payload={"repository_id", "branch", "focus", "status", "evidence_count"}`（摘要，不复制 summary 全文）、`repository_id=str(analysis.repository_id)`、`space_id=None`、`event_time=analysis.created_at`、无出边。

**mcp_coding_plan.py**（主事件 + 可选 work_item 锚双事件）：
- 公开 `build_plan_event(plan, version, *, edges=()) -> IngestionEvent` 纯构造函数——**mcp_execution_trace.py 的锚事件必须复用它**（task_result.py L229「锚事件短路重摄同源拼法」纪律：content 逐字节一致才不会两 normalizer 互相翻版本）。构造：`kind=EntityKind.TECH_PLAN`、`origin=EntityOrigin.MCP`、`source_kind="mcp_coding_plan"`、`source_id=str(plan.id)`、`title=plan.title`、`content`＝markdown 组装（`# {title}\n\n## 需求\n{requirement}\n\n## 步骤\n{steps 逐条}\n\n## 测试计划\n{test_plan}\n\n## 风险\n{risks}`，素材取**最新** `McpCodingPlanVersion`——`plan.versions.order_by("-version").afirst()` 由 normalize 查好传入）、`payload={"requirement"截断, "affected_files", "version", "repository_id", "branch", "status", "analysis_id"}`、`repository_id=str(plan.repository_id)`、`space_id=None`、`event_time=plan.created_at`。
- `normalize`：查 plan + 最新 version（version 缺失 → warning + `[]`）；主事件出边：
  a. `plan.analysis_id` 非空 → `EdgeSpec(REFERENCES, generate_entity_id("document", "mcp_repository_analysis", str(plan.analysis_id)))`；
  b. **RELATES_TO chat coding_plan（best-effort，locked「如可反查到同 work_item」）**：经 `McpWorkItemRepoTask.objects.filter(coding_plan_id=plan.id).select_related("technical_plan__context")` 反查 work_item 三元组；反查到后再查 `KnowledgeEntity.objects.filter(kind="tech_plan", source_kind="coding_plan")` 中经 `KnowledgeEdge` 与同一 work_item 锚实体有活跃边者，逐个挂 `EdgeSpec(RELATES_TO, entity.id)`；任一环节缺料静默跳过（不 warning 刷屏，这是常态）。
- **work_item 锚双事件**：上述反查拿到三元组（feishu_project_key/work_item_type/work_item_id 齐备）时，照抄 mcp_plan.py 锚事件构造（source_kind="feishu_work_item"、三元组 source_id、轻量锚 content），锚出边 `EdgeSpec(RELATES_TO, generate_entity_id("tech_plan", "mcp_coding_plan", str(plan.id)))`——**禁用 HAS_PLAN**：HAS_PLAN 是 exclusive 单方案语义（mcp_plan.py L98），若复用会把 work_item 既有的 HAS_PLAN→mcp_technical_plan 活跃边打失效；RELATES_TO 非排他，安全（在模块 docstring 记录此决策理由）。返回 `[work_item_event, plan_event]`；无三元组 → `[plan_event]` 单事件。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/knowledge/test_mcp_artifact_sources.py -x -q -k "analysis or coding_plan"</automated>
  </verify>
  <done>两 normalizer 三分支（正常/锚缺料/源缺失）行为正确；HAS_PLAN 禁用决策落 docstring；build_plan_event 可被外部复用。</done>
</task>

<task type="auto">
  <name>Task 2: mcp_execution_trace normalizer（[plan 锚, code_change] 双事件）</name>
  <files>server/knowledge/sources/mcp_execution_trace.py</files>
  <action>
照 task_result.py 双事件结构：

- `select_related("plan", "plan_version", "repository")` 查 trace，缺失 → warning + `[]`。
- **code_change 主事件**：`kind=EntityKind.CODE_CHANGE`、`origin=EntityOrigin.MCP`、`source_kind="mcp_execution_trace"`、`source_id=str(trace.id)`、`title=f"{repository.name} MCP 执行 @ {trace.commit_sha[:8] or trace.status}"`、`content`＝执行摘要 markdown（branch_summary 摘要 + file_changes 文件路径列表 + test_results 概要 + error 截断；**diff 原文（last_diff）绝不进 content/payload**——task_result.py T-14-24 payload 纪律同款）、`payload={"plan_id", "plan_version_id", "status", "branch_name", "target_branch", "commit_sha", "mr_url"（自 trace.mr_result 取 url 键，缺为 ""）, "file_change_count", "retry_count"}`（**execution→PR 信息入 payload**，locked decision）、`repository_id=str(trace.repository_id)`、`space_id=None`、`event_time=trace.completed_at or trace.created_at`（aware 化处理照 task_result.py L170-172）。
- **plan 锚事件**：`from knowledge.sources.mcp_coding_plan import build_plan_event` 复用构造（同源拼法纪律），edges 挂 `EdgeSpec(EdgeRelation.IMPLEMENTED_BY, generate_entity_id("code_change", "mcp_execution_trace", str(trace.id)))`——边方向与 task_result.py L219-225 完全一致（IMPLEMENTED_BY = 方案→代码变更，挂锚事件）。plan 最新 version 缺失 → 降级单事件（code_change 独立入图）+ warning。
- 返回 `[plan_anchor_event, code_change_event]`（锚在前）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/knowledge/test_mcp_artifact_sources.py -x -q -k "execution_trace"</automated>
  </verify>
  <done>双事件 + IMPLEMENTED_BY 边方向与 task_result.py 先例一致；锚事件 content 与 mcp_coding_plan normalizer 产出逐字节一致（专项断言）；diff 原文零泄漏。</done>
</task>

<task type="auto">
  <name>Task 3: 5 处写入点投递钩子</name>
  <files>server/mcp_tools/views.py, server/mcp_tools/work_item_execution_service.py</files>
  <action>
按 locked decision「写入点投递」逐点挂钩（一律 `from knowledge import ingestion` lazy import + `await ingestion.aschedule_ingestion(...)`，不传 initiated_by_user_id——MCP 链口径与 technical_plan_service.py L499 一致；aschedule_ingestion 自吞异常，无需再包 try/except）：

1. `views.py AnalyzeRepositoryView`（L1778 `McpRepositoryAnalysis.objects.acreate` 后）：`IngestionRequest("mcp_repository_analysis", str(artifact.id), "mcp_analysis_created")`。
2. `views.py CreateCodingPlanView`（L1894-1915 plan+version acreate 后）：`IngestionRequest("mcp_coding_plan", str(plan.id), "mcp_coding_plan_created")`。
3. `views.py ImproveCodingPlanView`（新版本 `McpCodingPlanVersion.objects.acreate` 后，L2044 version.tool_call 保存附近；grep `McpCodingPlanVersion.objects.acreate` 确认全覆盖）：`IngestionRequest("mcp_coding_plan", str(plan.id), "mcp_coding_plan_improved")`——同一 plan 重摄，content 变更走版本翻转，未变走 hash 短路，天然幂等。
4. `views.py ExecuteCodingPlanView`（L2083 trace acreate 后、dispatch 之前或之后均可，放 `refresh_execution_trace` 之后最佳——摄取时刻 trace 状态更完整）：`IngestionRequest("mcp_execution_trace", str(trace.id), "mcp_execution_created")`。
5. `work_item_execution_service.py`：`_ensure_coding_plan`（L239/247 acreate 分支末尾、return 前）投递 `("mcp_coding_plan", str(plan.id), "mcp_work_item_plan_created")`；`_execute_one_task`（L301 trace acreate 分支，`refresh_execution_trace` 后）投递 `("mcp_execution_trace", str(trace.id), "mcp_work_item_execution_created")`。

各钩子紧邻处加一行中文注释：`# KNOW-03：MCP 产物入统一知识库（INV-6 唯一通路，on_commit + 后台，异常自吞不阻塞）`。不改任何响应外形、不改既有 `mcp_technical_plan` 投递点（L608）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/knowledge/test_mcp_artifact_sources.py -x -q -k trigger && uv run pytest tests/mcp_tools/test_planning_tools.py tests/mcp_tools/test_execution_tools.py -x -q</automated>
  </verify>
  <done>5 写入点全部投递（触发断言逐点覆盖）；mcp_tools 既有 planning/execution 测试零回归。</done>
</task>

<task type="auto">
  <name>Task 4: E2E 边可达性 + 幂等重摄测试</name>
  <files>server/tests/knowledge/test_mcp_artifact_sources.py</files>
  <action>
新建测试文件（transaction=True；工厂造 InteractionRun/Repository/McpRepositoryAnalysis/McpCodingPlan+Version/McpCodingExecutionTrace/McpWorkItemRepoTask 最小闭包，参照 test_learning_cases.py 与 test_triggers.py 工厂；mock embedding/Qdrant 全套同 100-02 Task 3 清单）。四组：

1. **TestNormalizers**：三 normalizer 各自的事件字段/kind/source_kind/payload 断言；execution_trace 锚事件 content 与 `mcp_coding_plan.normalize` 主事件 content **逐字节相等**（同源拼法回归防线）；diff 原文哨兵串（塞进 last_diff）不出现在任何事件 content/payload。
2. **TestTriggers**：monkeypatch `knowledge.ingestion.aschedule_ingestion`，分别走 5 个写入点（views 层用 mcp_client fixture 走 HTTP，或 service 层直调 `_ensure_coding_plan`/`_execute_one_task`——dispatch 相关 monkeypatch `dispatch_execution` 免真实派发），断言 source_kind/source_id/trigger 三元正确。
3. **TestEdgeReachability（ROADMAP criterion 3 端到端）**：真跑 `ingest()`（mock 向量）依次摄取 work_item task 挂载的 plan 与 trace → 断言：
   - `KnowledgeEdge` 活跃边存在：plan 实体 —IMPLEMENTED_BY→ trace 实体；
   - work_item 锚实体 —RELATES_TO→ plan 实体（锚双事件生效）；
   - trace 实体 latest version payload 的 `mr_url`/`commit_sha` 与 fixture 一致（plan→execution→PR 链完整）；
   - `generate_entity_id("tech_plan", "mcp_coding_plan", plan_id) != generate_entity_id("tech_plan", "coding_plan", chat_plan_id)`（不同实体不重复入图，natural key 隔离断言）。
4. **TestIdempotentReingest**：三类产物各 ingest 两次 → 实体数不变、current_version==1、第二次 skipped 短路（criterion 4 覆盖 MCP 面）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/knowledge/test_mcp_artifact_sources.py -x -q</automated>
  </verify>
  <done>四组全绿；plan→execution→work_item 沿边可达 + PR 信息入 payload 有自动化断言；幂等断言覆盖三类产物。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 容器/runner 回写字段（last_diff/runner_logs 等）→ 知识库 | 可被 runner 篡改的输出进入图谱 content |
| MCP 调用方输入 → plan requirement/summary → 向量库 | 外部文本入图 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-100-06 | Information Disclosure | mcp_execution_trace content/payload | mitigate | diff 原文（last_diff）与 runner_logs 全文不入 content/payload，只入统计摘要（task_result.py T-14-24 同款）；哨兵串测试兜底 |
| T-100-07 | Tampering | HAS_PLAN exclusive 误用 | mitigate | work_item→mcp_coding_plan 锚边强制 RELATES_TO 非排他，杜绝打失效既有 HAS_PLAN→technical_plan 边（Task 1 docstring 存档 + E2E 断言既有边仍活跃） |
| T-100-08 | Denial of Service | 5 写入点钩子 | mitigate | aschedule_ingestion 异常自吞，投递失败不反噬 MCP 工具响应 |
| T-100-SC | Tampering | 依赖安装 | accept | 零新依赖 |
</threat_model>

<verification>
- `cd server && uv run pytest tests/knowledge/test_mcp_artifact_sources.py tests/mcp_tools/ -q` 全绿。
- E2E 边断言含「work_item 既有 HAS_PLAN→mcp_technical_plan 边未被 RELATES_TO 锚边打失效」回归项。
</verification>

<success_criteria>
- ROADMAP 成功标准 3 全量：三类产物可召回（kind 过滤经 100-01 已修）、plan→execution→work_item 沿边可达、与 chat coding_plan 显式关联不重复入图。
- ROADMAP 成功标准 4（MCP 面）：三类产物重复摄取幂等。
</success_criteria>

<output>
完成后创建 `.planning/phases/100-learning-case-mcp/100-03-SUMMARY.md`
</output>
