---
phase: 102-knowledge-consumption
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - server/agents/tools/knowledge_read_tools.py
  - server/agents/chat_runner.py
  - server/initiatives/services/project_doc_service.py
  - server/knowledge/sources/project_doc.py
  - server/tests/agents/tools/test_knowledge_read_tools.py
  - server/tests/knowledge/test_project_doc_source.py
  - server/tests/initiatives/test_state_api_materialize_hook.py
autonomous: true
requirements: [KNOW-05, KNOW-06]

must_haves:
  truths:
    - "Chat 对话可经白名单新增的 search_learning_cases / search_project_context / read_project_doc 三个工具主动读知识"
    - "三个工具权限 fail-closed：无会话 owner / 非项目成员且非 public_org → 拒绝，零泄漏"
    - "Chat 链召回写 RetrievalTrace（conversation_id 关联）"
    - "IDE 上报 ProjectStateApi 后，STATE 文档物化被调度且物化内容含该 API 清单行（METHOD path — status）"
  artifacts:
    - path: "server/agents/tools/knowledge_read_tools.py"
      provides: "三个薄封装 chat 知识读工具（@tool + ToolResult + fail-closed）"
      min_lines: 100
    - path: "server/agents/chat_runner.py"
      provides: "白名单挂载：search_learning_cases 进 _INDEXED、search_project_context/read_project_doc 进 _PROJECT_READ + import 注册"
      contains: "knowledge_read_tools"
    - path: "server/initiatives/services/project_doc_service.py"
      provides: "upsert_state_api 成功后调度 STATE 文档物化"
    - path: "server/knowledge/sources/project_doc.py"
      provides: "STATE 类型文档 normalizer 追加 live API 清单内容"
      contains: "ProjectStateApi"
  key_links:
    - from: "server/agents/chat_runner.py"
      to: "server/agents/tools/knowledge_read_tools.py"
      via: "import 侧效应注册 + 白名单常量"
      pattern: "import agents.tools.knowledge_read_tools"
    - from: "server/agents/tools/knowledge_read_tools.py"
      to: "mcp_tools.learning_case_service.search_learning_cases"
      via: "薄封装（Phase 100 定版后的向量版 service，user kwarg）"
      pattern: "search_learning_cases"
    - from: "server/initiatives/services/project_doc_service.py"
      to: "_schedule_materialization"
      via: "upsert_state_api 成功后按 (project, STATE) 反查 doc_id 调度"
      pattern: "_schedule_materialization"
    - from: "server/knowledge/sources/project_doc.py"
      to: "initiatives.models.ProjectStateApi"
      via: "STATE 文档 normalize 时 live 查询 API 行拼入 content"
      pattern: "ProjectStateApi"
---

<objective>
KNOW-05 + KNOW-06：给 Chat 对话补三个白名单知识读工具（`search_learning_cases` /
`search_project_context` / `read_project_doc`，薄封装既有 service、权限 fail-closed、
召回写 RetrievalTrace）；修复 ProjectStateApi 不可检索的断链——`upsert_state_api` 只推飞书
不物化 + normalizer 只读 snapshot（API 行不在 snapshot 里），两点都补上（不建第二通路，
INV-6 单一摄取入口）。对齐 ROADMAP Phase 102 成功标准 2、3。

Purpose: Chat 是统一知识库的主消费面之一；IDE 上报的 API 清单入向量库后
`search_project_context`（MCP 与 Chat 两链）都能命中。
Output: 新工具模块 + chat_runner 白名单接线 + upsert 物化钩子 + normalizer STATE live
内容 + 三个测试文件。
</objective>

<context>
@.planning/phases/102-knowledge-consumption/102-CONTEXT.md
@server/agents/tools/delivery_knowledge_tools.py
@server/agents/tools/project_read_tools.py
@server/agents/chat_runner.py
@server/initiatives/services/project_doc_service.py
@server/knowledge/sources/project_doc.py
@server/mcp_tools/views.py

前置事实（已核实，2026-07-22）：
- 薄封装范本：`delivery_knowledge_tools.py` L51-60 `_resolve_conversation_user`（fail-closed
  L111-113）；`project_read_tools.py` L38-82 `_resolve_project_scope`（bound_project 反查 +
  成员/public_org fail-closed）、L85-86 `_deny`、L100-106 `_CONV_ID_PARAM`。两个 helper 均可
  直接 import 复用。
- Phase 100 定版后的向量版 service：`mcp_tools/learning_case_service.py` L254-263
  `search_learning_cases(*, query, work_item_type, repo_hints, file_hints, symbol_hints,
  limit, user)` → `list[dict]`（内部已 fail-soft，Qdrant 不可用返回 []）。
- chat_runner.py：L37-45 import 注册块；L84-117 `_INDEXED_TOOL_NAMES`（search_delivery_knowledge
  在 L114-116）；L123-130 `_PROJECT_READ_TOOL_NAMES`；`_get_tool_names` L248-283 会把
  `_PROJECT_READ_TOOL_NAMES` 在 bound_project_id 非空时追加。conversation_id 参数由
  chat_runner 从 args_schema 剔除后闭包注入（既有机制，声明参数即可）。
- MCP 端对应实现（对齐但不复用 view 层）：`SearchProjectContextView`（views.py L3315-3394，
  `search_similar(include_document_kind=True, project_ids=[...])` + RetrievalTrace CHUNK
  形态 L3373-3384）；`ReadProjectDocView`（L3461-3538，`DocContentService.get_doc_render`
  + RetrievalTrace FILE 形态）。
- KNOW-06 断链：`upsert_state_api`（project_doc_service.py L373-432）只调 schedule_doc_push；
  `_schedule_materialization(doc_id, initiated_by_user_id)`（L637-661，staticmethod，
  全吞异常）需要 **doc_id** 而非 DocType——须先按 (project_id, DocType.STATE) 反查
  ProjectDoc；normalizer（knowledge/sources/project_doc.py L76 起）读
  `last_synced_snapshot`，API 行只存在于 ProjectStateApi 表 + 飞书系统区，不在 snapshot。
- `DocContentService._resolve_system_text`（doc_content_service.py L190-203）的 STATE 渲染
  格式为 `f"{method} {path} — {status}"`——normalizer live 渲染对齐此格式。注意
  `get_doc_render` 的 rendered_markdown 同样来自 snapshot 缓存，**不能**作为 live API 来源；
  live 来源必须直查 ProjectStateApi 表。
- 测试先例：`tests/initiatives/test_memory_materialize_hook.py`（物化钩子 mock 断言范式）、
  `tests/knowledge/test_project_doc_source.py`（normalizer 内容断言范式）、
  `tests/agents/tools/test_delivery_knowledge_tools.py`（chat 工具测试范式）。
- Phase 101 并行执行中：本 plan 文件面与 101 的（coding_completion.py /
  learning_case_extraction.py / work_item_execution_service.py / coding.py /
  coding_graph.py / tools/ 迁移）零交集；chat_runner.py 的改动仅白名单列表追加 +
  import 行，非 101 触及文件。
</context>

<tasks>

<task type="auto">
  <name>Task 1: 新建 knowledge_read_tools.py——三个薄封装工具 + Chat 链 RetrievalTrace</name>
  <files>server/agents/tools/knowledge_read_tools.py</files>
  <action>
per CONTEXT KNOW-05 locked decisions。新建模块（中文 docstring，注明 Phase 102 KNOW-05、
薄封装铁律：不写新业务逻辑，只做权限前置 + service 转调 + 结果映射 + 埋点）：

共用部分：
- `from agents.tools.base import ToolResult, tool`；
  `from agents.tools.delivery_knowledge_tools import _resolve_conversation_user`；
  `from agents.tools.project_read_tools import _CONV_ID_PARAM, _deny, _resolve_project_scope`。
- trace helper `_record_chat_retrieval(kind, payload, *, conversation_id, user)`：调
  `interactions.ledger.arecord_retrieval_trace(None, kind=..., payload=...,
  user_id=str(user.id) if user else None, conversation_id=conversation_id,
  source="chat")`，整体 try/except 吞异常（best-effort）。payload 对齐 KNOW-04/
  search_project_context 形态：source/result_count/scores/top_score/duration_ms（+ 各工具
  维度键）。每工具另发一条 `logger.info("<tool>_done", ..., category="caller",
  component="agents.tools")` 结构化事件（对齐 project_read_tools._log 风格）。

工具一 `search_learning_cases`（挂 _INDEXED，凭会话 owner fail-closed）：
- `@tool(name="search_learning_cases", category=ToolCategory.KNOWLEDGE.value)`，参数：
  query（required, string）、work_item_type（string, default ""）、repo_hints/file_hints/
  symbol_hints（array of string）、limit（integer, default 5）、conversation_id（注入）。
  description 写清「检索历史任务经验（LearningCase：根因/解法/已验证测试）；统一向量检索，
  hints 作为查询增强与排序提权；开始新需求前找相似经验时用」+ 与 search_delivery_knowledge
  的分工（案例级经验 vs 交付实体图谱）。
- 实现：`user = await _resolve_conversation_user(conversation_id)`，None → fail-closed
  错误（照抄 delivery_knowledge_tools L111-113 文案）。函数内延迟 import
  `from mcp_tools.learning_case_service import search_learning_cases as _search_cases`
  （避免模块级跨 app import 环），kwargs 全量透传（hints 默认空 list），
  `user=user`。输出 `{query, results, total}`。perf_counter 计耗时，
  trace kind=CHUNK、payload source="chat_search_learning_cases"。

工具二 `search_project_context`（挂 _PROJECT_READ，凭 bound_project fail-closed）：
- 参数：query（required）、top_k（integer, default 5）、entity_kinds（array, 可选）、
  conversation_id（注入，required，用 `**_CONV_ID_PARAM` 展开）。description：「语义检索
  『当前项目』的全部沉淀（工作区 5 文件/记忆/工件/需求/方案），回答『项目里关于 X 的
  上下文』时用；仅项目对话有效」。
- 实现：`project, user, reason = await _resolve_project_scope(conversation_id)`，
  project None → `_deny(reason)`。
  `results = await DeliveryKnowledgeSearchService().search_similar(query, user=user,
  project_ids=[str(project.id)], entity_kinds=entity_kinds or None, top_k=top_k,
  include_document_kind=True)`（对齐 SearchProjectContextView L3348-3357 参数口径），
  `serialize_search_results` 序列化（knowledge.exposure）。输出
  `{project_id, query, results, total}`。trace kind=CHUNK、payload
  source="chat_search_project_context" + project_id/scores/top_score/duration_ms
  （对齐 views.py L3373-3384）。检索异常 → logger.exception + ToolResult(success=False)
  （照抄 delivery_knowledge_tools L129-131 范式）。
- service 单例：模块级 `_service = DeliveryKnowledgeSearchService()`（对齐
  delivery_knowledge_tools L23）。

工具三 `read_project_doc`（挂 _PROJECT_READ）：
- 参数：doc_type（required, string，description 列出合法值 memory/state/milestones/
  research/preflight）、conversation_id（注入，required）。description：「读『当前项目』
  工作区单文档的渲染 markdown 与 block 分区；看项目记忆/状态/里程碑原文时用」。
- 实现：权限同工具二；`rendered = await DocContentService().get_doc_render(
  project_id=str(project.id), doc_type=doc_type)`（initiatives.services.doc_content_service，
  函数内延迟 import）；rendered None → ToolResult(success=False, error="工作区文件不存在")。
  输出 `{project_id, doc_type, rendered_markdown, blocks}`（对齐 ReadProjectDocView 输出键，
  无 run_id）。trace kind=FILE、payload source="chat_read_project_doc" +
  doc_type/block_count/duration_ms（对齐 views.py L3519-3530）。
- 文末 `__all__` 导出三个工具函数。ruff line 100。
  </action>
  <verify>
    <automated>cd server && uv run python -c "import os, django; os.environ.setdefault('DJANGO_SETTINGS_MODULE','friday.settings'); django.setup(); import agents.tools.knowledge_read_tools as m; from agents.tools.base import _tool_registry; assert {'search_learning_cases','search_project_context','read_project_doc'} <= set(_tool_registry)" && uv run ruff check agents/tools/knowledge_read_tools.py</automated>
  </verify>
  <done>模块 import 后三个工具进 _tool_registry；三个工具全部权限前置 fail-closed、复用既有 service、召回/读取写 RetrievalTrace（conversation_id 关联、吞异常）。</done>
</task>

<task type="auto">
  <name>Task 2: chat_runner 白名单接线 + 工具行为测试</name>
  <files>server/agents/chat_runner.py, server/tests/agents/tools/test_knowledge_read_tools.py</files>
  <action>
1. `server/agents/chat_runner.py`：
   - L40-43 import 注册块按字母序插入
     `import agents.tools.knowledge_read_tools  # noqa: F401  # KNOW-05 chat 知识读工具`。
   - `_INDEXED_TOOL_NAMES`（L114-116 RECALL-02 注释块后）追加 `"search_learning_cases"`，
     配注释「KNOW-05（v0.17.0 Phase 102）：历史任务经验检索（LearningCase 向量版），
     凭 conversation_id 解析会话 owner fail-closed」。
   - `_PROJECT_READ_TOOL_NAMES`（L123-130）追加 `"search_project_context"`、
     `"read_project_doc"`，配注释「KNOW-05：项目沉淀语义检索 + 工作区文档读取
     （bound_project fail-closed）」。
2. 新建 `server/tests/agents/tools/test_knowledge_read_tools.py`（范式照抄
   test_delivery_knowledge_tools.py：pytest.mark.django_db + asyncio、service/底层函数
   monkeypatch AsyncMock，不触 Qdrant）：
   - 白名单断言：`from agents.chat_runner import _INDEXED_TOOL_NAMES,
     _PROJECT_READ_TOOL_NAMES`，断言 "search_learning_cases" ∈ _INDEXED_TOOL_NAMES、
     {"search_project_context", "read_project_doc"} ⊆ _PROJECT_READ_TOOL_NAMES、
     三个名字都 ∈ `_tool_registry`。
   - search_learning_cases：无效 conversation_id → success=False（fail-closed）；有效会话
     （建 Conversation + created_by）+ monkeypatch
     `agents.tools.knowledge_read_tools._search_cases`（或按实际 import 名 patch
     mcp_tools.learning_case_service.search_learning_cases）返回两条 dict → 断言
     output.total==2、底层收到 user=会话 owner、hints/limit 透传。
   - search_project_context：非成员 + 非 public_org 项目 → success=False "fail-closed"
     文案；成员会话 + monkeypatch search_similar 返回 [] → success=True、断言
     search_similar 收到 project_ids=[项目 id]、include_document_kind=True。
   - read_project_doc：成员会话 + monkeypatch DocContentService.get_doc_render 返回
     {"rendered_markdown": "# STATE", "blocks": []} → output 键齐；返回 None →
     success=False。
   - trace best-effort：monkeypatch arecord_retrieval_trace side_effect RuntimeError →
     工具仍 success=True。
   - fixture 建 Project/ProjectMember/Conversation(bound_project=...) 参考
     tests/agents/tools/ 既有 conftest 与 test_chat_project_recall.py 的建法，能复用就复用。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/agents/tools/test_knowledge_read_tools.py -v</automated>
  </verify>
  <done>三工具挂进对应白名单常量、import 侧效应注册生效；新测试文件全绿覆盖：注册/白名单、三工具正反路径（fail-closed 与成功）、参数透传、trace best-effort。</done>
</task>

<task type="auto">
  <name>Task 3: KNOW-06 断链修复——upsert 物化钩子 + normalizer STATE live 内容</name>
  <files>server/initiatives/services/project_doc_service.py, server/knowledge/sources/project_doc.py</files>
  <action>
per CONTEXT KNOW-06 locked decision（不建第二通路，INV-6：复用既有
project_doc 物化 + 摄取管线）：

1. `ProjectDocService.upsert_state_api`（L373-432）：在既有 `schedule_doc_push` 调用之后
   追加物化调度（方法尾部、return 之前）：
   - 反查 STATE 文档 id：`doc_id = await ProjectDoc.objects.filter(project_id=project_id,
     doc_type=DocType.STATE).values_list("id", flat=True).afirst()`（本文件已有 ProjectDoc/
     DocType import）。
   - `doc_id` 非空 → `await self._schedule_materialization(doc_id,
     initiated_by_user_id or getattr(actor, "id", None))`（既有 staticmethod L637-661，
     内部全吞异常 fail-soft，无需再包）；为空（工作区未 provision）→ 静默跳过。
   - 注释写明：KNOW-06——API 上报后 STATE 文档物化进 delivery_knowledge；
     `report_project_state` 批量上报会逐条触发，摄取管线 content_hash 短路保证重复调度
     幂等空操作，不需要额外去抖。
2. `knowledge/sources/project_doc.py` normalize()：STATE 类型文档改用 live 渲染内容
   （**仅 STATE**，其他 doc_type 行为逐字不变）：
   - 在 `raw_body` 取值后、`redact_secrets_in_text` 之前插入：若
     `doc.doc_type == DocType.STATE`（`from initiatives.models import ProjectDoc,
     ProjectStateApi` + DocType，均函数内延迟 import 对齐本文件风格），live 查询该项目
     API 清单：`ProjectStateApi.objects.filter(project_id=doc.project_id)
     .order_by("path").values("method", "path", "status")` 异步迭代（`async for`），
     每行渲染 `f"{r['method']} {r['path']} — {r['status']}"`——格式对齐
     `DocContentService._resolve_system_text`（doc_content_service.py L203）。
   - 行数上限 500（防极端膨胀）。有 API 行时 `raw_body = raw_body + "\n\n## API 清单\n"
     + "\n".join(lines)`（snapshot 人工区内容保留，live API 清单追加——snapshot 里没有
     API 行，这是断链根因）。无行则 raw_body 不动。
   - 既有 empty-body warning 判断保持基于原始 snapshot 不动；脱敏仍对拼接后全文执行
     （脱敏不可绕过）。
   - `_doc_title` 逻辑不动；docstring 与 `project_doc_rag_normalize_completed` 事件补
     `state_api_count` kv（category/component 既有值不变）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/knowledge/test_project_doc_source.py tests/initiatives/test_doc_sync_inv6_guard.py -x -q && uv run ruff check initiatives/services/project_doc_service.py knowledge/sources/project_doc.py</automated>
  </verify>
  <done>upsert_state_api 成功路径调度 STATE 文档物化（工作区未 provision 时静默跳过、绝不抛）；normalizer 对 STATE 文档产出的 content 含 live「METHOD path — status」API 行，非 STATE 文档产出逐字不变；既有 project_doc source 测试不回归。</done>
</task>

<task type="auto">
  <name>Task 4: KNOW-06 验收测试——上报 → 物化调度 → 物化内容命中链</name>
  <files>server/tests/initiatives/test_state_api_materialize_hook.py, server/tests/knowledge/test_project_doc_source.py</files>
  <action>
1. 新建 `server/tests/initiatives/test_state_api_materialize_hook.py`（范式照抄
   test_memory_materialize_hook.py）：
   - `test_upsert_state_api_schedules_state_doc_materialization`：建 Project + STATE
     ProjectDoc；monkeypatch `knowledge.ingestion.aschedule_ingestion` 为 AsyncMock →
     `await ProjectDocService().upsert_state_api(project_id=..., method="GET",
     path="/api/x", initiated_by_user_id=user.id)` → 断言 aschedule_ingestion 被 await 且
     IngestionRequest 的 source_kind=="project_doc"、source_id==str(STATE doc id)、
     initiated_by_user_id 透传。
   - `test_upsert_state_api_without_state_doc_skips_silently`：无 STATE ProjectDoc →
     upsert 正常返回 (api, True)、aschedule_ingestion 未被调用、不抛。
   - `test_materialization_failure_does_not_break_upsert`：aschedule_ingestion side_effect
     RuntimeError → upsert 仍正常返回（_schedule_materialization 吞异常）。
2. `server/tests/knowledge/test_project_doc_source.py` 追加（沿用该文件既有 fixture/建法）：
   - `test_state_doc_content_includes_live_api_rows`：建 STATE ProjectDoc（
     last_synced_snapshot 不含 API 行）+ 2 条 ProjectStateApi（如 GET /api/x implemented）
     → `normalize(IngestionRequest(source_kind="project_doc", source_id=doc.id, ...))` →
     断言 `events[0].content` 含 "GET /api/x — implemented" 与 "## API 清单"。
   - `test_non_state_doc_content_unchanged`：MEMORY 类型文档 + 同项目存在 ProjectStateApi
     行 → content 不含 API 行（仅 snapshot）。
   - `test_report_to_search_content_chain`（验收链，CI 无 Qdrant 的诚实边界）：
     monkeypatch aschedule_ingestion 捕获 IngestionRequest → `upsert_state_api` 上报 →
     用捕获的 request 直接跑 `normalize(request)` → 断言产出 DOCUMENT 事件 content 含
     该 API 行。注释写明：向量入库与 search_project_context 命中由既有
     ingestion/search_similar 测试与 include_document_kind 路径（Phase 85 CTX-01 用例）
     覆盖，本链断言到「摄取内容包含 API 清单」这一确定性环节。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/initiatives/test_state_api_materialize_hook.py tests/knowledge/test_project_doc_source.py -v</automated>
  </verify>
  <done>两个测试文件全绿：上报触发物化调度（含负路径与 fail-soft）、STATE normalizer 内容含 live API 行、非 STATE 零变化、上报→物化内容链有端到端断言。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM → chat 工具 | LLM 可自主调用白名单工具，conversation_id 由 runner 注入不可伪造 |
| 会话 owner → 项目数据 | bound_project 读权限决定项目沉淀可见性 |
| ProjectStateApi → 向量库 | IDE 上报内容经物化进入全员可检索面 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-102-05 | E (提权) | 三个 chat 工具 | mitigate | search_learning_cases 走 _resolve_conversation_user fail-closed；search_project_context/read_project_doc 走 _resolve_project_scope（成员/public_org）fail-closed；检索层再经 search_similar resolve_allowed_project_ids 二次收口 |
| T-102-06 | I (信息泄露) | normalizer STATE live 内容 | mitigate | 拼接后全文过 redact_secrets_in_text 才入图（脱敏不可绕过）；API 行只含 method/path/status 三元 |
| T-102-07 | I (信息泄露) | RetrievalTrace payload | mitigate | 只记计数/score/耗时/doc_type，不记召回正文 |
| T-102-08 | D (拒绝服务) | upsert 批量物化调度 | accept | 摄取管线 content_hash 短路 + aschedule_ingestion on_commit 去抖，重复调度为幂等空操作；单次上报 ≤200 条（serializer max_length） |
| T-102-SC | Tampering | 无新增包安装 | accept | 本 plan 零新依赖 |
</threat_model>

<verification>
- `cd server && uv run pytest tests/agents/tools/test_knowledge_read_tools.py tests/initiatives/test_state_api_materialize_hook.py tests/knowledge/test_project_doc_source.py -v` 全绿。
- `cd server && uv run pytest tests/mcp_tools/test_report_project_state.py tests/agents/tools/test_delivery_knowledge_tools.py -q` 不回归。
- `cd server && uv run ruff check agents/tools/knowledge_read_tools.py agents/chat_runner.py initiatives/services/project_doc_service.py knowledge/sources/project_doc.py` 干净。
</verification>

<success_criteria>
ROADMAP Phase 102 成功标准 2、3 成立：Chat 对话可经白名单新增三工具主动读知识（薄封装 +
fail-closed）；IDE 上报 ProjectStateApi 后经 STATE 文档物化路径入向量库，
search_project_context 可命中该 API 清单。
</success_criteria>

<output>
Create `.planning/phases/102-knowledge-consumption/102-02-SUMMARY.md` when done.
</output>
