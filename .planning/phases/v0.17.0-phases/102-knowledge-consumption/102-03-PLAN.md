---
phase: 102-knowledge-consumption
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - server/mcp_tools/serializers.py
  - server/tests/mcp_tools/test_schema_snapshot.py
  - server/tests/mcp_tools/test_skills_snapshot_guard.py
  - skills/skills/friday-memory/SKILL.md
  - skills/skills/friday-code/SKILL.md
autonomous: true
requirements: [UNIFY-04]

must_haves:
  truths:
    - "TOOL_SCHEMA_SNAPSHOT 覆盖全部 30 个注册工具（补 report_project_state）"
    - "快照测试含『urls 注册工具名集合 == snapshot 键集合』防漏断言，未来加工具漏 snapshot 即红"
    - "skills SKILL.md 引用的 MCP 工具名 ⊆ snapshot 键集（grep 守卫测试，P5 文档面漂移 CI 可见）"
    - "friday-memory 检索语义已改写为统一向量检索（hints=查询增强/提权，score=0-1 融合分）"
    - "friday-code 阶段二收录 reverse_lookup_requirements 证据→需求反查路由"
  artifacts:
    - path: "server/mcp_tools/serializers.py"
      provides: "TOOL_SCHEMA_SNAPSHOT 含 report_project_state（30 键）"
      contains: "report_project_state"
    - path: "server/tests/mcp_tools/test_schema_snapshot.py"
      provides: "整表断言更新 + registered==snapshot 集合守卫"
      contains: "urlpatterns"
    - path: "server/tests/mcp_tools/test_skills_snapshot_guard.py"
      provides: "skills 文档工具名 ⊆ snapshot 键集 grep 守卫"
    - path: "skills/skills/friday-memory/SKILL.md"
      provides: "向量检索语义定版文档"
    - path: "skills/skills/friday-code/SKILL.md"
      provides: "阶段二含 reverse_lookup_requirements 路由"
      contains: "reverse_lookup_requirements"
  key_links:
    - from: "server/tests/mcp_tools/test_schema_snapshot.py"
      to: "mcp_tools/urls.py"
      via: "import urlpatterns 提取 tools/<name>/ 路由名集合"
      pattern: "tools/"
    - from: "server/tests/mcp_tools/test_skills_snapshot_guard.py"
      to: "skills/skills/*/SKILL.md"
      via: "仓库根相对路径 glob + 反引号工具名 token 抽取"
      pattern: "SKILL.md"
---

<objective>
UNIFY-04 对外契约与文档对齐：`TOOL_SCHEMA_SNAPSHOT` 补最后一个漏网工具
`report_project_state`（urls 注册 30 个、snapshot 只有 29 键），并加两道防漏守卫
（注册==snapshot 集合断言 + skills 文档工具名 ⊆ snapshot grep 测试，PITFALLS P5 文档面）；
`@friday-ai-codes/skills` 文档与 Phase 100 定版后的新行为对齐（learning case 统一向量检索
语义、`reverse_lookup_requirements` 收录进 friday-code 技能路由）。对齐 ROADMAP Phase 102
成功标准 4。

Purpose: 守卫测试让契约漂移在 CI 就死，不靠人背（CONTEXT specifics）。
Output: snapshot 补全 + 两道守卫测试 + 两份 SKILL.md 更新。
</objective>

<context>
@.planning/phases/102-knowledge-consumption/102-CONTEXT.md
@server/mcp_tools/serializers.py
@server/mcp_tools/urls.py
@server/tests/mcp_tools/test_schema_snapshot.py
@skills/skills/friday-memory/SKILL.md
@skills/skills/friday-code/SKILL.md

前置事实（已核实，2026-07-22）：
- `TOOL_SCHEMA_SNAPSHOT`（serializers.py L608-744）29 键；`urls.py` L38-107 注册 30 个
  `tools/<name>/` 路由，唯一差集 = `report_project_state`。
- `report_project_state` 契约已对照实现核实：request 键即
  `ReportProjectStateRequestSerializer` 字段（serializers.py L590-600）：project_id /
  branch_name / repository_id / apis；response 键（views.py `_skip` L3148-3155 与成功路径
  L3263-3269 完全一致）：applied / reason / results / total_applied / run_id。
- `test_schema_snapshot.py` 现状：仅一条整表 `==` 断言，无「注册 == snapshot」集合守卫。
- skills 文档现存工具型 token（反引号 + 动词前缀 grep 已全量核实）：全部 ∈ snapshot 键集
  ∪ snapshot request/response 字段名集（`create_document`、`create_merge_requests` 是
  create_feishu_technical_plan / execute_work_item_repo_tasks 的 request 字段名，不是工具名
  ——守卫允许集必须含字段名，否则误报）。
- `friday-memory/SKILL.md` L41 仍是旧 token 打分语义（「用 work_item_type、repo_hints、
  file_hints、symbol_hints、limit 收窄范围」）；Phase 100 定版后的真实语义
  （learning_case_service.py L264-274 docstring）：hints 拼入查询文本做增强 + rerank 提权
  （不做摆设、非过滤收窄），score = 0-1 向量融合分（locked 定版），Qdrant 不可用 fail-soft
  返回空。
- `friday-code/SKILL.md` 阶段二（L39-60）5 步 + 跨仓追接口小节；全文无
  reverse_lookup_requirements（全树 grep 为零）；该工具在 snapshot 键集中已存在
  （request: repository_id/file_path/line/chunk_id/branch）。
- 文档正文中文、代码标识保留英文（doc-writing 规范）。
- Phase 101 并行执行中：本 plan 文件面（serializers.py / 两测试文件 / 两 SKILL.md）与
  101 文件面零交集。
</context>

<tasks>

<task type="auto">
  <name>Task 1: snapshot 补 report_project_state + 注册==snapshot 防漏守卫</name>
  <files>server/mcp_tools/serializers.py, server/tests/mcp_tools/test_schema_snapshot.py</files>
  <action>
per CONTEXT UNIFY-04 locked decisions：

1. `serializers.py` TOOL_SCHEMA_SNAPSHOT：在 `report_project_knowledge` 条目之后（对齐
   urls.py 注册顺序）插入：
   `"report_project_state": {"request": ["project_id", "branch_name", "repository_id", "apis"], "response": ["applied", "reason", "results", "total_applied", "run_id"]}`
   （键序按 serializer 字段声明序 / view 输出字典序，已对照实现核实——见 context 前置事实；
   执行时请再对照 views.py L3148-3155 与 L3263-3269 复核一遍键集一致）。
2. `test_schema_snapshot.py`：
   - 整表断言同步插入同一条目（保持与 snapshot 逐字一致）。
   - 新增 `test_registered_tools_match_snapshot`：
     `from mcp_tools.urls import urlpatterns`；用
     `re.fullmatch(r"tools/([a-z0-9_]+)/", str(p.pattern))` 从每个 pattern 提取工具名，
     `registered = {提取的名字}`；断言 `registered == set(TOOL_SCHEMA_SNAPSHOT)`，失败消息
     打印两方向差集（`registered - snapshot`＝「注册了但没进 snapshot」、反向＝「snapshot
     残留幽灵键」），让未来加工具漏 snapshot 直接红且可读。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/mcp_tools/test_schema_snapshot.py -v</automated>
  </verify>
  <done>snapshot 30 键含 report_project_state；整表断言绿；registered==snapshot 集合守卫存在且绿（手工从 snapshot 删任一键可令其红）。</done>
</task>

<task type="auto">
  <name>Task 2: skills 文档工具名 ⊆ snapshot grep 守卫测试</name>
  <files>server/tests/mcp_tools/test_skills_snapshot_guard.py</files>
  <action>
per CONTEXT UNIFY-04「grep 守卫」locked decision（P5 skills 双源漂移之文档面）。新建
`server/tests/mcp_tools/test_skills_snapshot_guard.py`（纯文件读取 + 正则，无 DB，无
django_db mark；中文 docstring 注明 Phase 102 UNIFY-04 / PITFALLS P5）：

1. 定位仓库根：`REPO_ROOT = Path(__file__).resolve().parents[3]`（
   server/tests/mcp_tools/ → 上三级 = 仓库根），`SKILL_FILES =
   sorted(REPO_ROOT.glob("skills/skills/*/SKILL.md"))`。
2. `test_skill_files_discovered`：断言 SKILL_FILES 非空（≥4 个：friday / friday-code /
   friday-feishu / friday-memory）——防路径解析漂移让主守卫静默空跑假绿。
3. `test_skill_tool_references_subset_of_snapshot`：
   - token 抽取正则（只认反引号内、MCP 工具名动词前缀开头的 snake_case——已对全树实测，
     零误报零漏报）：
     `` r"`((?:search|create|get|list|execute|improve|analyze|summarize|route|find|grep|read|report|lookup|reverse|update)_[a-z0-9_]+)`" ``
   - 允许集 = `set(TOOL_SCHEMA_SNAPSHOT)` ∪ snapshot 全部条目的 request/response 字段名
     集合（skills 文档会用反引号引用参数名，如 `create_document`/`create_merge_requests`
     是请求字段而非工具名——不加字段名并集会误报）。
   - 对每个 SKILL.md 收集 `文件 → 越界 token 集`，断言全空；失败消息逐文件列出越界
     token，提示「skills 文档引用了 snapshot 之外的工具名：要么工具没进
     TOOL_SCHEMA_SNAPSHOT，要么文档写了不存在的工具（P5 文档面漂移）」。
4. 不扫 references/*.md（locked 范围是 `skills/skills/**/SKILL.md` 主文档；HTTP 兜底
   references 里的示例契约由 snapshot 测试本体守）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/mcp_tools/test_skills_snapshot_guard.py -v</automated>
  </verify>
  <done>守卫测试全绿；在任一 SKILL.md 临时写入一个 snapshot 外的伪工具名（如 `search_nonexistent_thing`）能令其红（验证后撤销）。</done>
</task>

<task type="auto">
  <name>Task 3: skills 文档对齐——friday-memory 向量语义 + friday-code 反查路由</name>
  <files>skills/skills/friday-memory/SKILL.md, skills/skills/friday-code/SKILL.md</files>
  <action>
per CONTEXT UNIFY-04 locked decisions（文档正文中文、代码标识/工具名保留英文、不引入
snapshot 外的新工具名）：

1. `friday-memory/SKILL.md`「### 检索（规划新工作前）」小节（L39-42）改写检索语义：
   - 步骤 1 改为：`search_learning_cases` 现为**统一向量检索**（与
     `search_delivery_knowledge` 同一向量库与融合排序）；`query` 用需求标题/描述；
     `work_item_type`、`repo_hints`、`file_hints`、`symbol_hints` 作为**查询增强与排序提权**
     （拼入查询文本 + 命中加权 rerank），不再是关键词收窄过滤——hints 给得准会提升相关
     结果排名，给错不会漏召回；`limit` 控制返回条数。
   - 「### 护栏」补一条：结果 `score` 为 0-1 向量融合分（越高越相关），只用于同一次检索
     内排序比较，不要跨检索设绝对阈值；检索依赖服务端向量库，暂时不可用时返回空
     `results`（fail-soft），稍后重试即可。
   - 其余章节（交付知识/as_of/输出/HTTP 兜底）不动。
2. `friday-code/SKILL.md` 阶段二（L39-60）收录 `reverse_lookup_requirements`：
   - 步骤列表在 `find_related_chunks`（现步骤 3）之后插入新步骤：
     `reverse_lookup_requirements` ——证据→需求反查：已知代码位置（`repository_id` +
     `file_path`/`line` 或 `chunk_id`，可选 `branch`），反查它关联的需求（
     `related_work_items`）、文档（`related_documents`）与追溯路径（`paths`）。回答
     「这段代码是为哪个需求/方案改的」「改这里会影响哪些已交付需求」时用；后续步骤
     编号顺延。
   - 「## 模式判定」表追加一行：「这段代码对应哪个需求 / 为什么这么改」→ 阶段二
     （reverse_lookup_requirements）。
   - 其余阶段不动。
3. 两文件改完后复跑 Task 2 守卫（新增引用的 `reverse_lookup_requirements`、
   `related_work_items` 等均 ∈ snapshot 键/字段集，应保持绿）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/mcp_tools/test_skills_snapshot_guard.py tests/mcp_tools/test_schema_snapshot.py -q && rg -c "reverse_lookup_requirements" ../skills/skills/friday-code/SKILL.md && rg -c "向量" ../skills/skills/friday-memory/SKILL.md</automated>
  </verify>
  <done>friday-memory 检索小节与护栏为向量语义（无「收窄范围」旧表述）；friday-code 阶段二含 reverse_lookup_requirements 步骤 + 模式判定行；两道守卫测试全绿；文档未引入 snapshot 外工具名。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 外部客户端（Cursor/skills）→ MCP 工具面 | snapshot 是对外契约的唯一事实源 |
| skills 文档 → 用户 agent 行为 | 文档教错用法 = agent 按过时契约调用 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-102-09 | R (抵赖/漂移) | TOOL_SCHEMA_SNAPSHOT | mitigate | registered==snapshot 集合守卫，未来加工具漏 snapshot CI 即红 |
| T-102-10 | T (篡改/漂移) | skills 文档 | mitigate | 工具名 ⊆ snapshot grep 守卫（P5 文档面），越界 token CI 即红 |
| T-102-11 | I (信息泄露) | 本 plan 改动 | accept | 纯契约声明与文档，无运行时数据面变更 |
| T-102-SC | Tampering | 无新增包安装 | accept | 本 plan 零新依赖 |
</threat_model>

<verification>
- `cd server && uv run pytest tests/mcp_tools/test_schema_snapshot.py tests/mcp_tools/test_skills_snapshot_guard.py -v` 全绿。
- `cd server && uv run ruff check mcp_tools/serializers.py tests/mcp_tools/` 干净。
- 人工抽查：`rg -n "收窄范围" skills/skills/friday-memory/SKILL.md` 零命中。
</verification>

<success_criteria>
ROADMAP Phase 102 成功标准 4 成立：TOOL_SCHEMA_SNAPSHOT 覆盖全部注册工具（补
report_project_state），快照测试全绿且含「注册工具 == snapshot 键集合」防漏断言；
@friday-ai-codes/skills 文档与新行为对齐（learning case 向量检索语义、
reverse_lookup_requirements 收录进 friday-code 技能路由），并有 grep 守卫防未来漂移。
</success_criteria>

<output>
Create `.planning/phases/102-knowledge-consumption/102-03-SUMMARY.md` when done.
</output>
