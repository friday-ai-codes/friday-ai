---
phase: 113-2
plan: 04
type: execute
wave: 3
depends_on: [113-02, 113-03]
files_modified:
  - task/core/blueprint_context_wait.py
  - task/core/knowledge_tools.py
  - server/services/process_runtime/blueprint_repo_waves.py
  - server/services/process_runtime/blueprint_repo_plan.py
  - server/subagent/api/callbacks.py
  - task/tests/test_blueprint_context_wait.py
  - server/tests/services/process_runtime/test_blueprint_repo_waves.py
  - server/tests/services/process_runtime/test_blueprint_context_wait_redispatch.py
autonomous: true
requirements: [BUS-02]
prohibitions:
  - "禁止修改 server/codegraph/services/repo_router_v2.py"
  - "禁止修改 process_runtime 六冻结文件：decompose_segments.py / research_adapter.py / architect_merge_adapter.py / merged_plan.py / clarify_adapter.py / render.py"
  - "禁止修改 server/services/process_runtime/resume.py 与 server/repositories/services/charter_service.py"
  - "禁止修改 ConvergenceSessionEvent 既有类型；禁止修改 builtin_processes.py 的 _TECHNICAL_PLAN_STAGES / _ECHO_STAGES —— 本 plan 不碰 builtin_processes.py"
  - "禁止修改 server/system/settings_service.py 既有 getter"
  - "🔒 禁止修改 task/core/knowledge_tools.py 的公共 handler 工厂 `_make_knowledge_handler`：`timeout=60.0` 禁改、禁加 per-tool 覆盖参数、`quota_counter` 计数逻辑禁改、**禁加 callback 参数**（短等待不发心跳）。本 plan 对该文件唯一允许的改动：追加第 10 个 schema 表项 + 为 await 工具挂一个自定义 handler（**复用工厂造出的 read handler 作数据源，不新造 HTTP 调用**）"
  - "禁止实现服务端长轮询（60s 硬超时 + ASGI 协程占用 + 本仓零先例）"
  - "禁止把 await 超时做成 is_error（会诱导 agent 重试而非降级）：超时必须返回正常结果 {\"hit\": false, ...}"
  - "禁止无界 while：deadline 即上界，max(0, timeout_minutes) 兜负值，timeout_minutes 硬上界 5"
  - "禁止为 waiting_context 新增 last_output.source 值（会破坏 callbacks 四链互斥判定）：waiting_context 是 output 内与 fitness/repo_plan 平级的段"
  - "禁止把 waiter 状态写进 stage_state（lost-update）；stage_state 只存波次预排结果与汇总视图"
  - "禁止在 callbacks.py 跑 ruff format；只跑 ruff check"
  - "禁止环检测靠定时扫描或超时兜底（CONTEXT 明令登记时判定，命中即抛澄清）"
  - "禁止把命中条目的 content 正文写进容器侧日志（工厂日志字段白名单只有 tool/status/duration_ms/quota_used）"
must_haves:
  truths:
    - "短等待命中：容器调 await_blueprint_context 后目标 key 出现即返回该条目并停止轮询（注入 _now/_sleep 可零真实 sleep 断言轮询次数）"
    - "短等待超时：返回 {\"hit\": false, \"waited_ms\": N} 的**正常结果**，MCP 返回体不含 is_error；轮询以 since_seq 递增增量拉取，不重复拉全量"
    - "长等待退出重派：容器以 output 内 waiting_context 段退出（source 仍为 blueprint_repo_plan，不新增 source 值）→ 回调登记 waiter 且不判完成 → 目标条目 report 写入时同事务置 superseded 并触发 dispatch(session, mode='plan', repository_ids={等待仓}) 重派续作（prompt 带 partial_plan_id 引用）"
    - "互等环抛澄清：A 等 B 且 B 等 A 时，第二次 register_waiter 立即开 BlueprintThread(ai_clarification, blocking) 且**不 dispatch**，不靠超时兜底"
    - "波次预排：按 apis_provided/apis_consumed 关系拓扑分层，provider 仓进更早波次；成环即抛澄清不静默打平；纯函数零 ORM 可单测"
    - "KNOWLEDGE_TOOL_SCHEMAS 从 9 项增至 10 项且 handler 工厂签名/timeout/quota 计数零改动"
  artifacts:
    - path: "task/core/blueprint_context_wait.py"
      provides: "await_blueprint_context 容器侧有界轮询（deadline 骨架照 question_loop，超时返正常结果）"
      contains: "deadline"
    - path: "server/services/process_runtime/blueprint_repo_waves.py"
      provides: "按 API provider/consumer 关系的纯函数拓扑分层 + 成环上报"
      contains: "def build_api_waves"
    - path: "server/subagent/api/callbacks.py"
      provides: "waiting_context 探测分支（登记 waiter 不判完成）"
      contains: "waiting_context"
    - path: "server/tests/services/process_runtime/test_blueprint_context_wait_redispatch.py"
      provides: "三条路径（短等待命中 / 长等待退出重派 / 互等环抛澄清）各一条可证伪断言"
      contains: "cycle_detected"
  key_links:
    - from: "task/core/blueprint_context_wait.py"
      to: "task/core/knowledge_tools.py"
      via: "复用工厂造出的 read_blueprint_context handler 作数据源（不新造 HTTP 调用、不改工厂）"
      pattern: "read_blueprint_context"
    - from: "server/subagent/api/callbacks.py"
      to: "server/delivery/services/blueprint_context_service.py"
      via: "waiting_context 段 → register_waiter（含登记时环检测）"
      pattern: "register_waiter"
    - from: "server/services/process_runtime/blueprint_repo_plan.py"
      to: "server/services/process_runtime/blueprint_research_adapter.py"
      via: "satisfy_waiters 返回的重派仓清单 → dispatch(session, mode='plan', repository_ids=...)"
      pattern: "repository_ids"
---

<objective>
把 Context Bus 的两档等待原语接成闭环：容器侧 `await_blueprint_context` 有界轮询（短等待）、`waiting_context` 结构化退出与条目就绪后自动重派（长等待）、按 API provider/consumer 关系的波次预排（第一道防线）、互等环在登记瞬间抛澄清（死锁防护）。

Purpose: BUS-02 全量 + ROADMAP SC2 的验收场景「A 仓 consumer 等 B 仓 provider 契约」三条路径。
Output: 1 个 task 侧轮询模块 + 第 10 个知识工具 + 1 个服务端纯函数波次模块 + `blueprint_repo_plan.py` 的重派接线 + `callbacks.py` 的 waiting_context 分支 + 3 个测试文件。
</objective>

<execution_context>
工作目录：worktree 根。wave 3，依赖 113-02（MCP 两侧与 `read_blueprint_context` handler）与 113-03（plan 派发面 `mode`/`repository_ids` 扩展与 `BlueprintRepoPlanAdapter`）。
`task/core/knowledge_tools.py` 与 `server/subagent/api/callbacks.py` 在前序 wave 已被改过 —— 本 plan 继续**纯追加**，`git diff | rg "^-"` 相对本 plan 起点仍须为空（工厂与既有链一行不删改）。
本 plan **不碰 `builtin_processes.py` / `entrypoint.py`**（stage 注册归 113-06）。
</execution_context>

<context>
@.planning/phases/113-2/113-CONTEXT.md（锁定：短等待 = **容器侧有界轮询**不做服务端长轮询、超时返正常结果非 is_error、**不发心跳**、配额靠派发时 400 吸收；长等待 = waiting_context 携 partial 产物 id 退出 + 编排层登记依赖 + 就绪后重新派发（prompt 带 partial 引用续作）；第一道防线是 wave 预排（provider 仓先行）；死锁靠登记时环检测抛澄清不靠超时；waiter 生命周期 = 写入侧同事务置 superseded + 超时清理挂 barrier 续驱）
@.planning/phases/113-2/113-RESEARCH-BUS.md 主题 2.1（`ask_user` 完整链路逐段坐标：`_now`/`_sleep` 注入、deadline 骨架、命中即返回+清位、心跳、超时三分支）、2.2（为什么不做服务端长轮询：60s 硬超时/ASGI 占用/无 pub-sub/配额语义，四条硬约束）、2.3（推荐实现路径伪码 + 参数建议：poll_interval 5.0、timeout 默认 3 上界 5）、主题 3.1（`waiting_context` 退出的推荐形状与「不新增 source 值」理由）、3.2（barrier→续驱链路坐标、增量派发白名单、重派路径全链）、Pitfalls P3（waiter 载体）、P4（超时上界与配额账的具体数值）
@.planning/phases/113-2/113-PATTERNS.md 第 7 类（`question_loop.py:59-121` 逐字骨架 + 六项差异表：数据源/poll_interval/timeout/心跳/超时语义/状态推进）、第 5 类（🔒 工厂七个共享点禁改表 + 「await handler 是唯一例外：需要循环包装但仍复用 read handler 作数据源」）、第 9 类（satisfy_waiters 同事务 + 重派复用 `:750` 单仓定向通路 + `:66` 白名单自动跳过已完成仓）、No Analog Found 第 1 项（波次预排算法无先例，沿用 `wave_layering.py` 纯函数拓扑分层形态自写）、第 4 项（waiting_context 的 source 取舍）
@.planning/phases/113-2/113-01-SUMMARY.md（`register_waiter` / `satisfy_waiters` / `expire_waiters` / `find_wait_cycles` / `matches_wait_pattern` 签名与返回形状）
@.planning/phases/113-2/113-02-SUMMARY.md（两个 MCP 工具 URL 与入参/返回、`satisfied_waiters` 字段与「重派接续点」注释位置、`KNOWLEDGE_TOOL_SCHEMAS` 现为 9 项）
@.planning/phases/113-2/113-03-SUMMARY.md（`dispatch(mode="plan", repository_ids=...)` 契约、`BLUEPRINT_REPO_PLAN_SOURCE`、`BlueprintRepoPlanAdapter` 方法签名、callbacks 第四链判定函数名、RepoPlan 的 `apis_provided`/`apis_consumed` 字段形状——波次预排的输入面）
@task/core/question_loop.py（L59-121 完整骨架——**逐字 analog**；L124-163 handler 三段防御；L184-206 build 与向后兼容短路）
@task/core/knowledge_tools.py（L52-235 白名单表；L253-371 **只读禁改**的工厂；L374-440 build 与 allowed_tools——await 的自定义 handler 在此处挂）
@server/services/process_runtime/wave_layering.py（L24-85 `build_repo_waves` 纯函数拓扑分层形态——**形态 analog**）
@server/services/process_runtime/blueprint_resume.py（L154 `aresume_blueprint_session` 续驱入口——超时清理挂载点）
@server/subagent/api/callbacks.py（第四链的完成处理函数——waiting_context 分支加在它内部最前）
</context>

<tasks>

<task type="auto">
  <name>Task 1: 容器侧 await_blueprint_context 有界轮询（第 10 个知识工具，工厂零改动）</name>
  <files>task/core/blueprint_context_wait.py, task/core/knowledge_tools.py, task/tests/test_blueprint_context_wait.py</files>
  <read_first>task/core/question_loop.py L59-121（`ask_user_and_wait` 签名的 `_now`/`_sleep`/`poll_interval_s` 注入 + `deadline = now() + max(0, timeout_minutes) * 60` + while 内命中即返回 + 心跳 + 超时三分支——**逐字骨架**）与 L124-163（handler 三段防御、永不 raise）；task/core/knowledge_tools.py L253-371（**只读**：确认 `_make_knowledge_handler` 返回的是可直接 await 的 handler 函数，await 工具将复用它造 read handler）与 L374-440（`build_knowledge_mcp_server` 的 SdkMcpTool 列表推导 —— 确认在哪里为 await 工具挂自定义 handler 最不侵入）；113-02-SUMMARY（read_blueprint_context 的入参与返回形状：entries / count / max_seq）</read_first>
  <action>
① 新建 `task/core/blueprint_context_wait.py`。模块 docstring 编号列约束（照 `question_loop.py:7-15`）：①「短等待原语：容器侧**有界**轮询 `read_blueprint_context`，**不做服务端长轮询**——公共 handler 工厂的 `timeout=60.0` 写死且共享于 10 个工具，改它波及全部」；②「**超时返回正常结果而非 `is_error`**：`is_error` 会诱导 agent 重试而非降级（记假设 + 开澄清线程）」；③「**不发心跳**：knowledge handler 工厂不持有 `callback`，加参数会波及全部既有工具；轮询自身每 5s 一次 HTTP 出站即活动性」；④「向后兼容：read handler 为 None（endpoint/token 缺失）时直接返回未命中，绝不崩」；⑤「脱敏：命中条目正文绝不进容器侧日志，只记 `hit` / `polls` / `waited_ms`」。

模块常量：`DEFAULT_TIMEOUT_MINUTES = 3`、`MAX_TIMEOUT_MINUTES = 5`（硬上界，超过应走长等待退出重派）、`DEFAULT_POLL_INTERVAL_S = 5.0`（不用 `ask_user` 的 3.0——这里是 HTTP + DB）。

```
async def await_blueprint_context(
    read_handler,                       # 复用 _make_knowledge_handler("read_blueprint_context", ...) 造出的 handler
    key_pattern: str,
    *,
    kind: str = "",
    since_seq: int = 0,
    timeout_minutes: int = DEFAULT_TIMEOUT_MINUTES,
    poll_interval_s: float = DEFAULT_POLL_INTERVAL_S,
    _now: Callable[[], float] | None = None,
    _sleep: Callable[[float], Awaitable[None]] | None = None,
) -> dict
```
实现（骨架照 `question_loop.py:97-121`，逐项落差异）：
1. `read_handler is None` → 立即 `{"hit": False, "reason": "tool_unavailable", "waited_ms": 0, "polls": 0, "max_seq": since_seq}`。
2. `timeout_minutes = min(max(0, timeout_minutes), MAX_TIMEOUT_MINUTES)`；`deadline = now() + timeout_minutes * 60`；`last_seq = since_seq`；`polls = 0`。
3. `while now() < deadline:` → `polls += 1` → `raw = await read_handler({"key_prefix": <key_pattern 的字面前缀部分>, "kind": kind, "since_seq": last_seq, "limit": 200})` → 解析 handler 返回体（`{"content": [{"type":"text","text": json_str}]}`，**沿用工厂的返回形状**；`is_error` 为真 → 视为本轮未命中并继续，不中断等待）→ `entries = body.get("entries") or []`；`last_seq = max(last_seq, body.get("max_seq") or last_seq)`（**增量幂等，避免重复拉全量**）→ 逐条用 `matches_key_pattern(entry["key"], key_pattern)`（本模块的极简纯函数：精确 / `*` 尾通配 / 前缀）判定 → 命中即 `return {"hit": True, "entry": entry, "waited_ms": …, "polls": polls, "max_seq": last_seq}` 并**停止轮询** → 未命中 `await sleep(poll_interval_s)`。**不发心跳**。
4. 超时 → `return {"hit": False, "reason": "timeout", "waited_ms": …, "polls": polls, "max_seq": last_seq}`（**正常结果，不是 `is_error`**）。
5. 日志只 `log.info("blueprint_context_await_finished", hit=…, polls=…, waited_ms=…)`——**不记 key_pattern 之外的任何正文**。

② `task/core/knowledge_tools.py` **纯追加**（工厂一行不改）：
- `KNOWLEDGE_TOOL_SCHEMAS` 追加第 10 项 `await_blueprint_context`：description「等待某条共享上下文出现：当你要消费的其他仓接口契约还没写进总线时用它短等（默认 3 分钟，最长 5 分钟）。**命中即返回；超时会返回 hit=false 的正常结果——此时请记录假设并继续（可开澄清线程），不要反复重试**。」properties：`key_pattern`（「（必填）等待的 key，支持尾部 `*` 通配，如 `repo:<uuid>.api_surface`」）/ `kind`（六值枚举，可选）/ `since_seq`（「从该 seq 之后开始等（增量）」）/ `timeout_minutes`（「等待上限分钟数，默认 3，最大 5」）。`required: ["key_pattern"]`。
- 在 `build_knowledge_mcp_server` 里：先按既有列表推导为 10 个 schema 造 handler；**随后对 `await_blueprint_context` 这一个工具替换成自定义 handler**——闭包内持有前面已造好的 `read_blueprint_context` handler，调 `await_blueprint_context(read_handler, ...)` 并把返回 dict 包成工厂同款 `{"content":[{"type":"text","text": json.dumps(result, ensure_ascii=False)}]}`（**超时路径不带 `is_error`**）；缺 `key_pattern` → 带 `is_error` 的参数错误文案；兜底 `except Exception` → `is_error`（RTOOL-04 永不 raise）。改动落在 build 函数内部**新增的几行**，`_make_knowledge_handler` 签名与函数体零改动。

③ 新建 `task/tests/test_blueprint_context_wait.py`（若 113-02 已建 task 侧测试目录，同目录追加）：
- ⭐ **命中即返回**：注入 `_now` 单调桩与计数 `_sleep`；`read_handler` 第 2 次调用返回目标条目 → `hit is True`、`polls == 2`、`_sleep` 被调用 1 次（**命中后不再轮询**）。
- ⭐ **超时返正常结果**：`read_handler` 恒返回空 → `hit is False`、`reason == "timeout"`，且返回 dict **不含** `is_error` 键；经 build 出的 MCP handler 包装后，返回体里 `is_error` 同样不存在（**这是可证伪的核心断言**）。
- ⭐ **since_seq 递增**：`read_handler` 每轮返回 `max_seq` 递增的空结果 → 断言第 N 轮传入的 `since_seq` 等于第 N-1 轮返回的 `max_seq`（不重复拉全量）。
- `timeout_minutes=99` 被夹到 5；`timeout_minutes=-1` 被夹到 0 且立即返回（不无限等）。
- `read_handler is None` → `hit False` + `reason == "tool_unavailable"`，不抛。
- `read_handler` 返回 `is_error` 的体 → 本轮当未命中继续，不中断等待。
- `matches_key_pattern` 纯函数三例：精确 / `repo:B.*` 通配 / 不匹配。
- **工厂零改动守护**（延续 113-02）：`inspect.signature(_make_knowledge_handler)` 参数名元组不变；`rg` 级断言由 acceptance_criteria 覆盖；`len(KNOWLEDGE_TOOL_SCHEMAS) == 10`、`len(knowledge_allowed_tools()) == 10`。
  </action>
  <verify>
    <automated>cd task && uv run pytest tests/test_blueprint_context_wait.py -x -q</automated>
  </verify>
  <acceptance_criteria>
- `cd task && uv run pytest -q` 全绿。
- `rg -c '"name": "' task/core/knowledge_tools.py` == 10。
- `rg -c "timeout=60.0" task/core/knowledge_tools.py` == 1；`rg -n "callback" task/core/knowledge_tools.py` 零命中。
- `git diff task/core/knowledge_tools.py | rg "^-" | rg -v "^---"` 输出为空（相对本 plan 起点纯追加）。
- `rg -n "def _make_knowledge_handler" -A 8 task/core/knowledge_tools.py | rg "quota_counter"` 命中（工厂签名未变）。
- `rg -n "deadline" task/core/blueprint_context_wait.py` 命中；`rg -n "while " task/core/blueprint_context_wait.py | rg -v "deadline"` 零命中（无无界 while）。
- `rg -n "is_error" task/core/blueprint_context_wait.py` 零命中（超时路径不产 is_error）。
- `rg -n "MAX_TIMEOUT_MINUTES = 5|DEFAULT_POLL_INTERVAL_S = 5.0" task/core/blueprint_context_wait.py` 两条均命中。
- `rg -n "httpx" task/core/blueprint_context_wait.py` 零命中（复用工厂 handler，不新造 HTTP 调用）。
- `git diff --stat task/core/executor.py task/core/config.py` 输出为空。
  </acceptance_criteria>
  <done>短等待原语就位：deadline 有界、命中即停、超时返正常结果无 is_error、since_seq 增量、不发心跳、复用 read handler 不碰工厂；白名单 10 项且工厂签名有守护断言。</done>
</task>

<task type="auto">
  <name>Task 2: 波次预排纯函数 + waiting_context 退出与自动重派接线</name>
  <files>server/services/process_runtime/blueprint_repo_waves.py, server/services/process_runtime/blueprint_repo_plan.py, server/subagent/api/callbacks.py</files>
  <read_first>server/services/process_runtime/wave_layering.py L24-85（`build_repo_waves` 的输入 dict → `{wave: [ids]}` 输出形态、零 ORM、成环处理——**形态 analog**）；server/subagent/api/callbacks.py 的第四链完成处理函数全文（113-03 新增，waiting_context 分支加在其最前）与 barrier 触发段；server/services/process_runtime/blueprint_repo_plan.py（113-03 交付的 `dispatch_plans` / `acollect_repo_plans` / `build_stage_state`——重派与预排在此接线）；server/services/process_runtime/blueprint_research_adapter.py L66（增量白名单注释）与 L750（单仓定向 dispatch 先例）；.planning/phases/113-2/113-01-SUMMARY.md（register_waiter / satisfy_waiters 返回形状）</read_first>
  <action>
① 新建 `server/services/process_runtime/blueprint_repo_waves.py`（**纯函数，零 ORM / 零 LLM**，模块 docstring 声明这条）：

```
def build_api_waves(repo_plans: dict[str, dict]) -> dict
```
- 输入：`{repository_id: repo_plan_section}`（`acollect_repo_plans` 的产物）。**首轮预排时 repo_plan 尚未产出** → 也接受 `{repository_id: {"apis_provided": [...], "apis_consumed": [...]}}` 形状的**预估输入**（来自确认门锁定的 `responsibility` 关键词或 112 findings 的 `api_contracts_exposed`；调用方决定喂什么，本函数只吃这两个键）。
- 建边：对每个仓的 `apis_consumed[]`，若某条的 `from_repository_id` 指向另一仓，或其 `name`/`path` 与另一仓 `apis_provided[].name`/`path` 匹配（**匹配规则抽成 `def match_api(consumed: dict, provided: dict) -> bool` 纯函数**：优先 `from_repository_id` 显式指定；否则 `(method, path)` 全等；再否则 `name` 全等），则建边 `provider → consumer`。
- 拓扑分层：无入边的仓为 wave 1，逐层剥离（Kahn）。
- 返回**恒定形状** `{"waves": {1: [rid...], 2: [...]}, "edges": [{"from": rid, "to": rid, "api": name}], "cycles": [[rid, rid]], "unresolved_consumed": [{"repository_id": rid, "api": name}]}`。
  - `cycles` 非空时：**成环的仓不静默打平**——把它们放进 `waves` 的最后一波并在 `cycles` 里如实上报，由调用方（Task 2 的 adapter 接线）开澄清线程。
  - `unresolved_consumed` = 找不到任何 provider 的 consumed 项（**113-05 的 needs_support 标注在此处已有前置信号**）。

② `server/services/process_runtime/blueprint_repo_plan.py` 追加（113-03 的既有方法不改）：
- `async def aplan_waves(self, session) -> dict`：取仓集与可用的 api 信息 → `build_api_waves(...)` → 结果写进 `stage_state["repo_plan"]["waves"]`（**只存 `{wave: [rid]}` 与 `cycle_count` 小摘要**，正文不进 stage_state）→ `cycles` 非空即 `await self.lifecycle_service.open_thread(kind="ai_clarification", blocking=True, question=<列出成环的仓与冲突的 api 名>)`。
- `dispatch_plans` 内接入预排：**按波次派发**（wave 1 的 provider 仓先起容器；后续波次待前一波 `aall_repo_plans_ready` 的子集就绪后再派）。为避免把 stage 变复杂，实现为「每次 `dispatch_plans` 只派发**当前可派发波次**的仓」，barrier 续驱时自然推进下一波——在函数 docstring 里写清这条推进语义。
- `async def aredispatch_waiting_repos(self, session, repository_ids: list[str]) -> int`：把 `satisfy_waiters` 返回的等待仓重新派发 —— 逐仓 `mark_stale` 后 `await self.research_adapter.dispatch(session, mode="plan", repository_ids={rid})`；**prompt 带 partial 引用续作**：从该仓最新 `PartialPlan` 取 id 与已产出的部分内容摘要，经 `_build_plan_prompt` 的续作段注入（在 113-03 的 `_build_plan_prompt` 里已预留位置则填充，否则在本 plan 追加一个可空串 section——**非续作场景该段为空串，prompt 与首轮逐字一致，零扰动**，照 `architect_merge_adapter._classification_prompt_parts` 的可空串范式）。返回重派仓数。`caller` 事件 + `ConvergenceSessionEvent`。
- `async def aexpire_stale_waiters(self, session) -> list[str]`：委托 `BlueprintContextService.expire_waiters`，供 barrier 续驱路径调用（**不新起定时任务**）。

③ `server/subagent/api/callbacks.py` **纯追加** waiting_context 分支（在 113-03 的 `_handle_blueprint_repo_plan_completion` **最前**）：
```
waiting = (parsed or {}).get("waiting_context")  # 与 fitness / repo_plan 平级的段
if isinstance(waiting, dict) and waiting.get("keys"):
    → 逐 key 调 BlueprintContextService().register_waiter(session=cs, from_repository_id=rid,
          wait_key_pattern=key, partial_plan_id=waiting.get("partial_plan_id") or "",
          reason=<截断脱敏的 reason>)
    → 若任一返回 cycle_detected（环检测在 register_waiter 内完成，见 113-01）→ **不 dispatch**，
      澄清线程已由 service 开好，此处只 emit caller 事件并 return
    → 否则：**不判完成**（不落 repo_plan 段、不置 task done），把该 task 置回可派发态由重派驱动；
      emit caller 事件后 return（**不触发 barrier 的「全部就绪」判定**）
```
**不新增 `last_output.source` 值**（`source` 仍是 `blueprint_repo_plan`，四链互斥判定与两个挂载点零改动）。
同时在 `report_blueprint_context` 的写入侧接上真正的重派：113-02 留下的「重派接续点」注释处，把 `satisfy_waiters` 返回的仓清单交给 `BlueprintRepoPlanAdapter().aredispatch_waiting_repos(session, ids)`（**整段 best-effort `except Exception` 吞掉 + warning，绝不让重派失败反噬 report 的 200**）。若跨模块 import 在 view 层不合适，则把这段接线放在 `BlueprintRepoPlanAdapter` 侧并由 view 通过一次函数调用触发——**实现自选，但必须是「同事务置 superseded」之后、且失败不反噬 report 响应**。
⚠️ `callbacks.py` 只跑 `ruff check`，**绝不跑 `ruff format`**。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/process_runtime/test_blueprint_repo_waves.py tests/subagent/ -x -q</automated>
  </verify>
  <acceptance_criteria>
- `rg -n "def build_api_waves|def match_api" server/services/process_runtime/blueprint_repo_waves.py` 两个纯函数命中。
- `rg -n "import.*models|objects\.|sync_to_async" server/services/process_runtime/blueprint_repo_waves.py` 零命中（纯函数零 ORM）。
- `rg -n "cycles|unresolved_consumed" server/services/process_runtime/blueprint_repo_waves.py` 命中（成环与无 provider 均如实上报）。
- `rg -n "waiting_context" server/subagent/api/callbacks.py` 命中；`rg -n '"source"' server/subagent/api/callbacks.py | rg "waiting"` 零命中（未为 waiting_context 新增 source 值）。
- `rg -n "register_waiter" server/subagent/api/callbacks.py` 命中。
- `git diff server/subagent/api/callbacks.py | rg "^-" | rg -v "^---"` 输出为空。
- `rg -n "aredispatch_waiting_repos" server/services/process_runtime/blueprint_repo_plan.py` 命中；同函数内 `rg -n 'mode="plan"' server/services/process_runtime/blueprint_repo_plan.py` 命中。
- `rg -n "expire_waiters" server/services/process_runtime/blueprint_repo_plan.py` 命中（超时清理挂 barrier 续驱，不新起定时任务）。
- `rg -n "apscheduler|CronTrigger|IntervalTrigger" server/services/process_runtime/blueprint_repo_plan.py server/services/process_runtime/blueprint_repo_waves.py` 零命中。
- `rg -n "waves" server/services/process_runtime/blueprint_repo_plan.py` 命中（stage_state 写入预排结果）。
- `git diff --stat server/services/process_runtime/builtin_processes.py server/services/process_runtime/entrypoint.py` 输出为空。
  </acceptance_criteria>
  <done>波次预排纯函数（provider 先行、成环如实上报不打平、无 provider 有前置信号）+ waiting_context 登记不判完成 + 条目就绪后自动重派续作 + 超时清理挂 barrier；全程未新增 source 值、未新起定时任务。</done>
</task>

<task type="auto">
  <name>Task 3: 三条路径端到端测试（短等待命中 / 长等待退出重派 / 互等环抛澄清）</name>
  <files>server/tests/services/process_runtime/test_blueprint_repo_waves.py, server/tests/services/process_runtime/test_blueprint_context_wait_redispatch.py</files>
  <read_first>server/tests/services/process_runtime/test_blueprint_research_stage.py L1-122（模块 docstring 编号清单、双 pytestmark、`_FakeDispatcher` 替身与工厂区段、事件断言口径）；113-01-SUMMARY（service 方法签名）；113-03-SUMMARY（adapter 方法签名与 dispatch 契约）；task/tests/test_blueprint_context_wait.py（Task 1 的容器侧断言，避免重复覆盖）</read_first>
  <action>
① 新建 `server/tests/services/process_runtime/test_blueprint_repo_waves.py`（**零 DB 纯函数**）：
- **provider 先行**：A 消费 B 提供的 `GET /x` → `waves == {1: ["B"], 2: ["A"]}`。
- **三层链**：C←B←A → 三波各一仓，顺序正确。
- **无依赖**：三仓互不引用 → 全在 wave 1（可完全并行）。
- **显式 from_repository_id 优先**：consumed 带 `from_repository_id="B"` 但 path 不匹配任何 provided → 仍建边 B→A。
- **成环**：A 消费 B、B 消费 A → `cycles` 非空且含 `["A","B"]`（顺序无关，用集合断言）；两仓**仍出现在 waves 里**（放最后一波，不丢仓）。
- **无 provider**：A 消费谁都不提供的 API → `unresolved_consumed` 含该项，A 仍进 wave 1。
- `match_api` 三例：`(method,path)` 全等 / `name` 全等 / 都不等。

② 新建 `server/tests/services/process_runtime/test_blueprint_context_wait_redispatch.py`（`django_db(transaction=True)` + `asyncio`，`_FakeDispatcher` 替身），模块 docstring 编号写明「本文件守 ROADMAP SC2 的三条路径，每条一个 ⭐ 可证伪断言」：

**路径 1 — 短等待命中**（服务端侧的可观测证据）：容器 A 调 `read_blueprint_context(since_seq=0)` 得空 → 服务端经 service 写入 `repo:B.api_surface` → A 再读（`since_seq=前次 max_seq`）**拿到该条目且 seq 严格大于前次**。⭐断言：两次 read 的返回条目集合无重叠（增量语义成立），且第二次命中目标 key。（容器侧的「命中即停轮询」由 `task/tests/test_blueprint_context_wait.py` 覆盖，此处不重复。）

**路径 2 — 长等待退出重派**：
1. 构造 plan 容器 A 的回调 output 含 `{"waiting_context": {"keys": ["repo:B.api_surface"], "partial_plan_id": "<id>", "reason": "需要 B 的接口契约"}}` → 走第四链完成钩子。
   ⭐断言 a：A 的 `PartialPlan.content` **没有** `repo_plan` 段（**不判完成**）；DB 里新增一条 `BlueprintContextEntry(kind="dependency_claim", status="active")`。
2. 容器 B 经 `report_blueprint_context`（或直接调 service + 重派接线）写入 `repo:B.api_surface`。
   ⭐断言 b：该 waiter 行 `status == "superseded"`，且 `_FakeDispatcher` 收到**恰好 1 次**新的 `DispatchTask`，其 `task_id` 以 `bp-plan-` 开头、`metadata` 对应 A 仓；**A 的 prompt 里含 `partial_plan_id`**（续作引用注入生效）。
   ⭐断言 c：**幂等** —— 再次写入同 key，dispatcher `await_count` 不再增加（同事务置位杜绝重复重派）。

**路径 3 — 互等环抛澄清**：
1. 登记 A 等 `repo:B.api_surface`（第一次，`cycle_detected is False`，无线程）。
2. 登记 B 等 `repo:A.api_surface`。
   ⭐断言：返回 `cycle_detected is True`；DB 存在一条 `BlueprintThread(kind="ai_clarification", blocking=True)`；`_FakeDispatcher.await_count == 0`（**命中环不 dispatch**）；第二条 waiter 行 `status == "superseded"`；线程问题文本里含两个仓 id 但**不含**任何 content 正文。
3. 时间上界证据：整个用例**不依赖任何 sleep/超时**（断言执行耗时无关——环在登记瞬间被判定，不靠超时兜底）。

补充用例：
- **波次预排驱动派发**：预排 `{1:["B"], 2:["A"]}` → 首次 `dispatch_plans` 只派 B（dispatcher `await_count == 1`）；B 产出 repo_plan 后再调 → 派 A。
- **expire_waiters**：`created_at` 回拨后由 `aexpire_stale_waiters` 清理并返回仓清单，且**未新起 apscheduler job**（`rg` 断言由 acceptance_criteria 覆盖）。
- **事件留痕**：`blueprint.context.waiter_registered` / `waiter_satisfied` 各 emit 一次，payload 只含标量与仓 id。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/process_runtime/test_blueprint_repo_waves.py tests/services/process_runtime/test_blueprint_context_wait_redispatch.py -x -q</automated>
  </verify>
  <acceptance_criteria>
- `cd server && uv run pytest tests/services/process_runtime/ tests/subagent/ tests/delivery/ tests/mcp_tools/ -q` 全绿。
- `rg -n "cycle_detected" server/tests/services/process_runtime/test_blueprint_context_wait_redispatch.py` 命中（路径 3 断言存在）。
- `rg -n "partial_plan_id" server/tests/services/process_runtime/test_blueprint_context_wait_redispatch.py` 命中（路径 2 续作引用断言存在）。
- `rg -n "superseded" server/tests/services/process_runtime/test_blueprint_context_wait_redispatch.py` 命中 ≥2（路径 2 与路径 3）。
- `rg -n "await_count == 0" server/tests/services/process_runtime/test_blueprint_context_wait_redispatch.py` 命中（环命中不 dispatch）。
- `rg -n "cycles" server/tests/services/process_runtime/test_blueprint_repo_waves.py` 命中。
- `rg -n "asyncio.sleep|time.sleep" server/tests/services/process_runtime/test_blueprint_context_wait_redispatch.py` 零命中（不靠超时兜底的证据）。
  </acceptance_criteria>
  <done>ROADMAP SC2 的三条路径各有能证伪的断言：短等待增量命中、长等待退出后恰好重派一次且带 partial 引用、互等环在登记瞬间抛澄清且零 dispatch；波次预排七类形态全覆盖。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 容器轮询 → 服务端 read 接口 | 高频调用面，占用共享知识配额 |
| 容器 waiting_context 段 → 编排层 waiter 登记 | 容器自述「我在等谁」，可被幻觉或恶意声明放大 |
| waiter 满足 → 自动重派容器 | 写入侧动作触发容器起停，是额度与并发的放大器 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113-20 | Denial of Service | 无界等待导致容器永久挂起 | mitigate | `deadline` 即 while 上界 + `timeout_minutes` 夹到 [0, 5]；超时返正常结果由 agent 降级；测试断言 `timeout_minutes=99` 被夹紧 |
| T-113-21 | Denial of Service | 轮询挤爆 10 工具共享的知识配额 | mitigate | `poll_interval_s=5.0` + `timeout` 上界 5 分钟 ⇒ 单次 await ≤60 轮；派发时配额提到 400（113-03）；配额耗尽时既有工厂返降级文案而非崩溃 |
| T-113-22 | Denial of Service | 互等环导致会话永久停滞 | mitigate | 登记瞬间 `find_wait_cycles` 判定 → 开 blocking 澄清线程交人裁决且**不 dispatch**；测试用例零 sleep 依赖 |
| T-113-23 | Denial of Service | waiter 反复满足触发重复重派烧额度 | mitigate | `satisfy_waiters` 同事务置 `superseded`；重派前 `dispatch` 的增量白名单自动跳过已完成仓；测试断言二次写入 `await_count` 不增 |
| T-113-24 | Tampering | 容器声明等待不存在的 key 制造无限等待/无限重派 | mitigate | waiter 有超时清理（`expire_waiters` 挂 barrier 续驱）；短等待自带上界；`unresolved_consumed` 会在 113-05 转成 `needs_support` 而非静默 |
| T-113-25 | Info Disclosure | 命中条目正文进容器侧日志或澄清线程文本 | mitigate | 容器侧只记 `hit`/`polls`/`waited_ms`；澄清问题只含仓 id 与 key 模式；测试断言线程文本不含 content 正文 |
| T-113-26 | Tampering | 为 await 改公共 handler 工厂波及既有 9 工具 | mitigate | await 走「复用 read handler 的自定义包装」，工厂签名/`timeout=60.0`/`quota_counter` 零改动；`inspect.signature` 与 `rg -c "timeout=60.0" == 1` 双守护 |
| T-113-SC | Tampering | 依赖供应链 | accept | 零新增外部依赖 |
</threat_model>

<verification>
- `cd server && uv run pytest tests/services/process_runtime/ tests/subagent/ tests/delivery/ tests/mcp_tools/ -q` 全绿；`cd task && uv run pytest -q` 全绿。
- `cd server && uv run python manage.py makemigrations --check --dry-run` 退出码 0。
- `cd server && uv run ruff check services/process_runtime/ subagent/`；`cd task && uv run ruff check core/`。**不对 `subagent/api/callbacks.py` 跑 format**。
- 冻结面自检：`git diff --name-only` 不含 `codegraph/services/repo_router_v2.py`、`process_runtime/{decompose_segments,research_adapter,architect_merge_adapter,merged_plan,clarify_adapter,render,resume,builtin_processes,entrypoint,blueprint_schema,blueprint_route,blueprint_spec_gate,blueprint_confirm_gate,blueprint_resume,blueprint_lifecycle_service}.py`、`repositories/services/charter_service.py`、`system/{models,settings_service}.py`、`delivery/services/event_taxonomy.py`。
- 🔒 工厂自检：`git diff task/core/knowledge_tools.py | rg "^-" | rg -v "^---"` 为空；`rg -c "timeout=60.0" task/core/knowledge_tools.py` == 1；`rg -n "callback" task/core/knowledge_tools.py` 零命中。
- 受限面自检：`git diff server/subagent/api/callbacks.py | rg "^-" | rg -v "^---"` 输出为空。
</verification>

<success_criteria>
- BUS-02 全量：短等待保活轮询带超时降级、长等待携 partial 产物退出且条目就绪后自动重派续作、互相等待环被检测并抛澄清。
- ROADMAP SC2 后半：「A 仓 consumer 等 B 仓 provider 契约」三条路径全部有能证伪的断言。
- 第一道防线：repo_plan 阶段按 API provider/consumer 预排波次（provider 仓先行），`await` 只兜预排不出来的动态依赖。
</success_criteria>

<output>
完成后创建 `.planning/phases/113-2/113-04-SUMMARY.md`（provides：`await_blueprint_context` 的入参/返回形状与超时语义、`build_api_waves` 返回四键契约（waves/edges/cycles/unresolved_consumed）、`aplan_waves` 写入 `stage_state["repo_plan"]["waves"]` 的形状、`aredispatch_waiting_repos` 与 `aexpire_stale_waiters` 签名、`waiting_context` 段的三键约定与「不新增 source 值」结论、`KNOWLEDGE_TOOL_SCHEMAS` 现为 10 项——113-05 消费 `unresolved_consumed` 作 needs_support 前置信号，113-06 在 barrier/stage 侧调用 `aexpire_stale_waiters`）。
</output>
