---
phase: 113-2
plan: 03
type: execute
wave: 2
depends_on: [113-01]
files_modified:
  - server/services/process_runtime/blueprint_repo_plan_schema.py
  - server/services/process_runtime/blueprint_repo_plan.py
  - server/services/process_runtime/blueprint_research_adapter.py
  - server/subagent/api/callbacks.py
  - server/tests/services/process_runtime/test_blueprint_repo_plan_schema.py
  - server/tests/services/process_runtime/test_blueprint_repo_plan_stage.py
  - server/tests/subagent/test_blueprint_repo_plan_callback.py
autonomous: true
requirements: [FLOW-05, SCHEMA-03]
prohibitions:
  - "禁止修改 server/codegraph/services/repo_router_v2.py"
  - "禁止修改 process_runtime 六冻结文件：decompose_segments.py / research_adapter.py / architect_merge_adapter.py / merged_plan.py / clarify_adapter.py / render.py（后者是 analog，只读参考、绝不 import）"
  - "禁止修改 server/services/process_runtime/resume.py 与 server/repositories/services/charter_service.py"
  - "禁止修改 ConvergenceSessionEvent 既有类型；禁止修改 builtin_processes.py 的 _TECHNICAL_PLAN_STAGES / _ECHO_STAGES —— 本 plan **完全不碰 builtin_processes.py**（stage 注册在 113-06）"
  - "禁止修改 server/system/settings_service.py 既有 getter"
  - "禁止修改 task/core/knowledge_tools.py（113-02/113-04 独占；公共 handler 工厂 timeout=60.0 / quota_counter / 无 callback 参数三条硬约束全程有效）"
  - "禁止修改 blueprint_schema.py（112-05 有「冻结面自检零命中」断言）：RepoPlan schema 一律进新建的 blueprint_repo_plan_schema.py；也禁止 import blueprint_schema 的私有函数（_format_error 等复制 12 行，不 import）"
  - "禁止修改 blueprint_route.py / blueprint_spec_gate.py / blueprint_confirm_gate.py / blueprint_resume.py / blueprint_lifecycle_service.py（受限面，本 plan 只读消费）"
  - "禁止新增 CallSource 枚举值：plan 链复用 111 已注册的 CallSource.BLUEPRINT_REPO_PLAN"
  - "禁止改 blueprint_research_adapter 的 env_FRIDAY_TASK_MODE / env_FRIDAY_TASK_TASK_MODE（保持 'explore'——它管 git 写拦截，与调研/拟方案正交）"
  - "禁止改 _bucket 分桶规则、禁止复用 _collect_candidates（阶段 2 的仓集来自确认门锁定的 repo_associations，语义已不同）"
  - "禁止在 callbacks.py 跑 ruff format（该文件有先于蓝图链的 format 漂移，format 会打破「纯追加」断言）；只跑 ruff check"
  - "禁止复用 aall_research_tasks_terminal 作 repo_plan 的完成判据（两 stage barrier 同源 + mark_stale 会让 stage 1 判据短暂为假）"
  - "禁止复用 ResearchService.retry_task（stage 名硬编码 'research' 会恒 raise）"
  - "禁止裸传 {'repo_plan': ...} 给 record_partial（整体覆写语义会吃掉 112 的 fitness 段）：必须读-合并-写"
must_haves:
  truths:
    - "direct 仓起 plan 容器：`dispatch(session, mode='plan')` 走 bp-plan- 前缀 session_id + last_output.source='blueprint_repo_plan' + CallSource.BLUEPRINT_REPO_PLAN；`mode='research'` 缺省路径与 112 逐字等价（两个既有调用方零改动）"
    - "给 plan 容器 session 跑 callbacks._is_blueprint_research 必须为 False（否则被调研解析器抢走并因缺 fitness.verdict 判失败）"
    - "写入 repo_plan 段后 acollect_fitness(session)[repo_id]['verdict'] 仍等于写入前的值（read-merge-write 未吃掉 112 的 fitness/findings/§7 五键）"
    - "RepoPlan 段过 validate_repo_plan 的 jsonschema：不合格触发有界重试 ≤2 轮，仍不合格开 BlueprintThread(ai_clarification, blocking) 而非静默降级"
    - "indirect 仓由服务端 LLM 合成能力引用清单（不起容器），产物与 direct 同形落 PartialPlan.content.repo_plan"
    - "一仓多条 PartialPlan 按 valid=True + -created_at 取最新一条作为该仓 canonical 产物；历史行不被覆盖"
    - "impl_items 逐项带 change_type（新建/改动/删除/间接完善）与 files_touched/depends_on，为 SCHEMA-03 的功能↔模块↔仓库映射提供原料"
  artifacts:
    - path: "server/services/process_runtime/blueprint_repo_plan_schema.py"
      provides: "BLUEPRINT_REPO_PLAN_SCHEMA + 预编译 validator + validate_repo_plan 纯函数（绝不外抛）"
      contains: "def validate_repo_plan"
    - path: "server/services/process_runtime/blueprint_repo_plan.py"
      provides: "BlueprintRepoPlanAdapter：仓集取确认门锁定产物、direct 派发、indirect LLM 合成、有界重试、完成判据"
      contains: "BLUEPRINT_REPO_PLAN_SOURCE"
    - path: "server/subagent/api/callbacks.py"
      provides: "第四条 PLAN 链（_is_blueprint_repo_plan 三件套 + 两个挂载点 + CallSource 映射）"
      contains: "_is_blueprint_repo_plan"
    - path: "server/tests/services/process_runtime/test_blueprint_repo_plan_stage.py"
      provides: "P-1（fitness 不被吃掉）与 P-4（source 互斥）两条可证伪断言"
      contains: "acollect_fitness"
  key_links:
    - from: "server/services/process_runtime/blueprint_repo_plan.py"
      to: "server/services/process_runtime/blueprint_research_adapter.py"
      via: "组合而非继承：调 dispatch(session, mode='plan') 复用派发五步与 metadata 构造"
      pattern: "mode=\"plan\""
    - from: "server/services/process_runtime/blueprint_repo_plan.py"
      to: "server/delivery/services/research_service.py"
      via: "read-merge-write 后 record_partial（已含置 done，不再调 mark_done）"
      pattern: "record_partial"
    - from: "server/subagent/api/callbacks.py"
      to: "server/services/process_runtime/blueprint_repo_plan_schema.py"
      via: "容器产物落库前过 validate_repo_plan，不合格触发有界重试"
      pattern: "validate_repo_plan"
---

<objective>
落地阶段 2 的分仓方案（RepoPlan）：新建独立 `blueprint_repo_plan_schema.py`（DESIGN §5.3 十一字段 jsonschema）与 `blueprint_repo_plan.py`（仓集取确认门锁定产物、direct 起 plan 容器、indirect 服务端 LLM 合成、有界重试、自写完成判据）；对 112 冻结外的派发面 `blueprint_research_adapter.py` 只做**加性关键字扩展**（`mode: str = "research"`，缺省逐字等价 112）；`callbacks.py` 纯追加第四条 PLAN 链。

Purpose: FLOW-05「阶段 2 按锁定职责逐仓拟定分仓方案，期间可多轮澄清、可发起单仓定向补调研」+ SCHEMA-03 的原料面（impl_items 逐项 change_type 与依赖）。
Output: 2 个新模块 + 派发面 4 个扩展点 + callbacks 第四链 + 3 个测试文件。
</objective>

<execution_context>
工作目录：worktree 根。wave 2，与 113-02 **文件零重叠**（本 plan 独占 `process_runtime/` 与 `subagent/api/callbacks.py`，113-02 独占 `mcp_tools/` 与 `task/`）。
本 plan **不碰 `builtin_processes.py`**：`_h_bp_repo_plan` handler 与 stage 注册全部在 113-06 收口，本 plan 只交付 adapter 与它的完成判据函数（供 113-06 的 handler 调用）。
`blueprint_research_adapter.py` 的改动必须**可枚举**：2 个函数签名各加一个带默认值的关键字 + 3 处 `mode` 分支 + 1 处配额键，`git diff | rg "^-"` 应只出现被改签名/分支的那几行。
</execution_context>

<context>
@.planning/phases/113-2/113-CONTEXT.md（锁定：载体沿用 PartialPlan.content 新增 repo_plan 段；direct 起容器复用 112 派发面 + plan 模式参数、indirect 服务端 LLM 合成；澄清与补调研复用 BlueprintThread + force_deep_repository_ids；**新建独立 blueprint_repo_plan_schema.py**；plan 模式四个扩展点全走带默认值 keyword-only、env_FRIDAY_TASK_MODE 保持 explore；阶段 2 复用同一 RepoResearchTask + mark_stale，_h_bp_repo_plan **自写完成判据**；一仓多条取最新；派发时 KNOWLEDGE_QUOTA 提到 400）
@.planning/phases/113-2/113-RESEARCH.md 主题 1（PartialPlan 字段与零校验、content 现状结构、record_partial 三件事与整体覆写语义、1.4 读-合并-写写法与 repo_plan 段十一字段）、主题 2（派发面签名全表、metadata 现状取值表与 explore 语义、**2.3 四个扩展点 E-1~E-4 表**与不建议扩展点、推荐用组合而非继承、回调第四链形状）、主题 3.1（validate_blueprint 三件套范式，供新 schema 模块照抄）、Pitfalls P-1/P-2/P-3/P-4/P-10
@.planning/phases/113-2/113-PATTERNS.md 第 1 类（blueprint_schema.py 三件套 + __all__ 契约 + 报错脱敏截断 12 行**复制不 import** + 字段与枚举对齐）、第 5/6 类的工厂硬约束（本 plan 不碰 task/ 但派发侧配额键归本 plan）、第 8 类（handler 纪律与 stage_state 键——本 plan 只产出 adapter 返回 dict）、第 11 类 Analog B（编排 stage 测试范式：替身/工厂/四类必测）
@.planning/phases/113-2/113-01-SUMMARY.md（BlueprintContextService 签名——plan 容器的总线读写由 113-02 的 MCP 面承担，本 plan 仅在 prompt 里条件式提示工具可用性）
@server/services/process_runtime/blueprint_research_adapter.py（L57-58 source 常量、L98-135 __init__/dispatch 签名、L268-364 候选与分桶、L365-456 派发五步、L458-536 _build_dispatch_metadata、L540-572 _build_prompt、L635-720 轻量合成、L724-770 aupgrade_to_deep、L774-831 acollect_fitness——**本 plan 唯一允许的非纯追加改动面**）
@server/services/process_runtime/blueprint_schema.py（L29-41 __all__ 与常量、L754-775 预编译 validator 与 _format_error、L793-834 validate_blueprint 签名与后置检查——**只读 analog，禁改禁 import 私有函数**）
@server/services/process_runtime/blueprint_confirm_gate.py（L244-301 build_locked_associations：确认门锁定的 repo_associations 形状 = 阶段 2 的仓集来源）
@server/subagent/api/callbacks.py（L1963-2012 第三链常量与判定/解析、L2090-2115 反查 task、L2118-2145 barrier、L991-998 / L1075-1082 两个挂载点——照第三链形状加第四条）
@server/delivery/services/research_service.py（L40-180 全部签名：create_tasks_for_session / mark_running / mark_stale / record_partial）
@.planning/technical-blueprint/DESIGN.md §5.3（L474-491 RepoPlan 十一字段权威形状）
</context>

<tasks>

<task type="auto">
  <name>Task 1: blueprint_repo_plan_schema.py —— RepoPlan 独立 jsonschema 模块（三件套 + 绝不外抛）</name>
  <files>server/services/process_runtime/blueprint_repo_plan_schema.py, server/tests/services/process_runtime/test_blueprint_repo_plan_schema.py</files>
  <read_first>server/services/process_runtime/blueprint_schema.py L1-45（模块 docstring 契约书四段 + __all__ + 版本常量）、L700-760（Block/must_haves 等子 schema 写法与预编译 validator 定义）、L760-834（_MAX_ERROR_CHARS / _format_error 12 行 + validate_blueprint 签名与后置检查——**逐字复制这 12 行，不 import**）；.planning/technical-blueprint/DESIGN.md L474-491（§5.3 RepoPlan 十一字段）；server/subagent/api/callbacks.py L1972-1983（_BLUEPRINT_VERDICTS / _BLUEPRINT_ROLES 白名单同值）</read_first>
  <action>
新建 `server/services/process_runtime/blueprint_repo_plan_schema.py`。模块 docstring 四段（照 `blueprint_schema.py:1-20` 语气）：①「阶段 2 分仓方案（RepoPlan）的 jsonschema 契约（DESIGN §5.3）——它是**中间产物**（落 `PartialPlan.content.repo_plan`），不是蓝图文档，故与 `blueprint_schema.py` 平级独立」；②「**绝不修改 `blueprint_schema.py`**：该文件在 112-05 冻结面自检清单内（零命中断言）；报错脱敏出口是**复制**过来的 12 行，不 import 其私有函数」；③「纯函数（无 IO / 无 ORM / 无 LLM），仅依赖 stdlib + jsonschema」；④「`validate_repo_plan` **绝不外抛异常**——调用方靠 `(ok, err)` 决定有界重试 ≤2 轮 → 开澄清线程」。

`__all__ = ["BLUEPRINT_REPO_PLAN_SCHEMA", "REPO_PLAN_CHANGE_TYPES", "validate_repo_plan"]`。

模块常量：
- `_MAX_ERROR_CHARS = 500` + `_TRUNCATED_SUFFIX`（复制 `blueprint_schema.py:760-761` 并保留其解释注释：jsonschema 对 type/enum 类失败会把被校验实例 repr 整段拼进 message 且不截断，而 content 是半可信正文）。
- `REPO_PLAN_ROLES = ("direct", "indirect")`、`REPO_PLAN_VERDICTS = ("suitable", "partial", "unsuitable")`（与 `callbacks._BLUEPRINT_ROLES/_BLUEPRINT_VERDICTS` **同值**，注释标明同源）。
- `REPO_PLAN_CHANGE_TYPES = ("new", "modify", "delete", "indirect")` —— 注释「SCHEMA-03 的 change_type：新建 / 改动 / 删除 / 间接完善；**取值须与 `blueprint_schema.py` 的 `implementation_overview.items[].change_type` 枚举逐字一致**，实现时先 `rg -n 'change_type' server/services/process_runtime/blueprint_schema.py` 核对，若既有枚举用别的字面量则以既有为准（融合投影要直接搬）」。

`BLUEPRINT_REPO_PLAN_SCHEMA`（模块级 dict 常量，`$schema` 用 Draft 2020-12，`description` 兼作 LLM prompting 说明，`additionalProperties` 保持默认允许以兼容演进）。十一字段按 DESIGN §5.3：
- `repository_id`（string，必填，minLength 1）
- `role`（enum `REPO_PLAN_ROLES`，必填）
- `responsibility`（Block[]：`{block_id, text}` 形状，与确认门 `_as_block_list` 产物同形）
- `fitness`（object：`{verdict: enum, reasons: string[], citations: string[]}`）
- `current_state`（array of object：`{summary, findings: [{title, detail, citations[]}]}`——**注释标明这是 merge 阶段 `current_state_analysis` 的直接投影源，字段名不得漂移**）
- `impl_items`（array，必填，minItems 0；每项 `{item_id, title, change_type: enum REPO_PLAN_CHANGE_TYPES, how, files_touched: string[], depends_on: string[], test_strategy, citations: string[]}`；`item_id/title/change_type/how` 必填）
- `apis_provided`（array：`{name, method, path, request_schema, response_schema, description, citations[]}`）
- `apis_consumed`（array：`{name, from_repository_id, method, path, request_schema, response_schema, availability: enum(available|needs_support|unknown), support_repository_id, description}`——**注释：`availability`/`support_repository_id` 是 113-05 跨仓对账的输入面，枚举值不得漂移**）
- `local_impact`（object：`{affected_modules: string[], affected_features: [{name, citations[]}], migration_required: bool, notes}`）
- `risks`（Block[]）
- `open_question_thread_ids`（string[]）
`required = ["repository_id", "role", "impl_items"]`（其余可缺——indirect 仓的能力引用清单只有 `apis_provided` 与 `responsibility`）。

`_REPO_PLAN_VALIDATOR = jsonschema.Draft202012Validator(BLUEPRINT_REPO_PLAN_SCHEMA)`（模块级预编译，注释「避免每次调用重新编译」）。

`def _format_error(json_path: str, message: str) -> str`（**复制** `blueprint_schema.py:764-775`：`redact_secrets_in_text` 包 `try/except Exception: pass` fail-safe + 截断 500 + `f"{json_path}: {msg}"`）。

`def validate_repo_plan(content: Any) -> tuple[bool, str | None]`：
1. `not isinstance(content, dict)` → `(False, "repo_plan 必须是 JSON 对象")`。
2. `errors = sorted(_REPO_PLAN_VALIDATOR.iter_errors(content), key=lambda e: e.json_path)`；有错取首条 → `(False, _format_error(...))`。
3. **后置检查 (a) impl_items 引用完整性**：每项 `depends_on` 里的 id 必须存在于本 RepoPlan 的 `impl_items[].item_id` 集合（跨仓依赖走 `apis_consumed`，不走 `depends_on`）→ 失败返回 `f"impl_items[{i}].depends_on 引用的 {dep} 不存在于本仓实现项"`。
4. **后置检查 (b) needs_support 闭环**：`apis_consumed[]` 中 `availability == "needs_support"` 的项必须有非空 `support_repository_id`（**这是 FLOW-06「消费的接口必须找到提供方或显式标注 needs_support」在仓级的前置守卫**）→ 失败返回可定位的中文错误串。
5. 整个函数体外层包 `try/except Exception as exc: return False, _format_error("$", str(exc))` —— **绝不外抛**。

新建 `server/tests/services/process_runtime/test_blueprint_repo_plan_schema.py`（**零 DB 纯函数测试，不加 django_db 标记**）：
- 合法 direct RepoPlan（含 3 个 impl_items 与 depends_on 内部引用）→ `(True, None)`。
- 合法 indirect RepoPlan（只有 role/responsibility/apis_provided，`impl_items=[]`）→ `(True, None)`。
- 缺 `repository_id` / `role` / `impl_items` 三条各一例 → `(False, err)` 且 `err` 含字段名。
- `role="maybe"` / `change_type="bogus"` / `availability="whatever"` 三条枚举越界 → `(False, err)`。
- `depends_on` 引用不存在的 item_id → `(False, err)` 且 err 含该 id。
- `availability="needs_support"` 但缺 `support_repository_id` → `(False, err)`。
- **绝不外抛**：传 `None` / `[]` / `"str"` / 自引用怪异 dict → 均返回 `(False, str)` 而非抛异常。
- **报错脱敏**：content 里塞 `"friday_pat_deadbeef"` 触发 type 错误 → 返回的 `err` 里 `"friday_pat_"` 零出现且 `len(err) <= 520`。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/process_runtime/test_blueprint_repo_plan_schema.py -x -q</automated>
  </verify>
  <acceptance_criteria>
- `cd server && uv run pytest tests/services/process_runtime/test_blueprint_repo_plan_schema.py -x -q` 全绿。
- `rg -n "def validate_repo_plan" server/services/process_runtime/blueprint_repo_plan_schema.py` 命中；`rg -n "Draft202012Validator" server/services/process_runtime/blueprint_repo_plan_schema.py` 命中（预编译）。
- `rg -n "from services.process_runtime.blueprint_schema import|from .blueprint_schema import" server/services/process_runtime/blueprint_repo_plan_schema.py` 零命中（不 import 受限模块）。
- `git diff --stat server/services/process_runtime/blueprint_schema.py` 输出为空（冻结面零命中）。
- `rg -n "repository_id|role|responsibility|fitness|current_state|impl_items|apis_provided|apis_consumed|local_impact|risks|open_question_thread_ids" server/services/process_runtime/blueprint_repo_plan_schema.py` 十一字段全命中。
- `rg -n "needs_support" server/services/process_runtime/blueprint_repo_plan_schema.py` 命中（仓级前置守卫存在）。
- `rg -n "raise " server/services/process_runtime/blueprint_repo_plan_schema.py` 零命中（绝不外抛）。
  </acceptance_criteria>
  <done>RepoPlan 十一字段 schema 独立成模块、预编译 validator + 两条后置检查、报错脱敏截断、绝不外抛；`blueprint_schema.py` 零改动。</done>
</task>

<task type="auto">
  <name>Task 2: 派发面 mode 关键字扩展（E-1~E-4）+ BlueprintRepoPlanAdapter（仓集/direct 派发/indirect 合成/有界重试/完成判据）</name>
  <files>server/services/process_runtime/blueprint_research_adapter.py, server/services/process_runtime/blueprint_repo_plan.py</files>
  <read_first>server/services/process_runtime/blueprint_research_adapter.py 全文的四个扩展点上下文：L117-135（dispatch 签名与返回形状）、L365-456（派发五步：session_id 前缀 L388、last_output L404-409、use_call_source L432）、L458-536（_build_dispatch_metadata 逐键 env，尤其 L476-478 explore 与 L526 knowledge endpoint）、L540-572（_build_prompt 现有 return 体）、L724-770（aupgrade_to_deep 既有调用方）、L774-831（acollect_fitness 取最新口径）；server/services/process_runtime/blueprint_confirm_gate.py L244-301（锁定 repo_associations 形状 = 仓集来源）；server/services/process_runtime/architect_merge_adapter.py L94-117（LLM 合成器五步骨架，**只读 analog**：ProviderConfigService.aresolve → build_chat_model → use_call_source → _content_to_text → _parse_json）；server/delivery/services/research_service.py L101-180（record_partial 整体覆写 + mark_stale）</read_first>
  <action>
**A. `blueprint_research_adapter.py` 的加性扩展（四个扩展点，改动可枚举）**

- **E-1** `dispatch(self, session, *, force_deep_repository_ids=None, mode: str = "research", repository_ids: set[str] | None = None) -> dict`
  - `mode="research"` 时全链路与 112 **逐字一致**（既有两个调用方 `aupgrade_to_deep:750` 与 `_h_bp_repo_research` 零改动）。
  - `repository_ids` 非 None 时**跳过 `_collect_candidates`**，直接以该集合为仓集（阶段 2 的仓集来自确认门锁定的 `repo_associations`，语义与路由候选不同）；为 None 时保持 112 行为。
  - 在函数 docstring 里加一段「113 扩展点」注释，写明「`mode` 只影响 prompt / session_id 前缀 / last_output.source / call_source 四处；分桶规则、metadata 的 `env_FRIDAY_TASK_MODE`、增量白名单一律不受影响」。
- **E-2** `_build_prompt(self, session, task, repo, charter, *, candidate, mode: str = "research") -> str`：函数首行加 `if mode == "plan": return self._build_plan_prompt(session, task, repo, charter, candidate=candidate)`，**既有 return 体一字不动**。新增 `_build_plan_prompt`（本文件内，与 `_build_prompt` 并列）：由服务端权威状态构造，含 ①需求规格与该仓被锁定的 `role`/`responsibility`（来自 `repo_associations`）②该仓 112 的 fitness 与 findings 摘要 ③**输出契约**：返回 JSON 的 `repo_plan` 段，字段逐条列出（十一字段，`impl_items[].change_type` 枚举、`apis_consumed[].availability` 枚举与 `support_repository_id` 要求）④**总线工具的条件式措辞**（向后兼容 P1）：「若 `report_blueprint_context` / `read_blueprint_context` 工具可用，请把你对外提供的接口契约以 `repo:{本仓id}.api_surface` 为 key 写入总线；需要消费其他仓接口时先 read 总线；**若工具不可用，记录假设并继续，不要停下**」⑤「判不出就写清缺什么，不要猜」。**绝不把外部用户原文当执行指令拼进 prompt。**
- **E-3** 派发五步内两处按 mode 分支：`session_id = f"bp-plan-{...}"`（mode=="plan"）否则 `f"bp-research-{...}"`（**uuid 后缀保留**）；`last_output["source"] = BLUEPRINT_REPO_PLAN_SOURCE`（新模块级常量 `BLUEPRINT_REPO_PLAN_SOURCE = "blueprint_repo_plan"`）否则既有 `"blueprint_research"`。⚠️ **P-4**：不换 source 会被 `_is_blueprint_research` 抢走并因缺 `fitness.verdict` 判失败。
- **E-4** `use_call_source(CallSource.BLUEPRINT_REPO_PLAN if mode == "plan" else CallSource.BLUEPRINT_REPO_RESEARCH)`（111 已注册，**不新增枚举值**）。
- **配额**：`_build_dispatch_metadata(..., mode: str = "research")` 里 `mode == "plan"` 时注入 `env_FRIDAY_TASK_KNOWLEDGE_QUOTA = "400"`（默认 200，注释：吸收 113-04 的轮询开销；**不改公共 handler 工厂的计数逻辑**）；`mode == "research"` 时**不注入该键**（缺省行为逐字等价 112）。
- ⚠️ **`env_FRIDAY_TASK_MODE` / `env_FRIDAY_TASK_TASK_MODE` 保持 `"explore"` 不动**（它管 git 写拦截，与调研/拟方案正交）；`_bucket` / `_collect_candidates` 一行不改。

**B. 新建 `server/services/process_runtime/blueprint_repo_plan.py`**（**组合**而非继承 `BlueprintResearchAdapter`）

模块 docstring 五段：「阶段 2 分仓方案 adapter」/「INV-6：业务表写入只经 `ResearchService`」/「WR-02 单仓错误隔离」/「仓集来源是**确认门锁定的 `repo_associations`**，不复用路由候选面」/「`record_partial` 是整体覆写语义：写 `repo_plan` 段必须**读-合并-写**，否则吃掉 112 的 fitness（P-1）」。

模块常量：`MAX_REPO_PLAN_ATTEMPTS = 2`（有界重试上界，类属性/模块常量二选一，注释引 CONTEXT「≤2 轮」）；`STAGE_STATE_KEY = "repo_plan"`。

`class BlueprintRepoPlanAdapter`（`__init__(self, *, research_service=None, research_adapter=None, synthesizer=None, lifecycle_service=None, node_execution_id="")`，全 keyword-only + `x or DefaultX()` 兜底）：

- `async def acollect_locked_repos(self, session) -> list[dict]`：从**最新** `ArtifactVersion`（`ArtifactVersion.objects.filter(artifact_id=…).order_by("-version_no").afirst()`，**不读 `session.current_artifact_version`**——它会落后，112-05 Deviation 3）的 `content["repo_associations"]` 取仓集，每项含 `repository_id/role/responsibility/fitness`。缺失回落 `stage_state["confirmation"]` 快照。返回 `[]` 时上层判「无可拟方案的仓」。
- `async def dispatch_plans(self, session) -> dict`：
  1. `repos = await self.acollect_locked_repos(session)`；空 → 返回恒定形状 `{"dispatched": 0, "synthesized": 0, "pending": 0, "completed": [], "repositories": []}`。
  2. 分两桶：`role == "direct"` → 起容器；`role == "indirect"` → 服务端 LLM 合成。
  3. **direct**：对每仓先 `await self.research_service.mark_stale([task.id])`（**P-2**：`record_partial` 已把 task 置 DONE，不 stale 则 `_DISPATCHABLE_STATUSES` 判定为 done 直接 skip，plan 容器永不启动），再 `await self.research_adapter.dispatch(session, mode="plan", repository_ids={rid, ...})`。**已产出 `repo_plan` 段的仓不重派**（读 `PartialPlan.content` 判定，见完成判据）。
  4. **indirect**：`await self._asynthesize_indirect_plan(session, repo)` —— 服务端 LLM 合成能力引用清单（照 `architect_merge_adapter.py:94-117` 五步骨架：`ProviderConfigService.aresolve()` → `build_chat_model(..., streaming=False)` → `with use_call_source(CallSource.BLUEPRINT_REPO_PLAN)` → `_content_to_text` → `_parse_json`；**`_content_to_text` / `_parse_json` 复制不 import**）。产物过 `validate_repo_plan`，不合格重试至 `MAX_REPO_PLAN_ATTEMPTS`，仍不合格 → 开澄清线程（见第 6 点）并落一份 **degraded 但合法** 的最小 repo_plan（`role="indirect"`, `impl_items=[]`, `risks` 记明缺失原因），**绝不静默丢弃**。
  5. 落库统一走 `await self._arecord_repo_plan(task, repo_plan_section)`：
     ```
     prev = await self._aload_latest_valid_content(task)   # valid=True + -created_at 取最新（复用 acollect_fitness 口径）
     content = {**prev, "repo_plan": repo_plan_section}     # 浅合并：保留 fitness / findings / §7 五键
     await self.research_service.record_partial(task, content)
     ```
     ⚠️ **P-1 的唯一防线**，函数上方写明「裸传 `{'repo_plan': ...}` 会让 `acollect_fitness` 读到空 fitness，确认门/投影全线失血」。
  6. 校验失败的兜底：`await self.lifecycle_service.open_thread(artifact, kind="ai_clarification", blocking=True, question=<含仓名 + 缺哪些字段，不含 content 正文>)`，并把 `thread_id` 写进该仓 repo_plan 的 `open_question_thread_ids`。
  7. 返回恒定形状 dict + 三事件（`blueprint_repo_plan_dispatch_started/completed/failed`，`category="sampling"`、`component="process_runtime"`、`duration_ms`；容器动作另记 `category="caller"` + `initiated_by_user_id`）。
- `async def acollect_repo_plans(self, session) -> dict[str, dict]`：每仓最新 `valid=True` PartialPlan 的 `content.get("repo_plan")`（**一仓多条取最新**，逐字复用 `acollect_fitness` 的三要素：`valid=True` 过滤 + `-created_at` 降序 + 每 `research_task_id` 只取首见）。
- `async def aall_repo_plans_ready(self, session) -> bool`：**自写完成判据**（`_h_bp_repo_plan` 用它，**不复用 `aall_research_tasks_terminal`**——两 stage barrier 同源 + `mark_stale` 会让 stage 1 判据短暂为假）。判据 = 锁定仓集里每个仓在 `acollect_repo_plans` 中都有非空 `repo_plan` 段，或该仓 task 已 `failed`（失败仓不阻塞 barrier，由 merge 阶段标未决项）。
- `async def arequest_targeted_research(self, session, repository_id: str) -> bool`：单仓定向补调研 —— 直接委托既有 `self.research_adapter.aupgrade_to_deep(session, repository_id)`（内部走 `dispatch(force_deep_repository_ids={rid})`），**不新建机制**。
- `def build_stage_state(self, *, plans: dict, dispatched: list, pending: list) -> dict`：产出 `stage_state["repo_plan"]` 小摘要 `{"ready_repository_ids": [...], "pending_repository_ids": [...], "attempts": {rid: n}}`（**只存 id/计数，单字段 < 2KB**，DESIGN §5.6 上下文纪律）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/process_runtime/ -x -q -k "blueprint_repo_plan or blueprint_research"</automated>
  </verify>
  <acceptance_criteria>
- `rg -n 'mode: str = "research"' server/services/process_runtime/blueprint_research_adapter.py` 命中 ≥2（dispatch 与 _build_prompt 各一）。
- `rg -n "bp-plan-" server/services/process_runtime/blueprint_research_adapter.py` 命中；`rg -n "BLUEPRINT_REPO_PLAN_SOURCE" server/services/process_runtime/blueprint_research_adapter.py` 命中。
- `rg -n "CallSource.BLUEPRINT_REPO_PLAN" server/services/process_runtime/blueprint_research_adapter.py` 命中；`rg -c "^\s+BLUEPRINT_" server/agents/call_source.py` 与改动前一致（**未新增枚举值**：`git diff --stat server/agents/call_source.py` 为空）。
- `rg -n 'env_FRIDAY_TASK_KNOWLEDGE_QUOTA' server/services/process_runtime/blueprint_research_adapter.py` 命中且同行/邻行含 `"400"`。
- `rg -n 'env_FRIDAY_TASK_MODE' server/services/process_runtime/blueprint_research_adapter.py -A1 | rg 'explore'` 命中（explore 未被改动）。
- `git diff server/services/process_runtime/blueprint_research_adapter.py | rg "^-" | rg -v "^---" | wc -l` ≤ 12（改动可枚举：仅被扩展的签名行与被加分支的原行）。
- `rg -n "_collect_candidates|_bucket" server/services/process_runtime/blueprint_repo_plan.py` 零命中（阶段 2 不复用路由候选面）。
- `rg -n "aall_research_tasks_terminal" server/services/process_runtime/blueprint_repo_plan.py` 零命中（自写完成判据）。
- `rg -n "retry_task" server/services/process_runtime/blueprint_repo_plan.py` 零命中。
- `rg -n '\{\*\*prev, "repo_plan"' server/services/process_runtime/blueprint_repo_plan.py` 命中（读-合并-写，P-1）。
- `rg -n "order_by\(\"-version_no\"\)" server/services/process_runtime/blueprint_repo_plan.py` 命中；`rg -n "session.current_artifact_version" server/services/process_runtime/blueprint_repo_plan.py` 零命中（P-6 残留口 2）。
- `rg -n "MAX_REPO_PLAN_ATTEMPTS = 2" server/services/process_runtime/blueprint_repo_plan.py` 命中。
- `git diff --stat server/services/process_runtime/builtin_processes.py server/services/process_runtime/entrypoint.py` 输出为空（stage 注册归 113-06）。
  </acceptance_criteria>
  <done>plan 模式四个扩展点全部走带默认值关键字且 research 缺省路径逐字等价 112；RepoPlan adapter 的仓集/派发/合成/落库/完成判据五面就位；read-merge-write 保住 fitness；配额提到 400 且未碰公共 handler 工厂。</done>
</task>

<task type="auto">
  <name>Task 3: callbacks.py 第四条 PLAN 链 + 编排面测试（P-1 / P-4 两条可证伪断言）</name>
  <files>server/subagent/api/callbacks.py, server/tests/subagent/test_blueprint_repo_plan_callback.py, server/tests/services/process_runtime/test_blueprint_repo_plan_stage.py</files>
  <read_first>server/subagent/api/callbacks.py L1963-2012（第三链段注释 + 常量 + _is_blueprint_research + _parse_blueprint_fitness）、L2090-2163（_aload_blueprint_research_task + 完成/失败处理 + barrier + 「record_partial 已含置 done」注释）、L991-998 与 L1075-1082（两个挂载点的 try/except 形状）、last_output.source → CallSource 映射处；server/tests/subagent/test_blueprint_research_callback.py（同构新链测试范式全文）；server/tests/services/process_runtime/test_blueprint_research_stage.py L1-122（模块 docstring 编号清单 + 双 pytestmark + _FakeDispatcher 与工厂区段）</read_first>
  <action>
① `server/subagent/api/callbacks.py` **纯追加**第四条链（形状与第三链逐条对称；既有三链与 `arevoke_task_tokens` 位置一字不动）：
- `_BLUEPRINT_REPO_PLAN_SOURCE = "blueprint_repo_plan"`（回调侧独立常量，与派发侧同值）。
- `def _is_blueprint_repo_plan(session) -> bool`：`task_type == PLAN and (last_output or {}).get("source") == "blueprint_repo_plan"`。**四链互斥**（`_is_plan_research` / `_is_blueprint_research` / `_is_repo_verify` / 本判定）。
- `def _parse_blueprint_repo_plan(output) -> dict | None`：优先结构化透传（`output` 含 `repo_plan`），否则从 `output["text"]` 提 ```json 围栏 / 花括号跨度再 `json.loads`；取出 `repo_plan` 段后过 `validate_repo_plan`（import 自 113-03 Task 1 的新模块）；不合格返回 `None` 并把错误串带出（用 `(section, err)` 元组返回，或模块内私有 dataclass —— 实现自选，但错误串必须可进 `mark_failed` 的 error dict）。
- `async def _aload_blueprint_plan_task(session)`：由 `last_output["research_task_id"]` 反查 `RepoResearchTask`；缺 id → None。
- `async def _handle_blueprint_repo_plan_completion(session)`：解析成功 → 经 `BlueprintRepoPlanAdapter()._arecord_repo_plan(task, section)`（**read-merge-write**，回调零裸 ORM 写，INV-6）+ emit completed；解析/校验失败 → `attempt = <从 PartialPlan/stage_state 读该仓已重试次数>`，`attempt < MAX_REPO_PLAN_ATTEMPTS` → `mark_stale` 触发重跑（有界重试）；否则 `mark_failed({"reason": "repo_plan_invalid", "detail": <脱敏截断的 err>})` + 开澄清线程；末尾触发 barrier。
- `async def _handle_blueprint_repo_plan_failure(session)`：`mark_failed` + emit + barrier。
- 两个挂载点：在 `_handle_completed` / `_handle_failed` 内紧随第三链之后各加一段 `if _is_blueprint_repo_plan(session): try: ... except Exception: logger.warning(...)`（注释逐字沿用既有「永不阻塞主流程」）。**新增 elif/if 写成单行条件形态**，避免 formatter 波及紧随其后的既有分支。
- `last_output.source → CallSource` 映射加一条 `"blueprint_repo_plan" → CallSource.BLUEPRINT_REPO_PLAN`。
- ⚠️ **只跑 `ruff check`，绝不跑 `ruff format`**（P-10）。

② 新建 `server/tests/subagent/test_blueprint_repo_plan_callback.py`（照第三链测试范式）：
- ⭐ **P-4 可证伪断言**：给 plan 容器 session（`source="blueprint_repo_plan"`）跑 `_is_blueprint_research(session)` **必须为 `False`**；反向 `_is_blueprint_repo_plan` 对 `source="blueprint_research"` 的 session 也为 `False`；对 `plan_research` / `repo_verify` 两链同样互斥（**四链两两互斥矩阵**）。
- 成功路径：结构化 output 带合法 `repo_plan` → 重读 DB 断言 `PartialPlan.content["repo_plan"]["impl_items"]` 就位且 `RepoResearchTask.status == done`。
- ⭐ **P-1 可证伪断言**：预置该 task 已有含 `fitness.verdict="suitable"` 与 `findings` 的 PartialPlan → 回调写入 repo_plan 后，`acollect_fitness(session)[repo_id]["verdict"] == "suitable"` 仍成立，且 `content["findings"]` 与 §7 五键仍在（**read-merge-write 未吃掉 112 产物**）。
- 文本路径：output 只有 ```json 围栏 → 同样解析成功。
- 校验失败有界重试：非法 `repo_plan`（缺 `impl_items`）第 1 次 → task 变 `stale`（触发重跑）且不落非法 content；第 3 次（超 `MAX_REPO_PLAN_ATTEMPTS`）→ `failed` + `reason == "repo_plan_invalid"` + 存在 `BlueprintThread(kind="ai_clarification", blocking=True)`。
- CallSource 映射：`"blueprint_repo_plan"` → `CallSource.BLUEPRINT_REPO_PLAN`（不回退 `sdk_agent_task`）。
- 既有链回归：`cd server && uv run pytest tests/subagent/ -q` 全绿。

③ 新建 `server/tests/services/process_runtime/test_blueprint_repo_plan_stage.py`（`django_db(transaction=True)` + `asyncio`，`_FakeDispatcher` 替身）：
- **仓集来源**：预置最新 `ArtifactVersion.content["repo_associations"] = [A(direct), B(indirect)]` → `acollect_locked_repos` 返回两仓；**断言未读 `session.current_artifact_version`**（构造一个落后的 session 版本，其 `repo_associations` 为空，仍能取到最新版的两仓）。
- **direct 派发**：断言 `DispatchTask.task_id` 以 `bp-plan-` 开头、`metadata["env_FRIDAY_TASK_KNOWLEDGE_QUOTA"] == "400"`、`metadata["env_FRIDAY_TASK_MODE"] == "explore"`（未被改）、`SubAgentSession.last_output["source"] == "blueprint_repo_plan"`。
- **mode 缺省等价性**（⭐守 112 零回归）：`dispatch(session)`（不传 mode）产出的 `task_id` 以 `bp-research-` 开头且 `last_output["source"] == "blueprint_research"`（112 路径逐字未变）。
- **indirect 合成**：mock LLM 返合法 JSON → 落 `repo_plan` 段且 dispatcher `await_count == 0`（不起容器）；mock LLM 返非法 JSON 三次 → 落 degraded 但**过 `validate_repo_plan`** 的最小 repo_plan 且开了澄清线程（**不静默丢弃**）。
- **完成判据**：两仓只有一仓有 `repo_plan` → `aall_repo_plans_ready` 为 `False`；两仓都有 → `True`；一仓 `task.status == failed` + 另一仓 ready → `True`（失败仓不阻塞 barrier）。
- **不复用 barrier**：`rg` 断言由 acceptance_criteria 覆盖；本测试另断言 `mark_stale` 后 `aall_repo_plans_ready` **不因 task 状态翻转而误判**（只看 `repo_plan` 段存在性）。
- **单仓定向补调研**：`arequest_targeted_research` 委托 `aupgrade_to_deep` 且返回其 bool（用 AsyncMock 断言被调一次、入参正确）。
- **stage_state 小摘要**：`build_stage_state` 产出只含 id/计数，`len(json.dumps(...)) < 2048`。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/subagent/ tests/services/process_runtime/test_blueprint_repo_plan_stage.py -x -q</automated>
  </verify>
  <acceptance_criteria>
- `cd server && uv run pytest tests/subagent/ tests/services/process_runtime/ -q` 全绿。
- `rg -c "_is_blueprint_repo_plan" server/subagent/api/callbacks.py` ≥ 3（定义 + 两个挂载点）。
- `rg -n "BLUEPRINT_REPO_PLAN" server/subagent/api/callbacks.py` 命中（CallSource 映射已补）。
- `git diff server/subagent/api/callbacks.py | rg "^-" | rg -v "^---"` 输出为空（**纯追加**）。
- `rg -n "validate_repo_plan" server/subagent/api/callbacks.py` 命中（落库前过 schema）。
- `rg -n "_is_blueprint_research" server/tests/subagent/test_blueprint_repo_plan_callback.py` 命中（P-4 互斥断言存在）。
- `rg -n "acollect_fitness" server/tests/subagent/test_blueprint_repo_plan_callback.py` 命中（P-1 断言存在）。
- `rg -n "bp-research-" server/tests/services/process_runtime/test_blueprint_repo_plan_stage.py` 命中（mode 缺省等价性断言存在）。
- `cd server && uv run ruff check subagent/` 通过；**不执行** `ruff format subagent/api/callbacks.py`。
  </acceptance_criteria>
  <done>第四条 PLAN 链与既有三链两两互斥、落库前过 RepoPlan schema 且有界重试后开澄清线程；P-1（fitness 不被吃掉）与 P-4（source 不被抢）两条可证伪断言在测试里成立；112 的 research 缺省路径实测零回归。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plan 容器回调 → `PartialPlan.content.repo_plan` | 容器内 AI 产出的半可信 JSON 进入融合投影数据 |
| indirect LLM 合成 → RepoPlan | 服务端 LLM 产物同样是半可信 |
| plan 容器 → 容器知识 MCP（配额 400） | 提高的配额意味着更大的调用面 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113-13 | Tampering | 容器返回伪造/残缺 RepoPlan 污染融合 | mitigate | 落库前过 `validate_repo_plan`（十一字段 + 两条后置检查）；不合格有界重试 ≤2 轮后开 blocking 澄清线程，**绝不静默降级** |
| T-113-14 | Tampering | plan 产物被调研解析器抢走并误判失败（P-4） | mitigate | 独立 `source="blueprint_repo_plan"` + 独立判定；测试断言四链两两互斥 |
| T-113-15 | Tampering | `record_partial` 整体覆写吃掉 112 的 fitness（P-1） | mitigate | 唯一落库入口 `_arecord_repo_plan` 强制读-合并-写；测试断言写入后 `acollect_fitness` 值不变 |
| T-113-16 | Denial of Service | 校验失败无界重跑烧容器额度 | mitigate | `MAX_REPO_PLAN_ATTEMPTS = 2` + 超界转澄清线程；已产出 `repo_plan` 段的仓不重派 |
| T-113-17 | Denial of Service | 提高配额到 400 导致单容器把知识面打爆 | accept | 配额仍有硬上界（400），耗尽后既有工厂返回「基于已有上下文继续」的降级文案；不改工厂计数逻辑是更高优先的约束 |
| T-113-18 | Info Disclosure | prompt / 异常文本夹带凭证或用户原文指令 | mitigate | prompt 由服务端权威状态构造，不拼外部原文当指令；异常经 `redact_secrets_in_text` 截断 500 后入日志；明文 token 不进日志与事件 payload |
| T-113-19 | Elevation of Privilege | plan 容器误获 git 写能力 | mitigate | `env_FRIDAY_TASK_MODE` / `env_FRIDAY_TASK_TASK_MODE` 保持 `explore`（双层 git 写拦截）；验收含 rg 断言 |
| T-113-SC | Tampering | 依赖供应链 | accept | 零新增外部依赖（jsonschema 已在用） |
</threat_model>

<verification>
- `cd server && uv run pytest tests/services/process_runtime/ tests/subagent/ tests/delivery/ -q` 全绿。
- `cd server && uv run python manage.py makemigrations --check --dry-run` 退出码 0（零模型改动）。
- `cd server && uv run ruff check services/process_runtime/ subagent/`；`uv run ruff format --check services/process_runtime/blueprint_repo_plan.py services/process_runtime/blueprint_repo_plan_schema.py`。**不对 `subagent/api/callbacks.py` 跑 format**。
- 冻结面自检：`git diff --name-only` 不含 `codegraph/services/repo_router_v2.py`、`process_runtime/{decompose_segments,research_adapter,architect_merge_adapter,merged_plan,clarify_adapter,render,resume,builtin_processes,entrypoint,blueprint_schema,blueprint_route,blueprint_spec_gate,blueprint_confirm_gate,blueprint_resume,blueprint_lifecycle_service}.py`、`repositories/services/charter_service.py`、`system/{models,settings_service}.py`、`delivery/services/event_taxonomy.py`、`agents/call_source.py`。
- 受限面自检：`git diff server/subagent/api/callbacks.py | rg "^-" | rg -v "^---"` 输出为空。
- 派发面改动可枚举：`git diff server/services/process_runtime/blueprint_research_adapter.py | rg "^-" | rg -v "^---" | wc -l` ≤ 12。
- 并行自检：`git diff --name-only` 与 113-02 零交集（不含 `mcp_tools/` 与 `task/`）。
</verification>

<success_criteria>
- FLOW-05：每个 direct 仓产出 RepoPlan（实现项含 change_type/how/涉及文件/测试策略、提供与消费 API、局部影响、风险），indirect 仓产出能力引用清单；拟定中可开澄清线程、可发起单仓定向补调研（`arequest_targeted_research` 复用既有 `force_deep_repository_ids` 通路）。
- SCHEMA-03 原料面：`impl_items[]` 逐项标 `change_type` 并带 `files_touched` / `depends_on`，为 113-05 的功能↔模块↔仓库映射与波次派生提供确定性输入。
- ROADMAP SC1：direct 起容器 / indirect 轻量合成两条产出路径均可跑通且产物过 schema。
</success_criteria>

<output>
完成后创建 `.planning/phases/113-2/113-03-SUMMARY.md`（provides：`validate_repo_plan` 签名与十一字段/两条后置检查、`REPO_PLAN_CHANGE_TYPES` 实际取值、`BlueprintRepoPlanAdapter` 六个公开方法签名与返回形状、`dispatch(mode=/repository_ids=)` 扩展契约与缺省等价性、`BLUEPRINT_REPO_PLAN_SOURCE` 常量、`stage_state["repo_plan"]` 键形状、`aall_repo_plans_ready` 的完成判据口径、callbacks 第四链的判定函数名——113-04 的 waiting_context 分支、113-05 的投影、113-06 的 handler 全部直接消费）。
</output>
