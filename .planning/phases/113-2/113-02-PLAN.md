---
phase: 113-2
plan: 02
type: execute
wave: 2
depends_on: [113-01]
files_modified:
  - server/mcp_tools/serializers.py
  - server/mcp_tools/views.py
  - server/mcp_tools/urls.py
  - task/core/knowledge_tools.py
  - server/tests/mcp_tools/test_blueprint_context_tools.py
  - task/tests/test_blueprint_context_tools_schema.py
autonomous: true
requirements: [BUS-01]
prohibitions:
  - "禁止修改 server/codegraph/services/repo_router_v2.py"
  - "禁止修改 process_runtime 六冻结文件：decompose_segments.py / research_adapter.py / architect_merge_adapter.py / merged_plan.py / clarify_adapter.py / render.py"
  - "禁止修改 server/services/process_runtime/resume.py 与 server/repositories/services/charter_service.py"
  - "禁止修改 ConvergenceSessionEvent 既有类型与 builtin_processes.py 的 _TECHNICAL_PLAN_STAGES / _ECHO_STAGES"
  - "禁止修改 server/system/settings_service.py 既有 getter"
  - "🔒 禁止修改 task/core/knowledge_tools.py 的公共 handler 工厂 `_make_knowledge_handler`（`timeout=60.0` 禁改、禁加 per-tool 覆盖参数、`quota_counter` 计数逻辑禁改、禁加 callback 参数）——它是 7 个既有工具共用的唯一工厂；本 plan 对该文件唯一允许的改动是往 `KNOWLEDGE_TOOL_SCHEMAS` 追加表项"
  - "禁止把新工具做成 extra 源挂载（extra 源必须自带 _BUILTIN_CODING_TOOLS，漏列会连带禁掉 Bash/Edit/Write，WR-02 前科）：三个总线工具一律走 knowledge 白名单"
  - "禁止实现服务端长轮询（容器侧 60s 硬超时 + ASGI 协程占用 + 本仓零先例）；本 plan 的两个 view 必须即时返回"
  - "禁止在 view 里裸写 ORM：读写一律经 113-01 的 BlueprintContextService（INV-6）"
  - "禁止任何路径返回 5xx（容器 handler 会把 5xx 吞成「调用失败」文案，agent 拿不到原因）；未知工具名 / 会话校验失败一律结构化 4xx 或 200+applied=false"
  - "禁止仅凭 X-Friday-Session-Id 头信任容器声明的会话归属（它是关联键不是授权凭据，P5）；也禁止只信请求体入参"
  - "禁止修改 blueprint_research_adapter.py（113-03 独占该文件，含配额提升）"
must_haves:
  truths:
    - "容器凭任务 token 调 POST /api/mcp/tools/report_blueprint_context/ 写入总线、调 read_blueprint_context 读回，写入即对同会话其他容器可见"
    - "三道会话校验缺任一条即拒绝：① header session id 对应的 SubAgentSession 存在且属 token owner；② 其关联 ConvergenceSession.process_type == 'technical_blueprint' 且 request.user 是该项目成员；③ 目标条目/入参会话与解析出的会话一致"
    - "跨会话读写被拒绝且不是 5xx：另一 token owner 的 session id、非 technical_blueprint 流程、条目会话不一致三种情形各返回结构化错误（4xx 或 200+applied=false，全 plan 统一为 4xx error_response）"
    - "老镜像（无新工具）不崩：新工具只是不被调用；新镜像打到不存在的 path 得 404 后由 handler 回显 HTTP code 且 return-not-raise，容器不崩"
    - "KNOWLEDGE_TOOL_SCHEMAS 从 7 项增至 9 项（read_/report_blueprint_context），knowledge_allowed_tools() 长度同步为 9；handler 工厂 timeout/quota/签名零改动"
    - "report 写入的 content 经 BlueprintContextService 递归脱敏后入库，view 层与容器侧日志均不含 content 正文"
  artifacts:
    - path: "server/mcp_tools/views.py"
      provides: "ReadBlueprintContextView / ReportBlueprintContextView 两个 McpToolView 子类 + 三道会话校验 helper"
      contains: "_aresolve_blueprint_session"
    - path: "server/mcp_tools/urls.py"
      provides: "tools/read_blueprint_context/ 与 tools/report_blueprint_context/ 两条 path"
      contains: "mcp-tool-read-blueprint-context"
    - path: "task/core/knowledge_tools.py"
      provides: "KNOWLEDGE_TOOL_SCHEMAS 追加两项（工厂零改动）"
      contains: "report_blueprint_context"
    - path: "server/tests/mcp_tools/test_blueprint_context_tools.py"
      provides: "三道校验各一条负向断言 + 脱敏结构保真 + 非 5xx 断言"
      contains: "session_mismatch"
  key_links:
    - from: "server/mcp_tools/views.py"
      to: "server/delivery/services/blueprint_context_service.py"
      via: "append_entry / read_entries（view 零裸 ORM 写，INV-6）"
      pattern: "BlueprintContextService"
    - from: "server/mcp_tools/views.py"
      to: "server/subagent/models"
      via: "X-Friday-Session-Id → SubAgentSession.last_output['blueprint_session_id'] → ConvergenceSession"
      pattern: "blueprint_session_id"
    - from: "task/core/knowledge_tools.py"
      to: "server/mcp_tools/urls.py"
      via: "url = f'{endpoint_base}/api/mcp/tools/{tool_name}/' —— tool_name 拼接，白名单加项即自动可达"
      pattern: "read_blueprint_context"
---

<objective>
把 Context Bus 接到容器：服务端新增 `read_blueprint_context` / `report_blueprint_context` 两个 MCP 工具入口（含**view 层自建的三道会话校验**，因为鉴权链只到 `token → owner(User)`，`X-Friday-Session-Id` 只是关联键不可信作授权依据），客户端 `task/core/knowledge_tools.py` 对称追加两项白名单表项。

Purpose: BUS-01 「蓝图容器经任务 token 实时读写会话级上下文总线，写入即对并行容器可见」。
Output: 2 个 RequestSerializer + 2 个 `McpToolView` 子类 + 2 条 url + 2 个 task 侧 schema 表项 + 2 个测试文件。
</objective>

<execution_context>
工作目录：worktree 根。wave 2，依赖 113-01 的 `BlueprintContextService`（本 plan 只调它，不写 ORM）。
与同 wave 的 113-03 **文件零重叠**：113-03 独占 `blueprint_research_adapter.py` / `callbacks.py` / `process_runtime/`，本 plan 独占 `mcp_tools/` 与 `task/core/knowledge_tools.py`。
`env_FRIDAY_TASK_KNOWLEDGE_QUOTA=400` 的派发侧注入归 **113-03**（它独占派发面文件），本 plan 不碰。
`await_blueprint_context`（第 10 个工具）归 **113-04**，本 plan 只加前两个。
</execution_context>

<context>
@.planning/phases/113-2/113-CONTEXT.md（锁定：MCP 白名单新增 read_/report_blueprint_context 与其入参；**会话隔离必须 view 层自建三道校验**（鉴权链纠偏）；content 过 redact_secrets_in_text；写入即对并行容器可见 server-authoritative；容器镜像向后兼容——未知工具返结构化错误不 500）
@.planning/phases/113-2/113-RESEARCH-BUS.md 主题 1.1（task 侧白名单 + handler 工厂逐行事实表：URL 由 tool_name 拼接、鉴权头三件套、错误约定、日志四字段、空值短路）、1.2（server 侧「一 path 一 View 类、无动态分发」+ 新增须动 4 个位置 + ReportProjectStateView 模板 + McpToolView 基类能力表）、1.3（**鉴权链纠偏**：`return (token.created_by, token)`，不存在 token→session→user；三道校验的确切数据路径）、Pitfalls P1（向后兼容三种组合 + allowed_tools 排他陷阱）、P2（脱敏点与 JSON 递归）、P5（session id 不是鉴权凭据）
@.planning/phases/113-2/113-PATTERNS.md 第 5 类（`search_rag_chunks` 完整 schema 模板逐字 + 🔒 公共 handler 工厂七个共享点禁改表）、第 6 类（view 四步骨架 + 基类白拿四件事 + fail-soft 约定 + serializer 宽松归一放 view + ⭐三道会话校验 + 写入脱敏）、第 11 类 Analog A（`test_report_project_state.py` 测试范式）
@.planning/phases/113-2/113-01-SUMMARY.md（BlueprintContextService 五方法签名与返回形状、kind 枚举、read_entries 的过滤参数与 list 形状）
@server/mcp_tools/views.py（L235-339 McpToolView 基类；L3320-3327 _assert_project_member 形状；L3330-3410 ReportProjectStateView 全文——**analog**；L2684/L3612 异常脱敏截断先例）
@server/mcp_tools/serializers.py（L560-640 ChoiceField 枚举白名单 + 宽松校验 + validate() 跨字段互斥范式）
@server/mcp_tools/urls.py（L5-39 import 区、L41-127 urlpatterns 与 name 命名规则）
@server/access_tokens/authentication.py（L52-103 认证返回 (token.created_by, token)）
@task/core/knowledge_tools.py（L44-235 白名单表；L253-371 handler 工厂——**只读，禁改**；L374-440 build 与 allowed_tools）
@server/tests/mcp_tools/test_report_project_state.py（测试范式全文）
</context>

<tasks>

<task type="auto">
  <name>Task 1: 服务端两个 MCP 工具 view + 三道会话校验 helper（serializers / views / urls）</name>
  <files>server/mcp_tools/serializers.py, server/mcp_tools/views.py, server/mcp_tools/urls.py</files>
  <read_first>server/mcp_tools/views.py L235-339（McpToolView 基类：authentication_classes / handle_exception / _begin / _validate / _record 的完整能力）与 L3320-3410（_assert_project_member + ReportProjectStateView 的 post 四步 + _handle 内 _finish/_skip 闭包 + fail-soft 注释）；server/mcp_tools/serializers.py L555-640（ChoiceField 枚举 + validate 互斥）；server/mcp_tools/urls.py L1-50 与 L100-127（import 区与 urlpatterns 尾部）；server/mcp_tools/errors.py（error_response 签名与信封）；server/subagent/models.py 里 SubAgentSession 的 session_id / last_output / agent_session 字段定义；server/services/process_runtime/blueprint_research_adapter.py L388-410（**只读**：派发侧写入的 session_id 命名与 last_output 四键，三道校验的数据来源）</read_first>
  <action>
① `server/mcp_tools/serializers.py` 追加两个 RequestSerializer（放文件尾部，既有内容零改动）：
- `ReadBlueprintContextRequestSerializer`：`key_prefix = CharField(required=False, allow_blank=True, default="", max_length=200)` / `kind = ChoiceField(choices=[finding, api_surface, contract, decision, dependency_claim, question], required=False, allow_blank=True, default="")` / `repository_id = CharField(required=False, allow_blank=True, default="", max_length=64)` / `since_seq = IntegerField(required=False, min_value=0, default=0)` / `limit = IntegerField(required=False, min_value=1, max_value=200, default=50)`。全部可选——**无参调用等于「拉本会话全部 active 条目（截至 limit）」**。
- `ReportBlueprintContextRequestSerializer`：`key = CharField(required=True, max_length=200)` / `kind = ChoiceField(choices=<同上六值>, required=True)` / `repository_id = CharField(required=False, allow_blank=True, default="", max_length=64)` / `content = JSONField(required=True)` —— `validate()` 断言 `content` 是 dict（不是 list/标量），否则 `ValidationError("content 必须是 JSON 对象")`。

② `server/mcp_tools/views.py` 追加（放文件尾部，既有 view 零改动）：

**三道会话校验 helper**（模块级 `async def _aresolve_blueprint_session(request) -> tuple[Any, Any, str]`，返回 `(convergence_session, subagent_session, error_code)`；`error_code` 为空串表示通过）：
1. 取 header：`raw_sid = request.headers.get("X-Friday-Session-Id", "")`。空 → `("", "", "missing_session_header")`。
2. `@sync_to_async` 查 `SubAgentSession.objects.filter(session_id=raw_sid).first()`（**注意读 FK 需在同步上下文内取标量或 select_related**）。不存在 → `"session_not_found"`。
3. **归属校验**：该 SubAgentSession 的 owner 必须是 `request.user`（token owner）。按既有派发链的归属路径解析（`agent_session.user` 或 `session.created_by`——先 `rg -n "class SubAgentSession" -A 40 server/subagent/models.py` 确认真实字段名，**按实际字段写，不臆造**）。不匹配 → `"session_not_owned"`。
4. `blueprint_session_id = (sub.last_output or {}).get("blueprint_session_id")`。缺失 → `"not_blueprint_session"`。
5. `ConvergenceSession.objects.filter(id=blueprint_session_id).first()`；不存在 → `"session_not_found"`；`process_type != "technical_blueprint"` → `"not_blueprint_session"`。
6. **项目成员校验**：`request.user` 对 `ConvergenceSession.project`（若字段名不同按实际写）走 `_assert_project_member` 同款判定。非成员 → `"not_member"`。
返回时把 `convergence_session` 与 `subagent_session` 一并给调用方。

**第三道（目标条目同会话）** 由两个 view 各自兜住：read 只按解析出的 `convergence_session` 过滤（结构上不可能跨会话）；report 只往解析出的会话写（不接受请求体里的 session 入参——**入参里根本不提供 session 字段**，这是最强的第三道校验：无跨会话入参面）。在两个 view 的类 docstring 里显式写明这条设计。

`class ReadBlueprintContextView(McpToolView)`：`tool_name = "read_blueprint_context"`；`post` 四步骨架照 `ReportProjectStateView:3352-3364`（`_begin` → `_validate` → `started_at` → `_handle`）。`_handle` 内：调 `_aresolve_blueprint_session`，`error_code` 非空 → `await self._record(...)` 后 `return error_response(error_code, <中文 detail>, status_code=403 if error_code in {"session_not_owned","not_member","not_blueprint_session"} else 404)`（**4xx 结构化错误，绝不 5xx**）；通过则 `entries = await BlueprintContextService().read_entries(session=cs, since_seq=…, key_prefix=…, kind=…, repository_id=…, limit=…)` → `output = {"entries": entries, "count": len(entries), "max_seq": max(seq) or since_seq}` → `_record` → `Response(output, 200)`。整段包 `try/except Exception as exc:` → `logger.warning("read_blueprint_context_failed", error=redact_secrets_in_text(str(exc))[:500])` + `return Response({"entries": [], "count": 0, "error": "internal_error"}, status=200)`（**兜底也不 5xx**）。

`class ReportBlueprintContextView(McpToolView)`：`tool_name = "report_blueprint_context"`；同骨架。通过校验后 `entry = await BlueprintContextService().append_entry(session=cs, key=…, kind=…, content=…, repository_id=…, produced_by=raw_sid, initiated_by_user_id=str(request.user.id))` → `output = {"applied": True, "entry_id": str(entry.id), "seq": entry.seq}`。**写入后立即触发 waiter 满足**：`redispatch = await BlueprintContextService().satisfy_waiters(session=cs, key=key, repository_id=repository_id)`，把 `{"satisfied_waiters": len(redispatch)}` 并入 output（**本 plan 只做「置 superseded 并回报计数」，真正的重派在 113-04 接上——在此处留一行注释标明接续点**）。异常兜底 → 200 + `{"applied": False, "reason": "internal_error"}`。
两个 view 的类 docstring 各列「N 道兜底绝不绕过」清单（照 `views.py:3339-3347`）。

③ `server/mcp_tools/urls.py`：import 区追加两个 view 名，`urlpatterns` 尾部追加两条
`path("tools/read_blueprint_context/", ReadBlueprintContextView.as_view(), name="mcp-tool-read-blueprint-context")` 与 `path("tools/report_blueprint_context/", ReportBlueprintContextView.as_view(), name="mcp-tool-report-blueprint-context")`。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/mcp_tools/test_blueprint_context_tools.py -x -q</automated>
  </verify>
  <acceptance_criteria>
- `rg -n "class ReadBlueprintContextView|class ReportBlueprintContextView" server/mcp_tools/views.py` 两处命中；`rg -n "_aresolve_blueprint_session" server/mcp_tools/views.py` 命中 ≥3（定义 + 两个 view 调用）。
- `rg -n "session_not_owned|not_blueprint_session|not_member|session_not_found" server/mcp_tools/views.py` 四个错误码全命中（三道校验的可归因出口）。
- `rg -n "mcp-tool-read-blueprint-context|mcp-tool-report-blueprint-context" server/mcp_tools/urls.py` 两条命中。
- `rg -n "BlueprintContextService" server/mcp_tools/views.py` 命中；`rg -n "BlueprintContextEntry.objects" server/mcp_tools/views.py` 零命中（view 零裸 ORM）。
- `rg -n "status=5[0-9][0-9]|status_code=5[0-9][0-9]" server/mcp_tools/views.py | rg -i "blueprint_context"` 零命中（新增 view 无 5xx 出口）。
- `rg -n "convergence_session_id|session_id" server/mcp_tools/serializers.py | rg -i "blueprint_context"` 零命中（请求体不提供会话入参面，第三道校验结构性成立）。
- `rg -n "redact_secrets_in_text" server/mcp_tools/views.py | rg -c "" ` ≥ 既有值 + 2（两个新 view 的异常分支各一处）。
- `git diff server/mcp_tools/urls.py | rg "^-" | rg -v "^---"` 输出为空（urls 纯追加）。
  </acceptance_criteria>
  <done>服务端两个工具入口就位：三道会话校验在 view 层自建且四个错误码可归因，全路径 4xx/200 无 5xx，写入经 service 脱敏入库并顺带置位命中的 waiter。</done>
</task>

<task type="auto">
  <name>Task 2: task 侧 KNOWLEDGE_TOOL_SCHEMAS 追加两项（公共 handler 工厂零改动）</name>
  <files>task/core/knowledge_tools.py, task/tests/test_blueprint_context_tools_schema.py</files>
  <read_first>task/core/knowledge_tools.py L44-235（七项 schema 表全文，尤其 L53-80 的 search_rag_chunks 模板与 description 三句式）、L253-371（**只读**：handler 工厂 URL 拼接/鉴权头/配额/错误约定/日志字段——确认新增表项无需碰它）、L374-440（build 函数与 knowledge_allowed_tools）；task/core/executor.py L136-158（挂载点与 allowed_tools 排他白名单）；task/core/config.py L101-110（knowledge_endpoint / knowledge_quota 默认值）</read_first>
  <action>
在 `task/core/knowledge_tools.py` 的 `KNOWLEDGE_TOOL_SCHEMAS`（`:52`）**列表尾部追加两项**，逐字照 `search_rag_chunks`（`:53-80`）的形状（三句式 description + 每个 property 标注必填/默认值/上界）。**除这两个表项外，本文件零改动**——不碰 `_make_knowledge_handler`、不碰 `build_knowledge_mcp_server`、不碰 `knowledge_allowed_tools`。

- `read_blueprint_context`：description「读取本次蓝图会话的共享上下文总线：拿到其他并行仓容器已写入的接口契约 / 现状结论 / 决策。适合「我要消费 B 仓的接口，先看它有没有写出来」类问题。**支持 since_seq 增量拉取，轮询时务必带上上次返回的 max_seq，避免重复拉全量**。」
  properties：`key_prefix`（「key 前缀过滤，如 `repo:<uuid>.` 或 `contract:`」）/ `kind`（「条目类型：finding / api_surface / contract / decision / dependency_claim / question」）/ `repository_id`（「按产出仓过滤（UUID）」）/ `since_seq`（「只返回 seq 大于该值的条目（增量拉取，默认 0 = 全量）」）/ `limit`（「返回条数上限（默认 50，最大 200）」）。`required: []`。
- `report_blueprint_context`：description「向本次蓝图会话的共享上下文总线写入一条结论：你产出的接口契约 / 关键现状 / 决策，写入后**立即对所有并行仓容器可见**。适合「我定下了对外接口，让等我的仓能继续」场景。key 用约定前缀：`repo:{仓UUID}.api_surface` / `contract:{名称}` / `decision:{线程id}`。」
  properties：`key`（「（必填）条目 key，遵循前缀约定」）/ `kind`（「（必填）条目类型，六值枚举」）/ `repository_id`（「产出仓 UUID（可选）」）/ `content`（`{"type": "object", "description": "（必填）结论正文 JSON 对象；服务端会递归脱敏，不要写入任何令牌/密钥"}`）。`required: ["key", "kind", "content"]`。

**向后兼容（P1）**：本文件是 task 侧硬编码白名单、不从服务端下发——老镜像没有这两项只是不调，天然安全；新镜像打到未部署的服务端得 404 → 工厂 `:329-340` 只回显 HTTP code + `is_error`，容器不崩。这条无需代码改动，但要在追加表项上方写一行注释记录该结论（供后续 reviewer 免于重推）。

新建 `task/tests/test_blueprint_context_tools_schema.py`（若 task 侧既有测试目录名不同，按实际目录放置——先 `ls task/tests/ 2>/dev/null || rg -l "KNOWLEDGE_TOOL_SCHEMAS" task/`）：
- **工厂零改动守护**（⭐可证伪）：`inspect.signature(_make_knowledge_handler)` 的参数名元组恒等于 `("tool_name","endpoint_base","user_token","session_id","quota","quota_counter")`（**新增参数即红**）；`inspect.getsource(_make_knowledge_handler)` 里 `"timeout=60.0"` 仍出现且 `"callback"` 零出现。
- **白名单计数**：`len(KNOWLEDGE_TOOL_SCHEMAS) == 9`；`len(knowledge_allowed_tools()) == 9`；七个既有工具名全部仍在（逐名断言，防误删）。
- **schema 形状**：两个新项均有 `name` / `description` / `input_schema`，`input_schema["type"] == "object"`，`report_blueprint_context` 的 `required == ["key","kind","content"]`，`read_blueprint_context` 的 `required` 为空。
- **URL 可达性**：`f"{base.rstrip('/')}/api/mcp/tools/{name}/"` 对两个新名分别等于 `/api/mcp/tools/read_blueprint_context/` 与 `/api/mcp/tools/report_blueprint_context/`（与 113-02 Task 1 的 urls 逐字一致）。
- **空值短路回归**：`build_knowledge_mcp_server("", "tok", "sid", 200) is None` 与 `build_knowledge_mcp_server("http://x", "", "sid", 200) is None`（老镜像/未配置零回归）。
  </action>
  <verify>
    <automated>cd task && uv run pytest tests/test_blueprint_context_tools_schema.py -x -q</automated>
  </verify>
  <acceptance_criteria>
- `rg -c '"name": "' task/core/knowledge_tools.py` == 9。
- `rg -n "read_blueprint_context|report_blueprint_context" task/core/knowledge_tools.py` 两个工具名命中。
- `rg -n "timeout=60.0" task/core/knowledge_tools.py` 命中且**仅 1 处**（`rg -c "timeout=60.0" task/core/knowledge_tools.py` == 1）。
- `rg -n "callback" task/core/knowledge_tools.py` 零命中（工厂未被加 callback 参数）。
- `git diff task/core/knowledge_tools.py | rg "^-" | rg -v "^---"` 输出为空（**纯追加**：工厂与 build 函数一行未删改）。
- `git diff --stat task/core/executor.py task/core/config.py` 输出为空（挂载点与配置零改动）。
- `cd task && uv run pytest -q` 全绿（既有 task 测试零扰动）。
  </acceptance_criteria>
  <done>容器侧两个总线工具进白名单且自动获得 handler/URL/allowed_tools；公共 handler 工厂签名与 timeout=60.0 有可证伪的守护断言；老镜像与未配置端点两条降级路径回归通过。</done>
</task>

<task type="auto">
  <name>Task 3: 端到端 MCP 工具测试（三道校验负向 + 脱敏保真 + 非 5xx + 观测留痕）</name>
  <files>server/tests/mcp_tools/test_blueprint_context_tools.py</files>
  <read_first>server/tests/mcp_tools/test_report_project_state.py 全文（fixture `mcp_client, access_user` 两件套、真打 URL、@sync_to_async 独立查询 helper、断言分层、必测负向清单）；server/tests/mcp_tools/conftest.py（mcp_client fixture 的 token 铸造方式）；113-PATTERNS 第 11 类的本文件守护点清单</read_first>
  <action>
新建 `server/tests/mcp_tools/test_blueprint_context_tools.py`，模块 docstring 用「覆盖：」+ 破折号清单列守护点。
`pytestmark = pytest.mark.django_db(transaction=True)`；`_READ_URL = "/api/mcp/tools/read_blueprint_context/"`、`_REPORT_URL = "/api/mcp/tools/report_blueprint_context/"`（**真打 URL，端到端过认证 + serializer + view**）。

工厂（`@sync_to_async` 或 `acreate`）：`_make_blueprint_session(owner)` 建 `ConvergenceSession(process_type="technical_blueprint", project=…)`；`_make_subagent_session(owner, blueprint_session)` 建 `SubAgentSession(task_type=PLAN, last_output={"source": "blueprint_repo_plan", "blueprint_session_id": str(cs.id), "repository_id": …})`；独立查询 helper `_entry_count(cs)` / `_first_entry(cs)`（**断言从 DB 重读，不信响应体**）。

必测用例：
1. **写读闭环**：report 一条 `kind="api_surface"` → 200 + `applied=True` + `seq==1`；另一「同会话不同 SubAgentSession」的 token 调 read → 拿到该条目（**写入即对并行容器可见**的可证伪断言）。
2. **增量拉取**：写 3 条 → `read(since_seq=1)` 只返回 seq 2/3 且 `max_seq==3`。
3. **三道校验负向各一条**（⭐核心）：
   - ① `X-Friday-Session-Id` 指向**另一个 token owner** 的 SubAgentSession → 403 且 `error_code == "session_not_owned"`；DB 零新增条目。
   - ② SubAgentSession 关联的 ConvergenceSession `process_type="technical_plan"`（非蓝图）→ 403 `not_blueprint_session`；DB 零新增。
   - ③ 项目成员校验：token owner 不是该 project 成员 → 403 `not_member`；DB 零新增。
   另加：缺 header → 404/403 `missing_session_header`；header 指向不存在的 session → 404 `session_not_found`。
4. **跨会话读隔离**（⭐核心）：会话 A 写 2 条、会话 B 写 1 条 → 用 B 的 session header 调 read → **只返回 B 的 1 条**（A 的条目一条都拿不到）。
5. **脱敏结构保真**：report `content={"raw":"friday_pat_deadbeefcafe1234","nested":{"n":7,"s":"sk-AAAABBBBCCCC"}}` → 重读 DB 断言 `"friday_pat_"` / `"sk-"` 在 `json.dumps(entry.content)` 中零出现，且 `entry.content["nested"]["n"] == 7`（结构未塌）。
6. **绝不 5xx**（⭐核心）：monkeypatch `BlueprintContextService.append_entry` 抛异常 → 响应 `status_code == 200` 且 body `applied is False`（**断言 `resp.status_code < 500`**）；未知工具路径 `/api/mcp/tools/no_such_tool/` → 404（Django 路由层）而非 500。
7. **kind 非法**：`kind="bogus"` → 400 `invalid_params`（serializer 层）且 DB 零新增。
8. **content 非 object**：`content=[1,2]` → 400 且 DB 零新增。
9. **waiter 顺带满足**：先经 service 登记一个等 `repo:X.api_surface` 的 waiter → report 该 key → 响应含 `satisfied_waiters == 1` 且该 waiter 行 `status == "superseded"`。
10. **观测留痕**：断言 `ToolCallRecord.tool_name` 与 `RequestMetric.labels["call_source"]` 落到两个新 `tool_name`；并断言这两条留痕里**不含** content 正文字符串。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/mcp_tools/test_blueprint_context_tools.py -x -q</automated>
  </verify>
  <acceptance_criteria>
- `cd server && uv run pytest tests/mcp_tools/test_blueprint_context_tools.py -x -q` 全绿。
- `rg -n "session_not_owned" server/tests/mcp_tools/test_blueprint_context_tools.py` 命中（跨 owner 负向断言存在）。
- `rg -n "not_blueprint_session" server/tests/mcp_tools/test_blueprint_context_tools.py` 命中（process_type 负向断言存在）。
- `rg -n "status_code < 500|< 500" server/tests/mcp_tools/test_blueprint_context_tools.py` 命中（非 5xx 可证伪断言存在）。
- `rg -n "friday_pat_" server/tests/mcp_tools/test_blueprint_context_tools.py` 命中（脱敏断言存在）。
- `cd server && uv run pytest tests/mcp_tools/ -q` 全绿（既有 MCP 工具测试零扰动）。
  </acceptance_criteria>
  <done>三道会话校验各有可证伪的负向断言、跨会话读隔离被实测、脱敏结构保真、全路径无 5xx、观测留痕不含正文。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 容器 → `/api/mcp/tools/*_blueprint_context/` | 容器凭 PAT 跨进程读写会话共享面；token 只证明「谁的 token」，不证明「哪个会话」 |
| `X-Friday-Session-Id` header | 派发时服务端自写、但传输链路上仍是容器可改的字符串 |
| 请求体 `content` | 容器内 AI 产出的半可信 JSON |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113-07 | Elevation of Privilege | 容器伪造 session id 读写他人会话总线 | mitigate | view 层三道校验 fail-closed：SubAgentSession 属 token owner + 关联 ConvergenceSession 是 technical_blueprint + owner 是项目成员；请求体**不提供会话入参面**，写入目标只能是解析出的会话 |
| T-113-08 | Info Disclosure | 跨会话读取他人需求的接口契约与现状结论 | mitigate | `read_entries` 只按解析出的 `convergence_session` 过滤（结构性隔离）；测试用「A 写 2 条 / B 读 0 条」可证伪 |
| T-113-09 | Info Disclosure | `content` 夹带凭证入库并被其他容器读到 | mitigate | 写入经 `BlueprintContextService._redact_json` 递归脱敏（113-01）；view/容器侧日志均不记 content |
| T-113-10 | Denial of Service | 老/新镜像与老/新服务端错配导致容器崩溃或整轮任务失败 | mitigate | 白名单 task 侧硬编码（老镜像只是不调）；新镜像打 404 由既有 return-not-raise 约定兜住；全路径 4xx/200 无 5xx（5xx 会被吞成「调用失败」而丢原因） |
| T-113-11 | Denial of Service | 无界 read 拉爆容器上下文 | mitigate | `limit` serializer 层 `max_value=200` + service 侧 `_MAX_READ_LIMIT` 双重夹紧 |
| T-113-12 | Tampering | 为新工具改公共 handler 工厂波及 7 个既有工具 | mitigate | 工厂零改动；`inspect.signature` 与 `timeout=60.0` 计数两条守护断言 + `git diff | rg "^-"` 为空 |
| T-113-SC | Tampering | 依赖供应链 | accept | 零新增外部依赖 |
</threat_model>

<verification>
- `cd server && uv run pytest tests/mcp_tools/ -q` 全绿；`cd task && uv run pytest -q` 全绿。
- `cd server && uv run python manage.py makemigrations --check --dry-run` 退出码 0（本 plan 零模型改动）。
- `cd server && uv run ruff check mcp_tools/` 通过。
- 冻结面自检：`git diff --name-only` 不含 `codegraph/services/repo_router_v2.py`、`process_runtime/{decompose_segments,research_adapter,architect_merge_adapter,merged_plan,clarify_adapter,render,resume,builtin_processes,blueprint_research_adapter}.py`、`repositories/services/charter_service.py`、`system/settings_service.py`、`delivery/services/event_taxonomy.py`。
- 🔒 工厂自检：`git diff task/core/knowledge_tools.py | rg "^-" | rg -v "^---"` 输出为空；`rg -c "timeout=60.0" task/core/knowledge_tools.py` == 1。
- 并行自检：`git diff --name-only` 与 113-03 零交集（不含 `blueprint_research_adapter.py` / `subagent/api/callbacks.py`）。
</verification>

<success_criteria>
- BUS-01：蓝图容器凭任务 token 实时读写会话级上下文总线（全程无分支），写入即对同会话并行容器可见；跨会话读写被 fail-closed 拒绝。
- ROADMAP SC2 前半：「容器凭任务 token 实时读写会话上下文总线」端到端可跑（真打 URL 的测试通过）。
- 向后兼容：老镜像不崩、未部署端点不崩、公共 handler 工厂与 7 个既有工具零影响。
</success_criteria>

<output>
完成后创建 `.planning/phases/113-2/113-02-SUMMARY.md`（provides：两个 MCP 工具的 URL/入参/返回形状、四个会话校验错误码与其 HTTP 状态映射、`_aresolve_blueprint_session` 的解析路径与真实字段名、`KNOWLEDGE_TOOL_SCHEMAS` 现为 9 项、report 响应里的 `satisfied_waiters` 字段与「重派接续点」注释位置——113-04 的 `await_blueprint_context` 与重派逻辑直接消费）。
</output>
