---
phase: 113-2
plan: 05
type: execute
wave: 4
depends_on: [113-03, 113-04]
files_modified:
  - server/services/process_runtime/blueprint_reconcile.py
  - server/services/process_runtime/blueprint_merge.py
  - server/tests/services/process_runtime/test_blueprint_reconcile.py
  - server/tests/services/process_runtime/test_blueprint_merge_stage.py
autonomous: true
requirements: [FLOW-06, SCHEMA-02, SCHEMA-03, SCHEMA-04, SCHEMA-05]
prohibitions:
  - "禁止修改 server/codegraph/services/repo_router_v2.py"
  - "禁止修改 process_runtime 六冻结文件：decompose_segments.py / research_adapter.py / **architect_merge_adapter.py**（本 plan 的主 analog，只读参考；`_content_to_text` / `_parse_merged_json` 等私有函数**复制不 import**）/ merged_plan.py / clarify_adapter.py / render.py"
  - "禁止修改 server/services/process_runtime/resume.py 与 server/repositories/services/charter_service.py"
  - "禁止修改 ConvergenceSessionEvent 既有类型；禁止修改 builtin_processes.py（含 _TECHNICAL_PLAN_STAGES / _ECHO_STAGES / _TECHNICAL_BLUEPRINT_STAGES）—— 本 plan 不碰它，stage 注册在 113-06"
  - "禁止修改 server/system/settings_service.py 既有 getter 与 system/models.py（SettingKeys 新常量在 113-06 追加；本 plan 的阈值只用模块常量兜底）"
  - "禁止修改 task/core/knowledge_tools.py（工厂三条硬约束全程有效）—— 本 plan 不碰 task/"
  - "禁止修改 blueprint_schema.py / blueprint_quality.py / blueprint_route.py / blueprint_confirm_gate.py / blueprint_resume.py / blueprint_lifecycle_service.py / entrypoint.py（受限面，本 plan 只读消费其公开函数）"
  - "禁止新增 CallSource 枚举值：分节 LLM 调用一律 CallSource.BLUEPRINT_MERGE（111 已注册）"
  - "禁止单次巨 prompt：六段中需推理的四段必须分节多次调用，便于按节归因重试"
  - "禁止用 LLM 做跨仓 API 对账（CONTEXT 明令纯函数）；禁止对账函数外抛异常（.get 防御 + 返回结构化 gaps）"
  - "禁止读 session.current_artifact_version 作融合基线（它会落后于 artifact.current_version，112-05 Deviation 3）"
  - "禁止投影只搬 fitness 而漏 rationale.citations（P-8：repo_associations 类条目分子恒 0，覆盖率被系统性拉低）"
  - "禁止把裸文件路径当 citation id 直接落蓝图（P-5：validate_blueprint 后置检查会判整份非法）"
  - "禁止在 blueprint_reconcile.py 顶层 import ORM / LLM（纯函数模块，必要时函数内懒 import）"
must_haves:
  truths:
    - "repo_associations 与 current_state_analysis 两段是**确定性投影**：mock 掉全部 LLM 后这两段仍完整产出，且与上游产物（确认门锁定 associations / 各仓 PartialPlan.content.current_state）**逐字段一致**"
    - "投影同时填 rationale.citations（不止 fitness.citations）：含 N 个 repo_associations 的样本经投影后 citation_coverage(blueprint) > 0"
    - "顶层 citations 引用池先建后填：各仓裸文件路径归一成 citation id 落顶层池，各段只填池内 id；装配产物过 validate_blueprint 为 (True, None)"
    - "跨仓 API 对账是纯函数：consumed 找不到 provider → availability=needs_support 且要求 support_repository_id 出现在 repo_associations，缺失即进 missing_support_repos（抛澄清）；provider/consumer 字段不一致 → conflicts 非空，绝不静默拍板"
    - "must_haves 由 requirement_spec 与 implementation_overview.items 确定性派生（无 LLM 参与），三键 truths/artifacts/key_links 齐全且过 schema"
    - "装配落 ArtifactVersion 幂等：同 content 重跑不翻版本（content_hash 相同复用 current）；produced_by_ref 带轮次可归因"
    - "implementation_overview.items 逐项带 change_type 与 depends_on/wave（SCHEMA-03）；interaction_flows 覆盖「页面→接口→参数→数据→流向→行为路径」（SCHEMA-04）；api_contracts 含请求响应示例与数据来源说明（SCHEMA-05）"
  artifacts:
    - path: "server/services/process_runtime/blueprint_reconcile.py"
      provides: "reconcile_cross_repo_apis 纯函数（gaps / conflicts / missing_support_repos 恒定三键）"
      contains: "needs_support"
    - path: "server/services/process_runtime/blueprint_merge.py"
      provides: "BlueprintMergeAdapter：确定性投影 + 分节 LLM 起草 + must_haves 派生 + 引用池 + 装配落版本"
      contains: "def derive_must_haves"
    - path: "server/tests/services/process_runtime/test_blueprint_merge_stage.py"
      provides: "「确定性投影不经 LLM」的逐字段一致断言 + citation_coverage > 0"
      contains: "citation_coverage"
    - path: "server/tests/services/process_runtime/test_blueprint_reconcile.py"
      provides: "无 DB 纯函数：needs_support / 缺 support_repository_id / 字段不一致三条断言"
      contains: "conflicts"
  key_links:
    - from: "server/services/process_runtime/blueprint_merge.py"
      to: "server/services/process_runtime/blueprint_schema.py"
      via: "装配后过 validate_blueprint（只读调用，不改该模块）"
      pattern: "validate_blueprint"
    - from: "server/services/process_runtime/blueprint_merge.py"
      to: "server/delivery/services/artifact_service.py"
      via: "add_version(artifact, content, produced_by_ref='blueprint_merge#attempt=N')（自带 content_hash 幂等）"
      pattern: "add_version"
    - from: "server/services/process_runtime/blueprint_merge.py"
      to: "server/services/process_runtime/blueprint_reconcile.py"
      via: "装配前跑纯函数对账，conflicts/missing_support_repos 非空即开澄清线程"
      pattern: "reconcile_cross_repo_apis"
---

<objective>
落地阶段 3 的融合装配：新建 `blueprint_reconcile.py`（跨仓 API 对账纯函数）与 `blueprint_merge.py`（**确定性投影优先、LLM 只写需要推理的四段**、`must_haves` 确定性派生、引用池归一、装配落 `ArtifactVersion`）。**绝不修改冻结的 `architect_merge_adapter.py`**——它只是范式来源。

Purpose: FLOW-06（六段融合 + 跨仓 API 对账）+ SCHEMA-02（关键结论带引用）+ SCHEMA-03/04/05（实现概述映射与波次 / 交互流程完整叙事 / API 段数据来源）。
Output: 2 个新模块 + 2 个测试文件。质量门与回退（覆盖率门、归因、超界出口、stage 注册）归 113-06。
</objective>

<execution_context>
工作目录：worktree 根。wave 4，依赖 113-03（RepoPlan 产物与 `acollect_repo_plans`）与 113-04（`build_api_waves` 的 `unresolved_consumed` 作 needs_support 前置信号、`waves` 供 `implementation_overview.items[].wave`）。
本 plan **只交付「装配出一份合法蓝图」**：`validate_blueprint` 硬门在本 plan 内（不过就不落版本），但**覆盖率门 / `_coverage_gaps` 归因 / 有界回退 / 超界转 STAGE_DONE / SettingKeys 阈值 / stage 注册**全部归 113-06。本 plan 的重试上界与阈值先用模块常量（`_DEFAULT_CITATION_COVERAGE_MIN` / `MAX_MERGE_ROUNDS`）兜底，113-06 再把它们接到 `SystemSetting`。
</execution_context>

<context>
@.planning/phases/113-2/113-CONTEXT.md（锁定：新建 blueprint_merge.py 绝不改 architect_merge_adapter；分节多次调用而非单次巨 prompt；六段来源分工逐条；must_haves 由 requirement_spec 与实现项确定性派生；跨仓 API 对账用**纯函数**——consumed 无 provider 标 needs_support 且要求 support_repository_id 出现在 repo_associations，字段不一致抛澄清绝不静默拍板；装配后过 validate_blueprint + 覆盖率门）
@.planning/phases/113-2/113-RESEARCH.md 主题 3.1（validate_blueprint 签名与两层校验、后置检查 (a) 引用完整性）、3.2（citation_coverage 入参形状与三类分母口径、**分母为 0 返 1.0**、⚠️ rationale.citations 隐患）、3.3（**must_haves 无派生代码需新写**，schema 形状与 execution_plan 派生先例）、3.4（ArtifactService.add_version 签名与 content_hash 幂等）、3.5（architect_merge_adapter 可借鉴范式表：Protocol 合成器 / 可空串 section / 类属性重试上界 / merge 七步骨架 / graceful 降级 / 返回四键）、Pitfalls P-5（引用池白名单二选一，**本 plan 显式选「先建引用池」**）、P-6（幂等与版本基线两个残留口）、P-8（rationale.citations）
@.planning/phases/113-2/113-PATTERNS.md 第 2 类（融合 adapter 逐条结构要点 + ⭐可空串 section 逐字范式 + 沿用清单：分节多调用、确定性投影可断言、轮次落 stage_state、基线读最新 version_no、超界出口、current_artifact_version 回填）、第 3 类（纯函数模块范式：docstring 分节声明依赖上界、__all__ 只列公开、走查器 generator 且口径写进 docstring、逐层 isinstance 防御、边界返回值写进 docstring）
@.planning/phases/113-2/113-03-SUMMARY.md（`acollect_repo_plans` 返回形状、RepoPlan 十一字段与 `apis_provided`/`apis_consumed`/`current_state`/`impl_items` 的确切字段名、`REPO_PLAN_CHANGE_TYPES` 实际取值）
@.planning/phases/113-2/113-04-SUMMARY.md（`build_api_waves` 四键返回：waves 供 items[].wave、unresolved_consumed 作 needs_support 前置信号、cycles）
@server/services/process_runtime/architect_merge_adapter.py（L50-58 可空串 section 逐字、L87-117 Protocol + 合成器五步、L119-164 system/human 分离与段落拼接、L170-193 类属性上界与 keyword-only 依赖、L195-237 merge 七步骨架、L239-286 pass 分支落库、L288-334 best-effort hook、L413-436 _record/_emit、L439-466 健壮 JSON 解析——**冻结只读 analog，禁改禁 import**）
@server/services/process_runtime/blueprint_schema.py（L713-732 must_haves schema 契约、L793-834 validate_blueprint、六段字段定义——**只读**）
@server/services/process_runtime/blueprint_quality.py（L20-79 __all__ / _cited / _iter_key_conclusion_citations / citation_coverage——**只读消费 + 第 3 类范式来源**）
@server/services/process_runtime/blueprint_execution.py（L139-215 derive_execution_plan：确定性派生的先例口径，must_haves 派生照此形态）
@server/delivery/services/artifact_service.py（L50-162 create / add_version 与 content_hash 幂等）
@server/services/process_runtime/blueprint_confirm_gate.py（L244-301 build_locked_associations：投影源的确切字段——注意它落的是 fitness.citations）
@.planning/technical-blueprint/DESIGN.md §3.7-§3.12（现状分析 / 实现概述 / API / 影响范围 / 交互流程字段形状 + must_haves 派生语义）
</context>

<tasks>

<task type="auto">
  <name>Task 1: blueprint_reconcile.py —— 跨仓 API 对账纯函数（needs_support / conflicts / missing_support_repos）</name>
  <files>server/services/process_runtime/blueprint_reconcile.py, server/tests/services/process_runtime/test_blueprint_reconcile.py</files>
  <read_first>server/services/process_runtime/blueprint_quality.py 全文（模块 docstring 分节声明、__all__ 只列公开、`_cited` 与 `_iter_key_conclusion_citations` 的 generator + 逐层 isinstance 防御、边界返回值写进 docstring——**逐条镜像的范式来源**）；.planning/phases/113-2/113-03-SUMMARY.md（`apis_provided[]` / `apis_consumed[]` 的确切字段名与 `availability` 枚举）；.planning/phases/113-2/113-04-SUMMARY.md（`match_api` 的匹配规则——本模块应复用同一口径，若可 import 则 import `blueprint_repo_waves.match_api`，两处口径漂移会导致「预排说有 provider、对账说没有」的自相矛盾）</read_first>
  <action>
新建 `server/services/process_runtime/blueprint_reconcile.py`。模块 docstring 三段（照 `blueprint_quality.py:1-14`）：①「本模块只有**纯函数节**：无 IO / 无 ORM / 无 LLM，stdlib only；顶层零 ORM import」；②「输入是半可信的 LLM 装配产物 / golden fixture，逐字段 `.get` 防御，**绝不抛**」；③「跨仓 API 对账**不用 LLM 自查**（CONTEXT 锁定）——判定必须可复现、可单测、可解释」。

`__all__ = ["reconcile_cross_repo_apis"]`（`coverage_gaps` 由 113-06 追加进 `__all__`）。

复用匹配口径：`from services.process_runtime.blueprint_repo_waves import match_api`（113-04 交付；**同一口径，不另写一套**。若该函数签名不便直用，则在本模块写 `_match_api` 并在其 docstring 显式写明「与 `blueprint_repo_waves.match_api` 同口径，改一处必须改两处」——二选一，**优先 import**）。

```
def reconcile_cross_repo_apis(blueprint: Any) -> dict
```
docstring 写清入参形状（**完整蓝图 content dict**，读 `repo_associations` 与 `api_contracts`；`api_contracts` 里每项带 `repository_id` / `direction(provided|consumed)` 或等价字段——**实现前先 `rg -n "api_contracts" -A 30 server/services/process_runtime/blueprint_schema.py` 核对真实字段名，按 schema 写，不臆造**）与**恒定三键返回**：
```
{
  "gaps": [{"repository_id", "api", "reason": "no_provider"}],
  "conflicts": [{"api", "provider_repository_id", "consumer_repository_id",
                 "field": "method|path|request_schema|response_schema|direction",
                 "provider_value", "consumer_value"}],
  "missing_support_repos": [{"repository_id", "api", "support_repository_id"}],
}
```
（形状恒定，下游无需判空分支——对齐 `blueprint_route` 的约定。）

判定逻辑：
1. 建 provider 索引：所有 `provided` 项按 `(repository_id, name, method, path)` 收集。
2. 逐条 `consumed`：
   - 找到 provider → 逐字段比对（`method` / `path` / `request_schema` / `response_schema`）。任一不一致 → 进 `conflicts`（**带 provider/consumer 双值，便于澄清问题直接引用**），**绝不静默拍板取其一**。
   - 找不到 provider → 进 `gaps`（`reason="no_provider"`）；此时调用方应把该条 `availability` 置 `needs_support`。
   - 若该条已标 `availability == "needs_support"`：校验其 `support_repository_id` **非空且出现在 `repo_associations[].repository_id` 集合中**；不满足 → 进 `missing_support_repos`（语义 = 缺协作仓，调用方抛澄清）。
3. 逐层 `isinstance` + `or []` 防御；非 dict 直接 `continue`；整个函数**不抛**。

新建 `server/tests/services/process_runtime/test_blueprint_reconcile.py`（**零 DB 纯函数测试**，模块 docstring 编号列守护点）：
- ⭐ **consumed 无 provider**：A 消费 `GET /x`，无人提供 → `gaps` 含该项且 `reason == "no_provider"`。
- ⭐ **needs_support 缺 support_repository_id**：标了 `needs_support` 但 `support_repository_id` 为空 → `missing_support_repos` 非空。
- ⭐ **support_repository_id 不在 repo_associations**：填了但该仓不在关联清单 → 同样进 `missing_support_repos`（**缺协作仓的两种形态都被捕获**）。
- ⭐ **字段不一致**：provider `POST /x`、consumer `GET /x` → `conflicts` 非空且 `field == "method"`，`provider_value`/`consumer_value` 各就位。
- `request_schema` / `response_schema` 不一致各一例。
- **完全闭环**：provider 与 consumer 逐字段一致 → 三键全空（`{"gaps": [], "conflicts": [], "missing_support_repos": []}`）。
- **绝不抛**：传 `None` / `{}` / `{"api_contracts": "not-a-list"}` / 项内字段类型错乱 → 均返回三键空 dict。
- **口径一致性**：同一组 provided/consumed 喂给 `build_api_waves` 与 `reconcile_cross_repo_apis`，「有 provider」的判定结论一致（**防两套匹配规则漂移**）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/process_runtime/test_blueprint_reconcile.py -x -q</automated>
  </verify>
  <acceptance_criteria>
- `cd server && uv run pytest tests/services/process_runtime/test_blueprint_reconcile.py -x -q` 全绿。
- `rg -n "def reconcile_cross_repo_apis" server/services/process_runtime/blueprint_reconcile.py` 命中。
- `rg -n "^from delivery|^import delivery|objects\.|sync_to_async|ainvoke|build_chat_model" server/services/process_runtime/blueprint_reconcile.py` 零命中（顶层零 ORM / 零 LLM）。
- `rg -n "raise " server/services/process_runtime/blueprint_reconcile.py` 零命中（绝不抛）。
- `rg -n "needs_support|missing_support_repos|conflicts|gaps" server/services/process_runtime/blueprint_reconcile.py` 四个键全命中。
- `rg -n "match_api" server/services/process_runtime/blueprint_reconcile.py` 命中（与波次预排同口径）。
- `git diff --stat server/services/process_runtime/architect_merge_adapter.py server/services/process_runtime/blueprint_quality.py` 输出为空。
  </acceptance_criteria>
  <done>跨仓 API 对账为可单测的纯函数：无 provider / 缺协作仓两形态 / 字段不一致三类结论恒定三键返回，与波次预排同一匹配口径，绝不抛也绝不静默拍板。</done>
</task>

<task type="auto">
  <name>Task 2: blueprint_merge.py —— 确定性投影 + 引用池 + 分节 LLM 起草 + must_haves 派生 + 装配落版本</name>
  <files>server/services/process_runtime/blueprint_merge.py</files>
  <read_first>server/services/process_runtime/architect_merge_adapter.py L50-58（⭐可空串 section 逐字）、L87-117（Protocol + LLM 合成器五步）、L119-164（system/human 分离与段落插槽拼接）、L170-193（类属性上界 + keyword-only 依赖 + lazy import）、L195-237（merge 七步骨架与 graceful 降级返回四键）、L239-286（pass 分支落库与 ArtifactContentInvalid 转 fail）、L413-466（_record/_emit 与健壮 JSON 解析）——**冻结只读，私有函数复制不 import**；server/services/process_runtime/blueprint_schema.py（六段完整字段定义 + L713-732 must_haves schema + L793-834 validate_blueprint）；server/services/process_runtime/blueprint_execution.py L139-215（确定性派生先例）；server/services/process_runtime/blueprint_quality.py L39-79（citation_coverage 的三类分母口径——投影必须喂饱它）；server/delivery/services/artifact_service.py L117-162（add_version 与幂等）；server/services/process_runtime/blueprint_confirm_gate.py L244-301（投影源字段）</read_first>
  <action>
新建 `server/services/process_runtime/blueprint_merge.py`。模块 docstring 六段：①「阶段 3 融合装配（DESIGN §5.2 merge 行）——新文件，**绝不修改冻结的 `architect_merge_adapter.py`**，其私有 helper 一律**复制**」；②「**确定性投影优先，LLM 只写需要推理的四段**——投影结果与上游产物逐字段一致，可断言不经 LLM」；③「分节多次调用而非单次巨 prompt（降幻觉、便于按节归因重试）」；④「跨仓 API 对账走 `blueprint_reconcile` 纯函数，绝不 LLM 自查」；⑤「INV-6：落库只经 `ArtifactService.add_version`；线程只经 `BlueprintLifecycleService`」；⑥「best-effort 不反噬主链：事件/埋点/后置 hook 一律 `except Exception` 吞掉 + warning」。

模块常量：`MAX_MERGE_ROUNDS = 2`（**类属性常量**，注释「合计上界 2 轮，113-06 会把它接到 `SettingKeys.BLUEPRINT_MERGE_CONFIG`；此处是兜底默认」）、`_DEFAULT_CITATION_COVERAGE_MIN = 0.8`（同上，本 plan 只定义不消费——门在 113-06）、`STAGE_STATE_KEY = "merge"`。

**（a）合成器抽象**（照 analog L87-117）：`@runtime_checkable class BlueprintSectionSynthesizer(Protocol)` 只声明 `async def draft(self, *, section: str, prompt_parts: dict) -> dict`；`LLMBlueprintSectionSynthesizer` 五步：`ProviderConfigService.aresolve()` → 取默认模型（缺则 `raise RuntimeError("no_default_model")`）→ `build_chat_model(resolved, model_name, streaming=False)` → `with use_call_source(CallSource.BLUEPRINT_MERGE): await model.ainvoke([system, human])` → `_content_to_text`（**复制** analog L439-452）→ `_parse_json`（**复制** L455-466：取首 `{` 到末 `}`、不 eval、非 dict 返 None）。parse 失败 `raise ValueError("blueprint_section_parse_failed")`，由 adapter graceful 接住。

**（b）引用池（P-5，本 plan 显式选「先建引用池」而非白名单丢弃）**：
`def build_citation_pool(repo_plans: dict, associations: list) -> tuple[list[dict], dict[str, str]]`（**纯函数**）——遍历各仓 `current_state[].findings[].citations`、`fitness.citations`、`impl_items[].citations`、`apis_*[].citations` 里的**裸文件路径/符号名**，去重后归一为 citation id（`f"cit_{sha1(raw)[:12]}"`，稳定可复现），产出 ① 顶层 `citations` 池条目列表（按 `blueprint_schema` 的 citation 对象形状写——**实现前 `rg -n "citations" -A 25 server/services/process_runtime/blueprint_schema.py` 核对必填字段**）② `raw → id` 映射表。各段填充时一律用映射表换成 id；映射不到的裸串**丢弃并计数**（记 warning，不让整份蓝图非法）。

**（c）确定性投影**（**不经 LLM，可断言**）：
- `def project_repo_associations(locked_associations: list, cite_map: dict) -> list`：从确认门锁定产物逐字段搬（`repository_id / repository_name / role / responsibility / routing_evidence / decided_by / confirmed_at_gate / fitness`），**并同时填 `rationale`**：`{"text": <责任/理由摘要>, "citations": [<cite_map 换算后的 id>]}`。⚠️ **P-8 的唯一防线**——函数上方写明「`citation_coverage` 读的是 `rationale.citations` 不是 `fitness.citations`；只搬 fitness 会让该类条目分子恒 0」。
- `def project_current_state(repo_plans: dict, cite_map: dict) -> list`：各仓 `PartialPlan.content.repo_plan.current_state` 逐字段搬进 `current_state_analysis[]`（含 `findings[].citations` 换 id）。
两个函数都是**模块级纯函数**（零 ORM / 零 LLM），便于测试直接调。

**（d）分节 LLM 起草**（四段，各一个 `_adraft_<section>`，各自 `use_call_source(CallSource.BLUEPRINT_MERGE)`）：
- `implementation_overview`：输入 = 各仓 `impl_items[]` + `build_api_waves` 的 `waves`。**`change_type` / `files_touched` / `depends_on` 直接从 RepoPlan 搬（不让 LLM 改写）**，LLM 只负责 `modules` 分组与跨仓叙事；`items[].wave` 由预排结果确定性填入。⇒ SCHEMA-03。
- `api_contracts`：输入 = 各仓 `apis_provided` / `apis_consumed` + 对账结果。要求每条含**接口描述 / 请求响应示例 / 数据来源说明（数据已有，或需哪个仓支持产出）**。⇒ SCHEMA-05。
- `interaction_flows`：要求每条流程覆盖**「在哪个页面 → 经哪个接口 → 传什么参数 → 拿到什么数据 → 数据流向哪里 → 有哪几条行为路径」六要素**，`steps[]` 带 `seq`。⇒ SCHEMA-04。
- `impact_analysis`：输入 = 各仓 `local_impact` + `risks`，产出 `affected_features[]`（**每条带 citations**，喂 `citation_coverage` 的第三类分母）。
每段 prompt 用 ⭐**可空串 section** 插槽（照 analog L50-58/L134-148）：无该类证据时该段为空串，prompt 与基础形态逐字一致（零扰动）。system 与 human 分离。

**（e）`must_haves` 确定性派生**（**111 只有 jsonschema 无派生代码，本 plan 新写**）：
```
def derive_must_haves(*, requirement_spec: dict, implementation_overview: dict) -> dict
```
**纯函数、零 LLM**，照 `blueprint_execution.derive_execution_plan` 的形态：
- `truths` ← `requirement_spec.feature_points[]` 的可观察行为表述（逐条转成断言句，模板化拼接，不调 LLM）。
- `artifacts` ← `implementation_overview.items[].files_touched` 去重聚合，每项 `{path, provides: <该 item 的 title>}`。
- `key_links` ← `items[].depends_on`（仓内）与 `api_contracts` 的 provider→consumer 边，每项 `{from, to, via}`。
产物必须过 `blueprint_schema` 的 `must_haves` 子 schema（三键必填）。函数 docstring 写明「三键的派生口径与边界（无 items 时 artifacts 为空列表而非缺键——缺键会让整份蓝图 schema 失败）」。

**（f）`class BlueprintMergeAdapter`**（`__init__(self, *, synthesizer=None, artifact_service=None, lifecycle_service=None, repo_plan_adapter=None, node_execution_id="")`，全 keyword-only + `x or DefaultX()`）：
`async def merge(self, session) -> dict` 七步（骨架照 analog L195-237）：
1. **读基线**：`ArtifactVersion.objects.filter(artifact_id=…).order_by("-version_no").afirst()`（⚠️ **绝不读 `session.current_artifact_version`**，P-6 残留口 2）→ 取 `repo_associations` 与 `requirement_spec`。
2. `repo_plans = await self.repo_plan_adapter.acollect_repo_plans(session)`；`waves = build_api_waves(repo_plans)`。
3. `attempt = (session.stage_state.get("merge") or {}).get("count", 0)`；emit `blueprint_merge_started`。
4. 建引用池 → 两段确定性投影 → 四段分节 LLM（每段包 `except Exception` → 该段降级为空结构并记 warning，**不上抛**；四段全失败才返回 `{"validation_status": "failed", ...}`）→ `derive_must_haves`。
5. `reconcile = reconcile_cross_repo_apis(assembled)`：`gaps` 逐条把对应 `api_contracts` 项的 `availability` 置 `needs_support`；`conflicts` 或 `missing_support_repos` 非空 → `await self.lifecycle_service.open_thread(kind="ai_clarification", blocking=True, question=<列出冲突的 api 与双方取值 / 缺的协作仓>)` 并返回 `{"validation_status": "needs_clarification", "report": {...}, "back_target": "merge", "attempt": attempt}`（**绝不静默拍板**）。
6. `ok, err = validate_blueprint(assembled)`；不过 → `{"validation_status": "failed", "report": {"schema_error": err}, "back_target": "merge", "attempt": attempt}`（**graceful，不上抛**）。
7. 过 → `version = await self.artifact_service.add_version(artifact, assembled, produced_by_session_id=str(session.id), produced_by_ref=f"blueprint_merge#attempt={attempt}")`（自带 `content_hash` 幂等；`produced_by_ref` 带轮次可归因，P-6 残留口 1）→ 返回 `{"validation_status": "passed", "artifact_version_id": str(version.id), "attempt": attempt, "reconcile": {...计数...}, "stage_state": {**state, "merge": {...}}}`。
   ⭐ **返回值必须携带 `artifact_version_id`** —— 113-06 的 `_h_bp_merge` 要用它回填 `StageOutcome.current_artifact_version`（本蓝图链首个用到该字段的 handler）。
`_emit` / `_record` 统一包 try/except（照 analog L413-436）。轮次递增点**单点串行**：只在 `merge()` 内、以「重读 session 新实例 → `{**state, ...}` 浅合并整体回写」的形态产出 `stage_state`，**回调路径永不触碰计数**。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/process_runtime/test_blueprint_merge_stage.py -x -q</automated>
  </verify>
  <acceptance_criteria>
- `rg -n "def project_repo_associations|def project_current_state|def derive_must_haves|def build_citation_pool" server/services/process_runtime/blueprint_merge.py` 四个纯函数全命中。
- `rg -n "rationale" server/services/process_runtime/blueprint_merge.py` 命中（P-8 防线存在）。
- `rg -n "order_by\(\"-version_no\"\)" server/services/process_runtime/blueprint_merge.py` 命中；`rg -n "session.current_artifact_version" server/services/process_runtime/blueprint_merge.py` 零命中（P-6）。
- `rg -c "use_call_source\(CallSource.BLUEPRINT_MERGE\)" server/services/process_runtime/blueprint_merge.py` ≥ 4（四段分节各一次，非单次巨 prompt）。
- `rg -n "from services.process_runtime.architect_merge_adapter import|from .architect_merge_adapter import" server/services/process_runtime/blueprint_merge.py` 零命中（不 import 冻结模块）。
- `rg -n "artifact_version_id" server/services/process_runtime/blueprint_merge.py` 命中（113-06 回填 current_artifact_version 的数据来源）。
- `rg -n "produced_by_ref=f\"blueprint_merge#attempt=" server/services/process_runtime/blueprint_merge.py` 命中（轮次可归因）。
- `rg -n "reconcile_cross_repo_apis" server/services/process_runtime/blueprint_merge.py` 命中；`rg -n "对账|reconcile" server/services/process_runtime/blueprint_merge.py | rg -i "ainvoke|llm"` 零命中（对账不经 LLM）。
- `git diff --stat server/services/process_runtime/architect_merge_adapter.py server/services/process_runtime/blueprint_schema.py server/services/process_runtime/blueprint_quality.py server/services/process_runtime/builtin_processes.py server/services/process_runtime/entrypoint.py` 输出全为空。
- `git diff --stat server/agents/call_source.py server/system/models.py` 输出为空（不新增枚举值、SettingKeys 归 113-06）。
  </acceptance_criteria>
  <done>六段装配就位：两段确定性投影（含 rationale.citations）、引用池先建后填、四段分节 LLM、must_haves 确定性派生、纯函数对账、装配过 validate_blueprint 后落幂等版本并回带 artifact_version_id。</done>
</task>

<task type="auto">
  <name>Task 3: 融合装配测试（确定性投影逐字段一致 + citation_coverage > 0 + 幂等 + 对账闭环）</name>
  <files>server/tests/services/process_runtime/test_blueprint_merge_stage.py</files>
  <read_first>server/tests/services/process_runtime/test_blueprint_research_stage.py L1-122（模块 docstring 编号清单、双 pytestmark、工厂与替身区段、mock 依赖注入而非 patch 全局）；113-PATTERNS 第 11 类 `test_blueprint_merge_stage.py` 行的四条守护点；server/tests/fixtures/blueprint_golden/（111-04 的 golden case，可作装配样本来源——先 `ls server/tests/fixtures/blueprint_golden/` 确认实际路径）</read_first>
  <action>
新建 `server/tests/services/process_runtime/test_blueprint_merge_stage.py`（`pytestmark = [django_db(transaction=True), asyncio]`），模块 docstring 编号列守护点。

工厂与替身：`_FakeSynthesizer`（按 section 返回预置 dict，可配置某段抛异常）；`_make_session_with_locked_associations(...)`（建 Artifact + 两个 ArtifactVersion，**其中 session 钉住的是旧版本**，用于验证「不读 session.current_artifact_version」）；`_make_repo_plan(rid, *, current_state=…, impl_items=…, apis_provided=…, apis_consumed=…)`。

必测用例：
1. ⭐ **确定性投影不经 LLM**（核心）：`synthesizer` 替身对**四段全部抛异常**（或返回空）→ 装配结果里 `repo_associations` 与 `current_state_analysis` **仍完整产出**，且与上游逐字段一致：逐 repo 断言 `repository_id / role / responsibility / decided_by / confirmed_at_gate` 与锁定产物**逐字段相等**；`current_state_analysis[i].findings[j].title/detail` 与该仓 `repo_plan.current_state` **逐字段相等**（不是「大致包含」，是相等）。
2. ⭐ **P-8 覆盖率非零**：含 3 个 repo_associations 且各带 citations 的样本 → 投影后 `citation_coverage(blueprint) > 0`（并断言 `repo_associations[0]["rationale"]["citations"]` 非空）。
3. ⭐ **引用完整性（P-5）**：装配产物过 `validate_blueprint` 为 `(True, None)`；且断言各段 `citations` 里的每个 id 都能在顶层 `citations` 池里找到（**裸文件路径零残留**：`rg`-free 的运行时断言——遍历所有 citations 值，断言全部匹配 `cit_` 前缀）。
4. ⭐ **基线读最新版本**：session 钉住 v1（`repo_associations` 为空），artifact 最新是 v2（含 2 仓）→ 融合产物含这 2 仓（**没读到 session 那一版**）。
5. **must_haves 派生**：`derive_must_haves` 产出三键齐全；`artifacts[].path` 来自 `items[].files_touched` 去重；`key_links` 含一条 provider→consumer 边；空 items 时 `artifacts == []` 而非缺键（**过 schema**）。
6. **分节而非单 prompt**：`_FakeSynthesizer.draft` 被调用 **4 次**且 `section` 参数分别为四段名（可证伪「单次巨 prompt」）。
7. **单段失败降级**：只让 `interaction_flows` 抛异常 → 其余三段仍产出，整体仍过 `validate_blueprint`（该段为合法空结构），返回 `validation_status == "passed"`（**graceful，不上抛**）。
8. **对账抛澄清**：consumer 与 provider 的 `method` 不一致 → 返回 `validation_status == "needs_clarification"`、`back_target == "merge"`，DB 存在 `BlueprintThread(kind="ai_clarification", blocking=True)`，且**未落新 ArtifactVersion**。
9. ⭐ **consumed 无 provider 必标 needs_support**：A 消费谁都不提供的 API → 装配产物里该 `api_contracts` 项 `availability == "needs_support"`；若其 `support_repository_id` 缺失 → 走用例 8 的澄清路径。
10. **幂等落版本**：同一输入连续 `merge()` 两次（synthesizer 返回逐字相同）→ `ArtifactVersion` 总数只 +1（`content_hash` 相同复用 current）；`produced_by_ref` 含 `attempt=`。
11. **schema 不过不落版本**：让投影产出缺必填段（构造非法）→ `validation_status == "failed"` 且 `ArtifactVersion` 数量不变。
12. **返回值带 artifact_version_id**：passed 路径返回 dict 含非空 `artifact_version_id`（113-06 回填 `StageOutcome.current_artifact_version` 的依据）。
13. **SCHEMA-03/04/05 形状**：`implementation_overview.items[]` 逐项有 `change_type` ∈ 枚举 且有 `wave`；`interaction_flows[].steps[]` 有 `seq` 且流程对象含六要素对应字段；`api_contracts[]` 含请求/响应示例与数据来源字段（**按 blueprint_schema 的真实字段名断言**）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/process_runtime/test_blueprint_merge_stage.py -x -q</automated>
  </verify>
  <acceptance_criteria>
- `cd server && uv run pytest tests/services/process_runtime/ -q` 全绿。
- `rg -n "citation_coverage" server/tests/services/process_runtime/test_blueprint_merge_stage.py` 命中（P-8 断言存在）。
- `rg -n "needs_support" server/tests/services/process_runtime/test_blueprint_merge_stage.py` 命中（consumed 无 provider 断言存在）。
- `rg -n "draft" server/tests/services/process_runtime/test_blueprint_merge_stage.py | rg "== 4|call_count"` 命中（分节可证伪）。
- `rg -n "current_artifact_version" server/tests/services/process_runtime/test_blueprint_merge_stage.py` 命中（基线读最新版本的断言存在）。
- `rg -n "cit_" server/tests/services/process_runtime/test_blueprint_merge_stage.py` 命中（引用池 id 化断言存在）。
- `cd server && uv run pytest tests/ -q -x --ignore=tests/e2e 2>&1 | tail -5` 无新增失败（全量回归；若既有 e2e 目录名不同则跑全量）。
  </acceptance_criteria>
  <done>「确定性投影逐字段一致且不经 LLM」「覆盖率 > 0」「引用池 id 化后过 schema」「consumed 无 provider 必标 needs_support」四条核心断言全部可证伪；分节调用次数、幂等落版本、单段失败降级均有守护。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 各仓 RepoPlan（容器产物）→ 融合蓝图 | 半可信 JSON 汇入项目级权威产物 |
| 分节 LLM 输出 → 蓝图四段 | LLM 幻觉可能编造接口、文件路径、引用 |
| 融合产物 → `ArtifactVersion` | 落库即成为 114 审查与人审的输入基线 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113-27 | Tampering | LLM 编造引用导致整份蓝图非法或引用不可溯源 | mitigate | 引用池**先建后填**：只有来自 RepoPlan 的真实 citations 才进池，各段只能填池内 id，池外一律丢弃计数；产物过 `validate_blueprint` 后置检查 (a) |
| T-113-28 | Tampering | LLM 改写 change_type / files_touched 造成实现项失真 | mitigate | 这些字段**从 RepoPlan 直接搬**，LLM 只做分组与叙事；测试断言逐项 `change_type` ∈ 枚举 |
| T-113-29 | Tampering | 跨仓 API 矛盾被 LLM 静默拍板 | mitigate | 对账是**纯函数**，conflicts 非空即开 blocking 澄清线程并 `needs_clarification` 返回，**不落版本** |
| T-113-30 | Tampering | 投影漏 `rationale.citations` 导致覆盖率被系统性拉低（P-8） | mitigate | `project_repo_associations` 同时填 `rationale.citations`；测试断言 `citation_coverage > 0` |
| T-113-31 | Tampering | 读落后的 session 版本覆盖确认门锁定的 repo_associations（P-6） | mitigate | 基线一律 `order_by("-version_no").afirst()`；测试构造「session 钉旧版」场景断言取到新版 |
| T-113-32 | Denial of Service | 融合重试造成版本膨胀 | mitigate | `add_version` 的 `content_hash` 幂等（同内容不翻版本）；轮次上界 `MAX_MERGE_ROUNDS = 2`（门在 113-06）；`produced_by_ref` 带轮次可归因 |
| T-113-33 | Info Disclosure | 澄清问题/日志夹带 RepoPlan 正文或凭证 | mitigate | 澄清问题只列 api 名与双方取值（有界截断）；异常经 `redact_secrets_in_text` 截断 500；事件 payload 只含计数 |
| T-113-34 | Denial of Service | 单段 LLM 失败拖垮整次融合 | mitigate | 每段独立 `except Exception` 降级为空结构 + warning；四段全失败才返回 failed；`ArtifactContentInvalid` 被接住转 failed 不上抛 |
| T-113-SC | Tampering | 依赖供应链 | accept | 零新增外部依赖 |
</threat_model>

<verification>
- `cd server && uv run pytest tests/services/process_runtime/ tests/delivery/ tests/subagent/ tests/mcp_tools/ -q` 全绿。
- `cd server && uv run python manage.py makemigrations --check --dry-run` 退出码 0（零模型改动）。
- `cd server && uv run ruff check services/process_runtime/ && uv run ruff format --check services/process_runtime/blueprint_merge.py services/process_runtime/blueprint_reconcile.py` 通过。
- 冻结面自检：`git diff --name-only` 不含 `codegraph/services/repo_router_v2.py`、`process_runtime/{decompose_segments,research_adapter,architect_merge_adapter,merged_plan,clarify_adapter,render,resume,builtin_processes,entrypoint,blueprint_schema,blueprint_quality,blueprint_route,blueprint_spec_gate,blueprint_confirm_gate,blueprint_resume,blueprint_lifecycle_service}.py`、`repositories/services/charter_service.py`、`system/{models,settings_service}.py`、`delivery/services/event_taxonomy.py`、`agents/call_source.py`、`subagent/api/callbacks.py`、`task/core/knowledge_tools.py`。
- 本 plan 只新增两个文件 + 两个测试：`git diff --name-only | rg -v "^\.planning/"` 应恰好是这 4 个路径。
</verification>

<success_criteria>
- FLOW-06：主 agent 融合装配六段蓝图并做跨仓 API 对账——消费的接口必须找到提供方或显式标注 `needs_support`，跨仓矛盾抛澄清而非静默拍板。
- SCHEMA-02：现状 finding / 选仓理由 / 影响判断三类关键结论均携带引用证据（引用池 id 化后可溯源），`citation_coverage` 可量测且 > 0。
- SCHEMA-03：实现概述逐项标 `change_type` 并给出功能↔模块↔仓库映射与实现依赖波次。
- SCHEMA-04：交互流程段覆盖「页面→接口→参数→数据→流向→行为路径」完整叙事。
- SCHEMA-05：API 段含接口描述 / 请求响应示例 / 数据来源说明（数据已有，或需哪个仓支持产出）。
- ROADMAP SC3 主体 + SC4 的对账半边。
</success_criteria>

<output>
完成后创建 `.planning/phases/113-2/113-05-SUMMARY.md`（provides：`reconcile_cross_repo_apis` 三键返回契约、`BlueprintMergeAdapter.merge(session)` 的四种 `validation_status` 与返回键（含 `artifact_version_id` / `back_target` / `attempt` / `stage_state`）、四个纯函数（`build_citation_pool` / `project_repo_associations` / `project_current_state` / `derive_must_haves`）的签名与口径、citation id 前缀约定、`MAX_MERGE_ROUNDS` 与 `_DEFAULT_CITATION_COVERAGE_MIN` 的模块常量名——113-06 的覆盖率门、`coverage_gaps` 归因、有界回退与 stage handler 全部直接消费）。
</output>
