---
phase: 12-kmod
plan: 02
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - server/knowledge/graph_store.py
  - server/tests/knowledge/test_graph_store.py
autonomous: true
requirements: [KMOD-04, KMOD-02]
must_haves:
  truths:
    - "通过 GraphStore.traverse 可完成 1–3 跳遍历；A→B→C→A 环正常终止且每实体只出现一次"
    - "max_hops=10 被 clamp 到 3：4 跳链的第 4 个节点不出现在结果中"
    - "invalid_at 已置位的边在默认遍历中不可见；传 as_of=失效前时点则可见（历史可查）"
    - "全仓 `WITH RECURSIVE` 与 knowledge_knowledgeedge 表名 raw SQL 仅出现在 knowledge/graph_store.py（grep 审计测试守护）"
    - "invalidate_entity_version 在单事务内同时失效版本与该实体出入边，失效后下游实体在多跳遍历中不可达"
  artifacts:
    - path: "server/knowledge/graph_store.py"
      provides: "GraphStore Protocol + TraversalResult/EdgeRecord dataclass + RelationalGraphStore 实现 + 模块级单例 graph_store"
      exports: ["GraphStore", "RelationalGraphStore", "TraversalResult", "EdgeRecord", "graph_store", "invalidate_entity_version"]
      min_lines: 200
    - path: "server/tests/knowledge/test_graph_store.py"
      provides: "遍历/防环/深度 clamp/有效性/as-of/UUID prep/级联失效/grep 审计/perf 全套测试"
  key_links:
    - from: "server/knowledge/graph_store.py"
      to: "knowledge_knowledgeedge 表"
      via: "connection.cursor() 递归 CTE（全仓唯一 raw SQL 存在地）"
      pattern: "WITH RECURSIVE"
    - from: "server/knowledge/graph_store.py"
      to: "异步调用方"
      via: "sync_to_async 桥接"
      pattern: "sync_to_async\\(self\\._traverse_sync\\)"
---

<objective>
交付 KMOD-04 的 GraphStore service 接口：图读写唯一收口，1–3 跳递归遍历（SQLite/PG 双后端兼容的 `WITH RECURSIVE`）、有效性过滤、防环、深度上限全部内化于接口，调用方拿不到绕过接口的裸 SQL 路径；同时交付 KMOD-02 的行为面（失效置位后默认遍历不可见、as_of 历史可见）与 P2 级联失效操作原语。

Purpose: GraphStore 是换图引擎逃生门（locked decision：不引入 Neo4j，PG 递归 CTE 实现）；有效性过滤埋进接口而非靠约定（locked decision）。Phase 13+ 全部图访问 import 本模块单例。
Output: `server/knowledge/graph_store.py`（全仓唯一边表 raw SQL 存在地）+ `test_graph_store.py` 全绿（含 grep 审计测试固化收口）。
</objective>

<execution_context>
@.cursor/gsd-core/workflows/execute-plan.md
@.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-kmod/12-RESEARCH.md
@.planning/phases/12-kmod/12-PATTERNS.md
@server/knowledge/models.py
@server/system/health_views.py
@server/code_relations/management/commands/rebuild_chunk_edges.py
</context>

## Artifacts this phase produces（Plan 02 范围）

- **模块**: `server/knowledge/graph_store.py`
- **类型**: `TraversalResult`（frozen dataclass：`entity_id: uuid.UUID`, `depth: int`）、`EdgeRecord`（frozen dataclass：`edge_id` / `source_id` / `target_id` / `target_chunk_id` / `relation` / `metadata` / `valid_at` / `invalid_at` / `created_at` / `expired_at`）
- **接口**: `GraphStore`（`typing.Protocol`）：`add_edge` / `invalidate_edge` / `expire_edge` / `neighbors` / `traverse`
- **实现**: `RelationalGraphStore`（常量 `MAX_HOPS = 3`、`_RESULT_LIMIT = 1000`；私有 `_traverse_sync` / `_build_sql` / `_prep_uuid` / `_to_uuid`）
- **模块级函数**: `invalidate_entity_version(entity_id, *, invalid_at)`（单事务级联失效原语）、`_require_aware(dt, field)`（P2 防线）
- **模块级单例**: `graph_store = RelationalGraphStore()`
- **测试**: `server/tests/knowledge/test_graph_store.py`（含 grep 审计测试与 `@pytest.mark.perf` 基准）

<tasks>

<task type="auto">
  <name>Task 1: GraphStore 接口、写路径与级联失效原语</name>
  <files>server/knowledge/graph_store.py</files>
  <read_first>
    - server/knowledge/models.py（Plan 01 产物：KnowledgeEdge/KnowledgeEntityVersion 字段、EdgeRelation 枚举、related_name）
    - .planning/phases/12-kmod/12-RESEARCH.md §GraphStore 设计 + §Code Examples（接口形态与 _require_aware 骨架——本文件无完整仓库类比，RESEARCH 骨架即规格）
    - server/code_relations/management/commands/rebuild_chunk_edges.py（`timezone.is_naive` 拒绝 naive datetime 的仓库先例，lines 104-108）
    - server/workflows/nodes/registry.py（模块尾部单例模式，末行 `registry = NodeRegistry()`）
  </read_first>
  <action>
    创建 `server/knowledge/graph_store.py`，本任务交付接口与 ORM 写路径（遍历 raw SQL 在 Task 2 补全，本任务先以 `raise NotImplementedError` 占位 `_traverse_sync`）：

    1. 模块 docstring（中文）写明三条实现契约：① 图访问唯一收口，`WITH RECURSIVE` 与 knowledge_knowledgeedge 的 raw SQL 全仓只允许出现在本文件（grep 审计测试守护）；② 默认语义 = 当前有效（`invalid_at IS NULL AND expired_at IS NULL`），历史查询走显式 as_of；③ 接口方法全部 keyword-only 参数——为 Phase 15 权限 scope 参数（project_id 等）预留非破坏性扩展位（ASVS V4 预埋）。
    2. dataclass：`TraversalResult(entity_id, depth)`、`EdgeRecord(edge_id, source_id, target_id, target_chunk_id, relation, metadata, valid_at, invalid_at, created_at, expired_at)`，均 `@dataclass(frozen=True)`。
    3. `_require_aware(dt: datetime, field: str) -> datetime`：`dt.tzinfo is None` 时 raise ValueError（中文消息注明 USE_TZ=True 禁止 naive，P2 防线）。
    4. `GraphStore(Protocol)` 五方法签名（全 async、self 后全部 keyword-only）：
       - `add_edge(*, source_id, target_id=None, target_chunk_id=None, relation, valid_at, metadata=None) -> uuid.UUID`
       - `invalidate_edge(edge_id, *, invalid_at) -> None`
       - `expire_edge(edge_id, *, expired_at) -> None`
       - `neighbors(entity_id, *, relations=None, direction="out", as_of=None) -> list[EdgeRecord]`
       - `traverse(start_id, *, max_hops=2, relations=None, direction="out", as_of=None) -> list[TraversalResult]`
    5. `RelationalGraphStore` 实现（写路径与单跳全走 ORM，经 `sync_to_async` 桥接）：
       - `add_edge`：`_require_aware(valid_at)`；relation 必须 ∈ `EdgeRelation.values` 否则 ValueError；`KnowledgeEdge.objects.acreate(...)` 返回 edge id。target_id 与 target_chunk_id 的 XOR 由 DB 约束兜底，接口层也先行校验并 raise ValueError（错误信息更友好）。
       - `invalidate_edge` / `expire_edge`：`_require_aware` 后 `KnowledgeEdge.objects.filter(id=edge_id).aupdate(invalid_at=...)` / `aupdate(expired_at=...)`；目标不存在时 raise `KnowledgeEdge.DoesNotExist`。
       - `neighbors`：ORM filter 实现（无需 raw SQL）。direction="out" 过滤 source_entity_id，"in" 过滤 target_entity_id，"both" 取并集；relations 列表过滤（逐值校验 ∈ EdgeRelation.values）；默认有效性过滤 `invalid_at__isnull=True, expired_at__isnull=True`；as_of 给定时改为 `valid_at__lte=as_of & (invalid_at isnull | gt as_of) & created_at__lte=as_of & (expired_at isnull | gt as_of)`（as_of 须 _require_aware）。返回 EdgeRecord 列表。
       - `traverse`：`hops = max(1, min(int(max_hops), self.MAX_HOPS))` 后 `await sync_to_async(self._traverse_sync)(...)`；`_traverse_sync` 本任务占位 raise NotImplementedError。
    6. 模块级 `invalidate_entity_version(entity_id, *, invalid_at) -> None`（async，内部 `sync_to_async` 包同步函数）：单事务（`transaction.atomic()`）内——① 该实体 `is_latest=True` 的版本行置 `invalid_at`；② 该实体全部活跃出入边（source_entity 或 target_entity 命中且 invalid_at isnull）置 `invalid_at`。docstring 注明：is_latest 翻转与重摄取触发在 Phase 13，本函数只交付操作原语与事务语义（P2 防线）。
    7. structlog logger（`structlog.get_logger(__name__)`），写路径关键事件：`knowledge_edge_added` / `knowledge_edge_invalidated` / `knowledge_entity_version_invalidated`（键值对风格）。
    8. 模块尾部 `graph_store = RelationalGraphStore()` 单例。
  </action>
  <verify>
    <automated>cd server && uv run python -c "import django,os; os.environ.setdefault('DJANGO_SETTINGS_MODULE','friday.settings'); django.setup(); from knowledge.graph_store import GraphStore, RelationalGraphStore, TraversalResult, EdgeRecord, graph_store, invalidate_entity_version; print('ok')" && uv run python manage.py check</automated>
  </verify>
  <acceptance_criteria>
    - `from knowledge.graph_store import GraphStore, RelationalGraphStore, TraversalResult, EdgeRecord, graph_store, invalidate_entity_version` 成功
    - `rg -c "def (add_edge|invalidate_edge|expire_edge|neighbors|traverse)" server/knowledge/graph_store.py` ≥ 10（Protocol 签名 + 实现各一套）
    - `RelationalGraphStore.MAX_HOPS == 3`；traverse 内含 `min(int(max_hops), self.MAX_HOPS)` 的 clamp 逻辑（源码断言）
    - `rg -c "_require_aware" server/knowledge/graph_store.py` ≥ 5（add/invalidate/expire/as_of/invalidate_entity_version 全部写路径覆盖）
    - `invalidate_entity_version` 内含 `transaction.atomic` 调用（源码断言）
  </acceptance_criteria>
  <done>接口与写路径（含级联失效原语）可 import 并通过 manage.py check，遍历入口已 clamp 并桥接到占位的 _traverse_sync。</done>
</task>

<task type="auto">
  <name>Task 2: 递归 CTE 遍历实现（SQLite/PG 双后端）</name>
  <files>server/knowledge/graph_store.py</files>
  <read_first>
    - server/knowledge/graph_store.py（Task 1 产物——在既有骨架上补 _traverse_sync/_build_sql）
    - .planning/phases/12-kmod/12-RESEARCH.md §递归 CTE：SQLite/PG 双方言要点 + SQL 骨架（可移植写法的唯一规格：字符串 path 防环、%s 占位、递归项深度上限、外层 LIMIT、UUID prep、validity 两个固定模板）
    - server/system/health_views.py（`with connection.cursor()` raw cursor 仓库标准姿势，lines 46-51）
  </read_first>
  <action>
    在 `server/knowledge/graph_store.py` 内实现遍历（全部私有方法，raw SQL 不出本文件）：

    1. `_prep_uuid(value)`：`KnowledgeEntity._meta.pk.get_db_prep_value(value, connection)`——Pitfall 1 防线（SQLite 32 位无连字符 hex / PG 原生 uuid 分叉），所有进 SQL 的 UUID 参数必经此函数。
    2. `_to_uuid(raw)`：`uuid.UUID(hex=raw) if isinstance(raw, str) else raw`（SQLite 回 str / PG psycopg 回 UUID 对象）。
    3. `_traverse_sync(start_id, hops, relations, direction, as_of)`：
       - 开头 `if connection.vendor not in ("sqlite", "postgresql"): raise NotImplementedError(f"GraphStore 不支持 {connection.vendor}")`（A1：MySQL 响亮失败）。
       - 调 `_build_sql` 拼装后 `with connection.cursor() as cur: cur.execute(sql, params)`，返回 `[TraversalResult(entity_id=self._to_uuid(r[0]), depth=r[1]) for r in rows]`。
    4. `_build_sql(prep_id, hops, relations, as_of, direction) -> tuple[str, list]`，按 RESEARCH SQL 骨架生成：
       - `WITH RECURSIVE walk(entity_id, depth, path)`：anchor 为起点 1 跳邻居（path 初始 `',' || CAST(source) || ',' || CAST(target) || ','`）；递归项 `JOIN walk ON e.source_entity_id = w.entity_id`、`WHERE w.depth < %s`、path 防环 `w.path NOT LIKE '%%,' || CAST(e.target_entity_id AS TEXT) || ',%%'`；外层 `SELECT entity_id, MIN(depth) GROUP BY entity_id ORDER BY depth LIMIT %s`（`_RESULT_LIMIT` fail-safe）。direction="in" 时 source/target 列互换；"both" 本阶段 raise NotImplementedError（接口预留，docstring 注明 Phase 15 需要时扩展）。
       - `{validity_predicate}` 仅两个固定模板字符串：默认 `e.invalid_at IS NULL AND e.expired_at IS NULL`；as_of 模板 `e.valid_at <= %s AND (e.invalid_at IS NULL OR e.invalid_at > %s) AND e.created_at <= %s AND (e.expired_at IS NULL OR e.expired_at > %s)`。as_of 值经参数绑定出现 4 次（anchor 与递归项各一套，注意参数顺序）。
       - `{relation_filter}`：relations 非空时逐值断言 ∈ `EdgeRelation.values`（白名单，不通过则 ValueError），SQL 端生成 `AND e.relation IN (%s, %s, ...)` 占位符，值全部参数绑定。**禁止任何用户输入直接拼进 SQL 字符串**（T-12-01）。
       - 占位符统一 `%s`（Django cursor 双后端适配）；表名硬编码 `knowledge_knowledgeedge`（仅此文件允许出现）。
       - 始终附加 `e.target_entity_id IS NOT NULL`（chunk 边不参与实体遍历）。
    5. structlog：`knowledge_graph_traversed`（start_id/hops/result_count/elapsed_ms 键值）。
  </action>
  <verify>
    <automated>cd server && uv run python -c "import django,os; os.environ.setdefault('DJANGO_SETTINGS_MODULE','friday.settings'); django.setup(); from knowledge.graph_store import RelationalGraphStore; s=RelationalGraphStore(); sql,params=s._build_sql('x',3,None,None,'out'); assert 'WITH RECURSIVE' in sql and 'NOT LIKE' in sql and '?' not in sql; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `_build_sql` 产出的 SQL 含 `WITH RECURSIVE`、`NOT LIKE`（path 防环）、`LIMIT`（外层 fail-safe），且不含 `?` 占位符（统一 `%s`）
    - `_traverse_sync` 首行做 `connection.vendor` 白名单检查（源码断言 `not in ("sqlite", "postgresql")`）
    - 所有 UUID 参数经 `_prep_uuid`（`rg -c "get_db_prep_value" server/knowledge/graph_store.py` ≥ 1，且 anchor 起点参数走该路径）
    - relations 过滤生成占位符前有 `EdgeRelation.values` 白名单校验（源码断言）
    - 递归项内不存在 `LIMIT`（PG 不允许；`rg` 检查 LIMIT 仅出现在最外层 SELECT 段）
  </acceptance_criteria>
  <done>traverse 在 SQLite 下可执行端到端查询（Task 3 测试验证），SQL 生成满足双后端可移植规格。</done>
</task>

<task type="auto">
  <name>Task 3: test_graph_store.py — 行为、防线与 grep 审计全套测试</name>
  <files>server/tests/knowledge/test_graph_store.py</files>
  <read_first>
    - server/knowledge/graph_store.py（被测对象——Task 1/2 产物）
    - server/tests/knowledge/conftest.py（entity_factory / edge_factory fixture）
    - server/tests/test_git_diff_index.py（line 29-30：`pytestmark = [pytest.mark.django_db(transaction=True), ...]`——SQLite + async 跨线程必需）
    - .planning/phases/12-kmod/12-RESEARCH.md §Code Examples 测试骨架 + §Validation Architecture（用例清单）
  </read_first>
  <action>
    创建 `server/tests/knowledge/test_graph_store.py`，`pytestmark = pytest.mark.django_db(transaction=True)`（asyncio_mode=auto 已全局开启），用例：

    遍历行为（KMOD-04）：
    - 线性链 A→B→C→D：max_hops=2 返回 {B:1, C:2}，D 不可达；max_hops=3 含 D
    - 环 A→B→C→A：max_hops=3 正常终止，结果实体集合 = {B, C, A} 且每实体一条（MIN depth）
    - 深度 clamp：5 节点链 A→B→C→D→E，max_hops=10 仍只达 D（第 3 跳），E 不出现
    - relations 过滤：HAS_PLAN 与 RELATES_TO 混合边，relations=["HAS_PLAN"] 只走 HAS_PLAN 边；relations 传非法值 raise ValueError
    - direction="in" 反向遍历命中上游实体
    - chunk 边（target_chunk_id 填、target_entity 空）不出现在实体遍历结果

    有效性与 as-of（KMOD-02）：
    - 失效边默认不可见：A→B 边置 invalid_at 后 traverse(A) 为空、neighbors(A) 为空
    - as_of 历史可见：as_of=失效前时点 traverse/neighbors 命中 B
    - 多跳中段失效：A→B→C，B→C 失效后 traverse(A, max_hops=3) 只含 B（下游不可达）
    - expired_at 置位（系统时间线作废）同样默认不可见
    - naive datetime 传入 add_edge / as_of → ValueError（P2）

    级联失效原语：
    - 建 A→B→C 链与各实体版本，`invalidate_entity_version(B.id, invalid_at=now)` 后：B 的 latest 版本 invalid_at 已置位、B 的出入边均失效、traverse(A, max_hops=3) 不再含 C（2–3 跳级联测试）

    UUID prep（Pitfall 1 专测）：
    - 直接以 Python uuid.UUID 调 traverse，SQLite 下能命中数据（隐式验证 get_db_prep_value 路径；对照组：绕过 prep 用 `str(uuid)` 手工 cursor 查询返回空——证明 prep 必要性）

    grep 审计（P9 防线固化）：
    - 同步纯文本用例（不标 django_db）：遍历 `server/` 下全部 `*.py`（排除 migrations 与 tests），断言含 `WITH RECURSIVE` 的文件集合 ⊆ {`knowledge/graph_store.py`}；含 `knowledge_knowledgeedge` 字面量的非测试源码文件集合 ⊆ {`knowledge/graph_store.py`}（models.py 用 Django 默认表名不含该字面量，若 Meta 显式 db_table 则一并豁免——以实际实现为准写白名单）

    perf 基准（CI skip）：
    - `@pytest.mark.perf`：bulk_create 2000 实体 / 10000 边（随机连接），traverse 3 跳耗时断言 < 2s
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/knowledge/test_graph_store.py -x -q && uv run pytest tests/knowledge/test_graph_store.py -k "invalid or as_of" -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/knowledge/test_graph_store.py -x` 全绿（perf 标记默认 skip），用例数 ≥ 14
    - `uv run pytest tests/knowledge/test_graph_store.py -k "invalid or as_of" -x` 收集到 ≥ 3 个用例且全绿（VALIDATION.md KMOD-02 验证命令）
    - 环用例、深度 clamp 用例、中段失效用例、grep 审计用例的测试函数名可被 `rg "def test_.*(cycle|clamp|midchain|audit|raw_sql)" server/tests/knowledge/test_graph_store.py` 命中 ≥ 4
    - 文件含 `pytest.mark.django_db(transaction=True)`（SQLite + async 纪律）
    - perf 用例带 `@pytest.mark.perf` 装饰器
  </acceptance_criteria>
  <done>KMOD-04 全部 success criteria 行为 + KMOD-02 有效性行为 + P9 raw SQL 收口审计有自动化测试固化，全套绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 调用方（Phase 13+ 服务/节点）→ GraphStore | relations / max_hops / as_of / UUID 参数跨入 raw SQL 执行层 |
| GraphStore → 数据库 | 全仓唯一边表 raw SQL 通道 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | `_build_sql` 递归 CTE | mitigate | Task 2：全参数化 `%s` 绑定；relations 经 `EdgeRelation.values` 白名单校验后才生成占位符；validity 谓词仅两个固定模板字符串，零用户输入拼接。Task 3 grep 审计 + relations 非法值 ValueError 测试固化 |
| T-12-03 | Denial of Service | traverse 递归遍历 | mitigate | Task 1：max_hops clamp ≤ MAX_HOPS=3；Task 2：字符串 path NOT LIKE 防环 + 外层 LIMIT 1000 fail-safe + 复用 Plan 01 的 idx_kedge_active_fanout 部分索引；Task 3 环/clamp 测试 + perf 基准固化 |
| T-12-02 | Information Disclosure | 接口权限 scope 缺失 | mitigate | Task 1：接口全 keyword-only 参数设计 + docstring 注明 Phase 15 权限 scope 参数扩展位（ASVS V4 预埋）；数据面权限维度由 Plan 01 实体 FK 与 Plan 03 payload schema 承担 |
| T-12-04 | Repudiation | 失效语义被绕过（物理删除） | mitigate | Task 1：接口只提供 invalidate/expire 置位方法，不提供 delete；级联失效走单事务原语并写 structlog 事件留痕 |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | 本计划零新依赖 |
</threat_model>

<verification>
- `cd server && uv run pytest tests/knowledge/ -x`（knowledge 全包绿）
- `cd server && uv run pytest`（wave 合并时全套回归）
- 手动（Manual-Only per VALIDATION.md）：docker compose 启 postgres，`DATABASE_URL` 指向后跑 `tests/knowledge/test_graph_store.py` 验证 PG 方言路径
</verification>

<success_criteria>
- ROADMAP SC#3 成立：GraphStore 接口完成 1–3 跳遍历，自动施加有效性过滤、深度上限、防环；边表 raw SQL 仅存在于 graph_store.py 且有 grep 审计测试守护
- ROADMAP SC#2 行为面成立：失效置位的边默认遍历不可见但 as_of 历史可查
- 级联失效操作原语（单事务）就位，Phase 13 重摄取可直接消费
</success_criteria>

<output>
完成后创建 `.planning/phases/12-kmod/12-02-SUMMARY.md`
</output>
