---
phase: 23-purge-reconcile
plan: 04
type: execute
wave: 3
depends_on: ["23-02"]
files_modified:
  - web/src/api/exclusions.ts
  - web/src/components/repository/ReconcilePanel.vue
  - web/src/pages/repositories/[id]/index.vue
  - web/src/locales/zh-CN.json
autonomous: true
requirements: [EXCL-06]
must_haves:
  truths:
    - "UI 能展示对账差异：已索引但现命中排除的文件清单 + 命中数（EXCL-06）"
    - "差异>0 时给出提示，无差异时显示『已对齐』空态"
    - "一键『普通清理』入口：执行后 toast + 刷新对账，差异归零"
    - "显式『敏感清理』入口与普通清理分开（不混一个按钮，§9.2），需更强不可逆确认"
    - "敏感清理确认文案如实声明不可逆 + 不承诺 git/备份物理消失（§9.1）"
    - "面板挂在仓库详情页 ExclusionRulesPanel 旁，复用既有 reka-ui + vue-query + vue-i18n 模式"
  artifacts:
    - path: "web/src/components/repository/ReconcilePanel.vue"
      provides: "对账差异展示 + 普通/敏感清理两入口 + 强确认"
      contains: "reconcile"
    - path: "web/src/api/exclusions.ts"
      provides: "reconcileApi.get / reconcileApi.cleanup 类型化 client"
      contains: "reconcileApi"
  key_links:
    - from: "web/src/components/repository/ReconcilePanel.vue"
      to: "web/src/api/exclusions.ts::reconcileApi"
      via: "vue-query 拉差异 + mutation 触发清理"
      pattern: "reconcileApi"
    - from: "web/src/pages/repositories/[id]/index.vue"
      to: "web/src/components/repository/ReconcilePanel.vue"
      via: "挂在 ExclusionRulesPanel 旁，传 repository.id"
      pattern: "ReconcilePanel"
---

<objective>
落地对账/清理前端（EXCL-06）：扩展 `api/exclusions.ts` 增加 `reconcileApi`（GET 差异 / POST 清理）；新建 `ReconcilePanel.vue` 展示"已索引但现命中排除"的差异文件清单与命中数，提供**一键普通清理**与**显式敏感清理**两个分开入口（不混一个按钮，§9.2），敏感清理走更强不可逆确认（如实声明不可逆 + 不承诺 git/备份物理消失，§9.1）；挂到仓库详情页 `ExclusionRulesPanel` 旁。复用既有 `@tanstack/vue-query` + `reka-ui` + `useConfirmDialog` + `vue-i18n`（默认中文）模式。

Purpose: 让用户"看得见差异、按需清理"——把 22 的排除规则面板补上"存量对账 + 两模式清理"的可视闭环，达成 Phase 23 成功标准 3（UI 能对比规则 vs 已索引内容、提示差异、一键清理）。
Output: `reconcileApi` 客户端、`ReconcilePanel.vue`、详情页接线、zh-CN i18n 文案。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/23-purge-reconcile/23-CONTEXT.md
@.planning/phases/23-purge-reconcile/23-02-PLAN.md
</context>

<artifacts>
## Artifacts this plan produces

| Symbol | Kind | File | Notes |
|--------|------|------|-------|
| `ReconcileReport` (TS interface) | type | `web/src/api/exclusions.ts` | `indexed_count`/`excluded_paths`/`match_count`/`suggested_mode` |
| `CleanupResponse` (TS interface) | type | `web/src/api/exclusions.ts` | `mode`/`match_count`/`dispatched` |
| `reconcileApi.get(repoId)` | api fn | `web/src/api/exclusions.ts` | GET `/repositories/{id}/reconcile/` |
| `reconcileApi.cleanup(repoId, mode)` | api fn | `web/src/api/exclusions.ts` | POST `/repositories/{id}/reconcile/` |
| `ReconcilePanel` | Vue component | `web/src/components/repository/ReconcilePanel.vue` | 差异展示 + 两模式清理 |
| `reconcile.*` i18n keys | locale | `web/src/locales/zh-CN.json` | 标题/差异/普通/敏感/确认/toast 文案 |
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: reconcileApi 客户端 + ReconcilePanel 组件 + i18n + 详情页接线</name>
  <read_first>
    - web/src/api/exclusions.ts（既有 exclusionsApi 风格、get/post import、类型定义）
    - web/src/api/client.ts（`get`/`post`、`ApiError`）
    - web/src/components/repository/ExclusionRulesPanel.vue（vue-query/useMutation/useConfirmDialog/useToast/useErrorHandler/i18n + card 模板与 securityNote 措辞，作为同构参照）
    - web/src/pages/repositories/[id]/index.vue（~13 import 区、~608 ExclusionRulesPanel 挂载位置、repository.id 传参）
    - web/src/locales/zh-CN.json（`exclusion.*` 键所在层级，新增 `reconcile.*` 兄弟键）
    - web/src/composables/useConfirmDialog.ts（confirm 强确认 + variant:'destructive' 用法）
  </read_first>
  <files>web/src/api/exclusions.ts, web/src/components/repository/ReconcilePanel.vue, web/src/pages/repositories/[id]/index.vue, web/src/locales/zh-CN.json</files>
  <action>
    在 `api/exclusions.ts` 增加 `ReconcileReport`/`CleanupResponse` interface 与 `reconcileApi`：`get(repoId)` → `get<ReconcileReport>('/repositories/${repoId}/reconcile/')`；`cleanup(repoId, mode)` → `post<CleanupResponse>('/repositories/${repoId}/reconcile/', { mode })`（mode: 'normal' | 'sensitive'）。新建 `ReconcilePanel.vue`（`<script setup lang="ts">`，props `repositoryId: string`）：用 `useQuery(['repository-reconcile', repositoryId], () => reconcileApi.get(...))` 拉差异；展示 `match_count`、`excluded_paths` 列表（命中文件 code 行）；`match_count===0` 显示『已对齐』空态。两个分开按钮：①『普通清理』→ `useConfirmDialog` 常规确认 → `reconcileApi.cleanup(id,'normal')` → success toast + invalidate 对账与排除两个 queryKey（差异随后归零）；②『敏感清理』→ 单独 `destructive` 强确认，描述文案如实声明**不可逆** + **不承诺 git object / 备份物理消失**（复用/对齐 exclusion.securityNote 措辞）→ `reconcileApi.cleanup(id,'sensitive')` → toast + invalidate。清理为后台派发（API 返回 202 dispatched），toast 提示"已开始清理（后台执行）"，并在短暂延迟后 refetch 对账。错误经 `useErrorHandler`。在 `zh-CN.json` 的 `exclusion` 同级新增 `reconcile` 键集合（title/subtitle/matchCount/empty/diffTitle/cleanupNormal/cleanupSensitive/confirm.normalTitle/confirm.normalDesc/confirm.sensitiveTitle/confirm.sensitiveDesc(含不可逆+不承诺物理消失)/toast.dispatched/error.load/error.cleanup）。在 `[id]/index.vue` import `ReconcilePanel` 并紧邻 `<ExclusionRulesPanel :repository-id="repository.id" />`（~608）下方挂 `<ReconcilePanel :repository-id="repository.id" />`。action 内不放 fenced code。
  </action>
  <acceptance_criteria>
    - `reconcileApi.get`/`reconcileApi.cleanup` 类型化，路径与 23-02 后端一致（`/repositories/{id}/reconcile/`）。
    - `ReconcilePanel.vue` 渲染差异清单 + 命中数；`match_count===0` 显示空态。
    - 普通清理与敏感清理为两个独立按钮（§9.2）；敏感清理用 destructive 强确认，文案含"不可逆"且声明不承诺 git/备份物理消失。
    - 详情页在 ExclusionRulesPanel 旁挂载 ReconcilePanel。
    - `cd web && pnpm vue-tsc --noEmit`（或既有 typecheck 脚本）无新增类型错误；`pnpm lint` 对改动文件无新增报错。
  </acceptance_criteria>
  <verify>
    <automated>cd web &amp;&amp; pnpm exec vue-tsc --noEmit</automated>
    <human-check>
      `cd web && pnpm dev`，进入一个已索引仓库详情页：(1) 在 ExclusionRulesPanel 新增命中某已索引文件的排除规则；(2) ReconcilePanel 出现差异提示 + 命中数 + 该文件在差异清单；(3) 点『普通清理』常规确认 → "已开始清理（后台执行）"toast → 刷新后差异归零、显示『已对齐』空态；(4) 点『敏感清理』确认为 destructive 强确认，文案含"不可逆"且声明"不承诺从 git 历史/备份物理消失"；(5) 普通与敏感是两个分离入口（非一个按钮切换）。
    </human-check>
  </verify>
  <done>对账/清理前端就位：差异可视、普通/敏感两入口分离、敏感强确认如实措辞、面板已接入详情页；人工验收差异→清理→归零闭环。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 用户操作 → 清理 API | 误触清理（尤其敏感不可逆）破坏数据 |
| UI 措辞 → 用户预期 | 过度安全承诺（"已彻底删除"）误导用户 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-14 | Tampering / 不可逆 | 误触敏感清理 | mitigate | 敏感清理独立入口 + destructive 强确认 + 不可逆文案（§9.2 不混一个按钮） |
| T-23-15 | Information Disclosure（认知） | UI 过度承诺 | mitigate | 确认/提示文案如实声明不承诺 git/备份物理消失（§9.1），对齐 exclusion.securityNote |
| T-23-16 | Spoofing / 越权 | 跨仓清理 | accept | API 层 IsAuthenticated + repository_id 作用域（23-02 守护）；前端仅传当前 repo id |
</threat_model>

<verification>
- `cd web && pnpm exec vue-tsc --noEmit` 无新增类型错误；`pnpm lint` 改动文件无新增报错。
- 人工验收：差异提示 → 一键普通清理 → 对账归零；敏感清理独立入口 + 强不可逆确认 + 如实措辞。
- 普通/敏感为两个分离入口（§9.2）。
</verification>

<success_criteria>
- UI 对比"排除规则 vs 已索引内容"、提示差异、支持一键清理（EXCL-06，成功标准 3）。
- 普通清理与敏感清理入口显式分离；敏感清理强确认且措辞如实（不承诺物理消失）。
- 面板接入仓库详情页，复用既有前端栈与中文 i18n。
</success_criteria>

<output>
Create `.planning/phases/23-purge-reconcile/23-04-SUMMARY.md` when done.
</output>
