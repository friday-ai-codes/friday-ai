---
phase: 143-eval
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - server/tests/initiatives/test_session_capture_eval.py
  - server/tests/initiatives/test_capture_service.py
  - server/tests/test_model_usage_call_source.py
  - server/tests/knowledge/test_session_capture_source.py
autonomous: true
requirements: [EVAL-01, EVAL-02, EVAL-03, EVAL-04, EVAL-05]
must_haves:
  truths:
    - "评估器的 high/medium/low 闭集、独立 distilled_essence、失败不降 low 先由 RED 契约锁定"
    - "Capture 状态只能经 CaptureService CAS 前进，评估与入图重试域彼此独立"
    - "low 永不产生摄取事件，medium/high 只投影 distilled_essence"
    - "无 repository/project 的 Capture 仍可评估并形成可摄取 DOCUMENT，Phase 144 读侧不可见不等于静默丢失"
  artifacts:
    - path: "server/tests/initiatives/test_session_capture_eval.py"
      provides: "Friday LLM 三档评估、用量、失败保留与 ProjectMemory 隔离契约"
    - path: "server/tests/initiatives/test_capture_service.py"
      provides: "Capture 状态机 CAS、attempt 与 retry 元数据契约"
    - path: "server/tests/knowledge/test_session_capture_source.py"
      provides: "session_capture DOCUMENT normalizer 精华-only 契约"
  key_links:
    - from: "server/tests/initiatives/test_session_capture_eval.py"
      to: "server/agents/call_source.py"
      via: "SESSION_CAPTURE_EVAL 与 ModelUsageRecord 断言"
      pattern: "session_capture_eval"
    - from: "server/tests/knowledge/test_session_capture_source.py"
      to: "knowledge.ingestion.IngestionEvent"
      via: "DOCUMENT/session_capture/essence-only 事件断言"
      pattern: "EntityKind\\.DOCUMENT"
---

<objective>
建立 Phase 143 第一组 tracer-first Wave 0 RED 契约，先钉死评估、状态机与知识投影的业务边界。

Purpose: 在生产实现前让失败默认 low、原文入向量、ProjectMemory 旁路、无锚 Capture 丢失等错误实现无法假绿。
Output: 四个可收集、按行为命名的 pytest 契约文件/扩展。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/143-eval/143-CONTEXT.md
@.planning/phases/143-eval/143-RESEARCH.md
@.planning/phases/143-eval/143-PATTERNS.md
@.planning/phases/143-eval/143-VALIDATION.md
@.planning/phases/141-capture/141-04-SUMMARY.md
@.planning/phases/142-mcp/142-04-SUMMARY.md
@server/tests/initiatives/test_capture_service.py
@server/tests/knowledge/test_project_memory_source.py
@server/tests/test_model_usage_call_source.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 建立 Friday LLM 评估与用量 RED 契约</name>
  <files>server/tests/initiatives/test_session_capture_eval.py, server/tests/test_model_usage_call_source.py</files>
  <action>新增 evaluator 单测，参数化 high/medium/low 严格 JSON 结果与非空 distilled_essence；非法 JSON、非法 tier、缺字段、空输入、缺 Friday default_model、上游异常都必须失败并保留 Capture，且绝不默认 low。patch `ProviderConfigService.aresolve`、`build_chat_model` 与 `arecord_llm_usage`，断言成功记录 token/TTFT/duration，失败记录 upstream status，call_source 固定为 `session_capture_eval`。增加静态守卫，禁止 evaluator import `evaluate_writeback_quality`、`knowledge.llm_grader`、repo-router confidence、`MemoryService` 或 `record_hook_writeback`。同步扩展完整 CallSource 期望集，补上既有遗漏 `initiative_profile` 与待实现的 `session_capture_eval`，目标闭集 47；此任务只写测试，不改生产枚举。</action>
  <verify>
    <automated>cd server &amp;&amp; uv run pytest tests/initiatives/test_session_capture_eval.py tests/test_model_usage_call_source.py::TestCallSourceEnum -q --tb=short</automated>
  </verify>
  <done>测试可收集，生产 evaluator/CallSource 尚未实现的断言明确 RED；既有无关测试不得因 fixture 或导入错误先于目标断言失败。</done>
</task>

<task type="auto">
  <name>Task 2: 扩展 CaptureService 状态机 RED 契约</name>
  <files>server/tests/initiatives/test_capture_service.py</files>
  <action>增加合法与非法 CAS 转移测试：`pending_eval|eval_failed → evaluating`、`evaluating → evaluated_low|ingest_pending`、`ingest_pending|ingest_failed → ingesting`、`ingesting → ingested|ingest_failed`。断言 eval_attempts 与 ingest_attempts 分开递增、失败错误先脱敏、next_retry_at 可写/可清、终态不可回退、并发 claim 只有一个成功。明确保留 legacy `evaluated` 但它既不是新 writer 目标也不可被 claim。所有测试经 CaptureService 公共方法驱动，不旁路 ORM 写状态；状态准备可用唯一 writer 内测试 helper。</action>
  <verify>
    <automated>cd server &amp;&amp; uv run pytest tests/initiatives/test_capture_service.py -k "claim or record_eval or record_ingest or cas or retry" -x</automated>
  </verify>
  <done>CAS、独立 attempt、重试元数据、legacy 状态与终态幂等均有失败契约。</done>
</task>

<task type="auto">
  <name>Task 3: 建立 session_capture normalizer RED 契约</name>
  <files>server/tests/knowledge/test_session_capture_source.py</files>
  <action>镜像 project_memory normalizer 测试形状，但固定 `EntityKind.DOCUMENT`、`EntityOrigin.MCP`、`source_kind=session_capture` 与 `source_id=capture UUID`。断言 content 仅来自再次脱敏后的 distilled_essence，payload 仅含 capture_id/value_tier/repository_id/project_id 标量且无 question/answer/transcript/essence 副本；low、缺失或未完成评估返回空。显式覆盖无仓库、无项目的 medium/high：仍返回事件，repository_id/space_id 为 None、edges 为空，使无锚 Capture 保持可评估/可重试/可入统一知识库，即使 Phase 144 过滤暂时不能暴露；有项目才添加 REFERENCES 与 space。</action>
  <verify>
    <automated>cd server &amp;&amp; uv run pytest tests/knowledge/test_session_capture_source.py -q --tb=short</automated>
  </verify>
  <done>DOCUMENT、精华-only、low 无事件、无锚不丢与可选项目边均有 RED 契约。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM output → Capture state | 非可信 JSON 试图写 value_tier/essence |
| Capture → knowledge event | 脱敏原文与可检索精华跨入 RAG 边界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-143-01 | Information Disclosure | evaluator/normalizer | mitigate | sentinel 测试禁止问答、精华副本和密钥进入日志/payload |
| T-143-02 | Tampering | LLM result parser | mitigate | 严格 JSON + tier 闭集；非法输出失败而非 low |
| T-143-04 | Elevation of Privilege | ProjectMemory boundary | mitigate | 静态与行为测试锁定零 Memory 写入口 |
| T-143-SC | Tampering | package installs | accept | 本阶段零新增依赖，无安装任务 |
</threat_model>

<verification>
Wave 0 允许目标断言 RED，但测试必须可收集、ruff 通过且失败原因只指向 Phase 143 尚未实现的符号/行为。
</verification>

<success_criteria>
- 三档评估、严格失败、用量归因、CAS、精华-only 与无锚行为均有自动化 tracer。
- 测试明确禁止质量门复用、ProjectMemory 写入和原始问答进入 RAG。
</success_criteria>

<output>
Create `.planning/phases/143-eval/143-01-SUMMARY.md` when done.
</output>
