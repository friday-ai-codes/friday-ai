---
phase: 143-eval
plan: "05"
type: execute
wave: 2
depends_on: ["143-01", "143-03"]
files_modified:
  - server/knowledge/sources/session_capture.py
  - server/knowledge/sources/__init__.py
  - server/knowledge/models.py
  - server/tests/knowledge/test_session_capture_source.py
autonomous: true
requirements: [EVAL-03, EVAL-05, OBS-04]
must_haves:
  truths:
    - "仅 medium/high SessionCapture 产生 DOCUMENT/session_capture 事件"
    - "RAG 正文只含 distilled_essence，原始 question/answer 不进 content 或 payload"
    - "low 不产生事件，因此不触发 embedding、Qdrant 或版本写入"
    - "无仓库/项目的 medium/high 仍产生无锚事件，等待 Phase 144 补读侧可见性"
  artifacts:
    - path: "server/knowledge/sources/session_capture.py"
      provides: "SessionCapture 到统一 IngestionEvent 的 normalizer"
    - path: "server/knowledge/sources/__init__.py"
      provides: "session_capture normalizer 惰性登记"
  key_links:
    - from: "server/knowledge/sources/session_capture.py"
      to: "server/knowledge/ingestion.py"
      via: "IngestionEvent DOCUMENT/source_kind=session_capture"
      pattern: "source_kind=\"session_capture\""
    - from: "server/knowledge/sources/session_capture.py"
      to: "ProjectKnowledgeGraphService.ensure_project_node"
      via: "仅已有授权 project FK 时建立 REFERENCES"
      pattern: "EdgeRelation\\.REFERENCES"
---

<objective>
实现 medium/high SessionCapture 到统一 delivery_knowledge 的精华-only normalizer。

Purpose: 复用既有 ingestion 六步序与稳定 uuid5，自始至终隔离原始问答、low 档和 ProjectMemory。
Output: `session_capture` source normalizer、注册表与 natural-key 文档。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/143-eval/143-CONTEXT.md
@.planning/phases/143-eval/143-RESEARCH.md
@.planning/phases/143-eval/143-PATTERNS.md
@.planning/phases/143-eval/143-01-SUMMARY.md
@.planning/phases/143-eval/143-03-SUMMARY.md
@server/knowledge/sources/project_memory.py
@server/knowledge/sources/__init__.py
@server/knowledge/models.py
@server/knowledge/ingestion.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 实现 medium/high 精华-only normalizer</name>
  <files>server/knowledge/sources/session_capture.py, server/tests/knowledge/test_session_capture_source.py</files>
  <behavior>
    - "medium/high + 非空 essence 返回一个 DOCUMENT/session_capture 事件"
    - "low、缺失、未就绪或空 essence 返回空列表"
    - "无 project/repository 的 medium/high 仍返回无边事件"
  </behavior>
  <action>新增 async normalize，按 source_id 查 SessionCapture 并 select_related project/project.space/repository。仅 value_tier medium/high、状态 ingest_pending|ingesting|ingested 且非空 distilled_essence 时产生一个 IngestionEvent；low、legacy evaluated、pending/evaluating/failure 或缺失行返回空。content 只取再次 `redact_secrets_in_text` 的 essence；title 从 essence 首个非空行截取，禁止退回 question/answer。固定 kind=EntityKind.DOCUMENT、origin=EntityOrigin.MCP、source_kind=session_capture、source_id=str(capture.id)、event_time=evaluated_at or updated_at。payload 只含 capture_id/value_tier/repository_id/project_id 标量，不复制正文。repository_id 透传 Capture FK。无 project 时 space_id=None、edges=()，即便 repository_id 也为空仍返回事件：此规划明确解决研究 OQ-2，未锚定中高价值 Capture 不静默丢失，Phase 144 的仓/项目过滤在补读侧白名单前可能看不到它，但行、状态、实体都可评估和重试。有 project 时复用 ensure_project_node，添加 REFERENCES 与 space_id；不调用任何 Memory writer。sampling 生命周期只记 id/tier/status/计数/duration。</action>
  <verify>
    <automated>cd server &amp;&amp; uv run pytest tests/knowledge/test_session_capture_source.py -q --tb=short</automated>
  </verify>
  <done>精华-only、low 空、无锚仍产事件、有项目加边与脱敏测试全部通过。</done>
</task>

<task type="auto">
  <name>Task 2: 注册 source_kind 与稳定 natural key</name>
  <files>server/knowledge/sources/__init__.py, server/knowledge/models.py</files>
  <action>在 `_NORMALIZERS` 增加 `"session_capture": "knowledge.sources.session_capture"`，不改 get_normalizer 行为。只在 generate_entity_id docstring natural-key 表追加 `session_capture | SessionCapture UUID str | IDE 会话评估精华（DOCUMENT）`；不得修改 uuid5 namespace、拼接公式、EntityKind、collection 或数据库约束。此处不调用 `aschedule_ingestion`、不直接写 KnowledgeEntity/Qdrant。</action>
  <verify>
    <automated>cd server &amp;&amp; uv run pytest tests/knowledge/test_session_capture_source.py tests/knowledge/test_ingestion.py -k "session_capture or idempotent_three_runs or pre_embed or edge" -q --tb=short &amp;&amp; uv run ruff check knowledge/sources/session_capture.py knowledge/sources/__init__.py knowledge/models.py</automated>
  </verify>
  <done>注册表可解析 normalizer，稳定 entity id 继续由 DOCUMENT/session_capture/capture UUID 生成，无新 collection/kind。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Capture storage → RAG | 私密原始问答与精华跨向量化边界 |
| Optional project FK → graph | 已授权挂钩决定是否建立项目边 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-143-01 | Information Disclosure | normalizer payload/content | mitigate | content 仅 essence，payload 标量白名单，再脱敏 |
| T-143-03 | Denial of Service | low captures | mitigate | low/未就绪直接空事件，不触发向量层 |
| T-143-05 | Information Disclosure | project edge | mitigate | 只信持久化时已授权绑定的 project FK |
| T-143-SC | Tampering | package installs | accept | 无新增依赖 |
</threat_model>

<verification>
运行 normalizer 与既有 ingestion 幂等选择集；静态确认无 aschedule_ingestion、Memory 或 Qdrant writer。
</verification>

<success_criteria>
- medium/high 唯一正文是 distilled_essence，low 零向量。
- unanchored Capture 仍形成可恢复统一知识实体，不被 Phase 144 读侧边界误当成丢失。
</success_criteria>

<output>
Create `.planning/phases/143-eval/143-05-SUMMARY.md` when done.
</output>
