---
phase: 143-eval
plan: "06"
type: execute
wave: 3
depends_on: ["143-02", "143-03", "143-04", "143-05"]
files_modified:
  - server/initiatives/services/session_capture_enqueue.py
  - server/durable/queues.py
  - server/durable/tasks.py
  - server/durable/tasks_impl.py
  - server/durable/handlers.py
  - server/tests/initiatives/test_session_capture_eval_tasks.py
autonomous: true
requirements: [EVAL-01, EVAL-03, EVAL-04, EVAL-05, OBS-04]
must_haves:
  truths:
    - "durable eval 与 durable ingest 独立投递和重试，ingest 失败不重复 LLM"
    - "稳定 key/lock、CAS 与 content hash 使 at-least-once 重放幂等"
    - "pending/failed Capture 行可在进程重启、首次 defer 失败或 SQLite job 丢失后恢复"
    - "worker 从 payload re-bind initiated_by_user_id，缺失时 system"
  artifacts:
    - path: "server/initiatives/services/session_capture_enqueue.py"
      provides: "纯标量 durable 投递与 stranded recovery"
    - path: "server/durable/tasks_impl.py"
      provides: "独立 eval/ingest 共用任务体"
    - path: "server/durable/tasks.py"
      provides: "Postgres wrappers 与周期恢复任务"
  key_links:
    - from: "server/durable/tasks_impl.py"
      to: "server/initiatives/services/session_capture_eval.py"
      via: "eval worker 独立调用 evaluator"
      pattern: "SessionCaptureEvaluator"
    - from: "server/durable/tasks_impl.py"
      to: "server/knowledge/ingestion.py"
      via: "ingest worker 直接 await ingest"
      pattern: "await ingest"
    - from: "server/initiatives/services/session_capture_enqueue.py"
      to: "DurableTaskService.has_active_by_key"
      via: "数据库状态恢复扫描"
      pattern: "has_active_by_key"
---

<objective>
实现 Session Capture 的 durable 双任务、独立重试与数据库行恢复管线。

Purpose: 保证服务重启、短暂模型/向量故障和至少一次重放不会丢工作、重复收费或倒退状态。
Output: knowledge 队列、双后端注册、eval/ingest worker、恢复扫描与退避。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/143-eval/143-CONTEXT.md
@.planning/phases/143-eval/143-RESEARCH.md
@.planning/phases/143-eval/143-PATTERNS.md
@.planning/phases/143-eval/143-02-SUMMARY.md
@.planning/phases/143-eval/143-03-SUMMARY.md
@.planning/phases/143-eval/143-04-SUMMARY.md
@.planning/phases/143-eval/143-05-SUMMARY.md
@server/durable/service.py
@server/durable/tasks.py
@server/durable/tasks_impl.py
@server/durable/handlers.py
@server/knowledge/ingestion.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 建立 knowledge 队列、双后端任务与投递/恢复 helper</name>
  <files>server/initiatives/services/session_capture_enqueue.py, server/durable/queues.py, server/durable/tasks.py, server/durable/handlers.py, server/tests/initiatives/test_session_capture_eval_tasks.py</files>
  <behavior>
    - "eval/ingest payload 只含 capture_id/attempt，actor 由 DurableTaskService 注入"
    - "两后端均把相同 kwargs 交给相同任务体"
    - "due 且无 active key 的可恢复状态被重派"
  </behavior>
  <action>新增 QUEUE_KNOWLEDGE 并加入 ALL_QUEUES/__all__，不改 worker 配置。创建 enqueue helper：eval/ingest 分别用任务名 `durable_session_capture_eval`/`durable_session_capture_ingest`，稳定 idempotency key 与 lock `capture-eval:{id}`/`capture-ingest:{id}`，payload 仅 capture_id/attempt，initiated_by_user_id 走 DurableTaskService 参数。helper 先读 Capture 状态：终态/processing/不匹配状态 skip；异常先脱敏、sampling 记录后返回 None，不删除 Capture。实现 `recover_stranded_session_captures` 扫 due 的 pending_eval/eval_failed/ingest_pending/ingest_failed，按对应 key 调 has_active_by_key 后逐行隔离重派；next_retry_at=None 的首次 pending 仍可恢复，达到自动上限的 failed 行保留且可由显式 enqueue helper 手工重试。tasks.py 注册两个 keyword-only wrapper和每五分钟 QUEUE_MAINTENANCE 周期恢复，后者带 queueing_lock、只写单条汇总 sampling；handlers.py 为两个任务添加 `run_*(**payload)` adapter/注册。解决研究 A3：自动最大 6 次、5*2^attempt 秒封顶 300；失败行永不删除/降 low，显式重派仍可恢复。</action>
  <verify>
    <automated>cd server &amp;&amp; uv run pytest tests/initiatives/test_session_capture_eval_tasks.py -k "queue or adapter or payload or recovery or backoff" -q --tb=short</automated>
  </verify>
  <done>knowledge 队列、两后端、稳定投递、周期恢复与独立 retry 参数全部通过测试。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现独立 eval 与 ingest worker</name>
  <files>server/durable/tasks_impl.py, server/tests/initiatives/test_session_capture_eval_tasks.py</files>
  <behavior>
    - "eval worker 非 claimable 时不调用 LLM"
    - "low 终止；medium/high 记录结果后只 defer ingest"
    - "ingest worker 失败只进入 ingest_failed，不重新评估"
  </behavior>
  <action>实现 `run_session_capture_eval` 与 `run_session_capture_ingest`。最外层先 `bind_task_context(user_id=initiated_by_user_id or "system", source="durable", component="knowledge")`，发 sampling started/completed/failed 且不记录正文。eval worker 经 CaptureService.claim_evaluation；失败 claim 立即 skipped。claim 成功后从数据库读取脱敏问答，调用 SessionCaptureEvaluator；结果经 record_evaluation 写 low→evaluated_low 或 medium/high→ingest_pending，后者再调 enqueue ingest。异常经 record_eval_failure 写 next_retry_at，并在未达 6 次时 re-defer eval。ingest worker只 claim_ingestion 后 `await knowledge.ingestion.ingest(IngestionRequest(source_kind="session_capture", source_id=capture_id, trigger="session_capture_eval"))`；禁止调用 `aschedule_ingestion`，因为它内部仅 background_runner。`ingest` 返回 1 或 hash 幂等短路都记录 ingested；返回 0 视作可重试失败，避免 normalizer 零事件假成功。异常只 record_ingest_failure/re-defer ingest，绝不回 pending_eval 或调用 evaluator。每次日志包含 capture_id/status/tier/attempt/duration/user，错误脱敏，观测异常 best-effort。</action>
  <verify>
    <automated>cd server &amp;&amp; uv run pytest tests/initiatives/test_session_capture_eval_tasks.py -q --tb=short &amp;&amp; uv run ruff check initiatives/services/session_capture_enqueue.py durable/queues.py durable/tasks.py durable/tasks_impl.py durable/handlers.py</automated>
  </verify>
  <done>low 零 ingest、中高独立摄取、失败域隔离、重放 no-op、actor rebind 与 sampling 生命周期全部变绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Capture DB → durable queue | 工作真相投影为可重复任务 |
| Queue → evaluator/ingestion | 至少一次执行跨 LLM 和向量副作用 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-143-01 | Information Disclosure | job payload/log | mitigate | 纯标量 payload、脱敏错误、正文禁入事件 |
| T-143-03 | Denial of Service | replay/retry | mitigate | CAS + stable key/lock + bounded backoff + hash idempotency |
| T-143-04 | Elevation of Privilege | worker sinks | mitigate | 只调 CaptureService/evaluator/ingest，禁止 Memory |
| T-143-SC | Tampering | package installs | accept | 无新包 |
</threat_model>

<verification>
运行完整 task 测试、ruff，并静态确认 Session Capture 路径不存在 aschedule_ingestion/background_runner/Memory writer。
</verification>

<success_criteria>
- eval 与 ingest 可各自失败、恢复和重放，互不重做另一阶段。
- Postgres 与 in-process 后端参数同语义，数据库状态可恢复丢失 job。
</success_criteria>

<output>
Create `.planning/phases/143-eval/143-06-SUMMARY.md` when done.
</output>
