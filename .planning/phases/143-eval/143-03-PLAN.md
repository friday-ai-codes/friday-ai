---
phase: 143-eval
plan: "03"
type: execute
wave: 1
depends_on: ["143-01"]
files_modified:
  - server/initiatives/models/session_capture.py
  - server/initiatives/models/__init__.py
  - server/initiatives/migrations/0016_session_capture_evaluation.py
  - server/initiatives/services/capture_service.py
  - server/tests/initiatives/test_capture_service.py
autonomous: true
requirements: [EVAL-01, EVAL-04, EVAL-05]
must_haves:
  truths:
    - "已有 Capture 行原样保留，迁移只加 nullable/default-safe 字段、choices 与索引"
    - "状态转移只经 CaptureService 条件更新，at-least-once 竞争仅一名 worker 获得 claim"
    - "评估与摄取 attempt、失败态和 retry 时间彼此独立"
  artifacts:
    - path: "server/initiatives/migrations/0016_session_capture_evaluation.py"
      provides: "向后兼容的 Capture 评估/入图状态字段迁移"
    - path: "server/initiatives/services/capture_service.py"
      provides: "INV-6 CAS 状态机唯一 writer"
  key_links:
    - from: "server/initiatives/services/capture_service.py"
      to: "server/initiatives/models/session_capture.py"
      via: "filter(status__in=...).update CAS"
      pattern: "status__in"
---

<objective>
扩展 SessionCapture 为可恢复的评估/摄取状态机，并保持 INV-6 与存量数据安全。

Purpose: 为独立 evaluator、durable worker 和恢复扫描提供数据库工作真相。
Output: additive migration、闭集状态/价值枚举与 CaptureService CAS API。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/143-eval/143-CONTEXT.md
@.planning/phases/143-eval/143-RESEARCH.md
@.planning/phases/143-eval/143-PATTERNS.md
@.planning/phases/143-eval/143-01-SUMMARY.md
@server/initiatives/models/session_capture.py
@server/initiatives/migrations/0015_session_capture.py
@server/initiatives/services/capture_service.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 添加向后兼容的 Capture 评估字段与 migration</name>
  <files>server/initiatives/models/session_capture.py, server/initiatives/models/__init__.py, server/initiatives/migrations/0016_session_capture_evaluation.py</files>
  <behavior>
    - "现有 pending_eval 行迁移后状态与原始问答不变"
    - "value_tier 只允许空值/high/medium/low，新 attempt 默认为 0"
    - "所有新增状态字面值不超过 status.max_length=20"
  </behavior>
  <action>新增 `SessionCaptureValueTier` 闭集 high/medium/low；状态增加 evaluating、evaluated_low、ingesting、ingested、ingest_failed，并保留 legacy `EVALUATED="evaluated"` 兼容已存在或手工写入的数据，但任何新 writer 不以它为目标、也不从它 claim。增加 `value_tier`（blank/default ""）、`distilled_essence`（blank/default ""）、`eval_attempts`/`ingest_attempts`（非负默认 0）、`last_error`（blank/default ""）、`next_retry_at`（nullable）、`evaluated_at`/`ingested_at`（nullable），以及 `(status,next_retry_at)` 恢复索引。migration 依赖 0015，只做 AddField/AlterField/AddIndex；禁止 RunPython 删除、重档、猜 provider/model 或把 legacy evaluated 批量改 low。导出 value tier 枚举供 evaluator/normalizer 使用。本任务必须用 pytest 证明行为而不只看 migrate --plan：`test_value_tier_choices_closed_set` 断言 choices 仅空/high/medium/low；`test_status_literals_fit_max_length_20` 断言全部状态字面值长度 ≤ status.max_length；`test_additive_eval_fields_default_safe` 断言新字段 nullable/default 不回填；`test_existing_pending_eval_row_keeps_question_answer` 在迁移后读取既有 pending_eval 行，question/answer/status 原值不变。</action>
  <verify>
    <automated>cd server &amp;&amp; uv run pytest tests/initiatives/test_capture_service.py -k "value_tier or max_length or additive or pending_eval_row" -q --tb=short &amp;&amp; uv run python manage.py migrate initiatives 0016_session_capture_evaluation --plan &amp;&amp; uv run python manage.py makemigrations initiatives --check --dry-run</automated>
  </verify>
  <done>模型闭集档位、status 字面值 ≤20、新字段 default-safe、存量 pending_eval 问答不变均有 pytest 证据；migration plan 仅 additive。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现 CaptureService CAS 状态机</name>
  <files>server/initiatives/services/capture_service.py, server/tests/initiatives/test_capture_service.py</files>
  <behavior>
    - "claim_evaluation 从 pending_eval/eval_failed 成功一次并递增 eval_attempts；已是 evaluating 则 resume 且不递增"
    - "record_evaluation 将 low 写 evaluated_low，将 medium/high 写 ingest_pending"
    - "claim_ingestion 从 ingest_pending/ingest_failed 递增；已是 ingesting 则 resume 且不递增；失败/成功不修改 eval_attempts"
  </behavior>
  <action>在唯一 writer 内增加 async `get_capture`、`claim_evaluation`、`record_evaluation`、`record_eval_failure`、`claim_ingestion`、`record_ingested`、`record_ingest_failure`。底层同步 helper 使用 `SessionCapture.objects.filter(pk=..., status__in=...).update(...)`，rowcount 0 返回 not_claimable/no-op，不把 at-least-once 重放当业务异常。`claim_evaluation`：从 pending_eval|eval_failed 转入 evaluating 时同一 CAS 用 F() 递增 eval_attempts；若当前已是 evaluating 则 resume 返回该行且不递增。`claim_ingestion` 同理：ingest_pending|ingest_failed 递增 ingest_attempts；已是 ingesting 则 resume 不递增。legacy evaluated、evaluated_low、ingested 不得 claim。成功清 last_error/next_retry_at 并记录 evaluated_at/ingested_at；失败错误先经 `redact_secrets_in_text`，保存所属失败态和 next_retry_at。`record_evaluation` 再次校验 tier 闭集与非空 essence，不允许失败/非法值写 low。保持 `persist`、first-write-wins、caller 日志与原始 question/answer 不变；CaptureService 仍不得调 durable、ingest 或 Memory sink。</action>
  <verify>
    <automated>cd server &amp;&amp; uv run pytest tests/initiatives/test_capture_service.py tests/initiatives/test_capture_inv6_guard.py -q --tb=short &amp;&amp; uv run ruff check initiatives/models/session_capture.py initiatives/services/capture_service.py initiatives/migrations/0016_session_capture_evaluation.py</automated>
  </verify>
  <done>所有合法 CAS 变绿，非法/终态重放 rowcount=0；评估和摄取 retry 元数据互不覆盖。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Concurrent workers → Capture row | 至少一次任务竞争状态所有权 |
| Existing DB → new schema | 存量 Capture 穿过 migration |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-143-02 | Tampering | value tier/state | mitigate | choices + service 校验 + CAS |
| T-143-03 | Denial of Service | concurrent replay | mitigate | 条件 update 与终态 no-op |
| T-143-04 | Elevation of Privilege | writer boundary | mitigate | INV-6 继续只允许 CaptureService 写 |
| T-143-SC | Tampering | package installs | accept | 无安装任务 |
</threat_model>

<verification>
运行模型字段 pytest（闭集档位、max_length、additive default、存量 pending_eval 原值）、migration plan、makemigrations check、状态机测试和 INV-6 守卫；不得删除或回填既有 Capture。
</verification>

<success_criteria>
- 数据库行完整表达 pending/processing/failure/terminal 状态与独立 retry 元数据。
- legacy evaluated 安全保留但不参与新状态流。
</success_criteria>

<output>
Create `.planning/phases/143-eval/143-03-SUMMARY.md` when done.
</output>
