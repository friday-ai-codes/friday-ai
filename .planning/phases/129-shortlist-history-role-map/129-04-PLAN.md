---
phase: 129-shortlist-history-role-map
plan: 04
type: execute
wave: 2
depends_on:
  - 129-01
  - 129-02
  - 129-03
files_modified:
  - server/services/process_runtime/blueprint_route.py
  - server/initiatives/services/repo_association_service.py
  - server/services/process_runtime/stage_sandbox.py
  - server/tests/services/process_runtime/test_funnel_shortlist.py
  - server/tests/services/process_runtime/test_funnel_team_gate.py
autonomous: true
requirements:
  - LIST-01
  - LIST-02
  - LIST-03
  - LIST-04
  - ROLE-01
  - ROLE-02
  - ROLE-03
user_setup: []

must_haves:
  truths:
    - "Blueprint 漏斗在 team_gate 通过后调用 shortlist（含历史 force-include）与 role_map，stage 结果携带二者"
    - "后续融合/细排候选硬限制在 shortlist；primary 不出现 out_of_team 或 shortlist 外仓"
    - "RepoAssociation/MCP 漏斗路径至少将候选收窄到 shortlist（或等价 team 内 shortlist）；不回退静默全库"
    - "未改 RepoRouterV2 内核；观测无需求原文"
  artifacts:
    - path: server/services/process_runtime/blueprint_route.py
      provides: "funnel wiring shortlist+role_map"
    - path: server/tests/services/process_runtime/test_funnel_shortlist.py
      provides: "接线与候选 ⊆ shortlist 守卫"
  key_links:
    - from: BlueprintRouteAdapter.route
      to: build_shortlist / build_role_map
      via: "team_gate → shortlist(+history_prior) → role_map → 融合"
      pattern: "build_shortlist|build_role_map|placement_defaults|shortlist_count"
---

<objective>
把 shortlist、历史先验 force-include、角色图接到 Blueprint 漏斗（及 association/MCP 候选收窄），并加接线测试（LIST+ROLE 端到端；D-01、D-03、D-15、D-16）。

Purpose: 使决策漏斗中段在主路径生效，为 Phase 130 提供 shortlist+角色约束。
Output: Adapter/入口接线 + `test_funnel_shortlist.py`；128 漏斗测不回归。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/129-shortlist-history-role-map/129-CONTEXT.md
@.planning/phases/129-shortlist-history-role-map/129-RESEARCH.md
@.planning/phases/128-initiative-profile-team-gate/128-03-SUMMARY.md
@server/services/process_runtime/shortlist.py
@server/services/process_runtime/history_prior.py
@server/services/process_runtime/role_map.py
@server/services/process_runtime/blueprint_route.py
@server/initiatives/services/repo_association_service.py
@server/services/process_runtime/stage_sandbox.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — 漏斗接线守卫测</name>
  <files>server/tests/services/process_runtime/test_funnel_shortlist.py</files>
  <behavior>
    - mock team_gate 通过后，Adapter.route（或抽测的编排函数）结果含 shortlist 与 role_map/placement_defaults
    - 融合候选 repository_id 集合 ⊆ shortlist ids
    - history force-include 仓（team_core 内、能力分 0）出现在 shortlist
    - role_map clarify(unmapped_role) 时路由 status=clarify 且 candidates 不塞全库 top-k
    - 可选：断言未 import/修改 V2 grouping 语义（或文件 mtime/守卫：grouping 仍 annotate-only）
  </behavior>
  <action>
    写 RED 接线测；复用 128 funnel 测试风格（stub router/charter/history）。覆盖 D-15。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/process_runtime/test_funnel_shortlist.py -q --tb=line 2>&1 | head -40</automated>
  </verify>
  <done>RED 就位。</done>
</task>

<task type="auto">
  <name>Task 2: GREEN — Adapter 与入口接线</name>
  <files>server/services/process_runtime/blueprint_route.py, server/initiatives/services/repo_association_service.py, server/services/process_runtime/stage_sandbox.py, server/tests/services/process_runtime/test_funnel_shortlist.py, server/tests/services/process_runtime/test_funnel_team_gate.py</files>
  <action>
    在 BlueprintRouteAdapter.route：team_gate 通过后调用 asplit_history_priors → build_shortlist(..., force_include_ids=...) → build_role_map；将 shortlist/role_map/placement_defaults 写入返回/观测。后续 V2/融合候选硬限制 ⊆ shortlist。role_map clarify → 返回 clarify 载荷（沿用 128 形状，additive）。RepoAssociation 与 stage_sandbox：在已有 team_core 范围内应用 shortlist 收窄（至少 primary/候选不超出 shortlist）；discretion：可不在这两入口写满 role_map，但不得回退全库。保持裸 V2 直调兼容。不修改 repo_router_v2.py。日志遵守 D-16。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/process_runtime/test_funnel_shortlist.py tests/services/process_runtime/test_funnel_team_gate.py tests/services/process_runtime/test_shortlist.py tests/services/process_runtime/test_history_prior.py tests/services/process_runtime/test_role_map.py -q --tb=short --reuse-db</automated>
  </verify>
  <done>接线测与 Wave1 模块测全绿；128 funnel 不回归。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP / Blueprint / Association 入口 | 用户触发的路由请求 |
| shortlist → primary | 候选宇宙收窄 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-129-09 | Elevation of Privilege | funnel primary | mitigate | primary ∈ shortlist ⊆ team_core∪合法 adjacent |
| T-129-10 | Information Disclosure | stage payload / logs | mitigate | 观测不含需求全文；计数与 reason 即可 |
| T-129-11 | Denial of Service | V2 within shortlist | accept | 候选已收窄；沿用既有超时/fail-soft |
| T-129-SC | Tampering | pip installs | accept | 无新包 |
</threat_model>

<verification>
funnel_shortlist + Wave1 模块 + funnel_team_gate 相关 pytest 全绿。
</verification>

<success_criteria>
主路径具备可解释 shortlist + 角色图；候选不逃逸；V2 未重写。
</success_criteria>

<output>
Create `.planning/phases/129-shortlist-history-role-map/129-04-SUMMARY.md` when done
</output>
