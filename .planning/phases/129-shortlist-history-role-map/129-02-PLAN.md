---
phase: 129-shortlist-history-role-map
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - server/services/process_runtime/history_prior.py
  - server/services/process_runtime/blueprint_route_history.py
  - server/tests/services/process_runtime/test_history_prior.py
autonomous: true
requirements:
  - LIST-03
user_setup: []

must_haves:
  truths:
    - "历史先验拆需求史 tech_plan 与上线史 document/code_change，并可分别归因到仓库"
    - "与 team_core 求交后产出 force_include ids 及 reason（history_demand|history_launch）"
    - "无 actor/检索失败 fail-soft 带 unavailable_reason，不抛垮；日志无召回正文"
  artifacts:
    - path: server/services/process_runtime/history_prior.py
      provides: "asplit_history_priors / force_include_from_history"
      exports: ["asplit_history_priors", "HistoryPriorResult"]
    - path: server/tests/services/process_runtime/test_history_prior.py
      provides: "LIST-03 分桶与求交单测"
  key_links:
    - from: asplit_history_priors
      to: team_core
      via: "force_include_ids = hits ∩ team_core"
      pattern: "history_demand|history_launch|asplit_history_priors"
---

<objective>
演进历史先验：拆分需求史与上线史，并与 team_core 求交生成 shortlist 可消费的 force-include 集合（LIST-03；D-05）。

Purpose: 让「上次同类需求/上线落点」能在能力树分低时仍进入短名单。
Output: `history_prior.py`（可薄封装既有 `ascore_history_match`）+ 必要演进 `blueprint_route_history.py` + 单测。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/129-shortlist-history-role-map/129-CONTEXT.md
@.planning/phases/129-shortlist-history-role-map/129-RESEARCH.md
@server/services/process_runtime/blueprint_route_history.py
@server/services/process_runtime/shortlist.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — 历史分桶与求交单测</name>
  <files>server/tests/services/process_runtime/test_history_prior.py</files>
  <behavior>
    - mock 检索命中：tech_plan→仓 A、document→仓 B、code_change→仓 C；asplit 标记 A=history_demand，B/C=history_launch
    - team_core={A,B} 时 force_include 仅含 A,B；C（out of team）剔除
    - no_acting_user / retrieval_error → force_include 空列表 + unavailable_reason 透出，不抛异常
  </behavior>
  <action>
    写 RED 测试；mock DeliveryKnowledgeSearchService 或注入检索层，避免真网。覆盖 D-05、LIST-03。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/process_runtime/test_history_prior.py -q --tb=line 2>&1 | head -40</automated>
  </verify>
  <done>RED 用例就位。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — 实现 history prior 分桶 API</name>
  <files>server/services/process_runtime/history_prior.py, server/services/process_runtime/blueprint_route_history.py, server/tests/services/process_runtime/test_history_prior.py</files>
  <action>
    新增 `asplit_history_priors`（或等价）：复用/演进 `ascore_history_match` 检索，按 entity kind 分桶 demand=`tech_plan`、launch=`document|code_change`；对每个命中仓记 top_score/hit_count；`force_include_ids` = 过阈值命中 ∩ team_core；返回 per_repo reasons。若需在 history 模块暴露 kind 级明细，additive 扩展 `HistoryMatchResult`（不破坏既有 score 字段）。观测：history_prior_started/completed/failed；RetrievalTrace 只记条数/耗时/score；禁止正文。阈值常数放模块顶并单测可覆盖。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/process_runtime/test_history_prior.py tests/services/process_runtime/test_blueprint_route_breakdown.py -q --tb=short --reuse-db</automated>
  </verify>
  <done>LIST-03 绿；既有 history breakdown 不回归。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| knowledge retrieval | 用户权限作用域内的历史实体 |
| shortlist 消费 force_include | 仅 team_core 交集 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-129-03 | Information Disclosure | history prior / RetrievalTrace | mitigate | 只记指标与 citation id；无 query/正文 |
| T-129-04 | Elevation of Privilege | no_acting_user | mitigate | fail-closed：不伪造 actor（沿用 T-112-10） |
| T-129-05 | Spoofing | force_include out_of_team | mitigate | 强制 ∩ team_core |
| T-129-SC | Tampering | pip installs | accept | 无新包 |
</threat_model>

<verification>
history_prior 与既有 breakdown 相关测全绿。
</verification>

<success_criteria>
需求史/上线史可分桶；与 team_core 求交 force-include；失败可观测不阻断。
</success_criteria>

<output>
Create `.planning/phases/129-shortlist-history-role-map/129-02-SUMMARY.md` when done
</output>
