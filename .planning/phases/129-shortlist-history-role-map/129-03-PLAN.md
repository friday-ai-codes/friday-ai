---
phase: 129-shortlist-history-role-map
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - server/services/process_runtime/role_map.py
  - server/tests/services/process_runtime/test_role_map.py
autonomous: true
requirements:
  - ROLE-01
  - ROLE-02
  - ROLE-03
user_setup: []

must_haves:
  truths:
    - "对 shortlist 逐仓映射固定四角色枚举并产出 primary|supporting|forbidden"
    - "触碰 boundaries 的仓不得作该角色 primary，除非带显式 override 理由"
    - "无法映射的章程拥有域 → clarify(unmapped_role)；导出 placement_defaults（状态域写方 ≠ app_shell primary）"
  artifacts:
    - path: server/services/process_runtime/role_map.py
      provides: "Role enum + build_role_map + placement_defaults"
      exports: ["RepoRole", "build_role_map", "PLACEMENT_DEFAULTS"]
    - path: server/tests/services/process_runtime/test_role_map.py
      provides: "ROLE-01/02/03 单测"
  key_links:
    - from: build_role_map
      to: placement_defaults
      via: "learning_state 默认写方约束禁止任意 app_shell primary"
      pattern: "app_shell|practice_reuse_host|course_config|learning_state|unmapped_role|placement_defaults"
---

<objective>
交付章程角色图：固定小枚举主/辅/禁赋值、boundary 降级、不可映射 clarify，并导出放置默认约束（ROLE-01/02/03；D-02、D-11~D-14）。

Purpose: 为 Phase 130 放置单元提供角色约束；本 plan 不接线 Adapter。
Output: `role_map.py` + 单测。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/129-shortlist-history-role-map/129-CONTEXT.md
@.planning/phases/129-shortlist-history-role-map/129-RESEARCH.md
@.planning/milestones/v0.23.0-DECISIONS.md
@server/services/process_runtime/blueprint_charter_match.py
@server/services/process_runtime/blueprint_route.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — 角色图与边界单测</name>
  <files>server/tests/services/process_runtime/test_role_map.py</files>
  <behavior>
    - 四仓合成章程分别贴近 App 壳 / 做题复用宿主 / 课程配置 / 学习状态 → 四角色各有 primary
    - 某仓 violated_boundaries 非空且无 override → assignment 不得为 primary（forbidden 或 supporting+demoted）
    - 带 override_reasons[repo_id]=非空理由时可保留非 forbidden（与 resolve_boundary_override 语义对齐）
    - 章程 owned domain 无法映射到四枚举 → status=clarify, clarify_reason=unmapped_role
    - placement_defaults 断言含「learning_state 写方不得默认 app_shell primary」类布尔/规则键
  </behavior>
  <action>
    写 RED 用例；对照表用领域关键词而非真实仓 UUID（D-14）。引用 D-02/D-11~D-13、ROLE-01/02/03。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/process_runtime/test_role_map.py -q --tb=line 2>&1 | head -40</automated>
  </verify>
  <done>RED 就位。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — 实现 role_map 模块</name>
  <files>server/services/process_runtime/role_map.py, server/tests/services/process_runtime/test_role_map.py</files>
  <action>
    实现 `RepoRole` 枚举（恰好四值，per D-02/D5）、启发式 domain→role 对照、`build_role_map(shortlist_repos, charters_by_repo, profile=None, override_reasons=None)`。输出 RoleMapResult：roles 聚合、per_repo、placement_defaults。boundary：复用 score_charter_match 的 violated_boundaries 或入参；无 override → 禁止 primary。观测 role_map_started/completed/failed，category=sampling，component=process_runtime，无需求原文。不实现放置算法。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/process_runtime/test_role_map.py -q --tb=short --reuse-db</automated>
  </verify>
  <done>ROLE-01/02/03 全绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| charter domains → role | 映射表可能误标角色 |
| override_reasons | 调用方提供的显式理由 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-129-06 | Tampering | override_reasons | mitigate | 无理由不得 primary；理由写入 evidence（脱敏/截断） |
| T-129-07 | Repudiation | role assignment | mitigate | per_repo evidence + charter_source/version 进结果 |
| T-129-08 | Information Disclosure | role_map logs | mitigate | 无需求原文；只记角色计数/clarify_reason |
| T-129-SC | Tampering | pip installs | accept | 无新包 |
</threat_model>

<verification>
`test_role_map.py` 全绿。
</verification>

<success_criteria>
四角色图可测；boundary 门禁生效；placement_defaults 可导出。
</success_criteria>

<output>
Create `.planning/phases/129-shortlist-history-role-map/129-03-SUMMARY.md` when done
</output>
