---
phase: 21-observability
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - server/tests/workflows/test_trigger_sync.py
  - server/tests/workflows/test_trigger_type_choices.py
  - server/tests/workflows/test_hooks.py
  - server/tests/test_trigger_dispatcher.py
autonomous: true
requirements: [TRIG-01, TRIG-02, TRIG-03, OBS-01]
must_haves:
  truths:
    - "保存含单数 event_type 的 feishu_event_trigger 节点后 async_sync_workflow_triggers 生成 WorkflowTrigger（测试断言）"
    - "仅含历史复数 event_types 的节点同步后仍生成 trigger（兜底回归测试）"
    - "dispatch 异常时 TriggerLog.status=error 且 error_message 非空（测试断言）"
    - "WebSocketBroadcastHook 在 node 失败时广播 message 含 error_message/error_code（测试断言）"
  artifacts:
    - path: "server/tests/workflows/test_trigger_sync.py"
      provides: "TRIG-01 同步/兜底/filter_config/端到端匹配 RED 测试"
      contains: "def test_"
    - path: "server/tests/workflows/test_trigger_type_choices.py"
      provides: "TRIG-02 trigger_type choices 断言"
      contains: "schedule"
    - path: "server/tests/test_trigger_dispatcher.py"
      provides: "TRIG-03 dispatch 失败持久化断言（扩展）"
      contains: "error"
  key_links:
    - from: "test_trigger_sync.py"
      to: "async_sync_workflow_triggers"
      via: "import + 调用"
      pattern: "async_sync_workflow_triggers"
    - from: "test_hooks.py"
      to: "WebSocketBroadcastHook"
      via: "mock channel_layer.group_send 断言 message dict"
      pattern: "group_send"
---

<objective>
建立 Phase 21 后端验证基建（Wave 0）：为 TRIG-01 / TRIG-02 / TRIG-03 / OBS-01（后端侧）创建/扩展 pytest 测试，断言**修复后**的目标行为。这些测试在本计划提交时应为 RED（实现尚未落地），由 Wave 1 实现计划转 GREEN。

Purpose: 给后续实现任务提供可运行的 `<automated>` 验证锚点，满足 Nyquist 规则；锁死契约（单数 event_type 同步、复数兜底、filter_config 形态、dispatch 失败落库、WS 广播失败字段）。
Output: 2 个新测试文件 + 2 个既有测试文件扩展。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/21-observability/21-RESEARCH.md
@.planning/phases/21-observability/21-PATTERNS.md
@.planning/phases/21-observability/21-VALIDATION.md
@server/tests/workflows/conftest.py
@server/tests/test_trigger_dispatcher.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 新建 test_trigger_sync.py — TRIG-01 同步/兜底/filter_config/端到端匹配</name>
  <files>server/tests/workflows/test_trigger_sync.py</files>
  <read_first>
    - server/workflows/api/views.py L96-166（`async_sync_workflow_triggers` 现状根因，被测目标）
    - server/workflows/models/trigger.py L104-166（`matches_event` / `_matches_filter` / `_get_nested_value` 匹配契约——端到端断言依据）
    - server/tests/workflows/conftest.py（既有 fixture：workflow/node 构造范式）
    - 21-PATTERNS.md L38-106（Pattern 1 + 匹配侧契约 + filter_config 形态）
    - 21-RESEARCH.md L388-397（端到端验证锚点伪代码）
  </read_first>
  <action>
    用 `@pytest.mark.django_db(transactional=False)` + `pytest.mark.asyncio` 创建测试，覆盖（per D-01）：
    (1) `test_singular_event_type_creates_trigger`：构造 workflow + 一个 `node_type="feishu_event_trigger"` 节点，`config={"event_type":"WorkitemStatusEvent","filter_status":["s1"]}`，await `async_sync_workflow_triggers(workflow)`，断言生成 1 条 `WorkflowTrigger`，`event_type=="WorkitemStatusEvent"`、`is_active is True`、`filter_config["cur_work_item_status.state_key"]==["s1"]`。
    (2) `test_legacy_event_types_array_fallback`：节点 `config={"event_types":["WorkitemStatusEvent"]}`（仅复数历史键），断言同步后仍生成 trigger（Pitfall 1 兜底）。
    (3) `test_filter_config_maps_project_and_work_item`：`config` 含 `filter_project_key`/`filter_work_item_type`，断言 `filter_config["project_key"]` / `filter_config["work_item_type_key"]` 正确写入。
    (4) `test_e2e_match`：同步生成 trigger 后，构造 payload `{"cur_work_item_status":{"state_key":"s1"}}`，断言 `trigger.matches_event("WorkitemStatusEvent", payload) is True`；并断言不匹配的 state_key 返回 False。
    (5) `test_exclude_and_project_ids_not_in_filter_config`：节点 config 含 `project_ids`/`exclude_project_ids`/`exclude_work_item_pattern`，断言这些键**不**出现在生成的 `filter_config` 中（OQ#1 裁定：负向/Space UUID 字段本阶段不正向同步，禁止静默误匹配）。
    使用 ORM 异步写入走 `sync_to_async` 或 `acreate`，遵循 conftest 既有范式。中文注释说明每个断言对应的需求点。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/workflows/test_trigger_sync.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/workflows/test_trigger_sync.py --collect-only` 列出 ≥5 个测试用例（collection 成功，无 import error）。
    - 测试在实现前为 RED（失败原因是当前 `async_sync_workflow_triggers` 读复数 → trigger 未生成），非 collection error。
    - `test_exclude_and_project_ids_not_in_filter_config` 编码 OQ#1 裁定（负向字段不入 filter_config）。
  </acceptance_criteria>
  <done>test_trigger_sync.py 存在且可被 pytest 收集，编码 TRIG-01 全部目标断言含 OQ#1 约束。</done>
</task>

<task type="auto">
  <name>Task 2: 新建 test_trigger_type_choices.py（TRIG-02）+ 扩展 test_trigger_dispatcher.py（TRIG-03）</name>
  <files>server/tests/workflows/test_trigger_type_choices.py, server/tests/test_trigger_dispatcher.py</files>
  <read_first>
    - server/workflows/models/workflow.py L30-34（`TriggerType` 现状，含 SCHEDULE）
    - server/feishu/models.py L14-20（`TriggerLogStatus` 枚举）、L71-76（status/error_message 字段）
    - server/feishu/views.py L693-731（`_dispatch_to_workflows` 现状，失败/无匹配恒 ACCEPTED）
    - server/tests/test_trigger_dispatcher.py L29-55（mock_engine / clear_registry autouse fixture 范式）、L171-189（无匹配断言范式）
    - 21-PATTERNS.md L150-194（Pattern 3）、L508-524（测试 fixture 范式）
  </read_first>
  <action>
    (A) test_trigger_type_choices.py（轻量，per D-02）：
    - `test_trigger_type_choices_exclude_schedule`：断言 `"schedule" not in [c[0] for c in Workflow.TriggerType.choices]`。
    - `test_trigger_type_choices_keep_others`：断言 manual/webhook/event 仍在 choices。
    (B) 扩展 test_trigger_dispatcher.py（per D-03，飞书路径失败持久化）：新增针对 `FeishuEventView._dispatch_to_workflows` 的测试类/函数（用 AsyncMock 注入 dispatcher 行为，构造真实 `TriggerLog` 行）：
    - `test_dispatch_exception_sets_triggerlog_error`：mock `TriggerDispatcher.dispatch` 抛异常，断言关联 `trigger_log.status == TriggerLogStatus.ERROR` 且 `error_message` 非空且长度 ≤2000。
    - `test_dispatch_no_match_sets_triggerlog_ignored`：mock `dispatch` 返回 `[]`，断言 `trigger_log.status == TriggerLogStatus.IGNORED` 且 `error_message` 含 event_type。
    复用同文件既有 fixture 风格（AsyncMock + registry 清理）；若直接测 `_dispatch_to_workflows` 需要 view 实例，使用最小构造或 monkeypatch `TriggerDispatcher`。中文注释标注 TRIG-02/TRIG-03。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/workflows/test_trigger_type_choices.py tests/test_trigger_dispatcher.py -k "schedule or fail or error or ignored" -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest --collect-only` 对两文件均成功，新增用例可被收集。
    - TRIG-02 choices 测试在实现前为 RED（当前 choices 含 schedule）。
    - TRIG-03 失败持久化测试断言 `status` ∈ {error, ignored} 且 `error_message` 非空，在实现前为 RED（当前恒 ACCEPTED）。
  </acceptance_criteria>
  <done>两测试文件可收集，编码 TRIG-02 choices 断言与 TRIG-03 飞书路径失败/无匹配持久化断言。</done>
</task>

<task type="auto">
  <name>Task 3: 扩展 test_hooks.py — OBS-01 WebSocketBroadcastHook 失败广播字段</name>
  <files>server/tests/workflows/test_hooks.py</files>
  <read_first>
    - server/workflows/hooks/builtin.py L38-78（`WebSocketBroadcastHook`，被测目标；L55-65 message 组装）、L313-317（AlertRuleHook 读 DB 对象，证明追加可选键不影响）
    - server/tests/workflows/test_hooks.py（既有测试结构 + channel_layer mock 范式，若已有 broadcast 测试则在其旁扩展）
    - server/workflows/models/execution.py L437-515（NodeExecution.error_message/error_code 字段）
    - 21-PATTERNS.md L198-237（Pattern 4 后端改点 + 契约一致性）
  </read_first>
  <action>
    在 test_hooks.py 扩展（per D-04 后端侧）：mock `channels.layers.get_channel_layer` 返回对象，其 `group_send` 为 AsyncMock，触发 `WebSocketBroadcastHook` 的 node 失败事件（`node_execution.status="failed"`，`error_message="变量解析失败"`，`error_code="VAR_RESOLUTION_FAILED"`）：
    - `test_broadcast_failed_node_includes_error_fields`：断言 `group_send` 调用的 message dict 含 `error_message=="变量解析失败"` 且 `error_code=="VAR_RESOLUTION_FAILED"`。
    - `test_broadcast_success_node_omits_error_fields`：node 状态非失败（如 completed）时断言 message dict **不含** `error_message`/`error_code` 键（Pitfall 5 向后兼容：仅失败态追加可选键）。
    - `test_broadcast_timeout_node_includes_error_fields`：status="timeout" 同样附带 error 字段。
    复用既有 hook 调用方式（grep 既有 broadcast 测试或 hook 的 on_node_failed/事件入口签名作模板）。中文注释标注 OBS-01。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/workflows/test_hooks.py -k "broadcast and error" -x</automated>
  </verify>
  <acceptance_criteria>
    - 新增 3 个用例可被 `pytest --collect-only` 收集。
    - 失败态用例在实现前为 RED（当前 message dict 不含 error_message/error_code）。
    - 成功态用例编码"仅失败态追加可选键"的向后兼容契约。
  </acceptance_criteria>
  <done>test_hooks.py 含 OBS-01 失败广播字段断言（失败态含、成功态不含）。</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Type | Requirement |
|----------|------|-------------|
| `server/tests/workflows/test_trigger_sync.py` | 新建测试 | TRIG-01 |
| `server/tests/workflows/test_trigger_type_choices.py` | 新建测试 | TRIG-02 |
| `server/tests/workflows/test_hooks.py`（扩展） | 测试 | OBS-01（后端） |
| `server/tests/test_trigger_dispatcher.py`（扩展） | 测试 | TRIG-03 |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 测试夹具 → ORM | 测试写入 TriggerLog/WorkflowTrigger，使用 ORM 非裸 SQL（V5） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01-01 | Information Disclosure | TriggerLog.error_message 断言夹具 | mitigate | 测试断言 error_message 长度 ≤2000（编码 ASVS V7 截断契约），不引入真实凭证/输出值 |
| T-21-01-SC | Tampering | 无 package 安装 | accept | 本计划零依赖新增（纯测试），无供应链面 |
</threat_model>

<verification>
- `cd server && uv run pytest tests/workflows/test_trigger_sync.py tests/workflows/test_trigger_type_choices.py tests/workflows/test_hooks.py tests/test_trigger_dispatcher.py --collect-only` 全部收集成功（无 import/collection error）。
- 新增/扩展用例在实现前为 RED（断言修复后行为），这是 Wave 0 预期。
</verification>

<success_criteria>
- 4 个测试文件可被 pytest 收集，编码 TRIG-01/02/03 + OBS-01 后端目标契约。
- 无 collection error；RED 失败为断言失败（非语法/导入错误）。
</success_criteria>

<output>
Create `.planning/phases/21-observability/21-01-SUMMARY.md` when done.
</output>
