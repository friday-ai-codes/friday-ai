---
phase: 75-dashboard-ui
plan: "75-04"
type: execute
wave: 2
depends_on: ["75-01"]
files_modified:
  - web/src/pages/admin/observability/logs.vue
  - web/src/components/observability/QueueCountersBar.vue
  - web/src/components/observability/SystemLogTable.vue
  - web/src/components/observability/LogDrilldownSheet.vue
  - web/src/components/observability/RuntimeLogConfigForm.vue
  - web/src/components/observability/__tests__/logs.spec.ts
autonomous: true
requirements: [UI-04]
user_setup: []

must_haves:
  truths:
    - "系统日志页 logs.vue（/admin/observability/logs，requiresAdmin）：顶部 ObservabilityTabs（高亮日志）+ 四计数 bar + 倒序日志列表 + 多维筛选 + 下钻 sheet + 按筛选清理 + 运行时日志配置表单"
    - "四计数 bar（QueueCountersBar）：队列 x/5000(进度条) · 写入 · 丢弃(>0 琥珀) · 失败(>0 红)，tabular；消费 querySystemLogs 返回的 counters"
    - "日志列表（SystemLogTable）倒序 + 多维筛选（时间段/级别/组件/user_id/source/关键词；call_source/provider/credential/model/关联键经 keyword 或 payload 兜底）+ 分页 + message 截断展开；消费 querySystemLogs"
    - "调用下钻 sheet（LogDrilldownSheet）：会话原始（getConversationDrilldown：messages/related_logs/related_runs）/ 调用明细（getCallDrilldown：tool_calls/retrieval/model_usages/events）/ webhook 原始（getWebhookEvent 脱敏 headers/raw_body），tab 切换；全部 pre 文本渲染禁 v-html"
    - "按当前筛选清理（destructive 按钮 + confirm dialog 明示删除范围/不可逆）调 clearSystemLogs（无筛选时要求 confirm_all）+ 运行时日志配置表单（级别/分组件级别/堆栈阈值/采样初始·后续/保留天数·大小，保存并生效/回滚默认）经 settings.ts getSetting/updateSetting 读写 SettingKeys.LOG_*"
  artifacts:
    - path: "web/src/pages/admin/observability/logs.vue"
      provides: "系统日志页（UI-04），definePage requiresAdmin，组合四计数 + 列表 + 筛选 + 下钻 + 清理 + 配置"
      contains: "requiresAdmin"
    - path: "web/src/components/observability/QueueCountersBar.vue"
      provides: "四计数 bar（队列 x/5000 进度 · 写入 · 丢弃 · 失败）"
      contains: "QueueCountersBar"
    - path: "web/src/components/observability/SystemLogTable.vue"
      provides: "倒序日志列表 + 多维筛选 + 分页 + message 展开 + 行点击下钻"
      contains: "querySystemLogs"
    - path: "web/src/components/observability/LogDrilldownSheet.vue"
      provides: "下钻抽屉（会话原始/调用明细/webhook 原始 三 tab）"
      contains: "LogDrilldownSheet"
    - path: "web/src/components/observability/RuntimeLogConfigForm.vue"
      provides: "运行时日志配置表单（LOG_* 读写 + 保存生效/回滚默认）"
      contains: "RuntimeLogConfigForm"
  key_links:
    - from: "web/src/components/observability/SystemLogTable.vue"
      to: "web/src/api/system.ts"
      via: "querySystemLogs 拉日志 + counters"
      pattern: "querySystemLogs"
    - from: "web/src/components/observability/LogDrilldownSheet.vue"
      to: "web/src/api/system.ts"
      via: "getConversationDrilldown / getCallDrilldown / getWebhookEvent"
      pattern: "Drilldown|getWebhookEvent"
    - from: "web/src/components/observability/RuntimeLogConfigForm.vue"
      to: "web/src/api/settings.ts"
      via: "getSetting/updateSetting 读写 SettingKeys.LOG_*"
      pattern: "getSetting|updateSetting"
---

<objective>
实现系统日志页（UI-04）：`/admin/observability/logs` 子路由页，含顶部四计数 bar、倒序日志列表 + 多维筛选、调用下钻抽屉（会话原始/调用明细/webhook 原始）、按筛选清理（二次确认）与运行时日志配置表单（实时生效）。消费 75-01 封装的 `querySystemLogs` / `clearSystemLogs` / 下钻 / webhook 函数与既有 `settings.ts` 的 LOG_* 读写，实现到 75-UI-SPEC §5。

Purpose: 把 Phase 71 的日志中心（队列化落库 + 可搜索 + 可清理 + 运行时可配 + 调用下钻 + webhook 留痕）落成超管可用的人机界面，闭环可观测性"排障日志"链路。
Output: `logs.vue` + `components/observability/` 下 QueueCountersBar / SystemLogTable / LogDrilldownSheet / RuntimeLogConfigForm + 组件单测。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/75-dashboard-ui/75-CONTEXT.md
@.planning/phases/75-dashboard-ui/75-UI-SPEC.md
@.planning/observability/REFERENCE-UI.md
@.cursor/rules/observability-logging.mdc

# 75-01 产出（直接消费）
@web/src/api/system.ts        # querySystemLogs/SystemLogRow/SystemLogResult/SystemLogQuery + clearSystemLogs + getCallDrilldown/getConversationDrilldown/listWebhookEvents/getWebhookEvent + CallDrilldown/ConversationDrilldown/WebhookEventRow
@web/src/components/observability/ObservabilityTabs.vue
@web/src/components/observability/ObservabilityTimeRange.vue
@web/src/components/observability/format.ts     # formatClock/formatDateTime/formatNumber
@web/src/components/observability/status.ts      # logLevelClass

# 既有 settings API（运行时日志配置复用，本 plan 不新封装端点）
@web/src/api/settings.ts      # getSetting/updateSetting(PUT /settings/<key>/)/listSettings + SettingKey 类型
# 后端契约（执行时精读）
@server/system/log_views.py   # 查询入参(component/level/user_id/source/start/end/keyword/limit/offset)+{items,total,counters}；清理 body(同筛选+confirm_all)→{deleted}，无筛选必须 confirm_all
@server/system/serializers.py # SystemLogEntrySerializer 字段（含 payload/correlation——call_source/provider/model/关联键在 payload/correlation 内）
@server/system/drilldown_views.py # 下钻返回结构（call: run/user/tool_calls/retrieval/model_usages/events；conversation: conversation/created_by/messages/related_logs/related_runs）
@server/system/webhook_views.py   # webhook 列表/详情（headers/raw_body 已脱敏）
@server/system/models.py      # SettingKeys.LOG_LEVEL/LOG_COMPONENT_LEVELS/LOG_STACK_THRESHOLD/LOG_SAMPLING_INITIAL/LOG_SAMPLING_RATE/LOG_RETENTION_DAYS/LOG_RETENTION_SIZE（点分 key 名 + 默认值注释）

# 前端范式（执行时精读）
@web/src/pages/admin/observability/index.vue  # 既有日志区呈现 + level 筛选 + fmtLogTime + logLevelClass 范式（迁到 status.ts 复用）
@web/src/pages/admin/audit/index.vue          # 列表 + 筛选 + 分页范式
@web/src/components/ui/table/index.ts + sheet/select/input/switch/dialog/alert-dialog/pagination/progress/collapsible/tabs/sonner/scroll-area
</context>

<tasks>

<task type="auto">
  <name>Task 1: 四计数 bar + 日志列表 + 多维筛选（QueueCountersBar / SystemLogTable）</name>
  <files>web/src/components/observability/QueueCountersBar.vue, web/src/components/observability/SystemLogTable.vue</files>
  <action>
实现日志呈现核心（Vue 3 `<script setup lang="ts">`，中文，lucide 无 emoji，tabular-nums，亮暗 token，骨架/空态；UI-SPEC §5.1–§5.2）：

1. **`QueueCountersBar.vue`**（UI-SPEC §5.1）：
   - props：`counters: Record<string, number> | null`（来自 querySystemLogs 的 `counters`，对齐 log_sink.snapshot_counters 键：执行时按后端核对实际键名，常见 `queue_size`/`enqueued`/`dropped`/`failed`/`sampled_out`；用宽松取键 + 兜底 0）。
   - 四计数卡/徽标：队列 `queue_size`/5000（`ui/progress` 进度条 + `x/5000`）· 写入 `enqueued` · 丢弃 `dropped`（>0 琥珀）· 失败 `failed`（>0 红）。tabular-nums。空/loading→骨架。键缺失显示 0 不报错。
2. **`SystemLogTable.vue`**（UI-SPEC §5.2，自取数）：
   - 用 @tanstack/vue-query 调 `querySystemLogs`（keepPreviousData），内部维护筛选 + 分页 state（watch refetch）；返回的 `counters` 经 emit `counters`（QueueCountersBar 由页面消费）或 props 回调，让页面顶部 bar 与列表同源刷新（注释抉择，建议 emit counters）。emit `rowClick`（SystemLogRow）供下钻。
   - 多维筛选条：时间段（`ObservabilityTimeRange` 或 from/to）、级别（Select debug/info/warn/error，注意后端 warn 归一）、组件 component（Select 或 input；选项可取既有 component 清单或自由输入）、user_id（input）、source（Select mcp/chat/compat/rest/webhook/workflow/task/scheduler）、关键词 keyword（input，全文 message__icontains）。**call_source/provider/credential/model/关联键**后端 SystemLogEntry 未做顶层列筛选——这些落在 `payload`/`correlation` 内：UI 提供这些为"关键词"提示或在前端对当前页 payload 客户端过滤（注释说明后端仅 component/level/user_id/source/keyword 顶层筛选；call_source 等用 keyword 兜底或前端按 payload 过滤当前页，**不臆造后端参数**）。
   - 表列（`ui/table`，对齐 UI-SPEC §5.2）：时间(ts→formatClock/DateTime) / 级别(logLevelClass 色徽标) / 组件 / category(caller·sampling 徽标) / user_id / source / 事件(event) / message（截断 + 点击展开 collapsible 或行展开显示完整 message + payload/correlation 摘要）。行 cursor-pointer emit rowClick。
   - 分页（limit/offset，默认 100；上下页 + total）。空态友好 + loading 骨架。量大说明：用分页（虚拟滚动可选，注释；后端已 _MAX_LIMIT=500 上限）。移动端横向滚动/卡片化不溢出。
  </action>
  <verify>
    <automated>cd web && pnpm vue-tsc --noEmit && pnpm exec eslint src/components/observability/QueueCountersBar.vue src/components/observability/SystemLogTable.vue</automated>
  </verify>
  <done>QueueCountersBar（队列 x/5000 进度 + 写入/丢弃(琥珀)/失败(红)，键缺失兜底）+ SystemLogTable（倒序 + 多维筛选 + 分页 + message 展开 + 行点击 + counters emit + 空态骨架）就位；call_source 等 payload 内维度处理方式注释清晰不臆造后端参数；typecheck + eslint 干净。</done>
</task>

<task type="auto">
  <name>Task 2: 下钻 sheet + 运行时配置表单 + 清理 + 页面组装</name>
  <files>web/src/components/observability/LogDrilldownSheet.vue, web/src/components/observability/RuntimeLogConfigForm.vue, web/src/pages/admin/observability/logs.vue, web/src/components/observability/__tests__/logs.spec.ts</files>
  <action>
实现下钻、运行时配置、清理与页面组装（UI-SPEC §5.3–§5.4）：

1. **`LogDrilldownSheet.vue`**（`ui/sheet` + `ui/tabs`，UI-SPEC §5.3）：
   - props：`open: boolean`、`context: { conversationId?: string, runId?: string, requestId?: string, webhookEventId?: number } | null`；emit `update:open`。
   - 三 tab 按 context 可用性渲染：
     - **会话原始**：context.conversationId → `getConversationDrilldown(id)`，展示 conversation 元信息 + created_by + messages（role/content，**`<pre>`/文本，禁 v-html**）+ related_logs / related_runs 摘要列表。
     - **调用明细**：context.runId/requestId → `getCallDrilldown({run_id|request_id})`，展示 run + 触发用户（user.username/fingerprint，**不显 token**）+ tool_calls / retrieval（query+chunk+score）/ model_usages / events（各列表，payload 用 pre 文本）。
     - **webhook 原始**：context.webhookEventId → `getWebhookEvent(id)`，展示 kind/received_at/verified + 脱敏 headers / raw_body（pre 文本）。
   - 用 vue-query 按 context 懒加载（tab 激活才取）；loading 骨架 + 错误/404 友好（ApiError.detail）；空态。a11y：sheet 标题/关闭 aria-label。
2. **`RuntimeLogConfigForm.vue`**（UI-SPEC §5.4，复用 `settings.ts`，**不调新端点**）：
   - 挂载读取 LOG_* 设置：对 `SettingKeys.LOG_LEVEL`/`LOG_COMPONENT_LEVELS`/`LOG_STACK_THRESHOLD`/`LOG_SAMPLING_INITIAL`/`LOG_SAMPLING_RATE`/`LOG_RETENTION_DAYS`/`LOG_RETENTION_SIZE` 逐键 `getSetting(key)`（key 为点分串如 `'log.level'`；若 settings.ts 的 `SettingKey` 类型为受限枚举不含 log.* 则按其 string 形态传入，注释；缺失键回退默认值占位）。
   - 表单字段：全局级别（Select DEBUG/INFO/WARNING/ERROR）/ 分组件级别（key-value 列表编辑，JSON map，如 `{rag:DEBUG}`）/ 堆栈阈值（Select 级别）/ 采样初始（number，首 N 全记）/ 采样后续比例（number 0..1）/ 保留天数（number）/ 保留行数上限（number）+ caller·sampling 说明（采样仅作用 sampling 类，caller 全记——文案注明，对齐规则）。
   - 「保存并生效」：逐键 `updateSetting(key, value)`（值序列化：map→JSON.stringify，数字→String）；后端 SystemSetting post_save signal 即时热更新（无需重启，per LOG-06）。成功 toast（sonner）说明"已实时生效"。「回滚默认」：把各字段重置为内置默认（注释默认值对齐 models.py SettingKeys 注释：level 空→env→INFO、sampling_initial 50、sampling_rate 0.1、retention_days 30、retention_max_rows 1_000_000）并提示需点保存才落库（或直接保存默认，二选一，注释）。异步保存禁用按钮 + spinner。folding（`ui/collapsible`）独立卡。
3. **清理（按当前筛选）**：在 logs.vue（或 SystemLogTable 工具栏）放 destructive 按钮 → `alert-dialog` 二次确认，明示"将删除符合当前筛选条件的日志，不可逆"；无任何筛选时 confirm 文案升级为"清空全部日志"并以 `clearSystemLogs({...filters, confirm_all: true})` 调用（对齐后端防误清：无筛选必须 confirm_all）；有筛选则传筛选条件（不带 confirm_all）。成功 toast 显示 deleted 行数 + 刷新列表/计数（invalidate query）。
4. **`logs.vue`**（页面组装）：
   - `definePage({ meta: { requiresAdmin: true } })`。
   - 顶部：标题栏 + `ObservabilityTabs`（高亮日志）+ `QueueCountersBar`（消费 SystemLogTable emit 的 counters）。
   - 主体：`SystemLogTable`（监听 rowClick → 解析行的 correlation/conversation_id/run_id/request_id 打开 `LogDrilldownSheet`；行内"会话/调用/webhook"下钻入口按可用关联键启用）+ 清理按钮。
   - 底部/折叠：`RuntimeLogConfigForm`。
5. **测试** `web/src/components/observability/__tests__/logs.spec.ts`（vitest + @vue/test-utils，mock `~/api/system` + `~/api/settings`）：(a) QueueCountersBar 渲染 4 计数 + dropped>0 命中琥珀 class；(b) SystemLogTable 选级别 error 以 level=error 调 querySystemLogs；(c) 清理无筛选时以 confirm_all=true 调 clearSystemLogs；(d) LogDrilldownSheet 给 conversationId 调 getConversationDrilldown 并以 pre 渲染 message（断言无 v-html）；(e) RuntimeLogConfigForm 保存调 updateSetting('log.level', ...)。
  </action>
  <verify>
    <automated>cd web && pnpm vue-tsc --noEmit && pnpm exec vitest run src/components/observability/__tests__/logs.spec.ts && pnpm exec eslint src/pages/admin/observability/logs.vue src/components/observability/LogDrilldownSheet.vue src/components/observability/RuntimeLogConfigForm.vue</automated>
  </verify>
  <done>LogDrilldownSheet（会话/调用/webhook 三 tab，pre 文本无 v-html，token 不显）+ RuntimeLogConfigForm（LOG_* 读写 + 保存实时生效 + 回滚默认）+ 按筛选清理（confirm + 无筛选 confirm_all）+ logs.vue 组装就位；typecheck + 单测 + eslint 干净。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 浏览器 → /api/system/logs·/clear·/calls·/conversations·/webhooks + /api/settings/log.* | 后端 IsSuperUser fail-closed；logs.vue requiresAdmin |
| 下钻/webhook 原始内容 | 后端写入时已 redact_for_ledger/redact_secrets_in_text 脱敏，前端只读直出 |
| 按筛选清理日志 | 不可逆批量删除；无筛选须 confirm_all |
| 运行时日志配置写入 | 改全局/分组件级别/采样/保留，实时生效 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-04-01 | Elevation of Privilege | 非 admin 访问日志/下钻/清理/改配置 | mitigate | logs.vue requiresAdmin + 后端全端点 IsSuperUser；清理/配置均经后端鉴权写 |
| T-75-04-02 | Information Disclosure | 下钻/webhook/message 渲染泄漏 token/凭证明文 | mitigate | 仅渲染后端已脱敏字段；token_fingerprint 仅展示哈希不显 token；原始内容用 `<pre>`/文本（**禁 v-html**），杜绝注入与误信任 |
| T-75-04-03 | Denial of Service | 误清空全部日志 | mitigate | 清理二次确认；无筛选时强制 confirm_all（对齐后端防误清）+ 文案明示不可逆 |
| T-75-04-04 | Tampering | 运行时配置被设为屏蔽全部日志/超量保留 | accept | 配置仅超管可改 + 后端 settings_service clamp（采样/级别归一）；属运维授权操作，前端如实提交 |
| T-75-04-SC | Tampering | npm 安装新依赖 | accept | 复用既有 reka-ui/vue-query/lucide/sonner + settings.ts，无新依赖 |

## XSS / secret-in-bundle 注记
**强制无 `v-html`**：日志 message / 下钻 payload / webhook raw_body / messages 全部用 `<pre>` 或文本插值渲染；无密钥进 bundle。
</threat_model>

<verification>
- `cd web && pnpm vue-tsc --noEmit` 全绿。
- `cd web && pnpm exec vitest run src/components/observability/__tests__/logs.spec.ts` 绿。
- `cd web && pnpm exec eslint src/pages/admin/observability/logs.vue src/components/observability` 干净。
- 人工：超管访问 `/admin/observability/logs`，四计数正确 + 倒序 + 多维筛选；行点击下钻三类（会话原始/调用明细/webhook 原始）；按筛选清理二次确认 + 无筛选 confirm_all；运行时配置改 log.level 后新日志即时按新级别（实时生效）；亮暗/响应式无溢出。
</verification>

<success_criteria>
- UI-04：顶部四计数（队列 x/5000·写入·丢弃·失败）+ 倒序 + 多维筛选（级别/组件/user_id/source/关键词，call_source 等 payload 维度兜底）+ 调用下钻（会话原始/召回内容/webhook 原始）+ 按筛选清理（二次确认）+ 运行时日志配置表单（实时生效/回滚默认）。
- 下钻/配置消费 71 drilldown/webhook/retrieval + settings.ts LOG_* 读写；清理对齐后端防误清。
- typecheck + 组件单测 + eslint 全绿；**无 v-html**、无新依赖、无 token/凭证明文渲染；requiresAdmin 守卫。
</success_criteria>

<output>
Create `.planning/phases/75-dashboard-ui/75-04-SUMMARY.md` when done.
</output>
