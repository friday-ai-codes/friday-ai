---
phase: 64-runner
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - runner/go.mod
  - runner/go.sum
  - runner/internal/exec/env.go
  - runner/internal/docker/executor.go
  - runner/internal/config/config.go
  - runner/internal/ws/client.go
  - runner/internal/cmd/run.go
  - runner/internal/cmd/executor_test.go
  - runner/internal/k8s/executor.go
  - runner/internal/k8s/job.go
  - runner/internal/k8s/executor_test.go
autonomous: true
requirements: [RUNNER-01, RUNNER-02]
must_haves:
  truths:
    - "默认/未配置时 runner 仍构造 DockerExecutor，docker 行为零回归（既有 docker executor_test.go 不改且全绿）"
    - "executor.type=kubernetes（或别名 k8s）时 run.go 构造 KubernetesExecutor 而非报错"
    - "KubernetesExecutor.StartContainer 经 k8s API 建 batch/v1 Job，携带 app/friday.task_id/friday.runner label 与 FRIDAY_TASK_* env（复用 BuildContainerEnv，无前缀漂移）"
    - "KubernetesExecutor.WaitContainer 取 Pod terminated exitCode；超时返回 exitCode=-1 且 err=nil（对齐 docker 语义，不与 ws/client.go 调用方分叉）"
    - "KubernetesExecutor.StreamLogs 经 Pods.GetLogs(Follow) 逐行回调 onLine"
    - "task→runner callbackURL 的 host 可配置：docker 模式默认 host.docker.internal 不变，k8s 模式取 FRIDAY_RUNNER_CALLBACK_HOST（runner Pod IP）"
  artifacts:
    - path: "runner/internal/exec/env.go"
      provides: "导出的 BuildContainerEnv 共享 env 装配（docker/k8s 复用）"
      contains: "func BuildContainerEnv"
    - path: "runner/internal/k8s/executor.go"
      provides: "KubernetesExecutor 结构体 + New + StartContainer/WaitContainer/StreamLogs（client-go）"
      contains: "func New"
    - path: "runner/internal/k8s/job.go"
      provides: "buildJobSpec 纯函数 + toEnvVars/sanitize/splitID 助手"
      contains: "func buildJobSpec"
    - path: "runner/internal/k8s/executor_test.go"
      provides: "fake clientset 单测（StartContainer/Wait/StreamLogs/job spec）"
      contains: "fake.NewSimpleClientset"
    - path: "runner/internal/cmd/executor_test.go"
      provides: "executor 选择/类型归一化单测（docker 默认 / kubernetes / k8s 别名 / 未知报错）"
      contains: "resolveExecutorKind"
  key_links:
    - from: "runner/internal/cmd/run.go"
      to: "runner/internal/k8s.New"
      via: "executor.type=kubernetes 分支构造"
      pattern: "k8s\\.New"
    - from: "runner/internal/k8s/executor.go"
      to: "runner/internal/exec.BuildContainerEnv"
      via: "StartContainer env 装配复用"
      pattern: "exec\\.BuildContainerEnv"
    - from: "runner/internal/ws/client.go"
      to: "cfg.CallbackHost"
      via: "callbackURL host 注入"
      pattern: "CallbackHost"
---

<objective>
补齐 RUNNER-01 的"接通"部分并实现 KubernetesExecutor 的核心派发/等待/日志三方法（RUNNER-02 主体的前半），同时修复 k8s 模式下 task→runner 回调地址不可达问题。

Purpose: 让 runner 可在 `executor.type=kubernetes` 下经 k8s API 起 Job 跑任务容器、流式回传日志、等待退出码，并保 docker 默认路径逐字零回归。
Output: 共享 `BuildContainerEnv`、KubernetesExecutor 核心实现（client-go）、executor 选择接通、可配置 callbackURL host，以及 fake-clientset/选择单测。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/64-runner/64-CONTEXT.md
@.planning/phases/64-runner/64-RESEARCH.md
@runner/internal/ws/client.go
@runner/internal/docker/executor.go
@runner/internal/docker/executor_test.go
@runner/internal/cmd/run.go
@runner/internal/config/config.go
@runner/internal/k8s/executor.go
@runner/go.mod
</context>

<interfaces>
契约真相源 `ws.ExecutorService`（`runner/internal/ws/client.go:59-67`，7 方法签名固定，禁改）：
- `StartContainer(ctx, task ws.TaskPayload, callbackURL, callbackToken string) (containerID, answerEndpoint string, err error)`
- `WaitContainer(ctx, containerID string, timeout time.Duration) (exitCode int, logs string, err error)`
- `StreamLogs(ctx, containerID string, onLine func(line string)) error`
- （`ReadContainerFile/RemoveContainer/StartupCleanup/ZombieScan` 由 64-02 实现，本 plan 保留 stub 返回 ErrNotImplemented 以过编译期 `var _ ws.ExecutorService = (*KubernetesExecutor)(nil)`）

关键不变式：`containerID` 在 k8s 下统一 = `"<namespace>/<jobName>"`（确定性、可重 get/list、ZombieScan 比对成立）。
env 装配真相源 `buildContainerEnv`（`docker/executor.go:119-167`，纯函数）——必须复用，禁重写（Pitfall 2：TaskConfig 只认 `FRIDAY_TASK_` 前缀）。
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 加 client-go 依赖 + 抽出共享 BuildContainerEnv（docker 零回归）</name>
  <files>runner/go.mod, runner/go.sum, runner/internal/exec/env.go, runner/internal/docker/executor.go</files>
  <read_first>
    - runner/internal/docker/executor.go（`buildContainerEnv` 第 119-176 行 + `taskModeForPython`）
    - runner/internal/docker/executor_test.go（调用 `buildContainerEnv(...)` 的契约，本 plan 禁改此文件）
    - runner/go.mod（go 1.25.0，现有依赖）
    - 64-RESEARCH.md §Standard Stack（client-go 版本）+ §Package Legitimacy Audit（client-go/api/apimachinery 均 Approved，无需人工 legitimacy checkpoint）
  </read_first>
  <action>
    在 `runner/` 下 `go get k8s.io/client-go k8s.io/api k8s.io/apimachinery`（三者 minor 对齐，pin 与 Go 1.25 兼容的 stable，目标 v0.34.x；以 `go get` 实际解析为准），随后 `go mod tidy`。
    新建中立包 `runner/internal/exec/env.go`，把 `buildContainerEnv` 的逻辑**逐字**移入并导出为 `func BuildContainerEnv(task ws.TaskPayload, callbackURL, callbackToken string) []string`，连同 `taskModeForPython` 一并移入（保持完全相同的 env 集合与顺序，含旧协议变量、`FRIDAY_TASK_*` 前缀修复、`metadata` 的 `env_` 透传逻辑）。
    在 `docker/executor.go` 中将 `buildContainerEnv` 改为**薄委托** `return exec.BuildContainerEnv(task, callbackURL, callbackToken)` 并删除已迁移的 `taskModeForPython`，新增 import `runner/internal/exec`。**保持函数名 `buildContainerEnv` 与签名不变**，使 `docker/executor_test.go` 逐字不改仍调用得到、行为等价（零回归命门，Pitfall 6）。
  </action>
  <verify>
    <automated>cd runner && go build ./... && go test ./internal/docker/...</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./...` 通过；`go.mod` 出现 `k8s.io/client-go`、`k8s.io/api`、`k8s.io/apimachinery` require 行（三者 minor 一致）。
    - `go test ./internal/docker/...` 全绿，且 `git diff --stat runner/internal/docker/executor_test.go` 为空（该测试文件未改动）。
    - `grep -n "func BuildContainerEnv" runner/internal/exec/env.go` 命中；`grep -n "exec.BuildContainerEnv" runner/internal/docker/executor.go` 命中。
  </acceptance_criteria>
  <done>client-go 入 go.mod；BuildContainerEnv 抽到 exec 包供复用；docker 经委托零回归（executor_test.go 不改且全绿）。</done>
</task>

<task type="auto">
  <name>Task 2: KubernetesExecutor 结构体 + New + StartContainer/WaitContainer/StreamLogs（client-go + fake 单测）</name>
  <files>runner/internal/k8s/executor.go, runner/internal/k8s/job.go, runner/internal/k8s/executor_test.go</files>
  <read_first>
    - runner/internal/k8s/executor.go（现桩：struct 空 + New() + 7 方法 ErrNotImplemented + 编译期接口检查）
    - runner/internal/docker/executor.go（StartContainer/WaitContainer/StreamLogs 行为参照，逐方法语义对齐）
    - runner/internal/ws/client.go（调用点：runTask 第 388/420/431 行，exitCode 判定第 444-468 行）
    - 64-RESEARCH.md §Method-by-Method Contract Mapping（1/2/4 节）+ §Code Examples（WaitContainer watch、StreamLogs GetLogs、fake clientset 单测）+ §Common Pitfalls 1/4/5
  </read_first>
  <action>
    重写 `runner/internal/k8s/executor.go`：`KubernetesExecutor` struct 字段 `cs kubernetes.Interface`、`namespace string`、`defaultImage string`、`runnerName string`、`backoffLimit int32`、`ttlSeconds int32`、`callbackHost string`、`imagePullSecret string`。保留编译期 `var _ ws.ExecutorService = (*KubernetesExecutor)(nil)`。新增 `Config` 结构（namespace/defaultImage/runnerName/backoffLimit/ttlSeconds/imagePullSecret）与 `func New(cfg Config) (*KubernetesExecutor, error)`：经 `rest.InClusterConfig()` 优先、失败回退 `clientcmd` 默认 loading rules（dev kubeconfig）构 `*rest.Config` → `kubernetes.NewForConfig`；namespace 为空时读 in-cluster `serviceaccount/namespace` 文件，再退 `"default"`；runnerName 经 sanitize 作 label value。另加测试用 `func NewWithClientset(cs kubernetes.Interface, cfg Config) *KubernetesExecutor`（不连真集群）。
    `ReadContainerFile/RemoveContainer/StartupCleanup/ZombieScan` 暂保留返回 `ErrNotImplemented`（64-02 实现）。
    实现 `StartContainer`：jobName=`friday-task-` + sanitize(task.TaskID)（RFC1123 小写 `[a-z0-9-]`、截断 ≤63 + 短哈希保唯一，Pitfall 5）；image 取 `task.Image` 否则 `defaultImage`（**无 ensureImage**，imagePullPolicy=IfNotPresent，imagePullSecret 非空则注入 Pod `ImagePullSecrets`）；env 经 `exec.BuildContainerEnv(task, callbackURL, callbackToken)` → `toEnvVars` 适配 `[]string("K=V")`→`[]corev1.EnvVar`；Job label `app=friday-task`、`friday.task_id=<sanitized>`、`friday.runner=<runnerName>`，Pod template label 同；`Jobs(ns).Create`；containerID=`ns+"/"+jobName`；best-effort 短暂 poll Job 的 Pod（label selector）拿 `status.podIP` 拼 answerEndpoint=`http://<podIP>:8977/answer`，拿不到返回空 answerEndpoint（对齐 docker inspect 失败回退，Pitfall 4）。Job 建成即返回 containerID（即使 answerEndpoint 空），日志仅记 task_id/job/answer_endpoint 非敏感字段（绝不打 env/token）。
    实现 `WaitContainer`：`splitID` 解 `ns/jobName`；`withTimeout(ctx,timeout)` 下 `Pods(ns).Watch`（label selector 选 Job 的 Pod）等 `ContainerStatuses[0].State.Terminated` 取 `ExitCode`；ctx 超时→best-effort 删 Job + 返回 `(-1, "", nil)`；仅 API error 才返 err（Pitfall 1，禁把超时/失败当 err 否则与 `ws/client.go:436` 分叉）；logs 恒返 `""`（日志走 StreamLogs）。
    实现 `StreamLogs`：选 Job 的 Pod（label `friday.task_id`，取最新/第一个），短暂 poll 等 Pod phase!=Pending（带上限）后 `Pods(ns).GetLogs(podName, &corev1.PodLogOptions{Follow:true}).Stream(ctx)` → `bufio.Scanner` 逐行 onLine（**无需 stdcopy**，k8s 日志已是合并文本流）；返回 `scanner.Err()`；ctx 取消即止。
    将 spec 装配抽成纯函数 `buildJobSpec(cfg, task, env []corev1.EnvVar) *batchv1.Job` 放 `job.go`（便于单测 label/env/backoffLimit/ttl/restartPolicy=Never），连同 `toEnvVars`/`sanitizeName`/`splitID` 助手。
    新建 `executor_test.go`：用 `k8s.io/client-go/kubernetes/fake` `NewSimpleClientset()`，`NewWithClientset` 构造，断言 StartContainer 建 1 个 Job 且 `friday.task_id`/`friday.runner` label 与关键 `FRIDAY_TASK_*` env（含 `FRIDAY_TASK_REMOTE_TOOLS`/`FRIDAY_TASK_CALLBACK_URL`）存在；用 fake watch reactor 注入 Pod terminated 事件断言 WaitContainer exitCode + 超时返回 (-1,nil)；buildJobSpec 纯函数断言 restartPolicy=Never/backoffLimit/ttl/labels。
  </action>
  <verify>
    <automated>cd runner && go build ./... && go vet ./internal/k8s/... && go test ./internal/k8s/...</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/k8s/...` 全绿，覆盖 StartContainer（Job 建成 + label + FRIDAY_TASK_* env）、WaitContainer（terminated exitCode + 超时 -1/nil）、StreamLogs、buildJobSpec。
    - `grep -n "ns + \"/\" + jobName\|ns+\"/\"+jobName\|containerID" runner/internal/k8s/executor.go` 证 containerID 为 `ns/jobName` 形态。
    - `grep -n "exec.BuildContainerEnv" runner/internal/k8s/executor.go` 命中（复用，无前缀漂移）。
    - `grep -n "friday.runner" runner/internal/k8s/job.go` 命中（多副本隔离 label，Pitfall 3）。
    - `go vet ./internal/k8s/...` 无告警。
  </acceptance_criteria>
  <done>KubernetesExecutor 经 client-go 实现 StartContainer/WaitContainer/StreamLogs，containerID=ns/jobName，env 复用 BuildContainerEnv，fake-clientset 单测全绿。</done>
</task>

<task type="auto">
  <name>Task 3: executor 选择接通（docker 零回归 + k8s 别名归一）+ callbackURL host 可配置 + 选择单测</name>
  <files>runner/internal/config/config.go, runner/internal/ws/client.go, runner/internal/cmd/run.go, runner/internal/cmd/executor_test.go</files>
  <read_first>
    - runner/internal/cmd/run.go（第 35-49 行 executor switch，kubernetes 分支当前 return error）
    - runner/internal/config/config.go（`bindEnvVars`、`GetExecutorType`、`GetDefaultImage`、`GetExecutorTimeout`、`GetCallbackPort`）
    - runner/internal/ws/client.go（第 143 行硬编码 `callbackURL=http://host.docker.internal:<port>/callback`）
    - 64-RESEARCH.md §Config 新增 + §Open Questions Q2（callbackURL）/Q5（type 命名统一）
  </read_first>
  <action>
    config.go：`bindEnvVars` 新增绑定 `callback.host`→`FRIDAY_RUNNER_CALLBACK_HOST`、`executor.k8s.namespace`→`FRIDAY_RUNNER_K8S_NAMESPACE`、`executor.k8s.backoff_limit`→`FRIDAY_RUNNER_K8S_BACKOFF_LIMIT`、`executor.k8s.ttl`→`FRIDAY_RUNNER_K8S_TTL`、`executor.k8s.service_account`→`FRIDAY_RUNNER_K8S_SERVICE_ACCOUNT`、`executor.k8s.image_pull_secret`→`FRIDAY_RUNNER_K8S_IMAGE_PULL_SECRET`。新增 getter：`GetCallbackHost() string`（默认 `""`）、`GetK8sNamespace() string`、`GetK8sBackoffLimit() int`（默认 0=不重试，对齐 docker 失败留存）、`GetK8sTTLSeconds() int`（默认 3600）、`GetK8sImagePullSecret() string`。`GetExecutorType()` 增归一：把 `"k8s"` 规整为 `"kubernetes"`（Open Q5：canonical=kubernetes，接受 k8s 别名），其余原样、空→`docker`。
    ws/client.go：`Config` 新增字段 `CallbackHost string`；`Run` 内将硬编码改为 `host := cfg.CallbackHost; if host == "" { host = "host.docker.internal" }` 再 `callbackURL := fmt.Sprintf("http://%s:%d/callback", host, cfg.CallbackPort)`（docker 默认零回归）。
    run.go：新增可测纯函数 `resolveExecutorKind(raw string) (string, error)`（`"docker"`/`""`→`docker`；`"kubernetes"`/`"k8s"`→`kubernetes`；其余→error）；将 switch 改用其结果：docker 分支不变（`docker.NewDockerExecutor`），`kubernetes` 分支改为构造 `k8s.New(k8s.Config{Namespace:config.GetK8sNamespace(), DefaultImage:config.GetDefaultImage(), RunnerName:config.GetRunnerName(), BackoffLimit:int32(config.GetK8sBackoffLimit()), TTLSeconds:int32(config.GetK8sTTLSeconds()), ImagePullSecret:config.GetK8sImagePullSecret()})`（失败 return 包装 error），赋给 `executor`。`ws.Config` 增 `CallbackHost: config.GetCallbackHost()`。
    新建 `cmd/executor_test.go`：表驱动测 `resolveExecutorKind`（docker/""/kubernetes/k8s/未知）。
  </action>
  <verify>
    <automated>cd runner && go build ./... && go test ./internal/cmd/... ./internal/config/... ./internal/docker/...</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/cmd/...` 全绿：`resolveExecutorKind("")`==`docker`、`"docker"`==`docker`、`"kubernetes"`==`kubernetes`、`"k8s"`==`kubernetes`、未知值返 error。
    - `grep -n "k8s.New" runner/internal/cmd/run.go` 命中且 kubernetes 分支不再 `return fmt.Errorf("kubernetes executor 未实现")`（`grep -c "未实现" runner/internal/cmd/run.go` == 0）。
    - `grep -n "CallbackHost" runner/internal/ws/client.go` 命中；docker 默认仍 host.docker.internal（`grep -n "host.docker.internal" runner/internal/ws/client.go` 命中）。
    - `go test ./internal/docker/...` 仍全绿（零回归）。
  </acceptance_criteria>
  <done>run.go kubernetes 分支真正构造 KubernetesExecutor，k8s/kubernetes 别名归一，callbackURL host 可配置（docker 默认不变），选择逻辑单测全绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runner → k8s API server | runner 经 client-go 操作 batch/v1 Job、core/v1 Pod（in-cluster SA token） |
| task Pod → runner（callback） | 任务容器经 callbackURL 回调 runner（k8s 下须 runner Pod IP 可达） |
| go module proxy → runner build | 新增 client-go 依赖供应链 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-64-01 | Tampering | taskID→jobName/label | mitigate | sanitize RFC1123（小写 `[a-z0-9-]`、截断 ≤63 + 短哈希），原始 taskID 仅入 env，防非法对象名注入 |
| T-64-02 | Information Disclosure | StartContainer 日志 | mitigate | zerolog 仅记 task_id/job/answer_endpoint，绝不打印 env 值/callbackToken（对齐 docker/executor.go:90） |
| T-64-03 | Spoofing/InfoDisc | answerEndpoint（Pod IP:8977） | accept | best-effort 对齐 docker 结构；HITL 端到端在 k8s 为已知限制（见 64-02 文档化 + manual 验收） |
| T-64-SC | Tampering | go get k8s.io/client-go(+api,apimachinery) | accept | 官方 k8s.io 一等公民包，64-RESEARCH §Package Legitimacy Audit 全 Approved/[VERIFIED proxy.golang.org]，无 [ASSUMED]/[SUS]，免人工 checkpoint；`go mod tidy` 后 `go build` 复验 |
</threat_model>

<verification>
- `cd runner && go build ./... && go vet ./...`
- `cd runner && go test ./internal/k8s/... ./internal/docker/... ./internal/config/... ./internal/cmd/...`
- docker 零回归证据：`git diff --stat runner/internal/docker/executor_test.go` 为空且测试全绿。
</verification>

<success_criteria>
- 默认 docker 路径零回归（既有 docker executor_test.go 不改且全绿）。
- executor.type=kubernetes/k8s 时 run.go 构造 KubernetesExecutor（不再报"未实现"）。
- KubernetesExecutor StartContainer/WaitContainer/StreamLogs 经 client-go 实现并以 fake clientset 单测覆盖。
- callbackURL host 可配置：docker 默认 host.docker.internal，k8s 取 FRIDAY_RUNNER_CALLBACK_HOST。
</success_criteria>

<artifacts_this_phase_produces>
- `runner/internal/exec/env.go`（共享 BuildContainerEnv）
- `runner/internal/k8s/executor.go`（KubernetesExecutor + New + 核心三方法；ReadContainerFile/Remove/Cleanup/Zombie 暂 stub）
- `runner/internal/k8s/job.go`（buildJobSpec 纯函数 + 助手）
- `runner/internal/k8s/executor_test.go`（fake-clientset 单测）
- `runner/internal/cmd/executor_test.go`（选择单测）
- 改动：`runner/go.mod`/`go.sum`、`runner/internal/docker/executor.go`、`runner/internal/config/config.go`、`runner/internal/ws/client.go`、`runner/internal/cmd/run.go`
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/64-runner/64-01-SUMMARY.md` when done
</output>
