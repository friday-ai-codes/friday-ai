---
phase: 124-coding-chain
plan: 02
type: execute
wave: 1
depends_on: ["124-00"]
files_modified:
  - server/services/code_graph/impact_report.py
  - server/friday/settings.py
  - server/tests/services/code_graph/test_impact_report.py
autonomous: true
requirements: [DIFF-04]

must_haves:
  truths:
    - "单一入口 build_impact_report_section 消费 run_detect_changes，渲染 ## 影响面 + Changes/Affected/Risk/Recommendations（D-05/D-07 / DIFF-04）"
    - "失败/超时/ACL 返回 stub（稳定 error_code），永不抛向调用方；部分成功仍出四段（D-09..D-12）"
    - "体积软上限 + truncated 标注；不嵌入源码正文（D-08 / T-124-03）"
    - "观测 started/completed/failed 带 duration_ms/section_chars/error_code，best-effort（D-15）"
    - "无产品 kill-switch；仅 timeout/max_chars settings（D-13）"
  artifacts:
    - path: "server/services/code_graph/impact_report.py"
      provides: "共享报告生成 + 幂等 append"
      exports: ["IMPACT_SECTION_MARKER", "build_impact_report_section", "append_impact_report"]
    - path: "server/friday/settings.py"
      provides: "CODE_GRAPH_IMPACT_REPORT_TIMEOUT_SECONDS / MAX_CHARS"
      contains: "CODE_GRAPH_IMPACT_REPORT_TIMEOUT_SECONDS"
    - path: "server/tests/services/code_graph/test_impact_report.py"
      provides: "formatter/stub/timeout/观测绿测"
  key_links:
    - from: "build_impact_report_section"
      to: "run_detect_changes"
      via: "asyncio.wait_for(timeout=settings.CODE_GRAPH_IMPACT_REPORT_TIMEOUT_SECONDS)"
      pattern: "run_detect_changes|wait_for"
    - from: "append_impact_report"
      to: "IMPACT_SECTION_MARKER"
      via: "幂等跳过已含 ## 影响面"
      pattern: "IMPACT_SECTION_MARKER|## 影响面"
---

<objective>
落地 DIFF-04 核心：共享 `impact_report` 模块（消费 `run_detect_changes`）+ settings 超时/体积阈值，并用 Wave 0 桩转绿。

Purpose: workflow 与 MCP 壳层禁止各写一套渲染（D-05/D-14）；本 plan 建立单一事实源与 fail-soft 契约，供 124-03 接线。
Output: `impact_report.py` + settings + `test_impact_report.py` 全绿。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/124-coding-chain/124-CONTEXT.md
@.planning/phases/124-coding-chain/124-RESEARCH.md
@.planning/phases/124-coding-chain/124-PATTERNS.md
@server/services/code_graph_tools.py
@server/workflows/services/pr_cross_reference.py
@server/services/code_graph/impact.py
@server/friday/settings.py
</context>

## Artifacts this phase produces

| Symbol | Module | Notes |
|--------|--------|-------|
| `IMPACT_SECTION_MARKER` | `services.code_graph.impact_report` | `"## 影响面"` |
| `build_impact_report_section` | `services.code_graph.impact_report` | async；永远返回 str，不抛 |
| `append_impact_report` | `services.code_graph.impact_report` | 纯函数；幂等 |
| `_render_four_sections` / `_stub_section` / `_map_error_code`（或等价私有 helper） | same | Discretion 命名 |
| `CODE_GRAPH_IMPACT_REPORT_TIMEOUT_SECONDS` | `friday.settings` | default 30.0 |
| `CODE_GRAPH_IMPACT_REPORT_MAX_CHARS` | `friday.settings` | default 10240 |

<tasks>

<task type="auto">
  <name>Task 1: settings 阈值（D-10/D-08/D-13）</name>
  <files>server/friday/settings.py</files>
  <read_first>
    - `server/friday/settings.py` 约 `CODE_GRAPH_BUILD_WAIT_TIMEOUT_SECONDS`（919–921）及邻近 CODE_GRAPH_* 族
    - `.planning/phases/124-coding-chain/124-RESEARCH.md` Discretionary Defaults
  </read_first>
  <action>
在 `CODE_GRAPH_BUILD_WAIT_TIMEOUT_SECONDS` 旁新增（django-environ，与族命名一致；per D-10/D-08 RESEARCH defaults）：
- `CODE_GRAPH_IMPACT_REPORT_TIMEOUT_SECONDS: float` env 同名，default `30.0`
- `CODE_GRAPH_IMPACT_REPORT_MAX_CHARS: int` env 同名，default `10240`

⛔ 不新增「关闭影响面」产品开关（D-13）。⛔ 不改 `repo_router_v2.py`（D-16）。提交仅 stage 本文件显式路径。
  </action>
  <acceptance_criteria>
    - `rg -n "CODE_GRAPH_IMPACT_REPORT_TIMEOUT_SECONDS|CODE_GRAPH_IMPACT_REPORT_MAX_CHARS" server/friday/settings.py` 各 ≥1
    - `rg -n "IMPACT_REPORT_ENABLED|DISABLE_IMPACT|kill.?switch" server/friday/settings.py` 无匹配（D-13）
    - `python -c "import django; ..."` 非必须；以 grep + 后续 import settings 的测试间接验证即可
  </acceptance_criteria>
  <verify>
    <automated>cd server && rg -n "CODE_GRAPH_IMPACT_REPORT_TIMEOUT_SECONDS" friday/settings.py && rg -n "CODE_GRAPH_IMPACT_REPORT_MAX_CHARS" friday/settings.py && (rg -n "IMPACT_REPORT_ENABLED|DISABLE_IMPACT" friday/settings.py || true) | tee /dev/stderr | (test "$(rg -c "IMPACT_REPORT_ENABLED|DISABLE_IMPACT" friday/settings.py || echo 0)" = "0")</automated>
  </verify>
  <done>超时与体积 settings 就位；无 kill-switch（D-13）</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: impact_report 模块 + 单测转绿（D-05/D-07..D-12/D-15）</name>
  <files>server/services/code_graph/impact_report.py, server/tests/services/code_graph/test_impact_report.py</files>
  <read_first>
    - `server/services/code_graph_tools.py` `run_detect_changes` 签名与信封字段（约 1056–1141、成功信封映射 RESEARCH §Code Examples）
    - `server/workflows/services/pr_cross_reference.py` fail-soft 空串兜底（约 109–111）
    - `server/services/code_graph/impact.py` `RiskLevel`（小写枚举 → 展示大写）
    - `.planning/phases/124-coding-chain/124-PATTERNS.md` §impact_report.py 全文
    - `server/tests/services/code_graph/test_impact_report.py`（Wave 0 桩）
    - `.cursor/rules/observability-logging.mdc`（category/component/redact）
  </read_first>
  <behavior>
    - fixture ok 信封 → 含 `## 影响面` 与四个 `### Changes|Affected|Risk|Recommendations`
    - ok=False / TimeoutError / GraphAccessDenied → stub 含稳定 error_code（not_indexed/timeout/unavailable）；函数不 raise
    - ok=True + staleness/degradation → 仍四段且 Risk/Recommendations 声明降级（D-12）
    - 超 MAX_CHARS → 截断并含 `truncated`；无源码正文/大块 code fence 自 envelope content
    - `append_impact_report`：空 section 原样；已含 marker 不重复；否则 append
    - stub/日志无堆栈、token、绝对路径字面量（T-124-02）
    - 观测事件名静态：`impact_report_started`/`impact_report_completed`/`impact_report_failed`；component=code_graph；category=caller；含 duration_ms / repository_id / section_chars 或 error_code；log 失败吞掉
  </behavior>
  <action>
新建 `server/services/code_graph/impact_report.py`（per D-05 RESEARCH path）：

1. 常量 `IMPACT_SECTION_MARKER = "## 影响面"`
2. `append_impact_report(description: str, section: str) -> str` — 幂等（D-06）
3. `async def build_impact_report_section(*, repository, user, compare: str, base_ref: str | None = None) -> str`：
   - best-effort 打 `impact_report_started`（component=code_graph, category=caller, repository_id）
   - `asyncio.wait_for(run_detect_changes(repository_id=str(repository.id), repo=repository, user=user, compare=compare, base_ref=base_ref), timeout=settings.CODE_GRAPH_IMPACT_REPORT_TIMEOUT_SECONDS)`（D-10）
   - 捕获 `TimeoutError` → stub `timeout`；`GraphAccessDenied` → stub `unavailable`（T-124-05）；其它 Exception → stub `unavailable`；信封 `ok=False` → stub（`repository_not_indexed` 规范化为 `not_indexed`，其它稳定短码可透传）（D-09/D-11）
   - `ok=True`（含 staleness/degradation/单符号 impact_error）→ 渲染四段（D-07/D-12）：Changes←files/summary；Affected←impacts 分组+截断计数；Risk←impacts risk_level 最严重（空→LOW；file_level_only/truncated→至少 MEDIUM）；Recommendations←规则短语（复核 d1 callers / 重索引等）；⛔ 不编造 `affected_processes` Process 叙事
   - top-N：files=15、symbols/file=8、impact seeds=10；整段 &gt; MAX_CHARS 截断注 truncated（D-08）；⛔ 不调 include_content、不塞源码（T-124-03）
   - stub 文案固定为 CONTEXT D-11 模板；若 stub 自身也失败 → 返回 `""`（对齐 pr_cross_reference）
   - 完成/失败观测：`impact_report_completed`/`impact_report_failed` + duration_ms + section_chars/error_code；异常文本经 `redact_secrets_in_text`；观测 try/except pass（D-15）
   - user 为 None → 不调用编排撞 ACL 绕过；直接 stub `unavailable`

移除 Wave 0 skip，mock `services.code_graph_tools.run_detect_changes`（或 impact_report 内 import 点）写绿全部 `test_impact_report.py` 节点。

⛔ 不在本模块重写 BFS/diff；⛔ 不改 `run_detect_changes` 本体行为；⛔ 不改 `mcp/` / `repo_router_v2.py`。
  </action>
  <acceptance_criteria>
    - `cd server && GALAXY_CACHE_WARM_ON_STARTUP=False GRAPH_BUILD_ORPHAN_RECONCILE_ON_STARTUP=False uv run pytest tests/services/code_graph/test_impact_report.py -q --reuse-db` 全绿
    - `rg -n "async def build_impact_report_section|def append_impact_report|IMPACT_SECTION_MARKER" server/services/code_graph/impact_report.py` 各命中
    - `rg -n "impact_report_started|impact_report_completed|impact_report_failed" server/services/code_graph/impact_report.py` 各命中
    - `rg -n "include_content\s*=\s*True" server/services/code_graph/impact_report.py` 无匹配
    - 模块对外函数文档/注释声明「永不 raise 阻断建 MR」（D-09）
  </acceptance_criteria>
  <verify>
    <automated>cd server && GALAXY_CACHE_WARM_ON_STARTUP=False GRAPH_BUILD_ORPHAN_RECONCILE_ON_STARTUP=False uv run pytest tests/services/code_graph/test_impact_report.py -q --reuse-db</automated>
  </verify>
  <done>共享 impact_report 单一事实源就绪；fail-soft/四段/观测可测（DIFF-04 核心）</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MR description builder → run_detect_changes | 服务端实时调用；ACL 失败不得变成「无影响」成功四段 |
| impact_report → MR body / logs | stub 与日志禁止堆栈/token/路径泄漏 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-124-01 | Tampering | N/A | accept | 本 plan 无 prompt 拼接 |
| T-124-02 | Information Disclosure | stub + structlog | mitigate | 仅稳定 error_code；redact_secrets_in_text；单测钉死 |
| T-124-03 | Information Disclosure | four-section render | mitigate | 不 include_content；top-N；无源码测 |
| T-124-04 | Denial of Service | wait_for 30s | mitigate | settings 超时→stub；不重试 |
| T-124-05 | Elevation / Spoofing | GraphAccessDenied | mitigate | stub unavailable；禁止空成功四段测 |
| T-124-SC | Tampering | package installs | accept | 零新包 |
</threat_model>

<verification>
`test_impact_report.py` 全绿；settings 键存在；冻结面未触碰。
</verification>

<success_criteria>
- `build_impact_report_section` 为唯一渲染入口（D-05）
- 四段结构与 fail-soft stub 契约可自动化验证（D-07/D-09..D-12）
- 观测与体积纪律就位（D-08/D-15）；无 kill-switch（D-13）
</success_criteria>

<output>
Create `.planning/phases/124-coding-chain/124-02-SUMMARY.md` when done
</output>
