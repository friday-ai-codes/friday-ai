---
phase: 124-coding-chain
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - server/tests/services/code_graph/test_impact_report.py
  - server/tests/workflows/test_coding_impact_report.py
  - server/tests/mcp_tools/test_mr_impact_report.py
  - task/tests/test_detect_changes_prompt.py
  - .planning/phases/124-coding-chain/124-VALIDATION.md
autonomous: true
requirements: [DIFF-03, DIFF-04]

must_haves:
  truths:
    - "后续每个 plan 的 <verify> 命令都能命中可被 pytest 收集到的节点名"
    - "DIFF-03 prompt/白名单与 DIFF-04 formatter/双链路/fail-soft/对等哨兵用例名已在 Wave 0 登记"
    - "Wave 0 不改生产代码、不改 mcp/ submodule、不改 repo_router_v2.py（D-16）"
  artifacts:
    - path: "server/tests/services/code_graph/test_impact_report.py"
      provides: "formatter / stub / timeout / 体积 / 观测桩"
      contains: "test_build_impact_report_four_sections"
    - path: "server/tests/workflows/test_coding_impact_report.py"
      provides: "AICodingNode MR fail-soft 桩"
      contains: "test_create_mr_failsoft_on_impact_error"
    - path: "server/tests/mcp_tools/test_mr_impact_report.py"
      provides: "MCP MR 路径 + D-14 对等哨兵桩"
      contains: "test_workflow_mcp_impact_section_parity"
    - path: "task/tests/test_detect_changes_prompt.py"
      provides: "system prompt 条件追加桩（D-01/D-03/D-04）"
      contains: "test_prompt_appends_detect_changes_when_knowledge_plan"
  key_links:
    - from: "124-VALIDATION.md Wave 0 Gaps"
      to: "四个新测试文件"
      via: "用例名与 RESEARCH Validation Architecture 表一一对应"
      pattern: "test_.*impact_report|test_.*detect_changes"
---

<objective>
建立 Phase 124 全部自动化验收地基：四个测试文件骨架 + VALIDATION/RESEARCH 点名的全部 pytest 节点（skip 桩），并刷新 124-VALIDATION 的 Per-Task 表为最终 plan/task ID。

Purpose: Nyquist 采样要求每个 task 的 automated verify 命中真实节点；Wave 0 不落地则 W1–W2 验收失锚（per D-01..D-16）。
Output: 4 个测试文件（skip 桩）+ 更新后的 `124-VALIDATION.md`。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/124-coding-chain/124-CONTEXT.md
@.planning/phases/124-coding-chain/124-RESEARCH.md
@.planning/phases/124-coding-chain/124-VALIDATION.md
@.planning/phases/124-coding-chain/124-PATTERNS.md
@task/tests/test_openspec_prompt.py
@server/tests/workflows/test_coding_pr_target_branch.py
@server/tests/mcp_tools/test_mr_tools.py
@server/tests/mcp_tools/test_detect_changes_tools.py
</context>

## Artifacts this phase produces

> Phase-level inventory（本 plan 只建测试桩；实现符号由后续 plan 落地）

| Symbol / artifact | Module | Introduced in |
|-------------------|--------|---------------|
| `detect_changes` schema entry in `KNOWLEDGE_TOOL_SCHEMAS` | `task/core/knowledge_tools.py` | 124-01 |
| `_detect_changes_guidance` / conditional append in `_get_system_prompt` | `task/core/executor.py` | 124-01 |
| `IMPACT_SECTION_MARKER` / `build_impact_report_section` / `append_impact_report` | `server/services/code_graph/impact_report.py` | 124-02 |
| `CODE_GRAPH_IMPACT_REPORT_TIMEOUT_SECONDS` / `CODE_GRAPH_IMPACT_REPORT_MAX_CHARS` | `server/friday/settings.py` | 124-02 |
| Workflow/MCP/`mr_service` MR description append hooks | `coding.py` / `merge_request_service.py` / `mr_service.py` | 124-03 |

<tasks>

<task type="auto">
  <name>Task 1: Server impact_report + MR 双链路测试骨架</name>
  <files>server/tests/services/code_graph/test_impact_report.py, server/tests/workflows/test_coding_impact_report.py, server/tests/mcp_tools/test_mr_impact_report.py</files>
  <read_first>
    - `.planning/phases/124-coding-chain/124-RESEARCH.md` §Validation Architecture → Phase Requirements → Test Map
    - `.planning/phases/124-coding-chain/124-VALIDATION.md` §Wave 0 Requirements / Threat → Test Mapping
    - `.planning/phases/124-coding-chain/124-PATTERNS.md` §test_impact_report / test_coding_impact_report / test_mr_impact_report
    - `server/tests/workflows/test_coding_pr_target_branch.py`（`_create_mr_for_repo` 直调形态）
    - `server/tests/mcp_tools/test_detect_changes_tools.py`（双面哨兵精神，约 `:207-218`）
  </read_first>
  <action>
新建三个 server 测试文件。每个行为用例体暂为 `pytest.fail("Wave 0 桩")` 并挂 `@pytest.mark.skip(reason="Wave 0 桩：由 124-02/124-03 落地")`。⛔ 不得改生产代码；⛔ 不得改 `mcp/` 或 `repo_router_v2.py`（D-16）。并发 WIP：提交只用显式路径，禁止 `git add -A`。

`tests/services/code_graph/test_impact_report.py`（归属 124-02；mock `run_detect_changes`；覆盖 D-05/D-07/D-08/D-09/D-10/D-11/D-12/D-15 与 T-124-02/03/04/05）：
- `test_build_impact_report_four_sections`
- `test_ok_false_yields_stub_error_code`
- `test_timeout_yields_stub_timeout`
- `test_graph_access_denied_yields_stub_unavailable`（T-124-05：禁止空成功四段）
- `test_partial_success_still_four_sections`（D-12：ok=True + staleness/degradation）
- `test_max_chars_truncation_note`
- `test_no_source_body_in_section`（T-124-03）
- `test_append_impact_report_idempotent`
- `test_stub_omits_stack_and_secrets`（T-124-02）
- `test_observability_events_static_names`（D-15：started/completed/failed）

`tests/workflows/test_coding_impact_report.py`（归属 124-03）：
- `test_create_mr_appends_impact_section`
- `test_create_mr_failsoft_on_impact_error`（异常仍调用 `create_merge_request`）
- `test_create_mr_description_contains_stub_on_timeout`

`tests/mcp_tools/test_mr_impact_report.py`（归属 124-03；D-14）：
- `test_mcp_create_mr_appends_impact_section`
- `test_mcp_explicit_description_append_idempotent`
- `test_mcp_create_mr_failsoft_on_impact_error`
- `test_workflow_mcp_impact_section_parity`
  </action>
  <acceptance_criteria>
    - `cd server && GALAXY_CACHE_WARM_ON_STARTUP=False GRAPH_BUILD_ORPHAN_RECONCILE_ON_STARTUP=False uv run pytest tests/services/code_graph/test_impact_report.py tests/workflows/test_coding_impact_report.py tests/mcp_tools/test_mr_impact_report.py --collect-only -q` 收集到上方全部节点名
    - 每个列出的用例带 `Wave 0 桩` skip reason（`grep -v '^#' | grep -c 'Wave 0'` ≥ 1 于各文件）
    - 三文件均不 import/修改 `repo_router_v2` / `mcp/` 包路径
  </acceptance_criteria>
  <verify>
    <automated>cd server && GALAXY_CACHE_WARM_ON_STARTUP=False GRAPH_BUILD_ORPHAN_RECONCILE_ON_STARTUP=False uv run pytest tests/services/code_graph/test_impact_report.py tests/workflows/test_coding_impact_report.py tests/mcp_tools/test_mr_impact_report.py --collect-only -q</automated>
  </verify>
  <done>server 侧 DIFF-04 全部 Wave 0 节点可被 pytest 收集</done>
</task>

<task type="auto">
  <name>Task 2: Task prompt 测试骨架 + VALIDATION 表对齐</name>
  <files>task/tests/test_detect_changes_prompt.py, .planning/phases/124-coding-chain/124-VALIDATION.md</files>
  <read_first>
    - `task/tests/test_openspec_prompt.py`（完整形态：MagicMock config + `_get_system_prompt`）
    - `.planning/phases/124-coding-chain/124-PATTERNS.md` §test_detect_changes_prompt
    - `.planning/phases/124-coding-chain/124-VALIDATION.md` 全文
  </read_first>
  <action>
1. 新建 `task/tests/test_detect_changes_prompt.py`（归属 124-01；照抄 openspec 测法；每用例 `@pytest.mark.skip(reason="Wave 0 桩：由 124-01 落地")`）：
   - `test_prompt_appends_detect_changes_when_knowledge_plan`（knowledge_endpoint+user_token+task_mode=plan → 含 `detect_changes`/自查关键词；D-01/D-03）
   - `test_prompt_appends_detect_changes_when_execute`
   - `test_prompt_skips_when_explore_mode`（explore 不追加）
   - `test_prompt_skips_when_knowledge_missing`（缺 endpoint 或 token 不追加）
   - `test_detect_changes_guidance_helper_independent`（独立 helper 非空静态文本；T-124-01）
   - `test_guidance_non_blocking_language`（文案含「继续交付」/「不要因为」类非阻断语义；D-04）
   - `test_openspec_and_detect_changes_can_coexist`（follow_openspec=True 且 knowledge 挂载 → 两段都在）

2. 更新 `124-VALIDATION.md`：
   - Per-Task Verification Map 的 Task ID 改为 `124-00-01` / `124-00-02` / `124-01-*` / `124-02-*` / `124-03-*` 与本相位最终 plan 对齐
   - Wave 0 Requirements 勾选文件存在项在执行后由 executor 标完成意图写明；frontmatter 保持 `wave_0_complete: false` 直至本 plan SUMMARY（本 plan 完成后 executor 可在 SUMMARY 后把 VALIDATION 的 Wave 0 勾上并设 `wave_0_complete: true`，`nyquist_compliant` 仍待全相位绿）
   - File Exists 列对新建四文件改为将存在

⛔ 本 task 不改 `test_knowledge_tools.py` 的 `== 10` 断言（留给 124-01 与白名单同提交，避免 Wave 0 单独把既有套件打红）。
  </action>
  <acceptance_criteria>
    - `cd task && uv run pytest tests/test_detect_changes_prompt.py --collect-only -q` 收集到上方全部节点名
    - `124-VALIDATION.md` Per-Task 表含字面量 `124-01`、`124-02`、`124-03` 与 `test_workflow_mcp_impact_section_parity`
    - `rg -n "124-\\*-\\*" .planning/phases/124-coding-chain/124-VALIDATION.md` 无匹配（占位符已消）
  </acceptance_criteria>
  <verify>
    <automated>cd task && uv run pytest tests/test_detect_changes_prompt.py --collect-only -q</automated>
  </verify>
  <done>task prompt 桩可收集；VALIDATION Task ID 与 plan 编号一致</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → future production hooks | Wave 0 只建桩；威胁由 124-01..03 对应用例钉死 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-124-01 | Tampering | `_detect_changes_guidance` | mitigate | `test_detect_changes_guidance_helper_independent` + 静态字面量验收在 124-01 |
| T-124-02 | Information Disclosure | impact_report stub/logs | mitigate | `test_stub_omits_stack_and_secrets` 桩 → 124-02 |
| T-124-03 | Information Disclosure | MR section body | mitigate | `test_no_source_body_in_section` 桩 → 124-02 |
| T-124-04 | Denial of Service | MR path timeout | mitigate | `test_timeout_yields_stub_timeout` + fail-soft 双链路桩 → 124-02/03 |
| T-124-05 | Elevation / Spoofing | ACL → empty success | mitigate | `test_graph_access_denied_yields_stub_unavailable` 桩 → 124-02 |
| T-124-SC | Tampering | package installs | accept | 本相位零新增 pip/npm 包（Package Legitimacy 空表） |
</threat_model>

<verification>
四个测试文件 `--collect-only` 全绿；后续 plan verify 节点名与本 plan 登记一致。
</verification>

<success_criteria>
- VALIDATION Wave 0 Gaps 四文件均存在且可收集
- DIFF-03/DIFF-04 行为均有对应 skip 桩节点
- 生产代码零改动（D-16 冻结面未触碰）
</success_criteria>

<output>
Create `.planning/phases/124-coding-chain/124-00-SUMMARY.md` when done
</output>
