---
phase: 100-learning-case-mcp
plan: 04
type: execute
wave: 3
depends_on: [100-01, 100-02, 100-03]
files_modified:
  - server/mcp_tools/learning_case_service.py
  - server/mcp_tools/views.py
  - server/mcp_tools/serializers.py
  - server/knowledge/management/commands/backfill_learning_cases.py
  - server/tests/mcp_tools/test_learning_cases.py
  - server/tests/knowledge/test_backfill_learning_cases.py
autonomous: true
requirements: [KNOW-02]

must_haves:
  truths:
    - "search_learning_cases 底层走 DeliveryKnowledgeSearchService.search_similar(entity_kinds=[\"learning_case\"])，token 打分实现已删除且无 fallback 开关"
    - "对外契约不变：TOOL_SCHEMA_SNAPSHOT 的 search_learning_cases request/response 键集逐字不动、payload 外形（case_id/title/.../score）与 learning_case_payload 一致"
    - "hint 参数真起作用：repo_hints/file_hints/symbol_hints/work_item_type 拼入查询文本增强 + 命中 hint 的 case 结果层提权排序，不静默丢弃"
    - "Qdrant 不可用时 search_learning_cases 返回空 results 不 500（fail-soft）"
    - "存量 McpLearningCase 与 MCP 三类产物可经 backfill_learning_cases 命令回填入图，重复执行幂等"
    - "golden set 对照测试（路径命中 / symbol 命中 / 中文问题描述）通过：目标 case 在 top-N（KNOW-02 验收门）"
    - "新召回路径写 RetrievalTrace（FILE trace 含 score/case_id），召回条数/耗时可查"
  artifacts:
    - path: "server/mcp_tools/learning_case_service.py"
      provides: "向量版 search_learning_cases（回捞行渲染既有 payload + hint rerank + fail-soft），token 打分删除"
      contains: "DeliveryKnowledgeSearchService"
    - path: "server/knowledge/management/commands/backfill_learning_cases.py"
      provides: "存量 learning case + MCP 三类产物回填命令（rebuild_project_context 范式）"
    - path: "server/tests/mcp_tools/test_learning_cases.py"
      provides: "golden set 对照测试 + 契约/fail-soft/RetrievalTrace 断言"
  key_links:
    - from: "server/mcp_tools/learning_case_service.py search_learning_cases"
      to: "knowledge.retrieval.DeliveryKnowledgeSearchService"
      via: "search_similar(entity_kinds=[\"learning_case\"]) + source_id 回捞 McpLearningCase"
      pattern: "entity_kinds=\\[.learning_case.\\]"
    - from: "server/mcp_tools/views.py SearchLearningCasesView"
      to: "interactions RetrievalTrace"
      via: "既有 _record traces（file trace 含 score/case_id）继续覆盖新路径"
      pattern: "learning_case"
    - from: "server/mcp_tools/serializers.py TOOL_SCHEMA_SNAPSHOT"
      to: "search_learning_cases 键集"
      via: "逐字不动（快照测试守门）"
      pattern: "search_learning_cases"
---

<objective>
KNOW-02 落地：`search_learning_cases` 底层从 token 打分切换为统一向量检索，对外契约不变；
token 实现退役删除（无 fallback 开关）；hint 参数变实（查询增强 + 结果 rerank）；Qdrant 故障
fail-soft；存量数据经 `backfill_learning_cases` 命令回填；golden set 对照测试作为验收门；
RetrievalTrace 观测覆盖。

Purpose: 「检索切换三件套（normalizer + backfill + 读切换）同 phase 闭环上线，避免切换当天检索全空」
（CONTEXT specifics + PITFALLS P1）。
Output: 向量版 service + backfill 命令 + golden set 验收门 + 观测断言。
</objective>

<context>
@.planning/phases/100-learning-case-mcp/100-CONTEXT.md （locked：「search_learning_cases 契约保持」「Backfill」「观测」「Golden set 验收门」四节全部照办）
@server/mcp_tools/learning_case_service.py （L40-62 learning_case_payload 外形保持源、L213-245 待删 token 实现）
@server/mcp_tools/views.py （L1686-1741 SearchLearningCasesView：调用点 + traces 结构、L280-319 _record；request.user 为检索权限主体）
@server/mcp_tools/serializers.py （L457-480 SearchLearningCasesRequestSerializer、L670-673 TOOL_SCHEMA_SNAPSHOT 键集——不动）
@server/knowledge/retrieval.py （search_similar 签名：user 关键字参数 fail-closed、SearchResultDTO.score 为向量融合分）
@server/knowledge/management/commands/rebuild_project_context.py （backfill 命令逐字范式：async 遍历 + aschedule_ingestion + caller 事件 + 绝不删库）
@server/tests/mcp_tools/test_learning_cases.py （既有两用例——切换后需适配）
@server/tests/mcp_tools/test_schema_snapshot.py + test_retrieval_trace.py （守门测试）
@server/tests/knowledge/conftest.py （mock_qdrant_client seam；golden set 可 monkeypatch QdrantService.get_client 为 QdrantClient(":memory:") 本地实例）
</context>

<tasks>

<task type="auto">
  <name>Task 1: search_learning_cases 向量切换 + token 退役 + fail-soft</name>
  <files>server/mcp_tools/learning_case_service.py, server/mcp_tools/views.py, server/mcp_tools/serializers.py</files>
  <action>
重写 `learning_case_service.search_learning_cases`（locked decision「search_learning_cases 契约保持」逐条兑现）：

1. **签名**：新增关键字参数 `user`（检索权限主体，`search_similar` fail-closed 需要）；其余参数（query/work_item_type/repo_hints/file_hints/symbol_hints/limit）不变。`views.py SearchLearningCasesView` 调用点补 `user=request.user`。
2. **查询增强**：`query_text = " ".join([query, work_item_type, *repo_hints, *file_hints, *symbol_hints]).strip()`（hint 拼入查询文本；全空时用 query 原文，query 也空则直接返回 []——向量检索无「无查询返回最新」语义，schema 描述同步说明 query 建议必填）。
3. **检索**：`DeliveryKnowledgeSearchService().search_similar(query_text, user=user, top_k=max(limit * 3, 10), entity_kinds=["learning_case"])`（超采样供 rerank/过滤余量；100-01 已保证 kind 真过滤）。
4. **回捞**：收集命中 `SearchResultDTO.entity.source_id`（即 McpLearningCase UUID str）→ `McpLearningCase.objects.filter(id__in=...)` 一次批量 async 查询建 dict；实体命中但行已删的跳过（弱引用语义）。`work_item_type` 非空时按行字段过滤（post-filter）。
5. **结果层 rerank（hint 提权，不做摆设）**：排序键 = `dto.score + bonus`，bonus 按 hint 命中累计（repo_hints 命中 case.repositories +0.05/类、file_hints 命中 case.files +0.05/类、symbol_hints 命中 case.symbols +0.05/类；子串不区分大小写匹配，沿用旧实现匹配语义）。**payload 的 `score` 字段 = 原始 `dto.score`（向量融合分 0-1 浮点，locked 定版），bonus 只影响排序不改显示分**。
6. **渲染**：取 rerank 后前 `limit` 条，`learning_case_payload(case, score=dto.score)` 渲染——payload 外形函数**一字不改**。
7. **fail-soft**：整段检索+回捞包 try/except（`except Exception`）：`logger.warning("learning_case_search_failed", error=redact_secrets_in_text(str(exc)), error_type=..., component="mcp_tools", category="caller")` 后返回 `[]`（Qdrant/embedding 不可用不 500）。
8. **token 退役**：删除 `_TOKEN_RE`、`_tokens` 与旧循环实现，**不留任何 fallback 开关**（locked）。文件 docstring 更新为中文，注明 v0.17.0 起底层为统一向量检索（KNOW-02）。
9. **观测**：函数入口/出口打 `learning_case_search_started/completed` 结构化事件（kv：query_len/hint_counts/result_count/duration_ms，component="mcp_tools"，category="caller"）；检索内部步骤沿用 knowledge 域既有 sampling 事件，不新增刷屏点。
10. **schema 描述定版**：`SearchLearningCasesRequestSerializer` 类 docstring + `query` 字段 `help_text` 写明（中文）：结果按统一向量检索排序；`score` 为向量融合分（0-1 浮点），自 v0.17.0 起语义由 token 命中计数变更为向量分；hints 参与查询增强与结果提权。**TOOL_SCHEMA_SNAPSHOT 键集与序列化字段名一个都不动**。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/mcp_tools/test_schema_snapshot.py -x -q && rg -c "_TOKEN_RE|def _tokens" mcp_tools/learning_case_service.py; test $? -eq 1</automated>
  </verify>
  <done>底层为 search_similar(entity_kinds=["learning_case"])；token 实现零残留；快照测试全绿（键集未动）；fail-soft 与 score 语义按 locked decision 落地。</done>
</task>

<task type="auto">
  <name>Task 2: backfill_learning_cases 管理命令</name>
  <files>server/knowledge/management/commands/backfill_learning_cases.py, server/tests/knowledge/test_backfill_learning_cases.py</files>
  <action>
照 `rebuild_project_context.py` 范式新建命令（中文 docstring：三件套闭环/幂等/绝不删库语义逐条说明）：

1. `_backfill() -> dict[str, int]`：四段 async 遍历，各自 `aschedule_ingestion`（trigger 统一 `"backfill_learning_cases"`，不传 initiated_by_user_id——命令行无触发用户记 system）：
   - `McpLearningCase.objects.all().aiterator()` → `IngestionRequest("learning_case", str(case.id), ...)`
   - `McpCodingPlan.objects.all().aiterator()` → `("mcp_coding_plan", str(plan.id), ...)`
   - `McpRepositoryAnalysis.objects.all().aiterator()` → `("mcp_repository_analysis", str(a.id), ...)`
   - `McpCodingExecutionTrace.objects.all().aiterator()` → `("mcp_execution_trace", str(t.id), ...)`
   返回按 source_kind 计数 dict。
2. `Command.handle`：`asyncio.run` + started/completed 结构化事件（scheduled 分类计数 + duration_ms + component="knowledge" + category="caller"）+ stdout SUCCESS 摘要。批次控制不需要（aschedule_ingestion 只是投递后台队列，轻量；进度输出为 Claude's Discretion——每 100 条 stdout 一行即可）。
3. 可选参数 `--only`（choices=learning_case/mcp_coding_plan/mcp_repository_analysis/mcp_execution_trace，可重复），缺省全量四类。
4. `server/tests/knowledge/test_backfill_learning_cases.py`（test_rebuild_project_context.py 范式）：monkeypatch `aschedule_ingestion` 收集调用 → 造各类存量行若干 → `call_command("backfill_learning_cases")` 断言按类计数与 source_id 正确；`--only learning_case` 只投一类；重复执行两次投递集合相同（命令层幂等——真正的内容幂等由 ingest hash 短路兜底，已在 100-02/03 断言）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/knowledge/test_backfill_learning_cases.py -x -q</automated>
  </verify>
  <done>命令四类全量投递 + --only 过滤 + started/completed 事件；测试全绿；docstring 说明「切换当天检索全空」防线定位（P1）。</done>
</task>

<task type="auto">
  <name>Task 3: golden set 验收门 + 契约/fail-soft/RetrievalTrace 断言</name>
  <files>server/tests/mcp_tools/test_learning_cases.py</files>
  <action>
改写/扩展 `test_learning_cases.py`（既有两用例适配向量路径 + 新增守门组）：

1. **测试基建（文件级 fixture）**：`golden_vector_stack` fixture——monkeypatch `QdrantService.get_client` 返回 `QdrantClient(":memory:")` 本地实例（qdrant-client 内嵌模式，零网络，pytest-socket 安全）+ 确定性假 embedding：`EmbeddingService.generate_embedding`/`generate_embeddings_batch` 用纯函数（如 token 集合 hash 到固定维度 bag-of-words 向量，同文本恒同向量、共享 token 越多余弦越近）+ `SparseEncoderService.encode`/`encode_batch` 用 token-hash 稀疏编码。embedding 维度与 `ensure_delivery_knowledge_collection` 建集合维度对齐（读 `knowledge/collection.py` 的维度来源并 monkeypatch `get_embedding_model_name`/维度配置为测试值）。摄取用真实 `ingest()` 全链路（PG 实体/版本 + 内存 Qdrant 向量点）。
2. **golden set 对照测试（KNOW-02 验收门，locked「含路径/symbol 类查询」）**：造 3-5 条内容判然不同的 McpLearningCase（沿用 `_technical_plan` 工厂扩展：登录超时 bug / 支付回调重试 / 前端构建报错等）→ 逐条 `ingest(IngestionRequest("learning_case", ...))` → 经 mcp_client 走 `/api/mcp/tools/search_learning_cases/`：
   - 中文问题描述 query（"登录超时 token 刷新"）→ results 非空且目标 case 在 top-3；
   - 路径类 query（file_hints=["src/auth/session.py"] + 简短 query）→ 目标 case 在 top-3（hint 增强+提权生效断言：无 hint 时排名 ≤ 有 hint 时排名）;
   - symbol 类 query（symbol_hints 命中造数 symbols 字段）→ 同上；
   - 断言 payload 外形键集 == learning_case_payload 全键集（case_id/title/work_item_type/work_item_id/problem/root_cause/solution/outcome/repositories/files/symbols/branches/mr_urls/tests/source_links/reuse_judgement/created_at/score），score 为 0-1 浮点。
3. **既有用例适配**：`test_create_and_search_learning_case_from_technical_plan` 挂 golden_vector_stack fixture 使创建→摄取→检索真走向量路径（create hook 100-02 已接；注意 aschedule_ingestion 走 on_commit+后台线程，测试内直接 `await ingest(...)` 同步补摄避免竞态）；`score > 0` 断言保留。第二用例（auto includes similar）零改动预期，跑绿确认。
4. **fail-soft 用例**：monkeypatch `DeliveryKnowledgeSearchService.search_similar` 抛 RuntimeError → 走 HTTP 断言 200 + `results == []` + `total == 0`（不 500）。
5. **RetrievalTrace 断言（ROADMAP criterion 5，MCP 链）**：golden set 用例检索后查 `RetrievalTrace.objects.filter(kind="file")`（test_retrieval_trace.py 查询范式）断言每条命中一行 trace 且 payload 含 `source="learning_case"`、`case_id`、`score`；`ToolCallRecord`/`RequestMetric` 行存在（duration_ms 非负）——Chat 链本 phase 只需 service 层可复用（`search_learning_cases(user=...)` 纯 service 签名已满足，白名单接入是 Phase 102 KNOW-05，不做）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/mcp_tools/test_learning_cases.py tests/mcp_tools/test_retrieval_trace.py tests/mcp_tools/test_schema_snapshot.py -x -q</automated>
  </verify>
  <done>golden set 三形态查询全部 top-N 命中；payload 键集/score 语义/fail-soft/RetrievalTrace 断言全绿；既有两用例向量路径下通过。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP 调用方 → query/hints 参数 → 向量检索与回捞 | 外部可控文本进入检索链 |
| PAT user → search_similar 权限闸 | 检索可见范围由 user 决定 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-100-09 | Elevation of Privilege | search_learning_cases 权限 | mitigate | 检索经 search_similar(user=request.user) fail-closed（user=None → 空）；回捞按命中实体 source_id 收口，不额外放大行集 |
| T-100-10 | Information Disclosure | fail-soft 异常日志 | mitigate | 异常文本经 redact_secrets_in_text 后才入日志（Qdrant URL/凭证不泄漏） |
| T-100-11 | Denial of Service | 超采样 top_k=limit*3 | accept | limit 序列化层已限 max（≤20 量级），放大 3 倍仍个位数十位查询开销 |
| T-100-SC | Tampering | 依赖安装 | accept | 零新依赖（qdrant-client :memory: 为既有依赖内嵌模式，仅测试使用） |
</threat_model>

<verification>
- `cd server && uv run pytest tests/mcp_tools/ tests/knowledge/ -q` 全绿（两域整体回归——token 退役影响面收口）。
- `rg -n "_tokens|_TOKEN_RE" server/mcp_tools/learning_case_service.py` 零命中（退役无残留）。
- ROADMAP criterion 1 收口：同一 case 在 `search_delivery_knowledge`（entity_kinds=["learning_case"]，100-01 已通）与 `search_learning_cases`（本 plan）两处可召回——golden set 用例中对同一 query 双工具各断言一次。
</verification>

<success_criteria>
- ROADMAP 成功标准 1（双入口召回同一 case + 统一向量排序）、2（契约不变 + golden set 验收门 + token 退役）、4 后半（Qdrant fail-soft 不 500）、5（RetrievalTrace + 条数/耗时/score，MCP 链全量 + Chat 链 service 可复用）全部成立。
- 三件套（normalizer 100-02/03 + backfill 本 plan + 读切换本 plan）同 phase 闭环（P1 防线）。
</success_criteria>

<output>
完成后创建 `.planning/phases/100-learning-case-mcp/100-04-SUMMARY.md`
</output>
