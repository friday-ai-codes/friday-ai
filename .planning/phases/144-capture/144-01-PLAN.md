---
phase: 144-capture
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - server/tests/mcp_tools/test_search_session_knowledge.py
  - server/tests/mcp_tools/test_get_session_capture.py
  - server/tests/agents/tools/test_search_session_knowledge.py
  - server/tests/knowledge/test_vector_recall.py
  - server/tests/services/test_project_context_packer.py
  - server/tests/mcp_tools/test_lookup_project_by_branch.py
  - server/tests/mcp_tools/test_report_session_knowledge.py
  - server/tests/mcp_tools/test_schema_snapshot.py
  - server/tests/mcp_tools/test_mcp_package_alignment.py
  - mcp/tests/server.test.ts
autonomous: true
requirements: [RECALL-01, RECALL-02, RECALL-03, RECALL-04, OBS-03]
must_haves:
  truths:
    - "Wave 0 契约已按仓库必填、AND 项目、闭集 source_kinds、回放 404、默认分支第三源与双链 RetrievalTrace 落成可收集测试"
    - "新 MCP 工具最终计数锁定为 54（search_session_knowledge + get_session_capture），三面键测试与 vitest 使用同一数字"
    - "本计划只写测试，不改生产代码、不加依赖"
  artifacts:
    - path: "server/tests/mcp_tools/test_search_session_knowledge.py"
      provides: "RECALL-01/OBS-03 MCP 会话检索 RED 契约"
    - path: "server/tests/mcp_tools/test_get_session_capture.py"
      provides: "RECALL-03 只读回放与 404 防枚举 RED 契约"
    - path: "server/tests/agents/tools/test_search_session_knowledge.py"
      provides: "Chat 同 helper、空命中 trace RED 契约"
  key_links:
    - from: "server/tests/mcp_tools/test_search_session_knowledge.py"
      to: "knowledge.session_capture_retrieval.search_session_knowledge"
      via: "monkeypatch 断言共享 helper 而非直连 Qdrant"
      pattern: "search_session_knowledge"
    - from: "mcp/tests/server.test.ts"
      to: "FRIDAY_TOOLS"
      via: "toHaveLength(54)"
      pattern: "toHaveLength\\(54\\)"
---

<objective>
为 Phase 144 建立 tracer-first Wave 0 RED 契约，覆盖 RECALL-01..04 与 OBS-03（D-01 仓库必填 AND 项目、D-02 账本回放、D-03 默认分支第三源、D-04 双链标量 RetrievalTrace）。

Purpose: 实现尚未落地时，错误的 DOCUMENT 混召、packer 排他过滤、Ledger 拼原文、main 误 matched、trace 含 query/正文都无法假绿。
Output: 新增/扩展测试文件可被 pytest/vitest 收集；生产行为仍为 RED。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/144-capture/144-CONTEXT.md
@.planning/phases/144-capture/144-RESEARCH.md
@.planning/phases/144-capture/144-PATTERNS.md
@.planning/phases/144-capture/144-VALIDATION.md
@.planning/phases/142-mcp/142-VERIFICATION.md
@.planning/phases/143-eval/143-VERIFICATION.md
@server/tests/mcp_tools/test_delivery_knowledge_tools.py
@server/tests/mcp_tools/test_lookup_project_by_branch.py
@server/tests/mcp_tools/test_report_session_knowledge.py
@server/tests/knowledge/test_vector_recall.py
@server/tests/services/test_project_context_packer.py
@server/tests/agents/tools/test_knowledge_read_tools.py
@server/tests/mcp_tools/conftest.py
@mcp/tests/server.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MCP 会话检索与 Capture 回放 RED 契约</name>
  <files>server/tests/mcp_tools/test_search_session_knowledge.py, server/tests/mcp_tools/test_get_session_capture.py</files>
  <behavior>
    - 缺 repository_id / 空 query / top_k 越界 → 400；未授权仓 → 200 空 results（D-01）
    - 同时传 repository_id 与 project_id 时 helper 收到 AND 而非 OR；只接受 source_kind=session_capture
    - 空命中仍写一条 RetrievalTrace，payload 无 query/title/text/question/answer/essence；create 抛错仍 HTTP 200（D-04）
    - 创建者可读 Capture question/answer allowlist；他用户/失 scope/无行同一 404 body；Ledger 查询抛错仍能回放（D-02）
  </behavior>
  <action>新增两个测试模块，复用 mcp_client/access_user。search：POST /api/mcp/tools/search_session_knowledge/；用 monkeypatch 捕获 knowledge.session_capture_retrieval.search_session_knowledge 的 kwargs（模块暂不存在时在测试函数内 import，ImportError 记为 RED 而非 skip）。断言不直接调用 DeliveryKnowledgeSearchService.search_similar 于 view 测试中拼 Qdrant。OBS-03：空 results 仍 append 一条 Kind.CHUNK trace；payload 键闭集 source/repository_id/project_id/source_kind/result_count/scores/top_score/duration_ms；monkeypatch RetrievalTrace.objects.create 抛错后业务 JSON 不变且 HTTP 200。get：POST tools/get_session_capture/；用 CaptureService.persist 建行；创建者 200 精确 allowlist（capture_id, question, answer, response_model, provider, input_tokens, output_tokens, session_id, branch_name, repository_id, project_id, link_reason, value_tier, status, created_at, updated_at, evaluated_at, ingested_at, run_id）；禁止 last_error/distilled_essence/question_hash/client。他用户、随机 UUID、创建者但仓/项目不在 resolve_allowed_* 同一 error_code/detail/status=404。monkeypatch ToolCallRecord/RetrievalTrace 的 objects.filter/get 为抛错，创建者回放仍 200。请求前后 status/attempts 不变、无 enqueue。本任务只写测试。</action>
  <verify>
    <automated>cd server && uv run pytest tests/mcp_tools/test_search_session_knowledge.py tests/mcp_tools/test_get_session_capture.py --collect-only -q</automated>
  </verify>
  <done>两文件可收集；对尚未存在的路由/helper 的行为断言处于 RED；无 pytest skip 掩盖缺口。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Chat、向量 filter 与 packer inclusion RED 契约</name>
  <files>server/tests/agents/tools/test_search_session_knowledge.py, server/tests/knowledge/test_vector_recall.py, server/tests/services/test_project_context_packer.py</files>
  <behavior>
    - Chat 工具必填 repository_id、可选 project_id AND、委托同一 search_session_knowledge helper（D-01）
    - source_kinds 非空进入 Qdrant must 的 source_kind MatchAny；None 不加该条件；[] 在 embedding 前返回 []（D-01）
    - packer _layer_rag 传 project_ids=[当前项目]、include_document_kind=True，且不得 exclusive source_kinds=["session_capture"]；session_capture 与既有 project_doc DTO 都能进 RAG（D-01 RECALL-02 Open Q1）
    - Chat 空命中 result_count=0；arecord_retrieval_trace 抛错仍 ToolResult.success=True；payload 无 query/正文（D-04）
  </behavior>
  <action>Chat 测试对齐 test_knowledge_read_tools.py fixtures：无 conversation owner fail-closed；top_k 钳到 20；registry 与 chat_runner._INDEXED_TOOL_NAMES 将包含 search_session_knowledge（当前缺失为 RED）。向量测试扩展 hybrid_calls fixture：断言真实 query_filter.must 含 key=source_kind 的 MatchAny(["session_capture"])；source_kinds=None 路径 must 无 source_kind；source_kinds=[] 时 hybrid_calls 为空。保留 project_id 空串逃生支既有断言。packer：monkeypatch DeliveryKnowledgeSearchService.search_similar，断言 kwargs project_ids 为当前项目单元素列表、include_document_kind is True、source_kinds 为 None 或不存在或非 ["session_capture"] 单元素排他列表；返回带 source_kind=session_capture 的 DTO 与带其它 document source 的 DTO 都能进入 rag 层文本。Open Q1 决议：不发明完整 inclusion frozenset。只写测试。</action>
  <verify>
    <automated>cd server && uv run pytest tests/agents/tools/test_search_session_knowledge.py tests/knowledge/test_vector_recall.py tests/services/test_project_context_packer.py --collect-only -q</automated>
  </verify>
  <done>三处扩展可收集；filter/packer/Chat 新断言在生产未改前 RED。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 默认分支第三源与 MCP 54 工具三面 RED 契约</name>
  <files>server/tests/mcp_tools/test_lookup_project_by_branch.py, server/tests/mcp_tools/test_report_session_knowledge.py, server/tests/mcp_tools/test_schema_snapshot.py, server/tests/mcp_tools/test_mcp_package_alignment.py, mcp/tests/server.test.ts</files>
  <behavior>
    - main/master/develop/仓 default_branch=trunk + 唯一 RepoAssociation → matched false、context 空、有 candidates、不调用 pack_project_context（D-03）
    - feat/login-page 第三源仍 matched true；默认分支上 ProjectBranch 或可解析 work item 仍 matched true
    - report_session_knowledge 在 main + 真实仓 + 唯一 association、未传 project_id 时 accepted、仓库 FK 在、project_id 空、reason 非 branch_unresolved（D-03）
    - FRIDAY_TOOLS / snapshot / URL 集合最终为 54；lookup 响应键含 binding_source（Open Q2）
  </behavior>
  <action>保留 test_repo_association_fallback_single_match 与 test_unparseable_branch_fail_soft 原断言。新增 parametrize 默认分支名。monkeypatch pack_project_context 计数，skip 路径调用次数为 0。lookup unmatched 响应含 binding_source=repo_association_skipped_default_branch。写路径新测试必须创建 Repository + RepoAssociation，不要只传 branch_name=main。将 test_schema_snapshot 中 lookup 响应期望键加入 binding_source（与 144-03 生产 snapshot 对齐；不要把尚未落地的 search/get 写进总表，以免 Wave 1 lookup 守卫被无关键拖红）。另写独立测试函数断言 snapshot 最终必须含 search_session_knowledge 与 get_session_capture 键及固定 request/response 字面量（search request: query, repository_id, project_id, top_k；search response: query, results, total, run_id；get request: capture_id；get response 与回放 allowlist+run_id 一致）——这些函数在 144-04/05 前保持 RED。mcp/tests/server.test.ts 将 toHaveLength(52) 改为 54（全仓库同一数字，禁止 53）。test_mcp_package_alignment 增加两工具 serializer↔snapshot↔npm properties 三面相等（工具尚未存在时断言 RED）。不扩大历史工具字段门禁。只写测试，不改 serializers/tools.ts 生产面。</action>
  <verify>
    <automated>cd server && uv run pytest tests/mcp_tools/test_lookup_project_by_branch.py tests/mcp_tools/test_report_session_knowledge.py tests/mcp_tools/test_schema_snapshot.py tests/mcp_tools/test_mcp_package_alignment.py --collect-only -q</automated>
  </verify>
  <done>lookup/report/三面对齐测试可收集；vitest 因 54 计数与现网 52 不一致而 RED；无生产 schema 改动。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP/Chat 调用方 → 知识检索 | 不可信 query/id，权限必须服务端 intersect |
| 回放 UUID → SessionCapture | IDOR 猜测主键 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-144-01 | Information disclosure | get_session_capture tests | mitigate | Wave 0 钉死同一 404 body |
| T-144-02 | Information disclosure | replay vs Ledger | mitigate | monkeypatch Ledger 查询仍回放 |
| T-144-03 | Information disclosure | source_kinds filter | mitigate | MatchAny 与未授权空结果断言 |
| T-144-04 | Information disclosure | RetrievalTrace payload | mitigate | 禁止 query/正文键 |
| T-144-05 | Tampering | lookup third source | mitigate | 默认分支 unmatched 断言 |
| T-144-06 | Denial of service | trace create boom | mitigate | HTTP 200 / ToolResult.success |
| T-144-SC | Tampering | package installs | accept | 本阶段不安装新包 |
</threat_model>

<verification>
collect-only 成功；本 wave 不要求 pytest 全绿。后续 144-02..05 必须使对应 RED 变绿。
</verification>

<success_criteria>
VALIDATION.md Wave 0 清单中的新测试文件均存在且可收集；FRIDAY_TOOLS 期望计数在测试侧统一为 54。
</success_criteria>

<output>
Create `.planning/phases/144-capture/144-01-SUMMARY.md` when done
</output>
