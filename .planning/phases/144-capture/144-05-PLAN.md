---
phase: 144-capture
plan: "05"
type: execute
wave: 3
depends_on: ["144-04"]
files_modified:
  - server/initiatives/services/capture_access.py
  - server/initiatives/services/__init__.py
  - server/tests/initiatives/test_capture_access.py
  - server/mcp_tools/views.py
  - server/mcp_tools/serializers.py
  - server/mcp_tools/urls.py
  - mcp/src/tools.ts
  - mcp/tests/server.test.ts
  - server/tests/mcp_tools/test_get_session_capture.py
  - server/tests/mcp_tools/test_get_session_capture_schema_pending.py
  - server/tests/mcp_tools/test_schema_snapshot.py
  - server/tests/mcp_tools/test_mcp_package_alignment.py
autonomous: true
requirements: [RECALL-03]
must_haves:
  truths:
    - "创建者且挂钩 scope 仍可见时可按 Capture UUID 只读回放脱敏 question/answer（D-02）"
    - "不存在与未授权返回同一 404，不泄漏存在性；非创建者即使仓成员也不可回放原文"
    - "回放正文只读 SessionCapture，不查询 Ledger；无状态推进/评估/入图"
    - "回放响应固定省略 client：SessionCapture 无该字段，禁止 null 占位、猜测或读取 Ledger 补齐"
    - "get_session_capture 与 search_session_knowledge 一起使 MCP 工具面为 54，三面键对齐"
  artifacts:
    - path: "server/initiatives/services/capture_access.py"
      provides: "aget_readable_capture"
      exports: ["aget_readable_capture"]
    - path: "server/mcp_tools/views.py"
      provides: "GetSessionCaptureView"
    - path: "mcp/src/tools.ts"
      provides: "get_session_capture 只读工具"
  key_links:
    - from: "GetSessionCaptureView.post"
      to: "initiatives.services.capture_access.aget_readable_capture"
      via: "None → 404 资源不存在"
      pattern: "aget_readable_capture"
    - from: "capture_access.py"
      to: "initiatives.SessionCapture"
      via: "select_related get，无 objects.create"
      pattern: "SessionCapture.objects"
---

<objective>
落地按 Capture id 只读回放（D-02，RECALL-03 Discretion：MCP 工具名 get_session_capture），并把第二枚 MCP 工具冻结到 serializer/snapshot/npm，使工具总数为 54。

Purpose: 授权用户能看原始结构化问答而不把 Ledger 当正文、不把仓成员扩大为回放主体。
Output: capture_access、GetSessionCaptureView、三面 schema、测试全绿。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/144-capture/144-PATTERNS.md
@server/initiatives/models/session_capture.py
@server/initiatives/services/capture_service.py
@server/initiatives/services/__init__.py
@server/mcp_tools/views.py
@server/knowledge/access_scope.py
@mcp/src/tools.ts
@server/tests/mcp_tools/test_get_session_capture.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 只读授权 aget_readable_capture</name>
  <files>server/initiatives/services/capture_access.py, server/initiatives/services/__init__.py, server/tests/initiatives/test_capture_access.py</files>
  <behavior>
    - 无行 → None
    - superuser → 可读
    - 普通用户必须 initiated_by_user_id == str(user.id)；有 repository/project FK 时还须仍在 resolve_allowed_* 内
    - 双 FK 空：仅创建者
    - 非创建者一律 None（不因仓成员放宽）
    - 无 persist/enqueue/CAS/objects.create
  </behavior>
  <action>新增 capture_access.py：aget_readable_capture(capture_id, user) -> SessionCapture | None。用 filter(pk=...).select_related("repository","project").afirst()。非法 UUID 由 serializer 挡在 view；本函数对缺失返回 None。按 PATTERNS 顺序校验。从 initiatives.services 显式 re-export。禁止 import ToolCallRecord/RetrievalTrace/arecord_tool_call。INV-6：本文件不得 SessionCapture.objects.create/update/delete。同步新增 tests/initiatives/test_capture_access.py：创建者无挂钩可读、他用户 None、superuser 可读、有仓但 allowed 空则创建者 None、不调用 Ledger。</action>
  <verify>
    <automated>cd server && uv run pytest tests/initiatives/test_capture_access.py -x --tb=short</automated>
  </verify>
  <done>aget_readable_capture 单测绿；无 MCP 路由也可独立验收授权函数。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 冻结 MCP get_session_capture 并收口 54 工具面</name>
  <files>server/mcp_tools/serializers.py, server/mcp_tools/views.py, server/mcp_tools/urls.py, mcp/src/tools.ts, mcp/tests/server.test.ts, server/tests/mcp_tools/test_get_session_capture_schema_pending.py, server/tests/mcp_tools/test_schema_snapshot.py, server/tests/mcp_tools/test_mcp_package_alignment.py</files>
  <behavior>
    - 创建者+可见挂钩 200 allowlist；他用户/失 scope/无行同一 404 not_found 资源不存在
    - 响应无 last_error/distilled_essence/question_hash/client/CoT；client 因 SessionCapture 无字段而固定省略，禁止 null/猜测/Ledger 补齐
    - 只读：不 enqueue、不改 status/attempts
    - FRIDAY_TOOLS 长度 54；serializer/snapshot/npm/URL 名集一致；get 与 search 均为 query annotations
  </behavior>
  <action>GetSessionCaptureRequestSerializer：capture_id UUID 必填。snapshot 在 Plan 04 的 53 工具完整字典上只增加 get_session_capture，并同步 test_schema_snapshot.py 完整 expected dict；保留 search_session_knowledge 与 lookup binding_source。将 test_get_session_capture_schema_pending.py 的 get-only RED 断言转为绿色独立契约（或在全字典已覆盖后改为精确响应字段/client 省略守卫），不得删除其验证意图。GetSessionCaptureView：_begin/_validate 后 await aget_readable_capture；None 则 error_response("not_found", "资源不存在", status_code=404)，与缺失同一文案。成功显式构造 allowlist（含 run_id），datetime isoformat；响应固定省略 client，因为 SessionCapture 不存在该字段；禁止返回 null、猜测或读取 ToolCallRecord/Interaction Ledger 补齐。_record(traces=[])。urls tools/get_session_capture/。npm 仅 capture_id required，query(...) annotations。mcp/tests/server.test.ts 从 Plan 04 的 53 精确改为 54，并增加 get 发现性/只读 annotations 断言。test_mcp_package_alignment.py 在既有 search 三面对齐上增加 get 三面对齐。禁止 Vue 页面。禁止扫描 Ledger。静态：回放模块源码不含 ToolCallRecord 查询。</action>
  <verify>
    <automated>cd server && uv run pytest tests/mcp_tools/test_get_session_capture.py tests/mcp_tools/test_get_session_capture_schema_pending.py tests/mcp_tools/test_schema_snapshot.py tests/mcp_tools/test_mcp_package_alignment.py tests/mcp_tools/test_search_session_knowledge.py -q --tb=short && cd ../mcp && npm test -- tests/server.test.ts</automated>
  </verify>
  <done>RECALL-03 与 MCP 54 三面对齐全绿；search 工具无回归。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Capture UUID → 原文问答 | 比 RAG 精华更敏感，仅创建者∩挂钩 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-144-01 | Information disclosure | GetSessionCaptureView | mitigate | 统一 404，禁止 403 分流 |
| T-144-02 | Information disclosure | capture_access / view | mitigate | 只读 SessionCapture allowlist；测试 monkeypatch Ledger |
| T-144-SC | Tampering | npm 白名单 | mitigate | get 工具四面同计划冻结；计数 54 |
</threat_model>

<verification>
VALIDATION Full suite command 中本阶段文件应可在执行波结束后全绿。本计划至少 get + schema + alignment + npm。
</verification>

<success_criteria>
授权创建者可回放原文；未授权不可枚举；MCP 发现面 54 且只读 annotations。
</success_criteria>

<output>
Create `.planning/phases/144-capture/144-05-SUMMARY.md` when done
</output>
