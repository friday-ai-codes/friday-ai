---
phase: 144-capture
plan: "04"
type: execute
wave: 2
depends_on: ["144-01", "144-02", "144-03"]
files_modified:
  - server/mcp_tools/views.py
  - server/mcp_tools/serializers.py
  - server/mcp_tools/urls.py
  - server/agents/tools/knowledge_read_tools.py
  - server/agents/chat_runner.py
  - mcp/src/tools.ts
  - mcp/tests/server.test.ts
  - server/tests/mcp_tools/test_search_session_knowledge.py
  - server/tests/mcp_tools/test_schema_snapshot.py
  - server/tests/mcp_tools/test_mcp_package_alignment.py
  - server/tests/agents/tools/test_search_session_knowledge.py
autonomous: true
requirements: [RECALL-01, OBS-03]
must_haves:
  truths:
    - "授权用户可按必填 repository_id 检索已入图 session_capture；可选 project_id 只做 AND 收窄（D-01）"
    - "MCP 与 Chat 只委托 search_session_knowledge helper，不各写 Qdrant filter（D-04）"
    - "两链空命中也写 result_count=0 的标量 RetrievalTrace；写入失败不改变检索结果（D-04，OBS-03）"
    - "search_session_knowledge 在 serializer、TOOL_SCHEMA_SNAPSHOT、urls、npm FRIDAY_TOOLS 与 query annotations 四面同时出现"
    - "Plan 04 完成时 npm 工具数独立为 53，schema 全字典只增加 search_session_knowledge；get_session_capture 仍由独立 pending RED 文件等待 Plan 05"
  artifacts:
    - path: "server/mcp_tools/views.py"
      provides: "SearchSessionKnowledgeView"
    - path: "mcp/src/tools.ts"
      provides: "search_session_knowledge 只读工具"
    - path: "server/agents/tools/knowledge_read_tools.py"
      provides: "Chat search_session_knowledge"
  key_links:
    - from: "SearchSessionKnowledgeView.post"
      to: "knowledge.session_capture_retrieval.search_session_knowledge"
      via: "await helper"
      pattern: "search_session_knowledge"
    - from: "SearchSessionKnowledgeView.post"
      to: "McpToolView._record"
      via: "traces=一条 CHUNK 标量 payload"
      pattern: "_record"
    - from: "agents.tools.knowledge_read_tools.search_session_knowledge"
      to: "_record_chat_retrieval"
      via: "source=chat_search_session_knowledge"
      pattern: "chat_search_session_knowledge"
---

<objective>
提供 MCP 与 Chat 会话知识检索入口：仓库必填、项目可选 AND、闭集 session_capture，并写 best-effort 脱敏 RetrievalTrace（D-01，D-04；已决议新增 Chat 工具而非扩展 search_project_context）。

Purpose: 已入图精华可按仓找回；观测失败不得 500。
Output: SearchSessionKnowledgeView、Chat @tool、npm/search schema 冻结；本计划独立达到 53 工具并全绿，get_session_capture 留给 144-05。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/144-capture/144-PATTERNS.md
@server/mcp_tools/views.py
@server/mcp_tools/serializers.py
@server/mcp_tools/urls.py
@server/agents/tools/knowledge_read_tools.py
@server/agents/chat_runner.py
@mcp/src/tools.ts
@server/knowledge/session_capture_retrieval.py
@server/knowledge/exposure.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 冻结 MCP search_session_knowledge</name>
  <files>server/mcp_tools/serializers.py, server/mcp_tools/views.py, server/mcp_tools/urls.py, mcp/src/tools.ts, mcp/tests/server.test.ts, server/tests/mcp_tools/test_schema_snapshot.py, server/tests/mcp_tools/test_mcp_package_alignment.py</files>
  <behavior>
    - 缺 repository_id/空 query/top_k 越界 400；未授权仓 200 空 results
    - 调用 helper 传入 repository_id 与可选 project_id
    - 基础设施异常 200 空 results（mcp_vector_search_degraded）
    - 空结果也 _record 一条 CHUNK trace；payload 无 query/title/text/question/answer/essence；create 失败仍 200
  </behavior>
  <action>新增 SearchSessionKnowledgeRequestSerializer：query 必填 max 4000；repository_id UUID 必填；project_id UUID 可选 allow_null；top_k 1..20 默认 5。TOOL_SCHEMA_SNAPSHOT 增加且只增加 search_session_knowledge 键（request/response 与契约字面量一致）；同步更新 test_schema_snapshot.py 的完整 expected dict，使其此时包含 search 而仍不包含 get，Plan 03 的 lookup binding_source 必须保留。SearchSessionKnowledgeView 沿用 McpToolView _begin/_validate/_record；只 await search_session_knowledge；serialize_search_results；output 含 query/results/total/run_id。traces 始终恰好一条 RetrievalTrace.Kind.CHUNK，字段闭集：source=mcp_search_session_knowledge、repository_id、project_id（无则 ""）、source_kind=session_capture、result_count、scores、top_score（空则 0）、duration_ms。禁止复制 SearchDeliveryKnowledgeView 逐条 title。urls 增加 tools/search_session_knowledge/ 并在 views import 列表登记。mcp/src/tools.ts：required ['repository_id','query']；TOOL_ANNOTATIONS 用 query(...) 只读；mcp/tests/server.test.ts 工具计数从 52 精确改为 53 并增加 search 发现性/只读 annotations 断言。test_mcp_package_alignment.py 只增加 search serializer↔snapshot↔npm properties 三面对齐。不要触碰独立 test_get_session_capture_schema_pending.py，该文件在本计划仍是预期 RED 且不纳入 verify。本任务不要加入 get_session_capture。不新增 npm/Python 依赖。structlog 异常走 redact_secrets_in_text；category=caller；component=mcp_tools。不新增 CallSource。</action>
  <verify>
    <automated>cd server && uv run pytest tests/mcp_tools/test_search_session_knowledge.py tests/mcp_tools/test_retrieval_trace.py tests/mcp_tools/test_schema_snapshot.py tests/mcp_tools/test_mcp_package_alignment.py -x --tb=short && cd ../mcp && npm test -- tests/server.test.ts</automated>
  </verify>
  <done>RECALL-01/OBS-03 MCP、完整 snapshot、package alignment 与 npm 53 工具全部独立全绿；get-only pending RED 未被误纳入。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Chat search_session_knowledge 薄封装</name>
  <files>server/agents/tools/knowledge_read_tools.py, server/agents/chat_runner.py</files>
  <behavior>
    - 无会话 owner fail-closed；缺 repository_id fail-closed
    - 与 MCP 同一 helper 与 AND 语义；挂 _INDEXED_TOOL_NAMES 不挂 _PROJECT_READ_TOOL_NAMES（已决议）
    - 空命中仍 _record_chat_retrieval result_count=0；arecord 抛错仍 success=True
    - payload source=chat_search_session_knowledge，无 query/正文
  </behavior>
  <action>在 knowledge_read_tools.py 增加 @tool search_session_knowledge，参数 query/repository_id/project_id/top_k/conversation_id；top_k 钳 1..20；_resolve_conversation_user；委托 search_session_knowledge helper（注意命名别与 tool 函数递归，import as _search_session_knowledge）。异常 redact 后 logger 与 ToolResult。完成后 logger.info 事件 search_session_knowledge_completed，kv duration_ms/result_count/repository_id，category=caller，component=agents.tools。更新 __all__。chat_runner.py 已 import knowledge_read_tools 副作用注册；把 search_session_knowledge 追加进 _INDEXED_TOOL_NAMES（靠近 search_delivery_knowledge）。禁止改 search_project_context 契约。禁止 Chat 直调 recall_similar_chunks。</action>
  <verify>
    <automated>cd server && uv run pytest tests/agents/tools/test_search_session_knowledge.py tests/mcp_tools/test_search_session_knowledge.py -x --tb=short</automated>
  </verify>
  <done>Chat/MCP 过滤与 trace 口径一致；观测失败不改 ToolResult。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP token / Chat LLM → 检索 | 仓/项目 id 不可信，必须 access_scope |
| RetrievalTrace payload → Ledger | 禁止正文与 query |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-144-03 | Information disclosure | SearchSessionKnowledgeView | mitigate | helper+allowed 空则 []，不 403  enumeration 向量点 |
| T-144-04 | Information disclosure | traces payload | mitigate | 字段闭集，redact_for_ledger 二道门 |
| T-144-06 | Denial of service | _record / _record_chat_retrieval | mitigate | arecord 已吞异常；view 在 _record 前组装 traces 无 IO |
| T-144-SC | Tampering | npm 工具白名单 | mitigate | 本工具 serializer/snapshot/urls/tools.ts 同计划落地 |
</threat_model>

<verification>
search MCP + Chat pytest 绿；完整 snapshot/package alignment 与 npm 测试在恰好 53 工具时全绿。get-only pending RED 文件不属于本波 verify。
</verification>

<success_criteria>
用户可按仓库检索 session_capture；MCP/Chat 同源；空命中可观测。
</success_criteria>

<output>
Create `.planning/phases/144-capture/144-04-SUMMARY.md` when done
</output>
