---
phase: 82-project-workspace-entity
plan: 05
type: execute
wave: 3
depends_on: ["82-02"]
files_modified:
  - server/initiatives/services/project_service.py
  - server/initiatives/views.py
  - server/initiatives/serializers.py
  - server/initiatives/urls.py
  - server/audit/services/taxonomy.py
  - server/tests/initiatives/test_project_workspace_rest.py
autonomous: true
requirements: [WS-03, DOC-02]
must_haves:
  truths:
    - "ProjectService.update 白名单含 visibility（可经 PATCH 改可见性）"
    - "feishu_folder_token 不在 update 白名单（仅后台 set_folder_token 可写）"
    - "项目可改归到其他空间（rehome_space 专用方法 + 审计，非塞进 update 白名单）"
    - "ProjectDoc 列表可读、可一键重建（rebuild 调 ProjectDocService.rebuild_workspace）"
    - "ProjectStateApi 可列表 + 手动增删（写经 ProjectDocService，成员/admin 闸）"
    - "新 REST 入口经统一中间件自动纳入 RequestMetric；写操作 AuditService.aemit 归因"
  artifacts:
    - path: "server/initiatives/views.py"
      provides: "ProjectDoc 列表/重建、ProjectStateApi CRUD、空间改归 视图"
      contains: "class ProjectWorkspaceDocsView"
    - path: "server/initiatives/services/project_service.py"
      provides: "update 白名单 +visibility；rehome_space 专用方法"
      contains: "def rehome_space"
    - path: "server/initiatives/urls.py"
      provides: "workspace docs / state-apis / rehome / rebuild 路由"
      contains: "state-apis"
  key_links:
    - from: "server/initiatives/views.py"
      to: "ProjectDocService.rebuild_workspace / upsert_state_api / remove_state_api"
      via: "REST 写经单一 service（INV-6）"
      pattern: "rebuild_workspace|upsert_state_api"
    - from: "server/initiatives/views.py"
      to: "ProjectService.rehome_space / update(visibility)"
      via: "改归 + 可见性写经 ProjectService"
      pattern: "rehome_space"
---

<objective>
补齐工作区的 REST 写/读入口与空间改归（WS-03 + DOC-02 的结构化 API 清单入口）：① `ProjectService.update` 白名单加 `visibility`（PATCH 改可见性），`feishu_folder_token` 维持仅后台 `set_folder_token`（不 PATCH）；② 新增 `rehome_space` 专用方法（owner/admin 闸 + 审计，不塞进 update 白名单，per Pitfall 3）改项目所属空间；③ ProjectDoc 列表 + 一键重建端点（调 82-02 的 `rebuild_workspace`，兜底飞书首建失败的 broken）；④ ProjectStateApi 列表 + 手动增删 REST（写经 `ProjectDocService`，DOC-02 的结构化 API 清单写入口 + 最小 REST，Cursor/hook 结构化回写留 Phase 86）。

Purpose: 兑现 WS-03（项目↔空间解绑/改归）与 DOC-02（STATE 结构化「已完成 API 清单」写入口 + 最小 REST）。visibility PATCH 让前端/用户翻转可见性。
Output: update 白名单 +visibility + rehome_space + 4 组 REST 端点 + serializers/urls + 新审计 action + REST 守护测试。
</objective>

<execution_context>
@.cursor/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/82-project-workspace-entity/82-CONTEXT.md
@.planning/phases/82-project-workspace-entity/82-RESEARCH.md
@.planning/phases/82-project-workspace-entity/82-02-PLAN.md

@server/initiatives/views.py
@server/initiatives/urls.py
@server/initiatives/serializers.py
@server/initiatives/services/project_service.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ProjectService update 白名单 +visibility + rehome_space 专用方法 + 审计</name>
  <files>
    server/initiatives/services/project_service.py,
    server/audit/services/taxonomy.py,
    server/tests/initiatives/test_project_workspace_rest.py
  </files>
  <read_first>
    - server/initiatives/services/project_service.py（被改文件：`update` 白名单 line 182、`_update_locked` line 205-216；`change_status`/`_change_status_locked` 审计范式 line 220-289 供 rehome 镜像）
    - server/audit/services/taxonomy.py（被改文件：常量 + __all__ + ALL_ACTIONS 三处登记；82-02 已加 workspace_provisioned/state_api_* 常量）
    - 82-RESEARCH.md「Pitfall 3」（visibility 入 update 白名单；feishu_folder_token 走专用 set_folder_token 不暴露 PATCH；space 改归走专用方法含审计，不塞 update 白名单；Project.space on_delete=CASCADE 非空，改归只换 FK 值）
    - server/initiatives/models/project.py（Project.space FK line 40-45；visibility 字段 82-01 落地）
  </read_first>
  <behavior>
    - update 接受 visibility（其余白名单不变）；feishu_folder_token 经 update 传入被忽略（不在白名单）
    - rehome_space(project_id, new_space_id) → 换 Project.space FK + 审计 space_rehomed；new_space 不存在 fail-loud
    - 改归幂等：同空间不审计（changed=False）
  </behavior>
  <action>
    在 ProjectService.update 的 `allowed` 集合加入 `"visibility"`（line 182）——其余不变；确认 `feishu_folder_token` **不**加入（仅 set_folder_token 后台写，per Pitfall 3）。

    新增 `async def rehome_space(self, *, project_id, new_space_id, actor=None, initiated_by_user_id=None) -> Project` + `@sync_to_async _rehome_space_locked`：select_for_update 取 project，校验 new_space 存在（不存在抛 `ProjectTransitionError` 或新 `ProjectMemberError`——复用既有异常或加一个 `ProjectRehomeError`，API 转 400/404）；同空间 → changed=False 幂等；否则换 `project.space_id = new_space_id` + save(update_fields=["space","updated_at"])。审计 `AuditService.aemit(action=ACTION_PROJECT_SPACE_REHOMED, before={"space_id": old}, after={"space_id": new}, metadata={component:"initiatives", category:"caller", initiated_by_user_id}, source="api")` + apush_project_event。

    在 taxonomy.py 三处登记 `ACTION_PROJECT_SPACE_REHOMED="project.space_rehomed"` 与 `ACTION_PROJECT_WORKSPACE_REBUILT="project.workspace_rebuilt"`（后者供 Task 2 重建端点）。

    新建 tests/initiatives/test_project_workspace_rest.py（本 task 先覆盖 service 半）：断言 update 接受 visibility 落字段；update 传 feishu_folder_token 被忽略（字段不变）；rehome_space 换空间 + 同空间幂等 + new_space 不存在 fail-loud。
  </action>
  <acceptance_criteria>
    - <automated>cd server && uv run pytest tests/initiatives/test_project_workspace_rest.py -k "visibility or rehome" -x</automated> 全绿。
    - 测试断言：update(visibility="members_only") 后 project.visibility=="members_only"；update(feishu_folder_token="x") 后字段未变（不在白名单）。
    - 测试断言：rehome_space 到新空间后 project.space_id 变更且审计 space_rehomed 一条；同空间 changed=False 不审计。
    - grep 断言 ACTION_PROJECT_SPACE_REHOMED / ACTION_PROJECT_WORKSPACE_REBUILT 出现在 taxonomy 三处（__all__/定义/ALL_ACTIONS）。
  </acceptance_criteria>
  <done>visibility 可经 update 改、folder_token 不可 PATCH、rehome_space 专用方法 + 审计就位。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ProjectDoc 列表/重建 + 空间改归 + visibility PATCH REST 端点</name>
  <files>
    server/initiatives/views.py,
    server/initiatives/serializers.py,
    server/initiatives/urls.py,
    server/tests/initiatives/test_project_workspace_rest.py
  </files>
  <read_first>
    - server/initiatives/views.py（被改文件：adrf APIView 范式 + `_aget_project_for_read`/`_aget_project_for_write` 权限助手 line 403-428；ProjectDetailView.patch line 183-203 范式；ProjectTransitionView line 206-233 写端点范式）
    - server/initiatives/serializers.py（被改文件：ProjectSerializer fields line 47-64、ProjectUpdateSerializer line 89-99——加 visibility；Serializer 风格）
    - server/initiatives/urls.py（被改文件：路由风格 line 28-127，字面段须在 router/通配之前——本文件全 path 显式）
    - server/initiatives/services/project_doc_service.py（82-02：rebuild_workspace 签名）
    - 82-RESEARCH.md「Pitfall 7」（视图 Project.objects.select_related("space")）「观测与日志强制项」（REST 入口自动 RequestMetric）
  </read_first>
  <action>
    serializers.py：ProjectSerializer fields 增 `visibility`、`feishu_folder_token`（read_only_fields 同步——展示用）。ProjectUpdateSerializer 增 `visibility = ChoiceField(required=False, choices=ProjectVisibility.choices)`。新增 `ProjectDocSerializer`(ModelSerializer：id/project_id/doc_type/feishu_document_id/feishu_doc_token/sync_status/last_synced_revision/created_at/updated_at，全 read_only) 与 `ProjectRehomeSerializer`(new_space_id=UUIDField)。

    views.py：
    - 新 `ProjectWorkspaceDocsView`(GET 列出项目 5 文件容器——读权限 `_aget_project_for_read`，序列化 ProjectDoc) 。
    - 新 `ProjectWorkspaceRebuildView`(POST 一键重建——写权限 `_aget_project_for_write`(Space admin+)，调 `await ProjectDocService().rebuild_workspace(project_id=, initiated_by_user_id=request.user.id)`，返回 202/200)。
    - 新 `ProjectRehomeView`(POST 改归空间——写权限 admin+，校验 new_space 存在否则 404，调 `ProjectService().rehome_space(...)`，异常转 400)。
    - visibility PATCH 复用既有 `ProjectDetailView.patch`（已走 ProjectUpdateSerializer → ProjectService.update，本 task 已把 visibility 入白名单与 serializer，无需新端点）。

    urls.py 追加：`<uuid:project_id>/workspace/docs/`(docs list)、`<uuid:project_id>/workspace/rebuild/`(rebuild)、`<uuid:project_id>/rehome/`(rehome)。

    扩 test_project_workspace_rest.py：端到端断言 docs list 返回 5 容器、rebuild 端点触发 rebuild_workspace（mock service 断言被调）、rehome 端点改空间、visibility PATCH 经 ProjectDetailView 改 visibility、非 admin 写端点 403、非成员/非 viewer 读 docs 403。
  </action>
  <acceptance_criteria>
    - <automated>cd server && uv run pytest tests/initiatives/test_project_workspace_rest.py -x</automated> 全绿。
    - 测试断言：GET workspace/docs 返回项目全部 ProjectDoc；非授权用户 403。
    - 测试断言：POST workspace/rebuild 以 admin 触发 rebuild_workspace（mock 断言调用 + initiated_by_user_id 透传）；非 admin 403。
    - 测试断言：POST rehome 改 space 成功、new_space 不存在 404、非 admin 403。
    - 测试断言：PATCH 项目 visibility=members_only 生效（经既有 ProjectDetailView）。
  </acceptance_criteria>
  <done>ProjectDoc 列表/重建 + 空间改归 + visibility PATCH REST 端点就位，权限 admin/viewer 闸正确。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ProjectStateApi 列表 + 手动增删 REST（DOC-02 结构化 API 清单写入口）</name>
  <files>
    server/initiatives/views.py,
    server/initiatives/serializers.py,
    server/initiatives/urls.py,
    server/tests/initiatives/test_project_workspace_rest.py
  </files>
  <read_first>
    - server/initiatives/views.py（被改文件：写端点权限 `_aget_project_for_write` line 403-413；ProjectMemoryListCreateView line 678-717 的 list+create 范式 / ProjectMemoryDetailView delete 范式）
    - server/initiatives/serializers.py（被改文件：ProjectMemoryCreateSerializer / Choice 字段范式）
    - server/initiatives/services/project_doc_service.py（82-02：upsert_state_api/remove_state_api 签名 + 审计）
    - server/initiatives/models/project_state_api.py（82-01：method/path/params/status/source + ApiStatus/ApiSource）
    - 82-RESEARCH.md「Open Questions / ProjectStateApi 写入来源」（本期建模型 + service 写入入口 + 最小 REST 列表/手动增删；Cursor/hook 结构化回写留 Phase 86）
  </read_first>
  <behavior>
    - GET state-apis → 列出项目结构化 API 清单（读权限）
    - POST state-apis → 手动新增（写权限），写经 ProjectDocService.upsert_state_api（成员/admin 闸）
    - DELETE state-apis/{id} → 移除，写经 ProjectDocService.remove_state_api；不存在 404
  </behavior>
  <action>
    serializers.py：新增 `ProjectStateApiSerializer`(ModelSerializer 响应：id/project_id/method/path/params/status/source/created_at/updated_at read_only) 与 `ProjectStateApiCreateSerializer`(method=CharField、path=CharField、params=JSONField/DictField required=False default=dict、status=ChoiceField(ApiStatus) default planned、source 固定 manual 由 view 注入或 ChoiceField default manual)。

    views.py：新 `ProjectStateApiListCreateView`(GET list 读权限；POST create 写权限 `_aget_project_for_write`，调 `ProjectDocService().upsert_state_api(project_id=, method=, path=, params=, status=, source="manual", actor=request.user, initiated_by_user_id=request.user.id)`) 与 `ProjectStateApiDetailView`(DELETE 写权限，调 `remove_state_api`，不存在 404)。写入一律经 ProjectDocService（INV-6），view 不直接写表。

    urls.py 追加：`<uuid:project_id>/workspace/state-apis/`(list/create)、`<uuid:project_id>/workspace/state-apis/<uuid:api_id>/`(delete)。

    扩 test_project_workspace_rest.py：断言 POST 新增一条 state api（method/path/status 落库 + 唯一约束幂等：重复同 method+path 返回既有/409）、GET 列出、DELETE 移除、非 admin 写 403、写经 ProjectDocService（INV-6 不旁路）。
  </action>
  <acceptance_criteria>
    - <automated>cd server && uv run pytest tests/initiatives/test_project_workspace_rest.py -k "state_api or stateapi" -x</automated> 全绿。
    - <automated>cd server && uv run pytest tests/initiatives/test_project_doc_inv6_guard.py -x</automated> 仍绿（REST 写不旁路 ProjectStateApi）。
    - 测试断言：POST state-api 落 method/path/status；重复 (method,path) 幂等（返回既有或 409，不重复建）；非 admin 403；DELETE 不存在 404。
    - <automated>cd server && uv run python manage.py makemigrations --check --dry-run</automated> 干净（本 task 无模型变更）。
  </acceptance_criteria>
  <done>ProjectStateApi 列表/手动增删 REST 就位，写经 ProjectDocService（INV-6），DOC-02 结构化 API 清单写入口可用。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 客户端 → REST 写端点 | rehome / visibility / state-api 增删 / 重建 均须 Space admin+ 或成员闸 fail-closed |
| REST 写 → DB | 写经 ProjectService/ProjectDocService 单一入口（INV-6），view 不直接写表 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-16 | Elevation of Privilege | 非成员/非 admin 改可见性/改归/写 StateApi | mitigate | 写端点走 `_aget_project_for_write`(Space admin+)；测试断言非 admin 403；写经 service（成员/admin 闸不变） |
| T-82-17 | Tampering | feishu_folder_token 被用户 PATCH 篡改 | mitigate | folder_token 不在 update 白名单，仅后台 set_folder_token 可写 |
| T-82-18 | Input Validation | method/path/status/visibility 非法值 | mitigate | DRF serializer 校验（ChoiceField 闭集 + 必填）；非法 → 400 |
| T-82-19 | Tampering | REST 旁路写 ProjectStateApi 绕过审计 | mitigate | 写经 ProjectDocService；INV-6 guard（82-02）回归断言不旁路 |
| T-82-20 | Repudiation | 写操作无归因 | mitigate | AuditService.aemit(initiated_by_user_id=request.user.id, category=caller, component=initiatives)；REST 入口自动入 RequestMetric |
| T-82-21-SC | Tampering | 包安装 | accept | 本 phase 不安装任何外部包 |
</threat_model>

<verification>
- `cd server && uv run pytest tests/initiatives/test_project_workspace_rest.py tests/initiatives/test_project_doc_inv6_guard.py -x` 全绿。
- `cd server && uv run pytest tests/initiatives/ -x` 既有用例零回归。
- `cd server && uv run python manage.py makemigrations --check --dry-run` 干净。
</verification>

<success_criteria>
- WS-03：项目可改归到其他空间（rehome_space + REST）；visibility 可 PATCH 翻转。
- DOC-02：ProjectStateApi 结构化 API 清单写入口 + 最小 REST（列表/手动增删）；活计算派生渲染留 Phase 83/84。
- ProjectDoc 列表 + 一键重建（兜底 broken）。
</success_criteria>

<output>
完成后创建 `.planning/phases/82-project-workspace-entity/82-05-SUMMARY.md`。
</output>
