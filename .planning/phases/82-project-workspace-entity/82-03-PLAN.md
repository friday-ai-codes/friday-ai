---
phase: 82-project-workspace-entity
plan: 03
type: execute
wave: 2
depends_on: ["82-01"]
files_modified:
  - server/services/project_context_packer.py
  - server/knowledge/access_scope.py
  - server/tests/services/test_project_context_packer.py
  - server/tests/test_chat_project_recall.py
autonomous: true
requirements: [WS-02]
must_haves:
  truths:
    - "public_org 项目的非成员可被召回打包（读放行）"
    - "members_only 项目的非成员零召回零泄漏（维持 fail-closed）"
    - "成员在两种可见性下均可召回（不回退）"
    - "写路径成员闸一字不动（非成员写记忆/StateApi/成员/文件仍被拒）"
    - "access_scope 把 public_org 项目并入可读集合，但 caller intersect 收窄语义不被破坏"
    - "召回放宽后仍写 RetrievalTrace（含 visibility 标记），非成员命中可归因"
  artifacts:
    - path: "server/services/project_context_packer.py"
      provides: "visibility 感知的读放行（is_member OR visibility==public_org）"
      contains: "public_org"
    - path: "server/knowledge/access_scope.py"
      provides: "public_org 项目并入 allowed_project_ids"
      contains: "public_org"
    - path: "server/tests/services/test_project_context_packer.py"
      provides: "对称守护：public_org 非成员可读 / members_only 非成员零召回 / 成员可读"
      contains: "members_only"
  key_links:
    - from: "server/services/project_context_packer.py"
      to: "initiatives.models.ProjectVisibility"
      via: "按 project.visibility 分流放行"
      pattern: "ProjectVisibility|visibility"
    - from: "server/knowledge/access_scope.py"
      to: "Project.visibility == public_org"
      via: "allowed 集合并入 public_org 项目"
      pattern: "public_org"
---

<objective>
权限翻转（WS-02 的"读"半）：把 3 处现存 fail-closed 召回闸改为 **visibility 感知**——`is_member OR project.visibility == public_org` 才放行读/召回，`members_only` 维持 fail-closed 零泄漏。写路径成员闸（`MemoryService._assert_member` 等）**一字不动**。补对称守护测试（public_org 非成员可读、members_only 非成员零召回、任意 visibility 非成员写被拒）。

Purpose: 兑现 Success Criteria 1 的权限语义——非成员可读可发起会话、写仅成员、召回 scope 随 visibility、脱敏不可绕过。本 plan 只动"读/召回放宽"，写闸与 REST 端点（visibility PATCH/rehome）分别在 82-05 落地。
Output: packer + access_scope 的 visibility 分流 + 对称守护测试。
</objective>

<execution_context>
@.cursor/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/82-project-workspace-entity/82-CONTEXT.md
@.planning/phases/82-project-workspace-entity/82-RESEARCH.md
@.planning/phases/82-project-workspace-entity/82-01-PLAN.md

@server/services/project_context_packer.py
@server/knowledge/access_scope.py
@server/chat/config.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: pack_project_context 改 visibility 感知（packer 是真闸）</name>
  <files>
    server/services/project_context_packer.py,
    server/tests/services/test_project_context_packer.py
  </files>
  <read_first>
    - server/services/project_context_packer.py（被改文件：`_is_member` line 72-78、fail-closed 落点 line 101-110、`pack_project_context` 接收 `project` 实例可读 `project.visibility`）
    - server/chat/config.py（`_maybe_pack_project_context` line 33-62 仅委托 packer、不做自己的成员判定——确认改 packer 即覆盖 chat 链，无需改 config）
    - 82-RESEARCH.md「Pitfall 2」「Code Examples / 权限翻转：召回 visibility 感知」（is_member OR public_org 放行；members_only 维持 fail-closed）
    - server/initiatives/models/project.py（ProjectVisibility 枚举，82-01 落地）
  </read_first>
  <behavior>
    - 成员 + 任意 visibility → 放行（打包非空，不回退）
    - 非成员 + public_org → 放行（打包按内容产出）
    - 非成员 + members_only → 返回 PackedContext()（零召回零泄漏）
    - RetrievalTrace 仍写入，payload 标记 visibility（命中可归因）
  </behavior>
  <action>
    在 pack_project_context 把 fail-closed 落点（line 101-110）改为：先 `allowed = await _is_member(project_id, user)`；若未命中且 `getattr(project, "visibility", "") == ProjectVisibility.PUBLIC_ORG` 则 `allowed = True`；`allowed` 仍为 False → 记 `project_context_pack_denied`（reason 区分 not_member_members_only）并 `return PackedContext()`。从 initiatives.models import ProjectVisibility（函数内或顶部，注意 async 安全——纯枚举读不触 DB）。在 `_write_trace` 的 payload 增 `"visibility": getattr(project,...)`（packer 已有 project 引用；若 _write_trace 仅收 result，则把 visibility 透传进 payload 上层）——确保非成员 public_org 命中也写 RetrievalTrace 可归因。脱敏闸（召回内容渲染）保持不变。

    扩 tests/services/test_project_context_packer.py：新增对称三例——(a) public_org 非成员 → packed.text 非空 / included_layers 非空；(b) members_only 非成员 → packed == PackedContext()（text 空、included_layers 空）；(c) 成员 + members_only → 放行非空（不回退）。复用既有 fail-closed 测试的 fixture（建 Project 时显式设 visibility）。
  </action>
  <acceptance_criteria>
    - <automated>cd server && uv run pytest tests/services/test_project_context_packer.py -x</automated> 全绿（含新增对称三例）。
    - 测试断言：members_only 非成员 packed.text == "" 且 included_layers == []（零泄漏）。
    - 测试断言：public_org 非成员 packed.text 非空。
    - grep 断言 packer 内仍保留 `return PackedContext()` fail-closed 分支（未被一刀切删除）。
  </acceptance_criteria>
  <done>packer 按 visibility 分流：public_org 非成员可读、members_only 非成员零召回、成员不回退；RetrievalTrace 仍写。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: access_scope 并入 public_org 项目 + chat 召回链对称回归</name>
  <files>
    server/knowledge/access_scope.py,
    server/tests/test_chat_project_recall.py
  </files>
  <read_first>
    - server/knowledge/access_scope.py（被改文件：`resolve_allowed_project_ids` line 20-45，membership-only allowed 集合 + caller intersect 收窄语义 line 39-45 不可被放宽破坏）
    - server/permissions/services.py（PermissionService.get_user_projects——membership 项目集合来源）
    - server/tests/test_chat_project_recall.py（被改文件：现有 chat 绑定项目召回用例，扩对称回归）
    - 82-RESEARCH.md「Pitfall 2」（access_scope 需把 public_org 项目并入 allowed，注意 caller intersect 不被放宽破坏）
  </read_first>
  <behavior>
    - 普通用户 allowed = membership 项目 ∪ public_org 项目
    - caller 传 project_ids 仍只能 intersect 收窄（含不可见/非 public_org id → []）
    - superuser 行为不变（全项目）
  </behavior>
  <action>
    在 resolve_allowed_project_ids：把普通用户的 `allowed_set` 由「membership 项目」扩为「membership 项目 ∪ visibility==public_org 项目」——新增 `@sync_to_async` 查询 `Project.objects.filter(visibility=ProjectVisibility.PUBLIC_ORG).values_list("id", flat=True)` 并并入 allowed_set（只读，不写）。保持 caller `project_ids` 的 `issubset(allowed_set)` 收窄语义不变（caller 传不在并集内的 id 仍返回 []）。superuser 分支不变。注意 async 经 sync_to_async；避免裸 lazy 访问。

    扩 tests/test_chat_project_recall.py：对称回归——public_org 项目非成员经 chat 绑定可召回到内容（端到端经 _maybe_pack_project_context → packer）；members_only 项目非成员零召回；成员两种可见性均可召回（不回退）。
  </action>
  <acceptance_criteria>
    - <automated>cd server && uv run pytest tests/test_chat_project_recall.py tests/services/test_project_context_packer.py -x</automated> 全绿。
    - 测试断言：caller 传一个 members_only 且非成员的 project_id → resolve_allowed_project_ids 返回 []（收窄语义未被破坏）。
    - 测试断言：public_org 项目 id 出现在普通用户的 allowed 集合。
    - grep 断言 access_scope 内 caller intersect（`issubset`）分支保留。
  </acceptance_criteria>
  <done>access_scope 并入 public_org 项目且 caller 收窄语义不破；chat 召回链对称回归全绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 非成员用户 → 项目召回/检索面 | 读放宽必须按 visibility 精确分流，members_only 不得泄漏 |
| 非成员用户 → 项目写入面 | 写闸保持 fail-closed（本 plan 不动写路径） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-10 | Information Disclosure | 权限翻转误放行 members_only 召回 | mitigate | visibility 分流（is_member OR public_org）+ 对称守护测试（members_only 非成员 packed 为空 + access_scope 收窄保留） |
| T-82-11 | Elevation of Privilege | 非成员写记忆/StateApi/成员/文件 | mitigate | 写路径成员闸一字不动（本 plan 仅改读/召回）；对称测试断言非成员写仍被拒由 82-05/既有成员闸覆盖 |
| T-82-12 | Information Disclosure | 脱敏被绕过 | accept | 召回内容渲染脱敏不变（本 plan 不动渲染/脱敏链路） |
| T-82-13 | Repudiation | 非成员命中召回无法归因 | mitigate | RetrievalTrace 仍写并标 visibility（packer best-effort，绝不反噬） |
</threat_model>

<verification>
- `cd server && uv run pytest tests/services/test_project_context_packer.py tests/test_chat_project_recall.py -x` 全绿。
- `cd server && uv run pytest tests/initiatives/ -k member -x` 写路径成员闸零回归（非成员写仍被拒）。
- 对称性人工核对：public_org 非成员可读、members_only 非成员零召回、任意 visibility 非成员写被拒。
</verification>

<success_criteria>
- WS-02 读半：非成员可读 public_org 项目并发起会话；members_only 维持 fail-closed；召回 scope 随 visibility（Success Criteria 1）。
- 写闸不动、脱敏不绕过。
</success_criteria>

<output>
完成后创建 `.planning/phases/82-project-workspace-entity/82-03-SUMMARY.md`。
</output>
