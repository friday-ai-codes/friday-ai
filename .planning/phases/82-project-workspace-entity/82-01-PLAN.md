---
phase: 82-project-workspace-entity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/initiatives/models/project.py
  - server/initiatives/models/project_doc.py
  - server/initiatives/models/project_state_api.py
  - server/initiatives/models/__init__.py
  - server/initiatives/migrations/0006_project_workspace_entities.py
autonomous: true
requirements: [WS-02, WS-04, DOC-01, DOC-02, DOC-03, DOC-04, DOC-05]
must_haves:
  truths:
    - "Project 有 visibility 字段，默认 public_org，可选 members_only"
    - "Project 有 feishu_folder_token 字段（空字符串默认，可空）"
    - "ProjectDoc 每项目每 doc_type 至多一行（唯一约束）"
    - "ProjectDocBlockMap 每文档每 feishu_block_id 至多一行（唯一约束）"
    - "ProjectStateApi 每项目每 (method, path) 至多一行（唯一约束）"
    - "三个新模型与两个新字段无业务 create/save 方法（INV-6）"
    - "migration 0006 仅 AddField + CreateModel，无任何数据回填"
  artifacts:
    - path: "server/initiatives/models/project.py"
      provides: "ProjectVisibility 枚举 + Project.visibility/feishu_folder_token 字段"
      contains: "class ProjectVisibility"
    - path: "server/initiatives/models/project_doc.py"
      provides: "ProjectDoc / ProjectDocBlockMap + DocType/DocSyncStatus/DocSection 枚举"
      contains: "class ProjectDoc"
    - path: "server/initiatives/models/project_state_api.py"
      provides: "ProjectStateApi + ApiStatus/ApiSource 枚举"
      contains: "class ProjectStateApi"
    - path: "server/initiatives/migrations/0006_project_workspace_entities.py"
      provides: "AddField(Project.visibility/feishu_folder_token) + CreateModel x3"
      contains: "0005_mergerequest_mergerequestevent_projectmemory_and_more"
  key_links:
    - from: "server/initiatives/models/__init__.py"
      to: "ProjectDoc / ProjectDocBlockMap / ProjectStateApi / ProjectVisibility"
      via: "re-export 供 service/serializer/guard 引用"
      pattern: "ProjectDoc|ProjectStateApi|ProjectVisibility"
---

<objective>
立起项目工作区的实体层：给 `Project` 扩两个字段（`visibility` 翻转权限的开关、`feishu_folder_token` 持有飞书专属文件夹），新增三个零业务方法模型（`ProjectDoc` 5 文件容器 / `ProjectDocBlockMap` 同步映射表骨架 / `ProjectStateApi` 结构化 API 清单），并产出纯 AddField+CreateModel 的迁移 `0006`（无任何数据回填——当前无历史项目，per D-01）。

Purpose: 这是 Wave 1 的数据地基；82-02（写入 service + 飞书 provision）、82-03（权限翻转读 `visibility`）、82-05（REST/StateApi）全部依赖本 plan 的模型与字段就位。
Output: 2 个新 `Project` 字段 + 3 个新模型 + 6 个新枚举 + migration 0006。
</objective>

<execution_context>
@.cursor/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/82-project-workspace-entity/82-CONTEXT.md
@.planning/phases/82-project-workspace-entity/82-RESEARCH.md

# 逐字镜像的范式来源（read_first 会再精确引用）
@server/initiatives/models/project.py
@server/initiatives/models/member.py
@server/initiatives/models/__init__.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 扩 Project 字段 + 新建三个工作区模型（零业务方法）</name>
  <files>
    server/initiatives/models/project.py,
    server/initiatives/models/project_doc.py,
    server/initiatives/models/project_state_api.py,
    server/initiatives/models/__init__.py
  </files>
  <read_first>
    - server/initiatives/models/project.py（被改文件：现有 Project 字段/Meta/constraints/indexes 范式，line 25-122；幂等键 partial UniqueConstraint line 106-114）
    - server/initiatives/models/member.py（镜像：UUID PK + TextChoices 闭集 + Meta.db_table/verbose_name/constraints/indexes，partial UniqueConstraint line 51-70，零业务方法）
    - server/initiatives/models/__init__.py（被改文件：现有 re-export 清单与排序，line 1-48）
    - 82-RESEARCH.md「Code Examples / 新模型枚举 + 字段」「Project 字段扩展（纯 AddField）」「Pattern 1」（枚举字面值、字段 max_length、唯一约束命名范式）
  </read_first>
  <action>
    在 project.py 顶部（Project 类之前）新增 `ProjectVisibility(models.TextChoices)`：`PUBLIC_ORG="public_org","全员可读"`、`MEMBERS_ONLY="members_only","仅成员"`（per D-06）。给 `Project` 增 `visibility = CharField(max_length=20, choices=ProjectVisibility.choices, default=ProjectVisibility.PUBLIC_ORG, verbose_name="可见性")`（WS-02，默认 public_org per D-01）与 `feishu_folder_token = CharField(max_length=200, blank=True, default="", verbose_name="飞书工作区文件夹 token")`（WS-04）。不加任何 create/save 方法。

    新建 project_doc.py：枚举 `DocType`(memory/state/milestones/research/preflight)、`DocSyncStatus`(pending="待创建"/ready="已就绪"/broken="失效待重建")、`DocSection`(system="系统区"/human="人工区")。模型 `ProjectDoc`：UUID PK；`project = FK("initiatives.Project", on_delete=CASCADE, related_name="docs")`；`doc_type`(choices=DocType)；`feishu_document_id`/`feishu_doc_token`(CharField blank default="")；`last_synced_revision = BigIntegerField(default=0)`；`last_synced_snapshot = TextField(blank=True, default="")`（内联快照，per Claude 裁量，不另起表）；`sync_status`(choices=DocSyncStatus, default=PENDING)；created_at/updated_at。Meta：`db_table="initiative_project_docs"`，唯一约束 `UniqueConstraint(fields=["project","doc_type"], name="uniq_project_doc_type")`，index `(project, doc_type)`。模型 `ProjectDocBlockMap`：UUID PK；`doc = FK(ProjectDoc, on_delete=CASCADE, related_name="block_maps")`；`feishu_block_id`(CharField)；`db_ref`(CharField blank default="")；`section`(choices=DocSection, default=SYSTEM)；`content_hash`(CharField blank default="")；时间戳。Meta：`db_table="initiative_project_doc_block_maps"`，唯一 `UniqueConstraint(fields=["doc","feishu_block_id"], name="uniq_doc_block")`，index `(doc, section)`。两模型均零业务方法，只 `__str__`。

    新建 project_state_api.py：枚举 `ApiStatus`(planned="待开发"/implemented="已完成"/deprecated="已废弃")、`ApiSource`(manual="手动"/agent="Agent"/hook="IDE 回写")。模型 `ProjectStateApi`：UUID PK；`project = FK("initiatives.Project", on_delete=CASCADE, related_name="state_apis")`；`method`(CharField max_length=10)；`path`(CharField max_length=500)；`params = JSONField(default=dict, blank=True)`；`status`(choices=ApiStatus, default=PLANNED)；`source`(choices=ApiSource, default=MANUAL)；时间戳。Meta：`db_table="initiative_project_state_apis"`，唯一 `UniqueConstraint(fields=["project","method","path"], name="uniq_project_state_api")`（支持后续 upsert 幂等），index `(project, status)`。零业务方法。

    在 models/__init__.py 追加 import 并加入 `__all__`：`ProjectVisibility`、`ProjectDoc`、`ProjectDocBlockMap`、`DocType`、`DocSyncStatus`、`DocSection`、`ProjectStateApi`、`ApiStatus`、`ApiSource`（保持既有排序风格）。docstring/注释用中文（zh-CN 约定）。
  </action>
  <acceptance_criteria>
    - 源码导入无误：`cd server && uv run python -c "import django; django.setup()" ` 前置后，`uv run python -c "from initiatives.models import ProjectDoc, ProjectDocBlockMap, ProjectStateApi, ProjectVisibility, DocType, DocSyncStatus, DocSection, ApiStatus, ApiSource"` 退出码 0。
    - <automated>cd server && uv run python manage.py check initiatives</automated> 通过（无系统检查错误）。
    - grep 断言三个新模型类体内无 `def create`/`def save`（保持 INV-6 零业务方法约定）。
    - `ProjectVisibility.PUBLIC_ORG == "public_org"`、`DocType.values == ["memory","state","milestones","research","preflight"]`、`DocSyncStatus.BROKEN == "broken"`。
  </acceptance_criteria>
  <done>三个新模型 + 两个新字段 + 六个新枚举落地并从 initiatives.models 可导入；django check 干净；无业务方法。</done>
</task>

<task type="auto">
  <name>Task 2: 生成 migration 0006（AddField + CreateModel，无回填）</name>
  <files>
    server/initiatives/migrations/0006_project_workspace_entities.py
  </files>
  <read_first>
    - server/initiatives/migrations/0005_mergerequest_mergerequestevent_projectmemory_and_more.py（最新迁移：dependencies 写法、CreateModel/AddField operation 形态、Meta 约束序列化范式）
    - 82-RESEARCH.md「关键数字」（新迁移名 0006，dependency `('initiatives','0005_mergerequest_mergerequestevent_projectmemory_and_more')`）
    - 82-CONTEXT.md `<decisions>`「权限翻转 + visibility 迁移」（migration 仅 AddField + default，不回填）
  </read_first>
  <action>
    用 `cd server && uv run python manage.py makemigrations initiatives` 由 Django 自动生成迁移，文件名重命名/确认为 `0006_project_workspace_entities.py`。校验生成内容：`dependencies` 含 `('initiatives','0005_mergerequest_mergerequestevent_projectmemory_and_more')`；operations 仅含 `AddField(Project.visibility default=public_org)`、`AddField(Project.feishu_folder_token default="")`、`CreateModel(ProjectDoc)`、`CreateModel(ProjectDocBlockMap)`、`CreateModel(ProjectStateApi)`，以及 M2M/index/constraint 的附带 operation。**绝不**包含任何 `RunPython`/数据回填/`migrations.RunSQL`（per D-01 无历史项目，纯结构迁移）。若自动生成把字段顺序/默认值写得不符，最小手改对齐 Task 1 字段定义。
  </action>
  <acceptance_criteria>
    - <automated>cd server && uv run python manage.py makemigrations --check --dry-run</automated> 退出码 0（无未生成的模型变更）。
    - <automated>cd server && uv run python manage.py migrate initiatives</automated> 成功（SQLite 测试库可正向迁移）。
    - grep 断言迁移文件无 `RunPython` 与 `RunSQL`（纯 AddField/CreateModel）。
    - grep 断言迁移文件 dependencies 含字符串 `0005_mergerequest_mergerequestevent_projectmemory_and_more`。
  </acceptance_criteria>
  <done>0006 迁移可正向 migrate；makemigrations --check 干净；无回填逻辑。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ORM 写入面 → DB | 新模型的写入须收口单一 service（本 plan 只建模型；写入收口由 82-02 落地 + INV-6 guard） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-01 | Tampering | 三个新模型旁路写表 | mitigate | 模型层零业务方法；写入唯一入口与 grep 守护在 82-02 落地（本 plan 不提供任何 create/save） |
| T-82-02 | Information Disclosure | Project.visibility 默认值 | accept | 默认 public_org 是产品锁定决策（D-01）；读放宽闸由 82-03 按 visibility 精确分流，本 plan 仅声明字段 |
| T-82-03-SC | Tampering | 包安装 | accept | 本 phase 不安装任何外部包（见 82-RESEARCH「Package Legitimacy Audit：SKIPPED」） |
</threat_model>

<verification>
- `cd server && uv run python manage.py makemigrations --check --dry-run` 干净。
- `cd server && uv run python manage.py migrate initiatives` 成功。
- `cd server && uv run pytest tests/initiatives/ -x` 既有用例零回归（新字段有默认值，不破坏现有项目/成员测试）。
</verification>

<success_criteria>
- Project 扩 visibility(默认 public_org)/feishu_folder_token（对应 Success Criteria 1 的字段地基、SC 3 的 token 存储地基）。
- ProjectDoc(5 type) + ProjectDocBlockMap + ProjectStateApi 模型就位（对应 Success Criteria 2 的模型骨架）。
- migration 0006 纯结构、无回填，可正向迁移。
</success_criteria>

<output>
完成后创建 `.planning/phases/82-project-workspace-entity/82-01-SUMMARY.md`。
</output>
