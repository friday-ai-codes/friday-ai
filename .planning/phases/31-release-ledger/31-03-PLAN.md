---
phase: 31-release-ledger
plan: 03
type: execute
wave: 3
depends_on: ["31-01", "31-02"]
files_modified:
  - server/services/feishu_bitable.py
  - server/delivery/services/bitable_release_adapter.py
  - server/delivery/services/__init__.py
  - server/tests/services/test_feishu_bitable.py
  - server/tests/delivery/test_bitable_release_adapter.py
autonomous: true
requirements: [REL-02]
must_haves:
  truths:
    - "BitableClient 用开放平台 tenant_access_token（open.feishu.cn/.../tenant_access_token/internal）取 token，非项目 plugin token"
    - "BitableClient 凭证来源（app_id/app_secret）经独立解析器，与项目 plugin token 来源解耦"
    - "list_records(app_token, table_id) 骨架方法走开放平台 bitable 端点形状（可调通 token + 端点）"
    - "BitableReleaseAdapter 把 Bitable 原始行经 ReleaseService 落库，raw_row 不丢，natural key 标识正确"
    - "无开放平台凭证时 adapter 降级不崩（返回空 + warning，不抛致调用方崩溃）"
  artifacts:
    - path: "server/services/feishu_bitable.py"
      provides: "BitableClient（开放平台 token 复用）+ create_bitable_client_for_project 独立凭证解析"
      exports: ["BitableClient"]
    - path: "server/delivery/services/bitable_release_adapter.py"
      provides: "BitableReleaseAdapter — Bitable 行 → ReleaseService 落库（保留 raw_row + natural key）"
      exports: ["BitableReleaseAdapter"]
    - path: "server/tests/services/test_feishu_bitable.py"
      provides: "respx mock token + list_records；凭证来源独立于 plugin token 守护"
    - path: "server/tests/delivery/test_bitable_release_adapter.py"
      provides: "adapter raw_row 无损 + natural key + 降级 守护"
  key_links:
    - from: "server/delivery/services/bitable_release_adapter.py"
      to: "delivery.services.ReleaseService"
      via: "经 ReleaseService.ingest_batch 落库（不旁路写表）"
      pattern: "ReleaseService\\(\\)\\.ingest_batch|self\\._service\\.ingest_batch"
    - from: "server/services/feishu_bitable.py"
      to: "open.feishu.cn open-platform tenant_access_token"
      via: "get_tenant_access_token（复用 FeishuDocClient 范式）"
      pattern: "tenant_access_token/internal"
---

<objective>
搭飞书 Bitable client/adapter **骨架**（REL-02）：新增 `server/services/feishu_bitable.py` 的 `BitableClient`，**复用既有 `FeishuDocClient` 的开放平台 `tenant_access_token` 模式**（`open.feishu.cn/open-apis/auth/v3/tenant_access_token/internal`，app_id/app_secret，2h 缓存），凭证来源经**独立解析器**（`create_bitable_client_for_project`，对齐 `create_feishu_doc_client_for_project`——Project / SystemSetting 加密凭证），**明确与项目 plugin token（`services/feishu.py`，`project.feishu.cn`）来源解耦**（REL-02 核心）。`BitableReleaseAdapter` 把 Bitable 原始行 → 经 31-02 `ReleaseService` 落库（**保留 raw_row** + natural key），列→字段只建**最小骨架/占位映射**（natural key + raw_row 必填，业务列映射 TODO 标 REL-03）。

**本 plan 是骨架**：不实现真实 Bitable 列映射 / 真实数据全量入库（缺开放平台凭证 + 列样例，延 v2 REL-03）。无凭证 → 骨架降级不崩。

Purpose: 立起开放平台 token 独立解析 + Bitable 端点形状 + 宽容映射底座，为 REL-03 真实列映射铺路。
Output: `feishu_bitable.py`（client + 凭证解析）、`bitable_release_adapter.py`、re-export、client 测试（respx）、adapter 测试。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-release-ledger/31-CONTEXT.md
@.planning/phases/31-release-ledger/31-01-PLAN.md
@.planning/phases/31-release-ledger/31-02-PLAN.md
@.planning/DOMAIN-MODEL.md

# 直接复用的开放平台 token 范式 + 独立凭证解析范式
@server/services/feishu_doc.py
@server/agents/tools/feishu_doc_tools.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: BitableClient 骨架 + 独立开放平台凭证解析（feishu_bitable.py）</name>
  <files>server/services/feishu_bitable.py</files>
  <read_first>
    - server/services/feishu_doc.py（lines 62–175：FeishuDocClient.__init__ + get_tenant_access_token 2h 缓存 + OPEN_API_BASE + httpx async + 错误码分类 + structlog；BitableClient 直接复用此 token 范式）
    - server/agents/tools/feishu_doc_tools.py（lines 18–84：_aget_system_feishu_credentials_for_doc + create_feishu_doc_client_for_project —— 开放平台凭证来源解析范式，Project.feishu_app_id/feishu_app_secret_encrypted → SystemSetting fallback；BitableClient 凭证来源对齐此，**独立于 plugin token**）
    - .planning/DOMAIN-MODEL.md §4（line 201–204：Bitable 端点 /open-apis/bitable/v1/apps/{app_token}/tables/{table_id}/records + natural key + 开放平台 token 不是 plugin token）
  </read_first>
  <action>
    新增 `server/services/feishu_bitable.py`，`BitableClient` **复用 FeishuDocClient 开放平台 token 模式**（REL-02）：
    - `OPEN_API_BASE = "https://open.feishu.cn/open-apis"`（与 FeishuDocClient 同——开放平台域，**非** `project.feishu.cn`）。
    - `__init__(self, app_id, app_secret)` + `_tenant_token`/`_token_expires_at` 缓存字段（镜像 FeishuDocClient）。
    - `async def get_tenant_access_token(self) -> str`：复用 FeishuDocClient 同款实现——POST `{OPEN_API_BASE}/auth/v3/tenant_access_token/internal` with `{app_id, app_secret}`，2h 缓存提前 5 分钟刷新，code!=0 抛 `BitableAPIError`。（为真正"复用"而非复制：可直接在内部组合一个 `FeishuDocClient` 实例并委托其 `get_tenant_access_token`，或抽取共享 token helper——任一由实现按 DRY 决定；务必保证 token 端点是开放平台 internal 端点。）
    - 骨架方法（REL-02 "骨架可调通 token 获取与端点形状，真实列解析留 adapter"）：
      - `async def list_records(self, app_token: str, table_id: str, *, page_token: str | None = None, page_size: int = 100) -> dict[str, Any]`：取 token → GET `{OPEN_API_BASE}/bitable/v1/apps/{app_token}/tables/{table_id}/records` with Bearer + 分页 params → code!=0 分类抛错（沿用 feishu_doc.py 错误码分类风格，至少 RateLimit/通用），成功返回 `data`（含 `items`/`has_more`/`page_token` 原始形状，**不解析列结构**——留 adapter/REL-03）。可选 `list_tables(app_token)` 骨架（同形）。
      - 端点字符串/形状对齐 DOMAIN §4 line 203；真实列解析处 `# TODO(REL-03): 真实多维表格列结构解析待开放平台凭证 + 列样例`。
    - 异常类 `BitableAPIError`（+ 可选 `RateLimitError` 子类，沿用 feishu_doc.py 风格）。
    - **独立凭证解析** `async def create_bitable_client_for_project(project) -> BitableClient`：镜像 `create_feishu_doc_client_for_project`——优先 `project.feishu_app_id` + `decrypt_value(project.feishu_app_secret_encrypted)`，回退 SystemSetting（`SettingKeys.FEISHU_APP_ID/FEISHU_APP_SECRET`，加密则 decrypt）；**绝不** 取 `services/feishu.py` 的 plugin token（plugin_id/secret/user_key，`project.feishu.cn`）。无凭证 → raise `ValueError`（与 doc client 同款降级语义，由 adapter 捕获降级）。中文 docstring 显式标注"开放平台 token 来源，独立于项目 plugin token（REL-02）"。

    httpx async；tenacity 重试可选（沿用 feishu_doc.py 风格，骨架可简化）；structlog；ruff line 100；中文 docstring。
  </action>
  <acceptance_criteria>
    - `BitableClient(app_id, app_secret).get_tenant_access_token` 打到 `open.feishu.cn/open-apis/auth/v3/tenant_access_token/internal`。
    - `list_records(app_token, table_id)` 打到 `open.feishu.cn/open-apis/bitable/v1/apps/{app_token}/tables/{table_id}/records` 并返回原始 data（不解析列）。
    - `create_bitable_client_for_project` 优先 project 开放平台凭证、回退 SystemSetting、无凭证 raise ValueError；源码**不** import/调用 `services.feishu.create_feishu_client_for_project` 或 plugin token。
    - 含 `TODO(REL-03)` 标记真实列解析占位。
  </acceptance_criteria>
  <verify>
    <automated>cd server && uv run python -c "from services.feishu_bitable import BitableClient, create_bitable_client_for_project; assert BitableClient.OPEN_API_BASE.startswith('https://open.feishu.cn'); print('ok')" && uv run ruff check services/feishu_bitable.py</automated>
  </verify>
  <done>BitableClient 骨架（开放平台 token 复用 + list_records 端点形状）+ 独立凭证解析就位，与 plugin token 解耦。</done>
</task>

<task type="auto">
  <name>Task 2: BitableReleaseAdapter 骨架（Bitable 行 → ReleaseService 落库，保留 raw_row + natural key）</name>
  <files>server/delivery/services/bitable_release_adapter.py, server/delivery/services/__init__.py</files>
  <read_first>
    - server/delivery/services/release_service.py（31-02 产出：ReleaseService.ingest_batch 入参形状 raw_rows/source/batch_meta；adapter 经此落库，不旁路写表）
    - server/delivery/models/release.py（build_bitable_record_key + ReleaseSource）
    - server/services/feishu_bitable.py（Task 1 产出：BitableClient.list_records 返回形状 + create_bitable_client_for_project 降级 ValueError）
    - server/agents/tools/feishu_doc_tools.py（lines 130–188：ValueError 捕获 → 降级返回的范式）
  </read_first>
  <action>
    新增 `server/delivery/services/bitable_release_adapter.py`，`BitableReleaseAdapter` 把 Bitable 原始行 → 经 31-02 `ReleaseService` 落库（**经 service 入口，不旁路写表**，守 INV-6）。骨架职责（REL-02）：
    - `__init__(self, *, client: BitableClient | None = None, service: ReleaseService | None = None)`（依赖可注入便于测试；缺省内部构造）。
    - `async def ingest_from_table(self, *, project, app_token: str, table_id: str, source: str = ReleaseSource.BITABLE) -> ReleaseBatch | None`：
      1. 解析凭证/建 client：若未注入 client，则 `client = await create_bitable_client_for_project(project)`；捕获 `ValueError`（无开放平台凭证）→ **降级**：structlog warning `bitable_credentials_missing` + 返回 None（**不抛、不崩**，对齐 doc tools 降级范式 + CONTEXT "无凭证骨架不崩")。
      2. `data = await client.list_records(app_token, table_id)`（骨架；真实分页遍历留 REL-03，可只取首页）。捕获 BitableAPIError → warning + 返回 None（降级不崩）。
      3. 把每条 Bitable record 经**占位映射** `_map_record(app_token, table_id, record)` → 一个 raw_row dict：**必含** `bitable_record_key = build_bitable_record_key(app_token, table_id, record["record_id"])` + 原始 record 全量保留（`raw_row` 即原始 record 或其 `fields` + 元信息——保证 31-02 落库时 raw_row 不丢）；业务列→字段（status/note/work_item_external_id）只建**占位**（从 record fields 顶层同名键尝试取，取不到留空/None）。`# TODO(REL-03): 真实业务列映射待列头/样例`。
      4. 调 `await self._service.ingest_batch(raw_rows=mapped_rows, source=source, batch_meta={...app_token/table_id...})` 落库，返回 ReleaseBatch。
    - natural key 唯一构造经 `build_bitable_record_key`（不在 adapter 内重复拼接）。
    - 真实数据全量入库 / 真实列结构映射**不做**（骨架，REL-03）；adapter 只证明"原始行经 service 落库 + raw_row 不丢 + natural key 标识 + 无凭证降级"。

    更新 `server/delivery/services/__init__.py`：re-export `BitableReleaseAdapter` + 加入 `__all__`。

    ruff line 100；中文 docstring；structlog。
  </action>
  <acceptance_criteria>
    - adapter 落库**经 ReleaseService.ingest_batch**，不直接写 Release 模型（31-02 INV-6 grep 守护对 adapter 文件仍 pass）。
    - 每条映射行 raw_row 保留原始 record（无损），且含经 build_bitable_record_key 生成的 natural key。
    - 无凭证（create_bitable_client_for_project raise ValueError）→ 返回 None + warning，不抛。
    - 含 `TODO(REL-03)` 标记占位业务列映射。
    - `BitableReleaseAdapter` 经 `delivery.services` 顶层可 import。
  </acceptance_criteria>
  <verify>
    <automated>cd server && uv run python -c "from delivery.services import BitableReleaseAdapter; print('ok')" && uv run ruff check delivery/services/bitable_release_adapter.py</automated>
  </verify>
  <done>BitableReleaseAdapter 骨架就位：Bitable 行经 ReleaseService 落库、raw_row 不丢、natural key 标识、无凭证降级。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: client（respx）+ adapter 守护测试</name>
  <files>server/tests/services/test_feishu_bitable.py, server/tests/delivery/test_bitable_release_adapter.py</files>
  <read_first>
    - server/services/feishu_bitable.py（Task 1）+ server/delivery/services/bitable_release_adapter.py（Task 2）
    - server/tests/delivery/test_document_service.py（django_db(transaction=True) + async 测试范式）
    - server/services/feishu_doc.py（token 端点 + 响应形状，供 respx mock 构造正确 JSON）
  </read_first>
  <behavior>
    client（`tests/services/test_feishu_bitable.py`，respx mock httpx，**无真实网络**）：
    - token 走开放平台：respx mock `open.feishu.cn/open-apis/auth/v3/tenant_access_token/internal` 返回 `{code:0, tenant_access_token:"t", expire:7200}`；`get_tenant_access_token()` 返回 "t"，且 respx 断言被请求的 URL host 是 `open.feishu.cn`（**不是** `project.feishu.cn`）。
    - list_records 端点形状：respx mock `.../bitable/v1/apps/{app_token}/tables/{table_id}/records` 返回带 items 的 data；`list_records` 返回该原始 data（含 items，未解析列）。
    - 凭证来源独立：构造一个带 feishu_app_id/feishu_app_secret_encrypted 的 Project（或 mock create_bitable_client_for_project 的两路径），断言 client 用开放平台 app_id/secret 构造，**源码层** test 断言 feishu_bitable.py 不 import services.feishu 的 plugin token 入口（可用源码 grep 断言 `create_feishu_client_for_project` 不出现 / `plugin` token 不被取）。
    - token 缓存：二次取 token 不再打网络（respx call_count==1）。

    adapter（`tests/delivery/test_bitable_release_adapter.py`，django_db(transaction=True)）：
    - raw_row 无损 + natural key：注入 fake client（其 list_records 返回 2 条带 record_id 的 record）+ 真实 ReleaseService → ingest_from_table 落库，断言 ReleaseRecord.raw_row 含原始 record 全量、bitable_record_key == "{app_token}:{table_id}:{record_id}"。
    - 经 service 落库：断言落库后 DB 有 ReleaseBatch + 对应 ReleaseRecord（经 ReleaseService，非旁路）。
    - 降级：注入 create_bitable_client_for_project 抛 ValueError（或 client=None 且 project 无凭证）→ ingest_from_table 返回 None、不抛、DB 无新增 batch。
  </behavior>
  <action>
    新建两个测试文件覆盖上述 behavior。client 测试用 `respx` mock httpx（沿用项目 respx + pytest-socket 范式，绝无真实网络）+ `pytest.mark.asyncio`/`anyio` 既有 async 测试约定。adapter 测试 `pytestmark = pytest.mark.django_db(transaction=True)`，用注入式 fake BitableClient（实现 `list_records` 返回固定 fixture）避免触网。中文 docstring；ruff line 100。
  </action>
  <acceptance_criteria>
    - client：token 端点 host 为 open.feishu.cn、list_records 端点形状正确、token 缓存 call_count==1、凭证来源独立于 plugin token —— 四类断言通过。
    - adapter：raw_row 无损 + natural key 正确 + 经 service 落库 + 无凭证降级返回 None 不抛 —— 用例通过。
    - 全程无真实网络（respx / fake client；pytest-socket 不报 SocketBlockedError）。
  </acceptance_criteria>
  <verify>
    <automated>cd server && uv run pytest tests/services/test_feishu_bitable.py tests/delivery/test_bitable_release_adapter.py -x -q</automated>
  </verify>
  <done>client + adapter 守护测试全绿：开放平台 token 独立 + 端点形状 + raw_row 无损 + natural key + 降级。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BitableClient → 飞书开放平台 | app_id/app_secret 与 tenant_access_token 跨网络边界（开放平台域，独立凭证体系） |
| Bitable record → BitableReleaseAdapter | 外部 Bitable 行作为不可信数据进入 adapter，经 ReleaseService 落库 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-06 | Information Disclosure | app_secret / tenant_access_token | mitigate | 凭证经 decrypt_value 解密后仅入 client 内存 + 单次 httpx 请求 body/header，不入 structlog（沿用 feishu_doc.py 不记 token 范式） |
| T-31-07 | Spoofing | 误用 plugin token 取 Bitable | mitigate | create_bitable_client_for_project 独立解析开放平台 app_id/secret，源码不取 plugin token；测试断言 token 端点为 open.feishu.cn 且不 import plugin 入口（REL-02 解耦核心） |
| T-31-08 | Denial of Service | 无凭证 / API 失败 | mitigate | adapter 捕获 ValueError/BitableAPIError → warning + 返回 None 降级，骨架不崩（CONTEXT 范围守护） |
| T-31-09 | Tampering | adapter 旁路写 Release 表 | mitigate | adapter 经 ReleaseService.ingest_batch 落库；31-02 test_release_inv6_guard.py grep 守护覆盖 adapter 文件 |

无 package 安装变更（复用既有 httpx/respx/structlog/tenacity）。
</threat_model>

<verification>
- `pytest tests/services/test_feishu_bitable.py tests/delivery/test_bitable_release_adapter.py` 全绿（无真实网络）。
- 31-02 `test_release_inv6_guard.py` 对新增 adapter 文件仍 pass（adapter 不旁路写表）。
- `ruff check services/feishu_bitable.py delivery/services/bitable_release_adapter.py` + 两测试文件 干净。
</verification>

<success_criteria>
- REL-02：BitableClient 用开放平台 tenant_access_token（open.feishu.cn）取 token，凭证来源经独立解析器、与项目 plugin token 来源解耦（已测）。
- list_records 骨架可调通 token + 开放平台 bitable 端点形状（真实列解析留 REL-03）。
- BitableReleaseAdapter 经 ReleaseService 落库、保留 raw_row、natural key `{app_token}:{table_id}:{record_id}` 标识正确（已测）。
- 无开放平台凭证时 adapter 降级不崩（返回 None + warning）。
- 真实列映射 / 真实数据全量入库**未做**（骨架，明确延 REL-03）。
</success_criteria>

<artifacts_this_phase_produces>
- `server/services/feishu_bitable.py` — BitableClient（开放平台 token 复用 + list_records 骨架）+ create_bitable_client_for_project（独立凭证解析，解耦 plugin token）。
- `server/delivery/services/bitable_release_adapter.py` — BitableReleaseAdapter（Bitable 行 → ReleaseService 落库，保留 raw_row + natural key + 降级）。
- `server/tests/services/test_feishu_bitable.py` — respx token + 端点 + 凭证独立守护。
- `server/tests/delivery/test_bitable_release_adapter.py` — adapter raw_row 无损 + natural key + 降级守护。
- `server/delivery/services/__init__.py`（修改）— BitableReleaseAdapter re-export。
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/31-release-ledger/31-03-SUMMARY.md` when done
</output>
