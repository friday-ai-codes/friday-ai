---
phase: 50-spec
plan: 05
type: execute
wave: 2
depends_on: ["50-04"]
files_modified:
  - web/src/components/spec/SpecReviewDialog.vue
  - web/src/components/spec/SpecTransitionActions.vue
  - web/src/pages/specs/index.vue
  - web/src/pages/specs/[id].vue
  - web/src/components/layout/AppSidebar.vue
  - web/src/components/spec/__tests__/SpecTransitionActions.test.ts
  - web/src/pages/specs/__tests__/specs-index.test.ts
autonomous: true
requirements: [SPECST-03]
must_haves:
  truths:
    - "用户经侧边栏入口 /specs 进入 spec 列表，按状态/仓库过滤，点击进详情"
    - "详情页展示正文 markdown + 状态徽标 + 关联链接 + 评审时间线 + 操作区"
    - "操作按钮按当前状态 × 用户权限显隐（superuser 才见 approve/reject/archive/mark_implemented）"
    - "approve/reject 经带 comment 对话框（reject comment 必填）；archive/mark_implemented 经确认框"
    - "流转成功 invalidate ['specs'] + ['spec', id] + toast；失败走 errorHandler"
  artifacts:
    - path: "web/src/pages/specs/index.vue"
      provides: "spec 列表页（/specs）"
      min_lines: 40
    - path: "web/src/pages/specs/[id].vue"
      provides: "spec 详情页（/specs/:id）"
      min_lines: 40
    - path: "web/src/components/spec/SpecTransitionActions.vue"
      provides: "状态×权限操作区 + 派发流转"
    - path: "web/src/components/spec/SpecReviewDialog.vue"
      provides: "批准/驳回 comment 对话框"
    - path: "web/src/components/layout/AppSidebar.vue"
      provides: "导航入口 /specs"
      contains: "/specs"
  key_links:
    - from: "web/src/components/spec/SpecTransitionActions.vue"
      to: "specsApi.transition + queryClient.invalidateQueries"
      via: "useMutation onSuccess invalidate ['specs'] / ['spec', id]"
      pattern: "invalidateQueries"
    - from: "web/src/pages/specs/[id].vue"
      to: "specsApi.detail + MarkdownRenderer + SpecReviewTimeline + SpecTransitionActions"
      via: "useQuery(['spec', id]) 装配详情"
      pattern: "spec"
    - from: "web/src/pages/specs/index.vue"
      to: "specsApi.list"
      via: "useQuery(['specs', filters])"
      pattern: "specs"
---

<objective>
装配 spec 治理交互层（SPECST-03，本里程碑最重前端，按 50-UI-SPEC）：列表页 + 详情页 + 状态流转操作区（state×action 矩阵 + 权限显隐）+ 评审对话框 + 侧边栏入口。复用 50-04 的 API/类型/徽标/时间线 + 既有 MarkdownRenderer/usePermission/useConfirmDialog/TanStack 派发→invalidate 范式。

Purpose: 让用户可见 spec 列表/详情/状态/评审并发起流转（提交评审/批准/驳回/归档/标记已实现）。
Output: 2 组件 + 2 页面 + 导航入口 + 交互测试。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/50-spec/50-UI-SPEC.md
@.planning/phases/50-spec/50-CONTEXT.md
@web/src/composables/usePermission.ts
@web/src/composables/useConfirmDialog.ts
@web/src/components/execution/MarkdownRenderer.vue
@web/src/components/layout/AppSidebar.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: SpecReviewDialog + SpecTransitionActions（state×权限矩阵 + 派发）</name>
  <files>web/src/components/spec/SpecReviewDialog.vue, web/src/components/spec/SpecTransitionActions.vue</files>
  <action>新建 SpecReviewDialog.vue（reka-ui Dialog，~/components/ui/dialog + ui/button + textarea）：props { open; mode: 'approve'|'reject' }，emit confirm(comment) / cancel；标题/描述按 specs.reviewDialog.*（mode 切换 approve/reject 文案）；reject 时 comment 必填（空则确认按钮 disabled，placeholder=specs.reviewDialog.commentRequired），approve 时 comment 可选（placeholder=specs.reviewDialog.commentOptional）；reka-ui Dialog 自带焦点管理（可达性）。新建 SpecTransitionActions.vue：props { spec: SddSpecDetail }。按 50-UI-SPEC「State × Action Matrix」+ usePermission().isSystemAdmin 决定按钮显隐——draft: 提交评审(认证)+归档(SU)；in_review: 批准(SU)+驳回(SU)+归档(SU)；approved: 标记已实现(SU)+归档(SU)；implemented: 归档(SU)；archived: 无（终态）。非 superuser 进入 in_review 仅显示 specs.actions.awaitingReview 提示，不渲染受限按钮（fail-closed）。approve/reject 点击打开 SpecReviewDialog；archive/mark_implemented 点击走 useConfirmDialog（archive 用 variant:'destructive'，文案 specs.confirm.*）。所有流转经 useMutation 调 specsApi.transition(spec.id, {action, comment})，onSuccess：queryClient.invalidateQueries(['specs']) + (['spec', spec.id]) + useToast 成功文案（specs.toast.*），onError：useErrorHandler（含 400 非法流转→specs.error.transition）。流转进行中按钮 disabled + lucide--loader-circle animate-spin（防重复派发）；按钮含图标+文字标签（可达性）；操作组 flex-wrap（窄屏换行）。</action>
  <verify>
    <automated>cd web && pnpm vue-tsc --noEmit -p tsconfig.json 2>&1 | head -40 && pnpm eslint src/components/spec/SpecReviewDialog.vue src/components/spec/SpecTransitionActions.vue</automated>
  </verify>
  <done>两组件按矩阵+权限+确认/对话框+派发invalidate范式落地，vue-tsc/eslint 干净。</done>
</task>

<task type="auto">
  <name>Task 2: 列表页 + 详情页 + 侧边栏导航入口</name>
  <files>web/src/pages/specs/index.vue, web/src/pages/specs/[id].vue, web/src/components/layout/AppSidebar.vue</files>
  <action>新建 web/src/pages/specs/index.vue（路由 /specs，file-based）：PageContainer + PageHeader（title=specs.title / subtitle=specs.subtitle）；状态/仓库过滤用 ~/components/ui/select（选项含 specs.filter.all + specs.status.*）；useQuery(['specs', filters]) 调 specsApi.list；LoadingState(variant="card") / EmptyState（区分无数据 specs.empty vs 过滤无结果 specs.emptyFiltered）；列表行展示 spec 标题/仓库 + SddSpecStatusBadge + 更新时间，点击行 router 进 /specs/<id>；加载失败 specs.loadError。新建 web/src/pages/specs/[id].vue（路由 /specs/:id）：useQuery(['spec', id]) 调 specsApi.detail；头部 SddSpecStatusBadge + 标题；关联链接区按 UI-SPEC「关联链接区」渲染 relations（repository→/repositories/<id> + SddMethodologyBadge；work_item / plan_version 按有无降级，缺失项不渲染）；正文用 ~/components/execution/MarkdownRenderer 渲染 body；SpecReviewTimeline 渲染 reviews（标题 specs.detail.reviewHistory）；底部 SpecTransitionActions 传 spec。在 web/src/components/layout/AppSidebar.vue 的 mainNavItems 数组加 { to: '/specs', label: 'spec 治理', icon: 'lucide--file-check-2' }（紧邻 /knowledge 相关项之后，对齐既有 NavItem 结构）。</action>
  <verify>
    <automated>cd web && pnpm vue-tsc --noEmit -p tsconfig.json 2>&1 | head -40 && pnpm eslint src/pages/specs/ src/components/layout/AppSidebar.vue</automated>
  </verify>
  <done>列表/详情页装配完整（过滤/空态/徽标/markdown/时间线/操作区/关联链接）、侧边栏入口生效、vue-tsc/eslint 干净。</done>
</task>

<task type="auto">
  <name>Task 3: 交互 vitest（列表渲染/过滤、操作按钮按状态+权限显隐、transition invalidate、真实 zh-CN 文案）</name>
  <files>web/src/components/spec/__tests__/SpecTransitionActions.test.ts, web/src/pages/specs/__tests__/specs-index.test.ts</files>
  <action>新建两个 vitest（mock specsApi + TanStack QueryClient + 真实 zh-CN messages；权限用 mock 的 auth store / usePermission，参考既有 pages/admin/__tests__/prompts.test.ts 与 components RoutingDecisionPanel.test.ts 范式）。SpecTransitionActions.test.ts：(1) draft + 普通认证用户 → 见「提交评审」，不见 approve/reject/archive；(2) in_review + 非 superuser → 仅见 awaitingReview 提示，无受限按钮；(3) in_review + superuser → 见批准/驳回/归档；(4) 点击提交评审触发 specsApi.transition 且 onSuccess 调 queryClient.invalidateQueries（断言 invalidate 被以 ['specs'] / ['spec', id] 调用）；断言按钮文案取自真实 specs.actions.*。specs-index.test.ts：mock specsApi.list 返回若干 spec，断言渲染行 + 状态徽标真实文案；空结果断言 specs.empty 文案。</action>
  <verify>
    <automated>cd web && pnpm vitest run src/components/spec/__tests__/SpecTransitionActions.test.ts src/pages/specs/__tests__/specs-index.test.ts</automated>
  </verify>
  <done>操作按钮状态×权限显隐、transition→invalidate、列表渲染/空态、真实 zh-CN 文案断言全绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 前端权限显隐 vs 后端授权 | 前端按 isSystemAdmin 隐藏受限按钮仅 UX；真正授权在 50-03 后端 IsSuperUser（前端非安全边界）|
| spec 正文 markdown 渲染 | 后端文本经 MarkdownRenderer 渲染，须为既有受信渲染器 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-14 | Elevation of Privilege | 前端隐藏受限按钮被绕过 | accept | 前端显隐仅 UX；权威授权在后端（T-50-07），绕过前端仍被 403 |
| T-50-15 | Tampering (XSS) | spec 正文 markdown | mitigate | 复用既有 MarkdownRenderer（markdown-it + Shiki，既有清洗），不引新 markdown 库 |
| T-50-16 | Denial of Service | 重复派发流转 | mitigate | 流转中按钮 disabled + spinner，防重复 mutation |
</threat_model>

<verification>
- `pnpm vitest run src/components/spec/ src/pages/specs/` 全绿。
- `pnpm vue-tsc --noEmit` 无新类型错误；`pnpm eslint src/pages/specs/ src/components/spec/ src/components/layout/AppSidebar.vue` 干净。
- 人工 UI 冒烟（建议，非阻断）：/specs 列表→详情→以 superuser 走一遍 submit→approve→archive。
</verification>

<success_criteria>
用户可经 /specs 看列表/详情/状态/评审并按状态×权限发起流转，流转 invalidate 刷新 + toast/错误反馈。映射：SPECST-03（前端可见可操作）；ROADMAP 成功标准 3、4。
</success_criteria>

<output>
Create `.planning/phases/50-spec/50-05-SUMMARY.md` when done
</output>
