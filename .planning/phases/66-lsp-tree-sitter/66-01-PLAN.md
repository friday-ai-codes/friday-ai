---
phase: 66-lsp-tree-sitter
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/friday/settings.py
  - server/codegraph/tests/test_go_extractor.py
  - .env.example
  - docker-compose.yaml
  - deploy/helm/friday/templates/configmap.yaml
  - deploy/helm/friday/values.yaml
autonomous: true
requirements: [LSP-01]
must_haves:
  truths:
    - "settings.VOLAR_BACKEND_ENABLED / GOPLS_BACKEND_ENABLED 默认 env.bool False；apps.ready 守卫天然跳过 register_backend，BACKEND_REGISTRY 全保 TreeSitterBackend"
    - "EXTRACTOR_BACKENDS['go'] 回落 tree_sitter；ts/tsx/vue/js/jsx 仍声明 volar（纯声明性，仅测试消费）"
    - "向量/图谱抽取两路径（get_extractor / get_backend）LSP 关闭时均自动回落 TreeSitterBackend，无需改抽取器"
    - "env 显式 true 可逆重开 LSP，无需改代码（env.bool）"
    - ".env.example / docker-compose.yaml / helm configmap+values 注释两开关用途与重开方式"
    - "预置 acceptance 测试转绿；test_go_extractor endpoint 断言改为 tree-sitter 抽到 gin 路由（行为更优非回归）"
  artifacts:
    - path: "server/friday/settings.py"
      provides: "两 kill-switch 默认 False + EXTRACTOR_BACKENDS go→tree_sitter"
      contains: "VOLAR_BACKEND_ENABLED"
---

# Phase 66 Plan 01 — 默认禁用 LSP（仅 tree-sitter）

## Tasks
1. `settings.py`：`VOLAR_BACKEND_ENABLED`/`GOPLS_BACKEND_ENABLED` 默认 True→False；`EXTRACTOR_BACKENDS["go"]` gopls→tree_sitter；更新注释。
2. `test_go_extractor.py`：`test_endpoints_empty_for_go` → `test_endpoints_extracted_for_go_treesitter`（断言 gin GET /users/:id + POST /users 被 tree-sitter 抽取）。
3. 文档：`.env.example` 新增 codegraph LSP 段；`docker-compose.yaml` server env 加两开关（默认 false）；helm `configmap.yaml` + `values.yaml` 加 codegraph 段。

## Verification
`cd server && uv run pytest codegraph/ -q` 全绿（含预置 acceptance 转绿）。
