---
phase: 111-schema
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - server/delivery/models/artifact.py
  - server/delivery/models/blueprint_thread.py
  - server/delivery/models/blueprint_reviewer.py
  - server/delivery/models/__init__.py
  - server/delivery/migrations/0031_blueprint_models.py
  - server/delivery/services/event_taxonomy.py
  - server/delivery/services/blueprint_lifecycle_service.py
  - server/delivery/services/blueprint_anchor.py
  - server/tests/delivery/test_blueprint_models.py
  - server/tests/delivery/test_blueprint_lifecycle_service.py
  - server/tests/delivery/test_blueprint_anchor.py
  - server/tests/delivery/test_blueprint_inv6_guard.py
autonomous: true
requirements: [LIFE-01, LIFE-02, LIFE-03]
prohibitions:
  - "禁止修改 server/codegraph/services/repo_router_v2.py（STATE.md 并行纪律 1）"
  - "禁止修改 process_runtime 六个冻结文件（decompose_segments/research_adapter/architect_merge_adapter/merged_plan/clarify_adapter/render，纪律 2）"
  - "禁止修改 ConvergenceSessionEvent 模型字段与既有事件类型常量；只在 event_taxonomy.py 追加 blueprint_* 新常量（纪律 3）；新常量禁止加入 ALL_EVENTS（RESEARCH P4：覆盖性反查守护必挂）"
  - "禁止在 ArtifactStatus / ConvergenceSessionStatus 等既有枚举上扩值——蓝图 11 态是 Artifact 上的新字段新枚举"
  - "migration 只新增（0031），不改既有 0001–0030 任何文件"
  - "禁止先 get 再 save 的读改写——blueprint_status 写库必须 CAS filter().update() 命中计数判定"
must_haves:
  truths:
    - "蓝图 11 态转移由 BlueprintLifecycleService 单点收口，非法转移抛 ValueError 且状态不变（LIFE-01）"
    - "存在 open+blocking 线程时 pending_review→confirmed 被拒绝；线程 resolved/dismissed 后可确认（LIFE-02 前半）"
    - "执行确认动作的用户自动进入 BlueprintReviewer 名单（首个动作留痕，重复确认不覆盖 first_action），名单可手动增补（LIFE-02 后半）"
    - "failed/superseded 是显式终态；failed→researching 人工重试可用；archived/superseded 无出边（LIFE-03）"
    - "并发双写被 CAS 拒绝（ConcurrentBlueprintTransitionError）；每次转移 best-effort 写 blueprint.* 事件（无 session 时跳过持久化只打 structlog，绝不反噬业务）"
    - "重锚定纯函数：block_id 精确命中→anchored；quoted_text 相似度≥0.85 模糊命中→重挂新 block_id；否则 orphaned，绝不删线程"
  artifacts:
    - path: "server/delivery/models/blueprint_thread.py"
      provides: "BlueprintThread + BlueprintThreadMessage（DESIGN §6.1 全字段）"
    - path: "server/delivery/models/blueprint_reviewer.py"
      provides: "BlueprintReviewer（artifact+user 唯一）"
    - path: "server/delivery/services/blueprint_lifecycle_service.py"
      provides: "11 态转移守卫 + CAS + reviewer upsert + best-effort 事件"
      exports: ["BlueprintLifecycleService", "ConcurrentBlueprintTransitionError"]
    - path: "server/delivery/services/blueprint_anchor.py"
      provides: "reanchor 纯函数（阈值 0.85）"
      exports: ["reanchor", "SIMILARITY_THRESHOLD"]
    - path: "server/delivery/migrations/0031_blueprint_models.py"
      provides: "Artifact.blueprint_status 字段 + 三张新表（依赖 0030_humantask）"
  key_links:
    - from: "server/delivery/services/blueprint_lifecycle_service.py"
      to: "delivery.Artifact.blueprint_status"
      via: "CAS 条件更新（唯一字段级 writer，INV-6 守护测试锁死旁路）"
      pattern: "filter\\(id=.*blueprint_status=.*\\)\\.update"
    - from: "server/delivery/services/blueprint_lifecycle_service.py"
      to: "delivery.BlueprintThread"
      via: "confirm 守卫查询 open+blocking（aexists）"
      pattern: "blocking=True"
    - from: "server/delivery/services/blueprint_lifecycle_service.py"
      to: "server/delivery/services/event_taxonomy.py"
      via: "EVENT_BLUEPRINT_STATUS_TRANSITIONED 常量（BLUEPRINT_EVENTS 独立 frozenset，不进 ALL_EVENTS）"
      pattern: "EVENT_BLUEPRINT_STATUS_TRANSITIONED"
---

<objective>
落地蓝图生命周期的数据与服务底座：Artifact 新增 blueprint_status 11 态字段、BlueprintThread/BlueprintThreadMessage/BlueprintReviewer 三张新表（一条 migration）、BlueprintLifecycleService 单点收口全部状态写入（转移表守卫 + CAS 并发安全 + confirm 阻塞线程守卫 + 评审人 upsert + best-effort 事件），以及 114 将消费的重锚定纯函数。

Purpose: LIFE-01/02/03 的全部机器。112–116 所有状态变更都必须经由本 service（INV-6），事件面只新增 blueprint.* 常量绝不动 v0.19.0 契约。
Output: 两个新模型文件 + artifact 字段追加 + migration 0031 + 两个新 service + taxonomy 常量 + 四个测试文件。
</objective>

<execution_context>
工作目录：worktree 根。与 111-01/111-03 同 wave 并行——本 plan 不触碰它们的任何文件（builtin_types.py / process_runtime 新文件 / repositories / agents）。
</execution_context>

<context>
@.planning/phases/111-schema/111-CONTEXT.md（锁定：11 态清单 / 守卫 / reviewer / 事件 best-effort / anchor 阈值 0.85）
@.planning/phases/111-schema/111-RESEARCH.md（落点地图 #4–#10，现状事实 2.1–2.4，Pitfalls P1/P2/P3/P4/P5）
@.planning/technical-blueprint/DESIGN.md §4.2（状态机全图——转移表唯一权威）/ §4.3（守卫）/ §6.1（线程字段）/ §6.4（评审人语义）
@server/delivery/services/convergence_session_service.py（INV-6 状态机权威样板：CAS L191-237 / 异常 L45-51 / best-effort 事件 L304-335，只读）
@server/delivery/models/research_task.py（模型骨架 analog）
@server/delivery/models/artifact.py（宿主）
@server/delivery/services/event_taxonomy.py（RESERVED_EVENTS 先例 L95-101）
@server/tests/delivery/test_sdd_spec_transitions.py + test_sdd_spec_inv6_guard.py（测试样板）
</context>

<tasks>

<task type="auto">
  <name>Task 1: 三模型 + Artifact.blueprint_status 字段 + migration 0031</name>
  <files>server/delivery/models/artifact.py, server/delivery/models/blueprint_thread.py, server/delivery/models/blueprint_reviewer.py, server/delivery/models/__init__.py, server/delivery/migrations/0031_blueprint_models.py, server/tests/delivery/test_blueprint_models.py</files>
  <read_first>server/delivery/models/artifact.py（Meta.indexes 追加点 L89-92、字段风格 L65-69）、server/delivery/models/research_task.py（双模型文件骨架）、server/delivery/models/convergence_session.py L104-110（user FK 范式）、server/delivery/models/__init__.py（curated re-export）</read_first>
  <action>
① `server/delivery/models/artifact.py` 最小追加（不动既有字段/枚举）：
- 模块级 `class BlueprintStatus(models.TextChoices)`，11 值英文 snake_case 存库 + 中文 label（CONTEXT 锁定）：RESEARCHING="researching","调研中"；DRAFTING="drafting","产出中"；AI_REVIEWING="ai_reviewing","AI 审查中"；NEEDS_CLARIFICATION="needs_clarification","需要澄清"；PENDING_REVIEW="pending_review","待人类审查"；CONFIRMED="confirmed","已确认"；IMPLEMENTING="implementing","实施中"；IMPLEMENTED="implemented","实施完成"；ARCHIVED="archived","已归档"；FAILED="failed","已失败"；SUPERSEDED="superseded","已废弃"。
- `Artifact` 追加 `blueprint_status = models.CharField(max_length=32, choices=BlueprintStatus.choices, blank=True, default="")`——空串 = 旧 v0 数据不参与状态机（P5：needs_clarification 长 19 字符，max_length=32 放宽）；行内注释声明「字段级唯一 writer = BlueprintLifecycleService（INV-6），与既有 ArtifactStatus 正交（映射见 DESIGN §4.3）」。
- `Meta.indexes` 追加 `models.Index(fields=["artifact_type", "blueprint_status"])`。

② 新建 `server/delivery/models/blueprint_thread.py`（镜像 research_task.py 骨架；docstring 声明「Phase 111 / DESIGN §6.1；状态与业务流转唯一 writer=BlueprintLifecycleService（111 只建模型与守卫查询，线程业务流转归 114）；模型层零业务方法」）。模块级枚举：`ThreadAnchorStatus(anchored/orphaned)`、`ThreadKind(ai_clarification/ai_review_finding/human_comment/repo_confirmation)`、`ThreadSeverity(blocker/warning/info)`、`ThreadStatus(open/answered/resolved/dismissed)`，全部英文值中文 label。
- `BlueprintThread`：`id` UUID pk（default=uuid.uuid4, editable=False）；`artifact` FK `"delivery.Artifact"` CASCADE related_name="blueprint_threads"；`created_on_version` FK `"delivery.ArtifactVersion"` SET_NULL null=True blank=True related_name="+"；`anchor` JSONField(null=True, blank=True)——null=全局线程，形状注释 `{section_path, block_id, start_offset, end_offset, quoted_text}`；`anchor_status` CharField(max_length=16, choices=ThreadAnchorStatus, default=anchored)；`kind` CharField(max_length=24, choices=ThreadKind)（P5：ai_review_finding/repo_confirmation 17 字符）；`severity` CharField(max_length=16, choices=ThreadSeverity, blank=True, default="")；`blocking` BooleanField(default=False)；`options` JSONField(default=list, blank=True)；`status` CharField(max_length=16, choices=ThreadStatus, default=open)；`return_stage` CharField(max_length=16, blank=True, default="")（取值 researching/drafting/ai_reviewing，注释指向 DESIGN §6.1）；`initiated_by_user_id` CharField(max_length=64, default="system")；created_at/updated_at（auto_now_add/auto_now）。Meta：db_table="delivery_blueprint_thread"、中文 verbose_name、`indexes=[models.Index(fields=["artifact", "status", "blocking"])]`（confirm 守卫查询驱动）。
- `BlueprintThreadMessage`：`id` UUID pk；`thread` FK CASCADE related_name="messages"；`author_type` CharField(max_length=8, choices ai/human)；`author` FK settings.AUTH_USER_MODEL SET_NULL null=True blank=True related_name="+"；`body` TextField；created_at。Meta：db_table="delivery_blueprint_thread_message"、ordering=["created_at"]。

③ 新建 `server/delivery/models/blueprint_reviewer.py`：`BlueprintReviewer`——`id` UUID pk；`artifact` FK `"delivery.Artifact"` CASCADE related_name="blueprint_reviewers"；`user` FK settings.AUTH_USER_MODEL CASCADE related_name="+"；`first_action` CharField(max_length=32)（取值示例 repo_confirmation/final_approve/final_reject/manual_add，开放字符串）；created_at。Meta：db_table="delivery_blueprint_reviewer"、`constraints=[models.UniqueConstraint(fields=["artifact", "user"], name="uq_blueprint_reviewer_artifact_user")]`。docstring 声明 writer=BlueprintLifecycleService。

④ `server/delivery/models/__init__.py` 追加三模型 + BlueprintStatus 的 import 与 `__all__`。

⑤ 生成 migration：`cd server && uv run python manage.py makemigrations delivery -n blueprint_models`——产出 `0031_blueprint_models.py`（依赖 0030_humantask；带语义 slug 便于同步点 rebase 重排，纪律 5）。禁止手写 migration。

⑥ 新建 `server/tests/delivery/test_blueprint_models.py`（django_db）：`set(BlueprintStatus.values)` 恰为 11 值集合；Artifact 默认 `blueprint_status == ""`；建 thread（含 anchor=None 全局线程）+ message + reviewer 各一行可读回；同 (artifact, user) 第二行 reviewer 触发 IntegrityError；ThreadKind/ThreadStatus 值集合断言。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/delivery/test_blueprint_models.py -x -q && uv run python manage.py makemigrations --check --dry-run</automated>
  </verify>
  <acceptance_criteria>
- `ls server/delivery/migrations/ | grep -c "0031_blueprint_models"` 等于 1。
- `cd server && uv run python manage.py makemigrations --check --dry-run` 退出码 0（无缺失 migration）。
- `cd server && uv run pytest tests/delivery/test_blueprint_models.py -x -q` 全绿。
- `rg -c "needs_clarification" server/delivery/models/artifact.py` ≥ 1 且 `rg -n "max_length=16" server/delivery/models/artifact.py | rg blueprint` 零命中（P5 长度坑）。
  </acceptance_criteria>
  <done>三表一字段 + 0031 migration 就位，形状/约束/枚举有测试锁定，既有字段与枚举零改动。</done>
</task>

<task type="auto">
  <name>Task 2: event_taxonomy blueprint.* 常量 + BlueprintLifecycleService（11 态守卫 + CAS + reviewer + best-effort 事件）</name>
  <files>server/delivery/services/event_taxonomy.py, server/delivery/services/blueprint_lifecycle_service.py, server/tests/delivery/test_blueprint_lifecycle_service.py</files>
  <read_first>server/delivery/services/convergence_session_service.py（逐行范式：ConcurrentTransitionError L45-51、非法转移报错 L157-162、CAS L191-237、best-effort 事件 L304-335）、server/delivery/services/event_taxonomy.py（点分命名 + RESERVED_EVENTS 先例）、server/tests/services/test_event_taxonomy_alignment.py L22-25/L121-141（为何不能进 ALL_EVENTS）、server/tests/delivery/test_sdd_spec_transitions.py（测试骨架）</read_first>
  <action>
① `server/delivery/services/event_taxonomy.py` 只追加（既有常量与 ALL_EVENTS 成员一个不动）：
- `EVENT_BLUEPRINT_STATUS_TRANSITIONED: Final = "blueprint.status.transitioned"`（111 唯一实际 emit）
- `EVENT_BLUEPRINT_STAGE_STARTED: Final = "blueprint.stage.started"` / `EVENT_BLUEPRINT_STAGE_COMPLETED: Final = "blueprint.stage.completed"` / `EVENT_BLUEPRINT_STAGE_FAILED: Final = "blueprint.stage.failed"`（供 112+ 消费，本相位仅定义）
- `BLUEPRINT_EVENTS: Final[frozenset[str]] = frozenset({上述 4 值})`——独立集合，镜像 RESERVED_EVENTS 的「已定义但不计入 ALL_EVENTS」先例（P4：ALL_EVENTS 有覆盖性反查守护，111 的 emit 点不在其扫描清单）；注释写明原因。`__all__` 追加 5 个名字。

② 新建 `server/delivery/services/blueprint_lifecycle_service.py`——蓝图状态唯一写入入口（镜像 convergence_session_service 全套范式）。模块 docstring：INV-6 声明（本 service 是 `Artifact.blueprint_status` 字段与 BlueprintThread/BlueprintThreadMessage/BlueprintReviewer 三表的唯一生产 writer；ArtifactService 仍是 Artifact 行创建/版本/status 的唯一 writer——字段级分工，P2）+ 转移表全文 + P3（事件 session 可空跳过）+ P10 备注（一项目一份活跃蓝图的唯一性守卫由 112 的创建入口负责，本 service 不做）。`logger = structlog.get_logger(__name__)`。

- `class ConcurrentBlueprintTransitionError(RuntimeError)`：CAS 命中 ≠1 专用异常（镜像 L45-51）。
- 模块级 `_ALLOWED_TRANSITIONS: dict[str, set[str]]` 写死 DESIGN §4.2 全部合法边（"" 空串 = v0/未进入状态机，[*] 入口）：
  `""→{researching}`；`researching→{drafting, needs_clarification, failed, superseded}`；`drafting→{needs_clarification, ai_reviewing, failed, superseded}`；`ai_reviewing→{needs_clarification, drafting, pending_review}`；`needs_clarification→{researching, drafting, ai_reviewing}`；`pending_review→{drafting, confirmed, superseded}`；`confirmed→{implementing, drafting, superseded}`；`implementing→{implemented, drafting}`；`implemented→{archived}`；`failed→{researching}`（人工重试，LIFE-03）；`archived→set()`；`superseded→set()`（显式终态）。
- `class BlueprintLifecycleService` 核心方法 `async def transition(artifact, to_status, *, initiated_by_user_id, acting_user=None, session=None, return_status=None) -> Artifact`：
  1. `from_status = artifact.blueprint_status`；`to_status not in _ALLOWED_TRANSITIONS.get(from_status, set())` → `raise ValueError(f"非法蓝图状态转移：{from_status or '<未进入状态机>'} → {to_status}；合法目标={sorted(...)}")`（镜像 L157-162 报错风格）。
  2. `to_status == needs_clarification`：`return_status` 缺省取 `from_status`；必须 ∈ {researching, drafting, ai_reviewing} 否则 ValueError（DESIGN §4.1-3「回到原状态」；持久承载走 BlueprintThread.return_stage 由调用方写，本 service 只校验与透传进事件 payload——RESEARCH A4，不给 Artifact 加列）。
  3. `to_status == confirmed` 守卫（LIFE-02）：`await BlueprintThread.objects.filter(artifact=artifact, status=ThreadStatus.OPEN, blocking=True).aexists()` 为 True → `raise ValueError("存在未解决的阻塞澄清线程，蓝图不可确认")`。
  4. CAS 写库（P1/P2，@sync_to_async 内核）：`updated = Artifact.objects.filter(id=artifact.id, blueprint_status=from_status).update(blueprint_status=to_status, updated_at=timezone.now())`（Artifact.updated_at 若为 auto_now 字段，.update() 会绕过 auto_now，必须显式带上——先读 artifact.py 确认字段名与行为再写）；`updated != 1` → `raise ConcurrentBlueprintTransitionError(...)`（镜像 L219-227）；成功后同步内存 `artifact.blueprint_status = to_status`。
  5. reviewer upsert（LIFE-02）：`to_status == confirmed and acting_user is not None` 时 `await BlueprintReviewer.objects.aget_or_create(artifact=artifact, user=acting_user, defaults={"first_action": "final_approve"})`——aget_or_create 保证 first_action 只在首插写入（「确认类动作自动 upsert、重复不覆盖」）。
  6. 事件 best-effort（P3）：先 structlog caller 事件 `logger.info("blueprint_status_transitioned", category="caller", component="process_runtime", artifact_id=str(artifact.id), from_status=..., to_status=..., initiated_by_user_id=..., return_status=..., duration_ms=round((time.monotonic()-started)*1000, 2))`；`session is not None` 时才落 `ConvergenceSessionEvent(session=session, event=EVENT_BLUEPRINT_STATUS_TRANSITIONED, payload={artifact_id, from, to, initiated_by_user_id, return_status})`，整体 try/except 吞掉只 `logger.warning`（镜像 L316-324，观测不反噬业务）；绝不给 event 模型加字段。
- 辅助方法 `async def add_reviewer(artifact, user, first_action: str) -> BlueprintReviewer`：`aget_or_create` 同款（SC3「名单可手动增补」）。
- `__all__ = ["BlueprintLifecycleService", "ConcurrentBlueprintTransitionError"]`。

③ 新建 `server/tests/delivery/test_blueprint_lifecycle_service.py`（`pytestmark = pytest.mark.django_db(transaction=True)`，镜像 test_sdd_spec_transitions.py 分区注释组织）：
- 合法矩阵：参数化遍历 `_ALLOWED_TRANSITIONS` 全部边（从 service 模块 import 该常量），每条边从预置 from 状态出发 transition 成功且 DB 重读（aget）一致——含 ""→researching 入口与 failed→researching 重试（LIFE-03）。
- 非法转移 fail-loud：代表性非法边（researching→confirmed、implemented→drafting、archived→researching、superseded→drafting、""→drafting）`pytest.raises(ValueError)` 且 DB 状态未变。
- needs_clarification：return_status 非法值（如 confirmed）→ ValueError；缺省 return_status 取 from_status（断言 structlog 可省，用事件 payload 或返回后行为断言）。
- confirm 守卫：pending_review 的 artifact + open+blocking 线程 → ValueError；线程置 resolved 后 transition 成功；open+非 blocking 线程不阻塞。
- reviewer：confirm 时带 acting_user → BlueprintReviewer 行存在 first_action="final_approve"；对同 user 再走一轮 confirmed→drafting→…→confirmed → 仍一行且 first_action 未变；add_reviewer 手动增补第二个 user → 两行。
- CAS 并发：内存对象 from=pending_review，DB 已被旁改为 drafting → `pytest.raises(ConcurrentBlueprintTransitionError)`。
- 事件：session=None 时 ConvergenceSessionEvent 计数为 0（转移仍成功）；传入真实 ConvergenceSession 时新增一行 event=="blueprint.status.transitioned" 且 payload 含 from/to/artifact_id/initiated_by_user_id。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/delivery/test_blueprint_lifecycle_service.py -x -q && uv run pytest tests/services/test_event_taxonomy_alignment.py -x -q</automated>
  </verify>
  <acceptance_criteria>
- `cd server && uv run pytest tests/delivery/test_blueprint_lifecycle_service.py -x -q` 全绿。
- `cd server && uv run pytest tests/services/test_event_taxonomy_alignment.py -x -q` 全绿（证明 blueprint 常量未污染 ALL_EVENTS 守护，P4）。
- `rg -n "BLUEPRINT_EVENTS" server/delivery/services/event_taxonomy.py` 命中且 `rg -n "blueprint" server/delivery/services/event_taxonomy.py | rg "ALL_EVENTS"` 零命中。
- `rg -c "aget_or_create" server/delivery/services/blueprint_lifecycle_service.py` ≥ 1（first_action 首插语义）。
- `rg -n "\.save\(\)" server/delivery/services/blueprint_lifecycle_service.py` 对 Artifact 零命中（状态写全走 CAS update）。
  </acceptance_criteria>
  <done>11 态矩阵全边可走、非法边与并发被拒、confirm 被阻塞线程挡住、评审人自动入名单、事件 best-effort 不反噬——LIFE-01/02/03 服务面闭环。</done>
</task>

<task type="auto">
  <name>Task 3: blueprint_anchor.py 重锚定纯函数 + INV-6 旁路写守护测试</name>
  <files>server/delivery/services/blueprint_anchor.py, server/tests/delivery/test_blueprint_anchor.py, server/tests/delivery/test_blueprint_inv6_guard.py</files>
  <read_first>server/tests/delivery/test_sdd_spec_inv6_guard.py（守护测试全结构：三正则 + 排除清单 + 防形同虚设反向断言）、server/tests/delivery/test_artifact_inv6_guard.py L159-180（字段级守护先例）、server/services/process_runtime/wave_layering.py（纯函数模块形态）</read_first>
  <action>
① 新建 `server/delivery/services/blueprint_anchor.py`（纯函数：stdlib only，无 ORM/LLM——形态沿 wave_layering.py，算法自写因代码库无 difflib 先例）。docstring：Phase 111 / DESIGN §6.2 重锚定；111 只交付算法与单测，批量应用到线程行的调用方在 114。
- `SIMILARITY_THRESHOLD = 0.85`（CONTEXT 锁定值，模块常量）；`ANCHOR_STATUS_ANCHORED = "anchored"` / `ANCHOR_STATUS_ORPHANED = "orphaned"`。
- 私有 `_block_text(block: dict) -> str`：text 为 str 直取；text 为 list（list 型 block 的 items[]）逐条 join；pseudocode 取 `code.source`；table 把 rows 扁平 join；全部 .get 防御，返回 str。
- `reanchor(anchor: dict, new_blocks: list[dict]) -> tuple[dict, str]`：
  1. anchor 非 dict 或空 → `(anchor, ORPHANED)` 防御；
  2. 精确：`anchor.get("block_id")` 在 new_blocks 的 block_id 集合中 → `(anchor 原样, ANCHORED)`；
  3. 模糊：`quoted_text = anchor.get("quoted_text") or ""`，为空 → orphaned；否则对每个 new_block 计算 `difflib.SequenceMatcher(None, quoted_text, _block_text(block)).ratio()`，取最佳（同分取 block_id 字典序小者，确定性）；最佳 ≥ SIMILARITY_THRESHOLD → 返回 `(dict(anchor, block_id=best_block_id), ANCHORED)`（quoted_text 保留原文）；
  4. 否则 `(anchor 原样, ORPHANED)`——失锚不删线程（调用方语义）。
- `__all__ = ["reanchor", "SIMILARITY_THRESHOLD", "ANCHOR_STATUS_ANCHORED", "ANCHOR_STATUS_ORPHANED"]`。

② 新建 `server/tests/delivery/test_blueprint_anchor.py`（纯函数，无 django_db）：精确命中分支；块文本轻微编辑（相似度 >0.85）→ 重挂新 block_id 且 status=anchored；完全不同文本 → orphaned 且 anchor 原样；阈值边界——构造一对字符串实测 ratio 落在 0.85 附近两侧（用 difflib 现算断言，不硬编码猜测值）；quoted_text 为空 → orphaned；list/pseudocode 型块的 _block_text 提取正确。

③ 新建 `server/tests/delivery/test_blueprint_inv6_guard.py`（纯源码扫描，无 DB，镜像 test_sdd_spec_inv6_guard.py 结构）：
- `_ALLOWED_WRITER = "delivery/services/blueprint_lifecycle_service.py"`；扫描根 `SERVER_DIR = Path(__file__).resolve().parents[2]`；排除 tests/、migrations/、models 定义文件自身、venv 等（照抄 analog 的 `_is_scanned` 剪枝）。
- 守护 1（模型旁路写）：正则锚定 `(BlueprintThread|BlueprintThreadMessage|BlueprintReviewer)` 的 `\.objects\.(a?create|bulk_create|a?get_or_create|a?update_or_create)` 与直接实例化 `BlueprintThread\(`（带负向前瞻排除更长符号如 BlueprintThreadMessage/枚举类名，照抄 analog 手法）与链式 `.save(`——命中文件必须 == allowed writer，否则 fail 并列 `文件:行`。
- 守护 2（字段级，镜像 feishu_chat_id 先例）：正则 `blueprint_status\s*=`——排除 models/artifact.py（字段定义）、migrations、tests、本守护文件后，仅允许出现在 allowed writer（CAS update kwargs）。
- 守护的守护（防形同虚设）：断言 allowed writer 源码里确实存在 `blueprint_status=` 写入与 `aget_or_create`（writer 真的在写，正则真的能命中）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/delivery/test_blueprint_anchor.py tests/delivery/test_blueprint_inv6_guard.py -x -q</automated>
  </verify>
  <acceptance_criteria>
- `cd server && uv run pytest tests/delivery/test_blueprint_anchor.py tests/delivery/test_blueprint_inv6_guard.py -x -q` 全绿。
- `rg -n "SIMILARITY_THRESHOLD = 0.85" server/delivery/services/blueprint_anchor.py` 恰命中 1 处。
- `rg -n "import difflib|from difflib" server/delivery/services/blueprint_anchor.py` 命中（stdlib，零新依赖）。
- blueprint_anchor.py 无 ORM import：`rg -n "from delivery.models|from django" server/delivery/services/blueprint_anchor.py` 零命中。
  </acceptance_criteria>
  <done>三分支重锚定 + 0.85 阈值边界有测试；三模型与 blueprint_status 字段的旁路写被源码级守护锁死且守护自身被反向验证。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| transition() 调用面 | 112–116 与未来 REST 以任意顺序/并发调用状态转移 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-111-03 | Tampering (TOCTOU) | blueprint_status 并发双写 | mitigate | CAS filter+update 命中计数判定 + ConcurrentBlueprintTransitionError（convergence WR-01 先例） |
| T-111-04 | Elevation | 绕过守卫直写状态/线程/评审人 | mitigate | INV-6 源码扫描守护测试（模型级 + blueprint_status 字段级）锁死旁路 |
| T-111-05 | Repudiation | 谁触发的转移不可考 | mitigate | structlog caller 事件强制 initiated_by_user_id；确认动作落 BlueprintReviewer 署名留痕 |
| T-111-SC | Tampering | 依赖供应链 | accept | 零新增依赖（difflib stdlib） |
</threat_model>

<verification>
- `cd server && uv run pytest tests/delivery/test_blueprint_models.py tests/delivery/test_blueprint_lifecycle_service.py tests/delivery/test_blueprint_anchor.py tests/delivery/test_blueprint_inv6_guard.py tests/services/test_event_taxonomy_alignment.py -q` 全绿。
- `cd server && uv run python manage.py makemigrations --check --dry-run` 退出码 0。
- 冻结面自检：`git diff --name-only` 不含 convergence_session_event.py / convergence_session.py / repo_router_v2.py / process_runtime 六冻结文件；event_taxonomy.py 的 diff 只有追加行。
</verification>

<success_criteria>
- LIFE-01：11 态转移单点收口、非法转移抛异常、CAS 防并发、每次转移可追溯（structlog + 可选事件行）——ROADMAP SC2。
- LIFE-02：open+blocking 线程阻塞 confirm；确认动作自动入评审人名单、可手动增补、署名时间留痕——ROADMAP SC2/SC3。
- LIFE-03：failed/superseded 显式终态，failed→researching 重试边可走——ROADMAP SC2。
- 事件面零污染：既有类型/字段不动，blueprint.* 走独立 frozenset。
</success_criteria>

<output>
完成后创建 `.planning/phases/111-schema/111-02-SUMMARY.md`（provides：BlueprintLifecycleService/三模型/anchor 导出面，供 111-04 集成冒烟与 Phase 112/114 消费）。
</output>
