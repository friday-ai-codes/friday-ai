---
phase: 111-schema
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - server/agents/call_source.py
  - .planning/observability/LOGGING-SPEC.md
  - server/repositories/models.py
  - server/repositories/migrations/0040_repo_charter.py
  - server/repositories/services/charter_service.py
  - server/repositories/services/__init__.py
  - server/repositories/serializers.py
  - server/repositories/charter_views.py
  - server/repositories/urls.py
  - server/tests/repositories/test_charter_service.py
  - server/tests/repositories/test_charter_api.py
autonomous: true
requirements: [CHARTER-01]
prohibitions:
  - "禁止修改 server/codegraph/services/repo_router_v2.py（STATE.md 并行纪律 1；章程证据的路由融合在 112 的 blueprint_route adapter，本 plan 只建模型与起草管道）"
  - "禁止修改 process_runtime 六个冻结文件（decompose_segments.py 只读参考其 LLM 五步骨架，绝不改动）"
  - "禁止修改 ConvergenceSessionEvent 既有契约（本 plan 不触及事件面）"
  - "call_source.py 只追加枚举成员与注释、只改计数 docstring，既有 36 值一个不动；LOGGING-SPEC §4.1 只加行"
  - "migration 只新增（repositories 0040），不改 0001–0039"
  - "source=human_confirmed 的章程正式字段禁止被 AI 起草路径写入——只能写 draft_content（CHARTER-01 不变量）"
must_haves:
  truths:
    - "AI 可从 ai_summary/facets + 近期 MR 历史 + verified/rejected RepoAssociation 蒸馏出章程草案（source=ai_draft）；LLM 不可用时返回 None 且不落任何行（CHARTER-01 起草面）"
    - "人工 confirm 后 source=human_confirmed、version+1、confirmed_by 署名；此后再起草只写 draft_content，正式字段逐字节不变（CHARTER-01 不覆盖不变量）"
    - "charter 三端点（读取/生成草案/confirm）经 IsAuthenticated 可用；无章程仓 GET 返回 404"
    - "章程起草 LLM 调用带 call_source=blueprint_charter_draft，8 个新枚举值已登记 LOGGING-SPEC §4.1"
  artifacts:
    - path: "server/repositories/models.py"
      provides: "RepoCharter（OneToOne Repository + version + source + 结构化 JSONField + draft_content）"
      contains: "class RepoCharter"
    - path: "server/repositories/services/charter_service.py"
      provides: "adraft_charter / aconfirm_charter / normalize_charter_draft（RepoCharter 唯一 writer）"
    - path: "server/repositories/charter_views.py"
      provides: "GET charter / POST charter/draft / POST charter/confirm 三端点"
    - path: "server/agents/call_source.py"
      provides: "8 个 blueprint_* call_source 枚举值（含 BLUEPRINT_CHARTER_DRAFT）"
      contains: "BLUEPRINT_CHARTER_DRAFT"
  key_links:
    - from: "server/repositories/services/charter_service.py"
      to: "server/agents/call_source.py"
      via: "use_call_source(CallSource.BLUEPRINT_CHARTER_DRAFT) 包住 ainvoke"
      pattern: "BLUEPRINT_CHARTER_DRAFT"
    - from: "server/repositories/charter_views.py"
      to: "server/repositories/services/charter_service.py"
      via: "视图零 ORM 写，全部委托 service（INV-6）"
      pattern: "charter_service"
    - from: "server/repositories/urls.py"
      to: "server/repositories/charter_views.py"
      via: "<uuid:repository_id>/charter/ 三条 path（router include 之后的资源子路由区）"
      pattern: "charter"
---

<objective>
建立仓库章程（意图面知识）的完整底座：RepoCharter 模型（一仓一份、版本化、草案/正式分离）+ AI 起草管道（三源蒸馏 → LLM 单调用 → 归一化落草案）+ confirm 收口（人工确认生效、AI 永不覆盖人工）+ REST 三端点；同时注册 call_source 8 个新枚举值并登记 LOGGING-SPEC（本 plan 的 charter 起草是 111 唯一 LLM 调用点，其余 7 值供 112–114 消费）。

Purpose: CHARTER-01 全量交付；112 的 blueprint_route 双面路由与确认门回灌读写本模型。
Output: RepoCharter 模型 + migration 0040 + charter_service + 三端点 + call_source/LOGGING-SPEC 登记 + 两个测试文件。
</objective>

<execution_context>
工作目录：worktree 根。与 111-01/111-02 同 wave 并行——不触碰 process_runtime 新文件、delivery 任何文件。
</execution_context>

<context>
@.planning/phases/111-schema/111-CONTEXT.md（锁定：RepoCharter 落位/字段/起草三源/confirm 语义/call_source 8 值含 blueprint_charter_draft）
@.planning/phases/111-schema/111-RESEARCH.md（落点地图 #12–#17，现状事实 2.7–2.11，Pitfall P11，Open Question Q1 已由 CONTEXT 定夺为第 8 值）
@.planning/technical-blueprint/DESIGN.md §5.7（RepoCharter 字段形状与「AI 不覆盖人工」原则的直接规格）
@server/repositories/models.py（单文件模块；GitCredential L654-682 OneToOne 样板、SensitiveFileSuggestion L1005+ 嵌套 TextChoices/约束样板）
@server/services/process_runtime/decompose_segments.py（LLM 五步骨架 L134-212 + JSON 三层防御 L35-105，只读冻结）
@server/repositories/route_views.py + urls.py + serializers.py（REST 三件套 analog）
@server/agents/call_source.py（36 值现状 + 注释风格 L66-107）
@.planning/observability/LOGGING-SPEC.md §4.1（call_source 登记表）
</context>

<tasks>

<task type="auto">
  <name>Task 1: call_source 8 值注册 + LOGGING-SPEC §4.1 登记 + RepoCharter 模型 + migration 0040</name>
  <files>server/agents/call_source.py, .planning/observability/LOGGING-SPEC.md, server/repositories/models.py, server/repositories/migrations/0040_repo_charter.py</files>
  <read_first>server/agents/call_source.py（成员注释风格、docstring 计数两处 L7/L38）、server/repositories/models.py L654-682（GitCredential OneToOne）与 L1005 起（SensitiveFileSuggestion 嵌套枚举/Meta 命名）、.planning/observability/LOGGING-SPEC.md §4.1 表格式</read_first>
  <action>
① `server/agents/call_source.py`：`CallSource` 枚举尾部追加 8 个成员（CONTEXT 锁定，含 RESEARCH Q1 定夺的第 8 值），每值带「phase 来源 + 用途」注释（沿既有 L66-107 风格）：
`BLUEPRINT_DECOMPOSE = "blueprint_decompose"`（112 需求对齐）/ `BLUEPRINT_SPEC_GATE = "blueprint_spec_gate"`（112 歧义门）/ `BLUEPRINT_REPO_RESEARCH = "blueprint_repo_research"`（112 逐仓调研）/ `BLUEPRINT_REROUTE = "blueprint_reroute"`（112 重路由）/ `BLUEPRINT_REPO_PLAN = "blueprint_repo_plan"`（113 分仓方案）/ `BLUEPRINT_MERGE = "blueprint_merge"`（113 融合装配）/ `BLUEPRINT_AI_REVIEW = "blueprint_ai_review"`（114 对抗审查）/ `BLUEPRINT_CHARTER_DRAFT = "blueprint_charter_draft"`（111 章程起草，本相位唯一实际调用点）。模块 docstring 与类 docstring 的「36 值」计数同步改「44 值」（两处，L7/L38）。既有成员、normalize、use_call_source 零改动。

② `.planning/observability/LOGGING-SPEC.md` §4.1 call_source 表追加 8 行（列格式照抄既有行：值 / 含义 / 引入 phase——标注 v0.20.0 Phase 111，前 7 值备注「调用点在 112–114 落地」）。§5/§10 本 plan 不动（blueprint_* structlog 事件已在 111-02 的 lifecycle service 与本 plan charter 事件按规范命名，目录登记随 112 编排相位统一补）。

③ `server/repositories/models.py` 文件尾部（SensitiveFileSuggestion 之后）追加 `RepoCharter`（repositories 惯例是单文件模块，不建分包）。docstring：DESIGN §5.7 出处 + INV-6 声明「唯一 writer = repositories/services/charter_service.py，模型层零业务方法」+「AI 不覆盖人工：source=human_confirmed 后 AI 起草只写 draft_content」契约。类内嵌套枚举（近期风格）：`class Source(models.TextChoices)`：AI_DRAFT="ai_draft","AI 草案"；HUMAN_CONFIRMED="human_confirmed","人工确认"。`class Evolution(models.TextChoices)`：ACTIVE="active","活跃演进"；MAINTENANCE_ONLY="maintenance_only","仅维护"；DEPRECATED="deprecated","已废弃"。
字段：`id` UUID pk（default=uuid.uuid4, editable=False）；`repository = models.OneToOneField(Repository, on_delete=models.CASCADE, related_name="charter")`（一仓一份，GitCredential L658-662 范式）；`version = models.PositiveIntegerField(default=1)`；`source = models.CharField(max_length=16, choices=Source.choices, default=Source.AI_DRAFT)`；`confirmed_by = models.ForeignKey(settings.AUTH_USER_MODEL, on_delete=models.SET_NULL, null=True, blank=True, related_name="+")`；`positioning = models.TextField(blank=True, default="")`（一句话定位）；`owned_domains = models.JSONField(default=list, blank=True)`（形状注释 `[{domain, status: implemented|planned, note, citations}]`）；`boundaries = models.JSONField(default=list, blank=True)`（`[{rule, decided_by, citations}]`）；`placement_preferences = models.JSONField(default=list, blank=True)`（`[{kind, target, note}]`）；`audience = models.CharField(max_length=64, blank=True, default="")`；`form = models.CharField(max_length=64, blank=True, default="")`（与 facets 服务对象/技术形态对齐）；`evolution = models.CharField(max_length=24, choices=Evolution.choices, default=Evolution.ACTIVE)`（maintenance_only 16 字符，max_length 放宽）；`draft_content = models.JSONField(default=dict, blank=True)`（pending 修订草案——「单行 + 草案列」承载版本化与不覆盖，RESEARCH A3）；created_at/updated_at。Meta：`db_table = "repo_charters"`、中文 verbose_name。

④ 生成 migration：`cd server && uv run python manage.py makemigrations repositories -n repo_charter` → `0040_repo_charter.py`（依赖 0039_repository_git_instance_credential + accounts；禁止手写）。
  </action>
  <verify>
    <automated>cd server && uv run python manage.py makemigrations --check --dry-run && uv run python -c "from repositories.models import RepoCharter; from agents.call_source import CallSource; assert CallSource.BLUEPRINT_CHARTER_DRAFT.value == 'blueprint_charter_draft'; assert len(list(CallSource)) == 44"</automated>
  </verify>
  <acceptance_criteria>
- `ls server/repositories/migrations/ | grep -c "0040_repo_charter"` 等于 1。
- `rg -c "BLUEPRINT_" server/agents/call_source.py` ≥ 8；`rg -c "36 值" server/agents/call_source.py` 等于 0（计数已更新为 44）。
- `rg -c "blueprint_charter_draft" .planning/observability/LOGGING-SPEC.md` ≥ 1（§4.1 已登记 8 行之一）。
- `rg -n "class RepoCharter" server/repositories/models.py` 命中；`rg -n "OneToOneField\(Repository" server/repositories/models.py | rg charter -i` 命中。
- `cd server && uv run python manage.py makemigrations --check --dry-run` 退出码 0。
  </acceptance_criteria>
  <done>8 值枚举 + 规范登记 + 一仓一份章程模型 + 0040 migration 就位，既有 36 值与 0039 之前 migration 零改动。</done>
</task>

<task type="auto">
  <name>Task 2: charter_service 起草管道与 confirm 收口 + 不覆盖不变量测试</name>
  <files>server/repositories/services/charter_service.py, server/repositories/services/__init__.py, server/tests/repositories/test_charter_service.py</files>
  <read_first>server/services/process_runtime/decompose_segments.py L35-212（五步骨架/JSON 双路解析/normalize/观测三事件——只读照抄范式）、server/agents/llm_factory.py L64-75（build_chat_model 签名）、server/initiatives/models/repo_association.py L30-92 与 merge_request.py L37-114（蒸馏输入字段）、server/tests/helpers/fake_chat_model.py 与 server/tests/services/test_decompose_segments.py（LLM mock 的 patch 点）</read_first>
  <action>
① 新建 `server/repositories/services/charter_service.py`（RepoCharter 唯一写入入口，INV-6）。模块 docstring：CHARTER-01 / DESIGN §5.7 / 三源蒸馏 / 「AI 不覆盖人工」不变量（P11）。`logger = structlog.get_logger(__name__)`；`__all__ = ["adraft_charter", "aconfirm_charter", "normalize_charter_draft"]`。模块函数风格（镜像 decompose_segments）：

- `normalize_charter_draft(data) -> dict`（独立可测纯函数）：白名单字段 positioning(str, 截断 500)/owned_domains(list[dict]，item 白名单 domain/status/note/citations，status 只认 implemented|planned 否则回退 implemented)/boundaries(list[dict]，白名单 rule/decided_by/citations)/placement_preferences(list[dict]，白名单 kind/target/note)/audience(str,64)/form(str,64)/evolution(str，只认 active|maintenance_only|deprecated 否则回退 active)；非法类型逐字段回退空值，绝不抛。
- `async def adraft_charter(repository_id: str, *, initiated_by_user_id: str = "system") -> RepoCharter | None`：
  1. `Repository.objects.aget(id=repository_id)`（不存在让 DoesNotExist 上抛，视图层转 404）。
  2. 蒸馏输入三源（全部 sync_to_async 包 ORM，P1）：`repo.overview_text` property + `repo.facets`；近期 MR 历史 `initiatives.models.MergeRequest.objects.filter(repository_id=repository_id).order_by("-created_at")[:20]` 取 title/status（懒 import initiatives，函数内）；`initiatives.models.RepoAssociation.objects.filter(repository_id=repository_id, status__in=["verified", "rejected"])[:20]` 取 status/routed_reason（verified 作 owned 证据、rejected 作 boundaries 候选）。
  3. LLM 五步骨架（逐条镜像 decompose_segments L150-183）：lazy import → `resolved = await ProviderConfigService.aresolve()` → `model_name = (resolved.extra or {}).get("default_model", "")`（无 provider/model → structlog warning + return None，不落行）→ `build_chat_model(resolved, model_name, streaming=False)` → `with use_call_source(CallSource.BLUEPRINT_CHARTER_DRAFT): response = await model.ainvoke(messages)`。
  4. prompt 两私有函数：`_system_prompt()` 写死输出 JSON 形状约定（§5.7 七字段，中文说明）；`_build_prompt(...)` 按「## 仓库摘要 / ## 语义分面 / ## 近期 MR / ## 关联裁决（verified→owned 证据、rejected→边界候选）」分节拼装。
  5. 解析：`_content_to_text`（兼容 reasoning content_blocks）→ ```json 代码块 + 裸 JSON 双路提取 → `normalize_charter_draft`。解析失败 → warning + return None。
  6. 落库（@sync_to_async 内核 + transaction.atomic）：无 charter → `RepoCharter.objects.create(repository=repo, source=Source.AI_DRAFT, version=1, **正式字段=草案值)`；已有且 `source == AI_DRAFT` → 正式字段就地更新（仍是草案，version 不变）；已有且 `source == HUMAN_CONFIRMED` → **只写 `draft_content = 草案 dict`，正式字段一个不碰**（P11 不变量）。
  7. 全程外层 try/except：异常经 `redact_secrets_in_text(str(exc))` 脱敏后 `logger.warning("charter_draft_failed", ...)`，return None 绝不抛（best-effort；DB 写失败同样吞）。观测三事件：`charter_draft_started/completed/failed`，kv 带 `category="caller"`、`component="charter_service"`、`repository_id`、`initiated_by_user_id`、`duration_ms`（completed/failed）。
- `async def aconfirm_charter(repository_id: str, user, *, edits: dict | None = None) -> RepoCharter`：charter 不存在 → `raise ValueError("章程不存在，请先生成草案")`；@sync_to_async 内核：若 `draft_content` 非空先将其（过 normalize_charter_draft）提升为正式字段；再套用 `edits`（同 normalize 白名单）；`version += 1`、`source = HUMAN_CONFIRMED`、`confirmed_by = user`、`draft_content = {}`；save。structlog `charter_confirmed`（category="caller", component="charter_service", initiated_by_user_id=str(user.id), repository_id, version）。
- `server/repositories/services/__init__.py` docstring 补一行 charter_service 说明（现状事实 2.7）。

② 新建 `server/tests/repositories/test_charter_service.py`（django_db(transaction=True)；LLM mock：patch `repositories.services.charter_service.build_chat_model` 返回 `tests.helpers.fake_chat_model.FakeChatModel`（响应体为合法章程 JSON），参照 test_decompose_segments.py 的 patch 手法）：
- 起草：无 charter 时 adraft_charter → 建行 source=ai_draft、version=1、positioning/owned_domains 来自 mock JSON 且过 normalize。
- provider 缺失：patch `ProviderConfigService.aresolve` 返回 None/无 default_model → 返回 None 且 `RepoCharter.objects.count() == 0`。
- LLM 返回非法 JSON → None、不落行。
- confirm：`aconfirm_charter(repo_id, user)` → source=human_confirmed、version=2、confirmed_by=user、draft_content=={}。
- **不覆盖不变量（P11 核心）**：human_confirmed 后再跑 adraft_charter（mock 返回不同 positioning）→ 重读行：正式字段（positioning/owned_domains/boundaries/placement_preferences/audience/form/evolution/source/version/confirmed_by）逐一与 confirm 后快照相等，仅 draft_content 变为新草案。
- confirm 带 edits：edits={"positioning": "人工改写"} → 生效且 evolution 非法值被 normalize 回退。
- draft_content 提升：human_confirmed + 非空 draft_content 再 confirm → 草案字段提升为正式、version+1、draft 清空。
- `normalize_charter_draft` 纯函数边界：非 dict 输入、owned_domains 非 list、status 非法值回退。
- INV-6 守护（源码扫描小节，无 DB）：扫描 server/（排除 tests/migrations/models.py 定义处）`RepoCharter` 的 `.objects.(a?create|a?update_or_create|a?get_or_create|bulk_create)`、`RepoCharter(` 实例化、`.save(` 链式——仅允许出现在 repositories/services/charter_service.py；附「writer 确实在写」反向断言。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/repositories/test_charter_service.py -x -q</automated>
  </verify>
  <acceptance_criteria>
- `cd server && uv run pytest tests/repositories/test_charter_service.py -x -q` 全绿。
- `rg -n "use_call_source\(CallSource.BLUEPRINT_CHARTER_DRAFT\)" server/repositories/services/charter_service.py` 命中。
- `rg -n "redact_secrets_in_text" server/repositories/services/charter_service.py` 命中（异常脱敏强制）。
- `rg -c "charter_draft_started|charter_draft_completed|charter_draft_failed|charter_confirmed" server/repositories/services/charter_service.py` ≥ 4（观测事件齐）。
  </acceptance_criteria>
  <done>三源蒸馏草案可产、confirm 生效署名、人工确认后 AI 只进 draft_content 的不变量有测试锁死、LLM 失败零副作用。</done>
</task>

<task type="auto">
  <name>Task 3: charter REST 三端点 + urls 接线 + API 测试</name>
  <files>server/repositories/charter_views.py, server/repositories/serializers.py, server/repositories/urls.py, server/tests/repositories/test_charter_api.py</files>
  <read_first>server/repositories/route_views.py（adrf APIView 微样板）、server/repositories/urls.py L149-322（<uuid:repository_id>/xxx/ 资源子路由排布——字面段在 router include 之前、资源子路由在之后）、server/repositories/serializers.py L272-297（只读 ModelSerializer 契约注释）、server/delivery/api/artifact_views.py（.data 必须 sync_to_async 包裹 L95-97 + 404 中性消息 L116-119）、server/tests/repositories/test_sensitive_suggestions_api.py（API 测试惯例：client 构造/鉴权/断言）</read_first>
  <action>
① `server/repositories/serializers.py` 追加 `RepoCharterSerializer(serializers.ModelSerializer)`：fields = ["id", "repository", "version", "source", "confirmed_by", "positioning", "owned_domains", "boundaries", "placement_preferences", "audience", "form", "evolution", "draft_content", "created_at", "updated_at"]，`read_only_fields = fields`（状态只经 service 变更，注释对齐 L272-297 契约）。

② 新建 `server/repositories/charter_views.py`（按功能拆小 view 文件的 repositories 惯例，不进 views.py 巨石）。三个 adrf `APIView`，`permission_classes = [IsAuthenticated]`，重依赖方法内懒 import，serializer `.data` 一律 `sync_to_async` 包裹：
- `RepoCharterDetailView.get(request, repository_id)`：`RepoCharter.objects.select_related("repository").aget(repository_id=repository_id)`（读路径在视图允许；写仍收口 service）→ 200 序列化；DoesNotExist → 404 `{"detail": "章程不存在"}`（中性消息）。
- `RepoCharterDraftView.post(request, repository_id)`：调 `charter_service.adraft_charter(str(repository_id), initiated_by_user_id=str(request.user.id))`；Repository.DoesNotExist → 404；返回 None（无 provider/LLM 失败）→ 503 `{"detail": "AI 起草暂不可用，请检查模型供应商配置"}`；成功 → 200 序列化（含 draft_content，供前端预览草案）。
- `RepoCharterConfirmView.post(request, repository_id)`：body 可带 `{"edits": {...}}`（无 body 允许）；调 `aconfirm_charter(str(repository_id), request.user, edits=body.get("edits"))`；ValueError（章程不存在）→ 404；成功 → 200 序列化。
- 视图内禁止任何 RepoCharter 写操作（INV-6，charter_service 测试的守护正则会扫到）。

③ `server/repositories/urls.py`：顶部 import 三视图；在 router include **之后**的 `<uuid:repository_id>/xxx/` 资源子路由区（L149-322 排布）追加三条：
`path("<uuid:repository_id>/charter/", RepoCharterDetailView.as_view(), name="repository-charter")`、
`path("<uuid:repository_id>/charter/draft/", RepoCharterDraftView.as_view(), name="repository-charter-draft")`、
`path("<uuid:repository_id>/charter/confirm/", RepoCharterConfirmView.as_view(), name="repository-charter-confirm")`（kebab-case name 风格；charter 是字面子段不与 uuid 通配冲突）。

④ 新建 `server/tests/repositories/test_charter_api.py`（镜像 test_sensitive_suggestions_api.py 的 client/鉴权惯例；LLM mock 同 Task 2 patch 点）：未认证请求 → 401/403（按既有测试实际断言值）；GET 无章程 → 404；POST draft（mock LLM）→ 200 且 body.source=="ai_draft"、positioning 来自 mock；GET → 200；POST confirm 带 edits → 200 且 source=="human_confirmed"、version==2、edits 生效；POST draft 当 provider 缺失（patch aresolve）→ 503；不存在的 repository_id → 404。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/repositories/test_charter_api.py -x -q</automated>
  </verify>
  <acceptance_criteria>
- `cd server && uv run pytest tests/repositories/test_charter_api.py -x -q` 全绿。
- `rg -c "charter/" server/repositories/urls.py` ≥ 3（三条路由）。
- `rg -n "IsAuthenticated" server/repositories/charter_views.py` 命中 ≥ 3。
- `rg -n "RepoCharter.objects.(create|update)" server/repositories/charter_views.py` 零命中（视图零写）。
- `rg -n "sync_to_async" server/repositories/charter_views.py` 命中（serializer .data 纪律）。
  </acceptance_criteria>
  <done>三端点鉴权可用、404/503 语义正确、写路径全部收口 service，API 测试覆盖全部分支。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| charter REST 三端点 | 认证用户输入（repository_id path + edits body）进入服务层 |
| LLM 上游响应 | 半可信 JSON 文本进入归一化与落库 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-111-06 | Elevation | charter 端点 | mitigate | IsAuthenticated（repositories 既有 view 同级，CONTEXT/§6.4 低门槛决策）；confirm 绑定 request.user 落 confirmed_by |
| T-111-07 | Tampering | edits body / LLM JSON | mitigate | normalize_charter_draft 白名单 + 截断 + 枚举回退；UUID path 由 Django converter 天然校验 |
| T-111-08 | Info Disclosure | LLM 异常文本 | mitigate | redact_secrets_in_text 包异常后再入日志（decompose_segments L209 先例） |
| T-111-09 | Tampering | AI 覆盖人工确认内容 | mitigate | service 单点判断 source==human_confirmed 只写 draft_content + P11 不变量专测 |
| T-111-SC | Tampering | 依赖供应链 | accept | 零新增依赖 |
</threat_model>

<verification>
- `cd server && uv run pytest tests/repositories/test_charter_service.py tests/repositories/test_charter_api.py -q` 全绿。
- `cd server && uv run python manage.py makemigrations --check --dry-run` 退出码 0。
- 冻结面自检：`git diff --name-only` 不含 repo_router_v2.py / process_runtime 六冻结文件 / delivery 任何文件；call_source.py 的 diff 只有追加与两处计数改动。
</verification>

<success_criteria>
- CHARTER-01：一仓一份版本化章程；AI 从 ai_summary/MR 历史/RepoAssociation 蒸馏草案；人工 confirm 生效（source=human_confirmed）；人工确认内容不被 AI 覆盖、AI 只可提修订草案（ROADMAP SC4 全量）。
- call_source 8 值注册 + LOGGING-SPEC §4.1 登记完成（观测规范强制项）。
</success_criteria>

<output>
完成后创建 `.planning/phases/111-schema/111-03-SUMMARY.md`（provides：RepoCharter/charter_service/三端点/8 个 call_source 值，供 112 双面路由与确认门回灌消费）。
</output>
