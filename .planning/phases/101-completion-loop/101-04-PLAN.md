---
phase: 101-completion-loop
plan: 04
type: execute
wave: 3
depends_on: [101-02, 101-03]
files_modified:
  - server/tools/handlers/__init__.py
  - server/tools/handlers/skill_steps.py
  - server/tools/migrations/0005_seed_platform_skills.py
  - server/tools/sources/skill.py
  - server/tools/executor.py
  - server/tools/views.py
  - server/mcp_tools/pr_review_capture.py
  - server/workflows/nodes/ai/coding.py
  - server/orchestration/coding_graph.py
  - server/tests/tools/__init__.py
  - server/tests/tools/test_platform_skills.py
  - server/tests/mcp_tools/test_pr_review_capture.py
autonomous: true
requirements: [LOOP-04, LOOP-05]
must_haves:
  truths:
    - "pre_coding_research / post_coding_capture 两个 RemoteTool(source=skill) 种子存在，POST /api/tools/execute/ 可调"
    - "Skill 执行每步产结构化事件 + 步级 ToolCallRecord（run 可用时），步级 trace 可查"
    - "PR 创建成功后开关开启时 best-effort 触发轻量 review 并沉淀为一条 learning case；开关默认关，关闭时零 LLM 调用"
  artifacts:
    - path: "server/tools/migrations/0005_seed_platform_skills.py"
      provides: "两个 SKILL 种子 + 步骤 builtin 工具种子"
    - path: "server/mcp_tools/pr_review_capture.py"
      provides: "acapture_pr_review（开关→diff 摘要→LLM review→沉淀入 LOOP-03 路径）"
      min_lines: 80
    - path: "server/tools/handlers/skill_steps.py"
      provides: "步骤工具 handler 薄封装（复用既有 service，零重复实现）"
  key_links:
    - from: "server/tools/sources/skill.py"
      to: "interactions.ledger.arecord_tool_call"
      via: "步级 trace 落 ledger"
      pattern: "arecord_tool_call"
    - from: "server/mcp_tools/pr_review_capture.py"
      to: "mcp_tools.learning_case_extraction"
      via: "review 结论沉淀复用 LOOP-03 入库路径"
      pattern: "aextract_learning_case|_apersist"
    - from: "server/mcp_tools/pr_review_capture.py"
      to: "workflows.nodes.ai.code_review.REVIEW_SYSTEM_PROMPT"
      via: "review prompt 基底复用残留常量"
      pattern: "REVIEW_SYSTEM_PROMPT"
---

<objective>
LOOP-04 + LOOP-05：种两个平台多步 Skill（`pre_coding_research`：route_repositories→search_rag_chunks→search_delivery_knowledge→search_learning_cases；`post_coding_capture`：summarize_branch→create_learning_case→report_project_knowledge），补齐 skill 步级 trace（结构化事件 + 步级 ToolCallRecord）；PR 创建成功锚点接可选轻量 review 沉淀（默认关、best-effort、结论落 learning case）。

Purpose: ROADMAP 成功标准 4 全部 + 标准 5 的 pr_review_capture 半（call_source 已由 101-02 先登记）。范围锁定"能跑通 + 沉淀"：不做评审 UI、不做规则引擎、不回写 review 意见到 PR（CONTEXT Out of Scope）。
Output: 步骤 handler 薄封装 + 种子 migration + skill 步级 trace + review capture 模块 + 双锚点触发接线 + 测试。
</objective>

<execution_context>
GSD Phase 101 wave 3（`coding.py` / `coding_graph.py` 与 101-03 冲突故后置）。完成后写 `.planning/phases/101-completion-loop/101-04-SUMMARY.md`。
</execution_context>

<context>
@.planning/phases/101-completion-loop/101-CONTEXT.md
@server/tools/sources/skill.py
@server/tools/executor.py
@server/tools/views.py
@server/tools/migrations/0002_seed_builtin_tools.py
@server/mcp_tools/merge_request_service.py
@server/workflows/nodes/ai/code_review.py
@server/mcp_tools/learning_case_extraction.py

关键既有坐标（已读码核实）：
- Skill 执行：`tools/sources/skill.py` `execute_skill(tool, arguments)`——steps 顺序执行、首败中断；现状仅顶层一次 `arecord_tool_call`（views.py L68–76）+ 每步 `logger.info("execute_skill_step", ...)`，**步级 ledger 缺失**（CONTEXT 关键事实）。
- 分发：`tools/executor.py` `execute_tool(tool_name, arguments)` → `_dispatch` 按 Source 路由（builtin 经 `config["handler"]` dotted-path 动态 import——`tools/sources/builtin.py`）。
- **步骤工具解析现状（executor 必读）**：skill steps 里的 `route_repositories` 等名字经 `execute_tool` 查 `RemoteTool` 表——这些 MCP 工具**不在表里**（只存在于 `mcp_tools/views.py` 的 HTTP 视图，如 RouteRepositoriesView L413+ 内联调 `RepoRouterV2.route`）。最小实现（planner 决策）：为 7 个步骤工具各种一行 `builtin` RemoteTool，handler 指向新建薄封装模块 `tools/handlers/skill_steps.py`——封装只做参数适配与结果 JSON 化，**逻辑全部委托既有可调用物**：
  - `route_repositories` → `codegraph.services.repo_router_v2.RepoRouterV2.route(query, top_k)`
  - `search_rag_chunks` → `services.retrieval.rag_search.search_rag(...)`（读该函数签名后适配；多仓参数按其现有形态）
  - `search_delivery_knowledge` → `knowledge` 检索 `DeliveryKnowledgeSearchService`（`agents/tools/delivery_knowledge_tools.py` L101 有 chat 侧封装可参考入参形态）
  - `search_learning_cases` → `mcp_tools.learning_case_service.search_learning_cases(query=..., work_item_type=..., repo_hints=[], file_hints=[], symbol_hints=[], limit=...)`（函数已存在）
  - `summarize_branch` → `mcp_tools.merge_request_service.summarize_branch(repository=<经 repository_id 查 Repository>, source_branch=..., target_branch=..., max_files=..., trace=None)`
  - `create_learning_case` → `mcp_tools.learning_case_service` 的 create 函数（需 technical_plan_id；封装接受其为参数，缺时返回明确错误 dict 而非抛裸异常）
  - `report_project_knowledge` → 读 `mcp_tools/views.py` 中同名视图找到底层 service 后委托（handler 需要的最小参数集以该 service 签名为准）
- 种子 migration 范本：`tools/migrations/0002_seed_builtin_tools.py`（get_or_create + reverse delete；最新 migration 为 0004）。
- 步骤参数机制现状：`step.get("arguments", {})` 仅静态——CONTEXT 锁定最小实现"steps 接受静态 arguments + 顶层输入透传，文档写明"。
- review 基底：`workflows/nodes/ai/code_review.py` L12–51 `REVIEW_SYSTEM_PROMPT`（残留常量，**只 import 不修改**——migration replay 依赖其字节级一致）。
- diff 摘要：`merge_request_service.summarize_branch`（L74–126，返回 files/risks/test_suggestions/mr_draft）。
- 沉淀路径：`learning_case_extraction.aextract_learning_case`（101-02 交付；`source="pr_review"` + `idempotency_suffix=":pr_review"` 复用其幂等/质量门/脱敏/入库/入图——**不另写入库代码**）。
- 开关：`SettingKeys.PR_REVIEW_CAPTURE`（101-02 已加，默认关）；LLM 范式镜像 memory_distill（`call_source=CallSource.PR_REVIEW_CAPTURE`，101-02 已登记）。
- 锚点：`coding.py` `_finalize_and_notify` 完工闭环块（101-03 已建）与 `coding_graph.py` PR 成功分支（101-03 已建）——本 plan 在两处闭环块内各加一行 review 调度。
</context>

<tasks>

<task type="auto">
  <name>Task 1: 步骤工具薄封装 + 两个平台 Skill 种子 migration</name>
  <files>server/tools/handlers/__init__.py, server/tools/handlers/skill_steps.py, server/tools/migrations/0005_seed_platform_skills.py</files>
  <action>
1. 新建 `server/tools/handlers/`（含空 `__init__.py`）与 `skill_steps.py`（中文 docstring：LOOP-04 步骤工具 handler；只做参数适配 + 结果 JSON 化，逻辑委托既有 service——按 context 中 7 项映射逐一实现 async 函数，签名与 RemoteTool input_schema 一致，全部 `**kwargs` 容忍多余键）。每个 handler 返回 dict（execute_builtin 会 str() 非 ToolResult 返回——为保持结果可读，handler 返回 `json.dumps(..., ensure_ascii=False)` 字符串或带 `to_content` 的轻量对象，取一致做法并在 docstring 写明）。`create_learning_case` / `summarize_branch` 缺必需参数时返回含 `error` 键的 JSON 而非抛异常（skill 步骤失败语义由 execute_tool 的 ok=False 承载）。
2. 新建 migration `server/tools/migrations/0005_seed_platform_skills.py`（dependencies=[("tools", "0004_delete_tooltokenbinding")]，范本照抄 0002）：
   - 先 get_or_create 7 个步骤工具行：`source="builtin"`、`config={"handler": "tools.handlers.skill_steps.<fn>"}`、`input_schema` 按各 handler 签名写 JSON schema（query/top_k/limit/repository_id/source_branch/target_branch/technical_plan_id 等，required 最小化）、`timeout` 检索类 30、summarize/create 类 60。
   - 再 get_or_create 两个 SKILL 行：
     - `pre_coding_research`：`source="skill"`、`description`（中文：编码前调研——路由仓库→语义检索→交付知识→历史 learning case）、`input_schema={"type":"object","properties":{"query":{"type":"string"}},"required":["query"]}`、`timeout=120`、`config={"steps":[{"tool_name":"route_repositories","arguments":{}},{"tool_name":"search_rag_chunks","arguments":{}},{"tool_name":"search_delivery_knowledge","arguments":{}},{"tool_name":"search_learning_cases","arguments":{}}]}`。
     - `post_coding_capture`：steps = summarize_branch → create_learning_case → report_project_knowledge，`input_schema` 声明 repository_id/source_branch/target_branch/technical_plan_id 等透传键，`timeout=180`。
   - reverse 按名字删除 9 行。
   - migration docstring 写明步骤参数语义：**顶层 arguments 透传合并进每步、步内静态 arguments 优先**（Task 2 实现该合并）。
  </action>
  <verify>
    <automated>cd server && uv run python manage.py migrate tools && uv run python -c "
import django, os; os.environ.setdefault('DJANGO_SETTINGS_MODULE','friday.settings'); django.setup()
from tools.models import RemoteTool
names = set(RemoteTool.objects.filter(is_active=True).values_list('name', flat=True))
assert {'pre_coding_research','post_coding_capture','route_repositories','search_rag_chunks','search_delivery_knowledge','search_learning_cases','summarize_branch','create_learning_case','report_project_knowledge'} <= names, names
import importlib
for t in RemoteTool.objects.filter(source='builtin', name__in=['route_repositories','summarize_branch']):
    mod, fn = t.config['handler'].rsplit('.', 1); assert hasattr(importlib.import_module(mod), fn)
"</automated>
  </verify>
  <done>migrate 后 9 个种子在表且 handler dotted-path 全部可 import（无 stale target）。</done>
</task>

<task type="auto">
  <name>Task 2: Skill 步级 trace 补齐 + 顶层输入透传 + /api/tools/execute/ 端到端测试</name>
  <files>server/tools/sources/skill.py, server/tools/executor.py, server/tools/views.py, server/tests/tools/__init__.py, server/tests/tools/test_platform_skills.py</files>
  <action>
1. `tools/executor.py`：`execute_tool` 与 `_dispatch` 追加 keyword `run=None`（`InteractionRun | None`，仅透传给 skill 分支——builtin/mcp 分支签名不动），保持既有调用方零改动（默认 None）。
2. `tools/sources/skill.py` `execute_skill(tool, arguments, run=None)` 改造：
   - **顶层输入透传**（CONTEXT 最小实现）：`effective_args = {**arguments, **step_args}`（步内静态优先），docstring 写明。
   - **步级结构化事件**：每步 started/completed/failed 三态事件（`skill_step_started` / `skill_step_completed` / `skill_step_failed`），kv 含 `skill=tool.name`、`step=i`、`step_tool`、`ok`、`duration_ms`、`category="caller"`、`component="tools"`（skill 是用户显式调用链）。
   - **步级 ledger**：run 非 None 时每步 `await arecord_tool_call(run, tool_name=f"{tool.name}#{i}:{step_name}", input=effective_args, output=result, status="ok"/"error", duration_ms=..., error=...)`，整段 try/except 吞（观测绝不反噬；`interactions.ledger.arecord_tool_call` 入库前自带 redact_for_ledger 脱敏）。
   - 首败中断语义与返回形状 `list[dict]` 不变（零回归）。
3. `tools/views.py` `RemoteToolExecuteView.post`：`execute_tool(tool_name, arguments, run=run)` 传入 run（其余顶层审计逻辑一字不动）。
4. 新建 `server/tests/tools/`（含 `__init__.py`）+ `test_platform_skills.py`：
   - 步级 trace：构造 skill RemoteTool（临时 acreate 两步 steps，步骤工具 handler patch 为桩函数）+ 真实 run（参照 `tests/` 中 begin_interaction_run 或直接 acreate InteractionRun 的既有工厂），调 `execute_skill(tool, {"query": "q"}, run=run)`，断言 ToolCallRecord 产生 2 行、tool_name 含 `#0:`/`#1:` 前缀、顶层透传生效（桩收到 query）。
   - 首败中断：第 1 步桩返回 ok=False → 只 1 行步级记录 + results 长度 1。
   - 端到端：`/api/tools/execute/` 以 PAT 调 `pre_coding_research`（4 个步骤 handler 全 patch 桩）→ 200 且 result 为 4 步结果列表（参照 `tools/views.py` 既有测试的 PAT 构造方式；若无既有测试参照 `tests/mcp_tools/` 的 AccessToken 工厂）。
   - `run=None` 路径（内部调用）不写步级 ledger 不抛。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/tools/test_platform_skills.py -x -q</automated>
  </verify>
  <done>步级事件 + ToolCallRecord 可查（ROADMAP 标准 4 "步级 trace 完整"）；/api/tools/execute/ 可调两 Skill；既有 execute_tool 调用方零改动。</done>
</task>

<task type="auto">
  <name>Task 3: LOOP-05 pr_review_capture 模块 + 双锚点触发</name>
  <files>server/mcp_tools/pr_review_capture.py, server/workflows/nodes/ai/coding.py, server/orchestration/coding_graph.py, server/tests/mcp_tools/test_pr_review_capture.py</files>
  <action>
1. 新建 `server/mcp_tools/pr_review_capture.py`（中文 docstring：LOOP-05，范围=能跑通+沉淀，不做评审 UI/规则引擎/PR 回评）：

```
async def acapture_pr_review(
    *,
    repository_id: str,
    source_branch: str,
    target_branch: str,
    pr_url: str,
    session_id: str,              # 幂等键源（+":pr_review" 后缀）
    requirement_text: str = "",
    work_item_type: str = "",
    work_item_id: int | None = None,
    initiated_by_user_id: str | None = None,
) -> None
```

管线（全程 fail-soft，外层兜底 try/except 记 `pr_review_capture_failed`）：
   - **开关**：`aget_bool_setting(SettingKeys.PR_REVIEW_CAPTURE, default=False)` 关 → `pr_review_capture_skipped`（reason="disabled"，category="sampling"，component="mcp_tools"）→ return（**默认关，零 LLM 调用**）。
   - **diff 摘要**：`Repository.objects.filter(id=repository_id).afirst()` → `summarize_branch(repository=repo, source_branch=..., target_branch=<空则 repo.default_branch>, max_files=30, trace=None)`；仓库缺失/异常 → skipped return。
   - **LLM review**：镜像 memory_distill `_acall_llm` 范式（`use_call_source(CallSource.PR_REVIEW_CAPTURE.value)` + `build_chat_model(max_output_tokens=1024, streaming=False)` + `arecord_llm_usage(call_source="pr_review_capture", source="mcp_tools")` 成功/异常双路 + fail-soft None）。prompt = `REVIEW_SYSTEM_PROMPT`（从 `workflows.nodes.ai.code_review` import，**不改动该常量**）为 system 基底 + user 内容（requirement_text + summarize_branch 的 files/risks/test_suggestions 摘要，截断 ~6000 字符），并追加一段中文指令：在 JSON 之外用 200 字内总结"本次变更最值得沉淀的 1 条经验/风险"。
   - **沉淀**：把 review 产物组装为 problem（需求/变更上下文）+ solution（review 结论与建议）+ outcome="review"，调 101-02 的 `aextract_learning_case(session_id=session_id, task_status="completed", ..., source="pr_review", idempotency_suffix=":pr_review", pr_url=pr_url, initiated_by_user_id=...)` **或**（若其 LLM 步不可绕过则）在 learning_case_extraction 中提供 `apersist_case(...)` 低层入口供本模块直接入库——二选一以"复用幂等/质量门/脱敏/入库/入图、不重复 LLM 调用"为准绳：推荐做法是在 101-02 模块内把"质量门→脱敏→入库→入图"拆为可复用私有序列并暴露 `apersist_extracted_case(parsed: dict, *, session_key, ...)`，review 模块 LLM 后直接调它（幂等检查在 persist 前做）。在代码注释写明该拆分决策。
   - 事件：`pr_review_capture_completed`（case_id、pr_url、duration_ms、category="caller"、component="mcp_tools"、initiated_by_user_id or "system"）。
2. 锚点接线（各一行调度，位于 101-03 建好的完工闭环块内，同样不 await）：
   - `coding.py` `_finalize_and_notify`：对每个 `successful_mrs` 项，取该仓对应 completed_session_id（succeeded 结果与 session 的对应关系沿用块内已有映射；取不到 session 的仓跳过），`run_in_background(lambda: acapture_pr_review(repository_id=..., source_branch=branch_name, target_branch=base_branch, pr_url=..., session_id=..., requirement_text=plan_title, work_item_type/work_item_id=<triple 可用时>, initiated_by_user_id=<triggered_by>), ...)`。
   - `coding_graph.py` PR 成功分支：`acapture_pr_review(repository_id=str(repo.id), source_branch=coding_session.branch_name, target_branch=<state target_branch 或 _resolve_target_branch>, pr_url=result.mr_url, session_id=<subagent session_id>, ...)` 同款调度。skip-PR 分支**不**触发 review（无 PR 无 diff 目标）。
3. 新建 `server/tests/mcp_tools/test_pr_review_capture.py`（patch `_acall_llm`/`summarize_branch`/persist 入口）：
   - 开关关（默认）→ summarize_branch 与 LLM 均未调用（零成本断言）。
   - 开关开 + 全链 mock → persist 被调且 idempotency key 带 `:pr_review` 后缀、outcome="review"、source_links 含 pr_url。
   - summarize_branch 抛异常 → 不上抛、无 persist。
   - 同 session 重入 → 只一条（复用 101-02 幂等，构造已存在 `{sid}:pr_review` 行断言 skip）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/mcp_tools/test_pr_review_capture.py tests/workflows/test_coding_writeback.py tests/test_coding_graph_completion.py -x -q</automated>
  </verify>
  <done>默认关零调用；开启后 review 沉淀为一条 learning case（幂等 + 入图复用 LOOP-03 路径）；双锚点接线且 101-03 测试零回归。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /api/tools/execute/ → skill steps | PAT 鉴权入口扇出到多个内部 service |
| diff 内容→LLM→知识库 | PR diff 可能含敏感串，review 结论入全员可检索知识库 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-101-04-01 | Elevation of Privilege | skill steps 扇出内部 service | mitigate | 入口维持 PAT-only fail-closed（views.py 不动权限逻辑）；步骤工具均为只读检索/既有 MCP 同能力，无新权限面 |
| T-101-04-02 | Information Disclosure | 步级 ledger / review 沉淀 | mitigate | arecord_tool_call 走 redact_for_ledger；review 产物经 101-02 persist 序列的 redact_secrets_in_text |
| T-101-04-03 | Denial of Service | review LLM 成本失控 | mitigate | 开关默认关 + 幂等后缀键 + call_source=pr_review_capture 可聚合告警（101-02 已登记） |
| T-101-04-04 | Tampering | handler dotted-path stale target | mitigate | migration 验证脚本断言全部 handler 可 import（Task 1 verify） |
| T-101-04-SC | Tampering | 依赖安装 | accept | 零新依赖 |
</threat_model>

<verification>
- `cd server && uv run pytest tests/tools/test_platform_skills.py tests/mcp_tools/test_pr_review_capture.py -q` 全绿。
- `cd server && uv run python manage.py migrate tools && uv run python manage.py makemigrations --check --dry-run tools` 无未生成变更。
- `rg -n "REVIEW_SYSTEM_PROMPT" server/workflows/nodes/ai/code_review.py | head -1` 常量原文件未被修改（git diff 该文件为空）。
- `cd server && uv run pytest tests/workflows/test_coding_writeback.py tests/test_coding_graph_completion.py tests/mcp_tools/test_work_item_execution.py -q` 全绿（锚点宿主零回归）。
</verification>

<success_criteria>
- ROADMAP 成功标准 4：两个 Skill 经 /api/tools/execute/ 可调、步级 trace（事件 + ToolCallRecord）完整；PR 后可选 review 沉淀（默认关）可跑通。
- ROADMAP 成功标准 5 后半：pr_review_capture 调用点带 call_source 且 ModelUsageRecord 可聚合；事件带 initiated_by_user_id。
</success_criteria>

<output>
完成后创建 `.planning/phases/101-completion-loop/101-04-SUMMARY.md`。
</output>
