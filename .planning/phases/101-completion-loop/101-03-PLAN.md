---
phase: 101-completion-loop
plan: 03
type: execute
wave: 2
depends_on: [101-01, 101-02]
files_modified:
  - server/delivery/services/coding_completion.py
  - server/delivery/services/__init__.py
  - server/mcp_tools/learning_case_extraction.py
  - server/workflows/nodes/ai/coding.py
  - web/src/types/workflow/schemas.ts
  - server/orchestration/coding_graph.py
  - server/mcp_tools/work_item_execution_service.py
  - docs/guide/workflows.md
  - server/tests/workflows/test_coding_writeback.py
  - server/tests/test_coding_graph_completion.py
  - server/tests/mcp_tools/test_work_item_execution.py
autonomous: true
requirements: [LOOP-02, LOOP-03]
must_haves:
  truths:
    - "工作流跑完 ai_coding（MR 已知锚点）后，能反查到 work_item 三元组时飞书工作项出现结果评论（与 MCP write_back 同格式）"
    - "存量工作流（config 无 write_back 键且未绑定 work_item）升级后行为零变化——不回写、不产 writeback_skipped 噪音（fallback 守门用例为证）"
    - "Chat 编码建 PR 成功后能反查到三元组即回写，反查不到自然跳过；skip-PR 分支不回写"
    - "三链路（workflow / chat / MCP）在 MR 结果已知锚点均 best-effort 触发 learning case 自动提炼，不阻塞主流程"
  artifacts:
    - path: "server/delivery/services/coding_completion.py"
      provides: "WorkItemTriple + aresolve_triple_from_plan_version + aresolve_triple_for_coding_session"
      contains: "aresolve_triple_from_plan_version"
    - path: "server/tests/workflows/test_coding_writeback.py"
      provides: "workflow 锚点三态（显式开/显式关/legacy fallback）+ 提炼触发测试"
    - path: "docs/guide/workflows.md"
      provides: "write_back 配置项与存量 fallback 升级说明"
  key_links:
    - from: "server/workflows/nodes/ai/coding.py"
      to: "delivery.services.coding_completion"
      via: "_finalize_and_notify MR 后调用公共回写"
      pattern: "CompletionWritebackService|awrite_back"
    - from: "server/orchestration/coding_graph.py"
      to: "mcp_tools.learning_case_extraction"
      via: "create_pr_or_skip_node 成功分支 best-effort 提炼"
      pattern: "aextract_for_session|learning_case_extraction"
    - from: "web/src/types/workflow/schemas.ts"
      to: "server/workflows/nodes/ai/coding.py config_schema"
      via: "aiCodingConfigSchema write_back 键同步"
      pattern: "write_back"
---

<objective>
LOOP-02 + LOOP-03 锚点接线：三元组反查器（workflow 链沿 `plan_version_id → ArtifactVersion → artifact.work_item → WorkItem`；chat 链沿 `coding_plan → delivery Artifact → WorkItem`）→ workflow `AICodingNode._finalize_and_notify` 接公共回写 + 提炼（含 `write_back` 配置键、模板默认开、**存量 fallback 守门**）→ 前端 schema 同步 → chat `create_pr_or_skip_node` 接回写 + 提炼（无会话级开关）→ MCP `execute_work_item_repo_tasks` 接提炼锚点。全部锚点挂"MR 结果已知"之后、best-effort fail-soft（STATE 关键约束：不挂容器回调 `_handle_completed`）。

Purpose: ROADMAP 成功标准 1（三链路回写一致可见）、标准 2（存量零变化 + 事件）、标准 3 的锚点半（任一链路编码成功完成自动提炼）。PITFALLS P3（回写默认值）的"模板默认"与"存量 fallback"区分在此落地。
Output: 反查器 + 三链路接线 + 前端同步 + 升级说明 + 三组测试。
</objective>

<execution_context>
GSD Phase 101 wave 2，依赖 101-01（公共回写 service）与 101-02（提炼管线）。完成后写 `.planning/phases/101-completion-loop/101-03-SUMMARY.md`。
</execution_context>

<context>
@.planning/phases/101-completion-loop/101-CONTEXT.md
@server/workflows/services/pr_cross_reference.py
@server/workflows/nodes/ai/coding.py
@server/orchestration/coding_graph.py
@server/mcp_tools/work_item_execution_service.py
@web/src/types/workflow/schemas.ts
@server/services/background_runner.py

关键既有坐标（已读码核实）：
- workflow 锚点：`coding.py` L1180–1322 `_finalize_and_notify`——MR 创建 L1195–1211、cross-ref L1216–1226、task_result ingestion L1251–1279（INGEST-02 锚点先例，回写/提炼块插在 ingestion 投递之后、`emit_sub_step("create_mr", COMPLETED)` 之前）。config_schema L131–162（现有 timeout_seconds/chat_id/polling_interval 三键）。节点读配置：`context.get_config(key, default)`（base.py L135–137，底层 `node_config.get`）；判定"键是否存在"用 `"write_back" in (context.node_config or {})`。
- 反查链范本：`pr_cross_reference.py` L62–111 `render_traceability_section`——`ArtifactVersion.afirst()` + `.values("artifact_id", "artifact__work_item_id")` 标量链路（async 安全），WorkItem 取 `feishu_project_key/work_item_type/work_item_id/title`；`WorkItem.space` FK 存在（`delivery/models/work_item.py` L38–40 三元组 + space index）。
- trigger 三元组 fallback 先例：`workflows/services/mr_service.py` L74–77 metadata 键 `feishu_work_item_id`/`feishu_project_key`/`feishu_work_item_type`；节点侧读 trigger 用 `context.get_trigger_data(key)`（base.py L174+）。
- chat 锚点：`coding_graph.py` L561–656 `create_pr_or_skip_node`——skip 分支 L575–602（不回写）、PR 成功分支 L625–648（INGEST-02 投递 L630–639 之后插回写+提炼）、失败分支 L650–656（不动）。`coding_session.subagent_session.session_id` 为幂等键源；发起用户 = conversation.created_by（标量 `created_by_id`，async 安全经 values 查询）。
- **chat 反查链现状（executor 必读）**：`chat.CodingPlan` 与 delivery `Artifact` 之间**无既有 FK**（`canonical_plan_id` 已在 Chassis v2 删除，见 `agents/tools/coding_tools.py` L264–266 注释）。CONTEXT 锁定链形 `coding_plan → delivery Artifact → WorkItem` + "反查链任一环缺失 no-op fail-soft"。实现（planner 决策，属 Claude's Discretion 范围）：hop 经 `ArtifactVersion.objects.filter(content__chat_coding_plan_id=str(coding_plan.id))` JSON 键反查（Django JSONField key transform，SQLite/Postgres 均支持）——该键当前无写入方，故存量 chat 会话全部走"反查不到自然跳过"（符合锁定决策语义，零行为变化）；未来编排/桥接写入该键后链路自动点亮。**禁止**为此重新引入 chat→delivery eager 投影（与 Chassis v2 决策冲突）。正向路径用测试构造 ArtifactVersion(content={"chat_coding_plan_id": ...}) 驱动。
- MCP 锚点：`work_item_execution_service.py` `execute_work_item_repo_tasks` L586–599——`_write_results_back` 之后、`overall` 计算之前插提炼；每仓 `task.execution_trace.subagent_session`（`McpCodingExecutionTrace.subagent_session` FK，models.py L202+）提供 session_id；`run.user_id` 为发起用户。
- 后台调度：`services/background_runner.py` `run_in_background(coro_factory, name=..., initiated_by_user_id=...)`（worker 线程常驻 loop，CTX-02 自动 bind 归因）——提炼调度统一走它，不阻塞主流程（CONTEXT LOOP-03 锁定）。
- 前端：`web/src/types/workflow/schemas.ts` L290–295 `aiCodingConfigSchema`；node-definitions.json 无 ai_coding 条目（无需改）。
</context>

<tasks>

<task type="auto">
  <name>Task 1: 三元组反查器 + aextract_for_session 便捷入口</name>
  <files>server/delivery/services/coding_completion.py, server/delivery/services/__init__.py, server/mcp_tools/learning_case_extraction.py, server/tests/delivery/test_coding_completion.py</files>
  <action>
1. `coding_completion.py` 追加：
   - `@dataclass(frozen=True) WorkItemTriple`：`feishu_project_key: str`、`work_item_type: str`、`work_item_id: int`、`title: str = ""`、`space_id: str | None = None`（space_id 供 awrite_back 免二次反查——awrite_back 增加可选 `space_id` 入参或由调用方先取 Space，取实现简单者）。
   - `async def aresolve_triple_from_plan_version(plan_version_id: str | None) -> WorkItemTriple | None`：镜像 `render_traceability_section` 的标量链路——`ArtifactVersion.filter(id=...).values("artifact__work_item_id").afirst()` → `WorkItem.filter(id=...).values("feishu_project_key", "work_item_type", "work_item_id", "title", "space_id").afirst()`；任一跳空/异常 → None（fail-soft，warning 事件 `triple_resolve_failed` 仅异常时记）。
   - `async def aresolve_triple_for_coding_session(coding_session) -> WorkItemTriple | None`：`coding_session.coding_plan_id` 为空 → None；hop = `ArtifactVersion.objects.filter(content__chat_coding_plan_id=str(coding_plan_id)).values("artifact__work_item_id").afirst()`（见 context 中 chat 反查链现状说明，docstring 里写明该 seam 的现状与点亮条件）→ 后半与上函数共用私有 helper `_atriple_from_work_item_id`。
2. `learning_case_extraction.py` 追加便捷入口（三链路共用，减少锚点侧样板）：

```
async def aextract_for_session(
    session_id: str,
    *,
    requirement_text: str = "",
    work_item_type: str = "",
    work_item_id: int | None = None,
    pr_url: str = "",
    initiated_by_user_id: str | None = None,
) -> None
```

   内部：`SubAgentSession.objects.filter(session_id=session_id).afirst()`（含 status）+ 关联 `TaskResult`（`values("text_output", "branch_name", "pr_url", "modified_files")` 标量，async 安全；无 TaskResult 时各字段空）→ 组装后调 `aextract_learning_case(session_id=..., task_status=session.status, text_output=..., branch_name=..., pr_url=pr_url or task_result.pr_url, modified_files=..., work_item_type=..., work_item_id=..., requirement_text=..., initiated_by_user_id=...)`。全程兜底 try/except（后台任务里跑，异常只记日志）。
3. `__init__.py` re-export `WorkItemTriple` 与两个 resolver。
4. `test_coding_completion.py` 追加：workflow 链正向（构造 WorkItem + Artifact + ArtifactVersion → 反查命中三元组）、断链（ArtifactVersion 缺 work_item → None）、chat 链正向（content 埋 chat_coding_plan_id → 命中）与现状路径（普通 coding_plan → None）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/delivery/test_coding_completion.py -x -q</automated>
  </verify>
  <done>两条反查链四路测试全绿；chat seam 的现状与点亮条件写进 docstring。</done>
</task>

<task type="auto">
  <name>Task 2: workflow 锚点——write_back 配置键 + 存量 fallback 守门 + 回写/提炼接线 + 前端同步 + 升级说明</name>
  <files>server/workflows/nodes/ai/coding.py, web/src/types/workflow/schemas.ts, docs/guide/workflows.md, server/tests/workflows/test_coding_writeback.py</files>
  <action>
1. `coding.py` `config_schema` 追加键（放 chat_id 之后）：`"write_back": {"type": "boolean", "title": "回写飞书工作项", "description": "编码完成（MR 结果已知）后将执行结果回写到绑定的飞书工作项（评论）。需要工作流上游绑定了工作项；未绑定时自动跳过。", "default": True}`（**模板默认开**——只影响新建节点；`required` 不加）。

2. `_finalize_and_notify` 在 INGEST-02 ingestion 投递块（L1272–1279）之后、`emit_sub_step("create_mr", COMPLETED)` 之前插入完工闭环块，整块外层 try/except 吞为 `log.warning("coding_completion_loop_failed", error=...)`（镜像 cross-ref fail-soft 范式，绝不影响 NodeResult）：
   - **三元组反查**：`triple = await aresolve_triple_from_plan_version((plan_data or {}).get("plan_version_id"))`；为 None 时 fallback trigger：`context.get_trigger_data("feishu_project_key")` + `get_trigger_data("feishu_work_item_id")` + `get_trigger_data("feishu_work_item_type")`（三者齐备才构造 WorkItemTriple，work_item_id 转 int 失败视为无）。
   - **回写守门（P3 锁定，三态）**：`raw_config = context.node_config or {}`：
     a. `"write_back" in raw_config` 且值为 False → 完全跳过回写（用户显式关）。
     b. `"write_back" in raw_config` 且值为 True → triple 存在则回写；triple 为 None 记 `writeback_skipped`（reason="no_work_item"）。
     c. **键不存在（存量工作流）→ triple 存在才回写；triple 为 None 静默跳过（debug 级日志即可，不产 warning/caller 事件——零行为变化）**。
   - **回写执行**：把 `mr_results` + `failed_repos` 映射为 `RepoResult` 列表（repo_name 取 `repository_name` 或经 repo 对象、status 用 "completed"/"failed"、mr_url/error 对应取值），`await CompletionWritebackService().awrite_back(..., title=plan_title, results=..., initiated_by_user_id=<triggered_by>)`——`triggered_by` 经 `context.workflow_execution.triggered_by_id` 标量（None 则传 None→service 记 system）。不传 doc 参数（workflow 链无 feishu_document_id）。
   - **提炼调度（LOOP-03 锚点）**：对每个 `completed_session_id`（该列表即成功 session）经 `run_in_background(lambda sid=sid: aextract_for_session(sid, requirement_text=plan_title, work_item_type=triple.work_item_type if triple else "", work_item_id=triple.work_item_id if triple else None, pr_url=<该 session 对应仓的 mr_url，取不到给"">, initiated_by_user_id=<triggered_by>), name=f"learning-case-{sid}", initiated_by_user_id=<triggered_by>)` 调度——**不 await Future**（不阻塞节点收尾）。lazy import 防循环。
   - 提炼调度与回写互不依赖：回写跳过不影响提炼（提炼有自己的开关与门）。

3. `web/src/types/workflow/schemas.ts` `aiCodingConfigSchema`（L290–295）追加 `write_back: z.boolean().default(true),`（与后端模板默认一致）。

4. `docs/guide/workflows.md` `ai_coding` 小节（L153 附近）追加升级说明段（中文）：新配置 `write_back`（新建节点默认开）；**存量工作流升级语义**——config 无该键时仅当工作流上下文能反查到绑定工作项才回写、反查不到行为与升级前完全一致；回写失败只记事件不重试；与下游 `notify_feishu_im` 的语义分工（评论=业务留痕，IM 卡片=即时通知）。

5. 新建 `server/tests/workflows/test_coding_writeback.py`（参照 `server/tests/workflows/` 既有节点测试构造 ExecutionContext/mock 的方式；`CompletionWritebackService.awrite_back` 与 `run_in_background` 一律 patch）覆盖：
   - legacy fallback：config 无 write_back 键 + 无三元组 → awrite_back 未调用、无 writeback_skipped（**零行为变化用例**）。
   - legacy fallback：config 无键 + 三元组可反查（patch resolver 返回 triple）→ awrite_back 被调且入参三元组正确。
   - 显式 False → 即使有 triple 也不调用。
   - 显式 True + 无 triple → 不调用 awrite_back 但记 writeback_skipped（capture 日志断言）。
   - 提炼调度：completed_session_ids 非空 → run_in_background 被逐 session 调用；整块抛异常不影响 NodeResult status。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/workflows/test_coding_writeback.py -x -q && cd ../web && pnpm run type-check</automated>
  </verify>
  <done>五路守门测试全绿；前端 `pnpm run type-check`（vue-tsc --noEmit）通过；升级说明落 docs。</done>
</task>

<task type="auto">
  <name>Task 3: chat 锚点——create_pr_or_skip_node 回写 + 提炼</name>
  <files>server/orchestration/coding_graph.py, server/tests/test_coding_graph_completion.py</files>
  <action>
1. `coding_graph.py` `create_pr_or_skip_node`：
   - **PR 成功分支**（L625 `result.success` 内、INGEST-02 投递之后、`store_coding_complete_to_message` 之前或之后均可但须在 return 前）插入完工闭环块（整块 try/except 吞 warning `coding_graph_completion_loop_failed`）：
     - 发起用户：`initiated_by = str(created_by_id)`——经 `Conversation.objects.filter(id=coding_session.conversation_id).values_list("created_by_id", flat=True).afirst()` 标量取（async 安全），None → None。
     - **回写**：`triple = await aresolve_triple_for_coding_session(coding_session)`；triple 为 None → 静默跳过（debug 日志，CONTEXT："反查不到自然跳过——避免新配置面"，不加会话级开关）；有 triple → `RepoResult(repo_name=repo.name, status="completed", branch_name=coding_session.branch_name, mr_url=result.mr_url)` 单元素列表 → `awrite_back(..., title=<coding_session.coding_plan.title 或 conversation 标题，标量取，空则 "编码任务">, initiated_by_user_id=initiated_by)`。
     - **提炼**：`coding_session.subagent_session_id` 非空时 `run_in_background(lambda: aextract_for_session(session_id, pr_url=result.mr_url, requirement_text=<plan title/tech_plan 摘要，截断>, work_item_type=triple.work_item_type if triple else "", work_item_id=triple.work_item_id if triple else None, initiated_by_user_id=initiated_by), name=..., initiated_by_user_id=initiated_by)`，其中 `session_id = str(coding_session.subagent_session.session_id)`（该分支 L630 已有同款访问先例）。
   - **skip-PR 分支**：**不回写**（CONTEXT 锁定）；提炼照常触发（编码成功完成，MR 已知=无；与 INGEST-02 skip 分支投递同位置，pr_url 传 ""）——在代码注释注明这是 LOOP-03 "任一链路编码成功完成" 语义（planner 决策，属 Claude's Discretion）。
   - 失败分支不动。
2. 新建 `server/tests/test_coding_graph_completion.py`（参照 `server/tests/test_coding_session_graph.py` 的节点级测试构造方式；patch `aresolve_triple_for_coding_session`、`CompletionWritebackService.awrite_back`、`run_in_background`、git client）覆盖：
   - PR 成功 + triple 可反查 → awrite_back 一次、run_in_background 一次、节点返回 `{"phase": "completed", "pr_url": ...}` 不变。
   - PR 成功 + triple None → awrite_back 未调用、提炼仍调度、返回值不变（零回归）。
   - skip-PR → awrite_back 未调用、提炼调度、返回 branch_url 不变。
   - 闭环块内抛异常（patch resolver raise）→ 节点返回值不变（fail-soft）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/test_coding_graph_completion.py tests/test_coding_session_graph.py -x -q</automated>
  </verify>
  <done>四路测试全绿且既有 coding_graph 测试零回归；skip-PR 不回写、无会话级开关。</done>
</task>

<task type="auto">
  <name>Task 4: MCP 锚点——execute_work_item_repo_tasks 接提炼</name>
  <files>server/mcp_tools/work_item_execution_service.py, server/tests/mcp_tools/test_work_item_execution.py</files>
  <action>
1. `execute_work_item_repo_tasks` 在 write_back 块之后、`status_values` 计算之前插提炼调度块（整块 try/except 吞 warning）：对每个 `executed` 中 `status == COMPLETED` 且 `execution_trace_id` 非空的 task——经标量查询取 `McpCodingExecutionTrace.objects.filter(id=task.execution_trace_id).values_list("subagent_session__session_id", flat=True).afirst()`（async 安全），session_id 非空时 `run_in_background(lambda sid=...: aextract_for_session(sid, requirement_text=technical_plan.title, work_item_type=technical_plan.work_item_type, work_item_id=technical_plan.work_item_id, pr_url=task.mr_url or "", initiated_by_user_id=<str(run.user_id) 或 None>), name=f"learning-case-{sid}", initiated_by_user_id=...)`。lazy import。注意：本锚点在 MR 已知之后（`_execute_one_task` 内已完成 MR 创建）——符合 STATE 约束"不挂容器回调"。
2. `test_work_item_execution.py` 追加 2 用例（patch `run_in_background` + `aextract_for_session`）：
   - 全部 COMPLETED（含 mr）→ 每仓一次调度、入参 session_id/work_item 三元组正确。
   - task FAILED / 无 execution_trace → 不调度；调度块抛异常不影响 `execute_work_item_repo_tasks` 返回值。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/mcp_tools/test_work_item_execution.py -x -q</automated>
  </verify>
  <done>MCP 链提炼锚点接通且既有执行/回写测试零回归。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| trigger payload→三元组 fallback | 工作流触发数据（飞书事件）作为回写目标兜底来源 |
| 存量工作流定义→新代码路径 | config 缺键时的行为兜底决定升级安全性 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-101-03-01 | Tampering | 存量部署静默行为变更（P3） | mitigate | 三态守门：缺键+无绑定=零变化（专项用例）；升级说明落 docs/guide/workflows.md |
| T-101-03-02 | Spoofing | trigger fallback 写错工作项 | mitigate | 三键齐备才构造三元组；主链（plan_version 反查）优先于 trigger fallback |
| T-101-03-03 | Denial of Service | 提炼阻塞节点收尾/回调链 | mitigate | run_in_background 后台线程 loop 调度，不 await；整块 fail-soft |
| T-101-03-04 | Repudiation | 回写/提炼不可归因 | mitigate | workflow 链 triggered_by_id、chat 链 conversation.created_by_id、MCP 链 run.user_id 逐链透传 |
| T-101-03-SC | Tampering | 依赖安装 | accept | 零新依赖 |
</threat_model>

<verification>
- `cd server && uv run pytest tests/workflows/test_coding_writeback.py tests/test_coding_graph_completion.py tests/mcp_tools/test_work_item_execution.py tests/delivery/test_coding_completion.py -q` 全绿。
- `cd server && uv run pytest tests/test_coding_wave.py tests/test_coding_session_graph.py -q` 全绿（两大锚点宿主零回归）。
- `rg -n "write_back" web/src/types/workflow/schemas.ts` 命中 aiCodingConfigSchema。
- `rg -n "_handle_completed" server/delivery/services/coding_completion.py server/mcp_tools/learning_case_extraction.py` 零命中（锚点不挂容器回调）。
</verification>

<success_criteria>
- ROADMAP 成功标准 1：workflow MR 后 + chat PR 后（可反查时）回写与 MCP 同格式评论；MCP 行为零回归。
- ROADMAP 成功标准 2：存量 fallback 零变化用例通过 + 升级说明存在；writeback_skipped/failed 事件语义就位（101-01 交付、本 plan 接线验证）。
- ROADMAP 成功标准 3 锚点半：三链路 MR 已知处均触发提炼调度（幂等/质量门由 101-02 保证）。
</success_criteria>

<output>
完成后创建 `.planning/phases/101-completion-loop/101-03-SUMMARY.md`。
</output>
