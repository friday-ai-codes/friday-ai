---
phase: 60-durable
plan: 02
type: execute
wave: 2
depends_on: [60-01]
files_modified:
  - server/repositories/apps.py
  - server/codegraph/apps.py
  - server/resumable/apps.py
  - server/tests/durable/test_process_role.py
autonomous: true
requirements: [DURABLE-02]
must_haves:
  truths:
    - "FRIDAY_PROCESS_ROLE=worker|migrate 进程不执行 repositories 的 _reset_stuck_indexing、codegraph 的 galaxy warm + orphan graph reconcile、resumable 的 _schedule_recovery"
    - "role 短路时记一条 info 日志（含 role + 跳过的 job 名），不静默"
    - "role=web（默认）仍执行三处既有 reconcile/sweep/startup jobs（零回归）"
    - "现有管理命令 argv 嗅探短路保留，与 role 门禁叠加（role 是显式入口，argv 是兜底）"
  artifacts:
    - path: "server/repositories/apps.py"
      provides: "RepositoriesConfig.ready() 经 should_run_startup_side_effects 门禁 _reset_stuck_indexing"
      contains: "should_run_startup_side_effects"
    - path: "server/codegraph/apps.py"
      provides: "CodegraphConfig galaxy warm + orphan graph reconcile 经 role 门禁（volar/gopls backend 注册保留）"
      contains: "should_run_startup_side_effects"
    - path: "server/resumable/apps.py"
      provides: "ResumableConfig._schedule_recovery 经 role 门禁（handler 注册保留）"
      contains: "should_run_startup_side_effects"
  key_links:
    - from: "server/repositories/apps.py"
      to: "durable.roles.should_run_startup_side_effects"
      via: "ready() 调度前判 role"
      pattern: "should_run_startup_side_effects"
    - from: "server/codegraph/apps.py"
      to: "durable.roles.should_run_startup_side_effects"
      via: "galaxy warm / orphan reconcile 调度前判 role"
      pattern: "should_run_startup_side_effects"
    - from: "server/resumable/apps.py"
      to: "durable.roles.should_run_startup_side_effects"
      via: "_schedule_recovery 调度前判 role"
      pattern: "should_run_startup_side_effects"
---

<objective>
用 Plan 01 的 `durable.roles.should_run_startup_side_effects` 收口三处 `AppConfig.ready()` 的 web-only 启动副作用：`repositories` 的 `_reset_stuck_indexing`、`codegraph` 的 galaxy warm + orphan graph reconcile、`resumable` 的 `_schedule_recovery`。worker/migrate/test 角色短路（记 info 日志），web 角色零回归。

Purpose: PoC 硬前置③——必须先收口启动副作用，否则后续 worker/migrate 进程会跑业务 reconcile 误杀在途任务 / 在迁移期报"业务表不存在"。这是 Phase 61 迁移的强制前置。
Output: 三处 `apps.py` 叠加 role 门禁 + 单元测试覆盖 worker/migrate 短路与 web 零回归。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-durable/60-CONTEXT.md
@.planning/phases/60-durable/60-RESEARCH.md
@server/durable/roles.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: repositories + resumable 启动副作用 role 门禁</name>
  <read_first>
    - server/durable/roles.py（Plan 01 产出：`current_role()`/`should_run_startup_side_effects(*, job, allowed)`）
    - server/repositories/apps.py:18-83（`ready()` 起 daemon thread 调 `_reset_stuck_indexing`；该方法标 INDEXING→FAILED + 孤儿 IndexHistory RUNNING→FAILED）
    - server/resumable/apps.py:18-89（`ready()`：`register_default_handlers()` 保留 + `_schedule_recovery` 含 argv 嗅探 + 补扫线程）
    - .planning/phases/60-durable/60-RESEARCH.md「Pattern 3: 进程角色门禁」「Component Responsibilities」「进程角色门禁 代码示例」
  </read_first>
  <action>
    `server/repositories/apps.py`：在 `RepositoriesConfig.ready()` 起线程调 `_reset_stuck_indexing` 之前插入 `from durable.roles import should_run_startup_side_effects`；仅当 `should_run_startup_side_effects(job="reset_stuck_indexing")` 为 True 才 `threading.Thread(...).start()`，否则直接 return（不起线程）。保持既有 join(timeout=5) 行为不变（仅在执行分支内）。
    `server/resumable/apps.py`：保留 `register_default_handlers()` 无条件执行（纯注册，无 DB 副作用，所有角色都需要 handler 可用）；把 `_schedule_recovery` 的调度用 `should_run_startup_side_effects(job="resumable_recovery")` 门禁——为 False 时不调度补扫线程。现有 `_schedule_recovery` 内部的 argv 嗅探（pytest / runserver RUN_MAIN / 管理命令）逐字保留并叠加在 role 判定之后（role 是显式入口、argv 是兜底；两者任一拦截即短路）。`RESUMABLE_RECOVERY_ON_STARTUP` 既有开关保留。
  </action>
  <acceptance_criteria>
    - `server/repositories/apps.py` 与 `server/resumable/apps.py` 均 import 并调用 `should_run_startup_side_effects`。
    - role=worker 时 `RepositoriesConfig.ready()` 不创建 `_reset_stuck_indexing` 线程（可经 monkeypatch 线程构造断言未被调用）。
    - role=web 时既有行为逐字保留：`_reset_stuck_indexing` 线程仍创建、`register_default_handlers()` 仍调用、`_schedule_recovery` 仍按既有 argv 嗅探调度。
    - `register_default_handlers()` 在所有角色下都被调用（与 role 无关）。
  </acceptance_criteria>
  <verify>
    <automated>cd server && uv run python manage.py check && uv run ruff check repositories/apps.py resumable/apps.py</automated>
  </verify>
  <done>repositories/resumable 两处启动副作用经 role 门禁，handler 注册保留，web 零回归。</done>
</task>

<task type="auto">
  <name>Task 2: codegraph 启动副作用 role 门禁（backend 注册保留）</name>
  <read_first>
    - server/durable/roles.py（Plan 01 产出）
    - server/codegraph/apps.py:105-220（`ready()`：volar/gopls backend 注册（纯内存）+ `_schedule_galaxy_cache_warm`(:123) + `_schedule_orphan_graph_build_reconcile`(:169)，两者均含 argv 嗅探 + 延迟 5s daemon 线程）
    - server/codegraph/apps.py:10-95（`reconcile_orphaned_graph_builds`：标超时 RUNNING GraphBuildHistory→FAILED + 归位仓库态——这是误杀在途任务的高风险副作用）
  </read_first>
  <action>
    `server/codegraph/apps.py`：`CodegraphConfig.ready()` 中 `_register_volar_backends` / `_register_gopls_backend` 保留无条件执行（纯内存 BACKEND_REGISTRY 注册，无 DB 副作用，所有角色需要）。对 `_schedule_galaxy_cache_warm` 与 `_schedule_orphan_graph_build_reconcile` 两处调度各加 `should_run_startup_side_effects(job="galaxy_cache_warm")` / `should_run_startup_side_effects(job="orphan_graph_reconcile")` 门禁——为 False 时不调度 daemon 线程。各自现有的 argv 嗅探（pytest/runserver/管理命令）逐字保留并叠加在 role 判定之后。既有 `GALAXY_CACHE_*` / `GRAPH_BUILD_ORPHAN_RECONCILE_ON_STARTUP` 等开关保留（与 role 门禁叠加，任一关闭即不跑）。
  </action>
  <acceptance_criteria>
    - `server/codegraph/apps.py` import 并对 galaxy warm + orphan reconcile 两处调用 `should_run_startup_side_effects`。
    - role=worker/migrate 时两处 daemon 线程不被调度；volar/gopls backend 注册仍执行。
    - role=web 时既有 argv 嗅探 + 开关行为逐字保留。
    - `reconcile_orphaned_graph_builds` 函数本体不改（仅其启动调度被门禁）。
  </acceptance_criteria>
  <verify>
    <automated>cd server && uv run python manage.py check && uv run ruff check codegraph/apps.py</automated>
  </verify>
  <done>codegraph 两处 DB 副作用经 role 门禁、backend 注册保留、web 零回归。</done>
</task>

<task type="auto">
  <name>Task 3: 进程角色门禁单元测试</name>
  <read_first>
    - server/durable/roles.py（被测对象）
    - server/repositories/apps.py / server/codegraph/apps.py / server/resumable/apps.py（Task 1/2 改后）
    - server/tests/conftest.py:36-72（`_reset_background_runner` autouse + monkeypatch 范式）
    - server/tests/durable/conftest.py（Plan 01 产出，durable 共享 fixture）
    - .planning/phases/60-durable/60-VALIDATION.md「Per-Task Verification Map」60-02 行（test_process_role.py）
  </read_first>
  <action>
    新建 `server/tests/durable/test_process_role.py`：
    用 `monkeypatch.setenv("FRIDAY_PROCESS_ROLE", ...)` 驱动 `durable.roles.current_role()` / `should_run_startup_side_effects(job=...)`——断言 web→True、worker/migrate/test→False，且 False 分支经 caplog/structlog capture 命中 `startup_side_effect_skipped_by_role`（含 role 与 job 字段）。
    三处 ready() 短路守护（用 monkeypatch patch 各模块的 `threading.Thread`，role=worker 时断言 Thread 未被以对应 target 构造；role=web 时断言被构造）：覆盖 `RepositoriesConfig`、`CodegraphConfig`（galaxy warm + orphan reconcile）、`ResumableConfig`。
    零回归断言：role=web 下 `resumable` 的 `register_default_handlers` 与 codegraph 的 backend 注册在任意 role 下均被调用（与 role 无关）。
    测试不依赖 Postgres（纯单元，SQLite/无 DB 行为），不带 postgres_queue marker。
  </action>
  <acceptance_criteria>
    - `server/tests/durable/test_process_role.py` 覆盖：roles helper 真值表 + info 日志、三处 ready() worker 短路 / web 执行、handler/backend 注册与 role 无关。
    - `cd server && uv run pytest tests/durable/test_process_role.py -q` 全绿。
    - 测试不命中 Postgres（无 SocketBlockedError）、不带 postgres_queue marker。
  </acceptance_criteria>
  <verify>
    <automated>cd server && uv run pytest tests/durable/test_process_role.py -q</automated>
  </verify>
  <done>role 门禁真值表 + 三处短路 + web 零回归 + 日志均有自动化守护且全绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 进程启动（任意 role）→ 业务 DB 副作用 | AppConfig.ready() 在 worker/migrate 进程也会执行，可能误杀在途业务行 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-03 | Tampering | worker/migrate 进程跑 web-only reconcile 误杀在途任务 | mitigate | should_run_startup_side_effects 门禁三处 DB 副作用；test_process_role.py 守护 worker 短路（PoC 硬前置③） |
| T-60-04 | DoS / 正确性 | role 误判导致 web 也短路（reconcile 不再跑→僵尸 RUNNING 累积） | mitigate | 默认 role=web、allowed 默认含 web；测试断言 web 零回归 + handler/backend 注册与 role 无关 |
</threat_model>

<verification>
- `cd server && uv run python manage.py check` 通过。
- `cd server && uv run pytest tests/durable/test_process_role.py -q` 全绿。
- `cd server && uv run pytest -q` 默认 SQLite 全量零回归（确认三处 apps.py 改动不破坏既有启动/测试）。
- `cd server && uv run ruff check repositories/apps.py codegraph/apps.py resumable/apps.py`。
</verification>

<success_criteria>
- `FRIDAY_PROCESS_ROLE=worker|migrate` 进程不跑三处 web-only reconcile/sweep/startup jobs（无误杀、无"业务表不存在"warning）。
- role=web 默认行为零回归，handler/backend 注册始终执行。
- 短路有 info 日志可观测，不静默。
</success_criteria>

<artifacts>
## Artifacts this phase produces (Plan 02)

**Modified files**
- `server/repositories/apps.py` — `RepositoriesConfig.ready()` role 门禁 `_reset_stuck_indexing`
- `server/codegraph/apps.py` — `CodegraphConfig` role 门禁 galaxy warm + orphan graph reconcile（backend 注册保留）
- `server/resumable/apps.py` — `ResumableConfig._schedule_recovery` role 门禁（handler 注册保留）

**New files**
- `server/tests/durable/test_process_role.py`

**Consumed symbols（来自 Plan 01）**
- `durable.roles.current_role`、`durable.roles.should_run_startup_side_effects`

**Env vars（语义消费）**
- `FRIDAY_PROCESS_ROLE`（web|worker|scheduler|migrate|test）
</artifacts>

<output>
Create `.planning/phases/60-durable/60-02-SUMMARY.md` when done.
</output>
