---
phase: 14-triggers
plan: 05
type: execute
wave: 3
depends_on: [14-01, 14-04]
files_modified:
  - server/knowledge/sources/feishu_work_item.py
  - server/feishu/views.py
  - server/tests/knowledge/test_triggers.py
autonomous: true
requirements: [INGEST-04]
must_haves:
  truths:
    - "飞书工作项创建/状态变更/字段更新三事件各投递一次摄取请求，webhook 请求路径零取材（只投 ID）"
    - "feishu_work_item normalizer 产出全量快照：名称、描述、自定义字段、PRD 与技术方案文档正文、关联工作项，content 按 ## 分段"
    - "同 natural key 重摄把 13-03 的轻量锚实体升级为全量快照版本（版本翻转，非新实体）"
    - "event_time 恒为 aware datetime（毫秒时间戳转换 + timezone.now() 兜底），不触发 require_aware 拒绝"
    - "文档拉取失败降级为不含正文的快照 + warning，不 raise；ingestion 异常时三 handler 宿主流程仍成功"
  artifacts:
    - path: "server/knowledge/sources/feishu_work_item.py"
      provides: "飞书工作项 → 全量快照单事件 normalizer（取材全在后台）"
      exports: ["normalize"]
    - path: "server/tests/knowledge/test_triggers.py"
      provides: "TestFeishuWorkItemNormalizer + TestFeishuTriggers 用例组（fake FeishuClient/FeishuDocClient）"
  key_links:
    - from: "server/feishu/views.py"
      to: "server/knowledge/ingestion.py"
      via: "三 handler 尾部 lazy import + aschedule_ingestion（只投三元组 ID）"
      pattern: "aschedule_ingestion"
    - from: "server/knowledge/sources/feishu_work_item.py"
      to: "server/services/feishu.py"
      via: "create_feishu_client_for_project + get_work_item / get_work_item_relations 后台取材"
      pattern: "get_work_item"
---

<objective>
落地 INGEST-04：飞书工作项三类关键事件（创建/状态变更/字段更新）接入统一摄取管线——feishu_work_item normalizer 后台拉取全量快照（fields/relations/PRD 与方案文档正文），webhook 路径只投 ID。

Purpose: 工作项演化以版本链记录（13-03 轻量锚同 key 升级为全量快照，预埋设计闭环），需求侧时间线素材齐备。
Output: sources/feishu_work_item.py + feishu/views.py 三 handler 接线 + test_triggers.py feishu 组。
</objective>

<execution_context>
@~/.claude/skills/gsd-execute-phase/SKILL.md
</execution_context>

<context>
@.planning/phases/14-triggers/14-CONTEXT.md
@.planning/phases/14-triggers/14-RESEARCH.md   （§触发点 3（handler 锚点 + 取材清单 + event_time）、Pitfall 3/6）
@.planning/phases/14-triggers/14-PATTERNS.md   （§1 接线铁律 + feishu 锚点行、§2 源缺失降级模式、§8 测试范式）
@.planning/phases/13-ingest/13-03-SUMMARY.md   （work_item 锚三元组 source_id 契约——同 key 重摄即升级）
@server/knowledge/sources/mcp_plan.py
@server/feishu/views.py
@server/services/feishu.py
@server/services/feishu_doc.py
</context>

## 规划定案（供 verify 对照）

- **取材全在后台（Pitfall 3，locked）**：webhook 3 秒超时即重推——三 handler 只投 `IngestionRequest("feishu_work_item", 三元组, trigger)`；get_work_item/relations/文档正文全部在 normalizer（background runner）内。
- **event_time（Pitfall 6）**：工作项更新时间字段（毫秒时间戳）→ `datetime.fromtimestamp(ms / 1000, tz=datetime.UTC)`；字段缺失/非法 → `timezone.now()` 兜底；恒 aware。
- **文档降级（locked）**：PRD/方案文档拉取失败 → 快照不含该正文段 + warning（13 范式：部分缺料降配不 raise）。
- **幂等既有三层**：ProcessedEvent + TriggerLog event_uuid unique（webhook 层）+ 摄取 hash 短路——接线不加自定义去重。

## Artifacts this phase produces

见 14-01-PLAN.md 同名章节的全阶段产物表；本 plan 交付其中 **feishu_work_item normalizer + 三 handler 接线**（INGEST-04）。

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: sources/feishu_work_item.py 全量快照 normalizer + TestFeishuWorkItemNormalizer（fake 客户端）</name>
  <files>server/knowledge/sources/feishu_work_item.py, server/tests/knowledge/test_triggers.py</files>
  <read_first>
    - .planning/phases/14-triggers/14-RESEARCH.md §触发点 3 normalizer 取材清单（get_work_item L104 返回字段 / get_work_item_relations L188 / KeyFields PRD_URL·TECH_DOC_URL 提取范例 views.py:737-740 / FeishuDocClient.get_document_content L127 / client 构造范例 plan_approval.py:198-202）
    - server/services/feishu.py（get_work_item 返回结构 L104-186：name/description/status/fields dict；get_work_item_relations 返回结构）
    - server/services/feishu_doc.py（get_document_content 签名与失败语义 L127 起）
    - server/feishu/views.py（KeyFields 提取写法 L737-740——doc token 解析照此）
    - server/knowledge/sources/mcp_plan.py（源缺失降级 + 事件字段填法范式）
  </read_first>
  <behavior>
    - Test 1（全量快照取材）：fake FeishuClient 返回 name/description/status/fields（含 PRD_URL/TECH_DOC_URL）/relations，fake FeishuDocClient 返回两文档正文 → 单事件：kind==WORK_ITEM、origin==FEISHU、source_kind=="feishu_work_item"、source_id==请求三元组原样、content 含 name、description、"## 自定义字段"、"## PRD"、"## 技术方案"、"## 关联工作项" 各段、payload 含 status/work_item_type、project_id 非空
    - Test 2（同 key 升级语义）：先以 13-03 轻量锚形态摄入实体，再经本 normalizer 产出事件走 ingest_events → 同实体版本 v2（实体数不变、版本链 supersedes）
    - Test 3（文档降级）：fake doc client 抛异常 → 事件仍产出，content 无 "## PRD" 段、含其余段，warning 记录
    - Test 4（event_time aware，双场景）：fields 含毫秒时间戳 → event_time 为对应 UTC aware；字段缺失 → event_time 接近 timezone.now() 且 aware（两断言均 `event_time.tzinfo is not None`）
    - Test 5（源缺失）：project_key 查无 Project 或 get_work_item 失败 → 空列表 + warning
  </behavior>
  <action>
    创建 `server/knowledge/sources/feishu_work_item.py`（统一签名、`__all__ = ["normalize"]`、中文 docstring 标注 INGEST-04 与降级语义）：
    1. source_id 解析三元组 `{project_key}:{work_item_type}:{work_item_id}`（split(":", 2)）；`Project.objects.filter(feishu_project_key=project_key).afirst()`，缺失 → 空列表 + warning。
    2. `create_feishu_client_for_project(project)` → `get_work_item(project_key, work_item_id, work_item_type)`（失败 → 空列表 + warning）；`get_work_item_relations`（失败 → 空关联列表 + warning，事件照常）。
    3. fields 内经 KeyFields.PRD_URL / TECH_DOC_URL 提取 doc token（views.py:737-740 同款解析），`create_feishu_doc_client_for_project` + `get_document_content` 拉正文；单文档失败独立降级（缺段不缺事件）。
    4. content markdown 拼接（## 分段契合 chunker）：`# {name}\n\n{description}\n\n## 自定义字段\n{key: value 表}\n\n## PRD\n{正文}\n\n## 技术方案\n{正文}\n\n## 关联工作项\n{relation_type/name/status 清单}`——缺料段整段省略。
    5. payload：{name, status, work_item_type, work_item_id, prd_url, tech_doc_url, relation_count}；project_id=str(project.id)；repository_id=None；event_time 按规划定案（毫秒转换 + 兜底）。
    6. test_triggers.py 追加 `TestFeishuWorkItemNormalizer`（behavior 五条）；fake FeishuClient/FeishuDocClient 以 monkeypatch `knowledge.sources.feishu_work_item` 模块属性形态注入（fixture 放本测试文件内，与 conftest 的 fake_git_platform 互不干扰）。**测试方法名一律 `test_feishu_*` 前缀**——pytest `-k feishu` 大小写敏感，选中锚定方法名而非 PascalCase 类名（14-01 规划定案节测试命名约定）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/knowledge/test_triggers.py -k feishu -x</automated>
  </verify>
  <acceptance_criteria>
    - `rg "os.environ|FRIDAY_" server/knowledge/sources/feishu_work_item.py` 零命中（凭证只经 create_feishu_client_for_project 既有 service 层）
    - Test 2 断言实体数不变且版本号递增（13-03 锚升级语义可证）
    - Test 4 两场景断言 tzinfo 非 None（require_aware 防线不触发）
    - 全部外部调用（work_item/relations/两文档）各有独立降级路径（Test 3/5 + 代码复核）
  </acceptance_criteria>
  <done>飞书工作项全量快照取材就绪，缺料降级语义钉死。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: feishu/views.py 三 handler 接线 + TestFeishuTriggers（投递/隔离/宿主回归）</name>
  <files>server/feishu/views.py, server/tests/knowledge/test_triggers.py</files>
  <read_first>
    - .planning/phases/14-triggers/14-PATTERNS.md §1（锚点表 feishu 行 + handler 既有风格摘录 L63-75）
    - server/feishu/views.py（_handle_workitem_create L751 / _handle_workitem_status L763 / _handle_workitem_update L857 三函数全文——确认 work_item_id/work_item_type 局部变量名与 _fetch_and_update_work_item 调用位置；project.feishu_project_key 可达性）
    - server/tests/knowledge/test_triggers.py（captured_requests fixture + TestExceptionIsolation 范式）
    - server/tests/feishu/（既有 webhook handler 测试构造方式——payload/project fixture 复用）
  </read_first>
  <behavior>
    - Test 1（三事件各投递一次）：分别调三 handler → captured_requests 各 1 条 ("feishu_work_item", f"{project.feishu_project_key}:{type}:{id}", "feishu_workitem_create" / "feishu_workitem_status" / "feishu_workitem_update")
    - Test 2（缺 ID 不投递）：payload 无 id → handler 早退（既有 warning 分支），零投递
    - Test 3（异常隔离）：run_in_background 抛 RuntimeError → 三 handler 仍正常完成（既有日志行为不变）
  </behavior>
  <action>
    三 handler 各自尾部（`_fetch_and_update_work_item` 调用之后、收尾 logger.info 之前或之后均可，以函数尾部为准）追加逐字 13-03 形态接线：函数体内 `from knowledge import ingestion` + `await ingestion.aschedule_ingestion(ingestion.IngestionRequest("feishu_work_item", f"{project.feishu_project_key}:{work_item_type}:{work_item_id}", "feishu_workitem_<event>"))`，trigger 后缀分别 create/status/update。零 try/except、零取材（Pitfall 3）。work_item_id 为 int 时 f-string 自然转 str（与 13-03 三元组格式一致性以 natural key 规则表为准）。test_triggers.py 追加 `TestFeishuTriggers`（behavior 三条，payload/project 构造参照 tests/feishu/ 既有 fixture）。**测试方法名一律 `test_feishu_*` 前缀**（-k 选中锚定方法名）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/knowledge/test_triggers.py -k feishu -x && cd server && uv run pytest tests/feishu/ -x</automated>
  </verify>
  <acceptance_criteria>
    - `rg -c "aschedule_ingestion" server/feishu/views.py` == 3（三 handler 各一，lazy import 形态不增计数）
    - `rg "from knowledge.ingestion import" server/feishu/views.py` 零命中
    - 接线处零 try/except、零 get_work_item/文档调用（webhook 路径零取材，人工复核三处插入块）
    - `tests/feishu/` 宿主套件零回归
  </acceptance_criteria>
  <done>INGEST-04 端到端：飞书三事件 → 后台全量快照版本，webhook 路径零加重。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 飞书 webhook → handler | 外部 POST 输入（token 校验既有，views.py:603-620） |
| 飞书 API 返回 → normalizer content | 外部数据进入图谱/向量 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-17 | Spoofing（伪造 webhook 污染图谱） | 三 handler 接线 | mitigate | 接线位于既有 token 校验之后的 handler 内，无旁路；Test 2 缺 ID 早退路径不投递 |
| T-14-18 | Tampering（payload 字段缺失/畸形，V5） | handler + normalizer | mitigate | handler 既有判空早退；normalizer 三元组解析/Project 查询/API 失败全部降级空列表 + warning（Task 1 Test 5） |
| T-14-19 | Denial of Service（webhook 3s 超时被取材拖垮） | 三 handler | mitigate | 只投 ID 零取材（acceptance 人工复核）+ aschedule_ingestion 异常全吞 + 隔离用例（Test 3） |
| T-14-20 | Information Disclosure（payload 权限维度缺失，V4） | feishu_work_item normalizer | mitigate | project_id 恒带（Test 1 断言非空）；凭证只走 service 层（env 零读取 grep） |
| T-14-21 | Denial of Service（naive datetime abort 后台摄取） | event_time | mitigate | 毫秒转换 + timezone.now() 兜底 + 双场景 aware 断言（Task 1 Test 4，Pitfall 6） |
</threat_model>

<verification>
- `cd server && uv run pytest tests/knowledge/ tests/feishu/ -x` 全绿
- `cd server && uv run ruff check knowledge/sources/feishu_work_item.py feishu/ tests/knowledge/` 通过
</verification>

<success_criteria>
- INGEST-04 三事件投递 + 全量快照（fields/relations/文档正文）+ 锚升级 + 降级语义全部测试在案
- webhook 请求路径零取材零加重（Pitfall 3 防线可 grep 验证）
</success_criteria>

<output>
Create `.planning/phases/14-triggers/14-05-SUMMARY.md` when done
</output>
