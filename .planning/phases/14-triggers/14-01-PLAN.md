---
phase: 14-triggers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/pyproject.toml
  - server/uv.lock
  - server/knowledge/models.py
  - server/knowledge/migrations/0003_codechangearchive_kedge_chunk_unique.py
  - server/knowledge/ingestion.py
  - server/knowledge/graph_store.py
  - server/knowledge/chunking.py
  - server/knowledge/sources/__init__.py
  - server/tests/knowledge/test_diff_archive.py
  - server/tests/knowledge/test_modifies_chunk.py
  - server/tests/knowledge/test_chunking.py
autonomous: true
requirements: [KMOD-05, ENH-01]
must_haves:
  truths:
    - "CodeChangeArchive 表可落库：zlib 压缩 diff 原文往返一致，commit_sha/mr_url/branch/仓库 FK/文件级 JSON 全部持久化"
    - "同 (source_kind, source_id, commit_sha) 重复归档被 unique 约束拒绝（重触发幂等锚）"
    - "apply_edge_specs 对 chunk 边（target_chunk_id）幂等：同 spec 3 连发只产生 1 条活跃边，metadata 持久化"
    - "KnowledgeEdge 对 chunk 边有 DB 级 partial unique 防线（Pitfall 4 封堵）"
    - "chunk_knowledge_text 对含 `diff --git` 的 content 按文件→hunk→硬切分层切块，chunk_kind='diff'，确定性不变（同输入同输出）"
  artifacts:
    - path: "server/knowledge/models.py"
      provides: "CodeChangeArchive 模型（KMOD-05 归档表）+ KnowledgeEdge chunk 边 partial unique"
      contains: "class CodeChangeArchive"
    - path: "server/knowledge/migrations/0003_codechangearchive_kedge_chunk_unique.py"
      provides: "归档表建表 + uniq_kedge_chunk_active 约束（makemigrations 自动生成，文件名以实际生成为准）"
    - path: "server/knowledge/ingestion.py"
      provides: "EdgeSpec 扩展（target_chunk_id XOR target_entity_id + metadata）+ apply_edge_specs chunk 边幂等分支"
    - path: "server/knowledge/graph_store.py"
      provides: "chunk_in_edges(chunk_id) 反查方法（图访问收口）"
      exports: ["chunk_in_edges"]
    - path: "server/knowledge/chunking.py"
      provides: "chunk_knowledge_text diff-aware 分支（签名不变、纯函数确定性不变）"
    - path: "server/knowledge/sources/__init__.py"
      provides: "workflow_plan / task_result / feishu_work_item 三行注册（模块由 14-04/05/06 落地，13-02 先注册先例）"
  key_links:
    - from: "server/knowledge/ingestion.py"
      to: "server/knowledge/graph_store.py"
      via: "apply_edge_specs chunk 分支调用 graph_store.add_edge(target_chunk_id=..., metadata=...)"
      pattern: "target_chunk_id"
    - from: "server/knowledge/models.py"
      to: "server/knowledge/migrations/"
      via: "makemigrations 生成 0003（不手写 migration）"
      pattern: "uniq_kedge_chunk_active"
---

<objective>
落地 Phase 14 全部"地基件"：unidiff 依赖、CodeChangeArchive 归档模型（KMOD-05 持久层）、chunk 边幂等收口（EdgeSpec 扩展 + apply_edge_specs + DB partial unique，ENH-01 边写入通路）、chunk_knowledge_text 的 diff-aware 分支、normalizer 注册表三行登记。

Purpose: 后续所有 plan（DiffArchiver / 三个 normalizer / 接线）只消费本 plan 产物，不再触碰 knowledge 核心模块——wave 内零文件交叠的前提。
Output: 模型 + migration + 管线扩展 + chunker 扩展 + 三个测试文件落点。
</objective>

<execution_context>
@~/.claude/skills/gsd-execute-phase/SKILL.md
</execution_context>

<context>
@.planning/phases/14-triggers/14-CONTEXT.md
@.planning/phases/14-triggers/14-RESEARCH.md   （§CodeChangeArchive schema、§MODIFIES_CHUNK 设计选项 A、§diff 文本 chunk 策略、Pitfall 4/8）
@.planning/phases/14-triggers/14-PATTERNS.md   （§4 models 约束命名范式、§5 EdgeSpec/apply_edge_specs 扩展锚点、§6 chunking diff 分支）
@.planning/phases/13-ingest/13-02-SUMMARY.md   （EdgeSpec/apply_edge_specs 契约）
@server/knowledge/models.py
@server/knowledge/ingestion.py
@server/knowledge/chunking.py
</context>

## 规划定案（供 verify 对照）

- **OQ-3（规划定案）**：文件级降级每文件边数封顶常量 `MAX_FILE_LEVEL_EDGES_PER_FILE = 20` 起步，不配置化（常量落在 14-03 的 diff_archive.py；本 plan 只交付边写入幂等通路）。
- **chunk 边幂等收口（locked，RESEARCH 选项 A）**：chunk 边走 EdgeSpec → apply_edge_specs 唯一通路，不在 normalizer 内直接调 graph_store；同时补 DB partial unique 防线。
- **注册表先登记（13-02 先例）**：sources/__init__.py 三行注册指向尚不存在的模块——模块落地前 get_normalizer 触发 ImportError 响亮失败，不静默。
- **diff content 可重派生（locked，Pitfall 8）**：diff chunk 只能从 version.content 派生，diff-aware 逻辑必须进 chunk_knowledge_text 纯函数，禁止 normalizer 旁路造 chunks。

## Artifacts this phase produces

| Artifact | Plan | 说明 |
|----------|------|------|
| `CodeChangeArchive` 模型 + migration 0003 | **14-01** | KMOD-05 归档表（zlib BinaryField + unique 幂等锚）+ KnowledgeEdge chunk partial unique |
| EdgeSpec(target_chunk_id, metadata) + apply_edge_specs chunk 幂等 + graph_store.chunk_in_edges | **14-01** | ENH-01 边写入与反查通路 |
| chunk_knowledge_text diff-aware 分支（chunk_kind="diff"） | **14-01** | 大 diff 分层切块（文件→hunk→硬切） |
| GitPlatformClient.get_branch_diff（GitHub/GitLab） | 14-02 | skip-PR 路径全量 diff 兜底 |
| `knowledge/diff_archive.py`（纯函数 + 对齐阶梯 + DiffArchiver） | 14-03 | unidiff 解析/生成文件判定/压缩/符号对齐 |
| `sources/workflow_plan.py` + plan_generation/approve_node 接线 | 14-04 | INGEST-01 |
| `sources/feishu_work_item.py` + 三 handler 接线 | 14-05 | INGEST-04 全量快照 |
| `sources/task_result.py` + 编码完成三锚点接线 | 14-06 | INGEST-02 + KMOD-05 + ENH-01 端到端闭环 |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: unidiff 依赖 + CodeChangeArchive 模型 + KnowledgeEdge chunk partial unique + migration（test_diff_archive.py 创建）</name>
  <files>server/pyproject.toml, server/uv.lock, server/knowledge/models.py, server/knowledge/migrations/0003_codechangearchive_kedge_chunk_unique.py, server/tests/knowledge/test_diff_archive.py</files>
  <read_first>
    - .planning/phases/14-triggers/14-RESEARCH.md §CodeChangeArchive 表 schema 建议（L208-257 完整字段表 + 设计要点）与 Pitfall 4（partial unique 防线）
    - .planning/phases/14-triggers/14-PATTERNS.md §4（约束/索引命名范式摘录 L182-217：uniq_*/idx_* 显式命名、SET_NULL FK、中文 docstring）
    - server/knowledge/models.py（KnowledgeEntityVersion/KnowledgeEdge Meta 约束写法、uniq_kedge_active 的 condition 形态 L302-307、EdgeRelation.MODIFIES_CHUNK 枚举占位）
    - server/tests/knowledge/test_models.py（约束触发测试的既有风格——IntegrityError 断言写法）
  </read_first>
  <behavior>
    - Test 1（压缩往返）：构造多行 diff 文本 → zlib.compress 落 `diff_compressed` + acreate → 重读 zlib.decompress 解码与原文逐字节一致；diff_size/compressed_size/diff_sha256 与实算一致
    - Test 2（unique 幂等锚）：同 (source_kind="task_result", source_id, commit_sha) 第二次 acreate 抛 IntegrityError（transaction.atomic 包裹断言）
    - Test 3（字段完整性）：repository FK（SET_NULL）、mr_url/mr_id/branch_name/base_branch/truncated/files JSON/file_count/total_additions/total_deletions/event_time 全部落库可读
    - Test 4（chunk partial unique，Pitfall 4 DB 防线）：直接 KnowledgeEdge.objects.acreate 两条同 (source_entity, target_chunk_id, relation="MODIFIES_CHUNK") 且 invalid_at=None 的边 → 第二条 IntegrityError；先 invalidate（invalid_at 置位）后可再建
  </behavior>
  <action>
    1. `cd server && uv add "unidiff>=0.7.5,<0.8"`（RESEARCH Package Legitimacy Audit 已 Approved [VERIFIED: PyPI]，无 checkpoint）。
    2. `server/knowledge/models.py` 追加 `CodeChangeArchive`，字段逐项按 RESEARCH §schema 建议：uuid4 PK、source_kind/source_id 弱引用（不 FK 到 KnowledgeEntity）、repository SET_NULL FK（related_name="code_change_archives"）、commit_sha/branch_name/base_branch/mr_url/mr_id、diff_compressed BinaryField、diff_size/compressed_size/diff_sha256/truncated、files JSONField(default=list)/file_count/total_additions/total_deletions、event_time/created_at。Meta：UniqueConstraint(["source_kind","source_id","commit_sha"], name="uniq_codechange_source_commit")；Index idx_cca_repo_commit / idx_cca_source。中文 docstring 标注 KMOD-05 与"diff 原文绝不进 payload/content 全量"边界。
    3. 同文件 KnowledgeEdge Meta 追加 partial UniqueConstraint：fields=["source_entity","target_chunk_id","relation"]，condition=Q(invalid_at__isnull=True)，name="uniq_kedge_chunk_active"（条件形态照 uniq_kedge_active 同款）。
    4. `uv run python manage.py makemigrations knowledge` 生成 0003（不手写；生成文件名以实际为准，frontmatter 路径为预估）。
    5. 创建 `server/tests/knowledge/test_diff_archive.py`，落 behavior 四用例（类名 `TestCodeChangeArchiveModel`）；14-03 在同文件扩展纯函数/大 diff 用例组。
  </action>
  <verify>
    <automated>cd server && uv run python manage.py makemigrations --check --dry-run && uv run pytest tests/knowledge/test_diff_archive.py tests/knowledge/test_models.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `rg '"unidiff' server/pyproject.toml` 命中且版本约束为 `>=0.7.5,<0.8`；uv.lock 同步更新
    - `rg -c "uniq_codechange_source_commit|uniq_kedge_chunk_active" server/knowledge/models.py` == 2（两约束均显式命名）
    - makemigrations --check --dry-run 退出码 0（模型与 migration 一致）
    - test_diff_archive.py 由本任务创建，behavior 四用例全绿（含 chunk partial unique 双向断言）
    - 既有 tests/knowledge/test_models.py 零回归
  </acceptance_criteria>
  <done>KMOD-05 持久层 + Pitfall 4 DB 防线就绪；unidiff 可 import。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: EdgeSpec 扩展 + apply_edge_specs chunk 边幂等 + graph_store.chunk_in_edges + 注册表三行（test_modifies_chunk.py 创建）</name>
  <files>server/knowledge/ingestion.py, server/knowledge/graph_store.py, server/knowledge/sources/__init__.py, server/tests/knowledge/test_modifies_chunk.py</files>
  <read_first>
    - .planning/phases/14-triggers/14-RESEARCH.md §MODIFIES_CHUNK 设计（载体与写入：选项 A 定案、unresolved 不建边、反查封装进 GraphStore）
    - .planning/phases/14-triggers/14-PATTERNS.md §5（EdgeSpec 现状 L228-239、apply_edge_specs 幂等核心 L244-270、add_edge XOR 签名 L274-295）
    - server/knowledge/ingestion.py（EdgeSpec dataclass、apply_edge_specs 全函数、ingest_events 阶段 B 调用点）
    - server/knowledge/graph_store.py（add_edge / invalidate_edge / neighbors 返回的 EdgeRecord 字段集，确认含 target_chunk_id）
    - server/tests/knowledge/test_ingestion.py（幂等三连发既有模式——chunk 边用例仿写）
  </read_first>
  <behavior>
    - Test 1（chunk 边三连发幂等）：EdgeSpec(relation="MODIFIES_CHUNK", target_chunk_id=cid, metadata={...}) 经 apply_edge_specs 同参数调 3 次 → KnowledgeEdge 活跃 chunk 边恰 1 条，metadata 与首次一致
    - Test 2（XOR 校验）：EdgeSpec 同时给 target_entity_id 与 target_chunk_id、或两者皆 None → apply_edge_specs 对该 spec warning 跳过（不 raise 整批）
    - Test 3（实体边零回归）：既有 HAS_PLAN exclusive 语义不变（同 relation 换 target 时旧边 invalidate）——复跑既有用例 + 新增混合批（实体边 + chunk 边同批）断言互不干扰
    - Test 4（chunk_in_edges 反查）：建 code_change 实体 →MODIFIES_CHUNK→ chunk 边后，graph_store.chunk_in_edges(cid) 返回该边（含 source_id/metadata）；invalidate 后默认不可见
  </behavior>
  <action>
    1. `EdgeSpec`（frozen dataclass）扩展：`target_entity_id: uuid.UUID | None = None`（由必填改默认 None，既有调用方全部关键字传参不受影响）、新增 `target_chunk_id: uuid.UUID | None = None`、`metadata: dict | None = None`。docstring 补 XOR 语义（chunk 边 Phase 14）。
    2. `apply_edge_specs` 分支：spec 两 target XOR 不满足 → `logger.warning("knowledge_ingest_edge_spec_invalid", ...)` 跳过该 spec；chunk 边按 `edge.target_chunk_id == spec.target_chunk_id` 比对 neighbors 既有活跃出边去重，建边走 `graph_store.add_edge(source_id=..., target_chunk_id=..., relation=..., valid_at=event_time, metadata=spec.metadata)`，IntegrityError（撞 uniq_kedge_chunk_active）warning 幂等放弃（既有 knowledge_ingest_edge_conflict 形态）。chunk 边不支持 exclusive（语义不适用，spec.exclusive 对 chunk 边忽略）。
    3. `graph_store.py` 新增 `async def chunk_in_edges(self, chunk_id: uuid.UUID) -> list[EdgeRecord]`：纯 ORM `KnowledgeEdge.objects.filter(target_chunk_id=chunk_id, invalid_at__isnull=True).select_related("source_entity")` → EdgeRecord 列表（含 metadata），照 invalidate_edge 的"纯 ORM + 结构化日志"方法风格；docstring 注明"chunk 反查唯一收口（P9 精神）"。
    4. `sources/__init__.py` `_NORMALIZERS` 追加三行：`"workflow_plan": "knowledge.sources.workflow_plan"`、`"task_result": "knowledge.sources.task_result"`、`"feishu_work_item": "knowledge.sources.feishu_work_item"`（模块由 14-04/05/06 落地，注释标注 13-02 先注册先例）。
    5. 创建 `server/tests/knowledge/test_modifies_chunk.py`（类 `TestChunkEdgeIdempotency` / `TestChunkInEdges`），behavior 四用例；ChunkRegistry 测试数据用 sync 工厂 + sync_to_async（test_triggers.py L217 风格）。14-03 在同文件扩展对齐阶梯用例组。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/knowledge/test_modifies_chunk.py tests/knowledge/test_ingestion.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `rg "KnowledgeEdge.objects" server/knowledge/ingestion.py` 零命中（边写仍只走 graph_store 收口）
    - `rg -c '"workflow_plan"|"task_result"|"feishu_work_item"' server/knowledge/sources/__init__.py` == 3
    - chunk 边三连发后 `KnowledgeEdge.objects.filter(target_chunk_id=cid, invalid_at__isnull=True).count() == 1`（测试断言原文）
    - 既有 test_ingestion.py 全量零回归（实体边语义不变）
    - test_modifies_chunk.py 由本任务创建且 `-k chunk` 非空选中
  </acceptance_criteria>
  <done>ENH-01 的边写入/幂等/反查通路全部就绪，14-03 对齐阶梯与 14-06 normalizer 只产出 EdgeSpec。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: chunk_knowledge_text diff-aware 分支（test_chunking.py 扩展）</name>
  <files>server/knowledge/chunking.py, server/tests/knowledge/test_chunking.py</files>
  <read_first>
    - .planning/phases/14-triggers/14-RESEARCH.md §diff 文本 chunk 策略（content 结构、可重派生约束、chunk_kind="diff"、MAX_DIFF_CHUNKS 预算）
    - .planning/phases/14-triggers/14-PATTERNS.md §6（_HEADING_PROBE_RE/_split_segments 摘录 L307-330、硬约束清单）
    - server/knowledge/chunking.py（全文件：MAX_CHUNK_CHARS、summary chunk 语义、_hard_split、KnowledgeChunk）
    - server/tests/knowledge/test_chunking.py（golden 断言风格）
  </read_first>
  <behavior>
    - Test 1（文件边界切分）：content = 摘要段 + 两个 `diff --git` 文件段 → 摘要产出 summary/section chunk（chunk_kind 不变），diff 区段每文件一 chunk 且 chunk_kind == "diff"
    - Test 2（hunk 再切 + 硬切兜底）：单文件 diff 超 MAX_CHUNK_CHARS → 按 `^@@ ` hunk 头再切；单 hunk 仍超长 → _hard_split，全部 chunk ≤ MAX_CHUNK_CHARS 且 chunk_kind == "diff"
    - Test 3（确定性）：同一 diff content 连调两次输出逐项相等（index/text/chunk_kind）
    - Test 4（无 diff 零影响）：纯 markdown content 输出与改动前快照逐项一致（既有用例零回归即覆盖）
  </behavior>
  <action>
    `chunk_knowledge_text(title, content)` 签名与既有非 diff 路径输出保持逐字节不变。新增模块级 `_DIFF_PROBE_RE = re.compile(r"^diff --git ", re.MULTILINE)`：探测命中时把 content 在首个 `diff --git` 行处划为前段（沿既有标题分段逻辑）与 diff 区段；diff 区段 `re.split(r"^(?=diff --git )", ..., flags=re.MULTILINE)` 按文件切，超长文件段按 `re.split(r"^(?=@@ )", ...)` 按 hunk 切（hunk 段拼回所属文件头两行作为上下文前缀），仍超长走 `_hard_split`；diff 来源的 chunk 一律 `chunk_kind="diff"`。纯函数、无新依赖、不引入随机性。中文 docstring 补 Pitfall 8（content 即真理，revectorize 重派生一致）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/knowledge/test_chunking.py -x</automated>
  </verify>
  <acceptance_criteria>
    - 既有 test_chunking.py 用例零回归（非 diff 路径输出不变）
    - behavior 四用例全绿；所有 diff chunk 的 `len(text) <= MAX_CHUNK_CHARS`（测试逐 chunk 断言）
    - `rg "import " server/knowledge/chunking.py` 无新增第三方依赖（仍 stdlib re/dataclasses 级别）
  </acceptance_criteria>
  <done>diff 文本可确定性分层切块，14-06 的 code_change content 经既有 revectorize 通路安全重派生。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| diff 原文 → 归档表/向量管线 | 来自外部 git 平台的不可信大文本进入持久层与 chunker |
| 重复触发/回放 → 归档写入 | 同一编码产出可能被重复投递 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering（回放重复归档） | CodeChangeArchive | mitigate | Task 1：uniq_codechange_source_commit 约束 + 测试钉死（三层防线之 DB 层） |
| T-14-02 | Tampering（重复边静默累积，Pitfall 4） | KnowledgeEdge chunk 边 | mitigate | Task 1 partial unique + Task 2 apply_edge_specs 代码级幂等，三连发测试 |
| T-14-03 | Denial of Service（超长 diff 文本拖垮 chunker） | chunk_knowledge_text | mitigate | Task 3：文件→hunk→硬切分层，逐 chunk ≤ MAX_CHUNK_CHARS 测试断言 |
| T-14-04 | Information Disclosure（payload 权限维度缺失） | code_change 实体 payload | mitigate | 本 plan 不写实体；权限维度（project_id/repository_id 恒带）由 14-06 normalizer 任务承接（T-14-24） |
| T-14-SC | Tampering | uv add unidiff | accept | RESEARCH Package Legitimacy Audit 已 Approved（unidiff [VERIFIED: PyPI]，12 年/15M 周下载/源仓库一致）；无 [ASSUMED]/[SUS] 包，无需 blocking checkpoint |
</threat_model>

<verification>
- `cd server && uv run pytest tests/knowledge/ -x` 全绿（既有 ~109 用例零回归 + 本 plan 新增）
- `cd server && uv run python manage.py makemigrations --check --dry-run` 退出码 0
- `cd server && uv run ruff check knowledge/ tests/knowledge/ && uv run ruff format --check knowledge/ tests/knowledge/` 通过
</verification>

<success_criteria>
- CodeChangeArchive + migration + chunk 边幂等（代码级 + DB 级）+ diff-aware chunker 全部就绪且有测试
- 注册表三行登记完成，后续 plan 不再触碰 knowledge 核心模块（models/ingestion/graph_store/chunking/sources/__init__）
- KMOD-05 持久层与 ENH-01 边通路的全部验收锚点（unique 约束名 / chunk_kind="diff" / chunk_in_edges）可被 14-03/14-06 直接消费
</success_criteria>

<output>
Create `.planning/phases/14-triggers/14-01-SUMMARY.md` when done
</output>
