---
phase: 130-placement-units-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/services/process_runtime/placement_units.py
  - server/tests/services/process_runtime/test_placement_units.py
autonomous: true
requirements:
  - UNIT-01
user_setup: []

must_haves:
  truths:
    - "feature 点按模块聚合为 Placement Units，单元数明显少于逐点检索"
    - "模块依赖边与正文「复用 X」边被解析进单元结构"
    - "unit.query_text 不含 acceptance/测试用例正文；观测无需求原文"
  artifacts:
    - path: server/services/process_runtime/placement_units.py
      provides: "build_placement_units + PlacementUnit 契约 + reuse 边解析"
      exports: ["build_placement_units", "PlacementUnit"]
    - path: server/tests/services/process_runtime/test_placement_units.py
      provides: "UNIT-01 单测"
  key_links:
    - from: build_placement_units
      to: features_flat/modules
      via: "同模块聚合 + depends_on/复用边"
      pattern: "build_placement_units|reuse_edges|unit_id|query_text"
---

<objective>
交付可单测的放置单元聚合器：将 feature 点按同模块、模块依赖与「复用 X」边聚合为 Placement Units，避免逐点独立全库检索（UNIT-01；D-04、D-05、D-06）。

Purpose: 漏斗后段「连贯落点」的输入契约；本 plan 不实现细落点、不改 Adapter。
Output: `placement_units.py` + 单元测试。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/130-placement-units-wiring/130-CONTEXT.md
@.planning/phases/130-placement-units-wiring/130-RESEARCH.md
@.planning/milestones/v0.23.0-DECISIONS.md
@.planning/REQUIREMENTS.md
@server/services/process_runtime/initiative_profile.py
@server/initiatives/services/feature_list_extractor.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — placement units 聚合单测</name>
  <files>server/tests/services/process_runtime/test_placement_units.py</files>
  <behavior>
    - 给定 2 模块各 3 个 feature，build_placement_units 产出的 unit 数 ≤ 模块数（默认同模块合并），且远小于 6 若再合并依赖边
    - 模块 A depends_on B（或 modules 元数据等价字段）时，按 discretion 合并或建立可测的 unit 关系，且总 unit 数 &lt; 独立 feature 数
    - feature description 含「复用端内做题组件」时，对应 unit.reuse_edges 非空且 reuse_host_hints 含 practice 相关 hint（不硬编码仓 UUID）
    - query_text 不含 acceptance 字段内容
    - 观测：placement_units_started/completed 事件存在；kwargs 无超长需求全文
  </behavior>
  <action>
    编写失败用例覆盖上述行为；构造合成 feature_list/modules/features_flat，不连 DB/Qdrant。引用 D-04~D-06、UNIT-01。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/process_runtime/test_placement_units.py -q --tb=line 2>&1 | head -40</automated>
  </verify>
  <done>测试文件存在且对缺失实现失败（RED）或明确 import 失败。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — 实现 build_placement_units</name>
  <files>server/services/process_runtime/placement_units.py, server/tests/services/process_runtime/test_placement_units.py</files>
  <action>
    实现 `build_placement_units`（sync 纯函数优先，必要时 async 包装）：输入 feature_list 或 features_flat+modules；同模块合并；解析模块依赖边；正则/短语解析「复用…」写入 reuse_edges/reuse_host_hints；生成稳定 unit_id 与 query_text（module+name+description，剔除 acceptance）。导出 PlacementUnit dataclass 与结果容器（含 unit_count、duration_ms、degrade_reasons）。structlog：placement_units_started/completed/failed，category=sampling，component=process_runtime；异常 redact_secrets_in_text。禁止需求全文入日志。不修改 repo_router_v2.py。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/process_runtime/test_placement_units.py -q --tb=short --reuse-db</automated>
  </verify>
  <done>UNIT-01 单测全绿；模块可被 Plan 02 import。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| feature_list 输入 | 调用方传入的模块/功能点文本（半可信） |
| 日志出口 | 不得泄漏需求全文 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-130-01 | Information Disclosure | placement_units logging | mitigate | 只记 unit_count/duration；redact_secrets_in_text；禁止原文 |
| T-130-02 | Tampering | reuse 边解析 | accept | 启发式短语，错误仅影响 hint；下游 hard_scope 再约束 |
| T-130-SC | Tampering | package installs | accept | 本 plan 无新依赖 |
</threat_model>

<verification>
pytest test_placement_units.py 全绿；无 V2 文件改动。
</verification>

<success_criteria>
- Placement Units 可从合成 feature list 确定性产出
- 复用边与模块聚合可测
- 观测合规
</success_criteria>

<output>
Create `.planning/phases/130-placement-units-wiring/130-01-SUMMARY.md` when done
</output>
