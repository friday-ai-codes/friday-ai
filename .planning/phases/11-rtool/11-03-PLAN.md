---
phase: 11-rtool
plan: 03
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - runner/internal/docker/executor.go
autonomous: true
requirements: [RTOOL-03]
must_haves:
  truths:
    - "buildContainerEnv 在保留旧 FRIDAY_REMOTE_TOOLS 顶层键的同时，新增 FRIDAY_TASK_REMOTE_TOOLS（同一 payload remote_tools 的 JSON），修复 TaskConfig 前缀错位（Pitfall 2）"
    - "FRIDAY_TASK_USER_TOKEN / FRIDAY_TASK_TOOLS_ENDPOINT 经既有 metadata env_ TrimPrefix 通道透传进容器（无需新增 Go 解析逻辑）"
    - "runner zerolog 不打印任何 env 值（PAT 绝不进 Go 日志）"
    - "无 PAT 键时不注入空 FRIDAY_TASK_USER_TOKEN（既有 s != \"\" 守卫保持，向后兼容）"
  artifacts:
    - path: "runner/internal/docker/executor.go"
      provides: "buildContainerEnv 新增 FRIDAY_TASK_REMOTE_TOOLS 行"
      contains: "FRIDAY_TASK_REMOTE_TOOLS"
  key_links:
    - from: "runner/internal/docker/executor.go buildContainerEnv"
      to: "task TaskConfig.remote_tools"
      via: "FRIDAY_TASK_REMOTE_TOOLS = json(payload[remote_tools])"
      pattern: "FRIDAY_TASK_REMOTE_TOOLS"
    - from: "payload.metadata env_FRIDAY_TASK_*"
      to: "容器 env FRIDAY_TASK_*"
      via: "既有 TrimPrefix(\"env_\") 循环"
      pattern: "TrimPrefix"
---

<objective>
runner（Go）env 透传修复（RTOOL-03 中间跳）：在 `buildContainerEnv` 增加 `FRIDAY_TASK_REMOTE_TOOLS`（来自同一 payload 的 `remote_tools`，JSON 序列化），修复 task `TaskConfig`（`FRIDAY_TASK_` 前缀）读不到旧 `FRIDAY_REMOTE_TOOLS` 的前缀错位（Pitfall 2）；`FRIDAY_TASK_USER_TOKEN` / `FRIDAY_TASK_TOOLS_ENDPOINT` 复用既有 metadata `env_` 透传通道（零新增解析）。

Purpose: RTOOL-03 的 server→runner→task 注入链在 runner 这一跳打通；保持 zerolog 不打印 env 值（PAT 不进 Go 日志）。
Output: `executor.go` 单文件最小增量（新增一行 FRIDAY_TASK_REMOTE_TOOLS 映射）；使 11-01 runner RED 用例转 GREEN。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-rtool/11-RESEARCH.md
@runner/internal/docker/executor.go
@runner/internal/docker/executor_test.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: executor.go — buildContainerEnv 新增 FRIDAY_TASK_REMOTE_TOOLS（前缀修复）</name>
  <files>runner/internal/docker/executor.go</files>
  <read_first>
    - runner/internal/docker/executor.go:90-133（buildContainerEnv 全函数：`remoteTools, _ := json.Marshal(task.Payload["remote_tools"])` 已存在、`FRIDAY_REMOTE_TOOLS=` 顶层注入、`FRIDAY_TASK_*` 前缀块、metadata `env_` TrimPrefix 循环 + `s != ""` 守卫——新增行紧随 FRIDAY_TASK_ 块）
    - runner/internal/docker/executor.go:86（现有 log.Info().Str(...)：确认**未**打印任何 env 值——保持，不新增 env 值日志）
    - .planning/phases/11-rtool/11-RESEARCH.md §Common Pitfalls 2（FRIDAY_REMOTE_TOOLS vs FRIDAY_TASK_REMOTE_TOOLS 前缀错位修复）+ §Dispatch Contract Map step 5（二选一：本 plan 取 Go 侧映射，单一真源 payload remote_tools，零 server 重复 fetch）
    - runner/internal/docker/executor_test.go（11-01 落地的 TestBuildContainerEnv_RemoteTools 断言契约）
  </read_first>
  <action>
    在 buildContainerEnv 的 `env := []string{...}` 字面量内、`FRIDAY_TASK_` 前缀块附近，新增一行：
    `"FRIDAY_TASK_REMOTE_TOOLS=" + string(remoteTools),`（复用函数顶部已有的 `remoteTools, _ := json.Marshal(task.Payload["remote_tools"])`，与旧 `FRIDAY_REMOTE_TOOLS` 同源、同值，仅前缀不同——TaskConfig 只认 `FRIDAY_TASK_` 前缀）。
    保留旧 `FRIDAY_REMOTE_TOOLS=` 行（兼容既有读者，勿删）。
    `FRIDAY_TASK_USER_TOKEN` / `FRIDAY_TASK_TOOLS_ENDPOINT` **不**需新增代码——server 经 `metadata["env_FRIDAY_TASK_USER_TOKEN"]` / `metadata["env_FRIDAY_TASK_TOOLS_ENDPOINT"]` 注入，既有 `env_` TrimPrefix 循环（含 `s != ""` 空值守卫）自动透传。
    **不**新增任何打印 env 值的 zerolog 语句（PAT 脱敏硬约束，per Pitfall 4）。`gofmt` 格式化。
  </action>
  <verify>
    <automated>cd runner && go test ./internal/docker/ -run TestBuildContainerEnv 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `cd runner && go test ./internal/docker/... passes`：TestBuildContainerEnv_RemoteTools 转 GREEN（FRIDAY_TASK_REMOTE_TOOLS 含 remote_tools JSON；FRIDAY_TASK_USER_TOKEN / FRIDAY_TASK_TOOLS_ENDPOINT 经 metadata 透传）。
    - 既有 `TestBuildContainerEnvSeparatesTaskModeAndTaskType` 仍 PASS（旧 FRIDAY_REMOTE_TOOLS 与 FRIDAY_TASK_* 块未破坏）。
    - 无 PAT metadata 键时容器 env 不含 FRIDAY_TASK_USER_TOKEN（既有 `s != ""` 守卫，向后兼容）。
    - `gofmt -l runner/internal/docker/executor.go` 无输出（格式合规）；executor.go 无任何打印 env 值的 log 语句。
  </acceptance_criteria>
  <done>FRIDAY_TASK_REMOTE_TOOLS 映射就位，runner RED 用例转 GREEN，PAT 不进 Go 日志。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dispatch payload（不可信编排）→ 容器 env | runner 把 server 下发的 metadata env_ 落成容器环境变量 |
| runner 进程日志（zerolog）↔ 运维可见 | env 值（含 PAT）绝不进日志 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-08 | Information Disclosure | runner zerolog 打印 env 值（含 PAT） | mitigate | 不新增任何打印 env 值的 log；现状仅 log task_id/container_id（保持） |
| T-11-09 | Tampering | 前缀错位致 remote_tools 永远空 | mitigate | 新增 FRIDAY_TASK_REMOTE_TOOLS 同源映射，TaskConfig 可读（test 守卫） |
| T-11-10 | Tampering | 注入空 FRIDAY_TASK_USER_TOKEN 覆盖 | mitigate | 复用既有 `s != ""` 守卫，空值不注入 |
| T-11-SC | Tampering | 供应链（依赖变更） | accept | 零新增 Go 依赖（仅用既有 encoding/json、strings；RESEARCH §Package Legitimacy Audit 不适用） |
</threat_model>

<verification>
- `cd runner && go test ./internal/docker/...` → 全 PASS（新 + 既有用例）。
- `gofmt -l runner/internal/docker/executor.go` → 无输出。
- `rg "Str\(" runner/internal/docker/executor.go` 复核：无打印 env 值（仅 task_id/container_id/answer_endpoint 等非敏感字段）。
</verification>

<success_criteria>
- RTOOL-03 runner 跳打通：FRIDAY_TASK_REMOTE_TOOLS 经前缀修复可被 TaskConfig 读取；USER_TOKEN/TOOLS_ENDPOINT 经既有 metadata env_ 透传。
- runner 日志零 env 值泄漏（PAT 不进 zerolog）。
- 既有 env 装配与测试不回退（向后兼容）。
</success_criteria>

<output>
Create `.planning/phases/11-rtool/11-03-SUMMARY.md` when done
</output>
