---
phase: 38-routing-recall
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/delivery/models/plan_session.py
  - server/delivery/migrations/0011_plansession_routing_recall_created_by.py
  - server/delivery/services/plan_session_service.py
  - server/tests/delivery/test_plan_session_service.py
autonomous: true
requirements: [ROUTE-01, RECALL-01]

must_haves:
  truths:
    - "PlanSession 持久化 routing 候选仓结果（候选 + confidence）"
    - "PlanSession 持久化 recall_context 召回上下文"
    - "PlanSession 记录发起编排的用户 created_by（可空）"
    - "transition 经单一写入入口把 routing/recall_context payload 落到对应字段（INV-6）"
    - "makemigrations --check 干净（模型与迁移一致）"
  artifacts:
    - path: "server/delivery/models/plan_session.py"
      provides: "PlanSession.routing / recall_context / created_by 字段"
      contains: "recall_context"
    - path: "server/delivery/migrations/0011_plansession_routing_recall_created_by.py"
      provides: "三字段 schema 迁移"
      contains: "AddField"
    - path: "server/delivery/services/plan_session_service.py"
      provides: "create_session(created_by) + routing/recall_context 持久化"
      contains: "recall_context"
  key_links:
    - from: "server/delivery/services/plan_session_service.py"
      to: "PlanSession.routing / recall_context 字段"
      via: "_PERSISTABLE_FIELDS 白名单 + 条件原子更新"
      pattern: "_PERSISTABLE_FIELDS"
---

<objective>
为 Phase 38 路由/召回接入扩展 `PlanSession` 的持久化底座：新增 `routing`、`recall_context` 两个 JSON 字段承载路由候选仓与召回上下文，新增 `created_by` 可空 FK 记录发起编排的用户（供召回 stage 作权限 actor）；并把这两个产物键接入 `PlanSessionService` 的单一写入入口（INV-6），使 38-02/38-03 的 engine transition 能落库。

Purpose: 路由/召回结果必须落 `PlanSession` 行才能被 Phase 39 并行调研消费；`created_by` 是召回 fail-closed 权限过滤的 actor 来源。本 plan 是 38-02/38-03 的硬前置（wave 1）。
Output: PlanSession 三新字段 + delivery 0011 迁移 + PlanSessionService 持久化接线 + 守护测试，`makemigrations --check` 干净。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/38-routing-recall/38-CONTEXT.md
@.planning/DOMAIN-MODEL.md

@server/delivery/models/plan_session.py
@server/delivery/services/plan_session_service.py
@server/delivery/migrations/0009_plansession.py
@server/tests/delivery/test_plan_session_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: PlanSession 新增 routing/recall_context/created_by 字段 + delivery 0011 迁移</name>
  <files>server/delivery/models/plan_session.py, server/delivery/migrations/0011_plansession_routing_recall_created_by.py</files>
  <action>
在 `PlanSession` 模型（紧随既有 `decomposition`/`error` JSON 字段之后）新增三个字段，支撑 ROUTE-01/RECALL-01 的结果持久化：
- `routing = models.JSONField(default=dict, blank=True)` —— 路由候选仓结果（38-02 写入 `{candidates:[{repo_id, confidence, repository_name?}], router_version, auto_selected}`）。
- `recall_context = models.JSONField(default=dict, blank=True)` —— 召回上下文（38-03 写入精简召回命中列表 `[{entity_id, kind, title, score}]`；默认 `dict` 与既有 JSON 字段范式一致，写入时按 list 覆盖，default 仅占位）。
- `created_by = models.ForeignKey(settings.AUTH_USER_MODEL, on_delete=models.SET_NULL, null=True, blank=True, related_name="+")` —— 发起编排的用户；`null=True` 满足系统/无交互用户场景（召回 stage 作权限 actor，为空走 fail-closed 空召回）。`related_name="+"` 不建反向访问器（避免污染 user 模型）。
在模型顶部 import 处加 `from django.conf import settings`（FK 用 `settings.AUTH_USER_MODEL` 字符串引用，避免直接 import User）。为 routing/recall_context 写简短 zh-CN 注释说明用途与写入 phase。
然后用 `cd server && uv run python manage.py makemigrations delivery` 生成迁移，确认文件名为 `0011_plansession_routing_recall_created_by.py`（若 Django 生成不同后缀名，重命名为该名并保持 `dependencies=[('delivery','0010_technicalplan_planversion_planexternalref')]`）。迁移须含三个 `AddField`（routing/recall_context/created_by）。不手写迁移正文——以 makemigrations 产物为准，仅校正文件名。
保持 ruff line-length 100。
  </action>
  <verify>
    <automated>cd server && uv run python manage.py makemigrations delivery --check --dry-run</automated>
  </verify>
  <done>三字段在模型上声明；0011 迁移存在且含 routing/recall_context/created_by 三 AddField；`makemigrations --check` 退出码 0（无待生成迁移）。</done>
</task>

<task type="auto">
  <name>Task 2: PlanSessionService 接入 created_by + routing/recall_context 持久化 + 守护测试</name>
  <files>server/delivery/services/plan_session_service.py, server/tests/delivery/test_plan_session_service.py</files>
  <action>
扩展 `PlanSessionService`（INV-6 单一写入入口，不旁路）：
1. `_PERSISTABLE_FIELDS` 元组追加 `"routing"` 和 `"recall_context"`（现有为 `("decomposition", "current_plan_version", "event_time")`）——这是 38-02/38-03 engine `transition(session, "routed", routing=...)` / `transition(session, "recalled", recall_context=...)` 落库的唯一通道（半可信 payload 仅白名单字段落库）。
2. `create_session` 新增可选关键字参数 `created_by=None`，透传到 `_create_session_sync`，并在 `PlanSession.objects.create(...)` 中传 `created_by=created_by`。`_create_session_sync` 形参同步新增 `created_by`。保持既有 entrypoint 校验与 async/sync_to_async 范式不变。
3. 不改 `transition` 的转移逻辑（routing/recall_context 走既有 `_apply_transition_sync` 的 `_PERSISTABLE_FIELDS` 循环即可落库，无需新分支）。
在 `server/tests/delivery/test_plan_session_service.py` 追加测试（沿用既有 `@pytest.mark.django_db @pytest.mark.asyncio` 范式）：
- `test_routed_persists_routing_field`：建 status=routing 的 session，`transition(session, "routed", routing={"candidates":[{"repo_id":"r1","confidence":"high"}],"router_version":"v2","auto_selected":True})`，从 DB 重取断言 status=recalling 且 `routing` 字段等于传入 dict。
- `test_recalled_persists_recall_context_field`：建 status=recalling 的 session，`transition(session, "recalled", recall_context=[{"entity_id":"e1","kind":"work_item","title":"t","score":0.9}])`，重取断言 status=clarifying 且 `recall_context` 等于传入 list。
- `test_create_session_with_created_by_persists`：建一个 user（用既有测试 user 工厂或 `get_user_model().objects.acreate(...)`），`create_session(PlanSessionEntrypoint.WORKFLOW, created_by=user)`，重取断言 `created_by_id == user.id`。
- `test_create_session_created_by_defaults_none`：`create_session(PlanSessionEntrypoint.CHAT)` 不传 created_by，断言 `created_by_id is None`（nullable 默认）。
若仓库已有 user fixture/factory，复用之；否则用 `django.contrib.auth.get_user_model()` 经 `sync_to_async` 创建最小 user。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/delivery/test_plan_session_service.py -x -q</automated>
  </verify>
  <done>_PERSISTABLE_FIELDS 含 routing/recall_context；create_session 接受并持久化 created_by；4 个新测试通过；既有 transition 测试不回归。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| transition payload → DB 字段 | engine 传入的 `**payload` 半可信，可能含越权键 |
| created_by FK | 决定召回 stage 的权限 actor（下游 38-03 fail-closed 依赖） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-01-01 | Tampering | `transition(**payload)` 写库 | mitigate | 仅 `_PERSISTABLE_FIELDS` 白名单键落库（routing/recall_context 显式加入），其余键忽略，禁止任意字段写入 |
| T-38-01-02 | Elevation of Privilege | `created_by` 字段 | accept | 本 plan 仅持久化 created_by；真实权限过滤在 38-03 召回 stage 经 search_similar fail-closed 完成（user=None → 空召回，不泄漏） |
| T-38-01-03 | Tampering | delivery 0011 迁移 | mitigate | 以 `makemigrations` 产物为准 + `--check` 守护模型/迁移一致，禁止手写漂移 |
</threat_model>

<verification>
- `cd server && uv run python manage.py makemigrations delivery --check --dry-run` 退出码 0。
- `cd server && uv run pytest tests/delivery/test_plan_session_service.py -q` 全绿。
- `cd server && uv run ruff check delivery/models/plan_session.py delivery/services/plan_session_service.py` 无新增告警。
</verification>

<success_criteria>
- PlanSession 三新字段落库（routing/recall_context JSON + created_by nullable FK）。
- PlanSessionService 经单一入口持久化 routing/recall_context payload 与 created_by（INV-6 不旁路）。
- makemigrations --check 干净，无模型/迁移漂移。
</success_criteria>

<output>
Create `.planning/phases/38-routing-recall/38-01-SUMMARY.md` when done
</output>
