---
phase: 116-entry
plan: 04
type: execute
wave: 3
depends_on: ["116-02"]
files_modified:
  - server/knowledge/api/views.py
  - server/knowledge/retrieval.py
  - server/knowledge/sources/blueprint.py
  - server/knowledge/sources/__init__.py
  - server/knowledge/models.py
  - server/delivery/services/artifact_service.py
  - server/delivery/api/blueprint_doc_views.py
  - server/tests/delivery/test_blueprint_log_redaction_guard.py
  - server/tests/knowledge/test_related_relations_param.py
  - server/tests/knowledge/test_blueprint_normalizer.py
  - web/src/api/knowledge.ts
  - web/src/components/blueprint/BlueprintAssociationsSection.vue
  - web/src/components/blueprint/__tests__/sections.spec.ts
autonomous: true
requirements: [VIEW-04]
prohibitions:
  - "⛔ ⭐ **绝不改 `knowledge/related.py` 的 `_DEFAULT_RELATIONS`**（`:18-22`，冻结面）：它是 `pages/knowledge/entities/[id].vue` 等既有面的默认遍历集，加 `REFERENCES` 会让所有实体详情页突然多出一批引用邻居（行为回归）。只**透传** `relations` 入参 —— 最底层 `fetch_related_entities` **已经有这个形参**（`related.py:31` / `:47` `rels = relations or list(_DEFAULT_RELATIONS)`），断的只是中间两层与前端"
  - "⛔ ⭐ **绝不改 `knowledge/ingestion.py` 的 `apply_edge_specs`**（公共函数，`:356-443`）：丢弃计数落 normalizer 内。⛔ 也不裸调 `graph_store` 建边（一律走 `EdgeSpec` + `apply_edge_specs` 的既有幂等通路）"
  - "⛔ **不新建 `EntityKind`**：实体 id = `generate_entity_id(\"tech_plan\", \"blueprint\", str(artifact_id))`（`tech_plan` 是既有 kind、`source_kind` 区分子类是 Phase 100 惯例）。新建 kind 要动 `kentity_kind_valid` 约束 + migration，且与 DESIGN §7.2「蓝图沿用 `KnowledgeEntity(kind=tech_plan)` 入图」相悖"
  - "⛔ **不新增 migration**（`knowledge/models.py` 只改 `generate_entity_id` 的 **docstring** natural key 规则表，零字段变更）；`makemigrations --check --dry-run` 退出码必须 0"
  - "⛔ ⭐ **`sections.spec.ts:587-596` 那条用例拆两条而不是整条翻转**：`getRelated` 转真实调用断言，⭐ **`getArtifactAssociations` 仍必须为 0** —— `knowledge/artifact_associations.py:75` 查的是 `generate_entity_id(DOCUMENT, \"artifact\", ...)`（`initiatives.Artifact` 投影），对 `delivery.Artifact` id 依然**必然落空**。整条翻转会把一个仍然成立的防线删掉"
  - "⛔ **前端 `maxHops` 必须显式传 1**：view（`knowledge/api/views.py:163`）与前端（`api/knowledge.ts:201`）默认都是 **2**，「被谁引用」要的是**直接引用者**；不传会把二跳实体也列进来"
  - "⛔ **`GET .../blueprint/` 的响应是纯追加第 8 键**（现有 7 键 `version_id/version_no/is_current/produced_by_ref/created_at/content/quality` 一个不动）；⛔ 不让前端复制 uuid5 规则（`generate_entity_id` docstring 明令它是唯一入口）"
  - "⛔ **`space_id` 反查不到就整体不入图**：`fetch_related_entities` 有**两处** `space_id is None` 短路（`:40-41` 判起点实体、`:79-80` 判每个对端实体）⇒ space 为空的实体是**双向不可见**的孤儿节点。「入了图却永远查不出来」是精确描述 ⇒ 不产事件 + warning"
  - "⛔ **导出/摄取事件不进 `BLUEPRINT_EVENTS`**（`len == 21` 被 `test_blueprint_event_taxonomy_112.py:111,119` 双断言锁死）；normalizer 的丢弃计数走 `category=\"sampling\"` 的 structlog 事件"
  - "⛔ **前端只改 0.20 归属的三个文件 + 一处 `api/knowledge.ts` 纯追加**：`BlueprintAssociationsSection.vue` / `sections.spec.ts`（115 自建）+ `api/knowledge.ts`（既有通用件，**只给 `getRelated` 的 options 加一个可选字段，删除行必须为 0**）。⛔ 不碰 `pages/knowledge/entities/[id].vue`、⛔ 不碰 `components/knowledge/EntityAssociationsCard.vue`、⛔ 不碰 §13.2 的四个 0.19 归属面"
  - "⚠️ **P-15**：本 plan 有前端门，跑完 `pnpm` 后 `git status` 检查并 `git checkout -- web/pnpm-workspace.yaml`（pnpm 会自动回填 `catalogs`，否则被边界核算误判为「新增依赖」）"
must_haves:
  truths:
    - "⭐ **第一个 task 必须是打通 `relations` 参数**（RESEARCH 给 PLAN 第 1 条）：不先做这条，后面所有边的工作**都无法被验收** —— `_DEFAULT_RELATIONS` 是 `[HAS_PLAN, IMPLEMENTED_BY, RELATES_TO]`**不含 `REFERENCES`**，而 `KnowledgeRelatedView.get`（`knowledge/api/views.py:150-176`）只解析 `as_of` / `direction` / `max_hops`、`DeliveryKnowledgeSearchService.get_related`（`retrieval.py:135-146`）形参里也没有 ⇒ `?relations=REFERENCES` 被**静默丢弃**、遍历只走三条默认关系、返回空数组。症状是**边全部正确入库、端点 200、页面空白**"
    - "**三处纯追加，不传时行为逐字不变**：① `knowledge/api/views.py:157` 附近解析 `?relations=A,B`，逐项白名单校验 `EdgeRelation.values`（非法 → **400**，与既有 `direction` 校验同形）；② `retrieval.get_related` 加 `relations: list[str] | None = None` 并透传（默认 None ⇒ 既有调用点零回归）；③ `web/src/api/knowledge.ts:193-202` 的 `getRelated` options 加 `relations?: string[]` 拼进 query"
    - "⭐ **反查的验收断言必须端到端**（P-1）：造一条 `blueprint → REFERENCES → knowledge_entity` 边，从**被引方** `GET /api/knowledge/related/<entity_id>/?direction=in&relations=REFERENCES&max_hops=1` 查回**引用方**。⛔ **不接受「断言 `KnowledgeEdge` 表里有那一行」这种止于 DB 的用例**"
    - "⭐ **投递门控挂 `add_version` **与** `create` 两处**（方案 a，P-10）：`create` 的调用面只有旧链 merge 与 echo，加 `schema_version` 判别对它们零影响；只挂 `add_version` 的话 **intake 建的 v1 骨架永远不入图**（它走 `create`），「新建蓝图 → 立刻查图谱 → 空」会被当 bug 反复排查。门控条件逐字 `isinstance(content, dict) and content.get(\"schema_version\") == BLUEPRINT_SCHEMA_VERSION`（⛔ 懒 import 常量，不复制字面量）"
    - "⭐ **`add_version` 的门控必须判断是否真的翻了版本**：`_add_version_sync` 在 `content_hash` 相等时 `return current`（`artifact_service.py:145-150`）⇒ 比对返回 version 的 `id` 与调用前的 `artifact.current_version_id`（或 `version_no`），不同才投递。否则每次无变化的重复写入都白跑一次 normalizer + 一次后台任务"
    - "投递 `aschedule_ingestion(IngestionRequest(\"blueprint\", str(artifact_id), \"blueprint_version_created\"))`；⛔ **调用方不再包一层 try**（`aschedule_ingestion` 内部已吞异常，`ingestion.py:118`；重复兜底会掩盖 normalizer 注册漏行这类应当响亮的错误）"
    - "⭐ **`_NORMALIZERS` 加一行 `\"blueprint\": \"knowledge.sources.blueprint\"` 必须与模块创建同一个 commit**：`get_normalizer` 对未知 kind **直接 raise KeyError**（`:45-53`，「响亮——触发点接错线必须立刻暴露」）"
    - "⭐ **实体身份**：`generate_entity_id(\"tech_plan\", \"blueprint\", str(artifact_id))`；`kind = EntityKind.TECH_PLAN`；同时在 `knowledge/models.py` 的 `generate_entity_id` docstring natural key 规则表**追加一行 `blueprint`**（该 docstring 明令它是唯一入口）"
    - "⭐ **`space_id` 是 `projects.Space.id` 不是 Project id**（`workflow_plan.py:105` 逐字：`project_id = str(execution.space_id)` 后赋给 `space_id=` —— 变量名是历史命名噪声）：蓝图链路是 `meta.project_id`（Project id）→ `initiatives.Project.space_id` → `IngestionEvent.space_id`。⭐ **反查不到 ⇒ 不产事件 + warning + 返回空列表**（源缺失的既有降级形态，`workflow_plan.py:69-75`）"
    - "⭐ **边只有两类，各带一条结构约束**：`citations` → `REFERENCES`（`exclusive=False`、**append-only** —— 新版本删掉某条引用**不**失效旧边，bi-temporal 下「v2 曾引用过它」仍是事实）；`meta.project_id` → 项目节点 `RELATES_TO`（`exclusive=True`）"
    - "⭐ **目标实体不存在的 spec 必须先过滤掉再交给 `apply_edge_specs`**：`KnowledgeEdge.target_entity` 是**真 FK**（`knowledge/models.py:305-311`）⇒ 目标不存在 → `IntegrityError` → 被 `apply_edge_specs:435-443` 吞成 `knowledge_ingest_edge_conflict` warning，**边静默消失**；⚠️ 该分支还与「撞 `uniq_kedge_active`（并发已建，良性）」**共用同一个 except**，日志里分不出来。⇒ 一次批量 `KnowledgeEntity.objects.filter(id__in=[...]).values_list(\"id\", flat=True)`，丢弃计数进 `sampling` 事件（`dropped_count` / `dropped_by_source_type` dict / `kept_count` / `artifact_id` / `category=\"sampling\"` / `component=\"knowledge\"`）"
    - "⭐ **同目标多条 citation 聚合成一条 `EdgeSpec`**：`uniq_kedge_active` 是 `(source_entity, target_entity, relation)` 唯一（`models.py:335-339`）⇒ 「一条 citation 一条边」从第二条起**稳定撞约束并被吞成 warning**。`metadata = {\"source\": \"blueprint\", \"citation_ids\": [...], \"source_types\": [...]}`"
    - "⭐ **`first_seen_version_no` 去掉**（P-5，方案 a）：重摄取时 `apply_edge_specs` 对已有活跃边调 `update_edge_metadata` **整体覆盖**（`:412-422`），而 normalizer 只能看到当前版本的 content、拿不到既有边 metadata ⇒ 该字段每次都被刷成当前版本号，**字段名与语义直接对不上**。它的信息量已由 `KnowledgeEdge.valid_at` / `created_at` 承载"
    - "⭐ **`RELATES_TO` 出边恰好 1 条**（P-6）：`exclusive=True` 的作用域是 `(source, relation)` **不是** `(source, relation, 目标类型)` ⇒ 多条 `RELATES_TO` 会互相 `invalidate_edge`，且走的是**正常路径**（不是异常、不是 warning）⇒ **完全静默**。这条要写进 normalizer 的**模块 docstring** 并配一条结构断言（normalizer 产出的 `EdgeSpec` 里 `relation == RELATES_TO` 的恰好 1 条）"
    - "**九种 `source_type` → 目标实体换算**（`_resolve_target_entity_id(citation) -> uuid | None`，No Analog #5）：`knowledge_entity`（`source_id` 本身即 entity id）/ `work_item`（还原 `{project_key}:{type_key}:{item_id}` 三元组）/ `feishu_doc`（doc token）/ `blueprint`（同款 natural key）/ `artifact_version`（一次 `ArtifactVersion → artifact_id` 查询后换算）四种直连；`repo_file` · `rag_chunk` · `repo_charter` 统一落 `generate_entity_id(\"repository\",\"repository\",repo_id)`（`metadata` 里区分具体来源）；`url` ⛔ **不成边**。**还原不出即丢弃并计数**；⭐ **九种各配一条用例**"
    - "⭐ **`GET .../blueprint/` 响应纯追加第 8 键 `knowledge_entity_id`**（现有 7 键一个不动），值经 `generate_entity_id` 唯一入口派生；⛔ 不让前端复制 uuid5 规则"
    - "⭐ **前端 `BlueprintAssociationsSection.vue` 补两块**：「被哪些方案 / 知识引用」（`getRelated(knowledge_entity_id, { direction: 'in', relations: ['REFERENCES'], maxHops: 1 })`）+「关联知识」（`direction: 'out'`）。两块走 TanStack Query（key `['blueprint','related',artifactId,...]`）；⛔ **`refetchInterval` 零命中**（源码守卫锁死，实时数据一律经 `useBlueprintLive`）；组件 docstring 里「本相位显式范围收窄」那一段**同步改写**为「116 已交付反向反查」"
    - "⭐ **`sections.spec.ts` 那条用例拆两条**：`getRelated` 改为真实调用断言（含 `direction: 'in'` / `relations: ['REFERENCES']` / `maxHops: 1` 三个入参逐字）；⭐ `getArtifactAssociations` **仍断言 `toHaveBeenCalledTimes(0)`**（它查 `initiatives.Artifact` 投影，对 `delivery.Artifact` id 依然必然落空）"
    - "全量后端门相对基线 **8609 passed / 1 failed** 无新增失败；前端门 `vitest` 相对基线 **1674 passed / 1 skipped** 无新增失败、`type-check` exit 0、`build` 通过、`lint` 在**本 plan 触及的文件**上零新增问题（仓库有 111 个既有问题）"
  artifacts:
    - path: "server/knowledge/api/views.py"
      provides: "KnowledgeRelatedView.get 解析 ?relations=A,B（EdgeRelation.values 白名单，非法 400，与 direction 同形），不传时行为逐字不变；删除行 0"
      contains: "relations"
    - path: "server/knowledge/retrieval.py"
      provides: "get_related 加 relations: list[str] | None = None 并透传 fetch_related_entities（默认 None ⇒ 既有调用点零回归）；删除行 0"
      contains: "relations"
    - path: "server/knowledge/sources/blueprint.py"
      provides: "normalize()：tech_plan/blueprint 实体 + space 反查（取不到不产事件 + warning）+ citations 聚合成 REFERENCES 边（exclusive=False、同目标聚合、目标存在性预过滤 + 丢弃计数 sampling 事件）+ project RELATES_TO 恰好 1 条 + _resolve_target_entity_id 九种换算"
      contains: "REFERENCES"
    - path: "server/delivery/services/artifact_service.py"
      provides: "create 与 add_version 两处 schema_version 门控投递 aschedule_ingestion（add_version 侧判断是否真的翻版本），删除行 0"
      contains: "aschedule_ingestion"
    - path: "server/delivery/api/blueprint_doc_views.py"
      provides: "GET blueprint/ 响应纯追加第 8 键 knowledge_entity_id（经 generate_entity_id 唯一入口）"
      contains: "knowledge_entity_id"
    - path: "server/tests/knowledge/test_related_relations_param.py"
      provides: "⭐ 端到端反查（造边 → 被引方 ?direction=in&relations=REFERENCES&max_hops=1 → 查回引用方）+ 非法 relation 400 + 不传 relations 时行为与改动前逐字一致"
      contains: "direction=in"
    - path: "server/tests/knowledge/test_blueprint_normalizer.py"
      provides: "目标不存在 ⇒ 不产边 + 记了 source_type 的 sampling 事件；同目标两条 citation ⇒ 活跃边恰好 1 条且 metadata.citation_ids 两项；RELATES_TO 出边恰好 1 条；space 反查不到 ⇒ 不产事件 + warning；九种 source_type 换算各一条；v1 骨架经 create 也入图"
      contains: "dropped_by_source_type"
    - path: "web/src/components/blueprint/BlueprintAssociationsSection.vue"
      provides: "补「被哪些方案/知识引用」与「关联知识」两块（getRelated direction in/out + relations REFERENCES + maxHops 1），docstring 收窄段改写；⛔ 零 refetchInterval"
      contains: "REFERENCES"
    - path: "web/src/api/knowledge.ts"
      provides: "getRelated options 加 relations?: string[] 并拼进 query（纯追加，删除行 0）"
      contains: "relations"
  key_links:
    - from: "server/knowledge/api/views.py"
      to: "server/knowledge/related.py"
      via: "?relations= → get_related(relations=...) → fetch_related_entities(relations=...)（最底层形参已存在 :31，断的是中间两层）"
      pattern: "relations"
    - from: "server/delivery/services/artifact_service.py"
      to: "server/knowledge/ingestion.py"
      via: "create/add_version 内 schema_version 门控 → aschedule_ingestion(IngestionRequest(\"blueprint\", artifact_id, \"blueprint_version_created\"))"
      pattern: "IngestionRequest"
    - from: "server/knowledge/sources/blueprint.py"
      to: "server/knowledge/models.py"
      via: "generate_entity_id(\"tech_plan\", \"blueprint\", artifact_id) 唯一入口 + natural key 规则表 docstring 追加一行"
      pattern: "generate_entity_id"
    - from: "server/knowledge/sources/blueprint.py"
      to: "server/initiatives/models/project.py"
      via: "meta.project_id → Project.space_id → IngestionEvent.space_id（反查不到即不产事件，避免双向不可见的孤儿节点）"
      pattern: "space_id"
    - from: "web/src/components/blueprint/BlueprintAssociationsSection.vue"
      to: "server/delivery/api/blueprint_doc_views.py"
      via: "消费 GET blueprint/ 新增的 knowledge_entity_id 作为 getRelated 的实参（⛔ 前端不复制 uuid5 规则）"
      pattern: "knowledge_entity_id"
---

<objective>
让「蓝图被谁引用」从**结构上不可能**变成真的能查——并且不让它变成本相位最典型的「断言全绿而功能为零」。

⭐ **本 plan 的第一个 task 必须是打通 `relations` 参数**，因为 CONTEXT 写的「反查零新端点，只补一个换算键」**不成立**（RESEARCH §0.4 实测推翻）：`_DEFAULT_RELATIONS` 是 `[HAS_PLAN, IMPLEMENTED_BY, RELATES_TO]`，**不含 `REFERENCES`**；`KnowledgeRelatedView` 只接三个 query 参数、`DeliveryKnowledgeSearchService.get_related` 形参里也没有 `relations`。⇒ 哪怕 REFERENCES 边全部正确物化，`?relations=REFERENCES` 会被**静默丢弃**，遍历只走三条默认关系。**症状是端点 200、边在库里、页面空白** —— 不先做这条，后面所有边的工作都无法被验收。

其余四处会静默假通过的地方，逐条钉死：

① **目标实体不存在 ⇒ 边静默消失**（P-1 的近亲）。`KnowledgeEdge.target_entity` 是**真 FK**，目标不存在时 `add_edge` 抛 `IntegrityError`，被 `apply_edge_specs:435-443` 吞成一条 warning；⚠️ 更糟的是该分支与「撞 `uniq_kedge_active`（并发已建，良性）」**共用同一个 except**，日志里根本分不出「边丢了」和「本来就有」。⇒ 必须**先批量过滤存在性**，并把丢弃计数按 `source_type` 记进 `sampling` 事件。

② **同目标多条 citation ⇒ 从第二条起稳定撞唯一约束**。`uniq_kedge_active` 是 `(source, target, relation)` 唯一，「一条 citation 一条边」的朴素写法会被吞成 warning。⇒ 必须在 normalizer 里 group 成**一个** `EdgeSpec`。

③ **`exclusive=True` 的作用域是 `(source, relation)`**，不含目标类型 ⇒ 多条 `RELATES_TO` 出边会互相 `invalidate_edge`，而且走的是**正常路径**（不是异常、不是 warning）—— **完全静默**。⇒ 结构断言「`RELATES_TO` 恰好 1 条」+ 写进模块 docstring。

④ **v1 骨架不入图**（P-10）。intake 建的 v1 走 `ArtifactService.create` **不经 `add_version`** ⇒ 只在 `add_version` 挂门控的话，新建的蓝图在图谱里查不到、直到第一次版本更新才补上，「新建蓝图 → 立刻查图谱 → 空」会被当 bug 反复排查。⇒ 两处都挂（`create` 的调用面只有旧链 merge 与 echo，加判别对它们零影响）。

Purpose: VIEW-04 由 PARTIAL 转 Complete —— 115-05 明确登记的「反向『被谁引用』顺延 116」在此闭环，且 115 在 `BlueprintAssociationsSection.vue` 里预留的两块位置**无需重构、补上即可**。
Output: 1 个新建 normalizer + 5 处后端纯追加/门控 + 3 处前端（1 处通用件纯追加 + 2 处 0.20 自建件）+ 2 个新建测试文件。**零 migration、零新 `EntityKind`、零新依赖。**
</objective>

<execution_context>
工作目录：worktree 根 `/Users/zaneliu/Projects/open-source/friday-ai/.claude/worktrees/v0.20-blueprint`，分支 `milestone/v0.20.0-blueprint`。⛔ 绝不读写主检出。

**wave 3，依赖 116-02**：功能上不依赖 SC-1，但与 116-02 **共同改 `tests/delivery/test_blueprint_log_redaction_guard.py`**（`_SCANNED_MODULES` 追加点）⇒ 文件重叠即隐式依赖，必须排在 116-02 之后的波次。⚠️ 该守卫 `_violations:69-70` 直接 `read_text()` **不兜 `FileNotFoundError`** ⇒ 本 plan 追加的那一行必须**与 `knowledge/sources/blueprint.py` 的创建在同一个 commit**。

**测试基线**：后端 `cd server && uv run pytest tests/ -q` = **8609 passed / 1 failed**（唯一失败 `test_skills_snapshot_guard::test_skill_files_discovered` 是 worktree 环境产物）；前端 `cd web && pnpm exec vitest run` = **1674 passed / 1 skipped**，`pnpm type-check` exit 0，`pnpm build` 通过，`eslint .` **111 problems**（判据是**本 plan 触及文件零新增问题**，⛔ 不是整体退出码 0）。

**受限面配额（硬约束）**：

| 文件 | 允许的改动 | 删除行上界 |
|---|---|---|
| `server/knowledge/api/views.py` | `get` 内追加 `relations` 解析 + 透传 | **0** |
| `server/knowledge/retrieval.py` | `get_related` 加一个形参 + 透传 | **1**（`fetch_related_entities(...)` 那一行重排） |
| `server/knowledge/models.py` | ⭐ **只改 `generate_entity_id` 的 docstring**（natural key 规则表 +1 行） | **0** |
| `server/knowledge/sources/__init__.py` | `_NORMALIZERS` +1 行（+ 注释） | **0** |
| `server/delivery/services/artifact_service.py` | `create` / `add_version` 各加一段门控 + 一个私有 helper | **0** |
| `server/delivery/api/blueprint_doc_views.py` | payload 追加第 8 键 | **0** |
| `server/tests/delivery/test_blueprint_log_redaction_guard.py` | `_SCANNED_MODULES` +1 行 | **0** |
| `web/src/api/knowledge.ts` | `getRelated` options 加一个可选字段 + 拼 query | **0** |
| `web/src/components/blueprint/BlueprintAssociationsSection.vue` | 补两块 + docstring 收窄段改写 | **8** |
| `web/src/components/blueprint/__tests__/sections.spec.ts` | 一条用例拆两条 | **4** |

⚠️ 签名与字段名一律按下列**实测**取，不得猜测：

- `fetch_related_entities(entity_id, *, user, direction="both", max_hops=None, relations: list[str] | None = None, as_of=None)`（`related.py:25-33`）—— ⭐ **形参已存在**；`rels = relations or list(_DEFAULT_RELATIONS)`（`:47`）；`_DEFAULT_RELATIONS`（`:18-22`）；`space_id is None` 两处短路（`:40-41` / `:79-80`）。
- `DeliveryKnowledgeSearchService.get_related(entity_id, *, user, direction="both", max_hops=2, as_of=None)`（`retrieval.py:135-146`）。
- `KnowledgeRelatedView.get(request, entity_id)`（`knowledge/api/views.py:153-176`）：既有 `direction` 白名单校验 `("both","out","in")` + 400 的**逐字形态**在 `:157-162`；`max_hops` 默认 **2** 在 `:163`。
- `EdgeRelation`（`knowledge/models.py`，六值）；`EntityKind.TECH_PLAN = "tech_plan"`（`:42`）；`generate_entity_id(kind, source_kind, source_id)` = `uuid5(KNOWLEDGE_NAMESPACE, f"{kind}:{source_kind}:{source_id}")`（`:96-137`，docstring `:105-123` 是 natural key 规则表）；`KnowledgeEdge.target_entity` 真 FK（`:305-311`）；`uniq_kedge_active` = `(source_entity, target_entity, relation)`（`:335-339`）。
- `IngestionRequest(source_kind, source_id, trigger)`（`ingestion.py:68-73`）；`IngestionEvent`（`:99-115`，含 `kind` / `origin` / `source_kind` / `source_id` / `title` / `content` / `payload` / `space_id` / `repository_id` / `event_time`（**必须 aware**）/ `edges` / `vectorize`）；`EdgeSpec`（`:76-96`，target XOR 语义 + `exclusive` + `metadata`）；`aschedule_ingestion(request, *, initiated_by_user_id=None)`（`:118`，`on_commit` + 后台、**内部吞异常**）；`apply_edge_specs`（`:356-443`，四条实测行为见 RESEARCH §D.3）。
- `knowledge/sources/workflow_plan.py` **全文 202 行** —— normalizer 的逐字 analog（模块 docstring 体例、`__all__ = ["normalize"]`、源缺失 warning + 返回空列表 `:69-75`、`_aware()` 时区处理、事件装配 `:144-155`、`EdgeSpec` 出边 `:192-200`、⭐ `space_id` 其实存 Space id 的那一行 `:105`）。
- `knowledge/sources/__init__.py:19-42` `_NORMALIZERS` + `:45-53` `get_normalizer`（未知 kind **raise KeyError**）。
- `artifact_service.py`：`create:53-84`、`_create_sync:95-115`、`add_version:117`、`_add_version_sync:145-150`（**hash 相等 return current**）。
- `blueprint_doc_views.py:266-284` 的 payload 装配段（7 键 + `_log`）。
- `blueprint_schema.py:96-107` 九种 `source_type` 枚举；`citations` 是 `{citation_id: Citation}`，Citation 必有 `citation_id` / `source_type`，`source_id` / `locator` / `quote` / `title` 全可选。
- `web/src/api/knowledge.ts:193-202` `getRelated` 现状（`direction` 默认 `'both'`、`max_hops` 默认 **2**）。
- `web/src/components/blueprint/__tests__/sections.spec.ts:586-598` 那条 `toHaveBeenCalledTimes(0)` 用例全文。
- `web/src/components/blueprint/BlueprintAssociationsSection.vue:1-45` 的 docstring 收窄段与 props（`artifactId` / `citations` / `projectId` / `projectName`）。
</execution_context>

<context>
@.planning/phases/116-entry/116-RESEARCH.md（⭐ §0.4 CONTEXT 判断被推翻的一条、§D.1 choke point 与 `create` 例外、§D.2 实体身份与 space 反查的双向不可见、§D.3 `apply_edge_specs` 四条实测行为 + 九种 source_type 换算表 + `first_seen_version_no` 与 `exclusive` 作用域两条、§D.4 丢弃计数、⭐ §D.5 三处纯追加与 `max_hops` 默认 2、P-1 / P-5 / P-6 / P-10、给 PLAN 的第 1、7 条）
@.planning/phases/116-entry/116-PATTERNS.md（§0.2 `_DEFAULT_RELATIONS` 与 `apply_edge_specs` 的冻结登记、§0.4 追加点 23/24/27/28/29/30/32/34/36、D1 normalizer 的「逐条照抄」与四条必须 DIFFER、D3 门控两条、D4 三处纯追加与端到端断言、D6、No Analog #5、⭐ 与 CONTEXT 的口径分歧 #1 / #7 / #8）
@.planning/phases/116-entry/116-CONTEXT.md（知识图谱物化与反查一节；specifics 第 4、5 条）
@server/knowledge/sources/workflow_plan.py（**全文，逐字 analog**）
@server/knowledge/ingestion.py（`:68-116` 三个 dataclass、`:118` `aschedule_ingestion`、`:282-284` 两阶段说明、⭐ `:356-443` `apply_edge_specs` 全文）
@server/knowledge/models.py（`:96-137` `generate_entity_id` 与 natural key 规则表 docstring、`EntityKind` / `EdgeRelation` / `EntityOrigin` 的取值、`:305-311` `target_entity` FK、`:335-339` `uniq_kedge_active`）
@server/knowledge/related.py（**全文**：`_DEFAULT_RELATIONS`、`fetch_related_entities` 签名与两处 `space_id is None` 短路）
@server/knowledge/retrieval.py（`:130-150` `get_related`）
@server/knowledge/api/views.py（`:150-176` `KnowledgeRelatedView.get` 与 `direction` 白名单校验的逐字形态）
@server/delivery/services/artifact_service.py（`:40-160` `create` / `_create_sync` / `add_version` / `_add_version_sync`）
@server/delivery/api/blueprint_doc_views.py（`:255-285` payload 装配与 `_log`）
@server/services/process_runtime/blueprint_schema.py（`:96-107` 九种 source_type、Citation 字段的可选性）
@server/tests/knowledge/test_feishu_document_normalizer.py（normalizer 测试的形状 analog）
@server/tests/knowledge/test_artifact_associations_api.py（反查端点测试的形状 analog）
@web/src/api/knowledge.ts（`:193-202` `getRelated`）
@web/src/components/blueprint/BlueprintAssociationsSection.vue（**全文**：docstring 的收窄段、既有两块的实现与 `citation-click` 契约）
@web/src/components/blueprint/__tests__/sections.spec.ts（`:586-598` 那条用例全文、`mountWith` 与 `knowledgeApiMock` 的组织方式）
@.planning/phases/116-entry/116-02-SUMMARY.md（骨架的 `citations` / `meta.project_id` 形状 —— normalizer 的输入）
</context>

<tasks>

<task type="auto">
  <name>Task 1: ⭐ relations 三层打通（view + service + 前端）+ 端到端反查用例</name>
  <files>server/knowledge/api/views.py, server/knowledge/retrieval.py, web/src/api/knowledge.ts, server/tests/knowledge/test_related_relations_param.py</files>
  <read_first>server/knowledge/api/views.py **L140-185**（`KnowledgeRelatedView` 全文：`permission_classes`、⭐ `:157-162` `direction` 白名单校验与 400 的**逐字形态**（本 Task 的 `relations` 校验逐字同形）、`:163` `_parse_int_param(..., 2, "max_hops")` 的默认 2、service 调用行与响应装配）；server/knowledge/retrieval.py **L120-150**（`get_related` 全文与它的其它调用点 —— 确认加一个带默认值的形参**零回归**）；server/knowledge/related.py **全文**（⭐ `:18-22` `_DEFAULT_RELATIONS`（**只读，⛔ 不改**）、`:25-33` 签名（`relations` 形参**已存在**）、`:40-41` 与 `:79-80` 两处 `space_id is None` 短路、`:47` `rels = relations or list(...)`、BFS 逐跳累积与 `RelatedEntityDTO` 的 `depth` 字段）；server/knowledge/models.py **L96-140 与 L290-345**（`generate_entity_id` 与规则表、`EdgeRelation` 六值的**确切字面量**、`KnowledgeEdge` 字段与 `uniq_kedge_active`）；server/knowledge/graph_store.py（建边/查边的公开面 —— 端到端用例要用它造一条 REFERENCES 边）；server/tests/knowledge/test_artifact_associations_api.py（反查端点测试的形状：如何造实体/边、如何带 user 调端点、`space_id` 与 `resolve_allowed_project_ids` 的可见性前置怎么满足）；web/src/api/knowledge.ts **L185-215**（`getRelated` 与相邻函数的 query 拼接惯例、`withAsOf` helper、`RelatedEntity` 类型）</read_first>
  <action>
**① `server/knowledge/api/views.py` 追加 `?relations=` 解析（删除行 0）。**

在 `direction` 校验**之后**、`max_hops` 解析附近追加，形状**逐字照 `:157-162`**：

```
raw_relations = request.query_params.get("relations") or ""
relations: list[str] | None = None
if raw_relations:
    parts = [p.strip() for p in raw_relations.split(",") if p.strip()]
    allowed = set(EdgeRelation.values)
    invalid = [p for p in parts if p not in allowed]
    if invalid:
        return Response(
            {"detail": f"relations must be a subset of: {', '.join(sorted(allowed))}"},
            status=400,
        )
    relations = parts
```
⭐ **不传时 `relations` 保持 `None`** ⇒ 下游 `rels = relations or list(_DEFAULT_RELATIONS)` 行为**逐字不变**。透传进 service 调用。⚠️ `EdgeRelation` 若非本文件已 import，加一行 import。

**② `server/knowledge/retrieval.py` 的 `get_related` 加形参并透传（删除行 ≤1）。**

```
async def get_related(self, entity_id, *, user, direction="both", max_hops=2, relations=None, as_of=None):
    return await fetch_related_entities(
        entity_id, user=user, direction=direction, max_hops=max_hops, relations=relations, as_of=as_of
    )
```
⭐ 默认 `None` ⇒ 既有调用点零回归（本文件其它调用点一行不改）。

**③ `web/src/api/knowledge.ts` 的 `getRelated` 加可选参数（删除行 0）。**

```
export async function getRelated(
  id: string,
  options?: { asOf?: string | null, direction?: string, maxHops?: number, relations?: string[] },
) {
  return get<RelatedEntity[]>(`/knowledge/related/${id}/`, {
    ...withAsOf({}, options?.asOf),
    direction: options?.direction ?? 'both',
    max_hops: options?.maxHops ?? 2,
    relations: options?.relations?.length ? options.relations.join(',') : undefined,
  })
}
```
⭐ **不传时不拼该 query 键**（`undefined` ⇒ 既有两个调用面零行为变化）。⛔ **不改 `maxHops` 的默认 2**（那是既有面的默认；「被谁引用」由调用方显式传 1）。

**④ 新建 `server/tests/knowledge/test_related_relations_param.py`**，docstring「守五件事」：

1. ⭐ **端到端反查（本 Task 的头号靶子，P-1）**：造两个同 space 的 `KnowledgeEntity`（A = 蓝图实体、B = 被引的 knowledge_entity）+ 一条 `A -REFERENCES-> B` 的活跃边 ⇒ 以有权限的 user 调 `GET /api/knowledge/related/<B.id>/?direction=in&relations=REFERENCES&max_hops=1` ⇒ **返回体里含 A**。⛔ **不接受止于 `KnowledgeEdge.objects.filter(...).exists()` 的断言**。
2. ⭐ **反向对照（证明第 1 条非恒真）**：**不传** `relations` 调同一个 URL ⇒ **返回空**（因为 `_DEFAULT_RELATIONS` 不含 `REFERENCES`）。这一条同时锁死「⛔ 不许有人顺手把 `REFERENCES` 加进 `_DEFAULT_RELATIONS`」——加了这条就转红。
3. **非法 relation → 400**：`?relations=NOT_A_RELATION` ⇒ 400 且 `detail` 提示合法集合；`?relations=REFERENCES,BOGUS` ⇒ 同样 400（部分非法也拒）。
4. **不传 `relations` 时行为与改动前逐字一致**：造 `HAS_PLAN` 边 ⇒ 不传 `relations` 仍能查到（既有三条默认关系不受影响）。
5. **`max_hops=1` 的效果**：造 A→B→C 三级 REFERENCES 链 ⇒ 从 C `?direction=in&relations=REFERENCES&max_hops=1` 只返回 B，`max_hops=2` 才返回 A + B（证明前端必须显式传 1）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/knowledge/test_related_relations_param.py -x -q && uv run pytest tests/knowledge/ -q</automated>
  </verify>
  <acceptance_criteria>
- `cd server && uv run pytest tests/knowledge/test_related_relations_param.py -q` 全绿；`rg -c "def test_" server/tests/knowledge/test_related_relations_param.py` ≥ **6**。
- ⭐ **端到端而不是止于 DB**：`cd server && uv run python -c "
src = open('tests/knowledge/test_related_relations_param.py').read()
assert 'direction=in' in src and 'relations=REFERENCES' in src, '缺端到端 query'
assert 'max_hops=1' in src, '缺 max_hops=1'
assert '/api/knowledge/related/' in src or 'knowledge-related' in src, '必须走真实端点'
print('end-to-end reverse lookup OK')"` 输出 `end-to-end reverse lookup OK`。
- ⭐ **`_DEFAULT_RELATIONS` 未被改**：`git diff server/knowledge/related.py` **为空**；且用例 2（不传 relations ⇒ 空）绿 —— 有人把 `REFERENCES` 加进默认集时它会转红。
- **三处纯追加**：`git diff server/knowledge/api/views.py | rg -c "^-[^-]"` == **0**；`git diff server/knowledge/retrieval.py | rg -c "^-[^-]"` ≤ **1**；`git diff web/src/api/knowledge.ts | rg -c "^-[^-]"` == **0**。
- **白名单校验与 `direction` 同形**：`rg -n "EdgeRelation" server/knowledge/api/views.py` 命中；`rg -n "status=400|HTTP_400_BAD_REQUEST" server/knowledge/api/views.py` 命中 ≥2（`direction` + `relations`）。
- **既有调用点零回归**：`cd server && uv run pytest tests/knowledge/ -q` 全绿；`cd web && pnpm exec vitest run src/api` 全绿（若该目录有用例）。
- **前端不传时不拼键**：`cd web && node -e "
const s = require('fs').readFileSync('src/api/knowledge.ts','utf8');
const i = s.indexOf('export async function getRelated');
const seg = s.slice(i, i + 700);
if (!/relations:/.test(seg)) throw new Error('缺 relations 拼接');
if (!/undefined/.test(seg)) throw new Error('不传时必须为 undefined（不拼该 query 键）');
console.log('frontend getRelated OK');
"` 输出 `frontend getRelated OK`。
- `cd server && uv run ruff check knowledge/api/views.py knowledge/retrieval.py tests/knowledge/test_related_relations_param.py` 通过；`cd web && pnpm exec eslint src/api/knowledge.ts` **零新增问题**（与基线逐条比对）。⚠️ 跑完 `git status` 并 `git checkout -- web/pnpm-workspace.yaml`（P-15）。
  </acceptance_criteria>
  <done>`relations` 三层打通且不传时行为逐字不变；反查有**端到端**用例（造边 → 被引方 `?direction=in&relations=REFERENCES&max_hops=1` → 查回引用方）并配了「不传 relations ⇒ 空」的反向对照——它同时锁死「⛔ 不许把 REFERENCES 加进 `_DEFAULT_RELATIONS`」；非法 relation 400、`max_hops` 一/二跳差异各有用例。</done>
</task>

<task type="auto">
  <name>Task 2: knowledge/sources/blueprint.py normalizer + 注册 + artifact_service 两处门控 + 脱敏守卫清单追加</name>
  <files>server/knowledge/sources/blueprint.py, server/knowledge/sources/__init__.py, server/knowledge/models.py, server/delivery/services/artifact_service.py, server/tests/delivery/test_blueprint_log_redaction_guard.py, server/tests/knowledge/test_blueprint_normalizer.py</files>
  <read_first>server/knowledge/sources/workflow_plan.py **全文 202 行**（⭐ 逐字 analog：`:1-17` 模块 docstring 写清「产几个实体几条边 + 缺料如何降级 + 事件顺序」的体例、`__all__ = ["normalize"]`、`_parse_source_id` 的畸形输入防御、`_aware()` 时区补齐、`:69-75` 源缺失 warning + **返回空列表**、`:105` ⭐ `space_id` 其实存 Space id 的那一行与其变量命名噪声、`:144-155` `IngestionEvent` 装配、`:192-200` `EdgeSpec(relation=..., target_entity_id=generate_entity_id(...), exclusive=True)`）；server/knowledge/ingestion.py **L60-130 与 L270-300 与 L350-450**（三个 dataclass 全字段与 `EdgeSpec` 的 target XOR 语义 + `metadata` 说明；`aschedule_ingestion` 的 `on_commit` + **内部吞异常**；`:282-284` 「阶段 A 全部实体持久化 → 阶段 B 统一处理边」的两阶段说明（⭐ **只保证同一批 events 内两端都在**，citation 指向的实体来自别的批次 ⇒ 存在性过滤不可省）；⭐ **`apply_edge_specs` 全文 `:356-443`**：`:377-386` target XOR 不满足 warning 跳过 / `:412-422` 已有活跃边时 `update_edge_metadata` **整体覆盖** / `:423-426` `exclusive` 逐条 `invalidate_edge` / **`:435-443` `IntegrityError` 被吞成 `knowledge_ingest_edge_conflict`**）；server/knowledge/models.py **L90-145 与 L290-345**（`generate_entity_id` 全文 + ⭐ **`:105-123` natural key 规则表 docstring**（本 Task 追加一行）、`EntityKind` / `EntityOrigin` / `EdgeRelation` 的**确切成员**、`KnowledgeEdge` 的 `target_entity` FK 与 `uniq_kedge_active`）；server/delivery/services/artifact_service.py **L40-165**（`create` / `_create_sync` / `add_version` / ⭐ `_add_version_sync:145-150` 的 hash 短路）；server/services/process_runtime/blueprint_schema.py **L90-170**（⭐ 九种 `source_type` 枚举的**确切字面量**、`Citation` 的必填 `citation_id`/`source_type` 与全可选的 `source_id`/`locator`/`quote`/`title`、`citations` 是 `{citation_id: Citation}` 的形状、`meta.project_id`）；server/initiatives/models/project.py **L40-60**（`Project.space` FK 与 `space_id`）；server/tests/knowledge/test_feishu_document_normalizer.py（normalizer 测试形状：如何直接调 `normalize(IngestionRequest(...))` 并断言事件与边）；server/tests/delivery/test_blueprint_log_redaction_guard.py **L27-44 与 L69-80**</read_first>
  <action>
**① 新建 `server/knowledge/sources/blueprint.py`。**

模块 docstring（⭐ **四条结构约束逐字写进去** —— PATTERNS D1 的硬要求）：

- 产出：**一个** `tech_plan` 实体（`source_kind="blueprint"`）+ **两类边**（`citations → REFERENCES` append-only / `meta.project_id → RELATES_TO` exclusive）。
- ⭐ 约束 ①：**目标实体不存在的 spec 先过滤**——`KnowledgeEdge.target_entity` 是真 FK，目标不存在 → `IntegrityError` → 被 `apply_edge_specs:435-443` 吞成 warning，边**静默消失**；且该分支与「撞 `uniq_kedge_active`（并发已建，良性）」共用同一个 except，日志分不出来。
- ⭐ 约束 ②：**同目标多条 citation 聚合成一条 `EdgeSpec`**——`uniq_kedge_active` 是 `(source, target, relation)` 唯一。
- ⭐ 约束 ③：**`RELATES_TO` 出边恰好 1 条**——`exclusive=True` 的作用域是 `(source, relation)` **不含目标类型**，多条会互相 `invalidate_edge` 且走**正常路径**（完全静默）。将来若想加「蓝图 → 仓库」的 `RELATES_TO`，必须先解决这条。
- ⭐ 约束 ④：**⛔ 不放 `first_seen_version_no`**——重摄取时 `update_edge_metadata` **整体覆盖**，normalizer 拿不到既有边 metadata ⇒ 该字段每次被刷成当前版本号，字段名与语义对不上；信息量已由 `valid_at` / `created_at` 承载。
- 降级：`artifact` / 当前版本 / `meta.project_id` / `Project.space_id` 任一取不到 ⇒ **warning + 返回空列表**（⛔ 不产半截事件）。⭐ 特别写明 space 反查失败为何是「整体不入图」：`fetch_related_entities` 有**两处** `space_id is None` 短路（`:40-41` / `:79-80`）⇒ space 为空的实体是**双向不可见**的孤儿节点。

`__all__ = ["normalize"]`；`async def normalize(request: IngestionRequest) -> list[IngestionEvent]`。

实现：
1. `artifact_id = request.source_id`；取 `Artifact` + 最新版本（`order_by("-version_no").afirst()`）；缺任一 ⇒ `logger.warning("knowledge_normalize_source_missing", ...)` + `return []`。
2. `content` 非 dict 或 `content.get("schema_version") != BLUEPRINT_SCHEMA_VERSION`（懒 import）⇒ warning + `return []`（⛔ 不给 v0 content 建蓝图实体）。
3. `project_id = content["meta"]["project_id"]`；`space_id = await Project.objects.filter(id=project_id).values_list("space_id", flat=True).afirst()`；⭐ 取不到 ⇒ warning `knowledge_normalize_blueprint_space_unresolved` + `return []`。
4. `entity_id = generate_entity_id("tech_plan", "blueprint", str(artifact_id))`（**唯一入口**）。
5. `content_text` = 供 embedding 的提炼文本（形状 Discretion；建议 `meta.title` + `requirement_spec.goal` 扁平文本 + 六段的 heading 与要点，⭐ **块取文本按 `text` → `code.source` → `rows` 的字段优先级，⛔ 不看 `block.type`** —— 与 `blueprint_anchor._block_text` 同源，否则与锚点坐标系分叉）。
6. `payload` = 结构化快照（建议 `{artifact_id, version_no, current_status, project_id, citation_count, ...}`；⛔ **不塞整份 content**）。
7. 边装配（新写两个私有函数）：
   - `_resolve_target_entity_id(citation) -> uuid | None` —— 九种 `source_type` 的换算（RESEARCH §D.3 表逐行）：`knowledge_entity`（`source_id` 本身，需能解析成 UUID）/ `work_item`（从 `source_id` 或 `locator` 还原 `{project_key}:{type_key}:{item_id}` 三元组 → `generate_entity_id("work_item","feishu_work_item", 三元组)`）/ `feishu_doc`（doc token → `generate_entity_id("document","feishu_document", token)`）/ `blueprint`（→ 同款 natural key）/ `artifact_version`（一次 `ArtifactVersion → artifact_id` 查询后换算成蓝图实体 id）/ `repo_file`·`rag_chunk`·`repo_charter`（统一 `generate_entity_id("repository","repository", repo_id)`，repo_id 从 `locator` 取，取不到即丢弃）/ `url`（⛔ **返回 None 不成边**）。**还原不出即返回 None**。
   - `_build_reference_edges(entity_id, citations) -> tuple[list[EdgeSpec], dict]` —— 先逐条换算目标 → ⭐ **按 target 分组聚合**（同目标的多条 citation 合成一个 `EdgeSpec`，`metadata = {"source": "blueprint", "citation_ids": [...], "source_types": [...]}`，两个列表**排序后写入**以保证幂等）→ ⭐ **一次批量存在性过滤** `KnowledgeEntity.objects.filter(id__in=[...]).values_list("id", flat=True)` → 不存在的丢弃并按 `source_type` 计数 → 返回 `(specs, {"dropped_count": n, "dropped_by_source_type": {...}, "kept_count": m})`。
   - 项目边：`EdgeSpec(relation=EdgeRelation.RELATES_TO, target_entity_id=<项目节点 entity id>, exclusive=True)` —— ⚠️ **项目节点的 entity id 按仓内既有惯例取**（执行期实读 `knowledge/artifact_associations.py` 或既有 normalizer 里项目节点的 `generate_entity_id` 用法，⛔ 不猜）；⭐ **目标不存在时同样过滤掉**（项目节点可能尚未入图）。
8. ⭐ 丢弃计数落一条 `category="sampling"` / `component="knowledge"` 的事件（`dropped_count` / `dropped_by_source_type` / `kept_count` / `artifact_id`）。
9. 返回 `[IngestionEvent(kind=EntityKind.TECH_PLAN, origin=<既有取值>, source_kind="blueprint", source_id=str(artifact_id), title=..., content=content_text, payload=payload, space_id=str(space_id), repository_id=None, event_time=<aware 的版本 created_at>, edges=tuple(specs))]`。

**② `knowledge/sources/__init__._NORMALIZERS` 追加一行**（删除行 0）：`"blueprint": "knowledge.sources.blueprint",` + 一行注释（「Phase 116 VIEW-04：蓝图 citations/项目关联物化」）。⭐ **与模块创建同 commit**（`get_normalizer` 未知 kind **raise KeyError**）。

**③ `knowledge/models.py` 的 `generate_entity_id` docstring natural key 规则表追加一行**（删除行 0，⛔ **只改 docstring，零字段变更**）：`tech_plan` / `blueprint` / `delivery.Artifact.id`。

**④ `delivery/services/artifact_service.py` 两处门控**（删除行 0）。

新增私有 helper（放模块级）：

```
async def _amaybe_schedule_blueprint_ingestion(artifact_id: Any, content: dict, *, trigger: str) -> None:
    """content 是 blueprint/v1 时投递知识图谱摄取（Phase 116 VIEW-04）。

    ⛔ 不包 try：``aschedule_ingestion`` 内部已吞异常（``ingestion.py:118``），
    重复兜底会掩盖 normalizer 注册漏行这类**应当响亮**的错误。
    判别常量懒 import 自 schema 模块（MN-10：⛔ 不复制 "blueprint/v1" 字面量）。
    """
```
- `create` 的 `return artifact` 之前调它（`trigger="blueprint_version_created"`）。
- `add_version` 的 async 外层：⭐ **先记下调用前的 `artifact.current_version_id`，拿到返回 version 后比对**——`id` 相同（或 `version_no` 未增）⇒ 说明 `content_hash` 相等、**没有翻版本** ⇒ **不投递**；不同才投递。
- ⚠️ 两处都在**事务提交之后**语义上安全（`aschedule_ingestion` 内部走 `on_commit`）。

**⑤ `tests/delivery/test_blueprint_log_redaction_guard._SCANNED_MODULES` 追加 1 行** `"knowledge/sources/blueprint.py"`（删除行 0，⭐ **与模块创建同 commit**）。

**⑥ 新建 `server/tests/knowledge/test_blueprint_normalizer.py`**，docstring「守十件事」：

1. ⭐ **目标不存在 ⇒ 不产边 + 一条记了 `source_type` 的 `sampling` 事件**（P-1 的近亲）：造一条指向不存在 `knowledge_entity` 的 citation ⇒ 活跃边 **0** 条，且捕获到 `dropped_by_source_type == {"knowledge_entity": 1}`。⛔ **不接受静默 warning**。
2. ⭐ **同目标两条 citation 聚合成一条边**：两条都指向同一个已存在的 `knowledge_entity` ⇒ 经完整 `ingest` 后**活跃边恰好 1 条**且 `metadata["citation_ids"]` 有 **2** 项。（⭐ 朴素写法会在第二条撞 `uniq_kedge_active` 并被吞成 warning ⇒ 这条用例是唯一能逮住它的形态。）
3. ⭐ **`RELATES_TO` 出边恰好 1 条**（结构断言，P-6）：直接断言 `normalize()` 返回的 `event.edges` 里 `relation == RELATES_TO` 的**恰好 1 个**。
4. ⭐ **⛔ 无 `first_seen_version_no`**（P-5）：断言任一 `REFERENCES` 边的 `metadata` 里**不含**该键。
5. ⭐ **space 反查不到 ⇒ 不产事件 + warning**：`meta.project_id` 指向不存在的 Project（或该 Project 无 space）⇒ `normalize()` 返回 `[]` 且有一条 warning。
6. **v0 content 不入图**：`schema_version` 缺失或非 `blueprint/v1` ⇒ 返回 `[]`。
7. ⭐ **九种 `source_type` 各一条**（参数化）：四种直连各断言换算出的 entity id 与 `generate_entity_id(...)` 一致；三种落仓库节点断言 `metadata` 里能区分具体来源；`url` 断言**不成边**；还原不出三元组的 `work_item` 断言被丢弃并计数。
8. ⭐ **v1 骨架经 `create` 也入图**（P-10）：经 `ArtifactService.create` 落一份 `blueprint/v1` ⇒ `aschedule_ingestion` 被调用一次（patch 断言）。**反向对照**：落一份 v0 content ⇒ **零调用**。
9. ⭐ **`add_version` 无变化不投递**：同一 content 连写两次 ⇒ `aschedule_ingestion` 只被调**一次**（第二次 hash 相等、版本未翻）。
10. **实体身份**：`generate_entity_id("tech_plan", "blueprint", artifact_id)` 与 normalizer 产出的实体 id 逐字相等；`kind == EntityKind.TECH_PLAN`；⛔ 断言未新建 `EntityKind`（`EntityKind` 成员数与改动前相同）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/knowledge/test_blueprint_normalizer.py -x -q && uv run pytest tests/knowledge/ tests/delivery/test_blueprint_log_redaction_guard.py -q && uv run python manage.py makemigrations --check --dry-run</automated>
  </verify>
  <acceptance_criteria>
- `cd server && uv run pytest tests/knowledge/test_blueprint_normalizer.py -q` 全绿；`rg -c "def test_" server/tests/knowledge/test_blueprint_normalizer.py` ≥ **14**。
- ⭐ **四条结构约束都写进了模块 docstring**：`cd server && uv run python -c "
src = open('knowledge/sources/blueprint.py').read()
doc = src[:src.index('from __future__')] if 'from __future__' in src else src[:3000]
for token in ('uniq_kedge_active', 'IntegrityError', 'exclusive', 'first_seen_version_no'):
    assert token in doc, ('模块 docstring 缺约束说明：', token)
print('normalizer docstring constraints OK')"` 输出 `normalizer docstring constraints OK`。
- ⭐ **存在性预过滤真的存在**：`rg -n "KnowledgeEntity.objects.filter" server/knowledge/sources/blueprint.py` 命中；`rg -n "id__in" server/knowledge/sources/blueprint.py` 命中（一次批量查，⛔ 不逐条）。
- ⭐ **丢弃计数是 sampling 事件**：`rg -n "dropped_by_source_type" server/knowledge/sources/blueprint.py` 命中；`rg -n 'category="sampling"' server/knowledge/sources/blueprint.py` 命中。
- ⭐ **⛔ 无 `first_seen_version_no`**：`rg -n "first_seen_version_no" server/knowledge/sources/blueprint.py` **零命中**。
- ⭐ **⛔ 不裸调 graph_store、不改 apply_edge_specs**：`rg -n "graph_store" server/knowledge/sources/blueprint.py` **零命中**；`git diff server/knowledge/ingestion.py` **为空**；`git diff server/knowledge/related.py` **为空**。
- ⭐ **实体 id 经唯一入口**：`rg -n "uuid5|KNOWLEDGE_NAMESPACE" server/knowledge/sources/blueprint.py` **零命中**；`rg -n "generate_entity_id" server/knowledge/sources/blueprint.py` 命中 ≥2。
- ⭐ **零新 `EntityKind` / 零 migration**：`git diff server/knowledge/models.py | rg "^\+" | rg -v "^\+\+\+" | rg -v "^\+\s*(#|\"|')"` **为空**（只改 docstring）；`cd server && uv run python manage.py makemigrations --check --dry-run` 退出码 **0**；`git status --porcelain server/knowledge/migrations/ server/delivery/migrations/` **为空**。
- ⭐ **两处门控 + 翻版本判断**：`rg -c "aschedule_ingestion" server/delivery/services/artifact_service.py` ≥ **1**（经共享 helper）；`cd server && uv run python -c "
src = open('delivery/services/artifact_service.py').read()
assert '_amaybe_schedule_blueprint_ingestion' in src
i = src.index('async def add_version')
body = src[i:i+2500]
assert 'current_version_id' in body or 'version_no' in body, 'add_version 侧必须判断是否真的翻了版本'
print('gating OK')"` 输出 `gating OK`；且 `git diff server/delivery/services/artifact_service.py | rg -c "^-[^-]"` == **0**。
- ⭐ **注册与模块同 commit**：`rg -n '"blueprint": "knowledge.sources.blueprint"' server/knowledge/sources/__init__.py` 命中；`cd server && uv run python -c "
from knowledge.sources import get_normalizer
assert get_normalizer('blueprint') is not None
print('normalizer registered OK')"` 输出 `normalizer registered OK`。
- ⭐ **脱敏守卫**：`cd server && uv run pytest tests/delivery/test_blueprint_log_redaction_guard.py -q` 全绿；`rg -c "knowledge/sources/blueprint.py" server/tests/delivery/test_blueprint_log_redaction_guard.py` == **1**；`git diff server/tests/delivery/test_blueprint_log_redaction_guard.py | rg "^-[^-]"` **为空**。
- ⭐ **块取文本口径同源**：`cd server && uv run python -c "
src = open('knowledge/sources/blueprint.py').read()
import re
i = src.find('def _block_text') if 'def _block_text' in src else src.find('content_text')
seg = src[max(0,i-200):i+1500]
assert '.get(\"type\")' not in seg and \"get('type')\" not in seg, '⛔ 块取文本不得按 block.type 分派（与锚点坐标系分叉）'
print('block text priority OK')"` 输出 `block text priority OK`。
- `cd server && uv run ruff check knowledge/sources/blueprint.py knowledge/sources/__init__.py knowledge/models.py delivery/services/artifact_service.py tests/knowledge/test_blueprint_normalizer.py` 通过；`uv run ruff format --check knowledge/sources/blueprint.py tests/knowledge/test_blueprint_normalizer.py` 通过。
  </acceptance_criteria>
  <done>normalizer 就位且四条结构约束（存在性预过滤 + 丢弃计数 / 同目标聚合成一条 / `RELATES_TO` 恰好 1 条 / ⛔ 无 `first_seen_version_no`）各写进模块 docstring 并各有一条断言；space 反查不到即整体不入图；九种 `source_type` 换算各有用例含「url 不成边」与「还原不出即丢弃」；门控挂在 `create` **与** `add_version` 两处且后者判断是否真的翻了版本；注册行与守卫清单均与模块创建同 commit；零 migration、零新 `EntityKind`。</done>
</task>

<task type="auto">
  <name>Task 3: knowledge_entity_id 换算键 + 前端关联段补两块 + sections.spec.ts 拆两条 + 前后端双门</name>
  <files>server/delivery/api/blueprint_doc_views.py, web/src/components/blueprint/BlueprintAssociationsSection.vue, web/src/components/blueprint/__tests__/sections.spec.ts</files>
  <read_first>server/delivery/api/blueprint_doc_views.py **L230-300**（`BlueprintDocumentView.get` 全文：前置三步、版本取法、`quality` 装配、⭐ **`:266-274` 的 7 键 payload 装配**与 `_log` 的字段（本 Task 追加第 8 键，7 键一个不动））；web/src/components/blueprint/BlueprintAssociationsSection.vue **全文**（⭐ `:1-24` docstring 的**收窄段**（本 Task 改写它）、props 定义、既有两块「本蓝图引用了」（`citations` 按 `source_type` 分组）与「关联项目」的实现、`citation-click` emit 契约、`CompactEmptyState` 与 `Badge` 的用法、`data-testid` 命名惯例）；web/src/components/blueprint/__tests__/sections.spec.ts **L580-620**（⭐ 那条 `9a.` 用例全文与它断言的**两个** mock、`mountWith` 的签名、`knowledgeApiMock` 的定义位置与形状）；web/src/api/knowledge.ts **L185-215**（Task 1 改造后的 `getRelated` 与 `RelatedEntity` 类型）；web/src/composables/useBlueprintLive.ts **L1-30**（⭐ **只读**：`refetchInterval` 全相位唯一允许出现的位置 —— 本 Task 的两块**不得**出现该字面量）；web/src/components/blueprint/BlueprintCitationChip.vue（既有 chip 的 props —— 新两块的条目呈现尽量复用它或同款卡片形态）；web/src/pages/knowledge/blueprints/[id].vue **L1-60**（⭐ **只读**：确认 `BlueprintAssociationsSection` 的 props 传参处 —— 若新增 prop 需要页面传，本 Task 才允许改页面并**必须在 SUMMARY 显式登记这处额外的前端改动及其理由**；能从既有 props 推导则 ⛔ 不改页面）</read_first>
  <action>
**① `blueprint_doc_views.py` 的 payload 追加第 8 键（删除行 0）。**

在既有 7 键之后追加：

```
"knowledge_entity_id": str(generate_entity_id("tech_plan", "blueprint", str(artifact_id))),
```
- ⭐ **懒 import `generate_entity_id`**（`from knowledge.models import generate_entity_id`，放函数内或模块级按本文件既有惯例），⛔ **绝不让前端复制 uuid5 规则**（`generate_entity_id` docstring 明令它是唯一入口）。
- 模块 docstring 的端点清单表里给该键加一行说明（「SC-4 反查用：`GET /api/knowledge/related/<它>/?direction=in&relations=REFERENCES&max_hops=1`」）。
- `_log` 不新增字段（该键是确定性派生值，无观测价值）。

**② `BlueprintAssociationsSection.vue` 补两块。**

- ⭐ **docstring 的收窄段改写**：把「本相位显式范围收窄：只兑现两块」整段替换为「**Phase 116 已交付反向反查**」，并保留证据链的**结论部分**：`getArtifactAssociations` 仍**必然落空**（它查 `initiatives.Artifact` 投影，`knowledge/artifact_associations.py:75`），⛔ 因此**仍然一次都不调它**；反查改走 `getRelated` + 116 物化的 REFERENCES 边。
- 新增 prop `knowledgeEntityId?: string | null`（从 `GET blueprint/` 的第 8 键来）。⭐ **优先让页面传**（它已持有 doc 响应）；若这需要改 `pages/knowledge/blueprints/[id].vue`，**允许改但必须在 SUMMARY 显式登记这处额外的前端改动及其理由**（0.20 自建页，删除行应为 0）。
- **块 A「被哪些方案 / 知识引用」**：`useQuery({ queryKey: ['blueprint','related-in',artifactId], queryFn: () => knowledgeApi.getRelated(knowledgeEntityId, { direction: 'in', relations: ['REFERENCES'], maxHops: 1 }), enabled: !!knowledgeEntityId })`。⭐ **`maxHops: 1` 必传**（默认 2 会把二跳实体列进来）。空态走 `CompactEmptyState`；失败**不进错误分档**（照 115 的「关联面失败只降级不报错」口径，⛔ 不弹 toast）。
- **块 B「关联知识」**：同款，`direction: 'out'`（本蓝图**指向**的图谱邻居 —— 与既有的「本蓝图引用了」（纯前端 `citations` 聚合，零端点）**互补而非重复**：前者是**已物化成边**的、后者是**引用池原样**。⭐ 两块的标题文案必须让用户分得清，i18n 键新增走既有子树。
- ⛔ **`refetchInterval` 零命中**（源码守卫锁死）；⛔ 零颜色字面量；⛔ 禁 `v-html`；条目呈现复用既有 chip / 卡片形态与 `data-testid` 命名惯例。
- ⚠️ **i18n 缺键处理**：本相位无 115-02 那样的集中 i18n plan ⇒ 新增键直接写进 `web/src/locales/zh-CN.json`（⭐ **纯追加，删除行 0**；这是本 plan 第 4 处前端改动，**在 SUMMARY 与 `files_modified` 里显式登记**）。⛔ 不留英文 token 降级。

**③ `sections.spec.ts` 那条用例拆两条（删除行 ≤4）。**

- **9a-1**（新）：`getRelated` **被真实调用**，且入参逐字断言 —— 第一个实参是 `knowledgeEntityId`、options 含 `direction: 'in'`、`relations: ['REFERENCES']`、`maxHops: 1`。⭐ 再加一条 `direction: 'out'` 的调用断言（块 B）。
- **9a-2**（保留原判据）：`getArtifactAssociations` **仍 `toHaveBeenCalledTimes(0)`**，并把「为什么仍必须为 0」的理由写进用例名或注释（它查 `initiatives.Artifact` 投影，对 `delivery.Artifact` id 必然落空）。⭐ **⛔ 不许把这条一起翻转**。
- 补一条：`knowledgeEntityId` 为空 ⇒ 两块都不发请求（`enabled` 生效），`getRelated` **零调用**（证明 `enabled` 不是摆设）。

**④ 前后端双门**（本 Task 收尾执行并登记进 SUMMARY）：
- 后端：`cd server && uv run pytest tests/ -q` 与基线 **8609 passed / 1 failed** 比对；`makemigrations --check --dry-run` 退出码 0。
- 前端：`cd web && pnpm exec vitest run`（基线 **1674 passed / 1 skipped**）、`pnpm type-check`（exit 0）、`pnpm build`（通过）、`pnpm exec eslint <本 plan 触及的前端文件>`（**零新增问题**，与 111 个既有问题逐条比对）。
- ⚠️ **P-15**：跑完 `pnpm` 后 `git status` 检查并 `git checkout -- web/pnpm-workspace.yaml`。
- ⚠️ **`pnpm build` 会重写 `web/src/components.d.ts` 并裁掉无关既有条目**（STATE 115-06 环境项）：`git checkout` 还原后**按字典序手工插入自己那几行**（若本 plan 未新增组件文件，该文件应保持零变更）。
  </action>
  <verify>
    <automated>cd web && pnpm exec vitest run src/components/blueprint/__tests__/sections.spec.ts && cd ../server && uv run pytest tests/delivery/test_blueprint_doc_views.py -q</automated>
  </verify>
  <acceptance_criteria>
- `cd web && pnpm exec vitest run src/components/blueprint/__tests__/sections.spec.ts` 全绿；`cd server && uv run pytest tests/delivery/test_blueprint_doc_views.py -q` 全绿。
- ⭐ **第 8 键纯追加且经唯一入口**：`cd server && uv run python -c "
src = open('delivery/api/blueprint_doc_views.py').read()
assert 'knowledge_entity_id' in src
assert 'generate_entity_id' in src
assert 'uuid5' not in src, '⛔ 不得复制 uuid5 规则'
print('entity id key OK')"` 输出 `entity id key OK`；`git diff server/delivery/api/blueprint_doc_views.py | rg -c "^-[^-]"` == **0**；且既有 7 键在 `git diff` 中**无删除行**。
- ⭐ **用例拆两条而不是翻转**：`cd web && node -e "
const s = require('fs').readFileSync('src/components/blueprint/__tests__/sections.spec.ts','utf8');
if (!/getArtifactAssociations\).toHaveBeenCalledTimes\(0\)/.test(s)) throw new Error('⛔ getArtifactAssociations 的 0 调用断言被删了');
if (!/relations: \[.REFERENCES.\]/.test(s)) throw new Error('缺 relations 入参断言');
if (!/maxHops: 1/.test(s)) throw new Error('缺 maxHops: 1 断言');
if (!/direction: .in./.test(s) || !/direction: .out./.test(s)) throw new Error('缺 in/out 两块的调用断言');
console.log('spec split OK');
"` 输出 `spec split OK`。
- ⭐ **⛔ refetchInterval 零命中**：`rg -n "refetchInterval" web/src/components/blueprint/BlueprintAssociationsSection.vue` **零命中**；`cd web && pnpm exec vitest run src/__tests__/blueprint-source-guard.spec.ts` 全绿。
- ⭐ **docstring 收窄段已改写**：`rg -n "本相位显式范围收窄|顺延 Phase 116|必然 404" web/src/components/blueprint/BlueprintAssociationsSection.vue` 的命中**只允许**落在「`getArtifactAssociations` 仍必然落空」那一条结论上（逐条人工核对并写进 SUMMARY）。
- ⭐ **两块的空态与失败都不报错**：`rg -n "toast|ApiError" web/src/components/blueprint/BlueprintAssociationsSection.vue` **零命中**（关联面失败只降级）。
- **i18n 零降级**：`rg -n "associations\." web/src/locales/zh-CN.json` 新键命中；`git diff web/src/locales/zh-CN.json | rg -c "^-[^-]"` == **0**。
- ⭐ **前端门**：`cd web && pnpm exec vitest run` 相对基线 **1674 passed / 1 skipped** 无新增失败；`pnpm type-check` 退出码 **0**；`pnpm build` 通过；`pnpm exec eslint src/components/blueprint/BlueprintAssociationsSection.vue src/components/blueprint/__tests__/sections.spec.ts src/api/knowledge.ts src/locales/zh-CN.json` **零新增问题**（与基线逐条比对写进 SUMMARY）。
- ⚠️ **环境项核算**：`git status --porcelain web/pnpm-workspace.yaml web/src/components.d.ts` 输出**为空**（P-15 + STATE 115-06 环境项，跑完门后已还原）。
- ⭐ **后端门**：`cd server && uv run pytest tests/ -q` 相对基线（8609/1）**无新增失败**；`makemigrations --check --dry-run` 退出码 0。
- ⭐ **0.19 归属面零改动**：`git diff web/src/components/chat/TechPlanCard.vue web/src/components/chat/RoutingDecisionPanel.vue web/src/components/execution/NodeDataTab.vue web/src/components/delivery/ArtifactTimeline.vue` **全部为空**；`git diff web/src/pages/knowledge/entities/` **为空**。
- 相位边界自检：`git diff --name-only` 只含本 plan 声明的文件（+ 若确需则含 `web/src/locales/zh-CN.json` 与 `web/src/pages/knowledge/blueprints/[id].vue`，**两者都必须在 SUMMARY 显式登记理由**）。
  </acceptance_criteria>
  <done>`GET .../blueprint/` 纯追加第 8 键且经 `generate_entity_id` 唯一入口；前端关联段补上「被哪些方案/知识引用」与「关联知识」两块并显式传 `maxHops: 1`、`relations: ['REFERENCES']`；⭐ `sections.spec.ts` 那条用例**拆成两条**（`getRelated` 转真实入参断言、`getArtifactAssociations` 的 0 调用断言原样保留）；`refetchInterval` 零命中、i18n 零降级、前后端双门相对基线无新增失败、0.19 归属面与 pnpm 环境项均已核算。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `?relations=` query（半可信） → 图谱遍历 | 未校验的 relation 会让遍历面不可控；校验形态必须与既有 `direction` 同源 |
| `meta.project_id` → `space_id` → 图谱可见性 | `fetch_related_entities` 按 `space_id` 做可见性判定；空 space 的实体双向不可见 |
| citations（半可信，含 LLM 产出）→ 图谱边 | 目标 id 由 citation 的 `source_id` / `locator` 还原，还原不出即丢弃 |
| 蓝图正文 → embedding content | 提炼文本进向量库，与 `blueprint_anchor` 的取文本口径必须同源 |
| 反查结果 → 前端渲染 | 二跳实体混进「直接引用者」会误导用户 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-116-27 | Tampering | `relations` 三层不透传 ⇒ REFERENCES 边永不被遍历，端点 200、边在库里、页面空白（**SC-4 最可能的假绿**） | mitigate | 三处纯追加 + ⭐ **端到端**验收断言（造边 → 被引方 `?direction=in&relations=REFERENCES&max_hops=1` → 查回引用方）+ 「不传 relations ⇒ 空」的反向对照 |
| T-116-28 | Tampering | 目标实体不存在 ⇒ `IntegrityError` 被 `apply_edge_specs:435` 吞成 warning，边**静默消失**，且与「并发已建（良性）」共用同一 except 分不出来 | mitigate | 批量存在性预过滤 + 丢弃计数进 `sampling` 事件 + 用例断言 `dropped_by_source_type` |
| T-116-29 | Tampering | 同目标多条 citation 从第二条起撞 `uniq_kedge_active` 被吞成 warning ⇒ 引用关系不完整 | mitigate | normalizer 内按 target 分组聚合成一条 `EdgeSpec` + 用例「活跃边恰好 1 条且 `citation_ids` 两项」 |
| T-116-30 | Tampering | 多条 `RELATES_TO` 出边互相 `invalidate_edge`（`exclusive` 作用域是 `(source, relation)`），走**正常路径** ⇒ 完全静默 | mitigate | 模块 docstring 明写 + 结构断言「`RELATES_TO` 恰好 1 条」 |
| T-116-31 | Repudiation | `first_seen_version_no` 每次重摄取被刷成当前版本号 ⇒ 字段名与语义对不上，成为一条会误导排查的假数据 | mitigate | 去掉该键（信息量由 `valid_at` / `created_at` 承载）+ 用例断言 metadata 里不含它 |
| T-116-32 | Information Disclosure | space 为空的实体入图 ⇒ **双向不可见**的孤儿节点（既查不出邻居也不出现在别人的邻居里），表现为「入图了但查不到」 | mitigate | space 反查不到即**不产事件 + warning** + 用例断言返回 `[]` |
| T-116-33 | Denial of Service | v1 骨架不入图（只挂 `add_version`）⇒「新建蓝图 → 立刻查图谱 → 空」被当 bug 反复排查 | mitigate | `create` 也挂同一门控 + 用例断言（含 v0 content 零调用的反向对照） |
| T-116-34 | Denial of Service | `add_version` 无变化时也投递 ⇒ 每次重复写入白跑一次 normalizer + 一次后台任务 | mitigate | 门控内比对返回 version 与调用前 `current_version_id` + 用例「连写两次只投递一次」 |
| T-116-35 | Information Disclosure | 前端不传 `maxHops: 1` ⇒ 二跳实体混进「直接引用者」，用户据错误的引用关系做决策 | mitigate | 组件显式传 1 + spec 入参逐字断言 + 后端用例的一跳/二跳差异对照 |
| T-116-36 | Tampering | 把 `REFERENCES` 加进 `_DEFAULT_RELATIONS` 图省事 ⇒ 所有既有实体详情页突然多出一批邻居（行为回归） | mitigate | prohibitions 明令 + `git diff related.py` 为空的核算 + 「不传 relations ⇒ 空」用例（加进去即转红） |
| T-116-37 | Information Disclosure | 蓝图正文（半可信）整份进 `payload` / 进日志 | mitigate | `payload` 只放标量与计数、`content` 是提炼文本；模块进 `_SCANNED_MODULES`；日志只记计数 |
| T-116-SC | Tampering | 依赖供应链 | accept | 零新增外部依赖、零 migration、零新 `EntityKind`、零新事件常量进 `BLUEPRINT_EVENTS`；`makemigrations --check` 退出码 0；pnpm 环境漂移已核算还原 |
</threat_model>

<verification>
- `cd server && uv run pytest tests/knowledge/ -q` 全绿（含既有 `test_ingestion.py` / `test_graph_store.py` / `test_artifact_associations_api.py` / `test_knowledge_api.py` —— 三处纯追加零回归的第一道证据）。
- `cd server && uv run pytest tests/delivery/ -q` 全绿（含脱敏守卫 14 项扫描面与 `blueprint_doc_views` 的既有用例）。
- ⭐ `cd server && uv run pytest tests/ -q` 相对基线（8609 passed / 1 failed）**无新增失败**。
- ⭐ `cd web && pnpm exec vitest run` 相对基线（1674 passed / 1 skipped）**无新增失败**；`pnpm type-check` exit 0；`pnpm build` 通过；`pnpm exec eslint <触及文件>` 零新增问题。
- `cd server && uv run python manage.py makemigrations --check --dry-run` 退出码 0；相位内**零** migration。
- 冻结面核算：`git diff server/knowledge/related.py`（`_DEFAULT_RELATIONS`）、`git diff server/knowledge/ingestion.py`（`apply_edge_specs`）、四个 0.19 归属前端面、`web/src/pages/knowledge/entities/` —— **全部为空**。
- 删除行核算：八个后端文件与 `web/src/api/knowledge.ts` 逐个不超过 execution_context 表的上界。
- 环境项核算：`git status --porcelain web/pnpm-workspace.yaml web/src/components.d.ts` 为空（P-15 + STATE 115-06）。
- ⭐ **额外前端改动登记**：若改了 `pages/knowledge/blueprints/[id].vue` 或 `locales/zh-CN.json`，逐个在 SUMMARY 写明理由与删除行（应为 0）。
</verification>

<success_criteria>
- ⭐ **「被谁引用」第一次真的能查**，且验收是**端到端**的：造一条 blueprint→REFERENCES→knowledge_entity 边，从被引方 `?direction=in&relations=REFERENCES&max_hops=1` 能查回引用方。⛔ 不是「表里有那一行」。
- **「不传 relations ⇒ 空」这条反向对照同时充当护栏**：任何人把 `REFERENCES` 加进 `_DEFAULT_RELATIONS` 图省事，它立刻转红。
- **四处会静默假通过的地方各有可证伪防线**：目标不存在的边被**计数**而不是被吞；同目标多条 citation 聚合成一条；`RELATES_TO` 恰好 1 条；v1 骨架经 `create` 也入图（且 v0 content 零调用的反向对照证明门控判据没写反）。
- **`first_seen_version_no` 这条会变成谎言的字段在落地前就被去掉**，理由与替代（`valid_at` / `created_at`）写进模块 docstring。
- **`space_id` 反查不到就整体不入图**，避免制造双向不可见的孤儿节点——「入了图却永远查不出来」这种最难排查的形态被物理堵死。
- **VIEW-04 由 PARTIAL 转 Complete**：115-05 登记的「反向『被谁引用』顺延 116」在此闭环；115 预留的两块位置无需重构、补上即可，且那条 `getArtifactAssociations` 的 0 调用断言**原样保留**（它仍然是成立的防线）。
- 零 migration、零新 `EntityKind`、零新依赖；`_DEFAULT_RELATIONS` 与 `apply_edge_specs` 逐字未动；前后端双门相对基线无新增失败。
</success_criteria>

<output>
完成后创建 `.planning/phases/116-entry/116-04-SUMMARY.md`，provides 至少包含：
- ⭐ **反查的完整调用链契约**：`GET .../blueprint/` 的 `knowledge_entity_id` → `GET /api/knowledge/related/<it>/?direction=in&relations=REFERENCES&max_hops=1`（含**必须显式传 `max_hops=1`** 的理由）；
- `?relations=` 的白名单、非法值状态码、**不传时的行为**（逐字不变）；
- ⭐ **normalizer 的产出契约**：实体 natural key（`tech_plan` / `blueprint` / `artifact_id`）、`space_id` 的来源链（Project id → Space id）、`content` / `payload` 的形状、两类边与各自的 `exclusive`；
- ⭐ **九种 `source_type` → 目标实体换算表**（含「`url` 不成边」与「还原不出即丢弃」两条）；
- ⭐ **四条结构约束及其对应用例名**（存在性预过滤 / 同目标聚合 / `RELATES_TO` 恰好 1 条 / 无 `first_seen_version_no`）；
- **两处门控的落点与「是否真的翻了版本」的判据**，以及 v1 骨架入图的对照用例；
- **丢弃计数事件的字段清单**（运维据它查「哪些引用没成边」）；
- 前端两块的 query key、入参、空态/失败口径，以及 ⭐ **`sections.spec.ts` 拆两条的前后对照**（为什么 `getArtifactAssociations` 仍必须为 0）；
- ⭐ **额外前端改动登记**（`locales/zh-CN.json` 与可能的 `[id].vue`，含理由与删除行核算）；
- 受限面删除行逐行登记；前后端双门与基线的差异比对；pnpm 环境项还原核算；
- ⭐ **VIEW-04 转 Complete 的证据清单**（哪几条用例分别兑现了「正向可查」与「反向被谁引用」）。
</output>
