---
phase: 116-entry
plan: 06
type: execute
wave: 5
depends_on: ["116-02", "116-03", "116-05"]
files_modified:
  - server/delivery/services/blueprint_answer_action.py
  - server/delivery/api/blueprint_review_views.py
  - server/services/process_runtime/blueprint_notify.py
  - server/services/process_runtime/blueprint_ambiguity_score.py
  - server/services/process_runtime/blueprint_spec_gate.py
  - server/mcp_tools/views.py
  - server/mcp_tools/urls.py
  - server/mcp_tools/serializers.py
  - server/mcp_tools/technical_plan_service.py
  - server/tests/delivery/test_blueprint_log_redaction_guard.py
  - server/tests/mcp_tools/test_blueprint_clarification_tools.py
  - server/tests/services/process_runtime/test_blueprint_assumptions_tiers.py
  - server/tests/services/process_runtime/test_blueprint_notify.py
autonomous: true
requirements: [GATE-01]
prohibitions:
  - "⛔ ⭐ **`create_feishu_technical_plan` 的工具名与既有响应键一个都不改**（`TOOL_SCHEMA_SNAPSHOT` 守门 + 该文件反复申明的外形兼容纪律）：只**追加** `blueprint_artifact_id` / `blueprint_status` / `pending_clarifications[]` 三键，并把 `status` 落 `partial`（既有三态之一，`technical_plan_service.py:41-43` 已有该映射与 `retry_state` 形态）⇒ 调用方零破坏"
  - "⛔ ⭐ **绝不在 MCP 层直写 `BlueprintThread`**（旁路 INV-6，`test_blueprint_inv6_guard.py` 三条正则连裸实例化都逮）、⛔ **绝不进程内自调 REST**。两条同时成立就只剩「共享 service」一条路 ⇒ 把作答调用序抽成 `delivery/services/blueprint_answer_action.py`，View 与 MCP 工具都调它"
  - "⛔ **不把新 service 加进 `test_blueprint_inv6_guard._ALLOWED_WRITER`** —— 唯一 writer 必须保持是 `blueprint_lifecycle_service.py`"
  - "⛔ ⭐ **不建第三个 list 工具**：pending 清单内联在 `get_technical_blueprint` 里。寻址键一律 **`artifact_id`** 而不是 `session_id`（既有 20 个蓝图端点全部以 `artifact_id` 为一级键并按它挂范围闸；且同一 artifact 上可并存 `technical_plan` 与 `technical_blueprint` 两条会话——112 review 的 CRITICAL 正是「按 artifact 取最近一条会话」踩出来的）"
  - "⛔ **不动 MCP 公共 handler 工厂**（`timeout=60.0` / 配额计数 / 无 callback，Phase 113 冻结）：配额按 113 的定夺**在 dispatch 处**处理。⛔ **不做服务端长轮询、不做推送** —— 「立即返回 + 轮询取件」，MCP 调用方也没有可回调地址"
  - "⛔ ⭐ **`assume_more` 绝不等于 `skip_clarification`**：档位只管「问不问」不管「问了等不等」，超时语义永远是显式 pending 不自动作答（§12 决策 4，本里程碑不可动）。⛔ **不得把档位实现成「跳过 spec_gate stage」** —— 那等于原地复活 GATE-01 要消灭的东西。变异用例断言 `assume_more` 档下四维打分**仍然执行**、超阈值**仍然开线程**（只是阈值更高）"
  - "⛔ **不新增 `CallSource` 枚举值**（清单锁 `tests/test_model_usage_call_source.py:77`）、⛔ **不新增事件常量进 `BLUEPRINT_EVENTS`**（`len == 21` 双断言）、⛔ **不新增 migration**（两个 SettingKeys 已由 116-01 声明）"
  - "⛔ **`pending_clarifications[]` 读失败 ⇒ 如实 503 + 中性 detail，且响应体逐字不含 `items` / `total`**（P-12，STATE「116 接线必读」）：⛔ 不包成 200 空结构（前端/调用方 `items.length === 0` 会读成空态）。观测另包一层 `try/except: pass`，⭐ **业务主体绝不包进 best-effort**"
  - "⛔ **`blueprint_notify.py` 必须是一个文件**（CONTEXT）：同步点 1 之后换 107 的送达设施时**只改这一个文件**；这条要写进模块 docstring。整段 best-effort，⛔ 失败只记事件绝不反噬挂起"
  - "⛔ **反查蓝图会话必须带 `process_type=\"technical_blueprint\"` 过滤**（112 review CRITICAL + 114-05 收件人口径）"
  - "⚠️ `tests/mcp_tools/test_skills_snapshot_guard.py` 在本 worktree **恒红**（`skills/` 空目录，P-16）—— ⛔ 别把它的红当成本 plan 引入、⛔ 也不得试图修它"
  - "⛔ **前端零改动**（`git diff --name-only | rg '^web/'` 为空）：MCP 协议面无 UI 触点；澄清送达走飞书卡片通道"
must_haves:
  truths:
    - "⭐ **`delivery/services/blueprint_answer_action.py` 抽取作答调用序**（PATTERNS B3，需 PLAN 拍板的设计决策 —— 本 plan 选 (a)）：analog 是 115 刚立的 `blueprint_comment_action.py`（「View 零 ORM 写 + 恒定键返回 dict」）。代价是改一处既有 View（`BlueprintReviewThreadAnswerView.post` 体改为调 service），**本 plan 显式登记这个追加点**；⛔ 选 (b)「MCP 复刻一遍三道闸」违反 fail-closed 单一实现纪律，且 114-CR-01 那条 finding 后门一旦漂移就是安全回归"
    - "⭐ **三道闸的顺序必须逐字复刻**（RESEARCH §B.2，`blueprint_review_views.py:633-660` 实读）：① `_aload_action_context`（含 `_aassert_project_scope`）→ ② **`is_blueprint_editable(artifact)` 在 `record_answer` 之前**（越界 400 且 DB 一字未动，114-MJ-04）→ ③ **`kind == ai_review_finding` 一律 400**（114-CR-01，端点分流 + 回灌链 `REFLOW_KINDS` 自身 fail-closed 的双重堵）。⭐ 变异用例「对 finding 线程调 MCP 作答 ⇒ 400 且线程状态**一字未变**（从 DB 重读）」正是在验闸 ③ 与闸 ②的先后"
    - "⭐ **service 必须带 `reflow` 结构**：analog 的 View 在 `record_answer` 之后**同一请求内**调 `aapply_thread_answers` 回灌产新版本，且「回灌失败绝不回滚、绝不改响应码，结果原样放进 `reflow` 键」（`:615-617`）。⇒ service 的恒定返回键含 `reflow`；MCP 工具的响应也带它（否则调用方无法区分「答案记下了但正文没更新」）"
    - "⭐ **既有 View 的对外契约逐字不变**：`BlueprintReviewThreadAnswerView.post` 改为调 service 后，**状态码映射、响应键集、`reflow` 五档语义、埋点字段全部逐字保持**；115-04 的前端按 `reflow.status` 五档分档 toast，任何键名/取值漂移都是前端回归。⭐ 用「既有 `test_blueprint_review_views.py` 全绿且零改动」核算"
    - "⭐ **两个新 MCP 工具（113-02 四处落点范式）**：`get_technical_blueprint`（按 `artifact_id` 取 `blueprint_status` + 六段摘要 + markdown + `pending_clarifications[]`，**终稿续取即用它**）与 `answer_blueprint_clarification`（按 `thread_id` 作答）。四处落点：`mcp_tools/views.py`（`McpToolView` 子类 + `tool_name` + `post` 三段式 `_begin` → `_validate` → `_handle`）/ `urls.py`（+2 `path`）/ `serializers.py`（+2 请求 serializer + **`TOOL_SCHEMA_SNAPSHOT` 两条新条目**）/ 双守门测试"
    - "⭐ **观测规范的「新增请求入口纳入 QPS/错误率/时长」自动满足**：基类 `_record` 已落 `RequestMetric(route=f\"mcp:{tool_name}\")`（`mcp_tools/views.py:300-308`）⇒ 两个新工具**无需另写指标埋点**，只要 `tool_name` 赋对。`_begin` 也已处理 `bind_source(MCP)` + `request.auth is None → 401` ⇒ ⛔ 不重复写鉴权"
    - "**MCP 工具异常兜底：绝不 5xx**（`views.py:4308-4324` 范式），错误经 `redact_secrets_in_text(str(exc))[:500]` 后进日志"
    - "⭐ **`markdown` 复用 116-05 的 `render_blueprint_markdown` 并传真实 `blueprint_status`**（⛔ 不在 MCP 层拼 markdown，⛔ 不传 `\"\"`）—— 未确认的蓝图经 MCP 取走时同样带「未经确认」标注"
    - "⭐ **`create_feishu_technical_plan` 追加三键 + `status=\"partial\"`**：`blueprint_artifact_id` / `blueprint_status` / `pending_clarifications[]`；⭐ **同步进它的 `TOOL_SCHEMA_SNAPSHOT` 条目 `response` 列表**（该文件 `:1229-1230` 自带教训注释：「`redispatched` 是 113-04 追加的真实响应键，漏在 snapshot 里会让容器侧/外部客户端按已发布契约以为它不存在（snapshot 是对外契约，不是内部注释）」）。⚠️ 仅在 mcp 开关为 `technical_blueprint` 时追加，开关关闭时响应**逐字与改动前相同**"
    - "⭐ **assumptions 三档不是「零新机制」**（P-11，CONTEXT 的判断需要修正）：`threshold` 可覆盖（`aload_spec_gate_config` 已读已 clamp），但 **`max_rounds` 是模块级常量 `_MAX_SPEC_GATE_ROUNDS = 3`**（`blueprint_spec_gate.py:73`，在 `:175` 判定与 `:183` 日志两处使用），`DEFAULT_SPEC_GATE_CONFIG` 里**根本没有这个键**（`:47-50`）⇒ **真实新增**：`aload_spec_gate_config` 返回值加 `max_rounds`（`int()` 强转 + 下界 1）+ 两处改读配置"
    - "⭐ **`aload_spec_gate_config(tier: str = \"\")` 传 tier 字符串而不是 session**（保持该函数无 ORM 依赖，它现在是纯配置读取）；⚠️ **该函数在 `blueprint_spec_gate.py` 被调两次**（`:211` 判定、`:359` `_lock_spec`），**两处都要拿到档位**，否则 `ambiguity_report` 里记的阈值与实际判定用的对不上"
    - "档位写入点 `stage_state.decomposition.assumptions_tier`（`_current_round` 读的是 `stage_state[\"spec_gate\"][\"round\"]`，放 `decomposition` 下不冲突）；留痕加进 `_ambiguity_report(...)`（`ambiguity_report` 的唯一装配点，已带 `capped` / `release_reason` / `scorer_unavailable`）"
    - "⭐ **`blueprint_notify.py` 一个文件收敛澄清送达**：调用序逐字照 `plan_research._send_clarify_card`（`:434-491`）—— `build_clarification_card` → `_resolve_space` → `_aresolve_project(space)` → `ProjectService().resolve_or_create_group(project, member_ids, initiated_by_user_id)` → `FeishuIMService.create(space)` → `send_card(receive_id=chat_id, receive_id_type=\"chat_id\", card=card)`；**整段 `try/except` 只 log**；每一步「取不到就 `return`」的早退形态"
    - "⭐ **收件人口径抄 114-05 已实装的 `blueprint_review_action.aremind_clarification_threads`（`:700`）**：`BlueprintReviewer` 名单 ∪ 蓝图会话 `created_by_id`，去重升序，**反查会话必须带 `process_type=\"technical_blueprint\"` 过滤**"
    - "⭐ **澄清题正文过 `redact_secrets_in_text`**（逐字照 `plan_research.py:510-517`）：蓝图澄清题来自 LLM（`scores[\"questions\"]`），同属半可信文本"
    - "⭐ **`blueprint_notify` 与 analog 的两处 DIFFER**：① analog 是 workflow 节点的方法（吃 `ExecutionContext`），本模块是**独立模块级函数**（四个入口都要用）⇒ space / project 由调用方或 `meta.project_id` 反查传入，⛔ 不依赖 `ExecutionContext`；② 模块 docstring 写明「同步点 1 之后换 107 的送达设施时**只改这一个文件**」（形状照 `useBlueprintLive.ts` 的收敛法自述）"
    - "两个新模块（`blueprint_answer_action.py` / `blueprint_notify.py`）与模块创建**同 commit** 追加进 `test_blueprint_log_redaction_guard._SCANNED_MODULES`"
    - "全量后端门相对基线 **8609 passed / 1 failed** 无新增失败（⚠️ `test_skills_snapshot_guard` 那一条是既有环境项）；`makemigrations --check --dry-run` 退出码 0"
  artifacts:
    - path: "server/delivery/services/blueprint_answer_action.py"
      provides: "aanswer_thread(...)：三道闸顺序（范围闸 → is_blueprint_editable 在写之前 → finding 一律拒）+ record_answer + 同请求回灌 aapply_thread_answers + 恒定键返回 dict（含 reflow / current_status）；INV-6 零 ORM 写、正文只记长度"
      contains: "is_blueprint_editable"
    - path: "server/delivery/api/blueprint_review_views.py"
      provides: "BlueprintReviewThreadAnswerView.post 体改为调 blueprint_answer_action.aanswer_thread（对外契约逐字不变：状态码 / 响应键 / reflow 五档 / 埋点字段）"
      contains: "aanswer_thread"
    - path: "server/mcp_tools/views.py"
      provides: "GetTechnicalBlueprintView / AnswerBlueprintClarificationView 两个 McpToolView 子类（三段式 + 绝不 5xx + markdown 走 render_blueprint_markdown 传真实状态 + pending 读失败 503 且不含 items/total）"
      contains: "answer_blueprint_clarification"
    - path: "server/mcp_tools/serializers.py"
      provides: "两个请求 serializer + TOOL_SCHEMA_SNAPSHOT 两条新条目 + create_feishu_technical_plan 条目 response 追加三键"
      contains: "get_technical_blueprint"
    - path: "server/mcp_tools/technical_plan_service.py"
      provides: "mcp 开关开启时响应追加 blueprint_artifact_id / blueprint_status / pending_clarifications[] 并把 status 落 partial；开关关闭时响应逐字不变"
      contains: "blueprint_artifact_id"
    - path: "server/services/process_runtime/blueprint_ambiguity_score.py"
      provides: "aload_spec_gate_config(tier=\"\") + 返回值追加 max_rounds（int 强转 + 下界 1）+ 三档预设读 SettingKeys.BLUEPRINT_ASSUMPTIONS_TIERS（畸形回默认）"
      contains: "max_rounds"
    - path: "server/services/process_runtime/blueprint_spec_gate.py"
      provides: "两处 aload_spec_gate_config 调用带上档位（:211 判定 / :359 _lock_spec）+ _MAX_SPEC_GATE_ROUNDS 两处改读配置（:175 / :183）+ _ambiguity_report 记档位"
      contains: "assumptions_tier"
    - path: "server/services/process_runtime/blueprint_notify.py"
      provides: "澄清飞书卡片送达的唯一收敛点（收件人 = BlueprintReviewer ∪ 会话发起人、反查带 process_type 过滤、题面过 redact_secrets_in_text、整段 best-effort、模块级函数不依赖 ExecutionContext）"
      contains: "redact_secrets_in_text"
    - path: "server/tests/mcp_tools/test_blueprint_clarification_tools.py"
      provides: "两工具 snapshot 条目存在 + create_feishu 追加三键在 snapshot 里 + ⭐ 对 ai_review_finding 线程作答 400 且线程状态一字未变 + 不可编辑态 400 且 DB 未动 + 非成员 404 + 响应含 reflow + pending 读失败 503 且不含 items/total"
      contains: "ai_review_finding"
    - path: "server/tests/services/process_runtime/test_blueprint_assumptions_tiers.py"
      provides: "三档 threshold/max_rounds 生效（两处调用点口径一致）+ ⭐ assume_more 仍打分仍开线程的变异用例 + 畸形配置回默认 + ambiguity_report 记录档位"
      contains: "assume_more"
  key_links:
    - from: "server/mcp_tools/views.py"
      to: "server/delivery/services/blueprint_answer_action.py"
      via: "AnswerBlueprintClarificationView 调同一个 service（⛔ 不直写 BlueprintThread、⛔ 不进程内自调 REST）"
      pattern: "aanswer_thread"
    - from: "server/delivery/api/blueprint_review_views.py"
      to: "server/delivery/services/blueprint_answer_action.py"
      via: "既有 View 改为调抽出的 service，两个调用方共享同一份三道闸实现"
      pattern: "aanswer_thread"
    - from: "server/mcp_tools/views.py"
      to: "server/services/process_runtime/blueprint_render.py"
      via: "get_technical_blueprint 的 markdown 走 render_blueprint_markdown(content, blueprint_status=<真实值>)（未确认标注同样带上）"
      pattern: "render_blueprint_markdown"
    - from: "server/services/process_runtime/blueprint_spec_gate.py"
      to: "server/services/process_runtime/blueprint_ambiguity_score.py"
      via: "两处 aload_spec_gate_config(tier=...) 调用（:211 判定 / :359 _lock_spec 必须同档位）+ max_rounds 改读配置"
      pattern: "aload_spec_gate_config"
    - from: "server/services/process_runtime/blueprint_notify.py"
      to: "server/feishu/cards/chat_question_card.py"
      via: "build_clarification_card → resolve_or_create_group → FeishuIMService.send_card（整段 best-effort；同步点 1 后只改这一个文件）"
      pattern: "build_clarification_card"
---

<objective>
让 MCP 入口**不再 skip_clarification**：立即返回会话与 pending 状态、澄清经新工具可作答、结果可续取，澄清同时推飞书卡片，交互密度按 assumptions 档位可配。

四件事，每件都有一处必须先拍板或必须逐字复刻的地方：

① ⭐ **作答通道必须先抽服务层**（PATTERNS B3）。MCP 工具「⛔ 绝不在 MCP 层直写 `BlueprintThread`（旁路 INV-6）」与「⛔ 绝不进程内自调 REST」这两条同时成立，就只剩「共享 service」一条路 —— 而作答那套调用序**目前完全内联在 `BlueprintReviewThreadAnswerView.post` 里**（`:633-716`），没有任何可 import 的东西。⇒ 抽 `blueprint_answer_action.aanswer_thread`，View 与 MCP 工具都调它。这需要改一处既有 View，本 plan **显式登记**这个追加点；⛔ 选「MCP 复刻一遍三道闸」违反 fail-closed 单一实现纪律，且 114-CR-01 那条 finding 后门一旦漂移就是**安全回归**。

② ⭐ **三道闸的顺序不能变**（RESEARCH §B.2 实读）：范围闸 → **`is_blueprint_editable` 在 `record_answer` 之前** → `kind == ai_review_finding` 一律 400。CONTEXT specifics 第 6 条要求的变异用例「对 finding 线程调 MCP 作答 ⇒ 400 且线程状态**一字未变**」正是在验闸 ③ 与闸 ② 的先后 —— 这是 114-CR-01 的对称面：回灌链落版本成功后会对被消费线程**无条件 `resolve_thread`**，让 finding 进来即等于「在 BLOCKER 上回一句任意文本」就解开 confirm 门。

③ ⭐ **assumptions 档位不是「零新机制」**（P-11，修正 CONTEXT 的判断）。`threshold` 确实可覆盖，但 **`max_rounds` 是模块级常量 `_MAX_SPEC_GATE_ROUNDS = 3`**、配置里根本没有这个键 ⇒ 这是一处**真实改动**（改 `aload_spec_gate_config` 签名 + 两处读配置）。照 CONTEXT 的「零新机制」写，会在执行中途才发现改不动，最后要么临时硬编码三个常量（档位不可运行时调，违背设计目的），要么半路返工。⇒ 本 plan 单列成一个 task。
⭐ 同时钉死：`assume_more` **绝不等于 `skip_clarification`** —— 档位只管「问不问」不管「问了等不等」。物理隔离本身是成立的（蓝图链根本没有 `clarify` dep），**只要不把档位实现成「跳过 spec_gate stage」**；变异用例断言 `assume_more` 档下四维打分**仍然执行**、超阈值**仍然开线程**。

④ **澄清送达封装成一个文件**。这同时兑现 STATE 登记的「114-05 的提醒只到『记事件 + 写周期锚点』为止，用户收不到任何通知 ⇒ CLAR-04 的用户可感知价值只兑现一半」的另一半。同步点 1 之后换 107 的送达设施时**只改这一个文件**。

Purpose: SC-2 全量交付。GATE-01 的「MCP 入口支持异步澄清协议（返回 pending、可作答、可续取），不再跳过澄清」这一半在此闭环。
Output: 2 个新建 service/模块 + 4 处既有后端改造 + 4 处 MCP 落点 + 1 处守卫清单 + 3 个新建测试文件。**零 migration、零新依赖、零新 `CallSource` 枚举、零新事件常量、前端零改动。**
</objective>

<execution_context>
工作目录：worktree 根 `/Users/zaneliu/Projects/open-source/friday-ai/.claude/worktrees/v0.20-blueprint`，分支 `milestone/v0.20.0-blueprint`。⛔ 绝不读写主检出。

**wave 5，依赖 116-02 / 116-03 / 116-05**：
- 116-02 的 intake 与 116-03 的 MCP 入口接线是本 plan「MCP 能建蓝图会话」的前提（`create_feishu_technical_plan` 追加三键要有真实数据可填）；
- 116-05 的 `render_blueprint_markdown` 是 `get_technical_blueprint` 的 `markdown` 键的来源；
- 与 116-05 **共同改 `tests/delivery/test_blueprint_log_redaction_guard.py`** ⇒ 文件重叠即隐式依赖，必须排在其后的波次。本 plan 追加的两行必须**与两个模块的创建在同一个 commit**。

开工前**必读**：`116-03-SUMMARY.md` 的「两处跨相位交接登记」第 ① 条（MCP 响应键追加三键归本 plan）与「四个入口的接线表」；`116-05-SUMMARY.md` 的 renderer 签名与调用点清单。

**测试基线**：`cd server && uv run pytest tests/ -q` = **8609 passed / 1 failed**。⚠️ 唯一失败是 `tests/mcp_tools/test_skills_snapshot_guard.py::test_skill_files_discovered`（本 worktree 的 `skills/` 是空目录）—— **本 plan 大量改动 `mcp_tools/`，务必不要把这条既有的红当成自己引入的**（P-16）。收口断言是「无**新增**失败」。

**受限面配额（硬约束）**：

| 文件 | 允许的改动 | 删除行上界 |
|---|---|---|
| `server/delivery/api/blueprint_review_views.py` | ⭐ **仅** `BlueprintReviewThreadAnswerView.post` 的函数体改为调 service（对外契约逐字不变） | **60**（原内联调用序整体下沉） |
| `server/mcp_tools/views.py` | 追加 2 个 `McpToolView` 子类 | **0** |
| `server/mcp_tools/urls.py` | 追加 2 条 `path`（现 35 条） | **0** |
| `server/mcp_tools/serializers.py` | 追加 2 个请求 serializer + snapshot 2 条新条目 + `create_feishu_technical_plan` 条目 `response` 追加 3 项 | **1**（既有条目 `response` 列表的收尾行） |
| `server/mcp_tools/technical_plan_service.py` | 响应装配处追加三键与 `status` 分支 | **4** |
| `server/services/process_runtime/blueprint_ambiguity_score.py` | `aload_spec_gate_config` 加形参 + 返回值加 `max_rounds` + 三档读取 | **6** |
| `server/services/process_runtime/blueprint_spec_gate.py` | 两处调用带档位 + 两处 `_MAX_SPEC_GATE_ROUNDS` 改读配置 + `_ambiguity_report` 记档位 | **6** |
| `server/tests/delivery/test_blueprint_log_redaction_guard.py` | `_SCANNED_MODULES` 追加 **2** 行 | **0** |

⚠️ 签名与字段名一律按下列**实测**取，不得猜测：

- ⭐ `BlueprintReviewThreadAnswerView.post`（`blueprint_review_views.py:633-716`）的**完整调用序**：`_aload_action_context` → `is_blueprint_editable` 400（`NOT_EDITABLE_DETAIL`）→ `_aload_thread` 404（`_THREAD_MISSING_DETAIL`）→ `kind == ThreadKind.AI_REVIEW_FINDING` 400（`_FINDING_NOT_ANSWERABLE_DETAIL`）→ `body = str(request.data.get("body") or "").strip()` → `record_answer` → `aapply_thread_answers` → 响应带 `reflow` → `_acurrent_status` 重读。
- `delivery/services/blueprint_comment_action.py`（115-A4）—— **service 形态的逐字 analog**：模块 docstring 首段 INV-6、`__all__`、`_COMPONENT`、`_user_id`（回落 `"system"`）/ `_detail`（`redact_secrets_in_text` + 截断 500）、**恒定键返回 dict**、正文只进 `body=`、日志只记 `body_len`。
- `blueprint_reflow.REFLOW_KINDS = (ThreadKind.AI_CLARIFICATION,)` 与 `aapply_thread_answers` 的签名（**显式传入的 threads 也按 kind 过滤**，fail-closed 不依赖调用方自觉）。
- `mcp_tools/views.py`：`McpToolView` 基类 `:238-325`（`_begin` `:254-268` 处理 `bind_source(MCP)` + `request.auth is None → 401`；`_validate` 400 `invalid_params`；`_record` `:300-308` 已落 `RequestMetric(route=f"mcp:{tool_name}")`）；⭐ `ReadBlueprintContextView` `:4231-4324` **全文**（模块 docstring 契约书体例、`post` 三段式 `:4250-4260`、异常兜底 `:4308-4324` 的 `redact_secrets_in_text(str(exc))[:500]`）。
- `mcp_tools/serializers.py`：`TOOL_SCHEMA_SNAPSHOT` 在 `:830`；⭐ `create_feishu_technical_plan` 条目 `:1092-1116`（request 9 键 / response 12 键，**逐字**）；两条蓝图条目 `:1224-1241`；⭐ `:1229-1230` 的教训注释（snapshot 是对外契约）。
- `mcp_tools/technical_plan_service.py`：`:41-43` 三态 `status` 映射到 `McpWorkItemTechnicalPlan.Status`；`:411-413` 已有 `partial` + `orchestration_pending` 的 `retry_state` 形态；`:488` 把 `space_id` 当 `"project_id"` 键回传（⚠️ **本 plan 不动这个既有键**，蓝图侧的 project 归 `blueprint_artifact_id` 对应的 artifact）。
- `blueprint_ambiguity_score.aload_spec_gate_config()`（`:239-268`）：**无形参**，只返 `{threshold, weights}`；`DEFAULT_SPEC_GATE_CONFIG`（`:47-50`）。
- `blueprint_spec_gate.py`：⭐ `_MAX_SPEC_GATE_ROUNDS = 3`（`:73`，在 `:175` 判定与 `:183` 日志两处使用）；`aload_spec_gate_config()` 被调**两次**（`:211` / `:359` `_lock_spec`）；`_current_round` 读 `stage_state["spec_gate"]["round"]`（`:621-627`）；`_ambiguity_report(...)` 是 `ambiguity_report` 的**唯一装配点**（已带 `capped` / `release_reason` / `scorer_unavailable`）；`_goal_text(content)`（`:632`）。
- `SettingKeys.BLUEPRINT_ASSUMPTIONS_TIERS`（116-01 已声明，JSON 三档 `{threshold, max_rounds}`）。
- `workflows/nodes/ai/plan_research.py:434-491` `_send_clarify_card` **全文** + `:493-518` `_acollect_round_questions`（⭐ `:510-517` 的 `redact_secrets_in_text` 逐字）。
- `delivery/services/blueprint_review_action.py:700` `aremind_clarification_threads` 的收件人口径。
- `test_blueprint_log_redaction_guard._SCANNED_MODULES`（116-05 之后共 **16** 项）。
</execution_context>

<context>
@.planning/phases/116-entry/116-RESEARCH.md（§B.1 新工具四处落点、⭐ §B.2 作答复用 114 通道的三道闸逐条 + `reflow` 那条容易漏的、⭐ §B.3 档位可配面比 CONTEXT 设想窄一半 + `max_rounds` 的真实改动、§B.4 飞书卡片送达、P-11 / P-12 / P-16、给 PLAN 的第 8 条）
@.planning/phases/116-entry/116-PATTERNS.md（B1/B2 MCP view 的「逐条照抄」与必须 DIFFER 五条、⭐ B3 抽 service 的两个选项与拍板依据、B4 snapshot 条目形状与 `:1229-1230` 教训注释、B5 送达模块的沿用与三条 DIFFER、B6 档位的真实新增表、§0.2 MCP 工厂冻结、§0.4 追加点 4/5/16/17/18/19/26、Shared Patterns S1/S2/S3）
@.planning/phases/116-entry/116-CONTEXT.md（MCP 异步澄清协议一节全文；specifics 第 6 条）
@server/delivery/api/blueprint_review_views.py（⭐ `:607-716` `BlueprintReviewThreadAnswerView` **全文**：类 docstring 的四条纪律（record_answer 正当用法 / 回灌不回滚不改码 / 状态闸在写之前 / finding 一律 400 的双重堵）与 `post` 的完整调用序 —— 本 plan 把它下沉进 service 且**对外契约逐字不变**）
@server/delivery/services/blueprint_comment_action.py（⭐ **service 形态的逐字 analog**：INV-6 docstring / `__all__` / `_COMPONENT` / `_user_id` / `_detail` / 恒定键返回 dict）
@server/services/process_runtime/blueprint_reflow.py（`REFLOW_KINDS` 与 `aapply_thread_answers` 的签名与 fail-closed 过滤）
@server/mcp_tools/views.py（`:238-325` `McpToolView` 基类三段式与 `_record` 的 `RequestMetric`；⭐ `:4231-4324` `ReadBlueprintContextView` 全文作范式）
@server/mcp_tools/serializers.py（`:830` `TOOL_SCHEMA_SNAPSHOT`、`:1092-1116` `create_feishu_technical_plan` 条目逐字、`:1224-1241` 两条蓝图条目与 ⭐ `:1229-1230` 教训注释）
@server/mcp_tools/technical_plan_service.py（`:41-43` 三态映射、`:405-420` `partial` + `retry_state` 的既有形态、`:480-495` 响应装配）
@server/services/process_runtime/blueprint_ambiguity_score.py（`:47-50` `DEFAULT_SPEC_GATE_CONFIG`、`:239-268` `aload_spec_gate_config` 全文）
@server/services/process_runtime/blueprint_spec_gate.py（`:73` `_MAX_SPEC_GATE_ROUNDS`、`:170-190` 判定与日志两处、`:205-215` 与 `:355-365` 两处配置读取、`:610-645` `_current_round` / `_ambiguity_report` / `_goal_text`）
@server/workflows/nodes/ai/plan_research.py（`:434-491` `_send_clarify_card` 全文、`:493-518` `_acollect_round_questions` 与 `:510-517` 的脱敏逐字）
@server/delivery/services/blueprint_review_action.py（`:690-720` 收件人口径与 `process_type` 过滤）
@.planning/phases/116-entry/116-05-SUMMARY.md（`render_blueprint_markdown` 签名与「哪些面拿得到真实状态」）
@.planning/phases/116-entry/116-03-SUMMARY.md（MCP 入口接线表与跨相位交接第 ① 条）
@.planning/STATE.md（Pending Todos 第 3 条「best-effort 只覆盖观测不覆盖业务」；114-05 有意边界「提醒渠道投递未实现」）
</context>

<tasks>

<task type="auto">
  <name>Task 1: blueprint_answer_action.py 抽取（三道闸逐字复刻）+ 既有 View 改调 service（对外契约不变）+ 守卫清单</name>
  <files>server/delivery/services/blueprint_answer_action.py, server/delivery/api/blueprint_review_views.py, server/tests/delivery/test_blueprint_log_redaction_guard.py</files>
  <read_first>server/delivery/api/blueprint_review_views.py **L595-730**（⭐ `BlueprintReviewThreadAnswerView` **全文**：类 docstring 的四条纪律逐字、`post` 的**完整调用序**（`_aload_action_context` → `is_blueprint_editable` 400 → `_aload_thread` 404 → finding 400 → `body` 取值 → `record_answer` → `aapply_thread_answers` → 响应装配含 `reflow` → `_acurrent_status` 重读）、每一步的**确切常量名与状态码**、埋点字段清单）；server/delivery/api/blueprint_review_views.py **L100-300**（`_aload_action_context` / `_aload_artifact` / `_aload_session` / `_aload_thread` / `_aassert_project_scope` / `_acurrent_status` / `_log` 的签名与返回约定 —— service 需要哪些能力、哪些留在 View）；server/delivery/services/blueprint_comment_action.py **全文**（⭐ **逐字 analog**：模块 docstring 首段 INV-6 声明、第二段与 analog 的语义差异、`__all__`、`_COMPONENT`、`_user_id` 回落 `"system"`、`_detail` 走 `redact_secrets_in_text` + 截断 500、`_alatest_version`、**恒定键返回 dict** 与 `status` 闭集、日志只记 `body_len` / `has_anchor`）；server/delivery/services/blueprint_lifecycle_service.py（`record_answer` 的**逐字签名与全部 keyword**、`EDITABLE_BLUEPRINT_STATUSES` / `is_blueprint_editable` / `NOT_EDITABLE_DETAIL`）；server/services/process_runtime/blueprint_reflow.py（`REFLOW_KINDS` 与 `aapply_thread_answers` 的**逐字签名**、返回值的 `status` 五档取值、显式传入 threads 也按 kind 过滤的 fail-closed 实现）；server/tests/delivery/test_blueprint_review_views.py（⭐ answer 端点的**既有用例全集** —— 本 Task 的核算判据是「这些用例零改动全绿」）；server/tests/delivery/test_blueprint_inv6_guard.py **L42-65**（五条正则）；server/tests/delivery/test_blueprint_log_redaction_guard.py **L27-44**</read_first>
  <action>
**① 新建 `server/delivery/services/blueprint_answer_action.py`（analog：`blueprint_comment_action.py`）。**

模块 docstring 三段：(a) ⭐ **INV-6 声明** + 「本模块是**作答调用序的唯一实现**，两个调用方（`BlueprintReviewThreadAnswerView` 与 MCP 的 `answer_blueprint_clarification`）共享它；⛔ **绝不复刻第二份** —— 114-CR-01 那条 finding 后门一旦漂移就是安全回归」；(b) ⭐ **三道闸的顺序与各自的理由**（逐字承接 View 的类 docstring 四条纪律：`record_answer` 的正当用法 / 回灌失败绝不回滚绝不改响应码 / 状态闸在写之前 / finding 一律拒的双重堵）；(c) 观测——「⛔ 答案正文、澄清题正文一律不进日志，只记 `body_len`」。

`_COMPONENT = "blueprint_answer_action"`；`__all__` 显式列公开函数；三个共用 helper（`_user_id` / `_detail` / `_alatest_version`）**照 analog 复制体例**。

唯一公开入口：

```
async def aanswer_thread(
    artifact, thread, *, body: str, user=None, initiated_by_user_id: str = "system",
    lifecycle_service=None, section_writer=None,
) -> dict
```

⭐ **恒定返回键**（闭集，两个调用方各自映射成 HTTP 码 / MCP `error_response`）：
`{"status": "answered" | "not_editable" | "not_answerable" | "invalid", "thread_id": str, "reflow": dict, "detail": str, "current_status": str}`

实现顺序（⭐ **闸的先后是本 Task 的头号靶子**）：
1. ⚠️ **范围闸留在调用方**（`_aload_action_context` 依赖 `request`，service 不该吃 `request`）—— 但 service 的 docstring **必须写明「调用方必须先过范围闸」**，且两个调用方各自都过（用例分别覆盖）。
2. ⭐ **`is_blueprint_editable(artifact)` 在 `record_answer` 之前** ⇒ 不满足即 `{"status": "not_editable", "detail": NOT_EDITABLE_DETAIL}` **且 DB 一字未动**。
3. ⭐ **`kind == ThreadKind.AI_REVIEW_FINDING` 一律拒** ⇒ `{"status": "not_answerable", "detail": <既有 `_FINDING_NOT_ANSWERABLE_DETAIL` 的逐字文案，⭐ 常量下沉到本模块并由 View import 复用，⛔ 不留两份文案>}`，**线程状态一字未变**。
4. `body` strip 后为空 ⇒ `{"status": "invalid", ...}`（不落库）。
5. `record_answer(...)`（唯一写口，形状逐字照既有 View）。
6. **同一调用内**跑 `aapply_thread_answers(...)`（`section_writer` 不传 ⇒ 走生产实现）；⭐ **回灌失败绝不回滚、绝不改 `status`**，结果原样放进 `reflow`（含它自己的 `status` 五档）。
7. `current_status` 取**回灌之后**重读的值（114-MJ-01 第二点：service 侧取值发生在续驱之前会让前端拿到「刷新一下就变」的状态）。⚠️ ⛔ **返回键用 `current_status` 不是字面 `blueprint_status`**（INV-6 `_RE_FIELD_DICT_KEY`）。
8. 一条 caller 事件 `blueprint_thread_answered`（`artifact_id` / `thread_id` / `body_len` / `reflow_status` / `initiated_by_user_id` / `duration_ms`）。⛔ 答案正文不进日志。

**② `BlueprintReviewThreadAnswerView.post` 改为调 service（删除行 ≤60）。**

⭐ **对外契约逐字不变**：状态码映射（`answered` → 200 / `not_editable` → 400 / `not_answerable` → 400 / `invalid` → 400 / 线程不存在 → 404 / 范围闸 → 400/404）、响应键集、`reflow` 五档语义、埋点字段**全部保持**。View 保留：`started` 计时、`_aload_action_context`（范围闸）、`_aload_thread` 与 404、`request.data` 取 `body`、`status` → HTTP 码映射、`_log`。⛔ **不改类 docstring 的四条纪律**（它们仍然成立，只是实现下沉；可加一句「实现已下沉到 `blueprint_answer_action`，MCP 工具共享同一份」）。

⭐ **核算判据**：`tests/delivery/test_blueprint_review_views.py` **零改动且全绿** —— 这是「对外契约逐字不变」唯一可核算的形态。

**③ `_SCANNED_MODULES` 追加 1 行** `"delivery/services/blueprint_answer_action.py"`（删除行 0，与模块创建同 commit；另一行 `blueprint_notify.py` 由 Task 3 追加）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/delivery/test_blueprint_review_views.py tests/delivery/test_blueprint_inv6_guard.py tests/delivery/test_blueprint_log_redaction_guard.py -q</automated>
  </verify>
  <acceptance_criteria>
- ⭐ **既有 answer 端点用例零改动全绿**：`git diff server/tests/delivery/test_blueprint_review_views.py` **为空**；`cd server && uv run pytest tests/delivery/test_blueprint_review_views.py -q` 全绿。这是「对外契约逐字不变」的唯一核算。
- ⭐ **三道闸顺序在 service 里可核**：`cd server && uv run python -c "
src = open('delivery/services/blueprint_answer_action.py').read()
i = src.index('async def aanswer_thread')
body = src[i:]
p_edit = body.index('is_blueprint_editable')
p_find = body.index('AI_REVIEW_FINDING')
p_rec = body.index('record_answer')
assert p_edit < p_rec, '状态闸必须在 record_answer 之前'
assert p_find < p_rec, 'finding 分流必须在 record_answer 之前'
print('gate order OK')"` 输出 `gate order OK`。
- ⭐ **INV-6 零 ORM 写**：`rg -n "objects\.(a?create|a?bulk_create|a?update|a?get_or_create|delete)" server/delivery/services/blueprint_answer_action.py` **零命中**；`rg -n "BlueprintThread\s*\(|BlueprintThreadMessage\s*\(" server/delivery/services/blueprint_answer_action.py` **零命中**；`rg -n "_ALLOWED_WRITER" server/tests/delivery/test_blueprint_inv6_guard.py | rg -c "blueprint_answer_action"` == **0**；`cd server && uv run pytest tests/delivery/test_blueprint_inv6_guard.py -q` 全绿。
- ⭐ **finding 文案只有一份**：`rg -c "_FINDING_NOT_ANSWERABLE_DETAIL" server/delivery/services/blueprint_answer_action.py server/delivery/api/blueprint_review_views.py` 两个文件均命中，且**定义只在 service**（`rg -n "_FINDING_NOT_ANSWERABLE_DETAIL\s*=" server/delivery/api/blueprint_review_views.py` **零命中**）。
- ⭐ **`reflow` 是恒定键**：`rg -n '"reflow"' server/delivery/services/blueprint_answer_action.py` 命中；`cd server && uv run python -c "
src = open('delivery/services/blueprint_answer_action.py').read()
for k in ('status','thread_id','reflow','detail','current_status'):
    assert '\"%s\"' % k in src, k
assert 'blueprint_status\"' not in src, '⛔ 返回键不得用字面 blueprint_status'
print('constant keys OK')"` 输出 `constant keys OK`。
- **正文不进日志**：`rg -n "body_len" server/delivery/services/blueprint_answer_action.py` 命中；脱敏守卫全绿且 `rg -c "blueprint_answer_action" server/tests/delivery/test_blueprint_log_redaction_guard.py` == **1**；`git diff server/tests/delivery/test_blueprint_log_redaction_guard.py | rg "^-[^-]"` **为空**。
- **删除行核算**：`git diff server/delivery/api/blueprint_review_views.py | rg -c "^-[^-]"` ≤ **60**（原内联调用序下沉，逐行登记进 SUMMARY）；⭐ 该文件的**其余六个端点** `git diff` 上无改动行（人工核对）。
- `cd server && uv run ruff check delivery/services/blueprint_answer_action.py delivery/api/blueprint_review_views.py` 通过；`uv run ruff format --check delivery/services/blueprint_answer_action.py` 通过。
  </acceptance_criteria>
  <done>作答调用序抽成 `blueprint_answer_action.aanswer_thread` 且三道闸的**先后顺序**有源码级断言；既有 View 改调 service 而**对外契约逐字不变**（既有测试文件零改动全绿）；finding 文案只有一份、INV-6 零 ORM 写且未进豁免名单；service 与守卫清单同 commit 落地。</done>
</task>

<task type="auto">
  <name>Task 2: 两个新 MCP 工具（四处落点）+ create_feishu_technical_plan 追加三键与 status=partial + 工具测试</name>
  <files>server/mcp_tools/views.py, server/mcp_tools/urls.py, server/mcp_tools/serializers.py, server/mcp_tools/technical_plan_service.py, server/tests/mcp_tools/test_blueprint_clarification_tools.py</files>
  <read_first>server/mcp_tools/views.py **L230-330 与 L4225-4330**（⭐ `McpToolView` 基类全文：`_begin` `:254-268`（`bind_source(MCP)` + `request.auth is None → 401`）/ `_validate` 的 400 `invalid_params` / ⭐ `_record` `:300-308` 的 `arecord_request_metric(route=f"mcp:{self.tool_name}")`（⇒ 指标自动满足）；⭐ `ReadBlueprintContextView` **全文 `:4231-4324`**：模块内 docstring 的「端点语义 → N 道兜底绝不绕过」编号清单体例（`:4237-4245`）、`tool_name` 类属性、`post` 三段式（`:4250-4260`）、`_handle` 的结构、异常兜底 `:4308-4324` 的 `redact_secrets_in_text(str(exc))[:500]` 与**绝不 5xx**）；server/mcp_tools/serializers.py **L825-840 与 L1085-1245**（`TOOL_SCHEMA_SNAPSHOT` 的起始与体例；⭐ `create_feishu_technical_plan` 条目 `:1092-1116` 的 **request 9 键 / response 12 键逐字**；两条蓝图条目 `:1224-1241` 的**键名逐字**与 ⭐ `:1229-1230` 的教训注释；请求 serializer 的既有写法（`serializers.Serializer` 子类、字段类型、`required=False` 的用法））；server/mcp_tools/urls.py **全文**（35 条 `path` 的命名与顺序惯例：`path("tools/<name>/", XxxView.as_view(), name="mcp-tool-<kebab>")`）；server/mcp_tools/technical_plan_service.py **L35-60 与 L400-500**（`:41-43` 三态 `status` → `McpWorkItemTechnicalPlan.Status` 的映射、`:405-420` `partial` + `orchestration_pending` 的 `retry_state` 既有形态、`:480-495` 响应装配段与 `:488` 的 `"project_id"` 键（⛔ 本 plan **不动它**））；server/tests/mcp_tools/test_schema_snapshot.py（⭐ 守门测试的**确切判据**：它怎么比对 snapshot 与实际响应键 —— 新工具与追加三键都要过它）；server/tests/mcp_tools/（113-02 两个蓝图工具的既有测试文件，作形状 analog）；server/delivery/services/blueprint_answer_action.py（Task 1 交付：`aanswer_thread` 的逐字签名与恒定返回键）；server/services/process_runtime/blueprint_render.py（116-05 交付：`render_blueprint_markdown` 签名）</read_first>
  <action>
**① `mcp_tools/views.py` 追加两个 `McpToolView` 子类（删除行 0）。**

两个类的**模块内 docstring 都写成契约书**（照 `:4237-4245` 的「N 道兜底绝不绕过」编号清单体例）。

**`GetTechnicalBlueprintView`**（`tool_name = "get_technical_blueprint"`）：
- 入参：`artifact_id`（**寻址键，⛔ 不是 `session_id`**；理由写进 docstring：既有 20 个蓝图端点全部以它为一级键并按它挂范围闸；同一 artifact 上可并存两条会话）。
- 三段式 `_begin` → `_validate(GetTechnicalBlueprintRequestSerializer)` → `_handle`。
- `_handle`：取 `Artifact` → **挂项目范围闸**（PAT 认证下 `request.user` 就是 token owner，`_ais_project_member` 照常成立；⭐ 复用 `blueprint_review_views._aassert_project_scope` 的**同源实现**，⛔ 不复制）→ 取最新版本 content → 装配响应：
  - `blueprint_status`（⚠️ ⛔ **响应字典键不得出现字面 `blueprint_status`**（INV-6 `_RE_FIELD_DICT_KEY` 扫全 `server/`）⇒ ⭐ **键名用 `current_status`**，与 115-01 立的既有解法一致；SUMMARY 里把这个命名决定写清楚，snapshot 条目也用 `current_status`）；
  - 六段摘要（每段的条目数与关键标题，⛔ 不塞整份 content）；
  - ⭐ `markdown`：`render_blueprint_markdown(content, blueprint_status=<真实值>)`（116-05，⛔ 不在 MCP 层拼、⛔ 不传 `""`）；
  - `pending_clarifications[]`：open+blocking 的 `BlueprintThread`（⭐ **不传 `kind`** —— `ai_clarification` 与 `repo_confirmation` 两类都算），每项 `{thread_id, kind, question, options, created_at}`，**问题正文过 `redact_secrets_in_text`**，显式 `order_by("created_at")`（`Meta` 无 `ordering`）。
- ⭐ **`pending_clarifications` 读失败 ⇒ 如实 503 + 中性 detail，响应体逐字不含 `items` / `total`**（P-12）；观测另包一层 `try/except: pass`；⭐ **业务主体绝不包进 best-effort**。

**`AnswerBlueprintClarificationView`**（`tool_name = "answer_blueprint_clarification"`）：
- 入参：`thread_id` + `body`（可选 `artifact_id` 作二次校验）。
- `_handle`：从 `thread_id` 反查 `artifact` → **挂范围闸** → `_aload_thread` 404 → ⭐ **调 Task 1 的 `aanswer_thread`**（⛔ 不直写 `BlueprintThread`、⛔ 不进程内自调 REST）→ 按恒定 `status` 映射成 MCP 响应：`answered` → 正常响应（⭐ **必须带 `reflow` 结构**，否则调用方无法区分「答案记下了但正文没更新」）/ `not_editable` / `not_answerable` / `invalid` → `error_response(code, detail)`。
- ⭐ **绝不 5xx**：整体兜底照 `:4308-4324`。

**② `mcp_tools/urls.py` 追加两条 `path`（删除行 0）**，`name` 为 `mcp-tool-get-technical-blueprint` / `mcp-tool-answer-blueprint-clarification`（kebab 惯例）。

**③ `mcp_tools/serializers.py`（删除行 ≤1）：**
- 追加两个请求 serializer（字段类型与 `required` 按既有写法）。
- ⭐ **`TOOL_SCHEMA_SNAPSHOT` 追加两条新条目**，`request` / `response` 的键名**逐字**与实际响应一致。
- ⭐ **`create_feishu_technical_plan` 条目的 `response` 列表追加三项** `blueprint_artifact_id` / `blueprint_status`（⚠️ 这是**响应键名字符串在列表里**，形态是 `"blueprint_status",` —— 后面既无 `=` 也无 `:` ⇒ INV-6 三条正则均不命中；⭐ **写之前手工验算**，若担心可与 View 侧统一用 `blueprint_current_status`，**执行期二选一但两侧必须一致**）/ `pending_clarifications`。⭐ **必须同步追加**——该文件 `:1229-1230` 的教训注释逐字说明：漏在 snapshot 里会让外部客户端按已发布契约以为它不存在。

**④ `mcp_tools/technical_plan_service.py` 响应追加三键（删除行 ≤4）：**
- ⭐ **仅在 mcp 开关为 `technical_blueprint` 时追加**（`aresolve_entry_process_type("mcp")`，字面量）；开关关闭时响应**逐字与改动前相同**。
- 追加 `blueprint_artifact_id` / `blueprint_status`（键名与 ③ 一致）/ `pending_clarifications[]`，并把 `status` 落 **`partial`**（既有三态之一，`:41-43` 的映射与 `:411-413` 的 `retry_state` 形态已支持 ⇒ 调用方零破坏）。
- ⛔ **不改 `:488` 的既有 `"project_id"` 键**（它回的是 Space id，是既有契约；蓝图侧的项目归属由 `blueprint_artifact_id` 对应的 artifact 承载）。

**⑤ 新建 `server/tests/mcp_tools/test_blueprint_clarification_tools.py`**，docstring「守十二件事」：

1. **两工具的 snapshot 条目存在**且 `request` / `response` 键集与实际响应**逐字一致**（复用 `test_schema_snapshot.py` 的判据或直接断言）。
2. ⭐ **`create_feishu_technical_plan` 的三个追加键在 snapshot 里**（防「漏在 snapshot 里」的那条教训重演）。
3. **未认证（`request.auth is None`）⇒ 401**（两工具各一条）。
4. ⭐ **非成员 ⇒ 中性 404**（范围闸生效，与 REST 面同源）。
5. ⭐ **对 `ai_review_finding` 线程调 `answer_blueprint_clarification` ⇒ 400 且线程状态一字未变**（从 DB **重读**断言 `status` / `updated_at` 均未变）—— 114-CR-01 的对称面，本文件头号靶子。
6. ⭐ **不可编辑态（`confirmed`）⇒ 400 且 DB 未动**（闸 ② 在写之前）：断言 `BlueprintThreadMessage` 计数不变。
7. **正路作答 ⇒ 响应含 `reflow`** 且 `reflow.status` 是五档之一；回灌抛异常时**仍不 5xx**、`reflow.status == "failed"`。
8. ⭐ **`get_technical_blueprint` 的 markdown 带未确认标注**（蓝图为 `pending_review` 时）且 `confirmed` 时不带 —— 证明它走的是 116-05 的 renderer 且传了真实状态。
9. ⭐ **`pending_clarifications` 含两类线程**（`ai_clarification` + `repo_confirmation`），且问题正文经脱敏（构造一条含 `sk-ant-xxx` 的题面，断言响应里已脱敏）。
10. ⭐ **`pending_clarifications` 读失败 ⇒ 503 且响应体逐字不含 `items` / `total`**（P-12）：patch 查询抛异常 ⇒ 断言状态码与响应键集。
11. ⭐ **`create_feishu_technical_plan` 开关两态**：mcp 开关 `technical_plan` ⇒ 响应键集与改动前**逐字相同**（⛔ 三个新键不出现）；开关 `technical_blueprint` ⇒ 三键出现且 `status == "partial"`。
12. **绝不 5xx**：两工具在内部异常时返回结构化错误而不是 500（patch 制造异常）。

⚠️ ⛔ **别把 `test_skills_snapshot_guard.py::test_skill_files_discovered` 的既有红当成本 plan 引入**（P-16）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/mcp_tools/test_blueprint_clarification_tools.py -x -q && uv run pytest tests/mcp_tools/ -q --deselect tests/mcp_tools/test_skills_snapshot_guard.py::test_skill_files_discovered</automated>
  </verify>
  <acceptance_criteria>
- `cd server && uv run pytest tests/mcp_tools/test_blueprint_clarification_tools.py -q` 全绿；`rg -c "def test_" server/tests/mcp_tools/test_blueprint_clarification_tools.py` ≥ **16**。
- ⭐ **契约快照守门全绿**：`cd server && uv run pytest tests/mcp_tools/test_schema_snapshot.py -q` 全绿；`cd server && uv run python -c "
from mcp_tools.serializers import TOOL_SCHEMA_SNAPSHOT as S
assert 'get_technical_blueprint' in S and 'answer_blueprint_clarification' in S
resp = S['create_feishu_technical_plan']['response']
for k in ('blueprint_artifact_id', 'pending_clarifications'):
    assert k in resp, k
print('snapshot entries OK')"` 输出 `snapshot entries OK`（⭐ 第三个状态键的**实际名字**按执行期定案，同时断言它在 `resp` 里）。
- ⭐ **既有 12 个响应键一个不少**：`cd server && uv run python -c "
from mcp_tools.serializers import TOOL_SCHEMA_SNAPSHOT as S
old = ['technical_plan_id','context_id','project_id','plan','markdown','repository_tasks','evidence','feishu_document','comment','status','retry_state','run_id']
resp = S['create_feishu_technical_plan']['response']
missing = [k for k in old if k not in resp]
assert not missing, missing
req = S['create_feishu_technical_plan']['request']
assert len(req) == 9, len(req)
print('backward compatible OK')"` 输出 `backward compatible OK`。
- ⭐ **⛔ MCP 层不直写线程、不自调 REST**：`rg -n "BlueprintThread\s*\(|BlueprintThreadMessage\s*\(|objects\.(a?create|a?update)" server/mcp_tools/views.py | rg -n "Blueprint"` **零命中**；`rg -n "reverse\(|requests\.|httpx\." server/mcp_tools/views.py | rg -c "blueprint"` == **0**；`rg -n "aanswer_thread" server/mcp_tools/views.py` 命中。
- ⭐ **范围闸复用不复制**：`rg -n "_aassert_project_scope" server/mcp_tools/views.py` 命中；`rg -c "async def _aassert_project_scope" server/mcp_tools/views.py` == **0**。
- ⭐ **markdown 走共享 renderer 且传真实状态**：`rg -n "render_blueprint_markdown" server/mcp_tools/views.py` 命中；`rg -n 'blueprint_status=""' server/mcp_tools/views.py` **零命中**。
- ⭐ **⛔ 无第三个 list 工具**：`rg -c "class .*BlueprintClarification.*View|tool_name = \"list_" server/mcp_tools/views.py` 的新增 View **恰为 2 个**（`rg -c "^class .*View\(McpToolView\)" server/mcp_tools/views.py` 相对改动前 **+2**）。
- ⭐ **INV-6 命名**：`rg -n "['\"]blueprint_status['\"]\s*:" server/mcp_tools/views.py server/mcp_tools/technical_plan_service.py` **零命中**；`cd server && uv run pytest tests/delivery/test_blueprint_inv6_guard.py -q` 全绿。
- ⭐ **开关关闭时响应逐字不变**：用例 11 绿；`cd server && uv run python -c "
import ast
t = ast.parse(open('mcp_tools/technical_plan_service.py').read())
found = False
for n in ast.walk(t):
    if isinstance(n, ast.Call):
        name = n.func.attr if isinstance(n.func, ast.Attribute) else getattr(n.func, 'id', '')
        if name == 'aresolve_entry_process_type':
            arg = n.args[0] if n.args else None
            assert isinstance(arg, ast.Constant), '开关实参必须是字面量'
            found = True
assert found, '缺开关查询'
print('switch literal OK')"` 输出 `switch literal OK`（⭐ 116-01 的 `ast` 守卫同时覆盖）。
- ⭐ **绝不 5xx**：`rg -n "redact_secrets_in_text" server/mcp_tools/views.py` 命中（新 View 的兜底段）；用例 12 绿。
- ⭐ **P-12**：用例 10 断言 503 响应体 `set(body)` **不含** `items` / `total`。
- **纯追加核算**：`git diff server/mcp_tools/views.py | rg -c "^-[^-]"` == **0**；`git diff server/mcp_tools/urls.py | rg -c "^-[^-]"` == **0**；`git diff server/mcp_tools/serializers.py | rg -c "^-[^-]"` ≤ **1**；`git diff server/mcp_tools/technical_plan_service.py | rg -c "^-[^-]"` ≤ **4**。
- ⛔ **MCP 公共 handler 工厂零改动**：`git diff --name-only | rg "knowledge_tools|handler_factory"` **为空**（若工厂在别的文件，按实读路径核算并登记）。
- ⚠️ `test_skills_snapshot_guard.py::test_skill_files_discovered` 仍红属**既有环境项**（P-16），SUMMARY 里显式登记「与改动前同一条」。
- `cd server && uv run ruff check mcp_tools/views.py mcp_tools/urls.py mcp_tools/serializers.py mcp_tools/technical_plan_service.py tests/mcp_tools/test_blueprint_clarification_tools.py` 通过。
  </acceptance_criteria>
  <done>两个新工具按 113-02 的四处落点范式落地且指标/鉴权由基类自动满足；寻址键是 `artifact_id`、⛔ 无第三个 list 工具；作答走 Task 1 的共享 service 因而三道闸自动继承（finding 400 且线程一字未变有用例）；`markdown` 走 116-05 的 renderer 并带未确认标注；`create_feishu_technical_plan` 只追加三键 + `status=partial` 且**同步进 snapshot**、开关关闭时响应逐字不变；`pending_clarifications` 读失败 503 且不含 `items`/`total`。</done>
</task>

<task type="auto">
  <name>Task 3: assumptions 三档（含 max_rounds 真实新增）+ blueprint_notify.py 澄清送达 + 守卫清单 + 全量后端门</name>
  <files>server/services/process_runtime/blueprint_ambiguity_score.py, server/services/process_runtime/blueprint_spec_gate.py, server/services/process_runtime/blueprint_notify.py, server/tests/delivery/test_blueprint_log_redaction_guard.py, server/tests/services/process_runtime/test_blueprint_assumptions_tiers.py, server/tests/services/process_runtime/test_blueprint_notify.py</files>
  <read_first>server/services/process_runtime/blueprint_ambiguity_score.py **L40-70 与 L230-275**（`DEFAULT_SPEC_GATE_CONFIG` 的**确切内容**（只有 `{threshold, weights}`）、`aload_spec_gate_config` **全文**（**无形参**、`copy.deepcopy` fallback、函数内懒 import、`_to_score` + clamp[0,1]、整段 try 回默认））；server/services/process_runtime/blueprint_spec_gate.py **L60-90 / L165-220 / L350-370 / L605-650**（⭐ `:73` `_MAX_SPEC_GATE_ROUNDS = 3` 的定义、⭐ `:175` 判定处与 `:183` 日志处的**两处使用**、`:205-215` 第一处 `aload_spec_gate_config()` 调用及其上下文、`:355-365` `_lock_spec` 内第二处调用、`_current_round:621-627` 读 `stage_state["spec_gate"]["round"]`、⭐ `_ambiguity_report(...)` **全文**（`ambiguity_report` 的唯一装配点，已带 `capped` / `release_reason` / `scorer_unavailable`）、`_goal_text:632` 与四维打分的三个输入）；server/system/models.py（116-01 声明的 `BLUEPRINT_ASSUMPTIONS_TIERS` 键名与 JSON 形状注释）；server/workflows/nodes/ai/plan_research.py **L425-520 全文**（⭐ `_send_clarify_card` 的**完整调用序**（`build_clarification_card` → `_resolve_space` → `_aresolve_project` → `ProjectService().resolve_or_create_group` → `FeishuIMService.create` → `send_card(receive_id=chat_id, receive_id_type="chat_id", card=card)`）、每一步「取不到就 `return`」的早退行（`:462` / `:469` / `:472` / `:480`）、`:490-491` 的 `# noqa: BLE001 — 发卡 best-effort，绝不反噬挂起`；⭐ `_acollect_round_questions:493-518` 与 **`:510-517` 的 `redact_secrets_in_text` 逐字**）；server/delivery/services/blueprint_review_action.py **L685-725**（⭐ `aremind_clarification_threads` 的收件人口径：`BlueprintReviewer` 名单 ∪ 会话 `created_by_id`、去重升序、**反查会话带 `process_type="technical_blueprint"` 过滤**）；server/feishu/cards/chat_question_card.py（`build_clarification_card` 的**逐字签名与入参形状**）；server/tests/services/process_runtime/test_blueprint_ambiguity_score.py 与 test_blueprint_spec_gate.py（既有用例 —— 本 Task 的零回归判据）；server/tests/test_blueprint_pending_reminder.py（送达/提醒测试的形状 analog）</read_first>
  <action>
**① `blueprint_ambiguity_score.aload_spec_gate_config` 加档位与 `max_rounds`（删除行 ≤6）。**

- 签名改 `async def aload_spec_gate_config(tier: str = "") -> dict[str, Any]` —— ⭐ **传 tier 字符串不是 session**（保持该函数无 ORM 依赖，它现在是纯配置读取）。
- `DEFAULT_SPEC_GATE_CONFIG` 追加 `"max_rounds": 3`（与 `_MAX_SPEC_GATE_ROUNDS` 的既有值一致 ⇒ **不传档位时行为逐字不变**）。
- 读取顺序：先读既有 `BLUEPRINT_SPEC_GATE_CONFIG` 得到 base（含新 `max_rounds`，`int()` 强转 + **下界 1**）；`tier` 非空时再读 `SettingKeys.BLUEPRINT_ASSUMPTIONS_TIERS`，取该档的 `{threshold, max_rounds}` **覆盖** base 对应项（逐字段强转 + clamp，形状照既有 `_to_score`）；档位未配置 / 畸形 / 档名不在三档内 ⇒ **回落 base** 并记一条 warning。
- 三档的**内置默认**（配置缺失时用它，⛔ 不硬编码在 spec_gate 里）：`strict`（更低阈值 = 更爱问，更多轮）/ `balanced`（= 现状 `threshold` 与 `max_rounds=3`，**默认档**）/ `assume_more`（更高阈值 = 更少问，轮数可更少）。⭐ 具体数值属 Discretion，但 **`balanced` 必须与现状逐字相等**（零回归）。
- 整段 `try/except` 回默认（⛔ 绝不外抛、绝不 eval，T-112-07 纪律）。

**② `blueprint_spec_gate.py` 两处调用带档位 + 两处 `_MAX_SPEC_GATE_ROUNDS` 改读配置（删除行 ≤6）。**

- 新增模块内 helper `def _assumptions_tier(session) -> str`：读 `stage_state["decomposition"]["assumptions_tier"]`，非三档之一回 `""`。
- ⭐ **`:211` 判定处与 `:359` `_lock_spec` 内的两处 `aload_spec_gate_config()` 都改成 `aload_spec_gate_config(tier=_assumptions_tier(session))`** —— 否则 `ambiguity_report` 里记的阈值与实际判定用的对不上（P-11 的第二半）。
- ⭐ `:175` 判定处改读 `config["max_rounds"]`；`:183` 日志处同改。⚠️ **`_MAX_SPEC_GATE_ROUNDS` 常量保留**作为 `DEFAULT_SPEC_GATE_CONFIG` 的取值来源（⛔ 不删——删了会让「配置缺失时的默认」失去单一事实源）。
- `_ambiguity_report(...)` 追加两个键：`assumptions_tier`（本次档位，`""` 表示默认）与 `max_rounds`（本次生效值）。⭐ **它是 `ambiguity_report` 的唯一装配点** ⇒ 加一次即全链留痕。

**③ 新建 `server/services/process_runtime/blueprint_notify.py`。**

模块 docstring 三段：(a) 用途——「蓝图澄清的**飞书卡片送达唯一收敛点**」；(b) ⭐ **收敛法自述**——「**同步点 1 之后换 107 的送达设施时只改这一个文件**」（形状照 `useBlueprintLive.ts` 的自述）；(c) best-effort——「整段 `try/except` 只 log，⛔ **失败只记事件绝不反噬挂起**」。

`_COMPONENT = "blueprint_notify"`；`__all__ = ["anotify_blueprint_clarification"]`。

```
async def anotify_blueprint_clarification(*, artifact, session, questions: list[dict], initiated_by_user_id: str = "system") -> None
```
调用序**逐字照** `_send_clarify_card`：
1. `questions` 逐条过 `redact_secrets_in_text`（形状逐字照 `plan_research.py:510-517` 的四键结构）；空列表即 `return`。
2. ⭐ **收件人**：`BlueprintReviewer` 名单 ∪ 蓝图会话 `created_by_id`，去重升序（口径抄 `blueprint_review_action.aremind_clarification_threads:700`）；⭐ **反查会话带 `process_type="technical_blueprint"` 过滤**。
3. `card = build_clarification_card(questions, ...)` → space（⭐ 由调用方传入或经 `meta.project_id → Project.space` 反查，⛔ **不依赖 `ExecutionContext`** —— 本模块是四个入口共用的模块级函数）→ `_aresolve_project(space)` → `ProjectService().resolve_or_create_group(project, member_ids, initiated_by_user_id)` → `FeishuIMService.create(space)` → `send_card(receive_id=chat_id, receive_id_type="chat_id", card=card)`。
4. 每一步「取不到就 `return`」的早退（照 analog `:462` / `:469` / `:472` / `:480`）；整段 `try/except Exception` 只 log（`# noqa: BLE001 — 发卡 best-effort，绝不反噬挂起`）。
5. 一条 `caller` 事件 `blueprint_clarification_card_sent`（`artifact_id` / `session_id` / `question_count` / `recipient_count` / `chat_id` / `duration_ms`）；失败记 `blueprint_clarification_card_failed`（`error` 过 `redact_secrets_in_text`）。⛔ **题面正文不进日志，只记条数**。

**接线点**：在 spec_gate 开出 blocking 澄清线程之后调它（执行期按实读的开线程位置接，⭐ **一处接线，⛔ 不在四个入口各接一次**）；整段 best-effort ⇒ 发卡失败**绝不**影响挂起。

**④ `_SCANNED_MODULES` 追加 1 行** `"services/process_runtime/blueprint_notify.py"`（删除行 0，与模块创建同 commit）。

**⑤ 新建两个测试文件。**

`test_blueprint_assumptions_tiers.py`（「守八件事」）：
1. **不传档位时行为逐字不变**：`aload_spec_gate_config()` 的 `threshold` 与改动前相同，`max_rounds == 3`。
2. **三档各自的 `threshold` / `max_rounds` 生效**（参数化三条）。
3. ⭐ **两处调用点口径一致**：跑一轮完整 spec_gate，断言 `ambiguity_report["threshold"]` 与实际判定用的阈值**相等**（P-11 的第二半；⛔ 不接受只测 `aload_spec_gate_config` 本身）。
4. ⭐ **`max_rounds` 真的生效**：`assume_more` 档配 `max_rounds=1` ⇒ 第 2 轮即 `capped`（⛔ 不再是硬编码的 3）。
5. ⭐ **`assume_more` ≠ `skip_clarification`**（本文件头号靶子）：`assume_more` 档下，构造一个歧义分**高于该档阈值**的需求 ⇒ 断言 (a) 四维打分**仍然执行**（`ambiguity_report` 里四个维度分数都在）、(b) **仍然开了 blocking 线程**。**并列反向对照**：同一需求在 `strict` 档下开更多线程 ⇒ 证明档位只改「问多少」不改「问不问这件事本身」。
6. **畸形配置回默认**：非 JSON / 非 dict / 档名不存在 / `max_rounds` 为 0 或负 ⇒ 回落且**不抛**。
7. `ambiguity_report` 里记了 `assumptions_tier` 与 `max_rounds`。
8. **既有 spec_gate 用例零回归**（`cd server && uv run pytest tests/services/process_runtime/test_blueprint_spec_gate.py tests/services/process_runtime/test_blueprint_ambiguity_score.py -q` 全绿）。

`test_blueprint_notify.py`（「守六件事」）：
1. **正路**：有 space + project + 收件人 ⇒ `send_card` 被调一次，`receive_id_type == "chat_id"`。
2. ⭐ **题面脱敏**：题面含 `sk-ant-xxx` ⇒ 传给 `build_clarification_card` 的实参已脱敏。
3. ⭐ **收件人口径**：`BlueprintReviewer` ∪ 会话发起人、去重升序；⭐ 反查会话时带 `process_type` 过滤（造一条同 artifact 的 `technical_plan` 会话作对照，断言它**不影响**收件人）。
4. ⭐ **best-effort**：`send_card` 抛异常 ⇒ 函数**不抛**、返回 None，且有一条 `blueprint_clarification_card_failed` 事件（`error` 已脱敏）。
5. **早退**：无 space / 无 project / 无收件人 / 空 questions ⇒ 各自早退且 `send_card` 零调用。
6. ⛔ **题面正文不进日志**：AST 断言日志 kwarg 里无 `question` / `questions` 原文实参。

**⑥ 全量后端门**（本 Task 收尾）：`cd server && uv run pytest tests/ -q` 与基线 **8609 passed / 1 failed** 逐条比对（⚠️ `test_skills_snapshot_guard` 那条是既有环境项）；`makemigrations --check --dry-run` 退出码 0 且 `git status --porcelain server/*/migrations/` 为空。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/process_runtime/test_blueprint_assumptions_tiers.py tests/services/process_runtime/test_blueprint_notify.py -x -q && uv run pytest tests/services/process_runtime/ tests/delivery/test_blueprint_log_redaction_guard.py -q</automated>
  </verify>
  <acceptance_criteria>
- 两个新测试文件全绿；`rg -c "def test_" server/tests/services/process_runtime/test_blueprint_assumptions_tiers.py` ≥ **10**；`rg -c "def test_" server/tests/services/process_runtime/test_blueprint_notify.py` ≥ **8**。
- ⭐ **`max_rounds` 真的进了配置**：`cd server && uv run python -c "
import os, django, asyncio, inspect
os.environ.setdefault('DJANGO_SETTINGS_MODULE','friday.settings'); django.setup()
from services.process_runtime.blueprint_ambiguity_score import aload_spec_gate_config, DEFAULT_SPEC_GATE_CONFIG
assert 'max_rounds' in DEFAULT_SPEC_GATE_CONFIG, 'DEFAULT 里必须有 max_rounds'
assert DEFAULT_SPEC_GATE_CONFIG['max_rounds'] == 3, '默认必须与现状逐字相等'
assert 'tier' in inspect.signature(aload_spec_gate_config).parameters
print('max_rounds config OK')"` 输出 `max_rounds config OK`。
- ⭐ **两处调用点都带上了档位**：`cd server && uv run python -c "
import ast
t = ast.parse(open('services/process_runtime/blueprint_spec_gate.py').read())
calls = [n for n in ast.walk(t) if isinstance(n, ast.Call) and (getattr(n.func,'id','') or getattr(n.func,'attr','')) == 'aload_spec_gate_config']
assert len(calls) >= 2, len(calls)
for c in calls:
    assert c.args or c.keywords, '每处调用都必须带档位实参'
print('two call sites tiered OK')"` 输出 `two call sites tiered OK`。
- ⭐ **`_MAX_SPEC_GATE_ROUNDS` 的两处使用改读配置**：`cd server && uv run python -c "
src = open('services/process_runtime/blueprint_spec_gate.py').read()
assert '_MAX_SPEC_GATE_ROUNDS' in src, '常量保留作默认来源'
import re
# 判定与日志两处不得再直接引用常量
i = src.index('def _'); # 粗定位后由人工核对
print('max rounds usage: %d' % src.count('_MAX_SPEC_GATE_ROUNDS'))"`，⭐ 输出的计数应为 **1**（只剩 `DEFAULT_SPEC_GATE_CONFIG` 的取值处；若为 2 需人工核对第二处是否合理并写进 SUMMARY）。
- ⭐ **`assume_more` ≠ `skip_clarification` 有正反并列**：`rg -n "assume_more" server/tests/services/process_runtime/test_blueprint_assumptions_tiers.py` 命中；`rg -n "strict" server/tests/services/process_runtime/test_blueprint_assumptions_tiers.py` 命中（反向对照）；⛔ `rg -n "skip_clarification" server/services/process_runtime/blueprint_ambiguity_score.py server/services/process_runtime/blueprint_spec_gate.py` **零命中**。
- ⭐ **既有 spec_gate / ambiguity 用例零回归**：`cd server && uv run pytest tests/services/process_runtime/test_blueprint_spec_gate.py tests/services/process_runtime/test_blueprint_ambiguity_score.py -q` 全绿。
- ⭐ **`blueprint_notify` 是一个文件且自述收敛法**：`cd server && uv run python -c "
src = open('services/process_runtime/blueprint_notify.py').read()
assert '同步点 1' in src, '模块 docstring 必须写明「同步点 1 后只改这一个文件」'
assert 'ExecutionContext' not in src.split('\"\"\"')[2] if src.count('\"\"\"') > 2 else True
print('notify convergence OK')"` 输出 `notify convergence OK`；`rg -n "ExecutionContext" server/services/process_runtime/blueprint_notify.py` **零命中**（模块级函数不依赖它）。
- ⭐ **脱敏与 best-effort**：`rg -n "redact_secrets_in_text" server/services/process_runtime/blueprint_notify.py` 命中；`rg -n "process_type" server/services/process_runtime/blueprint_notify.py` 命中（反查会话带过滤）；`cd server && uv run pytest tests/delivery/test_blueprint_log_redaction_guard.py -q` 全绿且 `rg -c "blueprint_notify" server/tests/delivery/test_blueprint_log_redaction_guard.py` == **1**。
- ⭐ **一处接线不是四处**：`rg -c "anotify_blueprint_clarification" server/ --glob '!tests/**'` == **2**（一个定义 + 一处调用）。
- **删除行核算**：`blueprint_ambiguity_score.py` ≤ **6**、`blueprint_spec_gate.py` ≤ **6**、`test_blueprint_log_redaction_guard.py` = **0**（逐行登记）。
- ⭐ **全量后端门**：`cd server && uv run pytest tests/ -q` 相对基线（8609 passed / 1 failed）**无新增失败** —— ⚠️ 逐条核对 `test_skills_snapshot_guard::test_skill_files_discovered` 是**同一条既有环境项**（P-16），并在 SUMMARY 显式登记。
- `cd server && uv run python manage.py makemigrations --check --dry-run` 退出码 **0**；`git status --porcelain server/*/migrations/` **为空**。
- 相位边界自检：`git diff --name-only` 只含本 plan 声明的 13 个文件；⛔ 不含六个冻结文件、`repo_router_v2.py`、`web/`。
- `cd server && uv run ruff check services/process_runtime/blueprint_ambiguity_score.py services/process_runtime/blueprint_spec_gate.py services/process_runtime/blueprint_notify.py tests/services/process_runtime/test_blueprint_assumptions_tiers.py tests/services/process_runtime/test_blueprint_notify.py` 通过。
  </acceptance_criteria>
  <done>assumptions 三档落地且 **`max_rounds` 这处真实新增**（配置化 + 两处使用改读配置 + 默认值与现状逐字相等）；⭐ **两处 `aload_spec_gate_config` 调用点都带上档位**，`ambiguity_report` 记的阈值与实际判定用的一致；`assume_more` 仍打分仍开线程有正反并列用例；澄清送达收敛进**一个** `blueprint_notify.py`（模块 docstring 自述「同步点 1 后只改这一个文件」、收件人口径抄 114-05 且反查带 `process_type` 过滤、题面脱敏、整段 best-effort），且**一处接线不是四处**；全量后端门相对基线无新增失败。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PAT 认证的 MCP 调用方 → 蓝图写入面 | 新增的第一个**非 SPA** 蓝图写入口；三道闸缺一即安全回归 |
| `kind == ai_review_finding` 的线程 | 回灌链落版本成功后会**无条件 `resolve_thread`** ⇒ 在 BLOCKER 上回一句任意文本就能解开 confirm 门 |
| 已 `confirmed` 及其后的蓝图 | 作答经回灌落新版本 ⇒ 越界即「确认锚定的内容被事后掉包且无痕」 |
| assumptions 档位配置 → 澄清行为 | 档位若被实现成「跳过澄清」，即原地复活 GATE-01 要消灭的东西 |
| LLM 产出的澄清题 → 飞书卡片 + 日志 | 半可信文本外发到 IM 与日志 |
| `TOOL_SCHEMA_SNAPSHOT` | 对外契约；漏项即外部客户端按错误的契约编程 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-116-48 | Elevation of Privilege | MCP 作答成为 finding 的**第二条后门** ⇒ 在 BLOCKER 上回一句任意文本即解开 confirm 门，绕开 `reason` 必填 / `[已修复]`-`[误报忽略]` 语义 / 「处置人：{uid}」留痕（114-CR-01 的对称面） | mitigate | 共享 service 里 finding 分流在 `record_answer` **之前** + 回灌链 `REFLOW_KINDS` 自身 fail-closed 的双重堵 + ⭐ 变异用例「400 且线程状态从 DB 重读一字未变」 |
| T-116-49 | Tampering | 已 `confirmed` 的蓝图经 MCP 作答被无声改写（114-MJ-04 的 MCP 面） | mitigate | `is_blueprint_editable` 闸在写之前 + 用例「400 且 `BlueprintThreadMessage` 计数不变」 |
| T-116-50 | Tampering | MCP 层直写 `BlueprintThread` 旁路 INV-6，或复刻一遍三道闸后与 REST 面漂移 | mitigate | 抽共享 service（唯一实现）+ INV-6 三条正则 + 源码扫描断言 MCP 层零 ORM 写、零进程内自调 REST |
| T-116-51 | Elevation of Privilege | MCP 工具缺项目范围闸 ⇒ 持任意 PAT 可读写任意项目的蓝图 | mitigate | 两工具都挂 `_aassert_project_scope`（import 复用同源实现）+ 非成员中性 404 用例 |
| T-116-52 | Repudiation | `assume_more` 档被实现成「跳过澄清」⇒ 原地复活 GATE-01 要消灭的 `skip_clarification` | mitigate | 档位只覆盖 `threshold` / `max_rounds`；⛔ 不碰 stage 流程；⭐ 变异用例「`assume_more` 下四维打分仍执行、超阈值仍开线程」+ `strict` 反向对照 + 源码 `skip_clarification` 零命中 |
| T-116-53 | Repudiation | `ambiguity_report` 记的阈值与实际判定用的不一致（两处调用只改了一处）⇒ 留痕撒谎，事后无法复盘为何这么问 | mitigate | ⭐ 两处调用都带档位的 `ast` 断言 + 端到端用例「report 的 threshold == 实际判定阈值」 |
| T-116-54 | Information Disclosure | 澄清题（LLM 产出）原样进飞书卡片与日志 ⇒ 半可信文本外发 | mitigate | 逐条过 `redact_secrets_in_text`（形状逐字照 analog）+ 日志只记条数 + 模块进 `_SCANNED_MODULES` |
| T-116-55 | Denial of Service | 发卡失败反噬挂起 ⇒ 一次 IM 抖动废掉整条澄清 | mitigate | 整段 best-effort（`try/except` 只 log）+ 每步早退 + 用例「`send_card` 抛异常时函数不抛」 |
| T-116-56 | Tampering | `pending_clarifications` 读失败被包成 200 空结构 ⇒ 调用方读成「没有待澄清」并据此推进 | mitigate | 如实 503 + 中性 detail + ⭐ 响应体逐字不含 `items` / `total` 的用例（P-12） |
| T-116-57 | Tampering | 追加的三键漏在 `TOOL_SCHEMA_SNAPSHOT` ⇒ 外部客户端按已发布契约以为它不存在（该文件自带的教训） | mitigate | snapshot 同步追加 + 用例断言三键在 snapshot 里 + 既有 12 个响应键一个不少的断言 |
| T-116-58 | Spoofing | 反查蓝图会话不带 `process_type` 过滤 ⇒ 收件人算到旧 `technical_plan` 会话的发起人 | mitigate | 收件人口径抄 114-05 且带过滤 + 用例造一条同 artifact 的 `technical_plan` 会话作对照 |
| T-116-SC | Tampering | 依赖供应链 | accept | 零新增外部依赖、零 migration、零新 `CallSource` 枚举、零新事件常量进 `BLUEPRINT_EVENTS`；MCP 公共 handler 工厂零改动；`makemigrations --check` 退出码 0 |
</threat_model>

<verification>
- `cd server && uv run pytest tests/delivery/ -q` 全绿（⭐ 含 `test_blueprint_review_views.py` **零改动**全绿 —— 「对外契约逐字不变」的唯一核算；含 INV-6 三条正则与脱敏守卫 18 项扫描面）。
- `cd server && uv run pytest tests/mcp_tools/ -q` 相对基线无新增失败（⚠️ `test_skills_snapshot_guard::test_skill_files_discovered` 是**既有**环境项，P-16，逐条核对后在 SUMMARY 登记）。
- `cd server && uv run pytest tests/services/process_runtime/ -q` 全绿（含既有 spec_gate 与 ambiguity 用例 —— 档位改造零回归的第一道证据）。
- ⭐ `cd server && uv run pytest tests/ -q` 相对基线（8609 passed / 1 failed）**无新增失败**。
- `cd server && uv run python manage.py makemigrations --check --dry-run` 退出码 0；相位内**零** migration。
- ⭐ **三条变异实跑**：① finding 线程作答 ⇒ 把 finding 分流挪到 `record_answer` 之后 ⇒ 用例转红；② `assume_more` ⇒ 把档位实现成跳过 spec_gate ⇒ 用例转红；③ 两处调用只改一处 ⇒ 「report 阈值 == 实际阈值」用例转红。红/绿输出首行贴进 SUMMARY。
- 冻结面核算：六个 `technical_plan` 冻结文件、`repo_router_v2.py`、MCP 公共 handler 工厂、`event_taxonomy.py`、`agents/call_source.py` —— `git diff` 逐个为空；`web/` 零命中。
- 删除行核算：八个受限文件逐个不超过 execution_context 表的上界。
</verification>

<success_criteria>
- **MCP 入口不再 skip_clarification**：`create_feishu_technical_plan` 立即返回 `status="partial"` + `blueprint_artifact_id` + pending 清单；`get_technical_blueprint` 可续取终稿（带六段摘要与**带未确认标注的** markdown）；`answer_blueprint_clarification` 可作答并返回 `reflow`。⭐ **既有工具名与 12 个响应键一个都没改**，开关关闭时响应逐字与改动前相同。
- ⭐ **作答通道只有一份实现**：REST 面与 MCP 面共享 `blueprint_answer_action.aanswer_thread`，三道闸的**先后顺序**有源码级断言；「对 finding 线程作答 ⇒ 400 且线程状态一字未变」把 114-CR-01 的对称面钉成事实。既有 answer 端点的测试文件**零改动全绿**证明对外契约没漂。
- ⭐ **assumptions 档位真的可运行时调**：`max_rounds` 这处 CONTEXT 以为「零新机制」的地方被识别为真实改动并落地；两处 `aload_spec_gate_config` 调用点都带档位，因而 `ambiguity_report` 记的阈值与实际判定用的一致（⛔ 留痕不撒谎）。
- ⭐ **`assume_more` 绝不等于 `skip_clarification`**：四维打分仍执行、超阈值仍开线程，有 `strict` 档的反向对照证明档位只改「问多少」。
- **CLAR-04 的另一半兑现**：澄清同时推飞书卡片，收件人 = `BlueprintReviewer` ∪ 会话发起人（反查带 `process_type` 过滤），题面脱敏、整段 best-effort、**一处接线**；且**收敛在一个文件里** —— 同步点 1 之后换 107 的送达设施只改它。
- 零 migration、零新依赖、零新 `CallSource` 枚举、零新事件常量、MCP 公共 handler 工厂逐字未动、前端零改动；全量后端门相对基线无新增失败。
</success_criteria>

<output>
完成后创建 `.planning/phases/116-entry/116-06-SUMMARY.md`，provides 至少包含：
- ⭐ **`blueprint_answer_action.aanswer_thread` 的逐字签名与恒定返回键**，以及**两个调用方各自的 status → 状态码 / MCP error 映射表**；三道闸的顺序与各自理由；
- ⭐ **「既有 answer 端点对外契约逐字不变」的核算证据**（`test_blueprint_review_views.py` 零改动 + 删除行逐行登记）；
- **两个新 MCP 工具的完整契约表**（tool_name / URL / `name` / request 键 / response 键逐字 / 错误码），以及 ⭐ **`current_status` 这个键名决定的理由**（INV-6 字段级守卫）；
- ⭐ **`create_feishu_technical_plan` 的追加三键 + `status=partial`** 与「开关两态下的响应键集对照」，以及 snapshot 同步追加的核算；
- ⭐ **assumptions 三档的完整配置表**（三档各自的 `threshold` / `max_rounds` 默认值），`balanced` 与现状逐字相等的核对，以及 ⭐ **`max_rounds` 这处真实新增的改动清单**（配置键 + 两处使用）；
- ⭐ **三条变异的红/绿实跑记录**（finding 闸顺序 / `assume_more` 不跳过 / 两处调用点口径一致）；
- `blueprint_notify.anotify_blueprint_clarification` 的签名、收件人口径、**唯一接线点**，以及 ⭐ **「同步点 1 后只改这一个文件」的收敛承诺**；
- 受限面删除行逐行登记；全量 `pytest tests/ -q` 与基线（8609/1）的差异比对，⚠️ **显式登记 `test_skills_snapshot_guard` 是同一条既有环境项**；
- ⭐ **GATE-01 的最终完成度自评**：本相位交付「四入口实现路径 + per-entry 开关 + MCP 异步澄清协议」，⏭ 顺延同步点 2 的只剩「默认切换 + 旧 process 收口 + 三处触点升级 + workflow 终态 HITL 挂起」——与 ROADMAP/REQUIREMENTS 的对账文案逐字对齐。
</output>
