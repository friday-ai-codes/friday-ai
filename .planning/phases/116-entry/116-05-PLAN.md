---
phase: 116-entry
plan: 05
type: execute
wave: 4
depends_on: ["116-04"]
files_modified:
  - server/services/process_runtime/blueprint_render.py
  - server/delivery/artifacts/builtin_types.py
  - server/delivery/api/artifact_serializers.py
  - server/delivery/api/blueprint_export_views.py
  - server/delivery/urls.py
  - server/tests/delivery/test_blueprint_log_redaction_guard.py
  - server/tests/services/process_runtime/test_blueprint_render.py
  - server/tests/delivery/test_blueprint_export_views.py
  - web/src/api/blueprints.ts
  - web/src/components/blueprint/BlueprintViewerHeader.vue
  - web/src/pages/knowledge/blueprints/[id].vue
  - web/src/locales/zh-CN.json
  - web/src/components/blueprint/__tests__/viewerHeaderExport.spec.ts
autonomous: true
requirements: [VIEW-05]
prohibitions:
  - "🔒 **`server/services/process_runtime/render.py` 零改动**（§13.2 冻结六文件之一，`git diff` 必须为空）：蓝图渲染器是**新文件** `blueprint_render.py`，判别分支加在 `builtin_types.py`（与 111 的 validator 分支同处同形）"
  - "⛔ ⭐ **`render_blueprint_markdown` 不得存在任何布尔开关参数**（CONTEXT：「给了早晚有人传 False」）：签名**恰为** `(content: dict, *, blueprint_status: str) -> str`。`blueprint_status` **必填 keyword-only、无默认值**；抑制集合是**闭合白名单** `{CONFIRMED, IMPLEMENTING, IMPLEMENTED}`，其余一切取值（含 `\"\"`、含未知字符串）**都渲染标注**。⭐ 唯一可机器验的形式是一条 `inspect.signature` 断言：参数名集合恰为 `{content, blueprint_status}`"
  - "⛔ **不改 `delivery/artifacts/registry.py`**：`ContentRenderer = Callable[[dict], str]`（`:16`）与 `render_markdown(artifact_type, content)`（`:65-70`）的签名一改就波及所有 artifact_type。注册表分支传 `blueprint_status=\"\"`（拿不到状态时 fail-safe 当作未确认），两个权威面**绕过注册表**直接调 renderer 并传真实状态"
  - "⛔ ⭐ **导出留痕绝不写进 `ArtifactVersion.content`**：`_content_hash` 是整份 content 的 canonical JSON sha256（`artifact_service.py:43-47`），`_add_version_sync` 在 hash 相等时复用 current 不翻版本（`:145-150`）⇒ 把 `exported_at` 写进 content 会让**每次导出翻一个版本**，版本历史被刷成噪声（114-04 已立此纪律）。⭐ 用例断言「导出前后 `ArtifactVersion` 计数不变」"
  - "⛔ ⭐ **导出事件不进 `BLUEPRINT_EVENTS`**（P-9）：该 frozenset 21 个常量被 `test_blueprint_event_taxonomy_112.py:111` 的 `len(...) == 21` 与 `:119` 的 `len(declared) == len(set(declared)) == 21` **双断言锁死**，且它同时是 115 `blueprint/events/` 端点的过滤集合 —— 加进去既转红又会把导出记录混进「阶段进展时间线」。留痕落 **Interaction Ledger + 独立 structlog caller 事件**"
  - "⛔ **上游失败如实回错，⛔ 不静默 200 空结构**（115-MJ-04 的反面教材）：飞书 API 失败 → **400 / 502 + 中性 detail**；异常文本过 `redact_secrets_in_text`；⛔ **不回显上游 body**。观测另包一层 `try/except: pass`，⭐ **业务主体绝不包进 best-effort**"
  - "⛔ **两个导出端点都挂 `_aassert_project_scope`**（import 复用 `blueprint_review_views` 的同源实现，⛔ 不造第四份）；⛔ 不引入 DRF serializer（本 View 家族全域手写 dict builder）；⛔ 不用 `rest_framework.views.APIView`（一律 `adrf.views.APIView`）"
  - "⛔ ⭐ **批注不导出**：`BlueprintThread` 本就不在 `content` 里（DESIGN §6.2）⇒ renderer 只读 content **天然满足**，⛔ **不写额外的过滤代码**（写了就是死码，且会让人误以为 content 里有批注）"
  - "⛔ **renderer 的块取文本按 `text` → `code.source` → `rows` 的字段优先级，完全不看 `block.type`**（与 `blueprint_anchor._block_text` 同源，115-RESEARCH P-13 已立为纪律）：按 type 分派会与锚点坐标系分叉"
  - "⛔ **不碰 0.19 归属的 `web/src/components/delivery/ArtifactTimeline.vue`**（`git diff` 必须为空）：`current_version_markdown` 的空壳问题**只修后端**（`artifact_serializers.py`）"
  - "⛔ **不新增 migration、不新增运行时依赖**；⚠️ **P-15**：跑完 `pnpm` 后 `git status` 检查并 `git checkout -- web/pnpm-workspace.yaml`；⚠️ `pnpm build` 会重写 `web/src/components.d.ts`（STATE 115-06 环境项），还原后按字典序手工插入自己那几行"
must_haves:
  truths:
    - "⭐ **`render_blueprint_markdown(content: dict, *, blueprint_status: str) -> str` 的三条不变量**（RESEARCH §C.2，本 SC 的第一个必须定的决策）：① `blueprint_status` **必填 keyword-only 无默认值** ⇒ 调用方无法省略；② 抑制集合是**闭合白名单** `{confirmed, implementing, implemented}`，**其余一切取值都渲染标注** ⇒ fail-safe 方向正确、没有任何取值能关掉它；③ ⛔ **不存在任何布尔开关参数**。⭐ **关键不变量是「没有任何取值能关掉标注」，而不是「只有一个调用点」**"
    - "⭐ **标注是文档第一行且无条件**：`blueprint_status ∉ 白名单` 时把「> ⚠️ 未经确认 —— 本方案尚未经人工终审（当前状态：X · 版本 vN）」写在**第一行**（`> ` blockquote 形态，飞书 markdown 可承载）"
    - "⭐ **两条变异用例**：(a) 把白名单改一个值（如去掉 `implementing`）⇒ 对应用例转红；(b) `inspect.signature(render_blueprint_markdown).parameters` 的键集**恰为** `{\"content\", \"blueprint_status\"}` ⇒ 任何人加 `include_watermark` 即转红。两条都要实跑一次真实变异"
    - "⭐ **`builtin_types._render_technical_plan` 的判别分支与 `_validate_technical_plan`（`:19-32`）逐字同构**：同一个懒 import（⛔ **不复制 `\"blueprint/v1\"` 字面量**，MN-10）、同一个判别式；蓝图分支调 `render_blueprint_markdown(content, blueprint_status=\"\")` —— 注册表签名拿不到状态，**传空串即「当作未确认」**。模块 docstring `:9` 那句「renderer 分支归 115/116，本相位不做」**同步改掉**"
    - "⭐ **P-4 的第二个面：`ArtifactTimelineSerializer.current_version_markdown`（`artifact_serializers.py:117-127`）加蓝图特判**——它是 `SerializerMethodField(obj: Artifact)`，**拿得到真实 `blueprint_status`** 但现在调 `render_markdown(...)` 在注册表这层被截断 ⇒ 给它加蓝图分支**直接调 `render_blueprint_markdown` 并传真实状态**，绕过注册表。⭐ **用例必须覆盖两个面**，且注册表分支传 `\"\"` 时断言标注**存在**。这同时把该字段「结构性空壳」的既有问题一并修掉（**后端修复，⛔ 不碰 `ArtifactTimeline.vue`**）"
    - "**导出内容 = 六段全量 + `requirement_spec` + `must_haves` + 决策记录附录 + 引用脚注**（RESEARCH §C.3 逐段核对表）：`requirement_spec`（`goal` / `background` block_list、`feature_points[]` 必有 `id/title/intent`）/ 仓库关联 / 现状分析 / 实现概述 / API 契约 / 影响范围 / 交互流程 / `must_haves`（`truths` / `artifacts` / `key_links`）"
    - "⭐ **`decision_log` 是零约束裸 array**（`blueprint_schema.py:733-736`，不在顶层 required、不在 `iter_blocks`）⇒ 114-04 写入的七个键是**约定不是契约**：逐项 `.get` 防御、缺键渲染「—」，**特别保 `answer` 与 `applied_in_version`**（§3.13 的存在意义就是「文档自包含、导出不丢决策」）"
    - "⭐ **`citations` 以每段末尾脚注形式给出**（title + 来源类型 + 可点链接）；**取不到链接就落 `title` / `quote` 快照，⛔ 不留白**。Citation 必有 `citation_id` / `source_type`，其余全可选"
    - "`[MEDIUM]` **markdown 版式定保守**（§C.4 A2 假设未解除）：heading ≤3 级、表格不嵌套、脚注用**普通列表**而非 `[^n]` 语法；⭐ **执行期用一次真实导出验证**并把观察到的版式退化记进 SUMMARY（退化不影响正确性与标注）"
    - "⭐ **两个新端点**：`POST /api/delivery/artifacts/<uuid:artifact_id>/blueprint/export-feishu/`（name=`blueprint-export-feishu`）与 `GET .../blueprint/export-feishu/availability/`（name=`blueprint-export-feishu-availability`）。**两个都挂 `_aassert_project_scope`**（import 复用）；未认证一律拒；非成员**中性 404 且与「artifact 不存在」逐字相同**"
    - "⭐ **availability 三判据镜像 `chat/views.py:1740-1781`**：① 无 space → `{available: false, reason: \"no_space\"}`；② `space.feishu_doc_folder_token` 空 → `no_folder_token`；③ space 级 `feishu_app_id + feishu_app_secret_encrypted` **或**系统级 `_aget_system_feishu_credentials_for_doc()` 任一可得 → `{available: true, reason: null}`，都没有 → `no_credentials`。⭐ **space 来源必须换**：从 `meta.project_id → Project.space` 反查（⛔ 不像 chat 那样有 `?space_id=`）。`{available, reason}` 两键逐字保持（前端据此**隐藏按钮**而不是点了才报错）"
    - "⭐ **导出执行体照 `feishu/coding_plan_exporter.export_coding_plan_to_feishu`（`:59-123`）**：拼一篇大 markdown → `create_feishu_doc_client_for_project(project)` → `doc_client.create_document(title, folder_token, content)` → 取 `document_id` / `url`。⭐ **失败一律 `FeishuDocAPIError` 上抛**（analog `:108-110` 只 log 不吞）—— 正合「如实回错、不静默 200」"
    - "⭐ **`400` vs `502` 的分档**：`[MEDIUM]` 执行期逐类核 `FeishuDocAPIError` / `RateLimitError` / `PERMISSION_CODES` / `NOT_FOUND_CODES`（`feishu_doc.py:175-183`）—— 配置类/权限类（folder 不存在、无权限）落 **400**，上游不可用/限流/超时落 **502**；两档的 `detail` 都是**中性文案**，⛔ 不含上游 body。分档结果写进 SUMMARY"
    - "⭐ **导出留痕落 Interaction Ledger + 一条独立 caller 事件**（`document_id` / `url` / `version_no` / `exported_by` / `exported_at`），⛔ **不写 content**、⛔ **不进 `BLUEPRINT_EVENTS`**。⭐ 用例断言「导出前后 `ArtifactVersion` 计数不变」"
    - "⭐ **前端「未经确认」横幅与后端判据逐字对齐**：`BlueprintViewerHeader.vue`（115 自建，0.20 归属可改）在**同一个闭合白名单**下渲染**常驻横幅**（⛔ 不可关闭、⛔ 不做 dismiss）；⭐ **前端也配一条变异用例**（把状态集合改一个值 ⇒ 转红）"
    - "⭐ **导出按钮按 availability 隐藏而不是点了才报错**：`availability.available === false` ⇒ 按钮不渲染（⛔ 不是 disabled + tooltip —— CONTEXT 逐字「前端据此隐藏按钮而不是点了才报错」）；点击后按状态码分档 toast（200 成功并给可点 url / 400 回显中性 detail / 502 提示稍后重试）"
    - "⭐ **组件只 emit、不发请求**（115-04 立的既有纪律）⇒ availability 查询与导出 mutation 归页面 `pages/knowledge/blueprints/[id].vue`（0.20 自建，**本 plan 显式登记这处改动**，删除行 0）"
    - "新建的 2 个模块（`blueprint_render.py` / `blueprint_export_views.py`）与模块创建**同 commit** 追加进 `test_blueprint_log_redaction_guard._SCANNED_MODULES`"
    - "全量后端门相对基线 **8609 passed / 1 failed** 无新增失败；前端门 `vitest` 相对基线 **1674 passed / 1 skipped** 无新增失败、`type-check` exit 0、`build` 通过、`lint` 在触及文件上零新增问题；`makemigrations --check --dry-run` 退出码 0"
  artifacts:
    - path: "server/services/process_runtime/blueprint_render.py"
      provides: "render_blueprint_markdown(content, *, blueprint_status)：不可关闭的「未经确认」首行标注（闭合白名单）+ 十段全量 + decision_log 附录（逐项 .get 防御）+ citations 脚注（取不到链接落 title/quote）+ _md_escape 与零行补「—」；⛔ 零布尔开关参数"
      contains: "blueprint_status"
    - path: "server/delivery/artifacts/builtin_types.py"
      provides: "_render_technical_plan 的 schema_version 判别分支（与 _validate_technical_plan 逐字同构、懒 import 常量、蓝图分支传 blueprint_status=\"\"）+ 模块 docstring 占位话术改写"
      contains: "render_blueprint_markdown"
    - path: "server/delivery/api/artifact_serializers.py"
      provides: "current_version_markdown 的蓝图特判：直接调 render_blueprint_markdown 并传 obj.blueprint_status 的真实值（绕过注册表），一并修掉该字段的结构性空壳"
      contains: "render_blueprint_markdown"
    - path: "server/delivery/api/blueprint_export_views.py"
      provides: "两端点（POST 导出 / GET availability）：adrf APIView + IsAuthenticated + import 复用 _aassert_project_scope + 手写 dict + availability 三判据（space 从 meta.project_id 反查）+ 上游失败 400/502 中性 detail + 留痕进 Interaction Ledger 与 caller 事件（⛔ 不写 content、⛔ 不进 BLUEPRINT_EVENTS）"
      contains: "_aassert_project_scope"
    - path: "server/tests/services/process_runtime/test_blueprint_render.py"
      provides: "⭐ inspect.signature 参数名集合恰为 {content, blueprint_status} + 白名单三态各一条 + \"\" 与未知串都出标注 + 白名单变异转红 + decision_log 缺键渲染「—」+ citations 无链接落快照 + 块取文本四分支优先级 + 批注天然不出现"
      contains: "inspect.signature"
    - path: "server/tests/delivery/test_blueprint_export_views.py"
      provides: "两端点未认证拒 + 非成员中性 404 与「不存在」逐字相同 + availability 三 reason 各一例 + 上游抛 FeishuDocAPIError ⇒ 400/502 且 detail 不含上游 body + ⭐ 导出前后 ArtifactVersion 计数不变 + 导出事件不在 BLUEPRINT_EVENTS 里"
      contains: "ArtifactVersion"
    - path: "web/src/components/blueprint/BlueprintViewerHeader.vue"
      provides: "「未经确认」常驻横幅（与后端同一闭合白名单、不可关闭）+ 导出按钮（availability false 时不渲染，只 emit）"
      contains: "blueprint-unconfirmed-banner"
    - path: "web/src/components/blueprint/__tests__/viewerHeaderExport.spec.ts"
      provides: "横幅在三个已确认态不出现、其余八态与未知串都出现 + ⭐ 前端白名单变异转红 + 导出按钮按 availability 隐藏 + 只 emit 不发请求"
      contains: "blueprint-unconfirmed-banner"
  key_links:
    - from: "server/delivery/artifacts/builtin_types.py"
      to: "server/services/process_runtime/blueprint_render.py"
      via: "懒 import BLUEPRINT_SCHEMA_VERSION 判别 + render_blueprint_markdown(content, blueprint_status=\"\")（注册表拿不到状态 ⇒ fail-safe 当作未确认）"
      pattern: "render_blueprint_markdown"
    - from: "server/delivery/api/artifact_serializers.py"
      to: "server/services/process_runtime/blueprint_render.py"
      via: "蓝图特判直接调 renderer 并传 obj 的真实 blueprint_status（绕过 Callable[[dict], str] 注册表）"
      pattern: "render_blueprint_markdown"
    - from: "server/delivery/api/blueprint_export_views.py"
      to: "server/delivery/api/blueprint_review_views.py"
      via: "import 复用 _aassert_project_scope / _aload_artifact / _ARTIFACT_MISSING_DETAIL（MJ-03 单一实现，既有文件零改动）"
      pattern: "from delivery.api.blueprint_review_views import"
    - from: "server/delivery/api/blueprint_export_views.py"
      to: "server/services/feishu_doc.py"
      via: "create_feishu_doc_client_for_project(project) → create_document(title, folder_token, markdown)；失败 FeishuDocAPIError 上抛后映射 400/502"
      pattern: "create_document"
    - from: "web/src/components/blueprint/BlueprintViewerHeader.vue"
      to: "server/services/process_runtime/blueprint_render.py"
      via: "「未经确认」的同一个闭合白名单（三个状态字面量逐字对齐，两侧各配变异用例）"
      pattern: "confirmed"
---

<objective>
让蓝图**可导出、且未确认版本在两个面上都逃不掉标注**——同时把 `ArtifactTimelineView.current_version_markdown` 的结构性空壳一并修掉（它与导出物共用同一个渲染器，这是有意为之）。

起点（RESEARCH §0 实测）：`delivery/artifacts/builtin_types.py` 的 renderer 对蓝图 content 调的是 v0 的 `render_merged_plan_markdown`（读 `title` / `summary` / `execution_plan` 顶层键）⇒ 蓝图的导出物与 `current_version_markdown` 现在都是**结构性空壳**；该文件 docstring `:9` 逐字写着「renderer 分支归 115/116」。蓝图链**零飞书集成**。

三处「会静默假通过」的地方，逐条钉死：

① ⭐ **renderer 注册契约拿不到 `blueprint_status`**（RESEARCH §C.2，CONTEXT 的设计与它不兼容）。`ContentRenderer = Callable[[dict], str]`（`registry.py:16`），而「未确认」判据的唯一事实源 `Artifact.blueprint_status` **不在 content 里**。解法保住 CONTEXT 那条不变量的**实质**：`blueprint_status` 做成**必填 keyword-only** + 抑制集合是**闭合白名单**（`""` 与任何未知串都渲染标注）+ 注册表分支传 `""`（fail-safe 方向正确）+ 两个权威面绕过注册表传真实状态。⭐ **关键不变量是「没有任何取值能关掉标注」，不是「只有一个调用点」**；唯一可机器验的形式是一条 `inspect.signature` 断言。

② ⭐ **标注在时间线面丢失**（P-4）。若只在导出端点传真实状态、注册表分支保持原样 `return render_merged_plan_markdown(content)`，`ArtifactTimelineView.current_version_markdown` 就**仍是空壳且无标注**——而它是**已经上线、已经有前端消费者**的面（`ArtifactTimeline.vue:192-196`）。⇒ 用例覆盖**两个**面，且注册表分支传 `""` 时断言标注**存在**。

③ ⭐ **留痕写进 content ⇒ 版本历史被刷成噪声**。`_content_hash` 是整份 content 的 canonical JSON sha256，写入 `exported_at` 会让每次导出都翻一个版本（114-04 已为时间戳立过同款纪律）。⇒ 留痕落 Interaction Ledger + 独立 caller 事件，⛔ 不进 `BLUEPRINT_EVENTS`（`len == 21` 双断言锁死，且它同时是 115 `blueprint/events/` 端点的过滤集合——加进去会把导出记录混进「阶段进展时间线」）。用例断言「导出前后 `ArtifactVersion` 计数不变」。

Purpose: VIEW-05 全量交付（导出含决策记录附录 + 未确认版本界面与导出物均带标注），且导出与时间线共用同一个渲染器——⛔ **不为赶工在导出器里就地拼 markdown**，那会让空壳问题留着、两处口径立刻分叉。
Output: 2 个新建源文件 + 3 处既有后端改造 + 1 处守卫清单追加 + 2 个新建后端测试 + 4 处前端（1 新建 spec + 3 处 0.20 自建件改造）。**零 migration、零新依赖、零新事件常量进 `BLUEPRINT_EVENTS`。**
</objective>

<execution_context>
工作目录：worktree 根 `/Users/zaneliu/Projects/open-source/friday-ai/.claude/worktrees/v0.20-blueprint`，分支 `milestone/v0.20.0-blueprint`。⛔ 绝不读写主检出。

**wave 4，依赖 116-04**：功能上不依赖 SC-4，但与 116-04 **共同改 `tests/delivery/test_blueprint_log_redaction_guard.py`**（`_SCANNED_MODULES`）⇒ 文件重叠即隐式依赖，必须排在其后的波次。本 plan 追加的两行必须**与两个模块的创建在同一个 commit**（`_violations:69-70` 直接 `read_text()` 不兜 `FileNotFoundError`）。

**测试基线**：后端 **8609 passed / 1 failed**（唯一失败 `test_skills_snapshot_guard::test_skill_files_discovered` 是 worktree 环境产物）；前端 **1674 passed / 1 skipped**、`type-check` exit 0、`build` 通过、`eslint .` **111 problems**（判据是**触及文件零新增问题**）。

**受限面配额（硬约束）**：

| 文件 | 允许的改动 | 删除行上界 |
|---|---|---|
| `server/delivery/artifacts/builtin_types.py`（全文 **45 行**） | `_render_technical_plan` 加判别分支 + 模块 docstring `:9` 改写 | **3** |
| `server/delivery/api/artifact_serializers.py` | `current_version_markdown`（`:117-127`）加蓝图特判 | **2** |
| `server/delivery/urls.py` | import 追加 + `urlpatterns` 追加 **2** 项 + 一行分组注释 | **0** |
| `server/tests/delivery/test_blueprint_log_redaction_guard.py` | `_SCANNED_MODULES` 追加 **2** 行 | **0** |
| `web/src/api/blueprints.ts` | 追加 2 个函数 + `export default` 里追加 2 个名字 | **1**（default 导出块的收尾行） |
| `web/src/components/blueprint/BlueprintViewerHeader.vue` | 追加横幅 + 导出按钮 + 一个 prop 与一个 emit | **4** |
| `web/src/pages/knowledge/blueprints/[id].vue` | availability query + 导出 mutation + toast 分档 + 传新 prop / 接新 emit | **4** |
| `web/src/locales/zh-CN.json` | 新键纯追加 | **0** |
| `server/delivery/api/blueprint_review_views.py` | **零改动**（`git diff` 必须为空） | — |
| `server/services/process_runtime/render.py` | **零改动**（冻结） | — |
| `web/src/components/delivery/ArtifactTimeline.vue` | **零改动**（0.19 归属） | — |

⚠️ 签名与字段名一律按下列**实测**取，不得猜测：

- `delivery/artifacts/builtin_types.py` **全文 45 行**：`_validate_technical_plan:19-32`（懒 import `BLUEPRINT_SCHEMA_VERSION` + `validate_blueprint`，MN-10 注释逐字）、`_render_technical_plan:35-38`（现无条件 `render_merged_plan_markdown`）、模块 docstring `:9` 的占位话术。
- `delivery/artifacts/registry.py:16` `ContentRenderer = Callable[[dict], str]`、`:65-70` `render_markdown(artifact_type, content)`（**两者都不改**）。
- `delivery/api/artifact_serializers.py:117-127` `current_version_markdown`（`SerializerMethodField`，`obj` 是 `Artifact` ⇒ **拿得到 `blueprint_status`**）。
- `delivery/models/artifact.py:45-63` `blueprint_status` 的 **11 态 TextChoices**（白名单三个成员的**确切字面量**从这里取）。
- `feishu/coding_plan_exporter.py:59-123` `export_coding_plan_to_feishu`（大 markdown → `create_feishu_doc_client_for_project` → `create_document` → `document_id` / `url`；`:108-110` 失败**只 log 不吞**）；`:184-244` `_compose_plan_markdown`（`parts: list[str]` + `_build_xxx_table` + **零行补 `| — | — |`** `:215-217`）；`:242-244` `_md_escape`（`|` → `\|`、换行 → 空格）—— ⭐ **复制一份到 `blueprint_render.py`**（`process_runtime` ⛔ 不反向依赖 `feishu/`）。
- `services/feishu_doc.py:335+` `FeishuDocClient.create_document(title, folder_token, content)`（内部 `markdown_to_blocks`）；`:175-183` `FeishuDocAPIError` / `RateLimitError` / `PERMISSION_CODES` / `NOT_FOUND_CODES`。
- `agents/tools/feishu_doc_tools.create_feishu_doc_client_for_project` 与 `_aget_system_feishu_credentials_for_doc`。
- `chat/views.py:1740-1781` `FeishuExportAvailabilityView` 三判据全文（⚠️ analog 用 `ChatAuthPermission` 且从 `?space_id=` 取，**两处都要换**）。
- `delivery/api/blueprint_doc_views.py:47-57` 的 import 复用说明书体例、`:206-216` `_log` 五件套、`:239-285` 端点骨架（`started = time.monotonic()` 第一行 → `_aload_artifact` → 404 → `_aassert_project_scope` → 装配 → `_log` → `Response`）。
- `blueprint_review_views.py:254` `_aassert_project_scope` / `:100` `_aload_artifact` / `_ARTIFACT_MISSING_DETAIL`。
- `blueprint_schema.py`：`:96-107` 九种 `source_type`；`:733-736` `decision_log` **零约束裸 array**；六段与 `must_haves` / `requirement_spec` 的字段（RESEARCH §C.3 表）。
- `blueprint_anchor._block_text` 的**字段优先级** `text` → `code.source` → `rows`（**完全不看 `block.type`**）。
- `delivery/services/event_taxonomy.py:185-208` `BLUEPRINT_EVENTS`（frozenset，**21**）+ 双断言 `test_blueprint_event_taxonomy_112.py:111,119`。
- `interactions/entry.py` 的 `begin_interaction_run` 范式（留痕落 Interaction Ledger）。
- `delivery/urls.py:141-160` 的分组注释体例与 `artifacts/<uuid:artifact_id>/` 分组位置。
- `web/src/api/blueprints.ts` 尾部 `export default { ... }` 的名字清单。
- `web/src/components/blueprint/BlueprintViewerHeader.vue`（115 自建）：`:48` 的「一律以人审快照响应体的 `current_status` 为准」注释、`:133` 的 `BlueprintStatusBadge`、`:206` 的 `current-status` 传参 —— ⭐ **横幅目前不存在，本 plan 新增**。
</execution_context>

<context>
@.planning/phases/116-entry/116-RESEARCH.md（⭐ §C.1 判别分支落点、⭐ §C.2 renderer 签名冲突与解法全节、§C.3 十段 + 附录的数据来源逐段核对表、§C.4 exporter 范式 / availability 三判据 / 留痕两个禁区、P-4 / P-9、给 PLAN 的第 6 条）
@.planning/phases/116-entry/116-PATTERNS.md（C1 组装形态 analog 与 ⭐ 签名契约表 + 其它 DIFFER 六条、C2 逐字同构 + P-4 第二个面、C3/C4/C5 View 骨架与导出面四条必须 DIFFER、C6/C7、§0.2 冻结面表、§0.4 追加点 20/21/22/31/33/35、No Analog #3、Shared Patterns S1/S2/S3/S6）
@.planning/phases/116-entry/116-CONTEXT.md（飞书导出与「未经确认」标注一节；specifics 第 3 条与「导出与渲染共用一个 renderer 是有意为之」一条）
@server/delivery/artifacts/builtin_types.py（**全文 45 行**）
@server/delivery/artifacts/registry.py（**全文 71 行**：`:16` 类型别名、`:65-70` `render_markdown` —— 两者都不改）
@server/delivery/api/artifact_serializers.py（`:100-140` `current_version_markdown` 与相邻字段）
@server/feishu/coding_plan_exporter.py（`:59-123` 导出执行体、`:184-244` markdown 组装与 `_md_escape`）
@server/services/feishu_doc.py（`:170-200` 错误类型、`:330-380` `create_document`）
@server/chat/views.py（`:1740-1781` availability 三判据全文）
@server/delivery/api/blueprint_doc_views.py（**View 骨架 analog**：`:47-57` import 复用说明书、`:206-216` `_log`、`:239-285` 端点骨架与手写 dict）
@server/services/process_runtime/blueprint_schema.py（六段字段、`:733-736` `decision_log`、Citation 可选性）
@server/delivery/services/event_taxonomy.py（`:185-208` `BLUEPRINT_EVENTS` —— **只读，⛔ 不加**）
@server/interactions/entry.py（`begin_interaction_run` 留痕范式）
@server/tests/delivery/test_blueprint_doc_views.py（端点测试 analog：范围闸正反并列、中性 404 逐字相同）
@web/src/components/blueprint/BlueprintViewerHeader.vue（**全文**：props/emits、`currentStatus` 的来源、`BlueprintStatusBadge` 的位置、操作区结构）
@web/src/pages/knowledge/blueprints/[id].vue（**只读为主**：动作端点的 toast 分档范式、`invalidateQueries({queryKey:['blueprint']})` 前缀失效纪律、⛔ 零乐观更新）
@.planning/phases/115-ui/115-04-SUMMARY.md（§3 六端点接线契约 —— 本 plan 的导出动作照同一形态：组件只 emit、页面发请求 + toast 分档）
@.planning/phases/116-entry/116-04-SUMMARY.md（`_SCANNED_MODULES` 的当前项数 —— 本 plan 在其后追加 2 行）
</context>

<tasks>

<task type="auto">
  <name>Task 1: blueprint_render.py（不可关闭标注 + 十段 + 决策附录 + 引用脚注）+ builtin_types 判别分支 + serializer 蓝图特判 + 守卫清单</name>
  <files>server/services/process_runtime/blueprint_render.py, server/delivery/artifacts/builtin_types.py, server/delivery/api/artifact_serializers.py, server/tests/delivery/test_blueprint_log_redaction_guard.py, server/tests/services/process_runtime/test_blueprint_render.py</files>
  <read_first>server/feishu/coding_plan_exporter.py **L180-250**（⭐ 组装形态 analog 全文：`_compose_plan_markdown` 的 `parts: list[str]` + `"\n".join(parts)`、每个表格一个 `_build_xxx_table` 私有函数返回 `"| 列 | 列 |\n| --- | --- |\n…"`、**零行补一行 `| — | — |`** `:215-217`、`_md_escape` `:242-244` 的**确切实现**（`|` → `\|`、换行 → 空格））；server/delivery/artifacts/builtin_types.py **全文 45 行**（⭐ `_validate_technical_plan:19-32` 的**逐字同构模板**：`if isinstance(content, dict) and content.get("schema_version"):` → 函数内懒 import `BLUEPRINT_SCHEMA_VERSION, validate_blueprint` → `if content.get("schema_version") == BLUEPRINT_SCHEMA_VERSION: return validate_blueprint(content)` → 回落 v0；MN-10 注释逐字；`_render_technical_plan:35-38` 现状；模块 docstring `:9` 的占位话术）；server/delivery/artifacts/registry.py **全文 71 行**（`:16` `ContentRenderer` 类型别名、`:65-70` `render_markdown` —— 确认**为何不能改签名**）；server/delivery/api/artifact_serializers.py **L95-140**（`current_version_markdown` 的 `SerializerMethodField` 全文、`obj` 的类型、它怎么取 current version、相邻字段的写法）；server/delivery/models/artifact.py **L40-70**（⭐ `blueprint_status` 的 **11 态 TextChoices 的确切成员名与字面量** —— 白名单三个成员从这里取，⛔ 不猜）；server/services/process_runtime/blueprint_schema.py **L140-760**（六段 + `requirement_spec` + `must_haves` 的**逐字段必填/可选**（对照 RESEARCH §C.3 表核一遍）、`:733-736` ⭐ **`decision_log` 是零约束裸 array**、`:96-107` 九种 `source_type`、Citation 的必填 `citation_id`/`source_type` 与全可选的 `source_id`/`locator`/`quote`/`title`）；server/delivery/services/blueprint_anchor.py（⭐ `_block_text` 的**字段优先级** `text` → `code.source` → `rows` 与它**完全不看 `block.type`** 的实现 —— renderer 逐字同源）；server/tests/delivery/test_blueprint_log_redaction_guard.py **L27-44**</read_first>
  <action>
**① 新建 `server/services/process_runtime/blueprint_render.py`。**

模块 docstring 四段：(a) 用途——「`blueprint/v1` → markdown 的**唯一**渲染器；导出物与 `ArtifactTimelineView.current_version_markdown` **共用它**（⛔ 不在导出器里就地拼 markdown —— 那会让时间线面的空壳问题留着、两处口径立刻分叉）」；(b) ⭐ **不可关闭的标注契约**（三条不变量逐字写进去 + 「关键不变量是『没有任何取值能关掉标注』而不是『只有一个调用点』」）；(c) 块取文本口径——「按 `text` → `code.source` → `rows` 的**字段优先级**，**完全不看 `block.type`**（与 `blueprint_anchor._block_text` 同源；按 type 分派会与锚点坐标系分叉）」；(d) ⛔ **批注不导出是天然满足的**（`BlueprintThread` 本就不在 content 里）⇒ **不写过滤代码**。

`_COMPONENT = "blueprint_render"`；`__all__ = ["render_blueprint_markdown"]`。

**签名与白名单（⭐ 本 SC 的第一个必须定的决策，⛔ 执行期不得改选）：**

```
_SUPPRESS_WATERMARK_STATUSES: frozenset[str] = frozenset({
    <BlueprintStatus.CONFIRMED 的字面量>,
    <IMPLEMENTING 的字面量>,
    <IMPLEMENTED 的字面量>,
})

def render_blueprint_markdown(content: dict, *, blueprint_status: str) -> str:
```
- `blueprint_status` **必填 keyword-only、无默认值**。
- `str(blueprint_status or "") not in _SUPPRESS_WATERMARK_STATUSES` ⇒ **无条件**把标注写在 `parts[0]`：
  `> ⚠️ 未经确认 —— 本方案尚未经人工终审（当前状态：{status_label} · 版本 v{version_no}）`
  ⚠️ `version_no` 不在 content 里 ⇒ 从 `content["meta"]` 取（若 schema 无该字段则**省略版本片段**，⛔ 不编造、⛔ 不因此改 schema）。`status_label` 用状态字面量本身（⛔ 不引前端 i18n）。
- ⛔ **不存在任何布尔开关参数**。

**组装（照 analog 的形状）**：`parts: list[str]` + `"\n".join(parts)`；每个表格一个 `_build_*_table` 私有函数；**零行补一行 `| — | — |`**；`_md_escape` **复制一份**（`process_runtime` ⛔ 不反向依赖 `feishu/`）；缺字段一律降级 `"—"`，⛔ 不留白。

**十段 + 附录（RESEARCH §C.3 逐段）**：`meta.title` 作 H1 → 未确认标注（已在首行）→ 需求规格（`goal` / `background` block_list 扁平、`feature_points[]` 表格含 `id/title/intent` 与可选 `acceptance_criteria`）→ 仓库关联 → 现状分析 → 实现概述 → API 契约 → 影响范围 → 交互流程 → 验收锚点（`must_haves` 三块）→ ⭐ **决策记录附录**（`decision_log` 逐条渲染「问题 / 结论 / 决策人 / 生效版本」，⭐ **逐项 `.get` 防御、缺键渲染「—」、特别保 `answer` 与 `applied_in_version`** —— 那七个键是约定不是契约）→ ⭐ **引用脚注**（每段末尾以**普通列表**给出该段引用的 `title` + 来源类型 + 可点链接；**取不到链接就落 `title` / `quote` 快照，⛔ 不留白**）。

`[MEDIUM]` **版式保守**：heading **≤3 级**、表格不嵌套、脚注用普通列表而非 `[^n]` 语法（`markdown_to_blocks` 的表达力上界未逐行核到，§C.4 A2）。

**② `builtin_types._render_technical_plan` 加判别分支（删除行 ≤3）。**

改成与 `_validate_technical_plan:19-32` **逐字同构**：同一个 `if isinstance(content, dict) and content.get("schema_version"):` 前置、同一处函数内懒 import（⛔ **不复制 `"blueprint/v1"` 字面量**）、命中即 `return render_blueprint_markdown(content, blueprint_status="")`，否则回落 `render_merged_plan_markdown(content)`。⭐ 传 `""` 的那一行**必须带注释**：「注册表签名 `Callable[[dict], str]` 拿不到状态；`\"\" ∉ 抑制白名单` ⇒ **当作未确认**，fail-safe 方向正确。两个权威面（导出端点 / `ArtifactTimelineSerializer`）绕过注册表传真实状态。」模块 docstring `:9` 的「renderer 分支归 115/116，本相位不做」**同步改掉**。

**③ `artifact_serializers.current_version_markdown` 加蓝图特判（删除行 ≤2）。**

取到 current version 的 `content` 后：若 `isinstance(content, dict) and content.get("schema_version") == BLUEPRINT_SCHEMA_VERSION`（懒 import）⇒ **直接** `return render_blueprint_markdown(content, blueprint_status=str(getattr(obj, "blueprint_status", "") or ""))`；否则维持既有 `render_markdown(obj.artifact_type, content)`。⭐ 注释写明这是 **P-4 的第二个面**：该字段已上线、已有前端消费者（`ArtifactTimeline.vue:192-196`），⛔ **只修后端、不碰组件**。
⚠️ ⛔ **响应键/字典键不得出现字面 `blueprint_status`**（INV-6 `_RE_FIELD_DICT_KEY`）—— 这里是 `getattr(obj, "blueprint_status", ...)` 的**属性读**形态，`_RE_FIELD_WRITE` 的 `[^=]` 与 `_RE_FIELD_DICT_KEY` 均不命中；写之前手工验算三条正则。

**④ `_SCANNED_MODULES` 追加 1 行** `"services/process_runtime/blueprint_render.py"`（删除行 0，与模块创建同 commit；另一行 `blueprint_export_views.py` 由 Task 2 追加）。

**⑤ 新建 `server/tests/services/process_runtime/test_blueprint_render.py`**，docstring「守十件事」：

1. ⭐ **`inspect.signature` 参数名集合恰为 `{content, blueprint_status}`**（P-4 的唯一可机器验形式）：`assert set(inspect.signature(render_blueprint_markdown).parameters) == {"content", "blueprint_status"}`，且 `blueprint_status` 的 `kind == KEYWORD_ONLY` 且 `default is inspect.Parameter.empty`。
2. ⭐ **白名单三态不出标注**（参数化三条，字面量从 `BlueprintStatus` 取）。
3. ⭐ **其余一切取值都出标注**（参数化：其余八个态 + `""` + `"totally_unknown"` + `None`（经 `str(x or "")` 归一））—— ⛔ 这一组必须包含 `""`。
4. ⭐ **标注在第一行**：`rendered.splitlines()[0]` 含「未经确认」。
5. ⭐ **白名单变异**：把 `_SUPPRESS_WATERMARK_STATUSES` 去掉 `implementing` ⇒ 用例 2 的 `implementing` 那条转红（**实跑一次并记 SUMMARY**）。
6. **`decision_log` 缺键渲染「—」**：一条只有 `question` 的记录 ⇒ 渲染出「—」且**不抛**；一条七键齐全的 ⇒ `answer` 与 `applied_in_version` **都出现在输出里**。
7. **`citations` 无链接落快照**：一条只有 `citation_id` + `source_type` + `quote` 的 citation ⇒ 输出含 `quote` 片段，⛔ 不留白。
8. ⭐ **块取文本四分支优先级**：分别构造 `{text}` / `{code:{source}}` / `{rows}` / 三者都无 的 block，断言取值顺序与 `blueprint_anchor._block_text` 的 fixture **逐字一致**；⭐ 再加一条：`{"type": "pseudocode", "text": "..."}` ⇒ **仍按 `text` 取**（⛔ 证明没按 type 分派）。
9. **批注天然不出现**：给 content 之外造几条 `BlueprintThread` ⇒ 渲染输出里**零出现**（⛔ 且源码里没有过滤批注的代码 —— 源码扫描断言 `BlueprintThread` 在 `blueprint_render.py` 里零命中）。
10. ⭐ **两个面都带标注**（P-4）：(a) 经注册表 `render_markdown("technical_plan", blueprint_content)` ⇒ 输出**含标注**（因为分支传 `""`）；(b) 经 `ArtifactTimelineSerializer` 拿一份 `confirmed` 的蓝图 ⇒ 输出**不含标注**且**六段结构在**（⛔ 不是 v0 空壳）；(c) 同一份蓝图状态为 `pending_review` ⇒ 输出**含标注**。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/process_runtime/test_blueprint_render.py -x -q && uv run pytest tests/delivery/test_blueprint_log_redaction_guard.py tests/delivery/test_blueprint_event_taxonomy_112.py -q</automated>
  </verify>
  <acceptance_criteria>
- `cd server && uv run pytest tests/services/process_runtime/test_blueprint_render.py -q` 全绿；`rg -c "def test_" server/tests/services/process_runtime/test_blueprint_render.py` ≥ **14**。
- ⭐ **签名不变量（唯一可机器验的形式）**：`cd server && uv run python -c "
import os, django, inspect
os.environ.setdefault('DJANGO_SETTINGS_MODULE','friday.settings'); django.setup()
from services.process_runtime.blueprint_render import render_blueprint_markdown as f
p = inspect.signature(f).parameters
assert set(p) == {'content','blueprint_status'}, set(p)
assert p['blueprint_status'].kind == inspect.Parameter.KEYWORD_ONLY
assert p['blueprint_status'].default is inspect.Parameter.empty, '必填无默认值'
print('renderer signature invariant OK')"` 输出 `renderer signature invariant OK`。
- ⭐ **没有任何取值能关掉标注**：`cd server && uv run python -c "
import os, django
os.environ.setdefault('DJANGO_SETTINGS_MODULE','friday.settings'); django.setup()
from services.process_runtime.blueprint_render import render_blueprint_markdown as f, _SUPPRESS_WATERMARK_STATUSES as W
c = {'schema_version':'blueprint/v1','meta':{'title':'t','project_id':'p'},'requirement_spec':{'goal':[],'feature_points':[]},'repo_associations':[],'current_state_analysis':[],'implementation_overview':{'requirement_narrative':[],'items':[]},'api_contracts':[],'impact_analysis':{'business_impact':[],'affected_features':[]},'interaction_flows':[],'must_haves':{'truths':[],'artifacts':[],'key_links':[]},'citations':{}}
for s in ['', 'unknown', 'drafting', 'pending_review']:
    assert '未经确认' in f(c, blueprint_status=s).splitlines()[0], s
for s in W:
    assert '未经确认' not in f(c, blueprint_status=s), s
print('watermark cannot be disabled OK')"` 输出 `watermark cannot be disabled OK`。
- ⭐ **⛔ 零布尔开关参数**：`rg -n "include_watermark|show_watermark|with_watermark|skip_watermark" server/services/process_runtime/blueprint_render.py` **零命中**。
- ⭐ **判别分支与 validator 逐字同构且不复制字面量**：`rg -n '"blueprint/v1"' server/delivery/artifacts/builtin_types.py` **零命中**；`rg -c "BLUEPRINT_SCHEMA_VERSION" server/delivery/artifacts/builtin_types.py` ≥ **2**（validator + renderer 两个分支）；`rg -n "renderer 分支归 115/116" server/delivery/artifacts/builtin_types.py` **零命中**（占位话术已改掉）。
- ⭐ **P-4 两个面**：`rg -n "render_blueprint_markdown" server/delivery/artifacts/builtin_types.py server/delivery/api/artifact_serializers.py` **两个文件各命中**；`rg -n 'blueprint_status=""' server/delivery/artifacts/builtin_types.py` 命中（注册表分支传空串）。
- ⭐ **⛔ 不改 registry / 冻结 render.py / 0.19 组件**：`git diff server/delivery/artifacts/registry.py` **为空**；`git diff server/services/process_runtime/render.py` **为空**；`git diff web/src/components/delivery/ArtifactTimeline.vue` **为空**。
- ⭐ **块取文本不按 type 分派**：`cd server && uv run python -c "
src = open('services/process_runtime/blueprint_render.py').read()
import re
i = src.find('def _block_text')
assert i >= 0, '缺 _block_text'
body = src[i:i+900]
assert '\"type\"' not in body and \"'type'\" not in body, '⛔ 不得按 block.type 分派'
print('block text priority OK')"` 输出 `block text priority OK`。
- ⭐ **⛔ 无批注过滤死码**：`rg -n "BlueprintThread|thread" server/services/process_runtime/blueprint_render.py` 的命中**只允许**落在 docstring 的说明句（逐条人工核对并写进 SUMMARY）。
- ⭐ **白名单变异实跑一次并登记**：SUMMARY 里写「去掉 `implementing` ⇒ 用例 X 转红（贴错误首行）；恢复即绿」。
- **删除行核算**：`git diff server/delivery/artifacts/builtin_types.py | rg -c "^-[^-]"` ≤ **3**；`git diff server/delivery/api/artifact_serializers.py | rg -c "^-[^-]"` ≤ **2**；`git diff server/tests/delivery/test_blueprint_log_redaction_guard.py | rg "^-[^-]"` **为空**。
- **`BLUEPRINT_EVENTS` 未被动**：`cd server && uv run pytest tests/delivery/test_blueprint_event_taxonomy_112.py -q` 全绿；`git diff server/delivery/services/event_taxonomy.py` **为空**。
- `cd server && uv run ruff check services/process_runtime/blueprint_render.py delivery/artifacts/builtin_types.py delivery/api/artifact_serializers.py tests/services/process_runtime/test_blueprint_render.py` 通过；`uv run ruff format --check services/process_runtime/blueprint_render.py tests/services/process_runtime/test_blueprint_render.py` 通过。
  </acceptance_criteria>
  <done>渲染器就位且三条不变量各有断言（必填 keyword-only / 闭合白名单让任何取值都关不掉标注 / ⛔ 零布尔开关，`inspect.signature` 断言 + 白名单变异实跑）；判别分支与 validator 逐字同构且不复制字面量；⭐ **两个面都带标注**——注册表路径传 `""` 出标注、时间线序列化器传真实状态且六段结构在（空壳一并修掉）；块取文本与锚点坐标系同源、批注天然不出现且无死码。</done>
</task>

<task type="auto">
  <name>Task 2: blueprint_export_views.py 两端点（导出 + availability）+ urls 两条路由 + 守卫清单 + 端点测试</name>
  <files>server/delivery/api/blueprint_export_views.py, server/delivery/urls.py, server/tests/delivery/test_blueprint_log_redaction_guard.py, server/tests/delivery/test_blueprint_export_views.py</files>
  <read_first>server/delivery/api/blueprint_doc_views.py **全文 444 行**（⭐ **View 骨架逐字 analog**：模块 docstring 的五段契约书体例、⭐ `:47-57` **import 复用私有符号的说明书注释**、`from adrf.views import APIView` 与逐 View 的 `permission_classes`、模块级三类常量、`:206-216` `_log` 五件套、`:239-285` 端点骨架（`started = time.monotonic()` 第一行 → `_aload_artifact` → 中性 404 → `_aassert_project_scope` → 装配 → `_log` → `Response`）、⭐ **手写 dict 响应零 DRF serializer**）；server/chat/views.py **L1735-1790**（⭐ availability 三判据全文：`no_space` / `no_folder_token` / space 级凭证 / 系统级 `_aget_system_feishu_credentials_for_doc()` / `no_credentials`，以及 `{available, reason}` 两键的**逐字形态** —— ⚠️ analog 用 `ChatAuthPermission` 且从 `?space_id=` 取，**两处都要换**）；server/feishu/coding_plan_exporter.py **L55-130**（导出执行体全文：`create_feishu_doc_client_for_project(project)` → `doc_client.create_document(title, folder_token, content)` → `document_id` / `url` 的取法、⭐ `:108-110` 失败**只 log 不吞**（`FeishuDocAPIError` 上抛））；server/services/feishu_doc.py **L165-200 与 L325-390**（⭐ `FeishuDocAPIError` / `RateLimitError` 的定义与 `PERMISSION_CODES` / `NOT_FOUND_CODES` 的**确切成员** —— 400/502 分档依据；`create_document` 签名与内部 `markdown_to_blocks`）；server/agents/tools/feishu_doc_tools.py（`create_feishu_doc_client_for_project` 与 `_aget_system_feishu_credentials_for_doc` 的签名与失败返回）；server/interactions/entry.py（`begin_interaction_run` 的**逐字签名与必填字段** —— 导出留痕落它）；server/delivery/urls.py **L1-45 与 L141-200**（import 块字母序与括号换行体例、`artifacts/<uuid:artifact_id>/` 分组、`blueprint-gate/` 与 `blueprint-review/` 分组注释的**确切措辞纪律**与「字面段写在前面」的顺序纪律）；server/delivery/services/event_taxonomy.py **L180-215**（⭐ **只读**：`BLUEPRINT_EVENTS` 21 个常量 —— 确认导出事件**不加进去**）；server/tests/delivery/test_blueprint_doc_views.py **L1-80**（端点测试 analog：`pytestmark`、范围闸工厂的复用、非成员 404 与「不存在」逐字相同的断言写法）</read_first>
  <action>
**① 新建 `server/delivery/api/blueprint_export_views.py`（2 端点）。**

模块 docstring 五段（照 analog 体例）：(a) 端点清单表；(b) ⭐ 授权判据——「两端点都 `IsAuthenticated` + `_aassert_project_scope`（**import 复用，绝不复制第四份**）」；(c) 为什么新建文件；(d) ⭐ **留痕两个禁区**——「⛔ 绝不写 `ArtifactVersion.content`（`_content_hash` 是整份 content 的 sha256，写 `exported_at` 会让每次导出翻一个版本）；⛔ 绝不把导出事件加进 `BLUEPRINT_EVENTS`（`len == 21` 双断言锁死，且它是 115 `blueprint/events/` 端点的过滤集合）」；(e) 观测口径——「⛔ 蓝图正文、上游 body 一律不进日志；异常文本过 `redact_secrets_in_text`」。

常量：`_COMPONENT = "blueprint_export_api"`；中性 404 常量**一并 import 复用** `_ARTIFACT_MISSING_DETAIL`（硬要求：本模块的「artifact 不存在」404 必须与闸内产出的非成员 404 **逐字相同**）；`_UPSTREAM_UNAVAILABLE_DETAIL` / `_EXPORT_CONFIG_DETAIL` 两个中性上游失败文案；`_log(event, request, artifact_id, started, **fields)` 在本文件内再声明一份。

**`BlueprintExportAvailabilityView.get(request, artifact_id)`**：`started = time.monotonic()` 第一行 → `_aload_artifact` → 中性 404 → `_aassert_project_scope` → ⭐ **space 从 `meta.project_id → Project.space` 反查**（取不到 ⇒ `{"available": False, "reason": "no_space"}`）→ 三判据镜像 `chat/views.py:1758-1781` → `_log("blueprint_export_availability_read", ..., available=..., reason=...)` → `Response({"available": ..., "reason": ...})`。⭐ **两键逐字保持**（前端据此隐藏按钮）。

**`BlueprintExportFeishuView.post(request, artifact_id)`**：
1. 前置三步同上。
2. 取当前版本（`order_by("-version_no").afirst()` 口径；⚠️ 支持可选 body `{"version_id": ...}` 取历史版本，非 UUID ⇒ 400、取不到 ⇒ 404）。
3. ⭐ **渲染走 Task 1 的 renderer 并传真实状态**：`markdown = render_blueprint_markdown(content, blueprint_status=str(getattr(artifact, "blueprint_status", "") or ""))`。⛔ **不在本文件拼 markdown**。
4. availability 不满足 ⇒ **400** + `_EXPORT_CONFIG_DETAIL`（⛔ 不 500、⛔ 不静默 200）。
5. 导出：`client = await create_feishu_doc_client_for_project(project)` → `result = await client.create_document(title, folder_token, markdown)` → 取 `document_id` / `url`。
   ⭐ **异常分档**（`[MEDIUM]`，执行期逐类核 `feishu_doc.py:175-183` 后定案并写进 SUMMARY）：配置/权限/资源不存在类（`PERMISSION_CODES` / `NOT_FOUND_CODES`）⇒ **400 + `_EXPORT_CONFIG_DETAIL`**；限流 / 超时 / 其它上游错误 ⇒ **502 + `_UPSTREAM_UNAVAILABLE_DETAIL`**。两档的 `detail` **都是中性常量**，异常原文只经 `redact_secrets_in_text(str(exc))[:500]` **进日志**，⛔ **不进响应体**。
   ⭐ **业务主体绝不包进 best-effort**（115-MJ-04）：这一段的 `try/except` 是**为了分档回错**，不是为了吞掉。
6. ⭐ **留痕**：`begin_interaction_run(...)`（Interaction Ledger）+ 一条 caller 事件 `blueprint_exported_to_feishu`（`artifact_id` / `version_no` / `document_id` / `url` / `exported_by` / `duration_ms`）。⛔ **不写 content、⛔ 不进 `BLUEPRINT_EVENTS`、⛔ 不产 `ConvergenceSessionEvent`**。
7. `Response({"document_id": ..., "url": ..., "version_no": ..., "exported_at": ...})`。

**② `delivery/urls.py` 追加两条路由（删除行 0）**：import 按字母序追加；在 `blueprint-review/` 分组**之后**追加一行分组注释 + 两条 `path`。注释要点：「116-05 蓝图飞书导出（VIEW-05）：`blueprint/export-feishu/` 与 `.../availability/` 两个整段精确匹配；与 115-01 的 `blueprint/` 与 `blueprint/events/` 互不遮挡（**字面段写在前面**的既有顺序纪律）」。`name` 为 `blueprint-export-feishu` / `blueprint-export-feishu-availability`。

**③ `_SCANNED_MODULES` 追加 1 行** `"delivery/api/blueprint_export_views.py"`（删除行 0，与模块创建同 commit）。

**④ 新建 `server/tests/delivery/test_blueprint_export_views.py`**，`pytestmark = pytest.mark.django_db(transaction=True)`，docstring「守十件事」：

1. **两端点未认证一律拒**（401/403，参数化两条）。
2. ⭐ **范围闸正反并列**：成员 200 / **非成员 404 且响应体与「artifact 不存在」的 404 逐字相同**（`assert a.json() == b.json()`）/ superuser 200。**两端点各跑一遍**。
3. **读不到 `meta.project_id` ⇒ 400**（复用闸的既有语义）。
4. ⭐ **availability 三 reason 各一例**：无 space → `no_space`；有 space 无 folder token → `no_folder_token`；有 folder token 但无任何凭证 → `no_credentials`；space 级凭证齐备 → `{available: true, reason: null}`；仅系统级凭证 → 同样 `true`。⭐ **两键逐字断言**（`set(body) == {"available", "reason"}`）。
5. **availability 不满足时 POST ⇒ 400**（⛔ 不 500、⛔ 不 200 空结构）。
6. ⭐ **上游失败分档**：patch `create_document` 抛权限类 `FeishuDocAPIError` ⇒ **400**；抛限流/超时类 ⇒ **502**；⭐ **两档都断言响应体 `detail` 不含上游 body 的任何片段**（构造一个含 `secret-token-xyz` 的上游错误文本，断言它**不出现在响应里**）。
7. ⭐ **导出前后 `ArtifactVersion` 计数不变**（留痕不进 content —— 本文件头号靶子）；且 `artifact.current_version_id` 不变。
8. ⭐ **导出事件不在 `BLUEPRINT_EVENTS` 里**：断言 `"blueprint_exported_to_feishu" not in BLUEPRINT_EVENTS` 且 `len(BLUEPRINT_EVENTS) == 21`（与既有双断言同源）；再断言导出后 `ConvergenceSessionEvent` 计数**不变**（⛔ 不污染阶段时间线）。
9. **成功路径**：返回 `document_id` / `url` / `version_no` / `exported_at` 四键；渲染内容里**含未确认标注**（蓝图为 `pending_review` 时）且**不含**（`confirmed` 时）—— ⭐ 经 patch 捕获传给 `create_document` 的 markdown 实参来断言。
10. ⭐ **一次真实导出验证**（`[MEDIUM]` A2）：**手工执行一次**（有飞书凭证的环境下）或在 SUMMARY 里明确登记「未具备凭证，版式退化风险未消解」——⛔ 不许默默跳过。观察到的版式退化（heading 层级 / 表格 / 列表）逐条记进 SUMMARY。
  </action>
  <verify>
    <automated>cd server && uv run python -c "
import os, django
os.environ.setdefault('DJANGO_SETTINGS_MODULE','friday.settings'); django.setup()
from django.urls import reverse
a='11111111-1111-1111-1111-111111111111'
for n in ('blueprint-export-feishu','blueprint-export-feishu-availability'):
    print(n, reverse(n, args=[a]))
" && uv run pytest tests/delivery/test_blueprint_export_views.py -x -q && uv run pytest tests/delivery/ -q</automated>
  </verify>
  <acceptance_criteria>
- `reverse` 输出两条 URL 且形如 `/api/delivery/artifacts/<uuid>/blueprint/export-feishu/` 与 `.../export-feishu/availability/`。
- `cd server && uv run pytest tests/delivery/test_blueprint_export_views.py -q` 全绿；`rg -c "def test_" server/tests/delivery/test_blueprint_export_views.py` ≥ **16**。
- ⭐ **范围闸 import 复用不是复制**：`rg -n "from delivery.api.blueprint_review_views import" server/delivery/api/blueprint_export_views.py` 命中且清单含 `_aassert_project_scope` 与 `_ARTIFACT_MISSING_DETAIL`；`rg -c "async def _aassert_project_scope" server/delivery/api/blueprint_export_views.py` == **0**；`git diff server/delivery/api/blueprint_review_views.py` **为空**。
- ⭐ **adrf + 零 serializer**：`rg -n "from adrf.views import APIView" server/delivery/api/blueprint_export_views.py` 命中；`rg -n "from rest_framework.views import APIView|serializers|Serializer" server/delivery/api/blueprint_export_views.py` **零命中**；`rg -c "permission_classes = \[IsAuthenticated\]" server/delivery/api/blueprint_export_views.py` == **2**。
- ⭐ **留痕不进 content**：`rg -n "add_version|ArtifactVersion.objects.create|\.content\s*\[" server/delivery/api/blueprint_export_views.py` **零命中**；用例 7 绿。
- ⭐ **导出事件不进 taxonomy**：`git diff server/delivery/services/event_taxonomy.py` **为空**；`cd server && uv run pytest tests/delivery/test_blueprint_event_taxonomy_112.py -q` 全绿；`rg -n "BLUEPRINT_EVENTS" server/delivery/api/blueprint_export_views.py` **零命中**。
- ⭐ **⛔ 不静默 200、⛔ 不回显上游 body**：`cd server && uv run python -c "
src = open('delivery/api/blueprint_export_views.py').read()
assert 'redact_secrets_in_text' in src, '异常文本必须脱敏'
assert 'HTTP_502_BAD_GATEWAY' in src or 'status=502' in src, '缺 502 分档'
assert 'HTTP_400_BAD_REQUEST' in src or 'status=400' in src, '缺 400 分档'
print('error mapping OK')"` 输出 `error mapping OK`；用例 6 的「响应体不含 `secret-token-xyz`」断言绿。
- ⭐ **渲染走共享 renderer 且传真实状态**：`rg -n "render_blueprint_markdown" server/delivery/api/blueprint_export_views.py` 命中；`rg -n 'blueprint_status=""' server/delivery/api/blueprint_export_views.py` **零命中**（导出端点必须传真实状态）。
- ⭐ **availability 两键逐字**：`rg -n '"available"|"reason"' server/delivery/api/blueprint_export_views.py` 命中；用例 4 的 `set(body) == {"available","reason"}` 断言绿。
- **纯追加核算**：`git diff server/delivery/urls.py | rg "^-[^-]"` **为空**；`git diff server/tests/delivery/test_blueprint_log_redaction_guard.py | rg "^-[^-]"` **为空**；`rg -c "blueprint_export_views" server/tests/delivery/test_blueprint_log_redaction_guard.py` == **1**。
- ⭐ **脱敏守卫全绿**：`cd server && uv run pytest tests/delivery/test_blueprint_log_redaction_guard.py -q`（此时扫描面含两个新模块）。
- ⭐ **400/502 分档依据已逐类核过**：SUMMARY 里给出 `FeishuDocAPIError` / `RateLimitError` / `PERMISSION_CODES` / `NOT_FOUND_CODES` → 状态码的**对照表**（`[MEDIUM]` 假设在此解除）。
- `cd server && uv run ruff check delivery/api/blueprint_export_views.py delivery/urls.py tests/delivery/test_blueprint_export_views.py` 通过；`uv run ruff format --check delivery/api/blueprint_export_views.py tests/delivery/test_blueprint_export_views.py` 通过。
  </acceptance_criteria>
  <done>两个导出端点就位并逐条遵循本 View 家族惯例（adrf / `IsAuthenticated` / import 复用范围闸 / 手写 dict / 中性 404 / caller 埋点 / 正文不进日志）；availability 三判据镜像 chat 面但 space 从 `meta.project_id` 反查、两键逐字；上游失败按 400/502 如实回错且响应体不含上游 body、异常文本脱敏；⭐ 留痕落 Interaction Ledger 与独立 caller 事件——导出前后 `ArtifactVersion` 计数不变、`BLUEPRINT_EVENTS` 与 `ConvergenceSessionEvent` 均未被污染。</done>
</task>

<task type="auto">
  <name>Task 3: 前端「未经确认」常驻横幅（同源判据 + 变异用例）+ 导出按钮按 availability 隐藏 + 前后端双门</name>
  <files>web/src/api/blueprints.ts, web/src/components/blueprint/BlueprintViewerHeader.vue, web/src/pages/knowledge/blueprints/[id].vue, web/src/locales/zh-CN.json, web/src/components/blueprint/__tests__/viewerHeaderExport.spec.ts</files>
  <read_first>web/src/components/blueprint/BlueprintViewerHeader.vue **全文**（props/emits 的**逐字定义**、`:48` 的「一律以人审快照响应体的 `current_status` 为准」注释、`:133` `BlueprintStatusBadge` 的位置、`:206` 的 `current-status` 传参、操作区（终审按钮等）的结构与 `Separator` 分隔、`data-testid` 命名惯例）；web/src/pages/knowledge/blueprints/[id].vue **L1-120 与动作处理段**（⭐ **六端点接线的既有形态**：mutation 定义、`invalidateQueries({ queryKey: ['blueprint'] })` 前缀失效、⛔ 零乐观更新、toast 分档的写法与 i18n 键组织；`BlueprintViewerHeader` 的挂载点与已传的 props/已接的 emits）；web/src/api/blueprints.ts **全文尾部**（既有函数的 `get`/`post` 用法与 `export default { ... }` 的名字清单 —— 追加两个函数与两个名字）；web/src/components/blueprint/__tests__/reviewActions.spec.ts（组件 spec 的形状 analog：`mountWith`、手写最小 i18n 键树（⚠️ **那类 spec 不 import `zh-CN.json`，缺键会让断言读到键名**）、`data-testid` 定位）；web/src/locales/zh-CN.json（`knowledge.blueprints.*` 子树的组织 —— 新键追加位置）；web/src/__tests__/blueprint-source-guard.spec.ts（⭐ **只读**：六条源码守卫的**确切判据**，尤其 `refetchInterval` 单文件那条与 404 文案单键那条 —— 本 Task 的改动不得触发它们）</read_first>
  <action>
**① `web/src/api/blueprints.ts` 追加两个函数（删除行 ≤1）。**

```
export async function getBlueprintExportAvailability(artifactId: string) {
  return get<{ available: boolean, reason: string | null }>(
    `/delivery/artifacts/${artifactId}/blueprint/export-feishu/availability/`,
  )
}

export async function exportBlueprintToFeishu(artifactId: string, payload?: { version_id?: string }) {
  return post<{ document_id: string, url: string, version_no: number, exported_at: string }>(
    `/delivery/artifacts/${artifactId}/blueprint/export-feishu/`,
    payload ?? {},
  )
}
```
并在 `export default { ... }` 里追加两个名字（保持既有分组不动，新名字追加在末尾 —— `@antfu/eslint-config` 的自动重排会误伤既有导出顺序）。

**② `BlueprintViewerHeader.vue` 加「未经确认」常驻横幅 + 导出按钮。**

- ⭐ **横幅判据与后端同一个闭合白名单**：组件内声明 `const CONFIRMED_STATUSES = ['confirmed', 'implementing', 'implemented'] as const`（**三个字面量与后端 `_SUPPRESS_WATERMARK_STATUSES` 逐字对齐**），`const unconfirmed = computed(() => !CONFIRMED_STATUSES.includes(props.currentStatus as any))`。⭐ **常驻、⛔ 不可关闭、⛔ 不做 dismiss**（115 反复用过的形状：把不能丢的东西做成无条件渲染、把开关物理删掉）。
- 渲染：`data-testid="blueprint-unconfirmed-banner"`，带语义描边的 `div` + `role="status"`（⚠️ **仓内没有 `ui/alert` 组件**，STATE 115-06 缺件登记 ⇒ 照 `blueprint-history-notice` / `blueprint-readonly-notice` 的既有形态；⛔ 本 plan **不新建 `ui/alert`**）。文案走 i18n 新键，含状态占位。
- **导出按钮**：新增 prop `exportAvailable?: boolean`（页面传）与 emit `export`。⭐ `exportAvailable !== true` ⇒ **按钮不渲染**（⛔ 不是 disabled + tooltip —— CONTEXT 逐字「前端据此隐藏按钮而不是点了才报错」）。⭐ **组件只 emit、不发请求**（115-04 纪律）。
- ⛔ 零颜色字面量（走既有语义类）；⛔ 禁 `v-html`；⛔ `refetchInterval` 零命中。

**③ `pages/knowledge/blueprints/[id].vue`（0.20 自建，⭐ 本 plan 显式登记这处改动，删除行 ≤4）。**

- availability query：`useQuery({ queryKey: ['blueprint','export-availability',artifactId], queryFn: () => blueprintsApi.getBlueprintExportAvailability(artifactId) })`；⭐ **失败不进错误分档**（照 gate 那条例外：只决定按钮是否渲染，⛔ 不弹 toast、⛔ 不影响四个主查询驱动的页面状态）。
- 把 `:export-available="availability?.available === true"` 传给 header，接住 `@export`。
- 导出 mutation：调 `exportBlueprintToFeishu` → **200** ⇒ 成功 toast 且给**可点 url**（`document_id` / `url` 来自响应）；**400** ⇒ 回显中性 `detail`；**502** ⇒ 「上游暂不可用，请稍后重试」；⭐ **⛔ 零乐观更新**；⛔ **不 invalidate `['blueprint']` 前缀**（导出不改任何蓝图状态——invalidate 会白刷五个查询）。

**④ `web/src/locales/zh-CN.json` 新键纯追加（删除行 0）**：横幅文案（含 `{status}` 插值）、导出按钮、三档 toast、availability 隐藏时无文案（按钮不渲染即可）。⛔ 不留英文 token 降级。

**⑤ 新建 `web/src/components/blueprint/__tests__/viewerHeaderExport.spec.ts`**，「守七件事」：

1. ⭐ **三个已确认态不出横幅**（参数化 `confirmed` / `implementing` / `implemented`）。
2. ⭐ **其余八态 + 未知串 + 空串都出横幅**（参数化）——⛔ 这一组必须含 `''`。
3. ⭐ **前端白名单变异**：把组件里的 `CONFIRMED_STATUSES` 去掉一个值 ⇒ 用例 1 对应那条转红（**实跑一次并记 SUMMARY**）。
4. ⭐ **横幅不可关闭**：DOM 里**不存在**任何关闭/dismiss 控件（`wrapper.find('[data-testid="blueprint-unconfirmed-banner"] button')` 不存在）。
5. **导出按钮按 availability 隐藏**：`exportAvailable: false` / `undefined` ⇒ 按钮**不存在于 DOM**；`true` ⇒ 存在。⛔ 断言的是「不存在」而不是「disabled」。
6. ⭐ **组件只 emit 不发请求**：点击导出按钮 ⇒ emit `export` 一次，且 `blueprintsApi.exportBlueprintToFeishu` 的 mock **零调用**。
7. **前后端判据逐字对齐**：源码扫描断言组件里的三个状态字面量集合与后端 `_SUPPRESS_WATERMARK_STATUSES`**成员相同**（读后端源码提取字面量做比对；若跨语言比对不便，则在两侧各留一条注释指向对方并在 SUMMARY 里贴出两份清单逐字对照）。

**⑥ 前后端双门**（本 Task 收尾并登记进 SUMMARY）：
- 后端：`cd server && uv run pytest tests/ -q` 与基线 **8609/1** 比对；`makemigrations --check --dry-run` 退出码 0。
- 前端：`cd web && pnpm exec vitest run`（基线 **1674 passed / 1 skipped**）、`pnpm type-check`（exit 0）、`pnpm build`（通过）、`pnpm exec eslint <本 plan 触及的前端文件>`（零新增问题）。
- ⚠️ **P-15**：`git status` 检查并 `git checkout -- web/pnpm-workspace.yaml`。
- ⚠️ **`pnpm build` 会重写 `web/src/components.d.ts`**（STATE 115-06）：还原后按字典序手工插入自己那几行（本 plan 未新建组件文件 ⇒ 该文件**应保持零变更**）。
  </action>
  <verify>
    <automated>cd web && pnpm exec vitest run src/components/blueprint/__tests__/viewerHeaderExport.spec.ts && pnpm exec vitest run src/__tests__/blueprint-source-guard.spec.ts</automated>
  </verify>
  <acceptance_criteria>
- `cd web && pnpm exec vitest run src/components/blueprint/__tests__/viewerHeaderExport.spec.ts` 全绿；`rg -c "it\(" web/src/components/blueprint/__tests__/viewerHeaderExport.spec.ts` ≥ **10**。
- ⭐ **前后端白名单逐字对齐**：`cd /Users/zaneliu/Projects/open-source/friday-ai/.claude/worktrees/v0.20-blueprint && node -e "
const fs = require('fs');
const be = fs.readFileSync('server/services/process_runtime/blueprint_render.py','utf8');
const fe = fs.readFileSync('web/src/components/blueprint/BlueprintViewerHeader.vue','utf8');
for (const s of ['confirmed','implementing','implemented']) {
  if (!be.includes(s)) throw new Error('后端缺 ' + s);
  if (!fe.includes(s)) throw new Error('前端缺 ' + s);
}
console.log('watermark whitelist aligned OK');
"` 输出 `watermark whitelist aligned OK`（⭐ 两侧的完整清单逐字贴进 SUMMARY）。
- ⭐ **横幅不可关闭**：`rg -n "dismiss|close|v-if=\"!dismissed\"" web/src/components/blueprint/BlueprintViewerHeader.vue` 的命中**不落在横幅段**（逐条人工核对并写进 SUMMARY）；用例 4 绿。
- ⭐ **导出按钮是隐藏不是 disabled**：`cd web && node -e "
const s = require('fs').readFileSync('src/components/blueprint/__tests__/viewerHeaderExport.spec.ts','utf8');
if (!/exists\(\)\).toBe\(false\)/.test(s)) throw new Error('必须断言按钮不存在于 DOM');
console.log('export button hidden OK');
"` 输出 `export button hidden OK`。
- ⭐ **组件只 emit 不发请求**：`rg -n "exportBlueprintToFeishu|getBlueprintExportAvailability" web/src/components/blueprint/BlueprintViewerHeader.vue` **零命中**；用例 6 绿。
- ⭐ **前端变异实跑一次并登记**：SUMMARY 里写「去掉 `implementing` ⇒ 用例 X 转红（贴错误首行）；恢复即绿」。
- ⭐ **源码守卫六条全绿**：`cd web && pnpm exec vitest run src/__tests__/blueprint-source-guard.spec.ts`；`rg -n "refetchInterval" web/src/components/blueprint/BlueprintViewerHeader.vue web/src/pages/knowledge/blueprints/\[id\].vue` **零命中**。
- **i18n 零降级**：`git diff web/src/locales/zh-CN.json | rg -c "^-[^-]"` == **0**；`rg -n "未经确认" web/src/locales/zh-CN.json` 命中；⛔ 组件内零中文硬编码（`rg -n "[\u4e00-\u9fa5]" web/src/components/blueprint/BlueprintViewerHeader.vue` 的命中只允许落在注释）。
- ⭐ **导出不 invalidate 蓝图前缀**：`cd web && node -e "
const s = require('fs').readFileSync('src/pages/knowledge/blueprints/[id].vue','utf8');
const i = s.indexOf('exportBlueprintToFeishu');
if (i < 0) throw new Error('页面未接导出 mutation');
const seg = s.slice(i, i + 1200);
if (/invalidateQueries/.test(seg)) throw new Error('⛔ 导出不改任何蓝图状态，不得 invalidate 前缀');
console.log('no needless invalidate OK');
"` 输出 `no needless invalidate OK`。
- ⭐ **前端门**：`cd web && pnpm exec vitest run` 相对基线 **1674 passed / 1 skipped** 无新增失败；`pnpm type-check` 退出码 **0**；`pnpm build` 通过；`pnpm exec eslint src/api/blueprints.ts src/components/blueprint/BlueprintViewerHeader.vue "src/pages/knowledge/blueprints/[id].vue" src/components/blueprint/__tests__/viewerHeaderExport.spec.ts` **零新增问题**（与基线逐条比对写进 SUMMARY）。
- ⚠️ **环境项核算**：`git status --porcelain web/pnpm-workspace.yaml web/src/components.d.ts` **为空**。
- ⭐ **后端门**：`cd server && uv run pytest tests/ -q` 相对基线（8609/1）**无新增失败**；`makemigrations --check --dry-run` 退出码 0。
- ⭐ **0.19 归属面与冻结面零改动**：`git diff web/src/components/chat/TechPlanCard.vue web/src/components/chat/RoutingDecisionPanel.vue web/src/components/execution/NodeDataTab.vue web/src/components/delivery/ArtifactTimeline.vue server/services/process_runtime/render.py server/delivery/artifacts/registry.py server/delivery/api/blueprint_review_views.py` **全部为空**。
- 相位边界自检：`git diff --name-only` 只含本 plan 声明的 13 个文件。
  </acceptance_criteria>
  <done>「未经确认」在**两个面**上都逃不掉：后端 renderer 无条件首行标注（无任何取值能关掉、`inspect.signature` 断言 + 变异实跑）、前端常驻横幅同源判据且**不可关闭**（变异实跑）；导出按钮按 availability **隐藏**而不是点了才报错、组件只 emit 不发请求、三档 toast 分档且不做无谓失效；i18n 零降级、源码守卫六条全绿、前后端双门相对基线无新增失败、0.19 归属面与冻结面逐字未动。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 未认证 / 非成员请求 → 两个导出端点 | 导出会把整份蓝图正文送到外部文档系统；越权导出即项目技术细节外泄 |
| 「artifact 存在」这一事实 | 中性 404 是不泄露存在性的唯一实现 |
| 飞书上游响应 / 异常文本 → 响应体与日志 | 上游 body 可能含 token、内部 URL、账号信息 |
| `blueprint_status` → 「未经确认」标注 | 这是 RELY-01 在文档面的唯一落点：标注丢失 = 未经人审的方案以正式方案的面貌流通 |
| 导出留痕 → `ArtifactVersion.content` | 写进去即污染 `content_hash`，把版本历史刷成噪声 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-116-38 | Repudiation | 「未经确认」标注可被参数关掉 ⇒ 未经人审的方案以正式方案面貌流通（正面违反 RELY-01） | mitigate | 必填 keyword-only + 闭合白名单（`""` 与未知串都渲染）+ ⛔ 零布尔开关 + ⭐ `inspect.signature` 参数名集合断言 + 白名单变异实跑；前端同源判据 + 常驻不可关闭 + 前端变异实跑 |
| T-116-39 | Repudiation | 标注只在导出端点生效、时间线面丢失（P-4）⇒ 已上线且已有消费者的面继续显示无标注的空壳 | mitigate | 注册表分支传 `""`（fail-safe 当作未确认）+ `ArtifactTimelineSerializer` 蓝图特判传真实状态 + ⭐ 用例覆盖**两个**面 |
| T-116-40 | Information Disclosure | 两个导出端点缺项目范围闸 ⇒ 任一登录用户可把任意项目的蓝图正文导出到自己可见的飞书文档 | mitigate | 两端点都挂 `_aassert_project_scope`（import 复用）+ 未认证拒 + 非成员中性 404 与「不存在」逐字相同（`a.json() == b.json()`） |
| T-116-41 | Information Disclosure | 上游错误 body 原样回显 ⇒ 泄露 token / 内部 URL | mitigate | 中性 detail 常量 + 异常文本只经 `redact_secrets_in_text` 进日志 + ⭐ 用例构造含 `secret-token-xyz` 的上游错误并断言它不出现在响应里 |
| T-116-42 | Tampering | 导出留痕写进 `ArtifactVersion.content` ⇒ 每次导出翻一个版本，版本历史被刷成噪声、diff 面被污染 | mitigate | 留痕落 Interaction Ledger + 独立 caller 事件；源码扫描断言本文件无 `add_version` / content 写；⭐ 用例「导出前后 `ArtifactVersion` 计数不变」 |
| T-116-43 | Tampering | 导出事件加进 `BLUEPRINT_EVENTS` ⇒ 双断言转红 + 导出记录混进「阶段进展时间线」 | mitigate | prohibitions 明令 + `git diff event_taxonomy.py` 为空 + 用例断言 `len == 21` 且导出事件不在其中 + `ConvergenceSessionEvent` 计数不变 |
| T-116-44 | Denial of Service | 上游失败被包成 200 空结构 ⇒ 用户以为导出成功、实际什么都没发生（115-MJ-04 的反面教材） | mitigate | 400/502 如实回错；业务主体**绝不**包进 best-effort（只有观测另包一层）；用例 5/6 |
| T-116-45 | Denial of Service | availability 不满足时才在点击后报错 ⇒ 用户反复点、反复失败 | mitigate | 前端按 availability **隐藏按钮**（用例断言「不存在于 DOM」而不是 disabled）+ 后端 POST 侧仍 400 兜底 |
| T-116-46 | Information Disclosure | 蓝图正文 / 上游 body 进日志 | mitigate | `_log` 只记标量与关联键；两个新模块进 `_SCANNED_MODULES`（与模块创建同 commit）；AST 守卫全绿 |
| T-116-47 | Tampering | renderer 按 `block.type` 分派取文本 ⇒ 与 `blueprint_anchor` 的锚点坐标系分叉，导出物与批注位置对不上 | mitigate | 字段优先级 `text` → `code.source` → `rows` + 源码扫描断言函数体内 `type` 零命中 + 「`pseudocode` 且 `text` 非空仍按 text 取」的用例 |
| T-116-SC | Tampering | 依赖供应链 | accept | 零新增外部依赖、零 migration、零新事件常量进 `BLUEPRINT_EVENTS`；`makemigrations --check` 退出码 0；pnpm 与 `components.d.ts` 两处环境漂移已核算还原 |
</threat_model>

<verification>
- `cd server && uv run pytest tests/delivery/ tests/services/process_runtime/ -q` 全绿（含脱敏守卫 16 项扫描面、`BLUEPRINT_EVENTS` 双断言、112/114/115/116 全部端点）。
- ⭐ `cd server && uv run pytest tests/ -q` 相对基线（8609 passed / 1 failed）**无新增失败**。
- ⭐ `cd web && pnpm exec vitest run` 相对基线（1674 passed / 1 skipped）**无新增失败**；`pnpm type-check` exit 0；`pnpm build` 通过；`pnpm exec eslint <触及文件>` 零新增问题；⭐ 源码守卫六条全绿。
- `cd server && uv run python manage.py makemigrations --check --dry-run` 退出码 0；相位内**零** migration。
- ⭐ **两条变异各实跑一次**（后端白名单 / 前端白名单），红/绿输出首行贴进 SUMMARY。
- ⭐ **一次真实飞书导出验证**（或明确登记「无凭证、版式退化风险未消解」），观察到的版式退化逐条记进 SUMMARY（`[MEDIUM]` A2）。
- ⭐ **400/502 分档依据逐类核过**并给出对照表（`[MEDIUM]` 假设解除）。
- 冻结面核算：`render.py` / `registry.py` / `blueprint_review_views.py` / `event_taxonomy.py` / 四个 0.19 归属前端面 —— `git diff` 逐个为空。
- 删除行核算：七个受限文件逐个不超过 execution_context 表的上界。
- 环境项核算：`git status --porcelain web/pnpm-workspace.yaml web/src/components.d.ts` 为空。
</verification>

<success_criteria>
- **蓝图可导出飞书文档**，内容是**六段全量 + 需求规格 + 验收锚点 + 决策记录附录 + 引用脚注**，且批注天然不进导出物（content 里本就没有，⛔ 无死码）。
- ⭐ **「未经确认」在两个面上都逃不掉**：导出物第一行与界面常驻横幅共用同一个闭合白名单；后端没有任何参数能关掉它（`inspect.signature` 断言 + 白名单变异实跑），前端横幅不可关闭（变异实跑）。
- ⭐ **`current_version_markdown` 的结构性空壳一并修掉**，且注册表路径拿不到状态时**fail-safe 当作未确认**——这是 P-4 那个「已上线、已有消费者却仍是空壳」的面第一次被覆盖。
- **导出与时间线共用同一个 renderer**（有意为之）：⛔ 没有在导出器里就地拼 markdown，两处口径不会分叉。
- **留痕不污染任何既有面**：导出前后 `ArtifactVersion` 计数不变、`BLUEPRINT_EVENTS` 仍是 21、`ConvergenceSessionEvent` 计数不变；留痕落 Interaction Ledger + 独立 caller 事件。
- **上游失败如实回错**：400/502 分档有逐类核过的依据表，`detail` 中性、异常文本脱敏、⛔ 不回显上游 body、⛔ 不静默 200 空结构。
- **导出按钮按 availability 隐藏而不是点了才报错**；组件只 emit、页面发请求并三档 toast，⛔ 零乐观更新、⛔ 无谓失效。
- VIEW-05 全量交付；零 migration、零新依赖、冻结面与 0.19 归属面逐字未动。
</success_criteria>

<output>
完成后创建 `.planning/phases/116-entry/116-05-SUMMARY.md`，provides 至少包含：
- ⭐ **`render_blueprint_markdown` 的逐字签名与三条不变量**，以及**四个调用点清单**（注册表分支传 `""` / `ArtifactTimelineSerializer` 传真实状态 / 导出端点传真实状态 / 测试）——供后来者一眼看出「哪些面拿得到真实状态」；
- ⭐ **抑制白名单的三个字面量**（后端 frozenset 与前端 `CONFIRMED_STATUSES` **两份清单逐字贴出对照**）与**两侧变异实跑记录**；
- **十段 + 决策附录 + 引用脚注的 markdown 版式说明**（heading 层级 / 表格列序 / 脚注形态），以及 ⭐ **一次真实导出验证的观察**（或「无凭证、风险未消解」的显式登记）；
- **两个端点的契约表**（URL / `name` / 方法 / 入参 / 响应键逐字 / **全部状态码映射**），前端与外部调用方照它写；
- ⭐ **400 vs 502 的分档对照表**（`FeishuDocAPIError` / `RateLimitError` / `PERMISSION_CODES` / `NOT_FOUND_CODES` → 状态码），`[MEDIUM]` 假设在此解除；
- **availability 三 reason 的判据链**与 ⭐ **space 从 `meta.project_id → Project.space` 反查**（与 chat 面的差异）；
- ⭐ **留痕落点清单**（Interaction Ledger 字段 + caller 事件字段）与「⛔ 不写 content / ⛔ 不进 `BLUEPRINT_EVENTS`」的核算证据（`ArtifactVersion` 计数不变、`len == 21`）；
- 前端两处改动的 props/emits 逐字（`exportAvailable` / `@export`）与页面侧的 toast 分档表；⭐ **`pages/.../[id].vue` 与 `locales/zh-CN.json` 两处额外前端改动的理由与删除行登记**；
- 受限面删除行逐行登记；前后端双门与基线的差异比对；pnpm 与 `components.d.ts` 两处环境项还原核算；
- ⭐ **VIEW-05 的验收映射表**（哪几条用例分别兑现「六段全量 + 决策附录」与「界面与导出物均带标注」）。
</output>
