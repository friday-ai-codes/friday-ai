---
phase: 116-entry
plan: 03
type: execute
wave: 3
depends_on: ["116-01", "116-02"]
files_modified:
  - server/workflows/nodes/ai/plan_research.py
  - server/agents/tools/plan_research_tools.py
  - server/mcp_tools/orchestration_delegate.py
  - server/services/process_runtime/answer_resume.py
  - server/feishu/callbacks/plan_clarify_callback.py
  - server/initiatives/services/feature_solution_service.py
  - server/subagent/api/callbacks.py
  - server/tests/services/process_runtime/test_entry_dispatch_wiring.py
  - server/tests/agents/test_chat_blueprint_entry.py
autonomous: true
requirements: [GATE-01]
prohibitions:
  - "⭐ **`PHASE_BASE` 纪律（所有边界/删除行核算的前提，B5）**：GSD 逐 Task 原子提交 ⇒ 收口自检跑时本 plan 的改动**早已 committed**，裸 `git diff` 恒空 ⇒ 所有冻结面、删除行、`--name-only` 边界断言都会**静默恒真**。⇒ 本 plan **第一条执行步骤**是 `PHASE_BASE=$(git rev-parse HEAD)` 并把该 sha 逐字记进 SUMMARY；下文所有边界检查一律写作 `git diff $PHASE_BASE -- <file>` / `git diff $PHASE_BASE --name-only`。⚠️ 同类坑：`rg -c` / `grep -c` **无命中时退出码为 1**（`rg -c` 还不打印任何东西）⇒ 计数型断言一律 `| grep -c '<pat>' || true` 再比对数字，⛔ 不写 `... | rg -c \"...\" == 0`"
  - "🔒 **§13.2 冻结六文件与 `repo_router_v2.py` 零改动**（`git diff $PHASE_BASE -- <该文件>` 必须为空）"
  - "⛔ ⭐ **不改 `subagent/api/callbacks.py:447`（`_schedule_chat_plan_resume`）** —— 它对蓝图会话**三重不可达**（分支条件 `last_output[\"source\"] == \"plan_research\"`（`:128`）而蓝图容器写 `\"blueprint_research\"` / `\"blueprint_repo_plan\"`（`:2003` / `:2287`，派发侧 `blueprint_research_adapter.py:471,483`）；函数体读 `lo.get(\"plan_session_id\")`（`:422`）而蓝图写 `blueprint_session_id`；再加 `entrypoint == CHAT` 守门（`:430`））。本 plan 只在**两个蓝图 barrier**（`:2150-2177` / `:2377-2396`）里补回灌"
  - "⛔ **`plan_deepen_service.py:99` 不改**（RESEARCH §A.4 表第 7 行：自己建 session、`process_type` 恒 `technical_plan`，非蓝图入口）。改它属 Discretion 但本 plan 明确**不改**——多改一个文件就多一处 rebase 冲突面"
  - "⛔ ⭐ **绝不把 `skip_clarification` / `force_confirm` 透传进蓝图链**：`build_blueprint_engine` 只接两个形参、蓝图链根本没有 `clarify` dep。MCP 的 `skip_clarification=True`（`orchestration_delegate.py:179`）在蓝图分支**无意义**；feature list 的 `force_confirm=True`（`feature_solution_service.py:222`）注入的是旧链 `ClarifyAdapter` 的题目组装器，蓝图链无对应面 —— ⛔ **不要试图移植它**，「强制确认关联仓」在蓝图链由 `repo_confirmation` 硬门天然承担"
  - "⛔ ⭐ **开关查询一律传字面量常量**：`aresolve_entry_process_type(\"workflow\")` / `(\"chat\")` / `(\"mcp\")` / `(\"feature_list\")`，⛔ **绝不传 `session.entrypoint`** —— MCP 入口传给 `start_orchestration` 的 `entrypoint` 实测是 `\"workflow\"`（既有约定），反推会让打开 workflow 键把 MCP 一起切走。116-01 的 `ast` 扫描守卫覆盖本 plan 全部四个入口文件，写错即转红"
  - "⛔ **`meta.project_id` 的推导一律 import 复用 116-02 的 `blueprint_intake.aresolve_project_id`**，⛔ 四个入口不得各写一份；⭐ **MCP 分支绝不透传 `McpWorkItemContext.space_id`**（P-8）"
  - "⛔ **不新增 stage 名**（P-13）：加了就必须同时补前端 `blueprintBlocks.SESSION_STAGE_ALIASES` + `PRE_TIMELINE_SESSION_STAGES`，否则 `indexOf` 返 `-1`、位序推断整条静默不生效。本 plan 的答案是**不加**"
  - "⛔ **不新增 migration、不改 `BLUEPRINT_EVENTS`、不新增 `CallSource` 枚举、前端零改动**（`git diff $PHASE_BASE --name-only | rg '^web/'` 为空）"
  - "⛔ **开关关闭时四个入口的行为必须与改动前逐字一致**：`aresolve_entry_process_type` 返 `technical_plan` ⇒ 走既有的 `start_orchestration` + `build_orchestration_engine` + 既有 driver，**参数一个不少一个不改**。这条由「四个入口的既有测试全部保持绿」核算"
must_haves:
  truths:
    - "⭐ **六个续驱点全部改成 `build_engine_for_session`（连 driver 一起换）**，RESEARCH §A.4 表逐行：① `workflows/nodes/ai/plan_research.py:361`（`_build_engine`）② `agents/tools/plan_research_tools.py:134` ③ `mcp_tools/orchestration_delegate.py:179` ④ `services/process_runtime/answer_resume.py:102`（**连同 `:103` 硬编码的 `resume.adrive_…` 一起换**）⑤ `feishu/callbacks/plan_clarify_callback.py:242` ⑥ `initiatives/services/feature_solution_service.py:222`。⛔ **不改** `subagent/api/callbacks.py:447`（蓝图不可达）与 `plan_deepen_service.py:99`（非蓝图入口）"
    - "⭐ **④ 与 ⑤ 是 CONTEXT 未点名的真续驱点**：`answer_resume.py:102-103` 是**澄清作答后的通用续驱**（飞书回调 91-03 + 会话端点 91-04 共用），`plan_clarify_callback.py:242` 是飞书澄清卡回调续驱（workflow 蓝图会话的澄清卡走这条）。漏改任一条 ⇒ 蓝图会话**作答后无人续驱**，卡在 `waiting_clarification` 永不推进且零异常"
    - "⭐ **源码扫描断言：六个文件里 `build_orchestration_engine(` 与 `adrive_convergence_session_to_pause_or_terminal(` 的直接调用零命中**（`import` 行不算——若因回落分支仍需 import，改成经 `build_engine_for_session` 单点获取）。这是「漏改一处」唯一能被机器逮住的形态"
    - "⭐ **四个入口各自的蓝图路径 = 三步**：(1) `process_type = await aresolve_entry_process_type(<字面量>)`；(2) `technical_blueprint` ⇒ 先 `project_id = await aresolve_project_id(...)`（**失败即按各自出口如实回错，⛔ 不建 session 不建 artifact**）再 `session = await start_blueprint_orchestration(..., project_id=project_id, entry_key=<同一字面量>)`；(3) `engine, adrive = build_engine_for_session(session, node_execution_id=...)` → `await adrive(engine, session, max_steps=...)`。⭐ **开关为 `technical_plan` 时逐字走既有路径**（含既有的 `skip_clarification` / `force_confirm` / `mode` / `feature_segments` / `feature_meta` 参数）"
    - "**四个入口的 project_id 推导来源**：workflow ⇒ `context.workflow_execution.space` 经 `_aresolve_project`（`plan_research._resolve_space` 已有）；chat ⇒ 会话所属 space（`_resolve_actor` 附近已有 `space_id` 上下文）；⭐ MCP ⇒ `McpWorkItemContext.space` **必须过 `_aresolve_project`**（`:space` 是 `projects.Space` FK，而 `technical_plan_service.py:488` 把它当 `\"project_id\"` 键回给调用方 —— 直接透传即 20 个端点恒 400，P-8）；feature list ⇒ `feature_meta.project_id`（已是 Project id，仍校验存在性）"
    - "**推不出 project_id 时四个入口各自的错误出口**：workflow `NodeResult(status=\"failed\", error=<中性 detail>, next_handle=\"error\")`（范式 `plan_research.py:194-199` 的 `missing_requirement`）；chat `ToolResult(success=False, error=<中性 detail>)`（范式 `plan_research_tools.py:96-99`）；MCP `error_response(code, detail)`；feature list 按其既有失败形态。⭐ 四处的 `detail` 统一取 `BlueprintIntakeRejected.detail`（中性、⛔ 不含内部路径与异常原文）"
    - "⭐ **feature list 入口把 `feature_segments` 透传进 `start_blueprint_orchestration`**，由 116-02 的 `_h_bp_decompose` 直采映射成 `feature_points`（⛔ 不走 LLM）；⛔ **不移植 `force_confirm`** —— 蓝图链的「强制确认关联仓」由 `repo_confirmation` 硬门承担"
    - "⭐ **chat 断链一：`_maybe_suspend` 的蓝图分支**（§A.5，P-7）。旧链两条判据对蓝图**全部失效且不抛异常**：`ClarificationService().ahas_pending(session.id)` 对蓝图**恒 False**（蓝图用 `BlueprintThread`）；`aall_research_tasks_terminal` 在蓝图卡在 `spec_gate` 时零 task **返 True**。⇒ 蓝图会话走蓝图版判据：`waiting_clarification` + **有 open+blocking `BlueprintThread`**（`ai_clarification` 与 `repo_confirmation` 两类，**不传 `kind`**，判据抄 `blueprint_resume.adrive_…` 的 pause 短路）⇒ 返回**挂起 marker**；`waiting_event` + 仍有在途调研 ⇒ 返回 blocking task marker"
    - "⭐ **chat 断链二：`_map_terminal` 的蓝图分支**（P-7）。现状 `status != DONE ⇒ ToolResult(success=False, error=\"plan session failed\")` ⇒ **一条正在等用户回答澄清的健康会话被报成「方案编排失败」**。蓝图版按 `blueprint_status` 分档：`needs_clarification` ⇒ **挂起 marker 而不是失败**；`pending_review` / `confirmed` ⇒ 成功并带 `artifact_id` 与状态文案；`failed` ⇒ 才失败。⭐ **用例：一条 chat 蓝图会话停在 `spec_gate` 等澄清时，工具返回值必须是挂起 marker 而不是 `success=False`**"
    - "⭐ **chat 断链三：两个蓝图 barrier 补 `entrypoint == CHAT` 回灌**（§A.5 断链一）。`_schedule_chat_plan_resume` 干两件事——续驱（d/e）+ `BarrierManager.task_completed` 回灌（f/g，`callbacks.py:468-488`）；蓝图侧 `_trigger_blueprint_research_barrier`（`:2150-2177`）与 `_trigger_blueprint_repo_plan_barrier`（`:2377-2396`）**只做续驱、不回灌** ⇒ chat 入口切蓝图后对话**永远停在「深入调研容器运行中…」**。⭐ **task key 必须是 `str(session.id)`**，与 `plan_research_tools.py:246-256` 注册 blocking task 时的 `{\"task_id\": str(session.id), \"task_type\": \"plan_research\", ...}` **逐字对齐** —— key 不一致的症状是 waiter 永远等不到，且**不抛异常**"
    - "回灌分支整段 best-effort（`try/except` 只 log），⛔ 绝不反噬 barrier 与容器回调；`BlockingTaskResult` 的 `success` / `output` / `error` 三键形状逐字照 `callbacks.py:470-478`（`output` 取 `str(session.current_artifact_version_id or \"\")`）"
    - "⭐ **两条 chat 断链各配一条「不做会怎样」的反向用例**：(a) 把 `_maybe_suspend` 的蓝图分支去掉 ⇒ 「等澄清的健康会话」用例返回 `success=False` ⇒ 转红；(b) 把 barrier 回灌分支去掉 ⇒ 「barrier waiter 收到结果」用例超时/未满足 ⇒ 转红。两条都要**实跑一次真实变异**并把红/绿输出记进 SUMMARY"
    - "⭐ **116-01 的 `ast` 字面量守卫必须仍绿**：本 plan 新增的四处 `aresolve_entry_process_type` 调用实参全部是 `ast.Constant`（守卫扫描面已含这四个入口文件，写成 `session.entrypoint` 即转红）"
    - "⭐ **开关关闭时零回归**：四个入口的既有测试（workflow 节点 / chat 工具 / MCP delegate / feature list）全部保持绿，且 `answer_resume` 与飞书澄清卡回调的既有测试保持绿"
    - "全量后端门相对基线 **8609 passed / 1 failed** 无新增失败；`makemigrations --check --dry-run` 退出码 0 且零新 migration"
  artifacts:
    - path: "server/workflows/nodes/ai/plan_research.py"
      provides: "_build_engine 改 build_engine_for_session（连 driver）+ _create_session 按开关分派 + workflow 的 project_id 推导与失败出口 NodeResult(next_handle=error)"
      contains: "build_engine_for_session"
    - path: "server/agents/tools/plan_research_tools.py"
      provides: "chat 入口分派 + 蓝图版 _maybe_suspend / _map_terminal（判据换 BlueprintThread 与 blueprint_status，健康挂起绝不报失败）+ 续驱改 build_engine_for_session"
      contains: "blueprint_status"
    - path: "server/mcp_tools/orchestration_delegate.py"
      provides: "MCP 入口分派 + ⭐ project_id 过 _aresolve_project（⛔ 不透传 space_id）+ 续驱改 build_engine_for_session 且不透传 skip_clarification"
      contains: "aresolve_project_id"
    - path: "server/services/process_runtime/answer_resume.py"
      provides: "通用作答后续驱改 build_engine_for_session（engine 与 driver 一起换，:102-103 两处）"
      contains: "build_engine_for_session"
    - path: "server/feishu/callbacks/plan_clarify_callback.py"
      provides: "飞书澄清卡回调续驱改 build_engine_for_session（带 node_execution_id）"
      contains: "build_engine_for_session"
    - path: "server/initiatives/services/feature_solution_service.py"
      provides: "feature list 入口分派 + feature_segments 透传 + ⛔ 不移植 force_confirm + 续驱改 build_engine_for_session"
      contains: "start_blueprint_orchestration"
    - path: "server/subagent/api/callbacks.py"
      provides: "两个蓝图 barrier 补 entrypoint == CHAT 的 BarrierManager.task_completed 回灌（task key = str(session.id)），删除行 0"
      contains: "task_completed"
    - path: "server/tests/services/process_runtime/test_entry_dispatch_wiring.py"
      provides: "六个续驱点源码扫描（build_orchestration_engine 直调零命中）+ 四入口开关开/关两态行为 + 四入口 project_id 推导与拒绝发起 + MCP 不透传 space_id/skip_clarification"
      contains: "build_engine_for_session"
    - path: "server/tests/agents/test_chat_blueprint_entry.py"
      provides: "⭐ 等澄清的健康蓝图会话返回挂起 marker 而非 success=False + 蓝图 barrier 回灌满足 waiter（task key 对齐）+ 两条反向变异"
      contains: "task_completed"
  key_links:
    - from: "server/workflows/nodes/ai/plan_research.py"
      to: "server/services/process_runtime/entrypoint.py"
      via: "build_engine_for_session(session, node_execution_id=...) 返回 (engine, driver) 二元组，替换 _build_engine 的硬编码"
      pattern: "build_engine_for_session"
    - from: "server/mcp_tools/orchestration_delegate.py"
      to: "server/services/process_runtime/blueprint_intake.py"
      via: "aresolve_project_id(entry=\"mcp\", work_item_context=context) —— ⛔ 绝不透传 context.space_id（P-8）"
      pattern: "aresolve_project_id"
    - from: "server/agents/tools/plan_research_tools.py"
      to: "server/delivery/models/blueprint_thread.py"
      via: "蓝图版 _maybe_suspend 的判据：open+blocking BlueprintThread（不传 kind），⛔ 不用 ClarificationService.ahas_pending（对蓝图恒 False）"
      pattern: "BlueprintThread"
    - from: "server/subagent/api/callbacks.py"
      to: "server/agents/tools/blocking_task_registry.py"
      via: "蓝图 barrier 的 BarrierManager.task_completed(str(session.id), result) —— key 与 register_blocking_task 的 task_id 逐字对齐"
      pattern: "task_completed"
    - from: "server/initiatives/services/feature_solution_service.py"
      to: "server/services/process_runtime/entrypoint.py"
      via: "start_blueprint_orchestration(..., feature_segments=..., feature_meta=..., project_id=...) —— 功能点直采、⛔ 不移植 force_confirm"
      pattern: "feature_segments"
---

<objective>
把 116-01 的分派器与 116-02 的 intake **接到四个真实入口上**，让「四入口都具备走 `technical_blueprint` 的可执行路径」从一句话变成能跑的代码——**同时把 chat 那条路上的三处断链一并补掉**，否则「chat 具备真实可执行路径」是假的。

三件事，每一件都有一处「不做就静默假通过」的地方：

① ⭐ **续驱点是八处不是四处，其中六处要改**（RESEARCH §A.4 逐点实测）。CONTEXT 点名的四处里有一处（`callbacks.py:447`）对蓝图**三重不可达**，而 CONTEXT 没点名的 `answer_resume.py:102`（澄清作答后的通用续驱，飞书回调与会话端点共用）与 `plan_clarify_callback.py:242`（飞书澄清卡回调）**是真续驱点**。漏改任一条 ⇒ 蓝图会话**作答后无人续驱**，卡在 `waiting_clarification` 永不推进且零异常。⇒ 用源码扫描断言「六个文件里直调 `build_orchestration_engine(` 零命中」把漏改变成机器可逮。

② ⭐ **chat 的两条断链**（§A.5，P-7），两条都**不抛异常**：
- `_maybe_suspend` 的两个分支都是旧链判据 —— `ClarificationService().ahas_pending` 对蓝图**恒 False**，`aall_research_tasks_terminal` 在蓝图卡在 `spec_gate` 时零 task **返 True** ⇒ 一条正在等 `BlueprintThread` 澄清的健康会话会穿过 `_maybe_suspend` 返回 None，落到 `_map_terminal` 拿到 `ToolResult(success=False, error="plan session failed")`。**用户看到「方案编排失败」，其实系统在等他回答问题。**
- 蓝图的两个 barrier **只续驱、不回灌**（`_schedule_chat_plan_resume` 那条链除了续驱还负责 `BarrierManager.task_completed`，`callbacks.py:468-488`）⇒ chat 入口切蓝图后，对话**永远停在「深入调研容器运行中…」**。

③ ⭐ **开关一律传字面量**（PATTERNS §0.1）。MCP 入口传给 `start_orchestration` 的 `entrypoint` 实测是 `"workflow"`（`orchestration_delegate.py:171-178`，该文件 docstring 逐字写明这是既有约定）⇒ 写成 `switch[session.entrypoint]` 会让「打开 workflow 键」把 MCP 一起切走。116-01 已把这条钉成 `ast` 扫描断言，本 plan 是它的第一批真实调用点。

Purpose: 本 plan 之后，把 `BLUEPRINT_ENTRY_SWITCH` 的任一键改成 `technical_blueprint`，那个入口就真的走蓝图链——**默认值仍全部是 `technical_plan`，翻默认顺延同步点 2 后的收尾 plan**（语义原因：蓝图 `DONE` = 待人审，而 `AIPlanResearchNode._map_terminal` 把 `DONE` 映射成 completed 并把 plan 喂给下游 `ai_coding`，现在翻默认 = 让下游拿着未经人审的蓝图去建分支写代码，正面违反 RELY-01）。
Output: 7 处既有源文件改造 + 2 个新建测试文件。**零 migration、零新依赖、零新 stage 名、零新枚举、前端零改动。**
</objective>

<execution_context>
工作目录：worktree 根 `/Users/zaneliu/Projects/open-source/friday-ai/.claude/worktrees/v0.20-blueprint`，分支 `milestone/v0.20.0-blueprint`。⛔ 绝不读写主检出。

⭐ **第一条执行步骤（先于读任何源码）：`PHASE_BASE=$(git rev-parse HEAD)`，把该 sha 逐字记进 SUMMARY。** 本 plan 内所有 `git diff` 形态的边界 / 删除行 / 冻结面断言一律写作 `git diff $PHASE_BASE -- <file>`；裸 `git diff` 在逐 Task 原子提交之后恒空、断言会静默恒真（B5）。计数型断言用 `| grep -c '<pat>' || true`。

**wave 3，依赖 116-01 + 116-02**。开工前**必读**：
- `116-01-SUMMARY.md` 的「`build_engine_for_session` 逐字签名与返回契约」+「`aresolve_entry_process_type` 的扫描器覆盖面清单」；
- `116-02-SUMMARY.md` 的「`aresolve_project_id` 四条推导链对照表」+「`BlueprintIntakeRejected` 的 `reason` 取值与中性 detail」+「`start_blueprint_orchestration` 的签名差异清单」+「给 116-03 的开工提示」。

**测试基线**：`cd server && uv run pytest tests/ -q` = **8609 passed / 1 failed**（唯一失败 `test_skills_snapshot_guard::test_skill_files_discovered` 是 worktree 环境产物）。收口断言是「无**新增**失败」。

**受限面配额（硬约束，全部为改造既有文件 —— 本 plan 是本相位改动面最广的一个，逐文件核算删除行）**：

| 文件 | 允许的改动 | 删除行上界 |
|---|---|---|
| `workflows/nodes/ai/plan_research.py` | `_build_engine` 改分派 + `_create_session` 加开关分支与 project_id 推导 | **8** |
| `agents/tools/plan_research_tools.py` | 建会话分派 + 续驱改分派 + `_maybe_suspend` / `_map_terminal` 加蓝图分支 | **10** |
| `mcp_tools/orchestration_delegate.py` | 建会话分派 + project_id 推导 + 续驱改分派（⛔ 不透传 `skip_clarification`） | **8** |
| `services/process_runtime/answer_resume.py` | `:102-103` 两行改分派 | **2** |
| `feishu/callbacks/plan_clarify_callback.py` | `:242` 改分派 | **2** |
| `initiatives/services/feature_solution_service.py` | 建会话分派 + `feature_segments` 透传 + 续驱改分派 | **8** |
| `subagent/api/callbacks.py` | 两个蓝图 barrier 各追加一个回灌分支 | **0** |

⚠️ 签名与字段名一律按下列**实测**取，不得猜测：

- `build_engine_for_session(session, *, session_service=None, node_execution_id="", skip_clarification=False, force_confirm=False) -> tuple[ProcessEngine, Driver]`（116-01）；两个 driver 签名逐字相同 `(engine, session, *, max_steps=20)`。
- `aresolve_entry_process_type(entry: str) -> str`（116-01，`blueprint_entry_switch.py`），四个字面量常量 `ENTRY_WORKFLOW` / `ENTRY_CHAT` / `ENTRY_MCP` / `ENTRY_FEATURE_LIST`。⚠️ ⭐ **可以用这四个模块级常量**（它们是 `ast.Constant` 的字符串赋值来源）—— 但守卫判据是**调用点实参必须是 `ast.Constant`** ⇒ **直接写字面量 `"mcp"` 最稳**；若用常量名会被判为 `ast.Name` 而转红。⭐ **执行期以守卫为准：直接写字面量**。
- `start_blueprint_orchestration(...)`（116-02，`entrypoint.py`）+ `blueprint_intake.aresolve_project_id(...)` + `BlueprintIntakeRejected`。
- `plan_research.py`：`_create_session:297`（传 `entrypoint="workflow"`）、`_build_engine:361`、`_map_terminal:544-570`（⚠️ **本 plan 不改它** —— workflow 节点的终态映射改造归同步点 2 后的收尾 plan）、`_send_clarify_card:434-491`、`_resolve_space`、`missing_requirement` 的失败范式 `:194-199`。
- `plan_research_tools.py`：`start_orchestration(entrypoint="chat", ...)` 在 `:124-131`、`build_orchestration_engine()` 在 `:134`、`_MAX_ADVANCE_STEPS`、`_resolve_actor` / `_filter_repos_in_space`、失败范式 `:96-99`、`_maybe_suspend:209-270`（**两个分支全文**含 `PLAN_CLARIFICATION_RENDER_MARKER` 与 blocking task marker 的**逐字键集**）、`register_blocking_task` 的 `{"task_id": str(session.id), "task_type": "plan_research", "params": {...}}`（`:246-256`）、`_map_terminal:274-297`。
- `orchestration_delegate.py`：`start_orchestration(entrypoint="workflow", ...)` 在 `:171-178`（⭐ 陷阱本体）、`build_orchestration_engine(skip_clarification=True)` 在 `:179`、best-effort 形态 `:162-163`。
- `answer_resume.py:102-103`：engine 与 driver **两行硬编码**，一起换。
- `feishu/callbacks/plan_clarify_callback.py:242`：带 `node_execution_id` 的续驱。
- `feature_solution_service.py`：`start_orchestration(..., mode="feature_list", feature_segments=..., feature_meta=...)` 在 `:143` 附近、`build_orchestration_engine(force_confirm=True)` 在 `:222`。
- `subagent/api/callbacks.py`：`_schedule_chat_plan_resume` 的回灌两步 `:468-488`（`BlockingTaskResult` 三键装配 + `get_barrier_manager().task_completed(str(plan_session.id), result)`）；`_trigger_blueprint_research_barrier:2150-2177`；`_trigger_blueprint_repo_plan_barrier:2377-2396`（两者均以 `blueprint_resume.aresume_blueprint_session` 结尾）。
- `blueprint_resume.adrive_blueprint_session_to_pause_or_terminal` 的 pause 短路判据 docstring（`:115-125`）：「`waiting_clarification` 且**有 open+blocking `BlueprintThread`**（`ai_clarification` 与 `repo_confirmation` 两类，故**不传 `kind`**）**且无待调研仓**」—— 蓝图版 `_maybe_suspend` 的判据抄它。
- `Artifact.blueprint_status` 的 11 态（`delivery/models/artifact.py:45-63`）；⚠️ ⛔ **响应键/字典键不得出现字面 `blueprint_status`**（INV-6 `_RE_FIELD_DICT_KEY` 扫全 `server/`），读值用比较形态或 `getattr` 是安全的。
</execution_context>

<context>
@.planning/phases/116-entry/116-RESEARCH.md（⭐ §A.4 八个续驱点表逐行含「本相位处置」列与 #8 为何不可达的三重理由、⭐ §A.5 chat 两条断链全节、§A.6 开关落点、§A.7 四条推导链与 MCP 陷阱、P-7 / P-8 / P-13、给 PLAN 的第 3、4 条）
@.planning/phases/116-entry/116-PATTERNS.md（⭐ §0.1 `session.entrypoint` 硬伤与两条对策、§0.4 追加点 7–15、A7 六个续驱点、A8 chat 两条断链的「旧链判据 → 蓝图版判据」对照表与 No Analog #7、A9 barrier 回灌的 analog）
@.planning/phases/116-entry/116-CONTEXT.md（入口收编一节；specifics 第 1、2 条）
@.planning/phases/116-entry/116-01-SUMMARY.md（`build_engine_for_session` 契约、`aresolve_entry_process_type` 签名与扫描器覆盖面）
@.planning/phases/116-entry/116-02-SUMMARY.md（`aresolve_project_id` 四链表、`BlueprintIntakeRejected`、`start_blueprint_orchestration` 签名差异、feature_segments 映射规则、给 116-03 的开工提示）
@server/workflows/nodes/ai/plan_research.py（`_create_session:297` / `_build_engine:361` / `_resolve_space` / `_send_clarify_card:434-491` / `missing_requirement:194-199`；⚠️ `_map_terminal:544-570` **只读不改**）
@server/agents/tools/plan_research_tools.py（**全文**：`:90-140` 建会话与续驱、⭐ `:209-270` `_maybe_suspend` 两分支的逐字键集、`:246-256` `register_blocking_task` 的 task key、`:274-297` `_map_terminal`）
@server/mcp_tools/orchestration_delegate.py（**全文**：`:4` 与 `:131` docstring 的「entrypoint=workflow」既有约定、`:162-163` best-effort、`:171-179` 建会话与 `skip_clarification=True`）
@server/services/process_runtime/answer_resume.py（`:90-115` 缺省 engine 分支两行硬编码）
@server/feishu/callbacks/plan_clarify_callback.py（`:230-255` 续驱段）
@server/initiatives/services/feature_solution_service.py（`:130-160` 建会话与三参数、`:210-235` `force_confirm=True` 续驱）
@server/subagent/api/callbacks.py（⭐ `:420-490` `_schedule_chat_plan_resume` 全文（尤其 f/g 两步的 `BlockingTaskResult` 装配与 `task_completed`）、`:2150-2177` 与 `:2377-2396` 两个蓝图 barrier 全文、`:128` 的 `last_output["source"]` 分支与 `:2003` / `:2287` 蓝图容器写的 source 值）
@server/services/process_runtime/blueprint_resume.py（`:112-143` 的 pause 短路判据 docstring —— 蓝图版 `_maybe_suspend` 逐字抄它、`aresume_blueprint_session` 的签名）
@.planning/STATE.md（§并行开发边界纪律六条；Blockers「116 的入口切换在同步点 2 前不可执行」）
</context>

<tasks>

<task type="auto">
  <name>Task 1: 六个续驱点全部改 build_engine_for_session（连 driver）+ 源码扫描断言</name>
  <files>server/workflows/nodes/ai/plan_research.py, server/agents/tools/plan_research_tools.py, server/mcp_tools/orchestration_delegate.py, server/services/process_runtime/answer_resume.py, server/feishu/callbacks/plan_clarify_callback.py, server/initiatives/services/feature_solution_service.py, server/tests/services/process_runtime/test_entry_dispatch_wiring.py</files>
  <read_first>server/workflows/nodes/ai/plan_research.py **L340-380**（`_build_engine` 全文：形参、`node_execution_id` 从哪来、返回值如何被调用方消费、后续 `adrive_…` 的调用行）；server/agents/tools/plan_research_tools.py **L110-160**（建会话 + `build_orchestration_engine()` + `adrive_convergence_session_to_pause_or_terminal(engine, session, max_steps=_MAX_ADVANCE_STEPS)` 三段的**逐字形态**与那段解释「与工作流节点同一工厂」的注释 —— 注释要同步改）；server/mcp_tools/orchestration_delegate.py **全文**（⭐ `:4` 与 `:131` 的「entrypoint=workflow」docstring、`:150-200` 建会话 + `build_orchestration_engine(skip_clarification=True)` + 续驱三段、`:162-163` 的 best-effort 包裹形态）；server/services/process_runtime/answer_resume.py **全文 ~130 行**（⭐ `:95-110` 的缺省 engine 分支：**engine 与 driver 两行都硬编码** + 调用方可传入 engine 的既有形参 —— 改造要保持「调用方传入时优先用调用方的」这条既有语义）；server/feishu/callbacks/plan_clarify_callback.py **L225-260**（续驱段全文与 `node_execution_id` 的来源）；server/initiatives/services/feature_solution_service.py **L120-240**（建会话段的三个 feature list 参数、`:210-235` 的 `build_orchestration_engine(force_confirm=True)` 与续驱、⚠️ **`force_confirm` 注入的是旧链 `ClarifyAdapter` 的题目组装器** —— 蓝图链无对应面）</read_first>
  <action>
**逐点改造（六处），统一形态：**

```
engine, adrive = build_engine_for_session(
    session,
    node_execution_id=<该点原有的值，无则省略>,
    skip_clarification=<该点原有的值，无则省略>,
    force_confirm=<该点原有的值，无则省略>,
)
session = await adrive(engine, session, max_steps=<该点原有的值，无则省略>)
```

⭐ **`skip_clarification` / `force_confirm` 照原样传给 `build_engine_for_session`**（它内部只在 `technical_plan` 分支透传给 `build_orchestration_engine`，蓝图分支丢弃并落一条 `blueprint_engine_ignored_legacy_flag`）——这样六个调用点的**旧链行为逐字不变**，而蓝图分支自动免疫。⛔ 不在调用点做 `if process_type` 分支（那会在六处复制同一份判据）。

| # | 文件:行 | 改法要点 |
|---|---|---|
| ① | `plan_research.py:361`（`_build_engine`） | 该方法改为返回二元组（或改名为 `_build_runtime`）；**全部调用点同步改**。⚠️ 若该方法被测试 patch，同步改测试 |
| ② | `plan_research_tools.py:134` | 与 `:137` 的 `adrive_…` 一起换；那段「与工作流节点同一 `build_orchestration_engine`」的注释同步改写成「与工作流节点同一 `build_engine_for_session`（engine 与 driver 一起按 `process_type` 分派）」 |
| ③ | `orchestration_delegate.py:179` | `skip_clarification=True` 照原样传给分派器；⛔ **不在此处判 process_type** |
| ④ | `answer_resume.py:102-103` | ⭐ **两行一起换**（engine + driver）。⚠️ 保持「调用方传入 engine 时优先用调用方的」这条既有语义：只有缺省分支走分派器；若调用方只传 engine 不传 driver，driver 仍由分派器给（此时**必须按 session 的 process_type 选**，⛔ 不能沿用旧 driver） |
| ⑤ | `plan_clarify_callback.py:242` | 带 `node_execution_id` 透传 |
| ⑥ | `feature_solution_service.py:222` | `force_confirm=True` 照原样传给分派器 |

⛔ **不改** `subagent/api/callbacks.py:447` 与 `plan_deepen_service.py:99`（理由见 prohibitions）。

**新建 `server/tests/services/process_runtime/test_entry_dispatch_wiring.py` 的第一组用例**（本 Task 部分，Task 2 继续往同一文件加）：

1. ⭐ **源码扫描：六个文件里直调零命中**。判据用 `ast`（⛔ 不要用朴素 grep —— `import` 行会误伤）：解析每个文件，找 `ast.Call` 且被调用名为 `build_orchestration_engine` 或 `adrive_convergence_session_to_pause_or_terminal`，断言**零命中**。文件清单写成模块级常量 `_REWIRED_FILES`（六项），逐项 `exists()` 后再读。
2. **反向对照（守卫非平凡）**：对 `services/process_runtime/plan_deepen_service.py` 跑同一个判据 ⇒ **必须命中**（证明扫描器真的能逮到，且明确登记「这一处是有意不改的」）。
3. **分派器被真的用上**：六个文件里 `build_engine_for_session` 各命中 ≥1（`ast` 判 `ast.Call`）。
4. ⭐ **`answer_resume` 的 driver 也换了**：对 `answer_resume.py` 单独断言「函数体内不存在 `adrive_convergence_session_to_pause_or_terminal` 的调用」，且存在 `build_engine_for_session` 的调用。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/process_runtime/test_entry_dispatch_wiring.py -x -q && uv run pytest tests/services/test_answer_resume.py tests/workflows -q -k "plan_research or clarify" </automated>
  </verify>
  <acceptance_criteria>
- `cd server && uv run pytest tests/services/process_runtime/test_entry_dispatch_wiring.py -q` 全绿。
- ⭐ **六点全改（ast 判据，`import` 行不误伤）**：`cd server && uv run python -c "
import ast
files = ['workflows/nodes/ai/plan_research.py','agents/tools/plan_research_tools.py','mcp_tools/orchestration_delegate.py','services/process_runtime/answer_resume.py','feishu/callbacks/plan_clarify_callback.py','initiatives/services/feature_solution_service.py']
bad = []
for f in files:
    t = ast.parse(open(f).read())
    for n in ast.walk(t):
        if isinstance(n, ast.Call):
            name = n.func.attr if isinstance(n.func, ast.Attribute) else getattr(n.func, 'id', '')
            if name in ('build_orchestration_engine','adrive_convergence_session_to_pause_or_terminal'):
                bad.append((f, n.lineno, name))
assert not bad, bad
print('all 6 resume points rewired OK')"` 输出 `all 6 resume points rewired OK`。
- ⭐ **⛔ 两处有意不改**：`git diff $PHASE_BASE -- server/services/process_runtime/plan_deepen_service.py` **为空**；`cd server && uv run python -c "
src = open('subagent/api/callbacks.py').read()
i = src.index('async def _schedule_chat_plan_resume')
nxt = src.index('\nasync def ', i+1)
assert 'build_orchestration_engine' in src[i:nxt], '⛔ callbacks.py:447 那条链不改（对蓝图不可达）'
print('untouched-by-design OK')"` 输出 `untouched-by-design OK`。
- **分派器真的被用上**：`rg -c "build_engine_for_session" server/workflows/nodes/ai/plan_research.py server/agents/tools/plan_research_tools.py server/mcp_tools/orchestration_delegate.py server/services/process_runtime/answer_resume.py server/feishu/callbacks/plan_clarify_callback.py server/initiatives/services/feature_solution_service.py` 六个文件**各 ≥1**。
- ⭐ **旧链行为零回归**：`cd server && uv run pytest tests/services/test_answer_resume.py -q` 全绿；`uv run pytest tests/ -q -k "plan_research or feature_solution or orchestration_delegate or plan_clarify"` 无新增失败。
- **删除行核算**：六个文件的 `git diff $PHASE_BASE -- <file> | grep -c '^-[^-]' || true` 逐个不超过 execution_context 表里的上界，逐行登记进 SUMMARY。
- `cd server && uv run ruff check` 对六个改动文件与新测试文件通过。
  </acceptance_criteria>
  <done>六个续驱点全部经 `build_engine_for_session` 拿 engine 与 driver（`answer_resume` 的两行一起换），旧链开关照原样透传因而旧链行为逐字不变；`ast` 扫描断言把「漏改一处」变成机器可逮，并用 `plan_deepen_service.py` 的反向命中证明扫描器非平凡；两处有意不改的点有显式断言与登记。</done>
</task>

<task type="auto">
  <name>Task 2: 四个入口的蓝图路径（字面量开关 + project_id 推导 + 拒绝发起）+ feature_segments 透传</name>
  <files>server/workflows/nodes/ai/plan_research.py, server/agents/tools/plan_research_tools.py, server/mcp_tools/orchestration_delegate.py, server/initiatives/services/feature_solution_service.py, server/tests/services/process_runtime/test_entry_dispatch_wiring.py</files>
  <read_first>server/workflows/nodes/ai/plan_research.py **L180-330**（`missing_requirement` 的失败范式 `:194-199` 逐字、`_resolve_space` 的实现与它在节点里的调用点、`_create_session:290-310` 的**逐字实参清单**含 `entrypoint="workflow"`）；server/agents/tools/plan_research_tools.py **L60-140**（工具入参、`_resolve_actor` / `_filter_repos_in_space` 的签名与 space 上下文从哪来、失败范式 `:96-99` 逐字、`start_orchestration(entrypoint="chat", ...)` 的**逐字实参清单**）；server/mcp_tools/orchestration_delegate.py **全文**（`McpWorkItemContext` 从哪个参数进来、`work_item` 与 `created_by` 的取法、`error_response` 的可用性 —— ⚠️ 若本模块拿不到 `error_response`，返回值形态按其**既有失败返回**照抄，⛔ 不新造一种）；server/mcp_tools/models.py **L265-290**（`McpWorkItemContext.space` FK）；server/initiatives/services/feature_solution_service.py **L120-240**（三个 feature list 参数的来源、`feature_meta` 的键集、既有失败返回形态）；server/services/process_runtime/blueprint_intake.py（116-02 交付：`aresolve_project_id` 的**逐字签名与四个分支的入参名**、`BlueprintIntakeRejected` 的属性）；server/services/process_runtime/blueprint_entry_switch.py（116-01 交付：`aresolve_entry_process_type` 签名与四个 `ENTRY_*` 常量）；server/tests/services/process_runtime/test_blueprint_entry_switch.py（⭐ 116-01 的 `ast` 字面量守卫的**扫描面清单与判据** —— 本 Task 的四处调用必须满足它）</read_first>
  <action>
**四个入口统一三步（形态一致，参数各异）：**

```
process_type = await aresolve_entry_process_type("<字面量>")
if process_type == "technical_blueprint":
    try:
        project_id = await aresolve_project_id(entry="<同一字面量>", <该入口的权威上下文>)
    except BlueprintIntakeRejected as exc:
        return <该入口的失败出口>(detail=exc.detail)
    session = await start_blueprint_orchestration(
        entrypoint=<该入口既有的 entrypoint 值，一字不改>,
        requirement_text=...,
        project_id=project_id,
        entry_key="<同一字面量>",
        <其余实参与旧路径逐字相同>,
    )
else:
    session = await start_orchestration(<逐字与改动前相同>, entry_key="<同一字面量>")
```

⭐ **`entry_key` 两条路径都传**（旧链靠它做退役观察分桶）；⭐ **`entrypoint` 实参一字不改**（它进 `ConvergenceSession` 列且有既有消费方，MCP 那条 `"workflow"` 是既有约定）。⭐ **`aresolve_entry_process_type` 与 `entry_key` 的实参必须是**字面量字符串**，⛔ 不用常量名（116-01 的守卫判 `ast.Constant`，`ast.Name` 会转红）。

| 入口 | 字面量 | 权威上下文 → `aresolve_project_id` | 失败出口 |
|---|---|---|---|
| workflow（`plan_research._create_session`） | `"workflow"` | `space=<_resolve_space(context) 的结果>` | `NodeResult(status="failed", error=exc.detail, next_handle="error")`（范式 `:194-199`） |
| chat（`plan_research_tools`） | `"chat"` | `space=<该工具已有的 space 上下文>` 或 `conversation=<conversation 对象>` | `ToolResult(success=False, error=exc.detail)`（范式 `:96-99`） |
| MCP（`orchestration_delegate`） | `"mcp"` | ⭐ `work_item_context=<McpWorkItemContext>`（**必须过 `_aresolve_project`，⛔ 绝不透传 `context.space_id`**，P-8） | 该模块既有的失败返回形态（`error_response(code, detail)` 或等价） |
| feature list（`feature_solution_service`） | `"feature_list"` | `feature_meta=<既有 feature_meta dict>` | 该 service 既有的失败返回形态 |

**额外要点：**

- ⭐ **feature list 的 `feature_segments` / `feature_meta` / `mode` 三参数照原样传进 `start_blueprint_orchestration`**（116-02 的 `_h_bp_decompose` 会直采映射成 `feature_points`，⛔ 不走 LLM）。⛔ **不移植 `force_confirm`** —— 蓝图链的「强制确认关联仓」由 `repo_confirmation` 硬门天然承担；在该处**加一行注释**写明这条定夺与理由。
- ⭐ **MCP 的响应键本 plan 不动**：`create_feishu_technical_plan` 追加 `blueprint_artifact_id` / `blueprint_status` / `pending_clarifications[]` 与 `status="partial"` 归 **116-06**。本 plan 只让 MCP 入口**能建蓝图会话并续驱**；开关开启时的响应形态**暂时与旧链相同**（这不构成契约破坏——开关默认关闭，且 116-06 在同一相位内补齐）。⭐ 在 `orchestration_delegate.py` 加一行 TODO 注释指向 116-06，并在 SUMMARY 登记这条跨 plan 交接。
- ⭐ **workflow 节点的 `_map_terminal` 本 plan 不改**（`plan_research.py:544-570`）：它把 `DONE` 无条件映射成 `completed` 并把 `plan` 喂给下游 `human_approval(plan_feishu)` / `ai_coding`，而蓝图的 `DONE` 语义是「等人审」 ⇒ 改成「`pending_review` → `waiting_event` 人审 HITL 挂起」**归同步点 2 后的收尾 plan**（其下游消费形态由 v0.19.0 Phase 109 的 execution 投影承担）。⭐ 在该函数上方**加一行 TODO 注释**写明这条边界与理由（⛔ 不改函数体）。这条同时解释了「为什么 workflow / chat 的开关默认不能翻」。

**`test_entry_dispatch_wiring.py` 的第二组用例：**

5. ⭐ **四入口 × 开关两态**（参数化八条）：开关为 `technical_plan` ⇒ 建出的会话 `process_type == "technical_plan"` 且实参与改动前逐字相同（用 `unittest.mock.patch` 断言 `start_orchestration` 被以既有 kwargs 调用）；开关为 `technical_blueprint` ⇒ 会话 `process_type == "technical_blueprint"` 且 `stage_state.decomposition.project_id` 非空。
6. ⭐ **`aresolve_entry_process_type` 的四处实参是字面量**：直接复跑 116-01 的守卫 `cd server && uv run pytest tests/services/process_runtime/test_blueprint_entry_switch.py -q` 全绿（此时它**不再是空扫描**）。
7. ⭐ **MCP 不透传 `space_id`**：构造 `McpWorkItemContext(space=<space>)`，其下有一个 `Project` ⇒ 建出的蓝图 `meta.project_id` **等于 `Project.id` 且不等于 `space.id`**。
8. ⭐ **四入口 × 推不出 project_id ⇒ 零副作用**（参数化四条）：`ConvergenceSession` 与 `Artifact` 计数与调用前逐字相等，且返回值是各自入口的失败形态（`NodeResult.next_handle == "error"` / `ToolResult.success is False` / MCP 失败体 / service 失败体）。
9. **feature list 的 `feature_segments` 到达 decompose**：开关开启 + 三条 segments ⇒ 新版本的 `requirement_spec.feature_points` 三条，且 LLM 未被调用（patch 断言）。
10. ⛔ **不移植 `force_confirm`**：源码扫描断言 `feature_solution_service.py` 里 `force_confirm` 只出现在旧链分支（`build_engine_for_session` 的实参位），⛔ 不出现在 `start_blueprint_orchestration` 的实参里。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/process_runtime/test_entry_dispatch_wiring.py tests/services/process_runtime/test_blueprint_entry_switch.py -x -q</automated>
  </verify>
  <acceptance_criteria>
- `cd server && uv run pytest tests/services/process_runtime/test_entry_dispatch_wiring.py -q` 全绿；`rg -c "def test_" server/tests/services/process_runtime/test_entry_dispatch_wiring.py` ≥ **14**。
- ⭐ **116-01 的字面量守卫此刻覆盖真实调用点且仍绿**：`cd server && uv run pytest tests/services/process_runtime/test_blueprint_entry_switch.py -q` 全绿；`cd server && uv run python -c "
import ast
files = ['workflows/nodes/ai/plan_research.py','agents/tools/plan_research_tools.py','mcp_tools/orchestration_delegate.py','initiatives/services/feature_solution_service.py']
hits = 0
for f in files:
    t = ast.parse(open(f).read())
    for n in ast.walk(t):
        if isinstance(n, ast.Call):
            name = n.func.attr if isinstance(n.func, ast.Attribute) else getattr(n.func, 'id', '')
            if name == 'aresolve_entry_process_type':
                arg = n.args[0] if n.args else next((k.value for k in n.keywords if k.arg == 'entry'), None)
                assert isinstance(arg, ast.Constant) and isinstance(arg.value, str), (f, n.lineno, ast.unparse(arg))
                hits += 1
assert hits >= 4, hits
print('4 literal switch call sites OK')"` 输出 `4 literal switch call sites OK`。
- ⭐ **project_id 推导单点复用**：`rg -c "aresolve_project_id" server/workflows/nodes/ai/plan_research.py server/agents/tools/plan_research_tools.py server/mcp_tools/orchestration_delegate.py server/initiatives/services/feature_solution_service.py` 四个文件**各 ≥1**；`rg -n "_aresolve_project\(" server/mcp_tools/orchestration_delegate.py server/agents/tools/plan_research_tools.py` **零命中**（换算收口在 `blueprint_intake`，⛔ 四处不各写一份）。
- ⭐ **MCP 不透传 space_id**：`cd server && uv run python -c "
src = open('mcp_tools/orchestration_delegate.py').read()
assert 'aresolve_project_id' in src
i = src.index('start_blueprint_orchestration')
seg = src[max(0,i-900):i+600]
assert 'project_id=project_id' in seg or 'project_id=' in seg, '必须显式传 project_id'
assert 'project_id=context.space_id' not in src and 'project_id=str(context.space_id)' not in src, '⛔ 透传 space_id'
print('MCP P-8 wiring OK')"` 输出 `MCP P-8 wiring OK`。
- ⭐ **⛔ 不移植 force_confirm**：`cd server && uv run python -c "
import ast
t = ast.parse(open('initiatives/services/feature_solution_service.py').read())
for n in ast.walk(t):
    if isinstance(n, ast.Call):
        name = n.func.attr if isinstance(n.func, ast.Attribute) else getattr(n.func, 'id', '')
        if name == 'start_blueprint_orchestration':
            assert not any(k.arg == 'force_confirm' for k in n.keywords), '⛔ force_confirm 不得进蓝图链'
print('no force_confirm leak OK')"` 输出 `no force_confirm leak OK`。
- ⭐ **workflow 终态映射未被改动且有边界注释**：`cd server && uv run python -c "
src = open('workflows/nodes/ai/plan_research.py').read()
i = src.index('def _map_terminal')
assert '同步点 2' in src[max(0,i-800):i], '缺 _map_terminal 的边界 TODO 注释'
print('map_terminal boundary noted OK')"` 输出 `map_terminal boundary noted OK`；且 `_map_terminal` 函数体本身在 `git diff $PHASE_BASE` 中**无改动行**（人工核对并登记）。
- ⭐ **拒绝发起零副作用**：`rg -n "objects.count()" server/tests/services/process_runtime/test_entry_dispatch_wiring.py` 命中 ≥4。
- ⭐ **开关关闭时零回归**：`cd server && uv run pytest tests/ -q -k "plan_research or feature_solution or orchestration_delegate"` 无新增失败。
- `cd server && uv run ruff check` 对四个改动文件与测试文件通过。
  </acceptance_criteria>
  <done>四个入口各自具备完整的蓝图路径：开关按字面量查（116-01 的守卫此刻覆盖真实调用点且仍绿）、`project_id` 经单一收口推导且 MCP 分支必过 `_aresolve_project`、推不出即按各自形态如实回错且 DB 零副作用；feature list 的 segments 直达 decompose 且 ⛔ 不移植 `force_confirm`；workflow 的终态映射与 MCP 的响应键两处跨相位边界各有 TODO 注释与 SUMMARY 登记。</done>
</task>

<task type="auto">
  <name>Task 3: chat 三条断链（蓝图版 _maybe_suspend / _map_terminal + 两个 barrier 的 CHAT 回灌）+ 两条反向变异 + 全量后端门</name>
  <files>server/agents/tools/plan_research_tools.py, server/subagent/api/callbacks.py, server/tests/agents/test_chat_blueprint_entry.py</files>
  <read_first>server/agents/tools/plan_research_tools.py **L200-300 全文**（⭐ `_maybe_suspend` 两个分支的**逐字返回键集**：`waiting_clarification` 分支的 `{clarification_id, pending, marker, question, options, allow_freeform, session_id}` 与 `PLAN_CLARIFICATION_RENDER_MARKER` 的语义注释（「marker 仅作前端渲染信号 …… 物理隔离，plan 澄清绝不写 ConversationIntentTrace」）；`waiting_event` 分支的 `register_blocking_task` 调用与 `{__blocking_task__, task_type, task_id, session_id, params, placeholder}` 键集；`_map_terminal` 全文的两条返回）；server/services/process_runtime/blueprint_resume.py **L112-200**（⭐ pause 短路判据 docstring 与其**实现代码** —— 蓝图版 `_maybe_suspend` 的判据必须与它同源：open+blocking `BlueprintThread`（**不传 `kind`**）**且**无待调研仓；`acollect_pending_research_repos` 的用法；`aresume_blueprint_session` 的签名）；server/delivery/models/blueprint_thread.py **L69-130**（`BlueprintThread` 的 `status` / `blocking` / `kind` 取值与 `Meta` **无 `ordering`** —— 查询要显式排序）；server/delivery/services/blueprint_lifecycle_service.py（`BlueprintStatus` 11 态的**确切成员名**）；server/subagent/api/callbacks.py **L415-495 全文**（⭐ `_schedule_chat_plan_resume` 的 (a)–(g) 七步，尤其 **f/g 两步**：`success = plan_session.status == ConvergenceSessionStatus.DONE`、`output_text = str(plan_session.current_artifact_version_id or "") if success else ""`、`error_text`、`BlockingTaskResult` 四键、`get_barrier_manager().task_completed(str(plan_session.id), result)` 与其上方那条「Pitfall 3：task_id 用 `str(plan_session.id)` 而非 session_id；chat barrier 注册键见 `plan_research_tools.py:249`」注释）、**L2150-2177** 与 **L2377-2396** 两个蓝图 barrier 全文（两者都以 `aresume_blueprint_session` 结尾，且都有 `ImportError` / `resume is None` 的早退）；server/agents/tools/blocking_task_registry.py（`register_blocking_task` 与 `BarrierManager` 的公开面：`task_completed(key, result)` 的返回值语义）</read_first>
  <action>
**① `plan_research_tools._maybe_suspend` 加蓝图分支（删除行 ≤4）。**

在函数开头按 `session.process_type` 分流（⛔ 不改旧链两个分支的任何一行）：

```
if str(getattr(session, "process_type", "")) == BLUEPRINT_PROCESS_TYPE:
    return await _maybe_suspend_blueprint(session, conversation_id)
```

新写 `async def _maybe_suspend_blueprint(session, conversation_id) -> ToolResult | None`，判据**与 `blueprint_resume.adrive_…` 的 pause 短路同源**（⛔ 不自造第二套）：

- `status == WAITING_CLARIFICATION` **且**存在 open+blocking 的 `BlueprintThread`（⭐ **不传 `kind`** —— `ai_clarification` 与 `repo_confirmation` 两类都算；查询**显式 `order_by("created_at")`**，`BlueprintThread.Meta` 无 `ordering`）⇒ 返回**挂起 marker**：复用 `PLAN_CLARIFICATION_RENDER_MARKER`（⛔ 不新建第二个 marker —— 前端按 `marker` 分派渲染，新 marker 会让蓝图澄清在对话里**什么都不渲染**），键集与旧链分支**逐字一致**但 `clarification_id` 位放 `str(thread.id)`、`question` 放线程首题（⭐ 过 `redact_secrets_in_text`），并追加一个 `artifact_id` 键供前端深链到查看器。⚠️ **键集变化要在 SUMMARY 里逐字登记**（前端消费方在 chat 侧）。
- `status == WAITING_EVENT` **且**仍有在途调研（`aall_research_tasks_terminal(session.id)` 为 False，此判据对蓝图**有效**，因为蓝图确实建 `RepoResearchTask`）⇒ 走既有 `register_blocking_task` + blocking task marker，键集**逐字复用旧链分支**（⭐ `task_id` 与 `params.session_id` 都用 `str(session.id)` —— 这是 barrier 回灌 key 对齐的另一半）。
- 其余 ⇒ `return None`。

**② `plan_research_tools._map_terminal` 加蓝图分支（删除行 ≤4）。**

同样在函数开头分流到新写的 `_map_terminal_blueprint(session, artifact)`：按 **`blueprint_status`** 分档（⛔ 不按 `session.status`）：

| `blueprint_status` | 返回 |
|---|---|
| `needs_clarification` | ⭐ **挂起 marker**（复用 `_maybe_suspend_blueprint` 的第一档）而**不是** `success=False` |
| `pending_review` | `ToolResult(success=True, output={session_id, artifact_id, current_status, message="蓝图已产出，等待人工终审"})` |
| `confirmed` / `implementing` / `implemented` | `success=True` + 对应文案 |
| `failed` | `success=False` + `session.error` 的消息 |
| 其余（`researching` / `drafting` / `reviewing` …） | `success=True` + 「仍在进行中」文案（⛔ **不报失败** —— 会话是 DONE 但状态还在中间态属可诊断的异常，报「失败」会误导用户） |

⚠️ ⛔ **返回体的字典键不得出现字面 `blueprint_status`**（INV-6 `_RE_FIELD_DICT_KEY` 扫全 `server/`）⇒ 用 `current_status`（114-05 立的既有解法）。

**③ 两个蓝图 barrier 补 `entrypoint == CHAT` 回灌（删除行 0）。**

在 `_trigger_blueprint_research_barrier` 与 `_trigger_blueprint_repo_plan_barrier` 的 `await resume(blueprint_session)` **之后**各追加一段（提成一个共享 helper `_afeedback_chat_blueprint_barrier(blueprint_session)`，⛔ 不复制两份）：

```
async def _afeedback_chat_blueprint_barrier(blueprint_session) -> None:
    """蓝图会话由 chat 入口发起时，把结果回灌 chat 的 blocking task waiter。

    与 _schedule_chat_plan_resume 的 (f)(g) 两步对称：蓝图侧的两个 barrier 此前**只续驱
    不回灌** ⇒ chat 对话永远停在「深入调研容器运行中…」。
    ⭐ task key 必须是 str(session.id)，与 plan_research_tools.py:246-256 注册 blocking task
    时的 {"task_id": str(session.id)} 逐字对齐 —— key 不一致的症状是 waiter 永远等不到，
    且**不抛异常**。
    """
```
- 守门：`str(getattr(blueprint_session, "entrypoint", "")) != CHAT` ⇒ 直接返回（⭐ 与 `callbacks.py:430` 的 `entrypoint == CHAT` 守门对称；⚠️ 取 `CHAT` 常量用本文件既有的那个，⛔ 不写裸字符串）。
- 重读会话（barrier 之后状态已变）→ 装配 `BlockingTaskResult` 四键，形状逐字照 `:470-478`：`success = <会话到终态且蓝图状态不是 failed>`、`output = str(session.current_artifact_version_id or "")`、`error = "" if success else str(session.error or {})`。⚠️ ⭐ **蓝图的「成功」判据不能只看 `ConvergenceSessionStatus.DONE`** —— 蓝图 DONE 语义是「等人审」，那**也是成功**（对 chat 用户而言方案已产出）；`failed` 才是失败。
- `satisfied = await get_barrier_manager().task_completed(str(blueprint_session.id), result)` + 一条 `blueprint_chat_barrier_notified` 事件（`category="sampling"` / `component="subagent"` / `session_id` / `barrier_satisfied`）。
- ⭐ **整段 `try/except Exception` 只 log**（best-effort，⛔ 绝不反噬 barrier 与容器回调）。

**④ 新建 `server/tests/agents/test_chat_blueprint_entry.py`**，docstring「守六件事」：

1. ⭐ **等澄清的健康会话返回挂起 marker 而不是失败**（P-7 的头号靶子）：造一条 chat 蓝图会话停在 `spec_gate`（`status=waiting_clarification` + 一条 open+blocking `ai_clarification` `BlueprintThread`）⇒ 工具返回 `ToolResult(success=True)` 且 `output["marker"] == PLAN_CLARIFICATION_RENDER_MARKER`、`output["pending"] is True`。**⛔ 断言里必须显式写 `success is True`** —— 那是与现状（`success=False, error="plan session failed"`）的直接对立面。
2. ⭐ **`repo_confirmation` 线程同样算挂起**（判据不传 `kind` 的证据）：把线程 kind 换成 `repo_confirmation` ⇒ 仍返回挂起 marker。
3. **零线程时不误挂起**（断言非恒真）：`waiting_clarification` 但无 open+blocking 线程 ⇒ 不返回挂起 marker（落到 `_map_terminal_blueprint`）。
4. ⭐ **终态分档**：`pending_review` ⇒ `success=True` 且 `output["current_status"] == "pending_review"`；`failed` ⇒ `success=False`。⛔ 断言 `output` 里**不含**字面 `blueprint_status` 键。
5. ⭐ **barrier 回灌满足 waiter**：注册一个 key 为 `str(session.id)` 的 blocking task waiter ⇒ 触发 `_trigger_blueprint_research_barrier`（全部 task 终态）⇒ 断言 `task_completed` 被以**该 key** 调用且 waiter 被满足。**并列一条 key 错配的反向对照**：用 `str(uuid4())` 作 key ⇒ waiter 不被满足（证明第一条不是恒真）。
6. **非 chat 入口不回灌**：`entrypoint="workflow"` 的蓝图会话 ⇒ `task_completed` **零调用**。

⭐ **两条反向变异（各实跑一次，红/绿输出记进 SUMMARY）**：
- (a) 去掉 `_maybe_suspend` 的蓝图分流 ⇒ 用例 1 转红（返回 `success=False`）。
- (b) 去掉 barrier 的回灌调用 ⇒ 用例 5 转红（waiter 未满足）。

**⑤ 全量后端门**（本 Task 收尾）：`cd server && uv run pytest tests/ -q` 与基线 **8609 passed / 1 failed** 逐条比对；`makemigrations --check --dry-run` 退出码 0 且零新 migration。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/agents/test_chat_blueprint_entry.py -x -q && uv run pytest tests/subagent tests/agents -q</automated>
  </verify>
  <acceptance_criteria>
- `cd server && uv run pytest tests/agents/test_chat_blueprint_entry.py -q` 全绿；`rg -c "def test_" server/tests/agents/test_chat_blueprint_entry.py` ≥ **8**。
- ⭐ **头号断言是「success is True」而不是「不等于某值」**：`rg -n "success is True" server/tests/agents/test_chat_blueprint_entry.py` 命中；`rg -n "PLAN_CLARIFICATION_RENDER_MARKER" server/tests/agents/test_chat_blueprint_entry.py` 命中。
- ⭐ **判据与 blueprint_resume 同源（不传 kind）**：`cd server && uv run python -c "
src = open('agents/tools/plan_research_tools.py').read()
i = src.index('_maybe_suspend_blueprint')
body = src[i:i+2500]
assert 'BlueprintThread' in body, '判据必须用 BlueprintThread'
assert 'ahas_pending' not in body, '⛔ 蓝图分支不得用 ClarificationService.ahas_pending（对蓝图恒 False）'
assert 'kind=' not in body.split('BlueprintThread')[1][:400], '⛔ 不传 kind（两类线程都算）'
assert 'order_by' in body, 'BlueprintThread.Meta 无 ordering，查询必须显式排序'
print('blueprint suspend criteria OK')"` 输出 `blueprint suspend criteria OK`。
- ⭐ **复用既有 marker**：`rg -n "PLAN_CLARIFICATION_RENDER_MARKER" server/agents/tools/plan_research_tools.py` 命中 ≥2（旧链 + 蓝图分支）；`cd server && uv run python -c "
src = open('agents/tools/plan_research_tools.py').read()
import re
markers = set(re.findall(r'[A-Z_]*RENDER_MARKER', src))
assert markers == {'PLAN_CLARIFICATION_RENDER_MARKER'}, markers
print('single marker OK')"` 输出 `single marker OK`（⛔ 不新建第二个 marker —— 前端按 marker 分派，新 marker 会让蓝图澄清在对话里什么都不渲染）。
- ⭐ **⛔ 返回体不出现字面 `blueprint_status` 键**：`rg -n "['\"]blueprint_status['\"]\s*:" server/agents/tools/plan_research_tools.py` **零命中**；`cd server && uv run pytest tests/delivery/test_blueprint_inv6_guard.py -q` 全绿。
- ⭐ **barrier 回灌是共享 helper 不是两份复制**：`cd server && uv run python -c "
src = open('subagent/api/callbacks.py').read()
assert src.count('_afeedback_chat_blueprint_barrier') >= 3, '一个定义 + 两处调用'
assert src.count('async def _afeedback_chat_blueprint_barrier') == 1, '⛔ 不得复制两份'
print('shared feedback helper OK')"` 输出 `shared feedback helper OK`。
- ⭐ **task key 逐字对齐**：`cd server && uv run python -c "
src = open('subagent/api/callbacks.py').read()
i = src.index('async def _afeedback_chat_blueprint_barrier')
body = src[i:i+2200]
assert 'task_completed(str(' in body.replace(' ', '').replace('\n',''), 'key 必须是 str(session.id)'
print('barrier key OK')"` 输出 `barrier key OK`；且测试里有 key 错配的反向对照（`rg -n "uuid4" server/tests/agents/test_chat_blueprint_entry.py` 命中）。
- ⭐ **回灌是 best-effort**：`rg -n "except Exception" server/subagent/api/callbacks.py | rg -c "" ` 与改动前相比 **+1**（新 helper 自带一层）；`git diff $PHASE_BASE -- server/subagent/api/callbacks.py | grep -c '^-[^-]' || true` 输出 **0**（纯追加）。
- ⭐ **两条反向变异各实跑一次并登记**：SUMMARY 里写明「去掉 X ⇒ 用例 Y 转红（贴错误首行）；恢复即绿」。
- ⭐ **全量后端门**：`cd server && uv run pytest tests/ -q` 相对基线（8609 passed / 1 failed）**无新增失败** —— 逐条差异写进 SUMMARY。
- `cd server && uv run python manage.py makemigrations --check --dry-run` 退出码 **0**；`git status --porcelain server/*/migrations/` 输出**为空**。
- 相位边界自检：`git diff $PHASE_BASE --name-only` 只含本 plan 声明的九个文件；⛔ 不含六个冻结文件、`repo_router_v2.py`、`plan_deepen_service.py`、`web/`。
- `cd server && uv run ruff check agents/tools/plan_research_tools.py subagent/api/callbacks.py tests/agents/test_chat_blueprint_entry.py` 通过。
  </acceptance_criteria>
  <done>chat 三条断链全部补上：等澄清的健康蓝图会话返回**挂起 marker 而不是「方案编排失败」**（判据与 `blueprint_resume` 的 pause 短路同源、不传 `kind`、复用既有渲染 marker）；终态按 `blueprint_status` 分档且 `pending_review` 算成功；两个蓝图 barrier 经**一个共享 helper** 补上 `entrypoint == CHAT` 的回灌且 task key 与注册键逐字对齐、整段 best-effort、纯追加；两条反向变异各完成一次真实实跑；全量后端门相对基线无新增失败。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 运行时开关值 → 入口 process 选择 | 一个键控制一个入口；从 session 反推会让一个键连带切走两个入口 |
| 入口权威上下文 → `meta.project_id` | 全链范围闸 / 图谱 / 导出的唯一来源；MCP 侧存在 Space ↔ Project 的同名混淆 |
| 容器回调（半可信 runner 输入）→ barrier 回灌 | 回灌 key 由服务端从会话反查，⛔ 不取 runner 提供的值 |
| 蓝图澄清题（LLM 产出）→ chat 对话渲染 | 半可信文本进对话与日志 |
| 蓝图 `DONE` 语义 → workflow 下游 `ai_coding` | 蓝图 `DONE` = 等人审；映射成 completed 会把**未经人审**的蓝图喂给编码代理 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-116-18 | Elevation of Privilege | workflow 开关翻成 blueprint ⇒ `_map_terminal` 把 `DONE`（= 等人审）映射成 completed，未经人审的蓝图直接进 `ai_coding` 建分支写代码（正面违反 RELY-01） | mitigate | ⭐ **开关默认全部 `technical_plan` 且本 plan 不翻默认**；`_map_terminal` 函数体一行不改并加边界 TODO 注释；翻默认与终态改 HITL 挂起顺延同步点 2 后的收尾 plan（下游消费形态归 0.19 Phase 109） |
| T-116-19 | Spoofing | 开关从 `session.entrypoint` 反推 ⇒ MCP 流量伪装成 workflow，一个键切走两个入口 | mitigate | 四处调用点全部字面量 + 116-01 的 `ast` 守卫（此刻覆盖真实调用点，本 plan 的验收里复跑它） |
| T-116-20 | Elevation of Privilege | MCP 入口把 `McpWorkItemContext.space_id` 当 `meta.project_id` ⇒ 蓝图落地即 20 个端点恒 400、图谱恒不入、导出恒不可用，且三条都「安静地什么都没发生」 | mitigate | 推导单点收口在 `blueprint_intake.aresolve_project_id`；源码扫描断言 `project_id=context.space_id` 零命中；用例断言 `meta.project_id != space.id` 且 `Project` 存在 |
| T-116-21 | Denial of Service | 漏改 `answer_resume.py:102` 或 `plan_clarify_callback.py:242` ⇒ 蓝图会话**作答后无人续驱**，卡在 `waiting_clarification` 永不推进且零异常 | mitigate | `ast` 源码扫描断言六个文件里直调零命中 + 用 `plan_deepen_service.py` 的反向命中证明扫描器非平凡 |
| T-116-22 | Denial of Service | chat 蓝图 barrier 不回灌 ⇒ 对话永远停在「深入调研容器运行中…」（waiter 永不满足，**不抛异常**） | mitigate | 共享 helper 补回灌 + task key 逐字对齐 + 用例 5 及其 key 错配反向对照 + 变异 (b) 实跑 |
| T-116-23 | Repudiation | 健康的等澄清会话被报成「方案编排失败」⇒ 用户以为系统坏了并放弃，真实待办（回答问题）被永久掩埋 | mitigate | 蓝图版 `_maybe_suspend` / `_map_terminal` + 用例 1 断言 `success is True` + 变异 (a) 实跑 |
| T-116-24 | Tampering | barrier 回灌的 key 取自容器上报的字段 ⇒ runner 可满足任意 waiter | mitigate | key 一律 `str(blueprint_session.id)`（服务端从会话反查），⛔ 不取 `last_output` 里的任何值；源码扫描断言 |
| T-116-25 | Information Disclosure | 蓝图澄清题（LLM 产出，半可信）原样进对话与日志 | mitigate | `question` 过 `redact_secrets_in_text`；日志只记长度与计数 |
| T-116-26 | Tampering | 把 `force_confirm` / `skip_clarification` 移植进蓝图链 ⇒ 前者注入旧链 `ClarifyAdapter` 组装器（蓝图无对应面）、后者原地复活 GATE-01 要消灭的「跳过澄清」 | mitigate | 分派器内部只在旧链分支透传；`ast` 断言 `start_blueprint_orchestration` 的实参里无 `force_confirm`；蓝图链无 `clarify` dep 是结构性隔离 |
| T-116-SC | Tampering | 依赖供应链 | accept | 零新增外部依赖、零 migration、零新 stage 名、零新枚举、零新事件常量进 `BLUEPRINT_EVENTS` |
</threat_model>

<verification>
- `cd server && uv run pytest tests/services/process_runtime/ tests/agents/ tests/subagent/ -q` 全绿。
- `cd server && uv run pytest tests/ -q -k "plan_research or feature_solution or orchestration_delegate or plan_clarify or answer_resume"` 无新增失败（⭐ 开关关闭时四入口行为逐字不变的第一道证据）。
- ⭐ `cd server && uv run pytest tests/ -q` 相对基线（8609 passed / 1 failed）**无新增失败**。
- ⭐ `cd server && uv run pytest tests/services/process_runtime/test_blueprint_entry_switch.py -q` 全绿（116-01 的字面量守卫此刻覆盖四处真实调用点，不再是空扫描）。
- `cd server && uv run pytest tests/delivery/test_blueprint_inv6_guard.py -q` 全绿。
- `cd server && uv run python manage.py makemigrations --check --dry-run` 退出码 0；相位内**零** migration。
- ⭐ **两条 chat 断链的反向变异各实跑一次**，红/绿输出首行贴进 SUMMARY。
- 冻结面核算：六个 `technical_plan` 冻结文件、`repo_router_v2.py`、`plan_deepen_service.py`、`subagent/api/callbacks.py:447` 那条链、`plan_research._map_terminal` 函数体 —— `git diff $PHASE_BASE -- <该文件>` 逐个核对；`web/` 零命中。
- 删除行核算：七个改动文件逐个不超过 execution_context 表的上界；`subagent/api/callbacks.py` = **0**。
- `cd server && uv run ruff check` 对全部改动文件通过。
</verification>

<success_criteria>
- **四个入口都真的能走蓝图链**：把 `BLUEPRINT_ENTRY_SWITCH` 的任一键改成 `technical_blueprint`，那个入口就建蓝图会话、落 `blueprint/v1` 骨架、用蓝图 engine 与蓝图 driver 推进——**改一个设置值即生效，回滚也是改一个设置值**。
- **默认值仍全部是 `technical_plan`**，且 `_map_terminal`（workflow 终态映射）一行未改并留下边界注释——「翻默认」在语义上还不成立（蓝图 `DONE` = 等人审，映射成 completed 会把未经人审的蓝图喂给 `ai_coding`），这条顺延同步点 2 后的收尾 plan。
- **六个续驱点一个不漏**，且「漏改一处」被 `ast` 扫描变成机器可逮的红——含 CONTEXT 未点名但实测为真续驱点的 `answer_resume.py:102-103`（连 driver 一起换）与 `plan_clarify_callback.py:242`。
- ⭐ **chat 入口的三条断链全部补上**：等澄清的健康会话返回挂起 marker 而不是「方案编排失败」；两个蓝图 barrier 补上与 `_schedule_chat_plan_resume` 对称的回灌，对话不再永远停在「深入调研容器运行中…」。两条都配了「不做会怎样」的反向变异并实跑过。
- ⭐ **`meta.project_id` 推不出就拒绝发起**，四个入口各自如实回错且 DB 零副作用；MCP 那条 Space/Project 混淆有源码级与用例级双防线。
- 开关关闭时四个入口的行为**逐字不变**（既有测试全绿）；全量后端门相对基线无新增失败、零 migration、零新 stage 名、零新枚举、前端零改动。
</success_criteria>

<output>
完成后创建 `.planning/phases/116-entry/116-03-SUMMARY.md`，provides 至少包含：
- ⭐ **六个续驱点的改造对照表**（文件:行 → 改前 → 改后 → 该点透传了哪些旧链开关），以及**两处有意不改**（`callbacks.py:447` 三重不可达 / `plan_deepen_service.py:99` 非蓝图入口）的登记；
- ⭐ **四个入口的接线表**：字面量 entry / `entrypoint` 实参值（**注意 MCP 是 `"workflow"`**）/ project_id 权威上下文 / 失败出口形态 / 蓝图分支额外传的参数；
- ⭐ **chat 挂起 marker 的逐字键集**（与旧链分支的差异：`clarification_id` 位放 `thread_id`、新增 `artifact_id`）——chat 前端消费方据它渲染；
- ⭐ **`_map_terminal_blueprint` 的 `blueprint_status` → `ToolResult` 分档表**（含「其余中间态一律 success=True 不报失败」的理由）；
- ⭐ **barrier 回灌的 helper 签名、守门条件、`BlockingTaskResult` 四键取值、task key 对齐点**（`plan_research_tools.py:246-256`），以及「蓝图的成功判据不能只看 `ConvergenceSessionStatus.DONE`」的理由；
- ⭐ **两条反向变异的红/绿实跑记录**；
- ⭐ **两处跨相位交接登记**：① MCP 响应键追加三键与 `status="partial"` 归 **116-06**（本 plan 只让 MCP 能建会话并续驱）；② workflow 终态 `pending_review → waiting_event` HITL 挂起与三处触点升级归**同步点 2 后的收尾 plan**；
- 受限面删除行逐行登记；全量 `pytest tests/ -q` 与基线（8609/1）的差异比对；
- **GATE-01 的当前完成度自评**（本相位交付「实现路径 + 开关」，默认切换顺延——与 ROADMAP/REQUIREMENTS 的对账文案对齐）。
</output>
