---
phase: 116-entry
plan: 02
type: execute
wave: 2
depends_on: ["116-01"]
files_modified:
  - server/services/process_runtime/blueprint_intake.py
  - server/services/process_runtime/entrypoint.py
  - server/services/process_runtime/builtin_processes.py
  - server/tests/delivery/test_blueprint_log_redaction_guard.py
  - server/tests/services/process_runtime/test_blueprint_intake.py
autonomous: true
requirements: [GATE-01]
prohibitions:
  - "🔒 **§13.2 冻结六文件零改动**（`git diff` 必须为空）：`decompose_segments.py` / `research_adapter.py` / `architect_merge_adapter.py` / `merged_plan.py` / `clarify_adapter.py` / `render.py`。⛔ 蓝图的功能点拆分**绝不复用 `decompose_segments.py`**（那是冻结的旧链实现），本 plan 在 `blueprint_intake.py` 内自建"
  - "⛔ **绝不给 `start_orchestration` 加 `process_type` 形参**（RESEARCH §A.1）：那会让旧链四个入口共享一个可传错的开关。蓝图会话由**纯追加的第二个函数** `start_blueprint_orchestration` 承担，签名对齐、`create_session` 第一实参改 `\"technical_blueprint\"`"
  - "⛔ **绝不裸写 `artifact.blueprint_status`**（INV-6，`tests/delivery/test_blueprint_inv6_guard.py` 三条正则扫全 `server/`，连裸实例化与 `**{...}` 展开都逮）：状态跳转一律经 `BlueprintLifecycleService.transition`，形状逐字照 `builtin_processes._abp_mark_drafting`。⛔ **不把 `blueprint_intake.py` 加进 `_ALLOWED_WRITER`** —— 唯一 writer 必须保持是 `blueprint_lifecycle_service.py`"
  - "⛔ **骨架 seed 走 `ArtifactService.create` 不是 `add_version`**：此刻 artifact 还不存在；`create` 自己建 Artifact + v1 + 置 `current_version`（`artifact_service.py:53-84`，`_create_sync` 的 v1 装配在 `:105-115`）"
  - "⭐ ⛔ **content 必须显式带 `\"schema_version\": \"blueprint/v1\"`**（P-2）：`validate_blueprint` 对**缺该键**的 content 返回 **`(True, None)`**（pass-through 保 v0 零迁移，§A.2 变体 H 实跑），而 `builtin_types._validate_technical_plan:20` 的判别式是 `if content.get(\"schema_version\")` ⇒ 漏写会**同时**让校验器走 v0、SC-3 的 renderer 走 v0 空壳、SC-4 的 `add_version`/`create` 门控**永不触发**，三条链静默降级且零异常。用例断言**落库版本的 `content[\"schema_version\"]`**，⛔ 不能只断言 `validate_blueprint` 通过"
  - "⭐ ⛔ **`meta.project_id` 必须是 `initiatives.Project.id`，推不出就拒绝发起**（P-8）：⛔ 不建 session、⛔ 不建 artifact、如实回错。落一份 `project_id` 为空/错类型的蓝图 = 该蓝图**全部 20 个端点恒 400、图谱恒不入、导出恒不可用**，且三条都「安静地什么都没发生」、没有任何补救入口"
  - "⛔ **不新增 `CallSource` 枚举值**：功能点拆分复用已注册的 `CallSource.BLUEPRINT_DECOMPOSE`（`agents/call_source.py:112`，清单锁 `tests/test_model_usage_call_source.py:77`）"
  - "⛔ **不给蓝图链新增任何 stage 名**（P-13）：本 plan 只落实既有的 `intake` / `decompose` 两个 handler。若执行期真的需要新 stage 名，**必须同时补前端 `blueprintBlocks.SESSION_STAGE_ALIASES` + `PRE_TIMELINE_SESSION_STAGES`**，否则症状是 `indexOf` 返 `-1`、位序推断整条静默不生效 —— 本 plan 的默认答案是**不加**"
  - "⛔ **不新增 migration**（骨架是 JSON content，零模型变更）、⛔ 不改 `BLUEPRINT_EVENTS`、⛔ 前端零改动（`git diff --name-only | rg '^web/'` 为空）"
  - "⛔ **不在本 plan 改任何入口文件**（`plan_research.py` / `plan_research_tools.py` / `orchestration_delegate.py` / `feature_solution_service.py` 的 `git diff` 必须为空）：四入口接线归 116-03。本 plan 只把「可被调用的能力」建好，并用**直接调用 `start_blueprint_orchestration`** 的用例证明它能跑通"
must_haves:
  truths:
    - "⭐ **新建 `services/process_runtime/blueprint_intake.py`**（模块级 async 函数形态，analog = `blueprint_reflow.py`；⛔ **不是 adapter 类** —— `build_blueprint_engine` 的 deps 名单只有七项，凭空加一个 dep 会破坏「名单与 handler `getattr` 逐字一致」的纪律）"
    - "⭐ **模块级常量 `MINIMAL_BLUEPRINT_SKELETON`（11 个必填顶层键）并单测「它能过 `validate_blueprint`」**：`schema_version` / `meta{title, project_id}` / `requirement_spec{goal, feature_points}` / `repo_associations[]` / `current_state_analysis[]` / `implementation_overview{requirement_narrative, items}` / `api_contracts[]` / `impact_analysis{business_impact, affected_features}` / `interaction_flows[]` / `must_haves{truths, artifacts, key_links}` / `citations{}`。§A.2 已实跑：最小骨架返 **`(True, None)`**，六段全部允许空数组/空对象，`meta.title` + `meta.project_id` 是仅有的两个必须有真实值的字段"
    - "⭐ **骨架的 `requirement_spec.goal` 必须装一个承载需求原文的 paragraph block**（§A.2 结论）：空 `goal` 会让规格门四维打分的 `_goal_text(content)`（`blueprint_spec_gate.py:632`）拿不到任何输入而 fail-closed 到满歧义 ⇒ 第一轮必然开一堆无意义澄清线程。block 形状与 `block_id` 命名沿用 111 的 `iter_blocks` 约定"
    - "⭐ **`aresolve_project_id(...)` 是四条推导链的唯一收口**（116-03 直接 import 复用，⛔ 不让四个入口各写一份）：workflow / chat 走 `board_split_review._aresolve_project(space)`（`:58-69`，优先 `feishu_project_key` 命中、否则首个；`plan_research._send_clarify_card:470` 已是这个用法）；⭐ **MCP 的 `McpWorkItemContext.space` 是 `projects.Space` FK**（`mcp_tools/models.py:276-282`）而 `technical_plan_service.py:488` 把它当 `\"project_id\"` 键回给调用方 ⇒ **必须过 `_aresolve_project`，⛔ 绝不透传 `space_id`**（P-8）；feature list 的 `feature_meta.project_id` 已是 Project id，直接用但**仍要校验它是 UUID 且 `Project` 存在**"
    - "⭐ **推不出 ⇒ 抛一个具名领域异常（如 `BlueprintIntakeRejected`）并在 `start_blueprint_orchestration` 之前发生**：⛔ 不建 session、⛔ 不建 artifact。四个入口各自的错误出口由 116-03 映射（workflow `NodeResult(status=\"failed\", next_handle=\"error\")` / chat `ToolResult(success=False, error=...)` / MCP `error_response(code, detail)`）"
    - "⭐ **`aseed_blueprint_artifact(...)` 的三件产出缺一不可**（§A.1 表）：① `Artifact(artifact_type=ARTIFACT_TYPE_TECHNICAL_PLAN)` 经 `ArtifactService.create` 建成（`title` 取需求原文首行截断）；② `blueprint_status` 经 `BlueprintLifecycleService.transition` 跳到 `researching`（入口边只有 `\"\" → researching`，形状逐字照 `_abp_mark_drafting`，⛔ 不裸写字段）；③ 返回 `artifact` 供 handler 把 `artifact.current_version_id` 放进 `StageOutcome`"
    - "⭐ **`_h_bp_intake` 返回的 `StageOutcome` 必须显式带 `current_artifact_version=artifact.current_version_id`**：`engine.py:108-119` 有整段注释说明「只在非 None 时才透传」（无条件透传会把每次不产版本的转移都把指针抹成 NULL）⇒ 不传则 `session.current_artifact_version_id` 恒 None，`blueprint_spec_gate._aload_current_version:516-523` 取不到即判 `needs_clarification` + `blueprint_spec_gate_no_artifact_version` warning（`:138-143`），会话**卡死在 spec_gate**；`_amap_blueprint_status` 与两个 `_aload_artifact` helper 同时静默降级"
    - "**`_h_bp_intake` 幂等**：会话已有 `current_artifact_version_id`（重入 / 重放）⇒ 不重复建 artifact，直接 `StageOutcome(event=\"intaken\")` 原样带回既有指针"
    - "⭐ **`_h_bp_decompose` 落实功能点拆分，两条路径**：(a) `stage_state.decomposition.feature_segments` 非空 ⇒ **直接映射成 `requirement_spec.feature_points`，⛔ 不走 LLM**（feature list 入口；映射 `{title, module, layer} → {id, title, intent, description?}`，`id` 确定性生成且**同一 segments 重跑得同一组 id**，⛔ 不用随机 uuid —— `feature_points` 有重复 id 校验（§A.2 变体 G）且重跑会刷版本）；(b) 否则走 LLM，**复用已注册的 `CallSource.BLUEPRINT_DECOMPOSE`**（`use_call_source(...)` 范式见 `blueprint_intent_classify.py:170`），LLM 不可得 ⇒ **fail-soft 保留空 `feature_points` 并记 warning，⛔ 不落 FAILED**（规格门本就会因信息不足而开澄清，那是正确的下一步）"
    - "拆分结果经 `ArtifactService.add_version` 落新版本（此时 artifact 已存在），并把新版本 id 放进 `StageOutcome.current_artifact_version`；⭐ **无变化时 `add_version` 在 `content_hash` 相等时返回 current 不翻版本**（`artifact_service.py:148-149`）⇒ 路径 (a) 重跑不产生版本噪声，用例断言之"
    - "⭐ **`entrypoint.start_blueprint_orchestration(...)` 是纯追加的第二个函数**：签名逐字对齐 `start_orchestration`（含 `mode` / `feature_segments` / `feature_meta` 三个 feature list 专用参数与 `entry_key`），⭐ **「非空才写键」纪律逐字沿用**（`:68-75` 的五个 `if`）—— 不提供时会话形态与既有入口逐字一致；`create_session` 第一实参 `\"technical_blueprint\"`；⛔ **不落 `technical_plan_entry_used` 事件**（那是旧链退役观察）；`__all__ += [\"start_blueprint_orchestration\"]` 另起一行"
    - "⭐ **`start_blueprint_orchestration` 在建会话之前先解析 `meta.project_id` 并写进 `stage_state.decomposition.project_id`**：intake handler 从那里取（handler 拿不到入口上下文）。解析失败 ⇒ 抛 `BlueprintIntakeRejected`，此时**会话尚未建立**"
    - "⭐ **`blueprint_intake.py` 与模块创建同一个 commit 追加进 `test_blueprint_log_redaction_guard._SCANNED_MODULES`**（`_violations:69-70` 直接 `read_text()` **不兜 `FileNotFoundError`** ⇒ 先占位会让参数化用例立刻红且报的是 `FileNotFoundError` 不是断言失败）。⛔ 不加进 `test_blueprint_inv6_guard._ALLOWED_WRITER`"
    - "⭐ **intake 用例的三条硬断言**（RESEARCH 给 PLAN 第 5 条）：① 落库版本的 `content[\"schema_version\"] == \"blueprint/v1\"`；② `session.current_artifact_version_id` 非空；③ `meta.project_id` 能被 `ProjectMember` 查中（⛔ 不是「非空」而是「查得中」）。⭐ **MCP 入口单列一条变异**：把 `_aresolve_project` 换成透传 `McpWorkItemContext.space_id` ⇒ 用例转红"
    - "全量后端门相对基线 **8609 passed / 1 failed** 无新增失败；`makemigrations --check --dry-run` 退出码 0 且零新 migration 文件"
  artifacts:
    - path: "server/services/process_runtime/blueprint_intake.py"
      provides: "MINIMAL_BLUEPRINT_SKELETON 常量 + aresolve_project_id（四链唯一收口，MCP 过 _aresolve_project）+ BlueprintIntakeRejected + aseed_blueprint_artifact（create + transition + 返回 artifact）+ adecompose_feature_points（feature_segments 直采 / LLM 走 BLUEPRINT_DECOMPOSE / fail-soft）"
      contains: "blueprint/v1"
    - path: "server/services/process_runtime/entrypoint.py"
      provides: "start_blueprint_orchestration（纯追加第二个函数，process_type=technical_blueprint、非空才写键、建会话前先解析 project_id）+ __all__ += 纯追加"
      contains: "start_blueprint_orchestration"
    - path: "server/services/process_runtime/builtin_processes.py"
      provides: "_h_bp_intake 落实（幂等 + 显式 current_artifact_version）与 _h_bp_decompose 落实（两条路径 + 版本落库），docstring 的「116 入口切换时接线」占位话术同步改掉"
      contains: "current_artifact_version"
    - path: "server/tests/services/process_runtime/test_blueprint_intake.py"
      provides: "骨架过校验 + 三条硬断言 + MCP space_id 变异 + 推不出 project_id 时零副作用 + intake 幂等 + decompose 两条路径与「无变化不翻版本」+ INV-6 源码扫描"
      contains: "schema_version"
    - path: "server/tests/delivery/test_blueprint_log_redaction_guard.py"
      provides: "_SCANNED_MODULES 追加 services/process_runtime/blueprint_intake.py（与模块创建同 commit），删除行 0"
      contains: "blueprint_intake"
  key_links:
    - from: "server/services/process_runtime/blueprint_intake.py"
      to: "server/delivery/services/artifact_service.py"
      via: "ArtifactService.create(ARTIFACT_TYPE_TECHNICAL_PLAN, skeleton, ...) 建 Artifact + v1 + 置 current_version（⛔ 不是 add_version）"
      pattern: "ArtifactService"
    - from: "server/services/process_runtime/blueprint_intake.py"
      to: "server/delivery/services/blueprint_lifecycle_service.py"
      via: "transition(artifact, RESEARCHING, ...) —— INV-6 唯一写口，⛔ 不裸写 blueprint_status 字段"
      pattern: "BlueprintLifecycleService"
    - from: "server/services/process_runtime/blueprint_intake.py"
      to: "server/workflows/nodes/integrations/board_split_review.py"
      via: "_aresolve_project(space) —— Space → Project 的唯一换算（P-8：MCP 的 context.space 是 Space FK 不是 Project id）"
      pattern: "_aresolve_project"
    - from: "server/services/process_runtime/builtin_processes.py"
      to: "server/services/process_runtime/blueprint_intake.py"
      via: "_h_bp_intake / _h_bp_decompose 内 lazy import 调用（handler 只返 StageOutcome，落库在 service，形状照 _abp_mark_drafting）"
      pattern: "blueprint_intake"
    - from: "server/services/process_runtime/entrypoint.py"
      to: "server/delivery/services/convergence_session_service.py"
      via: "create_session(\"technical_blueprint\", entrypoint, stage_state={\"decomposition\": ...}, ...)"
      pattern: "technical_blueprint"
---

<objective>
补上**全仓至今完全缺失的那一块**：没有任何代码会创建蓝图的初始 `Artifact` 与 `blueprint/v1` 骨架版本。

实测起点（RESEARCH §0.1 逐条坐实）：`_h_bp_intake` / `_h_bp_decompose` 是**零副作用 pass-through**（`builtin_processes.py:320-330`，后者 docstring 逐字写着「功能点拆分在 116 入口切换时接线」）；`blueprint_spec_gate.run` 要求 `session.current_artifact_version_id` 已存在、取不到就判 `needs_clarification` + warning；全仓 `ArtifactService.create` 的调用者只有 `architect_merge_adapter.py:252`（旧链）与 `builtin_processes.py:285`（echo 测试链）—— **蓝图链零调用**。⇒ 蓝图链至今只在测试里被预置产物驱动过，SC-1 的主体工作量在这里，不在「改一行 `process_type`」。

三处「会静默假通过」的地方，本 plan 逐条钉死：

① ⭐ **漏写 `schema_version` ⇒ 三条链同时静默降级**（P-2）。`validate_blueprint` 对缺该键的 content 返回 **`(True, None)`**（pass-through 保 v0 零迁移，已 `.venv` 实跑八变体证实），而 `builtin_types._validate_technical_plan:20` 的判别式是 `if content.get("schema_version")` ⇒ 漏写会让校验器走 v0、SC-3 的 renderer 走 v0 空壳、SC-4 的入图门控**永不触发**。三条全部无异常。⇒ 用例断言**落库版本的 `content["schema_version"]`**，不能只断言校验通过。

② ⭐ **`StageOutcome` 不带 `current_artifact_version` ⇒ 会话卡死在 spec_gate**。`engine.py:108-119` 只在非 None 时透传（那段注释解释了为什么不能无条件透传）；不传 ⇒ 指针恒 None ⇒ 规格门恒判需澄清、状态映射器静默降级。

③ ⭐ **MCP 的 `context.space` 是 `projects.Space` FK 不是 Project id**（P-8）。`technical_plan_service.py:488` 把它当 `"project_id"` 键回给调用方，直接透传会落一份 `meta.project_id` 为 Space id 的蓝图 —— 它的**全部 20 个端点恒 400、图谱恒不入、导出恒不可用**，且三条都「安静地什么都没发生」，没有任何补救入口。⇒ 必须过 `board_split_review._aresolve_project`，且「推不出就拒绝发起」而不是「落一份注定不可用的蓝图」。

Purpose: 116-03 的四个入口、116-04 的入图门控、116-05 的渲染判别、116-06 的 MCP 协议，全部依赖这里落出一份**形状正确**的 `blueprint/v1` 骨架。
Output: 1 个新建源文件 + 2 处既有源文件纯追加/落实 + 1 处守卫清单追加 + 1 个新建测试文件。**零 migration、零新依赖、零新 `CallSource` 枚举、零新 stage 名、前端零改动。**
</objective>

<execution_context>
工作目录：worktree 根 `/Users/zaneliu/Projects/open-source/friday-ai/.claude/worktrees/v0.20-blueprint`，分支 `milestone/v0.20.0-blueprint`。⛔ 绝不读写主检出的 `server/` 或 `web/`。

**wave 2，依赖 116-01**：本 plan 与 116-01 共同改 `entrypoint.py` 与 `builtin_processes.py`（**文件重叠即隐式依赖**），且 intake 落出的会话要能被 116-01 的 `build_engine_for_session` 正确分派、被两处 `isinstance` 自检放行。开工前先读 `116-01-SUMMARY.md` 的「`build_engine_for_session` 逐字签名与返回契约」。

**测试基线**：`cd server && uv run pytest tests/ -q` = **8609 passed / 1 failed**（唯一失败 `tests/mcp_tools/test_skills_snapshot_guard.py::test_skill_files_discovered` 是 worktree 环境产物，⛔ 不得试图修）。收口断言是「无**新增**失败」。

**受限面配额（硬约束）**：

| 文件 | 允许的改动 | 删除行上界 | formatter |
|---|---|---|---|
| `server/services/process_runtime/entrypoint.py` | 追加 `start_blueprint_orchestration` 整个函数 + `__all__ +=` 一行 | **0** | `ruff check` + `ruff format --check` |
| `server/services/process_runtime/builtin_processes.py` | 两个 handler 的函数体落实（含 docstring 改写） | **4**（两个 `return StageOutcome(event=...)` 与两处占位 docstring 行） | `ruff check` |
| `server/tests/delivery/test_blueprint_log_redaction_guard.py` | `_SCANNED_MODULES` 追加 **1** 行 | **0** | 只 `ruff check` |

⚠️ 签名与字段名一律按下列**实测**取，不得猜测：

- `ArtifactService.create(artifact_type, content, *, title="", work_item=None, produced_by_session_id="", produced_by_ref="", created_by_user_id="") -> Artifact`（`artifact_service.py:53-84`）：内部先 `validate_content(artifact_type, content)`，**不过校验直接抛 `ArtifactContentInvalid`**；`_create_sync` 在 `:105-115` 建 v1 并置 `artifact.current_version`。
- `ArtifactService.add_version(...)`（`:117`）：`_add_version_sync` 在 `content_hash` 相等时 **`return current` 不翻版本**（`:145-150`）。
- `StageOutcome`（`engine.py`，dataclass）四字段：`event` / `stage_state_update: dict | None` / `current_artifact_version: Any = None` / `error: dict | None`；engine 的「只在非 None 时透传」注释在 `:108-119`。
- `start_orchestration(entrypoint, requirement_text, *, work_item=None, created_by=None, include_repos=None, conversation_id=None, node_execution_id=None, initiated_by_user_id="", extra_evidence=None, mode="", feature_segments=None, feature_meta=None)`（`entrypoint.py:32-45`）+ `entry_key`（116-01 追加）；「非空才写键」五个 `if` 在 `:64-75`；`create_session` 调用在 `:77-86`。
- `technical_blueprint` 注册（`builtin_processes.py:984-991`）：`artifact_type="technical_plan"`、`initial_stage="intake"`；stage graph 在 `:850-960`；`intake` 的出边 event 是 `"intaken"`、`decompose` 是 `"decomposed"`（`:320-330` 现状返回值）。
- `_abp_mark_drafting`（`builtin_processes.py:466-490`）—— **状态跳转的逐字 analog**（经 `BlueprintLifecycleService.transition`，best-effort 包裹形态照抄）。
- `_abp_load_artifact`（`:453-463`）—— handler 侧取 artifact 的既有 helper。
- `blueprint_schema.py:123-135` 顶层 11 个必填键；`validate_blueprint(content) -> tuple[bool, str | None]`；`iter_blocks` 的 `section_path` 约定（111-01 决策：点分 + `[标识]`）。
- `_aresolve_project(space)`（`workflows/nodes/integrations/board_split_review.py:58-69`）；既有用法 `plan_research._send_clarify_card:470`。
- `McpWorkItemContext.space`（`mcp_tools/models.py:276-282`，`projects.Space` FK）；`technical_plan_service.py:488` 把它当 `"project_id"` 键回传（**这就是陷阱本身**）。
- `CallSource.BLUEPRINT_DECOMPOSE = "blueprint_decompose"`（`agents/call_source.py:112`）；`use_call_source(...)` 范式见 `blueprint_intent_classify.py:170`；枚举清单锁 `tests/test_model_usage_call_source.py:77`。
- `blueprint_spec_gate._aload_current_version`（`:516-523`）/ `_goal_text(content)`（`:632`）/ 无版本 warning `blueprint_spec_gate_no_artifact_version`（`:138-143`）。
- `test_blueprint_log_redaction_guard._SCANNED_MODULES`（`:27-40`，现有 **12** 项）+ `_violations:69-70` 的**无兜底 `read_text()`**。
- `test_blueprint_inv6_guard.py:42-65` 五条正则的确切形态 —— 写代码前手工验算自己的每一行。
</execution_context>

<context>
@.planning/phases/116-entry/116-RESEARCH.md（§A.1 intake 必须产出的三样东西与 `StageOutcome` 契约细节、⭐ §A.2 最小骨架的 `.venv` 实跑八变体表、§A.7 四条推导链与 MCP 陷阱、P-2 / P-8 / P-10 / P-13、给 PLAN 的第 5 条）
@.planning/phases/116-entry/116-PATTERNS.md（⭐ A1 全节：模块形态选型（为什么不是 adapter 类、为什么不直接写进 handler）、`ArtifactService.create` 的 v1 装配代码片、五条「必须 DIFFER」、「避免」三条；A3 的 `start_blueprint_orchestration` 逐字沿用与必须 DIFFER；§0.3 守卫顺序约束表；No Analog #1）
@.planning/phases/116-entry/116-CONTEXT.md（「蓝图 intake 是本 SC 的主体」一节、specifics 第 2 条）
@server/services/process_runtime/blueprint_reflow.py（**模块形态 analog**：模块级 async 函数 + `REFLOW_KINDS` 常量 fail-closed + `ArtifactService` 落版本 + `__all__` + `_COMPONENT`）
@server/services/process_runtime/blueprint_schema.py（`:38` `BLUEPRINT_SCHEMA_VERSION`、`:96-107` 九种 `source_type`、`:123-135` 11 个必填顶层键、`:142-166` `meta` 段、`iter_blocks` 与 `validate_blueprint` 的签名）
@server/delivery/services/artifact_service.py（`:40-115` `_content_hash` / `create` / `_create_sync` 全文、`:117-160` `add_version` 与「hash 相等返 current」）
@server/services/process_runtime/builtin_processes.py（`:305-330` 三条 handler 纪律注释与两个空 handler、`:453-490` `_abp_load_artifact` / `_abp_mark_drafting`、`:850-960` stage graph、`:984-991` 蓝图注册项）
@server/services/process_runtime/entrypoint.py（`:32-86` `start_orchestration` 全文含「非空才写键」、`:188-189` `__all__ +=` 的纯追加先例）
@server/services/process_runtime/engine.py（`StageOutcome` dataclass 与 `:108-119` 「只在非 None 时透传 `current_artifact_version`」的整段注释）
@server/workflows/nodes/integrations/board_split_review.py（`:50-69` `_resolve_space` 与 `_aresolve_project` 全文）
@server/services/process_runtime/blueprint_intent_classify.py（`:150-190` `use_call_source(...)` 的调用范式与 LLM 不可得时的 fail-soft 形状）
@server/tests/delivery/test_blueprint_log_redaction_guard.py（`:14` 「新增蓝图模块请一并加进」、`:27-40` `_SCANNED_MODULES`、`:69-70` 无兜底 `read_text`）
@server/tests/delivery/test_blueprint_inv6_guard.py（`:42-65` 五条正则）
@.planning/phases/116-entry/116-01-SUMMARY.md（`build_engine_for_session` 契约 + 两处 `isinstance` 自检的判据形态 —— intake 落出的会话要能被它们放行）
</context>

<tasks>

<task type="auto">
  <name>Task 1: blueprint_intake.py —— 最小骨架常量 + 四链 project_id 收口 + artifact seed + 脱敏守卫清单追加</name>
  <files>server/services/process_runtime/blueprint_intake.py, server/tests/delivery/test_blueprint_log_redaction_guard.py</files>
  <read_first>server/services/process_runtime/blueprint_reflow.py **全文**（模块形态 analog：docstring 首段的 INV-6 声明、`__all__` 显式列公开函数、`_COMPONENT` 常量、`REFLOW_KINDS` 那类模块级 fail-closed 常量、模块级 async 函数如何经 `ArtifactService` 落版本、日志五件套的写法）；server/services/process_runtime/blueprint_schema.py **L30-60 与 L110-200**（`BLUEPRINT_SCHEMA_VERSION` 常量的**确切字面量与所在行**、`validate_blueprint` 签名、⭐ **L123-135 顶层 11 个必填键的确切键名与顺序**、L142-166 `meta` 段的 `title`（非空约束）/ `project_id`（非空约束）/ `summary`（**是 block_list 不是字符串**）/ `revision_round`、`requirement_spec` 段的 `goal`（block_list）与 `feature_points[]` 的必填 `id/title/intent` 与**重复 id 校验**）；server/delivery/services/artifact_service.py **L40-160**（`_content_hash` 的 canonical JSON、`create` 全文含 `validate_content` 前置与 `ArtifactContentInvalid`、`_create_sync` L95-115 的 v1 装配与 `artifact.current_version = v1`、`add_version` 与 L145-150 的「hash 相等 return current」）；server/services/process_runtime/builtin_processes.py **L440-500**（`_abp_load_artifact:453-463` 与 ⭐ **`_abp_mark_drafting:466-490` 全文** —— 状态跳转经 `BlueprintLifecycleService.transition` 的逐字形状与其 best-effort 包裹）；server/delivery/services/blueprint_lifecycle_service.py **L1-120 与 transition 定义段**（`BlueprintStatus` 的 11 态、状态机入口边 `"" → researching` 的**确切合法边定义**、`transition(...)` 的**逐字签名与全部 keyword**）；server/workflows/nodes/integrations/board_split_review.py **L40-80**（`_resolve_space` 与 `_aresolve_project` 全文：优先 `feishu_project_key` 命中、否则首个、取不到返什么）；server/mcp_tools/models.py **L265-290**（`McpWorkItemContext` 的 `space` FK 与相邻字段 —— 确认它确是 `projects.Space`）；server/mcp_tools/technical_plan_service.py **L480-495**（⭐ `:488` 把 `space_id` 当 `"project_id"` 键回传的那一行 —— **陷阱的证据，只读**）；server/tests/delivery/test_blueprint_log_redaction_guard.py **L14-44 与 L69-80**（docstring 的追加要求、`_SCANNED_MODULES` 元组体例、⭐ `_violations` 的**无兜底 `read_text()`** 与 `@pytest.mark.parametrize`）</read_first>
  <action>
**① 新建 `server/services/process_runtime/blueprint_intake.py`。**

模块 docstring 四段：(a) 用途——「蓝图 intake：建初始 `Artifact` + 落一份过得了 `validate_blueprint` 的 `blueprint/v1` 骨架版本 + 把 `blueprint_status` 跳到 `researching`；全仓此前**没有任何代码**做这件事（`ArtifactService.create` 的调用者只有旧链 merge 与 echo 测试链）」；(b) ⭐ **INV-6 声明**——「状态跳转唯一经 `BlueprintLifecycleService.transition`，本模块零裸写 `blueprint_status`；⛔ 本模块**不**进 `_ALLOWED_WRITER`」；(c) ⭐ **三条「漏了会静默」**——逐字写明 P-2（`schema_version` 缺失 ⇒ 校验/渲染/入图三条链同时降级）、`StageOutcome.current_artifact_version` 必传（否则卡死 spec_gate）、P-8（MCP 的 `context.space` 是 Space FK，透传即 20 个端点恒 400）；(d) 观测——五件套 + ⛔ 需求原文只记长度。

`_COMPONENT = "blueprint_intake"`；`__all__` 显式列公开符号。

**常量与异常：**

- `MINIMAL_BLUEPRINT_SKELETON` —— ⭐ **不要写成模块级可变 dict 被直接 `deepcopy` 之外的方式使用**；提供 `def build_skeleton(*, title: str, project_id: str, goal_text: str) -> dict` 工厂，内部 `copy.deepcopy` 常量后填三个位（`meta.title` / `meta.project_id` / `requirement_spec.goal` 的 paragraph block）。11 个顶层键按 `blueprint_schema.py:123-135` 逐字：`schema_version`（**字面量取 `BLUEPRINT_SCHEMA_VERSION` 懒 import，⛔ 不复制 `"blueprint/v1"` 字符串**，MN-10 纪律）、`meta` / `requirement_spec` / `repo_associations` / `current_state_analysis` / `implementation_overview` / `api_contracts` / `impact_analysis` / `interaction_flows` / `must_haves` / `citations`。六段全部空数组/空对象（§A.2 变体 A 实跑为 `(True, None)`）。
- ⭐ `requirement_spec.goal` 装**一个** paragraph block：`{"block_id": "bp_goal_1", "type": "paragraph", "text": <需求原文，截断到 schema 允许的上界>}`。⚠️ `block_id` 命名与 111 的 `iter_blocks` 约定对齐（§A.2 变体 B 实跑产出 `[('requirement_spec.goal', 'bp_goal_1')]`，**沿用这个 id**）。
- `class BlueprintIntakeRejected(Exception)` —— 带 `reason` 与**中性 detail**（`detail` 供四个入口回显，⛔ 不含内部路径/异常原文）。放本模块或 `agents/core/exceptions.py`，**执行期二选一但只能有一处定义**；优先本模块（与 `blueprint_*` 家族同域）。

**公开函数：**

1. `async def aresolve_project_id(*, entry: str, space: Any = None, feature_meta: dict | None = None, conversation: Any = None, work_item_context: Any = None) -> str`
   —— ⭐ **四条推导链的唯一收口**（116-03 直接 import 复用）：
   - `feature_meta.get("project_id")` 非空 ⇒ 校验 `_is_uuid` + `Project.objects.filter(id=...).aexists()`，通过即返回。
   - 否则有 `space` ⇒ `await _aresolve_project(space)`（懒 import `board_split_review`）⇒ 取 `project.id`。
   - `work_item_context` 有 `space_id` ⇒ ⭐ **先取 `Space` 再过 `_aresolve_project`**，⛔ **绝不把 `space_id` 当 project id 返回**（P-8；这一行必须带注释指向 `technical_plan_service.py:488`）。
   - `conversation` ⇒ 按既有 chat 侧的 space 上下文换算（执行期按实读的 `Conversation` 字段取，⛔ 不猜字段名）。
   - 全都推不出 ⇒ `raise BlueprintIntakeRejected(reason="project_unresolved", detail=<中性文案>)`。
   - 收尾一条 `caller` 事件 `blueprint_intake_project_resolved`（`entry` / `project_id` / `source`，`duration_ms`）；失败记 `blueprint_intake_project_unresolved`。

2. `async def aseed_blueprint_artifact(*, session, requirement_text: str, project_id: str, title: str = "", created_by_user_id: str = "") -> Any`
   —— ① `content = build_skeleton(title=..., project_id=project_id, goal_text=requirement_text)`；② `artifact = await ArtifactService().create(ARTIFACT_TYPE_TECHNICAL_PLAN, content, title=<需求原文首行截断>, produced_by_session_id=str(session.id), produced_by_ref="blueprint_intake", created_by_user_id=created_by_user_id)`；③ 经 `BlueprintLifecycleService().transition(artifact, BlueprintStatus.RESEARCHING, ...)` 跳状态（形状逐字照 `_abp_mark_drafting`）；④ 返回 `artifact`。
   ⚠️ `ArtifactService.create` **内部先跑 `validate_content`，不过就抛 `ArtifactContentInvalid`** ⇒ 骨架形状错会**响亮失败**而不是静默落一份坏 content（这正是我们要的），⛔ 不要在外面包 try 吞掉它。
   埋点 `blueprint_intake_seeded`（`artifact_id` / `project_id` / `version_no` / `goal_len` / `duration_ms`，⛔ **需求原文只记长度**）。

3. `async def adecompose_feature_points(...)` —— 由 Task 2 落地（本 Task 只留函数签名与 `NotImplementedError`? ⛔ **不留半成品**）。⇒ ⭐ **本函数整体归 Task 2**，Task 1 不声明它。

**② `test_blueprint_log_redaction_guard._SCANNED_MODULES` 追加 1 行** `"services/process_runtime/blueprint_intake.py"`（删除行 0）。⭐ **必须与 `blueprint_intake.py` 的创建在同一个 commit**（`_violations:69-70` 直接 `read_text()` 不兜 `FileNotFoundError`，先占位会立刻 red 且报的是 `FileNotFoundError`）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/delivery/test_blueprint_log_redaction_guard.py tests/delivery/test_blueprint_inv6_guard.py -q && uv run python -c "
import os, django
os.environ.setdefault('DJANGO_SETTINGS_MODULE','friday.settings'); django.setup()
from services.process_runtime.blueprint_intake import build_skeleton
from services.process_runtime.blueprint_schema import validate_blueprint, BLUEPRINT_SCHEMA_VERSION
c = build_skeleton(title='t', project_id='11111111-1111-1111-1111-111111111111', goal_text='需求原文')
assert c['schema_version'] == BLUEPRINT_SCHEMA_VERSION, c.get('schema_version')
ok, err = validate_blueprint(c)
assert ok, err
print('skeleton valid OK')"</automated>
  </verify>
  <acceptance_criteria>
- 上述命令输出 `skeleton valid OK` 且两条守卫全绿。
- ⭐ **骨架带 `schema_version` 且不是复制的字面量**：`rg -n '"blueprint/v1"' server/services/process_runtime/blueprint_intake.py` **零命中**；`rg -n "BLUEPRINT_SCHEMA_VERSION" server/services/process_runtime/blueprint_intake.py` 命中（MN-10 懒 import 纪律）。
- ⭐ **11 个必填顶层键齐全**：`cd server && uv run python -c "
import os, django
os.environ.setdefault('DJANGO_SETTINGS_MODULE','friday.settings'); django.setup()
from services.process_runtime.blueprint_intake import build_skeleton
c = build_skeleton(title='t', project_id='p', goal_text='g')
need = {'schema_version','meta','requirement_spec','repo_associations','current_state_analysis','implementation_overview','api_contracts','impact_analysis','interaction_flows','must_haves','citations'}
assert need <= set(c), need - set(c)
assert c['requirement_spec']['goal'], 'goal 必须装一个承载需求原文的 block'
assert 'g' in str(c['requirement_spec']['goal']), 'goal block 必须真的带上需求原文'
print('11 keys + goal block OK')"` 输出 `11 keys + goal block OK`。
- ⭐ **骨架工厂不返回共享可变对象**：连续调两次 `build_skeleton`，改第一次的 `c1['repo_associations'].append(1)` 后第二次的 `repo_associations` 仍为空（写成一条 pytest 用例，⛔ 不接受只靠 review）。
- ⭐ **INV-6 零裸写**：`rg -n "blueprint_status\s*=\s*[^=]" server/services/process_runtime/blueprint_intake.py` **零命中**；`rg -n "['\"]blueprint_status['\"]\s*:" server/services/process_runtime/blueprint_intake.py` **零命中**；`rg -n "BlueprintLifecycleService" server/services/process_runtime/blueprint_intake.py` 命中；`rg -n "_ALLOWED_WRITER" server/tests/delivery/test_blueprint_inv6_guard.py | rg -c "blueprint_intake"` == **0**。
- ⭐ **MCP 陷阱有代码级防线**：`cd server && uv run python -c "
src = open('services/process_runtime/blueprint_intake.py').read()
i = src.index('def aresolve_project_id')
body = src[i: src.index('\nasync def ', i+1) if '\nasync def ' in src[i+1:] else len(src)]
assert '_aresolve_project' in body, 'MCP/workflow 链必须过 _aresolve_project'
assert 'space_id' in body, '必须显式处理 work_item_context.space_id'
print('P-8 defense OK')"` 输出 `P-8 defense OK`；且该函数体内 `return` 的值**不得**直接来自 `space_id`（人工核对并写进 SUMMARY）。
- ⭐ **推不出就拒绝**：`rg -n "BlueprintIntakeRejected" server/services/process_runtime/blueprint_intake.py` 命中 ≥2（定义 + raise）。
- ⭐ **create 不是 add_version**：`rg -n "ArtifactService\(\)\.create|\.create\(" server/services/process_runtime/blueprint_intake.py` 命中；`rg -n "add_version" server/services/process_runtime/blueprint_intake.py` 在**本 Task 结束时零命中**（`add_version` 属 Task 2 的 decompose 落版本）。
- ⭐ **正文只记长度**：`rg -n "goal_len|requirement_len|body_len" server/services/process_runtime/blueprint_intake.py` 命中；`cd server && uv run python -c "
import ast
tree = ast.parse(open('services/process_runtime/blueprint_intake.py').read())
bad = []
for n in ast.walk(tree):
    if isinstance(n, ast.Call) and isinstance(n.func, ast.Attribute) and n.func.attr in ('info','warning','error','debug','exception'):
        for kw in n.keywords or []:
            if kw.arg in ('requirement_text','goal_text','question','body','quote'):
                bad.append((kw.arg, ast.unparse(kw.value)))
assert not bad, bad
print('no raw text in logs')"` 输出 `no raw text in logs`。
- `git diff server/tests/delivery/test_blueprint_log_redaction_guard.py | rg "^-[^-]"` **为空**；`rg -c "blueprint_intake" server/tests/delivery/test_blueprint_log_redaction_guard.py` == **1**。
- `cd server && uv run ruff check services/process_runtime/blueprint_intake.py tests/delivery/test_blueprint_log_redaction_guard.py` 通过；`uv run ruff format --check services/process_runtime/blueprint_intake.py` 通过。
  </acceptance_criteria>
  <done>`blueprint_intake.py` 就位：骨架工厂产出的 11 键 content 实跑过 `validate_blueprint` 且 `schema_version` 取自懒 import 的常量、`requirement_spec.goal` 真的带上需求原文、返回值不是共享可变对象；`aresolve_project_id` 是四链唯一收口且 MCP 分支必过 `_aresolve_project`、推不出即抛 `BlueprintIntakeRejected`；状态跳转经 lifecycle service、INV-6 三条正则零命中且未进豁免名单；模块与脱敏守卫清单同 commit 落地。</done>
</task>

<task type="auto">
  <name>Task 2: start_blueprint_orchestration（纯追加）+ _h_bp_intake / _h_bp_decompose 落实（含功能点拆分两条路径）</name>
  <files>server/services/process_runtime/entrypoint.py, server/services/process_runtime/builtin_processes.py, server/services/process_runtime/blueprint_intake.py</files>
  <read_first>server/services/process_runtime/entrypoint.py **L32-90 与 L185-195**（`start_orchestration` 全文的**逐字签名顺序**、docstring 五段体例、⭐ `:64-75` 的「非空才写键」五个 `if`、`:77-86` 的 `create_session` 调用逐字、`__all__ +=` 纯追加先例；以及 116-01 新加的 `entry_key` 形参与 `technical_plan_entry_used` 事件所在位置 —— `start_blueprint_orchestration` ⛔ **不落该事件**）；server/services/process_runtime/builtin_processes.py **L300-340 与 L440-500 与 L840-1000**（两个空 handler 全文与三条纪律注释、`_abp_load_artifact` / `_abp_mark_drafting`、stage graph 里 `intake` / `decompose` 两个 `StageDef` 的 `transitions` 键名、蓝图注册项 `:984-991` 的 `initial_stage="intake"`）；server/services/process_runtime/engine.py（`StageOutcome` 四字段 + ⭐ **`:108-119` 「只在非 None 时透传 `current_artifact_version`」的整段注释与其代码**）；server/services/process_runtime/blueprint_intent_classify.py **L140-200**（`use_call_source(...)` 的调用范式全文、LLM 客户端如何取、**不可得时的 fail-soft 形状**与 warning 事件名）；server/services/process_runtime/blueprint_spec_gate.py **L120-160 与 L505-530 与 L620-645**（无版本时的 `needs_clarification` + `blueprint_spec_gate_no_artifact_version` warning `:135-149`、`_aload_current_version:516-523`、⭐ `_goal_text(content):632` 与四维打分读的三个输入 —— 证明空 `goal` 会 fail-closed 到满歧义）；server/services/process_runtime/blueprint_schema.py **L167-260**（`requirement_spec.feature_points[]` 的必填 `id/title/intent` 与可选 `description`（block_list）/ `acceptance_criteria[str]`；**重复 id 校验**的报错文案 §A.2 变体 G）；server/delivery/services/artifact_service.py **L117-160**（`add_version` 签名与「hash 相等 return current」）</read_first>
  <action>
**① `entrypoint.py` 追加 `start_blueprint_orchestration`（删除行 0）。**

```
async def start_blueprint_orchestration(
    entrypoint: str,
    requirement_text: str,
    *,
    <与 start_orchestration 逐字相同的全部 keyword>,
    project_id: str = "",
) -> ConvergenceSession:
```

- docstring 三段：(a) 与 `start_orchestration` 的关系——「**纯追加的第二个函数**，⛔ 不给 `start_orchestration` 加 `process_type` 形参（那会让旧链四入口共享一个可传错的开关）」；(b) ⭐ **`project_id` 必填语义**——「调用方须传已解析好的 `initiatives.Project.id`；为空时本函数经 `blueprint_intake.aresolve_project_id` 兜底解析，**仍解析不出即抛 `BlueprintIntakeRejected` 且此刻会话尚未建立**（⛔ 不落一份注定不可用的蓝图）」；(c) 「非空才写键」纪律与 feature list 三参数的透传。
- 实现：解析/校验 `project_id` → `decomposition` dict 逐字照 `start_orchestration:64-75`（**同样的五个 `if`**）+ **额外写一个键** `decomposition["project_id"] = project_id`（intake handler 从这里取；⛔ handler 拿不到入口上下文）→ `create_session("technical_blueprint", entrypoint, ...)` 其余实参逐字同源。
- ⛔ **不落 `technical_plan_entry_used`**（那是旧链退役观察）。可另落一条 `blueprint_orchestration_started`（`category="caller"` / `component="process_runtime"` / `entry_key` / `project_id` / `session_id` / `initiated_by_user_id`）。
- `__all__ += ["start_blueprint_orchestration"]` 另起一行（⛔ 既有 `__all__` 行一字不动）。

**② `builtin_processes._h_bp_intake` 落实（删除行 ≤2）。**

```
async def _h_bp_intake(session, engine) -> StageOutcome:
```
- **幂等前置**：`existing = str(getattr(session, "current_artifact_version_id", "") or "")`；非空 ⇒ `return StageOutcome(event="intaken", current_artifact_version=existing)`（重入/重放不重复建 artifact；⭐ **仍要带回指针**，否则 engine 会把它抹成 NULL）。
- 取 `decomposition = (session.stage_state or {}).get("decomposition") or {}`；`project_id = str(decomposition.get("project_id") or "")`、`requirement_text = str(decomposition.get("requirement_text") or "")`。
- `project_id` 为空 ⇒ ⚠️ **这在正常链路上不可能发生**（`start_blueprint_orchestration` 已挡）。此处**不建 artifact**，落一条 caller 事件 `blueprint_intake_missing_project` 并返回 `StageOutcome(event="intaken")`（⛔ 不带指针）—— 随后 spec_gate 会因无版本而判需澄清，那是正确的可见失败；⛔ **不要在 handler 里抛异常**（engine 通用 except 会把会话落 FAILED，抹掉可诊断信息）。
- 正路：lazy import `blueprint_intake.aseed_blueprint_artifact` → `artifact = await aseed_blueprint_artifact(session=session, requirement_text=..., project_id=..., created_by_user_id=...)` → ⭐ **`return StageOutcome(event="intaken", current_artifact_version=artifact.current_version_id, stage_state_update={"intake": {"artifact_id": str(artifact.id)}})`**。
  ⚠️ `stage_state_update` **只写自己的桶**（STATE 114-03 纪律：任何新增 stage 的 stage_state 写入都只写自己那个键，engine 顶层浅合并）。
- docstring 改写：删掉「本 stage 零副作用」，写清三件产出与「`current_artifact_version` 必须显式带回（`engine.py:108-119`）」的理由。

**③ `builtin_processes._h_bp_decompose` 落实（删除行 ≤2）+ `blueprint_intake.adecompose_feature_points`。**

在 `blueprint_intake.py` 新增：

```
async def adecompose_feature_points(*, session, artifact, requirement_text: str, feature_segments: list[dict] | None = None) -> Any
```
返回**新版本对象或 None**（None = 未产版本）。两条路径：

- **(a) `feature_segments` 非空 ⇒ 直接映射，⛔ 不走 LLM**（feature list 入口）：`{title, module, layer} → {"id": <确定性 id>, "title": ..., "intent": <按 layer/module 的确定性映射，取不到落 schema 允许的缺省值>}`；可选 `description` 装一个 paragraph block 承载 `module`/`layer` 原文。⭐ **`id` 必须确定性生成且同一 segments 重跑得同一组 id**（例如 `f"fp_{index+1}"` 或对 `title` 做稳定摘要）—— ⛔ 不用随机 uuid：`feature_points` 有重复 id 校验（§A.2 变体 G），且随机 id 会让每次重跑都翻一个新版本。
- **(b) 否则走 LLM**：`use_call_source(CallSource.BLUEPRINT_DECOMPOSE)` 范式照 `blueprint_intent_classify.py:170`；产出逐条过形状校验（缺 `id`/`title`/`intent` 的条目丢弃并计数），**id 去重**。⭐ **LLM 不可得 / 解析失败 ⇒ fail-soft：保留空 `feature_points`、记 warning `blueprint_decompose_unavailable`、⛔ 不抛、⛔ 不落 FAILED**（规格门本就会因信息不足而开澄清，那是正确的下一步）。
- 两条路径统一收尾：读当前 content（`order_by("-version_no").afirst()` 口径，STATE 114-04 纪律：⛔ 绝不读 `session.current_artifact_version`）→ `deepcopy` 后写 `requirement_spec.feature_points` → `ArtifactService().add_version(...)` → ⭐ **比对返回的 version 是否真的是新版本**（`version.id != 调用前的 current_version_id` 或 `version_no` 增大）；`feature_points` 与原值逐字相同 ⇒ `content_hash` 相等 ⇒ `add_version` 返回 current 不翻版本，此时返回 None。
- 埋点 `blueprint_decompose_completed`（`artifact_id` / `source="feature_segments"|"llm"` / `point_count` / `dropped_count` / `version_no` / `duration_ms`；⛔ 功能点标题正文不进日志，只记数量）。

`_h_bp_decompose` handler：取 `feature_segments` → `artifact = await _abp_load_artifact(session)` → 无 artifact ⇒ 原样 `StageOutcome(event="decomposed")` → 调 `adecompose_feature_points` → 有新版本 ⇒ `StageOutcome(event="decomposed", current_artifact_version=version.id, stage_state_update={"decompose": {...计数...}})`；无新版本 ⇒ ⭐ **仍带回既有指针** `current_artifact_version=session.current_artifact_version_id`（⛔ 不传 None —— 那会被 engine 抹成 NULL）。docstring 的「功能点拆分在 116 入口切换时接线」占位话术**同步改掉**。
  </action>
  <verify>
    <automated>cd server && uv run python -c "
import os, django
os.environ.setdefault('DJANGO_SETTINGS_MODULE','friday.settings'); django.setup()
from services.process_runtime.entrypoint import start_blueprint_orchestration, start_orchestration, __all__ as A
import inspect
sb = set(inspect.signature(start_blueprint_orchestration).parameters)
so = set(inspect.signature(start_orchestration).parameters)
missing = so - sb
assert not missing, ('签名未对齐，缺：', missing)
assert 'start_blueprint_orchestration' in A
print('start_blueprint_orchestration signature OK')" && uv run pytest tests/services/process_runtime/test_blueprint_process_graph.py tests/services/process_runtime/test_blueprint_spec_gate.py -q</automated>
  </verify>
  <acceptance_criteria>
- 上述命令输出 `start_blueprint_orchestration signature OK` 且两个既有 stage 用例全绿（⭐ 证明落实两个 handler **零回归**）。
- ⭐ **`create_session` 第一实参是 `technical_blueprint`**：`cd server && uv run python -c "
import inspect, os, django
os.environ.setdefault('DJANGO_SETTINGS_MODULE','friday.settings'); django.setup()
from services.process_runtime.entrypoint import start_blueprint_orchestration as f
src = inspect.getsource(f)
assert 'technical_blueprint' in src, '未传 technical_blueprint'
assert 'technical_plan_entry_used' not in src, '⛔ 新链不得落旧链退役观察事件'
print('blueprint session factory OK')"` 输出 `blueprint session factory OK`。
- ⭐ **「非空才写键」纪律沿用**：`cd server && uv run python -c "
import inspect, os, django
os.environ.setdefault('DJANGO_SETTINGS_MODULE','friday.settings'); django.setup()
from services.process_runtime.entrypoint import start_orchestration as a, start_blueprint_orchestration as b
sa, sb = inspect.getsource(a), inspect.getsource(b)
for key in ('extra_evidence', 'mode', 'feature_segments', 'feature_meta'):
    assert ('if %s' % key) in sb, ('缺非空才写键：', key)
print('conditional keys OK')"` 输出 `conditional keys OK`。
- ⭐ **两个 handler 都显式带回 `current_artifact_version`**：`cd server && uv run python -c "
src = open('services/process_runtime/builtin_processes.py').read()
for name in ('_h_bp_intake', '_h_bp_decompose'):
    i = src.index('async def %s' % name); nxt = src.index('\nasync def ', i+1)
    body = src[i:nxt]
    assert 'current_artifact_version' in body, name
print('stage outcome pointer OK')"` 输出 `stage outcome pointer OK`。
- ⭐ **占位话术已改掉**：`rg -n "116 入口切换时接线|本 stage 零副作用" server/services/process_runtime/builtin_processes.py` **零命中**。
- ⭐ **handler 不抛**：`cd server && uv run python -c "
src = open('services/process_runtime/builtin_processes.py').read()
for name in ('_h_bp_intake', '_h_bp_decompose'):
    i = src.index('async def %s' % name); nxt = src.index('\nasync def ', i+1)
    body = src[i:nxt]
    assert 'raise ' not in body, (name, '⛔ handler 不得抛异常（engine 通用 except 会落 FAILED 抹掉诊断信息）')
print('handlers never raise OK')"` 输出 `handlers never raise OK`。
- ⭐ **复用既有 call_source，⛔ 零新增枚举**：`rg -n "BLUEPRINT_DECOMPOSE" server/services/process_runtime/blueprint_intake.py` 命中；`git diff server/agents/call_source.py` **为空**；`cd server && uv run pytest tests/test_model_usage_call_source.py -q` 全绿。
- ⭐ **不读 `session.current_artifact_version` 取基线**：`rg -n "session.current_artifact_version" server/services/process_runtime/blueprint_intake.py` **零命中**（基线一律 `order_by("-version_no").afirst()`，STATE 114-04 纪律）。
- ⭐ **零新 stage 名**：`git diff server/services/process_runtime/builtin_processes.py | rg "^\+.*StageDef\("` **为空**；`git diff --name-only | rg "^web/"` **为空**（若真加了 stage 名，必须同时补前端别名表——本 plan 的默认答案是不加）。
- **删除行核算**：`git diff server/services/process_runtime/entrypoint.py | rg -c "^-[^-]"` == **0**；`git diff server/services/process_runtime/builtin_processes.py | rg -c "^-[^-]"` ≤ **4**。
- `cd server && uv run ruff check services/process_runtime/entrypoint.py services/process_runtime/builtin_processes.py services/process_runtime/blueprint_intake.py` 通过；`uv run ruff format --check services/process_runtime/entrypoint.py services/process_runtime/blueprint_intake.py` 通过。
  </acceptance_criteria>
  <done>`start_blueprint_orchestration` 是签名对齐的纯追加第二个函数、建会话前先把 `project_id` 解析并写进 `decomposition`、⛔ 不落旧链退役事件；`_h_bp_intake` 幂等且**永远显式带回** `current_artifact_version`、`project_id` 缺失时不建 artifact 也不抛；`_h_bp_decompose` 两条路径落地（feature_segments 直采确定性 id / LLM 复用既有 call_source 且不可得时 fail-soft），无变化时不翻版本；两处占位话术改掉、零新 stage 名、零新枚举。</done>
</task>

<task type="auto">
  <name>Task 3: test_blueprint_intake.py（三条硬断言 + MCP 变异 + 拒绝发起零副作用 + decompose 两路径）+ 全量后端门</name>
  <files>server/tests/services/process_runtime/test_blueprint_intake.py</files>
  <read_first>server/tests/services/process_runtime/test_blueprint_spec_gate.py **L1-120**（本 Task 的形状 analog：`pytestmark` 与 `transaction=True` 的理由、如何构造一条蓝图 `ConvergenceSession` 与 `Artifact` + `ArtifactVersion`、如何 patch adapter/LLM、fixture 的组织）；server/tests/delivery/test_blueprint_review_views.py **L83-100**（⭐ 范围闸工厂 `_make_project(project_id, member=...)` —— 「`meta.project_id` 能被 `ProjectMember` 查中」这条断言逐字复用它造数据）；server/tests/services/process_runtime/test_blueprint_process_graph.py（stage 推进的既有写法：如何让 engine 走一步、如何断言出边）；server/tests/test_model_usage_call_source.py **L70-85**（`call_source` 清单锁的形态 —— 确认本 plan 未新增枚举）</read_first>
  <action>
新建 `server/tests/services/process_runtime/test_blueprint_intake.py`，`pytestmark = pytest.mark.django_db(transaction=True)`，docstring「守十件事」编号清单（每条把可证伪断言写进条目本身）：

1. ⭐ **骨架过校验且带 `schema_version`（P-2 的正面）**：`build_skeleton(...)` ⇒ `validate_blueprint` 返 `(True, None)` **且** `content["schema_version"] == BLUEPRINT_SCHEMA_VERSION`。⭐ **反面并列**：把 `schema_version` 删掉后 `validate_blueprint` **仍返 `(True, None)`**（实跑变体 H）——这一条并列存在，才说明第一条不是恒真、才解释了「为什么必须直接断言那个键」。
2. **骨架工厂不返回共享可变对象**：连调两次、改第一份的嵌套容器，第二份不受影响。
3. ⭐ **三条硬断言（走真实 `start_blueprint_orchestration` + 驱一步 intake）**：① 落库版本的 `content["schema_version"] == "blueprint/v1"`（**读 DB 重读的 `ArtifactVersion.content`**，⛔ 不读内存对象）；② `session.current_artifact_version_id` 非空（DB 重读）；③ ⭐ `meta.project_id` **能被 `ProjectMember` 查中**（用 `_make_project` 工厂造成员关系后断言 `ProjectMember.objects.filter(project_id=content["meta"]["project_id"], user=...).exists()`）。
4. ⭐ **MCP 入口变异（P-8）**：构造 `McpWorkItemContext`（`space` 指向一个 Space，其下有一个 Project）⇒ `aresolve_project_id(entry="mcp", work_item_context=ctx)` 返回的是 **Project.id 而不是 Space.id**（`assert result != str(space.id)` **且** `Project.objects.filter(id=result).exists()`）。**变异验证**：把实现改成透传 `space_id` ⇒ 本用例转红（实跑一次并把红/绿输出记进 SUMMARY）。
5. ⭐ **推不出 project_id ⇒ 零副作用**：不提供任何可推导上下文 ⇒ `start_blueprint_orchestration` 抛 `BlueprintIntakeRejected`，且 `ConvergenceSession.objects.count()` 与 `Artifact.objects.count()` **与调用前逐字相等**（⛔ 不接受「落一份 project_id 为空的蓝图」）。
6. **`meta.project_id` 是 Space id 时的下游后果有对照**（把 P-8 的代价钉成事实）：手工造一份 `meta.project_id = <space_id>` 的蓝图 ⇒ 调一个既有蓝图端点（如 `blueprint-document`）⇒ **400**。这条证明「拒绝发起远优于落一份注定不可用的蓝图」不是修辞。
7. **intake 幂等**：同一会话连驱两次 intake ⇒ `Artifact.objects.count()` 只 +1，且第二次的 `StageOutcome.current_artifact_version` 仍非 None。
8. ⭐ **`StageOutcome` 不带指针的反向对照**：临时构造一个返回 `StageOutcome(event="intaken")`（不带 `current_artifact_version`）的替身 handler ⇒ 断言随后 `spec_gate` 判 `needs_clarification`（证明这条纪律的必要性，⛔ 不只是注释）。
9. ⭐ **decompose 路径 (a)**：`feature_segments` 三条 ⇒ 新版本的 `requirement_spec.feature_points` 三条、id **两两不同**、**⛔ 零 LLM 调用**（patch 掉 LLM 客户端并断言未被调）；**重跑一次 ⇒ `ArtifactVersion` 计数不变**（确定性 id + `content_hash` 相等 ⇒ 不翻版本）。
10. **decompose 路径 (b) 的 fail-soft**：无 `feature_segments` 且 LLM 抛异常 ⇒ **不抛、不落 FAILED**，`feature_points` 保持空，有一条 `blueprint_decompose_unavailable` warning。

**全量后端门**（本 Task 收尾执行并登记进 SUMMARY）：`cd server && uv run pytest tests/ -q` 与基线 **8609 passed / 1 failed** 逐条比对；`uv run python manage.py makemigrations --check --dry-run` 退出码 0 且 `git status --porcelain server/*/migrations/` 为空。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/process_runtime/test_blueprint_intake.py -x -q && uv run pytest tests/services/ tests/delivery/ -q</automated>
  </verify>
  <acceptance_criteria>
- `cd server && uv run pytest tests/services/process_runtime/test_blueprint_intake.py -q` 全绿；`rg -c "def test_" server/tests/services/process_runtime/test_blueprint_intake.py` ≥ **12**。
- ⭐ **P-2 的正反并列都在**：`rg -n "schema_version" server/tests/services/process_runtime/test_blueprint_intake.py` 命中 ≥3；`cd server && uv run python -c "
src = open('tests/services/process_runtime/test_blueprint_intake.py').read()
assert 'validate_blueprint' in src, '缺校验器断言'
assert src.count('(True, None)') >= 1 or 'is True' in src, '缺「删掉 schema_version 仍返 True」的反面并列'
print('P-2 both sides OK')"` 输出 `P-2 both sides OK`。
- ⭐ **三条硬断言各有一条用例**：`rg -n "ProjectMember" server/tests/services/process_runtime/test_blueprint_intake.py` 命中（⛔ 不接受只断言 `project_id` 非空）；`rg -n "current_artifact_version_id" server/tests/services/process_runtime/test_blueprint_intake.py` 命中。
- ⭐ **MCP 变异用例实跑过**：SUMMARY 里写明「把 `aresolve_project_id` 的 MCP 分支改成透传 `space_id` ⇒ 用例 `test_*mcp*` 转红（贴错误首行）；恢复即绿」。⛔ 只声明不实跑不算。
- ⭐ **拒绝发起零副作用有前后计数对比**：`rg -n "objects.count()" server/tests/services/process_runtime/test_blueprint_intake.py` 命中 ≥3。
- ⭐ **decompose 路径 (a) 断言零 LLM**：`rg -n "assert_not_called|call_count == 0" server/tests/services/process_runtime/test_blueprint_intake.py` 命中。
- ⭐ **重跑不翻版本**：`rg -n "ArtifactVersion.objects.count()" server/tests/services/process_runtime/test_blueprint_intake.py` 命中 ≥2。
- ⭐ **全量后端门**：`cd server && uv run pytest tests/ -q` 相对基线（8609 passed / 1 failed，唯一失败为 `test_skills_snapshot_guard::test_skill_files_discovered`）**无新增失败** —— 逐条差异写进 SUMMARY。
- `cd server && uv run python manage.py makemigrations --check --dry-run` 退出码 **0**；`git status --porcelain server/delivery/migrations/ server/system/migrations/ server/knowledge/migrations/` 输出**为空**。
- 相位边界自检：`git diff --name-only` 只含本 plan 声明的五个文件；⛔ **不含**六个冻结文件、`repo_router_v2.py`、四个入口文件（`plan_research.py` / `plan_research_tools.py` / `orchestration_delegate.py` / `feature_solution_service.py`）、`web/`。
- `cd server && uv run ruff check tests/services/process_runtime/test_blueprint_intake.py` 通过；`uv run ruff format --check tests/services/process_runtime/test_blueprint_intake.py` 通过。
  </acceptance_criteria>
  <done>三条硬断言（`schema_version` 落库值 / 会话指针非空 / `project_id` 能被 `ProjectMember` 查中）各有绿色用例；P-2 与 P-8 各有一条并列的反面用例说明「为什么这么断言」；「推不出就拒绝发起」有前后计数对比、`meta.project_id` 落成 Space id 的下游代价有 400 对照；decompose 两条路径（零 LLM 直采 + fail-soft）与「重跑不翻版本」全部绿；全量后端门相对基线无新增失败、零 migration。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 入口权威上下文 → `meta.project_id` | `meta.project_id` 是**全链范围闸的唯一来源**（114-MJ-03）、SC-4 图谱边与 space 归属的唯一来源、导出可用性的唯一来源；写错即三条防线同时失效且**不报错** |
| 需求原文（半可信） → 骨架 content + 日志 | 用户/上游输入落进 `requirement_spec.goal` block 并被后续 LLM 消费 |
| LLM 产出的 feature_points → schema | 半可信结构化输出进 content，缺字段/重复 id 会让后续每次落版本都校验失败 |
| `blueprint_status` 字段 → 状态机 | 裸写即绕过 CAS 与合法边校验（INV-6） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-116-10 | Elevation of Privilege | `meta.project_id` 落成 `projects.Space.id`（MCP 透传）⇒ 该蓝图的项目范围闸永远查不中 ⇒ 既非「拒绝」也非「放行」，而是 20 个端点恒 400 且**无补救入口** | mitigate | `aresolve_project_id` 是四链唯一收口且 MCP 分支必过 `_aresolve_project`；Task 3 第 4 条变异用例（透传 `space_id` ⇒ 转红）+ 第 6 条下游 400 对照 |
| T-116-11 | Tampering | 骨架漏写 `schema_version` ⇒ 校验器走 v0、renderer 走 v0 空壳、SC-4 门控永不触发，三条链同时静默降级 | mitigate | 常量取自懒 import 的 `BLUEPRINT_SCHEMA_VERSION`（⛔ 不复制字面量）；Task 3 第 1 条**正反并列**（含「删掉该键 `validate_blueprint` 仍返 True」的反面） |
| T-116-12 | Denial of Service | `StageOutcome` 不带 `current_artifact_version` ⇒ 指针恒 None ⇒ 规格门恒判需澄清、会话卡死在 spec_gate（零异常） | mitigate | 两个 handler 源码扫描断言 + Task 3 第 8 条反向对照（替身 handler 不带指针 ⇒ spec_gate 判需澄清） |
| T-116-13 | Tampering | 裸写 `artifact.blueprint_status` 绕过 CAS 与合法边（并可用 `**{...}` 展开规避人眼） | mitigate | 一律经 `BlueprintLifecycleService.transition`；INV-6 三条正则扫全 `server/`；⛔ 不进 `_ALLOWED_WRITER` |
| T-116-14 | Tampering | 随机 uuid 作 `feature_point.id` ⇒ 每次重跑都翻一个新版本，把版本历史刷成噪声（114-04 已为时间戳立过同款纪律） | mitigate | 确定性 id + Task 3 第 9 条「重跑 `ArtifactVersion` 计数不变」 |
| T-116-15 | Information Disclosure | 需求原文 / 功能点标题进日志 | mitigate | 只记 `goal_len` / `point_count`；Task 1 的 AST 断言（日志 kwarg 里不得出现 `requirement_text` / `goal_text` 等原文实参）；模块进 `_SCANNED_MODULES` |
| T-116-16 | Denial of Service | handler 内抛异常 ⇒ engine 通用 except 把会话落 FAILED，抹掉可诊断信息、用户看到「方案编排失败」 | mitigate | 源码扫描断言两个 handler 内 `raise ` 零命中；拒绝发起在 `start_blueprint_orchestration`（会话尚未建立）而非 handler |
| T-116-17 | Denial of Service | LLM 不可得时 decompose 落 FAILED ⇒ 一次上游抖动废掉整条蓝图 | mitigate | fail-soft 保留空 `feature_points` + warning；Task 3 第 10 条用例 |
| T-116-SC | Tampering | 依赖供应链 | accept | 零新增外部依赖、零 migration、零新 `CallSource` 枚举（清单锁 `test_model_usage_call_source.py` 保持绿）、零新 stage 名、零新事件常量进 `BLUEPRINT_EVENTS` |
</threat_model>

<verification>
- `cd server && uv run pytest tests/services/process_runtime/ -q` 全绿（⭐ 含既有 `test_blueprint_process_graph.py` / `test_blueprint_spec_gate.py` —— 两个 handler 由 pass-through 变为有副作用，这两个文件是零回归的第一道证据）。
- `cd server && uv run pytest tests/delivery/ -q` 全绿（含 INV-6 三条正则、脱敏守卫 13 项扫描面、112/114/115 全部端点）。
- ⭐ `cd server && uv run pytest tests/ -q` 相对基线（8609 passed / 1 failed）**无新增失败**。
- `cd server && uv run python manage.py makemigrations --check --dry-run` 退出码 0；相位内**零** migration 文件。
- ⭐ **MCP 变异实跑一次**（透传 `space_id` ⇒ 转红 → 恢复 → 转绿），红/绿输出首行贴进 SUMMARY。
- 冻结面核算：六个 `technical_plan` 冻结文件与 `repo_router_v2.py` 的 `git diff` 均为空；四个入口文件的 `git diff` 均为空（接线归 116-03）；`web/` 零命中。
- 删除行核算：`entrypoint.py` = 0、`test_blueprint_log_redaction_guard.py` = 0、`builtin_processes.py` ≤ 4（逐行登记）。
- `cd server && uv run ruff check services/process_runtime/ tests/services/process_runtime/test_blueprint_intake.py` 通过。
</verification>

<success_criteria>
- **蓝图链第一次有了生产起点**：调 `start_blueprint_orchestration` 就能得到一条带真实 `Artifact` + `blueprint/v1` v1 骨架 + `researching` 状态 + 有效 `current_artifact_version` 指针的会话——此前全仓没有任何代码能做到这件事，蓝图链只在测试里被预置产物驱动过。
- **规格门第一次能真的跑起来**：`goal` 装上了需求原文，四维打分有输入，`blueprint_spec_gate_no_artifact_version` 那条 warning 在正常链路上不再出现。
- **三条会静默假通过的地方各有可证伪防线**：`schema_version` 有「正反并列」用例（含「删掉它校验器仍返 True」的反面）；`StageOutcome` 指针有替身 handler 的反向对照；MCP 的 Space/Project 混淆有变异用例 + 下游 400 对照。
- **「推不出就拒绝发起」是可核算的事实**：抛异常时 `ConvergenceSession` 与 `Artifact` 计数与调用前逐字相等——⛔ 绝不落一份「20 个端点恒 400、图谱恒不入、导出恒不可用」且无补救入口的蓝图。
- **feature list 入口的功能点直采零 LLM 且重跑不翻版本**；LLM 路径不可得时 fail-soft 而不是废掉整条蓝图。
- 全量后端门相对基线无新增失败、零 migration、零新枚举、零新 stage 名、六个冻结文件与四个入口文件逐字未动。
</success_criteria>

<output>
完成后创建 `.planning/phases/116-entry/116-02-SUMMARY.md`，provides 至少包含：
- ⭐ **`build_skeleton` / `aresolve_project_id` / `aseed_blueprint_artifact` / `adecompose_feature_points` 的逐字签名与返回契约**（116-03 的四个入口直接 import 复用 `aresolve_project_id`，⛔ 不各写一份）；
- ⭐ **最小骨架的 11 键 JSON 全文**（含 `requirement_spec.goal` 的 block 形状与 `block_id` 取值）与「它过 `validate_blueprint`」的实跑输出；
- ⭐ **`aresolve_project_id` 的四条推导链对照表**（入口 → 权威上下文 → 换算 → 失败行为），特别标注 MCP 那条**必须过 `_aresolve_project`** 及其变异实跑记录；
- `BlueprintIntakeRejected` 的定义位置、`reason` 取值集合与**中性 detail 文案**（116-03 的四个入口各自映射成 `NodeResult` / `ToolResult` / `error_response`）；
- `start_blueprint_orchestration` 与 `start_orchestration` 的**签名差异清单**（多一个 `project_id`、`create_session` 第一实参不同、⛔ 不落退役事件）；
- 两个 handler 落实后的 `StageOutcome` 契约（三种分支各带什么 `current_artifact_version` 与 `stage_state_update` 桶名）；
- `feature_segments → feature_points` 的**映射表与确定性 id 规则**（116-03 的 feature list 入口据它传参）；
- decompose 的 `call_source` 复用登记（⛔ 零新增枚举，清单锁保持绿）；
- 受限面删除行逐行登记；全量 `pytest tests/ -q` 与基线（8609/1）的差异比对；
- ⭐ **给 116-03 的开工提示**：四个入口分别该传哪些参数、拿不到 project_id 时各自的错误出口形态。
</output>
