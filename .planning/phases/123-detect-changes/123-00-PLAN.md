---
phase: 123-detect-changes
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - server/tests/services/code_graph/test_detect_changes.py
  - server/tests/services/test_diff_mirror.py
  - server/tests/services/code_graph/test_detect_changes_orchestrator.py
  - server/tests/mcp_tools/test_detect_changes_tools.py
autonomous: true
requirements: [DIFF-01, DIFF-02]

must_haves:
  truths:
    - "后续每个 plan 的 <verify> 命令都能命中可被 pytest 收集到的节点名"
    - "交叠 / rename / formatting / 阈值 / 编排 / MCP 双面的用例名已在 Wave 0 登记"
    - "零 DB 交叠测试文件顶部不带 django_db（对齐 impact.py 内核测纪律）"
  artifacts:
    - path: "server/tests/services/code_graph/test_detect_changes.py"
      provides: "DIFF-01/02 纯内核用例桩（overlap / rename / formatting / threshold / exclusion）"
      contains: "test_ranges_overlap"
    - path: "server/tests/services/test_diff_mirror.py"
      provides: "diff_mirror / ensure_mirror_sha / rename argv 用例桩"
      contains: "test_diff_mirror_uses_find_renames"
    - path: "server/tests/services/code_graph/test_detect_changes_orchestrator.py"
      provides: "run_detect_changes 编排用例桩（base pin / batch impact / hard reject / staleness）"
      contains: "test_diff_base_pinned_to_last_indexed"
    - path: "server/tests/mcp_tools/test_detect_changes_tools.py"
      provides: "MCP + 双面哨兵用例桩"
      contains: "test_two_surfaces_same_payload_detect_changes"
  key_links:
    - from: "123-VALIDATION.md Wave 0 Gaps"
      to: "四个新测试文件"
      via: "用例名与 RESEARCH Validation Architecture 表一一对应"
      pattern: "test_.*detect_changes|test_diff_mirror"
---

<objective>
建立 Phase 123 全部自动化验收的地基：四个测试文件骨架 + RESEARCH/VALIDATION 表里点名的全部 pytest 节点（skip 桩），保证后续 plan 的 `<verify>` 不是空头支票。

Purpose: Nyquist 采样要求每个 task 的 automated verify 命中真实节点；Wave 0 不落地则 W1–W5 全部验收失锚（per D-01..D-16 验收面）。
Output: 4 个测试文件；用例以 `@pytest.mark.skip(reason="Wave 0 桩：由 123-NN 落地")` 标注（自检 collect 用例除外）。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/123-detect-changes/123-CONTEXT.md
@.planning/phases/123-detect-changes/123-RESEARCH.md
@.planning/phases/123-detect-changes/123-VALIDATION.md
@.planning/phases/123-detect-changes/123-PATTERNS.md
@server/tests/services/code_graph/test_impact.py
@server/tests/mcp_tools/test_impact_trace_tools.py
</context>

## Artifacts this phase produces

> Phase-level inventory（本 plan 只建测试桩；实现符号由后续 plan 落地）

| Symbol / artifact | Module | Introduced in |
|-------------------|--------|---------------|
| `ranges_overlap` / `parse_unified_diff` / `detect_affected_symbols` / `is_formatting_only` | `services.code_graph.detect_changes` | 123-01 |
| `DiffMirrorResult` / `diff_mirror` / `ensure_mirror_sha` | `services.repo_mirror` | 123-01 |
| `run_detect_changes` | `services.code_graph_tools` | 123-02 |
| `tool_trace_payload` detect_changes 分支 | `services.code_graph_tools` | 123-02 |
| `DetectChangesRequestSerializer` / `TOOL_SCHEMA_SNAPSHOT["detect_changes"]` | `mcp_tools.serializers` | 123-03 |
| `DetectChangesView` | `mcp_tools.views` | 123-03 |
| `tools/detect_changes/` url | `mcp_tools.urls` | 123-03 |
| `DetectChangesToolInput` / `detect_changes` `@tool` | `agents.tools` | 123-04 |
| Dual-surface sentinel + D-27 drift 7→8 记账 | tests + ROADMAP | 123-05 |

<tasks>

<task type="auto">
  <name>Task 1: 纯内核 + diff_mirror 测试骨架</name>
  <files>server/tests/services/code_graph/test_detect_changes.py, server/tests/services/test_diff_mirror.py</files>
  <read_first>
    - `.planning/phases/123-detect-changes/123-RESEARCH.md` §Validation Architecture → Phase Requirements → Test Map（权威用例名与 Behavior）
    - `.planning/phases/123-detect-changes/123-VALIDATION.md` §Wave 0 Requirements
    - `server/tests/services/code_graph/test_impact.py`（零 DB 内核测形态：无 `django_db`、模块 docstring 写明纪律）
    - `server/tests/services/code_graph/test_model.py`（AST/纯值断言风格）
  </read_first>
  <action>
新建两个测试文件。每个用例体暂为 `pytest.fail("Wave 0 桩")` 并挂 `@pytest.mark.skip(reason="Wave 0 桩：由 123-01 落地")`（本 task 归属 123-01 的节点）。⛔ 不得引入 `pytest.mark.django_db` 到 `test_detect_changes.py`。

`tests/services/code_graph/test_detect_changes.py`（模块 docstring：零 ORM / 不得 django_db；覆盖 D-05/D-06/D-07/D-08/D-15）：
- `test_ranges_overlap`
- `test_symbols_hit_by_old_hunk`
- `test_pure_insert_hunk_no_fake_uid`（纯新增 → 文件级 added 摘要，不伪造 uid）
- `test_rename_single_entry_not_delete_add`（D-06 / DIFF-02）
- `test_formatting_only_not_impact_seed`（D-07）
- `test_deleted_file_symbols_are_seeds`（D-08）
- `test_threshold_file_summary_shape`（>100 → truncated/not_expanded 字段形状，纯函数侧）
- `test_affected_symbol_min_fields`（D-15 六字段 + file:line + changeType 枚举）
- `test_exclusion_paths_absent`（交叠输入已过滤后输出不含排除路径；纯列表断言）

`tests/services/test_diff_mirror.py`（可挂 django_db 若需 settings；优先临时 bare repo + 直接调 helper）：
- `test_diff_mirror_uses_find_renames`（argv 含 `--find-renames` 与 `--unified=0`）
- `test_diff_mirror_two_dot_not_three_dot`（⛔ 不得出现 `...` 三-dot）
- `test_diff_mirror_rename_detected`（纯 rename commit → unified 含 rename 头）
- `test_ensure_mirror_sha_pins_object`（40 位 sha 可 fetch；不走 `refs/heads/{sha}`）
- `test_diff_mirror_output_byte_cap`（超 `DETECT_CHANGES_MAX_DIFF_BYTES` / 16MiB 上限 → MirrorError，挡 T-123-DOS）

另加一条**不 skip** 的自检：`test_wave0_detect_changes_kernel_nodes_collectable`（或仅靠 collect-only verify）——可选；以 collect-only 为准即可。
  </action>
  <acceptance_criteria>
    - `cd server && uv run pytest tests/services/code_graph/test_detect_changes.py tests/services/test_diff_mirror.py --collect-only -q` 至少收集到上方列出的全部节点名
    - `test_detect_changes.py` 源码中不出现字面量 `django_db`（除注释外：`grep -v '^#' | grep -c django_db` 为 0，或 AST 断言无 mark）
    - 每个列出的用例带 `Wave 0 桩` skip reason
  </acceptance_criteria>
  <verify>
    <automated>cd server && GALAXY_CACHE_WARM_ON_STARTUP=False GRAPH_BUILD_ORPHAN_RECONCILE_ON_STARTUP=False uv run pytest tests/services/code_graph/test_detect_changes.py tests/services/test_diff_mirror.py --collect-only -q</automated>
  </verify>
  <done>内核与 diff_mirror 全部 Wave 0 节点可被 pytest 收集</done>
</task>

<task type="auto">
  <name>Task 2: 编排 + MCP/双面测试骨架</name>
  <files>server/tests/services/code_graph/test_detect_changes_orchestrator.py, server/tests/mcp_tools/test_detect_changes_tools.py</files>
  <read_first>
    - `.planning/phases/123-detect-changes/123-RESEARCH.md` §Validation Architecture 表（hard_reject / batch_impact / staleness / base_ref_declarative / two_surfaces）
    - `server/tests/services/code_graph/test_impact_shell.py`（编排壳测形态：django_db + mock）
    - `server/tests/mcp_tools/test_impact_trace_tools.py`（`test_two_surfaces_same_payload` 范式；`_reset_code_graph_state` autouse）
  </read_first>
  <action>
新建：

`tests/services/code_graph/test_detect_changes_orchestrator.py`（`pytestmark = pytest.mark.django_db`；skip 归 123-02）：
- `test_diff_base_pinned_to_last_indexed`（mock mirror：diff argv 左端 == `last_indexed_commit_sha`；D-01）
- `test_base_ref_declarative_only`（传 `base_ref` 不改变 argv 左端；D-02 / DIFF-02）
- `test_hard_reject_unindexed`（空索引 → `ok=False` `repository_not_indexed`，非空清单；D-03）
- `test_hard_reject_mirror_error`（MirrorError → `ok=False` + error_code，非空清单；D-03）
- `test_hard_reject_acl`（mock `ensure_repository_readable` 抛 `GraphAccessDenied` → 上抛或 `ok=False`，且无空成功 `affected`；D-03 / T-123-ACL）
- `test_batch_impact_calls_run_impact_with_symbol_id`（spy：默认 max_depth=3 / min_confidence=1.0 / graph_branch=None；D-09/D-10）
- `test_formatting_only_skipped_from_impact_seeds`（D-07）
- `test_threshold_skips_batch_impact`（>100 → 零次 `run_impact` + not_expanded；D-08）
- `test_impact_fail_soft_per_symbol`（单符号 GraphError → impact_error，整体 ok=True；D-12）
- `test_staleness_behind_still_ok`（behind 大仍 ok=True + as_of；D-04/D-14）
- `test_affected_processes_placeholder_empty`（`affected_processes == []`；D-12）
- `test_compare_equals_base_sha_explicit`（head sha == base → 明确 error_code 或空清单+声明，不静默「可信无改动」；RESEARCH Pitfall 6）

`tests/mcp_tools/test_detect_changes_tools.py`（`pytestmark = django_db`；模块 docstring 写明 ⛔ 不许 mock `run_detect_changes`）：
- `test_mcp_detect_changes_requires_pat`（skip → 123-03；401）
- `test_mcp_detect_changes_success_envelope`（skip → 123-03）
- `test_conversational_detect_changes_registered`（skip → 123-04）
- `test_two_surfaces_same_payload_detect_changes`（skip → 123-05；成功 + 硬错误态）
- `test_tool_trace_payload_detect_changes_counts_only`（skip → 123-05；无路径/符号名）

Wave 0 ⛔ 不改 `TOOL_SCHEMA_SNAPSHOT`、不改生产代码、不碰 `mcp/` submodule、不碰 `repo_router_v2.py`。
  </action>
  <acceptance_criteria>
    - `cd server && uv run pytest tests/services/code_graph/test_detect_changes_orchestrator.py tests/mcp_tools/test_detect_changes_tools.py --collect-only -q` 收集到上方全部节点
    - `test_detect_changes_tools.py` 模块 docstring 含「不得 mock run_detect_changes」或等价英文/中文禁止语
    - 无生产路径（`server/services/` / `server/mcp_tools/` / `server/agents/`）在本 plan 被修改
  </acceptance_criteria>
  <verify>
    <automated>cd server && GALAXY_CACHE_WARM_ON_STARTUP=False GRAPH_BUILD_ORPHAN_RECONCILE_ON_STARTUP=False uv run pytest tests/services/code_graph/test_detect_changes_orchestrator.py tests/mcp_tools/test_detect_changes_tools.py --collect-only -q</automated>
  </verify>
  <done>编排与双面测试节点全部可收集；生产代码零改动</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → future API | Wave 0 只建桩；威胁在后续 plan 落地时由对应用例钉死 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-123-ACL | Information Disclosure | future orchestrator | mitigate | Wave 0 登记 hard_reject / ACL 相关编排测节点，由 123-02 落地 |
| T-123-EXCL | Information Disclosure | detect_changes kernel | mitigate | `test_exclusion_paths_absent` 桩 |
| T-123-CRED | Information Disclosure | repo_mirror | mitigate | diff_mirror 测要求错误路径走 `_scrub`（123-01 验收） |
| T-123-DOS | Denial of Service | diff bytes / batch impact | mitigate | `test_diff_mirror_output_byte_cap` + `test_threshold_skips_batch_impact` 桩 |
| T-123-BASE | Tampering | base_ref spoof | mitigate | `test_base_ref_declarative_only` + `test_diff_base_pinned_to_last_indexed` 桩 |
| T-123-TRACE | Information Disclosure | RetrievalTrace | mitigate | `test_tool_trace_payload_detect_changes_counts_only` 桩 |
| T-123-SC | Tampering | package installs | accept | 本相位零新增 pip/npm 包 |
</threat_model>

<verification>
四个测试文件 `--collect-only` 全绿；后续 plan verify 节点名与本 plan 登记一致。
</verification>

<success_criteria>
- VALIDATION Wave 0 Gaps 四文件均存在
- 所列 DIFF-01/DIFF-02 行为均有对应 skip 桩节点
</success_criteria>

<output>
Create `.planning/phases/123-detect-changes/123-00-SUMMARY.md` when done
</output>
