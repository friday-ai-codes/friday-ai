---
phase: 123-detect-changes
plan: 05
type: execute
wave: 5
depends_on: ["123-04"]
files_modified:
  - server/tests/mcp_tools/test_detect_changes_tools.py
  - server/services/code_graph_tools.py
  - .planning/ROADMAP.md
  - .planning/phases/123-detect-changes/123-VALIDATION.md
autonomous: true
requirements: [DIFF-01, DIFF-02]

must_haves:
  truths:
    - "同一输入下 MCP data（去 run_id）与对话 output.data 逐字节相等——成功态 + 硬错误态（D-13 / IMPACT-06 延续）"
    - "tool_trace_payload(detect_changes) 无符号名/路径/diff 正文（T-123-TRACE）"
    - "ROADMAP 将 mcp npm 漂移记账从 7 更新为 8（新增 detect_changes），且明确不修 mcp/ submodule（D-27）"
    - "相位验证：detect_changes 相关测绿；test_mcp_package_tools_match_server_snapshot 允许继续红"
  artifacts:
    - path: "server/tests/mcp_tools/test_detect_changes_tools.py"
      provides: "test_two_surfaces_same_payload_detect_changes"
      contains: "test_two_surfaces_same_payload_detect_changes"
    - path: ".planning/ROADMAP.md"
      provides: "D-27 漂移 7→8 记账"
  key_links:
    - from: "DetectChangesView"
      to: "detect_changes @tool"
      via: "共享 run_detect_changes；哨兵去 run_id 后 byte-equal"
      pattern: "_assert_surfaces_byte_equal|run_id"
---

<objective>
闭合双面同源、观测留痕无内容泄漏，并完成 D-27 漂移 7→8 台账（不修 `mcp/` submodule）。

Purpose: 没有双面哨兵，壳层分叉会在 Phase 124 编码链放大；D-27 必须写实「扩大了已知失败」而非假装无影响。
Output: 双面哨兵绿 + ROADMAP/VALIDATION 记账 + 相位 Nyquist 标记更新。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/123-detect-changes/123-CONTEXT.md
@.planning/phases/122-impact-trace/122-10-PLAN.md
@.planning/phases/122-impact-trace/122-10-SUMMARY.md
@server/tests/mcp_tools/test_impact_trace_tools.py
@.planning/ROADMAP.md
</context>

## Artifacts this phase produces

| Symbol / doc | Location |
|--------------|----------|
| `test_two_surfaces_same_payload_detect_changes` | `tests/mcp_tools/test_detect_changes_tools.py` |
| D-27 ROADMAP 文案 7→8 | `.planning/ROADMAP.md` |
| VALIDATION `nyquist_compliant` / wave_0_complete | `123-VALIDATION.md` |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 双面同源哨兵（成功 + 硬错误）</name>
  <files>server/tests/mcp_tools/test_detect_changes_tools.py, server/services/code_graph_tools.py</files>
  <read_first>
    - `server/tests/mcp_tools/test_impact_trace_tools.py`：`test_two_surfaces_same_payload`（约 301–359）与 `_assert_surfaces_byte_equal`
    - `server/tests/mcp_tools/test_detect_changes_tools.py` 桩与既有 MCP/对话测
    - `.planning/phases/123-detect-changes/123-RESEARCH.md` Pitfall 5（双面漂移）
  </read_first>
  <behavior>
    - 成功态：构造可产非空/空 files 的同一 payload；MCP 去掉 run_id 后与 tool output["data"] 逐字节相等
    - 硬错误态：repository_not_indexed 或 MirrorError 折信封；两侧 ok=False 信封同形（仍 byte-equal）
    - ⛔ 测试内 mock run_detect_changes
  </behavior>
  <action>
实现 `test_two_surfaces_same_payload_detect_changes`：
- 复用 impact 测的 `_reset_code_graph_state` / PAT / conversation fixture 模式
- mock 仅限 mirror/diff 下层（`diff_mirror` / `ensure_mirror_commit` / `ensure_mirror_sha`）与必要 Symbol 行，使编排可确定性返回
- 断言成功态 + 至少一种硬错误态（未索引或 mirror 失败）
- 若发现壳层键漂移（如对话多包 metadata 进 data），**修壳**而非放宽断言——data 段必须原样
- 清理 123-00 遗留的 `test_conversational_detect_changes_registered` skip：改为薄包装调用 agents 侧已绿断言，或删除并由 docstring 指向 `tests/agents/tools/test_graph_tools.py`（避免双份维护）

确认 `test_tool_trace_payload_detect_changes_counts_only` 仍绿；若 123-02 已绿则本 task 回归即可。必要时微调 `tool_trace_payload` 键集使 MCP `_record` 与对话 `_record_chat_retrieval` 使用同一函数无内容泄漏（T-123-TRACE）。

跑 full 相关套件：detect_changes 四文件 + 既有 `test_impact_trace_tools` 无回归。
  </action>
  <acceptance_criteria>
    - `cd server && uv run pytest tests/mcp_tools/test_detect_changes_tools.py -k two_surfaces -q --reuse-db` 通过
    - 测试源码不含 `mock.patch(...run_detect_changes` 或等价 mock
    - `cd server && uv run pytest tests/mcp_tools/test_impact_trace_tools.py -k two_surfaces -q --reuse-db` 仍通过
  </acceptance_criteria>
  <verify>
    <automated>cd server && GALAXY_CACHE_WARM_ON_STARTUP=False GRAPH_BUILD_ORPHAN_RECONCILE_ON_STARTUP=False uv run pytest tests/mcp_tools/test_detect_changes_tools.py tests/services/code_graph/test_detect_changes.py tests/services/test_diff_mirror.py tests/services/code_graph/test_detect_changes_orchestrator.py tests/mcp_tools/test_impact_trace_tools.py -k 'detect_changes or two_surfaces or diff_mirror or overlap or batch_impact or formatting or threshold or staleness or rename or hard_reject or base_ref or tool_trace' -q --reuse-db</automated>
  </verify>
  <done>双面同源成立；detect_changes 验收套件绿</done>
</task>

<task type="auto">
  <name>Task 2: D-27 漂移 7→8 记账 + VALIDATION 收口</name>
  <files>.planning/ROADMAP.md, .planning/phases/123-detect-changes/123-VALIDATION.md</files>
  <read_first>
    - `.planning/ROADMAP.md` 跨仓记账段（约 215 行：漂移 5→7 文案）
    - `.planning/phases/122-impact-trace/122-10-SUMMARY.md` §D-27 记账复述（写实「扩大已知失败」）
    - `.planning/phases/123-detect-changes/123-VALIDATION.md` frontmatter
  </read_first>
  <action>
1. 更新 ROADMAP 跨仓记账：Phase 123 新增 `detect_changes` MCP 工具后漂移 **7→8**；重申按 D-27 **不修** `mcp/` submodule；`test_mcp_package_tools_match_server_snapshot` 继续列为已知既有失败。写实：本相位使既有失败从 7 扩大到 8——不是「没有影响」。
2. 更新 Phase 123 条目的 Plans 列表若尚未由 planner 写全则对齐实际 6 个 plan 状态（执行结束时勾选归 execute；本 task 至少保证 D-27 段落正确）。
3. `123-VALIDATION.md` frontmatter：`wave_0_complete: true`；在全部自动化节点已绿的前提下设 `nyquist_compliant: true`；Per-Task 表相关行标 ✅（若执行时仍有红测则保持 false 并在 SUMMARY 说明——但本 task 的前置是 Task 1 绿）。
4. ⛔ 不修改 `server/tests/mcp_tools/test_mcp_package_alignment.py` 判据；⛔ 不碰 `mcp/` 目录。
  </action>
  <acceptance_criteria>
    - `rg -n "8|detect_changes" .planning/ROADMAP.md` 显示漂移 7→8 与 detect_changes 记账
    - ROADMAP 明文含「不修」`mcp` submodule 或 D-27 等价表述
    - `123-VALIDATION.md` frontmatter `wave_0_complete: true`
    - `git grep -n "detect_changes" mcp/package` 或 `git status -- mcp` 显示本 task 未改 mcp/（工作区无 mcp 变更）
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/zaneliu/Projects/open-source/friday-ai && rg -n "7→8|7 -> 8|漂移.*8|8 项" .planning/ROADMAP.md && rg -n "wave_0_complete:\\s*true" .planning/phases/123-detect-changes/123-VALIDATION.md && git status --porcelain -- mcp | wc -l | xargs test 0 -eq</automated>
  </verify>
  <done>D-27 台账写实；VALIDATION Wave0 收口；mcp/ 零改动</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP ↔ Chat surfaces | 必须同编排同信封，防止一侧绕过声明 |
| ledger RetrievalTrace | 禁止内容回流 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-123-TRACE | Information Disclosure | tool_trace_payload + RetrievalTrace | mitigate | counts-only 单测 + 双面不复制正文 |
| T-123-ACL | Information Disclosure | dual hard-error path | mitigate | 硬错误态哨兵防止一侧吞成空成功 |
| T-123-BASE | Tampering | dual surfaces | mitigate | 同源编排；base_ref 行为两侧一致 |
| T-123-EXCL | Information Disclosure | dual output | mitigate | 一侧若漏过滤会在 byte-equal 下双红 |
| T-123-DOS | Denial of Service | N/A | accept | 已在 01/02 落地；本 plan 回归 |
| T-123-CRED | Information Disclosure | N/A | accept | 已在 mirror 落地；本 plan 不新增日志字段 |
| T-123-SC | Tampering | mcp submodule drift | accept | D-27 故意不修；记账 7→8 |
</threat_model>

<verification>
detect_changes 全套 + impact/trace 双面回归绿；mcp 包对齐测允许红；ROADMAP D-27 已更新。
</verification>

<success_criteria>
- Phase 123 成功标准 1–3 均有自动化或台账证据
- D-13 双面闭合；D-27 写实
</success_criteria>

<output>
Create `.planning/phases/123-detect-changes/123-05-SUMMARY.md` when done
</output>
