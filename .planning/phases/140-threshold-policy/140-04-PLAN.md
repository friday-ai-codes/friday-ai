---
phase: 140-threshold-policy
plan: 04
type: execute
wave: 3
depends_on:
  - "140-02"
  - "140-03"
files_modified:
  - server/tests/codegraph/test_graph_bench_closure.py
autonomous: true
requirements:
  - BENCH-06
  - BENCH-07
  - EDGE-06
  - OBS-01
  - OBS-02
must_haves:
  truths:
    - "仓内默认测试证明 Phase 133–139 的权限/exclusion、同水位、partial/degradation、触发用户、manifest hash、响应版本和五消费面契约不回归。"
    - "真实 baseline/candidate/policy 齐全时，同条件 compare 只有全部 required gates 通过才 PASS；holdout 仅 final acceptance 显式开启。"
    - "真实已索引目标仓、Qdrant 或 v0.22 baseline 不可用时，结论明确为 human_needed/tech debt，绝不以 seed、mock、collect-only、skip 或 tokens=0 代替。"
    - "server、task 与 npm MCP 的既有测试、typecheck、build、prepack/pack 闸均实际执行且不删测。"
  artifacts:
    - path: server/tests/codegraph/test_graph_bench_closure.py
      provides: "跨 Phase 133–140 的仓内不变量与外部证据状态闭合测试"
    - path: server/codegraph/benchmark_policies/graph_query_threshold_policy.v1.json
      provides: "若真实 baseline 可用，最终锁定并通过审查的 policy"
    - path: compare report artifact
      provides: "若真实环境可用，baseline/candidate/policy 三 hash、逐例/桶 diff 与最终 verdict"
  key_links:
    - from: server/tests/codegraph/test_graph_bench_closure.py
      to: server/contracts/graph-query.v1.json
      via: "canonical manifest hash/response/ranking/version conformance"
      pattern: "manifest_hash|response_version|ranking_version"
    - from: server/codegraph/management/commands/compare_graph_bench.py
      to: server/codegraph/management/commands/evaluate_graph_bench.py
      via: "相同 comparison identity 的 baseline/candidate reports"
      pattern: "case_set_sha256|evaluator_sha256"
---

<objective>
执行 Phase 140 与 v0.24.0 的分层收口：先证明全部仓内契约，再在真实证据可用时运行 paired candidate/holdout gate；不可用时如实落 human_needed/tech debt。

Purpose: 给“v0.24 在相同条件下提升”建立不夸大证据的最终边界，同时确保安全、观测、契约和多消费面不回归。
Output: closure regression、全组件测试证据、真实 compare artifact 或明确 external verification debt。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.claude/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/140-threshold-policy/140-CONTEXT.md
@.planning/phases/140-threshold-policy/140-RESEARCH.md
@.planning/phases/140-threshold-policy/140-VALIDATION.md
@.planning/phases/140-threshold-policy/140-01-SUMMARY.md
@.planning/phases/140-threshold-policy/140-02-SUMMARY.md
@.planning/phases/140-threshold-policy/140-03-SUMMARY.md
@.planning/phases/137-unified-graph-query/137-01-SUMMARY.md
@.planning/phases/138-disambiguation-bounded-impact/138-01-SUMMARY.md
@.planning/phases/139-five-consumer-contract/139-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 建立跨阶段 closure 不变量测试</name>
  <files>server/tests/codegraph/test_graph_bench_closure.py</files>
  <read_first>
    - server/tests/codegraph/test_graph_bench_integration.py
    - server/tests/services/code_graph/test_query_contract_conformance.py
    - server/tests/services/code_graph/test_query_service.py
    - server/tests/services/code_graph/test_access.py
    - server/tests/services/code_graph/test_process_index.py
    - server/tests/services/code_graph/test_process_trace.py
    - server/tests/services/code_graph/test_impact.py
    - server/tests/mcp_tools/test_graph_query_tool.py
    - task/pyproject.toml
    - mcp/package.json
  </read_first>
  <behavior>
    - 权限/exclusion fail-closed、mixed watermark 拒绝、partial/degradation schema-preserving 与 no-observed-impact-not-safe 均保持。
    - process rebuild/index 的 `initiated_by_user_id` 传播与 system fallback 保持。
    - canonical manifest hash、contract_version、response_version、ranking_version 在 service、Chat、Django MCP、npm MCP、task allowed-tools 一致。
    - benchmark/policy/comparator 不含 auto-update/write-back/skip 路径，默认测试不读取 holdout。
    - 缺正式 baseline/policy/candidate 时，closure evidence 状态为 `HUMAN_NEEDED`，而不是 PASS；测试自身仍验证该状态机，不使用 pytest.skip。
  </behavior>
  <action>
新增组合层 `server/tests/codegraph/test_graph_bench_closure.py`，复用现有公开 helpers/contracts，不复制各子系统算法。测试应核对连接点和不变量，而非再造大 fixture：权限/exclusion、同水位、partial/degradation、initiated user、manifest/version/hash、正式 artifact presence/status。对外部环境状态设计显式 `READY | HUMAN_NEEDED | INVALID` 检查函数或命令结果断言；默认仓内 fixture 缺真实 baseline 时必须断言 `HUMAN_NEEDED`，不能 `pytest.skip`、不能把 seed 当正式 evidence、不能伪造阈值数字。不要删除、重命名或放宽既有测试以获得通过。Go 深化、真跨仓 IMPACT-03、LSP 默认翻转不进入通过条件。
  </action>
  <acceptance_criteria>
    - closure 测试能因任一权限、exclusion、水位、用户传播、partial、hash/version 回归而失败。
    - 外部 artifact 缺失被明确分类为 `HUMAN_NEEDED`，且不会产生 PASS/伪阈值。
    - 测试不含 skip/xfail/collect-only，也不从实现反查期望值形成自证。
  </acceptance_criteria>
  <verify>
    <automated>cd server && uv run pytest tests/codegraph/test_graph_bench_closure.py tests/services/code_graph/test_query_contract_conformance.py tests/services/code_graph/test_access.py tests/services/code_graph/test_query_service.py tests/services/code_graph/test_process_index.py tests/services/code_graph/test_process_trace.py tests/services/code_graph/test_impact.py tests/mcp_tools/test_graph_query_tool.py -x -q --reuse-db</automated>
  </verify>
  <done>Phase 133–140 的仓内关键连接有单一 closure gate，外部证据缺失不会被测试技巧掩盖。</done>
</task>

<task type="auto">
  <name>Task 2: 执行全量回归与真实证据分支</name>
  <files>server/tests/codegraph/test_graph_bench_closure.py</files>
  <read_first>
    - .planning/phases/140-threshold-policy/140-VALIDATION.md
    - server/codegraph/management/commands/evaluate_graph_bench.py
    - server/codegraph/management/commands/compare_graph_bench.py
    - server/codegraph/benchmark_policies/graph_query_threshold_policy.v1.json
    - mcp/package.json
    - task/pyproject.toml
  </read_first>
  <action>
按顺序实际运行 server benchmark/resolver/query/Process/impact/access/MCP 组合回归、task 全量 pytest、npm MCP `test`/`typecheck`/`build`/`prepack`/`npm pack --dry-run`，不得用 skip/delete/缩小选择器绕过失败。随后检查真实已索引目标仓、Qdrant/embedding、非占位 gold、v0.22 baseline report/run manifest/hashes 是否存在：若齐全，在完全相同 comparison identity 上运行 v0.24 candidate，先 locked_test compare，再仅通过显式 final-acceptance 参数打开 holdout，保存两命令、三 hash、raw trials、逐 case/bucket/cell diff 与 verdict；required gate 任一未过则 FAIL。若任一真实依赖缺失，停止在外部 gate 前，将 BENCH-06/07/EDGE-06 的真实数值验证标为 `human_needed` 和 tech debt，列明缺失 artifact 与可复现命令；仓内实现/测试结果仍完整报告，禁止生成数字、正式 policy 或 PASS。读 `graph_query_threshold_policy.v1.json` 时若文件因无 baseline 而未创建，按缺失处理，不要写入占位 JSON。此任务无人值守，不请求用户选择。日志与 ledger 继续 `redact_secrets_in_text` / `redact_for_ledger`。
  </action>
  <acceptance_criteria>
    - server、task、npm MCP 所列命令均真实执行并记录结果，没有删测、skip 或静默忽略失败。
    - 真实环境齐全时 compare report 的 final verdict 仅在全部 required/protected/language/capability gates 通过时为 PASS。
    - 真实环境不齐时最终状态明确区分“仓内闭合完成”与“外部验证 human_needed/tech debt”，不声称 v0.24 数值提升，不提交伪造阈值。
    - Go 深化、真跨仓 IMPACT-03、LSP 默认翻转不进入通过条件。
  </acceptance_criteria>
  <verify>
    <automated>cd server && uv run pytest tests/codegraph/test_graph_bench_*.py codegraph/resolver/tests tests/services/code_graph tests/mcp_tools/test_graph_query_tool.py -q --reuse-db && cd ../task && uv run pytest -q && cd ../mcp && npm test && npm run typecheck && npm run build && npm run prepack && npm pack --dry-run</automated>
  </verify>
  <done>全部仓内回归完成；真实证据可用则得到可审计 PASS/FAIL，不可用则得到准确 human_needed/tech debt 而非伪绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| five consumers → canonical service/manifest | 多语言生成物可能漂移，需完整 hash 与构建 conformance |
| real target/Qdrant/baseline → final claim | 外部运行证据决定是否可宣称提升，不得由 mock/seed 替代 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-140-14 | Tampering | closure tests | mitigate | 禁 skip/delete/缩小选择器，组合现有正向契约，HUMAN_NEEDED 显式状态 |
| T-140-15 | Elevation of Privilege | graph access | mitigate | 保留 GraphService 权限/exclusion fail-closed 回归 |
| T-140-16 | Tampering | consumer contract | mitigate | canonical manifest raw-byte hash + service/task/npm conformance/build gate |
| T-140-17 | Repudiation | final benchmark claim | mitigate | 两命令、三 hash、identity、raw trials、逐例/桶 diff 与 verdict |
| T-140-18 | Information Disclosure | final artifacts/logs | mitigate | 不记录 query/凭证，异常/ledger 使用规定脱敏入口 |
| T-140-SC | Tampering | package supply chain | mitigate | 不安装新包；npm 仅使用现有 lockfile 并执行 build/prepack/pack 验证 |
</threat_model>

<verification>
先跑仓内组合 gate，再判定真实环境可用性。真实 gate 不得由默认 pytest skip 替代；不可用即 human_needed/tech debt，可用则必须实际 compare 且全部 required gate 通过。
</verification>

<success_criteria>
- 五条 phase requirements 的仓内实现与测试全部闭合。
- 所有安全、观测、契约、同水位与 partial/degradation 不变量通过回归。
- 真实证据只产生 PASS、FAIL、INVALID 或 HUMAN_NEEDED 中的诚实状态，不伪造数字。
</success_criteria>

<artifacts_this_phase_produces>
- 跨 Phase 133–140 的 closure regression。
- server/task/npm MCP 全量回归证据。
- 环境齐全时：真实 v0.22 baseline、v0.24 candidate、locked policy、compare/holdout 报告。
- 环境缺失时：具体缺失 artifact、复现命令和 `human_needed`/tech debt 状态，不含伪造指标。
</artifacts_this_phase_produces>

<source_coverage_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | — | 同条件提升证明 + 安全可观测可回放收口 | 01-04 | COVERED | 外部证据不可用时诚实 human_needed
REQ | BENCH-06 | baseline 后独立锁 threshold policy，失败不刷新 | 02,04 | COVERED | 只读 JSON、hash pin、无 write-back
REQ | BENCH-07 | 同仓同 commit/query/gold/evaluator paired compare | 01,02,04 | COVERED | comparison/system identity 分离
REQ | EDGE-06 | language×framework×call_shape 三态与语言独立门 | 01,02,04 | COVERED | Go report-only
REQ | OBS-01 | graph query caller lifecycle | 03,04 | COVERED | 唯一三事件、无 query/凭证
REQ | OBS-02 | resolver/Process/lane/impact sampling | 03,04 | COVERED | debug/sampling + AST guard
RESEARCH | — | 真实 baseline 是数值 policy 前置 | 01,02,04 | COVERED | 缺失不造 policy/PASS
RESEARCH | — | comparison identity 与 system identity 分离 | 01,02 | COVERED | baseline/candidate revision 可不同
RESEARCH | — | edge-level ResolveResult 三态与 canonical taxonomy | 01 | COVERED | 含 re_export/component，冻结前对齐 from_import
RESEARCH | — | 四态 comparator 与 direction-aware gate | 02 | COVERED | INVALID 优先
RESEARCH | — | token/latency 不伪造证据 | 01,02,04 | COVERED | token unavailable；latency paired trials
CONTEXT | — | policy 独立版本化静态 JSON且只读 | 02 | COVERED | 仅真实 baseline 可生成
CONTEXT | — | holdout final-only | 01,04 | COVERED | 默认拒读并审计开启
CONTEXT | — | overall 不抵消 protected/language/capability | 02,04 | COVERED | 全部独立 required gate
CONTEXT | — | caller/sampling、脱敏、best-effort | 03,04 | COVERED | 行为 + AST 守卫
CONTEXT | Deferred | Go 深化、跨仓 impact、LSP 翻转、cursor/ranking 增强 | NONE | EXCLUDED | 明确 deferred/Future，不进入本阶段
</source_coverage_audit>

<output>
Create `.planning/phases/140-threshold-policy/140-04-SUMMARY.md` when done.
</output>
