---
phase: 115-ui
plan: 05
type: execute
wave: 4
depends_on: ["115-03"]
files_modified:
  - web/src/components/blueprint/sections/RequirementSpecSection.vue
  - web/src/components/blueprint/sections/RepoAssociationsSection.vue
  - web/src/components/blueprint/sections/CurrentStateSection.vue
  - web/src/components/blueprint/sections/ImplementationOverviewSection.vue
  - web/src/components/blueprint/sections/ApiContractsSection.vue
  - web/src/components/blueprint/sections/ImpactAnalysisSection.vue
  - web/src/components/blueprint/sections/InteractionFlowsSection.vue
  - web/src/components/blueprint/sections/MustHavesSection.vue
  - web/src/components/blueprint/sections/DecisionLogSection.vue
  - web/src/components/blueprint/BlueprintAssociationsSection.vue
  - web/src/components/blueprint/RepoAssociationCard.vue
  - web/src/components/blueprint/ImplementationItemCard.vue
  - web/src/components/blueprint/ApiContractCard.vue
  - web/src/components/blueprint/ImpactMatrixTable.vue
  - web/src/components/blueprint/__tests__/sections.spec.ts
  - web/src/components/blueprint/__tests__/mustHaves.spec.ts
autonomous: true
requirements: [VIEW-01, VIEW-02, VIEW-04]
prohibitions:
  - "🔒 **前端 CREATE-ONLY**：本 plan **零既有文件修改**（`git diff --name-only web/src | rg -v '^web/src/components/blueprint/'` 必须为空）。⛔ 不碰三处追加点（`zh-CN.json` / `main.css` / `api/index.ts`）；缺 i18n key 或缺 safelist 图标 ⇒ **回报而不是自己补**"
  - "⛔ **文件所有权**：只创建上方 14 个组件与 2 个测试文件。⛔ 不修改 115-03 / 115-04 的任何产物（`BlueprintBlock.vue` / `BlueprintBlockList.vue` / `BlueprintCitationChip.vue` / `CitationPreviewDialog.vue` / `citation/**` / `BlueprintThread*.vue` / `BlueprintReview*.vue` / `BlueprintBlockDiff.vue` / `annotationTokens.ts` 等）"
  - "⛔ ⭐ **段组件内不自行处理批注**（UI-SPEC §13.3）：`blockCtx = { threads, citations, readonly, activeThreadId, showClosed }` 五个 prop **原样透传**给 `BlueprintBlockList`。⛔ 段组件里出现 `sliceBlockText` / `<mark>` / `annotationClass` / `data-testid=\"blueprint-annotation-mark\"` 即违规（批注层的唯一实现点是 `BlueprintBlock.vue`）"
  - "⛔ ⭐ **`MustHavesSection.vue` 不收 `blockCtx`、不复用 `BlueprintBlockList`**（§6.9）：`iter_blocks` **不走查 `must_haves`**，它的三个数组是纯字符串与普通对象、**零 `block_id`** ⇒ 后端不会往那里挂线程。本段自渲染，是全文唯一的例外，**必须在组件内注释写明原因**"
  - "⛔ ⭐ **`DecisionLogSection.vue` 同样不接批注层**（P-14）：`decision_log` / `deferred_ideas` **也不在 `iter_blocks` 里**，且是**零 items 约束的裸 array**（`blueprint_schema.py:733-744` 三者均不在顶层 `required`）。UI-SPEC §13.3 给它派的 `emits: ['open-thread']` 语义**由本 plan 定夺为「跳转到该决策对应的线程」**（`decision_log[].thread_id` 存在时才渲染跳转按钮），⛔ 不是「在该段发起批注」"
  - "⛔ **三段可选裸 array 的字段访问逐项可选链**（P-14）：`decision_log` / `deferred_ideas` / `execution_plan` 的条目形状**运行期什么都有可能**（114-04 写入的 `{thread_id, question, answer, decision, decided_by, decided_at, applied_in_version}` 是**约定不是契约**）。缺键一律渲染「—」，⛔ 不渲染 `undefined`、⛔ 不抛。特别保 `answer` 键（唯一有下游消费方）"
  - "⛔ **`execution_plan` 段本相位不渲染**（CONTEXT `<deferred>` 已登记）：它是确认后确定性派生的执行计划，呈现面归属实施链路（116+），在只读评审面渲染会与 `TechPlanCard`（§13.2 禁区）职责重叠。`rg -n 'execution_plan' web/src/components/blueprint/` 除注释外零渲染"
  - "⛔ ⭐ **`associations` 段本相位只做两块**（P-5，SC-4 范围收窄）：「本蓝图引用了」（`content.citations` 引用池纯前端聚合，**零端点**）+「关联项目」（`meta.project_id` + `RouterLink`）。⛔ **不调 `knowledgeApi.getRelated` / `getArtifactAssociations`** —— 它们查 `initiatives.Artifact` 投影的 KnowledgeEntity（`server/knowledge/artifact_associations.py:75`），拿蓝图的 `delivery.Artifact.id` 去调**必然 404/空**；「引用了本蓝图 / 关联知识」顺延 Phase 116 的知识图谱物化"
  - "⛔ **`availability` / `support_repository_id` 只在 `data_source` 内读，顶层零残留**（113-05 决策）：前端**不得回落读顶层**，读不到即渲染「未标注」。回落读顶层会让「写错位置」这类后端缺陷在 UI 上被静默掩盖"
  - "⛔ **`MermaidDiagram` 的 prop 名是 `code` 不是 `source`**（P-12），且**空源码必须由调用方 `v-if`**（组件自己会渲一个空 `<pre>` 且无提示）。交互流程段一律经 `BlueprintBlock` 的 mermaid 分支渲染（115-03 已实现），⛔ 本 plan 不直接引入 `MermaidDiagram`"
  - "⛔ **`CompactEmptyState` 的 `icon` 传裸名**（P-6，如 `icon=\"lucide--file-x\"`），按钮放**默认 slot**（该组件**没有** `action-label` prop 也**没有** `@action` emit）。⛔ 不照抄同目录 `pages/knowledge/index.vue:309,317` 与 `entities/[id].vue:105,107-108` 的错误写法"
  - "⛔ **窄屏矩阵表降级为卡片堆叠，不做横向滚动表**（§6.7）"
  - "⛔ **禁 `v-html`**；⛔ 组件内零颜色字面量（`rg 'hsl\\('` 零命中）；⛔ 禁止在 `Badge` 上用 `:class` 追加颜色类（颜色一律经 `variant`）；⛔ `refetchInterval` / `edit-block` 零命中"
  - "⛔ **不引入语法高亮引擎**（`pseudocode` 由 115-03 的 `BlueprintBlock` 渲染，本 plan 不碰）；⛔ 不新增运行时依赖；后端零改动"
must_haves:
  truths:
    - "⭐ **九个 section 组件 + 一个 associations 段 = 十段全部有渲染实现**，与 UI-SPEC §6.1 的导航段表逐条对应（`requirement_spec` / `repo_associations` / `current_state_analysis` / `implementation_overview` / `api_contracts` / `impact_analysis` / `interaction_flows` / `must_haves` / `decision_log` / `associations`）"
    - "⭐ **段组件零批注实现**：`rg -n 'sliceBlockText|annotationClass|blueprint-annotation-mark|<mark' web/src/components/blueprint/sections/ web/src/components/blueprint/RepoAssociationCard.vue web/src/components/blueprint/ImplementationItemCard.vue web/src/components/blueprint/ApiContractCard.vue web/src/components/blueprint/ImpactMatrixTable.vue` **零命中**；`blockCtx` 五个 prop 原样透传给 `BlueprintBlockList`"
    - "⭐ **`must_haves` 必被渲染（§20 断言 9）**：三个子块都非空的 content ⇒ `[data-testid=blueprint-must-haves]` 存在、`truths` / `artifacts` / `key_links` 三块各自渲染出条目；**且该段内 `[data-testid=blueprint-annotation-mark]` 计数 == 0**。两个变异（漏渲 `must_haves` / 给它接上批注层）各自转红"
    - "⭐ **`must_haves` 三个数组同为空或整键缺失（v0 旧数据）⇒ 整段不渲染任何内容卡**（§6.9）；仅**部分**子块为空时该子块不渲染、其余照渲。⚠️ **但段容器（`<section id>`）与导航项由 115-06 无条件渲染** —— 本组件只决定「段内出不出内容」（P-4：条件渲染段容器会让 `AnchorNavLayout` 的 mount-only observer 永远观察不到）"
    - "⭐ **`decision_log` 的 `emits: ['open-thread']` 语义已定夺**：条目带 `thread_id` 时渲染一个「查看对应线程」按钮 emit 它；不带则不渲染该按钮（⛔ 不是死码、⛔ 不是发起批注）"
    - "⭐ **`deferred_ideas` 挂在 `decision_log` 段尾的默认折叠 `Collapsible`**（组头「本方案明确不做的事（{n}）」，§6.1 去向总表）；空则不渲染该 Collapsible"
    - "⭐ **`associations` 段的范围收窄被显式呈现**：只有「本蓝图引用了」（按 `source_type` 分组统计 + 可点 chip，走同一预览弹层）与「关联项目」（`RouterLink`）两块；⛔ 零 `getRelated` / `getArtifactAssociations` 调用"
    - "⭐ **仓库关联卡可直接跳转仓库页（SC-3 / VIEW-02）**：`RouterLink to=\"/repositories/{repository_id}\"`；`role` 双色徽标（`direct` → `default` 直接改动 / `indirect` → `secondary` 间接依赖，**不发明第三色**）；`fitness.verdict` 三档（`suitable`→`success` / `partial`→`warning` / `unsuitable`→`destructive`）；⭐ `unsuitable` 时的「替代建议」按 `fitness.reasons` **自由文本原样展示，不补 schema 字段**（§0.2 判定 6，同时定夺 STATE 登记的「Phase 112 残留 PARTIAL / FLOW-02」）"
    - "⭐ **API 契约卡的 `availability` 只从 `data_source` 内读**：`existing`→`success` 数据已有 / `needs_support`→`warning` 需 `{support_repository_name}` 支持产出（该仓名可点跳 `/repositories/{support_repository_id}`）/ 读不到 ⇒ 渲染「未标注」。⛔ 不回落读顶层"
    - "⭐ **`current_state_analysis` 的 finding `citations` 为空时渲染 `<Badge variant=\"destructive\">缺引用</Badge>`**（schema 要求必填，缺失是质量信号，⛔ 不隐藏）"
    - "跨段跳转 chip：`related_feature_points` → 滚动到 `requirement_spec` 对应功能点并 2s ring 高亮；`api_ref` → 滚动到 `api_contracts` 对应卡并 2s ring 高亮。两者一律 **emit 给页面**（`emits: { 'goto-anchor': [domId: string] }`），⛔ 段组件内不自行 `window.scrollTo`（偏移常量 88px 归页面统一处理）"
    - "实现概述段的**波次泳道条**（`wave 1 · 3 项 | wave 2 · 5 项 …`）可点筛选该波次实现项，**纯客户端筛选不改 URL**"
    - "三段可选裸 array 全程可选链、缺键渲染「—」不渲染 `undefined`、不抛；`answer` 键被显式保留渲染"
    - "组件内零 `v-html`、零颜色字面量、零 `refetchInterval`、零 `edit-block`；`defineEmits` 统一元组简写；表格一律 `~/components/ui/table` 的语义 `<table>` / `<th scope=\"col\">`"
    - "前端门：`cd web && pnpm exec vitest run` 全绿（含源码守卫 spec 复跑）、`pnpm lint` 通过、`pnpm type-check` 通过"
  artifacts:
    - path: "web/src/components/blueprint/sections/MustHavesSection.vue"
      provides: "验收锚点三子块（truths 勾选列表 / artifacts 紧凑表 / key_links 箭头行）；⭐ 不收 blockCtx、不复用 BlueprintBlockList、组件内注释写明原因；三块全空整段不出内容"
      contains: "blueprint-must-haves"
    - path: "web/src/components/blueprint/sections/DecisionLogSection.vue"
      provides: "决策记录条目（逐项可选链、缺键渲染「—」、保 answer 键）+ thread_id 存在时的「查看对应线程」emit + 段尾 deferred_ideas 默认折叠 Collapsible"
      contains: "open-thread"
    - path: "web/src/components/blueprint/RepoAssociationCard.vue"
      provides: "role 双色 + fitness 三档 + rationale/适配判定 Collapsible + direct/indirect 专属分区 + support_needed + routing_evidence（含 cross_team 徽标）+ confirmed_at_gate 徽标 + ⭐ RouterLink 跳仓库页（SC-3）"
      contains: "/repositories/"
    - path: "web/src/components/blueprint/ApiContractCard.vue"
      provides: "direction/kind/method 徽标 + 请求响应示例（JSON.stringify + <pre>，超 20 行折叠）+ data_source 分区（availability 只从 data_source 读，读不到渲染「未标注」）+ 消费方 chips"
      contains: "data_source"
    - path: "web/src/components/blueprint/ImpactMatrixTable.vue"
      provides: "affected_features 矩阵表 + regression_scope 紧凑表 + data_migrations（reversible=false 加「不可逆」徽标）+ 窄屏卡片堆叠降级（不做横向滚动表）"
      contains: "不可逆"
    - path: "web/src/components/blueprint/BlueprintAssociationsSection.vue"
      provides: "⭐ 范围收窄后的两块：本蓝图引用了（citations 按 source_type 分组统计 + 可点 chip）+ 关联项目（RouterLink）；⛔ 零 getRelated/getArtifactAssociations"
      contains: "source_type"
    - path: "web/src/components/blueprint/__tests__/mustHaves.spec.ts"
      provides: "§20 断言 9（三子块渲染 + 段内 mark 计数 == 0）+ 全空整段不出内容 + 部分子块空的正反并列"
      contains: "blueprint-must-haves"
  key_links:
    - from: "web/src/components/blueprint/sections/RepoAssociationsSection.vue"
      to: "web/src/components/blueprint/BlueprintBlockList.vue"
      via: "blockCtx 五个 prop 原样透传（段组件零批注实现）"
      pattern: "BlueprintBlockList"
    - from: "web/src/components/blueprint/RepoAssociationCard.vue"
      to: "web/src/pages/repositories/[id]/index.vue"
      via: "RouterLink to=\"/repositories/{repository_id}\"（SC-3 仓库关联卡直接跳转）"
      pattern: "/repositories/"
    - from: "web/src/components/blueprint/sections/MustHavesSection.vue"
      to: "server/services/process_runtime/blueprint_schema.py"
      via: "must_haves 是 required 键但不在 iter_blocks 走查里 ⇒ 零 block_id ⇒ 本段自渲染不接批注层（组件内注释写明）"
      pattern: "must_haves"
    - from: "web/src/components/blueprint/BlueprintAssociationsSection.vue"
      to: "web/src/components/blueprint/CitationPreviewDialog.vue"
      via: "「本蓝图引用了」的 chip 走同一预览弹层（页面接线），零新端点"
      pattern: "citation"
---

<objective>
把蓝图 content 的**十个可导航段**全部渲染出来，且**一行批注逻辑都不重复实现** —— 段组件的职责被压缩成「数据排版 + 把 Block[] 交给 `BlueprintBlockList`」：

① **七个 content 主段**（`requirement_spec` / `repo_associations` / `current_state_analysis` / `implementation_overview` / `api_contracts` / `impact_analysis` / `interaction_flows`）+ 四张承载复杂形状的卡（仓库关联 / 实现项 / API 契约 / 影响矩阵）。SC-1 的「六段结构化渲染（交互流程 mermaid / 伪代码块 / API 契约卡 / 影响矩阵 / 仓库关联卡）」在这里兑现。

② **两个特例段**：`must_haves`（⭐ **schema 的 required 键**，承载 goal-backward 的验收断言；但 `iter_blocks` **不走查它** ⇒ 零 `block_id` ⇒ **全文唯一不走 `BlueprintBlockList` 的正文段**）与 `decision_log`（同样不在 `iter_blocks` 里，且是**零 items 约束的裸 array**）。这两段是 P-14 与 §6.9 的落点，最容易被实现者「顺手接上批注层」而变成死码。

③ **`associations` 段（VIEW-04 / SC-4）**：⭐ 本相位**显式范围收窄**为「本蓝图引用了」+「关联项目」两块。原设计的「引用了本蓝图 / 关联知识」依赖 `knowledgeApi.getRelated` / `getArtifactAssociations`，而它们查的是 `initiatives.Artifact` 投影的 KnowledgeEntity —— 拿蓝图的 `delivery.Artifact.id` 去调**必然 404/空**（P-5），知识图谱物化明确是 Phase 116。⛔ 不靠 404 兜底糊过去。

Purpose: 这是 SC-1 的主体工作量（14 个文件里 13 个是排版件），但每一件都受同一条纪律约束——**批注与引用只有 `BlueprintBlock.vue` 一个实现点**。加上两个「不接批注」的特例段与一处必须显式登记的范围收窄，本 plan 的风险不在难度而在**纪律一致性**。
Output: 14 个新建组件 + 2 个新建测试文件。**零既有文件修改、零新增依赖、后端零改动。**
</objective>

<execution_context>
工作目录：worktree 根 `/Users/zaneliu/Projects/open-source/friday-ai/.claude/worktrees/v0.20-blueprint`。前端命令 `cd web && ...`，测试 `pnpm exec vitest run <path>`。

**wave 4，依赖 115-03**。以下**已存在**，⛔ 只 import 不重写：
- ⭐ `~/components/blueprint/BlueprintBlockList.vue`（115-03）—— **所有 Block[] 一律经它渲染**；其 props 逐字见 115-03-SUMMARY（`blocks` / `sectionPath` / `threads` / `citations` / `readonly` / `activeThreadId` / `showClosed` / `loading?` / `plainMermaid?`），emits 见同处（含 `cross-block-selection`）。
- `~/components/blueprint/BlueprintCitationChip.vue`（115-03）—— `associations` 段的「本蓝图引用了」chip 复用它。
- `~/components/blueprint/BlueprintStatusBadge.vue`（115-02）。
- `~/utils/blueprintBlocks` 的 `blockText` / `summaryText`；`~/config/blueprintStatus`。
- i18n `knowledge.blueprints.section.* / repo.* / api.* / impact.* / flow.* / mustHaves.* / block.*`（115-02 已写全，⛔ 缺键回报不自补）。
- safelist 图标（115-02 已追加 12 个）：`change_type` 四档用 `lucide--file-plus` / `lucide--file-pen-line` / `lucide--file-x` / `lucide--file-cog`（这四个是**运行期按 `change_type` 拼接的裸名**，故必须在 safelist）；⛔ 缺图标回报不自补。
- ⭐ **先分清两类再判断是否「缺图标」**（plan-checker N3）：只有**运行期拼接的裸名**（`` :class="`icon-[${x}]`" ``）才需要 safelist；**写死在 `.vue` 源码里的字面量完整类名**（如 `class="icon-[lucide--mouse-pointer-click]"`）由 content 扫描覆盖，**不在 safelist 里是正常的**，⛔ 不要据此回报 115-02 漏项。

⚠️ **115-04 的产物在本 plan 中不需要**（同为组件层、无依赖关系）；本 plan 也不得修改它们。

**schema 形状（写组件前必须实读，⛔ 不得凭 UI-SPEC 转述字段名）**：`server/services/process_runtime/blueprint_schema.py`
- `requirement_spec`：`goal` / `background` / `feature_points[]`（`id` / `description` / 意图分类字段）；
- `repo_associations[]`：`repository_id` / `role` / `rationale.text` / `rationale.citations` / `responsibility` / `fitness.verdict` / `fitness.reasons` / `planned_change_summary` / `support_needed` / `routing_evidence`（含 `score` / `confidence` / `cross_team`）/ `decided_by` / `confirmed_at_gate` / indirect 专属的「会被用到的能力」（`name` / `location` / `how_used`）；
- `current_state_analysis[]`：`repository_id` / `summary` / `findings[]`（`id` / `kind`（`capability`/`gap`/`risk`/`convention`）/ `topic` / `text` / `citations` / `related_feature_points`）；
- `implementation_overview`：`requirement_narrative` / `modules[]`（`id` / `name` / 关联功能点 / 关联仓 / `narrative`）/ `items[]`（`id` / `change_type` / 所属模块 / 所属仓 / `wave` / `depends_on` / `how` / `existing_integration` / `files_touched[]`（`path` / `action` / `note`）/ `test_strategy`）；
- `api_contracts[]`：`id` / `direction` / `kind` / `method` / `path` / `description` / `request_example` / `response_example` / `data_source`（`from_service` / `from_api` / `fields_needed` / ⭐ **`availability`** / ⭐ **`support_repository_id`** / `notes`）/ 消费方 / 所属仓；
- `impact_analysis`：`business_impact` / `affected_features[]`（`feature` / 涉及仓库 / `kind` / `description` / 证据）/ `regression_scope[]`（`level`）/ `compat_risks` / `rollback_plan` / `data_migrations[]`（`reversible`）；
- `interaction_flows[]`：`id` / `name` / `trigger` / `mermaid` / `steps[]`（`seq` / `actor` / `action` / `component` / `api_ref` / `data_in` / `data_out` / `note`）/ `alternative_paths[]`（`condition` + 同构 steps）；
- `must_haves`：`truths: string[]` / `artifacts[]`（`path` / `provides`）/ `key_links[]`（`from` / `to` / `via`）—— ⭐ **不在 `iter_blocks` 走查里**；
- `decision_log` / `deferred_ideas` / `execution_plan`（`:733-744`）：**三者都是零 items 约束的裸 array，都不在顶层 `required`，都不在 `iter_blocks` 里**。

**既有可复用件契约（实测）**：
- `~/components/ui/table`：语义 `<table>` / `<th scope="col">`，⛔ 不用 div 模拟。
- `~/components/ui/collapsible`：`aria-expanded` 由 reka-ui 提供。
- `~/components/ui/badge`：八 variant `default/secondary/destructive/outline/success/warning/info/muted`；⛔ 禁 `:class` 追加颜色。
- `~/components/common/CompactEmptyState.vue`（33 行）：`icon` 收**裸名**；**无 `action-label` prop、无 `@action` emit**，只有默认 slot。
- `~/components/knowledge/EntityMetadataCard.vue` + `web/DESIGN.md` 的标准卡片（`.card` + `px-5 py-3.5` 头 + `p-5` 体）：四张卡的形状 analog。
- `~/components/repository/IndexProgressTimeline.vue:31-46`：「`.card` → 头 → 体 → 行内空态」的骨架 analog。
- `pages/knowledge/entities/[id].vue:117-132`：`<section :id class="space-y-4">` 内三分支（`Skeleton` → 实渲 → `CompactEmptyState`）的 analog；⚠️ 其 `:105,107-108` 的 `CompactEmptyState` 用法（传完整类名 + 不存在的 `action-label`/`@action`）是**反例**，⛔ 不照抄。

**测试范式**：`components/prompts/__tests__/PromptVersionDiff.test.ts`（113 行，最规范）；`pages/knowledge/__tests__/entity-detail.spec.ts`（68 行：手写最小 i18n 键树、`stubs`）。⭐ **挂载任何可能间接渲染 mermaid 的组件必 `stubs: { MermaidDiagram: true }`**。
</execution_context>

<context>
@.planning/phases/115-ui/115-03-SUMMARY.md（**`BlueprintBlockList` 的 props/emits 逐字**、DOM 契约与 `data-testid` 清单、五类块的「是否可字符级划线」对照表 —— 段组件按它透传）
@.planning/phases/115-ui/115-02-SUMMARY.md（`blockText` / `summaryText` 签名；i18n 键清单；safelist 12 图标）
@.planning/phases/115-ui/115-UI-SPEC.md §6.1（**十段导航表 + content 14 键去向总表**逐字）、§6.3（**仓库关联卡的 ASCII 图 + 字段渲染契约表**逐行）、§6.4（现状分析段：kind 四档徽标 / topic 小标题 / related_feature_points chip / **缺引用徽标**）、§6.5（实现概述三层 + **change_type 四档徽标表** + 卡内分区 + 波次泳道条）、§6.6（**API 契约卡的 ASCII 图 + 字段渲染契约表**，含 ⚠️ 读取位置那一行）、§6.7（影响矩阵五块 + 窄屏降级）、§6.8（交互流程四层 + api_ref chip）、§6.9（**验收锚点段的三子块表 + 四条关键约束**逐字）、§10.2（关联区三块 —— ⭐ 其中一块由本 plan 收窄）、§16（空态与各段文案）、§20（断言 9）
@.planning/phases/115-ui/115-RESEARCH.md（§B.4 的 **13 处 collect 完整清单**（哪些字段是 Block[]、哪些不是——段组件据此决定哪些走 `BlueprintBlockList`）、P-5（associations 的范围收窄）、P-6（`CompactEmptyState` 的两种互斥写法）、P-12（mermaid 三条）、P-14（三段裸 array 的零约束与不接批注））
@.planning/phases/115-ui/115-PATTERNS.md（§D 的三个 analog（段容器三分支 / 块内 mustache / 卡片骨架）与四条「必须 DIFFER」、§D13 的「数据源必须换」）
@server/services/process_runtime/blueprint_schema.py（**字段名的唯一权威**：`$defs.block` / `$defs.citation` / 14 个顶层键的 properties 与 required；⚠️ 三段可选裸 array 在 `:733-744`）
@web/src/components/blueprint/BlueprintBlockList.vue（115-03 产物，段组件的唯一下游）
@web/src/components/knowledge/EntityMetadataCard.vue（卡片形状 analog）
@web/src/components/repository/IndexProgressTimeline.vue L31-46（卡片骨架与行内空态 analog）
@web/src/pages/knowledge/entities/[id].vue L117-162（段容器三分支 analog；⚠️ `:105,107-108` 的 `CompactEmptyState` 用法是反例）
@web/src/components/prompts/__tests__/PromptVersionDiff.test.ts（组件测试最规范范式）
</context>

<tasks>

<task type="auto">
  <name>Task 1: 段 0–2（需求规格 / 仓库关联 + RepoAssociationCard / 现状分析）</name>
  <files>web/src/components/blueprint/sections/RequirementSpecSection.vue, web/src/components/blueprint/sections/RepoAssociationsSection.vue, web/src/components/blueprint/RepoAssociationCard.vue, web/src/components/blueprint/sections/CurrentStateSection.vue</files>
  <read_first>.planning/phases/115-ui/115-UI-SPEC.md §6.3（**仓库关联卡的 ASCII 图与字段渲染契约表逐行**：role 双色 / fitness 三档 / reasons 原样展示 / 跳转 / cross_team / confirmed_at_gate / `lg:grid-cols-2`）、§6.4（现状分析段四条）；server/services/process_runtime/blueprint_schema.py 的 `requirement_spec` / `repo_associations` / `current_state_analysis` 三段 properties（**字段名逐字**，尤其 `rationale` 是对象还是 Block[]、`responsibility` / `planned_change_summary` / `support_needed` / `fitness.reasons` 各自是不是 Block[] —— 这决定哪些走 `BlueprintBlockList`）；.planning/phases/115-ui/115-RESEARCH.md §B.4 的 13 处 collect 清单（**权威判据**：`repo_associations[].rationale.text` / `.responsibility` / `.fitness.reasons` / `.planned_change_summary` / `.support_needed` 与 `current_state_analysis[].summary` / `.findings[].text` 都在 collect 里 ⇒ 全部是 Block[] ⇒ 全部走 `BlueprintBlockList`）；.planning/phases/115-ui/115-03-SUMMARY.md（`BlueprintBlockList` 的 props/emits 逐字）；web/src/components/knowledge/EntityMetadataCard.vue（卡片形状）；web/src/components/repository/IndexProgressTimeline.vue L31-46（`.card` + `px-5 py-3.5` + `p-5` 骨架与行内空态）；web/src/components/ui/collapsible/index.ts（导出子组件名）</read_first>
  <action>
**统一约定（本 plan 全部段组件共用，写在每个组件的 docstring 里）**：
- `props` 形如 `{ <段数据>, ...blockCtx }`，其中 `blockCtx = { threads, citations, readonly, activeThreadId, showClosed }` **五个 prop 原样透传**给 `BlueprintBlockList`；⛔ **段组件内不自行处理批注**（UI-SPEC §13.3）。
- `emits` 统一元组简写；跨段跳转一律 `'goto-anchor': [domId: string]`（⛔ 段内不自行 `window.scrollTo` —— 88px 偏移常量归页面统一处理）；引用点击由 `BlueprintBlockList` 透传上来，段组件原样再透传。
- 每段一个 `data-testid="blueprint-<段 key 的 kebab 形式>"`（如 `blueprint-repo-associations`）。
- **段内空态**用 `CompactEmptyState`（⭐ `icon` 传**裸名**、按钮放**默认 slot**），文案 `t('knowledge.blueprints.section.empty', { section })` =「本方案未涉及{段名}」。
- **段容器与导航项由 115-06 无条件渲染**（P-4）；本层组件只决定「段内出不出内容」。

**① `RequirementSpecSection.vue`**：`props: { spec, ...blockCtx }`；`emits: { 'goto-anchor' }`。
- `goal` / `background` 各一个 `BlueprintBlockList`（`sectionPath` 分别 `requirement_spec.goal` / `.background`）；
- `feature_points[]` 逐条一张紧凑卡：`id`（mono 小字，**DOM id 用 `fp-${id}`** 供跨段跳转定位）+ 意图分类徽标（`greenfield` / `brownfield` / `fix` 三档，用 `Badge variant` 承载颜色，⛔ 不发明第四档）+ `description` 走 `BlueprintBlockList`（`sectionPath` 为 `requirement_spec.feature_points[<id>].description`）；
- 段头 badge 数 = 功能点数。

**② `RepoAssociationCard.vue`**（本 Task 最重的一件，UI-SPEC §6.3 逐行实现）：`props: { association, repoName, ...blockCtx }`；`emits: { 'goto-anchor', 'citation-click', 'thread-click', 'selection-comment', 'cross-block-selection' }`（后四项由内部 `BlueprintBlockList` 透传）。
卡片用 `.card` + `p-4`，父层 `lg:grid-cols-2` 网格。逐字实现：
- 卡头一行：⭐ `role` **双色**徽标（`direct` → `<Badge variant="default">直接改动</Badge>`；`indirect` → `<Badge variant="secondary">间接依赖</Badge>`，**不发明第三色**）+ 仓名 + `fitness.verdict` 徽标（`suitable`→`success` / `partial`→`warning` / `unsuitable`→`destructive`）+ ⭐ 右上角 `RouterLink to="/repositories/{association.repository_id}"`（图标 `icon-[lucide--external-link]`，文案 `t('knowledge.blueprints.repo.openRepository')`，**SC-3 的落点**）；
- 「职责」：`responsibility` 走 `BlueprintBlockList`；
- 「选仓理由」`Collapsible`（默认折叠）：`rationale.text` 走 `BlueprintBlockList`；`rationale` 上若另有 `constraint_refs` 之类字段按实测渲染成 chip（读不到就不渲染，⛔ 不猜字段名）；
- 「适配判定」`Collapsible`：`fitness.verdict` 徽标 + `fitness.reasons` 走 `BlueprintBlockList`。⭐ **`unsuitable` 时的「替代建议」按 `reasons` 自由文本原样展示，⛔ 不补 schema 字段、⛔ 不做结构化解析**（§0.2 判定 6；同时定夺 STATE 登记的「Phase 112 残留 PARTIAL / FLOW-02 替代建议无结构化字段」——前端只是呈现方，为呈现去改已锁定的 schema 不划算）。**这条要写进组件 docstring**；
- `direct` 专属：「本仓改动摘要」`planned_change_summary` 走 `BlueprintBlockList`；
- `indirect` 专属：「会被用到的能力」列表（`name` / `location` / `how_used`，逐项可选链，缺键渲染「—」）；
- `support_needed`（**有值才渲染**）：标题「需要配合」+ `BlueprintBlockList`；
- `routing_evidence` 一行 mono 小字：`score` 保留两位 + `confidence` + ⭐ `cross_team === true` 时追加 `<Badge variant="warning">跨组协作</Badge>`；
- 底部一行：`decided_by`（AI 提议 / 人工确认）+ `confirmed_at_gate === true` 时 ✓「已在确认门锁定」，⭐ `=== false` 时追加 `<Badge variant="outline">未经确认门锁定</Badge>`。
`data-testid="blueprint-repo-card"`；⛔ 卡内零颜色字面量（一律 `Badge variant` 或 Tailwind 语义类）。

**③ `RepoAssociationsSection.vue`**：`props: { associations, repoNames, ...blockCtx }`；`lg:grid-cols-2` 网格渲染 N 张 `RepoAssociationCard`；空态走 `CompactEmptyState`；段头 badge = 仓库数（direct/indirect 分色 tone 由 115-06 的导航层决定，本组件只出数）。`data-testid="blueprint-repo-associations"`。

**④ `CurrentStateSection.vue`**：`props: { analysis, repoNames, ...blockCtx }`；`emits: { 'goto-anchor', ...透传 }`。
- 按 `repository_id` 分组，组头显示仓名 + finding 计数（形状照 `IndexProgressTimeline.vue` 的分组）；
- 组内 `summary` 走 `BlueprintBlockList`（`sectionPath` = `current_state_analysis[<repository_id>].summary`）；
- 每条 finding：`kind` 徽标四档（`capability`→`info` 能力 / `gap`→`warning` 缺口 / `risk`→`destructive` 风险 / `convention`→`secondary` 约定）+ `topic` 作小标题（14px/600）+ `text` 走 `BlueprintBlockList`（`sectionPath` = `...findings[<id>].text`）；
- `related_feature_points` 渲染成可点 chip ⇒ **emit `goto-anchor('fp-' + id)`**（⛔ 段内不自行滚动）；
- ⭐ **`citations` 为空时渲染 `<Badge variant="destructive">缺引用</Badge>`**（schema 要求必填，缺失是质量信号，⛔ 不隐藏）。
`data-testid="blueprint-current-state"`。
  </action>
  <verify>
    <automated>cd web && pnpm type-check && pnpm lint</automated>
  </verify>
  <acceptance_criteria>
- `cd web && pnpm type-check` 与 `pnpm lint` 通过。
- ⭐ **段组件零批注实现**：`rg -n "sliceBlockText|annotationClass|blueprint-annotation-mark|<mark" web/src/components/blueprint/sections/RequirementSpecSection.vue web/src/components/blueprint/sections/RepoAssociationsSection.vue web/src/components/blueprint/sections/CurrentStateSection.vue web/src/components/blueprint/RepoAssociationCard.vue` **零命中**。
- ⭐ **Block[] 一律走 `BlueprintBlockList`**：`rg -c "BlueprintBlockList" web/src/components/blueprint/RepoAssociationCard.vue` ≥ **4**（responsibility / rationale.text / fitness.reasons / planned_change_summary 或 support_needed）。
- ⭐ **SC-3 跳转**：`rg -n '/repositories/' web/src/components/blueprint/RepoAssociationCard.vue` 命中且是 `RouterLink`（`rg -n "RouterLink" web/src/components/blueprint/RepoAssociationCard.vue` 命中）。
- ⭐ **role 只有两色**：`cd web && node -e "const s=require('fs').readFileSync('src/components/blueprint/RepoAssociationCard.vue','utf8'); const m=s.match(/role[\s\S]{0,400}/); if(!/direct/.test(s)||!/indirect/.test(s)) throw new Error('缺 role 两档'); if(/role\s*===\s*['\"](?!direct|indirect)/.test(s)) throw new Error('发明了第三种 role'); console.log('role two-tone OK')"` 输出 `role two-tone OK`。
- ⭐ **不结构化解析替代建议**：`rg -n "替代建议|alternative" web/src/components/blueprint/RepoAssociationCard.vue` 的命中**只能在注释或 i18n key 里**，⛔ 不得出现对 `reasons` 的正则/切分解析（`rg -n "reasons[\s\S]{0,60}(split|match|replace)" web/src/components/blueprint/RepoAssociationCard.vue` 零命中）。
- ⭐ **缺引用徽标**：`rg -n "缺引用|missingCitations" web/src/components/blueprint/sections/CurrentStateSection.vue web/src/locales/zh-CN.json` 命中（文案在 i18n，组件传 key）。
- ⭐ **跨段跳转是 emit 不是 scrollTo**：`rg -n "window.scrollTo|scrollIntoView" web/src/components/blueprint/sections/ web/src/components/blueprint/RepoAssociationCard.vue` **零命中**；`rg -n "goto-anchor" web/src/components/blueprint/sections/CurrentStateSection.vue` 命中。
- ⭐ **`CompactEmptyState` 用法正确（P-6）**：`rg -n 'icon="icon-\[' web/src/components/blueprint/sections/` **零命中**；`rg -n "action-label|@action" web/src/components/blueprint/sections/` **零命中**。
- ⛔ 零 `v-html`、零颜色字面量：`rg -n "v-html|hsl\(|#[0-9a-fA-F]{6}" web/src/components/blueprint/sections/RequirementSpecSection.vue web/src/components/blueprint/sections/RepoAssociationsSection.vue web/src/components/blueprint/sections/CurrentStateSection.vue web/src/components/blueprint/RepoAssociationCard.vue` **零命中**。
- ⛔ 零既有文件改动：`git diff --name-only web/src | rg -v "^web/src/components/blueprint/"` 输出**为空**。
  </acceptance_criteria>
  <done>前三段渲染就位：所有 Block[] 经 `BlueprintBlockList`、段组件零批注实现；仓库关联卡逐行落地 §6.3（含 SC-3 跳转、role 双色、fitness 三档、`unsuitable` 的替代建议原样展示、cross_team 与 confirmed_at_gate 两枚旁注徽标）；现状分析的缺引用质量信号可见；跨段跳转一律 emit 给页面。</done>
</task>

<task type="auto">
  <name>Task 2: 段 3–6（实现概述 + ImplementationItemCard / API 契约 + ApiContractCard / 影响矩阵 + ImpactMatrixTable / 交互流程）</name>
  <files>web/src/components/blueprint/sections/ImplementationOverviewSection.vue, web/src/components/blueprint/ImplementationItemCard.vue, web/src/components/blueprint/sections/ApiContractsSection.vue, web/src/components/blueprint/ApiContractCard.vue, web/src/components/blueprint/sections/ImpactAnalysisSection.vue, web/src/components/blueprint/ImpactMatrixTable.vue, web/src/components/blueprint/sections/InteractionFlowsSection.vue</files>
  <read_first>.planning/phases/115-ui/115-UI-SPEC.md §6.5（**实现概述三层 + change_type 四档徽标表 + 卡内四分区 + 波次泳道条**逐字）、§6.6（**API 契约卡 ASCII 图 + 字段渲染契约表逐行**，尤其 ⚠️「读取位置」那一行：`availability` / `support_repository_id` **只在 `data_source` 内**，顶层零残留，前端不得回落读顶层）、§6.7（影响矩阵五块 + kind 五档 + level 三档 + `reversible=false` 徽标 + **窄屏卡片堆叠不做横向滚动表**）、§6.8（交互流程四层：卡头 / mermaid / 步骤表七列 + actor 四档 / api_ref chip / note 展开行 / alternative_paths）；server/services/process_runtime/blueprint_schema.py 的 `implementation_overview` / `api_contracts` / `impact_analysis` / `interaction_flows` 四段 properties（**字段名逐字**）；.planning/phases/115-ui/115-RESEARCH.md §B.4 的 13 处 collect 清单（判定哪些字段是 Block[]：`implementation_overview.requirement_narrative` / `.modules[].narrative` / `.items[].how` / `.existing_integration` / `.test_strategy`；`api_contracts[].description` / `.data_source.notes`；`impact_analysis.business_impact` / `.affected_features[].description` / `.compat_risks` / `.rollback_plan`；`interaction_flows[].steps[].note`）、§B.3 的 `MermaidDiagram` 契约表；.planning/phases/115-ui/115-03-SUMMARY.md（`BlueprintBlockList` 的 props，**mermaid 块由它内部的 `BlueprintBlock` 处理，本 plan 不直接引入 `MermaidDiagram`**）；web/src/components/ui/table/index.ts（导出的子组件名与用法）</read_first>
  <action>
**① `ImplementationItemCard.vue`**：`props: { item, moduleName, repoName, ...blockCtx }`；emits 同 Task 1 约定。
- 卡头：`change_type` 徽标四档（`create`→`success` 新建 `icon-[lucide--file-plus]` / `modify`→`info` 改动 `icon-[lucide--file-pen-line]` / `remove`→`destructive` 删除 `icon-[lucide--file-x]` / `indirect_refine`→`secondary` 间接完善 `icon-[lucide--file-cog]`；**图标已在 115-02 的 safelist**）；
- 锚定信息条（一行 mono 小字）：`change_type` 徽标 · 所属模块 · 所属仓 · `wave W` · `depends_on` chips；
- 卡内四分区：`how`（Block[]，常含 `pseudocode` 块）/ `existing_integration`（Block[]，**仅 `modify` / `indirect_refine` 且有值时渲染**，标题「与既有功能如何配合」）/ `files_touched`（紧凑表：`path`（`font-mono text-xs`）/ `action` 徽标 / `note`）/ `test_strategy`（Block[]）。四者中三个 Block[] 一律走 `BlueprintBlockList`。
`data-testid="blueprint-impl-item"`。

**② `ImplementationOverviewSection.vue`**：`props: { overview, repoNames, ...blockCtx }`。
三层：`requirement_narrative`（`BlueprintBlockList`）→ `modules[]`（卡片：`name` + 关联功能点 chips（点击 emit `goto-anchor('fp-'+id)`）+ 关联仓 chips + `narrative` 走 `BlueprintBlockList`）→ `items[]`（逐个 `ImplementationItemCard`）。
⭐ **波次泳道条**：段头一条 `wave 1 · 3 项 | wave 2 · 5 项 …`，点击**纯客户端筛选**该波次的实现项（本地 `ref`，⛔ **不改 URL**）；再次点击取消筛选。
`data-testid="blueprint-implementation-overview"`。

**③ `ApiContractCard.vue`**（UI-SPEC §6.6 逐行）：`props: { contract, repoName, supportRepoName, ...blockCtx }`。
- 卡头：`direction` 徽标（`provided`→`default` 本方案提供 / `consumed`→`secondary` 需要调用）+ `kind` 徽标（`http`/`rpc`/`event`/`mq` → `outline`，mono 小写）+ `method`（**单色 mono 徽标，`GET`/`POST`/`PUT`/`DELETE`/`PATCH` 不分色** —— `web/DESIGN.md` 禁彩虹）+ `path`（mono）+ 右侧仓名；
- `description` 走 `BlueprintBlockList`；
- 请求/响应示例：`JSON.stringify(v, null, 2)` 进 `<pre class="font-mono text-xs">`（**禁 `v-html`**），`lg` 以上双栏、以下上下堆叠；**超 20 行折叠为「展开全部」**；
- ⭐ **`data_source` 分区（`consumed` 专属）**：`from_service` / `from_api` / `fields_needed` chips + **`availability` 徽标**（`existing`→`success` 数据已有 / `needs_support`→`warning` 需 `{support_repository_name}` 支持产出，且该仓名可点跳 `/repositories/{support_repository_id}`）。⭐ **`availability` 与 `support_repository_id` 只从 `contract.data_source` 内读**，⛔ **不得回落读顶层**（113-05 决策：顶层零残留；回落读顶层会让后端「写错位置」这类缺陷在 UI 上被静默掩盖）；读不到 ⇒ 渲染 `t('knowledge.blueprints.api.availabilityUnknown')` =「未标注」。**这条要写进组件 docstring**；`data_source.notes` 走 `BlueprintBlockList`；
- 底部「消费方：mod_01, mod_02」chips。
`data-testid="blueprint-api-card"`。

**④ `ApiContractsSection.vue`**：`props: { contracts, repoNames, ...blockCtx }`；`lg:grid-cols-2` 卡片网格；空态 `CompactEmptyState`；段头 badge = 接口数（存在 `needs_support` 时 tone=warning 的判断由 115-06 的导航层做，本组件只出数与一个 `hasNeedsSupport` 派生值供页面用——通过 `defineExpose` 或由页面自算，**PLAN 取「由页面自算」**，本组件不 expose）。`data-testid="blueprint-api-contracts"`。

**⑤ `ImpactMatrixTable.vue`**：`props: { impact, repoNames, ...blockCtx }`。
- `business_impact`（Block[]）置顶，业务语言优先；
- `affected_features`：**矩阵表**（`~/components/ui/table`），列 = 功能 / 涉及仓库 / 影响类型 / 说明 / 证据；`kind` 徽标五档（`behavior_change`→`warning` 行为变更 / `perf`→`info` 性能 / `compat`→`warning` 兼容 / `data`→`destructive` 数据 / `none`→`muted` 无影响）；`description` 走 `BlueprintBlockList`；
- `regression_scope`：紧凑表，`level` 三档（`full`→`destructive` 全量回归 / `smoke`→`warning` 冒烟 / `none`→`muted` 无需回归）；
- `compat_risks` / `rollback_plan`：Block[] 走 `BlueprintBlockList`；
- `data_migrations`：条目列表，⭐ `reversible === false` 加 `<Badge variant="destructive">不可逆</Badge>`（⚠️ 判据是 `=== false` 不是 falsy —— 缺键时不加徽标）；
- ⭐ **窄屏（`< md`）矩阵表降级为卡片堆叠**（每行一卡，字段变成「标签: 值」），⛔ **不做横向滚动表**（用 `hidden md:table` + `md:hidden` 双份结构或等价实现）。
`data-testid="blueprint-impact-matrix"`。

**⑥ `ImpactAnalysisSection.vue`**：薄壳，`props: { impact, repoNames, ...blockCtx }`，渲染 `ImpactMatrixTable` + 空态。`data-testid="blueprint-impact-analysis"`。

**⑦ `InteractionFlowsSection.vue`**：`props: { flows, ...blockCtx }`；`emits: { 'goto-anchor', ...透传 }`。
每条 flow 一张卡：
1. 卡头：`name` + `trigger`（前缀图标写成**字面量完整类名** `class="icon-[lucide--mouse-pointer-click]"`；⭐ **字面量类名不需要 safelist**，Tailwind 的 content 扫描直接命中源码 —— 它确实不在 `main.css` 的 safelist 里，这**不是 115-02 的漏项**，⛔ 不要为此回报、⛔ 更不要自行追加 `main.css`。safelist 只服务**运行期拼接的裸名**（plan-checker N3））；
2. ⭐ **mermaid 图**：把 `flow.mermaid` 包装成一个 `type: 'mermaid'` 的合成 Block 交给 `BlueprintBlockList` 渲染（115-03 的 `BlueprintBlock` 已实现「prop 名 `code` + 调用方 `v-if` 判空 + `plainMermaid` 退化」三条契约）。⛔ **本 plan 不直接 import `MermaidDiagram`**。⚠️ 合成 Block 需要一个稳定 `block_id`：用 `flow.id` 派生（如 `flow-${flow.id}-mermaid`）并在 docstring 写明「这是**前端合成块**、后端不会往它上面挂线程 ⇒ `threads` 传空数组」。`flow.mermaid` 为空 ⇒ **不渲染该块**（只显示步骤表）；
3. **步骤表**：列 = `seq` / `actor` / `action` / `component` / `api_ref` / `data_in` / `data_out`；`actor` 徽标四档（`user`→`default` 用户 / `frontend`→`info` 前端 / `backend`→`secondary` 后端 / `service:*`→`outline` 显示服务名）；⭐ `api_ref` 渲染成 chip ⇒ **emit `goto-anchor('api-' + api_ref)`**；`note` 为 Block[] 作为该行的展开行（`Collapsible`）走 `BlueprintBlockList`；
4. `alternative_paths`：每条一个 `Collapsible`，标题 = `condition`，内容 = 同构步骤表。
`data-testid="blueprint-interaction-flows"`。
⚠️ **API 契约卡需要一个可被 `goto-anchor('api-<id>')` 命中的 DOM id** ⇒ `ApiContractCard` 根元素加 `:id="`api-${contract.id}`"`（Task 2 内一并落地）。
  </action>
  <verify>
    <automated>cd web && pnpm type-check && pnpm lint</automated>
  </verify>
  <acceptance_criteria>
- `cd web && pnpm type-check` 与 `pnpm lint` 通过。
- ⭐ **段组件零批注实现**：`rg -n "sliceBlockText|annotationClass|blueprint-annotation-mark|<mark" web/src/components/blueprint/sections/ web/src/components/blueprint/ImplementationItemCard.vue web/src/components/blueprint/ApiContractCard.vue web/src/components/blueprint/ImpactMatrixTable.vue` **零命中**。
- ⭐ **`availability` 只从 `data_source` 读**：`cd web && node -e "const s=require('fs').readFileSync('src/components/blueprint/ApiContractCard.vue','utf8'); const bad=/contract\.availability|contract\.support_repository_id|props\.contract\.availability/.test(s); if(bad) throw new Error('回落读了顶层 availability / support_repository_id（113-05 决策：顶层零残留）'); if(!/data_source/.test(s)) throw new Error('未从 data_source 读'); console.log('availability read-path OK')"` 输出 `availability read-path OK`。
- ⭐ **不直接引入 MermaidDiagram**：`rg -n "MermaidDiagram" web/src/components/blueprint/sections/InteractionFlowsSection.vue` **零命中**；`rg -n "mermaid" web/src/components/blueprint/sections/InteractionFlowsSection.vue` 命中（经合成 Block 交给 `BlueprintBlockList`）。
- ⭐ **`reversible === false` 严格判等**：`cd web && node -e "const s=require('fs').readFileSync('src/components/blueprint/ImpactMatrixTable.vue','utf8'); if(!/reversible\s*===\s*false/.test(s)) throw new Error('reversible 必须 === false 严格判等（缺键时不加不可逆徽标）'); console.log('reversible strict OK')"` 输出 `reversible strict OK`。
- ⭐ **窄屏不做横向滚动表**：`rg -n "overflow-x-auto|overflow-x-scroll" web/src/components/blueprint/ImpactMatrixTable.vue` **零命中**；`rg -n "md:hidden|hidden md:" web/src/components/blueprint/ImpactMatrixTable.vue` 命中（双份结构降级）。
- ⭐ **method 不分色**：`cd web && node -e "const s=require('fs').readFileSync('src/components/blueprint/ApiContractCard.vue','utf8'); if(/method[\s\S]{0,200}(GET[\s\S]{0,40}variant|POST[\s\S]{0,40}variant)/.test(s)) throw new Error('method 按动词分色了（DESIGN 禁彩虹）'); console.log('method mono OK')"` 输出 `method mono OK`。
- ⭐ **跨段跳转是 emit**：`rg -n "goto-anchor" web/src/components/blueprint/sections/InteractionFlowsSection.vue web/src/components/blueprint/sections/ImplementationOverviewSection.vue` 两文件各命中；`rg -n "window.scrollTo|scrollIntoView" web/src/components/blueprint/sections/ web/src/components/blueprint/ImplementationItemCard.vue web/src/components/blueprint/ApiContractCard.vue web/src/components/blueprint/ImpactMatrixTable.vue` **零命中**。
- ⭐ **`api-<id>` 锚点存在**：`rg -n ':id="`api-' web/src/components/blueprint/ApiContractCard.vue` 命中。
- **波次筛选不改 URL**：`rg -n "router.replace|router.push|useRoute" web/src/components/blueprint/sections/ImplementationOverviewSection.vue` **零命中**。
- **示例超 20 行折叠**：`rg -n "20" web/src/components/blueprint/ApiContractCard.vue` 命中且逻辑是行数阈值（人工核对，SUMMARY 登记）。
- ⛔ 零 `v-html`、零颜色字面量：`rg -n "v-html|hsl\(|#[0-9a-fA-F]{6}" web/src/components/blueprint/sections/ web/src/components/blueprint/ImplementationItemCard.vue web/src/components/blueprint/ApiContractCard.vue web/src/components/blueprint/ImpactMatrixTable.vue` **零命中**。
- ⛔ 零既有文件改动：`git diff --name-only web/src | rg -v "^web/src/components/blueprint/"` 输出**为空**。
  </acceptance_criteria>
  <done>中间四段与三张卡就位：实现项四档 change_type + 波次泳道纯客户端筛选；API 卡的 `availability` 严格只从 `data_source` 读且读不到渲染「未标注」；影响矩阵的窄屏降级是卡片堆叠不是横向滚动、`reversible` 严格判等；交互流程的 mermaid 经合成 Block 交给 `BlueprintBlockList`（不直接引入 `MermaidDiagram`）、`api_ref` chip 有可命中的 DOM 锚点。</done>
</task>

<task type="auto">
  <name>Task 3: 段 7–9（⭐ 验收锚点不接批注 / 决策记录 + deferred_ideas / ⭐ 关联段范围收窄）+ 两个组件测试（§20 断言 9）</name>
  <files>web/src/components/blueprint/sections/MustHavesSection.vue, web/src/components/blueprint/sections/DecisionLogSection.vue, web/src/components/blueprint/BlueprintAssociationsSection.vue, web/src/components/blueprint/__tests__/sections.spec.ts, web/src/components/blueprint/__tests__/mustHaves.spec.ts</files>
  <read_first>.planning/phases/115-ui/115-UI-SPEC.md §6.9（**验收锚点段的三子块表与四条关键约束逐字**，含「为什么必须渲染」那一段）、§6.1 的 content 14 键去向总表（`deferred_ideas` 挂 `decision_log` 段尾 Collapsible 的理由 / `execution_plan` 本相位不渲染的理由）、§10.2（关联区三块——⭐ 其中一块由本 plan 收窄）、§20（**断言 9 的两条变异说明**）；.planning/phases/115-ui/115-RESEARCH.md P-5（`getArtifactAssociations` 必然 404 的证据链）与 P-14（三段裸 array 的零约束 + `decision_log` 的 `emits` 语义待澄清 + 保 `answer` 键）；server/services/process_runtime/blueprint_schema.py L720-760（**三段可选裸 array 的确切声明**：`{"type": "array", "description": ...}`，无 `items`）与顶层 `required` 十一键（**确认三者都不在**）；.planning/STATE.md（114-04 关于 `decision_log` 物化「必须保 `answer` 键」的决策原文）；web/src/components/prompts/__tests__/PromptVersionDiff.test.ts（组件测试最规范范式）；web/src/pages/knowledge/__tests__/entity-detail.spec.ts（手写 i18n 键树 + stubs 范式）</read_first>
  <action>
**① `MustHavesSection.vue`（⭐ 全文唯一不走 `BlueprintBlockList` 的正文段）**：

`props: { mustHaves: { truths?: string[], artifacts?: Array<{path?, provides?}>, key_links?: Array<{from?, to?, via?}> } }` —— ⭐ **不收 `blockCtx`**。

⭐ **组件内注释必须写明原因**（§6.9 关键约束第二条要求）：「`iter_blocks`（`server/services/process_runtime/blueprint_schema.py:919-1036`）**不走查 `must_haves`** —— 它的三个数组是纯字符串与普通对象，**零 `block_id`** ⇒ 后端不会往这里挂线程。因此本段：不渲染 `<mark>`、选区 popover 在本段**不弹**、不参与 §9.2 的 block 级 diff（diff 视图中本段整段折叠）。**不复用 `BlueprintBlockList`**（它的契约前提是有 `block_id`），是全文唯一的例外。」

三块纵向排列（段内 `space-y-4`）：
- `truths[]`：`<ul class="space-y-1.5">`，每条前缀 `icon-[lucide--check] text-primary shrink-0 mt-0.5`，文字 `text-sm leading-relaxed`，**纯文本 mustache**；
- `artifacts[]`：紧凑表（`~/components/ui/table`）：列 = 产物路径 / 提供什么；`path` 用 `font-mono text-xs`、`provides` 用 `text-sm`；缺键渲染「—」；
- `key_links[]`：每条一行 `<from mono chip> → <to mono chip>`，箭头下方 `text-[11px] text-muted-foreground` 显示 `via`；窄屏（`< md`）改为竖排「起点 / 经由 / 终点」三行标签值。

⭐ **空态规则**：三个数组**同为空或整键缺失**（v0 旧数据无此键）⇒ **整段不渲染任何内容卡**（⛔ 不出空态卡，对齐 `deferred_ideas` 的处理）；仅**部分**子块为空时，该子块不渲染、其余照渲。⚠️ **段容器与导航项由 115-06 无条件渲染**（P-4）——本组件只决定「段内出不出内容」，docstring 写明这条分工。

`data-testid="blueprint-must-haves"`；三个子块各一个 `data-testid`（`blueprint-must-haves-truths` / `-artifacts` / `-key-links`）。

**② `DecisionLogSection.vue`**：

`props: { decisionLog?: unknown[], deferredIdeas?: unknown[] }` —— ⭐ **同样不收 `blockCtx`、不接批注层**（P-14：`decision_log` / `deferred_ideas` 也不在 `iter_blocks` 里）。组件内注释写明与 `MustHavesSection` 同款的原因。

⭐ **`emits: { 'open-thread': [threadId: string] }` 的语义由本 plan 定夺** = 「**跳转到该决策对应的线程**」（⛔ 不是「在该段发起批注」）。落地：条目上取 `item?.thread_id`，**存在时**才渲染一个「查看对应线程」按钮（`data-testid="blueprint-decision-goto-thread"`）emit 它；不存在则不渲染该按钮（⛔ 不渲染一个点了没反应的按钮）。**这条定夺要写进 docstring 与 SUMMARY**。

条目渲染（⭐ **逐项可选链，缺键渲染「—」，⛔ 不渲染 `undefined`、⛔ 不抛**）：`question` / ⭐ **`answer`（唯一有下游消费方的键，`blueprint_spec_gate._collect_prior_answers` 读它，STATE 有专条）** / `decision` / `decided_by` / `decided_at`（格式化，非法值原样显示）/ `applied_in_version`（有值时渲染成一个版本 chip）。条目形状**零 schema 约束** ⇒ 全程 `item?.x ?? '—'`。

⭐ **段尾 `deferred_ideas`**：一个**默认折叠**的 `Collapsible`，组头 `t('knowledge.blueprints.section.deferredIdeas', { n })` =「本方案明确不做的事（{n}）」；条目同样逐项可选链。`deferredIdeas` 为空 ⇒ **不渲染该 Collapsible**。

⛔ **`execution_plan` 本段不渲染**（CONTEXT `<deferred>` 已登记；理由写进组件 docstring：它是确认后确定性派生的执行计划，呈现面归属实施链路 116+，在只读评审面渲染会与 `TechPlanCard`（§13.2 禁区）职责重叠）。

`data-testid="blueprint-decision-log"`。

**③ `BlueprintAssociationsSection.vue`（⭐ SC-4 的范围收窄落点）**：

`props: { artifactId: string, citations: Record<string, Citation>, projectId: string | null, projectName?: string }`；`emits: { 'citation-click': [citationId: string] }`。

⭐ **组件 docstring 必须写明范围收窄与理由**：「本相位只兑现两块。原设计的『引用了本蓝图 / 关联知识』依赖 `knowledgeApi.getRelated` / `getArtifactAssociations`，实测它们查的是 `initiatives.Artifact` 投影的 KnowledgeEntity（`server/knowledge/artifact_associations.py:75`：`generate_entity_id(EntityKind.DOCUMENT, 'artifact', artifact_id)` 后 `KnowledgeEntity.objects.filter(id=...)`），而蓝图在 `delivery.Artifact` ⇒ 拿蓝图 id 去调**必然 404/空**。知识图谱物化明确是 **Phase 116**（ROADMAP:42）。⛔ 不靠 404 兜底糊过去。⭐ **该收窄已在 plan-checker BLOCKER-2 后完成对账**：ROADMAP Phase 115 的 **SC-4 原文已改写**为「蓝图引用的知识/仓库/其它蓝图可查（**反向「被谁引用」随 Phase 116 知识图谱物化交付**）」，REQUIREMENTS 的 **VIEW-04 已标 PARTIAL** 并写明顺延目标（Phase 116 SC-4「知识库反查『本蓝图被哪些方案/知识引用』可用」）。⛔ 执行期**不得**把 SC-4 再理解成「双向可查」——那个措辞已作废。同时仍在 115-05-SUMMARY 与 STATE 的 Pending Todos 登记。」

两块：
- **本蓝图引用了**：`citations` 引用池按 `source_type` **分组统计**（九档各一行「{类型名} {n}」）+ 每组下可点 chip（复用 115-03 的 `BlueprintCitationChip`，点击 emit `citation-click` → 页面开同一预览弹层）。引用池为空 ⇒ 该块不渲染。
- **关联项目**：`projectId` 非空时渲染项目名 + `RouterLink` 到项目页（路径按仓内既有项目路由实测取，⛔ 不猜）；为空则该块不渲染。
两块都为空 ⇒ 段内 `CompactEmptyState`（⭐ `icon` **裸名**）。
⛔ **零 `getRelated` / `getArtifactAssociations` 调用**。
`data-testid="blueprint-associations"`。

**④ 新建 `web/src/components/blueprint/__tests__/mustHaves.spec.ts`（⭐ §20 断言 9）**：
1. ⭐ **三子块都非空** ⇒ `[data-testid=blueprint-must-haves]` 存在；`truths` 条目数 == 数组长度；`artifacts` 表行数 == 数组长度；`key_links` 行数 == 数组长度；⭐ **该段内 `[data-testid=blueprint-annotation-mark]` 计数 == 0**（本段不接批注层）。**两个变异说明写进注释**：漏渲 `must_haves` ⇒ 第一条转红；给它接上批注层 ⇒ 最后一条转红。
2. **三块全空 / 整键缺失** ⇒ 组件不渲染任何内容卡（`wrapper.find('[data-testid=blueprint-must-haves]').exists() === false` 或内部三块均不存在——按实现选一种并注明）。
3. **部分子块为空**（只有 `truths`）⇒ `truths` 渲染、`artifacts` 与 `key_links` 不渲染（正反并列）。
4. **缺键条目**（`artifacts: [{}]`）⇒ 渲染「—」且**不抛**、文本**不含** `undefined`。
5. ⭐ **不复用 `BlueprintBlockList`**：`wrapper.findComponent({ name: 'BlueprintBlockList' }).exists() === false`。

**⑤ 新建 `web/src/components/blueprint/__tests__/sections.spec.ts`**（覆盖其余九段的关键分支，`stubs: { MermaidDiagram: true, BlueprintBlockList: true }` —— ⭐ stub `BlueprintBlockList` 后即可断言「段组件把 Block[] 交给了它」而不必渲染真实块）：
1. **每段透传 `blockCtx`**：参数化断言七个走 `BlueprintBlockList` 的段各自 `findAllComponents(BlueprintBlockList).length > 0` 且第一个的 `props()` 含 `threads` / `citations` / `readonly` / `activeThreadId` 四键。
2. ⭐ **段组件零批注**：`wrapper.html()` 不含 `blueprint-annotation-mark`（`BlueprintBlockList` 被 stub ⇒ 若段组件自己渲染了 mark 就会露出来）。
3. **`RepoAssociationCard`**：`role: 'direct'` / `'indirect'` 两档徽标文案不同；`fitness.verdict: 'unsuitable'` ⇒ `destructive` 徽标 + `reasons` 交给 `BlueprintBlockList`（**原样**，不做解析）；⭐ `RouterLink` 的 `to` 是 `/repositories/{id}`；`cross_team: true` ⇒ 「跨组协作」徽标；`confirmed_at_gate: false` ⇒ 「未经确认门锁定」徽标。
4. **`CurrentStateSection`**：finding 的 `citations: []` ⇒ 「缺引用」徽标出现；非空 ⇒ 不出现（正反并列）；点 `related_feature_points` chip ⇒ emit `goto-anchor` 载荷 `fp-<id>`。
5. ⭐ **`ApiContractCard` 的读取位置**：`data_source.availability: 'needs_support'` ⇒ warning 徽标 + 支持仓名可点；⭐ **顶层放一个 `availability: 'existing'` 而 `data_source` 内不放** ⇒ 渲染「未标注」（**⛔ 不得回落读顶层**，这条是 113-05 决策的 UI 侧证伪）。
6. **`ImpactMatrixTable`**：`reversible: false` ⇒ 「不可逆」徽标；`reversible` 缺键 ⇒ **不出现**该徽标（严格判等）。
7. **`InteractionFlowsSection`**：`mermaid` 为空 ⇒ 不渲染 mermaid 块（`BlueprintBlockList` 收到的 blocks 里无 mermaid 型）；非空 ⇒ 有；点 `api_ref` chip ⇒ emit `goto-anchor` 载荷 `api-<id>`。
8. **`DecisionLogSection`**：条目带 `thread_id` ⇒ 出现「查看对应线程」按钮且点击 emit `open-thread`；不带 ⇒ **不出现**该按钮；缺键条目渲染「—」不含 `undefined`；⭐ **`answer` 键被渲染**（断言文本含该值）；`deferredIdeas` 非空 ⇒ 出现折叠组头含条数，为空 ⇒ 不出现。
9. ⭐ **`BlueprintAssociationsSection` 零关联端点调用**：`vi.mock('~/api')` 后断言 `knowledgeApi.getRelated` 与 `getArtifactAssociations` 的 mock **`toHaveBeenCalledTimes(0)`**；引用池非空 ⇒ 按 `source_type` 分组统计正确；`projectId` 为空 ⇒ 「关联项目」块不渲染。
  </action>
  <verify>
    <automated>cd web && pnpm exec vitest run src/components/blueprint/__tests__ src/__tests__/blueprint-source-guard.spec.ts && pnpm type-check && pnpm lint</automated>
  </verify>
  <acceptance_criteria>
- `cd web && pnpm exec vitest run` **全绿**（本 plan 两个 spec + 115-02/03/04 的全部 spec + 既有 ~50 个，无新增失败）。
- `rg -c "it\(|test\(" web/src/components/blueprint/__tests__/mustHaves.spec.ts` ≥ **6**；`sections.spec.ts` ≥ **18**。
- ⭐ **§20 断言 9 的两半都在**：`cd web && node -e "const s=require('fs').readFileSync('src/components/blueprint/__tests__/mustHaves.spec.ts','utf8'); if(!/blueprint-must-haves/.test(s)) throw new Error('缺段存在性断言'); if(!/blueprint-annotation-mark/.test(s)) throw new Error('缺「段内 mark 计数 == 0」断言'); if(!/BlueprintBlockList/.test(s)) throw new Error('缺「不复用 BlueprintBlockList」断言'); console.log('assertion 9 OK')"` 输出 `assertion 9 OK`。
- ⭐ **`MustHavesSection` 不收 blockCtx**：`cd web && node -e "const s=require('fs').readFileSync('src/components/blueprint/sections/MustHavesSection.vue','utf8'); for (const k of ['threads','activeThreadId','citations']) if(new RegExp('props[\\\\s\\\\S]{0,300}'+k).test(s.slice(0, s.indexOf('</script>')))) throw new Error('MustHavesSection 收了 blockCtx 的 '+k); if(!/iter_blocks|block_id/.test(s)) throw new Error('缺「为什么不接批注层」的组件内注释'); console.log('must-haves isolation OK')"` 输出 `must-haves isolation OK`。
- ⭐ **`DecisionLogSection` 的 emit 语义已落地**：`rg -n "thread_id" web/src/components/blueprint/sections/DecisionLogSection.vue` 命中；`rg -n "open-thread" web/src/components/blueprint/sections/DecisionLogSection.vue` 命中；`rg -n "blueprint-decision-goto-thread" web/src/components/blueprint/sections/DecisionLogSection.vue` 命中。
- ⭐ **保 `answer` 键**：`rg -n "answer" web/src/components/blueprint/sections/DecisionLogSection.vue` 命中。
- ⭐ **三段裸 array 全可选链**：`cd web && node -e "const s=require('fs').readFileSync('src/components/blueprint/sections/DecisionLogSection.vue','utf8'); const tpl=s.slice(s.indexOf('<template>')); const risky=[...tpl.matchAll(/item\.([a-z_]+)/g)].filter(m=>!/\?\./.test(tpl.slice(Math.max(0,m.index-2), m.index+2))); if(risky.length) throw new Error('存在非可选链的裸访问: '+risky.slice(0,3).map(m=>m[0])); console.log('optional chaining OK')"` 输出 `optional chaining OK`（若实现改用 computed 归一化，改为核对归一化函数内逐项 `?? '—'` 并在 SUMMARY 登记）。
- ⭐ **关联段零端点调用（P-5）**：`rg -n "getRelated|getArtifactAssociations" web/src/components/blueprint/BlueprintAssociationsSection.vue` **零命中**；`sections.spec.ts` 含 `toHaveBeenCalledTimes(0)` 的断言。
- ⭐ **`execution_plan` 不渲染**：`cd web && node -e "const fs=require('fs'); const files=require('child_process').execSync('ls src/components/blueprint/sections').toString().trim().split('\n'); for(const f of files){const s=fs.readFileSync('src/components/blueprint/sections/'+f,'utf8'); const tpl=s.slice(s.indexOf('<template>')); if(/execution_plan/.test(tpl)) throw new Error('模板里渲染了 execution_plan: '+f);} console.log('no execution_plan render')"` 输出 `no execution_plan render`。
- ⭐ **`availability` 不回落顶层的 UI 侧证伪用例存在**：`sections.spec.ts` 含「顶层有 `availability` 而 `data_source` 内没有 ⇒ 渲染『未标注』」那一条（人工核对 + SUMMARY 登记用例名）。
- ⛔ 零 `v-html`、零颜色字面量、零 `refetchInterval`、零 `edit-block`：`rg -n "v-html|hsl\(|refetchInterval|edit-block" web/src/components/blueprint/sections/ web/src/components/blueprint/BlueprintAssociationsSection.vue` **零命中**。
- `pnpm exec vitest run src/__tests__/blueprint-source-guard.spec.ts` 绿；`pnpm lint` 与 `pnpm type-check` 通过。
- ⛔ 零既有文件改动：`git diff --name-only web/src | rg -v "^web/src/components/blueprint/"` 输出**为空**；`git diff --name-only server/` 输出**为空**。
  </acceptance_criteria>
  <done>两个「不接批注」的特例段各自落地且组件内写明原因（`must_haves` 是 required 键但无 `block_id`；`decision_log` / `deferred_ideas` 是零约束裸 array）；`decision_log` 的 `open-thread` 语义被定夺为「跳到对应线程」而非死码；三段裸 array 全程可选链、缺键渲染「—」、`answer` 键被保留；关联段的范围收窄有组件级注释与「零端点调用」用例；§20 断言 9 的两半（段被渲染 + 段内 mark 计数 0）各有能被变异逼红的用例。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| content 的可选/零约束字段 → 渲染 | `decision_log` / `deferred_ideas` 是零 items 约束的裸 array，运行期什么都有可能 |
| `data_source.availability` 的读取位置 → UI 结论 | 回落读顶层会让后端「写错位置」的缺陷被静默掩盖（113-05 决策的对称面） |
| 「关联」区的数据源 → SC-4 的兑现程度 | 用必然 404 的端点兜底 = 把范围收窄伪装成「暂时没数据」 |
| 段容器的条件渲染 → 左栏高亮 | 条件渲染会让 mount-only observer 永远观察不到（P-4，本 plan 只负责不越界） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115-40 | Tampering | 段组件自行实现批注（第二套划线逻辑）⇒ 与 `BlueprintBlock` 的坐标系/优先级分叉 | mitigate | `blockCtx` 原样透传 + 源码扫描断言（段组件内 `sliceBlockText` / `annotationClass` / `<mark>` 零命中）+ stub `BlueprintBlockList` 后「html 不含 mark」的用例 |
| T-115-41 | Tampering | 给 `must_haves` / `decision_log` 接上批注层 ⇒ 死码（后端不会往那里挂线程），且误导用户以为可以在此划线 | mitigate | 两段不收 `blockCtx`、不复用 `BlueprintBlockList`、组件内注释写明原因；§20 断言 9 的「段内 mark 计数 == 0」+「不复用 BlueprintBlockList」两条 |
| T-115-42 | Spoofing | `availability` 回落读顶层 ⇒ 后端写错位置时 UI 仍显示「数据已有」，评审据此放行 | mitigate | 只从 `data_source` 读 + 源码扫描禁 `contract.availability` + 「顶层有而 data_source 内无 ⇒ 未标注」的证伪用例 |
| T-115-43 | Information Disclosure | 关联区调必然 404 的端点并用兜底掩盖 ⇒ SC-4 的未兑现被伪装成「暂时没数据」 | mitigate | 零调用 + 组件 docstring 写明证据链与顺延目标 + `toHaveBeenCalledTimes(0)` 用例 + 在 115-02-SUMMARY 与 STATE Pending Todos 双处登记（P-5） |
| T-115-44 | Denial of Service | 零约束裸 array 的缺键访问 ⇒ 渲染 `undefined` 或直接抛，整段白屏 | mitigate | 全程可选链 + `?? '—'` + 「缺键条目不抛且不含 undefined」用例（P-14） |
| T-115-45 | Tampering | `reversible` 用 falsy 判断 ⇒ 缺键的迁移被误标「不可逆」 | mitigate | `=== false` 严格判等 + 源码扫描 + 正反用例 |
| T-115-46 | Information Disclosure | 现状分析的 finding 缺 `citations` 时隐藏质量信号 ⇒ 无证据的结论被当成有据 | mitigate | 「缺引用」`destructive` 徽标 + 正反用例（⛔ 不隐藏） |
| T-115-47 | Tampering | 段组件自行 `window.scrollTo` ⇒ 与 `AnchorNavLayout` 的 88px 偏移常量分叉，跨段跳转定位偏移 | mitigate | 一律 emit `goto-anchor` 给页面 + 源码扫描禁 `window.scrollTo` / `scrollIntoView` |
| T-115-48 | Tampering | 渲染 `execution_plan` ⇒ 与 `TechPlanCard`（§13.2 禁区）职责重叠，越过并行边界 | mitigate | 模板层零渲染 + 源码扫描断言 + 组件 docstring 写明理由 |
| T-115-SC | Tampering | 依赖供应链 | accept | 零新增依赖；mermaid 经 115-03 的 `BlueprintBlock` 间接使用（本 plan 不直接 import） |
</threat_model>

<verification>
- `cd web && pnpm exec vitest run` 全绿（本 plan 两个 spec + 115-02/03/04 全部 spec + 既有）。
- `cd web && pnpm type-check` 与 `pnpm lint` 通过。
- `rg -n "sliceBlockText|annotationClass|blueprint-annotation-mark|<mark" web/src/components/blueprint/sections/ web/src/components/blueprint/RepoAssociationCard.vue web/src/components/blueprint/ImplementationItemCard.vue web/src/components/blueprint/ApiContractCard.vue web/src/components/blueprint/ImpactMatrixTable.vue web/src/components/blueprint/BlueprintAssociationsSection.vue` 零命中。
- `rg -n "v-html|hsl\(|refetchInterval|edit-block|getRelated|getArtifactAssociations|MermaidDiagram" web/src/components/blueprint/sections/ web/src/components/blueprint/BlueprintAssociationsSection.vue` 零命中。
- `rg -n "window.scrollTo|scrollIntoView" web/src/components/blueprint/sections/` 零命中。
- `pnpm exec vitest run src/__tests__/blueprint-source-guard.spec.ts` 绿。
- `git diff --name-only web/src | rg -v "^web/src/components/blueprint/"` 输出为空；`git diff --name-only server/` 输出为空；`git diff web/package.json web/pnpm-lock.yaml` 输出为空。
- ⛔ 与 115-03 / 115-04 产物的文件交集核算：`git diff --name-only web/src/components/blueprint/` 的结果**不含** `BlueprintBlock.vue` / `BlueprintBlockList.vue` / `BlueprintCitationChip.vue` / `CitationPreviewDialog.vue` / `citation/**` / `annotationTokens.ts` / `BlueprintStatusBadge.vue` / `BlueprintThread*.vue` / `BlueprintFindingActions.vue` / `BlueprintSelectionPopover.vue` / `BlueprintReview*.vue` / `BlueprintRejectDialog.vue` / `BlueprintBlockedDialog.vue` / `BlueprintQualityPanel.vue` / `BlueprintVersionSwitcher.vue` / `BlueprintBlockDiff.vue`。
</verification>

<success_criteria>
- **SC-1 的结构化渲染主体完成**：十段全部有实现，交互流程走 mermaid、实现项含伪代码块、API 契约成卡、影响矩阵成表、仓库关联成卡且可直接跳仓库页（SC-3）。
- **批注层始终只有一个实现点**：14 个组件里零 `<mark>`、零 `sliceBlockText`、零 `annotationClass`，全部经 `blockCtx` 透传。
- ⭐ **两个特例段没有变成死码**：`must_haves`（required 键、无 `block_id`）与 `decision_log`/`deferred_ideas`（零约束裸 array）都不接批注层且组件内写明原因；`decision_log` 的 `open-thread` 被定夺为「跳到对应线程」并只在 `thread_id` 存在时渲染入口。
- ⭐ **SC-4 的范围收窄被显式呈现而不是靠 404 掩盖**：关联段只做「本蓝图引用了」+「关联项目」，零调用必然 404 的两个端点，理由与顺延目标写进组件 docstring 并已在 STATE 登记。
- **三处后端口径在 UI 侧有对称防线**：`availability` 只从 `data_source` 读（113-05）、`reversible` 严格判等、finding 缺 `citations` 亮质量信号。
- 零既有文件改动、零新增依赖、与同相位其它 plan 零文件交集。
</success_criteria>

<output>
完成后创建 `.planning/phases/115-ui/115-05-SUMMARY.md`，provides 至少包含：
- **十段组件的 props/emits 逐字表**（115-06 的页面按它接线），标注哪七段收 `blockCtx`、哪两段不收、`BlueprintAssociationsSection` 的四个 prop；
- **跨段跳转的锚点 id 约定**（`fp-<feature_point_id>` / `api-<contract_id>`）与 `goto-anchor` 的消费方（页面统一处理 88px 偏移）；
- ⭐ **`decision_log` 的 `open-thread` 语义定夺**（跳转到对应线程，`thread_id` 存在才渲染入口）；
- ⭐ **`must_haves` 段的四条约束落地情况**（不接批注 / 不复用 BlockList / 全空整段不出内容 / 段容器由页面无条件渲染）；
- ⭐ **SC-4 范围收窄的最终形态与证据链**（关联段两块 + 顺延 116），并确认已同步进 STATE 的 Pending Todos；
- `data-testid` 完整清单（本 plan 新增的 12+ 个）；
- **各段的空态规则表**（哪些段空了不渲染内容、哪些段出 `CompactEmptyState`）；
- ⭐ **UAT 清单**：mermaid 实渲、窄屏矩阵表的卡片堆叠、`lg:grid-cols-2` 断点、波次泳道筛选的交互手感、跨段跳转的滚动定位（需页面接线后才可测）；
- 若某个图标不在 115-02 的 safelist 而被回退替换，逐条登记。
</output>
