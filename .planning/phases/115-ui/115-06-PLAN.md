---
phase: 115-ui
plan: 06
type: execute
wave: 5
depends_on: ["115-03", "115-04", "115-05"]
files_modified:
  - web/src/components/blueprint/BlueprintViewerHeader.vue
  - web/src/components/blueprint/BlueprintSectionNav.vue
  - web/src/components/blueprint/BlueprintErrorState.vue
  - web/src/components/blueprint/BlueprintStageTimeline.vue
  - web/src/pages/knowledge/blueprints/[id].vue
  - web/src/components/common/FilterBar.vue
  - web/src/components/knowledge/BlueprintsTabPanel.vue
  - web/src/components/knowledge/BlueprintListCard.vue
  - web/src/components/project/warroom/ProjectBlueprintsCard.vue
  - web/src/pages/knowledge/index.vue
  - web/src/components/project/warroom/ProjectMaterialsPanel.vue
  - web/src/pages/knowledge/__tests__/blueprintViewer.spec.ts
  - web/src/pages/knowledge/__tests__/blueprintsTab.spec.ts
autonomous: true
requirements: [VIEW-01, VIEW-02, VIEW-03, VIEW-04, CLAR-01, FLOW-08]
prohibitions:
  - "🔒 **前端 CREATE-ONLY —— 本 plan 只允许改两个既有文件，且均为纯追加**（§0.1 五个追加点的 #1 与 #2）：`web/src/pages/knowledge/index.vue`（tab 列表 + 一个 `TabsContent`）与 `web/src/components/project/warroom/ProjectMaterialsPanel.vue`（一行 `defineAsyncComponent` import + 一处使用）。两者 `git diff <file> | rg \"^-[^-]\"` **必须为空**。⛔ 第三处既有文件修改 = 计划不通过"
  - "🔒 **零改动清单（`git diff` 必须为空）**：`TechPlanCard.vue` / `RoutingDecisionPanel.vue` / 执行时间线组件 / `components/delivery/ArtifactTimeline.vue` / `NodeDataTab.vue`（§13.2 的 0.19 归属面）；以及 `AnchorNavLayout.vue` / `CompactEmptyState.vue` / `PromptVersionDiff.vue` / `MermaidDiagram.vue` / `config/status.ts` / `api/client.ts` / `locales/zh-CN.json` / `styles/main.css` / `api/index.ts`（后三者已由 115-02 一次做完，⛔ 本 plan 不再碰；缺 key/图标 ⇒ **回报**）"
  - "⛔ ⭐ **十段的 `<section :id>` 容器无条件渲染**（P-4，本 plan 头号靶子）：⛔ **不写 `v-if=\"doc\"`**。`AnchorNavLayout` 在 `onMounted` 里按 `props.sections` 逐个 `getElementById`（`:19-40`），**没有** `watch(() => props.sections)`、没有 `MutationObserver` ⇒ 若段容器是条件渲染，mount 那一刻 DOM 里一个都没有 ⇒ observer 一个也挂不上 ⇒ **左栏高亮永远停在第一段，而点击跳转照常工作**（`scrollTo:47` 是点击时才查 DOM）—— 人肉自测只会觉得「高亮有点怪」，不会当成 bug。既有 `pages/knowledge/entities/[id].vue:149-153` 已踩过弱化版"
  - "⛔ ⭐ **`sections` 数组长度恒为 10**（对 UI-SPEC §6.9 的一处订正，理由同上）：`must_haves` / `decision_log` 即使无内容，**导航项与段容器仍渲染**，由段组件决定「段内出不出内容」（段高度可为 0）。⛔ 不做「按内容动态增删导航项」—— 那会让 `sections` 长度变化而新段永不被观察。**该订正必须写进页面 docstring 与 SUMMARY**"
  - "⛔ **`AnchorNavLayout` 的 badge 传 `''` 而不是 `0`**（P-18）：其空值判定（`:95`）是 `badge !== undefined && badge !== null && badge !== ''` ⇒ **`0` 会被渲染成一个灰色的 0**。零批注的段一律传 `''`"
  - "⛔ **`AnchorNavLayout` 由页面直接使用，不被任何新建组件包裹**（§5.1）：它是**整个两栏布局**（`<div class=\"flex gap-8\">` 包 `<aside class=\"hidden md:block w-48 shrink-0\">` + 默认 slot），`scrollTo` 是私有函数、零 emit、零 expose。⛔ **不得把它嵌进 `BlueprintSectionNav`** —— 那样正文将无处安放。第三栏在其默认 slot 内再开一层 `flex gap-6`"
  - "⛔ **页面标题用 `useHead` 不用 `definePage({ meta: { title } })`**（§4.1）：`layouts/default.vue:21-25` 把 `route.meta.title` **原样**渲染进 `<h1>`（不过 `t()`），传 i18n key 会让页面 H1 直接印出 `knowledge.blueprints.pageTitle`"
  - "⛔ ⭐ **404 只有一句中性文案**（§20 断言 4）：`t('knowledge.blueprints.error.notFoundOrForbidden')` =「无权访问或该蓝图不存在」。⛔ **不得**把 404 翻译成「该蓝图不存在」与「无权限」两句 —— 那就把后端 MJ-03 刻意不泄露存在性的闸门破了。404 时渲染全页 `CompactEmptyState`（`icon=\"lucide--lock\"` **裸名**）+「返回知识库」按钮放**默认 slot**，且**不渲染任何蓝图元信息**"
  - "⛔ ⭐ **`blueprint-gate/` 的非 200 不进错误分档**（§8.2 例外一 / P-10）：gate 链**至今没有项目范围闸**（`blueprint_gate_views._ablueprint_project_id` 只在一个 View 里用过一次），它的 404 混合了「门未开」「artifact 不存在」「无蓝图会话」三种语义 ⇒ **状态码不携带任何权限信息**。一律「gate 查询非 200 ⇒ 不渲染 gate 面板」，⛔ 不报错、不弹 toast、不写入任何错误态。页面的权限判定只由**四个主查询**（正文 / 快照 / threads / events）承担"
  - "⛔ ⭐ **`chunk-at` / `charter` 的失败不进错误分档**（§8.2 例外二）：一律走 115-03 的快照兜底；⛔ 不回显它们的 `detail`（错误体键是 `error`，`detail` 会回落成无意义的 `'请求失败'`）"
  - "⛔ **`answer` 端点恒 200，`reflow.status` 不得当作失败**：五档 toast 分档（`applied` / `unchanged` / `noop` / `conflict` / `failed|invalid`），⛔ 任何分支不渲染成红色错误态、不回滚 UI"
  - "⛔ **不做乐观更新**（MJ-01 第二点）：任何动作 2xx 后以响应体 `current_status` 为准 + `invalidateQueries({ queryKey: ['blueprint'] })`（**前缀匹配，⛔ 不用 UI-SPEC §3.7 的 `predicate` 写法** —— 仓内零先例，本页只有一个 artifact，全域失效无副作用）"
  - "⛔ **`refetchInterval` 字面量在本 plan 任何文件零命中**（§20 断言 6）：轮询一律经 115-02 的 `useBlueprintLive`；⛔ `edit-block` / `edit-blocks` / `editBlocks` 零命中（§20 断言 10）"
  - "⛔ **不改 `pages/knowledge/index.vue` 的 `normalizeTab` 实现、`?tab=` 双向同步 watcher、既有四个 tab 的任何一行**；追加的 `TabsTrigger` 走既有的内联 `as const` 数组（`:220-234`）"
  - "⛔ **`FilterBar.vue` 是通用件放 `components/common/`，不得引入任何蓝图专属 prop**（否则下一个页面复用不了，`web/DESIGN.md:198-211` 那份契约白写）：`props: { showClear?: boolean }` + `emits: ['clear']` + 默认 slot"
  - "⛔ **`ProjectBlueprintsCard` 的「无数据整块不渲染」是一个 `hide-when-empty` prop 由面板传入**（照 `ProjectMaterialsPanel.vue:52` 的 `HumanTaskInbox` 用法），⛔ **不是组件自决**"
  - "⛔ **禁 `v-html`**；⛔ 页面与新组件内零颜色字面量；⛔ 禁止在 `Badge` 上用 `:class` 追加颜色类；⛔ 不新增运行时依赖；后端零改动"
must_haves:
  truths:
    - "⭐ **查看器路由页可达**：`web/src/pages/knowledge/blueprints/[id].vue` → `/knowledge/blueprints/:id`（`:id` = artifact_id UUID，参数名取 `id` 对齐全仓 14 处 `[id].vue` 惯例）；`useRoute('/knowledge/blueprints/[id]')` 类型化；`useHead` 设标题（⛔ 不用 `definePage` 承载 i18n key）"
    - "⭐ **十段 `<section :id>` 容器无条件渲染**（P-4）：`sections` 数组长度**恒为 10**；骨架 / 实渲 / 空态三态全在段容器**内部**；⛔ 页面内 `v-if=\"doc\"` 不得包住任何 `<section :id>`"
    - "⭐ **`AnchorNavLayout` 由页面直接使用**，第三栏（`w-80` 线程侧栏）在其默认 slot 内再开一层 `flex gap-6`（左 `flex-1 min-w-0` 正文列、右 `hidden xl:flex w-80 shrink-0 sticky top-22`）；`< xl` 时右栏收成 `Sheet`；`< md` 时左栏由 `BlueprintSectionNav` 的 `Select` 承接。⛔ 不改 `AnchorNavLayout` 一行"
    - "⭐ **导航 badge 传 `''` 而非 `0`**（P-18）；`badgeTone` 规则：段内存在未决 BLOCKER → `danger`；存在 open 澄清 → `warning`；生成中 → `primary`；其余 → `muted`。⭐ **badge/tone 的计算提成可单测的纯函数或 computed**（`AnchorNavLayout` 在测试里被 stub，逻辑必须能脱离它验证）"
    - "⭐ **404 单一中性文案（§20 断言 4）**：四个主查询任一 404 ⇒ 全页 `CompactEmptyState`（`icon=\"lucide--lock\"` 裸名 + 按钮放默认 slot）+ **唯一一句** `error.notFoundOrForbidden`，且**不渲染任何蓝图元信息**；源码扫描断言页面内不存在第二个 404 文案键或中文竞品文案"
    - "⭐ **gate 非 200 不进分档**（§8.2 例外一）：只决定 `BlueprintGatePanel` 是否渲染（本 plan 预留插槽，面板本体归 115-07），⛔ 不报错、不弹 toast、不影响其余四个查询驱动的页面状态"
    - "⭐ **六个 query 参数与 ref 双向同步 + `normalize*` 兜底**（范式抄 `pages/knowledge/index.vue:43-60` 的 `router.replace({ query: { ...route.query, X: v } })` 展开写法，⭐ **其它 query 天然保留**）：`version` / `diff` / `diff_mode` / `section` / `thread` / `panel`"
    - "⭐ **`?thread=` 与 `?panel=` 一次性消费**：数据就绪后执行一次等效动作再 `router.replace` 移除该 query（刷新不重复触发）。`?panel=gate` 目标缺席 ⇒ **静默忽略**（不滚动、不报错、不 toast，仅移除 query）；`?panel=review` 目标缺席 ⇒ 滚动到正文顶部 + 一条 `info` toast `review.panelUnavailable` 后移除 query"
    - "⭐ **动作响应统一处理**（接线契约来自 115-04-SUMMARY）：approve 200 / **409 blocked → 开 `BlueprintBlockedDialog`** / 409 conflict → toast + 刷新；reject 200（含 `version_no` / `revision_round`）/ 409 → toast 带 `version_no` + 刷新；answer 恒 200 按 `reflow.status` 五档 toast；resolve/dismiss 200/noop/400；选区评论 200/400。**一律以响应体 `current_status` 为准 + `invalidateQueries({ queryKey: ['blueprint'] })`，⛔ 零乐观更新**"
    - "⭐ **实时进展只经 `useBlueprintLive`**：页面与本 plan 所有组件对 `refetchInterval` 零命中；生成中按段渲染骨架 + 进度文案（`sectionProgress`），已产出的段立即实渲（增量填充，⛔ 不做全页 loading）"
    - "⭐ **非 current 版本 = 只读**：正文顶部常驻 `Alert`「正在查看历史版本 v{n}，操作已禁用」+「回到当前版本」；此模式下**所有写动作组件不渲染**；`?diff=` 存在时进 diff 视图且**批注层与写动作全关**"
    - "⭐ **知识库 tab 纯追加（追加点 #1）**：`KnowledgeTab` 联合类型加 `'blueprints'`（`:40`）、`TABS` 数组加一项（`:41`）、`TabsTrigger` 内联 `as const` 数组加 `{ value: 'blueprints', icon: 'icon-[lucide--file-text]' }`（`:220-234`）、新增一个 `<TabsContent value=\"blueprints\">` 渲染 `<BlueprintsTabPanel />`。⛔ `normalizeTab` / `?tab=` watcher / 既有四 tab 一行不改"
    - "⭐ **列表深链直达查看器（SC-4）**：`BlueprintListCard` 整卡是 `RouterLink to=\"/knowledge/blueprints/{artifact_id}\"`；筛选与分页与 URL query 双向同步（`bp_status` / `project_id` / `repository_id` / `q` / `page`），刷新可复现；搜索走「输入值 / 已提交值分离」（仅回车或点按钮才提交）"
    - "⭐ **项目物料卡纯追加（追加点 #2）**：`ProjectMaterialsPanel` 追加一行 `defineAsyncComponent` import + 一处使用，`hide-when-empty` **由面板传入**；⭐ 建议**排在既有 `ArtifactTimeline` 之前**，且文案明确区分二者（P-17：蓝图与旧 technical_plan **共用 `artifact_type=\"technical_plan\"`**，新卡按 `blueprint_status != \"\"` 过滤 ⇒ 面板上会出现两块内容相近的区域）"
    - "⭐ **`FilterBar.vue` 按 `web/DESIGN.md:198-211` 的既有契约新建**（仓内零实现）：`props: { showClear?: boolean }` / `emits: ['clear']` / 默认 slot；容器 `.card` + `p-4` + `flex flex-wrap items-center gap-3`，清除按钮 `variant=\"ghost\" size=\"sm\"` 靠右。**通用件放 `common/`，零蓝图专属 prop**"
    - "全相位前端门：`cd web && pnpm exec vitest run` 全绿（含源码守卫 spec，此时扫描面已满）、`pnpm lint` 通过、`pnpm type-check` 通过、`pnpm build` 通过"
  artifacts:
    - path: "web/src/pages/knowledge/blueprints/[id].vue"
      provides: "查看器路由页：useHead / 六 query 双向同步与一次性消费 / AnchorNavLayout 直接使用 + 三栏装配 / ⭐ 十段无条件渲染 / 错误四档分档 / 六个动作端点的响应处理 / useBlueprintLive 接线 / gate 插槽预留"
      contains: "AnchorNavLayout"
    - path: "web/src/components/blueprint/BlueprintViewerHeader.vue"
      provides: "sticky 顶栏：标题 + 11 态徽标 + 三计数徽标（未决 BLOCKER / 待澄清 / 失锚）+ 版本切换器 + 已关闭批注开关 + 侧栏 toggle + 终审操作区（Separator 分隔）"
      contains: "blueprint-viewer-header"
    - path: "web/src/components/blueprint/BlueprintSectionNav.vue"
      provides: "⭐ 只负责 < md 的段跳转下拉（Select + 单 emit），容器 md:hidden sticky top-14 z-20；⛔ 不组合 AnchorNavLayout"
      contains: "md:hidden"
    - path: "web/src/components/blueprint/BlueprintErrorState.vue"
      provides: "错误四档呈现（404 中性单文案 / 409 / 400 回显 detail / 5xx 重试），props { status, detail }，emits ['retry']；CompactEmptyState 裸图标名 + 按钮放默认 slot"
      contains: "notFoundOrForbidden"
    - path: "web/src/components/blueprint/BlueprintStageTimeline.vue"
      provides: "八节点阶段时间线（spec_gate→route→repo_research→confirmation→repo_plan→merge→ai_review→pending_review）× 四态；节点可展开原始事件行；视觉范式对齐 IndexProgressTimeline"
      contains: "blueprint-stage-timeline"
    - path: "web/src/components/knowledge/BlueprintsTabPanel.vue"
      provides: "FilterBar + 列表网格 + Pagination；筛选与分页 ↔ URL query 双向同步；搜索输入/提交分离；空态 CompactEmptyState（裸名 lucide--file-x）"
      contains: "bp_status"
    - path: "web/src/components/project/warroom/ProjectBlueprintsCard.vue"
      provides: "项目侧只读蓝图卡（GET /delivery/blueprints/?project_id=）+ 11 态徽标 + 跳查看器；hide-when-empty 由面板传入；.flat-section/.flat-header 分区头范式"
      contains: "hide-when-empty"
    - path: "web/src/pages/knowledge/__tests__/blueprintViewer.spec.ts"
      provides: "§20 断言 4（404 单文案）+ 十段无条件渲染与 sections 长度恒 10 + badge 传 '' + gate 非 200 不报错 + 动作响应分档（409 blocked 开 Dialog / reflow 五档）"
      contains: "notFoundOrForbidden"
  key_links:
    - from: "web/src/pages/knowledge/blueprints/[id].vue"
      to: "web/src/components/layout/AnchorNavLayout.vue"
      via: "页面直接使用（不被包裹）+ sections 恒 10 项 + badge 传 '' + 段容器无条件渲染（P-4/P-18）"
      pattern: "AnchorNavLayout"
    - from: "web/src/pages/knowledge/blueprints/[id].vue"
      to: "web/src/composables/useBlueprintLive.ts"
      via: "唯一轮询消费点；页面零 refetchInterval 字面量"
      pattern: "useBlueprintLive"
    - from: "web/src/pages/knowledge/blueprints/[id].vue"
      to: "web/src/components/blueprint/BlueprintBlockedDialog.vue"
      via: "approve 409 blocked → 打开解药面板 → goto-thread → 设 activeThreadId + 滚动定位"
      pattern: "BlueprintBlockedDialog"
    - from: "web/src/pages/knowledge/index.vue"
      to: "web/src/components/knowledge/BlueprintsTabPanel.vue"
      via: "追加点 #1：TabsContent value=\"blueprints\"（纯追加，删除行 0）"
      pattern: "BlueprintsTabPanel"
    - from: "web/src/components/project/warroom/ProjectMaterialsPanel.vue"
      to: "web/src/components/project/warroom/ProjectBlueprintsCard.vue"
      via: "追加点 #2：defineAsyncComponent + hide-when-empty（纯追加，删除行 0）"
      pattern: "ProjectBlueprintsCard"
---

<objective>
把前四个 plan 的零件装配成**用户真正能打开的两个面**，并接通全部入口：

① **查看器路由页 `/knowledge/blueprints/:id`** —— 唯一权威渲染面。它承担四件页面级职责：三栏布局装配（`AnchorNavLayout` 直接使用 + 默认 slot 内再开一层三栏）、六个 query 的双向同步与一次性消费、错误四档分档（含 ⭐ **404 单一中性文案**与 ⭐ **gate 非 200 不进分档**两条特例）、以及**六个动作端点的响应处理**（含 approve 409 → 解药面板）。

② ⭐ **十段容器无条件渲染** —— 本 plan 的头号靶子，也是全相位「会静默假通过」清单里最隐蔽的一条（P-4）。`AnchorNavLayout` 只在 `onMounted` 那一刻按 `props.sections` 逐个 `getElementById`，既无 `watch` 也无 `MutationObserver`。若十段写成 `v-if="doc"`，异步数据到达时 observer **一个也没挂上** ⇒ 左栏高亮永远停在第一段，而**点击跳转照常工作** ⇒ 人肉自测只会觉得「高亮有点迟钝」。既有 `pages/knowledge/entities/[id].vue:149-153` 已经踩过弱化版。

③ **知识库「技术方案」tab + 项目物料卡（VIEW-03 / VIEW-04 / SC-4）** —— 两处**纯追加**接入既有宿主，加上一个按 `web/DESIGN.md` 既有契约补上的通用 `FilterBar`（契约写了但仓内零实现）。

④ **全相位前端门**：源码守卫 spec 此时扫描面已满，`refetchInterval` 单文件 / `edit-block` 零命中 / 404 文案单键三条断言在完整代码面下生效；另加 `pnpm build`（`vue-tsc -b && vite build`）作为最终把关。

Purpose: SC-1 / SC-2 / SC-3 / SC-4 / SC-5 在本 plan 之后**全部在界面上可达**——111–114 的十五个端点第一次有了人能点到的入口。
Output: 9 个新建文件（4 查看器骨架 + 1 页面 + 1 通用件 + 3 知识库/项目侧）+ 2 处纯追加 + 2 个测试文件。**零新增依赖、后端零改动。**
</objective>

<execution_context>
工作目录：worktree 根 `/Users/zaneliu/Projects/open-source/friday-ai/.claude/worktrees/v0.20-blueprint`。前端命令 `cd web && ...`，测试 `pnpm exec vitest run <path>`。

**wave 5，依赖 115-03 / 115-04 / 115-05**：以下**已存在**，⛔ 只 import 不修改——
- 115-02：`~/types/blueprint`、`~/api/blueprints`、`~/api/repositoryChunks`、`~/config/blueprintStatus`、`~/utils/blueprintBlocks`、`~/utils/blueprintAnnotations`、`~/stores/useBlueprintViewerStore`、`~/composables/{useBlueprintLive,useBlueprintAnnotations,useCitationPreview}`、`~/components/blueprint/{BlueprintStatusBadge.vue,annotationTokens.ts}`；
- 115-03：`BlueprintBlock.vue` / `BlueprintBlockList.vue` / `BlueprintCitationChip.vue` / `CitationPreviewDialog.vue` / `citation/**`；
- 115-04：`BlueprintThreadSidebar.vue` / `BlueprintThreadCard.vue` / `BlueprintThreadComposer.vue` / `BlueprintFindingActions.vue` / `BlueprintSelectionPopover.vue` / `BlueprintReviewActions.vue` / `BlueprintRejectDialog.vue` / `BlueprintBlockedDialog.vue` / `BlueprintQualityPanel.vue` / `BlueprintVersionSwitcher.vue` / `BlueprintBlockDiff.vue`；
- 115-05：`sections/**` 九件 + `BlueprintAssociationsSection.vue` + 四张卡。
⚠️ **各组件的 props/emits 逐字以对应 SUMMARY 为准**，⛔ 不得凭 UI-SPEC 猜（115-03 与 115-04 各有对 UI-SPEC 的扩写与订正）。

**115-07（确认门面板）尚未存在**：本 plan 在页面里**预留插槽**——一个 `gateSnapshot` query（`['blueprint','gate',artifactId]`）+ 一处「gate 面板挂载点」的注释锚点；⛔ 本 plan **不渲染** `BlueprintGatePanel`（那是 115-07 的所有权，它会在本页做一次纯追加）。gate 查询非 200 ⇒ 该挂载点不出任何东西且**不报错**。

**既有宿主与布局件契约（实测，不得猜测）**：
- `~/components/layout/AnchorNavLayout.vue`（110 行）：导出 `NavSection`（`{ id, label, icon?, badge?: string|number, badgeTone?: 'primary'|'success'|'warning'|'danger'|'muted' }`，`:4-10`）；结构 `<div class="flex gap-8">` 包 `<aside class="hidden md:block w-48 shrink-0">`（内含 `<nav class="sticky top-22">`）+ `<div class="flex-1 min-w-0 space-y-6"><slot /></div>`（`:71-110`）；⭐ `onMounted` 里 `props.sections.forEach(s => { const el = document.getElementById(s.id); if (el) observer?.observe(el) })`（`:19-40`），**无 `watch`**；`scrollTo` 是私有函数、偏移常量 `88`（`:46-53`）、零 emit 零 expose；`activeSection` 初值 `props.sections[0]?.id ?? ''`（`:16`）只取一次；⭐ badge 空值判定（`:95`）**不排除 `0`**；⭐ `NavSection.icon` 收**完整类名**（`:91` `:class="[section.icon, …]"`）——与 `CompactEmptyState`/`StatusBadge` 收裸名**不同，两者都对，不要统一**。
- `~/components/layout/PageContainer.vue`：页面容器。
- `~/components/common/CompactEmptyState.vue`（33 行）：`icon` 收**裸名**；**无 `action-label` prop、无 `@action` emit**，只有默认 slot。
- `useHead` from `@vueuse/head`（`pages/knowledge/entities/[id].vue:4,87` 的逐字用法）。
- `pages/knowledge/index.vue`（485 行）的**五处追加点**：`:40` `KnowledgeTab` 联合、`:41` `TABS` 数组、`:43-45` `normalizeTab`（**不改**）、`:51-60` `?tab=` 双向同步两个 watch（**不改**）、`:220-233` `TabsTrigger` 的内联 `as const` 数组（`{value, icon}`，标题走 `t('knowledge.tabs.' + tab.value)`）、`:236-434` 四个 `TabsContent`（加一个）。⭐ `router.replace({ query: { ...route.query, tab: v } })` 的展开写法**保留其它 query** ⇒ `bp_status` / `project_id` / `page` 天然共存。⚠️ 其 `:309,317` 的 `CompactEmptyState` 传了完整类名（**错的那一派**），⛔ 不照抄。
- `~/components/project/warroom/ProjectMaterialsPanel.vue`：分区流是**扁平堆叠**（`:44`）；懒加载四件走 `defineAsyncComponent`（`:27-30`）；⭐ 「无数据整块不渲染」是一个 **prop**（`:52` `<HumanTaskInbox :project-id="…" hide-when-empty />`）不是组件自决；分区头范式 `<section class="flat-section"><header class="flat-header"><span class="section-chip">…</span><h3>…</h3></header><div class="p-5">…</div></section>`（`:57-66`）；⭐ **`:76` 已渲染 `<ArtifactTimeline :space-id="project.space_id" artifact-type="technical_plan" />`**（P-17：蓝图与旧 technical_plan 共用同一 `artifact_type`，新卡会与它条目重叠 ⇒ 文案必须区分，建议新卡排在它**之前**）。
- `~/components/knowledge/KnowledgeDashboard.vue:801-808`：`<Pagination :page :total :items-per-page :sibling-count="1" show-edges @update:page>` 的用法范式。
- `pages/knowledge/index.vue:111-135`：「输入值 / 已提交值分离」（`queryInput` / `submittedQuery`，仅回车/点按钮才提交）的逐字范式。
- `~/components/repository/IndexProgressTimeline.vue`：阶段时间线的视觉范式（`.card` → `px-5 py-3.5` 头 → `p-5` 体 + 分组 + `StatusBadge`）。
- `~/components/ui/sheet`（窄屏线程抽屉）/ `select` / `pagination` / `alert` / `separator` / `switch` / `tooltip`。
- `~/composables/useToast`（toast 分档）/ `useErrorHandler`（⚠️ 本页错误分档自建，⛔ 不用它吞掉 404 语义）。

**测试范式**：`pages/knowledge/__tests__/entity-detail.spec.ts`（68 行，仓内唯一页面测试范式）：`vi.mock('vue-router', () => ({ useRoute: () => ({ params: { id: '…' } }) }))` + `vi.mock('~/api', ...)` + **手写最小 i18n 键树**（⛔ 不 import `zh-CN.json`）+ `mount(Page, { global: { plugins: [i18n, [VueQueryPlugin, { queryClient }]], stubs: { AnchorNavLayout: { template: '<div><slot /></div>' }, PageContainer: { template: '<div><slot /></div>' }, ... } } })` + `await new Promise(r => setTimeout(r, 50))`。
⚠️ ⭐ **`AnchorNavLayout` 在测试里被 stub** ⇒ 十段导航的 badge/tone 逻辑**必须能脱离它验证** ⇒ 把 `sections` 的计算提成页面内的 `computed` 并通过一个可测的纯函数或 `defineExpose` 暴露（**PLAN 取：提成 `web/src/utils/blueprintBlocks.ts` 之外的页面内 computed，并在测试里通过 `wrapper.findComponent(AnchorNavLayoutStub).props('sections')` 断言** —— stub 保留 props 传递即可，⛔ 不为此新建文件）。
</execution_context>

<context>
@.planning/phases/115-ui/115-02-SUMMARY.md（`useBlueprintLive` 的返回键与 `sectionProgress` 形状、`useBlueprintAnnotations` 与 `useCitationPreview` 的返回、i18n 键清单、四处 UI-SPEC 订正）
@.planning/phases/115-ui/115-03-SUMMARY.md（`BlueprintBlockList` 的 props/emits **含两处扩写**、DOM 契约与 `data-testid` 清单、选区侦测的 emit 载荷五键）
@.planning/phases/115-ui/115-04-SUMMARY.md（⭐ **接线契约**：11 个组件的 props/emits 逐字 + **六个动作端点的响应处理规则** + `reflow.status` 五档 toast 表 + `data-testid` 清单）
@.planning/phases/115-ui/115-05-SUMMARY.md（十段组件的 props/emits、跨段跳转锚点约定 `fp-<id>` / `api-<id>`、各段空态规则、`must_haves` 的四条约束落地）
@.planning/phases/115-ui/115-UI-SPEC.md §4（**路由契约：query 六键 + `?panel=` 缺席兜底两档 + 深链格式**逐字）、§5（**三栏骨架 ASCII + DOM 归属段 + 断点四档 + 滚动与粘性 + z-index 分层**逐字）、§6.1（**十段导航表 + badgeTone 规则**）、§8.1（**四态呈现表 + 21 事件 → 段 key 映射 + 段骨架形状**）、§8.2（**错误分档表逐行 + 例外一/二 + answer 的 reflow 纪律**）、§8.4（阶段时间线八节点）、§9.1（历史版本 Alert）、§12.1（**列表页布局 + FilterBar 契约 + 列表卡 ASCII**）、§12.2（项目物料卡四条）、§13.1/§13.2/§13.7（组件清单与职责）、§16（文案）、§18（a11y：焦点回归、Sheet 焦点陷阱、表格语义）、§20（断言 4/6/10 + `data-testid` 命名清单）
@.planning/phases/115-ui/115-RESEARCH.md（⭐ **P-4（`AnchorNavLayout` mount-only 观察）全文**、P-6（`CompactEmptyState` 两派写法）、P-8（事件 payload 键零 schema 保证 ⇒ 进度文案每键要有缺省）、P-9（轮询）、P-10（gate 无项目闸 ⇒ 前端不得据其 404 做权限推断）、P-17（物料面板条目重叠）、P-18（badge 的 0）；§B.7 tab 宿主的五处追加点表、§B.8 物料面板五条、§B.9 `AnchorNavLayout` 真实契约、§B.10 页面测试可达上界）
@.planning/phases/115-ui/115-PATTERNS.md（§C1 查看器路由页的 **五条「必须 DIFFER」**、§C2/C4/C5/C6 顶栏/时间线/段导航/错误态的 analog、§G7 FilterBar、§G8/G9 tab 面板与列表卡、§G10 项目物料卡的 DIFFER、§0.1 六处追加点表、§S8 追加面的纯追加纪律）
@web/src/components/layout/AnchorNavLayout.vue（**全文 110 行**：`NavSection` 接口、两栏结构、mount-only 观察、`scrollTo` 私有与偏移 88、badge 空值判定不排除 0、icon 收完整类名）
@web/src/pages/knowledge/entities/[id].vue（**全文 166 行**：类型化 route / `useHead` / `computed` queryKey / `is404` / 段容器三分支 / ⚠️ 其 `:105,107-108,149-153` 的三个已知缺陷**逐条不得照抄**）
@web/src/pages/knowledge/index.vue（**五处追加点的确切行** + `?tab=` 同步范式 + 输入/提交分离范式 + ⚠️ `:309,317` 的 `CompactEmptyState` 反例）
@web/src/components/project/warroom/ProjectMaterialsPanel.vue（**追加点 #2 的确切位置** + `hide-when-empty` prop 范式 + `.flat-section` 分区头 + ⚠️ `:76` 已有的 `ArtifactTimeline`）
@web/src/components/knowledge/KnowledgeDashboard.vue L780-830（筛选 + 网格 + `Pagination` 的用法范式）
@web/src/components/repository/IndexProgressTimeline.vue（阶段时间线视觉范式）
@web/src/pages/knowledge/__tests__/entity-detail.spec.ts（**全文 68 行**：页面测试唯一范式）
</context>

<tasks>

<task type="auto">
  <name>Task 1: 查看器骨架四件（Header / SectionNav / ErrorState / StageTimeline）</name>
  <files>web/src/components/blueprint/BlueprintViewerHeader.vue, web/src/components/blueprint/BlueprintSectionNav.vue, web/src/components/blueprint/BlueprintErrorState.vue, web/src/components/blueprint/BlueprintStageTimeline.vue</files>
  <read_first>.planning/phases/115-ui/115-UI-SPEC.md §5.1（**三栏 ASCII 图与顶栏内容**）、§5.3（顶栏 `sticky top-0 z-30`）、§8.2（**错误分档表逐行**，尤其 404 那一行的「渲染全页 CompactEmptyState + 不渲染任何蓝图元信息」）、§8.4（**阶段时间线的八个节点名与四态**）、§13.2（四件的 props/emits 契约表）、§16（顶栏文案：批注 {n} / 显示已关闭批注 / 侧栏折叠 aria-label）、§18.1（键盘导航）；.planning/phases/115-ui/115-04-SUMMARY.md（`BlueprintReviewActions` 与 `BlueprintVersionSwitcher` 的 props/emits —— 顶栏组合它们）；.planning/phases/115-ui/115-02-SUMMARY.md（`BlueprintStatusBadge` 的 props、`stageForEvent` 的签名——时间线按它聚合）；web/src/components/repository/IndexProgressTimeline.vue **全文**（阶段时间线的视觉范式：`.card` / `px-5 py-3.5` / `p-5` / 分组 / `StatusBadge` / 行内空态）；web/src/components/common/CompactEmptyState.vue **全文 33 行**（`icon` 裸名 + 只有默认 slot）；web/src/components/analytics/TimeRangeSelector.vue（`Select` + 单 emit 的 analog —— `BlueprintSectionNav` 照它）；web/src/pages/executions/components/ExecutionHeader.vue（顶栏 + 动作区 + `defineEmits<{}>` 的 analog）；web/src/components/layout/AnchorNavLayout.vue L4-10 与 L71-110（`NavSection` 接口与两栏结构 —— ⭐ 确认 `BlueprintSectionNav` **不能**包它）</read_first>
  <action>
**① `BlueprintErrorState.vue`**（先建，页面与其它组件都可能用）：
`props: { status: number, detail?: string }`；`emits: { retry: [] }`。按 §8.2 分档：
- ⭐ **404** ⇒ 全页 `CompactEmptyState`（**`icon="lucide--lock"` 裸名**）+ **唯一一句** `t('knowledge.blueprints.error.notFoundOrForbidden')` =「无权访问或该蓝图不存在」+「返回知识库」按钮**放默认 slot**（⛔ 该组件没有 `action-label` prop 也没有 `@action` emit，P-6）。⛔ **不渲染任何蓝图元信息**、⛔ **不得出现第二句 404 文案**（§20 断言 4）；
- **400** ⇒ 就近渲染，原样回显 `detail`（⛔ 不自行改写）；
- **5xx / 网络** ⇒ 全页 + 「重试」按钮 emit `retry` + 文案 `t('knowledge.blueprints.error.unavailable')`；
- **401/403** ⇒ 不处理（交由 `api/client.ts` 既有机制）。
`data-testid="blueprint-error-state"`。组件 docstring 写明「404 中性的理由」（后端 MJ-03 刻意不泄露存在性，前端多一句就把闸门破了）。

**② `BlueprintSectionNav.vue`**：
`props: { sections: NavSection[], activeId: string }`；`emits: { navigate: [sectionId: string] }`。
⭐ **只负责 `< md` 的段跳转下拉**：容器 `md:hidden sticky top-14 z-20`，内容是 `~/components/ui/select`（形状照 `TimeRangeSelector.vue`），选中即 emit `navigate`。
⛔ **不组合 `AnchorNavLayout`**（组件 docstring 逐字写明理由）：后者是**整个两栏页面布局**（`<div class="flex gap-8">` 包 `<aside>` + 默认 slot，`AnchorNavLayout.vue:72-109`），`scrollTo` 是私有函数、零 emit、零 expose —— 把它嵌进一个只收 `sections` 的组件里，**正文将无处安放**。`≥md` 的左栏由**页面**直接使用 `AnchorNavLayout` 承担。
`data-testid="blueprint-section-nav"`。

**③ `BlueprintViewerHeader.vue`**：
`props: { doc, snapshot, counts: { blocker: number, clarification: number, orphaned: number }, versions, currentVersionId, readonly, isLive, showClosed, sidebarCollapsed, currentStatus, revisionRound, submitting }`；
`emits: { 'toggle-sidebar': [], 'change-version': [versionId], 'open-diff': [baseVersionId], approve: [], reject: [], 'toggle-closed-annotations': [boolean] }`。
布局（§5.1 / §5.3）：`sticky top-0 z-30` 卡片条。左 → 右：
- 蓝图标题（`meta.title`，`text-2xl font-semibold`；⚠️ 与 `pages/knowledge/index.vue` 的 `text-2xl font-bold` 对齐时可沿用 `font-bold` —— §14 的唯一例外）；
- `<BlueprintStatusBadge :status="currentStatus" />`（115-02）；
- **三个计数徽标**：未决 BLOCKER / 待澄清 / 失锚。⭐ 计数为 0 时**不显示 0**（§16：「批注 {n}」在 `n === 0` 时作「批注」）；
- `<BlueprintVersionSwitcher>`（115-04）；
- `isLive === true` 时一个「生成中」指示（`animate-spin` 图标 + 状态文案）；
- 「显示已关闭批注」`Switch`（附 `text-xs text-muted-foreground` 一行说明）+ 侧栏折叠 toggle（`aria-label` 两态：「收起批注栏」/「展开批注栏」）+ 窄屏的「批注 {n}」按钮（唤起 `Sheet`；`aria-label` 作「查看批注，共 {n} 条」）；
- ⭐ **最右侧独立区块**：`~/components/ui/separator` + `ml-auto` + 容器 `pl-4 border-l border-border` 内放 `<BlueprintReviewActions>`（115-04，⛔ 本组件不重复实现它的 disabled/Tooltip 逻辑，原样透传 props/emits）。
`data-testid="blueprint-viewer-header"`。⛔ 零颜色字面量、⛔ 禁 `Badge` 上 `:class` 加色。

**④ `BlueprintStageTimeline.vue`**：
`props: { events: Array<{ id, event, payload, ts }>, currentStage: string, currentStatus: string }`。
按 §8.4：八个节点 `spec_gate` → `route` → `repo_research` → `confirmation` → `repo_plan` → `merge` → `ai_review` → `pending_review`，用 115-02 的 `stageForEvent(eventName)` 把事件聚合到节点；每节点四态（未开始 `muted` / 进行中 `info` + `animate-spin` / 完成 `success` / 失败 `destructive`）。
节点可展开（`~/components/ui/collapsible`）看该 stage 下的**原始事件行**：事件中文名（i18n）+ `ts` + payload 的**标量键值**（⚠️ `payload` 是自由 JSONField、**零 schema 保证**（P-8）⇒ 只渲染标量（string/number/boolean），对象与数组一律跳过或折叠显示键名，⛔ 不 `JSON.stringify` 整包糊上去、⛔ 不因缺键抛）。
⛔ **不呈现「人审驳回导致的会话复位」**（该动作当前无事件常量，114-REVIEW 可再议项，同步点 2 后与 0.19 的时间线契约一并定）—— 组件 docstring 写明这条。
视觉范式对齐 `IndexProgressTimeline.vue`。`data-testid="blueprint-stage-timeline"`。
  </action>
  <verify>
    <automated>cd web && pnpm type-check && pnpm lint</automated>
  </verify>
  <acceptance_criteria>
- `cd web && pnpm type-check` 与 `pnpm lint` 通过。
- ⭐ **404 单文案**：`cd web && node -e "const s=require('fs').readFileSync('src/components/blueprint/BlueprintErrorState.vue','utf8'); const keys=[...s.matchAll(/error\.([A-Za-z]+)/g)].map(m=>m[1]); const notfoundish=keys.filter(k=>/notFound|notExist|forbidden/i.test(k)); if(notfoundish.length!==1||notfoundish[0]!=='notFoundOrForbidden') throw new Error('404 分支必须只用 notFoundOrForbidden，实为: '+notfoundish.join(',')); if(/该蓝图不存在|无权限访问/.test(s)) throw new Error('出现中文竞品文案'); console.log('404 single copy OK')"` 输出 `404 single copy OK`。
- ⭐ **`CompactEmptyState` 用法正确（P-6）**：`rg -n 'icon="lucide--lock"' web/src/components/blueprint/BlueprintErrorState.vue` 命中；`rg -n 'icon="icon-\[|action-label|@action' web/src/components/blueprint/BlueprintErrorState.vue` **零命中**。
- ⭐ **`BlueprintSectionNav` 不包 `AnchorNavLayout`**：`rg -n "AnchorNavLayout" web/src/components/blueprint/BlueprintSectionNav.vue` **零命中**；`rg -n "md:hidden" web/src/components/blueprint/BlueprintSectionNav.vue` 命中。
- ⭐ **顶栏不重复实现终审逻辑**：`rg -n "BlueprintReviewActions" web/src/components/blueprint/BlueprintViewerHeader.vue` 命中；`rg -n "pending_review" web/src/components/blueprint/BlueprintViewerHeader.vue` **零命中**（可用性判断在 115-04 的组件里）。
- ⭐ **计数为 0 不显示 0**：`cd web && node -e "const s=require('fs').readFileSync('src/components/blueprint/BlueprintViewerHeader.vue','utf8'); if(!/>\s*0\s*\?|=== 0|> 0/.test(s)) throw new Error('缺「计数为 0 时不显示 0」的分支'); console.log('zero count OK')"` 输出 `zero count OK`。
- ⭐ **时间线 payload 只渲染标量**：`cd web && node -e "const s=require('fs').readFileSync('src/components/blueprint/BlueprintStageTimeline.vue','utf8'); if(/JSON.stringify\(\s*(e\.)?payload\s*\)/.test(s)) throw new Error('把整个 payload JSON 糊上去了（P-8）'); if(!/typeof/.test(s)) throw new Error('缺标量判定'); console.log('payload scalar-only OK')"` 输出 `payload scalar-only OK`。
- ⛔ 零 `v-html`、零颜色字面量、零 `refetchInterval`、零 `edit-block`：`rg -n "v-html|hsl\(|refetchInterval|edit-block" web/src/components/blueprint/BlueprintViewerHeader.vue web/src/components/blueprint/BlueprintSectionNav.vue web/src/components/blueprint/BlueprintErrorState.vue web/src/components/blueprint/BlueprintStageTimeline.vue` **零命中**。
- ⛔ 零既有文件改动：`git diff --name-only web/src` 此时只含 `web/src/components/blueprint/` 下的四个新文件。
  </acceptance_criteria>
  <done>四件查看器骨架就位：错误态的 404 只有一句中性文案且用对了 `CompactEmptyState` 的两条契约；段导航只管窄屏下拉、不越界包 `AnchorNavLayout`；顶栏组合既有组件不重复实现终审可用性逻辑、计数为 0 不显示 0；阶段时间线按 stage 聚合、payload 只渲染标量、不呈现无事件常量的会话复位。</done>
</task>

<task type="auto">
  <name>Task 2: 查看器路由页装配（⭐ 十段无条件渲染 / 三栏 / 六 query / 错误四档 / 六动作接线 / useBlueprintLive / gate 插槽预留）</name>
  <files>web/src/pages/knowledge/blueprints/[id].vue</files>
  <read_first>web/src/pages/knowledge/entities/[id].vue **全文 166 行**（⭐ 逐字级 analog，但有**五条必须 DIFFER**：① `:149-153` 的 `v-if="showAssociations"` 段容器条件渲染是 P-4 的实例，⛔ 不照抄；② `:105` 的 `icon="icon-[lucide--file-x]"` 传完整类名是 P-6 的错派，⛔ 不照抄；③ `:107-108` 的 `:action-label` 与 `@action` 是**不存在的 prop/emit**，⛔ 不照抄；④ `AnchorNavLayout` 由页面直接用（`:116` 的位置），⑤ badge 传 `''` 不传 `0`）；web/src/pages/knowledge/index.vue L40-60 与 L110-135（`normalizeTab` 兜底 + `?tab=` 双向同步两个 watch 的**确切写法**，⭐ `router.replace({ query: { ...route.query, X: v } })` 展开保留其它 query；输入/提交分离范式）；web/src/components/layout/AnchorNavLayout.vue **全文 110 行**（⭐ **mount-only 观察那一段逐行**；`NavSection` 的 `icon` 收**完整类名**；badge 空值判定不排除 `0`；偏移常量 88）；.planning/phases/115-ui/115-04-SUMMARY.md 的 ⭐ **接线契约节**（六个动作端点的响应处理规则 + `reflow.status` 五档 toast 表 + 11 个组件的 props/emits 逐字）；.planning/phases/115-ui/115-03-SUMMARY.md 与 115-05-SUMMARY.md（`BlueprintBlockList` 与十段组件的 props/emits、跨段锚点约定 `fp-<id>` / `api-<id>`）；.planning/phases/115-ui/115-02-SUMMARY.md（`useBlueprintLive` / `useBlueprintAnnotations` / `useCitationPreview` 的返回结构；`sectionProgress` 形状；`LIVE_BLUEPRINT_STATUSES`）；.planning/phases/115-ui/115-UI-SPEC.md §4.1（query 六键 + `?panel=` 缺席兜底两档表）、§5.1–§5.3（三栏 DOM 归属 / 断点四档 / 滚动粘性 / z-index）、§8.1（四态 + 段骨架形状）、§8.2（错误分档 + 两条例外）、§9.1（历史版本 Alert 与写动作全关）</read_first>
  <action>
新建 `web/src/pages/knowledge/blueprints/[id].vue`。

**① 页面 docstring（三段，必须写全）**：
- 「**唯一权威渲染面**：所有入口（知识库 tab / 项目物料卡 / 后续 chat 卡片）一律 `RouterLink` 深链跳到这里。⛔ 不做「全屏 Dialog 形态」的第二套实现 —— 那会让引用预览成为**嵌套 Dialog**（DESIGN §8.3 自承代码库无先例）；改成路由页后引用预览就是**第一层** `Dialog`，零新封装」；
- ⭐ 「**十段容器无条件渲染**（P-4）：`AnchorNavLayout` 只在 `onMounted` 按 `props.sections` 逐个 `getElementById`（`:19-40`），**无 `watch`、无 `MutationObserver`**。若段容器写成 `v-if="doc"`，mount 那一刻 DOM 里一个都没有 ⇒ observer 一个也挂不上 ⇒ **左栏高亮永远停在第一段，而点击跳转照常工作**（`scrollTo:47` 是点击时才查 DOM）—— 人肉自测只会觉得『高亮有点怪』。因此：**十个 `<section :id>` 恒渲染、`sections` 数组长度恒为 10**，骨架 / 实渲 / 空态三态全在段容器**内部**。⚠️ 这是对 UI-SPEC §6.9「`must_haves` 全空时整段与导航项都不渲染」的**一处显式订正**」；
- 「**并行纪律**：实时进展只经 `useBlueprintLive`（唯一轮询消费点）；本页 `refetchInterval` 字面量零命中；同步点 2 之后换 0.19 推送契约时只改那一个文件」。

**② 基础装配**：
`const route = useRoute('/knowledge/blueprints/[id]')`（⭐ 参数名取 `id`，对齐全仓 14 处 `[id].vue` 惯例）；`const artifactId = computed(() => String(route.params.id))`；`useHead({ title: computed(() => ...) })`（⛔ **不用 `definePage({ meta: { title } })`** —— `layouts/default.vue:21-25` 原样渲染 `route.meta.title` 不过 `t()`）。

**③ 六个 query ↔ ref 双向同步 + `normalize*` 兜底**（范式抄 `pages/knowledge/index.vue:43-60`）：`version`（uuid）/ `diff`（uuid）/ `diff_mode`（`inline` 默认 | `split`）/ `section`（段 key，`normalizeSection` 兜底到第一段）/ `thread`（uuid）/ `panel`（`gate` | `review`）。一律 `router.replace({ query: { ...route.query, X: v } })`（⭐ 展开写法**保留其它 query**）。

**④ 数据层**：
- `useBlueprintLive(artifactId)` → `{ isLive, events, stageTimeline, sectionProgress, currentStatus, doc, snapshot, refetchAll }`（⭐ 三个 live 查询在里面，页面**不再单独建** doc/snapshot/events 查询）；
- 另建三个非 live 查询：`threads`（`['blueprint','threads',artifactId]`）、`timeline`（`['blueprint','timeline',artifactId]`，走既有 `deliveryArtifacts.getArtifactTimeline`）、⭐ `gate`（`['blueprint','gate',artifactId]`，`retry: false`）；
- `useBlueprintAnnotations(...)` → `activeThreadId` 与分组、三个计数；`useCitationPreview()` → 预览弹层受控态；
- `readonly = computed(() => !isBlueprintEditable(currentStatus.value) || isHistoricalVersion.value || isDiffMode.value)`；
- `isHistoricalVersion = computed(() => !!versionQuery && doc.value && !doc.value.is_current)`。

**⑤ 错误分档（§8.2，本页最容易出偏差的一处）**：
- ⭐ **四个主查询**（doc / snapshot / threads / events）任一 `ApiError.status === 404` ⇒ 整页替换为 `<BlueprintErrorState :status="404" />`（**唯一一句中性文案**，⛔ 不渲染任何蓝图元信息）。判据逐字抄 `entities/[id].vue:93` 的 `err instanceof ApiError && err.status === 404`；
- **5xx / 网络** ⇒ 整页 `BlueprintErrorState` + 「重试」→ `refetchAll()`；
- **400** ⇒ 就近渲染原样回显 `detail`；
- ⭐ **gate 查询的任何非 200 ⇒ 只让 gate 挂载点不渲染**（§8.2 例外一 / P-10）。组件内注释逐字写明理由：「`blueprint-gate/` 链**至今没有项目范围闸**，它的 404 混合了『门未开』『artifact 不存在』『无蓝图会话』三种语义 ⇒ **状态码不携带任何权限信息**。页面的权限判定只由四个主查询承担」；
- ⭐ **`chunk-at` / `charter` 的失败不进分档**（例外二）：由 115-03 的预览子件内部兜底。

**⑥ 三栏装配（§5.1 DOM 归属逐字）**：
```
<PageContainer>
  <BlueprintViewerHeader ... />          ← sticky top-0 z-30
  <BlueprintSectionNav ... />            ← md:hidden
  <AnchorNavLayout :sections="sections"> ← ⭐ 页面直接使用，不被包裹
    <div class="flex gap-6">
      <div class="flex-1 min-w-0">       ← 正文列：StageTimeline + 十段 + gate 插槽 + QualityPanel
      <aside class="hidden xl:flex w-80 shrink-0 sticky top-22 max-h-[calc(100vh-6rem)]">
        <BlueprintThreadSidebar ... />   ← ScrollArea 内
```
`< xl` 时右栏收成 `~/components/ui/sheet`（`side="right"`，由顶栏「批注 {n}」按钮唤起；⭐ **关闭后焦点必须回到唤起它的按钮**，§18.1）。

**⑦ ⭐ `sections` 计算（恒 10 项）**：
`const sections = computed<NavSection[]>(() => [...])`，十项逐字照 UI-SPEC §6.1（key / 中文标题 `t('knowledge.blueprints.section.*')` / ⭐ **icon 传完整类名** `icon-[lucide--target]` 等——`NavSection.icon` 与 `CompactEmptyState`/`StatusBadge` 的裸名契约**不同，不要统一**）。
- ⭐ **badge 传 `''` 而非 `0`**（P-18：`AnchorNavLayout:95` 的空值判定不排除 `0`，会渲染一个灰色的 0）；
- `badgeTone`：段内存在未决 BLOCKER → `danger`；存在 open 澄清 → `warning`；生成中 → `primary`；其余 → `muted`；
- ⛔ **数组长度恒为 10**，⛔ 不按内容增删项。

**⑧ 十段渲染（⭐ 无条件容器）**：
十个 `<section :id="<段 key>" class="space-y-4" :data-testid="...">`，**每个都恒渲染**。段内三态：
- `docQuery.isLoading` ⇒ 该段骨架（形状按 §8.1 差异化：仓库关联/API 契约 2 张 `h-40 rounded-xl`；现状分析/实现概述 1 条 `h-5 w-40` + 3 条 `h-16`；影响范围 表头条 + 4 行 `h-9`；交互流程 1 张 `h-56` + 表头 + 3 行；验收锚点 3 条 `h-4` + 表头 + 2 行；决策记录/关联 3 条 `h-12`）；
- ⭐ **生成中**（`currentStatus ∈ LIVE_BLUEPRINT_STATUSES` 且该段在 content 中缺失/为空数组）⇒ **该段骨架 + 一行进度文案**（取 `sectionProgress[段key]`，⚠️ 插值键缺失时回落 `*Generic` 无参文案，P-8）；**已产出的段立即实渲**（增量填充，⛔ 不做全页 loading）；区域 `aria-busy="true"`、进度文案区 `aria-live="polite"`；
- 否则 ⇒ 渲染对应段组件（115-05），`blockCtx` 五个 prop 透传；
- 空且非生成中 ⇒ 由段组件出 `CompactEmptyState`（`must_haves` / `decision_log` 全空时段内不出内容，段容器高度可为 0 —— **容器本身仍在**）。

**⑨ 跨段跳转与滚动**（⭐ 页面统一处理，段组件只 emit）：监听各段的 `goto-anchor(domId)` ⇒ `document.getElementById(domId)` + `window.scrollTo({ top: el.offsetTop - 88, behavior: 'smooth' })`（⭐ **偏移常量 88 与 `AnchorNavLayout.scrollTo:50` 一致，⛔ 不另设**）+ 2s ring 高亮（`animate-fade-in` + 一个临时 class）。`?section=` 深链同款处理一次。

**⑩ `?thread=` 与 `?panel=` 的一次性消费**：
- `?thread=`：线程数据就绪后执行一次「点击侧栏条目」的等效动作（设 `activeThreadId` + 侧栏滚动 + 正文滚动定位；窄屏自动展开 `Sheet`），随后 `router.replace` **移除该 query**；
- `?panel=gate`：gate 查询 200 ⇒ 滚动到 gate 挂载点 + 2s ring；⭐ **非 200 ⇒ 静默忽略**（不滚动、不报错、不 toast，仅移除 query）—— 确认门未开启是绝对多数的正常态；
- `?panel=review`：`currentStatus ∈ {pending_review, confirmed}` ⇒ 滚动到 `BlueprintQualityPanel` + 2s ring；否则 ⇒ 滚动到正文顶部 + 一条 **info** toast `t('knowledge.blueprints.review.panelUnavailable')` 后移除 query。

**⑪ ⭐ 六个动作端点的接线（响应处理规则逐字照 115-04-SUMMARY 的接线契约）**：
- **approve**：200 ⇒ 成功 toast「已通过，蓝图进入『已确认』」+ 以响应体 `current_status` 为准 + `invalidateQueries({ queryKey: ['blueprint'] })`；⭐ **409 且 `blocked`** ⇒ 打开 `<BlueprintBlockedDialog :thread-ids="body.unresolved_blocker_thread_ids" :threads="threads">`，其 `goto-thread` ⇒ 关 Dialog + 打开侧栏 + 设 `activeThreadId` + 正文滚动定位；409 `conflict` ⇒ `destructive` toast + 回显 `detail` +「刷新重试」；400 ⇒ 原样回显 `detail`；
- **reject**：由 `BlueprintRejectDialog` 提交 ⇒ 200 成功 toast「已驳回，蓝图回到『产出中』（第 {revision_round} 轮修订）」+ invalidate；409 ⇒ toast「当前版本已到 v{version_no}，请刷新后重试」+ 自动 invalidate；
- **answer**：⭐ **恒 200**，按 `reflow.status` 五档 toast（`applied` 成功「答案已回灌，已产出 v{version_no}」+ 重取正文 / `unchanged`·`noop` info「答案已记录，本次未产生新版本」/ `conflict` warning + 列出 `conflict_block_ids` / `failed`·`invalid` warning「答案已保存，回灌未成功，可稍后重试」）。⛔ **任何分支都不当作失败**；
- **resolve / dismiss**：200 或 `noop` ⇒ 成功/幂等 toast + invalidate；400 ⇒ 回显 `detail`；
- **选区评论**：`BlueprintBlockList` 的 `selection-comment` ⇒ 渲染 `<BlueprintSelectionPopover :rect :can-comment="!readonly">`；「发起评论」⇒ 侧栏插入草稿卡 ⇒ 提交走 `POST threads/` ⇒ 200 invalidate / 400 回显；`cross-block-selection` ⇒ 一条 info toast「评论只能针对同一段落内的文字，请缩小选区」；
- ⛔ **全部动作零乐观更新**；⛔ invalidate 一律**前缀匹配** `{ queryKey: ['blueprint'] }`（不用 `predicate`）。

**⑫ 版本 / diff / 只读 Alert**：
- `?version=` 非 current ⇒ 正文顶部常驻 `~/components/ui/alert`「正在查看历史版本 v{n}，操作已禁用」+「回到当前版本」；此模式下**所有写动作组件不渲染**；
- `?diff=` 存在 ⇒ 正文区替换为 `<BlueprintBlockDiff :base-doc :target-doc :mode>`（需再取一份基线版本的 doc，`queryKey` 带 `diff` 的 version_id）；⭐ **diff 模式下批注层与所有写动作关闭**；
- `readonly === true`（不可编辑白名单外）⇒ 正文顶部常驻 `Alert`「已确认的蓝图不可直接改写，要改请先驳回」。

**⑬ gate 插槽预留**：在正文列 `BlueprintQualityPanel` **之前**放一个注释锚点 `<!-- gate-panel-mount: 115-07 在此挂 BlueprintGatePanel，条件 = gateQuery 成功且 status 200 -->` 与一个 `v-if="gateAvailable"` 的空容器 `<div id="gate" data-testid="blueprint-gate-mount" />`。⛔ 本 plan **不渲染** `BlueprintGatePanel`（115-07 的所有权）。

**⑭ `BlueprintQualityPanel`**：`currentStatus ∈ {pending_review, confirmed}` 时渲染于正文尾部（115-04 的组件），`hasKeyConclusions` 由页面派生（`current_state_analysis` / `repo_associations` / `impact_analysis` 三处**是否全空**）。
  </action>
  <verify>
    <automated>cd web && pnpm type-check && pnpm lint && pnpm exec vitest run src/__tests__/blueprint-source-guard.spec.ts</automated>
  </verify>
  <acceptance_criteria>
- `cd web && pnpm type-check` 与 `pnpm lint` 通过。
- ⭐ **P-4：段容器无条件渲染（本 plan 头号靶子）**：`cd web && node -e "const s=require('fs').readFileSync('src/pages/knowledge/blueprints/[id].vue','utf8'); const tpl=s.slice(s.indexOf('<template>')); const secs=[...tpl.matchAll(/<section[^>]*\bid=\"?[^\"> ]+/g)]; if(secs.length<10) throw new Error('段容器少于 10 个: '+secs.length); for(const m of secs){ const tag=tpl.slice(m.index, tpl.indexOf('>', m.index)); if(/v-if|v-show/.test(tag)) throw new Error('段容器上有条件渲染（P-4）: '+tag.slice(0,120)); } console.log('10 unconditional sections OK')"` 输出 `10 unconditional sections OK`。
- ⭐ **`sections` 恒 10 项**：`cd web && node -e "const s=require('fs').readFileSync('src/pages/knowledge/blueprints/[id].vue','utf8'); const i=s.indexOf('const sections'); const seg=s.slice(i, i+3000); const ids=[...seg.matchAll(/id:\s*'([a-z_]+)'/g)].map(m=>m[1]); if(ids.length!==10) throw new Error('sections 不是 10 项: '+ids.join(',')); if(/\.filter\(|\?\s*\[|&&\s*\{/.test(seg)) throw new Error('sections 存在条件增删'); console.log('sections fixed 10 OK', ids.join(','))"` 输出含 `sections fixed 10 OK`。
- ⭐ **badge 传 `''` 不传 `0`（P-18）**：`cd web && node -e "const s=require('fs').readFileSync('src/pages/knowledge/blueprints/[id].vue','utf8'); const i=s.indexOf('const sections'); const seg=s.slice(i, i+3000); if(/badge:\s*\w+Count\b(?!\s*\|\|)/.test(seg) && !/\?\s*''|\|\|\s*''|: ''/.test(seg)) throw new Error('badge 可能直接传了数字 0'); console.log('badge empty-string OK')"` 输出 `badge empty-string OK`（并在 SUMMARY 逐条登记十项 badge 的取值表达式）。
- ⭐ **`AnchorNavLayout` 由页面直接使用**：`rg -n "AnchorNavLayout" web/src/pages/knowledge/blueprints/\[id\].vue` 命中；`rg -n "AnchorNavLayout" web/src/components/blueprint/` **零命中**（⛔ 任何组件都不得包它）。
- ⭐ **`useHead` 而非 `definePage` 承载标题**：`rg -n "useHead" web/src/pages/knowledge/blueprints/\[id\].vue` 命中；`rg -n "definePage" web/src/pages/knowledge/blueprints/\[id\].vue` 若命中则其 `meta` **不含 `title`**（人工核对）。
- ⭐ **404 单一分档**：`cd web && node -e "const s=require('fs').readFileSync('src/pages/knowledge/blueprints/[id].vue','utf8'); const keys=[...s.matchAll(/blueprints\.error\.([A-Za-z]+)/g)].map(m=>m[1]); const nf=keys.filter(k=>/notFound|notExist|forbidden/i.test(k)); if(nf.length && (nf.length>1 || nf[0]!=='notFoundOrForbidden')) throw new Error('404 文案不唯一: '+nf.join(',')); if(/该蓝图不存在|无权限访问/.test(s)) throw new Error('中文竞品文案'); console.log('page 404 single OK')"` 输出 `page 404 single OK`。
- ⭐ **gate 非 200 不进分档（P-10）**：`cd web && node -e "const s=require('fs').readFileSync('src/pages/knowledge/blueprints/[id].vue','utf8'); const i=s.indexOf('gate'); if(!/gate/.test(s)) throw new Error('缺 gate 查询'); if(/gate[\s\S]{0,300}(BlueprintErrorState|toast)/.test(s)) throw new Error('gate 失败进了错误分档或弹了 toast（§8.2 例外一）'); console.log('gate exception OK')"` 输出 `gate exception OK`。
- ⭐ **零 `refetchInterval`（§20 断言 6）**：`rg -n "refetchInterval" web/src/pages/knowledge/blueprints/` **零命中**；`rg -n "useBlueprintLive" web/src/pages/knowledge/blueprints/\[id\].vue` 命中；`pnpm exec vitest run src/__tests__/blueprint-source-guard.spec.ts` 绿。
- ⭐ **零 `edit-block`（§20 断言 10）**：`rg -n "edit-block|edit-blocks|editBlocks" web/src/pages/knowledge/blueprints/` **零命中**。
- ⭐ **零乐观更新 + 前缀 invalidate**：`rg -n "invalidateQueries" web/src/pages/knowledge/blueprints/\[id\].vue` 命中且形态是 `{ queryKey: ['blueprint'] }`；`rg -n "predicate" web/src/pages/knowledge/blueprints/\[id\].vue` **零命中**；`rg -n "setQueryData" web/src/pages/knowledge/blueprints/\[id\].vue` **零命中**（⛔ 不写本地推断状态）。
- ⭐ **偏移常量 88 与既有一致**：`rg -n "88" web/src/pages/knowledge/blueprints/\[id\].vue` 命中且用于 `scrollTo` 偏移（人工核对）。
- ⭐ **409 blocked 接线**：`rg -n "BlueprintBlockedDialog|unresolved_blocker_thread_ids" web/src/pages/knowledge/blueprints/\[id\].vue` 两条命中。
- ⭐ **`reflow` 五档**：`cd web && node -e "const s=require('fs').readFileSync('src/pages/knowledge/blueprints/[id].vue','utf8'); for (const k of ['applied','unchanged','noop','conflict']) if(!s.includes(k)) throw new Error('reflow 分档缺: '+k); console.log('reflow five-branch OK')"` 输出 `reflow five-branch OK`。
- ⭐ **gate 插槽预留而不渲染**：`rg -n "BlueprintGatePanel" web/src/pages/knowledge/blueprints/\[id\].vue` **零命中**；`rg -n "blueprint-gate-mount" web/src/pages/knowledge/blueprints/\[id\].vue` 命中。
- ⛔ 零 `v-html`、零颜色字面量：`rg -n "v-html|hsl\(" web/src/pages/knowledge/blueprints/` **零命中**。
- ⛔ 零既有文件改动（本 Task 阶段）：`git diff --name-only web/src | rg -v "^web/src/(components/blueprint|pages/knowledge/blueprints)/"` 输出**为空**。
  </acceptance_criteria>
  <done>查看器路由页可跑：十段容器无条件渲染且 `sections` 恒 10 项（P-4 被物理堵死）、`AnchorNavLayout` 由页面直接用、badge 传空串、三栏与断点按 §5 装配；404 只有一句中性文案、gate 非 200 只影响面板渲染、`chunk-at`/`charter` 不进分档；六个动作端点的响应按接线契约处理（含 409 blocked 开解药面板与 `reflow` 五档），零乐观更新、前缀 invalidate、零 `refetchInterval`。</done>
</task>

<task type="auto">
  <name>Task 3: 知识库 tab 四件 + 两处纯追加点 + 两个页面测试 + 全相位前端门</name>
  <files>web/src/components/common/FilterBar.vue, web/src/components/knowledge/BlueprintsTabPanel.vue, web/src/components/knowledge/BlueprintListCard.vue, web/src/components/project/warroom/ProjectBlueprintsCard.vue, web/src/pages/knowledge/index.vue, web/src/components/project/warroom/ProjectMaterialsPanel.vue, web/src/pages/knowledge/__tests__/blueprintViewer.spec.ts, web/src/pages/knowledge/__tests__/blueprintsTab.spec.ts</files>
  <read_first>web/DESIGN.md L190-215（⭐ **`FilterBar` 的既有契约逐字**：`props: { showClear?: boolean }` / `emits: ['clear']` / 默认 slot / 容器规格 / 清除按钮样式 —— 仓内零实现，本 plan 按它补上）；web/src/pages/knowledge/index.vue **L38-62 与 L215-240 与 L300-330**（⭐ **五处追加点的确切行**：`KnowledgeTab` 联合 `:40` / `TABS` `:41` / `normalizeTab` `:43-45`（**不改**）/ `?tab=` watch `:51-60`（**不改**）/ `TabsTrigger` 内联 `as const` 数组 `:220-234` / `TabsContent` 群；以及 ⚠️ `:309,317` 的 `CompactEmptyState` 反例）；web/src/components/project/warroom/ProjectMaterialsPanel.vue **全文**（⭐ `defineAsyncComponent` 四行 `:27-30`、`hide-when-empty` 用法 `:52`、`.flat-section`/`.flat-header` 分区头 `:57-66`、⚠️ **`:76` 已有的 `<ArtifactTimeline artifact-type="technical_plan">`**）；web/src/components/knowledge/KnowledgeDashboard.vue L780-830（筛选 + 网格 + `Pagination` 的用法）；web/src/components/knowledge/DeliveryDocsTree.vue（条目卡 + `.card .card-interactive` 的 analog）；web/src/pages/knowledge/__tests__/entity-detail.spec.ts **全文 68 行**（页面测试唯一范式）；.planning/phases/115-ui/115-01-SUMMARY.md（列表端点的 query 参数与**五键分页体** + `current_status` 键）；.planning/phases/115-ui/115-UI-SPEC.md §12.1（列表页布局 + 列表卡 ASCII）、§12.2（项目物料卡四条）、§4.2（tab 深链与追加内容逐字）</read_first>
  <action>
**① `web/src/components/common/FilterBar.vue`**（按 `web/DESIGN.md:198-211` 的既有契约新建，仓内零实现）：
`props: { showClear?: boolean }`；`emits: { clear: [] }`；**默认 slot 承载筛选控件**。容器 `.card` + `p-4` + `flex flex-wrap items-center gap-3`；清除按钮 `variant="ghost" size="sm"` 靠右，仅 `showClear` 为真时渲染。
⛔ **通用件，零蓝图专属 prop**（否则下一个页面复用不了，DESIGN 里那份契约白写）。docstring 写明「契约来源：`web/DESIGN.md:198-211`（写了但仓内零实现，本相位按契约补上）」。

**② `BlueprintListCard.vue`**：`props: { item: BlueprintListItem }`。
整卡是 `RouterLink to="/knowledge/blueprints/{item.artifact_id}"`（`.card .card-interactive`），⭐ **SC-4 的深链直达**。内容照 §12.1 的 ASCII：
`<BlueprintStatusBadge :status="item.current_status" />`（⭐ **键是 `current_status`**，115-01 订正）+ 标题（2 行截断）+ 摘要（`text-xs muted`，clamp 2 行）+ 「项目名 · v{n} · 第 {r} 轮修订」+ 仓库 chip ×≤3 与 `+{n}` + 「💬 {thread_count}」+ ⭐ **`unresolved_blocker_count > 0` 时红色显示，`=== 0` 时不渲染** + 「更新于 {相对时间}」。
`data-testid="blueprint-list-card"`。

**③ `BlueprintsTabPanel.vue`**：
布局：`FilterBar`（内含搜索框 `q` / 状态 `Select`（12 态 + 全部）/ 项目 `Select` / 仓库 `Select`）→ `BlueprintListCard × N`（`grid gap-3 sm:grid-cols-2 lg:grid-cols-3`）→ `Pagination`（用法抄 `KnowledgeDashboard.vue:801-808`）。
- ⭐ **搜索走「输入值 / 已提交值分离」**（抄 `pages/knowledge/index.vue:111-135` 的 `queryInput` / `submittedQuery`）：**仅回车或点按钮才提交**（避免输入即请求）；
- ⭐ **所有筛选与分页 ↔ URL query 双向同步**：`bp_status` / `project_id` / `repository_id` / `q` / `page`（⭐ query 键取 `bp_status` 而非 `status`，与既有 `dep_type` 的模块前缀习惯一致），刷新可复现；写回一律 `router.replace({ query: { ...route.query, X: v } })`（保留 `tab` 等其它 query）；
- 数据走 `blueprintsApi.listBlueprints(...)`，`queryKey: computed(() => ['blueprint','list', filters])`，`staleTime: 30_000`；响应体是**五键分页**（`{total, items, page, page_size, has_next}`，115-01 订正）；
- 加载：`Skeleton h-32 rounded-2xl × 6` 网格；空态：`CompactEmptyState`（⭐ **`icon="lucide--file-x"` 裸名**，标题「没有匹配的技术方案」／描述「换个筛选条件，或在项目里发起一次方案编排」，按钮放**默认 slot**）。
`data-testid="blueprint-tab-panel"`。

**④ `ProjectBlueprintsCard.vue`**：`props: { projectId: string, hideWhenEmpty?: boolean }`。
- 只读：`listBlueprints({ project_id })` + 11 态徽标 + 更新时间 + `RouterLink` 跳查看器；
- 分区头沿用该面板既有的 `.flat-section` / `.flat-header` + `section-chip`；
- ⭐ **「无数据整块不渲染」由 `hide-when-empty` prop 驱动**（照 `ProjectMaterialsPanel.vue:52` 的 `HumanTaskInbox` 用法），⛔ **不是组件自决**；
- ⭐ **文案必须与既有 `ArtifactTimeline` 区分**（P-17：⚠️ 该面板 `:76` **已经渲染** `<ArtifactTimeline artifact-type="technical_plan">`，而蓝图与旧 technical_plan **共用同一 `artifact_type`** ⇒ 两块会出现内容相近的条目）。分区标题用「技术方案蓝图」并在描述里点明「结构化蓝图（含批注与人审）」，与既有分区的「交付物版本轨」区分。组件 docstring 写明这条重叠与区分策略。
`data-testid="project-blueprints-card"`。

**⑤ 追加点 #1 —— `web/src/pages/knowledge/index.vue`（纯追加，删除行 0）**：
- `:40` `KnowledgeTab` 联合类型加 `| 'blueprints'`；
- `:41` `TABS` 数组加 `'blueprints'`；
- `:220-234` `TabsTrigger` 的内联 `as const` 数组加一项 `{ value: 'blueprints', icon: 'icon-[lucide--file-text]' }`（⭐ 该图标**已在既有 safelist**）；
- 新增一个 `<TabsContent value="blueprints">` 渲染 `<BlueprintsTabPanel />`；
- ⛔ **`normalizeTab` 实现、`?tab=` 双向同步两个 watch、既有四个 tab 的任何一行都不改**；⛔ 不动既有 `CompactEmptyState` 的错误写法（那是既有面，不在本相位修复范围）。
- i18n `knowledge.tabs.blueprints` **已由 115-02 追加**，⛔ 本 plan 不改 `zh-CN.json`。

**⑥ 追加点 #2 —— `ProjectMaterialsPanel.vue`（纯追加，删除行 0）**：
- `:27-30` 的 `defineAsyncComponent` 组**追加第五行**：`const ProjectBlueprintsCard = defineAsyncComponent(() => import('./ProjectBlueprintsCard.vue'))`；
- 分区流里**追加一处使用**：`<ProjectBlueprintsCard :project-id="project.id" hide-when-empty />`，⭐ **建议排在既有 `<ArtifactTimeline>`（`:76`）之前**（蓝图是更新的形态，P-17）；
- ⛔ 既有四件懒加载与既有分区一行不改。

**⑦ 新建 `web/src/pages/knowledge/__tests__/blueprintViewer.spec.ts`**（范式抄 `entity-detail.spec.ts`：`vi.mock('vue-router')` + `vi.mock('~/api')` + **手写最小 i18n 键树** + `stubs`；⭐ **stub 清单必含 `MermaidDiagram: true`**，`AnchorNavLayout` 用 `{ template: '<div><slot /></div>', props: ['sections'] }` —— **保留 props 以便断言**）：
1. ⭐ **十段无条件渲染**：doc 查询**还在 loading** 时 `wrapper.findAll('section[id]')` 长度 **== 10**（⛔ 变异：给段容器加 `v-if="doc"` ⇒ 转红）。
2. ⭐ **`sections` 恒 10 项且 badge 非 `0`**：`wrapper.findComponent(AnchorNavLayoutStub).props('sections')` 长度 == 10；零批注的段其 `badge` **=== `''`**（⛔ 不是 `0`，P-18）。
3. ⭐ **§20 断言 4（404 单文案）**：doc 查询抛 `ApiError(404)` ⇒ 页面文本含中性文案**恰好一次**、**不含**任何蓝图元信息（标题/状态徽标不出现）；⛔ 变异：加一句「该蓝图不存在」⇒ 转红（用「针对 404 的文案节点数 == 1」形态断言）。
4. ⭐ **gate 非 200 不报错**：gate 查询抛 404 而四个主查询正常 ⇒ 页面**正常渲染**、`[data-testid=blueprint-error-state]` **不存在**、无 toast（mock `useToast` 断言零调用）。
5. ⭐ **409 blocked 开解药面板**：approve mock 抛 `ApiError(409, ..., { unresolved_blocker_thread_ids: ['a','b'] })` ⇒ `[data-testid=blueprint-blocked-dialog]` 出现且 `[data-testid=blueprint-blocked-item]` 计数 == 2。
6. ⭐ **`reflow` 五档不当失败**：answer mock 返回 `{ reflow: { status: 'noop' } }` ⇒ **无** `destructive` toast、页面无错误态（断言 toast 的 mock 收到的 variant 不是 destructive）。
7. **零乐观更新**：动作成功后断言调用了 `invalidateQueries`（mock `useQueryClient`）且**没有** `setQueryData`。
8. **生成中按段增量**：`currentStatus: 'drafting'` 且部分段为空 ⇒ 空段出骨架 + 进度文案，**非空段实渲**（⛔ 不是全页 loading）。
9. **历史版本只读**：`doc.is_current === false` ⇒ 历史版本 `Alert` 出现且 `[data-testid=blueprint-thread-composer]` 不存在。

**⑧ 新建 `web/src/pages/knowledge/__tests__/blueprintsTab.spec.ts`**：
1. 列表渲染 N 张卡；卡是 `RouterLink` 且 `to` 为 `/knowledge/blueprints/{id}`（SC-4 深链）。
2. ⭐ 消费的是 **`current_status`**（给 item 一个 `current_status` 而**不给** `blueprint_status` ⇒ 徽标正常渲染）。
3. `unresolved_blocker_count: 0` ⇒ blocker 计数**不渲染**；`> 0` ⇒ 渲染（正反并列）。
4. 筛选 ↔ URL：改状态筛选 ⇒ `router.replace` 被调用且 query 含 `bp_status` 且**保留了既有 `tab`**。
5. ⭐ 搜索输入/提交分离：输入不触发请求（api mock 调用次数不变），回车才触发（+1）。
6. 五键分页：`has_next: true` ⇒ 下一页可用；`total` 驱动 `Pagination`。
7. 空态用 `CompactEmptyState` 且 `icon` 是**裸名**（断言 stub 收到的 prop 值不含 `icon-[`）。

**⑨ ⭐ 全相位前端门**（本 Task 收尾执行，结果登记进 SUMMARY）：
- `cd web && pnpm exec vitest run`（全量）；
- `pnpm lint`；`pnpm type-check`；
- ⭐ `pnpm build`（`vue-tsc -b && vite build`）—— 最终把关，确保新页面进得了路由与打包；
- ⭐ `pnpm exec vitest run src/__tests__/blueprint-source-guard.spec.ts` 并在 SUMMARY 登记**扫描面文件数**（此时 `components/blueprint/**` 与 `pages/knowledge/blueprints/**` 都已满，三条断言在完整代码面下生效）。
  </action>
  <verify>
    <automated>cd web && pnpm exec vitest run && pnpm lint && pnpm type-check && pnpm build</automated>
  </verify>
  <acceptance_criteria>
- `cd web && pnpm exec vitest run` **全绿**（全相位五个 plan 的 spec + 既有 ~50 个，无新增失败）；`pnpm lint`、`pnpm type-check`、⭐ `pnpm build` 均通过。
- ⭐ **两处追加点纯追加**：`git diff web/src/pages/knowledge/index.vue web/src/components/project/warroom/ProjectMaterialsPanel.vue | rg "^-[^-]"` **输出为空**。
- ⭐ **不改 `normalizeTab` / `?tab=` watcher**：`cd web && node -e "const {execSync}=require('child_process'); const d=execSync('git diff -U0 src/pages/knowledge/index.vue').toString(); if(/normalizeTab/.test(d)) throw new Error('改动触及 normalizeTab'); if(/watch\(/.test(d)) throw new Error('改动触及 watcher'); console.log('tab host append-only OK')"` 输出 `tab host append-only OK`。
- ⭐ **追加点 #2 的位置**：`cd web && node -e "const s=require('fs').readFileSync('src/components/project/warroom/ProjectMaterialsPanel.vue','utf8'); const a=s.indexOf('ProjectBlueprintsCard', s.indexOf('<template>')); const b=s.indexOf('ArtifactTimeline', s.indexOf('<template>')); if(a<0) throw new Error('未挂载新卡'); if(b>=0 && a>b) console.warn('新卡排在 ArtifactTimeline 之后（P-17 建议之前，SUMMARY 需说明理由）'); if(!/hide-when-empty/.test(s.slice(a-200,a+200))) throw new Error('未由面板传入 hide-when-empty'); console.log('materials append OK')"` 输出 `materials append OK`。
- ⭐ **`FilterBar` 零蓝图专属 prop**：`cd web && node -e "const s=require('fs').readFileSync('src/components/common/FilterBar.vue','utf8'); if(/blueprint|artifact|bp_status/i.test(s)) throw new Error('通用件里出现蓝图专属概念'); if(!/showClear/.test(s)) throw new Error('缺 showClear prop'); console.log('FilterBar generic OK')"` 输出 `FilterBar generic OK`。
- ⭐ **列表键是 `current_status`**：`rg -n "current_status" web/src/components/knowledge/BlueprintListCard.vue` 命中；`rg -n "blueprint_status" web/src/components/knowledge/BlueprintListCard.vue` **零命中**。
- ⭐ **深链直达（SC-4）**：`rg -n "/knowledge/blueprints/" web/src/components/knowledge/BlueprintListCard.vue web/src/components/project/warroom/ProjectBlueprintsCard.vue` 两文件各命中且是 `RouterLink`。
- ⭐ **搜索输入/提交分离**：`rg -n "submittedQuery|queryInput" web/src/components/knowledge/BlueprintsTabPanel.vue` 命中。
- ⭐ **query 键是 `bp_status`**：`rg -n "bp_status" web/src/components/knowledge/BlueprintsTabPanel.vue` 命中；`rg -n "query.*['\"]status['\"]" web/src/components/knowledge/BlueprintsTabPanel.vue` **零命中**。
- ⭐ **`CompactEmptyState` 裸名（P-6）**：`rg -n 'icon="icon-\[' web/src/components/knowledge/BlueprintsTabPanel.vue web/src/components/knowledge/BlueprintListCard.vue web/src/components/project/warroom/ProjectBlueprintsCard.vue` **零命中**。
- ⭐ **页面测试断言齐备**：`rg -c "it\(|test\(" web/src/pages/knowledge/__tests__/blueprintViewer.spec.ts` ≥ **9**；`blueprintsTab.spec.ts` ≥ **7**；`rg -n "section\[id\]|findAll\('section" web/src/pages/knowledge/__tests__/blueprintViewer.spec.ts` 命中（十段无条件渲染的断言）；`rg -n "=== ''|toBe\(''\)" web/src/pages/knowledge/__tests__/blueprintViewer.spec.ts` 命中（badge 空串断言）。
- ⭐ **源码守卫在完整代码面下生效**：`pnpm exec vitest run src/__tests__/blueprint-source-guard.spec.ts` 绿，且 SUMMARY 登记扫描面文件数（应 ≥ 35）。
- ⛔ **零改动清单核对**：`git diff web/src/components/chat/TechPlanCard.vue web/src/components/delivery/ArtifactTimeline.vue web/src/components/layout/AnchorNavLayout.vue web/src/components/common/CompactEmptyState.vue web/src/components/prompts/PromptVersionDiff.vue web/src/components/project/warroom/MermaidDiagram.vue web/src/config/status.ts web/src/api/client.ts web/src/locales/zh-CN.json web/src/styles/main.css web/src/api/index.ts` 输出**全部为空**（⚠️ 若某文件不存在则该项跳过并在 SUMMARY 登记）。
- ⛔ 后端零改动：`git diff --name-only server/` 输出**为空**；`git diff web/package.json web/pnpm-lock.yaml` 输出**为空**。
  </acceptance_criteria>
  <done>知识库「技术方案」tab 与项目物料卡以纯追加方式接入既有宿主（两处 `git diff` 删除行为 0）；`FilterBar` 按 DESIGN 既有契约补上且不含蓝图专属概念；列表深链直达查看器、筛选与分页与 URL 双向同步、搜索输入/提交分离；两个页面测试覆盖十段无条件渲染、badge 空串、404 单文案、gate 非 200 不报错、409 blocked 解药面板与 `reflow` 不当失败；全相位前端门（vitest / lint / type-check / build / 源码守卫）全绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 后端 404 → 页面文案 | 多一句「该蓝图不存在」就把 MJ-03 刻意不泄露存在性的闸门破了 |
| `blueprint-gate/` 的状态码 → 权限推断 | 该链**无项目范围闸**，其 404 混合三种语义，不携带任何权限信息 |
| 动作响应 → 本地展示态 | 乐观更新会与续驱后的真值不一致（MJ-01 第二点） |
| 段容器的渲染时机 → IntersectionObserver | 条件渲染 ⇒ observer 一个也挂不上，且**表现得像只是高亮迟钝** |
| 既有宿主文件（tab / 物料面板）→ 并行边界 | 非纯追加会撞 §13.2 的 0.19 归属面与 rebase 冲突面 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115-49 | Information Disclosure | 404 被翻译成两种文案 ⇒ 可枚举蓝图存在性 | mitigate | i18n 只有一个键（115-02）+ 页面与 `BlueprintErrorState` 各一条源码扫描 + §20 断言 4 的「文案节点数 == 1」用例 |
| T-115-50 | Elevation of Privilege | 据 `blueprint-gate/` 的 404 推断权限 ⇒ 把「门未开」误判成「无权限」，或反之把无闸端点当权限来源 | mitigate | gate 非 200 只决定面板是否渲染（⛔ 不进分档、不 toast）+ 页面注释写明该链无闸 + 「gate 404 而页面正常」的用例（P-10） |
| T-115-51 | Denial of Service | 段容器条件渲染 ⇒ `AnchorNavLayout` 的 observer 全部落空，左栏高亮永久失效且**看起来只是迟钝** | mitigate | 十段无条件渲染 + `sections` 恒 10 项 + 两条源码扫描 + 「loading 期 `section[id]` 计数 == 10」的用例（P-4） |
| T-115-52 | Tampering | badge 传 `0` ⇒ 零批注的段渲染出一个灰色的 0，与 §16 的口径冲突 | mitigate | 一律传 `''` + 源码扫描 + `props('sections')` 的 badge 空串断言（P-18） |
| T-115-53 | Tampering | 乐观更新 / `setQueryData` 推断下一状态 ⇒ 与续驱后的真值不一致，用户看到「刷新一下就变」 | mitigate | 一律以响应体 `current_status` 为准 + 前缀 `invalidateQueries` + 源码扫描禁 `setQueryData` + 用例断言 |
| T-115-54 | Tampering | `answer` 的非 `applied` 被渲染成错误 ⇒ 用户重复提交、答案重复落库 | mitigate | 五档 toast 分档 + 「`noop` 不出 destructive toast」用例 |
| T-115-55 | Tampering | 对既有宿主文件的非纯追加改动 ⇒ 撞 §13.2 边界与 rebase 冲突 | mitigate | 两处追加点的 `git diff | rg "^-[^-]"` 为空断言 + 「diff 不触及 `normalizeTab` / watcher」的源码扫描（S8） |
| T-115-56 | Information Disclosure | 物料面板新卡与既有 `ArtifactTimeline` 条目重叠 ⇒ 用户以为系统重复展示、或误读为两份不同方案 | mitigate | 新卡按 `blueprint_status != ""` 过滤 + 分区标题与描述明确区分 + 建议排在其之前 + 组件 docstring 登记（P-17） |
| T-115-57 | Denial of Service | 页面里直接写 `refetchInterval` ⇒ 同步点 2 换推送契约时要改多处，且非响应式实现会让生成中的蓝图永不刷新 | mitigate | 唯一消费点 `useBlueprintLive` + 源码守卫 spec 在完整代码面下生效（§20 断言 6） |
| T-115-58 | Tampering | 跨段跳转自定义滚动偏移 ⇒ 与 `AnchorNavLayout.scrollTo` 的 88px 不一致，定位错位 | mitigate | 页面统一处理且常量 88 与既有一致；段组件零 `scrollTo`（115-05 已扫描） |
| T-115-SC | Tampering | 依赖供应链 | accept | 零新增依赖（`git diff web/package.json web/pnpm-lock.yaml` 为空）；`pnpm build` 作为最终把关 |
</threat_model>

<verification>
- `cd web && pnpm exec vitest run` 全绿（全相位五个 plan 的 spec + 既有）。
- `cd web && pnpm lint`、`pnpm type-check`、⭐ `pnpm build` 均通过。
- 两处追加点纯追加：`git diff web/src/pages/knowledge/index.vue web/src/components/project/warroom/ProjectMaterialsPanel.vue | rg "^-[^-]"` 输出为空。
- 零改动清单：`git diff web/src/components/chat/TechPlanCard.vue web/src/components/delivery/ArtifactTimeline.vue web/src/components/layout/AnchorNavLayout.vue web/src/components/common/CompactEmptyState.vue web/src/components/prompts/PromptVersionDiff.vue web/src/components/project/warroom/MermaidDiagram.vue web/src/config/status.ts web/src/api/client.ts web/src/locales/zh-CN.json web/src/styles/main.css web/src/api/index.ts` 全部为空。
- 源码守卫：`pnpm exec vitest run src/__tests__/blueprint-source-guard.spec.ts` 绿，扫描面文件数 ≥ 35（登记进 SUMMARY）。
- `rg -n "refetchInterval|edit-block|edit-blocks|editBlocks" web/src/components/blueprint/ web/src/pages/knowledge/blueprints/` 零命中。
- `rg -n "v-html" web/src/components/blueprint/ web/src/pages/knowledge/blueprints/ web/src/components/knowledge/BlueprintsTabPanel.vue web/src/components/knowledge/BlueprintListCard.vue web/src/components/project/warroom/ProjectBlueprintsCard.vue web/src/components/common/FilterBar.vue` 零命中。
- 后端零改动：`git diff --name-only server/` 输出为空；`git diff web/package.json web/pnpm-lock.yaml web/pnpm-workspace.yaml` 输出为空。
</verification>

<success_criteria>
- **111–114 的十五个端点第一次有了人能点到的入口**：查看器路由页可打开、知识库「技术方案」tab 可筛选可深链、项目物料面板可见蓝图。SC-1 / SC-2 / SC-3 / SC-4 / SC-5 全部在界面上可达（确认门 UI 归 115-07）。
- ⭐ **P-4 被物理堵死**：十段容器无条件渲染、`sections` 恒 10 项、badge 传空串，三条各有源码扫描与用例。这是全相位「会静默假通过」清单里最隐蔽的一条。
- ⭐ **两条错误分档特例正确**：404 只有一句中性文案（不泄露存在性）；`blueprint-gate/` 的非 200 只影响面板渲染、不进分档不报错（它那条链无项目闸，状态码不携带权限信息）。
- ⭐ **六个动作端点的响应处理零乐观更新**：一律以响应体 `current_status` 为准 + 前缀 invalidate；approve 409 blocked 打开解药面板；`answer` 的 `reflow` 五档都不当失败。
- **两处既有文件改动均为纯追加**（删除行 0），且不触及 `normalizeTab` / `?tab=` watcher / 既有四个 tab / 既有四件懒加载；`ArtifactTimeline` 与新卡的条目重叠有文案区分。
- **全相位前端门通过**：vitest 全绿、lint、type-check、`pnpm build`、源码守卫在完整代码面下三条断言生效。
</success_criteria>

<output>
完成后创建 `.planning/phases/115-ui/115-06-SUMMARY.md`，provides 至少包含：
- **查看器路由页的完整契约**：路由与六个 query 的语义 / 一次性消费规则 / `?panel=` 缺席兜底两档的实际行为；
- ⭐ **`sections` 十项的逐项表**（key / labelKey / icon 完整类名 / badge 表达式 / tone 规则）与「恒 10 项 + badge 传 `''`」的核对记录；
- ⭐ **对 UI-SPEC §6.9 的订正登记**（`must_haves` / `decision_log` 无内容时**段容器与导航项仍渲染**，只是段内不出内容——理由 P-4）；
- **错误分档的最终落点表**（四个主查询 404 / 5xx / 400；gate 非 200；`chunk-at`·`charter` 例外）；
- **六个动作端点的接线实现**与 `reflow` 五档 toast 的实际文案；
- **知识库 tab 与项目物料卡的追加点 diff 摘要**（逐行列出追加内容，证明删除行为 0）+ 新卡与 `ArtifactTimeline` 的**文案区分方案**（P-17）；
- `FilterBar` 的最终 API 与「它是通用件」的核对；
- ⭐ **全相位前端门结果**：vitest 用例总数与通过数、lint / type-check / build 结果、源码守卫的扫描面文件数；
- ⭐ **完整 UAT 清单**（happy-dom 测不了的全部项，供 `/gsd-verify-work` 使用）：左栏高亮随滚动推进（P-4 的真实验证）、滚动定位偏移、mermaid 实渲、三栏断点、`Sheet` 焦点陷阱与关闭后焦点回归、选区 popover 定位、颜色对比度、diff 并排视图、Tooltip 悬停；
- **六条 REQ → 实现落点与测试用例映射表**（VIEW-01/02/03/04, CLAR-01, FLOW-08）。
</output>
