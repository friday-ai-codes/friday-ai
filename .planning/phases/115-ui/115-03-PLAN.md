---
phase: 115-ui
plan: 03
type: execute
wave: 3
depends_on: ["115-02"]
files_modified:
  - web/src/components/blueprint/BlueprintBlock.vue
  - web/src/components/blueprint/BlueprintBlockList.vue
  - web/src/components/blueprint/BlueprintCitationChip.vue
  - web/src/components/blueprint/CitationPreviewDialog.vue
  - web/src/components/blueprint/citation/CitationKnowledgePreview.vue
  - web/src/components/blueprint/citation/CitationCodePreview.vue
  - web/src/components/blueprint/citation/CitationCharterPreview.vue
  - web/src/components/blueprint/citation/CitationBlueprintPreview.vue
  - web/src/components/blueprint/citation/CitationFallback.vue
  - web/src/components/blueprint/__tests__/BlueprintBlock.spec.ts
  - web/src/components/blueprint/__tests__/citationPreview.spec.ts
autonomous: true
requirements: [VIEW-01, VIEW-02, CLAR-01]
prohibitions:
  - "🔒 **前端 CREATE-ONLY**：本 plan **零既有文件修改**（`git diff --name-only web/src | rg -v '^web/src/components/blueprint/'` 必须为空）。i18n / safelist / api barrel 三处追加点已由 115-02 一次做完，⛔ **本 plan 不得再碰 `web/src/locales/zh-CN.json` / `web/src/styles/main.css` / `web/src/api/index.ts`**；缺 key 或缺图标 ⇒ 说明 115-02 漏了，**回报而不是自己补**（避免五向冲突面）"
  - "⛔ **禁 `v-html`**（S5 / UI-SPEC §19）：蓝图正文、线程消息、citation `quote`/`title`、JSON 示例、代码片段**一律 mustache + `<pre>`**。`rg -n 'v-html' web/src/components/blueprint/` 在本 plan 交付后必须**零命中**。唯一允许 `v-html` 的地方是 `MermaidDiagram.vue` 既有的 SVG 注入（既有面，本相位不改）"
  - "⛔ **不自造第三套 DOM 标识**：块级 DOM `id` 一律 `blk-${block.block_id}`。114 的重锚定判据是 `block_id → quoted_text → orphaned`，前端自造标识必然与重锚定结果错位。`section_path` **只**用于降级定位与失锚回显文案，⛔ 不参与 DOM id"
  - "⛔ **无 `edit-block` 入口 / emit / 文案 / 端点调用**（§0.1 硬边界第 3 条，顺延 Phase 116）：`BlueprintBlock.vue` 的 emits 只有三个（`thread-click` / `selection-comment` / `citation-click`）。源码守卫 `blueprint-source-guard.spec.ts` 的断言 10 从本 plan 起**真正开始生效**（扫描面首次非空）"
  - "⛔ **不重复实现批注与引用**：`BlueprintBlock.vue` 是**唯一**实现点（UI-SPEC §6.2）。后续 plan 的九个 section 组件一律经 `BlueprintBlockList` 透传 `blockCtx`，⛔ 不在段组件里自行处理批注"
  - "⛔ **不自写切分/取文本/分组逻辑**：`blockText` / `sliceBlockText` / `isValidAnchor` / `groupThreadsByBlock` / `annotationClass` 全部从 115-02 的 `~/utils/blueprintBlocks`、`~/utils/blueprintAnnotations`、`~/components/blueprint/annotationTokens` import。⛔ 组件内**不得**出现 `.type ===` 形态的取文本分派、不得出现颜色字面量（`rg 'hsl\\(' web/src/components/blueprint/*.vue` 零命中）"
  - "⛔ **`table` 与 `mermaid` 块不做字符级划线**（§7.3）：一律整块左侧色条 + 右上角计数角标。理由：后端 `table` 的坐标系是「所有单元格扁平后 `\\n` 连接」，前端渲染的是 `<table>`，offset **无法映射**"
  - "⛔ **`MermaidDiagram` 的 prop 名是 `code` 不是 `source`**（P-12）：写 `:source=` Vue 不报错、只是渲染一个空 `<pre>`。空源码时**必须由调用方 `v-if`**（组件自己不管，`code.trim()` 为空会渲染一个空 `<pre>` 且无提示）"
  - "⛔ **引用预览弹层内不渲染 mermaid**（P-12 次生）：`MermaidDiagram` 的放大层用 `vue-final-modal`（`:4,80`，`main.ts:6,17` 全局注册），与 reka-ui `Dialog` 是**两套模态栈**，在 Dialog 内点放大会叠放竞争。⇒ `BlueprintBlock` 增一个 `plainMermaid` prop，预览弹层内传 `true` 时 mermaid 块退化为源码 `<pre>`"
  - "⛔ **`chunk-at` 的兜底判据不是状态码**（P-3）：一律用 115-02 封装好的 `getChunkAt` 返回的 `usable` 字段；`locator.line_start` 缺失时**直接不发请求**立刻走兜底（省一次注定 400 的往返）。⛔ 任何调用点不得回显 `ApiError.detail`（`chunk-at` 的错误体键是 `error`，`detail` 会回落成无意义的 `'请求失败'`）"
  - "⛔ **兜底不留白、不关弹窗、不回显后端错误体**（§10.1）：任何来源取不到时渲染 `CitationFallback`（citation 自带的 `title` / `quote` 快照 + 一行「原始来源不可达，以下为引用时的快照」）。⚠️ 这与 analog `pages/knowledge/index.vue:165-191` 的 `catch` 分支（关弹窗 + toast）**完全相反**"
  - "⛔ **不调 `knowledgeApi.getRelated` / `getArtifactAssociations`**（P-5）：对蓝图的 `delivery.Artifact.id` 必然 404/空。本 plan 任何文件零命中"
  - "⛔ **`CompactEmptyState` 的 `icon` 传裸名**（P-6，如 `icon=\"lucide--file-x\"`）：组件内部做 `` :class=\"`icon-[${icon}]`\" ``，传完整类名会渲染成 `icon-[icon-[lucide--file-x]]` **图标出不来**。⚠️ 同目录的 `pages/knowledge/index.vue:309,317` 与 `entities/[id].vue:105` 就是错的那一派，⛔ 不要照抄；且该组件**没有** `action-label` prop 与 `@action` emit，按钮一律放**默认 slot**"
  - "⛔ **不引入 CodeMirror**：仓内**没有**只读 CodeMirror 封装（`components/execution/JsonViewer.vue:4-7` 自承是「替代 CodeMirror 的只读展示」，其余三个都是可编辑实例），UI-SPEC §3.6 的「复用既有 CodeMirror 只读封装」指向一个不存在的东西。代码片段一律 `<pre class=\"font-mono\">` + 行号（115-02 已定夺，见其 SUMMARY 的第三处 UI-SPEC 订正）"
  - "⛔ **不新增运行时依赖**、⛔ 不改 `web/package.json` / lockfile"
must_haves:
  truths:
    - "⭐ **`BlueprintBlock.vue` 是批注层与引用层的唯一实现点**：五类块分发（`paragraph` / `list` / `table` / `pseudocode` / `mermaid`）+ 字符区间 `<mark>` + citation chip 三件事都在这一个组件里；九个 section 组件（115-05）零重复"
    - "⭐ **DOM 契约逐字**：根元素 `:id=\"`blk-${block.block_id}`\"` + `:data-block-id=\"block.block_id\"` + `data-testid=\"blueprint-block\"`；`<mark>` 带 `data-thread-id` / `data-severity` / `data-thread-status` / `role=\"button\"` / `tabindex=\"0\"` / `aria-label` / `data-testid=\"blueprint-annotation-mark\"` 与 `@click`+`@keydown.enter`+`@keydown.space`"
    - "⭐ **越界降级 ≠ 失锚（P-7）**：offset 非整数 / 越界 / `start >= end` / 块类型是 `table` 或 `mermaid` ⇒ **整块左侧色条**（`border-l-2` + `pl-3`）+ 右上角 `icon-[lucide--message-square-dot]` 计数角标，**该线程仍按 `status` 归组、不进失锚组**；`anchor_status === 'orphaned'` 的线程在正文**完全不渲染**任何标记"
    - "⭐ **块文本一律经 `blockText(block)`**（115-02 的四分支纯函数），⛔ 组件内不按 `block.type` 取文本（P-13）"
    - "⭐ **零 `v-html`**：`rg -n 'v-html' web/src/components/blueprint/` 零命中"
    - "⭐ **无 block 编辑面**：`BlueprintBlock.vue` 的 `defineEmits` 恰好三项，`edit-block` / `edit-blocks` / `editBlocks` 在 `web/src/components/blueprint/**` 零命中；源码守卫 spec 的断言 10 首次在非空扫描面下转绿"
    - "⭐ **选区侦测的唯一落点是 `BlueprintBlockList.vue`**：监听 `document` 的 `selectionchange`（`@vueuse/core` 的 `useDebounceFn` 120ms，`onUnmounted` 必解绑），选区**完全落在同一个 `[data-block-id]` 子树内** ⇒ emit `selection-comment`（`{ blockId, startOffset, endOffset, quotedText, rect }`，offset 经 115-02 的 `rangeOffsets`，`quotedText` 上限 500 字符）；跨块 ⇒ emit `cross-block-selection`（由页面渲染 toast，文案 `knowledge.blueprints.annotation.crossBlock`）。⛔ 不在 `BlueprintBlock` 里各自监听（N 个块 N 个监听器）"
    - "⭐ **citation chip 分流**：外链类型（`work_item` / `feishu_doc` / `url`）是 `<a target=\"_blank\" rel=\"noopener noreferrer\">` **不弹 Dialog**；其余是 `<button>` 触发 `citation-click`；前缀图标按 `source_type` 九档取（图标已在 115-02 的 safelist）；`data-testid=\"blueprint-citation-chip\"`"
    - "⭐ **`CitationPreviewDialog` 是第一层 Dialog、无嵌套**（查看器是路由页），复用 `~/components/ui/dialog` 的 `Dialog` + `DialogScrollContent` + `DialogHeader` + `DialogTitle`（**`DialogTitle` 必填**，否则 reka-ui 报 a11y 警告）；尺寸 `w-[92vw] max-w-4xl`、内容区 `max-h-[72vh] overflow-auto`"
    - "⭐ **兜底不留白**：五类来源的任一失败（含 400/404/5xx/超时/解析失败/**`chunk-at` 的 200-空 chunks**）一律渲染 `CitationFallback`（`title` + `quote` 快照 + 一行说明），**弹窗保持打开**、⛔ 不回显后端错误体。三条并列用例：来源成功 / 来源 404 / `chunk-at` 200-空"
    - "⭐ **`CitationCodePreview` 本相位降级形态**：文件路径面包屑 + `line_start..line_end` 行号区间 + citation `quote` 快照，`<pre class=\"font-mono text-xs leading-6\">` + 行号列；⛔ 无 CodeMirror、⛔ 无源码正文（全仓无该读面，115-02 已登记订正）。`locator.line_start` 缺失 ⇒ **不发请求**直接兜底"
    - "⭐ **`CitationBlueprintPreview` 复用 `BlueprintBlockList`** 且传 `readonly` / 空 `threads` / `plainMermaid`：迷你只读渲染 `meta.title` + `meta.summary` + 被引块，**无批注层、无操作栏**；内部 citation chip **不开第二层弹层**，改「打开完整蓝图」`RouterLink`（§18.2）"
    - "组件测试：`BlueprintBlock.spec.ts` 覆盖五类块渲染 / `<mark>` 计数 / 越界整块色条 / orphaned 不渲染 / 无 `edit` 入口 / 跨块与同块选区 emit；`citationPreview.spec.ts` 覆盖九类 `source_type` 分发 + 三条兜底并列。**挂载任何含 `MermaidDiagram` 的组件必 `stubs: { MermaidDiagram: true }`**（否则要连带 `vue-final-modal` 插件）"
    - "前端门：`cd web && pnpm exec vitest run` 全绿（含 115-02 的源码守卫 spec 复跑）、`pnpm lint` 通过、`pnpm type-check` 通过"
  artifacts:
    - path: "web/src/components/blueprint/BlueprintBlock.vue"
      provides: "五类块分发 + <mark> 字符区间 + 整块降级色条 + citation chip 行；批注与引用的唯一实现点；props 含 plainMermaid；emits 恰好三项"
      contains: "data-testid=\"blueprint-block\""
    - path: "web/src/components/blueprint/BlueprintBlockList.vue"
      provides: "块序列渲染 + 段级骨架/空态三分支 + ⭐ 选区侦测唯一落点（selectionchange 去抖 + 同块/跨块分流）+ 透传 BlueprintBlock 全部 emits"
      contains: "selectionchange"
    - path: "web/src/components/blueprint/BlueprintCitationChip.vue"
      provides: "九档 source_type 图标与文案 + 外链走 <a> 其余走 <button> 的分流 + aria-label"
      contains: "noopener noreferrer"
    - path: "web/src/components/blueprint/CitationPreviewDialog.vue"
      provides: "第一层受控 Dialog + 按 source_type 分发到五个子件 + 任何失败一律 CitationFallback 且不关弹窗"
      contains: "DialogScrollContent"
    - path: "web/src/components/blueprint/citation/CitationCodePreview.vue"
      provides: "降级形态：路径面包屑 + 行号区间 + quote 快照 + <pre> 行号渲染；usable=false 或缺 line_start 一律兜底"
      contains: "usable"
    - path: "web/src/components/blueprint/citation/CitationFallback.vue"
      provides: "§10.1 的兜底不留白：title + quote 快照 + 「原始来源不可达，以下为引用时的快照」"
      contains: "quote"
    - path: "web/src/components/blueprint/__tests__/BlueprintBlock.spec.ts"
      provides: "五类块 + mark 计数 + 越界整块色条 + orphaned 不渲染 + 无 edit 入口 + 选区 emit 正反；MermaidDiagram 被 stub"
      contains: "blueprint-annotation-mark"
  key_links:
    - from: "web/src/components/blueprint/BlueprintBlock.vue"
      to: "web/src/utils/blueprintAnnotations.ts"
      via: "sliceBlockText 切区间、isValidAnchor 判越界（组件不自写切分算法）"
      pattern: "sliceBlockText"
    - from: "web/src/components/blueprint/BlueprintBlock.vue"
      to: "web/src/components/blueprint/annotationTokens.ts"
      via: "annotationClass(kind, severity, status, active) 唯一颜色来源，组件内零颜色字面量"
      pattern: "annotationClass"
    - from: "web/src/components/blueprint/BlueprintBlock.vue"
      to: "web/src/components/project/warroom/MermaidDiagram.vue"
      via: ":code=\"blockText(block)\"（prop 名是 code 不是 source）+ 调用方 v-if 空源码 + plainMermaid 时不渲染它"
      pattern: ":code="
    - from: "web/src/components/blueprint/citation/CitationCodePreview.vue"
      to: "web/src/api/repositoryChunks.ts"
      via: "getChunkAt 的 usable 字段作为唯一兜底判据（200-空 chunks 也算不可用）"
      pattern: "getChunkAt"
    - from: "web/src/components/blueprint/citation/CitationBlueprintPreview.vue"
      to: "web/src/components/blueprint/BlueprintBlockList.vue"
      via: "迷你只读渲染复用同一块渲染件（readonly + 空 threads + plainMermaid）"
      pattern: "BlueprintBlockList"
---

<objective>
把「蓝图正文怎么渲染 + 批注怎么显示 + 引用怎么二级预览」三件事做成**两个可复用件 + 一套预览弹层**，让 115-05 的九个 section 组件退化成纯粹的数据排版：

① **`BlueprintBlock.vue`** —— UI-SPEC §6.2 指定的**唯一**批注/引用实现点。一个 `Block` 进、一段 DOM 出：五类块分发、按 `anchor.start_offset/end_offset` 在**纯文本**上切区间并用 `<mark>` 包裹（飞书式下划线）、块底一行 citation chip。⭐ 全相位「最不能做错的一层」的可视半边落在这里。

② **`BlueprintBlockList.vue`** —— 段内块序列 + 段级三分支（骨架 / 实渲 / 空态）+ ⭐ **选区侦测的唯一落点**（CLAR-01 后半句「人也可对任意选区主动发起评论」的起点）。放在 List 而不是 Block 上，是为了避免 N 个块挂 N 个 `selectionchange` 监听器，也让「跨块选区」这一档有地方判。

③ **引用二级预览六件（VIEW-02）** —— 一个受控 `Dialog` 按 `source_type` 分发到五个子件。⭐ 本相位对它做了一处**显式降级**：`chunk-at` 只返回 `{chunk_id, file_path, line_start, line_end, chunk_index}`，**没有代码正文**，且全仓没有「按 path+行区间取源码」的读面 ⇒ 代码预览退化为「路径 + 行号区间 + `quote` 快照」（115-02 已登记该 UI-SPEC 订正，本 plan 落地）。

Purpose: 这一层是 SC-1（结构化渲染）与 SC-2（划线高亮）的可视面，也是 SC-3（引用二级预览）的全部。它一旦成型，后面的段组件、侧栏、页面装配都只是拼装。
Output: 9 个新建组件 + 2 个新建测试文件。**零既有文件修改、零新增依赖。**
</objective>

<execution_context>
工作目录：worktree 根 `/Users/zaneliu/Projects/open-source/friday-ai/.claude/worktrees/v0.20-blueprint`。前端命令 `cd web && ...`，测试用 `pnpm exec vitest run <path>`。

**wave 3，依赖 115-02**：以下全部**已存在**，⛔ 本 plan 只 import 不重写——
- `~/types/blueprint`（`BlueprintBlock` / `Citation` / `BlueprintThreadDetail` / `BlueprintV1`）；
- `~/utils/blueprintBlocks`（`blockText` / `iterBlocks` / `summaryText`）；
- `~/utils/blueprintAnnotations`（`sliceBlockText` / `isValidAnchor` / `degradedThreadIds` / `collectTextNodes` / `offsetInFlatText` / `rangeOffsets` / `groupThreadsByBlock`）；
- `~/components/blueprint/annotationTokens`（`annotationClass` / `ANNOTATION_PRIORITY` / `MARK_BASE_CLASS`）；
- `~/api/blueprints`（`getBlueprintDocument`）与 `~/api/repositoryChunks`（`getChunkAt` → `{chunks, usable}` / `getRepositoryCharter`）；
- `~/api/knowledge` 的 `getEntity`（既有，只读复用）；
- i18n 键 `knowledge.blueprints.block.*` / `.annotation.*` / `.citation.*` / `.error.*`（115-02 已写全，⛔ **缺键不自己补，回报**）。

⚠️ **与 115-02 的能力锁结论对接**：`rangeOffsets` 内部依赖 `collectTextNodes`。若 115-02 的 SUMMARY 记录 happy-dom **不支持** `TreeWalker`/`Range`，则本 plan 的「同块选区 emit」用例改走**直接调用 `offsetInFlatText` + 手工构造 payload** 的形态（断言 emit 的**装配逻辑**而非 DOM 取值），并把 DOM 取值那半列进 UAT。⛔ 不因此删用例。

**既有可复用件契约（实测，不得猜测）**：
- `~/components/project/warroom/MermaidDiagram.vue`（103 行）：prop 名 **`code: string`**（`:7`）；`securityLevel: 'strict'` / `startOnLoad: false`；渲染失败**自动**回退 `<pre>{{ props.code }}</pre>` + 一行「无法渲染流程图，已展示源码」（`:42-46,72-78`），**调用方零处理**；`code.trim()` 为空 ⇒ `svg=''` 且 `error=false` ⇒ 渲染一个**空 `<pre>` 且无提示**（`:29-33`）⇒ **空源码必须由调用方 `v-if`**；放大用 **`VueFinalModal`**（`:4,80`），不是 `~/components/ui/dialog`；文案内联中文未走 i18n。
- `~/components/common/CompactEmptyState.vue`（33 行）：`withDefaults(defineProps<{icon?: string}>(), { icon: 'lucide--inbox' })`，模板 `` :class="`icon-[${icon}]`" `` ⇒ 收**裸名**；**无 `action-label` prop、无 `@action` emit**，只有默认 slot（`:28-30`）。
- `~/components/ui/dialog`：`Dialog` / `DialogScrollContent` / `DialogHeader` / `DialogTitle`；用法范式即 `pages/knowledge/index.vue:23-29,165-191` 的工件查看弹窗（⭐ **但它的 `catch` 分支是关弹窗 + toast，与本相位相反**）。
- `~/components/ui/badge`：八个 variant `default/secondary/destructive/outline/success/warning/info/muted`；⛔ 禁止在 `Badge` 上用 `:class` 追加颜色类。
- `~/components/prompts/PromptVersionDiff.vue:90-97`：`v-for` + `<pre class="font-mono text-xs leading-6 whitespace-pre-wrap px-3 py-1">{{ seg.text }}</pre>` —— 块内 mustache + `<pre>` 的逐字范式；其 `:10-11` 的自述纪律「完全走 mustache，未使用 v-html，XSS 面 = 0」逐字适用于本 plan。
- `~/components/ui/table`：语义 `<table>` / `<th scope="col">`，⛔ 不用 div 模拟。
- `~/components/knowledge/EntityKindBadge.vue` / `~/components/spec/SddSpecStatusBadge.vue`：映射表 + `data-testid` 的 chip/badge 形状 analog。
- `MarkdownRenderer.vue`（既有）：知识实体正文渲染复用它，⛔ 不新引渲染器。

**测试设施**：`environment: 'happy-dom'`；`setupFiles: ['./src/test/setup.ts']` **只有 storage 垫片**（无 i18n / router / IntersectionObserver / fetch mock）。页面级范式见 `pages/knowledge/__tests__/entity-detail.spec.ts`（68 行）：`vi.mock('vue-router', ...)` + `vi.mock('~/api', ...)` + **手写最小 i18n 键树**（⛔ 不 import `zh-CN.json`）+ `mount(..., { global: { plugins: [i18n, [VueQueryPlugin, {...}]], stubs: {...} } })` + `await new Promise(r => setTimeout(r, 50))`。组件级最规范范式见 `components/prompts/__tests__/PromptVersionDiff.test.ts`（113 行）：顶部「覆盖路径」编号清单 + `makeVersion(overrides)` 工厂 + **正/负向成对断言** + `wrapper.findAll('[data-diff-column]')` 按 `data-*` 定位 + **真实 import 不 mock**。
</execution_context>

<context>
@.planning/phases/115-ui/115-02-SUMMARY.md（**纯函数签名清单为权威**：`blockText` / `sliceBlockText` / `isValidAnchor` / `degradedThreadIds` / `rangeOffsets` / `groupThreadsByBlock` / `annotationClass`；`getChunkAt` 的 `{chunks, usable}`；A2 能力锁结论；四处 UI-SPEC 订正；i18n 键清单）
@.planning/phases/115-ui/115-UI-SPEC.md §6.2（**BlueprintBlock 的 props/emits 与五类块分发表逐字**、DOM 契约、citation chip 的视觉类串与九档图标）、§7.1（切分四步与优先级）、§7.2（`<mark>` 元素契约逐字，含全部 `data-*` 与 a11y 属性）、§7.3（越界降级 vs 后端失锚的两种不同呈现表）、§7.4（选区 → 评论六步）、§7.5（令牌，由 `annotationTokens` 承载）、§10.1（`CitationPreviewDialog` 的分发表与兜底强制段）、§18.2（预览内 chip 不开第二层弹层）、§20（断言 8/9/10）
@.planning/phases/115-ui/115-RESEARCH.md（§B.1 切分与坐标系、§B.3 `MermaidDiagram` 契约表逐条、§B.6 `chunk-at` 六种响应形状表与「chunks 无正文」、§B.10 测试可达上界；P-3 / P-6 / P-7 / P-12 / P-13）
@.planning/phases/115-ui/115-PATTERNS.md（§D 内容渲染的三个 analog 与「必须 DIFFER」、§G1 `CitationPreviewDialog` 的 ⭐「与 analog 的 catch 分支完全相反」、No Analog #1（切分算法零先例）/ #3（无只读 CodeMirror 且 chunk 无正文））
@web/src/components/prompts/PromptVersionDiff.vue（块内 mustache + `<pre>` 的逐字范式，含 `:10-11` 的 XSS 自述纪律）
@web/src/components/project/warroom/MermaidDiagram.vue（**全文 103 行**：prop 名 `code`、空源码行为、失败自动回退、放大用 vue-final-modal）
@web/src/components/common/CompactEmptyState.vue（**全文 33 行**：`icon` 收裸名、只有默认 slot）
@web/src/pages/knowledge/index.vue L23-29,165-191（受控 Dialog 的 import 组与 open/loading/data 四 ref 范式；⚠️ 其 catch 分支是反例）
@web/src/components/prompts/__tests__/PromptVersionDiff.test.ts（**全文 113 行**：组件测试最规范范式）
@web/src/pages/knowledge/__tests__/entity-detail.spec.ts（**全文 68 行**：mount + i18n 手写键树 + stubs + 等 query 的范式）
</context>

<tasks>

<task type="auto">
  <name>Task 1: BlueprintBlock.vue（五类块 + &lt;mark&gt; 切分 + 整块降级 + citation chip 行）+ BlueprintCitationChip.vue</name>
  <files>web/src/components/blueprint/BlueprintBlock.vue, web/src/components/blueprint/BlueprintCitationChip.vue</files>
  <read_first>.planning/phases/115-ui/115-UI-SPEC.md §6.2 与 §7.1–§7.3、§7.5（**props/emits、五类块分发表、`<mark>` 契约、两种降级、令牌分档全部逐字**）；.planning/phases/115-ui/115-02-SUMMARY.md 的纯函数签名清单（`blockText` / `sliceBlockText` / `isValidAnchor` / `degradedThreadIds` / `annotationClass` / `MARK_BASE_CLASS` 的**确切入参与返回**）；web/src/components/prompts/PromptVersionDiff.vue L1-20 与 L85-100（模块 docstring 的「安全 / 性能」两段体例 + `v-for` + `<pre>` 的逐字类串）；web/src/components/project/warroom/MermaidDiagram.vue **全文**（prop 名 `code`、空源码渲染空 `<pre>`、失败自动回退——三条都决定调用方写法）；web/src/components/ui/table/index.ts（导出的子组件名与用法）；web/src/components/knowledge/EntityKindBadge.vue（映射表 + `data-testid` 的 chip analog）；web/src/components/ui/badge/index.ts（八个 variant 的确切名字）；web/src/components/ui/tooltip/index.ts（多线程 `<mark>` 的 `title`/tooltip 承载）</read_first>
  <action>
**① 新建 `web/src/components/blueprint/BlueprintCitationChip.vue`**（先建，`BlueprintBlock` 依赖它）：

`props: { citation: Citation, index: number }`；`emits: { click: [citationId: string] }`（⭐ 本相位 `components/blueprint/**` 内**统一用元组简写** `defineEmits<{ click: [id: string] }>()`，⛔ 不混用 `(e: 'click', id: string): void` 形态 —— 仓内两种都在用，PATTERNS 要求本目录内统一）。

渲染：形如 `[1] onion-practice/src/x.py:10-42`（序号 + `title` 或由 `locator` 拼出的定位串）。视觉类串**逐字照 UI-SPEC §6.2**：`inline-flex items-center gap-1 rounded-md border border-border bg-muted/60 px-1.5 py-0.5 text-[11px] font-mono text-muted-foreground hover:border-primary/40 hover:text-primary`。

⭐ **分流**：`source_type ∈ {work_item, feishu_doc, url}` ⇒ 渲染 `<a :href target="_blank" rel="noopener noreferrer">`（**不 emit、不弹 Dialog**）；其余六类 ⇒ `<button type="button" @click="emit('click', citation.citation_id)">`。

前缀图标九档（`source_type` → 裸图标名，全部已在 safelist）：`knowledge_entity`→`lucide--book-open`；`repo_file`/`rag_chunk`→`lucide--file-code`；`repo_charter`→`lucide--scroll-text`；`blueprint`/`artifact_version`→`lucide--file-text`；`work_item`→`lucide--list-checks`；`feishu_doc`/`url`→`lucide--external-link`。映射表是模块级 `Record` 常量（形状照 `EntityKindBadge.vue`），⛔ 不在模板里写 `v-if` 链。

`aria-label` = `t('knowledge.blueprints.citation.open', { title })`；`data-testid="blueprint-citation-chip"`；⭐ **焦点环**用 `outline: 2px solid var(--color-primary-600); outline-offset: 2px`（不透明，3.74:1）——⛔ 不复制既有 `.btn:focus-visible` 的 50% 透明 teal（实算 1.59:1，未过 WCAG 2.4.11）。

**② 新建 `web/src/components/blueprint/BlueprintBlock.vue`**。

模块 docstring 三段（照 `PromptVersionDiff.vue:1-20` 的体例）：
- 「**职责**：一个 `Block` 进、一段 DOM 出。**批注层与引用层的唯一实现点**（UI-SPEC §6.2）——九个 section 组件一律经 `BlueprintBlockList` 透传 `blockCtx`，⛔ 不在段组件里重复实现」；
- 「**安全**：块文本、citation `quote`/`title` 全程 Vue mustache + `<pre>`，**未使用 `v-html`**，XSS 面 = 0」；
- 「**坐标系**：块文本一律经 `blockText(block)`（`~/utils/blueprintBlocks`），它按 `text → code.source → rows` 的**字段优先级**取值、**不看 `block.type`** —— 与后端 `_block_text`（`server/delivery/services/blueprint_anchor.py:34-64`）逐字同源。⛔ 组件内不得自行按 type 取文本：坐标系不一致的后果是 offset 仍在合法范围内 ⇒ 不触发降级、不报错、`<mark>` 照渲，**只是圈错了字**」。

`props`（UI-SPEC §6.2 逐字 + 一项本 plan 新增）：`block` / `sectionPath` / `threads`（已按 block_id 预分组）/ `citations`（文档级引用池 `Record<string, Citation>`）/ `readonly` / `activeThreadId` / ⭐ `plainMermaid?: boolean`（默认 `false`；为 `true` 时 mermaid 块**退化为源码 `<pre>`**，供引用预览弹层使用——P-12 次生：`MermaidDiagram` 的放大层是 `vue-final-modal`，与 reka-ui `Dialog` 是两套模态栈）。

`emits`（**恰好三项**，⛔ 无 `edit-block`）：`thread-click: [threadId: string]` / `selection-comment: [payload: {...}]`（由 `BlueprintBlockList` 触发后透传，见 Task 2；本组件保留该 emit 只为 UI-SPEC 契约一致）/ `citation-click: [citationId: string]`。

**DOM 契约（重锚定与测试都依赖它，逐字）**：根元素 `:id="`blk-${block.block_id}`"` + `:data-block-id="block.block_id"` + `data-testid="blueprint-block"`。⛔ `section_path` 不参与 DOM id（只用于降级定位与失锚回显文案）。

**渲染分派（两层）**：

第一层——**是否做字符级划线**（`computed`）：
- 该块的 anchored 线程集合 = `props.threads.filter(t => t.anchor_status !== 'orphaned' && t.anchor?.block_id === props.block.block_id)`（⭐ `orphaned` 的线程在正文**完全不渲染**任何标记，§7.3）；
- 若 `block.type ∈ {table, mermaid}`，或该集合里**存在**任一 anchor 经 `isValidAnchor` 判为不合法（非整数 / 越界 / `start >= end`）⇒ 走**整块降级**（把不合法的那些线程放进 `degradedThreadIds`）；
- 整块降级呈现：根元素追加 `border-l-2 pl-3` + `annotationClass(...)` 给出的色（**优先级最高的一条线程决定颜色**，用 `ANNOTATION_PRIORITY`），右上角一个 `icon-[lucide--message-square-dot]` + 计数角标（`<button>`，点击 emit `thread-click` 取优先级最高那条），`data-testid="blueprint-block-degraded"`。⚠️ **这些线程仍按 `status` 归组、⛔ 不进失锚组**（P-7，侧栏侧由 115-02 的 `sidebarGroups` 保证，本组件只管正文呈现）。

第二层——**五类块的正文渲染**（与 schema `enum` 逐字对齐）：
- `paragraph`：`<p class="text-sm leading-relaxed">` + `v-for` 切分段（见下）。
- `list`：`text` 是条目字符串数组 ⇒ `<ul class="list-disc pl-5 space-y-1 text-sm">`；⭐ **offset 坐标系是「条目间用 `\n` 连接」的扁平串**（后端 `_block_text` 已验证用 `"\n".join`）⇒ 切分在**扁平串**上做，再按 `\n` 还原成 `<li>`（每个 `<li>` 内可含多个切分段）。⛔ 不按条目各自从 0 计 offset。
- `table`：`rows[0]` 表头、其余数据行，用 `~/components/ui/table` 的语义 `<table>` / `<th scope="col">`；⛔ **不做字符级划线**（整块色条，见第一层）。
- `pseudocode`：`code.language` 徽标 + 复制按钮 + `<pre class="font-mono text-xs leading-6">` + 行号列；⭐ **坐标系是 `code.source` 原文** ⇒ **可以**做字符级划线（§7.3 只把 `table`/`mermaid` 列为强制降级）；⛔ **不引入语法高亮引擎**（伪代码非真实语言，高亮易误导）。⚠️ 但若 `blockText(block)` 因四分支优先级取到了 `text` 而非 `code.source`（「同时带非空 `text` 与 `code`」的合法块），切分**仍以 `blockText` 的返回为准** —— 这正是 P-13 的要点。
- `mermaid`：`plainMermaid === true` ⇒ `<pre>` 源码；否则 ⇒ ⭐ **调用方 `v-if` 判空**：`v-if="blockText(block).trim()"` 才渲染 `<MermaidDiagram :code="blockText(block)" />`（**prop 名是 `code` 不是 `source`**；空源码时组件自己会渲一个空 `<pre>` 且无提示，所以判空必须在调用方）。⛔ **不做字符级划线**。

**切分段渲染（唯一实现）**：`sliceBlockText(blockText(block), anchors)` 的返回逐段 `v-for`：`threadIds.length === 0` ⇒ 纯文本 `<span>{{ seg.text }}</span>`；否则 ⇒ `<mark>`，属性逐字照 UI-SPEC §7.2：`:data-thread-id` / `:data-severity` / `:data-thread-status` / `:class="[MARK_BASE_CLASS, annotationClass(kind, severity, status, isActive)]"` / `role="button"` / `tabindex="0"` / `:aria-label="t('knowledge.blueprints.annotation.markLabel', { count, kind })"` / `data-testid="blueprint-annotation-mark"` / `@click` + `@keydown.enter.prevent` + `@keydown.space.prevent` 三者都 emit `thread-click`。
- 一个 `<mark>` 覆盖多条线程时：着色取 `ANNOTATION_PRIORITY` 最高的一条，`aria-label`/`title` 列出全部；点击 emit **优先级最高那条**的 id（⚠️ UI-SPEC §7.6 要求弹微型 `Popover` 让用户选——那个 Popover 归 115-04 的侧栏交互层，本组件只负责 emit 全量 `threadIds`：⇒ **emit 载荷改为 `thread-click: [threadId: string, allThreadIds: string[]]`**，第二个参数供上层决定是否弹选择器。**这是对 UI-SPEC §6.2 emits 签名的一处扩写，在 SUMMARY 登记**）。
- `resolved` / `dismissed` 的线程默认**不着色**（§7.5：默认隐藏，顶栏「显示已关闭批注」开关打开后可见）⇒ 组件增一个 `showClosed?: boolean` prop（默认 `false`），由页面从 Pinia store 透传。**这是第二处对 UI-SPEC §6.2 props 的扩写，一并登记。**

**citation chip 行**：块底部一行，`block.citations?.map(id => props.citations[id])` 过滤掉取不到的 id（⛔ 不渲染 `undefined`），逐个 `<BlueprintCitationChip :citation :index @click="emit('citation-click', $event)" />`。

⛔ **组件内零颜色字面量**（`rg 'hsl\(' ` 零命中）、零 `v-html`、零 `.type ===` 形态的取文本分派。
  </action>
  <verify>
    <automated>cd web && pnpm type-check && pnpm exec vitest run src/__tests__/blueprint-source-guard.spec.ts</automated>
  </verify>
  <acceptance_criteria>
- `cd web && pnpm type-check` 通过；`pnpm lint` 通过。
- ⭐ **零 `v-html`**：`rg -n "v-html" web/src/components/blueprint/` **零命中**。
- ⭐ **emits 恰好三项且无 edit**：`cd web && node -e "const s=require('fs').readFileSync('src/components/blueprint/BlueprintBlock.vue','utf8'); const m=s.match(/defineEmits<\{([\s\S]*?)\}>/); if(!m) throw new Error('缺 defineEmits 类型声明'); const keys=[...m[1].matchAll(/^\s*'?([a-zA-Z-]+)'?\s*:/gm)].map(x=>x[1]); if(keys.length!==3) throw new Error('emits 应恰好 3 项，实为 '+keys.join(',')); if(keys.some(k=>/edit/i.test(k))) throw new Error('出现 edit 相关 emit'); console.log('emits OK', keys.join(','))"` 输出 `emits OK thread-click,selection-comment,citation-click`。
- ⭐ **无 block 编辑面（§20 断言 10 首次在非空扫描面下生效）**：`rg -n "edit-block|edit-blocks|editBlocks" web/src/components/blueprint/` **零命中**；`pnpm exec vitest run src/__tests__/blueprint-source-guard.spec.ts` 绿。
- ⭐ **DOM 契约**：`rg -n 'blk-\$\{|data-block-id|data-testid="blueprint-block"' web/src/components/blueprint/BlueprintBlock.vue` 三条全命中；`rg -n "section_path|sectionPath" web/src/components/blueprint/BlueprintBlock.vue` 的命中**不出现在 `:id=` 附近**（人工核对，SUMMARY 登记）。
- ⭐ **不自写切分/取文本**：`rg -n "sliceBlockText|blockText|isValidAnchor|annotationClass" web/src/components/blueprint/BlueprintBlock.vue` 四个 token 全命中；`cd web && node -e "const s=require('fs').readFileSync('src/components/blueprint/BlueprintBlock.vue','utf8'); const scriptEnd=s.indexOf('</script>'); const script=s.slice(0,scriptEnd); const bad=/block\.type\s*===\s*['\"](?:pseudocode|paragraph|list)['\"]\s*\?\s*[^:]*(code|text)/.test(script); if(bad) throw new Error('出现按 type 取文本的分派（P-13）'); console.log('no type-dispatch text extraction')"` 输出 `no type-dispatch text extraction`（⚠️ 按 `type` 做**渲染分派**是允许且必须的，禁止的只是按 type **取文本**）。
- ⭐ **零颜色字面量**：`rg -n "hsl\(|#[0-9a-fA-F]{6}" web/src/components/blueprint/BlueprintBlock.vue web/src/components/blueprint/BlueprintCitationChip.vue` **零命中**（颜色一律经 `annotationTokens` 或 Tailwind 语义类）。
- ⭐ **mermaid 三条**：`rg -n ':code=' web/src/components/blueprint/BlueprintBlock.vue` 命中且 `rg -n ':source=' web/src/components/blueprint/BlueprintBlock.vue` **零命中**（P-12 prop 名）；`rg -n "plainMermaid" web/src/components/blueprint/BlueprintBlock.vue` 命中；`cd web && node -e "const s=require('fs').readFileSync('src/components/blueprint/BlueprintBlock.vue','utf8'); const i=s.indexOf('MermaidDiagram'); const around=s.slice(Math.max(0,i-400), i+200); if(!/v-if/.test(around)) throw new Error('mermaid 未由调用方 v-if 判空'); console.log('mermaid guard OK')"` 输出 `mermaid guard OK`。
- ⭐ **外链 chip 不弹 Dialog**：`rg -n 'rel="noopener noreferrer"' web/src/components/blueprint/BlueprintCitationChip.vue` 命中；`cd web && node -e "const s=require('fs').readFileSync('src/components/blueprint/BlueprintCitationChip.vue','utf8'); if(!/work_item|feishu_doc|url/.test(s)) throw new Error('缺外链三类分流'); console.log('chip branch OK')"` 输出 `chip branch OK`。
- ⭐ **焦点环不透明**：`rg -n "primary-600" web/src/components/blueprint/BlueprintCitationChip.vue` 命中；`rg -n "168 76% 42% / 0.5|/ 0.5\)" web/src/components/blueprint/` **零命中**。
- ⛔ 零既有文件改动：`git diff --name-only web/src | rg -v "^web/src/components/blueprint/"` 输出**为空**。
  </acceptance_criteria>
  <done>`BlueprintBlock.vue` 成为批注与引用的唯一实现点：五类块分发正确、字符区间 `<mark>` 全部经 115-02 的纯函数、`table`/`mermaid`/越界三档统一走整块色条且不与失锚混淆、mermaid 的三条契约（prop 名 / 调用方判空 / 预览内退化）落地、零 `v-html`、零颜色字面量、emits 恰好三项且无编辑入口。</done>
</task>

<task type="auto">
  <name>Task 2: BlueprintBlockList.vue（段级三分支 + ⭐ 选区侦测唯一落点）+ 引用二级预览六件</name>
  <files>web/src/components/blueprint/BlueprintBlockList.vue, web/src/components/blueprint/CitationPreviewDialog.vue, web/src/components/blueprint/citation/CitationKnowledgePreview.vue, web/src/components/blueprint/citation/CitationCodePreview.vue, web/src/components/blueprint/citation/CitationCharterPreview.vue, web/src/components/blueprint/citation/CitationBlueprintPreview.vue, web/src/components/blueprint/citation/CitationFallback.vue</files>
  <read_first>.planning/phases/115-ui/115-UI-SPEC.md §7.4（选区 → 评论六步逐字，含 120ms 去抖、跨块提示、`quoted_text` 上限 500、`readonly` 时按钮不渲染）、§10.1（**分发表五行 + 兜底强制段 + 「为什么必须是任何非 2xx」那段注释**）、§18.2（预览内 chip 改 `RouterLink` 不开第二层）；.planning/phases/115-ui/115-02-SUMMARY.md（`rangeOffsets` / `offsetInFlatText` / `collectTextNodes` 的签名与 **A2 能力锁结论**；`getChunkAt` 的 `{chunks, usable}`；`getRepositoryCharter` 的失败返回）；web/src/pages/knowledge/index.vue L23-29 与 L160-200（`Dialog` + `DialogScrollContent` 的 import 组与受控四 ref 范式；⚠️ **其 catch 分支关弹窗 + toast 是反例**）；web/src/components/common/CompactEmptyState.vue **全文**（`icon` 收裸名 + 只有默认 slot）；web/src/components/ui/skeleton/index.ts（段骨架用它）；web/src/api/knowledge.ts 的 `getEntity` 签名与返回形状；server/repositories/charter_views.py 或等价（`repository-charter` 的响应键：定位 / owned_domains / boundaries / 落点偏好——按实测取，不猜）；web/src/components/**/MarkdownRenderer.vue（知识实体正文渲染复用它）</read_first>
  <action>
**① 新建 `web/src/components/blueprint/BlueprintBlockList.vue`**。

`props: { blocks, sectionPath, threads, citations, readonly, activeThreadId, showClosed, loading?, plainMermaid? }`；emits **透传 `BlueprintBlock` 的三项** + 新增一项 `cross-block-selection: []`。

**段级三分支**（形状照 `pages/knowledge/entities/[id].vue:117-132` 的 `<section>` 三分支）：`loading` ⇒ `~/components/ui/skeleton` 的段骨架（形状由调用方通过 slot 或 prop 指定，默认 3 条 `h-16`）；`blocks.length === 0` ⇒ 默认 slot（由段组件决定放 `CompactEmptyState` 还是不渲染）；否则 ⇒ `v-for` 渲染 `BlueprintBlock`（`:key="block.block_id"`，`threads` 传该 block 的分组结果，`citations` 原样透传）。

⭐ **选区侦测（本 plan 的第二个核心，唯一落点）**：
1. `onMounted` 给 `document` 挂 `selectionchange`，回调经 `useDebounceFn(fn, 120)`（`@vueuse/core` 已在依赖）；`onUnmounted` **必须解绑**（⛔ 漏解绑会在路由切换后继续触发）。
2. 回调里取 `window.getSelection()`；`selection.isCollapsed` 或 `rangeCount === 0` ⇒ 直接 return（不 emit、不清状态）。
3. 取 `range = selection.getRangeAt(0)`；用 `range.commonAncestorContainer` 向上找最近的 `[data-block-id]` 祖先：
   - 找不到、或该祖先**不在本 list 的根元素内** ⇒ return（这是别的段/别的组件的选区，⛔ 不抢）；
   - `range.startContainer` 与 `range.endContainer` 各自向上找到的 `[data-block-id]` **不是同一个** ⇒ emit `cross-block-selection`（由页面渲染 toast `t('knowledge.blueprints.annotation.crossBlock')` =「评论只能针对同一段落内的文字，请缩小选区」），return。
4. 同块 ⇒ `blockRoot` 为该 `[data-block-id]` 元素；`startOffset = rangeOffsets(range, blockRoot).start`、`endOffset = ...end`（115-02 的 `rangeOffsets` 内部走 `collectTextNodes` + `offsetInFlatText`）；`quotedText = selection.toString().slice(0, 500)`（⭐ **上限 500 字符，超出截断**）。
5. emit `selection-comment` 载荷 `{ blockId, startOffset, endOffset, quotedText, rect }`，`rect = range.getBoundingClientRect()`（供 115-04 的 popover 定位虚拟锚点用；⚠️ happy-dom 下恒为 0 矩形，**这是已知且可接受的**——定位属 UAT）。
6. ⛔ **`readonly` 不在此处拦截**：本组件照常 emit，是否渲染「发起评论」按钮由 115-04 的 popover 依 `readonly` 决定（§7.9：`readonly` 时只留「复制原文」）。docstring 写明这条分工。

⭐ **定位方案的定夺（本 plan 落实 115-02 的决定，⛔ 执行期不得改选）**：选区 popover 走 **reka-ui `~/components/ui/popover` 的 `PopoverAnchor` + 零尺寸虚拟锚点 div**（`position: fixed` 到 `rect` 的坐标），⛔ **不引入 `@floating-ui/vue`**（全仓零调用点、API 形状 `[ASSUMED]`）。本组件只负责给出 `rect`；锚点 div 与 popover 本体归 115-04。

**② 新建引用二级预览六件。**

`CitationPreviewDialog.vue`：`props: { open: boolean, citation: Citation | null }`；`emits: { 'update:open': [boolean] }`。用 `Dialog` + `DialogScrollContent`（`class="w-[92vw] max-w-4xl"`，内容区 `max-h-[72vh] overflow-auto`）+ `DialogHeader` + **`DialogTitle`（必填，否则 reka-ui 报 a11y 警告）**；头部：`source_type` 徽标 + `title` 快照 + 右上「在新页面打开」（该来源有独立路由时）。按 `source_type` 分发到五个子件（`work_item`/`feishu_doc`/`url` 三类**根本不会进到这里**——chip 本身就是 `<a>`，见 Task 1）。

⭐ **兜底不留白（本组件最不能错的一条，且与 analog 相反）**：每个子件都接 `fallback: { title, quote }` prop；子件内部任何失败（含 400 / 404 / 5xx / 超时 / 解析失败 / ⭐ **`chunk-at` 的 `usable === false`**）一律渲染 `CitationFallback`。⛔ **不关弹窗**（analog `pages/knowledge/index.vue` 的 catch 是 `viewOpen.value = false` + toast，本相位**完全相反**）、⛔ **不回显后端错误体**（`chunk-at` 的错误体键是 `error`，`ApiError.detail` 会回落成无意义的 `'请求失败'`）。

- `CitationKnowledgePreview.vue`：`props: { entityId, fallback }`；`knowledgeApi.getEntity(entityId)`（TanStack Query，`queryKey: ['blueprint','citation','knowledge_entity',entityId]`，`staleTime: 5 * 60_000`）；渲染元数据卡 + 正文（复用既有 `MarkdownRenderer.vue`，⛔ 不新引渲染器）+「在知识库中打开」`RouterLink to="/knowledge/entities/{id}"`。
- ⭐ `CitationCodePreview.vue`：`props: { repositoryId, locator, fallback }`。**本相位降级形态**（115-02 已登记该 UI-SPEC §3.6/§10.1 订正）：
  - `locator.line_start` 缺失 ⇒ ⭐ **直接不发请求**，立刻渲染 `CitationFallback`（省一次注定 400 的往返：`chunk-at` 的 `path` 与 `line` 都是必填）；
  - 否则调 115-02 的 `getChunkAt(repositoryId, { path: locator.file_path, line: locator.line_start })`；`usable === false` ⇒ `CitationFallback`（⭐ **这一档包括「200 + 空 chunks」——它是最常见的一档：文件被 exclusion 规则排除 / chunk 未回填行号 / 分支名不匹配都会走到这里**，P-3）；
  - `usable === true` ⇒ 渲染 **文件路径面包屑 + `line_start..line_end` 行号区间徽标 + citation 的 `quote` 快照**（`<pre class="font-mono text-xs leading-6">` + 行号列，与 `pseudocode` 块同一套渲染）+ 命中多个 chunk 时一行「共 {n} 个片段」提示。⛔ **无源码正文、无 CodeMirror**——`chunks[]` 只有 `{chunk_id, file_path, line_start, line_end, chunk_index}`，全仓没有按 path+行区间取源码的读面（该读面归 Phase 116）。组件 docstring 逐字写明这条降级与理由。
- `CitationCharterPreview.vue`：`props: { repositoryId, locator, fallback }`；`getRepositoryCharter`（115-02 封装，失败返 `null` ⇒ 兜底）；分区渲染定位 / owned_domains / boundaries / 落点偏好（**键名按实测取，不猜**）；`locator` 指向某条时该条 2s ring 高亮。
- `CitationBlueprintPreview.vue`：`props: { artifactId, versionId, blockId, fallback }`；调 `getBlueprintDocument(artifactId, { version_id })`；⭐ **迷你只读渲染**：`meta.title` + `meta.summary`（经 `BlueprintBlockList`）+ **被引块**（按 `blockId` 从 `iterBlocks` 结果里定位）；一律传 `readonly`、`threads: []`（**无批注层**）、⭐ **`plainMermaid: true`**（P-12 次生：预览弹层内不渲染 mermaid）；⛔ 无操作栏；底部「打开完整蓝图」`RouterLink to="/knowledge/blueprints/{artifactId}"`。⭐ 预览内的 citation chip **不开第二层弹层**（§18.2）——传一个 `noNestedPreview` 语义（不接 `citation-click` 或把它转成 `RouterLink`）。
- `CitationFallback.vue`：`props: { title, quote }`；渲染 `title` + `<pre class="whitespace-pre-wrap text-sm">{{ quote }}</pre>` + 一行 `text-muted-foreground` `t('knowledge.blueprints.citation.fallback')` =「原始来源不可达，以下为引用时的快照」。⛔ 全程 mustache。若 `quote` 也为空 ⇒ 用 `CompactEmptyState`（**`icon="lucide--file-x"` 裸名**，按钮放**默认 slot**——⛔ 该组件没有 `action-label` prop 也没有 `@action` emit，P-6）。
  </action>
  <verify>
    <automated>cd web && pnpm type-check && pnpm lint</automated>
  </verify>
  <acceptance_criteria>
- `cd web && pnpm type-check` 与 `pnpm lint` 均通过。
- ⭐ **选区侦测唯一落点且解绑**：`rg -n "selectionchange" web/src/components/blueprint/` 命中**且只在 `BlueprintBlockList.vue`**（`rg -l "selectionchange" web/src/components/blueprint/ | wc -l` == 1）；`cd web && node -e "const s=require('fs').readFileSync('src/components/blueprint/BlueprintBlockList.vue','utf8'); if(!/onUnmounted|onBeforeUnmount/.test(s)) throw new Error('selectionchange 未解绑'); if(!/useDebounceFn|120/.test(s)) throw new Error('缺 120ms 去抖'); console.log('selection listener OK')"` 输出 `selection listener OK`。
- ⭐ **跨块分流**：`rg -n "cross-block-selection|crossBlock" web/src/components/blueprint/BlueprintBlockList.vue` 命中；`rg -n "slice\(0, 500\)|substring\(0, 500\)" web/src/components/blueprint/BlueprintBlockList.vue` 命中（`quotedText` 上限）。
- ⭐ **不引入 floating-ui**：`rg -n "@floating-ui/vue|useFloating" web/src/components/blueprint/` **零命中**。
- ⭐ **兜底不关弹窗（与 analog 相反）**：`cd web && node -e "const fs=require('fs'); for (const f of ['src/components/blueprint/CitationPreviewDialog.vue','src/components/blueprint/citation/CitationCodePreview.vue','src/components/blueprint/citation/CitationKnowledgePreview.vue','src/components/blueprint/citation/CitationCharterPreview.vue','src/components/blueprint/citation/CitationBlueprintPreview.vue']) { const s=fs.readFileSync(f,'utf8'); if(/emit\(['\"]update:open['\"],\s*false\)/.test(s) && /catch/.test(s)) { const i=s.indexOf('catch'); if(s.slice(i,i+300).includes('update:open')) throw new Error('catch 分支关掉了弹窗: '+f); } } console.log('fallback keeps dialog open')"` 输出 `fallback keeps dialog open`。
- ⭐ **P-3 判据**：`rg -n "usable" web/src/components/blueprint/citation/CitationCodePreview.vue` 命中；`rg -n "line_start" web/src/components/blueprint/citation/CitationCodePreview.vue` 命中且存在「缺 `line_start` 直接兜底不发请求」的分支（人工核对 + SUMMARY 登记）；`rg -n "ApiError|detail" web/src/components/blueprint/citation/CitationCodePreview.vue` **零命中**（⛔ 不回显错误体）。
- ⭐ **无 CodeMirror**：`rg -ni "codemirror" web/src/components/blueprint/` **零命中**。
- ⭐ **预览内不渲染 mermaid**：`rg -n "plainMermaid" web/src/components/blueprint/citation/CitationBlueprintPreview.vue` 命中且传 `true`（人工核对）。
- ⭐ **预览内 chip 不开第二层**：`cd web && node -e "const s=require('fs').readFileSync('src/components/blueprint/citation/CitationBlueprintPreview.vue','utf8'); if(/CitationPreviewDialog/.test(s)) throw new Error('预览内嵌套了第二层弹层（§18.2）'); console.log('no nested dialog')"` 输出 `no nested dialog`。
- ⭐ **`CompactEmptyState` 用法正确（P-6）**：`rg -n 'CompactEmptyState' web/src/components/blueprint/ -A 2 | rg -n 'icon="icon-\[' ` **零命中**（⛔ 不得传完整类名）；`rg -n "action-label|@action" web/src/components/blueprint/` **零命中**。
- ⭐ **不调 knowledge 关联端点**：`rg -n "getRelated|getArtifactAssociations" web/src/components/blueprint/` **零命中**（P-5）。
- `rg -n "DialogTitle" web/src/components/blueprint/CitationPreviewDialog.vue` 命中（a11y 必填）。
- ⛔ 零既有文件改动：`git diff --name-only web/src | rg -v "^web/src/components/blueprint/"` 输出**为空**。
  </acceptance_criteria>
  <done>选区侦测有唯一落点且带去抖与解绑、同块/跨块两档分流清晰、`quotedText` 有 500 上限、定位方案锁死为 reka-ui `PopoverAnchor`；引用二级预览六件就位，五类来源各有子件、任何失败一律 `CitationFallback` 且**弹窗保持打开不回显错误体**；代码预览的降级形态与理由写进组件 docstring；预览内既不渲染 mermaid 也不开第二层弹层。</done>
</task>

<task type="auto">
  <name>Task 3: 组件测试（五类块 / mark 计数 / 越界整块色条 / orphaned 不渲染 / 无编辑入口 / 选区 emit 正反 / 九档 source_type 分发 / 三条兜底并列）+ 源码守卫复跑</name>
  <files>web/src/components/blueprint/__tests__/BlueprintBlock.spec.ts, web/src/components/blueprint/__tests__/citationPreview.spec.ts</files>
  <read_first>web/src/components/prompts/__tests__/PromptVersionDiff.test.ts **全文 113 行**（⭐ 组件测试最规范范式：顶部「覆盖路径」编号清单、`makeVersion(overrides)` 工厂、**正/负向成对断言**（`expect(html).not.toContain(...)`）、`wrapper.findAll('[data-diff-column]')` 按 `data-*` 定位、`setProps` 验 watch、**真实 import 依赖不 mock**）；web/src/pages/knowledge/__tests__/entity-detail.spec.ts **全文 68 行**（`vi.mock('vue-router')` / `vi.mock('~/api')` / **手写最小 i18n 键树**（⛔ 不 import `zh-CN.json`）/ `mount` 的 `global.plugins` 与 `stubs` / `await new Promise(r => setTimeout(r, 50))`）；web/src/test/setup.ts **全文 37 行**（确认无 IntersectionObserver / fetch mock ⇒ 需要的一律自带 stub）；.planning/phases/115-ui/115-02-SUMMARY.md 的 A2 能力锁结论（决定选区用例是走真实 DOM 还是走 `offsetInFlatText` 直调）；.planning/phases/115-ui/115-UI-SPEC.md §20（断言 8/9/10 与 `data-testid` 命名清单）</read_first>
  <action>
**① 新建 `web/src/components/blueprint/__tests__/BlueprintBlock.spec.ts`**，顶部「覆盖路径」编号清单（照 `PromptVersionDiff.test.ts`），`makeBlock(overrides)` / `makeThread(overrides)` 两个工厂：

1. **五类块各渲染一条**：`paragraph` 出 `<p>`；`list` 出 `<ul>` 且 `<li>` 数 == 条目数；`table` 出 `<table>` 且 `<th>` 数 == `rows[0].length`；`pseudocode` 出 `<pre>` 且含 `code.source` 文本；`mermaid` 在 stub 下出 `MermaidDiagram` 组件（`stubs: { MermaidDiagram: true }`，⭐ **必 stub，否则要连带 `vue-final-modal` 插件**）。
2. ⭐ **`<mark>` 计数与属性**：一个 paragraph + 两条不重叠 anchored 线程 ⇒ `wrapper.findAll('[data-testid=blueprint-annotation-mark]')` 长度 == 2，且各自 `data-thread-id` 正确；**重叠**的两条 ⇒ 长度 == 3（切成不相交子段）且中间那段的 `aria-label` 提到 2 条。
3. ⭐ **越界整块色条（§20 断言 8）**：一条 `end_offset > blockText.length` 的 **anchored** 线程 ⇒ 该块出现 `[data-testid=blueprint-block-degraded]`、`[data-testid=blueprint-annotation-mark]` 计数 **== 0**；**负向对照**：把 offset 改合法 ⇒ 出现 mark 且不出现 degraded（证明断言非恒真）。
4. ⭐ **`table` / `mermaid` 强制整块**：给它们挂合法 offset 的线程 ⇒ 仍出 degraded、mark 计数 0。
5. ⭐ **`orphaned` 正文完全不渲染**：一条 `anchor_status === 'orphaned'` 的线程 ⇒ mark 计数 0 **且** degraded 也不出现（它连整块色条都不给——§7.3：正文完全不渲染任何标记）。
6. **`resolved`/`dismissed` 默认不着色**：`showClosed: false` ⇒ mark 计数 0；`showClosed: true` ⇒ 出现且类名含点线档（用 `annotationClass` 的真实返回断言，⛔ 不硬编码颜色）。
7. ⭐ **无编辑入口（§20 断言 10 的组件级对照）**：渲染后 `wrapper.html()` **不含** `edit` 相关的 `data-testid` / 文案；`wrapper.emitted()` 的键集 ⊆ 三项。
8. **citation chip**：块带两个 citation id（其中一个在池中不存在）⇒ chip 数 == 1（⛔ 不渲染 `undefined`）；`source_type: 'url'` 的 chip 是 `<a target="_blank">` 且**点击不 emit `citation-click`**；`source_type: 'knowledge_entity'` 的 chip 点击 **emit 一次**。
9. ⭐ **`blockText` 同源（P-13 的组件级对照）**：一个「`type: pseudocode` 且 `text` 非空」的块 ⇒ 渲染出的可划线文本是 **`text`** 而不是 `code.source`（与 115-02 的纯函数用例互为双保险）。
10. **选区 emit（`BlueprintBlockList` 上测）**：依据 115-02 的 A2 结论二选一——(a) 支持 `Range` ⇒ 构造真实选区断言 emit `selection-comment` 的载荷四键；(b) 不支持 ⇒ 直接调组件暴露的内部装配函数或用 `offsetInFlatText` 手工造 payload，断言**跨块时 emit `cross-block-selection` 而非 `selection-comment`**（这一档无论如何都能测：两个 block 根元素分别包含 start/end）。

**② 新建 `web/src/components/blueprint/__tests__/citationPreview.spec.ts`**：

1. **九档 `source_type` 分发**：六类进弹层的各断言渲染出对应子件（stub 子件用 `stubs: { CitationKnowledgePreview: true, ... }` 后断言存在性）；三类外链在 chip 层就走 `<a>`（在 `BlueprintBlock.spec.ts` 第 8 条已覆盖，此处只断言弹层**不为它们打开**）。
2. ⭐ **三条兜底并列**（本文件头号靶子）：(a) `getEntity` 成功 ⇒ 渲染正文、**不出现** `CitationFallback`；(b) `getEntity` 抛 404 ⇒ 出现 `CitationFallback` 且 `open` 仍为 `true`（**弹窗未被关掉**）；(c) ⭐ `getChunkAt` 返回 `{ chunks: [], usable: false }`（**200-空 chunks**，P-3 最常见的一档）⇒ 出现 `CitationFallback`。**三条并列才逮得住「只判非 2xx」的实现**。
3. **`locator.line_start` 缺失 ⇒ 不发请求**：`vi.fn()` 的 `getChunkAt` **`toHaveBeenCalledTimes(0)`** 且直接出 `CitationFallback`。
4. **不回显后端错误体**：让 mock 抛 `new ApiError(400, '请求失败', { error: '缺少必填参数 path' })` ⇒ 渲染文本**不含**「缺少必填参数」也不含「请求失败」。
5. **`CitationFallback` 的 `quote` 也为空** ⇒ 出 `CompactEmptyState`（stub 后断言存在）。
6. **`CitationBlueprintPreview` 不嵌套**：渲染后 `wrapper.findComponent({ name: 'CitationPreviewDialog' })` 在其子树内**不存在**；且传给 `BlueprintBlockList` 的 `plainMermaid` 为 `true`、`threads` 为空数组（用 `findComponent(...).props()` 断言）。

**③ 源码守卫复跑**：`pnpm exec vitest run src/__tests__/blueprint-source-guard.spec.ts` —— ⭐ 本 plan 之后 `web/src/components/blueprint/**` 首次非空，断言 6（`refetchInterval` 零命中）与断言 10（`edit-block` 零命中）**首次真正生效**。
  </action>
  <verify>
    <automated>cd web && pnpm exec vitest run src/components/blueprint/__tests__ src/__tests__/blueprint-source-guard.spec.ts</automated>
  </verify>
  <acceptance_criteria>
- `cd web && pnpm exec vitest run` **全绿**（含 115-02 的全部 spec 与既有 ~50 个 spec，无新增失败）。
- `rg -c "it\(|test\(" web/src/components/blueprint/__tests__/BlueprintBlock.spec.ts` ≥ **14**；`rg -c "it\(|test\(" web/src/components/blueprint/__tests__/citationPreview.spec.ts` ≥ **10**。
- ⭐ **必 stub mermaid**：`rg -n "MermaidDiagram: true|MermaidDiagram:" web/src/components/blueprint/__tests__/BlueprintBlock.spec.ts` 命中。
- ⭐ **越界 ≠ 失锚 三条并列**：`rg -n "blueprint-block-degraded" web/src/components/blueprint/__tests__/BlueprintBlock.spec.ts` 命中 ≥3（越界出 degraded / 合法 offset 不出 degraded / orphaned 连 degraded 都不出）。
- ⭐ **P-13 组件级对照存在**：`rg -n "pseudocode" web/src/components/blueprint/__tests__/BlueprintBlock.spec.ts` 命中，且该用例断言取到的是 `text` 的内容（人工核对，SUMMARY 登记用例名）。
- ⭐ **三条兜底并列**：`cd web && node -e "const s=require('fs').readFileSync('src/components/blueprint/__tests__/citationPreview.spec.ts','utf8'); for (const k of ['usable: false','CitationFallback','404']) if(!s.includes(k)) throw new Error('缺兜底用例要素: '+k); if(!/toHaveBeenCalledTimes\(0\)/.test(s)) throw new Error('缺「缺 line_start 不发请求」断言'); console.log('fallback trio OK')"` 输出 `fallback trio OK`。
- ⭐ **不回显错误体的用例存在**：`rg -n "缺少必填参数|请求失败" web/src/components/blueprint/__tests__/citationPreview.spec.ts` 命中且是 `not.toContain` 形态（人工核对）。
- ⭐ **源码守卫首次在非空扫描面下转绿**：`pnpm exec vitest run src/__tests__/blueprint-source-guard.spec.ts` 绿，且在 SUMMARY 登记「扫描面此时含 N 个文件」（证明不是空集合平凡通过）。
- `cd web && pnpm lint` 与 `pnpm type-check` 通过。
- ⛔ 零既有文件改动：`git diff --name-only web/src | rg -v "^web/src/components/blueprint/"` 输出**为空**；`git diff --name-only server/` 输出**为空**。
  </acceptance_criteria>
  <done>块渲染与批注可视层的九条关键行为（五类块 / mark 计数与重叠切分 / 越界整块 / table-mermaid 强制整块 / orphaned 不渲染 / 已关闭默认不着色 / 无编辑入口 / chip 分流 / `blockText` 同源）各有正反成对断言；引用预览的三条兜底并列用例把「只判非 2xx」的错误实现逼红；源码守卫首次在非空扫描面下生效。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 后端文本（block 正文 / citation `quote` `title` / 知识实体正文）→ DOM | 本相位唯一的 XSS 面；一处 `v-html` 即失守 |
| 前端 offset 坐标系 → `<mark>` 位置 | 与后端不一致会「圈错字」且不报错、不降级 |
| 引用来源的失败响应 → 预览弹层 | 回显后端错误体会泄露路径/内部细节；空壳渲染会让人以为「这里本来就没内容」 |
| `chunk-at` 的「200 + 空 chunks」 | 刻意不可区分的存在性防线；前端若按状态码判成功即渲染空壳 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115-21 | Tampering | `BlueprintBlock` / `CitationFallback` 用 `v-html` 渲染半可信文本 ⇒ 存储型 XSS | mitigate | 全程 mustache + `<pre>`；`rg -n 'v-html' web/src/components/blueprint/` 零命中断言；切分函数返回结构化数组而非 HTML 串 |
| T-115-22 | Tampering | 组件内自行按 `block.type` 取文本 ⇒ 与后端坐标系不一致，`<mark>` 圈错字（不报错、不降级） | mitigate | 一律 `blockText(block)`；源码扫描断言「script 段内无 type-dispatch 取文本」；「pseudocode 同时带 text 与 code.source」的组件级用例（P-13） |
| T-115-23 | Information Disclosure | 预览弹层回显 `chunk-at` / `charter` 的后端错误体 ⇒ 泄露内部路径与参数校验细节 | mitigate | 兜底只渲染 citation 自带快照；`CitationCodePreview` 内 `ApiError`/`detail` 零命中；专门一条「渲染文本不含错误体」用例 |
| T-115-24 | Information Disclosure | `chunk-at` 200-空 chunks 被当成成功 ⇒ 渲染空壳代码区，用户误判「该文件确实没有这段」 | mitigate | 判据封装在 115-02 的 `usable`；本 plan 只消费它；三条兜底并列用例（P-3） |
| T-115-25 | Denial of Service | `selectionchange` 监听未解绑 ⇒ 路由切换后持续触发、内存泄漏 | mitigate | `onUnmounted` 必解绑 + 源码扫描断言；120ms 去抖限制频率；监听器只在 List 层挂一个（⛔ 不是每块一个） |
| T-115-26 | Denial of Service | 在 reka-ui `Dialog` 内渲染 `MermaidDiagram` 并点放大 ⇒ vue-final-modal 与 reka-ui 两套模态栈叠放竞争，页面卡死焦点 | mitigate | `plainMermaid` prop + 预览弹层内一律传 `true`；测试断言该 prop 传值（P-12 次生） |
| T-115-27 | Tampering | 越界降级的线程被误归入失锚组 ⇒ 「批注错位」清单被噪声淹没，真失锚被忽略 | mitigate | 正文侧：越界出 degraded、orphaned 完全不渲染；侧栏侧由 115-02 的 `sidebarGroups` 保证；三条并列用例（P-7 / §20 断言 8） |
| T-115-28 | Spoofing | 前端自造第三套 DOM 标识（如用 `section_path` 作 id）⇒ 与 114 的重锚定结果错位 | mitigate | DOM id 一律 `blk-${block_id}`；源码核对 `section_path` 不参与 `:id=` |
| T-115-SC | Tampering | 依赖供应链 | accept | 零新增依赖；⛔ 不引入 `@floating-ui/vue` 的首个调用点（改用既有 reka-ui `PopoverAnchor`）；⛔ 不引入 CodeMirror 只读封装（改用 `<pre>` + 行号） |
</threat_model>

<verification>
- `cd web && pnpm exec vitest run` 全绿（新增两个 spec + 115-02 的五个 spec + 既有 ~50 个）。
- `cd web && pnpm type-check` 与 `pnpm lint` 通过。
- `rg -n "v-html" web/src/components/blueprint/` 零命中。
- `rg -n "edit-block|edit-blocks|editBlocks" web/src/components/blueprint/` 零命中；`rg -n "refetchInterval" web/src/components/blueprint/` 零命中（源码守卫 spec 已覆盖，此处二次核对）。
- `rg -n "hsl\(" web/src/components/blueprint/*.vue web/src/components/blueprint/citation/*.vue` 零命中（颜色只在 `annotationTokens.ts`）。
- `rg -n "@floating-ui/vue|useFloating|codemirror" web/src/components/blueprint/` 零命中。
- `git diff --name-only web/src | rg -v "^web/src/components/blueprint/"` 输出为空；`git diff --name-only server/` 输出为空；`git diff web/package.json web/pnpm-lock.yaml` 输出为空。
</verification>

<success_criteria>
- **批注与引用只有一个实现点**：`BlueprintBlock.vue`。后面九个 section 组件（115-05）只需透传 `blockCtx`，不会各自长出第二套划线逻辑。
- **越界 / 失锚 / 无 anchor 三态在正文侧被正确区分**（整块色条 / 完全不渲染 / 无标记），且有正反成对用例把「归错组」逼红。
- **CLAR-01 后半句有了起点**：选区侦测落在唯一位置、带去抖与解绑、同块与跨块两档分流、`quotedText` 有 500 上限、定位方案锁死为 reka-ui `PopoverAnchor`（绕开 `@floating-ui/vue` 的 `[ASSUMED]`）。
- **VIEW-02 的引用二级预览可用且永不留白**：五类来源各有子件，任何失败（含最常见的 `chunk-at` 200-空 chunks）一律渲染 citation 快照、**弹窗保持打开、不回显后端错误体**；代码预览的降级形态与理由写进组件 docstring 并已在 115-02 登记订正。
- **三条 mermaid 契约落地**（prop 名 `code` / 调用方判空 / 预览内退化为源码），避免了「空框」「传不进去」「两套模态栈叠放」三个具体错误。
- 零既有文件改动、零新增依赖、零 `v-html`、零颜色字面量；源码守卫首次在非空扫描面下生效。
</success_criteria>

<output>
完成后创建 `.planning/phases/115-ui/115-03-SUMMARY.md`，provides 至少包含：
- `BlueprintBlock.vue` 与 `BlueprintBlockList.vue` 的**最终 props/emits 逐字**（含本 plan 对 UI-SPEC §6.2 的**两处扩写**：`thread-click` 增加第二个 `allThreadIds` 参数、新增 `showClosed` 与 `plainMermaid` 两个 prop——**逐条登记理由**）；
- **DOM 契约表**（`blk-${block_id}` / `data-block-id` / 全部 `data-testid` 命名清单），115-04/05/06 的组件测试按它定位；
- **五类块的渲染形态与「是否可字符级划线」对照表**（`pseudocode` 可、`table`/`mermaid` 不可、越界一律整块）；
- 选区侦测的**完整契约**：监听位置、去抖、同块/跨块判据、emit 载荷五键、`rect` 的用途与 happy-dom 下恒 0 的已知限制；
- 引用预览六件的**分发表与兜底判据**，尤其 `CitationCodePreview` 的**降级形态**（路径 + 行号 + quote，⛔ 无源码正文）与该结论的证据链；
- ⭐ **UAT 清单**（happy-dom 测不了的）：选区 popover 的实际定位、mermaid 实渲、`<mark>` 的颜色对比度、焦点环可见性、代码预览的行号对齐；
- 源码守卫 spec 在本 plan 后的**扫描面文件数**（证明断言 6/10 不是空集合平凡通过）。
</output>
