---
phase: 115-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/delivery/api/blueprint_doc_views.py
  - server/delivery/api/blueprint_list_views.py
  - server/delivery/services/blueprint_comment_action.py
  - server/delivery/urls.py
  - server/tests/delivery/test_blueprint_log_redaction_guard.py
  - server/tests/delivery/test_blueprint_doc_views.py
  - server/tests/delivery/test_blueprint_list_views.py
autonomous: true
requirements: [VIEW-01, VIEW-03, VIEW-04, CLAR-01]
prohibitions:
  - "🔒 **`server/delivery/api/blueprint_review_views.py` 零改动**（`git diff` 输出必须为空）：范围闸与三个只读装配 helper 一律 **import 复用**（`from delivery.api.blueprint_review_views import _aassert_project_scope, _aload_artifact, _aload_session, _alatest_content`），⛔ 不提取共享模块、⛔ 不复制第三份实现。理由：提取会改既有文件（115 的前端 CREATE-ONLY 精神对后端同样适用于「能不改就不改」），复制会让 MJ-03 的四条语义出现第三个可漂移副本"
  - "⛔ **响应体与 ORM 过滤都不得出现字面 `blueprint_status`**（P-1）：`server/tests/delivery/test_blueprint_inv6_guard.py:57,61` 的 `_RE_FIELD_WRITE`（`\\bblueprint_status\\s*=\\s*[^=]`）与 `_RE_FIELD_DICT_KEY`（`['\\\"]blueprint_status['\\\"]\\s*:`）扫全 `server/`（豁免只有 writer / tests / migrations）。列表端点响应键一律 **`current_status`**；ORM 过滤走模块常量 `_STATUS_FIELD = \"blueprint_status\"` + `filter(**{_STATUS_FIELD: value})`。⛔ **绝不为迁就命名去豁免守卫**（STATE 114-05 纪律）"
  - "⛔ **`delivery/api/` 下的任何新文件都不得同时出现 `aunresolved_blocker_count` 与（`BlueprintStatus.CONFIRMED` 或 `to_status=\\\"confirmed\\\"`）**（P-2）：`test_blueprint_review_threads.py:365-392` 扫 `delivery/api/` 整目录，同现即判 TOCTOU。列表端点的 `unresolved_blocker_count` 一律 ORM `annotate(Count(...))` 自算，⛔ 不 import 那个符号"
  - "禁止视图裸写 ORM（INV-6）：`POST threads/` 的落库全部委托新 service `blueprint_comment_action`，并且该 service 的唯一写口是 `BlueprintLifecycleService.open_thread`。⛔ `BlueprintThread.objects.create(...)` / 裸 `BlueprintThread(...)` / `.save()` 三种形态在两个新 view 文件与新 service 中一律零命中（`_RE_ORM_WRITE` / `_RE_INSTANTIATE` / `_RE_INSTANCE_SAVE`）；⛔ **不把新 service 加进 `test_blueprint_inv6_guard._ALLOWED_WRITER`**——唯一 writer 必须保持是 `blueprint_lifecycle_service.py`"
  - "禁止同步 `rest_framework.views.APIView`：一律 `from adrf.views import APIView`；每个 View 逐个声明 `permission_classes = [IsAuthenticated]`"
  - "禁止 DRF serializer：本家族全域手写 dict builder（`blueprint_review_views` 零 serializer）。混进 serializer 会让同一响应里出现两套 None/空串归一口径"
  - "禁止改 `ArtifactListView`（`delivery/api/artifact_views.py`）：它无项目可见性过滤、返裸数组无分页，且已被 `web/src/components/delivery/ArtifactTimeline.vue:43` 消费——挂闸即改既有面行为。`git diff server/delivery/api/artifact_views.py` 必须为空"
  - "禁止用 `knowledge.access_scope.resolve_allowed_project_ids`：它返回的是**可见 Space id** 不是 project id（`knowledge/api/artifact_overview.py:7,26` docstring 逐字）。列表端点的可见集合一律 `initiatives.models.ProjectMember.objects.filter(user=request.user)`（与 `blueprint_review_views._ais_project_member:244-251` 同源）+ superuser 直通"
  - "禁止对列表候选逐条跑 `_aassert_project_scope`（N 次 meta 提取 + N 次 `aexists()`）：列表口径是「先算我能看的项目集合，再过滤」，与 artifact 级端点的「拿着 id 问能不能看」是两种口径，不得互相套用"
  - "禁止 `?version_id=` 走 `_alatest_content`（它只取最新）：需新写 `_aload_version(artifact_id, version_id)`；`is_current` 判据一律 `artifact.current_version_id == version.id`，⛔ 不用「version_no 最大」二次推断（并发落版本时两者会短暂不一致，而前端「回到当前版本」按钮依赖它）"
  - "禁止在端点侧把 `quality` 的 `None` 改写成 0：`ai_rejection_rate` / `human_edit_volume` / `clarification_rounds` 三项内部已各自 `try/except` 全兜（`blueprint_quality.py:150/172/196`），端点**不再包一层**。`None` 是「无数据」，0 是「零值」，两者在人审面板是不同文案"
  - "禁止 events 端点在无会话时返 404：会话不存在是**正常态**（蓝图还没跑过编排），一律 200 + `{session_id: \"\", current_stage: \"\", events: []}`。404 会被前端 §8.2 的 404 分档吞成全页中性空态"
  - "禁止反查会话不带 `process_type` 过滤：一律走既有 `_aload_session`（`blueprint_review_views.py:106-126`，其 docstring 已把这条记为 CRITICAL）。同一 artifact 上可能并存 `technical_plan` 与 `technical_blueprint` 两条会话"
  - "禁止把正文写进日志（T-114-36）：`POST threads/` 的 `body`、`GET blueprints/` 的 `?q=`、线程 `messages[].body`、citation `quote` 一律**只记长度不记内容**；任何 `error=` 实参必须过 `redact_secrets_in_text` 或本模块的 `_detail` helper"
  - "禁止改 `server/services/process_runtime/blueprint_quality.py` / `blueprint_schema.py` / `blueprint_anchor.py` / `event_taxonomy.py` / `delivery/services/blueprint_lifecycle_service.py` / `blueprint_review_action.py`（受限面，本 plan 只读消费）"
  - "禁止新增 migration：本 plan 是纯读面 + 一个写线程的薄 service，`makemigrations --check --dry-run` 退出码必须 0 且相位内零新 migration 文件"
must_haves:
  truths:
    - "新建 `server/delivery/api/blueprint_doc_views.py` **三个 View / 四个端点**：`GET artifacts/<uuid:artifact_id>/blueprint/`（name=`blueprint-document`）、`GET artifacts/<uuid:artifact_id>/blueprint/events/`（name=`blueprint-events`）、`GET`+`POST artifacts/<uuid:artifact_id>/blueprint-review/threads/`（name=`blueprint-review-threads`，同一集合资源的两个 HTTP 方法，**不是** `?action=` 分派）"
    - "新建 `server/delivery/api/blueprint_list_views.py` 一个 View：`GET /api/delivery/blueprints/`（name=`blueprint-list`）"
    - "⭐ **五端点全部挂 `_aassert_project_scope`（MJ-03 对称面）**：非成员 → **中性 404 且响应体与「artifact 不存在」逐字相同**；读不到 `meta.project_id` → **400**；superuser 直通。列表端点是同一语义的集合形态（只列调用者是成员的项目的蓝图，superuser 见全部）"
    - "⭐ **五端点未认证一律拒**（401/403），参数化断言无一例外"
    - "⭐ **正文端点返回结构化 content 而不是 markdown 串**：`{version_id, version_no, is_current, produced_by_ref, created_at, content, quality}`；`?version_id=` 取历史版本时 `is_current == false`；`is_current` 判据是 `artifact.current_version_id == version.id`"
    - "⭐ **`quality.ai_rejection_rate` / `human_edit_volume` / `clarification_rounds` 无数据回 `null` 不是 0**（三态并列用例：`null` / `0` / 正值）；`citation_coverage` 恒有值（分母为 0 时后端返 1.0）。三项同步函数经**一个** `@sync_to_async def _acollect_quality(artifact_id)` 一次性算完（P-15：直调会 `SynchronousOnlyOperation`）"
    - "⭐ **events 端点只出 21 个 `BLUEPRINT_EVENTS`**（`from delivery.services.event_taxonomy import BLUEPRINT_EVENTS`，`len == 21` 已有测试锁）、按 `ts` **显式升序**（`.order_by(\"ts\")` 覆盖 `Meta.ordering = [\"created_at\"]`）、payload 原样透传；**无会话回 200 空结构不是 404**"
    - "⭐ **threads GET 在 `_thread_row` 九键之上补三项**：`options`（逐项 `.get` 防御）/ `last_reminded_at` / `messages[]`；`messages[].author` 是 `SET_NULL` FK ⇒ `author_display` 容忍 `None` 不炸；N+1 走 `prefetch_related(Prefetch(\"messages\", queryset=...select_related(\"author\")))`；线程显式 `.order_by(\"created_at\")`（`BlueprintThread.Meta` 无 `ordering`）"
    - "⭐ **threads POST 建 `human_comment` 线程**：`kind=ThreadKind.HUMAN_COMMENT`、`blocking=False`、`severity=\"\"`、`created_on_version=` 当前最新版本、`initiated_by_user_id=<uid>`；`body` 空 → **400 且线程数不变**；蓝图状态 ∉ `EDITABLE_BLUEPRINT_STATUSES` → **400 且线程数不变**（与 `answer` 同闸，判据函数 `is_blueprint_editable` + 文案常量 `NOT_EDITABLE_DETAIL` 一律复用不自造）"
    - "⭐ **列表端点响应键是 `current_status` 不是 `blueprint_status`**，且源码内不出现字面 `blueprint_status=` / `\"blueprint_status\":`（ORM 过滤走 `_STATUS_FIELD` 常量）—— 与 INV-6 守卫互为双保险：改回去两条测试同时转红"
    - "列表端点分页体是 `artifact_overview` 五键 `{total, items, page, page_size, has_next}`（**不是 DRF 分页体**）；`_parse_page` / `_parse_page_size` clamp 到 `[1, 100]` 且非法值 fail-soft"
    - "列表可见性 fail-closed：可见项目集合为空 → 直接返空结构 + completed 日志，**零 DB 越权查询**；`blueprint_status == \"\"` 的旧 artifact 不出现在列表里"
    - "`thread_count` / `unresolved_blocker_count` 走 ORM `annotate(Count(...))`；⛔ 不 import `aunresolved_blocker_count`（P-2）"
    - "五端点各记一条 `caller` 结构化事件（`category=\"caller\"` + `component` + `artifact_id` + `initiated_by_user_id` + `duration_ms`），只读 GET 也记（`blueprint_review_snapshot_read` 是先例）；⛔ 正文/`?q=` 只记长度"
    - "新增的三个模块加进 `test_blueprint_log_redaction_guard._SCANNED_MODULES`（该文件 `:14` docstring 明令），且 AST 守卫全绿"
    - "全量后端门：`cd server && uv run pytest tests/ -q` 相对基线（**8546 passed / 1 failed**，唯一失败是 `tests/mcp_tools/test_skills_snapshot_guard.py::test_skill_files_discovered` 的 worktree 环境现象）**无新增失败**；`makemigrations --check --dry-run` 退出码 0 且零新 migration"
  artifacts:
    - path: "server/delivery/api/blueprint_doc_views.py"
      provides: "正文 / 事件 / 线程 GET+POST 四端点；adrf APIView + IsAuthenticated + import 复用 _aassert_project_scope + 手写 dict builder + _acollect_quality 的 sync_to_async 合成 + caller 埋点"
      contains: "_aassert_project_scope"
    - path: "server/delivery/api/blueprint_list_views.py"
      provides: "GET /delivery/blueprints/：ProjectMember 可见集合 + 方案 A 先聚合再切片 + _STATUS_FIELD 常量过滤 + current_status 响应键 + 五键分页体"
      contains: "current_status"
    - path: "server/delivery/services/blueprint_comment_action.py"
      provides: "aopen_selection_comment（选区评论建 human_comment 线程，经 BlueprintLifecycleService.open_thread，恒定返回键 + 状态闸 + 正文不进日志）"
      contains: "open_thread"
    - path: "server/tests/delivery/test_blueprint_doc_views.py"
      provides: "四端点鉴权 + 中性 404 正反并列 + 400 无 project_id + 历史版本 is_current=false + quality 三态并列 + events 无会话 200 空结构 + events 只含 BLUEPRINT_EVENTS 且 ts 升序 + threads GET 三补键与 author=None + threads POST 不可编辑态 400 且线程数不变 + 视图零 ORM 写源码扫描"
      contains: "blueprint-review-threads"
    - path: "server/tests/delivery/test_blueprint_list_views.py"
      provides: "鉴权 + 只列成员项目的蓝图（两项目正反并列）+ superuser 全见 + blueprint_status 空串不出现 + 五键分页与 has_next 边界 + ?q= 命中标题与摘要各一例 + ?repository_id= 过滤 + 响应键是 current_status 的防回归断言"
      contains: "current_status"
    - path: "server/delivery/urls.py"
      provides: "四条 path 追加（blueprints/ 顶层字面段单列 + 三条 artifacts/<uuid>/ 下的蓝图读面），删除行 0"
      contains: "blueprint-document"
  key_links:
    - from: "server/delivery/api/blueprint_doc_views.py"
      to: "server/delivery/api/blueprint_review_views.py"
      via: "import 复用 _aassert_project_scope / _aload_artifact / _aload_session / _alatest_content（MJ-03 的四条语义单一实现，既有文件零改动）"
      pattern: "from delivery.api.blueprint_review_views import"
    - from: "server/delivery/api/blueprint_doc_views.py"
      to: "server/services/process_runtime/blueprint_quality.py"
      via: "_acollect_quality 一次性算三项同步统计（sync_to_async 包裹，None 原样透传）"
      pattern: "clarification_rounds"
    - from: "server/delivery/api/blueprint_doc_views.py"
      to: "server/delivery/services/event_taxonomy.py"
      via: "BLUEPRINT_EVENTS（21 常量）作为 events 端点的唯一过滤集合"
      pattern: "BLUEPRINT_EVENTS"
    - from: "server/delivery/api/blueprint_doc_views.py"
      to: "server/delivery/services/blueprint_comment_action.py"
      via: "POST threads/ → aopen_selection_comment → BlueprintLifecycleService.open_thread（INV-6：视图零 ORM 写）"
      pattern: "aopen_selection_comment"
    - from: "server/delivery/api/blueprint_list_views.py"
      to: "server/initiatives/models.py"
      via: "ProjectMember.objects.filter(user=request.user) 算可见项目集合（fail-closed，空集合直接返空结构）"
      pattern: "ProjectMember"
---

<objective>
把「前端能看见蓝图」所需的**五个只读/轻写 REST 端点**一次性补齐——这是整个 115 相位的供数面，其余六个 plan 全部消费它：

① **`GET artifacts/<uuid>/blueprint/`（正文 + quality）** —— 全相位最关键的一条。实测 `blueprint-review/` 快照**不返回 content**（`_thread_row` 十二键无 content），`ArtifactTimelineView` 只给 `current_version_markdown`（**结构已丢**）⇒ 没有这个端点，block 锚定与 block 级 diff 从物理上无法实现，SC-1/SC-2 直接落空。顺带把 `blueprint_quality` 的三项 DB 统计接上第一个消费方（闭 STATE 登记的 114 review **MN-05：三项统计零消费方**）。

② **`GET artifacts/<uuid>/blueprint/events/`** —— `ConvergenceSessionEvent` 全仓既无 REST 也无 WS。阶段时间线与「生成中各段实时进展」两处都要它，且这是「**不新建推送通道**」这条并行纪律（§13.2 第 3 条）唯一可行的落地形态。

③④ **`GET` / `POST artifacts/<uuid>/blueprint-review/threads/`** —— 快照的 `_thread_row` 九键**既无 `options` 也无任何 `BlueprintThreadMessage`**，多轮回复根本没有数据；且全仓**没有**「按选区新开 `human_comment` 线程」的入口（唯一开人工评论线程的路径是 `reject/` 的副作用）。CLAR-01 的两半（多轮回复 / 主动选区评论）各缺一条。

⑤ **`GET /delivery/blueprints/`（列表）** —— VIEW-03 的 tab 与 VIEW-04 的项目物料卡共用。⛔ 不改 `ArtifactListView`（通用面、无项目闸、已被 `ArtifactTimeline.vue` 消费，挂闸即改既有面行为）。

Purpose: 五端点是本相位唯一必须新增的后端面，且每一个都有**已核实的必要性**（RESEARCH §A.0 五条主张逐条坐实）。它们同时把 MJ-03 的项目范围闸扩到蓝图读面的全部入口——STATE 明令「新增任何蓝图读写端点都要照挂 `_aassert_project_scope`」。
Output: 三个新建源文件（两 view + 一 service）+ 两个新建测试文件 + 两处纯追加（`delivery/urls.py` 四条路由、脱敏守卫的 `_SCANNED_MODULES`）。**零 migration、零新 SettingKeys、零新事件常量、零新依赖。**
</objective>

<execution_context>
工作目录：worktree 根 `/Users/zaneliu/Projects/open-source/friday-ai/.claude/worktrees/v0.20-blueprint`，分支 `milestone/v0.20.0-blueprint`。开工前 `git rev-parse --abbrev-ref HEAD` 复核。

**wave 1，无前置依赖**：111–114 的模型、service、守卫、事件常量全部已就位，本 plan 只读消费 + 新增读面。

**测试基线（执行前先复核，SUMMARY 登记前后值）**：`cd server && uv run pytest tests/ -q` 当前为 **8546 passed / 1 failed**；唯一失败是 `tests/mcp_tools/test_skills_snapshot_guard.py::test_skill_files_discovered`（本 worktree 的 `skills/` 是空目录，主检出里有内容——**纯环境现象，与蓝图无关，不得试图修它**）。收口断言是「无**新增**失败」。

**受限面配额（硬约束）**：

| 文件 | 允许的改动 | 删除行上界 | formatter |
|---|---|---|---|
| `server/delivery/urls.py` | import 块追加两条 import + `urlpatterns` 追加 **4** 项 + 两行分组注释 | **0** | 只 `ruff check` |
| `server/tests/delivery/test_blueprint_log_redaction_guard.py` | `_SCANNED_MODULES` 元组追加 **3** 行 | **0** | 只 `ruff check` |
| `server/delivery/api/blueprint_review_views.py` | **零改动**（`git diff` 必须为空） | — | — |

⚠️ 签名与字段名一律按下列**实测**取，不得猜测：

- `_aassert_project_scope(request, artifact) -> Response | None`（`blueprint_review_views.py:254`）；依赖 `_is_uuid`（`:223`）/ `_ablueprint_project_id`（`:233`）/ `_ais_project_member`（`:244`）/ `_alatest_content`（`:162`）。四条语义：superuser 直通（`:274`）→ 从最新版本 `meta.project_id` 取范围 → 非 UUID/缺失 **400** fail-closed（`:277-278`）→ 非 `ProjectMember` **中性 404**（`:279-280`）→ 放行返 `None`。
- `_aload_artifact(artifact_id)`（`:100`）/ `_aload_session(artifact_id)`（`:106`，**必带 `process_type=BLUEPRINT_PROCESS_TYPE`**）/ `_alatest_content(artifact)`（`:162`，`order_by("-version_no").afirst()`，非 dict 回 `{}`）。
- `_thread_row(thread)`（`:174`）九键：`thread_id / kind / severity / status / blocking / anchor_status / anchor / return_stage / created_at`，逐值 `str(x or "")` / `bool(x)` 归一。
- `_log(event, request, artifact_id, started, **fields)`（`:284-294`）：`category="caller"` + `component=_COMPONENT` + `artifact_id` + `initiated_by_user_id=... or "system"` + `duration_ms=round((time.monotonic()-started)*1000, 2)`。
- `BlueprintThread.options`（`models/blueprint_thread.py:101`，`JSONField(default=list)`，形状 `[{label, value, note}]`，**无 schema 校验**）/ `last_reminded_at`（`:118`，`null=True`）/ `BlueprintThreadMessage`（`:133-164`，`related_name="messages"`，`Meta.ordering = ["created_at"]`，`author` 是 `SET_NULL` FK `:146`）；`BlueprintThread.Meta`（`:120-127`）**无 `ordering`**。
- `BlueprintLifecycleService.open_thread(...)`（`blueprint_lifecycle_service.py:448`）——**全仓唯一合法开线程入口**；调用形状逐字见 `blueprint_review_action._aopen_reject_comment`（`:443-452`）。
- `is_blueprint_editable(artifact)` / `EDITABLE_BLUEPRINT_STATUSES`（`blueprint_lifecycle_service.py:92,107`）/ `NOT_EDITABLE_DETAIL`（`:104`，逐字「当前蓝图状态不允许改写正文，请先驳回或新建修订」）；既有用法见 `blueprint_review_views.py:636-649`。
- `BLUEPRINT_EVENTS`（`delivery/services/event_taxonomy.py:185-208`，frozenset，**21 个**，`test_blueprint_event_taxonomy_112.py:111` 用 `len(...) == 21` 锁死）。
- `BLUEPRINT_PROCESS_TYPE`（`services/process_runtime/blueprint_resume.py`）。
- `ConvergenceSessionEvent`（`models/convergence_session_event.py`）：`ts`（`:41`，`default=timezone.now`，**允许 emit 端传入** ⇒ 可与 `created_at` 不同）/ `payload`（`:39`，自由 JSONField）/ `Meta.ordering = ["created_at"]`（`:49`）/ 索引 `(session, ts)`（`:51`）。
- `blueprint_quality`：`citation_coverage(blueprint: dict) -> float`（`:68`，**同步纯函数**，入参是 content dict，分母为 0 → `1.0`（`:77`））；`ai_rejection_rate(artifact_id) -> float | None`（`:127`）/ `human_edit_volume(artifact_id) -> int | None`（`:157`）/ `clarification_rounds(artifact_id) -> int | None`（`:182`）——**后三项是同步函数且函数内直接查 ORM**（`:139/:167/:190`）。
- `Artifact`（`models/artifact.py`）：`current_version` 循环 FK（`:93`）、`blueprint_status`、复合索引 `Index(fields=["artifact_type", "blueprint_status"])`（`:123`）、`work_item` 可空（`:76-82`）、`title`（`:84`）；`ArtifactVersion`：`content`（`:149`）/ `content_hash`（`:151`）/ `produced_by_ref`（`:156`）/ `created_at`（`:164`），**无 `created_by_user_id`**。
- 分页范式：`knowledge/api/artifact_overview.py` 的 `_parse_page`（`:46`）/ `_parse_page_size`（`:54`，clamp `[1, _MAX_PAGE_SIZE]`）/ `_aggregate`（`:131`，纯同步 + `sync_to_async` 单点调用 `:226`）/ 响应五键（`:181-188`）/ fail-closed 短路（`:211-223`）/ 聚合整体 try 不 500（`:229-238`）。
- UUID query param 前置 400：`artifact_views._parse_uuid_param`（`:45-55`，返 `(值|None, 是否合法)` 二元组）。
</execution_context>

<context>
@.planning/phases/115-ui/115-RESEARCH.md（Part A 全节：五条主张核验、四端点的取版本/过滤/守卫口径、列表端点的三条可选路径与方案 A 判定、§A.5 范围闸复用而非复制、§A.7 观测三条；Part C 的 P-1 / P-2 / P-15 / P-16）
@.planning/phases/115-ui/115-PATTERNS.md（§0.2 三条守卫表、§A1/A2 的「逐条照抄」结构要点与四端点的「必须 DIFFER」表、§A3 列表端点的三条 DIFFER、§A4 service 范式、§A5 路由追加、§A6 两个测试文件的守护点清单、Shared Patterns S1/S2/S3/S4）
@.planning/phases/115-ui/115-UI-SPEC.md §3（五个新端点的响应形状契约；⚠️ §3.3 的 `blueprint_status` 键与「DRF 分页体」两处**由本 plan 定夺订正**，见 Task 2）
@server/delivery/api/blueprint_review_views.py（**主 analog，794 行**：L1-56 模块 docstring 的契约书体例（端点清单 → 授权判据 ⭐ 段 → 为什么新建文件 → INV-6 写入纪律 → 观测口径末段「评论正文、block 正文、答案正文、处置理由正文一律不进日志」）、L64-97 基类与三类模块级常量、L100-171 只读装配 helper（含 `_aload_session` 的 CRITICAL docstring 与 `_alatest_content` 的取基线口径）、L174-196 `_thread_row` 与 `_load_thread_rows`、L223-281 范围闸四语义、L284-294 `_log`、L325-337 共用前置三元组 helper、L357-414 GET 快照端点的完整骨架、L633-716 answer 端点的前六行状态闸与 body 取值）
@server/knowledge/api/artifact_overview.py（**列表端点主 analog，250 行**：L7,26 的「返回可见 Space id」docstring（**这一行是唯一不能照抄的**）、L46-62 两个 clamp 纯函数、L131 `_aggregate` 纯同步聚合、L181-188 响应五键、L202-248 started/completed 两条 caller 日志与整体 try、L211-223 fail-closed 短路）
@server/delivery/services/blueprint_review_action.py（L1-5 INV-6 模块 docstring、L72-81 `__all__` 与 `_COMPONENT`、L131-160 三个共用 helper（`_user_id` 回落 `"system"` / `_detail` 走 `redact_secrets_in_text` + 截断 500 / `_alatest_version`）、**L427-463 `_aopen_reject_comment` 全文**——全仓唯一「人工评论 → `human_comment` 线程」实现，本 plan 的 service 逐字照它的 `open_thread` 调用形状）
@server/delivery/urls.py（L1-45 import 块体例、L132-145 `artifacts/` 分组、L185-225 `blueprint-review/` 七条路由的分组注释与 `name` 命名纪律）
@server/tests/delivery/test_blueprint_review_views.py（L1-12 「守十四件事」docstring 体例、L33-34 `async_to_sync` 装配、L67 `pytestmark`、L69-70 源码扫描的 `SERVER_DIR` 定位、L83-87 `_SCOPE_PROJECT_ID` / `_OTHER_PROJECT_ID` / `_make_project(project_id, member=...)` 范围闸工厂（**本 plan 五端点的闸测试逐字复用**）、L73-75 懒 import 的 patch 目标注释）
@server/tests/delivery/test_blueprint_inv6_guard.py（L42-65 五条正则的**确切形态**——写代码前必须手工验算自己的每一行是否命中）
@server/tests/delivery/test_blueprint_log_redaction_guard.py（L14 docstring 的「新增蓝图模块请一并加进」、L27-37 `_SCANNED_MODULES`、L41 `_REDACTORS`、L44 `_TAINTED_KWARGS`）
@.planning/phases/114-ai/114-05-SUMMARY.md（七端点契约表、`_aassert_project_scope` 的最终语义、`produced_by_ref` 四前缀、三项统计的最终口径与「无版本→None / 零人工→0」的区分）
@.planning/STATE.md（§并行开发边界纪律六条；Accumulated Decisions 中 114-05 的三条：`current_status` 键名纪律 / 只读端点也记 caller 事件 / 无数据返 None）
</context>

<tasks>

<task type="auto">
  <name>Task 1: blueprint_doc_views.py 四端点（正文+quality / 事件 / 线程 GET+POST）+ blueprint_comment_action service + urls 前三条 + 脱敏守卫清单追加</name>
  <files>server/delivery/api/blueprint_doc_views.py, server/delivery/services/blueprint_comment_action.py, server/delivery/urls.py, server/tests/delivery/test_blueprint_log_redaction_guard.py</files>
  <read_first>server/delivery/api/blueprint_review_views.py **全文 794 行**（本 plan 的逐字 analog：docstring 契约书五段体例、模块级常量三类、`_aload_artifact` / `_aload_session` / `_alatest_content` 的懒 import 与取值口径、`_thread_row` 的手写 dict builder 与 `str(x or "")` 归一、`_is_uuid` / `_ablueprint_project_id` / `_ais_project_member` / `_aassert_project_scope` 四条语义、`_log` 五件套、L325-337 共用前置三元组 helper 的签名与返回约定、L357-414 GET 快照端点的完整骨架（`started = time.monotonic()` 是函数体第一行）、L633-716 answer 端点的状态闸与 body 取值前六行）；server/delivery/services/blueprint_review_action.py L1-90（模块 docstring 的 INV-6 首段 + `__all__` + `_COMPONENT`）、L131-160（`_user_id` / `_detail` / `_alatest_version` 三个共用 helper）、**L427-463 `_aopen_reject_comment` 全文**（`open_thread` 的逐字调用形状：`kind=ThreadKind.HUMAN_COMMENT` / `blocking=False` / `question=body` / `anchor=anchor if isinstance(anchor, dict) else None` / `created_on_version=version` / `return_stage=BlueprintStatus.DRAFTING`）；server/delivery/services/blueprint_lifecycle_service.py L88-110（`EDITABLE_BLUEPRINT_STATUSES` 的**确切成员**含 `""`、`NOT_EDITABLE_DETAIL` 逐字文案、`is_blueprint_editable(artifact)` 的入参是 **artifact 不是 status 字符串**）、L448-520（`open_thread` 的**逐字签名与全部 keyword**，含 `severity` 形参与 `created_on_version` 的类型）；server/delivery/models/blueprint_thread.py L69-164（`BlueprintThread` 全字段含 `options` `:101` / `last_reminded_at` `:118` / `Meta` `:120-127` **无 ordering**；`BlueprintThreadMessage` `:133-164` 含 `author` 的 `SET_NULL` `:146`、`author_type` 取值、`Meta.ordering` `:161`）；server/delivery/models/artifact.py L66-164（`current_version` 循环 FK `:93`、`blueprint_status`、复合索引 `:123`、`ArtifactVersion` 全字段 `:130-164`）；server/services/process_runtime/blueprint_quality.py L1-14 与 L60-200（模块 docstring 的「顶层零 ORM import」纪律 + `citation_coverage` 的**同步纯函数签名与分母 0 返 1.0** `:68-77` + 后三项的**同步签名与内部懒 import ORM** `:127/:157/:182` 与各自的 `except Exception` `:150/:172/:196`）；server/delivery/services/event_taxonomy.py L141-208（21 个 `BLUEPRINT_EVENTS` 常量与各 emit 点**预期** payload 键的注释——注释是预期不是契约，端点原样透传）；server/models/convergence_session_event.py **全文**（`ts` vs `created_at` 的差异、`payload` 自由 JSONField、`Meta.ordering` 与 `(session, ts)` 索引）；server/delivery/urls.py L1-45 与 L132-225（import 块字母序与括号换行体例、`artifacts/` 分组、`blueprint-review/` 分组注释的**确切措辞纪律**）；server/tests/delivery/test_blueprint_inv6_guard.py L42-65（五条正则的确切形态）；server/tests/delivery/test_blueprint_log_redaction_guard.py L14-44（docstring 的追加要求、`_SCANNED_MODULES` 的元组体例、`_REDACTORS` 与 `_TAINTED_KWARGS`）</read_first>
  <action>
**① 新建 `server/delivery/api/blueprint_doc_views.py`。**

模块 docstring 照 analog 的五段契约书体例：(a) 端点清单表（四端点 × URL × name × 用途）；(b) ⭐ 授权判据段——「四端点全部 `IsAuthenticated` + `_aassert_project_scope`（**import 复用 `blueprint_review_views` 的同源实现，绝不复制第三份**）：superuser 直通 / 读不到 `meta.project_id` → 400 fail-closed / 非 `ProjectMember` → **中性 404**（不泄露存在性，MJ-03）」；(c) 「为什么新建文件而不塞进 `blueprint_review_views.py`」——后者是**人审动作面**（七端点全是写路径 + 一个动作快照），本文件是**只读供数面**（前端渲染的正文/事件/线程详情），职责不同；且 115 的读面全部集中在一处便于 116 的入口收编按文件切；(d) INV-6 写入纪律——「本文件唯一的写路径是 `POST threads/`，其落库**全部**委托 `delivery/services/blueprint_comment_action.py`，View 零 ORM 写」；(e) 观测口径——逐字沿用「**评论正文、block 正文、答案正文、处置理由正文一律不进日志**」，并补一句「本文件另加：线程 `messages[].body` 与 citation `quote` 同样不进日志」。

基类与常量：`from adrf.views import APIView`；每个 View 声明 `permission_classes = [IsAuthenticated]`；模块级 `_COMPONENT = "blueprint_doc_api"`；中性 404 文案常量复用形态（`_ARTIFACT_MISSING_DETAIL` / `_VERSION_MISSING_DETAIL`，措辞与 analog 同款「不存在或无权访问」语义）；`_MAX_THREAD_MESSAGES` 之类上界常量若需要则按 analog `:94` 的体例声明。

复用 import（**这一行是本 plan 的关键交割**，且必须带注释说明为何 import 私有符号）：`from delivery.api.blueprint_review_views import _aassert_project_scope, _aload_artifact, _aload_session, _alatest_content`。注释逐字写明：「MJ-03 的四条范围闸语义只能有一份实现；提取共享模块要改既有文件、复制会产生可漂移的第三份副本，故直接 import 私有符号并在此登记」。

`_log(event, request, artifact_id, started, **fields)` 照 analog `:284-294` 在本文件内再声明一份（它是 5 行的埋点壳，`_COMPONENT` 不同，不跨文件复用）。

**四端点的实现要点：**

`BlueprintDocumentView.get(request, artifact_id)`：`started = time.monotonic()` 第一行 → `_aload_artifact` → None 则中性 404 → `_aassert_project_scope` → `version_id = request.query_params.get("version_id")`（有值但非 UUID → 400）→ 取版本：新写 `async def _aload_version(artifact_id, version_id=None)`——`version_id` 为空取 `ArtifactVersion.objects.filter(artifact_id=...).order_by("-version_no").afirst()`（⛔ **不读 `Artifact.current_version`**，理由是 STATE 114-04 立的「基线一律 `order_by("-version_no").afirst()`」纪律），有值则 `filter(artifact_id=..., id=version_id).afirst()`，取不到 → 404（用 `_VERSION_MISSING_DETAIL`）→ 装配响应七键 `version_id / version_no / is_current / produced_by_ref / created_at / content / quality`。`is_current = str(artifact.current_version_id or "") == str(version.id)`（⛔ 不用 version_no 最大推断）。`content` 取 `version.content if isinstance(version.content, dict) else {}`。

`quality` 四键：`citation_coverage` 直接同步调 `citation_coverage(content)`（纯函数，恒有值）；后三项合成**一个** `@sync_to_async def _collect_db_quality(artifact_id) -> dict`，内部依次调三个同步函数并原样返回（含 `None`）——P-15：三者内部直接查 ORM，在 adrf 异步 View 里直调必 `SynchronousOnlyOperation`，而它们各自的 `except Exception` **只兜 ORM 异常、不兜异步上下文错误**（那是在调用点抛的）⇒ 会直接 500 而不是被吞成 `None`。⛔ 端点侧不再包一层 try 把 `None` 改写成 0。

埋点：`_log("blueprint_document_read", request, artifact_id, started, version_no=..., is_current=..., block_count=<可选标量>)`。

`BlueprintEventsView.get(request, artifact_id)`：同前置三步 → `session = await _aload_session(artifact_id)`（**必带 `process_type` 过滤，走既有 helper 不自写**）→ session 为 None ⇒ **200** `{"session_id": "", "current_stage": "", "events": []}` 并照常 `_log`（⛔ 不 404）→ 有会话则 `ConvergenceSessionEvent.objects.filter(session_id=session.id, event__in=BLUEPRINT_EVENTS).order_by("ts")` 异步迭代装配 `events[]`，每项四键 `{id, event, payload, ts}`（`payload` 原样透传，非 dict 归一为 `{}`；`ts` 用 `.isoformat()`，None 归一空串）。`.order_by("ts")` 的**显式**调用要带注释：`Meta.ordering = ["created_at"]` 且 `ts` 允许 emit 端传入，两者可不同；显式 order 同时走上 `(session, ts)` 索引。`current_stage` 取会话上的当前 stage 字段（按 `ConvergenceSession` 实测字段名取，不猜）。埋点 `blueprint_events_read`（带 `event_count`）。

`BlueprintReviewThreadsView`（**一个 View，两个方法**）。在类 docstring 里写明：「这不违反 112 立的『一动作一 View』——那条禁的是 `?action=` / body 里带 `action` 的**动作分派**；`GET`/`POST` 是同一集合资源的两个 HTTP 方法，是 REST 的标准形态，且 URL 与既有 `threads/<uuid:thread_id>/<动作>/` 整段不同、互不遮挡」。

- `get`：前置三步 → `@sync_to_async` 的 `_load_thread_details(artifact_id) -> list[dict]`：`BlueprintThread.objects.filter(artifact_id=...).order_by("created_at").prefetch_related(Prefetch("messages", queryset=BlueprintThreadMessage.objects.select_related("author").order_by("created_at")))`。⚠️ `.order_by("created_at")` **不可省**（`BlueprintThread.Meta` 无 `ordering`；STATE 114 MN-01 专门为「无 ORDER BY 的窗口」立过纪律）。行序列化新写 `_thread_detail_row(thread)`——**在 `_thread_row` 九键之上扩写的同款手写 dict builder，⛔ 绝不引入 DRF serializer**（本文件家族全域零 serializer，混进来会让同一响应出现两套 None/空串口径）。补三键：`options`（`thread.options` 非 list 归一 `[]`，逐项 `.get("label","")` / `.get("value","")` / `.get("note","")` 防御——`JSONField(default=list)` 无 schema 校验）、`last_reminded_at`（`.isoformat()` 或 `None`）、`messages`（每项五键 `{id, author_type, author_user_id, author_display, body, created_at}`；`author` 是 `SET_NULL` FK ⇒ `author_display` 必须容忍 `None`，回落顺序 `author.username or author.email or ""`，author 为 None 时回落 `""`；⛔ 不因为 author 为 None 抛）。埋点 `blueprint_threads_read`（带 `thread_count` / `message_count`，⛔ 不带任何正文）。
- `post`：前置三步 → **状态闸**：`if not is_blueprint_editable(artifact): return Response({"detail": NOT_EDITABLE_DETAIL}, status=400)`（判据函数与文案常量一律 import 复用，⛔ 不自造白名单、不自造文案）→ 取 `body = str(request.data.get("body") or "").strip()`，空 → **400**（且此刻 DB 一字未动）→ 取 `anchor = request.data.get("anchor")`（非 dict 归一 None）→ `result = await aopen_selection_comment(artifact, body=body, anchor=anchor, user=request.user, initiated_by_user_id=str(getattr(request.user, "id", "") or "system"))` → 按 `result["status"]` 映射：`"created"` → **200** `{thread_id, current_status}`；`"invalid"` → **400** 回显 `result["detail"]`。⛔ **不接续驱**（选区评论是纯留痕，不改蓝图状态、不触发 stage 推进；`open_thread` 本身也不改状态）。埋点 `blueprint_thread_created`（带 `thread_id` / `body_len` / `has_anchor`，⛔ **`body` 正文不进日志**）。

**② 新建 `server/delivery/services/blueprint_comment_action.py`。**

模块 docstring 首段声明 INV-6（逐字照 `blueprint_review_action.py:3-5` 的措辞换成本模块）：「选区评论的 service 收口：视图零 ORM 写，线程与首条消息的落库都在本模块经 `BlueprintLifecycleService.open_thread` 完成」。第二段写明与 analog 的**语义差异**（见下）。`__all__` 显式列公开函数；`_COMPONENT = "blueprint_comment_action"`。

三个共用 helper 照 analog `:131-160` 复制体例（`_user_id` 回落 `"system"`、`_detail` 走 `redact_secrets_in_text` + 截断 500、`_alatest_version` ⛔ 绝不读 `session.current_artifact_version`）。

唯一公开入口：`async def aopen_selection_comment(artifact, *, body, anchor=None, user=None, initiated_by_user_id="system", lifecycle_service=None) -> dict`，恒定返回键 `{"status": "created"|"invalid", "thread_id": str, "detail": str, "current_status": str}`。实现：
1. `body` strip 后为空 → `{"status": "invalid", "detail": "评论内容不可为空"}`（不落库）；
2. `version = await _alatest_version(artifact)`（它回答「这条评论是针对哪一版提的」）；
3. `thread = await lifecycle.open_thread(artifact, kind=ThreadKind.HUMAN_COMMENT, blocking=False, severity="", question=body, anchor=anchor if isinstance(anchor, dict) else None, created_on_version=version, initiated_by_user_id=initiated_by_user_id, return_stage=BlueprintStatus.DRAFTING)`——`kind` 用**枚举常量不是字面量**；`blocking=False` + `severity=""` ⇒ 评论不受 114-01 的 finding 不变式约束、不把蓝图钉死（评论不该阻塞确认）；
4. **⭐ 与 analog 的关键 DIFFER**：`_aopen_reject_comment` 是驳回的**副作用**（best-effort，开不出线程也返空串不上抛）；本函数是**主动作** ⇒ `open_thread` 抛异常必须如实回错 `{"status": "invalid", "detail": _detail(exc)}`，⛔ **不吞**（吞了用户会看到「评论成功」但侧栏永远不出现那条评论）；
5. `current_status` 取 `str(getattr(artifact, "blueprint_status", "") or "")`（本函数不改状态，原样回传供前端对齐 §3.7 的「以响应体 `current_status` 为准」纪律）；
6. 收尾一条 `caller` 日志 `blueprint_selection_comment_created`：`category="caller"` + `component=_COMPONENT` + `artifact_id` + `thread_id` + `initiated_by_user_id` + `body_len` + `has_anchor` + `duration_ms`。⛔ **`body` 正文与 `anchor.quoted_text` 都不进日志**。

**③ `server/delivery/urls.py` 追加前三条路由**（删除行 0）：import 块按字母序追加 `from delivery.api.blueprint_doc_views import (BlueprintDocumentView, BlueprintEventsView, BlueprintReviewThreadsView)`；`urlpatterns` 在既有 `blueprint-review/` 分组**之后**追加一行分组注释 + 三条 `path(...)`。分组注释照既有纪律写明「几个端点 + 前缀语义 + 字面段/uuid 段的顺序纪律」，逐字要点：「115-01 蓝图只读供数面：正文（含 quality）/ 阶段事件 / 线程详情集合。`blueprint/` 与 `blueprint/events/` 是两个整段精确匹配，互不遮挡；`blueprint-review/threads/` 的字面段 `threads/` 与既有 `threads/<uuid:thread_id>/<动作>/` 整段不同，按既有纪律**字面段写在前面**」。三条 `name` 为 `blueprint-document` / `blueprint-events` / `blueprint-review-threads`。

**④ `server/tests/delivery/test_blueprint_log_redaction_guard.py` 的 `_SCANNED_MODULES` 追加三行**（删除行 0）：`"delivery/api/blueprint_doc_views.py"` / `"delivery/api/blueprint_list_views.py"`（Task 2 建）/ `"delivery/services/blueprint_comment_action.py"`。⚠️ Task 1 完成时 `blueprint_list_views.py` 尚不存在——**先确认该守卫对不存在的模块是跳过还是报错**（读 `_iter_modules` / 循环体实现）：若会报错，则本行留到 Task 2 追加，并在 SUMMARY 登记实际落点。
  </action>
  <verify>
    <automated>cd server && uv run python -c "
import os, django
os.environ.setdefault('DJANGO_SETTINGS_MODULE','friday.settings'); django.setup()
from django.urls import reverse
a='11111111-1111-1111-1111-111111111111'
for n in ('blueprint-document','blueprint-events','blueprint-review-threads'):
    print(n, reverse(n, args=[a]))
" && uv run pytest tests/delivery/test_blueprint_log_redaction_guard.py tests/delivery/test_blueprint_inv6_guard.py tests/delivery/test_blueprint_review_threads.py -q</automated>
  </verify>
  <acceptance_criteria>
- 上述 `reverse` 命令输出**三条** URL 且形如 `/api/delivery/artifacts/<uuid>/blueprint/`、`.../blueprint/events/`、`.../blueprint-review/threads/`。
- ⭐ **既有文件零改动**：`git diff server/delivery/api/blueprint_review_views.py` 输出**为空**；`git diff server/delivery/api/artifact_views.py` 输出**为空**。
- ⭐ **范围闸是 import 复用不是复制**：`rg -n "from delivery.api.blueprint_review_views import" server/delivery/api/blueprint_doc_views.py` 命中；`rg -c "async def _aassert_project_scope|async def _ais_project_member|async def _ablueprint_project_id" server/delivery/api/blueprint_doc_views.py` == **0**（本文件不重新定义这些 helper）。
- `rg -c "_aassert_project_scope" server/delivery/api/blueprint_doc_views.py` ≥ **5**（1 次 import + 4 个端点各一次调用；threads 的 get/post 各算一次）。
- `rg -n "from adrf.views import APIView" server/delivery/api/blueprint_doc_views.py` 命中；`rg -n "from rest_framework.views import APIView" server/delivery/api/blueprint_doc_views.py` **零命中**。
- `rg -c "permission_classes = \[IsAuthenticated\]" server/delivery/api/blueprint_doc_views.py` == **3**（三个 View）。
- `rg -c "^class Blueprint" server/delivery/api/blueprint_doc_views.py` == **3**；`rg -n "async def get|async def post" server/delivery/api/blueprint_doc_views.py | wc -l` == **4**。
- ⛔ 零 serializer：`rg -n "serializers|Serializer" server/delivery/api/blueprint_doc_views.py` **零命中**。
- ⭐ **INV-6 视图零 ORM 写**：`rg -n "objects\.(a?create|a?bulk_create|a?get_or_create|a?update_or_create|a?update|delete)" server/delivery/api/blueprint_doc_views.py` **零命中**；`rg -n "BlueprintThread\s*\(|BlueprintThreadMessage\s*\(|BlueprintReviewer\s*\(" server/delivery/api/blueprint_doc_views.py server/delivery/services/blueprint_comment_action.py` **零命中**（裸实例化也被 `_RE_INSTANTIATE` 逮）。
- ⭐ **唯一写口**：`rg -n "open_thread" server/delivery/services/blueprint_comment_action.py` 命中；`rg -n "_ALLOWED_WRITER" server/tests/delivery/test_blueprint_inv6_guard.py | rg -c "blueprint_comment_action"` == **0**（新 service **没有**被加进豁免名单）。
- ⭐ **`?version_id=` 不走 `_alatest_content`**：`cd server && uv run python -c "
src = open('delivery/api/blueprint_doc_views.py').read()
i = src.index('class BlueprintDocumentView'); nxt = src.find('\nclass ', i + 1)
body = src[i: nxt if nxt > 0 else len(src)]
assert 'current_version_id' in body, 'is_current 必须用 artifact.current_version_id 判据'
assert 'order_by(\"-version_no\")' in body or '_aload_version' in body, '取基线必须 order_by(-version_no)'
print('document view version semantics OK')"` 输出 `document view version semantics OK`。
- ⭐ **quality 不被改写成 0**：`rg -n "or 0|\?\? 0|, 0\)" server/delivery/api/blueprint_doc_views.py` 的命中逐条人工核对**均不落在 quality 装配段**（SUMMARY 逐条登记）；`rg -n "sync_to_async" server/delivery/api/blueprint_doc_views.py` 命中（三项同步统计已包裹，P-15）。
- ⭐ **events 无会话回 200**：`cd server && uv run python -c "
src = open('delivery/api/blueprint_doc_views.py').read()
i = src.index('class BlueprintEventsView'); nxt = src.find('\nclass ', i + 1)
body = src[i: nxt if nxt > 0 else len(src)]
assert 'BLUEPRINT_EVENTS' in body, 'events 端点必须按 BLUEPRINT_EVENTS 过滤'
assert 'order_by(\"ts\")' in body, 'ts 必须显式升序（Meta.ordering 是 created_at）'
print('events view OK')"` 输出 `events view OK`。
- ⭐ **threads GET 三补键**：`rg -n "options|last_reminded_at|messages" server/delivery/api/blueprint_doc_views.py` 三个 token 全命中；`rg -n "Prefetch" server/delivery/api/blueprint_doc_views.py` 命中（N+1 防线）；`rg -n 'order_by\("created_at"\)' server/delivery/api/blueprint_doc_views.py` 命中 ≥2（线程与消息各一次）。
- ⭐ **threads POST 状态闸复用**：`rg -n "is_blueprint_editable|NOT_EDITABLE_DETAIL" server/delivery/api/blueprint_doc_views.py` 两个 token 全命中；`rg -n "EDITABLE_BLUEPRINT_STATUSES\s*=" server/delivery/api/blueprint_doc_views.py` **零命中**（不自造白名单）。
- ⭐ **正文不进日志**：`rg -n "body_len|reason_len|q_len" server/delivery/api/blueprint_doc_views.py server/delivery/services/blueprint_comment_action.py` 命中；`cd server && uv run python -c "
import ast, sys
for p in ('delivery/api/blueprint_doc_views.py', 'delivery/services/blueprint_comment_action.py'):
    tree = ast.parse(open(p).read())
    for node in ast.walk(tree):
        if isinstance(node, ast.Call):
            for kw in node.keywords or []:
                if kw.arg in ('body', 'question', 'quote', 'q'):
                    src = ast.unparse(kw.value)
                    assert 'len(' in src or 'open_thread' in ast.unparse(node.func), (p, kw.arg, src)
print('no raw body in log kwargs')"` 输出 `no raw body in log kwargs`（人工复核：`open_thread(question=body)` 是**落库**不是日志，属唯一合法出现）。
- ⭐ **脱敏出口**：`cd server && uv run pytest tests/delivery/test_blueprint_log_redaction_guard.py -q` 全绿；`rg -n "blueprint_doc_views|blueprint_comment_action" server/tests/delivery/test_blueprint_log_redaction_guard.py` 两条命中。
- `rg -n 'category="caller"' server/delivery/api/blueprint_doc_views.py` 命中；`rg -n "duration_ms" server/delivery/api/blueprint_doc_views.py` 命中。
- `git diff server/delivery/urls.py | rg "^-[^-]"` **为空**（纯追加）；`git diff server/tests/delivery/test_blueprint_log_redaction_guard.py | rg "^-[^-]"` **为空**。
- `cd server && uv run pytest tests/delivery/ -q` 无新增失败（`test_blueprint_inv6_guard` / `test_blueprint_review_threads` 的 TOCTOU 扫描两条**必须仍绿** —— 它们扫的是 `delivery/api/` 整目录，新文件已在扫描面内）。
- `cd server && uv run ruff check delivery/api/blueprint_doc_views.py delivery/services/blueprint_comment_action.py delivery/urls.py tests/delivery/test_blueprint_log_redaction_guard.py` 通过；`uv run ruff format --check delivery/api/blueprint_doc_views.py delivery/services/blueprint_comment_action.py` 通过（**不对 `urls.py` 与守卫测试跑 format**）。
  </acceptance_criteria>
  <done>四端点就位且逐条遵循 114-05 立的端点惯例（adrf / IsAuthenticated / 手写 dict builder / 中性 404 / caller 埋点 / 正文不进日志）；范围闸是 import 复用、既有文件零改动；正文端点返结构化 content 且 `is_current` 用 `current_version_id` 判据、quality 的 `None` 原样透传；events 端点无会话回 200 空结构且按 `BLUEPRINT_EVENTS` 与 `ts` 升序；threads GET 补齐 `options`/`last_reminded_at`/`messages` 且 N+1 有防线；threads POST 经新 service 收口、状态闸复用既有判据、body 空与不可编辑态均 400 且不落库。</done>
</task>

<task type="auto">
  <name>Task 2: blueprint_list_views.py 列表端点（ProjectMember 可见集合 + 方案 A 先聚合再切片 + current_status 键 + 五键分页）+ urls 第四条</name>
  <files>server/delivery/api/blueprint_list_views.py, server/delivery/urls.py</files>
  <read_first>server/knowledge/api/artifact_overview.py **全文 250 行**（本 Task 的逐字 analog：L1-30 模块 docstring 与 ⚠️ **L7,26 的「返回可见 Space id」——这是唯一不能照抄的一行**；L40-62 `_DEFAULT_PAGE_SIZE` / `_MAX_PAGE_SIZE` / `_EMPTY` 常量与两个 clamp 纯函数；L131-190 `_aggregate` 纯同步聚合函数的完整形状与响应五键 `{total, items, page, page_size, has_next}` 及 `has_next = offset + len(items) < total`；L202-248 started/completed 两条 caller 日志 + L211-223 权限 fail-closed 短路（可见集合为空直接返 `dict(_EMPTY)`，零 DB 越权）+ L226 `await sync_to_async(_aggregate)(...)` 单点调用 + L229-238 聚合整体 try/except 返空结构不 500）；server/delivery/api/artifact_views.py L45-98（`_parse_uuid_param` 的二元组返回范式 + `ArtifactListView` 的现状——**确认它无任何项目可见性过滤、返裸数组无分页，本 plan 一行不改**）；server/delivery/api/blueprint_review_views.py L244-281（`_ais_project_member` 的**确切查询形态**与 `_aassert_project_scope` 的 superuser 直通行 `:274` —— 列表侧的可见性口径与它同源、对称）；server/delivery/models/artifact.py L66-128（`artifact_type` / `blueprint_status` / **复合索引 `Index(fields=["artifact_type", "blueprint_status"])` `:123`** / `current_version` `:93` / `title` `:84` / `work_item` 可空 `:76-82`）；server/delivery/models/blueprint_thread.py L120-130（`Index(fields=["artifact","status","blocking"])` `:126` —— `annotate(Count(...))` 走它）；server/services/process_runtime/blueprint_schema.py L142-166（`meta` 段的 `title` / `summary`（**是 `block_list` 不是字符串**）/ `project_id` / `revision_round`）与 L167-260（`repo_associations[]` 的 `repository_id` / `role` 字段名）；server/tests/delivery/test_blueprint_inv6_guard.py L56-65（三条字段级正则——写 `filter(...)` 与响应键前先手工验算）；server/tests/delivery/test_blueprint_review_threads.py L365-392（TOCTOU 扫描的**确切判据**：同一文件里 `aunresolved_blocker_count` 与 `BlueprintStatus.CONFIRMED` / `to_status="confirmed"` 同现即红）</read_first>
  <action>
**① 新建 `server/delivery/api/blueprint_list_views.py`**（一个 View：`BlueprintListView`）。

模块 docstring 四段：(a) 端点与用途（知识库「技术方案」tab 与项目物料卡共用的唯一列表面，VIEW-03 / VIEW-04）；(b) ⭐ **可见性口径**——「与四个 artifact 级端点**不同**：那四者是『拿着一个 artifact_id 问能不能看』（`_aassert_project_scope` 逐个判），本端点是『列出我能看的全部』。对 N 条候选逐条跑范围闸 = N 次 `meta.project_id` 提取 + N 次 `aexists()`，形态错误。正确做法是先算『我是成员的 project id 集合』再过滤；superuser 直通（与 `_aassert_project_scope:274` 对称）；集合为空 ⇒ fail-closed 直接返空结构，零 DB 越权查询」；(c) ⭐ **为什么不改 `ArtifactListView`**（通用面、无项目闸、返裸数组无分页、已被 `web/src/components/delivery/ArtifactTimeline.vue:43` 以 `space_id + artifact_type` 消费——挂闸即改既有面行为）；(d) ⛔ **命名纪律**（见下 ③）与观测口径（`?q=` 只记长度）。

常量：`_COMPONENT = "blueprint_list_api"`；`_DEFAULT_PAGE_SIZE = 20`；`_MAX_PAGE_SIZE = 100`；`_EMPTY = {"total": 0, "items": [], "page": 1, "page_size": _DEFAULT_PAGE_SIZE, "has_next": False}`；两个 clamp 纯函数 `_parse_page` / `_parse_page_size` 逐字照 analog（非法值 fail-soft）。

**② 聚合形态（方案 A，PLAN 在此写死，⛔ 执行期不得改选）：**

全部 ORM 收在**一个纯同步函数** `_aggregate(allowed_project_ids, is_superuser, filters, page, page_size) -> dict` 内，View 只 `await sync_to_async(_aggregate)(...)`（照 analog `:226`）。步骤：

1. **候选收窄走复合索引**：`Artifact.objects.filter(artifact_type="technical_plan").exclude(**{_STATUS_FIELD: ""})`——⚠️ `exclude(**{...})` 与 `filter(**{...})` 两种形态都必须走 `_STATUS_FIELD` 常量（见 ③）。
2. `.select_related("current_version")`（`meta.project_id` / `meta.summary` / `repo_associations` 全在 `current_version.content` 里）。
3. `.annotate(thread_count=Count("threads", distinct=True), unresolved_blocker_count=Count("threads", filter=Q(threads__severity="blocker") & Q(threads__blocking=True) & Q(threads__status__in=["open", "answered"]), distinct=True))`——⚠️ `related_name` 与 `severity`/`status` 的**确切取值**按 `BlueprintThread` 实测取，不猜；未决判据与 114-01 confirm 守卫的两条判据口径对齐（`{open, answered}` 都算未决）。⛔ **绝不 `import aunresolved_blocker_count`**（P-2）。
4. **Python 侧过滤**（`meta.project_id` 不是 DB 列，无法 `filter(project_id__in=...)`）：逐条读 `content["meta"]["project_id"]`，`is_superuser` 为 False 时要求 ∈ `allowed_project_ids`；再依次应用 `?project_id=` / `?blueprint_status=`（query **参数名**可以保留字面 `blueprint_status`——它后面既无 `=` 也无 `:`，三条正则都不命中，见 ③）/ `?repository_id=`（扫 `content["repo_associations"][].repository_id`）/ `?q=`（`title` 与 `meta.summary` 扁平文本的大小写不敏感包含；`summary` 是 `block_list` ⇒ 用与 `_block_text` 同款的取文本逻辑扁平化，⛔ 不 `json.dumps` 全文匹配）。
5. **过滤之后**再切片：`offset = (page - 1) * page_size`，`items = filtered[offset: offset + page_size]`，`total = len(filtered)`，`has_next = offset + len(items) < total`。
6. 行序列化 `_list_row(artifact, content, counts) -> dict`（手写 dict builder，逐值归一）：`artifact_id / title / summary（meta.summary 首块纯文本，截断 ≤200 字符）/ current_status / project_id / project_name / repositories[{id, name, role}] / thread_count / unresolved_blocker_count / revision_round / current_version_no / updated_at`。`project_name` 与 `repositories[].name` 需要跨表取名——**一次性批量取**（`Project.objects.filter(id__in=...).values_list("id","name")` / `Repository.objects.filter(id__in=...)...` 建 dict），⛔ 不在行循环里逐条查（N+1）。取不到名字回落空串，⛔ 不因此丢行。

**③ ⭐ 命名纪律（P-1，本 Task 最容易静默踩红的一条，务必先手工验算三条正则）：**

`test_blueprint_inv6_guard.py` 的 `_RE_FIELD_WRITE = \bblueprint_status\s*=\s*[^=]` 与 `_RE_FIELD_DICT_KEY = ['"]blueprint_status['"]\s*:` 扫**整个 `server/`**（豁免只有唯一 writer / tests / migrations），且守卫 docstring 明说这是**有意的**。因此：

- 响应键一律 **`current_status`**（⛔ 不是 `blueprint_status`）—— 逐字照 114-05 的既有解法（`blueprint_review_action._current_status`，其 docstring 就是这条规避的说明书）。**同步订正 UI-SPEC §3.3 的 `BlueprintListItem.blueprint_status` 为 `current_status`**，并在 SUMMARY 登记该订正（前端消费点在 115-02 的 `types/blueprint.ts` 与 `api/blueprints.ts`）。
- ORM 过滤一律经模块常量：`_STATUS_FIELD = "blueprint_status"`（**这一行两条正则都不命中**：`blueprint_status` 后面紧跟的是 `"` 不是 `=` 或 `:`）+ `filter(**{_STATUS_FIELD: value})` / `exclude(**{_STATUS_FIELD: ""})`。该常量处**必须写注释说明为何绕这一圈**（否则后人「顺手改直白」再把测试搞红，且报错信息指向「旁路写状态字段」这个完全无关的方向）。
- ✅ 安全形态（可以直接写）：`request.query_params.get("blueprint_status")`（后面既无 `=` 也无 `:`）、`if artifact.blueprint_status == X`（`[^=]` 排除比较）。

**④ View 主体**：`BlueprintListView(APIView)`，`permission_classes = [IsAuthenticated]`，`async def get`：
`started = time.monotonic()` → 解析并校验 query（`project_id` / `repository_id` 非空且非 UUID → **400**，用 `_parse_uuid_param` 范式）→ `is_superuser = bool(getattr(request.user, "is_superuser", False))` → 非 superuser 时 `allowed = await sync_to_async(_load_member_project_ids)(request.user)`（`ProjectMember.objects.filter(user=...).values_list("project_id", flat=True)`）→ **fail-closed 短路**：非 superuser 且 `allowed` 为空 ⇒ 直接 `Response({**_EMPTY, "page": page, "page_size": page_size})` + completed 日志（**零 DB 越权**）→ `payload = await sync_to_async(_aggregate)(...)`，整体 `try/except Exception` → warning（`error` 过 `redact_secrets_in_text`）+ 返空结构**不 500**（照 analog `:229-238`「聚合/观测永不反噬请求」）→ started/completed 两条 `caller` 日志（`blueprint_list_read_started` / `blueprint_list_read_completed`，字段 `initiated_by_user_id` / `project_count` / `page` / `page_size` / `total` / `q_len` / `duration_ms`；⛔ **`?q=` 只记长度不记内容**）。

⚠️ 本 View 的日志 helper 不能复用 Task 1 的 `_log`（那个签名带 `artifact_id`，列表端点没有单一 artifact）—— 本文件内另写一个同款五件套的 `_log_list(event, request, started, **fields)`，`component=_COMPONENT`，`artifact_id` 位不出现。

**⑤ `delivery/urls.py` 追加第四条**（删除行 0）：import 追加 `from delivery.api.blueprint_list_views import BlueprintListView`；`urlpatterns` 在 `artifacts/` 分组**之外**、与 `artifacts/`（`:133`）同级处追加一行单独注释 + `path("blueprints/", BlueprintListView.as_view(), name="blueprint-list")`。注释要点：「115-01 蓝图列表（VIEW-03/VIEW-04）：**顶层字面段**，不在 `artifacts/` 分组内；可见性口径与 artifact 级端点不同（先算成员项目集合再过滤，见 `blueprint_list_views` 模块 docstring）」。

**⑥ 若 Task 1 因守卫对不存在模块报错而未能追加 `blueprint_list_views.py` 到 `_SCANNED_MODULES`，在此补上**（并在 SUMMARY 登记落点）。
  </action>
  <verify>
    <automated>cd server && uv run python -c "
import os, django
os.environ.setdefault('DJANGO_SETTINGS_MODULE','friday.settings'); django.setup()
from django.urls import reverse
print('blueprint-list', reverse('blueprint-list'))
" && uv run pytest tests/delivery/test_blueprint_inv6_guard.py tests/delivery/test_blueprint_review_threads.py tests/delivery/test_blueprint_log_redaction_guard.py -q</automated>
  </verify>
  <acceptance_criteria>
- `reverse('blueprint-list')` 输出 `/api/delivery/blueprints/`。
- ⭐ **P-1 双保险（本 Task 头号靶子）**：`cd server && uv run pytest tests/delivery/test_blueprint_inv6_guard.py -q` **全绿**；且 `cd server && uv run python -c "
import re
src = open('delivery/api/blueprint_list_views.py').read()
assert not re.search(r'\bblueprint_status\s*=\s*[^=]', src), '命中 _RE_FIELD_WRITE'
assert not re.search(r'[\'\"]blueprint_status[\'\"]\s*:', src), '命中 _RE_FIELD_DICT_KEY'
assert '_STATUS_FIELD' in src, 'ORM 过滤必须走模块常量'
assert 'current_status' in src, '响应键必须是 current_status'
print('P-1 naming OK')"` 输出 `P-1 naming OK`。
- ⭐ **P-2 TOCTOU 扫描**：`rg -n "aunresolved_blocker_count" server/delivery/api/blueprint_list_views.py` 的命中**只能是响应键字符串**，⛔ 不得是 `from ... import aunresolved_blocker_count`；`rg -n "^from|^import" server/delivery/api/blueprint_list_views.py | rg -c "aunresolved_blocker_count"` == **0**；`cd server && uv run pytest tests/delivery/test_blueprint_review_threads.py::test_no_out_of_transaction_blocker_check_before_confirm -q` 全绿。
- ⭐ **可见性口径**：`rg -n "ProjectMember" server/delivery/api/blueprint_list_views.py` 命中；`rg -n "resolve_allowed_project_ids" server/delivery/api/blueprint_list_views.py` **零命中**（那返回的是 Space id 不是 project id）；`rg -n "is_superuser" server/delivery/api/blueprint_list_views.py` 命中（与 `_aassert_project_scope:274` 对称）。
- ⭐ **不逐条跑范围闸**：`rg -n "_aassert_project_scope" server/delivery/api/blueprint_list_views.py` **零命中**。
- ⭐ **不改既有通用面**：`git diff server/delivery/api/artifact_views.py` 输出**为空**。
- **分页体五键**：`rg -n '"has_next"' server/delivery/api/blueprint_list_views.py` 命中；`rg -n "_parse_page|_parse_page_size|_MAX_PAGE_SIZE" server/delivery/api/blueprint_list_views.py` 三个 token 全命中；`rg -n "PageNumberPagination|paginate_queryset" server/delivery/api/blueprint_list_views.py` **零命中**（⛔ 不用 DRF 分页，方案 A 的 Python 侧过滤用不上它）。
- **fail-closed 与不 500**：`cd server && uv run python -c "
src = open('delivery/api/blueprint_list_views.py').read()
assert '_EMPTY' in src, '缺 fail-closed 空结构常量'
assert 'except Exception' in src, '聚合必须整体 try 不 500'
print('list fail-closed OK')"` 输出 `list fail-closed OK`。
- **N+1 防线**：`rg -n "select_related|annotate|values_list" server/delivery/api/blueprint_list_views.py` 三个 token 全命中；`cd server && uv run python -c "
src = open('delivery/api/blueprint_list_views.py').read()
i = src.index('def _list_row'); nxt = src.find('\ndef ', i + 1)
body = src[i: nxt if nxt > 0 else len(src)]
assert '.objects.' not in body, '行序列化函数内不得查库（N+1）'
print('no N+1 in row builder')"` 输出 `no N+1 in row builder`。
- **观测**：`rg -n 'category="caller"' server/delivery/api/blueprint_list_views.py` 命中 ≥2（started/completed）；`rg -n "q_len" server/delivery/api/blueprint_list_views.py` 命中；`rg -n 'q=q\b|query=q\b|keyword=q\b' server/delivery/api/blueprint_list_views.py` **零命中**（`?q=` 正文不进日志）。
- `git diff server/delivery/urls.py | rg "^-[^-]"` **为空**；`rg -n 'name="blueprint-list"' server/delivery/urls.py` 命中。
- `cd server && uv run ruff check delivery/api/blueprint_list_views.py delivery/urls.py` 通过；`uv run ruff format --check delivery/api/blueprint_list_views.py` 通过。
  </acceptance_criteria>
  <done>列表端点就位：可见性是「先算成员项目集合再过滤」+ superuser 对称 + 空集合 fail-closed 零越权；方案 A 的先聚合再切片与五键分页体落地；响应键 `current_status` 与 `_STATUS_FIELD` 常量让 INV-6 两条正则与 TOCTOU 扫描全部保持绿；`ArtifactListView` 一行未改。</done>
</task>

<task type="auto">
  <name>Task 3: 两个端点测试文件（范围闸正反并列 / quality 三态 / events 无会话 200 / threads 三补键与状态闸 / 列表可见性与 current_status 防回归）+ 全量后端门</name>
  <files>server/tests/delivery/test_blueprint_doc_views.py, server/tests/delivery/test_blueprint_list_views.py</files>
  <read_first>server/tests/delivery/test_blueprint_review_views.py **L1-140**（「守十四件事」docstring 体例逐条；`pytestmark = pytest.mark.django_db(transaction=True)` 的理由（async service 跨线程写库）；`async_to_sync` 装配同步用例的写法 `:33-34`；源码扫描的 `SERVER_DIR = Path(__file__).resolve().parents[2]` 与 `_VIEWS_REL` 定位 `:69-70`；⭐ **范围闸工厂 `_SCOPE_PROJECT_ID` / `_OTHER_PROJECT_ID` / `_make_project(project_id, member=...)` `:83-100`——本 Task 逐字复用，不重造**；懒 import 的 patch 目标注释 `:73-75`）以及该文件里**「非成员 → 中性 404 且响应体与不存在逐字相同」**那条用例的完整写法（本 Task 五端点各抄一份）；server/tests/services/test_blueprint_quality.py（三态并列用例的既有形状——本 Task 的 `quality` 三态断言与它口径一致，⛔ 不重复实现统计逻辑，只断言端点原样透传 `None`）；server/tests/delivery/test_blueprint_event_taxonomy_112.py L100-120（`len(BLUEPRINT_EVENTS) == 21` 的锁 —— events 端点用例引用同一常量，不硬编码 21 条事件名）</read_first>
  <action>
**① 新建 `server/tests/delivery/test_blueprint_doc_views.py`**，`pytestmark = pytest.mark.django_db(transaction=True)`，docstring「守十二件事」编号清单（每条把可证伪断言写进条目本身）：

1. ⭐ **鉴权第一条**：四端点（`blueprint-document` GET / `blueprint-events` GET / `blueprint-review-threads` GET / 同 POST）未认证一律拒（401/403），**参数化四条**，无一例外。
2. ⭐ **范围闸正反并列（MJ-03 对称面）**：同一 artifact，(a) 成员 → 200；(b) **非成员 → 404 且响应体与「artifact 不存在」的 404 响应体逐字相同**（`assert resp.json() == missing_resp.json()`，这是不泄露存在性的唯一可证伪形态）；(c) superuser → 200。**四端点各跑一遍**（参数化）。
3. ⭐ **`meta.project_id` 缺失 → 400**（fail-closed，不是 404 也不是 200）。
4. **正文端点缺省取最新版本**：造三版，不带 `?version_id=` → 返 `version_no` 最大那版且 `is_current` 与 `artifact.current_version_id` 一致。
5. ⭐ **`?version_id=` 取历史版本且 `is_current == False`**；`?version_id=` 指向别的 artifact 的版本 / 不存在的 uuid → **404**；`?version_id=notauuid` → **400**。
6. ⭐ **`quality` 三态并列（闭 MN-05，本文件头号口径靶子）**：同一端点三组数据——(a) 零事件/零版本/零线程 ⇒ `ai_rejection_rate` / `human_edit_volume` / `clarification_rounds` 三项**均为 `null`**（`assert payload["quality"]["ai_rejection_rate"] is None`，⛔ 不是 `== 0`）；(b) 有版本但零人工编辑 ⇒ `human_edit_volume == 0`；(c) 有数据 ⇒ 正值。**并列存在才逮得住 `v ?? 0` 这类改写**。另断言 `citation_coverage` 恒是 float（空引用池时为 `1.0`，⛔ 不为 None）。
7. ⭐ **events 无会话回 200 空结构不是 404**：artifact 存在但无蓝图会话 ⇒ `200` 且 `{"session_id": "", "current_stage": "", "events": []}`。
8. **events 只出 `BLUEPRINT_EVENTS` 且 `ts` 升序**：造 3 条 `blueprint_*` 事件 + 2 条非蓝图事件（如 `stage.started` 之外的既有类型）+ 乱序 `ts` ⇒ 响应只含 3 条、按 `ts` 严格升序；断言集合用 `from delivery.services.event_taxonomy import BLUEPRINT_EVENTS` 而**不硬编码 21 条事件名**。另造一条 `process_type="technical_plan"` 的会话（**排序上更靠前**）证伪跨 process 污染：响应仍是蓝图会话的事件。
9. ⭐ **threads GET 三补键**：响应每项含 `options` / `last_reminded_at` / `messages`；(a) `options` 为非法形状（写入 `{"x": 1}` 而非 list）⇒ 归一为 `[]` **不抛**；(b) 一条消息的 `author` 置 None（`SET_NULL`）⇒ `author_display` 为空串**不抛 500**；(c) 线程按 `created_at` 升序、消息按 `created_at` 升序；(d) `_thread_row` 原九键仍在（键集 ⊇ 九键）。
10. ⭐ **threads POST 的三条 400 且线程数不变**：(a) `body` 空 / 纯空格 → 400；(b) 蓝图状态 `confirmed`（∉ `EDITABLE_BLUEPRINT_STATUSES`）→ 400 且 `detail == NOT_EDITABLE_DETAIL`；(c) 两种情形下 `BlueprintThread.objects.count()` 与调用前**相等**。正路：可编辑态 + 非空 body ⇒ 200 且新线程 DB 重读 `kind == "human_comment"`、`blocking is False`、`severity == ""`、`anchor` 落库、`created_on_version` 是当前最新版本。
11. **视图零 ORM 写源码扫描**（照既有第 9 条形态）：`delivery/api/blueprint_doc_views.py` 内 `objects.(create|acreate|bulk_create|update|bulk_update|delete)` 零命中；且 `BlueprintThread(` 裸实例化零命中。
12. **范围闸是 import 复用**：源码扫描断言 `blueprint_doc_views.py` 含 `from delivery.api.blueprint_review_views import` 且**不重新定义** `_aassert_project_scope`（防后人复制第三份）。

**② 新建 `server/tests/delivery/test_blueprint_list_views.py`**，docstring「守九件事」：

1. 未认证拒（401/403）。
2. ⭐ **只列「我是成员的项目」的蓝图（正反并列）**：造项目 A（我是成员）与项目 B（我不是），各一份蓝图 ⇒ 响应 `total == 1` 且只含 A 的 artifact_id；把我加进 B 的成员 ⇒ `total == 2`（**证明断言非恒真**）。
3. **superuser 见全部**；**非成员且零成员项目 ⇒ 空结构且 `total == 0`**（fail-closed）。
4. ⭐ **`blueprint_status == ""` 的旧 artifact 不出现**（造一条 v0 的 `technical_plan` artifact）。
5. ⭐ **响应键是 `current_status`（防回归，与 INV-6 守卫互为双保险）**：`assert "current_status" in item and "blueprint_status" not in item`。
6. **分页五键与 `has_next` 边界**：造 25 条 ⇒ `page=1&page_size=20` 时 `has_next is True` 且 `len(items) == 20`；`page=2` 时 `has_next is False` 且 `len(items) == 5`；`page_size=999` clamp 到 100；`page=0` / `page=abc` fail-soft 取 1。
7. **`?q=` 命中标题与摘要各一例**：一条只有标题命中、一条只有 `meta.summary` 命中 ⇒ 两条各自可被检出；不命中的 q ⇒ `total == 0`。
8. **`?project_id=` / `?repository_id=` / `?blueprint_status=` 各一条过滤用例**；非 UUID 的 `project_id` ⇒ **400**。
9. **计数字段**：造两条线程（其中一条 `severity=blocker & blocking=True & status=open`）⇒ `thread_count == 2` 且 `unresolved_blocker_count == 1`；另造一条 `status=resolved` 的 blocker ⇒ 不计入未决。

**③ 全量后端门**（在本 Task 收尾执行并把结果登记进 SUMMARY）：`cd server && uv run pytest tests/ -q` 与基线 **8546 passed / 1 failed** 逐条比对，任何差异登记；`uv run python manage.py makemigrations --check --dry-run` 退出码 0 且 `git status --porcelain server/*/migrations/` 为空（本 plan 零 migration）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/delivery/test_blueprint_doc_views.py tests/delivery/test_blueprint_list_views.py -x -q</automated>
  </verify>
  <acceptance_criteria>
- `cd server && uv run pytest tests/delivery/test_blueprint_doc_views.py tests/delivery/test_blueprint_list_views.py -q` 全绿。
- `rg -c "def test_" server/tests/delivery/test_blueprint_doc_views.py` ≥ **18**；`rg -c "def test_" server/tests/delivery/test_blueprint_list_views.py` ≥ **12**。
- ⭐ **中性 404 逐字相同**：`cd server && uv run python -c "
src = open('tests/delivery/test_blueprint_doc_views.py').read()
assert '.json() ==' in src, '缺「非成员 404 与不存在 404 响应体逐字相同」的断言'
print('neutral 404 assertion OK')"` 输出 `neutral 404 assertion OK`。
- ⭐ **quality 三态并列**：`rg -n "is None" server/tests/delivery/test_blueprint_doc_views.py` 命中 ≥3；`rg -n "== 0" server/tests/delivery/test_blueprint_doc_views.py` 命中（「零人工编辑 → 0」与「无数据 → None」并列存在）；`rg -n "citation_coverage" server/tests/delivery/test_blueprint_doc_views.py` 命中。
- ⭐ **events 三条**：`rg -n "BLUEPRINT_EVENTS" server/tests/delivery/test_blueprint_doc_views.py` 命中（不硬编码事件名）；`rg -n "technical_plan" server/tests/delivery/test_blueprint_doc_views.py` 命中（跨 process 污染证伪）；`rg -n '"events": \[\]|events.*== \[\]' server/tests/delivery/test_blueprint_doc_views.py` 命中（无会话 200 空结构）。
- ⭐ **threads 状态闸**：`rg -n "NOT_EDITABLE_DETAIL|confirmed" server/tests/delivery/test_blueprint_doc_views.py` 命中；`rg -n "count()" server/tests/delivery/test_blueprint_doc_views.py` 命中 ≥2（400 时线程数不变的前后对比）。
- ⭐ **列表可见性正反并列**：`rg -n "_OTHER_PROJECT_ID|other_project" server/tests/delivery/test_blueprint_list_views.py` 命中；`rg -n "total.*== 1|total.*== 2" server/tests/delivery/test_blueprint_list_views.py` 命中（加入成员后从 1 变 2，断言非恒真）。
- ⭐ **`current_status` 防回归**：`rg -n '"blueprint_status" not in' server/tests/delivery/test_blueprint_list_views.py` 命中。
- `rg -n "has_next" server/tests/delivery/test_blueprint_list_views.py` 命中 ≥2（True/False 两侧边界）。
- ⭐ **全量后端门**：`cd server && uv run pytest tests/ -q` 相对基线（8546 passed / 1 failed，唯一失败为 `test_skills_snapshot_guard::test_skill_files_discovered`）**无新增失败**——结果与基线的逐条差异写进 SUMMARY。
- `cd server && uv run python manage.py makemigrations --check --dry-run` 退出码 **0**；`git status --porcelain server/delivery/migrations/ server/knowledge/migrations/ server/initiatives/migrations/` 输出**为空**（本 plan 零 migration）。
- 相位级冻结面自检：`git diff --name-only` 的文件集只含本 plan 声明的七个文件；⛔ 不含 `server/delivery/api/blueprint_review_views.py` / `artifact_views.py` / `server/services/process_runtime/**` / `server/delivery/services/blueprint_lifecycle_service.py` / `event_taxonomy.py` / `web/`。
- `cd server && uv run ruff check tests/delivery/test_blueprint_doc_views.py tests/delivery/test_blueprint_list_views.py` 通过；`uv run ruff format --check tests/delivery/test_blueprint_doc_views.py tests/delivery/test_blueprint_list_views.py` 通过。
  </acceptance_criteria>
  <done>五端点的鉴权、范围闸正反并列（含中性 404 逐字相同）、fail-closed 400、quality 三态、events 的 200-空结构与跨 process 证伪、threads 三补键与状态闸、列表可见性与 `current_status` 防回归全部有绿色用例；全量后端门相对基线无新增失败且零 migration。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 未认证 / 非成员请求 → 五端点 | 唯一 fail-closed 不降级的边界；蓝图正文含项目技术细节，越权读取即敏感信息泄露 |
| 「artifact 存在」这一事实本身 | 中性 404 是不泄露存在性的唯一实现；多一句「该蓝图不存在」就把 MJ-03 的闸破了 |
| 用户 query（`?q=` / `?version_id=` / `?project_id=`）→ ORM | 半可信输入进查询与日志 |
| 用户评论正文 → 线程与首条消息 | 半可信文本落库并回显给同项目其他成员 |
| 统计口径 `None` vs `0` → 人审面板 | 写错会让「没数据」被读成「零打回」，人审据错误指标放行 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115-01 | Information Disclosure | 五端点缺项目范围闸 ⇒ 任一登录用户可读任意项目的蓝图正文 | mitigate | 四个 artifact 级端点 import 复用 `_aassert_project_scope`；列表端点用 `ProjectMember` 可见集合 + superuser 对称 + 空集合 fail-closed；Task 3 第 2 条**四端点参数化**的成员/非成员/superuser 三态并列 |
| T-115-02 | Information Disclosure | 非成员 404 与不存在 404 文案/体不同 ⇒ 可枚举 artifact 存在性 | mitigate | 中性 404 常量 + Task 3 第 2 条 `resp.json() == missing_resp.json()` 的逐字相同断言 |
| T-115-03 | Elevation of Privilege | 列表端点对 N 条候选逐条跑范围闸或用 Space id 冒充 project id ⇒ 越权列出 | mitigate | 口径写死为「先算成员项目集合再过滤」；⛔ `resolve_allowed_project_ids` 零命中（返回的是 Space id）；Task 3 第 2/3 条正反并列 |
| T-115-04 | Tampering | `POST threads/` 绕过 `BlueprintLifecycleService` 直写 ORM ⇒ INV-6 单一写入口失守 | mitigate | View 零 ORM 写 + service 唯一写口 `open_thread` + 三条 INV-6 正则源码扫描 + ⛔ 不把新 service 加进 `_ALLOWED_WRITER` |
| T-115-05 | Tampering | 已 `confirmed` 的蓝图仍可挂新评论线程 ⇒ 绕过 MJ-04 的可编辑白名单 | mitigate | POST 复用 `is_blueprint_editable` + `NOT_EDITABLE_DETAIL`（⛔ 不自造白名单）+ Task 3 第 10 条「400 且线程数不变」 |
| T-115-06 | Information Disclosure | 评论正文 / `?q=` / 线程消息正文进日志 | mitigate | 只记 `body_len` / `q_len` / 计数；AST 守卫覆盖三个新模块（`_SCANNED_MODULES` 追加）；`error=` 一律过 `redact_secrets_in_text` / `_detail` |
| T-115-07 | Spoofing | 反查会话不带 `process_type` ⇒ events 端点吐出旧 `technical_plan` 会话的事件流 | mitigate | 一律走既有 `_aload_session`（自带过滤）+ Task 3 第 8 条的跨 process 证伪用例（另造一条排序更靠前的 `technical_plan` 会话） |
| T-115-08 | Tampering | 响应键 / ORM 过滤用字面 `blueprint_status` ⇒ INV-6 守卫转红，且报错指向完全无关的「旁路写状态字段」 | mitigate | 响应键 `current_status` + `_STATUS_FIELD` 常量 + Task 2 的正则自查命令 + Task 3 第 5 条的 `"blueprint_status" not in item` 防回归（与守卫互为双保险） |
| T-115-09 | Denial of Service | `quality` 三项同步函数在 adrf 异步 View 里直调 ⇒ `SynchronousOnlyOperation` 500（其内部 try 不兜异步上下文错误） | mitigate | 合成一个 `@sync_to_async def _collect_db_quality` + Task 3 第 6 条三态用例（走真实端点，直调会直接红） |
| T-115-10 | Denial of Service | 列表端点行序列化内逐条查项目名/仓库名 ⇒ N+1 | mitigate | 批量取名建 dict + Task 2 的「行序列化函数内 `.objects.` 零命中」源码扫描；`thread_count` 走 `annotate` |
| T-115-11 | Tampering | events 端点无会话时 404 ⇒ 被前端 404 分档吞成全页中性空态，生成中的蓝图看起来像「无权限」 | mitigate | 无会话一律 200 空结构 + Task 3 第 7 条断言 |
| T-115-12 | Repudiation | 只读端点不记 caller 事件 ⇒ 谁读过哪份蓝图无痕 | mitigate | 五端点各一条 `caller` 事件（`blueprint_review_snapshot_read` 是先例），带 `initiated_by_user_id` 与 `duration_ms` |
| T-115-SC | Tampering | 依赖供应链 | accept | 零新增外部依赖、零 migration、零新 SettingKeys、零新事件常量；`makemigrations --check` 退出码 0 |
</threat_model>

<verification>
- `cd server && uv run pytest tests/delivery/ -q` 全绿（含 112 确认门八端点 / 114 七端点 / 本 plan 五端点 / 三条守卫扫描）。
- `cd server && uv run pytest tests/services/ -q` 全绿（`blueprint_quality` 三项统计未被改动，签名与口径不变）。
- ⭐ `cd server && uv run pytest tests/ -q` 相对基线（8546 passed / 1 failed）**无新增失败**。
- `cd server && uv run python manage.py makemigrations --check --dry-run` 退出码 0；相位内**零** migration 文件。
- 三条守卫单独复跑全绿：`test_blueprint_inv6_guard.py`（P-1 命名）、`test_blueprint_review_threads.py::test_no_out_of_transaction_blocker_check_before_confirm`（P-2 TOCTOU）、`test_blueprint_log_redaction_guard.py`（P-16 脱敏，含三个新模块）。
- 受限面删除行核算：`delivery/urls.py` = **0**；`test_blueprint_log_redaction_guard.py` = **0**；`blueprint_review_views.py` 与 `artifact_views.py` 的 `git diff` **为空**。
- `cd server && uv run ruff check delivery/ tests/delivery/` 通过；`ruff format --check` 只对本 plan 三个新建源文件与两个新建测试文件跑（**不含** `urls.py` 与守卫测试）。
</verification>

<success_criteria>
- 前端从此刻起**有结构化正文可渲染**：`GET blueprint/` 返回 `content` dict（含 `block_id`），block 锚定与 block 级 diff 在物理上成为可能（此前只有 markdown 串，结构已丢）。
- **阶段事件第一次可被读到**：`ConvergenceSessionEvent` 的 21 个 `blueprint_*` 子集有了 REST 读面，且「无会话 = 正常态 200 空结构」不会被前端误渲成空白页——这是「不新建推送通道」纪律的落地形态。
- **CLAR-01 的两半各自有数据面**：线程详情带 `options` 与多轮 `messages`（多轮回复可呈现）；选区评论有了全仓第一个主动创建 `human_comment` 线程的入口（此前唯一路径是 `reject/` 的副作用）。
- **VIEW-03/VIEW-04 有列表面**：`GET /delivery/blueprints/` 支持状态/项目/仓库/关键词筛选与稳定分页，且天然只列调用者可见项目的蓝图；`ArtifactListView` 一行未改。
- **MJ-03 的项目范围闸扩到全部蓝图读面**且只有一份实现（import 复用，`blueprint_review_views.py` 零改动）；中性 404 的「逐字相同」有可证伪断言。
- **闭 MN-05**：`blueprint_quality` 三项 DB 统计有了第一个消费方（正文端点的 `quality` 键），且 `None` 原样透传——三态并列用例锁死「无数据 ≠ 0」。
- P-1 / P-2 / P-15 / P-16 四条会静默假通过的陷阱各有源码级或用例级防线；全量后端门相对基线无新增失败、零 migration。
</success_criteria>

<output>
完成后创建 `.planning/phases/115-ui/115-01-SUMMARY.md`，provides 至少包含：
- **五端点契约表**（URL / `name` / 方法 / query 参数 / 响应键逐字 / 全部状态码映射），前端六个 plan 直接照它写 TS 接口；
- ⭐ **对 UI-SPEC 的两处订正登记**：§3.3 的 `BlueprintListItem.blueprint_status` → **`current_status`**（理由 P-1）、§3.3 的「DRF 分页体」→ **`{total, items, page, page_size, has_next}`** 五键（理由：方案 A 的 Python 侧过滤用不上 `paginate_queryset`）；
- `quality` 四键的**取值语义表**（`citation_coverage` 恒有值/分母 0 → 1.0；后三项 `None` = 无数据、`0` = 零值）与三态用例名；
- `blueprint_comment_action.aopen_selection_comment` 的**逐字签名与恒定返回键**，以及它与 `_aopen_reject_comment` 的语义差异（主动作不吞异常）；
- `_thread_detail_row` 的**完整键集**（九键 + `options` / `last_reminded_at` / `messages[]` 五键）与 `author_display` 的回落顺序；
- events 端点的响应形状与「无会话 → 200 空结构」的理由；
- 范围闸的**复用方式**（import 私有符号）与 `blueprint_review_views.py` 零改动的核算；
- `_STATUS_FIELD` 常量的存在理由（P-1）与两条守卫的绿色核对；
- 受限面删除行逐行登记（`delivery/urls.py` / `test_blueprint_log_redaction_guard.py` 各 0）；
- 全量 `pytest tests/ -q` 与基线（8546/1）的差异比对；
- **四条 REQ → 测试用例映射表**（VIEW-01 / VIEW-03 / VIEW-04 / CLAR-01）。
</output>
