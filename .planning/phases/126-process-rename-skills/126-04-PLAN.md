---
phase: 126-process-rename-skills
plan: 04
type: execute
wave: 3
depends_on: ["126-03"]
files_modified:
  - server/services/code_graph/rename_preview.py
  - server/services/code_graph_tools.py
  - server/mcp_tools/views.py
  - server/mcp_tools/urls.py
  - server/mcp_tools/serializers.py
  - server/agents/tools/graph_tools.py
  - server/agents/tools/schemas/graph_tools.py
  - server/agents/tools/__init__.py
  - task/core/knowledge_tools.py
  - server/tests/services/code_graph/test_rename_preview.py
  - server/tests/services/code_graph/test_frozen_surface_126.py
  - task/tests/core/test_knowledge_tools.py
autonomous: true
requirements: [RENAME-01]
tags: [rename-preview, grep-mirror, readonly]

# Scope note: ~12 files acceptable — dual-face MCP/agents shell fan-out + knowledge whitelist; do not split waves.

must_haves:
  truths:
    - "rename_preview 只读：applied 恒为 false；无 apply/rewrite API；不改工作树/mirror（D-09）"
    - "双源合并 confidence 仅为 graph|text_search；同 file:line 以 graph 为准；含 coverage_limitations（D-10/D-11）"
    - "文本半边只经 grep_mirror + exclusion fail-closed；消歧/未索引/ACL 失败 ok=False 不伪装零引用（D-11）"
    - "task knowledge_tools 白名单含 rename_preview；失败不阻断交付（D-12）"
  artifacts:
    - path: "server/services/code_graph/rename_preview.py"
      provides: "双源合并内核"
      contains: "applied"
    - path: "server/services/code_graph_tools.py"
      provides: "run_rename_preview"
      contains: "run_rename_preview"
    - path: "task/core/knowledge_tools.py"
      provides: "rename_preview 白名单条目"
      contains: "rename_preview"
  key_links:
    - from: "rename_preview text half"
      to: "grep_mirror + exclusion matcher"
      via: "no bare walk/re"
      pattern: "grep_mirror"
    - from: "MCP/agents/knowledge_tools"
      to: "run_rename_preview"
      via: "thin shell / whitelist"
      pattern: "run_rename_preview|rename_preview"
    - from: "envelope"
      to: "applied=false"
      via: "hard lock every success path"
      pattern: "applied.: false|\\\"applied\\\": False"
---

<objective>
Wave 3：落地只读 `rename_preview` 双源清单（图引用 + grep_mirror 文本兜底）、MCP/对话薄壳、编码容器白名单（per D-09..D-12）。闭合 RENAME-01。

Purpose: 编码代理改名前有可审计清单且服务端永不写仓。
Output: rename_preview.py + run_rename_preview + 双面壳 + knowledge_tools 白名单 + 测试绿。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/126-process-rename-skills/126-CONTEXT.md
@.planning/phases/126-process-rename-skills/126-RESEARCH.md
@.planning/phases/126-process-rename-skills/126-PATTERNS.md
@.planning/phases/126-process-rename-skills/126-03-SUMMARY.md
@server/services/code_graph_tools.py
@server/mcp_tools/views.py
@server/services/repo_mirror.py
@task/core/knowledge_tools.py
</context>

## Artifacts this phase produces

| Symbol / artifact | Module | Introduced in |
|-------------------|--------|---------------|
| dual-source rename kernel | `services/code_graph/rename_preview.py` | **126-04** |
| `run_rename_preview` | `code_graph_tools.py` | **126-04** |
| MCP/agents `rename_preview` shells | `mcp_tools` / `agents/tools` | **126-04** |
| knowledge_tools whitelist entry | `task/core/knowledge_tools.py` | **126-04** |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: rename_preview 内核 + run_rename_preview（D-09/D-10/D-11）</name>
  <files>server/services/code_graph/rename_preview.py, server/services/code_graph_tools.py, server/tests/services/code_graph/test_rename_preview.py, server/tests/services/code_graph/test_frozen_surface_126.py</files>
  <read_first>
    - `.planning/phases/126-process-rename-skills/126-CONTEXT.md` D-09 / D-10 / D-11
    - `.planning/phases/126-process-rename-skills/126-RESEARCH.md` rename_preview envelope shape + Assumptions A3
    - `server/services/code_graph/symbol_resolve.py` 或 tools 内 `resolve_symbol_in_graph`（122 D-19 消歧）
    - `server/mcp_tools/views.py` `_filter_grep_result` / exclusion matcher
    - `services/repo_mirror.grep_mirror` 签名与用法
    - Wave 0 `test_rename_preview.py`
  </read_first>
  <behavior>
    - 成功信封：applied is False；confidence ∈ {graph, text_search}；可含 sources[]；summary 含 total_edits/files_affected/graph_edits/text_search_edits；coverage_limitations 非空
    - 同 file:line 双源 → 保留一条，展示 confidence=graph
    - 消歧失败 / 未索引 / ACL → ok=False，不得返回 ok=True 且 edits=[] 伪装零引用
    - 源码调用 grep_mirror；无 os.walk+re 裸扫路径
  </behavior>
  <action>
新建 `server/services/code_graph/rename_preview.py` + `run_rename_preview` 编排（D-09/D-10/D-11）：

输入：`repository_id` + `symbol_id` 优先 + 必填 `new_name`；可选 symbol/file_path/symbol_type 消歧（对齐 impact）。图半边：定义点 + 一跳 incoming callers（predecessors）等 RESEARCH A3 范围；文本半边：`grep_mirror` + 与 MCP Grep 同款 exclusion fail-closed。合并按文件分组 edits；context 默认 context_lines=2 上限 5；短片段，可对 context 做 redact。

信封字段对齐 RESEARCH JSON：`ok`/`applied:false`/`coverage_limitations`/`symbol`/`files[]`/`summary` + staleness/degradation。⛔ 无 apply API、不写工作树、不写 mirror。

日志：`rename_preview_started/completed/failed`，`component="code_graph"`，category caller；勿记录 PAT；context 采样级。

去掉 rename 测试 skip；更新 frozen_surface 列表含 rename_preview.py。⛔ 不碰 mcp/ submodule 与 repo_router_v2。
  </action>
  <acceptance_criteria>
    - `rg -n "applied|grep_mirror|text_search|coverage_limitations" server/services/code_graph/rename_preview.py server/services/code_graph_tools.py` 均有关键命中
    - `rg -n "def run_rename_preview" server/services/code_graph_tools.py` 命中
    - `cd server && GALAXY_CACHE_WARM_ON_STARTUP=False GRAPH_BUILD_ORPHAN_RECONCILE_ON_STARTUP=False uv run pytest tests/services/code_graph/test_rename_preview.py tests/services/code_graph/test_frozen_surface_126.py -q` 全绿
  </acceptance_criteria>
  <verify>
    <automated>cd server && GALAXY_CACHE_WARM_ON_STARTUP=False GRAPH_BUILD_ORPHAN_RECONCILE_ON_STARTUP=False uv run pytest tests/services/code_graph/test_rename_preview.py tests/services/code_graph/test_frozen_surface_126.py -q</automated>
  </verify>
  <done>只读双源 rename_preview 内核与编排测试绿；applied 恒 false</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MCP/对话薄壳 + knowledge 白名单（D-06 形态 / D-12）</name>
  <files>server/mcp_tools/views.py, server/mcp_tools/urls.py, server/mcp_tools/serializers.py, server/agents/tools/graph_tools.py, server/agents/tools/schemas/graph_tools.py, server/agents/tools/__init__.py, task/core/knowledge_tools.py, task/tests/core/test_knowledge_tools.py</files>
  <read_first>
    - `.planning/phases/126-process-rename-skills/126-CONTEXT.md` D-12
    - `server/mcp_tools/views.py` DetectChangesView 薄壳
    - `task/core/knowledge_tools.py` `detect_changes` 白名单块（约 L347–379）与工具计数断言测试位置
    - 若 `task/tests/core/test_knowledge_tools.py` 不存在：搜索 `task/tests` 内 whitelist / knowledge_tools 计数测试并扩展该文件
  </read_first>
  <behavior>
    - MCP/agents rename_preview 调用 run_rename_preview；壳内无合并算法
    - MCP 与 agents `@tool` 两条链均写 RetrievalTrace（或既有 chat-chain 等价留痕）；禁止仅 MCP 有 trace（PATTERNS Dual-face）
    - knowledge_tools 白名单含 name=rename_preview；失败/配额用尽记录原因并继续交付（与 detect_changes 文案同路）
    - 工具计数断言从 N 更新为 N+1（以现有测试期望为准）
  </behavior>
  <action>
MCP：`RenamePreviewView` + serializer + url `tools/rename_preview/`；PAT fail-closed；RetrievalTrace；ok=False 仍 HTTP 200 + 信封。

对话：`@tool(name="rename_preview", ...)` 调 `run_rename_preview`；**强制**按既有 impact/detect_changes agents 路径写 RetrievalTrace（或 chat-chain 等价 Interaction Ledger 留痕），与 MCP 双面对齐——不得只在 MCP View 落 trace。

`task/core/knowledge_tools.py`：增加 rename_preview 白名单条目（D-12）；input_schema 含 repository_id / symbol_id 或消歧字段 / new_name；描述强调先 preview 再自行编辑、失败不阻断。更新 task 侧计数/白名单测试（路径按仓库实际；若文件不存在则创建 `task/tests/core/test_knowledge_tools.py` 最小断言：白名单 names 含 rename_preview）。

SUMMARY 记账 server MCP 工具名漂移（不改 mcp/）。⛔ 显式路径提交；勿 `git add -A`。
  </action>
  <acceptance_criteria>
    - `rg -n "rename_preview" server/mcp_tools/urls.py server/agents/tools/graph_tools.py task/core/knowledge_tools.py` 均命中
    - `rg -n "RetrievalTrace|write_retrieval_trace|retrieval_trace" server/mcp_tools/views.py server/agents/tools/graph_tools.py` 两侧均有命中（或 agents 侧等价 chat-chain ledger 调用），证明双面都留痕
    - `cd server && GALAXY_CACHE_WARM_ON_STARTUP=False GRAPH_BUILD_ORPHAN_RECONCILE_ON_STARTUP=False uv run pytest tests/services/code_graph/test_rename_preview.py -q` 仍绿
    - `cd task && uv run pytest -k rename_preview -q` 全绿（或等价白名单测试文件全绿）
  </acceptance_criteria>
  <verify>
    <automated>cd task && uv run pytest -k rename_preview -q</automated>
  </verify>
  <done>双面壳 + 容器白名单可用；只读契约不变</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → rename_preview | 符号/新名输入不可信；须 ACL + 消歧 |
| rename text half → repo mirror | 必须 exclusion；禁止裸文件系统扫描 |
| task container → server tool | 白名单调用；失败 fail-soft |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-126-01 | Information Disclosure | grep half | mitigate | 强制 grep_mirror + exclusion matcher；测试静态禁裸扫 |
| T-126-02 | Spoofing | fake-empty edits | mitigate | 消歧/未索引/ACL → ok=False |
| T-126-03 | Information Disclosure | context/logs | mitigate | context_lines≤5；redact_secrets_in_text |
| T-126-05 | Tampering | server-side write | mitigate | applied=false；无 apply 路由/函数 |
| T-126-06 | Tampering | frozen surfaces | mitigate | frozen_surface_126 含 rename_preview.py |
| T-126-SC | Tampering | package installs | accept | 零新包 |
</threat_model>

<verification>
rename_preview 测试绿；knowledge 白名单测绿；frozen surface 绿；无 apply API。
</verification>

<success_criteria>
- RENAME-01：双源清单 + confidence 二值 + 按文件分组 + 动态引用限制声明 + 只读
- D-12：容器可调 rename_preview 且不阻断交付
</success_criteria>

<output>
Create `.planning/phases/126-process-rename-skills/126-04-SUMMARY.md` when done
</output>
