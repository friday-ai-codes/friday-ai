---
phase: 126-process-rename-skills
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - server/tests/codegraph/test_process_trace_model.py
  - server/tests/services/code_graph/test_process_trace.py
  - server/tests/services/code_graph/test_process_enqueue.py
  - server/tests/services/code_graph/test_affected_processes.py
  - server/tests/services/code_graph/test_process_query.py
  - server/tests/services/code_graph/test_rename_preview.py
  - server/tests/services/code_graph/test_frozen_surface_126.py
  - .planning/phases/126-process-rename-skills/126-VALIDATION.md
autonomous: true
requirements: [EXEC-01, EXEC-02, EXEC-03, RENAME-01, SKILL-01]
tags: [process-trace, rename-preview, skills, wave0, frozen-surface]

must_haves:
  truths:
    - "Wave 0 测试桩可被 pytest 收集，覆盖 ProcessTrace schema / BFS 四硬闸 / enqueue lock / affected_processes / process_query list+get / rename_preview / 冻结面（D-01..D-16 验收节点）"
    - "126-VALIDATION.md Per-Task Map 恰好对应 126-01..05 五个 plan，无幽灵第六 plan"
    - "本 plan 不改生产业务逻辑；⛔ 不 stage repo_router_v2.py、⛔ 不改 mcp/（D-16）"
  artifacts:
    - path: "server/tests/codegraph/test_process_trace_model.py"
      provides: "ProcessTrace schema 验收桩（D-01）"
      contains: "test_process_trace_fields_and_unique_together"
    - path: "server/tests/services/code_graph/test_process_trace.py"
      provides: "BFS 硬闸 + 社区分类验收桩（D-02/D-05）"
      contains: "test_bfs_respects_depth_branching_min_steps_conf"
    - path: "server/tests/services/code_graph/test_process_enqueue.py"
      provides: "QUEUE_GRAPH + process lock 验收桩（D-03）"
      contains: "test_enqueue_uses_queue_graph_and_process_lock"
    - path: "server/tests/services/code_graph/test_affected_processes.py"
      provides: "assemble_affected_processes 验收桩（D-07）"
      contains: "test_assemble_affected_processes_single_dialect"
    - path: "server/tests/services/code_graph/test_process_query.py"
      provides: "run_list/get_process + MCP/agents 薄壳 call-through 验收桩（D-06 / EXEC-02）"
      contains: "test_run_list_processes_and_get_process"
    - path: "server/tests/services/code_graph/test_rename_preview.py"
      provides: "rename 只读双源验收桩（D-09/D-10/D-11）"
      contains: "test_applied_always_false"
    - path: "server/tests/services/code_graph/test_frozen_surface_126.py"
      provides: "冻结面守卫桩（D-16）"
      contains: "test_phase_126_does_not_touch_repo_router_v2"
  key_links:
    - from: "126-VALIDATION.md Wave 0"
      to: "七个新测试文件"
      via: "节点名与 RESEARCH Validation Architecture 一一对应"
      pattern: "test_bfs_respects|test_run_list_processes|test_applied_always_false|test_frozen_surface_126"
    - from: "test_process_query.py"
      to: "run_list_processes / run_get_process + MCP/@tool"
      via: "call-through 断言（126-03 去 skip）"
      pattern: "run_list_processes|run_get_process"
    - from: "test_frozen_surface_126.py"
      to: "process_trace.py / rename_preview.py / process_enqueue.py"
      via: "AST 禁止 import repo_router_v2（文件落地后生效）"
      pattern: "repo_router_v2"
---

<objective>
Wave 0（Nyquist）：建齐 Phase 126 全部自动化验收测试桩，并刷新 VALIDATION 表与最终五 plan ID 对齐。覆盖 EXEC-01/02/03、RENAME-01、SKILL-01 与 D-16 冻结面节点名（实现由 126-02..05 去 skip）。

Purpose: 后续每个 task 的 `<automated>` 必须命中可收集节点；禁止先写生产逻辑再补测试名。
Output: 七个 Wave 0 测试文件（skip 桩）+ VALIDATION 与 126-01..05 对齐。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/126-process-rename-skills/126-CONTEXT.md
@.planning/phases/126-process-rename-skills/126-RESEARCH.md
@.planning/phases/126-process-rename-skills/126-PATTERNS.md
@.planning/phases/126-process-rename-skills/126-VALIDATION.md
@server/tests/services/code_graph/test_frozen_surface_125.py
@server/tests/services/code_graph/test_community_enqueue.py
@server/tests/codegraph/test_symbol_community_model.py
</context>

## Artifacts this phase produces

> Phase-level inventory（本 plan 只落测试桩；实现符号由 126-02..05 落地）

| Symbol / artifact | Module | Introduced in |
|-------------------|--------|---------------|
| Wave 0 test stubs (model/BFS/enqueue/affected/process_query/rename/frozen) | `server/tests/**` | **126-01** |
| `ProcessTrace` model + migration `0013_*` | `codegraph/models.py` | 126-02 |
| `rebuild_processes` / forward BFS / community classify | `services/code_graph/process_trace.py` | 126-02 |
| `enqueue_process_rebuild` + `durable_process_rebuild` | `process_enqueue.py` + durable | 126-02 |
| `run_list_processes` / `run_get_process` / `assemble_affected_processes` | `code_graph_tools.py` | 126-03 |
| MCP/agents list+get shells + impact_report 执行流段 | `mcp_tools` / `agents` / `impact_report.py` | 126-03 |
| `run_rename_preview` + dual-source kernel | `rename_preview.py` + shells | 126-04 |
| `friday-impact` / `friday-refactoring` + SKILL_NAMES sync/hash | `skills/` + `task/` | 126-05 |

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 验收测试桩（七文件）</name>
  <files>server/tests/codegraph/test_process_trace_model.py, server/tests/services/code_graph/test_process_trace.py, server/tests/services/code_graph/test_process_enqueue.py, server/tests/services/code_graph/test_affected_processes.py, server/tests/services/code_graph/test_process_query.py, server/tests/services/code_graph/test_rename_preview.py, server/tests/services/code_graph/test_frozen_surface_126.py</files>
  <read_first>
    - `.planning/phases/126-process-rename-skills/126-VALIDATION.md` §Wave 0 Requirements / Per-Task Verification Map
    - `.planning/phases/126-process-rename-skills/126-RESEARCH.md` §Validation Architecture → Phase Requirements → Test Map + Security Domain
    - `.planning/phases/126-process-rename-skills/126-PATTERNS.md` Hard Locks + Tests analog 段
    - `server/tests/services/code_graph/test_frozen_surface_125.py`（AST 冻结面守卫完整形态）
    - `server/tests/services/code_graph/test_community_enqueue.py`（enqueue 桩/断言形态）
    - `server/tests/codegraph/test_symbol_community_model.py`（模型 Meta 断言形态）
  </read_first>
  <action>
新建下列测试文件。每个行为用例暂为 `pytest.fail("Wave 0 桩")` 并挂 `@pytest.mark.skip(reason="Wave 0 桩：由 126-02/03/04/05 落地")`。⛔ 不改生产业务模块；⛔ 不 stage `repo_router_v2.py` / `mcp/`（D-16）。提交只 stage 本 task 列出路径。

`tests/codegraph/test_process_trace_model.py`（归属 126-02；D-01/D-04）：
- `test_process_trace_fields_and_unique_together`（字段最小集 + unique_together=(repository, branch_name, process_key)；类名字面量 ProcessTrace）
- `test_process_trace_has_no_endpoint_fk`（entry_endpoint 为 JSONField；无指向 Endpoint 的 ForeignKey）
- `test_process_trace_not_named_process`（codegraph.models 无名为 Process 的 ORM 类）

`tests/services/code_graph/test_process_trace.py`（归属 126-02；D-02/D-05；T-126-04）：
- `test_bfs_respects_depth_branching_min_steps_conf`（硬闸 maxDepth=10 / maxBranching=4 / minSteps=3 / conf≥0.5）
- `test_cycle_marked_not_silently_skipped`
- `test_async_dispatch_boundary_not_crossed`
- `test_community_class_intra_cross_unknown`（intra / cross / community_class_unknown 降级，不编造）
- `test_get_graph_only_no_loader_import`（静态：process_trace.py 不 import loader/cache；只经 get_graph_service）

`tests/services/code_graph/test_process_enqueue.py`（归属 126-02；D-03）：
- `test_enqueue_uses_queue_graph_and_process_lock`（idempotency_key / queueing_lock = process:{repo_id}:{branch}；QUEUE_GRAPH）
- `test_enqueue_passes_initiated_by_user_id`
- `test_community_success_chains_process_enqueue`（run_community_rebuild 成功路径 best-effort enqueue；raise 不链式）

`tests/services/code_graph/test_affected_processes.py`（归属 126-03；D-07）：
- `test_assemble_affected_processes_single_dialect`（输出键 name/process_key/affected_steps/total_steps/community_class）
- `test_run_impact_fills_affected_processes`
- `test_run_detect_changes_fills_affected_processes`
- `test_no_intersection_returns_empty_list`

`tests/services/code_graph/test_process_query.py`（归属 126-03；D-06 / EXEC-02）：
- `test_run_list_processes_and_get_process`（共享编排：list 过滤/排序 + get by process_key；信封含 ok/staleness；默认 cross_community 优先）
- `test_mcp_list_get_process_call_through`（薄壳 View 只调 `run_list_processes` / `run_get_process`，无算法分叉）
- `test_agents_list_get_process_call_through`（`@tool` 同名只调同一 `run_*`）

`tests/services/code_graph/test_rename_preview.py`（归属 126-04；D-09/D-10/D-11；T-126-01/02/05）：
- `test_applied_always_false`
- `test_dual_source_confidence_graph_or_text_search`
- `test_same_file_line_prefers_graph`
- `test_grep_half_uses_grep_mirror_not_bare`（源文件静态禁止另起 walk/re 裸扫）
- `test_disambiguation_or_unindexed_ok_false_not_fake_empty`
- `test_coverage_limitations_declared`

`tests/services/code_graph/test_frozen_surface_126.py`（归属全程；D-16；T-126-06）：
- 照抄 125 形态：`_NO_ROUTER_IMPORT` 含 `services/code_graph/process_trace.py`、`services/code_graph/rename_preview.py`、`services/process_enqueue.py`（文件尚未存在时 skip 单项或 path.is_file() continue）
- `test_phase_126_does_not_touch_repo_router_v2`
- `test_phase_126_does_not_modify_mcp_submodule`
- 允许 `mcp_tools/views.py` adapter；禁止 import `mcp` 包
  </action>
  <acceptance_criteria>
    - `cd server && GALAXY_CACHE_WARM_ON_STARTUP=False GRAPH_BUILD_ORPHAN_RECONCILE_ON_STARTUP=False uv run pytest tests/codegraph/test_process_trace_model.py tests/services/code_graph/test_process_trace.py tests/services/code_graph/test_process_enqueue.py tests/services/code_graph/test_affected_processes.py tests/services/code_graph/test_process_query.py tests/services/code_graph/test_rename_preview.py tests/services/code_graph/test_frozen_surface_126.py --collect-only -q` 收集到上方全部节点名
    - 各新文件含 Wave 0 桩 skip reason（`grep -v '^#' | grep -c 'Wave 0'` ≥ 1 per file，frozen 文件可实现真实 AST 断言而不 skip）
    - `rg -n "ProcessTrace|test_run_list_processes|test_applied_always_false|process:\\{repo" server/tests/codegraph/test_process_trace_model.py server/tests/services/code_graph/test_process_query.py server/tests/services/code_graph/test_rename_preview.py server/tests/services/code_graph/test_process_enqueue.py` 均有命中
  </acceptance_criteria>
  <verify>
    <automated>cd server && GALAXY_CACHE_WARM_ON_STARTUP=False GRAPH_BUILD_ORPHAN_RECONCILE_ON_STARTUP=False uv run pytest tests/codegraph/test_process_trace_model.py tests/services/code_graph/test_process_trace.py tests/services/code_graph/test_process_enqueue.py tests/services/code_graph/test_affected_processes.py tests/services/code_graph/test_process_query.py tests/services/code_graph/test_rename_preview.py tests/services/code_graph/test_frozen_surface_126.py --collect-only -q</automated>
  </verify>
  <done>全部 Wave 0 节点可收集；生产代码未改；冻结面守卫文件存在</done>
</task>

<task type="auto">
  <name>Task 2: VALIDATION 与五 plan 编号对齐</name>
  <files>.planning/phases/126-process-rename-skills/126-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/126-process-rename-skills/126-VALIDATION.md` 全文
    - 本目录即将落地的 `126-01`..`126-05` plan frontmatter（wave / requirements）
  </read_first>
  <action>
更新 `126-VALIDATION.md`（硬锁——与现行五 plan 对齐，禁止幽灵第六 plan）：

- Per-Task Verification Map **恰好 5 行主 plan**：`126-01` / `126-02` / `126-03` / `126-04` / `126-05`；另可保留一行 `126-XX-F` frozen 交叉引用，但不得出现 `126-06`
- 每行 Wave / Requirement / Automated Command 与 plan frontmatter 一致：
  - `126-01` → Wave **0**，Requirement **EXEC-01..SKILL-01**，Command = 七桩 collect-only（含 `test_process_query.py`）
  - `126-02` → Wave **1**，Requirement **EXEC-01 + EXEC-02(内核)**，Command = model + process_trace + process_enqueue（去 skip 后绿）
  - `126-03` → Wave **2**，Requirement **EXEC-02(查询) + EXEC-03**，Command = affected_processes + process_query + impact_report + schema snapshot
  - `126-04` → Wave **3**，Requirement **RENAME-01**，Command = test_rename_preview + task rename_preview whitelist
  - `126-05` → Wave **4**，Requirement **SKILL-01**，Command = test_skills_injection
- Wave 0 Requirements 文件列表与 Task 1 七文件一致；frontmatter 保持 `wave_0_complete: false` 直至本 plan SUMMARY 后由 executor 勾选
- Sampling Rate 标明 quick（task）vs full（wave/phase）
  </action>
  <acceptance_criteria>
    - `126-VALIDATION.md` 含字面量 `126-01`、`126-02`、`126-03`、`126-04`、`126-05`、`test_process_query`
    - `grep -E '^\\| 126-06' .planning/phases/126-process-rename-skills/126-VALIDATION.md | grep -c .` 结果为 **0**
    - Map 中 Wave 列分别为 `0`/`1`/`2`/`3`/`4`；Requirement 列覆盖 EXEC-01、EXEC-02、EXEC-03、RENAME-01、SKILL-01
  </acceptance_criteria>
  <verify>
    <automated>python -c "from pathlib import Path; t=Path('.planning/phases/126-process-rename-skills/126-VALIDATION.md').read_text(); assert all(x in t for x in ['126-01','126-02','126-03','126-04','126-05']); assert '| 126-06' not in t; print('ok')"</automated>
  </verify>
  <done>VALIDATION 与五 plan 编号对齐；无 126-06 幽灵行</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → production | Wave 0 桩钉死后续安全行为（exclusion / applied=false / BFS caps / 冻结面） |
| planner docs → executor | VALIDATION Map 是 Nyquist 契约；错误 plan ID 会导致验收失真 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-126-01 | Information Disclosure | rename grep half | mitigate | Wave 0 桩 `test_grep_half_uses_grep_mirror_not_bare` → 126-04 强制 grep_mirror + exclusion |
| T-126-02 | Spoofing | rename empty list | mitigate | 桩 `test_disambiguation_or_unindexed_ok_false_not_fake_empty` → 126-04 ok=False |
| T-126-03 | Information Disclosure | logs / context | mitigate | 后续 plan 短 context + redact；本 plan 不落生产日志 |
| T-126-04 | Denial of Service | Process BFS | mitigate | 桩四硬闸 → 126-02 常量 Final |
| T-126-05 | Tampering | server-side write | mitigate | 桩 `test_applied_always_false` → 126-04 无 apply API |
| T-126-06 | Tampering | frozen surfaces | mitigate | `test_frozen_surface_126` AST/git 守卫（D-16） |
| T-126-SC | Tampering | package installs | accept | 本相位零新增 pip/npm 运行时包 |
</threat_model>

<verification>
collect-only 列出全部 Wave 0 节点（含 process_query）；VALIDATION 五 plan 对齐；工作树 diff 不含 `repo_router_v2.py` / `mcp/`。
</verification>

<success_criteria>
- EXEC/RENAME/SKILL/D-16 验收节点名已登记为可收集桩（含 EXEC-02 list/get call-through）
- VALIDATION Map 恰好 126-01..05
- 生产代码零改动
</success_criteria>

<output>
Create `.planning/phases/126-process-rename-skills/126-01-SUMMARY.md` when done
</output>
