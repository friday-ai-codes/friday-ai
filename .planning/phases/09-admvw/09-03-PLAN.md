---
phase: 09-admvw
plan: 03
type: execute
wave: 2
depends_on: ["09-01", "09-02"]
files_modified:
  - web/src/api/adminConversations.ts
  - web/src/components/admin/ReadonlyConversationView.vue
  - web/src/pages/admin/conversations.vue
  - web/src/components/layout/AppSidebar.vue
autonomous: true
requirements: [ADMVW-01, ADMVW-02, ADMVW-03]
must_haves:
  truths:
    - "管理员访问 /admin/conversations（requiresAdmin）看到所有用户会话的 DataTable（含 owner 列）"
    - "点开会话以只读方式查看消息（无输入框/发送/编辑/删除入口）"
    - "每行/详情有「fork 到我的名下」动作 → 调 fork 端点 → 跳转 /chat?conversation=<新 id>"
    - "admin 导航出现「会话管理」入口（isSystemAdmin 可见）"
  artifacts:
    - path: "web/src/api/adminConversations.ts"
      provides: "listAdminConversations / getAdminConversation / forkAdminConversation + DTO"
      contains: "forkAdminConversation"
    - path: "web/src/pages/admin/conversations.vue"
      provides: "DataTable 列表 + 只读详情 + fork 动作（requiresAdmin）"
      contains: "requiresAdmin"
    - path: "web/src/components/admin/ReadonlyConversationView.vue"
      provides: "轻量只读消息查看器（getMarkdownRenderer + hydrateLegacyMessage，无写入入口）"
      contains: "getMarkdownRenderer"
  key_links:
    - from: "web/src/pages/admin/conversations.vue"
      to: "/api/admin/conversations/"
      via: "listAdminConversations / forkAdminConversation"
      pattern: "AdminConversation"
    - from: "web/src/pages/admin/conversations.vue"
      to: "/chat?conversation="
      via: "router.push after fork"
      pattern: "/chat\\?conversation="
    - from: "web/src/components/layout/AppSidebar.vue"
      to: "/admin/conversations"
      via: "adminNavItems entry"
      pattern: "/admin/conversations"
---

<objective>
实现管理员只读会话后台前端：新增 api 模块 adminConversations.ts、轻量只读消息查看器 ReadonlyConversationView.vue、列表页 conversations.vue（DataTable + 只读详情 + fork→跳转 /chat?conversation=），并在 AppSidebar.adminNavItems 加入口，使 09-01 前端 RED spec 转 GREEN。

Purpose: 满足 ADMVW-01（管理员浏览所有会话 UI）、ADMVW-02（只读视图无写入入口）、ADMVW-03（fork 到自己名下后跳转普通 chat 续聊）。前端守卫 requiresAdmin 仅 UX 兜底，真正授权在 09-02 后端 IsSuperUser。
Output: 3 新增前端文件 + AppSidebar 导航增量。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-admvw/09-RESEARCH.md
@web/src/pages/admin/users.vue
@web/src/components/layout/AppSidebar.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: api 模块 adminConversations.ts + AppSidebar 导航入口</name>
  <files>web/src/api/adminConversations.ts, web/src/components/layout/AppSidebar.vue</files>
  <read_first>
    - web/src/api/users.ts（api 模块范式：import { get, post } from './client'；导出函数 + interface DTO）
    - web/src/api/client.ts（get/post 签名、ApiError）
    - web/src/components/layout/AppSidebar.vue:54-61（adminNavItems 数组，`{ to, label, icon }`，已被 isSystemAdmin gate）
    - .planning/phases/09-admvw/09-RESEARCH.md §Code Examples（adminConversations api / 导航入口）
  </read_first>
  <action>
    新建 web/src/api/adminConversations.ts：
    - interface AdminConversationListItem { id, title, status, message_count, owner: { id, username, display_name } | null, space_id, created_at, updated_at }
    - listAdminConversations(params?: { owner_id?: string, q?: string }) → `get<AdminConversationListItem[]>('/admin/conversations/' + 可选 querystring)`（用 URLSearchParams 拼接）。(ADMVW-01)
    - getAdminConversation(id) → `get('/admin/conversations/${id}/')`（返回会话+messages，类型可用 detail DTO 或 any/最小 interface）。(ADMVW-01/02)
    - forkAdminConversation(id) → `post<{ conversation_id: string }>('/admin/conversations/${id}/fork/', {})`。(ADMVW-03)

    在 AppSidebar.vue adminNavItems 数组新增一项 `{ to: '/admin/conversations', label: '会话管理', icon: 'lucide--messages-square' }`（放在 /admin/users 后或合适位置）。不改其它导航项。
  </action>
  <verify>
    <automated>cd web && pnpm vue-tsc --noEmit -p tsconfig.json 2>&1 | rg "adminConversations|AppSidebar" || echo "no type errors in new api/nav"</automated>
  </verify>
  <acceptance_criteria>
    - adminConversations.ts 导出 listAdminConversations / getAdminConversation / forkAdminConversation 与 AdminConversationListItem。
    - 路径均以 `/admin/conversations/` 为前缀（对齐 09-02 端点）。
    - AppSidebar adminNavItems 含 `to: '/admin/conversations'` 项；类型检查无新增报错。
  </acceptance_criteria>
  <done>api 契约与导航入口就位。</done>
</task>

<task type="auto">
  <name>Task 2: 轻量只读消息查看器 ReadonlyConversationView.vue</name>
  <files>web/src/components/admin/ReadonlyConversationView.vue</files>
  <read_first>
    - web/src/composables/useMarkdownRenderer（getMarkdownRenderer 用法）
    - web/src/composables/useMessageParts（hydrateLegacyMessage：parts/legacy 消息归一）
    - web/src/components/chat/ChatMessageBubble.vue:1-70（**仅参考** parts/role 渲染结构；不复用——深耦合 chatStore）
    - .planning/phases/09-admvw/09-RESEARCH.md §Standard Stack（getMarkdownRenderer + hydrateLegacyMessage）+ Alternatives Considered（为何新建轻量查看器）
  </read_first>
  <action>
    新建 web/src/components/admin/ReadonlyConversationView.vue：props `messages`（数组，来自 getAdminConversation 响应的 messages）。对每条消息用 hydrateLegacyMessage 归一 parts，再用 getMarkdownRenderer 渲染文本 parts；区分 role（user/assistant）样式。**纯只读**：组件内无 `<input>`/`<textarea>`/发送按钮/编辑/删除/导出入口，不引入 chatStore。空消息列表显示占位文案。
    保持轻量（无流式、无选择、无工具调用交互），仅展示历史消息内容。
  </action>
  <verify>
    <automated>cd web && pnpm vue-tsc --noEmit -p tsconfig.json 2>&1 | rg "ReadonlyConversationView" || echo "no type errors"</automated>
  </verify>
  <acceptance_criteria>
    - 组件接收 messages prop 并渲染（markdown via getMarkdownRenderer，parts via hydrateLegacyMessage）。
    - 组件源码不含输入框/发送/编辑/删除入口，且不 import chatStore（`rg "useChatStore|chatStore" web/src/components/admin/ReadonlyConversationView.vue` 为空）。
  </acceptance_criteria>
  <done>只读查看器实现，无任何写入入口，与普通 chat 渲染同源。</done>
</task>

<task type="auto">
  <name>Task 3: conversations.vue 列表页（DataTable + 只读详情 + fork 跳转）</name>
  <files>web/src/pages/admin/conversations.vue</files>
  <read_first>
    - web/src/pages/admin/users.vue（definePage requiresAdmin + DataTable + PageHeader + PageContainer + useErrorHandler/useToast + ColumnDef 范式）
    - web/src/components/common/DataTable.vue（props/列定义/行交互）
    - web/src/api/adminConversations.ts（本 plan Task 1 产物）
    - web/src/components/admin/ReadonlyConversationView.vue（本 plan Task 2 产物）
    - .planning/phases/09-admvw/09-RESEARCH.md §Code Examples（fork → `router.push('/chat?conversation=' + id)`，已确认 chat.ts restoreFromURL 读 `conversation` query 键）
  </read_first>
  <action>
    新建 web/src/pages/admin/conversations.vue：`definePage({ meta: { requiresAdmin: true } })`。用 PageContainer + PageHeader + DataTable 列出会话（列：owner（owner.display_name||username）、标题、状态、消息数、更新时间），onMounted 调 listAdminConversations 加载，含搜索（q）/owner 过滤可选。(ADMVW-01)
    - 行点击：调 getAdminConversation(id) 取详情，在抽屉/对话框中用 ReadonlyConversationView 只读展示 messages（无输入/发送/编辑/删除入口）。(ADMVW-02)
    - 提供「fork 到我的名下」动作（行操作或详情内按钮）：`const { conversation_id } = await forkAdminConversation(id)` → useToast success('已复制到我的名下') → `router.push('/chat?conversation=' + conversation_id)`。(ADMVW-03)
    - 错误用 useErrorHandler。照 admin/users.vue 风格组织。
  </action>
  <verify>
    <automated>cd web && pnpm vitest run src/pages/admin/__tests__/conversations.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && pnpm vitest run src/pages/admin/__tests__/conversations.spec.ts` exits 0（09-01 前端 spec 全 GREEN）。
    - 页面 definePage meta 含 requiresAdmin: true；挂载后调用 listAdminConversations 并渲染含 owner 的表格。
    - 只读：页面无消息输入/发送/编辑/删除入口。
    - fork 动作触发 forkAdminConversation 并 `router.push('/chat?conversation=<id>')`。
  </acceptance_criteria>
  <done>列表页 + 只读详情 + fork 跳转完成，前端 spec 转 GREEN。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 浏览器路由守卫 requiresAdmin | 仅 UX 兜底，非安全边界；真正授权在后端 IsSuperUser（09-02） |
| 前端 → /api/admin/conversations/ | 所有跨用户读取/fork 经后端鉴权；前端不持有特权数据 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-06 | Elevation of Privilege | 依赖前端守卫做安全 | mitigate | requiresAdmin 仅 UX；后端 IsSuperUser 为唯一可信 gate（V4，09-02 已实现并测试） |
| T-09-07 | Tampering | 只读视图被误用为写入入口 | mitigate | ReadonlyConversationView 不含任何写入控件、不耦合 chatStore；spec 断言无写入入口 |
| T-09-08 | Information Disclosure | fork 后跳转 URL 携带会话 id | accept | id 为非敏感 UUID；跳转后走普通 chat owner gate，admin 已是 owner，无越权 |
</threat_model>

<verification>
- `cd web && pnpm vitest run src/pages/admin/__tests__/conversations.spec.ts` → exit 0。
- `cd web && pnpm vue-tsc --noEmit -p tsconfig.json` 无新增类型错误。
- 人工（VALIDATION manual）：管理员登录 → 会话管理页见他人会话 → 只读详情无输入框 → fork → 跳转 chat 以 owner 续聊成功。
</verification>

<success_criteria>
- ADMVW-01/02/03 前端全部满足：DataTable 列出所有会话、只读消息查看、fork→跳转续聊、导航入口可见。
- 前端 spec 全 GREEN；类型检查通过。
</success_criteria>

<output>
Create `.planning/phases/09-admvw/09-03-SUMMARY.md` when done
</output>
