---
phase: 09-admvw
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - server/tests/test_admin_conversations.py
  - web/src/pages/admin/__tests__/conversations.spec.ts
autonomous: true
requirements: [ADMVW-01, ADMVW-02, ADMVW-03]
must_haves:
  truths:
    - "RED 测试集存在并可被收集：后端 test_admin_conversations.py + 前端 conversations.spec.ts"
    - "测试 1:1 钉死 ADMVW-01（admin 看全部 / 非 admin 403 / 匿名拒绝）、ADMVW-02（写方法 405）、ADMVW-03（fork 归属 admin + status=DRAFT + 复制消息）"
    - "Phase 8 隔离套件被显式纳入回归保障（不改动，必须保持全绿）"
  artifacts:
    - path: "server/tests/test_admin_conversations.py"
      provides: "ADMVW-01/02/03 + 非 admin 403 + 匿名拒绝 + fork 归属 的 RED 断言"
      contains: "def test_admin_list_sees_all_users"
    - path: "web/src/pages/admin/__tests__/conversations.spec.ts"
      provides: "admin 会话页 requiresAdmin + DataTable + 只读无写入入口 spec"
      contains: "requiresAdmin"
  key_links:
    - from: "server/tests/test_admin_conversations.py"
      to: "/api/admin/conversations/"
      via: "AsyncClient GET/POST"
      pattern: "/api/admin/conversations/"
---

<objective>
为 Phase 9（管理员只读会话后台）建立 Wave 0 RED 验证脚手架：写出后端 integration 测试与前端组件 spec，钉死 ADMVW-01/02/03 的可验证行为契约。生产代码尚未实现，故本批断言**预期全部 RED**；Wave 1（后端 09-02）与 Wave 2（前端 09-03）落地后转 GREEN。

Purpose: 先固化行为契约（admin 看全部 / 非 admin 403 / 只读 405 / fork 归属 admin），并显式纳入 Phase 8 隔离回归保障，确保后续实现不削弱普通路径 owner 隔离。
Output: server/tests/test_admin_conversations.py（新）、web/src/pages/admin/__tests__/conversations.spec.ts（新）。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-admvw/09-CONTEXT.md
@.planning/phases/09-admvw/09-RESEARCH.md
@.planning/phases/09-admvw/09-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 后端 RED 测试集 test_admin_conversations.py</name>
  <files>server/tests/test_admin_conversations.py</files>
  <read_first>
    - server/tests/test_conversation_isolation.py（复用 helper 范式：`_acreate_conversation(project, owner=...)`、`AsyncClient`、`RefreshToken.for_user(user)` 生成 JWT、`User = get_user_model()`；消息创建用 `Message.objects.acreate`）
    - server/chat/models.py（Conversation.Status 枚举含 DRAFT；Message 字段 role/content/tool_calls/parts/metadata）
    - .planning/phases/09-admvw/09-RESEARCH.md §Validation Architecture「Phase Requirements → Test Map」（测试函数名清单）+ §Common Pitfalls Pitfall 3（非 admin → 403，admin 取不存在会话 → 404）
  </read_first>
  <action>
    新建 server/tests/test_admin_conversations.py（pytest-asyncio + pytest-django，文件头 `pytestmark = pytest.mark.django_db`，async 用例标 `@pytest.mark.asyncio`）。复用 test_conversation_isolation.py 的 owner-可注入 helper 与 JWT helper（可 import 或仿写 `_acreate_conversation` / `RefreshToken.for_user`）。fixtures：创建一个 superuser（admin）、两个普通用户 user_a / user_b，各自 project + 至少一条带消息的会话。

    覆盖以下用例（端点前缀 /api/admin/conversations/，沿用 settings 默认认证，需登录）：
    - test_admin_list_sees_all_users：admin JWT GET 列表 → 200 且响应含 user_a 与 user_b 两个不同 owner 的会话（断言 owner.id 集合覆盖两人）。(ADMVW-01)
    - test_non_admin_list_403：user_a JWT GET 列表 → 403（IsSuperUser，非 admin 明确 403，非 404）。(ADMVW-01)
    - test_anonymous_denied：无 Authorization GET 列表 → 401 或 403（拒绝匿名；断言 status in {401, 403}）。(ADMVW-01)
    - test_admin_detail_other_user：admin GET `/<conversation_id>/`（user_a 的会话）→ 200，响应含 messages 列表。(ADMVW-01/02)
    - test_admin_readonly_no_write：admin 对 detail 路径 PATCH 与 DELETE → 405（只读，未实现写方法，DRF 自动 405）。(ADMVW-02)
    - test_admin_fork_creates_admin_owned_copy：admin POST `/<conversation_id>/fork/`（user_a 的会话，含 N 条消息）→ 200/201，返回 conversation_id；据返回 id 查库断言新会话 `created_by_id == admin.id` 且消息条数 == N。(ADMVW-03)
    - test_admin_fork_status_and_source_intact：fork 副本 `status == Conversation.Status.DRAFT`，且源会话 `created_by_id` 不变、消息条数不变。(ADMVW-03)
    - test_non_admin_fork_403：user_a POST fork → 403。(ADMVW-03)

    断言判定语义遵循 Pitfall 3：admin gate 用 403（非 404-everything）。禁用 `--watch`。NEVER 在普通 /api/chat/ 路径加任何断言改写。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/test_admin_conversations.py --co -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd server && uv run pytest tests/test_admin_conversations.py --co -q` 能成功收集全部用例（无 import/collection error）。
    - 此刻运行 `cd server && uv run pytest tests/test_admin_conversations.py -q` 因生产代码缺失而 RED（端点不存在 → 404/连接失败），属预期。
    - 文件含 `test_admin_list_sees_all_users`、`test_non_admin_list_403`、`test_admin_readonly_no_write`、`test_admin_fork_creates_admin_owned_copy`、`test_admin_fork_status_and_source_intact`、`test_non_admin_fork_403` 函数定义。
  </acceptance_criteria>
  <done>后端 RED 测试集可收集，ADMVW-01/02/03 全部行为契约以可执行断言固化。</done>
</task>

<task type="auto">
  <name>Task 2: 前端 RED 组件 spec conversations.spec.ts</name>
  <files>web/src/pages/admin/__tests__/conversations.spec.ts</files>
  <read_first>
    - web/src/pages/admin/users.vue（admin 列表页范式：`definePage({ meta: { requiresAdmin: true } })` + DataTable + PageHeader + PageContainer）
    - web/src/components/codegraph/__tests__/playground.spec.ts（vitest + @vue/test-utils + happy-dom mount/stub 范式，若存在；否则仿 web 既有 spec）
    - .planning/phases/09-admvw/09-RESEARCH.md §Frontend / Code Examples（adminConversations api 形态、fork→`/chat?conversation=` 跳转）
  </read_first>
  <action>
    新建 web/src/pages/admin/__tests__/conversations.spec.ts（vitest）。mock `~/api/adminConversations`（listAdminConversations 返回含 owner 的假数据、forkAdminConversation 返回 `{ conversation_id }`）与 `~/composables/useToast`/`useErrorHandler`，挂载 `~/pages/admin/conversations.vue`。

    断言（当前 RED，因 conversations.vue 尚不存在 → import 失败）：
    - 页面导出/`definePage` meta 含 `requiresAdmin: true`（可通过组件模块或路由 meta 断言；若框架限制，改为断言页面挂载后渲染 DataTable 容器）。
    - 挂载后调用 listAdminConversations 并渲染表格行（含 owner 列文本）。
    - 只读语义：渲染结果中**不含**消息输入框/发送按钮/编辑/删除入口（断言不存在对应 selector / 文案）。
    - 存在「fork 到我的名下」动作；触发后调用 forkAdminConversation 并以 `/chat?conversation=<id>` 形式触发 router.push（mock router 断言入参）。

    禁用 watch 模式（用 `pnpm vitest run`）。
  </action>
  <verify>
    <automated>cd web && pnpm vitest run src/pages/admin/__tests__/conversations.spec.ts || true</automated>
  </verify>
  <acceptance_criteria>
    - spec 文件存在且语法合法（vitest 能加载该 spec）。
    - 当前运行因 `conversations.vue` 缺失而 RED（模块解析失败或断言失败），属预期。
    - spec 含 requiresAdmin、DataTable 渲染、只读无写入入口、fork→`/chat?conversation=` 跳转 四类断言。
  </acceptance_criteria>
  <done>前端 RED spec 就位，覆盖 requiresAdmin、只读、fork 跳转契约。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→/api/admin/conversations/ | 跨用户会话读取入口，仅 superuser 可越过；测试在此边界钉死 403/401 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Elevation of Privilege | admin 端点（垂直越权） | mitigate | test_non_admin_list_403 / test_non_admin_fork_403 钉死非 admin → 403 |
| T-09-02 | Spoofing | 匿名访问 admin | mitigate | test_anonymous_denied 钉死匿名 → 401/403 |
| T-09-03 | Elevation of Privilege | Phase 8 普通路径回归 | mitigate | 显式保留 test_conversation_isolation.py 为回归基线（本 plan 不改动它，验收要求全绿） |
</threat_model>

<verification>
- `cd server && uv run pytest tests/test_admin_conversations.py --co -q` 通过收集。
- `cd server && uv run pytest tests/test_conversation_isolation.py -q` 仍全绿（未被本 plan 改动）。
- `cd web && pnpm vitest run src/pages/admin/__tests__/conversations.spec.ts` 可加载（RED 属预期）。
</verification>

<success_criteria>
- 后端 + 前端 RED 测试脚手架存在、可收集/加载，ADMVW-01/02/03 行为契约可执行化。
- Phase 8 隔离套件被纳入回归保障且未被改动。
</success_criteria>

<output>
Create `.planning/phases/09-admvw/09-01-SUMMARY.md` when done
</output>
