---
phase: 09-admvw
plan: 02
type: execute
wave: 1
depends_on: ["09-01"]
files_modified:
  - server/chat/conversation_service.py
  - server/chat/serializers.py
  - server/chat/admin_views.py
  - server/chat/admin_urls.py
  - server/friday/urls.py
autonomous: true
requirements: [ADMVW-01, ADMVW-02, ADMVW-03]
must_haves:
  truths:
    - "superuser GET /api/admin/conversations/ 返回跨用户全部会话（含 owner + message_count）"
    - "非 superuser → 403，匿名 → 401/403（IsSuperUser + 默认认证类，不复用 chat 开放模式认证）"
    - "admin 端点只读：detail 仅 GET，PATCH/DELETE → 405；无 stream/send 路由"
    - "admin fork 深拷贝会话+全部消息，created_by=admin，status=DRAFT，源会话不变"
    - "Phase 8 普通 /api/chat/ 路径与隔离套件不被改动、保持全绿"
  artifacts:
    - path: "server/chat/admin_views.py"
      provides: "AdminConversationListView / AdminConversationDetailView / AdminConversationForkView（IsSuperUser）"
      exports: ["AdminConversationListView", "AdminConversationDetailView", "AdminConversationForkView"]
    - path: "server/chat/admin_urls.py"
      provides: "/conversations/ , /conversations/<uuid>/ , /conversations/<uuid>/fork/ 路由"
      contains: "admin-conversation-list"
    - path: "server/chat/conversation_service.py"
      provides: "admin_list_conversations / admin_get_with_messages / admin_fork_to_own"
      contains: "async def admin_fork_to_own"
    - path: "server/chat/serializers.py"
      provides: "AdminConversationListSerializer（+owner +message_count）"
      contains: "class AdminConversationListSerializer"
  key_links:
    - from: "server/friday/urls.py"
      to: "chat.admin_urls"
      via: "api_patterns path admin/ include"
      pattern: "include\\(\"chat.admin_urls\"\\)"
    - from: "server/chat/admin_views.py"
      to: "ConversationService.admin_fork_to_own"
      via: "AdminConversationForkView.post"
      pattern: "admin_fork_to_own"
---

<objective>
实现物理分离、显式管理员授权的只读会话后台后端：在 chat app 内新增 admin_views.py + admin_urls.py，挂载到 /api/admin/conversations/，由 IsSuperUser + 默认认证类守卫；新增 ConversationService.admin_* 业务方法与 AdminConversationListSerializer，使 09-01 后端 RED 用例转 GREEN。**严禁**改动 Phase 8 锁定的 /api/chat/ 路径或在其上加 superuser bypass。

Purpose: 满足 ADMVW-01（跨用户只读列表/详情）、ADMVW-02（只读 = 不实现写方法 → 405）、ADMVW-03（admin fork 显式归属当前管理员），同时保持 ISO-03 隔离不回退。
Output: 5 个后端文件（2 新增 view/urls + 3 处增量：service/serializer/顶层 url）。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-admvw/09-RESEARCH.md
@server/chat/conversation_service.py
@server/chat/serializers.py
@server/permissions/api_permissions.py
@server/friday/urls.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ConversationService admin_* 业务方法 + AdminConversationListSerializer</name>
  <files>server/chat/conversation_service.py, server/chat/serializers.py</files>
  <read_first>
    - server/chat/conversation_service.py:751-823（`fork_conversation_before_message` 蓝本：acreate 字段 project_id/title/model/provider_credential_id_id/created_by_id；消息复制字段 role/content/tool_calls(deepcopy)/tool_call_id/metadata(deepcopy)/parts(deepcopy)）
    - server/chat/serializers.py:180-262（ConversationListSerializer / ConversationMessageSerializer / ConversationDetailSerializer 字段契约与 `_id` 列 async-safe 读法）
    - server/chat/models.py（Conversation.Status.DRAFT、Message ↔ Conversation related_name 用于 Count("messages")）
    - .planning/phases/09-admvw/09-RESEARCH.md §Pattern 3/4 + §Code Examples（admin_fork_to_own / admin_list_conversations / AdminConversationListSerializer 参考实现）+ Pitfall 1（select_related 预取 + sync_to_async 包 .data）
  </read_first>
  <behavior>
    - admin_list_conversations(owner_id=None, q="")：返回 `Conversation.objects.filter(is_deleted=False)` 全集（无 created_by 过滤），select_related("created_by","project") + annotate(message_count=Count("messages"))，可选叠加 created_by_id=owner_id 与 title__icontains=q，order_by("-updated_at")。
    - admin_get_with_messages(conversation_id)：取单会话（无 owner 过滤）+ 其消息列表（order_by created_at），select_related 预取 created_by/project；不存在 → 抛 DoesNotExist（view 兜成 404）。
    - admin_fork_to_own(conversation_id, admin_user)：深拷贝会话（created_by=admin_user，status=DRAFT，title 加「（管理员副本）」≤200）+ 复制**全部**消息（不截断），返回 `{"conversation_id": <new>}`；源会话不被修改。
  </behavior>
  <action>
    在 ConversationService 内新增三个 async @staticmethod（紧邻 fork_conversation_before_message 之后）：admin_list_conversations、admin_get_with_messages、admin_fork_to_own（per ADMVW-01/02/03）。

    - admin_fork_to_own 以 fork_conversation_before_message 为蓝本，**三处差异**：(1) created_by 显式 = admin_user（非继承源 owner，规避 Pitfall 5 owner 继承错误）；(2) 复制全部消息（去掉 `created_at__lt=target.created_at` 截断）；(3) status=Conversation.Status.DRAFT（规避 Pitfall 4 pin 冻结）。provider_credential_id_id 携带源值（与普通 fork 一致；status=DRAFT 不冻结，admin 可改）。结构化日志 `logger.info("admin_conversation_forked", admin_id=..., source_conversation_id=..., forked_conversation_id=..., copied_count=...)`（V7 审计留痕）。
    - admin_list_conversations 走无 owner 过滤 queryset（per Pattern 4），务必 select_related("created_by","project") 预取（Pitfall 1）。
    - admin_get_with_messages 返回 conversation 对象（已预取）+ messages 列表，供 view 用 ConversationDetailSerializer 序列化。

    serializers.py 新增 `_OwnerBriefSerializer`（id/username/display_name）与 `AdminConversationListSerializer`（id、space_id(source="project_id")、title、status、model、message_count(IntegerField read_only，来自 annotate)、owner(SerializerMethodField 读 obj.created_by，None 安全)、created_at、updated_at）。**不修改**既有 ConversationListSerializer/DetailSerializer/MessageSerializer 契约。

    NEVER 改动既有 owner-scoped 方法（aget_for_user 等）或 /api/chat/ 端点。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/test_admin_conversations.py -k "fork or status" -q || true</automated>
  </verify>
  <acceptance_criteria>
    - `admin fork sets created_by=admin and status=draft and copies messages`：admin_fork_to_own 后新会话 created_by_id == admin.id、status == DRAFT、消息条数 == 源会话消息条数，源会话不变。
    - admin_list_conversations 返回跨多个 owner 的会话且每条带 message_count（annotate）。
    - 既有 owner-scoped 方法签名与 /api/chat/ 端点未被改动（git diff 仅触及新增方法与新增 serializer 类）。
  </acceptance_criteria>
  <done>service 三方法 + AdminConversationListSerializer 落地，fork 归属/状态/消息复制断言可过。</done>
</task>

<task type="auto">
  <name>Task 2: admin_views.py（List / Detail / Fork，IsSuperUser + 默认认证）</name>
  <files>server/chat/admin_views.py</files>
  <read_first>
    - server/chat/views.py（ConversationListView/DetailView/ForkView：adrf `APIView` + `async def get/post` + Response 范式；注意**不要**复制其 authentication_classes/permission_classes）
    - server/accounts/views.py:187 附近（MeView：`sync_to_async(lambda: Serializer(...).data)()` 包裹 .data 的 async-safe 范式）
    - server/permissions/api_permissions.py:21（IsSuperUser）
    - .planning/phases/09-admvw/09-RESEARCH.md §Pattern 1/2 + Pitfall 2（不复用 OptionalJWTAuthentication/ChatAuthPermission）+ Pitfall 3（admin 取不存在会话 → 404）
  </read_first>
  <action>
    新建 server/chat/admin_views.py，定义三个继承 `adrf.views.APIView` 的类，**统一** `permission_classes = [IsSuperUser]`（import from permissions.api_permissions），**不设** authentication_classes（沿用 settings 默认 [AccessTokenAuthentication, CookieJWTAuthentication]，强制登录、拒匿名；规避 Pitfall 2）：
    - AdminConversationListView：`async def get(self, request)` 读 query_params owner_id/q → 调 admin_list_conversations → `sync_to_async(lambda: AdminConversationListSerializer(qs, many=True).data)()` → Response(data)。(ADMVW-01)
    - AdminConversationDetailView：`async def get(self, request, conversation_id)` → admin_get_with_messages；将 messages 附到 conversation 后用 ConversationDetailSerializer（复用，含 messages 嵌套）`sync_to_async` 序列化 → Response；DoesNotExist → `Response(status=404)`（普通 not-found，per Pitfall 3）。**只定义 get**（不写 patch/delete/post → DRF 自动 405，满足 ADMVW-02）。
    - AdminConversationForkView：`async def post(self, request, conversation_id)` → admin_fork_to_own(conversation_id, request.user) → `Response({"conversation_id": ...}, status=201)`；DoesNotExist → 404。(ADMVW-03)

    所有关联字段访问前确保 service 层已 select_related 预取；.data 一律 sync_to_async 包裹（Pitfall 1）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/test_admin_conversations.py -k "list or detail or readonly or 403 or anonymous" -q || true</automated>
  </verify>
  <acceptance_criteria>
    - `GET /api/admin/conversations/ as non-superuser returns 403`（test_non_admin_list_403）。
    - `GET /api/admin/conversations/ as superuser returns conversations from multiple owners`（test_admin_list_sees_all_users）。
    - 匿名 GET → 401/403（test_anonymous_denied）。
    - `PATCH /api/admin/conversations/{id}/ returns 405`、DELETE → 405（test_admin_readonly_no_write）。
    - admin GET detail 他人会话 → 200 含 messages（test_admin_detail_other_user）。
  </acceptance_criteria>
  <done>三个 admin view 实现，授权/只读/详情用例通过；无写方法、无匿名放行。</done>
</task>

<task type="auto">
  <name>Task 3: admin_urls.py 路由 + 顶层挂载到 /api/admin/</name>
  <files>server/chat/admin_urls.py, server/friday/urls.py</files>
  <read_first>
    - server/chat/urls.py（urlpatterns 形态：`path(...)` + `<uuid:conversation_id>` 参数命名约定）
    - server/friday/urls.py:22-61（api_patterns 列表，确认 `path("chat/", include("chat.urls"))` 位置，在其并列处新增 admin/ 行）
    - .planning/phases/09-admvw/09-RESEARCH.md §Code Examples「admin_urls.py」「顶层挂载」
  </read_first>
  <action>
    新建 server/chat/admin_urls.py，urlpatterns 三条（per ADMVW-01/02/03）：
    - `path("conversations/", AdminConversationListView.as_view(), name="admin-conversation-list")`
    - `path("conversations/<uuid:conversation_id>/", AdminConversationDetailView.as_view(), name="admin-conversation-detail")`
    - `path("conversations/<uuid:conversation_id>/fork/", AdminConversationForkView.as_view(), name="admin-conversation-fork")`
    从 chat.admin_views import 三个 view。`<uuid:...>` 路径转换器即 V5 输入校验（非法 id → URLconf 不匹配）。

    在 server/friday/urls.py 的 `api_patterns` 内、与 `path("chat/", include("chat.urls"))` 并列处新增一行 `path("admin/", include("chat.admin_urls"))` → 端点前缀 /api/admin/conversations/。**不得**改动 chat.urls 或其它既有行。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/test_admin_conversations.py tests/test_conversation_isolation.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_admin_conversations.py -q exits 0`（ADMVW-01/02/03 全 GREEN）。
    - `uv run pytest tests/test_conversation_isolation.py -q exits 0`（Phase 8 隔离回归保持全绿）。
    - `rg "is_superuser" server/chat/views.py server/chat/conversation_service.py` 在普通端点/owner-scoped 方法处**无**新增 superuser bypass（仅新增 admin_* 方法可含 admin 语义）。
    - /api/admin/conversations/ 三路由可解析（reverse("admin-conversation-list") 成功）。
  </acceptance_criteria>
  <done>路由挂载完成，admin 后端两套测试全绿且 Phase 8 隔离不回退。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→/api/admin/conversations/ | 跨用户会话读取/fork 入口；唯一可信授权点在后端 IsSuperUser |
| admin 路径 ↔ Phase 8 /api/chat/ 路径 | 物理分离边界；admin 端点不得回写普通路径的 owner 过滤 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Elevation of Privilege | 非 admin 调 admin 端点（垂直越权，V4） | mitigate | permission_classes=[IsSuperUser]；非 admin → 403（测试钉死） |
| T-09-02 | Spoofing | 匿名/X-Chat-Key 访问 admin | mitigate | 不复用 OptionalJWTAuthentication/ChatAuthPermission；用默认认证类要求登录 |
| T-09-03 | Elevation of Privilege | 经普通路径加 superuser bypass 绕过 ISO-03 | mitigate | admin 路径全新独立；不改 chat/views.py；回归套件 test_conversation_isolation.py 守卫 + grep is_superuser 守卫 |
| T-09-04 | Tampering/Repudiation | fork 归属混乱/冒充 | mitigate | fork 显式 created_by=admin（设计即归属变更）+ logger.info 审计留痕 admin_id+源会话 |
| T-09-05 | Tampering | list 过滤参数注入 | mitigate | ORM 参数化 filter(title__icontains=q)；owner_id 由 created_by_id 比对；fork id 用 `<uuid:>` 校验 |
</threat_model>

<verification>
- `cd server && uv run pytest tests/test_admin_conversations.py -q` → exit 0。
- `cd server && uv run pytest tests/test_conversation_isolation.py -q` → exit 0（回归基线全绿）。
- git diff 确认 server/chat/views.py、server/chat/urls.py 未被改动。
</verification>

<success_criteria>
- ADMVW-01/02/03 后端行为全部 GREEN：跨用户只读列表/详情、写方法 405、fork 归属 admin + status=DRAFT + 复制全部消息。
- Phase 8 隔离套件保持 100% 全绿；普通 /api/chat/ 路径零改动、无新增 superuser bypass。
</success_criteria>

<output>
Create `.planning/phases/09-admvw/09-02-SUMMARY.md` when done
</output>
