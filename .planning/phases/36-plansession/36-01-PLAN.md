---
phase: 36-plansession
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/workflows/nodes/ai/plan_generation.py
  - server/agents/tools/langchain_adapter.py
  - server/agents/tools/verify_plan.py
  - server/tests/test_langchain_adapter.py
  - server/tests/workflows/test_plan_generation_tools_guard.py
  - server/tests/agents/test_verify_plan.py
autonomous: true
requirements: [PF-01, PF-02]

must_haves:
  truths:
    - "plan_generation 引用的检索工具名与注册名一致（search_repository_code），prompt 与 get_enabled_tools 中不再出现 search_code"
    - "build_langchain_tools 对不在 _tool_registry 的工具名 fail-loud raise（ValueError），不再静默 continue"
    - "verify_plan 校验 execution_plan（title + 非空 execution_plan list + 逐项 repository_id/coding_instruction），不再校验不存在的 tasks"
    - "verify_plan 结果契约形状 {valid, errors, warnings, summary} 与 success=True 不变，plan_generation:484 verify_plan 结果读取仍可用"
  artifacts:
    - path: "server/workflows/nodes/ai/plan_generation.py"
      provides: "统一工具名 search_repository_code（prompt + get_enabled_tools）"
      contains: "search_repository_code"
    - path: "server/agents/tools/langchain_adapter.py"
      provides: "未知工具 fail-loud raise"
      contains: "raise ValueError"
    - path: "server/agents/tools/verify_plan.py"
      provides: "execution_plan schema 对齐校验"
      contains: "execution_plan"
    - path: "server/tests/workflows/test_plan_generation_tools_guard.py"
      provides: "工具名一致性守护测试"
    - path: "server/tests/agents/test_verify_plan.py"
      provides: "verify_plan execution_plan 校验测试"
  key_links:
    - from: "server/workflows/nodes/ai/plan_generation.py:get_enabled_tools"
      to: "agents.tools._tool_registry"
      via: "build_langchain_tools 装配"
      pattern: "search_repository_code"
    - from: "server/agents/tools/verify_plan.py"
      to: "server/workflows/schemas/technical_plan.py"
      via: "execution_plan 字段对齐"
      pattern: "execution_plan"
---

<objective>
修两个 blocking 前置漂移（PREFLIGHT PF-01 / PF-02），补齐 v0.7 方案质量与 PlanValidator 的地基：
1. **PF-01**：`ai_plan_generation` 引用的检索工具名 `search_code` 与注册名 `search_repository_code` 不一致；`build_langchain_tools` 对未知工具静默 `continue` 跳过 → server 端方案生成的检索工具从未真正生效。统一工具名 + 把静默跳过改 fail-loud raise。
2. **PF-02**：`verify_plan` 校验 `tasks` 字段，canonical 方案 schema（`technical_plan.py`）实为 `execution_plan` → 校验形同虚设。对齐到 `execution_plan`，使校验真正命中——作为 Phase 40 `PlanValidator` 的基础。

Purpose: 这两项是 should-fix-before-v0.7 的 blocking 项（PREFLIGHT 已 verified），必须在编排 engine 工作前落地并带回归守护测试。
Output: 修正后的工具名/适配器/校验工具 + 守护测试（工具名一致性、fail-loud、execution_plan 有效/无效用例）。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/PREFLIGHT.md
@.planning/phases/36-plansession/36-CONTEXT.md

# 源文件（grounding）
@server/workflows/nodes/ai/plan_generation.py
@server/agents/tools/langchain_adapter.py
@server/agents/tools/verify_plan.py
@server/workflows/schemas/technical_plan.py
@server/agents/tools/space_tools.py
@server/tests/test_langchain_adapter.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PF-01 统一检索工具名 + build_langchain_tools fail-loud</name>
  <files>
    server/workflows/nodes/ai/plan_generation.py,
    server/agents/tools/langchain_adapter.py,
    server/tests/test_langchain_adapter.py,
    server/tests/workflows/test_plan_generation_tools_guard.py
  </files>
  <behavior>
    - build_langchain_tools(["__nonexistent__"]) 抛 ValueError，错误消息包含未知工具名 "__nonexistent__" 与可用工具名集合（来自 _tool_registry keys）。
    - build_langchain_tools(["search_repository_code"]) 仍正常返回 1 个 StructuredTool（已知工具不受影响）。
    - 守护：get_enabled_tools(context) 返回的每个工具名都在 agents.tools.base._tool_registry。
    - 守护：_PLAN_GENERATION_BASE_PROMPT（system prompt 常量）中不再出现子串 "search_code"，且其引用的检索工具名 "search_repository_code" 存在于 _tool_registry。
  </behavior>
  <action>
    PF-01 工具名漂移修复（per 36-CONTEXT「PF-01 工具名漂移修复」决策，统一到注册名，不引入别名机制）：
    (1) 在 `plan_generation.py` `get_enabled_tools()` 的 `base_tools` 列表把 `"search_code"` 改为 `"search_repository_code"`（与 `space_tools.py` 注册名 `name="search_repository_code"` 一致）。
    (2) 在 `_PLAN_GENERATION_BASE_PROMPT` 常量内所有 `search_code` 文案改为 `search_repository_code`（含「仓库分析：使用 search_code 工具」段 与「每一轮迭代都应该调用至少一个工具（search_code、verify_plan...）」段，约原始第 52/83 行对应位置）。先用 Grep 在 server/ 全仓搜索 `search_code`（排除测试），把任何 prompt 种子/数据迁移中出现的 `search_code` 一并改为 `search_repository_code`，避免 DB 种子覆盖运行时 prompt 仍漂移。
    (3) 在 `langchain_adapter.py` `build_langchain_tools` 把 `if tool_name not in _tool_registry: continue` 改为 fail-loud：`raise ValueError`，消息含未知工具名 + 排序后的可用工具名集合（`sorted(_tool_registry)`）。同步更新该函数 docstring 中「不在 _tool_registry 的名字静默跳过」措辞为 fail-loud 语义（理由：静默跳过正是 PF-01 根因；白名单正确性由调用方 + 守护测试保证，不靠 adapter 吞错）。
    (4) 更新既有 `server/tests/test_langchain_adapter.py`：`test_unknown_tool_silently_skipped` 改为断言 `pytest.raises(ValueError)`（重命名为 test_unknown_tool_fails_loud），并断言消息含工具名；`test_unknown_tool_mixed_with_known_returns_only_known` 改为断言混入未知工具时整体 raise（不再返回部分结果）。模块顶部 docstring 「未知工具静默跳过」一行同步改为 fail-loud。
    (5) 新建 `server/tests/workflows/test_plan_generation_tools_guard.py`：实例化 `AIPlanGenerationNode`，对最小 ExecutionContext（node_config 含 user_prompt）调 `get_enabled_tools`，断言每个返回名都在 `_tool_registry`；并扫描 `_PLAN_GENERATION_BASE_PROMPT` 断言无 `search_code` 子串、含 `search_repository_code`。不要在 action 内联实现代码——按 behavior 写测试。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/test_langchain_adapter.py tests/workflows/test_plan_generation_tools_guard.py -x -q</automated>
  </verify>
  <done>build_langchain_tools 未知工具 raise ValueError（含工具名+可用集）；plan_generation prompt 与 get_enabled_tools 无 search_code、统一 search_repository_code；守护测试 + 既有适配器测试全绿。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PF-02 verify_plan 校验对齐到 execution_plan</name>
  <files>
    server/agents/tools/verify_plan.py,
    server/tests/agents/test_verify_plan.py
  </files>
  <behavior>
    - 合法方案 {title:"非空标题", execution_plan:[{repository_id:"r1", coding_instruction:"足够长的编码指令..."}]} → output.valid=True, errors=[]。
    - 缺 title → errors 含 field=title；execution_plan 缺失或空 list → errors 含 field=execution_plan。
    - execution_plan[i] 非对象 → errors 含 field=execution_plan[i]；缺 repository_id → errors 含 field=execution_plan[i].repository_id；缺 coding_instruction → errors 含 field=execution_plan[i].coding_instruction。
    - 工具自身恒 success=True，校验结论在 output；output 形状恒为 {valid, errors, warnings, summary}。
    - 校验失败时 valid=False；校验通过时可有 warnings（不阻断）。
  </behavior>
  <action>
    PF-02 schema 对齐（per 36-CONTEXT「PF-02 verify_plan schema 对齐」决策，本 phase 只做最小对齐，扩展校验留 Phase 40 PlanValidator）：
    (1) `verify_plan.py` Layer 1 必填字段从 `["title", "tasks"]` 改为 `["title", "execution_plan"]`；逐项校验从 `tasks` 改为 `execution_plan`：每项必须是 dict（否则 `execution_plan[i]` 错误），且含非空 `repository_id` 与 `coding_instruction`（对齐 `technical_plan.py` execution_plan items 与 DOMAIN §7 MergedPlan.execution_plan「每任务: repository_id + coding_instruction」）。
    (2) Layer 2 质量警告把 `task.get("instruction")` 改为 `item.get("coding_instruction")` 的长度提示（保留 title 过短警告）。
    (3) 工具 `description` 与 `parameters.properties.plan.description` 文案从「需包含 title 和 tasks」改为「需包含 title 和 execution_plan」。
    (4) 保持返回 `ToolResult(success=True, output={valid, errors, warnings, summary})` 契约形状不变（不改键集），确保 `plan_generation.py:484-490` 经 `verify_plan` 结果读取 `tool_input["plan"]` 的逻辑不受影响——该逻辑读 input 不读 output，本改动不触碰，但需在 SUMMARY 注明已核对未回归。
    (5) 新建 `server/tests/agents/test_verify_plan.py` 按 behavior 覆盖有效/各类无效 execution_plan 用例 + 契约形状断言（async test，verify_plan 是 async tool，用 pytest.mark.asyncio）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/agents/test_verify_plan.py -x -q</automated>
  </verify>
  <done>verify_plan 校验 execution_plan（title + 非空 list + 逐项 repository_id/coding_instruction）；契约 {valid,errors,warnings,summary}+success=True 不变；有效/无效用例全绿；已核对 plan_generation:484 读取不回归。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM → verify_plan | LLM 产出的方案 dict 是半可信输入，verify_plan 须对结构 fail-safe（缺字段/类型错不崩溃） |
| 节点白名单 → build_langchain_tools | 工具白名单由节点代码维护，错误名应 fail-loud 暴露而非静默吞 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-01-01 | Tampering | verify_plan(plan) 输入 | mitigate | 逐项 isinstance(dict) 校验 + .get 取值，非法结构落 errors 不抛异常（工具恒 success=True） |
| T-36-01-02 | Denial of Service | build_langchain_tools 错误工具名 | mitigate | fail-loud raise ValueError，使白名单错误在装配期立即暴露，不静默退化为「无检索工具」的劣化运行 |

无 npm/pip/cargo 安装（不新增依赖），无 T-36-SC 供应链门。
</threat_model>

<verification>
- `cd server && uv run pytest tests/test_langchain_adapter.py tests/workflows/test_plan_generation_tools_guard.py tests/agents/test_verify_plan.py -q` 全绿。
- `cd server && rg -n "search_code" workflows/ agents/ prompts/ --glob '!**/tests/**'` 无残留（仅允许 `search_repository_code`）。
- `cd server && uv run ruff format --check workflows/nodes/ai/plan_generation.py agents/tools/langchain_adapter.py agents/tools/verify_plan.py` 通过（line 100）。
</verification>

<success_criteria>
- 成功标准 1（PF-01）：server 端方案生成检索工具真正生效——prompt 引用名与注册名一致（search_repository_code），build_langchain_tools 未知工具 fail-loud raise 不再静默 continue。
- 成功标准 2（PF-02）：verify_plan 校验对齐 execution_plan（不再校验不存在的 tasks），真正命中关键字段，作为 v0.7 PlanValidator 的基础。
</success_criteria>

<output>
Create `.planning/phases/36-plansession/36-01-SUMMARY.md` when done.
</output>
