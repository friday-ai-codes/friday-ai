---
phase: 142-mcp
plan: "04"
type: execute
wave: 2
depends_on: ["142-02", "142-03"]
files_modified:
  - .planning/phases/142-mcp/142-VALIDATION.md
autonomous: true
requirements: [MCP-01, MCP-02, MCP-03, MCP-04]
must_haves:
  truths:
    - "服务端 HTTP、schema snapshot、npm 白名单与 annotations 在合并后同时通过"
    - "report_project_knowledge 的项目门闩、git-diff Memory 路径、质量门与 HTTP 语义零回归"
    - "新 view 继续满足 SessionCapture INV-6，且 CaptureService/SessionCapture 无 Phase 142 范围外改动"
    - "McpToolView 的 ToolCallRecord 与 RequestMetric 生命周期可观察，client 仅作为 redacted audit metadata 保留"
    - "Phase 142 的四项 requirement、全部锁定决策与研究约束均有可执行计划和自动化证据"
  artifacts:
    - path: ".planning/phases/142-mcp/142-VALIDATION.md"
      provides: "Wave 0、Nyquist、任务验证与最终 phase gate 的完成证据"
  key_links:
    - from: "server/mcp_tools/serializers.py"
      to: "mcp/src/tools.ts"
      via: "test_report_session_knowledge_request_keys_aligned"
      pattern: "serializer_keys == snapshot_keys == npm_property_keys"
    - from: "server/mcp_tools/views.py"
      to: "server/initiatives/services/capture_service.py"
      via: "HTTP 集成测试与 INV-6 guard"
      pattern: "CaptureService\\(\\)\\.persist"
    - from: "server/mcp_tools/views.py"
      to: "server/interactions/models.py"
      via: "McpToolView._record 产生 report_session_knowledge ToolCallRecord"
      pattern: "report_session_knowledge"
---

<objective>
执行 Phase 142 的跨面回归与范围审计，在所有自动化门禁通过后收口 Nyquist validation。

Purpose: 证明服务器、snapshot、npm 与旧工具在同一最终状态下兼容，而不是各自局部变绿。
Output: 已验证的 142-VALIDATION.md 与完整 phase gate 证据。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/142-mcp/142-CONTEXT.md
@.planning/phases/142-mcp/142-RESEARCH.md
@.planning/phases/142-mcp/142-VALIDATION.md
@.planning/phases/142-mcp/142-02-SUMMARY.md
@.planning/phases/142-mcp/142-03-SUMMARY.md
@.planning/phases/141-capture/141-VERIFICATION.md
@server/tests/mcp_tools/test_report_project_knowledge.py
@server/tests/initiatives/test_capture_inv6_guard.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 运行跨面 phase gate 并登记验证结果</name>
  <files>.planning/phases/142-mcp/142-VALIDATION.md</files>
  <action>先运行 quick server gate，随后运行完整 server phase gate 与 npm server tests。所有测试必须在包含 Plan 02 与 Plan 03 的最终工作树上通过；不得用 xfail/skip、放宽断言或删旧测试换绿。把 VALIDATION 的 Per-Task Verification Map 对应行更新为 green，Wave 0 文件项打勾并设置 wave_0_complete=true，但只有全部命令成功才可写完成状态。额外确认 test_client_metadata_is_accepted_and_audited 证明 client 在公开请求 schema 中可接受、经 McpToolView._record 保留于 redacted ToolCallRecord.input，同时 SessionCapture 无 client 列；不要新增 migration 或扩 CaptureService 签名。</action>
  <verify>
    <automated>cd server && uv run pytest tests/mcp_tools/test_report_session_knowledge.py tests/mcp_tools/test_schema_snapshot.py tests/mcp_tools/test_mcp_package_alignment.py tests/mcp_tools/test_report_project_knowledge.py tests/initiatives/test_capture_inv6_guard.py -q --tb=short && uv run pytest tests/mcp_tools/ tests/initiatives/test_capture_service.py tests/initiatives/test_capture_inv6_guard.py tests/initiatives/test_memory_inv6_guard.py -q --tb=short && cd ../mcp && npm test -- tests/server.test.ts</automated>
  </verify>
  <done>quick、full 与 npm 三道 gate 全绿，VALIDATION 的每项状态与实际命令结果一致。</done>
</task>

<task type="auto">
  <name>Task 2: 审计旧工具隔离、观测复用与规划范围</name>
  <files>.planning/phases/142-mcp/142-VALIDATION.md</files>
  <action>执行静态与差异审计：新 view 不得出现 SessionCapture.objects.create、_resolve_report_project_id、MemoryService、evaluate_writeback_quality、RetrievalTrace 正文或自建指标；必须通过 _begin/_validate/_record 与 CaptureService.persist。确认 report_project_knowledge 的生产 view、serializer、npm 条目和既有测试未因 Phase 142 改写；运行其全文件回归以保留项目门闩、git-diff Memory、draft/active、201 与 branch_unresolved 旧语义。确认 SessionCapture 模型和 CaptureService 没有为 client 扩列/扩签名，client 的唯一持久证据是既有 redacted ToolCallRecord audit metadata。确认未修改 skills、hooks、评估、入图、召回或回放文件，也未暂存/覆盖进入本阶段前的无关脏文件。全部满足后将 VALIDATION status 置为 validated、nyquist_compliant=true，完成 Sign-Off 清单并记录实际通过数量。</action>
  <verify>
    <automated>cd server && uv run pytest tests/mcp_tools/test_report_project_knowledge.py tests/initiatives/test_capture_inv6_guard.py tests/initiatives/test_memory_inv6_guard.py -q --tb=short && git diff --exit-code -- initiatives/models/session_capture.py initiatives/services/capture_service.py tests/mcp_tools/test_report_project_knowledge.py</automated>
  </verify>
  <done>旧工具和 INV-6 回归全绿，范围外核心文件无 diff，VALIDATION 只在证据齐全后标记 validated/Nyquist complete。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 三面 schema → 发布契约 | server serializer/snapshot 与 npm 白名单必须原子对齐 |
| 新 Capture 工具 → 旧 Memory 工具 | 两条写路径用途、门闩、HTTP 与持久化模型必须隔离 |
| 执行工作树 → 用户既有脏文件 | 计划执行不得覆盖、暂存或提交无关改动 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-142-02 | Information Disclosure | ToolCallRecord/Capture | mitigate | client 审计测试 + Capture 脱敏测试；无正文日志/trace |
| T-142-04 | Tampering | schema publication | mitigate | server snapshot、npm 名集与字段级三面 gate 同时运行 |
| T-142-06 | Tampering | accepted response | mitigate | HTTP 测试以数据库行证明 accepted=true |
| T-142-07 | Elevation of Privilege | legacy Memory path | mitigate | 旧工具全文件回归、INV-6 guard 与范围 diff gate |
| T-142-08 | Repudiation | MCP lifecycle | mitigate | ToolCallRecord 与 RequestMetric 继续由 McpToolView._record 产生 |
| T-142-SC | Tampering | dependency chain | accept | 无新包、无 lockfile 变更 |
</threat_model>

<source_coverage_audit>

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | IDE 可通过稳定新 MCP 工具提交会话知识，挂钩失败不影响 Capture | 01-04 | COVERED | HTTP、server、npm、final gate |
| REQ | MCP-01 | report_session_knowledge 必填 Q/A + 完整可选元数据 | 01-03 | COVERED | client 公开接受并审计保留 |
| REQ | MCP-02 | 无项目/仓解析失败/默认分支仍 200 accepted=true + Capture | 01-02 | COVERED | 禁止 branch_unresolved 未收 |
| REQ | MCP-03 | serializer/snapshot/npm 三面对齐 | 01-04 | COVERED | 新工具字段级，旧工具不扩 |
| REQ | MCP-04 | report_project_knowledge 零回归 | 01,02,04 | COVERED | 不改旧实现 + 全文件 gate |
| RESEARCH | — | 复用 CaptureService，不重写挂钩/脱敏/幂等 | 02,04 | COVERED | INV-6 + scope diff |
| RESEARCH | — | 复用 McpToolView 认证、InteractionRun、RequestMetric、Ledger | 01,02,04 | COVERED | client audit 测试与 lifecycle |
| RESEARCH | — | npm 52 工具及专用幂等 annotations | 01,03,04 | COVERED | 不改 generator() |
| RESEARCH | — | 无新依赖；不实现 eval/ingest/recall/hooks | 01-04 | COVERED | 每计划禁止项与最终范围审计 |
| CONTEXT | D-01..D-04 | 工具名、请求/响应、401/400/200 语义 | 01-03 | COVERED | Wave 0 + server/npm |
| CONTEXT | D-05..D-08 | persist-only、归因、link reason、幂等 | 01,02,04 | COVERED | HTTP DB 证据 |
| CONTEXT | D-09..D-12 | 三面对齐、annotations、专项测试、不修旧漂移 | 01-04 | COVERED | 字段级门禁仅新工具 |
| CONTEXT | D-13..D-16 | 旧工具隔离、McpToolView 观测、无敏感正文、零回归 | 01,02,04 | COVERED | full regression + diff gate |

Deferred Ideas（Phase 143–145 的评估/入图、召回/回放、skills/hooks，以及旧 snapshot 历史漂移修复）均未进入计划。
</source_coverage_audit>

<verification>
- `cd server && uv run pytest tests/mcp_tools/test_report_session_knowledge.py tests/mcp_tools/test_schema_snapshot.py tests/mcp_tools/test_mcp_package_alignment.py tests/mcp_tools/test_report_project_knowledge.py tests/initiatives/test_capture_inv6_guard.py -q --tb=short`
- `cd server && uv run pytest tests/mcp_tools/ tests/initiatives/test_capture_service.py tests/initiatives/test_capture_inv6_guard.py tests/initiatives/test_memory_inv6_guard.py -q --tb=short`
- `cd mcp && npm test -- tests/server.test.ts`
- `cd server && uv run ruff check mcp_tools/serializers.py mcp_tools/views.py mcp_tools/urls.py tests/mcp_tools/test_report_session_knowledge.py tests/mcp_tools/test_mcp_package_alignment.py tests/mcp_tools/test_schema_snapshot.py`
</verification>

<success_criteria>
- MCP-01..04 均有自动测试和最终合并态证据。
- VALIDATION frontmatter 为 status=validated、wave_0_complete=true、nyquist_compliant=true。
- client 公开契约与现有 SessionCapture 模型已显式协调，没有静默丢 schema 或范围外 migration。
- 旧 report_project_knowledge 与用户原有无关脏文件保持不变。
</success_criteria>

<output>
Create `.planning/phases/142-mcp/142-04-SUMMARY.md` when done.
</output>
