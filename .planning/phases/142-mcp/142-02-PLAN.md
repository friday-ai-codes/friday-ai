---
phase: 142-mcp
plan: "02"
type: execute
wave: 1
depends_on: ["142-01"]
files_modified:
  - server/mcp_tools/serializers.py
  - server/mcp_tools/views.py
  - server/mcp_tools/urls.py
autonomous: true
requirements: [MCP-01, MCP-02, MCP-03, MCP-04]
must_haves:
  truths:
    - "已认证用户可经 /api/mcp/tools/report_session_knowledge/ 提交问答并获得 HTTP 200、accepted=true 与真实 capture_id"
    - "任何仓库或项目挂钩结果只改变 reason/FK，不会在合法问答已持久化时变成 accepted=false"
    - "client 作为公开可选元数据被 serializer 接受，并随 McpToolView._record 进入脱敏审计输入；SessionCapture 与 CaptureService 公共签名保持不变"
    - "新入口通过 McpToolView 的 _begin/_validate/_record 复用认证、InteractionRun、RequestMetric 与 ToolCallRecord 生命周期"
    - "report_project_knowledge 的 serializer、view 与路由行为不变"
  artifacts:
    - path: "server/mcp_tools/serializers.py"
      provides: "ReportSessionKnowledgeRequestSerializer 与完整 TOOL_SCHEMA_SNAPSHOT 条目"
      contains: "class ReportSessionKnowledgeRequestSerializer"
    - path: "server/mcp_tools/views.py"
      provides: "只调用 CaptureService.persist 的 ReportSessionKnowledgeView"
      contains: "class ReportSessionKnowledgeView"
    - path: "server/mcp_tools/urls.py"
      provides: "report_session_knowledge MCP POST 路由"
      contains: "tools/report_session_knowledge/"
  key_links:
    - from: "server/mcp_tools/views.py"
      to: "server/initiatives/services/capture_service.py"
      via: "CaptureService.persist(actor=request.user, initiated_by_user_id=request.user.id, ...)"
      pattern: "CaptureService\\(\\)\\.persist"
    - from: "server/mcp_tools/views.py"
      to: "server/interactions/ledger.py"
      via: "McpToolView._record(input_data=validated, output_data=..., traces=[])"
      pattern: "self\\._record"
    - from: "server/mcp_tools/urls.py"
      to: "server/mcp_tools/views.py"
      via: "ReportSessionKnowledgeView.as_view()"
      pattern: "ReportSessionKnowledgeView\\.as_view"
---

<objective>
实现 report_session_knowledge 的 Django MCP 服务端契约，把 Phase 141 唯一 Capture writer 暴露为稳定、可审计且挂钩失败不丢数据的入口。

Purpose: 满足 MCP-01/02 的接受语义，并建立 MCP-03 服务端 schema 面；保持 MCP-04 旧项目记忆工具完全隔离。
Output: serializer、schema snapshot、McpToolView 子类与 URL 路由。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/142-mcp/142-CONTEXT.md
@.planning/phases/142-mcp/142-RESEARCH.md
@.planning/phases/142-mcp/142-PATTERNS.md
@.planning/phases/142-mcp/142-01-SUMMARY.md
@server/mcp_tools/serializers.py
@server/mcp_tools/views.py
@server/mcp_tools/urls.py
@server/initiatives/services/capture_service.py
@server/initiatives/models/session_capture.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 定义请求 serializer 与服务端 schema snapshot</name>
  <files>server/mcp_tools/serializers.py</files>
  <behavior>
    - D-01/D-02：question、answer 是唯一必填非空字段；其余十个元数据字段均可省略。
    - D-02：repository_id/project_id 用可空 UUIDField；session_id 是普通字符串；token 字段和 client 使用开放 CharField，未知客户端不得被 ChoiceField 拒收。
    - D-03/D-09：snapshot 请求键与 serializer 字段精确一致，响应键完整且固定。
    - D-12：report_project_knowledge 的历史 snapshot 条目不变。
  </behavior>
  <action>在 ReportProjectKnowledgeRequestSerializer 相邻位置新增 ReportSessionKnowledgeRequestSerializer。question/answer 使用 allow_blank=False、max_length=20000；git_url 最大 500；branch_name/session_id 最大 255；response_model 最大 128；provider/input_tokens/output_tokens/client 使用与既有模型和审计用途相称的开放 CharField，均 required=False、允许缺省/空值。不要加入 project_id/branch_name 至少一个的 validate 门闩。向 TOOL_SCHEMA_SNAPSHOT 新增 report_session_knowledge，严格使用 Wave 0 锁定的 12 个请求键和 7 个响应键；不要修 report_project_knowledge 的旧 snapshot 漂移。client 的公开字段必须保留，即使 SessionCapture 当前无列。本任务不注册 URL；test_registered_tools_match_snapshot 留给 Task 2。不要跑三面相等测试（Plan 03 并行尚未改 tools.ts）。</action>
  <verify>
    <automated>cd server && uv run pytest tests/mcp_tools/test_schema_snapshot.py::test_mcp_read_tool_schema_snapshot tests/mcp_tools/test_mcp_package_alignment.py::test_report_session_knowledge_serializer_matches_snapshot -q --tb=short</automated>
  </verify>
  <done>serializer 字段集与 TOOL_SCHEMA_SNAPSHOT request 精确一致，snapshot 字面量守卫通过；未要求 urls 名集或 npm properties。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 接线 McpToolView、CaptureService 与 URL</name>
  <files>server/mcp_tools/views.py, server/mcp_tools/urls.py</files>
  <behavior>
    - D-04/D-05：_begin/_validate 成功后调用 CaptureService.persist；合法问答 persist 成功一律 HTTP 200 accepted=true。
    - D-06：actor=request.user 与 initiated_by_user_id=request.user.id 显式透传，仓库、项目、会话、模型和 token 元数据交给既有 persist。
    - D-07/D-08：reason 原样取 result.link_reason；idempotent_hit 与既有 capture_id 原样响应。
    - D-14/D-15：成功路径调用 _record(..., traces=[])；不新增正文日志、RetrievalTrace、第二套指标或 ToolCallRecord 直写。
    - D-13/D-16：新 view 不引用 _resolve_report_project_id、MemoryService、质量门、draft/active 或 ProjectMemory。
  </behavior>
  <action>导入 CaptureService 与 ReportSessionKnowledgeRequestSerializer，新增 tool_name='report_session_knowledge' 的 McpToolView 子类。post 严格按 _begin → _validate → started_at → CaptureService.persist → output mapping → _record → Response(HTTP_200_OK) 执行。向 persist 透传 question、answer、actor、initiated_by_user_id、session_id、project_id、repository_id、git_url、branch_name、response_model、provider、input_tokens、output_tokens；不要传 client，因为现有 SessionCapture/CaptureService schema 没有该字段。必须把包含 client 的完整 validated input_data 交给 _record，使其由既有 redact_for_ledger 后保留为 audit metadata，明确协调公开 schema 与现有模型而不加 migration。响应 repository_id/project_id 从 result.capture 的实际 FK 转为字符串或 null；accepted 仅在 persist 返回后置 True。注册 tools/report_session_knowledge/ 路由，使 test_registered_tools_match_snapshot 在本任务 verify 中通过。不要调用 _resolve_report_project_id，不要直接 SessionCapture.objects.create，不要复制 report_project_knowledge 的质量门、201、accepted=false 或 blanket fail-soft。CaptureService 已有 caller started/completed/failed；本 view 只依赖 McpToolView _record 提供 InteractionRun、RequestMetric 与 ToolCallRecord，不新增可能携带正文的日志。不要跑 test_report_session_knowledge_request_keys_aligned（三面相等属 Plan 04）。</action>
  <verify>
    <automated>cd server && uv run pytest tests/mcp_tools/test_report_session_knowledge.py tests/mcp_tools/test_schema_snapshot.py::test_registered_tools_match_snapshot tests/initiatives/test_capture_inv6_guard.py -q --tb=short</automated>
  </verify>
  <done>服务端 HTTP 用例全部通过，client 可被接受并在审计输入保留，INV-6 仍只允许 CaptureService 写 SessionCapture，旧工具源码未改。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IDE MCP client → ReportSessionKnowledgeRequestSerializer | 不可信正文和元数据进入 DRF 校验边界 |
| ReportSessionKnowledgeView → CaptureService | 已认证请求进入唯一脱敏、授权、幂等 writer |
| McpToolView._record → metrics/ledger | 调用元数据进入 best-effort 观测与脱敏审计 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-142-01 | Spoofing | ReportSessionKnowledgeView | mitigate | 继承 McpToolView 认证与 _begin，不自建权限分支 |
| T-142-02 | Information Disclosure | question/answer/git_url/client | mitigate | 正文仅进 CaptureService 脱敏；审计仅经 _record/redact_for_ledger；不打正文日志 |
| T-142-03 | Elevation of Privilege | repository/project link | mitigate | 完整交给 CaptureService access scope；未授权不绑 FK 但仍落独立 Capture |
| T-142-05 | Tampering | idempotent retry | mitigate | 不在 view 重写幂等，原样映射 CapturePersistResult |
| T-142-06 | Tampering | accepted flag | mitigate | 只在 persist 成功返回后构造 accepted=true 和 capture_id |
| T-142-07 | Elevation of Privilege | legacy project memory | mitigate | 新 view 零 MemoryService/_resolve_report_project_id；运行旧工具回归 |
| T-142-SC | Tampering | package install | accept | 无新增依赖 |
</threat_model>

<verification>
- `cd server && uv run ruff check mcp_tools/serializers.py mcp_tools/views.py mcp_tools/urls.py tests/mcp_tools/test_report_session_knowledge.py`
- `cd server && uv run pytest tests/mcp_tools/test_report_session_knowledge.py tests/mcp_tools/test_schema_snapshot.py::test_registered_tools_match_snapshot tests/initiatives/test_capture_inv6_guard.py -q --tb=short`
- `git diff -- server/initiatives/models/session_capture.py server/initiatives/services/capture_service.py server/tests/mcp_tools/test_report_project_knowledge.py` 必须为空。
</verification>

<success_criteria>
- 新 URL 可通过既有 PAT/JWT MCP 认证调用。
- 所有合法问答在挂钩失败时仍产生 Capture 并返回 200/accepted=true。
- client 不被公开 schema 拒绝或悄悄删除，且只在现有脱敏审计元数据中保留，不扩大 SessionCapture。
- McpToolView lifecycle 与 CaptureService caller lifecycle 均被复用，没有重复观测实现。
</success_criteria>

<output>
Create `.planning/phases/142-mcp/142-02-SUMMARY.md` when done.
</output>
