---
phase: 84-project-workbench-ui
plan: 05
type: execute
wave: 3
depends_on: [84-01, 84-02, 84-03, 84-04]
files_modified:
  - web/src/pages/projects/index.vue
  - web/src/components/project/ProjectSearchPanel.vue
  - web/src/components/project/CreateProjectModal.vue
  - web/src/pages/projects/__tests__/projects-list.spec.ts
autonomous: true
requirements: [WB-05]
must_haves:
  truths:
    - "项目列表可按 空间 / 状态 / 成员 筛选过滤"
    - "提供全局 + 模糊搜索入口，搜索结果定位上下文属于哪个仓库/项目"
    - "创建入口可手动创建项目并支持绑定看板"
    - "列表/搜索有加载、空、错兜底"
  artifacts:
    - path: web/src/components/project/ProjectSearchPanel.vue
      provides: "全局 + 模糊搜索面板（结果带 repo/project 定位）"
    - path: web/src/pages/projects/index.vue
      provides: "空间/状态/成员 筛选 + 搜索入口 + 创建入口"
  key_links:
    - from: web/src/components/project/ProjectSearchPanel.vue
      to: web/src/api/projectWorkspace.ts
      via: "search(基础) + 结果 locator"
      pattern: "search\\(|SearchResult"
    - from: web/src/pages/projects/index.vue
      to: web/src/api/projects.ts
      via: "list(filters: space_id/status/member/q)"
      pattern: "projectsApi.list"
---

<objective>
落地 WB-05：项目列表按 空间/状态/成员 筛选过滤 + 全局/模糊搜索（接 84-01 基础搜索端点，结果定位"属哪个仓库/项目"；深度项目域 RAG 留 Phase 85，UI 预留位）+ 创建入口（手动创建项目，支持绑定看板）。在既有 `pages/projects/index.vue` 基础上增强（已具空间筛选 + 状态 + 仅我参与 + 防抖搜索 + CreateProjectModal）。

Purpose: 让用户从列表快速定位/进入项目，并能新建或绑定看板，作为工作台入口闭环。

> **Wave 3 依赖说明（W3）**：本 plan 列 `depends_on: [84-01,84-02,84-03,84-04]`。除 Wave 1 的 api/外壳/搜索端点外，搜索结果的"定位→深链跳转到项目工作台对应区块"需要 Wave 2 的 DocsSection/FeatureListSection/DependenciesSection 已落地才能完整验收（跳转落点为这些区块），故刻意排在 Wave 3，非过度串行。
Output: 增强列表筛选 + ProjectSearchPanel + CreateProjectModal 绑定看板 + 守护测试。
</objective>

<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/84-project-workbench-ui/84-CONTEXT.md
@.planning/phases/84-project-workbench-ui/84-UI-SPEC.md
@web/src/pages/projects/index.vue
@web/src/components/project/CreateProjectModal.vue
@web/src/api/projects.ts
@web/src/api/projectWorkspace.ts
@web/src/pages/projects/__tests__/projects-list.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 列表筛选增强（空间/状态/成员）+ ProjectSearchPanel</name>
  <files>web/src/pages/projects/index.vue, web/src/components/project/ProjectSearchPanel.vue</files>
  <read_first>
  - web/src/pages/projects/index.vue（现有 spaceFilter useLocalStorage、status Select、onlyMine、防抖 q、['projects', filters] query）
  - web/src/api/projects.ts（ProjectListFilters: space_id/status/member/q）
  - web/src/api/projectWorkspace.ts（search(id,q) → SearchResult.locator）
  - .planning/phases/84-project-workbench-ui/84-UI-SPEC.md（搜索文案 + 空态"没有匹配的内容"）
  </read_first>
  <action>
  增强 `index.vue`：成员筛选从"仅我参与"扩为可选成员（沿用 auth user 默认 + 可选其他成员，若成员下拉数据成本高则保留"仅我参与" + 备注）；空间/状态/成员/q 组合进 `ProjectListFilters`。新增 `ProjectSearchPanel.vue`：全局 + 模糊搜索输入（防抖），调用项目搜索（本期基础：项目内 `projectWorkspaceApi.search` 聚合 / 或列表 q；UI 预留 RAG 结果位标注 Phase 85），结果项展示 `locator`（"属于 仓库X / 项目Y"）并可点击跳转对应项目工作台对应 section（深链 #hash）。搜索为召回展示，无结果用 EmptyState 文案"没有匹配的内容"。
  </action>
  <verify>
  <automated>pnpm -C web test -- projects-list</automated>
  </verify>
  <done>列表可按 空间/状态/成员 + 关键词筛选；搜索面板返回结果带 repo/project 定位并可跳转；空/错兜底；vue-tsc 绿。</done>
</task>

<task type="auto">
  <name>Task 2: 创建入口 — 手动创建 + 绑定看板</name>
  <files>web/src/components/project/CreateProjectModal.vue, web/src/pages/projects/__tests__/projects-list.spec.ts</files>
  <read_first>
  - web/src/components/project/CreateProjectModal.vue（现创建表单 + projectsApi.create + 成功跳转）
  - web/src/api/projects.ts（create: ProjectCreate 字段，含 feishu_board_* / space）
  - web/src/pages/projects/__tests__/projects-list.spec.ts（真实 zh-CN.json + listMock + spaces mock 范式）
  </read_first>
  <action>
  增强 `CreateProjectModal.vue`：表单支持「手动创建项目」+「绑定看板」（飞书 project key / board 字段，沿用 ProjectCreate 既有字段，幂等 (space, feishu_project_key)）；校验用既有 vee-validate/zod 模式（若已用）。成功后沿用 `router.push('/projects/:id')` 进工作台。更新 `projects-list.spec.ts`：扩展断言成员筛选、搜索入口存在、创建按钮文案"创建项目"（真实 zh-CN.json）、绑定看板字段存在；保持既有断言绿（不破基线）。
  </action>
  <verify>
  <automated>pnpm -C web test -- projects-list</automated>
  </verify>
  <done>创建弹窗支持手动创建与绑定看板；创建成功进工作台；扩展测试 + 既有测试全绿；vue-tsc 绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 搜索输入 → REST | 用户关键词为不可信入站 |
| 创建表单 → REST | 项目创建/绑定看板写 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-13 | Information Disclosure | 搜索结果 | mitigate | 仅返回当前用户可见项目/上下文（public_org + 成员），后端把关；前端不越权展示 |
| T-84-14 | Tampering (XSS) | 搜索结果片段 | mitigate | 文本以 mustache 绑定，不 v-html 未净化片段 |
| T-84-15 | Elevation of Privilege | 创建/绑定看板 | accept | 后端校验空间权限；前端不自判可绕过 |
| T-84-SC | Tampering | 无新增包 | accept | 本 plan 不新增依赖 |
</threat_model>

<verification>
- 空间/状态/成员 三筛 + 关键词组合生效（测试覆盖 filters）
- 搜索结果定位 repo/project 并可跳转工作台 section
- RAG 深化诚实留 Phase 85（UI 预留位，不杜撰）
- 既有 projects-list.spec.ts 绿；vue-tsc 绿
</verification>

<success_criteria>
WB-05 列表筛选 + 全局/模糊搜索（带定位）+ 创建/绑定看板入口 完整；前端基线不破。
</success_criteria>

<output>
Create `.planning/phases/84-project-workbench-ui/84-05-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

- `ProjectSearchPanel.vue`（全局/模糊搜索 + 定位）
- 增强 `pages/projects/index.vue`（空间/状态/成员 筛选 + 搜索 + 创建入口）
- 增强 `CreateProjectModal.vue`（手动创建 + 绑定看板）
- 测试：扩展 `projects-list.spec.ts`
