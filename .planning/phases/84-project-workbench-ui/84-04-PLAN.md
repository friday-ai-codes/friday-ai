---
phase: 84-project-workbench-ui
plan: 04
type: execute
wave: 2
depends_on: [84-01, 84-02]
files_modified:
  - web/src/components/project/workbench/FeatureListSection.vue
  - web/src/components/project/workbench/DependenciesSection.vue
  - web/src/components/project/workbench/__tests__/FeatureListSection.spec.ts
  - web/src/components/project/workbench/__tests__/DependenciesSection.spec.ts
autonomous: true
requirements: [WB-02, WB-04]
must_haves:
  truths:
    - "feature list 以 模块→功能点→验收项 三层树形折叠展示"
    - "每个功能点显示进度灯（待开发/进行中/测试中/已完成），由看板 WorkItem 状态点亮"
    - "外部依赖/关联区块展示工件（原型/Spec/缺陷/UI稿/评审/复盘）+ 关联分支/知识/仓库/项目/PR"
    - "树折叠与依赖列表均有加载/空/错兜底"
  artifacts:
    - path: web/src/components/project/workbench/FeatureListSection.vue
      provides: "feature 树 + 进度灯（WB-02）"
      min_lines: 40
    - path: web/src/components/project/workbench/DependenciesSection.vue
      provides: "外部依赖/关联展示（WB-04）"
  key_links:
    - from: web/src/components/project/workbench/FeatureListSection.vue
      to: web/src/api/projectWorkspace.ts
      via: "getFeatureList → 树 + 状态灯"
      pattern: "getFeatureList"
    - from: web/src/components/project/workbench/DependenciesSection.vue
      to: web/src/api/projects.ts
      via: "artifacts/merge-requests/graph + spaces repositories"
      pattern: "artifactsApi|mergeRequestsApi|graph"
---

<objective>
补全 84-02 占位的 `FeatureListSection.vue`（WB-02）与 `DependenciesSection.vue`（WB-04）。WB-02：模块→功能点→验收项 树形折叠 + 进度灯（待开发/进行中/测试中/已完成，结合看板 WorkItem 状态，数据来自 84-01 `getFeatureList`）。WB-04：复用 `Artifact`（原型/Spec/缺陷/UI稿/评审/复盘）+ 关联分支/知识/仓库/项目/PR（复用既有 artifacts/merge-requests/graph/knowledge + spaces repositories 端点）。

Purpose: 让项目交付进度与外部依赖在工作台一屏可见，呼应"feature list → 交付流水线"。
Output: FeatureListSection（树+进度灯）+ DependenciesSection（工件/关联）+ 守护测试。
</objective>

<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/84-project-workbench-ui/84-CONTEXT.md
@.planning/phases/84-project-workbench-ui/84-UI-SPEC.md
@web/src/components/project/workbench/ArtifactsTab.vue
@web/src/components/project/workbench/LinksTab.vue
@web/src/api/projects.ts
@web/src/api/projectWorkspace.ts
@web/src/components/ui/collapsible
</context>

<tasks>

<task type="auto">
  <name>Task 1: FeatureListSection — 模块/功能点/验收项 树 + 进度灯</name>
  <files>web/src/components/project/workbench/FeatureListSection.vue, web/src/components/project/workbench/__tests__/FeatureListSection.spec.ts</files>
  <read_first>
  - web/src/api/projectWorkspace.ts（getFeatureList → FeatureNode 树，功能点状态字段）
  - web/src/components/ui/collapsible（reka-ui Collapsible 封装，折叠交互 + aria-expanded）
  - .planning/phases/84-project-workbench-ui/84-UI-SPEC.md（进度灯语义色表：待开发 muted / 进行中 teal / 测试中 amber / 已完成 emerald）
  - web/src/pages/projects/__tests__/projects-list.spec.ts（测试范式）
  </read_first>
  <action>
  补全 `FeatureListSection.vue`：调 `projectWorkspaceApi.getFeatureList(id)`（queryKey `['project-features', idRef]`），渲染三层 `Collapsible`（模块 → 功能点 → 验收项），每层可折叠（`aria-expanded`）。功能点行右侧进度灯圆点 + 文案，按状态映射 UI-SPEC 颜色：待开发 `bg-muted text-muted-foreground`、进行中 `bg-primary/15 text-primary`、测试中 `bg-amber-500/15 text-amber-500`、已完成 `bg-emerald-500/15 text-emerald-500`。加载 `LoadingState skeleton`、空 `EmptyState`（文案"还没有 feature"）、错 `useErrorHandler`+重试。新增测试断言树层级渲染、四态灯 class/文案、空态 zh-CN 文案。
  </action>
  <verify>
  <automated>pnpm -C web test -- FeatureListSection</automated>
  </verify>
  <done>三层树可折叠；功能点按 WorkItem 状态点亮四态灯；空/错兜底；测试通过；vue-tsc 绿。</done>
</task>

<task type="auto">
  <name>Task 2: DependenciesSection — 工件 + 关联（分支/知识/仓库/项目/PR）</name>
  <files>web/src/components/project/workbench/DependenciesSection.vue, web/src/components/project/workbench/__tests__/DependenciesSection.spec.ts</files>
  <read_first>
  - web/src/components/project/workbench/ArtifactsTab.vue（工件列表 + view，render_type 分支）
  - web/src/components/project/workbench/LinksTab.vue（graph 关联 + merge-requests 列表）
  - web/src/api/projects.ts（artifactsApi.list/view、mergeRequestsApi.list、projectsApi.graph、knowledge link）
  - web/src/api/spaces.ts（getSpaceRepositories — 项目所属空间的仓库）
  - .planning/phases/84-project-workbench-ui/84-UI-SPEC.md（deps 类别名）
  </read_first>
  <action>
  补全 `DependenciesSection.vue`：分组卡片展示——「外部工件」按类型分组（原型/Spec/缺陷/UI稿/评审/复盘；映射 Artifact 类型 key：ui_design/dev_spec/requirement_doc/ui_review/tracking_review/retrospective，缺陷走 WorkItem 提示——本期以既有 artifacts.list 渲染，无内置「缺陷/原型」类型者在 UI 文案标注其映射来源）+ 在线查看（复用 ArtifactsTab 的 view 弹窗模式）；「关联」分组展示 知识实体（graph）、仓库（经所属空间 repositories）、关联项目（graph/relation 若有则展示，无则空态）、PR（mergeRequestsApi.list，只读列表 + 外链）。各分组独立 query + 加载/空/错兜底。注意：ProjectBranch 多绑定为 Phase 85，UI 预留"分支"位但本期标注"待 Phase 85"，不杜撰端点。新增测试断言分组渲染、PR 列表、空态文案。
  </action>
  <verify>
  <automated>pnpm -C web test -- DependenciesSection</automated>
  </verify>
  <done>外部工件按类别展示并可查看；关联区展示知识/仓库/PR；分支位标注 Phase 85；空/错兜底；测试通过；vue-tsc 绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 工件正文/外链 → DOM | 工件 view 渲染或外链跳转 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-11 | Tampering (XSS) | 工件 markdown view | mitigate | 经 MarkdownRenderer（html:false）；外链 `rel="noopener noreferrer"` |
| T-84-12 | Information Disclosure | 关联数据 | accept | 仅展示后端授权返回项；可见性后端把关 |
| T-84-SC | Tampering | 无新增包 | accept | 本 plan 不新增依赖 |
</threat_model>

<verification>
- feature 树 + 进度灯按 WorkItem 状态正确点亮（测试覆盖四态）
- WB-04 类别齐：原型/Spec/缺陷/UI稿/评审/复盘 + 分支(Phase85占位)/知识/仓库/项目/PR
- 不杜撰不存在端点；缺失能力明确标注 Phase 85
- vue-tsc 绿；基线不破
</verification>

<success_criteria>
WB-02 feature 树+进度灯、WB-04 外部依赖/关联 完整呈现；缺口（ProjectBranch）诚实标注 Phase 85。
</success_criteria>

<output>
Create `.planning/phases/84-project-workbench-ui/84-04-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

- `FeatureListSection.vue`（WB-02 树 + 进度灯）
- `DependenciesSection.vue`（WB-04 工件 + 关联）
- queryKeys：`['project-features', id]`、`['project-deps-*', id]`
- 测试：`FeatureListSection.spec.ts`、`DependenciesSection.spec.ts`
