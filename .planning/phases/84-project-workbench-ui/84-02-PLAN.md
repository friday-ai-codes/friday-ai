---
phase: 84-project-workbench-ui
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/api/projectWorkspace.ts
  - web/src/api/projects.ts
  - web/src/api/index.ts
  - web/src/pages/projects/[id]/index.vue
  - web/src/components/project/workbench/WorkbenchShell.vue
  - web/src/components/project/workbench/OverviewSection.vue
  - web/src/components/project/workbench/DocsSection.vue
  - web/src/components/project/workbench/FeatureListSection.vue
  - web/src/components/project/workbench/DependenciesSection.vue
  - web/src/locales/zh-CN.json
  - web/src/pages/projects/__tests__/workbench-shell.spec.ts
autonomous: true
requirements: [WB-01]
must_haves:
  truths:
    - "项目工作台呈现为左导航栏 + 右主内容区（split），左栏切换右侧当前区块（非子页路由、非锚点滚动 tab）"
    - "大盘区块展示概览 + 人员带身份（PM/开发负责人/开发者/测试）+ 状态栏"
    - "首屏加载显示骨架屏；项目不存在显示空态；加载失败显示错误态 + 重试"
    - "左栏区块切换与 URL #hash 双向同步，可深链书签"
  artifacts:
    - path: web/src/components/project/workbench/WorkbenchShell.vue
      provides: "左导航栏+右主内容区外壳 + activeSection 状态机 + hash 同步"
      min_lines: 40
    - path: web/src/components/project/workbench/OverviewSection.vue
      provides: "大盘概览 + 人员身份 + 状态栏（WB-01）"
    - path: web/src/api/projectWorkspace.ts
      provides: "工作区 REST 客户端封装（docs/human-blocks/feature-list/search/state-apis）+ 类型"
  key_links:
    - from: web/src/pages/projects/[id]/index.vue
      to: web/src/components/project/workbench/WorkbenchShell.vue
      via: "工作台改用左导航外壳渲染各 section"
      pattern: "WorkbenchShell"
    - from: web/src/api/projectWorkspace.ts
      to: web/src/api/client.ts
      via: "get/put 经统一 fetch + cookie-JWT"
      pattern: "from '\\./client'|get<|put<"
---

<objective>
把 `web/src/pages/projects/[id]` 从「reka-ui Tabs 6 标签」升级为「**左导航栏 + 右主内容区**」工作台外壳（借鉴 `spaces/[id]` 视觉语言），并落地大盘区块（WB-01：概览 + 人员带身份 + 状态栏）。同时建立本期前端**数据层基座**：新增 `web/src/api/projectWorkspace.ts` 封装 Phase 84 全部工作区 REST（含 84-01 新增端点）+ 类型，预置 `projects.workbench.*` 全量 i18n keys，并为后续区块创建占位组件，使 Wave 2/3 plan 只新增/补全各自组件文件、零共享文件冲突。

Purpose: 立外壳 + 数据层 + i18n 契约，作为 WB-02/03/04/05 区块的统一承载；WB-01 完整交付。
Output: WorkbenchShell + OverviewSection（完整）+ Docs/FeatureList/Dependencies 占位 section + api 客户端 + i18n + 守护测试。
</objective>

<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/84-project-workbench-ui/84-CONTEXT.md
@.planning/phases/84-project-workbench-ui/84-UI-SPEC.md
@web/src/components/layout/AnchorNavLayout.vue
@web/src/pages/spaces/[id]/index.vue
@web/src/pages/projects/[id]/index.vue
@web/src/components/project/workbench/OverviewTab.vue
@web/src/api/projects.ts
@web/src/api/client.ts
@web/src/pages/projects/__tests__/projects-list.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 工作区 API 客户端 + 类型 + i18n 全量 keys（契约先行）</name>
  <files>web/src/api/projectWorkspace.ts, web/src/api/index.ts, web/src/api/projects.ts, web/src/locales/zh-CN.json</files>
  <read_first>
  - web/src/api/projects.ts（对象字面量 API 模块写法 + 现有类型）
  - web/src/api/spaces.ts（命名函数 + default 映射写法，二选一沿用 projects 风格）
  - web/src/api/index.ts（barrel：default export + export *）
  - web/src/api/client.ts（get/post/patch/put/del 签名 + ApiError）
  - web/src/locales/zh-CN.json（projects.* 命名空间现状、projects.memory.draft.* 结构）
  - .planning/phases/84-project-workbench-ui/84-UI-SPEC.md（Copywriting Contract 文案）
  </read_first>
  <action>
  新增 `web/src/api/projectWorkspace.ts`：封装 84-01 端点 —— `listDocs(id)`、`getDocContent(id, docType)`、`updateHumanBlocks(id, docType, blocks)`、`rebuildWorkspace(id)`、`listStateApis(id)` / `upsertStateApi` / `patchStateApi` / `deleteStateApi`、`getFeatureList(id)`、`listWorkItems(id)`（含 status）、`search(id, q)`，以及 TS 接口（`ProjectDocContent`/`DocBlock`(含 `section`/`editable`)/`FeatureNode`/`SearchResult`）。**契约单一来源（W2）**：TS 接口字段名必须照 84-01 serializer 的 snake_case wire 字段对齐（`block_id`/`db_ref`/`section`/`text`/`editable`/`rendered_markdown`/`sync_status`/`last_synced_revision`/`status_state_key`/`status_display_name`/`module_normalized`/`locator`），不在前端改名；在本文件顶部用注释列一份与 84-01 对齐的字段清单，避免后端/前端漂移（守护测试 mock 无法捕获）。经 `~/api/client` 的 `get/put` 等；遵循 `ProjectListFilters` 既有风格。在 `api/index.ts` 增 `export { default as projectWorkspaceApi } ...` + `export *`。在 `zh-CN.json` 增 `projects.workbench.*`（nav 标签：大盘/文件/feature/外部依赖/搜索；overview/people 角色名 PM/开发负责人/开发者/测试；docs view/edit/save 文案；feature 四态名；deps 类别名；list/search 文案；空/错文案），文案严格对齐 UI-SPEC。
  </action>
  <verify>
  <automated>pnpm -C web run type-check</automated>
  </verify>
  <done>`projectWorkspaceApi` 各函数有完整类型签名并经 barrel 导出；`zh-CN.json` 含 `projects.workbench.nav.*`/`overview.*`/`docs.*`/`feature.*`/`deps.*`/`search.*` 全量键；vue-tsc 0 error。</done>
</task>

<task type="auto">
  <name>Task 2: WorkbenchShell 外壳 + 区块占位 + [id] 工作台改造</name>
  <files>web/src/components/project/workbench/WorkbenchShell.vue, web/src/components/project/workbench/DocsSection.vue, web/src/components/project/workbench/FeatureListSection.vue, web/src/components/project/workbench/DependenciesSection.vue, web/src/pages/projects/[id]/index.vue</files>
  <read_first>
  - web/src/components/layout/AnchorNavLayout.vue（左轨视觉：w-48 shrink-0 sticky top-22、active 指示条、badge）
  - web/src/pages/projects/[id]/index.vue（现 6 Tab 结构、project 加载、usePermission、状态流转、飞书外链）
  - web/src/components/common/LoadingState.vue / EmptyState.vue（变体与 props）
  - .planning/phases/84-project-workbench-ui/84-UI-SPEC.md（Layout Contract）
  </read_first>
  <action>
  新增 `WorkbenchShell.vue`：`flex gap-8`，左 `aside.w-48.shrink-0.sticky.top-22.hidden.md:block`（窄屏折叠为顶部 `Select`/横向 chips），左项为 `<button>` 切 `activeSection`（active `bg-primary/8 text-primary font-medium` + `w-0.5 bg-primary` 指示条，非 active `text-muted-foreground hover:bg-muted/40`，项内距 `pl-4 pr-2.5 py-2`，lucide 图标 + 可选 badge）；右 `flex-1 min-w-0 space-y-6` slot 渲染当前 section；`activeSection` 与 `route.hash` 双向同步（onMounted 读 hash、切换 `router.replace({hash})`）；**不使用子路由、不使用整页锚点滚动**。section 列表：大盘/文件/feature/外部依赖（搜索为列表页职责，工作台内 4 区块）。新增 3 个占位 section（`DocsSection`/`FeatureListSection`/`DependenciesSection`）：仅渲染 `.card` 卡头 + `LoadingState`/“建设中”占位（Wave 2 plan 补全）。改造 `[id]/index.vue`：保留顶部面包屑/项目名/状态 badge/飞书外链/状态流转，把 Tabs 替换为 `<WorkbenchShell>`，按 activeSection 用 `defineAsyncComponent` 懒加载 4 个 section；保留首屏 `LoadingState variant="skeleton"`、项目不存在 `EmptyState`、错误 `useErrorHandler`。
  </action>
  <verify>
  <automated>pnpm -C web run type-check && pnpm -C web test -- workbench-shell</automated>
  </verify>
  <done>工作台呈左导航+右主区；点击左项切换右侧 section 并更新 #hash；刷新带 #hash 直达对应区块；加载/空/错三态可见；vue-tsc 绿。</done>
</task>

<task type="auto">
  <name>Task 3: OverviewSection 大盘（概览 + 人员身份 + 状态栏）+ 守护测试</name>
  <files>web/src/components/project/workbench/OverviewSection.vue, web/src/pages/projects/__tests__/workbench-shell.spec.ts</files>
  <read_first>
  - web/src/components/project/workbench/OverviewTab.vue（现概览实现、cursor-rules 调用）
  - web/src/components/project/workbench/MembersTab.vue（成员列表 + 角色显示、queryKey ['project-members', idRef]）
  - web/src/api/projects.ts（listMembers、get、ProjectStatus）
  - web/src/pages/projects/__tests__/projects-list.spec.ts（真实 zh-CN.json + VueQueryPlugin + mount stubs 范式）
  </read_first>
  <action>
  新增 `OverviewSection.vue`：复用 `OverviewTab` 概览内容 + 新增「人员（带身份）」块——调 `projectsApi.listMembers`，按 UI-SPEC 角色映射展示身份徽章（PM=pm、开发负责人=owner、开发者=frontend+backend、测试=qa；owner 加 crown 图标），用 `Badge`；「状态栏」展示项目 status（`StatusBadge`）+ 工作区 docs `sync_status` 概要（调 `projectWorkspaceApi.listDocs`，"重建工作区"按钮触发 `rebuildWorkspace` 并用 `useMutation`+`refetchInterval` 轮询 sync_status，仿 `ReconcilePanel` 范式）。queryKey 用 `['project-docs', idRef]`。新增守护测试 `workbench-shell.spec.ts`：导入真实 `zh-CN.json`，mock `~/api/projects` + `~/api/projectWorkspace`，断言左导航 4 项文案、切换 section、大盘人员身份徽章文案、空/错态文案。
  </action>
  <verify>
  <automated>pnpm -C web test -- workbench-shell</automated>
  </verify>
  <done>大盘显示概览/人员身份/状态栏；重建工作区触发后状态进入轮询；测试断言 zh-CN 关键文案 + 身份映射；全部通过。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 服务端 markdown → v-html | 大盘若渲染 markdown 经 MarkdownRenderer（html:false）|
| 用户输入 → REST 写 | 重建工作区等写操作经 cookie-JWT |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-05 | Information Disclosure | 前端展示 | mitigate | 人员/状态仅展示后端已授权返回字段；不在前端缓存敏感凭证 |
| T-84-06 | Tampering (XSS) | 任何 markdown 展示 | mitigate | 复用 `getMarkdownRenderer`（html:false）；v-html 仅用于其输出，遵循既有 eslint 例外 |
| T-84-07 | Spoofing | 写操作 | accept | cookie-JWT + 后端成员校验（见 84-01 T-84-01），前端不自判权限可绕过 |
| T-84-SC | Tampering | 无新增前端包（本 plan） | accept | 本 plan 不新增 npm 包；`@codemirror/lang-markdown` 在 84-03 安装 |
</threat_model>

<verification>
- 工作台为左导航+右主区（截图/测试断言 DOM 结构）
- vue-tsc 绿（`pnpm -C web run type-check`）
- 既有 `projects-list.spec.ts` 仍绿（不破基线）
- 文案全量 zh-CN 且进 i18n（无新硬编码英文）
</verification>

<success_criteria>
WB-01 大盘完整可用；工作台外壳/数据层/i18n 基座就位，供 Wave 2/3 复用；前端基线不破。
</success_criteria>

<output>
Create `.planning/phases/84-project-workbench-ui/84-02-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

- `web/src/api/projectWorkspace.ts`（`projectWorkspaceApi` + 类型 `ProjectDocContent`/`DocBlock`/`FeatureNode`/`SearchResult`/`StateApi`）
- `WorkbenchShell.vue`（左导航+右主区外壳 + activeSection/hash 同步）
- `OverviewSection.vue`（WB-01 大盘）
- 占位组件：`DocsSection.vue`/`FeatureListSection.vue`/`DependenciesSection.vue`（Wave 2 补全）
- i18n keys：`projects.workbench.*`（zh-CN.json）
- queryKeys：`['project-docs', id]`
- 测试：`web/src/pages/projects/__tests__/workbench-shell.spec.ts`
