---
phase: 132-integration-gaosan-regression
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/services/process_runtime/gaosan_eval.py
  - server/tests/services/process_runtime/test_gaosan_eval.py
  - server/tests/services/process_runtime/fixtures/gaosan_learning_tools.py
autonomous: true
requirements:
  - INT-02
user_setup: []

must_haves:
  truths:
    - "D2 bar 以 placement-unit primary 集合判定四基线各至少命中一次（含 alias 归一）"
    - "out_of_team_primary_count==0 时 passed；任一 out_of_team primary 则失败"
    - "合成 Learning-tools fixture 提供 team_core/out_of_team/modules 且不含活 Space 依赖"
  artifacts:
    - path: server/services/process_runtime/gaosan_eval.py
      provides: "D2 门槛纯函数 + 基线常量 + alias 归一"
      exports: ["BASELINE_REPOS", "normalize_repo_key", "score_placement_bar"]
    - path: server/tests/services/process_runtime/test_gaosan_eval.py
      provides: "INT-02 bar 单测"
    - path: server/tests/services/process_runtime/fixtures/gaosan_learning_tools.py
      provides: "合成 Learning-tools 宇宙 fixture"
  key_links:
    - from: score_placement_bar
      to: placements[].primary_repo + membership
      via: "normalize → hit set → missing_baselines / out_of_team count"
      pattern: "score_placement_bar|BASELINE_REPOS|out_of_team_primary"
---

<objective>
落地 INT-02 的可测 D2 验收标尺：placement-unit 级 `score_placement_bar` + alias 归一 + 合成 Learning-tools fixture（per D-01、D-02、D-03、D-06、D-08、D-09）。

Purpose: 后续漏斗回归与可选 live 评测共用同一 bar，避免 feature-point top1 或裸 V2 结果当标准。
Output: `gaosan_eval.py`、fixture 模块、单元测试。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/132-integration-gaosan-regression/132-CONTEXT.md
@.planning/phases/132-integration-gaosan-regression/132-RESEARCH.md
@.planning/milestones/v0.23.0-DECISIONS.md
@.planning/REQUIREMENTS.md
@.planning/quick/260809-repo-route-eval/SUMMARY.md
@server/services/process_runtime/place_units.py
@server/services/process_runtime/role_map.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — D2 bar 与 alias 归一单测</name>
  <files>server/tests/services/process_runtime/test_gaosan_eval.py</files>
  <behavior>
    - normalize：`frontend/onion-practice` 与 `onion-practice` 等价；`backend/study-course` 与 `study-course` 等价；`backend/study-user-status` / `frontend/onion-learning` 自洽
    - 四基线各至少一个 placement primary → `passed=True`，`missing_baselines=[]`
    - 缺任一基线 primary → `passed=False`，`missing_baselines` 含该基线规范名
    - 任一 primary 的 membership=`out_of_team` → `out_of_team_primary_count>=1` 且 `passed=False`（即使四基线齐全）
    - 空 placements → 失败且四基线均 missing
    - 评测粒度注释/常量声明为 placement-unit（非 43 点 top1）—— per D-01
  </behavior>
  <action>
    创建 `test_gaosan_eval.py`，先对尚未存在的 `services.process_runtime.gaosan_eval` API 写失败断言。导入期望：`BASELINE_REPOS`（四规范名）、`normalize_repo_key`、`score_placement_bar`（或等价名）。不要实现生产模块。不要依赖活 Space / Qdrant。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/process_runtime/test_gaosan_eval.py -q --tb=line 2>&1 | head -40; test ${PIPESTATUS[0]} -ne 0</automated>
  </verify>
  <done>RED：导入或断言失败；行为规格已写入测试。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — gaosan_eval + Learning-tools 合成 fixture</name>
  <files>server/services/process_runtime/gaosan_eval.py, server/tests/services/process_runtime/fixtures/gaosan_learning_tools.py, server/tests/services/process_runtime/test_gaosan_eval.py</files>
  <action>
    实现 `gaosan_eval.py`（per D-09）：常量列出四基线规范名与 alias 表；`normalize_repo_key` 去空白、统一小写路径分隔、剥离已知 `frontend/`/`backend/` 前缀做等价键（保留规范名用于报告）；`score_placement_bar(placements, membership)` 返回结构化结果含 `passed`、`missing_baselines`、`baseline_primary_hits`、`out_of_team_primary_count`、`normalized_primaries`。文档字符串写明 D2 门槛 = 每基线≥1 unit primary 且 out_of_team primary=0（per D-08）。观测：若打日志仅 sampling counts，禁止需求全文。

    新增 `fixtures/gaosan_learning_tools.py`（per D-06）：导出合成 `team_core`（含四基线 id/name）、`out_of_team` 诱饵（如 study-app / study-practice / vocation-problem-app）、`membership` map、压缩 `modules`/`features_flat`（参考 `.planning/feature-list-demo.md` 九模块语义，可 4–9 unit 规模）、建议的 role 期望映射（shell/practice/course/state ↔ 四基线）。不读活 DB。可被 plan 02 复用。

    使 Task 1 测试全绿。不修改 `repo_router_v2.py`。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/process_runtime/test_gaosan_eval.py -q --tb=short --reuse-db</automated>
  </verify>
  <done>bar 纯函数与合成 fixture 可用；D-01~D-03/D-08/D-09 可单测证明。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture → bar | 测试合成数据进入评分；无用户输入面 |
| bar → CI | 仅布尔/计数输出；无 PII |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-132-01 | Information Disclosure | gaosan_eval logging | mitigate | 禁止需求/feature 全文入日志；只记 counts |
| T-132-02 | Tampering | baseline constants | accept | 测试常量漂移即红；无远程配置 |
| T-132-SC | Tampering | npm/pip installs | accept | 本 plan 无新包 |
</threat_model>

<verification>
`test_gaosan_eval.py` 全绿；`gaosan_eval.py` 导出 bar API；fixture 无网络/DB。
</verification>

<success_criteria>
- D2 门槛可纯函数判定（四基线 + out_of_team=0）
- alias 归一单测锁定
- 合成 Learning-tools fixture 可供漏斗回归复用
</success_criteria>

<output>
Create `.planning/phases/132-integration-gaosan-regression/132-01-SUMMARY.md` when done
</output>
