---
phase: 132-integration-gaosan-regression
plan: 03
type: execute
wave: 2
depends_on:
  - 132-01
files_modified:
  - server/tests/services/process_runtime/test_int03_contracts.py
  - server/tests/services/process_runtime/test_funnel_gates_wiring.py
  - server/tests/services/process_runtime/test_reflection.py
  - server/services/process_runtime/blueprint_route.py
  - server/tests/mcp_tools/test_mcp_read_flow.py
autonomous: true
requirements:
  - INT-03
user_setup: []

must_haves:
  truths:
    - "128–131 漏斗/门禁/反思相关自动化测试在契约包下一键可跑且通过"
    - "至少一条接线级角色坍塌→反思修复用例：修复后不再含 role_collapse"
    - "本相位不修改 repo_router_v2.py 业务内核"
  artifacts:
    - path: server/tests/services/process_runtime/test_int03_contracts.py
      provides: "INT-03 契约聚合入口 + 接线级 collapse 用例或导入守卫"
  key_links:
    - from: BlueprintRouteAdapter / reflection
      to: role_collapse repair
      via: "trigger → run_reflection_loop → placements 修复 → re-detect"
      pattern: "role_collapse|run_reflection_loop|needs_human_review"
---

<objective>
交付 INT-03：既有漏斗/MCP 契约不回归包 + 至少一条 **接线级** 角色坍塌→反思修复合成用例（per D-11、D-12、D-13）；巩固 131 纯函数钩子。

Purpose: 里程碑收口时契约可重复验证，反思修复不止停留在 unit hook。
Output: `test_int03_contracts.py`（及必要的 wiring/reflection 扩测）；可选 mcp 子集纳入命令。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/132-integration-gaosan-regression/132-CONTEXT.md
@.planning/phases/132-integration-gaosan-regression/132-RESEARCH.md
@.planning/phases/131-gate-system-reflection/131-VERIFICATION.md
@server/tests/services/process_runtime/test_reflection.py
@server/tests/services/process_runtime/test_funnel_gates_wiring.py
@server/tests/mcp_tools/test_mcp_read_flow.py
@server/services/process_runtime/reflection.py
@server/services/process_runtime/blueprint_route.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — INT-03 契约包入口 + 接线级 collapse 用例</name>
  <files>server/tests/services/process_runtime/test_int03_contracts.py, server/tests/services/process_runtime/test_funnel_gates_wiring.py</files>
  <behavior>
    - `test_int03_contracts.py` 文档化契约包文件列表（128–131 funnel/gates/reflection/placement/team/shortlist/role 等），并提供可执行守卫：例如 subprocess/pytest 收集或显式 import-smoke + 关键断言「列表非空且路径存在」
    - 新增接线级用例（可放本文件或扩 `test_funnel_gates_wiring.py`）：`BlueprintRouteAdapter`（或等价已接线入口）在合成 role_collapse 场景下调用 reflection，最终 summary/placements 不再触发 `role_collapse`，且未对全库开放 primary；保留 131 纯函数钩子不删
    - 断言观测脱敏：载荷不含植入的需求全文标记串（沿用 131「高三提分专项请勿…」模式）
  </behavior>
  <action>
    写 RED 测试。接线级用例应 mock/stub 外部 LLM/V2，聚焦 Adapter→gates→reflection 控制流（参考 `test_funnel_gates_wiring.py` 既有 `test_reflection_called_for_repairable_trigger`）。若现有 wiring 仅断言「调用了 reflection」而未断言「修复后无 role_collapse」，本任务补上后半段断言使其先红。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/process_runtime/test_int03_contracts.py tests/services/process_runtime/test_funnel_gates_wiring.py -q --tb=line 2>&1 | head -60; test ${PIPESTATUS[0]} -ne 0</automated>
  </verify>
  <done>RED：INT-03 接线级修复断言或契约包入口失败规格就位。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — 接线修复路径 + 契约包命令跑绿</name>
  <files>server/tests/services/process_runtime/test_int03_contracts.py, server/tests/services/process_runtime/test_funnel_gates_wiring.py, server/tests/services/process_runtime/test_reflection.py, server/services/process_runtime/blueprint_route.py, server/tests/mcp_tools/test_mcp_read_flow.py</files>
  <action>
    实现/调整使 Task 1 变绿（per D-12）：优先在测试侧注入可修复的 collapse + repair_hook；若 Adapter 未把 reflection 结果写回 placements，对 `blueprint_route.py` 做**最小接线修补**并在 SUMMARY 注明。保持 `test_role_collapse_repair_path_for_int03_hook` 仍通过。

    契约包（per D-11）：在 `test_int03_contracts.py` 模块 docstring 或常量 `INT03_CONTRACT_PATHS` 列出一键命令，至少包含：
    `test_funnel_team_gate.py`, `test_funnel_shortlist.py`, `test_funnel_placement.py`, `test_funnel_gates.py`, `test_funnel_gates_wiring.py`, `test_reflection.py`, `test_gaosan_eval.py`, `test_gaosan_funnel_regression.py`（若 02 已存在）, 以及 `tests/mcp_tools/test_mcp_read_flow.py`（或证明等价选仓相关 MCP 子集）。执行该聚合 pytest 全绿。

    V2 freeze（per D-13）：确认本相位改动 diff 不含 `server/codegraph/services/repo_router_v2.py`。不借机大改 MCP 行为——仅确保既有测不红；若 mcp 测无关且 flaky，保留在包外并在 SUMMARY 说明，但至少跑通 process_runtime 契约列表。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/process_runtime/test_int03_contracts.py tests/services/process_runtime/test_funnel_gates.py tests/services/process_runtime/test_reflection.py tests/services/process_runtime/test_funnel_gates_wiring.py tests/services/process_runtime/test_funnel_placement.py tests/services/process_runtime/test_funnel_shortlist.py tests/services/process_runtime/test_funnel_team_gate.py tests/services/process_runtime/test_gaosan_eval.py -q --tb=line --reuse-db</automated>
  </verify>
  <done>INT-03：契约包可一键绿；接线级 role_collapse→reflection 修复成立；V2 未改。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Adapter → reflection | 路由结果进入反思补丁；须脱敏 |
| MCP read flow | 既有契约；无新攻击面 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-132-06 | Information Disclosure | reflection/wiring logs | mitigate | 断言无需求全文；redact_for_ledger 既有路径 |
| T-132-07 | Denial of Service | unbounded reflection | mitigate | 沿用 N=2；超限 needs_human_review（既有单测保留） |
| T-132-08 | Elevation of Privilege | collapse repair → full-lib primary | mitigate | 接线断言 hard_scope / 非全库 |
| T-132-SC | Tampering | package installs | accept | 无新包 |
</threat_model>

<verification>
INT-03 聚合命令绿；接线级 collapse 修复断言绿；`repo_router_v2.py` 无业务 diff。
</verification>

<success_criteria>
- 契约不回归可自动化证明
- ≥1 接线级 role_collapse→reflection 修复用例
- V2 freeze 保持
</success_criteria>

<output>
Create `.planning/phases/132-integration-gaosan-regression/132-03-SUMMARY.md` when done
</output>
