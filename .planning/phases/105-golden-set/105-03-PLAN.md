---
phase: 105-golden-set
plan: 03
type: execute
wave: 2
depends_on: [105-01]
files_modified:
  - server/codegraph/services/repo_router_v2.py
  - server/tests/codegraph/test_repo_router_v2_degraded.py
  - server/tests/services/test_engine_clarify.py
autonomous: true
requirements: [RELY-04, ROUTE-07, ROUTE-09]
must_haves:
  truths:
    - "Stage 1 三种失联情形（网关 400 / 连接错误 / 超时）下 route() 仍产出按分数 margin 确定性推导的 high/medium/low 分级，且 degraded=True"
    - "auto_selected 由确定性 confidence 驱动（首位最终 confidence == high → True），Stage 1 可用与不可用两条路径语义一致"
    - "LLM 的 confidence 输出只能把确定性分级降级，任何情况下不能把 low/medium 升为 high"
    - "repo_router_v2.py 中不再存在任何 min(score, 1.0) 截断与 DEPRECATED_PENALTY 乘性惩罚"
    - "每个候选 to_dict() 携带 breakdown 字典且 Σ贡献 == score"
    - "Stage 1 失联 + margin 达标时，clarify 默认 policy 判定无需澄清（编排自动推进）"
  artifacts:
    - path: server/codegraph/services/repo_router_v2.py
      provides: "接线纯函数打分核心的两阶段路由器（去截断、确定性 confidence、degraded、snapshot 材料）"
      contains: "from codegraph.services.repo_router_scoring import"
    - path: server/tests/codegraph/test_repo_router_v2_degraded.py
      provides: "三种失联情形 + 只降不升 + degraded 标志 的行为守护"
      min_lines: 120
  key_links:
    - from: server/codegraph/services/repo_router_v2.py
      to: server/codegraph/services/repo_router_scoring.py
      via: "_stage0_candidates 调 aggregate_and_score；confidence 调 derive_confidence/apply_llm_adjustment（推导只此一处，不散落调用方）"
      pattern: "aggregate_and_score|derive_confidence"
    - from: server/services/process_runtime/clarify_adapter.py
      to: server/codegraph/services/repo_router_v2.py
      via: "default_needs_clarification 读 session.routing.candidates[].confidence——语义修复后自动解锁，policy 代码零改动"
      pattern: "_CONFIDENT"
---

<objective>
把 `RepoRouterV2` 接线到 105-01 的纯函数打分核心，完成 CONTEXT 锁定的三组改造：(1) 删两处 `min(score,1.0)` 截断与 `DEPRECATED_PENALTY` 乘性惩罚，候选携带 breakdown（ROUTE-07）；(2) confidence 改确定性 margin 推导、LLM 只降不升、`auto_selected` 由确定性 confidence 驱动、结果加 `degraded` 标志（RELY-04——直接解开「Stage 1 失联 → 恒 low → auto_selected 恒 false → 编排卡死」的生产事故链，会话 ccd817d9）；(3) 稳定排序贯穿 + Stage 0 快照材料随结果携带（ROUTE-09 数据底座）。

下游 `clarify_adapter` / `feature_confirm_questions` / `research_adapter` 读 confidence 字段，语义修复后自动解锁，policy 代码零改动——但必须补行为级回归测试（RESEARCH Pitfall 1）。

Purpose: 编排不再因 Stage 1 失联永久停摆；分数可拆解落到候选结构。
Output: 重构后的 `repo_router_v2.py` + 失联行为测试 + clarify policy 集成测试。
</objective>

<context>
@.planning/phases/105-golden-set/105-CONTEXT.md
@.planning/phases/105-golden-set/105-RESEARCH.md
@server/codegraph/services/repo_router_v2.py
@server/codegraph/services/repo_router_scoring.py
@server/services/process_runtime/clarify_adapter.py
</context>

## Artifacts this phase produces（本 plan 新建/变更符号）

| Symbol | Kind | Location |
|--------|------|----------|
| `RepoRouteCandidateV2.breakdown: dict[str, float]`（带默认值，进 `to_dict()`） | dataclass 字段 | `server/codegraph/services/repo_router_v2.py` |
| `RepoRouteResultV2.degraded: bool = False` | dataclass 字段 | 同上 |
| `RepoRouteResultV2.snapshot: dict[str, Any]`（默认空 dict，Stage 0 快照材料 + versions） | dataclass 字段 | 同上 |
| `_conf_thresholds() -> tuple[float, float, float]`（settings 读取 helper） | 私有函数 | 同上 |
| 事件 `repo_router_v2_scored`（debug，category=sampling，component=repo_router_v2） | 日志事件 | 同上 |

<tasks>

<task type="auto">
  <name>Task 1: Stage 0 接线打分核心 + 数据结构扩展（去截断/breakdown/degraded/snapshot 材料）</name>
  <files>server/codegraph/services/repo_router_v2.py</files>
  <read_first>
    - server/codegraph/services/repo_router_v2.py（全文；必改点：`_aggregate_by_repo` 桶内排序、`_stage0_candidates` 病理公式与乘性惩罚、`_finalize_stage0` 截断+恒 low、`route()` auto_selected、dataclass 定义）
    - server/codegraph/services/repo_router_scoring.py（105-01 契约：ScoredCandidate 字段、aggregate_and_score 输入输出、常量名）
    - .planning/phases/105-golden-set/105-RESEARCH.md §2「必改代码点清单」与 §4 消费方矩阵（8 个调用方均按具名字段读取，加字段 additive-safe；**新增 dataclass 字段必须带默认值**，否则 3 个测试文件的 stub 构造炸掉——Pitfall 2）
  </read_first>
  <action>
    改造 `server/codegraph/services/repo_router_v2.py`（CONTEXT §分数可拆解与去截断 / §确定性与快照回放）：

    1. **dataclass 扩展（全部带默认值）**：`RepoRouteCandidateV2` 加 `breakdown: dict[str, float] = field(default_factory=dict)`，`to_dict()` 输出 breakdown（值 round 6）；`RepoRouteResultV2` 加 `degraded: bool = False` 与 `snapshot: dict[str, Any] = field(default_factory=dict)`。
    2. **Stage 0 打分换核**：`_stage0_candidates` 改为薄封装——调 `aggregate_and_score(node_hits)`（聚合分桶已在纯函数内完成；`_aggregate_by_repo` 保留仅供 Stage 1 组 prompt 取桶时使用，其桶内排序改为 `(-round(score,6), str(node_id))`），把 `ScoredCandidate` 转为既有 dict 形状（repo_id/repo_name/score/facets/hits）**加 "breakdown" 键**，截取 STAGE0_REPO_K。删除病理公式 `max_score * (1 + 0.1*min(hits-1,5))`、`score *= DEPRECATED_PENALTY` 与常量 `DEPRECATED_PENALTY`（模块 docstring 的分面降权描述同步更新为「活跃度封顶进加性活跃度项」）。
    3. **确定性 confidence**：新增 `_conf_thresholds()`（照 `_stage1_conf` 模式从 settings 读 `REPO_ROUTER_CONF_THETA_ABS/_MARGIN/_MED`，调用时读取）。`_finalize_stage0`：删 `min(c["score"], 1.0)`，score 直接用归一化分；confidence 赋值规则——**rank-1 候选**用 `derive_confidence([所有候选 score 降序], ...)`；**rank>1 候选**用 `"medium" if score >= theta_med else "low"`（high 仅 rank-1 可得，margin 语义只对首位有定义；此规则写进函数 docstring）。candidate.breakdown 从 stage0 dict 透传。
    4. **route() 语义统一**：
       - `use_llm=False` 与 Stage 1 失败/返回 None 路径：`RepoRouteResultV2(candidates=_finalize_stage0(...), router_version="v2_stage0_only", degraded=True, auto_selected=(首位 confidence == "high"))`——**降级路径也能 auto_selected**（这就是解锁点）。
       - v2 路径（Stage 1 成功）：LLM 候选的 confidence 不再直接采信——先按该候选在 stage0 排序中的位置算确定性 confidence（同第 3 步规则），再 `apply_llm_adjustment(deterministic, llm_confidence)`（只降不升）；score 用 stage0 归一化分（删 `min(float(base["score"]), 1.0)`）；breakdown 透传；`auto_selected = (llm_candidates[0] 最终 confidence == "high")`；`degraded=False`。
       - `_fallback_v1`：保持 confidence="low"、auto_selected=False，但 `degraded=True`（Stage 1 未参与）。
    5. **Stage 0 快照材料**：route() 组装 `result.snapshot`（v2/v2_stage0_only 路径）：`{"stage0": {"query": query, "node_hits": [{"node_id","repository_id","score","node_path","activity_facet"} 最小字段集]}, "candidates": [候选 to_dict() 精简], "versions": {"weight_set_version": WEIGHT_SET_VERSION, "index_version": sha256(参与候选各仓 built_at 按 repo_id 排序拼接)}}`。node_hits 只存最小字段集（RESEARCH Anti-pattern：禁存全量 payload）。stage1 材料由 105-05 补充；payload 落 `ConvergenceSessionEvent` 由 105-07 处理，本 plan 只保证材料随结果携带。
    6. **观测**：Stage 0 打分完成后记 `logger.debug("repo_router_v2_scored", candidate_count=…, top_score=…, confidence=…, degraded=…, duration_ms=…, category="sampling", component="repo_router_v2")`（debug 级——route 属高频内部步骤，禁 INFO 刷屏）。既有 stage1_completed/failed/skipped 事件保持。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/test_repo_router_adapter.py tests/knowledge/test_artifact_repo_routing.py tests/initiatives/test_repo_association_service.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `rg -c "min\(.*score.*, 1\.0\)" server/codegraph/services/repo_router_v2.py` 返回 0
    - `rg -c "DEPRECATED_PENALTY" server/codegraph/services/repo_router_v2.py` 返回 0
    - `_finalize_stage0` 内无 `confidence="low"` 硬编码（v1_fallback 路径保留）
    - 既有 3 个 stub 构造测试文件（repo_router_adapter / artifact_repo_routing / repo_association_service）不改动即通过（新字段全部带默认值）
    - 构造含 breakdown 的 `RepoRouteCandidateV2` 后 `to_dict()` 含 "breakdown" 键
  </acceptance_criteria>
  <done>路由器全路径产出确定性分级 + breakdown + degraded；消费方零破坏。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 三种失联情形行为测试（test_repo_router_v2_degraded.py）</name>
  <files>server/tests/codegraph/test_repo_router_v2_degraded.py</files>
  <read_first>
    - server/codegraph/services/repo_router_v2.py（Task 1 产物：降级链、_stage1_llm_reasoning 内 lazy import `agents.llm_factory.build_chat_model`）
    - server/tests/conftest.py（`fake_chat_model_factory` seam 与 `mock_aresolve_ok` 的用法——RESEARCH Pattern 2：patch 目标为 `agents.llm_factory.build_chat_model`，因函数内 lazy import）
    - .planning/phases/105-golden-set/105-RESEARCH.md §2「Stage 1 失联的三种情形与现有代码路径」表
  </read_first>
  <behavior>
    - 网关 400：fake model `ainvoke` raise provider SDK 风格异常 → route() 返回 v2_stage0_only、degraded=True、首位 confidence 按 margin 规则（构造高 margin 输入 → "high"）、auto_selected=True
    - 连接错误：fake model raise ConnectionError → 同上
    - 超时：fake model raise TimeoutError（或 ainvoke 内 sleep 超 monkeypatch 后的小超时值） → 同上
    - 静默 None 路径：provider_missing（mock aresolve 返回 ProviderMissingError）与 unparsable_llm_output（fake model 返回非 JSON 文本）→ 同样产出确定性分级 + degraded=True
    - 低分/小 margin 输入 → medium/low 分级、auto_selected=False（不误推进）
    - 只降不升：Stage 1 成功且 LLM 对首位输出 "medium"（确定性为 high）→ 最终 medium、auto_selected=False；LLM 输出 "high"（确定性为 low）→ 最终 low
    - degraded=False 仅在 v2 路径
  </behavior>
  <action>
    写 `server/tests/codegraph/test_repo_router_v2_degraded.py`。Stage 0 输入不打真 Qdrant：monkeypatch `RepoRouterV2._stage0_node_search` 返回构造 node_hits（含高 margin 与低 margin 两组 fixture）。Stage 1 失败注入按 read_first 的 seam。每条用例断言四元组（router_version / degraded / 首位 confidence / auto_selected）。覆盖 CONTEXT「Stage 1 完全失联（网关 400 / 连接错误 / 超时三种情形）时系统仍产出 high/medium/low 分级并自动推进」。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/codegraph/test_repo_router_v2_degraded.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - 三种失联情形各有独立用例且断言 `degraded is True` 与 `auto_selected is True`（高 margin 输入）
    - 只降不升两方向（high→medium 降级生效、low→high 升级被拒）均有断言
    - `uv run pytest tests/codegraph/test_repo_router_v2_degraded.py -q` 全绿，用例数 >= 7
  </acceptance_criteria>
  <done>RELY-04 的核心行为（失联仍分级 + 自动推进 + 只降不升）被自动化守护。</done>
</task>

<task type="auto">
  <name>Task 3: clarify policy 行为级集成测试（编排自动推进回归）</name>
  <files>server/tests/services/test_engine_clarify.py</files>
  <read_first>
    - server/services/process_runtime/clarify_adapter.py（`default_needs_clarification`：routing 候选无任一 confidence ∈ {high, medium} → 需澄清；`_CONFIDENT` 集合）
    - server/tests/services/test_engine_clarify.py（既有测试风格与 session fixture 构造方式）
    - server/services/process_runtime/repo_router_adapter.py（session.routing 的 dict 形状：candidates[].confidence）
  </read_first>
  <action>
    在 `server/tests/services/test_engine_clarify.py` 补两条用例（RESEARCH Pitfall 1——只测 router 单元不足以证明编排推进）：
    1. session.routing 构造为「Stage 1 失联降级但 margin 达标」的产物形状（candidates 首位 confidence="high"，router_version="v2_stage0_only"）→ `default_needs_clarification(session)` 返回 needs=False（不触发澄清，编排推进）。
    2. session.routing 全 low（margin 不达标降级产物）→ needs=True（仍要澄清，语义未被放松）。
    用例命名含 `degraded_routing`，注释引用 RELY-04 与生产事故链（confidence 恒 low → 无差别澄清）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/test_engine_clarify.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - 两条新用例存在且全绿；断言的是 policy 返回值（行为级），不是 router 内部状态
    - 既有用例零改动通过
  </acceptance_criteria>
  <done>「失联 + margin 达标 → 编排无需澄清自动推进」有贯穿 policy 层的回归守护。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM 响应 → 候选构造 | Stage 1 输出为不可信外部内容 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-105-06 | Tampering | _stage1_llm_reasoning | mitigate | 既有 repo_id 白名单校验保留（`by_id.get(rid) is None → 丢弃`）；LLM confidence 经 apply_llm_adjustment 只降不升——LLM 无法把任何候选推成 auto_selected |
| T-105-07 | Elevation of Privilege | auto_selected | mitigate | auto_selected 只由确定性 margin 规则驱动；测试锁定 low→high 升级被拒 |
| T-105-SC | Tampering | 包安装 | accept | 零新增依赖 |
</threat_model>

<verification>
- `cd server && uv run pytest tests/codegraph/ tests/services/test_engine_clarify.py tests/services/test_repo_router_adapter.py -q` 全绿
- `rg -n "min\(.*, 1\.0\)" server/codegraph/services/repo_router_v2.py` 无输出
</verification>

<success_criteria>
- Phase success criterion 1 成立：三种失联情形仍出 high/medium/low 并自动推进；LLM 只降不升
- Phase success criterion 2 后端部分成立：候选携带 breakdown、Σ==总分、截断消失
- 8 个消费方零破坏（stub 构造测试不改动通过）
</success_criteria>

<output>
完成后创建 `.planning/phases/105-golden-set/105-03-SUMMARY.md`。
</output>
