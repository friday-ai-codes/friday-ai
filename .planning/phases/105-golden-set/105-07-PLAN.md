---
phase: 105-golden-set
plan: 07
type: execute
wave: 4
depends_on: [105-05]
files_modified:
  - server/services/process_runtime/builtin_processes.py
  - server/services/process_runtime/repo_router_adapter.py
  - server/codegraph/services/repo_router_replay.py
  - server/tests/codegraph/test_repo_router_replay.py
  - server/tests/services/test_repo_router_adapter.py
autonomous: true
requirements: [ROUTE-09]
must_haves:
  truths:
    - "编排链每次路由把完整快照落 ConvergenceSessionEvent（复用 repo.routing 事件扩 payload，写入只经 _emit_event）：Stage 0 输入与聚合结果、Stage 1 redacted prompt/response、每候选 breakdown、版本四元组"
    - "从快照可离线回放出同一结果（候选顺序/分数/breakdown/confidence 逐字段相等）且全程零网络调用（pytest 默认 --disable-socket 下通过）"
    - "快照 payload 序列化后 < 64KB 且不含未脱敏敏感串（sk- 模式断言）"
    - "编排 adapter 的 session.routing 透传 degraded 标志（Phase 107 降级 UI 的数据底座）"
  artifacts:
    - path: server/codegraph/services/repo_router_replay.py
      provides: "离线回放：从快照 payload 重建输入 → 纯函数重算 → 与记录结果比对"
      exports: ["replay_route_from_snapshot", "verify_snapshot_replay"]
      min_lines: 60
    - path: server/tests/codegraph/test_repo_router_replay.py
      provides: "回放同结果 + 零网络 + 脱敏 + payload 上限 守护"
      min_lines: 80
  key_links:
    - from: server/services/process_runtime/builtin_processes.py
      to: server/delivery/services/convergence_session_service.py
      via: "_h_route 组装快照 payload 后经 _emit_event(EVENT_REPO_ROUTING) 写入（复用既有事件名，taxonomy 零改动）"
      pattern: "EVENT_REPO_ROUTING"
    - from: server/codegraph/services/repo_router_replay.py
      to: server/codegraph/services/repo_router_scoring.py
      via: "回放调用与 route()/harness 同一份纯函数（aggregate_and_score/derive_confidence/apply_llm_adjustment）"
      pattern: "aggregate_and_score"
---

<objective>
完成 ROUTE-09 的快照落盘与离线回放闭环（CONTEXT §确定性与快照回放）：`_h_route` 把 105-03/105-05 随 `RepoRouteResultV2.snapshot` 携带的材料组装成完整 payload（Stage 0 输入、Stage 1 redacted 材料、每候选 breakdown、版本四元组），经 `ConvergenceSessionService._emit_event` 落 `repo.routing` 事件（复用既有事件名扩 payload——event taxonomy 守护测试零改动，RESEARCH §6 推荐）；`repo_router_replay.py` 从快照重建输入、纯函数重算、零网络得到同一结果。adapter 同步透传 `degraded` 进 session.routing。

Purpose: success criterion 3 后半句成立——「从 ConvergenceSessionEvent 快照可离线回放出同一结果且全程零网络调用」；回放与 golden harness 共用同一纯函数路径。
Output: `_h_route` 快照 payload + adapter degraded 透传 + replay 模块 + 守护测试。
</objective>

<context>
@.planning/phases/105-golden-set/105-CONTEXT.md
@.planning/phases/105-golden-set/105-RESEARCH.md（§6 快照写入契约 + Pattern 3 payload 形状 + Pitfall 3）
@server/services/process_runtime/builtin_processes.py
@server/services/process_runtime/repo_router_adapter.py
@server/codegraph/services/repo_router_scoring.py
@server/interactions/redaction.py
</context>

## Artifacts this phase produces（本 plan 新建/变更符号）

| Symbol | Kind | Location |
|--------|------|----------|
| `replay_route_from_snapshot(payload: dict, *, theta_abs, theta_margin, theta_med) -> list[dict]` | 纯函数 | `server/codegraph/services/repo_router_replay.py` |
| `verify_snapshot_replay(payload: dict, ...) -> tuple[bool, str]`（比对重算 vs 记录，diff 文本） | 纯函数 | 同上 |
| `repo.routing` 事件 payload 扩充（candidates+breakdown / stage0 / stage1 / versions / degraded / auto_selected / router_version） | 事件 payload | `server/services/process_runtime/builtin_processes.py::_h_route` |
| adapter 返回 dict 新增 `degraded` 键 | dict 字段 | `server/services/process_runtime/repo_router_adapter.py` |

<tasks>

<task type="auto">
  <name>Task 1: adapter degraded/snapshot 透传 + _h_route 快照 payload 落盘</name>
  <files>server/services/process_runtime/repo_router_adapter.py, server/services/process_runtime/builtin_processes.py, server/tests/services/test_repo_router_adapter.py</files>
  <read_first>
    - server/services/process_runtime/repo_router_adapter.py（route() 返回精简 dict 的现状；snapshot/degraded 需随 dict 带出）
    - server/services/process_runtime/builtin_processes.py::_h_route（现 payload 只有 `{candidates: [{repo_id, confidence}]}`；`_emit_event(EVENT_REPO_ROUTING, session, trace)` 调用形状）
    - .planning/phases/105-golden-set/105-RESEARCH.md「Pattern 3: 快照 payload 结构」（目标形状原文）与 §6（复用 repo.routing 零 taxonomy 改动；Anti-pattern：绝不在 _emit_event 之外写事件）
    - server/interactions/redaction.py（`redact_for_ledger` 对 nested dict/list/str 全覆盖）
    - server/tests/services/test_repo_router_adapter.py（既有 stub 构造——RepoRouteResultV2 新字段带默认值，stub 不改也能跑；补 degraded 断言）
  </read_first>
  <action>
    1. `repo_router_adapter.route()` 返回 dict 增加 `"degraded": result.degraded` 与 `"snapshot": result.snapshot`（snapshot 仅供 _h_route 组 payload，**不落 session.routing**——planner 裁定：session.routing 保持精简，breakdown/快照细节 trace 里有；由 _h_route 从 dict pop 出使用）。candidates 精简 dict 保持现有三键不变（下游 clarify/confirm 读法零破坏）。
    2. `_h_route` 组装完整快照 payload（RESEARCH Pattern 3 形状）：`{"candidates": [{repo_id, confidence, score, breakdown}...], "router_version", "degraded", "auto_selected", "stage0": snapshot["stage0"], "stage1": snapshot["stage1"], "versions": snapshot["versions"]}`；整体过 `redact_for_ledger` 后经 `_emit_event(EVENT_REPO_ROUTING, session, payload)` 写入（复用既有事件名，event_taxonomy 三处零改动）。router 为 skipped（无 requirement_text）时保持现状最小 payload。snapshot 缺失（stub router / v1_fallback 无材料）时优雅降级为现状精简 payload（best-effort，绝不阻断编排——_emit_event 本身已吞异常）。stage_state_update 里的 routing dict 在落库前剔除 "snapshot" 键（防 session.routing 膨胀）。
    3. 观测：payload 组装完成记 `logger.debug("repo_router_v2_snapshot_emitted", payload_bytes=len(json.dumps(payload)), degraded=..., category="sampling", component="process_runtime")`（best-effort try/except 包裹，观测不反噬）。
    4. 更新 `test_repo_router_adapter.py`：断言 adapter dict 含 degraded；_h_route 测试断言 emit 的 payload 含 candidates[].breakdown / versions / stage0（用带 snapshot 的 stub result）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/test_repo_router_adapter.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `rg -c "_emit_event" server/services/process_runtime/builtin_processes.py` 中 repo.routing 相关调用仍只有一处（写入单一入口 INV-6）
    - `rg -c "redact_for_ledger" server/services/process_runtime/builtin_processes.py` >= 1
    - session.routing（stage_state_update）不含 "snapshot" 键（测试断言）
    - `uv run pytest tests/services/test_event_taxonomy_alignment.py -q` 零改动通过（复用既有事件名）
  </acceptance_criteria>
  <done>编排链每次路由落完整脱敏快照；session.routing 携带 degraded 且保持精简。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 离线 replay 模块 + 回放/脱敏/上限守护测试</name>
  <files>server/codegraph/services/repo_router_replay.py, server/tests/codegraph/test_repo_router_replay.py</files>
  <read_first>
    - server/codegraph/services/repo_router_scoring.py（纯函数契约——replay 唯一的重算入口）
    - server/services/process_runtime/builtin_processes.py::_h_route（Task 1 落的 payload 形状——replay 的输入契约）
    - server/codegraph/services/repo_router_v2.py（rank>1 confidence 规则与 apply_llm_adjustment 接入点——replay 须复刻同一推导顺序）
    - .planning/phases/105-golden-set/105-RESEARCH.md Pitfall 3（payload 上限）与 Security「快照中无 sk- 模式」
  </read_first>
  <behavior>
    - 回放同结果（无 Stage 1）：构造 route() 降级路径产出的 payload → `replay_route_from_snapshot(payload, θ...)` 重算候选（repo_id/score/breakdown/confidence）与 payload 记录的 candidates 逐字段相等
    - 回放同结果（有 Stage 1）：payload 含 stage1 排列记录 → replay 应用同一排列 + apply_llm_adjustment → 与记录相等
    - 零网络：测试文件不做任何 socket allow（pytest 默认 --disable-socket 生效即证明），且 replay 模块 import 不触 Qdrant/LLM/ORM
    - 脱敏：构造含 "sk-abc123..." 假密钥的 stage1 prompt 走 _h_route 组装路径 → 最终 payload 序列化文本无 "sk-abc123" 明文
    - payload 上限：50 个 node_hits 的满配快照 `len(json.dumps(payload).encode()) < 64 * 1024`
  </behavior>
  <action>
    创建 `server/codegraph/services/repo_router_replay.py`（零 I/O 纯函数模块）：
    - `replay_route_from_snapshot(payload, *, theta_abs, theta_margin, theta_med) -> list[dict]`：从 `payload["stage0"]["node_hits"]` 重建 aggregate_and_score 输入（最小字段集还原为 hit 形状：`{"id": node_id, "score": score, "payload": {"repository_id", "node_path", "facets": {"活跃度": activity_facet}}}`）→ 重算分数/排序 → 若 `payload["stage1"]` 含排列记录则按排列重排 + 逐候选 apply_llm_adjustment（confidence hint）→ 复刻 route() 的 rank-1/rank>1 confidence 规则 → 输出 `[{repo_id, score, breakdown, confidence}]`。
    - `verify_snapshot_replay(payload, *, theta...) -> tuple[bool, str]`：重算结果与 `payload["candidates"]` 记录逐字段比对（score 容差 1e-9），不一致返回 (False, diff 文本)。
    - 注意：replay 与 105-01 scoring、105-04 harness 共用同一纯函数——本模块只做「payload 形状 ↔ 纯函数输入输出」的适配，禁止内联第二份打分/confidence 实现（Anti-pattern：推导只在 scoring 模块一处）。
    写 `server/tests/codegraph/test_repo_router_replay.py` 覆盖 behavior 全部条目；快照构造走真实 `_h_route` 组装代码路径（mock engine/session/emit 捕获 payload），保证测试锁的是生产形状而非手写形状。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/codegraph/test_repo_router_replay.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - 回放相等断言覆盖「有 Stage 1」与「无 Stage 1」两条路径
    - `rg -c "derive_confidence|aggregate_and_score" server/codegraph/services/repo_router_replay.py` >= 2（复用纯函数）且模块内无 margin 阈值字面量重复实现
    - 脱敏断言（payload 文本无注入的假密钥明文）与 64KB 上限断言存在且通过
    - `uv run pytest tests/codegraph/test_repo_router_replay.py -q` 全绿（默认 --disable-socket 下，即零网络证明）
  </acceptance_criteria>
  <done>快照 → 离线回放 → 同结果闭环成立，与 harness 共用纯函数路径。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Stage 1 prompt/response → ConvergenceSessionEvent | 可能含敏感串的外部内容入库 |
| 快照 payload → DB | append-only JSON 持久化 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-105-15 | Information Disclosure | repo.routing payload | mitigate | 写入前整体过 `redact_for_ledger`；测试断言注入假密钥不出现在序列化 payload |
| T-105-16 | DoS | payload 无界膨胀 | mitigate | node_hits 只存最小字段集（105-03 已裁）；64KB 上限测试 |
| T-105-17 | Tampering | 事件写入 | mitigate | 只经 `_emit_event` 单一入口（event taxonomy 守护测试拦截旁路） |
| T-105-SC | Tampering | 包安装 | accept | 零新增依赖 |
</threat_model>

<verification>
- `cd server && uv run pytest tests/codegraph/test_repo_router_replay.py tests/services/test_repo_router_adapter.py tests/services/test_event_taxonomy_alignment.py -q` 全绿
- `cd server && uv run pytest tests/codegraph/ -q` 全绿（含 golden 门禁——本 plan 改动不得使其退化）
</verification>

<success_criteria>
- Phase success criterion 3 完整成立：双跑幂等（105-05）+ 快照落盘 + 离线回放零网络同结果（本 plan）
- degraded 数据底座随 session.routing 可达（Phase 107 UI 的输入就绪）
</success_criteria>

<output>
完成后创建 `.planning/phases/105-golden-set/105-07-SUMMARY.md`。
</output>
