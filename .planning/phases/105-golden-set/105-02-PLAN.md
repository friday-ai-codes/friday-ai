---
phase: 105-golden-set
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - server/codegraph/management/commands/measure_repo_index_stats.py
  - server/tests/codegraph/test_measure_repo_index_stats.py
  - .planning/phases/105-golden-set/105-MEASUREMENTS.md
autonomous: true
requirements: [ROUTE-08]
must_haves:
  truths:
    - "存在 management command `measure_repo_index_stats`，能按仓统计 repo_index_nodes 节点数并输出 N_r 直方图（p50/p90/p99/max）与 markdown 表"
    - "105-MEASUREMENTS.md 落盘：O-3（dense 余弦可得性）结论为确定性代码级答案；O-1（N_r 分布）含生产实例执行指引与占位表"
  artifacts:
    - path: server/codegraph/management/commands/measure_repo_index_stats.py
      provides: "O-1/O-3 一次性实测命令（Phase 106 公式定版输入）"
      min_lines: 60
    - path: .planning/phases/105-golden-set/105-MEASUREMENTS.md
      provides: "Phase 106 planning 的直接输入文档"
      contains: "O-3"
  key_links:
    - from: server/codegraph/management/commands/measure_repo_index_stats.py
      to: services/qdrant_service.py
      via: "QdrantService.get_client().count(collection_name='repo_index_nodes', exact=True) 按 repository_id 过滤计数"
      pattern: "count\\("
---

<objective>
落实 Phase 105 success criterion 5（CONTEXT「Phase 106 公式定版输入实测」）：实现 `measure_repo_index_stats` management command（全仓能力树节点数 N_r 分布直方图，O-1），并把 O-3（Stage 0 payload 中 dense 余弦是否可得）的代码级结论写入 `105-MEASUREMENTS.md`，作为 Phase 106 planning 的直接输入。

Purpose: Phase 106 的 MaxP 主干口径（余弦 vs RRF）与 pivoted size normalization 常数（N̄、b）依赖这两项实测；不先落文档则 106 是盲改。
Output: command + 命令测试 + `105-MEASUREMENTS.md`。
</objective>

<context>
@.planning/phases/105-golden-set/105-CONTEXT.md
@.planning/phases/105-golden-set/105-RESEARCH.md
@server/codegraph/management/commands/measure_extractor_precision.py
@server/services/qdrant_service.py
</context>

## Artifacts this phase produces（本 plan 新建符号）

| Symbol | Kind | Location |
|--------|------|----------|
| `measure_repo_index_stats`（`Command`） | management command | `server/codegraph/management/commands/measure_repo_index_stats.py` |
| `105-MEASUREMENTS.md` | phase 文档 | `.planning/phases/105-golden-set/` |

<tasks>

<task type="auto">
  <name>Task 1: 实现 measure_repo_index_stats management command</name>
  <files>server/codegraph/management/commands/measure_repo_index_stats.py, server/tests/codegraph/test_measure_repo_index_stats.py</files>
  <read_first>
    - server/codegraph/management/commands/measure_extractor_precision.py（既有 measure_* 命令的结构/输出先例）
    - .planning/phases/105-golden-set/105-RESEARCH.md §5「O-1：N_r 直方图统计途径」与「Code Examples: N_r 统计」（client.count 精确用法）
    - server/services/qdrant_service.py（`get_client()` 与 `search_by_name`/`hybrid_search_by_name` 返回结构——O-3 验证依据）
    - server/tests/test_milestone_e2e_learning_case.py:104-105（`QdrantClient(":memory:")` + monkeypatch `QdrantService.get_client` 先例）
  </read_first>
  <action>
    创建 `server/codegraph/management/commands/measure_repo_index_stats.py`：

    - `Command.handle`：遍历 `Repository.objects.filter(is_deleted=False)`，对每仓执行 `QdrantService.get_client().count(collection_name="repo_index_nodes", count_filter=models.Filter(must=[models.FieldCondition(key="repository_id", match=models.MatchValue(value=str(repo.id)))]), exact=True).count`（`from qdrant_client import models`）。
    - 统计输出：总仓数、有索引仓数、`statistics.quantiles(counts, n=100)` 取 p50/p90/p99、max、均值与**中位数**（ROUTING-RANKING §2.3：N̄ 建议用中位数）；按 markdown 表打印（列：指标/值），并支持 `--json` 选项输出机器可读 JSON（供转写 MEASUREMENTS）。
    - `--top N` 选项（默认 10）：打印节点数最多的 N 个仓（repo name + N_r），用于确认 monorepo 倾斜程度。
    - O-3 验证辅助：`--verify-cosine` 选项——对任一有索引仓发一次 `QdrantService.search_by_name`（dense-only，collection 距离为 COSINE）并打印返回 score 样例与耗时 ms，确证「余弦需单独 dense 查询可得 + 延迟代价」；同时打印说明：`hybrid_search_by_name` 的 FusionQuery(RRF) 返回分不含 per-prefetch 余弦。
    - 观测：structlog 事件 `measure_repo_index_stats_started/completed`（kv：repo_count/duration_ms，category="caller"，component="codegraph"；management command 由运维手动触发，日志 initiated_by 语义标 system）。命令本身 best-effort：单仓 count 异常记 warning 跳过，不中断全量统计。

    测试 `server/tests/codegraph/test_measure_repo_index_stats.py`：用 `QdrantClient(":memory:")` monkeypatch `QdrantService.get_client`，建 collection `repo_index_nodes` 灌 2 仓各若干点（payload 含 repository_id），factory 建对应 Repository 行，`call_command("measure_repo_index_stats", "--json")` 断言 JSON 中每仓计数正确、分位数字段存在。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/codegraph/test_measure_repo_index_stats.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run python manage.py measure_repo_index_stats --help` 列出 `--json`/`--top`/`--verify-cosine` 选项
    - 测试断言：per-repo count 与灌入点数一致；输出 JSON 含 `p50`/`p90`/`p99`/`max`/`median` 键
    - 命令代码不引入 numpy/scipy（`rg -c "import numpy|import scipy" server/codegraph/management/commands/measure_repo_index_stats.py` == 0，分位数用 `statistics.quantiles`）
  </acceptance_criteria>
  <done>命令在有真实索引的实例上可直接产出 O-1 全量数据；本地测试用内存 Qdrant 验证统计正确性。</done>
</task>

<task type="auto">
  <name>Task 2: 撰写 105-MEASUREMENTS.md（O-3 定论 + O-1 执行指引）</name>
  <files>.planning/phases/105-golden-set/105-MEASUREMENTS.md</files>
  <read_first>
    - .planning/phases/105-golden-set/105-RESEARCH.md §5（O-3 代码级答案：fusion 查询不回传 per-prefetch 余弦；两条取余弦路径）与「index_version 的现实口径」
    - .planning/research/ROUTING-RANKING.md §9（O-1/O-3 的定义与影响）
    - server/services/qdrant_service.py:1354-1437（hybrid_search_by_name 实现——引用行号需按当前 HEAD 核对）
  </read_first>
  <action>
    写 `.planning/phases/105-golden-set/105-MEASUREMENTS.md`（中文正文，代码/命令保留英文），结构：

    1. **O-3：Stage 0 dense 余弦可得性（已定论，代码级验证）**——结论：`QdrantService.hybrid_search_by_name` 用 `FusionQuery(fusion=RRF)`，返回 score 为 RRF 融合分，**不含** per-prefetch dense 余弦；取余弦须单独发一次 dense-only 查询（`QdrantService.search_by_name` 已存在，collection 距离 COSINE）；qdrant-client >=1.9 无「一次查询同时拿两种分」官方途径（标注 RESEARCH A1 假设：官方 changelog 未逐版本核对，生产执行 `--verify-cosine` 时顺带复核）。给 Phase 106 的推论：MaxP 主干若用余弦需 +1 次 Qdrant 往返（同机 <10ms 量级，待 `--verify-cosine` 实测数字回填）。
    2. **O-1：全仓 N_r 分布（待生产实例执行）**——执行指引：在有真实索引的部署实例（friday.yc345.tv）上运行 `uv run python manage.py measure_repo_index_stats --json --top 20 --verify-cosine`，把输出回填本节占位表（p50/p90/p99/max/median/top-20 表，全部标注「待生产实例执行补录」）；说明本地开发库无 259 仓数据、本地跑出的全 0 结果不得写入（RESEARCH Pitfall 8）。
    3. **给 Phase 106 的输入清单**：N̄ 取中位数、b 初值 0.6（ROUTING-RANKING §2.3）、MaxP 口径决策依赖 O-3 延迟实测。

    在本 plan SUMMARY 中显式标注：O-1 生产数据待人工执行命令后补录（autonomous 模式无生产实例访问，per planning context 约定）。
  </action>
  <verify>
    <automated>test -f .planning/phases/105-golden-set/105-MEASUREMENTS.md && rg -q "O-3" .planning/phases/105-golden-set/105-MEASUREMENTS.md && rg -q "measure_repo_index_stats" .planning/phases/105-golden-set/105-MEASUREMENTS.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - 文档含「O-3」定论段（含 `hybrid_search_by_name`/`search_by_name`/`FusionQuery` 标识符）
    - 文档含 O-1 占位表与完整生产执行命令行
    - 文档明确「本地全 0 数据不得回填」警告
  </acceptance_criteria>
  <done>Phase 106 planner 可直接消费本文档：O-3 已定论，O-1 有明确的补录路径。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| command → 生产 Qdrant | 只读 count/search 查询，无写操作 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-105-04 | DoS | measure_repo_index_stats | mitigate | 每仓一次 exact count（259 次轻查询）；单仓异常 warning 跳过不中断；命令为一次性手动触发 |
| T-105-05 | Information Disclosure | 命令输出 | accept | 输出仅仓名与节点计数，无凭证/敏感串 |
| T-105-SC | Tampering | 包安装 | accept | 零新增依赖（statistics/qdrant-client 均既有） |
</threat_model>

<verification>
- `cd server && uv run pytest tests/codegraph/test_measure_repo_index_stats.py -q` 全绿
- `105-MEASUREMENTS.md` 存在且含 O-3 定论 + O-1 执行指引
</verification>

<success_criteria>
- success criterion 5 的工具与文档骨架就位：命令可产出 N_r 直方图；O-3 有确定性结论
- O-1 生产数据的补录路径清晰（命令行 + 占位表），SUMMARY 标注待人工执行
</success_criteria>

<output>
完成后创建 `.planning/phases/105-golden-set/105-02-SUMMARY.md`。
</output>
