---
phase: 48-sdd-facets
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/components/repository/SddMethodologyBadge.vue
  - web/src/pages/repositories/tree.vue
  - web/src/locales/zh-CN.json
  - web/src/components/repository/__tests__/SddMethodologyBadge.spec.ts
autonomous: true
requirements: [SDD-02]
must_haves:
  truths:
    - "facets.methodology === \"SDD\" 时仓库列表卡片渲染显式 SDD 方法论徽标"
    - "facets.methodology === \"SDD\" 时仓库详情（能力树）渲染显式 SDD 徽标，区别于普通事实分面 chip"
    - "非 SDD（methodology 缺失或他值）不渲染 SDD 徽标"
    - "徽标文案来自既有 vue-i18n（zh-CN.json），默认中文"
  artifacts:
    - path: "web/src/components/repository/SddMethodologyBadge.vue"
      provides: "封装 methodology===SDD 守护 + i18n 的显式 SDD 徽标组件"
      contains: "useI18n"
    - path: "web/src/pages/repositories/tree.vue"
      provides: "在仓库卡片 + 能力树详情接入 SddMethodologyBadge"
      contains: "SddMethodologyBadge"
    - path: "web/src/locales/zh-CN.json"
      provides: "repositories.tree.sddBadge / sddBadgeTitle 文案"
      contains: "sddBadge"
    - path: "web/src/components/repository/__tests__/SddMethodologyBadge.spec.ts"
      provides: "徽标渲染/非渲染 + 真实 zh-CN.json 文案断言守护测试"
  key_links:
    - from: "web/src/pages/repositories/tree.vue 仓库卡片块（中栏 v-for card）"
      to: "SddMethodologyBadge"
      via: ":methodology=\"card.facets?.methodology\""
      pattern: "SddMethodologyBadge"
    - from: "web/src/pages/repositories/tree.vue 能力树详情块（右栏 repoTree.facets）"
      to: "SddMethodologyBadge"
      via: ":methodology=\"repoTree.facets?.methodology\""
      pattern: "SddMethodologyBadge"
    - from: "SddMethodologyBadge.vue"
      to: "zh-CN.json repositories.tree.sddBadge"
      via: "t('repositories.tree.sddBadge')"
      pattern: "repositories.tree.sddBadge"
---

<objective>
实现 SDD 方法论标签的前端透出（SDD-02）：用户在知识树页（`tree.vue`，facets 已透出的现成消费面）的仓库列表卡片与单仓能力树详情，对 `facets.methodology === "SDD"` 看到显式高亮的 "SDD" 方法论徽标，据此识别 spec-driven 仓库。

Purpose: 让 SDD 仓库在 UI 可被一眼识别（区别于普通事实分面 chip 的语义高亮），文案接入既有 vue-i18n（默认中文）。

Output:
- `web/src/components/repository/SddMethodologyBadge.vue`（封装 methodology===SDD 守护 + i18n 的显式徽标）
- `web/src/pages/repositories/tree.vue`（卡片 + 详情接入徽标）
- `web/src/locales/zh-CN.json`（徽标文案）
- `web/src/components/repository/__tests__/SddMethodologyBadge.spec.ts`（守护测试）

注（D-48-4 边界）：facets（含 methodology）当前仅经 `repositories/tree_views.py` 透出到知识树 API（`_repo_card` / `RepositoryIndexTreeView`），由 `tree.vue` 消费。标准 `/repositories` 列表/详情页（`index.vue` / `[id]/index.vue`）的 `Repository` 类型不含 facets——**本 phase 复用既有 facets 透出面（tree.vue），不新增后端 serializer 字段**（避免越界改动；与 CONTEXT D-48-4 一致）。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/48-sdd-facets/48-CONTEXT.md

# i18n 徽标组件 + 测试的既有范式（精确镜像其形状）
@web/src/components/knowledge/EntityKindBadge.vue
@web/src/components/knowledge/__tests__/entity-components.spec.ts

# 接入面：仓库卡片（中栏 v-for card，约 L351–371，monorepo/无树徽标处）
#         能力树详情 facets chips（右栏，约 L396–404）
@web/src/pages/repositories/tree.vue

# facets 类型来源（RepoCard.facets / RepoIndexTreeResponse.facets）
@web/src/api/repoTree.ts

# 真实 i18n 文件（新增 repositories.tree.* 键）
@web/src/locales/zh-CN.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: 新建 SddMethodologyBadge 组件 + i18n 文案 + 接入 tree.vue 卡片与详情</name>
  <files>web/src/components/repository/SddMethodologyBadge.vue, web/src/locales/zh-CN.json, web/src/pages/repositories/tree.vue</files>
  <action>
    实现 D-48-4 + SDD-02：

    1. `web/src/locales/zh-CN.json`：新增顶层 `repositories` 段（若不存在）下 `tree`：`"sddBadge": "SDD"` 与 `"sddBadgeTitle": "Spec-Driven（openspec）仓库"`。保持 JSON 合法（注意尾随逗号）；"SDD"/"openspec" 为专有名词保留英文。
    2. 新建 `web/src/components/repository/SddMethodologyBadge.vue`，精确镜像 `EntityKindBadge.vue` 形状：`<script setup lang="ts">` + `import { useI18n } from 'vue-i18n'`（接入既有 vue-i18n，i18n 约束）；`defineProps<{ methodology?: string | null }>()`；`const { t } = useI18n()`；`const isSdd = computed(() => props.methodology === 'SDD')`。模板：`<span v-if="isSdd" data-testid="sdd-methodology-badge" :title="t('repositories.tree.sddBadgeTitle')" class="...emerald 高亮...">{{ t('repositories.tree.sddBadge') }}</span>`——给予方法论语义高亮（用区别于普通灰色 chip 的高亮色，如 emerald），`methodology !== 'SDD'` 时不渲染任何节点（守护"非 SDD 不渲染"）。
    3. `web/src/pages/repositories/tree.vue`：
       - `import SddMethodologyBadge from '~/components/repository/SddMethodologyBadge.vue'`（与既有 CapabilityTreeNode/DomainTreeNode import 同处）。
       - 仓库列表卡片（中栏 v-for card，monorepo/无树徽标行内）插入 `<SddMethodologyBadge :methodology="card.facets?.methodology" />`。
       - 能力树详情（右栏，现有 `repoTree.facets` chip 区，约 L396–404）插入 `<SddMethodologyBadge :methodology="repoTree.facets?.methodology" />`；并把通用 chip 的 `v-for="(value, dim) in repoTree.facets"` 循环**跳过 methodology 值为 SDD 的项**（避免与显式徽标重复渲染，D-48-4 "区别于普通事实分面 chip"）——例如循环内 `v-if` 过滤 `!(dim === 'methodology' && value === 'SDD')`，或基于过滤后的 computed。其他 facets 仍照常 chip 展示。
    4. 不引入新依赖；vue-i18n 已是既有依赖。
  </action>
  <verify>
    <automated>cd web && pnpm vue-tsc --noEmit -p tsconfig.json && pnpm eslint src/components/repository/SddMethodologyBadge.vue src/pages/repositories/tree.vue && node -e "JSON.parse(require('fs').readFileSync('src/locales/zh-CN.json','utf8')).repositories.tree.sddBadge"</automated>
  </verify>
  <done>徽标组件 + i18n 键就位并接入 tree.vue 卡片与详情；vue-tsc 类型通过、eslint 干净、zh-CN.json 合法且含 repositories.tree.sddBadge；详情区 SDD 不再以通用 chip 重复渲染。映射 SDD-02 + ROADMAP 成功标准 3。原子提交边界：`feat(48-02): add SDD methodology badge + i18n + wire into tree view`。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SddMethodologyBadge 守护测试（渲染/非渲染 + 真实 zh-CN.json 文案）</name>
  <files>web/src/components/repository/__tests__/SddMethodologyBadge.spec.ts</files>
  <behavior>
    - methodology="SDD" → 渲染 `[data-testid="sdd-methodology-badge"]`，文案等于真实 `zh-CN.json` 的 `repositories.tree.sddBadge`（导入真实 json 断言，不内联硬编码文案）
    - methodology="维护中"（他值）→ 不渲染徽标
    - methodology 缺省（undefined/null）→ 不渲染徽标
    - title 属性等于真实 `zh-CN.json` 的 `repositories.tree.sddBadgeTitle`
  </behavior>
  <action>
    新建 `web/src/components/repository/__tests__/SddMethodologyBadge.spec.ts`，镜像 `entity-components.spec.ts` 范式：`import { createI18n } from 'vue-i18n'` + `import zhCN from '~/locales/zh-CN.json'`（导入**真实**文案，满足"以真实 zh-CN.json 断言文案"），`createI18n({ legacy: false, locale: 'zh-CN', messages: { 'zh-CN': zhCN } })`；`mount(SddMethodologyBadge, { props, global: { plugins: [i18n] } })`。断言用 `zhCN.repositories.tree.sddBadge` / `sddBadgeTitle` 取期望文案（绝不硬编码字符串字面量），覆盖 behavior 全部用例（含 data-testid 存在/不存在、文案、title）。
  </action>
  <verify>
    <automated>cd web && pnpm vitest run src/components/repository/__tests__/SddMethodologyBadge.spec.ts</automated>
  </verify>
  <done>守护测试全绿：SDD 渲染徽标且文案/标题取自真实 zh-CN.json，他值/缺省不渲染。映射 SDD-02 + ROADMAP 成功标准 3。原子提交边界：`test(48-02): guard SDD badge render + real zh-CN i18n`。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 知识树 API facets → 前端 DOM | facets.methodology 值来自后端，渲染进 Vue 模板 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-05 | Information Disclosure / Tampering (XSS) | SddMethodologyBadge 渲染 | mitigate | 徽标仅渲染 i18n 常量文案（t('...')），不把 facets 原值插入 DOM；仅以 `methodology === 'SDD'` 严格相等做布尔守护；Vue 文本插值默认转义 |
| T-48-06 | Tampering | 重复渲染（详情徽标 + 通用 chip） | mitigate | 详情通用 chip 循环过滤 methodology==='SDD'，避免语义重复，保证显式徽标唯一 |

无 npm 新增依赖（vue-i18n 既有）；无供应链检查项。
</threat_model>

<verification>
- `cd web && pnpm vitest run src/components/repository/__tests__/SddMethodologyBadge.spec.ts` 全绿。
- `cd web && pnpm vue-tsc --noEmit -p tsconfig.json` 类型通过；`pnpm eslint src/components/repository/SddMethodologyBadge.vue src/pages/repositories/tree.vue` 干净。
- 接入点：`grep -c SddMethodologyBadge web/src/pages/repositories/tree.vue` ≥ 2（卡片 + 详情）。
- 文案接入既有 vue-i18n：组件用 `t('repositories.tree.sddBadge')`，测试断言取自真实 zh-CN.json。
</verification>

<success_criteria>
- SDD-02：用户在知识树页仓库列表卡片与单仓详情看到 "SDD" 方法论标签，据此识别 spec-driven 仓库。
- ROADMAP 成功标准 3：用户在仓库列表与详情页可见 "SDD" 方法论标签。
- 非 SDD 仓库不渲染徽标（守护测试证明）；文案接入既有 vue-i18n（默认中文）。
</success_criteria>

<output>
Create `.planning/phases/48-sdd-facets/48-02-SUMMARY.md` when done
</output>
