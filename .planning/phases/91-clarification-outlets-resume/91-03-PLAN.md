---
phase: 91-clarification-outlets-resume
plan: 03
type: execute
wave: 2
depends_on: ["91-01", "91-02"]
files_modified:
  - server/feishu/callbacks/plan_clarify_callback.py
  - server/feishu/urls.py
  - server/tests/feishu/test_plan_clarify_callback.py
autonomous: true
requirements: [CLARIFY-05, CLARIFY-06]
must_haves:
  truths:
    - "群里提交澄清卡后，回调把 form_value 映射为 answers[] 并经共享 helper 写答案 + 续推 PlanSession"
    - "回调据卡片 clarification_id 定位 delivery.Clarification 轮、按 order 映射子题，不信回调直传 session_id"
    - "续推后用 approve_node 恢复挂起的 ai_plan_research 节点（节点重入自驱续推）"
    - "回调 3s 内同步返回确认卡，重活在后台线程做"
  artifacts:
    - path: server/feishu/callbacks/plan_clarify_callback.py
      provides: "@register_card_callback('plan_clarify_') 澄清回调状态机"
      contains: "register_card_callback"
    - path: server/feishu/urls.py
      provides: "回调模块注册导入"
      contains: "plan_clarify_callback"
  key_links:
    - from: server/feishu/callbacks/plan_clarify_callback.py
      to: server/services/plan_orchestration/answer_resume.py
      via: "aanswer_round_and_resume"
      pattern: "aanswer_round_and_resume"
    - from: server/feishu/callbacks/plan_clarify_callback.py
      to: server/workflows/engine/scheduler.py
      via: "WorkflowEngine().approve_node"
      pattern: "approve_node"
---

<objective>
建设工作流/群出口面收答侧（CLARIFY-05 回调 + CLARIFY-06 工作流续推）：新建飞书澄清专用回调 `@register_card_callback("plan_clarify_")`——群卡 form_submit → 取 `form_value`（q{i}/qt{i}）按 order 映射 `delivery.ClarificationQuestion` → 组 `answers[]` → 调共享 helper `aanswer_round_and_resume`（91-01）写答案 + 续推 PlanSession → `approve_node` 重调度挂起的 `ai_plan_research` 节点（91-02 建的订阅）→ 回 `build_clarification_answered_card` 置灰。

Purpose: 91-02 节点发了卡 + 建了订阅，本 plan 接「收答 → 续推 → 重调度」闭环，完成工作流侧端到端。
Output: 新回调模块 + urls 注册 + 回调测试（form_value→answers→answer_round→续推→approve_node）。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/91-clarification-outlets-resume/91-CONTEXT.md
@.planning/phases/91-clarification-outlets-resume/91-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: plan_clarify 回调状态机（同步即时返回 + 后台续推 + approve_node）</name>
  <files>server/feishu/callbacks/plan_clarify_callback.py, server/feishu/urls.py</files>
  <read_first>
    - server/feishu/callbacks/plan_revision_callback.py（逐行范式：@register_card_callback 前缀 / _extract_callback_data / _ack_card 同步即时返回 / _run_in_thread 后台 / bind_task_context re-bind / _aget_waiting_node 幂等门 / approve_node 恢复 / _resolve_space/_aresolve_project / _FeishuResponder / 全程 fail-soft 脱敏）
    - server/feishu/views.py:179-300（register_card_callback 装饰器 + CardCallbackView 前缀 startswith 路由 + form_value 合并进 action_value:251-254）
    - server/services/plan_orchestration/answer_resume.py（91-01 新 helper aanswer_round_and_resume；engine 传带 node_execution_id 的工作流 engine）
    - server/services/plan_orchestration/entrypoint.py（build_orchestration_engine(node_execution_id=...) — 工作流入口 engine 构造）
    - server/delivery/services/clarification_service.py:221-252（answer_round 入参契约）+ 子题按 order 查询形态
    - server/feishu/cards/chat_question_card.py:277+（build_clarification_answered_card 置灰卡）
    - server/feishu/urls.py:5-15（回调模块 import 触发注册的范式）
  </read_first>
  <action>
    新建 `server/feishu/callbacks/plan_clarify_callback.py`，`@register_card_callback("plan_clarify_")`（mirror plan_revision_callback，前缀 `plan_clarify_` 唯一，绝不撞 `chat_question_answer`/`plan_revise`/`plan_revision_`）。同步入口 `handle_plan_clarify_action(callback)`：经 `_extract_callback_data` 取 `action`/`execution_id`/`node_id`/`clarification_id`/`question_count`（CardCallbackView 已把 `form_value` 合并进 action_value，故 `q{i}`/`qt{i}` 同在 data 里）；`action != "plan_clarify_answer"` → None；缺 `clarification_id` 或 `execution_id`/`node_id` → warning + None。`_run_in_thread(_do_clarify_answer_async(...))` 后台续推 + `return _ack_card("已收到，正在记录澄清答复并继续生成方案…")`（3s 内同步返回，per Don't Hand-Roll）。
    后台 `_do_clarify_answer_async(*, execution_id, node_id, clarification_id, question_count, form_value_or_data, responder_id)`：`with bind_task_context(user_id=responder_id or None, source="feishu", component="plan_orchestration")`（re-bind 触发用户，观测约束）。① **幂等门**：`node_execution = await _aget_waiting_node(execution_id, node_id)`（非 waiting → log ignored + return，mirror plan_revision）。② **按 order 映射子题组 answers[]**（Pitfall 1 + 安全）：`from delivery.models import ClarificationQuestion`；按 `clarification_id` 取该轮子题 `ClarificationQuestion.objects.filter(clarification_id=clarification_id).order_by("order")`（**用卡片 clarification_id 取轮、绝不信回调直传的 session_id**，T-91-03 防伪造）；逐 `i, q` 从 data 取 `q{i}`（select 值，multi 为 list）/ `qt{i}`（「其他」freeform）→ 组 `answers.append({"question_id": str(q.id), "selected": <q{i} 值，single=str/multi=list>, "freeform_text": <qt{i}>})`。③ **续推（同源 helper）**：`engine = build_orchestration_engine(node_execution_id=str(node_execution.id))`（工作流入口带 node_execution_id）→ `await aanswer_round_and_resume(clarification_id, answers, engine=engine)`（91-01）。④ **重调度节点**：续推后 `node_execution.approval_data = {"clarification_answered": True, "clarification_id": clarification_id}` → `asave(update_fields=["approval_data"])`；若 workflow_execution.status==SUSPENDED → RUNNING；`await WorkflowEngine().approve_node(node_execution, _FeishuResponder(responder_id), "plan_clarify_answer")`（mirror plan_revision_confirm，节点重入 `_resolve_session` 据 output_data.session_id 续推）。⑤ **回置灰卡**（best-effort）：组 qa_pairs → `build_clarification_answered_card(...)` → `_send_card_best_effort`（取 space 经 `_resolve_space(node_execution)`）。全程 fail-soft：`except Exception` → `logger.error("plan_clarify_answer", status="failed", ...error=redact_secrets_in_text(str(exc)))`（绝不反噬飞书主响应 5xx）；正文/异常脱敏；写入只经 answer_round（INV-6，回调不旁路写表）。async 防裸 lazy-FK。最后在 `server/feishu/urls.py` 加 `import feishu.callbacks.plan_clarify_callback as _plan_clarify_callback  # noqa: F401`（import 触发 @register_card_callback 注册，mirror 既有回调导入）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/feishu/test_plan_clarify_callback.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - 回调注册前缀 `plan_clarify_`；`feishu/urls.py` 已 import 触发注册。
    - 测试覆盖：form_value(q0/qt0/q1...) → answers[] 按 order 映射子题 id → aanswer_round_and_resume 被调（mock）→ approve_node 被调（mock，非 waiting 则 no-op）；single/multi 形态；缺 clarification_id no-op；幂等（非 waiting 节点 ignored）；fail-soft（helper 抛错不冒泡、回 ack 不 5xx）。
    - 回调用卡片 clarification_id 取轮，不信回调直传 session_id；无旁路写 delivery 表；正文脱敏；ruff/mypy 干净。
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 飞书回调→server | 半可信外部回调，可篡改 form_value / value 锚（session_id/clarification_id/answers） |
| 回调→工作流引擎 | approve_node 重调度挂起节点 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-03-01 | Tampering/Elevation | 回调伪造 session_id/clarification_id | mitigate | 用卡片权威 `clarification_id` 查 `delivery.Clarification` 轮再取 session（经 helper），**绝不信回调直传 session_id**；按 order 映射子题，越界 id 经 answer_round `answered_at IS NULL` 过滤 no-op |
| T-91-03-02 | Tampering | 重复提交 / 重放 | mitigate | `_aget_waiting_node` 幂等门（非 waiting no-op）+ answer_round 按题幂等条件更新 |
| T-91-03-03 | Info Disclosure | 回调正文/异常文本泄密 | mitigate | 异常 `redact_secrets_in_text`；日志仅记 execution_id/node_id/clarification_id 标量 |
| T-91-03-04 | Repudiation | 回调无触发用户归因 | mitigate | `bind_task_context(user_id=callback.user_open_id, source="feishu")` re-bind |
| T-91-03-05 | DoS | 同步 handler 内 await 续推超 3s 判失败 | mitigate | `_ack_card` 即时返回 + `_run_in_thread` 后台续推（Don't Hand-Roll） |
| T-91-03-06 | Tampering | 旁路写 delivery 表 | mitigate | 写入只经 `answer_round`（INV-6）；回调无 `.objects.create/.update/.save` |
</threat_model>

<verification>
- `cd server && uv run pytest tests/feishu/test_plan_clarify_callback.py -q` 全绿。
- `cd server && uv run pytest tests/feishu tests/workflows -q` 无回归（既有回调路由 + 工作流恢复）。
- `cd server && uv run ruff format --check feishu/callbacks/plan_clarify_callback.py && uv run ruff check feishu/callbacks/plan_clarify_callback.py feishu/urls.py && uv run mypy feishu/callbacks/plan_clarify_callback.py`。
- `cd server && uv run pytest tests/delivery/test_clarification_service.py -k inv6 -x`（回调不触红 INV-6）。
</verification>

<success_criteria>
- 群卡提交 → 回调按 order 映射 answers[] → answer_round + adrive 续推（同源 helper，不造两套）→ approve_node 重调度节点 → 置灰卡。
- 回调防伪造（用卡片 clarification_id 取轮，不信直传 session_id）、幂等、3s 内同步返回、fail-soft 脱敏、INV-6 满足。
</success_criteria>

<artifacts>
## Artifacts this plan produces（供下游 plan / phase 引用）
- 新回调 action：`plan_clarify_answer`（前缀 `plan_clarify_`），handler `feishu.callbacks.plan_clarify_callback.handle_plan_clarify_action`。
- 工作流侧 CLARIFY-05/06 端到端闭环：群卡发出（91-02）→ 收答续推（本 plan）→ approve_node 重调度 ai_plan_research。
</artifacts>

<output>
Create `.planning/phases/91-clarification-outlets-resume/91-03-SUMMARY.md` when done.
</output>
