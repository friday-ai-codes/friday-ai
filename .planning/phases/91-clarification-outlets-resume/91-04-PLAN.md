---
phase: 91-clarification-outlets-resume
plan: 04
type: execute
wave: 2
depends_on: ["91-01"]
files_modified:
  - server/chat/serializers.py
  - server/chat/views.py
  - server/chat/urls.py
  - server/chat/conversation_service.py
  - server/tests/test_plan_clarification_answer_endpoint.py
autonomous: true
requirements: [CLARIFY-04, CLARIFY-06]
must_haves:
  truths:
    - "会话端 plan 多题轮经 runtime 暴露给前端渲染（结构化 questions[]，与 chat 单题澄清物理隔离）"
    - "新专属路由收结构化 answers[]，检测 plan 澄清时写 delivery.Clarification 并续推 PlanSession"
    - "跨用户/跨项目作答被 owner gate 拒绝（404 隐藏存在性）"
    - "续推后台 task 用干净 contextvars 启动（不带 CurrentThreadExecutor）"
  artifacts:
    - path: server/chat/views.py
      provides: "PlanClarificationAnswerView（plan 澄清专路由收答 + 续推）"
      contains: "PlanClarificationAnswerView"
    - path: server/chat/conversation_service.py
      provides: "runtime 暴露 plan 结构化澄清轮"
      contains: "pending_plan_clarification"
  key_links:
    - from: server/chat/views.py
      to: server/services/plan_orchestration/answer_resume.py
      via: "aanswer_round_and_resume"
      pattern: "aanswer_round_and_resume"
    - from: server/chat/conversation_service.py
      to: server/delivery/models
      via: "Clarification / ClarificationQuestion 结构化轮序列化"
      pattern: "pending_plan_clarification"
---

<objective>
建设 AI 会话出口面 + 回流（CLARIFY-04 后端 + CLARIFY-06 会话侧）：
1. runtime 暴露 plan 结构化澄清轮：在 `conversation_service` 检测到本会话关联 PlanSession 有 pending `delivery.Clarification` 轮时，序列化 `pending_plan_clarification`（多题 questions[]：question_id/qtype/options/recommended/selected/freeform）供前端渲染（Open Q1 推荐解：runtime 暴露结构化轮）。
2. 新建会话端**专属路由** `POST /api/chat/conversations/{conversation_id}/plan-clarification/answer/` 收结构化 `answers: [{question_id, selected, freeform_text}]`（与既有 `clarifications/{id}/answer/` chat 单题澄清**物理隔离**，不污染既有回归，Open Q1 推荐解 b + A2）；owner gate → 经共享 helper `aanswer_round_and_resume`（91-01）写 delivery.Clarification + 续推 PlanSession → 触发 chat 续推（既有 `_schedule_chat_plan_resume` barrier 回灌）。

Purpose: 会话侧与飞书回调（91-03）同源调用 helper（不造两套）；会话 plan 多题轮端到端渲染 + 回流。
Output: 新 serializer + 专路由 + view + runtime 序列化 + 扩展测试。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/91-clarification-outlets-resume/91-CONTEXT.md
@.planning/phases/91-clarification-outlets-resume/91-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: runtime 暴露 plan 结构化澄清轮（CLARIFY-04 数据传输）</name>
  <files>server/chat/conversation_service.py, server/tests/test_plan_clarification_answer_endpoint.py</files>
  <read_first>
    - server/chat/conversation_service.py:2305-2327（runtime 现 `pending_clarification`（chat 单题，ConversationIntentTrace）序列化范式 — plan 澄清要新增**独立** key 不污染）
    - server/delivery/models/plan_session.py:74（PlanSession.conversation_id nullable UUID 软引用 — 会话↔PlanSession 关联键）
    - server/delivery/services/clarification_service.py:330-352（ahas_pending）+ ClarificationQuestion 字段（order/question/qtype/options/recommended/selected/freeform_text/answered_at）
    - server/delivery/models（Clarification 容器 round_no/container_status；ClarificationQuestion 子题）
  </read_first>
  <action>
    在 `conversation_service` 的 runtime 序列化（紧邻现 `pending_clarification` 块，2305-2327 区域）新增独立 key `runtime["pending_plan_clarification"] = None` 默认。检测：经 `conv_uuid` 取本会话关联的 PlanSession（`PlanSession.objects.filter(conversation_id=conv_uuid).order_by("-created_at").afirst()`，标量软引用，async 防裸 lazy-FK）；若有且 `await ClarificationService().ahas_pending(session.id)` → 取 pending 轮 + 子题：`Clarification.objects.filter(session_id=session.id, answered_at__isnull=True).order_by("round_no").afirst()` + `ClarificationQuestion.objects.filter(clarification_id=round.id).order_by("order")` → 序列化 `{"clarification_id": str(round.id), "round_no": round.round_no or 1, "questions": [{"question_id": str(q.id), "question": q.question, "qtype": q.qtype, "options": q.options or [], "recommended": q.recommended, "selected": q.selected, "freeform_text": q.freeform_text}]}`（按 order）。与既有 `pending_clarification`（chat 单题）**完全独立**，不改既有键（A2 物理隔离，不污染 chat 澄清回归）。只读、不写库；序列化失败 best-effort（try/except 吞为 None，绝不反噬 runtime）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/test_plan_clarification_answer_endpoint.py -k runtime -x -q</automated>
  </verify>
  <acceptance_criteria>
    - 会话关联 PlanSession 有 pending 结构化轮 → runtime 含 `pending_plan_clarification.questions[]`（按 order、含 qtype/options/recommended/selected）。
    - 无 PlanSession / 无 pending → `pending_plan_clarification` 为 None；既有 `pending_clarification`（chat 单题）键零回归。
    - async 防裸 lazy-FK；序列化 fail-soft；ruff/mypy 干净。
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: plan 澄清专路由 endpoint 收 answers[] + owner gate + 续推（CLARIFY-04 收答 / CLARIFY-06 会话侧）</name>
  <files>server/chat/serializers.py, server/chat/views.py, server/chat/urls.py, server/tests/test_plan_clarification_answer_endpoint.py</files>
  <read_first>
    - server/chat/views.py:2784-2960（ClarificationAnswerView — owner gate（跨用户 404 + has_project_access）/ 幂等 / 后台 resume task 用干净 contextvars:2942-2947 / _BACKGROUND_TASKS 强引用 范式）
    - server/chat/serializers.py:729-749（ClarificationAnswerSerializer 现单题形态 — 新 serializer 收结构化 answers[]）
    - server/chat/urls.py:184-192（clarifications/<id>/answer/ + conversations/<uuid>/clarification/skip/ 路由形态）
    - server/services/plan_orchestration/answer_resume.py（91-01 helper aanswer_round_and_resume）
    - server/subagent/api/callbacks.py（_schedule_chat_plan_resume — chat 入口私有续推 + barrier 回灌 + entrypoint==CHAT 守门，43-03）
    - server/delivery/models/plan_session.py:74（conversation_id 软引用 — owner/归属校验链）
  </read_first>
  <action>
    ① 新 serializer `PlanClarificationAnswerSerializer`（chat/serializers.py，紧邻既有）：字段 `answers = serializers.ListField(child=serializers.DictField(), allow_empty=False)`，每条形态 `{question_id: str, selected: str|list[str], freeform_text?: str}`（视图层做 question_id 归属校验，serializer 只校验结构）。② 新路由（chat/urls.py）：`path("conversations/<uuid:conversation_id>/plan-clarification/answer/", PlanClarificationAnswerView.as_view(), name="conversation-plan-clarification-answer")`（**新专路由，物理隔离既有 chat 澄清 endpoint，A2/Open Q1-b**）。③ 新 view `PlanClarificationAnswerView(APIView)`（chat/views.py，mirror ClarificationAnswerView 的鉴权/owner gate/后台 task 范式）：`authentication_classes=[OptionalJWTAuthentication]`、`permission_classes=[IsAuthenticated]`；`async def post(self, request, conversation_id)`：取 conversation（`Conversation.objects.select_related("space").aget(id=conversation_id)`，DoesNotExist→404）；**owner gate（V4 复用 ClarificationAnswerView 范式）**：`conversation.created_by_id != user.id` 且非匿名 → 404（隐藏存在性）；非 superuser 再 `PermissionService.has_project_access(user, conversation.space, "member")` 否则 404。取本会话 pending plan 轮：`session = PlanSession.objects.filter(conversation_id=conversation_id).order_by("-created_at").afirst()`；无 session 或无 pending（`ahas_pending` False）→ 404/409（无可答 plan 澄清）。**归属校验**：serializer answers 的每个 `question_id` 必须属于该 session 的 pending 轮（查 `ClarificationQuestion.objects.filter(clarification__session_id=session.id, id__in=[...]).acount()` == 提交数，否则 400/丢弃越界，V5）。续推：取 pending 轮 id → `await aanswer_round_and_resume(round_id, answers)`（91-01 helper，chat engine 缺省）写 delivery + 续驱（同源，不造两套）。**chat 续推/barrier 回灌**：续驱后触发既有 `_schedule_chat_plan_resume`（43-03，entrypoint==CHAT 守门 + barrier 回灌 + adrive 幂等 no-op 安全）完成 chat 入口私有重调度 —— **后台 task 必须 `asyncio.create_task(coro, context=contextvars.Context())` 干净 contextvars 启动**（Pitfall 3：复制请求 contextvars 带 CurrentThreadExecutor，请求结束后 sync_to_async 抛 "already quit"，run 永卡；mirror chat/views.py:2942-2947）+ `_BACKGROUND_TASKS` 强引用。返回 200 + `{clarification_id, answered: true}`。INV-6：写入只经 helper→answer_round，view 不旁路写 delivery 表。观测：`plan_clarification_answer_recorded`（category=caller、component=chat、duration_ms）；异常脱敏 + best-effort（不反噬主响应）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/test_plan_clarification_answer_endpoint.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - 新路由 `conversations/<uuid>/plan-clarification/answer/` 收 answers[] → answer_round 写 delivery + 续推 PlanSession（mock helper/资源断言被调）+ 触发 chat 续推。
    - owner gate：跨用户 / 跨项目 → 404（隐藏存在性，落库/续推前）；越界 question_id 不属于该轮 → 400/丢弃（V5）。
    - 后台续推 task 用干净 contextvars 启动（Pitfall 3）；既有 `clarifications/<id>/answer/`（chat 单题）路由零回归（物理隔离）。
    - INV-6 不旁路写；正文/异常脱敏；ruff/mypy 干净。
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 客户端→endpoint | 用户提交 answers[]，conversation_id / question_id 可越权/越界 |
| endpoint→PlanSession | 续推他人会话的澄清 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-04-01 | Elevation | 跨会话/跨用户答澄清 | mitigate | owner gate（created_by_id 校验 + has_project_access），落库/续推前 404 隐藏存在性（复用 ClarificationAnswerView，无 superuser bypass） |
| T-91-04-02 | Tampering | answers 越界 question_id | mitigate | 校验 question_id 必属该 session pending 轮（acount 比对）；answer_round 再按 id + answered_at IS NULL 过滤 no-op（纵深） |
| T-91-04-03 | DoS/Availability | 续推后台 task contextvars 崩致 run 永卡 | mitigate | `asyncio.create_task(coro, context=contextvars.Context())` 干净启动（Pitfall 3，quick task 260612-crc 历史修复） |
| T-91-04-04 | Tampering | 旁路写 delivery 表 | mitigate | 写入只经 helper→answer_round（INV-6）；view 无 `.objects.create/.update/.save` delivery 模型 |
| T-91-04-05 | Info Disclosure | 异常文本/澄清正文泄密 | mitigate | 异常脱敏（redact_secrets_in_text）；日志仅记 conversation_id/clarification_id 标量 |
</threat_model>

<verification>
- `cd server && uv run pytest tests/test_plan_clarification_answer_endpoint.py -q` 全绿。
- `cd server && uv run pytest tests/test_clarification_answer_endpoint.py tests/chat -q` 无回归（既有 chat 单题澄清 endpoint 物理隔离不破）。
- `cd server && uv run pytest tests/delivery/test_clarification_service.py -k inv6 -x`（endpoint 不触红 INV-6）。
- `cd server && uv run ruff format --check chat/views.py chat/serializers.py chat/conversation_service.py chat/urls.py && uv run ruff check chat/ && uv run mypy chat/views.py`。
</verification>

<success_criteria>
- AI 会话 plan 多题轮经 runtime 暴露给前端渲染（结构化 questions[]，与 chat 单题澄清物理隔离）。
- 新专路由收结构化 answers[]，owner gate + answer_round 写 delivery + adrive 续推（与飞书回调同源 helper，不造两套）+ chat 续推 barrier 回灌。
- 越权/越界防护 + 干净 contextvars + INV-6 + 脱敏满足；既有 chat 澄清回归零破坏。
</success_criteria>

<artifacts>
## Artifacts this plan produces（供下游 plan / phase 引用）
- 新 endpoint：`POST /api/chat/conversations/{id}/plan-clarification/answer/`（view `PlanClarificationAnswerView`，serializer `PlanClarificationAnswerSerializer`，收 `answers:[{question_id,selected,freeform_text}]`）。
- runtime 新键：`pending_plan_clarification`（结构化轮 questions[]，供前端 91-05 渲染）。
- 会话侧 CLARIFY-04/06 闭环：runtime 暴露轮 → 专路由收答 → 同源 helper 续推 → chat barrier 回灌。
</artifacts>

<output>
Create `.planning/phases/91-clarification-outlets-resume/91-04-SUMMARY.md` when done.
</output>
