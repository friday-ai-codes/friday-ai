---
phase: 91-clarification-outlets-resume
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/services/plan_orchestration/answer_resume.py
  - server/services/plan_orchestration/__init__.py
  - server/services/plan_orchestration/clarify_adapter.py
  - server/services/plan_orchestration/clarification_questions.py
  - server/tests/services/test_answer_resume.py
  - server/tests/services/test_engine_clarify.py
autonomous: true
requirements: [CLARIFY-06, CLARIFY-07]
must_haves:
  truths:
    - "回调与会话 endpoint 调用同一个共享 helper 完成 answer_round + 续推（不造两套）"
    - "答后信息不足时引擎会再发一轮澄清，足够时继续编排"
    - "澄清轮次达上界（6）后带现有信息继续编排，不无限挂起"
  artifacts:
    - path: server/services/plan_orchestration/answer_resume.py
      provides: "入口无关共享回流 helper aanswer_round_and_resume"
      contains: "async def aanswer_round_and_resume"
    - path: server/services/plan_orchestration/clarify_adapter.py
      provides: "多轮重判 + round_no 上界（移除 CR-01 单轮硬限）"
      contains: "_MAX_CLARIFY_ROUNDS"
  key_links:
    - from: server/services/plan_orchestration/answer_resume.py
      to: server/delivery/services/clarification_service.py
      via: "ClarificationService.answer_round"
      pattern: "answer_round"
    - from: server/services/plan_orchestration/answer_resume.py
      to: server/services/plan_orchestration/resume.py
      via: "adrive_plan_session_to_pause_or_terminal"
      pattern: "adrive_plan_session_to_pause_or_terminal"
---

<objective>
建设 Phase 91 回流续推地基（后端服务层）：
1. 抽入口无关共享 helper `aanswer_round_and_resume`（薄封装 `answer_round` + `build_orchestration_engine` + `adrive_plan_session_to_pause_or_terminal`），让飞书回调（91-03）与会话 endpoint（91-04）**同源**调用，落 CONTEXT「工作流 + 会话同源，不造两套」（CLARIFY-06 核心）。
2. 放开多轮澄清：移除 `clarify_adapter.py` 的 CR-01 单轮硬限（`Clarification...aexists()` 短路），改为「带答案重判 + `round_no` 上界（默认 6）兜底」，超界带现有信息继续编排、不无限挂起（CLARIFY-07）。

Purpose: 续推/多轮是出口面（91-02~05）的公共依赖，必须先就绪且单一来源。
Output: 新 `answer_resume.py` + barrel 导出；改造后的 `clarify_adapter.py`（+ 必要时扩 `clarification_questions.py` 生成器入参）；新/扩测试。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/91-clarification-outlets-resume/91-CONTEXT.md
@.planning/phases/91-clarification-outlets-resume/91-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 入口无关共享回流 helper aanswer_round_and_resume（CLARIFY-06）</name>
  <files>server/services/plan_orchestration/answer_resume.py, server/services/plan_orchestration/__init__.py, server/tests/services/test_answer_resume.py</files>
  <read_first>
    - server/services/plan_orchestration/resume.py（adrive_plan_session_to_pause_or_terminal:24-48 — 入口无关 helper docstring + lazy import 规避环范本；engine 由调用方传入精神）
    - server/delivery/services/clarification_service.py:221-252（answer_round 入参 `[{question_id,selected,freeform_text}]` + server 端定格 recommendation_adopted）
    - server/services/plan_orchestration/entrypoint.py（build_orchestration_engine — chat 入口无 node_execution_id / 工作流带）
    - server/services/plan_orchestration/__init__.py（barrel：ask_clarification/adrive_... 已导出的形态）
  </read_first>
  <behavior>
    - 给定一个未答的 delivery.Clarification 轮 + answers=[{question_id,selected,freeform_text}]，调用后该轮子题被 answer_round 写入 selected/freeform、定格 recommendation_adopted。
    - 调用后 PlanSession 被续驱到「重挂起短路点或终态」（mock engine/adrive 断言 adrive 以同源 engine + 解析出的 session 被调用一次）。
    - engine 缺省（None）→ 经 build_orchestration_engine() 构造（chat 入口形态，无 node_execution_id）；显式传入 engine → 直接复用（工作流入口可带 node_execution_id 的 engine）。
    - clarification_service 缺省 → ClarificationService()；可注入复用（测试/调用方共享实例）。
    - 返回续驱后的 PlanSession 对象。
    - helper **不**驱动入口私有重调度（不 approve_node / 不写 chat barrier / 不写 marker）——那留各调用方。
    - answer_round 幂等（按题 answered_at IS NULL 条件更新）→ 重复 answers 安全 no-op，不二次覆盖首答。
  </behavior>
  <action>
    新建 `server/services/plan_orchestration/answer_resume.py`，签名 `async def aanswer_round_and_resume(clarification_or_id: Any, answers: list[dict[str, Any]], *, engine: Any = None, clarification_service: Any = None) -> Any`（per CLARIFY-06，mirror resume.py 抽取精神）。实现：函数内 lazy import 规避 import 环（`from delivery.services import ClarificationService`、`from .entrypoint import build_orchestration_engine`、`from .resume import adrive_plan_session_to_pause_or_terminal`）；① `clarification_service = clarification_service or ClarificationService()`；② `clar = await clarification_service.answer_round(clarification_or_id, answers)`；③ 由 clar 取 `session_id`（标量，async 防裸 lazy-FK：用 `clar.session_id`）后 `from delivery.models import PlanSession` → `session = await PlanSession.objects.filter(id=clar.session_id).afirst()`，None 则直接 return None；④ `engine = engine or build_orchestration_engine()`；⑤ `return await adrive_plan_session_to_pause_or_terminal(engine, session)`。docstring 显式标注「入口无关：engine 缺省=chat 入口；工作流入口可传带 node_execution_id 的 engine。**入口私有重调度（节点重入 / chat barrier 回灌）留各调用方**」。在 `__init__.py` barrel 加 `from services.plan_orchestration.answer_resume import aanswer_round_and_resume` + `__all__` 追加 `"aanswer_round_and_resume"`（mirror 既有 ask_clarification 导出）。观测：进出口 best-effort log `answer_round_and_resume_started/completed`（category=caller、component=plan_orchestration、duration_ms），异常**不**吞（让调用方 fail-soft 包裹），但日志走 `_safe_log` 精神不反噬。绝不在本 helper 内写 delivery 表（只经 answer_round，INV-6）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/test_answer_resume.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `aanswer_round_and_resume` 已导出于 barrel（`from services.plan_orchestration import aanswer_round_and_resume` 可导入）。
    - 测试覆盖：answer_round 被调 + adrive 以同源 engine 续驱 + engine 缺省走 build_orchestration_engine + 注入 service 复用 + 幂等重复提交 no-op。
    - helper 内无 `Clarification.objects.create/.update/.save`（INV-6 不旁路写）；ruff/mypy 干净。
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 放开多轮 + round_no 上界 + 带答案重判（CLARIFY-07）</name>
  <files>server/services/plan_orchestration/clarify_adapter.py, server/services/plan_orchestration/clarification_questions.py, server/tests/services/test_engine_clarify.py</files>
  <read_first>
    - server/services/plan_orchestration/clarify_adapter.py:95-161（clarify 三段判定；步骤 1 ahas_pending 短路 / 步骤 2 CR-01 `aexists()` 单轮硬限 / 步骤 3 首轮 policy + LLM 生成 + create_round/fallback）
    - server/services/plan_orchestration/clarification_questions.py（agenerate_clarification_questions 现签名 requirement/routing/recall_hits/max_questions；call_source=plan_clarification 已赋——本 task 不新增 LLM 调用点，复用）
    - server/delivery/services/clarification_service.py:330-352（ahas_pending 统一谓词 — 判存在性，零回归收口）
    - server/delivery/services/clarification_service.py:221-252（answer_round — 已答轮问答的读取来源）
  </read_first>
  <behavior>
    - 已有 pending（未答）→ 仍保持挂起（步骤 1 ahas_pending 不变，零回归）。
    - 已答 1 轮且信息仍不足（policy 仍判 needs）且 round_count < 上界 → 再生成一轮新问题（create_round，round_no=round_count+1），返回 {needs_clarification: True}（多轮）。
    - 已答 1 轮且重判信息足够（重判生成器返回 [] 或 policy 不再需要）→ 返回 {needs_clarification: False}（放行 researching）。
    - 已答轮数达上界（_MAX_CLARIFY_ROUNDS=6）→ 返回 {needs_clarification: False} 并 best-effort log `clarification_round_cap_reached`（带现有信息继续，不再发轮）。
    - 重判把已答轮问答喂进生成输入（防 Pitfall 2 同题死循环：答复必须改变重判输入）。
    - 生成器/LLM 异常仍 best-effort（返回 [] 视为信息足够放行），绝不上抛让 engine.advance 落 failed。
  </behavior>
  <action>
    在 `clarify_adapter.py` 顶部加常量 `_MAX_CLARIFY_ROUNDS = 6`（CONTEXT D Discretion，须 ≥5；注释说明「较宽松上界，实际极少触顶」）。改造 `clarify`：**移除步骤 2 的 `Clarification.objects.filter(session_id=...).aexists()` 单轮短路**（CR-01 硬限，per CLARIFY-07）。新逻辑（步骤 1 ahas_pending 短路保留不动）：先统计已答轮 `round_count = await Clarification.objects.filter(session_id=session.id).acount()`；若 `round_count >= _MAX_CLARIFY_ROUNDS` → best-effort `logger.info("clarification_round_cap_reached", category="sampling", component="plan_orchestration", session_id=str(session.id), round_count=round_count)` → `return {"needs_clarification": False}`（带现有信息继续，不无限挂起，落 CLARIFY-07 上界 + DoS 缓解）。否则进重判：跑 policy（needs==False → 放行 `{"needs_clarification": False}`）；needs==True → 调 `agenerate_clarification_questions(...)` 并**把已答轮问答喂进重判输入**（Pitfall 2 防同题死循环）——读已答子题：`ClarificationQuestion.objects.filter(clarification__session_id=session.id, answered_at__isnull=False).order_by("clarification__round_no","order").values("question","selected","freeform_text")`，组装为 `prior_answers` 文本拼进 `requirement`（首选最小 diff，不改生成器签名；若判定拼接不足以改变重判，则给 `agenerate_clarification_questions` 扩可选入参 `prior_answers: list[dict] | None = None` 并在 prompt 段注入已答上下文——二选一，按生成器现状定，记 SUMMARY）。生成非空 → `create_round(session, questions, round_no=round_count+1)`（INV-6）返回 `{"needs_clarification": True, "clarification_id": str(clar.id)}`；生成空（含异常吞为 []）→ 视为信息足够 `return {"needs_clarification": False}`。首轮（round_count==0）路径保持现状（policy + 生成 + create_round/fallback create_clarification），fallback 单题行零回归（保 90-03 CR-01 用例不破）。注意 async 防裸 lazy-FK（用 `session.id` 标量 / `.acount` / `.values`）。engine `_clarify` 逻辑不动（只看 needs_clarification 转移）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/test_engine_clarify.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - 新增/扩展用例：`-k multi_round`（答后信息不足再发一轮 round_no 递增）、`-k round_cap`（达上界带现有信息继续、不再发轮、log 触顶）、零回归（首轮 + fallback 单题 + 已答信息足够放行）。
    - `clarify_adapter.py` 已无 CR-01 `aexists()` 单轮硬限；`_MAX_CLARIFY_ROUNDS` 常量 ≥5。
    - 生成器异常路径不抛、不落 failed；ruff/mypy 干净；90-03 既有 clarify 用例零回归（`tests/services/test_engine_clarify.py` 全绿）。
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 调用方→helper | answers[] 由各入口（飞书回调/会话 endpoint）传入，question_id 可能越界/伪造 |
| adapter→LLM | 已答问答喂进重判 prompt（用户可控文本） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-01-01 | Tampering | answer_round 入参 question_id | mitigate | answer_round 已按 `id + answered_at IS NULL` 条件更新，越界/已答 id no-op（不在 helper 重复造校验，复用 service 权威过滤） |
| T-91-01-02 | DoS | 多轮无限挂起（自伤） | mitigate | `_MAX_CLARIFY_ROUNDS=6` 硬上界 + adrive `max_steps` fail-soft；超界放行不再发轮 |
| T-91-01-03 | DoS | 同题死循环（重判不吃答案） | mitigate | 重判把已答轮问答喂进生成输入（Pitfall 2），答复改变重判信号 |
| T-91-01-04 | Info Disclosure | 重判 prompt / 日志泄露澄清正文 | mitigate | 触顶/续推日志仅记计数标量（round_count/session_id）；不内联 selected/freeform 正文；生成器复用既有 call_source=plan_clarification 脱敏链 |
| T-91-01-05 | Tampering | helper 旁路写 delivery 表 | mitigate | 写入只经 `ClarificationService.answer_round`（INV-6 grep 守护）；helper 内无 `.objects.create/.update/.save` |
</threat_model>

<verification>
- `cd server && uv run pytest tests/services/test_answer_resume.py tests/services/test_engine_clarify.py -q` 全绿。
- `cd server && uv run pytest tests/delivery/test_clarification_service.py -k inv6 -x` 无回归（helper/adapter 不触红）。
- `cd server && uv run ruff format --check services/plan_orchestration/ && uv run ruff check services/plan_orchestration/ && uv run mypy services/plan_orchestration/answer_resume.py services/plan_orchestration/clarify_adapter.py`。
- `cd server && uv run python manage.py makemigrations --check`（本 plan 无新模型字段，应干净）。
</verification>

<success_criteria>
- 共享 helper `aanswer_round_and_resume` 就绪且 barrel 导出，飞书回调（91-03）与会话 endpoint（91-04）可同源调用。
- 多轮澄清放开：答后重判、信息不足再发一轮、足够则继续，且 `_MAX_CLARIFY_ROUNDS=6` 上界兜底不无限挂起。
- INV-6 / async 防裸 lazy-FK / 脱敏约束全部满足，既有澄清用例零回归。
</success_criteria>

<artifacts>
## Artifacts this plan produces（供下游 plan / phase 引用）
- 新符号：`services.plan_orchestration.answer_resume.aanswer_round_and_resume(clarification_or_id, answers, *, engine=None, clarification_service=None) -> PlanSession`（barrel 导出）。
- 改造符号：`ClarifyAdapter.clarify`（多轮重判 + `_MAX_CLARIFY_ROUNDS=6` 上界，移除 CR-01 单轮硬限）。
- 可能扩展：`agenerate_clarification_questions(..., prior_answers=None)`（仅当拼接 requirement 不足以改变重判时）。
</artifacts>

<output>
Create `.planning/phases/91-clarification-outlets-resume/91-01-SUMMARY.md` when done.
</output>
