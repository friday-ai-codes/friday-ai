---
phase: 91-clarification-outlets-resume
plan: 05
type: execute
wave: 3
depends_on: ["91-04"]
files_modified:
  - web/src/types/clarification.ts
  - web/src/api/chat.ts
  - web/src/stores/chat.ts
  - web/src/components/chat/ClarificationCard.vue
  - web/src/components/chat/ChatMessageArea.vue
  - web/src/locales/zh-CN.json
  - web/src/components/chat/__tests__/ClarificationCard.spec.ts
autonomous: true
requirements: [CLARIFY-04]
must_haves:
  truths:
    - "会话内联卡能渲染 plan 多题轮：每题按 qtype 单选(button)/多选(checkbox)，⭐推荐默认选中"
    - "每题可选自由输入，提交聚合为 answers:[{question_id,selected,freeform_text}] 回流专路由"
    - "提交后切「已回复」态，与既有 chat 单题澄清卡共存不串渲染"
  artifacts:
    - path: web/src/components/chat/ClarificationCard.vue
      provides: "多题多选澄清卡渲染 + answers[] 聚合提交"
      contains: "answers"
    - path: web/src/types/clarification.ts
      provides: "plan 多题轮类型 + answers[] 请求体"
      contains: "PlanClarificationPayload"
  key_links:
    - from: web/src/components/chat/ClarificationCard.vue
      to: web/src/api/chat.ts
      via: "postPlanClarificationAnswer"
      pattern: "postPlanClarificationAnswer"
    - from: web/src/api/chat.ts
      to: server/chat/views.py
      via: "POST /chat/conversations/{id}/plan-clarification/answer/"
      pattern: "plan-clarification/answer"
---

<objective>
建设 AI 会话出口面前端（CLARIFY-04）：扩展现有 `ClarificationCard.vue` 支持 plan 结构化**多题 + 多选（checkbox）**轮（现仅单题单选），渲染推荐项（⭐ / 默认选中）+ 每题可选自由输入；提交聚合为 `answers: [{question_id, selected, freeform_text}]` 回流 91-04 专路由。与既有 chat 单题澄清卡**共存物理隔离**（CONTEXT 锁定「扩展现有组件」+ Open Q1 隔离边界）。

Purpose: 会话用户能内联看到并作答 plan 多题澄清，对接 Phase 90 模型 + 91-04 runtime/endpoint。
Output: 扩展类型/api/store + 组件多题多选渲染 + i18n 文案 + 守护 spec（含新建 ClarificationCard.spec.ts，Wave 0 缺口）。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/91-clarification-outlets-resume/91-CONTEXT.md
@.planning/phases/91-clarification-outlets-resume/91-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 前端类型 + api + store 接线 plan 多题轮（CLARIFY-04 契约）</name>
  <files>web/src/types/clarification.ts, web/src/api/chat.ts, web/src/stores/chat.ts</files>
  <read_first>
    - web/src/types/clarification.ts（现 ClarificationPayload 单 question + options[]；ClarificationAnswerRequest 单题）
    - web/src/api/chat.ts:517-532（postClarificationAnswer 单题 client 范式）
    - web/src/stores/chat.ts:136-139（pendingClarifications Map）+ upsertClarification/markClarificationAnswered + runtime restore（pending_clarification 接入点；本 plan 接 91-04 新 runtime 键 pending_plan_clarification）
    - server 契约：91-04 runtime `pending_plan_clarification`（clarification_id/round_no/questions[]：question_id/question/qtype/options/recommended/selected/freeform_text）+ endpoint `POST /chat/conversations/{id}/plan-clarification/answer/` 收 `{answers:[{question_id,selected,freeform_text}]}`
  </read_first>
  <action>
    ① `types/clarification.ts` 新增 plan 多题轮类型（与 chat 单题 ClarificationPayload 并存，不改既有）：`PlanClarificationQuestion {question_id: string; question: string; qtype: 'single'|'multi'; options: string[]; recommended?: string|string[]; selected?: string|string[]; freeform_text?: string}`、`PlanClarificationPayload {clarification_id: string; round_no: number; questions: PlanClarificationQuestion[]; conversation_id?: string; status?: ClarificationStatus}`、`PlanClarificationAnswerItem {question_id: string; selected: string|string[]; freeform_text?: string}`、`PlanClarificationAnswerRequest {answers: PlanClarificationAnswerItem[]}`。② `api/chat.ts` 新增 `postPlanClarificationAnswer(conversationId: string, payload: PlanClarificationAnswerRequest)` → `POST /chat/conversations/${conversationId}/plan-clarification/answer/`（mirror postClarificationAnswer）。③ `stores/chat.ts`：runtime restore 增对 `pending_plan_clarification` 的接入（独立于 chat `pending_clarification`，按 conversation 维度存，避免跨会话串渲染——mirror pendingClarifications 的 conversation_id 防污染模式）；提供 `markPlanClarificationAnswered` 切「已回复」。conversation_id 维度隔离（types/clarification.ts 既有 conversation_id 注释范式）。
  </action>
  <verify>
    <automated>cd web && pnpm vue-tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - 新类型 `PlanClarificationPayload`/`PlanClarificationAnswerRequest` 导出；`postPlanClarificationAnswer` 命中专路由。
    - store 接 runtime `pending_plan_clarification`（conversation 维度隔离，不串既有 chat 单题澄清）。
    - `pnpm vue-tsc --noEmit` 通过（含 store/api 类型）。
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ClarificationCard 多题多选渲染 + answers[] 聚合提交 + i18n（CLARIFY-04 / Pattern 1）</name>
  <files>web/src/components/chat/ClarificationCard.vue, web/src/components/chat/ChatMessageArea.vue, web/src/locales/zh-CN.json, web/src/components/chat/__tests__/ClarificationCard.spec.ts</files>
  <read_first>
    - web/src/components/chat/ClarificationCard.vue:1-218（现单题：button radiogroup 单选:129-165 + Textarea freeform + skip + markClarificationAnswered；须保留 chat 单题路径零回归）
    - web/src/components/ui/checkbox/Checkbox.vue + index.ts（多选用既有 Checkbox 组件，A5 已确认存在）
    - web/src/components/chat/ChatMessageArea.vue（澄清卡独立于消息循环渲染的分支——plan 多题轮按 kind/payload 形态选渲染）
    - web/src/locales/zh-CN.json（澄清相关文案区；新增多题/多选/推荐措辞 key）
    - 91-04 endpoint answers 契约（single=str / multi=string[]）
  </read_first>
  <behavior>
    - 给定 PlanClarificationPayload（2 题：q1 single 3 选项含 1 推荐 / q2 multi 3 选项含 2 推荐），挂载后 q1 默认选中推荐项、q2 默认选中 2 个推荐项。
    - q1 切换单选（点其他项）只保留一个选中；q2 多选切换累加/取消（Set 语义）。
    - 每题可填 freeform；点提交 → 调 postPlanClarificationAnswer 传 answers=[{question_id:q1, selected:<str>, freeform_text}, {question_id:q2, selected:<string[]>, freeform_text}]。
    - 提交成功 → 切「已回复」态（markPlanClarificationAnswered）。
    - 渲染 plan payload 不影响既有 chat 单题 payload 渲染（两条路径互不串）。
  </behavior>
  <action>
    扩展 `ClarificationCard.vue`（CONTEXT 锁定「扩展现有组件」）：按 payload 形态分支——含 `questions[]`（PlanClarificationPayload）走新多题渲染，否则走既有单题 chat 渲染（零回归）。多题渲染：`v-for` 遍历 questions，每题标题（支持 markdown 加重）；`qtype==='single'` 用既有 button radiogroup 范式（每题独立 selectedId）；`qtype==='multi'` 用 `Checkbox`（A5）维护每题 `Set<string>`，⭐推荐项标记 + **默认选中**（single 取 recommended[0]、multi 取全部 recommended）；每题各带可选 freeform（Textarea）。提交：聚合 `answers: [{question_id, selected: single=str|multi=string[], freeform_text}]` → `postPlanClarificationAnswer(conversationId, {answers})`（conversationId 取 payload.conversation_id / 当前会话上下文）→ 成功 `markPlanClarificationAnswered`。`ChatMessageArea.vue`：澄清卡渲染分支增 plan 多题轮（按 payload 形态/kind 选渲染，独立于 chat 单题卡，不串）。i18n：新增措辞 key 进 `web/src/locales/zh-CN.json`（如「（可多选）」「推荐」「提交答复」等，**默认中文**），组件用 `t(...)`。守护 spec `__tests__/ClarificationCard.spec.ts`（**Wave 0 缺口，新建；先写失败用例再实现**）：用 `@vue/test-utils` + happy-dom，**以真实 zh-CN.json 作 createI18n messages**（锁文案不被改空，参考 STATE.md Phase 24 范式）；覆盖 behavior 全部用例 + 既有单题路径零回归断言。
  </action>
  <verify>
    <automated>cd web && pnpm vitest run src/components/chat/__tests__/ClarificationCard.spec.ts && pnpm vue-tsc --noEmit</automated>
    <human-check>启动前端，触发一次 plan 多题澄清（工作流/会话），确认会话内联卡渲染多题 + 单/多选 + ⭐推荐默认选中 + 自由输入，提交后切「已回复」且方案继续生成。</human-check>
  </verify>
  <acceptance_criteria>
    - `ClarificationCard.spec.ts` 全绿：多题渲染 + 单/多选默认选中推荐 + 切换语义 + answers[] 聚合提交（single=str/multi=string[]）+ 提交切已回复 + 既有单题路径零回归。
    - i18n 文案接 zh-CN.json（真实 json 守护，无英文硬编码）；`pnpm vue-tsc --noEmit` 通过。
    - 多题轮与 chat 单题澄清卡共存不串渲染。
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 浏览器→API | 前端提交 answers[]（最终由 91-04 endpoint owner gate + 归属校验把关） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-05-01 | Tampering | 前端构造越界 answers | accept | 前端为非权威面；服务端 91-04 owner gate + question_id 归属校验 + answer_round 过滤为最终防线（前端只组装 UI 选择） |
| T-91-05-02 | Info Disclosure | 跨会话澄清卡串渲染泄露他会话内容 | mitigate | conversation_id 维度隔离 pending 状态（mirror 既有 pendingClarifications 防污染模式，284-UAT 范式） |
| T-91-05-03 | Spoofing | i18n 文案被改空致告警/提示失效 | mitigate | spec 以真实 zh-CN.json 作 messages 锁关键文案（Phase 24 范式） |
</threat_model>

<verification>
- `cd web && pnpm vitest run src/components/chat/__tests__/ClarificationCard.spec.ts` 全绿。
- `cd web && pnpm vue-tsc --noEmit` 通过。
- `cd web && pnpm vitest run` 无回归（既有 chat 组件 spec）。
- `cd web && pnpm eslint src/components/chat/ClarificationCard.vue src/types/clarification.ts src/api/chat.ts`（@antfu 规范）。
</verification>

<success_criteria>
- AI 会话中 plan 澄清请求内联渲染为单/多选提问卡（⭐推荐默认选中 + 自由输入），用户作答经 91-04 专路由回流。
- 扩展现有 ClarificationCard.vue（不新建专组件）+ 与 chat 单题澄清卡共存物理隔离；i18n 默认中文 + 真实 json 守护。
</success_criteria>

<artifacts>
## Artifacts this plan produces（供下游 plan / phase 引用）
- 前端类型：`PlanClarificationPayload` / `PlanClarificationAnswerRequest`（types/clarification.ts）。
- api：`postPlanClarificationAnswer(conversationId, {answers})`。
- 组件：`ClarificationCard.vue` 多题多选扩展（按 payload 形态分支，chat 单题零回归）+ store `markPlanClarificationAnswered` + runtime `pending_plan_clarification` 接入。
- 新 spec：`web/src/components/chat/__tests__/ClarificationCard.spec.ts`（Wave 0 缺口补齐）。
</artifacts>

<output>
Create `.planning/phases/91-clarification-outlets-resume/91-05-SUMMARY.md` when done.
</output>
