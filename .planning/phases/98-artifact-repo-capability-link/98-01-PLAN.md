---
phase: 98-artifact-repo-capability-link
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/agents/call_source.py
  - .planning/observability/LOGGING-SPEC.md
  - server/initiatives/services/knowledge_graph.py
  - server/initiatives/services/artifact_repo_routing.py
  - server/knowledge/ingestion.py
autonomous: true
requirements: [KDEP-07]
must_haves:
  truths:
    - "ragable 文字工件摄取/更新完成后，正文经 RepoRouterV2.route() 路由 → 每个 matched 仓库落一条 artifact→repo 的 KnowledgeEdge(RELATES_TO)，metadata 携 source/artifact_id/node_paths/keywords/score"
    - "同一工件重跑路由幂等：(artifact_id, repo) 唯一，不产生重复活跃边；路由结果变化时 metadata 覆盖为最新、不再匹配的仓库派生边被失效置位（单向派生）"
    - "非 ragable（无正文 vectorize=False）工件跳过路由，仅保留 Phase 96 的 REFERENCES→project 边"
    - "路由 LLM 调用带受控 call_source=artifact_repo_route；建边步骤 started/completed/failed + duration_ms + 命中仓库数（component=knowledge, category=sampling）"
    - "路由/建边任何异常被吞掉且不打断工件摄取主流程（best-effort，反噬零容忍）"
  artifacts:
    - path: "server/agents/call_source.py"
      provides: "CallSource.ARTIFACT_REPO_ROUTE 受控枚举值"
      contains: "ARTIFACT_REPO_ROUTE"
    - path: "server/initiatives/services/knowledge_graph.py"
      provides: "ProjectKnowledgeGraphService.sync_artifact_repo_edges 幂等 upsert + metadata 覆盖 + 陈旧边失效（单一图写入口）"
      contains: "sync_artifact_repo_edges"
    - path: "server/initiatives/services/artifact_repo_routing.py"
      provides: "ArtifactRepoRoutingService.route_and_sync：路由编排 + call_source + keywords 派生 + 委托 graph 写入"
      contains: "route_and_sync"
  key_links:
    - from: "server/knowledge/ingestion.py"
      to: "ArtifactRepoRoutingService.route_and_sync"
      via: "ingest() 内 source_kind=='artifact' 的 best-effort 后置步骤（实体持久化之后）"
      pattern: "route_and_sync"
    - from: "server/initiatives/services/artifact_repo_routing.py"
      to: "RepoRouterV2.route"
      via: "use_call_source(CallSource.ARTIFACT_REPO_ROUTE) 包裹的路由调用"
      pattern: "RepoRouterV2\\.route"
    - from: "server/initiatives/services/knowledge_graph.py"
      to: "graph_store.add_edge / graph_store.invalidate_edge"
      via: "artifact→repo RELATES_TO 边的唯一图写入收口"
      pattern: "graph_store\\.(add_edge|invalidate_edge)"
---

<objective>
KDEP-07：让 ragable 文字工件在摄取/更新完成后，把正文经 `RepoRouterV2.route()` 路由到匹配仓库，落结构化 `KnowledgeEdge(RELATES_TO, artifact→repo)` 边（metadata 承载 node_paths / keywords / score），单一图写入口、幂等 upsert、单向派生、call_source 观测、fail-soft 绝不反噬工件摄取。

Purpose: 补齐 research §1.5 缺口「工件↔仓库无边、工件↔能力/关键词零关联」。复用既有 `RepoRouterV2`（不自写路由/打分）+ 既有 `graph_store` 边收口（不新建真相表；关键词/能力用边 metadata 承载，per research §2.4 锁定），为 KDEP-09 查询与 Phase 99 可视化提供关联底座。

Output: 新增受控 `CallSource.ARTIFACT_REPO_ROUTE`；`ProjectKnowledgeGraphService.sync_artifact_repo_edges`（图写入口）；`ArtifactRepoRoutingService`（路由编排服务）；`ingest()` 内 artifact-gated best-effort 后置钩子。
</objective>

<execution_context>
@.cursor/gsd-core/workflows/execute-plan.md
@.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/98-artifact-repo-capability-link/98-CONTEXT.md
@.planning/research/v0.16.3-external-deps-knowledge.md

@server/agents/call_source.py
@server/codegraph/services/repo_router_v2.py
@server/initiatives/services/knowledge_graph.py
@server/knowledge/sources/artifact.py
@server/knowledge/ingestion.py
@server/knowledge/graph_store.py
@server/knowledge/models.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: CallSource 新增 ARTIFACT_REPO_ROUTE + LOGGING-SPEC 登记</name>
  <files>server/agents/call_source.py, .planning/observability/LOGGING-SPEC.md</files>
  <read_first>
    - server/agents/call_source.py L90-96（`PLAN_DECOMPOSE` / `FEATURE_LIST_PARSE` 末尾成员，追加锚点）
    - server/agents/call_source.py L36-41（类 docstring 的「33 值」计数，需更新）
    - server/agents/call_source.py L98-112（`normalize` 遍历成员，无需改动，新值自动受控）
    - .planning/observability/LOGGING-SPEC.md L94-100（§4.1 call_source 表格末尾，末行 `plan_decompose`，追加锚点）
  </read_first>
  <action>
    在 `CallSource` 枚举末尾（`FEATURE_LIST_PARSE` 之后）新增成员 `ARTIFACT_REPO_ROUTE = "artifact_repo_route"`（per KDEP-07），加中文注释：v0.16.3 Phase 98——ragable 文字工件正文经 `RepoRouterV2` 路由到仓库/能力（摄取完成后 best-effort 后置步骤，随工件写入高频触发）。同步把类 docstring（L37 附近）与模块 docstring（L7 附近）的枚举计数从「33 值」订正为实际成员数（当前实测成员 + 1；执行时以 `len(list(CallSource))` 实际值为准，连带修正此前 `feature_list_parse` 漏计的偏差）。`normalize` 遍历成员，不改。不动其它枚举拼写/顺序。
    在 LOGGING-SPEC §4.1 表格 `plan_decompose` 行（L99）之后追加一行（列对齐、中文表述、代码标识保留英文，per doc-writing-zh）：
    `| \`artifact_repo_route\` | `ArtifactRepoRoutingService` 工件正文路由（v0.16.3 Phase 98，98-01） | ragable 工件正文经 RepoRouterV2 路由到仓库/能力节点，随摄取触发，best-effort |`
  </action>
  <verify>
    <automated>cd server &amp;&amp; uv run python -c "from agents.call_source import CallSource; assert CallSource.ARTIFACT_REPO_ROUTE.value=='artifact_repo_route'; assert CallSource.normalize('artifact_repo_route')=='artifact_repo_route'; assert CallSource.normalize('bogus')=='unknown'; print('ok', len(list(CallSource)))"</automated>
  </verify>
  <done>`CallSource.ARTIFACT_REPO_ROUTE` 存在且值受控；`normalize` 非法值回退 `unknown`；LOGGING-SPEC §4.1 含 `artifact_repo_route` 登记行；docstring 计数与 `len(list(CallSource))` 一致。</done>
</task>

<task type="auto">
  <name>Task 2: ProjectKnowledgeGraphService.sync_artifact_repo_edges（图写入口：幂等 upsert + metadata 覆盖 + 陈旧边失效）</name>
  <files>server/initiatives/services/knowledge_graph.py</files>
  <read_first>
    - server/initiatives/services/knowledge_graph.py L57-58（`repository_node_id`）、L88-108（`ensure_repository_node`，需 Repository 实例）
    - server/initiatives/services/knowledge_graph.py L129-161（`_add_edge_idempotent`：现有仅跳过、不覆盖 metadata——本任务需要覆盖语义，故新写方法）
    - server/knowledge/graph_store.py L193-229（`add_edge` 支持 metadata 参数、返回 edge id）、L231-254（`invalidate_edge` 失效置位、幂等）、L329-381（`neighbors` 返回 EdgeRecord.metadata）
    - server/knowledge/models.py L71-83（`EdgeRelation.RELATES_TO`）、L275-364（`KnowledgeEdge`，`uniq_kedge_active` 约束 = (source,target,relation) 活跃唯一）
  </read_first>
  <action>
    在 `ProjectKnowledgeGraphService` 新增 async 方法 `sync_artifact_repo_edges(self, *, artifact_entity_id: uuid.UUID, routes: list[dict], event_time=None) -> dict`（唯一 artifact→repo 图写入口，per KDEP-07 单一写入 + 单向派生）：
    - `routes` 每项形如 `{"repo_id": str, "repo_name": str, "node_paths": [str], "keywords": [str], "score": float}`（由 Task 3 组装）。`now = event_time or timezone.now()`（aware）。
    - 取现存活跃出边：`existing = await graph_store.neighbors(artifact_entity_id, relations=[EdgeRelation.RELATES_TO], direction="out")`；筛出 `metadata.source == "artifact"` 的边构成 `existing_by_target: {target_id: EdgeRecord}`（**只治理本 source 派生的边**，绝不碰 KDEP-08 的 project→repo 边或其它 source）。
    - 逐 route：`repo_id` → `repository_node_id(repo_id)` 得 target 节点 id。经 `sync_to_async` 加载 `repositories.models.Repository`（by id）；缺失则跳过该 route（fail-soft，warning）；存在则 `await self.ensure_repository_node(repo)`。组装 `new_meta = {"source":"artifact","artifact_id":<从 artifact_entity 反解或由 caller 传入>,"node_paths":route["node_paths"],"keywords":route["keywords"],"score":route["score"]}`（artifact_id 由 caller 在 routes 或方法参数显式传入，避免反解）。
      - target 已在 `existing_by_target`：比较其 metadata（node_paths/keywords/score/artifact_id）与 `new_meta`；**相等则跳过**（幂等 no-op）；**不等则** `await graph_store.invalidate_edge(edge.edge_id, invalid_at=now)` 后 `await graph_store.add_edge(source_id=artifact_entity_id, target_id=target_node_id, relation=RELATES_TO, valid_at=now, metadata=new_meta)`（metadata 覆盖为最新）。
      - target 不在：直接 `add_edge`（携 metadata）。`IntegrityError` 视为并发已建 → warning 放弃（对齐既有 `_add_edge_idempotent` L152-161 语义）。
    - 陈旧边失效（单向派生）：`existing_by_target` 中 target 不在本次 routes 命中集合的边，逐条 `graph_store.invalidate_edge(edge.edge_id, invalid_at=now)`。
    - 返回 `{"added": n, "updated": n, "removed": n, "skipped": n}` 供观测/测试断言。全程结构化事件 `artifact_repo_edges_synced`（component=knowledge, category=sampling, + duration_ms + matched_repo_count + added/updated/removed）。方法体内不吞异常（由 Task 3 caller best-effort 包裹）；async ORM 一律 `sync_to_async`；`valid_at` 用 aware `timezone.now()`。
    不要改动既有 `_add_edge_idempotent` / `link_repository` / `sync_relations_from_operational`（KDEP-08 在 98-02 处理）。
  </action>
  <verify>
    <automated>cd server &amp;&amp; uv run python -c "import ast,sys; s=open('initiatives/services/knowledge_graph.py').read(); assert 'async def sync_artifact_repo_edges' in s and 'invalidate_edge' in s and 'RELATES_TO' in s; ast.parse(s); print('ok')"</automated>
  </verify>
  <done>`sync_artifact_repo_edges` 存在：新仓库建边、路由结果变化时旧边失效 + 新边覆盖 metadata、不再命中的仓库边失效、相同结果幂等 no-op；仅治理 `metadata.source=="artifact"` 的边；经 `graph_store` 收口写入；模块可 import（语法 + 依赖 OK）。</done>
</task>

<task type="auto">
  <name>Task 3: ArtifactRepoRoutingService 路由编排 + ingest() best-effort 后置钩子</name>
  <files>server/initiatives/services/artifact_repo_routing.py, server/knowledge/ingestion.py</files>
  <read_first>
    - server/codegraph/services/repo_router_v2.py L46-69（`RepoRouteCandidateV2`：repo_id/repo_name/score/matched_node_paths，**无 keywords 字段** → keywords 由 node_paths 派生）、L84-137（`route()` 签名与返回 `RepoRouteResultV2`）
    - server/initiatives/services/repo_association_service.py L160-176（既有 `use_call_source(CallSource.AUX_REPO_ROUTER)` 包裹 route + `arecord_retrieval_trace(kind="routing")` 范式，本任务照抄用新 call_source）
    - server/knowledge/ingestion.py L158-179（`ingest()`：normalize → ingest_events 之后是挂后置钩子的点，实体此时已持久化）、L98-115（`IngestionEvent`：source_kind/source_id/content/vectorize/space_id）
    - server/knowledge/models.py L92-124（`generate_entity_id(DOCUMENT,"artifact",artifact_id)` 派生工件文档实体 id）、L34-51（`EntityKind.DOCUMENT`）
  </read_first>
  <action>
    新建 `server/initiatives/services/artifact_repo_routing.py`：`ArtifactRepoRoutingService`（无状态，async）。方法 `route_and_sync(self, events: list, *, initiated_by_user_id=None) -> None`（KDEP-07 路由编排收口）：
    - 逐 `event`（`IngestionEvent`）：仅处理 `event.source_kind == "artifact"` 且 `event.vectorize` 为 True 且 `event.content` 非空（非 ragable / 空正文跳过路由，per 决策——只保留 REFERENCES→project 边）。
    - `query = event.content`（已脱敏正文，见 sources/artifact.py L212-214）。`use_call_source(CallSource.ARTIFACT_REPO_ROUTE)` 包裹调用 `RepoRouterV2.route(query, top_k=<常量 3>, use_llm=True)`（复用既有降级链；不传 repository_ids = 全库路由，因工件不强绑 space 仓集，per「优雅」最小约束——如需收窄由后续里程碑）。
    - 召回留痕（复用 RepoRouterV2 链，覆盖非对话链路）：`await arecord_retrieval_trace(kind="routing", payload={"query_len": len(query), "candidates":[c.to_dict() for c in result.candidates], "router_version": result.router_version, "source":"artifact"}, user_id=..., source="knowledge")`，best-effort try/except（观测失败不反噬）。注意 payload 只放 query 长度不放正文（对齐 repo_association 日志纪律）。
    - 组装 routes：对每个 candidate，`node_paths = list(candidate.matched_node_paths)`；`keywords`（能力/关键词派生，per Claude's Discretion）= `node_paths` 各段按 "/" 切分后去重的非空 segment 集合（确定性、零额外 Qdrant 查询；关键词/能力用边 metadata 承载，不建实体表）。route dict 携 `repo_id/repo_name/node_paths/keywords/score`。
    - `artifact_entity_id = generate_entity_id(EntityKind.DOCUMENT, "artifact", event.source_id)`；`await ProjectKnowledgeGraphService().sync_artifact_repo_edges(artifact_entity_id=artifact_entity_id, artifact_id=event.source_id, routes=routes, event_time=event.event_time)`（把 artifact_id 传入以写入 metadata）。
    - 结构化事件 `artifact_repo_route_started/completed/failed` + `duration_ms` + `matched_repo_count`/`node_path_count`/`keyword_count`（component=knowledge, category=sampling，per 决策——随摄取高频用 sampling）。空候选（无匹配）记 completed matched_repo_count=0 并调用 sync_artifact_repo_edges(routes=[])——使此前的陈旧 artifact→repo 边被失效（保持一致性），仅留 project 边（fail-soft，research §4）。
    - 顶层 try/except 吞一切异常 + `artifact_repo_route_failed`（脱敏 `redact_secrets_in_text`）——方法整体 best-effort。
    在 `server/knowledge/ingestion.py` 的 `ingest()`（L178 `await ingest_events(...)` 之后、`return len(events)` 之前）挂 gated best-effort 后置步骤：`if request.source_kind == "artifact":` → lazy import `from initiatives.services.artifact_repo_routing import ArtifactRepoRoutingService` → `try: await ArtifactRepoRoutingService().route_and_sync(events) except Exception as exc: logger.warning("artifact_repo_route_hook_failed", ...)`（lazy import 对齐 sources 层跨 app 既有范式；异常不上抛，绝不阻断摄取主流程）。非 artifact source_kind 零影响。
  </action>
  <verify>
    <automated>cd server &amp;&amp; uv run pytest tests/knowledge/test_artifact_source.py -x -q 2>&amp;1 | tail -20</automated>
  </verify>
  <done>新建路由服务：ragable 工件路由 + call_source + RetrievalTrace + keywords 派生 + 委托图写入；`ingest()` artifact-gated best-effort 钩子在实体持久化后触发、异常不反噬；既有 `test_artifact_source.py`（含非 ragable 返回空、正文脱敏、REFERENCES 边）保持全绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM 调用维度标签 | `call_source` 作为指标/筛选维度，基数必须受控（新路由点复用受控枚举） |
| 工件正文 → LLM 路由 | 工件正文（可能含飞书内部文档）进 RepoRouterV2 上下文；正文入图前已 `redact_secrets_in_text` 脱敏（sources/artifact.py） |
| 摄取主流程 | 路由/建边为附加旁路，绝不可反噬工件摄取（fail-soft 硬约束） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-98-01 | Tampering | call_source 维度基数 | mitigate | `ARTIFACT_REPO_ROUTE` 为硬编码受控枚举成员；`CallSource.normalize` 对任意非法字符串回退 `unknown`（既有机制自动覆盖新值） |
| T-98-02 | Information Disclosure | 路由留痕 / 日志 | mitigate | RetrievalTrace payload 只记 query 长度不记正文；日志仅记命中计数；工件正文入图前经 `redact_secrets_in_text`；异常文本经脱敏 |
| T-98-03 | Denial of Service | 摄取主流程 | mitigate | 路由 + 建边全程 best-effort try/except，异常吞掉不上抛；RepoRouterV2 自带降级链（LLM 失败→Stage0→v1）；`top_k` 上限受控 |
| T-98-04 | Tampering | artifact→repo 派生边一致性 | mitigate | `sync_artifact_repo_edges` 仅治理 `metadata.source=="artifact"` 的边，单向派生（陈旧边失效置位、metadata 覆盖），经 `graph_store` 收口 + `uniq_kedge_active` 幂等 |
| T-98-SC | Tampering | npm/pip/cargo installs | accept | 本 plan 不安装任何包（纯仓内 Python：枚举 + 服务 + 钩子），无供应链面 |
</threat_model>

<verification>
- `cd server && uv run python -c "from agents.call_source import CallSource; print(CallSource.ARTIFACT_REPO_ROUTE.value)"` → `artifact_repo_route`
- `cd server && uv run python -c "from initiatives.services.artifact_repo_routing import ArtifactRepoRoutingService; from initiatives.services.knowledge_graph import ProjectKnowledgeGraphService; assert hasattr(ProjectKnowledgeGraphService,'sync_artifact_repo_edges'); print('ok')"`
- `cd server && uv run pytest tests/knowledge/test_artifact_source.py -x -q`（既有摄取守护全绿，含非 ragable 返回空 + 脱敏 + REFERENCES 边）
- `grep -F '| `artifact_repo_route` |' .planning/observability/LOGGING-SPEC.md`
</verification>

<success_criteria>
- ragable 文字工件摄取完成后正文经 `RepoRouterV2.route()` 路由，每个 matched 仓库落 `KnowledgeEdge(RELATES_TO, artifact→repo)` + metadata{source,artifact_id,node_paths,keywords,score}
- 幂等：重跑不产生重复活跃边；路由结果变化时 metadata 覆盖为最新、不再命中的仓库边失效置位（单向派生）
- 非 ragable / 空正文工件跳过路由，仅保留 REFERENCES→project 边
- 路由 LLM 带受控 `call_source=artifact_repo_route`；建边观测 started/completed/failed + duration_ms（category=sampling, component=knowledge）
- 路由/建边异常 best-effort 吞掉，绝不打断工件摄取主流程；既有 `test_artifact_source.py` 无回归
</success_criteria>

<output>
Create `.planning/phases/98-artifact-repo-capability-link/98-01-SUMMARY.md` when done.

## Artifacts this plan produces (98-01)
- 新符号：`CallSource.ARTIFACT_REPO_ROUTE = "artifact_repo_route"`（`server/agents/call_source.py`）
- 新方法：`ProjectKnowledgeGraphService.sync_artifact_repo_edges`（`server/initiatives/services/knowledge_graph.py`，artifact→repo RELATES_TO 边唯一图写入口）
- 新服务：`ArtifactRepoRoutingService.route_and_sync`（`server/initiatives/services/artifact_repo_routing.py`）
- 新钩子：`ingest()` 内 artifact-gated best-effort 路由后置步骤（`server/knowledge/ingestion.py`）
- 文档：LOGGING-SPEC §4.1 新增 `artifact_repo_route` 行
- 边 metadata 契约（供 98-02/98-03 与 Phase 99 消费）：artifact→repo `RELATES_TO`，`metadata={source:"artifact", artifact_id, node_paths:[str], keywords:[str], score:float}`
</output>
