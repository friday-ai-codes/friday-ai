---
phase: 122-impact-trace
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - server/tests/services/code_graph/conftest.py
  - server/tests/services/code_graph/test_impact.py
  - server/tests/services/code_graph/test_trace.py
  - server/tests/services/code_graph/test_symbol_resolve.py
  - server/tests/services/code_graph/test_cross_repo_hop.py
  - server/tests/services/code_graph/test_staleness.py
  - server/tests/services/code_graph/test_impact_shell.py
  - server/tests/mcp_tools/test_impact_trace_tools.py
  - server/tests/agents/tools/test_graph_tools.py
autonomous: true
requirements: [IMPACT-01, IMPACT-02, IMPACT-03, IMPACT-04, IMPACT-05, IMPACT-06]

must_haves:
  truths:
    - "每个后续 plan 的 <verify> 命令指向一个真实存在、可被 pytest 收集到的测试节点"
    - "内核断言可在零 DB 下逐点核对深度分组与最短路（合成冻结图）"
    - "跨仓四分支可在零生产样本下被合成数据覆盖"
  artifacts:
    - path: "server/tests/services/code_graph/conftest.py"
      provides: "known_topology / hub_topology / cross_repo_call_factory 三个 fixture"
      contains: "def known_topology"
    - path: "server/tests/services/code_graph/test_impact.py"
      provides: "IMPACT-01/02/04 的 8 个用例桩 + IMPACT-03 反向守护桩"
    - path: "server/tests/services/code_graph/test_trace.py"
      provides: "IMPACT-05 的 3 个用例桩"
    - path: "server/tests/services/code_graph/test_symbol_resolve.py"
      provides: "D-19 的 2 个用例桩"
    - path: "server/tests/services/code_graph/test_cross_repo_hop.py"
      provides: "IMPACT-03 四分支用例桩"
    - path: "server/tests/services/code_graph/test_staleness.py"
      provides: "D-22 两分支用例桩"
    - path: "server/tests/services/code_graph/test_impact_shell.py"
      provides: "D-24 / D-03 / D-19 短路的用例桩"
    - path: "server/tests/mcp_tools/test_impact_trace_tools.py"
      provides: "IMPACT-06 / D-21 / D-23 / GRAPH-04 回填的用例桩"
    - path: "server/tests/agents/tools/test_graph_tools.py"
      provides: "对话壳注册与 fail-closed 用例桩"
  key_links:
    - from: "server/tests/services/code_graph/conftest.py"
      to: "networkx.MultiDiGraph"
      via: "nx.freeze(g) —— 不冻结则「内核不改图」断言恒真"
      pattern: "nx\\.freeze"
---

<objective>
建立本相位全部自动化验收的地基：一个可逐点核对的**合成冻结** `MultiDiGraph` fixture、一个可调扇入的 hub fixture、一个跨仓四模型造数工厂，以及 `122-RESEARCH.md` §Validation Architecture 表里点名的**全部**测试节点的骨架。

Purpose: 后续 9 个 plan 的每一条 `<verify>` 都必须命中一个真实存在的 pytest 节点。Wave 0 不落地则所有后续任务的 Nyquist 验收都是空头支票。
Output: 1 个 conftest 追加 + 8 个测试文件（其中 2 个在 `tests/mcp_tools/` 与 `tests/agents/tools/`），全部用例以 `@pytest.mark.skip(reason="Wave 0 桩：由 122-NN 落地")` 标注。
</objective>

<execution_context>
@.cursor/gsd-core/workflows/execute-plan.md
@.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/122-impact-trace/122-CONTEXT.md
@.planning/phases/122-impact-trace/122-RESEARCH.md
@.planning/phases/122-impact-trace/122-PATTERNS.md
@.planning/phases/122-impact-trace/122-VALIDATION.md
@server/tests/services/code_graph/conftest.py
@server/tests/services/code_graph/test_loader.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: conftest 追加合成冻结图 fixture 与跨仓造数工厂</name>
  <read_first>
    - `server/tests/services/code_graph/conftest.py`（全文 —— 顶部两条约定：ORM 模型走函数体内 lazy import；`Symbol`/`CallEdge` 的 `branch_name=""` 表示 base，`RepositoryBranchIndex.branch_name` 存真实分支名。既有 5 个 fixture 与 `_reset_code_graph_state` autouse 钩子）
    - `server/tests/services/code_graph/test_loader.py:102-154`（`_make_cross_repo_call` —— 四模型造数链 `ApiWrapper` → `ApiCallSite` → `Endpoint` → `CrossRepoApiCall`，`endpoint_repository` 参数传另一个仓即造出真跨仓行）
    - `.planning/phases/122-impact-trace/122-RESEARCH.md` §Code Examples §5（`known_topology()` 的目标拓扑与 7 条可点检断言）
    - `server/services/code_graph/model.py:319-360`（`CodeGraph` docstring 的属性个数契约：节点恒 5 属性 `name`/`symbol_type`/`file_path`/`start_line`/`end_line`；边恒 3 属性 `kind`/`confidence`/`line_number`，`cross_repo` 档例外多一个 `match_confidence`）
  </read_first>
  <action>
在 `server/tests/services/code_graph/conftest.py` 末尾追加三个 fixture，形态照该文件既有工厂（模块级 `@pytest.fixture` + 闭包工厂 + docstring 写清「为什么这么造」），networkx 在文件顶部正常 import（测试文件不受 `model.py` 的 TYPE_CHECKING adapter seam 约束）。

① `known_topology()` —— 无参 fixture，返回一张 `nx.MultiDiGraph`，节点属性恰 5 个、边属性恰 3 个（`cross_repo` 边 4 个），返回前调 `nx.freeze(g)`。拓扑按 `122-RESEARCH.md` §Code Examples §5 逐条落地：`B→A(resolved)` / `E→B(resolved)` / `C→B(bare_name)` / `C→A(resolved)` / `D→C(resolved)` / `F→A(cross_repo, match_confidence=0.7)` / `A→G(resolved)`，另加孤立节点 `H`。**在此之上再加一个与 A 完全不连通的等长多解簇**：`P→Q(resolved)` / `Q→S(resolved)` / `P→R(resolved)` / `R→S(resolved)`，用于 `test_equal_length_paths_declared`（现有拓扑里 D→A 只有一条最短路，无法验证多解声明）。节点 id 用大写字母字符串，`name` 用小写、`symbol_type="FUNCTION"`、`file_path`/`start_line` 各不相同以便逐点核对。docstring 用 ASCII 图画出拓扑并写明「⚠️ 必须 `nx.freeze`：不冻结的话『内核不修改入参图』这条断言恒真，等于没有」。

② `hub_topology()` —— 闭包工厂 fixture，签名 `(fan_in: int, *, confidence: str = "resolved") -> nx.MultiDiGraph`：造一个 `hub` 节点 + `fan_in` 个直接前驱 + 每个前驱各带一个二级前驱，返回前 `nx.freeze`。docstring 写明它服务于 `test_truncation_summary` 与 `test_risk_levels` 的边界取值，理由引 `122-RESEARCH.md` Pitfall 6 的生产分布（解析边入度 max 2,803 / p99 25，200 条截断必然触发）。

③ `cross_repo_call_factory` —— DB 闭包工厂 fixture，签名与 `test_loader.py::_make_cross_repo_call` 同款（`caller_file` / `caller_function` / `endpoint_file` / `handler_name` / `match_confidence=0.7` / `caller_line=33` / `endpoint_repository=None`），四个模型全部走函数体内 lazy import。⛔ **不要**从 `test_loader.py` 搬走那个 helper——它是 Phase 121 的既有回归资产，跨模块 import 测试模块内私有 helper 不是本仓做法，改动它会给 121 的绿测引入无谓风险。docstring 里写明这份是有意的第二实现及其理由。
  </action>
  <acceptance_criteria>
    - `cd server && uv run python -c "import ast,sys; t=ast.parse(open('tests/services/code_graph/conftest.py').read()); names={n.name for n in ast.walk(t) if isinstance(n,(ast.FunctionDef,ast.AsyncFunctionDef))}; assert {'known_topology','hub_topology','cross_repo_call_factory'} <= names, sorted(names)"` 退出码 0
    - 新增一条自检用例 `tests/services/code_graph/test_impact.py::test_known_topology_fixture_is_frozen`（**不 skip**）：断言 `nx.is_frozen(known_topology) is True`、`known_topology.is_multigraph() is True`、`known_topology.number_of_nodes() == 12`（A–H 八个 + P/Q/R/S 四个）、`set(known_topology.nodes["A"]) == {"name","symbol_type","file_path","start_line","end_line"}`、`set(known_topology["F"]["A"][0]) == {"kind","confidence","line_number","match_confidence"}`、`set(known_topology["B"]["A"][0]) == {"kind","confidence","line_number"}`
    - `cd server && uv run pytest tests/services/code_graph/test_impact.py::test_known_topology_fixture_is_frozen -q` 通过
    - `cd server && uv run pytest tests/services/code_graph -q --reuse-db` 相对基线零新增失败（Phase 121 基线：96 passed）
  </acceptance_criteria>
  <verify>
    <automated>cd server && GALAXY_CACHE_WARM_ON_STARTUP=False GRAPH_BUILD_ORPHAN_RECONCILE_ON_STARTUP=False uv run pytest tests/services/code_graph/test_impact.py::test_known_topology_fixture_is_frozen -q --reuse-db</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: 内核测试文件骨架（test_impact / test_trace / test_symbol_resolve）</name>
  <read_first>
    - `server/tests/services/code_graph/test_model.py`（全文 —— 本包唯一的零 DB 测试文件形态：无 `pytest.mark.django_db`、纯 import + 值断言 + AST 自省）
    - `.planning/phases/122-impact-trace/122-RESEARCH.md` §Validation Architecture 的 Requirements → Test Map（26 行，Behavior 与 Automated Command 两列是权威，⛔ 不要改写）
    - `.planning/phases/122-impact-trace/122-VALIDATION.md` §Wave 0 Requirements
  </read_first>
  <action>
新建三个**零 DB**测试文件，每个用例只有 docstring + `pytest.fail("Wave 0 桩")` 的函数体，并逐个挂 `@pytest.mark.skip(reason="Wave 0 桩：由 122-NN 落地")`（NN 按下方归属填）。⛔ 文件顶部**不得**出现 `pytest.mark.django_db`——这三个文件的全部断言必须能在零数据库下跑完（D-01 的全部意义所在）。

`tests/services/code_graph/test_impact.py`（Task 1 已在其中放了 `test_known_topology_fixture_is_frozen`，本 task 在其后追加）：
`test_depth_grouping` / `test_max_depth_budget` / `test_kernel_does_not_mutate_graph` / `test_edge_confidence_and_reason` / `test_min_confidence_filter` / `test_bare_name_requires_both_gates` / `test_risk_levels` / `test_truncation_summary` → 归 122-03；`test_graph_cross_repo_edges_are_intra_repo` → 归 122-06。

`tests/services/code_graph/test_trace.py`：
`test_shortest_path_hops` / `test_equal_length_paths_declared` / `test_no_path_explicit_structure` → 归 122-04。

`tests/services/code_graph/test_symbol_resolve.py`：
`test_uid_takes_precedence` → 归 122-02（零 DB）；`test_ambiguous_returns_candidates` → 归 122-05（需 DB 取 `signature`，桩上额外挂 `@pytest.mark.django_db`）。

每个桩的 docstring **逐字抄写** RESEARCH 表里该行的 Behavior 列，并在末尾用 `（Req: IMPACT-0X, 决策: D-YY）` 标注归属，让执行方无需回翻表格。
  </action>
  <acceptance_criteria>
    - `cd server && uv run pytest tests/services/code_graph/test_impact.py tests/services/code_graph/test_trace.py tests/services/code_graph/test_symbol_resolve.py -q --collect-only` 收集到 **14** 个节点（1 真 + 13 桩），退出码 0
    - `cd server && uv run pytest tests/services/code_graph/test_impact.py tests/services/code_graph/test_trace.py -q` 结果为 `1 passed, 11 skipped`（这两个文件零 DB，⛔ 命令不带 `--reuse-db` 也必须通过）
    - `cd server && uv run python -c "import ast; [exec(compile(ast.unparse(ast.parse(open(p).read())),'<s>','exec')) if False else None for p in []]; src=open('tests/services/code_graph/test_impact.py').read()+open('tests/services/code_graph/test_trace.py').read(); assert 'django_db' not in src"` 退出码 0
  </acceptance_criteria>
  <verify>
    <automated>cd server && uv run pytest tests/services/code_graph/test_impact.py tests/services/code_graph/test_trace.py -q</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: 壳层测试文件骨架（cross_repo / staleness / impact_shell / mcp / agents）</name>
  <read_first>
    - `server/tests/mcp_tools/conftest.py`（`mcp_client` 返回 `(APIClient, plaintext_token)` 已带 Bearer 头；`indexed_repository`；autouse 的 `_disable_repo_mirror`）
    - `server/tests/mcp_tools/test_reverse_lookup_tool.py`（全文 149 行 —— `pytestmark = pytest.mark.django_db`、模块级 `URL` 常量、未认证 401 用例、被排除文件不泄漏用例）
    - `server/tests/agents/tools/test_knowledge_read_tools.py:39-48`（`test_tools_registered_and_whitelisted` 的断言形态：从 `agents.chat_runner` 取 `_INDEXED_TOOL_NAMES`，从 `agents.tools.base` 取 `_tool_registry`）
    - `.planning/phases/122-impact-trace/122-RESEARCH.md` §Validation Architecture 的后 9 行
  </read_first>
  <action>
新建 5 个测试文件，桩形态与 Task 2 相同（docstring + `pytest.mark.skip`），需要库的文件挂 `pytestmark = pytest.mark.django_db`。

`tests/services/code_graph/test_cross_repo_hop.py`（DB，归 122-06）：`test_cross_repo_success` / `test_unauthorized_repo_redacted` / `test_peer_unavailable_fail_soft` / `test_hop_budget`。

`tests/services/code_graph/test_staleness.py`（DB，归 122-05）：`test_behind_commits_reported` / `test_behind_commits_none_degrades_to_as_of`。

`tests/services/code_graph/test_impact_shell.py`（DB）：`test_over_budget_uses_seeded_subgraph`（归 122-05，D-24）/ `test_graph_error_translated_not_swallowed`（归 122-05，D-03）/ `test_ambiguous_symbol_short_circuits_before_graph_fetch`（归 122-07，D-19）。

`tests/mcp_tools/test_impact_trace_tools.py`（DB）：模块级两个常量 `IMPACT_URL = "/api/mcp/tools/impact_analysis/"` 与 `TRACE_URL = "/api/mcp/tools/trace_call_path/"`；用例 `test_impact_tool_unauthenticated` / `test_impact_tool_repository_not_indexed` / `test_degradation_markers_surfaced` / `test_excluded_files_invisible` / `test_staleness_declared` 归 122-08，`test_two_surfaces_same_payload` 归 122-10。

`tests/agents/tools/test_graph_tools.py`（DB，归 122-09）：`test_registered_and_whitelisted` / `test_conversation_owner_required_fail_closed`。

⚠️ `tests/mcp_tools/test_schema_snapshot.py` 本 task **不碰**——它已存在且当前是绿的，加两条字面量条目属 122-08 的交付（同批加 `urls.py` 与 `TOOL_SCHEMA_SNAPSHOT`，否则双向断言会在两个 wave 之间红着）。
  </action>
  <acceptance_criteria>
    - 五个文件均存在于磁盘
    - `cd server && GALAXY_CACHE_WARM_ON_STARTUP=False GRAPH_BUILD_ORPHAN_RECONCILE_ON_STARTUP=False uv run pytest tests/services/code_graph/test_cross_repo_hop.py tests/services/code_graph/test_staleness.py tests/services/code_graph/test_impact_shell.py tests/mcp_tools/test_impact_trace_tools.py tests/agents/tools/test_graph_tools.py -q --reuse-db` 结果为 `17 skipped`，退出码 0
    - `cd server && uv run pytest tests/mcp_tools/test_schema_snapshot.py -q --reuse-db` 仍通过（本 task 未触碰该文件，双向断言未被破坏）
    - `git diff --name-only` 不含 `mcp/` 下任何路径（D-27：submodule 全程不碰）
  </acceptance_criteria>
  <verify>
    <automated>cd server && GALAXY_CACHE_WARM_ON_STARTUP=False GRAPH_BUILD_ORPHAN_RECONCILE_ON_STARTUP=False uv run pytest tests/services/code_graph/test_cross_repo_hop.py tests/services/code_graph/test_staleness.py tests/services/code_graph/test_impact_shell.py tests/mcp_tools/test_impact_trace_tools.py tests/agents/tools/test_graph_tools.py tests/mcp_tools/test_schema_snapshot.py -q --reuse-db</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 无 | 本 plan 只新增测试文件与 fixture，零生产代码、零网络入口、零鉴权路径 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-122-exclusion 回流 | Information Disclosure | `tests/mcp_tools/test_impact_trace_tools.py` | mitigate | `test_excluded_files_invisible` 桩在本 plan 落地占位，122-08 填实；照 `test_reverse_lookup_tool.py` 的 `.env` 造数模板 |
| T-122-穿仓 | Information Disclosure | `conftest.cross_repo_call_factory` | mitigate | 工厂支持 `endpoint_repository` 指向另一个仓，让 122-06 能造出真跨仓行来验证 `REDACTED_REPOSITORY` 折叠 |
| T-122-SC | Tampering | npm/pip/cargo installs | accept | 本相位零新增依赖（`122-RESEARCH.md` §Package Legitimacy Audit：审计表为空，networkx 3.6.1 已是 Phase 121 的生产依赖），无 `[ASSUMED]`/`[SUS]` 包，无需人工核验闸 |
</threat_model>

<verification>
- `cd server && GALAXY_CACHE_WARM_ON_STARTUP=False GRAPH_BUILD_ORPHAN_RECONCILE_ON_STARTUP=False uv run pytest tests/services/code_graph tests/mcp_tools tests/agents --reuse-db -q` —— 相对 HEAD 基线零新增失败（已知既有失败：`test_mcp_package_alignment.py::test_mcp_package_tools_match_server_snapshot` 5 项漂移）
- `cd server && uv run ruff check tests/services/code_graph tests/mcp_tools tests/agents` 通过
- `git diff --name-only -- mcp` 为空（D-27）
</verification>

<success_criteria>
- `122-RESEARCH.md` §Validation Architecture 表里 27 行点名的每一个 pytest 节点都可被 `--collect-only` 收集到（除 `test_schema_snapshot.py` 与 `test_access.py` 两行既有文件外）
- `known_topology` 是**冻结**的 `MultiDiGraph`，节点/边属性个数与 Phase 121 的内存契约逐字一致
- 全部桩处于 skip 状态，`tests/services/code_graph` 的 passed 计数相对 Phase 121 基线恰好 +1
</success_criteria>

## Artifacts this phase produces

本 plan 交付的符号与文件：

| 类型 | 名称 | 位置 |
|---|---|---|
| pytest fixture | `known_topology` | `server/tests/services/code_graph/conftest.py` |
| pytest fixture（工厂） | `hub_topology(fan_in, *, confidence)` | 同上 |
| pytest fixture（工厂，DB） | `cross_repo_call_factory(...)` | 同上 |
| 测试文件（新建） | `test_impact.py` / `test_trace.py` / `test_symbol_resolve.py` / `test_cross_repo_hop.py` / `test_staleness.py` / `test_impact_shell.py` | `server/tests/services/code_graph/` |
| 测试文件（新建） | `test_impact_trace_tools.py` | `server/tests/mcp_tools/` |
| 测试文件（新建） | `test_graph_tools.py` | `server/tests/agents/tools/` |
| 测试常量 | `IMPACT_URL` / `TRACE_URL` | `server/tests/mcp_tools/test_impact_trace_tools.py` |
| 真实用例 | `test_known_topology_fixture_is_frozen` | `server/tests/services/code_graph/test_impact.py` |

<output>
Create `.planning/phases/122-impact-trace/122-01-SUMMARY.md` when done
</output>
