---
phase: 122-impact-trace
plan: 06
type: execute
wave: 3
depends_on: ["122-05"]
files_modified:
  - server/services/code_graph_cross_repo.py
  - server/tests/services/code_graph/test_access.py
  - server/tests/services/code_graph/test_cross_repo_hop.py
  - server/tests/services/code_graph/test_impact.py
autonomous: true
requirements: [IMPACT-03]

must_haves:
  truths:
    - "改后端 Endpoint 时能列出别的仓库里受影响的前端调用点，结果标 cross_repo: true 与 match_confidence 原值"
    - "对端仓无权限时整仓折叠为 REDACTED_REPOSITORY 裸标记，⛔ 不静默丢弃、⛔ 不泄漏任何对端标识符"
    - "对端仓未索引/建图超时时给 unavailable_reason 条目，本仓结果照常返回（fail-soft）"
    - "跨仓跳数上限 1，不递归"
    - "图里 kind == cross_repo 的边两端必在同仓，⛔ 不得被标成跨仓结果"
  artifacts:
    - path: "server/services/code_graph_cross_repo.py"
      provides: "跨仓一跳：CrossRepoApiCall ORM 直查 + 对端仓 get_graph 复核 + 三种显式条目"
      exports: ["collect_cross_repo_impact", "DEFAULT_MAX_CROSS_REPO_HOPS"]
      min_lines: 160
  key_links:
    - from: "server/services/code_graph_cross_repo.py"
      to: "codegraph.models.CrossRepoApiCall"
      via: "endpoint__repository_id=本仓 + .exclude(call_site__repository_id=本仓)"
      pattern: "call_site__repository_id"
    - from: "server/services/code_graph_cross_repo.py"
      to: "services.code_graph_tools.fetch_graph_for_tool"
      via: "每穿一仓复核一次权限（D-12）"
      pattern: "fetch_graph_for_tool"
---

<objective>
交付 IMPACT-03 的**唯一可行实现路径**：壳层用一次 `CrossRepoApiCall` ORM 查询做一跳穿仓，对端仓再走一次 `get_graph`（每仓复核权限），在对端图上继续反向 BFS。

🚨 **不要沿图边穿仓。** `loader._load_cross_repo_edges`（`loader.py:812-828`）只在 `call_site.repository_id` **与** `endpoint.repository_id` **同时等于本仓**时才建边，凡有一端在别的仓库的行一律 `unresolved_count += 1; continue`——图里 `kind == "cross_repo"` 的边**从来不跨仓**，真跨仓的行 100% 落在 `cross_repo_unresolved_count` 里、永不入图。⛔ **不许改 `loader.py` 的建边口径来「顺手修好」**，那是 Phase 121 已验证的冻结行为（D-25）。

⚠️ **生产零样本**：`CrossRepoApiCall` = 0 / `ApiCallSite` = 0 / `ApiWrapper` = 0（`Endpoint` = 6,014），上游产出器依赖 volar LSP 而 server 镜像无 Node（归 LSP-01 / Phase 127）。本 plan 用**合成数据**覆盖四条分支，SUMMARY 必须如实声明「跨仓路径未经任何真实数据验证」，⛔ 不得表述成「跨仓 impact 已可用」（D-26）。

Purpose: 跨仓 impact 是本里程碑相对 GitNexus 的反超点（它每仓独立图做不到）。同时它是本相位最集中的信息泄漏面（T-122-穿仓）。
Output: `server/services/code_graph_cross_repo.py` + 四分支用例 + 一条反向守护。
</objective>

<execution_context>
@.cursor/gsd-core/workflows/execute-plan.md
@.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/122-impact-trace/122-CONTEXT.md
@.planning/phases/122-impact-trace/122-RESEARCH.md
@.planning/phases/122-impact-trace/122-PATTERNS.md
@server/services/code_graph_tools.py
@server/services/code_graph/impact.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: CrossRepoApiCall 直查与按对端仓分组（D-25）</name>
  <read_first>
    - `server/agents/tools/find_api_callers.py:146-188`（仓里**唯一**的 `CrossRepoApiCall` 消费者，方向与 IMPACT-03 完全一致：后端 handler → 前端调用点。可照抄：两步查询 `Endpoint.values_list("id")` → `CrossRepoApiCall.filter(endpoint_id__in=...).select_related("call_site","call_site__api_wrapper")`、`match_confidence` 原样透传、按 `(caller_file, line_number)` 排序。⚠️ 三点必须补上它没做的：不按对端仓分组、不 `.exclude` 同仓行、零权限校验）
    - `server/services/code_graph/loader.py:791-844`（`_load_cross_repo_edges` 的两端同仓判据——本 task 存在的全部理由；`loader.py:751-754` 的注释把这笔账直接记给了 Phase 122）
    - `server/codegraph/models.py:176-330`（`Endpoint` 的 `repository_id` / `file_path` / `handler_name`；`ApiCallSite` 的 `repository_id` / `caller_file` / `caller_function` / `line_number`；`CrossRepoApiCall` 的 `endpoint` / `call_site` / `match_confidence`。⚠️ `CrossRepoApiCall` **自身没有 `repository` 字段**，按仓过滤只能走 `call_site__repository_id` / `endpoint__repository_id` 反查）
    - `server/tests/services/code_graph/conftest.py`（122-01 交付的 `cross_repo_call_factory`，`endpoint_repository` 传另一个仓即造真跨仓行）
  </read_first>
  <action>
新建 `server/services/code_graph_cross_repo.py`（与 `code_graph_tools.py` 同级，**不进** `services/code_graph/` 包——同 122-05 的位置理由）。三段式中文 docstring，第一段就把 D-25 的事实写清楚：**图里 `kind == "cross_repo"` 的边从来不跨仓**，真跨仓的行永不入图，所以本模块走 ORM 直查；并写明 ⛔ 不许改 `loader.py` 的建边口径。

模块级常量：`DEFAULT_MAX_CROSS_REPO_HOPS: Final[int] = 1`（D-11：改后端 `Endpoint` → 列出前端调用点，正是一跳；多跳会把尚未验证命中率的 `(file_path, name)` 二次解析误差累乘）。三个事件常量 `_EVENT_CROSS_REPO_HOP: Final[str] = "code_graph_cross_repo_hop_completed"` / `_EVENT_CROSS_REPO_REDACTED: Final[str] = "code_graph_cross_repo_peer_redacted"` / `_EVENT_CROSS_REPO_UNAVAILABLE: Final[str] = "code_graph_cross_repo_peer_unavailable"`。

私有 `async def _find_peer_call_sites(*, local_repository_id, symbol_file_path, symbol_name) -> dict[str, _PeerHits]`：
- 查 `CrossRepoApiCall.objects.filter(endpoint__repository_id=local_repository_id, endpoint__file_path=symbol_file_path, endpoint__handler_name=symbol_name).exclude(call_site__repository_id=local_repository_id)`——`.exclude` 那一句是 D-25 的核心（只要**真跨仓**的行；同仓行已经在图里，重复计入会让同一条影响被数两遍）。
- `.values_list("call_site__repository_id", "call_site__caller_file", "call_site__caller_function", "call_site__line_number", "match_confidence")`，按 `call_site__repository_id` 分组进 `_PeerHits`（`@dataclass(slots=True)`：`call_sites: list[dict]` / `max_match_confidence: float`）。
- 每个 call site 记 `{"caller_file","caller_function","line_number","match_confidence"}`，`match_confidence` **原值透传**（D-13：⛔ 不归一化到本仓档位数值——跨仓边的可信度来源与本仓完全不同，混在一起比较是错的）。
- 组内按 `(caller_file, line_number)` 稳定排序。
- ORM 模型走函数体内 lazy import。
  </action>
  <acceptance_criteria>
    - `test_cross_repo_success` 的**前半**落地：用 `cross_repo_call_factory` 造两条真跨仓行（`endpoint_repository` = 本仓，`call_site` 在仓 B）+ 一条同仓行 ⇒ `_find_peer_call_sites` 返回的字典恰有 1 个键（仓 B），组内 2 条，同仓那条**不在**结果里
    - 同一用例断言 `match_confidence` 原值：造 `match_confidence=0.63` ⇒ 结果里逐字为 `0.63`（⛔ 未被折算成 1.0/0.3 之类的档位常量）
    - AST 守护（⛔ 不用 grep —— docstring 里那句「⛔ 不许改 loader 建边口径」的散文含 `loader` 字面量）：`cd server && uv run python -c "import ast; t=ast.parse(open('services/code_graph_cross_repo.py').read()); mods=[a.module for a in ast.walk(t) if isinstance(a,ast.ImportFrom) and a.module]; bad=[m for m in mods if m.split('.')[-1] in {'loader','cache','signature','access'} and m.startswith('services.code_graph')]; assert not bad, bad"` 退出码 0
    - `git diff --name-only` 不含 `server/services/code_graph/loader.py`（D-25 冻结）
  </acceptance_criteria>
  <verify>
    <automated>cd server && GALAXY_CACHE_WARM_ON_STARTUP=False GRAPH_BUILD_ORPHAN_RECONCILE_ON_STARTUP=False uv run pytest tests/services/code_graph/test_cross_repo_hop.py -k cross_repo_success -q --reuse-db</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: 逐仓权限复核与三种显式条目（D-12 / D-14 / D-30）</name>
  <read_first>
    - `server/mcp_tools/views.py:525-542`（`SearchRagChunksView` 的多仓扇出骨架：循环 + 逐仓取权限对象 + 单目标/多目标区别对待。🚨 **本相位必须在这里分叉**——它对失败仓的 `continue` 正是 D-12/D-14 明令禁止的静默丢弃）
    - `server/services/code_graph/access.py:134-180`（`ensure_repository_readable` 的四道判定；「不存在」与「已软删」共用同一句文案与同一个异常类型，不泄漏存在性差异）
    - `server/services/code_graph/cache.py:689-700`（🚨 `ensure_repository_readable` 绝不因缓存命中而跳过——缓存键不带用户维度）
    - `server/services/code_graph/model.py:221-224`（`REDACTED_REPOSITORY = "redacted_repository"`；注释明写「折叠动作本身在 Phase 122 的跨仓 impact 里实现」，全仓**零个使用点**，本 plan 是第一个消费者）
    - `server/services/code_graph_tools.py`（122-05 交付的 `fetch_graph_for_tool` / `graph_error_to_tool_error` / `resolve_symbol_candidates`）
    - `.planning/phases/122-impact-trace/122-RESEARCH.md` §Security Domain 的 T-122-穿仓 与 T-122-折叠泄漏 两行
  </read_first>
  <action>
在 `code_graph_cross_repo.py` 补公开入口：

`async def collect_cross_repo_impact(*, local_repository_id, symbol_file_path, symbol_name, user, max_hops=DEFAULT_MAX_CROSS_REPO_HOPS, max_depth, min_confidence, include_low_confidence=False) -> list[dict]`

- `max_hops <= 0` ⇒ 直接返回 `[]`（不查库、不取图）。`max_hops` 的语义是**一跳，不递归**（D-11）：对端仓的 impact 结果里**不会**再触发第二轮穿仓，这一点写进 docstring 与代码结构（函数体内没有对自身的调用）。
- 调 Task 1 的 `_find_peer_call_sites`，对每个对端仓依次：
  ① 用 `resolve_symbol_candidates`（122-05）在对端仓按 `(caller_file, caller_function)` 定位 `Symbol` —— 与 loader 同口径的二次解析。命中不上就**计数不丢**（`unresolved_call_sites` 计数进条目），⛔ 别造虚拟节点（Phase 121 D-05）。
  ② 调 `fetch_graph_for_tool(peer_repo_id, "", user=user, seed_symbol_ids=[...], depth=max_depth, include_low_confidence=...)`——**这一步就是 D-12 的权限复核点**：`get_graph` 每次调用都跑 `ensure_repository_readable`。⛔ 不要为对端仓另查一次 `Repository`（那会绕过 `GraphAccessDenied` 的统一出口并制造存在性预言机）。
  ③ 在对端图上对每个解析到的种子调 `analyze_impact(...)`，把结果合并进该仓条目。

三种**显式**条目形态（⛔ 三种都不许静默丢弃——静默丢弃会让 agent 以为影响面更小）：
- **成功**：`{"cross_repo": True, "repository_id": <peer id>, "match_confidence": <该仓最高原值>, "call_sites": [...], "unresolved_call_sites": int, "impact": <analyze_impact 的 groups/summary>}`。
- **无权限**（`GraphAccessDenied`）：`{"cross_repo": True, "repository": REDACTED_REPOSITORY}` —— 🚨 **只有这两个键**。⛔ 不带 `repository_id`、⛔ 不带 `affected_count`、⛔ 不带仓名/文件路径/符号名/调用点数（D-30 + T-122-折叠泄漏：计数是存在性预言机，会泄漏一个调用方**无权访问**的仓库的内部规模）。折叠条目携带的信息止于「这里有一个你无权看的仓库」。这条裁决的理由必须写进该分支的注释。
- **不可用**（`GraphNotIndexed` / `GraphBuildTimeout` / `GraphBuildFailed` / 其他 `GraphError`）：`{"cross_repo": True, "repository_id": <peer id>, "unavailable_reason": <graph_error_to_tool_error(exc)[0]>}`（D-14 fail-soft：该仓折叠为一条带原因的条目，**本仓结果照常返回**）。⚠️ 此分支保留 `repository_id` 是安全的——`ensure_repository_readable` 已先跑过，能走到这里说明调用方对该仓有读权限，只是它没索引好。这条差异写进注释，防后人「统一一下」把它也抹掉。

三个埋点各自独立函数，DEBUG/INFO + `component="code_graph"` + `category="sampling"`，异常文本一律过 `redact_secrets_in_text(str(exc))[:500]`。🚨 `_log_cross_repo_peer_redacted` **不得**记 `repository_id`（那正是折叠要藏的东西）——只记 `local_repository_id` 与 `redacted_count`。

条目排序：成功条目在前（按 `repository_id`），其后 unavailable（按 `repository_id`），最后 redacted（无标识符可排，按出现顺序）。

🚨 **同批把本模块挂进 AST 观测契约**（D-04 的扫描面在 122-05 Task 4 已被扩展为「包内 glob + 显式兄弟清单」）：在 `server/tests/services/code_graph/test_access.py` 的 `_SIBLING_GUARDED_MODULES` 元组里加一项 `"code_graph_cross_repo.py"`。⛔ 只加这一项，不动该用例的任何判据。规矩是「谁新建包外兄弟模块，谁在同一个 plan 里把它加进清单」——本模块的三个埋点要把异常文本写进日志（`redact_secrets_in_text(str(exc))[:500]`），不挂进清单就**没有任何机制**强制那条脱敏，而这正是本相位泄漏面最集中的模块（T-122-穿仓）。
  </action>
  <acceptance_criteria>
    - `test_unauthorized_repo_redacted` 落地：patch `ensure_repository_readable` 对对端仓抛 `GraphAccessDenied` ⇒ 返回列表含恰一条折叠条目，`set(entry.keys()) == {"cross_repo", "repository"}`、`entry["repository"] == REDACTED_REPOSITORY`；**且**断言 `json.dumps(result, ensure_ascii=False)` 里**不出现**对端仓的 `repository_id` 字符串、仓名、`caller_file` 路径、`caller_function` 名（逐个 `assert x not in dumped`）
    - `test_peer_unavailable_fail_soft` 落地：对端仓抛 `GraphNotIndexed` ⇒ 条目为 `{"cross_repo": True, "repository_id": ..., "unavailable_reason": "repository_not_indexed"}`；同一次调用里另一个**正常**的对端仓仍返回成功条目（证明 fail-soft 不是全盘失败）；再用 `GraphBuildTimeout` 跑一遍，`unavailable_reason == "graph_build_timeout"`
    - `test_hop_budget` 落地：`max_hops=0` ⇒ 返回 `[]` 且 spy 断言 `fetch_graph_for_tool` **零调用**、`CrossRepoApiCall` 零查询；`max_hops=1` 且对端仓自身也有指向第三个仓的 `CrossRepoApiCall` 行 ⇒ 结果里**不含**第三个仓（不递归）
    - `test_cross_repo_success` 的后半：成功条目含 `cross_repo is True`、`match_confidence` 为原值、`impact` 段的结构与 `analyze_impact` 一致
    - `cd server && uv run python -c "import ast, inspect, services.code_graph_cross_repo as m; src=inspect.getsource(m.collect_cross_repo_impact); t=ast.parse(src.lstrip()); assert not [n for n in ast.walk(t) if isinstance(n,ast.Call) and isinstance(n.func,ast.Name) and n.func.id=='collect_cross_repo_impact'], '不递归'"` 退出码 0
    - **本模块已挂进观测契约清单**：`cd server && uv run python -c "import tests.services.code_graph.test_access as t; assert 'code_graph_cross_repo.py' in t._SIBLING_GUARDED_MODULES, t._SIBLING_GUARDED_MODULES"` 退出码 0
    - **契约对本模块实际生效**：`cd server && GALAXY_CACHE_WARM_ON_STARTUP=False GRAPH_BUILD_ORPHAN_RECONCILE_ON_STARTUP=False uv run pytest tests/services/code_graph/test_access.py -k "observability or upper_layer" -q --reuse-db` 通过（三个埋点的事件名前缀、`component`、`category` 与 `error=` 脱敏四条判据全部满足；不满足就改**埋点**，⛔ 不许改判据、⛔ 不许把本文件从清单里摘掉）
  </acceptance_criteria>
  <verify>
    <automated>cd server && GALAXY_CACHE_WARM_ON_STARTUP=False GRAPH_BUILD_ORPHAN_RECONCILE_ON_STARTUP=False uv run pytest tests/services/code_graph/test_cross_repo_hop.py tests/services/code_graph/test_access.py -k "cross_repo_success or unauthorized or fail_soft or hop_budget or observability or upper_layer" -q --reuse-db</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: 反向守护——图内 cross_repo 边不是跨仓结果（Pitfall 1）</name>
  <read_first>
    - `.planning/phases/122-impact-trace/122-RESEARCH.md` Pitfall 1 §How to avoid 第 3 条（「写一条回归断言：图里任何 `kind == "cross_repo"` 的边，两端节点必须都在本仓图内，即不要在这些边上做 `cross_repo: true` 标记」；Warning sign：impact 输出里 `cross_repo: true` 的条目其 `repository_id` 与查询仓相同）
    - `server/services/code_graph/impact.py`（122-03 交付；输出顶层的 `cross_repo` 字段位由壳层填充，内核恒为 `[]`）
    - `server/tests/services/code_graph/conftest.py`（`known_topology` 含 `F --cross_repo(match_confidence=0.7)--> A` 一条**同仓**的 cross_repo 边）
  </read_first>
  <action>
在 `tests/services/code_graph/test_impact.py` 里把 122-01 留下的 `test_graph_cross_repo_edges_are_intra_repo` 桩转成真实断言（零 DB，用 `known_topology`）。三条断言：

① `known_topology` 里每一条 `kind == "cross_repo"` 的边，其两端节点都在同一张图内（`u in g and v in g`）——把 Phase 121 的建边口径固化成本相位的前提：这些边是「本仓前端调本仓后端」，不是跨仓。
② `analyze_impact(g, "A", min_confidence=0.7)` 的结果里，F 那一条 item 的 `via["confidence"] == "cross_repo"`、`path_confidence == 0.7`（档位与原值都正确透出），但该 item **没有** `cross_repo` 键、也没有任何值为 `True` 的 `cross_repo` 标记——遍历整个结果 `assert not any(item.get("cross_repo") for group in groups for item in group["items"])`。
③ `analyze_impact` 返回的顶层 `result["cross_repo"] == []`——内核恒不填这个字段位，它只能由 `collect_cross_repo_impact`（本 plan Task 2）填。

用例 docstring 写清这条守护防的是什么：`EdgeKind.CROSS_REPO` 这个名字暗示它跨仓，但 `_load_cross_repo_edges` 要求两端都在本仓；一旦有人把这些边当跨仓边处理，输出的「跨仓影响面」实际上全在本仓，agent 会据此得出完全错误的结论。
  </action>
  <acceptance_criteria>
    - `cd server && uv run pytest tests/services/code_graph/test_impact.py -q` 结果为 `10 passed`（零 skip、**零 DB**；文件共 10 个节点：122-01 的 `test_known_topology_fixture_is_frozen` + 122-03 的 8 个 + 本 task 的 1 个）
    - 用例内含上述三条断言，且第 ② 条用**遍历全部结果**的形式（⛔ 不是只检查 F 那一条——防的是任何 item 被误标）
    - `cd server && uv run pytest tests/services/code_graph -q --reuse-db` 的 skip 计数相对 122-05 收尾下降，且零新增失败
  </acceptance_criteria>
  <verify>
    <automated>cd server && uv run pytest tests/services/code_graph/test_impact.py -q</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 本仓 → 对端仓 | 本相位最集中的信息泄漏面：低权用户以有权仓为起点，可能读到从未授权仓库的符号名/文件路径/行号 |
| 折叠条目 → agent | 折叠条目本身携带的任何计数都是存在性预言机 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-122-穿仓 | **Information Disclosure** | `collect_cross_repo_impact` | mitigate | D-12：对端仓一律经 `fetch_graph_for_tool` → `get_graph(peer_id, user=user)`，`ensure_repository_readable` 每次都跑；⛔ 不为对端仓另查 `Repository`。回归断言：`test_unauthorized_repo_redacted` 显式断言响应体里不出现对端仓的任何标识符 |
| T-122-折叠泄漏 | Information Disclosure | 无权限分支的条目形态 | mitigate | D-30：折叠条目**只有** `{"cross_repo": True, "repository": REDACTED_REPOSITORY}` 两个键——⛔ 无 `repository_id`、⛔ 无 `affected_count`、⛔ 无桶化计数。裁决理由（安全优先于便利）写进该分支注释与模块 docstring |
| T-122-绕闸 | Elevation of Privilege | 模块 import 面 | mitigate | AST 断言禁止 `services.code_graph.{loader,cache,signature,access}`；取图唯一去处是 122-05 的 `fetch_graph_for_tool` |
| T-122-空图误导 | Tampering（结论被污染） | 对端仓失败分支 | mitigate | D-14：`GraphNotIndexed`/`Timeout` 产出带 `unavailable_reason` 的**显式**条目，⛔ 不静默 `continue`（这正是 `SearchRagChunksView` 的做法，本相位必须在此分叉） |
| T-122-遍历 DoS | Denial of Service | 多仓扇出 | mitigate | `max_hops=1` 不递归（AST 断言无自调用）；每个对端仓的 `analyze_impact` 各自受 `max_depth`/`max_nodes`/`limit` 三重预算约束 |
| T-122-日志放大 | Denial of Service | 三个 `_log_cross_repo_*` | mitigate | 每个对端仓至多一条 `sampling` 事件；⛔ 逐 call site 不打日志。`_log_cross_repo_peer_redacted` **不记** `repository_id` |
| T-122-错误细节泄漏 | Information Disclosure | `unavailable_reason` | mitigate | 只取 `graph_error_to_tool_error(exc)[0]` 的错误码常量，⛔ 不透 `exc.details`；日志里的异常文本过 `redact_secrets_in_text(...)[:500]` |
| T-122-SC | Tampering | npm/pip/cargo installs | accept | 零新增依赖（`122-RESEARCH.md` §Package Legitimacy Audit 表为空） |
</threat_model>

<verification>
- `cd server && GALAXY_CACHE_WARM_ON_STARTUP=False GRAPH_BUILD_ORPHAN_RECONCILE_ON_STARTUP=False uv run pytest tests/services/code_graph -q --reuse-db` —— `test_cross_repo_hop.py` 零 skip，`test_impact.py` 10 passed
- `cd server && uv run ruff check services/ && uv run mypy services/code_graph_cross_repo.py` 零错误
- `git diff --name-only` **不含** `server/services/code_graph/loader.py`（D-25）、`server/codegraph/services/repo_router_v2.py`、`mcp/`
- `cd server && uv run python manage.py makemigrations --check --dry-run` 退出码 0

## 必须写进 SUMMARY 的如实声明（D-26）

1. 生产库 `CrossRepoApiCall` / `ApiCallSite` / `ApiWrapper` **均为 0 行**（`Endpoint` 6,014 行），上游产出器依赖 volar LSP 而 server 镜像无 Node（LSP-01 / Phase 127）。
2. 本 plan 的四条分支**全部由合成数据覆盖**；⛔ 不得表述成「跨仓 impact 已上线可用」或「跨仓能力已验证」。
3. `(file_path, name)` 二次解析的真实命中率在 Phase 127 之前**根本不可测**（121-10 记的「样本不足」实为**样本为零**，本 plan 更正之）。
4. ROADMAP 需记一条「Phase 127 补齐 LSP 后回来用真实样本复验 IMPACT-03」——该动作由 122-10 执行。
</verification>

<success_criteria>
- 四条分支（解析成功 / 无权限折叠 / 对端不可用 fail-soft / 跳数上限）各有独立用例且全绿
- 折叠条目的键集合恰为 `{"cross_repo", "repository"}`，序列化后不含对端仓的任何标识符
- 一个对端仓失败不影响另一个对端仓与本仓的结果
- `max_hops=1` 不递归（AST 断言 + 三仓链路用例双重证明）
- 图内 `cross_repo` 边在任何路径下都不会被标成跨仓结果
</success_criteria>

## Artifacts this phase produces

| 类型 | 名称 | 位置 |
|---|---|---|
| 模块（新建） | `services/code_graph_cross_repo.py` | `server/services/code_graph_cross_repo.py` |
| 常量 | `DEFAULT_MAX_CROSS_REPO_HOPS: Final[int] = 1` | 同上 |
| 事件名常量 | `_EVENT_CROSS_REPO_HOP = "code_graph_cross_repo_hop_completed"` / `_EVENT_CROSS_REPO_REDACTED = "code_graph_cross_repo_peer_redacted"` / `_EVENT_CROSS_REPO_UNAVAILABLE = "code_graph_cross_repo_peer_unavailable"` | 同上 |
| dataclass（私有） | `_PeerHits`（call_sites / max_match_confidence） | 同上 |
| 函数（async） | `collect_cross_repo_impact(*, local_repository_id, symbol_file_path, symbol_name, user, max_hops, max_depth, min_confidence, include_low_confidence)` | 同上 |
| 私有函数 | `_find_peer_call_sites` / `_log_cross_repo_hop` / `_log_cross_repo_peer_redacted` / `_log_cross_repo_peer_unavailable` | 同上 |
| 输出条目形态 | 成功：`cross_repo` / `repository_id` / `match_confidence` / `call_sites` / `unresolved_call_sites` / `impact` | `collect_cross_repo_impact` 返回项 |
| 输出条目形态 | 折叠：`cross_repo` / `repository`（= `REDACTED_REPOSITORY`），**仅此两键** | 同上 |
| 输出条目形态 | 不可用：`cross_repo` / `repository_id` / `unavailable_reason` | 同上 |
| 用例（落地） | `test_cross_repo_success` / `test_unauthorized_repo_redacted` / `test_peer_unavailable_fail_soft` / `test_hop_budget` / `test_graph_cross_repo_edges_are_intra_repo` | `server/tests/services/code_graph/` |
| 守护清单（修改） | `_SIBLING_GUARDED_MODULES` 增 `"code_graph_cross_repo.py"`（122-05 Task 4 建的清单） | `server/tests/services/code_graph/test_access.py` |

<output>
Create `.planning/phases/122-impact-trace/122-06-SUMMARY.md` when done
</output>
