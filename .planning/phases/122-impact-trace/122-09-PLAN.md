---
phase: 122-impact-trace
plan: 09
type: execute
wave: 6
depends_on: ["122-08"]
files_modified:
  - server/agents/tools/schemas/graph_tools.py
  - server/agents/tools/graph_tools.py
  - server/agents/tools/__init__.py
  - server/agents/chat_runner.py
  - server/tests/agents/tools/test_graph_tools.py
autonomous: true
requirements: [IMPACT-06]

must_haves:
  truths:
    - "两个 @tool 已注册进 _tool_registry 且挂进 chat_runner._INDEXED_TOOL_NAMES 白名单（注册 ≠ 暴露）"
    - "解析不到会话 owner 时拒绝执行，⛔ 绝不带 user=None 往下走（get_graph 的系统路径不会拒）"
    - "对话面的 data 段与 MCP 面同源同形：原样透出 run_impact/run_trace 的信封，壳内零加工"
    - "GraphError 翻译成 ToolResult(success=False) 的映射文案，⛔ 不吞成空影响面"
    - "每次调用一条 caller 类事件（绑定会话 owner）+ 一条汇总 RetrievalTrace（conversation_id 关联）"
  artifacts:
    - path: "server/agents/tools/graph_tools.py"
      provides: "impact_analysis / trace_call_path 两个对话工具壳（fail-closed + 留痕 + 异常翻译）"
      exports: ["impact_analysis", "trace_call_path"]
      min_lines: 200
    - path: "server/agents/tools/schemas/graph_tools.py"
      provides: "两个工具的 pydantic 输入契约（strict + extra=forbid + frozen）"
      exports: ["ImpactAnalysisToolInput", "TraceCallPathToolInput"]
    - path: "server/agents/chat_runner.py"
      provides: "_INDEXED_TOOL_NAMES 白名单增两项"
  key_links:
    - from: "server/agents/tools/graph_tools.py"
      to: "services.code_graph_tools.run_impact"
      via: "与 MCP 壳同一个编排入口（D-21：逻辑不许在壳里分叉）"
      pattern: "run_impact"
    - from: "server/agents/tools/graph_tools.py"
      to: "agents.tools.delivery_knowledge_tools._resolve_conversation_user"
      via: "对话面唯一的用户来源；返回 None 即 fail-closed"
      pattern: "_resolve_conversation_user"
---

<objective>
交付 IMPACT-06 的**对话面**：两个 `@tool` 薄壳，与 122-08 的 MCP 壳共用 122-07 的 `run_impact` / `run_trace`，并把它们挂进 chat 白名单。

Purpose: 「注册 ≠ 暴露」是本仓有现场记录的债——`chat_runner.py:92-100` 的注释写着「这些工具早已在 `agents/tools/__init__.py` 注册，此前漏挂进 chat 白名单导致 LLM 全程只能 RAG 搜索、无法利用 graph 能力」。本 plan 一次把两处都补齐。
🚨 同时这里有一条 `122-PATTERNS.md` 点名的**硬要求**：对话面拿不到会话 owner 时必须自己挡住。`get_graph(user=None)` 会走系统路径（`access._initiated_by` 返回 `"system"`，ACL 扩展点空实现放行），**不会**被拒——漏了这道闸，任何能触发对话的路径都能读到全库的图。
Output: 2 个 pydantic 输入契约 + 2 个 `@tool` + 两处注册 + 2 个用例落地。
</objective>

<execution_context>
@.cursor/gsd-core/workflows/execute-plan.md
@.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/122-impact-trace/122-CONTEXT.md
@.planning/phases/122-impact-trace/122-PATTERNS.md
@.cursor/rules/observability-logging.mdc
@server/services/code_graph_tools.py
@server/mcp_tools/views.py
@server/agents/tools/find_api_callers.py
@server/agents/tools/knowledge_read_tools.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: schemas/graph_tools.py 与两个 @tool 壳（fail-closed / 同源信封 / 留痕）</name>
  <read_first>
    - `server/agents/tools/find_api_callers.py`（全文 213 行 —— 与本相位主题最近的对话工具。四段照抄：模块 docstring 里的「**注册路径**：通过 `agents/tools/__init__.py` 顶层 import 触发 `@tool` 注册」；`_TOOL_DESCRIPTION` 带 USE WHEN / DO NOT USE 决策树；`_TOOL_PARAMETERS` 手写 JSON Schema；装饰函数只做日志与异常翻译、实现放 `_impl` 的双层防御。⚠️ 它两处 `logger.warning(... error=str(exc))` **没有**脱敏——`.cursor/rules/observability-logging.mdc` 要求脱敏，本 plan ⛔ 不照抄这处疏漏）
    - `server/agents/tools/knowledge_read_tools.py`（全文 342 行 —— 本仓**对话面观测的完整范式**，逐条照抄：`_COMPONENT = "agents.tools"`；`_record_chat_retrieval()` 用 `arecord_retrieval_trace(None, kind=…, payload=…, user_id=…, conversation_id=…, source="chat")` 且 `except Exception: pass`；收尾 `logger.info(<事件名>, conversation_id=…, duration_ms=…, component=_COMPONENT, category="caller")`；异常先 `redact_secrets_in_text(str(exc))` 再进日志与 `ToolResult.error`；LLM 直出参数一律钳上下界）
    - `server/agents/tools/delivery_knowledge_tools.py:56-65, 116-118`（`_resolve_conversation_user` 与它的 fail-closed 调用点：`if user is None: return ToolResult(success=False, error="无法解析会话 owner，拒绝检索（fail-closed）")`）
    - `server/agents/tools/project_read_tools.py:101-106`（`_CONV_ID_PARAM` —— `conversation_id` 的 JSON Schema 片段，由 chat_runner 注入）
    - `server/agents/tools/schemas/api_tools.py:88-137`（pydantic 输入契约形态：`ConfigDict(strict=True, extra="forbid", frozen=True)` 三重防漂移 + 每个字段带中文 `description`；⛔ 不 import Django ORM）
    - `server/agents/tools/base.py:134-160`（`@tool` 契约：必须 `async def`、重名注册直接 `ValueError`、`category` 取 `ToolCategory` 枚举）
    - `server/mcp_tools/views.py` 的 `ImpactAnalysisView`（122-08 交付 —— 本 task 的**同源对照物**：调同一个 `run_impact`、同一个 `resolve_tool_graph_branch`、同一个 `tool_trace_payload`、同一套 `ok`/`error_code`/`error` 失败语义）
    - `server/services/code_graph_tools.py`（`run_impact` / `run_trace` / `graph_error_to_tool_error` / `resolve_tool_graph_branch` / `tool_trace_payload`）
  </read_first>
  <action>
新建 `server/agents/tools/schemas/graph_tools.py`：两个 pydantic 输入契约 `ImpactAnalysisToolInput` / `TraceCallPathToolInput`，`ConfigDict(strict=True, extra="forbid", frozen=True)`，字段与**上下界**与 122-08 的两个 DRF Serializer **逐条对齐**（`max_depth` 1–3、`min_confidence` 0.0–1.0、`limit` 1–200、`max_cross_repo_hops` 0–1、`alt_path_cap` 1–50），每个字段带中文 `description`。⛔ 零 Django import。契约文件的模块 docstring 写明：**上下界必须与 MCP Serializer 同表**——两面参数域一旦分叉，同一个查询在两面就会得到不同结果，D-21 的「同源」就断在这一层。

新建 `server/agents/tools/graph_tools.py`，模块 docstring 三段式，第一段写明注册路径（`agents/tools/__init__.py` 顶层 import 触发 `@tool` 注册）+ 必须同时挂 `chat_runner._INDEXED_TOOL_NAMES`（注册 ≠ 暴露，`chat_runner.py:92-100` 的注释是这笔债的现场记录）。模块级：`logger = structlog.get_logger(__name__)`、`_COMPONENT = "agents.tools"`。

① `_DESC_IMPACT` / `_DESC_TRACE` 两个模块级描述常量，带 USE WHEN / DO NOT USE 决策树（照 `find_api_callers` 的形状）：impact 回答「改这个符号会影响谁」，trace 回答「A 到 B 怎么走的」；DO NOT USE：找 handler 用 `find_api_handler`、模糊语义检索用 `search_repository_code`、chunk 级关系游走用 `find_related_code`。描述里必须写清两件 agent 必须知道的事——**符号重名时工具会返回候选列表而不是猜一个**（生产 19.3% 的名字不唯一，这是主路径），拿到候选后应带 `symbol_id` 重试；以及**结果带 `staleness` 与 `graph` 两段声明**，读结论前先看它们。

② `_PARAMS_IMPACT` / `_PARAMS_TRACE` 两个手写 JSON Schema dict，字段与 ① 的 pydantic 契约同表，末尾并入 `_CONV_ID_PARAM`（`from agents.tools.project_read_tools import _CONV_ID_PARAM`），`required` 含 `repository_id` 与 `conversation_id`。

③ 私有 `async def _resolve_tool_repo(repository_id: str)`：函数体内 lazy import `Repository` / `IndexStatus`，`aget(id=…, is_deleted=False)`；不存在或 `index_status != INDEXED` 返回 `(None, <错误码>)`。docstring 必须写清这次 ORM 取仓**不是权限校验**——它只为拿 `staleness_payload` 需要的 `Repository` 对象与「未索引」这条早退；真正的授权是 `run_impact` 内部 `get_graph` 每次都跑的 `ensure_repository_readable(user=…)`（⛔ 不在壳里自查可见性，PATTERNS §权限：唯一收口点）。

④ 两个 `@tool`（`name="impact_analysis"` / `name="trace_call_path"`，`category=ToolCategory.PROJECT.value`，与 `find_api_callers` / `list_endpoints` 同档）。🚨 工具名与 MCP 面**逐字相同**——同一个能力在两面用两个名字，是让「双面同源」这件事在文档、skills 与人的脑子里最先断掉的地方。

每个工具的函数体（装饰函数只做参数收敛与异常翻译，主体放 `_impl`，双层防御照 `find_api_callers:71-118`）：
  1. `started = perf_counter()`。
  2. 🚨 **fail-closed 第一步**：`user = await _resolve_conversation_user(conversation_id)`；`if user is None: return ToolResult(success=False, error="无法解析会话 owner，拒绝分析（fail-closed）")`，并在此**之前不做任何取仓/取图动作**。这条注释必须写进代码：`get_graph(user=None)` 走的是系统路径（`access._initiated_by` 返回 `"system"`、`_check_user_acl` 空实现放行），**不会**被拒，所以这道闸是对话面唯一的权限起点，不是防御性冗余。
  3. pydantic 契约校验（`except ValidationError` → `ToolResult(success=False, error=str(exc))`）。
  4. `repo, err_code = await _resolve_tool_repo(...)`；`repo is None` ⇒ `ToolResult(success=False, error=<映射文案>)`（未索引仓 ⛔ 绝不降级成空影响面）。
  5. `graph_branch = await resolve_tool_graph_branch(repository_id, repo, branch)`（🚨 与 MCP 壳**同一个函数**，⛔ 不在这里重写那三行分支比较）。
  6. `orch_started = perf_counter()`；`result = await run_impact(...)` / `run_trace(...)`，`user=user` 显式透传；`except GraphError as exc:` ⇒ `code, message = graph_error_to_tool_error(exc)`；`logger.warning("impact_analysis_tool_failed", error_code=code, error=redact_secrets_in_text(str(exc))[:500], error_type=type(exc).__name__, component=_COMPONENT, category="caller", initiated_by_user_id=str(user.id))`；`return ToolResult(success=False, error=message)`。🚨 只出映射文案，⛔ 不把 `str(exc)` / `exc.details` 交给 LLM（T-122-错误细节泄漏），也⛔ 不 catch 成空结果（D-03）。
  7. **输出信封**：`ToolResult(success=True, output={"data": result, "metadata": {...}})`——`data` 是 `run_impact` 的返回值**原样**，⛔ 一个键都不改、不重排、不补充（D-21：MCP 面是 `{**result, "run_id": …}`，两面的 `data` 段因此逐字节相同；122-10 的 `test_two_surfaces_same_payload` 守这条）。`metadata` 只放对话面自有的东西：`{"repository_id", "branch", "conversation_id", "duration_ms"}`，⛔ 不把任何结果内容搬进来。
  8. **失败语义对齐**（🚨 本相位在此明确分叉于 `search_delivery_knowledge` 的半先例：同一个故障，一面报「没有结果」、一面报「工具坏了」）：`result["ok"] is False`（`symbol_not_found` / `ambiguous_symbol` / `symbol_not_in_graph`）仍返回 **`ToolResult(success=True, output={"data": result, …})`**——工具调用本身成功了，信封里的 `ok=False` 才是查询结论。`success=False` **只**留给三类：会话 owner 解析失败、输入校验失败、翻译后的 `GraphError`。这三类与 MCP 面的 401/400/4xx-5xx 一一对应。这段对应关系写进两个工具的 docstring。同理 `trace_call_path` 的 `found is False` 是**成功结果**（D-20），⛔ 不映射成 `success=False`。
  9. **留痕与观测**（`.cursor/rules/observability-logging.mdc`）：`duration_ms`；`await _record_chat_retrieval(RetrievalTrace.Kind.EDGE, tool_trace_payload(result, tool="impact_analysis", duration_ms=…, orchestration_ms=…), conversation_id=conversation_id, user=user)`——🚨 复用 122-08 的 `tool_trace_payload`，两面留痕 payload 同形；helper 本身照抄 `knowledge_read_tools._record_chat_retrieval`（`arecord_retrieval_trace(None, …, conversation_id=…, source="chat")` + `except Exception: pass`，⛔ 留痕失败绝不反噬对话）。再发一条 `caller` 事件 `logger.info("impact_analysis_tool_done", conversation_id=…, repository_id=…, ok=…, error_code=…, result_count=…, duration_ms=…, component=_COMPONENT, category="caller", initiated_by_user_id=str(user.id))`——⛔ 不记符号名与文件路径。

`__all__ = ["impact_analysis", "trace_call_path"]`。
  </action>
  <acceptance_criteria>
    - `cd server && uv run python -c "import django,os; os.environ.setdefault('DJANGO_SETTINGS_MODULE','friday.settings'); django.setup(); import inspect; from agents.tools.graph_tools import impact_analysis, trace_call_path; assert inspect.iscoroutinefunction(impact_analysis) and inspect.iscoroutinefunction(trace_call_path)"` 退出码 0
    - 参数域两面同表（防 D-21 在参数层分叉）：`cd server && uv run python -c "import django,os; os.environ.setdefault('DJANGO_SETTINGS_MODULE','friday.settings'); django.setup(); from mcp_tools.serializers import ImpactAnalysisRequestSerializer as S; from agents.tools.schemas.graph_tools import ImpactAnalysisToolInput as M; ser=set(S().fields); pyd=set(M.model_fields); assert ser <= pyd | {'branch'}, ser - pyd; assert (pyd - {'conversation_id'}) <= ser, pyd - ser"` 退出码 0
    - fail-closed 顺序（AST，⛔ 不用 grep —— docstring 里写着 `get_graph(user=None)` 这类散文会命中字面 grep）：`cd server && uv run python -c "import django,os; os.environ.setdefault('DJANGO_SETTINGS_MODULE','friday.settings'); django.setup(); import ast, inspect, agents.tools.graph_tools as m; src=inspect.getsource(m); t=ast.parse(src); fn=[n for n in ast.walk(t) if isinstance(n,ast.AsyncFunctionDef) and n.name.endswith('_impl')]; assert fn; import itertools; names=[[c.func.id for c in ast.walk(f) if isinstance(c,ast.Call) and isinstance(c.func,ast.Name)] for f in fn]; assert all('_resolve_conversation_user' in ns for ns in names); assert all(ns.index('_resolve_conversation_user') < min([ns.index(x) for x in ('run_impact','run_trace','_resolve_tool_repo') if x in ns] or [10**6]) for ns in names)"` 退出码 0
    - 壳内零算法（AST）：`cd server && uv run python -c "import django,os; os.environ.setdefault('DJANGO_SETTINGS_MODULE','friday.settings'); django.setup(); import ast, inspect, agents.tools.graph_tools as m; t=ast.parse(inspect.getsource(m)); calls={n.func.id for n in ast.walk(t) if isinstance(n,ast.Call) and isinstance(n.func,ast.Name)}; assert {'run_impact','run_trace'} <= calls; assert not ({'analyze_impact','trace_path','grade_risk','fetch_graph_for_tool','collect_cross_repo_impact','resolve_symbol_candidates'} & calls), calls"` 退出码 0
    - 信封零加工（AST）：`data` 键的取值必须是 `run_impact` / `run_trace` 结果那个名字本身（`ast.Name`），⛔ 不是就地构造的 dict —— `cd server && uv run python -c "import django,os; os.environ.setdefault('DJANGO_SETTINGS_MODULE','friday.settings'); django.setup(); import ast, inspect, agents.tools.graph_tools as m; t=ast.parse(inspect.getsource(m)); ds=[d for d in ast.walk(t) if isinstance(d,ast.Dict) and any(isinstance(k,ast.Constant) and k.value=='data' for k in d.keys if k is not None)]; assert ds; vals=[v for d in ds for k,v in zip(d.keys,d.values) if isinstance(k,ast.Constant) and k.value=='data']; assert all(isinstance(v,ast.Name) for v in vals), [ast.unparse(v)[:60] for v in vals]"` 退出码 0
    - 异常文本脱敏（AST，与包内契约同判据）：`cd server && uv run python -c "import django,os; os.environ.setdefault('DJANGO_SETTINGS_MODULE','friday.settings'); django.setup(); import ast, inspect, agents.tools.graph_tools as m; t=ast.parse(inspect.getsource(m)); bad=[ast.unparse(kw.value)[:60] for n in ast.walk(t) if isinstance(n,ast.Call) and isinstance(n.func,ast.Attribute) and getattr(n.func.value,'id',None)=='logger' for kw in n.keywords if kw.arg=='error' and 'redact_secrets_in_text' not in ast.unparse(kw.value)]; assert not bad, bad"` 退出码 0
    - `cd server && uv run ruff check agents/tools/graph_tools.py agents/tools/schemas/graph_tools.py` 通过
  </acceptance_criteria>
  <verify>
    <automated>cd server && GALAXY_CACHE_WARM_ON_STARTUP=False GRAPH_BUILD_ORPHAN_RECONCILE_ON_STARTUP=False uv run pytest tests/agents/tools -q --reuse-db</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: 两处注册（tools/__init__ 与 chat 白名单）+ 注册与 fail-closed 用例</name>
  <read_first>
    - `server/agents/tools/__init__.py:31-33, 69-78`（顶层 import 触发注册 + `__all__` 分组注释；`find_related_code` / `find_api_handler` / `find_api_callers` / `list_endpoints` 就在「Code graph tools」与「API graph tools」两组里）
    - `server/agents/chat_runner.py:86-125`（`_INDEXED_TOOL_NAMES` —— :92-100 的注释是「漏挂白名单」这笔债的现场记录；`find_related_code` / `list_endpoints` / `find_api_handler` / `find_api_callers` 四个是同簇工具，新工具紧挨着它们加）
    - `server/tests/agents/tools/test_knowledge_read_tools.py:39-48`（`test_tools_registered_and_whitelisted` 的断言形态：从 `agents.chat_runner` 取白名单常量、从 `agents.tools.base` 取 `_tool_registry`）
    - `server/agents/tools/delivery_knowledge_tools.py:56-65`（`_resolve_conversation_user` 的两条失败路径：`conversation_id` 为空串直接 `None`；`Conversation.DoesNotExist` / `ValueError` 也 `None`）
    - `.planning/phases/122-impact-trace/122-RESEARCH.md` §Validation Architecture 第 842 行（`test_registered_and_whitelisted` 的权威 Behavior 与命令）
  </read_first>
  <action>
① `server/agents/tools/__init__.py`：顶层加 `from agents.tools.graph_tools import impact_analysis, trace_call_path`（import 块按既有字母序插入，位置在 `find_related_code` 与 `langchain_adapter` 之间）；`__all__` 里在「Code graph tools」分组下加两项，并补一行注释说明它们是 Phase 122 的内存图工具面、与 `find_related_code` 的 chunk 级游走互补（前者吃符号出深度分组与最短路，后者吃 chunk 出邻居）。

② `server/agents/chat_runner.py`：在 `_INDEXED_TOOL_NAMES` 里 `find_api_callers` 之后加 `"impact_analysis"` 与 `"trace_call_path"`，并补一行注释写清它们与上面四个 graph 工具的分工，以及「本对话面靠 `conversation_id` 解析会话 owner 做 fail-closed」。⛔ 不动 `_PROJECT_READ_TOOL_NAMES` 与 `_DEEP_ANALYSIS_TOOL_NAMES`（后者由 `_INDEXED_TOOL_NAMES` 派生，自动继承）。

③ 把 `server/tests/agents/tools/test_graph_tools.py` 里 122-01 留下的两个桩转成真实断言（去掉 `@pytest.mark.skip`）：

- `test_registered_and_whitelisted`（零 mock，形态照 `test_knowledge_read_tools.py:39-48`）：从 `agents.tools.base` 取 `_tool_registry`、从 `agents.chat_runner` 取 `_INDEXED_TOOL_NAMES`，断言 `{"impact_analysis", "trace_call_path"} <= set(_tool_registry)` **且** `<= set(_INDEXED_TOOL_NAMES)`。🚨 两条都要——只断注册不断白名单，正是 `chat_runner.py:95-96` 记的那笔债的复现形态。再断言两个工具的 `ToolDefinition.parameters["properties"]` 含 `conversation_id`（chat_runner 靠它注入，schema 里不声明就注不进来）。
- `test_conversation_owner_required_fail_closed`：三种取不到 owner 的输入各跑一次——`conversation_id=""`、一个格式非法的字符串、一个格式合法但库里不存在的 UUID——每次断言 `result.success is False`、`"fail-closed" in result.error`；**并 spy `services.code_graph_tools.run_impact` 与 `_resolve_tool_repo`，断言两者 `call_count == 0`**（🚨 这才是这条用例的实质：不是「返回了错误」，而是**根本没往下走**。`get_graph(user=None)` 会走系统路径不会拒，只要有一次漏挡就等于开了一个免鉴权的全库图查询入口）。同一用例对 `trace_call_path` 再跑一遍。
  </action>
  <acceptance_criteria>
    - `cd server && GALAXY_CACHE_WARM_ON_STARTUP=False GRAPH_BUILD_ORPHAN_RECONCILE_ON_STARTUP=False uv run pytest tests/agents/tools/test_graph_tools.py -q --reuse-db` 结果为 `2 passed`（零 skip）
    - `cd server && uv run python -c "import django,os; os.environ.setdefault('DJANGO_SETTINGS_MODULE','friday.settings'); django.setup(); import agents.tools as t; from agents.tools.base import _tool_registry; from agents.chat_runner import _INDEXED_TOOL_NAMES, _DEEP_ANALYSIS_TOOL_NAMES; want={'impact_analysis','trace_call_path'}; assert want <= set(_tool_registry); assert want <= set(_INDEXED_TOOL_NAMES); assert want <= set(_DEEP_ANALYSIS_TOOL_NAMES); assert want <= set(t.__all__)"` 退出码 0
    - `test_conversation_owner_required_fail_closed` 内含 `run_impact` 与 `_resolve_tool_repo` 的 `call_count == 0` 断言（⛔ 不是只断 `success is False`）
    - `cd server && GALAXY_CACHE_WARM_ON_STARTUP=False GRAPH_BUILD_ORPHAN_RECONCILE_ON_STARTUP=False uv run pytest tests/agents -q --reuse-db` 相对基线零新增失败（重名注册会在 import 期直接 `ValueError`，这条命令同时兜住它）
    - `cd server && uv run ruff check agents/` 通过
    - `git diff --name-only` 不含 `mcp/`、`server/services/code_graph/loader.py`、`server/codegraph/services/repo_router_v2.py`
  </acceptance_criteria>
  <verify>
    <automated>cd server && GALAXY_CACHE_WARM_ON_STARTUP=False GRAPH_BUILD_ORPHAN_RECONCILE_ON_STARTUP=False uv run pytest tests/agents/tools/test_graph_tools.py tests/agents/tools/test_knowledge_read_tools.py -q --reuse-db</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM → 工具入参 | 参数直接由模型生成，上下界与类型必须在 pydantic 契约层收敛 |
| `conversation_id` → 权限 | 对话面唯一的用户来源；解析失败即无用户，`get_graph` 不会替你拒 |
| 工具输出 → 对话上下文 | `ToolResult.output` 会被序列化进对话消息与留痕，任何泄漏都是持久化的 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-122-系统路径越权 | **Elevation of Privilege** | 两个 `@tool` 的入口 | mitigate | 🚨 `user is None` 在**任何取仓/取图动作之前**拒绝（`get_graph(user=None)` 走系统路径不会拒，这是本相位对话面唯一的权限起点）。回归：`test_conversation_owner_required_fail_closed` 断言下游 `call_count == 0`，AST 断言 `_resolve_conversation_user` 的调用位次早于 `run_impact` / `_resolve_tool_repo` |
| T-122-穿仓 | Information Disclosure | `run_impact` 的跨仓段 | transfer | `user` 一路显式透传，逐仓复核与 `REDACTED_REPOSITORY` 折叠由 122-06 完成；本壳 ⛔ 不为任何仓另查 `Repository`（只为起点仓取一次，且只用于 staleness 与未索引早退） |
| T-122-空图误导 | **Tampering（结论被污染）** | 未索引仓 / `GraphError` 分支 | mitigate | 未索引仓早退为 `success=False` + 明确文案；`GraphError` 经 `graph_error_to_tool_error` 翻译，⛔ 不 catch 成空影响面。`ok=False` 与 `found=False` 一律走 `success=True` 的信封，⛔ 不与工具故障混为一谈 |
| T-122-错误细节泄漏 | Information Disclosure | 异常翻译分支 | mitigate | `ToolResult.error` 只出 `GRAPH_ERROR_MESSAGES` 的映射文案；日志里的异常文本一律 `redact_secrets_in_text(str(exc))[:500]`（AST 断言守），⛔ 不照抄 `find_api_callers` 的裸 `str(exc)` |
| T-122-遍历 DoS | Denial of Service | LLM 直出参数 | mitigate | pydantic 契约与 MCP Serializer **同表**上下界（AST/字段集断言守），LLM 传 `max_depth=99` 在校验层即被拒 |
| T-122-日志放大 | Denial of Service | 留痕与埋点 | mitigate | 一次调用恰一条汇总 `RetrievalTrace`（复用 `tool_trace_payload`，只出计数与分布）+ 一条 `caller` 事件；留痕 `except Exception: pass`，⛔ 绝不反噬对话主流程 |
| T-122-exclusion 回流 | Information Disclosure | `output["data"]` 与留痕 | mitigate | 被排除符号在 Phase 121 装配阶段已不入图；`metadata` 只放标量，留痕 payload 无正文键（122-08 的 AST 断言同守） |
| T-122-双面漂移 | Tampering | `output["data"]` | mitigate | `data` 必须是 `run_impact` 返回值那个名字本身（AST 断言禁止就地构造 dict）；分支解析与留痕 payload 均复用 122-08 的两面共用原语；byte 级守护由 122-10 补 |
| T-122-SC | Tampering | npm/pip/cargo installs | accept | 零新增依赖（`122-RESEARCH.md` §Package Legitimacy Audit 表为空） |
</threat_model>

<verification>
- `cd server && GALAXY_CACHE_WARM_ON_STARTUP=False GRAPH_BUILD_ORPHAN_RECONCILE_ON_STARTUP=False uv run pytest tests/agents tests/mcp_tools tests/services/code_graph -q --reuse-db` —— 相对 122-08 收尾基线零新增失败
- `cd server && uv run ruff check agents/ services/ && uv run mypy agents/tools/graph_tools.py agents/tools/schemas/graph_tools.py` 零错误
- `cd server && uv run python manage.py makemigrations --check --dry-run` 退出码 0
- `git diff --name-only` 不含 `mcp/`、`server/services/code_graph/loader.py`、`server/codegraph/services/repo_router_v2.py`
</verification>

<success_criteria>
- 两个工具在 `_tool_registry`、`agents.tools.__all__`、`_INDEXED_TOOL_NAMES` 三处齐备（注册 ≠ 暴露，缺一 LLM 就看不见）
- 取不到会话 owner 时下游零调用（不是「返回了错误」，是根本没往下走）
- `output["data"]` 是编排层返回值原样，AST 可证不是就地构造的 dict
- 参数上下界与 MCP Serializer 同表，LLM 无法用超界参数绕过遍历预算
- 每次调用一条 `caller` 事件（带会话 owner 与 `duration_ms`）+ 一条汇总 `RetrievalTrace`（`conversation_id` 关联、`source="chat"`）
</success_criteria>

## Artifacts this phase produces

| 类型 | 名称 | 位置 |
|---|---|---|
| 模块（新建） | `agents/tools/schemas/graph_tools.py` | `server/agents/tools/schemas/graph_tools.py` |
| pydantic 契约 | `ImpactAnalysisToolInput` / `TraceCallPathToolInput`（`strict + extra=forbid + frozen`） | 同上 |
| 模块（新建） | `agents/tools/graph_tools.py` | `server/agents/tools/graph_tools.py` |
| 对话工具 | `impact_analysis`（`category=PROJECT`） | 同上 |
| 对话工具 | `trace_call_path`（`category=PROJECT`） | 同上 |
| 描述/参数常量 | `_DESC_IMPACT` / `_DESC_TRACE` / `_PARAMS_IMPACT` / `_PARAMS_TRACE` | 同上 |
| 私有函数 | `_resolve_tool_repo` / `_record_chat_retrieval` / `_impact_analysis_impl` / `_trace_call_path_impl` | 同上 |
| 事件名 | `impact_analysis_tool_done` / `impact_analysis_tool_failed` / `trace_call_path_tool_done` / `trace_call_path_tool_failed`（`component="agents.tools"`, `category="caller"`） | 同上 |
| 注册（修改） | 顶层 import + `__all__` 两项 | `server/agents/tools/__init__.py` |
| 白名单（修改） | `_INDEXED_TOOL_NAMES` 增 `impact_analysis` / `trace_call_path` | `server/agents/chat_runner.py` |
| 留痕 | 每次调用一条 `RetrievalTrace(kind=EDGE, source="chat", conversation_id=…)` | `interactions` |
| 用例（落地） | `test_registered_and_whitelisted` / `test_conversation_owner_required_fail_closed` | `server/tests/agents/tools/test_graph_tools.py` |

<output>
Create `.planning/phases/122-impact-trace/122-09-SUMMARY.md` when done
</output>
