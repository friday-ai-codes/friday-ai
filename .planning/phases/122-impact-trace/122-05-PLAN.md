---
phase: 122-impact-trace
plan: 05
type: execute
wave: 2
depends_on: ["122-02", "122-03", "122-04"]
files_modified:
  - server/services/code_graph_tools.py
  - server/tests/services/code_graph/test_staleness.py
  - server/tests/services/code_graph/test_impact_shell.py
  - server/tests/services/code_graph/test_symbol_resolve.py
autonomous: true
requirements: [IMPACT-05, IMPACT-06]

must_haves:
  truths:
    - "超预算大仓的 impact/trace 走种子子图路径，不吃 GraphError（D-24）"
    - "GraphNotIndexed / GraphAccessDenied / GraphBuildTimeout 各自翻译成明确文案，⛔ 绝不 catch 成空结果"
    - "输出始终带数值 resolution_rate 与四个降级标记"
    - "索引落后 N commits 有值就报数字，behind_commits 为 None 时降级为只报 as_of，⛔ 不编造"
    - "符号重名时候选列表带 signature（ORM 补取、截断 200 字符）"
  artifacts:
    - path: "server/services/code_graph_tools.py"
      provides: "壳层共享的 ORM/编排原语：取图（带种子）、异常翻译、staleness、降级标记、候选 signature 补取"
      exports: ["fetch_graph_for_tool", "graph_error_to_tool_error", "staleness_payload", "degradation_payload", "resolve_symbol_candidates", "GRAPH_ERROR_MESSAGES"]
      min_lines: 180
  key_links:
    - from: "server/services/code_graph_tools.py"
      to: "services.code_graph.get_graph_service"
      via: "包根 barrel（D-02：⛔ 绝不 from services.code_graph.loader / cache import）"
      pattern: "from services\\.code_graph import"
    - from: "fetch_graph_for_tool"
      to: "GraphService.get_graph"
      via: "seed_symbol_ids + depth 必传（D-24）"
      pattern: "seed_symbol_ids"
---

<objective>
交付两个壳层**共用**的编排原语层：取图（必带种子与深度）、`GraphError` 逐类翻译、staleness 声明、降级标记透出、重名候选的 `signature` ORM 补取。

Purpose: D-21 要求 MCP 与对话两面共用同一内核。唯一能**机械保证**两面不漂移的形状，是把「解析符号 → 取图 → 调内核 → 附声明」这几步收进一个共享编排层；仓里最接近的先例（`search_delivery_knowledge`）恰恰没有共享编排函数，已经在失败语义上漂移了。本 plan 落地它的下半（原语），122-07 落地上半（`run_impact` / `run_trace`）。
Output: `server/services/code_graph_tools.py`（ORM 层）+ 5 个用例落地。

⚠️ **模块位置是硬约束**：必须放 `server/services/code_graph_tools.py`，⛔ **不能**放进 `services/code_graph/` 包内——`tests/services/code_graph/test_access.py::test_observability_contract` 用 `package_dir.glob("*.py")` 扫全包并强制每个 `logger.*` 的 `category == "sampling"`，而本模块要发的是 `caller` 类事件的上游数据；同时它必须碰 ORM，放进包内会破 D-01。
</objective>

<execution_context>
@.cursor/gsd-core/workflows/execute-plan.md
@.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/122-impact-trace/122-CONTEXT.md
@.planning/phases/122-impact-trace/122-RESEARCH.md
@.planning/phases/122-impact-trace/122-PATTERNS.md
@.planning/phases/121-graph-base/121-08-SUMMARY.md
@server/services/code_graph/model.py
@server/repositories/freshness_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 取图原语（D-24 种子透传）与 GraphError 翻译表（D-03）</name>
  <read_first>
    - `server/services/code_graph/cache.py:674-745`（`get_graph` 签名：`repository_id, branch="", *, user=None, include_low_confidence=False, seed_symbol_ids=None, depth=None`；`branch=""` 表示 base；`ensure_repository_readable` 在 `sync_to_async` 之前、**每次调用都跑**）
    - `server/services/code_graph/cache.py:93-98`（`_DEFAULT_SUBGRAPH_DEPTH = 2` / `_MAX_SUBGRAPH_DEPTH = 10`；🚨 缺省是 2 而 impact 默认 `max_depth=3`，**不显式传 `depth` 会让子图边界比遍历深度浅一层，d3 莫名残缺**）
    - `server/services/code_graph/cache.py:960-985`（超预算 + 无种子 ⇒ **抛 `GraphError`**，消息含 `seed_symbol_ids`；⛔ 不返回空图、不返回截断图）
    - `server/services/code_graph/cache.py:833-845`（⚠️ 子图路径**不进缓存也不进 single-flight**，大仓每次都重新装配、并发各建各的；这是已知设计，性能特性与小仓完全不同）
    - `server/services/code_graph/model.py:360-400`（`GraphError` 与四个子类；⚠️ `GraphError.details` 含 `estimated_bytes` / `max_graph_bytes`，`__str__` 会把 details 拼上去）
    - `server/mcp_tools/views.py:190-206`（`_MIRROR_ERROR_STATUS` + `_mirror_error_response`：本仓唯一的「异常类 → 错误码 + HTTP 码」映射表先例）
    - `server/services/code_graph/__init__.py`（barrel 17 项，`get_graph_service` 是唯一取图入口）
  </read_first>
  <action>
新建 `server/services/code_graph_tools.py`。模块 docstring 三段式，第一段就写明它**为什么不在 `services/code_graph/` 包内**（见本 plan `<objective>` 的硬约束），第三段写「边界与已知翻车点」。

import 纪律（D-02，本相位是全仓 AST import 守护的第一个真实承压者）：图相关一律 `from services.code_graph import GraphAccessDenied, GraphBuildFailed, GraphBuildTimeout, GraphError, GraphMeta, GraphNotIndexed, LOW_RESOLUTION_THRESHOLD, get_graph_service`；内核走 `from services.code_graph.impact import ...` / `from services.code_graph.trace import ...` / `from services.code_graph.symbol_resolve import ...`（D-28 已裁决合法）。⛔ 任何形态的 `loader` / `cache` / `signature` / `access` 直连都会让 `test_no_upper_layer_imports_internal_submodules` 当场变红。ORM 模型走**函数体内 lazy import**（与 `tests/services/code_graph/conftest.py` 的既有约定同款，避免 `services` 包在 Django app loading 早期触发模型导入）。

① `GRAPH_ERROR_MESSAGES: Final[Mapping[type[GraphError], tuple[str, str]]]`——`异常类 → (error_code, 面向 agent 的中文文案)`，形态照 `_MIRROR_ERROR_STATUS`：
- `GraphNotIndexed` → `("repository_not_indexed", "仓库尚未建立索引，无法分析影响面；请先完成索引后重试")`
- `GraphAccessDenied` → `("repository_access_denied", "无权访问该仓库")`
- `GraphBuildTimeout` → `("graph_build_timeout", "代码图构建超时，请稍后重试")`
- `GraphBuildFailed` → `("graph_build_failed", "代码图构建失败")`
- `GraphError` → `("graph_unavailable", "代码图当前不可用；若为超大仓库，请先定位起点符号再查影响面")`（兜底）

② `graph_error_to_tool_error(exc: GraphError) -> tuple[str, str]`：按 `type(exc).__mro__` 顺序在表里查最具体的一项。🚨 文案只取表里的常量与 `exc.message`，⛔ **不得**把 `str(exc)` 或 `exc.details` 直出给 agent——`details` 含 `estimated_bytes` / `max_graph_bytes` 这类内部量（T-122-错误细节泄漏）。`details` 只进（已脱敏的）日志。docstring 写明 D-03：⛔ 任何调用方都不许把 `GraphError` catch 成空结果——空影响面会被 agent 读成「改这里没影响」，是最危险的误导。

③ `async def fetch_graph_for_tool(repository_id, branch, *, user, seed_symbol_ids, depth, include_low_confidence=False) -> CodeGraph`：
- `seed_symbol_ids` 是**必填关键字参数、无默认值**（D-24：impact/trace 天然有种子，忘传就会在超预算大仓上直接吃 `GraphError`）。签名上不给默认值，是让「忘了传」在写代码时就报错而不是在生产上报错。
- `depth` 同样必填：调用方传 `max_depth`，⛔ 不许省略（见 read_first 的 `cache.py:93-98`）。
- 内部 `await get_graph_service().get_graph(repository_id, branch, user=user, include_low_confidence=..., seed_symbol_ids=list(seed_symbol_ids), depth=depth)`。
- ⛔ 本函数**不 catch** `GraphError`，原样上抛给壳层去翻译（D-03 的分层：内核与原语不吞，壳层翻译）。
- docstring 记下 `cache.py:833-845` 的性能特性差异（子图路径不进缓存、不进 single-flight），提醒验收时大仓与小仓要分开量。
- 埋点 `_log_graph_fetched(...)`：DEBUG、`component="code_graph"`、`category="sampling"`，记 `node_count` / `edge_count` / `degraded` / `duration_ms`。
  </action>
  <acceptance_criteria>
    - `test_over_budget_uses_seeded_subgraph` 落地（`tests/services/code_graph/test_impact_shell.py`）：把 `CODE_GRAPH_MAX_GRAPH_BYTES` 设为 1 触发超预算，断言 `fetch_graph_for_tool(..., seed_symbol_ids=[sid], depth=3)` **正常返回** `CodeGraph` 且 `meta.degraded` 以 `"on_demand_subgraph"` 开头；同一条件下手工调 `get_graph_service().get_graph(repo_id, "", user=user)`（不带种子）`pytest.raises(GraphError)`——两个断言并列，证明种子透传是那条分支的唯一区别
    - 同一用例断言 `depth` 真的传下去了：spy `GraphService.get_graph`，`assert kwargs["depth"] == 3 and kwargs["seed_symbol_ids"] == [sid]`
    - `test_graph_error_translated_not_swallowed` 落地：对未索引仓调 `fetch_graph_for_tool` ⇒ `pytest.raises(GraphNotIndexed)`（原语不吞）；`graph_error_to_tool_error(exc)` 返回 `("repository_not_indexed", <非空文案>)`；**且**断言返回文案里**不含** `exc.details` 的任何键名与值（用 `GraphError("x", {"estimated_bytes": 999})` 构造一个带 details 的实例，断言 `"999" not in message and "estimated_bytes" not in message`）
    - AST import 守护：`cd server && GALAXY_CACHE_WARM_ON_STARTUP=False GRAPH_BUILD_ORPHAN_RECONCILE_ON_STARTUP=False uv run pytest tests/services/code_graph/test_access.py -k upper_layer -q --reuse-db` 通过
    - `cd server && uv run python -c "import ast; t=ast.parse(open('services/code_graph_tools.py').read()); mods=[a.module for a in ast.walk(t) if isinstance(a,ast.ImportFrom) and a.module]; bad=[m for m in mods if m.startswith('services.code_graph.') and m.split('.')[-1] in {'loader','cache','signature','access'}]; assert not bad, bad"` 退出码 0
  </acceptance_criteria>
  <verify>
    <automated>cd server && GALAXY_CACHE_WARM_ON_STARTUP=False GRAPH_BUILD_ORPHAN_RECONCILE_ON_STARTUP=False uv run pytest tests/services/code_graph/test_impact_shell.py -k "over_budget or translated_not_swallowed" tests/services/code_graph/test_access.py -k upper_layer -q --reuse-db</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: staleness 声明（D-22）与降级标记透出（D-23）</name>
  <read_first>
    - `server/repositories/freshness_service.py`（全文 —— `compute_freshness_status(repo)` 的三态决策表 :25-40；`update_behind_commits_for_stale_repos` 只刷 `auto_index_enabled=True` 且 `stale` 的仓 :49-58；⚠️ `_calculate_commit_distance` 在**本地无 clone 时返回 `None`** :88-94，所以 `None` 是真实可达分支）
    - `server/repositories/models.py:299-320`（`remote_head_sha` / `last_indexed_commit_sha` / `behind_commits` / `behind_commits_calculated_at` 四个字段）
    - `server/services/code_graph/model.py:250-317`（`GraphMeta` 15 字段；注释里逐条标了哪些「🔔 上层工具必须透出」：`resolution_rate` 数值 / `low_resolution` / `partial_edges` + `partial_reason` / `degraded`（三个字面量）/ `cross_repo_unresolved_count` / `cross_repo_branch_unfiltered` / `include_low_confidence`）
    - `server/services/code_graph/model.py:193-219`（🚨 写给 Phase 122 的硬要求：本仓解析率常态就在 0.17 量级，`low_resolution` 是「比常态更差」的异常标记而非「够不够用」的判据；输出必须**始终**透出数值 `resolution_rate` 与保守性声明）
    - `.planning/phases/122-impact-trace/122-RESEARCH.md` §Code Examples §4（`staleness_payload` 的字段形态；⛔ 请求路径不起 git 子进程——那是两次 `create_subprocess_exec` + 30s/15s 超时）
  </read_first>
  <action>
在 `code_graph_tools.py` 补两个原语。

① `async def staleness_payload(repo) -> dict`（D-22）：调 `compute_freshness_status(repo)` 拿三态（⛔ 不要自己比 sha——三态决策表里「`remote_head_checked_at is None` → unknown」这条最容易漏），产出
`{"as_of": repo.last_indexed_commit_sha or "", "freshness": status, "behind_commits": repo.behind_commits, "behind_commits_calculated_at": <isoformat or None>, "declaration": <人话声明>}`。
`declaration` 的两条分支必须显式区分：`behind_commits` 有值 ⇒ `"索引落后 N commits"`（N 为 0 时 `"索引与远端一致"`）；`behind_commits is None` ⇒ **降级**为 `"索引 as_of <sha 前 12 位>；落后提交数未知"`。🚨 ⛔ **不编造数字**——`None` 是真实存在的分支（见 read_first：本地无 clone 时 `_calculate_commit_distance` 返回 `None`，且只有 `auto_index_enabled=True` 的仓会被定时任务刷）。⛔ 不在请求路径起 git 子进程。

② `def degradation_payload(meta: GraphMeta) -> dict`（D-23）：把 `GraphMeta` 上标了「🔔 上层工具必须透出」的字段原样搬出来，键名与 `GraphMeta` 字段名逐字一致：`resolution_rate`（**数值**，必带）/ `low_resolution` / `partial_edges` / `partial_reason` / `degraded` / `cross_repo_unresolved_count` / `cross_repo_branch_unfiltered` / `include_low_confidence` / `node_count` / `edge_count` / `excluded_file_count`。
额外产出一个 `"declarations": list[str]` 人话声明数组，按条件追加：
- **始终**追加解析率声明（形如 `"本仓约 X% 的调用边未解析到具体符号，影响面结论偏保守"`，X = `round((1 - resolution_rate) * 100)`）——🚨 ⛔ 不得只在 `low_resolution is True` 时才提醒：生产 218 个仓的解析率中位数只有 0.17，全库最高 0.56，**没有任何一个仓「解析得好」**，布尔量在这个常态下没有信息量（121-10 写给本相位的硬要求）。
- `partial_edges` ⇒ 追加「边构建在途，结果可能不完整」。
- `degraded == "on_demand_subgraph"` ⇒ 追加「已降级为按需子图，覆盖面小于全图（该子图在其深度内完整）」；`== "on_demand_subgraph_truncated"` ⇒ 追加「按需子图的某一轮邻接被截断，结论可能在边界处断掉」。两档必须区分——上层据此才知道「影响面就这么大」这句话能不能说。
- `cross_repo_unresolved_count > 0` ⇒ 追加「装配时有 N 条跨仓边无法定位到符号，已丢弃」。⚠️ 这个数与 122-06 的「本次 ORM 查到 N 条跨仓调用」是**两个不同的数**，键名与文案都不许混。
- `cross_repo_branch_unfiltered` ⇒ 追加「跨仓边无法按分支过滤」。
  </action>
  <acceptance_criteria>
    - `test_behind_commits_reported` 落地：造一个 `behind_commits=7`、`last_indexed_commit_sha` 与 `remote_head_sha` 不同、`remote_head_checked_at` 非空的仓 ⇒ `payload["behind_commits"] == 7`、`payload["freshness"] == "stale"`、`declaration` 含 `"7"`
    - `test_behind_commits_none_degrades_to_as_of` 落地：`behind_commits=None` ⇒ `payload["behind_commits"] is None`、`declaration` **含** `as_of` 的 sha 前 12 位、**不含**任何数字型「落后 N」表述（断言 `"落后" not in declaration or "未知" in declaration`）；另断言 `remote_head_checked_at=None` 时 `freshness == "unknown"`
    - `degradation_payload` 单测（放 `test_impact_shell.py`）：构造 `resolution_rate=0.17, low_resolution=False` 的 `GraphMeta` ⇒ `payload["resolution_rate"] == 0.17` **且** `declarations` 里**仍然**有解析率声明（证明它不依赖 `low_resolution` 布尔量）；构造 `degraded="on_demand_subgraph_truncated"` ⇒ 声明文案与 `"on_demand_subgraph"` 档**不同**
    - `cd server && uv run python -c "from services.code_graph.model import GraphMeta; import dataclasses, inspect, services.code_graph_tools as m; src=inspect.getsource(m.degradation_payload); need={'resolution_rate','low_resolution','partial_edges','partial_reason','degraded','cross_repo_unresolved_count','cross_repo_branch_unfiltered','include_low_confidence'}; assert all(k in src for k in need), need - {k for k in need if k in src}"` 退出码 0
  </acceptance_criteria>
  <verify>
    <automated>cd server && GALAXY_CACHE_WARM_ON_STARTUP=False GRAPH_BUILD_ORPHAN_RECONCILE_ON_STARTUP=False uv run pytest tests/services/code_graph/test_staleness.py tests/services/code_graph/test_impact_shell.py -q --reuse-db</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: 重名候选的 ORM 解析与 signature 补取（D-19 / Pitfall 2）</name>
  <read_first>
    - `.planning/phases/122-impact-trace/122-RESEARCH.md` Pitfall 2（生产分布：distinct `(repo,name)` 202,661 组，其中 39,031 组非唯一 = **19.3%**；`(repo,file,name)` 三元组仍有 24,312 组冲突。候选列表是主路径不是兜底）
    - `server/services/code_graph/loader.py:354-356`（⚠️ `signature` 是 TextField、可达数 KB，**装配时根本不取**，节点属性恒 5 个）
    - `server/services/code_graph/symbol_resolve.py`（122-02 交付的 `SymbolCandidate` / `SymbolResolution` / `resolve_symbol_in_graph`）
    - `server/mcp_tools/views.py:1153-1210`（`FindRelatedChunksView._resolve_source_chunk` —— ⚠️ **反面教材**：它对重名 `.afirst()` 静默取第一个，正是 D-19 明令禁止的。可照抄的只有 `branch_names = ["", graph_branch] if graph_branch else [""]` 这个分支口径）
    - `server/codegraph/models.py:12-60`（`Symbol` 模型字段：`repository_id` / `branch_name` / `name` / `symbol_type` / `file_path` / `start_line` / `signature`）
  </read_first>
  <action>
在 `code_graph_tools.py` 补 ORM 侧的符号解析——这是「图内定位」的**前置**一半，回答的是「取图之前，我该拿哪个 `symbol_id` 当种子」（D-24 的鸡生蛋问题：解析需要图，取图需要种子，所以解析必须先在 ORM 上做一遍）。

`async def resolve_symbol_candidates(*, repository_id, branch_names, symbol_id=None, name=None, file_path=None, symbol_type=None) -> SymbolResolution`：
- `symbol_id` 非空 ⇒ **uid 优先**，只查这一行确认存在与归属仓库，命中即 `resolved=symbol_id`。⛔ 绝不在此分支退化去按名字搜。
- 否则按 `Symbol.objects.filter(repository_id=..., branch_name__in=branch_names, name=name)`，可选叠加 `symbol_type__iexact` 与 `file_path`（相等或 `file_path__endswith`）。`branch_names` 口径照 read_first 的 `["", graph_branch]`（overlay 语义：feature 分支图 = base 全量 + 分支增量）。
- 排序 `.order_by("file_path", "start_line", "id")`（稳定，Pitfall 2 §2 明确要求）。
- 先 `.acount()` 拿 `total_candidates`，再 `[:CANDIDATE_LIMIT]` 取值。⛔ **绝不** `.afirst()`——那正是 D-19 与 REQUIREMENTS 明文禁止的静默取第一个。
- 恰 1 条 ⇒ `resolved` 置该 id 并直接返回（不构造候选列表，省一次序列化）。
- ≥2 条 ⇒ 构造 `SymbolCandidate` 元组，`signature` 由**同一次查询**的 `values_list("id","name","symbol_type","file_path","start_line","signature")` 取出并**截断到 200 字符**（token 纪律，与 D-17 同理；截断时补 `"…"`），`truncated = total_candidates > CANDIDATE_LIMIT`。
- 0 条 ⇒ 三项全空，由壳层翻成 `symbol_not_found`。

再补 `def resolution_to_payload(resolution: SymbolResolution) -> dict`：把 `SymbolResolution` 转成两个壳共用的 dict 形态 `{"resolved": ..., "ambiguous": bool, "candidates": [...], "total_candidates": int, "truncated": bool, "hint": str}`，`hint` 在 `ambiguous` 时给出可执行指引（形如 `"符号名不唯一；请带 symbol_id 重试，或补 file_path / symbol_type 收窄"`）——Pitfall 2 §4 要求「能一次收敛就不要往返两轮」。

埋点 `_log_candidates_resolved(...)`：DEBUG、`component="code_graph"`、`category="sampling"`，记 `total_candidates` / `truncated` / `by_uid`（bool），⛔ 不记符号名与文件路径。
  </action>
  <acceptance_criteria>
    - `test_ambiguous_returns_candidates` 落地（`tests/services/code_graph/test_symbol_resolve.py`，带 `@pytest.mark.django_db`）：造 3 个同 `(repository_id, name)` 但 `file_path` 不同的 `Symbol`（各带非空 `signature`）⇒ `resolved is None`、`total_candidates == 3`、`len(candidates) == 3`、每条 `signature` 非空、候选按 `(file_path, start_line)` 升序
    - 同一用例断言 signature 截断：造一个 `signature` 长 500 字符的符号 ⇒ 该候选的 `len(signature) <= 201`
    - 同一用例断言收窄参数生效：同样的三个符号，传 `file_path=<其中一个的路径>` ⇒ `resolved` 非 None（一次收敛，不再返回候选）
    - 反面教材守护：`cd server && uv run python -c "import ast, inspect, services.code_graph_tools as m; src=inspect.getsource(m.resolve_symbol_candidates); t=ast.parse(src.lstrip()); calls={n.func.attr for n in ast.walk(t) if isinstance(n,ast.Call) and isinstance(n.func,ast.Attribute)}; assert 'afirst' not in calls and 'first' not in calls, calls"` 退出码 0（AST 判据，⛔ 不用 grep——docstring 里那句「绝不 `.afirst()`」的散文会命中字面 grep）
    - `cd server && GALAXY_CACHE_WARM_ON_STARTUP=False GRAPH_BUILD_ORPHAN_RECONCILE_ON_STARTUP=False uv run pytest tests/services/code_graph/test_symbol_resolve.py -q --reuse-db` 结果零 skip
  </acceptance_criteria>
  <verify>
    <automated>cd server && GALAXY_CACHE_WARM_ON_STARTUP=False GRAPH_BUILD_ORPHAN_RECONCILE_ON_STARTUP=False uv run pytest tests/services/code_graph/test_symbol_resolve.py -q --reuse-db</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 壳层 ↔ 图服务 | `fetch_graph_for_tool` 是本相位所有取图的唯一去处；`ensure_repository_readable` 在其内每次都跑 |
| 壳层 ↔ ORM | `resolve_symbol_candidates` 是本相位唯一直查 `Symbol` 的地方 |
| 异常 ↔ agent | `GraphError.details` 含内部内存量，绝不能直出 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-122-绕闸 | Elevation of Privilege | `code_graph_tools.py` 的 import 面 | mitigate | D-02：图相关只经包根 barrel；AST 断言禁止 `services.code_graph.{loader,cache,signature,access}`；`test_no_upper_layer_imports_internal_submodules` 是机械防线，本模块是它的首个真实承压者 |
| T-122-错误细节泄漏 | Information Disclosure | `graph_error_to_tool_error` | mitigate | 只出映射表常量 + `exc.message`，⛔ 不用 `str(exc)`（`__str__` 会拼上 details）、不透 `exc.details`；用例显式断言 `estimated_bytes` 不出现在文案里 |
| T-122-空图误导 | Tampering（结论被污染） | `fetch_graph_for_tool` | mitigate | D-03：原语**不 catch** `GraphError`，原样上抛；未索引仓的 impact 必须是错误响应而非 `{"affected": []}` |
| T-122-半新图误导 | Tampering | `degradation_payload` | mitigate | D-23：四个标记 + **数值** `resolution_rate` 全部透出，`degraded` 的两个子档区分声明；⛔ 不允许只透布尔量 |
| T-122-遍历 DoS | Denial of Service | `resolve_symbol_candidates` | mitigate | `.acount()` + `[:CANDIDATE_LIMIT]` 双重封顶，⛔ 不物化全量候选；`signature` 截断 200 字符 |
| T-122-日志放大 | Denial of Service | 三个 `_log_*` | mitigate | 全部 DEBUG + `category="sampling"`；每个原语一次调用至多一条事件 |
| T-122-穿仓 | Information Disclosure | `fetch_graph_for_tool` | mitigate | 每次调用都经 `get_graph` ⇒ `ensure_repository_readable` 不因缓存命中跳过；跨仓复核在 122-06 复用同一原语 |
| T-122-SC | Tampering | npm/pip/cargo installs | accept | 零新增依赖（`122-RESEARCH.md` §Package Legitimacy Audit 表为空） |
</threat_model>

<verification>
- `cd server && GALAXY_CACHE_WARM_ON_STARTUP=False GRAPH_BUILD_ORPHAN_RECONCILE_ON_STARTUP=False uv run pytest tests/services/code_graph -q --reuse-db` —— `test_staleness.py` / `test_symbol_resolve.py` 零 skip，`test_impact_shell.py` 剩 1 skip（归 122-07）
- `cd server && uv run ruff check services/ && uv run mypy services/code_graph/ services/code_graph_tools.py` 本 plan 文件零错误
- `cd server && uv run python manage.py makemigrations --check --dry-run` 退出码 0（本相位零新增模型、零迁移）
- `git diff --name-only` 不含 `server/codegraph/services/repo_router_v2.py` 与 `mcp/`
</verification>

<success_criteria>
- 超预算大仓上带种子取图成功、不带种子抛 `GraphError`，两条并列断言证明种子透传是唯一区别
- `depth` 与 `seed_symbol_ids` 均被证明真的传到了 `get_graph`
- `GraphError` 的三个子类各自翻译成不同的 `error_code`，且文案里不含任何内部内存量
- `resolution_rate` 的数值声明在 `low_resolution == False` 时**仍然**出现
- `behind_commits is None` 时降级为 `as_of`，输出里没有编造的数字
- 重名候选带 `signature` 且解析器在任何路径下都不取第一个（AST 断言守）
</success_criteria>

## Artifacts this phase produces

| 类型 | 名称 | 位置 |
|---|---|---|
| 模块（新建） | `services/code_graph_tools.py` | `server/services/code_graph_tools.py` |
| 常量表 | `GRAPH_ERROR_MESSAGES`（5 项：`repository_not_indexed` / `repository_access_denied` / `graph_build_timeout` / `graph_build_failed` / `graph_unavailable`） | 同上 |
| 事件名常量 | `_EVENT_GRAPH_FETCHED = "code_graph_tool_graph_fetched"` / `_EVENT_CANDIDATES_RESOLVED = "code_graph_tool_candidates_resolved"` | 同上 |
| 函数（async） | `fetch_graph_for_tool(repository_id, branch, *, user, seed_symbol_ids, depth, include_low_confidence)` | 同上 |
| 函数 | `graph_error_to_tool_error(exc) -> (code, message)` | 同上 |
| 函数（async） | `staleness_payload(repo) -> dict` | 同上 |
| 函数 | `degradation_payload(meta) -> dict` | 同上 |
| 函数（async） | `resolve_symbol_candidates(*, repository_id, branch_names, symbol_id, name, file_path, symbol_type)` | 同上 |
| 函数 | `resolution_to_payload(resolution) -> dict` | 同上 |
| 私有埋点 | `_log_graph_fetched` / `_log_candidates_resolved` | 同上 |
| 输出字段 | `as_of` / `freshness` / `behind_commits` / `behind_commits_calculated_at` / `declaration` | `staleness_payload` |
| 输出字段 | `resolution_rate` / `low_resolution` / `partial_edges` / `partial_reason` / `degraded` / `cross_repo_unresolved_count` / `cross_repo_branch_unfiltered` / `include_low_confidence` / `node_count` / `edge_count` / `excluded_file_count` / `declarations` | `degradation_payload` |

<output>
Create `.planning/phases/122-impact-trace/122-05-SUMMARY.md` when done
</output>
