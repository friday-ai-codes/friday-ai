---
phase: 122-impact-trace
plan: 03
type: execute
wave: 1
depends_on: ["122-01"]
files_modified:
  - server/services/code_graph/impact.py
  - server/tests/services/code_graph/test_impact.py
autonomous: true
requirements: [IMPACT-01, IMPACT-02, IMPACT-04]

must_haves:
  truths:
    - "对任一符号做反向依赖分析，返回 d1/d2/d3 三档深度分组，同符号多层出现取最浅"
    - "每条结果带 confidence 分档 + reason + path_confidence（整条路径最小值）"
    - "min_confidence 单调收缩结果集；bare_name 边需两道闸同时开启才参与扩散"
    - "风险四级由确定性函数给出，弱证据（全路径最高档只到 bare_name）封顶 MEDIUM"
    - "结果超上限时按「深度升序 + 置信度降序」排序后截断，并给出分层计数"
  artifacts:
    - path: "server/services/code_graph/impact.py"
      provides: "分层反向 BFS + path-min 置信度 + 确定性风险分级 + 截断纪律（纯函数）"
      exports: ["analyze_impact", "grade_risk", "RiskLevel", "DEPTH_LABELS", "RISK_THRESHOLDS", "DEFAULT_MAX_DEPTH", "DEFAULT_RESULT_LIMIT", "DEFAULT_MAX_NODES"]
      min_lines: 200
  key_links:
    - from: "server/services/code_graph/impact.py"
      to: "services.code_graph.confidence_score / derive_reason"
      via: "包根 barrel import（⛔ 不自建数值表、不自拼理由串）"
      pattern: "from services\\.code_graph import"
---

<objective>
交付 impact 的**纯算法内核**：在冻结 `MultiDiGraph` 上做分层反向 BFS，产出深度分组、逐边置信度与 path-min、确定性风险四级与截断 summary。⛔ 零 ORM、零 Django settings、零图复制。

Purpose: 这是 Pitfall 1「裸名边假阳性灾难」的验收内核——置信度分层能不能一路透到 agent 眼前，取决于这一层有没有把档位算对并原样带出。同时它是全相位唯一能用合成图做**毫秒级精度回归**的部分（D-01 的全部意义）。
Output: `services/code_graph/impact.py` + `test_impact.py` 的 8 个用例落地。
</objective>

<execution_context>
@.cursor/gsd-core/workflows/execute-plan.md
@.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/122-impact-trace/122-CONTEXT.md
@.planning/phases/122-impact-trace/122-RESEARCH.md
@server/services/code_graph/model.py
@server/services/code_graph/signature.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 分层反向 BFS 与 path-min 置信度（IMPACT-01 / IMPACT-02）</name>
  <read_first>
    - `.planning/phases/122-impact-trace/122-RESEARCH.md` §Code Examples §1（已实测的 `reverse_layers` 骨架：`graph.predecessors()` + `graph[pred][node].values()` 逐档取边；30k/100k 图上 0.012ms；`reverse(copy=False)` 0.004ms 而 `copy()` 330–690ms）
    - `server/services/code_graph/model.py:53-165`（`EdgeKind` / `EdgeConfidence` 四档 / `confidence_score()`——`cross_repo` 档缺 `match_confidence` **故意抛 `ValueError`** / `derive_reason()` 四档文案）
    - `server/services/code_graph/model.py:319-360`（节点属性恒 5 个、边属性恒 3 个 + `cross_repo` 边的第 4 个 `match_confidence`；🚨 `graph` 是**共享对象**，缓存命中时所有调用方拿到同一个实例）
    - `server/services/code_graph/signature.py:1-90`（纯函数模块骨架：三段式 docstring、`Final[str]` 事件常量、`__all__`）
    - `server/services/code_graph/access.py:105-117`（埋点函数形态：`component`/`category` 字面量 + 异常吞掉）
    - `server/tests/services/code_graph/conftest.py`（122-01 交付的 `known_topology` 与 `hub_topology`）
  </read_first>
  <action>
新建 `server/services/code_graph/impact.py`。三段式中文 docstring（问题背景 / 方案 / 边界与已知翻车点），`from __future__ import annotations`，运行期 import 限于 stdlib（`collections.deque`、`dataclasses`、`enum`、`typing`）+ `structlog` + **包根 barrel** `from services.code_graph import EdgeConfidence, EdgeKind, confidence_score, derive_reason`。⛔ 零 Django、零 ORM、零 `networkx` 运行期 import（图对象走 `TYPE_CHECKING` 注解，照 `model.py` 的 adapter seam）。

模块级常量（各自带逐条解释的注释）：
- `DEFAULT_MAX_DEPTH: Final[int] = 3`（D-05：超过 3 层对 agent 没有行动价值）
- `DEPTH_LABELS: Final[Mapping[int, str]] = {1: "WILL_BREAK", 2: "LIKELY_AFFECTED", 3: "MAY_NEED_TESTING"}`
- `_CONFIDENCE_TIER_RANK: Final[Mapping[EdgeConfidence, int]]` = `{CHUNK_LEVEL: 0, BARE_NAME: 1, CROSS_REPO: 2, RESOLVED: 3}`（这是 D-15 第三个输入「路径最高置信档」的可比序，注释写明它与 `confidence_score()` 的**数值**是两个量：数值用于 `min_confidence` 过滤与 path-min，档序用于 D-29 的封顶判据）
- 事件常量 `_EVENT_IMPACT_ANALYZED: Final[str] = "code_graph_impact_analyzed"`

私有函数 `_edge_score(attrs) -> float`：`EdgeConfidence(attrs["confidence"])`，`CROSS_REPO` 档传 `match_confidence=attrs["match_confidence"]` 调 `confidence_score`，其余直接调。⛔ 不要自己写 `{"resolved": 1.0, ...}` 字典——那会把跨仓边的可信度静默抹成常量。

私有函数 `_reverse_layers(graph, seed_id, *, max_depth, min_confidence, allow_bare_name, max_nodes) -> dict[str, _Reach]`，其中 `_Reach` 是 `@dataclass(frozen=True, slots=True)`，字段 `depth: int` / `path_confidence: float` / `path_top_tier: int` / `via: dict`：
- 用 `graph.predecessors(node)` 做逐层展开；⛔ **绝不** `graph.copy()` / `graph.reverse(copy=True)`（实测 330–690ms，比视图慢 5 个数量级），也**不用** `nx.bfs_layers`（它给不出经由哪条边与 path_min，D-07 与逐跳渲染两条都要）。
- 首次访问即最浅层（BFS 天然性质）⇒ 已在 `best` 中的节点直接 `continue`，实现 D-05 的「同符号多层出现取最浅」。
- MultiDiGraph 上同一对符号并存多档边 ⇒ 必须 `for attrs in graph[pred][node].values()` 逐条取，⛔ 不能把 `graph[pred][node]` 当单个 dict 用。
- 每条边先算 `score = _edge_score(attrs)`；`score < min_confidence` ⇒ 跳过（不参与扩散）。
- `allow_bare_name is False` 且 `EdgeConfidence(attrs["confidence"]) is EdgeConfidence.BARE_NAME` ⇒ 跳过（D-08 的第二道闸，见 Task 2）。
- `path_confidence = min(前驱的 path_confidence, score)`（D-07：**最小值**，⛔ 不取平均、不取乘积）；同层多条到达路径时取 `max` 那条（widest-path 的层受限形式）。
- `path_top_tier = max(前驱的 path_top_tier, _CONFIDENCE_TIER_RANK[tier])`（D-29 的输入）。
- `max_nodes` 软上限（`DEFAULT_MAX_NODES: Final[int] = 2000`）：`best` 规模撞上限即停止扩展并返回一个 `truncated_by_nodes` 标记。理由写进注释：生产解析边入度 max 2,803，d1 就能到近 3,000 条，d2/d3 是指数级——遍历本身也要有预算，不只是输出截断（Pitfall 6 / T-122-遍历 DoS）。

公开函数 `analyze_impact(graph, seed_symbol_id, *, max_depth=DEFAULT_MAX_DEPTH, min_confidence=1.0, include_low_confidence=False, limit=DEFAULT_RESULT_LIMIT, max_nodes=DEFAULT_MAX_NODES, exclude_test_files=False, crosses_repo=False) -> dict`（本 task 先落 groups 与逐条字段，risk/summary 由 Task 2/3 补）。返回**结构化 dict**（D-10，渲染归壳层），每条 item：
`{"symbol_id","name","symbol_type","file_path","start_line","depth","label","path_confidence","path_top_tier","via":{"from","to","line_number","kind","confidence","reason"}}`。
`reason` 由 `derive_reason(EdgeKind(attrs["kind"]), EdgeConfidence(attrs["confidence"]), callee_name=..., match_confidence=attrs.get("match_confidence"))` **现推**（D-09，⛔ 不在图边属性上新增第四个属性）。`file_path` / `start_line` 取 `graph.nodes[nid]`，`via.line_number` 取边属性上的调用点行号。

🚨 **源码正文永不出现在输出里**（D-17）：本内核只出 `file:line` 与符号名，正文由 agent 自己按需要再去读文件（既有 `get_repository_file` MCP 工具）。这一点写进模块 docstring 的「边界」段——token 纪律是 agent 工具的生命线，2,803 条 × 一段源码就是上下文爆炸。

`exclude_test_files`（Claude's Discretion，做）：为真时丢弃 `file_path` 命中 `_TEST_PATH_HINTS`（模块级 `Final[tuple[str, ...]]`，如 `("_test.", "test_", "/tests/", ".spec.", ".test.")`）的结果条目——只在**输出**阶段过滤，⛔ 不影响扩散（否则会切断经由测试文件的真实调用链）。

埋点 `_log_impact_analyzed(...)`：DEBUG、`component="code_graph"`、`category="sampling"`，记 `depth` / `returned` / `total_found` / `duration_ms`，⛔ 不记符号名与路径，⛔ BFS 循环内零日志。
  </action>
  <acceptance_criteria>
    - `test_depth_grouping` 落地并在 `known_topology` 上逐点断言：`min_confidence=1.0` 时 A 的 d1 == `{"B","C"}`、d2 == `{"E","D"}`、F **不在**结果里（0.7 < 1.0）；`G` 在**任何** `min_confidence` 下都不出现（方向纪律：下游不进反向结果）；`min_confidence=0.7` 时 F 进 d1 且该条 `path_confidence == 0.7`
    - `test_max_depth_budget`：`max_depth=1` 时结果只含 d1；`max_depth=2` 时 D 出现在 d2；无论 `max_depth` 取多少，同时可达 d1 与 d2 的 C 恒在 d1（最浅优先）
    - `test_kernel_does_not_mutate_graph`：调用前后 `graph.number_of_nodes()` / `number_of_edges()` 相等，`nx.is_frozen(graph) is True` 仍成立，且调用未抛 `NetworkXError`
    - `test_edge_confidence_and_reason`：每条 item 的 `via["reason"]` 与直接调 `derive_reason(...)` 的返回**逐字相等**；`via["confidence"]` 取自边属性原值；F 那条的 `reason` 含 `0.7`
    - `cd server && uv run python -c "import ast; t=ast.parse(open('services/code_graph/impact.py').read()); calls=[ast.unparse(n) for n in ast.walk(t) if isinstance(n,ast.Call)]; assert not [c for c in calls if c.endswith('.copy()') or 'copy=True' in c], [c for c in calls if 'copy' in c]"` 退出码 0（性能红线：图不许 copy）
  </acceptance_criteria>
  <verify>
    <automated>cd server && uv run pytest tests/services/code_graph/test_impact.py -k "depth_grouping or max_depth_budget or does_not_mutate or confidence_and_reason" -q</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: min_confidence 过滤与 D-08 双闸（IMPACT-02）</name>
  <read_first>
    - `.planning/phases/122-impact-trace/122-CONTEXT.md` D-06 / D-08（`min_confidence` 按 `confidence_score()` 数值映射过滤；bare_name 需 `include_low_confidence=True` **且** `min_confidence` 低于 bare_name 分值，两道闸都开才放行）
    - `server/services/code_graph/model.py:88-131`（`_STATIC_CONFIDENCE_SCORE`：`resolved=1.0` / `bare_name=0.3` / `chunk_level=0.3`；`cross_repo` 取 `match_confidence` 原值不入静态表）
    - `server/services/code_graph/model.py:168-191`（`BARE_NAME_BLACKLIST` —— 装配侧的第三道过滤，本相位不重复实现）
    - `server/services/code_graph/model.py:306-312`（`GraphMeta.include_low_confidence` 字段注释：声明的是**装配口径**，不是「图里真的有裸名边」）
  </read_first>
  <action>
在 `impact.py` 中把 D-08 的双闸落成一个**独立的、可单测的**私有谓词 `_bare_name_allowed(*, include_low_confidence: bool, min_confidence: float) -> bool`，返回 `include_low_confidence and min_confidence <= confidence_score(EdgeConfidence.BARE_NAME)`。`analyze_impact` 把它的结果作为 `allow_bare_name` 传进 `_reverse_layers`。

谓词的 docstring 必须逐条解释为什么是**两道**而不是一道：`include_low_confidence` 决定的是「Phase 121 装配时有没有把裸名边装进图」（它是 `get_graph` 的缓存键分量之一），`min_confidence` 决定的是「本次查询愿不愿意看这一档」；单开任何一个都不足以放行，避免一次误配置把假阳性灾难放出来。⚠️ 措辞上不要出现会被自身验收 grep 命中的禁令字面量——本 plan 的验收一律用行为断言与 AST，不用 grep（Phase 121 有四个 plan 各踩过一次「验收 grep 命中自己写下的禁令散文」）。

同时补上 `min_confidence` 的两条语义，写进 `analyze_impact` 的参数说明：
- 过滤发生在**扩散**阶段而不是输出阶段——不合格的边不参与 BFS 推进，因此提高 `min_confidence` 一定让结果集**单调收缩**（可测性质）。
- `cross_repo` 档参与比较时用 `match_confidence` **原值**，⛔ 不归一化到本仓档位数值（D-13）——跨仓边的可信度来源与本仓完全不同，混在一起比较是错的。
  </action>
  <acceptance_criteria>
    - `test_min_confidence_filter` 落地：在 `known_topology` 上取 `min_confidence ∈ [0.0, 0.3, 0.7, 1.0]` 四档，断言结果 `symbol_id` 集合**逐档单调收缩**（后一档是前一档的子集）；`min_confidence=0.7` 时 F 在结果内且 `path_confidence == 0.7`，`min_confidence=0.71` 时 F 不在结果内（证明用的是 `match_confidence` 原值参与比较，不是被归一化成某个档位常量）
    - `test_bare_name_requires_both_gates` 落地，四种组合逐点断言：`(include_low_confidence=False, min_confidence=0.3)` ⇒ C→B 的 bare_name 边**不**参与扩散；`(True, 1.0)` ⇒ 同样不参与；`(False, 1.0)` ⇒ 不参与；`(True, 0.3)` ⇒ 参与。⚠️ 断言的观察点必须选一个**只能经 bare_name 边到达**的节点（`known_topology` 中需为此新增一个只有 `X --bare_name--> B` 一条入边的节点 `X`，若 122-01 未含则本 task 在 conftest 补上并同步更新 `test_known_topology_fixture_is_frozen` 的节点数断言）
    - `cd server && uv run python -c "from services.code_graph.impact import _bare_name_allowed as f; assert f(include_low_confidence=True, min_confidence=0.3) and not f(include_low_confidence=True, min_confidence=0.31) and not f(include_low_confidence=False, min_confidence=0.0)"` 退出码 0
  </acceptance_criteria>
  <verify>
    <automated>cd server && uv run pytest tests/services/code_graph/test_impact.py -k "min_confidence_filter or both_gates or known_topology_fixture" -q</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: 确定性风险四级（含 D-29 封顶）与截断 summary（IMPACT-04）</name>
  <read_first>
    - `.planning/phases/122-impact-trace/122-CONTEXT.md` D-15（阈值表：CRITICAL = d1 ≥ 20 或（穿仓 且 d1 ≥ 5）；HIGH = d1 ≥ 8 或穿仓；MEDIUM = d1 ≥ 3；LOW = 其余）与 **D-29**（封顶规则：全部到达路径的最高置信档只到 `bare_name` 时封顶 MEDIUM，**在阈值表之后**生效）
    - `.planning/phases/122-impact-trace/122-CONTEXT.md` D-16（上限 200，按「深度升序 + 置信度降序」排序后截断，`summary` 给 `total_found` / `returned` / `truncated_by_depth`）
    - `server/services/code_graph/model.py:193-224`（`LOW_RESOLUTION_THRESHOLD` 的常量注释形态：阈值 + 逐条校准记录 + 重新校准命令。⚠️ 本 plan 的阈值**没有**校准记录，docstring 必须反过来写明「未经真实数据校准的初值」，照抄格式、⛔ 不照抄「已校准」的语气）
    - `.planning/phases/122-impact-trace/122-RESEARCH.md` Pitfall 6（生产 fan-in：p50=1 / p99=25 / max=2,803，200 条截断必然触发）
  </read_first>
  <action>
在 `impact.py` 补三块内容。

① `class RiskLevel(str, Enum)`：`LOW` / `MEDIUM` / `HIGH` / `CRITICAL`。

② 模块级阈值常量表 `RISK_THRESHOLDS: Final[Mapping[str, int]]`，键为 `critical_d1`(20) / `critical_cross_repo_d1`(5) / `high_d1`(8) / `medium_d1`(3)，紧邻一段**逐条解释每个数从哪来**的注释，并**如实标注**：这四个数是**未经真实数据校准的初值**，校准需要工具上线后的真实使用样本，届时照 121-10 的复校范式回来改（`LOW_RESOLUTION_THRESHOLD` 由 0.6 校到 0.10 就是那个范式）。

③ 公开纯函数 `grade_risk(*, d1_count: int, crosses_repo: bool, best_path_tier: int) -> RiskLevel`：
- 先按 D-15 的阈值表算出初判（顺序：CRITICAL → HIGH → MEDIUM → LOW）。
- 再应用 **D-29 封顶**：若 `best_path_tier <= _CONFIDENCE_TIER_RANK[EdgeConfidence.BARE_NAME]`（即全部到达路径上都没有出现过 `resolved` 或 `cross_repo` 档），把结果封顶为 `RiskLevel.MEDIUM`，不得判 HIGH/CRITICAL。docstring 逐条解释：弱证据不该产生强告警，否则裸名边的假阳性会直接变成 CRITICAL 噪音——正是 Pitfall 1 要防的事。同时说明它**修正**了 D-15 的自相矛盾（原表列了三个输入却只用了两个），第三个输入自此有真实判据，不再是「收了却不用的参数」。
- `analyze_impact` 调它时，`best_path_tier = max(item.path_top_tier for item in 全部结果)`，无结果时取 `_CONFIDENCE_TIER_RANK[EdgeConfidence.CHUNK_LEVEL]`；`crosses_repo` 由调用方（壳层）透传。
- 输出里同时给 `risk_inputs = {"d1_count","crosses_repo","best_path_tier","capped_by_weak_evidence": bool}`——让 agent 能看懂这个等级是怎么算出来的（阈值可解释是 IMPACT-04 的明文要求）。

④ 截断与 summary：`DEFAULT_RESULT_LIMIT: Final[int] = 200`。在**截断之前**先按 `(depth 升序, path_confidence 降序, symbol_id 升序)` 排序（第三键保证稳定），再取前 `limit` 条。`summary` 给：
`{"total_found": int, "returned": int, "truncated_by_depth": {1: n1, 2: n2, 3: n3}, "truncated_by_nodes": bool, "result_limit": int}`，其中 `truncated_by_depth` 记**每一层被截掉的条数**（不是每层总数）。`truncated_by_nodes` 来自 Task 1 的 `max_nodes` 软上限。docstring 写明「排序键必须在截断之前算完，否则截掉的可能正是最该看的」。

⑤ 输出顶层再补两个字段位：`"affected_processes": []`（Phase 126 回填的叙事层，本相位**只预留字段位、不实现**——注释写明归属）与 `"cross_repo": []`（由壳层 122-06/122-07 填充；🚨 内核**绝不**根据图内 `kind == "cross_repo"` 的边往这里放东西，理由见 Task 的 read_first 与 122-06）。
  </action>
  <acceptance_criteria>
    - `test_risk_levels` 参数化落地，在 `d1_count ∈ {2, 3, 7, 8, 19, 20}` × `crosses_repo ∈ {False, True}` 上逐点断言 `grade_risk` 的返回：不穿仓时 2→LOW / 3→MEDIUM / 7→MEDIUM / 8→HIGH / 19→HIGH / 20→CRITICAL；穿仓时 2→HIGH / 3→HIGH / 7→CRITICAL / 8→CRITICAL / 20→CRITICAL（`best_path_tier` 取 RESOLVED 档，不触发封顶）
    - 同一用例另加封顶分支：`best_path_tier = _CONFIDENCE_TIER_RANK[EdgeConfidence.BARE_NAME]` 时，`d1_count=50, crosses_repo=True` 的返回**必须**是 `RiskLevel.MEDIUM`；把 tier 提到 `CROSS_REPO` 后同样输入返回 `RiskLevel.CRITICAL`（证明封顶规则真的在起作用而不是恒假）
    - `test_truncation_summary` 落地，用 `hub_topology(fan_in=250)`：断言 `summary["total_found"] >= 250`、`summary["returned"] == 200`、`sum(summary["truncated_by_depth"].values()) == total_found - returned`；把 `limit=10` 时断言返回的 10 条**全部**是 d1 且 `path_confidence` 非递增（证明排序在截断之前生效）
    - `cd server && uv run python -c "from services.code_graph.impact import RISK_THRESHOLDS, grade_risk, RiskLevel; import inspect; d=inspect.getdoc(grade_risk) or ''; assert '未经真实数据校准' in (open('services/code_graph/impact.py').read()); assert set(RISK_THRESHOLDS)=={'critical_d1','critical_cross_repo_d1','high_d1','medium_d1'}"` 退出码 0
    - `cd server && uv run pytest tests/services/code_graph/test_impact.py -q` 结果为 `8 passed, 1 skipped`（剩余 1 个 `test_graph_cross_repo_edges_are_intra_repo` 归 122-06）
  </acceptance_criteria>
  <verify>
    <automated>cd server && uv run pytest tests/services/code_graph/test_impact.py -q</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 内核 ↔ 图对象 | 只读冻结 `MultiDiGraph`；图是**跨请求共享**对象，就地修改即污染其他调用方 |
| 内核 ↔ 输出契约 | 置信度档位与 reason 在这一层定型，中间任何一层抹平即 Pitfall 1 复现 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-122-遍历 DoS | Denial of Service | `_reverse_layers` | mitigate | `max_depth=3`（D-05）+ `max_nodes=2000` 软上限 + `limit=200` 输出截断（D-16）+ D-08 双闸让 bare_name 不易被误开；三重预算各有独立标记 |
| T-122-日志放大 | Denial of Service | `_log_impact_analyzed` | mitigate | 一次 `analyze_impact` 恰一条 DEBUG `sampling` 事件；BFS 循环内零 `logger.*`（AST 契约 + 级别纪律） |
| T-122-exclusion 回流 | Information Disclosure | `analyze_impact` 输出 | mitigate | 被排除文件的符号在 Phase 121 **装配阶段**就不入图（`loader.py:401`）；本内核不做二次过滤（多余且会给人「这里也管」的错觉），端到端回归归 122-08 |
| T-122-空图误导 | Tampering（结论被污染） | `analyze_impact` | mitigate | 内核不 catch 任何 `GraphError`（D-03，它根本不取图）；`total_found == 0` 与「查询失败」在输出上是两种形态，翻译归壳层 |
| T-122-半新图误导 | Tampering | `analyze_impact` 输出 | transfer | 内核不感知水位；`partial_edges` / `degraded` 由壳层从 `GraphMeta` 透出（D-23，归 122-05） |
| T-122-绕闸 | Elevation of Privilege | `impact.py` | mitigate | 运行期零 Django/ORM import（AST 断言）；图只能由壳层经 `get_graph` 传入 |
| T-122-SC | Tampering | npm/pip/cargo installs | accept | 零新增依赖（`122-RESEARCH.md` §Package Legitimacy Audit 表为空） |
</threat_model>

<verification>
- `cd server && uv run pytest tests/services/code_graph/test_impact.py -q` → 8 passed / 1 skipped（**零 DB**，命令不带 `--reuse-db` 也必须通过）
- `cd server && GALAXY_CACHE_WARM_ON_STARTUP=False GRAPH_BUILD_ORPHAN_RECONCILE_ON_STARTUP=False uv run pytest tests/services/code_graph/test_access.py -k "observability or upper_layer" -q --reuse-db` 通过（新模块自动进 AST 观测契约 glob）
- `cd server && uv run ruff check services/code_graph/ && uv run mypy services/code_graph/` 本 plan 文件零错误
- `git diff --name-only` 不含 `server/services/code_graph/loader.py`（D-25：⛔ 不许改建边口径）与 `server/codegraph/services/repo_router_v2.py`
</verification>

<success_criteria>
- 在 `known_topology` 上，d1/d2/d3 的成员集合、每条边的 `confidence`/`reason`/`path_confidence` 均可逐点核对且全部命中
- 风险四级在 d1 = 2/3/7/8/19/20 六个边界 × 穿仓两态上逐点正确，且 D-29 封顶在弱证据下真的把 CRITICAL 压成 MEDIUM
- 250 扇入的 hub 上截断触发，三个计数自洽（`total_found - returned == Σ truncated_by_depth`）
- 内核全程未复制图、未修改图，冻结图在调用后仍冻结
</success_criteria>

## Artifacts this phase produces

| 类型 | 名称 | 位置 |
|---|---|---|
| 模块（新建） | `services/code_graph/impact.py` | `server/services/code_graph/impact.py` |
| 常量 | `DEFAULT_MAX_DEPTH=3` / `DEFAULT_RESULT_LIMIT=200` / `DEFAULT_MAX_NODES=2000` | 同上 |
| 常量表 | `DEPTH_LABELS`（1→WILL_BREAK / 2→LIKELY_AFFECTED / 3→MAY_NEED_TESTING） | 同上 |
| 常量表 | `RISK_THRESHOLDS`（critical_d1 / critical_cross_repo_d1 / high_d1 / medium_d1） | 同上 |
| 常量表（私有） | `_CONFIDENCE_TIER_RANK` / `_TEST_PATH_HINTS` | 同上 |
| 事件名常量 | `_EVENT_IMPACT_ANALYZED = "code_graph_impact_analyzed"` | 同上 |
| Enum | `RiskLevel`（LOW / MEDIUM / HIGH / CRITICAL） | 同上 |
| dataclass（私有） | `_Reach`（depth / path_confidence / path_top_tier / via） | 同上 |
| 函数 | `analyze_impact(graph, seed_symbol_id, *, max_depth, min_confidence, include_low_confidence, limit, max_nodes, exclude_test_files, crosses_repo)` | 同上 |
| 函数 | `grade_risk(*, d1_count, crosses_repo, best_path_tier)` | 同上 |
| 私有函数 | `_edge_score` / `_reverse_layers` / `_bare_name_allowed` / `_log_impact_analyzed` | 同上 |
| 输出字段位（预留） | `affected_processes`（Phase 126 回填）/ `cross_repo`（122-06 填充） | `analyze_impact` 返回 dict |

<output>
Create `.planning/phases/122-impact-trace/122-03-SUMMARY.md` when done
</output>
