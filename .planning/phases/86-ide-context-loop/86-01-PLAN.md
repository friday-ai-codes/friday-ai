---
phase: 86-ide-context-loop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/initiatives/services/memory_service.py
  - server/initiatives/services/project_doc_service.py
  - server/mcp_tools/views.py
  - server/mcp_tools/serializers.py
  - .planning/observability/LOGGING-SPEC.md
  - server/tests/initiatives/test_memory_hook_writeback.py
  - server/tests/mcp_tools/test_report_project_knowledge.py
autonomous: true
requirements: [HOOK-02]
must_haves:
  truths:
    - "stop hook 经 report_project_knowledge 以 active 模式调用时，MEMORY 内容直接写入 active ProjectMemory（不落 draft、不需人工确认）——用户授权的 accepted deviation"
    - "RESEARCH 内容以 active 模式直接 append 到该项目 RESEARCH ProjectDoc 的人工区（不落 draft）"
    - "三道兜底不可绕过：质量门槛过滤（低质/空/重复不写）+ 脱敏（redact_secrets_in_text 入库前强制）+ 审计可回滚（每次自动写入 emit AuditEvent，可经 supersede/remove 撤销）"
    - "归因：触发用户经 request.user 绑定 initiated_by_user_id；无法解析为项目成员 → 静默跳过不写（绝不抛、绝不阻断编码）"
    - "hook 无 PAT（未认证）/ 未绑定项目 / 非成员 → 静默跳过（HTTP 200 accepted=false），绝不 block"
    - "默认行为保持向后兼容：不带 active 标记的调用仍走既有 draft 路径（CURSOR-03 不回退）"
  artifacts:
    - path: "server/initiatives/services/memory_service.py"
      provides: "MemoryService.record_hook_writeback —— active 直写 + 非成员静默跳过 + 脱敏 + 审计"
      contains: "record_hook_writeback"
    - path: "server/mcp_tools/views.py"
      provides: "ReportProjectKnowledgeView active 写回模式（target=memory/research）+ 可选 ide_hook_distill"
      contains: "writeback_mode"
  key_links:
    - from: "server/mcp_tools/views.py"
      to: "initiatives.services.MemoryService.record_hook_writeback"
      via: "active 模式直写 active 记忆（绕过 draft，保留三道兜底）"
      pattern: "record_hook_writeback"
    - from: "server/mcp_tools/views.py"
      to: "initiatives.services.ProjectDocService"
      via: "RESEARCH active append 到 ProjectDoc 人工区"
      pattern: "ProjectDocService"
    - from: "server/mcp_tools/views.py"
      to: "services.cursor_writeback.evaluate_writeback_quality"
      via: "active 写回前复用质量门槛（不可绕过）"
      pattern: "evaluate_writeback_quality"
---

<objective>
落地 HOOK-02 写路径的**用户授权范围变更（accepted deviation, 2026-06-26）**：stop hook 组织上下文+用户改动后经 MCP `report_project_knowledge` 以 **active 模式**调用时，MEMORY/RESEARCH **直接写入生效（active），不经 draft 人工确认**——覆盖 REQUIREMENTS HOOK-02「落 draft 人工确认」与 Out-of-Scope「记忆全自动写入本期不做」。本 plan 只做服务端写回逻辑；stop hook 客户端脚本资产在 86-05 落地。

**强制保留的三道兜底（绝不绕过）**：质量门槛过滤（复用 `evaluate_writeback_quality`：低质/空/重复不写）+ 脱敏不可绕过（`redact_secrets_in_text` 入库前强制）+ 审计可回滚（每次自动写入 emit AuditEvent，可撤销）；并叠加归因（`request.user`→`initiated_by_user_id`，非成员/未解析 → 静默跳过）。hook 无 PAT / 未绑项目 / 非成员 → 静默跳过 200，绝不抛、绝不阻断编码。

Purpose: 兑现 CONTEXT「真·全自动直写生效（active）无需任何确认」的写路径，同时把防污染兜底做死。
Output: `MemoryService.record_hook_writeback`（active 直写）+ RESEARCH active append + `ReportProjectKnowledgeView` active 模式 + 序列化器扩展 + 可选 `ide_hook_distill` call_source 登记 + 守护测试。
</objective>

<execution_context>
@.planning/phases/86-ide-context-loop/86-CONTEXT.md
@.planning/project-workspace/MILESTONE-PROPOSAL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.cursor/rules/observability-logging.mdc
@server/initiatives/services/memory_service.py
@server/mcp_tools/views.py
@server/services/cursor_writeback.py
@server/initiatives/services/project_doc_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: MemoryService.record_hook_writeback —— MEMORY active 直写 + RESEARCH active append（三道兜底 + 非成员静默跳过）</name>
  <read_first>
    - server/initiatives/services/memory_service.py（`append` 89-122 现含成员校验 fail-closed + `redact_secrets_in_text` + 初始 revision + `_emit` 审计 + `_schedule_doc_push`/`_schedule_materialization`；`_is_member_sync` 72-77；`_emit` 463-489 审计入口；`create_draft` 288-321 现 draft 路径）
    - server/services/cursor_writeback.py（`evaluate_writeback_quality` 109-124 异步质量门槛入口，纯逻辑 best-effort）
    - server/common/logging.py（`redact_secrets_in_text` 签名——脱敏不可绕过）
    - server/initiatives/services/project_doc_service.py（`ProjectDocService.upsert_doc` 80-94 幂等容器；`_emit`/AuditService 审计范式；研究文件 doc_type=research 的人工区写入落点）
    - server/initiatives/models/project_doc.py（`ProjectDoc.last_synced_snapshot` 正文 + DocType.RESEARCH）
    - server/feishu/services/identity.py（`resolve_feishu_user` 28-42 归因 fail-soft，未映射 None）
  </read_first>
  <action>
    在 `MemoryService` 新增 `async def record_hook_writeback(self, *, project_id, content, contributor, initiated_by_user_id=None) -> dict`（accepted deviation 入口，区别于 `create_draft`）：
    - **非成员静默跳过**（绝不抛，区别于 `append` 的 `MemoryPermissionError`）：`_is_member_sync(project_id, contributor)` 为假 → 返回 `{"applied": False, "reason": "not_member"}`，不写任何表（hook 绝不阻断编码）。
    - **脱敏不可绕过**：复用 `append` 的 `redact_secrets_in_text` 路径——直接调既有 `append(..., _skip_member_check=True)`（成员校验本入口已做），保证落库内容脱敏 + 初始 revision（MEM-03 可追溯，天然可回滚）+ `_emit` 审计（ACTION_PROJECT_MEMORY_CREATED）+ 物化钩子复用。返回 `{"applied": True, "memory_id": str(memory.id)}`。
    - **审计可回滚**：append 已 `_emit` 审计；撤销路径复用既有 `supersede`（status→superseded，留 revision）。在 docstring 标注「accepted deviation：active 直写，撤销经 supersede」。

    在 `ProjectDocService` 新增 `async def append_research_note(self, *, project_id, content, contributor, initiated_by_user_id=None) -> dict`：
    - 非成员（复用与 MemoryService 同口径成员判定，或经 `ProjectMember` 查询）→ 返回 `{"applied": False, "reason": "not_member"}` 静默跳过。
    - 经 `upsert_doc(project_id, DocType.RESEARCH, ...)` 取/建 RESEARCH 容器，把脱敏后的 content **append**（绝不就地覆盖既有正文，与 MILESTONE-PROPOSAL §4.2「Agent 写一律 append」一致）到人工区/AI 建议区正文（`last_synced_snapshot` 追加段），经 `AuditService.aemit`（component=initiatives, category=caller, initiated_by_user_id）落审计（可回滚）。脱敏经 `redact_secrets_in_text`。返回 `{"applied": True}`。

    两入口都是 INV-6 单一写入收口（不旁路写表）；归因 `initiated_by_user_id`，未提供 → 取 contributor.id，仍无 → "system"。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/initiatives/test_memory_hook_writeback.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `MemoryService.record_hook_writeback` 成员调用 → 写入一条 **active** `ProjectMemory`（status=active）+ 一条初始 `ProjectMemoryRevision` + 一条 AuditEvent（ACTION_PROJECT_MEMORY_CREATED）；内容已脱敏（含密钥的输入入库后无明文）。
    - 非成员调用 `record_hook_writeback` / `append_research_note` → 返回 `applied=False`、不写任何表、**不抛异常**。
    - `ProjectDocService.append_research_note` 成员调用 → RESEARCH ProjectDoc 正文 append 新段（既有正文保留，非覆盖）+ AuditEvent。
    - 撤销路径存在：写入的 active 记忆可经 `MemoryService.supersede` 置 superseded（审计可回滚）。
  </acceptance_criteria>
  <done>active 直写入口落地（MEMORY 经 append active / RESEARCH append-only），非成员静默跳过、脱敏+审计齐备、可回滚，测试全绿。</done>
</task>

<task type="auto">
  <name>Task 2: ReportProjectKnowledgeView active 写回模式 + 序列化器扩展 + 可选 ide_hook_distill（call_source 登记）+ 守护测试</name>
  <read_first>
    - server/mcp_tools/views.py（`ReportProjectKnowledgeView` 2721-2810：`_begin`(认证/归因) + `_validate` + 质量门槛 `evaluate_writeback_quality` + 既有 draft 路径 `MemoryService.create_draft` + `_active_memory_contents` 2802-2810 + `_record`）
    - server/mcp_tools/serializers.py（`ReportProjectKnowledgeRequestSerializer` 493-500，当前仅 project_id/content/source_conversation_id）
    - server/services/cursor_writeback.py（`evaluate_writeback_quality` 复用，不改）
    - .planning/observability/LOGGING-SPEC.md（§4.1 call_source 枚举表 62-91：现有 `memory_distill`(Phase 80) 为 draft 提炼；新增 LLM 调用点必须登记一个 call_source）
    - server/initiatives/services/memory_distiller.py（若存在：Phase 80 MemoryDistiller，call_source=memory_distill 的 LLM 提炼分析范式，可作 ide_hook_distill 的 analog；不存在则 distill 设为可选直透）
  </read_first>
  <action>
    扩展 `ReportProjectKnowledgeRequestSerializer`：新增**可选** `writeback_mode`（CharField，choices=`draft`/`active`，default=`draft`——向后兼容 CURSOR-03）+ 可选 `target`（choices=`memory`/`research`，default=`memory`）+ 可选 `distill`（Boolean，default=False）。

    改 `ReportProjectKnowledgeView.post`：
    - `writeback_mode != "active"` → **逐字保持现有 draft 行为**（`create_draft`，CURSOR-03 不回退）。
    - `writeback_mode == "active"`（stop hook 路径，accepted deviation）：
      1. **未认证 / request.user 非真实用户** → 返回 `{"accepted": False, "reason": "unauthenticated", ...}` 200，静默跳过（hook 绝不被阻断；`_begin` 已处理认证，此处对 anonymous 兜底）。
      2. **质量门槛**（复用 `_active_memory_contents` + `evaluate_writeback_quality`）不过 → `accepted=False` 200（与现 draft 路径一致）。
      3. **可选 distill**（`distill=True` 且配置启用时）：调一个 best-effort LLM「组织上下文+用户改动 → 精炼记忆条目」的 helper（reuse `MemoryDistiller` 提炼基建 / 新建轻量 prompt），**赋 call_source `ide_hook_distill`** 并上报请求/token/TTFT/上游错误码；失败回退原始 content（绝不反噬）。distill 后内容再过质量门槛。
      4. `target == "memory"` → `MemoryService().record_hook_writeback(...)`；`target == "research"` → `ProjectDocService().append_research_note(...)`。`applied=False`（非成员）→ `accepted=False`、`reason="not_member"` 200。
      5. `applied=True` → `accepted=True` + 返回 `memory_id`/写入标识 200。
    - 全程 fail-soft：active 路径任何异常 → 捕获后返回 `accepted=False` 200，绝不 5xx、绝不阻断编码（保持 MCP 工具契约）。

    在 `.planning/observability/LOGGING-SPEC.md` §4.1 枚举表新增一行：`| ide_hook_distill | report_project_knowledge active 模式蒸馏 | stop hook 组织上下文→精炼记忆，单轮，best-effort |`（仅在引入 distill LLM 时生效；本 plan 登记以满足「新增 LLM 必须赋 call_source」）。

    在 `test_report_project_knowledge.py` 扩测：active+成员→写 active 记忆（断言 status=active、有审计、含密钥输入脱敏后无明文）；active+非成员→accepted=false 不写不抛；active+低质→accepted=false；draft 默认路径不回退（仍落 draft）；active+target=research→RESEARCH 正文 append；未认证 active→200 静默。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/mcp_tools/test_report_project_knowledge.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `POST /api/mcp/tools/report_project_knowledge/` 带 `writeback_mode=active` + 成员 PAT → 写入 **active** 记忆（非 draft），返回 `accepted=true` + `memory_id`，HTTP 200。
    - 不带 `writeback_mode`（或 `draft`）→ 行为与现状逐字一致（落 pending draft，HTTP 201），CURSOR-03 不回退。
    - active + 非成员 / 未认证 / 低质 / 重复 → `accepted=false` + reason，HTTP 200，不写库、不抛异常。
    - `target=research` + active + 成员 → RESEARCH ProjectDoc 正文新增 append 段（既有内容保留）。
    - 质量门槛与脱敏在 active 路径同样生效（断言含密钥输入入库后无明文；空/重复内容被拒）。
    - LOGGING-SPEC §4.1 含 `ide_hook_distill` 行（若启用 distill LLM）。
  </acceptance_criteria>
  <done>report_project_knowledge active 写回模式落地（MEMORY/RESEARCH 直写生效），三道兜底 + 归因 + fail-soft 齐备，draft 默认不回退，测试全绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IDE stop hook（半可信）→ report_project_knowledge active 写回 | PAT 认证用户经 hook 自动直写 active 共享记忆，须三道兜底防污染 |
| 上报正文 → ProjectMemory/ProjectDoc 入库 + 审计留痕 | 正文/异常文本须脱敏不可绕过 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-01-01 | Tampering | active 直写共享记忆（accepted deviation） | mitigate | 质量门槛（`evaluate_writeback_quality` 低质/空/重复拒收）+ 成员校验静默跳过 + 审计可回滚（supersede）三道兜底不可绕过 |
| T-86-01-02 | Information Disclosure | 上报正文/distill 入库 | mitigate | `redact_secrets_in_text` 入库前强制（`append`/`append_research_note` 内置，调用方无法绕过）；审计 before/after 经 AuditService `_redact_audit_payload` |
| T-86-01-03 | Elevation of Privilege | 非成员经 hook 写 active | mitigate | `record_hook_writeback`/`append_research_note` 成员校验 fail-closed → 非成员静默跳过不写（绝不抛、不阻断编码） |
| T-86-01-04 | Denial of Service | hook 写回失败反噬编码 | mitigate | active 路径全 fail-soft：异常/未认证/未绑项目 → 200 accepted=false，绝不 5xx、绝不 block |
| T-86-01-05 | Repudiation | 自动写入无归因 | mitigate | `initiated_by_user_id` 绑定（request.user，未解析 system）+ 每次写入 emit AuditEvent |
</threat_model>

## Artifacts this phase produces (Plan 01)

- **新方法（memory_service.py）**：`MemoryService.record_hook_writeback`（active 直写 + 非成员静默跳过 + 复用 append 脱敏/审计/物化）
- **新方法（project_doc_service.py）**：`ProjectDocService.append_research_note`（RESEARCH active append-only + 审计）
- **改动符号（views.py）**：`ReportProjectKnowledgeView.post` 增 active 写回分支（`writeback_mode`/`target`/`distill`）+ fail-soft 包裹
- **改动序列化器（serializers.py）**：`ReportProjectKnowledgeRequestSerializer` 新增可选 `writeback_mode`/`target`/`distill`
- **新 call_source**：`ide_hook_distill`（登记 LOGGING-SPEC §4.1；仅启用 distill LLM 时上报）
- **复用（不重造）**：`evaluate_writeback_quality`（质量门槛）、`redact_secrets_in_text`（脱敏）、`AuditService.aemit`（审计可回滚）、`MemoryService.append`/`supersede`
- **新测试**：`tests/initiatives/test_memory_hook_writeback.py`、扩充 `tests/mcp_tools/test_report_project_knowledge.py`

<verification>
- `cd server && uv run pytest tests/initiatives/test_memory_hook_writeback.py tests/mcp_tools/test_report_project_knowledge.py -q` 全绿。
- `cd server && uv run ruff check server/initiatives/services/memory_service.py server/initiatives/services/project_doc_service.py server/mcp_tools/views.py` 无新增 lint。
- accepted deviation 已在 plan/服务 docstring 标注，供 VERIFICATION 记为「用户授权偏离」并核三道兜底齐备。
</verification>

<success_criteria>
- stop hook active 模式 → MEMORY/RESEARCH 直写生效（active），不经 draft 人工确认（HOOK-02 accepted deviation）。
- 质量门槛 + 脱敏不可绕过 + 审计可回滚 + 归因四重兜底齐备；非成员/未认证/未绑项目静默跳过不阻断编码。
- draft 默认路径（CURSOR-03）不回退。
</success_criteria>

<output>
Create `.planning/phases/86-ide-context-loop/86-01-SUMMARY.md` when done.
</output>
