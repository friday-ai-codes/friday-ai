---
phase: 86-ide-context-loop
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - server/initiatives/services/ide_hook_assets.py
  - server/initiatives/services/cursor_rules.py
  - server/initiatives/views.py
  - server/initiatives/urls.py
  - server/initiatives/serializers.py
  - server/tests/initiatives/test_ide_hook_assets.py
autonomous: true
requirements: [HOOK-01]
must_haves:
  truths:
    - "三家（Cursor / Claude Code / Codex）各生成一条 always-on 规则，强制「先用分支名经 MCP lookup_project_by_branch 反查项目 + 召回上下文，再编码」——三家通用读路径"
    - "Claude Code 额外产出 UserPromptSubmit 注入 hook 资产（脚本 + settings 片段），调 lookup_project_by_branch 并把召回上下文经 stdout 注入（增强）"
    - "Cursor beforeSubmitPrompt 不能注入上下文 → 不产注入 hook，读路径只靠 always-on 规则 + MCP（资产/文档显式说明此限制）"
    - "读路径新召回经 lookup_project_by_branch 写 RetrievalTrace（Phase 85 既有），注入 hook 复用该工具不另起裸召回"
    - "下发：扩展项目 cursor-rules 端点为按 runtime 取读路径资产 bundle（复制/下载），未绑项目/无 PAT 时资产含静默降级说明"
  artifacts:
    - path: "server/initiatives/services/ide_hook_assets.py"
      provides: "build_read_path_assets(project, runtime) —— 三家 always-on 规则 + CC UserPromptSubmit 注入脚本/settings"
      contains: "build_read_path_assets"
    - path: "server/initiatives/views.py"
      provides: "ProjectIdeHookAssetsView —— 按 runtime 下发读路径资产 bundle"
      contains: "ProjectIdeHookAssetsView"
  key_links:
    - from: "server/initiatives/services/ide_hook_assets.py"
      to: "initiatives.services.cursor_rules.build_project_cursor_rules"
      via: "复用既有 Cursor 规则正文生成，扩展到三家 + 注入 hook"
      pattern: "build_project_cursor_rules"
    - from: "server/initiatives/views.py"
      to: "initiatives.services.ide_hook_assets.build_read_path_assets"
      via: "端点下发 runtime 读路径资产"
      pattern: "build_read_path_assets"
---

<objective>
落地 HOOK-01 读路径架构：**主走 MCP `lookup_project_by_branch`（Phase 85 多绑定）+ 一条 always-on 规则（Cursor/Claude Code/Codex 三家通用，强制「先反查项目 + 召回再编码」）**；Claude Code **额外**用 `UserPromptSubmit` 自动注入做增强。Cursor `beforeSubmitPrompt` 不能注入上下文 → **不押注入在 Cursor hook 上**，读路径靠规则 + MCP。本 plan 产出三家读路径资产（规则 + CC 注入脚本/settings）+ 按 runtime 下发端点；写路径 stop hook 资产在 86-05。

读路径 MCP 反查/注入复用 Phase 85 `lookup_project_by_branch`（已写 `RetrievalTrace` + 单/多/无命中 fail-soft 候选），不另起裸召回。

Purpose: 让三家 IDE 在分支开发时自动「先反查项目 + 召回再编码」，CC 再叠注入增强。
Output: `ide_hook_assets.build_read_path_assets`（三家规则 + CC UserPromptSubmit 资产）+ 下发端点/序列化器 + 测试。
</objective>

<execution_context>
@.planning/phases/86-ide-context-loop/86-CONTEXT.md
@.planning/project-workspace/MILESTONE-PROPOSAL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.cursor/rules/observability-logging.mdc
@server/initiatives/services/cursor_rules.py
@server/initiatives/views.py
@server/initiatives/urls.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: ide_hook_assets —— 三家 always-on 规则 + Claude Code UserPromptSubmit 注入资产（复用 cursor_rules）</name>
  <read_first>
    - server/initiatives/services/cursor_rules.py（`build_project_cursor_rules` 26-80：现 Cursor `.mdc`（frontmatter `alwaysApply: true`）强制「先 lookup_project_by_branch + 召回 → 再编码 → 完成上报」正文；`cursor_rules_filename` 21-23）
    - server/mcp_tools/views.py（`LookupProjectByBranchView` 2571+ / `_lookup_by_branch_binding` 2700-2718：读路径反查契约——branch_name[+repository_id] → matched/candidates/context + RetrievalTrace；CC 注入脚本据此调用）
    - server/mcp_tools/urls.py（`tools/lookup_project_by_branch/` 路由名——注入脚本/规则正文引用此端点）
    - .planning/project-workspace/MILESTONE-PROPOSAL.md（§7 三家差异 + §10 调研：Cursor beforeSubmitPrompt 不能注入；CC UserPromptSubmit 可注入；Codex hook 弱按仅 MCP+rules）
    - .cursor/rules/observability-logging.mdc（资产正文须强调脱敏：绝不上报凭证/密钥/token）
  </read_first>
  <action>
    新建 `server/initiatives/services/ide_hook_assets.py`，纯文本生成（项目对象传入，无 IO，可单测）。`RUNTIME` 常量枚举 `cursor` / `claude_code` / `codex`。

    `def build_read_path_assets(project, runtime: str) -> dict`：返回 `{"runtime", "files": [{"path", "filename", "content"}], "notes"}`：
    - **cursor**：复用 `build_project_cursor_rules(project)` 产 `.cursor/rules/friday-project-<id>.mdc`（alwaysApply）。`notes` 显式写「Cursor `beforeSubmitPrompt` 不能注入上下文，读路径靠本 always-on 规则 + MCP `lookup_project_by_branch`」。
    - **claude_code**：
      1. always-on 规则正文（复用同一「先反查+召回再编码」正文，输出为 Claude Code 约定的 `CLAUDE.md` 项目规则片段或 `.claude/rules` 片段——按 CC 规则约定）。
      2. **UserPromptSubmit 注入资产**：一段 hook 脚本模板（`.claude/hooks/friday-context-inject.sh`/`.py`）——读取当前 git 分支名，调 Friday MCP/REST `lookup_project_by_branch(branch_name=…, repository_id=可选)`（用用户 PAT），把返回 `context` 经 stdout 注入；matched=false 时按 candidates 提示、不阻断；**无 PAT/未绑项目/接口失败 → 静默 exit 0 不注入不阻断**。外加 `.claude/settings.json` 的 `hooks.UserPromptSubmit` 注册片段。
    - **codex**：always-on 规则（输出为 Codex 约定的 `AGENTS.md` 片段或规则文件）。`notes` 写「Codex hook 能力弱，按仅 MCP+rules 对待，不产注入 hook」。

    抽取三家共用的「强制流程」正文为内部 helper（复用 `cursor_rules` 现有措辞），按 accepted deviation 同步措辞：上报沉淀（86-02 active 直写）说明可与既有 cursor_rules 措辞解耦（cursor_rules 的「人工确认」措辞由 86-05 写路径资产统一更新，本 task 不改 cursor_rules 行为，仅复用读路径段）。

    所有资产正文含脱敏告诫：绝不上报凭证/密钥/token/个人敏感信息。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/initiatives/test_ide_hook_assets.py -k read_path -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `build_read_path_assets(project, "cursor")` 返回含 `.cursor/rules/*.mdc`（alwaysApply）正文含 `lookup_project_by_branch` 与「先反查+召回再编码」；notes 说明 beforeSubmitPrompt 不能注入。
    - `build_read_path_assets(project, "claude_code")` 返回含规则片段 + UserPromptSubmit 注入脚本（含 `lookup_project_by_branch` 调用 + 无 PAT/失败静默 exit 0）+ settings.json hooks 片段。
    - `build_read_path_assets(project, "codex")` 返回规则片段；notes 说明仅 MCP+rules。
    - 三家资产正文均含脱敏告诫；未知 runtime → 抛清晰错误或回退 cursor（任选其一，测试断言一致）。
  </acceptance_criteria>
  <done>三家读路径资产生成（CC 含 UserPromptSubmit 注入、Cursor/Codex 仅规则），复用 cursor_rules 措辞，脱敏告诫齐备，测试全绿。</done>
</task>

<task type="auto">
  <name>Task 2: 下发端点 ProjectIdeHookAssetsView（按 runtime 取读路径资产 bundle）+ 序列化器 + 测试</name>
  <read_first>
    - server/initiatives/views.py（`ProjectCursorRulesView` 927-945：取 project + `build_project_cursor_rules` + 返回 content/filename 的端点范式；权限/认证装配）
    - server/initiatives/urls.py（`<uuid:project_id>/cursor-rules/` 路由 145-147 注册范式）
    - server/initiatives/serializers.py（项目相关序列化器范式，加 runtime 查询参数校验）
    - server/initiatives/services/ide_hook_assets.py（Task 1 产 `build_read_path_assets`）
  </read_first>
  <action>
    新增 `ProjectIdeHookAssetsView(APIView)`（镜像 `ProjectCursorRulesView` 认证/取项目范式）：`GET /api/projects/<uuid:project_id>/ide-hook-assets/?runtime=cursor|claude_code|codex&kind=read`（`kind` 默认 read；write 由 86-05 扩展）。校验 runtime（无效 → 400），取 project → `build_read_path_assets(project, runtime)` → 返回 `{"runtime", "kind", "files": [...], "notes"}`，供前端「复制 / 下载」。读权限按既有项目读口径（public_org 可读 / members_only fail-closed，与项目读端点一致）。

    在 `urls.py` 注册路由 `project-ide-hook-assets`；如需查询参数校验在 `serializers.py` 加一个轻量 `IdeHookAssetsQuerySerializer`（runtime/kind choices）。

    在 `test_ide_hook_assets.py` 加端点测：三家 runtime 各取到 bundle（200，含 files）；无效 runtime → 400；members_only 项目对非成员 → 403/空（与项目读口径一致）；public_org 非成员可取。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/initiatives/test_ide_hook_assets.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `GET /api/projects/<id>/ide-hook-assets/?runtime=claude_code` 返回 200，`files` 含规则 + UserPromptSubmit 资产。
    - `runtime=cursor` / `runtime=codex` 各返回对应资产；无效 runtime → 400。
    - members_only 项目对非成员遵守项目读口径（不泄漏资产正文）；public_org 非成员可读。
    - 路由 `project-ide-hook-assets` 可 reverse。
  </acceptance_criteria>
  <done>读路径资产下发端点落地（按 runtime/kind），权限对齐项目读口径，测试全绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 资产下发端点 → 用户复制/下载 | 项目专属资产含 project_id，按项目读口径收口 |
| CC UserPromptSubmit 注入脚本 → Friday MCP 召回 | 脚本以用户 PAT 调 lookup，召回按成员权限收口（packer fail-closed） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-03-01 | Information Disclosure | 资产下发端点 | mitigate | 端点按项目读口径（members_only 非成员不可取）；资产仅含 project_id/规则正文，无凭证 |
| T-86-03-02 | Information Disclosure | CC 注入脚本召回 | mitigate | 召回经 `lookup_project_by_branch` → `pack_project_context` fail-closed（非成员零召回）；脚本以用户 PAT 鉴权，不内嵌任何密钥 |
| T-86-03-03 | Denial of Service | 注入脚本失败阻断编码 | mitigate | 脚本无 PAT/未绑项目/接口失败 → 静默 exit 0 不注入不阻断（fail-soft，资产模板内置） |
| T-86-03-04 | Tampering | 误把注入押在 Cursor hook | accept | 资产/notes 显式声明 Cursor beforeSubmitPrompt 不能注入，读路径走规则 + MCP（架构层规避） |
</threat_model>

## Artifacts this phase produces (Plan 03)

- **新模块（initiatives/services/ide_hook_assets.py）**：`build_read_path_assets`（三家 always-on 规则 + CC UserPromptSubmit 注入脚本/settings）+ `RUNTIME` 常量
- **新端点（initiatives/views.py）**：`ProjectIdeHookAssetsView`（`GET .../ide-hook-assets/?runtime=&kind=`）
- **新路由（initiatives/urls.py）**：`project-ide-hook-assets`
- **新序列化器（initiatives/serializers.py）**：`IdeHookAssetsQuerySerializer`（runtime/kind 校验）
- **复用（不重造）**：`cursor_rules.build_project_cursor_rules`（Cursor 规则正文）、`lookup_project_by_branch`（读路径反查 + RetrievalTrace，Phase 85）、`ProjectCursorRulesView` 端点范式
- **call_source**：本 plan 无新增 LLM 调用（读路径召回经既有 MCP 工具）；不涉及 §4.1 新值
- **新测试**：`tests/initiatives/test_ide_hook_assets.py`

<verification>
- `cd server && uv run pytest tests/initiatives/test_ide_hook_assets.py -q` 全绿。
- `cd server && uv run ruff check server/initiatives/services/ide_hook_assets.py server/initiatives/views.py` 无新增 lint。
- 路由 `project-ide-hook-assets` reverse 可解析。
</verification>

<success_criteria>
- 三家 always-on 规则强制「先反查项目 + 召回再编码」；CC 额外 UserPromptSubmit 注入增强（HOOK-01）。
- Cursor 注入不落空（架构层声明 beforeSubmitPrompt 限制，读路径走规则 + MCP）。
- 读路径召回写 RetrievalTrace（复用 lookup_project_by_branch）；资产下发按项目读口径，无 PAT/未绑项目静默降级。
</success_criteria>

<output>
Create `.planning/phases/86-ide-context-loop/86-03-SUMMARY.md` when done.
</output>
