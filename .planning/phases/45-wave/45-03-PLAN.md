---
phase: 45-wave
plan: 03
type: execute
wave: 3
depends_on: ["45-01", "45-02"]
files_modified:
  - server/tests/test_coding_wave.py
autonomous: true
requirements: [ARTIFACT-01, ARTIFACT-02]
must_haves:
  truths:
    - "端到端：wave1 后端仓 done（TaskResult 含 openapi 文件）→ aadvance 提取落 produced_artifacts → wave2 前端仓 dispatch → wave2 DispatchTask prompt/metadata 含 wave1 产出契约（SC-3 产物传递正确）"
    - "幂等：重复回调重复触发提取 → 覆盖写 no-op 语义，produced_artifacts 不漂移"
    - "fail-soft：提取异常 → wave 仍正确推进、下游注入段为空（零回归降级），容器回调不 5xx"
  artifacts:
    - path: "server/tests/test_coding_wave.py"
      provides: "D-13 端到端产物传递集成测试（扩充既有 wave 集成）"
      contains: "test_artifact_passthrough"
  key_links:
    - from: "server/tests/test_coding_wave.py"
      to: "wave2 DispatchTask.prompt / metadata"
      via: "捕获 dispatch 断言含 wave1 契约"
      pattern: "produced_artifacts|upstream"
---

<objective>
ARTIFACT-01 + ARTIFACT-02 端到端集成验收（SC-3）。在既有 `tests/test_coding_wave.py` 扩充：构造 wave1 后端仓 + wave2 前端仓（跨仓 `depends_on` 边），mock wave1 容器完成（`SubAgentSession` completed + `TaskResult` 含 openapi 文件）→ 经 `aadvance_coding_waves` 回填 done 触发 Plan 01 提取落 `produced_artifacts` → 推进 wave2 dispatch → 断言捕获的 wave2 `DispatchTask` 的 `prompt` / `metadata` 含 wave1 产出的契约文件名。覆盖 happy path（产物传递正确）+ 幂等/fail-soft（D-15）。

Purpose: 以 mock IO 边界（dispatcher / SubAgentSession / TaskResult）端到端验证「提取→注入」全链路；真实 runner+Docker E2E 沿用既有 deferred。
Output: `test_coding_wave.py` 新增端到端产物传递测试（happy path + 幂等 + fail-soft），复用既有 mock fixture。
</objective>

<execution_context>
@.planning/phases/45-wave/45-RESEARCH.md
@.planning/phases/45-wave/45-PATTERNS.md
@.planning/phases/45-wave/45-VALIDATION.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/45-wave/45-CONTEXT.md
@AGENTS.md
@.planning/phases/45-wave/45-01-PLAN.md
@.planning/phases/45-wave/45-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-13 端到端 happy-path 产物传递集成测试</name>
  <read_first>
    - server/tests/test_coding_wave.py（蓝本 `test_multi_wave_progression` line 247；mock fixture `_dispatched` line 66-86 / `_stub_provider_resolution` line 46-63；`_settle_session` line 172-185 已建 TaskResult；`_resume` line 188-190；`_dispatched_repo_ids` line 193-194 经 `t.metadata["repository_id"]` 取 → DispatchTask metadata 是断言面；`_make_plan_version`/`_make_repo` harness）
    - server/workflows/nodes/ai/coding.py（`_dispatch_next_wave` → `_dispatch_wave` → `_run_repo_coding` → `_build_coding_prompt` 注入链，Plan 02 已接线）
    - server/services/plan_orchestration/wave_progression.py（`_backfill_running_terminal` 提取钩子，Plan 01 已接线）
    - 45-PATTERNS.md test_coding_wave 扩展详解（line 391-416）+ 45-RESEARCH.md D-13（line 552）
  </read_first>
  <action>
    在 `test_coding_wave.py` 追加 `test_artifact_passthrough`（或等义命名）端到端集成测试，蓝本 `test_multi_wave_progression`。构造 wave1 后端仓 + wave2 前端仓，wave2 task `depends_on` wave1 task（跨仓边）。扩展 `_settle_session`（或就地构造）使 wave1 完成的 `TaskResult.modified_files` 含一个 openapi 契约文件（如 `api/openapi.yaml`）+ `pr_url`。流程：首发 dispatch wave1 → `_settle_session(wave1, ok=True)` → `_resume` 触发 `aadvance_coding_waves`（回填 wave1 done 触发提取落 `produced_artifacts` → 推进 wave2 dispatch）→ 捕获 wave2 `DispatchTask`（经既有 `_dispatched` mock）→ 断言 wave2 DispatchTask 的 `prompt` 含 wave1 契约文件名 `api/openapi.yaml`（且含「上游产物」段标题）。可附带从 DB `aget` wave1 task 断言其 `produced_artifacts["openapi"]` 含该文件（提取落库正确）。复用既有 `_stub_provider_resolution` / `_dispatched` / harness，不引入真实 runner/Docker。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/test_coding_wave.py::test_artifact_passthrough -x</automated>
  </verify>
  <acceptance_criteria>
    - source：`test_coding_wave.py` 新增端到端测试，构造 wave1+wave2 跨仓 depends_on，wave1 TaskResult.modified_files 含 openapi 文件
    - behavior：wave1 done 回填后 produced_artifacts 落库含 openapi 契约；wave2 dispatch 的 DispatchTask.prompt 含 wave1 契约文件名 + 「上游产物」段
    - test-command：`uv run pytest tests/test_coding_wave.py::test_artifact_passthrough -x` 全绿
    - 复用既有 mock fixture（_dispatched / _stub_provider_resolution / _settle_session），无真实 runner/Docker 依赖
  </acceptance_criteria>
  <done>端到端 happy-path 集成测试绿：wave1 done→提取落库→wave2 prompt 含上游契约。</done>
</task>

<task type="auto">
  <name>Task 2: 幂等 + fail-soft 集成验收（D-15）+ phase gate 全量回归</name>
  <read_first>
    - server/tests/test_coding_wave.py（Task 1 新增的端到端骨架 + 既有 4 集成测试零回归基线）
    - server/services/plan_orchestration/wave_progression.py（提取段独立 try/except fail-soft；mark_done 幂等仅 running→done）
    - 45-RESEARCH.md Pitfall 3（提取异常不让回调 5xx）+ D-15（line 554）+ 45-VALIDATION.md Sampling/Phase gate（line 30-33）
  </read_first>
  <action>
    在 `test_coding_wave.py` 追加两项 D-15 验收：(1) 幂等——对已 done 的 wave1 task 重复触发回填/提取（再 `_resume` 或重复调 advance）→ 断言 `produced_artifacts` 覆盖写后值不变（no-op 语义），wave2 不重复异常派发。(2) fail-soft——mock 提取链一环抛错（如 patch `build_produced_artifacts` 或 `record_produced_artifacts` 抛异常），断言 wave1 仍正确 done、wave 推进不失败、wave2 dispatch 仍发生且其 prompt 注入段为空（零回归降级），advance 不冒泡异常。完成后跑 phase gate：full server 测试套件（或至少 `tests/services/plan_orchestration tests/delivery tests/test_coding_wave.py tests/test_coding_node.py`）确认全绿、Phase 44 既有 wave/coding 测试零回归、INV-6 守护通过。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/plan_orchestration tests/delivery tests/test_coding_wave.py tests/test_coding_node.py -x</automated>
  </verify>
  <acceptance_criteria>
    - behavior（幂等）：重复回填提取 → produced_artifacts 不漂移，wave2 不重复异常
    - behavior（fail-soft）：提取异常 → wave1 仍 done、wave 推进成功、wave2 注入段为空、advance 不冒泡（容器回调不 5xx）
    - test-command：`uv run pytest tests/services/plan_orchestration tests/delivery tests/test_coding_wave.py tests/test_coding_node.py -x` 全绿
    - phase gate：Phase 44 既有 wave/coding 集成测试零回归 + INV-6 字段级守护通过
  </acceptance_criteria>
  <done>幂等 + fail-soft 集成验收绿，phase gate 全量套件零回归。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mock runner 产出（TaskResult） → 提取→注入全链路 | 集成测试以 mock IO 边界模拟半可信容器产物，验证端到端不泄漏/不膨胀/不 5xx |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-09 | DoS | 提取/注入异常 → 回调 5xx 重试风暴 | mitigate | fail-soft 集成测试断言提取异常被 swallow、wave 推进不失败、advance 不冒泡（覆盖 Pitfall 3 验收） |
| T-45-10 | Info Disclosure | 端到端产物经下游 prompt 泄漏敏感值 | mitigate | 集成断言 wave2 prompt 仅含路径/契约文件名（白名单字段），不含 token/raw_output 正文；产物源限定由 Plan 01 保证 |
| T-45-SC | Tampering | npm/pip/cargo installs | accept | 本 plan 仅新增测试代码，不安装任何外部包，无 supply-chain 面 |
</threat_model>

<verification>
- `uv run pytest tests/test_coding_wave.py::test_artifact_passthrough -x` 全绿（happy path 产物传递）
- `uv run pytest tests/services/plan_orchestration tests/delivery tests/test_coding_wave.py tests/test_coding_node.py -x` 全绿（幂等/fail-soft + phase gate 零回归）
- Phase 44 既有 4 wave 集成测试 + test_coding_node 12 用例零回归
</verification>

<success_criteria>
- SC-3：端到端构造跨仓依赖方案，断言 wave2 容器 prompt / DispatchTask metadata 含 wave1 产出的契约（产物传递正确）
- D-15 幂等：重复回调重复提取 → 覆盖写 no-op，产物不漂移
- D-15 fail-soft：提取异常 → wave 推进不失败、下游注入段空、回调不 5xx
- phase gate：全量 server 套件绿，Phase 44 + INV-6 守护零回归
</success_criteria>

## Artifacts this phase produces

本 plan 新增符号（ARTIFACT-01 + ARTIFACT-02 端到端验收）：

- **测试** `tests/test_coding_wave.py::test_artifact_passthrough`（D-13 端到端：wave1 done→提取→wave2 prompt 含契约）
- **测试** 幂等 + fail-soft 集成验收（D-15，扩充 test_coding_wave.py）
- 无新增生产符号（本 plan 仅集成验收，复用 Plan 01/02 产出的 build_produced_artifacts / record_produced_artifacts / acollect_upstream_artifacts / render_upstream_artifacts_section / dispatch 链透传参数）

<output>
完成后创建 `.planning/phases/45-wave/45-03-SUMMARY.md`
</output>
