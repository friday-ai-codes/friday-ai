---
phase: 62-crawl
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/durable/tasks_impl.py
  - server/durable/tasks.py
  - server/durable/handlers.py
  - server/delivery/models/ingest_run.py
  - server/delivery/migrations/0024_ingestrun_durable_queue.py
  - server/delivery/api/views.py
  - server/delivery/api/serializers.py
  - server/delivery/urls.py
  - server/tests/delivery/test_ingest_queue.py
  - server/tests/delivery/test_crawl_ingest_idempotent.py
autonomous: true
requirements: [CRAWL-01]
tags: [durable, crawl, ingest, delivery]
must_haves:
  truths:
    - "用户入队的爬取批次作为 durable 任务进 QUEUE_CRAWL_INGEST，IngestRun 是 DB 真相源"
    - "队列列表端点从 IngestRun（DB）重建，不依赖任何内存态——刷新/重启后队列可恢复"
    - "stop = cancel(durable_job_id) + status=STOPPED 终态；start/retry = 同 idempotency_key 重新 defer"
    - "run_crawl_ingest 重复执行不产生重复 WorkItem/Document/Archive（at-least-once 幂等）"
    - "重复 enqueue 同 batch 命中 queueing_lock 幂等不双跑"
  artifacts:
    - path: "server/durable/tasks_impl.py"
      provides: "run_crawl_ingest 任务体（薄封装 ingest_from_urls，DB 驱动）"
      contains: "async def run_crawl_ingest"
    - path: "server/delivery/models/ingest_run.py"
      provides: "IngestRun.Status.QUEUED/STOPPED + durable_job_id + idempotency_key 列"
      contains: "QUEUED"
    - path: "server/delivery/migrations/0024_ingestrun_durable_queue.py"
      provides: "加列迁移（兼容存量，不回退）"
    - path: "server/delivery/api/views.py"
      provides: "IngestQueueDispatchView/ListView/DetailView/ActionView（镜像 RepositoryReconcileView）"
      contains: "class IngestQueueDispatchView"
    - path: "server/delivery/urls.py"
      provides: "queue/ + queue/<uuid>/ + queue/<uuid>/<action>/ 路由（字面段在 uuid 前）"
  key_links:
    - from: "server/delivery/api/views.py"
      to: "durable.DurableTaskService.defer"
      via: "enqueue/start/retry 经 defer(QUEUE_CRAWL_INGEST)"
      pattern: "DurableTaskService\\.defer"
    - from: "server/durable/tasks_impl.py"
      to: "delivery.services.ingest_orchestrator.ingest_from_urls"
      via: "run_crawl_ingest 按 batch_id 逐 IngestRun 行调用"
      pattern: "ingest_from_urls"
    - from: "server/durable/handlers.py"
      to: "durable_crawl_ingest"
      via: "register_handler in-process adapter"
      pattern: "durable_crawl_ingest"
---

<objective>
把「批量爬取入库」从进程内 `run_in_background` 改造为 durable 任务（CRAWL-01）：新增 `run_crawl_ingest` 任务体（薄封装已天然幂等的现有摄取编排）、扩 `IngestRun`（QUEUED/STOPPED 状态 + durable_job_id + idempotency_key 列）、新增 delivery 队列动作 REST 端点（enqueue/list/detail/start/stop/retry），状态以 DB（IngestRun）为唯一真相源。

Purpose: 兑现 v0.12.0 首个用户可见垂直切片的后端半——刷新页面 + 容器/Pod 重建后任务不丢、自动续跑，入库 at-least-once 幂等。
Output: durable 任务体 + 双后端注册 + IngestRun 迁移 + 队列动作端点 + 幂等守护测试。本 plan 是 wave 1 唯一后端写入面；62-02（PageIndex）/62-03（前端）依赖本 plan 的端点与 tasks_impl.py。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/62-crawl/62-RESEARCH.md
@.planning/phases/62-crawl/62-CONTEXT.md

# 关键既有实现（执行前必读，照搬范式）
@server/durable/service.py
@server/durable/tasks.py
@server/durable/tasks_impl.py
@server/durable/handlers.py
@server/durable/queues.py
@server/delivery/models/ingest_run.py
@server/delivery/services/ingest_orchestrator.py
@server/delivery/services/json_ingest.py
@server/delivery/api/views.py
@server/delivery/api/serializers.py
@server/delivery/urls.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: IngestRun 扩列 + run_crawl_ingest 任务体 + 双后端注册</name>
  <files>server/delivery/models/ingest_run.py, server/delivery/migrations/0024_ingestrun_durable_queue.py, server/durable/tasks_impl.py, server/durable/tasks.py, server/durable/handlers.py, server/tests/delivery/test_crawl_ingest_idempotent.py</files>
  <read_first>
    - server/delivery/models/ingest_run.py（Status 枚举 + default_steps + 字段；OQ-1 决断：最小扩列）
    - server/durable/tasks_impl.py（run_page_index 占位 + run_index/run_graph keyword-only 范式）
    - server/durable/tasks.py（@app.task name= 显式裸名查找契约 + durable_page_index 包壳）
    - server/durable/handlers.py（_index/_graph/_page_index `**payload` 展开 adapter + register_business_handlers）
    - server/delivery/services/ingest_orchestrator.py（ingest_from_urls 解析 board_url → ingest_from_refs；终态 status 由 ingest_from_refs 写 COMPLETED/FAILED）
    - server/delivery/services/json_ingest.py（run_json_batch 的 asyncio.Semaphore 有界并发范式）
    - 最新 delivery 迁移为 0023_releaserecord_release_date_and_more.py（新迁移编号 0024，dependencies 指向 0023）
  </read_first>
  <behavior>
    - Test 1: 重复执行 run_crawl_ingest(batch_id=...) 两次 → WorkItemService.upsert 走三元组 unique，不产生重复 WorkItem（mock 摄取内核或断言 upsert 调用幂等）。
    - Test 2: run_crawl_ingest 只处理 batch 内非 COMPLETED 的 IngestRun 行；COMPLETED 行不再二次摄取（resume 安全）。
    - Test 3: in-process 后端经 register_handler("durable_crawl_ingest", _crawl_ingest) 注册后，handler(payload) → run_crawl_ingest(**payload) 入参对齐不抛 TypeError（双后端契约，研究 Pitfall 1）。
  </behavior>
  <action>
    OQ-1 决断（最小扩列，兼容存量不回退）：在 `IngestRun.Status` 增 `QUEUED = "queued", "排队中"` 与 `STOPPED = "stopped", "已停止"`（保留 RUNNING/COMPLETED/FAILED 不动）；新增字段 `durable_job_id = CharField(max_length=64, blank=True, default="")` 与 `idempotency_key = CharField(max_length=128, blank=True, default="", db_index=True)`。用 `manage.py makemigrations delivery` 生成 `0024_ingestrun_durable_queue`（dependencies 指向 0023）；新列均有 default 不回填存量、status 枚举新增值不改既有行（研究 Pitfall 5）。

    在 `server/durable/tasks_impl.py` 新增 `async def run_crawl_ingest(*, batch_id: str, concurrency: int = 3) -> dict`（keyword-only，对齐双后端 payload 契约）：函数体内局部 import `IngestRun` 与 `delivery.services.ingest_orchestrator.ingest_from_urls`、`delivery.services.json_ingest.clamp_concurrency`；查询该 batch 内 `status` ∈ {QUEUED, RUNNING, FAILED, STOPPED}（排除 COMPLETED，保证重复执行/断点恢复不重做已完成行）的 IngestRun，先经 `sync_to_async` 批量置 `status=RUNNING`，再用 `asyncio.Semaphore(clamp_concurrency(concurrency))` 有界并发对每行 `await ingest_from_urls(str(run.id), run.board_url, run.mr_url)`（ingest_from_urls 内部解析 board_url→三元组→ingest_from_refs，已天然幂等：三元组 upsert / 文档 content_hash / MR diff aarchive_exists；终态 status 由 ingest_from_refs 写 COMPLETED/FAILED）；单行异常 try/except 隔离 + structlog warning，不阻断整批；返回 `{"status": "ok", "batch_id": batch_id, "count": <处理行数>}`。绝不在 payload 落凭证（仅 batch_id/concurrency），DB 为唯一 specs 真相源（无需在 payload 持久化三元组）。

    在 `server/durable/tasks.py` 新增 procrastinate 包壳 `@app.task(name="durable_crawl_ingest", queue=QUEUE_CRAWL_INGEST)`（从 `durable.queues` import `QUEUE_CRAWL_INGEST`）`async def durable_crawl_ingest(**payload)` → 局部 import 后 `return await run_crawl_ingest(**payload)`（逐字镜像 durable_page_index 包壳，显式 name= 与 backends 裸名查找同源）。

    在 `server/durable/handlers.py` 新增 `async def _crawl_ingest(payload)` → `run_crawl_ingest(**payload)`，并在 `register_business_handlers()` 内 `register_handler("durable_crawl_ingest", _crawl_ingest)`（与 _page_index 同款，保证 SQLite/in-process 也注册，否则走 no-op）。

    新建 `server/tests/delivery/test_crawl_ingest_idempotent.py`：覆盖 behavior 三项——复用 tests/delivery 既有 fixture，mock/spy `ingest_from_urls` 或断言 WorkItemService.upsert 经三元组 unique 不产生重复；断言 COMPLETED 行被跳过；断言 in-process adapter 入参对齐。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/delivery/test_crawl_ingest_idempotent.py -x -q && uv run python manage.py makemigrations delivery --check --dry-run</automated>
  </verify>
  <acceptance_criteria>
    - `IngestRun.Status` 含 QUEUED/STOPPED；模型新增 durable_job_id + idempotency_key(db_index) 列；迁移 0024 存在且 `makemigrations --check` 干净。
    - `run_crawl_ingest` 为 keyword-only、按 batch_id 从 IngestRun（DB）重建 specs、有界并发、排除 COMPLETED、单行隔离。
    - durable_crawl_ingest 在 tasks.py（procrastinate 包壳）与 handlers.py（in-process adapter + register）两后端均注册，入参对齐不抛。
    - test_crawl_ingest_idempotent.py 全绿（重复执行幂等 + COMPLETED 跳过 + 双后端契约）。
  </acceptance_criteria>
  <done>IngestRun 迁移就绪、run_crawl_ingest 双后端可调、重复执行幂等守护通过。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 队列动作 REST 端点（enqueue/list/detail/start/stop/retry）+ 序列化器 + 路由</name>
  <files>server/delivery/api/views.py, server/delivery/api/serializers.py, server/delivery/urls.py, server/tests/delivery/test_ingest_queue.py</files>
  <read_first>
    - server/delivery/api/views.py（JsonIngestBatchView resolve→建 run→派发范式；IngestBatchDetailView 按 batch_id 聚合；IngestDispatchView 202 派发；全端点 IsAuthenticated）
    - server/delivery/api/serializers.py（JsonIngestRequestSerializer / IngestBatchRunSerializer / IngestRunSerializer 形状）
    - server/delivery/urls.py（字面段路由必须在 `<uuid:...>` 之前注册的范式）
    - server/repositories/views.py:1512-1614（RepositoryReconcileView.post 派发→轮询：先建 run 行拿 id 再派发，立即 202）
    - server/durable/service.py（defer/cancel/has_active_by_key 签名；cancel 仅取消 todo，doing 不可中断——研究 Pitfall 2/3）
    - server/delivery/services/json_ingest.py（aresolve_items 解析空间→三元组+board_url）
  </read_first>
  <behavior>
    - Test 1: POST queue/ 入队 → 对 resolved 项建 IngestRun(QUEUED) 共享 batch_id + DurableTaskService.defer 调用入参为 ("durable_crawl_ingest", {batch_id,concurrency}, queue=QUEUE_CRAWL_INGEST, idempotency_key="crawl_ingest:{batch_id}")，回写 durable_job_id/idempotency_key，返回 202。
    - Test 2: GET queue/ 从 IngestRun（DB）按 batch_id 分组重建队列列表（不依赖内存）；含聚合 status/progress/durable_job_id/时间戳——test_list_restores_from_db。
    - Test 3: stop → cancel(durable_job_id) 调用 + 非终态行 status=STOPPED；retry/start → 同 idempotency_key 重新 defer——test_stop_retry。
  </behavior>
  <action>
    在 `server/delivery/api/serializers.py` 新增 `IngestQueueItemSerializer`（plain `serializers.Serializer`，承载聚合队列项形状：`batch_id`、`status`（聚合，见下）、`total`、`done`、`url_count`、`durable_job_id`、`idempotency_key`、`started_at`、`updated_at`、`error`）；入队入参复用既有 `JsonIngestRequestSerializer`（items + concurrency），不另造。

    在 `server/delivery/api/views.py` 新增四个 adrf APIView（全部 `permission_classes=[IsAuthenticated]`，对齐既有 delivery 端点；V4 访问控制沿用 IngestRunDetailView 既有「无 owner + 不可猜 UUIDv4」归属说明）：
    - `IngestQueueDispatchView.post`：镜像 JsonIngestBatchView——`aresolve_items` 解析 → 对 resolved 项 `sync_to_async(IngestRun.objects.create)(batch_id=batch_id, board_url=item["board_url"], mr_url=item["mr_url"], status=IngestRun.Status.QUEUED, steps=default_steps(), project=...)`；`key = f"crawl_ingest:{batch_id}"`；`job_id = await DurableTaskService.defer("durable_crawl_ingest", {"batch_id": str(batch_id), "concurrency": concurrency}, queue=QUEUE_CRAWL_INGEST, idempotency_key=key)`（从 `durable` import `DurableTaskService, QUEUE_CRAWL_INGEST`，绝不直接 import procrastinate）；回写各行 `durable_job_id=job_id` + `idempotency_key=key`；不可解析项以 `skipped` 回报不建行；返回 `Response({"batch_id", "runs", "skipped", "dispatched": True}, 202)`。delivery 端点为 async，直接 `await defer(...)` 无需 async_to_sync（研究 Pitfall 6）。
    - `IngestQueueListView.get`：从 `IngestRun.objects.filter(batch_id__isnull=False)` 按 `batch_id` 分组取最近 N 批（OQ-4 per-batch 粒度；A4 内部工具按最近 N 批，建议 N=50，无分页）；每批聚合 `status` 按优先级 RUNNING>QUEUED>STOPPED>FAILED>COMPLETED（任一 RUNNING→running…全 COMPLETED→completed），`total`=行数、`done`=COMPLETED 行数、`started_at`=min、`updated_at`=max、`durable_job_id`/`idempotency_key` 取任一行；`IngestQueueItemSerializer(many=True)` 经 sync_to_async 序列化；这是 CRAWL-01 断点恢复命门（DB 真相源，不依赖内存 batchId）。
    - `IngestQueueDetailView.get(batch_id)`：复用 IngestBatchDetailView 聚合范式按 batch_id 返回该批 runs 明细（`IngestBatchRunSerializer`）+ 聚合 status；无 run → 404。
    - `IngestQueueActionView.post(batch_id, action)`：`action` ∈ {start, stop, retry}，非法值 400。抽私有 helper `_redefer_batch(batch_id)`（start/retry 共用）：把该批 QUEUED/STOPPED/FAILED 行置回 QUEUED，`await DurableTaskService.defer(..., idempotency_key=f"crawl_ingest:{batch_id}")`（queueing_lock 命中即幂等吞并，终态则建新 job，研究 Pattern 3），回写 durable_job_id；`stop`：`await DurableTaskService.cancel(<该批 durable_job_id>)`（best-effort，仅 todo 可取消），把非终态行（QUEUED/RUNNING）置 `STOPPED`（终态可重投），返回 200；明确不承诺中断 doing（研究 Pitfall 2）。该批不存在 → 404。

    在 `server/delivery/urls.py` 新增路由（字面段在 `<uuid>` 前）：`path("ingest/queue/", IngestQueueDispatchView.as_view(), name="ingest-queue-dispatch")`（GET 走 list？为避免方法歧义，list 用独立显式：`IngestQueueListView` 挂同 `ingest/queue/` 的 GET——同一 path 两 view 不可，故用单 view 同时实现 get+post，或 list 用 `ingest/queue/` GET + dispatch POST 合并到一个 `IngestQueueView`（get=list, post=enqueue））——采用合并：`IngestQueueView`(get=list, post=enqueue) 挂 `ingest/queue/`；`path("ingest/queue/<uuid:batch_id>/", IngestQueueDetailView.as_view(), ...)`；`path("ingest/queue/<uuid:batch_id>/<str:action>/", IngestQueueActionView.as_view(), ...)`。

    新建 `server/tests/delivery/test_ingest_queue.py`：覆盖 behavior 三项 + enqueue 契约（defer 入参、回写列）+ test_list_restores_from_db（仅建 IngestRun 行无内存态即可经 list 重建）+ test_stop_retry。用 monkeypatch/AsyncMock 桩 DurableTaskService.defer/cancel 捕获入参，DB 行真实落库。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/delivery/test_ingest_queue.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - 四端点（enqueue/list/detail/action）均 IsAuthenticated；enqueue 入参经 defer(QUEUE_CRAWL_INGEST, idempotency_key="crawl_ingest:{batch_id}") 并回写 durable_job_id/idempotency_key。
    - list 端点从 IngestRun（DB）按 batch_id 分组重建队列（聚合 status/progress），test_list_restores_from_db 通过——不依赖任何内存态。
    - stop=cancel+STOPPED、start/retry=同 key 重新 defer；test_stop_retry 通过。
    - urls.py 字面段在 `<uuid>` 前；视图绝不直接 import procrastinate（经 DurableTaskService + durable.queues 常量）。
  </acceptance_criteria>
  <done>队列动作端点全绿、DB 真相源 list 恢复验证通过、前端（62-03）可消费。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → delivery REST | 已登录用户贴 URL/三元组入队（不可信输入） |
| delivery → DurableTaskService → procrastinate_jobs | 入队 payload 跨进程持久化 |
| worker → ingest_from_urls → 外部（飞书/GitLab） | 爬取/摄取触达外部系统 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-01 | Spoofing/Elevation | 队列动作端点 | mitigate | 全端点 IsAuthenticated；沿用 IngestRunDetailView「无 owner + 不可猜 UUIDv4」归属说明，不新增越权面 |
| T-62-02 | InfoDisclosure | durable payload / IngestRun.error | mitigate | payload 仅放 batch_id/concurrency（绝不放 token）；error 经既有 `_safe_error`（_redact_secrets + 截断） |
| T-62-03 | Tampering/InfoDisclosure | 用户贴任意 URL 爬取 | accept（既有控制） | SSRF 边界由既有 `crawl_service._ais_safe_public_url`（公网校验 + 不跟随跳转）承载，本阶段不放宽、不触改 |
| T-62-04 | DoS | 重复 enqueue 堆积 | mitigate | idempotency_key(queueing_lock) 同 batch 不堆积；list N=50 上限避免大查询 |
| T-62-SC | Tampering | npm/pip/cargo installs | n/a | 本 plan 零新依赖（procrastinate 3.8.1 由 Phase 60 锁定），无安装任务 |
</threat_model>

<verification>
- `cd server && uv run pytest tests/durable tests/delivery -q` 全绿。
- `cd server && uv run python manage.py makemigrations --check --dry-run` 干净。
- grep 守护：delivery 视图无 `import procrastinate`（仅经 DurableTaskService + durable.queues 常量），由既有 `tests/durable/test_no_direct_import.py` 允许清单守护（delivery 不在清单内）。
- Postgres 专项（CI，可选）：`-m postgres_queue` 下 queueing_lock 真实去重。
</verification>

<success_criteria>
- CRAWL-01 后端半闭环：enqueue/list/detail/start/stop/retry 端点可用；IngestRun（DB）为真相源、list 可从 DB 重建队列；run_crawl_ingest 重复执行 at-least-once 幂等不产生重复数据；stop=cancel+STOPPED、retry=同 key 重 defer。
- 双后端注册齐备（procrastinate 包壳 + in-process adapter），worker 重启后领 todo 续跑（持久化由 Phase 60 procrastinate_jobs 承载）。
</success_criteria>

<artifacts_this_phase_produces>
- `run_crawl_ingest`（server/durable/tasks_impl.py）+ durable_crawl_ingest 双后端注册（tasks.py / handlers.py）。
- IngestRun.Status.QUEUED/STOPPED + durable_job_id + idempotency_key 列 + migration 0024。
- delivery 队列动作端点：`IngestQueueView`(get=list/post=enqueue)、`IngestQueueDetailView`、`IngestQueueActionView` + `IngestQueueItemSerializer` + 路由。
- 守护测试：test_ingest_queue.py、test_crawl_ingest_idempotent.py。
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/62-crawl/62-01-SUMMARY.md` when done.
</output>
