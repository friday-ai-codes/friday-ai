---
phase: 62-crawl
plan: 03
type: execute
wave: 2
depends_on: ["62-01"]
files_modified:
  - web/src/api/ingest.ts
  - web/src/components/knowledge/BatchIngestPanel.vue
  - web/src/locales/zh-CN.json
  - web/src/components/knowledge/__tests__/BatchIngestPanel.spec.ts
autonomous: true
requirements: [CRAWL-02]
tags: [frontend, vue, i18n, crawl, ingest]
must_haves:
  truths:
    - "面板队列列表 + 状态一律从后端 list 端点恢复，刷新页面后不丢（移除内存 batchId/runTriple/pollStartedAt 作为列表来源）"
    - "行内开始/停止/重试调对应队列动作 API；停止经 useConfirmDialog 破坏性确认"
    - "running/queued 项触发 refetchInterval=2s 轮询，全部终态停轮"
    - "feishu_not_configured 引导深链保留（既有行为不回退）"
    - "所有新增文案接入 zh-CN.json crawlQueue.*，守护测试以真实 zh-CN.json 锁文案"
  artifacts:
    - path: "web/src/api/ingest.ts"
      provides: "ingestApi.listQueue/enqueueQueue/startRun/stopRun/retryRun + CrawlQueueItem 类型"
      contains: "listQueue"
    - path: "web/src/components/knowledge/BatchIngestPanel.vue"
      provides: "后端恢复的 durable 队列面板 + 行内动作（UI-SPEC 契约）"
      contains: "crawl-queue-panel"
    - path: "web/src/locales/zh-CN.json"
      provides: "crawlQueue.* 命名空间文案"
      contains: "crawlQueue"
  key_links:
    - from: "web/src/components/knowledge/BatchIngestPanel.vue"
      to: "ingestApi.listQueue"
      via: "useQuery refetchInterval running/queued→2000"
      pattern: "listQueue"
    - from: "web/src/components/knowledge/BatchIngestPanel.vue"
      to: "useConfirmDialog"
      via: "stop 行内动作破坏性确认"
      pattern: "useConfirmDialog"
---

<objective>
改造 `web/src/components/knowledge/BatchIngestPanel.vue`（CRAWL-02 + 62-UI-SPEC 设计契约）：从后端 `IngestRun` 队列 list 端点恢复队列列表 + 实时状态，移除组件内存 `batchId`/`runTriple`/`pollStartedAt` 作为列表来源；行内开始/停止/重试调 62-01 队列动作端点；停止经破坏性确认；轮询沿用 `refetchInterval: running→2s/终态 false`；`feishu_not_configured` 引导深链保留；新增文案接入 `zh-CN.json` 的 `crawlQueue.*` 默认中文。

Purpose: 兑现 v0.12.0 首个用户可见垂直切片前端半——刷新页面后从后端恢复队列、行内可开始/停止/重试。
Output: ingest.ts 队列 API + 重写后的面板 + zh-CN.json 文案 + vitest 守护。依赖 62-01 队列端点契约。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/62-crawl/62-UI-SPEC.md
@.planning/phases/62-crawl/62-RESEARCH.md

# 关键既有实现（执行前必读，reuse-first）
@web/src/components/knowledge/BatchIngestPanel.vue
@web/src/api/ingest.ts
@web/src/api/reconcile.ts
@web/src/components/repository/ReconcilePanel.vue
@web/src/locales/zh-CN.json
# 62-01 端点契约（本 plan 依赖）：ingest/queue/(GET list, POST enqueue)、queue/<uuid>/(detail)、queue/<uuid>/<action>/(start|stop|retry)
@.planning/phases/62-crawl/62-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ingest.ts 队列 API + zh-CN.json crawlQueue.* 文案</name>
  <files>web/src/api/ingest.ts, web/src/locales/zh-CN.json</files>
  <read_first>
    - web/src/api/ingest.ts（ingestApi 既有方法 + IngestRun/IngestBatchRun 类型；crawlUrl 保留）
    - web/src/api/reconcile.ts（派发→轮询 API 客户端范式 + CleanupRun 类型形状）
    - web/src/locales/zh-CN.json（既有 reconcile.* / ingest 文案位 + JSON 结构，确认插入 crawlQueue 命名空间的位置）
    - .planning/phases/62-crawl/62-UI-SPEC.md（i18n 段：完整 crawlQueue.* 键集合 — 逐键落地）
    - .planning/phases/62-crawl/62-01-PLAN.md（端点路径 + 队列项聚合字段：batch_id/status/total/done/url_count/durable_job_id/started_at/updated_at/error）
  </read_first>
  <action>
    在 `web/src/api/ingest.ts` 新增类型 `CrawlQueueStatus = 'queued' | 'running' | 'stopped' | 'failed' | 'completed'` 与 `CrawlQueueItem`（字段对齐 62-01 `IngestQueueItemSerializer`：`batch_id`、`status: CrawlQueueStatus`、`total`、`done`、`url_count`、`durable_job_id`、`idempotency_key`、`started_at`、`updated_at`、`error`）。在 `ingestApi` 增方法（沿用 `get`/`post` client 范式）：`listQueue(): Promise<CrawlQueueItem[]>` → `get('/delivery/ingest/queue/')`；`enqueueQueue(items, concurrency?)` → `post('/delivery/ingest/queue/', { items, concurrency })`（入参形状复用既有 JsonIngestItem[]）；`startRun(batchId)` → `post('/delivery/ingest/queue/${batchId}/start/')`；`stopRun(batchId)` → `post('.../stop/')`；`retryRun(batchId)` → `post('.../retry/')`。保留 `crawlUrl`（爬取提取 + feishu_not_configured 深链）不动。

    在 `web/src/locales/zh-CN.json` 新增 `crawlQueue` 命名空间，逐键落地 62-UI-SPEC「i18n（zh-CN）」段的全部键（title/subtitle/input.{placeholder,enqueue,enqueuing}/enqueued/enqueueFailed/loading/loadError/retryLoad/refresh/listTitle/empty.{title,body}/item.{urlSummary,progress,enqueuedAt,updatedAt,error}/status.{queued,running,stopped,failed,completed}/actions.{start,stop,retry,starting,stopping,retrying}/stopConfirm.{title,description,confirmText}/feishuNotConfigured.configure），文案逐字采用 UI-SPEC 提供的中文。保持 JSON 合法（vue-i18n 加载），不破坏既有键。
  </action>
  <verify>
    <automated>cd web && pnpm vue-tsc --noEmit -p tsconfig.json 2>&1 | head -20; node -e "JSON.parse(require('fs').readFileSync('src/locales/zh-CN.json','utf8')); console.log('zh-CN.json valid')"</automated>
  </verify>
  <acceptance_criteria>
    - ingest.ts 新增 CrawlQueueItem/CrawlQueueStatus + listQueue/enqueueQueue/startRun/stopRun/retryRun，类型与 62-01 序列化器字段对齐；crawlUrl 保留。
    - zh-CN.json 含完整 crawlQueue.* 键（逐键对齐 UI-SPEC），JSON 合法。
    - vue-tsc 无新增类型错误。
  </acceptance_criteria>
  <done>队列 API 客户端 + 中文文案就绪，供面板消费。</done>
</task>

<task type="auto">
  <name>Task 2: BatchIngestPanel.vue 改造为后端恢复的 durable 队列面板 + vitest 守护</name>
  <files>web/src/components/knowledge/BatchIngestPanel.vue, web/src/components/knowledge/__tests__/BatchIngestPanel.spec.ts</files>
  <read_first>
    - web/src/components/knowledge/BatchIngestPanel.vue（现内存 batchId/runTriple/pollStartedAt:155-205 — 待移除作为列表来源；crawl 段 + feishu 引导块保留）
    - web/src/components/repository/ReconcilePanel.vue（useQuery refetchInterval running→2s + useConfirmDialog 破坏性确认 + useI18n t() 范式 — 逐处镜像）
    - web/src/components/knowledge/__tests__/entity-components.spec.ts（既有 knowledge vitest 范式 + happy-dom 挂载）
    - .planning/phases/62-crawl/62-UI-SPEC.md（Component States / Layout / Interaction / Reuse Map / data-testid 钩子 — 全部为执行契约）
  </read_first>
  <action>
    按 62-UI-SPEC 改造面板（reuse-first，不新增弹窗/badge variant/色板/registry）：
    - 引入 `useI18n` 的 `t`、`useQuery`/`useMutation`/`useQueryClient`、`useConfirmDialog`、`useToast`、`useErrorHandler`、`Badge`、`Skeleton`、`CompactEmptyState`、`Button`（按 Reuse Map 路径）。
    - 队列恢复：`const queueQuery = useQuery({ queryKey: ['crawl-ingest-queue'], queryFn: () => ingestApi.listQueue(), refetchInterval: q => (q.state.data?.some(r => r.status === 'running' || r.status === 'queued') ? 2000 : false) })`（镜像 ReconcilePanel）。**移除** `batchId`/`runTriple`/`pollStartedAt` 作为列表来源——列表与状态一律来自 queueQuery（DB 真相源，刷新即恢复）。
    - 入队：`useMutation` 调 `ingestApi.enqueueQueue(...)`（入队前预处理仍可走既有 crawlUrl→resolve 提取条目，作为 enqueue 入参；feishu_not_configured 走既有黄框引导深链分支，文案接 i18n，行为不回退）→ 成功 `toast.success(t('crawlQueue.enqueued'))` + `queryClient.invalidateQueries({ queryKey: ['crawl-ingest-queue'] })`；失败 `useErrorHandler`。
    - 行内动作（按 status 条件渲染，UI-SPEC Interaction）：queued/stopped/failed 显示「开始」(startRun)；running/queued 显示「停止」(stopRun，经 `useConfirmDialog().confirm({ title:t('crawlQueue.stopConfirm.title'), description:t('crawlQueue.stopConfirm.description'), confirmText:t('crawlQueue.stopConfirm.confirmText'), variant:'destructive' })`)；failed/stopped/completed 显示「重试」(retryRun，无确认)。动作后 invalidate 队列 key；进行中按钮 disabled + spinner。
    - 状态徽标：按 UI-SPEC 状态→variant 表（queued=muted/running=info/stopped=warning/failed=destructive/completed=success）+ 图标+文字（不靠颜色单独表意）。
    - Component States：loading→Skeleton 2–3 行；empty→CompactEmptyState(icon lucide--inbox, title/body 用 crawlQueue.empty.*)；error→红字 crawlQueue.loadError + 重试加载按钮(refetch)；failed 项展开后端 error 红字。
    - data-testid 钩子（守护测试稳定定位，逐一落地）：`crawl-queue-panel`/`crawl-url-input`/`crawl-enqueue-button`/`crawl-queue-list`/`crawl-queue-item`/`crawl-item-status`/`crawl-item-start`/`crawl-item-stop`/`crawl-item-retry`/`crawl-queue-empty`/`crawl-feishu-deeplink`。

    新建 `web/src/components/knowledge/__tests__/BatchIngestPanel.spec.ts`（vitest + @vue/test-utils + happy-dom，**以真实 zh-CN.json 作 createI18n messages** 锁文案，沿用 Phase 23/24 范式）：
    - test_list_restores_from_backend：mock ingestApi.listQueue 返回若干项 → 断言渲染 crawl-queue-item，状态徽标文案取自真实 zh-CN.json（关键状态/动作措辞不被改空）；不依赖任何内存 batchId。
    - test_inline_actions：点击 crawl-item-start/stop/retry 分别调 ingestApi.startRun/stopRun/retryRun；stop 触发确认弹窗（mock useConfirmDialog confirm）。
    - test_feishu_deeplink：crawlUrl 返回 feishu_not_configured → 渲染 crawl-feishu-deeplink 引导（既有行为不回退）。
  </action>
  <verify>
    <automated>cd web && pnpm vitest run src/components/knowledge/__tests__/BatchIngestPanel.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - 队列列表 + 状态来自 ingestApi.listQueue（DB 真相源）；组件无 batchId/runTriple/pollStartedAt 作为列表来源；刷新恢复由 useQuery 承载。
    - 行内 start/stop/retry 调对应 API；stop 经破坏性确认；refetchInterval running/queued→2000 否则 false。
    - feishu_not_configured 引导深链保留；data-testid 钩子齐全。
    - vitest 守护全绿，i18n 以真实 zh-CN.json 锁文案。
  </acceptance_criteria>
  <done>面板从后端恢复队列 + 行内动作 + 中文文案守护通过。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 浏览器 → delivery 队列端点 | 已登录用户经面板调队列动作 API（认证沿用既有 cookie-JWT） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-07 | Spoofing | 队列动作请求 | mitigate（既有） | 沿用 client.ts cookie-JWT + 后端 IsAuthenticated（62-01）；前端不持凭证 |
| T-62-08 | Tampering（文案被改空） | i18n 威胁缓解措辞 | mitigate | 守护测试以真实 zh-CN.json 作 messages 断言关键状态/停止确认措辞不被改空 |
| T-62-SC | Tampering | npm/pip/cargo installs | n/a | 零新依赖（@tanstack/vue-query / vue-i18n / reka-ui 均既有），无安装任务 |
</threat_model>

<verification>
- `cd web && pnpm vitest run src/components/knowledge` 全绿。
- `cd web && pnpm vue-tsc --noEmit` 无新增类型错误。
- 人工抽验（里程碑级，需真实 Postgres 部署）：贴链接入队 → 刷新页面 → 队列从后端恢复（见 62-VALIDATION Manual-Only）。
</verification>

<success_criteria>
- CRAWL-02 闭环：面板贴链接入队、队列列表 + 实时状态、行内开始/停止/重试、刷新后从后端恢复（不依赖内存 batchId/ref）；feishu_not_configured 深链保留；i18n 默认中文。
</success_criteria>

<artifacts_this_phase_produces>
- ingest.ts 队列 API（listQueue/enqueueQueue/startRun/stopRun/retryRun + CrawlQueueItem 类型）。
- 重写后的 BatchIngestPanel.vue（后端恢复队列 + 行内动作 + UI-SPEC 契约 + data-testid）。
- zh-CN.json crawlQueue.* 命名空间文案。
- 守护测试：BatchIngestPanel.spec.ts（真实 zh-CN.json i18n 锁文案）。
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/62-crawl/62-03-SUMMARY.md` when done.
</output>
