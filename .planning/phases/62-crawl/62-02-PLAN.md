---
phase: 62-crawl
plan: 02
type: execute
wave: 2
depends_on: ["62-01"]
files_modified:
  - server/durable/tasks_impl.py
  - server/repositories/models.py
  - server/repositories/migrations/0038_corpustreesnapshot_source_hash.py
  - server/codegraph/services/corpus_tree.py
  - server/repositories/tree_views.py
  - server/tests/durable/test_page_index.py
  - server/tests/repositories/test_knowledge_tree.py
autonomous: true
requirements: [PAGEIDX-01]
tags: [durable, page_index, corpus_tree, repositories]
must_haves:
  truths:
    - "知识树重建经 DurableTaskService.defer(QUEUE_PAGE_INDEX)，不再裸 run_in_background"
    - "run_page_index 先算 target hash，未变即 skipped 不调 build_full（重复执行无重复 snapshot）"
    - "hash 变化则真实 CorpusTreeService.build_full 重建，落 source_hash 供下次比对"
  artifacts:
    - path: "server/durable/tasks_impl.py"
      provides: "run_page_index 真实生成 + target-hash 跳过"
      contains: "CorpusTreeService"
    - path: "server/repositories/models.py"
      provides: "CorpusTreeSnapshot.source_hash 列"
      contains: "source_hash"
    - path: "server/repositories/tree_views.py"
      provides: "KnowledgeTreeRebuildView 改 durable defer"
      contains: "DurableTaskService"
  key_links:
    - from: "server/repositories/tree_views.py"
      to: "durable.DurableTaskService.defer"
      via: "KnowledgeTreeRebuildView.post 改 defer(QUEUE_PAGE_INDEX)"
      pattern: "DurableTaskService\\.defer"
    - from: "server/durable/tasks_impl.py"
      to: "codegraph.services.corpus_tree.CorpusTreeService.build_full"
      via: "run_page_index hash 变化时调用"
      pattern: "build_full"
---

<objective>
把 PageIndex/能力树生成接入 durable queue（PAGEIDX-01）：填充 `run_page_index` 占位任务体为真实生成（`CorpusTreeService.build_full` + target hash 跳过），收口 `repositories/tree_views.py` 裸 `run_in_background(CorpusTreeService.build_full)` 改 `DurableTaskService.defer(QUEUE_PAGE_INDEX)`，按 target hash 幂等（hash 未变跳过），重复执行无重复树/snapshot。

Purpose: 兑现 PAGEIDX-01——页面/树生成持久化、重启续跑、重复安全；与 crawl_ingest 独立逻辑队列避免长任务互堵。
Output: source_hash 列 + 真实 run_page_index + hash 计算 helper + tree_views 收口 + 幂等守护测试。
依赖说明：depends_on 62-01 仅因与其共享 `server/durable/tasks_impl.py` 文件（避免 wave 内文件冲突）；逻辑上与 crawl 独立。durable_page_index 包壳（tasks.py）与 _page_index adapter（handlers.py）Phase 61 已注册，本 plan 不改。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/62-crawl/62-RESEARCH.md
@.planning/phases/62-crawl/62-CONTEXT.md

# 关键既有实现（执行前必读）
@server/durable/tasks_impl.py
@server/durable/tasks.py
@server/durable/handlers.py
@server/durable/queues.py
@server/durable/service.py
@server/codegraph/services/corpus_tree.py
@server/repositories/tree_views.py
@server/repositories/models.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CorpusTreeSnapshot.source_hash 列 + 真实 run_page_index（hash 跳过/重建）</name>
  <files>server/repositories/models.py, server/repositories/migrations/0038_corpustreesnapshot_source_hash.py, server/codegraph/services/corpus_tree.py, server/durable/tasks_impl.py, server/tests/durable/test_page_index.py</files>
  <read_first>
    - server/durable/tasks_impl.py（run_page_index 当前占位：keyword-only + **kwargs，logger.debug 后恒等返回）
    - server/codegraph/services/corpus_tree.py（build_full：读全仓 ai_summary+facets 聚类；_activate_new_snapshot 写新 snapshot 切 is_active；get_active_tree）
    - server/repositories/models.py:786-815（CorpusTreeSnapshot：id/version/tree/manual_overrides/is_active/built_by/created_at；db_table=corpus_tree_snapshots）
    - 最新 repositories 迁移为 0037_graphfileindex.py（新迁移编号 0038，dependencies 指向 0037）
  </read_first>
  <behavior>
    - Test 1: target_hash == 当前计算 hash → run_page_index 返回 {"status":"skipped","reason":"hash_unchanged"}，不调用 build_full（spy build_full 未被调用）。
    - Test 2: target_hash 缺省/不等 → 调用 build_full 一次，返回含 source_hash；重复执行（hash 已落 active snapshot）第二次跳过——无重复 snapshot 行。
    - Test 3: _compute_corpus_source_hash 对相同仓库集合（id/ai_summary/facets）确定性同值；任一仓 ai_summary 变化 → hash 变化。
  </behavior>
  <action>
    OQ-3 决断（target hash = 域树输入指纹，存 snapshot）：在 `CorpusTreeSnapshot` 增 `source_hash = CharField(max_length=64, blank=True, default="")`；`manage.py makemigrations repositories` 生成迁移（AddField，nullable/default 不回填存量）。

    在 `server/codegraph/services/corpus_tree.py` 新增 `@classmethod async def compute_source_hash(cls) -> str`：读非删除 Repository 的 `(id, ai_summary, facets)`（沿用 build_full 的 `.only("id","name","ai_summary","facets")` 取材，剔除 `_` 前缀私有 facet 键），按 repo_id 排序后 `json.dumps(..., ensure_ascii=False, sort_keys=True)` → `hashlib.sha256(...).hexdigest()`（确定性指纹，A2）。在 `_activate_new_snapshot` 增形参 `source_hash: str = ""` 并写入新 snapshot（build_full 调用处传入构建时算得的 hash），使下次比对有据。

    填充 `server/durable/tasks_impl.py` 的 `run_page_index`：签名保持 keyword-only 容错 `async def run_page_index(*, target_id: str | None = None, target_hash: str = "", **kwargs) -> dict`（双后端 payload 契约、向后兼容现占位调用方）。函数体内局部 import `CorpusTreeService`；`current = await CorpusTreeService.compute_source_hash()`；若 `target_hash and target_hash == current` → 返回 `{"status":"skipped","reason":"hash_unchanged","target_id":target_id}`（不调 build_full，研究 Pitfall 4）；否则 `result = await CorpusTreeService.build_full()`（已幂等：unassigned 兜底 + 沿用旧 pin），返回 `{"status": result.get("status"), "target_id": target_id, "source_hash": current}`。绝不写库/外部副作用以外的旁路（build_full 自身落 snapshot）。

    新建 `server/tests/durable/test_page_index.py`（或扩 tests/durable/test_idempotency.py 的 page_index 段）：覆盖 behavior 三项——spy `CorpusTreeService.build_full` 验证 hash 未变不调用、hash 变调用一次；断言重复执行无重复 active snapshot；断言 compute_source_hash 确定性 + 输入变化 hash 变化。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/durable/test_page_index.py -x -q && uv run python manage.py makemigrations repositories --check --dry-run</automated>
  </verify>
  <acceptance_criteria>
    - CorpusTreeSnapshot 新增 source_hash 列 + 迁移；`makemigrations --check` 干净。
    - compute_source_hash 确定性（相同输入同值、输入变化值变）。
    - run_page_index：hash 未变返回 skipped 且不调 build_full；hash 变调 build_full 并返回 source_hash；重复执行无重复 snapshot。
    - test_page_index.py 全绿。
  </acceptance_criteria>
  <done>run_page_index 真实生成 + hash 幂等跳过，守护测试通过。</done>
</task>

<task type="auto">
  <name>Task 2: tree_views.py 收口裸 background_runner → durable defer(QUEUE_PAGE_INDEX)</name>
  <files>server/repositories/tree_views.py, server/tests/repositories/test_knowledge_tree.py</files>
  <read_first>
    - server/repositories/tree_views.py:175-188（KnowledgeTreeRebuildView.post 现 run_in_background(lambda: CorpusTreeService.build_full(), name="corpus_tree_full_rebuild")；IsAdminUser）
    - server/durable/__init__.py（curated re-export：from durable import DurableTaskService, QUEUE_PAGE_INDEX）
    - server/durable/service.py（defer 签名 + idempotency_key 语义）
    - server/codegraph/services/corpus_tree.py（compute_source_hash，本 plan Task 1 新增）
  </read_first>
  <action>
    改造 `KnowledgeTreeRebuildView.post`（保持 `IsAdminUser` 不放宽，V4/DoS：admin + hash 跳过 + queueing_lock 去重）：删除 `from services.background_runner import run_in_background` 与 `run_in_background(lambda: CorpusTreeService.build_full(), ...)`；改为 `from durable import DurableTaskService, QUEUE_PAGE_INDEX` + 局部 import CorpusTreeService，计算 `target_hash = await CorpusTreeService.compute_source_hash()`，`key = "page_index:corpus_tree"`，`job_id = await DurableTaskService.defer("durable_page_index", {"target_id": "corpus_tree", "target_hash": target_hash}, queue=QUEUE_PAGE_INDEX, idempotency_key=key)`；返回 `Response({"status": "rebuild_started", "job_id": job_id}, 202)`。view 为 adrf async，直接 `await defer(...)`（无需 async_to_sync，研究 Pitfall 6）。绝不直接 import procrastinate（经 DurableTaskService + durable.queues 常量）。

    新建/扩 `server/tests/repositories/test_knowledge_tree.py`：断言 rebuild 端点经 `DurableTaskService.defer`（monkeypatch/AsyncMock 捕获入参 task="durable_page_index"、queue=QUEUE_PAGE_INDEX、idempotency_key="page_index:corpus_tree"、payload 含 target_hash），返回 202；grep/契约断言文件内无 `run_in_background(` 残留于 rebuild 路径、无 `import procrastinate`。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/repositories/test_knowledge_tree.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - KnowledgeTreeRebuildView.post 经 DurableTaskService.defer(QUEUE_PAGE_INDEX, idempotency_key="page_index:corpus_tree")，不再 run_in_background；保持 IsAdminUser。
    - 测试捕获 defer 入参契约 + 202；rebuild 路径无 run_in_background / 无 import procrastinate 残留。
  </acceptance_criteria>
  <done>tree_views 重建收口到 durable，契约测试通过。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin → rebuild 端点 | 管理员触发全量域树重建（LLM 重活） |
| worker → CorpusTreeService.build_full → LLM | 域树聚类触达 LLM provider |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-05 | DoS | rebuild 端点重复触发重 LLM 聚类 | mitigate | 保持 IsAdminUser + run_page_index target-hash 未变跳过 + idempotency_key(queueing_lock) 同 key 不堆积 |
| T-62-06 | Tampering | 重复执行产生重复 snapshot | mitigate | hash 未变即 skipped 不重建；build_full 自身 _activate_new_snapshot 写新行再切 is_active（历史可回溯，单 active） |
| T-62-SC | Tampering | npm/pip/cargo installs | n/a | 零新依赖，无安装任务 |
</threat_model>

<verification>
- `cd server && uv run pytest tests/durable tests/repositories -q` 全绿。
- `cd server && uv run python manage.py makemigrations --check --dry-run` 干净。
- grep：repositories/tree_views.py rebuild 路径无 `run_in_background(`、无 `import procrastinate`。
</verification>

<success_criteria>
- PAGEIDX-01 闭环：知识树重建进 durable QUEUE_PAGE_INDEX；run_page_index 按 target hash 幂等（未变跳过、变则重建落 source_hash）；重复执行无重复 snapshot；重启续跑由 procrastinate_jobs 持久化承载。
- per-repo summary dispatch（OQ-2）本阶段不收口（保持 _schedule_auto_summary 经 Runner 容器现状），仅收口 tree_views 本地 build_full 路径。
</success_criteria>

<artifacts_this_phase_produces>
- CorpusTreeSnapshot.source_hash 列 + migration。
- CorpusTreeService.compute_source_hash + _activate_new_snapshot 落 source_hash。
- 真实 run_page_index（build_full + hash 跳过）。
- KnowledgeTreeRebuildView 改 durable defer。
- 守护测试：test_page_index.py、test_knowledge_tree.py。
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/62-crawl/62-02-SUMMARY.md` when done.
</output>
