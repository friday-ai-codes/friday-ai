---
phase: 94-entrypoint-unify
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - server/workflows/nodes/ai/plan_generation.py
  - server/tests/workflows/test_node_schema.py
  - web/src/components/workflow/sidebar/NodePalette.vue
  - web/src/components/__tests__/node-sync.test.ts
  - docs/workflows/ai-plan-generation-deprecation.md
autonomous: true
requirements: [UNIFY-02]
must_haves:
  truths:
    - "ai_plan_generation 节点仍 @register_node 注册（既有已实例化工作流可继续 execute）"
    - "ai_plan_generation 不再出现在前端节点库（NodePalette）"
    - "ai_plan_generation 代码标记 deprecated（ClassVar + docstring + 实例化 warning + 迁移指引）"
    - "node-sync 漂移守护保持绿（palette ⊆ fixture，删 palette 项安全）"
  artifacts:
    - path: "server/workflows/nodes/ai/plan_generation.py"
      provides: "deprecated ClassVar=True + docstring DEPRECATED 顶注 + __init__ 一次性 warning 日志"
      contains: "deprecated"
    - path: "web/src/components/workflow/sidebar/NodePalette.vue"
      provides: "AI 分组删除 ai_plan_generation 裸项"
      contains: "ai_coding"
    - path: "docs/workflows/ai-plan-generation-deprecation.md"
      provides: "ai_plan_generation → ai_plan_research 迁移指引"
      contains: "ai_plan_research"
  key_links:
    - from: "server/workflows/nodes/ai/plan_generation.py"
      to: "workflows.nodes.registry.register_node"
      via: "@register_node 保留（deprecated 不注销）"
      pattern: "@register_node"
---

<objective>
UNIFY-02：废弃旧 LangChain 单 agent 节点 `ai_plan_generation`——**保留 `@register_node` 注册**
（既有已实例化工作流向后兼容可运行）+ 代码标 deprecated（ClassVar `deprecated=True` + docstring
顶部「DEPRECATED」标记 + 实例化时一次性 `logger.warning` + 迁移指引文档），并从前端节点库
`NodePalette.vue` 移除（不再展示），新建路径改用 `ai_plan_research`。**绝不删代码、绝不注销注册、
绝不从 fixture 删**（CONTEXT 锁定 + 历史里程碑「向后兼容不回退」约束）。

Purpose: 节点库收口到统一编排入口，旧路径隐藏但既有实例不破坏。
Output: deprecated 标注 + palette 移除 + 迁移指引 + deprecated 注册守护测试。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/94-entrypoint-unify/94-CONTEXT.md
@.planning/phases/94-entrypoint-unify/94-RESEARCH.md
@server/workflows/nodes/ai/plan_generation.py
@server/workflows/nodes/base.py
@server/tests/workflows/test_node_schema.py
@web/src/components/workflow/sidebar/NodePalette.vue
@web/src/components/__tests__/node-sync.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: ai_plan_generation 标 deprecated（保留注册）+ 迁移指引（UNIFY-02 后端半）</name>
  <files>server/workflows/nodes/ai/plan_generation.py, server/tests/workflows/test_node_schema.py, docs/workflows/ai-plan-generation-deprecation.md</files>
  <read_first>
    - server/workflows/nodes/ai/plan_generation.py（`@register_node`/`class AIPlanGenerationNode`/`__init__` 第 134-139 行/类 docstring 第 110-120 行）
    - server/workflows/nodes/base.py（第 562-566 行 ClassVar 声明形态：requires_container/supports_retry/is_blocking）
    - server/tests/workflows/test_node_schema.py（NodeRegistry.get(...) 取节点类范式，第 41-49 行）
  </read_first>
  <action>
    在 `BaseNode` 加 `deprecated: ClassVar[bool] = False`（默认非废弃，对齐既有 requires_container 等
    ClassVar 声明形态，base.py:562-566）。在 `AIPlanGenerationNode` 设 `deprecated: ClassVar[bool] = True`，
    docstring 顶部加一行「DEPRECATED：已由统一编排入口 `ai_plan_research` 取代，仅为向后兼容保留注册；
    新建工作流请改用 ai_plan_research，迁移见 docs/workflows/ai-plan-generation-deprecation.md」。在
    `__init__` 末尾加一次性 `logger.warning("deprecated_node_instantiated", node_type="ai_plan_generation",
    category="sampling", component="workflow_node", migration="ai_plan_research")`（观测：deprecated
    实例化告警，category=sampling 避免高频 INFO 刷屏；best-effort 不反噬，节点正常构造）。**保留
    `@register_node` 装饰、保留全部节点类代码/端口/map_output**（既有 DB 实例 registry 查找与 execute
    不破坏，Anti-pattern：删类/注销注册会让既有 node_type="ai_plan_generation" 工作流崩溃）。新建
    `docs/workflows/ai-plan-generation-deprecation.md`（中文，doc-writing-zh 规范：正文中文、代码标识
    保留英文）说明：① 废弃原因（统一到 plan_orchestration）② 既有工作流不受影响（节点仍注册可运行）
    ③ 新建工作流改用 `ai_plan_research`（config 用 `requirement_text`，参照 technical_plan_generation /
    code_generation 模板）④ 不会自动迁移既有实例。在 `test_node_schema.py` 加 deprecated 注册守护
    用例：断言 `NodeRegistry.get("ai_plan_generation")` 非空（仍注册）且其 `deprecated is True`；
    并断言 `ai_plan_research` 的 `deprecated is False`（对照，未误标）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/workflows/test_node_schema.py -k "deprecated or registr or plan_generation" -x</automated>
  </verify>
  <acceptance_criteria>
    - ai_plan_generation 仍经 @register_node 注册（NodeRegistry.get 返回类）且 deprecated ClassVar True；ai_plan_research deprecated False。
    - 节点类代码/端口/map_output 逐字保留（仅加 ClassVar + docstring 注 + warning 行）。
    - docs/workflows/ai-plan-generation-deprecation.md 存在且含 ai_plan_research 迁移指引（中文正文）。
    - ruff format/check + mypy 干净。
  </acceptance_criteria>
  <done>ai_plan_generation 标 deprecated 且保留注册，迁移指引就位，registry 守护测试绿。</done>
</task>

<task type="auto">
  <name>Task 2: NodePalette 移除 ai_plan_generation + node-sync 守护（UNIFY-02 前端半）</name>
  <files>web/src/components/workflow/sidebar/NodePalette.vue, web/src/components/__tests__/node-sync.test.ts</files>
  <read_first>
    - web/src/components/workflow/sidebar/NodePalette.vue（AI 分组第 82-92 行，`{ type: 'ai_plan_generation', ... }` 在第 88 行）
    - web/src/components/__tests__/node-sync.test.ts（palette ⊆ fixture 红线第 47-51 行；正则提取第 40-42 行 `(?:type:\s*|fromDef\()'([^']+)'`；KNOWN_GHOSTS 第 45 行）
  </read_first>
  <action>
    删 `NodePalette.vue` AI 分组里的 `{ type: 'ai_plan_generation', name: 'AI 方案生成', description: 'AI 自动生成技术方案' }`
    一行（第 88 行）。**不**新增 ai_plan_research 裸项（ai_plan_research 不在前端 ALL_NODE_DEFINITIONS，
    且其入口面向编排/模板，节点库是否暴露由 Claude's Discretion——本 plan 只做「移除废弃项」，
    避免引入未声明节点；如后续需暴露 ai_plan_research 可另起）。**不改 fixture**——`node-types.fixture.json`
    仍含 ai_plan_generation（后端仍注册），palette ⊆ fixture 单向约束删 palette 项后依然成立（不会 orphan）。
    在 node-sync.test.ts 加一条守护断言：`paletteSet.has('ai_plan_generation')` 为 false（明确「已从
    palette 移除」）且 `fixtureTypes.has('ai_plan_generation')` 为 true（「后端仍注册、未从 fixture 删」），
    固化 UNIFY-02 不变量防回退。运行既有 palette ⊆ fixture / 幽灵守护红线确认零回归（无需 regen fixture：
    UNIFY-06 仅改 ai_plan_research 端口 schema 子属性，不增删端口名，fixture node_type/inputs/outputs 名集不变）。
  </action>
  <verify>
    <automated>cd web && pnpm vitest run node-sync</automated>
  </verify>
  <acceptance_criteria>
    - NodePalette.vue AI 分组不含 ai_plan_generation；其余项零回归。
    - node-sync.test.ts 全绿：palette ⊆ fixture、ai_plan_generation ∉ palette 且 ∈ fixture。
    - `pnpm vue-tsc --noEmit` 通过，受改文件 eslint 干净。
  </acceptance_criteria>
  <done>节点库不再展示 ai_plan_generation，node-sync 守护固化不变量且全绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 节点注册表 → 既有工作流实例 execute | DB 中 node_type="ai_plan_generation" 实例运行期经 NodeRegistry 查找 |
| 前端节点库 → 新建工作流 | palette 是新建工作流可拖拽节点来源（不影响既有实例） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94-02-COMPAT | Tampering（回归破坏） | ai_plan_generation 注销/删类 | mitigate | 保留 @register_node + 全部节点代码；deprecated 仅加 ClassVar/docstring/warning；test_node_schema 守护「仍注册」 |
| T-94-02-INFO | Information Disclosure | deprecated warning 日志 | mitigate | warning 仅记 node_type/migration 标量，category=sampling，不含 config/需求正文 |
| T-94-02-SC | Tampering | 依赖安装 | accept | 本 plan 无 npm/pip 安装（仅删 palette 行 + 加 ClassVar/doc，无新依赖） |
</threat_model>

<verification>
- `cd server && uv run pytest tests/workflows/test_node_schema.py -x` 全绿；`uv run ruff format --check . && uv run ruff check . && uv run mypy workflows/nodes/ai/plan_generation.py workflows/nodes/base.py` 干净。
- `cd web && pnpm vitest run node-sync && pnpm vue-tsc --noEmit` 通过。
- `cd server && uv run python manage.py makemigrations --check` 无新迁移。
</verification>

<success_criteria>
- ai_plan_generation 标 deprecated 且保留注册可运行；从 NodePalette 移除；迁移指引就位（UNIFY-02）。
- 既有实例向后兼容不回退；node-sync / registry 守护固化不变量。
</success_criteria>

<artifacts_this_phase_produces>
- 改动符号：`BaseNode.deprecated`（新 ClassVar，默认 False）；`AIPlanGenerationNode.deprecated=True` + docstring DEPRECATED 注 + `__init__` warning。
- 前端移除项：NodePalette.vue 删 ai_plan_generation 裸项。
- 新建文档：docs/workflows/ai-plan-generation-deprecation.md（迁移指引）。
- 测试：test_node_schema（deprecated 注册守护）、node-sync.test.ts（ai_plan_generation ∉ palette ∧ ∈ fixture）。
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/94-entrypoint-unify/94-02-SUMMARY.md` when done.
</output>
