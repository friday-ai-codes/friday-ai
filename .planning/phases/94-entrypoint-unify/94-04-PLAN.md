---
phase: 94-entrypoint-unify
plan: 04
type: execute
wave: 3
depends_on: [94-03]
files_modified:
  - server/mcp_tools/views.py
  - server/mcp_tools/planning_service.py
  - server/tests/mcp_tools/test_create_coding_plan_delegate.py
autonomous: true
requirements: [UNIFY-04]
must_haves:
  truths:
    - "create_coding_plan 产物口径对齐 plan_orchestration（不再走独立确定性 build_coding_plan seam 产分叉结构）"
    - "单仓约束：delegate 时 include_repos=[repository_id] 让编排只跑单仓"
    - "canonical execution_plan 中该仓 task 映射回旧响应字段 affected_files/steps/test_plan"
    - "McpCodingPlan / McpCodingPlanVersion 继续落库（兼容旧调用方）"
  artifacts:
    - path: "server/mcp_tools/views.py"
      provides: "CreateCodingPlanView delegate 路径（include_repos=[repository_id] + canonical 单仓映射）"
      contains: "delegate_plan_orchestration"
  key_links:
    - from: "server/mcp_tools/views.py"
      to: "server/mcp_tools/orchestration_delegate.py"
      via: "CreateCodingPlanView 复用 Plan 03 的 delegate_plan_orchestration"
      pattern: "delegate_plan_orchestration"
    - from: "server/mcp_tools/views.py"
      to: "McpCodingPlan / McpCodingPlanVersion"
      via: "delegate 后继续 acreate 落库（兼容）"
      pattern: "McpCodingPlanVersion.objects.acreate"
---

<objective>
UNIFY-04：MCP `create_coding_plan` 产物口径**收口到 `plan_orchestration`**——复用 Plan 03 的共享
`delegate_plan_orchestration` 核心，delegate 时 `include_repos=[repository_id]` **约束编排只跑单仓**
（Open Q2 决议），从 canonical `execution_plan` 中**该仓 task 子集**映射回旧单仓响应字段
（`affected_files`/`steps`/`test_plan`），不再走独立确定性 `build_coding_plan` seam 产分叉结构；
继续落 `McpCodingPlan` / `McpCodingPlanVersion` 保兼容。

Purpose: MCP 编码方案口径与工作流/对话/feishu 三入口统一到同一 canonical，消除分叉（CONTEXT
「最大化复用」）。
Output: CreateCodingPlanView delegate 接线 + 单仓映射 + 落库保留 + 响应外形/落库守护测试。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/94-entrypoint-unify/94-CONTEXT.md
@.planning/phases/94-entrypoint-unify/94-RESEARCH.md
@server/mcp_tools/views.py
@server/mcp_tools/planning_service.py
@server/mcp_tools/models.py
@server/mcp_tools/serializers.py
@server/mcp_tools/orchestration_delegate.py
@server/services/plan_orchestration/merged_plan.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: create_coding_plan delegate（单仓约束 + canonical 映射）+ 落库保留</name>
  <files>server/mcp_tools/views.py, server/mcp_tools/planning_service.py, server/tests/mcp_tools/test_create_coding_plan_delegate.py</files>
  <read_first>
    - server/mcp_tools/views.py（`CreateCodingPlanView.post` 第 1807-1911 行：现调 `build_coding_plan` 第 1848 行；落 McpCodingPlan 第 1857-1865 行 + McpCodingPlanVersion 第 1866-1878 行；output_data 第 1879-1888 行 keys：plan_id/version_id/version/repository_id/branch/plan/evidence/run_id；_record_agent_decision 第 1889-1899 行读 affected_files）
    - server/mcp_tools/serializers.py（CreateCodingPlanRequestSerializer 第 208-220 行：repository_id 必填 / branch / requirement / analysis_id / context_chunks / max_steps）
    - server/mcp_tools/planning_service.py（`build_coding_plan` 第 221-300 行：旧确定性单仓 payload keys：title/repository_id/repository_name/branch/requirement/affected_files/steps/test_plan/risks/...）
    - server/mcp_tools/orchestration_delegate.py（Plan 03 的 delegate_plan_orchestration + DelegateResult）
    - server/services/plan_orchestration/merged_plan.py（execution_plan[] 项含 repository_id + coding_instruction + dependencies；technical_plan schema 另强制 name/repository_name/branch_strategy）
  </read_first>
  <action>
    改 `CreateCodingPlanView.post`：方案生成从 `build_coding_plan` 确定性 seam 改为
    `delegate_plan_orchestration(requirement_text=str(input_data["requirement"]),
    include_repos=[repository_id], work_item=None, created_by=<actor>)`——**`include_repos=[repository_id]`
    约束编排只跑单仓**（Open Q2 决议）。从 `DelegateResult.content`（canonical MergedPlan）的
    `execution_plan` 中**筛该 repository_id 的 task**（或取首个，单仓约束下应仅该仓），映射回旧单仓
    响应/落库字段：`affected_files` ← 该 task 的候选文件/受影响文件（从 coding_instruction 关联文件或
    execution_plan 项可用字段 best-effort 提取，缺则空 list）、`steps` ← 该 task 的步骤（coding_instruction
    拆解 / execution_plan 项 steps，缺则保留最小结构）、`test_plan` ← 该 task 测试策略（缺则空 list）、
    `title` ← content.title 或 repo.name。**actor 解析**：从 `run` 的 created_by_id → User（afirst，无则
    None 文档化降级，V4 召回 fail-closed）。**继续落库**：`McpCodingPlan.objects.acreate(...)`（requirement/
    title/current_version=1，字段保留）+ `McpCodingPlanVersion.objects.acreate(plan_body=canonical content
    或映射后单仓 payload、affected_files/steps/test_plan/risks/evidence、change_summary、risk_delta，字段全
    保留兼容，A5）。**保留 output_data 全部既有键**（plan_id/version_id/version/repository_id/branch/plan/
    evidence/run_id）+ **新增可选** `session_id`（delegate partial 时供后续续推）+ `status`（delegate 终态
    映射，可选）。挂起态：delegate RESEARCHING→partial 时 plan_body 为 best-effort 当前 content（可空），
    落 McpCodingPlanVersion 仍建版本（version=1），output 携 session_id + status=partial（响应外形保留，
    Open Q1/Q2 一致）。`build_coding_plan` 函数**保留**（不删，作渲染/兼容 helper 或被其它测试引用；
    deprecated 注释指向 delegate 路径，对齐「seam 被取代但函数保留」State of the Art）。新建
    `tests/mcp_tools/test_create_coding_plan_delegate.py`：① delegate 被调用且 include_repos=[repository_id]
    （monkeypatch delegate_plan_orchestration 断言入参单仓）② canonical execution_plan 该仓 task → affected_files/steps/test_plan
    映射正确 ③ 响应键集合 snapshot（旧键全在 + session_id）④ McpCodingPlan/McpCodingPlanVersion 落库断言
    ⑤ partial 挂起态 output 携 session_id 不崩 ⑥ 缺 actor 降级。观测：CreateCodingPlanView 已纳入既有
    MCP 请求统计；delegate 进出口埋点已在 Plan 03 核心承担，无需 view 层重复。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/mcp_tools/test_create_coding_plan_delegate.py -x</automated>
  </verify>
  <acceptance_criteria>
    - create_coding_plan delegate（include_repos=[repository_id] 单仓约束）+ canonical execution_plan 该仓 task 映射回 affected_files/steps/test_plan。
    - McpCodingPlan / McpCodingPlanVersion 继续落库；output 旧键全保留 + 可选 session_id/status。
    - 6 守护用例全绿；ruff/mypy 干净；`makemigrations --check` 无新迁移。
  </acceptance_criteria>
  <done>create_coding_plan 产物口径收口到 plan_orchestration（单仓约束 + canonical 映射），响应/落库兼容守护绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client → CreateCodingPlanView | 外部/Codex agent 调用（经 McpToolView._begin 鉴权） |
| MCP delegate → plan_orchestration（单仓召回） | include_repos=[repository_id] 约束 + created_by actor 决定召回权限 |
| canonical execution_plan → 旧单仓响应/落库 | 映射白名单（affected_files/steps/test_plan） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94-04-ELEV | Elevation | delegate 漏带 actor / 跨仓泄漏 | mitigate | created_by 从 run 解析；`include_repos=[repository_id]` 单仓约束 + 仓库归属经既有 `_get_indexed_repo` 校验；召回对 None actor fail-closed |
| T-94-04-INFO | Information Disclosure | 响应暴露 canonical 内部键 / 他仓 task | mitigate | 仅映射该 repository_id 的 execution_plan task → 旧字段白名单；不透传全 content；他仓 task 不进单仓响应 |
| T-94-04-COMPAT | Tampering（契约破坏） | 响应外形 / McpCodingPlan(Version) 落库 | mitigate | 响应键集合 snapshot 守护不缩减；保留全部落库字段（A5）；session_id/status 为可选新增字段 |
| T-94-04-SC | Tampering | 依赖安装 | accept | 本 plan 无 npm/pip 安装（纯内部重构） |
</threat_model>

<verification>
- `cd server && uv run pytest tests/mcp_tools/test_create_coding_plan_delegate.py -x` 全绿；回归 `uv run pytest tests/mcp_tools -k "coding_plan" -x`。
- `cd server && uv run ruff format --check . && uv run ruff check . && uv run mypy mcp_tools/views.py mcp_tools/planning_service.py` 干净。
- `cd server && uv run python manage.py makemigrations --check` 无新迁移（不改 Mcp* 表 schema）。
- Phase gate：`cd server && uv run pytest tests/workflows tests/mcp_tools tests/agents tests/test_plan_clarification_answer_endpoint.py` + `cd web && pnpm vitest run` 全绿。
</verification>

<success_criteria>
- create_coding_plan 收口到 plan_orchestration（单仓约束 include_repos=[repository_id] + canonical 映射，UNIFY-04）。
- 响应外形兼容（旧键全保留 + 可选 session_id/status）+ McpCodingPlan/McpCodingPlanVersion 落库兼容。
- 三入口（feishu technical_plan / coding_plan / 工作流·对话）产同一 canonical MergedPlan 口径。
</success_criteria>

<artifacts_this_phase_produces>
- 改动符号：`CreateCodingPlanView.post` delegate 路径（include_repos=[repository_id] + canonical execution_plan 单仓 task 映射 + actor 解析）。
- 保留符号：`planning_service.build_coding_plan`（不删，标注被 delegate 取代）。
- 测试：test_create_coding_plan_delegate（单仓约束 + canonical 映射 + 响应 snapshot + 落库 + partial + actor 降级）。
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/94-entrypoint-unify/94-04-SUMMARY.md` when done.
</output>
