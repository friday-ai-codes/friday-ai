---
phase: 94-entrypoint-unify
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - server/agents/tools/plan_research_tools.py
  - server/tests/agents/test_start_plan_research_tool.py
autonomous: true
requirements: [UNIFY-05]
must_haves:
  truths:
    - "对话方案澄清挂起的权威唯一以 delivery.Clarification + PlanSession 为准（不再复用 chat 单题 ask_clarification marker）"
    - "start_plan_research 的 CLARIFYING ToolResult 不会被 chat graph _extract_pending_clarification 捕获（消除写错 ConversationIntentTrace 路径的二义）"
    - "marker 仅作前端渲染信号；收答续推唯一经 91-04 专路由 → aanswer_round_and_resume"
    - "chat 单题 ask_clarification（ConversationIntentTrace）路径零回归"
  artifacts:
    - path: "server/agents/tools/plan_research_tools.py"
      provides: "独立 plan 澄清渲染 marker（区别 chat 单题 ask_clarification）+ ToolResult 携 session_id/clarification_id"
      contains: "plan_clarification"
  key_links:
    - from: "server/agents/tools/plan_research_tools.py"
      to: "delivery.Clarification + PlanSession（单一来源）"
      via: "_maybe_suspend CLARIFYING 输出携 clarification_id/session_id（仅渲染信号，不进 chat 单题答复链）"
      pattern: "clarification_id"
---

<objective>
UNIFY-05：消除对话方案生成的「澄清双挂起二义」——`start_plan_research._maybe_suspend` 在
CLARIFYING 时不再复用 chat 单题 `CLARIFICATION_PENDING_MARKER`（="ask_clarification"），改用
**独立的 plan 澄清渲染 marker**（仅作前端渲染信号），并显式携带 `session_id` + `clarification_id`
让前端走 plan 多题卡而非 chat 单题卡。挂起/续推权威**唯一以 `delivery.Clarification` + `PlanSession`
为准**（收答经 91-04 专路由 `POST /conversations/{id}/plan-clarification/answer/` → `aanswer_round_and_resume`），
彻底切断「marker 偷渡进 chat 单题 `_extract_pending_clarification` → 写 `ConversationIntentTrace` →
不续推 PlanSession」的误路由可能。

Purpose: 单一来源收口，消除挂起表示二义（Pitfall 4）。
Output: 独立 plan marker + ToolResult 字段 + 二义消除守护测试。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/94-entrypoint-unify/94-CONTEXT.md
@.planning/phases/94-entrypoint-unify/94-RESEARCH.md
@server/agents/tools/plan_research_tools.py
@server/agents/tools/clarification.py
@server/orchestration/graph.py
@.planning/phases/91-clarification-outlets-resume/91-04-SUMMARY.md
@server/tests/agents/test_start_plan_research_tool.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: plan 澄清挂起改用独立渲染 marker（UNIFY-05 收口）</name>
  <files>server/agents/tools/plan_research_tools.py</files>
  <read_first>
    - server/agents/tools/plan_research_tools.py（`_maybe_suspend` 第 205-268 行 CLARIFYING 分支复用 `CLARIFICATION_PENDING_MARKER` 第 225-238 行；RESEARCHING 分支用 `__blocking_task__` 不动）
    - server/agents/tools/clarification.py（`CLARIFICATION_PENDING_MARKER="ask_clarification"` 第 36 行，chat 单题语义，绝不改）
    - server/orchestration/graph.py（`_extract_pending_clarification` 第 539-579 行：双条件过滤 `tc.name=="ask_clarification"` AND `payload.marker=="ask_clarification"`——确认 plan 工具新 marker 不被捕获）
  </read_first>
  <action>
    在 `plan_research_tools.py` 模块顶定义独立常量
    `PLAN_CLARIFICATION_RENDER_MARKER: Final[str] = "plan_clarification"`（docstring 注明：**仅前端
    渲染信号**，区别 chat 单题 `ask_clarification`；挂起/续推权威在 delivery.Clarification + PlanSession，
    收答经 91-04 专路由）。改 `_maybe_suspend` 的 CLARIFYING 分支：**移除** `from agents.tools.clarification
    import CLARIFICATION_PENDING_MARKER` 复用，ToolResult.output 的 `marker` 改为
    `PLAN_CLARIFICATION_RENDER_MARKER`，并**显式新增** `session_id`（已有）确保携带 + 保留
    `clarification_id`/`pending`/`question`/`options`/`allow_freeform`（前端据 session_id+clarification_id
    走 plan 多题卡，不走 chat 单题卡）。**不改** RESEARCHING 分支（`__blocking_task__` + register_blocking_task
    逐字保留，零回归）。语义保证：新 marker != "ask_clarification" → chat graph `_extract_pending_clarification`
    双条件（name + marker）必不命中 → plan 澄清绝不写 `ConversationIntentTrace`、不靠 chat graph interrupt
    收答（Tampering T-94-05-MARKER mitigate）。导出 `PLAN_CLARIFICATION_RENDER_MARKER` 到 `__all__`。
    观测豁免：仅改 marker 字面 + 字段透传，无新调用入口/LLM 调用。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/agents/test_start_plan_research_tool.py -x</automated>
  </verify>
  <acceptance_criteria>
    - _maybe_suspend CLARIFYING 输出 marker == "plan_clarification"（独立常量），携 session_id + clarification_id。
    - 不再 import / 复用 chat 的 CLARIFICATION_PENDING_MARKER；RESEARCHING 分支逐字零回归。
    - ruff format/check + mypy 干净。
  </acceptance_criteria>
  <done>plan 澄清挂起用独立渲染 marker，物理隔离 chat 单题路径，单一来源权威成立。</done>
</task>

<task type="auto">
  <name>Task 2: 二义消除守护测试（UNIFY-05 Wave 0 缺口）</name>
  <files>server/tests/agents/test_start_plan_research_tool.py</files>
  <read_first>
    - server/tests/agents/test_start_plan_research_tool.py（既有 start_plan_research 测试结构 / mock engine 范式）
    - server/orchestration/graph.py（`_extract_pending_clarification` 入参 `tool_calls_by_id: dict[str, dict]`，每项含 name/result）
    - server/agents/tools/clarification.py（chat 单题 ask_clarification marker，对照零回归）
  </read_first>
  <action>
    新增守护用例（UNIFY-05 续推/二义守护，对应 RESEARCH Wave 0 第 4 项）：
    ① **plan marker 独立性**：构造 start_plan_research CLARIFYING 挂起场景（mock session.status=CLARIFYING +
    ahas_pending=True + pending Clarification），断言 ToolResult.output["marker"] == "plan_clarification"
    且含 session_id + clarification_id（前端走 plan 卡的必要字段）。
    ② **不被 chat 单题路径捕获**：把该 ToolResult.output 包成 `_extract_pending_clarification` 的入参形态
    （`{"tc1": {"name": "start_plan_research", "result": <output dict>}}`），断言
    `_extract_pending_clarification(...)` 返回 None（plan 澄清绝不进 chat 单题 wait_clarification interrupt →
    不写 ConversationIntentTrace，T-94-05-MARKER 守护）。
    ③ **chat 单题零回归对照**：断言 chat 工具 `ask_clarification` 的 output marker 仍 == "ask_clarification"
    且能被 `_extract_pending_clarification` 捕获（name+marker 均 "ask_clarification"），证明两路径物理隔离。
    用 async test + monkeypatch engine/ClarificationService（不打真实 LLM/DB，对齐既有 mock 范式）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/agents/test_start_plan_research_tool.py tests/test_ask_clarification_tool.py -x</automated>
  </verify>
  <acceptance_criteria>
    - 新用例断言 plan marker="plan_clarification" 且不被 _extract_pending_clarification 捕获（返回 None）。
    - chat 单题 ask_clarification 仍被捕获（对照零回归）。
    - tests/test_ask_clarification_tool.py 既有用例全绿（chat 单题 marker 不动）。
  </acceptance_criteria>
  <done>二义消除经守护测试固化：plan 澄清单一来源、不误入 chat 单题答复链。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chat LLM → start_plan_research 工具 | 半可信 requirement_text（已有空需求 fail-closed 守护） |
| plan 澄清挂起表示 → 收答续推路径 | marker（渲染信号）vs delivery.Clarification + PlanSession（权威） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94-05-MARKER | Tampering | plan 澄清 marker 二义 | mitigate | 独立 `plan_clarification` marker，物理不被 chat graph `_extract_pending_clarification`（双条件 name+marker）捕获；收答唯一经 91-04 专路由 → aanswer_round_and_resume |
| T-94-05-ELEV | Elevation | 澄清跨用户/跨会话续推 | accept（已缓解） | 复用 91-04 既有 owner gate（created_by_id + has_project_access，无 superuser bypass）；本 plan 不新建收答入口、不绕过 |
| T-94-05-SC | Tampering | 依赖安装 | accept | 本 plan 无 npm/pip 安装（仅改 marker 常量 + 测试） |
</threat_model>

<verification>
- `cd server && uv run pytest tests/agents/test_start_plan_research_tool.py tests/test_ask_clarification_tool.py tests/test_plan_clarification_answer_endpoint.py -x` 全绿。
- `cd server && uv run ruff format --check . && uv run ruff check . && uv run mypy agents/tools/plan_research_tools.py` 干净。
- `cd server && uv run python manage.py makemigrations --check` 无新迁移。
</verification>

<success_criteria>
- 对话方案澄清挂起收敛单一来源 delivery.Clarification + PlanSession；marker 仅渲染信号（UNIFY-05）。
- plan 澄清不被 chat 单题路径捕获、不写 ConversationIntentTrace；chat 单题零回归。
</success_criteria>

<artifacts_this_phase_produces>
- 新增符号：`plan_research_tools.PLAN_CLARIFICATION_RENDER_MARKER`（独立渲染 marker，导出）。
- 改动符号：`_maybe_suspend` CLARIFYING 分支 marker 切换 + session_id/clarification_id 携带。
- 测试：test_start_plan_research_tool（marker 独立 + 不被 _extract_pending_clarification 捕获 + chat 单题对照零回归）。
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/94-entrypoint-unify/94-05-SUMMARY.md` when done.
</output>
