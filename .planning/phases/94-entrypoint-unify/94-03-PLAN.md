---
phase: 94-entrypoint-unify
plan: 03
type: execute
wave: 2
depends_on: [94-01]
files_modified:
  - server/services/plan_orchestration/entrypoint.py
  - server/mcp_tools/orchestration_delegate.py
  - server/mcp_tools/technical_plan_service.py
  - server/mcp_tools/views.py
  - server/tests/mcp_tools/test_create_feishu_technical_plan_delegate.py
autonomous: true
requirements: [UNIFY-03]
must_haves:
  truths:
    - "create_feishu_technical_plan 经 plan_orchestration 产 canonical MergedPlan/PlanVersion（不再走独立确定性 repo_task_matrix seam）"
    - "MCP 响应外形兼容（旧字段键集合不缩减，新增可选 session_id/status 字段）"
    - "McpWorkItemTechnicalPlan 继续落库（兼容旧调用方/artifact 查询）"
    - "编排挂起态语义明确：DONE→COMPLETED 映射 canonical；RESEARCHING→PARTIAL + session_id；MCP 入口跳过交互澄清（best-effort 用现有上下文）"
  artifacts:
    - path: "server/mcp_tools/orchestration_delegate.py"
      provides: "共享 MCP delegate 核心（start_orchestration + adrive + 取 canonical content + 终态/挂起映射）"
      contains: "delegate_plan_orchestration"
    - path: "server/services/plan_orchestration/entrypoint.py"
      provides: "build_orchestration_engine(skip_clarification=) 注入 no-clarify policy（MCP 同步友好）"
      contains: "skip_clarification"
    - path: "server/mcp_tools/technical_plan_service.py"
      provides: "build_work_item_technical_plan delegate 路径 + canonical→旧响应字段映射 + 落库保留"
      contains: "delegate_plan_orchestration"
  key_links:
    - from: "server/mcp_tools/technical_plan_service.py"
      to: "server/mcp_tools/orchestration_delegate.py"
      via: "build_work_item_technical_plan 调 delegate_plan_orchestration"
      pattern: "delegate_plan_orchestration"
    - from: "server/mcp_tools/orchestration_delegate.py"
      to: "services.plan_orchestration（start_orchestration/build_orchestration_engine/adrive）"
      via: "复用共享 helper，不在 MCP 层重写编排"
      pattern: "start_orchestration"
---

<objective>
UNIFY-03：MCP `create_feishu_technical_plan` 改为 **delegate 到 `plan_orchestration`**——建
`PlanSession`（entrypoint=workflow）→ `build_orchestration_engine` → `adrive` 续驱到终态/挂起 →
取 canonical `PlanVersion.content`（§7 MergedPlan）→ **显式映射回旧 MCP 响应字段**（外形兼容，
调用方不破坏），并继续落 `McpWorkItemTechnicalPlan` 保兼容。挂起态语义（Open Q1 决议）：MCP 入口
注入「跳过交互澄清」policy（best-effort 用现有上下文），编排若仍挂起 RESEARCHING（容器在途、MCP 无
resume 通路）则返回 `status=PARTIAL` + `session_id`（保留响应外形、新增可选字段）。

Purpose: MCP 入口归一到统一编排底座，产 canonical 同口径，不再产分叉结构（CONTEXT「最大化复用，
严禁重复造」）。
Output: 共享 MCP delegate 核心 + engine skip_clarification 开关 + feishu technical_plan delegate 接线 +
响应外形/落库守护测试。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/94-entrypoint-unify/94-CONTEXT.md
@.planning/phases/94-entrypoint-unify/94-RESEARCH.md
@server/mcp_tools/technical_plan_service.py
@server/mcp_tools/views.py
@server/mcp_tools/models.py
@server/mcp_tools/serializers.py
@server/services/plan_orchestration/entrypoint.py
@server/services/plan_orchestration/clarify_adapter.py
@server/services/plan_orchestration/resume.py
@server/services/plan_orchestration/merged_plan.py
@server/services/plan_orchestration/render.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: build_orchestration_engine skip_clarification 开关 + 共享 MCP delegate 核心</name>
  <files>server/services/plan_orchestration/entrypoint.py, server/mcp_tools/orchestration_delegate.py, server/tests/mcp_tools/test_create_feishu_technical_plan_delegate.py</files>
  <read_first>
    - server/services/plan_orchestration/entrypoint.py（`build_orchestration_engine` 第 58-87 行 ClarifyAdapter() 构造；`start_orchestration` 第 28-55 行签名）
    - server/services/plan_orchestration/clarify_adapter.py（`default_needs_clarification(session)->(bool,str,list)` 第 60 行；`ClarifyAdapter(policy=...)` 可注入第 94-98 行）
    - server/services/plan_orchestration/resume.py（`adrive_plan_session_to_pause_or_terminal(engine, session, max_steps=)` 续驱到挂起/终态）
    - server/workflows/nodes/ai/plan_research.py（`_map_terminal` 第 501-540 行：DONE 取 PlanVersion.content + 注入 plan_version_id 的终态映射范式）
    - server/services/plan_orchestration/merged_plan.py（MERGED_PLAN_FIELDS / validate_merged_plan）
  </read_first>
  <action>
    ① 在 `build_orchestration_engine` 加 keyword `skip_clarification: bool = False`：为 True 时用
    `ClarifyAdapter(policy=_no_clarify)`（`_no_clarify(session) -> (False, "", [])` 模块级常量函数，
    MCP 单次同步入口 best-effort「带现有信息继续」不发交互澄清，Open Q1 决议 #1）；为 False 时保持
    现状 `ClarifyAdapter()`（工作流/chat 入口零回归）。其余 adapters 逐字不变。
    ② 新建 `server/mcp_tools/orchestration_delegate.py`：`async def delegate_plan_orchestration(*,
    requirement_text: str, work_item: Any = None, include_repos: list[str] | None = None,
    created_by: Any = None) -> DelegateResult`（frozen dataclass `DelegateResult(session, status: str,
    content: dict, plan_version_id: str | None, markdown: str)`）。实现：`start_orchestration(
    entrypoint="workflow", requirement_text=, work_item=, created_by=, include_repos=)` →
    `engine = build_orchestration_engine(skip_clarification=True)` → `session =
    adrive_plan_session_to_pause_or_terminal(engine, session)`。终态/挂起映射（mirror plan_research
    `_map_terminal`）：DONE→取 `PlanVersion.objects.filter(id=session.current_plan_version).afirst()`
    的 content（isinstance dict 否则 {}），status="completed"，markdown=`render_merged_plan_markdown(content)`
    （复用 94-01 共享 helper，不造两套）；RESEARCHING/CLARIFYING（仍挂起、MCP 无 resume 通路）→
    status="partial"，content=best-effort 当前 current_plan_version content 或 {}，plan_version_id 可空，
    markdown best-effort；FAILED→status="failed"，content={}。**绝不在 MCP 层重写拆分/路由/调研**
    （只调共享 helper）。async 全程 `*_id` 标量 / afirst（防裸 lazy-FK）。观测：delegate 进出口
    best-effort 埋点 `mcp_plan_delegate_started/completed`（category=caller、component=mcp_tools、
    duration_ms、status）；编排内部 LLM/召回埋点已由 plan_orchestration adapters 承担（call_source
    链路完整，无需 MCP 层重复赋值）。
    ③ 新建 `tests/mcp_tools/test_create_feishu_technical_plan_delegate.py`，先写 delegate 核心单测
    （monkeypatch build_orchestration_engine / adrive 返回 DONE / RESEARCHING / FAILED session）：
    断言三态映射的 status + content + markdown + plan_version_id；DONE 含 render 后 markdown；
    RESEARCHING→partial + session_id 可取。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/mcp_tools/test_create_feishu_technical_plan_delegate.py -k "delegate" -x</automated>
  </verify>
  <acceptance_criteria>
    - build_orchestration_engine(skip_clarification=True) 注入 no-clarify policy；默认 False 零回归。
    - delegate_plan_orchestration 三态（DONE/partial/failed）映射正确，DONE 含 render markdown，复用 94-01 helper。
    - 编排逻辑不在 MCP 层重写（仅调 start_orchestration + adrive）。ruff/mypy 干净。
  </acceptance_criteria>
  <done>共享 MCP delegate 核心 + engine skip_clarification 开关就位，三态映射单测绿（供 Plan 04 复用）。</done>
</task>

<task type="auto">
  <name>Task 2: create_feishu_technical_plan delegate 接线 + 响应外形/落库守护</name>
  <files>server/mcp_tools/technical_plan_service.py, server/mcp_tools/views.py, server/tests/mcp_tools/test_create_feishu_technical_plan_delegate.py</files>
  <read_first>
    - server/mcp_tools/technical_plan_service.py（`build_work_item_technical_plan` 第 368-532 行：现走 `_build_repo_task_matrix` 确定性 seam 产 plan_body；output 字段集第 512-525 行：technical_plan_id/context_id/project_id/plan/markdown/repository_tasks/evidence/feishu_document/comment/status/retry_state/run_id；McpWorkItemTechnicalPlan.acreate 第 491-511 行落库字段）
    - server/mcp_tools/views.py（`CreateFeishuTechnicalPlanView.post` 第 1412-1472 行：调 build_work_item_technical_plan + _record_agent_decision 读 result.output["repository_tasks"]/["status"]/["feishu_document"]）
    - server/mcp_tools/models.py（McpWorkItemContext 字段 第 262-328 行：space/feishu_project_key/work_item_type/work_item_id/name/description；McpWorkItemTechnicalPlan 第 329+ 落库字段）
    - server/mcp_tools/orchestration_delegate.py（Task 1 的 delegate_plan_orchestration）
  </read_first>
  <action>
    重构 `build_work_item_technical_plan`：把「方案生成」从 `_build_repo_task_matrix` 确定性 seam 改为
    delegate——用 `_work_item_text(context)` 作 requirement_text、`repository_ids` 作 include_repos、
    按 (feishu_project_key, work_item_type, work_item_id) 解析 delivery `WorkItem`（afirst，无则 None，
    INV-2）作 work_item 锚、`created_by`（从 view 透传的 actor）→ `delegate_plan_orchestration(...)`。
    **canonical → 旧响应字段显式映射**（Pitfall 3 外形兼容，绝不直接透传全 content 内部键）：
    `plan` ← canonical content（MergedPlan，替代旧 plan_body）；`markdown` ← delegate.markdown
    （render_merged_plan_markdown）；`repository_tasks` ← 从 content.execution_plan[] 映射为旧矩阵形态
    （逐项取 repository_id/repository_name/coding_instruction→change_goal 等，best-effort 字段对应，
    缺字段填空不抛）；`status` ← delegate.status 映射到 `McpWorkItemTechnicalPlan.Status`
    （completed→COMPLETED / partial→PARTIAL / failed→FAILED）。**保留** output 全部既有键
    （technical_plan_id/context_id/project_id/plan/markdown/repository_tasks/evidence/feishu_document/
    comment/status/retry_state/run_id）+ **新增可选** `session_id`（partial 时供调用方后续经会话/工作流续推）。
    feishu 文档/评论 writeback（_create_feishu_document/_write_work_item_comment）逻辑保留（喂 delegate
    产的 markdown + 映射后的 repository_tasks）。继续 `McpWorkItemTechnicalPlan.objects.acreate(...)` 落库
    （plan_body=content / markdown / repository_tasks / evidence / status，字段全保留兼容，A5）。
    `CreateFeishuTechnicalPlanView.post` 解析 actor（从 `run` 的 created_by_id → User，afirst；无则 None
    并文档化降级——recall stage 对 None actor fail-closed 空召回，V4）传给 build_work_item_technical_plan。
    脱敏：markdown 经 render（结构化非原文）；上游飞书响应/异常文本已由既有 writeback 处理。补/扩
    `test_create_feishu_technical_plan_delegate.py`：响应键集合 snapshot（断言旧键全在 + session_id 存在）+
    McpWorkItemTechnicalPlan 落库断言（status/plan_body/markdown）+ delegate 被调（不再走 _build_repo_task_matrix）+
    缺 actor 降级不崩。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/mcp_tools/test_create_feishu_technical_plan_delegate.py -x</automated>
  </verify>
  <acceptance_criteria>
    - create_feishu_technical_plan 经 delegate_plan_orchestration 产 canonical；响应键集合不缩减 + 新增 session_id。
    - McpWorkItemTechnicalPlan 继续落库（plan_body=canonical content / markdown / status 映射）。
    - 响应 snapshot + 落库 + delegate 调用 + 缺 actor 降级用例全绿；ruff/mypy 干净。
  </acceptance_criteria>
  <done>create_feishu_technical_plan delegate 到统一编排，响应外形兼容 + 落库保留，守护测试绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client → CreateFeishuTechnicalPlanView | 外部/Codex agent 调用（经 McpToolView._begin 鉴权） |
| MCP delegate → plan_orchestration（召回 stage） | created_by actor 决定召回权限范围 |
| canonical content → MCP 响应 / 飞书 writeback | 映射白名单暴露旧契约字段 + markdown 推送 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94-03-ELEV | Elevation | delegate 漏带 actor → 越权召回 | mitigate | `start_orchestration(created_by=actor)`（从 run 解析）；召回 adapter 对 None actor fail-closed 空召回（文档化降级） |
| T-94-03-INFO | Information Disclosure | MCP 响应映射暴露 canonical 内部键 | mitigate | 显式字段映射白名单（只暴露旧契约字段 + session_id），绝不透传全 content 内部键；markdown 经 render 结构化非 LLM 原文 |
| T-94-03-COMPAT | Tampering（契约破坏） | MCP 响应外形 / McpWorkItemTechnicalPlan 落库 | mitigate | 响应键集合 snapshot 守护不缩减；保留全部落库字段（A5）；新增 session_id 为可选字段不破坏旧调用方 |
| T-94-03-VAL | Tampering | requirement_text 半可信输入 | mitigate | MCP serializer 既有 max_length 校验保留；编排内部对输入按既有 stage 处理 |
| T-94-03-SC | Tampering | 依赖安装 | accept | 本 plan 无 npm/pip 安装（纯内部重构，RESEARCH Package Audit 不适用） |
</threat_model>

<verification>
- `cd server && uv run pytest tests/mcp_tools/test_create_feishu_technical_plan_delegate.py -x` 全绿；回归 `uv run pytest tests/mcp_tools -k "technical_plan" -x`。
- `cd server && uv run ruff format --check . && uv run ruff check . && uv run mypy mcp_tools/orchestration_delegate.py mcp_tools/technical_plan_service.py services/plan_orchestration/entrypoint.py` 干净。
- `cd server && uv run python manage.py makemigrations --check` 无新迁移（不改 Mcp* 表 schema）。
</verification>

<success_criteria>
- create_feishu_technical_plan delegate 到 plan_orchestration 产 canonical MergedPlan/PlanVersion（UNIFY-03）。
- 响应外形兼容（旧键全保留 + 可选 session_id）+ McpWorkItemTechnicalPlan 落库兼容。
- 挂起态语义明确（DONE→completed / RESEARCHING→partial+session_id / MCP 跳过交互澄清）。
</success_criteria>

<artifacts_this_phase_produces>
- 新建符号：`mcp_tools/orchestration_delegate.py::delegate_plan_orchestration` + `DelegateResult`（共享 delegate 核心，供 Plan 04 复用）。
- 改动符号：`build_orchestration_engine(skip_clarification=)` + `_no_clarify` policy；`build_work_item_technical_plan` delegate 路径 + canonical→旧字段映射；`CreateFeishuTechnicalPlanView.post` 解析 actor。
- 测试：test_create_feishu_technical_plan_delegate（delegate 三态 + 响应 snapshot + 落库 + actor 降级）。
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/94-entrypoint-unify/94-03-SUMMARY.md` when done.
</output>
