---
phase: 44-repocodingtask-execution-plan-dag-wave
plan: 04
type: execute
wave: 3
depends_on: ["44-01", "44-03"]
files_modified:
  - server/services/plan_orchestration/wave_progression.py
  - server/services/plan_orchestration/__init__.py
  - server/tests/services/plan_orchestration/test_wave_progression.py
autonomous: true
requirements: [WAVE-02]
must_haves:
  truths:
    - "执行顺序：回填(running→终态) → 传递闭包阻断全部 failed 的下游 → 才决策出口（阻断在任何 early-return 之前完成）"
    - "决策出口：有 RUNNING 在途→waiting；否则有 depends_on 全 done 的 pending→dispatch 最小可派发 wave；否则(无 pending 无 running)→all_terminal。等待 keys off RUNNING，不靠最小 pending wave"
    - "失败隔离：单 wave 内单仓 failed 不影响同 wave 兄弟"
    - "下游阻断（传递闭包）：上游 failed → 其 depends_on 链上直接/间接下游在单次 aadvance 内全标 blocked（BFS/worklist 多跳），间接下游不残留 pending，收尾可达不死锁"
    - "幂等：重复调用经 service 条件更新 no-op，不重复推进"
    - "当前 wave 从 DB 重算（存在 pending/running 的最小 wave），不读内存"
  artifacts:
    - path: "server/services/plan_orchestration/wave_progression.py"
      provides: "acurrent_wave_all_terminal + aadvance_coding_waves 入口无关 helper"
      contains: "async def aadvance_coding_waves"
  key_links:
    - from: "server/services/plan_orchestration/wave_progression.py"
      to: "delivery.services.RepoCodingTaskService"
      via: "状态回填/阻断只经 service（不直接写 status）"
      pattern: "mark_(done|failed|blocked)"
    - from: "server/services/plan_orchestration/wave_progression.py"
      to: "subagent.SubAgentSession"
      via: "按 subagent_session_id 标量回填终态（服务端权威字段）"
      pattern: "subagent_session_id"
---

<objective>
立入口无关 wave 推进 helper `wave_progression.py`（WAVE-02 推进核心）——镜像 `research_aggregation.py`（barrier 全终态 + status guard 幂等）与 `resume.py`（入口无关续驱、不造两套）：回填当前 wave 各 task 终态（按 SubAgentSession 服务端权威状态）→ wave gate 判定（全终态才推进）→ 上游 failed 阻断下游 depends_on 链 → 返回下一 wave 待 dispatch task / `{"all_terminal": True}`。状态只经 `RepoCodingTaskService`，幂等、fail-soft。

Purpose: 为 plan 05 AICodingNode resume 段提供「判 gate → 推下一 wave 或收尾」的入口无关逻辑（workflow 入口先用，chat 入口未来复用，mirror RESUME-01「不造两套」）。wave 进度全从 DB 重算，支撑 callback 驱动多 wave resume。
Output: `acurrent_wave_all_terminal` + `aadvance_coding_waves` + barrel 导出 + wave gate/失败隔离/下游阻断/幂等单测。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-repocodingtask-execution-plan-dag-wave/44-CONTEXT.md
@.planning/phases/44-repocodingtask-execution-plan-dag-wave/44-RESEARCH.md
@.planning/phases/44-repocodingtask-execution-plan-dag-wave/44-PATTERNS.md

@server/services/plan_orchestration/research_aggregation.py
@server/services/plan_orchestration/resume.py
@server/delivery/services/repo_coding_task_service.py
@server/services/plan_orchestration/__init__.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: acurrent_wave_all_terminal + aadvance_coding_waves 入口无关 helper</name>
  <read_first>
    - server/services/plan_orchestration/research_aggregation.py（行 35-70 TERMINAL_STATUSES + aall_research_tasks_terminal barrier 判定；行 73-88 amaybe_complete_research 幂等 guard + 经 service 推进不直接写 status）
    - server/services/plan_orchestration/resume.py（行 24-79 入口无关 helper 设计 + lazy import 规避环 + `*_id` 标量 async ORM 安全）
    - server/delivery/services/repo_coding_task_service.py（plan 03 产物：mark_done/mark_failed/mark_blocked 签名）
    - server/delivery/models/repo_coding_task.py（字段 + 枚举）
    - .planning/phases/44-repocodingtask-execution-plan-dag-wave/44-RESEARCH.md（Pattern 3 行 222-236 + Code Examples 幂等 gate 行 366-376；Pitfall 3 async lazy-FK；Pitfall 7 wave 从 DB 重算）
    - .planning/phases/44-repocodingtask-execution-plan-dag-wave/44-PATTERNS.md（wave_progression Apply 段，行 415-419）
  </read_first>
  <action>
    新建 `server/services/plan_orchestration/wave_progression.py`，`__all__ = ["acurrent_wave_all_terminal", "aadvance_coding_waves"]`。函数内 lazy import `from delivery.models import RepoCodingTask, RepoCodingTaskStatus`（规避 import 环）：
    - `acurrent_wave_all_terminal(plan_version_id, wave: int) -> bool`：镜像 `aall_research_tasks_terminal`——`await RepoCodingTask.objects.filter(plan_version_id=plan_version_id, wave=wave, status__in=[PENDING, RUNNING]).aexists()` → 取反返回（终态含 failed，**不是仅 done**）。**仅可对在途 RUNNING 所在 wave 求值**，**绝不**对「最小 pending wave」求值用作 gate（未派发的 pending 下游 wave 不算「在途未终」，否则会抢先 return waiting、阻断/dispatch 永不可达 → 死锁）。本函数仍导出供 barrel/测试与 plan 05 复用，但 `aadvance_coding_waves` 的等待判定按下文 step 3 以 RUNNING 在途为准，不靠最小 pending wave。
    - `aadvance_coding_waves(plan_version_id, *, service=None) -> dict`：入口无关续驱（service 默认 `RepoCodingTaskService()`）。**严格按下列顺序：先回填 → 再传递闭包阻断全部 failed 的下游 → 最后才决策出口**（顺序是 liveness 关键：阻断必须在任何 early-return 之前完成，否则未派发的 pending 下游永不被阻断 → 死锁）：
      1. **回填（running→终态）**：`async for task in RepoCodingTask.objects.filter(plan_version_id=plan_version_id, status=RUNNING)`，按 `task.subagent_session_id` 标量取 `SubAgentSession`（`await SubAgentSession.objects.filter(id=task.subagent_session_id).afirst()`，**绝不裸访问 `task.subagent_session`**）；按其终态经 `service.mark_done(task)` / `service.mark_failed(task, error)` 回填（无 subagent_session_id 或非终态则跳过，等下次回调）。**不在此 step return**。
      2. **下游阻断（传递闭包，必须在决策出口前做完——liveness 关键，防死锁）**：回填后重查本 plan_version 全部 `failed` task，对其下游做**传递闭包 BFS/worklist 遍历**，而非单跳。算法：初始 `worklist = [所有 failed task]`、`seen = set()`；循环弹出一个 failed/blocked task `u`，遍历其 `dependents`（反查 M2M：`async for d in u.dependents.all()` 或同步块内 M2M 反查，避免裸 lazy-FK），对每个**仍处 pending** 的下游 `d` 经 `service.mark_blocked(d, upstream_ids=[u 的 repo/task 标识...])` 标 blocked，**并把 `d` 加入 worklist 继续向其 dependents 传播**（仅在本次新标 blocked 时入队，`seen` 去重防重复/防环重入）；直到 worklist 空。这样链 `A(wave0)→B(wave1)→C(wave2)`，A failed 时 **B 与 C 在同一次 aadvance 内均被标 blocked**（C 不会因 depends_on=B 永不为 done 而永久 pending）。注意：M2M 边方向以 `depends_on`（task 依赖谁）为准、`dependents` 为其反查（依赖我者）——阻断须沿 `dependents` 方向向下游传播；运行中/已终态的下游不强翻（mark_blocked 条件更新仅 pending→failed，幂等）。**不在此 step return**。
      3. **决策出口（阻断完成后才判定，按以下优先级精确取值）**：
         a. **仍在途 → 等待**：`await RepoCodingTask.objects.filter(plan_version_id=plan_version_id, status=RUNNING).aexists()` 为 True → 返回 `{"waiting": True}`（等下次回调，不推进）。**等待判定 keys off RUNNING 在途 task，不是最小 pending wave**。（若需 `acurrent_wave_all_terminal`，只能对这些 RUNNING 所在的 wave 求值。）
         b. **有可派发 pending → dispatch**：无 RUNNING；存在 `depends_on` 全为 `done` 的 pending task → 取其中**最小 wave** 的这批 task，返回 `{"dispatch": [待 dispatch task...], "wave": n}`（n=该最小可派发 wave）。
         c. **无 pending 且无 running → 收尾**：返回 `{"all_terminal": True}`。被阻断链全部 blocked（failed）后即落入此分支，保证收尾可达、不死锁。
    幂等：状态只经 service 条件更新（重复调用 no-op）；**绝不直接写 `task.status`**。fail-soft：函数自身不吞异常（由调用方 plan 05 包 try/except swallow），但用 service 幂等保证重复安全。中文 docstring，ruff line 100。
    在 `server/services/plan_orchestration/__init__.py` 新增 `from services.plan_orchestration.wave_progression import (acurrent_wave_all_terminal, aadvance_coding_waves)` 并追加进 `__all__`。
  </action>
  <verify>
    <automated>cd server && uv run python -c "from services.plan_orchestration import acurrent_wave_all_terminal, aadvance_coding_waves; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `cd server && uv run python -c "from services.plan_orchestration import acurrent_wave_all_terminal, aadvance_coding_waves"` 无 ImportError。
    - `cd server && uv run ruff check services/plan_orchestration/wave_progression.py` 通过。
    - grep 确认无 async 上下文裸访问 `task.subagent_session`（仅 `subagent_session_id` 标量 / `afirst`）与裸 `task.depends_on` async 访问。
    - 状态变更全经 `service.mark_*`（grep 确认 helper 内无直接 `task.status =` 或 `.objects...update(status=`）。
    - 下游阻断为**传递闭包**（worklist/BFS 沿 `dependents` 向下游多跳传播，含 `seen` 去重），非单跳——grep/审阅确认含 worklist 循环而非仅一次 `.dependents.all()` 迭代。
    - **step 顺序正确**：审阅确认 `aadvance_coding_waves` 在「决策出口（waiting/dispatch/all_terminal 任一 return）」**之前**已完成回填与传递闭包阻断——即没有任何 early-return 出现在阻断 step 之前。
    - **等待判定 keys off RUNNING**：审阅/grep 确认 waiting 分支条件为 `status=RUNNING` 的 `aexists()`，**不是**对「最小 pending wave」调 `acurrent_wave_all_terminal`；后者若使用仅对 RUNNING 在途 wave 求值。
  </acceptance_criteria>
  <done>wave 推进 helper 创建并经 barrel 导出；执行顺序为「回填→传递闭包阻断→决策出口」；等待判定按 RUNNING 在途、dispatch 按最小可派发 pending wave（depends_on 全 done）、收尾在无 pending 且无 running 时返回 all_terminal；下游阻断为传递闭包（间接下游同次标 blocked，收尾可达不死锁）、状态只经 service、async ORM 安全。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: wave 推进单测（gate/失败隔离/下游阻断/幂等）</name>
  <read_first>
    - server/services/plan_orchestration/wave_progression.py（Task 1 产物）
    - server/tests/delivery/test_repo_coding_task_service.py（plan 03 产物：fixture + SubAgentSession 构造）
    - server/services/plan_orchestration/research_aggregation.py（barrier 测试范式参考）
    - .planning/phases/44-repocodingtask-execution-plan-dag-wave/44-VALIDATION.md（WAVE-02 测试行 46-49；test 名 test_wave_gate/test_failure_isolation/test_downstream_blocked/test_idempotent）
  </read_first>
  <behavior>
    - test_wave_gate: wave0 有 1 running task（且 wave1 有 pending）→ aadvance 返回 {"waiting": True}（等待 keys off RUNNING 在途，**不**因 wave1 pending 而抢先 dispatch，也不死锁）；把 wave0 task 对应 SubAgentSession 置 done 后 aadvance → 回填 done、无 RUNNING、wave1 pending 的 depends_on 全 done → 返回 {"dispatch": [...], "wave": 1}。
    - test_failure_isolation: wave0 两仓，repoA done、repoB failed（均无 running）→ 同 wave 兄弟互不影响（repoA 仍 done，repoB failed）；因无 RUNNING，aadvance 不返回 waiting，继续做下游阻断与 dispatch 决策（依赖 repoA 的 pending 可派发、依赖 repoB 的被 blocked）。
    - test_downstream_blocked: repoC（wave1）depends_on repoB（wave0）；repoB failed（无 running）→ aadvance 把 repoC 标 blocked（status=failed, error.reason=upstream_failed），不出现在 dispatch 列表；此后无 pending 无 running → 同次返回 {"all_terminal": True}。
    - test_downstream_blocked_transitive_2hop（liveness 守护，防死锁）: 链 A(wave0)→B(wave1)→C(wave2)（B depends_on A，C depends_on B）；A failed → **单次 aadvance 内** B 与 C 均被标 blocked（status=failed, error.reason=upstream_failed），且**同一次 aadvance 后**返回 `{"all_terminal": True}`（无残留 pending → 收尾可达，不永久挂起）。断言 C 不会停留 pending、不出现在任何 dispatch 列表。
    - test_idempotent: 连续两次 aadvance（模拟重复 callback）→ 第二次 no-op，task 状态不重复翻转、不重复出现在 dispatch（断言 mark_done 只生效一次）。
    - test_all_terminal: 全 task done/failed → aadvance 返回 {"all_terminal": True}。
  </behavior>
  <action>
    新建 `server/tests/services/plan_orchestration/test_wave_progression.py`（`@pytest.mark.django_db` + `@pytest.mark.asyncio`）。用 plan 03 fixture 建 PlanVersion + 多 RepoCodingTask（经 RepoCodingTaskService.create_tasks_for_plan 或直接 service 写入，遵守 INV-6）+ mock/构造 SubAgentSession 终态（grep 现有 SubAgentSession 测试构造方式复用）。覆盖上述 5 场景。下游阻断断言用 `RepoCodingTask.objects.aget` 复读 error JSON。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/plan_orchestration/test_wave_progression.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `cd server && uv run pytest tests/services/plan_orchestration/test_wave_progression.py -x` 全绿。
    - `::test_wave_gate` / `::test_failure_isolation` / `::test_downstream_blocked` / `::test_idempotent` 各自可单独跑通（VALIDATION 引用名）。
    - test_idempotent 断言重复 aadvance 不重复推进（no-op）。
    - test_downstream_blocked 断言 error.reason == "upstream_failed"。
    - test_downstream_blocked_transitive_2hop 断言 2 跳链（A→B→C）A failed 后 **B 与 C 均 blocked** 且同次 aadvance 返回 `all_terminal=True`（liveness：收尾可达，无死锁）。
  </acceptance_criteria>
  <done>wave 推进单测全绿，覆盖 gate（全终态）、失败隔离、下游阻断（含 2 跳传递闭包 liveness 守护）、幂等、全终态收尾。</done>
</task>

</tasks>

<artifacts_this_phase_produces>
- **函数**：`acurrent_wave_all_terminal(plan_version_id, wave) -> bool`、`aadvance_coding_waves(plan_version_id, *, service=None) -> dict`（`server/services/plan_orchestration/wave_progression.py`）
- **barrel 导出**：`services.plan_orchestration.acurrent_wave_all_terminal` / `aadvance_coding_waves`
- **新文件**：`server/services/plan_orchestration/wave_progression.py`、`server/tests/services/plan_orchestration/test_wave_progression.py`
- **复用（不新增）**：`RepoCodingTaskService.mark_*`（plan 03）、`research_aggregation` barrier 范式、`SubAgentSession` 终态判定
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 容器回调 → wave 推进 | runner 经回调触发推进；推进读 SubAgentSession / RepoCodingTask 服务端权威状态判定 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-TAMPER | Tampering / Elevation | wave 归属与终态判定 | mitigate | 回填终态按服务端权威 `SubAgentSession.status`（经 `subagent_session_id` 标量取），**绝不**信 runner 经 progress 可篡改的 `last_output`（对齐 Phase 43 T-43-TAMPER）；推进范围限本 `plan_version_id` |
| T-44-IDEM | DoS | 重复回调 → 重复推进 / 重复 dispatch | mitigate | 状态回填经 `RepoCodingTaskService` 条件更新幂等（重复 no-op）；wave 从 DB 重算非内存态，并发已推进自然 no-op |
| T-44-GATE | Availability | wave gate 用「全 done」会因 failed 永挂 | mitigate | gate 用「全终态 done\|failed」，失败也是终态，不无限挂起 |
| T-44-DEADLOCK | Availability | 间接下游残留 pending（单跳阻断）→ all_terminal 永不触发、工作流死锁 | mitigate | 下游阻断为**传递闭包**（worklist/BFS 多跳）：上游 failed 时直接/间接下游在单次 aadvance 内全标 blocked，无残留 pending → 收尾可达；test_downstream_blocked_transitive_2hop 守护 liveness |

**Note:** 本 plan 无新外部端点、无凭证读取；唯一外部信号是回调触发，已用服务端权威字段守门 + 幂等 + 传递闭包阻断保证收尾可达（liveness）。
</threat_model>

<verification>
- `cd server && uv run pytest tests/services/plan_orchestration/test_wave_progression.py -x` 全绿。
- `cd server && uv run ruff check services/plan_orchestration/wave_progression.py` 通过。
- barrel 可导入两 helper。
</verification>

<success_criteria>
入口无关 wave 推进 helper 落地：wave gate=全终态、失败隔离、下游阻断、幂等、wave 从 DB 重算、状态只经 service；4+1 场景单测全绿。
</success_criteria>

<output>
Create `.planning/phases/44-repocodingtask-execution-plan-dag-wave/44-04-SUMMARY.md` when done
</output>
