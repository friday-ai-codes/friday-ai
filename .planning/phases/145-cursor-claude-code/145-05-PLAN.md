---
phase: 145-cursor-claude-code
plan: "05"
type: execute
wave: 3
depends_on: ["145-02", "145-03", "145-04"]
files_modified:
  - server/initiatives/services/ide_hook_assets.py
  - server/tests/initiatives/test_ide_hook_assets.py
  - skills
autonomous: false
requirements: [SKILL-03, SKILL-04, SKILL-05]
must_haves:
  truths:
    - "ide_hook_assets Cursor 采集走 before/after；stop 仅 project-memory 且无 Claude inject/report_session_knowledge（D-03, D-09, OQ-01）"
    - "Claude 写/读路径资产与实体 skills 事件模型一致：UPS 缓存+Stop 可见答案，Capture 无 git 门闩（D-01, D-02）"
    - "Codex 资产与语义零回归；无新 Python/npm 依赖（D-08）"
    - "父仓仅在 skills HEAD 可由远端 advertised ref 到达时暂存 gitlink；否则 distributed_asset_parity=blocked_on_skills_push，SKILL-04/SC3/Phase 145 不得宣称完成"
  artifacts:
    - path: "server/initiatives/services/ide_hook_assets.py"
      provides: "Cursor before/after、Claude UPS/Stop Capture 实体脚本与隔离的 project-memory stop"
      contains: "afterAgentResponse"
    - path: "server/tests/initiatives/test_ide_hook_assets.py"
      provides: "事件/字段/路径/安全不变量（非 byte-for-byte）"
  key_links:
    - from: "server/initiatives/services/ide_hook_assets.py"
      to: "skills/hooks/lib/session_capture.py"
      via: "不变量对齐而非 import 子模块源"
      pattern: "report_session_knowledge"
    - from: "server/tests/mcp_tools/test_skills_snapshot_guard.py"
      to: "skills/skills"
      via: "checkout gitlink 文档"
      pattern: "report_session_knowledge"
---

<objective>
让服务端 ide_hook_assets 与 skills 分发面对齐 SKILL-03/04/05（D-03, D-09），并按子模块先、父仓 gitlink 后的顺序收口。不改 report_session_knowledge 12 键。

Purpose: 控制台下载资产不能继续只注册 Cursor stop 当答案源。
Output: 更新的 generate bundle + 绿的资产测试；Codex 不变。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/145-cursor-claude-code/145-PATTERNS.md
@.planning/phases/145-cursor-claude-code/145-03-PLAN.md
@server/initiatives/services/ide_hook_assets.py
@server/tests/initiatives/test_ide_hook_assets.py
@skills/hooks/cursor/hooks.json
@skills/hooks/lib/session_capture.py
</context>

<interfaces>
build_write_path_assets(project, RUNTIME_CURSOR) files 必须包含：.cursor/hooks.json（version 1，hooks.beforeSubmitPrompt 与 afterAgentResponse；可保留 stop）、.cursor/hooks/friday-before-submit-prompt.sh、.cursor/hooks/friday-after-agent-response.sh、以及现有 .cursor/hooks/friday-stop-writeback.sh（project knowledge/STATE，行为保持 _stop_writeback_script）。

Cursor Capture 脚本：短 timeout、exit 0、report_session_knowledge、无 last_assistant_message、无 hookSpecificOutput/additionalContext、无 afterAgentThought、不以 diff --stat 为 Capture 门闩。

build_write_path_assets(project, RUNTIME_CLAUDE_CODE) 的 by_path 必须真实包含 `.claude/hooks/friday-session-capture-user-prompt-submit.sh` 与 `.claude/hooks/friday-session-capture-stop.sh`：前者缓存官方 `prompt`，后者只取 `last_assistant_message` 并调用 `report_session_knowledge`，Capture 不得被 empty-diff 跳过；`.claude/settings.json` 必须按真实路径注册 UserPromptSubmit cache 与 Stop Capture。既有 `.claude/hooks/friday-stop-writeback.sh` 独立保留 project knowledge/STATE，且不得包含 `question`、`answer`、`last_assistant_message`、`report_session_knowledge` 等 Capture 字段。notes 不能替代实体文件。

禁止 runtime import skills/hooks。不要 byte-for-byte 拷贝 npm 包脚本（项目资产可含 FRIDAY 项目环境变量约定）。
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 扩展 ide_hook_assets Cursor/Claude Capture 资产</name>
  <files>server/initiatives/services/ide_hook_assets.py</files>
  <behavior>
    - test_ide_hook_assets 用 by_path 断言 Claude 两个 Capture 实体脚本与 settings 注册均存在并可执行（D-01, D-09, SKILL-03/04）
    - test_write_path_cursor_stop_hook 仍通过且增加 afterAgentResponse（OQ-01）
    - Claude/Cursor 的 friday-stop-writeback.sh 只含 report_project_knowledge/report_project_state，不含任何 Capture 字段
    - Codex test_write_path_codex_manual_script 与 parametrize 静默/兜底断言零回归
  </behavior>
  <action>
    新增独立 session Capture 脚本生成器，保留 _stop_writeback_script。为 Claude bundle 产出 `.claude/hooks/friday-session-capture-user-prompt-submit.sh` 与 `.claude/hooks/friday-session-capture-stop.sh` 实体文件，并在 `.claude/settings.json` 用这些路径注册 UserPromptSubmit/Stop；不得只在 notes 描述。更新 _cursor_stop_hooks_snippet 为含 before/after（及可选 stop）的完整 v1 JSON，函数可改名但调用点必须输出完整 hooks.json。Cursor/Claude project-memory stop 脚本都不得出现 report_session_knowledge 或 Capture 字段。更新 notes：采集走专属 Capture 脚本；stop writeback 仅为交付记忆。无 PAT 仍 exit 0。不 uv add。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/initiatives/test_ide_hook_assets.py -x --tb=short</automated>
  </verify>
  <done>资产测试全绿，含 Codex。</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: skills 子 SHA 远端可达发布门</name>
  <files>skills</files>
  <what-built>145-02..04 已在 skills 子模块形成 commit，父仓 server 资产已通过独立测试；父仓尚未暂存 skills gitlink。</what-built>
  <action>
    执行 fetch + advertised-tip ancestry 自动检查。检查失败时执行器只记录 `distributed_asset_parity: blocked_on_skills_push` 并停止；不得暂存 gitlink、不得 push、不得声明阶段完成。等待操作员把 child SHA 发布到远端 ref 后再复核。
  </action>
  <how-to-verify>
    1. 执行器先运行 `git -C skills fetch origin --prune --tags`，取得 `child_sha=$(git -C skills rev-parse HEAD)`。
    2. 读取 `git -C skills ls-remote --heads --tags origin` 的全部 advertised tips（忽略 peeled `^{}` 行）；对每个 advertised tip SHA 运行 `git -C skills merge-base --is-ancestor "$child_sha" "$tip_sha"`。任一成功才表示 child SHA 由远端公开 ref 可达。禁止用 `git ls-remote origin "$child_sha"` 或 raw SHA pattern 冒充可达性证明。
    3. 若没有任一 advertised tip 包含 child SHA：立即停止，不得 `git add skills`、不得暂存/提交 gitlink、不得 push（用户未授权）。在 145-05-SUMMARY frontmatter 写 `distributed_asset_parity: blocked_on_skills_push`，正文列出 child SHA；不得声明 SKILL-04、ROADMAP SC3 或 Phase 145 complete。
    4. 该阻塞只能由操作员在 skills 仓按其发布流程把 child commit 推到远端 ref 后解除；执行器自身永不 push。操作员完成后重新运行步骤 1-2。
  </how-to-verify>
  <verify>
    <automated>bash -lc 'set -e; child_sha=$(git -C skills rev-parse HEAD); git -C skills fetch origin --prune --tags; tips=$(git -C skills ls-remote --heads --tags origin | awk '"'"'$2 !~ /\\^\\{\\}$/ {print $1}'"'"'); test -n "$tips"; found=1; for tip_sha in $tips; do if git -C skills merge-base --is-ancestor "$child_sha" "$tip_sha"; then found=0; break; fi; done; test "$found" -eq 0'</automated>
  </verify>
  <done>自动门通过并由操作员确认发布 ref 后才可继续；失败时 SUMMARY 明确 blocked_on_skills_push，父仓 gitlink 未暂存。</done>
  <resume-signal>操作员确认 skills commit 已推送，并提供远端 branch/tag；随后回复 “skills pushed” 以重跑可达门。</resume-signal>
</task>

<task type="auto">
  <name>Task 3: 远端门通过后暂存 gitlink并完成回归</name>
  <files>skills, server/tests/initiatives/test_ide_hook_assets.py</files>
  <action>
    仅在 Task 2 的 fetch + advertised-tip ancestry 检查通过后，才允许在父仓暂存 skills gitlink。父仓一次 commit 同时包含 gitlink、ide_hook_assets.py、必要时微调的 test_ide_hook_assets.py；禁止 git add skills/hooks 当普通文件。跑 VALIDATION full suite：hooks、ide assets、snapshot guard、report_session_knowledge/report_project_knowledge、MCP npm 与 skills node:test。若 12 键漂移则停止并回 Phase 142。若远端门失败则本任务不得开始，distributed_asset_parity 保持 blocked_on_skills_push。
  </action>
  <verify>
    <automated>bash -lc 'set -e; child_sha=$(git -C skills rev-parse HEAD); git -C skills fetch origin --prune --tags; tips=$(git -C skills ls-remote --heads --tags origin | awk '\"'\"'$2 !~ /\\^\\{\\}$/ {print $1}'\"'\"'); test -n \"$tips\"; found=1; for tip_sha in $tips; do if git -C skills merge-base --is-ancestor \"$child_sha\" \"$tip_sha\"; then found=0; break; fi; done; test \"$found\" -eq 0' && cd server && uv run pytest tests/hooks/test_session_capture_hooks.py tests/initiatives/test_ide_hook_assets.py tests/mcp_tools/test_skills_snapshot_guard.py tests/mcp_tools/test_report_session_knowledge.py tests/mcp_tools/test_report_project_knowledge.py tests/mcp_tools/test_schema_snapshot.py::test_registered_tools_match_snapshot tests/mcp_tools/test_mcp_package_alignment.py::test_report_session_knowledge_request_keys_aligned -x --tb=short && cd ../skills && node --test lib/*.test.mjs && cd ../mcp && npm test -- tests/server.test.ts</automated>
  </verify>
  <done>仅远端可达门与全套测试都绿时，父仓 gitlink 才可提交并声明 distributed_asset_parity=complete；否则保持 blocked_on_skills_push 且不声明 SKILL-04/SC3/Phase 完成。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 下载资产 → 用户机器 hook | 错误脚本会注入或泄漏 |
| 父仓 gitlink → CI checkout | 未推送 SHA 使 CI 红或装错包 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-145-04 | Tampering | Cursor stop asset | mitigate | stop 无 session Capture 字段 |
| T-145-03 | Tampering | Cursor scripts | mitigate | 无 additionalContext/hookSpecificOutput |
| T-145-05 | Denial of Service | generated hooks | mitigate | 不写 failClosed true；exit 0 |
| T-145-01 | Information Disclosure | generated curl | mitigate | 不 echo PAT |
| T-145-SC | Tampering | installs | accept | 无新包 |
</threat_model>

<verification>
本计划最终门禁是 Task 2 操作员发布 + Task 3 advertised-tip ancestry 自动复核。用户未授权 push，执行器永不 push；child SHA 未远端可达时这是预期的真实阻塞，不得被 auto-advance 绕过。真实 IDE 点一次安装可作为事后 smoke。
<human-check>可选：本机 Cursor/Claude 装 skills 后一轮问答且干净 git 树仍产生 Capture；非 Nyquist 门禁。</human-check>
</verification>

<success_criteria>
只有 distributed_asset_parity=complete 后，ROADMAP Phase 145 成功标准 1-5 才可宣称满足；blocked_on_skills_push 时 SKILL-04/SC3/Phase 145 保持未完成。MCP-04 report_project_knowledge 零回归。
</success_criteria>

<output>
Create `.planning/phases/145-cursor-claude-code/145-05-SUMMARY.md` when done
</output>
