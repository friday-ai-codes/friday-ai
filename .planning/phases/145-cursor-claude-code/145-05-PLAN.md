---
phase: 145-cursor-claude-code
plan: "05"
type: execute
wave: 3
depends_on: ["145-02", "145-03", "145-04"]
files_modified:
  - server/initiatives/services/ide_hook_assets.py
  - server/tests/initiatives/test_ide_hook_assets.py
  - skills
autonomous: true
requirements: [SKILL-03, SKILL-04, SKILL-05]
must_haves:
  truths:
    - "ide_hook_assets Cursor 采集走 before/after；stop 仅 project-memory 且无 Claude inject/report_session_knowledge（D-03, D-09, OQ-01）"
    - "Claude 写/读路径资产与实体 skills 事件模型一致：UPS 缓存+Stop 可见答案，Capture 无 git 门闩（D-01, D-02）"
    - "Codex 资产与语义零回归；无新 Python/npm 依赖（D-08）"
    - "父仓同一 commit 含已 push 的 skills gitlink 与 server 资产/守卫，禁止未推送 SHA（PATTERNS 提交序）"
  artifacts:
    - path: "server/initiatives/services/ide_hook_assets.py"
      provides: "Cursor before/after 脚本与 hooks.json v1"
      contains: "afterAgentResponse"
    - path: "server/tests/initiatives/test_ide_hook_assets.py"
      provides: "事件/字段/路径/安全不变量（非 byte-for-byte）"
  key_links:
    - from: "server/initiatives/services/ide_hook_assets.py"
      to: "skills/hooks/lib/session_capture.py"
      via: "不变量对齐而非 import 子模块源"
      pattern: "report_session_knowledge"
    - from: "server/tests/mcp_tools/test_skills_snapshot_guard.py"
      to: "skills/skills"
      via: "checkout gitlink 文档"
      pattern: "report_session_knowledge"
---

<objective>
让服务端 ide_hook_assets 与 skills 分发面对齐 SKILL-03/04/05（D-03, D-09），并按子模块先、父仓 gitlink 后的顺序收口。不改 report_session_knowledge 12 键。

Purpose: 控制台下载资产不能继续只注册 Cursor stop 当答案源。
Output: 更新的 generate bundle + 绿的资产测试；Codex 不变。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/145-cursor-claude-code/145-PATTERNS.md
@.planning/phases/145-cursor-claude-code/145-03-PLAN.md
@server/initiatives/services/ide_hook_assets.py
@server/tests/initiatives/test_ide_hook_assets.py
@skills/hooks/cursor/hooks.json
@skills/hooks/lib/session_capture.py
</context>

<interfaces>
build_write_path_assets(project, RUNTIME_CURSOR) files 必须包含：.cursor/hooks.json（version 1，hooks.beforeSubmitPrompt 与 afterAgentResponse；可保留 stop）、.cursor/hooks/friday-before-submit-prompt.sh、.cursor/hooks/friday-after-agent-response.sh、以及现有 friday-stop-writeback.sh（project knowledge/STATE，行为保持 _stop_writeback_script）。

Cursor Capture 脚本：短 timeout、exit 0、report_session_knowledge、无 last_assistant_message、无 hookSpecificOutput/additionalContext、无 afterAgentThought、不以 diff --stat 为 Capture 门闩。

Claude：可在既有 friday-context-inject / Stop writeback 之外增加与 skills 一致的 Capture 说明或脚本片段，但不得把 UPS 注入逻辑拷进 Cursor stop。settings 仍注册 Stop；Capture 不得被 empty-diff 跳过。若 Claude 下载资产仍只有 project-memory stop 脚本，必须在 notes 与/或独立 Capture 脚本中体现 UserPromptSubmit+last_assistant_message 路径，且测试 145-01 Task 3 要绿。

禁止 runtime import skills/hooks。不要 byte-for-byte 拷贝 npm 包脚本（项目资产可含 FRIDAY 项目环境变量约定）。
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 扩展 ide_hook_assets Cursor/Claude Capture 资产</name>
  <files>server/initiatives/services/ide_hook_assets.py</files>
  <behavior>
    - test_ide_hook_assets 145-01 扩展断言变绿（D-03, D-09, SKILL-03/04）
    - test_write_path_cursor_stop_hook 仍通过且增加 afterAgentResponse（OQ-01）
    - Codex test_write_path_codex_manual_script 与 parametrize 静默/兜底断言零回归
  </behavior>
  <action>
    新增独立 session Capture 脚本生成器，保留 _stop_writeback_script。更新 _cursor_stop_hooks_snippet 为含 before/after（及可选 stop）的完整 v1 JSON，函数可改名但调用点必须输出完整 hooks.json。Cursor stop 脚本不得出现 report_session_knowledge。更新 notes：采集走 afterAgentResponse；stop 仅为交付记忆。无 PAT 仍 exit 0。不 uv add。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/initiatives/test_ide_hook_assets.py -x --tb=short</automated>
  </verify>
  <done>资产测试全绿，含 Codex。</done>
</task>

<task type="auto">
  <name>Task 2: gitlink 原子收口与回归门禁</name>
  <files>skills, server/tests/initiatives/test_ide_hook_assets.py</files>
  <action>
    确认 skills 子模块 145-02..04 的 commit 已在可拉取 remote（至少本地 skills/.git 有独立 commit SHA）。父仓一次 commit 同时包含：git submodule 指针、ide_hook_assets.py、必要时微调的 test_ide_hook_assets.py。禁止 git add skills/hooks 当普通文件。跑 VALIDATION full suite 中本阶段相关命令：hooks 行为、ide assets、snapshot guard、report_session_knowledge 与 report_project_knowledge、mcp npm tests/server.test.ts、skills node:test。若 12 键漂移则停止并回 Phase 142，不要在本任务改 serializer。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/hooks/test_session_capture_hooks.py tests/initiatives/test_ide_hook_assets.py tests/mcp_tools/test_skills_snapshot_guard.py tests/mcp_tools/test_report_session_knowledge.py tests/mcp_tools/test_report_project_knowledge.py tests/mcp_tools/test_schema_snapshot.py::test_registered_tools_match_snapshot tests/mcp_tools/test_mcp_package_alignment.py::test_report_session_knowledge_request_keys_aligned -x --tb=short && cd ../skills && node --test lib/*.test.mjs && cd ../mcp && npm test -- tests/server.test.ts</automated>
  </verify>
  <done>阶段 full 相关套件绿；pyproject.toml / skills 与 mcp package.json 无新 runtime 依赖；父仓 gitlink 指向 skills 已提交 SHA。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 下载资产 → 用户机器 hook | 错误脚本会注入或泄漏 |
| 父仓 gitlink → CI checkout | 未推送 SHA 使 CI 红或装错包 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-145-04 | Tampering | Cursor stop asset | mitigate | stop 无 session Capture 字段 |
| T-145-03 | Tampering | Cursor scripts | mitigate | 无 additionalContext/hookSpecificOutput |
| T-145-05 | Denial of Service | generated hooks | mitigate | 不写 failClosed true；exit 0 |
| T-145-01 | Information Disclosure | generated curl | mitigate | 不 echo PAT |
| T-145-SC | Tampering | installs | accept | 无新包 |
</threat_model>

<verification>
本计划门禁即 Task 2 automated 命令。human_verify_mode=end-of-phase：真实 IDE 点一次安装可作为事后 smoke，不阻塞本计划。
<human-check>可选：本机 Cursor/Claude 装 skills 后一轮问答且干净 git 树仍产生 Capture；非 Nyquist 门禁。</human-check>
</verification>

<success_criteria>
ROADMAP Phase 145 成功标准 1-5 均有自动化证据；MCP-04 report_project_knowledge 零回归。
</success_criteria>

<output>
Create `.planning/phases/145-cursor-claude-code/145-05-SUMMARY.md` when done
</output>
