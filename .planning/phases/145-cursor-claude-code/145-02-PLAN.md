---
phase: 145-cursor-claude-code
plan: "02"
type: execute
wave: 1
depends_on: ["145-01"]
files_modified:
  - skills/hooks/lib/session_capture.py
  - skills/hooks/user-prompt-submit
  - skills/hooks/stop
  - skills/hooks/hooks.json
autonomous: true
requirements: [SKILL-01, SKILL-02, SKILL-03, SKILL-04, SKILL-05]
must_haves:
  truths:
    - "Claude UserPromptSubmit 先按 session 隔离缓存 prompt（0600），stdout 仍只注入 lookup 上下文（D-01）"
    - "Claude Stop 用 last_assistant_message 配对后 POST report_session_knowledge，不看 git diff；project knowledge 仍 empty-diff skip（D-02, SKILL-02）"
    - "两宿主共用 session_capture.py；无 CoT/transcript；缺 PAT/网络 fail-soft exit 0（D-06, D-07, D-08）"
  artifacts:
    - path: "skills/hooks/lib/session_capture.py"
      provides: "配对、精华、POST、TTL、HTTP record/force seam"
      contains: "report_session_knowledge"
    - path: "skills/hooks/user-prompt-submit"
      provides: "缓存 prompt 后 lookup"
    - path: "skills/hooks/stop"
      provides: "Capture 与 project-memory 分轨"
  key_links:
    - from: "skills/hooks/stop"
      to: "skills/hooks/lib/session_capture.py"
      via: "Stop.last_assistant_message 配对 POST"
      pattern: "last_assistant_message"
    - from: "skills/hooks/lib/session_capture.py"
      to: "/api/mcp/tools/report_session_knowledge/"
      via: "urllib 或 FRIDAY_CAPTURE_HTTP_RECORD"
      pattern: "report_session_knowledge"
---

<objective>
在 skills 子模块落地共用 Python 标准库 helper，并把 Claude Code UserPromptSubmit/Stop 接到 Capture（D-01, D-02, D-05, D-06, D-07, D-08）。OQ-02：background_tasks 非空仍上报本次可见答案。

Purpose: 消除 stop 里 empty-diff fail_soft 误杀 Capture 的现行缺口。
Output: session_capture.py + 增量接线的 user-prompt-submit/stop；hooks.json 官方事件名不变。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/145-cursor-claude-code/145-01-PLAN.md
@.planning/phases/145-cursor-claude-code/145-RESEARCH.md
@.planning/phases/145-cursor-claude-code/145-PATTERNS.md
@skills/hooks/user-prompt-submit
@skills/hooks/stop
@skills/hooks/hooks.json
@server/mcp_tools/serializers.py
@server/tests/hooks/test_session_capture_hooks.py
</context>

<interfaces>
session_capture.py（系统 python3 stdlib only：json os sys time hashlib pathlib tempfile urllib.request urllib.error）：提供 cache_user_prompt(event, client) 与 submit_visible_answer(event, client)（client 为 claude_code 或 cursor）。可被 bash 以 python3 path/to/session_capture.py cache|submit client_name 从 stdin 读 JSON 调用。

session_key：conversation_id 然后 session_id；否则 generation_id 与 workspace_roots[0] 或 cwd 同时存在时 stable hash；否则空串 → 调用方 skip。generation 匹配消费；generation 缺失仅当该 session 恰好一条 pending 才消费，否则 skip（D-05）。

凭证顺序与现 hook 相同：FRIDAY_BASE_URL+FRIDAY_ACCESS_TOKEN，其次 FRIDAY_API_URL+FRIDAY_PAT，再次 ~/.friday/config.json 的 baseUrl/accessToken。POST {base}/api/mcp/tools/report_session_knowledge/，timeout 10。成功：HTTP 200 且 JSON accepted 缺省真 → 删除 pending。失败不消费 pending、不把 body 写入 cache/stdout。FRIDAY_CAPTURE_HTTP_RECORD 记录成功请求；FRIDAY_CAPTURE_HTTP_FORCE 闭集 `timeout`/`http_error` 必须在发网前确定性进入对应失败分支，方便 subprocess 测试且不打真实网络。provider/token 字段空串不猜测。不传 project_id/repository_id。git_url 来自 git remote get-url origin 原样，无仓则空。

FRIDAY_HOOKS_DISABLED=1 时 wrapper exit 0 且不写 pending。FRIDAY_STOP_WRITEBACK=0 只跳过 project-memory 分支，Capture 仍跑。
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 实现 session_capture.py 共用 helper</name>
  <files>skills/hooks/lib/session_capture.py</files>
  <behavior>
    - 同一 POST 构造器服务两宿主；client 字面量正确（D-06）
    - 剥离 thinking/thought 块后空白 skip；不读 transcript_path 键对应文件（D-07）
    - 截断 16000；TTL 24h 清理；0600/0700；文件名无 token（D-08）
    - FRIDAY_CAPTURE_HTTP_RECORD 写入请求记录且不开 socket；FRIDAY_CAPTURE_HTTP_FORCE=timeout/http_error 确定性模拟两类失败且不开 socket
    - 缺凭证、urlopen 异常、非 2xx：不抛、返回 skip/fail 并由 wrapper exit 0
  </behavior>
  <action>
    按 PATTERNS.md session_capture.py 段实现零新依赖 helper。白名单答案字段仅 last_assistant_message（Claude）与 text（Cursor）。忽略 user_email、afterAgentThought、transcript_path 内容。原子写用临时文件加 os.replace。锁用同目录 lock 文件，锁失败视为 skip 且宿主仍 0。实现 FRIDAY_CAPTURE_HTTP_FORCE 的 timeout/http_error 闭集故障缝，且在 urllib 前执行。本文件只存在于 skills 子模块，禁止复制进 server/。本计划只允许在 skills 子模块提交，明确禁止在父仓暂存/提交 skills gitlink。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/hooks/test_session_capture_hooks.py::test_helper_builds_shared_payload tests/hooks/test_session_capture_hooks.py::test_claude_stop_fail_soft_modes_keep_pending -x --tb=short && python3 -m py_compile ../skills/hooks/lib/session_capture.py</automated>
  </verify>
  <done>helper 可被 python3 编译；Claude 相关 hook 测试在后续任务接线后变绿的前置已具备。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: UserPromptSubmit 先缓存问题再 lookup</name>
  <files>skills/hooks/user-prompt-submit</files>
  <behavior>
    - 读取官方 prompt 与 session_id（及 cwd）写入 pending，失败不影响 lookup（D-01）
    - stdout 仍仅为 hookSpecificOutput.additionalContext lookup 结果，不含 pending 路径/prompt/Bearer（T-145-03）
    - 无 PAT 时仍尝试写 pending（Stop 同样无 PAT 则 skip POST）；无 python3/FRIDAY_HOOKS_DISABLED 则 exit 0
  </behavior>
  <action>
    保留现有 shell wrapper 与 lookup/cache/注入。在解析 event 之后、因缺 PAT 或非 git 而 fail_soft 之前调用 helper 缓存 prompt（client=claude_code）。不得把缓存 IO 打印到 stdout。hooks.json 保持 UserPromptSubmit async false，不改事件名（D-09）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/hooks/test_session_capture_hooks.py::test_claude_user_prompt_submit_caches_prompt tests/hooks/test_session_capture_hooks.py::test_hook_outputs_do_not_leak_sensitive_values -x --tb=short</automated>
  </verify>
  <done>Claude 缓存问题测试绿；stdout 无敏感泄漏。执行器用实际测试名过滤，若 -k 过窄则跑 tests/hooks/test_session_capture_hooks.py 中 Claude UPS 用例。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Stop Capture 与 project-memory 分轨</name>
  <files>skills/hooks/stop, skills/hooks/hooks.json</files>
  <behavior>
    - Capture 使用 last_assistant_message；stop_hook_active true / 空答案 / 无可靠 pending / 重复 Stop 不二次 POST（D-01, SKILL-03）
    - 不把 git diff --stat 作为 Capture 前置；clean tree 仍 POST（D-02, SKILL-02）
    - project-memory 完整保留现有 diff/间隔/指纹/report_project_knowledge；empty diff 只 skip 该分支，不得 fail_soft 整个进程
    - background_tasks 非空仍 Capture（OQ-02）
    - 所有路径 exit 0（SKILL-05）
  </behavior>
  <action>
    将当前 L77-81 的 if not changes: fail_soft() 移入 project-memory 函数并改为 return。Capture 在凭证缺失时 skip POST 但不阻止后续 project-memory（若有 PAT）。Stop 保持 async true。不要输出 Cursor followup_message 或 Claude block。完成后只在 skills/ 提交本计划文件。本计划明确禁止在父仓执行 `git add skills`、`git add skills/...`、暂存或提交 skills gitlink；gitlink 只能由 145-05 在子 SHA 远端可达后处理。用户未授权 push，执行器不得 push。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/hooks/test_session_capture_hooks.py::test_claude_stop_posts_visible_answer_on_clean_tree tests/hooks/test_session_capture_hooks.py::test_claude_stop_preserves_project_memory_gate_on_dirty_tree tests/hooks/test_session_capture_hooks.py::test_claude_stop_fail_soft_modes_keep_pending tests/hooks/test_session_capture_hooks.py::test_claude_stop_strips_thinking_and_ignores_transcript tests/hooks/test_session_capture_hooks.py::test_claude_stop_handles_background_tasks -x --tb=short</automated>
  </verify>
  <done>VALIDATION 中 Claude/SKILL-02/fail-soft/CoT 相关 hook 测试绿；project knowledge empty-diff 仍 skip。执行器以文件内测试名为准跑完整 tests/hooks/test_session_capture_hooks.py 中非 Cursor wrapper 部分。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude stdin JSON → pending/POST | 只信白名单字段 |
| helper → Friday MCP | PAT 仅 Authorization 头 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-145-01 | Information Disclosure | session_capture / wrappers | mitigate | 永不 print token；异常不附 body |
| T-145-02 | Information Disclosure | pairs dir | mitigate | 仅 ~/.cache；不进 git |
| T-145-06 | Tampering | event JSON | mitigate | json.loads dict；prompt 不进 shell eval |
| T-145-07 | Information Disclosure | pairing | mitigate | generation 匹配否则 skip |
| T-145-08 | Information Disclosure | payload | mitigate | 忽略 user_email |
| T-145-09 | Information Disclosure | answer | mitigate | 不读 transcript；只删 thinking 块不补内容 |
| T-145-SC | Tampering | deps | accept | 无新包 |
</threat_model>

<verification>
独立验证本计划：运行各任务列出的显式 pytest node ID；Cursor wrapper 文件若仍缺失，对应测试可继续红（属 145-03）。禁止 broad `-k` 过滤，也禁止为绿而跳过 Claude/helper 失败。父仓 skills gitlink 必须保持未暂存。
</verification>

<success_criteria>
skills/hooks/lib/session_capture.py 存在且被 UPS/Stop 调用；clean tree Claude 会话会写入 HTTP record 含 question/answer/client=claude_code；stop 不再对 Capture 使用 empty-diff 退出。
</success_criteria>

<output>
Create `.planning/phases/145-cursor-claude-code/145-02-SUMMARY.md` when done
</output>
