---
phase: 145-cursor-claude-code
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - skills/package.json
  - skills/lib/cursor-hooks-merge.test.mjs
  - server/tests/hooks/conftest.py
  - server/tests/hooks/test_session_capture_hooks.py
  - server/tests/initiatives/test_ide_hook_assets.py
  - server/tests/mcp_tools/test_skills_snapshot_guard.py
autonomous: true
requirements: [SKILL-01, SKILL-02, SKILL-03, SKILL-04, SKILL-05]
must_haves:
  truths:
    - "Wave 0 把 Claude/Cursor 配对、clean-tree Capture、无 CoT、fail-soft 与 hooks.json merge 钉成可收集 RED 测试（D-01..D-09）"
    - "本计划只写测试与 skills 的 node:test script，不实现 hook/helper/installer 行为、不加 runtime 依赖"
    - "skills 子模块测试文件先在 skills 仓提交；父仓只提交 server/tests 与（若已有）gitlink，禁止 git add skills 当普通文件"
  artifacts:
    - path: "skills/lib/cursor-hooks-merge.test.mjs"
      provides: "SKILL-05 merge/幂等/非法 JSON RED"
    - path: "server/tests/hooks/test_session_capture_hooks.py"
      provides: "SKILL-01..05 hook 行为 RED"
    - path: "server/tests/hooks/conftest.py"
      provides: "XDG_CACHE_HOME、git fixture、HTTP record seam"
  key_links:
    - from: "server/tests/hooks/test_session_capture_hooks.py"
      to: "skills/hooks/lib/session_capture.py"
      via: "subprocess bash wrappers + FRIDAY_CAPTURE_HTTP_RECORD"
      pattern: "FRIDAY_CAPTURE_HTTP_RECORD"
    - from: "skills/lib/cursor-hooks-merge.test.mjs"
      to: "skills/lib/installer.mjs"
      via: "dynamic import mergeCursorHooksConfig"
      pattern: "mergeCursorHooksConfig"
---

<objective>
为 Phase 145 建立 tracer-first Wave 0 RED 合同，覆盖 SKILL-01..05（D-01 Claude UserPromptSubmit+Stop.visible answer；D-02 无 git 门闩；D-03 Cursor before/after 且不用 stop 采集；D-04 hooks.json v1 merge；D-05 可靠配对键；D-06 共用 POST 体；D-07 无 CoT/transcript；D-08 fail-soft 无新依赖；D-09 资产/文档守卫）。

Purpose: 实现未落地时，empty-diff skip、Cursor stop 抽答案、整文件覆盖 hooks.json、stdout 泄漏 PAT 都无法假绿。
Output: pytest/node:test 可收集；生产行为仍 RED。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/145-cursor-claude-code/145-CONTEXT.md
@.planning/phases/145-cursor-claude-code/145-RESEARCH.md
@.planning/phases/145-cursor-claude-code/145-PATTERNS.md
@.planning/phases/145-cursor-claude-code/145-VALIDATION.md
@.planning/phases/142-mcp/142-VERIFICATION.md
@.planning/phases/144-capture/144-VERIFICATION.md
@skills/lib/installer.mjs
@skills/hooks/stop
@skills/hooks/user-prompt-submit
@skills/hooks/hooks.json
@server/tests/initiatives/test_ide_hook_assets.py
@server/tests/mcp_tools/test_skills_snapshot_guard.py
@server/initiatives/services/ide_hook_assets.py
</context>

<interfaces>
Wave 0 钉死后续实现必须满足的测试缝与契约（实现在 145-02..05）。

HTTP 测试缝（子进程无法被 pytest patch urlopen）：环境变量 FRIDAY_CAPTURE_HTTP_RECORD 若为可写文件路径，helper 把 POST 的 url 与 JSON body 追加为一行 JSON，并视为 HTTP 200 且 accepted 缺省 true；未设置时才用 urllib.request.urlopen(timeout=10)。生产永不设置该变量。

共用 POST 体键（D-06，对齐 ReportSessionKnowledgeRequestSerializer，本阶段不改 serializer）：question, answer 必填非空；可选 git_url, branch_name, session_id, response_model, provider, input_tokens, output_tokens, client。禁止 project_id、repository_id、user_email。client 仅为 cursor 或 claude_code。

配对常量（D-05，discretion 钉死）：缓存目录 ${XDG_CACHE_HOME:-~/.cache}/friday-skills/pairs/；目录 0o700、文件 0o600；文件名 pending-{session_hash}-{generation_or_seq}.json 只含哈希不含 token/prompt；TTL 24h；答案 strip 后截断 16000 Unicode 字符；删除 thinking/thought 标签块后若空则 skip。

OQ 决议（写入测试注释并在后续计划实现）：OQ-01 保留 Cursor stop 仅作已有 project-memory 路径，Capture 永不走 stop；OQ-02 Claude Stop 在 background_tasks 非空时仍用本次 last_assistant_message 上报。
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: skills merge RED 与 node:test script</name>
  <files>skills/package.json, skills/lib/cursor-hooks-merge.test.mjs</files>
  <behavior>
    - 空/缺失目标生成 version 1 且含 beforeSubmitPrompt 与 afterAgentResponse Friday 条目（D-04）
    - 保留未知顶级键、用户 beforeSubmitPrompt/stop/其他事件；Friday 仅按 basename friday-before-submit-prompt.sh 与 friday-after-agent-response.sh 去重升级
    - 两次 merge deepEqual；project command 为 .cursor/hooks/...，global 为 ./hooks/...
    - 非法 JSON：原文件 bytes 不变且返回可操作 warning，不写空对象（D-04）
    - 输出无 failClosed true（T-145-05）
    - package.json 增加 test 脚本 node --test lib/*.test.mjs；dependencies 与 devDependencies 不新增包（D-08）
  </behavior>
  <action>
    仅在 skills 子模块内改文件。在 skills/lib/cursor-hooks-merge.test.mjs 用 node:test 与 node:assert/strict 动态 import installer.mjs：断言将导出 mergeCursorHooksConfig（纯函数：existing 对象或 null + fridayEntries）以及 writeMergedCursorHooksFile（路径 + fridayEntries，非法 JSON 不截断）。现有 installer 尚未导出这些函数时测试必须失败（RED），不要在本任务实现 merge。skills/package.json 的 scripts 只追加 test 键，禁止新增任何 npm 依赖。本任务完成后先在 skills/ 内 git commit（子模块独立 SHA），父仓本任务不要 git add skills 工作区文件。
  </action>
  <verify>
    <automated>cd skills && node --test lib/cursor-hooks-merge.test.mjs</automated>
  </verify>
  <done>node:test 文件存在且因缺少 merge 导出或行为未实现而失败（或 collect 后断言失败）；package.json 有 test 脚本且无新 dependency。本 wave 不要求该命令全绿。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: hook 行为 RED（Claude/Cursor/clean-tree/fail-soft/CoT）</name>
  <files>server/tests/hooks/conftest.py, server/tests/hooks/test_session_capture_hooks.py</files>
  <behavior>
    - Claude UserPromptSubmit.prompt + Stop.last_assistant_message 经同一 helper POST，client=claude_code，question/answer 非空（D-01, D-06, SKILL-01）
    - Cursor beforeSubmitPrompt.prompt + afterAgentResponse.text 同 helper，client=cursor；session 优先 conversation_id 再 session_id（D-03, D-05）
    - clean 与 dirty git tree 均 POST Capture；无 git 仓仍 POST 且 git_url/branch_name 可空（D-02, SKILL-02）
    - 仅 dirty 触发 report_project_knowledge；Capture 路径不得因 empty diff 提前退出整个 Stop（D-02）
    - stop_hook_active true、空/缺失 last_assistant_message 或 text、重复 Stop/after、generation 错配、多 pending 且无 generation → skip 且 returncode 0（D-05）
    - background_tasks 非空仍 POST（OQ-02）
    - 答案不含 thinking 块；transcript_path 存在也不被打开；无 afterAgentThought 读取（D-07, SKILL-04）
    - stdout/stderr/cache 文件名不含 PAT；stdout 不含 prompt/answer/上游 body；成功消费 pending，失败保留有界 pending（D-08, T-145-01/02）
    - 缺 PAT/URL、记录缝模拟超时/非 2xx → returncode 0 且不消费 pending（SKILL-05）
    - Cursor beforeSubmitPrompt stdout 不含 additionalContext；Claude UPS stdout 不含 pending 路径/prompt/Bearer（D-01, T-145-03）
  </behavior>
  <action>
    在父仓新增 server/tests/hooks/。conftest：tmp_path 设为 XDG_CACHE_HOME；构造 dirty/clean/无仓库临时 git；提供 run_hook(script_path, event_dict, extra_env) 用 subprocess.run bash 真实 wrapper（cwd 为 git fixture），capture_output，check=False；设置 FRIDAY_CAPTURE_HTTP_RECORD 指向 tmp 记录文件，FRIDAY_BASE_URL 与 FRIDAY_ACCESS_TOKEN 为假值（断言不得出现在 stdout/stderr/cache 名）。脚本路径指向仓库 checkout 的 skills/hooks/user-prompt-submit、skills/hooks/stop、以及尚未存在的 skills/hooks/cursor/before-submit-prompt 与 after-agent-response（文件缺失时测试失败即可）。pytest 默认 --disable-socket：禁止真实 urlopen。每个行为写成独立 test 函数，注释引用 VALIDATION 任务 ID 145-01-01 至 145-05-03。不要实现 helper 或改 hook 生产脚本。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/hooks/test_session_capture_hooks.py --collect-only -q</automated>
  </verify>
  <done>测试可收集；对缺失 wrapper/helper 运行 pytest -x 呈 RED。本任务 verify 只要求 collect-only 成功。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 扩展 ide_hook_assets 与 skills snapshot 守卫 RED</name>
  <files>server/tests/initiatives/test_ide_hook_assets.py, server/tests/mcp_tools/test_skills_snapshot_guard.py</files>
  <behavior>
    - Cursor write bundle 含 beforeSubmitPrompt 与 afterAgentResponse；可保留 stop；无 afterAgentThought（D-03, SKILL-03）
    - 更新而非删除 test_write_path_cursor_stop_hook：仍可含 stop+report_project_knowledge；必须另含 afterAgentResponse；stop 脚本无 last_assistant_message、report_session_knowledge、hookSpecificOutput、additionalContext（OQ-01, T-145-04）
    - Claude 资产保留 UserPromptSubmit 与 Stop；Capture 相关脚本含 report_session_knowledge、短 timeout、exit 0；不以 diff --stat 为 Capture 门闩（D-01, D-02）
    - Codex 原断言零回归（deferred 宿主不做采集）
    - friday / friday-dev / friday-memory 主文与各自 http-fallback（若文件存在）同时出现 report_session_knowledge 与 report_project_knowledge，并写明 clean tree 仍收集、只取可见最终答案、不上传 transcript/CoT/凭证、两工具职责不合并（D-09, SKILL-04）
    - 既有工具 token ⊆ TOOL_SCHEMA_SNAPSHOT 守卫保留；12 键不在本任务修改
  </behavior>
  <action>
    扩展现有 pytest：正向/负向字符串与 json.loads 断言按 PATTERNS.md ide_hook_assets 段。snapshot 守卫沿用 documents + required_terms + missing 聚合，覆盖 friday、friday-dev、friday-memory 的 SKILL.md 以及 friday-dev/references/http-fallback.md 与 friday-memory/references/http-fallback.md。本任务不改生产 ide_hook_assets.py 或 skills 文档。父仓提交仅 server/tests 文件。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/initiatives/test_ide_hook_assets.py tests/mcp_tools/test_skills_snapshot_guard.py --collect-only -q</automated>
  </verify>
  <done>新断言已写入且可收集；对当前生产资产/文档运行这些测试呈 RED。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IDE hook stdin → local cache/POST | 不可信宿主 JSON 进入配对文件与 HTTP |
| installer → 用户 hooks.json | 合并不得摧毁用户安全 hook |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-145-01 | Information Disclosure | hook stdout/cache | mitigate | Wave 0 断言 stdout/stderr/cache 名无 PAT |
| T-145-02 | Information Disclosure | pending 路径 | mitigate | 断言 pending 在 XDG_CACHE_HOME 下非仓库 |
| T-145-03 | Tampering | Cursor/Claude stdout | mitigate | 禁止 additionalContext 进 Cursor before；Claude stdout 无 pending/Bearer |
| T-145-04 | Tampering | Cursor stop | mitigate | 资产测试禁止 stop 脚本含 session Capture |
| T-145-05 | Denial of Service | failClosed | mitigate | merge 测试禁止 failClosed true |
| T-145-09 | Information Disclosure | CoT/transcript | mitigate | 白名单字段与不打开 transcript_path |
| T-145-10 | Tampering | hooks.json | mitigate | 非法 JSON bytes 不变 |
| T-145-SC | Tampering | package installs | accept | 无新包；只加 node:test script |
</threat_model>

<verification>
cd skills && node --test lib/cursor-hooks-merge.test.mjs 允许失败。cd server && uv run pytest tests/hooks/test_session_capture_hooks.py tests/initiatives/test_ide_hook_assets.py tests/mcp_tools/test_skills_snapshot_guard.py --collect-only 必须成功。禁止 uv add / npm install。
</verification>

<success_criteria>
VALIDATION.md Wave 0 文件清单均存在；skills 子模块已有独立 commit（若本环境可提交子模块）；父仓测试提交不含把 skills 当普通文件加入。
</success_criteria>

<output>
Create `.planning/phases/145-cursor-claude-code/145-01-SUMMARY.md` when done
</output>
