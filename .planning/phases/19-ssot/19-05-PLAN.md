---
phase: 19-ssot
plan: 05
type: execute
wave: 3
depends_on: ["19-01", "19-03"]
files_modified:
  - web/src/components/workflow/editor/nodes/nodeVisuals.ts
  - web/src/components/workflow/sidebar/NodePalette.vue
  - web/src/components/workflow/config/FetchProjectInfoConfig.vue
  - web/src/components/workflow/editor/nodes/IntegrationNode.vue
  - web/src/components/__tests__/node-sync.test.ts
  - web/scripts/validate-node-definitions.ts
  - web/src/components/__tests__/validate-nodes.test.ts
autonomous: true
requirements: [SSOT-03, SSOT-01]
must_haves:
  truths:
    - "前端 palette/视觉/配置组件中 fetch_project_info 全量改为 fetch_space_info"
    - "node-sync.test.ts 改为 fixture 驱动：palette types ⊆ 后端 fixture、无幽灵节点、fetch_space_info 在"
    - "validate-node-definitions.ts 的 API URL 修正为 /api/node-types/（不含 workflows/node-types）"
    - "确认死代码 IntegrationNode.vue 无引用后删除（或核实存活则改名）"
  artifacts:
    - path: "web/src/components/__tests__/node-sync.test.ts"
      provides: "fixture 驱动的前后端漂移守护（删 EXPECTED_NODES）"
      contains: "node-types.fixture.json"
    - path: "web/scripts/validate-node-definitions.ts"
      provides: "修正后的 API URL"
      contains: "/api/node-types/"
  key_links:
    - from: "node-sync.test.ts"
      to: "web/src/types/workflow/__fixtures__/node-types.fixture.json"
      via: "import fixture 对账 palette types ⊆ fixture"
      pattern: "node-types.fixture"
    - from: "NodePalette.vue"
      to: "fetch_space_info"
      via: "幽灵节点 type 改名"
      pattern: "fetch_space_info"
---

<objective>
幽灵节点前端全量改名收尾（D-03）+ 死代码清理 + 一致性 CI 守护（SSOT-03、D-05）：把 `nodeVisuals.ts`/`NodePalette.vue`/配置组件中残留的 `fetch_project_info` 改为真实 `fetch_space_info`；核实并清理死代码 `IntegrationNode.vue`；把 `node-sync.test.ts` 从手维 `EXPECTED_NODES` 静态对账重写为 fixture 驱动对账（以 19-01 入库的后端 fixture 为基准）；修正 `validate-node-definitions.ts` 错误 API URL 并加严 `validate-nodes.test.ts`。

Purpose: 让"幽灵节点消失、真实节点出现"在前端展示层闭环，并以离线 fixture（CI 不起后端）守护前后端节点 type/端口漂移。
Output: 幽灵全量改名、死代码删除、fixture 驱动漂移守护测试、修正的校验脚本与断言。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/19-ssot/19-CONTEXT.md
@.planning/phases/19-ssot/19-RESEARCH.md
@.planning/phases/19-ssot/19-PATTERNS.md
@.planning/phases/19-ssot/19-01-PLAN.md
@.planning/phases/19-ssot/19-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 幽灵节点前端视觉/palette/配置组件改名 + 死代码 IntegrationNode 清理</name>
  <files>web/src/components/workflow/editor/nodes/nodeVisuals.ts, web/src/components/workflow/sidebar/NodePalette.vue, web/src/components/workflow/config/FetchProjectInfoConfig.vue, web/src/components/workflow/editor/nodes/IntegrationNode.vue</files>
  <read_first>
    - web/src/components/workflow/editor/nodes/nodeVisuals.ts L50（`fetch_project_info` 视觉项）——见 19-PATTERNS.md L314-329
    - web/src/components/workflow/sidebar/NodePalette.vue L46（palette `{type:'fetch_project_info'}`，"数据获取"组）——Pitfall 2：可继续放该展示组
    - web/src/components/workflow/config/FetchProjectInfoConfig.vue（配置组件，节点 key/类型对齐 fetch_space_info；文件物理改名为可选 D-03）
    - web/src/components/workflow/editor/nodes/IntegrationNode.vue L21（`fetch_project_info: FolderSearch`）——Open Question A2 死代码核实
    - 19-RESEARCH.md L301-317（grep 命中清单 + A2 死代码核实说明）
  </read_first>
  <action>
    把运行时 type 字符串 `fetch_project_info` 改为 `fetch_space_info`：`nodeVisuals.ts` L50 key（保留为前端视觉源）；`NodePalette.vue` L46 `type:'fetch_space_info'`（可继续放"数据获取"展示组，Pitfall 2）；`FetchProjectInfoConfig.vue` 内引用的节点 key/类型对齐 `fetch_space_info`（CONFIG_COMPONENTS 已在 19-03 以 `fetch_space_info` 为 key 映射该组件）。死代码核实：`rg -n "IntegrationNode" web/src`——若仅 `components.d.ts`（unplugin 自动生成）命中、`editor/nodes/index.ts` 未映射它（19-PATTERNS.md L328 已判定大概率死代码），则删除 `IntegrationNode.vue`；若发现真实 import 引用则改名其中幽灵 key 而不删。删除后让 `pnpm type-check` 触发 `components.d.ts` 重生成（勿手改）。
  </action>
  <verify>
    <automated>pnpm -C web type-check && (rg -q "fetch_project_info" web/src/components web/src/types && exit 1 || echo "no ghost in components/types")</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "fetch_project_info" web/src/components web/src/types` 无命中（视觉/palette/配置/类型均已改名）
    - `rg -n "fetch_space_info" web/src/components/workflow/sidebar/NodePalette.vue` 命中
    - IntegrationNode：删除后 `rg -n "IntegrationNode" web/src --glob '!**/components.d.ts'` 无命中；若保留则附核实理由到 SUMMARY
    - `pnpm -C web type-check` 退出码 0
  </acceptance_criteria>
  <done>前端展示层无 fetch_project_info；CONFIG_COMPONENTS 以 fetch_space_info 取得配置组件；死代码已核实并处理。</done>
</task>

<task type="auto">
  <name>Task 2: node-sync.test.ts 重写为 fixture 驱动 + 修正 validate-node-definitions URL + 加严 validate-nodes 断言</name>
  <files>web/src/components/__tests__/node-sync.test.ts, web/scripts/validate-node-definitions.ts, web/src/components/__tests__/validate-nodes.test.ts</files>
  <read_first>
    - web/src/components/__tests__/node-sync.test.ts L9-12（palette 正则提取范式）、L20（手维 `EXPECTED_NODES` 含幽灵——删除）——见 19-PATTERNS.md L333-346
    - web/src/types/workflow/__fixtures__/node-types.fixture.json（19-01 产物，对账基准）
    - web/scripts/validate-node-definitions.ts L77（`/api/workflows/node-types/` 错误 URL）——见 19-PATTERNS.md L350-358
    - web/src/components/__tests__/validate-nodes.test.ts L12-14（现仅断言含 'node-types' 子串）——见 19-RESEARCH.md L406
    - 19-RESEARCH.md L295-297（Pitfall 6 fixture 漂移）、L399-406（Req→Test Map）
  </read_first>
  <action>
    重写 `node-sync.test.ts`：删 `EXPECTED_NODES`，`import` fixture JSON（`__fixtures__/node-types.fixture.json`），复用既有 palette 正则（L9-12）提取 `NodePalette.vue` 的 type 集合；断言 ① palette types ⊆ fixture `nodes[].node_type` 全集；② 无幽灵（`fetch_project_info`/`code_implement`/`technical_plan` 均不在 palette）；③ `fetch_space_info` 在 fixture；④ parallel/join 多端口对 fixture 端口集断言（不再依赖 `getDefaultPortsForNodeType`）。失败信息提示"运行 `gen:node-fixture` 重生成"（Pitfall 6）。修正 `validate-node-definitions.ts` L77 URL → `/api/node-types/`。加严 `validate-nodes.test.ts`：在现有 `toContain('node-types')` 基础上增 `expect(content).not.toContain('workflows/node-types')`。
  </action>
  <verify>
    <automated>pnpm -C web test:unit -- src/components/__tests__/node-sync.test.ts src/components/__tests__/validate-nodes.test.ts --run</automated>
  </verify>
  <acceptance_criteria>
    - `node-sync.test.ts` 不含 `EXPECTED_NODES`，含 `import` fixture（`rg -n "node-types.fixture|EXPECTED_NODES" web/src/components/__tests__/node-sync.test.ts`：fixture 命中、EXPECTED_NODES 无命中）
    - node-sync + validate-nodes 测试全绿
    - `rg -n "workflows/node-types" web/scripts/validate-node-definitions.ts` 无命中；`rg -n "/api/node-types/" web/scripts/validate-node-definitions.ts` 命中
    - `validate-nodes.test.ts` 含 `not.toContain('workflows/node-types')`
  </acceptance_criteria>
  <done>漂移守护改 fixture 驱动并全绿；palette 无幽灵；校验脚本 URL 修正且断言加严。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI（离线 vitest）→ 入库 fixture | 漂移守护以版本库内 fixture 为基准对账，不依赖在线后端 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-07 | Tampering (假绿) | fixture 与后端漂移但未更新 | mitigate | 提供 `gen:node-fixture` 一键再生成 + 测试失败信息指向该命令（Pitfall 6）；理想终态前端无硬编码节点表，漂移面缩小 |
| T-19-08 | Information Disclosure | 节点元数据展示 | accept | palette/视觉为公开节点目录，无敏感字段 |
</threat_model>

<verification>
- `pnpm -C web test:unit -- src/components/__tests__/node-sync.test.ts src/components/__tests__/validate-nodes.test.ts --run` 绿
- `pnpm -C web type-check` 全绿
- Phase gate：`pnpm -C web lint && pnpm -C web type-check && pnpm -C web test:unit` 全绿 + 后端 `tests/workflows/` 全绿
</verification>

<success_criteria>
- 前端无 `fetch_project_info`，真实 `fetch_space_info` 在 palette/视觉/配置
- node-sync 漂移守护 fixture 驱动并通过；validate-node-definitions URL 修正
- 死代码 IntegrationNode 已核实并处理
</success_criteria>

## Artifacts this phase produces

- `web/src/components/workflow/editor/nodes/nodeVisuals.ts` — 幽灵视觉项改名
- `web/src/components/workflow/sidebar/NodePalette.vue` — palette 幽灵改名
- `web/src/components/workflow/config/FetchProjectInfoConfig.vue` — 节点 key 对齐 fetch_space_info
- `web/src/components/workflow/editor/nodes/IntegrationNode.vue` — 死代码删除（或核实保留）
- `web/src/components/__tests__/node-sync.test.ts` — fixture 驱动漂移守护
- `web/scripts/validate-node-definitions.ts` — API URL 修正
- `web/src/components/__tests__/validate-nodes.test.ts` — 加严断言

<output>
Create `.planning/phases/19-ssot/19-05-SUMMARY.md` when done
</output>
