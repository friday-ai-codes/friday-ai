---
phase: 19-ssot
plan: 03
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - web/src/stores/useNodeTypesStore.ts
  - web/src/types/workflow/registry.ts
  - web/src/types/workflow/schemas.ts
  - web/src/types/workflow/index.ts
  - web/src/components/workflow/editor/nodes/index.ts
  - web/src/components/workflow/WorkflowMiniMap.vue
  - web/src/components/workflow/WorkflowDataTable.vue
  - web/src/composables/useNodeMeta.ts
  - web/src/components/workflow/editor/composables/useDragAndDrop.ts
  - web/src/components/workflow/editor/edges/GradientEdge.vue
  - web/src/components/workflow/CreateWorkflowModal.vue
  - web/src/types/workflow/__tests__/registry.test.ts
autonomous: true
requirements: [SSOT-01]
must_haves:
  truths:
    - "registry.ts 的 getNodeDefinition/getDefaultConfig/getNodesByCategory/validateNodeConfig 从 useNodeTypesStore 取值，不再读 NODE_REGISTRY"
    - "NODE_REGISTRY legacy 硬编码区块（含幽灵 fetch_project_info）被删除"
    - "configComponent 懒加载映射抽为独立 CONFIG_COMPONENTS（前端专属、保留）"
    - "所有 NODE_REGISTRY 值/NodeTypeKey 类型消费方改读 helper/store，pnpm type-check 通过"
    - "validateNodeConfig 降级为基于 config_schema.required/type 的轻量校验（完整校验交 Phase 20）"
  artifacts:
    - path: "web/src/stores/useNodeTypesStore.ts"
      provides: "NodeType 接口扩 ui_schema/default_config/execution_mode"
      contains: "default_config"
    - path: "web/src/types/workflow/registry.ts"
      provides: "store 适配器 helper + CONFIG_COMPONENTS 映射；删 NODE_REGISTRY legacy"
      contains: "CONFIG_COMPONENTS"
    - path: "web/src/types/workflow/__tests__/registry.test.ts"
      provides: "store 适配器单测（mock store → helper 取值）"
      contains: "getNodeDefinition"
  key_links:
    - from: "registry.ts helpers"
      to: "useNodeTypesStore"
      via: "toDefinition(nt) 适配器 snake→camel"
      pattern: "useNodeTypesStore"
    - from: "editor/nodes/index.ts / WorkflowMiniMap / WorkflowDataTable"
      to: "registry helper"
      via: "getNodeDefinition 替代 NODE_REGISTRY[type]"
      pattern: "getNodeDefinition"
---

<objective>
前端收敛核心：把 `registry.ts` 的对外 helper 改为从 `useNodeTypesStore`（唯一运行时源）读取，删除 `NODE_REGISTRY` legacy 硬编码区块（D-02），抽出前端专属 `CONFIG_COMPONENTS` 懒加载映射（保留，API 不可下发），并把所有直接消费 `NODE_REGISTRY` 值与 `NodeTypeKey` 类型的文件收敛到 helper/store——`pnpm type-check` 为硬门禁。

Purpose: 消除三套硬编码源中的主源（NODE_REGISTRY），让前端 palette/默认 config/显示名全部由后端 `GET /api/node-types/` 驱动（SSOT-01）。
Output: store 接口扩字段、store 适配器 helper、删 legacy、CONFIG_COMPONENTS 独立映射、消费方收敛、store 适配器单测。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/19-ssot/19-CONTEXT.md
@.planning/phases/19-ssot/19-RESEARCH.md
@.planning/phases/19-ssot/19-PATTERNS.md
@.planning/phases/19-ssot/19-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: store 接口扩字段 + registry helper 改 store 适配器 + CONFIG_COMPONENTS 抽出 + validateNodeConfig 降级</name>
  <files>web/src/stores/useNodeTypesStore.ts, web/src/types/workflow/registry.ts, web/src/types/workflow/schemas.ts, web/src/types/workflow/__tests__/registry.test.ts</files>
  <read_first>
    - web/src/stores/useNodeTypesStore.ts L22-33（`NodeType` 接口）、L60-62（`getNodeType` getter）、L64-77（`fetchNodeTypes`，`data.results || data`）——见 19-PATTERNS.md L137-158
    - web/src/types/workflow/registry.ts L80-276（`NODE_REGISTRY` legacy，幽灵 L182-192）、L284-350（`NodeTypeKey`/helper）——见 19-PATTERNS.md L162-212
    - web/src/types/workflow/schemas.ts L341/L363/L386（`FetchProjectInfoConfig` 类型 + 联合 + `NODE_CONFIG_SCHEMAS` key）
    - web/src/components/workflow/editor/nodes/nodeVisuals.ts L94-99（`getNodeVisual()`/`allNodeTypeKeys`，color 来源）
    - 19-RESEARCH.md L192-211（Pattern 2 适配器）、L289-293（Pitfall 5 validateNodeConfig 降级）
  </read_first>
  <behavior>
    - getNodeDefinition(type) 在 store 有该 type 时返回适配后的 NodeTypeDefinition（camelCase），无则 undefined
    - getDefaultConfig(type) 返回 store 节点的 default_config（来自后端，免 zod.parse({})）
    - getNodesByCategory(cat) 从 store nodeTypesByCategory 派生
    - validateNodeConfig(type, cfg) 仅校验 config_schema.required 字段存在 + 顶层类型（轻量），不再依赖整套 zod schema
    - CONFIG_COMPONENTS[type] 返回该节点的懒加载配置组件（前端专属映射）
  </behavior>
  <action>
    在 `useNodeTypesStore.ts` 的 `NodeType` 接口增 `ui_schema?: Record<string, any> | null`、`default_config?: Record<string, any>`、`execution_mode?: string`（`getNodeType` getter 不变）。在 `registry.ts`：① 新增 `toDefinition(nt: NodeType): NodeTypeDefinition` 适配器（`node_type→nodeType` 等 snake→camel；`color` 取 `getNodeVisual(nt.node_type).color`；`defaultConfig: nt.default_config ?? {}`；`uiSchema: nt.ui_schema ?? undefined`；`configComponent: CONFIG_COMPONENTS[nt.node_type]`，见 19-PATTERNS.md L196-209）；② 把散落在 legacy 各项的 `configComponent:` 抽成独立 `export const CONFIG_COMPONENTS: Record<string, () => Promise<...>>`；③ `getNodeDefinition/getDefaultConfig/getNodesByCategory` 改经 `useNodeTypesStore()` + `toDefinition`（保持对外签名）；④ `validateNodeConfig` 降级为基于 `config_schema.required`/`properties.*.type` 的轻量检查（Pitfall 5，避免引入 ajv）；⑤ schemas.ts 幽灵改名 `FetchProjectInfoConfig→FetchSpaceInfoConfig` + `NODE_CONFIG_SCHEMAS` key `fetch_space_info`，registry.ts 对应 import 同步改名。⑥ **暂保留** `NODE_REGISTRY` 与 `NodeTypeKey`（本任务不删，供 Task 2 收敛后再删），但 helper 不再引用它。新建 `web/src/types/workflow/__tests__/registry.test.ts`：mock/`setNodeTypes` 注入假 store 数据，断言 `getNodeDefinition/getDefaultConfig/getNodesByCategory` 从 store 取值（analog: node-sync.test.ts + @vue/test-utils，见 19-PATTERNS.md L393-397）。
  </action>
  <verify>
    <automated>pnpm -C web test:unit -- src/types/workflow/__tests__/registry.test.ts --run</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "useNodeTypesStore" web/src/types/workflow/registry.ts` 命中（helper 已接 store）
    - `rg -n "CONFIG_COMPONENTS" web/src/types/workflow/registry.ts` 命中（独立映射存在）
    - registry.test.ts 通过：mock store 注入后 `getNodeDefinition('ai_coding')?.displayName` 等于注入值
    - `rg -n "ui_schema|default_config|execution_mode" web/src/stores/useNodeTypesStore.ts` 命中三者
  </acceptance_criteria>
  <done>helper 由 store 驱动；CONFIG_COMPONENTS 抽出；validateNodeConfig 降级；store 适配器单测绿；NODE_REGISTRY 暂存待删。</done>
</task>

<task type="auto">
  <name>Task 2: 收敛所有 NODE_REGISTRY/NodeTypeKey 消费方 + 删除 NODE_REGISTRY legacy + barrel</name>
  <files>web/src/types/workflow/registry.ts, web/src/types/workflow/index.ts, web/src/components/workflow/editor/nodes/index.ts, web/src/components/workflow/WorkflowMiniMap.vue, web/src/components/workflow/WorkflowDataTable.vue, web/src/composables/useNodeMeta.ts, web/src/components/workflow/editor/composables/useDragAndDrop.ts, web/src/components/workflow/editor/edges/GradientEdge.vue, web/src/components/workflow/CreateWorkflowModal.vue</files>
  <read_first>
    - web/src/components/workflow/WorkflowMiniMap.vue L3,L40（`NODE_REGISTRY[nodeType]`）
    - web/src/components/workflow/WorkflowDataTable.vue L13,L67（`NODE_REGISTRY[type]`）
    - web/src/components/workflow/editor/nodes/index.ts L2,L10,L27-45（`NodeTypeKey` + `Object.keys(NODE_REGISTRY)` + `allNodeTypeKeys` 合并，已半收敛，见 19-PATTERNS.md L287-310）
    - web/src/composables/useNodeMeta.ts L1,L31-66（`NodeTypeKey` + 完全转发 registry helper）
    - web/src/components/workflow/editor/composables/useDragAndDrop.ts、web/src/components/workflow/editor/edges/GradientEdge.vue、web/src/components/workflow/CreateWorkflowModal.vue（grep NODE_REGISTRY/NodeTypeKey 命中点）
    - web/src/types/workflow/index.ts L17-25/L49（barrel re-export `NODE_REGISTRY`/`NodeTypeKey`）
    - 19-RESEARCH.md L277-281（Pitfall 3 消费方核销）、19-PATTERNS.md L414-418（消费方核销清单）
  </read_first>
  <action>
    全仓 grep-sweep `NODE_REGISTRY` 与 `NodeTypeKey` 逐一核销：① `WorkflowMiniMap.vue`/`WorkflowDataTable.vue` 把 `NODE_REGISTRY[type]` 改 `getNodeDefinition(type)`（显示名 → `.displayName`）；② `editor/nodes/index.ts` 删 `NODE_REGISTRY` import，类型映射改纯从 `allNodeTypeKeys`(nodeVisuals) + `specialNodes` 生成，保留 Proxy fallback→baseNode；③ `useNodeMeta.ts`/`useDragAndDrop.ts`/`GradientEdge.vue`/`CreateWorkflowModal.vue` 中 `NodeTypeKey` 类型改 `string`（或从 store/nodeVisuals keys 派生），值读 helper；④ 在 `registry.ts` 把 `NodeTypeKey` 重定义为 `string`（不再 `keyof typeof NODE_REGISTRY`），随后**删除 `NODE_REGISTRY` legacy 区块 L80-276**；⑤ `types/workflow/index.ts` barrel 移除/修正 `NODE_REGISTRY` 导出，`NodeTypeKey` 仍可导出（现为 string 别名）。`pnpm type-check` 必须通过（硬门禁）。
  </action>
  <verify>
    <automated>pnpm -C web type-check && (rg -q "NODE_REGISTRY" web/src && exit 1 || echo "NODE_REGISTRY removed")</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "NODE_REGISTRY" web/src` 无业务代码命中（注释/node-definitions 说明性文字除外，legacy 区块已删）
    - `pnpm -C web type-check` 退出码 0（NodeTypeKey 重定义后无类型断裂）
    - `WorkflowMiniMap.vue`/`WorkflowDataTable.vue`/`editor/nodes/index.ts` 不再 import `NODE_REGISTRY`
  </acceptance_criteria>
  <done>NODE_REGISTRY legacy 删除；所有消费方收敛到 helper/store；type-check 通过。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 前端运行时 → store 缓存 | 节点定义由后端 /api/node-types/ 经 store 提供；前端不再持有并行硬编码定义 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-04 | Tampering | validateNodeConfig 客户端校验降级 | accept | 客户端校验非安全边界；保存期权威校验由 Phase 20 后端 `WorkflowGraphValidator` 承接（VAL-01/02）；本阶段不放松后端既有校验 |
| T-19-05 | Denial of Service (UX) | store 未就绪空数据 | mitigate | helper/消费方经响应式读取；空 store 返回 undefined/空集而非抛错（Pitfall 1） |
</threat_model>

<verification>
- `pnpm -C web test:unit -- src/types/workflow/__tests__/registry.test.ts --run` 绿
- `pnpm -C web type-check` 全绿（硬门禁，Pitfall 3）
- wave 合并：`pnpm -C web test:unit` full suite 绿
</verification>

<success_criteria>
- `NODE_REGISTRY` legacy 删除，前端 palette/默认 config/显示名经 store
- `CONFIG_COMPONENTS`/`nodeVisuals` 等前端专属映射保留
- type-check 通过，无遗漏消费方
</success_criteria>

## Artifacts this phase produces

- `web/src/stores/useNodeTypesStore.ts` — `NodeType` 扩 `ui_schema`/`default_config`/`execution_mode`
- `web/src/types/workflow/registry.ts` — store 适配器 helper + `CONFIG_COMPONENTS`，删 `NODE_REGISTRY` legacy，`validateNodeConfig` 降级
- `web/src/types/workflow/schemas.ts` — 幽灵类型/键改名
- `web/src/types/workflow/index.ts` — barrel 修正
- 消费方收敛：`editor/nodes/index.ts`、`WorkflowMiniMap.vue`、`WorkflowDataTable.vue`、`useNodeMeta.ts`、`useDragAndDrop.ts`、`GradientEdge.vue`、`CreateWorkflowModal.vue`
- `web/src/types/workflow/__tests__/registry.test.ts` — store 适配器单测

<output>
Create `.planning/phases/19-ssot/19-03-SUMMARY.md` when done
</output>
