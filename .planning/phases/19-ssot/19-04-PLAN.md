---
phase: 19-ssot
plan: 04
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - web/src/components/workflow/editor/nodes/BaseWorkflowNode.vue
  - web/src/components/workflow/editor/utils/portConfig.ts
  - web/src/pages/workflows/[id].vue
  - web/src/stores/useWorkflowsStore.ts
  - web/src/components/workflow/editor/nodes/__tests__/BaseWorkflowNode.test.ts
autonomous: true
requirements: [SSOT-02]
must_haves:
  truths:
    - "BaseWorkflowNode 的 Handle 由后端 NodePort（store inputs/outputs）渲染"
    - "ai_coding 显示 plan 输入、ai_code_review 显示 coding_result 输入、审批节点显示 approved/rejected 输出"
    - "store 未就绪（首帧/离线）时回退最小端口（单 in/单 out default），就绪后自动重渲染"
    - "migratePortId 保留（兼容存量 edge），不删"
    - "hasTriggers 等节点集合判定由后端 category 派生，不再用硬编码 TRIGGER_NODE_TYPES"
  artifacts:
    - path: "web/src/components/workflow/editor/nodes/BaseWorkflowNode.vue"
      provides: "ports computed 从 store inputs/outputs 渲染 + 最小回退"
      contains: "useNodeTypesStore"
    - path: "web/src/components/workflow/editor/nodes/__tests__/BaseWorkflowNode.test.ts"
      provides: "Handle 渲染测试：空 store 回退 + 就绪后真实端口"
      contains: "approved"
  key_links:
    - from: "BaseWorkflowNode.vue ports computed"
      to: "useNodeTypesStore.getNodeType(type).inputs/outputs"
      via: "computed 响应式读取，store 就绪自动重渲染"
      pattern: "getNodeType"
    - from: "pages/workflows/[id].vue"
      to: "store.getNodeType(type).category"
      via: "hasTriggers 由 category 派生 + await fetchNodeTypes 先于 fetchWorkflow"
      pattern: "category"
---

<objective>
画布端口收敛（SSOT-02）：`BaseWorkflowNode.vue` 的 Handle 改由 `useNodeTypesStore` 的 `inputs/outputs`（后端 NodePort）渲染，store 未就绪时最小回退（D-04）；`portConfig.ts` 的硬编码端口推断退出渲染路径，仅保留 `migratePortId` 作存量 edge 兼容；硬编码节点类型集合（`TRIGGER_NODE_TYPES` 等）由后端 `category` 派生。修复 Pitfall 4（migratePortId 在 store 未就绪退化破坏存量 edge）。

Purpose: 让画布输入/输出 Handle 与 Phase 18 引擎 target_handle 语义一致、以后端 NodePort 为准。
Output: store 驱动的 Handle 渲染 + 最小回退、portConfig 降级、[id].vue 取数顺序化、Handle 渲染组件测试。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/19-ssot/19-CONTEXT.md
@.planning/phases/19-ssot/19-RESEARCH.md
@.planning/phases/19-ssot/19-PATTERNS.md
@.planning/phases/19-ssot/19-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: BaseWorkflowNode Handle 由 store 端口渲染 + 最小回退 + 组件测试</name>
  <files>web/src/components/workflow/editor/nodes/BaseWorkflowNode.vue, web/src/components/workflow/editor/nodes/__tests__/BaseWorkflowNode.test.ts</files>
  <read_first>
    - web/src/components/workflow/editor/nodes/BaseWorkflowNode.vue L18,L41-46（`ports = getDefaultPortsForNodeType(...)`、`inputPorts`/`outputPorts`）、L145-153/L176-184（Handle v-for + portLeft）——见 19-PATTERNS.md L252-283
    - web/src/stores/useNodeTypesStore.ts L60-62（`getNodeType` getter）
    - 19-RESEARCH.md L213-227（Pattern 3）、L265-269（Pitfall 1 store 异步未就绪）
  </read_first>
  <behavior>
    - 空 store（getNodeType 返回 undefined）→ ports 回退 [{id:'default',group:'input'},{id:'default',group:'output'}]
    - store 注入 ai_coding（inputs 含 plan）→ inputPorts 含 id 'plan'
    - store 注入审批节点（outputs 含 approved/rejected）→ outputPorts 含 'approved' 与 'rejected'
    - ports 为 computed，依赖 store ref → setNodeTypes 后自动更新（无需重新挂载）
  </behavior>
  <action>
    在 `BaseWorkflowNode.vue` 引入 `useNodeTypesStore`；把 `ports` computed 改为读 `store.getNodeType(props.data.nodeType)`，无则返回最小回退端口，有则 `[...nt.inputs.map(p=>({id:p.name,group:'input'})), ...nt.outputs.map(p=>({id:p.name,group:'output'}))]`（见 19-PATTERNS.md L269-281）。`inputPorts`/`outputPorts`/`portLeft` 与模板 Handle v-for 全部保留不改。新建 `web/src/components/workflow/editor/nodes/__tests__/BaseWorkflowNode.test.ts`（@vue/test-utils + happy-dom）：用例①空 store 断言回退端口；用例② `setNodeTypes` 注入 ai_coding/审批节点后断言 `plan` 输入与 `approved`/`rejected` 输出出现（Pitfall 1 验证）。
  </action>
  <verify>
    <automated>pnpm -C web test:unit -- src/components/workflow/editor/nodes/__tests__/BaseWorkflowNode.test.ts --run</automated>
  </verify>
  <acceptance_criteria>
    - BaseWorkflowNode.test.ts 两用例通过（空 store 回退 + 就绪后真实端口）
    - `rg -n "useNodeTypesStore|getNodeType" web/src/components/workflow/editor/nodes/BaseWorkflowNode.vue` 命中
    - 测试显式断言审批节点 outputs 含 `approved` 与 `rejected`
  </acceptance_criteria>
  <done>Handle 由后端端口渲染，空 store 有回退，就绪后自动重渲染；组件测试绿。</done>
</task>

<task type="auto">
  <name>Task 2: portConfig 降级保留 migratePortId + [id].vue 顺序化 + 派生节点集合</name>
  <files>web/src/components/workflow/editor/utils/portConfig.ts, web/src/pages/workflows/[id].vue, web/src/stores/useWorkflowsStore.ts</files>
  <read_first>
    - web/src/components/workflow/editor/utils/portConfig.ts L46-122（`getDefaultPortsForNodeType` + `migratePortId` L100-122 + `TRIGGER/APPROVAL/ERROR_OUTPUT_NODE_TYPES`）——见 19-PATTERNS.md L216-248
    - web/src/pages/workflows/[id].vue L40-74（`hasTriggers` L53-55 用 `TRIGGER_NODE_TYPES.includes`；`onMounted` L57-62 `Promise.all([fetchNodeTypes, fetchWorkflow])` 并行）——见 19-RESEARCH.md L230-231,L283-288
    - web/src/stores/useWorkflowsStore.ts L130-209（`toStoreEdges` L148-160 调 `migratePortId`）
    - 19-RESEARCH.md L283-288（Pitfall 4 顺序化方案）
  </read_first>
  <action>
    `portConfig.ts`：**保留** `migratePortId`（正则 `^(input|output)-\d+$` 守卫，新句柄透传——D-02 禁删）；`getDefaultPortsForNodeType` 退出正常渲染路径（仅供 `migratePortId` 端口顺序回退；如其余渲染引用已移除则降级为内部回退表）。`[id].vue`：`hasTriggers` 改 `nodes.some(n => store.getNodeType(n.type)?.category === 'trigger')`（移除 `TRIGGER_NODE_TYPES` 依赖）；按 Pitfall 4 把 `onMounted` 改为 `await fetchNodeTypes()` 先于 `fetchWorkflow()`（保证 `toStoreEdges`→`migratePortId` 时端口已就绪，避免存量 edge 退化 default）。`useWorkflowsStore.toStoreEdges` 保持调用 `migratePortId`，确认其在顺序化后拿到就绪端口。`APPROVAL/ERROR_OUTPUT_NODE_TYPES` 渲染路径不再需要时移除其使用点。
  </action>
  <verify>
    <automated>pnpm -C web type-check && pnpm -C web test:unit -- src/components/workflow/editor --run</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "export function migratePortId" web/src/components/workflow/editor/utils/portConfig.ts` 命中（migratePortId 保留）
    - `rg -n "TRIGGER_NODE_TYPES" web/src/pages/workflows/[id].vue` 无命中（已改 category 派生）
    - `[id].vue` 中 `fetchNodeTypes` 在 `fetchWorkflow` 之前 await（顺序化，Pitfall 4）
    - `pnpm -C web type-check` 退出码 0
  </acceptance_criteria>
  <done>portConfig 渲染推断退出主路径、migratePortId 保留；[id].vue 顺序化取数 + category 派生；存量 edge 不退化。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 画布渲染 → store 端口数据 | Handle 渲染消费后端 NodePort；纯展示层，无写路径 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-06 | Denial of Service (UX) | store 未就绪首帧空 Handle / 存量 edge 退化 | mitigate | computed 响应式 + 最小回退（Pitfall 1）；[id].vue 顺序化 fetchNodeTypes 先行（Pitfall 4）；migratePortId 正则守卫仅作用于 legacy 句柄 |
</threat_model>

<verification>
- `pnpm -C web test:unit -- src/components/workflow/editor/nodes/__tests__/BaseWorkflowNode.test.ts --run` 绿
- `pnpm -C web type-check` 全绿
- 手工（19-VALIDATION Manual-Only）：编辑器拖入 ai_coding/审批节点确认 Handle 正确、打开老工作流连线不退化
</verification>

<success_criteria>
- 画布 Handle 以后端 NodePort 为准（plan/coding_result/approved/rejected）
- store 未就绪有回退，就绪后自动重渲染
- migratePortId 保留，存量 edge 兼容
</success_criteria>

## Artifacts this phase produces

- `web/src/components/workflow/editor/nodes/BaseWorkflowNode.vue` — Handle 由 store 端口渲染 + 最小回退
- `web/src/components/workflow/editor/utils/portConfig.ts` — getDefaultPortsForNodeType 降级、migratePortId 保留
- `web/src/pages/workflows/[id].vue` — fetchNodeTypes 顺序化 + hasTriggers 由 category 派生
- `web/src/stores/useWorkflowsStore.ts` — toStoreEdges/migratePortId 顺序化兼容
- `web/src/components/workflow/editor/nodes/__tests__/BaseWorkflowNode.test.ts` — Handle 渲染测试

<output>
Create `.planning/phases/19-ssot/19-04-SUMMARY.md` when done
</output>
