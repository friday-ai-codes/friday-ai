---
phase: 19-ssot
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - server/workflows/migrations/0026_rename_fetch_project_info_nodes.py
autonomous: true
requirements: [SSOT-01]
must_haves:
  truths:
    - "存量 WorkflowNode.node_type == 'fetch_project_info' 的行被幂等重写为 'fetch_space_info'"
    - "迁移可重入（再次运行无副作用），reverse 为 noop"
    - "edge 句柄不受影响（仅改 node_type 字符串）"
  artifacts:
    - path: "server/workflows/migrations/0026_rename_fetch_project_info_nodes.py"
      provides: "幂等数据迁移：fetch_project_info → fetch_space_info"
      contains: "fetch_project_info"
  key_links:
    - from: "0026 migration RunPython"
      to: "WorkflowNode.node_type"
      via: "filter(node_type='fetch_project_info').update(node_type='fetch_space_info')"
      pattern: "fetch_space_info"
---

<objective>
处理 RESEARCH Open Question A1：存量 DB 中可能存在用户曾保存的幽灵节点行 `WorkflowNode.node_type == 'fetch_project_info'`（后端 registry 从无此 type，执行即未知节点）。新增一条幂等数据迁移，把存量行重写为真实节点 `fetch_space_info`，使前端收敛后老工作流仍能正确解析该节点（D-03）。

Purpose: 让"幽灵节点改名"在数据态闭环，避免老工作流打开后该节点退化为 fallback baseNode。
Output: 幂等的 Django 数据迁移 `0026`。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/19-ssot/19-CONTEXT.md
@.planning/phases/19-ssot/19-RESEARCH.md
@server/workflows/migrations/0010_rename_node_types.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 核查存量 + 幂等数据迁移 0026</name>
  <files>server/workflows/migrations/0026_rename_fetch_project_info_nodes.py</files>
  <read_first>
    - server/workflows/migrations/0010_rename_node_types.py（全文：`RENAME_MAP` + `RunPython(..., RunPython.noop)` 范式——本任务直接 analog）
    - server/workflows/migrations/0011_migrate_technical_plan_to_plan_generation.py（数据迁移幂等写法参考）
    - 19-RESEARCH.md L249-261 / L356-361（Runtime State Inventory 与 Open Question A1：核查 count、>0 则幂等迁移）
  </read_first>
  <action>
    先核查存量：`cd server && uv run python manage.py shell -c "from workflows.models import WorkflowNode; print(WorkflowNode.objects.filter(node_type='fetch_project_info').count())"`（记录数量到 SUMMARY）。新建 `server/workflows/migrations/0026_rename_fetch_project_info_nodes.py`：仿 `0010_rename_node_types.py`，`dependencies` 指向当前最新迁移 `0025_alert_rules`，`RunPython(forward, migrations.RunPython.noop)`，forward 内 `apps.get_model("workflows","WorkflowNode").objects.filter(node_type="fetch_project_info").update(node_type="fetch_space_info")`。幂等：filter+update 对 0 行无副作用、可重入。中文注释说明这是幽灵节点对齐（D-03）。
  </action>
  <verify>
    <automated>cd server && uv run python manage.py makemigrations workflows --check --dry-run; uv run python manage.py migrate workflows --plan | rg "0026_rename_fetch_project_info_nodes"</automated>
  </verify>
  <acceptance_criteria>
    - `uv run python manage.py migrate workflows` 应用 0026 成功；再次 `migrate` 无变更（幂等）
    - 迁移后 `WorkflowNode.objects.filter(node_type='fetch_project_info').count() == 0`
    - `rg -n "fetch_project_info|fetch_space_info|RunPython.noop" server/workflows/migrations/0026_rename_fetch_project_info_nodes.py` 命中三者
    - `makemigrations --check` 无新增待生成迁移（schema 未漂移）
  </acceptance_criteria>
  <done>0026 幂等数据迁移落地并可应用；存量幽灵节点行清零；reverse 为 noop。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → DB | 受控的服务端数据迁移，仅改 node_type 字符串字段，无外部输入 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-03 | Tampering / Data Integrity | WorkflowNode.node_type 批量更新 | mitigate | 仅 update 命中 `fetch_project_info` 的行为目标真实 type；不触碰 edge/handle/config；reverse=noop 避免误回滚破坏；幂等可重入 |
</threat_model>

<verification>
- `uv run python manage.py migrate workflows` 应用成功且幂等
- `makemigrations --check` 无待生成迁移
</verification>

<success_criteria>
- 存量 `fetch_project_info` 节点行被重写为 `fetch_space_info`，无残留
- 迁移幂等、reverse noop、不影响 edge 句柄
</success_criteria>

## Artifacts this phase produces

- `server/workflows/migrations/0026_rename_fetch_project_info_nodes.py` — 幂等幽灵节点数据迁移

<output>
Create `.planning/phases/19-ssot/19-02-SUMMARY.md` when done
</output>
