---
phase: 06-pat
plan: 02
type: execute
wave: 1
depends_on: ["06-01"]
files_modified:
  - server/access_tokens/models.py
  - server/access_tokens/migrations/0002_accesstoken_note_token_suffix.py
  - server/access_tokens/serializers.py
  - server/access_tokens/views.py
autonomous: true
requirements: [PAT-01, PAT-02, PAT-03, PAT-04, PAT-06]
must_haves:
  truths:
    - "AccessToken model has note + token_suffix columns, both with default='' (no data migration, historical rows get empty strings)"
    - "Creating a token persists note and token_suffix = plaintext[-4:] computed server-side in acreate"
    - "AccessTokenSerializer exposes note + token_suffix read-only and never plaintext/token_hash"
    - "owner isolation and soft-revoke behavior remain unchanged"
  artifacts:
    - path: "server/access_tokens/models.py"
      provides: "note + token_suffix fields"
      contains: "token_suffix = models.CharField"
    - path: "server/access_tokens/migrations/0002_accesstoken_note_token_suffix.py"
      provides: "AddField migration for note + token_suffix"
      contains: "AddField"
    - path: "server/access_tokens/serializers.py"
      provides: "read-only note + token_suffix output; create accepts optional note"
      contains: "token_suffix"
    - path: "server/access_tokens/views.py"
      provides: "acreate persists note + token_suffix=plaintext[-4:]"
      contains: "plaintext[-4:]"
  key_links:
    - from: "server/access_tokens/views.py"
      to: "AccessToken.objects.acreate"
      via: "token_suffix=plaintext[-4:], note=data.get('note','')"
      pattern: "plaintext\\[-4:\\]"
    - from: "server/access_tokens/serializers.py"
      to: "AccessToken model fields"
      via: "Meta.fields includes note + token_suffix, read_only_fields = fields"
      pattern: "read_only_fields = fields"
---

<objective>
Extend the existing `access_tokens` backend (model + migration + serializer + view) to add an optional `note` and a `token_suffix` fingerprint, turning the Wave-0 backend assertions (06-01) GREEN. This is a surgical, additive change — it does NOT touch authentication, hashing (unsalted sha256 via `runners.models.hash_token` stays contract-locked), owner isolation, or soft-revoke.

Purpose: Deliver the persistence + API half of PAT-01 (note on create), PAT-03 (suffix fingerprint + note in list output), while preserving PAT-02/04/06 (plaintext-once, revoke, owner isolation) by regression.
Output: Two new model columns, an additive `AddField` migration, serializer + create-serializer field additions, and `acreate` writing both new values.
</objective>

<execution_context>
@.cursor/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-pat/06-CONTEXT.md
@.planning/phases/06-pat/06-RESEARCH.md
@server/access_tokens/models.py
@server/access_tokens/serializers.py
@server/access_tokens/views.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add note + token_suffix model fields and generate AddField migration</name>
  <files>server/access_tokens/models.py, server/access_tokens/migrations/0002_accesstoken_note_token_suffix.py</files>
  <read_first>
    - server/access_tokens/models.py (existing `token_prefix = models.CharField(max_length=20, default="")` — mirror this exact pattern + Chinese comment style)
    - server/access_tokens/migrations/0001_initial.py (to confirm dependency name `("access_tokens", "0001_initial")` and CharField/default conventions)
  </read_first>
  <behavior>
    - `AccessToken` instance has attribute `note` defaulting to `""` and `token_suffix` defaulting to `""`.
    - Existing/historical rows load with empty `note`/`token_suffix` (no migration prompt, no data migration).
  </behavior>
  <action>
    In `models.py`, add two fields on `AccessToken`, mirroring the existing `token_prefix` declaration and Chinese comment convention:
    - `note = models.CharField(max_length=500, blank=True, default="")` (可选备注；空串=历史 token 无备注).
    - `token_suffix = models.CharField(max_length=8, default="")` (明文后 4 字符，与 token_prefix 对称形成 friday_pat_xxx…abcd 指纹，非敏感). Keep `max_length=8` (CONTEXT-locked headroom) but only ever store 4 chars.
    Both MUST carry `default=""` — do NOT omit it (a NOT NULL column without default forces an interactive one-off default / migration failure on SQLite/PG). Do NOT add a data migration / RunPython.
    Then generate the migration with `cd server && uv run python manage.py makemigrations access_tokens`. Confirm the generated operation set is exactly two `migrations.AddField` ops with `dependencies = [("access_tokens", "0001_initial")]`. If makemigrations names the file differently, rename `files_modified` is not required — keep the generated name; the important contract is a single new `0002_*` migration containing both AddField ops.
  </action>
  <verify>
    <automated>cd server && uv run python manage.py makemigrations access_tokens --check --dry-run</automated>
  </verify>
  <acceptance_criteria>
    - `models.py` contains `note = models.CharField(max_length=500, blank=True, default="")` and `token_suffix = models.CharField(max_length=8, default="")`.
    - A new `0002_*` migration exists under `server/access_tokens/migrations/` with two `AddField` operations (note, token_suffix) and dependency on `0001_initial`.
    - `makemigrations --check --dry-run` exits 0 (no further pending migrations after the file is committed).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Serializer + acreate persist note and token_suffix</name>
  <files>server/access_tokens/serializers.py, server/access_tokens/views.py</files>
  <read_first>
    - server/access_tokens/serializers.py (`AccessTokenSerializer.Meta.fields` + `read_only_fields = fields`; `AccessTokenCreateSerializer` fields)
    - server/access_tokens/views.py (`acreate` — current `token_prefix=plaintext[:12]` write; `data["name"]` access)
    - server/tests/test_access_tokens.py (the 06-01 assertions this task must satisfy: token_suffix, note, serializer read-only)
  </read_first>
  <behavior>
    - POST create with a `note` persists it; without `note` defaults to `""` (no KeyError).
    - `token_suffix` equals `plaintext[-4:]`, computed in `acreate` from the server-side plaintext only.
    - `AccessTokenSerializer(token).data` includes `note` + `token_suffix`, excludes `token_hash` and plaintext `token`.
  </behavior>
  <action>
    In `serializers.py`:
    - Add `"note"` and `"token_suffix"` to `AccessTokenSerializer.Meta.fields` (keep `read_only_fields = fields` unchanged — output stays fully read-only; this is the safety contract).
    - On `AccessTokenCreateSerializer`, add `note = serializers.CharField(max_length=500, required=False, allow_blank=True, default="")` so `validated_data` always contains `note` (avoids the KeyError pitfall). Do NOT add `token_suffix` as a create input — it is derived server-side.
    In `views.py` `acreate`: pass `note=data["note"]` (or `data.get("note", "")`) and `token_suffix=plaintext[-4:]` to `AccessToken.objects.acreate(...)`, alongside the existing `token_prefix=plaintext[:12]`. Compute `token_suffix` from the in-memory `plaintext` only — NEVER return plaintext to the client and re-derive the suffix; the one-time plaintext return (`response_data["token"] = plaintext`) stays exactly as-is.
    Keep `get_queryset` owner filter and `revoke` action untouched (PAT-04/06 regression).
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/test_access_tokens.py tests/test_no_plaintext_token_in_db.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `serializers.py` `AccessTokenSerializer.Meta.fields` includes `note` and `token_suffix`; `read_only_fields = fields` unchanged.
    - `AccessTokenCreateSerializer` has `note` with `required=False, allow_blank=True, default=""`; no `token_suffix` input field.
    - `views.py` `acreate` passes `token_suffix=plaintext[-4:]` and `note=...` to `acreate`.
    - `uv run pytest tests/test_access_tokens.py tests/test_no_plaintext_token_in_db.py -q` exits 0 (06-01 backend assertions GREEN; leak guards stay GREEN).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API (create) | Untrusted `note` string crosses here |
| server-side plaintext → DB | Plaintext must never persist; only hash + non-sensitive prefix/suffix |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Information Disclosure | new fields could leak plaintext into DB/serializer | mitigate | `token_suffix=plaintext[-4:]` (4 chars, non-recoverable, same class as 12-char prefix); serializer `read_only_fields = fields`, no plaintext/token_hash in output; guarded by `test_no_plaintext_token_in_db` + `test_list_never_returns_plaintext` |
| T-06-04 | Tampering | `note` writable on output serializer | mitigate | `note` only on `AccessTokenCreateSerializer` (input); output serializer keeps `read_only_fields = fields` |
| T-06-05 | Input Validation | oversized `note` | mitigate | Backend authoritative `max_length=500` on both model + create serializer (V5) |
| T-06-06 | Elevation of Privilege | cross-user create/read | accept (unchanged) | `get_queryset` owner filter untouched; `test_cross_user_isolation` regression |
| T-06-SC | Tampering | pip installs | accept | No package installs (additive code + generated migration only); RESEARCH Package Legitimacy Audit SKIPPED — zero new deps |
</threat_model>

<verification>
- `cd server && uv run python manage.py makemigrations access_tokens --check --dry-run` exits 0 (migration committed, none pending). [BLOCKING]
- `cd server && uv run pytest tests/test_access_tokens.py tests/test_no_plaintext_token_in_db.py -q` exits 0.
- `cd server && uv run python manage.py migrate access_tokens` succeeds on a DB with existing rows (historical rows get empty note/suffix).
</verification>

<success_criteria>
- note + token_suffix columns exist with `default=""`; additive migration committed; `makemigrations --check` clean.
- acreate persists note and token_suffix=plaintext[-4:]; plaintext still returned exactly once, never stored.
- Serializer output exposes note + token_suffix read-only; no plaintext/token_hash leak; owner isolation + revoke unchanged.
</success_criteria>

<output>
Create `.planning/phases/06-pat/06-02-SUMMARY.md` when done.
</output>
