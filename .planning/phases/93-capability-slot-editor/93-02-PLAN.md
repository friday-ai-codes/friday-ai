---
phase: 93-capability-slot-editor
plan: 02
type: execute
wave: 3
depends_on: [93-01]
files_modified:
  - web/src/components/workflow/editor/composables/useConnectionDragState.ts
  - web/src/components/workflow/editor/composables/usePortSnap.ts
  - web/src/components/workflow/editor/composables/__tests__/useConnectionDragState.test.ts
  - web/src/components/workflow/editor/composables/__tests__/usePortSnap.test.ts
autonomous: true
requirements: [SLOT-03]

must_haves:
  truths:
    - "拖拽连线开始时可记录源端口 shape，并据此判定任一目标 input handle 是否兼容（compatible-highlight 数据源）"
    - "指针进入兼容 handle 的吸附半径（28px 屏幕像素，按 viewport.zoom 换算）内时返回该 handle 的吸附端点"
    - "吸附只对形状兼容的目标 handle 生效（不兼容 handle 不吸附）"
    - "既有节点对齐 SNAP_THRESHOLD=5 不被改动（端口吸附为独立常量 PORT_SNAP_THRESHOLD=28）"
  artifacts:
    - path: "web/src/components/workflow/editor/composables/useConnectionDragState.ts"
      provides: "拖拽连接态共享 holder：源 handle/shape + 兼容判定"
      contains: "isCompatibleTarget"
    - path: "web/src/components/workflow/editor/composables/usePortSnap.ts"
      provides: "端口吸附几何计算（PORT_SNAP_THRESHOLD=28）"
      contains: "PORT_SNAP_THRESHOLD"
  key_links:
    - from: "usePortSnap.ts"
      to: "portShapes.ts"
      via: "仅吸附兼容 handle"
      pattern: "arePortShapesCompatible"
    - from: "useConnectionDragState.ts"
      to: "portShapes.ts"
      via: "源 shape 兼容判定"
      pattern: "resolvePortShape|arePortShapesCompatible"
---

<objective>
产出 SLOT-03 磁吸所需的两个共享逻辑 composable（无 UI）：拖拽连接态 holder（供节点 handle 的 compatible-highlight/forbidden 类与画布共享）+ 端口吸附几何（28px 阈值，按 zoom 换算，仅吸附兼容 handle）。被 Wave 3 的 BaseWorkflowNode（handle 类）与 WorkflowCanvas（connect-start/end + 吸附端点）共同消费——通过共享响应式模块通信，避免两 UI 组件互改文件。

Purpose: 把"拖拽态 + 吸附"抽成纯逻辑，使 Wave 3 两个 UI plan 可并行；吸附不绕过合法性。
Output: `useConnectionDragState.ts`、`usePortSnap.ts` + 单测。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/93-capability-slot-editor/93-UI-SPEC.md
@web/src/components/workflow/editor/composables/portShapes.ts
@web/src/components/workflow/editor/composables/useAlignmentGuides.ts
</context>

<artifacts_this_phase_produces>
- `useConnectionDragState.ts`：模块级响应式 holder——`startConnect(sourceNodeId, sourceHandle, sourceShape)` / `endConnect()` / `dragging` ref / `sourceShape` / `isCompatibleTarget(nodeType, handleId)`（用 portShapes 判定目标 input 是否与源兼容）。
- `usePortSnap.ts`：`PORT_SNAP_THRESHOLD = 28`（屏幕像素，独立于既有 `SNAP_THRESHOLD=5`）+ `findSnapTarget(pointer, candidates, viewportZoom)`：在兼容候选 handle 中找半径内最近者返回其中心吸附端点（flow 坐标），无命中返回 null。
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: useConnectionDragState 拖拽连接态 holder</name>
  <read_first>
    - web/src/components/workflow/editor/composables/portShapes.ts（resolvePortShape / arePortShapesCompatible）
    - web/src/components/workflow/editor/WorkflowCanvas.vue（@connect/onConnect 既有；将在 93-06 加 connect-start/end）
    - web/src/components/workflow/editor/nodes/BaseWorkflowNode.vue（handle 渲染结构，将在 93-05 消费本态）
  </read_first>
  <files>web/src/components/workflow/editor/composables/useConnectionDragState.ts</files>
  <action>
    新建 `useConnectionDragState.ts`：模块级单例响应式状态（`const dragging = ref(false)`、`const source = ref<{ nodeId, handleId, shape } | null>(null)`，在 composable 外定义以跨组件共享，对齐既有 alignment overlay 单例思路）。导出 `useConnectionDragState()` 返回：
      - `dragging`（只读 ref）
      - `startConnect(nodeId: string, handleId: string, shape: string | undefined)`：置 dragging=true + source
      - `endConnect()`：置 dragging=false + source=null
      - `isCompatibleTarget(targetNodeType: string, targetHandleId: string): boolean`：dragging 为 false 或无 source → false；否则 `arePortShapesCompatible(source.shape, resolvePortShape(targetNodeType, targetHandleId, 'input'))`。
    不做 DOM/几何计算（几何归 usePortSnap）。纯响应式逻辑，无日志。
  </action>
  <verify>
    <automated>cd web && pnpm vitest run useConnectionDragState</automated>
  </verify>
  <acceptance_criteria>
    - startConnect 后 dragging=true 且 isCompatibleTarget 对兼容 input 返回 true、对不兼容返回 false、对空契约（default/error）返回 true（空通配）。
    - endConnect 后 dragging=false 且 isCompatibleTarget 恒 false。
    - 多次取 `useConnectionDragState()` 共享同一单例状态（单测断言两实例 dragging 联动）。
  </acceptance_criteria>
  <done>跨组件拖拽态 holder 就绪，兼容判定复用 portShapes，单测覆盖兼容/不兼容/空通配/单例联动。</done>
</task>

<task type="auto">
  <name>Task 2: usePortSnap 端口吸附几何（28px / zoom 换算 / 仅兼容）</name>
  <read_first>
    - web/src/components/workflow/editor/composables/useAlignmentGuides.ts（既有 SNAP_THRESHOLD=5，本 plan 不得改动它）
    - web/src/components/workflow/editor/composables/portShapes.ts
    - .planning/phases/93-capability-slot-editor/93-UI-SPEC.md（"吸附阈值（prescriptive）" 段：PORT_SNAP_THRESHOLD=28，按 viewport.zoom 换算到 flow 坐标后比距；吸附只改拖拽连接线视觉端点，落点仍经 isValidConnection 双校验）
  </read_first>
  <files>web/src/components/workflow/editor/composables/usePortSnap.ts</files>
  <action>
    新建 `usePortSnap.ts`，导出独立常量 `export const PORT_SNAP_THRESHOLD = 28`（屏幕像素，**绝不**改 useAlignmentGuides 的 SNAP_THRESHOLD=5——端口吸附与节点对齐是两套逻辑）。导出纯函数 `findSnapTarget(pointer: { x: number, y: number }, candidates: Array<{ nodeId, handleId, x, y, compatible: boolean }>, zoom: number): { nodeId, handleId, x, y } | null`：
      - 仅在 `compatible===true` 的候选中比距；
      - 阈值换算：屏幕 28px 对应 flow 距离 = `PORT_SNAP_THRESHOLD / zoom`（保证不同缩放下手感一致）；
      - 取半径内欧氏距离最近者返回其 handle 中心（flow 坐标）作吸附端点；无命中返回 null。
    candidate 的兼容性由调用方（93-06）用 useConnectionDragState.isCompatibleTarget 预先标注（本函数不查 store，保持纯几何 + 可单测）。注释强调"吸附不绕过合法性：落点仍由 isValidConnection + getValidationError 双重校验"。
  </action>
  <verify>
    <automated>cd web && pnpm vitest run usePortSnap</automated>
  </verify>
  <acceptance_criteria>
    - 兼容候选在阈值内 → 返回最近 handle 端点；多个命中取最近。
    - 不兼容候选即便在阈值内 → 不返回（compatible=false 被跳过）。
    - zoom=0.5 时屏幕 28px 对应 flow 56px（换算正确，单测验证边界距离命中/不命中）。
    - useAlignmentGuides.ts 未被改动（SNAP_THRESHOLD 仍=5）。
  </acceptance_criteria>
  <done>端口吸附几何就绪（28px 独立常量 + zoom 换算 + 仅兼容），节点对齐逻辑零回归，纯函数单测覆盖命中/跳过/换算。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 吸附端点 → 落点校验 | 吸附仅改视觉端点，最终落点仍经合法性校验 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-02-BYPASS | Tampering | usePortSnap 吸附 | mitigate | 吸附只改拖拽连接线视觉端点，落点经 isValidConnection + getValidationError 双校验（93-01/93-06），吸附不放行非法连接 |
| T-93-02-SC | Tampering | 依赖安装 | mitigate | 无 npm 安装，无供应链面 |
</threat_model>

<verification>
- `pnpm -C web vitest run useConnectionDragState usePortSnap` 全绿。
- `pnpm -C web vue-tsc --noEmit` 通过；既有 useAlignmentGuides 测试零回归。
</verification>

<success_criteria>
- 拖拽态 holder + 端口吸附几何就绪，供 Wave 3 UI 消费；吸附仅兼容 handle、不绕合法性；节点对齐逻辑不受影响。
</success_criteria>

<output>
Create `.planning/phases/93-capability-slot-editor/93-02-SUMMARY.md` when done
</output>
