---
phase: 93-capability-slot-editor
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/components/workflow/sidebar/NodePalette.vue
  - web/src/components/workflow/editor/nodes/nodeVisuals.ts
  - web/src/components/__tests__/node-sync.test.ts
autonomous: true
requirements: [SLOT-03, SLOT-04]

must_haves:
  truths:
    - "clarification_card 节点在 NodePalette 可见可拖（Phase 92 fixture 已含 node_count=42）"
    - "clarification_card 有专属节点视觉（澄清=琥珀语义）"
    - "node-sync 红线保持绿（palette 节点类型 ⊆ 后端 fixture node_type 全集）"
  artifacts:
    - path: "web/src/components/workflow/sidebar/NodePalette.vue"
      provides: "clarification_card palette 收录项"
      contains: "clarification_card"
    - path: "web/src/components/workflow/editor/nodes/nodeVisuals.ts"
      provides: "clarification_card 视觉条目"
      contains: "clarification_card"
  key_links:
    - from: "NodePalette.vue"
      to: "node-types.fixture.json"
      via: "palette ⊆ fixture 漂移守护"
      pattern: "clarification_card"
---

<objective>
让 Phase 92 已注册的 `clarification_card` 节点在前端节点库可见可拖（92-03 已入 fixture，但 palette 未收录），并给它专属视觉（澄清=琥珀语义）。保持前后端节点漂移守护红线（palette ⊆ fixture）为绿。

Purpose: 澄清卡是 SLOT-04 子节点生态的关键原子节点；UI-SPEC「Palette 收录」明确要求收录使其可见可拖。
Output: NodePalette 收录项 + nodeVisuals 条目 + node-sync 守护更新（仍绿）。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/93-capability-slot-editor/93-UI-SPEC.md
@.planning/phases/92-capability-slot-backend/92-03-SUMMARY.md
@web/src/components/__tests__/node-sync.test.ts
</context>

<artifacts_this_phase_produces>
- NodePalette 新增 `{ type: 'clarification_card', name: '澄清卡', description: '发送澄清交互卡并等待回答' }`（归入 AI 分组，与 ai_plan_research 澄清生态呼应——Claude's Discretion）。
- nodeVisuals 新增 `clarification_card: { icon: <lucide MessageCircleQuestion 或同类>, color: 'orange' }`（orange=琥珀家族，呼应既有 clarify 琥珀语义）。
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: NodePalette 收录 clarification_card + nodeVisuals 视觉条目</name>
  <read_first>
    - web/src/components/workflow/sidebar/NodePalette.vue（nodeGroups 结构；fromDef 与裸 `{ type }` 两种写法；node-sync 正则提取 `type:` 与 `fromDef(`）
    - web/src/components/workflow/editor/nodes/nodeVisuals.ts（NODE_VISUALS map；色键仅 blue/green/purple/orange；getNodeVisual 回退 FALLBACK）
    - web/src/components/__tests__/node-sync.test.ts（palette ⊆ fixture 守护；fixture 已含 clarification_card）
    - .planning/phases/93-capability-slot-editor/93-UI-SPEC.md（"Palette 收录" 段 + 澄清=琥珀色）
  </read_first>
  <files>web/src/components/workflow/sidebar/NodePalette.vue, web/src/components/workflow/editor/nodes/nodeVisuals.ts</files>
  <action>
    NodePalette.vue：在 `AI` 分组 items 末尾追加裸项 `{ type: 'clarification_card', name: '澄清卡', description: '发送澄清交互卡并等待回答' }`（用裸 `{ type }` 而非 fromDef——clarification_card 可能不在前端 ALL_NODE_DEFINITIONS；node-sync 正则同时匹配 `type:` 故仍被收录守护）。保持既有分组顺序/其它项不动。
    nodeVisuals.ts：在 NODE_VISUALS 加 `clarification_card: { icon: MessageCircleQuestion, color: 'orange' }`（从 lucide-vue-next import 一个澄清语义图标如 `MessageCircleQuestion`；color 用 'orange'=琥珀家族，呼应既有 need_clarification 琥珀语义，避免新增色键牵动 useNodeStyle）。
  </action>
  <verify>
    <automated>cd web && pnpm vitest run node-sync</automated>
  </verify>
  <acceptance_criteria>
    - node-sync 测试全绿：palette ⊆ fixture（clarification_card ∈ fixture 故不报 orphan）、幽灵节点守护不破。
    - getNodeVisual('clarification_card') 返回专属图标 + orange（非 FALLBACK）。
    - palette 渲染含「澄清卡」项可拖（既有 NodePaletteItem 拖拽机制复用，不另写）。
    - `pnpm -C web vue-tsc --noEmit` 通过；受改文件 eslint 干净。
  </acceptance_criteria>
  <done>clarification_card 进 palette（AI 分组）+ 专属琥珀视觉，node-sync 红线保持绿，无新增色键。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 无 | 纯前端静态节点库/视觉数据，无信任边界变化 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-04-SC | Tampering | 依赖安装 | mitigate | 无 npm 安装（仅 import 既有 lucide-vue-next 图标），无供应链面 |
| T-93-04-DRIFT | Tampering | 前后端节点漂移 | mitigate | node-sync 守护断言 palette ⊆ fixture，收录的 clarification_card 必须存在于后端注册表（92-03 已落 fixture）才放行 |
</threat_model>

<verification>
- `pnpm -C web vitest run node-sync` 全绿。
- `pnpm -C web vue-tsc --noEmit` 通过。
</verification>

<success_criteria>
- clarification_card 在节点库可见可拖、有专属琥珀视觉，前后端漂移守护保持绿。
</success_criteria>

<output>
Create `.planning/phases/93-capability-slot-editor/93-04-SUMMARY.md` when done
</output>
