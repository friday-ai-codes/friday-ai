---
phase: 93-capability-slot-editor
plan: 06
type: execute
wave: 4
depends_on: [93-01, 93-02, 93-03]
files_modified:
  - web/src/components/workflow/editor/WorkflowCanvas.vue
  - web/src/components/workflow/editor/edges/CustomConnectionLine.vue
  - web/src/components/workflow/editor/__tests__/WorkflowCanvas.slot.test.ts
autonomous: false
requirements: [SLOT-03, SLOT-04]

must_haves:
  truths:
    - "拖拽连线开始/结束驱动共享拖拽态（@connect-start/@connect-end → useConnectionDragState）"
    - "指针进入兼容 handle 吸附半径内时拖拽连接线端点吸附到 handle 中心（snap-locked），落点仍经 isValidConnection + getValidationError 双校验"
    - "不兼容连接落点被拒并弹出形状不兼容 Toast（中文 shape 名）"
    - "把澄清节点拖到方案节点 clarify 槽 / 经 clarify→澄清输入连接时形成附着编组——单一实现：每个有附着子的父节点在画布渲染一个 .slot-attach-group 琥珀虚线容器（覆盖父子包围盒）+ 一个 .slot-attach-connector 短实线琥珀连接器（≤24px），删父级联删子"
    - "删除含附着子的方案节点 / 解除附着 前弹确认（deleteWithChildBody / detachBody）"
  artifacts:
    - path: "web/src/components/workflow/editor/WorkflowCanvas.vue"
      provides: "connect-start/end + 吸附消费 + 附着编组渲染 + 解除/删除确认"
      contains: "useConnectionDragState"
    - path: "web/src/components/workflow/editor/edges/CustomConnectionLine.vue"
      provides: "吸附端点消费"
      contains: "snap"
  key_links:
    - from: "WorkflowCanvas.vue"
      to: "usePortSnap.findSnapTarget"
      via: "拖拽中计算吸附端点"
      pattern: "findSnapTarget"
    - from: "WorkflowCanvas.vue onConnect"
      to: "store.attachChild"
      via: "clarify 槽连澄清节点 → 附着编组"
      pattern: "attachChild"
    - from: "WorkflowCanvas.vue onConnect"
      to: "getValidationError"
      via: "不兼容落点拒绝 + Toast"
      pattern: "getValidationError"
---

<objective>
画布层集成（SLOT-03 磁吸交互 + SLOT-04 附着编组视觉）：把 Wave 1/2 的拖拽态、吸附、契约校验、父子数据接入 `@vue-flow` 画布——拖拽时驱动共享态 + 吸附端点 + 不兼容拒绝 Toast；把澄清节点附着到方案节点 clarify 槽（琥珀虚线编组容器 + 短实线连接器）并兑现删父级联删子 + 解除/删除确认。本 plan 含人工验收 checkpoint（画布交互观感）。

Purpose: SLOT-03「兼容高亮 + 磁吸吸附 + 不兼容不可连」与 SLOT-04「附着子节点可视编组 + 下接发群」的最终用户可见兑现。
Output: WorkflowCanvas 磁吸/编组/确认 + CustomConnectionLine 吸附端点 + 人工验收。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/93-capability-slot-editor/93-CONTEXT.md
@.planning/phases/93-capability-slot-editor/93-UI-SPEC.md
@web/src/components/workflow/editor/WorkflowCanvas.vue
@web/src/components/workflow/editor/edges/CustomConnectionLine.vue
@web/src/components/workflow/editor/composables/usePortSnap.ts
@web/src/components/workflow/editor/composables/useConnectionDragState.ts
</context>

<artifacts_this_phase_produces>
- WorkflowCanvas：`@connect-start`/`@connect-end` 驱动 `useConnectionDragState`（含解析源 handle shape）；拖拽中收集候选 input handle（DOM/Vue Flow 几何 + isCompatibleTarget 标注）→ `usePortSnap.findSnapTarget` 算吸附端点 → 传 CustomConnectionLine + snap-locked 脉冲；`onConnect` 拒绝不兼容（getValidationError → Toast incompatibleBody）+ clarify 槽连澄清节点时 `store.attachChild`（绝对→相对坐标换算 + dock 右下）；**附着编组容器（单一实现，WARNING 2 收敛）**：在画布 Vue Flow 视口层渲染一个 `v-for` 覆盖层——对每个 `getChildNodes(parentId).length>0` 的父节点输出一个 `.slot-attach-group` 元素（琥珀虚线 `bg-amber-500/[0.04] border border-dashed border-amber-400/40 rounded-2xl`，绝对定位覆盖父子包围盒）+ 一个 `.slot-attach-connector` 短实线琥珀连接器（≤24px，父 clarify 槽→子 input）；删除/解除附着 AlertDialog 确认（deleteWithChildBody / detachTitle+detachBody）。
- CustomConnectionLine：可选 `snapX/snapY` props，命中吸附时用吸附端点替代 targetX/Y 绘制 + snap-locked 视觉。
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: 拖拽态 + 吸附端点 + 不兼容拒绝 Toast（SLOT-03 磁吸交互）</name>
  <read_first>
    - web/src/components/workflow/editor/WorkflowCanvas.vue（VueFlow 事件：onConnect 用 getValidationError+showError；CustomConnectionLine slot；viewport vfViewport；ConnectionMode.Strict）
    - web/src/components/workflow/editor/edges/CustomConnectionLine.vue（ConnectionLineProps：sourceX/Y/targetX/Y）
    - web/src/components/workflow/editor/composables/usePortSnap.ts / useConnectionDragState.ts（93-02）
    - web/src/components/workflow/editor/composables/useConnectionValidator.ts（93-01 契约规则）
    - .planning/phases/93-capability-slot-editor/93-UI-SPEC.md（SLOT-03 拖拽态机表 + 吸附阈值 + 实现落点）
  </read_first>
  <files>web/src/components/workflow/editor/WorkflowCanvas.vue, web/src/components/workflow/editor/edges/CustomConnectionLine.vue</files>
  <action>
    WorkflowCanvas：接 `@connect-start="onConnectStart"`（VueFlow 提供 `{ nodeId, handleId, handleType }`）→ 解析源 output 端口 shape（resolvePortShape）→ `useConnectionDragState().startConnect(nodeId, handleId, shape)`；`@connect-end="onConnectEnd"` → `endConnect()` + 清吸附端点 ref。
    拖拽中算吸附：监听拖拽指针（VueFlow connection 位置或 pointermove）→ 收集当前可见节点的 input handle 几何（flow 坐标，经 `findNode().dimensions/computedPosition` 或 DOM 查 `.vue-flow__handle` rect 换算）+ 用 `isCompatibleTarget` 标 compatible → `findSnapTarget(pointer, candidates, viewport.zoom)` → 命中则置 `snapEndpoint` ref（flow 坐标）。
    CustomConnectionLine：新增可选 `snapX?/snapY?` props，命中时用其替代 targetX/Y 绘制 bezier 端点 + snap-locked 视觉（端点小竖条换为更明显标记/脉冲，遵循 prefers-reduced-motion 既有全局降级）。WorkflowCanvas 经 `#connection-line` slot 把 snapEndpoint 透传。
    onConnect：保持既有 `getValidationError`（93-01 已含契约规则，返回 incompatibleBody 中文）→ 非 null 时 `showError(t('workflow.editor.slot.incompatibleTitle'), err)` 并 return（不连）；兼容则既有 store.addEdge（吸附不绕过：落点仍走此校验）。若吸附命中，addEdge 用吸附 handle 作 targetPort。
    高频 pointermove：节流/仅 dragging 时计算，纯几何 O(n) 候选，不打 INFO 日志。
  </action>
  <verify>
    <automated>cd web && pnpm vitest run WorkflowCanvas.slot</automated>
  </verify>
  <acceptance_criteria>
    - connect-start 后 useConnectionDragState.dragging=true 且源 shape 被记录；connect-end 后复位。
    - 不兼容落点 onConnect 不 addEdge 且 showError 含 incompatibleBody（真实 zh-CN.json 断言）。
    - 兼容且吸附半径内 → snapEndpoint 命中、addEdge 用吸附目标端口；吸附不放行不兼容（compatible=false 不吸附，落点仍被校验拒）。
    - 既有 onConnect 合法连线（default→default）零回归。
  </acceptance_criteria>
  <done>SLOT-03 磁吸交互闭环：拖拽态/吸附端点/兼容高亮数据源/不兼容 Toast 拒绝，吸附不绕合法性，既有连线零回归。</done>
</task>

<task type="auto">
  <name>Task 2: 附着编组渲染 + clarify 附着 + 级联删除/解除确认（SLOT-04）</name>
  <read_first>
    - web/src/components/workflow/editor/WorkflowCanvas.vue（onConnect / onNodesChange remove / removeNode）
    - web/src/stores/useWorkflowsStore.ts（93-03 attachChild/detachChild/removeNode 级联/getChildNodes）
    - web/src/components/workflow/editor/composables/useWorkflowTransform.ts（93-03 parentNode/extent 映射）
    - .planning/phases/93-capability-slot-editor/93-UI-SPEC.md（SLOT-04 编组视觉规格：琥珀虚线容器 + 短实线琥珀连接器 ≤24px + 子 dock 右下 + 拖父整组移动 + extent:'parent' 限制 + 解除经右键/工具栏确认）
    - ~/components/ui（AlertDialog 用于确认）
  </read_first>
  <files>web/src/components/workflow/editor/WorkflowCanvas.vue</files>
  <action>
    附着触发：onConnect 中当源 handle 为 `clarify`/shape=clarification_request（方案节点）且目标为 clarification_card 节点 input → 调 `store.attachChild(childId=澄清节点, parentId=方案节点, relativePosition)`（绝对→相对换算：子相对父 = 子绝对 − 父绝对；dock 到父卡 clarify 槽右下方偏移）而非建普通边（生命周期绑定，UI-SPEC 方案 A）。
    编组容器视觉（**单一实现方案，WARNING 2 收敛——不再留白/不再"实现可选"**）：附着子节点经 Vue Flow parent/child（93-03 已映射 extent:'parent'）天然包含；在 WorkflowCanvas 模板的 Vue Flow 视口插槽层（如 `<template #...>` 或绝对定位于 `.vue-flow__viewport` 内的 overlay 容器，随 viewport transform）用 `v-for` 渲染编组外框：对每个「有附着子」的父节点（`store.getChildNodes(parentId).length > 0`）输出**一个** `<div class="slot-attach-group">`，类含 `bg-amber-500/[0.04] border border-dashed border-amber-400/40 rounded-2xl`，绝对定位 + 尺寸取父子节点包围盒（经 `findNode().computedPosition/dimensions` 计算 min/max；happy-dom 无真实布局时尺寸可为 0，但元素必须渲染存在）。父 clarify 槽→子 input 之间渲染**一个** `<div class="slot-attach-connector">` 短实线琥珀连接器（amber 实线，长度 ≤24px，CSS 连接条，非普通 bezier edge）。两个类名为稳定 hook，供 vitest 断言存在性。计算包围盒为派生 computed，避免每帧建新对象。
    级联删除确认：删除带附着子的方案节点（onNodesChange remove / 工具栏删除）前，若 `store.getChildNodes(id).length>0` → AlertDialog 确认 `deleteWithChildBody`（具名插值 name/count）→ 确认才 `store.removeNode`（93-03 级联删子）。
    解除附着：子节点右键/工具栏「解除附着」→ AlertDialog `detachTitle`+`detachBody` 确认 → `store.detachChild(childId, absolutePosition)`（相对→绝对换算恢复独立坐标）。
    下游：子澄清节点 `feishu_message`（cyan）出口仍是普通 output handle，可正常拉 bezier 边连到发群/通知节点（不在编组容器内）——不特殊处理，既有连线机制即可（确认 onConnect 对 feishu_message→feishu_message 兼容放行）。
    i18n：组件内 useI18n().t，键已由 93-01 落 locale（本 plan 不改 zh-CN.json，避免与 93-05 同 Wave 冲突——两者均只读 locale）。
  </action>
  <verify>
    <automated>cd web && pnpm vitest run WorkflowCanvas.slot</automated>
  </verify>
  <acceptance_criteria>
    - clarify 槽连 clarification_card → 调 store.attachChild（断言被调 + 相对坐标换算），不建普通边。
    - **编组容器视觉断言（WARNING 2 最小 vitest）**：挂载含一对「父方案节点 + 附着澄清子节点」的图 → `wrapper.find('.slot-attach-group').exists()` 为 true 且渲染 `.slot-attach-connector`；挂载无附着关系的基线图 → `.slot-attach-group` 不存在（附着后才出现 amber 编组容器）。
    - 删带子的父节点前弹 deleteWithChildBody 确认；确认后父子均删（级联）。
    - 解除附着弹 detachBody 确认；确认后 detachChild 被调、子恢复独立绝对坐标。
    - 子 feishu_message 出口可连通知节点（兼容放行，不被编组限制）。
    - `pnpm -C web vue-tsc --noEmit` 通过；受改文件 eslint 干净。
  </acceptance_criteria>
  <done>SLOT-04 附着编组兑现：clarify 附着 + 单一实现的 .slot-attach-group 琥珀虚线容器/.slot-attach-connector 连接器（含最小 vitest 存在性断言）+ 删父级联删子确认 + 解除附着确认 + 子下接发群，i18n 守护文案到位。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    @vue-flow 编辑器形状磁吸（兼容高亮/吸附/不兼容禁止 + Toast）+ 澄清节点附着方案节点编组（琥珀虚线 + 附着徽标 + 删父级联 + 解除确认）+ 能力分类视觉 + IM 门控 + clarification_card 可拖。
  </what-built>
  <how-to-verify>
    1. `make dev`（或 `pnpm -C web dev`）打开工作流编辑器（/workflows/<id>）。
    2. 从节点库拖入 `澄清卡`（AI 分组）—— 确认可见可拖。
    3. 拖一个 ai_plan_research → 观察其右侧 `clarify`(琥珀方形凹槽)/`resume` 端口；从 clarify 槽拖向澄清卡输入：兼容槽应绿色高亮放大、靠近时吸附；落下后形成琥珀虚线编组 + 子卡『附着』徽标。
    4. 拖一个不兼容 typed shape 端口互连：应高亮禁止（红/降透明）+ 落点弹「形状不兼容」Toast，不建边。
    5. 删除该方案节点：应弹「将一并移除 N 个附着澄清节点」确认；确认后澄清子节点一并消失。
    6. 子澄清卡 feishu_message 出口拉线到 飞书通知 节点：应可连。
    7. 新建仅含 notify_feishu_im 的图（无建群节点）：该节点应降级（半透明 + 锁徽标 + tooltip「需先添加创建群聊节点」）；加入 create_group_chat 后门控解除。
    8. 既有工作流编辑/连线/单选卡：确认零回归（打开既有模板正常渲染/保存）。
  </how-to-verify>
  <resume-signal>输入 "approved" 或描述问题</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 画布交互 → 保存 | 前端磁吸/编组为编辑体验，落库经后端校验 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-06-BYPASS | Tampering | 吸附/磁吸落点 | mitigate | 吸附只改视觉端点，onConnect 落点仍经 getValidationError（契约校验）+ 后端 WorkflowGraphValidator 保存即校验；磁吸不放行非法连接 |
| T-93-06-INFO | Information Disclosure | Toast/确认文案 | mitigate | 文案只含 shape 中文名 + 节点 name + 附着数，不回显 config/凭证 |
| T-93-06-SC | Tampering | 依赖安装 | mitigate | 无 npm 安装（既有 @vue-flow/reka-ui），无供应链面 |
</threat_model>

<verification>
- `pnpm -C web vitest run WorkflowCanvas.slot` 全绿；`pnpm -C web vue-tsc --noEmit` 通过。
- 既有 WorkflowCanvas/连线/对齐相关 vitest 零回归。
- 人工验收 checkpoint 通过（画布交互观感）。
</verification>

<success_criteria>
- 用户可见兑现 SLOT-03（兼容高亮 + 磁吸吸附 + 不兼容不可连）与 SLOT-04（附着编组 + 删父级联 + 子下接发群）；既有编辑器零回归。
</success_criteria>

<output>
Create `.planning/phases/93-capability-slot-editor/93-06-SUMMARY.md` when done
</output>
