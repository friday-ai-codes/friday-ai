---
phase: 112-1
plan: 04
type: execute
wave: 3
depends_on: [112-02, 112-03]
files_modified:
  - server/services/process_runtime/blueprint_research_adapter.py
  - server/subagent/api/callbacks.py
  - server/tests/services/process_runtime/test_blueprint_research_stage.py
  - server/tests/subagent/test_blueprint_research_callback.py
  - server/tests/services/process_runtime/test_blueprint_reroute.py
autonomous: true
requirements: [FLOW-02, FLOW-04]
prohibitions:
  - "禁止修改 server/codegraph/services/repo_router_v2.py（STATE.md 并行纪律 1）"
  - "禁止修改 process_runtime 六冻结文件：research_adapter.py 是本 plan 的 analog，**复制范式到新文件，绝不改它、也不 import 它**；decompose_segments.py / architect_merge_adapter.py / merged_plan.py / clarify_adapter.py / render.py 同样零改动"
  - "禁止修改 server/services/process_runtime/resume.py（蓝图续驱走 112-05 的 blueprint_resume.py）"
  - "禁止复用 ResearchService.retry_task（其 stage 名硬编码 'research'，本相位 stage 名为 repo_research 会恒 raise）；也禁止为此修改 research_service.py 既有语义——重试路径在新 adapter 内自实现"
  - "禁止修改 ConvergenceSessionEvent 既有事件类型/字段；只消费 112-01 注册的 blueprint.repo_research.* 与 blueprint.reroute.triggered 常量"
  - "callbacks.py 唯一允许的改动形状：新增 _is_blueprint_research / _aload_blueprint_research_task / _handle_blueprint_research_completion|_failure 三件套 + 在 _handle_completed 与 _handle_failed 各加一段 try/except 调用 + 在 last_output→CallSource 映射处加一条；既有 plan_research 与 repo_verify 分支、arevoke_task_tokens 调用位置一字不动"
  - "禁止新写 token 吊销钩子：PLAN 链终态回调已无条件调用 arevoke_task_tokens，本相位只补 mint 与 env 注入两侧"
  - "禁止把明文 task token 落盘 / 进日志 / 进 ConvergenceSessionEvent payload（PAT-02 底线）"
  - "禁止扩容器 MCP 白名单（CONTEXT 明确留给 Phase 113 Context Bus）"
  - "禁止在各容器回调里各自读改写 stage_state 的 reroute 计数（lost-update）：计数只在 fan-out barrier 收敛后的单点串行转移里递增"
  - "禁止修改 builtin_processes.py（stage 注册在 112-05）、blueprint_lifecycle_service.py（112-02 独占）、blueprint_route.py（112-03 独占）"
  - "reroute 不收敛禁止落 STAGE_FAILED / session failed：必须带全部现状升确认门（CONTEXT「绝不静默失败」）"
must_haves:
  truths:
    - "direct 候选按仓 fan-out 起独立 SubAgentSession(TaskType.PLAN) 容器；单仓 dispatch 失败只标该 task failed 并 continue，绝不上抛拖垮整个 session（FLOW-02 过程对用户可见）"
    - "容器 metadata 注入 env_FRIDAY_TASK_TOOLS_ENDPOINT / env_FRIDAY_TASK_KNOWLEDGE_ENDPOINT / env_FRIDAY_TASK_USER_TOKEN 三键（endpoint 由 settings.FRIDAY_BASE_URL 推导，非 runner callback_url）；空值不注入该键；dispatch 失败主动 arevoke"
    - "章程内容随 prompt 注入容器（不扩 MCP 白名单）；prompt 由服务端权威 session 状态构造"
    - "回调把容器结果落 PartialPlan.content 的 fitness{verdict,reasons,citations} + role_suggestion + responsibility + findings[]（经 ResearchService.record_partial，回调零裸 ORM 写）；缺 verdict 视为不可解析 → mark_failed"
    - "indirect 候选默认服务端轻量合成（能力树 + RAG + 知识图谱证据），提供置回 PENDING 升级为深调研的入口"
    - "unsuitable 仓被排除后由主 agent 补候选重调研，轮次计数存 ConvergenceSession.stage_state 且上界 2；超限带全部现状升 repo_confirmation 而非落 failed"
  artifacts:
    - path: "server/services/process_runtime/blueprint_research_adapter.py"
      provides: "逐仓 fan-out dispatch + token/env 注入 + 章程 prompt 注入 + 轻量合成 + 自实现重试 + reroute 判定"
      contains: "env_FRIDAY_TASK_USER_TOKEN"
    - path: "server/subagent/api/callbacks.py"
      provides: "blueprint_research 第三种 PLAN 用途的回调三件套与两个挂载点"
      contains: "_is_blueprint_research"
    - path: "server/tests/services/process_runtime/test_blueprint_reroute.py"
      provides: "reroute ≤2 轮与超限升确认门的机制断言（含 stage_state 浅合并不丢既有键）"
      contains: "reroute_count"
  key_links:
    - from: "server/services/process_runtime/blueprint_research_adapter.py"
      to: "server/access_tokens/services.py"
      via: "mint_task_token(dispatch_user, session_id, timeout_seconds) 在 SubAgentSession 建行之后"
      pattern: "mint_task_token"
    - from: "server/subagent/api/callbacks.py"
      to: "server/delivery/services/research_service.py"
      via: "record_partial / mark_failed（回调零裸 ORM 写，INV-6）"
      pattern: "record_partial"
    - from: "server/services/process_runtime/blueprint_research_adapter.py"
      to: "server/subagent/api/callbacks.py"
      via: "last_output={\"source\": \"blueprint_research\", ...} 是回调路由的唯一依据（不靠 session_id 命名）"
      pattern: "blueprint_research"
---

<objective>
落地阶段 1 的逐仓容器调研与有界重路由：direct 候选起独立 claude code 容器（PLAN 链首次接通任务 token 与容器知识 endpoint）、章程随 prompt 注入、结论以 `fitness` / `role_suggestion` / `responsibility` / `findings` 落 `PartialPlan.content`；indirect 候选默认服务端轻量合成并可人工升级深调研；`unsuitable` 触发重路由，轮次上界 2，仍不收敛带全部现状升确认门。

Purpose: FLOW-02 全量（fitness 判定 + 有界重路由 + 过程可见）+ FLOW-04 后半（indirect 轻量、可升级深调研）。
Output: 新 `blueprint_research_adapter.py` + `callbacks.py` 的第三条 PLAN 分支 + 3 个测试文件。
</objective>

<execution_context>
工作目录：worktree 根。wave 3 唯一 plan（依赖 112-02 的线程 API 与 112-03 的候选清单）。
`research_adapter.py` 是 analog 但属 §13.2 冻结面：**复制范式建新文件，不改它也不 import 它**。`callbacks.py` 非冻结但受限——只允许「加一条同构新链」的形状（与 `repo_verify` 分支完全对称）。
</execution_context>

<context>
@.planning/phases/112-1/112-CONTEXT.md（锁定：容器 = SubAgentSession(TaskType.PLAN) 按仓 fan-out；派发时 mint_task_token + 3 个 FRIDAY_TASK_* env 键；章程随 prompt 注入不扩 MCP 白名单；token 吊销已复用无需重写；fitness 落 PartialPlan.content；retry_task stage 名不匹配需自实现重试；direct 深调研 / indirect 轻量 + 人工升级入口；reroute ≤2 轮计数存 stage_state，不收敛升确认门绝不静默失败）
@.planning/phases/112-1/112-RESEARCH-ROUTING.md §3（RepoResearchTask 全字段——**无 report 字段**；PartialPlan.content 是 fitness 真实落点与既有 §7 键；ResearchService 全签名，record_partial 已含 mark_done，retry_task stage 硬编码）、§4（mint/revoke 签名与 user 必须是真实 User 实例；coding.py 的 env 键全清单与「空值不注入」契约；PLAN 链现状缺的三键；last_output 路由约定与 callbacks 的判定/反查/挂载点行号；新增第三种 PLAN 用途必须用新的 source 值并补 CallSource 映射）、Pitfalls P1（吊销已覆盖 + 顺序陷阱 + timeout/cancelled 残余风险）、P2（async ORM 必须 sync_to_async 的位置）、P3（stage_state 整字典覆盖 + 只能随转移写 + 并行 lost-update 与单点串行缓解）、P5（record_partial 每次 create 新行 → 读 fitness 按 valid=True 取最新）
@.planning/phases/112-1/112-PATTERNS.md 第 4 类（派发侧五步与幂等白名单、单仓错误隔离 WR-02、session_id 必带 uuid 后缀、prompt 由服务端权威状态构造；回调侧三件套与挂载点、结果不可解析即失败、barrier 幂等）、第 5 类（token + env 注入逐字片段与「空值不注入」契约、dispatch 失败主动 arevoke）、第 9 类（容器 mock 断言口径）
@.planning/phases/112-1/112-03-SUMMARY.md（候选清单形状与 role 分桶依据：confidence + charter/history 证据）
@.planning/phases/112-1/112-02-SUMMARY.md（lifecycle 线程 API——升确认门时由 112-05 建线程，本 plan 只产出「需升门」信号与现状快照）
@server/services/process_runtime/research_adapter.py（analog 全文：L40-43 幂等白名单、L76-131 分桶与单仓隔离、L153-222 派发五步、L256-314 prompt 构造与轻量合成、L337-390 metadata 与 runner 计数——**冻结只读**）
@server/workflows/nodes/ai/coding.py（L1915-1990 tools_env 三键注入逐字范式 + metadata 分组合并；L2043-2047 dispatch 失败 arevoke）
@server/access_tokens/services.py（L28-102 mint_task_token / arevoke_task_tokens 语义与明文只暴露一次）
@server/subagent/api/callbacks.py（L925-1035 两个终态挂载点与 arevoke 位置、L1267-1280 last_output→CallSource 映射、L1699-1828 plan_research 三件套、L1830-1930 repo_verify 同构分支——本 plan 照 repo_verify 形状加第三条）
@server/delivery/services/research_service.py（L37-227 全部签名）
@server/tests/subagent/test_repo_verify_callback.py（同构新链的回调测试范式）
</context>

<tasks>

<task type="auto">
  <name>Task 1: blueprint_research_adapter 派发面 —— fan-out、token/env 注入、章程 prompt、轻量合成</name>
  <files>server/services/process_runtime/blueprint_research_adapter.py</files>
  <read_first>server/services/process_runtime/research_adapter.py 全文（逐条镜像：L40-43 幂等白名单、L76-100 deep/light 分桶与 runner 计数降级、L114-131 单仓 try/except + mark_failed + continue、L153-222 派发五步与 last_output、L256-314 prompt 与轻量合成、L337-390 _build_dispatch_metadata——冻结只读）；server/workflows/nodes/ai/coding.py L1915-1990 与 L2043-2047；server/access_tokens/services.py L28-64；server/delivery/services/research_service.py L37-125；server/services/process_runtime/blueprint_charter_match.py（112-03 的 aload_charters，章程 prompt 注入取数复用）</read_first>
  <action>
新建 `server/services/process_runtime/blueprint_research_adapter.py`。模块 docstring 五段：「阶段 1 逐仓调研派发（复制 research_adapter 范式的独立文件，§13.2 冻结面零改动、零 import）」/「INV-6：业务表写入只经 ResearchService；本文件不裸写 ORM」/「WR-02 单仓错误隔离：任何单仓失败不上抛」/「PAT-02：明文 token 只进 dispatch metadata，绝不落盘/进日志/进事件 payload」/「不扩容器 MCP 白名单（113 Context Bus）」。

模块常量：`_DISPATCHABLE_STATUSES = (RepoResearchTaskStatus.PENDING, RepoResearchTaskStatus.STALE)`（注释：resume/re-advance 时跳过 running/done/failed，既不重派已完成容器也不重复合成）；`_RESEARCH_TIMEOUT = 1800`；`_BLUEPRINT_RESEARCH_SOURCE = "blueprint_research"`（**新的 source 值，不能是 plan_research，否则被既有 handler 抢走**）；`_MAX_ATTEMPTS`（自实现重试上界）。

`class BlueprintResearchAdapter`（依赖 keyword-only 可注入：`research_service`、`dispatcher_factory`、`charters_loader`）：
- `async def dispatch(self, session) -> dict`：
  1. 从 `session.stage_state["routing"]`（112-03 写入的候选清单）取候选，按 `role_suggestion`/`confidence` 分 **deep（direct）** 与 **light（indirect）** 两桶并**按 repository_id 去重**。
  2. deep 桶前 `_count_online_runners()`（3 倍心跳窗口 120s，照 analog L379-390）；**无 runner 则整体降级为 light 不阻断**，记 `blueprint_repo_research_degraded_to_light` warning。
  3. `await research_service.create_tasks_for_session(session, deep_repos)`（幂等 get_or_create；`deep_repos` 每项 `{"repository_id": str, "routed_confidence": str}`）。
  4. 逐 task：`status not in _DISPATCHABLE_STATUSES` → skip；否则 `_dispatch_deep_task` 包 try/except → 失败 `await research_service.mark_failed(task, {...})` + emit failed + `continue`（**绝不上抛**）。
  5. light 桶 `_synthesize_light_partial(task_or_repo)`：服务端合成（能力树命中 + RAG 摘要 + 知识图谱证据），产出与深调研同形的 content（`fitness.verdict` 取 `"partial"` 为保守缺省 + `role_suggestion="indirect"` + `responsibility` + `findings` 带 citations），经 `record_partial` 落库。
  6. 返回 `{"dispatched": n_deep, "synthesized": n_light, "degraded": bool, "tasks": [task_id...]}`。
- `async def _dispatch_deep_task(self, session, task) -> None`（五步，逐条镜像 analog L153-222）：
  1. 取 `git_url`；缺失 → 直接判失败**不起占位容器**。
  2. `session_id = f"bp-research-{task.id.hex[:12]}-{uuid4().hex[:6]}"`（**必须带 uuid 后缀**，否则 stale 重跑撞 UNIQUE）。
  3. 建 `AgentSession(metadata={"source": _BLUEPRINT_RESEARCH_SOURCE, ...})` + `SubAgentSession(task_type=TaskType.PLAN, node_execution_id=None, last_output={"source": _BLUEPRINT_RESEARCH_SOURCE, "blueprint_session_id": str(session.id), "research_task_id": str(task.id), "repository_id": str(task.repository_id)})`（**回调路由只认 last_output**）。
  4. `metadata = await self._build_dispatch_metadata(session, task)` → `DispatchTask(task_type="plan", ...)` → `with use_call_source(CallSource.BLUEPRINT_REPO_RESEARCH): await get_dispatcher().dispatch(...)`。**dispatch 抛异常时先 `await arevoke_task_tokens(session_id)` 再向外层 except 传**（照 coding.py L2043-2047；避免铸出去的 token 悬空）。
  5. `await research_service.mark_running(task, subagent_session)` + emit `EVENT_BLUEPRINT_REPO_RESEARCH_STARTED`。
- `async def _build_dispatch_metadata(self, session, task) -> dict[str, str]`：以 analog L337-377 的 6 类键为底（`env_FRIDAY_TASK_MODE=explore` + `env_FRIDAY_TASK_TASK_MODE=explore` 双层只读拦截、Claude runtime、git token 各自 try/except 吞掉、`_repo_url` 经 pop 传出），**补齐本相位三键**（照 coding.py L1927-1940 逐字范式）：
  - `base = getattr(settings, "FRIDAY_BASE_URL", "").rstrip("/")`；`base` 非空才注入 `env_FRIDAY_TASK_TOOLS_ENDPOINT = f"{base}/api/tools/execute/"` 与 `env_FRIDAY_TASK_KNOWLEDGE_ENDPOINT = base`（**裸 base 无路径后缀**；**绝不用 runner callback_url**）。
  - `dispatch_user = await self._resolve_dispatch_user(session)`（`@sync_to_async` 取 `session.created_by`，必须是真实 `User` 实例）；非空才 `plaintext = await mint_task_token(dispatch_user, subagent_session_id, _RESEARCH_TIMEOUT)` 并注入 `env_FRIDAY_TASK_USER_TOKEN`；为空则**省略该键降级不挂**（不伪造 actor、不阻塞 dispatch）。
  - **修正 analog 的瑕疵**：`env_FRIDAY_TASK_CLAUDE_BASE_URL` 改为「空值不注入」（analog L354 无条件写入可能为空，本文件不沿用）。
  - mint 必须在 `SubAgentSession` 建行之后（token 的 `session_id` 与 session 一致，终态吊销按此定位）。
- `def _build_prompt(self, session, task, charter: dict | None) -> str`（`@staticmethod` 摘要截断照 analog L290-314）：由**服务端权威 session 状态**构造（需求目标 + 该仓的 feature_point 子集 + 路由证据摘要 + `## 仓库章程`（positioning / owned_domains / boundaries / evolution，来自 `charters_loader`）），并写死输出要求：返回 JSON 含 `fitness{verdict ∈ suitable|partial|unsuitable, reasons, citations}` / `role_suggestion ∈ direct|indirect` / `responsibility` / `findings[]`（每条带 citations），「判不出填 partial 并说明缺什么，不要猜」。**绝不把外部用户原文当执行指令拼进 prompt。**
- `async def aupgrade_to_deep(self, session, repository_id: str) -> bool`：人工升级入口——把该仓 task 置回 `PENDING`（经 `research_service` 的公开写路径；若无对应公开方法则新建 task 走 `create_tasks_for_session` 的幂等语义）后重跑 `dispatch`（幂等白名单天然支持）。**不调 `ResearchService.retry_task`**（其 stage 名硬编码 `"research"` 会恒 raise）。
- 观测：`blueprint_repo_research_dispatch_started/completed/failed` 三事件，`category="sampling"`、`component="process_runtime"`、`duration_ms`；容器动作另记 `category="caller"` + `initiated_by_user_id`（观测规范：容器动作必须绑定触发用户）。**明文 token、prompt 正文、git token 一律不进日志与事件 payload。**
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/process_runtime/ -x -q -k "blueprint_research or blueprint_reroute"</automated>
  </verify>
  <acceptance_criteria>
- `rg -n "env_FRIDAY_TASK_TOOLS_ENDPOINT|env_FRIDAY_TASK_KNOWLEDGE_ENDPOINT|env_FRIDAY_TASK_USER_TOKEN" server/services/process_runtime/blueprint_research_adapter.py` 三键全命中。
- `rg -n "FRIDAY_BASE_URL" server/services/process_runtime/blueprint_research_adapter.py` 命中；`rg -n "callback_url" server/services/process_runtime/blueprint_research_adapter.py` 零命中。
- `rg -n "mint_task_token" server/services/process_runtime/blueprint_research_adapter.py` 命中；`rg -n "arevoke_task_tokens" server/services/process_runtime/blueprint_research_adapter.py` 命中 ≥1（仅 dispatch 失败路径，不新增终态钩子）。
- `rg -n "\"source\": _BLUEPRINT_RESEARCH_SOURCE|blueprint_research" server/services/process_runtime/blueprint_research_adapter.py` 命中；`rg -n "plan_research" server/services/process_runtime/blueprint_research_adapter.py` 零命中（不与既有链 source 重叠）。
- `rg -n "retry_task" server/services/process_runtime/blueprint_research_adapter.py` 零命中（不复用 stage 名硬编码的重试）。
- `rg -n "import.*research_adapter|from services.process_runtime.research_adapter" server/services/process_runtime/blueprint_research_adapter.py` 零命中（不 import 冻结模块）。
- `rg -n "use_call_source\(CallSource.BLUEPRINT_REPO_RESEARCH\)" server/services/process_runtime/blueprint_research_adapter.py` 命中。
- `git diff --stat server/services/process_runtime/research_adapter.py` 输出为空。
  </acceptance_criteria>
  <done>按仓 fan-out 起 PLAN 容器、三个 env 键接通、章程随 prompt 注入、单仓失败隔离、无 runner 降级轻量、人工升级深调研入口就位；明文 token 零泄漏面。</done>
</task>

<task type="auto">
  <name>Task 2: callbacks.py 加第三条 PLAN 链（fitness 落 PartialPlan.content）</name>
  <files>server/subagent/api/callbacks.py, server/tests/subagent/test_blueprint_research_callback.py</files>
  <read_first>server/subagent/api/callbacks.py L925-1060（_handle_completed / _handle_failed 的 arevoke 位置与既有两处分支挂载 try/except 形状）、L1267-1280（last_output.source → 容器 CallSource 映射）、L1699-1828（plan_research 三件套 + parse_partial_plan_content 失败即 mark_failed + barrier）、L1830-1930（repo_verify 同构分支 + parse_verify_verdict 解析范式）；server/delivery/services/research_service.py L101-125（record_partial 已含 mark_done）；server/tests/subagent/test_repo_verify_callback.py 全文（同构新链测试范式）</read_first>
  <action>
在 `server/subagent/api/callbacks.py` 加**与 repo_verify 分支完全对称**的第三条链（既有 `plan_research` / `repo_verify` 分支与 `arevoke_task_tokens` 调用位置一字不动）：

① 三件套（放在 `repo_verify` 分支之后，保持文件内「一链一段」的排布）：
- `def _is_blueprint_research(session) -> bool`：`task_type == SubAgentSession.TaskType.PLAN` **且** `(session.last_output or {}).get("source") == "blueprint_research"`（双条件，与 `plan_research` 判定互斥）。
- `async def _aload_blueprint_research_task(session)`：由 `last_output["research_task_id"]` → `RepoResearchTask.objects.filter(id=...).afirst()`；缺 id / 已终态（done/failed）→ 返 `None` 使调用方 no-op（幂等）。
- `def _parse_blueprint_fitness(output) -> dict | None`：照 `parse_verify_verdict` 范式——优先结构化透传（`output` 已含 `fitness`），否则从 `output["text"]` 提 ```json 围栏 / 裸 JSON；归一化白名单 `fitness{verdict ∈ suitable|partial|unsuitable, reasons, citations}` + `role_suggestion ∈ direct|indirect`（非法回落 `direct` 保守值——要改动的仓不能被误判成不改动）+ `responsibility`（截断）+ `findings[]`（每项带 citations，条数上界）；**缺 `fitness.verdict` 键即视为不可解析返回 `None`**。
- `async def _handle_blueprint_research_completion(session) -> None`：`task = await _aload_blueprint_research_task(session)`，None → return；`content = _parse_blueprint_fitness(session.last_output/结果)`；None → `await ResearchService().mark_failed(task, {"reason": "empty_or_unparseable_result"})` + emit failed；否则 `content` 与既有 §7 键平级合并后 `await ResearchService().record_partial(task, content)`（**已含 mark_done，不再调 mark_done**）+ emit `EVENT_BLUEPRINT_REPO_RESEARCH_COMPLETED`；末尾触发 barrier（幂等）。
- `async def _handle_blueprint_research_failure(session) -> None`：同构，`mark_failed` + emit `EVENT_BLUEPRINT_REPO_RESEARCH_FAILED` + barrier。
**回调内零裸 ORM 写**（全部经 `ResearchService`，INV-6）。

② 两个挂载点：在 `_handle_completed` / `_handle_failed` 内，紧随既有 `repo_verify` 分支之后各加一段
```
if _is_blueprint_research(session):
    try:
        await _handle_blueprint_research_completion(session)   # 或 _failure
    except Exception:  # 永不阻塞主流程
        logger.warning(...)
```
（注释逐字沿用既有「永不阻塞主流程」表述；`arevoke_task_tokens` 在 task_type 分支之前已无条件执行，**不新增吊销调用**。）

③ `last_output.source → CallSource` 映射处（L1267-1280）加一条 `"blueprint_research" → CallSource.BLUEPRINT_REPO_RESEARCH`，否则该链容器内 LLM 调用来源回退成 `sdk_agent_task`（违反观测规范的显式 call_source 要求）。

新建 `server/tests/subagent/test_blueprint_research_callback.py`（照 `test_repo_verify_callback.py` 范式，`@pytest.mark.django_db(transaction=True)` + `@pytest.mark.asyncio`）：
- 路由判定：`last_output.source == "blueprint_research"` + PLAN → `_is_blueprint_research` True；`source == "plan_research"` → False（**互斥断言：既有 plan_research 链不被抢走**，反向也断言 `_is_plan_research` 对 blueprint 链返回 False）。
- 成功路径：结构化 output 带 fitness → 重读 DB 断言 `PartialPlan.content["fitness"]["verdict"]`、`role_suggestion`、`responsibility`、`findings` 就位，且 `RepoResearchTask.status == done`。
- 文本路径：output 只有 ```json 围栏文本 → 同样解析成功。
- 不可解析：缺 `fitness.verdict` → `status == failed` 且 `error["reason"] == "empty_or_unparseable_result"`，且**不产生** PartialPlan 行。
- 非法枚举：`verdict="great"` / `role_suggestion="maybe"` → 前者判不可解析、后者回落 `direct`。
- 幂等：已 `done` 的 task 再回调 → `_aload_blueprint_research_task` 返 None，PartialPlan 行数不变。
- 失败回调：`_handle_blueprint_research_failure` → `status == failed` + 事件 emit。
- CallSource 映射：`last_output.source == "blueprint_research"` 经映射函数得 `CallSource.BLUEPRINT_REPO_RESEARCH`（不再回退 `sdk_agent_task`）。
- 既有链回归：`cd server && uv run pytest tests/subagent/ -q` 全绿（plan_research 与 repo_verify 行为未变）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/subagent/ -x -q</automated>
  </verify>
  <acceptance_criteria>
- `cd server && uv run pytest tests/subagent/ -x -q` 全绿（含既有 repo_summary / repo_verify / cross_repo_relevance 测试）。
- `rg -c "_is_blueprint_research" server/subagent/api/callbacks.py` ≥ 3（定义 + 两个挂载点）。
- `rg -n "BLUEPRINT_REPO_RESEARCH" server/subagent/api/callbacks.py` 命中（CallSource 映射已补）。
- `rg -n "record_partial" server/subagent/api/callbacks.py` 命中；`rg -n "RepoResearchTask.objects.(update|aupdate|filter\(.*\)\.update)" server/subagent/api/callbacks.py` 零命中（回调零裸 ORM 写）。
- `git diff server/subagent/api/callbacks.py | rg "^-" | rg -v "^---"` 输出为空（纯追加，既有分支零删改）。
- `rg -n "empty_or_unparseable_result" server/tests/subagent/test_blueprint_research_callback.py` 命中。
  </acceptance_criteria>
  <done>第三条 PLAN 链的回调三件套与两个挂载点就位且与既有两链互斥；fitness/role/responsibility/findings 经 ResearchService 落 PartialPlan.content；容器 call_source 显式可归因。</done>
</task>

<task type="auto">
  <name>Task 3: reroute 有界循环（stage_state 单点串行计数）与超限升确认门</name>
  <files>server/services/process_runtime/blueprint_research_adapter.py, server/tests/services/process_runtime/test_blueprint_research_stage.py, server/tests/services/process_runtime/test_blueprint_reroute.py</files>
  <read_first>server/delivery/services/convergence_session_service.py L126-237（transition 签名与 stage_state 整字典替换、CAS 前置条件只保护 stage 推进）；server/delivery/models/convergence_session.py L80-151（stage_state 只读视图键：decomposition/routing/recall_context/classification——整段覆盖会清空它们）；server/services/process_runtime/research_adapter.py L316-335（轻量合成，只读参考）；server/delivery/models/research_task.py L89-115（PartialPlan.valid 与索引，读 fitness 按 valid=True 取最新）；.planning/phases/112-1/112-RESEARCH-ROUTING.md Pitfalls P3 与 P5</read_first>
  <action>
在 `blueprint_research_adapter.py` 追加 reroute 判定面（Task 1 的派发面不改）：

- `MAX_REROUTE_ROUNDS = 2`（模块常量，注释引 CONTEXT「上界 ≤2」）。
- `async def acollect_fitness(self, session) -> dict[str, dict]`：按 `RepoResearchTask` 聚合每仓最新有效结论——`PartialPlan.objects.filter(research_task__session=session, valid=True).order_by("-created_at")`，**每 task 只取最新一条**（P5：`record_partial` 每次 `create` 新行，重跑会有多行）；ORM 经 `@sync_to_async`；返回 `{repository_id: {verdict, role_suggestion, responsibility, findings, task_status}}`。
- `def decide_reroute(*, fitness: dict, round_no: int, max_rounds: int = MAX_REROUTE_ROUNDS) -> dict`（**纯函数，可单测**）：返回
  `{"action": "converged" | "reroute" | "escalate", "unsuitable_repository_ids": [...], "next_round": int, "reason": str}`：
  - 无 `unsuitable` → `converged`。
  - 有 `unsuitable` 且 `round_no < max_rounds` → `reroute`（`next_round = round_no + 1`，带被排除仓清单交主 agent 补候选）。
  - 有 `unsuitable` 且 `round_no >= max_rounds` → **`escalate`**（带全部现状升确认门；`reason="reroute_exhausted"`）。**绝不返回 failed 类动作**（CONTEXT「绝不静默失败」）。
- `async def aadvance_reroute(self, session) -> dict`：**唯一递增点，在 fan-out barrier 收敛后的单点串行处调用**（P3 lost-update 缓解）：
  1. `session = await ConvergenceSession.objects.aget(id=session.id)`（**必须重读新实例**，不用早先持有的陈旧对象）。
  2. `state = session.stage_state or {}`；`round_no = (state.get("reroute") or {}).get("count", 0)`。
  3. `fitness = await self.acollect_fitness(session)`；`decision = decide_reroute(fitness=fitness, round_no=round_no)`。
  4. **浅合并整体回写**：`merged = {**state, "reroute": {"count": decision["next_round"], "excluded": decision["unsuitable_repository_ids"], "last_reason": decision["reason"]}, "repo_research_fitness": <精简摘要>}`——注释显式写「stage_state 是整字典替换，只写增量会清空 routing/decomposition 等既有键」；`repo_research_fitness` 只存 verdict/role 等小摘要（STATE.md 上下文纪律：单字段 <2KB，正文由下游按 id 自取）。
  5. 返回 `{"event": "reroute_needed" | "converged" | "exhausted", "stage_state_update": merged, "escalation": {...当 escalate 时带全部现状快照...}}`——**adapter 只返回 dict，实际 transition 由 112-05 注册的 handler 用 `StageOutcome` 承担（engine 纯度）**。
  6. `reroute` 与 `exhausted` 各 emit `EVENT_BLUEPRINT_REROUTE_TRIGGERED`（payload：`round`、`excluded_count`、`action`）。
  7. **计数递增严禁出现在任何回调路径**（Task 2 的回调只标 task 终态 + 触发 barrier）。

新建 `server/tests/services/process_runtime/test_blueprint_reroute.py`（纯函数部分无 DB）：
- `decide_reroute` 三分支：无 unsuitable → converged；`round_no=0/1` + 有 unsuitable → reroute 且 `next_round` 正确；`round_no=2` + 有 unsuitable → **escalate 且 reason="reroute_exhausted"**（断言返回值集合里根本不存在 failed 类动作）。
- 混合结论：`suitable + partial + unsuitable` 共存时只把 unsuitable 计入排除清单。
- DB 部分（`django_db(transaction=True)`）：`acollect_fitness` 在一 task 多 PartialPlan 行时只取 `valid=True` 的最新一条；`valid=False` 行被忽略。
- `aadvance_reroute` 的 **stage_state 浅合并断言**（P3 核心）：预置 `stage_state = {"decomposition": {...}, "routing": {...}}` → 调用后返回的 `stage_state_update` **仍含** `decomposition` 与 `routing` 原值，且新增 `reroute.count`。
- `escalate` 时返回的 `escalation` 快照含每仓 verdict/role/responsibility（确认门要展示的全部现状）。

新建 `server/tests/services/process_runtime/test_blueprint_research_stage.py`（`django_db(transaction=True)` + `asyncio`；`get_dispatcher` patch 成 `AsyncMock`，`session.created_by` 建真 User）：
- deep 派发：断言 `DispatchTask.metadata` 含 `env_FRIDAY_TASK_KNOWLEDGE_ENDPOINT` 与 `env_FRIDAY_TASK_USER_TOKEN`（断言口径照 `tests/test_task_token_lifecycle.py` 与 `tests/test_remote_tool_dispatch.py`），且 `AccessToken` 表新增一行 `kind="task", session_id=<subagent session_id>`；**明文 token 不等于 DB 中任何存储值**（DB 只有 sha256）。
- 无 `created_by`：`env_FRIDAY_TASK_USER_TOKEN` 键**不存在**（空值不注入）且 dispatch 仍成功。
- `FRIDAY_BASE_URL` 为空（`override_settings`）：两个 endpoint 键均不注入且不抛。
- dispatch 抛异常：task 标 `failed`、其余仓继续派发（单仓隔离）、且该 session_id 的 token 已被 revoke（`revoked_at` 非空）。
- 幂等：已 `running`/`done` 的 task 再 `dispatch` → dispatcher `await_count` 不增。
- 无 online runner：deep 桶整体降级 light，`record_partial` 产出 `fitness.verdict == "partial"` 且 dispatcher 未被调用。
- 章程注入：prompt 文本含该仓 `positioning` 与 `owned_domains` 关键字（章程随 prompt 注入的证据）。
- 明文 token 零泄漏：断言 structlog 捕获与 `ConvergenceSessionEvent.payload` 中不含 `friday_pat_` 前缀字符串。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/process_runtime/test_blueprint_reroute.py tests/services/process_runtime/test_blueprint_research_stage.py -x -q</automated>
  </verify>
  <acceptance_criteria>
- `cd server && uv run pytest tests/services/process_runtime/test_blueprint_reroute.py tests/services/process_runtime/test_blueprint_research_stage.py -x -q` 全绿。
- `rg -n "MAX_REROUTE_ROUNDS = 2" server/services/process_runtime/blueprint_research_adapter.py` 命中。
- `rg -n "reroute_exhausted" server/services/process_runtime/blueprint_research_adapter.py` 命中；`rg -n "STAGE_FAILED|\"failed\"" server/services/process_runtime/blueprint_research_adapter.py | rg -v "mark_failed|RepoResearchTaskStatus.FAILED|task_status"` 零命中（不收敛不落 failed）。
- `rg -n "\{\*\*state" server/services/process_runtime/blueprint_research_adapter.py` 命中（stage_state 浅合并整体回写）。
- `rg -n "friday_pat_" server/services/process_runtime/blueprint_research_adapter.py` 零命中（明文 token 无字面留痕）。
- `rg -n "valid=True" server/services/process_runtime/blueprint_research_adapter.py` 命中（读 fitness 过滤有效行）。
- `cd server && uv run pytest tests/services/ tests/delivery/ tests/subagent/ -q` 全绿（既有链路零扰动）。
  </acceptance_criteria>
  <done>reroute 上界 2 有纯函数判定与三分支断言；计数只在 barrier 后单点串行递增且浅合并不丢既有 stage_state 键；不收敛升确认门带全部现状，绝不静默失败。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 容器回调 → PartialPlan.content | 容器内 AI 产出的半可信 JSON 进入蓝图投影数据 |
| server → 容器（env metadata） | 明文任务 token 与 endpoint 跨进程传递 |
| 容器 → server（任务 token 鉴权） | 容器凭 PAT 回调读知识/工具接口 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-112-15 | Info Disclosure | 明文 task token | mitigate | 明文只进 dispatch metadata；DB 只存 `hash_token`；日志/事件 payload 只记 `session_id`/`user_id`/`expires_in_seconds`（连指纹都不记）；验收含 `friday_pat_` 反向断言 |
| T-112-16 | Elevation of Privilege | token 生命周期超期存活 | mitigate | `mint_task_token` 带 `timeout_seconds + 10min` 余量；终态经既有 `arevoke_task_tokens` 无条件吊销；dispatch 失败路径主动 arevoke。**残余风险**：`amark_timeout`/`amark_cancelled` 不吊销（callbacks.py:942-944 已声明），靠 `expires_at` 自过期兜底——本相位接受（改 callbacks 终态钩子超出「加一条新链」的允许改动形状） |
| T-112-17 | Spoofing | 伪造 actor 获取 token | mitigate | `dispatch_user` 只取 `session.created_by` 真实 User 实例；为空则不注入 token 键，绝不伪造 system 用户提权 |
| T-112-18 | Tampering | 容器返回伪造 fitness / 编造 citations | mitigate | `_parse_blueprint_fitness` 白名单 + 枚举校验 + 缺 verdict 即判不可解析 → mark_failed；`role_suggestion` 非法回落保守 `direct`；findings 条数上界 |
| T-112-19 | Denial of Service | 无界 reroute 循环烧容器额度 | mitigate | `MAX_REROUTE_ROUNDS = 2` 纯函数判定 + 计数持久化 stage_state；超限升确认门交人裁决 |
| T-112-20 | Tampering | 并发回调 lost-update 清空 stage_state | mitigate | 计数只在 barrier 后单点串行递增；每次 `{**state, ...}` 浅合并整体回写且 session 为刚读新实例；验收含「浅合并不丢 routing/decomposition」断言 |
| T-112-21 | Info Disclosure | 上游/容器异常文本含凭证 | mitigate | 所有异常经 `redact_secrets_in_text` 后入日志；git token 解析失败静默吞掉不记正文 |
| T-112-SC | Tampering | 依赖供应链 | accept | 零新增外部依赖 |
</threat_model>

<verification>
- `cd server && uv run pytest tests/services/process_runtime/ tests/subagent/ tests/delivery/ -q` 全绿。
- `cd server && uv run pytest tests/test_task_token_lifecycle.py -q` 全绿（既有 token 生命周期未被新链打破）。
- `cd server && uv run python manage.py makemigrations --check --dry-run` 退出码 0（零模型改动）。
- 冻结面自检：`git diff --name-only` 不含 `repo_router_v2.py`、`process_runtime/{decompose_segments,research_adapter,architect_merge_adapter,merged_plan,clarify_adapter,render,resume,builtin_processes}.py`、`research_service.py`、`access_tokens/services.py`、`workflows/nodes/ai/coding.py`、`event_taxonomy.py`。
- 受限面自检：`git diff server/subagent/api/callbacks.py | rg "^-" | rg -v "^---"` 输出为空（callbacks.py 纯追加）。
- 并行自检：`git diff --name-only` 不含 `blueprint_spec_gate.py` / `blueprint_route.py` / `blueprint_lifecycle_service.py`。
</verification>

<success_criteria>
- FLOW-02：逐仓容器调研产出 fitness 判定（suitable/partial/unsuitable + 理由 + citations）；unsuitable 仓自动回主 agent 重路由，有界 ≤2 轮；仍不收敛升确认门由用户裁决；全过程有事件可见。
- FLOW-04：direct/indirect 区分落 `role_suggestion` 并各带结构化理由与证据；indirect 默认轻量调研且提供人工升级深调研入口。
- ROADMAP SC3：PLAN 链接通任务 token 与容器知识 endpoint（三个 env 键），容器回传 fitness + role 建议 + 职责描述 + 带 citations 的现状 findings。
</success_criteria>

<output>
完成后创建 `.planning/phases/112-1/112-04-SUMMARY.md`（provides：`BlueprintResearchAdapter.dispatch/aadvance_reroute/aupgrade_to_deep` 返回契约、`decide_reroute` 三分支、`PartialPlan.content` 的 fitness 形状、`stage_state` 的 `reroute`/`repo_research_fitness` 键、callbacks 的 `blueprint_research` source 约定——112-05 的确认门直接消费 `escalation` 快照与 fitness 聚合）。
</output>
