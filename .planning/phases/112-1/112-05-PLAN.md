---
phase: 112-1
plan: 05
type: execute
wave: 4
depends_on: [112-04]
files_modified:
  - server/services/process_runtime/blueprint_confirm_gate.py
  - server/delivery/services/blueprint_lifecycle_service.py
  - server/repositories/services/charter_draft_writeback.py
  - server/delivery/api/blueprint_gate_views.py
  - server/delivery/api/artifact_serializers.py
  - server/delivery/urls.py
  - server/services/process_runtime/builtin_processes.py
  - server/services/process_runtime/blueprint_resume.py
  - server/tests/services/process_runtime/test_blueprint_confirm_gate.py
  - server/tests/delivery/test_blueprint_gate_api.py
  - server/tests/repositories/test_charter_draft_writeback.py
  - server/tests/services/process_runtime/test_blueprint_process_graph.py
autonomous: true
requirements: [FLOW-03, CHARTER-03]
prohibitions:
  - "禁止修改 server/codegraph/services/repo_router_v2.py（STATE.md 并行纪律 1）"
  - "禁止修改 process_runtime 六冻结文件（decompose_segments.py / research_adapter.py / architect_merge_adapter.py / merged_plan.py / clarify_adapter.py / render.py）"
  - "禁止修改 server/services/process_runtime/resume.py：蓝图续驱一律走新建的 blueprint_resume.py，旧 technical_plan process 零感知"
  - "builtin_processes.py 唯一允许的改动形状：新增 7 个 _h_bp_* handler + 一个 _TECHNICAL_BLUEPRINT_STAGES 字典 + 第三次 register_process_type 调用 + 同步模块 docstring 的 process 计数；_TECHNICAL_PLAN_STAGES（:207-252）与既有 _h_* 函数、_ECHO_STAGES、既有两次注册调用一字不动"
  - "禁止修改 ConvergenceSessionEvent 既有事件类型/字段；只消费 112-01 注册的 blueprint.confirmation.* 常量"
  - "禁止修改 BlueprintLifecycleService 既有 transition / add_reviewer / _ALLOWED_TRANSITIONS 与 112-02 新增的四个线程方法行为（只追加确认门动作方法）"
  - "**禁止修改 server/repositories/services/charter_service.py 任何一行**（W2）：本 plan 只 import 并调用其现有公开 API（`normalize_charter_draft`），章程草案回灌的写入函数放在新文件 charter_draft_writeback.py；验收用 `git diff --stat server/repositories/services/charter_service.py` 必为空的机械断言"
  - "禁止绕过 charter_draft_writeback 直接 RepoCharter.objects.update()：会绕过 human_confirmed 保护（INV-6 + CHARTER-01 不变量）"
  - "章程回灌一律产 source=ai_draft，人工 confirm 才生效——禁止任何自动改写已确认章程正式字段的路径"
  - "禁止修改 settings_service / system/models.py / event_taxonomy.py / blueprint_schema.py（112-01 已定型）与 blueprint_route.py / blueprint_research_adapter.py / blueprint_spec_gate.py（前序 plan 独占）"
  - "禁止在视图里做任何 ORM 写：五个动作全部委托 BlueprintLifecycleService / charter_service；add_repo 等触发重调研的动作只置 task 为 PENDING + 推进 stage，不在视图里同步等容器"
  - "reroute 超限的 exhausted 事件必须指向 repo_confirmation，禁止指向 STAGE_FAILED"
must_haves:
  truths:
    - "确认门以 BlueprintThread(kind=repo_confirmation, blocking=True) 承载，options 存结构化仓库清单快照（每仓 role 建议/职责/fitness 结论/现状摘要/证据引用）（FLOW-03）"
    - "五个动作 confirm / remove_repo / add_repo / reclassify_role / edit_responsibility 经 REST 可用，全部经 BlueprintLifecycleService 收口，视图零 ORM 写（INV-6）"
    - "第七个端点 blueprint-gate/upgrade-research/ 让 indirect 候选经 REST 升级为深调研（经 BlueprintLifecycleService 收口后调 112-04 的 aupgrade_to_deep）——CONTEXT 明令走 REST 动作（FLOW-04）"
    - "五动作驱动重调研的链路闭合（SC-4）：add_repo / reclassify_role(indirect→direct) 置对应 RepoResearchTask 为可派发态并在快照打 pending_research → repo_confirmation 的 handler 返回 event=research_required → stage 回到 repo_research → dispatch 只为待调研仓起容器；remove_repo 不触发重调研，edit_responsibility 仅在显式 rerun=true 时触发"
    - "confirm 后仓库集与职责写入蓝图 repo_associations（confirmed_at_gate=true、decided_by=human、responsibility 落字段）并记 decision_log；新蓝图版本通过 schema 校验"
    - "确认/改判 role 产 owned_domains 草案、remove_repo 产 boundaries 草案，一律经 charter_service 落 source=ai_draft；对 human_confirmed 章程只写 draft_content（CHARTER-03）"
    - "RepoAssociation.status=rejected 的候选可经一键 API 沉淀为章程禁区候选（boundaries 草案）"
    - "确认动作执行者自动进 BlueprintReviewer 名单（复用 111 upsert 逻辑）"
    - "technical_blueprint process 已注册：intake→decompose→spec_gate→route→repo_research→reroute→repo_confirmation 七 stage，三个 pausable 的 wait_status ∈ {waiting_clarification, waiting_event}，reroute 的 exhausted 指向 repo_confirmation 而非 __failed__，repo_confirmation 有回 repo_research 的 research_required 边"
    - "_h_bp_route 以 StageOutcome(event=\"routed\", stage_state_update={\"routing\": <112-03 契约摘要>}) 写入 stage_state[\"routing\"]，该键是 112-04 dispatch 与确认门快照的唯一读取面（process graph 测试断言该键存在）"
    - "blueprint_resume 的 pause 判据查 open+blocking BlueprintThread（ai_clarification + repo_confirmation 两类），旧 technical_plan process 行为零变化"
  artifacts:
    - path: "server/services/process_runtime/blueprint_confirm_gate.py"
      provides: "确认门 adapter：快照组装 + 五动作语义 + 锁定写入 + 章程草案聚合"
      contains: "confirmed_at_gate"
    - path: "server/delivery/api/blueprint_gate_views.py"
      provides: "1 个只读快照 GET + 7 个动作端点（五动作 + rejected 一键沉淀 + upgrade-research），共 8 个 adrf APIView"
      contains: "IsAuthenticated"
    - path: "server/services/process_runtime/builtin_processes.py"
      provides: "_TECHNICAL_BLUEPRINT_STAGES 七 stage + 第三次 register_process_type"
      contains: "_TECHNICAL_BLUEPRINT_STAGES"
    - path: "server/services/process_runtime/blueprint_resume.py"
      provides: "蓝图专用 drive helper：pause 判据查 open+blocking BlueprintThread"
      contains: "ahas_open_blocking_threads"
    - path: "server/repositories/services/charter_draft_writeback.py"
      provides: "接受 draft dict 的定向草案写入（复用 charter_service.normalize_charter_draft + 同一套三分支落库语义），charter_service.py 零改动"
      contains: "asubmit_charter_draft"
  key_links:
    - from: "server/delivery/api/blueprint_gate_views.py"
      to: "server/delivery/services/blueprint_lifecycle_service.py"
      via: "五动作全部委托 service，视图零 ORM 写（INV-6，源码扫描守护会扫）"
      pattern: "BlueprintLifecycleService"
    - from: "server/services/process_runtime/blueprint_confirm_gate.py"
      to: "server/repositories/services/charter_draft_writeback.py"
      via: "职责聚合 → owned_domains 草案 / 移除仓 → boundaries 草案，产 source=ai_draft（charter_service.py 只被 import 不被改）"
      pattern: "asubmit_charter_draft"
    - from: "server/services/process_runtime/builtin_processes.py"
      to: "server/services/process_runtime/registry.py"
      via: "register_process_type(ProcessDefinition(process_type=\"technical_blueprint\", initial_stage=\"intake\", stages=_TECHNICAL_BLUEPRINT_STAGES))"
      pattern: "register_process_type"
    - from: "server/services/process_runtime/blueprint_resume.py"
      to: "server/delivery/services/blueprint_lifecycle_service.py"
      via: "pause 短路判据 = ahas_open_blocking_threads（112-02 提供），不用 ClarificationService"
      pattern: "ahas_open_blocking_threads"
---

<objective>
关上阶段 1 的出口硬门并把整条流水线注册进编排引擎：
① 确认门以 `BlueprintThread(repo_confirmation, blocking)` 承载结构化仓库清单快照；五个用户动作 + `rejected` 沉淀 + indirect 升级深调研（`upgrade-research`，共 7 个动作端点）经 REST 收口 `BlueprintLifecycleService`；confirm 后仓库集与职责写入 `repo_associations`（`confirmed_at_gate=true` / `decided_by=human` / `responsibility`）并记 `decision_log`；
② 确认/改判 → `owned_domains` 草案、移除仓 → `boundaries` 草案、`rejected` 候选一键沉淀为禁区候选，一律经 `charter_service` 产 `source=ai_draft`（人工 confirm 才生效）；确认者自动进评审人名单；
③ `builtin_processes.py` 新增 `technical_blueprint` 注册项（七 stage 骨架，`exhausted → repo_confirmation` 不落 failed，`repo_confirmation` 有回 `repo_research` 的 `research_required` 边），并新建 `blueprint_resume.py` 以「open+blocking BlueprintThread」为 pause 判据（**不改 `resume.py`**，避免旧 technical_plan 回归）。

## 五动作 → 重调研的闭合链路（B1 / ROADMAP SC-4）

SC-4 要求「用户移除仓、手动加仓（触发新仓调研）、改判 role、修改职责均驱动对应重调研」。本 plan 必须把这条链**首尾接通**，缺任一环 SC-4 即不可能为真：

| 动作 | 是否重调研 | 确定规则与实现动作 |
|---|---|---|
| `add_repo` | **是** | 新仓无任何 fitness → `apply_gate_action` 经 `ResearchService.create_tasks_for_session(session, [{"repository_id": ..., "routed_confidence": "low"}])`（幂等 get_or_create，落 `PENDING`）+ 快照该仓 `pending_research=True` |
| `remove_repo` | **否** | 仅收窄仓库集，既有结论不失效；只改快照 + `record_answer` 留痕（并产 boundaries 草案） |
| `reclassify_role` | **仅 indirect→direct 时是** | 深调研结论是轻量合成的超集：`indirect→direct` 需要容器级 fitness → `await ResearchService().mark_stale([task.id])`（`STALE` 在 112-04 的 `_DISPATCHABLE_STATUSES` 内）+ 快照 `pending_research=True`；`direct→indirect` 与同角色改判 → 否（已有深调研结论足够） |
| `edit_responsibility` | **仅 payload `rerun is True` 时是** | 职责文本变化是否改变调研范围无法机械判定 → 不猜：默认否（只更新快照），**只有调用方显式传 `rerun=true`（用户在 UI 勾选「重新调研该仓」）才** `mark_stale` + `pending_research=True` |
| `confirm` | 否 | 走锁定路径 |

链路三环（**必须同时存在，否则动作只改快照就断了**）：
1. **写标记 + 置可派发态**：`apply_gate_action` 按上表处理（见 Task 1 ②）。
2. **出边**：`repo_confirmation` 的 transitions 含 `{"research_required": "repo_research"}`，`_h_bp_repo_confirmation` 检出待调研标记时返回 `event="research_required"`（见 Task 3 ①）。
3. **增量派发**：`_h_bp_repo_research` 复用 112-04 的增量 `dispatch`（候选来源含 `stage_state["confirmation"]` 的 `pending_research` 仓，只派发 `PENDING`/`STALE`，已完成仓不重跑）。

Purpose: FLOW-03 全量 + CHARTER-03 全量 + 编排面接通（RESEARCH 主题 1/2 契约落地）。
Output: 确认门 adapter + lifecycle 动作方法 + charter 定向草案写入 + 6 个 REST 端点 + process 注册 + blueprint_resume + 3 个测试文件。
</objective>

<execution_context>
工作目录：worktree 根。wave 4 唯一 plan（依赖 112-04 的 `escalation` 快照与 fitness 聚合、112-03 的路由证据、112-02 的线程 API）。
本 plan 是 112 相位最后一个 plan：`builtin_processes.py` 的注册项在此落地，`git diff` 后须能一次跑通 `technical_blueprint` 七 stage 骨架。
</execution_context>

<context>
@.planning/phases/112-1/112-CONTEXT.md（锁定：确认门载体与 options 快照内容；五动作经 REST + BlueprintLifecycleService 收口；锁定语义 confirmed_at_gate/decided_by/responsibility + decision_log + 后续变更须重开确认门；章程回灌一律 ai_draft、rejected 一键沉淀 API；确认者自动进 BlueprintReviewer；builtin_processes 仅加注册项；状态映射全程 researching + 派生 needs_clarification 记 return_stage；pause 短路判据定夺为新建 blueprint_resume.py）
@.planning/phases/112-1/112-RESEARCH.md 主题 1（StageDef 五字段语义、pausable 只影响 self-loop、transitions 未登记 event 会 ValueError、终态哨兵 STAGE_DONE/STAGE_FAILED、惰性注册必须写在 builtin_processes.py 顶层、新增 process 的四个确切改动位置、1.5 的 transitions 建议表与「exhausted 不能落 failed」硬约束）与主题 2（advance 契约、stage_state 浅合并与持久化只经 transition、pause 三层协同、四种失败路径、wait_status 只能取两值、resume.py 判据不匹配的硬约束）
@.planning/phases/112-1/112-RESEARCH-ROUTING.md §2.2（**adraft_charter 不接受调用方传入草案内容**——必须新增接受 `draft: dict` 的写入函数，复用 `normalize_charter_draft` + 同一套三分支落库；禁止裸 update 绕过 human_confirmed 保护）、§2.3（owned_domains / boundaries 的白名单键与截断规则）、P2（transaction.atomic + select_for_update 必须同步函数内 + sync_to_async）
@.planning/phases/112-1/112-PATTERNS.md 第 1 类末段（BlueprintRepoConfirmAdapter 与 spec_gate 同形态：pending 门 → 组装 options 快照 → 建线程 → emit）、第 6 类（七个 _h_bp_* handler + 字典 + 一条注册；handler 只跑 adapter 返回 event 绝不自行 transition；缺 deps 用 getattr 兜底 pass-through；超限转 repo_confirmation 不转 STAGE_FAILED）、第 7 类（charter_views 的一动作一 View + lazy import + serializer .data 必须 sync_to_async + 状态码分层 + body 非 dict 防御 + 白名单归一放 service）、第 9 类（stage 测试四类必测）
@.planning/phases/112-1/112-04-SUMMARY.md（escalation 快照形状、fitness 聚合、stage_state 的 reroute 键、aupgrade_to_deep 入口）
@.planning/phases/112-1/112-02-SUMMARY.md（lifecycle 四个线程方法签名）
@server/services/process_runtime/builtin_processes.py（L1-13 模块 docstring 的 process 计数、L45-204 handler 实例与 getattr 兜底、L207-252 _TECHNICAL_PLAN_STAGES（**冻结**）、L295-301 _ECHO_STAGES、L304-322 两次注册调用——新代码追加在 :301 之后与 :322 之后）
@server/services/process_runtime/registry.py（L26-56 StageDef/ProcessDefinition、L29-30 终态哨兵、L86-97 惰性注册与 register_process_type）
@server/services/process_runtime/resume.py（L23-67 drive helper 的两个短路判据与 max_steps——**只读参考，新建 blueprint_resume.py 照此形状换判据**）
@server/repositories/charter_views.py（REST analog 全文）
@server/repositories/services/charter_service.py（L75-157 normalize_charter_draft、L359-460 adraft_charter/aconfirm_charter 与 L397-431 三分支落库 `_persist`）
@server/delivery/api/artifact_views.py（adrf 视图与 404 中性消息、serializer .data 的 sync_to_async 包裹）
@server/delivery/urls.py（L114-140 artifacts 资源子路由排布）
@server/initiatives/models/repo_association.py（L30-80 Status.VERIFIED/REJECTED 与 routed_reason）
@server/services/process_runtime/blueprint_schema.py（L225-318 repo_associations 全字段：responsibility/fitness/routing_evidence/decided_by/confirmed_at_gate；L728-731 decision_log）
</context>

<tasks>

<task type="auto">
  <name>Task 1: 确认门 adapter + lifecycle 确认动作方法（锁定写入 + decision_log）</name>
  <files>server/services/process_runtime/blueprint_confirm_gate.py, server/delivery/services/blueprint_lifecycle_service.py, server/tests/services/process_runtime/test_blueprint_confirm_gate.py</files>
  <read_first>server/services/process_runtime/blueprint_spec_gate.py（112-02 产出：pending 门 → 组装 → 建线程 → emit 的同形骨架与 add_version 写法，直接复用其结构与 fail-closed 纪律）；server/delivery/services/blueprint_lifecycle_service.py（112-02 追加的四个线程方法 + 既有 transition）；server/services/process_runtime/blueprint_schema.py L225-318 与 L728-731；server/delivery/services/artifact_service.py L117-163；server/services/process_runtime/blueprint_research_adapter.py（112-04 的 acollect_fitness 与 escalation 快照）</read_first>
  <action>
① 新建 `server/services/process_runtime/blueprint_confirm_gate.py`。模块 docstring 四段：「阶段 1 出口硬门，被 112-05 注册的 `technical_blueprint.repo_confirmation` handler 调用」/「INV-6：线程与蓝图版本写入经 BlueprintLifecycleService 与 ArtifactService，本文件零 ORM 写」/「锁定后变更须重开确认门（114 的 AI 审查据此判 BLOCKER）」/「章程回灌只产 ai_draft」。

`class BlueprintConfirmGateAdapter`（依赖 keyword-only 可注入：`lifecycle`、`artifacts`、`fitness_loader`（112-04 的 `acollect_fitness`）、`charter_writer`）：
- `async def open_gate(self, session) -> dict`（与 spec_gate 同形）：
  1. pending 门：`await lifecycle.ahas_open_blocking_threads(artifact, kind=ThreadKind.REPO_CONFIRMATION)` 为 True → 直接返回 `{"event": "awaiting_confirmation", "thread_id": <已有线程>}`（不重复开门）。
  2. 组装 `options` 快照：逐仓 `{repository_id, repository_name, role_suggestion, responsibility, fitness: {verdict, reasons, citations}, current_state_summary, routing_evidence: {total, router_base, charter_match, history_match, router_version, matched_domains, violated_boundaries, history_match_unavailable}}`（来源：112-03 的候选 breakdown + 112-04 的 fitness 聚合 / `escalation` 快照）。快照走「小摘要」纪律：正文类字段截断，明细由 115 按 id 自取。
  3. `await lifecycle.open_thread(artifact, kind=ThreadKind.REPO_CONFIRMATION, blocking=True, question=<中文确认提示>, options=<快照>, initiated_by_user_id=..., return_stage=BlueprintStatus.RESEARCHING)`。
  4. emit `EVENT_BLUEPRINT_CONFIRMATION_OPENED`（payload：`thread_id`/`repo_count`/`unsuitable_count`/`escalated`）。
  5. 返回 `{"event": "awaiting_confirmation", ...}`——**adapter 只返回 dict，stage 转移由 handler 的 StageOutcome 承担**。
- `def build_locked_associations(*, snapshot: list[dict], decisions: dict) -> list[dict]`（**纯函数**）：把快照 + 用户动作结果映射成蓝图 `repo_associations` 条目——写 `role`（direct/indirect）、`responsibility`（block_list 形状）、`fitness`、`routing_evidence`、`decided_by="human"`、`confirmed_at_gate=True`；被 `remove_repo` 的仓**不进** associations（其信息进 boundaries 草案与 `decision_log`）。字段名严格对齐 schema（`blueprint_schema.py:225-318`），不新增 schema 外键。
- `async def alock(self, session, *, acting_user, decisions: dict) -> dict`：
  1. 深拷贝当前 `ArtifactVersion.content` → `content["repo_associations"] = build_locked_associations(...)`；`content["decision_log"]` 追加 `{thread_id, action, repository_id, before, after, decided_at, decided_by}` 逐动作条目（**同 (thread_id, action, repository_id) 已存在则跳过**，幂等重跑不堆积）。
  2. `await artifacts.add_version(artifact, content, produced_by_session_id=..., produced_by_ref="blueprint_confirm_gate")`；捕获 `ArtifactContentInvalid` → 记 `blueprint_confirm_gate_invalid_content`（文本经 `redact_secrets_in_text`）并返回 `awaiting_confirmation`（**fail-closed：内容非法不放行，也不落 failed**）。
  3. `await lifecycle.resolve_thread(thread, ...)` 收尾确认门线程；`await lifecycle.add_reviewer(artifact, acting_user, first_action="repo_confirmation")`（确认者自动进名单，复用 111 upsert）。
  4. 章程草案聚合并交给 `charter_writer`（Task 2 的新 charter 函数）：确认/改判 role 的仓 → `owned_domains` 草案条目 `{domain: <职责聚合文本>, status: "implemented" if role==direct else "planned", note, citations}`；被移除的仓 → `boundaries` 草案条目 `{rule: <为何不该放这里>, decided_by: "human", citations}`。**逐仓独立 try/except**：单仓草案失败只 warning，不影响锁定（best-effort，绝不反噬）。
  5. emit `EVENT_BLUEPRINT_CONFIRMATION_LOCKED`（payload：`locked_repo_count`/`removed_count`/`decided_by`/`charter_draft_count`）；返回 `{"event": "confirmed", ...}`。

② 在 `BlueprintLifecycleService` **追加**确认门动作方法（既有 `transition`/`add_reviewer`/112-02 四个线程方法逐字不动）：
- `async def apply_gate_action(self, artifact, *, thread, action: str, payload: dict, acting_user, initiated_by_user_id: str) -> dict`：**五动作单点收口**。`action` 必须 ∈ `("confirm", "remove_repo", "add_repo", "reclassify_role", "edit_responsibility")` 否则 `raise ValueError`（白名单归一在 service 层，视图只做透传）。返回值恒为 `{"requires_research": bool, "repository_id": str | None, "thread_id": str, "ready_to_lock": bool}`（形状恒定，调用方无需判分支）。语义**逐条按 objective 的「五动作 → 重调研」确定规则表**实现：
  - `remove_repo` / `reclassify_role` / `edit_responsibility`：把动作结果写进线程 `options` 的对应仓快照（经 `@sync_to_async` + `transaction.atomic` 的私有方法，整段读改写单条线程行，避免半截状态）并 `record_answer` 追加人工消息留痕。`requires_research` 按规则表判定：`remove_repo` 恒 `False`；`reclassify_role` 仅 `indirect→direct` 为 `True`；`edit_responsibility` 仅 `payload.get("rerun") is True` 为 `True`。
  - `add_repo`：校验 `repository_id` 存在 → 写入快照占位（`fitness=None`、`role_suggestion` 取入参）→ 返回 `requires_research=True`。
  - **`requires_research=True` 的动作必须同时做完两件写入（B1：只写快照就断链）**，否则 `research_required` 边永远不会被触发：
    ① 快照该仓写 `pending_research = True`（112-04 的增量 `dispatch` 从 `stage_state["confirmation"]` 读这个标记）；
    ② 让该仓的 `RepoResearchTask` 落到可派发态——`add_repo` 走 `await ResearchService().create_tasks_for_session(session, [{"repository_id": rid, "routed_confidence": payload.get("confidence") or "low"}])`（幂等 `get_or_create`，新建即 `PENDING`）；`reclassify_role(indirect→direct)` 与 `edit_responsibility(rerun=true)` 走 `await ResearchService().mark_stale([task.id])`（`STALE` 已在 112-04 的 `_DISPATCHABLE_STATUSES` 内）。
    两处写入均经 `ResearchService` 公开方法（INV-6，**不裸写 RepoResearchTask**）；**视图不同步等容器**——容器由 `_h_bp_repo_research` 在下一次 advance 派发。
    **标记不需要清除**（无状态回收环节，避免多方写同一键）：`research_required` 的判据是「`pending_research=True` **且**该仓 task 仍在 `PENDING`/`STALE`」（见 Task 3 ①），task 一旦被 `dispatch` 推到 `running` 判据即自然为假，遗留标记不会导致重复回边或死循环。
  - `confirm`：只做前置校验（存在快照、无未决 `ai_clarification` 阻塞线程）并返回 `{"ready_to_lock": True}`，实际锁定由 `BlueprintConfirmGateAdapter.alock` 承担（保持「状态/线程写入在 service、蓝图内容写入经 ArtifactService」的分层）。
  - 每个动作 emit `EVENT_BLUEPRINT_CONFIRMATION_ACTION`（payload：`action`/`repository_id`/`thread_id`）+ structlog `category="caller"`、`component="blueprint_lifecycle"`、`initiated_by_user_id`。
- `async def aupgrade_repo_research(self, artifact, *, repository_id: str, acting_user, initiated_by_user_id: str) -> dict`：**第七个端点（B2）的 service 收口**，与五动作同一追加块、同一纪律。校验确认门线程存在且该仓在 `options` 快照内（否则 `raise ValueError`，视图转 404）→ 快照该仓标 `pending_research=True` + `role_suggestion="direct"` → `record_answer` 留痕 → emit `EVENT_BLUEPRINT_CONFIRMATION_ACTION`（payload `action="upgrade_research"`）→ lazy import 调 112-04 的 `BlueprintResearchAdapter().aupgrade_to_deep(session, repository_id)`（避免 delivery→process_runtime 模块级环）→ 返回 `{"upgraded": bool, "repository_id": ...}`。容器不在本方法内等待，由下一次 advance 的 `_h_bp_repo_research` 增量派发。

③ 新建 `server/tests/services/process_runtime/test_blueprint_confirm_gate.py`（`django_db(transaction=True)` + `asyncio`；fitness_loader/charter_writer 用 `AsyncMock` 注入）：
- `open_gate`：建 1 条 `repo_confirmation` open+blocking 线程，`options` 每仓含 role/responsibility/fitness/routing_evidence 四类键。
- pending 门：已有 open 确认门线程时再 `open_gate` → 线程数不增且 `fitness_loader.await_count == 0`。
- `build_locked_associations` 纯函数：`confirmed_at_gate is True`、`decided_by == "human"`、被 remove 的仓不出现；字段集是 schema 子集（用 `validate_blueprint` 间接验证）。
- `alock`：重读最新 `ArtifactVersion.content` → `repo_associations` 锁定字段就位、`decision_log` 逐动作条目就位、`validate_blueprint(content)` 返回 `(True, [])`；确认门线程变 `resolved`；`BlueprintReviewer` 内含 acting_user。
- decision_log 幂等：连续两次 `alock` → 条数不翻倍。
- 章程草案 best-effort：`charter_writer` 抛异常 → 锁定仍成功（`repo_associations` 已写入）且只有 warning。
- `apply_gate_action` 白名单：非法 action → `ValueError`；`add_repo` 返回 `requires_research=True` 且**未**调用 dispatcher（视图不同步等容器）。
- **五动作重调研规则表逐行断言（B1 / SC-4）**：`add_repo` → `requires_research is True` 且**该仓新建了 `RepoResearchTask` 且 `status == "pending"`** 且快照 `pending_research is True`；`remove_repo` → `requires_research is False` 且无 task 状态变化；`reclassify_role` indirect→direct → `True` 且该仓 task 变 `stale`，direct→indirect → `False` 且 task 状态不变；`edit_responsibility` 默认 → `False`，带 `payload={"rerun": True}` → `True` 且 task 变 `stale`。
- fail-closed：`artifacts.add_version` 抛 `ArtifactContentInvalid` → 返回 `awaiting_confirmation`，异常不上抛、蓝图不产生非法版本。
- 存在未决 `ai_clarification` 阻塞线程时 `confirm` 前置校验被拒（与 111 的 LIFE-02 语义一致）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/process_runtime/test_blueprint_confirm_gate.py tests/delivery/test_blueprint_lifecycle_service.py tests/delivery/test_blueprint_thread_service.py -x -q</automated>
  </verify>
  <acceptance_criteria>
- 上述 verify 全绿。
- `rg -n "confirmed_at_gate" server/services/process_runtime/blueprint_confirm_gate.py` 命中；`rg -n '"decided_by": "human"|decided_by=\"human\"' server/services/process_runtime/blueprint_confirm_gate.py` 命中。
- `rg -n "async def apply_gate_action" server/delivery/services/blueprint_lifecycle_service.py` 命中；`rg -c "confirm|remove_repo|add_repo|reclassify_role|edit_responsibility" server/delivery/services/blueprint_lifecycle_service.py` ≥ 5。
- `rg -n "\.objects\.(create|acreate|update|aupdate)" server/services/process_runtime/blueprint_confirm_gate.py` 零命中（adapter 零 ORM 写）。
- `rg -n "add_reviewer" server/services/process_runtime/blueprint_confirm_gate.py` 命中（确认者自动进名单）。
- `git diff server/delivery/services/blueprint_lifecycle_service.py | rg "^-" | rg -v "^---"` 输出为空（纯追加）。
  </acceptance_criteria>
  <done>确认门开得出、五动作在 service 层单点收口、confirm 后仓库集与职责锁定进蓝图并通过 schema 校验、decision_log 幂等留痕、确认者自动进评审人名单；内容非法与章程草案失败两条路径都不反噬。</done>
</task>

<task type="auto">
  <name>Task 2: charter 定向草案写入（新文件）+ 七个动作端点（五动作 + rejected 沉淀 + upgrade-research）</name>
  <files>server/repositories/services/charter_draft_writeback.py, server/delivery/api/blueprint_gate_views.py, server/delivery/api/artifact_serializers.py, server/delivery/urls.py, server/tests/delivery/test_blueprint_gate_api.py, server/tests/repositories/test_charter_draft_writeback.py</files>
  <read_first>server/repositories/services/charter_service.py 全文（L75-157 normalize_charter_draft 白名单、L359-460 adraft_charter/aconfirm_charter、L397-431 三分支落库 `_persist` —— 新函数必须复用同一套语义）；server/repositories/charter_views.py 全文（一动作一 View / lazy import / sync_to_async 包 .data / 404-503 分层 / body 非 dict 防御）；server/delivery/api/artifact_views.py 与 artifact_serializers.py（delivery 侧视图与序列化器组织）；server/delivery/urls.py L110-145（artifacts 资源子路由排布与 kebab-case name 风格）；server/initiatives/models/repo_association.py L30-80（Status.REJECTED / routed_reason）；server/tests/repositories/test_charter_api.py（API 测试惯例）</read_first>
  <action>
① **新建** `server/repositories/services/charter_draft_writeback.py`（**W2：`charter_service.py` 本 plan 一行不改，只 import 其现有公开 API**——`charter_service._persist` 是 `adraft_charter` 内部的闭包函数（`charter_service.py:420` 缩进层级可证），无法 import，所以「抽取内核」会不可避免地改动 `charter_service.py`；改为新文件承载写入、复用其公开归一函数，收益相同且冻结面机械可验证）。模块 docstring 三段：「本模块是确认门章程回灌的唯一写入面」/「归一逐字复用 `charter_service.normalize_charter_draft`，绝不另写一套白名单」/「三分支落库语义与 `adraft_charter` 等价，且对 `human_confirmed` 章程只写 `draft_content`（CHARTER-01 不变量）」。

`async def asubmit_charter_draft(repository_id: str, draft: dict, *, initiated_by_user_id: str = "system", merge: bool = True) -> RepoCharter | None`
- 先 `from repositories.services.charter_service import normalize_charter_draft` 并调用它（复用同一白名单/截断/枚举回退，**绝不另写一套归一**）。
- `merge=True` 时对 `owned_domains` / `boundaries` / `placement_preferences` 做**按 key 去重的追加合并**（`domain` / `rule` / `(kind,target)` 为去重键），标量字段仅在草案非空时覆盖——避免回灌把既有条目冲掉。
- 落库三分支与 `adraft_charter` **行为等价**（无 charter → `create(source=AI_DRAFT, version=1)`；已有且 `source=ai_draft` → 正式字段就地更新且 version 不变；已有 `human_confirmed` → **只写 `draft_content`，正式字段一个不碰**）。实现为本文件内的 `@sync_to_async` + `transaction.atomic` 私有同步函数（`select_for_update` 必须在同步函数内，RESEARCH-ROUTING P2）；等价性由 ③ 的对照测试锁死，不靠「diff 为空」这类不可机械验证的措辞。
- 全程 try/except → 异常经 `redact_secrets_in_text` 后 warning 并返 `None`（best-effort）。structlog `charter_draft_submitted`（`category="caller"`、`component="charter_draft_writeback"`、`repository_id`、`initiated_by_user_id`、`source_before`、`wrote_draft_content: bool`）。
- `__all__ = ["asubmit_charter_draft"]`。

② 新建 `server/delivery/api/blueprint_gate_views.py`（**一动作一 View**，analog 完备，不发明 action 分派）。全部 `from adrf.views import APIView` + `permission_classes = [IsAuthenticated]` + 重依赖方法体内 lazy import + serializer `.data` 一律 `await sync_to_async(lambda: S(obj).data)()`：
- `BlueprintGateSnapshotView.get(request, artifact_id)` —— 读当前 `repo_confirmation` open 线程与其 `options` 快照（读路径允许视图直查）；无门 → 404 中性 `{"detail": "确认门未开启"}`。
- `BlueprintGateConfirmView.post(request, artifact_id)` —— `apply_gate_action(action="confirm")` 前置校验通过后调 `BlueprintConfirmGateAdapter.alock(...)`；成功 200 返回锁定后快照；有未决澄清线程 → 409 `{"detail": "存在未解决的阻塞澄清线程"}`。
- `BlueprintGateRemoveRepoView.post` / `BlueprintGateAddRepoView.post` / `BlueprintGateReclassifyRoleView.post` / `BlueprintGateEditResponsibilityView.post` —— 各自 `body = request.data if isinstance(request.data, dict) else {}`，取 `repository_id` / `role` / `responsibility` / `reason`，全部委托 `BlueprintLifecycleService.apply_gate_action(action=...)`；`ValueError` → 400（非法 action/角色枚举）或 404（artifact/线程/仓不存在，按 analog 分层）；返回 `{"requires_research": bool, ...}`。
- `BlueprintRejectedToBoundaryView.post(request, artifact_id)` —— **rejected 一键沉淀**：取 `RepoAssociation.objects.filter(..., status=Status.REJECTED)` 的候选（读路径），把其 `routed_reason` 组装成 `boundaries` 草案条目后调 `charter_draft_writeback.asubmit_charter_draft(repository_id, {"boundaries": [...]}, initiated_by_user_id=...)`；返 `None`（依赖不可用）→ 503；成功 200 返回草案条数。
- **`BlueprintGateUpgradeResearchView.post(request, artifact_id)` —— 第七个动作端点（B2 / FLOW-04：CONTEXT L42 明令 indirect 升级深调研走 REST 动作，112-04 只交付了 `aupgrade_to_deep` 方法，无入口即半截）**：`body = request.data if isinstance(request.data, dict) else {}` 取 `repository_id`（缺失 → 400）；**经 `BlueprintLifecycleService` 收口**——service 侧新增 `async def aupgrade_repo_research(self, artifact, *, repository_id, acting_user, initiated_by_user_id) -> dict`（Task 1 ② 的同一追加块内）：校验确认门线程存在且该仓在快照内（不在 → `raise ValueError` → 视图 404）、把快照该仓标 `pending_research=True` 与 `role_suggestion="direct"`、`record_answer` 留痕、emit `EVENT_BLUEPRINT_CONFIRMATION_ACTION`（payload `action="upgrade_research"`），再调 112-04 的 `BlueprintResearchAdapter.aupgrade_to_deep(session, repository_id)`（lazy import，避免 delivery→process_runtime 的模块级环）；返回 `{"upgraded": bool, "repository_id": ...}`。`aupgrade_to_deep` 返 `False` → 视图 503（依赖不可用）；成功 200。**视图零 ORM 写，且不同步等容器**——容器由下一次 advance 的 `_h_bp_repo_research` 派发（与 `add_repo` 同一条增量链）。
- 七个动作 View 里**零 ORM 写**（源码扫描守护会扫本文件）；`add_repo` / `upgrade-research` 只置 task 可派发态 + 打 `pending_research` 标记，不同步等容器。
`server/delivery/api/artifact_serializers.py` 追加只读序列化器（快照与锁定结果），`read_only_fields = fields`。
`server/delivery/urls.py` 在 artifacts 资源子路由区追加 8 条 `path("artifacts/<uuid:artifact_id>/blueprint-gate/<动作>/", ...)`（1 个只读快照 + 7 个动作；kebab-case name，如 `blueprint-gate-confirm` / `blueprint-gate-upgrade-research`）。

③ 新建 `server/tests/delivery/test_blueprint_gate_api.py`（`django_db(transaction=True)`；照 `test_charter_api.py` 的 client/鉴权惯例）：
- 未认证 → 401/403（按既有测试实际断言值）。
- `GET` 快照：无门 → 404；有门 → 200 且 body 含每仓 role/responsibility/fitness/routing_evidence。
- `confirm` → 200；重读蓝图最新版本 `repo_associations[*].confirmed_at_gate is True` 且 `decided_by == "human"`；`BlueprintReviewer` 含请求用户。
- 存在未决 `ai_clarification` 阻塞线程时 `confirm` → 409。
- `remove_repo` → 200 且该仓不再出现在后续 confirm 的 associations 中；`boundaries` 草案经 `asubmit_charter_draft` 落 `source=ai_draft`（重读 `RepoCharter`）。
- `add_repo` → 200 且 `requires_research is True`；断言 dispatcher **未被调用**（视图不同步等容器）。
- `reclassify_role` 非法 role（如 `"maybe"`）→ 400；合法 direct↔indirect 改判 → 200 且快照更新。
- `edit_responsibility` → 200 且 confirm 后 `responsibility` 落进 associations。
- `rejected` 一键沉淀 → 200 且 `RepoCharter.boundaries`（或 `draft_content`）出现该规则；对 `source=human_confirmed` 的章程**只写 `draft_content`**（正式字段逐字段与 confirm 后快照相等——CHARTER-01 不变量在回灌路径上仍成立）。
- **`upgrade-research` 端点（B2）**：未认证 → 401/403；缺 `repository_id` → 400；`repository_id` 不在确认门快照内 → 404；正常 → 200 且 `{"upgraded": true}`，重读 DB 断言该仓 task 变 `stale`（或新建 `pending`）、快照 `pending_research is True`、且 **dispatcher 未被调用**（不同步等容器）；`aupgrade_to_deep` 打桩返 `False` → 503。
- 视图零 ORM 写守护：源码扫描断言 `blueprint_gate_views.py` 内无 `repo_associations` / `RepoCharter` / `BlueprintThread` / `RepoResearchTask` 的写操作正则命中。

③b 新建 `server/tests/repositories/test_charter_draft_writeback.py`（`django_db(transaction=True)` + `asyncio`）——**锁死新模块与 `adraft_charter` 的三分支行为等价（W2：等价性靠对照断言，不靠「diff 为空」这种不可机械验证的措辞）**：
- 无 charter → `asubmit_charter_draft` 建行且 `source == "ai_draft"`、`version == 1`。
- 已有 `source=ai_draft` → 正式字段就地更新且 `version` **不变**。
- 已有 `source=human_confirmed` → **只写 `draft_content`**，正式字段（`positioning`/`owned_domains`/`boundaries`/`evolution`）逐字段与写入前**完全相等**（CHARTER-01 不变量）。
- `merge=True` 去重：同 `domain` / 同 `rule` 重复提交不产生重复条目；不同 key 追加而非覆盖；`merge=False` 时按覆盖语义。
- 归一复用：`draft` 内非法 `status` / 超长文本 / 非 list 字段经 `normalize_charter_draft` 回退（与 `tests/repositories/test_charter_service.py` 对同一输入的归一结果**逐字相等**）。
- 依赖失败：`repository_id` 不存在 → 返 `None` 且不抛。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/delivery/test_blueprint_gate_api.py tests/repositories/test_charter_draft_writeback.py tests/repositories/test_charter_service.py tests/repositories/test_charter_api.py -x -q</automated>
  </verify>
  <acceptance_criteria>
- 上述 verify 全绿（新 API 测试 + 新模块三分支等价测试 + 既有 charter 测试零改动通过）。
- `rg -c "blueprint-gate" server/delivery/urls.py` ≥ 8（1 只读快照 + 7 动作端点）；`rg -n "blueprint-gate-upgrade-research" server/delivery/urls.py` 命中（B2 的第七个动作端点已挂路由）。
- `rg -c "IsAuthenticated" server/delivery/api/blueprint_gate_views.py` ≥ 8。
- `rg -n "aupgrade_to_deep" server/delivery/services/blueprint_lifecycle_service.py` 命中；`rg -n "aupgrade_repo_research" server/delivery/api/blueprint_gate_views.py` 命中（REST → service → 112-04 方法的三段链完整）。
- `rg -n "async def asubmit_charter_draft" server/repositories/services/charter_draft_writeback.py` 命中；`rg -n "normalize_charter_draft" server/repositories/services/charter_draft_writeback.py` 命中（复用 `charter_service` 的归一，未另写一套）。
- **W2 冻结断言（机械可验证）**：`git diff --stat server/repositories/services/charter_service.py` 输出为空 —— 本 plan 不修改 `charter_service.py`，只 import 并调用其现有 API。
- `rg -n "RepoCharter.objects.(update|aupdate)" server/repositories/services/charter_draft_writeback.py server/delivery/api/blueprint_gate_views.py` 零命中（不绕过 human_confirmed 保护）。
- `rg -n "(repo_associations|RepoCharter|BlueprintThread|RepoResearchTask)\.objects\.(create|acreate|update|aupdate)" server/delivery/api/blueprint_gate_views.py` 零命中（视图零 ORM 写）。
- `rg -n "sync_to_async" server/delivery/api/blueprint_gate_views.py` 命中（serializer .data 纪律）。
  </acceptance_criteria>
  <done>八个端点（1 只读 + 7 动作，含 B2 的 upgrade-research）鉴权可用且状态码语义分层；五动作、rejected 沉淀与 indirect 升级深调研全部经 service 收口；章程回灌只产 ai_draft 且对 human_confirmed 章程只写 draft_content（不变量与三分支等价性均有测试锁死）；`charter_service.py` 逐字未改。</done>
</task>

<task type="auto">
  <name>Task 3: technical_blueprint process 注册（七 stage）+ blueprint_resume.py 蓝图专用续驱</name>
  <files>server/services/process_runtime/builtin_processes.py, server/services/process_runtime/blueprint_resume.py, server/tests/services/process_runtime/test_blueprint_process_graph.py</files>
  <read_first>server/services/process_runtime/builtin_processes.py 全文（L1-13 docstring 计数、L45-204 handler 的 getattr 兜底与 StageOutcome 四种返回形、L207-252 _TECHNICAL_PLAN_STAGES 冻结快照、L295-322 追加位置）；server/services/process_runtime/registry.py L26-97；server/services/process_runtime/resume.py 全文（drive helper 形状：max_steps 兜底 fail、两个 status 短路、循环 advance + aget 重读——只读，照形换判据）；server/delivery/services/convergence_session_service.py L126-237（transition 与 stage_state 语义）；server/delivery/models/convergence_session.py L30-38（ConvergenceSessionStatus 只有两个挂起值）；server/tests/services/process_runtime/test_classify_stage.py（stage 测试四类必测）</read_first>
  <action>
① `server/services/process_runtime/builtin_processes.py`（**唯一允许的改动形状**）：
- 在 `_ECHO_STAGES` 之后、`# === registration ===` 之前追加七个 handler：`_h_bp_intake` / `_h_bp_decompose` / `_h_bp_spec_gate` / `_h_bp_route` / `_h_bp_repo_research` / `_h_bp_reroute` / `_h_bp_repo_confirmation`。签名恒为 `async def _h_bp_xxx(session: Any, engine: Any) -> StageOutcome`。每个 handler 三动作：软取依赖（`getattr(getattr(engine, "deps", None), "<name>", None)`；**缺依赖直接返回默认 pass-through outcome，不报错**）→ 调 adapter → `return StageOutcome(event=..., stage_state_update=...)`。**绝不在 handler 内调 `session_service.transition`**（engine 纯度）。事件 emit 经 `engine.session_service._emit_event(...)`，`category="sampling"`、`component="process_runtime"`（照既有 handler）。
  - `_h_bp_spec_gate`：`event = "needs_clarification" if adapter 返回需澄清 else "spec_locked"`。
  - **`_h_bp_route`（B4：`stage_state["routing"]` 的唯一写入方）**：调 112-03 的 `BlueprintRouteAdapter.route(session)` → `return StageOutcome(event="routed", stage_state_update={"routing": <route() 返回值原样>})`。该摘要的字段清单**逐字取 112-03-SUMMARY 的契约表**（顶层 `router_version` / `auto_selected` / `intent` / `weights_used` / `charter_supplement_count` / `unjustified_boundary_hit_count` / `candidates` / `citations`；`candidates[]` 每项 `repository_id` / `repository_name` / `role_suggestion` / `confidence` / `total` / `breakdown` / `evidence`），与 112-04 `dispatch` 的读取处**同一份清单**，不得裁剪或改名。adapter 缺依赖时按 handler 通例 pass-through（`stage_state_update=None`，不写半截 `routing` 键）。
  - `_h_bp_repo_research`：调 112-04 的**增量** `dispatch`（候选来源 = `stage_state["routing"].candidates` ∪ `stage_state["confirmation"]` 内 `pending_research` 仓；只派发 `PENDING`/`STALE`，已完成仓不重跑——B1③）。派发后 `event="research_dispatched"`（self-loop 挂起等回调）；全部 task 终态时 `event="research_complete"`。**本 stage 因此既是首轮调研入口，也是确认门动作驱动重调研时的复用入口**。
  - `_h_bp_reroute`：调 112-04 的 `aadvance_reroute` → `event ∈ {"reroute_needed", "converged", "exhausted"}`，`stage_state_update` 用其返回的**整份浅合并结果**。
  - **`_h_bp_repo_confirmation`（B1②：五动作驱动重调研的出边判定）**：先算「待调研集合」= `stage_state["confirmation"]` 内 `pending_research is True` **且**其 `RepoResearchTask.status ∈ {pending, stale}` 的仓（ORM 读经 `@sync_to_async`；两条件合取使标记无需清位、不会死循环）。
    - 待调研集合非空 → `event="research_required"`（**回 `repo_research` 增量派发新增/待重调研仓**）。
    - 否则未确认 → `event="awaiting_confirmation"`（self-loop 挂起）；已确认 → `event="confirmed"`。
    判定顺序固定为「先 research_required 再 awaiting_confirmation」——否则 add_repo 后会被 self-loop 挂起而永远到不了调研（SC-4 断链）。
- 追加 `_TECHNICAL_BLUEPRINT_STAGES: dict[str, StageDef]`（**不得插进 `_TECHNICAL_PLAN_STAGES` 内部**），逐 stage：
  | stage | transitions | pausable | wait_status |
  |---|---|---|---|
  | `intake` | `{"intaken": "decompose"}` | — | — |
  | `decompose` | `{"decomposed": "spec_gate"}` | — | — |
  | `spec_gate` | `{"spec_locked": "route", "needs_clarification": "spec_gate"}` | `True` | `"waiting_clarification"` |
  | `route` | `{"routed": "repo_research"}` | — | — |
  | `repo_research` | `{"research_dispatched": "repo_research", "research_complete": "reroute"}` | `True` | `"waiting_event"` |
  | `reroute` | `{"reroute_needed": "repo_research", "converged": "repo_confirmation", "exhausted": "repo_confirmation"}` | — | — |
  | `repo_confirmation` | `{"awaiting_confirmation": "repo_confirmation", "research_required": "repo_research", "confirmed": STAGE_DONE}` | `True` | `"waiting_clarification"` |
  每个 `StageDef.key` 必须等于 dict 键。**`repo_confirmation` 必须有 `{"research_required": "repo_research"}` 这条回边（B1①）**——没有它，`add_repo` / `reclassify_role` 驱动的重调研在 stage graph 上无路可走（未登记的 event 会直接 `ValueError`，见 RESEARCH 主题 1），ROADMAP SC-4「用户加仓/改判驱动对应重调研」不可能为真；注释写明该边与 `repo_research → reroute → repo_confirmation` 构成确认门的有界回路，边界由 `MAX_REROUTE_ROUNDS` 与「已完成仓不重派」共同约束。**`exhausted` 指向 `repo_confirmation`，绝不指向 `STAGE_FAILED`**（CONTEXT「绝不静默失败」）；`repo_confirmation.transitions["confirmed"]` 现指 `STAGE_DONE`，注释写明「113 接续只需把该值改为 `"repo_plan"` 并追加两个 StageDef，transitions 是数据无需改 engine」。模块级常量 `MAX_REROUTE_ROUNDS` 复用 112-04 的导入值（避免两处漂移）。
- 文件尾部追加第三次 `register_process_type(ProcessDefinition(process_type="technical_blueprint", artifact_type=<复用 Phase 111 的 blueprint artifact type>, initial_stage="intake", stages=_TECHNICAL_BLUEPRINT_STAGES))`（放在既有 echo 注册之后）。
- 模块 docstring 的「注册两个 ProcessDefinition」改为三个（否则文档与实态背离）。

② 新建 `server/services/process_runtime/blueprint_resume.py`（**不改 `resume.py`**——其判据绑 `delivery.Clarification`，与蓝图线程不匹配；改它会让旧 technical_plan process 回归）。模块 docstring 三段：「蓝图专用 drive helper，形状照 `resume.py` 但换 pause 判据」/「旧 process 零感知：本文件只被 technical_blueprint 链调用」/「INV-6：不写 status，靠 engine + transition」。
`async def adrive_blueprint_session_to_pause_or_terminal(engine, session, *, max_steps: int = 20)`：
- `WAITING_CLARIFICATION` 且 `await BlueprintLifecycleService().ahas_open_blocking_threads(artifact)` 为 True → 直接 return（**判据覆盖 `ai_clarification` 与 `repo_confirmation` 两类**，故不传 `kind`）。
- `WAITING_EVENT` 且 `not await aall_research_tasks_terminal(session.id)` → 直接 return（此判据可复用既有函数，只读 import）。
- 否则循环 `await engine.advance(session)` + `session = await ConvergenceSession.objects.aget(id=session.id)` 重读；步数超 `max_steps` → `transition(session, "fail", error={"reason": "advance_step_limit", "steps": steps})`（与 `resume.py` 同语义）。
- 蓝图状态映射经 `BlueprintLifecycleService.transition`：阶段 0/1 全程 `researching`；出现 open+blocking 线程时派生 `needs_clarification` 并带 `return_status=BlueprintStatus.RESEARCHING`（CONTEXT 锁定；`transition` 已校验合法恢复目标）。

③ 新建 `server/tests/services/process_runtime/test_blueprint_process_graph.py`（模块 docstring 编号列「本文件守哪几件事」，第一条是「既有 technical_plan 链路零扰动」）：
- **冻结快照**：`_TECHNICAL_PLAN_STAGES` 的 7 个 stage key、每个 stage 的 `transitions` dict、`pausable`、`wait_status` 逐字等于预期字面快照（任何改动即红）。
- 注册项存在：`get_process_definition("technical_blueprint")` 非 None，`initial_stage == "intake"`，`set(stages) == {intake, decompose, spec_gate, route, repo_research, reroute, repo_confirmation}`；每个 `StageDef.key == dict 键`。
- transitions 完整性：所有 target ∈ `stages ∪ {STAGE_DONE, STAGE_FAILED}`；**断言 `stages["reroute"].transitions["exhausted"] == "repo_confirmation"` 且 `!= STAGE_FAILED`**（CONTEXT 硬约束）。
- **`research_required` 回边存在（B1①）**：`stages["repo_confirmation"].transitions["research_required"] == "repo_research"`；且断言从 `repo_confirmation` 出发存在到 `repo_research` 的路径（图连通性，防止后续误删该边）。
- **`stage_state["routing"]` 契约断言（B4）**：以 `AsyncMock` 注入 route adapter 返回一份符合 112-03 契约的摘要 → 跑 `_h_bp_route` → 断言 `StageOutcome.event == "routed"` 且 `stage_state_update` **含 `"routing"` 键**；经 `transition` 落盘后重读 DB，`session.stage_state["routing"]` 顶层键集合 ⊇ `{router_version, intent, weights_used, candidates}`，且每个候选含 `{repository_id, role_suggestion, confidence, total, breakdown, evidence}`（112-04 `dispatch` 的读取面不被漏字段打破）；adapter 未注入时 `stage_state_update is None`（不写半截 `routing`）。
- **五动作驱动重调研的端到端机制断言（B1④ / SC-4，本文件最关键的一条）**：预置一个停在 `repo_confirmation` 且已有 open+blocking 确认门线程的 session（`stage_state` 含 `routing` 与已 `done` 的两仓 task）→ 调 `BlueprintLifecycleService.apply_gate_action(action="add_repo", payload={"repository_id": C})` → **跑一次 `engine.advance(session)`** → 重读 DB 断言：① `session.current_stage == "repo_research"`（走的是 `research_required` 边）；② **C 仓有 `RepoResearchTask` 且状态为 `pending` 或 `running`**（新仓调研已建/已派发）；③ A/B 两仓的 task 状态与 PartialPlan 行数**不变**（已完成仓不重跑，B1③）。
- **`reclassify_role` 同链断言**：`indirect→direct` 后一次 advance 同样落到 `repo_research` 且该仓 task 为 `stale`/`running`；`direct→indirect` 后一次 advance **仍停在 `repo_confirmation`**（`awaiting_confirmation` self-loop，不触发无谓重调研）。
- **无待调研仓时不误触发**：所有 `pending_research` 仓的 task 均已 `done` → `_h_bp_repo_confirmation` 返回 `awaiting_confirmation`（判据的合取条件生效，标记遗留不造成死循环）。
- pausable 合法性：三个 pausable stage 的 `wait_status ∈ {"waiting_clarification", "waiting_event"}`；每个 pausable stage 的 transitions 里存在 self-loop（`target == key`）。
- handler 四类必测（照 `test_classify_stage.py`）：deps 未注入 → pass-through 不抛且返回合法 event；`engine.deps` 整体 None → 同样安全穿过；正常路径落 `stage_state` 且 emit 一次事件（按事件名过滤 `_emit_event` spy 断言条数与 payload）；adapter 抛异常 → 经 engine 兜底落 `failed` 且 `error["stage"]` 为该 stage 名。
- `stage_state` 浅合并：`_h_bp_reroute` 返回的 `stage_state_update` 经 `transition` 后 DB 行仍含 `routing`/`decomposition` 既有键。
- `blueprint_resume`：`waiting_clarification` + 存在 open+blocking `repo_confirmation` 线程 → `engine.advance` 的 `AsyncMock.await_count == 0`（短路生效，**这是 `resume.py` 判据做不到的**）；线程 resolve 后再驱动则会 advance；步数超限落 `advance_step_limit`。
- 旧链回归：`get_process_definition("technical_plan")` 的 stages 与快照一致，且 `cd server && uv run pytest tests/services/process_runtime/ -q` 全绿。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/process_runtime/test_blueprint_process_graph.py -x -q</automated>
  </verify>
  <acceptance_criteria>
- `cd server && uv run pytest tests/services/process_runtime/test_blueprint_process_graph.py -x -q` 全绿。
- `cd server && uv run python manage.py shell -c "from services.process_runtime.registry import get_process_definition as g; d = g('technical_blueprint'); assert d is not None; assert d.initial_stage == 'intake'; assert set(d.stages) == {'intake','decompose','spec_gate','route','repo_research','reroute','repo_confirmation'}; assert d.stages['reroute'].transitions['exhausted'] == 'repo_confirmation'; assert d.stages['repo_confirmation'].transitions['research_required'] == 'repo_research'; print('ok')"` 输出 `ok`。
- `rg -n '"research_required": "repo_research"' server/services/process_runtime/builtin_processes.py` 命中（B1① 回边在 stage 表内）；`rg -n "research_required" server/services/process_runtime/builtin_processes.py` 命中 ≥ 2（stage 表 + handler 返回）。
- `rg -n '"routing"' server/services/process_runtime/builtin_processes.py` 命中（B4：`_h_bp_route` 写入该键）。
- `rg -c "^async def _h_bp_" server/services/process_runtime/builtin_processes.py` 等于 7。
- `rg -c "^register_process_type\(" server/services/process_runtime/builtin_processes.py` 等于 3。
- `rg -n "STAGE_FAILED" server/services/process_runtime/builtin_processes.py | rg -c "exhausted"` 等于 0（exhausted 不落 failed）。
- `rg -n "ahas_open_blocking_threads" server/services/process_runtime/blueprint_resume.py` 命中；`rg -n "ClarificationService|delivery.Clarification" server/services/process_runtime/blueprint_resume.py` 零命中。
- `git diff --stat server/services/process_runtime/resume.py` 输出为空（resume.py 逐字未改）。
- `git diff server/services/process_runtime/builtin_processes.py | rg "^-" | rg -v "^---" | rg -v "注册两个"` 输出为空（除 docstring 计数外纯追加）。
- `cd server && uv run pytest tests/services/ tests/delivery/ tests/subagent/ tests/repositories/ -q` 全绿。
  </acceptance_criteria>
  <done>七 stage 骨架注册完成，exhausted 升确认门与 `repo_confirmation → repo_research` 的 `research_required` 回边各有硬断言；`add_repo` 后一次 advance 落到 `repo_research` 并为新仓建 `RepoResearchTask`（SC-4 链路端到端可验证），已完成仓不被重跑；`_h_bp_route` 写入的 `stage_state["routing"]` 字段清单与 112-04 读取面一致且有断言；三个 pausable 的 wait_status 合法且各有 self-loop；旧 technical_plan stage graph 有逐字冻结快照保护；蓝图专用续驱以 BlueprintThread 为 pause 判据且 resume.py 逐字未改。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 八个确认门 REST 端点（1 只读 + 7 动作） | 认证用户输入（artifact_id path + action body）进入服务层与蓝图内容 |
| 用户动作 → RepoCharter 草案 | 人工决策文本回灌到跨蓝图共享的章程数据 |
| stage graph 注册 | 新 process 与既有 technical_plan 共享同一 engine 与 registry |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-112-22 | Elevation of Privilege | 确认门八端点 | mitigate | `IsAuthenticated`（与 repositories/delivery 既有 view 同级，§6.4 项目成员皆可确认的低门槛决策）；`confirm` 绑定 `request.user` 落 `decided_by`/`BlueprintReviewer` 留痕 |
| T-112-23 | Tampering | action / role / responsibility body | mitigate | `apply_gate_action` 的 action 白名单在 service 层 `raise ValueError`；role 走 direct/indirect 枚举；responsibility 截断；`body` 非 dict 按缺省处理；artifact_id 由 Django UUID converter 校验 |
| T-112-24 | Tampering | AI 覆盖人工确认的章程 | mitigate | `charter_draft_writeback.asubmit_charter_draft` 三分支落库与 `adraft_charter` 行为等价（对照测试锁死）：`human_confirmed` 只写 `draft_content`；禁止 `RepoCharter.objects.update()` 旁路（验收含反向 rg 断言 + API 测试锁不变量）；`charter_service.py` 逐字未改（`git diff --stat` 断言） |
| T-112-25 | Repudiation | 锁定决策无法追溯 | mitigate | `decision_log` 逐动作记 `{thread_id, action, repository_id, before, after, decided_at, decided_by}`；`confirmed_at_gate`/`decided_by=human` 落蓝图；事件 payload 带 action 与 repository_id；`initiated_by_user_id` 贯穿 |
| T-112-26 | Denial of Service | 确认门反复重开 / advance 死循环 | mitigate | pending 门短路不重复开线程；`blueprint_resume` 的 `max_steps=20` 兜底；pausable self-loop 靠 status 短路而非忙等 |
| T-112-27 | Tampering | 新 process 注册影响旧链 | mitigate | `_TECHNICAL_PLAN_STAGES` 逐字冻结快照测试 + `git diff` 纯追加断言；新 handler 全部 `getattr` 软取依赖，deps 缺失 pass-through 不抛 |
| T-112-28 | Info Disclosure | 快照/事件含需求正文或凭证 | mitigate | options 快照按「小摘要」纪律截断，明细由 115 按 id 自取；事件 payload 只记计数与 id；异常经 `redact_secrets_in_text`；schema 报错出口已截断脱敏 |
| T-112-SC | Tampering | 依赖供应链 | accept | 零新增外部依赖 |
</threat_model>

<verification>
- `cd server && uv run pytest tests/services/ tests/delivery/ tests/repositories/ tests/subagent/ -q` 全绿。
- `cd server && uv run pytest -q` 全套绿（相位收口）。
- `cd server && uv run python manage.py evaluate_blueprint_golden` 退出码 0（112 首次有真实路由/调研数据可评）。
- `cd server && uv run python manage.py makemigrations --check --dry-run` 退出码 0（本 plan 零模型改动）。
- 冻结面自检：`git diff --name-only` 不含 `codegraph/services/repo_router_v2.py`、`process_runtime/{decompose_segments,research_adapter,architect_merge_adapter,merged_plan,clarify_adapter,render,resume}.py`、`system/models.py`、`system/settings_service.py`、`event_taxonomy.py`、`blueprint_schema.py`、**`repositories/services/charter_service.py`（W2：本 plan 只 import 调用，一行不改）**。
- 受限面自检：`builtin_processes.py` 与 `blueprint_lifecycle_service.py` 的 diff 均为纯追加（除 builtin_processes 的 docstring 计数一行）。
</verification>

<success_criteria>
- FLOW-03 / ROADMAP SC-4：阶段 1 出口是硬确认门；可移除仓 / 手动加仓 / 改判 role / 修改职责，且**重调研链路首尾闭合**——动作侧置 task 可派发态 + 打 `pending_research` → `_h_bp_repo_confirmation` 返回 `research_required` → stage 回 `repo_research` → 增量 `dispatch` 只为新增/待重调研仓起容器（已完成仓保留结论）；确认后仓库集与职责锁定（`confirmed_at_gate`/`decided_by=human`/`responsibility`）+ `decision_log` 留痕，后续变更须重开确认门。
- CHARTER-03：确认/改判 → `owned_domains` 草案、移除 → `boundaries` 草案、`rejected` 候选一键沉淀为禁区候选，一律 AI 草案 + 人工 confirm 生效（ROADMAP SC5）；写入面在新模块 `charter_draft_writeback.py`，`charter_service.py` 逐字未改。
- FLOW-04 收口：`blueprint-gate/upgrade-research/` 端点让 indirect 候选经 REST 升级为深调研（CONTEXT 明令的 REST 动作），与 `add_repo` 共用同一条增量派发链。
- 编排面接通：`technical_blueprint` 七 stage 注册可用，`exhausted` 升确认门、`research_required` 回 `repo_research`；蓝图续驱以 `BlueprintThread` 为 pause 判据，旧 `technical_plan` process 零回归。
</success_criteria>

<output>
完成后创建 `.planning/phases/112-1/112-05-SUMMARY.md`（provides：确认门快照与锁定契约、8 个 REST 端点（1 只读 + 7 动作，含 `blueprint-gate/upgrade-research/`）、五动作 → 重调研的确定规则表与 `pending_research` 标记语义、`charter_draft_writeback.asubmit_charter_draft`、`technical_blueprint` stage graph（含 `research_required` 回边）与 113 接续点（把 `repo_confirmation.transitions["confirmed"]` 改为 `"repo_plan"`）、`adrive_blueprint_session_to_pause_or_terminal`）。
</output>
