---
phase: 112-1
plan: 02
type: execute
wave: 2
depends_on: [112-01]
files_modified:
  - server/delivery/services/blueprint_lifecycle_service.py
  - server/services/process_runtime/blueprint_ambiguity_score.py
  - server/services/process_runtime/blueprint_intent_classify.py
  - server/services/process_runtime/blueprint_spec_gate.py
  - server/tests/delivery/test_blueprint_thread_service.py
  - server/tests/services/process_runtime/test_blueprint_ambiguity_score.py
  - server/tests/services/process_runtime/test_blueprint_spec_gate.py
autonomous: true
requirements: [FLOW-01]
prohibitions:
  - "禁止修改 server/codegraph/services/repo_router_v2.py（STATE.md 并行纪律 1）"
  - "禁止修改 process_runtime 六冻结文件（decompose_segments.py / research_adapter.py / architect_merge_adapter.py / merged_plan.py / clarify_adapter.py / render.py）——clarify_adapter.py 与 decompose_segments.py 只读参考其骨架，一行都不改"
  - "禁止修改 server/services/process_runtime/resume.py（蓝图续驱走 112-05 的 blueprint_resume.py）"
  - "禁止 import 或复用 delivery.Clarification / ClarificationService（CONTEXT 明令：澄清载体只用 BlueprintThread）"
  - "禁止修改 ConvergenceSessionEvent 既有事件类型/字段；只消费 112-01 已注册的 blueprint_* 常量，不再改 event_taxonomy.py"
  - "禁止修改 BlueprintLifecycleService 既有 transition / add_reviewer / _apply_transition_sync / _record_transition_event 与 _ALLOWED_TRANSITIONS（只追加线程相关新方法）"
  - "禁止修改 settings_service 既有 getter；只消费 112-01 新增的 aget_json_setting"
  - "禁止修改 builtin_processes.py（stage 注册在 112-05）；禁止改 server/system/models.py（键已在 112-01 注册）"
  - "禁止把阈值/四维权重硬编码进 adapter：模块常量只作 SystemSetting 缺失时的兜底默认"
  - "adapter / LLM helper 零 ORM 写：线程与蓝图版本写入一律经 BlueprintLifecycleService 与 ArtifactService（INV-6）"
must_haves:
  truths:
    - "歧义四维（goal/boundary/constraint/acceptance）由单次 LLM 调用产出分数+理由，call_source=blueprint_spec_gate；加权总分 ≥ 阈值即开澄清、< 阈值即放行（FLOW-01）"
    - "LLM 不可用或响应不可解析时规格门 fail-closed——判定「需澄清」而非放行"
    - "超阈值时开 BlueprintThread(kind=ai_clarification, blocking=True, status=open)，options 携带候选选项与证据引用；已有 open+blocking 线程时短路挂起不重复提问"
    - "作答后规格锁定：requirement_spec 写入新蓝图版本、ambiguity_report.resolved_thread_ids 记已解决线程 id、结论物化进 decision_log；同一问题不再重复问（提问前查 decision_log 与 resolved 线程）"
    - "每个 feature_point 被赋 intent ∈ greenfield|brownfield|fix；分类不可得时取保守值并记事件，绝不写非法枚举"
  artifacts:
    - path: "server/delivery/services/blueprint_lifecycle_service.py"
      provides: "线程写入收口：open_thread / record_answer / resolve_thread / ahas_open_blocking_threads（BlueprintThread 唯一 writer）"
      contains: "async def open_thread"
    - path: "server/services/process_runtime/blueprint_ambiguity_score.py"
      provides: "LLM 四维打分 + normalize 纯函数 + 加权总分/阈值判定纯函数 + SystemSetting loader"
      contains: "BLUEPRINT_SPEC_GATE"
    - path: "server/services/process_runtime/blueprint_intent_classify.py"
      provides: "feature_point 意图分类（greenfield|brownfield|fix）"
      contains: "_VALID_INTENT"
    - path: "server/services/process_runtime/blueprint_spec_gate.py"
      provides: "spec_gate stage adapter：打分→提问→挂起→作答→规格锁定+decision_log"
      contains: "resolved_thread_ids"
  key_links:
    - from: "server/services/process_runtime/blueprint_spec_gate.py"
      to: "server/delivery/services/blueprint_lifecycle_service.py"
      via: "开/解澄清线程一律经 lifecycle service（INV-6），adapter 零 ORM 写"
      pattern: "BlueprintLifecycleService"
    - from: "server/services/process_runtime/blueprint_spec_gate.py"
      to: "server/delivery/services/artifact_service.py"
      via: "规格锁定经 ArtifactService.add_version 落新蓝图版本（requirement_spec + ambiguity_report + decision_log）"
      pattern: "add_version"
    - from: "server/services/process_runtime/blueprint_ambiguity_score.py"
      to: "server/system/settings_service.py"
      via: "aget_json_setting(SettingKeys.BLUEPRINT_SPEC_GATE_CONFIG) 读阈值与四维权重"
      pattern: "BLUEPRINT_SPEC_GATE_CONFIG"
---

<objective>
落地阶段 0 的规格门：需求进调研前先量歧义、超阈值必澄清、作答后锁规格，并给每个 feature_point 打上意图分类。
四维打分与意图分类各为一次 LLM 单调用（`call_source` 已在 111 注册），阈值与权重从 112-01 的 `SystemSetting` 键读取；澄清载体是 `BlueprintThread(ai_clarification, blocking)`，写入全部收口 `BlueprintLifecycleService`（本 plan 为其补齐线程写入方法——111 只交付了 `transition`/`add_reviewer`，尚无任何线程 writer）。

Purpose: FLOW-01 全量（歧义门 + 意图分类）；同时为 112-05 的确认门提供同一套线程写入 API（`repo_confirmation` 复用）。
Output: 3 个新 process_runtime 文件 + lifecycle service 线程方法 + 3 个测试文件。
</objective>

<execution_context>
工作目录：worktree 根。**与 112-03 同 wave 并行**——本 plan 只碰 `blueprint_ambiguity_score.py` / `blueprint_intent_classify.py` / `blueprint_spec_gate.py` / `blueprint_lifecycle_service.py` 四个文件，绝不碰 `blueprint_route.py`；`event_taxonomy.py` / `system/models.py` / `settings_service.py` / `blueprint_schema.py` 已由 112-01 定型，本 plan 只读消费。
</execution_context>

<context>
@.planning/phases/112-1/112-CONTEXT.md（锁定：四维打分单调用 + call_source=blueprint_spec_gate；阈值与权重外置默认 0.20；澄清载体 BlueprintThread(ai_clarification, blocking) 不复用 delivery.Clarification；作答后 requirement_spec 写入 + resolved_thread_ids + decision_log 物化 + 不重复提问；intent 落 feature_points 必填枚举）
@.planning/phases/112-1/112-RESEARCH.md 主题 2.3（pause = handler 返回 self-loop event，adapter 不写 status）与主题 4.4（getter 清单）
@.planning/phases/112-1/112-PATTERNS.md 第 1 类（clarify_adapter 的 HITL 回路 6 步顺序 / 上界兜底放行 / 策略可注入 / 事件 emit 吞异常）、第 2 类（feature_classify 的 LLM 五步骨架 + 三层解析防御 + 反幻觉归一 + 观测三事件 + 「结论失依据就降级」）、第 8 类（loader 全兜底 + 判定纯函数分离）、第 9 类（stage 测试四类必测）
@.planning/phases/112-1/112-01-SUMMARY.md（本 plan 前置：intent 枚举、BLUEPRINT_SPEC_GATE_CONFIG 默认形状、aget_json_setting、spec_gate 三个事件常量）
@server/services/process_runtime/clarify_adapter.py（HITL 回路 analog，L1-30 docstring 契约 / L95-107 策略注入 / L115-137 六步顺序 / L170-175 重判输入拼装 / L250-260 emit 吞异常——**冻结，只读**）
@server/services/process_runtime/feature_classify.py（LLM 单调用 analog：L36-42 契约常量 / L50-86 三层解析 / L89-147 normalize 反幻觉 / L150-207 prompt 拆分 / L226-306 五步骨架与观测三事件——只读参考）
@server/delivery/services/blueprint_lifecycle_service.py（L131-231 既有 transition/add_reviewer，L188-194 已有 open+blocking 线程查询写法可复用）
@server/delivery/models/blueprint_thread.py（L22-60 五个 TextChoices：ThreadKind.AI_CLARIFICATION/REPO_CONFIRMATION、ThreadStatus.OPEN/ANSWERED/RESOLVED/DISMISSED、ThreadAuthorType；L62-150 BlueprintThread/Message 字段，注意 return_stage max_length=16 存的是蓝图状态不是 stage 名）
@server/delivery/services/artifact_service.py（L117-163 add_version：hash 相等复用不翻版本 + validate_content 失败抛 ArtifactContentInvalid）
@server/services/process_runtime/blueprint_schema.py（L166-223 requirement_spec 段含 ambiguity_report；L728-731 decision_log）
@server/tests/services/process_runtime/test_classify_stage.py（stage 测试范式：双装饰器 / SimpleNamespace deps 注入 / 重读 DB / _emit_event spy）
</context>

<tasks>

<task type="auto">
  <name>Task 1: BlueprintLifecycleService 补齐线程写入方法（BlueprintThread 唯一 writer）</name>
  <files>server/delivery/services/blueprint_lifecycle_service.py, server/tests/delivery/test_blueprint_thread_service.py</files>
  <read_first>server/delivery/services/blueprint_lifecycle_service.py 全文（尤其 L131-231 方法风格、L188-194 open+blocking 查询、L252 起 _record_transition_event 的 best-effort 事件写法）；server/delivery/models/blueprint_thread.py L22-150（枚举与字段）；server/tests/delivery/test_blueprint_lifecycle_service.py（既有测试组织与 fixture）；server/tests/delivery/test_blueprint_inv6_guard.py（源码扫描守护的现行断言口径——新 writer 必须被它认可）</read_first>
  <action>
在 `BlueprintLifecycleService` 类内**追加**四个方法（既有 `transition` / `add_reviewer` / 两个私有方法逐字不动；`__all__` 不变）：

- `async def ahas_open_blocking_threads(self, artifact, *, kind: str | None = None) -> bool`：复用 L188-194 的查询形（`status=ThreadStatus.OPEN, blocking=True`），`kind` 非空时再过滤 `kind=kind`。给 spec_gate 与确认门做 pending 短路用；也给 112-05 的 `blueprint_resume` 判 pause 用。
- `async def open_thread(self, artifact, *, kind: str, blocking: bool, question: str, options: list | None = None, initiated_by_user_id: str = "system", created_on_version=None, anchor: dict | None = None, return_stage: str = "") -> BlueprintThread`：`acreate` 线程（`status=ThreadStatus.OPEN`、`options=options or []`）+ 同一次调用内建首条 `BlueprintThreadMessage(author_type=AI, body=question)`（两写包在一个 `@sync_to_async` + `transaction.atomic` 的私有 `_open_thread_sync`，避免半截线程）。`kind` 必须 ∈ `ThreadKind.values` 否则 `raise ValueError`；`return_stage` 超 16 字符时截断并记 warning（字段 max_length=16）。structlog `blueprint_thread_opened`（`category="caller"`、`component="blueprint_lifecycle"`、`artifact_id`、`kind`、`blocking`、`initiated_by_user_id`、`thread_id`）——**问题正文不进日志**。
- `async def record_answer(self, thread, *, body: str, author=None, author_type: str = ThreadAuthorType.HUMAN, initiated_by_user_id: str = "system") -> BlueprintThreadMessage`：追加消息 + 把线程 `status` 从 `OPEN` 推到 `ANSWERED`（已是 `ANSWERED`/`RESOLVED` 则不回退，幂等）。
- `async def resolve_thread(self, thread, *, resolution: str = "", initiated_by_user_id: str = "system", dismissed: bool = False) -> BlueprintThread`：置 `status = DISMISSED if dismissed else RESOLVED`；终态线程重复调用为幂等 no-op（保留首次）。structlog `blueprint_thread_resolved`（同 kv 组）。

全部方法：ORM 多步写经 `@sync_to_async` 私有方法 + `transaction.atomic`；单行读用 `aexists`/`afirst`（async ORM 防裸 lazy-FK）；事件/日志 best-effort（`except Exception` 只 warning，绝不反噬业务）。

新建 `server/tests/delivery/test_blueprint_thread_service.py`（`@pytest.mark.django_db(transaction=True)` + `@pytest.mark.asyncio`）：
- `open_thread` 建 `ai_clarification` 线程 → 重读 DB 断言 `kind/blocking/status=open/options` 与首条 AI 消息存在。
- 非法 kind → `ValueError`，且 `BlueprintThread.objects.acount()` 不增（原子性）。
- `ahas_open_blocking_threads`：无线程 → False；建 open+blocking → True；`resolve_thread` 后 → False；`kind` 过滤生效（`repo_confirmation` 过滤查不到 `ai_clarification` 线程）。
- `record_answer` → status=answered + 消息条数 +1；对 answered 线程再 record 不回退状态。
- `resolve_thread` 幂等：连调两次仍 `resolved`；`dismissed=True` → `dismissed`。
- `return_stage` 传 20 字符 → 落库值长度 ≤16 且不抛。
- 与既有守卫联动：`transition(artifact, "confirmed", ...)` 在存在 open+blocking 线程时仍被拒（既有 LIFE-02 行为未被新方法破坏）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/delivery/test_blueprint_thread_service.py tests/delivery/test_blueprint_lifecycle_service.py tests/delivery/test_blueprint_inv6_guard.py -x -q</automated>
  </verify>
  <acceptance_criteria>
- 上述 verify 全绿（新方法测试 + 既有 lifecycle 测试 + INV-6 守护测试）。
- `rg -c "^    async def (ahas_open_blocking_threads|open_thread|record_answer|resolve_thread)" server/delivery/services/blueprint_lifecycle_service.py` 等于 4。
- `rg -n "async def transition" server/delivery/services/blueprint_lifecycle_service.py` 命中且 `git diff server/delivery/services/blueprint_lifecycle_service.py | rg "^-" | rg -v "^---"` 输出为空（纯追加，零删改行）。
- `rg -n "BlueprintThread.objects.(create|acreate)" server/services/ server/delivery/api/ 2>/dev/null` 零命中（线程写入只在 lifecycle service）。
  </acceptance_criteria>
  <done>BlueprintThread 有了唯一 writer（开/答/解 + open+blocking 查询），既有 transition 行为逐字未变，112-05 确认门可直接复用同一套 API。</done>
</task>

<task type="auto">
  <name>Task 2: 歧义四维打分 + 意图分类两个 LLM helper（含判定纯函数与配置 loader）</name>
  <files>server/services/process_runtime/blueprint_ambiguity_score.py, server/services/process_runtime/blueprint_intent_classify.py, server/tests/services/process_runtime/test_blueprint_ambiguity_score.py</files>
  <read_first>server/services/process_runtime/feature_classify.py 全文（L36-42 契约常量 / L50-67 _content_to_text / L70-86 _parse_items_json / L89-147 normalize / L150-207 prompt / L226-306 五步骨架 + 观测三事件——只读，逐条镜像）；server/agents/call_source.py L108-135（BLUEPRINT_SPEC_GATE / BLUEPRINT_DECOMPOSE 值）；server/system/settings_service.py（112-01 新增的 aget_json_setting）；server/services/cursor_writeback.py L85-124（loader 全兜底 + 判定纯函数分离范式）；server/tests/helpers/fake_chat_model.py（LLM mock）</read_first>
  <action>
① 新建 `server/services/process_runtime/blueprint_ambiguity_score.py`。模块 docstring 写四段契约：「谁调用（blueprint_spec_gate stage）」/「fail-closed 方向：LLM 不可得时判需澄清而非放行」/「阈值与权重来自 SystemSetting，模块常量只作兜底」/「判定是纯函数，可被 golden set 评估」。`logger = structlog.get_logger(__name__)`。
模块级契约常量：`__all__ = ["ascore_ambiguity", "normalize_ambiguity_scores", "weighted_total", "is_ambiguous", "aload_spec_gate_config", "DEFAULT_SPEC_GATE_CONFIG"]`；`_DIMENSIONS = ("goal", "boundary", "constraint", "acceptance")`；`DEFAULT_SPEC_GATE_CONFIG = {"threshold": 0.20, "weights": {"goal": 0.30, "boundary": 0.25, "constraint": 0.20, "acceptance": 0.25}}`（与 112-01 SettingKeys 注释逐字一致）；`_MAX_PROMPT_CHARS`。
函数：
- `normalize_ambiguity_scores(data) -> dict`（纯函数）：输出恒 `{"dimensions": {dim: {"score": float, "reason": str}}, "questions": [...]}`。每维 `float()` 强转并 clamp `[0,1]`；缺维/非法 → **保守值 1.0**（最歧义，fail-closed 方向）；`reason` 截断 300；`questions` 每项白名单 `{text, options: list[str], citations: list[str]}`，`text` 空则整项丢弃，条数上界 `_MAX_QUESTIONS = 5`；**反幻觉：某维 score 高但 reason 为空 → reason 补占位且该维保持保守值**（镜像 feature_classify「结论失去依据就降级」）。任何非 dict 输入 → 全维保守值。
- `weighted_total(dimensions: dict, weights: dict) -> float`（纯函数）：逐维取 score × weight，权重缺失/非数 → 该维取 `DEFAULT_SPEC_GATE_CONFIG` 同维权重；权重和为 0 → 回退等权；返回 clamp `[0,1]`。
- `is_ambiguous(total: float, threshold: float) -> bool`（纯函数）：`total >= threshold`。
- `async def aload_spec_gate_config() -> dict`：`await aget_json_setting(SettingKeys.BLUEPRINT_SPEC_GATE_CONFIG, DEFAULT_SPEC_GATE_CONFIG)` → 逐字段强转（`threshold` 经 `float()` + clamp `[0,1]`，`weights` 逐维 `float()` 且负值取 0）→ 整段 `except Exception` 返默认。绝不抛、绝不 eval。
- `async def ascore_ambiguity(*, goal: str, feature_points: list[dict], constraints: list | None = None, session_id: str = "") -> dict | None`：五步骨架逐条镜像 feature_classify L226-259（`started = time.monotonic()` → lazy import → `ProviderConfigService.aresolve()` → 无 `default_model` 记 `blueprint_ambiguity_score_no_default_model` 返 `None` → `build_chat_model(..., streaming=False)` → `with use_call_source(CallSource.BLUEPRINT_SPEC_GATE): await model.ainvoke(...)`）→ `_content_to_text` → ```json 围栏 + 裸 JSON 双路解析 → `normalize_ambiguity_scores`。失败一律 `redact_secrets_in_text(str(exc))` + warning + 返 `None`（上游按 fail-closed 处理）。
prompt 两私有函数：`_system_prompt()` 写死输出 JSON 形状（四维 score 0-1 + reason + questions[].options/citations）+ 「判不出就给高分并说明缺什么，不要猜」；`_build_prompt()` 按 `### 需求目标 / ### 功能点 / ### 已知约束` 分节并截断。
观测三事件 `blueprint_ambiguity_score_started/completed/failed`，全带 `category="sampling"`、`component="process_runtime"`、`duration_ms`；completed 带分桶指标：`dimension_scores`（四个标量）、`weighted_total`、`threshold`、`above_threshold`、`question_count`——**需求正文与问题正文不进日志**。

② 新建 `server/services/process_runtime/blueprint_intent_classify.py`（同骨架，规模更小）：`_VALID_INTENT = ("greenfield", "brownfield", "fix")`（对齐 112-01 的 schema 枚举）；`normalize_intents(data, allowed_ids) -> dict[str, str]` 纯函数——只保留 `allowed_ids` 内的 feature_point id（丢弃 LLM 编造 id）、非法枚举值回落**保守值 `"brownfield"`**（存量改造假设更安全：不会让净新增假设跳过能力树证据）；`async def aclassify_intents(*, feature_points: list[dict], session_id: str = "") -> dict[str, str] | None` 用 `CallSource.BLUEPRINT_DECOMPOSE`；观测三事件 `blueprint_intent_classify_*`，completed 带 `greenfield_count/brownfield_count/fix_count`。

③ 新建 `server/tests/services/process_runtime/test_blueprint_ambiguity_score.py`（纯函数部分无 DB；LLM 部分 patch `blueprint_ambiguity_score.build_chat_model` 返回 `FakeChatModel`，不 mock provider 内部）：
- `normalize_ambiguity_scores`：合法输入原样归一；缺维补 1.0；score `"abc"`/`-1`/`5` 分别回落保守值或 clamp；非 dict 输入全维 1.0；questions 超 5 条被截断、空 text 项被丢弃；reason 为空时该维保持保守值。
- `weighted_total`：权重缺项用默认；权重全 0 回退等权；结果 clamp。
- `is_ambiguous` 边界：`total == threshold` 判 True（`>=`）。
- `aload_spec_gate_config`：缺键回默认；`threshold="0.5"` 字符串被强转；`weights` 顶层为 list → 回默认；`threshold=99` → clamp 到 1.0。
- `ascore_ambiguity`：FakeChatModel 返合法 JSON → 四维与 questions 正确；返 ```json 围栏包裹 → 同样解析成功；返非 JSON → `None`；patch `ProviderConfigService.aresolve` 无 `default_model` → `None`；`build_chat_model` 抛异常 → `None` 且不上抛。
- `normalize_intents`：编造 id 被丢弃；非法枚举回落 `brownfield`；三个合法值原样保留。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/process_runtime/test_blueprint_ambiguity_score.py -x -q</automated>
  </verify>
  <acceptance_criteria>
- `cd server && uv run pytest tests/services/process_runtime/test_blueprint_ambiguity_score.py -x -q` 全绿。
- `rg -n "use_call_source\(CallSource.BLUEPRINT_SPEC_GATE\)" server/services/process_runtime/blueprint_ambiguity_score.py` 命中。
- `rg -n "BLUEPRINT_SPEC_GATE_CONFIG" server/services/process_runtime/blueprint_ambiguity_score.py` 命中（阈值/权重不硬编码，走 SystemSetting）。
- `rg -c "blueprint_ambiguity_score_started|blueprint_ambiguity_score_completed|blueprint_ambiguity_score_failed" server/services/process_runtime/blueprint_ambiguity_score.py` ≥ 3；`rg -c "category=\"sampling\"" server/services/process_runtime/blueprint_ambiguity_score.py` ≥ 3。
- `rg -n "redact_secrets_in_text" server/services/process_runtime/blueprint_ambiguity_score.py server/services/process_runtime/blueprint_intent_classify.py` 两文件各命中。
- `rg -n "_VALID_INTENT = \(\"greenfield\", \"brownfield\", \"fix\"\)" server/services/process_runtime/blueprint_intent_classify.py` 命中。
- `rg -n "raise" server/services/process_runtime/blueprint_ambiguity_score.py | rg -v "ValueError.*kind" | wc -l` 为 0（helper 绝不上抛）。
  </acceptance_criteria>
  <done>四维打分与意图分类各为一次可 mock 的 LLM 调用；阈值/权重外置且畸形配置逐项降级；判定全是可单测纯函数；保守方向 = fail-closed。</done>
</task>

<task type="auto">
  <name>Task 3: blueprint_spec_gate adapter —— 提问/挂起/作答/规格锁定与 decision_log 物化</name>
  <files>server/services/process_runtime/blueprint_spec_gate.py, server/tests/services/process_runtime/test_blueprint_spec_gate.py</files>
  <read_first>server/services/process_runtime/clarify_adapter.py L1-30 / L95-137 / L160-180 / L240-261（六步顺序、上界兜底、重判输入拼装、emit 吞异常——冻结只读）；server/delivery/services/artifact_service.py L117-163（add_version 语义：hash 相等复用不翻版本、校验失败抛 ArtifactContentInvalid）；server/delivery/models/convergence_session.py L80-110（stage_state / current_artifact_version / created_by）；server/services/process_runtime/blueprint_schema.py L166-223（requirement_spec 含 ambiguity_report）与 L728-731（decision_log）；server/tests/services/process_runtime/test_classify_stage.py 全文（stage 测试四类必测）</read_first>
  <action>
新建 `server/services/process_runtime/blueprint_spec_gate.py`。模块 docstring 四段：「本 adapter 是阶段 0 规格门，只被 112-05 注册的 `technical_blueprint.spec_gate` handler 调用」/「fail-closed：打分不可得即判需澄清」/「INV-6：线程经 BlueprintLifecycleService、蓝图版本经 ArtifactService，本文件零 ORM 写」/「不复用 delivery.Clarification（CONTEXT 明令）」。

`class BlueprintSpecGateAdapter`（策略可注入，镜像 clarify_adapter L95-107：`__init__` 全 keyword-only，每个依赖 `x or DefaultX()`：`lifecycle`、`artifacts`、`scorer=ascore_ambiguity`、`classifier=aclassify_intents`）。模块级 `_MAX_SPEC_GATE_ROUNDS = 3`（重问上界；镜像 `_MAX_CLARIFY_ROUNDS` 的**达上界放行**语义，不挂死）。

主入口 `async def run(self, session) -> dict`，返回形状恒定 `{"event": "needs_clarification" | "spec_locked", "thread_id": str | None, "ambiguity": {...}, "round": int}`（**adapter 只返回 dict，stage 转移由 handler 的 StageOutcome 决定——engine 纯度**）。步骤带显式编号注释解释顺序理由：
1. **pending 短路**：`await lifecycle.ahas_open_blocking_threads(artifact, kind=ThreadKind.AI_CLARIFICATION)` 为 True → 直接返回 `needs_clarification`（不再打分、不重复提问）。
2. **轮数上界**：从 `session.stage_state.get("spec_gate", {}).get("round", 0)` 读；≥ `_MAX_SPEC_GATE_ROUNDS` → 记 `blueprint_spec_gate_cap_reached`（warning）后**放行**走第 6 步锁定（带 `ambiguity_report.capped=True`）。
3. **已答内容与既有结论拼装（不重复提问的核心）**：`_collect_prior_answers(artifact)` = 已 `RESOLVED`/`ANSWERED` 的 `ai_clarification` 线程的问题+回答 + 当前蓝图 `content["decision_log"]` 条目，一并拼进打分 prompt 输入（镜像 clarify_adapter L170-175）；同时算出 `answered_question_fingerprints`（问题文本归一化后的集合）。
4. **打分**：`await scorer(goal=..., feature_points=..., constraints=..., session_id=...)`；返 `None` → **fail-closed**：按「需澄清」走第 5 步，`ambiguity_report.scorer_unavailable=True`，且问题清单退化为一条通用规格澄清（要求补目标/边界/约束/验收）。
5. **超阈值 → 开线程**：`weighted_total` + `aload_spec_gate_config` 判 `is_ambiguous`；True 时把 `questions` 中指纹已在第 3 步集合内的条目**剔除**（同一问题不再问）；剔除后为空 → 视为已澄清转第 6 步；否则 `await lifecycle.open_thread(artifact, kind=ThreadKind.AI_CLARIFICATION, blocking=True, question=..., options=questions（含 options 候选与 citations 证据引用）, initiated_by_user_id=..., created_on_version=..., return_stage=BlueprintStatus.RESEARCHING)`；emit `EVENT_BLUEPRINT_SPEC_GATE_CLARIFICATION_ASKED`（payload 只记 `thread_id`/`question_count`/`weighted_total`）；返回 `needs_clarification` + `stage_state` 增量 `{"spec_gate": {"round": round+1, "thread_id": ...}}`。
6. **放行 → 规格锁定**：深拷贝当前 `ArtifactVersion.content`，写入
   - `requirement_spec` 的 `goal`/`boundaries`/`constraints`/`feature_points`（feature_points 逐条补 `intent`：先取已有值，缺失则用 `classifier` 结果，仍缺 → `"brownfield"` 保守值，**保证 schema 必填枚举永不违约**）；
   - `requirement_spec.ambiguity_report = {"dimensions": ..., "weighted_total": ..., "threshold": ..., "weights": ..., "resolved_thread_ids": [已解决 ai_clarification 线程 id], "capped": bool, "scorer_unavailable": bool}`；
   - `decision_log` 追加每个已解决线程的结论快照 `{thread_id, question, answer, decided_at, decided_by}`（**已存在同 thread_id 条目则跳过，保证幂等重跑不重复堆积**）；
   然后 `await artifacts.add_version(artifact, content, produced_by_session_id=str(session.id), produced_by_ref="blueprint_spec_gate")`；把仍 `ANSWERED` 的线程经 `lifecycle.resolve_thread` 收尾；emit `EVENT_BLUEPRINT_SPEC_GATE_SCORED` + `EVENT_BLUEPRINT_SPEC_GATE_LOCKED`；返回 `spec_locked`。
`ArtifactContentInvalid` 必须捕获 → 记 `blueprint_spec_gate_invalid_content`（error 文本先 `redact_secrets_in_text`）并返回 `needs_clarification`（fail-closed，绝不上抛让 engine 落 failed）。事件 emit 独立私有方法 + `except Exception` 吞掉。

新建 `server/tests/services/process_runtime/test_blueprint_spec_gate.py`（`@pytest.mark.django_db(transaction=True)` + `@pytest.mark.asyncio`；scorer/classifier 用 `AsyncMock` 注入构造参数，不 patch 模块全局；断言一律重读 DB）：
- 低分放行：scorer 返全维 0.05 → `spec_locked`；重读最新 `ArtifactVersion.content`：`requirement_spec.ambiguity_report.weighted_total` 与 `threshold` 就位、每个 feature_point 有合法 `intent`、`validate_blueprint(content)` 返回 `(True, [])`。
- 高分开门：scorer 返全维 0.9 → `needs_clarification` + DB 内存在 1 条 `ai_clarification` open+blocking 线程且 `options` 携带候选与 citations。
- pending 短路：已有 open+blocking 线程时再 `run` → scorer 的 `AsyncMock.await_count == 0`（不重复打分/提问）且返回 `needs_clarification`。
- **不重复提问**：线程已 resolved 且 `decision_log` 内含该结论时，scorer 再次给出同一问题 → 该问题被指纹剔除 → 结果为 `spec_locked` 且**不新建线程**（线程总数不变）。
- fail-closed：scorer 返 `None` → `needs_clarification` + `ambiguity_report`（下一轮锁定时）标 `scorer_unavailable`；且不抛异常。
- 上界放行：`stage_state["spec_gate"]["round"] = 3` + scorer 返高分 → `spec_locked` 且 `ambiguity_report.capped is True`（不挂死）。
- intent 兜底：classifier 返 `None` 且 feature_point 无 intent → 落 `"brownfield"` 且 `validate_blueprint` 通过。
- decision_log 幂等：连续两次 `run` 走锁定路径 → `decision_log` 条数不翻倍（同 thread_id 去重）。
- 内容非法：patch `artifacts.add_version` 抛 `ArtifactContentInvalid` → 返回 `needs_clarification`，异常不上抛。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/process_runtime/test_blueprint_spec_gate.py -x -q</automated>
  </verify>
  <acceptance_criteria>
- `cd server && uv run pytest tests/services/process_runtime/test_blueprint_spec_gate.py -x -q` 全绿。
- `rg -n "resolved_thread_ids" server/services/process_runtime/blueprint_spec_gate.py` 命中；`rg -n "decision_log" server/services/process_runtime/blueprint_spec_gate.py` 命中。
- `rg -n "Clarification" server/services/process_runtime/blueprint_spec_gate.py` 零命中（未复用 delivery.Clarification）。
- `rg -n "\.objects\.(create|acreate|update|aupdate|filter\(.*\)\.update)" server/services/process_runtime/blueprint_spec_gate.py` 零命中（adapter 零 ORM 写，INV-6）。
- `rg -n "_MAX_SPEC_GATE_ROUNDS" server/services/process_runtime/blueprint_spec_gate.py` 命中 ≥ 2（常量定义 + 比对）。
- `rg -n "EVENT_BLUEPRINT_SPEC_GATE_(SCORED|CLARIFICATION_ASKED|LOCKED)" server/services/process_runtime/blueprint_spec_gate.py` 三个常量全命中。
- `cd server && uv run pytest tests/services/ tests/delivery/ -q` 全绿（既有链路零扰动）。
  </acceptance_criteria>
  <done>规格门跑通完整回路：打分 → 超阈值开带选项与证据的阻塞线程 → 挂起不重复提问 → 作答后 requirement_spec/ambiguity_report/decision_log 一次性落新蓝图版本且通过 schema 校验；LLM 不可得与内容非法两条降级路径都 fail-closed 不落 failed。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM 上游响应 → 打分/分类归一 | 半可信 JSON（分数、问题文本、feature_point id）进入判定与蓝图内容 |
| SystemSetting → 阈值判定 | 运行时可改配置影响是否放行 |
| 用户澄清回答 → decision_log / 蓝图版本 | 人工输入正文进入持久化产物 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-112-05 | Tampering | LLM 打分响应 | mitigate | `normalize_ambiguity_scores` 白名单 + clamp + 缺维取保守值 1.0；`normalize_intents` 用 `allowed_ids` 过滤编造 id + 非法枚举回落 brownfield |
| T-112-06 | Spoofing / Bypass | 规格门被绕过放行 | mitigate | fail-closed：scorer 返 None / 内容校验失败 / 解析失败一律判「需澄清」；唯一放行例外是显式轮数上界且在 `ambiguity_report.capped` 留痕 |
| T-112-07 | Tampering | 阈值被写成极端值致门形同虚设 | mitigate | `aload_spec_gate_config` 对 `threshold` 做 `float()` + clamp `[0,1]`、权重负值取 0、非 dict 回默认；配置来源仅 SystemSetting（认证后台可写），绝不 eval |
| T-112-08 | Info Disclosure | 澄清正文/需求正文入日志或事件 payload | mitigate | 日志与事件 payload 只记 `thread_id`/计数/标量分数（clarify_adapter L119-120 先例）；异常文本经 `redact_secrets_in_text`；schema 报错出口已截断脱敏 |
| T-112-09 | Repudiation | 决策无留痕 | mitigate | `decision_log` 记 `thread_id/question/answer/decided_at/decided_by`，`initiated_by_user_id` 贯穿线程与事件（观测规范「谁触发的」） |
| T-112-SC | Tampering | 依赖供应链 | accept | 零新增外部依赖 |
</threat_model>

<verification>
- `cd server && uv run pytest tests/services/process_runtime/ tests/delivery/ -q` 全绿。
- `cd server && uv run python manage.py makemigrations --check --dry-run` 退出码 0（本 plan 零模型改动）。
- 冻结面自检：`git diff --name-only` 不含 `repo_router_v2.py`、`process_runtime/{decompose_segments,research_adapter,architect_merge_adapter,merged_plan,clarify_adapter,render,resume,builtin_processes}.py`、`system/models.py`、`system/settings_service.py`、`delivery/services/event_taxonomy.py`、`blueprint_schema.py`。
- 并行自检：`git diff --name-only` 不含 `blueprint_route.py`（112-03 的独占文件）。
- `git diff server/delivery/services/blueprint_lifecycle_service.py | rg "^-" | rg -v "^---"` 输出为空（纯追加）。
</verification>

<success_criteria>
- FLOW-01：需求歧义超阈值时编排停在「需要澄清」并抛出带候选选项与证据的问题；作答后规格锁定且同一问题不再重复问；每个 feature_point 带 greenfield/brownfield/fix 意图标注（ROADMAP SC1 全量）。
- 澄清载体为 `BlueprintThread(ai_clarification, blocking=true)`，`delivery.Clarification` 零复用；线程写入唯一入口在 `BlueprintLifecycleService`。
</success_criteria>

<output>
完成后创建 `.planning/phases/112-1/112-02-SUMMARY.md`（provides：lifecycle 四个线程方法（112-05 确认门复用）、`ascore_ambiguity`/`aclassify_intents`/判定纯函数、`BlueprintSpecGateAdapter.run` 返回契约与 stage_state 键 `spec_gate`）。
</output>
