---
phase: 112-1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/services/process_runtime/blueprint_schema.py
  - server/tests/helpers/blueprint_samples.py
  - server/tests/fixtures/blueprint_golden/gaokao_boost.json
  - server/tests/services/test_blueprint_schema.py
  - server/system/models.py
  - server/system/settings_service.py
  - server/delivery/services/event_taxonomy.py
  - server/tests/test_blueprint_settings.py
  - server/tests/delivery/test_blueprint_event_taxonomy_112.py
autonomous: true
requirements: [FLOW-01, CHARTER-02]
prohibitions:
  - "禁止修改 server/codegraph/services/repo_router_v2.py（STATE.md 并行纪律 1）"
  - "禁止修改 process_runtime 六冻结文件（decompose_segments.py / research_adapter.py / architect_merge_adapter.py / merged_plan.py / clarify_adapter.py / render.py）——本 plan 一个都不碰"
  - "禁止修改 server/services/process_runtime/resume.py（蓝图续驱走 112-05 新建的 blueprint_resume.py）"
  - "禁止修改 ConvergenceSessionEvent 既有事件类型/字段：event_taxonomy.py 只往 BLUEPRINT_EVENTS 与 __all__ 追加新常量，既有 4 个 blueprint_* 常量与 ALL_EVENTS 一字不动"
  - "禁止修改 settings_service 既有 getter 行为与签名（get_setting/get_bool/get_int/get_float/get_json/aget_setting/aget_bool/aget_int 逐字不动），只新增 aget_float_setting / aget_json_setting 两个函数"
  - "禁止修改 builtin_processes.py（stage graph 注册在 112-05）"
  - "feature_points 加 intent 只动 required 数组与 properties 字典；blueprint_schema.py 其余 schema 段、后置检查、iter_blocks 逻辑零改动"
  - "SettingKeys 只追加常量（无 migration）；system/models.py 既有键一个不改"
must_haves:
  truths:
    - "缺 intent 的 feature_point 被 validate_blueprint 拒绝；intent 取非法枚举值（如 refactor）同样被拒（FLOW-01 意图分类的 schema 强制）"
    - "make_blueprint() 产物与 gaokao_boost.json golden fixture 均带 intent 且通过 validate_blueprint；6 个既有消费测试文件零改动全绿"
    - "blueprint.spec_gate.config 与 blueprint.route.weights 未配置时 loader 回默认（阈值 0.20）；配置非法 JSON / 非 dict / 缺维度时逐项回默认且绝不抛"
    - "aget_float_setting / aget_json_setting 在 async 上下文可读 SystemSetting，语义与同步版一致（非法值回默认）"
    - "112 新增 11 个 blueprint_* 事件常量在 BLUEPRINT_EVENTS 内，且未污染 ALL_EVENTS"
  artifacts:
    - path: "server/services/process_runtime/blueprint_schema.py"
      provides: "feature_points[].intent 必填枚举（greenfield/brownfield/fix）"
      contains: "\"greenfield\""
    - path: "server/system/models.py"
      provides: "SettingKeys.BLUEPRINT_SPEC_GATE_CONFIG + SettingKeys.BLUEPRINT_ROUTE_WEIGHTS"
      contains: "blueprint.spec_gate.config"
    - path: "server/system/settings_service.py"
      provides: "aget_float_setting / aget_json_setting 两个新 async getter"
      contains: "async def aget_json_setting"
    - path: "server/delivery/services/event_taxonomy.py"
      provides: "112 阶段事件常量（spec_gate / route / repo_research / reroute / confirmation）"
      contains: "blueprint.route.scored"
    - path: "server/tests/fixtures/blueprint_golden/gaokao_boost.json"
      provides: "三条 feature_point 带 intent（brownfield/greenfield/greenfield）"
      contains: "\"intent\""
  key_links:
    - from: "server/tests/helpers/blueprint_samples.py"
      to: "server/services/process_runtime/blueprint_schema.py"
      via: "工厂样例必须满足新增 required intent，否则 6 个消费测试全红"
      pattern: "\"intent\""
    - from: "server/system/settings_service.py"
      to: "server/system/models.py"
      via: "新 async getter 读 SettingKeys 新键（消费方在 112-02/112-03）"
      pattern: "aget_float_setting|aget_json_setting"
---

<objective>
把 112 全相位共用的三块「地基改动」一次性落地，让 wave 2 的 spec_gate 与 route 可并行开工且互不改同一文件：
① `blueprint_schema` 的 `feature_points[]` 补必填枚举 `intent`（FLOW-01 意图分类的强制载体），并同步 2 个含字面数据的文件 + 回归 6 个消费测试；
② `SettingKeys` 注册歧义阈值/四维权重与路由意图权重两个点分键，`settings_service` 只新增 async float/json getter（CONTEXT 定夺方案 b）；
③ `event_taxonomy` 追加 112 的 11 个 `blueprint_*` 阶段事件常量，供 02/03/04/05 消费而无需再改该文件。

Purpose: intent 是 route 加权（CHARTER-02）的必需输入，阈值/权重外置是 CONTEXT 锁定项；事件常量前置可消除 wave 2 两个并行 plan 对同一文件的写冲突。
Output: schema 演进 + 2 份数据同步 + 2 个 SettingKeys 键 + 2 个 async getter + 11 个事件常量 + 2 个新测试文件 + 既有 schema 测试扩充。
</objective>

<execution_context>
工作目录：worktree 根 `/Users/zaneliu/Projects/open-source/friday-clean/.claude/worktrees/v0.20-blueprint`。
本 plan 是 wave 1 唯一 plan，无并行方；但它是 112-02/03 的硬前置（intent 字段 + 两个 SettingKeys 键 + 事件常量）。
</execution_context>

<context>
@.planning/phases/112-1/112-CONTEXT.md（锁定：intent 落位 feature_points 必填枚举；阈值与四维权重外置 SystemSetting，默认总分阈值 0.20；settings_service 只新增两个 async getter）
@.planning/phases/112-1/112-RESEARCH.md 主题 3（feature_points schema 现状 :173-209、必改 2 文件 / 回归 6 文件清单、最小改法 A、逐条核查过的断言不破证明）与主题 4（SettingKeys 点分命名惯例、getter 清单与 async float/json 缺口、测试范式）
@.planning/phases/112-1/112-PATTERNS.md 第 8 类（SettingKeys 常量注释样板 + loader 全兜底范式）
@server/services/process_runtime/blueprint_schema.py（:169 required、:173-209 feature_points、:823-840 与 :886-900 后置检查、:940-947 iter_blocks）
@server/tests/helpers/blueprint_samples.py（:3-12 docstring 字段说明、:48-61 两条 fp、:352-356 工厂）
@server/tests/fixtures/blueprint_golden/gaokao_boost.json（:34- 三条 fp，:42 自述 brownfield、:55 自述净新增）
@server/system/models.py（SettingKeys :31-163；LOG_* :127-136 与 ALERT-01 :149 的注释样板；追加位置 = :163 PR_REVIEW_CAPTURE 之后）
@server/system/settings_service.py（同步 typed getter :44-93、async 三个 :96-121、`_cache_key` 与缓存哨兵 :19-41）
@server/delivery/services/event_taxonomy.py（:39-43 __all__、:111-128 既有 4 个 blueprint_* 常量与独立 BLUEPRINT_EVENTS frozenset）
@server/tests/test_log_runtime_config.py（:37-90 运行时可配键的测试范式：`_save_setting` 用 instance.save()、autouse `_isolate` 清 60s 缓存）
</context>

<tasks>

<task type="auto">
  <name>Task 1: feature_points 补必填枚举 intent + 2 份字面数据同步 + schema 负向测试</name>
  <files>server/services/process_runtime/blueprint_schema.py, server/tests/helpers/blueprint_samples.py, server/tests/fixtures/blueprint_golden/gaokao_boost.json, server/tests/services/test_blueprint_schema.py</files>
  <read_first>server/services/process_runtime/blueprint_schema.py L166-223（requirement_spec.required 与 feature_points 全字段）与 L780-800（模块内 additionalProperties 注释约定）；server/tests/helpers/blueprint_samples.py L1-20 与 L44-62；server/tests/fixtures/blueprint_golden/gaokao_boost.json L30-75；server/tests/services/test_blueprint_schema.py L40-60（validate 通过样板）与 L160-215（负向断言样板：deepcopy 改字段 → 断言 (False, errors)）</read_first>
  <action>
① `server/services/process_runtime/blueprint_schema.py`：
- `feature_points.items.required`（现 `["id", "title"]`，:178）改为 `["id", "title", "intent"]`。
- 在 `properties` 内 `title` 之后、`description` 之前（保持语义相邻）插入：
  `"intent": {"type": "string", "enum": ["greenfield", "brownfield", "fix"], "description": "功能点意图分类（净新增 / 存量改造 / 缺陷修复；驱动 blueprint_route 加权，DESIGN §5.7）"}`。
- 模块 docstring 若逐字列了 feature_point 字段形状，同步补 `intent`（RESEARCH 主题 3.4 第 5 步）。
- 其余一切不动：`requirement_spec.required`（:169）保持 `["goal","feature_points"]`；不动 `additionalProperties` 默认策略；不动 id 唯一性后置检查（:886-900）、`items[].feature_point_id` 可解析检查（:823-840）、`iter_blocks`（:940-947，intent 是标量不进 block 走查）。

② `server/tests/helpers/blueprint_samples.py`：`_BASE_BLUEPRINT["requirement_spec"]["feature_points"]` 两条各补 `"intent"`——`fp_01` = `"greenfield"`（后端接口净新增）、`fp_02` = `"brownfield"`（前端入口改造，让工厂天然覆盖两个枚举分支，RESEARCH 已核查无断言依赖 fp_02 字段集）。同步 L3-12 docstring 的字段清单说明。不改 `make_blueprint` 签名与 deepcopy 语义。

③ `server/tests/fixtures/blueprint_golden/gaokao_boost.json`：三条 fp 各补 `"intent"`——`fp_01="brownfield"`（描述自述存量改造）、`fp_02="greenfield"`（自述净新增）、`fp_03="greenfield"`（新增专项练习/组卷能力）。取值即 112-03/112-04 路由加权断言的期望依据（greenfield 重章程 → onion-learning 靠 owned(planned) 进候选）。JSON 格式与缩进沿用原文件。

④ `server/tests/services/test_blueprint_schema.py` 追加三条断言（锁住 required 演进，否则无回归保护）：
- `test_feature_point_missing_intent_rejected`：`content = make_blueprint()` → `content["requirement_spec"]["feature_points"][0].pop("intent")` → `validate_blueprint(content)` 返回 `(False, errors)` 且 errors 非空。
- `test_feature_point_invalid_intent_rejected`：把 intent 设为 `"refactor"` → `(False, ...)`。
- `test_feature_point_intent_enum_accepted`：三个合法值分别赋给 `fp_01` 后 `validate_blueprint` 均返回 `(True, [])`（枚举白名单正向覆盖）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/test_blueprint_schema.py -x -q</automated>
  </verify>
  <acceptance_criteria>
- `cd server && uv run pytest tests/services/test_blueprint_schema.py -x -q` 全绿。
- **intent 必填的验收是行为断言，不是对 `required` 数组的单行 rg**（`ruff format` 把数组换行后单行正则必然失效，且注释也会被 `rg -c` 计入）：`cd server && uv run pytest tests/services/test_blueprint_schema.py -q -k "missing_intent or invalid_intent or intent_enum_accepted"` 三条全绿 —— 即「缺 intent 的 blueprint 被 `validate_blueprint` 拒绝」「intent 取非法枚举被拒」「三个合法枚举值通过」三条行为同时成立。
- `rg -c '"intent"' server/tests/helpers/blueprint_samples.py` ≥ 2；`rg -c '"intent"' server/tests/fixtures/blueprint_golden/gaokao_boost.json` ≥ 3。
- `rg -c "greenfield|brownfield|fix" server/services/process_runtime/blueprint_schema.py` ≥ 1（枚举白名单在 schema 内，非手写判断）。
- `rg -n "missing_intent|invalid_intent" server/tests/services/test_blueprint_schema.py` 命中 ≥ 2。
  </acceptance_criteria>
  <done>intent 成为 feature_point 必填枚举且有正负向测试锁死；两份字面数据同步完成，golden fixture 三条 intent 取值与 112-03/04 的路由断言期望一致。</done>
</task>

<task type="auto">
  <name>Task 2: 6 个消费测试回归 + golden command 门 + SettingKeys 两键与 async getter</name>
  <files>server/system/models.py, server/system/settings_service.py, server/tests/test_blueprint_settings.py</files>
  <read_first>server/system/models.py L120-163（LOG_*/METRIC_*/ALERT_*/LEARNING_CASE_* 的点分键 + 注释三段式：JSON 形状 / 默认值 / 消费方）；server/system/settings_service.py 全文（同步 get_float_setting L69-77 与 get_json_setting L80-93 的降级语义、async 三个 L96-121 的 afirst 写法）；server/tests/test_log_runtime_config.py L37-90（`_save_setting` + autouse `_isolate` 清缓存 + django_db 标记）</read_first>
  <action>
① 先跑 6 个消费测试与 golden command 确认 Task 1 的数据同步充分（**零代码改动，纯回归**）：`tests/services/test_blueprint_execution.py`、`tests/services/test_blueprint_quality.py`、`tests/delivery/test_blueprint_artifact_wiring.py`、`tests/delivery/test_evaluate_blueprint_golden.py`、`tests/delivery/test_blueprint_integration.py`、`tests/services/test_blueprint_schema.py` + `python manage.py evaluate_blueprint_golden`。任何一处红说明字面数据还有遗漏 → 回 Task 1 补，不得改这 6 个测试文件的断言来「适配」。

② `server/system/models.py` 的 `SettingKeys` 尾部（`PR_REVIEW_CAPTURE` 之后）追加两键，注释按既有三段式写清 JSON 形状 / 默认值 / 消费方：
- `BLUEPRINT_SPEC_GATE_CONFIG = "blueprint.spec_gate.config"` —— value 为 JSON：`{"threshold": float, "weights": {"goal": float, "boundary": float, "constraint": float, "acceptance": float}}`；未配置时用默认 `threshold=0.20` + 四维等权归一（`goal=0.30/boundary=0.25/constraint=0.20/acceptance=0.25`）；消费方 `services/process_runtime/blueprint_spec_gate.py`（112-02）。
- `BLUEPRINT_ROUTE_WEIGHTS = "blueprint.route.weights"` —— value 为 JSON：`{"<intent>": {"router_base": float, "charter_match": float, "history_match": float}}`，intent ∈ greenfield|brownfield|fix；未配置时默认 greenfield 重章程与历史（`0.40/0.35/0.25`）、brownfield 重能力树（`0.60/0.20/0.20`）、fix 最重能力树（`0.70/0.15/0.15`）；消费方 `services/process_runtime/blueprint_route.py`（112-03）。
无 migration（SettingKeys 是纯常量类，`models.py:148` 有先例声明）。

③ `server/system/settings_service.py` **只新增**两个 async getter（放在 `aget_int_setting` 之后，既有 8 个 getter 逐字不动）：
- `async def aget_float_setting(key: str, default: float = 0.0) -> float`：镜像 `get_float_setting` 的降级语义（原始值 None/空 → default；`float()` 抛 `ValueError`/`TypeError` → default），取值走既有 async 取原始值路径（与 `aget_int_setting` 同源，无缓存）。
- `async def aget_json_setting(key: str, default: dict | None = None) -> dict`：镜像 `get_json_setting`——`default is None` → `{}`；`json.loads` 失败或结果非 dict → 返回 default；绝不抛。
两函数 docstring 注明「与同步版语义一致，async 版不走 60s 缓存（沿 aget_* 既有约定）」。`__all__`（若有）同步追加。

④ 新建 `server/tests/test_blueprint_settings.py`（照 `test_log_runtime_config.py` 范式：`@pytest.mark.django_db`，`_save_setting` 用 `instance.save()` 触发 signal，autouse `_isolate` fixture 对两个新键前后各 `cache.delete(_cache_key(key))`）：
- 缺键回默认：不建 SystemSetting 行，`get_json_setting(SettingKeys.BLUEPRINT_SPEC_GATE_CONFIG, _DEFAULT)` 与 `await aget_json_setting(...)` 均等于传入默认。
- 设合法 JSON 后 `get_json_setting` / `await aget_json_setting` 读到新值（threshold 改 0.5 可见）。
- 非法值回默认三例：非 JSON 字符串、JSON 但顶层是 list、空字符串。
- `aget_float_setting`：缺键回默认、合法值生效、`"abc"` 回默认。
- 两键并存互不干扰（`BLUEPRINT_ROUTE_WEIGHTS` 设值不影响 spec_gate 键读取）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/test_blueprint_settings.py tests/services/test_blueprint_execution.py tests/services/test_blueprint_quality.py tests/delivery/test_blueprint_artifact_wiring.py tests/delivery/test_evaluate_blueprint_golden.py tests/delivery/test_blueprint_integration.py -q</automated>
  </verify>
  <acceptance_criteria>
- 上述 verify 命令全绿（6 个消费测试 + 新设置测试）。
- `cd server && uv run python manage.py evaluate_blueprint_golden` 退出码 0（golden fixture 过 validate_blueprint 门）。
- `rg -n "blueprint.spec_gate.config" server/system/models.py` 与 `rg -n "blueprint.route.weights" server/system/models.py` 各命中 1 次。
- `rg -c "^async def aget_(float|json)_setting" server/system/settings_service.py` 等于 2。
- `git diff --stat server/tests/services/test_blueprint_execution.py server/tests/services/test_blueprint_quality.py server/tests/delivery/test_blueprint_artifact_wiring.py server/tests/delivery/test_evaluate_blueprint_golden.py server/tests/delivery/test_blueprint_integration.py` 输出为空（6 个消费测试零改动）。
- `cd server && uv run python manage.py makemigrations --check --dry-run` 退出码 0（SettingKeys 无 migration）。
  </acceptance_criteria>
  <done>6 个消费测试与 golden command 在 intent 演进后零改动全绿；两个点分设置键注册完成，async float/json getter 补齐且既有 getter 行为逐字未变。</done>
</task>

<task type="auto">
  <name>Task 3: event_taxonomy 追加 112 的 11 个 blueprint_* 阶段事件常量</name>
  <files>server/delivery/services/event_taxonomy.py, server/tests/delivery/test_blueprint_event_taxonomy_112.py</files>
  <read_first>server/delivery/services/event_taxonomy.py L30-50（__all__ 结构）与 L105-130（既有 4 个 blueprint_* 常量、注释说明为何独立 frozenset、BLUEPRINT_EVENTS 定义）；server/tests/delivery/ 下既有 taxonomy 测试（若有，沿其断言口径）</read_first>
  <action>
在 `server/delivery/services/event_taxonomy.py` 的 blueprint 区段（既有 4 常量之后）追加 112 阶段事件常量，全部 `Final[str]`、点分三段命名，并把 11 个常量一并加进 `BLUEPRINT_EVENTS` frozenset 与 `__all__`。**既有 4 个常量、`ALL_EVENTS`、其它事件段一字不动**（STATE.md 并行纪律 3）：

- `EVENT_BLUEPRINT_SPEC_GATE_SCORED = "blueprint.spec_gate.scored"`（歧义四维打分完成，payload 记各维分数/加权总分/阈值/是否放行）
- `EVENT_BLUEPRINT_SPEC_GATE_CLARIFICATION_ASKED = "blueprint.spec_gate.clarification_asked"`（超阈值开澄清线程，payload 记 thread_id/问题数，**不含澄清正文**）
- `EVENT_BLUEPRINT_SPEC_GATE_LOCKED = "blueprint.spec_gate.locked"`（规格锁定，payload 记 resolved_thread_ids 数/decision_log 条数）
- `EVENT_BLUEPRINT_ROUTE_SCORED = "blueprint.route.scored"`（路由候选与 breakdown 快照，payload 记 candidate 数/router_version/各候选三分量）
- `EVENT_BLUEPRINT_REPO_RESEARCH_STARTED = "blueprint.repo_research.started"`
- `EVENT_BLUEPRINT_REPO_RESEARCH_COMPLETED = "blueprint.repo_research.completed"`
- `EVENT_BLUEPRINT_REPO_RESEARCH_FAILED = "blueprint.repo_research.failed"`
- `EVENT_BLUEPRINT_REROUTE_TRIGGERED = "blueprint.reroute.triggered"`（payload 记 round/被排除仓/新增候选数）
- `EVENT_BLUEPRINT_CONFIRMATION_OPENED = "blueprint.confirmation.opened"`
- `EVENT_BLUEPRINT_CONFIRMATION_ACTION = "blueprint.confirmation.action"`（payload 记 action ∈ confirm|remove_repo|add_repo|reclassify_role|edit_responsibility + repository_id）
- `EVENT_BLUEPRINT_CONFIRMATION_LOCKED = "blueprint.confirmation.locked"`（payload 记锁定仓数/decided_by）

每个常量上方一行中文注释写清 emit 点所属 plan（112-02/03/04/05）与 payload 关键键，供 115 前端时间线消费。

新建 `server/tests/delivery/test_blueprint_event_taxonomy_112.py`（纯常量断言，无 DB）：
- 11 个新常量全部 ∈ `BLUEPRINT_EVENTS`。
- 11 个新常量与既有 4 个均 **不在** `ALL_EVENTS`（既有 111 决策：blueprint 事件不进 ALL_EVENTS 以免 taxonomy 覆盖性反查失败）。
- 既有 4 个常量字面值仍为 `blueprint.status.transitioned` / `blueprint.stage.started` / `blueprint.stage.completed` / `blueprint.stage.failed`（冻结快照断言）。
- `len(BLUEPRINT_EVENTS) == 15` 且集合内全部以 `"blueprint."` 前缀开头、无重复。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/delivery/test_blueprint_event_taxonomy_112.py -x -q</automated>
  </verify>
  <acceptance_criteria>
- `cd server && uv run pytest tests/delivery/test_blueprint_event_taxonomy_112.py -x -q` 全绿。
- `rg -c "blueprint\.(spec_gate|route|repo_research|reroute|confirmation)\." server/delivery/services/event_taxonomy.py` 等于 11。
- `rg -n "blueprint.status.transitioned" server/delivery/services/event_taxonomy.py` 命中（既有常量未被改名）。
- `cd server && uv run pytest tests/delivery/ -q -k taxonomy` 全绿（既有 taxonomy 覆盖性测试未被新常量打破）。
  </acceptance_criteria>
  <done>112 全相位的阶段事件常量一次注册完毕，02/03/04/05 只消费不再改本文件；既有事件契约有冻结快照断言保护。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SystemSetting DB 值 → 编排判定 | 运行时可改的阈值/权重字符串进入打分与路由加权 |
| 蓝图 content（半可信 LLM 产物） → jsonschema | intent 值来自 LLM 生成的 feature_point |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-112-01 | Tampering | feature_points[].intent | mitigate | jsonschema `enum` 白名单（在 schema 内，非手写判断）；缺失/非法值一律 validate 失败，配 Task 1 三条正负向测试 |
| T-112-02 | Tampering / DoS | blueprint.spec_gate.config / blueprint.route.weights | mitigate | 只经 `get_json_setting`/`aget_json_setting` 读，非 dict 或解析失败回默认；消费侧（112-02/03）再做逐字段 float 强转与 clamp；绝不 eval |
| T-112-03 | Info Disclosure | validate_blueprint 报错出口 | accept | 既有 `_format_error` 已 `redact_secrets_in_text` + 截断 500 字（blueprint_schema.py:752-770），本 plan 不改该出口 |
| T-112-04 | Info Disclosure | 事件 payload | mitigate | 事件常量注释显式规定「payload 只记标量与关联键，澄清/需求正文不进 payload」，emit 点在 02–05 按此实现 |
| T-112-SC | Tampering | 依赖供应链 | accept | 零新增外部依赖（jsonschema/structlog 已在 server/pyproject.toml） |
</threat_model>

<verification>
- `cd server && uv run pytest tests/services/test_blueprint_schema.py tests/services/test_blueprint_execution.py tests/services/test_blueprint_quality.py tests/delivery/test_blueprint_artifact_wiring.py tests/delivery/test_evaluate_blueprint_golden.py tests/delivery/test_blueprint_integration.py tests/test_blueprint_settings.py tests/delivery/test_blueprint_event_taxonomy_112.py -q` 全绿。
- `cd server && uv run python manage.py evaluate_blueprint_golden` 退出码 0。
- `cd server && uv run python manage.py makemigrations --check --dry-run` 退出码 0。
- 冻结面自检：`git diff --name-only` 不含 `codegraph/services/repo_router_v2.py`、`process_runtime/{decompose_segments,research_adapter,architect_merge_adapter,merged_plan,clarify_adapter,render,resume,builtin_processes}.py`。
- `git diff server/system/settings_service.py` 只有新增两个函数的 hunk（既有 getter 无删改行）。
</verification>

<success_criteria>
- FLOW-01 前置：每个 feature_point 必须带 greenfield/brownfield/fix 意图标注，schema 层强制（ROADMAP SC1 后半的数据契约）。
- CHARTER-02 前置：路由意图权重与歧义阈值运行时可调（CONTEXT 锁定的权重外置），默认总分阈值 0.20。
- 既有 6 个蓝图消费测试与 golden command 在 schema 破坏性演进后零改动通过（111 底座不回退）。
</success_criteria>

<output>
完成后创建 `.planning/phases/112-1/112-01-SUMMARY.md`（provides：feature_points[].intent 枚举、两个 SettingKeys 键与默认值形状、aget_float/json_setting、11 个 112 事件常量——112-02/03/04/05 全部依赖本 plan 的产出）。
</output>
