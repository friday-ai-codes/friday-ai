---
phase: 39-parallel-research
plan: 04
type: execute
wave: 4
depends_on: [39-03]
files_modified:
  - server/services/plan_orchestration/engine.py
  - server/services/plan_orchestration/research_aggregation.py
  - server/services/plan_orchestration/__init__.py
  - server/subagent/api/callbacks.py
  - server/services/indexer.py
  - server/tests/services/test_research_aggregation.py
  - server/tests/services/test_research_completion_callback.py
  - server/tests/services/test_research_stale_hook.py
autonomous: true
requirements: [RESEARCH-01, RESEARCH-02, RESEARCH-03]
must_haves:
  truths:
    - "barrier 聚合：所有 RepoResearchTask 终态（done/failed）→ 经 PlanSessionService.transition 推 research_complete（researching→merging，§14）；仍有 pending/running/stale 则 researching 自留等待"
    - "容器回调解析 SubAgentSession/TaskResult → 结构化 PartialPlan（§7）经 ResearchService.record_partial 落库；非结构化优雅降级为 file 级摘要 partial；空/异常 → mark_failed"
    - "调研全程 emit repo.research.completed / repo.research.failed（§15 payload）"
    - "重索引 stale hook：indexer FINALIZING best-effort 调 ResearchService.invalidate_for_repo（Phase 24/25 范式），失败绝不阻断索引（RESEARCH-03）"
    - "engine._research 驱动 fan-out + barrier 驱动转移；engine 绝不直接 mutate session.status（purity guard 保持绿）"
  artifacts:
    - path: "server/services/plan_orchestration/research_aggregation.py"
      provides: "barrier 完成判定 + research_complete 驱动 + PartialPlan 结果解析（结构化+降级）"
      contains: "def parse_partial_plan_content"
    - path: "server/services/indexer.py"
      provides: "_run_research_stale_invalidation best-effort 钩子"
      contains: "_run_research_stale_invalidation"
  key_links:
    - from: "subagent/api/callbacks.py:_handle_completed"
      to: "research_aggregation 结果解析 + barrier 触发"
      via: "plan_research source 路由 → record_partial → amaybe_complete_research"
      pattern: "plan_research"
    - from: "services/indexer.py:clone_and_index_repository"
      to: "_run_research_stale_invalidation → ResearchService.invalidate_for_repo"
      via: "FINALIZING best-effort hook（base-only）"
      pattern: "_run_research_stale_invalidation"
    - from: "research_aggregation:amaybe_complete_research"
      to: "PlanSessionService.transition(research_complete)"
      via: "all terminal → merging"
      pattern: "research_complete"
---

<objective>
合上 map 段闭环：barrier 聚合（所有 RepoResearchTask 终态 → 经 service 推 research_complete →
merging，§14）+ 容器回调结果解析（SubAgentSession/TaskResult → 结构化 PartialPlan §7，非结构化
优雅降级）+ §15 调研事件（completed/failed）+ 重索引 stale 失效钩子（挂 indexer FINALIZING
best-effort，Phase 24/25 范式）+ engine `_research` 接线（dispatch 触发 fan-out，barrier/回调驱动
转移，engine 不直接写 status）。

Purpose: 39-03 把容器派出去（fire-and-forget）；本 plan 让结果回流落 partial、让 barrier 在所有仓
终态时推进编排、让重索引把过期 partial 置 stale。这是 RESEARCH-01 产物闭环 + RESEARCH-03 stale +
§15 事件的落点，且保持 engine 纯度（resume 经持久化状态驱动，对齐既有 waiting_event/callback 范式）。
Output: research_aggregation 模块 + engine._research 改写 + callback 接线 + indexer hook + 全 mock 单测。
**真实容器 E2E DEFERRED**（mock payload 覆盖回调解析/聚合/事件；真实 runner+docker 端到端延后）。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-parallel-research/39-CONTEXT.md
@.planning/DOMAIN-MODEL.md
@server/services/plan_orchestration/engine.py
@server/services/plan_orchestration/protocols.py
@server/delivery/services/plan_session_service.py
@server/delivery/services/research_service.py
@server/orchestration/barrier.py
@server/subagent/api/callbacks.py
@server/services/indexer.py
@server/tests/test_callbacks_cross_repo_relevance.py
@server/tests/services/test_plan_orchestration_engine.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: barrier 聚合 + 结果解析 + engine._research 接线</name>
  <files>server/services/plan_orchestration/research_aggregation.py, server/services/plan_orchestration/engine.py, server/services/plan_orchestration/__init__.py</files>
  <action>
  1. 新建 `research_aggregation.py`（zh-CN docstring 引 §7/§14，ruff line 100）：
     - `TERMINAL_STATUSES = {done, failed}`（barrier 完成判定集；**stale/pending/running 非终态**——
       stale 须重跑后才满足，per §14「所有 RepoResearchTask done/failed → merging」）。
     - `async def aall_research_tasks_terminal(session_id) -> bool`：该 session 至少有一个
       RepoResearchTask，且无任何 status in {pending, running, stale}（即全部 done/failed）。
       无任何 task → 返回 True（无需调研，直接可推进）。async ORM（aexists/acount）。
     - `async def amaybe_complete_research(session, *, session_service=None) -> bool`：guard
       `session.status == RESEARCHING`；若 `aall_research_tasks_terminal` → 经
       `(session_service or PlanSessionService()).transition(session, "research_complete")`
       推 researching→merging（**经 service 转移，不直接写 status**——engine 纯度）；return 是否推进。
       已非 researching（并发已推进）→ no-op return False。
     - `def parse_partial_plan_content(raw, *, repository_id) -> dict | None`（纯函数，结果解析）：
       入参 raw = TaskResult.raw_output(dict) 或 text_output(str)。优先从 raw 提结构化 §7 字段
       （research_summary/proposed_changes/candidate_files/api_contracts_exposed/
       dependencies_on_other_repos）——容器若返回 JSON dict 含这些键则直采；若仅有自由文本 →
       **优雅降级** file 级摘要 partial：`{repository_id, research_summary: text[:4000],
       proposed_changes: [], candidate_files: [], api_contracts_exposed: [],
       dependencies_on_other_repos: []}`；raw 为空/无可用文本 → 返回 None（调用方 mark_failed）。
       始终回填 `repository_id` 字段，列表字段缺失补 []（schema 健壮）。

  2. 改写 `engine.py:_research`（保持 engine 不直接写 status——purity guard）：
     - `await self.research.dispatch(session)`（39-03 adapter：filter + fan-out + 建 task + 派容器 +
       轻量合成）。
     - `from services.plan_orchestration.research_aggregation import amaybe_complete_research`；
       `completed = await amaybe_complete_research(session, session_service=self.session_service)`。
     - 若 `not completed`（仍有 pending/running 深入仓在途）→
       `await self.session_service.transition(session, "research_dispatched")`（researching 自留等待，
       §14 fan-out 等待行；barrier 由容器回调后续驱动 research_complete）。
     - 更新 `_research` docstring：去掉「简化为 dispatch 后直接 research_complete」TODO，写明
       barrier/回调驱动转移（对齐既有 AICodingNode waiting_event/callback resume 范式）。
     - 不删 `ResearchProtocol`/注入点；默认注入仍可由组装层换成 39-03 ResearchDispatchAdapter。

  3. `plan_orchestration/__init__.py`：re-export `amaybe_complete_research` /
     `aall_research_tasks_terminal` / `parse_partial_plan_content`（供 callback 复用）+ __all__。
  </action>
  <verify>
    <automated>cd server &amp;&amp; uv run pytest tests/services/test_research_aggregation.py tests/services/test_plan_orchestration_engine.py -x -q</automated>
  </verify>
  <done>aggregation 模块 3 函数行为正确；engine._research dispatch 后按 barrier 推 research_complete 或自留 research_dispatched；engine purity guard（test_engine_does_not_write_status_directly）保持绿；结果解析结构化+降级+None 三路覆盖。</done>
</task>

<task type="auto">
  <name>Task 2: 容器回调解析落 partial + §15 事件 + barrier 触发</name>
  <files>server/subagent/api/callbacks.py, server/tests/services/test_research_completion_callback.py</files>
  <action>
  在 `subagent/api/callbacks.py` 新增 best-effort helper（mirror cross_repo_relevance 范式：
  try/except swallow + warning，绝不让回调失败）：

  `async def _handle_research_completion(session, p, log)`：仅当
  `session.task_type == SubAgentSession.TaskType.PLAN and isinstance(session.last_output, dict)
  and session.last_output.get("source") == "plan_research"` 触发（路由判定）。流程：
  1. 取 `research_task_id` / `plan_session_id` 自 session.last_output；async 读 RepoResearchTask
     （select_related session + repository）；缺失/已终态 → no-op（幂等）。
  2. `content = parse_partial_plan_content(p.get("output") or {}, repository_id=str(task.repository_id))`
     （先试 raw_output dict，回退 text）。
  3. `content is None`（空/不可解析）→ `ResearchService().mark_failed(task, {"reason":
     "empty_or_unparseable_result"})` + emit `repo.research.failed`（§15 payload `{repo_id, task_id,
     error}`）。
  4. 否则 → `ResearchService().record_partial(task, content)`（落 §7 PartialPlan + 置 done）+ emit
     `repo.research.completed`（§15 payload `{repo_id, task_id, summary, candidate_files,
     api_contracts_exposed}`，取自 content）。事件经 `PlanSessionService()._emit_event` 钩子。
  5. barrier 触发：读回 PlanSession（async）→ `await amaybe_complete_research(session)`（所有终态则
     推 research_complete→merging；engine resume 时也会再判一次，幂等安全）。

  在 `_handle_completed` 末尾（创建 TaskResult、amark_completed 之后，与 cross_repo_relevance 钩子
  并列）追加：`await _handle_research_completion(session, p, log)`（独立 try/except，失败 swallow，
  保回调返 200）。同样在 `_handle_failed` 末尾追加研究失败路由：若 plan_research → mark_failed +
  emit repo.research.failed + amaybe_complete_research（failed 也是 barrier 终态，不卡死 merging）。
  </action>
  <verify>
    <automated>cd server &amp;&amp; uv run pytest tests/services/test_research_completion_callback.py -x -q</automated>
  </verify>
  <done>plan_research 容器完成回调 → 结构化/降级 partial 落库 + repo.research.completed；空结果 → mark_failed + repo.research.failed；所有终态触发 research_complete；非 plan_research 任务不触发；回调异常被 swallow 返 200。</done>
</task>

<task type="auto">
  <name>Task 3: 重索引 stale 失效钩子（indexer FINALIZING best-effort）</name>
  <files>server/services/indexer.py, server/tests/services/test_research_stale_hook.py</files>
  <action>
  在 `services/indexer.py` 新增 `async def _run_research_stale_invalidation(repository_id: str) ->
  None`（紧邻 `_run_modifies_chunk_reconcile`，同款 zh-CN docstring + 同契约）：整段 try/except，
  `from delivery.services import ResearchService`；`invalidated = await
  ResearchService().invalidate_for_repo(repository_id)`；log.info 成功计数；任何异常仅
  `logger.warning("research_stale_invalidation_failed", ...)`，**绝不抛、绝不阻断索引 success 终态**
  （对齐 D-04 / Phase 24/25 best-effort 范式）。

  在 `clone_and_index_repository` 的 base-only 钩子段（`if not branch:` 块，紧跟
  `await _run_modifies_chunk_reconcile(repository_id)` 之后）追加
  `await _run_research_stale_invalidation(repository_id)`——与既有钩子同时序（rmtree 之前不要求，
  本钩子纯 DB 操作不读克隆目录，放在对账钩子之后即可）。仅 base 索引路径触发（功能分支 overlay
  不触发，与既有钩子一致）。
  </action>
  <verify>
    <automated>cd server &amp;&amp; uv run pytest tests/services/test_research_stale_hook.py -x -q</automated>
  </verify>
  <done>_run_research_stale_invalidation 调 ResearchService.invalidate_for_repo 置 stale；service 抛异常时仅 warning 不冒泡；钩子接入 base-only FINALIZING 段。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 容器→server 回调 | runner 上报的 TaskResult/output 是半可信输入，解析为 PartialPlan 须健壮（降级不崩） |
| indexer→ResearchService | stale 失效是后台 best-effort，失败隔离不影响索引主轨 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-04-01 | Tampering | 容器返回畸形/恶意 JSON | mitigate | parse_partial_plan_content 健壮解析（缺字段补 []、非结构化降级 file 级摘要、空→failed），不 eval/不执行返回内容 |
| T-39-04-02 | Denial of Service | 单仓失败/无结果卡死 merging | mitigate | failed 也是 barrier 终态；_handle_failed 同样触发 amaybe_complete_research，barrier 不悬挂 |
| T-39-04-03 | Denial of Service | stale hook 异常阻断索引 | mitigate | _run_research_stale_invalidation 整段 try/except + warning，绝不阻断索引 success（Phase 24/25 契约） |
| T-39-04-04 | Tampering | engine 旁路写 status | mitigate | engine._research 只经 session_service.transition；test_engine_does_not_write_status_directly 守护 |
| T-39-04-SC | Tampering | 依赖安装 | accept | 复用既有 callbacks/indexer/barrier，无新增三方依赖，包合法性门 N/A |
</threat_model>

<verification>
- `uv run pytest tests/services/test_research_aggregation.py tests/services/test_research_completion_callback.py tests/services/test_research_stale_hook.py tests/services/test_plan_orchestration_engine.py -x -q` 全绿。
- engine 纯度守护 `test_engine_does_not_write_status_directly` 保持绿。
- `uv run ruff check services/plan_orchestration/ subagent/api/callbacks.py services/indexer.py`（line 100）通过。
- `uv run python manage.py makemigrations --check --dry-run` 干净（本 plan 不改模型，应零新增 migration）。
</verification>

<success_criteria>
- barrier 聚合（§14）：所有 RepoResearchTask done/failed → 经 service 推 research_complete（researching→merging）；在途则 researching 自留等待。
- 容器回调解析（RESEARCH-01）：结构化 PartialPlan §7 落库；非结构化优雅降级 file 级摘要；空/异常 → mark_failed（不污染其他仓）。
- §15 事件：repo.research.completed / repo.research.failed 经 _emit_event 钩子产出。
- 重索引 stale hook（RESEARCH-03）：indexer FINALIZING best-effort 调 invalidate_for_repo，失败绝不阻断索引。
- engine._research 接线且 engine 纯度守护保持绿；全 mock 单测覆盖，真实容器 E2E DEFERRED。
</success_criteria>

<output>
Create `.planning/phases/39-parallel-research/39-04-SUMMARY.md` when done.
</output>
