---
phase: 17-varref
plan: 03
type: execute
wave: 2
depends_on: [17-01]
files_modified:
  - server/workflows/engine/scheduler.py
  - server/tests/workflows/test_error_handling.py
autonomous: true
requirements: [VAR-02]

must_haves:
  truths:
    - "节点 config 引用不可解析时，该节点显式失败（NodeExecution status=failed），不进入节点业务逻辑"
    - "NodeExecution.error_message 同时含中文人类可读文案与结构化字段（reference/reason/available/template），Phase 21 可直接 JSON.parse 最后一行"
    - "所有调用 render_template/get_template_value 的节点均在业务副作用前渲染，且无调用方把解析错误吞成其他语义"
  artifacts:
    - path: "server/workflows/engine/scheduler.py"
      provides: "_execute_node 识别 TemplateResolutionError 并写结构化 error_message"
      contains: "TemplateResolutionError"
    - path: "server/tests/workflows/test_error_handling.py"
      provides: "解析失败 → error_message 端到端集成测试"
  key_links:
    - from: "server/workflows/engine/scheduler.py"
      to: "server/workflows/engine/template_resolver.py"
      via: "except 分支 isinstance 识别类型化异常"
      pattern: "isinstance.*TemplateResolutionError"
---

<objective>
把 17-01 定稿的类型化解析异常接入执行引擎的错误落盘路径（VAR-02 集成半边）：scheduler 识别 TemplateResolutionError 写入「中文一句话 + 结构化 JSON」的 error_message；并对 ~20 个调用 render_template/get_template_value 的节点做调用面审计与全量回归。

Purpose: 锁定决策要求结构化字段（reference/reason/available）落入 NodeExecution.error_message 供 Phase 21 错误展示直接复用；RESEARCH Assumption A1（节点渲染先于副作用）仅抽样验证过，需逐一核查。
Output: scheduler 结构化错误写入 + 集成测试 + 调用面审计结论（SUMMARY 记录）+ 后端 workflows 全量绿。
</objective>

<context>
@.planning/phases/17-varref/17-CONTEXT.md
@.planning/phases/17-varref/17-RESEARCH.md
@.planning/phases/17-varref/17-01-SUMMARY.md
@server/workflows/engine/scheduler.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: scheduler 结构化 error_message 写入 + 端到端集成测试</name>
  <files>server/workflows/engine/scheduler.py, server/tests/workflows/test_error_handling.py</files>
  <read_first>
    - server/workflows/engine/scheduler.py（:999-1045 _execute_node 的 except 分支与 amark_failed 调用——插入点；`_map_error_code` 方法全文——error_code 取值风格）
    - server/workflows/engine/template_resolver.py（17-01 产出的 TemplateResolutionError 属性名）
    - server/tests/workflows/test_error_handling.py（既有测试结构：execution 构造方式、engine 调用范式——新用例照抄范式）
    - server/workflows/models/execution.py（amark_failed :647-655——error_message 落盘行为，确认无长度截断风险）
  </read_first>
  <action>
    1. scheduler.py 顶部导入 `from workflows.engine.template_resolver import TemplateResolutionError`。在 `_execute_node` 的 `except Exception as _exc:` 分支（:1013）内、现有 `last_error = str(_exc)` 逻辑之前，增加分支：`isinstance(_exc, TemplateResolutionError)` 时 `last_error = f"{_exc}\n{json.dumps({'reference': _exc.reference, 'reason': _exc.reason, 'available': _exc.available, 'template': _exc.template}, ensure_ascii=False)}"`（编码格式 per RESEARCH Open Question #3 推荐：中文一句话 + 换行 + JSON 最后一行，Phase 21 可 JSON.parse 最后一行；json 导入确认 scheduler 已有，缺则补）。后续 amark_failed/aappend_log 复用 last_error，不另开字段（锁定决策：不开新 JSONField）。
    2. 查看 `_map_error_code` 现有分支取值风格，按同风格增加 TemplateResolutionError 映射（如已有 `validation_error`/`timeout` 之类小写蛇形，则用 `template_resolution_error`；若是其他风格则对齐——读后定）。
    3. test_error_handling.py 增加集成用例（async 范式照抄文件内既有用例）：
       a. test_template_resolution_error_writes_structured_message：构造 workflow（一个上游节点 + 一个下游节点，下游 config 含 `{{nodes.<上游short_id>.no_such_field}}`，节点类型选文件内既有用例已使用的、execute 顶部渲染 config 的类型），运行 engine，断言下游 NodeExecution.status == failed、error_message 含「不存在」、`json.loads(error_message.splitlines()[-1])` 成功且含 reason == "field_not_found" 与 available 列表
       b. test_unknown_prefix_error_structured：config 含 `{{foo.bar}}` → reason == "unknown_prefix"
       c. 断言两用例中节点业务逻辑未执行（fail-fast：以该节点类型无输出/无副作用产物佐证，按所选节点类型具体化）
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/workflows/test_error_handling.py -q</automated>
  </verify>
  <acceptance_criteria>
    - scheduler.py 的 _execute_node except 分支含 `isinstance(_exc, TemplateResolutionError)` 判断，结构化 JSON 用 `ensure_ascii=False`
    - 集成用例断言 error_message 最后一行可被 json.loads 且含 reference/reason/available/template 四键
    - `cd server && uv run pytest tests/workflows/test_error_handling.py -q` 全绿
    - error_message 的 JSON 中 available 只含键名不含上游输出值（用例显式断言上游输出的 value 字符串不出现在 error_message 中）
  </acceptance_criteria>
  <done>解析失败端到端落为 failed + 结构化 error_message，Phase 21 消费契约成立</done>
</task>

<task type="auto">
  <name>Task 2: 节点调用面审计（A1 验证）+ 后端全量回归</name>
  <files>server/workflows/engine/scheduler.py</files>
  <read_first>
    - .planning/phases/17-varref/17-RESEARCH.md「现状链路 §2 调用面」与「Assumptions Log A1」
    - `rg -n "render_template|get_template_value" server/workflows/nodes server/prompts` 的全部命中文件（~20 个节点文件 + prompts/services.py——逐文件核查命中处上下文）
  </read_first>
  <action>
    1. 运行 `rg -n "render_template\(|get_template_value\(" server/workflows/nodes/ server/prompts/` 列出全部调用点。对每个调用点核查两件事：
       a. 渲染是否发生在业务副作用（HTTP 请求、git 操作、DB 写入、飞书 API 调用）之前——验证 RESEARCH Assumption A1（fail-fast 时机依赖此事实）
       b. 渲染调用是否被裸 `try/except Exception`（或 `except ValueError`）包住且未重新抛出/未返回 NodeResult(failed)——即是否会把 TemplateResolutionError 吞成静默继续
    2. 发现违例时就地修复：渲染调用提前到副作用前，或在 except 中改为 `raise` / 返回 `NodeResult(status="failed", error=str(exc))`（修复文件加入本任务 files 并在 SUMMARY 列明）。抽样已证 prompt.py 无问题；预期违例为零或个位数。
    3. 审计结论以表格写入 17-03-SUMMARY.md（文件 → 调用点行号 → 渲染先于副作用 Y/N → 是否吞异常 Y/N → 处置）。
    4. 跑后端 workflows 全量 + 全套件抽查：`cd server && uv run pytest tests/workflows/ -q`，再 `cd server && uv run pytest -q`（若全套件因与本阶段无关的既有失败而红，记录失败用例名并确认与本阶段改动无关——以 git stash 对照或失败信息判断）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/workflows/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `rg` 列出的全部调用点逐一核查，结论表（≥15 行）写入 SUMMARY
    - 无任何调用点把 TemplateResolutionError 吞成静默继续（违例已修复并在 SUMMARY 标注）
    - `cd server && uv run pytest tests/workflows/ -q` 全绿
  </acceptance_criteria>
  <done>A1 假设被逐点证实或修复，解析严格化后无节点静默吞错，后端回归绿</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 节点输出 → error_message | 错误结构化内容会经 API/WS 暴露给前端 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-21 | Information Disclosure | error_message 结构化 JSON | mitigate | available 只列键名；template 字段仅含用户自己写的模板片段不含渲染值；集成用例断言上游输出值不出现在 error_message |
| T-17-SC | Tampering | npm/pip/cargo installs | accept | 本计划零新依赖、无包安装任务 |
</threat_model>

<verification>
- `cd server && uv run pytest tests/workflows/test_error_handling.py -q` 全绿
- `cd server && uv run pytest tests/workflows/ -q` 全绿
- SUMMARY 含完整调用面审计表
</verification>

<success_criteria>
- ROADMAP 成功标准 2 的端到端半边：解析失败 → 节点显式 failed → error_message 指明引用/节点/字段且结构化，与 17-01 合并后标准 2 完整达成
</success_criteria>

<output>
完成后创建 `.planning/phases/17-varref/17-03-SUMMARY.md`
</output>
