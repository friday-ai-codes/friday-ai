---
phase: 17-varref
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - server/workflows/templates/loader.py
  - server/workflows/api/views.py
  - server/tests/workflows/test_bulk_update_short_id.py
autonomous: true
requirements: [VAR-01]
must_haves:
  truths:
    - "bulk-update 保存时客户端提供的 short_id 直接落库为权威值"
    - "short_id 缺失（新节点）或冲突或非法格式时，服务端生成新值并同事务重写该工作流所有节点 config 中引用旧值的 {{nodes.<old>.*}}（含 $nodes. JSONPath 形式）"
    - "保存成功 ⇒ 引用可解析：保存后 config 中全部 nodes.* 引用的标识符都属于该工作流的 short_id 或 UUID 集合（核心不变式）"
    - "更新已有节点时 payload 不含 short_id 不会导致其被重置（存量行为不回退）"
  artifacts:
    - path: "server/tests/workflows/test_bulk_update_short_id.py"
      provides: "VAR-01 集成测试：落库/唯一性/重写/不变式"
      min_lines: 120
    - path: "server/workflows/templates/loader.py"
      provides: "公共重写引擎 rewrite_template_refs（由 _rewrite_template_refs 公共化）"
      contains: "def rewrite_template_refs"
  key_links:
    - from: "server/workflows/api/views.py"
      to: "server/workflows/templates/loader.py"
      via: "_bulk_update_nodes_and_edges 事务内调用重写引擎"
      pattern: "rewrite_template_refs"
    - from: "server/workflows/api/views.py"
      to: "server/common/short_id.py"
      via: "冲突/缺失时重生成"
      pattern: "generate_unique_short_id"
---

<objective>
打通 bulk-update 保存路径的 short_id 收敛（VAR-01）：客户端 short_id 权威落库、工作流内唯一性应用层校验、缺失/冲突/非法时服务端重生成并同事务重写全部 config 引用，建立"保存成功 ⇒ 引用可解析"不变式。

Purpose: 消除 short_id 漂移的服务端半边根因（前端发送侧由 17-03 计划补齐）；不做历史数据迁移，靠保存路径收敛（锁定决策）。
Output: `_bulk_update_nodes_and_edges` 含 short_id 解析/校验/重写逻辑；`rewrite_template_refs` 公共化；集成测试锁定不变式。
</objective>

<context>
@.planning/phases/17-varref/17-CONTEXT.md
@.planning/phases/17-varref/17-RESEARCH.md
@server/workflows/api/views.py
@server/workflows/templates/loader.py
@server/common/short_id.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 公共化重写引擎 + bulk-update 事务内 short_id 解析/唯一性/重写</name>
  <files>server/workflows/templates/loader.py, server/workflows/api/views.py</files>
  <read_first>
    - server/workflows/api/views.py（:153-201 _bulk_update_nodes_and_edges 现状——update 走 WorkflowNodeSerializer(partial)、create 走 WorkflowNodeCreateSerializer；:491-517 bulk_update action）
    - server/workflows/templates/loader.py（:63-95 _rewrite_template_refs——正则已覆盖 $nodes. JSONPath 形式与 re.escape，勿重写）
    - server/common/short_id.py（generate_unique_short_id 签名：existing_ids 集合，冲突自动 3→12 位加长）
    - server/workflows/api/serializers.py（:184-238 WorkflowNodeSerializer short_id 在 read_only_fields——本任务刻意不改 serializer，见 action 设计依据）
    - .planning/phases/17-varref/17-RESEARCH.md（Pattern 3、Pitfall 4/5、Assumptions A2）
  </read_first>
  <action>
    1. loader.py：将 `_rewrite_template_refs` 重命名为公共函数 `rewrite_template_refs`（docstring 更新为通用描述：把 config 中 `{{nodes.<old>.*}}` 与 `{{$nodes.<old>.*}}` 引用按 id_map 重写），模块内原调用点同步改名；保留 `_rewrite_template_refs = rewrite_template_refs` 私有别名不必要——直接全改，loader 内仅一处调用。
    2. views.py `_bulk_update_nodes_and_edges` 事务内增加 short_id 处理（serializer 完全不动——short_id 留在 read_only_fields，DRF 自动忽略 payload 中该字段，函数内显式读 `node_data.get("short_id")` 处理；此设计规避 Pitfall 5：单节点 node_detail PUT 路径行为零变化，无需为其补唯一性校验）：
       a. 第一遍历前：加载该工作流全部现有节点的 `{id: short_id}` 映射（含本次 payload 未涉及的节点）。
       b. 逐节点解析最终 short_id：格式白名单 `^[A-Za-z][A-Za-z0-9]{0,11}$`（与 common/short_id.py 约束一致，拒绝 `.`/`{`/`}`/空白等破坏模板语法或重写正则的字符，ASVS V5）。客户端值合法且未被本工作流其他节点占用（先到先得，按 payload 顺序；update 节点对自身现值不算冲突）→ 采纳为权威值；update 节点 payload 缺失 short_id → 保留 DB 现值（存量行为不回退）；新节点缺失 / 任何节点冲突或非法 → `generate_unique_short_id(已占用集合)` 重生成。存量 DB 内已有重复（A2：模型无 unique 约束）视为冲突走同一重生成路径自愈。
       c. 记录重写映射 id_map：仅当客户端显式提供了 short_id、最终值 ≠ 客户端值、且客户端值在最终分配后不再属于该工作流任何节点时，写入 `client_value → final_value`（最后一条防卫避免把指向"合法占用者"的引用误改）。
       d. 落库：update 分支 serializer.save() 后显式 `node.short_id = final; node.save(update_fields=["short_id"])`（仅变化时）；create 分支 `WorkflowNode.objects.create(..., short_id=final)`。
       e. id_map 非空时：对该工作流**全部**节点（含本次未改动的）执行 `node.config = rewrite_template_refs(node.config, id_map)` 并保存（仅 config 变化的节点 save）。重写范围严格限定 `workflow.nodes`（事务函数作用域天然满足，越权防护由 Task 2 测试锁定）。
    3. 函数 docstring 补充中文注释说明不变式与设计取舍（为何不动 serializer）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/workflows/test_api.py tests/workflows/test_template_loader.py -q</automated>
  </verify>
  <acceptance_criteria>
    - loader.py 存在 `def rewrite_template_refs(` 且 `rg -c "_rewrite_template_refs" server/workflows/` 退出码非 0（旧私有名已全部替换）
    - views.py `_bulk_update_nodes_and_edges` 内出现 `generate_unique_short_id` 与 `rewrite_template_refs` 调用，且 short_id 格式校验正则为 `^[A-Za-z][A-Za-z0-9]{0,11}$`
    - serializers.py 本任务零改动（git diff 该文件为空），node_detail 单节点 PUT 行为不变
    - 既有 `tests/workflows/test_api.py`、`test_template_loader.py` 全绿（无回归）
  </acceptance_criteria>
  <done>bulk-update 事务内完成 short_id 权威落库、应用层唯一性、缺失/冲突/非法重生成与全工作流 config 引用重写；重写引擎复用既有实现。</done>
</task>

<task type="auto">
  <name>Task 2: VAR-01 集成测试——落库/重写/不变式/越权防护</name>
  <files>server/tests/workflows/test_bulk_update_short_id.py</files>
  <read_first>
    - server/workflows/api/views.py（Task 1 改造后的 _bulk_update_nodes_and_edges——测试直接调用该同步函数，绕开 HTTP 层鉴权噪音）
    - server/tests/workflows/conftest.py（obs_project/obs_workflow 等工厂 fixture 范式）
    - server/tests/workflows/test_api.py（既有 bulk-update 相关测试的数据构造方式）
    - web 端口径参考：.planning/phases/17-varref/17-RESEARCH.md（Pitfall 4——bulk_update 响应 WorkflowSerializer 返回 DB 状态天然含重写后 config）
  </read_first>
  <action>
    新建 `server/tests/workflows/test_bulk_update_short_id.py`（django_db，直接调用 `_bulk_update_nodes_and_edges`；响应序列化断言用 WorkflowSerializer），覆盖：
    1. 客户端 short_id 落库：新节点 payload 含 short_id="aB1" → DB 中该节点 short_id == "aB1"
    2. 新节点缺失 short_id → 服务端生成（匹配 `^[A-Za-z][A-Za-z0-9]{2,11}$`）且工作流内唯一
    3. update 节点 payload 不含 short_id → DB 现值保留不变
    4. 冲突重生成 + 重写：两个新节点都声明 "aB1"，第二个节点的 config 含 `{{nodes.aB1.out}}` 与 `{{$nodes.aB1.items[*].x}}`——按先到先得语义，后者重生成为新值 N，且**该工作流全部节点** config 中… 注意此场景客户端值仍被首个节点合法占用，按 Task 1 防卫规则 id_map 不得包含 aB1（断言第二个节点 config 引用保持 aB1 原样——指向首个节点，语义正确）
    5. 非法格式重生成 + 重写：单个新节点声明 short_id="a.B{1"（非法），config 含 `{{nodes.a.B{1.out}}` 不构造（非法字符无法成为合法引用）——改为：节点 A 声明合法值 "xY9" 但与工作流既有节点冲突，A 自己与邻节点 config 含 `{{nodes.xY9.out}}`；重生成为 N 后断言两节点 config 都被重写为 `{{nodes.N.out}}`、`{{$nodes.xY9.*}}` 形式同步重写、未涉及该引用的字符串保持原样
    6. 不变式测试（命名 test_invariant_save_implies_resolvable）：构造多节点混合场景保存后，收集工作流全部 config 中 `\{\{\s*(?:\$\.?)?nodes\.([A-Za-z0-9-]+)\.` 捕获的标识符集合，断言每个标识符 ∈ {全部节点 short_id} ∪ {全部节点 UUID 字符串}
    7. 越权防护（T-17-11）：另建第二个 workflow，其节点 config 含 `{{nodes.xY9.out}}`；对第一个 workflow 触发含 xY9 的冲突重写后，断言第二个 workflow 的 config 原样未动
    8. 响应契约（Pitfall 4）：bulk-update 后用 WorkflowSerializer 序列化 workflow，断言 nodes[].config 为重写后内容、nodes[].short_id 为最终权威值
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/workflows/test_bulk_update_short_id.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `server/tests/workflows/test_bulk_update_short_id.py` 存在且包含名为 test_invariant_save_implies_resolvable 的测试
    - 用例覆盖 action 列出的 8 个场景（落库/生成/保留/冲突先到先得/冲突重写含 $nodes. 形式/不变式/跨工作流越权/响应契约）
    - `cd server && uv run pytest tests/workflows/test_bulk_update_short_id.py -q` 全绿
  </acceptance_criteria>
  <done>"保存成功 ⇒ 引用可解析"不变式与重写边界（先到先得防卫、跨工作流隔离、JSONPath 形式覆盖）全部被自动化测试锁定。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 客户端 bulk-update payload → 服务端落库 | short_id 与 config 均为不可信用户输入 |
| 引用重写引擎 → 全工作流节点 config | 服务端自动改写用户数据，范围必须受控 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-10 | Tampering | 恶意 short_id 注入重写正则/模板语法 | mitigate | 落库前白名单 `^[A-Za-z][A-Za-z0-9]{0,11}$`；rewrite_template_refs 内 re.escape 双保险（Task 1） |
| T-17-11 | Elevation of Privilege | 跨工作流引用重写越权 | mitigate | 重写严格限定 workflow.nodes 作用域；Task 2 场景 7 测试锁定 |
| T-17-12 | Spoofing | 绕过鉴权调用 bulk-update | accept | 既有 IsAuthenticated + WorkflowPermission + ProjectScopedQueryset 不动（本计划不触碰权限层） |
| T-17-SC | Tampering | npm/pip/cargo installs | accept | 本计划零新依赖（RESEARCH Package Legitimacy Audit：无安装） |
</threat_model>

<verification>
- `cd server && uv run pytest tests/workflows/test_bulk_update_short_id.py tests/workflows/test_api.py tests/workflows/test_template_loader.py -q` 全绿
- `cd server && uv run ruff check workflows/api/views.py workflows/templates/loader.py` 无报错
</verification>

<success_criteria>
- ROADMAP 成功标准 1（服务端半边）：bulk-update 落库客户端 short_id 或重写 config 引用，保存后引用不再因 short_id 漂移抛 ValueError
- 不变式"保存成功 ⇒ 引用可解析"有专项自动化测试（CONTEXT Specific Ideas）
- 工作流导入路径（Workflow.from_json）漂移为已知缺口，移交 Phase 20（RESEARCH OQ#2，本阶段按 ROADMAP 只锁 bulk-update）
</success_criteria>

<output>
完成后创建 `.planning/phases/17-varref/17-02-SUMMARY.md`
</output>
