---
phase: 29-comment-events
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/delivery/models/comment_event.py
  - server/delivery/models/__init__.py
  - server/delivery/migrations/0002_workitemcommentevent.py
  - server/tests/delivery/test_comment_event_models.py
autonomous: true
requirements: [CMT-01, CMT-02]
must_haves:
  truths:
    - "WorkItemCommentEvent 表存在，可落 created/replied/edited/deleted/approval 五种事件类型"
    - "append-only：编辑/删除是新事件行，模型层无就地改写既有行的逻辑（CMT-02）"
    - "approval_semantic 可记 none/approve/reject，默认 none"
  artifacts:
    - path: "server/delivery/models/comment_event.py"
      provides: "WorkItemCommentEvent 模型 + CommentEventType + ApprovalSemantic 枚举"
      contains: "class WorkItemCommentEvent"
    - path: "server/delivery/migrations/0002_workitemcommentevent.py"
      provides: "comment_event 表迁移（含 (work_item, event_time) 索引）"
      contains: "WorkItemCommentEvent"
    - path: "server/delivery/models/__init__.py"
      provides: "curated re-export 追加 WorkItemCommentEvent / CommentEventType / ApprovalSemantic"
      contains: "WorkItemCommentEvent"
  key_links:
    - from: "server/delivery/models/comment_event.py"
      to: "delivery.WorkItem"
      via: "ForeignKey(on_delete=CASCADE, related_name=comment_events)"
      pattern: "ForeignKey"
---

<objective>
新增 append-only `WorkItemCommentEvent` 模型 + 迁移（delivery app），逐字段对齐 DOMAIN §12.4，为 Phase 29 评论事件流提供持久层底座。

Purpose: 评论以事件流入库（非快照），编辑/删除作为新事件不就地改写（CMT-01/CMT-02）；本 plan 只建表 + 枚举，落库业务逻辑归 29-02 service（INV-6 精神：单一写入入口在 service）。
Output: `WorkItemCommentEvent` 模型、`0002` 迁移（已 migrate 建表）、curated re-export、模型层单测。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-comment-events/29-CONTEXT.md
@.planning/DOMAIN-MODEL.md
@server/delivery/models/work_item.py
@server/delivery/models/status_event.py
@server/delivery/models/sync_state.py
@server/delivery/models/__init__.py
@server/delivery/migrations/0001_initial.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: WorkItemCommentEvent 模型 + 枚举</name>
  <files>server/delivery/models/comment_event.py, server/delivery/models/__init__.py</files>
  <read_first>
    - server/delivery/models/status_event.py（append-only 事件模型范式：UUID pk、work_item FK CASCADE、event_time/ingested_at、(work_item, event_time) 索引、db_table 命名、中文 docstring）
    - server/delivery/models/sync_state.py（TextChoices 枚举范式）
    - server/delivery/models/work_item.py（同 app FK 直接类引用）
    - server/delivery/models/__init__.py（curated re-export + __all__）
    - .planning/DOMAIN-MODEL.md §12.4 + §2（字段权威）
  </read_first>
  <action>
    新建 server/delivery/models/comment_event.py，逐字段对齐 DOMAIN §12.4：
    定义 `CommentEventType(models.TextChoices)` —— CREATED="created"、REPLIED="replied"、EDITED="edited"、DELETED="deleted"、APPROVAL="approval"（中文 label）。在 docstring 注明 edited/deleted 为枚举占位：若飞书 webhook/API 不提供编辑/删除信号，本 phase 仅实际落 created/replied/approval，edited/deleted 留位 deferred（per CONTEXT Grey Area 3，CMT-01）。
    定义 `ApprovalSemantic(models.TextChoices)` —— NONE="none"、APPROVE="approve"、REJECT="reject"（中文 label）。
    定义 `WorkItemCommentEvent(models.Model)`：
      - `id = UUIDField(primary_key=True, default=uuid.uuid4, editable=False)`
      - `work_item = ForeignKey(WorkItem, on_delete=CASCADE, related_name="comment_events")`
      - `feishu_comment_id = CharField(max_length=128)`
      - `thread_parent_id = CharField(max_length=128, blank=True, default="")`（线程父，根评论为空，CONTEXT）
      - `event_type = CharField(max_length=16, choices=CommentEventType.choices)`
      - `author = CharField(max_length=128, blank=True, default="")`
      - `body = TextField(blank=True, default="")`
      - `attachments = JSONField(default=list)`
      - `approval_semantic = CharField(max_length=16, choices=ApprovalSemantic.choices, default=ApprovalSemantic.NONE)`
      - `event_time = DateTimeField(null=True, blank=True)`
      - `ingested_at = DateTimeField(auto_now_add=True)`
      - Meta：db_table="delivery_work_item_comment_event"、中文 verbose_name、`indexes=[Index(fields=["work_item", "event_time"])]`（DOMAIN §12.4）。
    模型层不写任何 create/save 业务逻辑（落库归 29-02 service，守 INV-6 精神）；append-only 体现在「编辑/删除作为新事件行」由 service 保证，模型不含就地改写方法。
    在 server/delivery/models/__init__.py 追加 re-export：`WorkItemCommentEvent`、`CommentEventType`、`ApprovalSemantic`，并加入 `__all__`。
  </action>
  <verify>
    <automated>cd server && uv run python -c "import django,os;os.environ.setdefault('DJANGO_SETTINGS_MODULE','friday.settings');django.setup();from delivery.models import WorkItemCommentEvent, CommentEventType, ApprovalSemantic;print(WorkItemCommentEvent._meta.db_table, [c[0] for c in CommentEventType.choices], [c[0] for c in ApprovalSemantic.choices])"</automated>
  </verify>
  <acceptance_criteria>
    - `from delivery.models import WorkItemCommentEvent, CommentEventType, ApprovalSemantic` 成功。
    - 字段集合与 DOMAIN §12.4 逐字段一致；event_type 五值、approval_semantic 三值（默认 none）。
    - db_table=`delivery_work_item_comment_event`，含 `(work_item, event_time)` 索引。
    - 模型层无 create/save/就地改写业务方法。
  </acceptance_criteria>
  <done>WorkItemCommentEvent 模型与两枚举落地并 re-export，Django 可加载、字段对齐 §12.4。</done>
</task>

<task type="auto">
  <name>Task 2 [BLOCKING]: 生成并应用 migration + 模型层单测</name>
  <files>server/delivery/migrations/0002_workitemcommentevent.py, server/tests/delivery/test_comment_event_models.py</files>
  <read_first>
    - server/delivery/migrations/0001_initial.py（migration 风格 + 索引/约束写法）
    - server/tests/delivery/test_models.py（factory-boy + pytest-django 模型单测范式：字段读回、append-only、django_db）
  </read_first>
  <action>
    [BLOCKING] 新模型 = 新表，必须生成并应用迁移后下游 service/测试才能落库。
    运行 `uv run python manage.py makemigrations delivery`（working_directory=server）生成 `0002_workitemcommentevent.py`（依赖 0001_initial），随后 `uv run python manage.py migrate delivery` 应用建出 `delivery_work_item_comment_event` 表。若自动生成的 migration import 块触发 ruff I001，用 `uv run ruff check --fix` 规范化（生成产物格式收尾）。
    新建 server/tests/delivery/test_comment_event_models.py，pytest-django + factory-boy（或直接 ORM 建对象）覆盖：
      ① 五种 event_type + 三种 approval_semantic 可落库读回；attachments 默认 list、approval_semantic 默认 none；
      ② append-only：对同一 feishu_comment_id 先后写 created 再写 edited 两行，断言两行并存（不就地改写）、event_time/ingested_at 区分；
      ③ work_item CASCADE：删 WorkItem 关联 comment_events 随删；
      ④ (work_item, event_time) 索引存在（可经 `WorkItemCommentEvent._meta.indexes` 断言字段集合）。
    测试需要 WorkItem 实例——直接 `WorkItem.objects.create(...)` 仅在测试内造数据（INV-6 守护扫描排除 tests/，见 29-03），或复用 tests/delivery 既有造数范式。
  </action>
  <verify>
    <automated>cd server && uv run python manage.py makemigrations delivery --check --dry-run && uv run pytest tests/delivery/test_comment_event_models.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `makemigrations delivery --check --dry-run` → No changes detected（迁移已就绪、干净）。
    - `migrate delivery` 成功，DB 实建 `delivery_work_item_comment_event` 表。
    - 模型单测全绿（≥4 用例覆盖 append-only / CASCADE / 默认值 / 索引）。
    - `ruff format --check` + `ruff check` 对新增文件干净。
  </acceptance_criteria>
  <done>0002 迁移生成并应用建表，模型层单测全绿（含 append-only 两行并存守护）。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | 本 plan 仅新增 DB 表 + 模型，无新外部输入/端点/IO |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01 | Repudiation | WorkItemCommentEvent 行 | mitigate | append-only 设计：编辑/删除为新事件行 + event_time/ingested_at 留痕，模型层不含就地改写方法（模型单测守护，CMT-02） |
| T-29-02 | Tampering | 表写入 | accept | 本 plan 只建表；落库单一入口（INV-6）在 29-02 service + 29-03 grep 守护落地，非本 plan 范围 |
</threat_model>

<verification>
- `makemigrations delivery --check --dry-run` 干净；`migrate delivery` 建表成功。
- `uv run pytest tests/delivery/ -q` 全绿（含既有 28 套件无回归）。
- `uv run ruff format --check delivery/models/ tests/delivery/test_comment_event_models.py` + `ruff check delivery/ tests/delivery/` 干净。
</verification>

<success_criteria>
- `WorkItemCommentEvent` 模型逐字段对齐 DOMAIN §12.4，含 (work_item, event_time) 索引。
- `0002` 迁移已应用建出表；`makemigrations --check` 干净。
- 模型层单测守护 append-only（编辑作为新行不改旧行）+ 默认值 + CASCADE。
</success_criteria>

<artifacts_produced>
- `server/delivery/models/comment_event.py` — `WorkItemCommentEvent` + `CommentEventType` + `ApprovalSemantic`
- `server/delivery/models/__init__.py` — 追加三项 re-export
- `server/delivery/migrations/0002_workitemcommentevent.py` — 建表迁移
- `server/tests/delivery/test_comment_event_models.py` — 模型层单测
</artifacts_produced>

<output>
Create `.planning/phases/29-comment-events/29-01-SUMMARY.md` when done
</output>
