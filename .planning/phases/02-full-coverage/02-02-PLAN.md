---
phase: 02-full-coverage
plan: 02
type: execute
wave: 1
depends_on:
  - 01-01
  - 01-02
  - 01-03
files_modified:
  - server/repositories/views.py
  - server/tests/audit/test_coverage_repositories.py
autonomous: true
requirements:
  - COV-02
  - COV-03
  - COV-04
---

<objective>
为 repositories app 中的供应商凭证（间接通过 system/views.py 已覆盖）、Git 实例凭证、仓库配置添加审计事件 emit。

覆盖范围：
- COV-02: 供应商凭证 CRUD（已由 02-01 Plan 覆盖 system/views.py，此处验证无遗漏）
- COV-03: Git 实例凭证 CRUD（创建/更新/删除）
- COV-04: 仓库配置（创建/删除）
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Git 实例凭证 + 仓库 CRUD 审计 emit</name>
  <files>
    server/repositories/views.py,
    server/tests/audit/test_coverage_repositories.py
  </files>
  <action>
在 `server/repositories/views.py` 中为以下操作添加 `aemit_audit_event()` 调用：

**GitInstanceCredentialsView.post** — 创建实例凭证后 emit `git_credential.created`（target_type="GitInstanceCredential", target_id=str(credential.id), after={host, provider, has_token=True}）
**GitInstanceCredentialDetailView._update** — 更新实例凭证后 emit `git_credential.updated`（target_type="GitInstanceCredential", target_id=str(credential.id), after={host, provider, token_changed}）
**GitInstanceCredentialDetailView.delete** — 删除实例凭证后 emit `git_credential.deleted`（target_type="GitInstanceCredential", target_id=str(credential_id)）

**RepositoryViewSet.acreate** — 创建仓库后 emit `repository.created`（target_type="Repository", target_id=str(repository.id), after={git_url, default_branch, name}）
**RepositoryViewSet.destroy** — 删除仓库后 emit `repository.deleted`（target_type="Repository", target_id=str(repository.id)）

**RepositoryExclusionRulesView.post** — 新增排除规则后 emit `exclusion_rule.created`（target_type="RepoExclusionRule", target_id=str(rule.id), after={pattern, rule_type, source, repository_id}）
**RepositoryExclusionRuleDetailView.delete** — 删除排除规则后 emit `exclusion_rule.deleted`（target_type="RepoExclusionRule", target_id=str(rule_id), before={repository_id}）

**RepositorySensitiveSuggestionActionView.post** — accept 建议后 emit `exclusion_rule.accepted`（target_type="SensitiveFileSuggestion", target_id=str(suggestion.id), after={path, action: "accept"}）

**RepositoryReconcileView.post** — 派发清理后 emit `cleanup.started`（target_type="CleanupRun", target_id=run_id, after={mode, match_count, repository_id}）

import `aemit_audit_event` from `audit.emitter`，每个 emit 包在 try/except 中（best-effort）。

写测试 `server/tests/audit/test_coverage_repositories.py`：
- 测试仓库创建、Git 实例凭证 CRUD、排除规则 CRUD、清理派发的 emit 点
- 每个测试执行真实操作后 assert AuditEvent 存在
  </action>
  <verify>
    <automated>cd /Users/zaneliu/Projects/open-source/friday-clean/.claude/worktrees/agent-a2a711c924e05550b/server && python -m pytest tests/audit/test_coverage_repositories.py -x -v 2>&1 | tail -30</automated>
  </verify>
</task>

</tasks>

<verification>
1. `python -m pytest tests/audit/test_coverage_repositories.py -x -v` 全部通过
2. `python manage.py check` 无错误
3. grep 确认所有 mutation 点均有 emit 调用
</verification>

<success_criteria>
- COV-03: Git 实例凭证创建/更新/删除均有审计 emit
- COV-04: 仓库创建/删除均有审计 emit
- 排除规则和清理派发也有审计 emit
- 所有测试通过
</success_criteria>

<output>
Create `.planning/phases/02-full-coverage/02-02-SUMMARY.md` when done
</output>
