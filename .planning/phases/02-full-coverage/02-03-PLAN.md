---
phase: 02-full-coverage
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
  - 02-02
files_modified:
  - server/feishu/views.py
  - server/services/exclusion.py
  - server/services/purge.py
  - server/tests/audit/test_coverage_services_feishu.py
autonomous: true
requirements:
  - COV-05
  - COV-06
  - COV-09
---

<objective>
为排除规则（services/exclusion.py 的 accept 流程）、清理任务（services/purge.py + sensitive_purge.py）、飞书同步添加审计事件 emit。

覆盖范围：
- COV-05: 排除规则 accept 流程（已由 02-02 Plan 视图层覆盖，此处确保服务层也能 emit）
- COV-06: 清理任务 emit（CleanupRun 的 started/completed/failed）
- COV-09: 飞书同步 webhook 处理完成时 emit
</objective>

<tasks>

<task type="auto">
  <name>Task 1: 清理任务 + 飞书同步审计 emit</name>
  <files>
    server/services/purge_reconcile.py,
    server/feishu/views.py,
    server/tests/audit/test_coverage_services_feishu.py
  </files>
  <action>
**1. 清理任务 emit（server/services/purge_reconcile.py）：**
在 `run_cleanup` 函数中：
- 开始时 emit `cleanup.started`（如果尚未从视图层 emit）
- 完成时 emit `cleanup.completed`（target_type="CleanupRun", after={status, match_count, failures}）
- 失败时 emit `cleanup.failed`（target_type="CleanupRun", after={status, error}）

**2. 飞书同步（server/feishu/views.py）：**
在 `FeishuWebhookView.post` 中：
- 事件处理成功后 emit `feishu_sync.event_received`（target_type="FeishuEvent", after={event_type, project_key}）

import `emit_audit_event` from `audit.emitter`（同步入口），每个 emit 包在 try/except 中（best-effort）。

写测试 `server/tests/audit/test_coverage_services_feishu.py`：
- 测试清理任务完成/失败的 emit
- 测试飞书事件接收的 emit
  </action>
  <verify>
    <automated>cd /Users/zaneliu/Projects/open-source/friday-clean/.claude/worktrees/agent-a2a711c924e05550b/server && python -m pytest tests/audit/test_coverage_services_feishu.py -x -v 2>&1 | tail -30</automated>
  </verify>
</task>

</tasks>

<verification>
1. `python -m pytest tests/audit/test_coverage_services_feishu.py -x -v` 全部通过
2. `python manage.py check` 无错误
3. 所有 9 个覆盖区域的审计 emit 均已就位
</verification>

<success_criteria>
- COV-05: 排除规则全流程审计覆盖
- COV-06: 清理任务 started/completed/failed 均有审计 emit
- COV-09: 飞书同步事件接收有审计 emit
- 所有测试通过
</success_criteria>

<output>
Create `.planning/phases/02-full-coverage/02-03-SUMMARY.md` when done
</output>
