---
phase: 90-clarification-capability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/delivery/models/clarification.py
  - server/delivery/models/__init__.py
  - server/delivery/migrations/0026_clarification_questions.py
autonomous: true
requirements: [CLARIFY-01]
must_haves:
  truths:
    - "Clarification 作为轮次容器可存 round_no/container_status/origin_repo/plan_version_id（全 nullable）"
    - "新增 ClarificationQuestion 子表可存 order/question/qtype/options/recommended/selected/freeform_text/answered_at/recommendation_adopted"
    - "旧 Clarification 行迁移后仍可读（既有字段 question/answer/answered_at/affected_partials 保留不删）"
    - "makemigrations --check 干净（迁移与模型一致，依赖 0025）"
  artifacts:
    - path: "server/delivery/models/clarification.py"
      provides: "Clarification 容器新字段 + ClarificationQuestion 子表模型"
      contains: "class ClarificationQuestion"
    - path: "server/delivery/migrations/0026_clarification_questions.py"
      provides: "容器 AddField×4 + 子表 CreateModel（nullable，不破坏旧行）"
      contains: "CreateModel"
    - path: "server/delivery/models/__init__.py"
      provides: "ClarificationQuestion barrel re-export"
      contains: "ClarificationQuestion"
  key_links:
    - from: "server/delivery/models/__init__.py"
      to: "delivery.models.clarification.ClarificationQuestion"
      via: "from ... import"
      pattern: "from delivery.models.clarification import"
---

<objective>
建立结构化澄清的数据脊柱（CLARIFY-01 模型半）：把 `Clarification` 扩展为「轮次容器」（新增 nullable 字段），并新建 `ClarificationQuestion` 子表承载多问题 + 单/多选 + 选项 + 推荐项 + 按题答案 + **持久化 `recommendation_adopted` 采纳信号**，便于后续按题 SQL 聚合采纳率。

Purpose: 后续 service 写入收口（90-02）、adapter LLM 接线（90-03）、统一 helper（90-04）、以及 Phase 91 出口面/多轮都围绕此模型建设。模型是 §6/§12 操作态脊柱，本 plan 只建表/字段，**零业务方法**（写入逻辑归 service，守 INV-6）。
Output: 扩展后的 `clarification.py`、新迁移 `0026_clarification_questions.py`、barrel re-export。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/90-clarification-capability/90-CONTEXT.md
@.planning/phases/90-clarification-capability/90-RESEARCH.md
@.planning/phases/90-clarification-capability/90-PATTERNS.md
</context>

<artifacts_produced>
本 plan 新增/修改的符号（下游 plan 90-02/03/04 与 Phase 91 消费）：
- 模型 `Clarification`（容器）新增字段：`round_no`(PositiveIntegerField null)、`container_status`(CharField(16) null，**不叫 `status` 避免与 PlanSession.status 混淆**)、`origin_repo`(CharField(255) null)、`plan_version_id`(UUIDField null，冗余绑定；canonical 绑定仍是 `session.current_plan_version`)。
- 新模型 `ClarificationQuestion`（子表，`delivery.models.clarification`）：字段 `id`/`clarification`(FK CASCADE related_name="questions")/`order`/`question`/`qtype`(避开内建 `type`)/`options`(JSON)/`recommended`(JSON)/`origin_repo`/`selected`(JSON null)/`freeform_text`/`answered_at`/`recommendation_adopted`(Boolean null)/`created_at`。
- 迁移 `server/delivery/migrations/0026_clarification_questions.py`（依赖 `delivery.0025_rename_project_to_space`）。
- barrel：`delivery.models.ClarificationQuestion`（`__init__.py` re-export + `__all__`）。
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: 扩展 Clarification 容器 + 新建 ClarificationQuestion 子表 + barrel re-export</name>
  <files>server/delivery/models/clarification.py, server/delivery/models/__init__.py</files>
  <read_first>
    - server/delivery/models/clarification.py（容器现状 20-49，保留全部既有字段不删）
    - server/delivery/models/technical_plan.py:96-128（父子 FK 子表范式：CASCADE + related_name + JSONField(default=...) + Meta.db_table/indexes）
    - server/delivery/models/__init__.py:7,64-70,109（re-export + __all__ 范式）
  </read_first>
  <action>
    在 `clarification.py` 的 `Clarification` 类**保留全部既有字段不删**，新增 4 个 nullable 容器字段：`round_no = models.PositiveIntegerField(null=True, blank=True)`（多轮序号，支撑 91 多轮）、`container_status = models.CharField(max_length=16, null=True, blank=True)`（pending/answered/skipped；**严禁命名 `status`** 以免与 `PlanSession.status` 语义混淆、迁移误判状态机字段）、`origin_repo = models.CharField(max_length=255, null=True, blank=True)`（CLARIFY-03 携带，标记澄清源自哪个仓的调研）、`plan_version_id = models.UUIDField(null=True, blank=True)`（采纳率分析便利的冗余绑定；docstring 注明 canonical 绑定仍是 `session.current_plan_version`）。新字段一律 `null=True, blank=True`——旧行不强制回填、不破坏 0016 schema。

    在同文件新增 `class ClarificationQuestion(models.Model)`：`id`(UUIDField PK default=uuid.uuid4 editable=False)、`clarification = models.ForeignKey("delivery.Clarification", on_delete=models.CASCADE, related_name="questions")`、`order = models.PositiveIntegerField(default=0)`、`question = models.TextField()`、`qtype = models.CharField(max_length=8, default="single")`（**避开 Python 内建 `type`**；docstring 注 single/multi）、`options = models.JSONField(default=list)`、`recommended = models.JSONField(default=list)`（single 存 [str] 或 str / multi 存 list）、`origin_repo = models.CharField(max_length=255, null=True, blank=True)`、`selected = models.JSONField(null=True, blank=True)`（答案：single=str / multi=list）、`freeform_text = models.TextField(blank=True, default="")`、`answered_at = models.DateTimeField(null=True, blank=True)`、`recommendation_adopted = models.BooleanField(null=True, blank=True)`（作答时定格，null=未答/无推荐/纯 freeform）、`created_at = models.DateTimeField(auto_now_add=True)`。`Meta`: `db_table="delivery_clarification_question"`、`ordering=["order"]`、`indexes=[models.Index(fields=["clarification", "order"])]`。给类与字段写中文 docstring/注释说明意图，但**绝不**在模型层加 create/save/answer 等业务方法（守 INV-6，写入归 service）。

    在 `__init__.py` 把 `from delivery.models.clarification import Clarification` 扩展为 `from delivery.models.clarification import (Clarification, ClarificationQuestion)`，并在 `__all__` 列表加 `"ClarificationQuestion"`。
  </action>
  <verify>
    <automated>cd server && uv run python -c "from delivery.models import Clarification, ClarificationQuestion; assert hasattr(Clarification, 'round_no') and hasattr(Clarification, 'container_status') and hasattr(Clarification, 'origin_repo') and hasattr(Clarification, 'plan_version_id'); print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `from delivery.models import ClarificationQuestion` 成功；`Clarification` 新增 4 字段可访问。
    - `ClarificationQuestion` 子表含全部约定字段，FK `related_name="questions"`、`Meta.db_table="delivery_clarification_question"`。
    - 既有 `question`/`answer`/`answered_at`/`affected_partials` 字段保留未删；模型层无任何业务方法。
    - 字段名用 `container_status`（非 `status`）、`qtype`（非 `type`）。
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: 生成迁移 0026（容器 AddField + 子表 CreateModel，依赖 0025）</name>
  <files>server/delivery/migrations/0026_clarification_questions.py</files>
  <read_first>
    - server/delivery/migrations/0016_clarification.py:9-35（CreateModel + Meta options 范式）
    - server/delivery/migrations/0024_ingestrun_durable_queue.py:6-28（AddField nullable 范式）
  </read_first>
  <action>
    先 `ls server/delivery/migrations/` 复核 head 仍为 `0025_rename_project_to_space`（RESEARCH 行 555 要求执行前复核）。然后 `cd server && uv run python manage.py makemigrations delivery` 自动生成迁移，重命名为 `0026_clarification_questions.py`。人工核对生成内容：`dependencies = [('delivery', '0025_rename_project_to_space')]`；包含对 `clarification` 的 4 个 `AddField`（`round_no`/`container_status`/`origin_repo`/`plan_version_id`，全 nullable）+ 1 个 `CreateModel('ClarificationQuestion', ...)`（FK CASCADE、`db_table='delivery_clarification_question'`、复合索引）。确认字段约束与 90-01 Task 1 模型一致（尤其新字段 `null=True`、子表 FK on_delete=CASCADE）。**不**手写迁移逻辑、不引入数据回填（CONTEXT 锁定不强制回填历史）。
  </action>
  <verify>
    <automated>cd server && uv run python manage.py makemigrations delivery --check --dry-run && uv run python manage.py migrate delivery 0026 && echo migrate-ok</automated>
  </verify>
  <acceptance_criteria>
    - `makemigrations --check --dry-run` 退出码 0（模型与迁移一致，无遗漏字段）。
    - `migrate delivery 0026` 成功建子表 `delivery_clarification_question` 并对旧表加 nullable 列（旧行不报错）。
    - 迁移 `dependencies` 指向 `0025_rename_project_to_space`。
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → 已部署 DB（含历史 Clarification 行） | 迁移对生产/本地既有澄清行执行；新增列必须 nullable 否则破坏旧行 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90-01-01 | Tampering | 0026 migration（既有 `delivery_clarification` 行） | mitigate | 容器新字段一律 `null=True, blank=True`；不强制回填；保留既有列不删（ASVS V5 数据完整性） |
| T-90-01-02 | Tampering | `ClarificationQuestion` 模型层 | mitigate | 模型零业务方法，写入只经 service（INV-6，90-02 grep 守护覆盖子模型） |
| T-90-01-03 | Denial of Service | 字段命名冲突（`status`/`type`） | mitigate | 用 `container_status`/`qtype` 规避状态机字段误判与 Python 内建遮蔽 |
| T-90-01-SC | Tampering | 依赖安装 | accept | 本 plan 无 `pip/npm install`（纯 Django 模型 + 迁移），无供应链面 |
</threat_model>

<verification>
- `cd server && uv run python manage.py makemigrations delivery --check --dry-run` 干净。
- `from delivery.models import Clarification, ClarificationQuestion` 导入成功，新字段齐备。
- 旧字段（`question`/`answer`/`answered_at`/`affected_partials`）保留未删。
</verification>

<success_criteria>
- 数据模型支持多轮容器 + 多问题子表 + 按题答案 + 采纳信号，全部 nullable 向后兼容。
- 迁移 0026 自动生成、依赖 0025、可正向 migrate，旧行不破坏。
- barrel re-export `ClarificationQuestion` 可用。
</success_criteria>

<output>
Create `.planning/phases/90-clarification-capability/90-01-SUMMARY.md` when done
</output>
