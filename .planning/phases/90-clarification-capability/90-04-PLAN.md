---
phase: 90-clarification-capability
plan: 04
type: execute
wave: 3
depends_on: ["90-02"]
files_modified:
  - server/services/plan_orchestration/ask_clarification.py
  - server/services/plan_orchestration/__init__.py
  - server/tests/services/test_ask_clarification_helper.py
autonomous: true
requirements: [CLARIFY-03]
must_haves:
  truths:
    - "plan_orchestration 提供入口无关 ask_clarification helper，薄封装 ClarificationService.create_round"
    - "helper 写 delivery.Clarification 轮 + 多问题，可携带 origin_repo"
    - "helper 不驱动 engine.advance、不做挂起 marker（驱动是入口私有）"
    - "与既有 chat tool agents/tools/clarification.py:ask_clarification 经模块路径区分，绝不复用 chat 资产"
  artifacts:
    - path: "server/services/plan_orchestration/ask_clarification.py"
      provides: "入口无关 ask_clarification helper（写 delivery.Clarification，INV-6）"
      contains: "async def ask_clarification"
    - path: "server/services/plan_orchestration/__init__.py"
      provides: "ask_clarification barrel re-export"
      contains: "ask_clarification"
    - path: "server/tests/services/test_ask_clarification_helper.py"
      provides: "helper 写 delivery + origin_repo + INV-6 + 不驱动 advance 守护"
      contains: "ask_clarification"
  key_links:
    - from: "server/services/plan_orchestration/ask_clarification.py"
      to: "ClarificationService.create_round"
      via: "薄封装写入（INV-6）"
      pattern: "create_round"
    - from: "server/services/plan_orchestration/__init__.py"
      to: "services.plan_orchestration.ask_clarification.ask_clarification"
      via: "barrel re-export"
      pattern: "from services.plan_orchestration.ask_clarification import"
---

<objective>
提供编排层入口无关的统一 `ask_clarification` 能力（CLARIFY-03）：`plan_orchestration` 内一个薄封装 helper，写 `delivery.Clarification` 轮 + 多问题（经 `ClarificationService.create_round`，守 INV-6），可携带 `origin_repo`。编排任意点（架构师融合卡住 / 某调研容器卡住）都能调它产出结构化澄清请求，工作流与对话复用同一 helper + 同一模型，不造两套。

Purpose: 把「编排代码主动发问」收口为单一能力，与「engine 自动澄清」（ClarifyAdapter）共用同一写入收口（`create_round`），不另写写库逻辑。
Output: 新建 `ask_clarification.py` + barrel re-export + 守护测试（含与 chat tool 同名撞车防护）。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/90-clarification-capability/90-RESEARCH.md
@.planning/phases/90-clarification-capability/90-PATTERNS.md
@.planning/phases/90-clarification-capability/90-02-SUMMARY.md
</context>

<artifacts_produced>
- 新模块 `server/services/plan_orchestration/ask_clarification.py`，导出 `async def ask_clarification(session, questions, *, origin_repo=None, clarification_service=None) -> Clarification`（薄封装 `create_round`，**不**驱动 advance / 不挂起）。
- barrel：`from services.plan_orchestration import ask_clarification`（`__init__.py` 导入 + `__all__`）。
- 新测试 `server/tests/services/test_ask_clarification_helper.py`（文件名/导入与既有 `tests/test_ask_clarification_tool.py` chat tool 显式区分）。
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: 新建 ask_clarification helper + barrel re-export</name>
  <files>server/services/plan_orchestration/ask_clarification.py, server/services/plan_orchestration/__init__.py</files>
  <read_first>
    - server/services/plan_orchestration/clarify_adapter.py:114-118（薄封装 create_clarification → 改 create_round 调用范式）
    - server/services/plan_orchestration/resume.py:1-17,42-44（入口无关 docstring 精神 + 函数内 lazy import 规避环）
    - server/agents/tools/clarification.py:1-36（**既有同名 chat tool**——撞名风险源，绝不复用/改动）
    - server/services/plan_orchestration/__init__.py:20-23,74-115（barrel import + __all__ 范式）
    - server/delivery/services/clarification_service.py（90-02 新增 create_round）
  </read_first>
  <action>
    新建 `server/services/plan_orchestration/ask_clarification.py`，模块 docstring 中文说明「入口无关：写 delivery.Clarification 轮 + 多问题（INV-6），不驱动 advance / 不挂起 marker（驱动是入口私有，对齐 entrypoint.py 精神）」，并显式注明**与 chat tool `agents/tools/clarification.py:ask_clarification` 同名但语义不同——本 helper 写 `delivery.Clarification`，chat tool 写 `chat.ConversationIntentTrace` 走 LangGraph interrupt，靠模块路径区分，绝不复用/改动 chat 资产**（Pitfall 1）。

    签名：`async def ask_clarification(session, questions: list[dict], *, origin_repo: str | None = None, clarification_service: "ClarificationService | None" = None) -> "Clarification"`。函数内 lazy import 规避 import 环：`from delivery.services import ClarificationService`（及类型注解所需 `from delivery.models import Clarification`，可放 `TYPE_CHECKING`）。实现仅：`svc = clarification_service or ClarificationService(); return await svc.create_round(session, questions, origin_repo=origin_repo)`。**不**驱动 `engine.advance`、**不**做挂起 marker、**不**碰 `session.status`（status 只经 transition）。async 禁裸 lazy-FK（只传 `session` 给 service，service 内已 sync_to_async）。

    在 `plan_orchestration/__init__.py` 加 `from services.plan_orchestration.ask_clarification import ask_clarification` 并在 `__all__` 加 `"ask_clarification"`。
  </action>
  <verify>
    <automated>cd server && uv run python -c "from services.plan_orchestration import ask_clarification; import inspect; assert inspect.iscoroutinefunction(ask_clarification); print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `from services.plan_orchestration import ask_clarification` 成功，是 async 函数。
    - 实现仅薄封装 `create_round`（不驱动 advance、不挂起、不写 status）。
    - docstring 显式标注与 chat tool 同名撞车防护；未 import/复用 `agents/tools/clarification.py`。
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: ask_clarification helper 守护测试（写 delivery + origin_repo + INV-6 + 不驱动 advance）</name>
  <files>server/tests/services/test_ask_clarification_helper.py</files>
  <read_first>
    - server/tests/delivery/test_clarification_service.py（落库断言 + INV-6 范式 + transaction=True 范式）
    - server/tests/services/test_engine_clarify.py（adapter/helper 行为断言范式）
    - server/services/plan_orchestration/ask_clarification.py（Task 1）
  </read_first>
  <action>
    新建 `server/tests/services/test_ask_clarification_helper.py`（**文件名/导入须与既有 `tests/test_ask_clarification_tool.py` chat tool 显式区分**，文件顶 docstring 注明这是 plan_orchestration helper 非 chat tool）。沿用 `django_db(transaction=True)` 范式建 `PlanSession`。用例：
    1. `test_ask_clarification_writes_delivery_round`：`await ask_clarification(session, [{question,type:single,options,recommended}, ...])` → 断言建 1 `Clarification` 容器 + N `ClarificationQuestion` 子题（经 service 落 delivery，非 chat trace）。
    2. `test_ask_clarification_carries_origin_repo`：传 `origin_repo="repo-x"` → 断言容器 `origin_repo=="repo-x"`。
    3. `test_ask_clarification_does_not_drive_or_suspend`：调用前后 `session.status` 不变（helper 不驱动 advance / 不挂起）——断言 session 状态未被 helper 改动。
    4. （可选）`test_ask_clarification_is_plan_orchestration_not_chat_tool`：断言 `ask_clarification.__module__ == "services.plan_orchestration.ask_clarification"`（与 chat tool 模块路径区分）。
    helper 写入只经 service → 由 90-02 的 INV-6 grep 守护覆盖（本测试不重复 grep，聚焦行为）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/test_ask_clarification_helper.py -q</automated>
  </verify>
  <acceptance_criteria>
    - helper 写 `delivery.Clarification` 轮 + 多问题、携带 origin_repo 落库正确。
    - 调用不改变 `session.status`（不驱动 / 不挂起）。
    - 测试文件与既有 chat tool 测试显式区分，无混淆。
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 编排代码 → ask_clarification helper → ClarificationService | 主动发问写入须经 service（INV-6） |
| 命名空间：plan_orchestration helper vs chat tool 同名 | import 混淆 / 误调可致 chat 澄清行为漂移 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90-04-01 | Tampering | INV-6 写入收口 | mitigate | helper 仅薄封装 `create_round`，不旁路写表；由 90-02 grep 守护覆盖（ASVS V5） |
| T-90-04-02 | Spoofing / Tampering | 与 chat tool `ask_clarification` 同名撞车 | mitigate | 放 `services/plan_orchestration/` 不同包，模块路径区分；docstring 显式标注；绝不 import/改 `agents/tools/clarification.py` |
| T-90-04-03 | Tampering | helper 越权驱动 / 改 status | mitigate | helper 不驱动 advance、不挂起、不碰 `session.status`（status 只经 transition）；测试守护 status 不变 |
| T-90-04-04 | Denial of Service | async 裸 lazy-FK | mitigate | helper 只把 `session` 透传给 service（service 内 sync_to_async），不裸访问 FK |
| T-90-04-SC | Tampering | 依赖安装 | accept | 无 `pip/npm install`，无供应链面 |
</threat_model>

<verification>
- `cd server && uv run pytest tests/services/test_ask_clarification_helper.py -q` 全绿。
- `from services.plan_orchestration import ask_clarification` 可用、是 async、薄封装 `create_round`。
- 与 chat tool 同名经模块路径区分，未复用 chat 资产。
</verification>

<success_criteria>
- 入口无关 `ask_clarification` helper 落地，写 delivery.Clarification（INV-6、origin_repo）。
- 不驱动 advance / 不挂起（驱动是入口私有）。
- 与既有 chat tool 同名撞车有防护。
</success_criteria>

<output>
Create `.planning/phases/90-clarification-capability/90-04-SUMMARY.md` when done
</output>
