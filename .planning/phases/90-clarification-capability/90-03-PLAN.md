---
phase: 90-clarification-capability
plan: 03
type: execute
wave: 3
depends_on: ["90-02"]
files_modified:
  - server/services/plan_orchestration/clarify_adapter.py
  - server/services/plan_orchestration/resume.py
  - server/tests/services/test_engine_clarify.py
  - server/tests/services/test_plan_research_e2e.py
autonomous: true
requirements: [CLARIFY-02]
must_haves:
  truths:
    - "ClarifyAdapter.clarify 首轮 needs==True 后调 agenerate_clarification_questions 产多题、经 create_round 落库"
    - "LLM 返回 [] 或异常 → fail-soft 回退建单题粗问题轮（不抛、不让 engine 落 failed）"
    - "ClarifyAdapter 的 pending/answered 判定改调 service ahas_pending（兼容旧单题行）"
    - "resume.adrive_plan_session_to_pause_or_terminal 的 CLARIFYING 短路改调 ahas_pending"
    - "CR-01 单轮短路零回归；e2e 驱动 helper 的 pending 查询同步升级"
  artifacts:
    - path: "server/services/plan_orchestration/clarify_adapter.py"
      provides: "LLM 多题接线 + fail-soft 回退 + ahas_pending 判定升级"
      contains: "agenerate_clarification_questions"
    - path: "server/services/plan_orchestration/resume.py"
      provides: "CLARIFYING pending 短路改调 ahas_pending"
      contains: "ahas_pending"
  key_links:
    - from: "server/services/plan_orchestration/clarify_adapter.py"
      to: "agenerate_clarification_questions"
      via: "首轮 needs==True 后调用产多题"
      pattern: "agenerate_clarification_questions"
    - from: "server/services/plan_orchestration/clarify_adapter.py"
      to: "ClarificationService.create_round / ahas_pending"
      via: "落库 + pending 判定收口"
      pattern: "create_round|ahas_pending"
---

<objective>
把已就绪的 LLM 多问题生成器 `agenerate_clarification_questions` 接入 `ClarifyAdapter.clarify`（CLARIFY-02）：静态 policy 判「要不要问」→ LLM 判「问什么」→ 经 `ClarificationService.create_round` 落结构化多题；LLM 返回 `[]`/异常时 **fail-soft 回退**建单题粗问题轮（绝不抛、绝不让 engine 落 failed）。同步把三处 pending 判定升级为调 service 统一谓词 `ahas_pending`（兼容旧单题行）：`clarify_adapter`、`resume`、e2e 驱动 helper。

Purpose: 让澄清更聚焦可交互（每题带选项 + 推荐），同时保 CR-01 单轮短路语义与历史挂起兼容不回归。
Output: 接线后的 `clarify_adapter.py` + 升级后的 `resume.py` + 扩充的 `test_engine_clarify.py` + e2e helper 同步。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/90-clarification-capability/90-RESEARCH.md
@.planning/phases/90-clarification-capability/90-PATTERNS.md
@.planning/phases/90-clarification-capability/90-02-SUMMARY.md
</context>

<artifacts_produced>
本 plan 修改的接线点（无新模型；消费 90-02 的 `create_round`/`ahas_pending`）：
- `ClarifyAdapter.clarify`：首轮 needs==True 后接 `agenerate_clarification_questions(requirement=session.decomposition.get("requirement_text"), routing=session.routing, recall_hits=session.recall_context)` → 非空 `create_round` / 空回退单题轮；pending/answered 判定改调 `clarification_service.ahas_pending(session.id)`。
- 新增观测事件 `clarification_fallback_coarse_question`（category=sampling, component=plan_orchestration），回退时记录。
- `resume.adrive_plan_session_to_pause_or_terminal`：CLARIFYING 短路查询从内联 `Clarification.objects.filter(answered_at__isnull=True)` 改调 `ahas_pending`。
- e2e 驱动 helper（`test_plan_research_e2e.py`）的 CLARIFYING pending 查询同步升级。
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: ClarifyAdapter 接 LLM 多题 + fail-soft 回退 + pending 判定升级</name>
  <files>server/services/plan_orchestration/clarify_adapter.py</files>
  <read_first>
    - server/services/plan_orchestration/clarify_adapter.py（三段判定 86-118：pending → CR-01 已答短路 → 首轮 policy）
    - server/services/plan_orchestration/clarification_questions.py:132-177（agenerate 签名 + 已 best-effort 返回 []）
    - server/services/plan_orchestration/engine.py:106-124,170-195（requirement 源 114；_clarify 通用 except 会落 failed 181-183）
    - server/delivery/services/clarification_service.py（90-02 新增 create_round / ahas_pending）
  </read_first>
  <action>
    改造 `clarify` 三段判定（per CLARIFY-02）：
    - 第 1 步 pending 判定：`if await self.clarification_service.ahas_pending(session.id): return {"needs_clarification": True, "pending": True}`（替换内联 `Clarification.objects.filter(answered_at__isnull=True).aexists()`，收口到 service 统一谓词、兼容旧行）。
    - 第 2 步 CR-01 已答短路：保留「已存在已答且无 pending → 放行 researching」语义。由于 `ahas_pending` 已为 False 才到此，判定「本 session 是否有过任何澄清轮」用 `await Clarification.objects.filter(session_id=session.id).aexists()`（有则视为已答轮完成放行，零回归 CR-01 无限挂起修复）。docstring 注明语义不变。
    - 第 3 步首轮 policy：`needs, question, affected = self.policy(session)`；`needs==False` 直接放行。`needs==True` 后接 LLM：`questions = await agenerate_clarification_questions(requirement=(session.decomposition or {}).get("requirement_text", ""), routing=session.routing if isinstance(session.routing, dict) else None, recall_hits=session.recall_context if isinstance(session.recall_context, list) else None)`。`questions` 非空 → `clar = await self.clarification_service.create_round(session, questions, origin_repo=None)`；空 → **fail-soft 回退**：`logger.info("clarification_fallback_coarse_question", category="sampling", component="plan_orchestration", session_id=str(session.id))` 后 `clar = await self.clarification_service.create_round(session, [{"question": question, "type": "single", "options": [], "recommended": []}])`。最后 `await self._emit_asked(session, clar, question)`（payload 可升级为多题摘要 best-effort）返回 `{"needs_clarification": True, "clarification_id": str(clar.id)}`。

    **绝不**在 adapter 内让 LLM 异常上抛——`agenerate_clarification_questions` 已 best-effort 返回 `[]`，只需「`[]` → 回退」一处分支（否则 engine.advance 通用 except 落 failed）。`from agents...` / 生成器 import 放函数内或模块顶（生成器自身已 lazy import SDK，模块顶 import `agenerate_clarification_questions` 安全）。async 禁裸 lazy-FK（用 `session.id`/标量）。i18n：回退粗问题文案保持中文（policy 已中文）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/test_engine_clarify.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - 首轮 needs==True 调 LLM 产多题经 `create_round` 落库（mock 生成器断言）。
    - LLM 返回 `[]`/异常 → 回退建单题轮、记 `clarification_fallback_coarse_question`、不抛。
    - pending/answered 判定改调 `ahas_pending`；CR-01 单轮短路零回归。
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: resume.py 与 e2e 驱动 helper 的 CLARIFYING pending 判定升级</name>
  <files>server/services/plan_orchestration/resume.py, server/tests/services/test_plan_research_e2e.py</files>
  <read_first>
    - server/services/plan_orchestration/resume.py:42-79（CLARIFYING 短路 63-68；函数内 lazy import 范式）
    - server/tests/services/test_plan_research_e2e.py（_drive_* helper 的 CLARIFYING pending 查询，约 178-184）
    - server/delivery/services/clarification_service.py（ahas_pending）
  </read_first>
  <action>
    `resume.py`：把 CLARIFYING 短路里的 `Clarification.objects.filter(session_id=session.id, answered_at__isnull=True).aexists()` 替换为调 service 统一谓词——函数内 lazy import `from delivery.services import ClarificationService`，`if session.status == PlanSessionStatus.CLARIFYING and await ClarificationService().ahas_pending(session.id): return session`。保留既有 docstring 的 clarifying HITL 保护说明，注明 pending 谓词已收口到 service（兼容旧单题行 + 新子题）。不改 researching 短路、不改 max_steps fail 分支（最小 diff 零回归）。

    `test_plan_research_e2e.py`：把 `_drive_*` helper 内 CLARIFYING pending 查询（约 178-184，`Clarification.answered_at IS NULL`）升级为「轮内有未答 question」或同样调 `ahas_pending`，保 `test_e2e_clarification_loop_reruns_only_affected`（及相关 clarif 用例）零回归——若既有 e2e 用例用 `create_clarification`（旧单题）建澄清，`ahas_pending` 的旧行兼容分支须使其仍判 pending。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/test_plan_research_e2e.py -k clarif -q && cd server && uv run pytest tests/services/test_plan_research_e2e.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `resume.py` CLARIFYING 短路改调 `ahas_pending`，行为等价（轮内未答 → 短路返回保护 HITL）。
    - e2e 驱动 helper pending 查询升级；`test_e2e_clarification_loop_reruns_only_affected` 等 clarif 用例零回归。
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: test_engine_clarify 扩展——LLM 多题接线 + fail-soft + pending 升级</name>
  <files>server/tests/services/test_engine_clarify.py</files>
  <read_first>
    - server/tests/services/test_engine_clarify.py（现 8 测：CR-01 短路 / policy 判定等）
    - server/services/plan_orchestration/clarify_adapter.py（Task 1 改造后）
  </read_first>
  <action>
    新增用例（mock `agenerate_clarification_questions`，勿真实调 LLM）：
    1. `test_clarify_wires_llm_multi_questions`：mock 生成器返回 `[{question,type:single,options,recommended}, ...]` → 断言经 `create_round` 落多子题（容器 + N `ClarificationQuestion`）、返回 `needs_clarification=True`。
    2. `test_clarify_fail_soft_empty_falls_back_to_coarse`（命名含 `fail_soft`）：mock 返回 `[]` → 断言回退建单题轮（1 子题、type=single、无 options）、不抛、记 `clarification_fallback_coarse_question`。
    3. `test_clarify_fail_soft_exception`（可选）：mock 生成器内部已吞异常返回 `[]`，此处验证 adapter 不因 LLM 失败抛、engine 不落 failed。
    4. `test_clarify_pending_uses_ahas_pending`：轮内有未答子题 → 第二次 clarify 返回 `pending=True`（不重复建轮）。
    保既有 8 测零回归（尤其 CR-01 单轮短路）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/test_engine_clarify.py -q && cd server && uv run pytest tests/services/test_engine_clarify.py -k "fail_soft or llm or pending" -q</automated>
  </verify>
  <acceptance_criteria>
    - LLM 多题接线、fail-soft 回退、pending 升级均有断言。
    - 既有 8 测零回归（CR-01 单轮短路保护）。
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM 上游 → ClarifyAdapter（产出 + 异常文本） | LLM 产出可能非法/超量/含敏感；异常文本可能含上游响应 |
| ClarifyAdapter → ClarificationService（写入） | 多题落库须经 service（INV-6） |
| engine.advance 通用 except | clarify stage 抛异常会落 session failed（可用性） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90-03-01 | Tampering / DoS | LLM 产出注入非法/超量问题 | mitigate | 写库前必经 `normalize_clarification_questions`（已实现：截断 max_questions=5 + recommended ∈ options + type 归一）（ASVS V5） |
| T-90-03-02 | Denial of Service | clarify stage LLM 异常上抛 → engine failed | mitigate | fail-soft：`agenerate` 已返回 `[]`，adapter 仅「`[]`→回退」一处分支，绝不新增抛点 |
| T-90-03-03 | Info Disclosure | 上游 LLM 响应体/异常文本入日志 | mitigate | 生成器异常只记 `str(exc)` 遵 `redact_secrets_in_text`；回退事件只记 session_id/category/component（ASVS V7） |
| T-90-03-04 | Tampering | pending 判定旁路 / 旧行误放行 | mitigate | 三处判定收口 service `ahas_pending`（兼容旧单题行 Pitfall 2），避免逻辑漂移 |
| T-90-03-05 | Denial of Service | async 裸 lazy-FK | mitigate | 标量 `session.id` + service `sync_to_async`，不裸访问 `session.work_item` |
| T-90-03-SC | Tampering | 依赖安装 | accept | 无 `pip/npm install`，无供应链面 |
</threat_model>

<verification>
- `cd server && uv run pytest tests/services/test_engine_clarify.py tests/services/test_plan_research_e2e.py -q` 全绿。
- LLM 调用经 `call_source=plan_clarification`（生成器内已埋点，不改）；adapter 回退记 `clarification_fallback_coarse_question`。
- CR-01 单轮短路与 e2e clarif 用例零回归。
</verification>

<success_criteria>
- `ClarifyAdapter.clarify` 接 LLM 多题 + fail-soft 回退，落库经 `create_round`（INV-6）。
- 三处 pending 判定收口 `ahas_pending`（兼容旧行）。
- 既有 clarify/e2e 测试零回归。
</success_criteria>

<output>
Create `.planning/phases/90-clarification-capability/90-03-SUMMARY.md` when done
</output>
