---
phase: 106-multi-signal-scoring
plan: 04
type: execute
wave: 3
depends_on: [106-02]
files_modified:
  - server/codegraph/management/commands/measure_repo_index_stats.py
  - server/codegraph/management/commands/calibrate_repo_router_metadata.py
  - server/tests/codegraph/test_measure_repo_index_stats.py
  - server/tests/codegraph/test_calibrate_repo_router_metadata.py
  - .planning/phases/106-multi-signal-scoring/106-MEASUREMENTS.md
autonomous: true
requirements: [ROUTE-03, ROUTE-04, ROUTE-05]
must_haves:
  truths:
    - "运维在生产实例执行 measure command 的 --write-snapshot 后，SystemSetting repo_router.nr_snapshot 持有 {repo_id: n_r} 全表 + n_bar 中位数 + generated_at——router（106-06）据此计算 pivoted breadth 而不逐次 count（ROUTE-03 数据管线）"
    - "measure command 的 --activity 输出全仓 last_commit_at 覆盖率与新鲜度分位数（p50/p90）+ facets 五维覆盖率——O-5 的执行工具就位，覆盖不足的仓在打分侧自动走枚举回退（ROUTE-05）"
    - "calibrate_repo_router_metadata 按 O-2 流程产出各 facet 的余弦分布统计（负样本 p95 → c_lo、正样本 p50 → c_hi）与逐 facet 判定（c_hi-c_lo < 0.10 → 放弃 T2），embedding 未配置时结构性样本模式仍可跑通管线（ROUTE-04）"
    - "106-MEASUREMENTS.md 沿用数据环境标注纪律：开发库数字只进「结构性结论」区，生产回填列为 deferred 挂账人工步骤（SC-5）"
  artifacts:
    - path: server/codegraph/management/commands/calibrate_repo_router_metadata.py
      provides: "O-2 校准 command（余弦分布 → c_lo/c_hi 建议 + T2 弃用判定）"
      min_lines: 150
    - path: .planning/phases/106-multi-signal-scoring/106-MEASUREMENTS.md
      provides: "O-2/O-5 占位表 + 生产执行指引 + 开发库结构性结论"
      min_lines: 60
  key_links:
    - from: server/codegraph/management/commands/measure_repo_index_stats.py
      to: server/system/models.py
      via: "--write-snapshot 写 SettingKeys.REPO_ROUTER_NR_SNAPSHOT（经 SystemSetting update_or_create，signal 自动失效读缓存）"
      pattern: "REPO_ROUTER_NR_SNAPSHOT"
    - from: server/codegraph/management/commands/calibrate_repo_router_metadata.py
      to: server/services/embedding.py
      via: "generate_embeddings_batch 批量取样本向量；CallSource.EMBEDDING 作用域"
      pattern: "generate_embeddings_batch|CallSource.EMBEDDING"
---

<objective>
落地 Phase 106 的两条实测管线（SC-5）：扩展 `measure_repo_index_stats`（105-02 产物）新增 `--activity`（O-5：全仓 `last_commit_at` 覆盖率/新鲜度 + facets 五维覆盖率统计）与 `--write-snapshot`（N_r 全表 + N̄ 中位数写入 SystemSetting，为 106-06 的 breadth 计算供数）；新建独立 command `calibrate_repo_router_metadata`（O-2：需求文本 × facet 值余弦分布 → c_lo/c_hi 校准建议 + 逐 facet T2 弃用判定）。生产实测数字按 CONTEXT 数据环境标注纪律 deferred——本 plan 交付管线、结构性测试与 106-MEASUREMENTS.md 占位表 + 执行指引。

Purpose: ROUTE-03 的 N_r/N̄ 数据源、ROUTE-05 的覆盖率证据、ROUTE-04 的 T2 校准通道全部具备可执行工具；生产回填只剩人工跑命令。
Output: 两个 command + 测试 + 106-MEASUREMENTS.md。
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/106-multi-signal-scoring/106-CONTEXT.md
@.planning/phases/106-multi-signal-scoring/106-RESEARCH.md
@.planning/phases/105-golden-set/105-MEASUREMENTS.md
@server/codegraph/management/commands/measure_repo_index_stats.py
@server/tests/codegraph/test_measure_repo_index_stats.py
</context>

## Artifacts this phase produces（本 plan 新建符号）

| Symbol | Kind | Location |
|--------|------|----------|
| `--activity` / `--write-snapshot` | command 参数 | `server/codegraph/management/commands/measure_repo_index_stats.py` |
| `calibrate_repo_router_metadata` | management command（参数：`--facet <dim>`、`--negatives <N，默认 200>`、`--positives-file <path>`、`--structural`、`--format json\|markdown`） | `server/codegraph/management/commands/calibrate_repo_router_metadata.py` |
| `repo_router.nr_snapshot` 值形状 `{"n_r_by_repo": {rid: int}, "n_bar": float, "generated_at": iso}` | SystemSetting 值契约（106-02 已建键与 loader） | SystemSetting |
| `.planning/phases/106-multi-signal-scoring/106-MEASUREMENTS.md` | 实测文档（O-2/O-5 占位表 + 指引） | 文件 |

<tasks>

<task type="auto">
  <name>Task 1: measure command 扩展——O-5 活跃度/facets 覆盖率统计 + N_r 快照写入</name>
  <files>server/codegraph/management/commands/measure_repo_index_stats.py, server/tests/codegraph/test_measure_repo_index_stats.py, .planning/phases/106-multi-signal-scoring/106-MEASUREMENTS.md</files>
  <read_first>
    - server/codegraph/management/commands/measure_repo_index_stats.py（既有 add_arguments/handle/per_repo 计数循环/JSON+markdown 双输出/_LOG_KV 观测模式——全部复用）
    - server/tests/codegraph/test_measure_repo_index_stats.py（内存 Qdrant 结构性测试先例）
    - server/repositories/models.py（FileIndex.last_commit_authored_at 字段与 Repository.facets/is_deleted）
    - server/codegraph/services/repo_router_config.py（load_nr_snapshot 读取的值形状——写入端必须与之对齐）
    - .planning/phases/105-golden-set/105-MEASUREMENTS.md（数据环境标注纪律与占位表模式，106-MEASUREMENTS.md 照抄结构）
  </read_first>
  <action>
    1. `measure_repo_index_stats.py` 新增两个互不排斥的开关：
       - `--activity`：对 `Repository.objects.filter(is_deleted=False)` 输出（a）`last_commit_at` 口径统计——`FileIndex.objects.values("repository_id").annotate(latest=Max("last_commit_authored_at"))` 一次聚合，覆盖率 = 有值仓数/总仓数，新鲜度分位数 p50/p90（距 now 天数，stdlib 线性插值，照 repo_router_eval 分位数实现模式，禁 numpy）；（b）facets 五维覆盖率——按 `业务线/产品线`（非空且非"未分类"）/`技术栈`/`团队归属`/`关键程度`/`活跃度` 逐键统计非空占比。结果并入既有 report JSON（新节 `activity_stats` / `facets_coverage`）与 markdown 输出。
       - `--write-snapshot`：把既有 per_repo 计数循环产出的 `{repository_id: node_count}` 组装为 `{"n_r_by_repo": {rid: n}, "n_bar": <node_count>0 的仓的中位数（stdlib statistics.median）>, "generated_at": <UTC iso>}`，`json.dumps(ensure_ascii=False)` 经 `SystemSetting.objects.update_or_create(key=SettingKeys.REPO_ROUTER_NR_SNAPSHOT, defaults={"value": ..., "is_encrypted": False, "description": "..."})` 写入（post_save signal 自动失效 loader 缓存——写后 router 下一次路由即读到新值，注释说明）。无任何已索引仓时拒绝写入并输出提示（防空快照覆盖有效值）。stdout 明确回显写入的 n_bar 与仓数。
       - 中位数口径注释：用中位数不用均值——monorepo 拉爆均值（ROUTING-RANKING §2.3 N̄ 行）。
    2. 观测：沿用既有 `_LOG_KV` 模式补 `measure_activity_stats_completed` / `nr_snapshot_written`（caller、initiated_by_user_id="system"、component="codegraph"、duration_ms）。
    3. 测试扩展 `test_measure_repo_index_stats.py`：内存 Qdrant + `pytest.mark.django_db` 构造 2-3 个仓（含一个无 FileIndex 行的仓）——`--activity` 输出覆盖率/分位数结构正确、无 FileIndex 仓计入未覆盖；`--write-snapshot` 后 `load_nr_snapshot()` 读回 n_r_by_repo/n_bar 与写入一致（写读闭环）；空库 `--write-snapshot` 不写入。
    4. 创建 `106-MEASUREMENTS.md`：照 105 结构建 O-5 节（占位表：生产覆盖率/新鲜度分位数/facets 五维覆盖率，标注 `数据环境: 生产实例（deferred）`；执行指引：`uv run python manage.py measure_repo_index_stats --activity --write-snapshot --format markdown`）与开发库结构性结论区（本地执行结果，标注 `数据环境: 开发库（结构性结论）`）。O-2 节由 Task 2 补。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/codegraph/test_measure_repo_index_stats.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/codegraph/test_measure_repo_index_stats.py -q` 全绿（既有用例零改动 + 新增 >= 4）
    - 写读闭环断言存在：`--write-snapshot` 执行后 `load_nr_snapshot()["n_bar"]` == statistics.median(构造仓的节点数)
    - `rg -c "statistics.median" server/codegraph/management/commands/measure_repo_index_stats.py` >= 1 且 `rg -c "numpy" ...` == 0
    - 106-MEASUREMENTS.md 含字面 `数据环境: 开发库（结构性结论）` 与 `deferred`
  </acceptance_criteria>
  <done>O-5 统计与 N_r 快照管线可执行、可测试、写读契约闭环；生产执行只剩跑命令回填。</done>
</task>

<task type="auto">
  <name>Task 2: calibrate_repo_router_metadata command（O-2 校准管线）+ MEASUREMENTS O-2 节</name>
  <files>server/codegraph/management/commands/calibrate_repo_router_metadata.py, server/tests/codegraph/test_calibrate_repo_router_metadata.py, .planning/phases/106-multi-signal-scoring/106-MEASUREMENTS.md</files>
  <read_first>
    - server/codegraph/management/commands/measure_repo_index_stats.py（command 骨架/参数/输出/观测模式——新 command 同款）
    - server/services/embedding.py（generate_embeddings_batch 批量接口与失败语义）
    - server/repositories/summary_service.py + server/system/ 中 FacetVocabulary 模型定义处（语义分面闭集值来源：FacetVocabulary.objects.filter(is_active=True)）
    - .planning/research/ROUTING-RANKING.md §3.2（校准流程原文：200 负样本 p95→c_lo、30 正样本 p50→c_hi、区分度 < 0.10 弃用 T2）
  </read_first>
  <action>
    1. 新建 command `calibrate_repo_router_metadata`（独立于 measure——O-2 与 Qdrant 统计职责不同，RESEARCH §6 裁决）：
       - 参数：`--facet <dim>`（默认全部语义 facet + 技术栈）、`--negatives <N>`（默认 200，随机需求文本 × 随机 facet 值——需求文本源用 WorkItem/历史会话可得文本，开发库缺数据时配合 --structural）、`--positives-file <path>`（JSON `[{"query": ..., "facet_value": ...}]` 人工确认对）、`--structural`（结构性样本模式：内置合成 query/值对，用于开发库定管线）、`--format json|markdown`。
       - 流程：取 facet 闭集值（语义分面查 FacetVocabulary、技术栈用 `_EXT_LANGUAGE_MAP` 枚举；跳过 "未分类"）→ `generate_embeddings_batch` 批量取向量（`use_call_source(CallSource.EMBEDDING)` 作用域）→ 逐对余弦（stdlib）→ 负样本分布 p95 → `c_lo` 建议、正样本 p50 → `c_hi` 建议（无 positives-file 时该列输出 `需人工正样本，deferred`）→ 逐 facet 判定 `c_hi - c_lo < 0.10 → 建议加入 t2_disabled_facets`。
       - 输出：分布统计（min/p50/p95/max）、建议值、判定表；结尾提示「将建议值经 PUT /api/settings/repo-router/weight-config/ 写入 constants.t2_c_lo/t2_c_hi 与 t2_disabled_facets，并更新 weight_set_version 与 embedding_model_id（换模型必须重校准，CONTEXT 锁定）」。
       - EmbeddingService 未配置/失败：报错退出并提示 `--structural`；`--structural` 下用固定伪向量（seed 确定性）跑通全管线（管线正确性与真实分布解耦）。
       - 观测：`calibrate_repo_router_metadata_started/completed/failed`（caller、initiated_by_user_id="system"、component="codegraph"、duration_ms）；异常文本经 `redact_secrets_in_text` 后入日志。
    2. 测试 `test_calibrate_repo_router_metadata.py`：`--structural` 模式端到端（无网络，`pytest.mark.django_db`）——输出含 c_lo 建议、判定表结构正确；mock embedding 返回 None → 命令报错退出且提示 --structural；分位数计算与 repo_router_eval 同口径断言（固定输入 → 已知 p95）。
    3. `106-MEASUREMENTS.md` 补 O-2 节：占位表（各 facet c_lo/c_hi/判定，`数据环境: 生产实例（deferred）`）+ 执行指引（先 measure 覆盖率、再人工确认 30 组正样本、后跑 calibrate）+ 开发库结构性结论区 + deferred 挂账清单（O-1 N_r 分布 / O-2 校准数字 / O-5 覆盖率生产回填三项，UAT 人工步骤）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/codegraph/test_calibrate_repo_router_metadata.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/codegraph/test_calibrate_repo_router_metadata.py -q` 全绿，用例数 >= 4
    - `--structural` 模式在 `--disable-socket` 测试环境下零网络跑通（pytest-socket 不报）
    - `rg -c "redact_secrets_in_text" server/codegraph/management/commands/calibrate_repo_router_metadata.py` >= 1
    - 106-MEASUREMENTS.md 含 O-2 与 O-5 两节、deferred 挂账清单含 O-1/O-2/O-5 三项
  </acceptance_criteria>
  <done>O-2 校准从采样到判定的完整管线可执行；生产校准数字回填路径与 deferred 纪律落文档。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| --positives-file 外部 JSON → command | 运维提供的文件内容不可信，需结构校验 |
| command → SystemSetting 写入 | nr_snapshot 影响线上打分（breadth 分母） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-106-09 | Tampering | --write-snapshot | mitigate | 空库拒绝写入防覆盖；写入值形状与 load_nr_snapshot 契约闭环测试；loader 侧（106-02）非法回退 n_bar=None |
| T-106-10 | Information Disclosure | calibrate 日志/输出 | mitigate | 异常文本 redact_secrets_in_text；样本 query 文本入日志截断；embedding key 绝不入输出 |
| T-106-11 | DoS | calibrate 批量 embedding | mitigate | negatives 上限参数化（默认 200）；batch_size 沿用 generate_embeddings_batch 默认 32；失败即退不重试轰炸 |
| T-106-SC | Tampering | 包安装 | accept | 零新增依赖（分位数/中位数 stdlib） |
</threat_model>

<verification>
- `cd server && uv run pytest tests/codegraph/test_measure_repo_index_stats.py tests/codegraph/test_calibrate_repo_router_metadata.py -q` 全绿
- `cd server && uv run ruff check codegraph/management/commands/measure_repo_index_stats.py codegraph/management/commands/calibrate_repo_router_metadata.py`
</verification>

<success_criteria>
- N_r 快照写入与 O-5 统计并入 measure command，写读契约与 106-02 loader 闭环
- O-2 校准 command 独立可执行，embedding 缺失有结构性样本降级路径
- 106-MEASUREMENTS.md 占位表 + 执行指引 + deferred 挂账齐备（SC-5 的文档面）
</success_criteria>

<output>
完成后创建 `.planning/phases/106-multi-signal-scoring/106-04-SUMMARY.md`。
</output>
