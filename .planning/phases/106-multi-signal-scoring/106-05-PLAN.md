---
phase: 106-multi-signal-scoring
plan: 05
type: execute
wave: 3
depends_on: [106-02]
files_modified:
  - web/src/api/settings.ts
  - web/src/components/settings/RepoRouterWeightSettings.vue
  - web/src/pages/admin/index.vue
  - web/src/components/chat/RoutingDecisionPanel.vue
  - web/src/components/chat/__tests__/RoutingDecisionPanel.test.ts
autonomous: true
requirements: [ROUTE-06, ROUTE-04]
must_haves:
  truths:
    - "superuser 在管理页新的「仓库路由权重」设置区能读到当前生效配置（含默认态标注）、修改权重/常数并保存；网格外取值与文本主导破坏在前端即时提示（后端校验为准）——运维不发版调权重的操作面就位（ROUTE-06）"
    - "RoutingDecisionPanel 分数分解对新信号 domain/stack/team 显示中文标签（业务域匹配/技术栈匹配/团队归属）；未知 key 仍回退英文原名（向前兼容）"
    - "保存后的配置回读与后端 GET 一致；400 错误的 errors 列表逐条展示给用户"
  artifacts:
    - path: web/src/components/settings/RepoRouterWeightSettings.vue
      provides: "权重配置设置区组件（表单 + 网格下拉 + 前端预校验 + 保存）"
      min_lines: 120
    - path: web/src/api/settings.ts
      provides: "getRepoRouterWeightConfig / putRepoRouterWeightConfig API 函数"
      exports: ["getRepoRouterWeightConfig", "putRepoRouterWeightConfig"]
  key_links:
    - from: web/src/components/settings/RepoRouterWeightSettings.vue
      to: web/src/api/settings.ts
      via: "getRepoRouterWeightConfig/putRepoRouterWeightConfig 调 /settings/repo-router/weight-config/ 专用端点（不走通用 updateSetting——后者无校验）"
      pattern: "repo-router/weight-config"
    - from: web/src/components/chat/RoutingDecisionPanel.vue
      to: server/codegraph/services/repo_router_scoring.py
      via: "SIGNAL_LABELS 键与后端 SIGNAL_DOMAIN/SIGNAL_STACK/SIGNAL_TEAM 常量字面对齐（domain/stack/team）"
      pattern: "domain.*业务域匹配"
---

<objective>
补齐 ROUTE-06 的运维操作面与 ROUTE-04 的前端展示：新建 `RepoRouterWeightSettings.vue` 设置区（照 `RerankSettings.vue` 模式挂进 `admin/index.vue`，调 106-02 的专用端点，前端预校验网格与文本主导、后端为准）；`RoutingDecisionPanel.vue` 的 `SIGNAL_LABELS` 补三个新信号中文标签（facet 来源层 T1/T2 的 UI 展示按 RESEARCH §10 裁决留给 Phase 107，本 phase 只落数据）。

Purpose: SC-4「运维在系统设置里调整任一权重或常数并保存」的用户触面；SC-2 元数据贡献「可见」的展示面。
Output: 设置区组件 + API 函数 + 标签扩展 + vitest。
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/106-multi-signal-scoring/106-CONTEXT.md
@.planning/phases/106-multi-signal-scoring/106-RESEARCH.md
@web/src/components/settings/RerankSettings.vue
@web/src/pages/admin/index.vue
@web/src/api/settings.ts
@web/src/components/chat/RoutingDecisionPanel.vue
</context>

## Artifacts this phase produces（本 plan 新建符号）

| Symbol | Kind | Location |
|--------|------|----------|
| `getRepoRouterWeightConfig(): Promise<RepoRouterWeightConfig>` / `putRepoRouterWeightConfig(config)` | API 函数 | `web/src/api/settings.ts` |
| `RepoRouterWeightConfig` interface（weight_set_version/weights/constants/criticality_anchors/t2_disabled_facets/is_default） | TS 类型 | 同上 |
| `WEIGHT_GRID_OPTIONS = [0, 0.05, 0.08, 0.10, 0.12, 0.15, 0.20, 0.30, 0.40, 0.55]` | 常量（与后端 WEIGHT_GRID 字面一致） | `web/src/components/settings/RepoRouterWeightSettings.vue` |
| `RepoRouterWeightSettings.vue` | Vue 设置区组件 | `web/src/components/settings/` |
| `SIGNAL_LABELS` 新键 `domain: '业务域匹配'`、`stack: '技术栈匹配'`、`team: '团队归属'` | 前端标签键 | `web/src/components/chat/RoutingDecisionPanel.vue` |

<tasks>

<task type="auto">
  <name>Task 1: 权重配置 API 函数 + RepoRouterWeightSettings 设置区 + admin 挂载</name>
  <files>web/src/api/settings.ts, web/src/components/settings/RepoRouterWeightSettings.vue, web/src/pages/admin/index.vue</files>
  <read_first>
    - web/src/components/settings/RerankSettings.vue（最贴近的 section 组件样例：加载/保存/错误提示/表单布局模式，全盘照抄骨架）
    - web/src/pages/admin/index.vue（设置 tab 的 section 注册方式——新组件同款挂载）
    - web/src/api/settings.ts（get/put helper 与既有函数风格）
    - web/src/api/client.ts（ApiError 形状——400 响应 body.errors 的取用方式）
    - server/codegraph/services/repo_router_config.py（后端校验规则——前端预校验逐条对齐：网格、fsum(domain+stack+team) <= 0.5*fsum(五权重)、c_lo < c_hi）
  </read_first>
  <action>
    1. `web/src/api/settings.ts`：新增 `RepoRouterWeightConfig` interface 与 `getRepoRouterWeightConfig()` / `putRepoRouterWeightConfig(config)`（`get/put('/settings/repo-router/weight-config/')`）；加入默认导出对象。**不新增 SettingKey enum 值**（专用端点不走通用 per-key API）。
    2. 新建 `RepoRouterWeightSettings.vue`（`<script setup lang="ts">`，照 RerankSettings 骨架）：
       - 加载态/错误态/保存态；顶部展示 `weight_set_version` 与 `is_default`（默认配置未落库时提示「当前为内置默认值」）。
       - 五个权重字段（text/domain/activity/stack/team，中文 label：文本证据/业务域/活跃度/技术栈/团队归属）用下拉（options = WEIGHT_GRID_OPTIONS——离散网格防过拟合，不给自由输入）；关键常数字段（lam/b/n_cap/half_life_days/offset_days/crit_band/s_top_c_lo/s_top_c_hi/t2_c_lo/t2_c_hi）数字输入；`t2_disabled_facets` 逗号分隔文本框。
       - 前端预校验（保存按钮前置拦截 + 内联提示）：元数据三权重之和 > 五权重总和一半 → 提示「文本证据必须占主导（INV-R2）」；c_lo >= c_hi → 提示区间非法。后端 400 时逐条渲染 `errors` 数组。
       - 保存成功后重新 GET 回读（以后端规范化结果为准）；提示「保存后下一次路由立即生效，无需发版」。
       - 修改权重时提示用户同步更新 `weight_set_version`（输入框可编辑；不同版本的路由结果不可比——CONTEXT 生效语义）。
    3. `admin/index.vue` 设置区注册新组件（与 RerankSettings 并列，标题「仓库路由权重」）。
  </action>
  <verify>
    <automated>cd web && pnpm vue-tsc --noEmit 2>&1 | (! rg "RepoRouterWeightSettings|settings.ts" ) && pnpm eslint src/api/settings.ts src/components/settings/RepoRouterWeightSettings.vue --no-warn-ignored</automated>
  </verify>
  <acceptance_criteria>
    - `rg -c "repo-router/weight-config" web/src/api/settings.ts` >= 2（GET/PUT 各一）
    - `rg -c "updateSetting" web/src/components/settings/RepoRouterWeightSettings.vue` == 0（不走通用无校验 PUT）
    - `rg -c "RepoRouterWeightSettings" web/src/pages/admin/index.vue` >= 1（已挂载）
    - WEIGHT_GRID_OPTIONS 数组与后端 WEIGHT_GRID 字面一致（10 个值逐一相同）
    - `pnpm vue-tsc --noEmit` 对新增/修改文件零类型错误
  </acceptance_criteria>
  <done>运维在管理页可读改权重配置；非法输入前端即时拦截、后端错误逐条可见。</done>
</task>

<task type="auto">
  <name>Task 2: RoutingDecisionPanel 新信号标签 + vitest 更新</name>
  <files>web/src/components/chat/RoutingDecisionPanel.vue, web/src/components/chat/__tests__/RoutingDecisionPanel.test.ts</files>
  <read_first>
    - web/src/components/chat/RoutingDecisionPanel.vue:77-95（SIGNAL_LABELS map 与 signalLabel 回退、Σbreakdown≈score 容差校验——重归一化后仍成立，无需改）
    - web/src/components/chat/__tests__/RoutingDecisionPanel.test.ts（既有断言模式）
  </read_first>
  <action>
    1. `SIGNAL_LABELS` 追加 `domain: '业务域匹配'`、`stack: '技术栈匹配'`、`team: '团队归属'`（键与后端 SIGNAL_DOMAIN/SIGNAL_STACK/SIGNAL_TEAM 字面对齐；`text/breadth/activity` 不动；注释更新——Phase 106 六信号已入分，criticality 为旁路字段不入 breakdown 故无标签）。
    2. 测试更新：含 domain/stack/team 键的 breakdown 渲染出对应中文标签；未知 key（如 "future_signal"）仍回退英文原名（既有回退行为回归断言）；Σbreakdown==score 的候选无 console.warn（重归一化兼容断言）。
  </action>
  <verify>
    <automated>cd web && pnpm vitest run src/components/chat/__tests__/RoutingDecisionPanel.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run src/components/chat/__tests__/RoutingDecisionPanel.test.ts` 全绿，新增用例 >= 2
    - `rg -c "业务域匹配|技术栈匹配|团队归属" web/src/components/chat/RoutingDecisionPanel.vue` == 3
    - 既有 text/breadth/activity 标签断言零改动通过
  </acceptance_criteria>
  <done>新信号进 breakdown 即有中文标签；未知信号向前兼容回退保留。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 浏览器表单 → PUT weight-config | 前端预校验仅为 UX，后端（106-02）为强制面 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-106-12 | Tampering | RepoRouterWeightSettings 保存 | transfer | 校验强制在后端 view（T-106-03）；前端预校验仅提前反馈，绕过前端无影响 |
| T-106-13 | Information Disclosure | 设置区展示 | accept | 权重配置非敏感（is_encrypted=False）；页面本身在 superuser 管理面之内 |
| T-106-SC | Tampering | 包安装 | accept | 零新增 npm 依赖 |
</threat_model>

<verification>
- `cd web && pnpm vitest run src/components/chat/__tests__/RoutingDecisionPanel.test.ts` 全绿
- `cd web && pnpm vue-tsc --noEmit`（新增/修改文件零类型错误）
- `cd web && pnpm eslint src/api/settings.ts src/components/settings/RepoRouterWeightSettings.vue src/components/chat/RoutingDecisionPanel.vue --no-warn-ignored`
</verification>

<success_criteria>
- 运维权重编辑面就位：读/改/存/错误反馈全链路，走专用校验端点
- 新信号中文标签与后端 breakdown key 对齐，未知 key 回退兼容
</success_criteria>

<output>
完成后创建 `.planning/phases/106-multi-signal-scoring/106-05-SUMMARY.md`。
</output>
