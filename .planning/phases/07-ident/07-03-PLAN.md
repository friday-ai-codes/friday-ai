---
phase: 07-ident
plan: 03
type: execute
wave: 2
depends_on: [07-02]
files_modified:
  - server/mcp_tools/views.py
autonomous: true
requirements: [IDENT-03, IDENT-04]
must_haves:
  truths:
    - "McpToolView base sets permission_classes = [IsAuthenticated] (fail-closed); all 17 subclasses inherit"
    - "McpToolView base declares authentication_classes = [AccessTokenAuthentication, CookieJWTAuthentication]"
    - "Anonymous/invalid-token MCP request returns 401 with error_code authentication_failed"
    - "Valid-PAT MCP request authenticates as owner and reaches the business path (request.auth.token_hash still recorded)"
  artifacts:
    - path: "server/mcp_tools/views.py"
      provides: "fail-closed McpToolView base (IsAuthenticated + explicit auth classes)"
      contains: "IsAuthenticated"
  key_links:
    - from: "server/mcp_tools/views.py"
      to: "rest_framework.permissions.IsAuthenticated"
      via: "McpToolView.permission_classes"
      pattern: "permission_classes = \\[IsAuthenticated\\]"
    - from: "server/mcp_tools/views.py"
      to: "common.authentication.CookieJWTAuthentication"
      via: "McpToolView.authentication_classes"
      pattern: "CookieJWTAuthentication"
---

<objective>
Tighten the MCP/tool entry from fail-open to fail-closed (IDENT-03). The single change is to the `McpToolView` base class — all 17 subclasses (including `CreateMergeRequestView`, which inherits via `SummarizeBranchView`) inherit it: `permission_classes` from `[AllowAny]` to `[IsAuthenticated]`, and `authentication_classes` explicitly declared as `[AccessTokenAuthentication, CookieJWTAuthentication]` (Web-triggered MCP supported, per resolved Open Question 2).

This depends on 07-02: once auth returns the owner, a valid PAT yields `request.user = owner` which satisfies `IsAuthenticated`; without it, valid-PAT MCP calls (`mcp_client` tests) would 401. After this plan, 07-01's MCP `authentication_failed` assertion turns GREEN and the existing valid-PAT MCP tests stay GREEN.

Purpose: anonymous / invalid-token MCP requests are rejected at the permission layer before the view body (fail-closed), without leaking internal structure.
Output: fail-closed `McpToolView` base in `mcp_tools/views.py`.
</objective>

<execution_context>
@.cursor/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ident/07-CONTEXT.md
@.planning/phases/07-ident/07-RESEARCH.md
@.planning/phases/07-ident/07-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make McpToolView base fail-closed (IsAuthenticated + explicit auth classes)</name>
  <files>server/mcp_tools/views.py</files>
  <read_first>
    - server/mcp_tools/views.py (lines 13-16 imports: `from rest_framework.permissions import AllowAny`; lines 22 `from interactions.entry import AccessTokenAuthentication, begin_interaction_run`; lines 141-164 — `McpToolView` class with `authentication_classes = [AccessTokenAuthentication]`, `permission_classes = [AllowAny]`, `handle_exception` already returning `authentication_failed`/401 for NotAuthenticated, and `_begin`'s `if request.auth is None:` guard)
    - RESEARCH §Pattern 3 (base-class change propagates to all 17 subclasses; CreateMergeRequestView inherits via SummarizeBranchView) + §Code Examples "MCP 基类 fail-closed" + §Pitfall 3 (permission layer rejects before _begin → handle_exception → authentication_failed; this is why 07-01 converged the test to authentication_failed)
    - RESEARCH §Don't Hand-Roll (do NOT touch other AllowAny entries — runner/feishu/webhook/compat/subagent callbacks are independent trust boundaries, out of scope)
    - server/common/authentication.py (CookieJWTAuthentication — the class added to MCP auth classes for Web-triggered calls, Open Question 2 resolved: include it)
  </read_first>
  <action>
    Edit `server/mcp_tools/views.py`:
    (a) Imports: replace `from rest_framework.permissions import AllowAny` with `from rest_framework.permissions import IsAuthenticated`; add `from common.authentication import CookieJWTAuthentication` (group with existing imports, ruff import order).
    (b) `McpToolView` class body (lines ~144-146): set `authentication_classes = [AccessTokenAuthentication, CookieJWTAuthentication]` (PAT first, mirrors global order so a friday_pat_ Bearer hits the PAT class and a Web JWT/cookie hits CookieJWT) and `permission_classes = [IsAuthenticated]` (IDENT-03 fail-closed).
    (c) Keep `handle_exception` exactly as-is — it already maps `AuthenticationFailed`/`NotAuthenticated` to `error_response("authentication_failed", ..., 401)`, hard-coding 401 so it is immune to the global 401→403 downgrade. Keep `_begin`'s `if request.auth is None:` block as defense-in-depth (Web JWT path has `request.auth = validated_token` without `token_hash`, so `begin_interaction_run`'s `getattr(request.auth, "token_hash", "")` degrades to an empty string — no error; IDENT-04 preserved for the PAT path where request.auth is the AccessToken).
    Do NOT remove `AllowAny` usages anywhere else and do NOT touch any other view module (runner/feishu/webhook/compat are out of scope per RESEARCH Don't Hand-Roll). Confirm no leftover `AllowAny` reference remains in this file after the import swap.
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/mcp_tools -q</automated>
  </verify>
  <acceptance_criteria>
    - `McpToolView.permission_classes == [IsAuthenticated]` and `McpToolView.authentication_classes == [AccessTokenAuthentication, CookieJWTAuthentication]`.
    - No remaining `AllowAny` reference in `server/mcp_tools/views.py` (import removed).
    - `tests/mcp_tools/test_mcp_auth_errors.py::test_missing_token_returns_error_code` passes (401 + `authentication_failed`).
    - The valid-PAT MCP tests (`test_repository_not_found_error_code`, `test_repository_not_indexed_error_code`) pass — owner identity satisfies IsAuthenticated.
    - No other view module modified.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external client → MCP HTTP entry | Untrusted (possibly anonymous) request crosses into tool execution; access control is the backend boundary's job |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-09 | Elevation of Privilege | MCP entry fail-open (AllowAny) lets anonymous call tools | mitigate | Base `permission_classes = [IsAuthenticated]`; regression test asserts anonymous → 401 |
| T-07-10 | Information Disclosure | error response leaks internal structure | mitigate | `handle_exception` returns generic `authentication_failed`/401, no internals |
| T-07-11 | Repudiation | audit fingerprint lost for MCP calls | mitigate | PAT path keeps `request.auth = AccessToken`; begin_interaction_run token_hash path unchanged (IDENT-04) |
| T-07-SC | Tampering | npm/pip installs | accept | Zero new dependencies (pure code edit); RESEARCH Package Legitimacy Audit: N/A |
</threat_model>

<verification>
- `cd server && uv run pytest tests/mcp_tools -q` GREEN (fail-closed anonymous 401 + valid-PAT business paths + retrieval trace/audit).
- Full-phase gate (run after all waves): `cd server && uv run pytest tests/test_auth.py tests/test_auth_e2e.py tests/test_access_tokens.py tests/test_pat_identity.py tests/test_interactions_ledger.py tests/mcp_tools -q` GREEN.
</verification>

<success_criteria>
- IDENT-03: MCP entry is fail-closed — anonymous/invalid token → 401 `authentication_failed`; valid PAT (owner) reaches the business path.
- IDENT-04: PAT-authenticated MCP call still records `request.auth.token_hash` into the InteractionRun fingerprint.
- Scope discipline: only `mcp_tools/views.py` changed; other AllowAny trust boundaries untouched.
</success_criteria>

<output>
Create `.planning/phases/07-ident/07-03-SUMMARY.md` when done.
</output>
