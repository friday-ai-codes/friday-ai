---
phase: 103-container-integration
plan: 04
type: execute
wave: 2
depends_on: [103-01]
files_modified:
  - server/services/project_context_packer.py
  - server/chat/coding_session_service.py
  - server/workflows/nodes/ai/coding.py
  - server/tests/chat/test_coding_dispatch_context.py
  - server/tests/workflows/test_coding_dispatch_project_context.py
autonomous: true
requirements: [AGENT-04]

must_haves:
  truths:
    - "工作流路径派发的编码容器 prompt 头部含 pack_project_context 输出，且注入 env_FRIDAY_TASK_PROJECT_CONTEXT（与 Chat 路径一致）"
    - "_dispatch_wave 层按 (project, branch) 解析一次、逐仓复用（同 project 不重复召回）"
    - "项目定位：ProjectBranch 反查优先，work_item 关联 fallback；user = triggered_by"
    - "解析失败/无项目/无 user → 空字符串 no-op，dispatch 与现状逐字一致（fail-soft）"
    - "chat 路径行为零回归（helper 上提为纯重构，_prepend_project_context 语义不变）"
    - "workflow 不 import chat（共享 helper 落 services 层）"
  artifacts:
    - path: "server/services/project_context_packer.py"
      provides: "prepend_project_context + aresolve_project_for_repo_branch + apack_dispatch_context 共享 helper"
      exports: ["prepend_project_context", "aresolve_project_for_repo_branch", "apack_dispatch_context"]
    - path: "server/tests/workflows/test_coding_dispatch_project_context.py"
      provides: "workflow 派发上下文注入/复用/fail-soft 测试"
      min_lines: 80
  key_links:
    - from: "server/workflows/nodes/ai/coding.py"
      to: "server/services/project_context_packer.py"
      via: "_dispatch_wave 解析一次 → _run_repo_coding prompt prepend + env 注入"
      pattern: "apack_dispatch_context|prepend_project_context"
    - from: "server/chat/coding_session_service.py"
      to: "server/services/project_context_packer.py"
      via: "改引用共享 helper（不再自持私有实现）"
      pattern: "from services.project_context_packer import"
---

<objective>
落地 AGENT-04 工作流上下文对齐：把 chat 侧 `_prepend_project_context`（coding_session_service.py L294–301）与项目解析 helper（`_lookup_project_by_branch` L219–235 的 ProjectBranch 反查）上提至 `server/services/project_context_packer.py`（chat 改引用，workflow 直接用——避免 workflow import chat）；`workflows/nodes/ai/coding.py` `_dispatch_wave` 派发前按 (project, branch) 解析一次 `pack_project_context`，结果逐仓复用传入 `_run_repo_coding`，prompt prepend + `env_FRIDAY_TASK_PROJECT_CONTEXT` 注入与 chat 路径（L401–408）一致。解析失败/无项目 → 空字符串 no-op（chat 既有 fail-soft 语义）。

Purpose: 工作流派发的编码容器与 Chat 派发一样"一上来即有项目上下文"，容器编码质量对齐。
Output: packer 共享 helper + chat 纯重构 + workflow _dispatch_wave/_run_repo_coding 注入 + 双侧测试。

**依赖 103-01（同文件 + 逻辑两重）**：本 plan 与 103-01 都改 `coding.py` 与 `coding_session_service.py`（文件占用 → wave 2）；且 workflow 侧 user 复用 103-01 引入的 `_resolve_dispatch_user`/`dispatch_user` 线传（triggered_by 解析已就位，本 plan 直接消费不重复解析）。Phase 101 对 coding.py 的 write_back 改动在 `_finalize` 区域，与本 plan 触点（_dispatch_wave/_run_repo_coding prompt 装配）不同区域。
</objective>

<context>
@.planning/phases/103-container-integration/103-CONTEXT.md
@server/chat/coding_session_service.py
@server/workflows/nodes/ai/coding.py
@server/services/project_context_packer.py
@server/tests/chat/test_coding_dispatch_context.py

已核实坐标：
- chat 现实现：`_lookup_project_by_branch`（L219–235，sync_to_async + ProjectBranch filter(repository_id, branch_name).select_related("project").first()）；`_resolve_project_context_for_dispatch`（L238–291：bound_project 优先 → 分支反查 fallback → `pack_project_context(project, user, query=branch_name, conversation_id=...)` → strip → `redact_secrets_in_text` → 全程 fail-soft 返回 ""）；`_prepend_project_context`（L294–301：`f"{block}\n\n---\n\n{prompt}"`）；注入点 dispatch_coding_task L401–408（env_FRIDAY_TASK_PROJECT_CONTEXT + prompt prepend，仅 task_type != "coding_commit"）。
- `pack_project_context` 签名（services/project_context_packer.py L85–93）：`(project, user, *, query="", conversation_id="", token_budget=..., history_messages=None) -> PackedContext`；packer 内置 visibility fail-closed + RetrievalTrace（观测面已覆盖，本 plan 不新建召回埋点）。
- workflow 侧：`_dispatch_wave`（L526–644）构建 coding_tasks 前是插入解析的位置；`_run_repo_coding`（L1508–1692）prompt 由 `_build_coding_prompt` 产出（L1530）——prepend 在其后；env 装配 metadata dict L1636–1647。`config.get("work_item_id", "")` 已存在（L1639）。branch_name 形参即工作分支。
- work_item fallback 关联：`initiatives/models/work_item_link.py` `ProjectWorkItemLink`（project ↔ delivery.WorkItem，unique (project, work_item)）；WorkItem 按飞书三元组身份，config 里 work_item_id 是飞书 work_item_id 字符串 → `ProjectWorkItemLink.objects.filter(work_item__work_item_id=<id>).select_related("project").first()`（多命中取首个 fail-soft）。
- triggered_by user：103-01 已把 `_resolve_dispatch_user(context)` + `dispatch_user` 形参线传至 `_dispatch_wave`/`_run_repo_coding`，本 plan 直接用。
- 既有测试：`server/tests/chat/test_coding_dispatch_context.py`（chat 派发上下文注入先例，重构后须全绿）。
</context>

<tasks>

<task type="auto">
  <name>Task 1: helper 上提 packer + chat 改引用（纯重构零行为变化）</name>
  <files>server/services/project_context_packer.py, server/chat/coding_session_service.py, server/tests/chat/test_coding_dispatch_context.py</files>
  <action>
per D-AGENT-04 上提决策（workflow 不 import chat）：

1. `server/services/project_context_packer.py` 追加三个共享 helper（放 pack_project_context 之后，docstring 标注"chat 与 workflow 派发链共享，勿在各自模块复制实现"）：
   - `def prepend_project_context(prompt: str, context: str) -> str`：从 chat `_prepend_project_context` 逐字迁移（含空值短路语义）。
   - `async def aresolve_project_for_repo_branch(repository_id, branch_name: str)`：从 chat `_lookup_project_by_branch` 迁移（sync_to_async 包装保持；无分支名/无绑定 → None；多绑定取首个 fail-soft）。
   - `async def apack_dispatch_context(project, user, *, query: str = "", conversation_id: str = "") -> str`：封装 chat `_resolve_project_context_for_dispatch` 的后半段——project/user 任一 None → ""；`pack_project_context(...)` → strip → 空则 ""；`redact_secrets_in_text`；全程 try/except fail-soft 返回 ""，warning 事件 `dispatch_project_context_failed`（kv：error_type、component 由调用方 bind 决定，category="sampling"）。
2. `chat/coding_session_service.py`：删除私有 `_prepend_project_context` 与 `_lookup_project_by_branch`，`_resolve_project_context_for_dispatch` 改为解析 project/user 后调 `apack_dispatch_context`；dispatch_coding_task L401–408 改调 `prepend_project_context`（import from services.project_context_packer）。**行为零变化**：bound_project 优先级、coding_commit 短路、召回空不注入，全部保持。
3. `server/tests/chat/test_coding_dispatch_context.py`：若有对私有 helper 的直接引用/patch target 改指新位置（patch target 必须可 import，Phase 26 stale target 前科）；断言本身不变，全绿即证零回归。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/chat/test_coding_dispatch_context.py tests/chat -x -q && rg -c "_prepend_project_context|_lookup_project_by_branch" server/chat/coding_session_service.py; test $? -ne 0</automated>
  </verify>
  <done>chat 派发上下文测试全绿（零回归）；私有 helper 在 chat 模块零残留；共享 helper 可从 services 层 import。</done>
</task>

<task type="auto">
  <name>Task 2: workflow _dispatch_wave 解析一次逐仓复用 + prompt/env 注入 + 测试</name>
  <files>server/workflows/nodes/ai/coding.py, server/tests/workflows/test_coding_dispatch_project_context.py</files>
  <action>
per D-AGENT-04 workflow 接入（解析一次逐仓复用 + work_item fallback）：

1. `coding.py` 新增私有方法 `async def _resolve_wave_project_contexts(self, *, repo_ids, repositories, branch_name, config, dispatch_user, log) -> dict[str, str]`：
   - 对每个 repo_id：`project = await aresolve_project_for_repo_branch(repository_id=repo.id, branch_name=branch_name)`；None 时 fallback——`work_item_id = config.get("work_item_id", "")` 非空则经 `ProjectWorkItemLink.objects.filter(work_item__work_item_id=work_item_id).select_related("project").afirst()` 取 project（lazy import initiatives.models 防循环）。
   - **按 project 去重召回**：以 `str(project.id)` 为键缓存 `await apack_dispatch_context(project, dispatch_user, query=branch_name)` 结果，同 project 多仓复用（"按 (project, branch) 解析一次逐仓复用"——branch 在单次 wave 内恒定，project 维度去重即达成）；project/dispatch_user 任一 None → ""。
   - 返回 `{repo_id: context_text}`；任何异常 fail-soft 空串，绝不阻断 dispatch。
2. `_dispatch_wave`（L526 起）：openspec gate 之后、构建 coding_tasks 之前调用上述方法，`_run_repo_coding(...)` 新增形参 `project_context: str = ""` 传入各仓结果（默认空 → 非 wave/legacy 调用路径零回归）。**两个 _dispatch_wave 调用方（execute 首发 + aadvance 续 wave）均无需改**——解析在 _dispatch_wave 内部完成。
3. `_run_repo_coding`：`prompt = self._build_coding_prompt(...)` 之后，`project_context` 非空时 `prompt = prepend_project_context(prompt, project_context)` 且 metadata 追加 `"env_FRIDAY_TASK_PROJECT_CONTEXT": project_context`（镜像 chat L401–408 两件套）；空 → 与现状逐字一致。
4. 新建 `server/tests/workflows/test_coding_dispatch_project_context.py`（fixture 风格参考 tests/workflows conftest 与 test_remote_tool_dispatch.py 的 _run_repo_coding 直调法）：
   - ProjectBranch 绑定命中：dispatch metadata 含 env_FRIDAY_TASK_PROJECT_CONTEXT 且 DispatchTask.prompt 以上下文块开头（`---` 分隔符在）。
   - work_item fallback：无 ProjectBranch 绑定但 ProjectWorkItemLink 命中 → 同样注入。
   - 复用：同 project 两仓 mock `apack_dispatch_context` 断言只被调用一次。
   - fail-soft：无 project / 无 dispatch_user / packer 抛异常 → dispatch 成功且 metadata 无该键、prompt 无 prepend（与现状逐字一致）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/workflows/test_coding_dispatch_project_context.py tests/test_remote_tool_dispatch.py -x -q</automated>
  </verify>
  <done>workflow 派发容器 prompt 含 pack_project_context 输出 + env 注入与 chat 一致；同 project 只召回一次；fail-soft 零回归断言绿；103-01 的 dispatch 测试不受影响。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 项目上下文 → 容器 prompt/env | 召回内容进入容器 agent 上下文与 env |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-13 | Information Disclosure | 项目上下文携密钥进容器 | mitigate | apack_dispatch_context 内 redact_secrets_in_text 强制（迁移自 chat 既有防线，测试覆盖） |
| T-103-14 | Elevation of Privilege | 非成员 user 召回他人项目上下文 | mitigate | pack_project_context 内置 visibility fail-closed（既有机制，helper 不绕过） |
| T-103-15 | Denial of Service | 召回失败阻断编码派发 | mitigate | 全链 fail-soft 空串 no-op + 专项测试（Task 2） |
| T-103-SC | Tampering | 依赖安装 | accept | 零新增依赖 |
</threat_model>

<verification>
- `cd server && uv run pytest tests/workflows/test_coding_dispatch_project_context.py tests/chat/test_coding_dispatch_context.py tests/test_remote_tool_dispatch.py -q` 全绿。
- `rg "import chat|from chat" server/workflows/nodes/ai/coding.py` 零命中（workflow 不 import chat）。
- `rg "pack_project_context|RetrievalTrace" server/services/project_context_packer.py` 确认召回仍走 packer 单一入口（RetrievalTrace 埋点天然继承，无绕过）。
</verification>

<success_criteria>
对应 ROADMAP Phase 103 SC5 上下文半：工作流路径派发的编码容器 prompt 含 pack_project_context 输出（与 Chat 路径一致，_dispatch_wave 层解析一次逐仓复用）；观测经 packer 内置 RetrievalTrace 天然覆盖。
</success_criteria>

<output>
Create `.planning/phases/103-container-integration/103-04-SUMMARY.md` when done
</output>
