---
phase: 103-container-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/access_tokens/models.py
  - server/access_tokens/migrations/0003_accesstoken_kind_session_id.py
  - server/access_tokens/services.py
  - server/access_tokens/context.py
  - server/chat/coding_session_service.py
  - server/mcp_tools/execution_service.py
  - server/mcp_tools/views.py
  - server/mcp_tools/work_item_execution_service.py
  - server/workflows/nodes/ai/coding.py
  - server/workflows/nodes/base.py
  - server/workflows/engine/scheduler.py
  - server/workflows/api/views.py
  - server/workflows/triggers/dispatcher.py
  - server/subagent/api/callbacks.py
  - server/runners/consumers.py
  - server/tests/test_task_token_lifecycle.py
  - server/tests/test_remote_tool_dispatch.py
  - server/tests/test_trigger_dispatcher.py
  - server/tests/test_coding_wave.py
autonomous: true
requirements: [AGENT-01, AGENT-02]

must_haves:
  truths:
    - "三条派发链路（workflow / chat / MCP）派发编码任务时为发起用户铸造任务级短 TTL token，明文仅 dispatch 内存生成后直进容器 env"
    - "MCP 链桥接会话携带发起用户（Conversation.created_by = 调用视图的 request.user），MCP 派发后存在 kind=task 的 AccessToken（三链覆盖不静默失效）"
    - "DB 只存 sha256（token_hash），kind=task、session_id 关联 subagent session，expires_at = 任务 timeout + 10 分钟余量"
    - "全部真实终态写入点（callbacks _handle_completed/_handle_failed、consumers WS _handle_completed/_handle_failed、断连收敛）均按 session_id 幂等吊销任务 token"
    - "user 不可解析时不注入 token env，dispatch 行为与现状一致（降级不挂）"
    - "容器产物与服务端持久化数据（SubAgentSession.last_output / WorkflowExecution.context / 日志）无 friday_pat_ 明文泄漏"
    - "存量 personal token 行为零变化（kind 默认 personal，认证类零改动）"
  artifacts:
    - path: "server/access_tokens/services.py"
      provides: "mint_task_token / arevoke_task_tokens 统一铸造与吊销入口"
      exports: ["mint_task_token", "arevoke_task_tokens"]
    - path: "server/access_tokens/migrations/0003_accesstoken_kind_session_id.py"
      provides: "AccessToken.kind + session_id 字段迁移"
    - path: "server/tests/test_task_token_lifecycle.py"
      provides: "铸造/过期/吊销/认证复用/泄漏防线/MCP 链覆盖专项测试"
      min_lines: 80
  key_links:
    - from: "server/chat/coding_session_service.py"
      to: "server/access_tokens/services.py"
      via: "dispatch_coding_task 内 mint_task_token 后写 env_FRIDAY_TASK_USER_TOKEN"
      pattern: "mint_task_token"
    - from: "server/mcp_tools/execution_service.py"
      to: "chat.models.Conversation.created_by"
      via: "dispatch_execution(initiating_user=...) → _create_bridge_session 透传 created_by"
      pattern: "initiating_user"
    - from: "server/workflows/nodes/ai/coding.py"
      to: "server/access_tokens/services.py"
      via: "_run_repo_coding 内 mint（替换 user_pat_plaintext 透传）"
      pattern: "mint_task_token"
    - from: "server/subagent/api/callbacks.py"
      to: "server/access_tokens/services.py"
      via: "_handle_completed/_handle_failed 终态吊销"
      pattern: "arevoke_task_tokens"
    - from: "server/runners/consumers.py"
      to: "server/access_tokens/services.py"
      via: "WS _handle_completed/_handle_failed + 断连收敛终态吊销"
      pattern: "arevoke_task_tokens"
---

<objective>
落地 AGENT-01 任务级短 TTL token：复用 `AccessToken` 模型（加 `kind` + `session_id` 字段），新建 `server/access_tokens/services.py` 统一铸造入口 `mint_task_token`，三条派发链路统一铸造并经 `env_FRIDAY_TASK_USER_TOKEN` 注入容器；全部真实终态写入点按 `session_id` 吊销；同时在两条派发路径注入 `env_FRIDAY_TASK_KNOWLEDGE_ENDPOINT`（AGENT-02 的服务端 env 面，与 token env 同点位装配，收在本 plan 保持 103-02 纯 task 侧文件不相交）。

**MCP 链覆盖修订（checker BLOCKER 1）**：chat 与 MCP 共用 `dispatch_coding_task`，但 MCP 桥接会话（`_create_bridge_session`）创建的 `Conversation` 现状**不带 `created_by`**——若不修，MCP 链 mint 会静默跳过，SC1 三链覆盖失效。本 plan 给 `dispatch_execution` 加 `initiating_user` 显式参数，两个 MCP 视图调用链把 `request.user` 透传为桥接 Conversation 的 `created_by`。

**终态吊销全覆盖（checker BLOCKER 2）**：真实终态写入点共五处——callbacks HTTP 版 `_handle_completed`/`_handle_failed`、`runners/consumers.py` WS 版 `_handle_completed`/`_handle_failed`（独立写终态，不经 callbacks handler）、断连收敛 `_handle_disconnect_timeout`。五处全部挂吊销钩子。

短 TTL token 决策已定版（STATE.md 关键约束：显式推翻 PATX-04 搁置）；PAT-02 底线不破——mint 是"新造"不是"从 DB 反取"，明文仅内存生成直进 env，绝不落盘、绝不进日志。现有 `user_pat_plaintext` ContextVar 机会性透传通道被 mint 替换后成为死机制，一并移除（含两个引用该机制的存量测试迁移：`test_trigger_dispatcher.py`、`test_coding_wave.py`——checker BLOCKER 3）。

Purpose: 容器知识 MCP（AGENT-02）的前置——容器要带上短命、任务终态即吊销的凭证才能安全回调服务端工具面。
Output: kind/session_id 迁移 + services.py + 三链接线（含 MCP created_by 透传）+ 五点终态吊销 + 泄漏防线测试。

**并发执行注意**：本 plan 修改的 `server/workflows/nodes/ai/coding.py` 与 `server/mcp_tools/work_item_execution_service.py` 在 Phase 101 也有改动（write_back / `initiated_by_user_id` 线传）；本 plan 在 101/102 完成后执行，按 post-101 状态实施，以符号定位（本 plan 触点：env 装配区、`_resolve_user_pat`、`_dispatch_wave` 形参、`dispatch_execution` 调用点），不碰 write_back 逻辑。
</objective>

<context>
@.planning/phases/103-container-integration/103-CONTEXT.md
@.planning/ROADMAP.md
@server/access_tokens/models.py
@server/access_tokens/context.py
@server/chat/coding_session_service.py
@server/mcp_tools/execution_service.py
@server/workflows/nodes/ai/coding.py
@server/subagent/api/callbacks.py
@server/runners/consumers.py
@server/tests/test_remote_tool_dispatch.py

已核实坐标（规划期读码确认，行号为 Phase 100 完成时基线，101/102 合入后以符号定位为准）：
- `AccessToken`：PAT_PREFIX/`generate_pat()`（models.py L21–26）、`token_hash` sha256（L40）、`expires_at`/`revoked_at`（L54–57）、`is_valid`（L76–78）；`authentication.py` 前缀闸门 + sha256 查表零改动；`views.py acreate` L46–90 为创建先例（hash_token(plaintext)/token_prefix/token_suffix 写法照抄）。
- chat+MCP 汇聚点：`dispatch_coding_task`（coding_session_service.py L356–468）；`sub_session` 在第 4 步 `create_sub_session` 后才有 `session_id`，mint 须在其后、构造 `DispatchTask` 前写入 `env_metadata`；DispatchTask timeout 硬编码 3600。chat 路径目前**不注入任何 tools env**（grep 证实）。
- **MCP 桥接会话（BLOCKER 1 根因）**：`execution_service.py` `_create_bridge_session`（L86–122）`Conversation.objects.create(space=..., title=..., status=...)` **无 created_by**（chat/models.py L63–65：created_by 可空、SET_NULL）；`dispatch_execution`（L125–202）调用它后走 `dispatch_coding_task`。两个调用方均在有 `request.user` 的视图链上：`mcp_tools/views.py` ExecuteCodingPlanView（~L2124 `await dispatch_execution(...)`）；`work_item_execution_service.py` `_execute_one_task`（~L311 `await dispatch_execution(...)`，其上游 `execute_work_item_repo_tasks`（L539 起）由 `ExecuteWorkItemRepoTasksView`（views.py ~L1571）调用。post-101 该文件已有 `initiated_by_user_id` 线传先例（L471/L583），本 plan 的 `initiating_user`（User 实例）与之并行不混用。
- workflow 触点：`_resolve_user_pat`（coding.py L1488–1506，被替换对象）；两个调用点 L367（execute 首发）与 L999（aadvance 续 wave）；`_dispatch_wave` 形参 `user_pat`（L539）线传至 `_run_repo_coding(user_pat=...)`（L1519）；env 装配 tools_env L1596–1602；timeout = `config.get("timeout_seconds", 1800)`（L1633）；`session_id = generate_execution_id()`（L1537）。
- 死通道全链（唯一消费方是 coding 节点，mint 替换后全部移除）：`access_tokens/context.py`（ContextVar 模块）→ `workflows/api/views.py` L493–508（capture）→ `workflows/triggers/dispatcher.py` L105–107（get_request_pat）→ `engine/scheduler.py` L399 + L1117（`_user_pat_plaintext` 下传、`start_execution` 的 `user_pat` 形参）→ `nodes/base.py` L112（`user_pat_plaintext` 字段）→ coding.py `_resolve_user_pat`。**引用该机制的存量测试（BLOCKER 3）**：`server/tests/test_trigger_dispatcher.py`（L232 向 start_execution/dispatch 传 `user_pat=""`）、`server/tests/test_coding_wave.py`（L130 `node._resolve_user_pat = AsyncMock(return_value="")`）、`server/tests/test_remote_tool_dispatch.py`（整套机制测试）。
- **吊销落点（BLOCKER 2 修订后全集，已核实）**：(a) `subagent/api/callbacks.py` `_handle_completed`（~L818）/`_handle_failed`（~L914）——HTTP 回调；(b) `runners/consumers.py` `RunnerConsumer._handle_completed`（`await session.amark_completed()` ~L419）/`_handle_failed`（`await session.amark_failed(...)` ~L487）——**WS 直连路径独立写终态，不经 callbacks handler**；(c) `runners/consumers.py` `_handle_disconnect_timeout`（~L750，经 `session.amark_failed` 收敛）。`SubAgentSession.amark_timeout/amark_cancelled` 无调用方，残余假想路径由 TTL 自过期兜底。
- 用户解析：chat 链 `conversation.created_by_id`（conversation 已 select_related，本地字段安全）；MCP 链修复后同走此路径（created_by 已透传）；workflow 链 `context.workflow_execution.triggered_by_id`（先例：`workflows/nodes/triggers/manual.py` L64–74）。
- 观测：结构化事件按 `.cursor/rules/observability-logging.mdc`——`task_token_minted`/`task_token_revoked`，kv 字段，category="caller"，component="access_tokens"，绝不含明文。
</context>

<tasks>

<task type="auto">
  <name>Task 1: AccessToken 扩展 kind/session_id + mint/revoke service + 生命周期测试</name>
  <files>server/access_tokens/models.py, server/access_tokens/migrations/0003_accesstoken_kind_session_id.py, server/access_tokens/services.py, server/tests/test_task_token_lifecycle.py</files>
  <action>
per D-AGENT-01（CONTEXT §AGENT-01）：

1. `models.py`：`AccessToken` 加两字段——`kind = models.CharField(max_length=16, choices=[("personal","Personal"),("task","Task")], default="personal", db_index=True)`；`session_id = models.CharField(max_length=64, null=True, blank=True, db_index=True)`（任务 token 关联 subagent session_id；personal token 恒 None）。注释写明 kind 语义与兼容承诺（存量行为零变化：default=personal，认证类不读这两个字段）。生成迁移 `0003_accesstoken_kind_session_id.py`（手写或 makemigrations 产出均可，编号顺延 0002）。

2. 新建 `server/access_tokens/services.py`：
   - `async def mint_task_token(user, session_id: str, timeout_seconds: int) -> str`：`plaintext = generate_pat()`；`await AccessToken.objects.acreate(name=f"task:{session_id}", token_hash=hash_token(plaintext), token_prefix=plaintext[:12], token_suffix=plaintext[-4:], created_by=user, kind="task", session_id=session_id, expires_at=timezone.now() + timedelta(seconds=timeout_seconds) + timedelta(minutes=10))`（余量 10 分钟为 Claude's Discretion 采纳值）；结构化事件 `logger.info("task_token_minted", session_id=..., user_id=str(user.id), expires_in_seconds=..., category="caller", component="access_tokens")`——**绝不含明文/hash 前缀外的任何 token 材料**；返回明文（只在内存，调用方直接进 env）。
   - `async def arevoke_task_tokens(session_id: str) -> int`：`AccessToken.objects.filter(kind="task", session_id=session_id, revoked_at__isnull=True).aupdate(revoked_at=timezone.now())`，幂等（重复调用第二次 count=0）；成功后事件 `task_token_revoked`（session_id、count、initiated_by_user_id="system"、category="caller"、component="access_tokens"）；整体 try/except 吞异常返回 0（best-effort，绝不反噬回调主流程）。
   - 模块 docstring 写明 PAT-02 语义：mint 是新签发（明文只在内存返回一次），与"从 DB 反取明文"（被禁止且不可能——DB 只有 sha256）本质不同。

3. 新建 `server/tests/test_task_token_lifecycle.py`（pytest-django，参考 `server/tests/test_access_tokens.py` 既有 fixture 风格）：
   - mint 返回值以 `friday_pat_` 开头；DB 行 `token_hash == hash_token(plaintext)`、`kind=="task"`、`session_id` 正确、`expires_at` ≈ now + timeout + 600s（±60s 容差）；明文不出现在该行任何字段。
   - 认证零改动复用：用 minted 明文构造 `Authorization: Bearer` 请求打任一 AccessToken 保护端点（或直接调 `AccessTokenAuthentication().authenticate`），认证通过且 `request.auth.kind == "task"`。
   - 过期/吊销：将 `expires_at` 回拨后 `is_valid is False`；`arevoke_task_tokens(session_id)` 后 `is_valid is False` 且二次调用返回 0（幂等）。
   - 存量兼容：不带 kind 参数创建的 token（views.py 路径）kind=="personal"、session_id is None。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/test_task_token_lifecycle.py tests/test_access_tokens.py -x -q && uv run python manage.py makemigrations --check --dry-run</automated>
  </verify>
  <done>迁移与既有迁移链一致无缺失；mint/revoke 全生命周期测试绿；存量 access_tokens 测试零回归。</done>
</task>

<task type="auto">
  <name>Task 2: 三链接线——chat dispatch mint + MCP 桥接会话 created_by 透传 + workflow 替换机会性透传</name>
  <files>server/chat/coding_session_service.py, server/mcp_tools/execution_service.py, server/mcp_tools/views.py, server/mcp_tools/work_item_execution_service.py, server/workflows/nodes/ai/coding.py, server/tests/test_coding_wave.py</files>
  <action>
per D-AGENT-01 接入点 + D-AGENT-02 env 三要素（服务端注入面）：

**chat 链**（`dispatch_coding_task`，MCP 经桥接会话复用同函数）：
1. 在第 4 步 `create_sub_session` 之后、构造 `DispatchTask` 之前：仅当 `task_type != "coding_commit"`（不跑 SDK 的 commit 任务无需 token）——解析发起用户 `user_id = coding_session.conversation.created_by_id`（conversation 已加载，本地字段异步安全）；非 None 时 `user = await User.objects.filter(id=user_id).afirst()`；user 可解析时 `plaintext = await mint_task_token(user, sub_session.session_id, 3600)`（timeout 对齐本函数 DispatchTask 硬编码 3600）并 `env_metadata["env_FRIDAY_TASK_USER_TOKEN"] = plaintext`；user 不可解析 → 不注入（降级），日志只记 `has_user_token=bool`。
2. 同点位注入 `env_FRIDAY_TASK_KNOWLEDGE_ENDPOINT`：`base = settings.FRIDAY_BASE_URL.rstrip("/")` 非空时注入 base（**不带路径**，task 侧自行拼 `/api/mcp/tools/{name}/`；镜像 workflow tools_env 的空值不注入契约）。
3. **确认泄漏面**：`sub_session.last_output["dispatch"]` 落库 dict（L470–479）只含 prompt/branch 等，不得把 env_metadata 或 token 加进去（现状即不含，保持）。

**MCP 链（checker BLOCKER 1 修复）**：
4. `mcp_tools/execution_service.py`：`_create_bridge_session` 加 keyword 参数 `created_by=None`，`Conversation.objects.create(..., created_by=created_by)`；`dispatch_execution` 签名加 keyword `initiating_user=None`（User | None，默认 None 保持向后兼容——None 时行为与现状一致即不 mint），透传给 `_create_bridge_session(created_by=initiating_user)`。
5. 两个调用方补传：`mcp_tools/views.py` ExecuteCodingPlanView 的 `await dispatch_execution(...)`（~L2124）加 `initiating_user=request.user`；`work_item_execution_service.py`——`execute_work_item_repo_tasks`（L539 起）加 keyword `initiating_user=None` 并线传至 `_execute_one_task` → 其内 `dispatch_execution(...)`（~L311）；`ExecuteWorkItemRepoTasksView`（views.py ~L1571 调用处）传 `request.user`。注意与 post-101 已有的 `initiated_by_user_id`（字符串，观测归因）**并行不合并**——一个是 ORM User 实例给 created_by/mint，一个是日志归因字符串。
6. created_by 透传后 MCP 链在 `dispatch_coding_task` 内天然走第 1 步的 mint 路径，无需额外分支。

**workflow 链**（coding.py）：
7. 把 `_resolve_user_pat(context)` 替换为 `_resolve_dispatch_user(context)`：读 `context.workflow_execution.triggered_by_id`（先例 triggers/manual.py L64–74，fields_cache 已缓存则直用），返回 `User | None`；docstring 写明"有 user 就 mint，不再依赖请求头明文"。两个调用点（execute 首发 ~L367、aadvance 续 wave ~L999）同步替换；`_dispatch_wave` 形参 `user_pat: str` 改 `dispatch_user`（User | None）线传。
8. `_run_repo_coding`：形参 `user_pat: str = ""` 改 `dispatch_user=None`；env 装配处（tools_env 块）改为——`dispatch_user` 非 None 时 `plaintext = await mint_task_token(dispatch_user, session_id, config.get("timeout_seconds", 1800))` 并注入 `env_FRIDAY_TASK_USER_TOKEN`（session_id 即本函数 L1537 生成的 execution id，与预建 SubAgentSession.session_id 一致）；None → 不注入。同块注入 `env_FRIDAY_TASK_KNOWLEDGE_ENDPOINT`（同 chat：FRIDAY_BASE_URL 非空才注入）。既有 `env_FRIDAY_TASK_TOOLS_ENDPOINT` 注入保持不动。日志纪律沿用：只记 `has_user_token=bool`。
9. **同步迁移存量测试**（`_resolve_user_pat` 移除即打红，checker BLOCKER 3 之一）：`server/tests/test_coding_wave.py` L130 `node._resolve_user_pat = AsyncMock(return_value="")` 改为 `node._resolve_dispatch_user = AsyncMock(return_value=None)`（语义等价：无 user 不注入）。

注意：本 task 改动的 coding.py 区域（env 装配、_dispatch_wave 形参）与 Phase 101 的 write_back 改动（_finalize 区域/config_schema）不同区域；rebase 时以符号定位。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/test_coding_wave.py -x -q && uv run python -c "import django,os;os.environ.setdefault('DJANGO_SETTINGS_MODULE','friday.settings');django.setup();import workflows.nodes.ai.coding,chat.coding_session_service,mcp_tools.execution_service,mcp_tools.work_item_execution_service" && ! rg -q "_resolve_user_pat" workflows/nodes/ai/coding.py && rg -q "initiating_user" mcp_tools/execution_service.py</automated>
  </verify>
  <done>三链 dispatch 均经 mint_task_token 注入 token env；MCP 桥接 Conversation 携带 created_by；coding.py 无 `_resolve_user_pat` 残留；test_coding_wave 迁移后绿；全模块可 import。</done>
</task>

<task type="auto">
  <name>Task 3: 死通道清理 + 五点终态吊销 + 泄漏防线/MCP 覆盖测试 + 存量测试迁移</name>
  <files>server/workflows/nodes/base.py, server/workflows/engine/scheduler.py, server/workflows/api/views.py, server/workflows/triggers/dispatcher.py, server/access_tokens/context.py, server/subagent/api/callbacks.py, server/runners/consumers.py, server/tests/test_remote_tool_dispatch.py, server/tests/test_trigger_dispatcher.py, server/tests/test_task_token_lifecycle.py</files>
  <action>
per D-AGENT-01 吊销与泄漏防线 + 死通道收尾：

1. **死通道清理**（Task 2 已替换消费方，此处删上游管道，不留死机制）：删除 `access_tokens/context.py` 模块；删除 `workflows/api/views.py` L493–508 的 set_request_pat/reset_request_pat capture 块；删除 `workflows/triggers/dispatcher.py` L105–107 的 get_request_pat 读取及 user_pat 传参；删除 `engine/scheduler.py` L399 `execution._user_pat_plaintext` 写入与 L1117 下传、`start_execution` 的 `user_pat` 形参及全部调用方传参（rg 定位）；删除 `nodes/base.py` L112 `user_pat_plaintext` 字段。
2. **同步迁移存量测试**（checker BLOCKER 3 之二）：`server/tests/test_trigger_dispatcher.py` L232 向 start_execution/dispatch 传 `user_pat=""` 的调用随形参删除而移除该 kwarg（及文件内其它 request_pat/user_pat 引用，rg 全文件核对）。
3. **终态吊销五点全挂（checker BLOCKER 2 修复）**：
   - `subagent/api/callbacks.py`：`_handle_completed` 与 `_handle_failed` 各在既有 best-effort 区段（与回写/通知同风格 try/except）追加 `await arevoke_task_tokens(session.session_id)`。
   - `runners/consumers.py` **WS 版**（独立终态写入点，不经 callbacks handler）：`RunnerConsumer._handle_completed` 在 `await session.amark_completed()`（~L419）之后、`RunnerConsumer._handle_failed` 在 `await session.amark_failed(...)`（~L487）之后，各追加 best-effort 吊销（try/except 吞异常，绝不阻塞 WS 消息处理）。
   - `runners/consumers.py` `_handle_disconnect_timeout`（~L780 `session.amark_failed` 收敛处）之后同样追加。
   - 在 callbacks 追加处写注释：`amark_timeout/amark_cancelled` 无调用方（规划期核实），残余假想路径由 expires_at 自过期兜底。
4. `server/tests/test_task_token_lifecycle.py` 追加泄漏防线与吊销集成测试：
   - chat 链集成：mock runner 在线 + dispatcher，调 `dispatch_coding_task` 后断言 (a) DispatchTask.metadata 含 `env_FRIDAY_TASK_USER_TOKEN` 且值以 friday_pat_ 开头、`env_FRIDAY_TASK_KNOWLEDGE_ENDPOINT` == settings.FRIDAY_BASE_URL；(b) 对应 AccessToken 行 kind=="task"、session_id==sub_session.session_id、token_hash==hash_token(env 中明文)（证明"新签发"而非复用存量）；(c) **落库泄漏扫描**：`json.dumps(SubAgentSession last_output)` 与 CodingSession 相关持久化字段均不含 "friday_pat_"。
   - **MCP 链覆盖（checker BLOCKER 1 验收钉）**：构造 McpCodingPlan/version fixture，`dispatch_execution(..., initiating_user=<user>)`（mock runner/dispatcher 同上）后断言桥接 `Conversation.created_by == user` 且存在 kind=="task"、created_by==user 的 AccessToken（session_id 对应新建 sub_session）。
   - 终态吊销双路径：对同一 session 分别经 callbacks `_handle_completed` 与 consumers WS `_handle_completed`（直调 handler 或构造消息）触发，断言 task token `revoked_at` 非空；重复触发不报错（幂等）。
   - user 不可解析降级：conversation.created_by 为 None 时 dispatch 成功且 metadata 无 `env_FRIDAY_TASK_USER_TOKEN` 键。
5. 更新 `server/tests/test_remote_tool_dispatch.py`（存量 PAT-02 断言语义保持，机制换代）：
   - 删除/替换 ContextVar 与 `_resolve_user_pat` 机制测试（test_pat_context_var_roundtrip、test_resolve_user_pat_reads_execution_context_field、test_dispatch_opportunistic_pat_injected_when_present 等）为 mint 机制等价物：workflow `_run_repo_coding` 传 `dispatch_user` 时 metadata 注入新签发 token；无 user 时省略键。
   - `test_dispatch_never_reads_access_token_plaintext`（T-11-02 spy）语义更新：mint 会 `acreate` 新行——断言收窄为"dispatch 路径绝不调用 AccessToken 的**读取类** manager 方法取存量 token"；模块 docstring 更新说明：PAT-02 底线不变（明文不落盘、不可从 DB 反取），mint 是新签发（Key Decisions 已定版推翻 PATX-04）。
   - 保留 `test_dispatch_metadata_includes_tools_endpoint` 等与本次无关断言。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/test_task_token_lifecycle.py tests/test_remote_tool_dispatch.py tests/test_trigger_dispatcher.py tests/subagent -x -q && ! rg -q "user_pat_plaintext|set_request_pat|get_request_pat|_resolve_user_pat" . --glob '!tests/**' && ! rg -q "user_pat" tests/test_trigger_dispatcher.py tests/test_coding_wave.py</automated>
  </verify>
  <done>五个终态写入点（callbacks×2 + consumers WS×2 + 断连收敛）后 task token 被吊销（幂等）；MCP 链派发后 kind=task token 存在断言绿；泄漏扫描断言绿；死通道 rg 守门零命中（源码与两个迁移测试均无残留）；PAT-02 存量测试按新语义全绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server → task 容器 env | token 明文经 runner WS + docker env 进入不可信工作负载 |
| 容器 → server HTTP 工具面 | 容器持 task token 回调，认证/授权由服务端裁决 |
| MCP 调用方 → 桥接会话归属 | initiating_user 决定 token 归属与项目上下文可见性 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-01 | Information Disclosure | mint_task_token / dispatch 链 | mitigate | 明文只在 dispatch 内存；DB 只存 sha256；日志只记 has_user_token=bool；落库泄漏扫描测试（Task 3） |
| T-103-02 | Elevation of Privilege | 泄漏的 task token 被长期滥用 | mitigate | expires_at=timeout+10min 短 TTL + 五点终态幂等吊销（Task 3）；认证复用 is_valid 天然拒过期/吊销 |
| T-103-03 | Spoofing | 伪造 session_id 吊销他人 token | accept | 吊销入口仅服务端内部调用（callbacks/consumers），无外部可达面；吊销本身是失效操作，无提权价值 |
| T-103-04 | Repudiation | token 铸造/吊销不可归因 | mitigate | task_token_minted/revoked 结构化事件（user_id/session_id/category=caller）；MCP 桥接会话 created_by 落库可归因（Task 2） |
| T-103-16 | Spoofing | initiating_user 由服务端视图从已认证 request.user 取得 | accept | 非用户可控入参；仅服务端代码传入 |
| T-103-SC | Tampering | 依赖安装 | accept | 本 plan 零新增第三方依赖（纯存量栈） |
</threat_model>

<verification>
- `cd server && uv run pytest tests/test_task_token_lifecycle.py tests/test_remote_tool_dispatch.py tests/test_access_tokens.py tests/test_trigger_dispatcher.py tests/test_coding_wave.py tests/subagent tests/chat -q` 全绿。
- 死通道守门（repo 根目录执行）：`! rg -q "user_pat_plaintext|_resolve_user_pat|set_request_pat|get_request_pat" server/` 通过（含测试迁移后全仓零命中）。
- 明文泄漏面（repo 根目录执行）：`rg -n "friday_pat_" server/ --glob '!server/tests/**' --glob '!server/access_tokens/**'` 无新增明文拼接点（人工核对输出）。
- `cd server && uv run python manage.py makemigrations --check --dry-run` 无缺失迁移。
</verification>

<success_criteria>
对应 ROADMAP Phase 103 SC1 + SC3 泄漏半：三链统一铸造短 TTL token（MCP 链经桥接会话 created_by 透传不静默失效；明文内存直进 env、DB 只存 sha256、expires=timeout+余量、五点终态吊销、PAT-02 不违反）；容器产物与持久化数据无 friday_pat_ 泄漏；user 不可解析时降级不注入零回归；`env_FRIDAY_TASK_KNOWLEDGE_ENDPOINT` 已在两条派发路径就位（供 103-02 task 侧消费）。
</success_criteria>

<output>
Create `.planning/phases/103-container-integration/103-01-SUMMARY.md` when done
</output>
