---
phase: 103-container-integration
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - task/scripts/sync_skills.py
  - task/assets/skills/friday-code/**
  - task/assets/skills/friday-memory/**
  - task/Dockerfile
  - task/core/runner.py
  - task/tests/test_skills_injection.py
autonomous: true
requirements: [AGENT-03]

must_haves:
  truths:
    - "容器内编码代理可见并遵循 friday-code / friday-memory skills（{workspace}/.claude/skills/ 经 setting_sources=[\"project\"] 既有通道加载，零 executor 改动）"
    - "镜像物料构建期从 skills/skills/ 同源 COPY（经 task/assets/skills/ 中转，因 build context 是 ./task）"
    - "运行时注入同名不覆盖仓库自带 skills"
    - "hash 一致性测试防双源漂移（task/assets/skills/ 与 skills/skills/ 逐文件一致），CI 可跑、仓库根不可达时 skip"
    - "镜像无 skills 目录 / 本地 CLI 模式下注入静默跳过，存量任务零回归"
  artifacts:
    - path: "task/scripts/sync_skills.py"
      provides: "幂等同步脚本 skills/skills/{friday-code,friday-memory} → task/assets/skills/"
    - path: "task/assets/skills/friday-code/SKILL.md"
      provides: "提交入库的镜像物料（可重现构建 + hash 测试可跑）"
    - path: "task/tests/test_skills_injection.py"
      provides: "hash 一致性 + 运行时注入（含同名跳过）测试"
      min_lines: 60
  key_links:
    - from: "task/Dockerfile"
      to: "task/assets/skills/"
      via: "COPY --chown=friday:friday assets/skills/ /opt/friday/skills/"
      pattern: "opt/friday/skills"
    - from: "task/core/runner.py"
      to: "/opt/friday/skills/"
      via: "run() 在 git_ops.setup() 后 copytree 到 {workspace}/.claude/skills/（同名跳过）"
      pattern: "_inject_skills|\\.claude/skills"
---

<objective>
落地 AGENT-03 skills 同源注入：`skills/skills/{friday-code,friday-memory}` 是单一事实源（STATE.md 关键约束：禁止第二份手工物料）；因 task 镜像 build context 是 `./task`，经幂等同步脚本拷入 `task/assets/skills/`（**提交入库**——保证可重现构建 + hash 测试可跑，CONTEXT 已定不走 .gitignore）；Dockerfile COPY 到镜像固定路径 `/opt/friday/skills/`；运行时 `runner.py run()` 在 `git_ops.setup()` 之后拷入 `{workspace}/.claude/skills/`（同名跳过不覆盖仓库自带），经 executor 既有 `setting_sources=["project"]` 通道加载，零 executor 改动；hash 一致性测试防双源漂移（PITFALLS P5）。

不裁剪 skills 内容（CONTEXT 决策：setup 向导段对容器无害，裁剪引入生成逻辑得不偿失）。

Purpose: 容器内代理与平台侧同源可见 Friday 技能路由，编码行为对齐。
Output: 同步脚本 + 提交入库的 assets + Dockerfile COPY + 运行时注入 + hash 测试。

**时序说明**：Phase 102-03 会更新 `skills/skills/{friday-code,friday-memory}/SKILL.md`（snapshot 对齐）；本 plan 在 102 完成后执行，同步脚本跑在 post-102 状态上，assets 天然含最新内容。
</objective>

<context>
@.planning/phases/103-container-integration/103-CONTEXT.md
@task/Dockerfile
@task/core/runner.py
@task/core/executor.py

已核实坐标：
- `skills/skills/` 现有四目录：friday / friday-code / friday-feishu / friday-memory，各含 SKILL.md + references/；**只同步 friday-code 与 friday-memory 两个**（friday-feishu / friday 总技能已列 Deferred Ideas，勿扩）。
- `task/Dockerfile`：多阶段（builder + runtime）；runtime 阶段 `adduser -D -u 1000 friday` 在 L61–64，`COPY entrypoint.sh` L66，`USER friday` L71。skills COPY 放 adduser 之后、entrypoint COPY 附近，用 `--chown=friday:friday`。现无 assets/ 目录、无 .dockerignore 阻碍。
- `task/core/runner.py run()`：`git_ops.setup()` L92；分支处理 L96–120；`ClaudeRunner` 初始化 L123–125。注入点放 setup() 成功后、ClaudeRunner 之前（plan/execute/explore/repo_summary 各模式统一生效）。workspace 路径 = `self.git_ops.get_workspace_path()`。
- executor `setting_sources=["project"]`（executor.py L597）已加载 `{cwd}/.claude/` 项目级配置——注入到 workspace/.claude/skills/ 即可见，零 executor 改动。
- `task/scripts/` 目录不存在（新建）；`task/assets/` 不存在（新建）。
</context>

<tasks>

<task type="auto">
  <name>Task 1: 同步脚本 + 执行同步提交 assets + Dockerfile COPY</name>
  <files>task/scripts/sync_skills.py, task/assets/skills/friday-code/**, task/assets/skills/friday-memory/**, task/Dockerfile</files>
  <action>
per D-AGENT-03（CONTEXT §AGENT-03，脚本语言 Claude's Discretion 采纳 Python——跨平台 + hash 测试同语言）：

1. 新建 `task/scripts/sync_skills.py`（纯 stdlib，无第三方依赖）：常量 `SKILL_NAMES = ("friday-code", "friday-memory")`；仓库根定位 `Path(__file__).resolve().parents[2]`（task/scripts/ → 仓库根），源 `repo_root/skills/skills/{name}`，目标 `task/assets/skills/{name}`。幂等：目标存在先 `shutil.rmtree` 再 `shutil.copytree`；源缺失 → 报错退出非 0（防静默漂移）；结束打印各 skill 文件数摘要。加 `if __name__ == "__main__":` 入口与模块 docstring（写明"单一事实源是 skills/skills/，本目录是构建物料镜像拷贝，改动技能请改源头后重跑本脚本"）。

2. 执行 `python task/scripts/sync_skills.py`，将产出的 `task/assets/skills/{friday-code,friday-memory}/`（SKILL.md + references/ 全量）提交入库。在 `task/assets/skills/` 放置 `README.md` 一行说明：由 sync_skills.py 生成，勿手工编辑（防第二份手工物料，P5）。

3. `task/Dockerfile` runtime 阶段：在 `adduser` RUN 块之后追加 `COPY --chown=friday:friday assets/skills/ /opt/friday/skills/`（固定路径 per CONTEXT）。注释标注同源链路：skills/skills/ → (sync_skills.py) → assets/skills/ → (COPY) → /opt/friday/skills/ → (runner 运行时) → workspace/.claude/skills/。
  </action>
  <verify>
    <automated>python3 task/scripts/sync_skills.py && python3 task/scripts/sync_skills.py && test -f task/assets/skills/friday-code/SKILL.md && test -f task/assets/skills/friday-memory/SKILL.md && grep -c "opt/friday/skills" task/Dockerfile | grep -qv "^0$"</automated>
  </verify>
  <done>脚本幂等（连跑两次成功）；assets 两技能目录完整入库；Dockerfile 含 COPY 行且 chown friday。</done>
</task>

<task type="auto">
  <name>Task 2: 运行时注入（同名不覆盖）+ hash 一致性测试</name>
  <files>task/core/runner.py, task/tests/test_skills_injection.py</files>
  <action>
per D-AGENT-03 运行时注入与 P5 防漂移：

1. `runner.py`：模块级常量 `IMAGE_SKILLS_DIR = Path("/opt/friday/skills")`；新增私有方法 `_inject_skills(self, log) -> None`——源目录不存在（本地 CLI/旧镜像）→ `log.debug` 后静默返回（零回归）；存在则对每个子目录 `{IMAGE_SKILLS_DIR}/{name}`：目标 `{workspace}/.claude/skills/{name}` **已存在 → 跳过不覆盖**（仓库自带优先，逐目录同名判断）；不存在 → `shutil.copytree`；全程 try/except 吞异常只 warning（skills 注入 best-effort，绝不因此挂任务）；日志记 injected/skipped 技能名列表（无敏感内容）。在 `run()` 的 `git_ops.setup()` 成功后、分支处理之前调用 `self._inject_skills(log)`（各 task_mode 统一生效）。

2. 新建 `task/tests/test_skills_injection.py` 两组测试：
   - **hash 一致性（P5 核心钉）**：从测试文件向上逐级查找含 `skills/skills/friday-code/SKILL.md` 的目录定位仓库根；找不到 → `pytest.skip("repo root not reachable — task 独立构建上下文")`；找到则对 `task/assets/skills/{name}` 与 `skills/skills/{name}` 递归比对：相对路径文件集合一致 + 每文件 sha256 一致；失败信息提示"跑 python task/scripts/sync_skills.py 重新同步"。
   - **运行时注入单测**：monkeypatch `IMAGE_SKILLS_DIR` 指向 tmp_path 构造的假镜像 skills（两个技能目录），构造假 workspace；调 `_inject_skills` 断言 (a) 两技能拷达 `.claude/skills/`；(b) workspace 预置同名 `friday-code`（内容不同）时不被覆盖、`friday-memory` 正常拷入；(c) 源目录不存在时不抛异常。
  </action>
  <verify>
    <automated>cd task && uv run pytest tests/test_skills_injection.py -x -q && uv run pytest tests/ -q</automated>
  </verify>
  <done>hash 一致性测试在仓库根可达时红绿可证（改动 assets 任一文件即红）；同名不覆盖/静默降级断言绿；task 全量测试零回归。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 镜像物料 → 容器 workspace | skills 内容进入 agent 可执行上下文（提示注入面） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-10 | Tampering | assets 与源双源漂移（物料被单独篡改） | mitigate | hash 一致性 CI 测试 + README 勿手工编辑 + 脚本单向同步（Task 1/2） |
| T-103-11 | Elevation of Privilege | 注入覆盖仓库自带 skills 改变 agent 行为 | mitigate | 同名跳过不覆盖，仓库自带优先（Task 2 断言） |
| T-103-12 | Denial of Service | 注入异常挂掉任务 | mitigate | best-effort try/except + 静默降级测试（Task 2） |
| T-103-SC | Tampering | 依赖安装 | accept | 零新增依赖（stdlib shutil/hashlib） |
</threat_model>

<verification>
- `python3 task/scripts/sync_skills.py && git status --porcelain task/assets/` 输出为空（同步后无 diff = 无漂移）。
- `cd task && uv run pytest tests/ -q` 全绿。
- `grep -n "opt/friday/skills" task/Dockerfile task/core/runner.py` 两处路径一致。
</verification>

<success_criteria>
对应 ROADMAP Phase 103 SC4：容器内代理可见并遵循 friday-code / friday-memory skills（构建期同源 COPY + 运行时注入同名不覆盖），hash 一致性测试通过防双源漂移。
</success_criteria>

<output>
Create `.planning/phases/103-container-integration/103-03-SUMMARY.md` when done
</output>
