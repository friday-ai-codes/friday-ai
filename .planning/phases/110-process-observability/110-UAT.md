---
status: testing
phase: 110-process-observability
source: [110-VERIFICATION.md, 110-UI-REVIEW.md]
started: 2026-07-31T10:10:00Z
updated: 2026-07-31T10:10:00Z
---

## Current Test

number: 1
name: SSE 直播链端到端实跑（前半程是否秒级）
expected: |
  真实会话触发一次跨仓编排，肉眼确认前半程（拆分 / 路由 / 召回 / 澄清）的阶段是**秒级**逐个出现，
  而不是每 2 秒跳一格。若呈 2 秒节拍，说明 `get_stream_writer()` 在 tool 调用栈里解析不到 writer，
  SSE 直播链静默失效——功能不坏，退化为快照节拍。
  全部 fan-out 用例都 monkeypatch 掉了 `langgraph.config.get_stream_writer`，端到端 SSE 路径从未真正跑过。
awaiting: user response

## Tests

### 1. SSE 直播链端到端实跑（前半程是否秒级）
expected: 阶段推进与后端事件同步出现（秒级）；呈 2 秒节拍即为直播链静默失效
requirement: OBS-01
result: [pending]

### 2. `plan_session_id` 与 tool result `session_id` 的跨进程字符串相等
expected: 两者逐字相同，日志组出现在编排气泡内
note: 两侧各有后端用例，但跨进程相等性从未端到端测量。若看到「时间线在跑、日志组永不出现」，这是第一嫌疑。
requirement: OBS-02
result: [pending]

### 3. 真实调研容器的日志呈现
expected: 每仓一张卡、卡标题是仓库名、日志随容器 stdout 增量刷新，编排完成后仍可展开查看；无裸 UUID 上屏；凭据类文本已在服务端脱敏
requirement: OBS-02
result: [pending]

### 4. 前半程失败的时间线表现（GAP-1 修复后复验）
expected: 打断 route 或 recall stage 后，时间线立即把该步标红并显示「未知原因」，标题变为失败态——不再持续显示「正在生成技术方案」、不需要刷新才自愈
note: GAP-1 已在 commit `ba761025` 闭合（快照缺席时用失败事件兜底），配 4 条用例 + 负向对照。此项是浏览器实测复验。同根因的脉冲缺席已在 `0681b463` 一并修复：前半程直播时该步应有脉冲。
requirement: OBS-03
result: [pending]

### 5. 读屏走调研日志组的折叠按钮
expected: 能听到「方案调研 · N 个仓库」
note: 当前实现用 `aria-label="展开方案调研日志"` 覆盖了可见文案，读屏用户听不到仓库数（WCAG 2.5.3 Label in Name，Level A）。属已知待修项。
requirement: OBS-02
result: [pending]

### 6. 五仓并行时 live region 的播报节奏
expected: 只在活跃阶段 key 或会话状态变化时播报一次，不随各仓完成连播
note: 代码层已守住（`COPY.stepCount` 不进 live region，卡内 `aria-live` 恰 1 个，有用例锁），须实听确认节奏
requirement: OBS-03
result: [pending]

### 7. 亮/暗主题下 `skipped` / `unknown` 空心点的可辨识度
expected: 空心与实心的形状差异在两种主题下都能分辨（它是这两态唯一的非文字信号）
requirement: OBS-03
result: [pending]

## Summary

total: 7
passed: 0
issues: 0
pending: 7
skipped: 0
blocked: 0

## Gaps
