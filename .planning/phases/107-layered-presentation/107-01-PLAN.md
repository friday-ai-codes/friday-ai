---
phase: 107-layered-presentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/friday/settings.py
  - .env.example
  - server/codegraph/services/repo_router_ranking.py
  - server/tests/codegraph/test_repo_router_ranking.py
  - server/tests/codegraph/test_repo_router_golden.py
autonomous: true
requirements: [ROUTE-01, ROUTE-02, RELY-05]
assumptions:
  - "A2/Pitfall 5：delta / α / K / 总预算 / 澄清超时全部走 settings+env（与既有 REPO_ROUTER_STAGE1_* / REPO_ROUTER_CONF_THETA_* 同轨），**不进** weight_config.constants——`_validate_constants` 白名单派生自 DEFAULT_WEIGHT_CONFIG['constants']，新键会进快照 constants 节污染 106-07 回放比对与 golden fixture 形状，且 weight_set_version 语义是「打分口径版本」，α/K/delta 不改打分口径。若后续确认 delta 需运维热改，另开独立 SettingKeys 键，不动 weight_config。"
  - "A5：`REPO_ROUTER_STAGE1_TIMEOUT_SECONDS`（per-call）**保持 90.0 不下调**，新增 `REPO_ROUTER_STAGE1_TOTAL_BUDGET_SECONDS` 默认 120.0。理由：下调 per-call 会把原本 70–89s 能成功的调用变成用户可见降级，而 A5 要求「先做 O-6 实测再定这个数」——O-6 生产数字本 phase deferred（107-02），故本 phase 只把总预算这一新参数加上（重试仅在快速失败场景生效），per-call 下调留待 107-MEASUREMENTS 回填后另议。"
  - "D-7：α=0.35 取锁定值，离线 harness 结构上不跑 Stage 1（α 恒 0）故无法用 golden 校准；局限由 107-02 写入 107-MEASUREMENTS.md。"
must_haves:
  truths:
    - "分组标注是纯函数：给定候选 repo_id 列表 + 项目关联仓集合，产出 group ∈ {in_project, global} 与 trust ∈ {trusted, needs_confirmation}；`project_repo_ids is None`（调用方无项目上下文）时全部 global 且不抛"
    - "组别绝不进分数：ranking 模块不导出任何返回「分数偏移」的函数，annotate_groups 的返回值里没有任何 float（SC-2 后半句的机制级守护）"
    - "block_order 迟滞：`S_global(1) - S_in_project(1) >= delta` 才把 global 置顶；差值恰在阈值下不翻转、恰在阈值上翻转；同一输入重复调用结果恒等（幂等）"
    - "有项目上下文时 block_order 恒为长度 2（即使某组为空）；无项目上下文时为 ['global']——这是前端区分两种 all-global 场景的唯一依据（UI-SPEC covered 11）"
    - "K=3 rank-swap 预算后置条件可断言且在「LLM 只返回 Stage 0 子集」的常态下有定义：base rank 取**该 rid 在「被 LLM 返回的那个子集」内的 Stage 0 相对位次**（两侧同集合同长度），裁剪后每个候选满足 `|final_rank - stage0_relative_rank| <= K`；违规数被返回供留痕；LLM 编造的 repo_id 被丢弃。**绝不拿 `llm_order` 的下标去减全量 `stage0_order`（8 元窗口）的绝对下标**——LLM 只返回窗口末尾几个时位移恒 > K，修复循环无法收敛、整体回退全量 stage0_order 会把 LLM 重排完全丢弃并让 `rank_budget_violations` 稳定报非零"
    - "凸组合 `S_ranked = (1-α)·S_final + α·S_llm`，`S_llm = 1 - (rank_llm-1)/(N-1)`；α<=0 或 N<=1 恒等返回 S_final（防除零、Stage 1 降级 α=0）"
    - "降级原因分类是 6 值受控闭集（timeout / upstream_error / provider_missing / unparsable / no_node_index / unknown），非降级路径（no_stage0_candidates / use_llm_false）返回空串"
    - "golden cross_group 机制断言：gk-008 / gk-009 的 `S_global(1) - S_in_project(1)` 均 >= 默认 delta，且 decide_block_order 对两者均判 ['global','in_project']；delta 的可用上界由 gk-008 约束（>0.1771 即 gk-008 退回故障）"
    - "delta / α / K / 总预算 / 澄清超时全部外置且非法值 clamp 回合法域（delta,α ∈ [0,1]、K >= 0、超时 > 0），不发版可调"
  artifacts:
    - path: server/codegraph/services/repo_router_ranking.py
      provides: "分组标注 / block_order 迟滞 / rank-swap 裁剪 / 凸组合 / 降级原因分类 / 参数 clamp 六个纯函数（零 Django import，可进 golden 与单测）"
      exports: ["annotate_groups", "decide_block_order", "clamp_llm_permutation", "blend_ranked_scores", "classify_degrade_reason", "clamp_ranking_params", "GROUP_IN_PROJECT", "GROUP_GLOBAL", "TRUST_TRUSTED", "TRUST_NEEDS_CONFIRMATION", "CROSS_GROUP_NOTE", "DEGRADE_REASONS"]
      min_lines: 120
    - path: server/tests/codegraph/test_repo_router_ranking.py
      provides: "ROUTE-01（分组/迟滞/幂等/长度 2 契约）与 RELY-05（K 裁剪后置条件/凸组合/N==1/α=0）单测 + 零 Django import 守护"
    - path: server/friday/settings.py
      contains: "REPO_ROUTER_GROUP_DELTA"
  key_links:
    - from: server/friday/settings.py
      to: server/codegraph/services/repo_router_ranking.py
      via: "settings 只存值；clamp 与语义在纯函数侧（clamp_ranking_params），读取点由 107-03 接线"
      pattern: "REPO_ROUTER_GROUP_DELTA"
    - from: server/tests/codegraph/test_repo_router_golden.py
      to: server/codegraph/services/repo_router_ranking.py
      via: "golden cross_group 样本经 score_case + project_scope 计算两组首位分差，喂 decide_block_order 断言置顶"
      pattern: "decide_block_order"
---

<objective>
本 phase 全部排序/分组/降级判定的**纯函数底座**与**参数外置**一次落地：新建 `server/codegraph/services/repo_router_ranking.py`（零 Django import，与 `repo_router_config.py` / `repo_router_eval.py` 同款纪律），导出分组标注、block_order 迟滞比较、rank-swap 预算裁剪、凸组合、降级原因分类、参数 clamp 六个纯函数；`friday/settings.py` + `.env.example` 补齐本 phase 全部新参数（delta / α / K / Stage 1 总预算与退避 / 澄清超时与扫描频率与批量上限 / 出口动作）。

**打分口径本 plan 一行不改**：`repo_router_scoring.py` 与 `weight_set_version`（`phase106-v2`）零改动；凸组合是打分核心的**外层包装**，组别只进独立字段（CONTEXT D-3 / ROUTING-RANKING §5.1）。

Purpose: 后续 6 个 plan（router 接线 / Stage 1 有界 / 入口改造 / 前端）全部消费这一份纯函数与参数，避免同一判定在多处各写一遍；纯函数形态是 golden 机制断言与幂等断言的前提。
Output: 1 个新纯函数模块 + 9 个新 settings 键 + 2 个测试文件（1 新建 1 扩充）。
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/107-layered-presentation/107-CONTEXT.md
@.planning/phases/107-layered-presentation/107-RESEARCH.md
@.planning/research/ROUTING-RANKING.md
@server/codegraph/services/repo_router_config.py
@server/codegraph/services/repo_router_eval.py
@server/tests/codegraph/test_repo_router_golden.py
</context>

## Artifacts this phase produces（本 plan 新建符号）

| Symbol | Kind | Location |
|--------|------|----------|
| `repo_router_ranking.py` | 新模块（零 Django import 纯函数） | `server/codegraph/services/repo_router_ranking.py` |
| `GROUP_IN_PROJECT = "in_project"` / `GROUP_GLOBAL = "global"` | 常量 | 同上 |
| `TRUST_TRUSTED = "trusted"` / `TRUST_NEEDS_CONFIRMATION = "needs_confirmation"` | 常量 | 同上 |
| `CROSS_GROUP_NOTE = "未关联当前平台，可能涉及跨组协作"` | 常量（后端留痕用；前端不渲染后端自由文本，T-107-06） | 同上 |
| `DEGRADE_REASONS`（6 值 frozenset） | 常量 | 同上 |
| `annotate_groups(repo_ids, *, project_repo_ids)` | 纯函数 | 同上 |
| `decide_block_order(in_project_top, global_top, *, delta, has_project_context)` | 纯函数 | 同上 |
| `clamp_llm_permutation(llm_order, stage0_order, *, k)` | 纯函数 | 同上 |
| `blend_ranked_scores(stage0_scores, llm_order, *, alpha)` | 纯函数 | 同上 |
| `classify_degrade_reason(skipped_reason, *, exc_type_name=None)` | 纯函数 | 同上 |
| `clamp_ranking_params(*, delta, alpha, k)` | 纯函数 | 同上 |
| `REPO_ROUTER_GROUP_DELTA`（默认 0.15） | settings + env 键 | `server/friday/settings.py` / `.env.example` |
| `REPO_ROUTER_STAGE1_ALPHA`（默认 0.35） | settings + env 键 | 同上 |
| `REPO_ROUTER_STAGE1_RANK_BUDGET_K`（默认 3） | settings + env 键 | 同上 |
| `REPO_ROUTER_STAGE1_TOTAL_BUDGET_SECONDS`（默认 120.0） | settings + env 键 | 同上 |
| `REPO_ROUTER_STAGE1_RETRY_BACKOFF_SECONDS`（默认 2.0） | settings + env 键 | 同上 |
| `CLARIFICATION_TIMEOUT_HOURS`（默认 24） | settings + env 键 | 同上 |
| `CLARIFICATION_EXPIRY_CHECK_INTERVAL_SECONDS`（默认 600） | settings + env 键 | 同上 |
| `CLARIFICATION_EXPIRY_SCAN_LIMIT`（默认 200） | settings + env 键 | 同上 |
| `CLARIFICATION_TIMEOUT_EXIT_ACTION`（默认 `"resume_with_assumptions"`，备选 `"fail"`） | settings + env 键 | 同上 |
| `test_repo_router_ranking.py` | 新测试文件 | `server/tests/codegraph/test_repo_router_ranking.py` |

<tasks>

<task type="auto">
  <name>Task 1: 本 phase 全部新参数外置（settings + .env.example）</name>
  <files>server/friday/settings.py, .env.example</files>
  <read_first>
    - server/friday/settings.py:320-352（`REPO_ROUTER_STAGE1_*` 与 `REPO_ROUTER_CONF_THETA_*` 既成范式：`env.float` / `env.int` + 中文注释写清默认值来源与调整方向——新键逐字沿用同款风格与注释密度）
    - .env.example（既有 `REPO_ROUTER_*` 段落位置与注释风格；该文件 documents all vars，新键必须同步）
    - .planning/phases/107-layered-presentation/107-RESEARCH.md §Standard Stack Alternatives + Pitfall 5（为何不进 weight_config.constants）
    - .planning/phases/107-layered-presentation/107-RESEARCH.md §5(a)（per-call 与总预算必须拆成两个参数的硬约束）
  </read_first>
  <action>
    在 `settings.py` 现有 `REPO_ROUTER_STAGE1_*` 段落之后追加**呈现/重排参数**三键，并在 `REPO_ROUTER_CONF_THETA_*` 之后追加 Stage 1 有界化两键；澄清超时四键另起一段（放在可恢复任务段之前或与其它编排配置同区，按文件既有分区就近原则）：

    1. `REPO_ROUTER_GROUP_DELTA = env.float("REPO_ROUTER_GROUP_DELTA", default=0.15)` —— 注释必须写明：block ranking 迟滞阈值，绝不用 0（0.001 级波动会反复翻转置顶，破坏幂等）；**可用上界受 golden gk-008 实测 0.1771 约束**，超过即 gk-008 退回「正确仓被压在下面」。
    2. `REPO_ROUTER_STAGE1_ALPHA = env.float("REPO_ROUTER_STAGE1_ALPHA", default=0.35)` —— 注释写明：凸组合 `S_ranked=(1-α)·S_final+α·S_llm` 的 α；D-7 锁定初值，**离线 harness 结构上不跑 Stage 1 故 α 无法用 golden 校准**（局限记 107-MEASUREMENTS.md）；Stage 1 降级时按 0 处理。
    3. `REPO_ROUTER_STAGE1_RANK_BUDGET_K = env.int("REPO_ROUTER_STAGE1_RANK_BUDGET_K", default=3)` —— 注释写明 `|rank_llm - rank_stage0| <= K` 的语义与「裁剪在消费侧做，不改 prompt」（改 prompt 需 bump `PROMPT_TEMPLATE_VERSION`，本 phase 刻意不改以保留历史缓存命中）。
    4. `REPO_ROUTER_STAGE1_TOTAL_BUDGET_SECONDS = env.float("REPO_ROUTER_STAGE1_TOTAL_BUDGET_SECONDS", default=120.0)` —— 注释写明：首调 + 1 次重试**共享**的总延迟上界；`REPO_ROUTER_STAGE1_TIMEOUT_SECONDS` 保持 per-call 语义且**本 phase 不下调**（A5：下调需先有 O-6 生产实测，见 107-MEASUREMENTS.md）；120 = 90 per-call + 退避余量，重试只在快速失败场景生效 → 相对今日行为零回归。
    5. `REPO_ROUTER_STAGE1_RETRY_BACKOFF_SECONDS = env.float("REPO_ROUTER_STAGE1_RETRY_BACKOFF_SECONDS", default=2.0)` —— 指数退避基数；实际睡眠不得超过剩余预算。
    6. `CLARIFICATION_TIMEOUT_HOURS = env.int("CLARIFICATION_TIMEOUT_HOURS", default=24)` —— 注释写明：澄清 pending 轮的超时（起算时间为 **pending 轮 `Clarification.created_at`**，不用 `session.updated_at`，见 Pitfall 7）；**同一个键同时驱动工作流侧 `WorkflowEventSubscription.timeout_at`**（D-4 单一超时口径，消除「工作流已 TIMEOUT 而会话仍 waiting_clarification」的矛盾态窗口）。
    7. `CLARIFICATION_EXPIRY_CHECK_INTERVAL_SECONDS = env.int("CLARIFICATION_EXPIRY_CHECK_INTERVAL_SECONDS", default=600)` —— 注释写明 10min 频率的理由：超时默认 24h，分钟级精度足够；避免与 `sample_gauges`(45s) / `evaluate_system_alerts`(60s) 争 SQLite 写锁。
    8. `CLARIFICATION_EXPIRY_SCAN_LIMIT = env.int("CLARIFICATION_EXPIRY_SCAN_LIMIT", default=200)` —— 单次扫描处理上限（T-107-08 防长事务/内存膨胀）。
    9. `CLARIFICATION_TIMEOUT_EXIT_ACTION = env.str("CLARIFICATION_TIMEOUT_EXIT_ACTION", default="resume_with_assumptions")` —— 受控二值（`resume_with_assumptions` | `fail`）；注释写明默认为「带未澄清假设继续推进」，备选「如实失败并说明原因」（CONTEXT RELY-02 两条路径），非法值按默认处理（读取侧兜底）。

    `.env.example` 同步追加这 9 个键（注释可精简为一行，但必须写默认值）；**不得**把任何键塞进 `SettingKeys.REPO_ROUTER_WEIGHT_CONFIG` 的 `constants` 段（Pitfall 5）。
  </action>
  <verify>
    <automated>cd server && uv run python -c "
import os, django
os.environ.setdefault('DJANGO_SETTINGS_MODULE','friday.settings'); django.setup()
from django.conf import settings as s
assert abs(s.REPO_ROUTER_GROUP_DELTA - 0.15) < 1e-9
assert abs(s.REPO_ROUTER_STAGE1_ALPHA - 0.35) < 1e-9
assert s.REPO_ROUTER_STAGE1_RANK_BUDGET_K == 3
assert abs(s.REPO_ROUTER_STAGE1_TOTAL_BUDGET_SECONDS - 120.0) < 1e-9
assert abs(s.REPO_ROUTER_STAGE1_TIMEOUT_SECONDS - 90.0) < 1e-9, 'per-call must stay 90 (A5)'
assert s.CLARIFICATION_TIMEOUT_HOURS == 24
assert s.CLARIFICATION_EXPIRY_CHECK_INTERVAL_SECONDS == 600
assert s.CLARIFICATION_EXPIRY_SCAN_LIMIT == 200
assert s.CLARIFICATION_TIMEOUT_EXIT_ACTION == 'resume_with_assumptions'
print('settings ok')
"</automated>
  </verify>
  <acceptance_criteria>
    - 上述 automated 断言全过（9 个新键存在且默认值精确；`REPO_ROUTER_STAGE1_TIMEOUT_SECONDS` 仍为 90.0——per-call 未被下调）
    - `.env.example` 含全部 9 个键：`test "$(rg -c '^(REPO_ROUTER_GROUP_DELTA|REPO_ROUTER_STAGE1_ALPHA|REPO_ROUTER_STAGE1_RANK_BUDGET_K|REPO_ROUTER_STAGE1_TOTAL_BUDGET_SECONDS|REPO_ROUTER_STAGE1_RETRY_BACKOFF_SECONDS|CLARIFICATION_TIMEOUT_HOURS|CLARIFICATION_EXPIRY_CHECK_INTERVAL_SECONDS|CLARIFICATION_EXPIRY_SCAN_LIMIT|CLARIFICATION_TIMEOUT_EXIT_ACTION)=' .env.example)" = "9"`（行首锚定，注释行不计入）
    - weight_config 未被污染：`test "$(rg -c '^[[:space:]]*"(alpha|delta|k|rank_budget_k)"' server/codegraph/services/repo_router_scoring.py || echo 0)" = "0"`
    - golden / 回放不受影响：`cd server && uv run pytest tests/codegraph/test_repo_router_golden.py tests/codegraph/test_repo_router_replay.py -q` 全绿
  </acceptance_criteria>
  <done>本 phase 全部阈值/参数不发版可调，且与打分口径版本语义完全隔离。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: repo_router_ranking.py 六个纯函数 + 单测</name>
  <files>server/codegraph/services/repo_router_ranking.py, server/tests/codegraph/test_repo_router_ranking.py</files>
  <read_first>
    - .planning/phases/107-layered-presentation/107-RESEARCH.md §Code Examples（annotate_groups / clamp_llm_permutation / blend_ranked_scores / classify_degrade_reason 四段参考实现与其注意事项）
    - .planning/phases/107-layered-presentation/107-RESEARCH.md §6（6 个降级分支 → 粗粒度枚举映射表 + 异常类型映射建议）
    - .planning/research/ROUTING-RANKING.md §1.3(b)(c)（rank-swap budget 与凸组合的原始定义）与 §5.1/§5.2（组别绝不进分数 + block ranking 迟滞）与 §6.2 第 4 条（稳定 tie-breaking：先 round(score,6) 再比，第二键用 repository_id）
    - server/codegraph/services/repo_router_v2.py:1206-1256（**base rank 语义的决定性证据**：最终候选**只由 `parsed` 构造**——未被 LLM 返回的 Stage 0 仓被整体丢弃，故 `llm_order` 常态是 `stage0_order` 的真子集）与 server/friday/settings.py:331（`REPO_ROUTER_STAGE1_MAX_CANDIDATES` 默认 8 = Stage 0 窗口大小）
    - server/codegraph/services/repo_router_config.py（同款「零 Django import 纯函数 + 参数注入」纪律与 docstring 风格）
  </read_first>
  <behavior>
    - annotate_groups：`project_repo_ids=None` → 全部 `(global, needs_confirmation)`；`project_repo_ids=frozenset({"a"})` + `repo_ids=["a","b"]` → a 为 `(in_project, trusted)`、b 为 `(global, needs_confirmation)`；返回值中不含任何 float
    - decide_block_order：`has_project_context=False` → `["global"]`（长度 1）；有上下文时恒长度 2；`global_top - in_project_top == delta - 1e-9` → `["in_project","global"]`；`== delta` → `["global","in_project"]`；`in_project_top=None`（该组为空）→ `["global","in_project"]`；`global_top=None` → `["in_project","global"]`；**`has_project_context=True` 且两组 top 均为 `None`（两组皆空）→ 仍返回长度 2**（顺序取确定的默认，实现自选但必须恒定）；同一输入连调 3 次结果恒等
    - clamp_llm_permutation：**base rank 取「被 LLM 返回子集」内的 Stage 0 相对位次**，即 `base_order = [r for r in stage0_order if r in returned_set]`、`base_rank(rid) = base_order.index(rid)`。后置条件——返回 order 中每个 rid 满足 `|order.index(rid) - base_rank(rid)| <= k`；`llm_order` 含 stage0 之外的 id 被丢弃（不进 `returned_set`）；`llm_order == stage0_order` → 原样返回且 violations == 0
    - clamp_llm_permutation（**子集常态，BLOCKER 修复的核心用例**）：`stage0_order = [r1..r8]`、`llm_order = [r8, r7, r6]`（LLM 只返回窗口末三位）、k=3 → 返回长度 3、只含这三个 id、后置条件成立（`base_order = [r6,r7,r8]` → base 0/1/2 与 order 下标同域）、**不发生「整体回退全量 stage0_order」**（返回长度必须 == 3 而非 8）、`violations` 反映的是子集内相对位移（`r8` 从相对第 2 位提到第 0 位 = 位移 2 <= k → violations == 0）
    - clamp_llm_permutation（越界仍被裁剪）：`stage0_order = [r1..r8]`、`llm_order = [r8, r1, r2, r3, r4, r5, r6, r7]`（全集，r8 从第 7 位提到第 0 位）、k=3 → violations >= 1 且 r8 最终位次不早于 `7-3`
    - blend_ranked_scores：`alpha=0` → 逐键等于 `stage0_scores`；`len(llm_order)==1` → 同上（不除零）；`alpha=0.35, llm_order=[a,b], stage0={a:0.5,b:0.9}` → `a = 0.65*0.5+0.35*1.0`、`b = 0.65*0.9+0.35*0.0`；`llm_order` 中不在 `stage0_scores` 的 id 被跳过
    - classify_degrade_reason：`provider_missing`/`no_model_configured` → `provider_missing`；`unparsable_llm_output`/`no_valid_candidates_in_llm_output` → `unparsable`；`v1_fallback` → `no_node_index`；`no_stage0_candidates`/`use_llm_false` → `""`；`exc_type_name="TimeoutError"` → `timeout`；`"APIConnectionError"`/`"APIStatusError"`/`"ConnectError"`/`"BadRequestError"` → `upstream_error`；其它 → `unknown`；返回值恒 ∈ `DEGRADE_REASONS | {""}`
    - clamp_ranking_params：`delta=-1, alpha=2, k=-5` → `(0.0, 1.0, 0)`；合法值原样返回；`delta=float("nan")` → 回退默认（0.15/0.35/3 由调用方传入 fallback 或函数内定义常量默认，实现自选但必须不抛）
  </behavior>
  <action>
    新建 `server/codegraph/services/repo_router_ranking.py`（模块 docstring 写明：本模块是 Phase 106 定版打分口径的**外层包装**，绝不改 `score`/`breakdown`；零 Django import 是硬纪律，便于进 golden 与单测）。逐函数要点：

    1. `annotate_groups(repo_ids: list[str], *, project_repo_ids: frozenset[str] | None) -> dict[str, tuple[str, str]]`：`None` = 调用方无项目上下文 → 全 `global`。**返回值只含组别与 trust 字符串，禁止返回任何分数偏移**（SC-2 的机制守护点）。
    2. `decide_block_order(in_project_top: float | None, global_top: float | None, *, delta: float, has_project_context: bool) -> list[str]`：无上下文 → `[GROUP_GLOBAL]`；有上下文时**恒返回长度 2**（UI-SPEC covered 11 的后端契约要求：即使某组为空也必须给两元素，前端据 `len==2` 判定是否启用分组呈现）；某组为空按「另一组置顶」处理；两组都有值时用 `>= delta` 比较（迟滞，绝不用 `> 0`）。
    3. `clamp_llm_permutation(llm_order: list[str], stage0_order: list[str], *, k: int) -> tuple[list[str], int]`：
       - 先按 `stage0_order` 白名单过滤 + 去重（防御，调用方也会过滤）得到 `rids`（保持 `llm_order` 的顺序）；
       - 🔴 **base rank 必须用「被 LLM 返回子集」内的 Stage 0 相对位次**：`base_order = [r for r in stage0_order if r in set(rids)]`、`base[rid] = base_order.index(rid)`。函数注释必须写明这条的因果：`repo_router_v2.py:1206-1256` 显示最终候选**只由 `parsed` 构造**（未被 LLM 返回的 Stage 0 仓被整体丢弃），而 `REPO_ROUTER_STAGE1_MAX_CANDIDATES` 默认 8 —— 若拿 `order.index`（域 0..len(rids)-1）去减全量 `stage0_order.index`（域 0..7），LLM 只返回窗口末尾几位时位移恒 > k，修复循环无法收敛 → 整体回退全量 `stage0_order` → **LLM 重排在最常见情形下被完全丢弃，且 `rank_budget_violations` 稳定报非零，看起来像「裁剪在工作」**。两侧必须同集合、同长度、同下标域。
       - `desired[rid] = min(max(llm_rank, base[rid]-k), base[rid]+k)`（`llm_rank` = 该 rid 在 `rids` 中的下标）；`order = sorted(rids, key=lambda rid: (desired[rid], base[rid]))`；
       - 随后**修复循环**保证后置条件：逐轮检查 `|order.index(rid) - base[rid]| > k` 的元素，把最早的越界元素移到其 `base[rid]` 位置（其余顺移），循环上界 `len(rids)` 轮；仍不满足则整体回退 `base_order`（**注意是 `base_order` 而不是全量 `stage0_order`**——回退结果的元素集合必须与输入子集一致，否则会凭空引入没有对应候选的 repo_id）。
       - `violations` = **LLM 请求位移越界的元素个数**（裁剪前按子集内相对位次统计，供留痕；不是修复轮数）。
       - 函数注释写明：ROUTING-RANKING §1.3b 要的是「损害有硬上界且可测」，算法细节可换，**后置条件、base 的子集相对语义、violations 留痕三者不可换**。
    4. `blend_ranked_scores(stage0_scores: dict[str, float], llm_order: list[str], *, alpha: float) -> dict[str, float]`：`n = len(llm_order)`；`n <= 1 or alpha <= 0.0` → `dict(stage0_scores)`；否则 `s_llm = 1.0 - idx/(n-1)`，`out[rid] = (1-alpha)*stage0_scores[rid] + alpha*s_llm`；`rid not in stage0_scores` 跳过。**函数不返回 `score`，返回的是旁路 `score_ranked` 的取值**（D-3：调用方必须写进新字段，绝不覆盖 `score`）。
    5. `classify_degrade_reason(skipped_reason: str, *, exc_type_name: str | None = None) -> str`：按 §6 映射表；`no_stage0_candidates` / `use_llm_false` 返回 `""`（数据缺失与主动纯检索不是「上游不可用」，不该提示用户「本次未经 LLM 推理」的原因行）；异常名按子串匹配（`Timeout` → timeout；`Connect`/`APIStatus`/`APIError`/`HTTPStatus`/`BadRequest` → upstream_error）；兜底 `unknown`。**函数只接受异常类型名字符串，不接受异常实例/消息**——保证脱敏边界（原文脱敏在调用侧做，T-107-02）。
    6. `clamp_ranking_params(*, delta: float, alpha: float, k: int) -> tuple[float, float, int]`：clamp 到 `delta,alpha ∈ [0,1]`、`k >= 0`；非有限值（NaN/inf）回退模块内默认常量；**绝不抛**（T-107-05 fail-safe，照 `repo_router_config._CONSTANT_RULES` 范式）。

    新建 `server/tests/codegraph/test_repo_router_ranking.py`：按 `<behavior>` 逐条写用例；`clamp_llm_permutation` 追加一条**子集 × 排列穷举属性测试**——外层 `itertools.combinations(stage0_order, m)` 遍历 `m ∈ 1..6`（`stage0_order` 取 6 元），内层对每个子集跑 `itertools.permutations(subset)`，逐例断言 (a) 后置条件 `|order.index(rid) - base_order.index(rid)| <= 3` 恒成立、(b) 返回元素集合 == 输入子集（**不因回退而膨胀回全量**）、(c) 函数不抛。**只跑等长全排列（`permutations(range(6))`，720 例）结构上抓不到 BLOCKER 描述的子集缺陷**，故等长排列只是这个穷举的 `m == 6` 那一层。补一条零 Django import 静态守护测试（读文件内容按行首正则断言）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/codegraph/test_repo_router_ranking.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd server && uv run pytest tests/codegraph/test_repo_router_ranking.py -x -q` 全绿，且用例覆盖 `<behavior>` 全部 6 组行为
    - 零 Django import：`test "$(rg -c '^[[:space:]]*(import|from)[[:space:]]+django' server/codegraph/services/repo_router_ranking.py || echo 0)" = "0"`（行首锚定，docstring 提及不计入）
    - 组别不进分数的机制守护：`annotate_groups` 返回值中不存在 float（测试内 `assert all(isinstance(v, str) for pair in result.values() for v in pair)`）
    - 后置条件穷举通过：6 元 `stage0_order` 的**全部非空子集 × 该子集全排列**（`Σ C(6,m)·m!`，含 `m==6` 的 720 例）× k=3 下 `|final_rank - base_order.index(rid)| <= 3` 零违例
    - 子集常态不被整体回退：穷举内断言返回元素集合恒 == 输入子集（长度不得膨胀回 6）；另有一条独立命名用例覆盖「`stage0_order` 8 元、`llm_order` 只含末三位」并断言返回长度 == 3
    - 打分核心零改动：`cd server && git diff --name-only | rg -q 'repo_router_scoring\.py' && exit 1 || true`（本 plan 不应改该文件）
    - `cd server && uv run ruff check codegraph/services/repo_router_ranking.py tests/codegraph/test_repo_router_ranking.py && uv run ruff format --check codegraph/services/repo_router_ranking.py tests/codegraph/test_repo_router_ranking.py`
  </acceptance_criteria>
  <done>分组、置顶、裁剪、凸组合、降级分类五类判定各只有一份可测实现，后续 6 个 plan 直接消费。</done>
</task>

<task type="auto">
  <name>Task 3: golden cross_group 机制断言（gk-008 / gk-009 的 delta 与置顶）</name>
  <files>server/tests/codegraph/test_repo_router_golden.py</files>
  <read_first>
    - server/tests/codegraph/test_repo_router_golden.py（既有 9 个用例：门禁三规则 / 版本字面绑定 / 耗时预算 / gk-001 机制断言三件套 / 确定性 / diff 自测——新用例沿用同款 fixture 与 `_rank_of` 辅助风格）
    - server/tests/codegraph/fixtures/repo_router_golden/golden_main.json（gk-008-cross-group-auth / gk-009-cross-group-payment 两条：`cross_group` + `project_scope` + `expected_repos`）
    - .planning/phases/107-layered-presentation/107-RESEARCH.md §10（本次离线实测：gk-008 delta=0.1771 / gk-009 delta=0.2614；机制断言写法与「不要写 == 0.1771」的反例）
    - server/codegraph/services/repo_router_eval.py（`score_case` 入口：单 case 六信号打分，零网络）
  </read_first>
  <action>
    在 `test_repo_router_golden.py` 追加两个用例（**离线零网络**，走 `score_case`，不引入任何 Django import 到 harness 侧）：

    1. `test_cross_group_cases_trigger_block_order_promotion`：对 `gk-008-cross-group-auth` 与 `gk-009-cross-group-payment` 各自——用 `case["project_scope"]`（该 case 的「本项目关联仓」离线表达）把 `score_case` 输出的候选切成两组，取各组首位分数，断言 (a) `S_global(1) - S_in_project(1) >= REPO_ROUTER_GROUP_DELTA 的默认值 0.15`（**用常量比较，不写 `== 0.1771`**——权重微调即假红，ROUTING-RANKING §7.4）；(b) `decide_block_order(in_project_top, global_top, delta=0.15, has_project_context=True) == ["global", "in_project"]`；(c) 正确仓（`expected_repos[0]`）落在被置顶的 global 组内。
    2. `test_cross_group_delta_upper_bound_is_binding`：断言 gk-008 的实测差值 `< 0.1772`（**上界语义锁定**）并在注释里写明因果：`delta` 一旦 > gk-008 的差值，gk-008 就会退回「本项目组置顶而正确仓被压在下面」——即重演本里程碑要修的故障。断言写成 `assert delta_gk008 < 0.1772` + `assert 0.15 <= delta_gk008`（区间断言，抗权重微调但锁住"默认 delta 可用且余量很薄"这一事实）。

    两个用例共用一个 module 级 helper（如 `_group_tops(case)` 返回 `(in_project_top, global_top, ranked)`），helper 缺失 `project_scope` 时直接 `pytest.fail`（fixture 契约退化必须红）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/codegraph/test_repo_router_golden.py -x -q -k "cross_group or golden_gate"</automated>
  </verify>
  <acceptance_criteria>
    - `cd server && uv run pytest tests/codegraph/test_repo_router_golden.py -q` 全绿且总耗时仍 < 10s（既有耗时断言覆盖）
    - 新用例引用 `decide_block_order`：`test "$(rg -c 'decide_block_order' server/tests/codegraph/test_repo_router_golden.py)" != "0"`
    - 无脆弱结果断言：`test "$(rg -c 'assert.*0\.1771' server/tests/codegraph/test_repo_router_golden.py || echo 0)" = "0"`（**只禁 assert 行上的精确等值**；上界用 `< 0.1772`。注释里出现 `0.1771` 是 action 明确要求的因果说明，不受此断言约束——原先的全文件禁用与 action 自相矛盾）
    - baseline 未被重建：`cd server && git diff --name-only | rg -q 'golden_baseline\.json' && exit 1 || true`（本 plan 不改打分口径 → baseline 不得变动）
  </acceptance_criteria>
  <done>「跨组正确仓能被置顶」成为 CI 门禁里的机制断言，delta 的可用上界被测试锁住。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env / SystemSetting → 排序判定 | 运维可调参数进入排序与超时逻辑，极端值可使排序退化或超时永不触发 |
| 上游异常对象 → 降级原因枚举 | 异常文本可能含凭证/上游 body 回显，不得穿过分类函数流向用户 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-107-05 | Tampering | `REPO_ROUTER_GROUP_DELTA` / `_ALPHA` / `_RANK_BUDGET_K` | mitigate | `clamp_ranking_params` clamp 到 `[0,1]` / `k>=0`，非有限值回退默认且绝不抛（照 `repo_router_config._CONSTANT_RULES` fail-safe 范式）；单测覆盖负值/超界/NaN |
| T-107-02 | Information Disclosure | `classify_degrade_reason` | mitigate | 函数签名只接受 `skipped_reason: str` 与 `exc_type_name: str \| None`——**结构上无法接收异常消息**；返回值恒 ∈ 6 值闭集 ∪ `{""}`，基数受控可做指标维度 |
| T-107-07 | Denial of Service | Stage 1 总预算参数 | mitigate | 新增总预算键使「首调 + 1 次重试」共享硬上界（实现在 107-05）；本 plan 保证参数存在且可 clamp |
| T-107-SC | Tampering | npm/pip/cargo installs | accept | 本 phase 零新增外部依赖（107-RESEARCH §Package Legitimacy Audit：不适用，无包可审）；`server/uv.lock` / `web/pnpm-lock.yaml` 不变 |
</threat_model>

<verification>
- `cd server && uv run pytest tests/codegraph -q` 全绿（含 golden 门禁与回放，确认参数外置未污染快照/baseline 形状）
- `cd server && uv run ruff check codegraph/services/repo_router_ranking.py tests/codegraph/test_repo_router_ranking.py tests/codegraph/test_repo_router_golden.py friday/settings.py`
- `test "$(rg -c '^[[:space:]]*(import|from)[[:space:]]+django' server/codegraph/services/repo_router_ranking.py || echo 0)" = "0"`

## 归零断言纪律（本 phase 统一口径，写给执行者）

1. 任何 `rg -c ... = "0"` 归零断言，若其模式串**同时出现在本 task 要求写的注释里**（或文件里既有注释中）→ **必须**先滤注释行：`rg -v '^[[:space:]]*#' <file> | rg -c 'PATTERN' || echo 0`。`.vue` 用 `rg -v '^[[:space:]]*(//|<!--)'`。`rg -c` 计的是行、不区分代码与注释，不滤即自引入必红。本 plan 的两条归零断言用**行首锚定**（`^[[:space:]]*"(alpha|delta|k|...)"` / `^[[:space:]]*(import|from)`）达到同一效果，故不需额外过滤；新增断言若无法行首锚定，一律按上面的过滤口径写。
2. 任何 `-A N` 窗口归零断言，若窗口**可能越过被断言的代码构造**（函数/分支结束后的相邻代码或 docstring）→ 改为对**构造本身**的精确断言，或在窗口输出上滤 `^[0-9]+[-:][[:space:]]*#`（`-A` 输出的行前缀是 `NNN:` / `NNN-`）。
</verification>

<success_criteria>
- 9 个新参数外置且默认值精确、非法值 clamp；per-call Stage 1 超时未被下调（A5 纪律）
- 六个纯函数全部有单测；`clamp_llm_permutation` 后置条件经「子集 × 排列」穷举（base rank 为子集内相对位次，覆盖「LLM 只返回 Stage 0 子集」的常态）
- gk-008 / gk-009 的「跨组正确仓被置顶」进 CI 门禁；delta 上界语义被锁定
- `repo_router_scoring.py` 与 `golden_baseline.json` 零改动（打分口径冻结）
</success_criteria>

<output>
完成后创建 `.planning/phases/107-layered-presentation/107-01-SUMMARY.md`。
</output>
