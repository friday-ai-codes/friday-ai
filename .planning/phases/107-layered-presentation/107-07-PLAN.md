---
phase: 107-layered-presentation
plan: 07
type: execute
wave: 3
depends_on: [107-03]
files_modified:
  - server/codegraph/services/repo_group_scope.py
  - server/tests/codegraph/test_repo_group_scope.py
  - server/services/process_runtime/repo_router_adapter.py
  - server/tests/services/test_repo_router_adapter.py
  - server/agents/tools/repository_relevance.py
  - server/agents/tools/schemas/repository_relevance.py
  - server/tests/agents/test_repository_relevance_tool.py
autonomous: true
requirements: [ROUTE-01, ROUTE-02]
assumptions:
  - "**T-107-01 前提确认（必须显式记录，不得默认成立）**：放开候选范围后 Stage 0 走全库召回，结果里会出现用户所在项目外的仓名。团队既有判断是「仓名不敏感」——依据：`mcp_tools/views.py:434`（`RouteRepositoriesView`）与 `repositories/route_views.py:39`（`POST /api/repositories/route/`）两个**已上线**入口本来就是全库 `route(query, top_k=...)`，无任何 per-user 可见性过滤。本 plan 沿用该既有判断，**不新增可见性面**；`_stage0_node_search` 侧不存在按用户过滤的机制，故「放开 Space 硬过滤」不会绕过任何现存权限检查。该前提须在 SUMMARY 与 VERIFICATION 中如实记录，若团队判断改变则需在 Phase 109/110 补可见性过滤层。"
  - "A1/D-2：「本项目关联仓」取**宽口径并集** = 项目所属 `Space.repositories` ∪ 该项目下 `RepoAssociation(status=verified)`。依据 CONTEXT D-2：ROUTE-01/02 的原文是「哪些是本平台内的」「未关联当前平台」——分界线在平台/组级别而非单个工作项的已验证关联；窄口径（只取 verified）会让 in_project 组几乎恒空、分组失去信息量。chat 入口只有 `space_id`（无 Project 实例时）→ 退化为 `Space.repositories`，不报错。"
  - "OQ-1 采纳「放开硬过滤」：编排与 chat 两个入口改为 `grouping_repository_ids=<项目关联仓>` + `repository_ids=None`（全库召回）。**这是本 phase 唯一有实际回归风险的改动**（候选构成变化），故独立成 task 并配回归守护；其余 6 个 `RepoRouterV2.route()` 调用方**逐字不改**。"
  - "候选数变化：分组启用后 router 按组各取 `top_k`（107-03 落），编排入口的 `candidates` 长度可能从 <=3 变为 <=6。下游 clarify policy 判定「候选无任一 confidence ∈ {high, medium}」在候选变多时**更容易**满足「有 confident 候选」→ 澄清触发变少，方向与本里程碑目标一致；仍需回归测试确认既有 adapter 测试断言不破。"
must_haves:
  truths:
    - "「本项目关联仓」= `Space.repositories` ∪ 该项目 `RepoAssociation(status=verified)` 的宽口径并集（D-2），有一个可单测的解析入口；无 Space / 无 Project 时优雅退化且不抛"
    - "编排入口（`repo_router_adapter`）与 chat 入口（`repository_relevance`）把项目关联仓从「硬过滤」改为「分组依据」：`grouping_repository_ids=<项目关联仓>` + `repository_ids=None`（D-1）"
    - "其余 6 个 `RepoRouterV2.route()` 调用方零改动：`space_tools` / `repo_association_service` / `knowledge.sources.artifact` / `skill_steps` / MCP / REST 六处的调用行仍不传 `grouping_repository_ids`，行为逐字不变"
    - "global 组不再恒空：构造「正确仓在项目关联范围之外」的场景，断言结果里存在 `group == 'global'` 的候选（若恒空即命中 Pitfall 2，ROUTE-01/02 上线即无效果）"
    - "chat 链候选携带新字段：`RepositoryRelevanceCandidate` 新增 `group` / `trust` / `score_ranked`（全部带默认值），逐跳透传到 `RepositoryRoutingTrace.candidates` JSON"
    - "既有回归基线不破：`tests/services/test_repo_router_adapter.py`、`tests/agents/test_repository_relevance_tool.py`、`tests/initiatives/test_repo_association_service.py` 全绿"
    - "Output schema 加字段不触发 Input schema snapshot 守护（fixture 只冻结 `RepositoryRelevanceInput`）"
  artifacts:
    - path: server/codegraph/services/repo_group_scope.py
      provides: "项目关联仓宽口径并集解析（by work_item_id / by space_id 两个入口，async ORM 经 sync_to_async）"
      exports: ["aresolve_grouping_repo_ids"]
    - path: server/services/process_runtime/repo_router_adapter.py
      provides: "编排入口：grouping_repository_ids 承接项目关联仓、repository_ids 放开为 None"
      contains: "grouping_repository_ids"
    - path: server/agents/tools/repository_relevance.py
      provides: "chat 入口同款改造 + 候选新字段映射"
      contains: "grouping_repository_ids"
    - path: server/agents/tools/schemas/repository_relevance.py
      contains: "score_ranked"
  key_links:
    - from: server/services/process_runtime/repo_router_adapter.py
      to: server/codegraph/services/repo_group_scope.py
      via: "aresolve_grouping_repo_ids(work_item_id=...) 取宽口径并集作为分组依据"
      pattern: "aresolve_grouping_repo_ids"
    - from: server/agents/tools/repository_relevance.py
      to: server/codegraph/services/repo_router_v2.py
      via: "RepoRouterV2.route(query, top_k=..., repository_ids=None, grouping_repository_ids=<项目关联仓>)"
      pattern: "grouping_repository_ids"
---

<objective>
兑现 D-1：把项目/空间关联仓从 `RepoRouterV2.route(repository_ids=...)` 的**硬过滤**改为独立的**分组依据**，只改编排（`repo_router_adapter`）与 chat（`repository_relevance`）两个入口——不改这一处，`global` 组恒空、ROUTE-01/02 上线即无效果且看不出来（只是 UI 少一个分区，Pitfall 2）。同时落 D-2 的「本项目关联仓」宽口径并集解析（`Space.repositories` ∪ 该项目 `RepoAssociation(status=verified)`），以及 chat 链候选新字段的 pydantic 透传。

**这是本 phase 唯一有实际回归风险的改动**（候选构成从「空间内仓」变为「全库」）：候选范围语义改动独立成 Task 2 承载，配三个既有测试文件作回归基线 + 一条「global 组非空」的正向断言。

Purpose: ROUTE-01/02 从「字段就位」变成「用户真能看到两组」；chat 链把分组事实送达前端。
Output: 1 个新解析模块 + 2 个入口改造 + 1 个 pydantic schema 扩展 + 3 个测试文件补用例。
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/107-layered-presentation/107-CONTEXT.md
@.planning/phases/107-layered-presentation/107-RESEARCH.md
@server/codegraph/services/repo_router_v2.py
@server/services/process_runtime/repo_router_adapter.py
@server/agents/tools/repository_relevance.py
@server/agents/tools/schemas/repository_relevance.py
</context>

## Artifacts this phase produces（本 plan 新建符号）

| Symbol | Kind | Location |
|--------|------|----------|
| `repo_group_scope.py` | 新模块 | `server/codegraph/services/repo_group_scope.py` |
| `aresolve_grouping_repo_ids(*, work_item_id=None, space_id=None) -> frozenset[str] \| None` | async 函数（D-2 宽口径并集；`None` = 无项目上下文） | 同上 |
| `repo_group_scope_resolved`（structlog 事件名，`category="sampling"`, `component="repo_router_v2"`） | 事件名（kv: `space_repo_count` / `verified_repo_count` / `union_count`） | 同上 |
| `RepositoryRelevanceCandidate.group: str = ""` | pydantic 字段 | `server/agents/tools/schemas/repository_relevance.py` |
| `RepositoryRelevanceCandidate.trust: str = ""` | pydantic 字段 | 同上 |
| `RepositoryRelevanceCandidate.score_ranked: float \| None = None` | pydantic 字段 | 同上 |
| `RepoRouterV2Adapter._resolve_grouping_repository_ids` | 内部方法 | `server/services/process_runtime/repo_router_adapter.py` |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: repo_group_scope 宽口径并集解析（D-2）</name>
  <files>server/codegraph/services/repo_group_scope.py, server/tests/codegraph/test_repo_group_scope.py</files>
  <read_first>
    - server/services/process_runtime/repo_router_adapter.py:76-89（`_project_repository_ids`：`WorkItem.objects.select_related("space")` → `wi.space.repositories.values_list("id", flat=True)`，`sync_to_async` 的既成范式——新模块沿用同款）
    - server/initiatives/services/repo_association_service.py（`get_verified_associations(project=..., work_item=...)` 的契约与返回结构 `[{repository_id, repo_name, verdict, score, ...}]`；只取 `status=verified`）
    - server/workflows/nodes/integrations/board_split_review.py（`_aresolve_project(space)` / `_resolve_space(context)` 两个 helper 的解析规则——若从 `codegraph` 直接 import `workflows` 会造成分层倒置，则按同一规则在本模块内实现最小等价查询并在注释里写明为何不复用）
    - server/agents/tools/repository_relevance.py:194-206（chat 侧现行空间仓查询：`Repository.objects.filter(spaces=project, is_deleted=False, index_status="indexed")`——注意它同时做了 `index_status` 过滤，分组依据是否要跟随该过滤需在实现中明确并写注释）
    - .planning/phases/107-layered-presentation/107-RESEARCH.md §1（三条归属数据源对照表 + 8 个调用方项目上下文矩阵）
    - .planning/phases/107-layered-presentation/107-CONTEXT.md D-2（宽口径并集的裁决原文与理由）
  </read_first>
  <behavior>
    - `work_item_id` 指向的 WorkItem 有 space，space 有 2 个仓，该 space 下 Project 有 1 条 `verified` 关联指向第 3 个仓 → 返回三者并集（3 个 id）
    - 同一个仓同时在 `Space.repositories` 与 `verified` 关联中 → 并集去重（返回 1 个 id）
    - space 存在但无 Project → 返回 `Space.repositories`（不抛）
    - `work_item_id` 不存在 / WorkItem 无 space → 返回 `None`（= 无项目上下文，调用方据此跳过分组）
    - `space_id` 入口：给定 space → 返回并集（Project 存在时含 verified）；space 不存在 → `None`
    - `RepoAssociation` 查询抛异常（monkeypatch）→ 降级为只返回 `Space.repositories`，不抛（并集的可选半边失败不该毁掉分组）
    - 返回值为 `frozenset[str]`（id 一律 `str()` 归一，与 `RepoRouteCandidateV2.repo_id` 的类型一致）
    - 两个入口都不接受同时传 `work_item_id` 与 `space_id`（传两个时以 `work_item_id` 优先并记一条 sampling 日志）
  </behavior>
  <action>
    新建 `server/codegraph/services/repo_group_scope.py`（模块 docstring 写明 D-2 裁决：宽口径并集的语义与「窄口径会让 in_project 组几乎恒空」的理由）：

    1. `async def aresolve_grouping_repo_ids(*, work_item_id=None, space_id=None) -> frozenset[str] | None`：
       - 先解析 Space（`work_item_id` → `WorkItem.objects.select_related("space")`；否则 `space_id` 直取）；解析不到 → `None`；
       - `space_ids = {str(r) for r in space.repositories.values_list("id", flat=True)}`；
       - 尝试解析该 Space 下的 Project → 有则取 `RepoAssociationService().get_verified_associations(project=project)` 的 `repository_id` 集合并并入；这一半**整体 `try/except`** 降级（失败只用 space 半边，记 warning）；
       - 返回 `frozenset(space_ids | verified_ids)`；空集时**仍返回空 frozenset 而非 `None`**（`None` 专表「无项目上下文」，空集表「有上下文但该项目零关联仓」——两者语义不同，直接决定 `block_order` 是长度 2 还是 `["global"]`，注释必须写清）。
       - 同步 ORM 全部经 `sync_to_async`（async ORM 纪律）。
       - 记一条 `repo_group_scope_resolved`（`category="sampling"`, `component="repo_router_v2"`, kv: `space_repo_count` / `verified_repo_count` / `union_count` / `source`），整块 `try/except: pass`。
    2. `index_status` 决策：**分组依据不做 `index_status` 过滤**（分组是「归属」语义，未索引的仓也属于本项目；候选本来就只可能来自已索引的仓，过滤与否对结果无影响但语义更干净）——在注释里写明该判断。
    3. 新建 `server/tests/codegraph/test_repo_group_scope.py`：按 `<behavior>` 逐条写用例（`pytest.mark.django_db`）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/codegraph/test_repo_group_scope.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `<behavior>` 8 条全绿
    - `None` 与空集语义可区分：有独立用例分别断言 `is None` 与 `== frozenset()`
    - async ORM 纪律：`test "$(rg -c 'sync_to_async' server/codegraph/services/repo_group_scope.py)" != "0"`
    - 并集半边失败降级：有用例 monkeypatch `get_verified_associations` 抛异常后仍返回 space 半边
    - `cd server && uv run ruff check codegraph/services/repo_group_scope.py tests/codegraph/test_repo_group_scope.py`
  </acceptance_criteria>
  <done>「本项目关联仓」有唯一、可单测、口径明确（D-2 宽口径并集）的解析入口。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: D-1 候选范围语义改造——两个入口改传分组依据（唯一回归风险点，独立承载）</name>
  <files>server/services/process_runtime/repo_router_adapter.py, server/tests/services/test_repo_router_adapter.py, server/agents/tools/repository_relevance.py, server/tests/agents/test_repository_relevance_tool.py</files>
  <read_first>
    - server/services/process_runtime/repo_router_adapter.py:29-89（`route()` 全文 + `_resolve_repository_ids` 三级优先解析：`include_repos` → `work_item.space` 仓 → `None`）
    - server/agents/tools/repository_relevance.py:180-260（`_analyze_relevance_core`：space 仓查询 → `RepoRouterV2.route(query, top_k=top_k, repository_ids=repo_ids)` → `repo_by_id.get(c.repo_id)` 过滤 → 候选映射）
    - server/codegraph/services/repo_router_v2.py:188-215（107-03 落的 `grouping_repository_ids` 参数与其「与 `repository_ids` 正交」的 docstring）
    - server/codegraph/services/repo_group_scope.py（Task 1 产出的解析入口）
    - server/tests/services/test_repo_router_adapter.py 与 server/tests/agents/test_repository_relevance_tool.py（**回归基线**：现有断言逐条读过，改动后必须全绿；若某条断言本质上锁定了「候选只来自空间内仓」，需在 SUMMARY 中如实记录该断言的语义变更与调整理由）
    - .planning/phases/107-layered-presentation/107-RESEARCH.md OQ-1（推荐方案与三条落地要求 (a)(b)(c)）+ Pitfall 2（global 组恒空的成因与检出方式）
  </read_first>
  <behavior>
    - 编排入口：session 有 `work_item_id` → `RepoRouterV2.route` 收到 `repository_ids=None` 且 `grouping_repository_ids == <宽口径并集>`（替身捕获实参断言）
    - 编排入口：`decomposition["include_repos"]` 非空 → **`repository_ids` 仍为该显式列表**（调用方明确要限定范围时硬过滤语义保留），`grouping_repository_ids` 仍为项目关联仓
    - 编排入口：无 `work_item_id`（chat 发起的编排）→ `repository_ids=None`、`grouping_repository_ids=None`，不抛
    - chat 入口：给定 `space_id` → `repository_ids=None`、`grouping_repository_ids == <宽口径并集>`
    - chat 入口：候选中出现空间外的仓（`repo_by_id` 无该 id）→ **不再被静默丢弃**：用候选自带的 `repo_name` 兜底成 `repository_name`（否则全库召回的跨组候选会在映射阶段全部消失，`global` 组仍然恒空）
    - global 组非空（正向断言）：构造「正确仓在项目关联范围之外」的替身场景 → 结果候选中存在 `group == "global"` 的项，且 `block_order` 长度 == 2
    - 其余 6 个调用方零改动：静态断言这 6 处调用行不含 `grouping_repository_ids`
    - MCP / REST 两个无项目上下文入口：`grouping_repository_ids` 缺省 → 全部 `group == "global"`、`block_order == ["global"]`、不报错
  </behavior>
  <action>
    1. **编排入口**（`repo_router_adapter.py`）：
       - 新增 `async def _resolve_grouping_repository_ids(self, session) -> list[str] | None`：调 `aresolve_grouping_repo_ids(work_item_id=session.work_item_id)`，`None` 原样返回 `None`，否则返回 `sorted(ids)`（排序保证快照/回放确定性）；
       - `_resolve_repository_ids` 语义收窄为「**仅** `include_repos` 显式限定 → 返回该列表；否则 `None`」——删掉「② work_item.space 仓」这一级（它正是硬过滤的来源），并在注释里写明 D-1 的因果：该级过滤会让 `global` 组恒空，ROUTE-01/02 上线即无效果；空间归属信息现在走 `grouping_repository_ids`；
       - `route()` 调用改为 `RepoRouterV2.route(query, top_k=self.top_k, repository_ids=<收窄后>, grouping_repository_ids=<分组依据>, use_llm=True)`；
       - 精简 dict 的候选映射补 `group` / `trust`（下游 clarify/research/feature_confirm 只读 `confidence`，加字段 additive-safe），结果 dict 补 `block_order` / `degrade_reason`（供 `_h_route` 组 `repo.routing` 事件 payload）。
    2. **chat 入口**（`repository_relevance.py`）：
       - `RepoRouterV2.route(query, top_k=top_k, repository_ids=None, grouping_repository_ids=<aresolve_grouping_repo_ids(space_id=space_id) 的 sorted list>)`；
       - `repo_by_id.get(c.repo_id)` 为 None 时**不再 `continue`**：改为用 `c.repo_name` 兜底 `repository_name`（注释写明：全库召回后跨组候选必然不在 `repo_by_id` 里，继续 `continue` 会让 `global` 组恒空——这是 Pitfall 2 的第二个入口）；仍保留对 `repo_id` 为空的防御性跳过。
    3. **回归守护**：
       - 先跑基线：`cd server && uv run pytest tests/services/test_repo_router_adapter.py tests/agents/test_repository_relevance_tool.py tests/initiatives/test_repo_association_service.py -q` 记下改前结果；
       - 改后必须全绿。若某条断言本质上锁定了「候选只来自空间内仓」这一旧语义 → 调整该断言并在 SUMMARY 的 Deviations 里逐条记录（断言原文 / 语义变更 / 为何是预期），**不得**为了让测试过而回退 D-1；
       - 两个测试文件各补 `<behavior>` 中对应入口的用例，其中「global 组非空」与「跨组候选不被映射阶段丢弃」两条必须独立命名。
    4. **T-107-01 前提记录**：在 `repository_relevance.py` 与 `repo_router_adapter.py` 的改动处各加一条注释，写明「放开硬过滤后结果含项目外仓名；沿用 MCP/REST 两个已上线全库入口的既有判断（仓名不敏感），本改动不新增可见性面」；并在 SUMMARY 中作为显式前提记录。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/test_repo_router_adapter.py tests/agents/test_repository_relevance_tool.py tests/initiatives/test_repo_association_service.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `<behavior>` 8 条全绿；「global 组非空」用例独立存在（Pitfall 2 的检出手段）
    - 只改两个入口：`test "$(rg -c 'grouping_repository_ids' server/agents/tools/space_tools.py server/initiatives/services/repo_association_service.py server/knowledge/sources/artifact.py server/tools/handlers/skill_steps.py server/mcp_tools/views.py server/repositories/route_views.py || echo 0)" = "0"`
    - 硬过滤语义保留给显式调用方：有用例断言 `include_repos` 非空时 `repository_ids` 仍为该列表
    - 跨组候选不再被丢弃：`test "$(rg -n 'repo_by_id.get' -A 3 server/agents/tools/repository_relevance.py | rg -c 'continue' || echo 0)" = "0"`
    - 三个回归测试文件全绿；若有断言调整，SUMMARY 的 Deviations 段逐条记录
    - `cd server && uv run pytest tests/codegraph tests/services tests/agents -q` 全绿
    - `cd server && uv run ruff check services/process_runtime/repo_router_adapter.py agents/tools/repository_relevance.py`
  </acceptance_criteria>
  <done>global 组不再恒空——ROUTE-01/02 从「字段就位」变成「用户真能看到两组」；硬过滤语义完整保留给需要它的调用方。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: chat 链候选字段透传（pydantic Output schema 扩展）</name>
  <files>server/agents/tools/schemas/repository_relevance.py, server/agents/tools/repository_relevance.py, server/tests/agents/test_repository_relevance_tool.py</files>
  <read_first>
    - server/agents/tools/schemas/repository_relevance.py:1-60（模块 docstring 明确「fixture 只冻结 Input schema」；`RepositoryRelevanceCandidate` 现有字段与 `breakdown` 的 `Field(default_factory=dict)` 范式——新字段同款默认值纪律）
    - server/tests/agents/fixtures/repository_relevance_input_schema.json（**只冻结 Input**——确认 Output 加字段不触发该守护）
    - server/agents/tools/repository_relevance.py:239-255（`RepositoryRelevanceCandidate(...)` 构造处：105-06 加 `breakdown` 的同一条路，新字段在此映射）
    - server/agents/tools/repository_relevance.py:261-280（`trace = await RepositoryRoutingTrace.objects.acreate(candidates=[c.model_dump() for c in candidates], ...)`——新字段经 `model_dump` 自动进 JSON）
    - .planning/phases/107-layered-presentation/107-UI-SPEC.md §前端数据契约变更（前端期望的键名与语义，必须字面对齐）
  </read_first>
  <behavior>
    - `RepositoryRelevanceCandidate(repository_id="r", repository_name="n", score=0.5, level="high", evidence="e", selected_by_ai=False, selected_by_user_final=False)` 可构造 → `group == ""`、`trust == ""`、`score_ranked is None`
    - `model_dump()` 键集合含 `group` / `trust` / `score_ranked`
    - router 候选带 `group="global"` / `trust="needs_confirmation"` / `score_ranked=0.42` → 映射后 pydantic 候选三字段值一致
    - `RepositoryRoutingTrace.candidates` JSON 中每个元素含这三键（`model_dump` 透传，无需额外代码）
    - Input schema fixture 未变（`git diff` 对该 fixture 为空）
    - `score_ranked` 为 `None` 时 `model_dump()` 输出 `None`（不被转成 0.0）
  </behavior>
  <action>
    1. `RepositoryRelevanceCandidate` 追加三字段（**与 UI-SPEC 的键名字面对齐**）：`group: str = ""`、`trust: str = ""`、`score_ranked: float | None = None`。各带中文注释：`group` 缺省视为 `global`；`score_ranked` 是旁路排序分，前端排序用 `score_ranked ?? score`，**徽标与分数分解合计行继续用 `score`**（D-3）。
       **不加** `cross_group_note`：UI-SPEC 明确前端不渲染后端自由文本（T-107-06），该字段只在 router 侧留痕，不必进 chat 链（少一个字段少一条泄漏面）。
    2. `repository_relevance.py` 的候选构造处映射三字段（`group=c.group`、`trust=c.trust`、`score_ranked=c.score_ranked`）。
    3. `test_repository_relevance_tool.py` 补用例覆盖 `<behavior>` 全部 6 条。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/agents/test_repository_relevance_tool.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `<behavior>` 6 条全绿
    - Input schema fixture 未变：`cd server && git diff --name-only | rg -q 'repository_relevance_input_schema\.json' && exit 1 || true`
    - 键名与 UI-SPEC 字面一致：`test "$(rg -c 'score_ranked' server/agents/tools/schemas/repository_relevance.py)" != "0"` 且 `test "$(rg -c 'cross_group_note' server/agents/tools/schemas/repository_relevance.py || echo 0)" = "0"`
    - `cd server && uv run pytest tests/agents -q` 全绿 + `uv run ruff check agents/tools/schemas/repository_relevance.py agents/tools/repository_relevance.py`
  </acceptance_criteria>
  <done>分组事实进入 `RepositoryRoutingTrace.candidates`，前端链路的上游数据齐备。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 候选范围放开 → 结果含项目外仓名 | 全库召回可能把用户所在项目外的仓名/路径暴露到分组结果 |
| Space / RepoAssociation 查询 → 分组依据 | 分组依据错误会让 in_project 组过宽（无信息量）或过窄（用户被无谓打扰） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-107-01 | Information Disclosure | 放开 `repository_ids` 硬过滤后的全库召回 | accept（前提显式记录） | 前提：`mcp_tools/views.py:434` 与 `repositories/route_views.py:39` 两个**已上线**入口本来就全库路由且无 per-user 可见性过滤 → 团队既有判断为「仓名不敏感」；`_stage0_node_search` 侧不存在按用户过滤的机制，本改动**不绕过任何现存权限检查、不新增可见性面**。前提写进代码注释 + SUMMARY + VERIFICATION；若团队判断改变，需在 Phase 109/110 补可见性过滤层 |
| V4 Access Control | Information Disclosure | `RepositoryRoutingTrace` 跨用户/跨项目校验 | mitigate | `chat/views.py:2688`/`:2702` 的既有校验（`routing_trace_manual_override_denied_cross_user` / `_cross_project`）不得被绕过——本 plan 不改这两处，107-08 改 payload 时同样不得绕过（该 plan 已列为验收项） |
| T-107-12 | Tampering | 分组依据口径错误 | mitigate | D-2 宽口径并集有单测（8 条行为）；`None`（无上下文）与空集（有上下文零关联仓）语义分离，直接决定 `block_order` 形状 |
| T-107-SC | Tampering | npm/pip/cargo installs | accept | 零新增依赖 |
</threat_model>

<verification>
- `cd server && uv run pytest tests/codegraph tests/services tests/agents tests/initiatives -q` 全绿（回归基线三文件在内）
- `cd server && uv run ruff check codegraph/services/repo_group_scope.py services/process_runtime/repo_router_adapter.py agents/tools/repository_relevance.py agents/tools/schemas/repository_relevance.py`
- 六个未改调用方静态自检（见 Task 2 acceptance criteria）
- T-107-01 前提已在 SUMMARY 记录（人工核对）
</verification>

<success_criteria>
- `global` 组不再恒空（有正向断言）；硬过滤语义保留给 `include_repos` 等显式调用方
- 「本项目关联仓」= D-2 宽口径并集，有独立可单测入口；`None` 与空集语义分离
- 其余 6 个 `route()` 调用方零改动（静态可验证）
- 三个既有回归测试文件全绿；断言调整（若有）逐条记入 SUMMARY
- chat 链候选携带 `group` / `trust` / `score_ranked`；Input schema fixture 未动
</success_criteria>

<output>
完成后创建 `.planning/phases/107-layered-presentation/107-07-SUMMARY.md`。
</output>
