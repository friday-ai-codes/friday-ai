---
phase: 107-layered-presentation
plan: 09
type: execute
wave: 5
depends_on: [107-08]
files_modified:
  - web/src/types/routing.ts
  - web/src/stores/routing.ts
  - web/src/stores/chat.ts
  - web/src/components/chat/RoutingDecisionPanel.vue
  - web/src/stores/__tests__/routing.test.ts
  - web/src/components/chat/__tests__/RoutingDecisionPanel.test.ts
autonomous: true
requirements: [ROUTE-01, ROUTE-02, RELY-03]
assumptions:
  - "UI-SPEC unresolved 1：「本项目关联仓」的实际口径由后端 D-2 裁决（Space ∪ verified RepoAssociation 宽口径），前端只消费 `group` 字段，口径变化零前端改动。"
  - "UI-SPEC unresolved 2：OQ-1 已采纳「放开硬过滤」（107-07），故 `global` 组不再恒空；若后续回退该裁决，前端行为仍正确（只渲染本项目组），但需在 VERIFICATION 中如实记录 ROUTE-01/02 无实际效果。"
  - "UI-SPEC unresolved 3：编排链（`ConvergenceSession.routing` / `EVENT_REPO_ROUTING`）在 web 端仍零引用，本 phase 的 UI 只覆盖 chat 链的 `RepositoryRoutingTrace`；编排侧呈现 → Phase 110。"
  - "UI-SPEC unresolved 4：降级原因的排障下钻（脱敏后的原始异常文本）只入事件 payload / `SystemLogEntry`，**不做前端展开区**（A6：面板内看原文需 superuser 可见性设计，属额外权限面）→ 留后续。"
  - "UI-SPEC unresolved 5：RELY-02 的澄清 pending 可见性与「未澄清假设」标注**本 phase 无前端渲染面**（D-6 只写 `stage_state`，产出渲染受 v0.20.0 DEPTH 冻结约束）→ Phase 109/110 或 v0.20.0。"
  - "UI-SPEC unresolved 6：`RoutingDecisionPanel` 家族整体硬编码中文（`SIGNAL_LABELS` 惯例，106-05 既定），本 phase 跟随现状不引入 vue-i18n key；统一迁移属技术债。"
  - "UI-SPEC unresolved 7：`trust` 与 `group === 'global'` 在本 phase 语义完全重合 → 不渲染第二个徽标；`trust` 仅作数据契约留存。"
must_haves:
  truths:
    - "分组分区呈现（ROUTE-01）：`block_order?.length === 2` 时渲染「本项目关联仓（n）」/「全局候选（n）」两区；区顺序严格等于后端 `block_order`，区内按 `score_ranked ?? score` 降序；空组不渲染。**必须删除** `sortedCandidates` 的全局 `b.score - a.score` 重排（Pitfall 4）"
    - "Top-3 + pin-in + 溢出披露：每组默认可见集 = `Top-3 ∪ (selected_by_ai || selected_by_user_final)`；剩余进组内 `Collapsible`（`显示其余 {n} 个候选`）；组标题计数为该组总数"
    - "全局组默认折叠：`block_order[0] === 'in_project' && inProject[0]?.level === 'high'` 时默认折叠，否则展开；用户操作后本地态优先，trace 变化后重算"
    - "跨组标注两层（ROUTE-02）：全局组标题下常驻完整句「未关联当前平台，可能涉及跨组协作」+ 每候选 `Badge variant=\"info\"`「跨组」（Tooltip/`aria-label` 承载完整句）"
    - "迟滞置顶提示（ROUTE-02）：`block_order[0] === 'global'` 时在两区之上渲染 info 提示条「更匹配的仓不在本项目关联范围内」（`role=\"status\"`）；与降级横幅并存时降级在上"
    - "降级横幅（RELY-03）：`degraded === true` 时在候选列表之前渲染 amber `role=\"alert\"` 告警条，主句「本次未经 LLM 推理，置信度仅供参考」+ 次行「降级原因：{中文枚举}」；`degrade_reason` 缺失只出主句"
    - "折叠态降级徽标：面板收起时标题行渲染 `Badge variant=\"warning\"`「降级」，降级事实不被折叠隐藏"
    - "confidence 徽标降级样式：`degraded` 时全部候选徽标 `variant=\"muted\"`，Tooltip 切换为降级三句；`level` / `score` / `breakdown` 一律不改"
    - "降级原因文案闭集 + 不回显原始值：前端 `DEGRADE_REASON_LABELS` 6 值 map；未命中回退「未知原因」，**绝不**回显原始字符串（T-107-02 / Pitfall 10）。`cross_group_note` 等后端自由文本一律不渲染"
    - "前端数据契约：`RoutingCandidate` 新增 `group?` / `trust?` / `score_ranked?` / `cross_group_note?`；`RoutingDecisionData` 新增 `router_version?` / `degraded?` / `degrade_reason?` / `block_order?`（全部 optional）"
    - "后端契约要求已由 107-03/107-08 满足：有项目上下文时 `block_order` 恒为长度 2（即使一组为空）；无上下文时省略或 `['global']`——这是前端区分两种 all-global 场景的唯一依据"
    - "override 不丢字段（Pitfall 3，必修既有缺陷）：`stores/routing.ts` `applyManualOverride` 重建 trace 时从 `original` 继承 `router_version` / `degraded` / `degrade_reason` / `block_order`，且新候选保留 `group` / `trust` / `score_ranked`；验收 = 勾选一次后降级横幅与分组分区仍在"
    - "视觉零漂移：不新增颜色 / 字号 / 字重 / 间距值 / 组件 / 依赖；不新建 `ui/alert`；遵守 DESIGN.md Badge `:class` 禁令与彩虹卡片禁令"
  backstops:
    - statement: "历史 trace 完全兼容：无 group / trust / degraded / block_order / score_ranked 的 trace → 平铺列表、无组标题、无跨组 Badge、无提示条、按 score 降序，与今日渲染逐像素一致，且不抛错、不打 warn"
      verification: backstop
    - statement: "部分字段缺失：某候选缺 group → 视为 'global'；score_ranked 为 null/undefined → 回退 score；degraded 缺失 → false；degrade_reason 非受控值 → 「未知原因」"
      verification: backstop
    - statement: "无项目上下文入口（MCP / REST / skill_steps 产出的 trace）→ 平铺、无跨组标注（此时「跨组」无意义，标了反而误导）"
      verification: backstop
    - statement: "展开态不持久化：全局组折叠态与溢出披露态均为组件本地 ref，trace 更新后重置/重算，与既有 expandedBreakdowns 惯例一致"
      verification: backstop
    - statement: "无 v-html：所有新文案走 {{ }} 插值（组件现状实读无 v-html，保持）"
      verification: backstop
    - statement: "可访问性：降级横幅 role=\"alert\" aria-live=\"polite\"；置顶提示 role=\"status\"；跨组 Badge 带 aria-label 完整句；折叠 trigger 为原生 button（Collapsible 既有行为）"
      verification: backstop
    - statement: "测试扩充：RoutingDecisionPanel.test.ts 既有 12 passed 零回归，新增区顺序/区内排序/折叠两分支/Top-3 与 pin-in/跨组两层/置顶提示/降级横幅与 6 枚举/非受控值回退/折叠态徽标/历史 trace 平铺共 10 类用例；routing.test.ts 新增 override 不丢字段用例"
      verification: backstop
  artifacts:
    - path: web/src/types/routing.ts
      provides: "RoutingGroup / RoutingTrust / RoutingDegradeReason 三个类型 + 候选与 trace 的 8 个 optional 新字段"
      contains: "RoutingDegradeReason"
    - path: web/src/stores/routing.ts
      provides: "applyManualOverride 从 original 兜底继承 4 个 trace 级字段（Pitfall 3 前端半边）"
      contains: "block_order"
    - path: web/src/components/chat/RoutingDecisionPanel.vue
      provides: "分区渲染 + 跨组两层标注 + 置顶提示 + 降级横幅与徽标灰化 + 溢出披露"
      min_lines: 380
  key_links:
    - from: web/src/components/chat/RoutingDecisionPanel.vue
      to: web/src/stores/routing.ts
      via: "按 trace.block_order 分区、区内按 score_ranked ?? score 排序（替换全局 b.score - a.score 重排）"
      pattern: "block_order"
    - from: web/src/stores/chat.ts
      to: web/src/stores/routing.ts
      via: "三处 upsertTrace 透传新字段（detail hydrate / chat_tool / deep_analysis）"
      pattern: "upsertTrace"
---

<objective>
把后端已备的分组/跨组/降级三类事实渲染出来，并修掉两个必修的既有前端缺陷：`sortedCandidates` 的全局 `b.score - a.score` 重排（会覆盖后端分区顺序与 block 置顶，Pitfall 4）与 `applyManualOverride` 重建 trace 只保留 5 键（用户改一次勾选后降级横幅消失，Pitfall 3）。

**唯一组件落点**：`web/src/components/chat/RoutingDecisionPanel.vue`（三块增量：分区呈现 / 跨组标注与置顶提示 / 降级横幅与徽标灰化）。零新依赖、零新色板、零新组件——全部复用 `Badge`(8 variant) / `Collapsible` / `Tooltip` 与仓内三处既有告警条 DOM 形状（`CommitConfirmCard.vue:100-105` / `ContextExceededCard.vue:79-81` / `ReconcilePanel.vue:132-138`）。

Purpose: SC-1 / SC-2 / SC-3 的用户可见面——「用户能一眼看出哪些是本平台内的」「跨组候选带明确标注」「降级时看得见提示」。
Output: 类型契约 + 两个 store 修正 + 组件三块增量 + 两个测试文件补用例。
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/107-layered-presentation/107-UI-SPEC.md
@.planning/phases/107-layered-presentation/107-RESEARCH.md
@web/src/types/routing.ts
@web/src/stores/routing.ts
@web/src/components/chat/RoutingDecisionPanel.vue
@web/DESIGN.md
</context>

## Artifacts this phase produces（本 plan 新建符号）

| Symbol | Kind | Location |
|--------|------|----------|
| `RoutingGroup = 'in_project' \| 'global'` | 类型 | `web/src/types/routing.ts` |
| `RoutingTrust = 'trusted' \| 'needs_confirmation'` | 类型 | 同上 |
| `RoutingDegradeReason`（6 值联合） | 类型 | 同上 |
| `RoutingCandidate.group?` / `.trust?` / `.score_ranked?` / `.cross_group_note?` | 类型字段（全 optional） | 同上 |
| `RoutingDecisionData.router_version?` / `.degraded?` / `.degrade_reason?` / `.block_order?` | 类型字段（全 optional） | 同上 |
| `DEGRADE_REASON_LABELS`（6 值中文 map，未命中回退「未知原因」） | 组件常量 | `RoutingDecisionPanel.vue` |
| `GROUP_LABELS`（`本项目关联仓` / `全局候选`） | 组件常量 | 同上 |
| `CROSS_GROUP_SENTENCE = '未关联当前平台，可能涉及跨组协作'` | 组件常量（**前端常量，不用后端 `cross_group_note`**） | 同上 |
| `PROMOTION_SENTENCE = '更匹配的仓不在本项目关联范围内'` | 组件常量 | 同上 |
| `DEGRADED_BANNER_TITLE = '本次未经 LLM 推理，置信度仅供参考'` | 组件常量 | 同上 |
| `CONFIDENCE_TOOLTIPS_DEGRADED`（降级态三句） | 组件常量 | 同上 |
| `groupedBlocks` | computed（替代 `sortedCandidates` 的分区结构） | 同上 |
| `globalGroupOpen` / `overflowOpen` | 组件本地 ref（trace 变化后重算/重置） | 同上 |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 类型契约 + store 透传与 override 兜底继承（Pitfall 3 前端半边）</name>
  <files>web/src/types/routing.ts, web/src/stores/routing.ts, web/src/stores/chat.ts, web/src/stores/__tests__/routing.test.ts</files>
  <read_first>
    - web/src/types/routing.ts（全 50 行：`RoutingCandidate` / `RoutingDecisionData` / `ManualOverrideResponse` 现有形状与 `breakdown?` 的 optional 注释风格）
    - web/src/stores/routing.ts:60-88（`applyManualOverride`：重建 `newTrace` 只给 5 键——`degraded` / `degrade_reason` / `router_version` / `block_order` 全丢，横幅随之消失）
    - web/src/stores/chat.ts:513-515（detail hydrate）与 :1275-1290（`chat_tool`）与 :1300-1314（`deep_analysis`）三处 `upsertTrace` —— 后两处是**手工构造 `RoutingDecisionData`**（只填 5 键），必须补透传新字段
    - .planning/phases/107-layered-presentation/107-UI-SPEC.md §前端数据契约变更（键名与语义必须字面对齐后端）
    - .planning/phases/107-layered-presentation/107-RESEARCH.md Pitfall 3（两处都要改；后端半边已由 107-08 落地：override 响应现在回传 4 键）
  </read_first>
  <behavior>
    - `applyManualOverride` 成功后：新 trace 的 `router_version` / `degraded` / `degrade_reason` / `block_order` 取**响应值优先、`original` 兜底**（响应缺键时不丢）
    - `applyManualOverride` 后新 trace 的候选保留 `group` / `trust` / `score_ranked`
    - `original` 不存在（store 里没有原 trace）且响应也没给 → 四个字段为 `undefined`，不抛
    - `chat.ts` 的 `chat_tool` 分支：工具返回 data 含 `router_version` / `degraded` / `degrade_reason` / `block_order` → 构造的 trace 携带这四键
    - `chat.ts` 的 `deep_analysis` 分支：无这四键时构造的 trace 四键为 `undefined`（不填假值）
    - detail hydrate 分支：`detail.routing_trace` 直接 `upsertTrace`（后端已出 9 键）→ 无需字段白名单，透传即可（补一条断言防将来被改成显式字段列表）
    - 类型层面：`RoutingDegradeReason` 是 6 值联合；给 trace 赋非受控字符串时 `vue-tsc` 报错（类型守护，编译期即拦）
  </behavior>
  <action>
    1. `web/src/types/routing.ts`：新增 `RoutingGroup` / `RoutingTrust` / `RoutingDegradeReason` 三个类型；`RoutingCandidate` 加 `group?` / `trust?` / `score_ranked?: number | null` / `cross_group_note?`（后者注释写明「后端留痕用，**前端不渲染**」，T-107-06）；`RoutingDecisionData` 加 `router_version?: string` / `degraded?: boolean` / `degrade_reason?: RoutingDegradeReason` / `block_order?: RoutingGroup[]`。全部 optional（历史 trace 兼容）。
    2. `web/src/stores/routing.ts` 的 `applyManualOverride`：`newTrace` 改为
       ```
       router_version: response.router_version ?? original?.router_version,
       degraded: response.degraded ?? original?.degraded,
       degrade_reason: response.degrade_reason ?? original?.degrade_reason,
       block_order: response.block_order ?? original?.block_order,
       ```
       并给 `ManualOverrideResponse` 类型补这四个 optional 键。注释写明因果（Pitfall 3）：不继承则用户改一次勾选后降级横幅与分组分区凭空消失。
    3. `web/src/stores/chat.ts` 三处 `upsertTrace`：`chat_tool` 与 `deep_analysis` 两处手工构造处补四键透传（从 `data` 取，缺则 `undefined`，**不填假值**）；detail hydrate 处确认是整对象透传（若是则不改，加一行注释固化该契约）。
    4. `web/src/stores/__tests__/routing.test.ts` 补用例覆盖 `<behavior>` 前 3 条 + detail 透传断言。
  </action>
  <verify>
    <automated>cd web && pnpm vitest run src/stores/__tests__/routing.test.ts && pnpm exec vue-tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `<behavior>` 7 条全绿（类型守护一条由 `vue-tsc --noEmit` 覆盖）
    - override 兜底继承落地：`test "$(rg -c 'original\?\.degraded' web/src/stores/routing.ts)" != "0"`
    - 不填假值：`test "$(rg -n 'degraded:' web/src/stores/chat.ts | rg -c 'false' || echo 0)" = "0"`
    - `cd web && pnpm vitest run src/stores` 全绿（既有 chat store 用例零回归）
    - `cd web && pnpm exec eslint src/types/routing.ts src/stores/routing.ts src/stores/chat.ts`
  </acceptance_criteria>
  <done>降级与分组事实在前端全链路（hydrate / 工具流 / override）不再丢失（Pitfall 3 闭合）。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 分区渲染 + Top-3/pin-in/折叠 + 跨组标注 + 置顶提示（移除全局重排，Pitfall 4）</name>
  <files>web/src/components/chat/RoutingDecisionPanel.vue, web/src/components/chat/__tests__/RoutingDecisionPanel.test.ts</files>
  <read_first>
    - web/src/components/chat/RoutingDecisionPanel.vue:44-125（`effectiveTraceId` / `trace` / **`sortedCandidates`（:50-54 的全局重排，必须删）** / `levelCounts` / `collapsed` / `variantOf` / `labelOf` / `SIGNAL_LABELS` / `CONFIDENCE_TOOLTIPS` / `expandedBreakdowns` 与其 `watch(effectiveTraceId)` 重置惯例）
    - web/src/components/chat/RoutingDecisionPanel.vue:150-303（template 全文：`Card` → 折叠按钮 → query/threshold 行 → `TooltipProvider > ul > li` 逐候选 → 两个 Button；`Collapsible` 分数分解的 trigger 图标与 `rotate-90` 惯例）
    - web/src/components/ui/badge/index.ts（8 个 variant 的确切名称与色值——跨组用 `info`、降级徽标用 `warning`、灰化用 `muted`）
    - web/src/components/chat/CommitConfirmCard.vue:100-105 与 web/src/components/chat/ReconcilePanel.vue:132-138（告警条 DOM 形状：`flex items-start gap-2 rounded-lg border border-X/30 bg-X/5 px-3 py-2.5` + `icon-[lucide--*] shrink-0 mt-0.5`——置顶提示逐字沿用，换 teal + `icon-[lucide--arrow-up-narrow-wide]`）
    - .planning/phases/107-layered-presentation/107-UI-SPEC.md §交互契约 A/B（启用条件、区顺序、区内排序、空组、组标题、默认折叠判定、Top-3 与 pin-in、跨组两层、置顶提示位置与并存优先级）+ §Copywriting Contract（文案逐字）+ §Spacing/Typography/Color（不得引入新值）
    - .planning/phases/107-layered-presentation/107-RESEARCH.md Pitfall 4（全局 re-sort 会覆盖后端顺序）
  </read_first>
  <behavior>
    - 区顺序等于后端 `block_order`：`block_order=["global","in_project"]` → DOM 中「全局候选」组标题先出现
    - 区内按 `score_ranked` 排序：构造 `score_ranked` 与 `score` 排序相反的候选 → 渲染顺序按 `score_ranked`
    - `score_ranked` 为 `null`/`undefined` → 该候选按 `score` 参与排序（混合场景不抛）
    - 全局组默认折叠：`block_order[0]==='in_project'` 且 in_project 首位 `level==='high'` → 全局组内容不可见；否则可见
    - 用户手动展开后 → 本地态优先；`effectiveTraceId` 变化 → 重算默认态
    - Top-3 截断 + pin-in：某组 5 个候选、其中排第 5 的 `selected_by_user_final=true` → 可见集为 4 个（Top-3 ∪ 已选），溢出 trigger 文案 `显示其余 1 个候选`
    - 组标题计数为该组**总数**：上例组标题为「（5）」
    - 跨组两层：全局组标题下有完整句常驻可见；每个全局候选有 `Badge` 文案「跨组」且带 `aria-label` 完整句；本项目候选**无**该 Badge
    - 置顶提示：`block_order[0]==='global'` → 两区之上出现 `role="status"` 提示条含「更匹配的仓不在本项目关联范围内」；`block_order[0]==='in_project'` → 无该提示条
    - 空组不渲染：某组 0 条 → 该组标题与列表都不出现（无空态占位）
    - 历史 trace（无 `block_order` / `group` / `score_ranked`）→ 平铺一个 `ul`、无组标题、无跨组 Badge、无提示条、按 `score` 降序；既有 12 个用例零回归
    - 兜底：`block_order` 缺失但存在 `group === 'in_project'` 的候选 → 按 `['in_project','global']` 启用分组
    - `block_order` 长度 1（`['global']`，无项目上下文）→ 平铺、无跨组标注
  </behavior>
  <action>
    1. **删除** `sortedCandidates` 的 `[...candidates].sort((a, b) => b.score - a.score)`，改为：
       - `rankKey = (c) => c.score_ranked ?? c.score`；
       - `groupedBlocks` computed → `Array<{ group: RoutingGroup, total: number, visible: RoutingCandidate[], overflow: RoutingCandidate[] }>`，按 `block_order` 的顺序产出（缺失时按兜底规则）；区内按 `rankKey` 降序（tie 时按 `repository_id` 升序，与后端稳定 tie-break 同口径）；
       - 分组未启用（`block_order?.length !== 2` 且无 `in_project` 候选）→ 产出单块 `{ group: 'global', ... }` 并在 template 走「平铺分支」（无组标题、无 Badge、无提示条）。
       - `levelCounts` 与容差校验的 `watch` 改吃**扁平化后的全部候选**（不是可见集），保持既有语义。
    2. 组标题：`text-xs` 一行，组名 `font-semibold text-foreground` + 计数 `text-muted-foreground` 格式 `（{total}）`；本项目组为纯静态标题（不可折叠）；全局组整块包 `Collapsible`，标题作 `CollapsibleTrigger` 含 `icon-[lucide--chevron-right]`（展开 `rotate-90`，沿用「分数分解」trigger 惯例）。
    3. 默认折叠判定（纯呈现派生）：`defaultGlobalCollapsed = block_order[0] === 'in_project' && inProjectVisible[0]?.level === 'high'`；本地 `globalGroupOpen` ref，`watch(effectiveTraceId)` 时重算（与 `expandedBreakdowns` 重置惯例一致）。
    4. 溢出披露：每组第二个 `Collapsible`，trigger 文案 `显示其余 {n} 个候选` / `收起其余候选`；无溢出则不渲染 trigger。可见集 = `Top-3 ∪ (selected_by_ai || selected_by_user_final)`——**pin-in 是硬要求**：被隐藏的已选候选无法取消勾选，且用户会看到「勾了的仓不见了」。
    5. 跨组标注两层（**文案一律取前端常量**，不渲染 `cross_group_note`，T-107-06）：全局组标题下方一行 `text-xs text-muted-foreground` 渲染 `CROSS_GROUP_SENTENCE`；每个全局候选行加 `<Badge variant="info">跨组</Badge>` 外包 `Tooltip`（content = 完整句）并带 `aria-label` = 完整句。`trust` **不渲染第二个徽标**（与 `group === 'global'` 语义重合）。
    6. 置顶提示：`block_order[0] === 'global'` 时，在 query/阈值行之后、第一个组标题之前渲染 `role="status"` 的 teal 提示条（`border-teal-500/30` + `bg-teal-500/5` + `text-teal-700` + `icon-[lucide--arrow-up-narrow-wide]`，DOM 形状照 `CommitConfirmCard.vue:100-105`）含 `PROMOTION_SENTENCE`。与降级横幅并存时**降级在上**（DOM 顺序：降级横幅 → 置顶提示 → 分组区）。
    7. 间距/字号/颜色**只用 UI-SPEC 列出的既有值**（含四个既有半步值 `space-y-1.5` / `py-1.5` / `py-2.5` / `mt-0.5`）；禁给分组染不同底色（彩虹卡片禁令）；禁在 `Badge` 上用 `:class` 追加颜色。
    8. `RoutingDecisionPanel.test.ts` 补用例覆盖 `<behavior>` 全部 12 条（既有 12 passed 必须零回归）。
  </action>
  <verify>
    <automated>cd web && pnpm vitest run src/components/chat/__tests__/RoutingDecisionPanel.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `<behavior>` 12 条全绿 + 既有 12 个用例零回归（总数 >= 24 passed）
    - 全局重排已删除：`test "$(rg -c 'b\.score - a\.score' web/src/components/chat/RoutingDecisionPanel.vue || echo 0)" = "0"`
    - 后端自由文本未渲染：`test "$(rg -c 'cross_group_note' web/src/components/chat/RoutingDecisionPanel.vue || echo 0)" = "0"`
    - 无 `v-html`：`test "$(rg -c 'v-html' web/src/components/chat/RoutingDecisionPanel.vue || echo 0)" = "0"`
    - 零新依赖 / 零新组件：`cd web && git diff --name-only | rg -q 'package\.json|pnpm-lock\.yaml|src/components/ui/' && exit 1 || true`
    - `cd web && pnpm exec vue-tsc --noEmit && pnpm exec eslint src/components/chat/RoutingDecisionPanel.vue`
  </acceptance_criteria>
  <done>用户能一眼看出哪些仓在本平台内、跨组候选有明确标注、置顶有因果解释（SC-1 / SC-2 的可见面成立）。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 降级横幅 + 徽标灰化 + 折叠态徽标 + 文案闭集</name>
  <files>web/src/components/chat/RoutingDecisionPanel.vue, web/src/components/chat/__tests__/RoutingDecisionPanel.test.ts</files>
  <read_first>
    - web/src/components/chat/RoutingDecisionPanel.vue:56-105（`variantOf` / `labelOf` / `CONFIDENCE_TOOLTIPS` 三处——降级态需要覆盖 variant 与 Tooltip 文案，但**不改 `level` / `score` / `breakdown`**）
    - web/src/components/chat/ChatMessageBubble.vue:1205-1215（`降级回答 · {reason}` + `icon-[lucide--triangle-alert]` 的既有降级徽标语义与图标——折叠态紧凑徽标沿用同款）
    - web/src/components/chat/CommitConfirmCard.vue:100-105（amber 告警条 DOM 形状逐字沿用）
    - .planning/phases/107-layered-presentation/107-UI-SPEC.md §交互契约 C（触发条件、位置、形态、折叠态不隐藏、徽标降级、6 值枚举 map 与「未知 key 回退不回显原始值」的刻意差异）+ §Copywriting Contract（降级三句 Tooltip 与横幅文案逐字）
    - .planning/phases/107-layered-presentation/107-RESEARCH.md Pitfall 10 / T-107-02（不得回显原始异常文本）
  </read_first>
  <behavior>
    - `degraded === true` → 渲染 `role="alert" aria-live="polite"` 的 amber 横幅，含主句「本次未经 LLM 推理，置信度仅供参考」
    - `degrade_reason` 为 6 值之一 → 次行「降级原因：{对应中文}」（六个枚举各一条断言：`timeout`→上游超时 / `upstream_error`→网关错误 / `provider_missing`→未配置模型 / `unparsable`→解析失败 / `no_node_index`→无能力树索引 / `unknown`→未知原因）
    - `degrade_reason` 缺失 → 只出主句，不渲染次行
    - `degrade_reason` 为**非受控值**（如 `"APIStatusError: sk-ant-xxx"`）→ 渲染「未知原因」，且 DOM 中**不含**该原始字符串（与 `SIGNAL_LABELS` 的「未命中回显原始 key」刻意不同）
    - `degraded === false` 或缺失 → 零提示（历史 trace 现状不变）
    - `degraded === true` → 全部候选 confidence 徽标 `variant === 'muted'`；`degraded === false` → 徽标为既有 `success`/`warning`/`secondary` 映射
    - `degraded === true` → 徽标 Tooltip 文案为降级三句之一（按 level）；`degraded === false` → 沿用既有 `CONFIDENCE_TOOLTIPS`
    - 徽标文案格式与 `score` 未变：仍显示 `{score%} 高/中/低`（`score_ranked` **不出现在任何可见文案中**）
    - 面板 `collapsed === true` 且 `degraded === true` → 标题行渲染 `Badge variant="warning"`「降级」（折叠不隐藏降级事实）
    - `level` / `score` / 分数分解区在 `degraded` 下值不变（断言 breakdown 合计行仍显示 `score`）
  </behavior>
  <action>
    1. 组件常量新增 `DEGRADE_REASON_LABELS: Record<string, string>`（6 值，UI-SPEC 表逐字）与 `CONFIDENCE_TOOLTIPS_DEGRADED: Record<RoutingLevel, string>`（降级三句逐字）。
       🔴 `degradeReasonLabel(key)` 的回退与 `signalLabel` **刻意不同**：未命中一律返回「未知原因」，**绝不**回显原始值——`degrade_reason` 的上游是异常分类，一旦后端出现非受控值（异常名或截断的上游 body）回显即泄漏面（T-107-02 / Pitfall 10）。在函数上方加注释写明这一差异及其理由。
    2. 降级横幅：`degraded === true` 时渲染在**折叠切换按钮之下、query/阈值行之上**（用户看到任何候选之前先看到它）；DOM 形状逐字照 `CommitConfirmCard.vue:100-105`（`icon-[lucide--triangle-alert] text-amber-500 shrink-0 mt-0.5` + 主句 `text-sm font-medium text-foreground` + 次行 `text-xs text-muted-foreground`）；`role="alert" aria-live="polite"`。
    3. 折叠态徽标：`collapsed === true && degraded === true` → 标题行右侧 `<Badge variant="warning">降级</Badge>`（沿用 `ChatMessageBubble.vue:1210` 的语义与 `icon-[lucide--triangle-alert]` 惯例）。
    4. 徽标灰化：`variantOf(level)` 增加 degraded 分支 → 返回 `'muted'`（**不改 `level` 值本身**）；Tooltip 文案在 degraded 下取 `CONFIDENCE_TOOLTIPS_DEGRADED`。徽标百分比与「分数分解」合计行**继续用 `score`**（`score_ranked` 只用于排序，不出现在可见文案——避免「徽标 87% 但排在 91% 前面」的无法解释现象）。
    5. **不改** 分数分解区与 1e-6 容差 `console.warn` 逻辑（后端不变量守护，前端零介入）。
    6. `RoutingDecisionPanel.test.ts` 补用例覆盖 `<behavior>` 全部 10 条（6 个枚举文案各一条断言；非受控值回退一条含「DOM 不含原始串」断言）。
  </action>
  <verify>
    <automated>cd web && pnpm vitest run src/components/chat/__tests__/RoutingDecisionPanel.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `<behavior>` 10 条全绿；6 个枚举文案各有断言
    - 非受控值不回显：测试断言 `wrapper.html()` 不含注入的 `sk-ant-` 与原始异常名
    - `score_ranked` 不进可见文案：`test "$(rg -n 'score_ranked' web/src/components/chat/RoutingDecisionPanel.vue | rg -c 'toFixed|Math.round' || echo 0)" = "0"`
    - 零新色板：`test "$(rg -o 'amber-[0-9]+|teal-[0-9]+' web/src/components/chat/RoutingDecisionPanel.vue | sort -u | wc -l | tr -d ' ')" -le "4"`（只允许 UI-SPEC 列出的 amber-500 / teal-500 两族）
    - 无 Badge `:class` 追加颜色：`test "$(rg -n '<Badge' -A 2 web/src/components/chat/RoutingDecisionPanel.vue | rg -c ':class' || echo 0)" = "0"`
    - `cd web && pnpm vitest run src/components/chat src/stores` 全绿 + `pnpm exec vue-tsc --noEmit` + `pnpm exec eslint src/components/chat/RoutingDecisionPanel.vue`
  </acceptance_criteria>
  <done>降级时用户看得见明确提示与灰化徽标，且折叠也藏不住；原始异常文本无任何前端回显路径（SC-3 成立）。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 后端 trace payload → DOM | 后端字段若含自由文本，渲染即泄漏/XSS 面 |
| 用户勾选 → override → 重建 trace | 重建时字段丢失会让可信度提示消失（信息缺失型缺陷） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-107-06 | Tampering | 新增文案渲染 | mitigate | 跨组说明句/置顶提示/降级原因**一律取前端常量**；`cross_group_note` 与任何后端自由文本不渲染（静态断言）；全部走 `{{ }}` 插值，组件内无 `v-html`（静态断言） |
| T-107-02 | Information Disclosure | `degrade_reason` 渲染 | mitigate | 6 值 map + 未命中一律「未知原因」**不回显原始值**；测试注入 `sk-ant-` 断言 DOM 无明文 |
| T-107-13 | Tampering | `applyManualOverride` 丢字段 | mitigate | 响应优先 + `original` 兜底继承 4 个 trace 级字段；store 用例断言 override 后 `degraded` / `block_order` / 候选 `group` 不丢 |
| T-107-14 | Tampering | 前端重排覆盖后端决策 | mitigate | 删除全局 `b.score - a.score`；区顺序严格等于 `block_order`、区内按 `score_ranked ?? score`；组件测试断言渲染顺序等于后端给的顺序 |
| T-107-SC | Tampering | npm/pnpm installs | accept | 零新增依赖（`package.json` / `pnpm-lock.yaml` 不得变动，静态断言） |
</threat_model>

<verification>
- `cd web && pnpm vitest run src/components/chat src/stores` 全绿（`RoutingDecisionPanel.test.ts` 既有 12 passed 零回归）
- `cd web && pnpm exec vue-tsc --noEmit`
- `cd web && pnpm exec eslint src/types/routing.ts src/stores/routing.ts src/stores/chat.ts src/components/chat/RoutingDecisionPanel.vue`
- 零新依赖 / 零新 ui 组件：`cd web && git diff --name-only | rg -q 'package\.json|pnpm-lock\.yaml|src/components/ui/' && exit 1 || true`
- 人工视觉验收（记入 107-UAT，非本 plan 阻塞项）：对话路由面板确认两组分区、跨组 badge、置顶提示、降级横幅与徽标灰化与 107-UI-SPEC 一致
</verification>

<success_criteria>
- 区顺序等于后端 `block_order`、区内按 `score_ranked ?? score`；全局重排代码删除（Pitfall 4 闭合）
- Top-3 + pin-in + 溢出披露 + 全局组默认折叠四条判定全部有用例
- 跨组两层标注与迟滞置顶提示按 `block_order` 出现/消失
- 降级横幅 + 6 枚举文案 + 非受控值回退「未知原因」+ 折叠态徽标 + 徽标灰化全部有用例
- override 后降级横幅与分组分区仍在（Pitfall 3 闭合）
- 视觉零漂移：零新依赖、零新组件、零新色板/字号/间距值
</success_criteria>

<output>
完成后创建 `.planning/phases/107-layered-presentation/107-09-SUMMARY.md`。
</output>
