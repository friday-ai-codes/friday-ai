---
phase: 107-layered-presentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - server/codegraph/management/commands/measure_stage1_latency.py
  - server/tests/codegraph/test_measure_stage1_latency.py
  - .planning/phases/107-layered-presentation/107-MEASUREMENTS.md
autonomous: true
requirements: [RELY-05]
assumptions:
  - "O-6 的生产分位数字本 phase **deferred**：本地/CI 无生产 SystemLogEntry 数据，且 SQLite 无 `percentile_cont`。落地的是「可复测的查询管线 + 文档骨架 + 数据环境标注纪律」，生产数字由运维在有数据环境执行本命令后回填，并记入 107-UAT（沿用 105/106 同款纪律）。"
  - "延迟数据源取 `SystemLogEntry.payload['duration_ms']`（事件 `repo_router_v2_stage1_completed`）而非 `ModelUsageRecord`——Stage 1 直调 `build_chat_model(...).ainvoke(...)`，不经 `record_model_usage` 的 chokepoint，故 `ModelUsageRecord` 里查不到 `aux_repo_router` 的行（107-RESEARCH §9 已 VERIFIED）。`ModelUsageRecord` 埋点补齐在 107-05，补齐后本命令新增一条 ledger 口径对照即可（本 plan 不预写）。"
  - "该事件 `category=\"sampling\"` 且为 `logger.info`：落库量受运行时采样配置（`SettingKeys.LOG_*`）与组件级日志级别影响，可能不是全量 → 文档必须标注采样率与「口径是未命中缓存时的上游真实延迟」（缓存命中路径不发 LLM 调用也不打该事件）。"
must_haves:
  truths:
    - "运维可用一条 management command 在任意环境算出 Stage 1 延迟 p50/p90/p99 与样本量，无需手写 SQL：Postgres 走 `percentile_cont`（LOGGING-SPEC §4.3 纪律，不自研直方图），非 Postgres 回退 Python 侧分位计算"
    - "命令输出只含分位数/计数/时间窗等聚合量，不含任何原始 payload、模型名之外的自由文本或凭证串"
    - "107-MEASUREMENTS.md 存在且如实记录：O-6 结论（含 deferred 状态与复测命令）、α 未经离线校准这一局限（D-7）、delta=0.15 的实测依据与 0.1771 上界、per-call 超时暂不下调的理由（A5）、「输入哈希缓存 + 快照回放为主要收益来源」的设计取舍"
    - "命令零样本时不抛异常、输出 n=0 与明确提示（生产未开采样 / 组件日志级别被调高时的常见情形）"
  artifacts:
    - path: server/codegraph/management/commands/measure_stage1_latency.py
      provides: "Stage 1 延迟分位查询命令（`--days` 时间窗 / `--event` 事件名 / `--json` 输出模式）"
      min_lines: 60
    - path: .planning/phases/107-layered-presentation/107-MEASUREMENTS.md
      provides: "O-6 实测文档 + 本 phase 三个数值参数（delta / α / per-call 超时）的取值依据与已知局限"
      contains: "α"
    - path: server/tests/codegraph/test_measure_stage1_latency.py
      provides: "命令单测：构造 SystemLogEntry 行断言分位与计数；零样本不抛；输出无敏感串"
  key_links:
    - from: server/codegraph/management/commands/measure_stage1_latency.py
      to: server/system/models.py
      via: "查询 SystemLogEntry(event='repo_router_v2_stage1_completed') 的 payload.duration_ms"
      pattern: "repo_router_v2_stage1_completed"
---

<objective>
O-6（Stage 1 延迟压降结论）落地为**可复测的查询管线 + 文档**：新建 `measure_stage1_latency` management command（镜像 105 的 `measure_repo_index_stats`：命令形态、参数风格、测试文件命名），从 `SystemLogEntry` 的 `repo_router_v2_stage1_completed` 事件 payload 取 `duration_ms` 算 p50/p90/p99；新建 `107-MEASUREMENTS.md` 记录 O-6 结论与本 phase 三个数值参数（delta / α / per-call 超时）的取值依据与已知局限。

**数据环境标注纪律（沿用 105/106）**：生产分位数字需要生产库数据，本 phase 明确标注 deferred 并写清「用哪条命令在哪个环境复测」；文档不得出现编造的分位数字。

Purpose: SC-5 后半句（「O-6 的延迟压降结论已落文档，若压不下来则缓存与快照回放作为主要收益来源已体现在设计中」）+ 为 A5（per-call 超时是否下调）提供后续判定依据。
Output: 1 个新命令 + 1 个新测试 + 1 份 MEASUREMENTS 文档。
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/107-layered-presentation/107-CONTEXT.md
@.planning/phases/107-layered-presentation/107-RESEARCH.md
@.planning/phases/106-multi-signal-scoring/106-MEASUREMENTS.md
@.planning/observability/LOGGING-SPEC.md
</context>

## Artifacts this phase produces（本 plan 新建符号）

| Symbol | Kind | Location |
|--------|------|----------|
| `measure_stage1_latency`（management command 名） | 命令 | `server/codegraph/management/commands/measure_stage1_latency.py` |
| `--days` / `--event` / `--json` | 命令参数 | 同上 |
| `stage1_latency_measured`（structlog 事件名，`category="sampling"`, `component="repo_router_v2"`） | 事件名 | 同上 |
| `107-MEASUREMENTS.md` | 文档 | `.planning/phases/107-layered-presentation/107-MEASUREMENTS.md` |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: measure_stage1_latency 命令 + 单测</name>
  <files>server/codegraph/management/commands/measure_stage1_latency.py, server/tests/codegraph/test_measure_stage1_latency.py</files>
  <read_first>
    - server/codegraph/management/commands/measure_repo_index_stats.py（105-02 先例：`add_arguments` 风格、`self.stdout.write` 输出形态、`--json` 模式、结构化日志字段）
    - server/tests/codegraph/test_measure_repo_index_stats.py（该命令的测试范式：`call_command` + `StringIO` 捕获 + 断言输出键）
    - server/system/models.py:342-395（`SystemLogEntry` 字段与 `Meta.db_table` / 索引——原始 SQL 里的表名必须取自 model，不得硬编码猜名）
    - server/codegraph/services/repo_router_v2.py:1184-1192（`repo_router_v2_stage1_completed` 事件的实际字段：`model` / `candidate_count` / `parsed_count` / `duration_ms` / `category` / `component`——查询依据）
    - .planning/phases/107-layered-presentation/107-RESEARCH.md §9（数据源两条 + Postgres `percentile_cont` SQL 骨架 + 两个前置条件）
    - .planning/observability/LOGGING-SPEC.md §4.3（分位统计纪律：用 `percentile_cont`，不自研直方图/聚合器）
  </read_first>
  <behavior>
    - 构造 5 条 `SystemLogEntry(event="repo_router_v2_stage1_completed", payload={"duration_ms": v})`，v ∈ {1000, 2000, 3000, 4000, 5000} → 输出 `n == 5` 且 `p50` 落在 [2900, 3100]（SQLite 走 Python 分位，取 3000）
    - 时间窗过滤：一条 `ts` 早于 `--days` 窗口的行不计入 `n`
    - 事件名过滤：一条 `event="other_event"` 的行不计入 `n`
    - payload 缺 `duration_ms` 或值非数值 → 该行被跳过，不抛异常，`n` 不含它
    - 零样本：库中无匹配行 → 退出码 0，输出含 `n=0` 与提示语（不抛 `ZeroDivisionError`）
    - `--json` 模式：stdout 为可 `json.loads` 的单个对象，键含 `n` / `p50_ms` / `p90_ms` / `p99_ms` / `window_days` / `event` / `db_vendor`
    - 输出无敏感串：断言 stdout 不含 `sk-` / `Bearer ` / `AIza`
  </behavior>
  <action>
    新建 `server/codegraph/management/commands/measure_stage1_latency.py`（`BaseCommand`，同步实现即可——纯查询无异步引擎）：

    1. `add_arguments`：`--days`（int，默认 7）、`--event`（str，默认 `"repo_router_v2_stage1_completed"`）、`--json`（store_true）。
    2. 取分位：`from django.db import connection`；`connection.vendor == "postgresql"` → 单条原始 SQL 用 `percentile_cont(0.5|0.9|0.99) WITHIN GROUP (ORDER BY (payload->>'duration_ms')::numeric)` + `count(*)`，**表名取自 `SystemLogEntry._meta.db_table`**（不硬编码），时间窗用参数化 `%s` 绑定（禁字符串拼接）；非 Postgres → ORM `values_list("payload", flat=True)` 拉取后在 Python 侧用 `statistics.quantiles` 或排序索引法算分位（SQLite 无 `percentile_cont`，LOGGING-SPEC 允许 dev 降级）。
    3. 输出：默认人读表格（`window_days` / `event` / `db_vendor` / `n` / `p50_ms` / `p90_ms` / `p99_ms`）；`--json` 输出单对象。**输出字段只允许聚合量**——不得回显任何 payload 原文、异常文本或凭证（T-107-02）。
    4. 观测：命令结束打一条 `logger.info("stage1_latency_measured", category="sampling", component="repo_router_v2", window_days=..., sample_count=..., p50_ms=..., p90_ms=..., p99_ms=..., db_vendor=..., initiated_by_user_id="system")`——命令由运维/scheduler 手动执行，无触发用户按规范记 `system`；整个观测块 `try/except: pass` 包裹（best-effort，绝不反噬命令退出码）。
    5. 零样本与异常：无匹配行 → 输出 `n=0` + 一行提示「无样本：确认 `category=sampling` 采样配置与 `repo_router_v2` 组件日志级别（该事件为 logger.info，级别调到 WARNING 即无行），或缓存命中率过高（命中路径不发 LLM 调用也不打该事件）」；任何查询异常记 warning 后以非零退出码结束（命令是排障工具，失败要可见）。

    新建 `server/tests/codegraph/test_measure_stage1_latency.py`：按 `<behavior>` 逐条写用例（`pytest.mark.django_db` + `call_command(..., stdout=StringIO())`）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/codegraph/test_measure_stage1_latency.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd server && uv run pytest tests/codegraph/test_measure_stage1_latency.py -x -q` 全绿且覆盖 `<behavior>` 全部 7 条
    - 表名不硬编码：`test "$(rg -c 'system_log_entry' server/codegraph/management/commands/measure_stage1_latency.py || echo 0)" = "0"`（表名必须来自 `_meta.db_table`）
    - SQL 参数化：`test "$(rg -c 'f\"SELECT|% \(|\.format\(' server/codegraph/management/commands/measure_stage1_latency.py || echo 0)" = "0"`（无 f-string / % / format 拼 SQL）
    - 手动跑一次不炸（本地 SQLite，零样本路径）：`cd server && uv run python manage.py measure_stage1_latency --days 7 --json` 退出码 0 且输出可 `json.loads`
    - `cd server && uv run ruff check codegraph/management/commands/measure_stage1_latency.py tests/codegraph/test_measure_stage1_latency.py`
  </acceptance_criteria>
  <done>Stage 1 延迟分位在任意环境可一条命令复测，口径与运维大盘一致（`percentile_cont`）。</done>
</task>

<task type="auto">
  <name>Task 2: 107-MEASUREMENTS.md（O-6 结论 + 三个参数取值依据与局限）</name>
  <files>.planning/phases/107-layered-presentation/107-MEASUREMENTS.md</files>
  <read_first>
    - .planning/phases/106-multi-signal-scoring/106-MEASUREMENTS.md（数据环境标注纪律的既有形态：环境 / 时间窗 / 样本量 / 是否含缓存 / deferred 标注方式——本文档逐条沿用）
    - .planning/phases/107-layered-presentation/107-RESEARCH.md §9（O-6 数据源与两个前置条件）与 §10（delta 离线实测表：gk-008 0.1771 / gk-009 0.2614）
    - .planning/phases/107-layered-presentation/107-CONTEXT.md（D-7 α 锁定值与局限的原文表述；delta 上界结论）
    - .planning/phases/107-layered-presentation/107-01-PLAN.md（assumptions 段：A5 per-call 不下调的理由——文档需与其一致，不得两处说法冲突）
  </read_first>
  <action>
    新建 `.planning/phases/107-layered-presentation/107-MEASUREMENTS.md`，章节固定为五节（中文正文，专有名词/键名保留英文）：

    1. **数据环境标注**：表格列出「环境 / 数据库 / 时间窗 / 样本量 n / 采样率 / 是否含缓存命中」；本地与 CI 一律标 `n=0（无生产数据）`，生产行标 **deferred** 并写明复测命令 `python manage.py measure_stage1_latency --days 7 --json`（Postgres 下走 `percentile_cont`）。
    2. **O-6：Stage 1 延迟分布**：写清口径——数据源是 `SystemLogEntry.payload.duration_ms`（事件 `repo_router_v2_stage1_completed`），**测的是缓存未命中时的上游真实延迟**，不是用户感知延迟（缓存命中路径不打该事件）；已知参考值只引用 CONTEXT/研究已记录的观测区间「34–71s」并明确标注其来源为既有生产观测而非本次实测；p50/p90/p99 三格留 `deferred`。
    3. **设计取舍（压不下来时的主要收益来源）**：如实写明——若生产分位确认压不到可接受范围，本 phase 的收益主要来自「Stage 1 输入哈希缓存（命中即零上游调用）+ 快照回放（离线零网络复现）+ 总预算硬上界（超出即降级继续，用户不再无限等待）」，而非上游延迟本身的下降。
    4. **三个数值参数的取值依据与局限**（逐个一小节）：
       - `REPO_ROUTER_GROUP_DELTA = 0.15`：依据 = golden cross_group 两条样本离线实测（gk-008 差值 0.1771 / gk-009 0.2614，均触发置顶）；**局限 = 可用上界受 gk-008 约束（> 0.1771 即 gk-008 退回「正确仓被压在下面」）**，余量仅 +0.0271，组织内跨组协作频率变化时需重新校准；上界语义已由 107-01 Task 3 的测试锁定。
       - `REPO_ROUTER_STAGE1_ALPHA = 0.35`（D-7）：依据 = ROUTING-RANKING §1.3c 的锁定初值；**局限 = 未经数据校准，因离线 harness（`score_case` / `evaluate_cases`）结构上不跑 Stage 1（α 恒为 0），golden set 无法用于扫 α**；如需校准只能靠生产 A/B 或人工抽检；明确写下「不得为了校准 α 把凸组合塞进离线 harness——会污染 `phase106-v2` baseline」。
       - `REPO_ROUTER_STAGE1_TIMEOUT_SECONDS = 90.0`（per-call，未下调）+ `REPO_ROUTER_STAGE1_TOTAL_BUDGET_SECONDS = 120.0`（新增总预算）：依据 = A5「必须先做 O-6 实测再定 per-call」，O-6 生产数字 deferred → 本 phase 保持 per-call 不动以避免把原本 70–89s 能成功的调用变成用户可见降级；总预算 120 相对今日行为零回归（重试只在快速失败场景生效）。回填 O-6 数字后的下调建议区间（研究给出 40–45s 可切长尾）作为**待办**记录，不在本 phase 生效。
    5. **待回填清单（UAT 交接）**：逐条列出需要在有生产数据环境完成的动作与对应命令，标注「记入 107-UAT」。

    文档中**不得出现任何编造的分位数字**；未实测处一律写 `deferred` 并给复测方式。
  </action>
  <verify>
    <automated>test -f .planning/phases/107-layered-presentation/107-MEASUREMENTS.md && test "$(rg -c 'deferred' .planning/phases/107-layered-presentation/107-MEASUREMENTS.md)" != "0" && test "$(rg -c 'measure_stage1_latency' .planning/phases/107-layered-presentation/107-MEASUREMENTS.md)" != "0" && test "$(rg -c '0\.1771' .planning/phases/107-layered-presentation/107-MEASUREMENTS.md)" != "0" && test "$(rg -c 'REPO_ROUTER_STAGE1_ALPHA' .planning/phases/107-layered-presentation/107-MEASUREMENTS.md)" != "0"</automated>
  </verify>
  <acceptance_criteria>
    - 文档存在且五节齐备（数据环境标注 / O-6 / 设计取舍 / 三参数依据与局限 / 待回填清单）
    - α 局限如实记录：文档含「离线 harness 结构上不跑 Stage 1」这一因果表述且含 `phase106-v2`（不得污染 baseline 的告警）
    - delta 上界 0.1771 与 gk-008 因果表述在文（automated verify 覆盖字面）
    - per-call 超时未下调的理由与 107-01 assumptions 一致（人工对读；两处不得冲突）
    - 无编造数字：p50/p90/p99 三格为 `deferred`（`test "$(rg -c 'p99' .planning/phases/107-layered-presentation/107-MEASUREMENTS.md)" != "0"` 且该行同时出现 deferred 标注）
  </acceptance_criteria>
  <done>SC-5 后半句成立：O-6 结论与三个数值参数的依据/局限有据可查，生产复测路径明确。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SystemLogEntry.payload → 命令 stdout | 日志 payload 可能含上游错误文本；命令输出会被贴进工单/文档 |
| 命令参数 `--days` / `--event` → SQL | 用户可控字符串进入原始 SQL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-107-02 | Information Disclosure | `measure_stage1_latency` stdout | mitigate | 输出只含聚合量（n / 分位 / 时间窗 / vendor）；测试断言 stdout 不含 `sk-` / `Bearer ` / `AIza`；payload 原文一律不回显 |
| T-107-09 | Tampering | 原始 SQL 拼接 | mitigate | 表名取 `SystemLogEntry._meta.db_table`；时间窗与事件名走 `%s` 参数化绑定；验收断言禁 f-string/`%`/`.format` 拼 SQL |
| T-107-03 | Elevation of Privilege | management command 触发面 | accept | 命令需 shell 权限，无 HTTP 触发端点；纯只读查询，不写任何表 |
| T-107-SC | Tampering | npm/pip/cargo installs | accept | 零新增依赖（`statistics` 为 stdlib） |
</threat_model>

<verification>
- `cd server && uv run pytest tests/codegraph/test_measure_stage1_latency.py -q` 全绿
- `cd server && uv run python manage.py measure_stage1_latency --days 7 --json` 本地零样本路径退出码 0
- `cd server && uv run ruff check codegraph/management/commands/measure_stage1_latency.py tests/codegraph/test_measure_stage1_latency.py`
- `test -f .planning/phases/107-layered-presentation/107-MEASUREMENTS.md`
</verification>

<success_criteria>
- 延迟分位可一条命令在任意环境复测；Postgres 走 `percentile_cont`，SQLite 有明确回退
- 命令输出零敏感串、零样本不抛
- 107-MEASUREMENTS.md 如实记录 O-6 deferred 状态、α 未校准局限（D-7）、delta 上界、per-call 不下调理由
</success_criteria>

<output>
完成后创建 `.planning/phases/107-layered-presentation/107-02-SUMMARY.md`。
</output>
