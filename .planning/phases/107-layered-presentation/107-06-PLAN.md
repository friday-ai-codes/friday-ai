---
phase: 107-layered-presentation
plan: 06
type: execute
wave: 3
depends_on: [107-01, 107-04]
files_modified:
  - server/delivery/management/commands/expire_pending_clarifications.py
  - server/tests/delivery/test_expire_pending_clarifications.py
  - server/agents/management/commands/runapscheduler.py
  - server/services/process_runtime/clarify_adapter.py
  - server/tests/services/test_engine_clarify.py
  - server/tests/services/test_event_taxonomy_alignment.py
autonomous: true
requirements: [RELY-02]
assumptions:
  - "A3：出口不新增 `ConvergenceSessionStatus` 枚举值——`resumed_with_assumptions` / `failed_no_answer` 是语义名。出口 = `transition(session, \"clarified\")`（→ `current_stage=research`, `status=running`）或 `transition(session, \"fail\")`；语义标注写 `stage_state.clarification_exit`。若产品后续要求「超时放行」在看板上与「正常运行」视觉区分，需要新状态或投影层额外字段（`workflows/lifecycle_projection.py`），本 phase 记为已知局限。"
  - "A8：`select_for_update(skip_locked=True)` 在 SQLite 下退化为 no-op（仓内多处已显式记录该事实）。本地/CI 的并发保护实际来自单进程 + `max_instances=1` + `transition` CAS；生产 Postgres 下三层齐备。幂等的**权威保证**是 CAS（`current_stage == from_stage` 条件更新），不是行锁。"
  - "A9：生产已存在停在 `waiting_clarification` 的历史会话（CONTEXT 记录会话 `ccd817d9` 有 2 条），首次上线的 job 会立刻命中并批量推进；其中部分可能已被人工绕道处理过（agent 绕道 `create_coding_plan` 徒手编方案），批量推进可能产生重复产出。故命令必须支持 `--dry-run` 与 `--limit`，首次上线由运维先跑 dry-run 看清影响面（这也是 UAT 的天然验收手段）。"
  - "D-5：chat 单题澄清（`chat.ConversationIntentTrace` + LangGraph interrupt）**只纳入观测**，不纳入出口——LangGraph checkpoint 的恢复语义与 stage graph 完全不同，混做会让改动面不可控。本 plan 只在扫描时统计「超期未答协商卡数」并记一条采样日志。该边界须在 VERIFICATION 中如实标注。"
must_haves:
  truths:
    - "无人应答的澄清有明确超时出口：到期后按配置默认「带未澄清假设继续推进」（`transition(session, \"clarified\")`）或「如实失败并说明原因」（`transition(session, \"fail\")`），会话不会再永久停在 `waiting_clarification`（SC-4 后半句）"
    - "出口幂等：连续跑两次扫描只推进一次、只 emit 一次——复用 `ConvergenceSessionService.transition` 的 CAS（`current_stage == from_stage` 条件更新），并发第二方命中 0 行抛 `ConcurrentTransitionError`，捕获后当 no-op；**不新建任何幂等机制**"
    - "起算时间取 pending 轮的 `Clarification.created_at`（`auto_now_add`，不会被刷新），**不用** `session.updated_at`（任何无关 transition 都会刷新它 → 超时永不到达）"
    - "未到期不动、已答不动、终态（done/failed）不动——三条各有测试"
    - "两条立即出口条件（不等满超时）：该轮 `container_status == 'delivery_failed'`（卡片没送出去，等 24h 无意义）；以及「workflow 已 TIMEOUT 而会话仍 waiting_clarification」（D-4 纵深防御，兜住携带旧 60min `timeout_at` 的存量订阅行）"
    - "「未澄清假设」只写 `stage_state.clarification_exit`（含 `unclarified_points`），不改任何受 DEPTH 冻结的渲染文件（D-6）"
    - "出口后不会再被 clarify policy 立刻重新挂起：policy 读 `stage_state.clarification_exit` 短路（Pitfall 6；`_MAX_CLARIFY_ROUNDS=6` 是第二道兜底）"
    - "定时任务归因：apscheduler job 走 `@_with_scheduler_log_context`（`user_id=\"system\"`），每条出口日志/事件另带被推进会话的 `initiated_by_user_id`（无则 `system`）——「谁的会话被超时放行」可归因"
    - "事务纪律：`select_for_update(skip_locked=True)` 的 `atomic` 块内**只收集**目标，异步引擎重驱在事务外 `asyncio.run`（逐字镜像 `check_timeouts.py`）"
    - "命令支持 `--dry-run`（只报不改）与 `--limit`（默认取 `CLARIFICATION_EXPIRY_SCAN_LIMIT`）；best-effort，单条失败不影响其余"
  artifacts:
    - path: server/delivery/management/commands/expire_pending_clarifications.py
      provides: "过期 pending 澄清扫描 + 出口推进 + 幂等 + dry-run/limit"
      min_lines: 120
    - path: server/tests/delivery/test_expire_pending_clarifications.py
      provides: "RELY-02 出口/幂等/起算时间/边界/立即出口/dry-run 全量覆盖"
    - path: server/agents/management/commands/runapscheduler.py
      contains: "expire_pending_clarifications"
    - path: server/services/process_runtime/clarify_adapter.py
      provides: "policy 读 stage_state.clarification_exit 短路（防出口后立刻重挂）"
  key_links:
    - from: server/delivery/management/commands/expire_pending_clarifications.py
      to: server/delivery/services/convergence_session_service.py
      via: "出口只经 transition 唯一入口（INV-6），CAS 天然幂等；ConcurrentTransitionError 当 no-op"
      pattern: "ConcurrentTransitionError"
    - from: server/agents/management/commands/runapscheduler.py
      to: server/delivery/management/commands/expire_pending_clarifications.py
      via: "call_command 范式注册 IntervalTrigger job（镜像 check_workflow_event_timeouts_job）"
      pattern: "call_command"
    - from: server/services/process_runtime/clarify_adapter.py
      to: server/delivery/management/commands/expire_pending_clarifications.py
      via: "policy 读 stage_state.clarification_exit 短路，消费出口写入的标记"
      pattern: "clarification_exit"
---

<objective>
补上 RELY-02 的时间维度出口：新建 `expire_pending_clarifications` management command（**逐字镜像** `workflows/management/commands/check_timeouts.py` 的两段式范式：`select_for_update(skip_locked=True)` 事务内只收集、异步引擎重驱在事务外 `asyncio.run`），扫描超期 pending 澄清并经 `ConvergenceSessionService.transition` 出口；在 `runapscheduler.py` 注册一个 `IntervalTrigger` job（`call_command` 范式，镜像 `check_workflow_event_timeouts_job`）；并给 clarify policy 加一条 `stage_state.clarification_exit` 短路，防止「出口放行 → 回到 clarify → 又挂起」的循环（Pitfall 6）。

**幂等不新建机制**：`transition` 已用「DB 行 `current_stage == from_stage`」做 CAS 原子更新，天然保证出口只生效一次；扫描侧只需捕获 `ConcurrentTransitionError` 当 no-op。

Purpose: SC-4「无人应答时有明确超时出口，会话不会再永久停在 `waiting_clarification`」——断掉生产事故里 agent 绕道徒手编方案的前提。
Output: 1 个新命令 + 1 个新测试 + 1 个 job 注册 + policy 短路 + taxonomy 豁免删除。
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/107-layered-presentation/107-CONTEXT.md
@.planning/phases/107-layered-presentation/107-RESEARCH.md
@server/workflows/management/commands/check_timeouts.py
@server/delivery/services/convergence_session_service.py
@server/delivery/services/clarification_service.py
@server/services/process_runtime/clarify_adapter.py
</context>

## Artifacts this phase produces（本 plan 新建符号）

| Symbol | Kind | Location |
|--------|------|----------|
| `expire_pending_clarifications`（management command 名） | 命令 | `server/delivery/management/commands/expire_pending_clarifications.py` |
| `--dry-run` / `--limit` / `--session-id` | 命令参数 | 同上 |
| `stage_state["clarification_exit"] = {clarification_id, round_no, action, reason, waited_seconds, unclarified_points, at}` | 会话状态契约（D-6 的唯一落点） | 写入方同上；消费方 `clarify_adapter.py` |
| `clarification_timeout_exit`（structlog 事件名，`category="caller"`, `component="delivery"`） | 事件名 | 同上 |
| `clarification_timeout_exit_noop_concurrent`（structlog 事件名，`category="sampling"`） | 事件名 | 同上 |
| `chat_clarification_unanswered_observed`（structlog 事件名，`category="sampling"`；D-5 只观测不出口） | 事件名 | 同上 |
| `expire_pending_clarifications_job()` | apscheduler job wrapper（`@_with_scheduler_log_context`） | `server/agents/management/commands/runapscheduler.py` |
| job id `"expire_pending_clarifications"` | scheduler job 标识 | 同上 |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: expire_pending_clarifications 命令核心（扫描 + 出口 + 幂等 + stage_state + 事件）</name>
  <files>server/delivery/management/commands/expire_pending_clarifications.py, server/tests/delivery/test_expire_pending_clarifications.py</files>
  <read_first>
    - server/workflows/management/commands/check_timeouts.py:30-80（两段式范式全文：`transaction.atomic()` + `select_for_update(skip_locked=True)` 内只收集、事务外 `asyncio.run` 重驱、逐条 `try/except logger.exception`——本命令逐字镜像）
    - server/delivery/services/convergence_session_service.py:126-230（`transition` 签名与 `stage_state` 参数；`_apply_transition_sync` 的 CAS 与 `ConcurrentTransitionError`）与 :304-330（`_emit_event`）
    - server/delivery/services/clarification_service.py（`ahas_pending(session_id)` 统一谓词——pending 判定必须走它，**不要**自己写 `answered_at__isnull` 或按 `container_status` 过滤：旧行 `container_status` 可能为 NULL）
    - server/delivery/models/convergence_session.py:25-60（`ConvergenceSessionStatus` 6 值、`stage_state` / `initiated_by_user_id` / `current_stage` 字段）
    - server/services/process_runtime/builtin_processes.py:285-315（`_TECHNICAL_PLAN_STAGES["clarify"]` 的 `transitions={"clarified": "research", ...}` 与 `wait_status`——出口事件名依据）
    - server/delivery/services/event_taxonomy.py（107-04 落的 `EVENT_CLARIFICATION_TIMED_OUT` 常量与 payload 契约）
    - .planning/phases/107-layered-presentation/107-RESEARCH.md §Code Examples「澄清超时出口」骨架 + Pattern 2/3 + Pitfall 9（禁在持锁事务内调异步引擎）
  </read_first>
  <behavior>
    - 一个会话 `status=waiting_clarification`、pending 轮 `created_at` 早于 `now - CLARIFICATION_TIMEOUT_HOURS` → 跑命令后 `current_stage == "research"`、`status == "running"`，`stage_state["clarification_exit"]` 含 `action == "resumed_with_assumptions"` / `clarification_id` / `waited_seconds` / `unclarified_points`
    - 同一场景连跑两次 → 只推进一次、`ConvergenceSessionEvent` 中 `clarification.timed_out` 只有 1 行（幂等）
    - `CLARIFICATION_TIMEOUT_EXIT_ACTION="fail"` → `status == "failed"` 且 `error` 含 `clarification_timeout_no_answer`；同样只 emit 一次
    - 并发 CAS 冲突（monkeypatch `transition` 抛 `ConcurrentTransitionError`）→ 命令不抛、记 `clarification_timeout_exit_noop_concurrent`、退出码 0
    - `unclarified_points` 取该 pending 轮的子题（问题文本，经既有脱敏），为 list 且非空时逐条含 `question_id` 与 `question`
    - 出口日志带 `initiated_by_user_id`：会话有值取该值，为空取 `"system"`
    - 单条会话处理抛异常 → 其余会话仍被处理（best-effort），命令退出码 0 且记 `logger.exception`
    - 事务纪律：`transition` 调用不在 `atomic()` 块内（用 monkeypatch 断言调用时 `transaction.get_connection().in_atomic_block is False`）
  </behavior>
  <action>
    新建 `server/delivery/management/commands/expire_pending_clarifications.py`（`BaseCommand`，同步 `handle` + 事务外 `asyncio.run`，逐字镜像 `check_timeouts.py` 结构）：

    1. `add_arguments`：`--dry-run`（store_true）、`--limit`（int，默认 `getattr(settings, "CLARIFICATION_EXPIRY_SCAN_LIMIT", 200)`）、`--session-id`（可选，单会话定向处理，便于 UAT）。
    2. **收集阶段**（`with transaction.atomic():`）：
       ```
       ConvergenceSession.objects.select_for_update(skip_locked=True)
         .filter(status=ConvergenceSessionStatus.WAITING_CLARIFICATION)[:limit]
       ```
       对每个会话在事务内**只做同步读**：取「最早未答轮」（`Clarification.objects.filter(session_id=..., answered_at__isnull=True).order_by("round_no", "created_at").values("id","round_no","created_at","container_status").first()`，与 `plan_research.py:387` 的既有排序范式一致）→ 算 `waited_seconds = now - round.created_at`（**起算时间用轮的 `created_at`，绝不用 `session.updated_at`**，Pitfall 7）→ 判定是否到期 → 到期者收集成 `(session_id, round_meta, reason)` 元组列表。事务内**不调任何异步引擎、不写库**（Pitfall 9）。
    3. **执行阶段**（事务外）：逐条 `asyncio.run(self._aexit_one(...))`：
       - 重新按 id 取 session（事务外新鲜行）；
       - 组 `stage_state = dict(session.stage_state or {})`，写 `stage_state["clarification_exit"] = {"clarification_id", "round_no", "action", "reason", "waited_seconds": round(x,1), "unclarified_points": [...], "at": <ISO>}`；`unclarified_points` 取该轮子题（`ClarificationQuestion` 按 `order`，正文经既有脱敏 helper——需求原文可能含凭证，V8）；
       - `action` 取 `CLARIFICATION_TIMEOUT_EXIT_ACTION`（非法值按 `resume_with_assumptions`）→ `resume_with_assumptions` 调 `transition(session, "clarified", stage_state=stage_state)`；`fail` 调 `transition(session, "fail", error={"stage": "clarify", "reason": "clarification_timeout_no_answer", "clarification_id": ...})`（fail 路径也要把 `stage_state` 落上——若 `transition("fail")` 不接受 `stage_state`，先 `aupdate` 写 `stage_state` 再 transition，顺序写进注释说明为何）；
       - 捕获 `ConcurrentTransitionError` → 记 `clarification_timeout_exit_noop_concurrent`（`category="sampling"`, `component="delivery"`）后 `return`（幂等 no-op：已被并发扫描或真实作答推进）；
       - 成功后 `await svc._emit_event(EVENT_CLARIFICATION_TIMED_OUT, session, {...})`（payload 与 107-04 常量注释的契约一致：`clarification_id` / `round_no` / `exit_action`（`resumed_with_assumptions` | `failed_no_answer`）/ `waited_seconds` / `unclarified_points`）；
       - 记 `logger.info("clarification_timeout_exit", category="caller", component="delivery", session_id=..., clarification_id=..., exit_action=..., waited_seconds=..., initiated_by_user_id=session.initiated_by_user_id or "system")`。
    4. `--dry-run`：只输出将要处理的会话/轮/等待时长，**零写库、零 emit**。
    5. 逐条 `try/except Exception: logger.exception("clarification_timeout_exit_error", session_id=...)`，命令整体退出码 0（best-effort，绝不因单条失败中断 scheduler）。
    6. 收尾输出统计（扫描数 / 出口数 / no-op 数 / 失败数）并记一条汇总日志。

    新建 `server/tests/delivery/test_expire_pending_clarifications.py`：按 `<behavior>` 逐条写用例（`pytest.mark.django_db` + factory/直接建模型 + `override_settings` 控超时与动作 + `call_command`）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/delivery/test_expire_pending_clarifications.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `<behavior>` 8 条全绿；`-k idempotent` 能命中幂等用例
    - 幂等不新建机制：`test "$(rg -c 'ConcurrentTransitionError' server/delivery/management/commands/expire_pending_clarifications.py)" != "0"` 且 `test "$(rg -c 'select_for_update' server/delivery/management/commands/expire_pending_clarifications.py)" != "0"`；命令内无自建锁（`test "$(rg -c 'advisory_lock|Redis|threading.Lock' server/delivery/management/commands/expire_pending_clarifications.py || echo 0)" = "0"`）
    - 起算时间正确（Pitfall 7）：`test "$(rg -c 'updated_at' server/delivery/management/commands/expire_pending_clarifications.py || echo 0)" = "0"`；且有一条测试在刷新 `session.updated_at` 后仍命中出口
    - 状态写入只经 transition（INV-6）：`test "$(rg -n 'ConvergenceSession.objects' -A 3 server/delivery/management/commands/expire_pending_clarifications.py | rg -c '\.update\(.*status|status=' || echo 0)" = "0"`
    - pending 判定不按 container_status 过滤：命令的收集查询用 `answered_at__isnull=True`（不用 `container_status="pending"`）——有一条测试用 `container_status=None` 的旧行断言仍被命中
    - `cd server && uv run ruff check delivery/management/commands/expire_pending_clarifications.py tests/delivery/test_expire_pending_clarifications.py`
  </acceptance_criteria>
  <done>无人应答的澄清有了幂等、可归因、事务安全的出口；会话不再永久停在 `waiting_clarification`。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 边界条件、两条立即出口与运维开关（含 D-5 chat 观测）</name>
  <files>server/delivery/management/commands/expire_pending_clarifications.py, server/tests/delivery/test_expire_pending_clarifications.py</files>
  <read_first>
    - server/delivery/models/clarification.py（`container_status` 取值语义，含 107-04 新增的 `delivery_failed`；「该轮仍算 pending」的 docstring）
    - server/workflows/models/execution.py（`WorkflowEventSubscription` 与 `NodeExecution` / `WorkflowExecution` 的 `status` TIMEOUT 取值——D-4 纵深条件的查询依据；确认会话与 workflow execution 的关联路径：`ConvergenceSession.node_execution_id`）
    - server/chat/models.py（`ConversationIntentTrace` 的 `answered_at` 与 `created_at`——D-5 只统计不出口）
    - .planning/phases/107-layered-presentation/107-CONTEXT.md D-4 与 D-5 原文
    - .planning/phases/107-layered-presentation/107-RESEARCH.md §Runtime State Inventory（存量数据两行：历史卡死会话 + `container_status` 为 NULL 的旧行）
  </read_first>
  <behavior>
    - 未到期（pending 轮 `created_at` 在窗口内）→ 不动（`current_stage` / `status` 不变、零 emit）
    - 已答（`answered_at` 非空 → `ahas_pending` 为 False）→ 不动
    - 终态（`status` ∈ {done, failed}）→ 不在收集范围（`filter` 只取 `waiting_clarification`）
    - 立即出口 ①：pending 轮 `container_status == "delivery_failed"` 且**未到期** → 仍被出口，`stage_state["clarification_exit"]["reason"] == "delivery_failed"`
    - 立即出口 ②：会话关联的 `NodeExecution` / `WorkflowExecution` 已为 `TIMEOUT` 而会话仍 `waiting_clarification` 且**未到期** → 仍被出口，`reason == "workflow_timeout"`（D-4 纵深防御，兜住携带旧 60min `timeout_at` 的存量订阅行）
    - 正常到期 → `reason == "no_answer_timeout"`
    - `--limit 1`：库中 3 个到期会话 → 只处理 1 个
    - `--dry-run`：3 个到期会话 → 零写库、零 emit，stdout 列出 3 条
    - `--session-id <id>`：只处理指定会话
    - D-5 观测：库中有超期未答 `ConversationIntentTrace` → 命令记一条 `chat_clarification_unanswered_observed`（`category="sampling"`, `component="delivery"`, kv 含 `count`），且**不改动**任何 `ConversationIntentTrace` 行、不碰 LangGraph
  </behavior>
  <action>
    1. 到期判定改为三条件取或（在收集阶段计算，注释逐条写明理由）：
       - `waited_seconds >= CLARIFICATION_TIMEOUT_HOURS * 3600` → `reason="no_answer_timeout"`；
       - `round.container_status == "delivery_failed"` → `reason="delivery_failed"`（卡片没送出去，等满 24h 毫无意义；107-04 已保证该标记与「仍算 pending」并存）；
       - 会话关联的 workflow 侧已 TIMEOUT → `reason="workflow_timeout"`（D-4：任何时刻都不得存在「工作流已判超时而会话仍在等」的窗口）。关联路径按 `ConvergenceSession.node_execution_id` → `NodeExecution.status` / 其 `workflow_execution.status`；关联缺失（chat 入口无 node_execution）→ 该条件不成立，不报错。
    2. `stage_state["clarification_exit"]["reason"]` 与事件 payload 同步带上 `reason`（三值受控枚举，便于 Phase 110 时间线区分出口成因）。
    3. `--limit` / `--dry-run` / `--session-id` 三个开关落地（T-107-03/T-107-08：降低误操作面与长事务风险）。
    4. D-5 观测块（命令收尾，独立 `try/except: pass`）：统计 `ConversationIntentTrace.objects.filter(answered_at__isnull=True, created_at__lt=now - timeout).count()` 并记 `chat_clarification_unanswered_observed`；注释明确写：**只观测不出口**——路径 C 走 LangGraph interrupt 恢复语义，与 stage graph 完全不同，出口留给后续 phase（D-5）。
    5. `test_expire_pending_clarifications.py` 补用例覆盖 `<behavior>` 全部 11 条。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/delivery/test_expire_pending_clarifications.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `<behavior>` 11 条全绿；`-k "not_expired or answered or terminal"` 与 `-k "dry_run or limit"` 两个筛选各能命中用例
    - 三条出口成因是受控枚举：测试断言 `reason in {"no_answer_timeout", "delivery_failed", "workflow_timeout"}`
    - dry-run 零副作用：测试断言运行前后 `ConvergenceSession.objects.values_list("status","current_stage")` 与 `ConvergenceSessionEvent.objects.count()` 完全不变
    - D-5 边界：`test "$(rg -c 'resume_clarification_run|langgraph|LangGraph' server/delivery/management/commands/expire_pending_clarifications.py || echo 0)" = "0"`（不碰 chat 单题澄清的恢复路径）
    - `cd server && uv run pytest tests/delivery -q` 全绿
  </acceptance_criteria>
  <done>存量卡死会话与「工作流已超时/卡片未送达」两类矛盾态都有立即出口；运维有 dry-run 与批量上限。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: apscheduler job 注册 + clarify policy 短路 + taxonomy 豁免删除</name>
  <files>server/agents/management/commands/runapscheduler.py, server/services/process_runtime/clarify_adapter.py, server/tests/services/test_engine_clarify.py, server/tests/services/test_event_taxonomy_alignment.py</files>
  <read_first>
    - server/agents/management/commands/runapscheduler.py:20-60（`@_with_scheduler_log_context` 装饰器：`bind_task_context(user_id="system", source="scheduler", component="scheduler")`）与 :750-780（`check_workflow_event_timeouts_job` 的 `call_command` 范式 + `scheduler.add_job(..., IntervalTrigger, max_instances=1, replace_existing=True)` + `job_registered` 日志）与 :700-710（改 trigger 间隔后 `job_state` 残留旧 trigger 的既有坑注释）
    - server/services/process_runtime/clarify_adapter.py:60-92（`default_needs_clarification` 三条判定规则的确切结构——短路必须加在最前，且只读 `stage_state`，不动 prompt/schema）
    - server/services/process_runtime/builtin_processes.py:300-315（`merge` 的 `validation_failed_reclarify` → `clarify` 回退路径——Pitfall 6 的循环成因）
    - server/tests/services/test_engine_clarify.py（clarify policy 与 stage 流转的既有测试结构）
    - server/tests/services/test_event_taxonomy_alignment.py（107-04 若加了 `EVENT_CLARIFICATION_TIMED_OUT` 的待落地豁免，本 task 删除它）
    - .planning/phases/107-layered-presentation/107-RESEARCH.md Pitfall 6（出口后回到 clarify 会再判「需澄清」并无限循环）+ §Don't Hand-Roll「周期任务调度 / 后台任务的用户归因」两行
  </read_first>
  <behavior>
    - `default_needs_clarification(session)` 在 `session.stage_state["clarification_exit"]` 存在时返回 `(False, "", [])`——即使 routing 候选全 low、即使 `decomposition["ambiguous"]` 为真（短路优先级最高）
    - `clarification_exit` 不存在 → 三条既有判定规则行为逐字不变（既有测试零回归）
    - scheduler 注册后存在 id 为 `expire_pending_clarifications` 的 job，`max_instances == 1`，trigger 间隔 == `CLARIFICATION_EXPIRY_CHECK_INTERVAL_SECONDS`
    - job wrapper 内部命令抛异常 → wrapper 吞掉并记日志，scheduler 主循环不受影响
    - taxonomy 双向守护测试在无豁免的情况下绿（`EVENT_CLARIFICATION_TIMED_OUT` 现在有真实 emit 点）
  </behavior>
  <action>
    1. `clarify_adapter.default_needs_clarification` 最前面加短路（**韧性向最小改动**，非 DEPTH 向——不碰 prompt / schema / 产出结构，D-6）：
       ```
       stage_state = session.stage_state if isinstance(session.stage_state, dict) else {}
       if stage_state.get("clarification_exit"):
           return False, "", []
       ```
       注释写明因果（Pitfall 6）：出口把 stage 推到 `research` 后，若经 `merge` 的 `validation_failed_reclarify` 回到 `clarify`，`_h_clarify` 会重跑本 policy——若 routing 仍无 high/medium 候选就会再建一轮新澄清并再次挂起，形成无限循环；`_MAX_CLARIFY_ROUNDS=6` 是第二道兜底但不足（它只在下一次 `clarify()` 被调用时生效）。
    2. `runapscheduler.py` 追加 job（逐字镜像 `check_workflow_event_timeouts_job`）：
       - `@_with_scheduler_log_context def expire_pending_clarifications_job():` → `call_command("expire_pending_clarifications")`，`try/except` 记 `job_error` 不抛（绝不打断 scheduler 主循环）；docstring 写明归因：wrapper 固定 `user_id="system"`（周期任务无触发用户，符合规范），被推进会话的 `initiated_by_user_id` 由命令内逐条携带；
       - `scheduler.add_job(..., trigger=IntervalTrigger(seconds=getattr(settings, "CLARIFICATION_EXPIRY_CHECK_INTERVAL_SECONDS", 600)), id="expire_pending_clarifications", name="Expire pending clarifications past timeout (resume with assumptions / fail)", max_instances=1, replace_existing=True)` + `logger.info("job_registered", job=..., schedule=...)`；
       - 在注册处加注释引用 `:705` 的既有坑：新 job 首次上线无历史行无需清理；**若后续改 trigger 间隔**，需在生产执行 `DELETE FROM django_apscheduler_djangojob WHERE id='expire_pending_clarifications';`。
    3. 若 107-04 在 taxonomy 守护测试里加了 `EVENT_CLARIFICATION_TIMED_OUT` 的待落地豁免 → **本 task 删除该豁免**（emit 点已在 107-06 Task 1 落地），并确认双向断言绿。
    4. `test_engine_clarify.py` 补 policy 短路两条用例（`<behavior>` 前两条）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/test_engine_clarify.py tests/services/test_event_taxonomy_alignment.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `<behavior>` 5 条全绿
    - job 已注册：`test "$(rg -c 'expire_pending_clarifications' server/agents/management/commands/runapscheduler.py)" -ge "3"`（wrapper 定义 / call_command / add_job id）
    - `max_instances=1` 与 `replace_existing=True` 均在该 `add_job` 调用内（人工核对 + 一条测试断言 scheduler job 属性，或按既有 job 的测试方式）
    - 豁免已删除：`test "$(rg -c '107-06' server/tests/services/test_event_taxonomy_alignment.py || echo 0)" = "0"`
    - DEPTH 冻结自检：`cd server && git diff --name-only | rg -q 'clarification_questions\.py|spec_generation\.py|merged_plan\.py|architect_merge_adapter\.py|render\.py|decompose_segments\.py|research_adapter\.py' && exit 1 || true`；`clarify_adapter.py` 的 diff 只含 policy 短路（`git diff -- services/process_runtime/clarify_adapter.py | rg -c '^\+' <= 12`）
    - `cd server && uv run pytest tests/services tests/delivery tests/agents -q` 全绿 + `uv run ruff check agents/management/commands/runapscheduler.py services/process_runtime/clarify_adapter.py`
  </acceptance_criteria>
  <done>出口每 ~10 分钟自动执行且归因清晰；出口后不会被 policy 立刻重新挂起；两个新事件名的守护测试无豁免全绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scheduler → 会话状态写入 | 定时任务可批量推进他人会话 |
| 澄清正文（需求原文）→ `stage_state.unclarified_points` | 需求原文可能含用户粘贴的凭证 |
| 用户「刻意不答」→ 超时放行 | 可被用于绕过 HITL 拿到带假设的方案 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-107-03 | Elevation of Privilege | 超时扫描 job | mitigate | job 无外部触发面（只由 scheduler 调用，**不加任何 HTTP 端点**）；management command 需 shell 权限；`--dry-run` / `--limit` / `--session-id` 降低误操作面 |
| T-107-04 | Tampering / Repudiation | 超时出口绕过 HITL | mitigate | 出口必留痕：`EVENT_CLARIFICATION_TIMED_OUT` + `stage_state.clarification_exit`（含 `unclarified_points` 与 `reason`）+ 日志带 `initiated_by_user_id`；「哪些点未澄清」结构化可查 |
| T-107-08 | Denial of Service | 单次扫描无上界 | mitigate | `--limit`（默认 `CLARIFICATION_EXPIRY_SCAN_LIMIT=200`）+ `select_for_update(skip_locked=True)` 让并发扫描互不阻塞 + `max_instances=1` 防 job 重叠 |
| V8 数据保护 | Information Disclosure | `unclarified_points` 入 `stage_state` / 事件 payload | mitigate | 子题正文经既有脱敏 helper（`redact_secrets_in_text`）后才写入；事件入库另经 `redact_for_ledger` |
| T-107-11 | Denial of Service | 持锁事务内调异步引擎 | mitigate | 逐字镜像 `check_timeouts.py` 两段式（事务内只收集、事务外 `asyncio.run`）；测试断言 `transition` 调用时不在 `atomic_block` 内（Pitfall 9） |
| T-107-SC | Tampering | npm/pip/cargo installs | accept | 零新增依赖（`apscheduler` / `django-apscheduler` 已装且已有 21 个 job 在跑） |
</threat_model>

<verification>
- `cd server && uv run pytest tests/delivery tests/services tests/agents tests/workflows -q` 全绿
- `cd server && uv run ruff check delivery/management/commands/expire_pending_clarifications.py tests/delivery/test_expire_pending_clarifications.py agents/management/commands/runapscheduler.py services/process_runtime/clarify_adapter.py`
- 手动 dry-run 冒烟：`cd server && uv run python manage.py expire_pending_clarifications --dry-run` 退出码 0（空库输出 0 条）
- DEPTH 冻结自检（见 Task 3 acceptance criteria 的 git diff 断言）
</verification>

<success_criteria>
- 会话不会再永久停在 `waiting_clarification`：到期/送达失败/工作流已超时三类情形都有出口
- 出口幂等（复用 `transition` CAS，不新建机制）、事务安全（两段式）、可归因（`initiated_by_user_id`）
- 「未澄清假设」只写 `stage_state.clarification_exit`（D-6），不改任何 DEPTH 冻结文件
- 出口后不会被 clarify policy 立刻重新挂起（Pitfall 6 短路）
- job 每 ~10 分钟运行、`max_instances=1`、失败不打断 scheduler
</success_criteria>

<output>
完成后创建 `.planning/phases/107-layered-presentation/107-06-SUMMARY.md`。
</output>
