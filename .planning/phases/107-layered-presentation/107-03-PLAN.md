---
phase: 107-layered-presentation
plan: 03
type: execute
wave: 2
depends_on: [107-01]
files_modified:
  - server/codegraph/services/repo_router_v2.py
  - server/tests/codegraph/test_repo_router_v2_meta.py
  - server/tests/codegraph/test_repo_router_v2_degraded.py
autonomous: true
requirements: [ROUTE-01, ROUTE-02, RELY-03]
assumptions:
  - "**扁平候选列表顺序不按 block_order 重排**：`RepoRouteResultV2.candidates` 继续按 `score_ranked ?? score` 全局降序（稳定 tie-break `(-round(v,6), repo_id)`），`block_order` 是独立的呈现字段。理由：8 个消费方里存在「candidates[0] 即最佳」的隐含假设（`auto_selected`、adapter 精简 dict、clarify policy），按 block_order 重排扁平列表会让呈现偏好穿透到编排决策——这正是 CONTEXT「组别绝不进分数/决策」要禁的事。分区顺序由前端按 `block_order` 消费。"
  - "**`auto_selected` 只由全局最高分候选驱动**，与 `block_order` 无关：block ranking 是呈现层置顶，不得改变编排是否自动推进（否则组别间接进了决策路径）。"
  - "**分组启用时按组各取 top_k**：`grouping_repository_ids is not None` 时从两组各取 `top_k` 后并集，再按全局分数降序输出（长度 <= 2*top_k）。理由：ROUTE-01 要求「组内各展示 Top-3」，若维持「全局总共 top_k=3」则 global 组常为 0 条，分组上线即无信息量。非分组调用方（`grouping_repository_ids=None`）`top_k` 语义逐字不变。候选数变化的回归守护在 107-07（改两个入口的那个 plan）。"
  - "A7：`RepositoryRoutingTrace.degrade_reason` 走新列（107-08 落迁移），本 plan 只保证 router 侧产出该值。"
must_haves:
  truths:
    - "`route()` 新增与 `repository_ids` 正交的 `grouping_repository_ids` 参数：前者仍是 Qdrant 硬过滤（候选范围），后者只做分组依据，互不影响（D-1 的机制前提）"
    - "候选携带 `group` / `trust` / `cross_group_note` / `score_ranked` 四个新字段且全部有默认值（dataclass additive-safe，位置参数构造的既有测试替身不炸）；`to_dict()` 同步输出这四键"
    - "结果携带 `block_order` 与 `degrade_reason`：有分组上下文时 `block_order` 恒为长度 2（即使某组为空），无上下文时为 `['global']`（UI-SPEC covered 11 的后端契约）"
    - "`score` 与 `breakdown` 一行不改：`Σbreakdown == score` 的两条既有断言（test_repo_router_v2_meta.py:248/:462）继续绿；`score_ranked` 默认 `None`（未重排）"
    - "组别绝不进分数：任一候选的 `score` 与其 `group` 无关——同一候选在「传 grouping_repository_ids」与「不传」两次调用下 `score` 与 `breakdown` 逐字节相同（SC-2 后半句的机制断言）"
    - "`degraded=True` 的三条出口（`v2_stage0_only` 的 LLM 失败/未配/不可解析、`v1_fallback`）都产出 6 值闭集内的 `degrade_reason`；`no_stage0_candidates` / `use_llm_false` 产出空串（非用户可见降级原因）"
    - "上游异常文本经 `redact_secrets_in_text` 脱敏后才进日志与快照 payload——`str(exc)[:200]` 的截断式处理被替换（截断不是脱敏）"
    - "无项目上下文入口（`grouping_repository_ids=None`，对应 MCP / REST / skill_steps）返回全部 `group='global'` 且不报错"
  artifacts:
    - path: server/codegraph/services/repo_router_v2.py
      provides: "候选/结果新字段 + grouping_repository_ids 参数 + 分组与 block_order 接线 + 降级原因分类与脱敏"
      contains: "grouping_repository_ids"
  key_links:
    - from: server/codegraph/services/repo_router_v2.py
      to: server/codegraph/services/repo_router_ranking.py
      via: "annotate_groups / decide_block_order / classify_degrade_reason / clamp_ranking_params 由 router 层读 settings 后参数注入调用"
      pattern: "from codegraph.services.repo_router_ranking import"
    - from: server/codegraph/services/repo_router_v2.py
      to: server/common/logging.py
      via: "上游异常文本入日志/快照前经 redact_secrets_in_text 脱敏"
      pattern: "redact_secrets_in_text"
---

<objective>
把 107-01 的纯函数接进 `RepoRouterV2`：候选与结果扩出分组/信任/跨组说明/旁路排序分四个候选字段与 `block_order` / `degrade_reason` 两个结果字段（全部 additive-safe 默认值）；`route()` 增加与 `repository_ids` **正交**的 `grouping_repository_ids` 参数（D-1 的机制前提——硬过滤语义保留给确实需要限定范围的调用方，分组依据独立承载）；三条降级出口统一产出 6 值闭集的 `degrade_reason` 并把上游异常文本改走 `redact_secrets_in_text`。

**打分口径与 confidence 推导一行不改**：`score` / `breakdown` 保持 Stage 0 六信号口径（D-3）；`_deterministic_confidence` 继续吃 Stage 0 分数列表，绝不改吃 `score_ranked`（否则 LLM 重新变成置信度决策者，回退 RELY-04）。

Purpose: ROUTE-01/02 的数据底座 + RELY-03 的事实来源（前端只渲染事实、不推断）。
Output: router 单文件改造 + 两个既有测试文件补用例。
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/107-layered-presentation/107-CONTEXT.md
@.planning/phases/107-layered-presentation/107-RESEARCH.md
@.planning/phases/107-layered-presentation/107-UI-SPEC.md
@.planning/research/ROUTING-RANKING.md
@server/codegraph/services/repo_router_ranking.py
@server/codegraph/services/repo_router_v2.py
</context>

## Artifacts this phase produces（本 plan 新建符号）

| Symbol | Kind | Location |
|--------|------|----------|
| `RepoRouteCandidateV2.group: str = ""` | dataclass 字段（`""` = 未标注 → 消费方视为 global） | `server/codegraph/services/repo_router_v2.py` |
| `RepoRouteCandidateV2.trust: str = ""` | dataclass 字段 | 同上 |
| `RepoRouteCandidateV2.cross_group_note: str = ""` | dataclass 字段（后端留痕；前端不渲染后端自由文本） | 同上 |
| `RepoRouteCandidateV2.score_ranked: float \| None = None` | dataclass 字段（旁路排序分，D-3；`None` = 未重排 → 回退 `score`） | 同上 |
| `RepoRouteResultV2.block_order: list[str]`（default_factory=list） | dataclass 字段 | 同上 |
| `RepoRouteResultV2.degrade_reason: str = ""` | dataclass 字段（6 值闭集 ∪ `""`） | 同上 |
| `to_dict()` 新增键 `group` / `trust` / `cross_group_note` / `score_ranked` | 序列化契约（进快照 candidates 节） | 同上 |
| `route(..., grouping_repository_ids: list[str] \| None = None)` | 函数签名扩展（与 `repository_ids` 正交） | 同上 |
| `_ranking_conf()` | 内部 helper：读 `REPO_ROUTER_GROUP_DELTA` / `_ALPHA` / `_RANK_BUDGET_K` 并经 `clamp_ranking_params` clamp | 同上 |
| `_apply_presentation(candidates, *, grouping_repository_ids, delta, top_k)` | 内部 helper：分组标注 + 组内取 top_k + 全局排序 + block_order 计算（三条出口共用） | 同上 |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 候选/结果新字段 + to_dict + 参数读取 clamp</name>
  <files>server/codegraph/services/repo_router_v2.py, server/tests/codegraph/test_repo_router_v2_meta.py</files>
  <read_first>
    - server/codegraph/services/repo_router_v2.py:138-183（`RepoRouteCandidateV2` / `to_dict` / `RepoRouteResultV2` 现行形状——`criticality` 是「旁路展示字段、不进 breakdown」的既成先例，新字段逐字沿用同款注释纪律）
    - server/codegraph/services/repo_router_v2.py:100-136（`_stage1_conf` / `_conf_thresholds` 两个 settings 读取 helper 的既成范式：调用时读取、`getattr(settings, key, default)`——`_ranking_conf` 同款）
    - server/codegraph/services/repo_router_ranking.py（107-01 产出：`clamp_ranking_params` 签名与默认值语义）
    - server/tests/codegraph/test_repo_router_v2_meta.py:240-260 与 :455-470（两条 `Σbreakdown == score` 断言——本 plan 必须让它们继续绿）
    - .planning/phases/107-layered-presentation/107-RESEARCH.md §2 兼容性约束（8 个消费方全部按具名字段读取，dataclass 新字段必须有 default）
  </read_first>
  <behavior>
    - `RepoRouteCandidateV2("r1", "n1", 0.5, "high", "why")`（位置参数，仅 5 个必填）仍可构造 → 新字段取默认值（既有测试替身不炸）
    - `to_dict()` 输出键集合 == 既有 9 键 ∪ {`group`, `trust`, `cross_group_note`, `score_ranked`}
    - `to_dict()` 的 `score_ranked` 为 `None` 时输出 `None`（不输出 0.0——`None` 与 0.0 语义不同：前者「未重排」、后者「重排后分数为 0」）
    - `RepoRouteResultV2(candidates=[], router_version="v2", auto_selected=False)` 仍可构造 → `block_order == []`、`degrade_reason == ""`
    - `_ranking_conf()` 在 settings 给出非法值（delta=-1 / alpha=2 / k=-3）时返回 clamp 后的合法三元组且不抛
  </behavior>
  <action>
    1. `RepoRouteCandidateV2` 追加四字段（全部带默认值，紧跟 `criticality` 之后，注释写明纪律）：
       - `group: str = ""`（`""` = 未标注；消费方按 global 处理）
       - `trust: str = ""`
       - `cross_group_note: str = ""`（**后端留痕/排障用**；前端一律用前端常量渲染文案，不渲染本字段，T-107-06）
       - `score_ranked: float | None = None` —— 注释必须写明 D-3 硬约束：**这是旁路排序分，绝不覆盖 `score`**；`α·S_llm` 不是任何信号的贡献，塞进 `breakdown` 会让「分数分解」变成假的并打断 `Σbreakdown == score` 的两条既有断言与前端 1e-6 容差校验（ROUTE-07 承诺）。
    2. `to_dict()` 追加对应四键：`score_ranked` 为 `None` 时原样输出 `None`，非 `None` 时 `round(v, 4)`（与 `score` 同精度）；其余三键原样字符串。
    3. `RepoRouteResultV2` 追加 `block_order: list[str] = field(default_factory=list)` 与 `degrade_reason: str = ""`，注释写明：`block_order` 有分组上下文时**恒长度 2**（即使某组为空）、无上下文时 `["global"]`，这是前端判定「是否启用分组呈现」的唯一依据（UI-SPEC covered 11）；`degrade_reason` 为 6 值闭集 ∪ `""`。
    4. 新增 `_ranking_conf() -> tuple[float, float, int]`：读 `REPO_ROUTER_GROUP_DELTA` / `REPO_ROUTER_STAGE1_ALPHA` / `REPO_ROUTER_STAGE1_RANK_BUDGET_K`（`getattr(settings, ..., default)`，调用时读取以保持「改配置即生效」），交给 `clamp_ranking_params` clamp 后返回（T-107-05）。
    5. `test_repo_router_v2_meta.py` 补用例覆盖 `<behavior>` 五条（尤其位置参数构造与 `to_dict` 键集合）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/codegraph/test_repo_router_v2_meta.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd server && uv run pytest tests/codegraph/test_repo_router_v2_meta.py -q` 全绿，含两条既有 `Σbreakdown == score` 断言（fsum 1e-9）
    - `score` / `breakdown` 计算逻辑零改动：`test "$(rg -c 'score_ranked' server/codegraph/services/repo_router_scoring.py || echo 0)" = "0"`（打分核心完全不知道 `score_ranked` 的存在）
    - `to_dict` 键集合断言进测试（不是人工核对）
    - `cd server && uv run ruff check codegraph/services/repo_router_v2.py`
  </acceptance_criteria>
  <done>字段契约就位且 additive-safe，`Σbreakdown == score` 不变量未受影响。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: grouping_repository_ids 参数 + 分组标注/block_order 接线（三条出口共用）</name>
  <files>server/codegraph/services/repo_router_v2.py, server/tests/codegraph/test_repo_router_v2_meta.py</files>
  <read_first>
    - server/codegraph/services/repo_router_v2.py:188-360（`route()` 全貌：三条出口 `_fallback_v1`(215) / `_stage0_only_result`(294,307,336) / 正常 v2(350)——四个 return 点都要带上呈现字段）
    - server/codegraph/services/repo_router_v2.py:800-900（`_stage0_only_result` 与 `_build_snapshot` 签名：`stage1_meta` 与 `snapshot_extras` 的传参风格，新参数同款透传）
    - server/codegraph/services/repo_router_v2.py:1280-1310（`_fallback_v1`：v1 回落路径，也需带 block_order/degrade_reason）
    - server/codegraph/services/repo_router_ranking.py（`annotate_groups` / `decide_block_order` / 常量 `GROUP_*` / `TRUST_*` / `CROSS_GROUP_NOTE`）
    - .planning/research/ROUTING-RANKING.md §5.1（组别只进独立字段，严禁任何 in-domain boost 或 group-conditional 偏移）与 §6.2 第 4 条（稳定 tie-breaking：先 `round(score, 6)` 再比，第二键用 `repository_id`）
  </read_first>
  <behavior>
    - `route(q, grouping_repository_ids=None)` → 全部候选 `group == "global"`、`trust == "needs_confirmation"`、结果 `block_order == ["global"]`，不抛
    - `route(q, grouping_repository_ids=["a"])`，候选含 a/b → a 为 `in_project`/`trusted`、b 为 `global`/`needs_confirmation`；`block_order` 长度 == 2
    - 某组为空（`grouping_repository_ids=["zzz"]`，无候选命中）→ `block_order` 仍为长度 2 且首元素为 `"global"`
    - 迟滞：构造 global 首位比 in_project 首位高 `delta+0.01` → `block_order[0] == "global"`；高 `delta-0.01` → `block_order[0] == "in_project"`
    - 机制断言（SC-2）：同一 query 与同一替身命中，分别以 `grouping_repository_ids=None` 与 `=["a"]` 调用 → 两次结果中同一 `repo_id` 的 `score` 与 `breakdown` 逐键相等（组别零分数影响）
    - 分组启用时按组各取 `top_k`：两组各有 5 个候选、`top_k=3` → 结果候选数 == 6，且 `candidates` 按 `score_ranked ?? score` 全局降序
    - `auto_selected` 与 `block_order` 无关：构造「global 组置顶且其首位 confidence=medium，而全局最高分候选在 in_project 且 confidence=high」→ `auto_selected is True`
    - 扁平列表 `candidates[0]` 恒为全局最高 `score_ranked ?? score` 者（不因 block_order 变化）
  </behavior>
  <action>
    1. `route()` 签名追加 `grouping_repository_ids: list[str] | None = None`（keyword-only，放在 `repository_ids` 之后）；docstring 明确两者**正交**：`repository_ids` 仍是 Qdrant 硬过滤（候选范围），`grouping_repository_ids` 只做 `group` 标注依据、**不参与任何过滤或打分**；`None` = 调用方无项目上下文 → 全部 `global` 并跳过分组呈现（不报错，CONTEXT 明确允许）。
    2. 新增 `_apply_presentation(candidates, *, grouping_repository_ids, delta, top_k) -> tuple[list[RepoRouteCandidateV2], list[str]]`：
       - `project_repo_ids = frozenset(str(r) for r in grouping_repository_ids) if grouping_repository_ids is not None else None`；
       - 调 `annotate_groups` 写回每个候选的 `group` / `trust`；`group == GROUP_GLOBAL` 且**有分组上下文时**写 `cross_group_note = CROSS_GROUP_NOTE`（无上下文时不写——此时「跨组」无意义，标了反而误导，UI-SPEC backstop 3）；
       - 有上下文时按组各取 `top_k`（组内先按 `(-round(score_ranked ?? score, 6), repo_id)` 排序再截断），并集后再按同一 key 全局排序；无上下文时行为逐字不变（`[:top_k]`）；
       - `block_order = decide_block_order(in_project_top, global_top, delta=delta, has_project_context=(project_repo_ids is not None))`，两组首位取各组排序后第一个的 `score_ranked ?? score`，组空传 `None`。
       - **严禁**在此函数内对 `score` / `breakdown` 做任何写操作（注释写明：ROUTING-RANKING §5.1 的 in-domain boost 禁令）。
    3. 四个 return 点接线：`route()` 正常 v2 出口、两处 `_stage0_only_result`、`_fallback_v1`——全部先过 `_apply_presentation` 再构造结果，并把 `block_order` 填进 `RepoRouteResultV2`。`_stage0_only_result` 与 `_fallback_v1` 需要新增 `grouping_repository_ids` / `delta` 透传参数（带默认值，保持既有直调方与替身可用）。
    4. `auto_selected` 计算点保持读**扁平列表首位**（即全局最高分候选），并在旁边加注释：block ranking 是呈现层置顶，**不得**改变编排是否自动推进（组别不进决策路径）。
    5. `_build_snapshot` 的 candidates 节自动带上 `to_dict()` 新键（无需额外改动，验证一下）；快照另加 `block_order` 一键便于回放比对。
    6. `test_repo_router_v2_meta.py` 补用例覆盖 `<behavior>` 全部 8 条，其中「机制断言（SC-2）」与「auto_selected 与 block_order 无关」两条必须独立命名便于门禁定位。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/codegraph/test_repo_router_v2_meta.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `<behavior>` 8 条全部有对应用例且全绿
    - 无分数偏移：`test "$(rg -c 'score \+=|score \* 1\.|boost' server/codegraph/services/repo_router_v2.py || echo 0)" = "0"`
    - `confidence` 推导未改吃 `score_ranked`：`_deterministic_confidence` 的入参构造行仍取 `stage0_candidates` 的 `score`（人工核对 + 一条断言：降级路径与正常路径同一 Stage 0 分数列表下 confidence 相同）
    - 无项目上下文回归：`cd server && uv run pytest tests/codegraph/test_repo_router_v2_meta.py -x -q -k no_project_context` 有用例且绿
    - `cd server && uv run pytest tests/codegraph tests/services/test_repo_router_adapter.py tests/agents/test_repository_relevance_tool.py -q` 全绿（8 消费方零改动仍绿——本 plan 未改任何入口，候选构成不变）
  </acceptance_criteria>
  <done>分组与置顶成为 router 的输出事实；组别对分数与自动推进零影响，可机制断言。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 降级原因分类接线 + 异常文本脱敏</name>
  <files>server/codegraph/services/repo_router_v2.py, server/tests/codegraph/test_repo_router_v2_degraded.py</files>
  <read_first>
    - server/codegraph/services/repo_router_v2.py:290-345（`route()` 的 `except Exception as exc` 分支：现行 `error=str(exc)[:200]` + `skipped_reason=f"stage1_failed:{type(exc).__name__}"`）
    - server/codegraph/services/repo_router_v2.py:1015-1060 与 :1200-1265（`provider_missing` / `no_model_configured` / `unparsable_llm_output` / `no_valid_candidates_in_llm_output` 四处 `skipped_reason` 产出点）
    - server/codegraph/services/repo_router_v2.py:1290-1305（`_fallback_v1` 的 `skipped_reason="v1_fallback"`）
    - server/common/logging.py:362-370（`redact_secrets_in_text` 的覆盖范围：`sk-*` / `sk-ant-*` / `AIza*` / `Bearer *` / PEM）
    - server/tests/codegraph/test_repo_router_v2_degraded.py（既有降级用例结构：替身注入失败 → 断言 `degraded` / `router_version` / `skipped_reason`）
    - .planning/phases/107-layered-presentation/107-RESEARCH.md §6（映射表全量）与 Pitfall 10（不得把上游 400 body 原文透给用户）
  </read_first>
  <behavior>
    - Stage 1 抛 `asyncio.TimeoutError` → `degraded is True`、`router_version == "v2_stage0_only"`、`degrade_reason == "timeout"`
    - Stage 1 抛名为 `APIConnectionError` 的异常 → `degrade_reason == "upstream_error"`
    - provider 未解析 / 无 model 名 → `degrade_reason == "provider_missing"`
    - LLM 输出不可解析 / 无合法候选 → `degrade_reason == "unparsable"`
    - v1 回落 → `router_version == "v1_fallback"`、`degrade_reason == "no_node_index"`
    - `use_llm=False` 与 Stage 0 零候选 → `degrade_reason == ""`（`degraded` 仍为 True，但无用户可见原因行）
    - 脱敏：Stage 1 抛异常且异常消息含 `sk-ant-xxxxxxxx` → 结果快照 stage1 节与 warning 日志中均不含 `sk-ant-`（断言 `"sk-ant-" not in json.dumps(result.snapshot)`）
    - `degrade_reason` 恒 ∈ `DEGRADE_REASONS | {""}`（任意异常类型下）
  </behavior>
  <action>
    1. 四个 `skipped_reason` 产出点与 `_fallback_v1` 保持原 `skipped_reason` 字符串不变（快照/排障口径不破），另在结果构造处调 `classify_degrade_reason(skipped_reason, exc_type_name=...)` 填 `RepoRouteResultV2.degrade_reason`。`_stage0_only_result` 需要接收 `exc_type_name`（可选参数，默认 `None`）以区分 `timeout` / `upstream_error`。
    2. `route()` 的 `except Exception as exc` 分支：
       - 日志 `error=` 改为 `redact_secrets_in_text(str(exc))`（保留长度截断也可，但**脱敏必须在截断之前**——截断不是脱敏）；
       - `stage1_meta` 追加 `error_redacted`（脱敏后文本，供排障下钻）与 `degrade_reason`（枚举）两键；**不得**把原始 `str(exc)` 放进任何会回前端的结构。
    3. 快照：`_build_snapshot` 的 stage1 节带上 `degrade_reason` 与 `error_redacted`（后者已脱敏；入库整体仍走既有 `redact_for_ledger`，双保险）。
    4. `test_repo_router_v2_degraded.py` 补用例覆盖 `<behavior>` 全部 8 条，其中脱敏一条与「枚举闭集」一条各自独立命名（`-k redact` / `-k degrade_reason` 可定位，与 107-VALIDATION 的命令口径一致）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/codegraph/test_repo_router_v2_degraded.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `<behavior>` 8 条全绿；`-k redact` 与 `-k degrade_reason` 两个筛选各自能命中用例
    - 截断式处理被替换：`test "$(rg -c 'str\(exc\)\[:200\]' server/codegraph/services/repo_router_v2.py || echo 0)" = "0"`
    - `redact_secrets_in_text` 已被引用：`test "$(rg -c 'redact_secrets_in_text' server/codegraph/services/repo_router_v2.py)" != "0"`
    - 闭集守护：测试内断言 `result.degrade_reason in DEGRADE_REASONS | {""}`（对 5 种注入异常类型循环）
    - `cd server && uv run pytest tests/codegraph -q` 全绿 + `uv run ruff check codegraph/services/repo_router_v2.py tests/codegraph/test_repo_router_v2_degraded.py`
  </acceptance_criteria>
  <done>降级原因成为受控闭集事实，原始异常文本不再有裸露路径（RELY-03 的后端事实齐备）。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 上游 LLM 网关 → router 结果 → 前端 | 上游异常 body 可能含请求回显（prompt 片段 / header 残留） |
| 调用方传入的 `grouping_repository_ids` → 分组标注 | 分组信息暴露「项目外有哪些仓更匹配」 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-107-02 | Information Disclosure | `route()` except 分支 / 快照 stage1 节 | mitigate | `redact_secrets_in_text(str(exc))` 后才入日志/快照；回前端只走 6 值枚举 `degrade_reason`；测试注入 `sk-ant-` 断言输出无明文 |
| T-107-01 | Information Disclosure | 分组标注暴露项目外仓名 | accept | 本 plan **不改任何候选范围**（`repository_ids` 语义与既有调用方逐字不变）→ 可见性面零变化；放开硬过滤的风险确认与前提记录归 107-07（改入口的那个 plan） |
| T-107-05 | Tampering | `_ranking_conf` 读取的 delta/α/K | mitigate | 经 `clamp_ranking_params` clamp 且非法值回退默认、绝不抛（fail-safe 照 `repo_router_config` 范式） |
| T-107-06 | Tampering | `cross_group_note` 自由文本 | mitigate | 后端字段仅留痕；前端渲染一律用前端常量（契约写进字段注释与 UI-SPEC），后端自由文本不进 DOM |
| T-107-SC | Tampering | npm/pip/cargo installs | accept | 零新增依赖 |
</threat_model>

<verification>
- `cd server && uv run pytest tests/codegraph -q` 全绿（含 golden 门禁 + 回放 + meta/degraded 两个文件）
- `cd server && uv run pytest tests/services/test_repo_router_adapter.py tests/agents/test_repository_relevance_tool.py tests/initiatives/test_repo_association_service.py -q` 全绿（消费方零改动回归）
- `cd server && uv run ruff check codegraph/services/repo_router_v2.py tests/codegraph/test_repo_router_v2_meta.py tests/codegraph/test_repo_router_v2_degraded.py`
- `test "$(rg -c 'score \+=|boost' server/codegraph/services/repo_router_v2.py || echo 0)" = "0"`
</verification>

<success_criteria>
- `grouping_repository_ids` 与 `repository_ids` 正交且默认不改变任何既有调用方行为
- 分组/信任/跨组说明/`score_ranked`/`block_order`/`degrade_reason` 六个新字段全部 additive-safe
- `Σbreakdown == score` 两条既有断言继续绿；组别对 `score` / `breakdown` / `auto_selected` 零影响（各有机制断言）
- 降级原因是 6 值闭集；上游异常文本无裸露路径
</success_criteria>

<output>
完成后创建 `.planning/phases/107-layered-presentation/107-03-SUMMARY.md`。
</output>
