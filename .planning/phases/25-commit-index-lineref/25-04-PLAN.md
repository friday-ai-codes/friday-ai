---
phase: 25-commit-index-lineref
plan: 04
type: execute
wave: 2
depends_on: ["25-01", "25-03"]
files_modified:
  - server/services/indexer.py
  - server/tests/services/test_commit_index_integration.py
autonomous: true
requirements: [IDX-01]
must_haves:
  truths:
    - "全量与增量索引完成后（删除临时克隆前）触发 commit 历史索引"
    - "commit 索引失败/缺供应商绝不阻断既有索引 success 终态（best-effort）"
    - "建若干含特定 message/author 的 commit，索引后经 search_rag 用关键字/author 召回到对应 commit 文档"
    - "被排除文件的变更不出现在召回的 commit 文档摘要中"
    - "二次索引同 HEAD 不新增 commit 文档（增量边界正确）"
  artifacts:
    - path: "server/services/indexer.py"
      provides: "clone_and_index_repository 内 commit 索引 best-effort 派发挂接"
      contains: "index_commits"
    - path: "server/tests/services/test_commit_index_integration.py"
      provides: "search_rag 召回 + 排除 + 增量端到端守护测试"
  key_links:
    - from: "server/services/indexer.py::clone_and_index_repository"
      to: "server/services/commit_index.py::index_commits"
      via: "base 索引完成、rmtree 之前 best-effort 调用"
      pattern: "index_commits"
    - from: "commit 文档（kind=commit, 主 collection）"
      to: "server/services/retrieval/rag_search.py::search_rag"
      via: "既有 chokepoint 自然召回（合成 file_path 不被排除）"
      pattern: "search_rag"
---

<objective>
把 commit 历史索引（25-03 的 `index_commits`）挂接进既有索引流程，并端到端验证经 `search_rag` 可召回（IDX-01 闭环）。

Purpose: 全量/增量索引完成后自动摄取 commit 历史，commit 文档落主 collection 后经既有 `search_rag` chokepoint 自然召回（无需改检索侧），且同受 Phase 22 排除约束。
Output: `clone_and_index_repository` 内的 best-effort 派发挂接 + search_rag 召回/排除/增量端到端守护测试。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-commit-index-lineref/25-CONTEXT.md
@.planning/phases/25-commit-index-lineref/25-03-PLAN.md
</context>

<artifacts_this_phase_produces>
本 plan 产出：
- `clone_and_index_repository` 内的 commit 索引派发：紧随既有 `_run_sensitive_detection` 之后、`return index_result` 之前（删除 temp_dir 之前，确保读真实克隆），仅 base 索引路径（`if not branch:`）。
- best-effort 包裹 helper `_run_commit_index(repository_id, repo_path)`：整段 try/except 吞异常 warning `commit_index_dispatch_failed`，绝不阻断索引 success（对齐 24-02 `_run_sensitive_detection` fail-safe 范式，per D-04）。
- 端到端守护测试：search_rag 召回 commit、被排除文件不入摘要、增量只新增。

本 plan **不产出**：search 检索侧改动（commit 文档落主 collection 经既有 search_rag 自然召回；不改 rag_search.py/branch_search.py）。
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: clone_and_index_repository 内 commit 索引 best-effort 派发</name>
  <files>server/services/indexer.py</files>
  <read_first>
    - server/services/indexer.py 行 3346-3372（`_run_sensitive_detection`：best-effort 范式 —— try/except 吞异常 warning、await 完成在 rmtree 之前、只读全局有界）
    - server/services/indexer.py 行 3698-3704（挂接点：`if not branch: await _run_sensitive_detection(repository_id, temp_dir, index_result)` 紧接 `return index_result`，在 finally rmtree 之前；全量与增量均流经此函数）
    - server/services/indexer.py 行 3750-3753（finally `shutil.rmtree(temp_dir)`，commit 索引必须在此之前完成）
    - server/services/commit_index.py（25-03 产出的 `index_commits(repository_id, repo_path)` 签名与返回 dict）
  </read_first>
  <action>
    在 `server/services/indexer.py` 新增模块级 `async def _run_commit_index(repository_id: str, repo_path: str) -> None`，照搬 `_run_sensitive_detection` 的 best-effort 结构：`try: from services.commit_index import index_commits; result = await index_commits(repository_id, repo_path); logger.info("commit_index_completed", ...) except Exception as e: logger.warning("commit_index_dispatch_failed", repository_id=..., error=str(e))`。整段 await（同步完成于 rmtree 之前，复用真实克隆目录，与 sensitive detect 同理由）。在挂接点（行 3701-3702 既有 `if not branch:` 块内，`_run_sensitive_detection` 之后）追加 `await _run_commit_index(repository_id, temp_dir)`。仅 base 索引路径执行（功能分支 overlay 不在范围）。不改既有 sensitive detection、index_result、错误处理与 finally。commit 索引失败绝不影响 `return index_result` 的 success 终态（per D-04/T-24-05 同款契约）。
  </action>
  <verify>
    <automated>cd server && .venv/bin/python -m pytest tests/services/test_commit_index_integration.py -k dispatch -x</automated>
  </verify>
  <done>base 索引（全量+增量）完成后、rmtree 之前触发 index_commits；commit 索引抛异常仅 warning 不冒泡、索引 success 不受损；功能分支不触发。</done>
</task>

<task type="auto">
  <name>Task 2: search_rag 召回 + 排除 + 增量端到端守护测试</name>
  <files>server/tests/services/test_commit_index_integration.py</files>
  <read_first>
    - server/services/retrieval/rag_search.py 全文（chokepoint：BranchAwareSearchService.search → 排除过滤 by file_path → dedup key (repo_id, file_path, chunk_index)；commit 文档合成 file_path `.friday/commits/{sha}` 不被排除，可召回）
    - server/tests/services/test_retrieval_exclusion.py（search_rag 相关测试的 fixture/mock 范式：如何造 repo、mock embedding、断言召回项）
    - server/services/commit_index.py（index_commits 行为，构建本测试的输入 commit）
    - server/tests/services/test_commit_index.py（25-03 的 git 仓库/commit 构造 fixture，复用）
  </read_first>
  <action>
    新建 `server/tests/services/test_commit_index_integration.py`，覆盖 IDX-01 守护断言（per 25-CONTEXT Specific Ideas）：
    (1) 召回：构造含特定 message（如唯一关键字）+ author 的 commit，跑 index_commits 入库后，经 `search_rag(query=<关键字>, repo_ids=[repo_id])` 召回结果含该 commit 文档（payload kind=commit、commit_sha 匹配）；另断言用 author 名作 query 亦可召回。
    (2) 排除：commit 改动含一个被排除文件（如 `.env` 或 `secrets/x.pem`）+ 一个普通文件，索引后召回的 commit 文档 `changed_files`/content 含普通文件、**不含**被排除文件路径。
    (3) 增量：同 HEAD 第二次跑 index_commits 返回 indexed=0（boundary==HEAD 无新 commit）；新增一个 commit 后再跑只新增 1 条。
    (4) dispatch：mock `index_commits`，断言 `_run_commit_index` 在其抛异常时不冒泡（返回 None、仅 warning）。
    embedding/sparse 按既有 retrieval 测试范式 mock（避免真实模型依赖）；如本机无 git 用 25-03 的临时 git 仓库 fixture。标 `pytest.mark.django_db`。
  </action>
  <verify>
    <automated>cd server && .venv/bin/python -m pytest tests/services/test_commit_index_integration.py -x</automated>
  </verify>
  <done>search_rag 用关键字/author 召回 commit 文档；被排除文件不出现在召回摘要；二次同 HEAD 增量 0 条、新增 commit 后 +1；dispatch 失败不冒泡。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 索引流程 → commit 索引派发 | commit 索引失败不得污染既有索引 success 终态 |
| commit 文档 → search_rag 召回面 | commit 文档同受 Phase 22 排除/fail-closed 约束 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-12 | Denial of Service | commit 索引异常/缺供应商阻断主索引 success | mitigate | `_run_commit_index` best-effort try/except 吞异常 warning，不冒泡（对齐 _run_sensitive_detection）；守护测试断言 dispatch 失败不冒泡 |
| T-25-13 | Information Disclosure | commit 文档经 search_rag 泄漏被排除文件内容 | mitigate | 摘要在 25-03 摄取期已剔除被排除文件；本 plan 守护测试断言召回文档不含被排除文件；search_rag 既有 file_path 排除过滤为第二道防线 |
| T-25-14 | Tampering | 增量边界错误致 commit 漏召回/重复召回 | mitigate | 端到端测试断言二次同 HEAD 增量 0 条、新增 +1；确定性 uuid5 point id 去重（25-03） |
| T-25-15 | Tampering | commit 文档挤占代码 chunk dedup key 致代码召回受损 | mitigate | 合成 file_path + chunk_index=0 保证 dedup key 与代码 chunk 不冲突（25-03 T-25-10）；可加断言代码 chunk 与 commit 文档并存召回 |
</threat_model>

<verification>
- `cd server && .venv/bin/python -m pytest tests/services/test_commit_index_integration.py`
- `cd server && .venv/bin/python -m pytest tests/services/test_retrieval_exclusion.py -q`（search_rag 既有排除行为不回归）
- grep 守护：`rg "_run_commit_index|index_commits" server/services/indexer.py`（确认仅 base 路径挂接、best-effort）
- `cd server && .venv/bin/python -m mypy services/indexer.py`
</verification>

<success_criteria>
commit 历史在索引完成后自动摄取、经 search_rag 用关键字/author 召回；被排除文件不泄漏；增量只新增新 commit；commit 索引失败绝不阻断既有索引 success。
</success_criteria>

<output>
Create `.planning/phases/25-commit-index-lineref/25-04-SUMMARY.md` when done.
</output>
