---
phase: 25-commit-index-lineref
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/code_relations/types.py
  - server/services/indexer.py
  - server/tests/services/test_chunk_line_backfill.py
autonomous: true
requirements: [IDX-02]
must_haves:
  truths:
    - "新索引写入的每个 ChunkRegistry row 携带 1-based 闭区间 line_start/line_end"
    - "重切分（content_hash 变化或 chunk 行移动）时 line_start/line_end 被更新而非保留旧值"
    - "历史未回填 row 仍可为 NULL（向后兼容，不强制回填历史）"
  artifacts:
    - path: "server/code_relations/types.py"
      provides: "ChunkRegistryRow 增加 line_start/line_end 键"
      contains: "line_start"
    - path: "server/services/indexer.py"
      provides: "_build_points 透传 chunk.start_line/end_line，_bulk_upsert_registry_atomic 写入 line 字段"
      contains: "line_start"
    - path: "server/tests/services/test_chunk_line_backfill.py"
      provides: "行号回填守护测试"
  key_links:
    - from: "server/services/indexer.py::_build_points"
      to: "server/code_relations/models.py::ChunkRegistry.line_start/line_end"
      via: "ChunkRegistryRow → _bulk_upsert_registry_atomic get_or_create/save"
      pattern: "line_start"
---

<objective>
回填 `ChunkRegistry.line_start` / `line_end`（IDX-02 前半）：索引时把每个 chunk 的源码起止行写入 ChunkRegistry，作为 `file:line → chunk_id` 反查（25-02）的数据地基。

Purpose: `ChunkRegistry.line_start/line_end` 字段已存在（migration `code_relations/0003` + `0004` CheckConstraint `chunkreg_line_range_valid`），但 indexer 当前**只把行号写进 Qdrant payload，未写进 ChunkRegistry**。本 plan 打通这条回填链路。**无需新 migration**（per D-02，字段已就位）。
Output: `ChunkRegistryRow` 携带行号、`_build_points` 透传、`_bulk_upsert_registry_atomic` 落库（create + update 双路径）。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-commit-index-lineref/25-CONTEXT.md
</context>

<artifacts_this_phase_produces>
本 plan 产出：
- `ChunkRegistryRow` TypedDict 新增 `line_start: int | None` / `line_end: int | None` 键。
- `IndexerService._build_points` 在构建 registry_rows 时透传 `chunk.start_line` / `chunk.end_line`。
- `IndexerService._bulk_upsert_registry_atomic` 在 `get_or_create` defaults 与既有 update 分支写入 line 字段。
- 行号回填守护测试。

本 plan **不产出**：migration（line 字段已存在）、find_chunk_at / chunk-at API（25-02）、commit 索引（25-03/25-04）。
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ChunkRegistryRow 携带行号 + _build_points 透传</name>
  <files>server/code_relations/types.py, server/services/indexer.py, server/tests/services/test_chunk_line_backfill.py</files>
  <read_first>
    - server/code_relations/types.py（ChunkRegistryRow TypedDict 全文，与 ChunkRegistry 模型字段一一对应的契约）
    - server/services/code_chunk.py（CodeChunk dataclass：已有 `start_line` / `end_line`，1-based 闭区间，tree-sitter `node.start_point[0] + 1`）
    - server/services/indexer.py 行 3232-3328（`_build_points`：已把 `chunk.start_line` / `chunk.end_line` 写进 Qdrant payload，registry_rows 里缺）
    - server/code_relations/models.py 行 39-110（ChunkRegistry.line_start/line_end 已存在 + CheckConstraint chunkreg_line_range_valid）
  </read_first>
  <behavior>
    - Test: `_build_points` 对一个 start_line=5/end_line=12 的 CodeChunk，返回的 registry_rows[0] 含 line_start=5、line_end=12。
    - Test: embedding 为 None 的 chunk 不入 registry_rows（既有行为不回归），行号不影响该跳过逻辑。
    - Test: ChunkRegistryRow 的键集合恰为既有键 + line_start + line_end（mypy 契约不漂移）。
  </behavior>
  <action>
    在 `server/code_relations/types.py` 的 `ChunkRegistryRow` 增加两键 `line_start: int | None` 与 `line_end: int | None`（紧随 `branch_name`），docstring 注明 1-based 闭区间、可为 None（历史/非 AST 切分回退）。在 `server/services/indexer.py::_build_points` 构建 `registry_rows.append({...})` 处，新增 `"line_start": chunk.start_line` 与 `"line_end": chunk.end_line`（直接取 CodeChunk 既有属性，已是 1-based 闭区间，与同处写入 Qdrant payload 的 `start_line`/`end_line` 同源，保证 payload 与 ChunkRegistry 行号一致）。不改 chunk_index 计数、分支归一化（`_norm_branch`）、embedding=None 跳过等既有逻辑。per D-02。
  </action>
  <verify>
    <automated>cd server && .venv/bin/python -m pytest tests/services/test_chunk_line_backfill.py -k build_points -x</automated>
  </verify>
  <done>ChunkRegistryRow 含 line_start/line_end 键；_build_points 透传 CodeChunk 行号进 registry_rows；mypy 不报键缺失；embedding=None 跳过逻辑不变。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: _bulk_upsert_registry_atomic 落库 line_start/line_end（create + update）</name>
  <files>server/services/indexer.py, server/tests/services/test_chunk_line_backfill.py</files>
  <read_first>
    - server/services/indexer.py 行 3175-3231（`_bulk_upsert_registry_atomic`：`get_or_create(chunk_id=..., defaults={...})` + 既有 content_hash/file_path/chunk_index 变化时 `obj.save(update_fields=[...])`）
    - server/code_relations/models.py 行 86-95（CheckConstraint：line_start/line_end 任一 NULL 或 line_end>=line_start，写入务必满足）
  </read_first>
  <behavior>
    - Test: 新 chunk get_or_create → 落库 row 的 line_start/line_end 等于传入值。
    - Test: 同 chunk_id 二次写入且 content_hash 变化（重切分行号位移：line_start 5→8）→ 落库行号被更新为新值（不保留旧 5/12）。
    - Test: 传入 line_start=None/line_end=None → 落库 NULL，不违反 chunkreg_line_range_valid 约束。
    - Test: 传入 line_end < line_start 的非法行号 → IntegrityError（DB 约束兜底，indexer 不静默落错乱区间）。
  </behavior>
  <action>
    在 `server/services/indexer.py::_bulk_upsert_registry_atomic` 的 `get_or_create` defaults dict 增加 `"line_start": row["line_start"]` 与 `"line_end": row["line_end"]`。在既有 update 分支（当 content_hash 变化或 file_path/chunk_index 漂移时）把 `line_start`/`line_end` 一并赋值并加入 `obj.save(update_fields=[...])` 列表——重切分导致行号位移时必须更新，否则 25-02 反查命中错位。判定是否需要 update 的条件应额外纳入「行号变化」（`obj.line_start != row["line_start"] or obj.line_end != row["line_end"]`），避免仅行号变、hash/路径未变时漏更新。保持单 `transaction.atomic()` + `select_for_update()` 既有强一致语义不变。
  </action>
  <verify>
    <automated>cd server && .venv/bin/python -m pytest tests/services/test_chunk_line_backfill.py -x && cd server && .venv/bin/python -m pytest tests/code_relations/test_chunkregistry_line_fields.py -x</automated>
  </verify>
  <done>create 路径与 update 路径均落 line_start/line_end；行号位移触发 update；None 落 NULL；非法区间被 CheckConstraint 拒绝；既有 content_hash 变化检测与原子语义不回归。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| indexer 切分结果 → ChunkRegistry | tree-sitter / fallback 产出的行号写入 DB，错乱区间会让下游反查命中错位 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | Tampering | `_bulk_upsert_registry_atomic` 写入错乱行号（line_end < line_start） | mitigate | 复用既有 `chunkreg_line_range_valid` CheckConstraint DB 层兜底；守护测试断言非法区间 IntegrityError |
| T-25-02 | Information Disclosure | 行号回填本身不引入新读取面 | accept | 仅写 ChunkRegistry 既有字段；不暴露被排除文件内容（排除在扫描阶段已生效，被排除文件根本不进 chunks，per 22-02） |
| T-25-03 | Tampering | 重切分行号位移未更新 → 反查错位 | mitigate | update 分支显式纳入行号变化判定 + 守护测试覆盖行号位移更新 |
</threat_model>

<verification>
- `cd server && .venv/bin/python -m pytest tests/services/test_chunk_line_backfill.py tests/code_relations/test_chunkregistry_line_fields.py`
- `cd server && .venv/bin/python -m pytest -k "indexer or build_points" -q`（既有索引测试不回归）
- `cd server && .venv/bin/python -m mypy services/indexer.py code_relations/types.py`（TypedDict 键漂移静态拦截）
</verification>

<success_criteria>
新索引写入的 ChunkRegistry row 携带正确的 1-based 闭区间 line_start/line_end；重切分时行号被更新；历史 NULL 兼容；DB 约束拒绝错乱区间；既有索引行为零回归。
</success_criteria>

<output>
Create `.planning/phases/25-commit-index-lineref/25-01-SUMMARY.md` when done.
</output>
