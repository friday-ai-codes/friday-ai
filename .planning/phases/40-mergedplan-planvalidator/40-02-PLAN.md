---
phase: 40-mergedplan-planvalidator
plan: 02
type: execute
wave: 2
depends_on: [40-01]
files_modified:
  - server/services/plan_orchestration/architect_merge_adapter.py
  - server/services/plan_orchestration/engine.py
  - server/services/plan_orchestration/__init__.py
  - server/tests/services/test_architect_merge_adapter.py
  - server/tests/services/test_plan_orchestration_engine_merge.py
  - server/tests/delivery/test_architect_merge_inv6_guard.py
autonomous: true
requirements: [MERGE-01, MERGE-02, MERGE-03]
must_haves:
  truths:
    - "融合通过路径：收齐 valid PartialPlan → 注入 synthesizer 产 MergedPlan → PlanValidator 通过 → 经 TechnicalPlanService.create_from 落 canonical + 置 PlanSession.current_plan_version + ArchitectMerge(passed, merged_plan_version)；engine 转 merged→done"
    - "融合失败路径：PlanValidator 失败 / LLM 合成失败 → ArchitectMerge(failed, report) + 不落 canonical；engine 按 report 回退（默认 clarifying，partial stale/缺 → researching）"
    - "限次回退：ArchitectMerge.attempt 超上限（默认 1 次重试）后融合再失败 → engine 转 failed 终态，不无限循环"
    - "融合段产出 plan.merge.started / plan.merge.completed / plan.validation.failed 事件（§15）"
  artifacts:
    - path: "server/services/plan_orchestration/architect_merge_adapter.py"
      provides: "ArchitectMergeAdapter(MergeProtocol) + 可注入 MergedPlanSynthesizer"
      contains: "class ArchitectMergeAdapter"
    - path: "server/services/plan_orchestration/engine.py"
      provides: "_merge 接线（pass→merged / fail→限次回退 / 超限→fail）"
      contains: "async def _merge"
    - path: "server/tests/delivery/test_architect_merge_inv6_guard.py"
      provides: "ArchitectMerge 落库唯一写入入口守护"
  key_links:
    - from: "server/services/plan_orchestration/architect_merge_adapter.py"
      to: "delivery.services.TechnicalPlanService.create_from"
      via: "canonical 落库唯一入口（INV-6）"
      pattern: "create_from"
    - from: "server/services/plan_orchestration/engine.py"
      to: "delivery.services.PlanSessionService.transition"
      via: "merged / validation_failed_reclarify / validation_failed_reresearch / fail（engine 纯度 transition only）"
      pattern: "transition"
    - from: "server/services/plan_orchestration/architect_merge_adapter.py"
      to: "services.plan_orchestration.plan_validator.validate_plan"
      via: "融合产物跑 PlanValidator（MERGE-02）"
      pattern: "validate_plan"
---

<objective>
实现 reduce 段的**真实融合接线**（MERGE-01）：`ArchitectMergeAdapter(MergeProtocol)` 收齐 session 的 valid `PartialPlan`，调**可注入的 LLM 合成器**产结构化 `MergedPlan`，跑 `PlanValidator`（40-01）；通过 → 经 `TechnicalPlanService.create_from(origin="orchestration")` 落 canonical `PlanVersion` + 置 `PlanSession.current_plan_version` + `ArchitectMerge(passed)`；失败 → `ArchitectMerge(failed, report)` 不落 canonical。`engine._merge` 据结果做 §14 转移：通过 merging→done，失败按报告**限次回退** clarifying/researching，超限落 failed 终态。融合段产 `plan.merge.*` / `plan.validation.failed` 事件（§15）。

Purpose: 串起编排 reduce 段闭环，让一次需求经全流水线产出一份带跨仓依赖的 canonical 主方案（MERGE-01/02/03），同时守 INV-6（canonical 经 service、ArchitectMerge 经融合 service）、INV-2（产物挂 session.work_item，chat null 允许）与 engine 纯度。
Output: `architect_merge_adapter.py`（adapter + synthesizer 协议 + 默认真实 LLM 合成器）；`engine._merge` 接线；mock-synthesizer 单测（pass/fail/限次回退）+ engine 接线测试 + ArchitectMerge INV-6 grep 守护。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/40-mergedplan-planvalidator/40-CONTEXT.md
@.planning/phases/40-mergedplan-planvalidator/40-01-PLAN.md
@.planning/DOMAIN-MODEL.md

# engine + 协议 + §14 转移表 + 事件钩子（接线基底）
@server/services/plan_orchestration/engine.py
@server/services/plan_orchestration/protocols.py
@server/delivery/services/plan_session_service.py

# 既有 adapter 范式（fan-out/事件/单仓隔离/INV-6/async ORM 防裸 lazy-load）
@server/services/plan_orchestration/research_adapter.py
@server/services/plan_orchestration/research_aggregation.py

# canonical 落库唯一入口（INV-6）+ ArchitectMerge 模型 + PlanValidator/MergedPlan（40-01 产出）
@server/delivery/services/technical_plan_service.py
@server/delivery/models/research_task.py

# LLM 解析 + 构造（真实 synthesizer 复用；注意 async 下读 *_id 避免 SynchronousOnlyOperation）
@server/services/provider_config.py
@server/agents/llm_factory.py

# 守护与测试范式
@server/tests/services/test_plan_orchestration_engine.py
@server/tests/delivery/test_research_inv6_guard.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ArchitectMergeAdapter(MergeProtocol) + 可注入 MergedPlanSynthesizer（MERGE-01）</name>
  <files>server/services/plan_orchestration/architect_merge_adapter.py, server/services/plan_orchestration/__init__.py, server/tests/services/test_architect_merge_adapter.py</files>
  <behavior>
    - pass 路径（mock synthesizer 返 PlanValidator-合法的 MergedPlan）：`merge(session)` → 收齐 valid PartialPlan → synthesize → validate_plan 通过 → `TechnicalPlanService.create_from(origin="orchestration", payload={"content": merged}, work_item=<by id or None>)` 落 canonical → 置 `PlanSession.current_plan_version = <新版本 PlanVersion.id>`（经 PlanSessionService）→ 建 `ArchitectMerge(validation_status=passed, merged_plan_version=<PlanVersion.id>, attempt=N)`；返回 `{"validation_status": "passed", "plan_version_id": <id>, "attempt": N}`；emit plan.merge.started + plan.merge.completed。
    - fail 路径（mock synthesizer 返 PlanValidator-不合法 MergedPlan）：不调 create_from、不置 current_plan_version；建 `ArchitectMerge(validation_status=failed, validation_report=<report>, attempt=N)`；返回 `{"validation_status": "failed", "report": {...}, "back_target": "clarifying"|"researching", "attempt": N}`；emit plan.merge.started + plan.validation.failed。
    - LLM 降级路径（synthesizer 抛异常）：捕获 → ArchitectMerge(failed, report={"reason":"synthesis_failed","error":...}) → 返回 failed 结果（**不崩编排**，不向上抛）。
    - INV-2：`session.work_item_id` 为 None（chat）时 create_from(work_item=None) 合法、不报错。
    - back_target 判定：partial 存在 stale/缺失（无 valid partial 或有 stale task）→ "researching"；否则默认 "clarifying"。
  </behavior>
  <action>
  新建 `server/services/plan_orchestration/architect_merge_adapter.py`（zh-CN docstring，说明：架构师 = server 端可注入 LLM 合成，非容器；reduce 单点收敛；INV-6 canonical 经 TechnicalPlanService、ArchitectMerge 经本 adapter 唯一写入）。

  **MergedPlanSynthesizer 协议（可注入）**：定义 `typing.Protocol`（`async def synthesize(self, session, partials: list[dict]) -> dict`，返回 §7 MergedPlan content dict），与 `protocols.py` 的 Skeleton* 范式一致。提供默认 `LLMMergedPlanSynthesizer`：经 `ProviderConfigService.aresolve(...)` + `agents.llm_factory.build_chat_model(...)` 构造 chat model，拼 prompt（注入各 partial 的 `repository_id` / `research_summary` / `api_contracts_exposed` / `dependencies_on_other_repos` / `candidate_files` / `proposed_changes`，要求产 §7 MergedPlan JSON，含 dependency_dag 邻接表 + execution_plan[].dependencies，对齐 40-01 PlanValidator 约定形状），`await model.ainvoke(...)` 后用既有健壮 JSON 解析（参考 `research_aggregation._try_parse_json_struct` 的「取首 { 到末 }」容错思路，**不 eval**）解析为 dict。真实 LLM 失败/解析失败 → 抛异常（由 adapter 捕获降级）。**真实 LLM 路径本 phase 仅构造+单测 mock 覆盖，E2E 真容器/真模型 deferred（对齐 39-04 真实容器 E2E deferred）**。

  **ArchitectMergeAdapter**（满足 `MergeProtocol`）：
  - `__init__(self, *, synthesizer: MergedPlanSynthesizer | None = None, session_service=None, plan_service=None)`；默认 `LLMMergedPlanSynthesizer()` / `PlanSessionService()` / `TechnicalPlanService()`。
  - `async def merge(self, session) -> dict`：
    1. **收齐 valid PartialPlan（async ORM，禁裸 lazy-load）**：`PartialPlan.objects.filter(research_task__session_id=session.id, valid=True)` 取 `.values("content")` 或 async 迭代取 `content`（JSONField，安全）；**绝不**在 async 上下文做 `partial.research_task.xxx` 等同步 lazy-FK（规避 Phase 38 CR-01 / async-lazy-load bug 类——只用 `session_id` 过滤、`.values()` 取标量/JSON）。同时探测是否有 stale/缺 partial（`RepoResearchTask.objects.filter(session_id=..., status=STALE).aexists()` 或 valid partial 数为 0）以定 back_target。
    2. 计算本次 `attempt`：`await ArchitectMerge.objects.filter(session_id=session.id).acount()`（已有融合次数）作为序号。
    3. emit `plan.merge.started`（payload `{"partials": [repo_id...]}`，经 `self.session_service._emit_event`，best-effort try/except 不阻断）。
    4. `try: merged = await self.synthesizer.synthesize(session, partials)` —— 异常 → 走降级失败分支（report `{"reason":"synthesis_failed","error":str(exc)}`，back_target="researching"）。
    5. `report = validate_plan(merged)`（40-01）；`if report["valid"]:` 通过分支，否则失败分支。
    6. **通过分支**：`work_item = await WorkItem.objects.filter(id=session.work_item_id).afirst()`（by id，INV-2 None 合法）；`plan = await self.plan_service.create_from("orchestration", {"content": merged}, work_item=work_item)`（INV-6 唯一入口）；取新版本 id `plan.current_version_id`（async 安全标量）；经 `self.session_service` 把 `session.current_plan_version` 写为该 id（**经 service，不裸写**——见下「current_plan_version 写入」）；`await self._record_merge(session, status=PASSED, merged_plan_version=plan.current_version_id, report=report, attempt=attempt)`；emit `plan.merge.completed` `{"plan_version_id": str(plan.current_version_id)}`；return passed dict。
    7. **失败分支**：`await self._record_merge(session, status=FAILED, merged_plan_version=None, report=report_payload, attempt=attempt)`；emit `plan.validation.failed` `{"reasons": [e["check"] for e in report.get("errors", [])]}`；return failed dict（含 back_target）。
  - **ArchitectMerge 写入（INV-6 融合 service 唯一入口）**：`@sync_to_async _record_merge(...)` 用 `ArchitectMerge.objects.create(...)` 落行——本 adapter 是 ArchitectMerge 的**唯一允许写入模块**（Task 3 grep 守护断言）。
  - **current_plan_version 写入**：`origin="orchestration"` 须为 `TechnicalPlanOrigin` 合法值——**先确认** `TechnicalPlanOrigin` 是否含 `orchestration`；若无，本 task 顺带在 `delivery/models/technical_plan.py` 的 `TechnicalPlanOrigin` 增 `ORCHESTRATION = "orchestration"` 并 `makemigrations`（choices 改动需迁移；如已存在则跳过）。`PlanSession.current_plan_version` 写入复用 `PlanSessionService`：现有 `transition` 的 `_PERSISTABLE_FIELDS` 已含 `current_plan_version`，但 merging 阶段的写入不在转移点（转移由 engine 做）——为不旁路模型写，给 `PlanSessionService` 增一个窄方法 `set_current_plan_version(session, version_id)`（`@sync_to_async` 条件更新 `PlanSession.objects.filter(id=...).update(current_plan_version=..., updated_at=now)` + 同步内存态），adapter 调它（不裸 `session.save`）。
  注意：所有 ORM 写经 `sync_to_async`；async 上下文一律 `*_id` / `afirst` / `acount` / `aexists`，禁裸 lazy-FK（CR-01 bug 类）。ruff 行宽 100。
  curated re-export `ArchitectMergeAdapter`（+ synthesizer 协议/默认实现）到 `__init__.py` import 块与 `__all__`。

  测试 `server/tests/services/test_architect_merge_adapter.py`（注入 **mock synthesizer**，真实 PlanSession/PartialPlan/ResearchService 直建数据，`@pytest.mark.django_db` + `@pytest.mark.asyncio`，参考 engine 测试范式）：
  - pass 路径：mock synthesizer 返合法 MergedPlan（含 2 仓 execution_plan + 一致契约 + 无环 dag + 回滚覆盖）→ 断言 canonical 建出（`TechnicalPlan` 存在、origin=orchestration）、`PlanSession.current_plan_version` 被置、`ArchitectMerge.validation_status=passed` 且 `merged_plan_version` 非空、`_emit_event` 收到 plan.merge.started + plan.merge.completed。
  - fail 路径：mock synthesizer 返不合法 MergedPlan（如成环 dag）→ 断言无 canonical（`TechnicalPlan.objects.count()==0`）、`ArchitectMerge.validation_status=failed` 且 report 非空、current_plan_version 仍 None、emit plan.validation.failed。
  - 降级路径：mock synthesizer `side_effect=RuntimeError` → 不抛出、落 ArchitectMerge(failed, reason=synthesis_failed)。
  - INV-2：session.work_item=None 时 pass 路径不报错（canonical.work_item is None）。
  </action>
  <verify>
    <automated>cd server && uv run python manage.py makemigrations delivery --check --dry-run && uv run pytest tests/services/test_architect_merge_adapter.py -q</automated>
  </verify>
  <done>ArchitectMergeAdapter pass 路径落 canonical + current_plan_version + ArchitectMerge(passed) + 事件；fail/降级路径不落 canonical + ArchitectMerge(failed,report) + 不崩；canonical 仅经 TechnicalPlanService、ArchitectMerge 仅经本 adapter；async 无裸 lazy-FK；makemigrations 干净。</done>
</task>

<task type="auto">
  <name>Task 2: engine._merge 接线——pass→merged / fail→限次回退 / 超限→fail（§14 + engine 纯度）</name>
  <files>server/services/plan_orchestration/engine.py, server/tests/services/test_plan_orchestration_engine_merge.py</files>
  <action>
  改写 `engine.py` 的 `_merge`（替换现有 `await self.merge.merge(session); transition("merged")` 占位 TODO）：
  - `result = await self.merge.merge(session)`（result 为 Task 1 adapter 返回的 dict；防御 `isinstance(result, dict)`，与 `_route`/`_recall` 对称取值）。
  - `status = result.get("validation_status")`：
    - `"passed"` → `await self.session_service.transition(session, "merged")`（merging→done，§14；副作用 canonical 已由 adapter 落，engine 仅转移）。
    - 否则（failed/缺）→ **限次回退**：读 `attempt = result.get("attempt", 0)` 与上限 `MAX_MERGE_RETRIES = 1`（模块常量，CONTEXT「默认 1，超限 failed」）。
      - `if attempt >= MAX_MERGE_RETRIES:` → `await self.session_service.transition(session, "fail", error={"stage": str(PlanSessionStatus.MERGING), "reason": "merge_validation_exhausted", "report": result.get("report", {})})`（落 failed 终态，防无限循环）。
      - `else:` 按 `back_target = result.get("back_target", "clarifying")` 选 §14 回退事件：`"researching"` → `transition("validation_failed_reresearch")`（merging→researching）；否则 → `transition("validation_failed_reclarify")`（merging→clarifying）。
  - **engine 纯度**：绝不直接写 `session.status`（守 `test_engine_does_not_write_status_directly` 既有 grep 守护）；所有状态变更经 `session_service.transition`。`MAX_MERGE_RETRIES` 写为模块级常量并在 docstring 注明 CONTEXT 决策来源。更新 `_merge` docstring 去掉 Phase 40 TODO，写明 pass/fail/限次回退三分支 + §14 行引用。
  注意：`advance` 的通用 except 会把 stage 异常落 fail——adapter 已 graceful（不抛），故 `_merge` 内不应再吞异常；但 transition 失败（如 ConcurrentTransitionError）应沿用 `_research` 的良性并发处理思路评估（merging 通常无并发 barrier，可不特判，但若 transition 抛 ConcurrentTransitionError 应 log 而非落 failed——与 `_research` 对称，按需加 try/except ConcurrentTransitionError no-op）。
  测试 `server/tests/services/test_plan_orchestration_engine_merge.py`（注入 **mock merge adapter**，真实 PlanSession + PlanSessionService）：
  - pass：merge.merge 返 `{"validation_status":"passed","plan_version_id":"x","attempt":0}` → session merging→done。
  - fail 首次（attempt=0, back_target=clarifying）→ merging→clarifying。
  - fail 首次（attempt=0, back_target=researching）→ merging→researching。
  - fail 超限（attempt=1）→ merging→failed，error.reason=="merge_validation_exhausted"。
  - 复用/补充：engine 纯度 grep 守护（`test_engine_does_not_write_status_directly` 既在 test_plan_orchestration_engine.py，确认仍绿；本文件可再断言一次 `.status =` 不出现于 `_merge` 改动）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/test_plan_orchestration_engine_merge.py tests/services/test_plan_orchestration_engine.py -q</automated>
  </verify>
  <done>engine._merge：pass→done、fail→按 back_target 回退 clarifying/researching、attempt 超限→failed 终态；engine 仅经 transition 变更状态（纯度守护绿）；既有 engine 测试不回退。</done>
</task>

<task type="auto">
  <name>Task 3: ArchitectMerge INV-6 落库守护 + 融合段端到端集成测试</name>
  <files>server/tests/delivery/test_architect_merge_inv6_guard.py, server/tests/services/test_architect_merge_adapter.py</files>
  <action>
  新建 `server/tests/delivery/test_architect_merge_inv6_guard.py`（纯本地源码扫描，复刻 `test_research_inv6_guard.py` 范式）：
  - 唯一允许写 `ArchitectMerge` 的模块 `_ALLOWED_WRITER = "services/plan_orchestration/architect_merge_adapter.py"`（融合 service）。
  - 正则扫 `server/` 源码（剪 venv/缓存、排除 `tests/`、`/migrations/`、`delivery/models/` 与 writer 自身）断言无旁路 `ArchitectMerge.objects.(create|bulk_create|get_or_create|update_or_create|update)` / 直接实例化 `ArchitectMerge(` / 链式 `.save(`；命中列 `文件:行`。注意正则 `\bArchitectMerge\s*\(` 对 `ArchitectMergeStatus(` / `ArchitectMergeAdapter(` 误伤——用 `\bArchitectMerge(?!Status|Adapter)\s*\(` 或显式排除前缀，并跳过 `class ArchitectMerge` 定义行。
  - `test_inv6_writer_actually_writes`：断言 writer 文件确含 `ArchitectMerge.objects.create` 与 `validation_status`（守护有效性，防形同虚设）。
  在 `test_architect_merge_adapter.py` 追加**端到端集成测试**（用真实 `PlanOrchestrationEngine` + 注入 ArchitectMergeAdapter（其内注入 mock synthesizer），session 起于 `MERGING`）：
  - pass：`engine.advance(session)` → session done + canonical 存在 + current_plan_version 置 + ArchitectMerge(passed)（验证 adapter↔engine 接线闭环，INV-6 canonical 仅经 service）。
  - fail：mock synthesizer 返成环 dag → `advance` → session 回退 clarifying（attempt 0）+ ArchitectMerge(failed) + 无 canonical。
  确认 `makemigrations delivery --check --dry-run` 仍干净（若 Task 1 改了 TechnicalPlanOrigin choices 已生成迁移）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/delivery/test_architect_merge_inv6_guard.py tests/services/test_architect_merge_adapter.py -q && uv run python manage.py makemigrations --check --dry-run</automated>
  </verify>
  <done>ArchitectMerge 旁路写入被 grep 守护拦截、唯一 writer 确实写状态；engine+adapter 端到端 pass/fail 路径闭环（canonical 经 service、状态经 transition）；全仓 makemigrations 零漂移。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM 合成器 → adapter | LLM 产出的 MergedPlan JSON 是半可信结构，经 PlanValidator + validate_technical_plan 双闸方可落 canonical |
| adapter → canonical / ArchitectMerge | 落库须经唯一写入入口（INV-6）：canonical→TechnicalPlanService、ArchitectMerge→本 adapter |
| engine → PlanSession.status | 状态变更只经 PlanSessionService.transition（engine 纯度） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-02-01 | Tampering | LLM MergedPlan 产物落 canonical | mitigate | validate_plan（5 项）+ create_from 内 validate_technical_plan 双校验；JSON 解析不 eval、取首{末}容错 |
| T-40-02-02 | Denial of Service | 验证失败无限回退重融合 | mitigate | ArchitectMerge.attempt 限次（MAX_MERGE_RETRIES=1），超限 transition fail 终态 |
| T-40-02-03 | Elevation of Privilege / 数据完整性 | 旁路写 ArchitectMerge / canonical | mitigate | INV-6 grep 守护（ArchitectMerge 唯一 writer = adapter；canonical 唯一 = TechnicalPlanService）；engine 纯度 grep 守护 |
| T-40-02-04 | Denial of Service | LLM 合成异常拖垮编排 | mitigate | synthesize 异常 graceful 降级（落 failed report，不上抛、不崩 advance） |
| T-40-02-05 | Information Disclosure | async 裸 lazy-FK 触发 SynchronousOnlyOperation / 泄漏 | mitigate | async 上下文一律 `*_id`/`afirst`/`acount`/`aexists`/`.values()`，禁裸 lazy-load（规避 Phase 38 CR-01 bug 类） |
</threat_model>

<verification>
- `cd server && uv run python manage.py makemigrations --check --dry-run` 全仓零漂移。
- `cd server && uv run pytest tests/services/test_architect_merge_adapter.py tests/services/test_plan_orchestration_engine_merge.py tests/services/test_plan_orchestration_engine.py tests/delivery/test_architect_merge_inv6_guard.py -q` 全绿。
- `cd server && uv run ruff check services/plan_orchestration/architect_merge_adapter.py services/plan_orchestration/engine.py` 无 lint。
- 既有 `tests/delivery/test_research_inv6_guard.py` / `test_technical_plan_inv6_guard.py` 不回退。
</verification>

<success_criteria>
- MERGE-01：融合收齐 valid PartialPlan → 注入 LLM 合成器产 §7 MergedPlan → 经 `TechnicalPlanService.create_from(origin="orchestration")` 落 canonical PlanVersion + 置 `PlanSession.current_plan_version`（INV-6 / INV-2）。
- MERGE-02：PlanValidator 失败 / 合成失败 → `ArchitectMerge.validation_status=failed` + report，engine 按 §14 回退 clarifying/researching，限次后落 failed 终态。
- MERGE-03：MergedPlan 的 dependency_dag + execution_plan[].dependencies 显式落入 canonical content（为 v0.8 wave 拓扑铺底）。
- §15：融合段产 plan.merge.started / plan.merge.completed / plan.validation.failed 事件（经 `_emit_event` 钩子，真实 sink Phase 41）。
- engine 纯度保持（仅 transition），ArchitectMerge/canonical 落库唯一入口经 INV-6 守护，makemigrations 干净。
</success_criteria>

<output>
Create `.planning/phases/40-mergedplan-planvalidator/40-02-SUMMARY.md` when done
</output>
