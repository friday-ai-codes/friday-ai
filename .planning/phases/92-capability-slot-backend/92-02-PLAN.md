---
phase: 92-capability-slot-backend
plan: 02
type: execute
wave: 2
depends_on: [92-01]
files_modified:
  - server/workflows/nodes/ai/plan_research.py
  - server/feishu/cards/chat_question_card.py
  - server/tests/workflows/test_plan_research_node.py
  - server/tests/feishu/test_chat_question_card.py
autonomous: true
requirements: [SLOT-02]

must_haves:
  truths:
    - "ai_plan_research 暴露 clarify（输出，契约 clarification_request）/ resume（输入，契约 clarification_answer）插槽端口"
    - "ai_plan_research 既有 default/error 端口原样保留，execute 运行时分支不变（既有工作流零回归）"
    - "build_clarification_card 支持自定义回调 action 前缀（默认 plan_clarify_answer），供 standalone 澄清卡节点路由到独立回调（92-03 消费）"
  artifacts:
    - path: "server/workflows/nodes/ai/plan_research.py"
      provides: "clarify(out, shape=clarification_request) + resume(in, shape=clarification_answer) 端口声明"
      contains: "clarification_request"
    - path: "server/feishu/cards/chat_question_card.py"
      provides: "build_clarification_card 增 action 关键字参数（默认 plan_clarify_answer，向后兼容）"
      contains: "action"
  key_links:
    - from: "server/workflows/nodes/ai/plan_research.py::AIPlanResearchNode.outputs"
      to: "shapes.KNOWN_PORT_SHAPES"
      via: "clarify 端口 shape=clarification_request"
      pattern: "shape=\"clarification_request\""
---

<objective>
给方案编排入口节点 `ai_plan_research` 暴露澄清「插槽端口」声明——`clarify`（输出，契约 `clarification_request`，凹槽）/ `resume`（输入，契约 `clarification_answer`，凸点）；并把 `build_clarification_card` 的回调 action 前缀参数化（默认保持 `plan_clarify_answer`），为 92-03 的 standalone 澄清卡节点路由到独立回调铺平。落 SLOT-02（端口暴露半 + 卡片复用准备）。

Purpose: per D-B——`ai_plan_research` 暴露澄清插槽端口供编辑器磁吸（93）+ validator 识别契约（92-01）。**关键约束（Pitfall 5 / A4）：SLOT-02「暴露插槽端口」= 端口声明，不改 execute 运行时分支**（91 发卡逻辑已在 `_maybe_suspend`/`_send_clarify_card`，不依赖 clarify handle 路由）。保留 default/error 零回归（per D-B + Pattern 2）。
Output: ai_plan_research clarify/resume 端口声明 + build_clarification_card action 参数化。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/92-capability-slot-backend/92-CONTEXT.md
@.planning/phases/92-capability-slot-backend/92-RESEARCH.md
@.planning/phases/91-clarification-outlets-resume/91-02-SUMMARY.md

# 改动落点
@server/workflows/nodes/ai/plan_research.py
@server/feishu/cards/chat_question_card.py
</context>

<artifacts_this_phase_produces>
本 plan 新建符号（92-03 引用）：
- `ai_plan_research` inputs 增 `resume`（shape=`clarification_answer`）；outputs 增 `clarify`（shape=`clarification_request`）
- `build_clarification_card(..., *, action: str = "plan_clarify_answer")` 关键字参数（form_submit value.action 取该值）
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ai_plan_research 暴露 clarify/resume 插槽端口（additive 零回归）</name>
  <read_first>
    - server/workflows/nodes/ai/plan_research.py:100-137（inputs/outputs 现状 default/error）、139-194（execute / _maybe_suspend 调用——确认 next_handle 只走 default/error，不改）
    - 92-RESEARCH.md §Code Examples（ai_plan_research 加插槽端口）、§Architecture Pattern 2、§Common Pitfalls Pitfall 5（clarify 端口非运行时分支）
    - server/workflows/nodes/shapes.py（92-01 产，KNOWN_PORT_SHAPES）
  </read_first>
  <behavior>
    - Test 1: `AIPlanResearchNode` inputs 含 name="resume" 且 `shape=="clarification_answer"`；既有 name="default" 端口保留。
    - Test 2: outputs 含 name="clarify" 且 `shape=="clarification_request"`；既有 name="default"/"error" 端口保留且 shape=="" （default 端口契约恒空，validator 通配命门）。
    - Test 3: `get_schema()` outputs 中 clarify 项含 `"shape": "clarification_request"`（前端可读）。
    - Test 4（零回归）：既有 `test_plan_research_node.py` 12 测全绿，execute/_maybe_suspend 行为不变。
  </behavior>
  <files>server/workflows/nodes/ai/plan_research.py, server/tests/workflows/test_plan_research_node.py</files>
  <action>
    在 `AIPlanResearchNode.inputs` 追加 `NodePort(name="resume", label="澄清答复", port_type=PortType.OBJECT, required=False, shape="clarification_answer", description="回流澄清答案续推（凸点）")`（per D-B；引用 92-RESEARCH §Code Examples）。保留既有 `default` 端口逐字不变。
    在 `AIPlanResearchNode.outputs` 追加 `NodePort(name="clarify", label="澄清请求", port_type=PortType.OBJECT, shape="clarification_request", description="需澄清时吐出澄清请求（凹槽）")`。保留既有 `default`（含 schema）/`error` 端口逐字不变——**它们不贴 shape（保持 shape=""）**：per Open Questions 决议 #3，**不**给现有 default/plan 等生产端口补契约（保「空契约=通配」零回归，避免拦截既有 plan→coding 边）。
    **绝不改 execute / _map_terminal / _maybe_suspend**（per Pitfall 5 + A4：clarify 是声明，91 发卡逻辑不依赖 clarify handle 路由；NodeResult.next_handle 仍只走 default/error）。新端口仅供前端磁吸 + validator 识别契约。
    扩 `server/tests/workflows/test_plan_research_node.py` 新增端口存在性用例（behavior 1-3，可加 `-k port` 可定位）；运行既有 12 测确认零回归（behavior 4）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/workflows/test_plan_research_node.py -x -q</automated>
  </verify>
  <done>ai_plan_research 含 clarify(out, clarification_request)/resume(in, clarification_answer) 端口 + default/error 保留（default shape 恒空）；get_schema 输出 clarify shape；test_plan_research_node.py 12+ 测全绿、execute 零回归。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: build_clarification_card 回调 action 前缀参数化（向后兼容）</name>
  <read_first>
    - server/feishu/cards/chat_question_card.py:132-303（build_clarification_card——form_submit value 构造，91-02 已设 value.action="plan_clarify_answer" + clarification_id）
    - 91-02-SUMMARY.md（value.action 由 chat_question_answer 改 plan_clarify_answer + clarification_id 入参背景）
  </read_first>
  <behavior>
    - Test 1（向后兼容）：`build_clarification_card(questions, execution_id, node_id, clarification_id="cid")` 不传 action → form_submit value.action == "plan_clarify_answer"（91 工作流澄清回调路由不变，零回归）。
    - Test 2（standalone 路由）：`build_clarification_card(..., action="clarify_card_answer")` → form_submit value.action == "clarify_card_answer"（供 92-03 独立卡回调 startswith("clarify_card_") 命中）。
    - Test 3: value 仍并列携 execution_id/node_id/clarification_id（92-03 据其定位本节点 NodeExecution + 取轮）。
  </behavior>
  <files>server/feishu/cards/chat_question_card.py, server/tests/feishu/test_chat_question_card.py</files>
  <action>
    给 `build_clarification_card` 新增关键字参数 `action: str = "plan_clarify_answer"`（默认值=91 现状，向后兼容命门，Test 1）。把内部 form_submit `value` 字典里硬编码的 `"action": "plan_clarify_answer"` 改为 `"action": action`。其余 value 字段（execution_id/node_id/clarification_id/`q{i}`/`qt{i}` 字段命名）逐字不变。
    docstring 补 `action` 参数说明：回调路由前缀——`plan_clarify_answer`（91 工作流 ai_plan_research 澄清，绑 PlanSession）/ `clarify_card_answer`（92 standalone 澄清卡节点，独立回调）物理隔离（CardCallbackView startswith 匹配）。
    扩 `server/tests/feishu/test_chat_question_card.py` 新增 behavior 1-3（断言 value.action 默认值 + 自定义值 + 并列 id 字段）。**不**改 91 既有调用点（plan_research.py `_send_clarify_card` 不传 action → 默认 plan_clarify_answer，零回归）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/feishu/test_chat_question_card.py tests/workflows/test_plan_research_node.py -x -q</automated>
  </verify>
  <done>build_clarification_card 支持 action 关键字（默认 plan_clarify_answer）；不传时 91 路由零回归；传 clarify_card_answer 时 value.action 切换；既有卡片用例 + 91 发卡用例零回归。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ai_plan_research 端口声明 → /api/node-types/ | 端口契约只读流出，前端磁吸消费 |
| build_clarification_card → 飞书卡片 form_submit | 卡片 value 承载服务端权威路由锚（action/execution_id/node_id/clarification_id） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-02-REGRESS | Tampering/DoS | ai_plan_research 端口/卡片改动破坏既有工作流 | mitigate | clarify/resume 为 additive 声明，default/error 逐字保留且 shape 恒空（validator 通配）；execute 不改（Pitfall 5）；build_clarification_card action 默认值=91 现状（向后兼容）；既有 12 测 + 卡片测零回归红线 |
| T-92-02-ROUTE | Spoofing | 新 action 前缀与既有回调路由冲突/抢占 | mitigate | `clarify_card_` 前缀与 `plan_clarify_` / `chat_question_answer` 物理不交叉（CardCallbackView startswith 互不抢，91-02 同款隔离范式）；本 plan 仅参数化，注册在 92-03 |
| T-92-02-INFO | Information Disclosure | 卡片正文泄漏需求/凭证明文 | accept | 卡片正文脱敏由发卡方（91 `_send_clarify_card` / 92-03 节点）经 redact_secrets_in_text 负责，本 plan 不改正文组装 |
| T-92-02-SC | Tampering | npm/pip/cargo installs | accept | 本 plan 无任何包安装（纯仓内 Python 改动） |
</threat_model>

<verification>
- `cd server && uv run pytest tests/workflows/test_plan_research_node.py tests/feishu/test_chat_question_card.py -x -q` 全绿。
- `cd server && uv run pytest tests/workflows tests/feishu -q` 无回归（既有发卡/订阅/澄清用例不变红）。
- `cd server && uv run ruff format --check workflows/nodes/ai/plan_research.py feishu/cards/chat_question_card.py && uv run ruff check workflows/nodes/ai/plan_research.py feishu/cards/chat_question_card.py && uv run mypy workflows/nodes/ai/plan_research.py`。
- `cd server && uv run python manage.py makemigrations --check`（无迁移）。
</verification>

<success_criteria>
- ai_plan_research 暴露 clarify/resume 插槽端口（契约 clarification_request/clarification_answer），default/error 零回归、execute 运行时不变。
- build_clarification_card 可路由到独立回调前缀，91 工作流澄清路由默认零回归。
</success_criteria>

<output>
Create `.planning/phases/92-capability-slot-backend/92-02-SUMMARY.md` when done
</output>
