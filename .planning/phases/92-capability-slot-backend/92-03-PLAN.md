---
phase: 92-capability-slot-backend
plan: 03
type: execute
wave: 3
depends_on: [92-02]
files_modified:
  - server/workflows/nodes/integrations/clarification_card.py
  - server/feishu/callbacks/clarify_card_callback.py
  - server/feishu/urls.py
  - web/src/types/workflow/__fixtures__/node-types.fixture.json
  - server/tests/workflows/test_clarification_card_node.py
  - server/tests/feishu/test_clarify_card_callback.py
autonomous: true
requirements: [SLOT-02]

must_haves:
  truths:
    - "clarification_card 节点可被注册与编排：入 clarification_request、出 clarification_answer + feishu_message，shape 契约经 get_schema 暴露"
    - "节点 execute 据 clarification_request 发飞书交互卡 → 建 ClarifyCardCallback 订阅 → waiting_event 挂起（发卡 best-effort 不反噬挂起）"
    - "standalone 卡回调 clarify_card_ 据卡片权威 execution_id/node_id 定位本节点 NodeExecution、幂等门收答 → answer_round 落库（有 clarification_id 时，INV-6）→ approve_node 本节点（绝不绑 ai_plan_research/PlanSession）"
    - "新增节点后 node-types.fixture.json node_count 36→37，node-sync.test.ts 绿"
  artifacts:
    - path: "server/workflows/nodes/integrations/clarification_card.py"
      provides: "ClarificationCardNode（node_type=clarification_card，INTEGRATION，is_blocking）"
      contains: "clarification_card"
    - path: "server/feishu/callbacks/clarify_card_callback.py"
      provides: "@register_card_callback('clarify_card_') 收答 → answer_round → approve_node 本节点"
      contains: "clarify_card_"
    - path: "web/src/types/workflow/__fixtures__/node-types.fixture.json"
      provides: "node_count 37（含 clarification_card）"
      contains: "clarification_card"
  key_links:
    - from: "server/workflows/nodes/integrations/clarification_card.py::execute"
      to: "WorkflowEventSubscription(event_type='ClarifyCardCallback')"
      via: "发卡后建订阅 + waiting_event"
      pattern: "ClarifyCardCallback"
    - from: "server/feishu/callbacks/clarify_card_callback.py"
      to: "ClarificationService.answer_round"
      via: "有 clarification_id 时按 order 映射 answers 落库（INV-6）"
      pattern: "answer_round"
    - from: "server/feishu/urls.py"
      to: "feishu.callbacks.clarify_card_callback"
      via: "import 触发 @register_card_callback 注册"
      pattern: "clarify_card_callback"
---

<objective>
新增可注册可编排的「澄清卡」原子节点 `clarification_card`（入 `clarification_request`、出 `clarification_answer` + `feishu_message`）：吃澄清请求 → 发飞书交互卡（群/会话）→ 挂起等回答 → 收答续推本节点。回调 standalone 自建（mirror `chat_question_callback`，**不**绑 91 `plan_clarify_callback` 的 `PlanSession`/approve `ai_plan_research`），收答写 `delivery.Clarification` 经 `ClarificationService.answer_round`（INV-6）。落 SLOT-02（节点半 + 回调闭环），并重跑 fixture 守护。

Purpose: per D-B + Open Questions 决议 #1——澄清能力升级为可拖拽拼接的原子节点；standalone 回调据 `execution_id/node_id` approve 本 card 节点（自洽闭环，无 PlanSession 依赖），payload 至少 `{clarification_id, answers[]}`，落库经 answer_round。
Output: `ClarificationCardNode` + `clarify_card_callback` + 注册接线 + fixture 同步。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/92-capability-slot-backend/92-CONTEXT.md
@.planning/phases/92-capability-slot-backend/92-RESEARCH.md
@.planning/phases/91-clarification-outlets-resume/91-03-SUMMARY.md

# 蓝本 + 复用资产（必读）
@server/workflows/nodes/integrations/chat_question.py
@server/feishu/callbacks/chat_question_callback.py
@server/feishu/callbacks/plan_clarify_callback.py
@server/delivery/services/clarification_service.py
</context>

<artifacts_this_phase_produces>
本 plan 新建符号：
- `ClarificationCardNode`（`server/workflows/nodes/integrations/clarification_card.py`，`@register_node`，node_type=`clarification_card`，INTEGRATION，execution_mode=server_local，is_blocking=True）；inputs=[clarification_request(shape=clarification_request)]、outputs=[clarification_answer(shape=clarification_answer), feishu_message(shape=feishu_message)]
- `handle_clarify_card_action`（`server/feishu/callbacks/clarify_card_callback.py`，`@register_card_callback("clarify_card_")`）+ helper（`_extract_callback_data`/`_ack_card`/`_aget_waiting_node`/`_acollect_round_questions`/`_build_answers`/`_do_clarify_card_async`/`_FeishuResponder`）
- `feishu/urls.py` 新增 import 触发注册
- 订阅事件键 `ClarifyCardCallback`
- fixture node_count 36→37（含 clarification_card）
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ClarificationCardNode（发卡 + 订阅 + waiting_event）</name>
  <read_first>
    - server/workflows/nodes/integrations/chat_question.py:54-256（GroupChatQuestionNode 蓝本——发卡 try/except + WorkflowEventSubscription.acreate + waiting_event，多输出端口，_get_feishu_credentials）
    - server/feishu/cards/chat_question_card.py:132-303（build_clarification_card，92-02 已加 action 参数）
    - server/workflows/nodes/ai/plan_research.py:309-462（_send_clarify_card/_acollect_round_questions/_resolve_initiator 发卡订阅范式 + 脱敏 + initiated_by）
    - server/delivery/services/clarification_service.py:158-219（create_round / ClarificationQuestion 形态，取轮子题字段 question/qtype/options/recommended/order）
    - server/workflows/nodes/shapes.py（92-01）；92-RESEARCH.md §Code Examples（clarification_card 节点骨架）、§Architecture Pattern 3、Pitfall 5（声明 vs 运行时）
  </read_first>
  <behavior>
    - Test 1（注册）：`NodeRegistry.get("clarification_card")` 非空；`get_schema()` inputs 含 clarification_request(shape=clarification_request)、outputs 含 clarification_answer(shape=clarification_answer) + feishu_message(shape=feishu_message)；category=integration、is_blocking=True。
    - Test 2（发卡 + 挂起，有 clarification_id）：input clarification_request={clarification_id, chat_id} 指向已建轮（含 2 子题）→ execute 取轮子题（order）→ build_clarification_card(action="clarify_card_answer") → send_card → 建 WorkflowEventSubscription(event_type="ClarifyCardCallback") → 返回 NodeResult(status="waiting_event")，output 携 clarification_id/chat_id/question_count。
    - Test 3（raw questions 透传，无 clarification_id）：input clarification_request={questions:[...], chat_id} → 用 raw questions 渲染卡、waiting_event（output 标记 persisted=False，回调走透传不落库）。
    - Test 4（发卡失败 best-effort）：send_card 抛异常 → 仍返回 waiting_event（不反噬挂起，output 标 card_sent=False），不抛过引擎。
    - Test 5（缺 chat_id 且缺 questions/clarification_id）：execute 返回 failed + next_handle="error"（无澄清内容无意义，D-4 范式）。
  </behavior>
  <files>server/workflows/nodes/integrations/clarification_card.py, server/tests/workflows/test_clarification_card_node.py</files>
  <action>
    新建 `server/workflows/nodes/integrations/clarification_card.py` 的 `@register_node class ClarificationCardNode(BaseNode)`（逐行 mirror GroupChatQuestionNode；92-RESEARCH §Code Examples 骨架）：
      - ClassVar：`node_type="clarification_card"`、`display_name="澄清卡"`、`description="吃澄清请求 → 发飞书交互卡 → 收答 → 吐结构化答案"`、`icon="help-circle"`、`category=NodeCategory.INTEGRATION`、`execution_mode="server_local"`、`is_blocking=True`。
      - inputs：`[NodePort(name="clarification_request", label="澄清请求", port_type=PortType.OBJECT, required=True, shape="clarification_request")]`（shape 引用 92-01 字段，取值 ∈ KNOWN_PORT_SHAPES）。
      - outputs：`[NodePort(name="clarification_answer", label="澄清答复", port_type=PortType.OBJECT, shape="clarification_answer"), NodePort(name="feishu_message", label="飞书消息", port_type=PortType.OBJECT, shape="feishu_message")]`。
      - config_schema：可选 `chat_id`（模板变量，群聊 ID 兜底来源）+ `title`/`reason`（卡片标题/原因，可选）。
      - `execute(context)`：
        1. 取 clarification_request 输入 payload（`context.get_input("clarification_request")` 或上游 default 输出；兼容 dict）；解析 `clarification_id`(str)、`questions`(list)、`chat_id`、`title`、`reason`。chat_id 缺则回退 config.render_template("chat_id")。
        2. 若有 `clarification_id`：经 `ClarificationQuestion.objects.filter(clarification_id=...).order_by("order")` 取整轮子题组 questions（mirror plan_clarify_callback `_acollect_round_questions`，async 标量 .values，per async ORM 约束）；标 `persisted=True`。否则用 raw `questions` 标 `persisted=False`。
        3. 二者皆空 **或** 缺 chat_id → 返回 `NodeResult(status="failed", error="缺少澄清内容或群聊", next_handle="error")`（Test 5，D-4）。
        4. `build_clarification_card(questions, execution_id=context.execution_id, node_id=context.node_id, clarification_id=clarification_id, action="clarify_card_answer", title=title, reason=reason)`；卡片正文经发卡链路脱敏（build_clarification_card 内已处理结构；额外用 `redact_secrets_in_text` 包 reason/title 等自由文本，T-92-03-INFO）。
        5. 解析飞书凭证发卡（mirror GroupChatQuestionNode `_get_feishu_credentials` + FeishuIMClient.send_card 或 `_send_clarify_card` 范式）；**整段发卡 try/except best-effort**（失败 log warning + 标 card_sent=False，仍继续挂起，T-92-03-DOS / Test 4）。
        6. `if context.workflow_execution and context.node_execution:` 建 `WorkflowEventSubscription.objects.acreate(workflow_execution=..., node_execution=..., event_type="ClarifyCardCallback", project_key=..., timeout_at=now+timedelta(minutes=60), timeout_action="fail")`（订阅 acreate **不**包 try/except——超时兜底是可靠性机制，guard 已确保 FK 有效，mirror 91-02 决策）。
        7. 返回 `NodeResult(status="waiting_event", output={"clarification_id": clarification_id, "chat_id": chat_id, "question_count": len(questions), "persisted": persisted, "card_sent": ..., "questions_meta": [{"id":..., "order":..., "qtype":...} ...] })`（questions_meta 供 raw-questions 透传模式回调据 order 映射；脱敏不含正文）。
      - **不实现 resume 分支**：节点经引擎 `approve_node`（回调侧，Task 2）直接 complete 携 approval_data 即可——mirror chat_question 不在 execute 内处理 resume（per scheduler.approve_node 直接 amark_completed，不重入 execute）。
      - 观测：structlog snake_case `clarification_card_started/card_sent/card_send_failed`，带 `category="caller"`/`component="workflow_node"`/关键 duration_ms；`initiated_by_user_id` 经 context 解析（缺记 `system`，T-92-03-ATTR）。
    新建 `server/tests/workflows/test_clarification_card_node.py`（mirror test_chat_question / test_plan_research_node）：覆盖 behavior 1-5（真实 PlanSession + ClarificationService.create_round 建轮供 Test 2；send_card mock/respx；发卡失败注入）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/workflows/test_clarification_card_node.py -x -q</automated>
  </verify>
  <done>clarification_card 注册 + 端口 shape 经 get_schema 暴露；execute 发卡（best-effort）+ ClarifyCardCallback 订阅 + waiting_event；缺内容→failed/error；test_clarification_card_node.py 全绿。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: clarify_card 独立回调（收答 → answer_round → approve_node 本节点）</name>
  <read_first>
    - server/feishu/callbacks/chat_question_callback.py:33-309（standalone 回调蓝本——@register_card_callback + 同步 ack + _run_in_thread 后台 + _aget_waiting_node 本节点 + approve_node 本节点）
    - server/feishu/callbacks/plan_clarify_callback.py:55-345（_extract_callback_data/_ack_card/_aget_waiting_node/_acollect_round_questions/_build_answers/bind_task_context/_FeishuResponder/置灰卡 best-effort 范式）
    - server/delivery/services/clarification_service.py:221-292（answer_round 按题幂等 + 采纳信号 server 端定格）
    - server/feishu/urls.py（import 触发注册范式）
  </read_first>
  <behavior>
    - Test 1（前缀注册唯一）：`clarify_card_` 前缀已注册，与 `plan_clarify_` / `chat_question_answer` 物理不交叉。
    - Test 2（同步 ack + 缺 id no-op）：缺 execution_id/node_id → 返回 None + warning；action != "clarify_card_answer" → None；正常 → 即时返回 ack 卡 + 派发后台。
    - Test 3（_build_answers 映射固化）：按 order 枚举 q{i}(single=str/multi=list)/qt{i}(freeform) → answers[{question_id, selected, freeform_text}]，q0→子题0.id（防错位，WARNING #3 同款）。
    - Test 4（后台续推 + approve_node 本节点）：waiting 的 clarification_card NodeExecution → 收答 → 有 clarification_id 时 `ClarificationService().answer_round(clarification_id, answers)` 落库（INV-6）→ approval_data 置 answers → SUSPENDED→RUNNING → `WorkflowEngine().approve_node(本 node_execution, ...)`（approve 的是本 card 节点，**不是** ai_plan_research）。
    - Test 5（非 waiting 幂等）：本节点非 waiting_event → ignored no-op（防重放）。
    - Test 6（fail-soft）：answer_round / approve_node 抛错 → log error(脱敏) 绝不反噬飞书 5xx。
    - Test 7（transient 无 clarification_id）：无 clarification_id（raw questions 模式）→ 跳过 answer_round（无轮可写）、仍 approve_node 携 answers（透传），不报错。
  </behavior>
  <files>server/feishu/callbacks/clarify_card_callback.py, server/feishu/urls.py, server/tests/feishu/test_clarify_card_callback.py</files>
  <action>
    新建 `server/feishu/callbacks/clarify_card_callback.py`（mirror `chat_question_callback` 结构 + `plan_clarify_callback` 的 fail-soft/脱敏/bind_task_context 细节；**关键区别：approve 本 card 节点，不绑 PlanSession/不 approve ai_plan_research**，per Open Questions 决议 #1 + Pitfall 4）：
      - `@register_card_callback("clarify_card_")` 同步 `handle_clarify_card_action(callback)`：`_extract_callback_data` 取 action/execution_id/node_id/clarification_id/question_count；`action != "clarify_card_answer"` → None；缺 execution_id/node_id → warning + None（T-92-03-SPOOF：据服务端权威 execution_id/node_id 定位，绝不信回调直传可伪造字段）；`_run_in_thread(_do_clarify_card_async(...))` + 即时返回 `_ack_card`（3s 内同步）。
      - 后台 `_do_clarify_card_async(*, execution_id, node_id, clarification_id, question_count, data, responder_id, chat_id)`：`with bind_task_context(user_id=responder_id or None, source="feishu", component="workflow_node")`（re-bind 触发用户，T-92-03-ATTR）：
        ① `_aget_waiting_node(execution_id, node_id)`（filter status=WAITING_EVENT + select_related space）；非 waiting → log ignored + return（幂等门，Test 5）。**额外校验 `node.node_type=="clarification_card"`**（防跨节点误 approve；从 node_execution.node 经 sync_to_async 取）。
        ② 取 questions：有 clarification_id → `_acollect_round_questions(clarification_id)`（order_by order，mirror plan_clarify_callback，绝不信直传 session_id）；无 clarification_id → 从 `node_execution.output_data["questions_meta"]`（order 序）取（transient 模式，Test 7）。
        ③ `_build_answers(questions, data)`（纯函数，按 order 枚举 q{i}/qt{i} → answers，Test 3）。
        ④ 落库（仅有 clarification_id 时，INV-6，Test 4）：`await ClarificationService().answer_round(clarification_id, answers)`；无 clarification_id 跳过（transient 透传，Test 7）。
        ⑤ approve 本节点：`node_execution.approval_data = {"clarification_answered": True, "clarification_id": clarification_id, "answers": answers}`（answers 即将随 approve_node 进 output，供下游/clarification_answer 端口语义消费）→ asave(update_fields=["approval_data"]) → workflow_execution SUSPENDED→RUNNING → `WorkflowEngine().approve_node(node_execution, _FeishuResponder(responder_id), "clarify_card_answer")`（approve 本 card 节点）。
        ⑥ 置灰卡 best-effort（mirror plan_clarify_callback `_send_answered_card_best_effort`，发到 callback.chat_id，正文脱敏）。
        全程 `except Exception` fail-soft → log error(`redact_secrets_in_text`，仅记 execution_id/node_id/clarification_id 标量)，绝不反噬飞书主响应（T-92-03-DOS / Test 6）；写入只经 answer_round（INV-6，回调无 `.objects.create/.update/.save` 旁路写 delivery 表）。
      - `_FeishuResponder` 轻量类（mirror plan_clarify_callback）。
    `server/feishu/urls.py` 加 `import feishu.callbacks.clarify_card_callback`（触发 @register_card_callback 注册，mirror plan_clarify_callback import）。
    新建 `server/tests/feishu/test_clarify_card_callback.py`（mirror test_plan_clarify_callback）：覆盖 behavior 1-7。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/feishu/test_clarify_card_callback.py tests/workflows/test_clarification_card_node.py -x -q</automated>
  </verify>
  <done>clarify_card_ 回调注册唯一；据 execution_id/node_id 定位 + node_type 校验 + 幂等门收答；有 clarification_id 经 answer_round 落库（INV-6）、无则透传；approve 本 card 节点（不绑 ai_plan_research）；fail-soft 脱敏；test_clarify_card_callback.py 全绿。</done>
</task>

<task type="auto">
  <name>Task 3: 重跑 node fixture（node_count 36→37）+ node-sync 守护</name>
  <read_first>
    - server/workflows/management/commands/dump_node_fixture.py（_to_fixture_node 只 dump name，不含 shape——**不改本命令**）
    - web/src/components/__tests__/node-sync.test.ts（palette ⊆ fixture 对账，node_type 全集）
    - 92-RESEARCH.md §Common Pitfalls Pitfall 3（fixture 漂移守护：仅 shape 不改 fixture；新节点须重跑）
  </read_first>
  <files>web/src/types/workflow/__fixtures__/node-types.fixture.json</files>
  <action>
    重跑 fixture 生成命令使后端事实源同步到前端快照（per Pitfall 3 + scope_reminder）：`cd server && uv run python manage.py dump_node_fixture`（等价 `pnpm -C web gen:node-fixture`）。新增 `clarification_card` 节点 → `node_count` 36→37；同时捕获 92-02 `ai_plan_research` 的 clarify/resume 端口名进 inputs/outputs name 数组（_to_fixture_node 只 dump name，不含 shape，故 shape 字段不入 fixture——符合 scope_reminder「仅加 shape 字段不改 fixture」）。
    **不改 `dump_node_fixture._to_fixture_node`**（保持只 dump name，per scope_reminder）。
    校验 fixture diff 仅为 +clarification_card 节点项 + ai_plan_research 端口名增量（无其它节点漂移）。
    NOTE（Pitfall 3）：`node-sync.test.ts` 断言 palette ⊆ fixture（node_type 全集），clarification_card 在 palette（前端节点库）中**暂不可见/不可拖**（palette 收录是 Phase 93 前端工作）；fixture 多一个 palette 没有的节点**不**让该测试变红——本 plan 只保后端注册 + fixture 同步，编辑器可见性留 93。
  </action>
  <verify>
    <automated>cd web && pnpm vitest run node-sync</automated>
  </verify>
  <done>node-types.fixture.json node_count=37 含 clarification_card + ai_plan_research 端口名同步；node-sync.test.ts 绿；fixture diff 无意外节点漂移。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 飞书卡回调 → CardCallbackView（clarify_card_ 前缀） | 不可信回调 payload 跨此边界（含可伪造的 session_id 等字段） |
| clarification_card 节点 → 飞书群（发卡） | 卡片正文承载澄清问题（可能含需求上下文） |
| 回调 → delivery.Clarification（answer_round） | 答案落库唯一入口（INV-6） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-03-SPOOF | Spoofing/Tampering | 伪造 execution_id/node_id/clarification_id 越权 approve | mitigate | 据服务端权威 execution_id/node_id 定位本节点 NodeExecution + 校验 node_type=="clarification_card" + WAITING_EVENT 幂等门（非 waiting no-op）；clarification_id 仅用于取轮，绝不信回调直传 session_id（mirror 91-03 T-91-03 范式，Task 2 Test 5） |
| T-92-03-INV6 | Tampering | 回调旁路写 delivery 表 | mitigate | 落库只经 `ClarificationService.answer_round`（INV-6），回调/节点无 `.objects.create/.save` 旁路写；INV-6 grep 守护既有断言覆盖子模型 |
| T-92-03-INFO | Information Disclosure | 卡片正文/日志泄漏需求/凭证明文 | mitigate | 卡片自由文本（reason/title/置灰卡）经 `redact_secrets_in_text`；异常文本脱敏；日志仅记 execution_id/node_id/clarification_id 标量 + category/component |
| T-92-03-DOS | DoS | 发卡/回调异常反噬主流程（5xx 崩工作流） | mitigate | 节点发卡 best-effort try/except（失败仍 waiting_event 不反噬挂起）；回调后台 fail-soft（绝不上抛飞书 5xx）；置灰卡 best-effort（mirror 91 范式） |
| T-92-03-ATTR | Repudiation | 后台续推无触发用户归因 | mitigate | 回调后台 `bind_task_context(user_id=callback.user_open_id, source=feishu)` re-bind；节点 initiated_by_user_id 缺记 system（观测规范） |
| T-92-03-SC | Tampering | npm/pip/cargo installs | accept | 本 plan 无任何包安装（纯仓内 Python + fixture JSON 重生成，无新依赖） |
</threat_model>

<verification>
- `cd server && uv run pytest tests/workflows/test_clarification_card_node.py tests/feishu/test_clarify_card_callback.py -x -q` 全绿。
- `cd server && uv run pytest tests/workflows tests/feishu tests/delivery -q` 无回归（含 INV-6 子模型守护）。
- `cd server && uv run ruff format --check workflows/nodes/integrations/clarification_card.py feishu/callbacks/clarify_card_callback.py && uv run ruff check workflows/nodes/integrations/clarification_card.py feishu/callbacks/clarify_card_callback.py && uv run mypy workflows/nodes/integrations/clarification_card.py feishu/callbacks/clarify_card_callback.py`。
- `cd server && uv run python manage.py makemigrations --check`（无迁移）。
- `cd web && pnpm vitest run node-sync` 绿（node_count=37）。
</verification>

<success_criteria>
- clarification_card 节点注册 + 端口 shape 契约经 get_schema 暴露（可编排，validator 识别）。
- 发卡 → ClarifyCardCallback 订阅 → waiting_event → 独立回调收答 → answer_round 落库（INV-6）→ approve_node 本节点闭环（不绑 ai_plan_research/PlanSession）。
- fixture node_count 36→37，node-sync 守护绿。
</success_criteria>

<output>
Create `.planning/phases/92-capability-slot-backend/92-03-SUMMARY.md` when done
</output>
