---
phase: 146-mcp-contract-registry
plan: "05"
type: execute
wave: 5
depends_on: ["146-04"]
files_modified:
  - server/mcp_tools/views.py
  - server/mcp_tools/urls.py
  - server/tests/mcp_tools/test_manifest_api.py
  - mcp/src/server.ts
  - mcp/src/cli.ts
  - mcp/src/contractDiff.ts
  - mcp/tests/cli.test.ts
  - mcp/tests/server.test.ts
  - mcp/package.json
autonomous: true
requirements: [PUB-01, PUB-05]
must_haves:
  truths:
    - "已认证 manifest API 报告 package/build metadata server version 与 canonical on-disk bytes hash。"
    - "npm SERVER_VERSION 直接来自 package.json，无第二份字面量。"
    - "doctor 比较 bundled/deployed 同一 canonical manifest bytes/hash，准确报告 exact/additive/incompatible 与具体工具差异。"
  artifacts:
    - path: "server/tests/mcp_tools/test_manifest_api.py"
      provides: "manifest auth/version/exact-bytes hash/observability tests"
    - path: "mcp/src/contractDiff.ts"
      provides: "doctor compatibility diff pure functions"
    - path: "mcp/tests/cli.test.ts"
      provides: "doctor version/hash/diff/error/redaction tests"
  key_links:
    - from: "server/mcp_tools/views.py"
      to: "server/contracts/mcp-tools.v1.json"
      via: "read exact committed bytes and SHA-256"
      pattern: "mcp-tools.v1.json"
    - from: "mcp/src/cli.ts"
      to: "/api/mcp/manifest/"
      via: "authenticated PAT GET"
      pattern: "mcp/manifest"
    - from: "mcp/src/server.ts"
      to: "mcp/package.json"
      via: "resolveJsonModule version import"
      pattern: "package.json"
---

<objective>
提供 server manifest 身份、package-derived npm version 与安全 doctor 差异诊断，完成 PUB-01 的可验证 identity 面与 PUB-05。

Purpose: 外部 Agent 能判断本地 npm 与部署 server 的真实版本/契约关系，而不是把 hash 不同笼统视为失败。
Output: authenticated manifest endpoint、统一 version source、contract diff 与 doctor tests。
</objective>

<execution_context>
@./.cursor/gsd-core/workflows/execute-plan.md
@./.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/146-mcp-contract-registry/146-RESEARCH.md
@.planning/phases/146-mcp-contract-registry/146-04-SUMMARY.md
@server/contracts/mcp-tools.v1.json
@server/mcp_tools/manifest.py
@mcp/src/cli.ts
@mcp/package.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 发布 authenticated exact-bytes manifest identity</name>
  <files>server/mcp_tools/views.py, server/mcp_tools/urls.py, server/tests/mcp_tools/test_manifest_api.py</files>
  <behavior>
    - Test 1: GET `/api/mcp/manifest/` 返回 package/build metadata `server_version`、contract version、55-tool manifest 与 hash。
    - Test 2: hash 精确等于 committed `mcp-tools.v1.json` on-disk bytes SHA-256；API 不反序列化后重新 canonicalize。
    - Test 3: 未认证拒绝；响应/日志无 PAT、业务数据、job ID、堆栈、绝对路径。
    - Test 4: caller started/completed/failed 含 actor/correlation/component/duration/tool_count/hash/status，观测异常不影响响应。
  </behavior>
  <action>新增独立 authenticated GET，不改匿名 health。server version 按 research resolved decision 读取已安装 Friday package/build metadata，registry 不保存版本。API 读取 committed canonical JSON 的同一 exact bytes、计算 SHA-256 并返回解析 manifest。记录 `mcp_manifest_started/completed/failed`，category caller、component mcp_tools、duration_ms、user/request/source/trace、tool_count/hash/status；仅 ID/计数/hash，异常先脱敏，best-effort，不写 Ledger。</action>
  <verify>
    <automated>cd server &amp;&amp; uv run pytest tests/mcp_tools/test_manifest_api.py -q</automated>
  </verify>
  <done>manifest API 的 bytes/hash/version 来源唯一、已认证、可观测且不泄密。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 消除 npm SERVER_VERSION 字面量漂移</name>
  <files>mcp/package.json, mcp/src/server.ts, mcp/tests/server.test.ts</files>
  <behavior>
    - Test 1: exported SERVER_VERSION 严格等于 packageJson.version。
    - Test 2: server handshake 与 CLI banner 使用同一 export。
    - Test 3: repo 中不存在另一个手写 SERVER_VERSION 值。
  </behavior>
  <action>利用现有 `resolveJsonModule` 从 `package.json.version` 直接派生 SERVER_VERSION；不把 server Python version、git tag或里程碑号混作 npm client version。保持 package version 当前值，不升级依赖/Node/pnpm。</action>
  <verify>
    <automated>cd mcp &amp;&amp; pnpm test -- --run tests/server.test.ts &amp;&amp; pnpm typecheck</automated>
  </verify>
  <done>npm handshake、banner 与 package metadata 永远同源。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 实现 doctor version/hash/contract diff</name>
  <files>mcp/src/cli.ts, mcp/src/contractDiff.ts, mcp/tests/cli.test.ts</files>
  <behavior>
    - Test 1: doctor 输出 client/server versions、bundled/deployed hashes、missing_on_client/server、changed_tools。
    - Test 2: exact 和 server-only additive 返回 0（additive warning）；missing client tool 或 breaking schema 返回 nonzero。
    - Test 3: 401/403、404 old server、network、invalid JSON 分别稳定诊断并非零。
    - Test 4: 输出只含 token 长度、版本、hash、工具名；不含 token、manifest body、detail body、raw exception。
  </behavior>
  <action>用现有 config/PAT GET manifest endpoint；pure `contractDiff` 采用与 Python checker相同的兼容规则。doctor 只比较 deployed parsed manifest 与 bundled exact-bytes hash/catalog，不让远端覆盖本地 allowlist。hash 不同不可一律失败；404 明确表示旧 server 不支持诊断。导出可测试 runDoctor/exit code，保留注册状态和 latency。</action>
  <verify>
    <automated>cd mcp &amp;&amp; pnpm test -- --run tests/cli.test.ts &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
  </verify>
  <done>doctor 对 exact/additive/breaking/auth/old-server/network 均给出准确、安全、可机器判定结果。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| PAT client→manifest API | 认证凭证跨 HTTP；端点只返回契约元数据。 |
| deployed manifest→doctor | 远端 manifest 不可信，只比较不替换 bundled allowlist。 |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-146-14 | Spoofing | manifest identity | mitigate | auth + package/build metadata + exact file bytes hash。 |
| T-146-15 | Information Disclosure | logs/doctor | mitigate | 只输出版本/hash/工具名/计数/token 长度，原文不回显。 |
| T-146-16 | Tampering | remote manifest | mitigate | pure diff only，远端不能更新 bundled catalog。 |
| T-146-SC | Tampering | package installs | accept | 无新依赖或版本升级。 |
</threat_model>

<verification>
- `cd server && uv run pytest tests/mcp_tools/test_manifest_api.py -q`
- `cd mcp && pnpm test -- --run tests/server.test.ts tests/cli.test.ts && pnpm typecheck && pnpm build`
</verification>

<success_criteria>
- PUB-01/PUB-05 完成：npm version 单源，server/API hash 单源，doctor 能显示并分类具体差异。
- manifest hash 在 generator、API、npm bundled catalog 和 doctor 中均指向同一 on-disk bytes。
- endpoint/CLI 观测与输出满足 caller、actor/correlation、duration 和脱敏要求。
</success_criteria>

<output>
Create `.planning/phases/146-mcp-contract-registry/146-05-SUMMARY.md` when done.
</output>
