---
phase: 146-mcp-contract-registry
plan: "06"
type: execute
wave: 6
depends_on: ["146-05"]
files_modified:
  - .github/workflows/ci.yaml
  - server/tests/mcp_tools/test_contract_registry.py
  - server/tests/mcp_tools/test_contract_generation.py
  - server/tests/mcp_tools/test_mcp_package_alignment.py
  - mcp/.github/workflows/publish.yml
  - mcp/package.json
  - mcp/README.md
  - README.md
  - docs/integrations/mcp.md
autonomous: true
requirements: [PUB-01, PUB-03, PUB-05]
must_haves:
  truths:
    - "根 CI 在 mcp 缺失/未 checkout/generated drift 时 hard fail，并完整验证 registry→HTTP→manifest→npm stdio。"
    - "npm publish 只在 parent 已接纳同一 child SHA 且 compatibility/version/doctor/secret/build 门通过后执行。"
    - "所有 MCP 安装/注册文档钉住 package.json 当前版本并解释 doctor 结果。"
  artifacts:
    - path: ".github/workflows/ci.yaml"
      provides: "recursive-submodule mcp-contract-ci"
    - path: "mcp/.github/workflows/publish.yml"
      provides: "parent gitlink 与 authoritative contract publish gate"
    - path: "server/tests/mcp_tools/test_mcp_package_alignment.py"
      provides: "无 skip 的四面一致性硬门"
  key_links:
    - from: ".github/workflows/ci.yaml"
      to: "server/mcp_tools/management/commands/generate_mcp_contracts.py"
      via: "recursive checkout 后 --check 与 targeted tests"
      pattern: "generate_mcp_contracts --check"
    - from: "mcp/.github/workflows/publish.yml"
      to: "server/contracts/mcp-tools.compat.v1.json"
      via: "checkout parent main、验证 gitlink 后运行 authoritative gates"
      pattern: "compat"
---

<objective>
把 PUB-01/PUB-03/PUB-05 的一致性、版本与诊断证据接入 CI、npm publish 和版本钉扎文档。

Purpose: 防止缺 submodule 假绿、未被 parent 接纳的 child 包发布，以及 `npx -y` 默拉不同契约。
Output: root mcp-contract-ci、child publish guards、无 skip alignment tests 与同步文档。
</objective>

<execution_context>
@./.cursor/gsd-core/workflows/execute-plan.md
@./.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/146-mcp-contract-registry/146-RESEARCH.md
@.planning/phases/146-mcp-contract-registry/146-05-SUMMARY.md
@.github/workflows/ci.yaml
@mcp/.github/workflows/publish.yml
@mcp/package.json
@mcp/README.md
@docs/integrations/mcp.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 将生成与四面对齐设为根 CI 硬门</name>
  <files>.github/workflows/ci.yaml, server/tests/mcp_tools/test_contract_registry.py, server/tests/mcp_tools/test_contract_generation.py, server/tests/mcp_tools/test_mcp_package_alignment.py</files>
  <behavior>
    - Test 1: mcp 目录/`package.json`/generated catalog 缺失或 `git submodule status mcp` 以 `-` 开头时非零，不 skip/xfail。
    - Test 2: registry、真实 URL、canonical manifest、generated TS、stdio tools/list 的 55 工具全字段相等。
    - Test 3: generator --check、compatibility、manifest/output/error tests 与 npm frozen install/typecheck/test/build 均为 blocking steps。
  </behavior>
  <action>给 root push/PR paths 加 `mcp/**`、`.gitmodules` 与 contract/generator 路径；新增 `mcp-contract-ci` recursive checkout 和显式 existence/gitlink checks。依次运行 server locked sync、generator `--check`、registry/generation/compatibility/manifest/alignment/output/error tests，再运行 `pnpm install --frozen-lockfile`、typecheck、test、build。移除 alignment tests 中全部 skip/xfail 逃生口；无 continue-on-error。PUB-01 的五面一致性必须由真实 files/URL/stdio 对拍，不能只比较同一 Python 对象。</action>
  <verify>
    <automated>cd server &amp;&amp; uv run python manage.py generate_mcp_contracts --check &amp;&amp; uv run pytest tests/mcp_tools/test_contract_registry.py tests/mcp_tools/test_contract_generation.py tests/mcp_tools/test_contract_compatibility.py tests/mcp_tools/test_manifest_api.py tests/mcp_tools/test_mcp_package_alignment.py tests/mcp_tools/test_output_contracts.py tests/mcp_tools/test_mcp_auth_errors.py -q</automated>
  </verify>
  <done>root CI 对缺 submodule、生成漂移、breaking change 与任一消费面差异均明确失败。</done>
</task>

<task type="auto">
  <name>Task 2: 收紧 publish gate 并同步版本钉扎文档</name>
  <files>mcp/.github/workflows/publish.yml, mcp/package.json, mcp/README.md, README.md, docs/integrations/mcp.md</files>
  <action>publish 使用仓库声明的 Node/pnpm 与 frozen lockfile，删除 `npm@latest` 和非冻结 npm install。package scripts/prepublish 运行 catalog integrity、SERVER_VERSION、doctor synthetic、secret scan、test/typecheck/build。authoritative baseline 位于 parent，因此 workflow checkout Friday AI parent main 并 recursive 初始化 mcp，断言 parent gitlink 精确等于待发布 child SHA，再运行 parent generator `--check`、compatibility/alignment tests；未接纳 SHA 明确阻断，parent 合并后可 workflow_dispatch 重跑，禁止向 child 复制 baseline。把 setup/register/serve 的 `@friday-ai-codes/mcp` 命令全部钉到 `mcp/package.json.version`，说明 doctor 的 client/server version/hash、exact/additive warning/incompatible nonzero；文档无真实 token。不得升级 Node、pnpm 或依赖 major。</action>
  <verify>
    <automated>cd mcp &amp;&amp; pnpm install --frozen-lockfile &amp;&amp; pnpm typecheck &amp;&amp; pnpm test &amp;&amp; pnpm build</automated>
  </verify>
  <done>publish 只能发布 parent 已接纳且全门禁通过的 child SHA，文档命令版本固定并与 package metadata 一致。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| CI checkout→contract gate | submodule/generated 缺失必须失败。 |
| parent gitlink→npm publish | child commit 只有被 parent 接纳后可发布。 |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-146-17 | Tampering | CI | mitigate | recursive checkout、existence、--check、五面对拍且无 skip。 |
| T-146-18 | Tampering | npm publish | mitigate | parent gitlink==child SHA + authoritative compatibility/version/doctor/secret/build gates。 |
| T-146-19 | Spoofing | install docs | mitigate | npm package version 显式钉扎并由测试比对 package.json。 |
| T-146-SC | Tampering | package install | mitigate | committed lockfile + frozen pnpm；不使用 latest、不新增依赖。 |
</threat_model>

<verification>
- `cd server && uv run python manage.py generate_mcp_contracts --check`
- `cd server && uv run pytest tests/mcp_tools/test_contract_registry.py tests/mcp_tools/test_contract_generation.py tests/mcp_tools/test_contract_compatibility.py tests/mcp_tools/test_manifest_api.py tests/mcp_tools/test_mcp_package_alignment.py tests/mcp_tools/test_output_contracts.py tests/mcp_tools/test_mcp_auth_errors.py -q`
- `cd mcp && pnpm install --frozen-lockfile && pnpm typecheck && pnpm test && pnpm build`
</verification>

<success_criteria>
- PUB-03 缺 submodule/漂移硬失败且无 skip。
- PUB-01 五面一致性成为 CI 硬门。
- PUB-05 version/doctor/publish/doc pinning 全部可复核。
</success_criteria>

<source_audit>
SOURCE | ID | Feature/Requirement | Plans | Status
GOAL | — | server、HTTP、npm stdio 任一公开面契约一致、可验证、向后兼容 | 01-06 | COVERED
REQ | PUB-01 | registry/URL/schema/annotations/stdio 单一事实源 | 01,02,03,04,05,06 | COVERED
REQ | PUB-02 | 55 工具全部 stdio 可发现可调用 | 03 | COVERED
REQ | PUB-03 | mcp 缺失 hard fail 与生成 `--check` | 02,06 | COVERED
REQ | PUB-04 | 独立 baseline 拦截 breaking change | 01 | COVERED
REQ | PUB-05 | package version、manifest identity、doctor 与 publish | 05,06 | COVERED
REQ | PUB-06 | outputSchema、structuredContent、machine errors | 04 | COVERED
RESEARCH | — | canonical registry/schema/manifest/compat baseline | 01 | COVERED
RESEARCH | — | deterministic generator/generated artifacts/只读 check | 02 | COVERED
RESEARCH | — | URL cutover/npm 55 catalog/dirty handoff preservation | 03 | COVERED
RESEARCH | — | frozen actual-200 schemas/structuredContent/errors | 01,04 | COVERED
RESEARCH | — | manifest exact-bytes hash/version/doctor | 02,05 | COVERED
RESEARCH | — | CI/publish/docs/no new runtime dependencies | 06 | COVERED
RESEARCH | — | observability/security/redaction | 02,04,05,06 | COVERED
CONTEXT | — | 无 Phase 146 CONTEXT.md/D-XX；用户 checker corrections 为本轮锁定约束 | 01-06 | COVERED
</source_audit>

<output>
Create `.planning/phases/146-mcp-contract-registry/146-06-SUMMARY.md` when done.
</output>
