---
phase: 146-mcp-contract-registry
plan: "02"
type: execute
wave: 2
depends_on: ["146-01"]
files_modified:
  - server/mcp_tools/management/__init__.py
  - server/mcp_tools/management/commands/__init__.py
  - server/mcp_tools/management/commands/generate_mcp_contracts.py
  - server/mcp_tools/generated/__init__.py
  - server/mcp_tools/generated/schema_snapshot.py
  - server/contracts/mcp-tools.v1.json
  - server/tests/mcp_tools/test_contract_registry.py
  - server/tests/mcp_tools/test_contract_generation.py
  - mcp/src/generated/toolCatalog.ts
autonomous: true
requirements: [PUB-01, PUB-03]
must_haves:
  truths:
    - "一个确定性命令从 Plan 01 registry 生成 canonical JSON、Python snapshot 与唯一 npm catalog 路径。"
    - "`--check` 只读，任一目标缺失/漂移或 mcp submodule 未 checkout 时明确非零。"
    - "生成 manifest 不含 hash/runtime versions；manifest hash 是最终落盘 canonical JSON 精确 bytes 的 SHA-256。"
  artifacts:
    - path: "server/mcp_tools/management/commands/generate_mcp_contracts.py"
      provides: "多目标原子 generator 与只读 --check"
    - path: "server/contracts/mcp-tools.v1.json"
      provides: "不含自引用 hash/runtime version 的 canonical 55-tool bytes"
    - path: "server/mcp_tools/generated/schema_snapshot.py"
      provides: "generated 兼容 snapshot"
    - path: "mcp/src/generated/toolCatalog.ts"
      provides: "唯一 generated npm catalog；tools.ts 只在 Plan 03 做 barrel"
  key_links:
    - from: "server/mcp_tools/management/commands/generate_mcp_contracts.py"
      to: "server/mcp_tools/registry.py"
      via: "从冻结 registry 与 response serializers 一次渲染全部目标"
      pattern: "McpToolContract|build_manifest"
    - from: "mcp/src/generated/toolCatalog.ts"
      to: "server/contracts/mcp-tools.v1.json"
      via: "嵌入同一工具结构与 exact-bytes SHA-256"
      pattern: "CLIENT_MANIFEST_HASH"
---

<objective>
实现 registry 到三个 generated artifacts 的确定性生成与严格只读检查，完成 PUB-01 的生成链及 PUB-03 的 `--check` 基础。

Purpose: 让后续 URL/npm 消费现成生成物，避免把生成、运行时切换和 dirty submodule 合并成一个超大执行单元。
Output: management command、canonical manifest、generated snapshot、`mcp/src/generated/toolCatalog.ts` 与生成测试。
</objective>

<execution_context>
@./.cursor/gsd-core/workflows/execute-plan.md
@./.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/146-mcp-contract-registry/146-RESEARCH.md
@.planning/phases/146-mcp-contract-registry/146-PATTERNS.md
@.planning/phases/146-mcp-contract-registry/146-01-SUMMARY.md
@server/mcp_tools/registry.py
@server/mcp_tools/manifest.py
@server/tests/mcp_tools/test_contract_registry.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 建立 generator 与 canonical JSON bytes</name>
  <files>server/mcp_tools/management/__init__.py, server/mcp_tools/management/commands/__init__.py, server/mcp_tools/management/commands/generate_mcp_contracts.py, server/contracts/mcp-tools.v1.json, server/tests/mcp_tools/test_contract_generation.py</files>
  <behavior>
    - Test 1: 连续生成 canonical JSON 两次 bytes 完全一致，工具稳定按 name 排序且无时间戳/本机绝对路径。
    - Test 2: JSON 使用 UTF-8、sorted keys、紧凑 separators、单个结尾换行，并省略 `manifest_hash/generated_at/server_version/client_version`。
    - Test 3: hash 等于 `sha256(Path("server/contracts/mcp-tools.v1.json").read_bytes())`。
  </behavior>
  <action>先写 canonical bytes/hash 测试，再建立 management command 从 Plan 01 冻结 registry 渲染 JSON。普通模式先在内存渲染，再以同目录临时文件和原子 replace 落盘。compatibility baseline 不在 targets 集合。生成器不得扫描数据库或网络。</action>
  <verify>
    <automated>cd server &amp;&amp; uv run pytest tests/mcp_tools/test_contract_registry.py tests/mcp_tools/test_contract_generation.py -q</automated>
  </verify>
  <done>canonical JSON 可重复生成，exact on-disk bytes 与 SHA-256 定义唯一。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 生成 Python snapshot 与唯一 TS catalog</name>
  <files>server/mcp_tools/management/commands/generate_mcp_contracts.py, server/mcp_tools/generated/__init__.py, server/mcp_tools/generated/schema_snapshot.py, mcp/src/generated/toolCatalog.ts, server/tests/mcp_tools/test_contract_generation.py</files>
  <behavior>
    - Test 1: Python snapshot 与 TypeScript catalog 和 canonical JSON 的 55 工具全字段一致。
    - Test 2: generated TS 嵌入 exact-bytes hash 与全部 schema/annotations/endpoint。
    - Test 3: 唯一 TS 路径是 `mcp/src/generated/toolCatalog.ts`；不存在 `generated/tools.ts`，`mcp/src/tools.ts` 不由 generator 写。
  </behavior>
  <action>扩展同一命令渲染 Python snapshot 与 TypeScript catalog。明确文件命名决策：只生成 `mcp/src/generated/toolCatalog.ts`；后续 `mcp/src/tools.ts` 永远是 handwritten barrel，不允许第二 generated 名称。三个目标在全部渲染成功后才逐一原子替换。</action>
  <verify>
    <automated>cd server &amp;&amp; uv run pytest tests/mcp_tools/test_contract_generation.py -q</automated>
  </verify>
  <done>三个 generated targets 与同一 canonical manifest/hash 一致，TS 文件名唯一。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 实现只读 --check、缺失硬失败与安全日志</name>
  <files>server/mcp_tools/management/commands/generate_mcp_contracts.py, server/tests/mcp_tools/test_contract_generation.py</files>
  <behavior>
    - Test 1: `--check` 在一致树返回 0 且前后目标 bytes/git status 不变。
    - Test 2: 任一目标 missing/changed、`mcp/` 不存在或 submodule 未 checkout 均 `CommandError` 非零并列出目标，不 skip、不写回。
    - Test 3: generator 永不修改 `mcp-tools.compat.v1.json`、`mcp/src/server.ts`、`mcp/src/tools.ts` 或 handoff tests。
    - Test 4: started/completed/failed 日志仅含 category/component/duration、目标数/hash/diff_count 和 system actor；观测故障不改变命令结果。
  </behavior>
  <action>让 `--check` 只比较预期 bytes 与磁盘 bytes，输出精简差异；不允许隐式 regenerate 修复。按规范记录 `mcp_contract_generation_started/completed/failed`，`category="caller"`、`component="mcp_tools"`、`initiated_by_user_id="system"`、`duration_ms`，只记录路径标识/数量/hash，异常文本先脱敏，logging best-effort。添加静态保护，证明 compatibility baseline 与 handwritten mcp 文件不在可写目标集合。</action>
  <verify>
    <automated>cd server &amp;&amp; uv run pytest tests/mcp_tools/test_contract_generation.py -q &amp;&amp; uv run python manage.py generate_mcp_contracts --check</automated>
  </verify>
  <done>`--check` 是可用于 CI 的无副作用硬门，缺文件/脏文件/缺 submodule 均失败，日志合规且不反噬。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| registry→generated artifacts | 单一服务端声明跨 Python/JSON/TS 文件边界发布。 |
| parent→mcp submodule | generator 可写 generated 文件但不得覆盖 handwritten dirty handoff 工作。 |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-146-05 | Tampering | generator | mitigate | 稳定 bytes、原子 replace、只读 `--check` 与 exact SHA-256 tests。 |
| T-146-06 | Information Disclosure | generator logs | mitigate | 只记录路径标识、计数、hash、duration；正文与异常先脱敏。 |
| T-146-07 | Tampering | compatibility baseline | mitigate | baseline 永不进入 writable targets，静态测试锁定。 |
| T-146-SC | Tampering | package installs | accept | 无新依赖或安装任务。 |
</threat_model>

<verification>
- `cd server && uv run pytest tests/mcp_tools/test_contract_registry.py tests/mcp_tools/test_contract_generation.py -q`
- `cd server && uv run python manage.py generate_mcp_contracts --check`
</verification>

<success_criteria>
- PUB-01 的所有 generated projections 来自同一 registry，且 TS 路径无命名歧义。
- PUB-03 的 `--check` 在一致树零差异，在缺失/漂移/submodule 未 checkout 时硬失败。
- manifest hash 只定义为 canonical JSON 最终落盘精确 bytes 的 SHA-256。
</success_criteria>

<output>
Create `.planning/phases/146-mcp-contract-registry/146-02-SUMMARY.md` when done.
</output>
