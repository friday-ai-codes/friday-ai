---
phase: 146-mcp-contract-registry
plan: "04"
type: execute
wave: 4
depends_on: ["146-03"]
files_modified:
  - server/mcp_tools/response_serializers.py
  - server/mcp_tools/errors.py
  - server/mcp_tools/views.py
  - server/contracts/mcp-tools.v1.json
  - server/mcp_tools/generated/schema_snapshot.py
  - server/tests/mcp_tools/test_contract_registry.py
  - server/tests/mcp_tools/test_output_contracts.py
  - server/tests/mcp_tools/test_contract_compatibility.py
  - server/tests/mcp_tools/test_mcp_auth_errors.py
  - mcp/src/generated/toolCatalog.ts
  - mcp/src/server.ts
  - mcp/src/resultAdapters.ts
  - mcp/src/errors.ts
  - mcp/tests/server.test.ts
autonomous: true
requirements: [PUB-01, PUB-06]
must_haves:
  truths:
    - "Plan 01 冻结的 55 outputSchema 不被 structuredContent 实现做 incompatible 修改。"
    - "所有成功调用返回通过既有 outputSchema 的 structuredContent，并保留同源 JSON TextContent。"
    - "错误使用稳定机器码且不回显 PAT、上游正文、异常或堆栈。"
    - "若实际 fixture 暴露 schema 遗漏，只允许 additive correction，并重生成全部 artifacts、证明 compatibility additive-only；baseline 永不由 generator 写。"
  artifacts:
    - path: "server/tests/mcp_tools/test_output_contracts.py"
      provides: "Plan 01 actual-200 fixtures 的无回退验证"
    - path: "mcp/src/errors.ts"
      provides: "allowlisted machine error mapping"
    - path: "mcp/src/resultAdapters.ts"
      provides: "普通结果与 handoff 安全结果 adapter"
    - path: "mcp/src/server.ts"
      provides: "outputSchema、structuredContent 与兼容 TextContent"
  key_links:
    - from: "mcp/src/server.ts"
      to: "mcp/src/generated/toolCatalog.ts"
      via: "tools/list 直接暴露已冻结 outputSchema"
      pattern: "outputSchema"
    - from: "mcp/src/server.ts"
      to: "mcp/src/resultAdapters.ts"
      via: "一次组装 structured object 后派生两个通道"
      pattern: "structuredContent"
---

<objective>
在不改变 Plan 01 冻结 schema 语义的前提下实现 structuredContent 与稳定机器错误，完成 PUB-01 的协议面和 PUB-06。

Purpose: 外部 Agent 按机器契约编排，同时保住旧 TextContent 和 handoff 正文隔离。
Output: output conformance 回归、HTTP/stdio machine errors、result adapters 与协议测试。
</objective>

<execution_context>
@./.cursor/gsd-core/workflows/execute-plan.md
@./.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/146-mcp-contract-registry/146-RESEARCH.md
@.planning/phases/146-mcp-contract-registry/146-03-SUMMARY.md
@server/mcp_tools/response_serializers.py
@server/tests/mcp_tools/test_output_contracts.py
@mcp/src/generated/toolCatalog.ts
@mcp/src/server.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 锁定 outputSchema 无 incompatible 漂移</name>
  <files>server/mcp_tools/response_serializers.py, server/tests/mcp_tools/test_contract_registry.py, server/tests/mcp_tools/test_output_contracts.py, server/tests/mcp_tools/test_contract_compatibility.py</files>
  <behavior>
    - Test 1: Plan 01 每工具 actual HTTP 200 fixtures 继续通过其冻结 outputSchema。
    - Test 2: required 仍是全部合法 200 fixtures 的保守交集，禁止 blanket-all-fields。
    - Test 3: baseline→current 分类为 exact 或 additive，任何 property 删除/type/required/enum 收紧都失败。
  </behavior>
  <action>先运行 Plan 01 fixtures 和 compatibility gate，再接 stdio。不得为适配 structuredContent 删除/收紧任何 schema。若 fixture 暴露真实遗漏，只能 additive 修改 response serializer；随后必须运行 generator 重生成 `server/contracts/mcp-tools.v1.json`、`server/mcp_tools/generated/schema_snapshot.py`、`mcp/src/generated/toolCatalog.ts`，再次证明 compatibility 为 additive-only。`mcp-tools.compat.v1.json` 保持 generator-read-only且本任务不得修改。</action>
  <verify>
    <automated>cd server &amp;&amp; uv run pytest tests/mcp_tools/test_contract_registry.py tests/mcp_tools/test_output_contracts.py tests/mcp_tools/test_contract_compatibility.py -q &amp;&amp; uv run python manage.py generate_mcp_contracts --check</automated>
  </verify>
  <done>structured protocol 开工前 schema 已证明 exact/additive-only；任何必要 correction 已全量重生成且未写 baseline。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 统一 HTTP 与 stdio machine errors</name>
  <files>server/mcp_tools/errors.py, server/tests/mcp_tools/test_mcp_auth_errors.py, mcp/src/errors.ts, mcp/src/server.ts, mcp/tests/server.test.ts</files>
  <behavior>
    - Test 1: HTTP 保留 `{error_code,detail}` 并 additive 提供 retryable/run_id。
    - Test 2: stdio 非 2xx 返回 isError 与 `{error_code,http_status,retryable,run_id}` structuredContent。
    - Test 3: config/network/timeout/auth/invalid_json/unknown_tool/manifest_mismatch 使用固定 client codes。
    - Test 4: PAT、raw body、HTML、异常原文和堆栈不进入 text/structuredContent。
  </behavior>
  <action>扩展统一 error envelope 而不移除旧字段。stdio 仅解析 allowlisted machine fields，区分 timeout/network，删除 `bodyText.slice(...)` 和 raw exception 回显；401/403 使用稳定认证失败码，错误映射自身必须 fail-soft，不杀 stdio。中文 detail 只作安全兼容展示，不能承担控制流。</action>
  <verify>
    <automated>cd server &amp;&amp; uv run pytest tests/mcp_tools/test_mcp_auth_errors.py -q &amp;&amp; cd ../mcp &amp;&amp; pnpm test -- --run tests/server.test.ts</automated>
  </verify>
  <done>所有错误机器可判且不依赖中文，不泄露凭证、上游正文或实现堆栈。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 返回 schema-valid structuredContent 与 handoff 摘要</name>
  <files>mcp/src/resultAdapters.ts, mcp/src/server.ts, mcp/tests/server.test.ts</files>
  <behavior>
    - Test 1: 55 tools/list entries 均含 generated outputSchema。
    - Test 2: 普通成功 structuredContent 等于 HTTP JSON；TextContent 从同一对象序列化。
    - Test 3: 每个代表性成功结果通过冻结 outputSchema，run_id 传播不变。
    - Test 4: handoff 仍按 0700/0600/atomic rename 写完整原始 bytes，structuredContent/text 只含坐标、文件、hash、计数摘要。
  </behavior>
  <action>增加 structuredContent 并从同一结构对象生成兼容 TextContent。按 generated resultAdapter metadata 选择 handwritten handoff adapter；迁移现有持久化 helper 时逐字保留权限、原始 bytes、rename 和 SHA-256，不让 generator 生成 IO。禁止把 canonical_content/markdown/repository_tasks 正文挂入 handoff structuredContent。</action>
  <verify>
    <automated>cd mcp &amp;&amp; pnpm test -- --run tests/server.test.ts &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
  </verify>
  <done>55 个成功响应双通道同源且 schema-valid，handoff 模型上下文仍只有安全摘要。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| HTTP response→stdio | 服务端成功/错误正文进入本地 adapter。 |
| stdio result→model | structuredContent 直接进入 Agent 上下文。 |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-146-11 | Information Disclosure | result/error adapters | mitigate | allowlisted machine fields；handoff 仅路径/hash/计数；原文不回显。 |
| T-146-12 | Tampering | output schema | mitigate | actual-200 fixtures + additive-only compatibility + 全量 regeneration。 |
| T-146-13 | Repudiation | run_id | mitigate | 成功与错误保留安全 run_id，后续仍传 X-Friday-Run-ID。 |
| T-146-SC | Tampering | package installs | accept | 不新增依赖。 |
</threat_model>

<verification>
- `cd server && uv run pytest tests/mcp_tools/test_contract_registry.py tests/mcp_tools/test_output_contracts.py tests/mcp_tools/test_contract_compatibility.py tests/mcp_tools/test_mcp_auth_errors.py -q`
- `cd server && uv run python manage.py generate_mcp_contracts --check`
- `cd mcp && pnpm typecheck && pnpm test -- --run tests/server.test.ts && pnpm build`
</verification>

<success_criteria>
- PUB-01/PUB-06 的 output schema、structuredContent、TextContent 与 errors 全面对齐。
- Plan 01 schema 不发生 incompatible 漂移；必要 correction 仅 additive、全量 regenerate、baseline 只读。
- handoff 与错误脱敏边界保持 fail-closed。
</success_criteria>

<output>
Create `.planning/phases/146-mcp-contract-registry/146-04-SUMMARY.md` when done.
</output>
