---
phase: 146-mcp-contract-registry
plan: "03"
type: execute
wave: 3
depends_on: ["146-02"]
files_modified:
  - server/mcp_tools/urls.py
  - server/mcp_tools/views.py
  - server/mcp_tools/serializers.py
  - server/tests/mcp_tools/test_contract_registry.py
  - server/tests/mcp_tools/test_schema_snapshot.py
  - server/tests/mcp_tools/test_mcp_package_alignment.py
  - mcp/src/generated/toolCatalog.ts
  - mcp/src/tools.ts
  - mcp/src/server.ts
  - mcp/tests/server.test.ts
autonomous: true
requirements: [PUB-01, PUB-02]
must_haves:
  truths:
    - "Django 55 条旧 URL/route name 与 runtime request serializer 改由 registry 投影且行为不变。"
    - "npm tools/list 与 generated catalog 暴露同一 55 工具、endpoint、schema 和 annotations，12 个历史缺口均可调用。"
    - "`mcp/src/tools.ts` 只做 `mcp/src/generated/toolCatalog.ts` 的类型/barrel re-export。"
    - "现有 confirmed handoff 0700/0600/atomic rename/小摘要未提交改动完整保留。"
  artifacts:
    - path: "server/mcp_tools/urls.py"
      provides: "registry-generated backward-compatible URL patterns"
    - path: "server/tests/mcp_tools/test_contract_registry.py"
      provides: "registry 与实际 URL/view/request serializer 对拍"
    - path: "mcp/src/tools.ts"
      provides: "稳定 handwritten barrel；不含第二份 catalog"
    - path: "server/tests/mcp_tools/test_mcp_package_alignment.py"
      provides: "server manifest、generated TS 与 stdio catalog 全字段对齐"
  key_links:
    - from: "server/mcp_tools/urls.py"
      to: "server/mcp_tools/registry.py"
      via: "build_tool_urlpatterns"
      pattern: "build_tool_urlpatterns"
    - from: "mcp/src/tools.ts"
      to: "mcp/src/generated/toolCatalog.ts"
      via: "barrel re-export"
      pattern: "generated/toolCatalog"
    - from: "mcp/src/server.ts"
      to: "mcp/src/generated/toolCatalog.ts"
      via: "allowlisted endpoint dispatch"
      pattern: "endpoint"
---

<objective>
切换 Django URL/runtime validation 与 npm stdio 到 generated contract，并关闭 55/43 工具缺口，完成 PUB-01 的消费面和 PUB-02。

Purpose: 将 generator 与运行时迁移分离，给 dirty mcp handoff 工作独立保护边界。
Output: registry URL cutover、generated snapshot re-export、55-tool npm barrel/dispatch 与跨面测试。
</objective>

<execution_context>
@./.cursor/gsd-core/workflows/execute-plan.md
@./.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/146-mcp-contract-registry/146-RESEARCH.md
@.planning/phases/146-mcp-contract-registry/146-PATTERNS.md
@.planning/phases/146-mcp-contract-registry/146-02-SUMMARY.md
@server/mcp_tools/registry.py
@server/mcp_tools/generated/schema_snapshot.py
@mcp/src/generated/toolCatalog.ts
@mcp/src/server.ts
@mcp/tests/server.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 切换 URL、request serializer 与 snapshot 投影</name>
  <files>server/mcp_tools/urls.py, server/mcp_tools/views.py, server/mcp_tools/serializers.py, server/tests/mcp_tools/test_contract_registry.py, server/tests/mcp_tools/test_schema_snapshot.py</files>
  <behavior>
    - Test 1: 55 个 `(name,path,route_name,view_class)` 与 Plan 01 characterization fixture 完全一致。
    - Test 2: McpToolView 按自身 class 从 registry 懒取 tool name/request serializer，避免 views↔registry import cycle。
    - Test 3: 旧 `TOOL_SCHEMA_SNAPSHOT` import 继续可用且来自 generated module。
    - Test 4: task 私有 `await_blueprint_context` 与 container callback MCP 不进入 public registry。
  </behavior>
  <action>让 `urls.py` 使用 `build_tool_urlpatterns()`，保持路径 `tools/{name}/` 与 route name 逐字兼容。让 `McpToolView._validate` 默认取 registry 绑定 serializer，移除各 post 的重复选择但不改变 auth、permission、service、Ledger、metric、run_id。`serializers.py` 只 re-export generated snapshot。PUB-01 要求实际 runtime 与生成时用同一绑定；禁止把私有 task/callback 工具纳入 public 55。</action>
  <verify>
    <automated>cd server &amp;&amp; uv run pytest tests/mcp_tools/test_contract_registry.py tests/mcp_tools/test_schema_snapshot.py -q</automated>
  </verify>
  <done>55 条 HTTP URL 与旧 reverse 完全兼容，runtime validation 与 generated schema 同源。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 启用 npm 55-tool barrel 与 allowlisted endpoint dispatch</name>
  <files>mcp/src/generated/toolCatalog.ts, mcp/src/tools.ts, mcp/src/server.ts, mcp/tests/server.test.ts, server/tests/mcp_tools/test_mcp_package_alignment.py</files>
  <behavior>
    - Test 1: npm/server 的 name、endpoint、inputSchema、outputSchema、annotations、manifest hash 全等。
    - Test 2: stdio tools/list 包含 manifest 全集，12 个历史缺失工具逐项存在并可经 mocked call 到 generated endpoint。
    - Test 3: `mcp/src/tools.ts` 不再保存 tool literal/annotations，只 re-export `generated/toolCatalog.ts`。
    - Test 4: handoff 文件权限、原始 bytes、atomic rename、hash 与正文不进摘要测试保持通过。
  </behavior>
  <action>迁移 npm 到唯一 `mcp/src/generated/toolCatalog.ts`，`tools.ts` 保持 handwritten barrel；server 使用 tool definition.endpoint，未知 name fail-closed。alignment test 读取 canonical JSON/generated exports，不再正则解析手写数组，也不允许缺 mcp 时 skip。明确验证 12 个缺口工具。执行前保存当前 `git -C mcp diff -- src/server.ts src/tools.ts tests/server.test.ts`；合并现有 +101/-4 handoff 工作，禁止 reset/checkout/clean 或从旧 42/43 工具版本覆盖。child 变更单独提交后再由父仓更新 gitlink，不混入无关 dirty 文件。</action>
  <verify>
    <automated>cd server &amp;&amp; uv run pytest tests/mcp_tools/test_contract_registry.py tests/mcp_tools/test_mcp_package_alignment.py -q &amp;&amp; cd ../mcp &amp;&amp; pnpm test -- --run tests/server.test.ts &amp;&amp; pnpm typecheck</automated>
  </verify>
  <done>stdio 发现/调用全部 55 工具，四面契约一致，tools.ts 仅 barrel，handoff 安全工作不丢失。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| registry→Django URL | 公开 HTTP 路由由声明动态投影，必须保持旧路径与 auth。 |
| generated catalog→stdio HTTP | 不受信工具名只能映射到 allowlisted endpoint。 |
| parent→dirty mcp child | 迁移不得覆盖未提交 handoff 安全实现。 |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-146-08 | Elevation of Privilege | stdio dispatch | mitigate | generated allowlist + fixed endpoint，未知工具拒绝。 |
| T-146-09 | Tampering | URL projection | mitigate | 独立 55 tuple characterization 与 reverse tests。 |
| T-146-10 | Information Disclosure | handoff | mitigate | 保留 0700/0600、原子 rename 与仅摘要测试。 |
| T-146-SC | Tampering | package installs | accept | 无新依赖；仅运行已有 pnpm scripts。 |
</threat_model>

<verification>
- `cd server && uv run pytest tests/mcp_tools/test_contract_registry.py tests/mcp_tools/test_schema_snapshot.py tests/mcp_tools/test_mcp_package_alignment.py -q`
- `cd mcp && pnpm test -- --run tests/server.test.ts && pnpm typecheck`
</verification>

<success_criteria>
- PUB-01 的 HTTP/runtime/npm 消费面全部绑定 canonical registry/generated catalog。
- PUB-02 的 55 工具与 12 个历史缺口全部可 list/call。
- generated 文件命名唯一，tools.ts 只做 barrel，handoff 工作完整保留。
</success_criteria>

<output>
Create `.planning/phases/146-mcp-contract-registry/146-03-SUMMARY.md` when done.
</output>
