---
phase: 22-fail-closed
plan: 02
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - server/services/code_parser.py
  - server/services/indexer.py
  - server/tests/services/test_indexer_exclusion.py
autonomous: true
requirements: [EXCL-02]
must_haves:
  truths:
    - "索引扫描阶段被排除文件不进入索引（full + incremental 两条扫描路径）"
    - "scan_directory 注释不再谎称已应用 .gitignore（PF-04 修正），真实过滤口径如实描述"
    - "排除判定复用 Plan 01 单一匹配器，相对仓库根 POSIX 路径，扫描期 fail-closed"
  artifacts:
    - path: "server/services/code_parser.py"
      provides: "scan_directory 支持注入 matcher / 修正注释"
      contains: "def scan_directory"
    - path: "server/services/indexer.py"
      provides: "full + incremental 扫描挂接 build_matcher_for_repo 过滤"
      contains: "build_matcher_for_repo"
  key_links:
    - from: "server/services/indexer.py:run_full_index"
      to: "services.exclusion.build_matcher_for_repo"
      via: "扫描后按相对路径过滤 files_to_process"
      pattern: "build_matcher_for_repo"
    - from: "server/services/indexer.py:run_incremental_index"
      to: "matcher.is_excluded"
      via: "local_hashes/diff 计算前过滤被排除文件"
      pattern: "is_excluded"
---

<objective>
把 Plan 01 的单一匹配器挂接到**索引扫描面**：full index（`run_full_index`，scan_directory ~812）与 incremental index（`run_incremental_index`，scan_directory ~2114）两条路径在构建待索引文件集时剔除被排除文件，使新内容不会进入 Qdrant/FileIndex/codegraph。同时修正 PF-04 —— `indexer.py` ~833 注释谎称"已应用 .gitignore"，`code_parser.scan_directory` 实际仅按目录名 + 扩展名白名单。

Purpose: 这是 EXCL-02 四个读取面的第一面（索引扫描）。从源头不索引被排除文件，是 fail-closed 的最强形态。
Output: scan_directory 修正 + indexer 两路径挂接 + 守护测试。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-fail-closed/22-CONTEXT.md
@.planning/PREFLIGHT.md
@.planning/phases/22-fail-closed/22-01-PLAN.md
@server/services/exclusion.py
</context>

<artifacts_this_phase_produces>
- `server/services/code_parser.py:scan_directory`：新增可选 `extra_excluded(rel_path)->bool` 回调参数（或接收编译后的 matcher），用于在扩展名白名单之外应用相对路径级排除；修正其 docstring（不提 .gitignore）。
- `server/services/indexer.py`：full + incremental 路径在扫描后用 `build_matcher_for_repo(self.repository_id)` 过滤；移除/改写 ~833 谎称 .gitignore 注释。
- `server/tests/services/test_indexer_exclusion.py`：扫描排除守护测试。
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 修正 scan_directory（PF-04）并支持相对路径级排除回调</name>
  <files>server/services/code_parser.py, server/services/indexer.py</files>
  <read_first>
    - server/services/code_parser.py（scan_directory ~642：os.walk + dirs 过滤 + 扩展名白名单；返回绝对路径列表）
    - server/services/indexer.py（~812 run_full_index 调用点；~833 谎称 .gitignore 的注释；~2114 incremental 调用点）
    - server/services/exclusion.py（ExclusionMatcher.is_excluded、normalize_rel_path —— 口径以相对仓库根 POSIX）
  </read_first>
  <behavior>
    - scan_directory 新增可选参数（matcher 或 `is_excluded_rel: Callable[[str], bool] | None`），在产出文件时按「相对 directory 根的 POSIX 路径」调用判定，命中即跳过；同时让目录级裁剪（dirs[:]）也能利用 matcher 的 dir 规则尽早剪枝（性能优化，不改变结果）。
    - 既有 exclude_patterns（目录名粗粒度）行为保留，向后兼容（不传新参数时与现状字节等价）。
    - 修正 scan_directory docstring：如实写明"按目录名 + 扩展名白名单 + 可选排除匹配器过滤"，不再声称 .gitignore。
  </behavior>
  <action>
    在 code_parser.scan_directory 增加可选排除判定回调（不引入对 services.exclusion 的硬依赖，保持纯函数 + 调用方注入，避免循环导入）。在 os.walk 内：对 dirs 先做名字级裁剪（现状）再对剩余文件按相对路径回调判定。修正 docstring 与 indexer.py ~833 注释（PF-04）。注意 scan_directory 返回绝对路径，相对路径需以 directory 为 base 计算后再喂回调。
  </action>
  <verify>
    <automated>cd server && python -m pytest tests/services/test_indexer_exclusion.py -k scan_directory -x -q</automated>
  </verify>
  <acceptance_criteria>
    - 不传排除回调时，scan_directory 输出与改动前一致（向后兼容断言）。
    - 传入命中 `.env` / `secrets/` 的回调时，对应文件不出现在返回列表。
    - `grep -n "gitignore" server/services/indexer.py server/services/code_parser.py` 不再出现"已应用 .gitignore"谎称表述（PF-04 修正）。
  </acceptance_criteria>
  <done>scan_directory 支持相对路径排除且注释如实，PF-04 关闭。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: indexer full + incremental 扫描挂接排除过滤（fail-closed）</name>
  <files>server/services/indexer.py, server/tests/services/test_indexer_exclusion.py</files>
  <read_first>
    - server/services/indexer.py（run_full_index ~767-900：files=scan_directory → files_to_process 构建；run_incremental_index ~2074-2160：files=scan_directory → local_hashes → _compute_diff）
    - server/services/exclusion.py（build_matcher_for_repo async 加载器 + 缓存）
    - server/tests/services 既有 indexer 相关测试结构（命名/fixture idiom）
  </read_first>
  <behavior>
    - run_full_index：scan_directory 后（或经回调）确保被排除文件不进入 files_to_process；matcher 通过 build_matcher_for_repo(self.repository_id) 异步获取一次，传入 scan_directory 回调或在循环内 is_excluded 判定。
    - run_incremental_index：构建 local_hashes 时跳过被排除文件，使其在 _compute_diff 里既不算 ADD 也不算 UPDATE（被排除文件如已被索引，其清理交给 Phase 23；本阶段只保证扫描不再新增/更新它们）。
    - 扫描期判定异常 → fail-closed（跳过该文件，不索引）。
    - 不改 Qdrant/FileIndex/ChunkRegistry 写入结构（per D-04）。
  </behavior>
  <action>
    在 run_full_index 与 run_incremental_index 的扫描段调用 build_matcher_for_repo 获取 matcher，并把「相对仓库根路径」判定接入 scan_directory（首选注入回调）或在既有 rel_path 计算处加 is_excluded 判定跳过。matcher 为 async 加载，注意在 async 方法内 await，scan_directory 为同步——用预取 matcher 后传同步回调（matcher.is_excluded 是纯同步）。保持续传锚点逻辑不被破坏（被排除文件本就不该进 files_to_process）。
  </action>
  <verify>
    <automated>cd server && python -m pytest tests/services/test_indexer_exclusion.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - 守护测试：构造含 `.env`、`config/secret.json`、正常 `app.py` 的临时仓库目录 + 一条命中 `*.env`/`secrets` 的 per-repo 规则；run_full_index 后断言被排除文件未写入 FileIndex / 未进入 files_to_process（可 mock parse 计数或断言 FileIndex 集合）。
    - incremental 路径：被排除文件不出现在 _compute_diff 的 added/modified。
    - 内置全局默认（无任何 per-repo 规则）即可使 `.env` 不被索引（断言开箱即用）。
    - 匹配异常注入 → 该文件被跳过（fail-closed），不抛出中断整个索引。
  </acceptance_criteria>
  <done>两条扫描路径均 fail-closed 过滤被排除文件，守护测试全绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 仓库工作树文件 → 索引管线 | 工作树可能含密钥/敏感文件；进入索引即长期可被检索泄漏 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-05 | Information Disclosure | scan_directory 漏过被排除文件 → 进入 Qdrant | mitigate | 扫描期统一匹配器过滤 + 守护测试（full+incremental）；内置默认兜底 |
| T-22-06 | Repudiation | PF-04 谎称 .gitignore 误导后续维护者以为已过滤 | mitigate | 修正注释/docstring 如实描述；grep gate 断言无谎称表述 |
| T-22-07 | Information Disclosure | 扫描期判定异常 fail-open 仍索引 | mitigate | except → 跳过该文件（fail-closed） |
</threat_model>

<verification>
- `python -m pytest tests/services/test_indexer_exclusion.py -q` 全绿。
- `grep -rn "已应用 .gitignore" server/services/` 0 命中。
- 既有 indexer 测试不回归（`pytest tests/services -k index -q` 抽样）。
</verification>

<success_criteria>
- 被排除文件在索引扫描（full + incremental）不可见，PF-04 修正完成。
</success_criteria>

<output>
Create `.planning/phases/22-fail-closed/22-02-SUMMARY.md` when done.
</output>
