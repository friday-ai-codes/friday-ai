---
phase: 22-fail-closed
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/repositories/models.py
  - server/repositories/migrations/0032_repo_exclusion_rule.py
  - server/system/models.py
  - server/services/exclusion.py
  - server/tests/services/test_exclusion_matcher.py
autonomous: true
requirements: [EXCL-01, EXCL-02]
must_haves:
  truths:
    - "存在单一匹配器 is_excluded(repository_id, rel_path) 作为所有读取面唯一判定入口"
    - "全局默认规则内置一组安全模式（.env / *.pem / id_rsa / .git/ / node_modules/ 等），开箱即用"
    - "匹配器支持 dir / glob / regex 三类规则，相对仓库根 POSIX 路径匹配"
    - "匹配/归一化出错时 fail-closed（视为排除，宁可多排不可漏）"
    - "非法 regex 在保存校验阶段 fail-loud（构造匹配器时拒绝），运行期匹配异常 fail-closed"
  artifacts:
    - path: "server/services/exclusion.py"
      provides: "ExclusionMatcher + ExclusionRuleSpec + BUILTIN_GLOBAL_DEFAULTS + build_matcher_for_repo + is_excluded + log_exclusion_blocked"
      min_lines: 120
    - path: "server/repositories/models.py"
      provides: "RepoExclusionRule model"
      contains: "class RepoExclusionRule"
    - path: "server/system/models.py"
      provides: "SettingKeys.CODE_INDEX_EXCLUSION_GLOBAL_DEFAULTS"
      contains: "CODE_INDEX_EXCLUSION_GLOBAL_DEFAULTS"
    - path: "server/repositories/migrations/0032_repo_exclusion_rule.py"
      provides: "RepoExclusionRule 建表迁移（仅加表，不回填）"
      contains: "RepoExclusionRule"
  key_links:
    - from: "server/services/exclusion.py:build_matcher_for_repo"
      to: "RepoExclusionRule + SystemSetting(CODE_INDEX_EXCLUSION_GLOBAL_DEFAULTS)"
      via: "async ORM (sync_to_async) 合并 全局默认 ∪ per-repo 规则"
      pattern: "RepoExclusionRule\\.objects"
    - from: "server/services/exclusion.py:is_excluded"
      to: "ExclusionMatcher.is_excluded"
      via: "编译规则一次 + 复用 + 失败 fail-closed"
      pattern: "ExclusionMatcher"
---

<objective>
建立排除配置的**单一事实源**与**单一匹配器**：新增 per-repo `RepoExclusionRule` 模型、全局默认 `SystemSetting` 键，以及 `services/exclusion.py` 中编译一次/复用的 `ExclusionMatcher` 与统一入口 `is_excluded(repository_id, rel_path)`。后续所有读取面（索引扫描、MCP/agent 工具、RAG 检索、编码容器）都只调用此入口，保证判定一致与 fail-closed。

Purpose: 没有单一源与单一匹配器，各读取面会各写一套过滤，必然漂移、留缝（这正是本里程碑要堵的安全漏洞）。本 plan 是 Wave 2 全部 enforcement plan 的地基。
Output: 数据模型 + 迁移 + 设置键 + 匹配器模块 + 单测。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/22-fail-closed/22-CONTEXT.md
@.planning/DOMAIN-MODEL.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
本 plan 新增符号（供 Wave 2 plans 直接引用）：
- `server/services/exclusion.py`
  - `ExclusionRuleSpec`（@dataclass(frozen=True)：`pattern: str`、`rule_type: str`、`enabled: bool=True`、`source: str="user"`）
  - `RuleType` 常量集合 / `Literal["dir","glob","regex"]`
  - `BUILTIN_GLOBAL_DEFAULTS: list[ExclusionRuleSpec]`（内置安全默认）
  - `class ExclusionMatcher`：`__init__(rules)`（编译一次）、`is_excluded(rel_path: str) -> bool`
  - `normalize_rel_path(path: str, base: str | None = None) -> str`
  - `async def build_matcher_for_repo(repository_id: str) -> ExclusionMatcher`（合并 builtin ∪ 全局设置 ∪ per-repo，应用 per-repo override；带 TTL 缓存 `_matcher_cache` + `invalidate_matcher_cache(repository_id)`）
  - `async def is_excluded(repository_id: str, rel_path: str) -> bool`（统一入口）
  - `def log_exclusion_blocked(*, surface: str, repository_id: str, rel_path: str) -> None`（结构化审计埋点 `exclusion.blocked`）
  - `class InvalidExclusionRuleError(ValueError)`（非法 regex 等 fail-loud）
- `server/repositories/models.py`：`class RepoExclusionRule`
- `server/system/models.py`：`SettingKeys.CODE_INDEX_EXCLUSION_GLOBAL_DEFAULTS = "code_index.exclusion.global_defaults"`
- `server/repositories/migrations/0032_repo_exclusion_rule.py`
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RepoExclusionRule 模型 + 全局默认设置键 + 迁移</name>
  <files>server/repositories/models.py, server/system/models.py, server/repositories/migrations/0032_repo_exclusion_rule.py</files>
  <read_first>
    - server/repositories/models.py（Repository 模型 ~141；UUID PK + auto 时间戳 idiom；末尾追加新模型）
    - server/system/models.py（SettingKeys 类 ~29；新增键常量；SystemSetting 表）
    - server/repositories/migrations/0031_convert_ssh_git_urls.py（确认最新迁移号 → 新迁移依赖 0031）
  </read_first>
  <behavior>
    - RepoExclusionRule 字段：`id`(UUID PK)、`repository`(FK Repository, CASCADE, related_name="exclusion_rules")、`pattern`(CharField)、`rule_type`(CharField choices dir|glob|regex)、`enabled`(Bool default True)、`source`(CharField choices user|ai_suggested|global, default user)、`created_at`/`updated_at`(auto)。
    - source="global" + enabled=False 的行表示「关闭某条全局默认」的 override 标记。
    - unique_together=(repository, rule_type, pattern, source) 防重复；index(repository, enabled)。
    - SettingKeys 新增 CODE_INDEX_EXCLUSION_GLOBAL_DEFAULTS = "code_index.exclusion.global_defaults"，value 为 JSON 规则列表（结构 = ExclusionRuleSpec 序列化形）。
    - 迁移仅 CreateModel（不回填历史数据，per D-04）。
  </behavior>
  <action>
    在 server/repositories/models.py 末尾新增 `RepoExclusionRule`，沿用文件既有 UUID PK + auto 时间戳约定与中文 docstring。FK 指向 Repository，related_name="exclusion_rules"，on_delete=CASCADE。rule_type/source 用 TextChoices 内部类。加 unique_together 与 index（写在 Meta）。在 server/system/models.py 的 SettingKeys 类中按既有分组注释新增 `CODE_INDEX_EXCLUSION_GLOBAL_DEFAULTS` 常量（实现 EXCL-01 全局默认单一源，复用既有键体系，不绕过 SystemSetting）。运行 `python manage.py makemigrations repositories` 生成 0032，确认其 dependencies 指向 0031 且只含 CreateModel。注释说明本迁移不回填（清理是 Phase 23）。
  </action>
  <verify>
    <automated>cd server && python manage.py makemigrations --check --dry-run repositories && python -c "import django,os;os.environ.setdefault('DJANGO_SETTINGS_MODULE','friday.settings');django.setup();from repositories.models import RepoExclusionRule;from system.models import SettingKeys;assert SettingKeys.CODE_INDEX_EXCLUSION_GLOBAL_DEFAULTS=='code_index.exclusion.global_defaults'"</automated>
  </verify>
  <acceptance_criteria>
    - RepoExclusionRule 可导入；字段/choices/unique_together/index 与 behavior 一致。
    - SettingKeys.CODE_INDEX_EXCLUSION_GLOBAL_DEFAULTS 存在且值正确。
    - 迁移 0032 存在、依赖 0031、仅建表、`makemigrations --check` 干净（无悬挂变更）。
  </acceptance_criteria>
  <done>模型 + 设置键 + 迁移落地，migrate 通过，无未生成迁移。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ExclusionMatcher + 内置全局默认 + 单一匹配器（编译一次/复用，fail-closed）</name>
  <files>server/services/exclusion.py, server/tests/services/test_exclusion_matcher.py</files>
  <read_first>
    - server/services/code_parser.py（scan_directory ~642 现有目录名 exclude 列表，理解既有粗粒度排除，作为内置默认的参考集）
    - server/services/qdrant_service.py（~710 delete_by_file_path、file_path payload 用相对仓库根 POSIX 路径 —— 匹配器归一化口径必须与之一致）
    - server/common/logging.py（configure_structlog；结构化事件 idiom，审计埋点遵循同 idiom）
  </read_first>
  <behavior>
    - ExclusionRuleSpec：frozen dataclass(pattern, rule_type, enabled=True, source="user")。
    - ExclusionMatcher.__init__(rules)：过滤 enabled=True，按 rule_type 编译：dir → 归一化为以 "/" 结尾的前缀集合（匹配该目录及子树，并匹配目录本身）；glob → fnmatch/pathspec 编译，相对仓库根；regex → re.compile + 对相对路径 fullmatch。非法 regex 抛 InvalidExclusionRuleError（fail-loud，供保存校验用）。
    - normalize_rel_path：转 POSIX、去前导 "./"、折叠 "../" 越界 → 视为非法返回 None/抛错触发 fail-closed；大小写敏感跟随既有索引行为（不强制 lower）。
    - is_excluded(rel_path)：归一化后任一规则命中即 True；归一化失败或运行期匹配异常 → True（fail-closed）。
    - BUILTIN_GLOBAL_DEFAULTS 覆盖常见密钥/敏感与无意义目录：`.env`/`.env.*`(glob)、`*.pem`/`*.key`/`*.p12`/`*.keystore`/`*.pfx`(glob)、`id_rsa`/`id_dsa`/`id_ed25519`(glob)、`.git/`/`node_modules/`/`.ssh/`/`secrets/`(dir)、`*credentials*`/`*secret*.json`(glob 视情)。
    - build_matcher_for_repo(repository_id)：effective = (BUILTIN_GLOBAL_DEFAULTS ∪ 全局设置 JSON) 去掉「per-repo source=global & enabled=False」标记的同 pattern 项，再 ∪ per-repo enabled 规则。带 TTL 缓存（dict + monotonic 过期，参考 chat_tools._indexed_paths_cache idiom）+ invalidate_matcher_cache(repository_id)。ORM 访问走 sync_to_async。
    - is_excluded(repository_id, rel_path)（async 统一入口）：build_matcher_for_repo + matcher.is_excluded。
    - log_exclusion_blocked：logger.info("exclusion.blocked", surface=..., repository_id=..., rel_path=...)。
  </behavior>
  <action>
    新建 server/services/exclusion.py 实现上述符号，中文 docstring，ruff line-length 100。匹配器构造与运行期严格区分失败模式：构造期非法 regex → InvalidExclusionRuleError（fail-loud，Plan 05 保存 API 复用）；运行期匹配/归一化异常 → fail-closed 返回 True 并 log。缓存设计要可被 Plan 05 规则变更后 invalidate（导出 invalidate_matcher_cache）。BUILTIN_GLOBAL_DEFAULTS 措辞与 DOMAIN §9.1 安全边界一致（仅承诺"Friday 不可见"）。不改写任何既有写入结构（per D-04，只做判定层）。
  </action>
  <verify>
    <automated>cd server && python -m pytest tests/services/test_exclusion_matcher.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - dir 规则匹配目录本身与其子树文件（如 `node_modules/` 命中 `node_modules/a/b.js`）。
    - glob 规则按相对仓库根匹配（`.env` 命中根 `.env`、`*.pem` 命中 `certs/x.pem`）。
    - regex 规则对相对路径 fullmatch；非法 regex 在构造 ExclusionMatcher 时抛 InvalidExclusionRuleError。
    - 归一化越界路径（`../etc/passwd`）→ is_excluded 返回 True（fail-closed），不抛到调用方。
    - 运行期匹配异常（mock 触发）→ is_excluded 返回 True 且产生 `exclusion.blocked` 日志（surface 标记）可断言。
    - build_matcher_for_repo 正确合并 builtin ∪ 全局设置 ∪ per-repo，并按 source=global+enabled=False override 去除对应默认；二次调用命中缓存（ORM 仅查一次，可用 mock 计数断言）。
    - BUILTIN_GLOBAL_DEFAULTS 至少覆盖 .env / *.pem / id_rsa / .git/ / node_modules/（断言集合包含）。
  </acceptance_criteria>
  <done>匹配器 + 内置默认 + 合并加载器 + 审计埋点齐备，单测全绿，fail-closed/fail-loud 行为分明。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 用户/前端 → 规则配置（regex/glob） | 用户提供的 regex 是不可信输入（保存在 Plan 05，但匹配器在此定义校验入口） |
| 规则评估 → 读取面判定 | 任何归一化/匹配失败若 fail-open 即造成敏感文件泄漏 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | Information Disclosure | ExclusionMatcher.is_excluded 运行期异常 | mitigate | except → 返回 True（fail-closed）+ log_exclusion_blocked；单测覆盖异常路径 |
| T-22-02 | Tampering | 路径归一化绕过（`../`、绝对路径、symlink 名） | mitigate | normalize_rel_path 越界/绝对路径 → fail-closed True；以相对仓库根 POSIX 口径与 qdrant payload 对齐 |
| T-22-03 | Denial of Service | 用户 regex 灾难性回溯（ReDoS） | accept | 本 plan 在构造期 re.compile 校验非法语法（fail-loud）；ReDoS 复杂度限制留待 Plan 05 保存校验（标注），运行期匹配异常仍 fail-closed |
| T-22-04 | Information Disclosure | 全局默认缺失导致开箱即裸奔 | mitigate | BUILTIN_GLOBAL_DEFAULTS 即使无任何配置也生效（向后兼容 + 安全默认，per D-04） |
</threat_model>

<verification>
- `python -m pytest tests/services/test_exclusion_matcher.py -q` 全绿。
- `python manage.py makemigrations --check --dry-run repositories` 无悬挂迁移。
- grep 确认 is_excluded / ExclusionMatcher 为唯一判定实现（Wave 2 plans 不得另起炉灶）。
</verification>

<success_criteria>
- 单一匹配器入口 `is_excluded(repository_id, rel_path)` 可用且 fail-closed。
- per-repo 模型 + 全局默认设置键 + 内置安全默认就绪，支撑 EXCL-01 配置与 EXCL-02 拦截。
</success_criteria>

<output>
Create `.planning/phases/22-fail-closed/22-01-SUMMARY.md` when done.
</output>
