---
phase: 22-fail-closed
plan: 03
type: execute
wave: 3
depends_on: ["22-01", "22-02"]
files_modified:
  - server/agents/tools/chat_tools.py
  - server/agents/tools/space_tools.py
  - server/services/retrieval/rag_search.py
  - server/services/retrieval/hybrid_search.py
  - server/tests/agents/test_tools_exclusion.py
  - server/tests/services/test_retrieval_exclusion.py
autonomous: true
requirements: [EXCL-02]
must_haves:
  truths:
    - "MCP/agent 文件读取工具 browse_file_content 命中排除路径时拒读（不返回明文片段）"
    - "list_space_structure 文件树不展示被排除文件"
    - "RAG 检索（search_repository_code / HybridSearchService）结果在返回前剔除被排除文件，即使 Qdrant 仍有残留 point"
    - "图谱邻居（hop1/hop2/cross-repo）渲染行也剔除被排除文件路径"
    - "命中即 fail-closed 返回明确「已排除」信号，绝不降级泄漏明文"
  artifacts:
    - path: "server/agents/tools/chat_tools.py"
      provides: "browse_file_content / list_space_structure fail-closed 过滤"
      contains: "is_excluded"
    - path: "server/services/retrieval/rag_search.py"
      provides: "search_rag 结果按排除过滤（统一 RAG 链路 chokepoint）"
      contains: "is_excluded"
    - path: "server/services/retrieval/hybrid_search.py"
      provides: "图谱邻居渲染剔除被排除路径"
      contains: "exclud"
  key_links:
    - from: "server/services/retrieval/rag_search.py:search_rag"
      to: "services.exclusion.is_excluded / build_matcher_for_repo"
      via: "过滤 all_results 中 repository_id+file_path 命中排除项"
      pattern: "is_excluded|build_matcher_for_repo"
    - from: "server/agents/tools/chat_tools.py:browse_file_content"
      to: "services.exclusion.is_excluded"
      via: "scroll 前/后按 repository_id+file_path 拒读"
      pattern: "is_excluded"
---

<objective>
在 **进程内 chat/agent 工具面 + RAG 检索面** 挂接 fail-closed 过滤（注：externally-exposed MCP HTTP 工具 grep/get_file/list/find_related 的 bare-mirror 读取面由 22-06 覆盖，本 plan 只处理 `server/agents/tools/` 进程内工具 + 共享 RAG 链路）：
- `browse_file_content`（get_file 等价）命中排除路径 → 拒读，返回明确「已排除」信号，不返回任何 chunk 明文。
- `list_space_structure` 文件树过滤掉被排除文件。
- RAG 主线 `search_rag`（被 `search_repository_code` / HybridSearchService 共用）在返回前剔除被排除文件项；图谱邻居渲染（hop1/hop2/cross-repo）同样剔除。

即使 Qdrant 仍残留被排除文件的 point（存量清理是 Phase 23），读取面也绝不暴露。本 plan 还承载本阶段的**跨面 fail-closed 守护测试**（索引扫描 + browse + RAG 三面同一被排除文件均不可见）。

Purpose: EXCL-02 核心——读取/暴露面 fail-closed，不降级泄漏。`search_rag` 是 RAG 的单一 chokepoint，过滤一处即覆盖 chat/agent/workflow 所有 RAG 调用方。
Output: 工具/检索过滤 + 邻居过滤 + 跨面守护测试。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-fail-closed/22-CONTEXT.md
@.planning/DOMAIN-MODEL.md
@.planning/phases/22-fail-closed/22-01-PLAN.md
@.planning/phases/22-fail-closed/22-02-PLAN.md
@server/services/exclusion.py
</context>

<artifacts_this_phase_produces>
- `server/agents/tools/chat_tools.py`：browse_file_content 拒读分支（返回 error="File is excluded by policy" 形态，data.chunks=[]）；list_space_structure 过滤 file 列表。
- `server/agents/tools/space_tools.py`：search_repository_code 结果再保险过滤（依赖 search_rag 已过滤；此处仅断言不回流）。
- `server/services/retrieval/rag_search.py`：search_rag 在去重/截断前剔除被排除文件项（per repo matcher）。
- `server/services/retrieval/hybrid_search.py`：`_render_graph_context` / 邻居渲染剔除被排除 file_path。
- 测试：`tests/agents/test_tools_exclusion.py`、`tests/services/test_retrieval_exclusion.py`（含跨面守护）。
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RAG 链路 chokepoint —— search_rag + 图谱邻居过滤（fail-closed）</name>
  <files>server/services/retrieval/rag_search.py, server/services/retrieval/hybrid_search.py, server/tests/services/test_retrieval_exclusion.py</files>
  <read_first>
    - server/services/retrieval/rag_search.py（search_rag：all_results 每项 {**r, "repository_id"}，payload.file_path；去重 key=(repo_id,file_path,chunk_index)）
    - server/services/retrieval/hybrid_search.py（_render_graph_context ~130、_render_neighbor_line ~111、NeighborMetadata.file_path；search 分发 rag_only/graph_capable 两路径，二者均经 search_rag）
    - server/services/exclusion.py（build_matcher_for_repo + matcher.is_excluded；按 repository_id 取 matcher 后复用）
  </read_first>
  <behavior>
    - search_rag：在收集 all_results 时（或 sort/截断前），对每项按 item["repository_id"] + payload["file_path"] 判定排除，命中即丢弃；按 repo 预取 matcher（每个 repo 一次 build_matcher_for_repo，避免逐项加载）。命中时 log_exclusion_blocked(surface="rag")。判定异常 → 丢弃该项（fail-closed）。
    - hybrid_search 邻居渲染：_render_graph_context 中对 hop1/hop2/cross_repo 邻居按 file_path 过滤被排除项（neighbor 无 repository_id 时按其所属 repo_ids 集合判定——若邻居 metadata 无 repo 归属，保守地对传入的 repo_ids 逐一判定，任一命中即剔除，fail-closed）。
    - 不改 search_rag 的返回结构与既有 byte-eq 测试关注的格式（仅减少 items；空结果路径已有处理）。
  </behavior>
  <action>
    在 rag_search.search_rag 内对 repo 维度预取 matcher（dict[repo_id]→matcher via build_matcher_for_repo），在 append all_results 前用 matcher.is_excluded(payload.file_path) 过滤；命中 log。在 hybrid_search._render_graph_context（及/或 _filtered_sorted）加入被排除 file_path 过滤——为拿到 matcher，给 _render_graph_context 传入一个预构建的 `is_excluded_path: Callable[[str],bool]`（由 _search_graph_capable 在已知 repo_ids 时合成：对 repo_ids 的 matcher 做 any 命中）。保持 file_path=="<unknown>" 既有过滤不变。注意不要破坏 _search_rag_only 的 implementation byte-eq 测试（该路径不渲染 graph，仅受 search_rag 过滤影响——过滤后 items 变化属预期，更新相关 golden 断言或用新仓库数据隔离）。
  </action>
  <verify>
    <automated>cd server && python -m pytest tests/services/test_retrieval_exclusion.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - 构造 search_rag mock（BranchAwareSearchService.search 返回含被排除 file_path 的项）→ 返回 LayerSnapshot.items 不含被排除文件，含正常文件。
    - 命中排除产生 `exclusion.blocked` surface="rag" 日志（可断言）。
    - graph_capable 路径 final_context / graph_context 不含被排除文件路径的邻居行。
    - 判定异常注入 → 该项被丢弃（fail-closed），不抛出。
    - 每个 repo 的 matcher 只构建一次（mock build_matcher_for_repo 计数）。
  </acceptance_criteria>
  <done>RAG 单一 chokepoint + 图谱邻居均 fail-closed 过滤，测试全绿。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MCP/agent 工具面 —— browse_file_content 拒读 + list_space_structure 过滤</name>
  <files>server/agents/tools/chat_tools.py, server/agents/tools/space_tools.py, server/tests/agents/test_tools_exclusion.py</files>
  <read_first>
    - server/agents/tools/chat_tools.py（browse_file_content ~205：scroll by file_path；fuzzy resolve；list_space_structure ~417 构建文件树）
    - server/agents/tools/space_tools.py（search_repository_code ~247：调用 HybridSearchService；结果 file_path 来自 payload）
    - server/services/exclusion.py（is_excluded 统一 async 入口）
  </read_first>
  <behavior>
    - browse_file_content：在 scroll 之前用 is_excluded(repository_id, file_path) 判定；命中 → 直接返回 ToolResult(success=True, output={"data":{...,"chunks":[],"total_chunks":0},"error":"File is excluded by policy"}) 且 log_exclusion_blocked(surface="browse_file_content")。fuzzy resolve 命中真实路径后，也要对 resolved_path 再判定（防止经后缀匹配绕过）。绝不返回任何 chunk 明文。
    - list_space_structure：构建文件树时按 (repository_id, file_path) 过滤被排除文件（每 repo 预取 matcher）。
    - search_repository_code：results 已由 search_rag 过滤；此处加再保险——对返回 results 的 file_path 二次断言/过滤（防御未来新增不经 search_rag 的回流路径）。
  </behavior>
  <action>
    在 chat_tools.browse_file_content 入口（拿到 repository_id+file_path 后、scroll 前）加 is_excluded 判定与拒读返回；fuzzy 解析出 resolved_path 后再判定一次。list_space_structure 在收集 file 项处按 matcher 过滤（注意该工具可能跨多 repo，按各自 repo matcher）。space_tools.search_repository_code 在组装 all_results 后加一道 is_excluded 过滤兜底（命中 log surface="search_repository_code"）。所有命中走 log_exclusion_blocked。保持工具返回结构与既有契约一致（success=True + error 文案），不破坏既有 fuzzy/诊断逻辑。
  </action>
  <verify>
    <automated>cd server && python -m pytest tests/agents/test_tools_exclusion.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - browse_file_content 对被排除 file_path → chunks=[]、error 含"excluded"、无任何明文；产生 surface="browse_file_content" 日志。
    - fuzzy resolve 到被排除真实路径时同样拒读（断言后缀匹配不能绕过）。
    - list_space_structure 输出树不含被排除文件。
    - search_repository_code 即便底层强行返回被排除项也被兜底过滤掉。
  </acceptance_criteria>
  <done>工具读取面 fail-closed 拒读 + 过滤，不降级泄漏明文。</done>
</task>

<task type="auto">
  <name>Task 3: 跨面 fail-closed 守护测试（索引扫描 + browse + RAG 同一文件均不可见）</name>
  <files>server/tests/services/test_retrieval_exclusion.py, server/tests/agents/test_tools_exclusion.py</files>
  <read_first>
    - server/tests/services/test_indexer_exclusion.py（Plan 02 产出，复用其临时仓库 fixture idiom）
    - server/tests/agents/test_tools_exclusion.py（Task 2 产出）
    - .planning/phases/22-fail-closed/22-CONTEXT.md（specifics：四面守护测试要求）
  </read_first>
  <behavior>
    - 一个集成式守护测试：给定一条命中某敏感文件（如 `config/secret.env`）的排除规则，断言该文件在 (a) 索引扫描不进入待索引集 (b) browse_file_content 拒读 (c) search_rag/search_repository_code 结果不含它 —— 三面一致不可见。
    - 测试用内置全局默认 + 一条 per-repo 规则两种情形各跑一遍（验证 EXCL-01 两层都生效）。
  </behavior>
  <action>
    在 tests/services/test_retrieval_exclusion.py（或新建 tests/test_exclusion_failclosed_guard.py）写跨面守护，复用 Plan 02 的扫描 fixture 与 Task 2 的工具 mock。容器面（Plan 04）有其独立测试，本守护聚焦 server 内三面。断言要明确「不可见」而非「降级返回占位」。
  </action>
  <verify>
    <automated>cd server && python -m pytest tests/services/test_retrieval_exclusion.py tests/agents/test_tools_exclusion.py -q</automated>
  </verify>
  <acceptance_criteria>
    - 同一被排除文件在 索引扫描 / browse_file_content / RAG 三面断言均不可见。
    - 内置全局默认 与 per-repo 规则两种来源各自独立触发排除。
  </acceptance_criteria>
  <done>跨面 fail-closed 守护测试全绿，兑现 specifics 要求。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Qdrant 残留 point → 读取面 | 存量被排除文件 point 未清（Phase 23），读取面必须自行拦截 |
| 检索结果 → LLM 上下文 | 任何漏过的 file_path/明文进入 LLM 即泄漏且不可控扩散 |
| LLM/agent 请求 → 工具层 | 工具入参 file_path 不可信，可能尝试访问被排除文件 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-08 | Information Disclosure | browse_file_content 返回被排除文件明文 | mitigate | scroll 前拒读 + resolved_path 二次判定；返回 chunks=[] + error，绝不带明文 |
| T-22-09 | Information Disclosure | fuzzy 后缀匹配绕过排除（请求伪路径解析到被排除真实文件） | mitigate | 对 resolved_path 再次 is_excluded；守护测试覆盖 |
| T-22-10 | Information Disclosure | RAG 结果含 Qdrant 残留被排除文件 | mitigate | search_rag 单一 chokepoint 过滤 + space_tools 兜底过滤 |
| T-22-11 | Information Disclosure | 图谱邻居行泄漏被排除文件路径 | mitigate | _render_graph_context 过滤被排除 file_path |
| T-22-12 | Information Disclosure | 工具判定异常 fail-open 返回内容 | mitigate | except → 拒读/丢弃（fail-closed）+ log_exclusion_blocked |
</threat_model>

<verification>
- `python -m pytest tests/services/test_retrieval_exclusion.py tests/agents/test_tools_exclusion.py -q` 全绿。
- grep 确认 search_rag 为 RAG 唯一过滤点，无旁路（rg "BranchAwareSearchService.search" 调用方仅 search_rag）。
- 既有 hybrid_search byte-eq 测试已据过滤更新且通过。
</verification>

<success_criteria>
- 被排除文件在 MCP get_file（browse_file_content）/ 文件树 / RAG 检索 fail-closed 不可见，命中即拒读不降级。
</success_criteria>

<output>
Create `.planning/phases/22-fail-closed/22-03-SUMMARY.md` when done.
</output>
