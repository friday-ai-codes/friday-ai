---
phase: 88
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/initiatives/models/repo_association.py
  - server/initiatives/models/__init__.py
  - server/initiatives/migrations/0010_repo_association.py
  - server/subagent/models.py
  - server/agents/call_source.py
  - .planning/observability/LOGGING-SPEC.md
  - server/tests/initiatives/test_repo_association_models.py
autonomous: true
requirements: [REPO-01, REPO-02]
must_haves:
  truths:
    - "新增业务级「项目↔仓库关联」持久化（现状缺口：仅分支级 ProjectBranch / 项目级 ProjectRelation / 空间级 Space.repositories，无业务级 project↔repo 关联模型）——RepoAssociation 承载 D-05 净新增模型，镜像 RepoResearchTask 的 INV-6 范式"
    - "RepoAssociation 状态机 proposed→confirmed→verifying→verified|rejected 承载 D-01（选仓提案）→ D-03（用户确认）→ D-02（逐仓深验）→ 终态/回退（D-03 mismatch 可回退）整条交互回路状态"
    - "RepoVerifyTask 逐字镜像 RepoResearchTask/PartialPlan 形状（status pending/running/done/failed/stale + subagent_session SET_NULL 回填 + attempt + error JSON + verdict JSON），承载 D-02 per-repo 容器深验任务 + verdict 留痕"
    - "模型层零业务方法（无 create/save/状态变更），写入收口留给 RepoAssociationService（INV-6，88-02/88-03 落地）；跨 app FK 用字符串前向引用避免 import 环"
    - "SubAgentSession.TaskType 新增 REPO_VERIFY（便于 observability_views 按 task_type 区分容器深验，D-02）；容器侧按 task_mode=explore 分流不读 task_type（RESEARCH A1）"
    - "观测自检：新增 LLM 调用来源登记 repo_verify_container（容器深验 LLM）+ repo_association（Agent 自处理/候选细化 LLM），并登记 LOGGING-SPEC §4.1；既有 aux_repo_router 保留待 88-02 包裹"
  artifacts:
    - path: "server/initiatives/models/repo_association.py"
      provides: "RepoAssociation + RepoAssociationStatus + RepoVerifyTask + RepoVerifyTaskStatus（无业务方法）"
      contains: "class RepoAssociation"
    - path: "server/initiatives/migrations/0010_repo_association.py"
      provides: "纯 CreateModel 迁移（无回填）+ subagent AlterField choices"
      contains: "CreateModel"
    - path: "server/agents/call_source.py"
      provides: "CallSource.REPO_VERIFY_CONTAINER + CallSource.REPO_ASSOCIATION 新枚举值"
      contains: "REPO_VERIFY_CONTAINER"
    - path: ".planning/observability/LOGGING-SPEC.md"
      provides: "§4.1 新增 repo_verify_container / repo_association 行 + 枚举计数 25→27"
      contains: "repo_verify_container"
    - path: "server/subagent/models.py"
      provides: "SubAgentSession.TaskType.REPO_VERIFY 枚举值"
      contains: "REPO_VERIFY"
  key_links:
    - from: "server/initiatives/models/repo_association.py::RepoVerifyTask.association"
      to: "server/initiatives/models/repo_association.py::RepoAssociation"
      via: "FK CASCADE related_name=verify_tasks（一关联多 per-repo 校验任务）"
      pattern: "ForeignKey"
    - from: "server/initiatives/models/repo_association.py::RepoVerifyTask.subagent_session"
      to: "subagent.SubAgentSession"
      via: "FK SET_NULL（dispatch 容器后回填，删会话不删 task，镜像 RepoResearchTask）"
      pattern: "subagent.SubAgentSession"
---

<objective>
交付 Phase 88 的持久化与可观测枚举地基（D-05 净新增模型 + 观测登记）：新建业务级「项目↔仓库关联」+「per-repo 容器校验任务」两个持久化模型（现状确无此类模型），登记容器深验 / Agent 自处理两个新 `call_source`，并为容器深验任务新增 `SubAgentSession.TaskType.REPO_VERIFY`。

锁定决策（CONTEXT/RESEARCH）：D-05 净新增 `RepoAssociation`/`RepoVerifyTask`（镜像 `RepoResearchTask`/`PartialPlan` 的 INV-6 范式）；D-02 逐仓容器深验需可被 `observability_views` 按 task_type 区分 → 新增 `REPO_VERIFY`；观测强制——新 LLM 调用赋 `call_source` 并登记 §4.1。

Purpose: 为 88-02～88-05 的选仓服务 / 容器深验 / 卡片 HITL 提供真相源（DB canonical）与受控观测维度，所有写入收口单一 service（INV-6）由后续 plan 落地。
Output: `RepoAssociation`/`RepoVerifyTask` 模型 + 迁移 0010 + `SubAgentSession.TaskType.REPO_VERIFY` + `CallSource.{REPO_VERIFY_CONTAINER,REPO_ASSOCIATION}` + LOGGING-SPEC §4.1 登记 + 模型层测试。
</objective>

<execution_context>
@.planning/phases/88-repo-association/88-CONTEXT.md
@.planning/phases/88-repo-association/88-RESEARCH.md
@.planning/observability/LOGGING-SPEC.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@server/delivery/models/research_task.py
@server/initiatives/models/__init__.py
@server/subagent/models.py
@server/agents/call_source.py
@.cursor/rules/observability-logging.mdc
</context>

<artifacts_produced>
- NEW `server/initiatives/models/repo_association.py`：
  - `RepoAssociationStatus`：`PROPOSED="proposed"`/`CONFIRMED="confirmed"`/`VERIFYING="verifying"`/`VERIFIED="verified"`/`REJECTED="rejected"`。
  - `RepoAssociation`：`id` UUID PK；`project` FK `initiatives.Project`(CASCADE, related_name="repo_associations")；可选 `work_item` FK `delivery.WorkItem`(SET_NULL, null/blank, related_name="+")；`repository` FK `repositories.Repository`(CASCADE, related_name="+")；`status`(default PROPOSED)；`score` Float(default 0.0)；`confidence` Char(16, blank)；`routed_reason` Text(blank)；`source` Char(32, default "router_v2")；`matched_node_paths` JSON(default list)；`initiated_by_user_id` Char(64, blank, default "system")；`created_at`/`updated_at`。`Meta`：`db_table="initiatives_repo_association"`、`unique_together=(("project","repository"),)`、`indexes=[Index(["project","status"]), Index(["repository"])]`。模型层**无**业务方法。
  - `RepoVerifyTaskStatus`：`PENDING/RUNNING/DONE/FAILED/STALE`（逐字镜像 `RepoResearchTaskStatus`）。
  - `RepoVerifyTask`：`id` UUID PK；`association` FK `RepoAssociation`(CASCADE, related_name="verify_tasks")；`repository` FK `repositories.Repository`(CASCADE, related_name="+")；`subagent_session` FK `subagent.SubAgentSession`(SET_NULL, null/blank, related_name="+")；`status`(default PENDING)；`attempt` Int(default 0)；`error` JSON(default dict)；`verdict` JSON(default dict——schema `{fit, confidence, summary, evidence_files, mismatch_reasons}`，容器产 server 解析)；`initiated_by_user_id` Char(64, blank, default "system")；`created_at`/`updated_at`。`Meta`：`db_table="initiatives_repo_verify_task"`、`indexes=[Index(["association","status"]), Index(["repository"])]`。模型层**无**业务方法。
- EXTEND `server/initiatives/models/__init__.py`：re-export 四符号 + 加入 `__all__`。
- NEW `server/initiatives/migrations/0010_repo_association.py`：`makemigrations initiatives` 生成的纯 `CreateModel`（无 RunPython）；`dependencies` 含 0009 + repositories/subagent/delivery 最新迁移（makemigrations 自动解析）。
- EXTEND `server/subagent/models.py`：`SubAgentSession.TaskType` 新增 `REPO_VERIFY = "repo_verify", "Repo Verify"`；`makemigrations subagent` 生成 AlterField（choices 变更，无回填）。
- EXTEND `server/agents/call_source.py`：枚举末尾新增 `REPO_VERIFY_CONTAINER = "repo_verify_container"`（Phase 88 容器深验 LLM）+ `REPO_ASSOCIATION = "repo_association"`（Phase 88 Agent 自处理/候选细化 LLM）；更新模块/类 docstring「25 值」→「27 值」。
- EXTEND `.planning/observability/LOGGING-SPEC.md` §4.1：新增 `repo_verify_container` 行（入口=`RepoVerifyDispatchService` per-repo explore 容器，v0.16.0 Phase 88）+ `repo_association` 行（入口=`RepoAssociationService` 候选细化/Agent 自处理，v0.16.0 Phase 88）；同步标题处枚举计数。
- NEW `server/tests/initiatives/test_repo_association_models.py`：唯一约束 `(project,repository)`、默认 status=proposed、verdict/error 默认空 dict、REPO_VERIFY 枚举存在、call_source.normalize 命中。
- structlog：本 plan 仅建模型/枚举，无运行时事件；观测语义由 88-02/88-03 消费这些枚举落地。
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: RepoAssociation + RepoVerifyTask 模型（镜像 RepoResearchTask，INV-6 无业务方法）</name>
  <read_first>
    - server/delivery/models/research_task.py（RepoResearchTask/RepoResearchTaskStatus/PartialPlan 全文——字段/状态机/db_table/indexes/跨 app FK 字符串引用/「模型层不写业务方法」契约，逐字镜像）
    - server/initiatives/models/__init__.py（re-export + __all__ 范式）
    - server/initiatives/models/project_branch.py（initiatives 模型文件风格 + 跨 app FK 写法）
  </read_first>
  <action>
    新建 `server/initiatives/models/repo_association.py`，定义 `RepoAssociationStatus`/`RepoAssociation`/`RepoVerifyTaskStatus`/`RepoVerifyTask`（字段/Meta 见 artifacts_produced）。`RepoAssociation` 锚定业务用 `project` FK（必填）+ 可选 `work_item` FK（D-06：可挂 work_item 跟踪），`repository` FK 指被关联仓；状态机覆盖 proposed→confirmed→verifying→verified|rejected。`RepoVerifyTask` 逐字镜像 `RepoResearchTask` 形状（status 5 态 + subagent_session SET_NULL 回填 + attempt + error JSON）并加 `verdict` JSON。跨 app FK 一律字符串前向引用（`"delivery.WorkItem"`/`"repositories.Repository"`/`"subagent.SubAgentSession"`）避免 import 环。模型层**绝不**写 create/save/状态变更/校验方法（守 INV-6，写入收口 88-02 service）。在 `models/__init__.py` re-export 四符号并加入 `__all__`。
  </action>
  <verify>
    <automated>cd server && uv run python -c "import django; django.setup(); from initiatives.models import RepoAssociation, RepoVerifyTask, RepoAssociationStatus, RepoVerifyTaskStatus; assert RepoAssociationStatus.PROPOSED=='proposed' and RepoVerifyTaskStatus.STALE=='stale'"</automated>
  </verify>
  <acceptance_criteria>
    - `server/initiatives/models/repo_association.py` 含 `class RepoAssociation` / `class RepoVerifyTask`，且二者无 `def save` / `def create` / 状态变更方法（grep 确认模型层无业务方法）。
    - `RepoAssociation.Meta.unique_together` 含 `("project","repository")`；`RepoVerifyTask.verdict`/`.error` 默认 `dict`。
    - `from initiatives.models import RepoAssociation, RepoVerifyTask` 成功（已在 `__all__`）。
  </acceptance_criteria>
  <done>两个新模型按 RepoResearchTask 范式落地，无业务方法（INV-6 由 88-02 guard 守护），可被 service 写入。</done>
</task>

<task type="auto">
  <name>Task 2: 迁移 0010（initiatives CreateModel）+ SubAgentSession.TaskType.REPO_VERIFY + subagent 迁移</name>
  <read_first>
    - server/subagent/models.py（TaskType 枚举 行 41-46 + 既有 choices；新增 REPO_VERIFY 后需 makemigrations 生成 AlterField）
    - server/initiatives/migrations/0009_project_feishu_chat_id.py（最近迁移编号/依赖写法，确认下一编号为 0010）
    - .planning/phases/82-project-workspace-entity/82-01-PLAN.md（「迁移纯 CreateModel/AddField 无回填 + makemigrations --check 干净」范式，如可读）
  </read_first>
  <action>
    在 `subagent/models.py` 的 `SubAgentSession.TaskType` 末尾新增 `REPO_VERIFY = "repo_verify", "Repo Verify"`。运行 `cd server && uv run python manage.py makemigrations initiatives subagent` 生成两个迁移：initiatives `0010_repo_association`（纯 `CreateModel` RepoAssociation + RepoVerifyTask，无 RunPython 回填）、subagent AlterField（task_type choices 变更）。确认 `makemigrations --check --dry-run` 干净（无遗漏）。迁移文件不手改业务逻辑，仅核对 `dependencies` 含 initiatives 0009 + repositories/subagent/delivery 已有最新迁移。
  </action>
  <verify>
    <automated>cd server && uv run python manage.py makemigrations --check --dry-run && uv run python manage.py migrate --plan 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `makemigrations --check --dry-run` 退出 0（无未生成迁移）。
    - `server/initiatives/migrations/0010_repo_association.py` 存在且含 `CreateModel`、无 `RunPython`。
    - `SubAgentSession.TaskType.REPO_VERIFY == "repo_verify"`（python -c 断言）。
  </acceptance_criteria>
  <done>新模型迁移与 REPO_VERIFY 枚举落地，迁移纯 schema 无回填，迁移图一致。</done>
</task>

<task type="auto">
  <name>Task 3: 登记 call_source repo_verify_container + repo_association（枚举 + LOGGING-SPEC §4.1）+ 模型测试</name>
  <read_first>
    - server/agents/call_source.py（CallSource 枚举 + docstring 计数「25 值」+ normalize 行 71-85；AUX_REPO_ROUTER 已存在 行 54）
    - .planning/observability/LOGGING-SPEC.md（§4.1 表 行 80-92——aux_repo_router/board_split 既有行格式 + 枚举计数注释）
    - server/tests/initiatives/（既有 initiatives 测试风格，pytest-django + factory）
  </read_first>
  <action>
    `call_source.py`：枚举末尾新增 `REPO_VERIFY_CONTAINER = "repo_verify_container"`（注释：Phase 88 逐仓 explore 容器深验 LLM）+ `REPO_ASSOCIATION = "repo_association"`（注释：Phase 88 候选细化/Agent 自处理 LLM）；模块 docstring + 类 docstring「25 值」改「27 值」。`LOGGING-SPEC.md` §4.1 表在 `board_split` 行后追加两行：`repo_verify_container`（入口=RepoVerifyDispatchService per-repo explore 容器，深入仓库代码验证业务适配性，多仓 fan-out）、`repo_association`（入口=RepoAssociationService 候选细化/Agent 自处理，多轮可多次），并把 §4.1 标题处枚举计数与 call_source.py 同步为 27。新建 `test_repo_association_models.py`：唯一约束（pytest.raises(IntegrityError) on 重复 (project,repository)，复用 initiatives factory/fixtures）、默认 status、verdict 默认 dict、`CallSource.normalize('repo_verify_container')=='repo_verify_container'`、`CallSource.normalize('repo_association')=='repo_association'`、`SubAgentSession.TaskType.REPO_VERIFY` 存在。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/initiatives/test_repo_association_models.py -x -q && uv run python -c "import django; django.setup(); from agents.call_source import CallSource; assert CallSource.normalize('repo_verify_container')=='repo_verify_container' and CallSource.normalize('repo_association')=='repo_association'"</automated>
  </verify>
  <acceptance_criteria>
    - `CallSource.REPO_VERIFY_CONTAINER.value=='repo_verify_container'`、`CallSource.REPO_ASSOCIATION.value=='repo_association'`；normalize 命中不回退 unknown。
    - LOGGING-SPEC §4.1 含 `repo_verify_container` 与 `repo_association` 两行；枚举计数注释一致（27）。
    - 模型测试全绿：唯一约束抛 IntegrityError、默认值正确、REPO_VERIFY 枚举存在。
  </acceptance_criteria>
  <done>两个新 call_source 受控登记可作指标/留痕标签；模型不变量经测试守护。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 新模型字段 → DB | 业务关联 + verify verdict 落库（DB canonical 真相源） |
| call_source 维度 | 受控枚举，防外部输入污染指标基数 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88-01-TAMPER | Tampering | 旁路写新模型 | mitigate | 模型层零业务方法；写入收口 RepoAssociationService（INV-6 guard 88-02 守护） |
| T-88-01-CARD | Tampering | call_source 维度基数失控 | mitigate | 新值经 CallSource.normalize 受控，非法回退 unknown |
| T-88-01-SC | Tampering | npm/pip/cargo installs | accept | 本 plan 无任何包安装（RESEARCH Package Legitimacy Audit = N/A，纯内部模型/枚举） |
</threat_model>

<verification>
- `cd server && uv run pytest tests/initiatives/test_repo_association_models.py -q` 全绿。
- `makemigrations --check --dry-run` 干净；迁移纯 CreateModel/AlterField 无回填。
- grep `repo_association.py` 确认模型层无 `def save`/`create`/状态变更方法。
- LOGGING-SPEC §4.1 含 repo_verify_container + repo_association；call_source 计数同步 27。
</verification>

<success_criteria>
- 业务级 RepoAssociation + per-repo RepoVerifyTask 模型落地（D-05），状态机覆盖 propose→confirm→verify→终态/回退（D-01/02/03）。
- SubAgentSession.TaskType.REPO_VERIFY + 两新 call_source 登记，观测维度受控。
- 迁移纯 schema 无回填，模型层零业务方法（INV-6 守护就位待 88-02）。
</success_criteria>

<output>
完成后创建 `.planning/phases/88-repo-association/88-01-SUMMARY.md`
</output>
