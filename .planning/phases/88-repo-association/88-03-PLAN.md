---
phase: 88
plan: 03
type: execute
wave: 3
depends_on: ["88-02"]
files_modified:
  - server/initiatives/services/repo_verify_dispatch.py
  - server/initiatives/services/repo_association_service.py
  - server/subagent/api/callbacks.py
  - server/tests/initiatives/test_repo_verify_dispatch.py
  - server/tests/subagent/test_repo_verify_callback.py
  - .planning/phases/88-repo-association/88-UAT.md
autonomous: true
requirements: [REPO-02]
must_haves:
  truths:
    - "逐仓深验 = 派 claude code task 容器深入仓库代码验证业务适配性（D-02），复刻 ResearchDispatchAdapter 结构（不直接复用，避免污染 PlanSession 编排）：per-repo SubAgentSession(REPO_VERIFY) + DispatchTask + node_execution_id 关联 + get_dispatcher().dispatch"
    - "容器只读 explore 模式（D-02 不写 git）：metadata 双层拦截 env_FRIDAY_TASK_MODE='explore' + env_FRIDAY_TASK_TASK_MODE='explore'（mirror research_adapter._build_dispatch_metadata），git token 经 aresolve_git_token 注入不入日志"
    - "单仓 fail-soft 隔离（D-03，Pitfall 3）：每仓 dispatch + 聚合 try/except 隔离，单仓 clone/容器异常仅 mark_failed(task) + continue，绝不上抛拖垮其余仓；runner 离线降级（mirror research_adapter runner_offline：跳过容器、仓标 unknown，不阻断终态）"
    - "容器回调续驱（Pitfall 2）：SubAgentSession.node_execution_id 非空 → 容器完成经既有 _schedule_workflow_resume 重入挂起节点；新增 verify 完成/失败钩子解析 verdict JSON 落 RepoVerifyTask（经 service，INV-6），整段 fail-soft swallow（mirror _handle_research_completion，Pitfall 4 不回 5xx）"
    - "聚合 collect_verdicts：按各 RepoVerifyTask.verdict 聚合 fit/mismatch/unknown（缺失仓记 unknown 不阻断终态，D-03 fail-soft）"
    - "观测自检：容器 token 桥接 call_source 经 _derive_container_call_source 映射 REPO_VERIFY→repo_verify_container；initiated_by_user_id 透传（dispatch payload + SubAgentSession.last_output 带触发用户）；verdict/异常经 redact_secrets_in_text，日志仅记 repo_id/计数"
  artifacts:
    - path: "server/initiatives/services/repo_verify_dispatch.py"
      provides: "RepoVerifyDispatchService（per-repo explore 容器 fan-out + node_execution_id 续驱 + 单仓 fail-soft + runner 离线降级，复刻 ResearchDispatchAdapter）"
      exports: ["RepoVerifyDispatchService"]
    - path: "server/initiatives/services/repo_association_service.py"
      provides: "confirm_repos / dispatch_verify / collect_verdicts / mark_verify_running / mark_verify_failed / record_verdict（RepoVerifyTask 唯一写入口，INV-6）"
      contains: "def collect_verdicts"
    - path: "server/subagent/api/callbacks.py"
      provides: "_handle_repo_verify_completion / _handle_repo_verify_failure 钩子（last_output.source=='repo_verify'）+ _derive_container_call_source 映射 REPO_VERIFY"
      contains: "repo_verify"
    - path: ".planning/phases/88-repo-association/88-UAT.md"
      provides: "deferred live 验证（容器 explore E2E + 未知 task_type 容错 A1）"
      contains: "A1"
  key_links:
    - from: "server/initiatives/services/repo_verify_dispatch.py"
      to: "runners.dispatcher.DispatchTask / get_dispatcher().dispatch"
      via: "per-repo dispatch（task_type='repo_verify', env_FRIDAY_TASK_MODE='explore', node_execution_id）"
      pattern: "get_dispatcher\\(\\).dispatch"
    - from: "server/subagent/api/callbacks.py::_handle_completed"
      to: "server/subagent/api/callbacks.py::_handle_repo_verify_completion"
      via: "verify 容器完成 → 解析 verdict → RepoAssociationService.record_verdict（INV-6），fail-soft swallow"
      pattern: "_handle_repo_verify_completion"
---

<objective>
交付 REPO-02 的「逐仓容器深验 + 聚合」核心：复刻 `ResearchDispatchAdapter` 结构建 `RepoVerifyDispatchService`，对用户确认的每个仓库 fan-out 独立 claude code explore 容器深入代码验证业务适配性；容器回调经既有续驱链路落 verdict（经 service，INV-6）；聚合 fit/mismatch/unknown，单仓失败 fail-soft 隔离。

锁定决策：D-02 开 claude code task 容器深入代码验证（非元数据匹配）、explore 只读、发现不符可回退；D-03 全程 fail-soft 单仓失败不阻断其余；D-04 复用 v0.12 durable + v0.8 dispatch + claude code task，不重造；观测强制——带 `initiated_by_user_id`、容器 LLM 赋 `repo_verify_container` call_source。

Purpose: 把「用户确认的仓库」变成「逐仓深度代码校验 verdict」，为 88-05 的 mismatch 回退 / 最终确认提供聚合结论。
Output: `RepoVerifyDispatchService` + service confirm/dispatch/collect 方法 + 容器回调 verify 钩子 + 88-UAT.md。
</objective>

<execution_context>
@.planning/phases/88-repo-association/88-CONTEXT.md
@.planning/phases/88-repo-association/88-RESEARCH.md
@.planning/observability/LOGGING-SPEC.md
</execution_context>

<context>
@.planning/PROJECT.md
@server/services/plan_orchestration/research_adapter.py
@server/subagent/api/callbacks.py
@server/delivery/services/research_service.py
@server/initiatives/services/repo_association_service.py
@server/services/git_credentials.py
@server/services/provider_config.py
@.cursor/rules/observability-logging.mdc
</context>

<artifacts_produced>
- NEW `server/initiatives/services/repo_verify_dispatch.py`：`RepoVerifyDispatchService`（复刻 `ResearchDispatchAdapter`，verify 语义）。
  - `__init__(*, association_service=None, node_execution_id="")`。
  - `dispatch(association, confirmed_repo_ids) -> dict`：对每个确认仓建 `RepoVerifyTask`（经 service）+ `_dispatch_verify_task`；单仓 try/except 隔离（mark_verify_failed + continue）；runner 在线计数 0 → runner_offline 降级（仓标 unknown，不起容器，不阻断）；返回 `{dispatched, failed, runner_offline}`。
  - `_dispatch_verify_task(association, task, repo)`：缺 git_url → mark_failed("missing_git_url")；建 `AgentSession` + `SubAgentSession(task_type=REPO_VERIFY, node_execution_id=self.node_execution_id or None, last_output={source:"repo_verify", repo_verify_task_id, association_id, repository_id, initiated_by_user_id})`；`prompt=_build_verify_prompt`；`metadata=_build_dispatch_metadata`（explore 双层 + cc 凭证 + git token）；`DispatchTask(task_type="repo_verify", repo_url, branch=default_branch or main, prompt, node_execution_id, session_id, metadata, timeout=30*60)`；`get_dispatcher().dispatch` → `mark_verify_running`。
  - `_build_verify_prompt(association, task, repo)`：注入「本仓应承接的 feature 列表（粗，来自 association 候选 routed_reason/matched_node_paths）+ 需校验业务适配性」，要求容器 explore 读代码产 **JSON verdict**（schema `{fit:"fit"|"mismatch"|"unknown", confidence, summary, evidence_files[], mismatch_reasons[]}`）；prompt 来自 server 权威状态（非用户原文拼执行指令，V5）。
  - `_build_dispatch_metadata(repo)`（逐字复刻 research_adapter：explore 双层 + aget_claude_code_runtime_config + aresolve_git_token + git@→https 改写 + _repo_url）。
  - `_count_online_runners()` / `_get_repository()`（mirror research_adapter）。
- EXTEND `server/initiatives/services/repo_association_service.py`（RepoVerifyTask 唯一写入口，INV-6）：
  - `confirm_repos(*, project, repo_ids, initiated_by_user_id) -> list[RepoAssociation]`（proposed→confirmed，未在 repo_ids 的 proposed 候选可标 rejected/移除——选保留不动仅确认子集；association.status=confirmed）。
  - `dispatch_verify(*, project, confirmed, node_execution_id, initiated_by_user_id) -> dict`（薄委托 `RepoVerifyDispatchService(node_execution_id=...).dispatch`；置 association.status=verifying）。
  - `create_verify_task` / `mark_verify_running(task, subagent_session)` / `mark_verify_failed(task, error)` / `record_verdict(task, verdict)`（RepoVerifyTask 写入收口，状态机 pending→running→done|failed，attempt 计数）。
  - `collect_verdicts(association) -> dict`：聚合各 RepoVerifyTask.verdict → `{fit:[...], mismatch:[...], unknown:[...], all_terminal:bool}`（缺 verdict/缺 task 的确认仓记 unknown，不阻断）。
- EXTEND `server/subagent/api/callbacks.py`：
  - `_is_repo_verify(session)`（task_type==REPO_VERIFY and last_output.source=="repo_verify"）。
  - `_handle_repo_verify_completion(session, p, log)`：从 last_output.repo_verify_task_id 取 RepoVerifyTask；`parse_verify_verdict(p.output)`（解析 JSON verdict，空/不可解析 → mark_verify_failed("empty_or_unparseable")，否则 record_verdict）；整段独立 try/except swallow（mirror _handle_research_completion，绝不阻塞 _handle_completed）。
  - `_handle_repo_verify_failure(session, p, log)`：mark_verify_failed("container_failed")。
  - 在 `_handle_completed` / `_handle_failed` 内 verify 钩子调用（与 research 钩子并列，独立 try/except）；`_schedule_workflow_resume` 已自动覆盖（node_execution_id 非空）。
  - `_derive_container_call_source`：新增分支 `session.task_type==REPO_VERIFY → CallSource.REPO_VERIFY_CONTAINER`（服务端权威派生，token 桥接落 repo_verify_container）。
- NEW `server/tests/initiatives/test_repo_verify_dispatch.py`：dispatch explore（mode=explore + node_execution_id + token 注入断言，dispatcher mock）；per-repo isolation（一仓 dispatch 抛 → 仅该 task failed，其余仍 dispatch）；runner offline 降级（_count_online_runners=0 → 仓标 unknown 不起容器）；collect_verdicts 聚合（fit/mismatch/unknown + 缺失记 unknown）。
- NEW `server/tests/subagent/test_repo_verify_callback.py`：verify completion 解析 verdict 落 RepoVerifyTask（record_verdict）；空 output → mark_verify_failed；非 repo_verify session 不触发；_derive_container_call_source 映射 repo_verify_container。
- NEW `.planning/phases/88-repo-association/88-UAT.md`：deferred live（A1 容器对未知 task_type=repo_verify 容错按 task_mode=explore 分流；真实 runner+Docker explore E2E 读代码产 verdict）。
- structlog 事件：`repo_verify_dispatch_started`/`_completed`(caller, component=repo_association, +duration_ms, dispatched/failed/runner_offline)、`repo_verify_task_failed`(caller, repo_id, reason 脱敏)、`repo_verify_verdict_recorded`(caller, repo_id, fit)、容器内步骤 sampling。
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: RepoVerifyDispatchService（复刻 ResearchDispatchAdapter，per-repo explore 容器 + fail-soft + runner 离线降级）</name>
  <read_first>
    - server/services/plan_orchestration/research_adapter.py（全文——dispatch fan-out 行 66-150 / _dispatch_deep_task 行 152-219 / _build_dispatch_metadata explore 双层 + git token 行 310-350 / _count_online_runners 行 352-363 / 单仓 try/except 隔离 WR-02 行 116-130，逐字复刻为 verify 语义）
    - server/services/git_credentials.py（aresolve_git_token per-repo→host fallback）
    - server/services/provider_config.py（aget_claude_code_runtime_config api_key/base_url/model）
    - server/initiatives/services/repo_association_service.py（88-02 已建的 service 结构 + INV-6 写入收口风格）
  </read_first>
  <action>
    新建 `repo_verify_dispatch.py`，`RepoVerifyDispatchService` 复刻 `ResearchDispatchAdapter` 结构（**复刻不复用**，避免污染 PlanSession 编排状态机，RESEARCH Alternatives）：`dispatch(association, confirmed_repo_ids)` 对每仓建 `RepoVerifyTask`（经 `association_service.create_verify_task`）+ `_dispatch_verify_task`，单仓 `try/except` 隔离（异常 → `mark_verify_failed` + continue，绝不上抛 Pitfall 3）；deep 仓前先 `_count_online_runners()`，==0 则 runner_offline 降级（确认仓 verdict 记 unknown 不起容器，mirror research_adapter，不阻断终态）。`_dispatch_verify_task`：缺 git_url → mark_failed；建 `AgentSession`+`SubAgentSession(task_type=REPO_VERIFY, node_execution_id=self.node_execution_id or None, last_output={source:"repo_verify", repo_verify_task_id, association_id, repository_id, initiated_by_user_id})`；`metadata=_build_dispatch_metadata(repo)`（**explore 双层** env_FRIDAY_TASK_MODE/TASK_MODE='explore' + cc 凭证 + aresolve_git_token + git@→https）；`DispatchTask(task_type="repo_verify", node_execution_id=self.node_execution_id or "", session_id, prompt=_build_verify_prompt, timeout=30*60, metadata)` → `get_dispatcher().dispatch` → `mark_verify_running`。`_build_verify_prompt` 注入该仓应承接 feature（粗）+ 要求 explore 产 JSON verdict（schema 见 artifacts），prompt 来自 server 权威状态。日志仅记 repo_id/计数 + `has_git_token` 布尔（token 绝不入日志）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/initiatives/test_repo_verify_dispatch.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `_build_dispatch_metadata` 产 `env_FRIDAY_TASK_MODE=="explore"` 且 `env_FRIDAY_TASK_TASK_MODE=="explore"`（断言双层）。
    - `SubAgentSession` 建时 `node_execution_id` 取自 self（非空时透传），`last_output.source=="repo_verify"`。
    - per-repo isolation：mock 一仓 dispatch 抛 → 仅该 RepoVerifyTask failed，其余仓仍 dispatch（断言不上抛）。
    - runner offline（_count_online_runners=0）→ 不调 dispatch、确认仓不起容器（runner_offline=True）。
    - grep 确认 token 不入日志（仅 has_git_token 布尔）。
  </acceptance_criteria>
  <done>per-repo explore 容器深验 fan-out 落地（复刻 research_adapter），explore 只读 + node_execution_id 续驱 + 单仓 fail-soft + runner 离线降级。</done>
</task>

<task type="auto">
  <name>Task 2: RepoAssociationService confirm/dispatch/collect + RepoVerifyTask 写入收口（INV-6）</name>
  <read_first>
    - server/delivery/services/research_service.py（create_tasks_for_session / mark_running / mark_failed / record_partial 状态机写入范式，镜像为 verify 任务）
    - server/initiatives/services/repo_association_service.py（88-02 已建 propose/_persist_candidates 写入收口风格）
    - server/initiatives/services/repo_verify_dispatch.py（Task 1，dispatch 调 service 的 create_verify_task/mark_verify_running/mark_verify_failed）
  </read_first>
  <action>
    在 `repo_association_service.py` 扩 verify 段（RepoVerifyTask **唯一**写入口，INV-6）：`confirm_repos(*, project, repo_ids, initiated_by_user_id)`（命中 proposed 候选置 status=confirmed，返回 confirmed RepoAssociation 列表）；`dispatch_verify(*, project, confirmed, node_execution_id, initiated_by_user_id)`（置 association.status=verifying，薄委托 `RepoVerifyDispatchService(node_execution_id=...).dispatch`）；`create_verify_task(association, repository, initiated_by_user_id)`/`mark_verify_running(task, subagent_session)`/`mark_verify_failed(task, error)`/`record_verdict(task, verdict)`（条件 update 幂等状态机 pending→running→done|failed，attempt 计数，verdict JSON 写入；正文经 redact）；`collect_verdicts(association)`（聚合各 RepoVerifyTask.verdict → `{fit, mismatch, unknown, all_terminal}`，缺 verdict/未建 task 的确认仓记 unknown，不阻断）。所有 ORM async 走 sync_to_async；状态变更条件更新（.filter(status=...).update(...)）幂等。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/initiatives/test_repo_verify_dispatch.py -x -q -k "collect or confirm or isolation" && uv run pytest tests/initiatives/test_repo_association_inv6_guard.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `confirm_repos` 把命中 proposed 候选置 confirmed；`dispatch_verify` 置 association.status=verifying。
    - `collect_verdicts` 聚合 fit/mismatch/unknown，缺 verdict 仓记 unknown，`all_terminal` 正确。
    - RepoVerifyTask 写入只经 service（INV-6 guard 仍绿，88-02 守护覆盖本 plan 新增写入）。
    - 状态机条件更新幂等（重复 record_verdict 不翻已终态/no-op）。
  </acceptance_criteria>
  <done>verify 任务状态机 + 聚合落 service 单一收口（INV-6），collect_verdicts 输出 fit/mismatch/unknown 供回退判定。</done>
</task>

<task type="auto">
  <name>Task 3: 容器回调 verify 钩子（解析 verdict 落库 + call_source 映射）+ 88-UAT.md</name>
  <read_first>
    - server/subagent/api/callbacks.py（_handle_completed 行 805-887 / _handle_failed 行 890-943 内 research 钩子调用范式 + 独立 try/except swallow 行 867-884；_handle_research_completion 行 1579-1635；_aload_research_task last_output 反查 行 1545-1567；_derive_container_call_source 行 1100-1123；_schedule_workflow_resume 行 204-293）
    - server/initiatives/services/repo_association_service.py（Task 2 的 record_verdict/mark_verify_failed）
    - .planning/phases/87-board-split-card/87-01-PLAN.md（87-UAT.md deferred 清单写法参考，如可读）
  </read_first>
  <action>
    在 `subagent/api/callbacks.py` 新增（lazy import initiatives service 防循环）：`_is_repo_verify(session)`（task_type==REPO_VERIFY and last_output.source=="repo_verify"）；`parse_verify_verdict(output)`（从容器 output 提 JSON verdict，复用 `_parse_summary_json` 风格的 json 围栏代码块提取 / 首个花括号到末个花括号跨度提取，失败返 None）；`_handle_repo_verify_completion(session, p, log)`（取 repo_verify_task_id → RepoVerifyTask；verdict None→`mark_verify_failed("empty_or_unparseable")` 否则 `record_verdict`，verdict 文本经 redact_secrets_in_text；fail-soft swallow）；`_handle_repo_verify_failure`（mark_verify_failed("container_failed")）。在 `_handle_completed`/`_handle_failed` 内以独立 `try/except`（mirror research 钩子 noqa BLE001）调用，绝不阻塞主流程（Pitfall 4 不回 5xx）；`_schedule_workflow_resume` 已覆盖节点续驱（node_execution_id 非空），无需新写续驱。`_derive_container_call_source` 加分支 `session.task_type==SubAgentSession.TaskType.REPO_VERIFY → CallSource.REPO_VERIFY_CONTAINER`。新建 `test_repo_verify_callback.py`（completion 解析 verdict→record_verdict、空→mark_failed、非 verify session 不触发、call_source 映射）。新建 `88-UAT.md`：A1（容器对未知 task_type=repo_verify 按 task_mode=explore 分流容错，需真实 runner 验证）+ 真实 explore E2E（读代码产 verdict）deferred，autonomous 不打断。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/subagent/test_repo_verify_callback.py -x -q && test -f .planning/phases/88-repo-association/88-UAT.md && grep -q "A1" .planning/phases/88-repo-association/88-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - verify 容器 completion → 解析 JSON verdict → `record_verdict`（断言落 RepoVerifyTask.verdict）；空/不可解析 → `mark_verify_failed`。
    - verify 钩子异常被 swallow（断言 _handle_completed 仍返回 200，不回 5xx）。
    - 非 repo_verify session（如 plan_research）不触发 verify 钩子。
    - `_derive_container_call_source(REPO_VERIFY session)=="repo_verify_container"`。
    - 88-UAT.md 存在含 A1 + 真实 explore E2E deferred。
  </acceptance_criteria>
  <done>容器深验 verdict 经回调落 service（INV-6），续驱复用既有链路，call_source 服务端权威映射；真机 E2E deferred 记 88-UAT.md。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Friday → 容器 dispatch（git token/env） | 注入凭证到容器 env |
| 容器 → 远端仓库（explore） | 只读，绝不可写 git |
| 容器回调 verdict 文本 → 日志/DB | 半可信，可能含敏感片段 |
| runner 回调字段 → 续驱/归因 | 半可信，不可信任 runner 可篡改字段 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88-03-TAMPER | Tampering | 容器深验意外写远端仓 | mitigate | explore 双层拦截 env_FRIDAY_TASK_MODE/TASK_MODE='explore'（mirror research_adapter）；容器侧 _check_workspace_clean 兜底（Pitfall 1） |
| T-88-03-DOS | Denial of Service | 单仓失败拖垮全部 | mitigate | per-repo try/except 隔离 + mark_verify_failed + continue；runner 离线降级标 unknown 不阻断（Pitfall 3） |
| T-88-03-5XX | Denial of Service | 回调重入异常 5xx 风暴 | mitigate | verify 钩子整段 fail-soft swallow，绝不回灌容器回调 5xx（Pitfall 4） |
| T-88-03-INFO | Information Disclosure | token/verdict 明文落日志 | mitigate | token 经 aresolve_git_token 注入 env，日志仅 has_git_token 布尔；verdict/异常经 redact_secrets_in_text |
| T-88-03-SPOOF | Spoofing | 容器回调伪造续驱 | mitigate | 沿用既有 subagent/api/callbacks 鉴权 + node_execution_id/session_id 服务端权威字段；call_source 服务端权威派生不信 runner |
| T-88-03-SC | Tampering | npm/pip/cargo installs | accept | 本 plan 无包安装（纯内部复刻 + 既有 dispatch/durable 底座） |
</threat_model>

<verification>
- `cd server && uv run pytest tests/initiatives/test_repo_verify_dispatch.py tests/subagent/test_repo_verify_callback.py tests/initiatives/test_repo_association_inv6_guard.py -q` 全绿。
- grep `repo_verify_dispatch.py` 确认 explore 双层 + node_execution_id 透传 + token 不入日志。
- grep `callbacks.py` 确认 verify 钩子独立 try/except swallow + REPO_VERIFY→repo_verify_container 映射。
- 88-UAT.md 存在含 A1 + explore E2E deferred。
</verification>

<success_criteria>
- 逐仓 claude code explore 容器深验落地（D-02），复刻 research_adapter 结构、explore 只读、node_execution_id 续驱、单仓 fail-soft、runner 离线降级（REPO-02）。
- 容器 verdict 经回调落 RepoVerifyTask（service 收口 INV-6），collect_verdicts 聚合 fit/mismatch/unknown。
- 容器 LLM 赋 repo_verify_container call_source + initiated_by_user_id 透传 + 脱敏（观测强制）。
</success_criteria>

<output>
完成后创建 `.planning/phases/88-repo-association/88-03-SUMMARY.md`
</output>
