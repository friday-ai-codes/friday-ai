---
phase: 127-semgrep-lsp
plan: 05
type: execute
wave: 4
depends_on: ["127-02"]
files_modified:
  - server/codegraph/lsp/orphan_reap.py
  - server/codegraph/lsp/supervisor.py
  - server/codegraph/lsp/volar_backend.py
  - server/codegraph/lsp/gopls_backend.py
  - server/codegraph/lsp/volar_pool.py
  - server/codegraph/management/commands/measure_lsp_baseline.py
  - server/codegraph/management/commands/revisit_impact03_samples.py
  - server/codegraph/lsp/tests/test_orphan_reap.py
  - server/tests/codegraph/test_revisit_impact03.py
  - server/tests/codegraph/test_lsp_defaults_unchanged.py
  - .planning/phases/127-semgrep-lsp/lsp-baseline-report.json
  - .planning/phases/127-semgrep-lsp/impact03-revisit.md
autonomous: true
requirements: [LSP-01]
tags: [lsp-orphan, lsp-baseline, impact03-revisit, kill-switch-gate]

must_haves:
  truths:
    - "LSP 子进程绑定索引生命周期 finally stop；孤儿 gopls/vue-language-server 可被 reap 并记 lsp_process_reaped（D-13/D-14；T-127-04）"
    - "measure_lsp_baseline 可对至少 1×Vue/TS + 1×Go 产出 before/after 质量/耗时/稳定性 JSON，缺二进制 skip-on-missing exit 0（D-15）"
    - "默认翻转仅数据驱动：SUMMARY/ROADMAP 记录建议翻或保持 False；本计划执行后 settings 默认仍 False 除非基准同时满足质量与延迟门槛（D-12, D-16）"
    - "IMPACT-03：真实 CrossRepoApiCall 样本复验四分支，或诚实延期记账二选一必须落地；不得宣称跨仓已验证（D-17）"
    - "node_check/go_check 探测失败仍 fail-soft 回落 TreeSitter，不硬失败整条索引（D-13）"
  artifacts:
    - path: "server/codegraph/lsp/orphan_reap.py"
      provides: "reap_orphan_lsp_processes"
      contains: "reap_orphan_lsp_processes"
    - path: "server/codegraph/management/commands/measure_lsp_baseline.py"
      provides: "before/after 基准命令"
      contains: "measure_lsp_baseline"
    - path: "server/codegraph/management/commands/revisit_impact03_samples.py"
      provides: "真实样本复验或诚实延期"
      contains: "revisit_impact03"
    - path: ".planning/phases/127-semgrep-lsp/lsp-baseline-report.json"
      provides: "可复跑基准产物"
      contains: "before"
    - path: ".planning/phases/127-semgrep-lsp/impact03-revisit.md"
      provides: "复验或诚实延期记录"
      contains: "IMPACT-03"
  key_links:
    - from: "index / backend lifecycle"
      to: "supervisor.stop + orphan_reap"
      via: "finally 路径"
      pattern: "reap_orphan_lsp_processes|lsp_process_reaped"
    - from: "measure_lsp_baseline"
      to: "SUMMARY D-16 decision"
      via: "质量+延迟门槛 → 建议翻默认或保持 False"
      pattern: "VOLAR_BACKEND_ENABLED|建议翻默认|保持 False"
    - from: "revisit_impact03_samples"
      to: "CrossRepoApiCall count"
      via: "count==0 诚实延期；count>0 四分支"
      pattern: "CrossRepoApiCall|诚实|四分支"
---

<objective>
Wave 4：闭合 LSP 孤儿清扫、可复跑抽取基准报告、数据驱动默认翻转门禁记录、以及 IMPACT-03 真实样本复验或诚实延期（LSP-01；D-12..D-17）。

Purpose: 降低 LSP 开启门槛并交出证据；禁止盲翻 kill-switch；关掉「跨仓样本为零」账本。
Output: orphan_reap + measure/revisit 命令 + 相位报告文件 + SUMMARY/ROADMAP 决策记录。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/127-semgrep-lsp/127-CONTEXT.md
@.planning/phases/127-semgrep-lsp/127-RESEARCH.md
@.planning/phases/127-semgrep-lsp/127-PATTERNS.md
@server/codegraph/lsp/supervisor.py
@server/codegraph/lsp/node_check.py
@server/codegraph/lsp/go_check.py
@server/codegraph/lsp/volar_pool.py
@server/codegraph/management/commands/measure_gopls_init_time.py
@server/codegraph/management/commands/measure_repo_index_stats.py
@server/tests/services/code_graph/test_cross_repo_hop.py
@server/friday/settings.py
</context>

## Artifacts this phase produces

| Symbol / artifact | Module | Introduced in |
|-------------------|--------|---------------|
| `reap_orphan_lsp_processes` / `lsp_process_reaped` | `codegraph/lsp/orphan_reap.py` | **127-05** |
| Lifecycle finally wiring | `supervisor` / backends / pool（按需） | **127-05** |
| `measure_lsp_baseline` Command | `codegraph/management/commands/measure_lsp_baseline.py` | **127-05** |
| `revisit_impact03_samples` Command | `codegraph/management/commands/revisit_impact03_samples.py` | **127-05** |
| `lsp-baseline-report.json` | `.planning/phases/127-semgrep-lsp/` | **127-05** |
| `impact03-revisit.md` | `.planning/phases/127-semgrep-lsp/` | **127-05** |
| D-16 / D-17 决策段落 | `127-05-SUMMARY.md` + ROADMAP 备注 | **127-05** |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: LSP 孤儿收割 + 生命周期 finally</name>
  <files>server/codegraph/lsp/orphan_reap.py, server/codegraph/lsp/supervisor.py, server/codegraph/lsp/volar_backend.py, server/codegraph/lsp/gopls_backend.py, server/codegraph/lsp/volar_pool.py, server/codegraph/lsp/tests/test_orphan_reap.py</files>
  <read_first>
    - `.planning/phases/127-semgrep-lsp/127-CONTEXT.md` D-13 / D-14
    - `.planning/phases/127-semgrep-lsp/127-RESEARCH.md` Pitfall 5 + orphan 草图
    - `server/codegraph/lsp/supervisor.py` — `stop` / `_stop_client_silently`
    - `server/codegraph/lsp/node_check.py` / `go_check.py` — 探测 fail-soft（复用勿重造）
    - `server/codegraph/lsp/volar_pool.py` — pool 生命周期
    - `server/codegraph/lsp/tests/test_orphan_reap.py`
  </read_first>
  <behavior>
    - reap_orphan_lsp_processes 匹配 gopls/vue-language-server 等孤儿并返回计数；异常 best-effort 不抛
    - 索引/backend 异常退出路径调用 supervisor.stop；随后或同时 reap
    - 事件 lsp_process_reaped 含 count；category=sampling；component=codegraph.lsp
  </behavior>
  <action>
新建 `codegraph/lsp/orphan_reap.py`（D-14；T-127-04）：
- 用已有依赖 `psutil.process_iter`；cmdline/name 匹配 `gopls` / `vue-language-server`（及必要时 `typescript-language-server`）
- 排除 supervisor/live-set 仍持有的 PID（若可注入 live pid set；否则用 ppid 存活启发式）
- `reap_orphan_lsp_processes(...) -> int`：terminate/kill 后返回 reaped 计数；全程 try/except best-effort，**永不**反噬索引主路径
- 观测：`logger.info("lsp_process_reaped", category="sampling", component="codegraph.lsp", count=reaped)`（高频用 sampling）

接线（按现有调用点最小改动）：
- 确保 `LspSupervisor.stop` 在索引/backend/pool 释放路径 `finally` 被调用（已有则补缺口）
- 在索引任务收尾或 pool shutdown 调用 `reap_orphan_lsp_processes`（best-effort）

**D-13：** 不改探测语义——`check_node_runtime` / `check_go_runtime` 失败仍 `available=False` → `LspUnhealthyError` → TreeSitter 回落；日志可透出「LSP 未启用：缺 X 运行时」。

去 skip `test_orphan_reap.py` 至绿（mock psutil）。⛔ 不盲翻 kill-switch（D-12）；⛔ 不改 `repo_router_v2.py`/`mcp/`（D-18）。
  </action>
  <acceptance_criteria>
    - `rg -n "reap_orphan_lsp_processes|lsp_process_reaped" server/codegraph/lsp/orphan_reap.py` 命中
    - 至少一处生产路径（backend/pool/indexer）`rg` 到 `reap_orphan` 或 `orphan_reap` 调用
    - `cd server && uv run pytest codegraph/lsp/tests/test_orphan_reap.py -q` 绿
  </acceptance_criteria>
  <verify>
    <automated>cd server && GALAXY_CACHE_WARM_ON_STARTUP=False GRAPH_BUILD_ORPHAN_RECONCILE_ON_STARTUP=False uv run pytest codegraph/lsp/tests/test_orphan_reap.py -q</automated>
  </verify>
  <done>孤儿收割可测；生命周期 finally 无缺口</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 基准报告 + IMPACT-03 复验/诚实延期 + D-16 门禁</name>
  <files>server/codegraph/management/commands/measure_lsp_baseline.py, server/codegraph/management/commands/revisit_impact03_samples.py, server/tests/codegraph/test_revisit_impact03.py, server/tests/codegraph/test_lsp_defaults_unchanged.py, .planning/phases/127-semgrep-lsp/lsp-baseline-report.json, .planning/phases/127-semgrep-lsp/impact03-revisit.md</files>
  <read_first>
    - `.planning/phases/127-semgrep-lsp/127-CONTEXT.md` D-15 / D-16 / D-17
    - `.planning/phases/127-semgrep-lsp/127-RESEARCH.md` Pitfall 6 + Discretion baseline/IMPACT-03
    - `server/codegraph/management/commands/measure_gopls_init_time.py` / `measure_repo_index_stats.py`
    - `server/tests/services/code_graph/test_cross_repo_hop.py` 文档串（合成绿 ≠ 真实验证）
    - `server/friday/settings.py` kill-switch 默认行
  </read_first>
  <behavior>
    - measure_lsp_baseline --skip-on-missing-binary 在缺二进制时 exit 0
    - revisit：CrossRepoApiCall count==0 → 写诚实延期；count>0 → 调用四分支抽样路径
    - test_lsp_defaults_unchanged 仍断言默认 False（除非 SUMMARY 明确记录已通过 D-16 门槛并改默认——默认预期保持 False）
  </behavior>
  <action>
**D-15 基准：** 新增 `measure_lsp_baseline` 管理命令（对标 `measure_gopls_init_time`）：
- 参数：`--vue-repo` / `--go-repo`（或单一 `--repo-root` 可重复）、`--output-json` 默认写 `.planning/phases/127-semgrep-lsp/lsp-baseline-report.json`、`--skip-on-missing-binary` default True
- 指标至少：Symbol/Endpoint/CallEdge（及 ApiWrapper/ApiCallSite/CrossRepoApiCall 若可得）计数差分；已知方言备注（gin endpoint 等）；索引墙钟冷/热、LSP 冷启动、相对 tree-sitter 增量；探测失败回落次数、孤儿收割计数、OOM/超时标记
- before = kill-switch False（tree-sitter）；after = 临时 env True（仅测量进程内，**不**改 settings 默认文件，除非 D-16 通过）
- 缺二进制 → stdout 说明并 exit 0

**D-16 门禁：** 跑完（或 skip）后，在 `127-05-SUMMARY.md` **必须**写明：
- 「建议翻默认 VOLAR/GOPLS=True」**仅当** (a) 质量无关键性回归或净收益 **且** (b) 延迟/内存可接受；**否则保持 False**
- ⛔ 不得以「镜像已装好」为唯一理由翻默认；若保持 False，settings 默认字面量不变；`test_lsp_defaults_unchanged` 保持绿

**D-17 IMPACT-03：** 新增 `revisit_impact03_samples`：
- 统计真实 `CrossRepoApiCall`（及必要时 ApiCallSite/ApiWrapper）行数
- `count > 0`：抽样复验 Phase 122 四分支（成功 / 对端无权限折叠 / 对端未索引 fail-soft / 跳数超限）；测 `(file_path, name)` 二次解析命中率；写入 `impact03-revisit.md` + SUMMARY
- `count == 0`：写诚实延期段落到 `impact03-revisit.md` + SUMMARY/ROADMAP——标明「仍为零 / 不可测」与是否需 follow-up（产出器缺口 vs 仅缺运行时）；**禁止**宣称跨仓 impact 已验证
- 去 skip `test_revisit_impact03.py`：mock count=0 / count>0 两路径

更新 ROADMAP Phase 127 备注引用基准结论与 IMPACT-03 复验/延期结果（executor 在 SUMMARY 阶段做）。

观测：命令级 `*_started/completed` best-effort；`initiated_by_user_id=system` 若无用户。
  </action>
  <acceptance_criteria>
    - `rg -n "skip_on_missing_binary|measure_lsp_baseline" server/codegraph/management/commands/measure_lsp_baseline.py` 命中
    - `rg -n "CrossRepoApiCall|诚实|四分支" server/codegraph/management/commands/revisit_impact03_samples.py .planning/phases/127-semgrep-lsp/impact03-revisit.md` 有命中（命令落地后跑一次生成 md；若环境无 DB 数据，命令在 count=0 路径写诚实延期）
    - `cd server && uv run pytest tests/codegraph/test_revisit_impact03.py tests/codegraph/test_lsp_defaults_unchanged.py codegraph/lsp/tests/test_orphan_reap.py -q` 绿
    - `rg -n 'default=False' server/friday/settings.py | rg -n 'VOLAR_BACKEND_ENABLED|GOPLS_BACKEND_ENABLED'` 仍体现默认 False（除非 SUMMARY 明示 D-16 通过并改码——默认预期不改）
  </acceptance_criteria>
  <verify>
    <automated>cd server && GALAXY_CACHE_WARM_ON_STARTUP=False GRAPH_BUILD_ORPHAN_RECONCILE_ON_STARTUP=False uv run pytest codegraph/lsp/tests/test_orphan_reap.py tests/codegraph/test_revisit_impact03.py tests/codegraph/test_lsp_defaults_unchanged.py -q</automated>
  </verify>
  <done>LSP-01 基准与孤儿清扫闭合；IMPACT-03 复验或诚实延期二选一落地；默认翻转有数据门禁记录</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host process table → reap | 误杀风险：仅匹配 LSP cmdline + 排除 live-set |
| Baseline command → settings defaults | 测量不得偷偷改全局默认 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-127-01 | Information Disclosure | baseline JSON | mitigate | 报告不含凭证；路径可相对化 |
| T-127-02 | Denial of Service | baseline LSP runs | mitigate | skip-on-missing；测量超时 bound；不阻塞 API |
| T-127-03 | Tampering | N/A | accept | 无新 subprocess 用户输入 |
| T-127-04 | Denial of Service | orphan LSP | mitigate | finally stop + psutil reap + lsp_process_reaped |
| T-127-05 | Spoofing | IMPACT-03 claims | mitigate | 诚实延期路径；禁伪称已验证 |
| T-127-SC | Tampering | no new packages | accept | 复用 psutil |
</threat_model>

<verification>
orphan + revisit + defaults 测试绿；baseline JSON 与 impact03-revisit.md 存在；SUMMARY 含 D-16/D-17 结论。
</verification>

<success_criteria>
- LSP-01 交付：运行时（127-02）+ 探测复用 + 孤儿清扫 + 基准 + 默认门禁
- D-15..D-17 闭合；D-12/D-16 默认不盲翻
</success_criteria>

<output>
Create `.planning/phases/127-semgrep-lsp/127-05-SUMMARY.md` when done
</output>
