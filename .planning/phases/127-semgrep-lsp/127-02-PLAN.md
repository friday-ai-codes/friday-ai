---
phase: 127-semgrep-lsp
plan: 02
type: execute
wave: 1
depends_on: ["127-01"]
files_modified:
  - server/Dockerfile
  - server/friday/settings.py
  - server/system/models.py
  - server/codegraph/models.py
  - server/codegraph/migrations/0014_securityfinding.py
  - server/tests/codegraph/test_security_finding_model.py
  - server/tests/services/code_graph/test_dockerfile_semgrep_lsp_layers.py
  - server/tests/codegraph/test_lsp_defaults_unchanged.py
autonomous: true
requirements: [TAINT-01, TAINT-03, LSP-01]
tags: [dockerfile, securityfinding, settings, semgrep-bin, lsp-runtime]

must_haves:
  truths:
    - "server 镜像 runtime 在 USER friday 前安装独立 Semgrep≥1.172.0（/opt/semgrep）、Node 22 LTS、@vue/language-server+typescript、Go+gopls~v0.23.x，且 friday PATH 可达（D-01, D-11）"
    - "server/pyproject.toml 与 uv.lock 不含 semgrep 依赖；代码永不 import semgrep（D-01）"
    - "Settings 暴露 SEMGREP_BIN/TIMEOUT/墙钟/CONFIGS；SettingKeys.SEMGREP_APP_TOKEN（加密）与 CONCURRENCY_SCAN_MAX；VOLAR/GOPLS 默认仍 False（D-09, D-10, D-12）"
    - "SecurityFinding 软引用模型落库且无 Symbol FK；message 写入路径约定脱敏（D-05；T-127-01）"
  artifacts:
    - path: "server/Dockerfile"
      provides: "独立 Semgrep + Node/Go/LSP 运行时"
      contains: "/opt/semgrep"
    - path: "server/codegraph/models.py"
      provides: "SecurityFinding"
      contains: "class SecurityFinding"
    - path: "server/codegraph/migrations/0014_securityfinding.py"
      provides: "SecurityFinding 迁移"
      contains: "SecurityFinding"
    - path: "server/system/models.py"
      provides: "SEMGREP_APP_TOKEN / CONCURRENCY_SCAN_MAX keys"
      contains: "SEMGREP_APP_TOKEN"
    - path: "server/friday/settings.py"
      provides: "SEMGREP_* settings；kill-switch 默认 False"
      contains: "SEMGREP_BIN"
  key_links:
    - from: "Dockerfile ENV PATH"
      to: "SEMGREP_BIN + node/gopls"
      via: "USER friday 前安装 + PATH"
      pattern: "SEMGREP_BIN|/opt/semgrep|gopls"
    - from: "SettingKeys.SEMGREP_APP_TOKEN"
      to: "SystemSetting is_encrypted"
      via: "Fernet encrypt_value 写入路径（对齐 FEISHU_APP_SECRET）"
      pattern: "SEMGREP_APP_TOKEN"
    - from: "SecurityFinding"
      to: "repositories.Repository"
      via: "FK repository；无 Symbol FK"
      pattern: "class SecurityFinding"
---

<objective>
Wave 1：交付 Semgrep/LSP 镜像运行时地基、Semgrep settings/加密 key、以及 `SecurityFinding` 软引用模型（D-01/D-05/D-09/D-10/D-11/D-12）。去 skip Wave 0 对应 Dockerfile/模型/defaults 测试。

Purpose: 扫描与 LSP 轨道的运行时与数据契约先就位，后续 CLI/MR/基准计划只消费这些符号。
Output: Dockerfile 安装层 + settings/keys + SecurityFinding 迁移 + 相关测试绿。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/127-semgrep-lsp/127-CONTEXT.md
@.planning/phases/127-semgrep-lsp/127-RESEARCH.md
@.planning/phases/127-semgrep-lsp/127-PATTERNS.md
@server/Dockerfile
@server/friday/settings.py
@server/system/models.py
@server/codegraph/models.py
@server/codegraph/migrations/0013_processtrace.py
</context>

## Artifacts this phase produces

| Symbol / artifact | Module | Introduced in |
|-------------------|--------|---------------|
| `/opt/semgrep` + `SEMGREP_BIN` image env | `server/Dockerfile` | **127-02** |
| Node 22 + `@vue/language-server` + typescript tsdk | `server/Dockerfile` | **127-02** |
| Go toolchain + `gopls` v0.23.x | `server/Dockerfile` | **127-02** |
| `SEMGREP_BIN` / `SEMGREP_TIMEOUT` / `SEMGREP_TASK_TIMEOUT` / `SEMGREP_CONFIGS` | `friday/settings.py` | **127-02** |
| `SettingKeys.SEMGREP_APP_TOKEN` / `CONCURRENCY_SCAN_MAX` | `system/models.py` | **127-02** |
| `SecurityFinding` + migration `0014_securityfinding` | `codegraph/models.py` | **127-02** |
| （其余符号见 127-01 清单，由后续 plan 引入） | — | 127-03..05 |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SecurityFinding 模型 + SettingKeys</name>
  <files>server/codegraph/models.py, server/codegraph/migrations/0014_securityfinding.py, server/system/models.py, server/tests/codegraph/test_security_finding_model.py</files>
  <read_first>
    - `.planning/phases/127-semgrep-lsp/127-CONTEXT.md` D-05 / D-09
    - `server/codegraph/models.py` — `SymbolCommunity` / `ProcessTrace` 软引用字段形态
    - `server/codegraph/migrations/0013_processtrace.py` — 迁移风格
    - `server/system/models.py` — `FEISHU_APP_SECRET` / `CONCURRENCY_CHARTER_MAX` 键命名
    - `server/tests/codegraph/test_security_finding_model.py` — Wave 0 节点名
  </read_first>
  <behavior>
    - SecurityFinding 含 repository FK、branch_name、mr_key、rule_id、severity、file_path、line、message、fingerprint、scan_sha、status（默认 open）
    - 无指向 Symbol 的 ForeignKey
    - SettingKeys 含 SEMGREP_APP_TOKEN 与 CONCURRENCY_SCAN_MAX 字面量
  </behavior>
  <action>
在 `codegraph` app 新增 `SecurityFinding`（D-05）：UUID PK；`repository` → `repositories.Repository` CASCADE；`branch_name`/`mr_key` CharField；`rule_id`/`severity`/`file_path`/`message`/`fingerprint`/`scan_sha`/`status`（default `"open"`）；`line` 可空 PositiveIntegerField。⛔ 不对 Symbol/Endpoint FK。迁移文件名 `0014_securityfinding.py`（若并行迁移冲突则顺延下一编号，但 SUMMARY 记实际名）。

在 `SettingKeys` 增加（D-09 + discretion）：
- `SEMGREP_APP_TOKEN = "semgrep_app_token"`（写入路径必须 `is_encrypted=True` + `encrypt_value`，对齐飞书 secret；本 task 至少落 key 常量，写入 helper 可在 127-04 完成若已有通用 set-setting API——若本相位无 UI，至少文档/注释写明加密约定，并保证扫描侧用 `decrypt_value`）
- `CONCURRENCY_SCAN_MAX = "concurrency_scan_max"`（默认语义 2，由 concurrency 模块在 127-03 读取）

去 skip 并实现 `test_security_finding_model.py` 三节点至绿。⛔ 不改 `pyproject.toml`/`uv.lock`；⛔ 不改 `repo_router_v2.py`/`mcp/`（D-18）。
  </action>
  <acceptance_criteria>
    - `rg -n "class SecurityFinding" server/codegraph/models.py` 命中；模型字段含 `rule_id`、`fingerprint`、`scan_sha`、`mr_key`
    - `rg -n "SEMGREP_APP_TOKEN|CONCURRENCY_SCAN_MAX" server/system/models.py` 均命中
    - `cd server && uv run pytest tests/codegraph/test_security_finding_model.py -q` 绿（无 skip）
    - AST/静态：SecurityFinding 无 `to=.*Symbol` FK
  </acceptance_criteria>
  <verify>
    <automated>cd server && GALAXY_CACHE_WARM_ON_STARTUP=False GRAPH_BUILD_ORPHAN_RECONCILE_ON_STARTUP=False uv run pytest tests/codegraph/test_security_finding_model.py -q</automated>
  </verify>
  <done>SecurityFinding + keys 落地；模型测试绿</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SEMGREP_* settings + kill-switch 默认不变</name>
  <files>server/friday/settings.py, server/tests/codegraph/test_lsp_defaults_unchanged.py</files>
  <read_first>
    - `.planning/phases/127-semgrep-lsp/127-CONTEXT.md` D-01 / D-04 / D-10 / D-12
    - `server/friday/settings.py` 现有 `VOLAR_BACKEND_ENABLED` / `GOPLS_BACKEND_ENABLED` / `EXTRACTOR_BACKENDS` 段
    - `server/tests/codegraph/test_lsp_defaults_unchanged.py`
  </read_first>
  <behavior>
    - VOLAR_BACKEND_ENABLED 与 GOPLS_BACKEND_ENABLED 默认 False
    - SEMGREP_BIN 默认 `/opt/semgrep/bin/semgrep`（env 可覆盖）
    - SEMGREP_TIMEOUT（规则）默认 5；任务墙钟默认 180；SEMGREP_CONFIGS 默认含 p/python,p/django,p/javascript,p/typescript,p/golang
  </behavior>
  <action>
在 `friday/settings.py` 增加（D-01/D-04/D-10）：
- `SEMGREP_BIN: str` — env `SEMGREP_BIN`，default `/opt/semgrep/bin/semgrep`
- `SEMGREP_TIMEOUT: int` — 单规则超时秒，default `5`（注入 CLI `--timeout` / `SEMGREP_TIMEOUT`）
- `SEMGREP_TASK_TIMEOUT: float|int` — 任务墙钟秒，default `180`
- `SEMGREP_CONFIGS: str` — CSV packs，default `p/python,p/django,p/javascript,p/typescript,p/golang`（可配置可文档化，D-10）
- 可选 escape hatch 读取 env `SEMGREP_APP_TOKEN` **仅当** SystemSetting 空时由扫描层使用（D-09 discretion）——settings 可暴露 `SEMGREP_APP_TOKEN_ENV` 或直接 `env.str("SEMGREP_APP_TOKEN", default="")` 但不写日志

**硬锁 D-12：** 保持 `VOLAR_BACKEND_ENABLED` / `GOPLS_BACKEND_ENABLED` 的 `default=False` 字面量不变。`EXTRACTOR_BACKENDS["go"]` 可改为 `"gopls"` 作为「重开目标」**仅当**注释写明仍受 kill-switch 约束；若改声明，SUMMARY 记录；**禁止**把 kill-switch 默认改 True（D-16）。

去 skip 并实现 `test_lsp_defaults_unchanged.py` 至绿。观测：无新增高频 INFO。
  </action>
  <acceptance_criteria>
    - `rg -n "SEMGREP_BIN|SEMGREP_TIMEOUT|SEMGREP_TASK_TIMEOUT|SEMGREP_CONFIGS" server/friday/settings.py` 均命中
    - `rg -n 'VOLAR_BACKEND_ENABLED.*default=False|GOPLS_BACKEND_ENABLED.*default=False' server/friday/settings.py` 两处仍成立（或等价 env.bool(..., default=False)）
    - `cd server && uv run pytest tests/codegraph/test_lsp_defaults_unchanged.py -q` 绿
    - `rg -n "semgrep" server/pyproject.toml server/uv.lock` 无新增依赖行（应为 0 匹配或仅历史无关；执行时确认未添加）
  </acceptance_criteria>
  <verify>
    <automated>cd server && GALAXY_CACHE_WARM_ON_STARTUP=False GRAPH_BUILD_ORPHAN_RECONCILE_ON_STARTUP=False uv run pytest tests/codegraph/test_lsp_defaults_unchanged.py -q</automated>
  </verify>
  <done>SEMGREP_* settings 就位；LSP kill-switch 默认仍 False</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Dockerfile 安装 Semgrep + Node/Go/volar/gopls</name>
  <files>server/Dockerfile, server/tests/services/code_graph/test_dockerfile_semgrep_lsp_layers.py</files>
  <read_first>
    - `.planning/phases/127-semgrep-lsp/127-CONTEXT.md` D-01 / D-11
    - `.planning/phases/127-semgrep-lsp/127-RESEARCH.md` §Standard Stack Installation Dockerfile 草图 + Package Legitimacy + Pitfall 4 PATH
    - `server/Dockerfile` runtime 阶段（`USER friday` 之前）
    - `server/tests/services/code_graph/test_dockerfile_semgrep_lsp_layers.py`
  </read_first>
  <behavior>
    - Dockerfile 含独立 /opt/semgrep 安装且版本钉 ≥1.172.0
    - 含 Node 22、@vue/language-server、typescript、Go、gopls 安装指令
    - 安装发生在 USER friday 之前；ENV PATH 全局可见
  </behavior>
  <action>
修改 `server/Dockerfile` runtime 阶段、**在 `USER friday` 之前**（D-01, D-11）：

1. Semgrep：`uv tool install semgrep==1.172.0 --install-dir /opt/semgrep`（或等价独立 venv）；`ENV PATH="/opt/semgrep/bin:${PATH}"`；`ENV SEMGREP_BIN=/opt/semgrep/bin/semgrep`。⛔ 禁止把 semgrep 写进 `server/pyproject.toml` / `uv.lock`。
2. Node **22 LTS** + 全局 `@vue/language-server@3.3.9`（或 3.x 钉版）+ `typescript`（满足 `discover_tsdk()`）。
3. Go 官方 toolchain tarball + `gopls@v0.23.0`（或 v0.23.x）装入 `/usr/local` 或 `/opt`。
4. 构建末探针（root 或 `su friday`）：`semgrep --version && node -v && gopls version`（失败则 build fail）。
5. 发布说明：SUMMARY 记镜像体积预估 +400–550MB（真实 `docker images` 若本环境可跑则记实测；否则诚实记「待 CI 测」）。

去 skip 并实现 `test_dockerfile_semgrep_lsp_layers.py` 静态断言至绿。⛔ 不改 `mcp/`、`repo_router_v2.py`（D-18）。
  </action>
  <acceptance_criteria>
    - `rg -n "semgrep==1.172|/opt/semgrep|vue/language-server|gopls" server/Dockerfile` 均有命中
    - Dockerfile 中 Semgrep/Node/Go 安装指令行号均早于 `USER friday`
    - `cd server && uv run pytest tests/services/code_graph/test_dockerfile_semgrep_lsp_layers.py -q` 绿
    - `rg -n "semgrep" server/pyproject.toml` 无新增依赖
  </acceptance_criteria>
  <verify>
    <automated>cd server && GALAXY_CACHE_WARM_ON_STARTUP=False GRAPH_BUILD_ORPHAN_RECONCILE_ON_STARTUP=False uv run pytest tests/services/code_graph/test_dockerfile_semgrep_lsp_layers.py tests/codegraph/test_lsp_defaults_unchanged.py tests/codegraph/test_security_finding_model.py -q</automated>
  </verify>
  <done>镜像层与模型/settings 地基齐备；相关 Wave 0 测试绿</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Image build → runtime | 第三方 CLI/LSP 二进制进入信任计算基 |
| Settings/DB → process env | `SEMGREP_APP_TOKEN` 仅解密后注入子进程，不入日志 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-127-01 | Information Disclosure | SettingKeys.SEMGREP_APP_TOKEN | mitigate | Fernet `is_encrypted=True` + decrypt 仅扫描路径；禁明文日志（D-09） |
| T-127-02 | Denial of Service | SEMGREP_TASK_TIMEOUT / CONFIGS | mitigate | 默认墙钟 180s + 精选 pack CSV 可收窄（D-04/D-10） |
| T-127-03 | Tampering | SEMGREP_BIN absolute path | mitigate | 绝对路径配置；后续 argv list（127-03） |
| T-127-04 | Denial of Service | gopls/volar in image | accept | 本 plan 只装运行时；孤儿收割在 127-05 |
| T-127-05 | Spoofing | N/A this plan | accept | 文案在 127-04 |
| T-127-SC | Tampering | Dockerfile package installs | mitigate | 仅 RESEARCH Approved：PyPI semgrep、npm @vue/language-server/typescript、gopls；禁 npm 假 semgrep 包 |
</threat_model>

<verification>
模型/Dockerfile/defaults 三类测试全绿；pyproject 无 semgrep。
</verification>

<success_criteria>
- D-01/D-05/D-09/D-11/D-12 落地
- Kill-switch 默认仍 False
- SecurityFinding 可 migrate
</success_criteria>

<output>
Create `.planning/phases/127-semgrep-lsp/127-02-SUMMARY.md` when done
</output>
