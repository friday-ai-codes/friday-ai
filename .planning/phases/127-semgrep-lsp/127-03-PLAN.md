---
phase: 127-semgrep-lsp
plan: 03
type: execute
wave: 2
depends_on: ["127-02"]
files_modified:
  - server/services/repo_mirror.py
  - server/services/code_graph/semgrep_scan.py
  - server/services/code_graph/semgrep_enqueue.py
  - server/durable/queues.py
  - server/durable/concurrency.py
  - server/durable/tasks.py
  - server/durable/tasks_impl.py
  - server/durable/handlers.py
  - server/tests/services/code_graph/test_semgrep_scan.py
  - server/tests/services/code_graph/test_semgrep_enqueue.py
autonomous: true
requirements: [TAINT-01]
tags: [semgrep-scan, queue-scan, durable, fail-open, worktree]

must_haves:
  truths:
    - "公开 ensure_worktree_for_scan 可供扫描使用，业务层不长期依赖 _ensure_worktree（D-02）"
    - "semgrep_scan 经固定 SEMGREP_BIN argv 列表调用 scan（非 ci），--baseline-commit=merge-base，永不 import semgrep（D-01, D-03；T-127-03）"
    - "QUEUE_SCAN + scan-slot N=2 + durable_semgrep_scan；入队 best-effort 带 initiated_by_user_id；超时/失败 fail-open 稳定 error_code（D-02, D-04；T-127-02）"
    - "finding 落 SecurityFinding，message 经 redact_secrets_in_text（D-05；T-127-01）"
    - "扫描模块零耦合 GraphService 热路径（D-18）"
  artifacts:
    - path: "server/services/repo_mirror.py"
      provides: "ensure_worktree_for_scan 公共 API"
      contains: "ensure_worktree_for_scan"
    - path: "server/services/code_graph/semgrep_scan.py"
      provides: "CLI 封装 + persist findings"
      contains: "run_semgrep_scan"
    - path: "server/services/code_graph/semgrep_enqueue.py"
      provides: "enqueue_semgrep_scan"
      contains: "enqueue_semgrep_scan"
    - path: "server/durable/queues.py"
      provides: "QUEUE_SCAN"
      contains: "QUEUE_SCAN"
    - path: "server/durable/concurrency.py"
      provides: "scan_slot_lock / ascan_lock"
      contains: "scan_slot_lock"
  key_links:
    - from: "enqueue_semgrep_scan"
      to: "QUEUE_SCAN + durable_semgrep_scan"
      via: "DurableTaskService.defer + scan-slot lock"
      pattern: "QUEUE_SCAN|durable_semgrep_scan|scan-slot"
    - from: "run_semgrep_scan"
      to: "SEMGREP_BIN subprocess"
      via: "create_subprocess_exec argv list + merge-base"
      pattern: "--baseline-commit|SEMGREP_BIN"
    - from: "run_semgrep_scan"
      to: "SecurityFinding"
      via: "persist redacted findings"
      pattern: "SecurityFinding|redact_secrets_in_text"
---

<objective>
Wave 2：实现 diff-aware Semgrep CLI 封装、公开 worktree API、独立 `QUEUE_SCAN` durable 任务与 fail-open 契约（TAINT-01；D-01..D-05）。

Purpose: 死亡螺旋四件套中的「独立 CLI + diff-aware + 异步 + fail-open」在扫描内核层闭合；MR 文案挂载留给 127-04。
Output: `semgrep_scan` / `semgrep_enqueue` / durable QUEUE_SCAN + 测试绿。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/127-semgrep-lsp/127-CONTEXT.md
@.planning/phases/127-semgrep-lsp/127-RESEARCH.md
@.planning/phases/127-semgrep-lsp/127-PATTERNS.md
@server/services/repo_mirror.py
@server/services/indexer.py
@server/repositories/charter_enqueue.py
@server/services/process_enqueue.py
@server/durable/queues.py
@server/durable/concurrency.py
@server/durable/tasks.py
@server/durable/tasks_impl.py
@server/durable/handlers.py
</context>

## Artifacts this phase produces

| Symbol / artifact | Module | Introduced in |
|-------------------|--------|---------------|
| `ensure_worktree_for_scan` | `services/repo_mirror.py` | **127-03** |
| `run_semgrep_scan` / `build_semgrep_argv` / persist helpers | `services/code_graph/semgrep_scan.py` | **127-03** |
| `enqueue_semgrep_scan` | `services/code_graph/semgrep_enqueue.py` | **127-03** |
| `QUEUE_SCAN` in `ALL_QUEUES` | `durable/queues.py` | **127-03** |
| `DEFAULT_SCAN_CONCURRENCY` / `scan_slot_lock` / `ascan_lock` | `durable/concurrency.py` | **127-03** |
| `durable_semgrep_scan` / `run_semgrep_scan` (tasks_impl) | `durable/tasks.py` / `tasks_impl.py` | **127-03** |
| handler registration `durable_semgrep_scan` | `durable/handlers.py` | **127-03** |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: worktree 公共 API + semgrep_scan CLI</name>
  <files>server/services/repo_mirror.py, server/services/code_graph/semgrep_scan.py, server/tests/services/code_graph/test_semgrep_scan.py</files>
  <read_first>
    - `.planning/phases/127-semgrep-lsp/127-CONTEXT.md` D-01..D-05
    - `.planning/phases/127-semgrep-lsp/127-PATTERNS.md` Pattern semgrep_scan + repo_mirror
    - `server/services/repo_mirror.py` — `ensure_mirror_sha` / `_ensure_worktree` / `_run_cmd`
    - `server/services/indexer.py` — `_get_merge_base` 形态
    - `server/tests/fixtures/semgrep/sample_findings.json`
    - `server/tests/services/code_graph/test_semgrep_scan.py`
  </read_first>
  <behavior>
    - argv 含 scan、--baseline-commit=&lt;merge-base&gt;、--json、--config packs；不含 ci
    - 无 import semgrep
    - timeout/mirror/CLI 失败返回稳定 error_code，不向调用方抛阻断异常（fail-open 结果对象/字典）
    - packs 来自 SEMGREP_CONFIGS
  </behavior>
  <action>
在 `repo_mirror.py` 新增薄公共 API `ensure_worktree_for_scan(repository_id, commit_sha) -> Path`（名称可微调但须公开无下划线前缀），内部复用 `_ensure_worktree` / `ensure_mirror_sha`（D-02；Pitfall 3）。扫描前确保 source/target 两端 SHA 在 mirror 内可解析。

新建 `services/code_graph/semgrep_scan.py`（D-01..D-05）：
- 用 `asyncio.create_subprocess_exec(*argv_list, ...)`；`SEMGREP_BIN` 取自 settings 绝对路径；⛔ 永不 `import semgrep`；⛔ 用 `semgrep scan` 非 `semgrep ci`（Pitfall 7）。
- baseline：`git merge-base(target_sha, source_sha)` → `--baseline-commit`；⛔ 禁止用 target HEAD 当 baseline（D-03）。
- 可选：diff 文件集 `--include` 收窄。
- `--json --quiet`；`--timeout` ← `SEMGREP_TIMEOUT`；`--config` 自 `SEMGREP_CONFIGS` CSV 展开（D-10）。
- 任务墙钟：`asyncio.wait_for(..., timeout=SEMGREP_TASK_TIMEOUT)`；超时 → `error_code=timeout`（D-04；T-127-02）。
- 解析 JSON → upsert/create `SecurityFinding`；`message`/snippet 入库前 `redact_secrets_in_text`（D-05；T-127-01）。
- Pro token：若 SystemSetting `SEMGREP_APP_TOKEN` 有值则 decrypt 后仅注入子进程 env；空则 CE；env escape hatch 仅当 setting 空（D-09）。**永不** log/返回 token。
- 观测：`semgrep_scan_started/completed/failed` + `duration_ms`；`category=caller`（任务级）或 finding 循环 `sampling`；`component=code_graph`；`initiated_by_user_id` 由上层 bind；best-effort `except: pass`。
- 返回结构化结果（findings 计数 / error_code / baseline_sha），供 127-04 建段；本 task 可不写 MR。

去 skip 实现 `test_semgrep_scan.py` 至绿（mock subprocess）。⛔ 不改 GraphService；⛔ 不改 `repo_router_v2.py`/`mcp/`（D-18）。
  </action>
  <acceptance_criteria>
    - `rg -n "ensure_worktree_for_scan" server/services/repo_mirror.py` 命中
    - `rg -n "import semgrep|from semgrep" server/services/code_graph/semgrep_scan.py` 计数为 0
    - `rg -n "--baseline-commit|SEMGREP_BIN|redact_secrets_in_text|SecurityFinding" server/services/code_graph/semgrep_scan.py` 均命中
    - `cd server && uv run pytest tests/services/code_graph/test_semgrep_scan.py -q` 绿
  </acceptance_criteria>
  <verify>
    <automated>cd server && GALAXY_CACHE_WARM_ON_STARTUP=False GRAPH_BUILD_ORPHAN_RECONCILE_ON_STARTUP=False uv run pytest tests/services/code_graph/test_semgrep_scan.py -q</automated>
  </verify>
  <done>CLI 契约 + worktree 公共 API + fail-open 结果对象落地</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: QUEUE_SCAN + durable_semgrep_scan + enqueue</name>
  <files>server/durable/queues.py, server/durable/concurrency.py, server/durable/tasks.py, server/durable/tasks_impl.py, server/durable/handlers.py, server/services/code_graph/semgrep_enqueue.py, server/tests/services/code_graph/test_semgrep_enqueue.py</files>
  <read_first>
    - `.planning/phases/127-semgrep-lsp/127-CONTEXT.md` D-02 / D-04
    - `.planning/phases/127-semgrep-lsp/127-RESEARCH.md` Discretion QUEUE_SCAN N=2
    - `server/durable/queues.py` / `concurrency.py` — `QUEUE_CHARTER` / `charter_slot_lock` / `acharacter_lock`
    - `server/repositories/charter_enqueue.py` 与 `server/services/process_enqueue.py`
    - `server/durable/tasks.py` `durable_charter_draft`；`tasks_impl.py` `run_community_rebuild` bind_task_context
    - `server/durable/handlers.py` `register_business_handlers`
  </read_first>
  <behavior>
    - enqueue 使用 QUEUE_SCAN、idempotency_key semgrep:{repo}:{mr_key}、scan-slot lock、传递 initiated_by_user_id
    - enqueue 失败返回 None 不 raise
    - QUEUE_SCAN 在 ALL_QUEUES 中
  </behavior>
  <action>
按 discretion 落地新队列（D-02）：
- `QUEUE_SCAN = "scan"` 加入 `ALL_QUEUES` 与 `__all__`
- `DEFAULT_SCAN_CONCURRENCY = 2`；`scan_slot_lock` / `ascan_lock` 读 `SettingKeys.CONCURRENCY_SCAN_MAX`（对齐 charter）
- `semgrep_enqueue.enqueue_semgrep_scan(repository_id, mr_key, source_sha, target_sha, ..., initiated_by_user_id=...)` → `DurableTaskService.defer("durable_semgrep_scan", ..., queue=QUEUE_SCAN, idempotency_key=f"semgrep:{repo}:{mr_key}", lock=await ascan_lock(repo_id), initiated_by_user_id=...)`；失败 log + return None（不抛）
- `tasks.py`：`@app.task(name="durable_semgrep_scan", queue=QUEUE_SCAN)`
- `tasks_impl.run_semgrep_scan`：`bind_task_context(user_id=initiated_by_user_id or "system", source="durable", component="code_graph")`；调用 `semgrep_scan` 核心；墙钟内完成；业务语义 fail-open（超时/CLI 失败记 `error_code`，观测 `*_failed`，**不**把异常变成建 MR 阻断——任务可返回含 error_code 的 dict）；MR 回填可在本 task 留 hook/TODO 接口函数名供 127-04 实现，或先实现 patch stub 调用点
- `handlers.py` 注册 in-process adapter

观测事件：`enqueue_semgrep_scan_completed/failed`、`semgrep_scan_job_started/completed/failed` + `duration_ms`；`category=caller`；凭证脱敏。

去 skip `test_semgrep_enqueue.py` 至绿。⛔ 不挂 GraphService；⛔ 不改冻结面（D-18）。
  </action>
  <acceptance_criteria>
    - `rg -n "QUEUE_SCAN" server/durable/queues.py` 命中且出现在 `ALL_QUEUES`
    - `rg -n "scan_slot_lock|ascan_lock|DEFAULT_SCAN_CONCURRENCY" server/durable/concurrency.py` 命中
    - `rg -n "durable_semgrep_scan|enqueue_semgrep_scan" server/durable/tasks.py server/services/code_graph/semgrep_enqueue.py server/durable/handlers.py` 均命中
    - `cd server && uv run pytest tests/services/code_graph/test_semgrep_enqueue.py -q` 绿
  </acceptance_criteria>
  <verify>
    <automated>cd server && GALAXY_CACHE_WARM_ON_STARTUP=False GRAPH_BUILD_ORPHAN_RECONCILE_ON_STARTUP=False uv run pytest tests/services/code_graph/test_semgrep_scan.py tests/services/code_graph/test_semgrep_enqueue.py -q</automated>
  </verify>
  <done>QUEUE_SCAN 异步扫描链路可入队可执行；fail-open 契约测试绿</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo worktree content → Semgrep CLI | 不可信代码触发扫描 CPU |
| Scan subprocess env | 仅注入必要 SEMGREP_APP_TOKEN，不透出到日志 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-127-01 | Information Disclosure | finding persist / logs | mitigate | `redact_secrets_in_text`；token 不入 log/返回值 |
| T-127-02 | Denial of Service | CLI CPU | mitigate | `SEMGREP_TASK_TIMEOUT` + slot N=2 + fail-open error_code |
| T-127-03 | Tampering | subprocess | mitigate | 固定绝对 `SEMGREP_BIN` + argv 列表；校验 SHA/路径 |
| T-127-04 | Denial of Service | N/A | accept | LSP 孤儿属 127-05 |
| T-127-05 | Spoofing | N/A | accept | advisory 文案属 127-04 |
| T-127-SC | Tampering | no new packages | accept | 无新 pip/npm 安装 |
</threat_model>

<verification>
semgrep_scan + enqueue 测试全绿；queues 含 QUEUE_SCAN；无 import semgrep。
</verification>

<success_criteria>
- TAINT-01 CLI/异步/fail-open 内核可测
- D-01..D-05（扫描侧）闭合
</success_criteria>

<output>
Create `.planning/phases/127-semgrep-lsp/127-03-SUMMARY.md` when done
</output>
