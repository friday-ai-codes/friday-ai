---
phase: 33-hist-diff-bitemporal
plan: 02
type: execute
wave: 2
depends_on:
  - 33-01
files_modified:
  - server/knowledge/modifies_chunk.py
  - server/knowledge/graph_store.py
  - server/services/indexer.py
  - server/tests/knowledge/test_modifies_chunk_reconcile.py
autonomous: true
requirements:
  - HDIFF-02
must_haves:
  truths:
    - "目标分支演进+重索引后，过期 MODIFIES_CHUNK 边被置 invalid_at（置位不删除，保留历史可追溯）"
    - "as-of 查询：给定 as_of 见当年成立的边（valid_at <= as_of < invalid_at）；当前视图默认只见未失效边"
    - "对账挂既有 reindex 完成钩子，best-effort：失败仅 warning，绝不阻断索引 success"
    - "时间次序：invalid_at 恒晚于 valid_at（复用 kedge_valid_range 约束，逐边降级不掀翻批次）"
  artifacts:
    - path: "server/knowledge/modifies_chunk.py"
      provides: "as-of 查询 helper + areconcile_modifies_chunk_edges 对账函数"
      contains: "areconcile_modifies_chunk_edges"
    - path: "server/services/indexer.py"
      provides: "_run_modifies_chunk_reconcile 重索引完成钩子（base 路径 best-effort）"
      contains: "_run_modifies_chunk_reconcile"
  key_links:
    - from: "server/services/indexer.py"
      to: "knowledge.modifies_chunk.areconcile_modifies_chunk_edges"
      via: "clone_and_index_repository 收尾（rmtree 前 / base 路径）best-effort 调用"
      pattern: "_run_modifies_chunk_reconcile"
    - from: "server/knowledge/modifies_chunk.py"
      to: "knowledge.graph_store.graph_store.invalidate_edge"
      via: "过期边逐条置 invalid_at"
      pattern: "invalidate_edge"
---

<objective>
落地 HDIFF-02「bi-temporal 失效对账 + as-of 查询」：在既有重索引完成路径上挂一个
best-effort 对账钩子——目标分支演进 / 文件重索引后，把指向**已过期 chunk 版本**的
`MODIFIES_CHUNK` 边 `invalid_at` 置位（复用既有 `KnowledgeEdge` bi-temporal 字段，置位不删除）；
并提供最小的 knowledge 内部 as-of 查询 helper（历史 as_of 见当年边、当前视图只见未失效边）。
修 PF-08。

Purpose: master/目标分支演进后旧的 diff→chunk 关联当年成立但当前已过期，必须能区分历史/当前，
否则历史边污染当前视图、反查不可信（PF-08 根因）。
Output: `knowledge/modifies_chunk.py`（as-of helper + 对账函数）、`graph_store.chunk_in_edges` 的
as_of 扩展、`indexer._run_modifies_chunk_reconcile` 钩子、守护测试。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-hist-diff-bitemporal/33-CONTEXT.md
@server/knowledge/graph_store.py
@server/knowledge/models.py
@server/code_relations/models.py
@server/services/indexer.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: knowledge as-of 查询 helper（MODIFIES_CHUNK 边）</name>
  <files>server/knowledge/modifies_chunk.py, server/knowledge/graph_store.py, server/tests/knowledge/test_modifies_chunk_reconcile.py</files>
  <read_first>
    - server/knowledge/graph_store.py 第 244-330 行（chunk_in_edges 当前视图反查 + neighbors 的 as_of bi-temporal 谓词写法：valid_at<=as_of AND (invalid_at IS NULL OR invalid_at>as_of) AND created_at<=as_of AND (expired_at IS NULL OR expired_at>as_of)）
    - server/knowledge/models.py 第 260-353 行（KnowledgeEdge：target_chunk_id 弱引用、relation、bi-temporal 字段、活跃边索引）
    - server/tests/knowledge/test_modifies_chunk.py（chunk_in_edges 反查测试风格、entity_factory、apply_edge_specs）
  </read_first>
  <behavior>
    - 当前视图（as_of=None）：只返回 invalid_at IS NULL AND expired_at IS NULL 的 MODIFIES_CHUNK 边。
    - 历史 as_of：返回 valid_at<=as_of AND (invalid_at IS NULL OR as_of<invalid_at) AND created_at<=as_of AND (expired_at IS NULL OR as_of<expired_at) 的边。
    - 一条边被 invalidate（invalid_at=T）后：as_of < T 仍可见（当年成立）；as_of >= T 与当前视图均不可见。
    - as_of 为 naive datetime → 经 require_aware 拒绝（ValueError）。
  </behavior>
  <action>
    新建 `server/knowledge/modifies_chunk.py`（中文模块 docstring：HDIFF-02 as-of 查询 + 对账唯一收口；
    Task 2 的对账函数也落本模块）。提供 as-of 查询 helper
    `async def amodifies_chunk_edges(*, repository_id: str | None = None, target_chunk_id: uuid.UUID | None = None, as_of: datetime | None = None) -> list[EdgeRecord]`：
    - 复用 `graph_store` 的 bi-temporal 谓词语义（与 neighbors/chunk_in_edges 一致）；为避免在本模块复刻 raw 过滤，
      首选**扩展 graph_store**：给 `RelationalGraphStore.chunk_in_edges` 增加可选 keyword `as_of: datetime | None = None`
      （默认 None=当前视图，保持既有调用方零回归），as_of 给定时切换为 bi-temporal as-of 谓词（参照 neighbors 的写法 + require_aware）。
    - `amodifies_chunk_edges` 在 graph_store 之上做 `relation=MODIFIES_CHUNK` + 可选 repository_id（经
      `source_entity__repository_id`）/ target_chunk_id 过滤，返回 EdgeRecord 列表。本 helper 是 knowledge 内部查询面
      （CONTEXT：最小内部 helper，不新建对外检索 UI）。加入模块 `__all__`。

    新建 `tests/knowledge/test_modifies_chunk_reconcile.py`（pytestmark django_db(transaction=True)），
    `TestAsOfQuery`：建 code_change 实体 + MODIFIES_CHUNK 边（apply_edge_specs，valid_at=T0），
    invalidate（invalid_at=T1>T0）后断言：as_of=T0.5（T0<T0.5<T1）见到该边；as_of=T2(>T1) 不见；
    as_of=None（当前视图）不见；naive as_of 抛 ValueError。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/knowledge/test_modifies_chunk_reconcile.py -k AsOf -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `graph_store.chunk_in_edges(chunk_id, as_of=...)` 当前视图（as_of=None）零回归；as_of 给定走 bi-temporal 谓词。
    - `amodifies_chunk_edges` 支持 repository_id / target_chunk_id / as_of 过滤，仅返回 MODIFIES_CHUNK 边。
    - 历史 as_of 见当年成立边、当前视图只见未失效边（behavior 全部满足）；naive as_of 被 require_aware 拒绝。
  </acceptance_criteria>
  <done>as-of 查询 helper 就位；当前视图/历史回溯语义测试全绿。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 重索引对账——过期 MODIFIES_CHUNK 边置 invalid_at</name>
  <files>server/knowledge/modifies_chunk.py, server/tests/knowledge/test_modifies_chunk_reconcile.py</files>
  <read_first>
    - server/knowledge/graph_store.py 第 196-219 行（invalidate_edge：仅 invalid_at IS NULL 时置位、幂等 no-op、require_aware）
    - server/knowledge/models.py 第 302-338 行（kedge_valid_range 约束：invalid_at 必须 > valid_at）
    - server/code_relations/models.py（ChunkRegistry：chunk_id PK、content_hash、branch_name="" base 命名空间、(repository,branch_name,file_path) 索引）
    - server/code_relations/utils.py 第 12-40 行（generate_chunk_id：chunk_id 由 repo+file_path+chunk_index 派生，与内容无关——同位置内容变更仍同 chunk_id 但 content_hash 变；文件删/收缩则 chunk_id 消失）
    - Task 1 新建的 server/knowledge/modifies_chunk.py
  </read_first>
  <behavior>
    - 输入某 repository 重索引后状态；对账只作用于该 repo 的活跃 MODIFIES_CHUNK 边（source_entity.repository_id == repo）。
    - 过期判定（reliable 信号，取既有重索引可提供者）：边 target_chunk_id 在当前 ChunkRegistry(repo, branch_name="") 中
      ① 已不存在（文件删除 / chunk 收缩）→ 过期；或 ② 存在但 content_hash != 边 metadata.chunk_content_hash（同位置内容已被新版本取代）→ 过期。
    - 过期边 → graph_store.invalidate_edge(edge_id, invalid_at=now)（置位不删除）；未过期边不动（幂等可重入）。
    - 边 metadata 无 chunk_content_hash（历史边/空串）→ 仅用①存在性判定，不因缺指纹误失效（保守）。
    - best-effort：单边 invalidate 抛（如 kedge_valid_range：now<=valid_at 的异常边）→ 逐边 try/except 记 warning 跳过，不掀翻批次。
    - 返回失效计数（结构化日志可观测）。
  </behavior>
  <action>
    在 `knowledge/modifies_chunk.py` 新增
    `async def areconcile_modifies_chunk_edges(repository_id: str, *, invalid_at: datetime) -> int`：
    require_aware(invalid_at)。① 取该 repo 当前 base chunk 映射：
    `ChunkRegistry.objects.filter(repository_id=repository_id, branch_name="").values_list("chunk_id","content_hash")`
    建 dict `current = {chunk_id: content_hash}`。② 取活跃 MODIFIES_CHUNK 边：
    `KnowledgeEdge.objects.filter(relation=EdgeRelation.MODIFIES_CHUNK, invalid_at__isnull=True, expired_at__isnull=True, source_entity__repository_id=repository_id)`
    （`async for`，取 id/target_chunk_id/metadata）。③ 逐边判过期：`cid not in current`（不存在）→ 过期；
    `cid in current` 且 `meta.get("chunk_content_hash")`（非空）且 `!= current[cid]` → 过期。
    ④ 过期边 `try: await graph_store.invalidate_edge(edge_id, invalid_at=invalid_at) except (IntegrityError, KnowledgeEdge.DoesNotExist) as exc: logger.warning(...)`（逐边隔离，best-effort），计数。
    结尾 `logger.info("knowledge_modifies_chunk_reconciled", repository_id=..., checked=..., invalidated=...)`。加入 `__all__`。
    边只经 graph_store.invalidate_edge 收口（不裸写 KnowledgeEdge.update，保持一致性 + 防改写历史）。

    扩展 `tests/knowledge/test_modifies_chunk_reconcile.py`，`TestReconcile`：
    - chunk 删除：建 ChunkRegistry chunk + MODIFIES_CHUNK 边（metadata.chunk_content_hash=该 content_hash），
      删除 ChunkRegistry 行后对账 → 边 invalid_at 被置（边行仍存在=不删除，acount 不变）。
    - 内容取代：边 metadata.chunk_content_hash="old"，ChunkRegistry 同 chunk_id 现 content_hash="new" → 对账失效。
    - 未变：content_hash 一致 → 边不被失效（幂等：二次对账仍 0 失效）。
    - 缺指纹保守：metadata 无 chunk_content_hash 且 chunk 仍存在 → 不失效。
    - 时间次序：失效后边的 invalid_at > valid_at（断言）；构造一条 valid_at 在未来的异常边验证逐边降级不抛。
    - 跨 repo 隔离：另一 repo 的边不被本 repo 对账触及。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/knowledge/test_modifies_chunk_reconcile.py -k Reconcile -x -q</automated>
  </verify>
  <acceptance_criteria>
    - chunk 删除 / 内容取代两类过期边均被置 invalid_at（边行保留，不删除）。
    - 未变 chunk 边不被失效；缺指纹边仅按存在性判定（保守不误失效）；对账幂等可重入。
    - invalid_at > valid_at 恒成立；异常边逐边 try/except 降级不掀翻批次。
    - 跨 repo 不串味（仅失效本 repo 的边）；边写入仅经 graph_store.invalidate_edge。
  </acceptance_criteria>
  <done>重索引对账把过期 MODIFIES_CHUNK 边置 invalid_at（置位不删）；测试全绿。</done>
</task>

<task type="auto">
  <name>Task 3: 挂载对账到既有重索引完成钩子（best-effort 降级）</name>
  <files>server/services/indexer.py, server/tests/knowledge/test_modifies_chunk_reconcile.py</files>
  <read_first>
    - server/services/indexer.py 第 3411-3466 行（_run_sensitive_detection / _run_commit_index：best-effort try/except + warning + 绝不阻断 success 的范式模板）
    - server/services/indexer.py 第 3792-3805 行（clone_and_index_repository 收尾：`if not branch:` 内 rmtree 前依次 await _run_sensitive_detection / _run_commit_index 的挂载点）
    - Task 2 的 areconcile_modifies_chunk_edges
  </read_first>
  <action>
    在 `indexer.py` 仿 `_run_commit_index` 新增模块级
    `async def _run_modifies_chunk_reconcile(repository_id: str) -> None`：中文 docstring 说明
    HDIFF-02——目标分支重索引后对账失效过期 MODIFIES_CHUNK 边，与 _run_sensitive_detection/_run_commit_index
    同款 best-effort fail-safe（整段 try/except，失败仅 `logger.warning("modifies_chunk_reconcile_failed", repository_id=..., error=...)`，
    绝不阻断索引 success）。体内 `from knowledge.modifies_chunk import areconcile_modifies_chunk_edges`
    + `from django.utils import timezone`，调 `await areconcile_modifies_chunk_edges(repository_id, invalid_at=timezone.now())`，
    成功记 info（含 invalidated 计数）。

    在 `clone_and_index_repository` 收尾 `if not branch:` 块内（_run_commit_index 调用之后、return 之前）
    追加 `await _run_modifies_chunk_reconcile(repository_id)`——仅 base 索引路径（功能分支 overlay 不触发对账，
    与既有 base-only 钩子一致）。

    扩展 `tests/knowledge/test_modifies_chunk_reconcile.py`，`TestReconcileHookFailSafe`：
    monkeypatch `knowledge.modifies_chunk.areconcile_modifies_chunk_edges` 抛异常，调 `_run_modifies_chunk_reconcile`
    断言不上抛（best-effort 吞 + warning，对齐 D-04/T-25-12 降级范式）。
    （对账逻辑本身已在 Task 2 充分覆盖；本 task 只验钩子降级不阻断。）
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/knowledge/test_modifies_chunk_reconcile.py -x -q &amp;&amp; uv run ruff check knowledge/modifies_chunk.py services/indexer.py</automated>
  </verify>
  <acceptance_criteria>
    - `_run_modifies_chunk_reconcile` 挂在 clone_and_index_repository 收尾 base 路径（`if not branch:`），与既有 best-effort 钩子并列。
    - 对账抛异常时钩子吞掉 + warning，绝不阻断索引 success 终态。
    - ruff 通过；test_modifies_chunk_reconcile.py 全绿。
  </acceptance_criteria>
  <done>对账挂既有 reindex 完成路径，best-effort 降级；测试全绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 重索引后台路径 → knowledge 边失效写入 | 对账在索引后台线程运行；输入为本仓库自身 ChunkRegistry 状态（内部可信），无外部不可信输入跨界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-04 | Tampering | 对账误删/误改历史边 | mitigate | 仅 graph_store.invalidate_edge 置位（不删除、仅 invalid_at IS NULL 时写一次、不覆盖原失效时间）；边写入唯一收口，禁裸 update |
| T-33-05 | Denial of Service | 对账异常阻断索引 | mitigate | _run_modifies_chunk_reconcile 整段 try/except + 逐边 try/except，失败仅 warning，绝不阻断索引 success（沿用 v0.5 best-effort 范式） |
| T-33-06 | Integrity | 误把当前有效边判为过期 | mitigate | 过期判定双信号（存在性 + content_hash 指纹）；缺指纹边仅按存在性保守判定，不因缺数据误失效；跨 repo 严格按 source_entity.repository_id 隔离 |

无新增 npm/pip/cargo 依赖安装，故无 T-33-SC 供应链项。
</threat_model>

<verification>
- HDIFF-02 成功标准 2/3：`cd server && uv run pytest tests/knowledge/test_modifies_chunk_reconcile.py -q` 全绿（对账置 invalid_at 不删 + as-of 历史/当前区分 + 降级不阻断 + 时间次序）。
- 静态：`cd server && uv run ruff check knowledge/modifies_chunk.py knowledge/graph_store.py services/indexer.py`。
- 既有图查询零回归：`cd server && uv run pytest tests/knowledge/test_modifies_chunk.py tests/knowledge/test_graph_store.py -q`。
</verification>

<success_criteria>
- 目标分支演进 + 重索引后，过期 MODIFIES_CHUNK 边被置 `invalid_at`（置位不删，复用既有 bi-temporal 字段，未新建时间模型）。
- as-of 查询区分历史/当前：历史 as_of 见当年成立边，当前视图默认只见未失效边。
- 对账挂既有 reindex 完成路径，best-effort：失败仅 warning，绝不阻断索引 success。
- 时间次序约束（invalid_at > valid_at）满足；边失效唯一经 graph_store.invalidate_edge 收口。
</success_criteria>

<artifacts>
## Artifacts this phase produces (Plan 02)

- `server/knowledge/modifies_chunk.py`：`amodifies_chunk_edges`（as-of 查询 helper）+ `areconcile_modifies_chunk_edges`（重索引对账失效）。
- `graph_store.chunk_in_edges` 的 `as_of` 可选参数扩展（历史回溯，既有调用方零回归）。
- `services/indexer.py` 的 `_run_modifies_chunk_reconcile`（重索引完成 best-effort 钩子，base 路径）。
- 守护测试 `tests/knowledge/test_modifies_chunk_reconcile.py`（as-of / 对账失效 / 降级 / 时间次序 / 跨 repo 隔离）。
</artifacts>

<output>
Create `.planning/phases/33-hist-diff-bitemporal/33-02-SUMMARY.md` when done
</output>
