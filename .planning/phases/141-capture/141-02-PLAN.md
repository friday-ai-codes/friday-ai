---
phase: 141-capture
plan: 02
type: execute
wave: 1
depends_on:
  - "141-01"
files_modified:
  - server/initiatives/models/session_capture.py
  - server/initiatives/models/__init__.py
  - server/initiatives/migrations/0015_session_capture.py
  - server/initiatives/services/capture_service.py
  - server/initiatives/services/__init__.py
  - server/services/git_url.py
  - server/tests/initiatives/test_capture_service.py
  - server/tests/initiatives/test_capture_inv6_guard.py
autonomous: true
requirements:
  - STORE-01
  - STORE-02
  - STORE-03
  - STORE-05
  - OBS-02
must_haves:
  truths:
    - "SessionCapture 行可经 CaptureService.persist 落库，含脱敏后的 question/answer 与标量元数据，且 status=pending_eval"
    - "project_id 与 repository_id 均可空；无项目/无仓不拒绝落库"
    - "未知 model/provider/token 字段保存字面 unknown；入库前强制 redact_secrets_in_text"
    - "业务代码除 capture_service.py 外不能旁路 SessionCapture ORM 写；writer 含 objects.create 且不含延迟入图/记忆入口"
  artifacts:
    - path: server/initiatives/models/session_capture.py
      provides: "SessionCapture + SessionCaptureStatus；db_table=initiative_session_captures"
      contains: "initiative_session_captures"
    - path: server/initiatives/migrations/0015_session_capture.py
      provides: "建表 migration；FK SET_NULL；UniqueConstraint 三元组"
      contains: "SET_NULL"
    - path: server/initiatives/services/capture_service.py
      provides: "INV-6 async persist 唯一 writer"
      contains: "SessionCapture.objects.create"
    - path: server/services/git_url.py
      provides: "normalize_git_url 共享入口（SSH→HTTPS + strip/lower/.git）"
      contains: "normalize_git_url"
  key_links:
    - from: server/initiatives/services/capture_service.py
      to: common.logging.redact_secrets_in_text
      via: "create 前对 question/answer 分别脱敏"
      pattern: "redact_secrets_in_text"
    - from: server/initiatives/services/capture_service.py
      to: server/initiatives/models/session_capture.py
      via: "transaction.atomic + objects.create status=pending_eval"
      pattern: "pending_eval|objects\\.create"
    - from: server/tests/initiatives/test_capture_inv6_guard.py
      to: initiatives/services/capture_service.py
      via: "唯一 writer 放行"
      pattern: "_ALLOWED_WRITER"
---

<objective>
落地独立 Capture 账本模型、migration、共享 git URL 规范化，以及 INV-6 `CaptureService.persist` 核心写路径（无仓/无项目仍落库、脱敏、unknown、pending_eval），使 STORE-01/02/03/05 与 OBS-02 红测变绿（挂钩与观测生命周期留给后续 plan）。

Purpose: 实现「永不丢失」的持久化地基，且不复用 ProjectMemory/Ledger，不扩展 report_project_knowledge（D-01/D-15）。
Output: SessionCapture + 0015 migration + CaptureService 核心 + git_url helper + 对应测试变绿。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/141-capture/141-CONTEXT.md
@.planning/phases/141-capture/141-RESEARCH.md
@.planning/phases/141-capture/141-PATTERNS.md
@.planning/phases/141-capture/141-01-PLAN.md
@server/initiatives/models/memory.py
@server/initiatives/models/merge_request.py
@server/initiatives/services/memory_service.py
@server/initiatives/migrations/0014_project_context_link.py
@server/repositories/serializers.py

## Locked decisions for this plan

D-01, D-02, D-03, D-04, D-06, D-09, D-10, D-11, D-12, D-15. Linking depth (D-07/D-08) and caller logs (D-13/D-14) stubbed minimally so persist 可跑：本 plan 允许 link_reason=`unanchored`/`project_only` 等简单路径；完整 URL/授权状态机在 141-03。

## Worktree safety

Do not touch unrelated dirty files (`test_skills_snapshot_guard.py`, `skills`, debug md). No MCP views, no CallSource enum, no frontend.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SessionCapture 模型 + migration 0015 + 导出</name>
  <files>server/initiatives/models/session_capture.py, server/initiatives/models/__init__.py, server/initiatives/migrations/0015_session_capture.py</files>
  <read_first>
    - server/initiatives/models/memory.py
    - server/initiatives/models/merge_request.py
    - server/initiatives/models/repo_association.py
    - server/initiatives/models/__init__.py
    - server/initiatives/migrations/0014_project_context_link.py
  </read_first>
  <behavior>
    - db_table=`initiative_session_captures`（D-01）。
    - project/repository FK null=True, on_delete=SET_NULL（禁止 CASCADE）（D-02）。
    - SessionCaptureStatus TextChoices 含 pending_eval/eval_failed/ingest_pending/evaluated；默认 pending_eval（D-04）。
    - question/answer TextField；response_model/provider/input_tokens/output_tokens CharField default unknown（D-12，STORE-05）。
    - session_id 非空 CharField，缺省语义由 service 写 unspecified（D-06）；question_hash CharField(64)；link_reason CharField；branch_name 元数据；initiated_by_user_id CharField default system。
    - UniqueConstraint(initiated_by_user_id, session_id, question_hash) name=uniq_session_capture_user_session_question（D-05）。
    - 模型 docstring 声明无业务写方法，写入收口 CaptureService（D-11）。
  </behavior>
  <action>
新建模型并对齐 MergeRequest 的 SET_NULL FK 形状与 memory 的 INV-6 docstring。从 `models/__init__.py` 导出 SessionCapture、SessionCaptureStatus。生成 `0015_session_capture.py`，dependencies 含 initiatives.0014；migration 文本对 project/repository 不得出现 CASCADE。字段长度按 Django 惯例由实现者决定（Claude's Discretion）。不创建 admin/REST。
  </action>
  <verify>
    <automated>cd server && uv run python -c "from initiatives.models import SessionCapture, SessionCaptureStatus; assert SessionCapture._meta.db_table=='initiative_session_captures'; assert SessionCaptureStatus.PENDING_EVAL=='pending_eval'" && uv run python manage.py migrate initiatives 0015_session_capture --plan 2>&1 | head -40</automated>
  </verify>
  <done>模型可导入；表名与状态枚举正确；migration 计划可应用且 FK 为 SET_NULL。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: git_url helper + CaptureService 核心 persist + INV-6 变绿</name>
  <files>server/services/git_url.py, server/initiatives/services/capture_service.py, server/initiatives/services/__init__.py, server/tests/initiatives/test_capture_service.py, server/tests/initiatives/test_capture_inv6_guard.py</files>
  <read_first>
    - server/initiatives/services/memory_service.py
    - server/repositories/serializers.py
    - server/initiatives/services/mr_service.py
    - server/initiatives/services/__init__.py
    - server/tests/initiatives/test_capture_service.py
    - server/tests/initiatives/test_capture_inv6_guard.py
  </read_first>
  <behavior>
    - normalize_git_url = ssh_git_url_to_https 后再 strip/lower/rstrip('/')/去 .git；禁止在 capture_service 再写 SSH regex（Discretion + RESEARCH）。
    - CaptureService.persist async：redact question/answer → unknown 归一 → session_id 空则 unspecified → question_hash=sha256(NFKC(strip(redacted_question))) → atomic create status=pending_eval（D-06/D-12/D-04）。
    - 本 plan 挂钩可先实现「无 id/url → unanchored；有合法 project 无仓 → project_only」；完整 git/授权在 141-03，但不得调用 _resolve_report_project_id（D-07 部分 / D-15）。
    - CapturePersistResult(capture, link_reason, idempotent_hit, created)；IntegrityError 回读既有行不更新 answer（D-05 最小实现，完整用例 141-03 加固）。
    - 禁止 MemoryService/aschedule_ingestion/background_runner/record_hook_writeback/arecord_tool_call（D-09/D-10）。
    - barrel 导出 CaptureService、CapturePersistResult。
  </behavior>
  <action>
实现 `server/services/git_url.py` 与 `capture_service.py`（对照 MemoryService 的 sync_to_async+atomic+redact，但**禁止**复制成员拒绝语义）。使以下测试变绿：`test_persist_without_project_or_repo`、`test_unknown_scalars`、`test_redaction_and_actor`、`test_persist_does_not_write_memory_or_ledger`，以及 INV-6 守卫（含 writer 自检与 deferred-sink）。链接/未授权/完整幂等若仍红，留给 141-03，但不得删除这些用例。观测测试可仍红。component 日志可在本 plan 留 stub 或最小 started/completed，完整 OBS 在 141-04。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/initiatives/test_capture_inv6_guard.py tests/initiatives/test_capture_service.py -k "without_project_or_repo or unknown_scalars or redaction or actor or does_not_write_memory or inv6 or deferred" -x -q</automated>
  </verify>
  <done>核心 persist 与 INV-6 守卫绿；无项目/无仓可落库；脱敏与 unknown 成立；无记忆/Ledger 正文写入。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| actor input → CaptureService | 未信任问答与标量；必须脱敏与 unknown 归一后再 ORM create |
| CaptureService → DB | 唯一写入口；FK 可空 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-141-01 | Information Disclosure | question/answer fields | mitigate | redact_secrets_in_text before create（D-12） |
| T-141-02 | Tampering | SessionCapture ORM | mitigate | INV-6 guard + writer-only create（D-11） |
| T-141-06 | Tampering | CASCADE delete | mitigate | SET_NULL on project/repository FKs（D-02） |
| T-141-07 | Elevation of Privilege | Memory/ingestion side effects | mitigate | static ban deferred sinks（D-10） |
| T-141-SC | Tampering | package installs | accept | 无新依赖 |
</threat_model>

<verification>
migrate plan 干净；核心 STORE 测试与 INV-6 绿；capture_service 源码无 deferred sinks。
</verification>

<success_criteria>
- 独立账本可持久化 pending_eval 行
- 脱敏与 unknown 字面值正确
- INV-6 守卫通过
- 未改 MCP/UI；未扩展 report_project_knowledge
</success_criteria>

<output>
Create `.planning/phases/141-capture/141-02-SUMMARY.md` when done
</output>
