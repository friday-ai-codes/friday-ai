---
phase: 141-capture
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - server/tests/initiatives/test_capture_service.py
  - server/tests/initiatives/test_capture_inv6_guard.py
  - server/tests/initiatives/test_capture_observability.py
  - .planning/phases/141-capture/141-VALIDATION.md
autonomous: true
requirements:
  - STORE-01
  - STORE-02
  - STORE-03
  - STORE-04
  - STORE-05
  - OBS-01
  - OBS-02
must_haves:
  truths:
    - "Wave 0 三个 Capture 测试模块存在且当前对缺失实现失败（RED），覆盖 STORE-01..05 与 OBS-01/02 行为契约"
    - "INV-6 守卫测试文件已声明唯一 writer 路径与禁止旁路/延迟入口断言"
    - "VALIDATION.md 将 Wave 0 缺口标为已落地文件（仍可为红），且不含 frontend/UI 验收"
  artifacts:
    - path: server/tests/initiatives/test_capture_service.py
      provides: "persist / 挂钩 / 幂等 / unknown / 脱敏 / 分离账本行为测试骨架"
      contains: "test_persist_without_project_or_repo"
    - path: server/tests/initiatives/test_capture_inv6_guard.py
      provides: "SessionCapture INV-6 静态扫描与 deferred-sink 禁止断言"
      contains: "capture_service.py"
    - path: server/tests/initiatives/test_capture_observability.py
      provides: "caller 生命周期与日志无正文/best-effort 观测测试骨架"
      contains: "session_capture_persist"
  key_links:
    - from: server/tests/initiatives/test_capture_service.py
      to: initiatives.services.CaptureService
      via: "async persist 调用约定（实现尚未存在，测试先挂名）"
      pattern: "CaptureService|persist"
    - from: server/tests/initiatives/test_capture_inv6_guard.py
      to: initiatives/services/capture_service.py
      via: "_ALLOWED_WRITER 唯一放行路径"
      pattern: "_ALLOWED_WRITER"
---

<objective>
建立 Phase 141 的 tests-first Wave 0：先写齐 Capture 行为 / INV-6 / 观测三类失败测试，钉死 STORE-01..05 与 OBS-01/02 契约后再进入实现波次。

Purpose: tracer-first，避免实现后补测导致「永不丢失」与 INV-6 契约漂移。
Output: 三个 RED 测试模块 + 更新后的 VALIDATION Wave 0 清单。

Decision IDs covered by test contracts: D-01..D-15（见 CONTEXT 映射；本计划只落测试，不实现生产代码）。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/141-capture/141-CONTEXT.md
@.planning/phases/141-capture/141-RESEARCH.md
@.planning/phases/141-capture/141-PATTERNS.md
@.planning/phases/141-capture/141-VALIDATION.md
@server/tests/initiatives/test_memory_inv6_guard.py
@server/tests/initiatives/test_repo_association_inv6_guard.py
@server/tests/initiatives/test_memory_service.py
@server/tests/services/code_graph/test_query_observability.py

## CONTEXT Decision ID Map (locked; cite in later plans)

- D-01: `SessionCapture` in `initiatives`, table `initiative_session_captures`; not ProjectMemory/Ledger
- D-02: nullable `project`/`repository` FKs; repo-primary, project optional
- D-03: store only structured question/answer + scalar metadata
- D-04: status enum reserves `pending_eval`/`eval_failed`/`ingest_pending`/`evaluated`; new rows only `pending_eval`
- D-05: idempotency = user + session_id + question_hash; first write wins
- D-06: missing session_id accepted as literal `unspecified`
- D-07: resolve order explicit repository_id → normalized git_url → optional project; failures keep row + reason
- D-08: unauthorized → no FK, still persist Capture owned by actor
- D-09: no LLM/eval in Phase 141
- D-10: no `aschedule_ingestion` / `background_runner` / `MemoryService.append` / `record_hook_writeback`
- D-11: INV-6 `CaptureService` only + grep guard
- D-12: redact before create; unknown scalars literal `unknown`
- D-13: `session_capture_persist_started|completed|failed`, `category=caller`, `component=knowledge`
- D-14: logs/Ledger metadata only (no Q/A body); observability best-effort
- D-15: do not expand `report_project_knowledge`; Capture is separate entry

## Worktree safety

Do not modify, stage, or revert unrelated dirty paths: `server/tests/mcp_tools/test_skills_snapshot_guard.py`, `skills` submodule, `.planning/debug/multica-friday-agent-e2e.md`. No frontend/UI files.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CaptureService 行为测试骨架（STORE-01/02/04/05）</name>
  <files>server/tests/initiatives/test_capture_service.py</files>
  <read_first>
    - server/tests/initiatives/test_memory_service.py
    - .planning/phases/141-capture/141-RESEARCH.md
    - .planning/phases/141-capture/141-PATTERNS.md
    - .planning/phases/141-capture/141-VALIDATION.md
  </read_first>
  <behavior>
    - pytestmark = django_db(transaction=True)；通过 CaptureService.persist 落库，不在生产路径测试里依赖旁路 create 作为工厂习惯。
    - test_persist_without_project_or_repo: 无 project/repository 仍插入 status=pending_eval（D-02/D-04，STORE-02）。
    - test_unknown_scalars: 缺 model/provider/tokens → 字面 unknown，禁止猜测（D-12，STORE-05）。
    - test_redaction_and_actor: question/answer 经 redact；initiated_by_user_id 绑定 actor（D-12/D-11，STORE-03/OBS-02）。
    - test_persist_does_not_write_memory_or_ledger: ProjectMemory 计数不变；未调用 arecord_tool_call（D-01/D-15，STORE-01）。
    - test_idempotent_returns_existing: 同 user+session+question 第二次同 id、不改 answer（D-05）。
    - test_*link* / test_unauthorized_does_not_set_fk: SSH/HTTPS 挂钩、repo_unresolved、显式 id 优先、未授权 FK 空仍有行（D-07/D-08，STORE-04）。
  </behavior>
  <action>
新建 `server/tests/initiatives/test_capture_service.py`。对照 `test_memory_service.py` 的 User/Space/ProjectService/Repository 夹具写法；语义反转 Memory「非成员拒绝」为 Capture「非成员仍落行、FK 空」。测试可 import 尚不存在的 `CaptureService`/`SessionCapture`——本波预期整文件收集或执行失败（RED）。禁止调用 `_resolve_report_project_id` / `lookup_project_by_branch`。不要改 `report_project_knowledge` 生产或测试语义以外的无关脏文件。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/initiatives/test_capture_service.py -x -q 2>&1 | tee /tmp/141-01-service-red.txt; test ${PIPESTATUS[0]} -ne 0</automated>
  </verify>
  <done>行为测试文件存在，且因缺少 CaptureService/SessionCapture 实现而失败（RED），用例名覆盖 VALIDATION 中 STORE 映射。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: INV-6 与观测测试骨架 + VALIDATION Wave 0</name>
  <files>server/tests/initiatives/test_capture_inv6_guard.py, server/tests/initiatives/test_capture_observability.py, .planning/phases/141-capture/141-VALIDATION.md</files>
  <read_first>
    - server/tests/initiatives/test_repo_association_inv6_guard.py
    - server/tests/initiatives/test_memory_inv6_guard.py
    - server/tests/services/code_graph/test_query_observability.py
    - .planning/phases/141-capture/141-VALIDATION.md
  </read_first>
  <behavior>
    - INV-6：_ALLOWED_WRITER=initiatives/services/capture_service.py；扫描 create/bulk_create/get_or_create/update_or_create/update、SessionCapture( 实例化、.save(；排除 tests/migrations/models/writer（D-11）。
    - test_inv6_writer_module_actually_writes 断言 writer 含 SessionCapture.objects.create（实现前失败）。
    - test_writer_does_not_call_deferred_sinks 禁止 aschedule_ingestion/MemoryService/record_hook_writeback/background_runner/_resolve_report_project_id/lookup_project_by_branch（D-09/D-10/D-15）。
    - 观测：success started+completed caller/knowledge/duration_ms/initiated_by_user_id；failure started+failed；无 eval sampling；sentinel 问答与 sk-ant 不进日志 JSON；logger 抛错仍返回 Capture（D-13/D-14，OBS-01/02）。
  </behavior>
  <action>
按 PATTERNS 复制 association 守卫（含 update）与 query_observability 的 capture_logs 模式，新建两测试文件。更新 `141-VALIDATION.md`：Wave 0 Requirements 三个文件勾选为已创建；`wave_0_complete` 可仍为 false（实现未绿）；`nyquist_compliant` 保持 false 直到 Plan 04。禁止前端改动；禁止扩展 report_project_knowledge。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/initiatives/test_capture_inv6_guard.py tests/initiatives/test_capture_observability.py -x -q 2>&1 | tee /tmp/141-01-obs-guard-red.txt; test ${PIPESTATUS[0]} -ne 0</automated>
  </verify>
  <done>INV-6 与观测测试存在且 RED；VALIDATION Wave 0 文件清单与本计划一致。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → future CaptureService | 测试定义未信任输入（密钥、伪造 repository_id、非成员）必须仍落行且不绑未授权 FK |
| logs vs Capture body | 观测不得复制问答正文 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-141-01 | Information Disclosure | Capture body / logs | mitigate | Wave 0 测试强制 redaction + 日志无正文 sentinel（OBS-02） |
| T-141-02 | Tampering | ORM bypass | mitigate | INV-6 静态扫描测试骨架（STORE-03） |
| T-141-03 | Elevation of Privilege | FK linking | mitigate | unauthorized FK 空仍有行的行为测试（STORE-04） |
| T-141-04 | Tampering | idempotency | mitigate | first-write-wins 幂等测试 |
| T-141-05 | Denial of Service | logger failure | mitigate | logger 抛错不丢 Capture 测试 |
| T-141-SC | Tampering | package installs | accept | 本阶段无新 pip/npm 依赖 |
</threat_model>

<verification>
Wave 0：三个测试模块存在；pytest 对缺失实现失败；VALIDATION Wave 0 清单已指向这些文件。
</verification>

<success_criteria>
- STORE/OBS 契约以失败测试形式钉死
- 无生产实现、无 MCP/UI、无新依赖
- 无关 worktree 变更未被触碰
</success_criteria>

<output>
Create `.planning/phases/141-capture/141-01-SUMMARY.md` when done
</output>
