---
phase: 99-association-visualization-crosslink
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - server/knowledge/api/repository_artifacts.py
  - server/knowledge/api/urls.py
  - server/initiatives/serializers.py
  - server/tests/knowledge/test_repository_artifacts_api.py
  - server/tests/initiatives/test_artifact_serializer_entity_id.py
autonomous: true
requirements: [KDEP-11, KDEP-12]
must_haves:
  truths:
    - "GET /api/knowledge/repositories/{id}/artifacts/ 返回该仓库相关的交付文档（反向查询），每项带 entity_id 供前端跳知识实体"
    - "越权/不可见仓库返回空列表，未认证 401（access_scope fail-closed）"
    - "ArtifactSerializer 响应新增 entity_id 字段（= document 实体 id，供作战室跳知识实体）"
  artifacts:
    - path: "server/knowledge/api/repository_artifacts.py"
      provides: "RepositoryArtifactsView（薄委托 find_artifacts_by_repository + 每项补 entity_id）"
      contains: "find_artifacts_by_repository"
    - path: "server/knowledge/api/urls.py"
      provides: "repositories/<uuid>/artifacts/ 路由"
      contains: "knowledge-repository-artifacts"
    - path: "server/initiatives/serializers.py"
      provides: "ArtifactSerializer.entity_id SerializerMethodField"
      contains: "entity_id"
  key_links:
    - from: "server/knowledge/api/repository_artifacts.py"
      to: "knowledge.artifact_associations.ArtifactAssociationService.find_artifacts_by_repository"
      via: "adrf APIView 薄委托 Phase 98 反向查询（不重复遍历）"
      pattern: "find_artifacts_by_repository"
    - from: "server/initiatives/serializers.py::ArtifactSerializer.get_entity_id"
      to: "knowledge.models.generate_entity_id(EntityKind.DOCUMENT, \"artifact\", id)"
      via: "确定性 uuid5 派生 document 实体 id"
      pattern: "generate_entity_id"
---

<objective>
KDEP-11/KDEP-12 后端支撑：为「仓库视角反向展示相关交付文档」补一个最小反查端点 `GET /api/knowledge/repositories/{id}/artifacts/`（薄委托 Phase 98 `ArtifactAssociationService.find_artifacts_by_repository`，端点仅在此铺开反向面），并为「作战室→知识」跳转在 `ArtifactSerializer` 暴露确定性 `entity_id`（= document 实体 id）。

Purpose: 99-03 实体详情页（仓库实体）需要反查接口反向展示相关文档并双向可导航；99-04 作战室外部依赖区需要 artifact 的 entity_id 才能拼知识实体路由。二者都是纯只读、最小面积后端支撑，复用 Phase 98 服务与既有 access_scope，不新建关联真相源。

Output: 新反查端点 + 路由；`ArtifactSerializer` 新增 `entity_id`；两处测试。
</objective>

<execution_context>
@.cursor/gsd-core/workflows/execute-plan.md
@.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/99-association-visualization-crosslink/99-CONTEXT.md
@.planning/phases/98-artifact-repo-capability-link/98-03-SUMMARY.md

@server/knowledge/artifact_associations.py
@server/knowledge/api/artifact_associations.py
@server/knowledge/api/urls.py
@server/initiatives/serializers.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 仓库→相关交付文档反查端点（薄委托 Phase 98）+ 路由</name>
  <files>server/knowledge/api/repository_artifacts.py, server/knowledge/api/urls.py, server/tests/knowledge/test_repository_artifacts_api.py</files>
  <read_first>
    - server/knowledge/api/artifact_associations.py L1-56（`ArtifactAssociationsView` 范式：adrf APIView + IsAuthenticated + @extend_schema + started/completed 观测，完全对齐此写法）
    - server/knowledge/artifact_associations.py L145-229（`find_artifacts_by_repository(repository_id, *, user, capability_path=None, keyword=None)` 返回外形：list[{artifact_id, title, type_key, project_id, project_name, node_paths, keywords, score}]，不可见仓库 → []）
    - server/knowledge/api/urls.py L1-25（urlpatterns + import 组织；`artifacts/<uuid:artifact_id>/associations/` 命名范式）
    - server/knowledge/models.py L92-124（`generate_entity_id(kind, source_kind, source_id)`；document 工件实体 id = generate_entity_id(EntityKind.DOCUMENT, "artifact", str(artifact_id))）
  </read_first>
  <action>
    新建 `server/knowledge/api/repository_artifacts.py::RepositoryArtifactsView`（per KDEP-11、99-CONTEXT「反向：仓库视角实体详情反向展示相关交付文档」决策）——镜像 `ArtifactAssociationsView` 结构：adrf `APIView`、`permission_classes=[IsAuthenticated]`、`@extend_schema(tags=["knowledge"])`。`async def get(self, request, repository_id: uuid.UUID)`：调 `ArtifactAssociationService().find_artifacts_by_repository(repository_id, user=request.user)`（复用 Phase 98 反向查询，天然 access_scope fail-closed，不可见 → []）；对每一项补 `entity_id = str(generate_entity_id(EntityKind.DOCUMENT, "artifact", row["artifact_id"]))`（供前端 99-03 反向条目跳知识实体详情 → 双向可导航闭环）；返回 `Response({"artifacts": [...]})`。观测 `repository_artifacts_api_started/completed`（category=caller, component="knowledge", +repository_id/artifact_count/duration_ms），per AGENTS.md。
    在 `server/knowledge/api/urls.py` 新增 `path("repositories/<uuid:repository_id>/artifacts/", RepositoryArtifactsView.as_view(), name="knowledge-repository-artifacts")` 并 import。
    新建 `server/tests/knowledge/test_repository_artifacts_api.py`（复用 `test_artifact_associations_api.py` 的 fixture 与鉴权范式）：可见仓库有关联 → 200 + artifacts 列表含 entity_id（且 entity_id == generate_entity_id(DOCUMENT, artifact, id)）；越权/不可见仓库 → 200 + 空列表（find_artifacts_by_repository fail-closed 返回 []）；未认证 → 401。
  </action>
  <verify>
    <automated>cd server &amp;&amp; uv run pytest tests/knowledge/test_repository_artifacts_api.py -x</automated>
  </verify>
  <done>`GET /api/knowledge/repositories/{id}/artifacts/` 反查相关文档、每项带 entity_id；access_scope fail-closed（不可见空列表 / 未认证 401）；薄委托 Phase 98 服务不重复遍历。</done>
</task>

<task type="auto">
  <name>Task 2: ArtifactSerializer 暴露 entity_id（作战室→知识跳转锚点）</name>
  <files>server/initiatives/serializers.py, server/tests/initiatives/test_artifact_serializer_entity_id.py</files>
  <read_first>
    - server/initiatives/serializers.py L394-419（`ArtifactSerializer`：现有 type_key/type_name/ragable SerializerMethodField/CharField 范式 + Meta.fields/read_only_fields）
    - server/knowledge/models.py L34-50 + L92-124（`EntityKind.DOCUMENT` 字面值 + `generate_entity_id` 拼接规则 `f"{kind}:{source_kind}:{source_id}"`；工件 source_kind="artifact"）
  </read_first>
  <action>
    在 `ArtifactSerializer` 新增 `entity_id = serializers.SerializerMethodField()`（per KDEP-12、99-CONTEXT「工件→entity id 映射复用 generate_entity_id，后端 view 响应带 entity_id」决策——取「后端带 entity_id」的优雅方案，前端零 uuid5 计算）。实现 `def get_entity_id(self, obj) -> str: from knowledge.models import EntityKind, generate_entity_id; return str(generate_entity_id(EntityKind.DOCUMENT, "artifact", str(obj.id)))`（函数内 import 避免 initiatives→knowledge 顶层耦合）。把 `"entity_id"` 加入 `Meta.fields`（read_only_fields=fields 自动覆盖）。纯只读派生字段、零行为风险，不改既有字段。
    新建 `server/tests/initiatives/test_artifact_serializer_entity_id.py`：建一个 Artifact，`ArtifactSerializer(a).data["entity_id"]` 等于 `str(generate_entity_id(EntityKind.DOCUMENT, "artifact", str(a.id)))`，且与 Phase 98 `get_artifact_associations` 内部使用的 entity_id 派生一致（同一函数）。
  </action>
  <verify>
    <automated>cd server &amp;&amp; uv run pytest tests/initiatives/test_artifact_serializer_entity_id.py -x</automated>
  </verify>
  <done>`ArtifactSerializer` 响应含确定性 `entity_id`（= document 实体 id），前端可据此直接拼 `/knowledge/entities/{entity_id}`；测试验证派生一致。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→反查端点 | 未信任 repository_id 输入；跨用户可见范围必须 fail-closed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-02-01 | Information Disclosure | 仓库→文档反查 | mitigate | 委托 `find_artifacts_by_repository`（内建 `resolve_allowed_repository_ids` + 二次 space `resolve_allowed_project_ids` fail-closed）；不可见 → []，绝不泄漏越权工件 |
| T-99-02-02 | Spoofing | 反查端点鉴权 | mitigate | `IsAuthenticated`；未认证 401（与既有 associations 端点一致） |
| T-99-02-03 | Information Disclosure | entity_id 派生字段 | accept | entity_id 是 uuid5 确定性派生（不含凭证/正文），仅作跳转锚点；实体详情本身另有 access_scope 收口 |
| T-99-SC | Tampering | npm/pip/cargo installs | mitigate | 本 plan 不安装任何包（仓内视图/序列化器/路由 + 测试），无供应链面 |
</threat_model>

<verification>
- `cd server && uv run pytest tests/knowledge/test_repository_artifacts_api.py tests/initiatives/test_artifact_serializer_entity_id.py -x` → 全绿
- `cd server && uv run python -c "from django.urls import reverse; import django,os; os.environ.setdefault('DJANGO_SETTINGS_MODULE','friday.settings'); django.setup(); import uuid; print(reverse('knowledge-repository-artifacts', args=[uuid.uuid4()]))"` → `/api/knowledge/repositories/{id}/artifacts/`
- `cd server && uv run ruff check knowledge/api/repository_artifacts.py knowledge/api/urls.py initiatives/serializers.py` → 无错误
</verification>

<success_criteria>
- 反查端点返回仓库相关交付文档、每项带 entity_id、access_scope fail-closed（KDEP-11 反向支撑）
- `ArtifactSerializer` 暴露确定性 entity_id（KDEP-12 跳转锚点）
- 两处均薄委托 / 复用既有 Phase 98 服务与 generate_entity_id，最小面积、不新建真相源
</success_criteria>

<output>
Create `.planning/phases/99-association-visualization-crosslink/99-02-SUMMARY.md` when done.

## Artifacts this plan produces (99-02)
- `GET /api/knowledge/repositories/{id}/artifacts/`（`server/knowledge/api/repository_artifacts.py` + urls.py，名 `knowledge-repository-artifacts`）——每项带 entity_id
- `ArtifactSerializer.entity_id`（`server/initiatives/serializers.py`）
- 契约（供 99-03/99-04 前端消费）：反查响应 `{artifacts:[{artifact_id,title,type_key,project_id,project_name,node_paths,keywords,score,entity_id}]}`；Artifact 响应新增 `entity_id`
</output>
