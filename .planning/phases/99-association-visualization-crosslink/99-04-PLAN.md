---
phase: 99-association-visualization-crosslink
plan: 04
type: execute
wave: 3
depends_on: [99-01, 99-02, 99-03]
files_modified:
  - web/src/api/projectGalaxy.ts
  - web/src/components/project/warroom/ProjectGalaxyCard.vue
  - web/src/api/artifacts.ts
  - web/src/components/project/workbench/DependenciesSection.vue
  - web/src/locales/zh-CN.json
autonomous: true
requirements: [KDEP-10, KDEP-12]
must_haves:
  truths:
    - "作战室星图渲染 artifact / capability 新节点类型（语义色 + 图例 + 兜底列表 + 节点详情一致）"
    - "作战室「外部依赖」区每个工件查看处有一个『知识』次级入口，跳转到该工件知识实体（携带 entity_id）"
    - "跳转入口低调优雅（图标 + tooltip 次级操作），不喧宾夺主"
    - "作战室 ↔ 知识 双向闭环成立（反向由 KDEP-11 覆盖）"
  artifacts:
    - path: "web/src/api/projectGalaxy.ts"
      provides: "ProjectGalaxyNodeType 扩展 artifact/capability + 新 relation"
      contains: "capability"
    - path: "web/src/components/project/warroom/ProjectGalaxyCard.vue"
      provides: "artifact/capability 节点语义色 + 图例 + i18n label"
      contains: "artifact"
    - path: "web/src/components/project/workbench/DependenciesSection.vue"
      provides: "工件行『知识』跨入口（→ /knowledge/entities/{entity_id}）"
      contains: "/knowledge/entities/"
  key_links:
    - from: "web/src/components/project/workbench/DependenciesSection.vue"
      to: "/knowledge/entities/{artifact.entity_id}"
      via: "RouterLink 次级操作按钮（消费 99-02 ArtifactSerializer.entity_id）"
      pattern: "entity_id"
    - from: "web/src/components/project/warroom/ProjectGalaxyCard.vue"
      to: "web/src/api/projectGalaxy.ts (ProjectGalaxyNodeType)"
      via: "TYPE_COLOR/TYPE_LABEL 覆盖 artifact/capability（消费 99-01 galaxy payload）"
      pattern: "TYPE_COLOR"
---

<objective>
收官前端：把 99-01 galaxy payload 的 `artifact`/`capability` 新节点渲染出来（KDEP-10 可视化落地），并在作战室「外部依赖」区（`DependenciesSection.vue`）为每个工件加一个跳知识实体的低调次级入口（KDEP-12），闭合「作战室 ↔ 知识」双向环（反向由 KDEP-11 覆盖）。

Purpose: 让 KDEP-10 的星图新节点「看得见」、KDEP-12 的跨入口「点得到」，是里程碑 v0.16.3 收官的最后一块可视化 + 交叉入口。read-only 展示，复用既有星图组件与工件行样式、消费 99-02 暴露的 entity_id，最小新增、样式一致、闭环顺滑。

Output: projectGalaxy.ts 节点类型扩展；ProjectGalaxyCard 语义色/图例/i18n；artifacts.ts Artifact 增 entity_id；DependenciesSection 工件行知识入口；zh-CN 文案。
</objective>

<execution_context>
@.cursor/gsd-core/workflows/execute-plan.md
@.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/99-association-visualization-crosslink/99-CONTEXT.md
@.planning/phases/99-association-visualization-crosslink/99-01-PLAN.md
@.planning/phases/99-association-visualization-crosslink/99-02-PLAN.md

@web/src/api/projectGalaxy.ts
@web/src/components/project/warroom/ProjectGalaxyCard.vue
@web/src/components/project/workbench/DependenciesSection.vue
@web/src/api/artifacts.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 星图前端渲染 artifact / capability 新节点（KDEP-10 可视化）</name>
  <files>web/src/api/projectGalaxy.ts, web/src/components/project/warroom/ProjectGalaxyCard.vue, web/src/locales/zh-CN.json</files>
  <read_first>
    - web/src/api/projectGalaxy.ts L9-49（`ProjectGalaxyNodeType` 联合 + `ProjectGalaxyNode`/`ProjectGalaxyEdge`/`ProjectGalaxyResponse` 类型）
    - web/src/components/project/warroom/ProjectGalaxyCard.vue L26-50（`TYPE_COLOR` / `TYPE_LABEL` / `legend` computed——新增节点类型必须在这两张表补齐否则图例/兜底列表色缺失）
    - web/src/components/project/warroom/ProjectGalaxyCard.vue L162-209（兜底列表 + 节点详情用 `TYPE_COLOR[n.type]` / `TYPE_LABEL[n.type]`，需容纳新类型）
    - .planning/phases/99-association-visualization-crosslink/99-01-PLAN.md（新节点 type `artifact`/`capability`；新 relation `HAS_ARTIFACT`/`ARTIFACT_REPO`/`ARTIFACT_CAPABILITY`）
  </read_first>
  <action>
    1. `web/src/api/projectGalaxy.ts`：把 `ProjectGalaxyNodeType` 联合扩为 `... | 'artifact' | 'capability'`（per 99-01 契约）。`ProjectGalaxyNode` 已是开放 `[k: string]: unknown`，无需改字段（type_key/carrier/path 落在索引签名）；relation 为 string 无需改。
    2. `ProjectGalaxyCard.vue`：`TYPE_COLOR` 新增 `artifact` / `capability` 语义色（与既有 design token 语义区分：如 artifact=amber `#f59e0b`、capability=rose/pink `#ec4899`，遵循 99-CONTEXT「artifact 节点视觉由 plan/execute 按既有约定自定」的 Claude 裁量），`TYPE_LABEL` 新增 `artifact: 'artifact'` / `capability: 'capability'`。`legend` / 兜底列表 / 节点详情自动纳入（它们遍历 `TYPE_COLOR` keys）。nodeColor 已有 `?? '#94a3b8'` 兜底保持。
    3. i18n：`web/src/locales/zh-CN.json` 新增 `projects.warroom.galaxy.type.artifact`（如「工件」）、`projects.warroom.galaxy.type.capability`（如「能力」），默认中文。
    纯可视化映射，不改数据流 / 不新增依赖；星图用内联色值圆点、无动态 icon class，无需 safelist。
  </action>
  <verify>
    <automated>cd web &amp;&amp; pnpm exec vue-tsc --noEmit</automated>
  </verify>
  <done>星图图例/兜底列表/节点详情正确渲染 artifact/capability 节点（语义色 + 中文 label）；类型检查通过。</done>
</task>

<task type="auto">
  <name>Task 2: 作战室外部依赖区工件『知识』跨入口（KDEP-12）</name>
  <files>web/src/api/artifacts.ts, web/src/components/project/workbench/DependenciesSection.vue, web/src/locales/zh-CN.json</files>
  <read_first>
    - web/src/api/artifacts.ts L19-35（`Artifact` interface：补 `entity_id`）
    - web/src/components/project/workbench/DependenciesSection.vue L219-259（工件行：现有「查看」`deps-view-artifact-btn` + 「飞书/外链打开」按钮组，新入口与之并列）
    - web/src/components/project/workbench/DependenciesSection.vue L1-14（import 区：需引 RouterLink——本仓 Vue 3 + unplugin-vue-router，RouterLink 全局可用，确认无需显式 import）
    - .planning/phases/99-association-visualization-crosslink/99-02-PLAN.md（`ArtifactSerializer` 新增 `entity_id` 契约）
  </read_first>
  <action>
    1. `web/src/api/artifacts.ts`：`Artifact` interface 新增 `entity_id: string`（消费 99-02 后端新字段），类型安全。
    2. `DependenciesSection.vue`：在工件行操作区（`deps-view-artifact-btn` 与外链按钮同一 `<div class="flex items-center gap-1.5 shrink-0">`）新增一个「知识」次级入口——用 `RouterLink :to="\`/knowledge/entities/${a.entity_id}\`"`（per KDEP-12 与 99-CONTEXT「携带 artifact 对应 entity id」「与查看/飞书打开并列的次级操作，图标+tooltip，不喧宾夺主」决策）。样式对齐既有次级按钮（`inline-flex ... border border-border/60 px-2 h-7 text-xs ... hover:text-primary`），图标用 `icon-[lucide--brain]`（与知识区 `deps-knowledge` 图标一致）+ `:title="t('projects.workbench.deps.viewKnowledge')"` tooltip，`data-testid="deps-view-knowledge-btn"`。低调：仅图标 + tooltip（可选极短文案），不抢「查看」主操作视觉。
    3. i18n：`web/src/locales/zh-CN.json` 新增 `projects.workbench.deps.viewKnowledge`（如「知识实体」），默认中文。
    仅只读跳转，不引入新依赖、不新增 safelist（lucide--brain 既用于本组件知识区已 safelist）。
  </action>
  <verify>
    <automated>cd web &amp;&amp; pnpm exec vue-tsc --noEmit</automated>
  </verify>
  <done>每个工件行有一个低调『知识』入口跳 `/knowledge/entities/{entity_id}`（携带 entity_id）；样式与既有查看/打开次级按钮一致；类型检查通过。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 浏览器→知识实体路由 | 前端仅拼接 entity_id 路由；目标实体详情页有 access_scope 收口，前端不做鉴权判断 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-04-01 | Information Disclosure | 知识实体跳转 | mitigate | entity_id 仅路由锚点；目标 `/knowledge/entities/{id}` 后端 access_scope fail-closed，越权访问后端 404，前端不泄漏内容 |
| T-99-04-02 | Cross-Site Scripting | 星图节点 label 渲染 | mitigate | 节点 label / 工件标题走文本插值（非 v-html），后端字段非 HTML sink |
| T-99-04-03 | Availability | 星图新节点缺失映射 | mitigate | `TYPE_COLOR` 有 `?? '#94a3b8'` 兜底；新增 type 已补齐图例/label，无未定义崩溃 |
| T-99-SC | Tampering | npm/pip/cargo installs | mitigate | 本 plan 不安装任何包（仅前端类型/组件/文案改动，复用既有 lucide 图标），无供应链面 |
</threat_model>

<verification>
- `cd web && pnpm exec vue-tsc --noEmit` → 通过
- `cd web && pnpm exec eslint src/api/projectGalaxy.ts src/components/project/warroom/ProjectGalaxyCard.vue src/api/artifacts.ts src/components/project/workbench/DependenciesSection.vue` → 无错误
- 人工（延后 UAT）：星图见工件/能力彩色节点 + 图例；作战室工件行点『知识』跳到该工件知识实体详情（KDEP-11 关联可回看 → 闭环）
</verification>

<success_criteria>
- 星图渲染 artifact/capability 新节点（语义色 + 图例 + 兜底一致，KDEP-10 可视化落地）
- 作战室工件行有低调『知识』入口，携带 entity_id 跳知识实体（KDEP-12）
- 「作战室 ↔ 知识」双向闭环成立（反向 KDEP-11）
- 最小新增、复用既有组件/样式、类型安全、i18n 中文
</success_criteria>

<output>
Create `.planning/phases/99-association-visualization-crosslink/99-04-SUMMARY.md` when done.

## Artifacts this plan produces (99-04)
- `web/src/api/projectGalaxy.ts`：`ProjectGalaxyNodeType` 扩 artifact/capability
- `web/src/components/project/warroom/ProjectGalaxyCard.vue`：新节点语义色 + 图例 + i18n
- `web/src/api/artifacts.ts`：`Artifact.entity_id`
- `web/src/components/project/workbench/DependenciesSection.vue`：工件行『知识』跨入口
- `web/src/locales/zh-CN.json`：galaxy type + deps.viewKnowledge 文案
</output>
