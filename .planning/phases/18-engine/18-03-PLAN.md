---
phase: 18-engine
plan: 03
type: execute
wave: 3
depends_on: ["18-02"]
files_modified:
  - server/workflows/engine/scheduler.py
  - server/tests/workflows/test_engine_waiting.py
  - server/tests/workflows/test_engine_deadlock.py
  - server/tests/workflows/test_engine.py
autonomous: true
requirements: [ENG-01, ENG-04]
must_haves:
  truths:
    - "末端 waiting_event 节点的执行收口为 SUSPENDED，绝不 amark_completed（CONTEXT 锁定：等待就是 suspended）"
    - "带下游的 waiting_event 执行同样立即 SUSPENDED（run_sync 立即返回），5s 轮询分支被删除，不再产生永久 running 僵尸"
    - "挂起时 execution_suspended hook 被触发（WS 广播链路可达）"
    - "waiting_approval 节点不再热循环：单次执行内节点 execute 恰好一次"
    - "主循环死锁（pending 非空、无 ready、无 waiting/running）转 FAILED，error_message 末行可 json.loads 且含 pending/short_id/waiting_on/handle"
    - "stop_before_node_id 提前退出出口与正常出口走同一收口判定（Pitfall 5 双出口覆盖）"
  artifacts:
    - path: "server/workflows/engine/scheduler.py"
      provides: "完成/挂起/死锁单一收口判定函数，主循环两个出口共用；轮询分支删除"
      contains: "execution_suspended"
    - path: "server/tests/workflows/test_engine_waiting.py"
      provides: "waiting_event/waiting_approval 挂起语义集成测试（ENG-01 主循环侧）"
    - path: "server/tests/workflows/test_engine_deadlock.py"
      provides: "死锁转 failed + 结构化诊断集成测试（ENG-04 主循环侧）"
  key_links:
    - from: "server/workflows/engine/scheduler.py"
      to: "server/workflows/engine/routing.py"
      via: "收口判定调用 diagnose_deadlock"
      pattern: "diagnose_deadlock"
    - from: "server/workflows/engine/scheduler.py"
      to: "server/workflows/hooks"
      via: "amark_suspended 后 hooks.trigger('execution_suspended')"
      pattern: "execution_suspended"
---

<objective>
把主循环的完成/挂起/死锁三类终局判定收口为单一函数并修复 ENG-01 全部主循环侧故障：waiting_event/waiting_approval 都进 waiting 集合不加回 pending（消灭热循环）、删除 5s 轮询分支、waiting 非空即 amark_suspended + execution_suspended hook、死锁经 routing.diagnose_deadlock 转 FAILED 并写结构化 error_message、两个循环出口（正常退出 + stop_before 提前退出）共用收口。

Purpose: 落实 CONTEXT 锁定决策"waiting_event 完成判定（ENG-01）"与"死锁诊断（ENG-04）"——执行状态真实可信：等待就是 suspended、死锁就是 failed，不再出现"显示完成实际没跑完 / 永远 running"。回调续跑侧的恢复语义归 18-04。
Output: scheduler 收口改造 + test_engine_waiting.py / test_engine_deadlock.py 集成测试 + 既有失效测试改写。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-engine/18-CONTEXT.md
@.planning/phases/18-engine/18-RESEARCH.md
@.planning/phases/18-engine/18-PATTERNS.md
@.planning/phases/18-engine/18-02-SUMMARY.md
@server/workflows/engine/scheduler.py
@server/workflows/engine/routing.py
</context>

## Artifacts this phase produces（本计划部分）

- 修改 `server/workflows/engine/scheduler.py`：
  - 新增私有收口判定方法（建议名 `_finalize_run_state`，async）：输入 waiting/pending/failed/completed 集合 + node_statuses + dag + node_outputs，按优先级判定 suspended / deadlock-failed / failed / completed 并执行对应 amark_* + hook
  - waiting_event 与 waiting_approval 统一进 `waiting_nodes_mem: set[str]`，均不加回 pending（:819-825 改造）
  - 删除 5s 轮询分支（:635-677，含 all_blocked 检查与单键刷新缺陷点 :672）
  - 死锁分支（:679-697）替换为 `routing.diagnose_deadlock` + 结构化 amark_failed
  - 正常出口（:838-851）与 stop_before 出口（:611-621）均改走收口函数
- 新测试 `server/tests/workflows/test_engine_waiting.py`（主循环侧集成类）
- `server/tests/workflows/test_engine_deadlock.py` 追加集成类
- 改写 `server/tests/workflows/test_engine.py` 中依赖旧轮询语义的用例

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 完成/挂起/死锁收口函数 + 删轮询 + 热循环修复 + waiting 集成测试</name>
  <files>server/workflows/engine/scheduler.py, server/tests/workflows/test_engine_waiting.py</files>
  <read_first>
    - server/workflows/engine/scheduler.py:505-859（主循环全段——重点 :611-621 stop_before 出口、:635-677 轮询分支、:679-697 现状死锁分支、:819-825 waiting 处理、:838-851 正常出口）
    - server/workflows/engine/scheduler.py:652-662（挂起动作三连：amark_suspended + execution_suspended hook——正确写法保留复用，问题只在可达性）
    - .planning/phases/18-engine/18-RESEARCH.md §1（六条故障链）、Pattern 1（收口设计）、Pitfall 1/5（回调成唯一推进通道、双出口）、Assumption A3（轮询删除后 timeout 兜底）
    - server/tests/workflows/test_error_handling.py:287-383（run_sync 集成测试范式 + 类装饰器组合）
    - server/tests/workflows/test_hooks.py（hook 事件断言范式——execution_suspended 触发断言参考）
  </read_first>
  <behavior>
    - Test 1（末端等待 ⇒ suspended）: waiting_terminal_workflow + run_sync → execution.status == SUSPENDED（绝非 COMPLETED）；WaitEventNode 的 NE 状态为 WAITING_EVENT
    - Test 2（带下游等待 ⇒ suspended 且立即返回）: waiting_workflow + run_sync → 立即返回 SUSPENDED（无 5s 轮询延时——断言耗时 < 3s）；下游 NE 仍 PENDING
    - Test 3（execution_suspended hook 触发）: 注册自定义 hook（engine.hooks.register("execution_suspended", spy)）→ Test 1 场景下 spy 被调用且 execution.status == "suspended"
    - Test 4（waiting_approval 不热循环）: trigger→WaitApprovalNode 工作流 + run_sync → SUSPENDED 且 `WaitApprovalNode._exec_count == 1`（恰好一次，热循环复发即 >1）
    - Test 5（stop_before 出口不误判）: waiting_workflow 配 stop_before_node_id=下游节点 → 不得 COMPLETED（suspended 或按 stop_before 语义停止，断言非 COMPLETED 且无 running 残留）
  </behavior>
  <action>
    改造 `_run_execution`：

    1. 新增 `waiting_nodes_mem: set[str]`；结果处理段（:819-825）：`waiting_approval` 与 `waiting_event` 统一——从 pending 移除（已移除则保持）、加入 waiting_nodes_mem、node_statuses[node_id] = STATUS_WAITING，**都不加回 pending**（waiting_approval 不再加回 :819-821，消灭 §1.4 热循环；审批推进完全依赖 approve_node 回调闭环，Pitfall 1 已核实七条恢复入口）。
    2. 新增收口方法 `async def _finalize_run_state(self, execution, dag, *, pending, waiting, failed, completed, node_statuses, node_handles, node_outputs) -> None`，判定优先级：
       a. waiting 非空 → `amark_suspended()` + `hooks.trigger("execution_suspended", ...)` + structlog `workflow_suspended`（照抄 :652-662 三连与日志风格）+ return；
       b. waiting 空且 pending 非空 → 构造 RoutingState 调 `routing.diagnose_deadlock(dag, state, pending)`；返回 dict 时 `json.dumps(diag, ensure_ascii=False)` 拼 `f"工作流死锁：{len(pending)} 个节点无法调度\n{structured}"` 写 `amark_failed` + execution_failed hook（编码三要素照抄 scheduler.py:1019-1032 的 Phase 17 约定）；
       c. failed 非空 → 现状 amark_failed 路径（:855-858 文案保留）；
       d. 否则 → 现状 final_output 收集 + amark_completed + execution_completed hook（:860-868 逻辑迁入）。
    3. 删除 :635-677 整段（all_blocked 检查 + asyncio.sleep(5) 轮询 + :672 单键刷新）；删除/替换 :679-697 旧死锁分支——循环内"无 ready 且 pending 非空"时直接调 `_finalize_run_state` 后 return（覆盖死锁与 waiting 混合场景：waiting 非空先挂起，恢复续跑后纯死锁在 18-04 重入路径暴露）。
    4. 正常出口（:838-851）与 stop_before 出口（:611-621）改为统一调 `_finalize_run_state`（Pitfall 5：stop_before 出口现状同样 amark_completed 不查 waiting——必须一并走收口；stop_before 的"提前完成"语义保留为 d 分支前置旁路，但 waiting 非空时仍判 suspended）。
    5. A3 核查：`rg "timeout_at" server/workflows/ server/tests/workflows/test_timeout_scheduler.py` 确认 wait 节点超时处理者是否依赖主循环线程存活；若超时由外部调度器（django-apscheduler/WorkflowEventSubscription 处理者）兜底则零影响，若依赖轮询线程则在 SUMMARY 记录已知行为变化（不在本阶段扩大修复，CONTEXT 边界）。

    测试写入新文件 `server/tests/workflows/test_engine_waiting.py`：类 `TestWaitingSuspension`（@pytest.mark.asyncio + @pytest.mark.django_db(transaction=True)，消费 18-02 conftest 的 engine/engine_test_nodes/waiting_*_workflow 夹具）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/workflows/test_engine_waiting.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `rg "asyncio.sleep\(5\)" server/workflows/engine/scheduler.py` 零命中（轮询分支已删）
    - `rg "_finalize_run_state" server/workflows/engine/scheduler.py` ≥ 3 处命中（定义 + 正常出口 + stop_before 出口/循环内无 ready 分支）
    - Test 1-5 全绿；Test 4 的 `_exec_count == 1` 断言存在（热循环防回归锚点）
    - `rg "waiting_approval" server/workflows/engine/scheduler.py` 上下文中不再有"加回 pending"逻辑
  </acceptance_criteria>
  <done>等待即 suspended（双出口覆盖）、hook 可达、热循环消灭、轮询僵尸根除</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 死锁结构化诊断集成测试（ENG-04 主循环侧）</name>
  <files>server/tests/workflows/test_engine_deadlock.py</files>
  <read_first>
    - server/tests/workflows/test_engine_deadlock.py（18-01 的 TestDiagnoseDeadlockUnit 类——集成类并列追加，勿动 unit 部分）
    - server/tests/workflows/test_error_handling.py:709-722（error_message 末行 json.loads 断言范式）
    - server/workflows/engine/scheduler.py（Task 1 产出的 _finalize_run_state 签名）
  </read_first>
  <behavior>
    - Test 1（收口判定写 FAILED + 结构化诊断）: 真实 execution（手建简单工作流 + start 后人工构造状态）+ 人造死锁入参（pending 非空、waiting 空、statuses 无 ready 可能）直接调 `engine._finalize_run_state(...)` → execution.status == FAILED；error_message 首行含"工作流死锁"中文一句话；末行 `json.loads` 成功且 `payload["reason"] == "deadlock"`、`payload["pending"][0]` 含 {"node","short_id","waiting_on"}、waiting_on 元素含 {"node","short_id","status","handle"}
    - Test 2（诊断不含输出值）: 给 node_outputs 塞入哨兵串（如 "SECRET_OUTPUT_VALUE"）→ error_message 全文不含该哨兵串（V5 信息泄露防线端到端）
    - Test 3（waiting 优先于死锁）: 同样的死锁状态但 waiting 集合非空 → SUSPENDED 而非 FAILED（判定优先级锚定）
  </behavior>
  <action>
    在 `server/tests/workflows/test_engine_deadlock.py` 追加 `TestDeadlockIntegration` 类（@pytest.mark.asyncio + @pytest.mark.django_db(transaction=True)）。说明：18-02 级联修复后，正常图结构不再能自然产生死锁（这正是修复目标），死锁守卫是针对状态不一致等异常态的防御网——故集成测试通过直接调用收口函数注入人造状态来验证 scheduler 写入侧的完整链路（FAILED 状态 + error_message 编码 + execution_failed hook）；真实状态重建路径的死锁兜底（CANCELLED 依赖等场景）归 18-04 测试。工作流可用 conftest 的 branch_workflow 或最简两节点工厂，execution 经 `WorkflowExecution.objects.acreate`（或 start 后捞取）取得真实行。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/workflows/test_engine_deadlock.py -x</automated>
  </verify>
  <acceptance_criteria>
    - Test 1 的 json.loads 断言键集与 18-01 diagnose_deadlock 输出形状一致（reason/pending/short_id/waiting_on/handle）
    - Test 2 哨兵串断言存在（`"SECRET_OUTPUT_VALUE" not in execution.error_message`）
    - 18-01 的 TestDiagnoseDeadlockUnit 类零改动；`cd server && uv run pytest tests/workflows/test_engine_deadlock.py -x` 全绿
  </acceptance_criteria>
  <done>死锁转 FAILED 的写入链路（状态 + 结构化 error_message + hook）经集成测试锁定，Phase 21 可直接 JSON.parse</done>
</task>

<task type="auto">
  <name>Task 3: 既有失效测试改写 + 全量回归</name>
  <files>server/tests/workflows/test_engine.py</files>
  <read_first>
    - server/tests/workflows/test_engine.py（重点 test_approval_node_waits :260-285 附近——断言 `waiting_count >= 0` 恒真；test_cancel_running_execution——依赖旧轮询/running 语义）
    - .planning/phases/18-engine/18-PATTERNS.md "既有测试需同步改写"小节
  </read_first>
  <action>
    按新语义改写两个用例：
    - `test_approval_node_waits`：删除恒真断言，改为断言 `execution.status == ExecutionStatus.SUSPENDED` 且审批节点 NE 状态为 WAITING_APPROVAL（真实锁定 ENG-01 语义）；
    - `test_cancel_running_execution`：旧语义依赖执行线程存活轮询；按新语义调整——若用例依赖"挂起后取消"，断言对 SUSPENDED 执行取消生效；若依赖"running 中取消"，改用 SlowNode 类长任务节点维持 running 窗口（参考 test_error_handling.py 的 SlowNode 范式）。
    其余 test_engine.py 用例若因新语义失败（如依赖分支两边都跑、依赖轮询推进），逐个按新语义改写断言；改写清单记入 SUMMARY。禁止为让旧测试通过而在 scheduler 中保留旧语义旁路。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/workflows/ -x</automated>
  </verify>
  <acceptance_criteria>
    - `rg "waiting_count >= 0" server/tests/workflows/test_engine.py` 零命中（恒真断言已删）
    - `cd server && uv run pytest tests/workflows/ -x` 全量绿（含全部新旧测试）
    - `cd server && uv run ruff format --check workflows/engine/scheduler.py && uv run ruff check workflows/engine/` 通过
  </acceptance_criteria>
  <done>既有测试与新状态机语义对齐，全目录回归绿，无旧语义兼容旁路残留</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 死锁诊断 → execution.error_message → 前端/API | 诊断经 API 暴露给用户，不得携带节点输出值 |
| 挂起状态 → WS 广播 → 浏览器 | 仅广播 {event, execution_id, status} 元数据（既有 WebSocketBroadcastHook 形状不变） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-03 | Information Disclosure | _finalize_run_state 死锁写入 | mitigate | error_message 仅由 diagnose_deadlock 输出拼装（18-01 已保证零输出值），Task 2 Test 2 哨兵串端到端断言 |
| T-18-04 | DoS | 删除轮询后的执行线程生命周期 | mitigate | waiting 即挂起返回，线程不再常驻——消除"永久 running 僵尸线程"这一现存 DoS 面；A3 timeout 兜底核查记录于 SUMMARY |
| T-18-SC | Tampering | 包安装 | accept | 零新依赖 |
</threat_model>

<verification>
- `cd server && uv run pytest tests/workflows/test_engine_waiting.py tests/workflows/test_engine_deadlock.py -x` 全绿
- `cd server && uv run pytest tests/workflows/ -x` 全量零回归（改写项在 SUMMARY 列出）
- `cd server && uv run ruff format --check workflows/engine/scheduler.py && uv run ruff check workflows/engine/`
</verification>

<success_criteria>
- waiting_event（末端/带下游）与 waiting_approval 场景执行收口一律 SUSPENDED，hook 触发，run_sync 立即返回
- waiting_approval 节点单次执行恰好一次（热循环消灭）
- 主循环死锁转 FAILED，error_message 符合 Phase 17 结构化约定且零输出值泄露
- 双出口（正常 + stop_before）共用收口；轮询分支与旧死锁分支删除
</success_criteria>

<output>
完成后创建 `.planning/phases/18-engine/18-03-SUMMARY.md`，记录：_finalize_run_state 最终签名（18-04 重入路径消费）、A3 timeout 兜底核查结论、既有测试改写清单、删除代码段范围。
</output>
