---
phase: 18-engine
plan: 04
type: execute
wave: 4
depends_on: ["18-03"]
files_modified:
  - server/workflows/engine/scheduler.py
  - server/subagent/api/callbacks.py
  - server/feishu/callbacks/coding_callback.py
  - server/tests/workflows/test_engine_waiting.py
  - server/tests/workflows/test_engine_deadlock.py
autonomous: true
requirements: [ENG-01, ENG-02, ENG-04]
must_haves:
  truths:
    - "回调续跑（事件到达）后从挂起点恢复调度，路由/级联/完成/死锁语义与主循环一致——同一工作流走主循环或走挂起-续跑，最终 NE 状态集合相同（CONTEXT 锁定：恢复后语义与主循环一致）"
    - "带 _resume_from_callback 标记且仍 WAITING_* 的节点在续跑入口被经 _execute_node 重跑（消费标记），容器回调断裂链路修复（RESEARCH A1）"
    - "并发续跑互斥：执行级原子抢锁，同一挂起执行同时到达两个回调只有一个续跑"
    - "coding_callback 的手工迷你调度器删除，回调路径不再手工构建 ExecutionContext"
    - "重建续跑路径的死锁兜底：恢复后纯死锁状态（如依赖 CANCELLED）转 FAILED 而非静默挂死"
  artifacts:
    - path: "server/workflows/engine/scheduler.py"
      provides: "_continue_after_node 重入式薄入口 + 执行级互斥；_check_dependencies_ready/_check_execution_complete 旧判定退役"
      contains: "_continue_after_node"
    - path: "server/subagent/api/callbacks.py"
      provides: "容器回调：标记/重跑节点 → 统一续跑入口"
    - path: "server/feishu/callbacks/coding_callback.py"
      provides: "分支确认回调收敛到统一入口（手工调度器删除）"
    - path: "server/tests/workflows/test_engine_waiting.py"
      provides: "回调续跑/双路径一致性/互斥集成测试"
  key_links:
    - from: "server/workflows/engine/scheduler.py"
      to: "server/workflows/engine/routing.py"
      via: "重入路径复用同一 routing 纯函数（重建 RoutingState 从 DB output_data._next_handle 填 handles）"
      pattern: "_next_handle"
    - from: "server/subagent/api/callbacks.py"
      to: "server/workflows/engine/scheduler.py"
      via: "延迟导入 WorkflowEngine 调统一续跑入口"
      pattern: "from workflows.engine.scheduler import WorkflowEngine"
---

<objective>
把回调续跑改造为"重建状态重入主循环"：`_continue_after_node` 退化为薄入口——(a) 带恢复标记且仍 WAITING_* 的节点先经 `_execute_node` 重跑，(b) 节点终态后从 DB 重建状态集合重入 `_run_execution`，配执行级原子抢锁防双循环并发。容器回调断裂链路（RESEARCH A1）与 coding_callback 第三套手工调度器一并收敛。

Purpose: 落实 CONTEXT 锁定决策"回调续跑后从挂起点恢复调度，恢复后语义与主循环一致"（ENG-01）与"两条路径共用同一路由函数"（ENG-02）——这是审计定性"两套路由实现漂移"的最终消除点；`_check_execution_complete` 的死锁兜底补齐 ENG-04 的回调侧。
Output: scheduler 重入式续跑 + 互斥 + 两个回调模块收敛 + 双路径一致性/断裂修复/互斥集成测试。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-engine/18-CONTEXT.md
@.planning/phases/18-engine/18-RESEARCH.md
@.planning/phases/18-engine/18-PATTERNS.md
@.planning/phases/18-engine/18-03-SUMMARY.md
@server/workflows/engine/scheduler.py
@server/subagent/api/callbacks.py
@server/feishu/callbacks/coding_callback.py
</context>

## Artifacts this phase produces（本计划部分）

- 修改 `server/workflows/engine/scheduler.py`：
  - `_continue_after_node` 重构为重入式薄入口（标记重跑 + 状态重建 + 重入 `_run_execution`）
  - 执行级互斥：`WorkflowExecution.objects.filter(pk=..., status__in=[SUSPENDED, RUNNING]).aupdate(...)` 原子抢锁（精确条件见 Task 1）
  - `_check_dependencies_ready`（:1517-1532，仅认 COMPLETED 的 Pitfall 4 根因）与 `_check_execution_complete`（:1488-1515）旧判定退役/委托收口函数
  - `resume_execution` 的 COMPLETED→SKIPPED 改写手法废弃（按真实 NE 状态重建）
  - tolerated fallback 输出持久化到 NE.output_data（重建可恢复，Pitfall 4 决策）
- 修改 `server/subagent/api/callbacks.py`：`_schedule_workflow_resume` 走"重置 RUNNING → _execute_node 重跑 → 统一续跑入口"
- 修改 `server/feishu/callbacks/coding_callback.py`：`_schedule_branch_confirmation` 手工 ExecutionContext/迷你调度器删除，收敛统一入口
- `server/tests/workflows/test_engine_waiting.py` 追加回调续跑测试类；`test_engine_deadlock.py` 追加重建路径死锁兜底用例

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: _continue_after_node 重入式重构 + 执行级互斥 + 旧判定退役</name>
  <files>server/workflows/engine/scheduler.py, server/tests/workflows/test_engine_waiting.py</files>
  <read_first>
    - server/workflows/engine/scheduler.py:1371-1554（_continue_after_node 递归骨架 :1371-1457、_handle_node_failure :1459-1486、_check_execution_complete :1488-1515、_check_dependencies_ready :1517-1532、_collect_all_outputs :1534-1554）
    - server/workflows/engine/scheduler.py:1131-1181（resume_execution 的"重建 initial_outputs → 重入"示范与待废弃的 COMPLETED→SKIPPED 改写 :1151-1156）
    - server/workflows/engine/scheduler.py（18-02/03 改造后的 _run_execution 签名、node_statuses/node_handles 维护点、_finalize_run_state 签名——以 18-02/18-03 SUMMARY 为准）
    - .planning/phases/18-engine/18-RESEARCH.md Pattern 2（重入方案 + 互斥要求原文）、Pitfall 2/4/6（handle 双来源、COMPLETED-only 判定、并发双循环）、§1 恢复路径全景表（七条入口）
    - server/workflows/models/execution.py:17-43（状态枚举——抢锁 filter 条件用）
  </read_first>
  <behavior>
    - Test 1（续跑恢复完成）: waiting_workflow run_sync → SUSPENDED；测试手工把 Waiter NE amark_completed（模拟 wait_feishu 事件到达范式）→ 调 `engine._continue_after_node(execution, waiter_ne)` → 下游节点执行完成，execution.status == COMPLETED
    - Test 2（双路径一致性，ENG-02 锚点）: 同一结构的分支工作流两次执行——路径 A：纯主循环 run_sync；路径 B：分支节点前插 WaitEventNode，挂起后回调续跑——两次执行最终各 NE 状态（按 node name 对照 completed/skipped）完全一致
    - Test 3（标记重跑，A1 断裂修复）: Waiter NE 处于 WAITING_EVENT 且 output_data 含 `_resume_from_callback: True` → 调 _continue_after_node → WaitEventNode 被重新 execute（类计数器 +1）且执行推进（修复前该场景节点不重跑、下游不执行——红测先行坐实 A1）
    - Test 4（并发互斥）: SUSPENDED 执行连续两次调 _continue_after_node（第二次在第一次已抢锁置 RUNNING 后）→ 第二次放弃续跑（无重复节点执行：节点类计数器不多加；structlog 有放弃记录）
    - Test 5（重建路径死锁兜底，写入 test_engine_deadlock.py）: 人工构造执行——节点 X 的 NE 为 PENDING、其前置依赖 NE 为 CANCELLED（非终态解析集合、非 waiting/running）→ 续跑入口重建后 → execution.status == FAILED 且 error_message 末行 json.loads 含 reason=="deadlock"
  </behavior>
  <action>
    重构 `_continue_after_node(execution, node_execution)`（保持签名——七条恢复入口的既有调用零改动）：

    1. **标记重跑**（消费容器回调标记）：若 `node_execution.status` 仍为 WAITING_*（EVENT/APPROVAL/INPUT）且 `output_data` 含 `_resume_from_callback` 类恢复标记——先把 NE 重置 RUNNING，经 `self._execute_node(...)` 重跑该节点（复用重试/超时/on_error 全套；入参 dag/node_outputs 由下述重建步骤先行构造），节点终态后进入步骤 3。无标记且仍 WAITING_* → structlog warning 后 return（等待真正的事件回调，不空转）。
    2. **执行级互斥**：`WorkflowExecution.objects.filter(pk=execution.pk, status=ExecutionStatus.SUSPENDED).aupdate(status=ExecutionStatus.RUNNING)` 返回行数 0 时——若执行当前已是 RUNNING（主循环存活或他人已抢到）则放弃并 structlog `resume_lock_lost`；非 SUSPENDED 非 RUNNING（终态）直接 return。注意：approve_node 等入口在执行仍 RUNNING（旧轮询时代）调用本函数的场景已随 18-03 消亡——挂起是唯一等待形态，抢锁条件以 SUSPENDED 为主；为兼容回调先于挂起落库的竞态窗口，抢锁失败且 status==RUNNING 时按放弃处理（主循环线程仍在，由其收口）。
    3. **重建重入**：`_collect_all_outputs` 重建双键 node_outputs；遍历执行的全部 NE 按真实状态填 completed/failed/skipped/waiting 集合与 node_statuses（FAILED 且曾 tolerated 的节点按 output_data 中 tolerated 标记归 tolerated——见第 5 点持久化）、node_handles 从各 NE `output_data["_next_handle"]` 填（Pitfall 2 的 DB 来源）；pending = DAG 中无 NE 终态记录的节点；然后重入 `_run_execution` 的调度循环（推荐：抽出 `_run_scheduling_loop(execution, dag, 初始集合...)` 供首跑与重入共用，首跑入口零行为变化）。重入后死锁/挂起/完成判定天然走 18-03 的 `_finalize_run_state`（Test 5 的兜底即由此达成）。
    4. **旧判定退役**：`_check_dependencies_ready` 与 `_check_execution_complete` 的调用点随递归骨架移除而消亡——删除两方法或保留 `_check_execution_complete` 为兼容入口并委托收口函数（被 workflows/api/views.py 等外部调用时保留委托，先 `rg "_check_execution_complete|_check_dependencies_ready" server/` 确认全部调用点）。
    5. **tolerated fallback 持久化**：主循环 tolerated 处理点（18-02 后的 :804-811 对应位置）把 fallback 输出与 `_tolerated: true` 标记写入 NE.output_data（amark_failed 后补 asave 或扩展现有写入）——重建时据此恢复 tolerated 语义与下游可见的 fallback 值（Pitfall 4 的显式决策：持久化方案）。
    6. **resume_execution 改写废弃**：:1151-1156 的 COMPLETED→SKIPPED 改写删除，改为按真实 NE 状态重建（skipped 已是正式终态，复用步骤 3 的重建逻辑）。

    测试写入 test_engine_waiting.py 新类 `TestCallbackResume`（Test 5 写入 test_engine_deadlock.py 的 TestDeadlockIntegration 类）。Test 3 按 TDD 红测先行：先写测试确认现状失败（断言现状不推进），再实现修复转绿。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/workflows/test_engine_waiting.py tests/workflows/test_engine_deadlock.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `rg "aupdate\(status" server/workflows/engine/scheduler.py` 命中执行级抢锁语句
    - `rg "_check_dependencies_ready" server/workflows/engine/scheduler.py` 零命中或仅余委托殼（无 COMPLETED-only 判定体）
    - `rg "SKIPPED" server/workflows/engine/scheduler.py` 中 resume_execution 不再含 COMPLETED→SKIPPED 改写
    - Test 1-5 全绿；Test 3 提交历史含红→绿两步（或 SUMMARY 说明红测验证过程）
  </acceptance_criteria>
  <done>回调续跑与主循环字面同源（同一调度循环 + 同一收口），互斥防双循环，重建路径死锁有兜底</done>
</task>

<task type="auto">
  <name>Task 2: 容器回调修复 + coding_callback 手工调度器删除</name>
  <files>server/subagent/api/callbacks.py, server/feishu/callbacks/coding_callback.py</files>
  <read_first>
    - server/subagent/api/callbacks.py:177-260（_schedule_workflow_resume 现状：只写标记 + _continue_after_node）
    - server/feishu/callbacks/coding_callback.py:203-325（_schedule_branch_confirmation 手工迷你调度器：手动重置 RUNNING、手工 ExecutionContext（previous_outputs={} 且无 trigger_data）、手工复刻 _next_handle 写入——全部待删）
    - server/tasks/agent_tasks.py:212-272（可工作的标准范式：标记终态 → _continue_after_node，失败走 _handle_node_failure；延迟导入 WorkflowEngine 模式）
    - .planning/phases/18-engine/18-PATTERNS.md callbacks 小节（延迟导入与 Don't Hand-Roll 表）
  </read_first>
  <action>
    1. `callbacks.py _schedule_workflow_resume`：保留写入 `_resume_from_callback` / `_session_results` 标记的现状逻辑，其后调用的 `_continue_after_node` 已具备标记重跑能力（Task 1 步骤 1）——核对调用处传参与异常处理与 agent_tasks.py 范式对齐（延迟导入 `from workflows.engine.scheduler import WorkflowEngine` 保持函数体内）；若现状在标记写入外还有状态翻转缺失（SUSPENDED→RUNNING 由 Task 1 抢锁统一负责），删除本模块内的临时翻转代码。
    2. `coding_callback.py _schedule_branch_confirmation`：删除手工重置 NE、手工构建 ExecutionContext、手工重跑 execute、手工复刻 `_next_handle` 写入的整段迷你调度器；改为写入分支选择结果（含 `_next_handle` 与必要的恢复标记到 NE.output_data）后调统一入口 `engine._continue_after_node(execution, node_execution)`——节点重跑（如需）与路由全部由统一入口完成。保留该回调的飞书交互/卡片部分不动；SUSPENDED→RUNNING 翻转代码（:251-253）删除（抢锁统一负责）。
    3. 两模块的失败分支统一走 `_handle_node_failure`（agent_tasks 范式）；不得新增任何手工 ExecutionContext 构建（Don't Hand-Roll 表）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/workflows/ tests/subagent/ tests/feishu/ -x --co -q 2>&1 | tail -3 && cd server && uv run pytest tests/workflows/ -x</automated>
  </verify>
  <acceptance_criteria>
    - `rg "ExecutionContext\(" server/feishu/callbacks/coding_callback.py` 零命中（手工构建删除）
    - `rg "_next_handle" server/feishu/callbacks/coding_callback.py` 不再有"复刻写入逻辑"（仅允许把分支选择写入 output_data 的数据准备）
    - `rg "from workflows.engine.scheduler import" server/subagent/api/callbacks.py server/feishu/callbacks/coding_callback.py` 均为函数体内延迟导入
    - `cd server && uv run pytest tests/ -x` 相关目录（workflows/subagent/feishu）全绿
  </acceptance_criteria>
  <done>三套回调实现收敛为一套统一续跑入口，第三套迷你调度器（漂移源）根除</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 外部回调（runner HTTP / 飞书事件）→ 续跑入口 | 既有鉴权（runner token / 飞书验签）不动，本计划不新增任何 API 面 |
| 并发回调 → 同一执行 | 多回调并发竞争执行推进权 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-05 | Tampering/DoS | 并发续跑双循环（重复执行节点/重复发卡/重复 dispatch 容器） | mitigate | 执行级原子抢锁 `filter(status=SUSPENDED).aupdate(status=RUNNING)`，Task 1 Test 4 断言第二次放弃；抢锁失败 structlog 可审计 |
| T-18-06 | Spoofing | 伪造回调推进他人执行 | accept | 既有回调鉴权（runner token / feishu 验签 / IsAuthenticated+ApprovalPermission）覆盖，本计划所有 _continue_after_node 调用方均在鉴权后；不新增入口 |
| T-18-SC | Tampering | 包安装 | accept | 零新依赖 |
</threat_model>

<verification>
- `cd server && uv run pytest tests/workflows/test_engine_waiting.py tests/workflows/test_engine_deadlock.py -x` 全绿
- `cd server && uv run pytest tests/ -x` 全量（workflows + subagent + feishu 受影响目录必须绿）
- `cd server && uv run ruff format --check workflows/engine/scheduler.py subagent/api/callbacks.py feishu/callbacks/coding_callback.py && uv run ruff check workflows/ subagent/ feishu/`
</verification>

<success_criteria>
- 双路径一致性测试通过：同一工作流主循环 vs 挂起-续跑最终 NE 状态集合一致
- 容器回调断裂（A1）修复：标记节点被重跑且执行推进（红→绿）
- 并发续跑互斥生效，重建路径死锁兜底转 FAILED
- coding_callback 迷你调度器与 _check_dependencies_ready 的 COMPLETED-only 判定不复存在
</success_criteria>

<output>
完成后创建 `.planning/phases/18-engine/18-04-SUMMARY.md`，记录：统一续跑入口最终形态（_run_scheduling_loop 抽取与否）、抢锁条件最终定稿、tolerated fallback 持久化键名、A1 红测验证过程、_check_execution_complete 外部调用点处理方式（18-05 与 Phase 21 消费）。
</output>
