---
phase: 18-engine
plan: 02
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - server/tests/workflows/conftest.py
  - server/workflows/engine/scheduler.py
  - server/tests/workflows/test_engine_routing.py
  - server/tests/workflows/test_engine_inputs.py
autonomous: true
requirements: [ENG-02, ENG-05]
must_haves:
  truths:
    - "条件分支工作流 run_sync 执行后仅选中支的节点 COMPLETED，未选中支节点 NE 状态为 SKIPPED（CONTEXT 锁定：未选中分支级联 skipped）"
    - "菱形汇合一支 skip 一支活时汇合节点照常执行（一条活路即执行）"
    - "skipped 参与完成判定：含 skipped 分支的执行最终 COMPLETED，不残留 PENDING、不永远 running"
    - "边 target_handle 非 default 时下游节点 inputs 中出现该端口键（值为上游完整输出），且上游同名键不被双重嵌套"
    - "主循环就绪判定/级联/后继选择/输入收集全部经 routing 纯函数，scheduler 内不再有第二套 if-else 路由实现"
  artifacts:
    - path: "server/tests/workflows/conftest.py"
      provides: "引擎测试共享基建：可控测试节点 + 工作流工厂夹具 + engine fixture"
      contains: "branch_workflow"
    - path: "server/workflows/engine/scheduler.py"
      provides: "主循环就绪/级联/输入委托 routing 纯函数"
      contains: "evaluate_node_readiness"
    - path: "server/tests/workflows/test_engine_routing.py"
      provides: "分支路由/级联 skipped 集成测试（django_db + run_sync）"
    - path: "server/tests/workflows/test_engine_inputs.py"
      provides: "target_handle 归集端到端集成测试"
  key_links:
    - from: "server/workflows/engine/scheduler.py"
      to: "server/workflows/engine/routing.py"
      via: "主循环 import 并调用 evaluate_node_readiness/compute_skippable/select_successors/collect_inputs"
      pattern: "from workflows.engine.routing import|from workflows.engine import routing"
    - from: "server/tests/workflows/test_engine_routing.py"
      to: "server/tests/workflows/conftest.py"
      via: "branch_workflow / engine_test_nodes 夹具"
      pattern: "branch_workflow"
---

<objective>
把 18-01 产出的 routing 纯函数接入调度主循环：就绪判定换成边感知的 `evaluate_node_readiness`、每轮用 `compute_skippable` 级联标记 skipped、后继选择经 `select_successors`、`_collect_inputs` 委托 `collect_inputs`。同时建立本阶段全部集成测试共用的 conftest 基建（可控测试节点 + 工作流工厂）。

Purpose: 落实 CONTEXT 锁定决策"分支路由一致性（ENG-02）"的主循环侧——条件分支不再两边都跑，未选中支级联 skipped 且参与完成判定；落实"target_handle 语义（ENG-05）"的运行时接线。回调续跑侧（18-04）将复用同一套纯函数。
Output: scheduler 主循环路由改造 + conftest 工厂/测试节点 + test_engine_routing.py、test_engine_inputs.py 的集成测试类。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-engine/18-CONTEXT.md
@.planning/phases/18-engine/18-RESEARCH.md
@.planning/phases/18-engine/18-PATTERNS.md
@.planning/phases/18-engine/18-01-SUMMARY.md
@server/workflows/engine/routing.py
@server/workflows/engine/scheduler.py
</context>

## Artifacts this phase produces（本计划部分）

- 修改 `server/tests/workflows/conftest.py`：
  - 可控测试节点：`BranchNode`（test_branch，按类属性 `_next_handle` 返回 next_handle）、`WaitEventNode`（test_wait_event，返回 waiting_event）、`WaitApprovalNode`（test_wait_approval，返回 waiting_approval，带类计数器 `_exec_count`）、`EchoInputsNode`（test_echo_inputs，输出 `context.input_data`）、`EchoTriggerDataNode`（test_echo_trigger，输出 `context.trigger_data`）
  - 夹具：`engine`（WorkflowEngine 实例）、`engine_test_nodes`（注册/注销上述节点）、`branch_workflow`（trigger→branch→true/false 两支→菱形汇合）、`waiting_workflow`（trigger→WaitEventNode→下游）、`waiting_terminal_workflow`（trigger→WaitEventNode 末端）
- 修改 `server/workflows/engine/scheduler.py`：主循环（`_run_execution`）就绪判定/级联/后继选择改走 routing；`_collect_inputs` 委托 `routing.collect_inputs`；新增 `node_statuses: dict[str, str]` 与 `node_handles: dict[str, str]` 状态映射
- `server/tests/workflows/test_engine_routing.py` 追加集成测试类（django_db + run_sync）
- `server/tests/workflows/test_engine_inputs.py` 追加 target_handle 端到端集成测试类

<tasks>

<task type="auto">
  <name>Task 1: conftest 测试基建——可控测试节点 + 工作流工厂 + engine 夹具</name>
  <files>server/tests/workflows/conftest.py</files>
  <read_first>
    - server/tests/workflows/conftest.py（现有内容，obs_* 夹具——新工厂并列追加，不动既有）
    - server/tests/workflows/test_error_handling.py:33-137（可控测试节点定义 + autouse 注册/注销夹具 + engine fixture 范式——逐项照抄）
    - server/tests/workflows/test_engine.py:37-112（workflow 工厂三件套范式：Workflow + WorkflowNode + WorkflowEdge）
    - server/tests/workflows/test_dag.py:104-120（条件分支边写法：source_handle="true"/"false"）
    - .planning/phases/18-engine/18-PATTERNS.md 的 conftest 小节（工厂清单与模式 A/B/C 原文）
  </read_first>
  <action>
    在 `server/tests/workflows/conftest.py` 追加（不动既有 obs_* 夹具）：

    五个可控测试节点（BaseNode 子类，照抄 test_error_handling.py:33-47 的 AlwaysFailNode 结构，含 node_type/display_name/description/category/execution_mode 类属性）：
    - `BranchNode`（node_type="test_branch"）：execute 返回 `NodeResult(status="completed", output={"branch": cls._next_handle}, next_handle=cls._next_handle)`，类属性 `_next_handle = "true"` 作可控旋钮；
    - `WaitEventNode`（node_type="test_wait_event"）：返回 `NodeResult(status="waiting_event", output={})`，类属性 `_exec_count` 计数；
    - `WaitApprovalNode`（node_type="test_wait_approval"）：返回 `NodeResult(status="waiting_approval", output={})`，类属性 `_exec_count` 计数（18-03 热循环断言用）；
    - `EchoInputsNode`（node_type="test_echo_inputs"）：返回 `NodeResult(status="completed", output={"echoed_inputs": dict(context.input_data)})`；
    - `EchoTriggerDataNode`（node_type="test_echo_trigger"）：返回 `NodeResult(status="completed", output={"echoed_trigger": dict(context.trigger_data)})`（18-05 消费）。

    夹具（均为具名 fixture，不用 autouse——conftest 级 autouse 会影响全目录测试）：
    - `engine`：返回 `WorkflowEngine()`（照抄 test_error_handling.py:134-137）；
    - `engine_test_nodes`：注册五个节点到 NodeRegistry，yield 后逐个 `NodeRegistry._nodes.pop(..., None)` 注销（照抄模式 B），同时重置各类属性旋钮/计数器；
    - `branch_workflow(db, ...)`：manual_trigger → BranchNode → true 支节点 B（test_echo_inputs）/ false 支节点 C（test_echo_inputs）→ 菱形汇合节点 D（test_echo_inputs），边 source_handle 分别为 "true"/"false"，B→D、C→D 均 default；
    - `waiting_workflow(db, ...)`：manual_trigger → WaitEventNode → 下游 EchoInputsNode；
    - `waiting_terminal_workflow(db, ...)`：manual_trigger → WaitEventNode（末端无下游）。

    project 依赖照抄 test_engine.py 的 engine_project 写法（若该 fixture 在 test_engine.py 局部，conftest 内自建等价 project fixture，命名 `engine_project` 避免冲突时改名）。全部节点 name 唯一且语义化（"Branch"/"TrueSide"/"FalseSide"/"Join"/"Waiter"/"Downstream"），便于 NE 按 node__name 查询断言。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/workflows/ --collect-only -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `rg "test_branch|test_wait_event|test_wait_approval|test_echo_inputs|test_echo_trigger" server/tests/workflows/conftest.py` 五个 node_type 全部命中
    - `rg "def branch_workflow|def waiting_workflow|def waiting_terminal_workflow|def engine_test_nodes" server/tests/workflows/conftest.py` 四个夹具齐备
    - conftest 不含 `autouse=True` 的新增节点注册夹具（既有 obs_* 若有 autouse 不动）
    - `cd server && uv run pytest tests/workflows/ --collect-only -q` 收集无错误；既有用例数不减少
  </acceptance_criteria>
  <done>本阶段全部集成测试（18-02/03/04/05）可直接消费的共享测试基建就位</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 主循环就绪/级联/后继/输入接入 routing 纯函数</name>
  <files>server/workflows/engine/scheduler.py</files>
  <read_first>
    - server/workflows/engine/routing.py（18-01 产物全文：RoutingState、evaluate_node_readiness、select_successors、compute_skippable、collect_inputs 最终签名——以 18-01-SUMMARY.md 记录的签名为准）
    - server/workflows/engine/scheduler.py:505-859（主循环全段：就绪判定 :565-586、ready 批量 gather :774-785、结果处理 :793-825、内存 node_outputs 双键写入 :798-802）
    - server/workflows/engine/scheduler.py:950-970（result["handle"] 内存来源——node_handles 的填充点）与 :1093-1107（_collect_inputs 现状扁平合并）
    - .planning/phases/18-engine/18-RESEARCH.md Pattern 3（边感知判定语义）与 Pitfall 2（handle 双来源）、Pitfall 9（condition fan-out 存量检查）
  </read_first>
  <behavior>
    - Test 1（集成，test_engine_routing.py 新增 TestBranchRoutingIntegration 类，django_db(transaction=True) + asyncio + run_sync）: branch_workflow、BranchNode._next_handle="true" → 执行 COMPLETED；TrueSide NE 为 COMPLETED，FalseSide NE 为 SKIPPED，Join NE 为 COMPLETED（菱形一活一死照常执行）
    - Test 2: _next_handle="false" → FalseSide COMPLETED、TrueSide SKIPPED、Join COMPLETED（对称）
    - Test 3（skipped 参与完成判定）: 上述执行最终 execution.status == COMPLETED，全部 NE 处于终态（无 PENDING 残留）
    - Test 4（default 回退兼容）: BranchNode._next_handle="nonexistent_handle"（无匹配边且无 default 边）→ 两支均 SKIPPED、Join SKIPPED、执行 COMPLETED（无死锁无 running 残留）——锁定 select_successors/evaluate 回退语义的端到端表现
  </behavior>
  <action>
    改造 `_run_execution` 主循环（scheduler.py:505-859），所有路由判定改走 routing 纯函数：

    1. 模块顶部 import：`from workflows.engine.routing import RoutingState, evaluate_node_readiness, compute_skippable, select_successors`（或经 `workflows.engine` barrel，与 18-01-SUMMARY 一致）。
    2. 主循环新增两个 dict 与既有集合并行维护：`node_statuses: dict[str, str]`（节点终态时写入 routing 模块常量：completed/skipped/failed/tolerated/waiting）与 `node_handles: dict[str, str]`（节点完成时从 `result["handle"]` 写入，缺省不写=default——封堵 Pitfall 2 双来源不对称：热路径只用内存来源）。既有 completed/skipped/failed 集合保留（最小 diff，完成收尾逻辑 18-03 再收口）。
    3. 就绪判定段（:565-586）替换：对每个 pending 节点构造 `RoutingState(statuses=node_statuses, handles=node_handles)` 调 `evaluate_node_readiness(dag, node_id, state)`——"ready" 进 ready_nodes；"skip_failed"/"skip_unselected" 调既有 `self._skip_node(execution, dag_node, 原因)`（skip_failed 原因沿用现状文案"前置节点失败"，skip_unselected 用"分支未选中"）并同步 skipped 集合 + node_statuses；"blocked" 留 pending。每轮判定前先调 `compute_skippable(dag, state, pending)` 把可级联 skip 的节点批量标记（_skip_node 含 node_skipped hook，逐个调用）。
    4. tolerated 处理（:804-811）：tolerated 节点除入 completed 集合外，node_statuses 写 STATUS_TOLERATED（routing 的 tolerated 语义=已解析且按 default 选中）。
    5. `_collect_inputs`（:1093-1107）：方法保留签名，函数体改为委托 `routing.collect_inputs(dag, str(dag_node.id), node_outputs)`——调用点零改动；删除原扁平合并循环。
    6. Pitfall 9 存量检查：`rg -l "condition" server/workflows/templates/` 检查内置模板中 condition 节点是否被当 fan-out 使用（两分支都期望执行）；发现即在 SUMMARY 列出迁移说明，未发现也记录结论。

    禁改项：waiting_event/waiting_approval 的处理分支（:819-825）本任务不动（归 18-03）；5s 轮询分支不动（归 18-03）；`_continue_after_node` 不动（归 18-04）。集成测试写入 test_engine_routing.py 新类 `TestBranchRoutingIntegration`（消费 Task 1 夹具，类装饰器 `@pytest.mark.asyncio` + `@pytest.mark.django_db(transaction=True)`）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/workflows/test_engine_routing.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `rg "evaluate_node_readiness|compute_skippable" server/workflows/engine/scheduler.py` 至少各 1 处命中（主循环消费）
    - 主循环就绪判定段不再含 `all_deps_completed` 手写 ANY/ALL 判定（`rg "all_deps_completed" server/workflows/engine/scheduler.py` 零命中）
    - `_collect_inputs` 函数体内无扁平合并循环（`rg "inputs.update" server/workflows/engine/scheduler.py` 在该函数体内零命中），委托 routing.collect_inputs
    - Test 1-4 全绿；`cd server && uv run pytest tests/workflows/ -x` 全量零回归（既有 368+ 例；test_engine.py 中依赖"分支两边都跑"旧语义的用例若有失败，属预期语义变更，按新语义改写断言并在 SUMMARY 记录）
  </acceptance_criteria>
  <done>主循环按 next_handle×source_handle 真路由，未选中支级联 skipped 且执行正常完成，输入收集走 target_handle 归集</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: target_handle 端到端集成测试（ENG-05 运行时落点）</name>
  <files>server/tests/workflows/test_engine_inputs.py</files>
  <read_first>
    - server/tests/workflows/test_engine_inputs.py（18-01 已写的 TestCollectInputsUnit 类——集成类并列追加，勿动 unit 部分）
    - server/tests/workflows/conftest.py（Task 1 产出的 EchoInputsNode 与工厂范式）
    - server/tests/workflows/test_dag.py:104-120（WorkflowEdge target_handle 写法）
  </read_first>
  <behavior>
    - Test 1（端口键归集端到端）: 手建工作流 trigger → A（test_branch，输出含 "branch" 键）→ B（test_echo_inputs），边 target_handle="custom_port" → run_sync 后 B 的 echoed_inputs 含 "custom_port" 键且值为 A 的完整输出 dict（含 "branch" 键）
    - Test 2（扁平保底并存）: 同一执行中 B 的 echoed_inputs 同时含扁平键 "branch"（现状语义保底，与端口键并存）
    - Test 3（default target_handle 零变化）: 边 target_handle 留空/"default" → B 的 echoed_inputs 仅扁平键，无 "default" 端口键
  </behavior>
  <action>
    在 `server/tests/workflows/test_engine_inputs.py` 追加 `TestTargetHandleIntegration` 类（`@pytest.mark.asyncio` + `@pytest.mark.django_db(transaction=True)`，消费 engine/engine_test_nodes 夹具）。工作流在测试内手建（不进 conftest——target_handle 定制边仅此处用），照抄 test_engine.py 工厂三件套写法，WorkflowEdge 显式传 `target_handle="custom_port"`。断言路径：`NodeExecution.objects.get(node__name="...").output_data["echoed_inputs"]`。每个用例断言 execution.status == COMPLETED 兜底（防 silent hang 被误判通过）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/workflows/test_engine_inputs.py -x</automated>
  </verify>
  <acceptance_criteria>
    - Test 1 断言 `echoed_inputs["custom_port"] == A 完整输出 dict`（整包语义，非单字段）
    - 18-01 的 TestCollectInputsUnit 类零改动（`git diff` 仅显示追加类）
    - `cd server && uv run pytest tests/workflows/test_engine_inputs.py -x` 全绿
  </acceptance_criteria>
  <done>target_handle 语义经真实调度端到端验证，ENG-05 的"端口名存实亡"在运行时闭环</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 上游节点输出 → 下游节点输入（collect_inputs） | 引擎内部数据流，无新外部输入面 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-02 | DoS | compute_skippable fixpoint 级联 | mitigate | 18-01 纯函数实现为有界迭代（pending 集合单调缩小），集成 Test 4（无匹配 handle）证明不产生无限循环/无限 running |
| T-18-SC | Tampering | 包安装 | accept | 零新依赖（同 18-01 审计结论） |
</threat_model>

<verification>
- `cd server && uv run pytest tests/workflows/test_engine_routing.py tests/workflows/test_engine_inputs.py -x` 全绿
- `cd server && uv run pytest tests/workflows/ -x` 全量零回归（旧语义依赖用例的改写在 SUMMARY 记录）
- `cd server && uv run ruff format --check workflows/engine/scheduler.py tests/workflows/conftest.py && uv run ruff check workflows/ tests/workflows/`
</verification>

<success_criteria>
- 条件分支主循环真路由：仅选中支执行、未选中支级联 SKIPPED、skipped 参与完成判定（执行 COMPLETED 不残留 pending）
- 主循环内无第二套路由 if-else，全部判定经 routing 纯函数
- target_handle 归集经端到端集成测试锁定（端口键 + 扁平保底并存）
- conftest 基建可被 18-03/04/05 直接消费
</success_criteria>

<output>
完成后创建 `.planning/phases/18-engine/18-02-SUMMARY.md`，记录：node_statuses/node_handles 的维护点（18-03/04 消费）、conftest 夹具与测试节点清单（18-03/05 消费）、Pitfall 9 内置模板 condition 用法检查结论、旧语义测试改写清单。
</output>
