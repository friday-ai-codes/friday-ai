---
phase: 10-mcpb
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - server/tools/models.py
  - server/tools/migrations/0003_tooltokenbinding.py
autonomous: true
requirements: [MCPB-01, MCPB-03]
must_haves:
  truths:
    - "ToolTokenBinding 模型存在：user FK / access_token FK / remote_tool FK / created_at / updated_at，三 FK 均 on_delete=CASCADE"
    - "ToolTokenBinding 有 unique 约束 (user, remote_tool)：每用户对同一工具最多一条绑定"
    - "迁移 0003_tooltokenbinding 仅 CreateModel（无数据回填），可正向 migrate 与反向回滚"
    - "删除 AccessToken 或 RemoteTool 或 User 时，相关 ToolTokenBinding 自动级联消失"
  artifacts:
    - path: "server/tools/models.py"
      provides: "ToolTokenBinding 模型（user↔token↔tool 持久绑定，Phase 11 注入依据）"
      contains: "class ToolTokenBinding"
    - path: "server/tools/migrations/0003_tooltokenbinding.py"
      provides: "CreateModel 迁移"
      contains: "ToolTokenBinding"
  key_links:
    - from: "server/tools/models.py ToolTokenBinding.access_token"
      to: "access_tokens.AccessToken"
      via: "ForeignKey on_delete=CASCADE"
      pattern: "access_tokens.AccessToken"
    - from: "server/tools/models.py ToolTokenBinding.remote_tool"
      to: "tools.RemoteTool"
      via: "ForeignKey on_delete=CASCADE"
      pattern: "on_delete=models.CASCADE"
---

<objective>
建立绑定关系的数据地基：在 tools app 新增 `ToolTokenBinding` 模型（三 FK + CASCADE + unique(user, remote_tool)）并生成 CreateModel 迁移。该表是 MCPB-01「绑定入库」与 MCPB-03「查看/解除绑定」的存储层，也是 Phase 11「容器据 tool→token 映射选令牌注入」的持久依据。

Purpose: 满足 MCPB-01（绑定入库 + 每用户每工具唯一）、MCPB-03（owner 可管理的绑定行）的 Storage 层职责（per RESEARCH §Architectural Responsibility Map）。
Output: 1 处 models.py 增量 + 1 个迁移文件，无数据回填。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-mcpb/10-RESEARCH.md
@server/tools/models.py
@server/access_tokens/models.py
@server/tools/migrations/0001_initial.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: ToolTokenBinding 模型（tools/models.py 追加）</name>
  <files>server/tools/models.py</files>
  <read_first>
    - server/tools/models.py:1-35（RemoteTool 既有定义；新模型追加在其后，复用同文件 import）
    - server/access_tokens/models.py:29-66（AccessToken：id 为 UUIDField 主键、created_by FK；绑定的 access_token FK 目标 + related_name 不可与既有冲突）
    - .planning/phases/10-mcpb/10-RESEARCH.md §Code Examples「模型」（字段/CASCADE/unique_together/db_table/ordering 精确形态）
  </read_first>
  <action>
    在 server/tools/models.py 末尾新增 `class ToolTokenBinding(models.Model)`（中文 docstring 说明「用户令牌↔skill/mcp 持久绑定，Phase 11 容器注入依据」，per MCPB-01）：
    - `user = models.ForeignKey("accounts.User", on_delete=models.CASCADE, related_name="tool_token_bindings")`
    - `access_token = models.ForeignKey("access_tokens.AccessToken", on_delete=models.CASCADE, related_name="tool_bindings")`
    - `remote_tool = models.ForeignKey(RemoteTool, on_delete=models.CASCADE, related_name="token_bindings")`
    - `created_at = models.DateTimeField(auto_now_add=True)` / `updated_at = models.DateTimeField(auto_now=True)`
    - `class Meta`: `db_table = "tool_token_bindings"`、`unique_together = (("user", "remote_tool"),)`（每用户对同一工具最多一条，per MCPB-01「重复绑定即更新」）、`ordering = ["-created_at"]`。

    三 FK 全部 `on_delete=models.CASCADE`（令牌/工具/用户删除时绑定自动消失，per RESEARCH locked decision）。不新增任何会暴露明文的字段（绑定只引用 access_token，明文从不入此表）。
  </action>
  <verify>
    <automated>cd server && uv run python -c "import django,os;os.environ.setdefault('DJANGO_SETTINGS_MODULE','friday.settings');django.setup();from tools.models import ToolTokenBinding;f={x.name for x in ToolTokenBinding._meta.get_fields()};assert {'user','access_token','remote_tool','created_at','updated_at'} <= f;assert ToolTokenBinding._meta.unique_together==(('user','remote_tool'),);print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `ToolTokenBinding has unique_together (user, remote_tool)`（断言 `_meta.unique_together == (("user","remote_tool"),)`）。
    - 三 FK 字段 user/access_token/remote_tool 存在且 `on_delete=CASCADE`（field.remote_field.on_delete is models.CASCADE）。
    - created_at/updated_at 字段存在（auto_now_add / auto_now）。
    - models.py 中无任何明文/hash 字段（绑定仅引用 access_token FK）。
  </acceptance_criteria>
  <done>ToolTokenBinding 模型落地，三 FK CASCADE + unique(user, remote_tool) 满足。</done>
</task>

<task type="auto">
  <name>Task 2: 迁移 0003_tooltokenbinding（CreateModel）</name>
  <files>server/tools/migrations/0003_tooltokenbinding.py</files>
  <read_first>
    - server/tools/migrations/0001_initial.py（迁移依赖声明与 CreateModel 形态范式）
    - server/tools/migrations/0002_seed_builtin_tools.py:41-48（dependencies 链：本迁移须 depends_on ("tools","0002_seed_builtin_tools") + access_tokens / accounts 最新迁移以满足 FK）
    - .planning/phases/10-mcpb/10-RESEARCH.md §Runtime State Inventory（结论：仅一个 CreateModel migration，无 RunPython 回填）
  </read_first>
  <action>
    用 `cd server && uv run python manage.py makemigrations tools` 生成迁移，确认产物为 `tools/migrations/0003_tooltokenbinding.py` 且仅含 `CreateModel`（含 unique_together / db_table / FK 约束），无 RunPython。dependencies 应自动包含 tools 上一迁移 + access_tokens / accounts 的 FK 依赖迁移；若自动命名非 `0003_tooltokenbinding`，用 `--name tooltokenbinding` 规整。**严禁**手写数据回填（per RESEARCH：无历史数据）。

    生成后跑 `uv run python manage.py migrate tools` 验证正向可迁移，再 `uv run python manage.py makemigrations --check --dry-run` 确认无遗漏（模型与迁移一致）。
  </action>
  <verify>
    <automated>cd server && uv run python manage.py makemigrations --check --dry-run 2>&1 | tail -5 && uv run python manage.py migrate tools 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `tools/migrations/0003_tooltokenbinding.py` 存在且操作仅 `CreateModel`（无 RunPython 数据回填）。
    - `uv run python manage.py makemigrations --check --dry-run` 报告无未生成迁移（模型/迁移一致）。
    - `uv run python manage.py migrate tools` 正向成功；迁移 `migrate tools 0002` 可反向回滚（CreateModel 默认可逆）。
  </acceptance_criteria>
  <done>0003 迁移落地，正反向可用，模型与迁移一致无漂移。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB schema ↔ 应用层 | 绑定表是 owner 隔离与执行选令牌的存储事实源；unique 约束防重复绑定状态漂移 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04 | Tampering | 同一 (user, tool) 多条绑定造成令牌选择歧义 | mitigate | DB 层 unique_together(user, remote_tool)，重复绑定靠 10-03 upsert 收敛 |
| T-10-05 | Information Disclosure | 绑定表落明文/hash | mitigate | 模型只存 access_token FK，绝不复制 token_hash/明文字段 |
| T-10-06 | Tampering | 令牌/工具删除后绑定悬挂指向失效资源 | mitigate | 三 FK on_delete=CASCADE，删除即级联清理 |
| T-10-SC | Tampering | 供应链（包安装） | accept | 本阶段零新增依赖，无 install 任务 |
</threat_model>

<verification>
- `cd server && uv run python manage.py makemigrations --check --dry-run` → 无未生成迁移。
- `cd server && uv run python manage.py migrate tools` → 正向成功。
- 模型自检脚本断言 unique_together 与三 FK CASCADE（见 Task 1 verify）。
</verification>

<success_criteria>
- ToolTokenBinding 模型 + 0003 迁移落地，满足 MCPB-01 唯一性与 MCPB-03 owner 管理的存储地基。
- 三 FK CASCADE 行为成立；表中无任何明文/hash 字段。
- 迁移仅 CreateModel、正反向可用、无 schema 漂移。
</success_criteria>

<output>
Create `.planning/phases/10-mcpb/10-02-SUMMARY.md` when done
</output>
