---
phase: 133-commit-v0-22-baseline
plan: 04
type: execute
wave: 3
depends_on:
  - "133-01"
  - "133-02"
  - "133-03"
files_modified:
  - server/codegraph/management/commands/evaluate_graph_bench.py
  - server/tests/codegraph/test_evaluate_graph_bench_command.py
  - server/tests/codegraph/test_graph_bench_integration.py
autonomous: true
requirements:
  - BENCH-01
  - BENCH-03
must_haves:
  truths:
    - "评测者可用 --repo --branch --commit-sha --split 运行 benchmark；run 前按 (repository_id, branch) 解析三方水位，不一致/为空即标 INVALID、写 manifest 含 invalid_reason、非零退出、不跑任何 case、绝不调用 get_graph（BENCH-01）"
    - "run manifest 写入 run identity 五元组 + index_key_source + 三方水位输入与校验结果 + run_id + 可复现命令行，供 Phase 140 同条件对比复用（BENCH-01）"
    - "OK 路径逐 case 调用未修改的 v0.22 能力（get_graph/resolve/impact/trace/检索 lane），冷（invalidate 后）热（缓存命中）各计时，产出逐 case + 逐桶无阈值 baseline JSON（BENCH-03）"
    - "评测全程只读：不写生产索引、不触发回填/重建；holdout 切分在 baseline 阶段被拒绝读取（BENCH-03）"
    - "观测埋点：caller 生命周期事件（含 duration_ms/run_id/触发用户 system）+ 逐 case sampling 事件，不记 query 正文，异常脱敏，best-effort 不反噬评测"
  artifacts:
    - path: server/codegraph/management/commands/evaluate_graph_bench.py
      provides: "薄 command —— 加载 gold、水位闸、真跑被测能力、冷/热计时、写 manifest 与无阈值 baseline JSON（唯一 I/O 层）"
      contains: "class Command"
    - path: server/tests/codegraph/test_evaluate_graph_bench_command.py
      provides: "默认套件：fixture 权威校验 + 水位 INVALID 短路 + schema 错误 fail-closed"
    - path: server/tests/codegraph/test_graph_bench_integration.py
      provides: "integration 标记（默认排除）：真仓 OK 路径端到端"
  key_links:
    - from: server/codegraph/management/commands/evaluate_graph_bench.py
      to: server/codegraph/services/graph_bench_eval.py
      via: "import validate_gold_dataset/build_run_identity/validate_watermark/evaluate_case/aggregate_report/build_report"
      pattern: "from codegraph.services.graph_bench_eval import"
    - from: server/codegraph/management/commands/evaluate_graph_bench.py
      to: server/services/code_graph/cache.py
      via: "get_graph_service().get_graph(...) + invalidate_repository(...)（冷/热控制）；复用内置 ensure_repository_readable fail-closed，不绕过"
      pattern: "get_graph_service|invalidate_repository"
    - from: server/codegraph/management/commands/evaluate_graph_bench.py
      to: server/tests/fixtures/graph_bench/
      via: "json.load gold manifest + split 文件 → validate_gold_dataset（fail-closed）"
      pattern: "graph_bench"
---

<objective>
交付薄 management command `evaluate_graph_bench`：把 Plan 01/03 的纯函数与 Plan 02 的冻结数据集接成可运行的只读评测——水位闸（fail-closed INVALID 短路）→ 逐 case 调未修改 v0.22 能力（冷/热计时）→ 无阈值 baseline JSON + run manifest，并按项目 observability 强约束埋点。

Purpose: BENCH-01 的「水位不一致即 INVALID 且不出结论」与 BENCH-03 的「未修改 v0.22 能力在冻结数据集上产原始 baseline」都只在 command 层真正落地。复刻 recall eval「纯模块 + 薄 command」分工，command 只做 I/O、计时、落盘，不复制任何被测管线逻辑。
Output: command + 默认套件测试（INVALID/fixture 校验）+ integration 测试 scaffold。
</objective>

<execution_context>
@/Users/zaneliu/Projects/open-source/friday-ai/.claude/gsd-core/workflows/execute-plan.md
@/Users/zaneliu/Projects/open-source/friday-ai/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/133-commit-v0-22-baseline/133-CONTEXT.md
@.planning/phases/133-commit-v0-22-baseline/133-RESEARCH.md
@server/codegraph/management/commands/evaluate_repo_route_recall.py
@server/codegraph/services/graph_bench_eval.py
@.planning/observability/LOGGING-SPEC.md
@.planning/phases/133-commit-v0-22-baseline/133-01-SUMMARY.md
@.planning/phases/133-commit-v0-22-baseline/133-03-SUMMARY.md

<interfaces>
<!-- 已核对于本仓源码的被测能力入口（直接调用，⛔ 不复制其管线逻辑、不修改其行为）： -->
- `services/code_graph/cache.py`：`get_graph_service() -> GraphService`（单例工厂）；`invalidate_repository(repository_id)`（冷路径驱逐）；`GraphService.get_graph(repository_id, branch="", *, user=None, include_low_confidence=False, seed_symbol_ids=None, depth=None) -> CodeGraph`（async，内置 `access.ensure_repository_readable` fail-closed）
- `services/code_graph/model.py`：`CodeGraph.graph: nx.MultiDiGraph`、`CodeGraph.meta: GraphMeta`
- `services/code_graph/symbol_resolve.py`：`resolve_symbol_in_graph(graph, *, symbol_id=None, name=None, file_path=None, symbol_type=None) -> SymbolResolution`（.resolved/.candidates/.total_candidates）
- `services/code_graph/impact.py`：`analyze_impact(graph, seed_symbol_id, *, max_depth=..., min_confidence=1.0, ...) -> dict`（`seed_in_graph=False` 区分「不存在」vs「无影响」）
- `services/code_graph/trace.py`：`trace_path(graph, source_symbol_id, target_symbol_id, *, min_confidence=1.0, alt_path_cap=...) -> dict`（`found`/`reason`∈{no_path,node_not_in_graph}/`path`）
- `services/retrieval/rag_search.py`：`search_rag(query, *, repo_ids: list[str], branch_name=None, top_k=30) -> LayerSnapshot`（items 含 payload/score/repository_id）
- `services/query_embedding.py`：`embed_query(text, *, drop_noise=True, ...) -> QueryVectors`（检索 lane embedding 链，token 计量走既有 ModelUsageRecord 口径）
- 水位来源（按 `(repository_id, branch)` 解析）：`repositories/models.py` `RepositoryBranchIndex.last_indexed_commit_sha`(L747)/`head_sha`(L745)/`is_base_branch`(L742)，仓级兜底 `Repository.last_indexed_commit_sha`(L233)；`codegraph/models.py` `ProcessTrace.built_at_sha`(L427)、`CallEdge`(caller/callee FK + callee_file/callee_qualifier，仅作预测侧)、`ProcessTrace.steps`(L415)/`process_key`
- 观测：`common/logging.py` `redact_secrets_in_text`(L391)、`interactions/redaction.py` `redact_for_ledger`(L48)、`interactions/models.py` `ModelUsageRecord`(L240)/`RetrievalTrace`(L185)
- 薄 command 范式：`evaluate_repo_route_recall.py`（argparse、asyncio.run 串行、structlog `_LOG_KV`{category:caller, component, initiated_by_user_id:system}、单 case 容错 try/except、观测 best-effort、输出/落盘、CommandError 非零退出）
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: command 骨架 + 参数 + run identity + 水位闸 INVALID 短路 + 默认套件测试</name>
  <files>server/codegraph/management/commands/evaluate_graph_bench.py, server/tests/codegraph/test_evaluate_graph_bench_command.py</files>
  <read_first>
    - server/codegraph/management/commands/evaluate_repo_route_recall.py  （薄 command 形态：add_arguments/handle/asyncio.run/_LOG_KV/CommandError/落盘）
    - server/codegraph/services/graph_bench_eval.py  （build_run_identity/validate_watermark/validate_gold_dataset 签名）
    - server/repositories/models.py  （RepositoryBranchIndex 水位字段 L740-760；Repository.last_indexed_commit_sha L233）
    - server/tests/codegraph/test_repo_route_recall_eval.py  （纯测试风格；本 task 测试需 ORM，参考 pytest-django 用法）
  </read_first>
  <action>
    新建 `server/codegraph/management/commands/evaluate_graph_bench.py`（`class Command(BaseCommand)`）。ORM 访问一律 `sync_to_async`（项目异步约束）。

    `add_arguments`：`--repo`(required, repository_id)、`--branch`(default "")、`--commit-sha`(required，源码 checkout sha)、`--split`(choices=["dev","locked_test","all"], default "all"；baseline 只用 dev+locked_test)、`--gold`(default `tests/fixtures/graph_bench`)、`--output-manifest`(default "")、`--output-json`(default "")、`--cold-only`(flag)、`--min-bucket-samples`(type=int, default 3)。若 `--split holdout` 传入 → `CommandError`（holdout 保留 Phase 140 最终验收，baseline 阶段不读）。

    `handle` 顺序（fail-closed）：
    1. `json.load` gold manifest + 选定 split 文件 → `validate_gold_dataset(manifest, cases)`；schema 错误 → `CommandError`（指明缺失/越界字段）。
    2. 按 `(repository_id, branch)` 解析三方水位（`sync_to_async` 读 ORM）：`index_built_at_sha` ← `RepositoryBranchIndex.last_indexed_commit_sha`（无该行则仓级 `Repository.last_indexed_commit_sha`）；`source_checkout_sha` ← `--commit-sha` 并与 `RepositoryBranchIndex.head_sha` 校验一致；`gold_annotated_at_sha` ← manifest；`process_built_at_sha` ← 该仓该分支任一 `ProcessTrace.built_at_sha`（无则 None）。
    3. `run_id = uuid4`；`build_run_identity(repository, branch, commit_sha, index_key=index_built_at_sha, gold_version, index_key_source="last_indexed_commit_sha")`；`validate_watermark(...)`。
    4. **INVALID 短路**：写 manifest（identity + 三方水位输入 + watermark="INVALID" + invalid_reason + run_id + split + 可复现命令行 + started_at），随后 `CommandError` 非零退出；⛔ 此路径绝不调用 `get_graph_service().get_graph` 或任何被测能力、绝不跑任何 case。
    5. OK 路径留给 Task 2（本 task 先让 handle 在水位 OK 时调用一个 `_run_all_cases` 协程序 stub，Task 2 填充）。

    写默认套件测试 `server/tests/codegraph/test_evaluate_graph_bench_command.py`：
    - fixture 权威校验：对 Plan 02 真实 fixtures（manifest/dev/locked_test.json）跑 `validate_gold_dataset`，断言通过（锁住 Plan 02 schema 不漂移）。
    - 水位 INVALID：测试库建 `Repository` + `RepositoryBranchIndex`（last_indexed_commit_sha 与 head_sha 与 gold annotated_at_sha 互不一致），mock `get_graph_service`，跑 command → 断言抛 `CommandError`、manifest 写出且 watermark=="INVALID"、`get_graph` mock 未被调用。
    - schema 错误：构造缺必填维度的 gold → `CommandError`。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/codegraph/test_evaluate_graph_bench_command.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd server && uv run pytest tests/codegraph/test_evaluate_graph_bench_command.py -x -q` 退出码 0
    - `cd server && uv run pytest tests/codegraph/test_evaluate_graph_bench_command.py --collect-only -q | grep -cE '::'` ≥ 3（fixture 校验/INVALID/schema 错误三类）
    - `cd server && grep -cE 'validate_watermark|build_run_identity|validate_gold_dataset' codegraph/management/commands/evaluate_graph_bench.py` ≥ 3
    - `cd server && grep -cE 'holdout' codegraph/management/commands/evaluate_graph_bench.py` ≥ 1（holdout 拒绝读取守卫存在）
    - `cd server && uv run ruff check codegraph/management/commands/evaluate_graph_bench.py` 退出码 0
  </acceptance_criteria>
  <done>command 可加载 gold（fail-closed）、按 (repo,branch) 解析水位、INVALID 时写 manifest 并非零退出且 get_graph 未被调用；holdout 被拒；fixture 权威校验/INVALID/schema 错误三类默认套件测试全绿</done>
</task>

<task type="auto">
  <name>Task 2: _run_case 能力编排（get_graph 冷/热 + resolve/impact/trace/检索 lane）+ integration 测试</name>
  <files>server/codegraph/management/commands/evaluate_graph_bench.py, server/tests/codegraph/test_graph_bench_integration.py</files>
  <read_first>
    - server/services/code_graph/cache.py  （get_graph 签名 L674、invalidate_repository L1132、get_graph_service L1111；CodeGraph.graph/.meta）
    - server/services/code_graph/impact.py  （analyze_impact 签名 L483，seed_in_graph 语义）
    - server/services/code_graph/trace.py  （trace_path 签名 L228，found/reason 三态）
    - server/services/code_graph/symbol_resolve.py  （resolve_symbol_in_graph 签名 L175）
    - server/services/retrieval/rag_search.py  （search_rag 签名 L87，LayerSnapshot items 形状）
    - server/codegraph/models.py  （CallEdge caller/callee L97-170、ProcessTrace.steps/process_key L390-440）
    - server/codegraph/services/graph_bench_eval.py  （evaluate_case 的 arrival set 形参）
  </read_first>
  <action>
    在 command 内实现 `_run_case`（async）与 `_run_all_cases`（async，串行——沿袭 recall command 串行理由：并发撞限流且延迟失真）。全程只读：只调下列现成入口 + ORM 读，⛔ 不调用任何索引写入/回填/重建路径，不复制被测管线逻辑。

    冷/热控制：每 case 先 `await sync_to_async(invalidate_repository)(repository_id)`，再 `get_graph_service().get_graph(repository_id, branch, user=None)` 计时得 cold_ms；紧接第二次 `get_graph`（缓存命中）计时得 warm_ms（`--cold-only` 时跳过热跑）。取 `graph.graph`（nx.MultiDiGraph）供纯函数能力用。

    每个 case 产出 arrival set 喂给 `evaluate_case`（Plan 03）：
    - predicted_symbol_uids：调检索 lane（`search_rag(query, repo_ids=[repository_id], branch_name=branch, top_k=...)`），按 LayerSnapshot items 的 payload 提取符号标识并归一化为可与 gold `expected_symbols[].uid` 比较的字符串，取前 5（此提取依检索结果真实形状实现，integration 才可达）。
    - candidate_processes：`sync_to_async` 读该仓该分支 `ProcessTrace`，每条产 `{process_key, step_symbol_uids}`（step_symbol_uids 从 `steps` 提取），供 `rank_process_candidates` 确定性排序（baseline 测量映射，非生产 Process 检索器）。
    - predicted_edges：对 edge_golds 涉及的 caller，`sync_to_async` 读 `CallEdge`（该仓该分支）产 `(caller_uid, callee_uid)` 预测边（CallEdge 仅作预测侧；gold 来自独立 callsite 标注）。
    - impact_result：对每个 impact_gold 的 seed_uid 调 `analyze_impact(graph.graph, seed_uid, ...)`，取其受影响 uid 列表与 `seed_in_graph`。
    - trace_results：对每个 trace_gold 调 `trace_path(graph.graph, source_uid, target_uid, ...)`，收集 found/reason/path。
    单 case 异常用 try/except 容错（沿袭 recall command），error 记 `f"{type(exc).__name__}: {redact_secrets_in_text(str(exc))[:200]}"`，不中断整轮。

    汇总：`evaluate_case(...)` → `CaseOutcome` 列表 → `aggregate_report` → `build_report(identity, watermark="OK", split, cases)`；manifest 与 baseline JSON 落盘（manifest 含 run_id + 可复现命令行 + duration_ms）。

    写 `server/tests/codegraph/test_graph_bench_integration.py`：整体 `@pytest.mark.integration`（默认套件排除），scaffold 真仓 OK 路径——断言产出 manifest + baseline 且含 per_case/per_bucket/overall 结构与 run_id；文件头注释写明运行方式（需已索引仓 + Qdrant，`uv run pytest -m integration`）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/codegraph/test_evaluate_graph_bench_command.py -x -q && uv run pytest tests/codegraph/test_graph_bench_integration.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd server && uv run pytest tests/codegraph/test_evaluate_graph_bench_command.py -x -q` 退出码 0（默认套件不回归）
    - `cd server && uv run pytest tests/codegraph/test_graph_bench_integration.py --collect-only -q | grep -cE '::'` ≥ 1 且 `grep -cE 'pytest.mark.integration' tests/codegraph/test_graph_bench_integration.py` ≥ 1（integration 标记，默认排除）
    - `cd server && grep -cE 'analyze_impact|trace_path|search_rag|invalidate_repository|get_graph' codegraph/management/commands/evaluate_graph_bench.py` ≥ 5（五类被测入口均被调用）
    - `cd server && grep -cE 'cold_ms|warm_ms' codegraph/management/commands/evaluate_graph_bench.py` ≥ 2（冷/热计时存在）
    - 只读：`cd server && grep -cE 'GraphWriter|trigger_reindex|backfill_symbol_resolution' codegraph/management/commands/evaluate_graph_bench.py` == 0（不触索引写入/回填）
    - `cd server && uv run ruff check codegraph/management/commands/evaluate_graph_bench.py && uv run mypy codegraph/management/commands/evaluate_graph_bench.py` 退出码 0
  </acceptance_criteria>
  <done>_run_case 串行编排 get_graph 冷/热 + resolve/impact/trace/检索 lane 产 arrival set 喂 evaluate_case；只读不触写入；单 case 容错；integration scaffold 带 integration 标记可 collect；默认套件不回归；lint/mypy 全绿</done>
</task>

<task type="auto">
  <name>Task 3: 观测埋点（caller 生命周期 + sampling + 脱敏 + token）+ 无阈值落盘 + CLI 输出</name>
  <files>server/codegraph/management/commands/evaluate_graph_bench.py</files>
  <read_first>
    - server/codegraph/management/commands/evaluate_repo_route_recall.py  （_LOG_KV、structlog info、best-effort try/except、_print CLI 输出形态）
    - .planning/observability/LOGGING-SPEC.md  （§4.1 call_source 枚举、§5 component、§9 自检清单；caller/sampling 分类与 duration_ms 规范）
    - server/common/logging.py  （redact_secrets_in_text L391）
    - server/interactions/models.py  （ModelUsageRecord L240 token 口径；RetrievalTrace L185）
    - server/services/query_embedding.py  （embed_query L241 计量链，token 复用来源）
  </read_first>
  <action>
    在 command 内按项目 observability 强约束补埋点（全部 best-effort，绝不反噬评测；只记 sha/计数/闭集枚举/run_id，⛔ 不记 query 正文或凭证）：

    - `logger = structlog.get_logger(__name__)`；复用 `_LOG_KV = {category, component, initiated_by_user_id}` 模式。
    - caller 生命周期（每 run 一次，`category="caller"`，`component="codegraph"`，`initiated_by_user_id="system"`）：`graph_bench_run_started`（run_id/repository/branch/commit_sha/gold_version/split）、`graph_bench_run_completed`（+ total_cases/duration_ms）、`graph_bench_run_failed`（异常文本经 `redact_secrets_in_text`）、水位 INVALID 时 `graph_bench_run_invalid`（+ 三方 sha 值 + invalid_reason，sha 非凭证）。
    - 逐 case sampling（`category="sampling"`）：`graph_bench_case_scored`（run_id/case_id/language/framework/entry_type/symbol_recall/duration_ms），不记 query 正文；逐边/逐跳统计走 sampling，禁 INFO 循环刷屏。
    - token：复用既有 `embed_query`/检索链的 `ModelUsageRecord` 计量（不新增计量通道）；按 run_id 关联，逐 case 记录该 case 检索/embedding 调用的 token 增量，纯图/resolve case 记 0，写入 CaseOutcome.tokens。
    - 观测写库（ModelUsageRecord/RetrievalTrace）一律 try/except best-effort，失败不中断 run、不改业务响应。

    落盘与 CLI：manifest（Task 1 结构）与 baseline JSON（`build_report` 输出 + run_id + duration_ms + 可复现命令行）分别写 `--output-manifest`/`--output-json`；`_print` 沿袭 recall command 打印 total_cases、overall 各指标、INSUFFICIENT_DATA 桶清单、受保护桶清单。确认 baseline JSON 只含原始值与空结果标记。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/codegraph/test_evaluate_graph_bench_command.py -x -q && uv run ruff check codegraph/management/commands/evaluate_graph_bench.py</automated>
  </verify>
  <acceptance_criteria>
    - `cd server && uv run pytest tests/codegraph/test_evaluate_graph_bench_command.py -x -q` 退出码 0
    - `cd server && grep -cE 'graph_bench_run_started|graph_bench_run_completed|graph_bench_run_failed|graph_bench_run_invalid' codegraph/management/commands/evaluate_graph_bench.py` ≥ 4（caller 生命周期事件齐备）
    - `cd server && grep -cE 'graph_bench_case_scored' codegraph/management/commands/evaluate_graph_bench.py` ≥ 1 且 `grep -cE 'category=.sampling.'|"sampling"' codegraph/management/commands/evaluate_graph_bench.py` ≥ 1（sampling 事件存在）
    - `cd server && grep -cE 'initiated_by_user_id' codegraph/management/commands/evaluate_graph_bench.py` ≥ 1 且 `grep -cE 'duration_ms' codegraph/management/commands/evaluate_graph_bench.py` ≥ 1（用户归因 + duration_ms）
    - `cd server && grep -cE 'redact_secrets_in_text' codegraph/management/commands/evaluate_graph_bench.py` ≥ 1（脱敏入口复用）
    - `cd server && grep -cE 'tolerance|threshold|compare_to_baseline|target_value' codegraph/management/commands/evaluate_graph_bench.py` == 0（落盘无回归门字段，BENCH-03）
    - `cd server && uv run ruff check codegraph/management/commands/evaluate_graph_bench.py && uv run mypy codegraph/management/commands/evaluate_graph_bench.py` 退出码 0
  </acceptance_criteria>
  <done>caller 生命周期（started/completed/failed/invalid）+ 逐 case sampling 事件齐备且含 duration_ms/run_id/system 归因；异常脱敏、观测 best-effort；token 复用 ModelUsageRecord 口径；manifest 与无阈值 baseline JSON 落盘；CLI 打印含 INSUFFICIENT_DATA 与受保护桶；落盘零回归门字段；lint/mypy 全绿</done>
</task>

</tasks>

## Artifacts this phase produces (Plan 04)

- `server/codegraph/management/commands/evaluate_graph_bench.py` — 薄 command（唯一 I/O 层）
  - CLI 标志：`--repo` / `--branch` / `--commit-sha` / `--split` / `--gold` / `--output-manifest` / `--output-json` / `--cold-only` / `--min-bucket-samples`
  - 函数：`_run_case()` / `_run_all_cases()` / `_print()`；`class Command`
  - structlog 事件：`graph_bench_run_started` / `graph_bench_run_completed` / `graph_bench_run_failed` / `graph_bench_run_invalid`（category=caller）；`graph_bench_case_scored`（category=sampling）
- `server/tests/codegraph/test_evaluate_graph_bench_command.py` — 默认套件（fixture 权威校验 + INVALID 短路 + schema 错误）
- `server/tests/codegraph/test_graph_bench_integration.py` — integration 标记（默认排除）

运行产物（生成物，非源码）：run manifest JSON（identity 五元组 + index_key_source + 三方水位 + 校验结果 + run_id + 可复现命令行）、baseline JSON（`build_report` 无阈值原始报告）。

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI 输入（--repo/--branch/--commit-sha/--gold）→ ORM/文件读取 | 不可信字符串在此进入查询与路径；须校验 + 参数化，不拼 SQL/shell |
| command → 被测能力（get_graph/检索/图分析） | 复用 `ensure_repository_readable` fail-closed；harness 不绕过权限/exclusion |
| 被测能力异常/检索正文 → 日志与 ledger | 异常文本与检索内容可能含敏感信息；须脱敏 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-133-09 | Elevation of Privilege | get_graph 调用（跨仓/越权读图） | high | mitigate | 只经 `get_graph_service().get_graph`（内置 `ensure_repository_readable` fail-closed + exclusion），不直连 ORM 取图、不绕过 ACL |
| T-133-10 | Tampering | CLI 参数（--repo/--commit-sha/--gold 路径） | medium | mitigate | argparse 类型/choices 校验 + `--split holdout` 拒绝 + 参数化 ORM 查询（不拼 SQL/shell）；gold 路径仅读 fixtures 目录 |
| T-133-11 | Information Disclosure | 日志/ledger（query 正文/异常/凭证） | high | mitigate | 只记 sha/计数/闭集枚举/run_id；异常文本经 `redact_secrets_in_text`，入库经 `redact_for_ledger`；不记 query 正文或凭证 |
| T-133-12 | Denial of Service | 观测埋点 | medium | mitigate | 观测写库 try/except best-effort，失败不中断 run；逐 case/逐边统计走 sampling 禁 INFO 刷屏 |
| T-133-13 | Tampering | 评测只读性（写生产索引） | high | mitigate | command 只调只读入口 + ORM 读；acceptance grep 强制无索引写入/回填符号；不写生产索引 |
| T-133-SC | Tampering | pip/uv installs | high | mitigate | 本 phase 零新增生产依赖（研究 Package Legitimacy Audit 确认），无安装面，supply-chain 门不触发 |
</threat_model>

<verification>
- `cd server && uv run pytest tests/codegraph/test_evaluate_graph_bench_command.py -x -q` 全绿（默认套件）
- `cd server && uv run pytest tests/codegraph/ -k graph_bench -q` 全绿（不含 integration）
- `cd server && uv run pytest tests/codegraph/test_graph_bench_integration.py --collect-only -q` 可 collect（integration 默认排除）
- `grep -cE 'tolerance|threshold|compare_to_baseline|target_value' codegraph/management/commands/evaluate_graph_bench.py` == 0
- ruff + mypy 通过
</verification>

<success_criteria>
- 水位不一致/为空 → INVALID 短路、manifest 含 invalid_reason、非零退出、get_graph 未被调用（BENCH-01）
- OK 路径逐 case 调未修改 v0.22 能力产逐 case + 逐桶无阈值 baseline，冷/热计时与 token 记录（BENCH-03）
- run manifest 含 identity 五元组 + index_key_source + 可复现命令行（BENCH-01，供 Phase 140 复用）
- 评测只读、holdout 拒读；观测 caller/sampling + 脱敏 + best-effort 符合强约束
</success_criteria>

<output>
Create `.planning/phases/133-commit-v0-22-baseline/133-04-SUMMARY.md` when done
</output>
