---
phase: 114-ai
plan: 02
type: execute
wave: 2
depends_on: ["114-01"]
files_modified:
  - server/services/process_runtime/blueprint_review.py
  - server/tests/services/process_runtime/test_blueprint_review_rules.py
autonomous: true
requirements: [FLOW-07]
prohibitions:
  - "禁止修改 §13.2 冻结面（零改动，`git diff --name-only` 必须零命中）：`process_runtime/{decompose_segments,research_adapter,architect_merge_adapter,clarify_adapter,render}.py`、delivery 侧 `merged_plan.py`、`process_runtime/resume.py`、`repositories/services/charter_service.py`"
  - "禁止修改 `task/core/knowledge_tools.py` 公共 handler 工厂（本相位不碰 `task/`）"
  - "🔒 本 plan **一行都不改** `builtin_processes.py`（stage 注册与新 handler 归 114-03，且只允许「只加注册与新 handler」）；`_TECHNICAL_PLAN_STAGES` / `_ECHO_STAGES` 永不触碰"
  - "🔒 本 plan **一行都不改** `server/services/process_runtime/blueprint_resume.py`（其映射表追加归 114-03，且**删除行上界 0**）"
  - "⛔ **`record_answer` 禁用于 finding 留痕**：本 plan 是纯函数模块，`rg -n 'record_answer' server/services/process_runtime/blueprint_review.py` 必须**零命中**（留痕通道只有 114-01 的 `append_note`，落库归 114-03）"
  - "禁止在本模块出现任何 ORM 写：`rg -n 'objects\\.(create|update|bulk_|filter)' server/services/process_runtime/blueprint_review.py` 零命中（INV-6，`test_blueprint_inv6_guard` 源码扫描守护）"
  - "禁止修改 `blueprint_quality.py`（111 已交付、单测锁口径）：规则②的**条目级**走查在本模块自写，只复刻 `_iter_key_conclusion_citations` 的三类口径"
  - "禁止修改 `blueprint_schema.py` / `blueprint_anchor.py` / `blueprint_merge.py` / `blueprint_reconcile.py` / `blueprint_repo_plan.py` / `blueprint_charter_match.py` / `blueprint_confirm_gate.py` / `blueprint_spec_gate.py` / `blueprint_lifecycle_service.py`（受限面，本 plan 只读消费）"
  - "禁止新增 `CallSource` 枚举值：goal-backward 一律用 111 已注册的 `CallSource.BLUEPRINT_AI_REVIEW`（`server/agents/call_source.py:130`），`agents/call_source.py` 零改动"
  - "禁止让 LLM 承担六类机械规则中的任何一条（CONTEXT 明令纯函数化——可复现、可单测、可证伪是立项前提）"
  - "禁止任何规则外抛异常：整函数 `try/except` 兜底并返回**已积累的结果**（不是空结果），照 `blueprint_reconcile.py:113-115`"
  - "禁止把 `validate_blueprint` 的返回直接当规则①结论（`blueprint_schema.py:809-810` 对缺 `schema_version` 的 content **直接 `return True, None`** → 必须先自断言 `content.get(\"schema_version\") == \"blueprint/v1\"`）"
  - "禁止只用 `citation_coverage` 比率做规则②（分母为 0 返回 **1.0**，`blueprint_quality.py:76`，空文档拿满分）"
  - "禁止把 `api_contracts[].direction` 写成 `\"produced\"`（schema 实际枚举是 `provided` / `consumed`，`blueprint_schema.py:522-524`，写错永远匹配不到 = 规则④恒通过）"
  - "禁止 findings 正文进日志 / 进事件 payload / 进 `stage_state`（只进线程 body；日志与 payload 只放计数与分级分布）"
must_haves:
  truths:
    - "新建 `server/services/process_runtime/blueprint_review.py`，六类机械规则**在无 LLM、无 DB、无网络下**产确定性结论：同一 content 反复调用 `run_mechanical_rules` 输出逐字相等（含顺序）"
    - "统一 finding dict 形状恒定六键 `{rule_id, severity, section_path, block_id, repository_id, detail}`，`severity ∈ {blocker, warning, info}`；模块常量 `SEVERITY_BLOCKER/WARNING/INFO` 与 `ThreadSeverity` 枚举值相等（由测试锁死，模块本身不 import Django）"
    - "六类各有一条构造样例可证伪：缺引用 → `citation_missing` BLOCKER；角色不一致 → `role_mismatch` BLOCKER；API 断链 → `api_ref_dangling` BLOCKER；以周为单位排期 → `forbidden_schedule` BLOCKER；越确认门锁定 → `gate_lock_violation` BLOCKER；违章程（`evolution=maintenance_only/deprecated` 且无 `decision_log` 支撑）→ `charter_violation` BLOCKER"
    - "⭐ 前置完整性短路：`repo_associations` / `implementation_overview.items` / `requirement_spec.feature_points` 三者任一为空 → 单条 `precondition_missing` BLOCKER 且**不跑后五条**（空蓝图绝不产出 `[]` 假通过）"
    - "⭐ 缺 `schema_version` 的 content 不靠 `validate_blueprint` 假通过：规则① 先自断言 `schema_version == \"blueprint/v1\"`，缺失/不等即 `schema_version_missing` BLOCKER（正反并列样例）"
    - "规则⑥ 对**没有章程的仓跳过**（`aload_charters` 缺章程的仓不出现在结果里）——不判 BLOCKER；`charters=None` 时整条规则退化为空结果"
    - "唯一模糊匹配项（indirect 仓 `capabilities_used` 未被引用）判 **WARNING**；纯集合运算项（item 指向 `role=\"indirect\"` 的仓 / direct 仓零实现项）判 BLOCKER"
    - "规则⑤ 扫 out_of_scope 引入时**排除 `deferred_ideas` 段**（该段是 scope 外想法的正当落位，扫它会误报）"
    - "恒不抛：`None` / `{}` / 类型错乱（list 当 dict、str 当 list）输入下每个公开函数返回 list（可为空或已积累结果），不抛异常"
    - "`agoal_backward_review` 为唯一 LLM 一类，`call_source=CallSource.BLUEPRINT_AI_REVIEW`（不新增枚举值），**独立 fresh context**（只喂 digest，不带起草/融合会话历史），不可得时返回 `None`"
    - "⭐ LLM fail-closed：`None` 由 `normalize_review_findings(None)` 归一成**一条 `severity=\"warning\"` 的 meta finding**（`rule_id=\"goal_backward_unavailable\"`），**绝不当作「无问题」放行**"
    - "有界追加防 HITL 刷屏：`_MAX_FINDINGS` 上界 + `detail` 字符截断，超界静默丢弃（结论已足够打回/开线程）"
    - "去重幂等键纯函数 `finding_dedupe_key(finding) -> str` 存在（`(rule_id, block_id or section_path)`），供 114-03 落库前查既有线程"
  artifacts:
    - path: "server/services/process_runtime/blueprint_review.py"
      provides: "六类机械规则纯函数 + run_mechanical_rules 总入口 + agoal_backward_review LLM 一类 + normalize_review_findings + finding_dedupe_key + STAGE_STATE_KEY"
      contains: "def run_mechanical_rules"
    - path: "server/tests/services/process_runtime/test_blueprint_review_rules.py"
      provides: "六类各一条证伪样例 + 空蓝图短路 + schema_version 假通过防回归 + direction 枚举防回归 + deferred_ideas 不误报 + 章程缺失跳过 + 恒不抛 + LLM 不可得降级"
      contains: "run_mechanical_rules"
  key_links:
    - from: "server/services/process_runtime/blueprint_review.py"
      to: "server/services/process_runtime/blueprint_schema.py"
      via: "validate_blueprint / iter_blocks / BLUEPRINT_SCHEMA_VERSION（不自写递归、不自写校验）"
      pattern: "iter_blocks"
    - from: "server/services/process_runtime/blueprint_review.py"
      to: "server/services/process_runtime/blueprint_repo_plan.py"
      via: "确认门锁定基线复用 `_normalize_locked_repos` 投影（函数内 lazy import，避免两处口径漂移）"
      pattern: "_normalize_locked_repos"
    - from: "server/services/process_runtime/blueprint_review.py"
      to: "server/agents/call_source.py"
      via: "use_call_source(CallSource.BLUEPRINT_AI_REVIEW)（函数内 lazy import，零新增枚举值）"
      pattern: "BLUEPRINT_AI_REVIEW"
---

<objective>
新建 `server/services/process_runtime/blueprint_review.py`，交付 FLOW-07 的**判定内核**：

① **六类机械规则纯函数化**（无 IO / 无 ORM / 无 LLM，stdlib + 既有纯函数）——schema 完整性、引用覆盖、角色一致性、API 闭环、禁令、章程边界，外加 CONTEXT specifics 要求的**前置完整性短路**与**确认门锁定校验**；
② 统一 finding dict 与归一/去重/有界追加 helper（114-03 落线程、114-05 呈现共用同一形状）；
③ **仅 goal-backward 一类走 LLM**（`agoal_backward_review`，`call_source=blueprint_ai_review`，不可得返回 `None` 且上游 fail-closed 记 warning meta finding）；
④ 纯函数测试逐条证伪：六类各一条缺陷样例 + 空蓝图不假通过 + 三条已知陷阱（`schema_version` 假通过 / `direction` 枚举 / `deferred_ideas` 误报）的正反并列断言。

Purpose: 「可复现、可单测、可证伪」是本相位的立项前提。六类若交给 LLM，审查结论每次不同、无法回归、打回决策不可解释；`validate_blueprint` 的 v0 pass-through 与 `citation_coverage` 的分母为 0 返 1.0 两条既有语义，会让半成品蓝图**一片假通过**——本 plan 的短路与条目级走查就是为了堵这两个洞。
Output: 一个纯函数模块 + 一个零 DB 零 mock 的测试文件。**无 migration、无新 stage、无 ORM 访问、无新 CallSource 枚举值。**
</objective>

<execution_context>
工作目录：worktree 根 `/Users/zaneliu/Projects/open-source/friday-clean/.claude/worktrees/v0.20-blueprint`。wave 2，依赖 114-01（`open_thread(severity=)` / `append_note` / `aunresolved_blocker_count` 三个契约已就位）。

本 plan **只新建两个文件**，对既有代码零改动（`git diff --name-only` 应为空，只有 untracked 新文件）。判定内核与落库/编排彻底分离：
- 本 plan 不建线程、不转状态、不写 `stage_state`（只定义 `STAGE_STATE_KEY = "ai_review"` 常量供 114-03 用）；
- 本 plan 不注册 stage、不改 `merge.transitions`、不碰 `blueprint_resume`；
- 本 plan 的 `agoal_backward_review` 只做「LLM 单调用 + 归一 + 降级」，是否打回由 114-03 判。

⚠️ 字段名与签名一律按 114-RESEARCH §1 实测，**不得猜测**。关键实测清单（照抄，别重新推断）：
- `validate_blueprint(content) -> tuple[bool, str | None]`（`blueprint_schema.py:793`；`:809-810` 缺 `schema_version` **直接 `return True, None``）；`BLUEPRINT_SCHEMA_VERSION == "blueprint/v1"`（`:37`）
- `iter_blocks(content) -> list[tuple[str, dict]]`（`blueprint_schema.py:919`，section_path 为「点分 + `[标识]`」）
- `repo_associations[].role ∈ {direct, indirect}`（`:247-251`，required）/ `capabilities_used`（`:300-303`，自由 array）/ `confirmed_at_gate`（`:313-316`，bool）/ `responsibility`（`:272-275`，block_list）/ `rationale.citations`（`blueprint_quality.py:45-46` 三类口径之一）/ `rationale.constraint_refs`（`:260-264`）
- `api_contracts[].id`（`:509`）/ `direction ∈ {provided, consumed}`（`:522-524`，**不是 produced**）/ `data_source.availability ∈ {existing, needs_support}`（`:555-559`）/ `data_source.support_repository_id`（`:560-563`）
- `interaction_flows[].steps[].api_ref`（`:684-687`）
- `requirement_spec.boundaries`（`:216-219`，弱 schema object）/ `constraints`（`:220-223`，弱 schema array）/ `deferred_ideas`（`:737-740`）/ `decision_log`（`:733-736`，弱 schema array）
- `RepoCharter.evolution ∈ {active, maintenance_only, deprecated}`（`repositories/models.py:1113`）/ `boundaries = [{rule, decided_by, citations}]`（`:1144`）；批量读走 `aload_charters(repository_ids) -> dict[str, dict]`（`blueprint_charter_match.py:288`，缺章程的仓**不出现**；`_CHARTER_VALUE_FIELDS` 含 `evolution`，**不含 `draft_content`**）
- `CallSource.BLUEPRINT_AI_REVIEW = "blueprint_ai_review"`（`agents/call_source.py:130`，已注册）

本模块**可跑 `ruff format`**（新文件，非受限面）。
</execution_context>

<context>
@.planning/phases/114-ai/114-CONTEXT.md（锁定：六类机械规则纯函数化的逐条判据；仅 goal-backward 走 LLM 且 `call_source=blueprint_ai_review`；审查代理独立 fresh context；确认门锁定偏离即 BLOCKER；Discretion = 内部函数切分 / prompt 措辞 / 去重聚合策略自定）
@.planning/phases/114-ai/114-RESEARCH.md §1（六类规则判据来源逐条行号 + 枚举纠偏 + 章程读法）、§2 findings 形状、Pitfalls P1（半成品蓝图三条假通过陷阱 + 「被减集合为空判 skip 而非 pass」）、Don't Hand-Roll 表
@.planning/phases/114-ai/114-PATTERNS.md 第 1 类（`blueprint_reconcile.py` 的模块 docstring 编号契约书 / 恒定形状返回 / 整函数 try 兜底返已积累结果 / `_append` 有界追加 / 「一侧缺值 ≠ 矛盾」降噪纪律 / 口径同源写进 docstring）、第 2 类（`blueprint_ambiguity_score.ascore_ambiguity` 六步骨架 + lazy import + `None` 语义 + 归一独立纯函数 + 上界常量集中模块头 + 异常脱敏）、第 10 类 Analog A（`test_blueprint_reconcile.py` 的「守 N 件事」编号清单 + 正负并列 + 跨模块口径一致性断言）
@server/services/process_runtime/blueprint_reconcile.py（**主 analog，全文 333 行**：模块 docstring 四段、`__all__` 只列公开判定、内部 helper 集中文件尾 `# ── 内部纯函数 ──`、`_MAX_FINDINGS = 50`、`_append`、`_cited`/`_absent`/`_normalized`）
@server/services/process_runtime/blueprint_ambiguity_score.py（L360-458 `ascore_ambiguity` 六步 + L153 `normalize_ambiguity_scores` 归一纯函数 + L55-63 上界常量）
@server/services/process_runtime/blueprint_quality.py（L34-79 `_cited` / `_iter_key_conclusion_citations` 三类关键结论口径 / `citation_coverage` 分母为 0 返 1.0——规则②要复刻口径但**不改本文件**）
@server/services/process_runtime/blueprint_schema.py（L216-223 boundaries/constraints、L247-323 repo_associations 全字段、L509-569 api_contracts + data_source、L684-687 steps.api_ref、L733-740 decision_log/deferred_ideas、L793-917 `validate_blueprint` 含 `:809-810` v0 pass-through 与 5 项后置检查、L919-1043 `iter_blocks`）
@server/delivery/services/blueprint_anchor.py（L34-63 `_block_text` 四型块取文本口径——规则⑤扫文本复用它，**不自写第二套**；本模块 stdlib-only、零 Django import，可顶层 import）
@server/services/process_runtime/blueprint_repo_plan.py（L1087-1124 `_normalize_locked_repos` 投影：`repo_associations` / 确认门快照 → `[{repository_id, repository_name, role, responsibility, fitness}]`，`removed is True` 跳过、role 非法回落 `direct`——确认门锁定校验的对比基线）
@server/tests/services/process_runtime/test_blueprint_reconcile.py（L1-31 测试体例基准：「守八件事」编号清单 + 「零 DB、零 mock」首行声明 + 模块级期望常量）
</context>

<tasks>

<task type="auto">
  <name>Task 1: 模块骨架（docstring 契约书 + 常量 + finding 形状 + 归一/去重/有界追加）+ 前置完整性短路 + 规则①②③</name>
  <files>server/services/process_runtime/blueprint_review.py</files>
  <read_first>server/services/process_runtime/blueprint_reconcile.py L1-40（模块 docstring 四段的**确切措辞与分段方式** + `__all__` 只列公开判定 + `_MAX_FINDINGS` 与注释理由）、L40-72（`reconcile_cross_repo_apis` 的「恒定形状返回 + docstring 逐键解释」）、L105-120（整函数 `try/except` 返回**已积累结果**的确切写法与 `# noqa: BLE001` 注释）、L212-333（`# ── 内部纯函数 ──` 区段布局 + `_cited` / `_absent` / `_normalized` / `_append` 四个 helper 的粒度）；server/services/process_runtime/blueprint_quality.py L34-66（`_cited` 判定 + `_iter_key_conclusion_citations` 的**三类关键结论确切路径**：`current_state_analysis[].findings[].citations` / `repo_associations[].rationale.citations` / `impact_analysis.affected_features[].citations`）；server/services/process_runtime/blueprint_schema.py L37（`BLUEPRINT_SCHEMA_VERSION` 字面值）、L793-816（`validate_blueprint` 签名 + `:809-810` 的 v0 pass-through 分支）、L919-960（`iter_blocks` 返回形状与 section_path 生成方式）、L247-323（`repo_associations` 逐字段名）；server/delivery/services/blueprint_anchor.py L34-63（`_block_text`）</read_first>
  <action>
新建 `server/services/process_runtime/blueprint_review.py`。

**① 模块 docstring = 编号契约书**（照 `blueprint_reconcile.py:1-20` 的四段体例，逐段写）：
- 第 1 段：「本模块分两节：**机械规则纯函数节**（无 IO / 无 ORM / 无 LLM，stdlib + 既有纯函数）与 **goal-backward LLM 节**（唯一 LLM 一类，best-effort 返 `None`）。顶层零 ORM import；ORM 只由调用方（114-03 adapter）经 `BlueprintLifecycleService` 触达（INV-6）。」
- 第 2 段：「输入是**半可信**装配产物——`merge` 的 `exhausted` 出口也走 `merged` 边（`builtin_processes.py:694-696`），故本模块拿到的蓝图可能是『已成形但未达标』的半成品：整段缺失、`repo_associations` 为空、`citations` 池不存在都属常态。逐字段 `.get` 防御、逐层 `isinstance` 检查、**绝不外抛**——审查结论是编排层『打回还是升人审』的判据，抛异常会把『有缺陷』升级成『整轮失败』。」
- 第 3 段：「**不用 LLM 做机械判定**：六类必须可复现、可单测、可解释。LLM 只承担 goal-backward 一类，且其不可得（`None`）**一律 fail-closed 记一条 warning meta finding**，绝不当作『无问题』。」
- 第 4 段（纠偏段，照 reconcile 第 4 段的作用）：「三条已知假通过陷阱，写死在实现里：(a) `validate_blueprint` 对缺 `schema_version` 的 content **直接 `return True, None`**（`blueprint_schema.py:809-810`）→ 规则①**先自断言** `schema_version == BLUEPRINT_SCHEMA_VERSION`；(b) `citation_coverage` 分母为 0 返回 **1.0**（`blueprint_quality.py:76`）→ 规则②走**条目级**走查，不看比率；(c) `api_contracts[].direction` 枚举是 `provided` / `consumed`（`blueprint_schema.py:522-524`），写成 `produced` 会永远匹配不到 ⇒ 规则④恒通过。另：被减集合为空时判 **skip 或 BLOCKER，绝不判 pass**——`repo_associations` 为空时『每个 direct 仓 ≥1 实现项』恒真，那是假阳性通过（P1）。」

**② 常量集中模块头**（照 `blueprint_ambiguity_score.py:55-63`）：
```
STAGE_STATE_KEY = "ai_review"      # stage_state 顶层键（实测未被占用；⚠️ 绝不复用 "merge" 桶）
SEVERITY_BLOCKER = "blocker"
SEVERITY_WARNING = "warning"
SEVERITY_INFO = "info"
_MAX_FINDINGS = 50                 # 结论会进线程 body 与人审面板，无界列表会把 HITL 刷爆
_MAX_DETAIL_CHARS = 500            # 与 blueprint_schema._MAX_ERROR_CHARS 同量级
_WEEK_SCHEDULE_PATTERN = ...        # 规则⑤：`\d+\s*周` / `week`（re.IGNORECASE）
```
`SEVERITY_*` 用字面量（本模块顶层零 Django import，拿不到 `ThreadSeverity`）；**行内注释写 `# == ThreadSeverity.BLOCKER`**，并在 Task 3 加一条等值断言测试锁死（沿用 `blueprint_resume._STAGE_BLUEPRINT_STATUS` 的同款纪律）。

**③ finding 形状与三个公共 helper**：
- `def _finding(rule_id, severity, *, section_path="", block_id="", repository_id="", detail="") -> dict`：恒定六键，`detail` 截断到 `_MAX_DETAIL_CHARS`。docstring 逐键解释语义与**调用方动作**（`severity=blocker` ⇒ 114-03 以 `blocking=True` 开线程并计入打回判据；`warning/info` ⇒ 只作人审参考不打回）。
- `def _append(bucket: list[dict], entry: dict) -> None`：有界追加（照 `blueprint_reconcile.py:330-333`）。
- `def finding_dedupe_key(finding: Any) -> str`：`f"{rule_id}|{block_id or section_path}"`。docstring：「114-03 落库前的幂等键——命中既有 `kind=ai_review_finding` 且 `status ∈ {open, answered}` 的线程时走 `append_note('第 N 轮仍存在')` 而非新开线程（同一 block 反复开线程会让人审侧噪声爆炸）。」
- `def normalize_review_findings(raw: Any) -> list[dict]`：把任意来源（含 LLM 原始输出、`None`）归一到 finding 形状；**`raw is None` ⇒ 返回单条 `_finding("goal_backward_unavailable", SEVERITY_WARNING, detail="goal-backward 审查未能执行（LLM 不可得），本轮不据此打回，请人审关注")`**。docstring 写明「`None` 是『LLM 一类不可得』信号，**不是**『无问题』——上游 fail-closed 的落点就在这里」。非法 severity 回落 `SEVERITY_WARNING`（宁可少挡不误钉）。

**④ 前置完整性（短路判定，公开）**：
```
def check_preconditions(content: Any) -> list[dict]:
```
`repo_associations` / `implementation_overview.items` / `requirement_spec.feature_points` 三者任一非非空 list → 单条 `precondition_missing` BLOCKER（`detail` 列出**缺哪几段的段名**，不带正文）。docstring 写明「命中即**短路**，`run_mechanical_rules` 不再跑后五条：被减集合为空会让规则③④⑤⑥恒真，跑出来是一片假通过噪声（P1）」。

**⑤ 规则① schema 完整性**：
```
def check_schema(content: Any) -> list[dict]:
```
先 `if not isinstance(content, dict) or content.get("schema_version") != BLUEPRINT_SCHEMA_VERSION:` → `schema_version_missing` BLOCKER（docstring 引 `:809-810` 的 pass-through 理由）；否则 `ok, error = validate_blueprint(content)`，`ok is False` → `schema_invalid` BLOCKER，`detail` 直接用 `error`（**已由 `_format_error` 脱敏截断 500 字符**，可安全引用；仍过 `_MAX_DETAIL_CHARS`）。**不重写 5 项后置检查**（Don't Hand-Roll）。

**⑥ 规则② 引用覆盖（条目级走查，自写但口径复刻）**：
```
def check_citations(content: Any) -> list[dict]:
```
内部 `_iter_conclusion_entries(content) -> Iterator[tuple[str, str, Any, bool]]`（`(section_path, repository_id, citations, is_key_conclusion)`），三类口径**逐字复刻** `blueprint_quality._iter_key_conclusion_citations`（`current_state_analysis[].findings[]` / `repo_associations[].rationale` / `impact_analysis.affected_features[]`），但同时产出 `section_path`（用 `iter_blocks` 的点分 + `[标识]` 约定手工拼，如 `current_state_analysis[{repository_id}].findings[{index}]`）。判定：关键结论条目 citations 空 → `citation_missing` **BLOCKER**；其余事实性断言（`implementation_overview.items[].rationale` 之类存在但无 citations 的条目）→ `citation_missing_weak` **WARNING**。docstring 写明「口径同源于 `blueprint_quality._iter_key_conclusion_citations`：两处漂移会导致『覆盖率说达标、审查说缺引用』的自相矛盾（那种矛盾无法被任何一侧的测试逮住）」，并写明「**不看 `citation_coverage` 比率**，理由见模块 docstring 第 4 段 (b)」。

**⑦ 规则③ 角色一致性**：
```
def check_roles(content: Any) -> list[dict]:
```
三条，按可证伪度分档：
- `role == "direct"` 的 `repository_id` 集合 − `items[].repository_id` 集合 ≠ ∅ → 每仓一条 `role_mismatch` **BLOCKER**（`detail`：「direct 仓无任何实现项」）；
- 某 `item.repository_id` 指向 `role == "indirect"` 的仓 → `role_mismatch` **BLOCKER**（`detail`：「改动 indirect 仓」，纯集合运算）；
- indirect 仓的 `capabilities_used` 归一取文本后，未在任何 `items` 的文本（`how` / `text` 走 `_block_text`）或 `api_contracts[].data_source.from_api|from_service` 中被包含匹配 → `capability_unreferenced` **WARNING**（唯一模糊匹配项，A4：强判 BLOCKER 会产生不可复现假阳性）。
`capabilities_used` 元素可能是 str 也可能是 dict → 内部 `_capability_text(item) -> str` 归一。

全部公开函数整体包 `try/except Exception` 并 `return result`（已积累结果），`# noqa: BLE001` 注释照 `blueprint_reconcile.py:113`。`__all__` 只列公开判定 + 三个公共 helper + `STAGE_STATE_KEY` + `SEVERITY_*`；内部 helper 全 `_` 前缀并集中在文件尾 `# ── 内部纯函数 ──` 区段。
  </action>
  <verify>
    <automated>cd server && uv run python -c "from services.process_runtime import blueprint_review as m; print(sorted(m.__all__)); print(m.check_preconditions({})); print(m.check_schema({}))"</automated>
  </verify>
  <acceptance_criteria>
- `rg -n "^STAGE_STATE_KEY = \"ai_review\"" server/services/process_runtime/blueprint_review.py` 命中（**不是 `"merge"`**）。
- `rg -n "def check_preconditions|def check_schema|def check_citations|def check_roles|def normalize_review_findings|def finding_dedupe_key" server/services/process_runtime/blueprint_review.py` 六条全命中。
- `rg -n "BLUEPRINT_SCHEMA_VERSION" server/services/process_runtime/blueprint_review.py` 命中（规则①先自断言 schema_version，不靠 `validate_blueprint`）。
- `rg -n "citation_coverage" server/services/process_runtime/blueprint_review.py` **零命中**（规则②走条目级，不看比率）。
- `rg -n "objects\.(create|update|filter|bulk_)" server/services/process_runtime/blueprint_review.py` **零命中**（顶层零 ORM，INV-6）。
- `rg -n "record_answer" server/services/process_runtime/blueprint_review.py` **零命中**（留痕只走 114-01 的 `append_note`，且落库在 114-03）。
- `rg -c "noqa: BLE001" server/services/process_runtime/blueprint_review.py` ≥4（每条公开判定各有兜底）。
- `cd server && uv run python -c "from services.process_runtime.blueprint_review import check_schema, check_roles, check_citations, check_preconditions
for fn in (check_preconditions, check_schema, check_citations, check_roles):
    for bad in (None, {}, [], 'x', {'repo_associations': 'not-a-list'}):
        assert isinstance(fn(bad), list), (fn, bad)
print('no-raise OK')"` 输出 `no-raise OK`（恒不抛）。
- `cd server && uv run python -c "from services.process_runtime.blueprint_review import check_preconditions
out = check_preconditions({'schema_version': 'blueprint/v1'})
assert len(out) == 1 and out[0]['rule_id'] == 'precondition_missing' and out[0]['severity'] == 'blocker', out
print('precondition OK')"` 输出 `precondition OK`（**空蓝图绝不 pass**）。
- `cd server && uv run ruff check services/process_runtime/blueprint_review.py && uv run ruff format --check services/process_runtime/blueprint_review.py` 通过。
- `git diff --name-only` 为空（本 Task 只新建 untracked 文件，零既有文件改动）。
  </acceptance_criteria>
  <done>模块骨架、四段 docstring 契约书、常量、finding 六键形状与三个 helper 就位；前置完整性能对空蓝图产 BLOCKER 短路；规则①不被 `schema_version` 缺失假通过；规则②走条目级；规则③三档判定齐；全部公开函数恒不抛。</done>
</task>

<task type="auto">
  <name>Task 2: 规则④⑤⑥ + 确认门锁定校验 + run_mechanical_rules 总入口 + agoal_backward_review（LLM 一类，fail-closed）</name>
  <files>server/services/process_runtime/blueprint_review.py</files>
  <read_first>server/services/process_runtime/blueprint_schema.py L509-574（`api_contracts` 的 `id` / `direction` 枚举**确切两个值** / `data_source.availability` 两个值 / `support_repository_id` 键名）、L670-751（`interaction_flows[].steps[].api_ref` 路径 + `decision_log` / `deferred_ideas` 两段的确切键名与弱 schema 声明）、L216-264（`requirement_spec.boundaries` / `constraints` 形状 + `rationale.constraint_refs`）；server/repositories/models.py L1091-1167（`RepoCharter.evolution` 三个 choices 的**字面值** + `boundaries` 元素形状 + `draft_content` 不生效）；server/services/process_runtime/blueprint_charter_match.py L263-310（`_CHARTER_VALUE_FIELDS` 含哪些键、`aload_charters` 返回形状与「缺章程的仓不出现」）；server/services/process_runtime/blueprint_repo_plan.py L1087-1124（`_normalize_locked_repos` 的确切签名与返回键）；server/services/process_runtime/blueprint_ambiguity_score.py L360-458（**六步骨架逐行**：`time.monotonic()` → `*_started` sampling 事件 → `ProviderConfigService.aresolve()` 取 `extra.default_model` 缺则 `return None` → `build_chat_model(resolved, model_name, streaming=False)` → `with use_call_source(...)` + `ainvoke` → `_parse_object_json(_content_to_text(...))` 失败 `return None`；含全部 lazy import 的**确切模块路径**与 `redact_secrets_in_text` 兜底）、L153-200（`normalize_ambiguity_scores` 作为独立归一纯函数的形态）；server/services/process_runtime/blueprint_merge.py L774-810（`_feature_point_digest` / `_impl_items_digest` 的裁剪投影写法——goal-backward 的 digest 照此构造）</read_first>
  <action>
在 `blueprint_review.py` 内继续追加（不动 Task 1 已写内容）。

**① 规则④ API 闭环**：
```
def check_api_closure(content: Any) -> list[dict]:
```
两条纯集合运算，均 **BLOCKER**：
- `{step.api_ref for flow in interaction_flows for step in flow.steps if api_ref}` − `{c.id for c in api_contracts}` ≠ ∅ → 每个悬空 ref 一条 `api_ref_dangling`（`section_path` 用 `interaction_flows[{flow_id}].steps[{index}]`）；
- `direction == "consumed"` 且 `data_source.availability == "needs_support"` 时，`data_source.support_repository_id` 缺失或不在 `{assoc.repository_id}` → `support_repo_missing`。
⚠️ docstring 显式写「`direction` 的合法值是 **`provided` / `consumed`**（`blueprint_schema.py:522-524`）——写成 `produced` 会永远匹配不到，规则④就变成恒通过的装饰品；Task 3 有一条防回归断言」。**只读 `data_source.*` 子路径，顶层同名键一概不读**（照 `blueprint_reconcile` 第 4 段纠偏纪律）。

**② 规则⑤ 禁令**：
```
def check_prohibitions(content: Any) -> list[dict]:
```
三条：
- **排期禁令**：用 `iter_blocks(content)` 走查全部 block，取文本走 `_block_text`（顶层 `from delivery.services.blueprint_anchor import _block_text`，注释「口径同源：不自写第二套取文本规则；该模块 stdlib-only、零 Django import，顶层 import 不破坏本模块纪律」），命中 `_WEEK_SCHEDULE_PATTERN` → `forbidden_schedule` **BLOCKER**（`detail` 只写命中的**片段 ≤80 字符**与 section_path，不贴整块正文）；
- **out_of_scope 引入**：`requirement_spec.boundaries.out_of_scope` 归一（容忍 `list[str]` 与 `list[dict]`，dict 取 `text` / `name`），在 block 文本中被包含匹配 → `out_of_scope_introduced` **WARNING**（文本包含是弱判据）。⚠️ **扫描时排除 `deferred_ideas` 段**（`blueprint_schema.py:737-740` 是「scope 外想法」的正当落位，扫它必误报）——实现上先算出 `deferred_ideas` 下所有 section_path 前缀并跳过；
- **constraint 引用悬空**：`{ref for assoc.rationale.constraint_refs}` − `{c.id for c in requirement_spec.constraints}` ≠ ∅ → `constraint_ref_dangling` **BLOCKER**（`rationale.constraint_refs` 是**唯一已存在的 constraint 引用通道**；「语义与 constraints 冲突」交 goal-backward LLM 一类，机械规则不碰——写进 docstring）。

**③ 规则⑥ 章程边界**：
```
def check_charters(content: Any, *, charters: dict[str, dict] | None = None) -> list[dict]:
```
- `charters` 为 `None` / `{}` → 返回 `[]`（**跳过而非 BLOCKER**；docstring：「`aload_charters` 对缺章程的仓不返回条目，异常时整体返 `{}`——章程读失败不该把整份蓝图判成违章」）；
- `role == "direct"` 且该仓 `charters[rid]["evolution"] ∈ {"maintenance_only", "deprecated"}` 且 `decision_log` 中**无该 repository_id 的支撑条目** → `charter_violation` **BLOCKER**；支撑判定内部 helper `_has_decision_support(content, repository_id) -> bool`（扫 `decision_log[]` 的 `repository_id` 或 `question`/`answer` 文本包含该 repo id / name，弱 schema 逐字段 `.get`）；
- 「违背 `boundaries[].rule`」→ `charter_boundary_risk` **WARNING**（A4：`rule` 是自由文本，强判 BLOCKER 会产生不可复现假阳性；语义判定交 LLM 一类）。
- ⚠️ 只读正式字段，**绝不读 `draft_content`**（`repositories/models.py:1156` 草案不生效，AI 草案不该约束人）。

**④ 确认门锁定校验**：
```
def check_gate_lock(content: Any, *, locked_snapshot: Any = None) -> list[dict]:
```
- 基线：`locked_snapshot`（114-03 传 `session.stage_state["confirmation"]`）经 **`_normalize_locked_repos` 投影**（函数内 lazy import：`from services.process_runtime.blueprint_repo_plan import _normalize_locked_repos`，注释「复用既有投影做对比基线，避免两处口径漂移；lazy import 守本模块顶层零 ORM 纪律」）；`locked_snapshot` 为空时回落到 content 内 `confirmed_at_gate is True` 的条目自比对（此时只能检出「锁定条目被整条移除」）。
- 判定（均 **BLOCKER**，`rule_id="gate_lock_violation"`）：基线中的 `repository_id` 在当前 `repo_associations` 中**消失**；`role` 与基线不一致；`responsibility` 文本（走 `_block_text` join）与基线不一致。`section_path` 用稳定锚 `repo_associations[{repository_id}].responsibility`，`block_id` 用 `blk_gate_resp_{repository_id}`（112 写入侧稳定命名，`blueprint_confirm_gate.py:291-303`）。
- docstring：「偏离 112 锁定即 BLOCKER——要变必须**重开确认门**，不允许在阶段 3 之后悄悄改仓库集/职责（CONTEXT 锁定）。」

**⑤ 总入口**：
```
def run_mechanical_rules(
    content: Any, *, charters: dict[str, dict] | None = None, locked_snapshot: Any = None
) -> list[dict]:
```
顺序：`check_preconditions` → **非空即 return（短路）** → 依次 `check_schema` / `check_citations` / `check_roles` / `check_api_closure` / `check_prohibitions` / `check_charters` / `check_gate_lock`，逐条经 `_append` 有界汇总。docstring 写明「**确定性**：同一输入输出逐字相等（含顺序）——顺序即上述固定顺序，集合运算前一律 `sorted()`，禁止依赖 dict/set 迭代顺序」。整体 `try/except` 返已积累结果。

**⑥ goal-backward LLM 一类**：
```
async def agoal_backward_review(
    *,
    feature_points: list[dict[str, Any]],
    impl_items: list[dict[str, Any]],
    test_strategy: Any = None,
    must_haves: Any = None,
    key_links: Any = None,
    session_id: str = "",
) -> list[dict] | None:
```
**逐条照抄 `ascore_ambiguity` 六步骨架**（`blueprint_ambiguity_score.py:360-458`）：
- 全 keyword-only；重依赖**全在函数内 lazy import**（`langchain_core.messages` / `agents.call_source` 的 `CallSource, use_call_source` / `build_chat_model` / `ProviderConfigService` / `common.logging.redact_secrets_in_text`）；
- `started = time.monotonic()` → emit `blueprint_review_goal_backward_started`（`category="sampling"`、`component="process_runtime"`，**只记计数与布尔**：`feature_point_count` / `impl_item_count` / `has_must_haves` / `has_key_links`，**正文不进日志**）；
- `ProviderConfigService.aresolve()` 取 `extra.default_model`（**与起草同档，不强制换模型**，§12 已定），**缺则 warning + `return None`**（不 raise）；
- `build_chat_model(resolved, model_name, streaming=False)` → `with use_call_source(CallSource.BLUEPRINT_AI_REVIEW): await model.ainvoke(messages)`；
- 解析走本模块内 `_parse_object_json` / `_content_to_text`（照 `blueprint_ambiguity_score` 同名 helper 自写小副本，纯 stdlib），失败 → warning（`reason="unparsable_response"`）+ `return None`；
- 成功 → `normalize_review_findings(payload.get("findings"))`（LLM 原始输出**从不直接进业务**）→ emit `blueprint_review_goal_backward_completed`（带 `finding_count` / 三档分级计数 / `duration_ms`）→ `return findings`；
- 兜底 `except Exception as exc` → `logger.warning(..., error=redact_secrets_in_text(str(exc)), duration_ms=...)` + `return None`。
**独立 fresh context**：prompt 只喂 digest——内部 `_goal_backward_digest(...)` 照 `blueprint_merge._feature_point_digest`（:774）/ `_impl_items_digest`（:790）的裁剪投影构造，**不带任何起草/融合会话历史**（CONTEXT 锁定，降相关性偏差），总长受 `_MAX_PROMPT_CHARS` 约束。docstring 写明「`None` 的语义是**『goal-backward 不可得』**，上游必须 fail-closed（`normalize_review_findings(None)` 会产一条 warning meta finding），**绝不当作『无问题』放行**；机械六类照常跑，审查不因 LLM 挂掉而空转」。
  </action>
  <verify>
    <automated>cd server && uv run python -c "from services.process_runtime.blueprint_review import run_mechanical_rules, check_api_closure, check_prohibitions, check_charters, check_gate_lock; print(run_mechanical_rules(None)); print(check_charters({}, charters=None))"</automated>
  </verify>
  <acceptance_criteria>
- `rg -n "def check_api_closure|def check_prohibitions|def check_charters|def check_gate_lock|def run_mechanical_rules|async def agoal_backward_review" server/services/process_runtime/blueprint_review.py` 六条全命中。
- `rg -n '"consumed"|"provided"' server/services/process_runtime/blueprint_review.py` 命中；`rg -n '"produced"' server/services/process_runtime/blueprint_review.py` **零命中**（枚举纠偏）。
- `rg -n "deferred_ideas" server/services/process_runtime/blueprint_review.py` 命中（out_of_scope 扫描排除该段）。
- `rg -n "draft_content" server/services/process_runtime/blueprint_review.py` **零命中**（规则⑥只读正式字段）。
- `rg -n "_normalize_locked_repos" server/services/process_runtime/blueprint_review.py` 命中（确认门基线复用既有投影，未自写第二套）。
- `rg -n "CallSource.BLUEPRINT_AI_REVIEW" server/services/process_runtime/blueprint_review.py` 命中；`git diff server/agents/call_source.py` 输出为空（**零新增枚举值**）。
- `rg -n "use_call_source|build_chat_model|ProviderConfigService" server/services/process_runtime/blueprint_review.py` 三者全命中且**均在 `async def agoal_backward_review` 函数体内**（`rg -n` 行号 > `agoal_backward_review` 定义行号，人工核对并在 SUMMARY 登记）。
- `cd server && uv run python -c "import ast,sys
src=open('services/process_runtime/blueprint_review.py').read()
tree=ast.parse(src)
top=[n for n in tree.body if isinstance(n,(ast.Import,ast.ImportFrom))]
names=[getattr(n,'module','') or '' for n in top]
bad=[m for m in names if m.startswith(('delivery.models','repositories.models','django'))]
assert not bad, bad
print('top-level import clean:', names)"` 通过（顶层零 ORM / 零 Django import）。
- `cd server && uv run python -c "from services.process_runtime.blueprint_review import normalize_review_findings as n
out = n(None)
assert len(out) == 1 and out[0]['severity'] == 'warning' and out[0]['rule_id'] == 'goal_backward_unavailable', out
print('fail-closed OK')"` 输出 `fail-closed OK`（LLM 不可得 ⇒ warning meta finding，不是空列表）。
- `cd server && uv run python -c "from services.process_runtime.blueprint_review import run_mechanical_rules as r
for bad in (None, {}, [], 'x', 123):
    assert isinstance(r(bad), list)
print('no-raise OK')"` 输出 `no-raise OK`。
- `cd server && uv run ruff check services/process_runtime/blueprint_review.py && uv run ruff format --check services/process_runtime/blueprint_review.py` 通过。
- `git diff --name-only` 为空（除新建文件外零改动；尤其 `builtin_processes.py` / `blueprint_resume.py` / `call_source.py` 无改动）。
  </acceptance_criteria>
  <done>六类规则齐备且每条的判定档位（BLOCKER vs WARNING）与 RESEARCH 的可证伪度分档一致；确认门锁定校验复用既有投影做基线；总入口确定性且前置不全时短路；goal-backward 走 `blueprint_ai_review` 且不可得时 fail-closed 成 warning meta finding。</done>
</task>

<task type="auto">
  <name>Task 3: 纯函数测试（六类各一条证伪样例 + 空蓝图不假通过 + 三条陷阱正反并列 + 恒不抛 + LLM 降级）</name>
  <files>server/tests/services/process_runtime/test_blueprint_review_rules.py</files>
  <read_first>server/tests/services/process_runtime/test_blueprint_reconcile.py L1-40（**「守 N 件事」编号清单 docstring 的确切体例** + 「零 DB、零 mock」首行声明 + 模块级期望常量 + 正负并列条目的写法）、L40-120（构造 dict 样例的粒度：最小可判定样例而非完整蓝图）；server/tests/services/process_runtime/test_blueprint_merge_gate.py L1-40（若有 `SettingKeys` / monkeypatch 体例可参考；本文件**不需要 django_db**）</read_first>
  <action>
新建 `server/tests/services/process_runtime/test_blueprint_review_rules.py`。首行 docstring 声明「**零 DB、零 mock**：六类机械规则是纯函数，全部断言直接喂 dict」+「守十二件事」编号清单，每条把**可证伪断言直接写进条目**（不是「测了规则③」而是「`rule_id == 'role_mismatch'` 且 `severity == 'blocker'`」）。**不加 `pytest.mark.django_db`**（唯一例外是第 11 条的枚举等值断言，它只 import `delivery.models` 的枚举类、不触库——若 import 需要 Django settings 已配，pytest-django 已在 conftest 完成 setup，无需 db 标记）。

模块级最小样例工厂：`_blueprint(**overrides) -> dict`（返回一份**六段齐全、无缺陷**的最小合法蓝图：`schema_version="blueprint/v1"` + 一个 direct 仓 + 一条实现项 + 一个 feature_point + 一条 api_contract + 一条 interaction_flow + `citations` 池齐备），各用例在其上**注入单一缺陷**——这保证「断言非恒真」（无缺陷基线必须产零 BLOCKER）。

守的十二件事：

1. ⭐ **无缺陷基线零 BLOCKER**：`run_mechanical_rules(_blueprint())` 中 `severity == "blocker"` 的条数 == 0（**断言非恒真的地基**；WARNING 允许存在）。
2. ⭐ **空蓝图短路且绝不 pass**：`run_mechanical_rules({"schema_version": "blueprint/v1"})` → 恰好一条 `precondition_missing` BLOCKER，且**不含** `role_mismatch` / `api_ref_dangling`（证明短路生效，没有跑出一片假通过/假失败噪声）。
3. ⭐ **规则① `schema_version` 假通过防回归（正反并列）**：缺 `schema_version` 的半成品 → `check_schema` 产 `schema_version_missing` BLOCKER；同一 content 直接调 `validate_blueprint` 断言其**返回 `(True, None)`**（把 `blueprint_schema.py:809-810` 的 pass-through 语义写进测试，防将来有人「简化」掉自断言）。另一条：`schema_version` 正确但结构非法 → `schema_invalid` BLOCKER 且 `detail` 非空。
4. **规则② 缺引用**：关键结论条目（`current_state_analysis[].findings[]`）`citations: []` → `citation_missing` BLOCKER；同一样例断言 `citation_coverage(content) < 1.0`（口径一致性）；**并列**：三类条目全空的空文档 `citation_coverage == 1.0` 但 `check_citations` **不据此判 pass**（走第 2 条短路）。
5. **规则③ 角色不一致（三档并列）**：direct 仓零实现项 → `role_mismatch` BLOCKER；item 指向 indirect 仓 → `role_mismatch` BLOCKER；indirect 仓 `capabilities_used` 未被引用 → `capability_unreferenced` **WARNING**（断言 `severity == "warning"`，锁死「唯一模糊项不升 BLOCKER」）。
6. **规则④ API 断链（两条 + 枚举防回归）**：`steps[].api_ref` 指向不存在的契约 id → `api_ref_dangling` BLOCKER；`consumed` + `availability="needs_support"` 缺 `support_repository_id` → `support_repo_missing` BLOCKER；⭐ **并列**：把同一条契约的 `direction` 写成 `"produced"` 时该契约**不被当作 consumed 处理**（断言不产 `support_repo_missing`）——这条锁死「实现里若误用 `produced` 就会恒通过」的回归面。
7. **规则⑤ 排期禁令**：block 文本含「预计 3 周完成」→ `forbidden_schedule` BLOCKER，且 `detail` 长度 ≤ 80+ 前缀（不贴整块正文）；`week` 大小写不敏感各一条。
8. ⭐ **规则⑤ `deferred_ideas` 不误报**：把 out_of_scope 词条**只**写在 `deferred_ideas` 段 → 不产 `out_of_scope_introduced`；把同一词条写进 `implementation_overview.items` 的 block 文本 → 产 `out_of_scope_introduced` WARNING（**正反并列**，杜绝「排除段没生效还看起来通过」）。
9. **规则⑤ constraint 悬空**：`rationale.constraint_refs` 引用不存在的 constraint id → `constraint_ref_dangling` BLOCKER。
10. ⭐ **规则⑥ 章程三态**：`charters=None` → `[]`（跳过）；某 direct 仓 `evolution="maintenance_only"` 且 `decision_log` 无支撑 → `charter_violation` BLOCKER；**同仓补一条 `decision_log` 支撑条目后 → 该 BLOCKER 消失**（证明「有决策记录支撑即放行」这条语义真的生效，而不是恒判违章）。另断言「章程 dict 里没有该仓（缺章程）→ 不判 BLOCKER」。
11. **确认门锁定校验 + 枚举等值**：`locked_snapshot` 给一份基线，当前 content 把某仓 `role` 从 direct 改成 indirect / 把 `responsibility` 文本改写 / 整条移除 → 各产一条 `gate_lock_violation` BLOCKER 且 `block_id == f"blk_gate_resp_{rid}"`；未改动时零命中。⭐ 同用例断言 `SEVERITY_BLOCKER == ThreadSeverity.BLOCKER` / `SEVERITY_WARNING == ThreadSeverity.WARNING` / `SEVERITY_INFO == ThreadSeverity.INFO`（字面量与枚举防漂移，沿用 `test_stage_status_table_matches_enum` 的同款纪律），并断言 `STAGE_STATE_KEY == "ai_review"` 且 `STAGE_STATE_KEY != blueprint_merge.STAGE_STATE_KEY`（**绝不复用 merge 桶**，P2 的第一道门）。
12. ⭐ **恒不抛 + 确定性 + LLM 降级**：`None` / `{}` / `[]` / `"x"` / 类型错乱（`repo_associations` 是 str、`items` 是 dict）喂全部公开函数 → 均返回 list 不抛；同一 content 连调 `run_mechanical_rules` 两次结果**逐字相等**（含顺序）；`normalize_review_findings(None)` → 恰好一条 `goal_backward_unavailable` **WARNING**（不是 `[]`、不是 BLOCKER）；`normalize_review_findings([{"rule_id": "x", "severity": "fatal"}])` → severity 回落 `warning`；超 `_MAX_FINDINGS` 的输入被截断到上界。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/process_runtime/test_blueprint_review_rules.py -x -q</automated>
  </verify>
  <acceptance_criteria>
- `cd server && uv run pytest tests/services/process_runtime/test_blueprint_review_rules.py -q` 全绿。
- `rg -c "def test_" server/tests/services/process_runtime/test_blueprint_review_rules.py` ≥14。
- 六类证伪样例的 rule_id 全部出现在测试里：`rg -n "citation_missing|role_mismatch|api_ref_dangling|forbidden_schedule|gate_lock_violation|charter_violation" server/tests/services/process_runtime/test_blueprint_review_rules.py` 六个 token 各 ≥1 命中。
- `rg -n "precondition_missing" server/tests/services/process_runtime/test_blueprint_review_rules.py` 命中（空蓝图不假通过）。
- `rg -n '"produced"' server/tests/services/process_runtime/test_blueprint_review_rules.py` 命中（枚举防回归用例存在）。
- `rg -n "deferred_ideas" server/tests/services/process_runtime/test_blueprint_review_rules.py` 命中（误报防护的正反并列存在）。
- `rg -n "ThreadSeverity" server/tests/services/process_runtime/test_blueprint_review_rules.py` 命中（字面量与枚举等值断言）。
- `rg -n "django_db" server/tests/services/process_runtime/test_blueprint_review_rules.py` **零命中**（纯函数测试零 DB）。
- `cd server && uv run pytest tests/services/process_runtime/ tests/delivery/ -q` 无新增失败（111/112/113 既有断言全绿）。
- `cd server && uv run python manage.py makemigrations --check --dry-run` 退出码 0（**零 migration**）。
- `cd server && uv run ruff check services/process_runtime/blueprint_review.py tests/services/process_runtime/test_blueprint_review_rules.py && uv run ruff format --check services/process_runtime/blueprint_review.py tests/services/process_runtime/test_blueprint_review_rules.py` 通过。
- 冻结面自检：`git diff --name-only` 为空（本 plan 全部产物为两个新建文件）。
  </acceptance_criteria>
  <done>六类规则各有一条可证伪样例且无缺陷基线零 BLOCKER（断言非恒真）；空蓝图短路成 BLOCKER 而非 pass；三条已知陷阱（`schema_version` 假通过 / `produced` 枚举 / `deferred_ideas` 误报）各有正反并列断言；severity 字面量与 `ThreadSeverity` 枚举等值且 `STAGE_STATE_KEY` 不等于 merge 桶；恒不抛与确定性被锁死。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `merge` 产出的蓝图 content → 审查判定 | 半可信 LLM 装配产物（可能夹带凭证样本、可能整段缺失），是本模块唯一输入 |
| 机械规则结论 → 打回决策 | BLOCKER 的有无直接决定「打回 repo_plan/merge」还是「升人审」，误判两端都有代价 |
| LLM goal-backward 输出 → finding 列表 | 模型可返回任意 JSON，绝不能直接进业务 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-114-07 | Tampering | 半成品蓝图靠 `validate_blueprint` 的 v0 pass-through「假通过」规则① | mitigate | `check_schema` 先自断言 `schema_version == BLUEPRINT_SCHEMA_VERSION`；Task 3 第 3 条把 pass-through 语义写进正反并列断言 |
| T-114-08 | Tampering | 空 / 半成品蓝图让规则③④⑤⑥的集合运算恒真 ⇒ 一片假阳性通过 | mitigate | `check_preconditions` 三段非空判定 + `run_mechanical_rules` 短路；Task 3 第 2 条断言恰好一条 `precondition_missing` |
| T-114-09 | Spoofing | `direction` 误写 `produced` / 只看 `citation_coverage` 比率 ⇒ 规则形同虚设但测试「看起来绿」 | mitigate | 模块 docstring 第 4 段纠偏 + `rg '"produced"'` / `rg 'citation_coverage'` 零命中验收 + Task 3 第 4/6 条正反并列 |
| T-114-10 | Info Disclosure | finding `detail` 夹带蓝图正文（可能含凭证样本）外泄进日志 | mitigate | `detail` 截断 `_MAX_DETAIL_CHARS`；排期命中只带 ≤80 字符片段；日志与事件只记计数与分级分布；LLM 异常文本走 `redact_secrets_in_text` |
| T-114-11 | Elevation of Privilege | LLM 不可得被当成「无问题」⇒ goal-backward 一类静默失效、蓝图带缺陷升人审 | mitigate | `agoal_backward_review` 返 `None` + `normalize_review_findings(None)` 产 warning meta finding；Task 3 第 12 条断言不是 `[]` |
| T-114-12 | Denial of Service | 无界 findings 把 HITL 面板刷爆 / 无界 prompt 打爆上下文 | mitigate | `_MAX_FINDINGS` + `_append` 有界追加 + `_MAX_DETAIL_CHARS` + digest 投影受 `_MAX_PROMPT_CHARS` 约束 |
| T-114-13 | Tampering | 本模块直写 ORM 绕过 INV-6 | mitigate | 顶层零 ORM / 零 Django import（AST 断言）+ `objects.` 零命中验收 + `test_blueprint_inv6_guard` 保持绿 |
| T-114-SC | Tampering | 依赖供应链 | accept | 零新增外部依赖（`re` / `difflib` / `json` 均 stdlib，LLM 通道走既有 `build_chat_model`）、零 migration |
</threat_model>

<verification>
- `cd server && uv run pytest tests/services/process_runtime/ -q` 全绿（113 的 reconcile / merge / stage 断言一并背书）。
- `cd server && uv run pytest tests/delivery/ -q` 无新增失败（114-01 的线程底座测试仍绿）。
- `cd server && uv run python manage.py makemigrations --check --dry-run` 退出码 0（**零 migration**）。
- `cd server && uv run ruff check services/process_runtime/blueprint_review.py tests/services/process_runtime/test_blueprint_review_rules.py` 与 `ruff format --check` 同两文件均通过。
- 顶层零 ORM 自检（AST，见 Task 2 验收）通过；`rg -n "objects\.|record_answer" server/services/process_runtime/blueprint_review.py` 零命中。
- 冻结面 + 受限面自检：`git diff --name-only` **为空**（本 plan 只新增两个 untracked 文件）；`git status --porcelain` 中新增项仅 `server/services/process_runtime/blueprint_review.py` 与 `server/tests/services/process_runtime/test_blueprint_review_rules.py`。特别核对 `builtin_processes.py` / `blueprint_resume.py` / `blueprint_quality.py` / `blueprint_schema.py` / `blueprint_lifecycle_service.py` / `agents/call_source.py` / `event_taxonomy.py` / `task/` 全部零改动。
</verification>

<success_criteria>
- 六类机械规则可在**无 LLM、无 DB、无网络**下对同一蓝图产出逐字相同的分级 findings，每类有一条构造样例证伪、且无缺陷基线零 BLOCKER（断言非恒真）。
- 三条既有假通过陷阱（`schema_version` v0 pass-through / `citation_coverage` 分母为 0 / `direction` 枚举）在实现与测试两侧都被堵死。
- 空 / 半成品蓝图走短路出 BLOCKER，绝不产出 `[]` 让编排层误判「审查通过」。
- goal-backward 一类是唯一 LLM 调用点，用已注册的 `blueprint_ai_review`、独立 fresh context、不可得时 fail-closed 成 warning meta finding。
- 114-03 可以直接消费四个稳定契约：`run_mechanical_rules(content, charters=, locked_snapshot=)`、`agoal_backward_review(...)`、`normalize_review_findings(raw)`、`finding_dedupe_key(finding)` 与 `STAGE_STATE_KEY`。
</success_criteria>

<output>
完成后创建 `.planning/phases/114-ai/114-02-SUMMARY.md`（provides：`blueprint_review.py` 的 `__all__` 全清单与每个公开函数的**逐字签名**、finding dict 六键与**全部 rule_id 枚举清单**（114-03 落线程、114-05 呈现要按此对齐）、每个 rule_id 的 severity 档位表、`STAGE_STATE_KEY` 值、`SEVERITY_*` 常量、`normalize_review_findings(None)` 的 meta finding 确切 `rule_id` 与文案、`finding_dedupe_key` 的键构造规则、`agoal_backward_review` 的完整签名与 `None` 语义、digest 投影所含字段、`_MAX_FINDINGS` / `_MAX_DETAIL_CHARS` 实际取值、以及「本 plan 零既有文件改动」的 `git diff --name-only` 空输出证据）。
</output>
