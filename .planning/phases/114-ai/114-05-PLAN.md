---
phase: 114-ai
plan: 05
type: execute
wave: 5
depends_on: ["114-04"]
files_modified:
  - server/delivery/api/blueprint_review_views.py
  - server/delivery/urls.py
  - server/delivery/services/blueprint_review_action.py
  - server/services/process_runtime/blueprint_quality.py
  - server/system/models.py
  - server/tests/delivery/test_blueprint_review_views.py
  - server/tests/services/test_blueprint_quality.py
autonomous: true
requirements: [FLOW-07, CLAR-03, CLAR-04]
prohibitions:
  - "禁止修改 §13.2 冻结面（`git diff --name-only` 零命中）：`process_runtime/{decompose_segments,research_adapter,architect_merge_adapter,clarify_adapter,render,resume}.py`、delivery 侧 `merged_plan.py`、`repositories/services/charter_service.py`"
  - "禁止修改 `task/core/knowledge_tools.py` 公共 handler 工厂（本相位不碰 `task/`）"
  - "🔒 本 plan **一行都不改** `builtin_processes.py`（stage 注册与 handler 已在 114-03 定稿）；`_TECHNICAL_PLAN_STAGES` / `_ECHO_STAGES` 永不触碰"
  - "🔒 本 plan **一行都不改** `server/services/process_runtime/blueprint_resume.py`（映射表追加已在 114-03 完成，**删除行上界 0** 的纪律持续有效；pending 提醒**不在**该文件加新函数——挂载点放在本 plan 的新模块 + 只读端点上）"
  - "⛔ **`record_answer` 禁用于 finding 留痕**：本 plan 唯一允许使用 `record_answer` 的地方是 **`threads/<uuid>/answer/` 端点（人类回答澄清线程）**——那是它的正当用法。`rg -n \"record_answer\" server/delivery/services/blueprint_review_action.py server/services/process_runtime/blueprint_quality.py` 必须**零命中**；`blueprint_review_views.py` 中 `record_answer` 只允许出现在 answer 端点的 View 内（源码扫描断言其不出现在 approve/reject/edit-blocks 三个 View 的函数体内）"
  - "🔒 `blueprint_lifecycle_service.py` **零改动**（114-01/114-04 已用完全部配额）：`git diff server/delivery/services/blueprint_lifecycle_service.py` 输出必须**为空**"
  - "🔒 `blueprint_quality.py` 的改动**严格限于三个占位函数体的实装 + `human_edit_volume` 的口径 docstring 修正**：删除行上界 **≤ 10**（三处 `# TODO` 注释 + 三处 `return None` + `human_edit_volume` docstring 的口径两行），逐行登记；**保持三个函数的同步签名不变**（`evaluate_blueprint_golden` 离线评估在调）；顶层**保持零 ORM import**（ORM 走函数内懒 import，`:11` 已立此纪律）；`citation_coverage` / `target_repo_hit_rate` / `_cited` / `_iter_key_conclusion_citations` **一行不改**。本文件只跑 `ruff check`，**不跑 `ruff format`**"
  - "🔒 `system/models.py` **只允许在 `BLUEPRINT_REVIEW_CONFIG` 的既有注释块追加一行**（补 `pending_reminder_hours` 键说明）：删除行 **0**，只跑 `ruff check`；**禁止新增 SettingKeys 键**（复用 114-03 已加的 `blueprint.review.config`）；禁止改 `server/system/settings_service.py` 既有 getter"
  - "禁止修改 `blueprint_schema.py` / `blueprint_anchor.py` / `blueprint_merge.py` / `blueprint_reconcile.py` / `blueprint_confirm_gate.py` / `blueprint_spec_gate.py` / `blueprint_repo_plan.py` / `blueprint_review.py` / `blueprint_reflow.py` / `blueprint_block_edit.py` / `artifact_service.py` / `event_taxonomy.py`（受限面，本 plan 只读消费；新逻辑一律进本 plan 的两个新文件）"
  - "禁止改 `blueprint_gate_views.py`（112 的八端点文件）：本相位端点一律进新建 `blueprint_review_views.py`，URL 前缀 `blueprint-review/`（与 `blueprint-gate/` 区分）"
  - "禁止视图裸写 ORM（INV-6）：五端点的写入全部委托 `BlueprintLifecycleService` / `ArtifactService` / 本 plan 的 `blueprint_review_action` service；读路径允许视图直查；serializer `.data` 一律 `sync_to_async` 包裹"
  - "⛔ **approve 端点禁止在事务外补「未决 BLOCKER」二次查询**（会重新打开 TOCTOU 窗口）：守卫判据已由 114-01 收敛进 `_apply_transition_sync` 的同一 `transaction.atomic()` 单次 `Q` 查询；视图只 `transition` 并按异常映射状态码。`aunresolved_blocker_count` **只允许**用于 GET 只读快照的呈现，且不得与 `to_status=\"confirmed\"` / `BlueprintStatus.CONFIRMED` 出现在同一个 View 内"
  - "禁止扩 `_apply_transition_sync` 加 `revision_round`：驳回一律走「读 current content → `meta.revision_round += 1` → `add_version` → `transition(\"drafting\")`」三步，**先落版本再转状态**；重试前必须**重读 current content** 而非用内存副本"
  - "禁止同步 `rest_framework.views.APIView`：一律 `from adrf.views import APIView`（异步）；权限一律 `permission_classes = [IsAuthenticated]`；不存在类错误用**中性 404 消息常量**（不泄露资源存在性）"
  - "禁止发明 action 分派参数：一动作一 View（112 已立的边界）"
  - "禁止在视图里重复包续驱 try / 因续驱失败改响应码：续驱失败隔离在 `blueprint_resume.aresume_after_gate_action` 内；GET 只读端点**不接续驱**"
  - "禁止 `human_edit_volume` 用 `created_by_user_id`（`ArtifactVersion` **无该字段**，会直接 `FieldError`）：必须用 `produced_by_ref__startswith=\"human_edit:\"`，并把 111 的口径 docstring 同步修正"
  - "禁止三项统计在无数据时返回 0：必须继续返回 `None`（否则 `evaluate_blueprint_golden` 会把「没数据」当成「零打回」）"
  - "禁止为 pending 提醒新起 OS 级注册（cron / apscheduler job）：周期走 `SettingKeys.BLUEPRINT_REVIEW_CONFIG`，挂载点在只读端点与 service 调用路径上（同 113「超时清理挂在续驱路径、不新起定时任务」）；提醒**不自动作答、不判失败**"
must_haves:
  truths:
    - "新建 `server/delivery/api/blueprint_review_views.py` 五端点，URL 前缀 `artifacts/<uuid:artifact_id>/blueprint-review/`：GET 只读快照 / POST `approve/` / POST `reject/` / POST `edit-blocks/` / POST `threads/<uuid:thread_id>/answer/`，`name` 为 `blueprint-review-*`"
    - "⭐ 五端点**未认证一律拒**（`IsAuthenticated`）：无凭证请求全部 401/403，无一例外"
    - "⭐ approve 有未决 BLOCKER → **409**，清空后 → 200 且 DB 重读 `artifact.blueprint_status == \"confirmed\"`；视图**零事务外二次查询**（守卫判据在 `_apply_transition_sync` 的单一事务内，源码扫描断言）"
    - "⭐ approve 的并发安全：`ConcurrentBlueprintTransitionError` → 409（不是 500、不是静默成功）；`ValueError`（非法转移）→ 409 且 DB 状态不变"
    - "⭐ reject → 版本数 +1 且新版本 `content[\"meta\"][\"revision_round\"]` 比旧值 +1 且 DB 重读状态为 `drafting`；顺序为**先落版本再转状态**（本相位是 `revision_round` 的首个写入方）；带划线评论时开一条 `kind=human_comment` 线程"
    - "reject 幂等：重试时重读 current content（不用内存副本），同一驳回连提两次不会把 `revision_round` 连加两次"
    - "`edit-blocks/` 经 114-04 的 `aapply_block_edit` 收口：不合法 → 400/409 且**版本数不变**；合法 → 200 且 `produced_by_ref == f\"human_edit:{user_id}\"`；编辑者进 `BlueprintReviewer`"
    - "`threads/<uuid>/answer/` 用 `record_answer`（**此处才是它的正当用法**）；作答后可经 114-04 的回灌链消费"
    - "改状态的端点在**持久化成功之后**调 `blueprint_resume.aresume_after_gate_action`；GET **不接**续驱；续驱抛异常 → 端点仍 2xx 且动作已持久化（失败隔离）"
    - "reviewer upsert：approve / reject / edit-blocks / answer 的操作者一并 `add_reviewer(artifact, user, first_action=...)`（`aget_or_create`，已在名单原样返回、`first_action` 不覆盖）"
    - "⭐ `blueprint_quality` 三项 DB 统计实装且**保持同步签名**：`ai_rejection_rate` 按 `ConvergenceSessionEvent` 的 `blueprint.review.*` / `blueprint.stage.*` 事件算（打回轮次 / 审查总轮次）；`human_edit_volume` **用 `produced_by_ref__startswith=\"human_edit:\"`**（`created_by_user_id` 字段不存在，口径 docstring 同步修正）；`clarification_rounds` 按 `BlueprintThread` + `BlueprintThreadMessage`（`author_type=human` 的消息数）算"
    - "⭐ 三项统计**无数据时返回 `None` 而不是 0**（`evaluate_blueprint_golden` 会把 0 当成「零打回」）；顶层保持零 ORM import（函数内懒 import）"
    - "pending 超时提醒挂载点存在：`amaybe_remind_pending_review(artifact, *, initiated_by_user_id)` 按 `SettingKeys.BLUEPRINT_REVIEW_CONFIG.pending_reminder_hours`（缺配置回落常量）判到期，产出**提醒对象名单 = `BlueprintReviewer` 名单 + 发起人**并记 `caller` 事件/日志；**不自动作答、不判失败、不新起 OS 级注册**"
    - "全量相位门：`pytest tests/ -q` 无新增失败 + `makemigrations --check` 退出码 0 + 冻结面 `git diff --name-only` 只含本 plan 声明文件"
  artifacts:
    - path: "server/delivery/api/blueprint_review_views.py"
      provides: "五端点（GET 快照 / approve / reject / edit-blocks / threads answer），adrf APIView + IsAuthenticated + 视图零 ORM 写 + 404/400/409 分层 + 续驱接线"
      contains: "blueprint-review"
    - path: "server/delivery/services/blueprint_review_action.py"
      provides: "aapprove_blueprint / areject_blueprint（revision_round 三步链 + 划线评论线程）/ amaybe_remind_pending_review（pending 超时提醒挂载点）"
      contains: "revision_round"
    - path: "server/services/process_runtime/blueprint_quality.py"
      provides: "ai_rejection_rate / human_edit_volume / clarification_rounds 三项 DB 统计实装（同步签名不变，懒 import ORM，无数据返 None）"
      contains: "produced_by_ref__startswith"
    - path: "server/tests/delivery/test_blueprint_review_views.py"
      provides: "五端点鉴权 + approve 409/200 + 无 TOCTOU 源码扫描 + reject 版本先于状态 + 不合法编辑不落版本 + 同 hash 不翻版本 + reviewer upsert + 视图零 ORM 写扫描 + 续驱正反与失败隔离"
      contains: "409"
  key_links:
    - from: "server/delivery/api/blueprint_review_views.py"
      to: "server/delivery/services/blueprint_lifecycle_service.py"
      via: "transition(artifact, \"confirmed\", acting_user=request.user, ...)——守卫 + CAS + reviewer upsert 同事务，视图不加事务外二次查询"
      pattern: "transition"
    - from: "server/delivery/api/blueprint_review_views.py"
      to: "server/services/process_runtime/blueprint_resume.py"
      via: "aresume_after_gate_action（改状态端点在持久化之后调；失败隔离在 helper 内）"
      pattern: "aresume_after_gate_action"
    - from: "server/services/process_runtime/blueprint_quality.py"
      to: "server/delivery/models/artifact.py"
      via: "ArtifactVersion.objects.filter(produced_by_ref__startswith=\"human_edit:\")（函数内懒 import；口径 docstring 已修正）"
      pattern: "human_edit:"
---

<objective>
把人审操作面与度量面闭环，收口整个相位：

① 新建 `blueprint_review_views.py` 五端点（前缀 `blueprint-review/`，与 112 的 `blueprint-gate/` 区分）：GET 只读快照（findings + 线程 + 失锚列表）、`approve/`（→ `confirmed`，**直接依赖 114-01 的事务内单查询守卫，视图零二次查询**）、`reject/`（→ `drafting` 且 `meta.revision_round + 1`，**本相位是该字段首个写入方**）、`edit-blocks/`（委托 114-04 的 `aapply_block_edit`）、`threads/<uuid>/answer/`（**`record_answer` 唯一正当用法**）；
② 新建 `blueprint_review_action.py`：`areject_blueprint` 的三步链（读 current content → bump → `add_version` → `transition`）+ reviewer upsert + **pending 超时提醒挂载点**（周期可配、对象 = `BlueprintReviewer` 名单 + 发起人、不自动作答不判失败、不新起定时任务）；
③ `blueprint_quality` 三项 DB 统计**首次实装**（111 留的占位本相位首次有真实数据），其中 `human_edit_volume` 必须用 `produced_by_ref__startswith="human_edit:"`——111 docstring 写的 `created_by_user_id` **在 `ArtifactVersion` 上根本不存在**，照 docstring 写会 `FieldError`，而若"绕开"改成按用户过滤别的字段则指标恒为零；
④ 全量相位门：五端点鉴权/状态码分层/续驱正反与失败隔离/视图零 ORM 写扫描 + 相位级 `pytest tests/ -q`、`makemigrations --check`、冻结面与受限面 diff 核算。

Purpose: FLOW-07 的人审出口 + CLAR-03 的编辑入口 + CLAR-04 的 pending 语义，都必须落在**经 service 收口、无 TOCTOU、无静默卡死**的端点上；三项统计是 v0.20 唯一能量化「AI 打回率 / 人审修改量 / 澄清轮次」的通道，写错字段名会让指标恒零而测试看起来全绿。
Output: 两个新建文件 + 三处受限修改 + 两个测试文件（一新一扩）。**无 migration、无新模型字段、无新 stage、无新 SettingKeys 键、无新事件常量。**
</objective>

<execution_context>
工作目录：worktree 根 `/Users/zaneliu/Projects/open-source/friday-clean/.claude/worktrees/v0.20-blueprint`。wave 5，依赖 114-04（`aapply_block_edit` / `aapply_thread_answers` / `areanchor_threads` 已就位）与 114-01（confirm 守卫已收敛进事务、`aunresolved_blocker_count` 可用于呈现）。

**受限面配额（硬约束，逐条登记）**：

| 文件 | 允许的改动 | 删除行上界 | formatter |
|---|---|---|---|
| `blueprint_quality.py` | 三个占位函数体实装 + `human_edit_volume` 口径 docstring 修正 + 函数内懒 import | **≤10** | 只 `ruff check` |
| `system/models.py` | `BLUEPRINT_REVIEW_CONFIG` 既有注释块**追加一行**（补 `pending_reminder_hours`） | **0** | 只 `ruff check` |
| `delivery/urls.py` | import 块追加一条 `from delivery.api.blueprint_review_views import (...)` + `urlpatterns` 追加 5 项 + 一行分组注释 | **0** | 只 `ruff check` |
| `blueprint_lifecycle_service.py` | **零改动**（`git diff` 必须为空） | — | — |

⚠️ 字段名与签名一律按 114-RESEARCH §5/§6 与 114-01/114-04-SUMMARY 实测，**不得猜测**：
- 112 端点七条惯例（`blueprint_gate_views.py:1-28` docstring 已立）：URL 形状 `artifacts/<uuid:artifact_id>/<前缀>/<动作段>/` 字面 kebab-case + `name="<前缀>-<动作>"`；一动作一 View；`from adrf.views import APIView`；`permission_classes = [IsAuthenticated]`；**视图零 ORM 写**、serializer `.data` 走 `sync_to_async`；错误码分层（不存在类 → 中性 404 常量 `_ARTIFACT_MISSING_DETAIL` / `_SESSION_MISSING_DETAIL` 同款、入参类 → 400、service 拒绝 → 409 + 码→中文映射）；改状态端点在**持久化之后**调 `blueprint_resume.aresume_after_gate_action`，失败隔离在 helper 内、视图不重复包 try、不因续驱失败改响应码，只读端点不接。
- `BlueprintLifecycleService.transition(artifact, to_status, *, initiated_by_user_id, acting_user=None, session=None, return_status=None)`（`:143`）；`ValueError` = 非法转移（DB 不写，`:169-173`）；`ConcurrentBlueprintTransitionError`（`:70`）= CAS 冲突 ⇒ **两类异常状态码分开**。
- 114-01 的 confirm 守卫：两条判据（无 `open+blocking` 线程 + 无未决 BLOCKER finding）已在 `_apply_transition_sync` 的**同一 `transaction.atomic()`** 内以**单次 `Q` 查询**完成；**新报错文案见 114-01-SUMMARY**（视图的 409 映射按该文案对齐，不猜）。
- `add_reviewer(artifact, user, first_action)`（`:215`，`aget_or_create`，已在名单原样返回、`first_action` 不覆盖）。
- `record_answer(thread, *, body, author=None, author_type=ThreadAuthorType.HUMAN, initiated_by_user_id="system")`（`:425`）——**只在 answer 端点使用**。
- `meta.revision_round`：`{"type": "integer", "minimum": 0}`，位于 content 的 `meta` 段（`blueprint_schema.py:160-164`），**不是模型字段**，全仓无写入方（A6，本相位首个写入方）。
- `ArtifactVersion` 无 `created_by_user_id`；只有 `produced_by_session_id`(64) / `produced_by_ref`(255)（`delivery/models/artifact.py:154-156`）。
- 三项统计签名（**保持不变**）：`ai_rejection_rate(artifact_id: str) -> float | None`（`blueprint_quality.py:111`）/ `human_edit_volume(artifact_id: str) -> int | None`（`:122`）/ `clarification_rounds(artifact_id: str) -> int | None`（`:132`）。
- 事件名：114-03 追加的 `blueprint.review.started/completed/failed`（`event_taxonomy`）+ 111/112 的 `blueprint.stage.started/completed/failed`；`ai_rejection_rate` 的分子分母口径按 114-03-SUMMARY 登记的 payload 键（`review_status` / `back_target`）对齐，**不猜 payload 形状**。
- `SettingKeys.BLUEPRINT_REVIEW_CONFIG`（114-03 已加，值 JSON）。
</execution_context>

<context>
@.planning/phases/114-ai/114-CONTEXT.md（锁定：人审操作端点属本相位且经 lifecycle 收口——通过 → `confirmed`、驳回 → `drafting` 且 `revision_round + 1`；`revision_round` 走「改 content → `add_version` → `transition`」三步不扩 `_apply_transition_sync`；confirm 守卫收进事务消除 TOCTOU；权限项目成员皆可编辑、编辑者与人审操作者一并 upsert 进 `BlueprintReviewer`；pending 超时保持 pending + 按可配周期提醒、对象为 `BlueprintReviewer` 名单 + 发起人、不自动作答不判失败；AI 打回率与人审修改量喂 `blueprint_quality` DB 统计接口）
@.planning/phases/114-ai/114-RESEARCH.md §5（三项统计的口径原文 + **A2 已知偏差**：`human_edit_volume` 的 docstring 写 `created_by_user_id` 但模型无该字段 → 必须改用 `produced_by_ref__startswith`，且要登记为「口径 docstring 同步修正」；三项无数据必须返 None 而非 0；保持同步签名 + 函数内懒 import）、§6.1（112 端点七条惯例表）、§6.2（本相位端点落位建议 + 五端点表 + `revision_round` 三步链与顺序要求与幂等注意 + **approve 不加事务外二次查询**）、Pitfalls P4（两类异常语义与状态码分层）
@.planning/phases/114-ai/114-PATTERNS.md 第 8 类（`blueprint_gate_views.py` 七条惯例逐条 + `_aapply_action` 三元组前置 helper 形状 + 改状态端点完整形状（fail-closed 409 + 续驱在持久化之后）+ 五端点表 + 「approve 不加事务外 BLOCKER 二次查询」）、第 10 类 Analog B（`test_blueprint_gate_api.py` 的「守十件事」：鉴权第 1 条 / 状态码分层 / **断言从 DB 重读不信响应体** / 续驱接线正反 + 失败隔离 / 视图零 ORM 写源码扫描 / 端到端证伪线不桩依赖且断言绝不静默落 FAILED）、No Analog Found 第 3 项（三项统计实装的全部约束）、第 4 项（pending 超时提醒：无定时任务先例、不新起 OS 级注册、挂载点建议、提醒对象）
@server/delivery/api/blueprint_gate_views.py（**主 analog，548 行**：L1-28 docstring 七条纪律、L30-65 import 与错误消息常量体例、L67-100 只读装配 helper（`_aload_artifact` / `_aload_session` 的 `process_type` 过滤理由）、L153-193 `_aapply_action` 三元组前置、L206-260 改状态端点的完整形状含 409 分层与续驱在持久化之后）
@server/delivery/urls.py（L1-45 import 块体例、L135-177 `blueprint-gate/` 八条路由的分组注释与 `name` 命名）
@server/services/process_runtime/blueprint_quality.py（**全文 141 行**：L1-14 模块 docstring 的两节声明与「顶层零 ORM import，未来实装走函数内懒 import」纪律、L105-141 三个占位函数的**确切签名与口径 docstring**）
@server/delivery/models/artifact.py（L66-128 `Artifact.current_version` / `blueprint_status`、L130-157 `ArtifactVersion` 全字段——**确认无 `created_by_user_id`**）
@server/delivery/services/blueprint_lifecycle_service.py（L143-214 `transition` 全文与两类异常、L215-228 `add_reviewer`、L229-279 `_apply_transition_sync`（114-01 改造后的单查询守卫）、L425-454 `record_answer`）
@server/services/process_runtime/blueprint_resume.py（L160-200 `aresume_after_gate_action` 的失败隔离契约——视图不重复包 try）
@server/tests/delivery/test_blueprint_gate_api.py（L1-23 「守十件事」编号清单 + 续驱桩掉的写法 + 视图零 ORM 写扫描 + SC-4 端到端证伪线）
@server/tests/services/test_blueprint_quality.py（L1-20 import 与体例、L89-95 `test_db_stat_placeholders_return_none`——**本 plan 必须改写这条**：实装后无数据仍返 None，但查询需要 DB ⇒ 加 `pytest.mark.django_db` 并重命名为「无数据返 None」，再补有数据的正向用例）
@.planning/phases/114-ai/114-01-SUMMARY.md（confirm 守卫的**新报错文案**与 `aunresolved_blocker_count` 签名——409 映射与 GET 呈现按它对齐）
@.planning/phases/114-ai/114-03-SUMMARY.md（`blueprint.review.*` 事件的 payload 键集——`ai_rejection_rate` 的分子分母按它算；`SettingKeys.BLUEPRINT_REVIEW_CONFIG` 的 value 形状）
@.planning/phases/114-ai/114-04-SUMMARY.md（`aapply_block_edit` / `aapply_thread_answers` 的恒定返回键与各 `status` 语义——端点的状态码映射按它对齐）
</context>

<tasks>

<task type="auto">
  <name>Task 1: blueprint_review_action service（approve/reject 三步链 + revision_round 首写 + reviewer upsert + pending 提醒挂载点）</name>
  <files>server/delivery/services/blueprint_review_action.py, server/system/models.py</files>
  <read_first>server/services/process_runtime/blueprint_confirm_gate.py L492-621（**五步固定顺序**：读最新版本作基线（不读 session 那一版）→ `deepcopy` → 改 content → `add_version` 的 `try/except ArtifactContentInvalid` fail-closed → 成功后才 `resolve_thread` + `add_reviewer`）；server/delivery/services/blueprint_lifecycle_service.py L143-214（`transition` 的**逐字签名**、`ValueError` 与 `ConcurrentBlueprintTransitionError` 的抛出点与语义、`acting_user` 触发的 reviewer upsert）、L215-228（`add_reviewer` 的 `aget_or_create` 幂等语义）；server/services/process_runtime/blueprint_schema.py L142-165（`meta` 段的**全部键与 `revision_round` 的 `type`/`minimum`**——bump 前必须容忍缺 `meta` 段与非 int 旧值）；server/services/process_runtime/blueprint_merge.py L1995-2017（`_aload_merge_config` 的「配置坏了回落常量、绝不阻断」写法——`pending_reminder_hours` 照此读）；server/system/models.py L186-192（`BLUEPRINT_REVIEW_CONFIG` 注释块的**确切位置与体例**——只允许追加一行）；server/delivery/models/blueprint_reviewer.py（`BlueprintReviewer` 的字段名与 `first_action` 语义——提醒名单从这里取）</read_first>
  <action>
**① `system/models.py` 追加一行注释**（删除行 0）：在 `BLUEPRINT_REVIEW_CONFIG` 既有注释块末尾追加
```
    # 114-05 复用同一键追加 {"pending_reminder_hours": int}：pending_review 超时提醒周期
    # （缺配置回落 _DEFAULT_PENDING_REMINDER_HOURS=24；不新起 OS 级定时任务）。
```
**不新增 SettingKeys 键**（复用 114-03 已加的 `blueprint.review.config`）。

**② 新建 `server/delivery/services/blueprint_review_action.py`**。模块 docstring 四段：① 「人审操作的 service 收口（INV-6）：视图零 ORM 写，通过/驳回/提醒的全部落库都在本模块经 `BlueprintLifecycleService` / `ArtifactService`」；② 「**通过（approve）不做事务外二次查询**：『无 open+blocking 线程 + 无未决 BLOCKER』两条判据已由 114-01 收敛进 `_apply_transition_sync` 的同一 `transaction.atomic()` 单次 `Q` 查询。视图/本模块**再查一次就是重新打开 TOCTOU 窗口**（P4）——本模块只调 `transition` 并把两类异常分开上报」；③ 「**驳回（reject）走三步**：读 current content → `meta.revision_round += 1` → `add_version` → `transition("drafting")`。**顺序不可换**：先转状态会留下『状态已 drafting 而轮次未加』的窗口，AI 会拿旧轮次重跑。重试前**重读 current content**（内存副本会让同 hash 幂等失效、轮次连加两次）。本相位是 `meta.revision_round` 的**首个写入方**（此前全仓无写入方）」；④ 「pending 提醒 best-effort：**不自动作答、不判失败、不新起 OS 级注册**——周期读 `SettingKeys.BLUEPRINT_REVIEW_CONFIG.pending_reminder_hours`，判到期后只产出提醒对象名单并记 `caller` 事件/日志；实际渠道投递（飞书卡片重推/站内通知）由 115/116 的通知面消费本事件，本相位不新建推送通道」。

三个公开入口 + 一个纯函数：
```
def bump_revision_round(content: Any) -> tuple[dict, int]
async def aapprove_blueprint(artifact, *, user=None, initiated_by_user_id="system", session=None) -> dict
async def areject_blueprint(artifact, *, user=None, comment="", anchor=None, initiated_by_user_id="system", session=None, artifact_service=None, lifecycle_service=None) -> dict
async def amaybe_remind_pending_review(artifact, *, initiated_by_user_id="system", session=None) -> dict
```
- `bump_revision_round`（**纯函数**）：`deepcopy` → `meta = content.setdefault("meta", {})`（容忍缺段/非 dict）→ 旧值非 int 或 < 0 时按 0 起算 → `meta["revision_round"] = old + 1` → 返回 `(new_content, new_round)`。恒不抛。
- `aapprove_blueprint`：恒定返回键 `{"status": "confirmed"|"blocked"|"conflict"|"invalid", "detail": str, "blueprint_status": str}`。实现：`try: await lifecycle.transition(artifact, BlueprintStatus.CONFIRMED, initiated_by_user_id=..., acting_user=user, session=session)` → `status="confirmed"`；`except ValueError as exc:` → `status="blocked"`，`detail` 取异常文本（**经 `redact_secrets_in_text`**；文案对齐 114-01-SUMMARY 登记的守卫报错）；`except ConcurrentBlueprintTransitionError:` → `status="conflict"`。成功后 `add_reviewer(artifact, user, "review_approve")`（`user` 为 None 时跳过；`transition` 的 `acting_user` 已做 upsert 时此调用幂等无副作用）+ `caller` 日志（`blueprint_review_approved`，含 `artifact_id` / `initiated_by_user_id` / `duration_ms`）。**全程零二次查询**。
- `areject_blueprint`：恒定返回键 `{"status": "rejected"|"unchanged"|"invalid"|"conflict", "version_id": str, "version_no": int, "revision_round": int, "thread_id": str, "detail": str}`。五步：
  1. **重读**最新版本作基线（`ArtifactVersion.objects.filter(artifact_id=...).order_by("-version_no").afirst()`；⚠️ 每次调用都重读，**不接受调用方传入的 content**）；无版本 → `invalid`；
  2. `new_content, new_round = bump_revision_round(base.content)`；
  3. `add_version(artifact, new_content, produced_by_session_id=..., produced_by_ref=f"blueprint_review_reject:{user_id}")`，`try/except ArtifactContentInvalid` → warning（脱敏）+ `invalid`（**不落半合法版本**）；
  4. `transition(artifact, BlueprintStatus.DRAFTING, initiated_by_user_id=..., acting_user=user, session=session)`，`ValueError` → `invalid` / `ConcurrentBlueprintTransitionError` → `conflict`（此时版本已落 —— 在返回体里如实带上 `version_no`，让端点能把「版本已落但状态未转」这一半成功状态告诉用户，绝不静默）；
  5. `comment` 非空 → `open_thread(artifact, kind=ThreadKind.HUMAN_COMMENT, blocking=False, severity="", question=comment, anchor=anchor, created_on_version=new_version, initiated_by_user_id=..., return_stage="drafting")`（划线评论；`severity` 传空 ⇒ 不受 114-01 的 finding 不变式约束）→ `add_reviewer(artifact, user, "review_reject")` → `caller` 日志（`blueprint_review_rejected`，含 `revision_round` / `version_no`；**评论正文绝不进日志**）。
- `amaybe_remind_pending_review`：恒定返回键 `{"due": bool, "hours": int, "recipients": list[str], "reminded": bool}`。实现：仅当 `artifact.blueprint_status == BlueprintStatus.PENDING_REVIEW`（或存在 open+blocking 线程的 `needs_clarification` 态，按 CLAR-04「blocking 澄清无人应答」口径二选一并在 docstring 写清）；到期判据 = `timezone.now() - artifact.updated_at >= timedelta(hours=hours)`，`hours` 读配置（照 `_aload_merge_config` 的整段 `try/except` 回落 `_DEFAULT_PENDING_REMINDER_HOURS = 24`）；`recipients` = `BlueprintReviewer` 名单的 user id ∪ `{artifact 关联会话的 initiated_by_user_id}`（只读查询）；到期则记一条 `caller` 结构化日志 + best-effort emit（复用 114-03 的 `blueprint.review.*` 事件之一或不 emit——**不新增事件常量**，若不 emit 则 docstring 写明「提醒只落日志与返回值，通知渠道由 115/116 消费」）。**不改状态、不作答、不判失败**；整体 `try/except` 吞掉（提醒失败绝不反噬人审）。
  </action>
  <verify>
    <automated>cd server && uv run python -c "from delivery.services.blueprint_review_action import bump_revision_round as b
for bad in (None, {}, {'meta': 'x'}, {'meta': {'revision_round': 'x'}}, {'meta': {'revision_round': 3}}):
    c, n = b(bad); print(type(c).__name__, n)
assert b({'meta': {'revision_round': 3}})[1] == 4
assert b({})[1] == 1
print('bump OK')"</automated>
  </verify>
  <acceptance_criteria>
- 上述 `bump OK` 命令通过（缺 `meta` 段 / `meta` 非 dict / 旧值非 int 全部安全起算，且 3 → 4）。
- `rg -n "def bump_revision_round|async def aapprove_blueprint|async def areject_blueprint|async def amaybe_remind_pending_review" server/delivery/services/blueprint_review_action.py` 四条全命中。
- ⭐ `rg -n "aunresolved_blocker_count|ahas_open_blocking_threads" server/delivery/services/blueprint_review_action.py` 在 `aapprove_blueprint` 函数体内**零命中**（approve 零事务外二次查询；只允许出现在提醒/呈现路径）。
- `rg -n "revision_round" server/delivery/services/blueprint_review_action.py` 命中；`rg -n "_apply_transition_sync" server/delivery/services/blueprint_review_action.py` **零命中**（不扩既有事务函数）。
- `rg -n "ConcurrentBlueprintTransitionError" server/delivery/services/blueprint_review_action.py` 命中（两类异常分开）。
- `rg -n "blueprint_review_reject:" server/delivery/services/blueprint_review_action.py` 命中（驳回版本的 `produced_by_ref` 前缀）。
- `rg -n "ThreadKind.HUMAN_COMMENT" server/delivery/services/blueprint_review_action.py` 命中（划线评论用枚举常量非字面量）。
- ⛔ `rg -n "record_answer" server/delivery/services/blueprint_review_action.py` **零命中**。
- `rg -n "BlueprintThread.objects.(create|acreate)|ArtifactVersion.objects.(create|acreate)|\.update\(" server/delivery/services/blueprint_review_action.py` **零命中**（INV-6：写入全经 service）。
- `rg -n "apscheduler|crontab|add_job" server/delivery/services/blueprint_review_action.py` **零命中**（不新起 OS 级注册）。
- `git diff server/system/models.py | rg "^-[^-]"` **为空**（只追加注释行）；`git diff server/system/models.py | rg "^\+" | rg -c "BLUEPRINT_REVIEW_CONFIG ="` == 0（**不新增键**，只补注释）。
- `git diff server/delivery/services/blueprint_lifecycle_service.py` 输出**为空**（本 plan 零改动）。
- `cd server && uv run pytest tests/delivery/ tests/test_blueprint_settings.py -q` 无新增失败。
- `cd server && uv run python manage.py makemigrations --check --dry-run` 退出码 0。
- `cd server && uv run ruff check delivery/services/blueprint_review_action.py system/models.py && uv run ruff format --check delivery/services/blueprint_review_action.py` 通过（**不对 `system/models.py` 跑 format**）。
  </acceptance_criteria>
  <done>approve 只调 `transition` 且两类异常分开（零事务外二次查询）；reject 的三步链顺序固定、每次重读 current content、`revision_round` 首写成立、半成功状态如实上报；划线评论经 `open_thread(human_comment)`；pending 提醒挂载点周期可配、名单为 reviewer ∪ 发起人、不作答不判失败不新起定时任务。</done>
</task>

<task type="auto">
  <name>Task 2: 五端点 REST（GET 快照 / approve / reject / edit-blocks / threads answer）+ urls 追加 + blueprint_quality 三项 DB 统计实装</name>
  <files>server/delivery/api/blueprint_review_views.py, server/delivery/urls.py, server/services/process_runtime/blueprint_quality.py</files>
  <read_first>server/delivery/api/blueprint_gate_views.py L1-100（docstring 七条纪律 + import 块 + 错误消息常量 + `_aload_artifact` / `_aload_session` 的只读装配 helper 与 `process_type` 过滤理由）、L153-193（`_aapply_action` 三元组前置 helper 的**确切签名与返回约定**）、L206-260（改状态端点完整形状：`_aapply_action` → 分层 409 → service → 持久化后调 `aresume_after_gate_action`、视图不重复包 try）；server/delivery/urls.py L1-45（import 块的字母序与括号换行体例）、L135-177（`blueprint-gate/` 八条路由的分组注释、path 形状与 `name` 命名）；server/services/process_runtime/blueprint_quality.py L1-14（模块 docstring 两节声明 + **顶层零 ORM import、实装走函数内懒 import** 的纪律原文）、L105-141（三个占位函数的**逐字签名与口径 docstring**——`human_edit_volume` 的 `created_by_user_id` 一句是要修正的对象）；server/delivery/models/artifact.py L130-157（`ArtifactVersion` 字段全表——确认无 `created_by_user_id`）；server/delivery/models/blueprint_thread.py L69-159（`BlueprintThread` / `BlueprintThreadMessage` 的字段与 `author_type` 取值——`clarification_rounds` 的口径依据）；.planning/phases/114-ai/114-03-SUMMARY.md（`blueprint.review.*` 事件的 **payload 键集**——`ai_rejection_rate` 分子分母按它算，不猜）；.planning/phases/114-ai/114-04-SUMMARY.md（`aapply_block_edit` / `aapply_thread_answers` 的恒定返回键与 `status` 语义——端点状态码映射按它对齐）</read_first>
  <action>
**① 新建 `server/delivery/api/blueprint_review_views.py`**，docstring 逐条照抄 112 的七条纪律并换成本相位措辞（含「为什么新建文件而不塞进 `blueprint_gate_views.py`」：后者已有八 View + 门专属 helper，再塞语义不同的 View 会让「确认门」文件承担两个门的语义；URL 前缀区分让 115 的数据面一目了然 —— `blueprint-gate/` = 阶段 1 门，`blueprint-review/` = 阶段 4 人审）。

基类/权限/错误常量：`from adrf.views import APIView`、`permission_classes = [IsAuthenticated]`、中性 404 常量（`_ARTIFACT_MISSING_DETAIL` / `_SESSION_MISSING_DETAIL` / `_THREAD_MISSING_DETAIL`，**不泄露资源存在性**）、409 文案映射表（键对齐 Task 1 与 114-04 的 `status` 值：`blocked` / `conflict` / `invalid` / `rejected`）。只读装配 helper 照 `blueprint_gate_views._aload_artifact` / `_aload_session`（**`process_type="technical_blueprint"` 过滤不可省**）。

五个 View（一动作一 View，**不发明 action 分派**）：

| View | 方法 | URL 段 | 行为 |
|---|---|---|---|
| `BlueprintReviewSnapshotView` | GET | `blueprint-review/` | 只读快照：findings 线程（`kind=ai_review_finding`，按 severity 分组）+ 澄清线程 + **失锚列表**（`anchor_status="orphaned"`）+ `stage_state["ai_review"]` 的未决清单 + `aunresolved_blocker_count`（呈现用）+ `meta.revision_round`。**不接续驱**；视图直查允许；顺带 best-effort 调 `amaybe_remind_pending_review`（pending 提醒挂载点，失败吞掉不影响响应） |
| `BlueprintReviewApproveView` | POST | `blueprint-review/approve/` | `aapprove_blueprint(...)` → `confirmed` 200 / `blocked` **409** / `conflict` **409** / `invalid` 400；持久化成功后调 `aresume_after_gate_action` |
| `BlueprintReviewRejectView` | POST | `blueprint-review/reject/` | 入参 `{comment?, anchor?}` → `areject_blueprint(...)` → `rejected` 200（返回 `version_no` / `revision_round` / `thread_id`）/ `conflict` 409 / `invalid` 400；持久化成功后调续驱 |
| `BlueprintReviewEditBlocksView` | POST | `blueprint-review/edit-blocks/` | 入参 `{ops: [...]}`，`ops` 非 list → **400**；委托 114-04 的 `aapply_block_edit(artifact, ops, user=request.user, ...)` → `applied` 200 / `unchanged` 200（带 `unchanged` 标记）/ `rejected` **400**（回显 `rejected` 清单）/ `invalid` **400**（回显中文错因）；持久化成功后调续驱 |
| `BlueprintReviewThreadAnswerView` | POST | `blueprint-review/threads/<uuid:thread_id>/answer/` | 入参 `{body}`，空 → 400；线程不属于该 artifact → 中性 **404**；委托 `BlueprintLifecycleService.record_answer(thread, body=..., author=request.user, author_type=ThreadAuthorType.HUMAN, ...)`（⭐ **此处才是 `record_answer` 的正当用法**）+ `add_reviewer(artifact, request.user, "thread_answer")`；持久化成功后调续驱 |

纪律硬点（逐条落实现）：
- **视图零 ORM 写**：全部委托 service；读路径可直查；serializer / `.data` 若使用一律 `sync_to_async` 包裹。
- ⭐ **approve 绝不在事务外补 BLOCKER 二次查询**：不调 `aunresolved_blocker_count`、不调 `ahas_open_blocking_threads`，只 `aapprove_blueprint` 并按 `status` 映射码。
- **续驱接线**：四个改状态端点在**持久化成功之后**调 `blueprint_resume.aresume_after_gate_action(session, initiated_by_user_id=str(request.user.id))`；**视图不重复包 try、不因续驱失败改响应码**；GET **不接**。
- 日志：每端点一条 `caller` 结构化事件（`component="blueprint_review_api"`，含 `artifact_id` / `initiated_by_user_id` / `status` / `duration_ms`；**评论正文、block 正文、答案正文绝不进日志**）。

**② `delivery/urls.py` 追加**（删除行 0）：import 块追加一条 `from delivery.api.blueprint_review_views import (...)`（五个 View，字母序）；`urlpatterns` 追加分组注释一行（「阶段 4 人审（114-05，FLOW-07 / CLAR-03 / CLAR-04）：1 个只读快照 + 4 个动作端点，前缀 `blueprint-review/` 与阶段 1 的 `blueprint-gate/` 区分」）+ 5 条 `path(...)`，`name` 分别为 `blueprint-review-snapshot` / `-approve` / `-reject` / `-edit-blocks` / `-thread-answer`。⚠️ 字面段 `threads/` 在 `<uuid:thread_id>` 之前，注意与 `artifact-timeline` 的整段精确匹配互不遮挡（照 `:136-137` 的注释理由）。

**③ `blueprint_quality.py` 三项实装**（受限：删除行 ≤10，保持同步签名，顶层零 ORM import）：
- `ai_rejection_rate(artifact_id)`：函数内懒 import `ConvergenceSessionEvent`（与 artifact 的关联路径按模型实测确定，**不猜字段名**：若事件表通过 `session` 关联，则先由 artifact 反查其蓝图会话 id 再过滤）。分母 = `blueprint.review.completed` 事件总数（= 审查总轮次）；分子 = 其中 `payload["review_status"] == "retry"`（打回）的条数——**payload 键按 114-03-SUMMARY 登记的键集取，不猜**。分母为 0 → `return None`（无数据 ≠ 零打回）。
- `human_edit_volume(artifact_id)`：⭐ **口径 docstring 修正**——把「按 `created_by_user_id` 非系统的版本行计数」改写为「按 `produced_by_ref` 以 `human_edit:` 开头的 `ArtifactVersion` 行计数（**111 docstring 的 `created_by_user_id` 是已知偏差：`ArtifactVersion` 无该字段，`add_version` 只写 `produced_by_session_id` / `produced_by_ref`（`artifact_service.py:151-159`）**——照原 docstring 写会 `FieldError`，改按用户字段过滤则指标恒零）」。实现 `ArtifactVersion.objects.filter(artifact_id=artifact_id, produced_by_ref__startswith="human_edit:").count()`；该 artifact **一条版本都没有** → `return None`；有版本但零人工编辑 → `return 0`（这是真实的「零人工修改」，与「无数据」区分）。
- `clarification_rounds(artifact_id)`：`BlueprintThread` 该 artifact 无线程 → `None`；否则 `BlueprintThreadMessage.objects.filter(thread__artifact_id=artifact_id, author_type=ThreadAuthorType.HUMAN).count()`（每线程一问一答记一轮，口径按 111 docstring 原文）。
三者整体 `try/except Exception` → warning + `return None`（**离线评估不该因统计异常崩**）。模块 docstring 的「DB 统计接口节：占位」一句改写为「DB 统计接口节：**114-05 已实装**（数据来自审查事件 / 人工编辑版本 / 澄清线程）；顶层仍保持零 ORM import，查询走函数内懒 import」。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/services/test_blueprint_quality.py -x -q && uv run python -c "
import django, os
os.environ.setdefault('DJANGO_SETTINGS_MODULE','friday.settings'); django.setup()
from django.urls import reverse
for n in ('blueprint-review-snapshot','blueprint-review-approve','blueprint-review-reject','blueprint-review-edit-blocks'):
    print(n, reverse(n, args=['11111111-1111-1111-1111-111111111111']))
print(reverse('blueprint-review-thread-answer', args=['11111111-1111-1111-1111-111111111111','22222222-2222-2222-2222-222222222222']))"</automated>
  </verify>
  <acceptance_criteria>
- 上述 `reverse` 命令输出五条 URL 且形如 `/api/.../artifacts/<uuid>/blueprint-review/...`（前缀正确、`name` 命名一致）。
- `rg -c "^class Blueprint" server/delivery/api/blueprint_review_views.py` == 5（一动作一 View）。
- `rg -n "from adrf.views import APIView" server/delivery/api/blueprint_review_views.py` 命中；`rg -n "from rest_framework.views import APIView" server/delivery/api/blueprint_review_views.py` **零命中**。
- `rg -c "permission_classes = \[IsAuthenticated\]" server/delivery/api/blueprint_review_views.py` == 5。
- ⭐ approve 无 TOCTOU 源码扫描：`cd server && uv run python -c "
import re
src = open('delivery/api/blueprint_review_views.py').read()
i = src.index('class BlueprintReviewApproveView')
j = min([k for k in (src.find('class Blueprint', i+1), len(src)) if k > 0])
body = src[i:j]
for bad in ('aunresolved_blocker_count', 'ahas_open_blocking_threads'):
    assert bad not in body, bad
print('approve has no pre-check query')"` 输出 `approve has no pre-check query`。
- ⭐ `record_answer` 只在 answer 端点：`cd server && uv run python -c "
src = open('delivery/api/blueprint_review_views.py').read()
for name in ('BlueprintReviewApproveView','BlueprintReviewRejectView','BlueprintReviewEditBlocksView'):
    i = src.index('class ' + name); nxt = src.find('class Blueprint', i+1)
    body = src[i: nxt if nxt > 0 else len(src)]
    assert 'record_answer' not in body, name
assert 'record_answer' in src
print('record_answer scoped OK')"` 输出 `record_answer scoped OK`。
- 视图零 ORM 写扫描：`rg -n "objects\.(create|acreate|bulk_create|update|bulk_update|delete)" server/delivery/api/blueprint_review_views.py` **零命中**。
- `rg -n "aresume_after_gate_action" server/delivery/api/blueprint_review_views.py | rg -c ""` == 4（四个改状态端点各一次；GET 不接）。
- `git diff server/delivery/urls.py | rg "^-[^-]"` **为空**（纯追加）。
- ⭐ `rg -n 'produced_by_ref__startswith' server/services/process_runtime/blueprint_quality.py` 命中；`rg -n "created_by_user_id" server/services/process_runtime/blueprint_quality.py` **零命中**（口径已修正，不会 `FieldError`）。
- `rg -n "^from delivery|^from django|^import django" server/services/process_runtime/blueprint_quality.py` **零命中**（顶层仍零 ORM import）。
- `cd server && uv run python -c "
import ast
tree = ast.parse(open('services/process_runtime/blueprint_quality.py').read())
sigs = {n.name: ast.unparse(n.args) for n in tree.body if isinstance(n, ast.FunctionDef)}
assert sigs['ai_rejection_rate'] == 'artifact_id: str' and sigs['human_edit_volume'] == 'artifact_id: str' and sigs['clarification_rounds'] == 'artifact_id: str', sigs
assert not any(isinstance(n, ast.AsyncFunctionDef) for n in tree.body), '签名必须保持同步（evaluate_blueprint_golden 在调）'
print('quality signatures unchanged')"` 输出 `quality signatures unchanged`。
- ⭐ 受限面删除行核算：`git diff server/services/process_runtime/blueprint_quality.py | rg "^-" | rg -v "^---" | wc -l` **≤ 10**，且逐行落在三个占位函数体与 `human_edit_volume` docstring 内 —— SUMMARY 逐行登记。
- `cd server && uv run pytest tests/services/test_blueprint_quality.py tests/delivery/ -q` 全绿。
- `cd server && uv run ruff check delivery/api/blueprint_review_views.py delivery/urls.py services/process_runtime/blueprint_quality.py` 通过；`ruff format --check` **只对 `blueprint_review_views.py`** 跑并通过。
  </acceptance_criteria>
  <done>五端点就位且遵循 112 的七条惯例；approve 源码内不存在任何事务外的 BLOCKER 预查询；`record_answer` 严格局限在 answer 端点；四个改状态端点接续驱、GET 不接；三项统计实装且用 `produced_by_ref__startswith` 而非不存在的 `created_by_user_id`、无数据返 None、同步签名与顶层零 ORM import 不变。</done>
</task>

<task type="auto">
  <name>Task 3: 端点测试 + 统计测试 + 全量相位门</name>
  <files>server/tests/delivery/test_blueprint_review_views.py, server/tests/services/test_blueprint_quality.py</files>
  <read_first>server/tests/delivery/test_blueprint_gate_api.py L1-23（「守十件事」编号清单体例）、L24-120（认证 client fixture / artifact+session 构造工厂 / **续驱桩掉的写法**——复用，不重复造）、以及其「视图零 ORM 写源码扫描」与「SC-4 端到端不桩依赖」两条用例的完整写法；server/tests/services/test_blueprint_quality.py L1-20（import 与体例）、L85-95（`test_db_stat_placeholders_return_none` 的现状——**本 Task 必须改写它**）；server/tests/delivery/test_blueprint_reanchor_edit.py（114-04 的 fixture 与 patch 构造 —— edit-blocks 端点测试复用其 ops 样例）</read_first>
  <action>
**① 新建 `server/tests/delivery/test_blueprint_review_views.py`**（`pytestmark = [pytest.mark.django_db(transaction=True), pytest.mark.asyncio]`），docstring「守十二件事」编号清单，每条把可证伪断言写进条目：

1. ⭐ **鉴权第一条**：五端点未认证一律拒（401/403），无一例外（参数化五条）。
2. **只读快照**：无 artifact → 中性 404；有数据 → 200 且响应含 findings 分级分组 / 澄清线程 / **失锚列表**（`anchor_status="orphaned"` 的线程出现在其中）/ 未决清单 / `revision_round`。GET **不接续驱**（桩的 `aresume_after_gate_action` `await_count == 0`）。
3. ⭐ **approve 有未决 BLOCKER → 409**：先开一条 `severity=blocker, blocking=True` 的 finding 线程 → approve **409**，DB 重读 `blueprint_status` **仍是 `pending_review`**（状态不变、DB 不写）。
4. ⭐ **approve 清空后 → 200**：`resolve_thread` 掉该 finding → approve 200，**DB 重读** `artifact.blueprint_status == "confirmed"`，请求用户进 `BlueprintReviewer`。
5. ⭐ **approve 无 TOCTOU（源码扫描 + 并发行为）**：源码扫描断言 `BlueprintReviewApproveView` 函数体内不含 `aunresolved_blocker_count` / `ahas_open_blocking_threads`；行为侧 monkeypatch `_has_confirm_blockers_sync` 在守卫查询发生的那一刻插入一条 `open+blocking` finding → approve **409** 且状态仍 `pending_review`（对照：不插入时 200 ⇒ 断言非恒真）。
6. ⭐ **reject：先版本后状态**：reject 200 → 版本数 **+1**、新版本 `content["meta"]["revision_round"]` == 旧值 + 1、`produced_by_ref` 以 `blueprint_review_reject:` 开头、**DB 重读**状态 `drafting`；带 `comment` 时新增一条 `kind=human_comment` 线程且 `blocking is False`。
7. **reject 幂等**：连续两次 reject → `revision_round` 每次 +1（各自基于重读的 current content，**不会因内存副本连加两次或不加**）；`revision_round` 单调递增被断言。
8. ⭐ **edit-blocks 不合法 → 400 且版本数不变**：构造让 `validate_blueprint` 失败的 ops → 400、`detail` 非空（中文错因）、`ArtifactVersion` 行数与调用前**相等**。
9. ⭐ **edit-blocks 同 hash 不翻版本**：同一 ops 连提两次 → 第二次 200 且 `unchanged`、版本行数不变；成功版本 `produced_by_ref == f"human_edit:{user.id}"`；编辑者进 `BlueprintReviewer`（`first_action == "block_edit"`，重复编辑不覆盖）。
10. **threads answer**：线程不属于该 artifact → 中性 404；空 body → 400；正常 → 200 且 DB 重读线程 `status == "answered"`、最后一条消息 `author_type == "human"`；答者进 `BlueprintReviewer`。
11. ⭐ **续驱接线正反 + 失败隔离**：四个改状态端点各 `await_count == 1` 且入参 session 是该 artifact 的蓝图会话；GET 为 0；续驱**抛异常**时四端点仍 2xx 且动作已持久化（**不回滚、不回 5xx**）。
12. **视图零 ORM 写源码扫描**（照 112 第 9 条）+ **绝不静默落 FAILED**：端到端跑一次 reject → approve 流程后断言会话状态不是 `FAILED`。

**② 改写 `server/tests/services/test_blueprint_quality.py` 的 DB 统计节**（其余用例一字不动，删除行逐行登记）：
- 把 `test_db_stat_placeholders_return_none`（`:92-95`）改写为 `test_db_stats_return_none_when_no_data`，**加 `@pytest.mark.django_db`**（实装后要查库；不加会因 pytest-django 的 DB 访问守卫直接报错），断言不存在的 artifact_id 三项仍返回 **`None`**（无数据 ≠ 0）。
- 新增有数据的正向用例（`@pytest.mark.django_db`）：
  - `human_edit_volume`：造 3 个版本（其中 2 个 `produced_by_ref="human_edit:u1"`）→ **== 2**；造 1 个版本且零人工 → **== 0**（与「无版本 → None」区分，⭐ 这条锁死 A2：若实现误用不存在的 `created_by_user_id` 会 `FieldError`，若误按用户字段过滤会恒为 0 而这两条并列会把它逮住）；
  - `clarification_rounds`：造 2 条线程各 1 条 human 消息 + 1 条 ai 消息 → **== 2**（只数 human）；
  - `ai_rejection_rate`：造 3 条 `blueprint.review.completed` 事件（其中 1 条 `review_status="retry"`）→ **≈ 1/3**（用 `pytest.approx`）；零事件 → `None`。
  - **恒不抛**：给三项喂非法 artifact_id（非 uuid 字符串）→ 返回 `None` 不抛。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/delivery/test_blueprint_review_views.py tests/services/test_blueprint_quality.py -x -q</automated>
  </verify>
  <acceptance_criteria>
- `cd server && uv run pytest tests/delivery/test_blueprint_review_views.py tests/services/test_blueprint_quality.py -q` 全绿。
- `rg -c "def test_" server/tests/delivery/test_blueprint_review_views.py` ≥14。
- `rg -n "409" server/tests/delivery/test_blueprint_review_views.py` 命中 ≥2（approve 未决 BLOCKER + 并发）。
- `rg -n "_has_confirm_blockers_sync" server/tests/delivery/test_blueprint_review_views.py` 命中（TOCTOU 行为用例存在）。
- `rg -n "revision_round" server/tests/delivery/test_blueprint_review_views.py` 命中 ≥2（+1 与幂等两条）。
- `rg -n "orphaned" server/tests/delivery/test_blueprint_review_views.py` 命中（失锚列表在快照里）。
- `rg -n "await_count" server/tests/delivery/test_blueprint_review_views.py` 命中 ≥2（续驱正反）。
- `rg -n "human_edit:" server/tests/services/test_blueprint_quality.py` 命中；`rg -n "== 0" server/tests/services/test_blueprint_quality.py` 命中（「零人工编辑 → 0」与「无版本 → None」并列存在）。
- `git diff server/tests/services/test_blueprint_quality.py | rg "^-" | rg -v "^---" | wc -l` ≤ 6（仅改写 DB 统计节那一条用例），逐行登记。
- ⭐ 全量相位门：`cd server && uv run pytest tests/ -q` **无新增失败**（与 114-01 前的基线逐条比对，任何差异在 SUMMARY 登记）。
- `cd server && uv run python manage.py makemigrations --check --dry-run` 退出码 0（**全相位零 migration**）。
- `cd server && uv run ruff check delivery/ services/process_runtime/ system/models.py tests/ ` 通过；`ruff format --check` 只对本相位新建文件跑并通过。
- ⭐ 相位级冻结面自检：`git diff --name-only HEAD~<本相位提交数>` 的文件集**不含** `server/codegraph/services/repo_router_v2.py`、`server/services/process_runtime/{decompose_segments,research_adapter,architect_merge_adapter,clarify_adapter,render,resume}.py`、`server/delivery/{merged_plan.py}`、`server/repositories/services/charter_service.py`、`server/system/settings_service.py`、`server/agents/call_source.py`、`server/delivery/models/`、`task/`。
- ⭐ 相位级受限面自检（逐文件汇总进 SUMMARY）：`blueprint_resume.py` 删除行 **0**；`event_taxonomy.py` **0**；`system/models.py` **0**；`delivery/urls.py` **0**；`blueprint_lifecycle_service.py` 全相位 ≤12（全部由 114-01 消耗，114-04 追加部分为 0，114-05 为 0）；`builtin_processes.py` ≤3；`blueprint_quality.py` ≤10。
- ⭐ 四条 REQ 覆盖自检（写进 SUMMARY）：FLOW-07（114-02/03/05）、CLAR-02（114-04）、CLAR-03（114-04/05）、CLAR-04（114-05）各有对应的绿色测试用例清单。
  </acceptance_criteria>
  <done>五端点鉴权/状态码分层/续驱正反与失败隔离/视图零 ORM 写全部被锁死；approve 的无 TOCTOU 有源码与并发双证；reject 的「先版本后状态」与 `revision_round` 幂等成立；三项统计有数据/零数据/无数据三态被区分且 A2 偏差被并列用例逮住；相位级全量门（pytest / makemigrations / 冻结面 / 受限面删除行）全部达标。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 未认证请求 → 五端点 | 唯一 fail-closed 不降级的边界（安全边界不 best-effort） |
| `pending_review → confirmed` | 跨越即「蓝图被认可、可进编码」，守卫失守等于绕过人审 |
| 用户 patch ops / 评论正文 → 版本与线程 | 半可信文本进文档与 HITL 面板 |
| 统计口径 → golden 评估 | 字段名写错会让指标恒零/恒 None 而测试看起来全绿 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-114-32 | Elevation of Privilege | 未认证/越权调用人审端点直接把蓝图推到 `confirmed` | mitigate | 五端点 `IsAuthenticated` + Task 3 第 1 条参数化鉴权断言（安全边界不降级） |
| T-114-33 | Tampering | approve 在事务外补「未决 BLOCKER」查询 ⇒ TOCTOU 窗口复活 | mitigate | 判据已在 114-01 收敛进单一事务的单次 `Q` 查询；View 内禁用 `aunresolved_blocker_count` / `ahas_open_blocking_threads`（源码扫描）+ Task 3 第 5 条并发用例（守卫内插入行仍 409） |
| T-114-34 | Tampering | reject 先转状态后落版本 ⇒ 「状态 drafting 而轮次未加」的窗口里 AI 拿旧轮次重跑 | mitigate | 三步链顺序固定 + 每次重读 current content + Task 3 第 6/7 条断言版本先于状态且轮次单调 |
| T-114-35 | Tampering | 半合法 content 经 edit-blocks 落版本 | mitigate | 114-04 的显式 `validate_blueprint` + `add_version` 双层 + Task 3 第 8 条断言版本数不变 |
| T-114-36 | Info Disclosure | 中性 404 缺失导致资源存在性枚举；或评论/block/答案正文进日志 | mitigate | 中性 404 常量（照 112）+ 日志只记 `artifact_id` / `status` / `duration_ms` 标量 + 错因走 `_format_error`（已脱敏截断）与 `redact_secrets_in_text` |
| T-114-37 | Repudiation | 人工编辑/驳回无归属 ⇒ 无法审计谁改了蓝图 | mitigate | `produced_by_ref` 带 `human_edit:{user_id}` / `blueprint_review_reject:{user_id}` + `add_reviewer` upsert + Task 3 第 9/10 条断言 |
| T-114-38 | Spoofing | `human_edit_volume` 按 111 docstring 用 `created_by_user_id` ⇒ `FieldError`；或改按别的用户字段 ⇒ 指标恒零但测试绿 | mitigate | `produced_by_ref__startswith="human_edit:"` + `rg "created_by_user_id"` 零命中 + Task 3「2 个人工版本 → 2」与「零人工 → 0」「无版本 → None」三态并列 |
| T-114-39 | Denial of Service | pending 提醒新起 OS 级定时任务 / 自动作答 / 判失败 ⇒ 无声卡死或误判（CLAR-04 明令禁止） | mitigate | 挂载点在只读端点与 service 调用路径上 + `rg "apscheduler|add_job"` 零命中 + 提醒只产名单与事件、不改状态 |
| T-114-40 | Denial of Service | 续驱失败让端点回 5xx 或回滚已持久化动作 | mitigate | 失败隔离在 `aresume_after_gate_action` 内 + 视图不重复包 try + Task 3 第 11 条断言抛异常时仍 2xx 且已持久化 |
| T-114-41 | Tampering | 统计异常导致 `evaluate_blueprint_golden` 离线评估崩 / 「无数据」被当成「零打回」 | mitigate | 三项整体 `try/except` → warning + `None`；无数据一律 `None` 而非 0 + Task 3 的三态并列用例 |
| T-114-SC | Tampering | 依赖供应链 | accept | 零新增外部依赖、零 migration、零新 SettingKeys 键、零新事件常量 |
</threat_model>

<verification>
- `cd server && uv run pytest tests/delivery/ -q` 全绿（112-05 确认门八端点 + 114-01 线程底座 + 114-04 重锚/编辑 + 本 plan 五端点）。
- `cd server && uv run pytest tests/services/ tests/services/process_runtime/ -q` 全绿（三项统计 + 114-02/03 判定与 stage + 113 覆盖率门）。
- ⭐ `cd server && uv run pytest tests/ -q` **无新增失败**（相位级全量门；与 114-01 前基线逐条比对，差异登记进 SUMMARY）。
- `cd server && uv run python manage.py makemigrations --check --dry-run` 退出码 0（**全相位零 migration**）。
- `cd server && uv run ruff check delivery/ services/process_runtime/ system/models.py tests/` 通过；`ruff format --check` **只对**本相位新建文件跑（`blueprint_review.py` / `blueprint_reflow.py` / `blueprint_block_edit.py` / `blueprint_review_action.py` / `blueprint_review_views.py` / 本相位新建测试文件），**不对** `blueprint_lifecycle_service.py` / `builtin_processes.py` / `blueprint_resume.py` / `entrypoint.py` / `event_taxonomy.py` / `system/models.py` / `delivery/urls.py` / `blueprint_quality.py` 跑。
- 受限面删除行相位级汇总（逐文件命令见 Task 3 验收）：`blueprint_resume.py` = 0、`event_taxonomy.py` = 0、`system/models.py` = 0、`delivery/urls.py` = 0、`blueprint_lifecycle_service.py` ≤12（全部在 114-01）、`builtin_processes.py` ≤3、`entrypoint.py` ≤2、`blueprint_quality.py` ≤10、`test_blueprint_quality.py` ≤6。
- 冻结面自检：`git diff --name-only` 只含本 plan 声明的七个文件；相位级冻结清单核对见 Task 3 验收。
- 端到端证伪线（不桩续驱）：REST `edit-blocks` → `reject` → `approve` 全链跑通，DB 重读状态依次为 `drafting` → `confirmed`，且会话**绝不静默落 FAILED**。
</verification>

<success_criteria>
- 人审能在 REST 上完成全部操作：查快照（含失锚评论与未决 BLOCKER 清单）、通过、驳回带划线评论、直接改 block、回答澄清；全部经 service 收口、视图零 ORM 写、错误码分层清晰。
- 通过端点**无 TOCTOU**：并发下带未决 BLOCKER 的蓝图不会被放行，且视图里根本不存在事务外的预查询（源码扫描可证）。
- 驳回把 `meta.revision_round` 正确 +1 且**先落版本再转状态**，重试不会连加两次——本相位成为该字段可靠的首个写入方。
- `blueprint_quality` 三项统计首次有真实数据且口径正确（`produced_by_ref__startswith="human_edit:"`），无数据返 `None` 而非 0，签名与顶层零 ORM import 不变。
- pending 超时有显式提醒挂载点：周期可配、对象是 reviewer 名单 + 发起人、不自动作答、不判失败、不新起定时任务。
- 相位收口：四条 REQ（FLOW-07 / CLAR-02 / CLAR-03 / CLAR-04）各有绿色测试背书；全量 `pytest tests/ -q` 无新增失败；零 migration；冻结面与受限面删除行核算全部达标。
</success_criteria>

<output>
完成后创建 `.planning/phases/114-ai/114-05-SUMMARY.md`（provides：五端点的 URL / `name` / 方法 / 入参键 / 全部响应状态码映射表、`blueprint_review_action` 三个入口的**逐字签名与恒定返回键**、`bump_revision_round` 的容错规则、`produced_by_ref` 前缀清单（`human_edit:` / `ai_review_reflow:` / `blueprint_review_reject:`）、`add_reviewer` 的 `first_action` 取值全集、三项统计的**最终口径与实测 SQL 语义**（含 `human_edit_volume` 的 docstring 修正前后对照与「无版本→None / 零人工→0」的区分）、pending 提醒的配置键路径与默认小时数与 `recipients` 组成、**受限面删除行逐行登记**（`blueprint_quality.py` / `delivery/urls.py` / `system/models.py` / `test_blueprint_quality.py`）、相位级 `pytest tests/ -q` 与基线差异比对、**四条 REQ → 测试用例映射表**、以及相位收口清单（零 migration / 冻结面零命中 / 受限面删除行汇总））。
</output>
