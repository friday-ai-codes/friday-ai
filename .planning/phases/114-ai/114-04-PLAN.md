---
phase: 114-ai
plan: 04
type: execute
wave: 3
depends_on: ["114-02"]
files_modified:
  - server/delivery/services/blueprint_lifecycle_service.py
  - server/delivery/services/blueprint_block_edit.py
  - server/services/process_runtime/blueprint_reflow.py
  - server/tests/delivery/test_blueprint_reanchor_edit.py
  - server/tests/services/process_runtime/test_blueprint_reflow.py
autonomous: true
requirements: [CLAR-02, CLAR-03]
prohibitions:
  - "禁止修改 §13.2 冻结面（`git diff --name-only` 零命中）：`process_runtime/{decompose_segments,research_adapter,architect_merge_adapter,clarify_adapter,render,resume}.py`、delivery 侧 `merged_plan.py`、`repositories/services/charter_service.py`"
  - "禁止修改 `task/core/knowledge_tools.py` 公共 handler 工厂（本相位不碰 `task/`）"
  - "🔒 本 plan **一行都不改** `builtin_processes.py`（stage 注册与 handler 全部归 114-03；本 plan 不新增 stage、不改任何出边、不接线任何 handler）"
  - "🔒 本 plan **一行都不改** `server/services/process_runtime/blueprint_resume.py`（映射表追加归 114-03，删除行上界 0）"
  - "🔒 **B3 定夺：`blueprint_merge.py` 一行不改**（保持只读受限）——人工块保护的**入口**在本 plan 的 `blueprint_reflow.py`，**接线点**在 114-03 的 `ai_review` 入口/新版本落库后；`git diff server/services/process_runtime/blueprint_merge.py` 必须为空"
  - "🔒 本 plan **一行都不改** `server/services/process_runtime/blueprint_review.py`（114-02 的判定内核 + 114-03 的 adapter 节；`git diff` 必须为空）"
  - "禁止给任何模型加字段 / 建 migration（**本 plan 内**）：`makemigrations --check --dry-run` 退出码必须 0。全相位唯一的字段追加（`BlueprintThread.last_reminded_at`）与唯一一条 migration 由 **114-05** 承载"
  - "⛔ **`record_answer` 禁用于 finding 留痕**：本 plan 的 AI 侧留痕（含「与人工内容冲突」的询问）一律 `open_thread` + `append_note`；`record_answer` 只允许出现在**人类回答澄清线程**的路径上（本 plan 不含该端点，归 114-05）。`rg -n \"record_answer\" server/services/process_runtime/blueprint_reflow.py server/delivery/services/blueprint_block_edit.py` 必须**零命中**"
  - "🔒 `blueprint_lifecycle_service.py` **只允许在文件尾纯追加** `areanchor_threads` + 一个 `@sync_to_async` 私有 helper + `__all__ += [...]`：`git diff server/delivery/services/blueprint_lifecycle_service.py | rg \"^-[^-]\"` **必须为空**（删除行 = 0；114-01 已用掉该文件前 1065 行唯一的改动配额）。不改 `transition` / `open_thread` / `record_answer` / `resolve_thread` / `append_note` / `ahas_open_blocking_threads` / `apply_gate_action` / `_apply_transition_sync` / `_has_confirm_blockers_sync` 的任何既有语义与签名。本文件**只跑 `ruff check`，不跑 `ruff format`**"
  - "禁止修改 `blueprint_anchor.py`（111 交付 + 单测锁死 0.85 阈值与同分字典序）：性能优化只做「`diff_blueprint_blocks` 预筛变动块」这一层，**不改 `reanchor` 本体**、不引入 `quick_ratio` 预筛"
  - "禁止修改 `blueprint_schema.py` / `blueprint_quality.py` / `blueprint_merge.py` / `blueprint_reconcile.py` / `blueprint_confirm_gate.py` / `blueprint_spec_gate.py` / `blueprint_repo_plan.py` / `blueprint_charter_match.py` / `blueprint_review.py` / `artifact_service.py`（受限面，本 plan 只读消费）"
  - "禁止在 view / adapter / 纯函数层直写 `BlueprintThread.anchor` 或 `BlueprintThread.objects.*`（INV-6）：线程行写入只有 `areanchor_threads` 一条新通道，且它在 `BlueprintLifecycleService` 内；`rg -n \"BlueprintThread.objects\" server/services/process_runtime/blueprint_reflow.py server/delivery/services/blueprint_block_edit.py` 只允许**只读**查询（`filter(...).values/aexists/afirst/async for`），零写调用"
  - "禁止自写 block diff / 自写模糊匹配 / 自写 hash 比对 / 自写 section_path 递归：一律用 `diff_blueprint_blocks`(`blueprint_schema.py:1044`) / `reanchor`(`blueprint_anchor.py:66`) / `add_version`(`artifact_service.py:117`) / `iter_blocks`(`blueprint_schema.py:919`)"
  - "禁止扩 `_apply_transition_sync` 加 `revision_round`：`meta.revision_round` 是 **content 的 `meta` 段字段**（`blueprint_schema.py:160-164`），不是模型字段。它的写入方在 114-05（人审驳回）；本 plan 只提供并测试可复用的 content 改写 helper，不改 transition"
  - "禁止 `decision_log` 只写 `decision` 不写 `answer`：必须保 `answer` 键（`blueprint_spec_gate._collect_prior_answers:587` 读的是 `item.get(\"answer\")`），否则「同一问题不再重复问」在审查阶段断链"
  - "禁止 `decided_at` 用 `timezone.now()`：回灌是**可重放路径**，时间戳每次变会改 `content_hash`（`sort_keys=True` 只消除 key 顺序影响）→ 每次都翻新版本 → 破坏「同 hash 不翻版本」的幂等意图。一律取**线程作答消息的 `created_at`**"
  - "禁止读 `session.current_artifact_version` 作改写基线（`blueprint_confirm_gate.py:546-550` 已记：`add_version` 已推进 current，读 session 那一版会把上游成果覆盖回旧内容）；禁止不 `deepcopy` 直接改 `version.content`"
  - "禁止 `add_version` 失败上抛（`ArtifactContentInvalid` 一律 fail-closed：warning + 返回「待处置」结果，不落半合法版本、不落 failed）；禁止先 `transition` 再 `add_version`"
  - "禁止把线程 anchor 的失锚线程删除或隐藏：失锚一律 `anchor_status=\"orphaned\"` 保留且可集中查询（115 展示「失锚评论」）"
  - "禁止 AI 覆盖人工内容：冲突时**不写版本**，改为开阻塞线程询问（镜像 `ProjectContextLink` 的「AI 不覆盖人工」原则）"
must_haves:
  truths:
    - "`BlueprintLifecycleService.areanchor_threads(artifact, new_content, *, old_content=None, initiated_by_user_id=\"system\") -> dict` 存在（文件尾纯追加，删除行 0），返回恒定键 `{checked, reanchored, orphaned, skipped}`"
    - "⭐ 性能防护（P3）：先用 `diff_blueprint_blocks(old_content, new_content)` 算变动块集合，**只对 anchor 落在变动块上的线程走 `reanchor`**，未变动的直接 `skipped`（改一两个块的常见场景从 O(N×M) 降到 O(1)）；`old_content` 缺省（None）时退化为全量重锚（正确性优先）"
    - "⭐ `section_path` 被一并刷新：`reanchor` 只改 `block_id`，批量侧用 `iter_blocks(new_content)` 的 path 刷新 `anchor[\"section_path\"]`（115 渲染依赖它定位）"
    - "失锚线程**不删**：`anchor_status` 置 `orphaned` 且线程行保留，可用 `filter(anchor_status=\"orphaned\")` 集中查询"
    - "批量写回用**一次** `bulk_update([\"anchor\", \"anchor_status\", \"updated_at\"])`（`.update()`/`bulk_update` 绕过 `auto_now` ⇒ 必须显式带 `updated_at=timezone.now()`），不逐行 `save`；写线程行只在 service 内（INV-6）"
    - "`apply_block_ops(content, ops) -> tuple[dict, list[dict]]` 纯函数：支持 `replace` / `insert` / `delete` 三种 op，按 `block_id` 定位，`deepcopy` 后改，**入参不被原地修改**；未知 op / 找不到 block_id → 记进 `rejected` 列表且**不静默跳过**"
    - "⭐ 人工编辑不合法即拒绝且**不落版本**：`aapply_block_edit` 先显式 `validate_blueprint(new_content)` 拿到可回显的中文错因，非法直接返回 `{\"status\": \"invalid\", \"detail\": <脱敏截断错因>}`，版本数不变"
    - "人工编辑落版本用 `produced_by_ref=f\"human_edit:{user_id}\"`（`ArtifactVersion.produced_by_ref` `max_length=255` 足够），与 AI 版本同链路同 diff 视图；成功后自动 `areanchor_threads` + `add_reviewer(artifact, user, first_action=\"block_edit\")` upsert"
    - "⭐ 同 content_hash 不翻版本：无实质改动的 patch（或重复提交同一 patch）经 `add_version` 复用 current，`version_no` 不推进、不建行（`_add_version_sync:148-149`）"
    - "澄清回灌走**三步**：改 content（`deepcopy` 最新版本 → 段落重产 + `decision_log` 合并）→ `add_version(produced_by_ref=f\"ai_review_reflow:{thread_id}\")` → `transition`；顺序固定为**先落版本再转状态**"
    - "`decision_log` 物化保规格门形状的超集 `{thread_id, question, answer, decided_at, decided_by, applied_in_version}`，按 `thread_id` 去重（复用 `_merge_decision_log` 同款去重逻辑），`decided_at` 取线程作答消息的 `created_at`、`decided_by` 默认 `human`（有 `author_id` 取其 str，AI 侧写 `ai`）"
    - "⭐ 同一问题不重复问（可证伪）：回灌后 `blueprint_spec_gate._collect_prior_answers` 能从新版本 content 的 `decision_log` 读到该 `question`/`answer` 并产出指纹（断言指纹集合含该问题）"
    - "线程收尾：`resolve_thread(thread, resolution=...)` 且 `decision_log[].applied_in_version` 记**基线版本 id**（写入前已知、可重放稳定；产出版本经 `produced_by_ref == f\"ai_review_reflow:{thread_id}\"` 反查——`BlueprintThreadMessage` 无「结论」字段，结构化留痕只能在 `decision_log`）"
    - "⭐ AI 不覆盖人工：回灌前用 `diff_blueprint_blocks` 算出「人工版本相对其 `supersedes` 改过的 block 集合」与「本次 AI 将改写的 block 集合」，交集非空 ⇒ **不落版本**，改为 `open_thread(kind=ThreadKind.AI_CLARIFICATION, blocking=True, return_stage=\"ai_reviewing\")` 询问，返回 `{\"status\": \"conflict\", \"thread_id\": ...}`"
    - "⭐ **`section_writer` 有生产实现（B1）**：`ablock_section_writer` 存在且是 `aapply_thread_answers` 的**默认段落重产实现**——`section_writer=None` ⇒ 用它（不再是「跳过段落重产」）。它按线程 `anchor.block_id` 定位块、单块单次 LLM 改写（`call_source=CallSource.BLUEPRINT_AI_REVIEW`，`streaming=False`，块数受 `_MAX_REWRITE_BLOCKS` 上界），LLM 不可得 / 不可解析 / 块找不到 ⇒ **该块原样保留**（`decision_log` 物化照常，答案永不丢失）"
    - "⭐ **人工块保护的可复用入口（B3）**：`acollect_human_block_ids(artifact) -> list[str]`（沿版本链取 `produced_by_ref__startswith=\"human_edit:\"` 的版本，各自与其 `supersedes` 做 `diff_blueprint_blocks` 求 `added ∪ modified` 并集，只读查询）与 `arestore_human_blocks(artifact, *, ...) -> dict`（恒定键 `{status, preserved, conflicted, thread_id, version_id, version_no}`）：**等价则保留、实质冲突则把人工块写回并开阻塞线程询问，绝不静默覆盖**；无需改动时经 `add_version` 的同 hash 幂等返回 `unchanged`（不翻版本）。114-03 的 `ai_review` 入口消费它（本 plan 只交付与单测，不接线）"
    - "⭐ `arestore_human_blocks` 落版本时 `produced_by_ref=f\"human_block_restore:{version_no}\"`，与 `human_edit:` / `ai_review_reflow:` 并列为本相位第三个前缀，归属可审计"
  artifacts:
    - path: "server/delivery/services/blueprint_lifecycle_service.py"
      provides: "areanchor_threads 批量重锚定（文件尾纯追加，删除行 0）"
      contains: "async def areanchor_threads"
    - path: "server/delivery/services/blueprint_block_edit.py"
      provides: "apply_block_ops 纯函数（replace/insert/delete）+ aapply_block_edit service 收口（validate → add_version(human_edit:) → 重锚定 → reviewer upsert）"
      contains: "def apply_block_ops"
    - path: "server/services/process_runtime/blueprint_reflow.py"
      provides: "澄清答案回灌三步链 + decision_log 物化 + resolve_thread + applied_in_version + AI 不覆盖人工的冲突检测与开线程 + ablock_section_writer 生产段落重产（B1）+ acollect_human_block_ids / arestore_human_blocks 人工块保护入口（B3）"
      contains: "ablock_section_writer"
    - path: "server/tests/delivery/test_blueprint_reanchor_edit.py"
      provides: "重锚定四分支 + section_path 刷新 + 失锚不删 + diff 预筛跳过 + patch 三 op + 不合法拒绝不落版本 + 同 hash 不翻版本 + reviewer upsert"
      contains: "orphaned"
    - path: "server/tests/services/process_runtime/test_blueprint_reflow.py"
      provides: "三步顺序 + decision_log 保 answer 键 + decided_at 稳定 + 同一问题不重复问 + AI 不覆盖人工冲突开线程 + 非法 content fail-closed + section_writer 默认走生产实现（B1）+ 人工块保护等价保留/冲突开线程（B3）"
      contains: "arestore_human_blocks"
  key_links:
    - from: "server/delivery/services/blueprint_lifecycle_service.py"
      to: "server/delivery/services/blueprint_anchor.py"
      via: "reanchor(anchor, new_blocks)（不自写模糊匹配，不改 111 算法本体）"
      pattern: "reanchor"
    - from: "server/delivery/services/blueprint_block_edit.py"
      to: "server/delivery/services/artifact_service.py"
      via: "add_version(content, produced_by_ref=f\"human_edit:{user_id}\")（先校验后落库 + 同 hash 幂等）"
      pattern: "add_version"
    - from: "server/services/process_runtime/blueprint_reflow.py"
      to: "server/services/process_runtime/blueprint_schema.py"
      via: "diff_blueprint_blocks 算人工改过的 block 集合与 AI 将改写集合的交集（冲突判据）"
      pattern: "diff_blueprint_blocks"
---

<objective>
把线程与版本的**闭环**补齐（CLAR-02 + CLAR-03）：

① `BlueprintLifecycleService` 文件尾纯追加 `areanchor_threads`——**批量重锚定的唯一调用方**（111 只交付了单条算法与单测）：`diff_blueprint_blocks` 预筛变动块 → 只对受影响线程调 `reanchor` → **一并刷新 `section_path`** → 一次 `bulk_update` → 失锚置 `orphaned` 且不删；
② 新建 `blueprint_block_edit.py`：block 级 patch ops（`replace` / `insert` / `delete`）纯函数 + service 收口（**先 `validate_blueprint` 回显中文错因**、不合法不落版本 → `add_version(produced_by_ref="human_edit:{user_id}")` → 重锚定 → `BlueprintReviewer` upsert）；
③ 新建 `blueprint_reflow.py`：澄清答案回灌的**三步链**（改 content → `add_version` → `transition`，先落版本再转状态）+ `decision_log` 物化（保 `answer` 键、`decided_at` 用作答消息时间戳保幂等）+ `resolve_thread` 记 `applied_in_version` + **AI 不覆盖人工**（冲突不写版本，改开阻塞线程询问）+ **`ablock_section_writer` 生产段落重产实现（B1）** + **`acollect_human_block_ids` / `arestore_human_blocks` 人工块保护入口（B3，供 114-03 的 `ai_review` 入口消费）**；
④ 两个测试文件把上述全部可证伪点锁死，其中四条是本 plan 的头号靶子：**同一问题不重复问**、**人工编辑不被 AI 覆盖**、**同 content_hash 不翻版本**、**重装后人工块被保留且冲突有线程**。

Purpose: 没有 ② 蓝图只能被 AI 改、人只能干看；没有 ① 版本一变旧批注全部错位或凭空消失（CLAR-02 明写「失锚线程集中可见、不静默丢失」）；没有 ③ 的 `answer` 键与稳定 `decided_at`，112 立下的「同一问题不再重复问」纪律会在审查阶段断链、且每次回灌都翻新版本把版本历史刷成噪声。**B1/B3 定夺追加的两件事同样在这里立地基**：`section_writer` 若停在「默认 None ⇒ 不重产段落」，答案就只进 `decision_log` 而正文永不更新（等于答案不落地）；人工块保护若不提供可复用入口，114-03 打回后的 `repo_rework`/`remerge` 重装会**静默抹掉人工编辑**。
Output: 一个受限面纯追加方法 + 两个新建 service 模块 + 两个测试文件。**无 migration、无新模型字段、无新 stage、无新事件常量。**（全相位唯一的模型字段与 migration 是 `BlueprintThread.last_reminded_at`，由 114-05 承载。）
</objective>

<execution_context>
工作目录：worktree 根 `/Users/zaneliu/Projects/open-source/friday-clean/.claude/worktrees/v0.20-blueprint`。**wave 3**，依赖 114-01（`open_thread(severity=)` / `append_note` 已就位）。

⚠️ **wave 顺序已在有界修订第 1 轮调整（原为 wave 4，在 114-03 之后）**：B1/B3 定夺要求 114-03 的 `ai_review` 入口显式接线 `aapply_thread_answers`（答案消费）、`areanchor_threads`（批量重锚）与人工块保护，而这三者都由**本 plan** 交付 ⇒ 本 plan 必须先于 114-03。功能上本 plan 只依赖 114-01；列 `depends_on: ["114-02"]` 是为**保持五 plan 全串行**——本相位多个 plan 的冻结面自检基于 `git diff --name-only` / `git status --porcelain` 的**全仓快照**（如 114-02 断言「新增项仅两个文件」），并行执行会让彼此的 untracked 新文件互相污染这些断言。串行的代价（吞吐）远低于「自检失效」的代价，故本轮不改为并行。

**受限面配额（硬约束）**：`blueprint_lifecycle_service.py` 的改动配额在 114-01 已用于前 1065 行；本 plan **只允许在文件尾纯追加**：
- `async def areanchor_threads(...)`（公开）+ `@sync_to_async def _reanchor_threads_sync(...)`（私有）；
- `__all__ += [...]` **不需要**（导出的是类，方法追加无需改导出面——若确实要改，逐行登记）。
- 验收：`git diff server/delivery/services/blueprint_lifecycle_service.py | rg "^-[^-]"` **为空**；本文件**只跑 `ruff check`，不跑 `ruff format`**。

⚠️ 字段名与签名一律按 114-RESEARCH §4 实测，**不得猜测**：
- `reanchor(anchor: dict, new_blocks: list[dict]) -> tuple[dict, str]`（`blueprint_anchor.py:66`）；`SIMILARITY_THRESHOLD = 0.85`（`:28`）、`ANCHOR_STATUS_ANCHORED = "anchored"` / `ANCHOR_STATUS_ORPHANED = "orphaned"`（`:30-31`）。三分支：block_id 精确命中 → **原对象返回**（`new_anchor is anchor`）；模糊 ≥0.85 → `dict(anchor, block_id=best)`（**入参不被原地修改**、`quoted_text` 保留原文）；否则 `(anchor, orphaned)`。同分取 `block_id` 字典序小者。空/非 dict anchor → 直接 `orphaned`。
- `iter_blocks(content) -> list[tuple[str, dict]]`（`blueprint_schema.py:919`）；`new_blocks = [b for _path, b in iter_blocks(new_content)]`。
- `diff_blueprint_blocks(old_content, new_content) -> {"added": [...], "removed": [...], "modified": [...]}`（`blueprint_schema.py:1044`，三组 block_id 列表，各组 `sorted`，`modified` 用 canonical JSON 比对）。
- `ArtifactService.add_version(artifact, content, *, produced_by_session_id="", produced_by_ref="") -> ArtifactVersion`（`artifact_service.py:117`）：**先校验后落库**（`:126-130`，失败 `raise ArtifactContentInvalid`）；幂等（`:145-162` 全程 `transaction.atomic`：`refresh_from_db(fields=["current_version"])` → 同 `content_hash` **直接返回 current，不建行不推进 `version_no`**）；hash 口径 `json.dumps(sort_keys=True, ensure_ascii=False, separators=(",",":"))` + sha256（`:43-47`）。
- `ArtifactVersion` 字段（`delivery/models/artifact.py:130-157`）：`version_no` / `supersedes`（FK，可空）/ `content` / `content_hash` / `produced_by_session_id`(64) / `produced_by_ref`(**255**)。
- `BlueprintThread` 字段（`blueprint_thread.py`）：`anchor`(:84，`{section_path, block_id, start_offset, end_offset, quoted_text}`) / `anchor_status`(:85，默认 `anchored`) / `created_on_version`(:75，FK 可空 SET_NULL) / `kind`(:91) / `severity`(:92) / `status`(:102) / `return_stage`(:108，`max_length=16`)。
- `BlueprintThreadMessage`（:128-159）：`author_type`(`ai`/`human`) / `author` FK 可空 / `body` / `ordering = ["created_at"]`。**无「结论」字段** ⇒ 结构化留痕只能进 `decision_log`。
- `decision_log` 弱 schema（`blueprint_schema.py:733-736`）；规格门形状 `{thread_id, question, answer, decided_at, decided_by}` + 去重键 `thread_id`（`blueprint_spec_gate._merge_decision_log:497-510`）；读侧 `_collect_prior_answers:583-593` 读 `item.get("question")` / `item.get("answer")`。
- `resolve_thread(thread, *, resolution="", initiated_by_user_id="system", dismissed=False)`（`:456`，幂等，终态 no-op 不覆盖首次结论）；`add_reviewer(artifact, user, first_action)`（`:215`，`aget_or_create`，已在名单原样返回、`first_action` 不覆盖）；`open_thread(..., severity=...)`（114-01 新签名，见 114-01-SUMMARY）。
- `.update()` / `bulk_update` **绕过 `auto_now`** ⇒ 必须显式带 `updated_at=timezone.now()`（`blueprint_lifecycle_service.py:243-244` 已立此纪律）。

两个新建模块可跑 `ruff format`。
</execution_context>

<context>
@.planning/phases/114-ai/114-CONTEXT.md（锁定：答案消费产新 ArtifactVersion 并复用 113 幂等口径；线程置 resolved 并记 `applied_in_version`；决策物化进 `decision_log` 保文档自包含；重锚定接线 block_id → quoted_text 0.85 → orphaned 且失锚不删可集中查询；人工 patch ops 经 REST → service 收口 → 新版本 `produced_by_ref="human_edit:{user_id}"`；**人工内容不被 AI 覆盖，冲突必须开线程询问**；编辑后仍须过 `validate_blueprint` 不合法直接拒绝且不落半合法版本；编辑者一并 upsert 进 `BlueprintReviewer`）
@.planning/phases/114-ai/114-RESEARCH.md §4.1（`add_version` 签名 + 幂等判定源码 + hash 口径 + **`decided_at` 用 `timezone.now()` 会每次翻版本**的推论）、§4.2（`reanchor` 契约三分支 + `new_blocks` 取法 + **`section_path` 不被 reanchor 更新**）、§4.3（`decision_log` 两个既有写入方形状表 + A5「必须保 `answer` 键」的断链后果）、Pitfalls P3（重锚定准平方级复杂度 + `diff_blueprint_blocks` 预筛的降幂方案 + `bulk_update` 一次写回；**默认不改 `blueprint_anchor.py`**）
@.planning/phases/114-ai/114-PATTERNS.md 第 6 类（`blueprint_confirm_gate.alock` 的**五步固定顺序**：读最新版本作基线 → `deepcopy` 改 content → `decision_log` 经 `_merge_decision_log` 去重合并 → `add_version` → 成功后才 `resolve_thread` + `add_reviewer` + `_emit`；CAS 基线防交错；`ArtifactContentInvalid` fail-closed；三条链路的 `produced_by_ref` 对照表）、第 7 类（`areanchor_threads` 的目标形态 + 111 单测立的**六条行为契约**逐条）、Shared Patterns（best-effort / async ORM 防裸 lazy-FK / `.update()` 绕过 auto_now / 受限文件纯追加纪律）、No Analog Found 第 2 项（批量重锚定零先例的具体要求）
@server/delivery/services/blueprint_anchor.py（**全文 107 行**：`_block_text` 四型块口径、`reanchor` 三分支与同分字典序、三个常量）
@server/tests/delivery/test_blueprint_anchor.py（**111-02 单测＝期望调用形状**：L35-43 精确命中原对象返回、L57-61 模糊命中保留 quoted_text 且入参不被原地改、同分字典序、失锚不删不改、阈值边界用 `difflib` 现算、`_block_text` 四型块）
@server/services/process_runtime/blueprint_confirm_gate.py（L492-621 `alock` 全链路：L546-562 读最新版本 + deepcopy + `decision_log` 合并、L581-609 `add_version` + `ArtifactContentInvalid` fail-closed + `resolve_thread` + `add_reviewer`；L935-953 `_merge_decision_log` 去重键）
@server/services/process_runtime/blueprint_spec_gate.py（L497-510 `_merge_decision_log` 按 `thread_id` 去重、L525-596 `_collect_prior_answers` 的 `decision_log` **读侧口径**（`item.get("answer")`）与指纹去重——「同一问题不重复问」的判据来源）
@server/delivery/services/artifact_service.py（L43-47 hash 口径、L117-162 `add_version` 先校验后落库 + 同 hash 幂等 + supersedes 链）
@server/delivery/services/blueprint_lifecycle_service.py（L456-483 `resolve_thread` 形态（`areanchor_threads` 照它的「公开 async = 校验 + 委托 `@sync_to_async` 事务 + 结构化日志三段」）、L215-228 `add_reviewer`、L229-279 `_apply_transition_sync` 的 `updated_at=timezone.now()` 纪律、L1159-1165 文件尾与 `__all__ +=` 位置）
@server/delivery/models/artifact.py（L66-128 `Artifact.current_version` / `blueprint_status`、L130-157 `ArtifactVersion` 全字段含 `supersedes` / `produced_by_ref` 长度）
@server/delivery/models/blueprint_thread.py（L69-122 全字段与三条索引，尤其 `anchor` / `anchor_status` / `created_on_version`）
@server/tests/delivery/test_blueprint_gate_api.py（L1-23 「守 N 件事」体例 + 「断言从 DB 重读，不信响应体」）
</context>

<tasks>

<task type="auto">
  <name>Task 1: areanchor_threads 批量重锚定（diff 预筛 + section_path 刷新 + bulk_update + orphaned 不删）+ apply_block_ops 三 op 纯函数</name>
  <files>server/delivery/services/blueprint_lifecycle_service.py, server/delivery/services/blueprint_block_edit.py</files>
  <read_first>server/delivery/services/blueprint_lifecycle_service.py L456-483（`resolve_thread` 的三段形态：公开 async 校验 + 委托 `@sync_to_async` + structlog）、L543-576（`_resolve_thread_sync` 的 `@sync_to_async` + `transaction.atomic` + 显式 `updated_at` 写法）、L1155-1165（**文件尾的确切位置**与 `__all__ +=` 现状——追加点在此之后）；server/delivery/services/blueprint_anchor.py 全文（`reanchor` 三分支的**返回语义**：精确命中返回**同一对象**故不可用 `is not anchor` 判断是否变更；`ANCHOR_STATUS_*` 常量名）；server/tests/delivery/test_blueprint_anchor.py 全文（六条行为契约——批量侧必须逐条保持）；server/services/process_runtime/blueprint_schema.py L919-960（`iter_blocks` 的 `(section_path, block)` 二元组与 path 生成）、L1044-1090（`diff_blueprint_blocks` 的三键返回与 `modified` 判定方式）</read_first>
  <action>
**① `blueprint_lifecycle_service.py` 文件尾纯追加**（删除行必须为 0）：

```
    async def areanchor_threads(
        self,
        artifact: Artifact,
        new_content: dict,
        *,
        old_content: dict | None = None,
        initiated_by_user_id: str = "system",
    ) -> dict:
```
docstring（这段是给 114-05 与 115 的契约书）：
- 「新版本装配/编辑后把该 artifact 全部线程的 anchor 重挂到新 block 序列。返回**恒定四键** `{"checked": int, "reanchored": int, "orphaned": int, "skipped": int}`（下游无需判空分支）。」
- 「**性能防护（P3）**：`reanchor` 对每条线程遍历全部新 block 做 `difflib.SequenceMatcher.ratio()`（准平方级），而本方法挂在**同步请求路径**（人工编辑 → 产新版本 → 重锚定）。故先用 `diff_blueprint_blocks(old_content, new_content)` 求出 `added ∪ removed ∪ modified` 的变动块集合，**只对 anchor.block_id 落在变动集合内（或 block_id 为空/已消失）的线程**走 `reanchor`，其余计入 `skipped` —— 把『改一两个块』的常见场景从 O(N×M) 降到 O(1)。`old_content is None` 时**退化为全量重锚**（正确性优先于性能）。**不改 `blueprint_anchor.py`**（111 交付、单测锁死 0.85 阈值与同分字典序）。」
- 「⚠️ `reanchor` **不更新 `anchor.section_path`**（只改 `block_id`）。本方法用 `iter_blocks(new_content)` 的 path 一并刷新 `anchor["section_path"]` —— 115 渲染靠它定位，漏刷会让批注挂在错误的段落标题下。」
- 「**失锚不删**：`anchor_status="orphaned"` 的线程行原样保留（`filter(anchor_status='orphaned')` 可集中查询，供 115 展示『失锚评论』）。」
- 「best-effort：任何异常 → warning + 返回已积累计数，**绝不上抛**（重锚定失败不该让『人工编辑成功』变成 5xx；版本已落，下一次编辑会再试）。」

实现：公开方法只做入参归一（`new_content` 非 dict → 返回全 0 四键）+ 委托私有 `@sync_to_async def _reanchor_threads_sync(...)` + structlog（`blueprint_threads_reanchored`，`category="caller"`、`component="blueprint_lifecycle"`、`artifact_id`、四键计数、`initiated_by_user_id`、`duration_ms`；**anchor 内容与 quoted_text 绝不进日志**）。

`_reanchor_threads_sync` 内（`transaction.atomic`）：
1. `blocks = [b for _path, b in iter_blocks(new_content)]`；`path_by_block_id = {b.get("block_id"): path for path, b in iter_blocks(new_content) if b.get("block_id")}`（函数内 lazy import `iter_blocks`，避免 delivery → process_runtime 的顶层耦合）；
2. `changed = set(diff["added"]) | set(diff["removed"]) | set(diff["modified"])`（`old_content` 为 None 时 `changed = None` 表示「不预筛」）；
3. `rows = list(BlueprintThread.objects.filter(artifact=artifact).only("id", "anchor", "anchor_status"))`；
4. 逐行：`anchor` 非 dict/空 → `reanchor` 会直接给 `orphaned`（照契约，不特判）；`changed is not None` 且 `anchor.get("block_id")` 既**不在** `changed` 又**仍在** `path_by_block_id` → `skipped += 1` 且 `section_path` 若与 `path_by_block_id[bid]` 不同则仍需刷新（**刷新也算一次写**，进 `to_update`）；否则调 `reanchor(anchor, blocks)` → `(new_anchor, status)`；
5. `status == anchored` 时把 `new_anchor` 做一次 `dict(new_anchor)` 拷贝再写 `section_path = path_by_block_id.get(new_anchor["block_id"], new_anchor.get("section_path", ""))`（⚠️ **精确命中分支返回的是同一对象**，直接改会原地修改入参 dict——111 单测第 2 条契约明写「入参不被原地修改」，这里必须拷贝）；
6. 变更行进 `to_update`（同时设 `thread.updated_at = timezone.now()`，因为 `bulk_update` **绕过 auto_now**）；
7. `BlueprintThread.objects.bulk_update(to_update, ["anchor", "anchor_status", "updated_at"])` **一次落库**（不逐行 save）；
8. 返回四键计数。

**② 新建 `server/delivery/services/blueprint_block_edit.py` 的纯函数节**（service 收口归 Task 2）：
模块 docstring 三段（照 `blueprint_reconcile` 体例）：① 「本模块 = block 级 patch 的纯函数节 + service 收口节；纯函数节无 IO / 无 ORM」；② 「patch 来自**用户**（半可信但需可回显）：未知 op、找不到 `block_id`、缺必填字段一律进 `rejected` 清单**如实上报**，绝不静默跳过（静默跳过 = 用户以为改了其实没改，是最坏的编辑体验）」；③ 「合法性由 `validate_blueprint` 兜最终底：本节只做结构操作，不做语义校验」。

```
def apply_block_ops(content: Any, ops: Any) -> tuple[dict, list[dict]]:
```
- 返回 `(new_content, rejected)`；`new_content = copy.deepcopy(content)`（**入参不被原地修改**）；
- op 形状 `{"op": "replace"|"insert"|"delete", "block_id": str, "block": dict | None, "position": "before"|"after"}`（`insert` 用 `block_id` 作锚点 + `position`，缺省 `after`；`insert` 的新 block 必须自带非空 `block_id`，否则 `rejected`）；
- 定位走 `iter_blocks` 求 `(section_path, block)`，再按 section_path 反查其所在 list 容器与索引——内部 helper `_locate(content, block_id) -> tuple[list | None, int]`（逐层 `.get` + `isinstance` 防御）；
- `replace`：整块替换（保留原 `block_id`，防用户改 id 把线程 anchor 全打散——`block_id` 不一致时以**原 id** 为准并在 `rejected` 里记一条 `reason="block_id_immutable"` 的**提示级**条目）；
- `insert`：在锚点前/后插入；`delete`：移除该块；
- `rejected` 条目形状 `{"op", "block_id", "reason"}`，`reason ∈ {unknown_op, block_not_found, missing_block, missing_block_id, block_id_immutable}`；
- 整函数 `try/except Exception` → 返回 `(deepcopy(content), rejected + [{"reason": "apply_failed"}])`（**绝不外抛**，也绝不返回半改内容）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/delivery/test_blueprint_anchor.py tests/delivery/test_blueprint_lifecycle_service.py tests/delivery/test_blueprint_inv6_guard.py -x -q</automated>
  </verify>
  <acceptance_criteria>
- `rg -n "async def areanchor_threads" server/delivery/services/blueprint_lifecycle_service.py` 命中；`rg -n "def _reanchor_threads_sync" server/delivery/services/blueprint_lifecycle_service.py` 命中。
- ⭐ `git diff server/delivery/services/blueprint_lifecycle_service.py | rg "^-[^-]"` **必须为空**（删除行 = 0，纯追加）。
- `rg -n "diff_blueprint_blocks" server/delivery/services/blueprint_lifecycle_service.py` 命中（P3 预筛已实现）；`rg -n "quick_ratio" server/delivery/services/blueprint_lifecycle_service.py` **零命中**（不改 111 算法本体的路线）。
- `git diff server/delivery/services/blueprint_anchor.py` 输出**为空**（111 算法一行不改）。
- `rg -n "bulk_update" server/delivery/services/blueprint_lifecycle_service.py` 命中；`rg -n 'updated_at=timezone.now\(\)|updated_at = timezone.now\(\)' server/delivery/services/blueprint_lifecycle_service.py | rg -c ""` ≥2（`bulk_update` 绕过 auto_now 已补）。
- `rg -n 'section_path' server/delivery/services/blueprint_lifecycle_service.py` 命中（path 刷新已实现）。
- `rg -n "\.delete\(\)" server/delivery/services/blueprint_lifecycle_service.py` **零命中**（失锚线程不删）。
- `rg -n "def apply_block_ops" server/delivery/services/blueprint_block_edit.py` 命中；`rg -n "unknown_op|block_not_found|block_id_immutable" server/delivery/services/blueprint_block_edit.py` 三个 token 全命中。
- `cd server && uv run python -c "from delivery.services.blueprint_block_edit import apply_block_ops as f
base = {'schema_version': 'blueprint/v1'}
for bad in (None, {}, 'x', 123):
    out, rej = f(bad, [{'op': 'delete', 'block_id': 'blk_x'}])
    assert isinstance(out, dict) and isinstance(rej, list), (out, rej)
out, rej = f(base, [{'op': 'frobnicate', 'block_id': 'b'}])
assert rej and rej[0]['reason'] == 'unknown_op', rej
print('apply_block_ops OK')"` 输出 `apply_block_ops OK`。
- `cd server && uv run pytest tests/delivery/ -q` 无新增失败（114-01 的线程底座 + 112-05 确认门八端点 + INV-6 扫描全绿 ⇒ 追加未破坏既有语义）。
- `cd server && uv run python manage.py makemigrations --check --dry-run` 退出码 0。
- `cd server && uv run ruff check delivery/services/blueprint_lifecycle_service.py delivery/services/blueprint_block_edit.py` 通过；`ruff format --check` **只对 `blueprint_block_edit.py`** 跑并通过（`blueprint_lifecycle_service.py` 守受限面纪律不跑 format）。
  </acceptance_criteria>
  <done>批量重锚定就位且保持 111 单测立的六条契约（含「入参不被原地修改」）；`section_path` 一并刷新、失锚置 orphaned 不删、一次 `bulk_update` 且显式带 `updated_at`；`diff_blueprint_blocks` 预筛让未变动块的线程直接跳过；patch 三 op 纯函数恒不抛且 `rejected` 如实上报；受限面删除行 0。</done>
</task>

<task type="auto">
  <name>Task 2: 人工 block 编辑 service 收口（validate → human_edit 版本 → 重锚定 → reviewer upsert）+ 澄清回灌三步链（decision_log 物化 + AI 不覆盖人工）</name>
  <files>server/delivery/services/blueprint_block_edit.py, server/services/process_runtime/blueprint_reflow.py</files>
  <read_first>server/services/process_runtime/blueprint_confirm_gate.py L492-621（**五步固定顺序的逐行写法**：`_aload_latest_version` 取最新版本作基线（注释解释为何不读 session 那一版）→ `copy.deepcopy` → `decision_log` 合并 → `add_version(produced_by_session_id=..., produced_by_ref=...)` 的 `try/except ArtifactContentInvalid` fail-closed 分支（warning + 返回待处置结果）→ 成功后才 `resolve_thread` + `add_reviewer`；以及 L543-579 的 `thread.updated_at` 快照 CAS 防交错）、L935-953（`_merge_decision_log` 去重键实现）；server/services/process_runtime/blueprint_spec_gate.py L497-510（按 `thread_id` 去重的 `_merge_decision_log`）、L525-596（`_collect_prior_answers`：读 `decision_log[].question` / `[].answer` 的**确切键名**、`decided_by` 的三档口径、`ThreadAuthorType.AI` 判断问题体的方式、`answered_threads` 的过滤条件）；server/delivery/services/artifact_service.py L117-162（`add_version` 的**确切签名与幂等分支**、`ArtifactContentInvalid` 抛出点）；server/services/process_runtime/blueprint_schema.py L1044-1090（`diff_blueprint_blocks`——冲突判据的唯一实现）；server/delivery/models/artifact.py L130-157（`supersedes` FK 的 `related_name` 与可空性——取「人工版本的前一版」靠它）</read_first>
  <action>
**① `blueprint_block_edit.py` 追加 service 收口节**：

```
async def aapply_block_edit(
    artifact: Any,
    ops: Any,
    *,
    user: Any = None,
    initiated_by_user_id: str = "system",
    session_id: str = "",
    artifact_service: Any = None,
    lifecycle_service: Any = None,
) -> dict:
```
docstring 写明**恒定返回键** `{"status": "applied"|"unchanged"|"invalid"|"rejected", "version_id": str, "version_no": int, "rejected": list, "detail": str, "reanchor": dict}` 与**五步固定顺序**（照 `alock`）：
1. **读最新版本作基线**（`ArtifactVersion.objects.filter(artifact_id=...).order_by("-version_no").afirst()`，注释：**绝不读 `session.current_artifact_version`**——上游 `add_version` 已推进 current，读 session 那一版会把 AI 成果覆盖回旧内容，`blueprint_confirm_gate.py:546-550` 同源坑）；无版本 → `{"status": "invalid", "detail": "蓝图尚无版本，无法编辑"}`；
2. `new_content, rejected = apply_block_ops(base.content, ops)`；`rejected` 中存在**硬失败**（`unknown_op` / `block_not_found` / `missing_block` / `missing_block_id`）→ `{"status": "rejected", "rejected": rejected}` 且**不落版本**（提示级 `block_id_immutable` 不阻断，随成功结果一并回显）；
3. ⭐ **显式 `validate_blueprint(new_content)`**（lazy import）：`ok is False` → `{"status": "invalid", "detail": <error>}`（`error` 已由 `_format_error` 脱敏截断 500 字符，可直接回显中文错因），**不落版本、不落半合法版本**；
4. `add_version(artifact, new_content, produced_by_session_id=session_id, produced_by_ref=f"human_edit:{user_id}")`（`user_id` 取 `getattr(user, "id", "")` 或 `initiated_by_user_id`；⚠️ `produced_by_ref` `max_length=255`，`user_id` 是 uuid ⇒ 长度安全）。`try/except ArtifactContentInvalid` → warning（`redact_secrets_in_text`）+ `{"status": "invalid", ...}`；**同 content_hash 复用 current 时**（`new_version.id == base.id`）→ `{"status": "unchanged", ...}`（版本未翻，也**不必**重锚定）；
5. 成功落新版本后：`await lifecycle.areanchor_threads(artifact, new_content, old_content=base.content, initiated_by_user_id=...)` → `await lifecycle.add_reviewer(artifact, user, "block_edit")`（`user` 为 None 时跳过）→ 结构化日志（`blueprint_block_edit_applied`，`category="caller"`、`component="blueprint_block_edit"`、`artifact_id` / `version_no` / `op_count` / `rejected_count` / 四键重锚计数 / `initiated_by_user_id` / `duration_ms`；**block 正文绝不进日志**）。

**② 新建 `server/services/process_runtime/blueprint_reflow.py`**（澄清答案回灌）。模块 docstring 四段：① 「本模块把**已作答**的澄清/审查线程消费成新版本：改 content → `add_version` → `transition`，**顺序固定为先落版本再转状态**（否则状态已 `drafting` 而内容未更新的窗口里 AI 会拿旧内容重跑）」；② 「INV-6：本模块零 ORM 写，线程与状态一律经 `BlueprintLifecycleService`，版本经 `ArtifactService`」；③ 「**AI 不覆盖人工**：回灌前先算冲突，交集非空则不写版本、改开阻塞线程询问」；④ 「幂等：`decided_at` 取**线程作答消息的 `created_at`** 而非 `timezone.now()` —— 回灌是可重放路径，时间戳每次变会改 `content_hash`（`sort_keys=True` 只消除 key 顺序影响），每次都翻新版本，破坏『同 hash 不翻版本』的幂等意图」。

```
def build_decision_entries(threads_payload: list[dict]) -> list[dict]
def merge_decision_log(existing: Any, entries: list[dict]) -> list[Any]
def detect_human_conflicts(*, human_version_content, human_base_content, ai_new_content) -> list[str]
async def ablock_section_writer(content: dict, answers: list[dict], *, session=None) -> dict
async def aapply_thread_answers(
    artifact, *, threads=None, session=None, initiated_by_user_id="system",
    section_writer=None, artifact_service=None, lifecycle_service=None,
) -> dict
async def acollect_human_block_ids(artifact) -> list[str]
async def arestore_human_blocks(
    artifact, *, initiated_by_user_id="system", session=None,
    artifact_service=None, lifecycle_service=None,
) -> dict
```
- `build_decision_entries`：条目形状 **`{thread_id, question, answer, decided_at, decided_by, applied_in_version}`**（⚠️ **必须保 `answer` 键**——`blueprint_spec_gate._collect_prior_answers:587` 读的是 `item.get("answer")`；只写 `decision` 会让「同一问题不再重复问」在审查阶段断链）。`question` 取线程首条 `author_type == ai` 的消息 body；`answer` 取全部 `author_type == human` 的消息 `"；".join`；`decided_at` = **最后一条 human 消息的 `created_at.isoformat()`**；`decided_by` 默认 `"human"`，有 `author_id` 取 `str(author_id)`，AI 侧写 `"ai"`（照 `blueprint_spec_gate.py:548, 558-559`）。
- `merge_decision_log`：按 `thread_id` 去重追加（逐字照 `blueprint_spec_gate._merge_decision_log:497-510` 的逻辑，注释「口径同源：两处漂移会让同一线程在导出时出现两条互相矛盾的决策」）。
- `detect_human_conflicts`（**纯函数**，AI 不覆盖人工的判据）：`human_changed = added ∪ removed ∪ modified of diff_blueprint_blocks(human_base_content, human_version_content)`；`ai_changed = 同上 of diff_blueprint_blocks(human_version_content, ai_new_content)`；返回 `sorted(human_changed & ai_changed)`。docstring：「交集非空 ⇒ AI 要改的正是人手动改过的块 ⇒ **不写版本**，开线程询问（镜像 `ProjectContextLink` 的『AI 不覆盖人工』原则）。人工版本的『前一版』经 `ArtifactVersion.supersedes` 取，`produced_by_ref` 以 `human_edit:` 开头才算人工版本。」
- ⭐ **`ablock_section_writer`（B1 的生产段落重产实现，`aapply_thread_answers` 的默认 writer）**：
  - 入参 `content`（已 deepcopy 的蓝图）+ `answers`（`build_decision_entries` 的条目，含 `thread_id` / `question` / `answer` / `anchor`）；返回**新的 content dict**（原地改的是已 deepcopy 的副本，绝不改调用方原对象）。
  - 逐条：按 `anchor.block_id` 用 `iter_blocks(content)` 定位块；**找不到就跳过**（记 warning，不新建块——凭 LLM 猜落位是把答案写到错误段落的最快路径）。
  - 单块单次 LLM 改写：**逐条照 `blueprint_ambiguity_score.ascore_ambiguity`（`:360-458`）的六步骨架**——重依赖全在函数内 lazy import（`langchain_core.messages` / `CallSource, use_call_source` / `build_chat_model` / `ProviderConfigService` / `redact_secrets_in_text`）；`ProviderConfigService.aresolve()` 取 `extra.default_model`，**缺则 warning + 该块原样保留**；`with use_call_source(CallSource.BLUEPRINT_AI_REVIEW): await model.ainvoke(...)`（**不新增 `CallSource` 枚举值**）；解析走 `_parse_object_json` 期望 `{"text": str}`，失败 ⇒ 该块原样保留。
  - 写回**只改块的文本字段**（读侧口径复用 `blueprint_anchor._block_text` 判断块型），`block_id` / `block_type` **逐字不变**（改 id 会把该块上的全部线程 anchor 打散）。
  - 上界：`_MAX_REWRITE_BLOCKS = 5`（一轮最多改写 5 块，超出的答案只进 `decision_log` 并记 warning）、`_MAX_BLOCK_PROMPT_CHARS`（prompt 裁剪）、`_MAX_REWRITTEN_TEXT_CHARS`（回写截断）。
  - 事件/日志：`blueprint_reflow_section_rewritten`，`category="sampling"`、`component="process_runtime"`，只记 `block_count` / `rewritten_count` / `skipped_count` / `duration_ms`；**问题、答案与块正文绝不进日志**。
  - 整函数 `try/except Exception` → warning（脱敏）+ 返回**未改写的 content**（`# noqa: BLE001`）。docstring 写明「**best-effort 但不静默丢答案**：任一块改写失败只影响该块正文，`decision_log` 物化与线程收尾照常由 `aapply_thread_answers` 完成 ⇒ 答案永远可追溯」。
- ⭐ **`acollect_human_block_ids` / `arestore_human_blocks`（B3 的人工块保护入口；本 plan 只交付+单测，接线归 114-03）**：
  - `acollect_human_block_ids(artifact) -> list[str]`：**只读**查 `ArtifactVersion.objects.filter(artifact=artifact, produced_by_ref__startswith="human_edit:").order_by("version_no")`（`select_related("supersedes")` 防 async 裸 lazy-FK）；每条与其 `supersedes.content` 做 `diff_blueprint_blocks` 取 `added ∪ modified`，全部并集后 `sorted()` 返回（确定性）。无人工版本 → `[]`。整体 `try/except` → `[]`（保护读失败不该阻断审查）。docstring：「保护集 = 版本链中 `produced_by_ref` 带 `human_edit:` 的版本各自改过的 block_id 并集 —— 这是『哪些块是人写的』的**唯一判据**（`ArtifactVersion` 无 `created_by_user_id`，`produced_by_ref` 前缀是本仓唯一的人工归属通道）。」
  - `arestore_human_blocks(...) -> dict`：恒定键 `{"status": "noop"|"unchanged"|"restored"|"conflict", "preserved": list[str], "conflicted": list[str], "thread_id": str, "version_id": str, "version_no": int}`。步骤：
    1. `protected = await acollect_human_block_ids(artifact)`；空 → `noop`（零查询开销路径）。
    2. 读**最新**版本作当前态（`order_by("-version_no").afirst()`，⚠️ 不读 `session.current_artifact_version`）；取最近一条 `human_edit:` 版本作**人工基准**。
    3. 逐个 `block_id ∈ protected`：取当前态块与人工基准块，用 **canonical JSON**（`json.dumps(sort_keys=True, ensure_ascii=False, separators=(",",":"))`，与 `artifact_service` 的 hash 口径同源）比对：
       - 相等 ⇒ 无事（人工内容仍在）；
       - **当前态块缺失或不等 ⇒ 实质冲突**：把**人工基准块写回** deepcopy 后的当前 content（⇒ 人工编辑不被抹掉），并记进 `conflicted`；
       - 写回成功的块同时记进 `preserved`。
    4. `conflicted` 非空 ⇒ `open_thread(artifact, kind=ThreadKind.AI_CLARIFICATION, blocking=True, severity="", question=<列出冲突 block_id + 「AI 重装与人工编辑冲突，已保留人工版本，请裁决」，**不贴两侧正文**>, anchor={"block_id": conflicted[0]}, created_on_version=<当前最新版本>, return_stage="ai_reviewing", initiated_by_user_id=...)`（⛔ **绝不静默覆盖**）。
    5. content 有实际变化 ⇒ `add_version(artifact, content, produced_by_ref=f"human_block_restore:{base.version_no}")`（`ArtifactContentInvalid` → warning + `status="noop"`，**不落半合法版本**）；同 hash 复用 current ⇒ `status="unchanged"`（不翻版本）。有冲突线程 ⇒ `status="conflict"`，否则 `status="restored"`。
    6. 落新版本后 `await lifecycle.areanchor_threads(artifact, content, old_content=base.content, ...)`。
    整体 `try/except Exception` → warning（脱敏）+ `{"status": "noop", ...}`，**绝不上抛**（保护失败不该把 stage 打成异常，但 114-03 会据 `status` 决定是否停等）。
- `aapply_thread_answers` 七步（恒定返回键 `{"status": "applied"|"unchanged"|"conflict"|"invalid"|"noop", "version_id", "version_no", "thread_ids", "conflict_block_ids", "thread_id", "detail"}`）：
  1. 取**最新**版本作基线（同上纪律）；无版本 → `noop`；
  2. 收集待消费线程：`threads` 显式传入优先；否则查该 artifact 上 `status=ANSWERED` 的线程（`kind ∈ {ai_clarification, ai_review_finding}`）+ 其消息（**只读查询，adapter 允许直查**）；空 → `noop`；
  3. `content = copy.deepcopy(base.content)`；`writer = section_writer or ablock_section_writer` → `content = await writer(content, answers, session=session)`（⭐ **B1：`None` 不再等于「跳过段落重产」**——缺省即走生产实现 `ablock_section_writer`。docstring 逐字写明「若默认为 no-op，答案只进 `decision_log` 而蓝图正文永不更新 ⇒ SC-2/SC-3 的『答案回灌产新版本』只剩一条日志行，等于答案不落地。测试要 no-op 时**显式注入桩**，不靠默认值」）。writer 返回非 dict 或抛异常 ⇒ 回落到未改写的 `content`（warning，不上抛）；
  4. ⭐ **冲突检测**：若 `base.produced_by_ref` 以 `"human_edit:"` 开头（或沿 `supersedes` 链上溯最近的人工版本），取 `human_base = base.supersedes.content` → `conflicts = detect_human_conflicts(...)`；非空 → `open_thread(kind=ThreadKind.AI_CLARIFICATION, blocking=True, question=<列出冲突 block_id 与「AI 修订与人工编辑冲突，请裁决」，**不贴两侧正文**>, anchor={"block_id": conflicts[0]}, created_on_version=base, return_stage="ai_reviewing", initiated_by_user_id=...)` → 返回 `{"status": "conflict", "thread_id": ..., "conflict_block_ids": conflicts}`，**不落版本**；
  5. `entries = build_decision_entries(...)`；`content["decision_log"] = merge_decision_log(content.get("decision_log"), entries)`；
  6. `add_version(artifact, content, produced_by_session_id=..., produced_by_ref=f"ai_review_reflow:{primary_thread_id}")`，`try/except ArtifactContentInvalid` → warning + `{"status": "invalid", "detail": ...}`（**不落半合法版本、不落 failed**）。同 hash 复用 current → `status="unchanged"`（**不重复 resolve、不重复重锚**）；
  7. 落新版本后：`await lifecycle.resolve_thread(thread, resolution=f"答案已回灌，产出版本 v{new_version.version_no}。", initiated_by_user_id=...)`（逐条，单条失败 warning 不上抛）→ `await lifecycle.areanchor_threads(artifact, content, old_content=base.content, ...)` → 结构化日志（`blueprint_reflow_applied`，`category="caller"`、`component="process_runtime"`、`thread_count` / `version_no` / 四键重锚计数 / `duration_ms`；**问题与答案正文绝不进日志**）。

  ⚠️ **`applied_in_version` 的落地口径（定案，必须写进 docstring 并在 SUMMARY 登记）**：条目里的 `applied_in_version` 取**基线版本 id**（`str(base.id)`，即「答案是在哪一版之上被应用的」）。理由：产出版本的 id 由 `add_version` 在写库时生成，写入前不可知；若为了填自身 id 而二次 `add_version`，会额外翻一版并破坏「同 hash 不翻版本」的幂等意图。基线 id 写入前已知且**可重放稳定**（重放时 `merge_decision_log` 按 `thread_id` 去重，条目值不变 ⇒ hash 不变 ⇒ 不翻版本）。**产出版本的反查方式**（115 消费面依赖，写进 docstring）：`ArtifactVersion.objects.filter(artifact=..., produced_by_ref=f"ai_review_reflow:{thread_id}")`，或沿 `supersedes` 链取 base 的后继。
  整个 `aapply_thread_answers` 外层 `try/except Exception` → warning（脱敏）+ 返回 `{"status": "invalid", "detail": "reflow_failed"}`，**绝不上抛**（回灌失败不该把会话打成 FAILED）。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/delivery/ -q --no-header -x</automated>
  </verify>
  <acceptance_criteria>
- `rg -n "async def aapply_block_edit" server/delivery/services/blueprint_block_edit.py` 命中；`rg -n 'human_edit:' server/delivery/services/blueprint_block_edit.py` 命中。
- `rg -n "validate_blueprint" server/delivery/services/blueprint_block_edit.py` 命中（编辑后显式校验，回显中文错因）；`rg -n "ArtifactContentInvalid" server/delivery/services/blueprint_block_edit.py` 命中（fail-closed 分支存在）。
- `rg -n "areanchor_threads" server/delivery/services/blueprint_block_edit.py server/services/process_runtime/blueprint_reflow.py` 两文件各 ≥1 命中（编辑与回灌都接了重锚定）。
- `rg -n "add_reviewer" server/delivery/services/blueprint_block_edit.py` 命中（编辑者 upsert）。
- `rg -n "def detect_human_conflicts|def build_decision_entries|def merge_decision_log|async def aapply_thread_answers" server/services/process_runtime/blueprint_reflow.py` 四条全命中。
- ⭐ B1：`rg -n "async def ablock_section_writer" server/services/process_runtime/blueprint_reflow.py` 命中；`rg -n "section_writer or ablock_section_writer" server/services/process_runtime/blueprint_reflow.py` 命中（**默认走生产实现**，不是 no-op）；`rg -n "_MAX_REWRITE_BLOCKS" server/services/process_runtime/blueprint_reflow.py` 命中；`rg -n "CallSource.BLUEPRINT_AI_REVIEW" server/services/process_runtime/blueprint_reflow.py` 命中且 `git diff server/agents/call_source.py` 输出为空（零新增枚举值）。
- ⭐ B3：`rg -n "async def acollect_human_block_ids|async def arestore_human_blocks" server/services/process_runtime/blueprint_reflow.py` 两条全命中；`rg -n 'produced_by_ref__startswith="human_edit:"' server/services/process_runtime/blueprint_reflow.py` 命中；`rg -n "human_block_restore:" server/services/process_runtime/blueprint_reflow.py` 命中；`rg -n "created_by_user_id" server/services/process_runtime/blueprint_reflow.py` **零命中**（`ArtifactVersion` 无该字段）。
- ⭐ B3 恒定返回键断言：`cd server && uv run python -c "import inspect
from services.process_runtime.blueprint_reflow import arestore_human_blocks as f, acollect_human_block_ids as g
for fn in (f, g):
    assert inspect.iscoroutinefunction(fn), fn
p = inspect.signature(f).parameters
assert list(p)[0] == 'artifact' and all(v.kind is inspect.Parameter.KEYWORD_ONLY for k, v in p.items() if k != 'artifact'), p
print('B3 entry signatures OK')"` 输出 `B3 entry signatures OK`。
- `git diff server/services/process_runtime/blueprint_review.py` 输出**为空**（B3 保护逻辑落在 `blueprint_reflow.py`，本 plan 不碰 114-02 的判定内核；接线归 114-03）。
- `git diff server/services/process_runtime/blueprint_merge.py` 输出**为空**（**B3 定夺：不改 `blueprint_merge.py`**，保持只读受限）。
- ⭐ `rg -n '"answer"' server/services/process_runtime/blueprint_reflow.py` 命中（`decision_log` 保 `answer` 键）。
- ⭐ `rg -n "timezone.now\(\)" server/services/process_runtime/blueprint_reflow.py` **零命中**（`decided_at` 取作答消息 `created_at`，保 hash 幂等）。
- `rg -n "diff_blueprint_blocks" server/services/process_runtime/blueprint_reflow.py` 命中（冲突判据用既有纯函数）。
- ⛔ `rg -n "record_answer" server/services/process_runtime/blueprint_reflow.py server/delivery/services/blueprint_block_edit.py` **零命中**。
- `rg -n "session.current_artifact_version|current_artifact_version_id" server/services/process_runtime/blueprint_reflow.py server/delivery/services/blueprint_block_edit.py` **零命中**（基线只取最新 `version_no`）。
- `rg -n "BlueprintThread.objects.(create|acreate|update|bulk_update)|ArtifactVersion.objects.(create|acreate|update)" server/services/process_runtime/blueprint_reflow.py server/delivery/services/blueprint_block_edit.py` **零命中**（INV-6：零 ORM 写）。
- `rg -n "\.deepcopy\(|copy.deepcopy" server/services/process_runtime/blueprint_reflow.py` 命中（不原地改 version.content）。
- `cd server && uv run pytest tests/delivery/ tests/services/process_runtime/ -q` 无新增失败；`cd server && uv run pytest tests/delivery/test_blueprint_inv6_guard.py -q` 全绿。
- `cd server && uv run python manage.py makemigrations --check --dry-run` 退出码 0。
- `cd server && uv run ruff check delivery/services/blueprint_block_edit.py services/process_runtime/blueprint_reflow.py && uv run ruff format --check delivery/services/blueprint_block_edit.py services/process_runtime/blueprint_reflow.py` 通过。
  </acceptance_criteria>
  <done>人工编辑经 service 收口：不合法拒绝且不落版本、合法落 `human_edit:{user_id}` 版本并自动重锚定与 reviewer upsert、同 hash 返回 `unchanged`；澄清回灌三步链顺序固定且 `decision_log` 保 `answer` 键与稳定 `decided_at`；AI 与人工冲突时不写版本改开阻塞线程；两模块零 ORM 写、零 `record_answer`、基线绝不读 session 那一版。</done>
</task>

<task type="auto">
  <name>Task 3: 测试（重锚定四分支 + patch 三 op + 不合法不落版本 + 同 hash 不翻版本 + 同一问题不重复问 + AI 不覆盖人工）</name>
  <files>server/tests/delivery/test_blueprint_reanchor_edit.py, server/tests/services/process_runtime/test_blueprint_reflow.py</files>
  <read_first>server/tests/delivery/test_blueprint_anchor.py 全文（六条行为契约的**断言写法**，尤其「阈值边界用 `difflib` 现算断言，不硬编码猜测值」与「`new_anchor is anchor`」）；server/tests/delivery/test_blueprint_gate_api.py L1-23（「守 N 件事」编号清单 + 「断言从 DB 重读，不信响应体」）；server/tests/delivery/test_blueprint_lifecycle_service.py（fixture 工厂：artifact / version / thread 的构造 helper —— **复用，不重复造**；`pytest.mark.django_db(transaction=True)` 的位置）；server/tests/helpers/blueprint_samples.py（`make_blueprint` 签名与可注入字段）；server/tests/services/process_runtime/test_blueprint_spec_gate.py（`_collect_prior_answers` 相关用例的调用方式——「同一问题不重复问」的断言要复用同一读侧函数）</read_first>
  <action>
**① 新建 `server/tests/delivery/test_blueprint_reanchor_edit.py`**（`pytestmark = [pytest.mark.django_db(transaction=True), pytest.mark.asyncio]`），docstring「守十件事」编号清单：

1. **精确命中保持 anchored 且 `section_path` 被刷新**：线程 anchor 的 `block_id` 在新内容中仍在但**落位变了**（section 改名/移动）→ `anchor_status == "anchored"` 且 DB 重读的 `anchor["section_path"] == iter_blocks` 给出的新 path（⭐ 这条锁死 RESEARCH 指出的「`reanchor` 不刷 section_path」缺口）。
2. **模糊命中换 block_id 且保留 quoted_text**：原 block_id 消失、有文本相似 ≥0.85 的新块 → `anchor["block_id"]` 换成新 id、`quoted_text` 逐字不变、`anchor_status == "anchored"`。相似度用 `difflib` **现算断言**（不硬编码）。
3. ⭐ **失锚不删**：完全不同的新内容 → `anchor_status == "orphaned"`、线程行**仍存在**（`BlueprintThread.objects.filter(id=...).acount() == 1`）、`anchor` 内容原样保留；且 `filter(anchor_status="orphaned")` 能集中查到它。
4. ⭐ **入参不被原地修改**：调用前深拷贝一份 anchor dict，调用后原 dict 与拷贝**逐字相等**（111 单测第 2 条契约在批量侧的延续——精确命中分支 `reanchor` 返回同一对象，实现里若漏拷贝这条即红）。
5. ⭐ **diff 预筛跳过**（P3）：构造 30 个 block、10 条线程，只改 1 个 block → 传 `old_content` 时返回的 `skipped >= 9`；不传 `old_content`（None）时 `skipped == 0`（全量重锚，正确性优先）。⭐ 并断言两种模式下**最终 anchor 结果一致**（预筛不改变正确性）。
6. **一次 bulk_update + updated_at 被更新**：记录调用前 `updated_at`，调用后 DB 重读**严格大于**（`bulk_update` 绕过 auto_now，实现里漏显式带就会相等 ⇒ 这条即红）。
7. **patch 三 op**：`replace` 改块文本 → 新版本该块文本已变、`block_id` 不变；`insert`（`position` 前/后各一条）→ 新块出现在正确位置；`delete` → 该块消失。`rejected` 为空。
8. ⭐ **不合法 content 被拒绝且版本数不变**：构造一个会让 `validate_blueprint` 失败的 patch（如 delete 掉被 `citations` 引用的块、或插入缺 `block_id` 的块）→ `status == "invalid"`、`detail` 非空且**不含**明文凭证样本、`ArtifactVersion.objects.filter(artifact=...).acount()` 与调用前**相等**（**不落半合法版本**）。
9. ⭐ **同 content_hash 不翻版本**：同一 patch 连提两次 → 第二次 `status == "unchanged"`、`version_no` 不推进、版本行数不变。
10. **归属可审计 + reviewer upsert**：成功版本的 `produced_by_ref == f"human_edit:{user.id}"`；`BlueprintReviewer` 中出现该 user（`first_action == "block_edit"`）；重复编辑不覆盖 `first_action`。

**② 新建 `server/tests/services/process_runtime/test_blueprint_reflow.py`**（同 pytestmark），docstring「守九件事」：

1. **三步顺序（先版本后状态）**：桩 `section_writer` 改一段 → 断言新版本已落（`version_no + 1`）**且**状态转移发生在其后（用 monkeypatch 记录调用序：`add_version` 的调用时刻早于 `transition`；或断言 `transition` 被调用时 artifact 的 `current_version_id` 已是新版本）。
2. ⭐ **`decision_log` 物化保 `answer` 键**：回灌后新版本 `content["decision_log"]` 含该 `thread_id` 的条目，且键集 ⊇ `{thread_id, question, answer, decided_at, decided_by}`；`answer` 非空。
3. ⭐ **同一问题不再重复问**（CONTEXT specifics 第 3 条）：把回灌后的 content 喂 `BlueprintSpecGateAdapter._collect_prior_answers`（**复用读侧同一函数**，不自写指纹逻辑）→ 断言返回的 `fingerprints` 含该问题的指纹、`text` 含该问答。⭐ 配对照：`decision_log` 条目里把 `answer` 键改名成 `decision` 后同一断言**失败**（证明「必须保 `answer` 键」不是恒真的装饰）。
4. ⭐ **`decided_at` 稳定 ⇒ 同 hash 不翻版本**：同一批已作答线程连续回灌两次 → 第二次 `status == "unchanged"`、版本行数不变（若 `decided_at` 用了 `timezone.now()` 这条必红）。
5. **线程收尾 + `applied_in_version` 口径**：回灌成功后线程 `status == "resolved"` 且 `resolution` 文本含新版本号；`resolve_thread` 幂等（再回灌一次不覆盖首次结论）；`decision_log` 条目的 `applied_in_version == str(base.id)`（基线版本 id）；产出版本可反查：`ArtifactVersion.objects.filter(artifact=..., produced_by_ref=f"ai_review_reflow:{thread_id}").acount() == 1`。
6. ⭐ **AI 不覆盖人工（正反并列）**：先经 `aapply_block_edit` 产一个 `human_edit:` 版本（改 block X）→ 桩 `section_writer` 让 AI 也改 block X → `status == "conflict"`、`conflict_block_ids == ["blk_x"]`、**版本行数不变**、且新开了一条 `kind=ai_clarification, blocking=True, return_stage="ai_reviewing"` 的线程；线程 question **不含**两侧正文（只含 block_id 与裁决提示）。**对照**：让 AI 改 block Y（人工没改过）→ `status == "applied"` 且版本 +1（证明冲突判据不是恒真）。
7. **非法 content fail-closed**：桩 `section_writer` 产出非法 content → `status == "invalid"`、版本行数不变、**不落 failed**、无异常外泄。
8. **恒不抛 + 空输入**：`threads=[]` / 无 artifact 版本 / `section_writer` 抛异常 → 均返回合法 `status` 且不抛。
9. **零 ORM 写源码扫描**：读 `blueprint_reflow.py` 与 `blueprint_block_edit.py` 源码断言不含 `BlueprintThread.objects.create` / `.update(` / `bulk_update` / `ArtifactVersion.objects.create`，且不含 `record_answer`（与 INV-6 守卫同款扫描）。
10. ⭐ **`section_writer` 默认走生产实现（B1，正反并列）**：不传 `section_writer` 时，monkeypatch `blueprint_reflow.ablock_section_writer` 为一个记录调用的桩 → 断言它**被调用一次**且入参含 `answers`（锁死「默认不是 no-op」）；再把桩改成「原样返回 content」→ 断言 `decision_log` 仍被物化、线程仍 `resolved`（**答案不因段落未改写而丢失**）。另一条直接测 `ablock_section_writer` 本体：`ProviderConfigService.aresolve` 桩成返回无 `default_model` ⇒ **返回的 content 与入参逐字相等**（LLM 不可得 ⇒ 该块原样保留，不抛、不清空）；再桩 `build_chat_model` 返回可解析 `{"text": "新正文"}` ⇒ 目标块文本已更新且 `block_id` **逐字不变**；超 `_MAX_REWRITE_BLOCKS` 的答案批只改写前 5 块。
11. ⭐ **人工块保护（B3，三态）**：
    - **等价 ⇒ 保留且不翻版本**：先经 `aapply_block_edit` 产 `human_edit:` 版本改 block X，再让当前最新版本的 block X 与人工版本**逐字相同** → `arestore_human_blocks` 返回 `status == "unchanged"`、`conflicted == []`、版本行数不变；
    - ⭐ **实质冲突 ⇒ 人工块未被抹掉 + 有线程**：把当前最新版本的 block X 改成 AI 重装后的不同内容（模拟 `repo_rework`/`remerge` 重装）→ `arestore_human_blocks` 返回 `status == "conflict"`、`conflicted == ["blk_x"]`、**新版本的 block X 内容与人工版本逐字相等**（人工编辑未被抹掉，这是 B3 的头号断言）、且新增一条 `kind=ai_clarification, blocking=True, return_stage="ai_reviewing"` 的线程，其 question **不含**两侧正文（只含 block_id 与裁决提示）；
    - **无人工版本 ⇒ noop**：从未有 `human_edit:` 版本 → `status == "noop"`、零查询副作用、版本行数不变（**证明断言非恒真**）；
    - `acollect_human_block_ids` 单测：两个 `human_edit:` 版本各改一块 → 返回**排序后的两个 block_id**（确定性）；无人工版本 → `[]`；`supersedes` 为 None 的边界不抛。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/delivery/test_blueprint_reanchor_edit.py tests/services/process_runtime/test_blueprint_reflow.py -x -q</automated>
  </verify>
  <acceptance_criteria>
- `cd server && uv run pytest tests/delivery/test_blueprint_reanchor_edit.py tests/services/process_runtime/test_blueprint_reflow.py -q` 全绿。
- `rg -c "def test_" server/tests/delivery/test_blueprint_reanchor_edit.py` ≥11；`rg -c "def test_" server/tests/services/process_runtime/test_blueprint_reflow.py` ≥15。
- ⭐ `rg -n "ablock_section_writer" server/tests/services/process_runtime/test_blueprint_reflow.py` 命中 ≥2（B1「默认走生产实现」的正反并列存在）。
- ⭐ `rg -n "arestore_human_blocks" server/tests/services/process_runtime/test_blueprint_reflow.py` 命中 ≥3（B3 等价 / 冲突 / noop 三态）；`rg -n "acollect_human_block_ids" server/tests/services/process_runtime/test_blueprint_reflow.py` 命中；`rg -n "human_block_restore:" server/tests/services/process_runtime/test_blueprint_reflow.py` 命中。
- `rg -n "orphaned" server/tests/delivery/test_blueprint_reanchor_edit.py` 命中；`rg -n "section_path" server/tests/delivery/test_blueprint_reanchor_edit.py` 命中（path 刷新被断言）。
- `rg -n "difflib" server/tests/delivery/test_blueprint_reanchor_edit.py` 命中（阈值边界现算，不硬编码）。
- `rg -n "skipped" server/tests/delivery/test_blueprint_reanchor_edit.py` 命中（diff 预筛用例存在）。
- `rg -n "unchanged" server/tests/delivery/test_blueprint_reanchor_edit.py server/tests/services/process_runtime/test_blueprint_reflow.py` 两文件各 ≥1（同 hash 不翻版本被断言两处）。
- `rg -n "_collect_prior_answers" server/tests/services/process_runtime/test_blueprint_reflow.py` 命中（同一问题不重复问复用读侧函数）。
- `rg -n "conflict_block_ids|\"conflict\"" server/tests/services/process_runtime/test_blueprint_reflow.py` 命中（AI 不覆盖人工用例存在）。
- `rg -n "record_answer" server/tests/services/process_runtime/test_blueprint_reflow.py` 命中（源码扫描断言存在）。
- `rg -n "transaction=True" server/tests/delivery/test_blueprint_reanchor_edit.py server/tests/services/process_runtime/test_blueprint_reflow.py` 两文件各命中。
- `cd server && uv run pytest tests/delivery/ tests/services/process_runtime/ -q` 全绿。
- `cd server && uv run pytest tests/ -q` 无新增失败（与本 plan 前基线逐条比对，差异登记）。
- `cd server && uv run python manage.py makemigrations --check --dry-run` 退出码 0。
- `cd server && uv run ruff check tests/delivery/test_blueprint_reanchor_edit.py tests/services/process_runtime/test_blueprint_reflow.py && uv run ruff format --check tests/delivery/test_blueprint_reanchor_edit.py tests/services/process_runtime/test_blueprint_reflow.py` 通过。
- 冻结面自检：`git diff --name-only` 只含本 plan 声明的五个文件，且不含 `blueprint_anchor.py`、`blueprint_schema.py`、`artifact_service.py`、`builtin_processes.py`、`blueprint_resume.py`、`blueprint_review.py`、`event_taxonomy.py`、`delivery/models/`、`task/`。
- 受限面自检：`git diff server/delivery/services/blueprint_lifecycle_service.py | rg "^-[^-]"` **为空**（删除行 0）。
  </acceptance_criteria>
  <done>重锚定四分支 + section_path 刷新 + 失锚不删 + 入参不被改 + diff 预筛正确性等价 + `updated_at` 被更新全部被锁死；patch 三 op 与「不合法不落版本」「同 hash 不翻版本」「归属可审计」成立；「同一问题不重复问」用读侧同一函数断言且配了会失败的对照；「AI 不覆盖人工」正反并列且冲突时零版本增长。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 用户 patch ops → 蓝图 content | 半可信用户输入直接改文档结构，是本 plan 唯一的外部写入面 |
| AI 回灌 → 人工已编辑的 block | 跨越此边界即「AI 覆盖了人的决定」，是 CLAR-03 的核心安全语义 |
| 版本链（`add_version`） | 半合法内容一旦落版本，下游（115 渲染 / 116 导出）全部受污染 |
| 线程 anchor 行 | 唯一写入通道必须是 `areanchor_threads`（INV-6） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-114-23 | Tampering | AI 回灌覆盖人工编辑过的 block ⇒ 用户的修改被静默抹掉 | mitigate | `detect_human_conflicts` 用 `diff_blueprint_blocks` 求交集 ⇒ 非空则**不写版本**、开阻塞线程询问；Task 3 第 6 条正反并列（改人工改过的块 → conflict 零版本增长；改别的块 → applied） |
| T-114-23b | Tampering | **打回后 `repo_rework`/`remerge` 重装 merge** 覆盖人工编辑的 block（B3：这是主要产版本路径，回灌侧的冲突检测挡不住它） | mitigate | `acollect_human_block_ids` 以 `produced_by_ref__startswith="human_edit:"` 求保护集 + `arestore_human_blocks` 逐块 canonical JSON 比对（等价保留 / 冲突写回人工版本 + 开阻塞线程）；**不改 `blueprint_merge.py`**（`git diff` 为空验收）；Task 3 第 11 条三态断言，头号断言为「新版本 block X 与人工版本逐字相等」；114-03 在 `ai_review` 入口接线 |
| T-114-23c | Elevation of Privilege | `section_writer` 默认 no-op ⇒ 答案只进 `decision_log`、蓝图正文永不更新（SC-2/SC-3 成孤儿，B1） | mitigate | 缺省即 `ablock_section_writer` 生产实现 + `rg "section_writer or ablock_section_writer"` 验收 + Task 3 第 10 条断言默认 writer 被调用；LLM 不可得只让该块原样保留，`decision_log` 与线程收尾照常 ⇒ 降级不丢答案 |
| T-114-24 | Tampering | 半合法 content 落版本（patch 破坏 citations 引用完整性等） | mitigate | 显式 `validate_blueprint` 前置回显中文错因 + `add_version` 自身的 `ArtifactContentInvalid`（`:126-130`）双层；Task 3 第 8 条断言版本数不变 |
| T-114-25 | Denial of Service | `decided_at` 用 `timezone.now()` ⇒ 每次回灌都改 hash ⇒ 版本历史被刷成噪声、diff 视图不可用 | mitigate | `decided_at` 取作答消息 `created_at` + `rg "timezone.now()"` 零命中验收 + Task 3 第 4 条连续回灌断言 `unchanged` |
| T-114-26 | Tampering | 「同一问题不再重复问」断链（`decision_log` 只写 `decision` 不写 `answer`） | mitigate | `build_decision_entries` 保规格门形状超集 + 复用 `_collect_prior_answers` 做读侧断言 + Task 3 第 3 条的**会失败的对照用例** |
| T-114-27 | Info Disclosure | 批注 anchor 的 `quoted_text`（可能含凭证样本）或 block 正文经日志/线程 question 外泄 | mitigate | 重锚定日志只记四键计数；冲突线程 question 只含 `block_id` 与裁决提示（**不贴两侧正文**）；编辑日志只记 `op_count` / `version_no`；错因走 `_format_error`（已脱敏截断 500 字符）+ `redact_secrets_in_text` |
| T-114-28 | Denial of Service | 重锚定在大蓝图上准平方级 ⇒ 同步请求路径超时（P3） | mitigate | `diff_blueprint_blocks` 预筛只处理受影响线程 + 一次 `bulk_update`；Task 3 第 5 条断言 `skipped >= 9` 且预筛与全量结果一致 |
| T-114-29 | Tampering | 失锚线程被删除或隐藏 ⇒ 用户的批注静默消失（CLAR-02 明令禁止） | mitigate | `anchor_status="orphaned"` 保留行 + `rg "\.delete\(\)"` 零命中 + Task 3 第 3 条断言线程仍在且可集中查询 |
| T-114-30 | Tampering | 批量写线程绕过 service / 绕过 auto_now ⇒ INV-6 失守、`updated_at` 陈旧 | mitigate | 唯一写通道在 lifecycle service 内 + 显式 `updated_at=timezone.now()` + Task 3 第 6 条与第 9 条源码扫描 + `test_blueprint_inv6_guard` 保持绿 |
| T-114-31 | Tampering | 基线读 `session.current_artifact_version` ⇒ 把上游成果覆盖回旧内容 | mitigate | 一律 `order_by("-version_no").afirst()` + `rg "current_artifact_version"` 零命中验收 |
| T-114-SC | Tampering | 依赖供应链 | accept | 零新增外部依赖（`copy` / `difflib` / `json` 均 stdlib）、零 migration |
</threat_model>

<verification>
- `cd server && uv run pytest tests/delivery/ -q` 全绿（111-02 anchor 单测六条契约 + 114-01 线程底座 + 112-05 确认门八端点 + INV-6 扫描）。
- `cd server && uv run pytest tests/services/process_runtime/ -q` 全绿（114-03 stage 端到端 + 113 覆盖率门 + 规格门指纹去重）。
- `cd server && uv run pytest tests/ -q` 无新增失败（与本 plan 前基线逐条比对，差异登记）。
- `cd server && uv run python manage.py makemigrations --check --dry-run` 退出码 0（**零 migration**）。
- `cd server && uv run ruff check delivery/services/ services/process_runtime/ tests/delivery/ tests/services/process_runtime/` 通过；`ruff format --check` **只对**本 plan 新建的四个文件（`blueprint_block_edit.py` / `blueprint_reflow.py` / 两个测试文件）跑，**不对 `blueprint_lifecycle_service.py` 跑**。
- 受限面自检：`git diff server/delivery/services/blueprint_lifecycle_service.py | rg "^-[^-]"` **为空**（删除行 = 0，纯追加）；`git diff server/delivery/services/blueprint_anchor.py` 输出为空。
- 冻结面自检：`git diff --name-only` 只含五个声明文件；`codegraph/services/repo_router_v2.py`、`process_runtime/{decompose_segments,research_adapter,architect_merge_adapter,merged_plan,clarify_adapter,render,resume,builtin_processes,blueprint_resume,blueprint_schema,blueprint_quality,blueprint_merge,blueprint_reconcile,blueprint_confirm_gate,blueprint_spec_gate,blueprint_repo_plan,blueprint_review}.py`、`delivery/services/{blueprint_anchor,artifact_service,event_taxonomy}.py`、`repositories/services/charter_service.py`、`system/`、`agents/call_source.py`、`delivery/models/`、`delivery/migrations/`、`task/` 全部零改动。
- ⭐ B3 受限面额外核对：`git diff server/services/process_runtime/blueprint_merge.py` 与 `git diff server/services/process_runtime/blueprint_review.py` 输出**均为空**（人工块保护落在 `blueprint_reflow.py`，接线归 114-03）。
</verification>

<success_criteria>
- 版本一变，旧批注不再错位也不再凭空消失：精确/模糊/失锚三分支正确，`section_path` 一并刷新，失锚线程保留且可集中查询，性能上只处理受影响线程。
- 人能直接改蓝图：block 级 `replace`/`insert`/`delete` 经 service 收口，不合法**拒绝并回显中文错因且不落版本**，合法落 `human_edit:{user_id}` 版本、归属可审计、编辑者进 `BlueprintReviewer`、自动重锚定。
- 澄清答案能回灌成新版本且决策物化进文档（`decision_log` 保 `answer` 键），**同一问题不会被重复问**（用读侧同一函数断言），**重复回灌不翻版本**。
- **AI 不覆盖人工**被代码强制：回灌侧冲突时零版本增长 + 开阻塞线程询问；**重装侧**（B3）人工块被逐一比对、等价保留、冲突写回人工版本并开线程，判据是 `diff_blueprint_blocks` + canonical JSON 的确定性比对（可复现、可证伪）。
- **答案真的落地**（B1）：`section_writer` 缺省即生产实现 `ablock_section_writer`，段落按 `anchor.block_id` 单块改写；LLM 不可得只让该块保持原样，`decision_log` 与线程收尾照常 ⇒ 答案永不丢失。
- 114-03 可以直接消费两个人工块保护契约：`acollect_human_block_ids(artifact)`、`arestore_human_blocks(artifact, ...)`；114-05 可以直接消费三个稳定契约：`aapply_block_edit(artifact, ops, user=...)`、`aapply_thread_answers(artifact, ...)`、`areanchor_threads(artifact, new_content, old_content=...)`，以及 `produced_by_ref` 的三个前缀约定（`human_edit:` / `ai_review_reflow:` / `human_block_restore:`）。
</success_criteria>

<output>
完成后创建 `.planning/phases/114-ai/114-04-SUMMARY.md`（provides：`areanchor_threads` 的**逐字签名与四键返回**、`old_content` 缺省时的退化语义、`apply_block_ops` 的 op 形状与 `rejected` 的 `reason` 全枚举、`aapply_block_edit` 与 `aapply_thread_answers` 的**恒定返回键全表**与每个 `status` 的语义、`build_decision_entries` 的条目键集（**含 `answer` 与 `decided_at` 的取值来源**）、`detect_human_conflicts` 的判据与 `human_edit:` 前缀识别方式、**`ablock_section_writer` 的逐字签名 / 默认注入方式（`section_writer or ablock_section_writer`）/ 三个上界常量取值 / LLM 不可得时的块级降级语义**、**`acollect_human_block_ids` 与 `arestore_human_blocks` 的逐字签名与恒定返回键、四个 `status` 的语义、canonical JSON 比对口径、冲突线程的 kind/blocking/return_stage 与 question 组成（无正文）**、**`applied_in_version` 的落地口径与反查方式**（`produced_by_ref == f"ai_review_reflow:{thread_id}"`，115 消费面依赖）、`produced_by_ref` 两个前缀的确切格式、`add_reviewer` 的 `first_action` 取值、`bulk_update` 的字段清单、**受限面 `blueprint_lifecycle_service.py` 删除行 = 0 的 `git diff | rg "^-[^-]"` 空输出证据**、diff 预筛与全量重锚的结果等价性验证结果、全量 `pytest tests/ -q` 与基线的差异比对）。
</output>
