---
phase: 114-ai
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/delivery/services/blueprint_lifecycle_service.py
  - server/tests/delivery/test_blueprint_review_threads.py
autonomous: true
requirements: [FLOW-07]
prohibitions:
  - "禁止修改 server/codegraph/services/repo_router_v2.py"
  - "禁止修改 §13.2 冻结面：process_runtime/{decompose_segments,research_adapter,architect_merge_adapter,clarify_adapter,render}.py、delivery 侧 merged_plan.py、resume.py、repositories/services/charter_service.py"
  - "🔒 禁止触碰 builtin_processes.py 的 `_TECHNICAL_PLAN_STAGES` / `_ECHO_STAGES`；本 plan 一行都不改 builtin_processes.py"
  - "禁止修改 ConvergenceSessionEvent 既有事件类型与 payload 字段（event_taxonomy 本 plan 零改动，114 的新事件常量归 114-03）"
  - "禁止修改 server/system/settings_service.py 既有 getter"
  - "禁止修改 task/core/knowledge_tools.py 公共 handler 工厂（本相位不碰 task/）"
  - "禁止修改 server/services/process_runtime/blueprint_resume.py（本 plan 零改动；其映射表追加归 114-03，删除行上界 0）"
  - "⛔ **`record_answer` 禁用于 finding 留痕**——它在同事务把 `open` 推到 `answered`（`_record_answer_sync:536-540`），会让 `ahas_open_blocking_threads`（只认 open）判为无门、confirm 守卫误放行、续驱 pause 失守。中间留痕一律用本 plan 新增的 `append_note`"
  - "禁止给 `BlueprintThread` / `BlueprintThreadMessage` 加字段（**本 plan 内**）：`severity` 字段**已存在**（`blueprint_thread.py:92`）→ 本 plan **零 migration**。⚠️ 全相位唯一的模型字段追加是 `BlueprintThread.last_reminded_at`（pending 提醒周期用），**由 114-05 承载**（含唯一一条 migration）；本 plan 一行不改 `delivery/models/`"
  - "禁止新增第二条旁路写表路径：`append_note` 必须是从既有私有 `_append_thread_message_sync`（:1055-1064）**提炼**的公开方法，`_arecord_gate_note`（:1043-1053）改为调它（`test_blueprint_inv6_guard` 源码扫描会挂）"
  - "禁止改 `record_answer` / `resolve_thread` / `ahas_open_blocking_threads` / `apply_gate_action` / `transition` 的既有语义与签名（只允许纯追加形参与新方法）"
  - "禁止把 `severity` 做成必填形参：必须是 `severity: str = \"\"`（默认空 ⇒ 既有 112/113 全部调用逐字等价）"
  - "禁止在 confirm 路径的**事务外**新增「未决 BLOCKER」二次查询（会重新打开 TOCTOU 窗口）——判据一律收敛进 `_apply_transition_sync` 已有的那个 `transaction.atomic()`"
  - "禁止修改 blueprint_schema.py / blueprint_quality.py / blueprint_anchor.py / blueprint_merge.py / blueprint_reconcile.py / blueprint_confirm_gate.py / blueprint_spec_gate.py / blueprint_repo_plan.py（受限面，本 plan 只读）"
must_haves:
  truths:
    - "`open_thread` 可落 severity：新增 `severity: str = \"\"` 形参 + 入参校验（`severity ∈ ThreadSeverity.values or \"\"`，非法值 raise ValueError 且 DB 不写），`_open_thread_sync` 的 `objects.create` 带上该字段；零 migration"
    - "既有调用行为逐字不变：112/113 现存全部 `open_thread(...)` 调用（不传 severity）落库后 `thread.severity == \"\"`，且 112/113 既有测试全绿"
    - "`blocking == (severity == \"blocker\")` 不变式对 `kind=ai_review_finding` 强制成立：错配组合（severity=blocker & blocking=False、severity=warning|info & blocking=True）被 `open_thread` 直接 raise ValueError 且 DB 不写"
    - "公开 `append_note(thread, *, body, author=None, author_type=ThreadAuthorType.AI)` 存在且**不改线程 status**：对 `status=open` 的线程留痕后重读 DB 仍为 `open`"
    - "`_arecord_gate_note` 改为委托 `append_note(..., author_type=ThreadAuthorType.HUMAN)`，确认门留痕的 `author_type` 与改动前逐字等价（仍是 human），且不新增写表路径"
    - "confirm 守卫的两条判据（无 open+blocking 线程 + 无未解决 BLOCKER finding）在 `_apply_transition_sync` 的**同一个 `transaction.atomic()`** 内用**单次查询**完成，视图层与 adapter 层零事务外二次查询"
    - "反向断言成立：用 `record_answer` 给 BLOCKER finding 留痕会把线程推到 `answered` 并使 `pending_review → confirmed` 放行——该用例断言的是「必须用 append_note」的理由，而不是允许该用法"
    - "新增只读统计 helper `aunresolved_blocker_count(artifact)`（供 114-03 未决清单与 114-05 人审呈现），**只用于报告，不参与 confirm 守卫判定**"
  artifacts:
    - path: "server/delivery/services/blueprint_lifecycle_service.py"
      provides: "open_thread 的 severity 形参 + blocking 不变式校验 + 公开 append_note + confirm 守卫单查询收敛 + aunresolved_blocker_count"
      contains: "def append_note"
    - path: "server/tests/delivery/test_blueprint_review_threads.py"
      provides: "severity 落库 / 不变式错配拒绝 / append_note 不改状态 / record_answer 反向断言 / confirm 守卫并发不放行 / 112-113 调用等价性"
      contains: "append_note"
  key_links:
    - from: "server/delivery/services/blueprint_lifecycle_service.py"
      to: "server/delivery/models/blueprint_thread.py"
      via: "ThreadSeverity 枚举做入参白名单（不写字面量），severity 字段已存在故零 migration"
      pattern: "ThreadSeverity"
    - from: "server/delivery/services/blueprint_lifecycle_service.py"
      to: "server/delivery/services/blueprint_lifecycle_service.py"
      via: "_arecord_gate_note → append_note（提炼后的唯一写消息路径，不新增旁路）"
      pattern: "append_note"
---

<objective>
给 114 的 findings 线程铺底座，全部落在 `BlueprintLifecycleService` 一个文件里（受限面纯追加为主）：
① `open_thread` 追加 `severity: str = ""` 形参（零 migration、既有调用逐字等价）；
② 对 `kind=ai_review_finding` 强制 **`blocking == (severity == "blocker")`** 不变式；
③ 从私有 `_append_thread_message_sync` **提炼公开 `append_note`**（留痕不改线程 status），并把 `_arecord_gate_note` 改为调它；
④ 把 CONTEXT 要求的 confirm 第二条判据（无未决 BLOCKER）**收敛进 `_apply_transition_sync` 已有的那个事务内的单次查询**，从根上消除视图层「先查后转」的 TOCTOU 窗口；
⑤ 测试锁死上述四条，含「用 `record_answer` 留痕会破坏守卫」的**反向断言**。

Purpose: FLOW-07 的前置——没有 severity 形参，findings 分级全为 `""`、`blocking` 与 severity 的不变式无从建立、人审无法分级；没有 `append_note`，中间留痕只能走 `record_answer`，那会让带未决 BLOCKER 的蓝图被人审放行（112 已记教训）。
Output: `blueprint_lifecycle_service.py` 的四项能力 + 一个新测试文件。**无新 migration、无新模型字段、无新事件常量。**
</objective>

<execution_context>
工作目录：worktree 根 `/Users/zaneliu/Projects/open-source/friday-clean/.claude/worktrees/v0.20-blueprint`。wave 1，无依赖，本 plan 是全相位第一个动工点。

本 plan 是**全相位唯一改 `blueprint_lifecycle_service.py` 前 1065 行的 plan**（114-04 只在文件尾追加 `areanchor_threads`，属纯追加）。改动形状严格受限：

- **纯追加**：`open_thread` 的 `severity` 形参与校验、`_open_thread_sync` 的形参与 `create` 字段、公开 `append_note`、`aunresolved_blocker_count`、`_has_confirm_blockers_sync` 私有 helper、`__all__` 不变（导出的是类，无需改）。
- **允许的删除行（须在 SUMMARY 逐行登记，合计 ≤ 12 行）**：
  1. `_append_thread_message_sync`（:1055-1064）的 `author_type=ThreadAuthorType.HUMAN` 硬编码行改为形参（≤2 行）；
  2. `_arecord_gate_note`（:1053）的函数体一行改为调 `append_note`（≤1 行）；
  3. `_apply_transition_sync`（:252-259）内联的 5 行 `BlueprintThread.objects.filter(...)` 守卫查询改为调 `_has_confirm_blockers_sync(artifact)`（≤7 行，含 docstring 一行补述）。
- 本文件**只跑 `ruff check`，不跑 `ruff format`**（守受限面纪律，防 format 漂移把纯追加约束打破）。

⚠️ `_arecord_gate_note` 的行为必须**逐字等价**：确认门留痕原本是 `author_type=HUMAN`，提炼后必须显式传 `ThreadAuthorType.HUMAN`（不是让它跟着 `append_note` 的 AI 默认值走）。112-05 的确认门测试会背书这条。
</execution_context>

<context>
@.planning/phases/114-ai/114-CONTEXT.md（锁定：`open_thread` 加 `severity` 形参零 migration；finding 留痕禁用 `record_answer`，从 `_append_thread_message_sync` 提炼公开 `append_note`；`blocking == (severity=="blocker")` 强制一致；confirm 守卫收进单一事务消除 TOCTOU）
@.planning/phases/114-ai/114-RESEARCH.md §2（模型字段全表 + `open_thread`/`record_answer`/`resolve_thread`/`ahas_open_blocking_threads`/`add_reviewer`/`transition` 六个签名逐字 + ⚠️112 review 教训的后果链 + findings 去重建议）、§6.2（approve 端点必须先查未决 BLOCKER 的两条判据收敛建议）、Pitfalls P4（`transition` 的 CAS 语义、`.update()` 绕过 auto_now、守卫与 CAS 同事务 MN-01）
@.planning/phases/114-ai/114-PATTERNS.md 冻结清单 §③（`record_answer` 禁用于留痕的三级后果链 + **四通道表**：创建 finding / 中间留痕 / 已修复或误报 / 人类回答，各用什么、线程状态如何）、受限面节（`blueprint_lifecycle_service.py` 三项追加的登记要求）、第 4 类（findings 落库四条 + `open_thread` 调用形状逐字）、No Analog Found 第 1 项（`append_note` 本体的形态要求：照 `resolve_thread` 的「公开 async 方法 = 校验 + 委托 `@sync_to_async` 事务 + 结构化日志三段」）
@server/delivery/services/blueprint_lifecycle_service.py（L1-41 模块 docstring 的 INV-6 与转移表、L230-279 `_apply_transition_sync` 含 L252-259 confirm 守卫、L347-363 `ahas_open_blocking_threads`、L365-423 `open_thread`、L425-454 `record_answer`、L456-483 `resolve_thread`、L485-517 `_open_thread_sync`、L519-541 `_record_answer_sync`、L543-565 `_resolve_thread_sync`、L1043-1064 `_arecord_gate_note` + `_append_thread_message_sync`）
@server/delivery/models/blueprint_thread.py（L22-52 四个枚举：`ThreadAnchorStatus` / `ThreadKind`（含 `AI_REVIEW_FINDING`）/ `ThreadSeverity`（blocker/warning/info）/ `ThreadStatus`；L69-122 全部字段与三条索引，尤其 L92 `severity`（blank 默认 `""`，字段已存在）与 L119-122 的 `Index(artifact,status,blocking)`）
@server/tests/delivery/test_blueprint_lifecycle_service.py（111-02 的既有守护点——本 plan 不得让任何一条变红）
@server/tests/delivery/test_blueprint_inv6_guard.py（源码扫描的具体扫法：新增写表路径会挂）
</context>

<tasks>

<task type="auto">
  <name>Task 1: open_thread 追加 severity 形参 + blocking 不变式强制 + 只读统计 helper</name>
  <files>server/delivery/services/blueprint_lifecycle_service.py</files>
  <read_first>server/delivery/models/blueprint_thread.py L22-52（四个枚举的**确切类名与成员名**：`ThreadSeverity.BLOCKER/WARNING/INFO`、`ThreadKind.AI_REVIEW_FINDING`、`ThreadStatus.OPEN/ANSWERED/RESOLVED/DISMISSED`——一律用枚举常量，禁写字面量）、L92（`severity` 字段的 `max_length` 与 blank 默认）；server/delivery/services/blueprint_lifecycle_service.py L365-423（`open_thread` 全文：`kind` 白名单校验 → `return_stage` 截断 → 委托 `_open_thread_sync` → structlog `blueprint_thread_opened` 三段式）、L485-517（`_open_thread_sync` 的 `objects.create` 字段清单与同事务首条消息）、L53-63（models import 块——`ThreadSeverity` 是否已在其中，未在则加进同一个 import 语句）</read_first>
  <action>
在 `server/delivery/services/blueprint_lifecycle_service.py` 内：

**① import**：`from delivery.models import (...)` 块内追加 `ThreadSeverity`（保持既有字母序与格式；这是既有 import 块的**唯一**允许改动，属追加一个名字）。

**② `open_thread` 追加形参**（在 `return_stage: str = ""` 之后、`) -> BlueprintThread:` 之前）：
```
        severity: str = "",
```
默认空串 ⇒ 112/113 现存全部调用逐字等价。docstring 追加两条：
- 「`severity` ∈ `ThreadSeverity.values`（`blocker`/`warning`/`info`）或空串；非法值 `raise ValueError`（与 `kind` 同款，DB 不写）。`BlueprintThread.severity` 字段自 111-02 起已存在，本形参**零 migration**。」
- 「⭐ **不变式**：`kind == ThreadKind.AI_REVIEW_FINDING` 时强制 `blocking == (severity == ThreadSeverity.BLOCKER)`——错配即 `raise ValueError`。理由：`pending_review → confirmed` 的事务内守卫（`_apply_transition_sync`）只认 `open + blocking`；若 `severity=blocker` 却 `blocking=False`，带未决 BLOCKER 的蓝图会被人审放行；若 `severity=warning` 却 `blocking=True`，警告会把蓝图钉死。两者必须同源。」

**③ 校验实现**（紧跟既有 `if kind not in ThreadKind.values:` 之后，纯追加）：
```
        sev = str(severity or "")
        if sev and sev not in ThreadSeverity.values:
            raise ValueError(
                f"非法线程 severity={severity!r}；合法值={sorted(ThreadSeverity.values)} 或空串"
            )
        if kind == ThreadKind.AI_REVIEW_FINDING and bool(blocking) != (sev == ThreadSeverity.BLOCKER):
            raise ValueError(
                "ai_review_finding 线程必须满足 blocking == (severity == 'blocker')："
                f"当前 severity={sev!r} blocking={bool(blocking)}"
            )
```
两处 raise 都在**任何 DB 写之前**（`_open_thread_sync` 尚未调用）⇒ 非法入参零副作用。

**④ 透传**：`_open_thread_sync(...)` 调用追加 `severity=sev,`；`_open_thread_sync` 签名追加 `severity: str,`（keyword-only 区内），`BlueprintThread.objects.create(...)` 追加 `severity=severity,`。

**⑤ 日志**：既有 `blueprint_thread_opened` 的 `logger.info` 追加 `severity=sev,`（标量，合规；finding 正文在 `question` 里，**不进日志**）。

**⑥ 只读统计 helper（纯追加，放在 `ahas_open_blocking_threads` 之后）**：
```
    async def aunresolved_blocker_count(self, artifact: Artifact) -> int:
```
docstring 写明：「未决 BLOCKER finding 计数——`kind=ai_review_finding` 且 `severity=blocker` 且 `status ∈ {open, answered}` 的线程数。**仅供报告**（114-03 的 `unresolved` 快照、114-05 的人审呈现），**绝不用于 confirm 守卫判定**——守卫必须在 `_apply_transition_sync` 的事务内完成（见 Task 2），事务外查一次就是 TOCTOU。」
实现：`await BlueprintThread.objects.filter(artifact=artifact, kind=ThreadKind.AI_REVIEW_FINDING, severity=ThreadSeverity.BLOCKER, status__in=[ThreadStatus.OPEN, ThreadStatus.ANSWERED]).acount()`。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/delivery/test_blueprint_lifecycle_service.py tests/delivery/test_blueprint_inv6_guard.py -x -q</automated>
  </verify>
  <acceptance_criteria>
- `rg -n 'severity: str = ""' server/delivery/services/blueprint_lifecycle_service.py` 命中（形参存在且有默认值）。
- `rg -n "ThreadSeverity" server/delivery/services/blueprint_lifecycle_service.py` 命中 ≥3（import + 白名单校验 + 不变式）。
- `rg -n 'blocking\) != \(sev == ThreadSeverity.BLOCKER\)|blocking == \(severity' server/delivery/services/blueprint_lifecycle_service.py` 命中（不变式实现或其 docstring 表述存在）。
- `rg -n "def aunresolved_blocker_count" server/delivery/services/blueprint_lifecycle_service.py` 命中。
- `rg -n 'severity=' server/delivery/services/blueprint_lifecycle_service.py | rg -c "" ` ≥4（透传 + create + 日志 + helper filter）。
- `cd server && uv run python manage.py makemigrations --check --dry-run` 退出码 0（**零 migration** 的硬证据）。
- `git diff server/delivery/models/blueprint_thread.py` 输出为空（模型一行不改）。
- `cd server && uv run pytest tests/delivery/ -q` 无新增失败（112/113 既有 `open_thread` 调用不传 severity 仍全绿 ⇒ 逐字等价）。
  </acceptance_criteria>
  <done>`open_thread` 能落 severity 且默认空串对既有调用无感；`ai_review_finding` 的 `blocking`/`severity` 错配在 DB 写之前被拒；只读 BLOCKER 计数 helper 就位且 docstring 明写「不参与守卫」；零 migration。</done>
</task>

<task type="auto">
  <name>Task 2: 提炼公开 append_note（不改线程 status）+ _arecord_gate_note 改调它 + confirm 守卫收敛为事务内单次查询</name>
  <files>server/delivery/services/blueprint_lifecycle_service.py</files>
  <read_first>server/delivery/services/blueprint_lifecycle_service.py L456-483（`resolve_thread` 的「公开 async 方法 = 校验 + 委托 `@sync_to_async` + structlog 三段」形态——`append_note` 照此写）、L1043-1064（`_arecord_gate_note` 的 docstring 三行理由 + `_append_thread_message_sync` 的 `author_type=ThreadAuthorType.HUMAN` **硬编码行**）、L230-279（`_apply_transition_sync` 全文：L251 `transaction.atomic()`、L252-259 confirm 守卫、L261-269 CAS、L272-277 reviewer upsert、L278 内存同步）、L519-541（`_record_answer_sync` 的 open→answered 推进——**对照物，本 Task 一行不改它**）</read_first>
  <action>
**① 公开 `append_note`（纯追加，放在 `resolve_thread` 之后、`_open_thread_sync` 之前）**：
```
    async def append_note(
        self,
        thread: BlueprintThread,
        *,
        body: str,
        author: Any = None,
        author_type: str = ThreadAuthorType.AI,
        initiated_by_user_id: str = "system",
    ) -> BlueprintThreadMessage:
```
docstring 逐字写明（这段是给后续 reviewer 免于重推的契约书）：
- 「**只追加消息，绝不推进线程状态**——与 `record_answer` 的唯一区别，也是本方法存在的全部理由。」
- 「⛔ **AI 审查 finding 的中间留痕必须用本方法，不得用 `record_answer`**。后果链（112 教训，`_arecord_gate_note` docstring 已记）：`record_answer` 在同事务把 `open` 推到 `answered` → `ahas_open_blocking_threads`（只认 `open`）判为无门 → `pending_review → confirmed` 的事务内守卫放行（**人审能通过带未决 BLOCKER 的蓝图**）→ `blueprint_resume` 的 pause 判据失守，续驱把会话一路 advance 到 `max_steps` 后落 FAILED。」
- 「`author_type` 默认 `ai`（审查留痕的主用场景）；确认门留痕经 `_arecord_gate_note` 显式传 `human`，与 111/112 行为逐字等价。」
实现：`message = await self._append_thread_message_sync(thread, body=str(body or ""), author=author, author_type=author_type)` → structlog `logger.info("blueprint_thread_note_appended", category="caller", component="blueprint_lifecycle", thread_id=..., kind=thread.kind, severity=thread.severity, author_type=author_type, initiated_by_user_id=initiated_by_user_id or "system")`（⚠️ **`body` 绝不进日志**——finding 正文只进消息行）→ `return message`。

**② `_append_thread_message_sync` 追加 `author_type` 形参**（唯一改动是把硬编码值换成形参）：
```
    @sync_to_async
    def _append_thread_message_sync(
        self, thread: BlueprintThread, *, body: str, author: Any,
        author_type: str = ThreadAuthorType.HUMAN,
    ) -> BlueprintThreadMessage:
```
`create(..., author_type=author_type, ...)`。默认值保持 `HUMAN` ⇒ 即使有遗漏的调用方也逐字等价。

**③ `_arecord_gate_note` 改为委托 `append_note`**（保留其既有 docstring 全文，追加一行「本方法自 114-01 起委托 `append_note`，行为不变（仍 `author_type=human`）；**不新增第二条写表路径**」）：
```
        return await self.append_note(
            thread, body=str(body or ""), author=author, author_type=ThreadAuthorType.HUMAN
        )
```

**④ confirm 守卫收敛为事务内单次查询（消除 TOCTOU 的正解）**：
现状（`_apply_transition_sync:252-259`）已在 `transaction.atomic()` 内，但只查 `status=OPEN & blocking=True` 一条判据。CONTEXT 要求的是「无 open+blocking 线程 **+** 无未解决 BLOCKER」两条。**绝不**在视图层补第二次查询（事务外 ⇒ TOCTOU 复活）。做法：把内联的 5 行守卫查询提炼为一个私有同步 helper，并在**同一条 ORM 查询**里用 `Q` 覆盖两条判据：
```
    @staticmethod
    def _has_confirm_blockers_sync(artifact: Artifact) -> bool:
```
docstring 写明：「confirm 唯一守卫判据（**必须在 `_apply_transition_sync` 的 `transaction.atomic()` 内调用**）。单次查询用 `Q` 覆盖两条：
① 任意 `status=open & blocking=True` 线程（LIFE-02 原口径，逐字保留）；
② `kind=ai_review_finding & severity=blocker & status ∈ {open, answered}`（CONTEXT 的第二条判据）。
②在 `blocking == (severity=='blocker')` 不变式成立时是①的子集，此处仍显式写出作为**纵深防御**：万一有旁路把 finding 推到 `answered`，①会漏挡而②不会。两条合成一次查询 ⇒ 不引入第二个 check-then-act 窗口。」
实现：`from django.db.models import Q`（顶层 import 块追加，与既有 `from django.db import transaction` 同区）；
```
        return (
            BlueprintThread.objects.filter(artifact=artifact)
            .filter(
                Q(status=ThreadStatus.OPEN, blocking=True)
                | Q(
                    kind=ThreadKind.AI_REVIEW_FINDING,
                    severity=ThreadSeverity.BLOCKER,
                    status__in=[ThreadStatus.OPEN, ThreadStatus.ANSWERED],
                )
            )
            .exists()
        )
```
`_apply_transition_sync` 内把 L252-259 换成：
```
            if to_status == BlueprintStatus.CONFIRMED and self._has_confirm_blockers_sync(artifact):
                raise ValueError("存在未解决的阻塞澄清线程或未决 BLOCKER 审查发现，蓝图不可确认")
```
⚠️ 错误消息文案变了：`blueprint_gate_views.py` 若有按消息串匹配的映射需同步核对（**只核对、不扩大改动面**；若确有匹配则在 SUMMARY 登记）。`_apply_transition_sync` 的 docstring 首条 confirm 守卫说明同步补一句「自 114-01 起为单次 `Q` 查询覆盖两条判据」。
⚠️ `ahas_open_blocking_threads`（:347）**一行不改**——它是 `blueprint_resume` 的 pause 判据与 112 门专属过滤（带 `kind` 参数），语义与 confirm 守卫不同，混用会让规格门/确认门互相误挡。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/delivery/test_blueprint_lifecycle_service.py tests/delivery/test_blueprint_gate_api.py tests/delivery/test_blueprint_inv6_guard.py -x -q</automated>
  </verify>
  <acceptance_criteria>
- `rg -n "async def append_note" server/delivery/services/blueprint_lifecycle_service.py` 命中；`rg -n "author_type: str = ThreadAuthorType.AI" server/delivery/services/blueprint_lifecycle_service.py` 命中。
- `rg -n "def _has_confirm_blockers_sync" server/delivery/services/blueprint_lifecycle_service.py` 命中；`rg -n "from django.db.models import Q" server/delivery/services/blueprint_lifecycle_service.py` 命中。
- `rg -n "self.append_note\(" server/delivery/services/blueprint_lifecycle_service.py` 命中（`_arecord_gate_note` 已委托）。
- `rg -c "BlueprintThreadMessage.objects.create" server/delivery/services/blueprint_lifecycle_service.py` == **4**（唯一确定值。实测四处：`_open_thread_sync:512` 首条问 + `_record_answer_sync:530` + `_resolve_thread_sync:560` 结论 + `_append_thread_message_sync:1059`。本 plan 的提炼**不新增第 5 处**，且改动后该值仍为 4；SUMMARY 登记实测输出）。
- `rg -n "body=" server/delivery/services/blueprint_lifecycle_service.py | rg "logger" ` 零命中（消息正文不进日志）。
- ⭐ 受限面自检：`git diff server/delivery/services/blueprint_lifecycle_service.py | rg "^-" | rg -v "^---" | wc -l` ≤ 12，且逐行核对全部落在 `_append_thread_message_sync` / `_arecord_gate_note` / `_apply_transition_sync`（含其 docstring）与顶层 import 块内 —— SUMMARY 逐行登记。
- `cd server && uv run pytest tests/delivery/ -q` 无新增失败（112-05 确认门八端点全绿 ⇒ `_arecord_gate_note` 行为逐字等价）。
- `cd server && uv run ruff check delivery/` 通过。**不跑 `ruff format`**（守受限面纯追加纪律）。
  </acceptance_criteria>
  <done>`append_note` 作为唯一「留痕不改状态」的公开通道就位、`_arecord_gate_note` 委托它且行为等价、confirm 守卫两条判据在同一事务的同一次 `Q` 查询内完成、`ahas_open_blocking_threads` 零改动、受限面删除行 ≤12 且逐行登记。</done>
</task>

<task type="auto">
  <name>Task 3: 线程底座测试（severity / 不变式 / append_note 不改状态 / record_answer 反向断言 / 守卫无 TOCTOU）</name>
  <files>server/tests/delivery/test_blueprint_review_threads.py</files>
  <read_first>server/tests/delivery/test_blueprint_lifecycle_service.py（111-02 的 fixture 工厂与 `pytestmark` 写法——⚠️ async service 跨线程写库必须 `pytest.mark.django_db(transaction=True)`；复用其 artifact/version 工厂，不重复造）；server/tests/delivery/test_blueprint_gate_api.py L1-23（模块 docstring「守 N 件事」编号清单体例 + 反向断言写法）；server/tests/delivery/test_blueprint_inv6_guard.py（源码扫描断言的写法——本文件要加一条同款扫描）</read_first>
  <action>
新建 `server/tests/delivery/test_blueprint_review_threads.py`，`pytestmark = [pytest.mark.django_db(transaction=True), pytest.mark.asyncio]`，模块 docstring 用「守八件事」编号清单，每条把可证伪断言直接写进条目：

1. **severity 落库**：`open_thread(kind=ThreadKind.AI_REVIEW_FINDING, severity=ThreadSeverity.WARNING, blocking=False, ...)` → 从 DB 重读（`await BlueprintThread.objects.aget(id=...)`）断言 `severity == "warning"`、`blocking is False`、`status == "open"`。三档 severity 各一条参数化。
2. **既有调用逐字等价**：不传 `severity` 的 `open_thread(kind=ThreadKind.AI_CLARIFICATION, blocking=True, ...)` → DB 重读 `severity == ""`（默认空串），且 `status/blocking/kind` 与 111/112 期望一致。
3. ⭐ **不变式错配被拒且 DB 不写**（正反并列，四条）：
   - `severity=blocker, blocking=False` → `pytest.raises(ValueError)` 且 `BlueprintThread.objects.filter(artifact=...).acount()` 未增加；
   - `severity=warning, blocking=True` → 同上；
   - `severity=info, blocking=True` → 同上；
   - 合法组合 `severity=blocker, blocking=True` 与 `severity=warning, blocking=False` → 正常落库（**证明断言非恒真**）。
   - 非 finding kind 不受不变式约束：`kind=ai_clarification, severity="", blocking=True` 正常落库。
4. **非法 severity 被拒**：`severity="fatal"` → `ValueError` 且 DB 不写。
5. ⭐ **`append_note` 不改线程 status**：对 `status=open` 的 BLOCKER finding 调 `append_note(body="第 2 轮仍未修复")` → DB 重读 `status == "open"`；消息数 +1 且最后一条 `author_type == "ai"`；再断言 `await service.ahas_open_blocking_threads(artifact) is True`（门仍在）。
6. ⭐ **反向断言：用 `record_answer` 留痕会破坏守卫**（这条是「必须用 append_note」的理由，不是允许该用法）——同样的 BLOCKER finding 改用 `record_answer(body="第 2 轮仍未修复")` → DB 重读 `status == "answered"`、`ahas_open_blocking_threads(...) is False`；并断言 `transition(artifact, "confirmed", ...)` 在**旧口径**下会放行。⭐ 而在本 plan 的**新守卫**下必须仍被拒（`pytest.raises(ValueError)`）——即 Task 2 的判据②纵深防御真的挡住了这条旁路。用例名与 docstring 显式写「本用例证明的是 `record_answer` 不可用于 finding 留痕」。
7. ⭐ **confirm 守卫无 TOCTOU（并发下不放行）**：先把蓝图推到 `pending_review` 且无阻塞线程，然后**在同一事务窗口内**制造竞态——用 `monkeypatch` 把 `BlueprintLifecycleService._has_confirm_blockers_sync` 包一层：进入时先真实插入一条 `open+blocking` 的 finding 行（模拟「守卫查询发生的那一刻线程刚被建出来」），再调原实现 → 断言 `transition(..., "confirmed")` **被拒**（`ValueError`），且 DB 重读 `blueprint_status` 仍是 `pending_review`（CAS 未执行）。配一条对照：守卫内不插入行时 `transition` 成功 ⇒ 断言非恒真。
   同时断言守卫**在事务内**：`rg` 源码扫描 `_apply_transition_sync` 函数体内 `_has_confirm_blockers_sync` 的出现位置在 `with transaction.atomic():` 之后（用读文件 + 行号比较实现，可执行）。
8. **视图/adapter 零事务外二次查询（源码扫描）**：扫 `server/delivery/api/` 与 `server/services/process_runtime/blueprint_review.py`（若已存在）中不得出现「查 BLOCKER 线程后再调 transition」的形状——具体断言：`aunresolved_blocker_count` 在 `server/delivery/api/` 下的出现不与 `to_status="confirmed"` / `BlueprintStatus.CONFIRMED` 出现在同一文件（本 plan 阶段这两处文件尚不存在则跳过该文件，用 `Path.exists()` 守卫，保证用例在 114-05 交付后自动生效）。
9. **`_arecord_gate_note` 行为等价**：调它留痕 → 最后一条消息 `author_type == "human"`、线程 `status` 不变。
  </action>
  <verify>
    <automated>cd server && uv run pytest tests/delivery/test_blueprint_review_threads.py -x -q</automated>
  </verify>
  <acceptance_criteria>
- `cd server && uv run pytest tests/delivery/test_blueprint_review_threads.py -q` 全绿。
- `rg -n "record_answer" server/tests/delivery/test_blueprint_review_threads.py` 命中（反向断言存在）。
- `rg -n "append_note" server/tests/delivery/test_blueprint_review_threads.py` 命中 ≥2。
- `rg -n "pytest.raises\(ValueError\)" server/tests/delivery/test_blueprint_review_threads.py | rg -c ""` ≥4（不变式四条错配 + 非法 severity）。
- `rg -n "transaction=True" server/tests/delivery/test_blueprint_review_threads.py` 命中（async service 跨线程写库）。
- `rg -n "_has_confirm_blockers_sync" server/tests/delivery/test_blueprint_review_threads.py` 命中（TOCTOU 用例存在）。
- `cd server && uv run pytest tests/delivery/ tests/services/process_runtime/ -q` 无新增失败。
- `cd server && uv run ruff check tests/delivery/test_blueprint_review_threads.py && uv run ruff format --check tests/delivery/test_blueprint_review_threads.py` 通过（新文件可 format）。
  </acceptance_criteria>
  <done>severity 三档落库、不变式四条错配被拒且 DB 不写、`append_note` 留痕后门仍在、`record_answer` 的破坏性被反向断言坐实且新守卫纵深挡住、并发下 confirm 不放行且守卫确在事务内。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AI 审查 findings → 线程 `blocking` 语义 | `blocking` 决定「人审能否通过」，是本相位唯一的安全闸 |
| `pending_review → confirmed` 转移 | 跨越此边界即「蓝图被认可、可进编码」——守卫失守等于绕过人审 |
| 线程留痕通道 | 写消息的路径若能顺带改 status，就能间接放行守卫 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-114-01 | Elevation of Privilege | 中间留痕用 `record_answer` 把 BLOCKER finding 推到 `answered`，confirm 守卫放行 | mitigate | 提炼 `append_note` 作为唯一留痕通道 + prohibitions 明令禁用 `record_answer` + Task 3 第 6 条反向断言 + 守卫判据②纵深覆盖 `answered` 的 blocker finding |
| T-114-02 | Tampering | `severity=blocker` 却 `blocking=False`（或反之）造成「blocker 不阻塞」/「warning 却阻塞」 | mitigate | `open_thread` 在 DB 写之前强制 `blocking == (severity=="blocker")`，四条错配用例 + 两条合法对照 |
| T-114-03 | Tampering | 视图层「先查 BLOCKER 再 transition」的 check-then-act 窗口 | mitigate | 两条判据收敛进 `_apply_transition_sync` 已有事务的**单次 `Q` 查询`**；`aunresolved_blocker_count` docstring 明写「仅供报告」；Task 3 第 7/8 条并发用例 + 源码扫描 |
| T-114-04 | Tampering | 提炼 `append_note` 时新增第二条旁路写表路径，绕过 INV-6 | mitigate | `_arecord_gate_note` 改为委托、`BlueprintThreadMessage.objects.create` 出现处计数不增、`test_blueprint_inv6_guard` 保持绿 |
| T-114-05 | Info Disclosure | finding 正文（半可信蓝图内容，可能夹带凭证样本）经日志外泄 | mitigate | `append_note` / `open_thread` 的日志只记 `thread_id` / `kind` / `severity` / `author_type` 标量，`body` 与 `question` 绝不进日志 |
| T-114-06 | Denial of Service | `severity` 形参做成必填导致 112/113 既有调用全挂 | mitigate | 强制默认空串 + 「既有调用逐字等价」用例 + `tests/delivery/` 全量无新增失败 |
| T-114-SC | Tampering | 依赖供应链 | accept | 零新增外部依赖、零 migration |
</threat_model>

<verification>
- `cd server && uv run pytest tests/delivery/ -q` 全绿（112-05 确认门八端点、111-02 lifecycle、INV-6 扫描一并背书）。
- `cd server && uv run pytest tests/services/process_runtime/ -q` 无新增失败（112/113 的 `open_thread` 调用方）。
- `cd server && uv run python manage.py makemigrations --check --dry-run` 退出码 0（**零 migration**）。
- `cd server && uv run ruff check delivery/ tests/delivery/` 通过；**不对 `blueprint_lifecycle_service.py` 跑 `ruff format`**。
- 冻结面自检：`git diff --name-only` 只含 `server/delivery/services/blueprint_lifecycle_service.py` 与 `server/tests/delivery/test_blueprint_review_threads.py` 两个文件，且不含 `codegraph/services/repo_router_v2.py`、`process_runtime/{decompose_segments,research_adapter,architect_merge_adapter,merged_plan,clarify_adapter,render,resume,builtin_processes,blueprint_resume,blueprint_schema,blueprint_quality,blueprint_merge,blueprint_confirm_gate,blueprint_spec_gate}.py`、`repositories/services/charter_service.py`、`system/settings_service.py`、`delivery/services/event_taxonomy.py`、`agents/call_source.py`、`delivery/models/blueprint_thread.py`、`task/`。
- 受限面自检：`git diff server/delivery/services/blueprint_lifecycle_service.py | rg "^-" | rg -v "^---" | wc -l` ≤ 12，逐行登记归属（`_append_thread_message_sync` / `_arecord_gate_note` / `_apply_transition_sync` / 顶层 import）。
</verification>

<success_criteria>
- findings 分级可落库：`open_thread` 的 `severity` 形参就位、零 migration、既有 112/113 调用逐字等价。
- `blocking == (severity == "blocker")` 成为**被代码强制、被单测锁死**的不变式，从而 confirm 守卫可收敛为一条判据。
- 中间留痕有了不改线程状态的公开通道（`append_note`），`record_answer` 的破坏性被反向断言写进测试而非只写进文档。
- confirm 守卫的两条判据在同一事务的同一次查询内完成，视图层不需要（也被禁止）事务外二次查询 ⇒ TOCTOU 窗口从根上不存在。
- 114-02/03/04/05 可以直接消费 `open_thread(severity=...)` / `append_note` / `aunresolved_blocker_count` 三个稳定契约。
</success_criteria>

<output>
完成后创建 `.planning/phases/114-ai/114-01-SUMMARY.md`（provides：`open_thread` 的完整新签名与 `severity` 校验规则、**不变式的确切报错文案**（114-02/03 的错配用例要对齐）、`append_note` 的完整签名与默认 `author_type`、`_append_thread_message_sync` 的新形参、`_has_confirm_blockers_sync` 的两条 `Q` 判据与「必须在事务内调用」约束、`aunresolved_blocker_count` 签名与「仅供报告」约束、`_apply_transition_sync` 的**新报错文案**（`blueprint_gate_views.py` 若有按串匹配须登记核对结果）、`BlueprintThreadMessage.objects.create` 的实测出现处数（**期望 4**，证明未新增写表路径）、**受限面删除行逐行登记**（行号 + 归属函数 + 理由）、零 migration 的验证命令与结果）。
</output>
