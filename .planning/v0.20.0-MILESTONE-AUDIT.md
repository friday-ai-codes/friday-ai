---
milestone: v0.20.0
milestone_name: 技术方案蓝图
audited: 2026-08-01T23:45:00+08:00
closure_applied: 2026-08-02T00:45:00+08:00
status: tech_debt
status_rationale: |
  `gaps_found` 的**唯一决定性依据**是 CLAR-03（见 §2.4 / §11），它已按 §11 的路径 (a) 补面闭合
  （§13，后端零改动）⇒ 判据消失。

  ⭐ **G1/G3/G4 为何不再拦归档，理由写在这里而不是被静默删掉**：三者都在 `gaps.integration` 下，
  但本报告自己已两处判定它们**不阻塞归档** —— ① 三者均在 GATE-01 的 PARTIAL 覆盖范围内（该需求已
  明示入口收编待同步点 2 完成）；② 四个 per-entry 开关默认值全为 `technical_plan` ⇒ **默认开关下零
  生产影响**（§4.1 逐条标注「默认开关下潜伏」）；③ 修法**硬阻塞在同步点 2**（v0.19.0 Phase 109/110
  合并），且必须与翻默认同批做 —— 本里程碑内**结构上不可能闭合**，强行闭合是主动制造 RELY-01 违规。
  ⇒ 与 CLAR-03 的判据（可闭合性）正好相反，三者转入 `tech_debt.116-entry`，⛔ 不是被认为已解决。

  `status: tech_debt` 的含义因此是：**无可在本里程碑内闭合的缺口**，而非「无遗留问题」。
  遗留问题的权威清单是下方 `tech_debt` 与 `STATE.md` Pending Todos。
phases: "111-116"
scores:
  requirements: 34/35
  phases: 6/6
  integration: 7/10
  flows: 5/6
closed_gaps:
  - id: "CLAR-03"
    closed_at: 2026-08-02
    path: "(a) 补面（⛔ 不是 (b) 收窄措辞）—— 需求文本一字未改"
    commits: ["25228ead", "83be08fc", "7760ae2d", "4b32c642", "24df6187"]
    evidence: "见 §13"
gaps:
  # ⛔ 两个桶均已清空，但清空的**原因不同**：requirements 是真闭合（CLAR-03，见 closed_gaps 与
  # §13）；integration 是**重分类**为技术债（G1/G3/G4，理由逐条见 status_rationale 与
  # tech_debt.116-entry 的新增条目）。§4.1 的三张接缝判定表**一字未改**，它们仍是 BROKEN。
  requirements: []
  integration: []
gaps_closed_detail:
  requirements:
    - id: "CLAR-03"
      severity: "blocker（已闭）"
      phase: "Phase 114 后端交付 / Phase 115 前端顺延 / Phase 116 未接手"
      claimed_status: "Complete（REQUIREMENTS.md:51 复选框 [x]、:131 Traceability Complete）"
      actual: "需求首句「人类可直接编辑蓝图内容（block 级）」在产品面**不可达**。后端 `POST .../blueprint-review/edit-blocks/` 已路由并实现，归属审计与「人工编辑不被 AI 覆盖」两条保护链均为真；但 `web/src/api/blueprints.ts` 无对应函数、`web/src/` 零消费方，且 115 留了一条**主动阻止**该入口出现的守卫（`blueprint-source-guard.spec.ts:90` 断言 `edit-blocks?|editBlocks` 零命中）"
      why_audit_only: "114 的验证口径是后端（UI 归 115）、115 的需求清单不含 CLAR-03（它owns VIEW-01~04/CLAR-01/FLOW-08）⇒ 该缺口结构性地落在两个相位验证范围的夹缝里，六份 VERIFICATION 无一能看见"
      contradiction: "115 守卫的「怎么修」写的是『删掉该入口；确实需要改内容时走人审驳回（reject/）再重新产出』——这是**反对**实现该 UI 的设计论证，而同一守卫的上一行又写『顺延 Phase 116』。116 的七个计划（入口分派/intake/MCP 澄清/飞书导出/图谱物化/assumptions 分档/file-lines）无一接手。⇒ 需求文本与实现意图分歧，且**未经调和**：REQUIREMENTS 措辞一字未改、状态直接转 Complete"
      closeable_in_milestone: true
      closure_options:
        - "建 closure phase 落 block 编辑面（后端已就绪，前端需删守卫 + 加 API 函数 + 编辑态 UI）"
        - "或按 SC-3/SC-4 的既有做法**正式收窄 CLAR-03 措辞**（改为『人工编辑经 API 可用 + 不被 AI 覆盖』），在 REQUIREMENTS/ROADMAP/STATE 三处同步记录理由，并把 115 守卫的矛盾注释改成单一立场"
      resolution: "✅ **已闭（2026-08-02）—— 取 closure_options 第 1 条（补面）**。取补面而非收窄措辞的理由：需求首句承诺的是一条具体的用户能力、后端完全就绪、无任何语义障碍 ⇒ **兑现能力比事后重定义需求更诚实**（路径 2 虽正当，但它把「实现方认为不该有这个能力」这件事变成需求本身的退让，而 114-MJ-04 的状态闸已经让那条设计论证的前提不成立）。REQUIREMENTS 的 CLAR-03 措辞**一字未改**。后端**零改动**。逐条证据见 §13"
carried_forward_detail:
  # ⛔ 本段不是「已解决」清单，是**重分类**：三条接缝仍 BROKEN（§4.1 三张表一字未改），
  # 只是按可闭合性判据转入 tech_debt。理由见 frontmatter 的 status_rationale。
  integration:
    - id: "G1"
      severity: "blocker（默认开关下潜伏）"
      seam: "蓝图会话 → workflow 入口出口映射"
      detail: "`plan_research._maybe_suspend`（`:468-471`）用旧链模型 `ClarificationService().ahas_pending` 判待答；蓝图链**从不写 `Clarification` 行**（本轮 grep 实证：`Clarification.objects.create` 全仓仅 `clarification_service.py:110` 一处，蓝图侧写的是 `BlueprintThread`）⇒ 停在 `waiting_clarification` 的蓝图会话 `pending=None`、不发卡、不建订阅、`suspend=None`，落到 `_map_terminal` 非 DONE 分支 ⇒ `status=failed` / `error_code=plan_session_failed` / `next_handle=error`。**每一次规格门提问与确认硬门都会把会话判死**，比已登记的「DONE→completed 一行」严重一档"
    - id: "G3"
      severity: "blocker（默认开关下潜伏）"
      seam: "蓝图会话 → MCP 入口主载荷"
      detail: "`orchestration_delegate.py:267-276` 的 `DONE→completed` 无蓝图分支且 `_load_canonical` 用 v0 渲染器；`technical_plan_service.py:440` 的 `_map_execution_plan_to_repository_tasks` 读 `content[\"execution_plan\"]`，blueprint/v1 无该顶层键 ⇒ 返回 `[]`。**响应结构合法而语义为空**（静默降级）。GATE-01 的异步澄清那一半是真接通的，空的是主载荷"
    - id: "G4"
      severity: "blocker（默认开关下潜伏）"
      seam: "蓝图会话 → feature_list 入口出口映射"
      detail: "`initiatives/services/feature_solution_service.py:485-512` 同为旧链模型：FAILED→failed、DONE→completed+旧形态 attach、其余一律 `STATUS_RESEARCHING` 且待答问题取自 `ClarificationQuestion`。阻塞在 `BlueprintThread` 上的蓝图会话 ⇒ 永久 researching + 空问题列表 + 该面无法解阻"
  flows:
    - "confirmed → execution_plan → 编码分发（无生产驱动方，见 §3.3）"
partial:
  - id: "GATE-01"
    status: "partial"
    phase: "Phase 116 入口收编与导出（+ 同步点 2 后的收尾）"
    claimed_by_plans: ["116-01-PLAN.md", "116-02-PLAN.md", "116-03-PLAN.md", "116-06-PLAN.md"]
    completed_by_plans: ["116-01-SUMMARY.md", "116-02-SUMMARY.md", "116-03-SUMMARY.md", "116-06-SUMMARY.md"]
    verification_status: "passed"
    evidence: "四入口蓝图路径全部可达且经 ast 守卫锁死字面量 entry_key（plan_research.py:304 / plan_research_tools.py:122 / orchestration_delegate.py:156 + technical_plan_service.py:71 / feature_solution_service.py:265），MCP 异步澄清协议全量交付。⏭ 未做：DEFAULT_ENTRY_SWITCH 四键仍全 technical_plan（blueprint_entry_switch.py:60-65 实测）、plan_research._map_terminal（plan_research.py:626）仍把 DONE 映射成 completed。⭐ 顺延有语义硬理由而非排期理由：蓝图 DONE 语义是「等人审」，今天翻默认 = 未经人审的蓝图直送 ai_coding，正面违反 RELY-01。硬依赖同步点 2（v0.19.0 Phase 109/110 合并），⛔ 无法在本里程碑内闭合。ROADMAP SC-1 / REQUIREMENTS GATE-01 / STATE Pending Todos 三处措辞已实测一致。⚠️ **本轮修正「已交付」那一半的口径**：四入口只是能**建**蓝图会话，其中三个（workflow / feature_list / mcp）不能正确**观测**它——见 gaps.integration G1/G3/G4。chat 是唯一做对的（`plan_research_tools._map_terminal_blueprint:474-531` 按 blueprint_status 分桶、pending_review 计成功）。⇒ 同步点 2 的待办清单比 STATE.md 记的「四件事」大：`_map_terminal` 一行之外，还要重做 workflow 的挂起判据、feature_list 的问题来源、MCP 的主载荷映射。"
nyquist:
  compliant_phases: []
  partial_phases: []
  missing_phases: [111, 112, 113, 114, 115, 116]
  overall: missing
tech_debt:
  - phase: 116-entry
    items:
      - "⭐ 同步点 2 的四件事必须同批做：翻四个 per-entry 开关默认值 + **三个入口的出口映射整体重做**（见下条 G1/G3/G4，⚠️ 不是「`_map_terminal` 一行」）+ TechPlanCard/NodeDataTab/ArtifactTimeline 三处触点升级 + 旧 technical_plan process 退役收口。任何一件单独做都造成回退。阻塞在 v0.19.0 Phase 109/110 合并节奏"
      - "⛔ **G1/G3/G4 三道边界接缝（由 gaps.integration 重分类而来，⛔ 非已解决）** —— 蓝图链能建会话的三个入口不能正确**观测**它：**G1 workflow** `_maybe_suspend`（`plan_research.py:468-471`）用旧链 `ClarificationService().ahas_pending` 判待答而蓝图链只写 `BlueprintThread` ⇒ 每一次规格门提问与确认硬门都把会话判成 `failed`（要改的是**挂起判据**，`_map_terminal:626` 只是同一处的第二半）；**G3 MCP** `_map_execution_plan_to_repository_tasks`（`technical_plan_service.py:440`）读 blueprint/v1 不存在的 `content['execution_plan']` ⇒ `repository_tasks` 恒 `[]`（**结构合法而语义为空**的静默降级，要改的是**主载荷映射**）；**G4 feature_list** 待答问题取自 `ClarificationQuestion` ⇒ 永久 `researching` + 空问题列表（要改的是**问题来源**）。对照组 chat 的 `_map_terminal_blueprint`（`plan_research_tools.py:474-531`）做对了。⭐ **转技术债而非归档阻塞项的三条理由**：① 三者均在 GATE-01 的 PARTIAL 覆盖范围内；② 四个开关默认全 `technical_plan` ⇒ 默认开关下**零生产影响**；③ 修法硬阻塞同步点 2 且须与翻默认同批 ⇒ 本里程碑内**结构上不可能闭合**。⚠️ **排期顺序提醒**：真翻 workflow 或 feature_list 开关后**第一次澄清**就撞 G1/G4，**早于**「未经人审的蓝图进 ai_coding」这个已知风险（后者要等会话跑到终态才显形）"
      - "蓝图 confirmed 之后无下游驱动方：IMPLEMENTING / IMPLEMENTED / ARCHIVED / SUPERSEDED **四态**在 _ALLOWED_TRANSITIONS 内有边、无生产 writer（本轮 grep：非测试命中只剩转移表声明与 blueprint_resume._HUMAN_OWNED_STATUSES 这个读侧短路清单）；derive_technical_plan_document 的唯一调用方是 evaluate_blueprint_golden 命令，编码分发链路尚未真正消费蓝图派生产物（execution 投影归 v0.19.0 Phase 109）。⭐ SUPERSEDED 无 writer 单独值得记：它是「新蓝图取代旧蓝图」的机制位，SCHEMA-07 的项目级多版本演进目前只走 ArtifactVersion.supersedes 版本链，蓝图级取代语义尚未启用"
      - "SettingKeys.BLUEPRINT_ENTRY_SWITCH 无运维面：全仓消费方只有解析器与其测试，无 admin UI、无 seed 行 ⇒ 翻某个入口需要经通用设置 API 裸写 SystemSetting。同步点 2 翻默认时会第一次真正需要它"
      - "确认门 stage_state 回退分支在锁定后失效：`blueprint_confirm_gate.py:529-534` 对已锁定会话回 `{\"repos\": []}` 存根 ⇒ `acollect_locked_repos` 文档所述的「回退到 stage_state 快照」在最需要它时恒空。失败是**可见**的（dispatch_plans 归零 → _abp_repo_plan_is_stuck → 开阻塞线程 + needs_clarification），不是静默，故记债不记缺口"
      - "`blueprintBlocks.ts:433` 别名表 docstring 引 `builtin_processes.py:850-960`，实际 stage 字典在 `:1030-1143`（行号漂移，纯注释）"
      - "chat 回灌的挂载点靠人工清单维持（_afeedback_chat_blueprint_barrier 三个挂载点，多挂幂等安全但「哪些路径通向终态」无结构性保证）；彻底形态需让终态转移发事件，要动 §13.2 冻结的 ConvergenceSessionEvent，与「areopen_stage 未发事件」同批定夺"
      - "~~apscheduler 周期提醒尚未接上 116-06 的送达收口~~ —— **归档前收尾已闭**（commit 162eecd5，见 §12）。残留：澄清飞书卡片仍是通知形态，交互回调等同步点 1"
      - "⛔ **新发现（子模块 checkout 后才可见）：mcp npm 包与服务端工具面漂移四个工具** —— tests/mcp_tools/test_mcp_package_alignment.py 实测 `服务端有、包缺失：['answer_blueprint_clarification', 'get_technical_blueprint', 'read_blueprint_context', 'report_blueprint_context']`。该守卫在 mcp/ 子模块未 checkout 时 pytest.skip ⇒ 全里程碑从未实跑过。四个工具全部是本里程碑新增（113-02 两个总线工具 + 116-06 两个澄清工具），mcp/src/tools.ts 的 FRIDAY_TOOLS 是**静态白名单、未知工具直接拒绝** ⇒ 经 npm 包接入的 agent **调不到它们**。⚠️ 这削弱 GATE-01「MCP 异步澄清协议全量交付」的口径：服务端端点齐备，但 npm 客户端不可达。修法是给 mcp 子模块补四个条目并发版（跨仓改动，⛔ 不在本 worktree 动子模块指针）"
  - phase: 115-ui
    items:
      - "115-MN-03：_aassert_project_scope 的 400 分支对 meta.project_id 非法的那批 artifact 仍构成存在性预言机；116 让暴露面从 11 扩到 15 个端点（已知代价、import 复用同源实现）。正确形态是四语义契约整体改版，⛔ 不是单点改一行状态码"
      - "UI 审计 4 项跳过：UI-SPEC §11.1 Separator+border-l 双竖线冗余（契约自身冗余）、§11.2 指标单位（登记为刻意简化）、7 个死 i18n 键（其中 2 条是缺口标记而非垃圾）、useCitationPreview 五个返回键零消费（架构取舍，等 §13.8 修订）"
      - "ui/badge 的 warning variant 绕开 --color-warning 令牌（全站级，既有共享原语不在本里程碑 CREATE-ONLY 边界内）"
  - phase: 114-ai
    items:
      - "全仓仍有二十余处 error=str(exc) 未脱敏（均早于本纪律）；114 已把蓝图链九个模块收口并加 AST 守卫，全仓收口另起独立清理相位"
      - "ConvergenceSessionService.areopen_stage 未发 ConvergenceSessionEvent（§13.2 把既有事件类型定为 consume-only）；若时间线需要「人审驳回导致的会话复位」可见，需同步点 2 后与 0.19 时间线契约一并定"
  - phase: 113-2
    items:
      - "113-MN-09：RepoPlan 重试计数无服务端权威来源（两条建议修法一条撞冻结面、一条是安全倒退）；正解是加服务端权威计数列，另起小相位"
      - "test_threaded_concurrent_appends_have_no_duplicate_or_gap 在满套件下偶发失败（SQLite 表级写锁，生产 PG/MySQL 不出现；核心不变量断言从未失败，失败的是「竞争真的发生过」的测试自检）"
  - phase: 112-1
    items:
      - "FLOW-02 的「替代建议」无结构化承载字段（fitness 只收 verdict/reasons/citations/role_suggestion/responsibility/findings）；需求文本未要求结构化故判 Complete，补 schema 字段等机器消费方出现"
      - "112-MN-06：删除/启用皆属零行为收益的 churn，MJ-06 的 match_kind 证据字段一并保留"
  - phase: 111-schema
    items:
      - "111-MN-06：需新增 migration 才能修，留待有 migration 批次时一并做"
      - "平台级观察：RepositoryPermission 是「任意登录用户可读任意存在的仓库」而非仓库级 ACL（非本里程碑引入），与 111-MN-12 权限口径一并定夺"
  - phase: platform
    items:
      - "redact_secrets_in_text 不覆盖数据库连接串（SENSITIVE_VALUE_PATTERN 只替换 sk-ant-*/sk-*/AIza*/Bearer */PEM）；实测 postgres://user:pass@host/db 原样进日志。与「全仓 error=str(exc)」合并成独立清理相位"
      - "六个相位均无 *-VALIDATION.md（workflow.nyquist_validation = true）；蓝图链的 Nyquist 采样覆盖从未被独立审计过"
human_verification:
  - test: "对一份 pending_review 的真实蓝图点「导出飞书文档」，打开生成的文档"
    expected: "首行是未经确认标注；六段全量 + 决策记录附录 + 引用脚注可读；heading/表格/列表版式无结构性丢失"
    why_human: "本机与 CI 均无飞书凭证；markdown 生成侧与端点侧均已自动化覆盖，未验的只有 markdown_to_blocks 转换侧（本里程碑零改动的既有件）"
    blocking: false
  - test: "把某个入口开关翻到 technical_blueprint，在真实环境跑一条端到端需求"
    expected: "会话建成 technical_blueprint、intake 落 v1 骨架、spec_gate 按 assumptions 档位开澄清、作答后续驱推进"
    why_human: "真实 LLM / 真实容器参与的端到端体感。⚠️ 默认仍是 technical_plan，本项不是上线前置"
    blocking: false
  - test: "在真实浏览器打开含 mermaid 块的蓝图；拖选正文观察 popover 落点；滚动观察左栏导航高亮；< xl / < md 两档窗宽下检查三栏收拢"
    expected: "mermaid 出图；popover 贴选区末端不遮挡；高亮随滚动逐段推进；侧栏按断点收拢"
    why_human: "happy-dom 无版面引擎（getBoundingClientRect 返回零矩形）、不计算媒体查询；各自的可判定内核已由自动化覆盖"
    blocking: false
  - test: "复跑 tests/mcp_tools/test_skills_snapshot_guard.py（原记为「须在主检出跑」）"
    expected: "三条全绿，含 skills 文档工具名 ⊆ TOOL_SCHEMA_SNAPSHOT 的子集断言"
    why_human: "本 worktree 的 skills/ 为空目录 ⇒ SKILL_FILES 为空 ⇒ 子集断言在此空跑。本里程碑新增两个 MCP 工具与一个动词前缀，该守卫的文档面从未在此实跑过"
    blocking: false
    status: "✅ 已完成（归档前收尾）—— 在本 worktree 初始化 skills/ 与 mcp/ 子模块后就地复跑，3 passed。SKILL_FILES 非空 ⇒ 子集断言不再空跑，两个新 MCP 工具与 answer 动词前缀均过文档面。⚠️ 同一次子模块初始化让另一条此前恒 skip 的守卫 test_mcp_package_alignment 首次实跑并**转红**，见 tech_debt.116-entry 的新增条目与 §12"
---

# v0.20.0 技术方案蓝图 — 里程碑审计报告

> ⭐⭐ **本报告的判定已于 2026-08-02 更新为 `tech_debt`**：唯一的决定性依据 CLAR-03 已按 §11 的路径 (a) 补面闭合（**后端零改动**），见新增的 **§13「CLAR-03 闭环」**。⛔ **G1/G3/G4 三道边界接缝未闭**，它们按可闭合性判据转入 `tech_debt.116-entry`（理由见 frontmatter `status_rationale` 与 §13.4）—— §2 至 §12 的所有判定与证据**一字未改**，下面这段结论保留的是**闭环前**的原貌。

**结论（闭环前原貌）：GAPS_FOUND** — 35 条需求中 33 条完全满足、1 条（GATE-01）按已记录的范围调和判 PARTIAL 且**无法在本里程碑内闭合**、**1 条（CLAR-03）判缺口且可在本里程碑内闭合**；六个相位全部 verified、跨相位七道内部接缝全部 WIRED，但**蓝图链与其消费方之间的三道边界接缝断裂**（workflow / feature_list / MCP 三个入口能建蓝图会话却不能正确观测它）；§13.2 并行边界纪律全里程碑守住（冻结面 `git diff` 为空）。

**判 `gaps_found` 的决定性依据是 CLAR-03**：需求首句「人类可直接编辑蓝图内容（block 级）」被记为 Complete，但该能力在产品面**不可达**——后端端点齐备、前端零消费方，且 115 留了一条**主动阻止**该入口出现的守卫。它落在 114（只验后端）与 115（需求清单不含 CLAR-03）两个相位验证范围的夹缝里，六份 VERIFICATION 结构性地看不见它。与 GATE-01 不同，**这条能在本里程碑内关闭**（后端已就绪），因此本工作流对 `gaps_found` 的路由——插入 closure phase——在这里既可执行也应执行。（⇒ **已执行，见 §13**。）

> **本报告经两轮独立核查**：第一轮为审计员本人的四源交叉 + 逐条回源；第二轮为独立集成核查代理的跨相位接缝走查。第二轮推翻了第一轮的两处结论（§6.1 的「未发现乐观转 Complete」、§2.3 对 GATE-01「已交付」那一半的口径），并把 §3.3 的死态从 2 个扩到 4 个。所有推翻结论均已由审计员亲自回源码复证（证据行号见各节），不采信代理自述。

---

## 1. 审计方法

- **口径**：六份 `*-VERIFICATION.md` 的 frontmatter + Requirements Coverage 表、35 份 `*-PLAN.md` / `*-SUMMARY.md` 的 frontmatter、`REQUIREMENTS.md` Traceability、`ROADMAP.md` 的 Success Criteria、`STATE.md` 的 Pending Todos，四源交叉。
- **立场**：对抗性。本里程碑每个相位都出过至少一次「静默假通过」（112 的 `excluded` 只写不读、113 的三个跨模块断链、114 的「指标接了不存在的字段」、115 的四条 MAJOR、116 的两条「写了没人读」），且 116 的收口发现 26 行 Traceability 长期滞留 Pending。因此**不采信任何 SUMMARY / VERIFICATION 的自述**，Traceability 逐行抽样回到源码验证。
- **实跑**：后端全量、前端 vitest、type-check、`makemigrations --check`、冻结面 `git diff`，全部本轮亲自执行（读数见 §8）。
- **基线**：`git merge-base HEAD main` = `b974a0c2`；全里程碑 `371 files changed, +124177 / −1116`。工作区 `git status --porcelain` 空输出。

---

## 2. 需求覆盖（33/35 satisfied，1 partial，1 gap，0 orphaned）

### 2.1 三源交叉结果

| 源 | 结果 |
|---|---|
| `REQUIREMENTS.md` Traceability | 35 行，全部 `[x]`；34 行 Status = Complete，GATE-01 = PARTIAL |
| 六份 VERIFICATION 的 Requirements Coverage 表 | 8 + 6 + 9 + 4 + 6 + 4（含 116 闭合的 VIEW-02/04）= 逐条 SATISFIED，GATE-01 判「与实际交付一致（PARTIAL）」 |
| 35 份 PLAN frontmatter 的 `requirements` 并集 | 与 Traceability 的相位映射**完全一致**，无孤儿、无重复 |

**孤儿检测：零命中。** 不存在「Traceability 说归某相位但没有任何 plan 认领」或「所有 VERIFICATION 都没提到」的需求。

**⚠️ 一处记账缺陷（不影响判定，但破坏了本工作流的自动核算能力）：** 115 与 116 的 **14 份 SUMMARY 全部缺 `requirements:` frontmatter 字段**（111–114 的 21 份都有）。按本工作流的状态判定矩阵，「VERIFICATION passed + SUMMARY frontmatter missing」应落 `partial（verify manually）`。本次已**逐条人工回源**（PLAN frontmatter + VERIFICATION Coverage 表 + 源码抽查三向对齐），结论为 satisfied；但下次自动化审计会在这 6 条需求上重新报 partial。建议补齐字段。

### 2.2 逐条判定

| REQ-ID | Phase | 判定 | 抽查证据（本轮回源，非采信文档） |
|---|---|---|---|
| SCHEMA-01 | 111 | ✅ satisfied | `blueprint_schema.py:123` 顶层 `required` 含 schema_version/meta/requirement_spec 与六段；`builtin_types.py:24-32` 按 `schema_version` 懒 import 判别分支接进 `ArtifactService` 强制门 |
| SCHEMA-02 | 113 | ✅ satisfied | 投影同时填 `rationale.citations`；`blueprint_merge.py:1696` 覆盖率门 + `coverage_gaps` 归因 |
| SCHEMA-03 | 113 | ✅ satisfied | RepoPlan `impl_items[].change_type`（枚举与 `BLUEPRINT_JSON_SCHEMA` 同源，有直接读取比对的用例）+ 融合侧 `implementation_overview.items[].wave` |
| SCHEMA-04 | 113 | ✅ satisfied（见 §6.3 深度观察） | `interaction_flows[].steps` required = `seq/actor/action`，`api_ref`/`data_in`/`data_out` 为可选字段 |
| SCHEMA-05 | 113 | ✅ satisfied（见 §6.3） | `api_contracts` required = `id/name/kind/direction`；`request_example` / `data_source.availability` 为可选字段，`needs_support` 落位由 grep 证实顶层 `availability` 零残留 |
| SCHEMA-06 | 111 | ✅ satisfied（⚠️ 无生产消费方，见 §3.3） | `blueprint_execution.py:139 derive_execution_plan` / `:216 derive_technical_plan_document`，输出过既有 `validate_technical_plan`、确定性双跑 |
| SCHEMA-07 | 111 | ✅ satisfied | 版本链 supersedes + `diff_blueprint_blocks` 三分类；唯一性守卫由 112 创建入口承担 |
| LIFE-01 | 111 | ✅ satisfied（⚠️ 11 态中 **4 态**无生产 writer，见 §3.3） | `_ALLOWED_TRANSITIONS` 25 边与 DESIGN §4.2 逐边一致；CAS `filter().update()` 为唯一字段级 writer |
| LIFE-02 | 111/114 | ✅ satisfied | `blueprint_lifecycle_service.py:324` 守卫与 CAS 在**同一 `transaction.atomic()` 内**（TOCTOU 窗口从根上不存在）；判据②把 `answered` 的 blocker finding 一并视为未决 |
| LIFE-03 | 111 | ✅ satisfied（⚠️ `superseded` 声明为终态但无生产 writer，见 §3.3） | failed / superseded 显式终态 + `failed→researching` 重试边 |
| CHARTER-01 | 111 | ✅ satisfied | `charter_service` 三源蒸馏 + `human_confirmed` 分支只写 `draft_content`（服务层与 API 层双重不变量专测） |
| CHARTER-02 | 112 | ✅ satisfied | `blueprint_route.py:167` `total = sum(components.values())` 三分量恒等式；`⛔ repo_router_v2.py` 全里程碑零改动 |
| CHARTER-03 | 112 | ✅ satisfied | `blueprint_confirm_gate.py:607` → `charter_draft_writeback.asubmit_charter_draft`（三分支落库，human_confirmed 只写草案） |
| FLOW-01 | 112 | ✅ satisfied | `blueprint_spec_gate` 五步闭环 + `intent` 必填枚举；LLM 不可得时 **fail-closed 判需澄清**；116-06 追加 assumptions 三档且 `assume_more ≠ 跳过澄清` |
| FLOW-02 | 112 | ✅ satisfied（残留见 §7） | GAP-1 已闭：`blueprint_research_adapter.py:296 _excluded_repository_ids` 是 `excluded` 的**唯一读取方**、`:332` 从两条候选来源同时剔除、`:1082` 排除集累积、`:1105` 回写；补候选走 `BlueprintRouteAdapter.route(exclude_repository_ids=)`，补不到即升门不空转 |
| FLOW-03 | 112 | ✅ satisfied | 硬确认门八端点，五动作经 `BlueprintLifecycleService` 收口；六例经真实 REST 入口的端到端证伪线 |
| FLOW-04 | 112 | ✅ satisfied | direct/indirect 分桶 + `upgrade-research` 端点全链路（`aupgrade_to_deep` → `force_deep_repository_ids`） |
| FLOW-05 | 113 | ✅ satisfied | `acollect_locked_repos` 取确认门锁定产物（**非路由候选**）；`arequest_targeted_research` 复用既有机制 |
| FLOW-06 | 113 | ✅ satisfied | `reconcile_cross_repo_apis` 纯函数三桶恒定；矛盾时**不落版本**改开阻塞澄清（有专测） |
| FLOW-07 | 114 | ✅ satisfied | `blueprint_review.py` 七类规则各有强制点；`MAX_REVIEW_ROUNDS = 2`（`:1453`）；超界 `review_exhausted → STAGE_DONE + pending_review`，`ai_review` 的 transitions **不含 failed 出边** |
| FLOW-08 | 115 | ✅ satisfied | 终审两按钮 `disabled` + Tooltip（刻意区别于 readonly 的「不存在于 DOM」）；驳回 comment 必填非空 + `revisionRound + 1` 常驻提示；approve 409 的 `unresolved_blocker_thread_ids` 逐条可点 |
| CLAR-01 | 115 | ✅ satisfied | `<mark data-testid="blueprint-annotation-mark">` 三处渲染分支；选区侦测唯一落点在 `BlueprintBlockList.vue:152`（配对解绑 `:157`）；写口经 `aopen_selection_comment` 收口（View 零 ORM 写） |
| CLAR-02 | 114 | ✅ satisfied | `aapply_thread_answers` 三步链；`decision_log` 保 `answer` 键（规格门 `_collect_prior_answers` 读的就是它）、`decided_at` 取作答消息时间戳保幂等；`orphaned` 线程不删且有 `orphaned_threads` 呈现面 |
| CLAR-03 | 114（后端）+ 115（前端顺延）+ 116（未接手）+ **closure（前端补面）** | ⛔ gap ⇒ ✅ **satisfied @ §13** | 见 §2.4 与 §13。后端三条为真（`produced_by_ref=human_edit:{user_id}`；`detect_human_conflicts` 交集非空 ⇒ 不落版本改开阻塞线程；`arestore_human_blocks` 第二条保护链），但首句的用户能力无产品面 |
| CLAR-04 | 114 | ✅ satisfied（送达通道 deferred） | apscheduler **真实周期**（`runapscheduler.py:182/753`，非伪挂载点）+ `last_reminded_at` 去重 + `order_by(nulls_first)` 防饿死；提醒路径除锚点外零写 |
| BUS-01 | 113 | ✅ satisfied | 三道会话校验（归属 fail-closed / `process_type` / 条目同会话由请求体无会话入参面结构性兜住）+ 项目成员闸 |
| BUS-02 | 113 | ✅ satisfied | 三条路径各有能证伪的断言；长等待那条**真打 `POST /api/mcp/tools/report_blueprint_context/`**；环检测在 `register_waiter` 登记时判定 |
| BUS-03 | 113 | ✅ satisfied | `blueprint_merge.py:2058` **只调** `distill_to_draft`，全仓无 `MemoryService.append` / `confirm_draft` 命中 |
| VIEW-01 | 115 | ✅ satisfied | 十段 `<section>` 静态字面量 id 且无条件渲染（`AnchorNavLayout` 是 mount-only observer，条件渲染会让左栏高亮永久静默失效）；`useBlueprintLive` 是唯一轮询消费点 |
| VIEW-02 | 115 + 116-07 | ✅ satisfied | `repositories/urls.py:295 file-lines/`（`name="repository-file-lines"`）+ `services/repo_file_read.py` 唯一实现（MCP 与 SPA 共享）；SPA 面三态映射成逐字相同的 200 空，MCP 面 404 `file_excluded` 逐字保留 |
| VIEW-03 | 115 | ✅ satisfied | `pages/knowledge/index.vue:42/46/231/243` 四处纯追加；四筛选项与 URL query 双向同步，搜索走已提交值分离 |
| VIEW-04 | 115 + 116-04 | ✅ satisfied | `knowledge/sources/blueprint.py:243` `REFERENCES` + `:373` `RELATES_TO`；`relations` 三层打通（`knowledge/api/views.py:52/196` → service → 最底层既有形参），`_DEFAULT_RELATIONS` **一字未改**；反查用例 `test_related_relations_param.py:67` 从被引方经真实端点查回引用方 |
| VIEW-05 | 116-05 | ✅ satisfied | `render_blueprint_markdown(content, *, blueprint_status)`（`blueprint_render.py:504`，keyword-only 无默认值）+ 闭合白名单 `frozenset({confirmed, implementing, implemented})`（`:50`）⇒ 空串与未知串都出标注；四个非测试调用点无一能传出白名单内的值 |
| GATE-01 | 116 + 同步点 2 | ⚠️ **partial** | 见 §2.3 |
| GATE-02 | 111 | ✅ satisfied（口径见 §6.3） | `evaluate_blueprint_golden` 六道门槛（schema / 派生 / 确定性双跑 / 覆盖率 / 命中率 / 必备功能点），任一 FAIL 非零退出 |

### 2.3 GATE-01 的 PARTIAL 判定与「为什么不判 gap」

**已交付（本轮逐条回源为真）：**

- 四个入口各自的蓝图可执行路径。**不是「有一个开关模块」就算数**——四处 `aresolve_entry_process_type("<字面量>")` 都在生产代码路径的 `if` 上，蓝图分支各 return 一条真实会话：`plan_research.py:304`（workflow）/ `plan_research_tools.py:122`（chat）/ `orchestration_delegate.py:156` + `technical_plan_service.py:71`（mcp）/ `feature_solution_service.py:265`（feature_list）。
- 蓝图 intake、`build_engine_for_session` 按 `process_type` 同时换 **engine 与 driver**、六个续驱点（含 CONTEXT 未点名的 `answer_resume` 与 `plan_clarify_callback` 两条）、feature list 的 `feature_segments → feature_points` 映射、chat 两条断链修复、旧链残余流量按独立 `entry_key` 分桶。
- MCP 异步澄清协议全量（两个新工具 + `create_feishu_technical_plan` 追加三键与 `status=partial` + assumptions 三档 + 飞书卡片首次送达）。

**未做（本轮实测为真的「未做」）：** `DEFAULT_ENTRY_SWITCH` 四键仍全 `PROCESS_TECHNICAL_PLAN`（`blueprint_entry_switch.py:60-65`）；`plan_research.py:626 _map_terminal` 的 `DONE → completed` 一行未改。

**判 partial 而非 gap 的三条理由：**

1. **顺延有语义硬理由，不是排期理由。** 蓝图 stage graph 的 `ai_review.review_passed → STAGE_DONE` 时 `blueprint_status = pending_review`，即蓝图会话的 `DONE` 语义是「等人审」；而 `_map_terminal` 把 `DONE` 无条件映射成 completed 并把 plan 喂给下游 `human_approval` / `ai_coding`。**今天翻默认 = 让编码代理拿着未经人审的蓝图去建分支写代码**，正面违反 v0.19.0 的 RELY-01。提前关闭这条「缺口」是主动制造缺陷。
2. **无法在本里程碑内闭合。** ROADMAP Phase 116 的 Depends on 行逐字把触点升级与默认切换定为**硬依赖同步点 2**（v0.19.0 Phase 109/110 合并）。本工作流对 `gaps_found` 的路由是「插入一个 closure phase」——在这里插不进去，也不该插。
3. **范围调和是记录在案且三处一致的。** 本轮实测 `ROADMAP.md:175`（SC-1 重写）、`REQUIREMENTS.md:75-77 / :139`（GATE-01 的 ✅/⏭ 两段式）、`STATE.md:206`（Pending Todos 第 2 条）三处措辞一致，**没有一处把「未做」写成「做了」**。反向证据更有说服力：GATE-01 这条本可以顺势转 Complete，116 的收口却保持了 PARTIAL。

**⚠️ 唯一的记账不一致：** `REQUIREMENTS.md:75` 的 GATE-01 复选框是 `[x]`，而同行标题与 `:139` 的 Traceability 状态都是 PARTIAL。复选框比正文乐观一档。建议改成 `[~]` 或在复选框行内标注 PARTIAL。

**⭐ 第二轮核查对「已交付」那一半的修正（本轮回源复证）：** 上面「四个入口各自的蓝图可执行路径」这句**只在「能建会话」这个意义上成立**。三个入口能建蓝图会话却不能正确**观测**它：

| 入口 | 建会话 | 观测会话 | 实证 |
|---|---|---|---|
| chat | ✅ | ✅ | `_map_terminal_blueprint`（`plan_research_tools.py:474-531`）按 `blueprint_status` 分桶，`pending_review` 计成功 —— **唯一做对的** |
| workflow | ✅ | ⛔ | 停在 `waiting_clarification` 的蓝图会话被判 **failed**（G1，详见 §4.1） |
| feature_list | ✅ | ⛔ | 永久 `researching` + 空问题列表（G4） |
| mcp | ✅ | ⛔ | 主载荷 `repository_tasks` 恒为 `[]`（G3；异步澄清那一半是真通的） |

这不改变 GATE-01 判 partial 的结论（三条理由仍成立，且这三处同样只在翻开关后才显形），但**改变顺延项的规模**：`STATE.md` Pending Todos 第 1 条记的「四件事」里，`_map_terminal` 那一行实际是三个入口的出口映射重做。归档前应把这条 Pending Todo 的措辞扩到实际范围，否则同步点 2 的排期会基于一个偏小的估计。另需注意先后：真翻 `workflow` 或 `feature_list` 开关，**第一次澄清就会撞上 G1/G4**，比「未经人审的蓝图送进 ai_coding」这个已知风险来得更早。

### 2.4 CLAR-03 —— 判 gap 的理由，以及它为什么六份 VERIFICATION 都没看见

**需求原文（`REQUIREMENTS.md:51`）：** 「人类可直接编辑蓝图内容（block 级），编辑生成新版本、归属可审计；人工编辑不被 AI 覆盖，冲突时 AI 必须开线程询问。」

三个分句里，后两句**完全交付**（114 的两条保护链本轮回源为真）。首句——也就是这条需求的用户能力主张——**在产品面不可达**：

- 后端齐备：`POST .../blueprint-review/edit-blocks/` 已路由（`delivery/urls.py:220-222`）并实现（`blueprint_block_edit.py`）。
- 前端零消费：`web/src/api/blueprints.ts` 无对应函数；全 `web/src/` 对 `edit-blocks?|editBlocks` 的命中**只有两处，且都是断言它不存在**（`blueprint-source-guard.spec.ts:90`、`BlueprintBlock.spec.ts:384`）。
- 115 留了一条**主动阻止**它出现的守卫，而该守卫的注释自相矛盾：第 95 行写「本相位**没有** block 正文编辑面（UI-SPEC §0.1 硬边界第 3 条），**顺延 Phase 116**」，第 98-99 行的「怎么修」却写「**删掉该入口**；确实需要改内容时走人审驳回（`reject/`）再重新产出」。前者是排期语气，后者是**反对该能力存在**的设计论证。
- 116 的七个计划（入口分派 / intake / MCP 澄清 / 飞书导出 / 图谱物化 / assumptions 分档 / file-lines）无一接手。

**为什么判 gap 而不是顺延：** 与 GATE-01 逐条对照——

| | GATE-01 | CLAR-03 |
|---|---|---|
| 顺延理由 | 语义硬理由（提前做 = 违反 RELY-01） | 无语义障碍，后端已就绪 |
| 能否在本里程碑内闭合 | ⛔ 硬依赖外部里程碑同步点 2 | ✅ 可以 |
| 范围调和是否记录在案 | ✅ ROADMAP / REQUIREMENTS / STATE 三处措辞一致，且**保持 PARTIAL 未转 Complete** | ⛔ 措辞一字未改、直接转 Complete、实现意图与需求文本的分歧从未写进任何一处 |

**为什么相位验证看不见它：** 114 的验证口径是后端（UI 归 115），115 的需求清单不含 CLAR-03（它 owns VIEW-01~04 / CLAR-01 / FLOW-08）。缺口精确地落在两个相位验证范围的**夹缝**里——这正是里程碑审计相对于相位验证的存在理由。

**两条闭合路径（二选一，都需一个 closure phase）：**

1. **补面**：删守卫 + 加 API 函数 + 编辑态 UI。后端零改动。
2. **正式收窄需求**：按 SC-3 / SC-4 已用过的做法改写 CLAR-03 措辞（如「人工编辑经 API 可用 + 不被 AI 覆盖」），在 REQUIREMENTS / ROADMAP / STATE 三处同步记录理由，并把 115 守卫那段自相矛盾的注释统一到单一立场。

⭐ 路径 2 完全正当——115 的设计论证（「在只读评审面开编辑入口 = 绕过『要改先驳回』的纪律」）有实质内容。**不正当的是现状**：需求承诺一个能力、实现方论证不该有这个能力、而记账把它记成已交付。

> ✅ **本节结论已由 §13 闭合：取路径 1（补面）。** 补充一条 §2.4 当时未展开的事实 —— 115 那条设计论证的**前提在 114-MJ-04 之后已不成立**：`edit-blocks` 端点如今有 `is_blueprint_editable` 状态闸，已 `confirmed` / `implementing` / `archived` 的蓝图一律 400 ⇒「要改先驳回」由**后端结构性兜住**，不再依赖「前端不给入口」这条软纪律。这也是取路径 1 而非路径 2 的决定性理由：路径 2 会把「实现方认为不该有这个能力」写成需求本身的退让，而支撑该主张的那条前提已经失效。

---

## 3. 端到端链路：从核心价值倒推

里程碑的核心价值（`DESIGN.md §0`）：**「技术方案蓝图 = 一个项目一份的结构化技术方案文档……人类可直接读，AI 可基于它完备编码。」** 下面按「一条需求走完全程」倒推，而不是数勾。

### 3.1 主链路（六段蓝图的产出与消费）—— 通

| # | 环节 | 状态 | 依据 |
|---|---|---|---|
| 1 | 需求进入 → 规格门锁定 | ✅ 通 | `intake → decompose → spec_gate`；歧义超阈值开带候选选项的阻塞线程，LLM 不可得时 fail-closed **判需澄清而非放行** |
| 2 | 双面路由 → 逐仓容器调研 | ✅ 通 | `spec_locked → route → repo_research`；三分量融合可拆解，逐仓容器带任务 token 与知识 MCP |
| 3 | 不合适仓有界重路由 | ✅ 通 | `reroute` ≤2 轮，排除集累积 + 补候选真跑路由，补不到即升门（**不空转**——这正是 112 GAP-1 修掉的形状） |
| 4 | 硬确认门锁定仓库集与职责 | ✅ 通 | `repo_confirmation` 五动作驱动重调研；确认后 `confirmed → repo_plan` |
| 5 | 分仓方案 + 上下文总线协商 | ✅ 通 | `repo_plan` + `BlueprintContextEntry`；短等待 / 长等待重派 / 互等环三条路径各有能证伪的断言 |
| 6 | 六段融合 + 跨仓 API 对账 | ✅ 通 | `merge`；覆盖率门真生效、`needs_support` 落 `data_source` 下、矛盾抛澄清而非静默拍板 |
| 7 | AI 对抗审查 → 有界归因打回 | ✅ 通 | `merge.merged → ai_review`；仓级回该仓、融合级回 merge，≤2 轮后携未决清单转 `pending_review`，**绝不落 FAILED** |
| 8 | 澄清收敛 + 人工 block 编辑 | ✅ 通 | 答案回灌产新版本 + `decision_log` 物化 + 重锚定；人工块两条保护链 |
| 9 | 查看器阅读 / 批注 / 版本 diff | ✅ 通 | 十段结构化渲染 + 划线高亮 + 选区评论 + block 级 diff |
| 10 | 人审终审 → confirmed | ✅ 通 | approve 经事务内守卫（含对 `answered` blocker 的纵深防御）；reject 三步链 + `areopen_stage` 复位到 merge |
| 11 | 导出飞书 + 图谱物化与反查 | ✅ 通 | 十段全量 + 决策附录 + **不可关闭**的未确认标注；`REFERENCES`/`RELATES_TO` 两类边 + 经真实端点的端到端反查 |

**判定：`需求 → 人类可读的蓝图` 这半条核心价值，链路完整闭合。**

### 3.2 缝隙一：整条链路默认不通电

第 1 步的入口在四个 `entry_key` 上都默认走**旧** `technical_plan` process。上面 11 步全部只在**把某个 per-entry 开关手工翻到 `technical_blueprint` 之后**才可达。这是本里程碑最大的一处「可达但未启用」，且如 §2.3 所述**顺延正确**——但审计口径上必须说清：**今天没有任何一个真实用户会走到这条链路上**，除非运维显式改一个设置。

### 3.3 缝隙二：`confirmed` 之后没有下游 —— 「AI 可依此完备编码」尚未闭合

这是本次审计**新定位**的一处结构性缝隙，各相位 VERIFICATION 都没有覆盖（它们的口径都是相位内）：

- `_ALLOWED_TRANSITIONS` 里有 `CONFIRMED → IMPLEMENTING → IMPLEMENTED → ARCHIVED` 与 `* → SUPERSEDED`，但**这 4 态全无生产 writer**。本轮 grep 实证：`BlueprintStatus.(IMPLEMENTING|IMPLEMENTED|ARCHIVED|SUPERSEDED)` 的非测试命中只剩两处——转移表自身的**声明**（`blueprint_lifecycle_service.py:137-174`）与 `blueprint_resume._HUMAN_OWNED_STATUSES`（`:82-85`）这个**读侧短路清单**。零处 `transition(..., X)`。11 态里这 4 态目前是纯声明态。
- 其中 `SUPERSEDED` 值得单列：它是「新蓝图取代旧蓝图」的机制位。SCHEMA-07 的「一个项目一份活跃蓝图，多版本演进」目前靠 `ArtifactVersion.supersedes` 版本链 + 112 创建入口的唯一性守卫兑现（**这两条都为真，故 SCHEMA-07 仍判 satisfied**），但蓝图**级**的取代语义尚未启用。
- `derive_technical_plan_document`（SCHEMA-06 的「编码分发链路零改动可消费」）的**唯一非测试调用方是 `evaluate_blueprint_golden` 管理命令**。没有任何生产路径把一份 confirmed 蓝图派生成 technical_plan 文档交给编码链。该模块 docstring 承诺的「保证下游 coding dispatcher 零改动可消费」因此**只在 golden set 的静态 fixture 上验证过，从未在生产接线上验证过**。

**这不构成需求缺口**：SCHEMA-06 的措辞是「可消费」（可消费性已由「派生输出过既有 `validate_technical_plan` + 确定性双跑」证明），LIFE-01 的措辞是「状态转移有守卫且全程可追溯」（状态机本身完备）。**但它是核心价值后半句的实际断点**，且与 §3.2 同源——两者都归同步点 2 之后的 execution 投影（v0.19.0 Phase 109）。审计将其记为顺延而非缺口，但明确登记：**v0.20.0 交付的是「人类可读」那半条，「AI 可依此完备编码」在编排层已就绪、在分发层尚未接线。**

### 3.4 E2E 流判定（5/6）

| Flow | 判定 | 断点 |
|---|---|---|
| 需求 → 规格锁定 → 路由调研 → 确认门 | ✅ 通 | — |
| 规格门澄清一问一答的**入口侧回环** | ⚠️ 仅 chat 通 | workflow 判 failed（G1）、feature_list 永久 researching 且无问题（G4）、mcp 无续驱面（G3）——三者均只在翻开关后显形，见 §4.1 |
| 确认门 → 分仓方案 → 总线协商 → 六段融合 | ✅ 通 | — |
| 融合 → AI 审查 → 澄清/编辑收敛 → pending_review | ✅ 通 | — |
| 查看器阅读/批注/终审 → confirmed | ✅ 通 | — |
| 导出飞书 + 图谱物化 + 反查 | ✅ 通 | 飞书转换侧待 UAT（无凭证） |
| confirmed → execution_plan → 编码分发 | ⛔ **未接线** | `derive_technical_plan_document` 无生产消费方；`implementing` / `implemented` / `archived` / `superseded` 四态无生产 writer（顺延同步点 2 / v0.19.0 Phase 109） |

---

## 4. 跨阶段集成（相位间 7/7 WIRED，边界 3 道 BROKEN ⇒ 7/10）

各相位 VERIFICATION 的 Key Links 表只覆盖相位**内**接线，本节只打**跨相位接缝**。§4.1 是第二轮核查新增的一类：蓝图链与其**消费方**之间的边界接缝——它既不属于任何单个相位，也不属于相位与相位之间，因此此前无人打过。

| # | 接缝 | 判定 | 证据 |
|---|---|---|---|
| 1 | 111 → 112/113/114：`BlueprintLifecycleService` 是否真是全里程碑唯一 writer | ✅ WIRED | INV-6 守护测试（模型级 + 字段级 + 守护的守护）跨四个相位持续绿；111 deferred 的 `blueprint_anchor` / 7 个 `call_source` 值均在 112–114 取得生产调用方 |
| 2 | 112 → 113：确认门锁定产物 → 分仓方案输入 | ✅ WIRED | 113 的 `acollect_locked_repos` 取**确认门锁定产物**而非路由候选（有专测把这条契约钉死） |
| 3 | 113 → 114：融合产物 → 审查对象 | ✅ WIRED | `builtin_processes.py:935 "merged": "ai_review"`（蓝图链）对照 `:245 "merged": STAGE_DONE`（legacy 链）—— 只改了蓝图链 |
| 4 | 114 → 115：快照字段键名与语义两侧一致 | ✅ WIRED | 前端 `isUnresolvedBlocker` 的三条 AND 与后端 confirm 闸 `blueprint_lifecycle_service.py:441-446` **逐字相同**（都不看 `blocking`、不看 `anchor_status`）；`orphaned_threads` / `unresolved` 均有后端产出与前端消费 |
| 5 | 115 → 116：开关分派进的是同一张 stage graph | ✅ WIRED | 四入口经 `start_blueprint_orchestration` 建同一 `process_type`；前端 `BLUEPRINT_STAGES` 与后端 stage 名的换算走 `SESSION_STAGE_ALIASES`（`repo_confirmation→confirmation` / `reroute→route`）+ `PRE_TIMELINE_SESSION_STAGES`，**别名表存在**（缺它 `indexOf` 返 −1、位序推断整条静默失效） |
| 6 | stage graph 十节点可达性与出边完整性 | ✅ WIRED | `intake→decompose→spec_gate→route→repo_research→reroute→repo_confirmation→repo_plan→merge→ai_review`；三条回边（`spec_gate` self-loop / `repo_confirmation.research_required→repo_research` / `reroute.reroute_needed→repo_research`）与两条升门边（`reroute.exhausted→repo_confirmation`、`review_exhausted→DONE`）齐全，蓝图链**无 failed 出边**（对照 legacy 链保留 `"exhausted": STAGE_FAILED`） |
| 7 | 双链隔离：蓝图改动不回归旧 `technical_plan` 链 | ✅ WIRED | `register_process_type` 计数恒为 3；`_TECHNICAL_PLAN_STAGES` 全里程碑零改动；`test_two_chains_do_not_pollute_each_other` / `test_technical_plan_stage_graph_is_frozen` 等 5 条隔离断言绿 |

**一处共享面改动需在合并时复核（非缺口）：** `process_runtime/engine.py` 的 artifact 指针条件透传修复（113-06）落在**两条链共享**的代码上。它对旧链是严格改进（修复前旧链指针同样每步被抹成 NULL），且有正反双断言；但与 v0.19.0 合并后需复跑旧链回归。`STATE.md:262` 已登记。

### 4.1 边界接缝：蓝图链 → 其消费方（3 道 BROKEN，均在默认开关下潜伏）

相位 111–116 之间的接缝没有一道断——**断的全在蓝图链与外部消费方的交界处**。三道都由 per-entry 开关挡着不影响当前生产流量，但都会在同步点 2 翻默认时第一时间显形。

| # | 边界接缝 | 判定 | 本轮回源实证 |
|---|---|---|---|
| G1 | 蓝图会话 → **workflow 入口**的挂起判据 | ⛔ BROKEN | `_maybe_suspend`（`plan_research.py:468-471`）用旧链模型 `ClarificationService().ahas_pending` 判待答。蓝图链**从不写 `Clarification` 行**——全仓 `Clarification.objects.create` 只有 `clarification_service.py:110` 一处，蓝图侧写的是 `BlueprintThread`。⇒ 停在 `waiting_clarification` 的蓝图会话 `pending=None` → 不发飞书卡 → 不建 `WorkflowEventSubscription` → `suspend=None` → 落 `_map_terminal` 非 DONE 分支 → `status="failed"` / `error_code="plan_session_failed"` / `next_handle="error"`（`:665-675`）。**每一次规格门提问与每一次确认硬门都会把工作流判死**。这比已登记的「DONE→completed 一行」严重一档：不只是终态映射错，是**所有中间挂起都映射成失败** |
| G3 | 蓝图会话 → **MCP 入口**主载荷 | ⛔ BROKEN | `orchestration_delegate.py:267-276` 的 `DONE → completed` 无蓝图分支、`_load_canonical` 用 v0 渲染器；`technical_plan_service.py:440` 的 `_map_execution_plan_to_repository_tasks` 读 `content["execution_plan"]`，blueprint/v1 无此顶层键（required 键表见 `blueprint_schema.py:123-134`）⇒ 返回 `[]`。**响应十二键结构合法、语义为空**——静默降级。GATE-01 的异步澄清那一半（`blueprint_artifact_id` / `pending_clarifications` 装饰 + 两个新工具）是真接通的，空的是主载荷 |
| G4 | 蓝图会话 → **feature_list 入口** | ⛔ BROKEN | `initiatives/services/feature_solution_service.py:485-512`：FAILED→failed、DONE→completed+旧形态 attach、其余一律 `STATUS_RESEARCHING`，待答问题取自 `ClarificationQuestion`（旧链子题模型）。⇒ 阻塞在 `BlueprintThread` 上的蓝图会话在该面**永久显示 researching、问题列表为空、无解阻手段** |

**对照组（说明这不是「蓝图链没做完」而是「三个适配器没跟上」）：** chat 入口的 `_map_terminal_blueprint`（`plan_research_tools.py:474-531`）按 `blueprint_status` 分桶而非按会话状态，把 `pending_review` 正确计成功。同一条链、同一批状态，做对了。

**判债不判需求缺口的理由：** 三者都在 GATE-01 的 PARTIAL 覆盖范围内（该需求已明示入口收编待同步点 2 完成），且默认开关下零生产影响。但它们**扩大了 GATE-01 的剩余工作量**，见 §2.3 的修正表。

---

## 5. 并行边界纪律（§13.2）—— 全里程碑守住

不逐相位采信，直接对里程碑基线 `b974a0c2..HEAD` 做全量 `git diff`：

| 冻结项 | 结果 |
|---|---|
| `codegraph/services/repo_router_v2.py` | ✅ 零改动 |
| 六个 legacy `technical_plan` process 文件（`decompose_segments` / `research_adapter` / `architect_merge_adapter` / `merged_plan` / `clarify_adapter` / `render`） | ✅ 零改动 |
| 四个 0.19 归属的前端文件（`TechPlanCard.vue` / `RoutingDecisionPanel.vue` / `NodeDataTab.vue` / `ArtifactTimeline.vue`） | ✅ 零改动（四文件均存在于 `git ls-files`，非 glob 空匹配） |
| `ConvergenceSessionEvent` 契约只消费不修改 | ✅ `event_taxonomy.py` 全里程碑 **+108 / −0**，纯追加；`BLUEPRINT_EVENTS` 独立 frozenset 不进 `ALL_EVENTS` |

---

## 6. 对抗性记账抽查

### 6.1 Traceability 逐行回源（发现 1 条乐观转 Complete + 1 条口径偏乐观）

116-07 收口时把 26 行 Pending 转 Complete。本轮抽查其引用的相位分数与代码事实，**全部属实**：111 = 24/24、113 = 54/54、115 = 107/107、116 = 121/121；112 确为 `gaps_found` 16/17 且 GAP-1 的核心（`excluded` 只写不读）实测已闭（`_excluded_repository_ids` 是唯一读取方，两条候选来源同时剔除）。

抽查 10 条需求回到源码（见 §2.2 证据列），其中 9 条对得上。**两处对不上：**

1. **CLAR-03 是一条真正的乐观转 Complete**（§2.4）。它不是分数造假，也不是引用错相位——后端确实做完了，114 的验证也确实为真。问题在于这条需求的**首句是前端能力**，而记账在后端完成时就把整条转了 Complete，随后 115 顺延、116 未接手，**没有任何一处把「首句未做」写下来**。这是本里程碑唯一一条「需求文本承诺的用户能力被记成已交付、实则产品面不可达」。
2. **GATE-01 的「已交付」半句口径偏乐观**（§2.3 修正表）。「四个入口各自的蓝图可执行路径」只在「能建会话」意义上成立；三个入口不能正确观测会话。措辞本身没写假话（路径确实存在），但读者会据此低估剩余工作量。

**第一轮审计漏掉这两条的原因，值得记进 RETROSPECTIVE：** 第一轮的抽样方法是「拿 Traceability 的每一行去源码找对应实现」——这个方法能抓住「说做了其实没写代码」，抓不住「**代码写了但没接到用户面**」。后者要求反向验证：从需求承诺的**用户能力**出发问「用户在产品里点哪里能做到这件事」。CLAR-03 的后端 grep 全绿，正是这种正向验证的盲区。

### 6.2 三处文档记账不一致（不影响判定，应在归档前修）

| # | 位置 | 现状 | 应为 |
|---|---|---|---|
| 1 | `ROADMAP.md:31` 里程碑标题 / `:474` Progress 导语 | 仍写 `— PLANNING` / 「待 `$gsd-plan-phase 111`」 | 六相位全部完成 |
| 2 | `ROADMAP.md:481` / `:483` Progress 表 | 114 = 「4/5 🟡 In Progress」、116 = 「0/TBD Not started」 | 114 = 5/5 passed 83/83、116 = 7/7 passed 121/121（同文件 `:40` / `:42` 的清单行已是 `[x] completed`，两处自相矛盾） |
| 3 | `ROADMAP.md:89-93` / `:112-117` 的 Plans 复选框 | 112 五条、113 六条全为 `[ ]` | 应为 `[x]`（111 / 114 / 115 / 116 的均已勾） |

另两处**次要**不一致：`112-VERIFICATION.md` frontmatter 仍是 `status: gaps_found`（正文已追加 Gap Closure 判 FIXED，且 116 独立复验为真）；`113-REVIEW.md` frontmatter 仍是 `status: findings`（Fix Log 结论为 14 fixed / 1 skipped）。两者都是**保守方向**的滞后，不构成风险，但会让自动化审计低估。

### 6.3 三条深度观察（非缺口，属质量口径的诚实边界）

1. **六段骨架是 schema 强制的，段内深度不是。** `interaction_flows[].steps` 只 required `seq/actor/action`，`api_ref` / `data_in` / `data_out` 可选；`api_contracts` 只 required `id/name/kind/direction`，`request_example` / `data_source` 可选。SCHEMA-04/05 的「完整业务编排叙事」与「请求响应示例」因此**依赖 LLM 起草质量 + 引用覆盖率门 + AI 审查规则**，而非 schema 拒绝。AI 审查的七类规则里没有「step 缺 api_ref」这一类。需求文本未要求 schema 强制，故判 satisfied——但这是最可能随提示词漂移而静默退化的一面。
2. **GATE-02 的 golden set 是确定性层的回归门，不是 LLM 质量门。** 六道门槛覆盖 schema 有效性、派生有效性、确定性、引用覆盖率、目标仓命中率、必备功能点；且 case 是**静态 JSON fixture**，不跑真实流水线 ⇒ 结构性无法检出「LLM 把交互流程写浅了」这类退化。`REQUIREMENTS.md` 的 Future Requirements 已把「golden set 弱标签扩样」列为本里程碑不做，判定与之一致。结合观察 1，**这是本里程碑最大的一处质量盲区**。
3. **`test_skills_snapshot_guard` 的红不只是「一条环境红」。** 该文件三条断言里，`test_skill_files_discovered` 正是防空跑假绿的自检（本 worktree `skills/` 为空 ⇒ 它红）；而 `test_skill_tool_references_subset_of_snapshot` 迭代的正是这个空集合 ⇒ **在本 worktree 里空跑为绿**。本里程碑新增了两个 MCP 工具与一个 `answer` 动词前缀（该文件本里程碑的唯一改动就是扩前缀表），**其「skills 文档 ⊆ snapshot」这一面从未在此实跑过**。`test_tool_token_prefixes_cover_all_snapshot_keys` 不依赖该集合、已实跑通过。⇒ 归档/合并后**必须在主检出复跑该文件**（已列入 human_verification）。

---

## 7. 顺延项与阻塞判定

`STATE.md` Pending Todos 表头自述「进里程碑审计时真正待办的是六条」。本轮逐条独立复核，并加判「是否阻塞归档」：

| # | 顺延项 | 复核结论 | 阻塞归档？ |
|---|---|---|---|
| 1 | 同步点 2 的四件事同批做（翻默认 + `_map_terminal` 改 HITL + 三处触点升级 + 旧 process 退役） | 实测为真的「未做」；**硬依赖外部里程碑，且提前做违反 RELY-01**。⚠️ **本轮修正范围**：第 2 件事不是「`_map_terminal` 一行」，而是 **workflow / feature_list / MCP 三个入口的出口映射整体重做**（§4.1 的 G1/G3/G4）。且顺序上，翻 workflow 或 feature_list 开关后**第一次澄清**就会撞 G1/G4，早于「未经人审的蓝图进 ai_coding」这个已知风险 | **否**（本里程碑内不可能闭合，强行闭合是制造缺陷）；⭐ 但 STATE.md 该条措辞应扩到实际范围，否则同步点 2 的排期基于偏小的估计 |
| 1b | **CLAR-03 的 block 编辑面**（本轮新增，非 STATE.md 原有条目） | **不是顺延项，是缺口**：后端就绪、无语义障碍、可在本里程碑内闭合，但被记成 Complete（§2.4） | ~~**是**~~ ⇒ **✅ 已闭（§13）**，不再阻塞 |
| 2 | `redact_secrets_in_text` 不覆盖 DB 连接串 | 平台级、早于本纪律、非本里程碑引入；蓝图链九模块已收口并有 AST 守卫 | 否（独立清理相位） |
| 3 | 115-MN-03 的四语义契约整体改版 | 116 让暴露面 11 → 15 个端点，**已知代价且 import 复用同源实现**（非复制第四份）；暴露面仅限 `meta.project_id` 非 UUID 的小批 | 否（改一行状态码是错解；正解是契约改版） |
| 4 | apscheduler 周期提醒接 `blueprint_notify` + 卡片交互回调 | 首次送达已在 116-06 落地；**残留是周期提醒那条一行调用**。CLAR-04 的四条可判定内核（显式 pending / 按周期 / 不重复轰炸 / 随时作答恢复 / 不自动作答不判失败）已全部落地 | 否（但这是六条里**最小、最该顺手做掉**的一条：一行调用 + 一条用例） |
| 5 | FLOW-02 的「替代建议」补结构化字段 | 需求文本只要求「+ 替代建议」，自由文本已承载且前端原样呈现；结构化等机器消费方出现 | 否 |
| 6 | chat 回灌挂载点无结构性保证 | 三个挂载点、多挂幂等安全、有守门对照；彻底形态要动 §13.2 冻结的 `ConvergenceSessionEvent` | 否（与「`areopen_stage` 未发事件」同批，等同步点 2） |

**判定：六条中无一条阻塞归档。** 其中第 1 条**结构上不可能**在本里程碑内闭合；第 4 条是唯一「成本极低、收益直接（用户能收到提醒）」的一条，建议在归档前或紧随其后单独做掉。

---

## 8. 门禁实跑（本轮亲自执行，不采信各相位记录）

| 门 | 命令 | 实测 | 判定 |
|---|---|---|---|
| 后端全量 | `cd server && uv run pytest tests/ -q` | **1 failed, 8980 passed, 63 skipped, 26 deselected, 1 xfailed**（522.05s） | ✅ 与预期逐字相符 |
| 唯一失败归因 | — | `tests/mcp_tools/test_skills_snapshot_guard.py::test_skill_files_discovered`；本 worktree `skills/` 实测为空目录（`total 0`），主检出有 8 项内容；该文件本里程碑唯一改动是给正则加 `answer` 前缀 ⇒ **环境产物，非里程碑缺陷**（但见 §6.3 观察 3） | ✅ |
| migrations | `manage.py makemigrations --check --dry-run` | `No changes detected`，退出码 0 | ✅ |
| 前端单测 | `pnpm exec vitest run` | **215 files passed / 1 skipped；1744 passed / 1 skipped**（13.26s） | ✅ |
| type-check | `pnpm type-check` | 退出码 **0**（`vue-tsc --noEmit` 无输出） | ✅ |
| 工作区洁净 | `git status --porcelain` | **空输出** | ✅ |
| 冻结面 | `git diff b974a0c2..HEAD -- <11 项>` | **空输出** | ✅ |

> 审计核算改动面时的两个已知环境噪声（本轮未触发，工作区干净）：`pnpm build` 会重写 `web/src/components.d.ts`、`pnpm` 会回填 `web/pnpm-workspace.yaml` 的 catalog。**请以 `git diff` 而非工作区状态判「新增依赖」。**

---

## 9. Nyquist 覆盖（overall: missing）

`workflow.nyquist_validation = true`（`config.json`），但六个相位**全部无 `*-VALIDATION.md`**（`.planning/phases/` 下唯一一份属 v0.19.0 的 `105-golden-set`）。

| Phase | VALIDATION.md | Compliant | 建议动作 |
|---|---|---|---|
| 111 蓝图底座 | missing | — | `/gsd-validate-phase 111` |
| 112 规格门与双面路由调研 | missing | — | `/gsd-validate-phase 112` |
| 113 分仓方案与融合 + Context Bus | missing | — | `/gsd-validate-phase 113` |
| 114 审查与澄清收敛 | missing | — | `/gsd-validate-phase 114` |
| 115 前端查看器与知识库 | missing | — | `/gsd-validate-phase 115` |
| 116 入口收编与导出 | missing | — | `/gsd-validate-phase 116` |

**判定：不阻塞归档，但应登记。** 六个相位的 VERIFICATION 深度显著高于常规（合计 405 条 must-have 逐条 goal-backward 判定，且各相位均对「断言能否证伪」做了专项复核），实测覆盖不弱；缺的是 Nyquist 采样口径本身的独立审计。另：`security_enforcement = true` 但六相位也均无 `*-SECURITY.md`——本里程碑的安全面（三道会话校验、四道范围闸、fail-closed 排除判定、脱敏 AST 守卫）在各相位 REVIEW 中被对抗性审查覆盖，但未走 `/gsd-secure-phase` 的独立流程。

---

## 10. 代码审查累计（6 相位 + 1 次 UI 审计）

| Phase | Findings | 处置 | 冻结面 |
|---|---|---|---|
| 111 | 0 C / 3 MJ / 13 MN | 3 MJ + 11 MN fixed；MN-06（需新 migration）/ MN-12（权限口径）skipped | clean |
| 112 | 2 C / 7 MJ / 7 MN | 15 fixed / 1 skipped（MN-06 零收益 churn） | clean |
| 113 | 1 C / 4 MJ / 10 MN | 14 fixed / 1 skipped（MN-09 两条建议修法一条撞冻结面、一条是安全倒退） | clean（11 项） |
| 114 | 1 C / 4 MJ / 6 MN | 10 fixed / 1 skipped（MN-05 后由 115-04 闭） | clean（13 项） |
| 115 | 0 C / 4 MJ / 3 MN | 6 fixed / 1 skipped（MN-03，见 §7 第 3 条） | clean |
| 115 UI 审计 | 2 H / 6 M / 11 L | 13 fixed / 4 skipped（2 条是 UI-SPEC 自身修订项） | 23/24 |
| 116 | 0 C / 3 MJ / 6 MN | **9 fixed / 0 skipped** | clean |

**值得记录的模式：** 每个相位的评审都抓到了 VERIFICATION 结构上照不到的一类缺陷——**「一条链上每个文件各自都对，但两端没接上」**（112 的 `excluded` 只写不读、113 的三条跨模块断链、114 的「指标接了不存在的字段」、116 的 assumptions 三档无写入方与 `error_detail` 无消费方）。这类缺陷在本里程碑出现了 **7 次**，全部由相位级 code review 而非 verification 逮到。⇒ 该里程碑的质量得以成立，主要靠的是「verification + 独立对抗审查」双层，任何一层单独都不够。这条经验应进 RETROSPECTIVE。

---

## 11. 结论

> ⭐ **本节是闭环前的原貌，逐字保留。** 现行判定为 **`tech_debt`**，见 §13。

**status: gaps_found（闭环前）。**

- **需求：33/35 完全满足、1 条 PARTIAL（GATE-01，不可在本里程碑内闭合）、1 条 GAP（CLAR-03，可闭合）、0 条 orphaned。**
- **链路：六段蓝图从需求进入到人审确认、导出与图谱物化，11 步全部接通。** 三处缝隙：① 整条链路默认不通电（四个开关默认旧链，翻默认会违反 RELY-01）；② `confirmed` 之后无下游驱动（四态无生产 writer、`derive_technical_plan_document` 无生产消费方）；③ 三个入口能建蓝图会话却不能正确观测它 ⇒ **核心价值的「人类可读」半条已兑现，「AI 可依此完备编码」半条在编排层就绪、分发层待同步点 2 接线。**
- **集成：七道跨相位接缝全部 WIRED**（两轮独立走查一致），双链隔离由 5 条专项断言背书；**三道边界接缝 BROKEN**（G1/G3/G4，默认开关下潜伏）；一处共享面改动（`engine.py`）已登记待合并复核。
- **边界：§13.2 全里程碑守住**，11 项冻结面全量 `git diff` 为空、事件面纯追加 +108/−0。
- **门禁：**后端 8980 passed / 1 failed（环境）、前端 1744 passed / 1 skipped、type-check 0、`makemigrations --check` 干净、工作区洁净。

**判 `gaps_found` 而非 `tech_debt` 的唯一决定性依据是 CLAR-03。** 判据是可闭合性，不是严重性：本工作流对 `gaps_found` 的路由是「插入 closure phase」，而 CLAR-03 的后端已就绪、无语义障碍、不依赖任何外部里程碑——这条路由**可执行**。GATE-01 则相反（硬依赖同步点 2，提前闭合 = 主动制造 RELY-01 违规），故仍判 partial 而非 gap。⭐ 需要说清的是：CLAR-03 的严重性并不高（后端可用、有替代路径「驳回后重出」），**它被判成 blocker 是因为记账把一个不可达的用户能力记成了已交付**——归档会把这个错误声明冻进项目记录，而这正是里程碑审计要拦的东西。

**同时不判 `passed` 的其余理由：** GATE-01 PARTIAL；六条真实顺延项（其中「confirmed 之后无下游」为本次新定位）；六相位全部缺 Nyquist 与 SECURITY 产物；§6.2 的三处 ROADMAP 记账不一致尚未订正。

### 归档前必做（阻塞）—— ✅ **已完成，见 §13**

**closure phase：CLAR-03 二选一**——(a) 补 block 编辑面（删 115 守卫 + 加 `blueprints.ts` API 函数 + 编辑态 UI，后端零改动）；或 (b) 按 SC-3/SC-4 的既有做法**正式收窄 CLAR-03 措辞**并在 REQUIREMENTS / ROADMAP / STATE 三处同步记录理由，同时把 115 守卫那段自相矛盾的注释统一到单一立场。⭐ 选 (b) 完全正当——115 的设计论证有实质内容；**不正当的是让需求文本与实现意图的分歧留在无人记录的状态**。

### 归档前建议（不阻塞，按性价比排序）

1. **把 `STATE.md` Pending Todos 第 1 条的范围扩到实测规模**（§2.3 修正表）——当前措辞「`_map_terminal` 一行」实际是三个入口的出口映射重做，会让同步点 2 的排期偏小。
2. **订正 §6.2 的三处 ROADMAP 记账**（标题 PLANNING、Progress 表两行、112/113 的 Plans 复选框）——纯文档，5 分钟。
3. **在主检出复跑 `test_skills_snapshot_guard.py`**（§6.3 观察 3）——本里程碑新增的两个 MCP 工具从未过该守卫的文档面。
4. **顺手做掉 apscheduler 周期提醒的一行接线**（§7 第 4 条）——顺延项里唯一「成本极低、用户可感知」的一条。
5. **补 115/116 十四份 SUMMARY 的 `requirements:` frontmatter**（§2.1）——否则下次自动化审计会在 6 条需求上重报 partial。
6. 把三条模式写进 `RETROSPECTIVE.md`：§10 的「跨模块断链」、§6.3 的质量盲区（段内深度非 schema 强制 + golden set 不覆盖 LLM 质量），以及 **§6.1 新增的那条方法论教训——正向抽样（需求→找代码）抓不住「代码写了但没接到用户面」，需求验证必须反向问「用户在产品里点哪里能做到这件事」**。

---

## 12. 归档前收尾（本次执行）

本节记录「归档前建议」里已落地的条目。⛔ **不含 §11「归档前必做」的 CLAR-03 closure phase**——那条仍未动。

| # | 建议 | 处置 | commit | 证据 |
|---|---|---|---|---|
| 0 | 在主检出复跑 `test_skills_snapshot_guard.py`（建议 3） | ✅ 已做 | — （无代码改动） | 在本 worktree **初始化 `skills/` 与 `mcp/` 子模块**后就地复跑：**3 passed**。`SKILL_FILES` 非空 ⇒ `test_skill_tool_references_subset_of_snapshot` **不再是空跑**，本里程碑新增的两个 MCP 工具与 `answer` 动词前缀均已过该守卫的文档面。⇒ §6.3 观察 3 的「文档面从未实跑过」**已消解**，`human_verification` 该条转 ✅ |
| 1 | 订正三处 ROADMAP 记账（建议 2） | ✅ 已做 | `96127524` | 里程碑标题 `PLANNING` → `COMPLETE`；Progress 导语「待 `$gsd-plan-phase 111`」→ 六相位全完成 + 审计结论；Progress 表 114 `4/5 In Progress` → `5/5 ✅ passed 83/83`、116 `0/TBD Not started` → `7/7 ✅ passed 121/121`；112 五条 + 113 六条 Plans 复选框全部补勾。另补顶部 Milestones 索引缺失的 v0.20.0 条目（全文扫描后 v0.20.0 段内 `PLANNING` / `Not started` / `In Progress` / 未勾复选框**零残留**） |
| 2 | 补 14 份 SUMMARY 的 `requirements:`（建议 5） | ✅ 已做 | `c2fc4036` | 逐份取自**同号 PLAN 的 frontmatter**（不猜不推断），插入位置按 111–114 约定放在 `subsystem:` 之后。补齐后 34 份 SUMMARY 的 `requirements` 并集**恰为 35 条需求全集**（补齐前 115/116 的 14 份全缺）⇒ §2.1 警告的「下次自动化审计在 6 条需求上重报 partial」已消除 |
| 3 | apscheduler 周期提醒接送达收口（建议 4） | ✅ 已做（**实际不止一行**） | `162eecd5` | **先核实后动手**：`aremind_clarification_threads`（`blueprint_review_action.py`）实测只写 `last_reminded_at` 锚点 + 记 `blueprint_clarification_reminded` 事件，**全函数无任何投递调用**；`anotify_blueprint_clarification` 全仓生产调用方实测恰 1 处（`blueprint_spec_gate:345/347`，即 116-06 的「首次送达」）⇒ 接线确实缺失，非误报。接法：**锚点写回成功之后**按 artifact 分组推一张卡（顺序不可换——锚点是防轰炸的唯一依据；一蓝图 N 条到期只推一张卡，N 张卡本身就是轰炸）；题面优先取 `BlueprintThread.options`（规格门把整份 questions 原样存了进去）、否则回落首条消息正文。整段 best-effort，返回值仍是恒定四键。新增 4 条用例，前 3 条经**实跑变异**（回退接线调用）转红后恢复全绿 |
| 4 | GATE-01 复选框与 PARTIAL 对齐（§2.3 唯一记账不一致） | ✅ 已做 | `5fc8e9e6` | `REQUIREMENTS.md:75` 的 `- [x]` → `- [ ]`，行内 PARTIAL 标注与 `:139` Traceability 一字未改。⭐ 采用的是**本文件既有的 partial 约定**而非审计建议的 `[~]`：VIEW-04 在 115 阶段判 PARTIAL 时的写法就是「`- [ ]` + 行内 `（**PARTIAL @ Phase 115…**）`」（`git show 8e25461c~60`），⛔ 不引入本文件从未用过的第三态 |

**⭐ 本次收尾的最大产出不是这五条，而是子模块初始化撞出的一条新缺口**：`test_mcp_package_alignment` 此前因 `mcp/` 未 checkout 而 `pytest.skip`，**全里程碑从未实跑**；跑通后立刻报出 mcp npm 包缺四个工具（`read_blueprint_context` / `report_blueprint_context` / `get_technical_blueprint` / `answer_blueprint_clarification`，全部本里程碑新增）。⇒ 与 §6.3 观察 3 完全同构：**「守卫在本 worktree 空跑」这件事本身，掩盖的不止一条**。已登记进 `tech_debt.116-entry`；⛔ 本次未修（要动 `mcp/` 子模块并发版，跨仓改动、且本 worktree 明确不动子模块指针）。

**门禁实跑（本次收尾之后）：**

| 门 | 命令 | 实测 |
|---|---|---|
| 后端全量 | `uv run pytest tests/ -q` | **1 failed, 8986 passed, 61 skipped, 26 deselected, 1 xfailed**（511s）。⭐ `test_skills_snapshot_guard` **已转绿**（子模块就位）；唯一失败是上面那条新暴露的 `test_mcp_package_alignment`，**与本次改动无关**（`git diff 8e25461c HEAD -- server/mcp_tools mcp server/tests/mcp_tools` 为**空**） |
| migrations | `manage.py makemigrations --check --dry-run` | `No changes detected`，退出码 0（收尾零 migration） |
| ruff | 触及三文件 `check` + `format --check` | `All checks passed` / `3 files already formatted` |
| 前端 | — | **未跑**：本次收尾 `web/` 零改动 |

**status 未变（仍 `gaps_found`）**：本次关掉的是记账与一条顺延项，§11「归档前必做」的 CLAR-03 closure phase 未动，GATE-01 的 PARTIAL 与同步点 2 的三道边界接缝（G1/G3/G4）也全部原样成立，且本次还**新增**一条 tech_debt（mcp 包漂移）。

---

## 13. CLAR-03 闭环（2026-08-02）

**结论：§11「归档前必做」的唯一一条已闭，`status` 由 `gaps_found` 转 `tech_debt`。**

### 13.1 取哪条路径，以及为什么

取 §11 的 **路径 (a) 补面**，⛔ 不是路径 (b) 收窄措辞。三条理由：

1. **需求首句承诺的是一条具体的用户能力**（「人类可直接编辑蓝图内容（block 级）」），后端完全就绪、无任何语义障碍 ⇒ 兑现它比事后重定义需求更诚实。REQUIREMENTS 的 CLAR-03 措辞**一字未改**。
2. ⭐ **115 那条设计论证的前提已经失效**。它说「在只读评审面开编辑入口 = 绕过『要改先驳回』的纪律」——但 **114-MJ-04 给 `edit-blocks` 加了 `is_blueprint_editable` 状态闸**（`blueprint_lifecycle_service.py:92-109`），已 `confirmed` / `implementing` / `archived` 的蓝图一律 400 且 DB 一字未动。「要改先驳回」如今由**后端结构性兜住**，不再依赖「前端不给入口」这条软纪律。路径 (b) 会把一条**前提已失效**的主张写成需求本身的退让。
3. 路径 (a) 的成本实测就是 §11 估的那三件（删守卫 + api 函数 + 编辑态 UI），**后端零改动**。

### 13.2 交付内容（三条 commit）

| # | commit | 内容 |
|---|--------|------|
| 1 | `25228ead` | **api 层**（纯追加）：`editBlueprintBlocks` + `BlueprintBlockOp` / `BlueprintBlockEditRejection` / `BlueprintBlockEditResponse` 三个类型，逐字对齐后端 `apply_block_ops` 的 op 契约与端点的恒定响应键；docstring 里那句「⛔ 不封装块级批量改写端点」改写成兑现说明 |
| 2 | `83be08fc` | **编辑面**：新建 `blockEditOps.ts`（纯判据）与 `BlueprintBlockEditDialog.vue`（受控 Dialog，范式同 `BlueprintRejectDialog`）；选区浮层追加「编辑此块」入口；查看器接线三档分档 + i18n 十三键 |
| 3 | `7760ae2d` | **守卫反转 + 18 例测试**：原 §20 断言 10 换靶子为「端点路径字面量只许出现在 api 层」，另补一条**正向**断言 10b |
| 4 | `4b32c642` | **记账**：REQUIREMENTS 的 CLAR-03（措辞未改，补闭环说明）+ 本节 + frontmatter 转 `tech_debt` + STATE 的两条待办 |
| 5 | `24df6187` | **还原 `components.d.ts`**：commit 2 误把 `pnpm build` 重写过的版本一并提交，build 的组件扫描只覆盖构建入口图可达的部分 ⇒ 28 条无关登记被静默删掉。改回「基线 + 手工追加一行」（⚠️ 这正是 §8 那条「以 `git diff` 而非工作区状态判改动面」的环境噪声，本次亲自踩了一遍） |

### 13.3 逐条对上审计提出的要求

| 审计要求 | 落地 | 证据 |
|---|---|---|
| 删掉主动阻止入口的守卫，且**注释统一到单一立场** | 守卫**不删而是换靶子**（组件里不许手写端点 URL），矛盾注释整段重写为「block 编辑面已交付」+ 前提失效的说明 | `blueprint-source-guard.spec.ts` §20 断言 10 |
| ⭐ 防「守卫全绿而能力不可达」再来一次 | 新增**正向**断言 10b：编辑弹窗组件 / 经 api 层的调用点 / 选区浮层入口三个支点缺一即红。⛔ 只留「不许手写 URL」那条的话，入口整个消失时它同样平凡通过 —— 那正是 CLAR-03 栽的坑 | 同上，断言 10b |
| 入口只在可编辑状态下可达，`confirmed` 不给 | `canEditBlueprintBlock` 收口三条 AND（非历史版本 ∧ 非 diff ∧ `isBlueprintEditable` ∧ 该块有文本落点）；白名单成员与后端 `EDITABLE_BLUEPRINT_STATUSES` 逐字对齐；为假时按钮**不存在于 DOM**（⛔ 不是 disabled），115 已有的 `blueprint-readonly-notice` 只读提示条照常渲染 | 用例 1a–1e |
| 冲突态是**可操作状态**而非静默 no-op | 400 且 `rejected` 含 `block_not_found` ⇒ 独立冲突面板 + 「刷新后重试」出口；输入框与保存按钮**都不渲染**（⛔ 不让用户对着过时基线继续点保存） | 用例 3a–3c |
| 文案走 i18n、令牌色、destructive 二次确认 | 十三个 `knowledge.blueprints.edit.*` 键 + 一个 selection 键；`border-warning/40 bg-warning/10` 令牌类；删块经 `useConfirmDialog({variant:'destructive'})` | 用例 5a–5c |
| 后端零改动 | `git diff` 对 `server/` 为空 | 见 §13.5 |

### 13.4 ⛔ 本次**没有**闭的（转技术债，⛔ 不是被认为已解决）

- **G1 / G3 / G4 三道边界接缝**：§4.1 的三张判定表**一字未改**，三者仍 BROKEN。转 `tech_debt.116-entry` 的判据是**可闭合性**而非严重性 —— ① 均在 GATE-01 的 PARTIAL 覆盖范围内；② 四个开关默认全 `technical_plan` ⇒ 默认开关下**零生产影响**；③ 修法硬阻塞同步点 2 且须与翻默认同批 ⇒ 本里程碑内**结构上不可能闭合**，强行闭合是主动制造 RELY-01 违规。⚠️ 排期顺序提醒已写进 `STATE.md` Pending Todo 第 1 条：真翻 workflow 或 feature_list 开关后**第一次澄清**就撞 G1/G4，早于「未经人审的蓝图进 ai_coding」这个已知风险。
- **GATE-01 仍 PARTIAL**、§3.3 的「confirmed 之后无下游」仍未接线、六相位 Nyquist/SECURITY 产物仍缺 —— §11「同时不判 passed 的其余理由」原样成立，它们本来就不是归档阻塞项。
- **mcp npm 包漂移四个工具**（`test_mcp_package_alignment`）：要改 `mcp` 子模块并发版，跨仓改动 ⛔ 不在本里程碑范围内。已在 `tech_debt.116-entry` 与 `STATE.md` 各记一条。

### 13.5 两处刻意收窄（登记，非缺口）

1. **`insert`（新增块）后端支持而 UI 不给入口**：需求文本说的是「编辑蓝图**内容**」，新增块属「扩写蓝图」；且 `insert` 要求新块自带非空 `block_id` ⇒ 前端得发明 id 生成与落位语义，而 `block_id` 是 anchor / citation / block 级 diff 三处的对齐键。⇒ 顺延。
2. **`table` 块不给编辑入口**：其文本坐标系是「单元格扁平后 `\n` 连接」，单框文本编辑压平行列后无法还原成 `rows`。与 115 对 table **强制整块批注**的处置同源（`BlueprintBlock.forcedWholeBlock`）。⇒ 顺延。

### 13.6 门禁实跑（本次闭环之后）

| 门 | 命令 | 实测 |
|---|---|---|
| 前端单测 | `pnpm exec vitest run` | **216 files passed / 1 skipped；1763 passed / 1 skipped** —— 对审计基线 1744/1 **+19**（新增 18 例 + 守卫新增 1 条），**零回归** |
| type-check | `pnpm type-check` | 退出码 **0** |
| lint | `pnpm lint` | **111 problems**，与审计基线**逐字相同** ⇒ 触及文件**零新增**（中途出现的 3 条 `perfectionist/sort-imports` 已 `--fix`） |
| build | `pnpm build` | ✓ built。⛔ `web/pnpm-workspace.yaml` 无 catalog 回填；`src/components.d.ts` 只有**一行**新组件登记（追加形态） |
| 后端全量 | `uv run pytest tests/ -q` | **1 failed / 8986 passed**，与 §12 收尾读数逐字相同；唯一失败是 `test_mcp_package_alignment`（跨仓技术债），见 §13.8 |
| 变异证据 | 见 §13.7 | 两条实跑变异均转红 |

### 13.7 可证伪性与后端基线

**变异证据（实跑，非声称）：**

| 变异 | 结果 |
|---|---|
| 删掉 `canEditBlueprintBlock` 里的 `isBlueprintEditable` 判断 | 用例 1a **转红**（`confirmed` 从 false 变 true）—— 1 failed / 17 passed |
| 删掉选区浮层按钮的 `v-if="canEdit"` | 用例 1c / 1e **转红** —— 2 failed / 16 passed |

⇒ 可达性闸的两条断言均**非恒真**。两次变异后均已还原并复跑全绿。

**后端基线**：见 §13.8 的实跑读数。判据是 `git diff` 对 `server/` 为空 ⇒ 本次闭环对后端零影响。

### 13.8 后端全量读数

`cd server && uv run pytest tests/ -q` → **1 failed, 8986 passed, 61 skipped, 26 deselected, 1 xfailed**（515.53s）。

⭐ **与 §12 收尾后的读数逐字相同**（同为 8986 passed / 1 failed），唯一失败仍是
`tests/mcp_tools/test_mcp_package_alignment.py::test_mcp_package_tools_match_server_snapshot`
—— mcp npm 包缺本里程碑新增的四个工具，属 §12 ⭐ 段登记的跨仓技术债。

**归因判据（不靠自述）**：`git diff HEAD -- server/ mcp/` **空输出** ⇒ 本次闭环对后端与子模块
**零改动**，该失败与它无关。子模块指针**未动**（本 worktree 明确不动它）。

---

_Audited: 2026-08-01 · 审计基线 `b974a0c2..HEAD`（371 files, +124177/−1116）_
_Auditor: gsd-audit-milestone（对抗性立场：默认文档乐观，逐条回源码反证）_
_两轮核查：第一轮四源交叉 + Traceability 逐行回源；第二轮独立集成代理走查跨相位与边界接缝。第二轮推翻第一轮两处结论（§6.1、§2.3），全部经审计员亲自回源码复证后并入。_
