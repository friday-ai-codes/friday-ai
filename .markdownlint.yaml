# markdownlint 配置
# MD060/table-column-style: CJK 字符（中文）的显示宽度约为 ASCII 字符的两倍，
# 但 linter 按字符数计算，导致包含中文的表格列永远无法真正对齐。
# 禁用此规则以允许两种表格风格共存。
MD060: false
# MD013/line-length: 技术文档中列表项、表格行、代码路径和中文段落
# 的自然长度通常超过 80 字符，强制换行会严重破坏可读性。
MD013: false
