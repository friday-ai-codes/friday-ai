"""Jinja2 sandbox engine — module-level lazy singleton (Task 3 stub)。
本文件在 Task 3 创建最小 stub 以让 `apps.py:ready` 的 lazy import 不抛 ImportError。
Task 5 会覆盖为最终形态（完整 ImmutableSandboxedEnvironment 构造 + pre-warm）。
"""
from __future__ import annotations
def get_jinja_env -> object:
 """Task 3 stub — Task 5 替换为真实 ImmutableSandboxedEnvironment 单例。"""
 return None
