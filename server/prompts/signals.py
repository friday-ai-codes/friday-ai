"""Prompt 缓存失效 signal (Task 3 stub)。
本文件在 Task 3 创建最小 stub 以让 `apps.py:ready` 的 lazy import 不抛 ImportError。
Task 7 会覆盖为最终形态（@receiver(post_save, sender=Prompt) handler）。
"""
from __future__ import annotations
