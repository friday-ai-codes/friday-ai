"""Phase Wave — stub 测试文件。Plan 的 task 05-01 将把真实 assertion 填入。
Scope: Analytics 多维分组聚合契约。覆盖 验收项：
 GET /api/analytics/overview/?group_by=provider_type 返回
 {total:{cost, tokens}, groups:[{provider_type, cost, tokens}]} 结构。
Consumer: Plan Wave Task 05-01。
"""
from __future__ import annotations
import pytest
# TODO(Plan Task 05-01)：填入以下用例
# - test_group_by_provider_type_returns_aggregated_groups
# - test_group_by_model_returns_per_model_breakdown
# - test_total_matches_sum_of_groups
# - test_empty_range_returns_zero_total
# - test_group_by_invalid_key_returns_400
# - test_time_range_filter_applied_before_grouping
# - test_groups_ordered_by_cost_desc
def test_placeholder -> None:
 """Phase Wave stub — Plan fill real assertion."""
 pytest.skip("Phase Wave stub — Plan fill real assertion")
