"""Phase Wave — stub 测试文件。Plan 的 task 08-04 将把真实 assertion 填入。
Scope: v8.1 `/api/projects/<id>/claude-config/` 端点硬删 404 契约。覆盖 验收项：
 后端 ClaudeConfigView(Set) 代码删除后，URL resolver 返回 404 响应
 （确保 Phase 前端 UI cutover 后真正无端点残留）。
Consumer: Plan Wave Task 08-04。
"""
from __future__ import annotations
import pytest
# TODO(Plan Task 08-04)：填入以下用例
# - test_get_claude_config_returns_404
# - test_post_claude_config_returns_404
# - test_put_claude_config_returns_404
# - test_delete_claude_config_returns_404
# - test_url_pattern_removed_from_resolver
# - test_no_claude_config_view_symbol_in_module
def test_placeholder -> None:
 """Phase Wave stub — Plan fill real assertion."""
 pytest.skip("Phase Wave stub — Plan fill real assertion")
