"""Phase Wave — stub 测试文件。Plan 的 task 06-01 将把真实 assertion 填入。
Scope: 四层 ProviderConfig 解析链契约（单元测试）。覆盖 验收项：
 ProviderConfigService.aresolve_with_chain 返回四层解析链
 + winning source 标识（node > conversation > project > system）。
Consumer: Plan Wave Task 06-01。
"""
from __future__ import annotations
import pytest
# TODO(Plan Task 06-01)：填入以下用例
# - test_node_level_wins_over_lower_layers
# - test_conversation_level_wins_when_node_empty
# - test_project_level_wins_when_conversation_empty
# - test_system_level_fallback_when_all_empty
# - test_chain_contains_all_four_layers_always
# - test_inactive_credential_skipped_in_chain
# - test_deleted_credential_treated_as_missing
def test_placeholder -> None:
 """Phase Wave stub — Plan fill real assertion."""
 pytest.skip("Phase Wave stub — Plan fill real assertion")
