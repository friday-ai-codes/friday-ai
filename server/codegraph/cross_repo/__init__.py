"""跨仓 API join 模块 —— initial implementation。

包含：
- path_normalizer: URL 路径归一化（work item）
- confidence: match_confidence 评分（work item）
- join_service: offline join 服务（work item）
- incremental: 增量更新（work item）
"""
