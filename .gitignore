# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
# General
*.log
node_modules
.env
.env.back
.env.*
!.env.example
.logs/

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
.venv/
venv/

# Python tools
.mypy_cache/
.pytest_cache/
.ruff_cache/
.coverage
coverage.xml
htmlcov/

# Database
*.db
*.sqlite
*.sqlite3

# Runtime data
.superpowers/
server/data/
server/data-test/
server/.coverage
playwright-report/
test-results/
coverage/

# VitePress
docs/.vitepress/dist/
mcp/dist/
docs/.vitepress/cache/

# GSD 本地 worktree（开发者机器上的临时 worktree，不应进仓库）。
# 历史上 .claude/worktrees/v0.10-audit 被误当 gitlink 提交且不在 .gitmodules 里，
# 导致 CI 一旦 checkout submodules 就 fatal: No url found for submodule path。
.claude/worktrees/

# Go 运行器本地编译产物（48MB Mach-O，由 runner/Makefile 构建；二进制绝不进 git 历史）
runner/friday-runner
